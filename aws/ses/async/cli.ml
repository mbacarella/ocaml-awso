(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let clone_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and originalRuleSetName =
         flag "original-rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.clone_receipt_rule_set
           (Values.CloneReceiptRuleSetRequest.make ~ruleSetName
              ~originalRuleSetName ())
           (Some Values.CloneReceiptRuleSetResponse.to_json)
           (Some Values.CloneReceiptRuleSetResponse.error_to_json)])
let create_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSet =
         flag "configuration-set" (required json_arg)
           ~doc:"JSON ConfigurationSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set
           (Values.CreateConfigurationSetRequest.make
              ~configurationSet:(Values.ConfigurationSet.of_json
                                   configurationSet) ())
           (Some Values.CreateConfigurationSetResponse.to_json)
           (Some Values.CreateConfigurationSetResponse.error_to_json)])
let create_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestination =
         flag "event-destination" (required json_arg)
           ~doc:"JSON EventDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set_event_destination
           (Values.CreateConfigurationSetEventDestinationRequest.make
              ~configurationSetName
              ~eventDestination:(Values.EventDestination.of_json
                                   eventDestination) ())
           (Some
              Values.CreateConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.CreateConfigurationSetEventDestinationResponse.error_to_json)])
let create_configuration_set_tracking_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and trackingOptions =
         flag "tracking-options" (required json_arg)
           ~doc:"JSON TrackingOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set_tracking_options
           (Values.CreateConfigurationSetTrackingOptionsRequest.make
              ~configurationSetName
              ~trackingOptions:(Values.TrackingOptions.of_json
                                  trackingOptions) ())
           (Some Values.CreateConfigurationSetTrackingOptionsResponse.to_json)
           (Some
              Values.CreateConfigurationSetTrackingOptionsResponse.error_to_json)])
let create_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName"
       and fromEmailAddress =
         flag "from-email-address" (required string)
           ~doc:"STRING FromAddress"
       and templateSubject =
         flag "template-subject" (required string) ~doc:"STRING Subject"
       and templateContent =
         flag "template-content" (required string)
           ~doc:"STRING TemplateContent"
       and successRedirectionURL =
         flag "success-redirection-u-r-l" (required string)
           ~doc:"STRING SuccessRedirectionURL"
       and failureRedirectionURL =
         flag "failure-redirection-u-r-l" (required string)
           ~doc:"STRING FailureRedirectionURL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_verification_email_template
           (Values.CreateCustomVerificationEmailTemplateRequest.make
              ~templateName ~fromEmailAddress ~templateSubject
              ~templateContent ~successRedirectionURL ~failureRedirectionURL
              ()) None None])
let create_receipt_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (required json_arg) ~doc:"JSON ReceiptFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_receipt_filter
           (Values.CreateReceiptFilterRequest.make
              ~filter:(Values.ReceiptFilter.of_json filter) ())
           (Some Values.CreateReceiptFilterResponse.to_json)
           (Some Values.CreateReceiptFilterResponse.error_to_json)])
let create_receipt_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and after =
         flag "after" (optional string) ~doc:"STRING ReceiptRuleName"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and rule = flag "rule" (required json_arg) ~doc:"JSON ReceiptRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_receipt_rule
           (Values.CreateReceiptRuleRequest.make ?after ~ruleSetName
              ~rule:(Values.ReceiptRule.of_json rule) ())
           (Some Values.CreateReceiptRuleResponse.to_json)
           (Some Values.CreateReceiptRuleResponse.error_to_json)])
let create_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_receipt_rule_set
           (Values.CreateReceiptRuleSetRequest.make ~ruleSetName ())
           (Some Values.CreateReceiptRuleSetResponse.to_json)
           (Some Values.CreateReceiptRuleSetResponse.error_to_json)])
let create_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and template =
         flag "template" (required json_arg) ~doc:"JSON Template" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template
           (Values.CreateTemplateRequest.make
              ~template:(Values.Template.of_json template) ())
           (Some Values.CreateTemplateResponse.to_json)
           (Some Values.CreateTemplateResponse.error_to_json)])
let delete_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set
           (Values.DeleteConfigurationSetRequest.make ~configurationSetName
              ()) (Some Values.DeleteConfigurationSetResponse.to_json)
           (Some Values.DeleteConfigurationSetResponse.error_to_json)])
let delete_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set_event_destination
           (Values.DeleteConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName ())
           (Some
              Values.DeleteConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.DeleteConfigurationSetEventDestinationResponse.error_to_json)])
let delete_configuration_set_tracking_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set_tracking_options
           (Values.DeleteConfigurationSetTrackingOptionsRequest.make
              ~configurationSetName ())
           (Some Values.DeleteConfigurationSetTrackingOptionsResponse.to_json)
           (Some
              Values.DeleteConfigurationSetTrackingOptionsResponse.error_to_json)])
let delete_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_verification_email_template
           (Values.DeleteCustomVerificationEmailTemplateRequest.make
              ~templateName ()) None None])
let delete_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity
           (Values.DeleteIdentityRequest.make ~identity ())
           (Some Values.DeleteIdentityResponse.to_json)
           (Some Values.DeleteIdentityResponse.error_to_json)])
let delete_identity_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_policy
           (Values.DeleteIdentityPolicyRequest.make ~identity ~policyName ())
           (Some Values.DeleteIdentityPolicyResponse.to_json)
           (Some Values.DeleteIdentityPolicyResponse.error_to_json)])
let delete_receipt_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterName =
         flag "filter-name" (required string) ~doc:"STRING ReceiptFilterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_receipt_filter
           (Values.DeleteReceiptFilterRequest.make ~filterName ())
           (Some Values.DeleteReceiptFilterResponse.to_json)
           (Some Values.DeleteReceiptFilterResponse.error_to_json)])
let delete_receipt_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING ReceiptRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_receipt_rule
           (Values.DeleteReceiptRuleRequest.make ~ruleSetName ~ruleName ())
           (Some Values.DeleteReceiptRuleResponse.to_json)
           (Some Values.DeleteReceiptRuleResponse.error_to_json)])
let delete_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_receipt_rule_set
           (Values.DeleteReceiptRuleSetRequest.make ~ruleSetName ())
           (Some Values.DeleteReceiptRuleSetResponse.to_json)
           (Some Values.DeleteReceiptRuleSetResponse.error_to_json)])
let delete_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template
           (Values.DeleteTemplateRequest.make ~templateName ())
           (Some Values.DeleteTemplateResponse.to_json)
           (Some Values.DeleteTemplateResponse.error_to_json)])
let delete_verified_email_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_verified_email_address
           (Values.DeleteVerifiedEmailAddressRequest.make ~emailAddress ())
           None None])
let describe_active_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_active_receipt_rule_set
           (Values.DescribeActiveReceiptRuleSetRequest.make ())
           (Some Values.DescribeActiveReceiptRuleSetResponse.to_json)
           (Some Values.DescribeActiveReceiptRuleSetResponse.error_to_json)])
let describe_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetAttributeNames =
         flag "configuration-set-attribute-names" (optional json_arg)
           ~doc:"JSON ConfigurationSetAttributeList"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration_set
           (Values.DescribeConfigurationSetRequest.make
              ?configurationSetAttributeNames:(Option.map
                                                 ~f:Values.ConfigurationSetAttributeList.of_json
                                                 configurationSetAttributeNames)
              ~configurationSetName ())
           (Some Values.DescribeConfigurationSetResponse.to_json)
           (Some Values.DescribeConfigurationSetResponse.error_to_json)])
let describe_receipt_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING ReceiptRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_receipt_rule
           (Values.DescribeReceiptRuleRequest.make ~ruleSetName ~ruleName ())
           (Some Values.DescribeReceiptRuleResponse.to_json)
           (Some Values.DescribeReceiptRuleResponse.error_to_json)])
let describe_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_receipt_rule_set
           (Values.DescribeReceiptRuleSetRequest.make ~ruleSetName ())
           (Some Values.DescribeReceiptRuleSetResponse.to_json)
           (Some Values.DescribeReceiptRuleSetResponse.error_to_json)])
let get_account_sending_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_sending_enabled (Fn.id ())
           (Some Values.GetAccountSendingEnabledResponse.to_json)
           (Some Values.GetAccountSendingEnabledResponse.error_to_json)])
let get_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_verification_email_template
           (Values.GetCustomVerificationEmailTemplateRequest.make
              ~templateName ())
           (Some Values.GetCustomVerificationEmailTemplateResponse.to_json)
           (Some
              Values.GetCustomVerificationEmailTemplateResponse.error_to_json)])
let get_identity_dkim_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identities =
         flag "identities" (required json_arg) ~doc:"JSON IdentityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_dkim_attributes
           (Values.GetIdentityDkimAttributesRequest.make
              ~identities:(Values.IdentityList.of_json identities) ())
           (Some Values.GetIdentityDkimAttributesResponse.to_json)
           (Some Values.GetIdentityDkimAttributesResponse.error_to_json)])
let get_identity_mail_from_domain_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identities =
         flag "identities" (required json_arg) ~doc:"JSON IdentityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_mail_from_domain_attributes
           (Values.GetIdentityMailFromDomainAttributesRequest.make
              ~identities:(Values.IdentityList.of_json identities) ())
           (Some Values.GetIdentityMailFromDomainAttributesResponse.to_json)
           (Some
              Values.GetIdentityMailFromDomainAttributesResponse.error_to_json)])
let get_identity_notification_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identities =
         flag "identities" (required json_arg) ~doc:"JSON IdentityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_notification_attributes
           (Values.GetIdentityNotificationAttributesRequest.make
              ~identities:(Values.IdentityList.of_json identities) ())
           (Some Values.GetIdentityNotificationAttributesResponse.to_json)
           (Some
              Values.GetIdentityNotificationAttributesResponse.error_to_json)])
let get_identity_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and policyNames =
         flag "policy-names" (required json_arg) ~doc:"JSON PolicyNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_policies
           (Values.GetIdentityPoliciesRequest.make ~identity
              ~policyNames:(Values.PolicyNameList.of_json policyNames) ())
           (Some Values.GetIdentityPoliciesResponse.to_json)
           (Some Values.GetIdentityPoliciesResponse.error_to_json)])
let get_identity_verification_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identities =
         flag "identities" (required json_arg) ~doc:"JSON IdentityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_verification_attributes
           (Values.GetIdentityVerificationAttributesRequest.make
              ~identities:(Values.IdentityList.of_json identities) ())
           (Some Values.GetIdentityVerificationAttributesResponse.to_json)
           (Some
              Values.GetIdentityVerificationAttributesResponse.error_to_json)])
let get_send_quota =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_send_quota (Fn.id ())
           (Some Values.GetSendQuotaResponse.to_json)
           (Some Values.GetSendQuotaResponse.error_to_json)])
let get_send_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_send_statistics (Fn.id ())
           (Some Values.GetSendStatisticsResponse.to_json)
           (Some Values.GetSendStatisticsResponse.error_to_json)])
let get_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template (Values.GetTemplateRequest.make ~templateName ())
           (Some Values.GetTemplateResponse.to_json)
           (Some Values.GetTemplateResponse.error_to_json)])
let list_configuration_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_sets
           (Values.ListConfigurationSetsRequest.make ?nextToken ?maxItems ())
           (Some Values.ListConfigurationSetsResponse.to_json)
           (Some Values.ListConfigurationSetsResponse.error_to_json)])
let list_custom_verification_email_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_verification_email_templates
           (Values.ListCustomVerificationEmailTemplatesRequest.make
              ?nextToken ?maxResults ())
           (Some Values.ListCustomVerificationEmailTemplatesResponse.to_json)
           (Some
              Values.ListCustomVerificationEmailTemplatesResponse.error_to_json)])
let list_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityType =
         flag "identity-type" (optional json_arg) ~doc:"JSON IdentityType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identities
           (Values.ListIdentitiesRequest.make
              ?identityType:(Option.map ~f:Values.IdentityType.of_json
                               identityType) ?nextToken ?maxItems ())
           (Some Values.ListIdentitiesResponse.to_json)
           (Some Values.ListIdentitiesResponse.error_to_json)])
let list_identity_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_policies
           (Values.ListIdentityPoliciesRequest.make ~identity ())
           (Some Values.ListIdentityPoliciesResponse.to_json)
           (Some Values.ListIdentityPoliciesResponse.error_to_json)])
let list_receipt_filters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_receipt_filters (Values.ListReceiptFiltersRequest.make ())
           (Some Values.ListReceiptFiltersResponse.to_json)
           (Some Values.ListReceiptFiltersResponse.error_to_json)])
let list_receipt_rule_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_receipt_rule_sets
           (Values.ListReceiptRuleSetsRequest.make ?nextToken ())
           (Some Values.ListReceiptRuleSetsResponse.to_json)
           (Some Values.ListReceiptRuleSetsResponse.error_to_json)])
let list_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_templates
           (Values.ListTemplatesRequest.make ?nextToken ?maxItems ())
           (Some Values.ListTemplatesResponse.to_json)
           (Some Values.ListTemplatesResponse.error_to_json)])
let list_verified_email_addresses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_verified_email_addresses (Fn.id ())
           (Some Values.ListVerifiedEmailAddressesResponse.to_json)
           (Some Values.ListVerifiedEmailAddressesResponse.error_to_json)])
let put_configuration_set_delivery_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryOptions =
         flag "delivery-options" (optional json_arg)
           ~doc:"JSON DeliveryOptions"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_delivery_options
           (Values.PutConfigurationSetDeliveryOptionsRequest.make
              ?deliveryOptions:(Option.map ~f:Values.DeliveryOptions.of_json
                                  deliveryOptions) ~configurationSetName ())
           (Some Values.PutConfigurationSetDeliveryOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetDeliveryOptionsResponse.error_to_json)])
let put_identity_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_identity_policy
           (Values.PutIdentityPolicyRequest.make ~identity ~policyName
              ~policy ()) (Some Values.PutIdentityPolicyResponse.to_json)
           (Some Values.PutIdentityPolicyResponse.error_to_json)])
let reorder_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and ruleNames =
         flag "rule-names" (required json_arg)
           ~doc:"JSON ReceiptRuleNamesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reorder_receipt_rule_set
           (Values.ReorderReceiptRuleSetRequest.make ~ruleSetName
              ~ruleNames:(Values.ReceiptRuleNamesList.of_json ruleNames) ())
           (Some Values.ReorderReceiptRuleSetResponse.to_json)
           (Some Values.ReorderReceiptRuleSetResponse.error_to_json)])
let send_bounce =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and explanation =
         flag "explanation" (optional string) ~doc:"STRING Explanation"
       and messageDsn =
         flag "message-dsn" (optional json_arg) ~doc:"JSON MessageDsn"
       and bounceSenderArn =
         flag "bounce-sender-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and originalMessageId =
         flag "original-message-id" (required string) ~doc:"STRING MessageId"
       and bounceSender =
         flag "bounce-sender" (required string) ~doc:"STRING Address"
       and bouncedRecipientInfoList =
         flag "bounced-recipient-info-list" (required json_arg)
           ~doc:"JSON BouncedRecipientInfoList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_bounce
           (Values.SendBounceRequest.make ?explanation
              ?messageDsn:(Option.map ~f:Values.MessageDsn.of_json messageDsn)
              ?bounceSenderArn ~originalMessageId ~bounceSender
              ~bouncedRecipientInfoList:(Values.BouncedRecipientInfoList.of_json
                                           bouncedRecipientInfoList) ())
           (Some Values.SendBounceResponse.to_json)
           (Some Values.SendBounceResponse.error_to_json)])
let send_bulk_templated_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceArn =
         flag "source-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and replyToAddresses =
         flag "reply-to-addresses" (optional json_arg)
           ~doc:"JSON AddressList"
       and returnPath =
         flag "return-path" (optional string) ~doc:"STRING Address"
       and returnPathArn =
         flag "return-path-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and defaultTags =
         flag "default-tags" (optional json_arg) ~doc:"JSON MessageTagList"
       and templateArn =
         flag "template-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and defaultTemplateData =
         flag "default-template-data" (optional string)
           ~doc:"STRING TemplateData"
       and source = flag "source" (required string) ~doc:"STRING Address"
       and template =
         flag "template" (required string) ~doc:"STRING TemplateName"
       and destinations =
         flag "destinations" (required json_arg)
           ~doc:"JSON BulkEmailDestinationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_bulk_templated_email
           (Values.SendBulkTemplatedEmailRequest.make ?sourceArn
              ?replyToAddresses:(Option.map ~f:Values.AddressList.of_json
                                   replyToAddresses) ?returnPath
              ?returnPathArn ?configurationSetName
              ?defaultTags:(Option.map ~f:Values.MessageTagList.of_json
                              defaultTags) ?templateArn ?defaultTemplateData
              ~source ~template
              ~destinations:(Values.BulkEmailDestinationList.of_json
                               destinations) ())
           (Some Values.SendBulkTemplatedEmailResponse.to_json)
           (Some Values.SendBulkTemplatedEmailResponse.error_to_json)])
let send_custom_verification_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING Address"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_custom_verification_email
           (Values.SendCustomVerificationEmailRequest.make
              ?configurationSetName ~emailAddress ~templateName ())
           (Some Values.SendCustomVerificationEmailResponse.to_json)
           (Some Values.SendCustomVerificationEmailResponse.error_to_json)])
let send_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replyToAddresses =
         flag "reply-to-addresses" (optional json_arg)
           ~doc:"JSON AddressList"
       and returnPath =
         flag "return-path" (optional string) ~doc:"STRING Address"
       and sourceArn =
         flag "source-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and returnPathArn =
         flag "return-path-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON MessageTagList"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and source = flag "source" (required string) ~doc:"STRING Address"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON Destination"
       and message = flag "message" (required json_arg) ~doc:"JSON Message" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_email
           (Values.SendEmailRequest.make
              ?replyToAddresses:(Option.map ~f:Values.AddressList.of_json
                                   replyToAddresses) ?returnPath ?sourceArn
              ?returnPathArn
              ?tags:(Option.map ~f:Values.MessageTagList.of_json tags)
              ?configurationSetName ~source
              ~destination:(Values.Destination.of_json destination)
              ~message:(Values.Message.of_json message) ())
           (Some Values.SendEmailResponse.to_json)
           (Some Values.SendEmailResponse.error_to_json)])
let send_raw_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and source = flag "source" (optional string) ~doc:"STRING Address"
       and destinations =
         flag "destinations" (optional json_arg) ~doc:"JSON AddressList"
       and fromArn =
         flag "from-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and sourceArn =
         flag "source-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and returnPathArn =
         flag "return-path-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON MessageTagList"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and rawMessage =
         flag "raw-message" (required json_arg) ~doc:"JSON RawMessage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_raw_email
           (Values.SendRawEmailRequest.make ?source
              ?destinations:(Option.map ~f:Values.AddressList.of_json
                               destinations) ?fromArn ?sourceArn
              ?returnPathArn
              ?tags:(Option.map ~f:Values.MessageTagList.of_json tags)
              ?configurationSetName
              ~rawMessage:(Values.RawMessage.of_json rawMessage) ())
           (Some Values.SendRawEmailResponse.to_json)
           (Some Values.SendRawEmailResponse.error_to_json)])
let send_templated_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replyToAddresses =
         flag "reply-to-addresses" (optional json_arg)
           ~doc:"JSON AddressList"
       and returnPath =
         flag "return-path" (optional string) ~doc:"STRING Address"
       and sourceArn =
         flag "source-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and returnPathArn =
         flag "return-path-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON MessageTagList"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and templateArn =
         flag "template-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and source = flag "source" (required string) ~doc:"STRING Address"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON Destination"
       and template =
         flag "template" (required string) ~doc:"STRING TemplateName"
       and templateData =
         flag "template-data" (required string) ~doc:"STRING TemplateData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_templated_email
           (Values.SendTemplatedEmailRequest.make
              ?replyToAddresses:(Option.map ~f:Values.AddressList.of_json
                                   replyToAddresses) ?returnPath ?sourceArn
              ?returnPathArn
              ?tags:(Option.map ~f:Values.MessageTagList.of_json tags)
              ?configurationSetName ?templateArn ~source
              ~destination:(Values.Destination.of_json destination) ~template
              ~templateData ())
           (Some Values.SendTemplatedEmailResponse.to_json)
           (Some Values.SendTemplatedEmailResponse.error_to_json)])
let set_active_receipt_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (optional string)
           ~doc:"STRING ReceiptRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_active_receipt_rule_set
           (Values.SetActiveReceiptRuleSetRequest.make ?ruleSetName ())
           (Some Values.SetActiveReceiptRuleSetResponse.to_json)
           (Some Values.SetActiveReceiptRuleSetResponse.error_to_json)])
let set_identity_dkim_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and dkimEnabled =
         flag "dkim-enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_dkim_enabled
           (Values.SetIdentityDkimEnabledRequest.make ~identity ~dkimEnabled
              ()) (Some Values.SetIdentityDkimEnabledResponse.to_json)
           (Some Values.SetIdentityDkimEnabledResponse.error_to_json)])
let set_identity_feedback_forwarding_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and forwardingEnabled =
         flag "forwarding-enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_feedback_forwarding_enabled
           (Values.SetIdentityFeedbackForwardingEnabledRequest.make ~identity
              ~forwardingEnabled ())
           (Some Values.SetIdentityFeedbackForwardingEnabledResponse.to_json)
           (Some
              Values.SetIdentityFeedbackForwardingEnabledResponse.error_to_json)])
let set_identity_headers_in_notifications_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and notificationType =
         flag "notification-type" (required json_arg)
           ~doc:"JSON NotificationType"
       and enabled = flag "enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_headers_in_notifications_enabled
           (Values.SetIdentityHeadersInNotificationsEnabledRequest.make
              ~identity
              ~notificationType:(Values.NotificationType.of_json
                                   notificationType) ~enabled ())
           (Some
              Values.SetIdentityHeadersInNotificationsEnabledResponse.to_json)
           (Some
              Values.SetIdentityHeadersInNotificationsEnabledResponse.error_to_json)])
let set_identity_mail_from_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mailFromDomain =
         flag "mail-from-domain" (optional string)
           ~doc:"STRING MailFromDomainName"
       and behaviorOnMXFailure =
         flag "behavior-on-m-x-failure" (optional json_arg)
           ~doc:"JSON BehaviorOnMXFailure"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_mail_from_domain
           (Values.SetIdentityMailFromDomainRequest.make ?mailFromDomain
              ?behaviorOnMXFailure:(Option.map
                                      ~f:Values.BehaviorOnMXFailure.of_json
                                      behaviorOnMXFailure) ~identity ())
           (Some Values.SetIdentityMailFromDomainResponse.to_json)
           (Some Values.SetIdentityMailFromDomainResponse.error_to_json)])
let set_identity_notification_topic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snsTopic =
         flag "sns-topic" (optional string) ~doc:"STRING NotificationTopic"
       and identity =
         flag "identity" (required string) ~doc:"STRING Identity"
       and notificationType =
         flag "notification-type" (required json_arg)
           ~doc:"JSON NotificationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_notification_topic
           (Values.SetIdentityNotificationTopicRequest.make ?snsTopic
              ~identity
              ~notificationType:(Values.NotificationType.of_json
                                   notificationType) ())
           (Some Values.SetIdentityNotificationTopicResponse.to_json)
           (Some Values.SetIdentityNotificationTopicResponse.error_to_json)])
let set_receipt_rule_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and after =
         flag "after" (optional string) ~doc:"STRING ReceiptRuleName"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING ReceiptRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_receipt_rule_position
           (Values.SetReceiptRulePositionRequest.make ?after ~ruleSetName
              ~ruleName ())
           (Some Values.SetReceiptRulePositionResponse.to_json)
           (Some Values.SetReceiptRulePositionResponse.error_to_json)])
let test_render_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName"
       and templateData =
         flag "template-data" (required string) ~doc:"STRING TemplateData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_render_template
           (Values.TestRenderTemplateRequest.make ~templateName ~templateData
              ()) (Some Values.TestRenderTemplateResponse.to_json)
           (Some Values.TestRenderTemplateResponse.error_to_json)])
let update_account_sending_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_sending_enabled
           (Values.UpdateAccountSendingEnabledRequest.make ?enabled ()) None
           None])
let update_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestination =
         flag "event-destination" (required json_arg)
           ~doc:"JSON EventDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_set_event_destination
           (Values.UpdateConfigurationSetEventDestinationRequest.make
              ~configurationSetName
              ~eventDestination:(Values.EventDestination.of_json
                                   eventDestination) ())
           (Some
              Values.UpdateConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.UpdateConfigurationSetEventDestinationResponse.error_to_json)])
let update_configuration_set_reputation_metrics_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and enabled = flag "enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_set_reputation_metrics_enabled
           (Values.UpdateConfigurationSetReputationMetricsEnabledRequest.make
              ~configurationSetName ~enabled ()) None None])
let update_configuration_set_sending_enabled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and enabled = flag "enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_set_sending_enabled
           (Values.UpdateConfigurationSetSendingEnabledRequest.make
              ~configurationSetName ~enabled ()) None None])
let update_configuration_set_tracking_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and trackingOptions =
         flag "tracking-options" (required json_arg)
           ~doc:"JSON TrackingOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_set_tracking_options
           (Values.UpdateConfigurationSetTrackingOptionsRequest.make
              ~configurationSetName
              ~trackingOptions:(Values.TrackingOptions.of_json
                                  trackingOptions) ())
           (Some Values.UpdateConfigurationSetTrackingOptionsResponse.to_json)
           (Some
              Values.UpdateConfigurationSetTrackingOptionsResponse.error_to_json)])
let update_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fromEmailAddress =
         flag "from-email-address" (optional string)
           ~doc:"STRING FromAddress"
       and templateSubject =
         flag "template-subject" (optional string) ~doc:"STRING Subject"
       and templateContent =
         flag "template-content" (optional string)
           ~doc:"STRING TemplateContent"
       and successRedirectionURL =
         flag "success-redirection-u-r-l" (optional string)
           ~doc:"STRING SuccessRedirectionURL"
       and failureRedirectionURL =
         flag "failure-redirection-u-r-l" (optional string)
           ~doc:"STRING FailureRedirectionURL"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_verification_email_template
           (Values.UpdateCustomVerificationEmailTemplateRequest.make
              ?fromEmailAddress ?templateSubject ?templateContent
              ?successRedirectionURL ?failureRedirectionURL ~templateName ())
           None None])
let update_receipt_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING ReceiptRuleSetName"
       and rule = flag "rule" (required json_arg) ~doc:"JSON ReceiptRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_receipt_rule
           (Values.UpdateReceiptRuleRequest.make ~ruleSetName
              ~rule:(Values.ReceiptRule.of_json rule) ())
           (Some Values.UpdateReceiptRuleResponse.to_json)
           (Some Values.UpdateReceiptRuleResponse.error_to_json)])
let update_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and template =
         flag "template" (required json_arg) ~doc:"JSON Template" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template
           (Values.UpdateTemplateRequest.make
              ~template:(Values.Template.of_json template) ())
           (Some Values.UpdateTemplateResponse.to_json)
           (Some Values.UpdateTemplateResponse.error_to_json)])
let verify_domain_dkim =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING Domain" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_domain_dkim
           (Values.VerifyDomainDkimRequest.make ~domain ())
           (Some Values.VerifyDomainDkimResponse.to_json)
           (Some Values.VerifyDomainDkimResponse.error_to_json)])
let verify_domain_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING Domain" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_domain_identity
           (Values.VerifyDomainIdentityRequest.make ~domain ())
           (Some Values.VerifyDomainIdentityResponse.to_json)
           (Some Values.VerifyDomainIdentityResponse.error_to_json)])
let verify_email_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_email_address
           (Values.VerifyEmailAddressRequest.make ~emailAddress ()) None None])
let verify_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_email_identity
           (Values.VerifyEmailIdentityRequest.make ~emailAddress ())
           (Some Values.VerifyEmailIdentityResponse.to_json)
           (Some Values.VerifyEmailIdentityResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("clone-receipt-rule-set", clone_receipt_rule_set);
    ("create-configuration-set", create_configuration_set);
    ("create-configuration-set-event-destination",
      create_configuration_set_event_destination);
    ("create-configuration-set-tracking-options",
      create_configuration_set_tracking_options);
    ("create-custom-verification-email-template",
      create_custom_verification_email_template);
    ("create-receipt-filter", create_receipt_filter);
    ("create-receipt-rule", create_receipt_rule);
    ("create-receipt-rule-set", create_receipt_rule_set);
    ("create-template", create_template);
    ("delete-configuration-set", delete_configuration_set);
    ("delete-configuration-set-event-destination",
      delete_configuration_set_event_destination);
    ("delete-configuration-set-tracking-options",
      delete_configuration_set_tracking_options);
    ("delete-custom-verification-email-template",
      delete_custom_verification_email_template);
    ("delete-identity", delete_identity);
    ("delete-identity-policy", delete_identity_policy);
    ("delete-receipt-filter", delete_receipt_filter);
    ("delete-receipt-rule", delete_receipt_rule);
    ("delete-receipt-rule-set", delete_receipt_rule_set);
    ("delete-template", delete_template);
    ("delete-verified-email-address", delete_verified_email_address);
    ("describe-active-receipt-rule-set", describe_active_receipt_rule_set);
    ("describe-configuration-set", describe_configuration_set);
    ("describe-receipt-rule", describe_receipt_rule);
    ("describe-receipt-rule-set", describe_receipt_rule_set);
    ("get-account-sending-enabled", get_account_sending_enabled);
    ("get-custom-verification-email-template",
      get_custom_verification_email_template);
    ("get-identity-dkim-attributes", get_identity_dkim_attributes);
    ("get-identity-mail-from-domain-attributes",
      get_identity_mail_from_domain_attributes);
    ("get-identity-notification-attributes",
      get_identity_notification_attributes);
    ("get-identity-policies", get_identity_policies);
    ("get-identity-verification-attributes",
      get_identity_verification_attributes);
    ("get-send-quota", get_send_quota);
    ("get-send-statistics", get_send_statistics);
    ("get-template", get_template);
    ("list-configuration-sets", list_configuration_sets);
    ("list-custom-verification-email-templates",
      list_custom_verification_email_templates);
    ("list-identities", list_identities);
    ("list-identity-policies", list_identity_policies);
    ("list-receipt-filters", list_receipt_filters);
    ("list-receipt-rule-sets", list_receipt_rule_sets);
    ("list-templates", list_templates);
    ("list-verified-email-addresses", list_verified_email_addresses);
    ("put-configuration-set-delivery-options",
      put_configuration_set_delivery_options);
    ("put-identity-policy", put_identity_policy);
    ("reorder-receipt-rule-set", reorder_receipt_rule_set);
    ("send-bounce", send_bounce);
    ("send-bulk-templated-email", send_bulk_templated_email);
    ("send-custom-verification-email", send_custom_verification_email);
    ("send-email", send_email);
    ("send-raw-email", send_raw_email);
    ("send-templated-email", send_templated_email);
    ("set-active-receipt-rule-set", set_active_receipt_rule_set);
    ("set-identity-dkim-enabled", set_identity_dkim_enabled);
    ("set-identity-feedback-forwarding-enabled",
      set_identity_feedback_forwarding_enabled);
    ("set-identity-headers-in-notifications-enabled",
      set_identity_headers_in_notifications_enabled);
    ("set-identity-mail-from-domain", set_identity_mail_from_domain);
    ("set-identity-notification-topic", set_identity_notification_topic);
    ("set-receipt-rule-position", set_receipt_rule_position);
    ("test-render-template", test_render_template);
    ("update-account-sending-enabled", update_account_sending_enabled);
    ("update-configuration-set-event-destination",
      update_configuration_set_event_destination);
    ("update-configuration-set-reputation-metrics-enabled",
      update_configuration_set_reputation_metrics_enabled);
    ("update-configuration-set-sending-enabled",
      update_configuration_set_sending_enabled);
    ("update-configuration-set-tracking-options",
      update_configuration_set_tracking_options);
    ("update-custom-verification-email-template",
      update_custom_verification_email_template);
    ("update-receipt-rule", update_receipt_rule);
    ("update-template", update_template);
    ("verify-domain-dkim", verify_domain_dkim);
    ("verify-domain-identity", verify_domain_identity);
    ("verify-email-address", verify_email_address);
    ("verify-email-identity", verify_email_identity)]
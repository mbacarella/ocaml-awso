(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.ses
let apiVersion = "2010-12-01"
let endpointPrefix = "email"
let serviceFullName = "Amazon Simple Email Service"
let signatureVersion = "v4"
let protocol = "query"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon SES"
let xmlNamespace = "http://ses.amazonaws.com/doc/2010-12-01/"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ExtensionFieldName =
  struct
    type nonrec t = string
    let context_ = "ExtensionFieldName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExtensionFieldName" j
    let to_json = simple_to_json to_value
  end
module ExtensionFieldValue =
  struct
    type nonrec t = string
    let context_ = "ExtensionFieldValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExtensionFieldValue" j
    let to_json = simple_to_json to_value
  end
module HeaderName =
  struct
    type nonrec t = string
    let context_ = "HeaderName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HeaderName" j
    let to_json = simple_to_json to_value
  end
module HeaderValue =
  struct
    type nonrec t = string
    let context_ = "HeaderValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HeaderValue" j
    let to_json = simple_to_json to_value
  end
module Address =
  struct
    type nonrec t = string
    let context_ = "Address"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Address" j
    let to_json = simple_to_json to_value
  end
module AmazonResourceName =
  struct
    type nonrec t = string
    let context_ = "AmazonResourceName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AmazonResourceName" j
    let to_json = simple_to_json to_value
  end
module BounceMessage =
  struct
    type nonrec t = string
    let context_ = "BounceMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BounceMessage" j
    let to_json = simple_to_json to_value
  end
module BounceSmtpReplyCode =
  struct
    type nonrec t = string
    let context_ = "BounceSmtpReplyCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BounceSmtpReplyCode" j
    let to_json = simple_to_json to_value
  end
module BounceStatusCode =
  struct
    type nonrec t = string
    let context_ = "BounceStatusCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BounceStatusCode" j
    let to_json = simple_to_json to_value
  end
module InvocationType =
  struct
    type nonrec t =
      | Event 
      | RequestResponse 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Event -> "Event"
      | RequestResponse -> "RequestResponse"
      | Non_static_id s -> s
    let of_string =
      function
      | "Event" -> Event
      | "RequestResponse" -> RequestResponse
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InvocationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InvocationType" j)
    let to_json = simple_to_json to_value
  end
module S3BucketName =
  struct
    type nonrec t = string
    let context_ = "S3BucketName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3BucketName" j
    let to_json = simple_to_json to_value
  end
module S3KeyPrefix =
  struct
    type nonrec t = string
    let context_ = "S3KeyPrefix"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3KeyPrefix" j
    let to_json = simple_to_json to_value
  end
module SNSActionEncoding =
  struct
    type nonrec t =
      | UTF_8 
      | Base64 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | UTF_8 -> "UTF-8" | Base64 -> "Base64" | Non_static_id s -> s
    let of_string =
      function | "UTF-8" -> UTF_8 | "Base64" -> Base64 | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SNSActionEncoding" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SNSActionEncoding" j)
    let to_json = simple_to_json to_value
  end
module StopScope =
  struct
    type nonrec t =
      | RuleSet 
      | Non_static_id of string 
    let make i = i
    let to_string = function | RuleSet -> "RuleSet" | Non_static_id s -> s
    let of_string = function | "RuleSet" -> RuleSet | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StopScope" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StopScope" j)
    let to_json = simple_to_json to_value
  end
module DefaultDimensionValue =
  struct
    type nonrec t = string
    let context_ = "DefaultDimensionValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DefaultDimensionValue" j
    let to_json = simple_to_json to_value
  end
module DimensionName =
  struct
    type nonrec t = string
    let context_ = "DimensionName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DimensionName" j
    let to_json = simple_to_json to_value
  end
module DimensionValueSource =
  struct
    type nonrec t =
      | MessageTag 
      | EmailHeader 
      | LinkTag 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MessageTag -> "messageTag"
      | EmailHeader -> "emailHeader"
      | LinkTag -> "linkTag"
      | Non_static_id s -> s
    let of_string =
      function
      | "messageTag" -> MessageTag
      | "emailHeader" -> EmailHeader
      | "linkTag" -> LinkTag
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DimensionValueSource" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DimensionValueSource" j)
    let to_json = simple_to_json to_value
  end
module MessageTagName =
  struct
    type nonrec t = string
    let context_ = "MessageTagName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MessageTagName" j
    let to_json = simple_to_json to_value
  end
module MessageTagValue =
  struct
    type nonrec t = string
    let context_ = "MessageTagValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MessageTagValue" j
    let to_json = simple_to_json to_value
  end
module ExtensionField =
  struct
    type nonrec t =
      {
      name: ExtensionFieldName.t
        [@ocaml.doc
          "The name of the header to add. Must be between 1 and 50 characters, inclusive, and consist of alphanumeric (a-z, A-Z, 0-9) characters and dashes only."];
      value: ExtensionFieldValue.t
        [@ocaml.doc
          "The value of the header to add. Must be less than 2048 characters, and must not contain newline characters (\"\\r\" or \"\\n\")."]}
    let context_ = "ExtensionField"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Some (ExtensionFieldName.to_value x.name)));
        ("Value", (Some (ExtensionFieldValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        ExtensionFieldValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let name =
        ExtensionFieldName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~value ~name ()
    let of_json json =
      let value = field_map_exn json "Value" ExtensionFieldValue.of_json in
      let name = field_map_exn json "Name" ExtensionFieldName.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Additional X-headers to include in the Delivery Status Notification (DSN) when an email that Amazon SES receives on your behalf bounces. For information about receiving email through Amazon SES, see the Amazon SES Developer Guide."]
module AddHeaderAction =
  struct
    type nonrec t =
      {
      headerName: HeaderName.t
        [@ocaml.doc
          "The name of the header to add. Must be between 1 and 50 characters, inclusive, and consist of alphanumeric (a-z, A-Z, 0-9) characters and dashes only."];
      headerValue: HeaderValue.t
        [@ocaml.doc
          "Must be less than 2048 characters, and must not contain newline characters (\"\\r\" or \"\\n\")."]}
    let context_ = "AddHeaderAction"
    let make ~headerName =
      fun ~headerValue -> fun () -> { headerName; headerValue }
    let to_value x =
      structure_to_value
        [("HeaderName", (Some (HeaderName.to_value x.headerName)));
        ("HeaderValue", (Some (HeaderValue.to_value x.headerValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let headerValue =
        HeaderValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HeaderValue") in
      let headerName =
        HeaderName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HeaderName") in
      make ~headerValue ~headerName ()
    let of_json json =
      let headerValue = field_map_exn json "HeaderValue" HeaderValue.of_json in
      let headerName = field_map_exn json "HeaderName" HeaderName.of_json in
      make ~headerValue ~headerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action adds a header to the received email. For information about adding a header using a receipt rule, see the Amazon SES Developer Guide."]
module BounceAction =
  struct
    type nonrec t =
      {
      topicArn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic to notify when the bounce action is taken. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."];
      smtpReplyCode: BounceSmtpReplyCode.t
        [@ocaml.doc "The SMTP reply code, as defined by RFC 5321."];
      statusCode: BounceStatusCode.t option
        [@ocaml.doc "The SMTP enhanced status code, as defined by RFC 3463."];
      message: BounceMessage.t
        [@ocaml.doc "Human-readable text to include in the bounce message."];
      sender: Address.t
        [@ocaml.doc
          "The email address of the sender of the bounced email. This is the address from which the bounce message will be sent."]}
    let context_ = "BounceAction"
    let make ?topicArn =
      fun ?statusCode ->
        fun ~smtpReplyCode ->
          fun ~message ->
            fun ~sender ->
              fun () ->
                { topicArn; statusCode; smtpReplyCode; message; sender }
    let to_value x =
      structure_to_value
        [("TopicArn", (Option.map x.topicArn ~f:AmazonResourceName.to_value));
        ("SmtpReplyCode",
          (Some (BounceSmtpReplyCode.to_value x.smtpReplyCode)));
        ("StatusCode",
          (Option.map x.statusCode ~f:BounceStatusCode.to_value));
        ("Message", (Some (BounceMessage.to_value x.message)));
        ("Sender", (Some (Address.to_value x.sender)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sender =
        Address.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Sender") in
      let message =
        BounceMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      let statusCode =
        (Option.map ~f:BounceStatusCode.of_xml)
          (Xml.child xml_arg0 "StatusCode") in
      let smtpReplyCode =
        BounceSmtpReplyCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SmtpReplyCode") in
      let topicArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TopicArn") in
      make ~sender ~message ?statusCode ~smtpReplyCode ?topicArn ()
    let of_json json =
      let sender = field_map_exn json "Sender" Address.of_json in
      let message = field_map_exn json "Message" BounceMessage.of_json in
      let statusCode = field_map json "StatusCode" BounceStatusCode.of_json in
      let smtpReplyCode =
        field_map_exn json "SmtpReplyCode" BounceSmtpReplyCode.of_json in
      let topicArn = field_map json "TopicArn" AmazonResourceName.of_json in
      make ~sender ~message ?statusCode ~smtpReplyCode ?topicArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action rejects the received email by returning a bounce response to the sender and, optionally, publishes a notification to Amazon Simple Notification Service (Amazon SNS). For information about sending a bounce message in response to a received email, see the Amazon SES Developer Guide."]
module LambdaAction =
  struct
    type nonrec t =
      {
      topicArn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic to notify when the Lambda action is taken. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."];
      functionArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the AWS Lambda function. An example of an AWS Lambda function ARN is arn:aws:lambda:us-west-2:account-id:function:MyFunction. For more information about AWS Lambda, see the AWS Lambda Developer Guide."];
      invocationType: InvocationType.t option
        [@ocaml.doc
          "The invocation type of the AWS Lambda function. An invocation type of RequestResponse means that the execution of the function will immediately result in a response, and a value of Event means that the function will be invoked asynchronously. The default value is Event. For information about AWS Lambda invocation types, see the AWS Lambda Developer Guide. There is a 30-second timeout on RequestResponse invocations. You should use Event invocation in most cases. Use RequestResponse only when you want to make a mail flow decision, such as whether to stop the receipt rule or the receipt rule set."]}
    let context_ = "LambdaAction"
    let make ?topicArn =
      fun ?invocationType ->
        fun ~functionArn ->
          fun () -> { topicArn; invocationType; functionArn }
    let to_value x =
      structure_to_value
        [("TopicArn", (Option.map x.topicArn ~f:AmazonResourceName.to_value));
        ("FunctionArn", (Some (AmazonResourceName.to_value x.functionArn)));
        ("InvocationType",
          (Option.map x.invocationType ~f:InvocationType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationType =
        (Option.map ~f:InvocationType.of_xml)
          (Xml.child xml_arg0 "InvocationType") in
      let functionArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FunctionArn") in
      let topicArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TopicArn") in
      make ?invocationType ~functionArn ?topicArn ()
    let of_json json =
      let invocationType =
        field_map json "InvocationType" InvocationType.of_json in
      let functionArn =
        field_map_exn json "FunctionArn" AmazonResourceName.of_json in
      let topicArn = field_map json "TopicArn" AmazonResourceName.of_json in
      make ?invocationType ~functionArn ?topicArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action calls an AWS Lambda function and, optionally, publishes a notification to Amazon Simple Notification Service (Amazon SNS). To enable Amazon SES to call your AWS Lambda function or to publish to an Amazon SNS topic of another account, Amazon SES must have permission to access those resources. For information about giving permissions, see the Amazon SES Developer Guide. For information about using AWS Lambda actions in receipt rules, see the Amazon SES Developer Guide."]
module S3Action =
  struct
    type nonrec t =
      {
      topicArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the Amazon SNS topic to notify when the message is saved to the Amazon S3 bucket. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."];
      bucketName: S3BucketName.t
        [@ocaml.doc
          "The name of the Amazon S3 bucket that incoming email will be saved to."];
      objectKeyPrefix: S3KeyPrefix.t option
        [@ocaml.doc
          "The key prefix of the Amazon S3 bucket. The key prefix is similar to a directory name that enables you to store similar data under the same directory in a bucket."];
      kmsKeyArn: AmazonResourceName.t option
        [@ocaml.doc
          "The customer master key that Amazon SES should use to encrypt your emails before saving them to the Amazon S3 bucket. You can use the default master key or a custom master key you created in AWS KMS as follows: To use the default master key, provide an ARN in the form of arn:aws:kms:REGION:ACCOUNT-ID-WITHOUT-HYPHENS:alias/aws/ses. For example, if your AWS account ID is 123456789012 and you want to use the default master key in the US West (Oregon) region, the ARN of the default master key would be arn:aws:kms:us-west-2:123456789012:alias/aws/ses. If you use the default master key, you don't need to perform any extra steps to give Amazon SES permission to use the key. To use a custom master key you created in AWS KMS, provide the ARN of the master key and ensure that you add a statement to your key's policy to give Amazon SES permission to use it. For more information about giving permissions, see the Amazon SES Developer Guide. For more information about key policies, see the AWS KMS Developer Guide. If you do not specify a master key, Amazon SES will not encrypt your emails. Your mail is encrypted by Amazon SES using the Amazon S3 encryption client before the mail is submitted to Amazon S3 for storage. It is not encrypted using Amazon S3 server-side encryption. This means that you must use the Amazon S3 encryption client to decrypt the email after retrieving it from Amazon S3, as the service has no access to use your AWS KMS keys for decryption. This encryption client is currently available with the AWS SDK for Java and AWS SDK for Ruby only. For more information about client-side encryption using AWS KMS master keys, see the Amazon S3 Developer Guide."]}
    let context_ = "S3Action"
    let make ?topicArn =
      fun ?objectKeyPrefix ->
        fun ?kmsKeyArn ->
          fun ~bucketName ->
            fun () -> { topicArn; objectKeyPrefix; kmsKeyArn; bucketName }
    let to_value x =
      structure_to_value
        [("TopicArn", (Option.map x.topicArn ~f:AmazonResourceName.to_value));
        ("BucketName", (Some (S3BucketName.to_value x.bucketName)));
        ("ObjectKeyPrefix",
          (Option.map x.objectKeyPrefix ~f:S3KeyPrefix.to_value));
        ("KmsKeyArn",
          (Option.map x.kmsKeyArn ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "KmsKeyArn") in
      let objectKeyPrefix =
        (Option.map ~f:S3KeyPrefix.of_xml)
          (Xml.child xml_arg0 "ObjectKeyPrefix") in
      let bucketName =
        S3BucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BucketName") in
      let topicArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TopicArn") in
      make ?kmsKeyArn ?objectKeyPrefix ~bucketName ?topicArn ()
    let of_json json =
      let kmsKeyArn = field_map json "KmsKeyArn" AmazonResourceName.of_json in
      let objectKeyPrefix =
        field_map json "ObjectKeyPrefix" S3KeyPrefix.of_json in
      let bucketName = field_map_exn json "BucketName" S3BucketName.of_json in
      let topicArn = field_map json "TopicArn" AmazonResourceName.of_json in
      make ?kmsKeyArn ?objectKeyPrefix ~bucketName ?topicArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action saves the received message to an Amazon Simple Storage Service (Amazon S3) bucket and, optionally, publishes a notification to Amazon Simple Notification Service (Amazon SNS). To enable Amazon SES to write emails to your Amazon S3 bucket, use an AWS KMS key to encrypt your emails, or publish to an Amazon SNS topic of another account, Amazon SES must have permission to access those resources. For information about giving permissions, see the Amazon SES Developer Guide. When you save your emails to an Amazon S3 bucket, the maximum email size (including headers) is 30 MB. Emails larger than that will bounce. For information about specifying Amazon S3 actions in receipt rules, see the Amazon SES Developer Guide."]
module SNSAction =
  struct
    type nonrec t =
      {
      topicArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic to notify. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."];
      encoding: SNSActionEncoding.t option
        [@ocaml.doc
          "The encoding to use for the email within the Amazon SNS notification. UTF-8 is easier to use, but may not preserve all special characters when a message was encoded with a different encoding format. Base64 preserves all special characters. The default value is UTF-8."]}
    let context_ = "SNSAction"
    let make ?encoding = fun ~topicArn -> fun () -> { encoding; topicArn }
    let to_value x =
      structure_to_value
        [("TopicArn", (Some (AmazonResourceName.to_value x.topicArn)));
        ("Encoding", (Option.map x.encoding ~f:SNSActionEncoding.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encoding =
        (Option.map ~f:SNSActionEncoding.of_xml)
          (Xml.child xml_arg0 "Encoding") in
      let topicArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TopicArn") in
      make ?encoding ~topicArn ()
    let of_json json =
      let encoding = field_map json "Encoding" SNSActionEncoding.of_json in
      let topicArn = field_map_exn json "TopicArn" AmazonResourceName.of_json in
      make ?encoding ~topicArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action publishes a notification to Amazon Simple Notification Service (Amazon SNS). This action includes a complete copy of the email content in the Amazon SNS notifications. Amazon SNS notifications for all other actions simply provide information about the email. They do not include the email content itself. If you own the Amazon SNS topic, you don't need to do anything to give Amazon SES permission to publish emails to it. However, if you don't own the Amazon SNS topic, you need to attach a policy to the topic to give Amazon SES permissions to access it. For information about giving permissions, see the Amazon SES Developer Guide. You can only publish emails that are 150 KB or less (including the header) to Amazon SNS. Larger emails will bounce. If you anticipate emails larger than 150 KB, use the S3 action instead. For information about using a receipt rule to publish an Amazon SNS notification, see the Amazon SES Developer Guide."]
module StopAction =
  struct
    type nonrec t =
      {
      scope: StopScope.t
        [@ocaml.doc
          "The scope of the StopAction. The only acceptable value is RuleSet."];
      topicArn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic to notify when the stop action is taken. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."]}
    let context_ = "StopAction"
    let make ?topicArn = fun ~scope -> fun () -> { topicArn; scope }
    let to_value x =
      structure_to_value
        [("Scope", (Some (StopScope.to_value x.scope)));
        ("TopicArn", (Option.map x.topicArn ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topicArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TopicArn") in
      let scope =
        StopScope.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Scope") in
      make ?topicArn ~scope ()
    let of_json json =
      let topicArn = field_map json "TopicArn" AmazonResourceName.of_json in
      let scope = field_map_exn json "Scope" StopScope.of_json in
      make ?topicArn ~scope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action terminates the evaluation of the receipt rule set and, optionally, publishes a notification to Amazon Simple Notification Service (Amazon SNS). For information about setting a stop action in a receipt rule, see the Amazon SES Developer Guide."]
module WorkmailAction =
  struct
    type nonrec t =
      {
      topicArn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic to notify when the WorkMail action is called. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."];
      organizationArn: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the Amazon WorkMail organization. An example of an Amazon WorkMail organization ARN is arn:aws:workmail:us-west-2:123456789012:organization/m-68755160c4cb4e29a2b2f8fb58f359d7. For information about Amazon WorkMail organizations, see the Amazon WorkMail Administrator Guide."]}
    let context_ = "WorkmailAction"
    let make ?topicArn =
      fun ~organizationArn -> fun () -> { topicArn; organizationArn }
    let to_value x =
      structure_to_value
        [("TopicArn", (Option.map x.topicArn ~f:AmazonResourceName.to_value));
        ("OrganizationArn",
          (Some (AmazonResourceName.to_value x.organizationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OrganizationArn") in
      let topicArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TopicArn") in
      make ~organizationArn ?topicArn ()
    let of_json json =
      let organizationArn =
        field_map_exn json "OrganizationArn" AmazonResourceName.of_json in
      let topicArn = field_map json "TopicArn" AmazonResourceName.of_json in
      make ~organizationArn ?topicArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When included in a receipt rule, this action calls Amazon WorkMail and, optionally, publishes a notification to Amazon Simple Notification Service (Amazon SNS). You will typically not use this action directly because Amazon WorkMail adds the rule automatically during its setup procedure. For information using a receipt rule to call Amazon WorkMail, see the Amazon SES Developer Guide."]
module CloudWatchDimensionConfiguration =
  struct
    type nonrec t =
      {
      dimensionName: DimensionName.t
        [@ocaml.doc
          "The name of an Amazon CloudWatch dimension associated with an email sending metric. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Contain less than 256 characters."];
      dimensionValueSource: DimensionValueSource.t
        [@ocaml.doc
          "The place where Amazon SES finds the value of a dimension to publish to Amazon CloudWatch. If you want Amazon SES to use the message tags that you specify using an X-SES-MESSAGE-TAGS header or a parameter to the SendEmail/SendRawEmail API, choose messageTag. If you want Amazon SES to use your own email headers, choose emailHeader."];
      defaultDimensionValue: DefaultDimensionValue.t
        [@ocaml.doc
          "The default value of the dimension that is published to Amazon CloudWatch if you do not provide the value of the dimension when you send an email. The default value must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Contain less than 256 characters."]}
    let context_ = "CloudWatchDimensionConfiguration"
    let make ~dimensionName =
      fun ~dimensionValueSource ->
        fun ~defaultDimensionValue ->
          fun () ->
            { dimensionName; dimensionValueSource; defaultDimensionValue }
    let to_value x =
      structure_to_value
        [("DimensionName", (Some (DimensionName.to_value x.dimensionName)));
        ("DimensionValueSource",
          (Some (DimensionValueSource.to_value x.dimensionValueSource)));
        ("DefaultDimensionValue",
          (Some (DefaultDimensionValue.to_value x.defaultDimensionValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultDimensionValue =
        DefaultDimensionValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DefaultDimensionValue") in
      let dimensionValueSource =
        DimensionValueSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DimensionValueSource") in
      let dimensionName =
        DimensionName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DimensionName") in
      make ~defaultDimensionValue ~dimensionValueSource ~dimensionName ()
    let of_json json =
      let defaultDimensionValue =
        field_map_exn json "DefaultDimensionValue"
          DefaultDimensionValue.of_json in
      let dimensionValueSource =
        field_map_exn json "DimensionValueSource"
          DimensionValueSource.of_json in
      let dimensionName =
        field_map_exn json "DimensionName" DimensionName.of_json in
      make ~defaultDimensionValue ~dimensionValueSource ~dimensionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the dimension configuration to use when you publish email sending events to Amazon CloudWatch. For information about publishing email sending events to Amazon CloudWatch, see the Amazon SES Developer Guide."]
module Charset =
  struct
    type nonrec t = string
    let context_ = "Charset"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Charset" j
    let to_json = simple_to_json to_value
  end
module MessageData =
  struct
    type nonrec t = string
    let context_ = "MessageData"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MessageData" j
    let to_json = simple_to_json to_value
  end
module AddressList =
  struct
    type nonrec t = Address.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Address.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Address.of_xml)
    let of_json j =
      list_of_json ~kind:"AddressList" ~of_json:Address.of_json j
    let to_json v = composed_to_json to_value v
  end
module MessageTag =
  struct
    type nonrec t =
      {
      name: MessageTagName.t
        [@ocaml.doc
          "The name of the tag. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Contain less than 256 characters."];
      value: MessageTagValue.t
        [@ocaml.doc
          "The value of the tag. The value must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Contain less than 256 characters."]}
    let context_ = "MessageTag"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Some (MessageTagName.to_value x.name)));
        ("Value", (Some (MessageTagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        MessageTagValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let name =
        MessageTagName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~value ~name ()
    let of_json json =
      let value = field_map_exn json "Value" MessageTagValue.of_json in
      let name = field_map_exn json "Name" MessageTagName.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the name and value of a tag that you can provide to SendEmail or SendRawEmail to apply to an email. Message tags, which you use with configuration sets, enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module DiagnosticCode =
  struct
    type nonrec t = string
    let context_ = "DiagnosticCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DiagnosticCode" j
    let to_json = simple_to_json to_value
  end
module DsnAction =
  struct
    type nonrec t =
      | Failed 
      | Delayed 
      | Delivered 
      | Relayed 
      | Expanded 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Failed -> "failed"
      | Delayed -> "delayed"
      | Delivered -> "delivered"
      | Relayed -> "relayed"
      | Expanded -> "expanded"
      | Non_static_id s -> s
    let of_string =
      function
      | "failed" -> Failed
      | "delayed" -> Delayed
      | "delivered" -> Delivered
      | "relayed" -> Relayed
      | "expanded" -> Expanded
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DsnAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DsnAction" j)
    let to_json = simple_to_json to_value
  end
module DsnStatus =
  struct
    type nonrec t = string
    let context_ = "DsnStatus"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DsnStatus" j
    let to_json = simple_to_json to_value
  end
module ExtensionFieldList =
  struct
    type nonrec t = ExtensionField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ExtensionField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExtensionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ExtensionFieldList" ~of_json:ExtensionField.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module LastAttemptDate =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module RemoteMta =
  struct
    type nonrec t = string
    let context_ = "RemoteMta"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RemoteMta" j
    let to_json = simple_to_json to_value
  end
module Cidr =
  struct
    type nonrec t = string
    let context_ = "Cidr"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Cidr" j
    let to_json = simple_to_json to_value
  end
module ReceiptFilterPolicy =
  struct
    type nonrec t =
      | Block 
      | Allow 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Block -> "Block" | Allow -> "Allow" | Non_static_id s -> s
    let of_string =
      function | "Block" -> Block | "Allow" -> Allow | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReceiptFilterPolicy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ReceiptFilterPolicy" j)
    let to_json = simple_to_json to_value
  end
module VerificationToken =
  struct
    type nonrec t = string
    let context_ = "VerificationToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VerificationToken" j
    let to_json = simple_to_json to_value
  end
module ReceiptAction =
  struct
    type nonrec t =
      {
      s3Action: S3Action.t option
        [@ocaml.doc
          "Saves the received message to an Amazon Simple Storage Service (Amazon S3) bucket and, optionally, publishes a notification to Amazon SNS."];
      bounceAction: BounceAction.t option
        [@ocaml.doc
          "Rejects the received email by returning a bounce response to the sender and, optionally, publishes a notification to Amazon Simple Notification Service (Amazon SNS)."];
      workmailAction: WorkmailAction.t option
        [@ocaml.doc
          "Calls Amazon WorkMail and, optionally, publishes a notification to Amazon Amazon SNS."];
      lambdaAction: LambdaAction.t option
        [@ocaml.doc
          "Calls an AWS Lambda function, and optionally, publishes a notification to Amazon SNS."];
      stopAction: StopAction.t option
        [@ocaml.doc
          "Terminates the evaluation of the receipt rule set and optionally publishes a notification to Amazon SNS."];
      addHeaderAction: AddHeaderAction.t option
        [@ocaml.doc "Adds a header to the received email."];
      sNSAction: SNSAction.t option
        [@ocaml.doc
          "Publishes the email content within a notification to Amazon SNS."]}
    let make ?s3Action =
      fun ?bounceAction ->
        fun ?workmailAction ->
          fun ?lambdaAction ->
            fun ?stopAction ->
              fun ?addHeaderAction ->
                fun ?sNSAction ->
                  fun () ->
                    {
                      s3Action;
                      bounceAction;
                      workmailAction;
                      lambdaAction;
                      stopAction;
                      addHeaderAction;
                      sNSAction
                    }
    let to_value x =
      structure_to_value
        [("S3Action", (Option.map x.s3Action ~f:S3Action.to_value));
        ("BounceAction",
          (Option.map x.bounceAction ~f:BounceAction.to_value));
        ("WorkmailAction",
          (Option.map x.workmailAction ~f:WorkmailAction.to_value));
        ("LambdaAction",
          (Option.map x.lambdaAction ~f:LambdaAction.to_value));
        ("StopAction", (Option.map x.stopAction ~f:StopAction.to_value));
        ("AddHeaderAction",
          (Option.map x.addHeaderAction ~f:AddHeaderAction.to_value));
        ("SNSAction", (Option.map x.sNSAction ~f:SNSAction.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sNSAction =
        (Option.map ~f:SNSAction.of_xml) (Xml.child xml_arg0 "SNSAction") in
      let addHeaderAction =
        (Option.map ~f:AddHeaderAction.of_xml)
          (Xml.child xml_arg0 "AddHeaderAction") in
      let stopAction =
        (Option.map ~f:StopAction.of_xml) (Xml.child xml_arg0 "StopAction") in
      let lambdaAction =
        (Option.map ~f:LambdaAction.of_xml)
          (Xml.child xml_arg0 "LambdaAction") in
      let workmailAction =
        (Option.map ~f:WorkmailAction.of_xml)
          (Xml.child xml_arg0 "WorkmailAction") in
      let bounceAction =
        (Option.map ~f:BounceAction.of_xml)
          (Xml.child xml_arg0 "BounceAction") in
      let s3Action =
        (Option.map ~f:S3Action.of_xml) (Xml.child xml_arg0 "S3Action") in
      make ?sNSAction ?addHeaderAction ?stopAction ?lambdaAction
        ?workmailAction ?bounceAction ?s3Action ()
    let of_json json =
      let sNSAction = field_map json "SNSAction" SNSAction.of_json in
      let addHeaderAction =
        field_map json "AddHeaderAction" AddHeaderAction.of_json in
      let stopAction = field_map json "StopAction" StopAction.of_json in
      let lambdaAction = field_map json "LambdaAction" LambdaAction.of_json in
      let workmailAction =
        field_map json "WorkmailAction" WorkmailAction.of_json in
      let bounceAction = field_map json "BounceAction" BounceAction.of_json in
      let s3Action = field_map json "S3Action" S3Action.of_json in
      make ?sNSAction ?addHeaderAction ?stopAction ?lambdaAction
        ?workmailAction ?bounceAction ?s3Action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An action that Amazon SES can take when it receives an email on behalf of one or more email addresses or domains that you own. An instance of this data type can represent only one action. For information about setting up receipt rules, see the Amazon SES Developer Guide."]
module Recipient =
  struct
    type nonrec t = string
    let context_ = "Recipient"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Recipient" j
    let to_json = simple_to_json to_value
  end
module CloudWatchDimensionConfigurations =
  struct
    type nonrec t = CloudWatchDimensionConfiguration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CloudWatchDimensionConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CloudWatchDimensionConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"CloudWatchDimensionConfigurations"
        ~of_json:CloudWatchDimensionConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventType =
  struct
    type nonrec t =
      | Send 
      | Reject 
      | Bounce 
      | Complaint 
      | Delivery 
      | Open 
      | Click 
      | RenderingFailure 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Send -> "send"
      | Reject -> "reject"
      | Bounce -> "bounce"
      | Complaint -> "complaint"
      | Delivery -> "delivery"
      | Open -> "open"
      | Click -> "click"
      | RenderingFailure -> "renderingFailure"
      | Non_static_id s -> s
    let of_string =
      function
      | "send" -> Send
      | "reject" -> Reject
      | "bounce" -> Bounce
      | "complaint" -> Complaint
      | "delivery" -> Delivery
      | "open" -> Open
      | "click" -> Click
      | "renderingFailure" -> RenderingFailure
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EventType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventType" j)
    let to_json = simple_to_json to_value
  end
module Content =
  struct
    type nonrec t =
      {
      data: MessageData.t [@ocaml.doc "The textual data of the content."];
      charset: Charset.t option
        [@ocaml.doc "The character set of the content."]}
    let context_ = "Content"
    let make ?charset = fun ~data -> fun () -> { charset; data }
    let to_value x =
      structure_to_value
        [("Data", (Some (MessageData.to_value x.data)));
        ("Charset", (Option.map x.charset ~f:Charset.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let charset =
        (Option.map ~f:Charset.of_xml) (Xml.child xml_arg0 "Charset") in
      let data =
        MessageData.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Data") in
      make ?charset ~data ()
    let of_json json =
      let charset = field_map json "Charset" Charset.of_json in
      let data = field_map_exn json "Data" MessageData.of_json in
      make ?charset ~data ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents textual data, plus an optional character set specification. By default, the text must be 7-bit ASCII, due to the constraints of the SMTP protocol. If the text must contain any other characters, then you must also specify a character set. Examples include UTF-8, ISO-8859-1, and Shift_JIS."]
module BulkEmailStatus =
  struct
    type nonrec t =
      | Success 
      | MessageRejected 
      | MailFromDomainNotVerified 
      | ConfigurationSetDoesNotExist 
      | TemplateDoesNotExist 
      | AccountSuspended 
      | AccountThrottled 
      | AccountDailyQuotaExceeded 
      | InvalidSendingPoolName 
      | AccountSendingPaused 
      | ConfigurationSetSendingPaused 
      | InvalidParameterValue 
      | TransientFailure 
      | Failed 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Success -> "Success"
      | MessageRejected -> "MessageRejected"
      | MailFromDomainNotVerified -> "MailFromDomainNotVerified"
      | ConfigurationSetDoesNotExist -> "ConfigurationSetDoesNotExist"
      | TemplateDoesNotExist -> "TemplateDoesNotExist"
      | AccountSuspended -> "AccountSuspended"
      | AccountThrottled -> "AccountThrottled"
      | AccountDailyQuotaExceeded -> "AccountDailyQuotaExceeded"
      | InvalidSendingPoolName -> "InvalidSendingPoolName"
      | AccountSendingPaused -> "AccountSendingPaused"
      | ConfigurationSetSendingPaused -> "ConfigurationSetSendingPaused"
      | InvalidParameterValue -> "InvalidParameterValue"
      | TransientFailure -> "TransientFailure"
      | Failed -> "Failed"
      | Non_static_id s -> s
    let of_string =
      function
      | "Success" -> Success
      | "MessageRejected" -> MessageRejected
      | "MailFromDomainNotVerified" -> MailFromDomainNotVerified
      | "ConfigurationSetDoesNotExist" -> ConfigurationSetDoesNotExist
      | "TemplateDoesNotExist" -> TemplateDoesNotExist
      | "AccountSuspended" -> AccountSuspended
      | "AccountThrottled" -> AccountThrottled
      | "AccountDailyQuotaExceeded" -> AccountDailyQuotaExceeded
      | "InvalidSendingPoolName" -> InvalidSendingPoolName
      | "AccountSendingPaused" -> AccountSendingPaused
      | "ConfigurationSetSendingPaused" -> ConfigurationSetSendingPaused
      | "InvalidParameterValue" -> InvalidParameterValue
      | "TransientFailure" -> TransientFailure
      | "Failed" -> Failed
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BulkEmailStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BulkEmailStatus" j)
    let to_json = simple_to_json to_value
  end
module Error =
  struct
    type nonrec t = string
    let context_ = "Error"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Error" j
    let to_json = simple_to_json to_value
  end
module MessageId =
  struct
    type nonrec t = string
    let context_ = "MessageId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MessageId" j
    let to_json = simple_to_json to_value
  end
module Destination =
  struct
    type nonrec t =
      {
      toAddresses: AddressList.t option
        [@ocaml.doc
          "The recipients to place on the To: line of the message."];
      ccAddresses: AddressList.t option
        [@ocaml.doc
          "The recipients to place on the CC: line of the message."];
      bccAddresses: AddressList.t option
        [@ocaml.doc
          "The recipients to place on the BCC: line of the message."]}
    let make ?toAddresses =
      fun ?ccAddresses ->
        fun ?bccAddresses ->
          fun () -> { toAddresses; ccAddresses; bccAddresses }
    let to_value x =
      structure_to_value
        [("ToAddresses", (Option.map x.toAddresses ~f:AddressList.to_value));
        ("CcAddresses", (Option.map x.ccAddresses ~f:AddressList.to_value));
        ("BccAddresses", (Option.map x.bccAddresses ~f:AddressList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bccAddresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "BccAddresses") in
      let ccAddresses =
        (Option.map ~f:AddressList.of_xml) (Xml.child xml_arg0 "CcAddresses") in
      let toAddresses =
        (Option.map ~f:AddressList.of_xml) (Xml.child xml_arg0 "ToAddresses") in
      make ?bccAddresses ?ccAddresses ?toAddresses ()
    let of_json json =
      let bccAddresses = field_map json "BccAddresses" AddressList.of_json in
      let ccAddresses = field_map json "CcAddresses" AddressList.of_json in
      let toAddresses = field_map json "ToAddresses" AddressList.of_json in
      make ?bccAddresses ?ccAddresses ?toAddresses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the destination of the message, consisting of To:, CC:, and BCC: fields. Amazon SES does not support the SMTPUTF8 extension, as described in RFC6531. For this reason, the local part of a destination email address (the part of the email address that precedes the \\@ sign) may only contain 7-bit ASCII characters. If the domain part of an address (the part after the \\@ sign) contains non-ASCII characters, they must be encoded using Punycode, as described in RFC3492."]
module MessageTagList =
  struct
    type nonrec t = MessageTag.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MessageTag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MessageTag.of_xml)
    let of_json j =
      list_of_json ~kind:"MessageTagList" ~of_json:MessageTag.of_json j
    let to_json v = composed_to_json to_value v
  end
module TemplateData =
  struct
    type nonrec t = string
    let context_ = "TemplateData"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:262144); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateData" j
    let to_json = simple_to_json to_value
  end
module BounceType =
  struct
    type nonrec t =
      | DoesNotExist 
      | MessageTooLarge 
      | ExceededQuota 
      | ContentRejected 
      | Undefined 
      | TemporaryFailure 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DoesNotExist -> "DoesNotExist"
      | MessageTooLarge -> "MessageTooLarge"
      | ExceededQuota -> "ExceededQuota"
      | ContentRejected -> "ContentRejected"
      | Undefined -> "Undefined"
      | TemporaryFailure -> "TemporaryFailure"
      | Non_static_id s -> s
    let of_string =
      function
      | "DoesNotExist" -> DoesNotExist
      | "MessageTooLarge" -> MessageTooLarge
      | "ExceededQuota" -> ExceededQuota
      | "ContentRejected" -> ContentRejected
      | "Undefined" -> Undefined
      | "TemporaryFailure" -> TemporaryFailure
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BounceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BounceType" j)
    let to_json = simple_to_json to_value
  end
module RecipientDsnFields =
  struct
    type nonrec t =
      {
      finalRecipient: Address.t option
        [@ocaml.doc
          "The email address that the message was ultimately delivered to. This corresponds to the Final-Recipient in the DSN. If not specified, FinalRecipient will be set to the Recipient specified in the BouncedRecipientInfo structure. Either FinalRecipient or the recipient in BouncedRecipientInfo must be a recipient of the original bounced message. Do not prepend the FinalRecipient email address with rfc 822;, as described in RFC 3798."];
      action: DsnAction.t
        [@ocaml.doc
          "The action performed by the reporting mail transfer agent (MTA) as a result of its attempt to deliver the message to the recipient address. This is required by RFC 3464."];
      remoteMta: RemoteMta.t option
        [@ocaml.doc
          "The MTA to which the remote MTA attempted to deliver the message, formatted as specified in RFC 3464 (mta-name-type; mta-name). This parameter typically applies only to propagating synchronous bounces."];
      status: DsnStatus.t
        [@ocaml.doc
          "The status code that indicates what went wrong. This is required by RFC 3464."];
      diagnosticCode: DiagnosticCode.t option
        [@ocaml.doc
          "An extended explanation of what went wrong; this is usually an SMTP response. See RFC 3463 for the correct formatting of this parameter."];
      lastAttemptDate: LastAttemptDate.t option
        [@ocaml.doc
          "The time the final delivery attempt was made, in RFC 822 date-time format."];
      extensionFields: ExtensionFieldList.t option
        [@ocaml.doc "Additional X-headers to include in the DSN."]}
    let context_ = "RecipientDsnFields"
    let make ?finalRecipient =
      fun ?remoteMta ->
        fun ?diagnosticCode ->
          fun ?lastAttemptDate ->
            fun ?extensionFields ->
              fun ~action ->
                fun ~status ->
                  fun () ->
                    {
                      finalRecipient;
                      remoteMta;
                      diagnosticCode;
                      lastAttemptDate;
                      extensionFields;
                      action;
                      status
                    }
    let to_value x =
      structure_to_value
        [("FinalRecipient",
           (Option.map x.finalRecipient ~f:Address.to_value));
        ("Action", (Some (DsnAction.to_value x.action)));
        ("RemoteMta", (Option.map x.remoteMta ~f:RemoteMta.to_value));
        ("Status", (Some (DsnStatus.to_value x.status)));
        ("DiagnosticCode",
          (Option.map x.diagnosticCode ~f:DiagnosticCode.to_value));
        ("LastAttemptDate",
          (Option.map x.lastAttemptDate ~f:LastAttemptDate.to_value));
        ("ExtensionFields",
          (Option.map x.extensionFields ~f:ExtensionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let extensionFields =
        (Option.map ~f:ExtensionFieldList.of_xml)
          (Xml.child xml_arg0 "ExtensionFields") in
      let lastAttemptDate =
        (Option.map ~f:LastAttemptDate.of_xml)
          (Xml.child xml_arg0 "LastAttemptDate") in
      let diagnosticCode =
        (Option.map ~f:DiagnosticCode.of_xml)
          (Xml.child xml_arg0 "DiagnosticCode") in
      let status =
        DsnStatus.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      let remoteMta =
        (Option.map ~f:RemoteMta.of_xml) (Xml.child xml_arg0 "RemoteMta") in
      let action =
        DsnAction.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Action") in
      let finalRecipient =
        (Option.map ~f:Address.of_xml) (Xml.child xml_arg0 "FinalRecipient") in
      make ?extensionFields ?lastAttemptDate ?diagnosticCode ~status
        ?remoteMta ~action ?finalRecipient ()
    let of_json json =
      let extensionFields =
        field_map json "ExtensionFields" ExtensionFieldList.of_json in
      let lastAttemptDate =
        field_map json "LastAttemptDate" LastAttemptDate.of_json in
      let diagnosticCode =
        field_map json "DiagnosticCode" DiagnosticCode.of_json in
      let status = field_map_exn json "Status" DsnStatus.of_json in
      let remoteMta = field_map json "RemoteMta" RemoteMta.of_json in
      let action = field_map_exn json "Action" DsnAction.of_json in
      let finalRecipient = field_map json "FinalRecipient" Address.of_json in
      make ?extensionFields ?lastAttemptDate ?diagnosticCode ~status
        ?remoteMta ~action ?finalRecipient ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Recipient-related information to include in the Delivery Status Notification (DSN) when an email that Amazon SES receives on your behalf bounces. For information about receiving email through Amazon SES, see the Amazon SES Developer Guide."]
module TemplateName =
  struct
    type nonrec t = string
    let context_ = "TemplateName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateName" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ReceiptRuleSetName =
  struct
    type nonrec t = string
    let context_ = "ReceiptRuleSetName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReceiptRuleSetName" j
    let to_json = simple_to_json to_value
  end
module ReceiptFilterName =
  struct
    type nonrec t = string
    let context_ = "ReceiptFilterName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReceiptFilterName" j
    let to_json = simple_to_json to_value
  end
module ReceiptIpFilter =
  struct
    type nonrec t =
      {
      policy: ReceiptFilterPolicy.t
        [@ocaml.doc
          "Indicates whether to block or allow incoming mail from the specified IP addresses."];
      cidr: Cidr.t
        [@ocaml.doc
          "A single IP address or a range of IP addresses that you want to block or allow, specified in Classless Inter-Domain Routing (CIDR) notation. An example of a single email address is 10.0.0.1. An example of a range of IP addresses is 10.0.0.1/24. For more information about CIDR notation, see RFC 2317."]}
    let context_ = "ReceiptIpFilter"
    let make ~policy = fun ~cidr -> fun () -> { policy; cidr }
    let to_value x =
      structure_to_value
        [("Policy", (Some (ReceiptFilterPolicy.to_value x.policy)));
        ("Cidr", (Some (Cidr.to_value x.cidr)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidr =
        Cidr.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let policy =
        ReceiptFilterPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Policy") in
      make ~cidr ~policy ()
    let of_json json =
      let cidr = field_map_exn json "Cidr" Cidr.of_json in
      let policy = field_map_exn json "Policy" ReceiptFilterPolicy.of_json in
      make ~cidr ~policy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A receipt IP address filter enables you to specify whether to accept or reject mail originating from an IP address or range of IP addresses. For information about setting up IP address filters, see the Amazon SES Developer Guide."]
module FailureRedirectionURL =
  struct
    type nonrec t = string
    let context_ = "FailureRedirectionURL"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FailureRedirectionURL" j
    let to_json = simple_to_json to_value
  end
module FromAddress =
  struct
    type nonrec t = string
    let context_ = "FromAddress"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FromAddress" j
    let to_json = simple_to_json to_value
  end
module Subject =
  struct
    type nonrec t = string
    let context_ = "Subject"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Subject" j
    let to_json = simple_to_json to_value
  end
module SuccessRedirectionURL =
  struct
    type nonrec t = string
    let context_ = "SuccessRedirectionURL"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SuccessRedirectionURL" j
    let to_json = simple_to_json to_value
  end
module ConfigurationSetName =
  struct
    type nonrec t = string
    let context_ = "ConfigurationSetName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConfigurationSetName" j
    let to_json = simple_to_json to_value
  end
module Counter =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module VerificationStatus =
  struct
    type nonrec t =
      | Pending 
      | Success 
      | Failed 
      | TemporaryFailure 
      | NotStarted 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "Pending"
      | Success -> "Success"
      | Failed -> "Failed"
      | TemporaryFailure -> "TemporaryFailure"
      | NotStarted -> "NotStarted"
      | Non_static_id s -> s
    let of_string =
      function
      | "Pending" -> Pending
      | "Success" -> Success
      | "Failed" -> Failed
      | "TemporaryFailure" -> TemporaryFailure
      | "NotStarted" -> NotStarted
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration VerificationStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"VerificationStatus" j)
    let to_json = simple_to_json to_value
  end
module Enabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NotificationTopic =
  struct
    type nonrec t = string
    let context_ = "NotificationTopic"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NotificationTopic" j
    let to_json = simple_to_json to_value
  end
module BehaviorOnMXFailure =
  struct
    type nonrec t =
      | UseDefaultValue 
      | RejectMessage 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UseDefaultValue -> "UseDefaultValue"
      | RejectMessage -> "RejectMessage"
      | Non_static_id s -> s
    let of_string =
      function
      | "UseDefaultValue" -> UseDefaultValue
      | "RejectMessage" -> RejectMessage
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BehaviorOnMXFailure" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BehaviorOnMXFailure" j)
    let to_json = simple_to_json to_value
  end
module CustomMailFromStatus =
  struct
    type nonrec t =
      | Pending 
      | Success 
      | Failed 
      | TemporaryFailure 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "Pending"
      | Success -> "Success"
      | Failed -> "Failed"
      | TemporaryFailure -> "TemporaryFailure"
      | Non_static_id s -> s
    let of_string =
      function
      | "Pending" -> Pending
      | "Success" -> Success
      | "Failed" -> Failed
      | "TemporaryFailure" -> TemporaryFailure
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CustomMailFromStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CustomMailFromStatus" j)
    let to_json = simple_to_json to_value
  end
module MailFromDomainName =
  struct
    type nonrec t = string
    let context_ = "MailFromDomainName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MailFromDomainName" j
    let to_json = simple_to_json to_value
  end
module VerificationTokenList =
  struct
    type nonrec t = VerificationToken.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:VerificationToken.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VerificationToken.of_xml)
    let of_json j =
      list_of_json ~kind:"VerificationTokenList"
        ~of_json:VerificationToken.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReceiptActionsList =
  struct
    type nonrec t = ReceiptAction.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReceiptAction.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReceiptAction.of_xml)
    let of_json j =
      list_of_json ~kind:"ReceiptActionsList" ~of_json:ReceiptAction.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ReceiptRuleName =
  struct
    type nonrec t = string
    let context_ = "ReceiptRuleName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReceiptRuleName" j
    let to_json = simple_to_json to_value
  end
module RecipientsList =
  struct
    type nonrec t = Recipient.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Recipient.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Recipient.of_xml)
    let of_json j =
      list_of_json ~kind:"RecipientsList" ~of_json:Recipient.of_json j
    let to_json v = composed_to_json to_value v
  end
module TlsPolicy =
  struct
    type nonrec t =
      | Require 
      | Optional 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Require -> "Require"
      | Optional -> "Optional"
      | Non_static_id s -> s
    let of_string =
      function
      | "Require" -> Require
      | "Optional" -> Optional
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TlsPolicy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TlsPolicy" j)
    let to_json = simple_to_json to_value
  end
module CloudWatchDestination =
  struct
    type nonrec t =
      {
      dimensionConfigurations: CloudWatchDimensionConfigurations.t
        [@ocaml.doc
          "A list of dimensions upon which to categorize your emails when you publish email sending events to Amazon CloudWatch."]}
    let context_ = "CloudWatchDestination"
    let make ~dimensionConfigurations = fun () -> { dimensionConfigurations }
    let to_value x =
      structure_to_value
        [("DimensionConfigurations",
           (Some
              (CloudWatchDimensionConfigurations.to_value
                 x.dimensionConfigurations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dimensionConfigurations =
        CloudWatchDimensionConfigurations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DimensionConfigurations") in
      make ~dimensionConfigurations ()
    let of_json json =
      let dimensionConfigurations =
        field_map_exn json "DimensionConfigurations"
          CloudWatchDimensionConfigurations.of_json in
      make ~dimensionConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information associated with an Amazon CloudWatch event destination to which email sending events are published. Event destinations, such as Amazon CloudWatch, are associated with configuration sets, which enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module EventDestinationName =
  struct
    type nonrec t = string
    let context_ = "EventDestinationName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventDestinationName" j
    let to_json = simple_to_json to_value
  end
module EventTypes =
  struct
    type nonrec t = EventType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EventType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventType.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypes" ~of_json:EventType.of_json j
    let to_json v = composed_to_json to_value v
  end
module KinesisFirehoseDestination =
  struct
    type nonrec t =
      {
      iAMRoleARN: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the IAM role under which Amazon SES publishes email sending events to the Amazon Kinesis Firehose stream."];
      deliveryStreamARN: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the Amazon Kinesis Firehose stream that email sending events should be published to."]}
    let context_ = "KinesisFirehoseDestination"
    let make ~iAMRoleARN =
      fun ~deliveryStreamARN -> fun () -> { iAMRoleARN; deliveryStreamARN }
    let to_value x =
      structure_to_value
        [("IAMRoleARN", (Some (AmazonResourceName.to_value x.iAMRoleARN)));
        ("DeliveryStreamARN",
          (Some (AmazonResourceName.to_value x.deliveryStreamARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deliveryStreamARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DeliveryStreamARN") in
      let iAMRoleARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IAMRoleARN") in
      make ~deliveryStreamARN ~iAMRoleARN ()
    let of_json json =
      let deliveryStreamARN =
        field_map_exn json "DeliveryStreamARN" AmazonResourceName.of_json in
      let iAMRoleARN =
        field_map_exn json "IAMRoleARN" AmazonResourceName.of_json in
      make ~deliveryStreamARN ~iAMRoleARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the delivery stream ARN and the IAM role ARN associated with an Amazon Kinesis Firehose event destination. Event destinations, such as Amazon Kinesis Firehose, are associated with configuration sets, which enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module SNSDestination =
  struct
    type nonrec t =
      {
      topicARN: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the Amazon SNS topic that email sending events will be published to. An example of an Amazon SNS topic ARN is arn:aws:sns:us-west-2:123456789012:MyTopic. For more information about Amazon SNS topics, see the Amazon SNS Developer Guide."]}
    let context_ = "SNSDestination"
    let make ~topicARN = fun () -> { topicARN }
    let to_value x =
      structure_to_value
        [("TopicARN", (Some (AmazonResourceName.to_value x.topicARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topicARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TopicARN") in
      make ~topicARN ()
    let of_json json =
      let topicARN = field_map_exn json "TopicARN" AmazonResourceName.of_json in
      make ~topicARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the topic ARN associated with an Amazon Simple Notification Service (Amazon SNS) event destination. Event destinations, such as Amazon SNS, are associated with configuration sets, which enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module HtmlPart =
  struct
    type nonrec t = string
    let context_ = "HtmlPart"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HtmlPart" j
    let to_json = simple_to_json to_value
  end
module SubjectPart =
  struct
    type nonrec t = string
    let context_ = "SubjectPart"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SubjectPart" j
    let to_json = simple_to_json to_value
  end
module TextPart =
  struct
    type nonrec t = string
    let context_ = "TextPart"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TextPart" j
    let to_json = simple_to_json to_value
  end
module RuleOrRuleSetName =
  struct
    type nonrec t = string
    let context_ = "RuleOrRuleSetName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RuleOrRuleSetName" j
    let to_json = simple_to_json to_value
  end
module CustomRedirectDomain =
  struct
    type nonrec t = string
    let context_ = "CustomRedirectDomain"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomRedirectDomain" j
    let to_json = simple_to_json to_value
  end
module RawMessageData =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module Body =
  struct
    type nonrec t =
      {
      text: Content.t option
        [@ocaml.doc
          "The content of the message, in text format. Use this for text-based email clients, or clients on high-latency networks (such as mobile devices)."];
      html: Content.t option
        [@ocaml.doc
          "The content of the message, in HTML format. Use this for email clients that can process HTML. You can include clickable links, formatted text, and much more in an HTML message."]}
    let make ?text = fun ?html -> fun () -> { text; html }
    let to_value x =
      structure_to_value
        [("Text", (Option.map x.text ~f:Content.to_value));
        ("Html", (Option.map x.html ~f:Content.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let html = (Option.map ~f:Content.of_xml) (Xml.child xml_arg0 "Html") in
      let text = (Option.map ~f:Content.of_xml) (Xml.child xml_arg0 "Text") in
      make ?html ?text ()
    let of_json json =
      let html = field_map json "Html" Content.of_json in
      let text = field_map json "Text" Content.of_json in make ?html ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the body of the message. You can specify text, HTML, or both. If you use both, then the message should display correctly in the widest variety of email clients."]
module BulkEmailDestinationStatus =
  struct
    type nonrec t =
      {
      status: BulkEmailStatus.t option
        [@ocaml.doc
          "The status of a message sent using the SendBulkTemplatedEmail operation. Possible values for this parameter include: Success: Amazon SES accepted the message, and will attempt to deliver it to the recipients. MessageRejected: The message was rejected because it contained a virus. MailFromDomainNotVerified: The sender's email address or domain was not verified. ConfigurationSetDoesNotExist: The configuration set you specified does not exist. TemplateDoesNotExist: The template you specified does not exist. AccountSuspended: Your account has been shut down because of issues related to your email sending practices. AccountThrottled: The number of emails you can send has been reduced because your account has exceeded its allocated sending limit. AccountDailyQuotaExceeded: You have reached or exceeded the maximum number of emails you can send from your account in a 24-hour period. InvalidSendingPoolName: The configuration set you specified refers to an IP pool that does not exist. AccountSendingPaused: Email sending for the Amazon SES account was disabled using the UpdateAccountSendingEnabled operation. ConfigurationSetSendingPaused: Email sending for this configuration set was disabled using the UpdateConfigurationSetSendingEnabled operation. InvalidParameterValue: One or more of the parameters you specified when calling this operation was invalid. See the error message for additional information. TransientFailure: Amazon SES was unable to process your request because of a temporary issue. Failed: Amazon SES was unable to process your request. See the error message for additional information."];
      error: Error.t option
        [@ocaml.doc
          "A description of an error that prevented a message being sent using the SendBulkTemplatedEmail operation."];
      messageId: MessageId.t option
        [@ocaml.doc
          "The unique message identifier returned from the SendBulkTemplatedEmail operation."]}
    let make ?status =
      fun ?error -> fun ?messageId -> fun () -> { status; error; messageId }
    let to_value x =
      structure_to_value
        [("Status", (Option.map x.status ~f:BulkEmailStatus.to_value));
        ("Error", (Option.map x.error ~f:Error.to_value));
        ("MessageId", (Option.map x.messageId ~f:MessageId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let messageId =
        (Option.map ~f:MessageId.of_xml) (Xml.child xml_arg0 "MessageId") in
      let error = (Option.map ~f:Error.of_xml) (Xml.child xml_arg0 "Error") in
      let status =
        (Option.map ~f:BulkEmailStatus.of_xml) (Xml.child xml_arg0 "Status") in
      make ?messageId ?error ?status ()
    let of_json json =
      let messageId = field_map json "MessageId" MessageId.of_json in
      let error = field_map json "Error" Error.of_json in
      let status = field_map json "Status" BulkEmailStatus.of_json in
      make ?messageId ?error ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that contains the response from the SendBulkTemplatedEmail operation."]
module BulkEmailDestination =
  struct
    type nonrec t =
      {
      destination: Destination.t ;
      replacementTags: MessageTagList.t option
        [@ocaml.doc
          "A list of tags, in the form of name/value pairs, to apply to an email that you send using SendBulkTemplatedEmail. Tags correspond to characteristics of the email that you define, so that you can publish email sending events."];
      replacementTemplateData: TemplateData.t option
        [@ocaml.doc
          "A list of replacement values to apply to the template. This parameter is a JSON object, typically consisting of key-value pairs in which the keys correspond to replacement tags in the email template."]}
    let context_ = "BulkEmailDestination"
    let make ?replacementTags =
      fun ?replacementTemplateData ->
        fun ~destination ->
          fun () -> { replacementTags; replacementTemplateData; destination }
    let to_value x =
      structure_to_value
        [("Destination", (Some (Destination.to_value x.destination)));
        ("ReplacementTags",
          (Option.map x.replacementTags ~f:MessageTagList.to_value));
        ("ReplacementTemplateData",
          (Option.map x.replacementTemplateData ~f:TemplateData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let replacementTemplateData =
        (Option.map ~f:TemplateData.of_xml)
          (Xml.child xml_arg0 "ReplacementTemplateData") in
      let replacementTags =
        (Option.map ~f:MessageTagList.of_xml)
          (Xml.child xml_arg0 "ReplacementTags") in
      let destination =
        Destination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destination") in
      make ?replacementTemplateData ?replacementTags ~destination ()
    let of_json json =
      let replacementTemplateData =
        field_map json "ReplacementTemplateData" TemplateData.of_json in
      let replacementTags =
        field_map json "ReplacementTags" MessageTagList.of_json in
      let destination = field_map_exn json "Destination" Destination.of_json in
      make ?replacementTemplateData ?replacementTags ~destination ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An array that contains one or more Destinations, as well as the tags and replacement data associated with each of those Destinations."]
module BouncedRecipientInfo =
  struct
    type nonrec t =
      {
      recipient: Address.t
        [@ocaml.doc
          "The email address of the recipient of the bounced email."];
      recipientArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to receive email for the recipient of the bounced email. For more information about sending authorization, see the Amazon SES Developer Guide."];
      bounceType: BounceType.t option
        [@ocaml.doc
          "The reason for the bounce. You must provide either this parameter or RecipientDsnFields."];
      recipientDsnFields: RecipientDsnFields.t option
        [@ocaml.doc
          "Recipient-related DSN fields, most of which would normally be filled in automatically when provided with a BounceType. You must provide either this parameter or BounceType."]}
    let context_ = "BouncedRecipientInfo"
    let make ?recipientArn =
      fun ?bounceType ->
        fun ?recipientDsnFields ->
          fun ~recipient ->
            fun () ->
              { recipientArn; bounceType; recipientDsnFields; recipient }
    let to_value x =
      structure_to_value
        [("Recipient", (Some (Address.to_value x.recipient)));
        ("RecipientArn",
          (Option.map x.recipientArn ~f:AmazonResourceName.to_value));
        ("BounceType", (Option.map x.bounceType ~f:BounceType.to_value));
        ("RecipientDsnFields",
          (Option.map x.recipientDsnFields ~f:RecipientDsnFields.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recipientDsnFields =
        (Option.map ~f:RecipientDsnFields.of_xml)
          (Xml.child xml_arg0 "RecipientDsnFields") in
      let bounceType =
        (Option.map ~f:BounceType.of_xml) (Xml.child xml_arg0 "BounceType") in
      let recipientArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "RecipientArn") in
      let recipient =
        Address.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Recipient") in
      make ?recipientDsnFields ?bounceType ?recipientArn ~recipient ()
    let of_json json =
      let recipientDsnFields =
        field_map json "RecipientDsnFields" RecipientDsnFields.of_json in
      let bounceType = field_map json "BounceType" BounceType.of_json in
      let recipientArn =
        field_map json "RecipientArn" AmazonResourceName.of_json in
      let recipient = field_map_exn json "Recipient" Address.of_json in
      make ?recipientDsnFields ?bounceType ?recipientArn ~recipient ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Recipient-related information to include in the Delivery Status Notification (DSN) when an email that Amazon SES receives on your behalf bounces. For information about receiving email through Amazon SES, see the Amazon SES Developer Guide."]
module ArrivalDate =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ReportingMta =
  struct
    type nonrec t = string
    let context_ = "ReportingMta"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReportingMta" j
    let to_json = simple_to_json to_value
  end
module TemplateMetadata =
  struct
    type nonrec t =
      {
      name: TemplateName.t option [@ocaml.doc "The name of the template."];
      createdTimestamp: Timestamp.t option
        [@ocaml.doc "The time and date the template was created."]}
    let make ?name =
      fun ?createdTimestamp -> fun () -> { name; createdTimestamp }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:TemplateName.to_value));
        ("CreatedTimestamp",
          (Option.map x.createdTimestamp ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreatedTimestamp") in
      let name =
        (Option.map ~f:TemplateName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?createdTimestamp ?name ()
    let of_json json =
      let createdTimestamp =
        field_map json "CreatedTimestamp" Timestamp.of_json in
      let name = field_map json "Name" TemplateName.of_json in
      make ?createdTimestamp ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about an email template."]
module ReceiptRuleSetMetadata =
  struct
    type nonrec t =
      {
      name: ReceiptRuleSetName.t option
        [@ocaml.doc
          "The name of the receipt rule set. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Start and end with a letter or number. Contain less than 64 characters."];
      createdTimestamp: Timestamp.t option
        [@ocaml.doc "The date and time the receipt rule set was created."]}
    let make ?name =
      fun ?createdTimestamp -> fun () -> { name; createdTimestamp }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:ReceiptRuleSetName.to_value));
        ("CreatedTimestamp",
          (Option.map x.createdTimestamp ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreatedTimestamp") in
      let name =
        (Option.map ~f:ReceiptRuleSetName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?createdTimestamp ?name ()
    let of_json json =
      let createdTimestamp =
        field_map json "CreatedTimestamp" Timestamp.of_json in
      let name = field_map json "Name" ReceiptRuleSetName.of_json in
      make ?createdTimestamp ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a receipt rule set. A receipt rule set is a collection of rules that specify what Amazon SES should do with mail it receives on behalf of your account's verified domains. For information about setting up receipt rule sets, see the Amazon SES Developer Guide."]
module ReceiptFilter =
  struct
    type nonrec t =
      {
      name: ReceiptFilterName.t
        [@ocaml.doc
          "The name of the IP address filter. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Start and end with a letter or number. Contain less than 64 characters."];
      ipFilter: ReceiptIpFilter.t
        [@ocaml.doc
          "A structure that provides the IP addresses to block or allow, and whether to block or allow incoming mail from them."]}
    let context_ = "ReceiptFilter"
    let make ~name = fun ~ipFilter -> fun () -> { name; ipFilter }
    let to_value x =
      structure_to_value
        [("Name", (Some (ReceiptFilterName.to_value x.name)));
        ("IpFilter", (Some (ReceiptIpFilter.to_value x.ipFilter)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipFilter =
        ReceiptIpFilter.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpFilter") in
      let name =
        ReceiptFilterName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~ipFilter ~name ()
    let of_json json =
      let ipFilter = field_map_exn json "IpFilter" ReceiptIpFilter.of_json in
      let name = field_map_exn json "Name" ReceiptFilterName.of_json in
      make ~ipFilter ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A receipt IP address filter enables you to specify whether to accept or reject mail originating from an IP address or range of IP addresses. For information about setting up IP address filters, see the Amazon SES Developer Guide."]
module PolicyName =
  struct
    type nonrec t = string
    let context_ = "PolicyName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyName" j
    let to_json = simple_to_json to_value
  end
module Identity =
  struct
    type nonrec t = string
    let context_ = "Identity"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Identity" j
    let to_json = simple_to_json to_value
  end
module CustomVerificationEmailTemplate =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t option
        [@ocaml.doc "The name of the custom verification email template."];
      fromEmailAddress: FromAddress.t option
        [@ocaml.doc
          "The email address that the custom verification email is sent from."];
      templateSubject: Subject.t option
        [@ocaml.doc "The subject line of the custom verification email."];
      successRedirectionURL: SuccessRedirectionURL.t option
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is successfully verified."];
      failureRedirectionURL: FailureRedirectionURL.t option
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is not successfully verified."]}
    let make ?templateName =
      fun ?fromEmailAddress ->
        fun ?templateSubject ->
          fun ?successRedirectionURL ->
            fun ?failureRedirectionURL ->
              fun () ->
                {
                  templateName;
                  fromEmailAddress;
                  templateSubject;
                  successRedirectionURL;
                  failureRedirectionURL
                }
    let to_value x =
      structure_to_value
        [("TemplateName",
           (Option.map x.templateName ~f:TemplateName.to_value));
        ("FromEmailAddress",
          (Option.map x.fromEmailAddress ~f:FromAddress.to_value));
        ("TemplateSubject",
          (Option.map x.templateSubject ~f:Subject.to_value));
        ("SuccessRedirectionURL",
          (Option.map x.successRedirectionURL
             ~f:SuccessRedirectionURL.to_value));
        ("FailureRedirectionURL",
          (Option.map x.failureRedirectionURL
             ~f:FailureRedirectionURL.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureRedirectionURL =
        (Option.map ~f:FailureRedirectionURL.of_xml)
          (Xml.child xml_arg0 "FailureRedirectionURL") in
      let successRedirectionURL =
        (Option.map ~f:SuccessRedirectionURL.of_xml)
          (Xml.child xml_arg0 "SuccessRedirectionURL") in
      let templateSubject =
        (Option.map ~f:Subject.of_xml) (Xml.child xml_arg0 "TemplateSubject") in
      let fromEmailAddress =
        (Option.map ~f:FromAddress.of_xml)
          (Xml.child xml_arg0 "FromEmailAddress") in
      let templateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "TemplateName") in
      make ?failureRedirectionURL ?successRedirectionURL ?templateSubject
        ?fromEmailAddress ?templateName ()
    let of_json json =
      let failureRedirectionURL =
        field_map json "FailureRedirectionURL" FailureRedirectionURL.of_json in
      let successRedirectionURL =
        field_map json "SuccessRedirectionURL" SuccessRedirectionURL.of_json in
      let templateSubject = field_map json "TemplateSubject" Subject.of_json in
      let fromEmailAddress =
        field_map json "FromEmailAddress" FromAddress.of_json in
      let templateName = field_map json "TemplateName" TemplateName.of_json in
      make ?failureRedirectionURL ?successRedirectionURL ?templateSubject
        ?fromEmailAddress ?templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a custom verification email template."]
module ConfigurationSet =
  struct
    type nonrec t =
      {
      name: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set. The name must meet the following requirements: Contain only letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Contain 64 characters or fewer."]}
    let context_ = "ConfigurationSet"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Some (ConfigurationSetName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~name ()
    let of_json json =
      let name = field_map_exn json "Name" ConfigurationSetName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The name of the configuration set. Configuration sets let you create groups of rules that you can apply to the emails you send using Amazon SES. For more information about using configuration sets, see Using Amazon SES Configuration Sets in the Amazon SES Developer Guide."]
module SendDataPoint =
  struct
    type nonrec t =
      {
      timestamp: Timestamp.t option [@ocaml.doc "Time of the data point."];
      deliveryAttempts: Counter.t option
        [@ocaml.doc "Number of emails that have been sent."];
      bounces: Counter.t option
        [@ocaml.doc "Number of emails that have bounced."];
      complaints: Counter.t option
        [@ocaml.doc
          "Number of unwanted emails that were rejected by recipients."];
      rejects: Counter.t option
        [@ocaml.doc "Number of emails rejected by Amazon SES."]}
    let make ?timestamp =
      fun ?deliveryAttempts ->
        fun ?bounces ->
          fun ?complaints ->
            fun ?rejects ->
              fun () ->
                { timestamp; deliveryAttempts; bounces; complaints; rejects }
    let to_value x =
      structure_to_value
        [("Timestamp", (Option.map x.timestamp ~f:Timestamp.to_value));
        ("DeliveryAttempts",
          (Option.map x.deliveryAttempts ~f:Counter.to_value));
        ("Bounces", (Option.map x.bounces ~f:Counter.to_value));
        ("Complaints", (Option.map x.complaints ~f:Counter.to_value));
        ("Rejects", (Option.map x.rejects ~f:Counter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rejects =
        (Option.map ~f:Counter.of_xml) (Xml.child xml_arg0 "Rejects") in
      let complaints =
        (Option.map ~f:Counter.of_xml) (Xml.child xml_arg0 "Complaints") in
      let bounces =
        (Option.map ~f:Counter.of_xml) (Xml.child xml_arg0 "Bounces") in
      let deliveryAttempts =
        (Option.map ~f:Counter.of_xml)
          (Xml.child xml_arg0 "DeliveryAttempts") in
      let timestamp =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "Timestamp") in
      make ?rejects ?complaints ?bounces ?deliveryAttempts ?timestamp ()
    let of_json json =
      let rejects = field_map json "Rejects" Counter.of_json in
      let complaints = field_map json "Complaints" Counter.of_json in
      let bounces = field_map json "Bounces" Counter.of_json in
      let deliveryAttempts =
        field_map json "DeliveryAttempts" Counter.of_json in
      let timestamp = field_map json "Timestamp" Timestamp.of_json in
      make ?rejects ?complaints ?bounces ?deliveryAttempts ?timestamp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents sending statistics data. Each SendDataPoint contains statistics for a 15-minute period of sending activity."]
module IdentityVerificationAttributes =
  struct
    type nonrec t =
      {
      verificationStatus: VerificationStatus.t
        [@ocaml.doc
          "The verification status of the identity: \"Pending\", \"Success\", \"Failed\", or \"TemporaryFailure\"."];
      verificationToken: VerificationToken.t option
        [@ocaml.doc
          "The verification token for a domain identity. Null for email address identities."]}
    let context_ = "IdentityVerificationAttributes"
    let make ?verificationToken =
      fun ~verificationStatus ->
        fun () -> { verificationToken; verificationStatus }
    let to_value x =
      structure_to_value
        [("VerificationStatus",
           (Some (VerificationStatus.to_value x.verificationStatus)));
        ("VerificationToken",
          (Option.map x.verificationToken ~f:VerificationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verificationToken =
        (Option.map ~f:VerificationToken.of_xml)
          (Xml.child xml_arg0 "VerificationToken") in
      let verificationStatus =
        VerificationStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VerificationStatus") in
      make ?verificationToken ~verificationStatus ()
    let of_json json =
      let verificationToken =
        field_map json "VerificationToken" VerificationToken.of_json in
      let verificationStatus =
        field_map_exn json "VerificationStatus" VerificationStatus.of_json in
      make ?verificationToken ~verificationStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the verification attributes of a single identity."]
module Policy =
  struct
    type nonrec t = string
    let context_ = "Policy"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Policy" j
    let to_json = simple_to_json to_value
  end
module IdentityNotificationAttributes =
  struct
    type nonrec t =
      {
      bounceTopic: NotificationTopic.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic where Amazon SES will publish bounce notifications."];
      complaintTopic: NotificationTopic.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic where Amazon SES will publish complaint notifications."];
      deliveryTopic: NotificationTopic.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic where Amazon SES will publish delivery notifications."];
      forwardingEnabled: Enabled.t
        [@ocaml.doc
          "Describes whether Amazon SES will forward bounce and complaint notifications as email. true indicates that Amazon SES will forward bounce and complaint notifications as email, while false indicates that bounce and complaint notifications will be published only to the specified bounce and complaint Amazon SNS topics."];
      headersInBounceNotificationsEnabled: Enabled.t option
        [@ocaml.doc
          "Describes whether Amazon SES includes the original email headers in Amazon SNS notifications of type Bounce. A value of true specifies that Amazon SES will include headers in bounce notifications, and a value of false specifies that Amazon SES will not include headers in bounce notifications."];
      headersInComplaintNotificationsEnabled: Enabled.t option
        [@ocaml.doc
          "Describes whether Amazon SES includes the original email headers in Amazon SNS notifications of type Complaint. A value of true specifies that Amazon SES will include headers in complaint notifications, and a value of false specifies that Amazon SES will not include headers in complaint notifications."];
      headersInDeliveryNotificationsEnabled: Enabled.t option
        [@ocaml.doc
          "Describes whether Amazon SES includes the original email headers in Amazon SNS notifications of type Delivery. A value of true specifies that Amazon SES will include headers in delivery notifications, and a value of false specifies that Amazon SES will not include headers in delivery notifications."]}
    let context_ = "IdentityNotificationAttributes"
    let make ?headersInBounceNotificationsEnabled =
      fun ?headersInComplaintNotificationsEnabled ->
        fun ?headersInDeliveryNotificationsEnabled ->
          fun ~bounceTopic ->
            fun ~complaintTopic ->
              fun ~deliveryTopic ->
                fun ~forwardingEnabled ->
                  fun () ->
                    {
                      headersInBounceNotificationsEnabled;
                      headersInComplaintNotificationsEnabled;
                      headersInDeliveryNotificationsEnabled;
                      bounceTopic;
                      complaintTopic;
                      deliveryTopic;
                      forwardingEnabled
                    }
    let to_value x =
      structure_to_value
        [("BounceTopic", (Some (NotificationTopic.to_value x.bounceTopic)));
        ("ComplaintTopic",
          (Some (NotificationTopic.to_value x.complaintTopic)));
        ("DeliveryTopic",
          (Some (NotificationTopic.to_value x.deliveryTopic)));
        ("ForwardingEnabled", (Some (Enabled.to_value x.forwardingEnabled)));
        ("HeadersInBounceNotificationsEnabled",
          (Option.map x.headersInBounceNotificationsEnabled
             ~f:Enabled.to_value));
        ("HeadersInComplaintNotificationsEnabled",
          (Option.map x.headersInComplaintNotificationsEnabled
             ~f:Enabled.to_value));
        ("HeadersInDeliveryNotificationsEnabled",
          (Option.map x.headersInDeliveryNotificationsEnabled
             ~f:Enabled.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let headersInDeliveryNotificationsEnabled =
        (Option.map ~f:Enabled.of_xml)
          (Xml.child xml_arg0 "HeadersInDeliveryNotificationsEnabled") in
      let headersInComplaintNotificationsEnabled =
        (Option.map ~f:Enabled.of_xml)
          (Xml.child xml_arg0 "HeadersInComplaintNotificationsEnabled") in
      let headersInBounceNotificationsEnabled =
        (Option.map ~f:Enabled.of_xml)
          (Xml.child xml_arg0 "HeadersInBounceNotificationsEnabled") in
      let forwardingEnabled =
        Enabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ForwardingEnabled") in
      let deliveryTopic =
        NotificationTopic.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DeliveryTopic") in
      let complaintTopic =
        NotificationTopic.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComplaintTopic") in
      let bounceTopic =
        NotificationTopic.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BounceTopic") in
      make ?headersInDeliveryNotificationsEnabled
        ?headersInComplaintNotificationsEnabled
        ?headersInBounceNotificationsEnabled ~forwardingEnabled
        ~deliveryTopic ~complaintTopic ~bounceTopic ()
    let of_json json =
      let headersInDeliveryNotificationsEnabled =
        field_map json "HeadersInDeliveryNotificationsEnabled"
          Enabled.of_json in
      let headersInComplaintNotificationsEnabled =
        field_map json "HeadersInComplaintNotificationsEnabled"
          Enabled.of_json in
      let headersInBounceNotificationsEnabled =
        field_map json "HeadersInBounceNotificationsEnabled" Enabled.of_json in
      let forwardingEnabled =
        field_map_exn json "ForwardingEnabled" Enabled.of_json in
      let deliveryTopic =
        field_map_exn json "DeliveryTopic" NotificationTopic.of_json in
      let complaintTopic =
        field_map_exn json "ComplaintTopic" NotificationTopic.of_json in
      let bounceTopic =
        field_map_exn json "BounceTopic" NotificationTopic.of_json in
      make ?headersInDeliveryNotificationsEnabled
        ?headersInComplaintNotificationsEnabled
        ?headersInBounceNotificationsEnabled ~forwardingEnabled
        ~deliveryTopic ~complaintTopic ~bounceTopic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the notification attributes of an identity, including whether an identity has Amazon Simple Notification Service (Amazon SNS) topics set for bounce, complaint, and/or delivery notifications, and whether feedback forwarding is enabled for bounce and complaint notifications."]
module IdentityMailFromDomainAttributes =
  struct
    type nonrec t =
      {
      mailFromDomain: MailFromDomainName.t
        [@ocaml.doc
          "The custom MAIL FROM domain that the identity is configured to use."];
      mailFromDomainStatus: CustomMailFromStatus.t
        [@ocaml.doc
          "The state that indicates whether Amazon SES has successfully read the MX record required for custom MAIL FROM domain setup. If the state is Success, Amazon SES uses the specified custom MAIL FROM domain when the verified identity sends an email. All other states indicate that Amazon SES takes the action described by BehaviorOnMXFailure."];
      behaviorOnMXFailure: BehaviorOnMXFailure.t
        [@ocaml.doc
          "The action that Amazon SES takes if it cannot successfully read the required MX record when you send an email. A value of UseDefaultValue indicates that if Amazon SES cannot read the required MX record, it uses amazonses.com (or a subdomain of that) as the MAIL FROM domain. A value of RejectMessage indicates that if Amazon SES cannot read the required MX record, Amazon SES returns a MailFromDomainNotVerified error and does not send the email. The custom MAIL FROM setup states that result in this behavior are Pending, Failed, and TemporaryFailure."]}
    let context_ = "IdentityMailFromDomainAttributes"
    let make ~mailFromDomain =
      fun ~mailFromDomainStatus ->
        fun ~behaviorOnMXFailure ->
          fun () ->
            { mailFromDomain; mailFromDomainStatus; behaviorOnMXFailure }
    let to_value x =
      structure_to_value
        [("MailFromDomain",
           (Some (MailFromDomainName.to_value x.mailFromDomain)));
        ("MailFromDomainStatus",
          (Some (CustomMailFromStatus.to_value x.mailFromDomainStatus)));
        ("BehaviorOnMXFailure",
          (Some (BehaviorOnMXFailure.to_value x.behaviorOnMXFailure)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let behaviorOnMXFailure =
        BehaviorOnMXFailure.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BehaviorOnMXFailure") in
      let mailFromDomainStatus =
        CustomMailFromStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MailFromDomainStatus") in
      let mailFromDomain =
        MailFromDomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MailFromDomain") in
      make ~behaviorOnMXFailure ~mailFromDomainStatus ~mailFromDomain ()
    let of_json json =
      let behaviorOnMXFailure =
        field_map_exn json "BehaviorOnMXFailure" BehaviorOnMXFailure.of_json in
      let mailFromDomainStatus =
        field_map_exn json "MailFromDomainStatus"
          CustomMailFromStatus.of_json in
      let mailFromDomain =
        field_map_exn json "MailFromDomain" MailFromDomainName.of_json in
      make ~behaviorOnMXFailure ~mailFromDomainStatus ~mailFromDomain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the custom MAIL FROM domain attributes of a verified identity (email address or domain)."]
module IdentityDkimAttributes =
  struct
    type nonrec t =
      {
      dkimEnabled: Enabled.t
        [@ocaml.doc
          "Is true if DKIM signing is enabled for email sent from the identity. It's false otherwise. The default value is true."];
      dkimVerificationStatus: VerificationStatus.t
        [@ocaml.doc
          "Describes whether Amazon SES has successfully verified the DKIM DNS records (tokens) published in the domain name's DNS. (This only applies to domain identities, not email address identities.)"];
      dkimTokens: VerificationTokenList.t option
        [@ocaml.doc
          "A set of character strings that represent the domain's identity. Using these tokens, you need to create DNS CNAME records that point to DKIM public keys that are hosted by Amazon SES. Amazon Web Services eventually detects that you've updated your DNS records. This detection process might take up to 72 hours. After successful detection, Amazon SES is able to DKIM-sign email originating from that domain. (This only applies to domain identities, not email address identities.) For more information about creating DNS records using DKIM tokens, see the Amazon SES Developer Guide."]}
    let context_ = "IdentityDkimAttributes"
    let make ?dkimTokens =
      fun ~dkimEnabled ->
        fun ~dkimVerificationStatus ->
          fun () -> { dkimTokens; dkimEnabled; dkimVerificationStatus }
    let to_value x =
      structure_to_value
        [("DkimEnabled", (Some (Enabled.to_value x.dkimEnabled)));
        ("DkimVerificationStatus",
          (Some (VerificationStatus.to_value x.dkimVerificationStatus)));
        ("DkimTokens",
          (Option.map x.dkimTokens ~f:VerificationTokenList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dkimTokens =
        (Option.map ~f:VerificationTokenList.of_xml)
          (Xml.child xml_arg0 "DkimTokens") in
      let dkimVerificationStatus =
        VerificationStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DkimVerificationStatus") in
      let dkimEnabled =
        Enabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DkimEnabled") in
      make ?dkimTokens ~dkimVerificationStatus ~dkimEnabled ()
    let of_json json =
      let dkimTokens =
        field_map json "DkimTokens" VerificationTokenList.of_json in
      let dkimVerificationStatus =
        field_map_exn json "DkimVerificationStatus"
          VerificationStatus.of_json in
      let dkimEnabled = field_map_exn json "DkimEnabled" Enabled.of_json in
      make ?dkimTokens ~dkimVerificationStatus ~dkimEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the DKIM attributes of a verified email address or a domain."]
module ReceiptRule =
  struct
    type nonrec t =
      {
      name: ReceiptRuleName.t
        [@ocaml.doc
          "The name of the receipt rule. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Start and end with a letter or number. Contain less than 64 characters."];
      enabled: Enabled.t option
        [@ocaml.doc
          "If true, the receipt rule is active. The default value is false."];
      tlsPolicy: TlsPolicy.t option
        [@ocaml.doc
          "Specifies whether Amazon SES should require that incoming email is delivered over a connection encrypted with Transport Layer Security (TLS). If this parameter is set to Require, Amazon SES will bounce emails that are not received over TLS. The default is Optional."];
      recipients: RecipientsList.t option
        [@ocaml.doc
          "The recipient domains and email addresses that the receipt rule applies to. If this field is not specified, this rule will match all recipients under all verified domains."];
      actions: ReceiptActionsList.t option
        [@ocaml.doc
          "An ordered list of actions to perform on messages that match at least one of the recipient email addresses or domains specified in the receipt rule."];
      scanEnabled: Enabled.t option
        [@ocaml.doc
          "If true, then messages that this receipt rule applies to are scanned for spam and viruses. The default value is false."]}
    let context_ = "ReceiptRule"
    let make ?enabled =
      fun ?tlsPolicy ->
        fun ?recipients ->
          fun ?actions ->
            fun ?scanEnabled ->
              fun ~name ->
                fun () ->
                  {
                    enabled;
                    tlsPolicy;
                    recipients;
                    actions;
                    scanEnabled;
                    name
                  }
    let to_value x =
      structure_to_value
        [("Name", (Some (ReceiptRuleName.to_value x.name)));
        ("Enabled", (Option.map x.enabled ~f:Enabled.to_value));
        ("TlsPolicy", (Option.map x.tlsPolicy ~f:TlsPolicy.to_value));
        ("Recipients", (Option.map x.recipients ~f:RecipientsList.to_value));
        ("Actions", (Option.map x.actions ~f:ReceiptActionsList.to_value));
        ("ScanEnabled", (Option.map x.scanEnabled ~f:Enabled.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scanEnabled =
        (Option.map ~f:Enabled.of_xml) (Xml.child xml_arg0 "ScanEnabled") in
      let actions =
        (Option.map ~f:ReceiptActionsList.of_xml)
          (Xml.child xml_arg0 "Actions") in
      let recipients =
        (Option.map ~f:RecipientsList.of_xml)
          (Xml.child xml_arg0 "Recipients") in
      let tlsPolicy =
        (Option.map ~f:TlsPolicy.of_xml) (Xml.child xml_arg0 "TlsPolicy") in
      let enabled =
        (Option.map ~f:Enabled.of_xml) (Xml.child xml_arg0 "Enabled") in
      let name =
        ReceiptRuleName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?scanEnabled ?actions ?recipients ?tlsPolicy ?enabled ~name ()
    let of_json json =
      let scanEnabled = field_map json "ScanEnabled" Enabled.of_json in
      let actions = field_map json "Actions" ReceiptActionsList.of_json in
      let recipients = field_map json "Recipients" RecipientsList.of_json in
      let tlsPolicy = field_map json "TlsPolicy" TlsPolicy.of_json in
      let enabled = field_map json "Enabled" Enabled.of_json in
      let name = field_map_exn json "Name" ReceiptRuleName.of_json in
      make ?scanEnabled ?actions ?recipients ?tlsPolicy ?enabled ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Receipt rules enable you to specify which actions Amazon SES should take when it receives mail on behalf of one or more email addresses or domains that you own. Each receipt rule defines a set of email addresses or domains that it applies to. If the email addresses or domains match at least one recipient address of the message, Amazon SES executes all of the receipt rule's actions on the message. For information about setting up receipt rules, see the Amazon SES Developer Guide."]
module EventDestination =
  struct
    type nonrec t =
      {
      name: EventDestinationName.t
        [@ocaml.doc
          "The name of the event destination. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Contain less than 64 characters."];
      enabled: Enabled.t option
        [@ocaml.doc
          "Sets whether Amazon SES publishes events to this destination when you send an email with the associated configuration set. Set to true to enable publishing to this destination; set to false to prevent publishing to this destination. The default value is false."];
      matchingEventTypes: EventTypes.t
        [@ocaml.doc
          "The type of email sending events to publish to the event destination."];
      kinesisFirehoseDestination: KinesisFirehoseDestination.t option
        [@ocaml.doc
          "An object that contains the delivery stream ARN and the IAM role ARN associated with an Amazon Kinesis Firehose event destination."];
      cloudWatchDestination: CloudWatchDestination.t option
        [@ocaml.doc
          "An object that contains the names, default values, and sources of the dimensions associated with an Amazon CloudWatch event destination."];
      sNSDestination: SNSDestination.t option
        [@ocaml.doc
          "An object that contains the topic ARN associated with an Amazon Simple Notification Service (Amazon SNS) event destination."]}
    let context_ = "EventDestination"
    let make ?enabled =
      fun ?kinesisFirehoseDestination ->
        fun ?cloudWatchDestination ->
          fun ?sNSDestination ->
            fun ~name ->
              fun ~matchingEventTypes ->
                fun () ->
                  {
                    enabled;
                    kinesisFirehoseDestination;
                    cloudWatchDestination;
                    sNSDestination;
                    name;
                    matchingEventTypes
                  }
    let to_value x =
      structure_to_value
        [("Name", (Some (EventDestinationName.to_value x.name)));
        ("Enabled", (Option.map x.enabled ~f:Enabled.to_value));
        ("MatchingEventTypes",
          (Some (EventTypes.to_value x.matchingEventTypes)));
        ("KinesisFirehoseDestination",
          (Option.map x.kinesisFirehoseDestination
             ~f:KinesisFirehoseDestination.to_value));
        ("CloudWatchDestination",
          (Option.map x.cloudWatchDestination
             ~f:CloudWatchDestination.to_value));
        ("SNSDestination",
          (Option.map x.sNSDestination ~f:SNSDestination.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sNSDestination =
        (Option.map ~f:SNSDestination.of_xml)
          (Xml.child xml_arg0 "SNSDestination") in
      let cloudWatchDestination =
        (Option.map ~f:CloudWatchDestination.of_xml)
          (Xml.child xml_arg0 "CloudWatchDestination") in
      let kinesisFirehoseDestination =
        (Option.map ~f:KinesisFirehoseDestination.of_xml)
          (Xml.child xml_arg0 "KinesisFirehoseDestination") in
      let matchingEventTypes =
        EventTypes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MatchingEventTypes") in
      let enabled =
        (Option.map ~f:Enabled.of_xml) (Xml.child xml_arg0 "Enabled") in
      let name =
        EventDestinationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?sNSDestination ?cloudWatchDestination ?kinesisFirehoseDestination
        ~matchingEventTypes ?enabled ~name ()
    let of_json json =
      let sNSDestination =
        field_map json "SNSDestination" SNSDestination.of_json in
      let cloudWatchDestination =
        field_map json "CloudWatchDestination" CloudWatchDestination.of_json in
      let kinesisFirehoseDestination =
        field_map json "KinesisFirehoseDestination"
          KinesisFirehoseDestination.of_json in
      let matchingEventTypes =
        field_map_exn json "MatchingEventTypes" EventTypes.of_json in
      let enabled = field_map json "Enabled" Enabled.of_json in
      let name = field_map_exn json "Name" EventDestinationName.of_json in
      make ?sNSDestination ?cloudWatchDestination ?kinesisFirehoseDestination
        ~matchingEventTypes ?enabled ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the event destination that the specified email sending events will be published to. When you create or update an event destination, you must provide one, and only one, destination. The destination can be Amazon CloudWatch, Amazon Kinesis Firehose or Amazon Simple Notification Service (Amazon SNS). Event destinations are associated with configuration sets, which enable you to publish email sending events to Amazon CloudWatch, Amazon Kinesis Firehose, or Amazon Simple Notification Service (Amazon SNS). For information about using configuration sets, see the Amazon SES Developer Guide."]
module LastFreshStart =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ConfigurationSetAttribute =
  struct
    type nonrec t =
      | EventDestinations 
      | TrackingOptions 
      | DeliveryOptions 
      | ReputationOptions 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EventDestinations -> "eventDestinations"
      | TrackingOptions -> "trackingOptions"
      | DeliveryOptions -> "deliveryOptions"
      | ReputationOptions -> "reputationOptions"
      | Non_static_id s -> s
    let of_string =
      function
      | "eventDestinations" -> EventDestinations
      | "trackingOptions" -> TrackingOptions
      | "deliveryOptions" -> DeliveryOptions
      | "reputationOptions" -> ReputationOptions
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConfigurationSetAttribute" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ConfigurationSetAttribute" j)
    let to_json = simple_to_json to_value
  end
module Domain =
  struct
    type nonrec t = string
    let context_ = "Domain"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Domain" j
    let to_json = simple_to_json to_value
  end
module InvalidTemplateException =
  struct
    type nonrec t = {
      templateName: TemplateName.t option }
    let make ?templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName",
           (Option.map x.templateName ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "TemplateName") in
      make ?templateName ()
    let of_json json =
      let templateName = field_map json "TemplateName" TemplateName.of_json in
      make ?templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the template that you specified could not be rendered. This issue may occur when a template refers to a partial that does not exist."]
module TemplateDoesNotExistException =
  struct
    type nonrec t = {
      templateName: TemplateName.t option }
    let make ?templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName",
           (Option.map x.templateName ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "TemplateName") in
      make ?templateName ()
    let of_json json =
      let templateName = field_map json "TemplateName" TemplateName.of_json in
      make ?templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the Template object you specified does not exist in your Amazon SES account."]
module Template =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc
          "The name of the template. You will refer to this name when you send email using the SendTemplatedEmail or SendBulkTemplatedEmail operations."];
      subjectPart: SubjectPart.t option
        [@ocaml.doc "The subject line of the email."];
      textPart: TextPart.t option
        [@ocaml.doc
          "The email body that will be visible to recipients whose email clients do not display HTML."];
      htmlPart: HtmlPart.t option [@ocaml.doc "The HTML body of the email."]}
    let context_ = "Template"
    let make ?subjectPart =
      fun ?textPart ->
        fun ?htmlPart ->
          fun ~templateName ->
            fun () -> { subjectPart; textPart; htmlPart; templateName }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)));
        ("SubjectPart", (Option.map x.subjectPart ~f:SubjectPart.to_value));
        ("TextPart", (Option.map x.textPart ~f:TextPart.to_value));
        ("HtmlPart", (Option.map x.htmlPart ~f:HtmlPart.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let htmlPart =
        (Option.map ~f:HtmlPart.of_xml) (Xml.child xml_arg0 "HtmlPart") in
      let textPart =
        (Option.map ~f:TextPart.of_xml) (Xml.child xml_arg0 "TextPart") in
      let subjectPart =
        (Option.map ~f:SubjectPart.of_xml) (Xml.child xml_arg0 "SubjectPart") in
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ?htmlPart ?textPart ?subjectPart ~templateName ()
    let of_json json =
      let htmlPart = field_map json "HtmlPart" HtmlPart.of_json in
      let textPart = field_map json "TextPart" TextPart.of_json in
      let subjectPart = field_map json "SubjectPart" SubjectPart.of_json in
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ?htmlPart ?textPart ?subjectPart ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The content of the email, composed of a subject line, an HTML part, and a text-only part."]
module InvalidLambdaFunctionException =
  struct
    type nonrec t =
      {
      functionArn: AmazonResourceName.t option
        [@ocaml.doc "Indicates that the ARN of the function was not found."]}
    let make ?functionArn = fun () -> { functionArn }
    let to_value x =
      structure_to_value
        [("FunctionArn",
           (Option.map x.functionArn ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let functionArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "FunctionArn") in
      make ?functionArn ()
    let of_json json =
      let functionArn =
        field_map json "FunctionArn" AmazonResourceName.of_json in
      make ?functionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the provided AWS Lambda function is invalid, or that Amazon SES could not execute the provided function, possibly due to permissions issues. For information about giving permissions, see the Amazon SES Developer Guide."]
module InvalidS3ConfigurationException =
  struct
    type nonrec t =
      {
      bucket: S3BucketName.t option
        [@ocaml.doc "Indicated that the S3 Bucket was not found."]}
    let make ?bucket = fun () -> { bucket }
    let to_value x =
      structure_to_value
        [("Bucket", (Option.map x.bucket ~f:S3BucketName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucket =
        (Option.map ~f:S3BucketName.of_xml) (Xml.child xml_arg0 "Bucket") in
      make ?bucket ()
    let of_json json =
      let bucket = field_map json "Bucket" S3BucketName.of_json in
      make ?bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the provided Amazon S3 bucket or AWS KMS encryption key is invalid, or that Amazon SES could not publish to the bucket, possibly due to permissions issues. For information about giving permissions, see the Amazon SES Developer Guide."]
module InvalidSnsTopicException =
  struct
    type nonrec t =
      {
      topic: AmazonResourceName.t option
        [@ocaml.doc "Indicates that the topic does not exist."]}
    let make ?topic = fun () -> { topic }
    let to_value x =
      structure_to_value
        [("Topic", (Option.map x.topic ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topic =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "Topic") in
      make ?topic ()
    let of_json json =
      let topic = field_map json "Topic" AmazonResourceName.of_json in
      make ?topic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the provided Amazon SNS topic is invalid, or that Amazon SES could not publish to the topic, possibly due to permissions issues. For information about giving permissions, see the Amazon SES Developer Guide."]
module LimitExceededException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that a resource could not be created because of service limits. For a list of Amazon SES limits, see the Amazon SES Developer Guide."]
module RuleDoesNotExistException =
  struct
    type nonrec t =
      {
      name: RuleOrRuleSetName.t option
        [@ocaml.doc "Indicates that the named receipt rule does not exist."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:RuleOrRuleSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:RuleOrRuleSetName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" RuleOrRuleSetName.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Indicates that the provided receipt rule does not exist."]
module RuleSetDoesNotExistException =
  struct
    type nonrec t =
      {
      name: RuleOrRuleSetName.t option
        [@ocaml.doc
          "Indicates that the named receipt rule set does not exist."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:RuleOrRuleSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:RuleOrRuleSetName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" RuleOrRuleSetName.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the provided receipt rule set does not exist."]
module TemplateContent =
  struct
    type nonrec t = string
    let context_ = "TemplateContent"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateContent" j
    let to_json = simple_to_json to_value
  end
module ConfigurationSetDoesNotExistException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."]}
    let make ?configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Indicates that the configuration set does not exist."]
module InvalidTrackingOptionsException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the custom domain to be used for open and click tracking redirects is invalid. This error appears most often in the following situations: When the tracking domain you specified is not verified in Amazon SES. When the tracking domain you specified is not a valid domain or subdomain."]
module TrackingOptionsDoesNotExistException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "Indicates that a TrackingOptions object does not exist in the specified configuration set."]}
    let make ?configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the TrackingOptions object you specified does not exist."]
module TrackingOptions =
  struct
    type nonrec t =
      {
      customRedirectDomain: CustomRedirectDomain.t option
        [@ocaml.doc
          "The custom subdomain that will be used to redirect email recipients to the Amazon SES event tracking domain."]}
    let make ?customRedirectDomain = fun () -> { customRedirectDomain }
    let to_value x =
      structure_to_value
        [("CustomRedirectDomain",
           (Option.map x.customRedirectDomain
              ~f:CustomRedirectDomain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customRedirectDomain =
        (Option.map ~f:CustomRedirectDomain.of_xml)
          (Xml.child xml_arg0 "CustomRedirectDomain") in
      make ?customRedirectDomain ()
    let of_json json =
      let customRedirectDomain =
        field_map json "CustomRedirectDomain" CustomRedirectDomain.of_json in
      make ?customRedirectDomain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A domain that is used to redirect email recipients to an Amazon SES-operated domain. This domain captures open and click events generated by Amazon SES emails. For more information, see Configuring Custom Domains to Handle Open and Click Tracking in the Amazon SES Developer Guide."]
module EventDestinationDoesNotExistException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."];
      eventDestinationName: EventDestinationName.t option
        [@ocaml.doc "Indicates that the event destination does not exist."]}
    let make ?configurationSetName =
      fun ?eventDestinationName ->
        fun () -> { configurationSetName; eventDestinationName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value));
        ("EventDestinationName",
          (Option.map x.eventDestinationName ~f:EventDestinationName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestinationName =
        (Option.map ~f:EventDestinationName.of_xml)
          (Xml.child xml_arg0 "EventDestinationName") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?eventDestinationName ?configurationSetName ()
    let of_json json =
      let eventDestinationName =
        field_map json "EventDestinationName" EventDestinationName.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?eventDestinationName ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Indicates that the event destination does not exist."]
module InvalidCloudWatchDestinationException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."];
      eventDestinationName: EventDestinationName.t option
        [@ocaml.doc "Indicates that the event destination does not exist."]}
    let make ?configurationSetName =
      fun ?eventDestinationName ->
        fun () -> { configurationSetName; eventDestinationName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value));
        ("EventDestinationName",
          (Option.map x.eventDestinationName ~f:EventDestinationName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestinationName =
        (Option.map ~f:EventDestinationName.of_xml)
          (Xml.child xml_arg0 "EventDestinationName") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?eventDestinationName ?configurationSetName ()
    let of_json json =
      let eventDestinationName =
        field_map json "EventDestinationName" EventDestinationName.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?eventDestinationName ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the Amazon CloudWatch destination is invalid. See the error message for details."]
module InvalidFirehoseDestinationException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."];
      eventDestinationName: EventDestinationName.t option
        [@ocaml.doc "Indicates that the event destination does not exist."]}
    let make ?configurationSetName =
      fun ?eventDestinationName ->
        fun () -> { configurationSetName; eventDestinationName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value));
        ("EventDestinationName",
          (Option.map x.eventDestinationName ~f:EventDestinationName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestinationName =
        (Option.map ~f:EventDestinationName.of_xml)
          (Xml.child xml_arg0 "EventDestinationName") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?eventDestinationName ?configurationSetName ()
    let of_json json =
      let eventDestinationName =
        field_map json "EventDestinationName" EventDestinationName.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?eventDestinationName ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the Amazon Kinesis Firehose destination is invalid. See the error message for details."]
module InvalidSNSDestinationException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."];
      eventDestinationName: EventDestinationName.t option
        [@ocaml.doc "Indicates that the event destination does not exist."]}
    let make ?configurationSetName =
      fun ?eventDestinationName ->
        fun () -> { configurationSetName; eventDestinationName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value));
        ("EventDestinationName",
          (Option.map x.eventDestinationName ~f:EventDestinationName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestinationName =
        (Option.map ~f:EventDestinationName.of_xml)
          (Xml.child xml_arg0 "EventDestinationName") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?eventDestinationName ?configurationSetName ()
    let of_json json =
      let eventDestinationName =
        field_map json "EventDestinationName" EventDestinationName.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?eventDestinationName ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the Amazon Simple Notification Service (Amazon SNS) destination is invalid. See the error message for details."]
module InvalidRenderingParameterException =
  struct
    type nonrec t = {
      templateName: TemplateName.t option }
    let make ?templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName",
           (Option.map x.templateName ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "TemplateName") in
      make ?templateName ()
    let of_json json =
      let templateName = field_map json "TemplateName" TemplateName.of_json in
      make ?templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that one or more of the replacement values you provided is invalid. This error may occur when the TemplateData object contains invalid JSON."]
module MissingRenderingAttributeException =
  struct
    type nonrec t = {
      templateName: TemplateName.t option }
    let make ?templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName",
           (Option.map x.templateName ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "TemplateName") in
      make ?templateName ()
    let of_json json =
      let templateName = field_map json "TemplateName" TemplateName.of_json in
      make ?templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that one or more of the replacement values for the specified template was not specified. Ensure that the TemplateData object contains references to all of the replacement tags in the specified template."]
module RenderedTemplate =
  struct
    type nonrec t = string
    let context_ = "RenderedTemplate"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RenderedTemplate" j
    let to_json = simple_to_json to_value
  end
module NotificationType =
  struct
    type nonrec t =
      | Bounce 
      | Complaint 
      | Delivery 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Bounce -> "Bounce"
      | Complaint -> "Complaint"
      | Delivery -> "Delivery"
      | Non_static_id s -> s
    let of_string =
      function
      | "Bounce" -> Bounce
      | "Complaint" -> Complaint
      | "Delivery" -> Delivery
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration NotificationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NotificationType" j)
    let to_json = simple_to_json to_value
  end
module AccountSendingPausedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that email sending is disabled for your entire Amazon SES account. You can enable or disable email sending for your Amazon SES account using UpdateAccountSendingEnabled."]
module ConfigurationSetSendingPausedException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "The name of the configuration set for which email sending is disabled."]}
    let make ?configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that email sending is disabled for the configuration set. You can enable or disable email sending for a configuration set using UpdateConfigurationSetSendingEnabled."]
module MailFromDomainNotVerifiedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the message could not be sent because Amazon SES could not read the MX record required to use the specified MAIL FROM domain. For information about editing the custom MAIL FROM domain settings for an identity, see the Amazon SES Developer Guide."]
module MessageRejected =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the action failed, and the message could not be sent. Check the error stack for more information about what caused the error."]
module RawMessage =
  struct
    type nonrec t =
      {
      data: RawMessageData.t
        [@ocaml.doc
          "The raw data of the message. This data needs to base64-encoded if you are accessing Amazon SES directly through the HTTPS interface. If you are accessing Amazon SES using an AWS SDK, the SDK takes care of the base 64-encoding for you. In all cases, the client must ensure that the message format complies with Internet email standards regarding email header fields, MIME types, and MIME encoding. The To:, CC:, and BCC: headers in the raw message can contain a group list. If you are using SendRawEmail with sending authorization, you can include X-headers in the raw message to specify the \"Source,\" \"From,\" and \"Return-Path\" addresses. For more information, see the documentation for SendRawEmail. Do not include these X-headers in the DKIM signature, because they are removed by Amazon SES before sending the email. For more information, go to the Amazon SES Developer Guide."]}
    let context_ = "RawMessage"
    let make ~data = fun () -> { data }
    let of_header_and_body = ((fun (xs, pipe) -> make ~data:pipe ())
      [@warning "-27"])
    let to_value x =
      structure_to_value [("Data", (Some (RawMessageData.to_value x.data)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let data =
        RawMessageData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Data") in
      make ~data ()
    let of_json json =
      let data = field_map_exn json "Data" RawMessageData.of_json in
      make ~data ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the raw data of the message."]
module Message =
  struct
    type nonrec t =
      {
      subject: Content.t
        [@ocaml.doc
          "The subject of the message: A short summary of the content, which will appear in the recipient's inbox."];
      body: Body.t [@ocaml.doc "The message body."]}
    let context_ = "Message"
    let make ~subject = fun ~body -> fun () -> { subject; body }
    let to_value x =
      structure_to_value
        [("Subject", (Some (Content.to_value x.subject)));
        ("Body", (Some (Body.to_value x.body)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body =
        Body.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Body") in
      let subject =
        Content.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Subject") in
      make ~body ~subject ()
    let of_json json =
      let body = field_map_exn json "Body" Body.of_json in
      let subject = field_map_exn json "Subject" Content.of_json in
      make ~body ~subject ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the message to be sent, composed of a subject and a body."]
module CustomVerificationEmailTemplateDoesNotExistException =
  struct
    type nonrec t =
      {
      customVerificationEmailTemplateName: TemplateName.t option
        [@ocaml.doc
          "Indicates that the provided custom verification email template does not exist."]}
    let make ?customVerificationEmailTemplateName =
      fun () -> { customVerificationEmailTemplateName }
    let to_value x =
      structure_to_value
        [("CustomVerificationEmailTemplateName",
           (Option.map x.customVerificationEmailTemplateName
              ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customVerificationEmailTemplateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "CustomVerificationEmailTemplateName") in
      make ?customVerificationEmailTemplateName ()
    let of_json json =
      let customVerificationEmailTemplateName =
        field_map json "CustomVerificationEmailTemplateName"
          TemplateName.of_json in
      make ?customVerificationEmailTemplateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that a custom verification email template with the name you specified does not exist."]
module FromEmailAddressNotVerifiedException =
  struct
    type nonrec t =
      {
      fromEmailAddress: FromAddress.t option
        [@ocaml.doc
          "Indicates that the from email address associated with the custom verification email template is not verified."]}
    let make ?fromEmailAddress = fun () -> { fromEmailAddress }
    let to_value x =
      structure_to_value
        [("FromEmailAddress",
           (Option.map x.fromEmailAddress ~f:FromAddress.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fromEmailAddress =
        (Option.map ~f:FromAddress.of_xml)
          (Xml.child xml_arg0 "FromEmailAddress") in
      make ?fromEmailAddress ()
    let of_json json =
      let fromEmailAddress =
        field_map json "FromEmailAddress" FromAddress.of_json in
      make ?fromEmailAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the sender address specified for a custom verification email is not verified, and is therefore not eligible to send the custom verification email."]
module ProductionAccessNotGrantedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the account has not been granted production access."]
module BulkEmailDestinationStatusList =
  struct
    type nonrec t = BulkEmailDestinationStatus.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BulkEmailDestinationStatus.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BulkEmailDestinationStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"BulkEmailDestinationStatusList"
        ~of_json:BulkEmailDestinationStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module BulkEmailDestinationList =
  struct
    type nonrec t = BulkEmailDestination.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BulkEmailDestination.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BulkEmailDestination.of_xml)
    let of_json j =
      list_of_json ~kind:"BulkEmailDestinationList"
        ~of_json:BulkEmailDestination.of_json j
    let to_json v = composed_to_json to_value v
  end
module BouncedRecipientInfoList =
  struct
    type nonrec t = BouncedRecipientInfo.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BouncedRecipientInfo.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BouncedRecipientInfo.of_xml)
    let of_json j =
      list_of_json ~kind:"BouncedRecipientInfoList"
        ~of_json:BouncedRecipientInfo.of_json j
    let to_json v = composed_to_json to_value v
  end
module Explanation =
  struct
    type nonrec t = string
    let context_ = "Explanation"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Explanation" j
    let to_json = simple_to_json to_value
  end
module MessageDsn =
  struct
    type nonrec t =
      {
      reportingMta: ReportingMta.t
        [@ocaml.doc
          "The reporting MTA that attempted to deliver the message, formatted as specified in RFC 3464 (mta-name-type; mta-name). The default value is dns; inbound-smtp.\\[region\\].amazonaws.com."];
      arrivalDate: ArrivalDate.t option
        [@ocaml.doc
          "When the message was received by the reporting mail transfer agent (MTA), in RFC 822 date-time format."];
      extensionFields: ExtensionFieldList.t option
        [@ocaml.doc "Additional X-headers to include in the DSN."]}
    let context_ = "MessageDsn"
    let make ?arrivalDate =
      fun ?extensionFields ->
        fun ~reportingMta ->
          fun () -> { arrivalDate; extensionFields; reportingMta }
    let to_value x =
      structure_to_value
        [("ReportingMta", (Some (ReportingMta.to_value x.reportingMta)));
        ("ArrivalDate", (Option.map x.arrivalDate ~f:ArrivalDate.to_value));
        ("ExtensionFields",
          (Option.map x.extensionFields ~f:ExtensionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let extensionFields =
        (Option.map ~f:ExtensionFieldList.of_xml)
          (Xml.child xml_arg0 "ExtensionFields") in
      let arrivalDate =
        (Option.map ~f:ArrivalDate.of_xml) (Xml.child xml_arg0 "ArrivalDate") in
      let reportingMta =
        ReportingMta.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportingMta") in
      make ?extensionFields ?arrivalDate ~reportingMta ()
    let of_json json =
      let extensionFields =
        field_map json "ExtensionFields" ExtensionFieldList.of_json in
      let arrivalDate = field_map json "ArrivalDate" ArrivalDate.of_json in
      let reportingMta =
        field_map_exn json "ReportingMta" ReportingMta.of_json in
      make ?extensionFields ?arrivalDate ~reportingMta ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Message-related information to include in the Delivery Status Notification (DSN) when an email that Amazon SES receives on your behalf bounces. For information about receiving email through Amazon SES, see the Amazon SES Developer Guide."]
module ReceiptRuleNamesList =
  struct
    type nonrec t = ReceiptRuleName.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReceiptRuleName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReceiptRuleName.of_xml)
    let of_json j =
      list_of_json ~kind:"ReceiptRuleNamesList"
        ~of_json:ReceiptRuleName.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidPolicyException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the provided policy is invalid. Check the error stack for more information about what caused the error."]
module InvalidDeliveryOptionsException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Indicates that provided delivery option is invalid."]
module DeliveryOptions =
  struct
    type nonrec t =
      {
      tlsPolicy: TlsPolicy.t option
        [@ocaml.doc
          "Specifies whether messages that use the configuration set are required to use Transport Layer Security (TLS). If the value is Require, messages are only delivered if a TLS connection can be established. If the value is Optional, messages can be delivered in plain text if a TLS connection can't be established."]}
    let make ?tlsPolicy = fun () -> { tlsPolicy }
    let to_value x =
      structure_to_value
        [("TlsPolicy", (Option.map x.tlsPolicy ~f:TlsPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tlsPolicy =
        (Option.map ~f:TlsPolicy.of_xml) (Xml.child xml_arg0 "TlsPolicy") in
      make ?tlsPolicy ()
    let of_json json =
      let tlsPolicy = field_map json "TlsPolicy" TlsPolicy.of_json in
      make ?tlsPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies whether messages that use the configuration set are required to use Transport Layer Security (TLS)."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module TemplateMetadataList =
  struct
    type nonrec t = TemplateMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TemplateMetadata.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateMetadataList"
        ~of_json:TemplateMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxItems =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for MaxItems" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ReceiptRuleSetsLists =
  struct
    type nonrec t = ReceiptRuleSetMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReceiptRuleSetMetadata.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReceiptRuleSetMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"ReceiptRuleSetsLists"
        ~of_json:ReceiptRuleSetMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReceiptFilterList =
  struct
    type nonrec t = ReceiptFilter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReceiptFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReceiptFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ReceiptFilterList" ~of_json:ReceiptFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyNameList =
  struct
    type nonrec t = PolicyName.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PolicyName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyName.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyNameList" ~of_json:PolicyName.of_json j
    let to_json v = composed_to_json to_value v
  end
module IdentityList =
  struct
    type nonrec t = Identity.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Identity.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Identity.of_xml)
    let of_json j =
      list_of_json ~kind:"IdentityList" ~of_json:Identity.of_json j
    let to_json v = composed_to_json to_value v
  end
module IdentityType =
  struct
    type nonrec t =
      | EmailAddress 
      | Domain 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EmailAddress -> "EmailAddress"
      | Domain -> "Domain"
      | Non_static_id s -> s
    let of_string =
      function
      | "EmailAddress" -> EmailAddress
      | "Domain" -> Domain
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IdentityType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IdentityType" j)
    let to_json = simple_to_json to_value
  end
module CustomVerificationEmailTemplates =
  struct
    type nonrec t = CustomVerificationEmailTemplate.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CustomVerificationEmailTemplate.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CustomVerificationEmailTemplate.of_xml)
    let of_json j =
      list_of_json ~kind:"CustomVerificationEmailTemplates"
        ~of_json:CustomVerificationEmailTemplate.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ConfigurationSets =
  struct
    type nonrec t = ConfigurationSet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ConfigurationSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConfigurationSet.of_xml)
    let of_json j =
      list_of_json ~kind:"ConfigurationSets"
        ~of_json:ConfigurationSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module SendDataPointList =
  struct
    type nonrec t = SendDataPoint.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SendDataPoint.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SendDataPoint.of_xml)
    let of_json j =
      list_of_json ~kind:"SendDataPointList" ~of_json:SendDataPoint.of_json j
    let to_json v = composed_to_json to_value v
  end
module Max24HourSend =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module MaxSendRate =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module SentLast24Hours =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module VerificationAttributes =
  struct
    type nonrec t = (Identity.t * IdentityVerificationAttributes.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types Identity IdentityVerificationAttributes"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (Identity.to_value x) |>
                    (fun x ->
                       (IdentityVerificationAttributes.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:Identity.of_string
        ~of_json:IdentityVerificationAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyMap =
  struct
    type nonrec t = (PolicyName.t * Policy.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((PolicyName.of_string chopped),
                              (Policy.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (PolicyName.to_value x) |>
                    (fun x -> (Policy.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:PolicyName.of_string
        ~of_json:Policy.of_json j
    let to_json v = composed_to_json to_value v
  end
module NotificationAttributes =
  struct
    type nonrec t = (Identity.t * IdentityNotificationAttributes.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types Identity IdentityNotificationAttributes"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (Identity.to_value x) |>
                    (fun x ->
                       (IdentityNotificationAttributes.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:Identity.of_string
        ~of_json:IdentityNotificationAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module MailFromDomainAttributes =
  struct
    type nonrec t = (Identity.t * IdentityMailFromDomainAttributes.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types Identity IdentityMailFromDomainAttributes"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (Identity.to_value x) |>
                    (fun x ->
                       (IdentityMailFromDomainAttributes.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:Identity.of_string
        ~of_json:IdentityMailFromDomainAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module DkimAttributes =
  struct
    type nonrec t = (Identity.t * IdentityDkimAttributes.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types Identity IdentityDkimAttributes"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (Identity.to_value x) |>
                    (fun x ->
                       (IdentityDkimAttributes.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:Identity.of_string
        ~of_json:IdentityDkimAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReceiptRulesList =
  struct
    type nonrec t = ReceiptRule.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReceiptRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReceiptRule.of_xml)
    let of_json j =
      list_of_json ~kind:"ReceiptRulesList" ~of_json:ReceiptRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventDestinations =
  struct
    type nonrec t = EventDestination.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EventDestination.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventDestination.of_xml)
    let of_json j =
      list_of_json ~kind:"EventDestinations"
        ~of_json:EventDestination.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReputationOptions =
  struct
    type nonrec t =
      {
      sendingEnabled: Enabled.t option
        [@ocaml.doc
          "Describes whether email sending is enabled or disabled for the configuration set. If the value is true, then Amazon SES will send emails that use the configuration set. If the value is false, Amazon SES will not send emails that use the configuration set. The default value is true. You can change this setting using UpdateConfigurationSetSendingEnabled."];
      reputationMetricsEnabled: Enabled.t option
        [@ocaml.doc
          "Describes whether or not Amazon SES publishes reputation metrics for the configuration set, such as bounce and complaint rates, to Amazon CloudWatch. If the value is true, reputation metrics are published. If the value is false, reputation metrics are not published. The default value is false."];
      lastFreshStart: LastFreshStart.t option
        [@ocaml.doc
          "The date and time at which the reputation metrics for the configuration set were last reset. Resetting these metrics is known as a fresh start. When you disable email sending for a configuration set using UpdateConfigurationSetSendingEnabled and later re-enable it, the reputation metrics for the configuration set (but not for the entire Amazon SES account) are reset. If email sending for the configuration set has never been disabled and later re-enabled, the value of this attribute is null."]}
    let make ?sendingEnabled =
      fun ?reputationMetricsEnabled ->
        fun ?lastFreshStart ->
          fun () ->
            { sendingEnabled; reputationMetricsEnabled; lastFreshStart }
    let to_value x =
      structure_to_value
        [("SendingEnabled",
           (Option.map x.sendingEnabled ~f:Enabled.to_value));
        ("ReputationMetricsEnabled",
          (Option.map x.reputationMetricsEnabled ~f:Enabled.to_value));
        ("LastFreshStart",
          (Option.map x.lastFreshStart ~f:LastFreshStart.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastFreshStart =
        (Option.map ~f:LastFreshStart.of_xml)
          (Xml.child xml_arg0 "LastFreshStart") in
      let reputationMetricsEnabled =
        (Option.map ~f:Enabled.of_xml)
          (Xml.child xml_arg0 "ReputationMetricsEnabled") in
      let sendingEnabled =
        (Option.map ~f:Enabled.of_xml) (Xml.child xml_arg0 "SendingEnabled") in
      make ?lastFreshStart ?reputationMetricsEnabled ?sendingEnabled ()
    let of_json json =
      let lastFreshStart =
        field_map json "LastFreshStart" LastFreshStart.of_json in
      let reputationMetricsEnabled =
        field_map json "ReputationMetricsEnabled" Enabled.of_json in
      let sendingEnabled = field_map json "SendingEnabled" Enabled.of_json in
      make ?lastFreshStart ?reputationMetricsEnabled ?sendingEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the reputation settings for a configuration set."]
module ConfigurationSetAttributeList =
  struct
    type nonrec t = ConfigurationSetAttribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ConfigurationSetAttribute.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConfigurationSetAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"ConfigurationSetAttributeList"
        ~of_json:ConfigurationSetAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module CannotDeleteException =
  struct
    type nonrec t =
      {
      name: RuleOrRuleSetName.t option
        [@ocaml.doc
          "Indicates that a resource could not be deleted because no resource with the specified name exists."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:RuleOrRuleSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:RuleOrRuleSetName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" RuleOrRuleSetName.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the delete operation could not be completed."]
module AlreadyExistsException =
  struct
    type nonrec t =
      {
      name: RuleOrRuleSetName.t option
        [@ocaml.doc
          "Indicates that a resource could not be created because the resource name already exists."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:RuleOrRuleSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:RuleOrRuleSetName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" RuleOrRuleSetName.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that a resource could not be created because of a naming conflict."]
module TrackingOptionsAlreadyExistsException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "Indicates that a TrackingOptions object already exists in the specified configuration set."]}
    let make ?configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the configuration set you specified already contains a TrackingOptions object."]
module ConfigurationSetAlreadyExistsException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."]}
    let make ?configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the configuration set could not be created because of a naming conflict."]
module InvalidConfigurationSetException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the configuration set is invalid. See the error message for details."]
module EventDestinationAlreadyExistsException =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc "Indicates that the configuration set does not exist."];
      eventDestinationName: EventDestinationName.t option
        [@ocaml.doc "Indicates that the event destination does not exist."]}
    let make ?configurationSetName =
      fun ?eventDestinationName ->
        fun () -> { configurationSetName; eventDestinationName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Option.map x.configurationSetName
              ~f:ConfigurationSetName.to_value));
        ("EventDestinationName",
          (Option.map x.eventDestinationName ~f:EventDestinationName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestinationName =
        (Option.map ~f:EventDestinationName.of_xml)
          (Xml.child xml_arg0 "EventDestinationName") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      make ?eventDestinationName ?configurationSetName ()
    let of_json json =
      let eventDestinationName =
        field_map json "EventDestinationName" EventDestinationName.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      make ?eventDestinationName ?configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the event destination could not be created because of a naming conflict."]
module VerifyEmailIdentityResponse =
  struct
    type verifyEmailIdentityResult = unit
    and responseMetaData = unit
    and t =
      {
      verifyEmailIdentityResult: verifyEmailIdentityResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () = { verifyEmailIdentityResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module VerifyEmailIdentityRequest =
  struct
    type nonrec t =
      {
      emailAddress: Address.t
        [@ocaml.doc "The email address to be verified."]}
    let context_ = "VerifyEmailIdentityRequest"
    let make ~emailAddress = fun () -> { emailAddress }
    let to_value x =
      structure_to_value
        [("EmailAddress", (Some (Address.to_value x.emailAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emailAddress =
        Address.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EmailAddress") in
      make ~emailAddress ()
    let of_json json =
      let emailAddress = field_map_exn json "EmailAddress" Address.of_json in
      make ~emailAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to begin email address verification with Amazon SES. For information about email address verification, see the Amazon SES Developer Guide."]
module VerifyEmailAddressRequest =
  struct
    type nonrec t =
      {
      emailAddress: Address.t
        [@ocaml.doc "The email address to be verified."]}
    let context_ = "VerifyEmailAddressRequest"
    let make ~emailAddress = fun () -> { emailAddress }
    let to_value x =
      structure_to_value
        [("EmailAddress", (Some (Address.to_value x.emailAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emailAddress =
        Address.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EmailAddress") in
      make ~emailAddress ()
    let of_json json =
      let emailAddress = field_map_exn json "EmailAddress" Address.of_json in
      make ~emailAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to begin email address verification with Amazon SES. For information about email address verification, see the Amazon SES Developer Guide."]
module VerifyDomainIdentityResponse =
  struct
    type verifyDomainIdentityResult =
      {
      verificationToken: VerificationToken.t
        [@ocaml.doc
          "A TXT record that you must place in the DNS settings of the domain to complete domain verification with Amazon SES. As Amazon SES searches for the TXT record, the domain's verification status is \"Pending\". When Amazon SES detects the record, the domain's verification status changes to \"Success\". If Amazon SES is unable to detect the record within 72 hours, the domain's verification status changes to \"Failed.\" In that case, if you still want to verify the domain, you must restart the verification process from the beginning."]}
    and responseMetaData = unit
    and t =
      {
      verifyDomainIdentityResult: verifyDomainIdentityResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "VerifyDomainIdentityResponse"
    let make ~verificationToken =
      fun () ->
        {
          verifyDomainIdentityResult = { verificationToken };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.verifyDomainIdentityResult in
      structure_to_wrapped_value
        [("VerificationToken",
           (Some (VerificationToken.to_value x.verificationToken)))]
        ~wrapper:"VerifyDomainIdentityResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "VerifyDomainIdentityResult" in
      let verificationToken =
        VerificationToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VerificationToken") in
      make ~verificationToken ()
    let of_json json =
      let verificationToken =
        field_map_exn json "VerificationToken" VerificationToken.of_json in
      make ~verificationToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a TXT record that you must publish to the DNS server of your domain to complete domain verification with Amazon SES."]
module VerifyDomainIdentityRequest =
  struct
    type nonrec t =
      {
      domain: Domain.t [@ocaml.doc "The domain to be verified."]}
    let context_ = "VerifyDomainIdentityRequest"
    let make ~domain = fun () -> { domain }
    let to_value x =
      structure_to_value [("Domain", (Some (Domain.to_value x.domain)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        Domain.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Domain") in
      make ~domain ()
    let of_json json =
      let domain = field_map_exn json "Domain" Domain.of_json in
      make ~domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to begin Amazon SES domain verification and to generate the TXT records that you must publish to the DNS server of your domain to complete the verification. For information about domain verification, see the Amazon SES Developer Guide."]
module VerifyDomainDkimResponse =
  struct
    type verifyDomainDkimResult =
      {
      dkimTokens: VerificationTokenList.t
        [@ocaml.doc
          "A set of character strings that represent the domain's identity. If the identity is an email address, the tokens represent the domain of that address. Using these tokens, you need to create DNS CNAME records that point to DKIM public keys that are hosted by Amazon SES. Amazon Web Services eventually detects that you've updated your DNS records. This detection process might take up to 72 hours. After successful detection, Amazon SES is able to DKIM-sign email originating from that domain. (This only applies to domain identities, not email address identities.) For more information about creating DNS records using DKIM tokens, see the Amazon SES Developer Guide."]}
    and responseMetaData = unit
    and t =
      {
      verifyDomainDkimResult: verifyDomainDkimResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "VerifyDomainDkimResponse"
    let make ~dkimTokens =
      fun () ->
        { verifyDomainDkimResult = { dkimTokens }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.verifyDomainDkimResult in
      structure_to_wrapped_value
        [("DkimTokens", (Some (VerificationTokenList.to_value x.dkimTokens)))]
        ~wrapper:"VerifyDomainDkimResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "VerifyDomainDkimResult" in
      let dkimTokens =
        VerificationTokenList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DkimTokens") in
      make ~dkimTokens ()
    let of_json json =
      let dkimTokens =
        field_map_exn json "DkimTokens" VerificationTokenList.of_json in
      make ~dkimTokens ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns CNAME records that you must publish to the DNS server of your domain to set up Easy DKIM with Amazon SES."]
module VerifyDomainDkimRequest =
  struct
    type nonrec t =
      {
      domain: Domain.t
        [@ocaml.doc
          "The name of the domain to be verified for Easy DKIM signing."]}
    let context_ = "VerifyDomainDkimRequest"
    let make ~domain = fun () -> { domain }
    let to_value x =
      structure_to_value [("Domain", (Some (Domain.to_value x.domain)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        Domain.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Domain") in
      make ~domain ()
    let of_json json =
      let domain = field_map_exn json "Domain" Domain.of_json in
      make ~domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to generate the CNAME records needed to set up Easy DKIM with Amazon SES. For more information about setting up Easy DKIM, see the Amazon SES Developer Guide."]
module UpdateTemplateResponse =
  struct
    type updateTemplateResult = unit
    and responseMetaData = unit
    and t =
      {
      updateTemplateResult: updateTemplateResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidTemplateException of InvalidTemplateException.t 
      | `TemplateDoesNotExistException of TemplateDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { updateTemplateResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "InvalidTemplateException" ->
          `InvalidTemplateException (InvalidTemplateException.of_json json)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidTemplateException" ->
          `InvalidTemplateException (InvalidTemplateException.of_xml xml)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidTemplateException e ->
          `Assoc
            [("error", (`String "InvalidTemplateException"));
            ("details", (InvalidTemplateException.to_json e))]
      | `TemplateDoesNotExistException e ->
          `Assoc
            [("error", (`String "TemplateDoesNotExistException"));
            ("details", (TemplateDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an email template. Email templates enable you to send personalized email to one or more destinations in a single API operation. For more information, see the Amazon SES Developer Guide. You can execute this operation no more than once per second."]
module UpdateTemplateRequest =
  struct
    type nonrec t = {
      template: Template.t }
    let context_ = "UpdateTemplateRequest"
    let make ~template = fun () -> { template }
    let to_value x =
      structure_to_value
        [("Template", (Some (Template.to_value x.template)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let template =
        Template.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Template") in
      make ~template ()
    let of_json json =
      let template = field_map_exn json "Template" Template.of_json in
      make ~template ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an email template. Email templates enable you to send personalized email to one or more destinations in a single API operation. For more information, see the Amazon SES Developer Guide. You can execute this operation no more than once per second."]
module UpdateReceiptRuleResponse =
  struct
    type updateReceiptRuleResult = unit
    and responseMetaData = unit
    and t =
      {
      updateReceiptRuleResult: updateReceiptRuleResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidLambdaFunctionException of InvalidLambdaFunctionException.t 
      | `InvalidS3ConfigurationException of InvalidS3ConfigurationException.t 
      | `InvalidSnsTopicException of InvalidSnsTopicException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `RuleDoesNotExistException of RuleDoesNotExistException.t 
      | `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { updateReceiptRuleResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "InvalidLambdaFunctionException" ->
          `InvalidLambdaFunctionException
            (InvalidLambdaFunctionException.of_json json)
      | "InvalidS3ConfigurationException" ->
          `InvalidS3ConfigurationException
            (InvalidS3ConfigurationException.of_json json)
      | "InvalidSnsTopicException" ->
          `InvalidSnsTopicException (InvalidSnsTopicException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_json json)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidLambdaFunctionException" ->
          `InvalidLambdaFunctionException
            (InvalidLambdaFunctionException.of_xml xml)
      | "InvalidS3ConfigurationException" ->
          `InvalidS3ConfigurationException
            (InvalidS3ConfigurationException.of_xml xml)
      | "InvalidSnsTopicException" ->
          `InvalidSnsTopicException (InvalidSnsTopicException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_xml xml)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidLambdaFunctionException e ->
          `Assoc
            [("error", (`String "InvalidLambdaFunctionException"));
            ("details", (InvalidLambdaFunctionException.to_json e))]
      | `InvalidS3ConfigurationException e ->
          `Assoc
            [("error", (`String "InvalidS3ConfigurationException"));
            ("details", (InvalidS3ConfigurationException.to_json e))]
      | `InvalidSnsTopicException e ->
          `Assoc
            [("error", (`String "InvalidSnsTopicException"));
            ("details", (InvalidSnsTopicException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `RuleDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleDoesNotExistException"));
            ("details", (RuleDoesNotExistException.to_json e))]
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module UpdateReceiptRuleRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the receipt rule set that the receipt rule belongs to."];
      rule: ReceiptRule.t
        [@ocaml.doc
          "A data structure that contains the updated receipt rule information."]}
    let context_ = "UpdateReceiptRuleRequest"
    let make ~ruleSetName = fun ~rule -> fun () -> { ruleSetName; rule }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("Rule", (Some (ReceiptRule.to_value x.rule)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rule =
        ReceiptRule.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~rule ~ruleSetName ()
    let of_json json =
      let rule = field_map_exn json "Rule" ReceiptRule.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~rule ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to update a receipt rule. You use receipt rules to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module UpdateCustomVerificationEmailTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc
          "The name of the custom verification email template that you want to update."];
      fromEmailAddress: FromAddress.t option
        [@ocaml.doc
          "The email address that the custom verification email is sent from."];
      templateSubject: Subject.t option
        [@ocaml.doc "The subject line of the custom verification email."];
      templateContent: TemplateContent.t option
        [@ocaml.doc
          "The content of the custom verification email. The total size of the email must be less than 10 MB. The message body may contain HTML, with some limitations. For more information, see Custom Verification Email Frequently Asked Questions in the Amazon SES Developer Guide."];
      successRedirectionURL: SuccessRedirectionURL.t option
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is successfully verified."];
      failureRedirectionURL: FailureRedirectionURL.t option
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is not successfully verified."]}
    let context_ = "UpdateCustomVerificationEmailTemplateRequest"
    let make ?fromEmailAddress =
      fun ?templateSubject ->
        fun ?templateContent ->
          fun ?successRedirectionURL ->
            fun ?failureRedirectionURL ->
              fun ~templateName ->
                fun () ->
                  {
                    fromEmailAddress;
                    templateSubject;
                    templateContent;
                    successRedirectionURL;
                    failureRedirectionURL;
                    templateName
                  }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)));
        ("FromEmailAddress",
          (Option.map x.fromEmailAddress ~f:FromAddress.to_value));
        ("TemplateSubject",
          (Option.map x.templateSubject ~f:Subject.to_value));
        ("TemplateContent",
          (Option.map x.templateContent ~f:TemplateContent.to_value));
        ("SuccessRedirectionURL",
          (Option.map x.successRedirectionURL
             ~f:SuccessRedirectionURL.to_value));
        ("FailureRedirectionURL",
          (Option.map x.failureRedirectionURL
             ~f:FailureRedirectionURL.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureRedirectionURL =
        (Option.map ~f:FailureRedirectionURL.of_xml)
          (Xml.child xml_arg0 "FailureRedirectionURL") in
      let successRedirectionURL =
        (Option.map ~f:SuccessRedirectionURL.of_xml)
          (Xml.child xml_arg0 "SuccessRedirectionURL") in
      let templateContent =
        (Option.map ~f:TemplateContent.of_xml)
          (Xml.child xml_arg0 "TemplateContent") in
      let templateSubject =
        (Option.map ~f:Subject.of_xml) (Xml.child xml_arg0 "TemplateSubject") in
      let fromEmailAddress =
        (Option.map ~f:FromAddress.of_xml)
          (Xml.child xml_arg0 "FromEmailAddress") in
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ?failureRedirectionURL ?successRedirectionURL ?templateContent
        ?templateSubject ?fromEmailAddress ~templateName ()
    let of_json json =
      let failureRedirectionURL =
        field_map json "FailureRedirectionURL" FailureRedirectionURL.of_json in
      let successRedirectionURL =
        field_map json "SuccessRedirectionURL" SuccessRedirectionURL.of_json in
      let templateContent =
        field_map json "TemplateContent" TemplateContent.of_json in
      let templateSubject = field_map json "TemplateSubject" Subject.of_json in
      let fromEmailAddress =
        field_map json "FromEmailAddress" FromAddress.of_json in
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ?failureRedirectionURL ?successRedirectionURL ?templateContent
        ?templateSubject ?fromEmailAddress ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to update an existing custom verification email template."]
module UpdateConfigurationSetTrackingOptionsResponse =
  struct
    type updateConfigurationSetTrackingOptionsResult = unit
    and responseMetaData = unit
    and t =
      {
      updateConfigurationSetTrackingOptionsResult:
        updateConfigurationSetTrackingOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `InvalidTrackingOptionsException of InvalidTrackingOptionsException.t 
      | `TrackingOptionsDoesNotExistException of
          TrackingOptionsDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        updateConfigurationSetTrackingOptionsResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "InvalidTrackingOptionsException" ->
          `InvalidTrackingOptionsException
            (InvalidTrackingOptionsException.of_json json)
      | "TrackingOptionsDoesNotExistException" ->
          `TrackingOptionsDoesNotExistException
            (TrackingOptionsDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "InvalidTrackingOptionsException" ->
          `InvalidTrackingOptionsException
            (InvalidTrackingOptionsException.of_xml xml)
      | "TrackingOptionsDoesNotExistException" ->
          `TrackingOptionsDoesNotExistException
            (TrackingOptionsDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `InvalidTrackingOptionsException e ->
          `Assoc
            [("error", (`String "InvalidTrackingOptionsException"));
            ("details", (InvalidTrackingOptionsException.to_json e))]
      | `TrackingOptionsDoesNotExistException e ->
          `Assoc
            [("error", (`String "TrackingOptionsDoesNotExistException"));
            ("details", (TrackingOptionsDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module UpdateConfigurationSetTrackingOptionsRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set for which you want to update the custom tracking domain."];
      trackingOptions: TrackingOptions.t }
    let context_ = "UpdateConfigurationSetTrackingOptionsRequest"
    let make ~configurationSetName =
      fun ~trackingOptions ->
        fun () -> { configurationSetName; trackingOptions }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("TrackingOptions",
          (Some (TrackingOptions.to_value x.trackingOptions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trackingOptions =
        TrackingOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrackingOptions") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~trackingOptions ~configurationSetName ()
    let of_json json =
      let trackingOptions =
        field_map_exn json "TrackingOptions" TrackingOptions.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~trackingOptions ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to update the tracking options for a configuration set."]
module UpdateConfigurationSetSendingEnabledRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set that you want to update."];
      enabled: Enabled.t
        [@ocaml.doc
          "Describes whether email sending is enabled or disabled for the configuration set."]}
    let context_ = "UpdateConfigurationSetSendingEnabledRequest"
    let make ~configurationSetName =
      fun ~enabled -> fun () -> { configurationSetName; enabled }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("Enabled", (Some (Enabled.to_value x.enabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        Enabled.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Enabled") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~enabled ~configurationSetName ()
    let of_json json =
      let enabled = field_map_exn json "Enabled" Enabled.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~enabled ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to enable or disable the email sending capabilities for a specific configuration set."]
module UpdateConfigurationSetReputationMetricsEnabledRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set that you want to update."];
      enabled: Enabled.t
        [@ocaml.doc
          "Describes whether or not Amazon SES will publish reputation metrics for the configuration set, such as bounce and complaint rates, to Amazon CloudWatch."]}
    let context_ = "UpdateConfigurationSetReputationMetricsEnabledRequest"
    let make ~configurationSetName =
      fun ~enabled -> fun () -> { configurationSetName; enabled }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("Enabled", (Some (Enabled.to_value x.enabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        Enabled.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Enabled") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~enabled ~configurationSetName ()
    let of_json json =
      let enabled = field_map_exn json "Enabled" Enabled.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~enabled ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to modify the reputation metric publishing settings for a configuration set."]
module UpdateConfigurationSetEventDestinationResponse =
  struct
    type updateConfigurationSetEventDestinationResult = unit
    and responseMetaData = unit
    and t =
      {
      updateConfigurationSetEventDestinationResult:
        updateConfigurationSetEventDestinationResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `EventDestinationDoesNotExistException of
          EventDestinationDoesNotExistException.t 
      | `InvalidCloudWatchDestinationException of
          InvalidCloudWatchDestinationException.t 
      | `InvalidFirehoseDestinationException of
          InvalidFirehoseDestinationException.t 
      | `InvalidSNSDestinationException of InvalidSNSDestinationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        updateConfigurationSetEventDestinationResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "EventDestinationDoesNotExistException" ->
          `EventDestinationDoesNotExistException
            (EventDestinationDoesNotExistException.of_json json)
      | "InvalidCloudWatchDestinationException" ->
          `InvalidCloudWatchDestinationException
            (InvalidCloudWatchDestinationException.of_json json)
      | "InvalidFirehoseDestinationException" ->
          `InvalidFirehoseDestinationException
            (InvalidFirehoseDestinationException.of_json json)
      | "InvalidSNSDestinationException" ->
          `InvalidSNSDestinationException
            (InvalidSNSDestinationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "EventDestinationDoesNotExistException" ->
          `EventDestinationDoesNotExistException
            (EventDestinationDoesNotExistException.of_xml xml)
      | "InvalidCloudWatchDestinationException" ->
          `InvalidCloudWatchDestinationException
            (InvalidCloudWatchDestinationException.of_xml xml)
      | "InvalidFirehoseDestinationException" ->
          `InvalidFirehoseDestinationException
            (InvalidFirehoseDestinationException.of_xml xml)
      | "InvalidSNSDestinationException" ->
          `InvalidSNSDestinationException
            (InvalidSNSDestinationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `EventDestinationDoesNotExistException e ->
          `Assoc
            [("error", (`String "EventDestinationDoesNotExistException"));
            ("details", (EventDestinationDoesNotExistException.to_json e))]
      | `InvalidCloudWatchDestinationException e ->
          `Assoc
            [("error", (`String "InvalidCloudWatchDestinationException"));
            ("details", (InvalidCloudWatchDestinationException.to_json e))]
      | `InvalidFirehoseDestinationException e ->
          `Assoc
            [("error", (`String "InvalidFirehoseDestinationException"));
            ("details", (InvalidFirehoseDestinationException.to_json e))]
      | `InvalidSNSDestinationException e ->
          `Assoc
            [("error", (`String "InvalidSNSDestinationException"));
            ("details", (InvalidSNSDestinationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module UpdateConfigurationSetEventDestinationRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set that contains the event destination that you want to update."];
      eventDestination: EventDestination.t
        [@ocaml.doc
          "The event destination object that you want to apply to the specified configuration set."]}
    let context_ = "UpdateConfigurationSetEventDestinationRequest"
    let make ~configurationSetName =
      fun ~eventDestination ->
        fun () -> { configurationSetName; eventDestination }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("EventDestination",
          (Some (EventDestination.to_value x.eventDestination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestination =
        EventDestination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EventDestination") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~eventDestination ~configurationSetName ()
    let of_json json =
      let eventDestination =
        field_map_exn json "EventDestination" EventDestination.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~eventDestination ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to update the event destination of a configuration set. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module UpdateAccountSendingEnabledRequest =
  struct
    type nonrec t =
      {
      enabled: Enabled.t option
        [@ocaml.doc
          "Describes whether email sending is enabled or disabled for your Amazon SES account in the current AWS Region."]}
    let make ?enabled = fun () -> { enabled }
    let to_value x =
      structure_to_value
        [("Enabled", (Option.map x.enabled ~f:Enabled.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        (Option.map ~f:Enabled.of_xml) (Xml.child xml_arg0 "Enabled") in
      make ?enabled ()
    let of_json json =
      let enabled = field_map json "Enabled" Enabled.of_json in
      make ?enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to enable or disable the email sending capabilities for your entire Amazon SES account."]
module TestRenderTemplateResponse =
  struct
    type testRenderTemplateResult =
      {
      renderedTemplate: RenderedTemplate.t option
        [@ocaml.doc
          "The complete MIME message rendered by applying the data in the TemplateData parameter to the template specified in the TemplateName parameter."]}
    and responseMetaData = unit
    and t =
      {
      testRenderTemplateResult: testRenderTemplateResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `InvalidRenderingParameterException of
          InvalidRenderingParameterException.t 
      | `MissingRenderingAttributeException of
          MissingRenderingAttributeException.t 
      | `TemplateDoesNotExistException of TemplateDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "TestRenderTemplateResponse"
    let make ?renderedTemplate =
      fun () ->
        {
          testRenderTemplateResult = { renderedTemplate };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "InvalidRenderingParameterException" ->
          `InvalidRenderingParameterException
            (InvalidRenderingParameterException.of_json json)
      | "MissingRenderingAttributeException" ->
          `MissingRenderingAttributeException
            (MissingRenderingAttributeException.of_json json)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRenderingParameterException" ->
          `InvalidRenderingParameterException
            (InvalidRenderingParameterException.of_xml xml)
      | "MissingRenderingAttributeException" ->
          `MissingRenderingAttributeException
            (MissingRenderingAttributeException.of_xml xml)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRenderingParameterException e ->
          `Assoc
            [("error", (`String "InvalidRenderingParameterException"));
            ("details", (InvalidRenderingParameterException.to_json e))]
      | `MissingRenderingAttributeException e ->
          `Assoc
            [("error", (`String "MissingRenderingAttributeException"));
            ("details", (MissingRenderingAttributeException.to_json e))]
      | `TemplateDoesNotExistException e ->
          `Assoc
            [("error", (`String "TemplateDoesNotExistException"));
            ("details", (TemplateDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.testRenderTemplateResult in
      structure_to_wrapped_value
        [("RenderedTemplate",
           (Option.map x.renderedTemplate ~f:RenderedTemplate.to_value))]
        ~wrapper:"TestRenderTemplateResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "TestRenderTemplateResult" in
      let renderedTemplate =
        (Option.map ~f:RenderedTemplate.of_xml)
          (Xml.child xml_arg0 "RenderedTemplate") in
      make ?renderedTemplate ()
    let of_json json =
      let renderedTemplate =
        field_map json "RenderedTemplate" RenderedTemplate.of_json in
      make ?renderedTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a preview of the MIME content of an email when provided with a template and a set of replacement data. You can execute this operation no more than once per second."]
module TestRenderTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc "The name of the template that you want to render."];
      templateData: TemplateData.t
        [@ocaml.doc
          "A list of replacement values to apply to the template. This parameter is a JSON object, typically consisting of key-value pairs in which the keys correspond to replacement tags in the email template."]}
    let context_ = "TestRenderTemplateRequest"
    let make ~templateName =
      fun ~templateData -> fun () -> { templateName; templateData }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)));
        ("TemplateData", (Some (TemplateData.to_value x.templateData)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateData =
        TemplateData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateData") in
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ~templateData ~templateName ()
    let of_json json =
      let templateData =
        field_map_exn json "TemplateData" TemplateData.of_json in
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ~templateData ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a preview of the MIME content of an email when provided with a template and a set of replacement data. You can execute this operation no more than once per second."]
module SetReceiptRulePositionResponse =
  struct
    type setReceiptRulePositionResult = unit
    and responseMetaData = unit
    and t =
      {
      setReceiptRulePositionResult: setReceiptRulePositionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `RuleDoesNotExistException of RuleDoesNotExistException.t 
      | `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { setReceiptRulePositionResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_json json)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_xml xml)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `RuleDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleDoesNotExistException"));
            ("details", (RuleDoesNotExistException.to_json e))]
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetReceiptRulePositionRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the receipt rule set that contains the receipt rule to reposition."];
      ruleName: ReceiptRuleName.t
        [@ocaml.doc "The name of the receipt rule to reposition."];
      after: ReceiptRuleName.t option
        [@ocaml.doc
          "The name of the receipt rule after which to place the specified receipt rule."]}
    let context_ = "SetReceiptRulePositionRequest"
    let make ?after =
      fun ~ruleSetName ->
        fun ~ruleName -> fun () -> { after; ruleSetName; ruleName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("RuleName", (Some (ReceiptRuleName.to_value x.ruleName)));
        ("After", (Option.map x.after ~f:ReceiptRuleName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let after =
        (Option.map ~f:ReceiptRuleName.of_xml) (Xml.child xml_arg0 "After") in
      let ruleName =
        ReceiptRuleName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ?after ~ruleName ~ruleSetName ()
    let of_json json =
      let after = field_map json "After" ReceiptRuleName.of_json in
      let ruleName = field_map_exn json "RuleName" ReceiptRuleName.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ?after ~ruleName ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to set the position of a receipt rule in a receipt rule set. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module SetIdentityNotificationTopicResponse =
  struct
    type setIdentityNotificationTopicResult = unit
    and responseMetaData = unit
    and t =
      {
      setIdentityNotificationTopicResult: setIdentityNotificationTopicResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () =
      { setIdentityNotificationTopicResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetIdentityNotificationTopicRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity (email address or domain) that you want to set the Amazon SNS topic for. You can only specify a verified identity for this parameter. You can specify an identity by using its name or by using its Amazon Resource Name (ARN). The following examples are all valid identities: sender\\@example.com, example.com, arn:aws:ses:us-east-1:123456789012:identity/example.com."];
      notificationType: NotificationType.t
        [@ocaml.doc
          "The type of notifications that will be published to the specified Amazon SNS topic."];
      snsTopic: NotificationTopic.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon SNS topic. If the parameter is omitted from the request or a null value is passed, SnsTopic is cleared and publishing is disabled."]}
    let context_ = "SetIdentityNotificationTopicRequest"
    let make ?snsTopic =
      fun ~identity ->
        fun ~notificationType ->
          fun () -> { snsTopic; identity; notificationType }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("NotificationType",
          (Some (NotificationType.to_value x.notificationType)));
        ("SnsTopic", (Option.map x.snsTopic ~f:NotificationTopic.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snsTopic =
        (Option.map ~f:NotificationTopic.of_xml)
          (Xml.child xml_arg0 "SnsTopic") in
      let notificationType =
        NotificationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotificationType") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ?snsTopic ~notificationType ~identity ()
    let of_json json =
      let snsTopic = field_map json "SnsTopic" NotificationTopic.of_json in
      let notificationType =
        field_map_exn json "NotificationType" NotificationType.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ?snsTopic ~notificationType ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to specify the Amazon SNS topic to which Amazon SES will publish bounce, complaint, or delivery notifications for emails sent with that identity as the Source. For information about Amazon SES notifications, see the Amazon SES Developer Guide."]
module SetIdentityMailFromDomainResponse =
  struct
    type setIdentityMailFromDomainResult = unit
    and responseMetaData = unit
    and t =
      {
      setIdentityMailFromDomainResult: setIdentityMailFromDomainResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () =
      { setIdentityMailFromDomainResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetIdentityMailFromDomainRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The verified identity for which you want to enable or disable the specified custom MAIL FROM domain."];
      mailFromDomain: MailFromDomainName.t option
        [@ocaml.doc
          "The custom MAIL FROM domain that you want the verified identity to use. The MAIL FROM domain must 1) be a subdomain of the verified identity, 2) not be used in a \"From\" address if the MAIL FROM domain is the destination of email feedback forwarding (for more information, see the Amazon SES Developer Guide), and 3) not be used to receive emails. A value of null disables the custom MAIL FROM setting for the identity."];
      behaviorOnMXFailure: BehaviorOnMXFailure.t option
        [@ocaml.doc
          "The action that you want Amazon SES to take if it cannot successfully read the required MX record when you send an email. If you choose UseDefaultValue, Amazon SES will use amazonses.com (or a subdomain of that) as the MAIL FROM domain. If you choose RejectMessage, Amazon SES will return a MailFromDomainNotVerified error and not send the email. The action specified in BehaviorOnMXFailure is taken when the custom MAIL FROM domain setup is in the Pending, Failed, and TemporaryFailure states."]}
    let context_ = "SetIdentityMailFromDomainRequest"
    let make ?mailFromDomain =
      fun ?behaviorOnMXFailure ->
        fun ~identity ->
          fun () -> { mailFromDomain; behaviorOnMXFailure; identity }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("MailFromDomain",
          (Option.map x.mailFromDomain ~f:MailFromDomainName.to_value));
        ("BehaviorOnMXFailure",
          (Option.map x.behaviorOnMXFailure ~f:BehaviorOnMXFailure.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let behaviorOnMXFailure =
        (Option.map ~f:BehaviorOnMXFailure.of_xml)
          (Xml.child xml_arg0 "BehaviorOnMXFailure") in
      let mailFromDomain =
        (Option.map ~f:MailFromDomainName.of_xml)
          (Xml.child xml_arg0 "MailFromDomain") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ?behaviorOnMXFailure ?mailFromDomain ~identity ()
    let of_json json =
      let behaviorOnMXFailure =
        field_map json "BehaviorOnMXFailure" BehaviorOnMXFailure.of_json in
      let mailFromDomain =
        field_map json "MailFromDomain" MailFromDomainName.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ?behaviorOnMXFailure ?mailFromDomain ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to enable or disable the Amazon SES custom MAIL FROM domain setup for a verified identity. For information about using a custom MAIL FROM domain, see the Amazon SES Developer Guide."]
module SetIdentityHeadersInNotificationsEnabledResponse =
  struct
    type setIdentityHeadersInNotificationsEnabledResult = unit
    and responseMetaData = unit
    and t =
      {
      setIdentityHeadersInNotificationsEnabledResult:
        setIdentityHeadersInNotificationsEnabledResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        setIdentityHeadersInNotificationsEnabledResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetIdentityHeadersInNotificationsEnabledRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity for which to enable or disable headers in notifications. Examples: user\\@example.com, example.com."];
      notificationType: NotificationType.t
        [@ocaml.doc
          "The notification type for which to enable or disable headers in notifications."];
      enabled: Enabled.t
        [@ocaml.doc
          "Sets whether Amazon SES includes the original email headers in Amazon SNS notifications of the specified notification type. A value of true specifies that Amazon SES will include headers in notifications, and a value of false specifies that Amazon SES will not include headers in notifications. This value can only be set when NotificationType is already set to use a particular Amazon SNS topic."]}
    let context_ = "SetIdentityHeadersInNotificationsEnabledRequest"
    let make ~identity =
      fun ~notificationType ->
        fun ~enabled -> fun () -> { identity; notificationType; enabled }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("NotificationType",
          (Some (NotificationType.to_value x.notificationType)));
        ("Enabled", (Some (Enabled.to_value x.enabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        Enabled.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Enabled") in
      let notificationType =
        NotificationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotificationType") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~enabled ~notificationType ~identity ()
    let of_json json =
      let enabled = field_map_exn json "Enabled" Enabled.of_json in
      let notificationType =
        field_map_exn json "NotificationType" NotificationType.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~enabled ~notificationType ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to set whether Amazon SES includes the original email headers in the Amazon SNS notifications of a specified type. For information about notifications, see the Amazon SES Developer Guide."]
module SetIdentityFeedbackForwardingEnabledResponse =
  struct
    type setIdentityFeedbackForwardingEnabledResult = unit
    and responseMetaData = unit
    and t =
      {
      setIdentityFeedbackForwardingEnabledResult:
        setIdentityFeedbackForwardingEnabledResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        setIdentityFeedbackForwardingEnabledResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetIdentityFeedbackForwardingEnabledRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity for which to set bounce and complaint notification forwarding. Examples: user\\@example.com, example.com."];
      forwardingEnabled: Enabled.t
        [@ocaml.doc
          "Sets whether Amazon SES will forward bounce and complaint notifications as email. true specifies that Amazon SES will forward bounce and complaint notifications as email, in addition to any Amazon SNS topic publishing otherwise specified. false specifies that Amazon SES will publish bounce and complaint notifications only through Amazon SNS. This value can only be set to false when Amazon SNS topics are set for both Bounce and Complaint notification types."]}
    let context_ = "SetIdentityFeedbackForwardingEnabledRequest"
    let make ~identity =
      fun ~forwardingEnabled -> fun () -> { identity; forwardingEnabled }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("ForwardingEnabled", (Some (Enabled.to_value x.forwardingEnabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let forwardingEnabled =
        Enabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ForwardingEnabled") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~forwardingEnabled ~identity ()
    let of_json json =
      let forwardingEnabled =
        field_map_exn json "ForwardingEnabled" Enabled.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~forwardingEnabled ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to enable or disable whether Amazon SES forwards you bounce and complaint notifications through email. For information about email feedback forwarding, see the Amazon SES Developer Guide."]
module SetIdentityDkimEnabledResponse =
  struct
    type setIdentityDkimEnabledResult = unit
    and responseMetaData = unit
    and t =
      {
      setIdentityDkimEnabledResult: setIdentityDkimEnabledResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () =
      { setIdentityDkimEnabledResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetIdentityDkimEnabledRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity for which DKIM signing should be enabled or disabled."];
      dkimEnabled: Enabled.t
        [@ocaml.doc
          "Sets whether DKIM signing is enabled for an identity. Set to true to enable DKIM signing for this identity; false to disable it."]}
    let context_ = "SetIdentityDkimEnabledRequest"
    let make ~identity =
      fun ~dkimEnabled -> fun () -> { identity; dkimEnabled }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("DkimEnabled", (Some (Enabled.to_value x.dkimEnabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dkimEnabled =
        Enabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DkimEnabled") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~dkimEnabled ~identity ()
    let of_json json =
      let dkimEnabled = field_map_exn json "DkimEnabled" Enabled.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~dkimEnabled ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to enable or disable Amazon SES Easy DKIM signing for an identity. For more information about setting up Easy DKIM, see the Amazon SES Developer Guide."]
module SetActiveReceiptRuleSetResponse =
  struct
    type setActiveReceiptRuleSetResult = unit
    and responseMetaData = unit
    and t =
      {
      setActiveReceiptRuleSetResult: setActiveReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { setActiveReceiptRuleSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module SetActiveReceiptRuleSetRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t option
        [@ocaml.doc
          "The name of the receipt rule set to make active. Setting this value to null disables all email receiving."]}
    let make ?ruleSetName = fun () -> { ruleSetName }
    let to_value x =
      structure_to_value
        [("RuleSetName",
           (Option.map x.ruleSetName ~f:ReceiptRuleSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleSetName =
        (Option.map ~f:ReceiptRuleSetName.of_xml)
          (Xml.child xml_arg0 "RuleSetName") in
      make ?ruleSetName ()
    let of_json json =
      let ruleSetName =
        field_map json "RuleSetName" ReceiptRuleSetName.of_json in
      make ?ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to set a receipt rule set as the active receipt rule set. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module SendTemplatedEmailResponse =
  struct
    type sendTemplatedEmailResult =
      {
      messageId: MessageId.t
        [@ocaml.doc
          "The unique message identifier returned from the SendTemplatedEmail action."]}
    and responseMetaData = unit
    and t =
      {
      sendTemplatedEmailResult: sendTemplatedEmailResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccountSendingPausedException of AccountSendingPausedException.t 
      | `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `ConfigurationSetSendingPausedException of
          ConfigurationSetSendingPausedException.t 
      | `MailFromDomainNotVerifiedException of
          MailFromDomainNotVerifiedException.t 
      | `MessageRejected of MessageRejected.t 
      | `TemplateDoesNotExistException of TemplateDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "SendTemplatedEmailResponse"
    let make ~messageId =
      fun () ->
        { sendTemplatedEmailResult = { messageId }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_json json)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_json json)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_json json)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_json json)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_xml xml)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_xml xml)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_xml xml)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_xml xml)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountSendingPausedException e ->
          `Assoc
            [("error", (`String "AccountSendingPausedException"));
            ("details", (AccountSendingPausedException.to_json e))]
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `ConfigurationSetSendingPausedException e ->
          `Assoc
            [("error", (`String "ConfigurationSetSendingPausedException"));
            ("details", (ConfigurationSetSendingPausedException.to_json e))]
      | `MailFromDomainNotVerifiedException e ->
          `Assoc
            [("error", (`String "MailFromDomainNotVerifiedException"));
            ("details", (MailFromDomainNotVerifiedException.to_json e))]
      | `MessageRejected e ->
          `Assoc
            [("error", (`String "MessageRejected"));
            ("details", (MessageRejected.to_json e))]
      | `TemplateDoesNotExistException e ->
          `Assoc
            [("error", (`String "TemplateDoesNotExistException"));
            ("details", (TemplateDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.sendTemplatedEmailResult in
      structure_to_wrapped_value
        [("MessageId", (Some (MessageId.to_value x.messageId)))]
        ~wrapper:"SendTemplatedEmailResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "SendTemplatedEmailResult" in
      let messageId =
        MessageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MessageId") in
      make ~messageId ()
    let of_json json =
      let messageId = field_map_exn json "MessageId" MessageId.of_json in
      make ~messageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Composes an email message using an email template and immediately queues it for sending. In order to send email using the SendTemplatedEmail operation, your call to the API must meet the following requirements: The call must refer to an existing email template. You can create email templates using the CreateTemplate operation. The message must be sent from a verified email address or domain. If your account is still in the Amazon SES sandbox, you may only send to verified addresses or domains, or to email addresses associated with the Amazon SES Mailbox Simulator. For more information, see Verifying Email Addresses and Domains in the Amazon SES Developer Guide. The maximum message size is 10 MB. Calls to the SendTemplatedEmail operation may only include one Destination parameter. A destination is a set of recipients who will receive the same version of the email. The Destination parameter can include up to 50 recipients, across the To:, CC: and BCC: fields. The Destination parameter must include at least one recipient email address. The recipient address can be a To: address, a CC: address, or a BCC: address. If a recipient email address is invalid (that is, it is not in the format UserName\\@\\[SubDomain.\\]Domain.TopLevelDomain), the entire message will be rejected, even if the message contains other recipients that are valid. If your call to the SendTemplatedEmail operation includes all of the required parameters, Amazon SES accepts it and returns a Message ID. However, if Amazon SES can't render the email because the template contains errors, it doesn't send the email. Additionally, because it already accepted the message, Amazon SES doesn't return a message stating that it was unable to send the email. For these reasons, we highly recommend that you set up Amazon SES to send you notifications when Rendering Failure events occur. For more information, see Sending Personalized Email Using the Amazon SES API in the Amazon Simple Email Service Developer Guide."]
module SendTemplatedEmailRequest =
  struct
    type nonrec t =
      {
      source: Address.t
        [@ocaml.doc
          "The email address that is sending the email. This email address must be either individually verified with Amazon SES, or from a domain that has been verified with Amazon SES. For information about verifying identities, see the Amazon SES Developer Guide. If you are sending on behalf of another user and have been permitted to do so by a sending authorization policy, then you must also specify the SourceArn parameter. For more information about sending authorization, see the Amazon SES Developer Guide. Amazon SES does not support the SMTPUTF8 extension, as described in RFC6531. For this reason, the local part of a source email address (the part of the email address that precedes the \\@ sign) may only contain 7-bit ASCII characters. If the domain part of an address (the part after the \\@ sign) contains non-ASCII characters, they must be encoded using Punycode, as described in RFC3492. The sender name (also known as the friendly name) may contain non-ASCII characters. These characters must be encoded using MIME encoded-word syntax, as described inRFC 2047. MIME encoded-word syntax uses the following form: =?charset?encoding?encoded-text?=."];
      destination: Destination.t
        [@ocaml.doc
          "The destination for this email, composed of To:, CC:, and BCC: fields. A Destination can include up to 50 recipients across these three fields."];
      replyToAddresses: AddressList.t option
        [@ocaml.doc
          "The reply-to email address(es) for the message. If the recipient replies to the message, each reply-to address will receive the reply."];
      returnPath: Address.t option
        [@ocaml.doc
          "The email address that bounces and complaints will be forwarded to when feedback forwarding is enabled. If the message cannot be delivered to the recipient, then an error message will be returned from the recipient's ISP; this message will then be forwarded to the email address specified by the ReturnPath parameter. The ReturnPath parameter is never overwritten. This email address must be either individually verified with Amazon SES, or from a domain that has been verified with Amazon SES."];
      sourceArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to send for the email address specified in the Source parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to send from user\\@example.com, then you would specify the SourceArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the Source to be user\\@example.com. For more information about sending authorization, see the Amazon SES Developer Guide."];
      returnPathArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to use the email address specified in the ReturnPath parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to use feedback\\@example.com, then you would specify the ReturnPathArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the ReturnPath to be feedback\\@example.com. For more information about sending authorization, see the Amazon SES Developer Guide."];
      tags: MessageTagList.t option
        [@ocaml.doc
          "A list of tags, in the form of name/value pairs, to apply to an email that you send using SendTemplatedEmail. Tags correspond to characteristics of the email that you define, so that you can publish email sending events."];
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "The name of the configuration set to use when you send an email using SendTemplatedEmail."];
      template: TemplateName.t
        [@ocaml.doc "The template to use when sending this email."];
      templateArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the template to use when sending this email."];
      templateData: TemplateData.t
        [@ocaml.doc
          "A list of replacement values to apply to the template. This parameter is a JSON object, typically consisting of key-value pairs in which the keys correspond to replacement tags in the email template."]}
    let context_ = "SendTemplatedEmailRequest"
    let make ?replyToAddresses =
      fun ?returnPath ->
        fun ?sourceArn ->
          fun ?returnPathArn ->
            fun ?tags ->
              fun ?configurationSetName ->
                fun ?templateArn ->
                  fun ~source ->
                    fun ~destination ->
                      fun ~template ->
                        fun ~templateData ->
                          fun () ->
                            {
                              replyToAddresses;
                              returnPath;
                              sourceArn;
                              returnPathArn;
                              tags;
                              configurationSetName;
                              templateArn;
                              source;
                              destination;
                              template;
                              templateData
                            }
    let to_value x =
      structure_to_value
        [("Source", (Some (Address.to_value x.source)));
        ("Destination", (Some (Destination.to_value x.destination)));
        ("ReplyToAddresses",
          (Option.map x.replyToAddresses ~f:AddressList.to_value));
        ("ReturnPath", (Option.map x.returnPath ~f:Address.to_value));
        ("SourceArn",
          (Option.map x.sourceArn ~f:AmazonResourceName.to_value));
        ("ReturnPathArn",
          (Option.map x.returnPathArn ~f:AmazonResourceName.to_value));
        ("Tags", (Option.map x.tags ~f:MessageTagList.to_value));
        ("ConfigurationSetName",
          (Option.map x.configurationSetName ~f:ConfigurationSetName.to_value));
        ("Template", (Some (TemplateName.to_value x.template)));
        ("TemplateArn",
          (Option.map x.templateArn ~f:AmazonResourceName.to_value));
        ("TemplateData", (Some (TemplateData.to_value x.templateData)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateData =
        TemplateData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateData") in
      let templateArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TemplateArn") in
      let template =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Template") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      let tags =
        (Option.map ~f:MessageTagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let returnPathArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "ReturnPathArn") in
      let sourceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "SourceArn") in
      let returnPath =
        (Option.map ~f:Address.of_xml) (Xml.child xml_arg0 "ReturnPath") in
      let replyToAddresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "ReplyToAddresses") in
      let destination =
        Destination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destination") in
      let source =
        Address.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Source") in
      make ~templateData ?templateArn ~template ?configurationSetName ?tags
        ?returnPathArn ?sourceArn ?returnPath ?replyToAddresses ~destination
        ~source ()
    let of_json json =
      let templateData =
        field_map_exn json "TemplateData" TemplateData.of_json in
      let templateArn =
        field_map json "TemplateArn" AmazonResourceName.of_json in
      let template = field_map_exn json "Template" TemplateName.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      let tags = field_map json "Tags" MessageTagList.of_json in
      let returnPathArn =
        field_map json "ReturnPathArn" AmazonResourceName.of_json in
      let sourceArn = field_map json "SourceArn" AmazonResourceName.of_json in
      let returnPath = field_map json "ReturnPath" Address.of_json in
      let replyToAddresses =
        field_map json "ReplyToAddresses" AddressList.of_json in
      let destination = field_map_exn json "Destination" Destination.of_json in
      let source = field_map_exn json "Source" Address.of_json in
      make ~templateData ?templateArn ~template ?configurationSetName ?tags
        ?returnPathArn ?sourceArn ?returnPath ?replyToAddresses ~destination
        ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to send a templated email using Amazon SES. For more information, see the Amazon SES Developer Guide."]
module SendRawEmailResponse =
  struct
    type sendRawEmailResult =
      {
      messageId: MessageId.t
        [@ocaml.doc
          "The unique message identifier returned from the SendRawEmail action."]}
    and responseMetaData = unit
    and t =
      {
      sendRawEmailResult: sendRawEmailResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccountSendingPausedException of AccountSendingPausedException.t 
      | `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `ConfigurationSetSendingPausedException of
          ConfigurationSetSendingPausedException.t 
      | `MailFromDomainNotVerifiedException of
          MailFromDomainNotVerifiedException.t 
      | `MessageRejected of MessageRejected.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "SendRawEmailResponse"
    let make ~messageId =
      fun () -> { sendRawEmailResult = { messageId }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_json json)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_json json)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_json json)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_xml xml)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_xml xml)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_xml xml)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountSendingPausedException e ->
          `Assoc
            [("error", (`String "AccountSendingPausedException"));
            ("details", (AccountSendingPausedException.to_json e))]
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `ConfigurationSetSendingPausedException e ->
          `Assoc
            [("error", (`String "ConfigurationSetSendingPausedException"));
            ("details", (ConfigurationSetSendingPausedException.to_json e))]
      | `MailFromDomainNotVerifiedException e ->
          `Assoc
            [("error", (`String "MailFromDomainNotVerifiedException"));
            ("details", (MailFromDomainNotVerifiedException.to_json e))]
      | `MessageRejected e ->
          `Assoc
            [("error", (`String "MessageRejected"));
            ("details", (MessageRejected.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.sendRawEmailResult in
      structure_to_wrapped_value
        [("MessageId", (Some (MessageId.to_value x.messageId)))]
        ~wrapper:"SendRawEmailResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "SendRawEmailResult" in
      let messageId =
        MessageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MessageId") in
      make ~messageId ()
    let of_json json =
      let messageId = field_map_exn json "MessageId" MessageId.of_json in
      make ~messageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a unique message ID."]
module SendRawEmailRequest =
  struct
    type nonrec t =
      {
      source: Address.t option
        [@ocaml.doc
          "The identity's email address. If you do not provide a value for this parameter, you must specify a \"From\" address in the raw text of the message. (You can also specify both.) Amazon SES does not support the SMTPUTF8 extension, as described inRFC6531. For this reason, the local part of a source email address (the part of the email address that precedes the \\@ sign) may only contain 7-bit ASCII characters. If the domain part of an address (the part after the \\@ sign) contains non-ASCII characters, they must be encoded using Punycode, as described in RFC3492. The sender name (also known as the friendly name) may contain non-ASCII characters. These characters must be encoded using MIME encoded-word syntax, as described in RFC 2047. MIME encoded-word syntax uses the following form: =?charset?encoding?encoded-text?=. If you specify the Source parameter and have feedback forwarding enabled, then bounces and complaints will be sent to this email address. This takes precedence over any Return-Path header that you might include in the raw text of the message."];
      destinations: AddressList.t option
        [@ocaml.doc
          "A list of destinations for the message, consisting of To:, CC:, and BCC: addresses."];
      rawMessage: RawMessage.t
        [@ocaml.doc
          "The raw email message itself. The message has to meet the following criteria: The message has to contain a header and a body, separated by a blank line. All of the required header fields must be present in the message. Each part of a multipart MIME message must be formatted properly. Attachments must be of a content type that Amazon SES supports. For a list on unsupported content types, see Unsupported Attachment Types in the Amazon SES Developer Guide. The entire message must be base64-encoded. If any of the MIME parts in your message contain content that is outside of the 7-bit ASCII character range, we highly recommend that you encode that content. For more information, see Sending Raw Email in the Amazon SES Developer Guide. Per RFC 5321, the maximum length of each line of text, including the <CRLF>, must not exceed 1,000 characters."];
      fromArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to specify a particular \"From\" address in the header of the raw email. Instead of using this parameter, you can use the X-header X-SES-FROM-ARN in the raw message of the email. If you use both the FromArn parameter and the corresponding X-header, Amazon SES uses the value of the FromArn parameter. For information about when to use this parameter, see the description of SendRawEmail in this guide, or see the Amazon SES Developer Guide."];
      sourceArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to send for the email address specified in the Source parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to send from user\\@example.com, then you would specify the SourceArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the Source to be user\\@example.com. Instead of using this parameter, you can use the X-header X-SES-SOURCE-ARN in the raw message of the email. If you use both the SourceArn parameter and the corresponding X-header, Amazon SES uses the value of the SourceArn parameter. For information about when to use this parameter, see the description of SendRawEmail in this guide, or see the Amazon SES Developer Guide."];
      returnPathArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to use the email address specified in the ReturnPath parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to use feedback\\@example.com, then you would specify the ReturnPathArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the ReturnPath to be feedback\\@example.com. Instead of using this parameter, you can use the X-header X-SES-RETURN-PATH-ARN in the raw message of the email. If you use both the ReturnPathArn parameter and the corresponding X-header, Amazon SES uses the value of the ReturnPathArn parameter. For information about when to use this parameter, see the description of SendRawEmail in this guide, or see the Amazon SES Developer Guide."];
      tags: MessageTagList.t option
        [@ocaml.doc
          "A list of tags, in the form of name/value pairs, to apply to an email that you send using SendRawEmail. Tags correspond to characteristics of the email that you define, so that you can publish email sending events."];
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "The name of the configuration set to use when you send an email using SendRawEmail."]}
    let context_ = "SendRawEmailRequest"
    let make ?source =
      fun ?destinations ->
        fun ?fromArn ->
          fun ?sourceArn ->
            fun ?returnPathArn ->
              fun ?tags ->
                fun ?configurationSetName ->
                  fun ~rawMessage ->
                    fun () ->
                      {
                        source;
                        destinations;
                        fromArn;
                        sourceArn;
                        returnPathArn;
                        tags;
                        configurationSetName;
                        rawMessage
                      }
    let to_value x =
      structure_to_value
        [("Source", (Option.map x.source ~f:Address.to_value));
        ("Destinations", (Option.map x.destinations ~f:AddressList.to_value));
        ("RawMessage", (Some (RawMessage.to_value x.rawMessage)));
        ("FromArn", (Option.map x.fromArn ~f:AmazonResourceName.to_value));
        ("SourceArn",
          (Option.map x.sourceArn ~f:AmazonResourceName.to_value));
        ("ReturnPathArn",
          (Option.map x.returnPathArn ~f:AmazonResourceName.to_value));
        ("Tags", (Option.map x.tags ~f:MessageTagList.to_value));
        ("ConfigurationSetName",
          (Option.map x.configurationSetName ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      let tags =
        (Option.map ~f:MessageTagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let returnPathArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "ReturnPathArn") in
      let sourceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "SourceArn") in
      let fromArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "FromArn") in
      let rawMessage =
        RawMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RawMessage") in
      let destinations =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "Destinations") in
      let source =
        (Option.map ~f:Address.of_xml) (Xml.child xml_arg0 "Source") in
      make ?configurationSetName ?tags ?returnPathArn ?sourceArn ?fromArn
        ~rawMessage ?destinations ?source ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      let tags = field_map json "Tags" MessageTagList.of_json in
      let returnPathArn =
        field_map json "ReturnPathArn" AmazonResourceName.of_json in
      let sourceArn = field_map json "SourceArn" AmazonResourceName.of_json in
      let fromArn = field_map json "FromArn" AmazonResourceName.of_json in
      let rawMessage = field_map_exn json "RawMessage" RawMessage.of_json in
      let destinations = field_map json "Destinations" AddressList.of_json in
      let source = field_map json "Source" Address.of_json in
      make ?configurationSetName ?tags ?returnPathArn ?sourceArn ?fromArn
        ~rawMessage ?destinations ?source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to send a single raw email using Amazon SES. For more information, see the Amazon SES Developer Guide."]
module SendEmailResponse =
  struct
    type sendEmailResult =
      {
      messageId: MessageId.t
        [@ocaml.doc
          "The unique message identifier returned from the SendEmail action."]}
    and responseMetaData = unit
    and t =
      {
      sendEmailResult: sendEmailResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccountSendingPausedException of AccountSendingPausedException.t 
      | `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `ConfigurationSetSendingPausedException of
          ConfigurationSetSendingPausedException.t 
      | `MailFromDomainNotVerifiedException of
          MailFromDomainNotVerifiedException.t 
      | `MessageRejected of MessageRejected.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "SendEmailResponse"
    let make ~messageId =
      fun () -> { sendEmailResult = { messageId }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_json json)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_json json)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_json json)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_xml xml)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_xml xml)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_xml xml)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountSendingPausedException e ->
          `Assoc
            [("error", (`String "AccountSendingPausedException"));
            ("details", (AccountSendingPausedException.to_json e))]
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `ConfigurationSetSendingPausedException e ->
          `Assoc
            [("error", (`String "ConfigurationSetSendingPausedException"));
            ("details", (ConfigurationSetSendingPausedException.to_json e))]
      | `MailFromDomainNotVerifiedException e ->
          `Assoc
            [("error", (`String "MailFromDomainNotVerifiedException"));
            ("details", (MailFromDomainNotVerifiedException.to_json e))]
      | `MessageRejected e ->
          `Assoc
            [("error", (`String "MessageRejected"));
            ("details", (MessageRejected.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.sendEmailResult in
      structure_to_wrapped_value
        [("MessageId", (Some (MessageId.to_value x.messageId)))]
        ~wrapper:"SendEmailResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "SendEmailResult" in
      let messageId =
        MessageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MessageId") in
      make ~messageId ()
    let of_json json =
      let messageId = field_map_exn json "MessageId" MessageId.of_json in
      make ~messageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a unique message ID."]
module SendEmailRequest =
  struct
    type nonrec t =
      {
      source: Address.t
        [@ocaml.doc
          "The email address that is sending the email. This email address must be either individually verified with Amazon SES, or from a domain that has been verified with Amazon SES. For information about verifying identities, see the Amazon SES Developer Guide. If you are sending on behalf of another user and have been permitted to do so by a sending authorization policy, then you must also specify the SourceArn parameter. For more information about sending authorization, see the Amazon SES Developer Guide. Amazon SES does not support the SMTPUTF8 extension, as described in RFC6531. For this reason, the local part of a source email address (the part of the email address that precedes the \\@ sign) may only contain 7-bit ASCII characters. If the domain part of an address (the part after the \\@ sign) contains non-ASCII characters, they must be encoded using Punycode, as described in RFC3492. The sender name (also known as the friendly name) may contain non-ASCII characters. These characters must be encoded using MIME encoded-word syntax, as described in RFC 2047. MIME encoded-word syntax uses the following form: =?charset?encoding?encoded-text?=."];
      destination: Destination.t
        [@ocaml.doc
          "The destination for this email, composed of To:, CC:, and BCC: fields."];
      message: Message.t [@ocaml.doc "The message to be sent."];
      replyToAddresses: AddressList.t option
        [@ocaml.doc
          "The reply-to email address(es) for the message. If the recipient replies to the message, each reply-to address will receive the reply."];
      returnPath: Address.t option
        [@ocaml.doc
          "The email address that bounces and complaints will be forwarded to when feedback forwarding is enabled. If the message cannot be delivered to the recipient, then an error message will be returned from the recipient's ISP; this message will then be forwarded to the email address specified by the ReturnPath parameter. The ReturnPath parameter is never overwritten. This email address must be either individually verified with Amazon SES, or from a domain that has been verified with Amazon SES."];
      sourceArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to send for the email address specified in the Source parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to send from user\\@example.com, then you would specify the SourceArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the Source to be user\\@example.com. For more information about sending authorization, see the Amazon SES Developer Guide."];
      returnPathArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to use the email address specified in the ReturnPath parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to use feedback\\@example.com, then you would specify the ReturnPathArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the ReturnPath to be feedback\\@example.com. For more information about sending authorization, see the Amazon SES Developer Guide."];
      tags: MessageTagList.t option
        [@ocaml.doc
          "A list of tags, in the form of name/value pairs, to apply to an email that you send using SendEmail. Tags correspond to characteristics of the email that you define, so that you can publish email sending events."];
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "The name of the configuration set to use when you send an email using SendEmail."]}
    let context_ = "SendEmailRequest"
    let make ?replyToAddresses =
      fun ?returnPath ->
        fun ?sourceArn ->
          fun ?returnPathArn ->
            fun ?tags ->
              fun ?configurationSetName ->
                fun ~source ->
                  fun ~destination ->
                    fun ~message ->
                      fun () ->
                        {
                          replyToAddresses;
                          returnPath;
                          sourceArn;
                          returnPathArn;
                          tags;
                          configurationSetName;
                          source;
                          destination;
                          message
                        }
    let to_value x =
      structure_to_value
        [("Source", (Some (Address.to_value x.source)));
        ("Destination", (Some (Destination.to_value x.destination)));
        ("Message", (Some (Message.to_value x.message)));
        ("ReplyToAddresses",
          (Option.map x.replyToAddresses ~f:AddressList.to_value));
        ("ReturnPath", (Option.map x.returnPath ~f:Address.to_value));
        ("SourceArn",
          (Option.map x.sourceArn ~f:AmazonResourceName.to_value));
        ("ReturnPathArn",
          (Option.map x.returnPathArn ~f:AmazonResourceName.to_value));
        ("Tags", (Option.map x.tags ~f:MessageTagList.to_value));
        ("ConfigurationSetName",
          (Option.map x.configurationSetName ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      let tags =
        (Option.map ~f:MessageTagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let returnPathArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "ReturnPathArn") in
      let sourceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "SourceArn") in
      let returnPath =
        (Option.map ~f:Address.of_xml) (Xml.child xml_arg0 "ReturnPath") in
      let replyToAddresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "ReplyToAddresses") in
      let message =
        Message.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      let destination =
        Destination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destination") in
      let source =
        Address.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Source") in
      make ?configurationSetName ?tags ?returnPathArn ?sourceArn ?returnPath
        ?replyToAddresses ~message ~destination ~source ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      let tags = field_map json "Tags" MessageTagList.of_json in
      let returnPathArn =
        field_map json "ReturnPathArn" AmazonResourceName.of_json in
      let sourceArn = field_map json "SourceArn" AmazonResourceName.of_json in
      let returnPath = field_map json "ReturnPath" Address.of_json in
      let replyToAddresses =
        field_map json "ReplyToAddresses" AddressList.of_json in
      let message = field_map_exn json "Message" Message.of_json in
      let destination = field_map_exn json "Destination" Destination.of_json in
      let source = field_map_exn json "Source" Address.of_json in
      make ?configurationSetName ?tags ?returnPathArn ?sourceArn ?returnPath
        ?replyToAddresses ~message ~destination ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to send a single formatted email using Amazon SES. For more information, see the Amazon SES Developer Guide."]
module SendCustomVerificationEmailResponse =
  struct
    type sendCustomVerificationEmailResult =
      {
      messageId: MessageId.t option
        [@ocaml.doc
          "The unique message identifier returned from the SendCustomVerificationEmail operation."]}
    and responseMetaData = unit
    and t =
      {
      sendCustomVerificationEmailResult: sendCustomVerificationEmailResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `CustomVerificationEmailTemplateDoesNotExistException of
          CustomVerificationEmailTemplateDoesNotExistException.t 
      | `FromEmailAddressNotVerifiedException of
          FromEmailAddressNotVerifiedException.t 
      | `MessageRejected of MessageRejected.t 
      | `ProductionAccessNotGrantedException of
          ProductionAccessNotGrantedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "SendCustomVerificationEmailResponse"
    let make ?messageId =
      fun () ->
        {
          sendCustomVerificationEmailResult = { messageId };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "CustomVerificationEmailTemplateDoesNotExistException" ->
          `CustomVerificationEmailTemplateDoesNotExistException
            (CustomVerificationEmailTemplateDoesNotExistException.of_json
               json)
      | "FromEmailAddressNotVerifiedException" ->
          `FromEmailAddressNotVerifiedException
            (FromEmailAddressNotVerifiedException.of_json json)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_json json)
      | "ProductionAccessNotGrantedException" ->
          `ProductionAccessNotGrantedException
            (ProductionAccessNotGrantedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "CustomVerificationEmailTemplateDoesNotExistException" ->
          `CustomVerificationEmailTemplateDoesNotExistException
            (CustomVerificationEmailTemplateDoesNotExistException.of_xml xml)
      | "FromEmailAddressNotVerifiedException" ->
          `FromEmailAddressNotVerifiedException
            (FromEmailAddressNotVerifiedException.of_xml xml)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_xml xml)
      | "ProductionAccessNotGrantedException" ->
          `ProductionAccessNotGrantedException
            (ProductionAccessNotGrantedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `CustomVerificationEmailTemplateDoesNotExistException e ->
          `Assoc
            [("error",
               (`String
                  "CustomVerificationEmailTemplateDoesNotExistException"));
            ("details",
              (CustomVerificationEmailTemplateDoesNotExistException.to_json e))]
      | `FromEmailAddressNotVerifiedException e ->
          `Assoc
            [("error", (`String "FromEmailAddressNotVerifiedException"));
            ("details", (FromEmailAddressNotVerifiedException.to_json e))]
      | `MessageRejected e ->
          `Assoc
            [("error", (`String "MessageRejected"));
            ("details", (MessageRejected.to_json e))]
      | `ProductionAccessNotGrantedException e ->
          `Assoc
            [("error", (`String "ProductionAccessNotGrantedException"));
            ("details", (ProductionAccessNotGrantedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.sendCustomVerificationEmailResult in
      structure_to_wrapped_value
        [("MessageId", (Option.map x.messageId ~f:MessageId.to_value))]
        ~wrapper:"SendCustomVerificationEmailResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "SendCustomVerificationEmailResult" in
      let messageId =
        (Option.map ~f:MessageId.of_xml) (Xml.child xml_arg0 "MessageId") in
      make ?messageId ()
    let of_json json =
      let messageId = field_map json "MessageId" MessageId.of_json in
      make ?messageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response received when attempting to send the custom verification email."]
module SendCustomVerificationEmailRequest =
  struct
    type nonrec t =
      {
      emailAddress: Address.t [@ocaml.doc "The email address to verify."];
      templateName: TemplateName.t
        [@ocaml.doc
          "The name of the custom verification email template to use when sending the verification email."];
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "Name of a configuration set to use when sending the verification email."]}
    let context_ = "SendCustomVerificationEmailRequest"
    let make ?configurationSetName =
      fun ~emailAddress ->
        fun ~templateName ->
          fun () -> { configurationSetName; emailAddress; templateName }
    let to_value x =
      structure_to_value
        [("EmailAddress", (Some (Address.to_value x.emailAddress)));
        ("TemplateName", (Some (TemplateName.to_value x.templateName)));
        ("ConfigurationSetName",
          (Option.map x.configurationSetName ~f:ConfigurationSetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      let emailAddress =
        Address.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EmailAddress") in
      make ?configurationSetName ~templateName ~emailAddress ()
    let of_json json =
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      let emailAddress = field_map_exn json "EmailAddress" Address.of_json in
      make ?configurationSetName ~templateName ~emailAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to send a custom verification email to a specified recipient."]
module SendBulkTemplatedEmailResponse =
  struct
    type sendBulkTemplatedEmailResult =
      {
      status: BulkEmailDestinationStatusList.t
        [@ocaml.doc
          "The unique message identifier returned from the SendBulkTemplatedEmail action."]}
    and responseMetaData = unit
    and t =
      {
      sendBulkTemplatedEmailResult: sendBulkTemplatedEmailResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccountSendingPausedException of AccountSendingPausedException.t 
      | `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `ConfigurationSetSendingPausedException of
          ConfigurationSetSendingPausedException.t 
      | `MailFromDomainNotVerifiedException of
          MailFromDomainNotVerifiedException.t 
      | `MessageRejected of MessageRejected.t 
      | `TemplateDoesNotExistException of TemplateDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "SendBulkTemplatedEmailResponse"
    let make ~status =
      fun () ->
        { sendBulkTemplatedEmailResult = { status }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_json json)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_json json)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_json json)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_json json)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountSendingPausedException" ->
          `AccountSendingPausedException
            (AccountSendingPausedException.of_xml xml)
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "ConfigurationSetSendingPausedException" ->
          `ConfigurationSetSendingPausedException
            (ConfigurationSetSendingPausedException.of_xml xml)
      | "MailFromDomainNotVerifiedException" ->
          `MailFromDomainNotVerifiedException
            (MailFromDomainNotVerifiedException.of_xml xml)
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_xml xml)
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountSendingPausedException e ->
          `Assoc
            [("error", (`String "AccountSendingPausedException"));
            ("details", (AccountSendingPausedException.to_json e))]
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `ConfigurationSetSendingPausedException e ->
          `Assoc
            [("error", (`String "ConfigurationSetSendingPausedException"));
            ("details", (ConfigurationSetSendingPausedException.to_json e))]
      | `MailFromDomainNotVerifiedException e ->
          `Assoc
            [("error", (`String "MailFromDomainNotVerifiedException"));
            ("details", (MailFromDomainNotVerifiedException.to_json e))]
      | `MessageRejected e ->
          `Assoc
            [("error", (`String "MessageRejected"));
            ("details", (MessageRejected.to_json e))]
      | `TemplateDoesNotExistException e ->
          `Assoc
            [("error", (`String "TemplateDoesNotExistException"));
            ("details", (TemplateDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.sendBulkTemplatedEmailResult in
      structure_to_wrapped_value
        [("Status",
           (Some (BulkEmailDestinationStatusList.to_value x.status)))]
        ~wrapper:"SendBulkTemplatedEmailResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "SendBulkTemplatedEmailResult" in
      let status =
        BulkEmailDestinationStatusList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      make ~status ()
    let of_json json =
      let status =
        field_map_exn json "Status" BulkEmailDestinationStatusList.of_json in
      make ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Composes an email message to multiple destinations. The message body is created using an email template. In order to send email using the SendBulkTemplatedEmail operation, your call to the API must meet the following requirements: The call must refer to an existing email template. You can create email templates using the CreateTemplate operation. The message must be sent from a verified email address or domain. If your account is still in the Amazon SES sandbox, you may only send to verified addresses or domains, or to email addresses associated with the Amazon SES Mailbox Simulator. For more information, see Verifying Email Addresses and Domains in the Amazon SES Developer Guide. The maximum message size is 10 MB. Each Destination parameter must include at least one recipient email address. The recipient address can be a To: address, a CC: address, or a BCC: address. If a recipient email address is invalid (that is, it is not in the format UserName\\@\\[SubDomain.\\]Domain.TopLevelDomain), the entire message will be rejected, even if the message contains other recipients that are valid. The message may not include more than 50 recipients, across the To:, CC: and BCC: fields. If you need to send an email message to a larger audience, you can divide your recipient list into groups of 50 or fewer, and then call the SendBulkTemplatedEmail operation several times to send the message to each group. The number of destinations you can contact in a single call to the API may be limited by your account's maximum sending rate."]
module SendBulkTemplatedEmailRequest =
  struct
    type nonrec t =
      {
      source: Address.t
        [@ocaml.doc
          "The email address that is sending the email. This email address must be either individually verified with Amazon SES, or from a domain that has been verified with Amazon SES. For information about verifying identities, see the Amazon SES Developer Guide. If you are sending on behalf of another user and have been permitted to do so by a sending authorization policy, then you must also specify the SourceArn parameter. For more information about sending authorization, see the Amazon SES Developer Guide. Amazon SES does not support the SMTPUTF8 extension, as described in RFC6531. For this reason, the local part of a source email address (the part of the email address that precedes the \\@ sign) may only contain 7-bit ASCII characters. If the domain part of an address (the part after the \\@ sign) contains non-ASCII characters, they must be encoded using Punycode, as described in RFC3492. The sender name (also known as the friendly name) may contain non-ASCII characters. These characters must be encoded using MIME encoded-word syntax, as described in RFC 2047. MIME encoded-word syntax uses the following form: =?charset?encoding?encoded-text?=."];
      sourceArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to send for the email address specified in the Source parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to send from user\\@example.com, then you would specify the SourceArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the Source to be user\\@example.com. For more information about sending authorization, see the Amazon SES Developer Guide."];
      replyToAddresses: AddressList.t option
        [@ocaml.doc
          "The reply-to email address(es) for the message. If the recipient replies to the message, each reply-to address will receive the reply."];
      returnPath: Address.t option
        [@ocaml.doc
          "The email address that bounces and complaints will be forwarded to when feedback forwarding is enabled. If the message cannot be delivered to the recipient, then an error message will be returned from the recipient's ISP; this message will then be forwarded to the email address specified by the ReturnPath parameter. The ReturnPath parameter is never overwritten. This email address must be either individually verified with Amazon SES, or from a domain that has been verified with Amazon SES."];
      returnPathArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to use the email address specified in the ReturnPath parameter. For example, if the owner of example.com (which has ARN arn:aws:ses:us-east-1:123456789012:identity/example.com) attaches a policy to it that authorizes you to use feedback\\@example.com, then you would specify the ReturnPathArn to be arn:aws:ses:us-east-1:123456789012:identity/example.com, and the ReturnPath to be feedback\\@example.com. For more information about sending authorization, see the Amazon SES Developer Guide."];
      configurationSetName: ConfigurationSetName.t option
        [@ocaml.doc
          "The name of the configuration set to use when you send an email using SendBulkTemplatedEmail."];
      defaultTags: MessageTagList.t option
        [@ocaml.doc
          "A list of tags, in the form of name/value pairs, to apply to an email that you send to a destination using SendBulkTemplatedEmail."];
      template: TemplateName.t
        [@ocaml.doc "The template to use when sending this email."];
      templateArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the template to use when sending this email."];
      defaultTemplateData: TemplateData.t option
        [@ocaml.doc
          "A list of replacement values to apply to the template when replacement data is not specified in a Destination object. These values act as a default or fallback option when no other data is available. The template data is a JSON object, typically consisting of key-value pairs in which the keys correspond to replacement tags in the email template."];
      destinations: BulkEmailDestinationList.t
        [@ocaml.doc
          "One or more Destination objects. All of the recipients in a Destination will receive the same version of the email. You can specify up to 50 Destination objects within a Destinations array."]}
    let context_ = "SendBulkTemplatedEmailRequest"
    let make ?sourceArn =
      fun ?replyToAddresses ->
        fun ?returnPath ->
          fun ?returnPathArn ->
            fun ?configurationSetName ->
              fun ?defaultTags ->
                fun ?templateArn ->
                  fun ?defaultTemplateData ->
                    fun ~source ->
                      fun ~template ->
                        fun ~destinations ->
                          fun () ->
                            {
                              sourceArn;
                              replyToAddresses;
                              returnPath;
                              returnPathArn;
                              configurationSetName;
                              defaultTags;
                              templateArn;
                              defaultTemplateData;
                              source;
                              template;
                              destinations
                            }
    let to_value x =
      structure_to_value
        [("Source", (Some (Address.to_value x.source)));
        ("SourceArn",
          (Option.map x.sourceArn ~f:AmazonResourceName.to_value));
        ("ReplyToAddresses",
          (Option.map x.replyToAddresses ~f:AddressList.to_value));
        ("ReturnPath", (Option.map x.returnPath ~f:Address.to_value));
        ("ReturnPathArn",
          (Option.map x.returnPathArn ~f:AmazonResourceName.to_value));
        ("ConfigurationSetName",
          (Option.map x.configurationSetName ~f:ConfigurationSetName.to_value));
        ("DefaultTags",
          (Option.map x.defaultTags ~f:MessageTagList.to_value));
        ("Template", (Some (TemplateName.to_value x.template)));
        ("TemplateArn",
          (Option.map x.templateArn ~f:AmazonResourceName.to_value));
        ("DefaultTemplateData",
          (Option.map x.defaultTemplateData ~f:TemplateData.to_value));
        ("Destinations",
          (Some (BulkEmailDestinationList.to_value x.destinations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinations =
        BulkEmailDestinationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destinations") in
      let defaultTemplateData =
        (Option.map ~f:TemplateData.of_xml)
          (Xml.child xml_arg0 "DefaultTemplateData") in
      let templateArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "TemplateArn") in
      let template =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Template") in
      let defaultTags =
        (Option.map ~f:MessageTagList.of_xml)
          (Xml.child xml_arg0 "DefaultTags") in
      let configurationSetName =
        (Option.map ~f:ConfigurationSetName.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetName") in
      let returnPathArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "ReturnPathArn") in
      let returnPath =
        (Option.map ~f:Address.of_xml) (Xml.child xml_arg0 "ReturnPath") in
      let replyToAddresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "ReplyToAddresses") in
      let sourceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "SourceArn") in
      let source =
        Address.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Source") in
      make ~destinations ?defaultTemplateData ?templateArn ~template
        ?defaultTags ?configurationSetName ?returnPathArn ?returnPath
        ?replyToAddresses ?sourceArn ~source ()
    let of_json json =
      let destinations =
        field_map_exn json "Destinations" BulkEmailDestinationList.of_json in
      let defaultTemplateData =
        field_map json "DefaultTemplateData" TemplateData.of_json in
      let templateArn =
        field_map json "TemplateArn" AmazonResourceName.of_json in
      let template = field_map_exn json "Template" TemplateName.of_json in
      let defaultTags = field_map json "DefaultTags" MessageTagList.of_json in
      let configurationSetName =
        field_map json "ConfigurationSetName" ConfigurationSetName.of_json in
      let returnPathArn =
        field_map json "ReturnPathArn" AmazonResourceName.of_json in
      let returnPath = field_map json "ReturnPath" Address.of_json in
      let replyToAddresses =
        field_map json "ReplyToAddresses" AddressList.of_json in
      let sourceArn = field_map json "SourceArn" AmazonResourceName.of_json in
      let source = field_map_exn json "Source" Address.of_json in
      make ~destinations ?defaultTemplateData ?templateArn ~template
        ?defaultTags ?configurationSetName ?returnPathArn ?returnPath
        ?replyToAddresses ?sourceArn ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to send a templated email to multiple destinations using Amazon SES. For more information, see the Amazon SES Developer Guide."]
module SendBounceResponse =
  struct
    type sendBounceResult =
      {
      messageId: MessageId.t option
        [@ocaml.doc "The message ID of the bounce message."]}
    and responseMetaData = unit
    and t =
      {
      sendBounceResult: sendBounceResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `MessageRejected of MessageRejected.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "SendBounceResponse"
    let make ?messageId =
      fun () -> { sendBounceResult = { messageId }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "MessageRejected" -> `MessageRejected (MessageRejected.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `MessageRejected e ->
          `Assoc
            [("error", (`String "MessageRejected"));
            ("details", (MessageRejected.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.sendBounceResult in
      structure_to_wrapped_value
        [("MessageId", (Option.map x.messageId ~f:MessageId.to_value))]
        ~wrapper:"SendBounceResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "SendBounceResult" in
      let messageId =
        (Option.map ~f:MessageId.of_xml) (Xml.child xml_arg0 "MessageId") in
      make ?messageId ()
    let of_json json =
      let messageId = field_map json "MessageId" MessageId.of_json in
      make ?messageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a unique message ID."]
module SendBounceRequest =
  struct
    type nonrec t =
      {
      originalMessageId: MessageId.t
        [@ocaml.doc "The message ID of the message to be bounced."];
      bounceSender: Address.t
        [@ocaml.doc
          "The address to use in the \"From\" header of the bounce message. This must be an identity that you have verified with Amazon SES."];
      explanation: Explanation.t option
        [@ocaml.doc
          "Human-readable text for the bounce message to explain the failure. If not specified, the text will be auto-generated based on the bounced recipient information."];
      messageDsn: MessageDsn.t option
        [@ocaml.doc
          "Message-related DSN fields. If not specified, Amazon SES will choose the values."];
      bouncedRecipientInfoList: BouncedRecipientInfoList.t
        [@ocaml.doc
          "A list of recipients of the bounced message, including the information required to create the Delivery Status Notifications (DSNs) for the recipients. You must specify at least one BouncedRecipientInfo in the list."];
      bounceSenderArn: AmazonResourceName.t option
        [@ocaml.doc
          "This parameter is used only for sending authorization. It is the ARN of the identity that is associated with the sending authorization policy that permits you to use the address in the \"From\" header of the bounce. For more information about sending authorization, see the Amazon SES Developer Guide."]}
    let context_ = "SendBounceRequest"
    let make ?explanation =
      fun ?messageDsn ->
        fun ?bounceSenderArn ->
          fun ~originalMessageId ->
            fun ~bounceSender ->
              fun ~bouncedRecipientInfoList ->
                fun () ->
                  {
                    explanation;
                    messageDsn;
                    bounceSenderArn;
                    originalMessageId;
                    bounceSender;
                    bouncedRecipientInfoList
                  }
    let to_value x =
      structure_to_value
        [("OriginalMessageId",
           (Some (MessageId.to_value x.originalMessageId)));
        ("BounceSender", (Some (Address.to_value x.bounceSender)));
        ("Explanation", (Option.map x.explanation ~f:Explanation.to_value));
        ("MessageDsn", (Option.map x.messageDsn ~f:MessageDsn.to_value));
        ("BouncedRecipientInfoList",
          (Some
             (BouncedRecipientInfoList.to_value x.bouncedRecipientInfoList)));
        ("BounceSenderArn",
          (Option.map x.bounceSenderArn ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bounceSenderArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "BounceSenderArn") in
      let bouncedRecipientInfoList =
        BouncedRecipientInfoList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "BouncedRecipientInfoList") in
      let messageDsn =
        (Option.map ~f:MessageDsn.of_xml) (Xml.child xml_arg0 "MessageDsn") in
      let explanation =
        (Option.map ~f:Explanation.of_xml) (Xml.child xml_arg0 "Explanation") in
      let bounceSender =
        Address.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BounceSender") in
      let originalMessageId =
        MessageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginalMessageId") in
      make ?bounceSenderArn ~bouncedRecipientInfoList ?messageDsn
        ?explanation ~bounceSender ~originalMessageId ()
    let of_json json =
      let bounceSenderArn =
        field_map json "BounceSenderArn" AmazonResourceName.of_json in
      let bouncedRecipientInfoList =
        field_map_exn json "BouncedRecipientInfoList"
          BouncedRecipientInfoList.of_json in
      let messageDsn = field_map json "MessageDsn" MessageDsn.of_json in
      let explanation = field_map json "Explanation" Explanation.of_json in
      let bounceSender = field_map_exn json "BounceSender" Address.of_json in
      let originalMessageId =
        field_map_exn json "OriginalMessageId" MessageId.of_json in
      make ?bounceSenderArn ~bouncedRecipientInfoList ?messageDsn
        ?explanation ~bounceSender ~originalMessageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to send a bounce message to the sender of an email you received through Amazon SES."]
module ReorderReceiptRuleSetResponse =
  struct
    type reorderReceiptRuleSetResult = unit
    and responseMetaData = unit
    and t =
      {
      reorderReceiptRuleSetResult: reorderReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `RuleDoesNotExistException of RuleDoesNotExistException.t 
      | `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { reorderReceiptRuleSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_json json)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_xml xml)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `RuleDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleDoesNotExistException"));
            ("details", (RuleDoesNotExistException.to_json e))]
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module ReorderReceiptRuleSetRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc "The name of the receipt rule set to reorder."];
      ruleNames: ReceiptRuleNamesList.t
        [@ocaml.doc
          "A list of the specified receipt rule set's receipt rules in the order that you want to put them."]}
    let context_ = "ReorderReceiptRuleSetRequest"
    let make ~ruleSetName =
      fun ~ruleNames -> fun () -> { ruleSetName; ruleNames }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("RuleNames", (Some (ReceiptRuleNamesList.to_value x.ruleNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleNames =
        ReceiptRuleNamesList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleNames") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~ruleNames ~ruleSetName ()
    let of_json json =
      let ruleNames =
        field_map_exn json "RuleNames" ReceiptRuleNamesList.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~ruleNames ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to reorder the receipt rules within a receipt rule set. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module PutIdentityPolicyResponse =
  struct
    type putIdentityPolicyResult = unit
    and responseMetaData = unit
    and t =
      {
      putIdentityPolicyResult: putIdentityPolicyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidPolicyException of InvalidPolicyException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { putIdentityPolicyResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "InvalidPolicyException" ->
          `InvalidPolicyException (InvalidPolicyException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPolicyException" ->
          `InvalidPolicyException (InvalidPolicyException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPolicyException e ->
          `Assoc
            [("error", (`String "InvalidPolicyException"));
            ("details", (InvalidPolicyException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module PutIdentityPolicyRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity that the policy will apply to. You can specify an identity by using its name or by using its Amazon Resource Name (ARN). Examples: user\\@example.com, example.com, arn:aws:ses:us-east-1:123456789012:identity/example.com. To successfully call this API, you must own the identity."];
      policyName: PolicyName.t
        [@ocaml.doc
          "The name of the policy. The policy name cannot exceed 64 characters and can only include alphanumeric characters, dashes, and underscores."];
      policy: Policy.t
        [@ocaml.doc
          "The text of the policy in JSON format. The policy cannot exceed 4 KB. For information about the syntax of sending authorization policies, see the Amazon SES Developer Guide."]}
    let context_ = "PutIdentityPolicyRequest"
    let make ~identity =
      fun ~policyName ->
        fun ~policy -> fun () -> { identity; policyName; policy }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("PolicyName", (Some (PolicyName.to_value x.policyName)));
        ("Policy", (Some (Policy.to_value x.policy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        Policy.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Policy") in
      let policyName =
        PolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~policy ~policyName ~identity ()
    let of_json json =
      let policy = field_map_exn json "Policy" Policy.of_json in
      let policyName = field_map_exn json "PolicyName" PolicyName.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~policy ~policyName ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to add or update a sending authorization policy for an identity. Sending authorization is an Amazon SES feature that enables you to authorize other senders to use your identities. For information, see the Amazon SES Developer Guide."]
module PutConfigurationSetDeliveryOptionsResponse =
  struct
    type putConfigurationSetDeliveryOptionsResult = unit
    and responseMetaData = unit
    and t =
      {
      putConfigurationSetDeliveryOptionsResult:
        putConfigurationSetDeliveryOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `InvalidDeliveryOptionsException of InvalidDeliveryOptionsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { putConfigurationSetDeliveryOptionsResult = (); responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "InvalidDeliveryOptionsException" ->
          `InvalidDeliveryOptionsException
            (InvalidDeliveryOptionsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "InvalidDeliveryOptionsException" ->
          `InvalidDeliveryOptionsException
            (InvalidDeliveryOptionsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `InvalidDeliveryOptionsException e ->
          `Assoc
            [("error", (`String "InvalidDeliveryOptionsException"));
            ("details", (InvalidDeliveryOptionsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An HTTP 200 response if the request succeeds, or an error message if the request fails."]
module PutConfigurationSetDeliveryOptionsRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set that you want to specify the delivery options for."];
      deliveryOptions: DeliveryOptions.t option
        [@ocaml.doc
          "Specifies whether messages that use the configuration set are required to use Transport Layer Security (TLS)."]}
    let context_ = "PutConfigurationSetDeliveryOptionsRequest"
    let make ?deliveryOptions =
      fun ~configurationSetName ->
        fun () -> { deliveryOptions; configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("DeliveryOptions",
          (Option.map x.deliveryOptions ~f:DeliveryOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deliveryOptions =
        (Option.map ~f:DeliveryOptions.of_xml)
          (Xml.child xml_arg0 "DeliveryOptions") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ?deliveryOptions ~configurationSetName ()
    let of_json json =
      let deliveryOptions =
        field_map json "DeliveryOptions" DeliveryOptions.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ?deliveryOptions ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to modify the delivery options for a configuration set."]
module ListVerifiedEmailAddressesResponse =
  struct
    type listVerifiedEmailAddressesResult =
      {
      verifiedEmailAddresses: AddressList.t option
        [@ocaml.doc "A list of email addresses that have been verified."]}
    and responseMetaData = unit
    and t =
      {
      listVerifiedEmailAddressesResult: listVerifiedEmailAddressesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListVerifiedEmailAddressesResponse"
    let make ?verifiedEmailAddresses =
      fun () ->
        {
          listVerifiedEmailAddressesResult = { verifiedEmailAddresses };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listVerifiedEmailAddressesResult in
      structure_to_wrapped_value
        [("VerifiedEmailAddresses",
           (Option.map x.verifiedEmailAddresses ~f:AddressList.to_value))]
        ~wrapper:"ListVerifiedEmailAddressesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ListVerifiedEmailAddressesResult" in
      let verifiedEmailAddresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "VerifiedEmailAddresses") in
      make ?verifiedEmailAddresses ()
    let of_json json =
      let verifiedEmailAddresses =
        field_map json "VerifiedEmailAddresses" AddressList.of_json in
      make ?verifiedEmailAddresses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of email addresses that you have verified with Amazon SES under your AWS account."]
module ListTemplatesResponse =
  struct
    type listTemplatesResult =
      {
      templatesMetadata: TemplateMetadataList.t option
        [@ocaml.doc
          "An array the contains the name and creation time stamp for each template in your Amazon SES account."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token indicating that there are additional email templates available to be listed. Pass this token to a subsequent call to ListTemplates to retrieve the next 50 email templates."]}
    and responseMetaData = unit
    and t =
      {
      listTemplatesResult: listTemplatesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTemplatesResponse"
    let make ?templatesMetadata =
      fun ?nextToken ->
        fun () ->
          {
            listTemplatesResult = { templatesMetadata; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listTemplatesResult in
      structure_to_wrapped_value
        [("TemplatesMetadata",
           (Option.map x.templatesMetadata ~f:TemplateMetadataList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
        ~wrapper:"ListTemplatesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "ListTemplatesResult" in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let templatesMetadata =
        (Option.map ~f:TemplateMetadataList.of_xml)
          (Xml.child xml_arg0 "TemplatesMetadata") in
      make ?nextToken ?templatesMetadata ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let templatesMetadata =
        field_map json "TemplatesMetadata" TemplateMetadataList.of_json in
      make ?nextToken ?templatesMetadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the email templates present in your Amazon SES account in the current AWS Region. You can execute this operation no more than once per second."]
module ListTemplatesRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from a previous call to ListTemplates to indicate the position in the list of email templates."];
      maxItems: MaxItems.t option
        [@ocaml.doc
          "The maximum number of templates to return. This value must be at least 1 and less than or equal to 10. If you do not specify a value, or if you specify a value less than 1 or greater than 10, the operation will return up to 10 results."]}
    let make ?nextToken = fun ?maxItems -> fun () -> { nextToken; maxItems }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxItems", (Option.map x.maxItems ~f:MaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:MaxItems.of_xml) (Xml.child xml_arg0 "MaxItems") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?maxItems ?nextToken ()
    let of_json json =
      let maxItems = field_map json "MaxItems" MaxItems.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?maxItems ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the email templates present in your Amazon SES account in the current AWS Region. You can execute this operation no more than once per second."]
module ListReceiptRuleSetsResponse =
  struct
    type listReceiptRuleSetsResult =
      {
      ruleSets: ReceiptRuleSetsLists.t option
        [@ocaml.doc
          "The metadata for the currently active receipt rule set. The metadata consists of the rule set name and the timestamp of when the rule set was created."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token indicating that there are additional receipt rule sets available to be listed. Pass this token to successive calls of ListReceiptRuleSets to retrieve up to 100 receipt rule sets at a time."]}
    and responseMetaData = unit
    and t =
      {
      listReceiptRuleSetsResult: listReceiptRuleSetsResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListReceiptRuleSetsResponse"
    let make ?ruleSets =
      fun ?nextToken ->
        fun () ->
          {
            listReceiptRuleSetsResult = { ruleSets; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listReceiptRuleSetsResult in
      structure_to_wrapped_value
        [("RuleSets",
           (Option.map x.ruleSets ~f:ReceiptRuleSetsLists.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
        ~wrapper:"ListReceiptRuleSetsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ListReceiptRuleSetsResult" in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let ruleSets =
        (Option.map ~f:ReceiptRuleSetsLists.of_xml)
          (Xml.child xml_arg0 "RuleSets") in
      make ?nextToken ?ruleSets ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let ruleSets = field_map json "RuleSets" ReceiptRuleSetsLists.of_json in
      make ?nextToken ?ruleSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of receipt rule sets that exist under your AWS account."]
module ListReceiptRuleSetsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from a previous call to ListReceiptRuleSets to indicate the position in the receipt rule set list."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to list the receipt rule sets that exist under your AWS account. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module ListReceiptFiltersResponse =
  struct
    type listReceiptFiltersResult =
      {
      filters: ReceiptFilterList.t option
        [@ocaml.doc
          "A list of IP address filter data structures, which each consist of a name, an IP address range, and whether to allow or block mail from it."]}
    and responseMetaData = unit
    and t =
      {
      listReceiptFiltersResult: listReceiptFiltersResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListReceiptFiltersResponse"
    let make ?filters =
      fun () ->
        { listReceiptFiltersResult = { filters }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listReceiptFiltersResult in
      structure_to_wrapped_value
        [("Filters", (Option.map x.filters ~f:ReceiptFilterList.to_value))]
        ~wrapper:"ListReceiptFiltersResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ListReceiptFiltersResult" in
      let filters =
        (Option.map ~f:ReceiptFilterList.of_xml)
          (Xml.child xml_arg0 "Filters") in
      make ?filters ()
    let of_json json =
      let filters = field_map json "Filters" ReceiptFilterList.of_json in
      make ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of IP address filters that exist under your AWS account."]
module ListReceiptFiltersRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to list the IP address filters that exist under your AWS account. You use IP address filters when you receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module ListIdentityPoliciesResponse =
  struct
    type listIdentityPoliciesResult =
      {
      policyNames: PolicyNameList.t
        [@ocaml.doc
          "A list of names of policies that apply to the specified identity."]}
    and responseMetaData = unit
    and t =
      {
      listIdentityPoliciesResult: listIdentityPoliciesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListIdentityPoliciesResponse"
    let make ~policyNames =
      fun () ->
        { listIdentityPoliciesResult = { policyNames }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listIdentityPoliciesResult in
      structure_to_wrapped_value
        [("PolicyNames", (Some (PolicyNameList.to_value x.policyNames)))]
        ~wrapper:"ListIdentityPoliciesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ListIdentityPoliciesResult" in
      let policyNames =
        PolicyNameList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyNames") in
      make ~policyNames ()
    let of_json json =
      let policyNames =
        field_map_exn json "PolicyNames" PolicyNameList.of_json in
      make ~policyNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of names of sending authorization policies that apply to an identity."]
module ListIdentityPoliciesRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity that is associated with the policy for which the policies will be listed. You can specify an identity by using its name or by using its Amazon Resource Name (ARN). Examples: user\\@example.com, example.com, arn:aws:ses:us-east-1:123456789012:identity/example.com. To successfully call this API, you must own the identity."]}
    let context_ = "ListIdentityPoliciesRequest"
    let make ~identity = fun () -> { identity }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~identity ()
    let of_json json =
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return a list of sending authorization policies that are attached to an identity. Sending authorization is an Amazon SES feature that enables you to authorize other senders to use your identities. For information, see the Amazon SES Developer Guide."]
module ListIdentitiesResponse =
  struct
    type listIdentitiesResult =
      {
      identities: IdentityList.t [@ocaml.doc "A list of identities."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token used for pagination."]}
    and responseMetaData = unit
    and t =
      {
      listIdentitiesResult: listIdentitiesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListIdentitiesResponse"
    let make ?nextToken =
      fun ~identities ->
        fun () ->
          {
            listIdentitiesResult = { nextToken; identities };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listIdentitiesResult in
      structure_to_wrapped_value
        [("Identities", (Some (IdentityList.to_value x.identities)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
        ~wrapper:"ListIdentitiesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "ListIdentitiesResult" in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let identities =
        IdentityList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Identities") in
      make ?nextToken ~identities ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let identities = field_map_exn json "Identities" IdentityList.of_json in
      make ?nextToken ~identities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of all identities that you have attempted to verify under your AWS account, regardless of verification status."]
module ListIdentitiesRequest =
  struct
    type nonrec t =
      {
      identityType: IdentityType.t option
        [@ocaml.doc
          "The type of the identities to list. Possible values are \"EmailAddress\" and \"Domain\". If this parameter is omitted, then all identities will be listed."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to use for pagination."];
      maxItems: MaxItems.t option
        [@ocaml.doc
          "The maximum number of identities per page. Possible values are 1-1000 inclusive."]}
    let make ?identityType =
      fun ?nextToken ->
        fun ?maxItems -> fun () -> { identityType; nextToken; maxItems }
    let to_value x =
      structure_to_value
        [("IdentityType",
           (Option.map x.identityType ~f:IdentityType.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxItems", (Option.map x.maxItems ~f:MaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:MaxItems.of_xml) (Xml.child xml_arg0 "MaxItems") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let identityType =
        (Option.map ~f:IdentityType.of_xml)
          (Xml.child xml_arg0 "IdentityType") in
      make ?maxItems ?nextToken ?identityType ()
    let of_json json =
      let maxItems = field_map json "MaxItems" MaxItems.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let identityType = field_map json "IdentityType" IdentityType.of_json in
      make ?maxItems ?nextToken ?identityType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return a list of all identities (email addresses and domains) that you have attempted to verify under your AWS account, regardless of verification status."]
module ListCustomVerificationEmailTemplatesResponse =
  struct
    type listCustomVerificationEmailTemplatesResult =
      {
      customVerificationEmailTemplates:
        CustomVerificationEmailTemplates.t option
        [@ocaml.doc
          "A list of the custom verification email templates that exist in your account."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token indicating that there are additional custom verification email templates available to be listed. Pass this token to a subsequent call to ListTemplates to retrieve the next 50 custom verification email templates."]}
    and responseMetaData = unit
    and t =
      {
      listCustomVerificationEmailTemplatesResult:
        listCustomVerificationEmailTemplatesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListCustomVerificationEmailTemplatesResponse"
    let make ?customVerificationEmailTemplates =
      fun ?nextToken ->
        fun () ->
          {
            listCustomVerificationEmailTemplatesResult =
              { customVerificationEmailTemplates; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listCustomVerificationEmailTemplatesResult in
      structure_to_wrapped_value
        [("CustomVerificationEmailTemplates",
           (Option.map x.customVerificationEmailTemplates
              ~f:CustomVerificationEmailTemplates.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
        ~wrapper:"ListCustomVerificationEmailTemplatesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "ListCustomVerificationEmailTemplatesResult" in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let customVerificationEmailTemplates =
        (Option.map ~f:CustomVerificationEmailTemplates.of_xml)
          (Xml.child xml_arg0 "CustomVerificationEmailTemplates") in
      make ?nextToken ?customVerificationEmailTemplates ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let customVerificationEmailTemplates =
        field_map json "CustomVerificationEmailTemplates"
          CustomVerificationEmailTemplates.of_json in
      make ?nextToken ?customVerificationEmailTemplates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A paginated list of custom verification email templates."]
module ListCustomVerificationEmailTemplatesRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "An array the contains the name and creation time stamp for each template in your Amazon SES account."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of custom verification email templates to return. This value must be at least 1 and less than or equal to 50. If you do not specify a value, or if you specify a value less than 1 or greater than 50, the operation will return up to 50 results."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to list the existing custom verification email templates for your account. For more information about custom verification email templates, see Using Custom Verification Email Templates in the Amazon SES Developer Guide."]
module ListConfigurationSetsResponse =
  struct
    type listConfigurationSetsResult =
      {
      configurationSets: ConfigurationSets.t option
        [@ocaml.doc "A list of configuration sets."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token indicating that there are additional configuration sets available to be listed. Pass this token to successive calls of ListConfigurationSets."]}
    and responseMetaData = unit
    and t =
      {
      listConfigurationSetsResult: listConfigurationSetsResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "ListConfigurationSetsResponse"
    let make ?configurationSets =
      fun ?nextToken ->
        fun () ->
          {
            listConfigurationSetsResult = { configurationSets; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listConfigurationSetsResult in
      structure_to_wrapped_value
        [("ConfigurationSets",
           (Option.map x.configurationSets ~f:ConfigurationSets.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
        ~wrapper:"ListConfigurationSetsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ListConfigurationSetsResult" in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let configurationSets =
        (Option.map ~f:ConfigurationSets.of_xml)
          (Xml.child xml_arg0 "ConfigurationSets") in
      make ?nextToken ?configurationSets ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let configurationSets =
        field_map json "ConfigurationSets" ConfigurationSets.of_json in
      make ?nextToken ?configurationSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of configuration sets associated with your AWS account. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module ListConfigurationSetsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from a previous call to ListConfigurationSets to indicate the position of the configuration set in the configuration set list."];
      maxItems: MaxItems.t option
        [@ocaml.doc "The number of configuration sets to return."]}
    let make ?nextToken = fun ?maxItems -> fun () -> { nextToken; maxItems }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxItems", (Option.map x.maxItems ~f:MaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:MaxItems.of_xml) (Xml.child xml_arg0 "MaxItems") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?maxItems ?nextToken ()
    let of_json json =
      let maxItems = field_map json "MaxItems" MaxItems.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?maxItems ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to list the configuration sets associated with your AWS account. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module GetTemplateResponse =
  struct
    type getTemplateResult = {
      template: Template.t option }
    and responseMetaData = unit
    and t =
      {
      getTemplateResult: getTemplateResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `TemplateDoesNotExistException of TemplateDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetTemplateResponse"
    let make ?template =
      fun () -> { getTemplateResult = { template }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "TemplateDoesNotExistException" ->
          `TemplateDoesNotExistException
            (TemplateDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `TemplateDoesNotExistException e ->
          `Assoc
            [("error", (`String "TemplateDoesNotExistException"));
            ("details", (TemplateDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getTemplateResult in
      structure_to_wrapped_value
        [("Template", (Option.map x.template ~f:Template.to_value))]
        ~wrapper:"GetTemplateResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "GetTemplateResult" in
      let template =
        (Option.map ~f:Template.of_xml) (Xml.child xml_arg0 "Template") in
      make ?template ()
    let of_json json =
      let template = field_map json "Template" Template.of_json in
      make ?template ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Displays the template object (which includes the Subject line, HTML part and text part) for the template you specify. You can execute this operation no more than once per second."]
module GetTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc "The name of the template you want to retrieve."]}
    let context_ = "GetTemplateRequest"
    let make ~templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ~templateName ()
    let of_json json =
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Displays the template object (which includes the Subject line, HTML part and text part) for the template you specify. You can execute this operation no more than once per second."]
module GetSendStatisticsResponse =
  struct
    type getSendStatisticsResult =
      {
      sendDataPoints: SendDataPointList.t option
        [@ocaml.doc
          "A list of data points, each of which represents 15 minutes of activity."]}
    and responseMetaData = unit
    and t =
      {
      getSendStatisticsResult: getSendStatisticsResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetSendStatisticsResponse"
    let make ?sendDataPoints =
      fun () ->
        { getSendStatisticsResult = { sendDataPoints }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getSendStatisticsResult in
      structure_to_wrapped_value
        [("SendDataPoints",
           (Option.map x.sendDataPoints ~f:SendDataPointList.to_value))]
        ~wrapper:"GetSendStatisticsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "GetSendStatisticsResult" in
      let sendDataPoints =
        (Option.map ~f:SendDataPointList.of_xml)
          (Xml.child xml_arg0 "SendDataPoints") in
      make ?sendDataPoints ()
    let of_json json =
      let sendDataPoints =
        field_map json "SendDataPoints" SendDataPointList.of_json in
      make ?sendDataPoints ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a list of data points. This list contains aggregated data from the previous two weeks of your sending activity with Amazon SES."]
module GetSendQuotaResponse =
  struct
    type getSendQuotaResult =
      {
      max24HourSend: Max24HourSend.t option
        [@ocaml.doc
          "The maximum number of emails the user is allowed to send in a 24-hour interval. A value of -1 signifies an unlimited quota."];
      maxSendRate: MaxSendRate.t option
        [@ocaml.doc
          "The maximum number of emails that Amazon SES can accept from the user's account per second. The rate at which Amazon SES accepts the user's messages might be less than the maximum send rate."];
      sentLast24Hours: SentLast24Hours.t option
        [@ocaml.doc
          "The number of emails sent during the previous 24 hours."]}
    and responseMetaData = unit
    and t =
      {
      getSendQuotaResult: getSendQuotaResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetSendQuotaResponse"
    let make ?max24HourSend =
      fun ?maxSendRate ->
        fun ?sentLast24Hours ->
          fun () ->
            {
              getSendQuotaResult =
                { max24HourSend; maxSendRate; sentLast24Hours };
              responseMetaData = ()
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getSendQuotaResult in
      structure_to_wrapped_value
        [("Max24HourSend",
           (Option.map x.max24HourSend ~f:Max24HourSend.to_value));
        ("MaxSendRate", (Option.map x.maxSendRate ~f:MaxSendRate.to_value));
        ("SentLast24Hours",
          (Option.map x.sentLast24Hours ~f:SentLast24Hours.to_value))]
        ~wrapper:"GetSendQuotaResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "GetSendQuotaResult" in
      let sentLast24Hours =
        (Option.map ~f:SentLast24Hours.of_xml)
          (Xml.child xml_arg0 "SentLast24Hours") in
      let maxSendRate =
        (Option.map ~f:MaxSendRate.of_xml) (Xml.child xml_arg0 "MaxSendRate") in
      let max24HourSend =
        (Option.map ~f:Max24HourSend.of_xml)
          (Xml.child xml_arg0 "Max24HourSend") in
      make ?sentLast24Hours ?maxSendRate ?max24HourSend ()
    let of_json json =
      let sentLast24Hours =
        field_map json "SentLast24Hours" SentLast24Hours.of_json in
      let maxSendRate = field_map json "MaxSendRate" MaxSendRate.of_json in
      let max24HourSend =
        field_map json "Max24HourSend" Max24HourSend.of_json in
      make ?sentLast24Hours ?maxSendRate ?max24HourSend ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents your Amazon SES daily sending quota, maximum send rate, and the number of emails you have sent in the last 24 hours."]
module GetIdentityVerificationAttributesResponse =
  struct
    type getIdentityVerificationAttributesResult =
      {
      verificationAttributes: VerificationAttributes.t
        [@ocaml.doc
          "A map of Identities to IdentityVerificationAttributes objects."]}
    and responseMetaData = unit
    and t =
      {
      getIdentityVerificationAttributesResult:
        getIdentityVerificationAttributesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetIdentityVerificationAttributesResponse"
    let make ~verificationAttributes =
      fun () ->
        {
          getIdentityVerificationAttributesResult =
            { verificationAttributes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getIdentityVerificationAttributesResult in
      structure_to_wrapped_value
        [("VerificationAttributes",
           (Some (VerificationAttributes.to_value x.verificationAttributes)))]
        ~wrapper:"GetIdentityVerificationAttributesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "GetIdentityVerificationAttributesResult" in
      let verificationAttributes =
        VerificationAttributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VerificationAttributes") in
      make ~verificationAttributes ()
    let of_json json =
      let verificationAttributes =
        field_map_exn json "VerificationAttributes"
          VerificationAttributes.of_json in
      make ~verificationAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon SES verification status of a list of identities. For domain identities, this response also contains the verification token."]
module GetIdentityVerificationAttributesRequest =
  struct
    type nonrec t =
      {
      identities: IdentityList.t [@ocaml.doc "A list of identities."]}
    let context_ = "GetIdentityVerificationAttributesRequest"
    let make ~identities = fun () -> { identities }
    let to_value x =
      structure_to_value
        [("Identities", (Some (IdentityList.to_value x.identities)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identities =
        IdentityList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Identities") in
      make ~identities ()
    let of_json json =
      let identities = field_map_exn json "Identities" IdentityList.of_json in
      make ~identities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the Amazon SES verification status of a list of identities. For domain identities, this request also returns the verification token. For information about verifying identities with Amazon SES, see the Amazon SES Developer Guide."]
module GetIdentityPoliciesResponse =
  struct
    type getIdentityPoliciesResult =
      {
      policies: PolicyMap.t [@ocaml.doc "A map of policy names to policies."]}
    and responseMetaData = unit
    and t =
      {
      getIdentityPoliciesResult: getIdentityPoliciesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetIdentityPoliciesResponse"
    let make ~policies =
      fun () ->
        { getIdentityPoliciesResult = { policies }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getIdentityPoliciesResult in
      structure_to_wrapped_value
        [("Policies", (Some (PolicyMap.to_value x.policies)))]
        ~wrapper:"GetIdentityPoliciesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "GetIdentityPoliciesResult" in
      let policies =
        PolicyMap.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Policies") in
      make ~policies ()
    let of_json json =
      let policies = field_map_exn json "Policies" PolicyMap.of_json in
      make ~policies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the requested sending authorization policies."]
module GetIdentityPoliciesRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity for which the policies will be retrieved. You can specify an identity by using its name or by using its Amazon Resource Name (ARN). Examples: user\\@example.com, example.com, arn:aws:ses:us-east-1:123456789012:identity/example.com. To successfully call this API, you must own the identity."];
      policyNames: PolicyNameList.t
        [@ocaml.doc
          "A list of the names of policies to be retrieved. You can retrieve a maximum of 20 policies at a time. If you do not know the names of the policies that are attached to the identity, you can use ListIdentityPolicies."]}
    let context_ = "GetIdentityPoliciesRequest"
    let make ~identity =
      fun ~policyNames -> fun () -> { identity; policyNames }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("PolicyNames", (Some (PolicyNameList.to_value x.policyNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyNames =
        PolicyNameList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyNames") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~policyNames ~identity ()
    let of_json json =
      let policyNames =
        field_map_exn json "PolicyNames" PolicyNameList.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~policyNames ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the requested sending authorization policies for an identity. Sending authorization is an Amazon SES feature that enables you to authorize other senders to use your identities. For information, see the Amazon SES Developer Guide."]
module GetIdentityNotificationAttributesResponse =
  struct
    type getIdentityNotificationAttributesResult =
      {
      notificationAttributes: NotificationAttributes.t
        [@ocaml.doc "A map of Identity to IdentityNotificationAttributes."]}
    and responseMetaData = unit
    and t =
      {
      getIdentityNotificationAttributesResult:
        getIdentityNotificationAttributesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetIdentityNotificationAttributesResponse"
    let make ~notificationAttributes =
      fun () ->
        {
          getIdentityNotificationAttributesResult =
            { notificationAttributes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getIdentityNotificationAttributesResult in
      structure_to_wrapped_value
        [("NotificationAttributes",
           (Some (NotificationAttributes.to_value x.notificationAttributes)))]
        ~wrapper:"GetIdentityNotificationAttributesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "GetIdentityNotificationAttributesResult" in
      let notificationAttributes =
        NotificationAttributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotificationAttributes") in
      make ~notificationAttributes ()
    let of_json json =
      let notificationAttributes =
        field_map_exn json "NotificationAttributes"
          NotificationAttributes.of_json in
      make ~notificationAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the notification attributes for a list of identities."]
module GetIdentityNotificationAttributesRequest =
  struct
    type nonrec t =
      {
      identities: IdentityList.t
        [@ocaml.doc
          "A list of one or more identities. You can specify an identity by using its name or by using its Amazon Resource Name (ARN). Examples: user\\@example.com, example.com, arn:aws:ses:us-east-1:123456789012:identity/example.com."]}
    let context_ = "GetIdentityNotificationAttributesRequest"
    let make ~identities = fun () -> { identities }
    let to_value x =
      structure_to_value
        [("Identities", (Some (IdentityList.to_value x.identities)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identities =
        IdentityList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Identities") in
      make ~identities ()
    let of_json json =
      let identities = field_map_exn json "Identities" IdentityList.of_json in
      make ~identities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the notification attributes for a list of identities you verified with Amazon SES. For information about Amazon SES notifications, see the Amazon SES Developer Guide."]
module GetIdentityMailFromDomainAttributesResponse =
  struct
    type getIdentityMailFromDomainAttributesResult =
      {
      mailFromDomainAttributes: MailFromDomainAttributes.t
        [@ocaml.doc "A map of identities to custom MAIL FROM attributes."]}
    and responseMetaData = unit
    and t =
      {
      getIdentityMailFromDomainAttributesResult:
        getIdentityMailFromDomainAttributesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetIdentityMailFromDomainAttributesResponse"
    let make ~mailFromDomainAttributes =
      fun () ->
        {
          getIdentityMailFromDomainAttributesResult =
            { mailFromDomainAttributes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getIdentityMailFromDomainAttributesResult in
      structure_to_wrapped_value
        [("MailFromDomainAttributes",
           (Some
              (MailFromDomainAttributes.to_value x.mailFromDomainAttributes)))]
        ~wrapper:"GetIdentityMailFromDomainAttributesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "GetIdentityMailFromDomainAttributesResult" in
      let mailFromDomainAttributes =
        MailFromDomainAttributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "MailFromDomainAttributes") in
      make ~mailFromDomainAttributes ()
    let of_json json =
      let mailFromDomainAttributes =
        field_map_exn json "MailFromDomainAttributes"
          MailFromDomainAttributes.of_json in
      make ~mailFromDomainAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the custom MAIL FROM attributes for a list of identities."]
module GetIdentityMailFromDomainAttributesRequest =
  struct
    type nonrec t =
      {
      identities: IdentityList.t
        [@ocaml.doc "A list of one or more identities."]}
    let context_ = "GetIdentityMailFromDomainAttributesRequest"
    let make ~identities = fun () -> { identities }
    let to_value x =
      structure_to_value
        [("Identities", (Some (IdentityList.to_value x.identities)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identities =
        IdentityList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Identities") in
      make ~identities ()
    let of_json json =
      let identities = field_map_exn json "Identities" IdentityList.of_json in
      make ~identities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the Amazon SES custom MAIL FROM attributes for a list of identities. For information about using a custom MAIL FROM domain, see the Amazon SES Developer Guide."]
module GetIdentityDkimAttributesResponse =
  struct
    type getIdentityDkimAttributesResult =
      {
      dkimAttributes: DkimAttributes.t
        [@ocaml.doc "The DKIM attributes for an email address or a domain."]}
    and responseMetaData = unit
    and t =
      {
      getIdentityDkimAttributesResult: getIdentityDkimAttributesResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetIdentityDkimAttributesResponse"
    let make ~dkimAttributes =
      fun () ->
        {
          getIdentityDkimAttributesResult = { dkimAttributes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getIdentityDkimAttributesResult in
      structure_to_wrapped_value
        [("DkimAttributes",
           (Some (DkimAttributes.to_value x.dkimAttributes)))]
        ~wrapper:"GetIdentityDkimAttributesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "GetIdentityDkimAttributesResult" in
      let dkimAttributes =
        DkimAttributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DkimAttributes") in
      make ~dkimAttributes ()
    let of_json json =
      let dkimAttributes =
        field_map_exn json "DkimAttributes" DkimAttributes.of_json in
      make ~dkimAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the status of Amazon SES Easy DKIM signing for an identity. For domain identities, this response also contains the DKIM tokens that are required for Easy DKIM signing, and whether Amazon SES successfully verified that these tokens were published."]
module GetIdentityDkimAttributesRequest =
  struct
    type nonrec t =
      {
      identities: IdentityList.t
        [@ocaml.doc
          "A list of one or more verified identities - email addresses, domains, or both."]}
    let context_ = "GetIdentityDkimAttributesRequest"
    let make ~identities = fun () -> { identities }
    let to_value x =
      structure_to_value
        [("Identities", (Some (IdentityList.to_value x.identities)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identities =
        IdentityList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Identities") in
      make ~identities ()
    let of_json json =
      let identities = field_map_exn json "Identities" IdentityList.of_json in
      make ~identities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request for the status of Amazon SES Easy DKIM signing for an identity. For domain identities, this request also returns the DKIM tokens that are required for Easy DKIM signing, and whether Amazon SES successfully verified that these tokens were published. For more information about Easy DKIM, see the Amazon SES Developer Guide."]
module GetCustomVerificationEmailTemplateResponse =
  struct
    type getCustomVerificationEmailTemplateResult =
      {
      templateName: TemplateName.t option
        [@ocaml.doc "The name of the custom verification email template."];
      fromEmailAddress: FromAddress.t option
        [@ocaml.doc
          "The email address that the custom verification email is sent from."];
      templateSubject: Subject.t option
        [@ocaml.doc "The subject line of the custom verification email."];
      templateContent: TemplateContent.t option
        [@ocaml.doc "The content of the custom verification email."];
      successRedirectionURL: SuccessRedirectionURL.t option
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is successfully verified."];
      failureRedirectionURL: FailureRedirectionURL.t option
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is not successfully verified."]}
    and responseMetaData = unit
    and t =
      {
      getCustomVerificationEmailTemplateResult:
        getCustomVerificationEmailTemplateResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `CustomVerificationEmailTemplateDoesNotExistException of
          CustomVerificationEmailTemplateDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetCustomVerificationEmailTemplateResponse"
    let make ?templateName =
      fun ?fromEmailAddress ->
        fun ?templateSubject ->
          fun ?templateContent ->
            fun ?successRedirectionURL ->
              fun ?failureRedirectionURL ->
                fun () ->
                  {
                    getCustomVerificationEmailTemplateResult =
                      {
                        templateName;
                        fromEmailAddress;
                        templateSubject;
                        templateContent;
                        successRedirectionURL;
                        failureRedirectionURL
                      };
                    responseMetaData = ()
                  }
    let error_of_json name json =
      match name with
      | "CustomVerificationEmailTemplateDoesNotExistException" ->
          `CustomVerificationEmailTemplateDoesNotExistException
            (CustomVerificationEmailTemplateDoesNotExistException.of_json
               json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "CustomVerificationEmailTemplateDoesNotExistException" ->
          `CustomVerificationEmailTemplateDoesNotExistException
            (CustomVerificationEmailTemplateDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `CustomVerificationEmailTemplateDoesNotExistException e ->
          `Assoc
            [("error",
               (`String
                  "CustomVerificationEmailTemplateDoesNotExistException"));
            ("details",
              (CustomVerificationEmailTemplateDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getCustomVerificationEmailTemplateResult in
      structure_to_wrapped_value
        [("TemplateName",
           (Option.map x.templateName ~f:TemplateName.to_value));
        ("FromEmailAddress",
          (Option.map x.fromEmailAddress ~f:FromAddress.to_value));
        ("TemplateSubject",
          (Option.map x.templateSubject ~f:Subject.to_value));
        ("TemplateContent",
          (Option.map x.templateContent ~f:TemplateContent.to_value));
        ("SuccessRedirectionURL",
          (Option.map x.successRedirectionURL
             ~f:SuccessRedirectionURL.to_value));
        ("FailureRedirectionURL",
          (Option.map x.failureRedirectionURL
             ~f:FailureRedirectionURL.to_value))]
        ~wrapper:"GetCustomVerificationEmailTemplateResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "GetCustomVerificationEmailTemplateResult" in
      let failureRedirectionURL =
        (Option.map ~f:FailureRedirectionURL.of_xml)
          (Xml.child xml_arg0 "FailureRedirectionURL") in
      let successRedirectionURL =
        (Option.map ~f:SuccessRedirectionURL.of_xml)
          (Xml.child xml_arg0 "SuccessRedirectionURL") in
      let templateContent =
        (Option.map ~f:TemplateContent.of_xml)
          (Xml.child xml_arg0 "TemplateContent") in
      let templateSubject =
        (Option.map ~f:Subject.of_xml) (Xml.child xml_arg0 "TemplateSubject") in
      let fromEmailAddress =
        (Option.map ~f:FromAddress.of_xml)
          (Xml.child xml_arg0 "FromEmailAddress") in
      let templateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "TemplateName") in
      make ?failureRedirectionURL ?successRedirectionURL ?templateContent
        ?templateSubject ?fromEmailAddress ?templateName ()
    let of_json json =
      let failureRedirectionURL =
        field_map json "FailureRedirectionURL" FailureRedirectionURL.of_json in
      let successRedirectionURL =
        field_map json "SuccessRedirectionURL" SuccessRedirectionURL.of_json in
      let templateContent =
        field_map json "TemplateContent" TemplateContent.of_json in
      let templateSubject = field_map json "TemplateSubject" Subject.of_json in
      let fromEmailAddress =
        field_map json "FromEmailAddress" FromAddress.of_json in
      let templateName = field_map json "TemplateName" TemplateName.of_json in
      make ?failureRedirectionURL ?successRedirectionURL ?templateContent
        ?templateSubject ?fromEmailAddress ?templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The content of the custom verification email template."]
module GetCustomVerificationEmailTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc
          "The name of the custom verification email template that you want to retrieve."]}
    let context_ = "GetCustomVerificationEmailTemplateRequest"
    let make ~templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ~templateName ()
    let of_json json =
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to retrieve an existing custom verification email template."]
module GetAccountSendingEnabledResponse =
  struct
    type getAccountSendingEnabledResult =
      {
      enabled: Enabled.t option
        [@ocaml.doc
          "Describes whether email sending is enabled or disabled for your Amazon SES account in the current AWS Region."]}
    and responseMetaData = unit
    and t =
      {
      getAccountSendingEnabledResult: getAccountSendingEnabledResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetAccountSendingEnabledResponse"
    let make ?enabled =
      fun () ->
        { getAccountSendingEnabledResult = { enabled }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getAccountSendingEnabledResult in
      structure_to_wrapped_value
        [("Enabled", (Option.map x.enabled ~f:Enabled.to_value))]
        ~wrapper:"GetAccountSendingEnabledResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "GetAccountSendingEnabledResult" in
      let enabled =
        (Option.map ~f:Enabled.of_xml) (Xml.child xml_arg0 "Enabled") in
      make ?enabled ()
    let of_json json =
      let enabled = field_map json "Enabled" Enabled.of_json in
      make ?enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the email sending status for your Amazon SES account in the current AWS Region."]
module DescribeReceiptRuleSetResponse =
  struct
    type describeReceiptRuleSetResult =
      {
      metadata: ReceiptRuleSetMetadata.t option
        [@ocaml.doc
          "The metadata for the receipt rule set, which consists of the rule set name and the timestamp of when the rule set was created."];
      rules: ReceiptRulesList.t option
        [@ocaml.doc
          "A list of the receipt rules that belong to the specified receipt rule set."]}
    and responseMetaData = unit
    and t =
      {
      describeReceiptRuleSetResult: describeReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeReceiptRuleSetResponse"
    let make ?metadata =
      fun ?rules ->
        fun () ->
          {
            describeReceiptRuleSetResult = { metadata; rules };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeReceiptRuleSetResult in
      structure_to_wrapped_value
        [("Metadata",
           (Option.map x.metadata ~f:ReceiptRuleSetMetadata.to_value));
        ("Rules", (Option.map x.rules ~f:ReceiptRulesList.to_value))]
        ~wrapper:"DescribeReceiptRuleSetResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeReceiptRuleSetResult" in
      let rules =
        (Option.map ~f:ReceiptRulesList.of_xml) (Xml.child xml_arg0 "Rules") in
      let metadata =
        (Option.map ~f:ReceiptRuleSetMetadata.of_xml)
          (Xml.child xml_arg0 "Metadata") in
      make ?rules ?metadata ()
    let of_json json =
      let rules = field_map json "Rules" ReceiptRulesList.of_json in
      let metadata = field_map json "Metadata" ReceiptRuleSetMetadata.of_json in
      make ?rules ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the details of the specified receipt rule set."]
module DescribeReceiptRuleSetRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc "The name of the receipt rule set to describe."]}
    let context_ = "DescribeReceiptRuleSetRequest"
    let make ~ruleSetName = fun () -> { ruleSetName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~ruleSetName ()
    let of_json json =
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the details of a receipt rule set. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module DescribeReceiptRuleResponse =
  struct
    type describeReceiptRuleResult =
      {
      rule: ReceiptRule.t option
        [@ocaml.doc
          "A data structure that contains the specified receipt rule's name, actions, recipients, domains, enabled status, scan status, and Transport Layer Security (TLS) policy."]}
    and responseMetaData = unit
    and t =
      {
      describeReceiptRuleResult: describeReceiptRuleResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `RuleDoesNotExistException of RuleDoesNotExistException.t 
      | `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeReceiptRuleResponse"
    let make ?rule =
      fun () ->
        { describeReceiptRuleResult = { rule }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_json json)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_xml xml)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `RuleDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleDoesNotExistException"));
            ("details", (RuleDoesNotExistException.to_json e))]
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeReceiptRuleResult in
      structure_to_wrapped_value
        [("Rule", (Option.map x.rule ~f:ReceiptRule.to_value))]
        ~wrapper:"DescribeReceiptRuleResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeReceiptRuleResult" in
      let rule =
        (Option.map ~f:ReceiptRule.of_xml) (Xml.child xml_arg0 "Rule") in
      make ?rule ()
    let of_json json =
      let rule = field_map json "Rule" ReceiptRule.of_json in make ?rule ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the details of a receipt rule."]
module DescribeReceiptRuleRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the receipt rule set that the receipt rule belongs to."];
      ruleName: ReceiptRuleName.t
        [@ocaml.doc "The name of the receipt rule."]}
    let context_ = "DescribeReceiptRuleRequest"
    let make ~ruleSetName =
      fun ~ruleName -> fun () -> { ruleSetName; ruleName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("RuleName", (Some (ReceiptRuleName.to_value x.ruleName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleName =
        ReceiptRuleName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~ruleName ~ruleSetName ()
    let of_json json =
      let ruleName = field_map_exn json "RuleName" ReceiptRuleName.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~ruleName ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the details of a receipt rule. You use receipt rules to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module DescribeConfigurationSetResponse =
  struct
    type describeConfigurationSetResult =
      {
      configurationSet: ConfigurationSet.t option
        [@ocaml.doc
          "The configuration set object associated with the specified configuration set."];
      eventDestinations: EventDestinations.t option
        [@ocaml.doc
          "A list of event destinations associated with the configuration set."];
      trackingOptions: TrackingOptions.t option
        [@ocaml.doc
          "The name of the custom open and click tracking domain associated with the configuration set."];
      deliveryOptions: DeliveryOptions.t option ;
      reputationOptions: ReputationOptions.t option
        [@ocaml.doc
          "An object that represents the reputation settings for the configuration set."]}
    and responseMetaData = unit
    and t =
      {
      describeConfigurationSetResult: describeConfigurationSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeConfigurationSetResponse"
    let make ?configurationSet =
      fun ?eventDestinations ->
        fun ?trackingOptions ->
          fun ?deliveryOptions ->
            fun ?reputationOptions ->
              fun () ->
                {
                  describeConfigurationSetResult =
                    {
                      configurationSet;
                      eventDestinations;
                      trackingOptions;
                      deliveryOptions;
                      reputationOptions
                    };
                  responseMetaData = ()
                }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeConfigurationSetResult in
      structure_to_wrapped_value
        [("ConfigurationSet",
           (Option.map x.configurationSet ~f:ConfigurationSet.to_value));
        ("EventDestinations",
          (Option.map x.eventDestinations ~f:EventDestinations.to_value));
        ("TrackingOptions",
          (Option.map x.trackingOptions ~f:TrackingOptions.to_value));
        ("DeliveryOptions",
          (Option.map x.deliveryOptions ~f:DeliveryOptions.to_value));
        ("ReputationOptions",
          (Option.map x.reputationOptions ~f:ReputationOptions.to_value))]
        ~wrapper:"DescribeConfigurationSetResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeConfigurationSetResult" in
      let reputationOptions =
        (Option.map ~f:ReputationOptions.of_xml)
          (Xml.child xml_arg0 "ReputationOptions") in
      let deliveryOptions =
        (Option.map ~f:DeliveryOptions.of_xml)
          (Xml.child xml_arg0 "DeliveryOptions") in
      let trackingOptions =
        (Option.map ~f:TrackingOptions.of_xml)
          (Xml.child xml_arg0 "TrackingOptions") in
      let eventDestinations =
        (Option.map ~f:EventDestinations.of_xml)
          (Xml.child xml_arg0 "EventDestinations") in
      let configurationSet =
        (Option.map ~f:ConfigurationSet.of_xml)
          (Xml.child xml_arg0 "ConfigurationSet") in
      make ?reputationOptions ?deliveryOptions ?trackingOptions
        ?eventDestinations ?configurationSet ()
    let of_json json =
      let reputationOptions =
        field_map json "ReputationOptions" ReputationOptions.of_json in
      let deliveryOptions =
        field_map json "DeliveryOptions" DeliveryOptions.of_json in
      let trackingOptions =
        field_map json "TrackingOptions" TrackingOptions.of_json in
      let eventDestinations =
        field_map json "EventDestinations" EventDestinations.of_json in
      let configurationSet =
        field_map json "ConfigurationSet" ConfigurationSet.of_json in
      make ?reputationOptions ?deliveryOptions ?trackingOptions
        ?eventDestinations ?configurationSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the details of a configuration set. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module DescribeConfigurationSetRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc "The name of the configuration set to describe."];
      configurationSetAttributeNames: ConfigurationSetAttributeList.t option
        [@ocaml.doc "A list of configuration set attributes to return."]}
    let context_ = "DescribeConfigurationSetRequest"
    let make ?configurationSetAttributeNames =
      fun ~configurationSetName ->
        fun () -> { configurationSetAttributeNames; configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("ConfigurationSetAttributeNames",
          (Option.map x.configurationSetAttributeNames
             ~f:ConfigurationSetAttributeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetAttributeNames =
        (Option.map ~f:ConfigurationSetAttributeList.of_xml)
          (Xml.child xml_arg0 "ConfigurationSetAttributeNames") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ?configurationSetAttributeNames ~configurationSetName ()
    let of_json json =
      let configurationSetAttributeNames =
        field_map json "ConfigurationSetAttributeNames"
          ConfigurationSetAttributeList.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ?configurationSetAttributeNames ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the details of a configuration set. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module DescribeActiveReceiptRuleSetResponse =
  struct
    type describeActiveReceiptRuleSetResult =
      {
      metadata: ReceiptRuleSetMetadata.t option
        [@ocaml.doc
          "The metadata for the currently active receipt rule set. The metadata consists of the rule set name and a timestamp of when the rule set was created."];
      rules: ReceiptRulesList.t option
        [@ocaml.doc "The receipt rules that belong to the active rule set."]}
    and responseMetaData = unit
    and t =
      {
      describeActiveReceiptRuleSetResult: describeActiveReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeActiveReceiptRuleSetResponse"
    let make ?metadata =
      fun ?rules ->
        fun () ->
          {
            describeActiveReceiptRuleSetResult = { metadata; rules };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeActiveReceiptRuleSetResult in
      structure_to_wrapped_value
        [("Metadata",
           (Option.map x.metadata ~f:ReceiptRuleSetMetadata.to_value));
        ("Rules", (Option.map x.rules ~f:ReceiptRulesList.to_value))]
        ~wrapper:"DescribeActiveReceiptRuleSetResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeActiveReceiptRuleSetResult" in
      let rules =
        (Option.map ~f:ReceiptRulesList.of_xml) (Xml.child xml_arg0 "Rules") in
      let metadata =
        (Option.map ~f:ReceiptRuleSetMetadata.of_xml)
          (Xml.child xml_arg0 "Metadata") in
      make ?rules ?metadata ()
    let of_json json =
      let rules = field_map json "Rules" ReceiptRulesList.of_json in
      let metadata = field_map json "Metadata" ReceiptRuleSetMetadata.of_json in
      make ?rules ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the metadata and receipt rules for the receipt rule set that is currently active."]
module DescribeActiveReceiptRuleSetRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to return the metadata and receipt rules for the receipt rule set that is currently active. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module DeleteVerifiedEmailAddressRequest =
  struct
    type nonrec t =
      {
      emailAddress: Address.t
        [@ocaml.doc
          "An email address to be removed from the list of verified addresses."]}
    let context_ = "DeleteVerifiedEmailAddressRequest"
    let make ~emailAddress = fun () -> { emailAddress }
    let to_value x =
      structure_to_value
        [("EmailAddress", (Some (Address.to_value x.emailAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emailAddress =
        Address.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EmailAddress") in
      make ~emailAddress ()
    let of_json json =
      let emailAddress = field_map_exn json "EmailAddress" Address.of_json in
      make ~emailAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete an email address from the list of email addresses you have attempted to verify under your AWS account."]
module DeleteTemplateResponse =
  struct
    type deleteTemplateResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteTemplateResult: deleteTemplateResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () = { deleteTemplateResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an email template. You can execute this operation no more than once per second."]
module DeleteTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc "The name of the template to be deleted."]}
    let context_ = "DeleteTemplateRequest"
    let make ~templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ~templateName ()
    let of_json json =
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete an email template. For more information, see the Amazon SES Developer Guide."]
module DeleteReceiptRuleSetResponse =
  struct
    type deleteReceiptRuleSetResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteReceiptRuleSetResult: deleteReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `CannotDeleteException of CannotDeleteException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { deleteReceiptRuleSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "CannotDeleteException" ->
          `CannotDeleteException (CannotDeleteException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "CannotDeleteException" ->
          `CannotDeleteException (CannotDeleteException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `CannotDeleteException e ->
          `Assoc
            [("error", (`String "CannotDeleteException"));
            ("details", (CannotDeleteException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteReceiptRuleSetRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc "The name of the receipt rule set to delete."]}
    let context_ = "DeleteReceiptRuleSetRequest"
    let make ~ruleSetName = fun () -> { ruleSetName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~ruleSetName ()
    let of_json json =
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete a receipt rule set and all of the receipt rules it contains. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module DeleteReceiptRuleResponse =
  struct
    type deleteReceiptRuleResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteReceiptRuleResult: deleteReceiptRuleResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { deleteReceiptRuleResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteReceiptRuleRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the receipt rule set that contains the receipt rule to delete."];
      ruleName: ReceiptRuleName.t
        [@ocaml.doc "The name of the receipt rule to delete."]}
    let context_ = "DeleteReceiptRuleRequest"
    let make ~ruleSetName =
      fun ~ruleName -> fun () -> { ruleSetName; ruleName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("RuleName", (Some (ReceiptRuleName.to_value x.ruleName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleName =
        ReceiptRuleName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~ruleName ~ruleSetName ()
    let of_json json =
      let ruleName = field_map_exn json "RuleName" ReceiptRuleName.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~ruleName ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete a receipt rule. You use receipt rules to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module DeleteReceiptFilterResponse =
  struct
    type deleteReceiptFilterResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteReceiptFilterResult: deleteReceiptFilterResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () = { deleteReceiptFilterResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteReceiptFilterRequest =
  struct
    type nonrec t =
      {
      filterName: ReceiptFilterName.t
        [@ocaml.doc "The name of the IP address filter to delete."]}
    let context_ = "DeleteReceiptFilterRequest"
    let make ~filterName = fun () -> { filterName }
    let to_value x =
      structure_to_value
        [("FilterName", (Some (ReceiptFilterName.to_value x.filterName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filterName =
        ReceiptFilterName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FilterName") in
      make ~filterName ()
    let of_json json =
      let filterName =
        field_map_exn json "FilterName" ReceiptFilterName.of_json in
      make ~filterName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete an IP address filter. You use IP address filters when you receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module DeleteIdentityResponse =
  struct
    type deleteIdentityResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteIdentityResult: deleteIdentityResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () = { deleteIdentityResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteIdentityRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity to be removed from the list of identities for the AWS Account."]}
    let context_ = "DeleteIdentityRequest"
    let make ~identity = fun () -> { identity }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~identity ()
    let of_json json =
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete one of your Amazon SES identities (an email address or domain)."]
module DeleteIdentityPolicyResponse =
  struct
    type deleteIdentityPolicyResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteIdentityPolicyResult: deleteIdentityPolicyResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () = { deleteIdentityPolicyResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteIdentityPolicyRequest =
  struct
    type nonrec t =
      {
      identity: Identity.t
        [@ocaml.doc
          "The identity that is associated with the policy that you want to delete. You can specify the identity by using its name or by using its Amazon Resource Name (ARN). Examples: user\\@example.com, example.com, arn:aws:ses:us-east-1:123456789012:identity/example.com. To successfully call this API, you must own the identity."];
      policyName: PolicyName.t
        [@ocaml.doc "The name of the policy to be deleted."]}
    let context_ = "DeleteIdentityPolicyRequest"
    let make ~identity =
      fun ~policyName -> fun () -> { identity; policyName }
    let to_value x =
      structure_to_value
        [("Identity", (Some (Identity.to_value x.identity)));
        ("PolicyName", (Some (PolicyName.to_value x.policyName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyName =
        PolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let identity =
        Identity.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Identity") in
      make ~policyName ~identity ()
    let of_json json =
      let policyName = field_map_exn json "PolicyName" PolicyName.of_json in
      let identity = field_map_exn json "Identity" Identity.of_json in
      make ~policyName ~identity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete a sending authorization policy for an identity. Sending authorization is an Amazon SES feature that enables you to authorize other senders to use your identities. For information, see the Amazon SES Developer Guide."]
module DeleteCustomVerificationEmailTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc
          "The name of the custom verification email template that you want to delete."]}
    let context_ = "DeleteCustomVerificationEmailTemplateRequest"
    let make ~templateName = fun () -> { templateName }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ~templateName ()
    let of_json json =
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete an existing custom verification email template."]
module DeleteConfigurationSetTrackingOptionsResponse =
  struct
    type deleteConfigurationSetTrackingOptionsResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteConfigurationSetTrackingOptionsResult:
        deleteConfigurationSetTrackingOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `TrackingOptionsDoesNotExistException of
          TrackingOptionsDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        deleteConfigurationSetTrackingOptionsResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "TrackingOptionsDoesNotExistException" ->
          `TrackingOptionsDoesNotExistException
            (TrackingOptionsDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "TrackingOptionsDoesNotExistException" ->
          `TrackingOptionsDoesNotExistException
            (TrackingOptionsDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `TrackingOptionsDoesNotExistException e ->
          `Assoc
            [("error", (`String "TrackingOptionsDoesNotExistException"));
            ("details", (TrackingOptionsDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteConfigurationSetTrackingOptionsRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set from which you want to delete the tracking options."]}
    let context_ = "DeleteConfigurationSetTrackingOptionsRequest"
    let make ~configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete open and click tracking options in a configuration set."]
module DeleteConfigurationSetResponse =
  struct
    type deleteConfigurationSetResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteConfigurationSetResult: deleteConfigurationSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { deleteConfigurationSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteConfigurationSetRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc "The name of the configuration set to delete."]}
    let context_ = "DeleteConfigurationSetRequest"
    let make ~configurationSetName = fun () -> { configurationSetName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~configurationSetName ()
    let of_json json =
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete a configuration set. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module DeleteConfigurationSetEventDestinationResponse =
  struct
    type deleteConfigurationSetEventDestinationResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteConfigurationSetEventDestinationResult:
        deleteConfigurationSetEventDestinationResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `EventDestinationDoesNotExistException of
          EventDestinationDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        deleteConfigurationSetEventDestinationResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "EventDestinationDoesNotExistException" ->
          `EventDestinationDoesNotExistException
            (EventDestinationDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "EventDestinationDoesNotExistException" ->
          `EventDestinationDoesNotExistException
            (EventDestinationDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `EventDestinationDoesNotExistException e ->
          `Assoc
            [("error", (`String "EventDestinationDoesNotExistException"));
            ("details", (EventDestinationDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module DeleteConfigurationSetEventDestinationRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set from which to delete the event destination."];
      eventDestinationName: EventDestinationName.t
        [@ocaml.doc "The name of the event destination to delete."]}
    let context_ = "DeleteConfigurationSetEventDestinationRequest"
    let make ~configurationSetName =
      fun ~eventDestinationName ->
        fun () -> { configurationSetName; eventDestinationName }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("EventDestinationName",
          (Some (EventDestinationName.to_value x.eventDestinationName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestinationName =
        EventDestinationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EventDestinationName") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~eventDestinationName ~configurationSetName ()
    let of_json json =
      let eventDestinationName =
        field_map_exn json "EventDestinationName"
          EventDestinationName.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~eventDestinationName ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to delete a configuration set event destination. Configuration set event destinations are associated with configuration sets, which enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module CustomVerificationEmailTemplateAlreadyExistsException =
  struct
    type nonrec t =
      {
      customVerificationEmailTemplateName: TemplateName.t option
        [@ocaml.doc
          "Indicates that the provided custom verification email template with the specified template name already exists."]}
    let make ?customVerificationEmailTemplateName =
      fun () -> { customVerificationEmailTemplateName }
    let to_value x =
      structure_to_value
        [("CustomVerificationEmailTemplateName",
           (Option.map x.customVerificationEmailTemplateName
              ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customVerificationEmailTemplateName =
        (Option.map ~f:TemplateName.of_xml)
          (Xml.child xml_arg0 "CustomVerificationEmailTemplateName") in
      make ?customVerificationEmailTemplateName ()
    let of_json json =
      let customVerificationEmailTemplateName =
        field_map json "CustomVerificationEmailTemplateName"
          TemplateName.of_json in
      make ?customVerificationEmailTemplateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that a custom verification email template with the name you specified already exists."]
module CustomVerificationEmailInvalidContentException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that custom verification email template provided content is invalid."]
module CreateTemplateResponse =
  struct
    type createTemplateResult = unit
    and responseMetaData = unit
    and t =
      {
      createTemplateResult: createTemplateResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidTemplateException of InvalidTemplateException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { createTemplateResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidTemplateException" ->
          `InvalidTemplateException (InvalidTemplateException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidTemplateException" ->
          `InvalidTemplateException (InvalidTemplateException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidTemplateException e ->
          `Assoc
            [("error", (`String "InvalidTemplateException"));
            ("details", (InvalidTemplateException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an email template. Email templates enable you to send personalized email to one or more destinations in a single API operation. For more information, see the Amazon SES Developer Guide. You can execute this operation no more than once per second."]
module CreateTemplateRequest =
  struct
    type nonrec t =
      {
      template: Template.t
        [@ocaml.doc
          "The content of the email, composed of a subject line, an HTML part, and a text-only part."]}
    let context_ = "CreateTemplateRequest"
    let make ~template = fun () -> { template }
    let to_value x =
      structure_to_value
        [("Template", (Some (Template.to_value x.template)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let template =
        Template.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Template") in
      make ~template ()
    let of_json json =
      let template = field_map_exn json "Template" Template.of_json in
      make ~template ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create an email template. For more information, see the Amazon SES Developer Guide."]
module CreateReceiptRuleSetResponse =
  struct
    type createReceiptRuleSetResult = unit
    and responseMetaData = unit
    and t =
      {
      createReceiptRuleSetResult: createReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { createReceiptRuleSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CreateReceiptRuleSetRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the rule set to create. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Start and end with a letter or number. Contain less than 64 characters."]}
    let context_ = "CreateReceiptRuleSetRequest"
    let make ~ruleSetName = fun () -> { ruleSetName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~ruleSetName ()
    let of_json json =
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create an empty receipt rule set. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module CreateReceiptRuleResponse =
  struct
    type createReceiptRuleResult = unit
    and responseMetaData = unit
    and t =
      {
      createReceiptRuleResult: createReceiptRuleResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidLambdaFunctionException of InvalidLambdaFunctionException.t 
      | `InvalidS3ConfigurationException of InvalidS3ConfigurationException.t 
      | `InvalidSnsTopicException of InvalidSnsTopicException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `RuleDoesNotExistException of RuleDoesNotExistException.t 
      | `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { createReceiptRuleResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidLambdaFunctionException" ->
          `InvalidLambdaFunctionException
            (InvalidLambdaFunctionException.of_json json)
      | "InvalidS3ConfigurationException" ->
          `InvalidS3ConfigurationException
            (InvalidS3ConfigurationException.of_json json)
      | "InvalidSnsTopicException" ->
          `InvalidSnsTopicException (InvalidSnsTopicException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_json json)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidLambdaFunctionException" ->
          `InvalidLambdaFunctionException
            (InvalidLambdaFunctionException.of_xml xml)
      | "InvalidS3ConfigurationException" ->
          `InvalidS3ConfigurationException
            (InvalidS3ConfigurationException.of_xml xml)
      | "InvalidSnsTopicException" ->
          `InvalidSnsTopicException (InvalidSnsTopicException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "RuleDoesNotExistException" ->
          `RuleDoesNotExistException (RuleDoesNotExistException.of_xml xml)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidLambdaFunctionException e ->
          `Assoc
            [("error", (`String "InvalidLambdaFunctionException"));
            ("details", (InvalidLambdaFunctionException.to_json e))]
      | `InvalidS3ConfigurationException e ->
          `Assoc
            [("error", (`String "InvalidS3ConfigurationException"));
            ("details", (InvalidS3ConfigurationException.to_json e))]
      | `InvalidSnsTopicException e ->
          `Assoc
            [("error", (`String "InvalidSnsTopicException"));
            ("details", (InvalidSnsTopicException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `RuleDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleDoesNotExistException"));
            ("details", (RuleDoesNotExistException.to_json e))]
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CreateReceiptRuleRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the rule set that the receipt rule will be added to."];
      after: ReceiptRuleName.t option
        [@ocaml.doc
          "The name of an existing rule after which the new rule will be placed. If this parameter is null, the new rule will be inserted at the beginning of the rule list."];
      rule: ReceiptRule.t
        [@ocaml.doc
          "A data structure that contains the specified rule's name, actions, recipients, domains, enabled status, scan status, and TLS policy."]}
    let context_ = "CreateReceiptRuleRequest"
    let make ?after =
      fun ~ruleSetName -> fun ~rule -> fun () -> { after; ruleSetName; rule }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("After", (Option.map x.after ~f:ReceiptRuleName.to_value));
        ("Rule", (Some (ReceiptRule.to_value x.rule)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rule =
        ReceiptRule.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let after =
        (Option.map ~f:ReceiptRuleName.of_xml) (Xml.child xml_arg0 "After") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~rule ?after ~ruleSetName ()
    let of_json json =
      let rule = field_map_exn json "Rule" ReceiptRule.of_json in
      let after = field_map json "After" ReceiptRuleName.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~rule ?after ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create a receipt rule. You use receipt rules to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module CreateReceiptFilterResponse =
  struct
    type createReceiptFilterResult = unit
    and responseMetaData = unit
    and t =
      {
      createReceiptFilterResult: createReceiptFilterResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { createReceiptFilterResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CreateReceiptFilterRequest =
  struct
    type nonrec t =
      {
      filter: ReceiptFilter.t
        [@ocaml.doc
          "A data structure that describes the IP address filter to create, which consists of a name, an IP address range, and whether to allow or block mail from it."]}
    let context_ = "CreateReceiptFilterRequest"
    let make ~filter = fun () -> { filter }
    let to_value x =
      structure_to_value
        [("Filter", (Some (ReceiptFilter.to_value x.filter)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        ReceiptFilter.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Filter") in
      make ~filter ()
    let of_json json =
      let filter = field_map_exn json "Filter" ReceiptFilter.of_json in
      make ~filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create a new IP address filter. You use IP address filters when you receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
module CreateCustomVerificationEmailTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: TemplateName.t
        [@ocaml.doc "The name of the custom verification email template."];
      fromEmailAddress: FromAddress.t
        [@ocaml.doc
          "The email address that the custom verification email is sent from."];
      templateSubject: Subject.t
        [@ocaml.doc "The subject line of the custom verification email."];
      templateContent: TemplateContent.t
        [@ocaml.doc
          "The content of the custom verification email. The total size of the email must be less than 10 MB. The message body may contain HTML, with some limitations. For more information, see Custom Verification Email Frequently Asked Questions in the Amazon SES Developer Guide."];
      successRedirectionURL: SuccessRedirectionURL.t
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is successfully verified."];
      failureRedirectionURL: FailureRedirectionURL.t
        [@ocaml.doc
          "The URL that the recipient of the verification email is sent to if his or her address is not successfully verified."]}
    let context_ = "CreateCustomVerificationEmailTemplateRequest"
    let make ~templateName =
      fun ~fromEmailAddress ->
        fun ~templateSubject ->
          fun ~templateContent ->
            fun ~successRedirectionURL ->
              fun ~failureRedirectionURL ->
                fun () ->
                  {
                    templateName;
                    fromEmailAddress;
                    templateSubject;
                    templateContent;
                    successRedirectionURL;
                    failureRedirectionURL
                  }
    let to_value x =
      structure_to_value
        [("TemplateName", (Some (TemplateName.to_value x.templateName)));
        ("FromEmailAddress",
          (Some (FromAddress.to_value x.fromEmailAddress)));
        ("TemplateSubject", (Some (Subject.to_value x.templateSubject)));
        ("TemplateContent",
          (Some (TemplateContent.to_value x.templateContent)));
        ("SuccessRedirectionURL",
          (Some (SuccessRedirectionURL.to_value x.successRedirectionURL)));
        ("FailureRedirectionURL",
          (Some (FailureRedirectionURL.to_value x.failureRedirectionURL)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureRedirectionURL =
        FailureRedirectionURL.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FailureRedirectionURL") in
      let successRedirectionURL =
        SuccessRedirectionURL.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SuccessRedirectionURL") in
      let templateContent =
        TemplateContent.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateContent") in
      let templateSubject =
        Subject.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateSubject") in
      let fromEmailAddress =
        FromAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FromEmailAddress") in
      let templateName =
        TemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateName") in
      make ~failureRedirectionURL ~successRedirectionURL ~templateContent
        ~templateSubject ~fromEmailAddress ~templateName ()
    let of_json json =
      let failureRedirectionURL =
        field_map_exn json "FailureRedirectionURL"
          FailureRedirectionURL.of_json in
      let successRedirectionURL =
        field_map_exn json "SuccessRedirectionURL"
          SuccessRedirectionURL.of_json in
      let templateContent =
        field_map_exn json "TemplateContent" TemplateContent.of_json in
      let templateSubject =
        field_map_exn json "TemplateSubject" Subject.of_json in
      let fromEmailAddress =
        field_map_exn json "FromEmailAddress" FromAddress.of_json in
      let templateName =
        field_map_exn json "TemplateName" TemplateName.of_json in
      make ~failureRedirectionURL ~successRedirectionURL ~templateContent
        ~templateSubject ~fromEmailAddress ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create a custom verification email template."]
module CreateConfigurationSetTrackingOptionsResponse =
  struct
    type createConfigurationSetTrackingOptionsResult = unit
    and responseMetaData = unit
    and t =
      {
      createConfigurationSetTrackingOptionsResult:
        createConfigurationSetTrackingOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `InvalidTrackingOptionsException of InvalidTrackingOptionsException.t 
      | `TrackingOptionsAlreadyExistsException of
          TrackingOptionsAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        createConfigurationSetTrackingOptionsResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "InvalidTrackingOptionsException" ->
          `InvalidTrackingOptionsException
            (InvalidTrackingOptionsException.of_json json)
      | "TrackingOptionsAlreadyExistsException" ->
          `TrackingOptionsAlreadyExistsException
            (TrackingOptionsAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "InvalidTrackingOptionsException" ->
          `InvalidTrackingOptionsException
            (InvalidTrackingOptionsException.of_xml xml)
      | "TrackingOptionsAlreadyExistsException" ->
          `TrackingOptionsAlreadyExistsException
            (TrackingOptionsAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `InvalidTrackingOptionsException e ->
          `Assoc
            [("error", (`String "InvalidTrackingOptionsException"));
            ("details", (InvalidTrackingOptionsException.to_json e))]
      | `TrackingOptionsAlreadyExistsException e ->
          `Assoc
            [("error", (`String "TrackingOptionsAlreadyExistsException"));
            ("details", (TrackingOptionsAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CreateConfigurationSetTrackingOptionsRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set that the tracking options should be associated with."];
      trackingOptions: TrackingOptions.t }
    let context_ = "CreateConfigurationSetTrackingOptionsRequest"
    let make ~configurationSetName =
      fun ~trackingOptions ->
        fun () -> { configurationSetName; trackingOptions }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("TrackingOptions",
          (Some (TrackingOptions.to_value x.trackingOptions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trackingOptions =
        TrackingOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrackingOptions") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~trackingOptions ~configurationSetName ()
    let of_json json =
      let trackingOptions =
        field_map_exn json "TrackingOptions" TrackingOptions.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~trackingOptions ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create an open and click tracking option object in a configuration set."]
module CreateConfigurationSetResponse =
  struct
    type createConfigurationSetResult = unit
    and responseMetaData = unit
    and t =
      {
      createConfigurationSetResult: createConfigurationSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetAlreadyExistsException of
          ConfigurationSetAlreadyExistsException.t 
      | `InvalidConfigurationSetException of
          InvalidConfigurationSetException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { createConfigurationSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ConfigurationSetAlreadyExistsException" ->
          `ConfigurationSetAlreadyExistsException
            (ConfigurationSetAlreadyExistsException.of_json json)
      | "InvalidConfigurationSetException" ->
          `InvalidConfigurationSetException
            (InvalidConfigurationSetException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetAlreadyExistsException" ->
          `ConfigurationSetAlreadyExistsException
            (ConfigurationSetAlreadyExistsException.of_xml xml)
      | "InvalidConfigurationSetException" ->
          `InvalidConfigurationSetException
            (InvalidConfigurationSetException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ConfigurationSetAlreadyExistsException"));
            ("details", (ConfigurationSetAlreadyExistsException.to_json e))]
      | `InvalidConfigurationSetException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationSetException"));
            ("details", (InvalidConfigurationSetException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CreateConfigurationSetRequest =
  struct
    type nonrec t =
      {
      configurationSet: ConfigurationSet.t
        [@ocaml.doc
          "A data structure that contains the name of the configuration set."]}
    let context_ = "CreateConfigurationSetRequest"
    let make ~configurationSet = fun () -> { configurationSet }
    let to_value x =
      structure_to_value
        [("ConfigurationSet",
           (Some (ConfigurationSet.to_value x.configurationSet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSet =
        ConfigurationSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSet") in
      make ~configurationSet ()
    let of_json json =
      let configurationSet =
        field_map_exn json "ConfigurationSet" ConfigurationSet.of_json in
      make ~configurationSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create a configuration set. Configuration sets enable you to publish email sending events. For information about using configuration sets, see the Amazon SES Developer Guide."]
module CreateConfigurationSetEventDestinationResponse =
  struct
    type createConfigurationSetEventDestinationResult = unit
    and responseMetaData = unit
    and t =
      {
      createConfigurationSetEventDestinationResult:
        createConfigurationSetEventDestinationResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ConfigurationSetDoesNotExistException of
          ConfigurationSetDoesNotExistException.t 
      | `EventDestinationAlreadyExistsException of
          EventDestinationAlreadyExistsException.t 
      | `InvalidCloudWatchDestinationException of
          InvalidCloudWatchDestinationException.t 
      | `InvalidFirehoseDestinationException of
          InvalidFirehoseDestinationException.t 
      | `InvalidSNSDestinationException of InvalidSNSDestinationException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        createConfigurationSetEventDestinationResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_json json)
      | "EventDestinationAlreadyExistsException" ->
          `EventDestinationAlreadyExistsException
            (EventDestinationAlreadyExistsException.of_json json)
      | "InvalidCloudWatchDestinationException" ->
          `InvalidCloudWatchDestinationException
            (InvalidCloudWatchDestinationException.of_json json)
      | "InvalidFirehoseDestinationException" ->
          `InvalidFirehoseDestinationException
            (InvalidFirehoseDestinationException.of_json json)
      | "InvalidSNSDestinationException" ->
          `InvalidSNSDestinationException
            (InvalidSNSDestinationException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConfigurationSetDoesNotExistException" ->
          `ConfigurationSetDoesNotExistException
            (ConfigurationSetDoesNotExistException.of_xml xml)
      | "EventDestinationAlreadyExistsException" ->
          `EventDestinationAlreadyExistsException
            (EventDestinationAlreadyExistsException.of_xml xml)
      | "InvalidCloudWatchDestinationException" ->
          `InvalidCloudWatchDestinationException
            (InvalidCloudWatchDestinationException.of_xml xml)
      | "InvalidFirehoseDestinationException" ->
          `InvalidFirehoseDestinationException
            (InvalidFirehoseDestinationException.of_xml xml)
      | "InvalidSNSDestinationException" ->
          `InvalidSNSDestinationException
            (InvalidSNSDestinationException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConfigurationSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "ConfigurationSetDoesNotExistException"));
            ("details", (ConfigurationSetDoesNotExistException.to_json e))]
      | `EventDestinationAlreadyExistsException e ->
          `Assoc
            [("error", (`String "EventDestinationAlreadyExistsException"));
            ("details", (EventDestinationAlreadyExistsException.to_json e))]
      | `InvalidCloudWatchDestinationException e ->
          `Assoc
            [("error", (`String "InvalidCloudWatchDestinationException"));
            ("details", (InvalidCloudWatchDestinationException.to_json e))]
      | `InvalidFirehoseDestinationException e ->
          `Assoc
            [("error", (`String "InvalidFirehoseDestinationException"));
            ("details", (InvalidFirehoseDestinationException.to_json e))]
      | `InvalidSNSDestinationException e ->
          `Assoc
            [("error", (`String "InvalidSNSDestinationException"));
            ("details", (InvalidSNSDestinationException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CreateConfigurationSetEventDestinationRequest =
  struct
    type nonrec t =
      {
      configurationSetName: ConfigurationSetName.t
        [@ocaml.doc
          "The name of the configuration set that the event destination should be associated with."];
      eventDestination: EventDestination.t
        [@ocaml.doc
          "An object that describes the AWS service that email sending event information will be published to."]}
    let context_ = "CreateConfigurationSetEventDestinationRequest"
    let make ~configurationSetName =
      fun ~eventDestination ->
        fun () -> { configurationSetName; eventDestination }
    let to_value x =
      structure_to_value
        [("ConfigurationSetName",
           (Some (ConfigurationSetName.to_value x.configurationSetName)));
        ("EventDestination",
          (Some (EventDestination.to_value x.eventDestination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventDestination =
        EventDestination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EventDestination") in
      let configurationSetName =
        ConfigurationSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationSetName") in
      make ~eventDestination ~configurationSetName ()
    let of_json json =
      let eventDestination =
        field_map_exn json "EventDestination" EventDestination.of_json in
      let configurationSetName =
        field_map_exn json "ConfigurationSetName"
          ConfigurationSetName.of_json in
      make ~eventDestination ~configurationSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create a configuration set event destination. A configuration set event destination, which can be either Amazon CloudWatch or Amazon Kinesis Firehose, describes an AWS service in which Amazon SES publishes the email sending events associated with a configuration set. For information about using configuration sets, see the Amazon SES Developer Guide."]
module CloneReceiptRuleSetResponse =
  struct
    type cloneReceiptRuleSetResult = unit
    and responseMetaData = unit
    and t =
      {
      cloneReceiptRuleSetResult: cloneReceiptRuleSetResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `RuleSetDoesNotExistException of RuleSetDoesNotExistException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { cloneReceiptRuleSetResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "RuleSetDoesNotExistException" ->
          `RuleSetDoesNotExistException
            (RuleSetDoesNotExistException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `RuleSetDoesNotExistException e ->
          `Assoc
            [("error", (`String "RuleSetDoesNotExistException"));
            ("details", (RuleSetDoesNotExistException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element returned on a successful request."]
module CloneReceiptRuleSetRequest =
  struct
    type nonrec t =
      {
      ruleSetName: ReceiptRuleSetName.t
        [@ocaml.doc
          "The name of the rule set to create. The name must: This value can only contain ASCII letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). Start and end with a letter or number. Contain less than 64 characters."];
      originalRuleSetName: ReceiptRuleSetName.t
        [@ocaml.doc "The name of the rule set to clone."]}
    let context_ = "CloneReceiptRuleSetRequest"
    let make ~ruleSetName =
      fun ~originalRuleSetName ->
        fun () -> { ruleSetName; originalRuleSetName }
    let to_value x =
      structure_to_value
        [("RuleSetName", (Some (ReceiptRuleSetName.to_value x.ruleSetName)));
        ("OriginalRuleSetName",
          (Some (ReceiptRuleSetName.to_value x.originalRuleSetName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originalRuleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginalRuleSetName") in
      let ruleSetName =
        ReceiptRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      make ~originalRuleSetName ~ruleSetName ()
    let of_json json =
      let originalRuleSetName =
        field_map_exn json "OriginalRuleSetName" ReceiptRuleSetName.of_json in
      let ruleSetName =
        field_map_exn json "RuleSetName" ReceiptRuleSetName.of_json in
      make ~originalRuleSetName ~ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to create a receipt rule set by cloning an existing one. You use receipt rule sets to receive email with Amazon SES. For more information, see the Amazon SES Developer Guide."]
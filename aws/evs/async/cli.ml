(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_eip_to_vlan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and vlanName =
         flag "vlan-name" (required string)
           ~doc:"STRING AssociateEipToVlanRequestVlanNameString"
       and allocationId =
         flag "allocation-id" (required string) ~doc:"STRING AllocationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_eip_to_vlan
           (Values.AssociateEipToVlanRequest.make ?clientToken ~environmentId
              ~vlanName ~allocationId ())
           (Some Values.AssociateEipToVlanResponse.to_json)
           (Some Values.AssociateEipToVlanResponse.error_to_json)])
let create_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId"
       and entitlementType =
         flag "entitlement-type" (required json_arg)
           ~doc:"JSON EntitlementType"
       and vmIds = flag "vm-ids" (required json_arg) ~doc:"JSON VmIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_entitlement
           (Values.CreateEntitlementRequest.make ?clientToken ~environmentId
              ~connectorId
              ~entitlementType:(Values.EntitlementType.of_json
                                  entitlementType)
              ~vmIds:(Values.VmIdList.of_json vmIds) ())
           (Some Values.CreateEntitlementResponse.to_json)
           (Some Values.CreateEntitlementResponse.error_to_json)])
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and serviceAccessSecurityGroups =
         flag "service-access-security-groups" (optional json_arg)
           ~doc:"JSON ServiceAccessSecurityGroups"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and serviceAccessSubnetId =
         flag "service-access-subnet-id" (required string)
           ~doc:"STRING SubnetId"
       and vcfVersion =
         flag "vcf-version" (required json_arg) ~doc:"JSON VcfVersion"
       and termsAccepted =
         flag "terms-accepted" (required bool) ~doc:"BOOL Boolean"
       and licenseInfo =
         flag "license-info" (required json_arg) ~doc:"JSON LicenseInfoList"
       and initialVlans =
         flag "initial-vlans" (required json_arg) ~doc:"JSON InitialVlans"
       and hosts =
         flag "hosts" (required json_arg) ~doc:"JSON HostInfoForCreateList"
       and connectivityInfo =
         flag "connectivity-info" (required json_arg)
           ~doc:"JSON ConnectivityInfo"
       and vcfHostnames =
         flag "vcf-hostnames" (required json_arg) ~doc:"JSON VcfHostnames"
       and siteId = flag "site-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentRequest.make ?clientToken
              ?environmentName ?kmsKeyId
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags)
              ?serviceAccessSecurityGroups:(Option.map
                                              ~f:Values.ServiceAccessSecurityGroups.of_json
                                              serviceAccessSecurityGroups)
              ~vpcId ~serviceAccessSubnetId
              ~vcfVersion:(Values.VcfVersion.of_json vcfVersion)
              ~termsAccepted
              ~licenseInfo:(Values.LicenseInfoList.of_json licenseInfo)
              ~initialVlans:(Values.InitialVlans.of_json initialVlans)
              ~hosts:(Values.HostInfoForCreateList.of_json hosts)
              ~connectivityInfo:(Values.ConnectivityInfo.of_json
                                   connectivityInfo)
              ~vcfHostnames:(Values.VcfHostnames.of_json vcfHostnames)
              ~siteId ()) (Some Values.CreateEnvironmentResponse.to_json)
           (Some Values.CreateEnvironmentResponse.error_to_json)])
let create_environment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON ConnectorType"
       and applianceFqdn =
         flag "appliance-fqdn" (required string) ~doc:"STRING ApplianceFqdn"
       and secretIdentifier =
         flag "secret-identifier" (required string)
           ~doc:"STRING SecretIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_connector
           (Values.CreateEnvironmentConnectorRequest.make ?clientToken
              ~environmentId ~type_:(Values.ConnectorType.of_json type_)
              ~applianceFqdn ~secretIdentifier ())
           (Some Values.CreateEnvironmentConnectorResponse.to_json)
           (Some Values.CreateEnvironmentConnectorResponse.error_to_json)])
let create_environment_host =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and esxVersion =
         flag "esx-version" (optional string) ~doc:"STRING EsxVersion"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and host =
         flag "host" (required json_arg) ~doc:"JSON HostInfoForCreate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_host
           (Values.CreateEnvironmentHostRequest.make ?clientToken ?esxVersion
              ~environmentId ~host:(Values.HostInfoForCreate.of_json host) ())
           (Some Values.CreateEnvironmentHostResponse.to_json)
           (Some Values.CreateEnvironmentHostResponse.error_to_json)])
let delete_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId"
       and entitlementType =
         flag "entitlement-type" (required json_arg)
           ~doc:"JSON EntitlementType"
       and vmIds = flag "vm-ids" (required json_arg) ~doc:"JSON VmIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_entitlement
           (Values.DeleteEntitlementRequest.make ?clientToken ~environmentId
              ~connectorId
              ~entitlementType:(Values.EntitlementType.of_json
                                  entitlementType)
              ~vmIds:(Values.VmIdList.of_json vmIds) ())
           (Some Values.DeleteEntitlementResponse.to_json)
           (Some Values.DeleteEntitlementResponse.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentRequest.make ?clientToken ~environmentId
              ()) (Some Values.DeleteEnvironmentResponse.to_json)
           (Some Values.DeleteEnvironmentResponse.error_to_json)])
let delete_environment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_connector
           (Values.DeleteEnvironmentConnectorRequest.make ?clientToken
              ~environmentId ~connectorId ())
           (Some Values.DeleteEnvironmentConnectorResponse.to_json)
           (Some Values.DeleteEnvironmentConnectorResponse.error_to_json)])
let delete_environment_host =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and hostName =
         flag "host-name" (required string) ~doc:"STRING HostName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_host
           (Values.DeleteEnvironmentHostRequest.make ?clientToken
              ~environmentId ~hostName ())
           (Some Values.DeleteEnvironmentHostResponse.to_json)
           (Some Values.DeleteEnvironmentHostResponse.error_to_json)])
let disassociate_eip_from_vlan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and vlanName =
         flag "vlan-name" (required string)
           ~doc:"STRING DisassociateEipFromVlanRequestVlanNameString"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_eip_from_vlan
           (Values.DisassociateEipFromVlanRequest.make ?clientToken
              ~environmentId ~vlanName ~associationId ())
           (Some Values.DisassociateEipFromVlanResponse.to_json)
           (Some Values.DisassociateEipFromVlanResponse.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment
           (Values.GetEnvironmentRequest.make ~environmentId ())
           (Some Values.GetEnvironmentResponse.to_json)
           (Some Values.GetEnvironmentResponse.error_to_json)])
let get_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_versions (Values.GetVersionsRequest.make ())
           (Some Values.GetVersionsResponse.to_json)
           (Some Values.GetVersionsResponse.error_to_json)])
let list_environment_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_connectors
           (Values.ListEnvironmentConnectorsRequest.make ?nextToken
              ?maxResults ~environmentId ())
           (Some Values.ListEnvironmentConnectorsResponse.to_json)
           (Some Values.ListEnvironmentConnectorsResponse.error_to_json)])
let list_environment_hosts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_hosts
           (Values.ListEnvironmentHostsRequest.make ?nextToken ?maxResults
              ~environmentId ())
           (Some Values.ListEnvironmentHostsResponse.to_json)
           (Some Values.ListEnvironmentHostsResponse.error_to_json)])
let list_environment_vlans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_vlans
           (Values.ListEnvironmentVlansRequest.make ?nextToken ?maxResults
              ~environmentId ())
           (Some Values.ListEnvironmentVlansResponse.to_json)
           (Some Values.ListEnvironmentVlansResponse.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON EnvironmentStateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make ?nextToken ?maxResults
              ?state:(Option.map ~f:Values.EnvironmentStateList.of_json state)
              ()) (Some Values.ListEnvironmentsResponse.to_json)
           (Some Values.ListEnvironmentsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_vm_entitlements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId"
       and entitlementType =
         flag "entitlement-type" (required json_arg)
           ~doc:"JSON EntitlementType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vm_entitlements
           (Values.ListVmEntitlementsRequest.make ?nextToken ?maxResults
              ~environmentId ~connectorId
              ~entitlementType:(Values.EntitlementType.of_json
                                  entitlementType) ())
           (Some Values.ListVmEntitlementsResponse.to_json)
           (Some Values.ListVmEntitlementsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON RequestTagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.RequestTagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_environment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applianceFqdn =
         flag "appliance-fqdn" (optional string) ~doc:"STRING ApplianceFqdn"
       and secretIdentifier =
         flag "secret-identifier" (optional string)
           ~doc:"STRING SecretIdentifier"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_connector
           (Values.UpdateEnvironmentConnectorRequest.make ?clientToken
              ?applianceFqdn ?secretIdentifier ~environmentId ~connectorId ())
           (Some Values.UpdateEnvironmentConnectorResponse.to_json)
           (Some Values.UpdateEnvironmentConnectorResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-eip-to-vlan", associate_eip_to_vlan);
    ("create-entitlement", create_entitlement);
    ("create-environment", create_environment);
    ("create-environment-connector", create_environment_connector);
    ("create-environment-host", create_environment_host);
    ("delete-entitlement", delete_entitlement);
    ("delete-environment", delete_environment);
    ("delete-environment-connector", delete_environment_connector);
    ("delete-environment-host", delete_environment_host);
    ("disassociate-eip-from-vlan", disassociate_eip_from_vlan);
    ("get-environment", get_environment);
    ("get-versions", get_versions);
    ("list-environment-connectors", list_environment_connectors);
    ("list-environment-hosts", list_environment_hosts);
    ("list-environment-vlans", list_environment_vlans);
    ("list-environments", list_environments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-vm-entitlements", list_vm_entitlements);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-environment-connector", update_environment_connector)]
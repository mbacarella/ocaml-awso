(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_acknowledge_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acknowledgeActionRequests =
         flag "acknowledge-action-requests" (required json_arg)
           ~doc:"JSON AcknowledgeAlarmActionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_acknowledge_alarm
           (Values.BatchAcknowledgeAlarmRequest.make
              ~acknowledgeActionRequests:(Values.AcknowledgeAlarmActionRequests.of_json
                                            acknowledgeActionRequests) ())
           (Some Values.BatchAcknowledgeAlarmResponse.to_json)
           (Some Values.BatchAcknowledgeAlarmResponse.error_to_json)])
let batch_disable_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disableActionRequests =
         flag "disable-action-requests" (required json_arg)
           ~doc:"JSON DisableAlarmActionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disable_alarm
           (Values.BatchDisableAlarmRequest.make
              ~disableActionRequests:(Values.DisableAlarmActionRequests.of_json
                                        disableActionRequests) ())
           (Some Values.BatchDisableAlarmResponse.to_json)
           (Some Values.BatchDisableAlarmResponse.error_to_json)])
let batch_enable_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableActionRequests =
         flag "enable-action-requests" (required json_arg)
           ~doc:"JSON EnableAlarmActionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_enable_alarm
           (Values.BatchEnableAlarmRequest.make
              ~enableActionRequests:(Values.EnableAlarmActionRequests.of_json
                                       enableActionRequests) ())
           (Some Values.BatchEnableAlarmResponse.to_json)
           (Some Values.BatchEnableAlarmResponse.error_to_json)])
let batch_put_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messages =
         flag "messages" (required json_arg) ~doc:"JSON Messages" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_message
           (Values.BatchPutMessageRequest.make
              ~messages:(Values.Messages.of_json messages) ())
           (Some Values.BatchPutMessageResponse.to_json)
           (Some Values.BatchPutMessageResponse.error_to_json)])
let batch_reset_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resetActionRequests =
         flag "reset-action-requests" (required json_arg)
           ~doc:"JSON ResetAlarmActionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_reset_alarm
           (Values.BatchResetAlarmRequest.make
              ~resetActionRequests:(Values.ResetAlarmActionRequests.of_json
                                      resetActionRequests) ())
           (Some Values.BatchResetAlarmResponse.to_json)
           (Some Values.BatchResetAlarmResponse.error_to_json)])
let batch_snooze_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snoozeActionRequests =
         flag "snooze-action-requests" (required json_arg)
           ~doc:"JSON SnoozeAlarmActionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_snooze_alarm
           (Values.BatchSnoozeAlarmRequest.make
              ~snoozeActionRequests:(Values.SnoozeAlarmActionRequests.of_json
                                       snoozeActionRequests) ())
           (Some Values.BatchSnoozeAlarmResponse.to_json)
           (Some Values.BatchSnoozeAlarmResponse.error_to_json)])
let batch_update_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectors =
         flag "detectors" (required json_arg)
           ~doc:"JSON UpdateDetectorRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_detector
           (Values.BatchUpdateDetectorRequest.make
              ~detectors:(Values.UpdateDetectorRequests.of_json detectors) ())
           (Some Values.BatchUpdateDetectorResponse.to_json)
           (Some Values.BatchUpdateDetectorResponse.error_to_json)])
let describe_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyValue =
         flag "key-value" (optional string) ~doc:"STRING KeyValue"
       and alarmModelName =
         flag "alarm-model-name" (required string)
           ~doc:"STRING AlarmModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alarm
           (Values.DescribeAlarmRequest.make ?keyValue ~alarmModelName ())
           (Some Values.DescribeAlarmResponse.to_json)
           (Some Values.DescribeAlarmResponse.error_to_json)])
let describe_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyValue =
         flag "key-value" (optional string) ~doc:"STRING KeyValue"
       and detectorModelName =
         flag "detector-model-name" (required string)
           ~doc:"STRING DetectorModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_detector
           (Values.DescribeDetectorRequest.make ?keyValue ~detectorModelName
              ()) (Some Values.DescribeDetectorResponse.to_json)
           (Some Values.DescribeDetectorResponse.error_to_json)])
let list_alarms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and alarmModelName =
         flag "alarm-model-name" (required string)
           ~doc:"STRING AlarmModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_alarms
           (Values.ListAlarmsRequest.make ?nextToken ?maxResults
              ~alarmModelName ()) (Some Values.ListAlarmsResponse.to_json)
           (Some Values.ListAlarmsResponse.error_to_json)])
let list_detectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stateName =
         flag "state-name" (optional string) ~doc:"STRING StateName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and detectorModelName =
         flag "detector-model-name" (required string)
           ~doc:"STRING DetectorModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_detectors
           (Values.ListDetectorsRequest.make ?stateName ?nextToken
              ?maxResults ~detectorModelName ())
           (Some Values.ListDetectorsResponse.to_json)
           (Some Values.ListDetectorsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-acknowledge-alarm", batch_acknowledge_alarm);
    ("batch-disable-alarm", batch_disable_alarm);
    ("batch-enable-alarm", batch_enable_alarm);
    ("batch-put-message", batch_put_message);
    ("batch-reset-alarm", batch_reset_alarm);
    ("batch-snooze-alarm", batch_snooze_alarm);
    ("batch-update-detector", batch_update_detector);
    ("describe-alarm", describe_alarm);
    ("describe-detector", describe_detector);
    ("list-alarms", list_alarms);
    ("list-detectors", list_detectors)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchAcknowledgeAlarm: (BatchAcknowledgeAlarmRequest.t,
  BatchAcknowledgeAlarmResponse.t, BatchAcknowledgeAlarmResponse.error) t 
  | BatchDisableAlarm: (BatchDisableAlarmRequest.t,
  BatchDisableAlarmResponse.t, BatchDisableAlarmResponse.error) t 
  | BatchEnableAlarm: (BatchEnableAlarmRequest.t, BatchEnableAlarmResponse.t,
  BatchEnableAlarmResponse.error) t 
  | BatchPutMessage: (BatchPutMessageRequest.t, BatchPutMessageResponse.t,
  BatchPutMessageResponse.error) t 
  | BatchResetAlarm: (BatchResetAlarmRequest.t, BatchResetAlarmResponse.t,
  BatchResetAlarmResponse.error) t 
  | BatchSnoozeAlarm: (BatchSnoozeAlarmRequest.t, BatchSnoozeAlarmResponse.t,
  BatchSnoozeAlarmResponse.error) t 
  | BatchUpdateDetector: (BatchUpdateDetectorRequest.t,
  BatchUpdateDetectorResponse.t, BatchUpdateDetectorResponse.error) t 
  | DescribeAlarm: (DescribeAlarmRequest.t, DescribeAlarmResponse.t,
  DescribeAlarmResponse.error) t 
  | DescribeDetector: (DescribeDetectorRequest.t, DescribeDetectorResponse.t,
  DescribeDetectorResponse.error) t 
  | ListAlarms: (ListAlarmsRequest.t, ListAlarmsResponse.t,
  ListAlarmsResponse.error) t 
  | ListDetectors: (ListDetectorsRequest.t, ListDetectorsResponse.t,
  ListDetectorsResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchAcknowledgeAlarm -> `POST
  | BatchDisableAlarm -> `POST
  | BatchEnableAlarm -> `POST
  | BatchPutMessage -> `POST
  | BatchResetAlarm -> `POST
  | BatchSnoozeAlarm -> `POST
  | BatchUpdateDetector -> `POST
  | DescribeAlarm -> `GET
  | DescribeDetector -> `GET
  | ListAlarms -> `GET
  | ListDetectors -> `GET
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchAcknowledgeAlarm ->
          (Format.kasprintf Uri.of_string) "/alarms/acknowledge"
      | BatchDisableAlarm ->
          (Format.kasprintf Uri.of_string) "/alarms/disable"
      | BatchEnableAlarm -> (Format.kasprintf Uri.of_string) "/alarms/enable"
      | BatchPutMessage ->
          (Format.kasprintf Uri.of_string) "/inputs/messages"
      | BatchResetAlarm -> (Format.kasprintf Uri.of_string) "/alarms/reset"
      | BatchSnoozeAlarm -> (Format.kasprintf Uri.of_string) "/alarms/snooze"
      | BatchUpdateDetector -> (Format.kasprintf Uri.of_string) "/detectors"
      | DescribeAlarm ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/alarms/%s/keyValues/"
               (AlarmModelName.to_header
                  x.DescribeAlarmRequest.alarmModelName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("keyValue", (KeyValue.to_header v)))
                  x.keyValue])
      | DescribeDetector ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/detectors/%s/keyValues/"
               (DetectorModelName.to_header
                  x.DescribeDetectorRequest.detectorModelName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("keyValue", (KeyValue.to_header v)))
                  x.keyValue])
      | ListAlarms ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/alarms/%s"
               (AlarmModelName.to_header x.ListAlarmsRequest.alarmModelName))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults])
      | ListDetectors ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/detectors/%s"
               (DetectorModelName.to_header
                  x.ListDetectorsRequest.detectorModelName))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("stateName", (StateName.to_header v)))
                  x.stateName;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchAcknowledgeAlarm ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("acknowledgeActionRequests",
                           (AcknowledgeAlarmActionRequests.to_value
                              req.BatchAcknowledgeAlarmRequest.acknowledgeActionRequests))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchDisableAlarm ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("disableActionRequests",
                           (DisableAlarmActionRequests.to_value
                              req.BatchDisableAlarmRequest.disableActionRequests))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchEnableAlarm ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("enableActionRequests",
                           (EnableAlarmActionRequests.to_value
                              req.BatchEnableAlarmRequest.enableActionRequests))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchPutMessage ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("messages",
                           (Messages.to_value
                              req.BatchPutMessageRequest.messages))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchResetAlarm ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("resetActionRequests",
                           (ResetAlarmActionRequests.to_value
                              req.BatchResetAlarmRequest.resetActionRequests))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchSnoozeAlarm ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("snoozeActionRequests",
                           (SnoozeAlarmActionRequests.to_value
                              req.BatchSnoozeAlarmRequest.snoozeActionRequests))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchUpdateDetector ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("detectors",
                           (UpdateDetectorRequests.to_value
                              req.BatchUpdateDetectorRequest.detectors))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeAlarm ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeDetector ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListAlarms ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDetectors ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchAcknowledgeAlarm ->
      if is_success
      then Ok (BatchAcknowledgeAlarmResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some BatchAcknowledgeAlarmResponse.error_of_json))
  | BatchDisableAlarm ->
      if is_success
      then Ok (BatchDisableAlarmResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some BatchDisableAlarmResponse.error_of_json))
  | BatchEnableAlarm ->
      if is_success
      then Ok (BatchEnableAlarmResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some BatchEnableAlarmResponse.error_of_json))
  | BatchPutMessage ->
      if is_success
      then Ok (BatchPutMessageResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some BatchPutMessageResponse.error_of_json))
  | BatchResetAlarm ->
      if is_success
      then Ok (BatchResetAlarmResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some BatchResetAlarmResponse.error_of_json))
  | BatchSnoozeAlarm ->
      if is_success
      then Ok (BatchSnoozeAlarmResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some BatchSnoozeAlarmResponse.error_of_json))
  | BatchUpdateDetector ->
      if is_success
      then Ok (BatchUpdateDetectorResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some BatchUpdateDetectorResponse.error_of_json))
  | DescribeAlarm ->
      if is_success
      then Ok (DescribeAlarmResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeAlarmResponse.error_of_json))
  | DescribeDetector ->
      if is_success
      then Ok (DescribeDetectorResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DescribeDetectorResponse.error_of_json))
  | ListAlarms ->
      if is_success
      then Ok (ListAlarmsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListAlarmsResponse.error_of_json))
  | ListDetectors ->
      if is_success
      then Ok (ListDetectorsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListDetectorsResponse.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.iotevents_data
let apiVersion = "2018-10-23"
let endpointPrefix = "data.iotevents"
let serviceFullName = "AWS IoT Events Data"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Seconds =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Seconds" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TimerName =
  struct
    type nonrec t = string
    let context_ = "TimerName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TimerName" j
    let to_json = simple_to_json to_value
  end
module VariableName =
  struct
    type nonrec t = string
    let context_ = "VariableName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z][a-zA-Z0-9_]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VariableName" j
    let to_json = simple_to_json to_value
  end
module VariableValue =
  struct
    type nonrec t = string
    let context_ = "VariableValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VariableValue" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Note =
  struct
    type nonrec t = string
    let context_ = "Note"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Note" j
    let to_json = simple_to_json to_value
  end
module SnoozeDuration =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SnoozeDuration" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ComparisonOperator =
  struct
    type nonrec t =
      | GREATER 
      | GREATER_OR_EQUAL 
      | LESS 
      | LESS_OR_EQUAL 
      | EQUAL 
      | NOT_EQUAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GREATER -> "GREATER"
      | GREATER_OR_EQUAL -> "GREATER_OR_EQUAL"
      | LESS -> "LESS"
      | LESS_OR_EQUAL -> "LESS_OR_EQUAL"
      | EQUAL -> "EQUAL"
      | NOT_EQUAL -> "NOT_EQUAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "GREATER" -> GREATER
      | "GREATER_OR_EQUAL" -> GREATER_OR_EQUAL
      | "LESS" -> LESS
      | "LESS_OR_EQUAL" -> LESS_OR_EQUAL
      | "EQUAL" -> EQUAL
      | "NOT_EQUAL" -> NOT_EQUAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ComparisonOperator" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ComparisonOperator" j)
    let to_json = simple_to_json to_value
  end
module InputPropertyValue =
  struct
    type nonrec t = string
    let context_ = "InputPropertyValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InputPropertyValue" j
    let to_json = simple_to_json to_value
  end
module ThresholdValue =
  struct
    type nonrec t = string
    let context_ = "ThresholdValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ThresholdValue" j
    let to_json = simple_to_json to_value
  end
module TriggerType =
  struct
    type nonrec t =
      | SNOOZE_TIMEOUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SNOOZE_TIMEOUT -> "SNOOZE_TIMEOUT" | Non_static_id s -> s
    let of_string =
      function | "SNOOZE_TIMEOUT" -> SNOOZE_TIMEOUT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TriggerType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TriggerType" j)
    let to_json = simple_to_json to_value
  end
module TimerDefinition =
  struct
    type nonrec t = {
      name: TimerName.t ;
      seconds: Seconds.t }
    let context_ = "TimerDefinition"
    let make ~name = fun ~seconds -> fun () -> { name; seconds }
    let to_value x =
      structure_to_value
        [("name", (Some (TimerName.to_value x.name)));
        ("seconds", (Some (Seconds.to_value x.seconds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let seconds =
        Seconds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "seconds") in
      let name =
        TimerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~seconds ~name ()
    let of_json json =
      let seconds = field_map_exn json "seconds" Seconds.of_json in
      let name = field_map_exn json "name" TimerName.of_json in
      make ~seconds ~name ()
    let to_json v = composed_to_json to_value v
  end
module VariableDefinition =
  struct
    type nonrec t = {
      name: VariableName.t ;
      value: VariableValue.t }
    let context_ = "VariableDefinition"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Some (VariableName.to_value x.name)));
        ("value", (Some (VariableValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        VariableValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let name =
        VariableName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~value ~name ()
    let of_json json =
      let value = field_map_exn json "value" VariableValue.of_json in
      let name = field_map_exn json "name" VariableName.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end
module StateName =
  struct
    type nonrec t = string
    let context_ = "StateName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StateName" j
    let to_json = simple_to_json to_value
  end
module Timer =
  struct
    type nonrec t = {
      name: TimerName.t ;
      timestamp: Timestamp.t }
    let context_ = "Timer"
    let make ~name = fun ~timestamp -> fun () -> { name; timestamp }
    let to_value x =
      structure_to_value
        [("name", (Some (TimerName.to_value x.name)));
        ("timestamp", (Some (Timestamp.to_value x.timestamp)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "timestamp") in
      let name =
        TimerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~timestamp ~name ()
    let of_json json =
      let timestamp = field_map_exn json "timestamp" Timestamp.of_json in
      let name = field_map_exn json "name" TimerName.of_json in
      make ~timestamp ~name ()
    let to_json v = composed_to_json to_value v
  end
module Variable =
  struct
    type nonrec t = {
      name: VariableName.t ;
      value: VariableValue.t }
    let context_ = "Variable"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Some (VariableName.to_value x.name)));
        ("value", (Some (VariableValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        VariableValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let name =
        VariableName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~value ~name ()
    let of_json json =
      let value = field_map_exn json "value" VariableValue.of_json in
      let name = field_map_exn json "name" VariableName.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end
module AcknowledgeActionConfiguration =
  struct
    type nonrec t = {
      note: Note.t option }
    let make ?note = fun () -> { note }
    let to_value x =
      structure_to_value [("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      make ?note ()
    let of_json json =
      let note = field_map json "note" Note.of_json in make ?note ()
    let to_json v = composed_to_json to_value v
  end
module CustomerActionName =
  struct
    type nonrec t =
      | SNOOZE 
      | ENABLE 
      | DISABLE 
      | ACKNOWLEDGE 
      | RESET 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SNOOZE -> "SNOOZE"
      | ENABLE -> "ENABLE"
      | DISABLE -> "DISABLE"
      | ACKNOWLEDGE -> "ACKNOWLEDGE"
      | RESET -> "RESET"
      | Non_static_id s -> s
    let of_string =
      function
      | "SNOOZE" -> SNOOZE
      | "ENABLE" -> ENABLE
      | "DISABLE" -> DISABLE
      | "ACKNOWLEDGE" -> ACKNOWLEDGE
      | "RESET" -> RESET
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CustomerActionName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CustomerActionName" j)
    let to_json = simple_to_json to_value
  end
module DisableActionConfiguration =
  struct
    type nonrec t = {
      note: Note.t option }
    let make ?note = fun () -> { note }
    let to_value x =
      structure_to_value [("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      make ?note ()
    let of_json json =
      let note = field_map json "note" Note.of_json in make ?note ()
    let to_json v = composed_to_json to_value v
  end
module EnableActionConfiguration =
  struct
    type nonrec t = {
      note: Note.t option }
    let make ?note = fun () -> { note }
    let to_value x =
      structure_to_value [("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      make ?note ()
    let of_json json =
      let note = field_map json "note" Note.of_json in make ?note ()
    let to_json v = composed_to_json to_value v
  end
module ResetActionConfiguration =
  struct
    type nonrec t = {
      note: Note.t option }
    let make ?note = fun () -> { note }
    let to_value x =
      structure_to_value [("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      make ?note ()
    let of_json json =
      let note = field_map json "note" Note.of_json in make ?note ()
    let to_json v = composed_to_json to_value v
  end
module SnoozeActionConfiguration =
  struct
    type nonrec t =
      {
      snoozeDuration: SnoozeDuration.t option ;
      note: Note.t option }
    let make ?snoozeDuration =
      fun ?note -> fun () -> { snoozeDuration; note }
    let to_value x =
      structure_to_value
        [("snoozeDuration",
           (Option.map x.snoozeDuration ~f:SnoozeDuration.to_value));
        ("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      let snoozeDuration =
        (Option.map ~f:SnoozeDuration.of_xml)
          (Xml.child xml_arg0 "snoozeDuration") in
      make ?note ?snoozeDuration ()
    let of_json json =
      let note = field_map json "note" Note.of_json in
      let snoozeDuration =
        field_map json "snoozeDuration" SnoozeDuration.of_json in
      make ?note ?snoozeDuration ()
    let to_json v = composed_to_json to_value v
  end
module SimpleRuleEvaluation =
  struct
    type nonrec t =
      {
      inputPropertyValue: InputPropertyValue.t option ;
      operator: ComparisonOperator.t option ;
      thresholdValue: ThresholdValue.t option }
    let make ?inputPropertyValue =
      fun ?operator ->
        fun ?thresholdValue ->
          fun () -> { inputPropertyValue; operator; thresholdValue }
    let to_value x =
      structure_to_value
        [("inputPropertyValue",
           (Option.map x.inputPropertyValue ~f:InputPropertyValue.to_value));
        ("operator", (Option.map x.operator ~f:ComparisonOperator.to_value));
        ("thresholdValue",
          (Option.map x.thresholdValue ~f:ThresholdValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let thresholdValue =
        (Option.map ~f:ThresholdValue.of_xml)
          (Xml.child xml_arg0 "thresholdValue") in
      let operator =
        (Option.map ~f:ComparisonOperator.of_xml)
          (Xml.child xml_arg0 "operator") in
      let inputPropertyValue =
        (Option.map ~f:InputPropertyValue.of_xml)
          (Xml.child xml_arg0 "inputPropertyValue") in
      make ?thresholdValue ?operator ?inputPropertyValue ()
    let of_json json =
      let thresholdValue =
        field_map json "thresholdValue" ThresholdValue.of_json in
      let operator = field_map json "operator" ComparisonOperator.of_json in
      let inputPropertyValue =
        field_map json "inputPropertyValue" InputPropertyValue.of_json in
      make ?thresholdValue ?operator ?inputPropertyValue ()
    let to_json v = composed_to_json to_value v
  end
module EventType =
  struct
    type nonrec t =
      | STATE_CHANGE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | STATE_CHANGE -> "STATE_CHANGE" | Non_static_id s -> s
    let of_string =
      function | "STATE_CHANGE" -> STATE_CHANGE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EventType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventType" j)
    let to_json = simple_to_json to_value
  end
module StateChangeConfiguration =
  struct
    type nonrec t = {
      triggerType: TriggerType.t option }
    let make ?triggerType = fun () -> { triggerType }
    let to_value x =
      structure_to_value
        [("triggerType", (Option.map x.triggerType ~f:TriggerType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let triggerType =
        (Option.map ~f:TriggerType.of_xml) (Xml.child xml_arg0 "triggerType") in
      make ?triggerType ()
    let of_json json =
      let triggerType = field_map json "triggerType" TriggerType.of_json in
      make ?triggerType ()
    let to_json v = composed_to_json to_value v
  end
module TimerDefinitions =
  struct
    type nonrec t = TimerDefinition.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TimerDefinition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TimerDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"TimerDefinitions" ~of_json:TimerDefinition.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module VariableDefinitions =
  struct
    type nonrec t = VariableDefinition.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:VariableDefinition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VariableDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"VariableDefinitions"
        ~of_json:VariableDefinition.of_json j
    let to_json v = composed_to_json to_value v
  end
module EpochMilliTimestamp =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int64_max i ~max:9223372036854775807L) >>=
             (fun () -> check_int64_min i ~min:1L));
        i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module DetectorModelName =
  struct
    type nonrec t = string
    let context_ = "DetectorModelName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DetectorModelName" j
    let to_json = simple_to_json to_value
  end
module DetectorModelVersion =
  struct
    type nonrec t = string
    let context_ = "DetectorModelVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DetectorModelVersion" j
    let to_json = simple_to_json to_value
  end
module DetectorStateSummary =
  struct
    type nonrec t = {
      stateName: StateName.t option }
    let make ?stateName = fun () -> { stateName }
    let to_value x =
      structure_to_value
        [("stateName", (Option.map x.stateName ~f:StateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stateName =
        (Option.map ~f:StateName.of_xml) (Xml.child xml_arg0 "stateName") in
      make ?stateName ()
    let of_json json =
      let stateName = field_map json "stateName" StateName.of_json in
      make ?stateName ()
    let to_json v = composed_to_json to_value v
  end
module KeyValue =
  struct
    type nonrec t = string
    let context_ = "KeyValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9\\-_:]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KeyValue" j
    let to_json = simple_to_json to_value
  end
module AlarmModelName =
  struct
    type nonrec t = string
    let context_ = "AlarmModelName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AlarmModelName" j
    let to_json = simple_to_json to_value
  end
module AlarmModelVersion =
  struct
    type nonrec t = string
    let context_ = "AlarmModelVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AlarmModelVersion" j
    let to_json = simple_to_json to_value
  end
module AlarmStateName =
  struct
    type nonrec t =
      | DISABLED 
      | NORMAL 
      | ACTIVE 
      | ACKNOWLEDGED 
      | SNOOZE_DISABLED 
      | LATCHED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | NORMAL -> "NORMAL"
      | ACTIVE -> "ACTIVE"
      | ACKNOWLEDGED -> "ACKNOWLEDGED"
      | SNOOZE_DISABLED -> "SNOOZE_DISABLED"
      | LATCHED -> "LATCHED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "NORMAL" -> NORMAL
      | "ACTIVE" -> ACTIVE
      | "ACKNOWLEDGED" -> ACKNOWLEDGED
      | "SNOOZE_DISABLED" -> SNOOZE_DISABLED
      | "LATCHED" -> LATCHED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AlarmStateName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AlarmStateName" j)
    let to_json = simple_to_json to_value
  end
module Timers =
  struct
    type nonrec t = Timer.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Timer.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Timer.of_xml)
    let of_json j = list_of_json ~kind:"Timers" ~of_json:Timer.of_json j
    let to_json v = composed_to_json to_value v
  end
module Variables =
  struct
    type nonrec t = Variable.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Variable.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Variable.of_xml)
    let of_json j =
      list_of_json ~kind:"Variables" ~of_json:Variable.of_json j
    let to_json v = composed_to_json to_value v
  end
module CustomerAction =
  struct
    type nonrec t =
      {
      actionName: CustomerActionName.t option ;
      snoozeActionConfiguration: SnoozeActionConfiguration.t option ;
      enableActionConfiguration: EnableActionConfiguration.t option ;
      disableActionConfiguration: DisableActionConfiguration.t option ;
      acknowledgeActionConfiguration: AcknowledgeActionConfiguration.t option ;
      resetActionConfiguration: ResetActionConfiguration.t option }
    let make ?actionName =
      fun ?snoozeActionConfiguration ->
        fun ?enableActionConfiguration ->
          fun ?disableActionConfiguration ->
            fun ?acknowledgeActionConfiguration ->
              fun ?resetActionConfiguration ->
                fun () ->
                  {
                    actionName;
                    snoozeActionConfiguration;
                    enableActionConfiguration;
                    disableActionConfiguration;
                    acknowledgeActionConfiguration;
                    resetActionConfiguration
                  }
    let to_value x =
      structure_to_value
        [("actionName",
           (Option.map x.actionName ~f:CustomerActionName.to_value));
        ("snoozeActionConfiguration",
          (Option.map x.snoozeActionConfiguration
             ~f:SnoozeActionConfiguration.to_value));
        ("enableActionConfiguration",
          (Option.map x.enableActionConfiguration
             ~f:EnableActionConfiguration.to_value));
        ("disableActionConfiguration",
          (Option.map x.disableActionConfiguration
             ~f:DisableActionConfiguration.to_value));
        ("acknowledgeActionConfiguration",
          (Option.map x.acknowledgeActionConfiguration
             ~f:AcknowledgeActionConfiguration.to_value));
        ("resetActionConfiguration",
          (Option.map x.resetActionConfiguration
             ~f:ResetActionConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resetActionConfiguration =
        (Option.map ~f:ResetActionConfiguration.of_xml)
          (Xml.child xml_arg0 "resetActionConfiguration") in
      let acknowledgeActionConfiguration =
        (Option.map ~f:AcknowledgeActionConfiguration.of_xml)
          (Xml.child xml_arg0 "acknowledgeActionConfiguration") in
      let disableActionConfiguration =
        (Option.map ~f:DisableActionConfiguration.of_xml)
          (Xml.child xml_arg0 "disableActionConfiguration") in
      let enableActionConfiguration =
        (Option.map ~f:EnableActionConfiguration.of_xml)
          (Xml.child xml_arg0 "enableActionConfiguration") in
      let snoozeActionConfiguration =
        (Option.map ~f:SnoozeActionConfiguration.of_xml)
          (Xml.child xml_arg0 "snoozeActionConfiguration") in
      let actionName =
        (Option.map ~f:CustomerActionName.of_xml)
          (Xml.child xml_arg0 "actionName") in
      make ?resetActionConfiguration ?acknowledgeActionConfiguration
        ?disableActionConfiguration ?enableActionConfiguration
        ?snoozeActionConfiguration ?actionName ()
    let of_json json =
      let resetActionConfiguration =
        field_map json "resetActionConfiguration"
          ResetActionConfiguration.of_json in
      let acknowledgeActionConfiguration =
        field_map json "acknowledgeActionConfiguration"
          AcknowledgeActionConfiguration.of_json in
      let disableActionConfiguration =
        field_map json "disableActionConfiguration"
          DisableActionConfiguration.of_json in
      let enableActionConfiguration =
        field_map json "enableActionConfiguration"
          EnableActionConfiguration.of_json in
      let snoozeActionConfiguration =
        field_map json "snoozeActionConfiguration"
          SnoozeActionConfiguration.of_json in
      let actionName = field_map json "actionName" CustomerActionName.of_json in
      make ?resetActionConfiguration ?acknowledgeActionConfiguration
        ?disableActionConfiguration ?enableActionConfiguration
        ?snoozeActionConfiguration ?actionName ()
    let to_json v = composed_to_json to_value v
  end
module RuleEvaluation =
  struct
    type nonrec t = {
      simpleRuleEvaluation: SimpleRuleEvaluation.t option }
    let make ?simpleRuleEvaluation = fun () -> { simpleRuleEvaluation }
    let to_value x =
      structure_to_value
        [("simpleRuleEvaluation",
           (Option.map x.simpleRuleEvaluation
              ~f:SimpleRuleEvaluation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let simpleRuleEvaluation =
        (Option.map ~f:SimpleRuleEvaluation.of_xml)
          (Xml.child xml_arg0 "simpleRuleEvaluation") in
      make ?simpleRuleEvaluation ()
    let of_json json =
      let simpleRuleEvaluation =
        field_map json "simpleRuleEvaluation" SimpleRuleEvaluation.of_json in
      make ?simpleRuleEvaluation ()
    let to_json v = composed_to_json to_value v
  end
module SystemEvent =
  struct
    type nonrec t =
      {
      eventType: EventType.t option ;
      stateChangeConfiguration: StateChangeConfiguration.t option }
    let make ?eventType =
      fun ?stateChangeConfiguration ->
        fun () -> { eventType; stateChangeConfiguration }
    let to_value x =
      structure_to_value
        [("eventType", (Option.map x.eventType ~f:EventType.to_value));
        ("stateChangeConfiguration",
          (Option.map x.stateChangeConfiguration
             ~f:StateChangeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stateChangeConfiguration =
        (Option.map ~f:StateChangeConfiguration.of_xml)
          (Xml.child xml_arg0 "stateChangeConfiguration") in
      let eventType =
        (Option.map ~f:EventType.of_xml) (Xml.child xml_arg0 "eventType") in
      make ?stateChangeConfiguration ?eventType ()
    let of_json json =
      let stateChangeConfiguration =
        field_map json "stateChangeConfiguration"
          StateChangeConfiguration.of_json in
      let eventType = field_map json "eventType" EventType.of_json in
      make ?stateChangeConfiguration ?eventType ()
    let to_json v = composed_to_json to_value v
  end
module ErrorCode =
  struct
    type nonrec t =
      | ResourceNotFoundException 
      | InvalidRequestException 
      | InternalFailureException 
      | ServiceUnavailableException 
      | ThrottlingException 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ResourceNotFoundException -> "ResourceNotFoundException"
      | InvalidRequestException -> "InvalidRequestException"
      | InternalFailureException -> "InternalFailureException"
      | ServiceUnavailableException -> "ServiceUnavailableException"
      | ThrottlingException -> "ThrottlingException"
      | Non_static_id s -> s
    let of_string =
      function
      | "ResourceNotFoundException" -> ResourceNotFoundException
      | "InvalidRequestException" -> InvalidRequestException
      | "InternalFailureException" -> InternalFailureException
      | "ServiceUnavailableException" -> ServiceUnavailableException
      | "ThrottlingException" -> ThrottlingException
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ErrorCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ErrorCode" j)
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module MessageId =
  struct
    type nonrec t = string
    let context_ = "MessageId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MessageId" j
    let to_json = simple_to_json to_value
  end
module DetectorStateDefinition =
  struct
    type nonrec t =
      {
      stateName: StateName.t ;
      variables: VariableDefinitions.t ;
      timers: TimerDefinitions.t }
    let context_ = "DetectorStateDefinition"
    let make ~stateName =
      fun ~variables ->
        fun ~timers -> fun () -> { stateName; variables; timers }
    let to_value x =
      structure_to_value
        [("stateName", (Some (StateName.to_value x.stateName)));
        ("variables", (Some (VariableDefinitions.to_value x.variables)));
        ("timers", (Some (TimerDefinitions.to_value x.timers)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timers =
        TimerDefinitions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "timers") in
      let variables =
        VariableDefinitions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "variables") in
      let stateName =
        StateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stateName") in
      make ~timers ~variables ~stateName ()
    let of_json json =
      let timers = field_map_exn json "timers" TimerDefinitions.of_json in
      let variables =
        field_map_exn json "variables" VariableDefinitions.of_json in
      let stateName = field_map_exn json "stateName" StateName.of_json in
      make ~timers ~variables ~stateName ()
    let to_json v = composed_to_json to_value v
  end
module RequestId =
  struct
    type nonrec t = string
    let context_ = "RequestId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RequestId" j
    let to_json = simple_to_json to_value
  end
module EphemeralInputName =
  struct
    type nonrec t = string
    let context_ = "EphemeralInputName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9][a-zA-Z0-9_.-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EphemeralInputName" j
    let to_json = simple_to_json to_value
  end
module Payload =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module TimestampValue =
  struct
    type nonrec t = {
      timeInMillis: EpochMilliTimestamp.t option }
    let make ?timeInMillis = fun () -> { timeInMillis }
    let to_value x =
      structure_to_value
        [("timeInMillis",
           (Option.map x.timeInMillis ~f:EpochMilliTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeInMillis =
        (Option.map ~f:EpochMilliTimestamp.of_xml)
          (Xml.child xml_arg0 "timeInMillis") in
      make ?timeInMillis ()
    let of_json json =
      let timeInMillis =
        field_map json "timeInMillis" EpochMilliTimestamp.of_json in
      make ?timeInMillis ()
    let to_json v = composed_to_json to_value v
  end
module DetectorSummary =
  struct
    type nonrec t =
      {
      detectorModelName: DetectorModelName.t option ;
      keyValue: KeyValue.t option ;
      detectorModelVersion: DetectorModelVersion.t option ;
      state: DetectorStateSummary.t option ;
      creationTime: Timestamp.t option ;
      lastUpdateTime: Timestamp.t option }
    let make ?detectorModelName =
      fun ?keyValue ->
        fun ?detectorModelVersion ->
          fun ?state ->
            fun ?creationTime ->
              fun ?lastUpdateTime ->
                fun () ->
                  {
                    detectorModelName;
                    keyValue;
                    detectorModelVersion;
                    state;
                    creationTime;
                    lastUpdateTime
                  }
    let to_value x =
      structure_to_value
        [("detectorModelName",
           (Option.map x.detectorModelName ~f:DetectorModelName.to_value));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("detectorModelVersion",
          (Option.map x.detectorModelVersion ~f:DetectorModelVersion.to_value));
        ("state", (Option.map x.state ~f:DetectorStateSummary.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdateTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let state =
        (Option.map ~f:DetectorStateSummary.of_xml)
          (Xml.child xml_arg0 "state") in
      let detectorModelVersion =
        (Option.map ~f:DetectorModelVersion.of_xml)
          (Xml.child xml_arg0 "detectorModelVersion") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let detectorModelName =
        (Option.map ~f:DetectorModelName.of_xml)
          (Xml.child xml_arg0 "detectorModelName") in
      make ?lastUpdateTime ?creationTime ?state ?detectorModelVersion
        ?keyValue ?detectorModelName ()
    let of_json json =
      let lastUpdateTime = field_map json "lastUpdateTime" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let state = field_map json "state" DetectorStateSummary.of_json in
      let detectorModelVersion =
        field_map json "detectorModelVersion" DetectorModelVersion.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let detectorModelName =
        field_map json "detectorModelName" DetectorModelName.of_json in
      make ?lastUpdateTime ?creationTime ?state ?detectorModelVersion
        ?keyValue ?detectorModelName ()
    let to_json v = composed_to_json to_value v
  end
module ErrorMessage__lc1 =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage__lc1"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage__lc1" j
    let to_json = simple_to_json to_value
  end
module AlarmSummary =
  struct
    type nonrec t =
      {
      alarmModelName: AlarmModelName.t option ;
      alarmModelVersion: AlarmModelVersion.t option ;
      keyValue: KeyValue.t option ;
      stateName: AlarmStateName.t option ;
      creationTime: Timestamp.t option ;
      lastUpdateTime: Timestamp.t option }
    let make ?alarmModelName =
      fun ?alarmModelVersion ->
        fun ?keyValue ->
          fun ?stateName ->
            fun ?creationTime ->
              fun ?lastUpdateTime ->
                fun () ->
                  {
                    alarmModelName;
                    alarmModelVersion;
                    keyValue;
                    stateName;
                    creationTime;
                    lastUpdateTime
                  }
    let to_value x =
      structure_to_value
        [("alarmModelName",
           (Option.map x.alarmModelName ~f:AlarmModelName.to_value));
        ("alarmModelVersion",
          (Option.map x.alarmModelVersion ~f:AlarmModelVersion.to_value));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("stateName", (Option.map x.stateName ~f:AlarmStateName.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdateTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let stateName =
        (Option.map ~f:AlarmStateName.of_xml)
          (Xml.child xml_arg0 "stateName") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelVersion =
        (Option.map ~f:AlarmModelVersion.of_xml)
          (Xml.child xml_arg0 "alarmModelVersion") in
      let alarmModelName =
        (Option.map ~f:AlarmModelName.of_xml)
          (Xml.child xml_arg0 "alarmModelName") in
      make ?lastUpdateTime ?creationTime ?stateName ?keyValue
        ?alarmModelVersion ?alarmModelName ()
    let of_json json =
      let lastUpdateTime = field_map json "lastUpdateTime" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let stateName = field_map json "stateName" AlarmStateName.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelVersion =
        field_map json "alarmModelVersion" AlarmModelVersion.of_json in
      let alarmModelName =
        field_map json "alarmModelName" AlarmModelName.of_json in
      make ?lastUpdateTime ?creationTime ?stateName ?keyValue
        ?alarmModelVersion ?alarmModelName ()
    let to_json v = composed_to_json to_value v
  end
module DetectorState =
  struct
    type nonrec t =
      {
      stateName: StateName.t ;
      variables: Variables.t ;
      timers: Timers.t }
    let context_ = "DetectorState"
    let make ~stateName =
      fun ~variables ->
        fun ~timers -> fun () -> { stateName; variables; timers }
    let to_value x =
      structure_to_value
        [("stateName", (Some (StateName.to_value x.stateName)));
        ("variables", (Some (Variables.to_value x.variables)));
        ("timers", (Some (Timers.to_value x.timers)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timers =
        Timers.of_xml (Xml.child_exn ~context:context_ xml_arg0 "timers") in
      let variables =
        Variables.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "variables") in
      let stateName =
        StateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stateName") in
      make ~timers ~variables ~stateName ()
    let of_json json =
      let timers = field_map_exn json "timers" Timers.of_json in
      let variables = field_map_exn json "variables" Variables.of_json in
      let stateName = field_map_exn json "stateName" StateName.of_json in
      make ~timers ~variables ~stateName ()
    let to_json v = composed_to_json to_value v
  end
module AlarmState =
  struct
    type nonrec t =
      {
      stateName: AlarmStateName.t option ;
      ruleEvaluation: RuleEvaluation.t option ;
      customerAction: CustomerAction.t option ;
      systemEvent: SystemEvent.t option }
    let make ?stateName =
      fun ?ruleEvaluation ->
        fun ?customerAction ->
          fun ?systemEvent ->
            fun () ->
              { stateName; ruleEvaluation; customerAction; systemEvent }
    let to_value x =
      structure_to_value
        [("stateName", (Option.map x.stateName ~f:AlarmStateName.to_value));
        ("ruleEvaluation",
          (Option.map x.ruleEvaluation ~f:RuleEvaluation.to_value));
        ("customerAction",
          (Option.map x.customerAction ~f:CustomerAction.to_value));
        ("systemEvent", (Option.map x.systemEvent ~f:SystemEvent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let systemEvent =
        (Option.map ~f:SystemEvent.of_xml) (Xml.child xml_arg0 "systemEvent") in
      let customerAction =
        (Option.map ~f:CustomerAction.of_xml)
          (Xml.child xml_arg0 "customerAction") in
      let ruleEvaluation =
        (Option.map ~f:RuleEvaluation.of_xml)
          (Xml.child xml_arg0 "ruleEvaluation") in
      let stateName =
        (Option.map ~f:AlarmStateName.of_xml)
          (Xml.child xml_arg0 "stateName") in
      make ?systemEvent ?customerAction ?ruleEvaluation ?stateName ()
    let of_json json =
      let systemEvent = field_map json "systemEvent" SystemEvent.of_json in
      let customerAction =
        field_map json "customerAction" CustomerAction.of_json in
      let ruleEvaluation =
        field_map json "ruleEvaluation" RuleEvaluation.of_json in
      let stateName = field_map json "stateName" AlarmStateName.of_json in
      make ?systemEvent ?customerAction ?ruleEvaluation ?stateName ()
    let to_json v = composed_to_json to_value v
  end
module Severity =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:2147483647) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Severity" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BatchUpdateDetectorErrorEntry =
  struct
    type nonrec t =
      {
      messageId: MessageId.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage__lc1: ErrorMessage.t option }
    let make ?messageId =
      fun ?errorCode ->
        fun ?errorMessage__lc1 ->
          fun () -> { messageId; errorCode; errorMessage__lc1 }
    let to_value x =
      structure_to_value
        [("messageId", (Option.map x.messageId ~f:MessageId.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("ErrorMessage__lc1",
          (Option.map x.errorMessage__lc1 ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage__lc1 =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "ErrorMessage__lc1") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let messageId =
        (Option.map ~f:MessageId.of_xml) (Xml.child xml_arg0 "messageId") in
      make ?errorMessage__lc1 ?errorCode ?messageId ()
    let of_json json =
      let errorMessage__lc1 =
        field_map json "ErrorMessage__lc1" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let messageId = field_map json "messageId" MessageId.of_json in
      make ?errorMessage__lc1 ?errorCode ?messageId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateDetectorRequest =
  struct
    type nonrec t =
      {
      messageId: MessageId.t ;
      detectorModelName: DetectorModelName.t ;
      keyValue: KeyValue.t option ;
      state: DetectorStateDefinition.t }
    let context_ = "UpdateDetectorRequest"
    let make ?keyValue =
      fun ~messageId ->
        fun ~detectorModelName ->
          fun ~state ->
            fun () -> { keyValue; messageId; detectorModelName; state }
    let to_value x =
      structure_to_value
        [("messageId", (Some (MessageId.to_value x.messageId)));
        ("detectorModelName",
          (Some (DetectorModelName.to_value x.detectorModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("state", (Some (DetectorStateDefinition.to_value x.state)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        DetectorStateDefinition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "state") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let detectorModelName =
        DetectorModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "detectorModelName") in
      let messageId =
        MessageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "messageId") in
      make ~state ?keyValue ~detectorModelName ~messageId ()
    let of_json json =
      let state = field_map_exn json "state" DetectorStateDefinition.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let detectorModelName =
        field_map_exn json "detectorModelName" DetectorModelName.of_json in
      let messageId = field_map_exn json "messageId" MessageId.of_json in
      make ~state ?keyValue ~detectorModelName ~messageId ()
    let to_json v = composed_to_json to_value v
  end
module BatchAlarmActionErrorEntry =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage__lc1: ErrorMessage.t option }
    let make ?requestId =
      fun ?errorCode ->
        fun ?errorMessage__lc1 ->
          fun () -> { requestId; errorCode; errorMessage__lc1 }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("ErrorMessage__lc1",
          (Option.map x.errorMessage__lc1 ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage__lc1 =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "ErrorMessage__lc1") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?errorMessage__lc1 ?errorCode ?requestId ()
    let of_json json =
      let errorMessage__lc1 =
        field_map json "ErrorMessage__lc1" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let requestId = field_map json "requestId" RequestId.of_json in
      make ?errorMessage__lc1 ?errorCode ?requestId ()
    let to_json v = composed_to_json to_value v
  end
module SnoozeAlarmActionRequest =
  struct
    type nonrec t =
      {
      requestId: RequestId.t ;
      alarmModelName: AlarmModelName.t ;
      keyValue: KeyValue.t option ;
      note: Note.t option ;
      snoozeDuration: SnoozeDuration.t }
    let context_ = "SnoozeAlarmActionRequest"
    let make ?keyValue =
      fun ?note ->
        fun ~requestId ->
          fun ~alarmModelName ->
            fun ~snoozeDuration ->
              fun () ->
                { keyValue; note; requestId; alarmModelName; snoozeDuration }
    let to_value x =
      structure_to_value
        [("requestId", (Some (RequestId.to_value x.requestId)));
        ("alarmModelName", (Some (AlarmModelName.to_value x.alarmModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("note", (Option.map x.note ~f:Note.to_value));
        ("snoozeDuration", (Some (SnoozeDuration.to_value x.snoozeDuration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snoozeDuration =
        SnoozeDuration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "snoozeDuration") in
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      let requestId =
        RequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ~snoozeDuration ?note ?keyValue ~alarmModelName ~requestId ()
    let of_json json =
      let snoozeDuration =
        field_map_exn json "snoozeDuration" SnoozeDuration.of_json in
      let note = field_map json "note" Note.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      let requestId = field_map_exn json "requestId" RequestId.of_json in
      make ~snoozeDuration ?note ?keyValue ~alarmModelName ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module ResetAlarmActionRequest =
  struct
    type nonrec t =
      {
      requestId: RequestId.t ;
      alarmModelName: AlarmModelName.t ;
      keyValue: KeyValue.t option ;
      note: Note.t option }
    let context_ = "ResetAlarmActionRequest"
    let make ?keyValue =
      fun ?note ->
        fun ~requestId ->
          fun ~alarmModelName ->
            fun () -> { keyValue; note; requestId; alarmModelName }
    let to_value x =
      structure_to_value
        [("requestId", (Some (RequestId.to_value x.requestId)));
        ("alarmModelName", (Some (AlarmModelName.to_value x.alarmModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      let requestId =
        RequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let of_json json =
      let note = field_map json "note" Note.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      let requestId = field_map_exn json "requestId" RequestId.of_json in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module BatchPutMessageErrorEntry =
  struct
    type nonrec t =
      {
      messageId: MessageId.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage__lc1: ErrorMessage.t option }
    let make ?messageId =
      fun ?errorCode ->
        fun ?errorMessage__lc1 ->
          fun () -> { messageId; errorCode; errorMessage__lc1 }
    let to_value x =
      structure_to_value
        [("messageId", (Option.map x.messageId ~f:MessageId.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("ErrorMessage__lc1",
          (Option.map x.errorMessage__lc1 ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage__lc1 =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "ErrorMessage__lc1") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let messageId =
        (Option.map ~f:MessageId.of_xml) (Xml.child xml_arg0 "messageId") in
      make ?errorMessage__lc1 ?errorCode ?messageId ()
    let of_json json =
      let errorMessage__lc1 =
        field_map json "ErrorMessage__lc1" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let messageId = field_map json "messageId" MessageId.of_json in
      make ?errorMessage__lc1 ?errorCode ?messageId ()
    let to_json v = composed_to_json to_value v
  end
module Message =
  struct
    type nonrec t =
      {
      messageId: MessageId.t ;
      inputName: EphemeralInputName.t ;
      payload: Payload.t ;
      timestamp: TimestampValue.t option }
    let context_ = "Message"
    let make ?timestamp =
      fun ~messageId ->
        fun ~inputName ->
          fun ~payload ->
            fun () -> { timestamp; messageId; inputName; payload }
    let to_value x =
      structure_to_value
        [("messageId", (Some (MessageId.to_value x.messageId)));
        ("inputName", (Some (EphemeralInputName.to_value x.inputName)));
        ("payload", (Some (Payload.to_value x.payload)));
        ("timestamp", (Option.map x.timestamp ~f:TimestampValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:TimestampValue.of_xml)
          (Xml.child xml_arg0 "timestamp") in
      let payload =
        Payload.of_xml (Xml.child_exn ~context:context_ xml_arg0 "payload") in
      let inputName =
        EphemeralInputName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inputName") in
      let messageId =
        MessageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "messageId") in
      make ?timestamp ~payload ~inputName ~messageId ()
    let of_json json =
      let timestamp = field_map json "timestamp" TimestampValue.of_json in
      let payload = field_map_exn json "payload" Payload.of_json in
      let inputName =
        field_map_exn json "inputName" EphemeralInputName.of_json in
      let messageId = field_map_exn json "messageId" MessageId.of_json in
      make ?timestamp ~payload ~inputName ~messageId ()
    let to_json v = composed_to_json to_value v
  end
module EnableAlarmActionRequest =
  struct
    type nonrec t =
      {
      requestId: RequestId.t ;
      alarmModelName: AlarmModelName.t ;
      keyValue: KeyValue.t option ;
      note: Note.t option }
    let context_ = "EnableAlarmActionRequest"
    let make ?keyValue =
      fun ?note ->
        fun ~requestId ->
          fun ~alarmModelName ->
            fun () -> { keyValue; note; requestId; alarmModelName }
    let to_value x =
      structure_to_value
        [("requestId", (Some (RequestId.to_value x.requestId)));
        ("alarmModelName", (Some (AlarmModelName.to_value x.alarmModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      let requestId =
        RequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let of_json json =
      let note = field_map json "note" Note.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      let requestId = field_map_exn json "requestId" RequestId.of_json in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module DisableAlarmActionRequest =
  struct
    type nonrec t =
      {
      requestId: RequestId.t ;
      alarmModelName: AlarmModelName.t ;
      keyValue: KeyValue.t option ;
      note: Note.t option }
    let context_ = "DisableAlarmActionRequest"
    let make ?keyValue =
      fun ?note ->
        fun ~requestId ->
          fun ~alarmModelName ->
            fun () -> { keyValue; note; requestId; alarmModelName }
    let to_value x =
      structure_to_value
        [("requestId", (Some (RequestId.to_value x.requestId)));
        ("alarmModelName", (Some (AlarmModelName.to_value x.alarmModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      let requestId =
        RequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let of_json json =
      let note = field_map json "note" Note.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      let requestId = field_map_exn json "requestId" RequestId.of_json in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module AcknowledgeAlarmActionRequest =
  struct
    type nonrec t =
      {
      requestId: RequestId.t ;
      alarmModelName: AlarmModelName.t ;
      keyValue: KeyValue.t option ;
      note: Note.t option }
    let context_ = "AcknowledgeAlarmActionRequest"
    let make ?keyValue =
      fun ?note ->
        fun ~requestId ->
          fun ~alarmModelName ->
            fun () -> { keyValue; note; requestId; alarmModelName }
    let to_value x =
      structure_to_value
        [("requestId", (Some (RequestId.to_value x.requestId)));
        ("alarmModelName", (Some (AlarmModelName.to_value x.alarmModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("note", (Option.map x.note ~f:Note.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let note = (Option.map ~f:Note.of_xml) (Xml.child xml_arg0 "note") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      let requestId =
        RequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let of_json json =
      let note = field_map json "note" Note.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      let requestId = field_map_exn json "requestId" RequestId.of_json in
      make ?note ?keyValue ~alarmModelName ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module DetectorSummaries =
  struct
    type nonrec t = DetectorSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DetectorSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DetectorSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DetectorSummaries" ~of_json:DetectorSummary.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module InternalFailureException =
  struct
    type nonrec t = {
      message: ErrorMessage__lc1.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage__lc1.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage__lc1.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage__lc1.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module InvalidRequestException =
  struct
    type nonrec t = {
      message: ErrorMessage__lc1.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage__lc1.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage__lc1.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage__lc1.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage__lc1.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage__lc1.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage__lc1.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage__lc1.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ServiceUnavailableException =
  struct
    type nonrec t = {
      message: ErrorMessage__lc1.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage__lc1.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage__lc1.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage__lc1.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: ErrorMessage__lc1.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage__lc1.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage__lc1.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage__lc1.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AlarmSummaries =
  struct
    type nonrec t = AlarmSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AlarmSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AlarmSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AlarmSummaries" ~of_json:AlarmSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Detector =
  struct
    type nonrec t =
      {
      detectorModelName: DetectorModelName.t option ;
      keyValue: KeyValue.t option ;
      detectorModelVersion: DetectorModelVersion.t option ;
      state: DetectorState.t option ;
      creationTime: Timestamp.t option ;
      lastUpdateTime: Timestamp.t option }
    let make ?detectorModelName =
      fun ?keyValue ->
        fun ?detectorModelVersion ->
          fun ?state ->
            fun ?creationTime ->
              fun ?lastUpdateTime ->
                fun () ->
                  {
                    detectorModelName;
                    keyValue;
                    detectorModelVersion;
                    state;
                    creationTime;
                    lastUpdateTime
                  }
    let to_value x =
      structure_to_value
        [("detectorModelName",
           (Option.map x.detectorModelName ~f:DetectorModelName.to_value));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("detectorModelVersion",
          (Option.map x.detectorModelVersion ~f:DetectorModelVersion.to_value));
        ("state", (Option.map x.state ~f:DetectorState.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdateTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let state =
        (Option.map ~f:DetectorState.of_xml) (Xml.child xml_arg0 "state") in
      let detectorModelVersion =
        (Option.map ~f:DetectorModelVersion.of_xml)
          (Xml.child xml_arg0 "detectorModelVersion") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let detectorModelName =
        (Option.map ~f:DetectorModelName.of_xml)
          (Xml.child xml_arg0 "detectorModelName") in
      make ?lastUpdateTime ?creationTime ?state ?detectorModelVersion
        ?keyValue ?detectorModelName ()
    let of_json json =
      let lastUpdateTime = field_map json "lastUpdateTime" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let state = field_map json "state" DetectorState.of_json in
      let detectorModelVersion =
        field_map json "detectorModelVersion" DetectorModelVersion.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let detectorModelName =
        field_map json "detectorModelName" DetectorModelName.of_json in
      make ?lastUpdateTime ?creationTime ?state ?detectorModelVersion
        ?keyValue ?detectorModelName ()
    let to_json v = composed_to_json to_value v
  end
module Alarm =
  struct
    type nonrec t =
      {
      alarmModelName: AlarmModelName.t option ;
      alarmModelVersion: AlarmModelVersion.t option ;
      keyValue: KeyValue.t option ;
      alarmState: AlarmState.t option ;
      severity: Severity.t option ;
      creationTime: Timestamp.t option ;
      lastUpdateTime: Timestamp.t option }
    let make ?alarmModelName =
      fun ?alarmModelVersion ->
        fun ?keyValue ->
          fun ?alarmState ->
            fun ?severity ->
              fun ?creationTime ->
                fun ?lastUpdateTime ->
                  fun () ->
                    {
                      alarmModelName;
                      alarmModelVersion;
                      keyValue;
                      alarmState;
                      severity;
                      creationTime;
                      lastUpdateTime
                    }
    let to_value x =
      structure_to_value
        [("alarmModelName",
           (Option.map x.alarmModelName ~f:AlarmModelName.to_value));
        ("alarmModelVersion",
          (Option.map x.alarmModelVersion ~f:AlarmModelVersion.to_value));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value));
        ("alarmState", (Option.map x.alarmState ~f:AlarmState.to_value));
        ("severity", (Option.map x.severity ~f:Severity.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdateTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let severity =
        (Option.map ~f:Severity.of_xml) (Xml.child xml_arg0 "severity") in
      let alarmState =
        (Option.map ~f:AlarmState.of_xml) (Xml.child xml_arg0 "alarmState") in
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelVersion =
        (Option.map ~f:AlarmModelVersion.of_xml)
          (Xml.child xml_arg0 "alarmModelVersion") in
      let alarmModelName =
        (Option.map ~f:AlarmModelName.of_xml)
          (Xml.child xml_arg0 "alarmModelName") in
      make ?lastUpdateTime ?creationTime ?severity ?alarmState ?keyValue
        ?alarmModelVersion ?alarmModelName ()
    let of_json json =
      let lastUpdateTime = field_map json "lastUpdateTime" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let severity = field_map json "severity" Severity.of_json in
      let alarmState = field_map json "alarmState" AlarmState.of_json in
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelVersion =
        field_map json "alarmModelVersion" AlarmModelVersion.of_json in
      let alarmModelName =
        field_map json "alarmModelName" AlarmModelName.of_json in
      make ?lastUpdateTime ?creationTime ?severity ?alarmState ?keyValue
        ?alarmModelVersion ?alarmModelName ()
    let to_json v = composed_to_json to_value v
  end
module BatchUpdateDetectorErrorEntries =
  struct
    type nonrec t = BatchUpdateDetectorErrorEntry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BatchUpdateDetectorErrorEntry.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchUpdateDetectorErrorEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchUpdateDetectorErrorEntries"
        ~of_json:BatchUpdateDetectorErrorEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateDetectorRequests =
  struct
    type nonrec t = UpdateDetectorRequest.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:UpdateDetectorRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UpdateDetectorRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"UpdateDetectorRequests"
        ~of_json:UpdateDetectorRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchAlarmActionErrorEntries =
  struct
    type nonrec t = BatchAlarmActionErrorEntry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BatchAlarmActionErrorEntry.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchAlarmActionErrorEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchAlarmActionErrorEntries"
        ~of_json:BatchAlarmActionErrorEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module SnoozeAlarmActionRequests =
  struct
    type nonrec t = SnoozeAlarmActionRequest.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:SnoozeAlarmActionRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SnoozeAlarmActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"SnoozeAlarmActionRequests"
        ~of_json:SnoozeAlarmActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResetAlarmActionRequests =
  struct
    type nonrec t = ResetAlarmActionRequest.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:ResetAlarmActionRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResetAlarmActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"ResetAlarmActionRequests"
        ~of_json:ResetAlarmActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchPutMessageErrorEntries =
  struct
    type nonrec t = BatchPutMessageErrorEntry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BatchPutMessageErrorEntry.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchPutMessageErrorEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchPutMessageErrorEntries"
        ~of_json:BatchPutMessageErrorEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module Messages =
  struct
    type nonrec t = Message.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:Message.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Message.of_xml)
    let of_json j = list_of_json ~kind:"Messages" ~of_json:Message.of_json j
    let to_json v = composed_to_json to_value v
  end
module EnableAlarmActionRequests =
  struct
    type nonrec t = EnableAlarmActionRequest.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:EnableAlarmActionRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnableAlarmActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"EnableAlarmActionRequests"
        ~of_json:EnableAlarmActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module DisableAlarmActionRequests =
  struct
    type nonrec t = DisableAlarmActionRequest.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:DisableAlarmActionRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DisableAlarmActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"DisableAlarmActionRequests"
        ~of_json:DisableAlarmActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module AcknowledgeAlarmActionRequests =
  struct
    type nonrec t = AcknowledgeAlarmActionRequest.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:AcknowledgeAlarmActionRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AcknowledgeAlarmActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"AcknowledgeAlarmActionRequests"
        ~of_json:AcknowledgeAlarmActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListDetectorsResponse =
  struct
    type nonrec t =
      {
      detectorSummaries: DetectorSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?detectorSummaries =
      fun ?nextToken -> fun () -> { detectorSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("detectorSummaries",
           (Option.map x.detectorSummaries ~f:DetectorSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let detectorSummaries =
        (Option.map ~f:DetectorSummaries.of_xml)
          (Xml.child xml_arg0 "detectorSummaries") in
      make ?nextToken ?detectorSummaries ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let detectorSummaries =
        field_map json "detectorSummaries" DetectorSummaries.of_json in
      make ?nextToken ?detectorSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListDetectorsRequest =
  struct
    type nonrec t =
      {
      detectorModelName: DetectorModelName.t ;
      stateName: StateName.t option ;
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListDetectorsRequest"
    let make ?stateName =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~detectorModelName ->
            fun () -> { stateName; nextToken; maxResults; detectorModelName }
    let to_value x =
      structure_to_value
        [("detectorModelName",
           (Some (DetectorModelName.to_value x.detectorModelName)));
        ("stateName", (Option.map x.stateName ~f:StateName.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let stateName =
        (Option.map ~f:StateName.of_xml) (Xml.child xml_arg0 "stateName") in
      let detectorModelName =
        DetectorModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "detectorModelName") in
      make ?maxResults ?nextToken ?stateName ~detectorModelName ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let stateName = field_map json "stateName" StateName.of_json in
      let detectorModelName =
        field_map_exn json "detectorModelName" DetectorModelName.of_json in
      make ?maxResults ?nextToken ?stateName ~detectorModelName ()
    let to_json v = composed_to_json to_value v
  end
module ListAlarmsResponse =
  struct
    type nonrec t =
      {
      alarmSummaries: AlarmSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?alarmSummaries =
      fun ?nextToken -> fun () -> { alarmSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("alarmSummaries",
           (Option.map x.alarmSummaries ~f:AlarmSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let alarmSummaries =
        (Option.map ~f:AlarmSummaries.of_xml)
          (Xml.child xml_arg0 "alarmSummaries") in
      make ?nextToken ?alarmSummaries ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let alarmSummaries =
        field_map json "alarmSummaries" AlarmSummaries.of_json in
      make ?nextToken ?alarmSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListAlarmsRequest =
  struct
    type nonrec t =
      {
      alarmModelName: AlarmModelName.t ;
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListAlarmsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~alarmModelName ->
          fun () -> { nextToken; maxResults; alarmModelName }
    let to_value x =
      structure_to_value
        [("alarmModelName",
           (Some (AlarmModelName.to_value x.alarmModelName)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      make ?maxResults ?nextToken ~alarmModelName ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      make ?maxResults ?nextToken ~alarmModelName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeDetectorResponse =
  struct
    type nonrec t = {
      detector: Detector.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?detector = fun () -> { detector }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("detector", (Option.map x.detector ~f:Detector.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detector =
        (Option.map ~f:Detector.of_xml) (Xml.child xml_arg0 "detector") in
      make ?detector ()
    let of_json json =
      let detector = field_map json "detector" Detector.of_json in
      make ?detector ()
    let to_json v = composed_to_json to_value v
  end
module DescribeDetectorRequest =
  struct
    type nonrec t =
      {
      detectorModelName: DetectorModelName.t ;
      keyValue: KeyValue.t option }
    let context_ = "DescribeDetectorRequest"
    let make ?keyValue =
      fun ~detectorModelName -> fun () -> { keyValue; detectorModelName }
    let to_value x =
      structure_to_value
        [("detectorModelName",
           (Some (DetectorModelName.to_value x.detectorModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let detectorModelName =
        DetectorModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "detectorModelName") in
      make ?keyValue ~detectorModelName ()
    let of_json json =
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let detectorModelName =
        field_map_exn json "detectorModelName" DetectorModelName.of_json in
      make ?keyValue ~detectorModelName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAlarmResponse =
  struct
    type nonrec t = {
      alarm: Alarm.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?alarm = fun () -> { alarm }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("alarm", (Option.map x.alarm ~f:Alarm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let alarm = (Option.map ~f:Alarm.of_xml) (Xml.child xml_arg0 "alarm") in
      make ?alarm ()
    let of_json json =
      let alarm = field_map json "alarm" Alarm.of_json in make ?alarm ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAlarmRequest =
  struct
    type nonrec t =
      {
      alarmModelName: AlarmModelName.t ;
      keyValue: KeyValue.t option }
    let context_ = "DescribeAlarmRequest"
    let make ?keyValue =
      fun ~alarmModelName -> fun () -> { keyValue; alarmModelName }
    let to_value x =
      structure_to_value
        [("alarmModelName",
           (Some (AlarmModelName.to_value x.alarmModelName)));
        ("keyValue", (Option.map x.keyValue ~f:KeyValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyValue =
        (Option.map ~f:KeyValue.of_xml) (Xml.child xml_arg0 "keyValue") in
      let alarmModelName =
        AlarmModelName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "alarmModelName") in
      make ?keyValue ~alarmModelName ()
    let of_json json =
      let keyValue = field_map json "keyValue" KeyValue.of_json in
      let alarmModelName =
        field_map_exn json "alarmModelName" AlarmModelName.of_json in
      make ?keyValue ~alarmModelName ()
    let to_json v = composed_to_json to_value v
  end
module BatchUpdateDetectorResponse =
  struct
    type nonrec t =
      {
      batchUpdateDetectorErrorEntries:
        BatchUpdateDetectorErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchUpdateDetectorErrorEntries =
      fun () -> { batchUpdateDetectorErrorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchUpdateDetectorErrorEntries",
           (Option.map x.batchUpdateDetectorErrorEntries
              ~f:BatchUpdateDetectorErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchUpdateDetectorErrorEntries =
        (Option.map ~f:BatchUpdateDetectorErrorEntries.of_xml)
          (Xml.child xml_arg0 "batchUpdateDetectorErrorEntries") in
      make ?batchUpdateDetectorErrorEntries ()
    let of_json json =
      let batchUpdateDetectorErrorEntries =
        field_map json "batchUpdateDetectorErrorEntries"
          BatchUpdateDetectorErrorEntries.of_json in
      make ?batchUpdateDetectorErrorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchUpdateDetectorRequest =
  struct
    type nonrec t = {
      detectors: UpdateDetectorRequests.t }
    let context_ = "BatchUpdateDetectorRequest"
    let make ~detectors = fun () -> { detectors }
    let to_value x =
      structure_to_value
        [("detectors", (Some (UpdateDetectorRequests.to_value x.detectors)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detectors =
        UpdateDetectorRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "detectors") in
      make ~detectors ()
    let of_json json =
      let detectors =
        field_map_exn json "detectors" UpdateDetectorRequests.of_json in
      make ~detectors ()
    let to_json v = composed_to_json to_value v
  end
module BatchSnoozeAlarmResponse =
  struct
    type nonrec t = {
      errorEntries: BatchAlarmActionErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errorEntries = fun () -> { errorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errorEntries",
           (Option.map x.errorEntries
              ~f:BatchAlarmActionErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorEntries =
        (Option.map ~f:BatchAlarmActionErrorEntries.of_xml)
          (Xml.child xml_arg0 "errorEntries") in
      make ?errorEntries ()
    let of_json json =
      let errorEntries =
        field_map json "errorEntries" BatchAlarmActionErrorEntries.of_json in
      make ?errorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchSnoozeAlarmRequest =
  struct
    type nonrec t = {
      snoozeActionRequests: SnoozeAlarmActionRequests.t }
    let context_ = "BatchSnoozeAlarmRequest"
    let make ~snoozeActionRequests = fun () -> { snoozeActionRequests }
    let to_value x =
      structure_to_value
        [("snoozeActionRequests",
           (Some (SnoozeAlarmActionRequests.to_value x.snoozeActionRequests)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snoozeActionRequests =
        SnoozeAlarmActionRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "snoozeActionRequests") in
      make ~snoozeActionRequests ()
    let of_json json =
      let snoozeActionRequests =
        field_map_exn json "snoozeActionRequests"
          SnoozeAlarmActionRequests.of_json in
      make ~snoozeActionRequests ()
    let to_json v = composed_to_json to_value v
  end
module BatchResetAlarmResponse =
  struct
    type nonrec t = {
      errorEntries: BatchAlarmActionErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errorEntries = fun () -> { errorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errorEntries",
           (Option.map x.errorEntries
              ~f:BatchAlarmActionErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorEntries =
        (Option.map ~f:BatchAlarmActionErrorEntries.of_xml)
          (Xml.child xml_arg0 "errorEntries") in
      make ?errorEntries ()
    let of_json json =
      let errorEntries =
        field_map json "errorEntries" BatchAlarmActionErrorEntries.of_json in
      make ?errorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchResetAlarmRequest =
  struct
    type nonrec t = {
      resetActionRequests: ResetAlarmActionRequests.t }
    let context_ = "BatchResetAlarmRequest"
    let make ~resetActionRequests = fun () -> { resetActionRequests }
    let to_value x =
      structure_to_value
        [("resetActionRequests",
           (Some (ResetAlarmActionRequests.to_value x.resetActionRequests)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resetActionRequests =
        ResetAlarmActionRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resetActionRequests") in
      make ~resetActionRequests ()
    let of_json json =
      let resetActionRequests =
        field_map_exn json "resetActionRequests"
          ResetAlarmActionRequests.of_json in
      make ~resetActionRequests ()
    let to_json v = composed_to_json to_value v
  end
module BatchPutMessageResponse =
  struct
    type nonrec t =
      {
      batchPutMessageErrorEntries: BatchPutMessageErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchPutMessageErrorEntries =
      fun () -> { batchPutMessageErrorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BatchPutMessageErrorEntries",
           (Option.map x.batchPutMessageErrorEntries
              ~f:BatchPutMessageErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchPutMessageErrorEntries =
        (Option.map ~f:BatchPutMessageErrorEntries.of_xml)
          (Xml.child xml_arg0 "BatchPutMessageErrorEntries") in
      make ?batchPutMessageErrorEntries ()
    let of_json json =
      let batchPutMessageErrorEntries =
        field_map json "BatchPutMessageErrorEntries"
          BatchPutMessageErrorEntries.of_json in
      make ?batchPutMessageErrorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchPutMessageRequest =
  struct
    type nonrec t = {
      messages: Messages.t }
    let context_ = "BatchPutMessageRequest"
    let make ~messages = fun () -> { messages }
    let to_value x =
      structure_to_value
        [("messages", (Some (Messages.to_value x.messages)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let messages =
        Messages.of_xml (Xml.child_exn ~context:context_ xml_arg0 "messages") in
      make ~messages ()
    let of_json json =
      let messages = field_map_exn json "messages" Messages.of_json in
      make ~messages ()
    let to_json v = composed_to_json to_value v
  end
module BatchEnableAlarmResponse =
  struct
    type nonrec t = {
      errorEntries: BatchAlarmActionErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errorEntries = fun () -> { errorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errorEntries",
           (Option.map x.errorEntries
              ~f:BatchAlarmActionErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorEntries =
        (Option.map ~f:BatchAlarmActionErrorEntries.of_xml)
          (Xml.child xml_arg0 "errorEntries") in
      make ?errorEntries ()
    let of_json json =
      let errorEntries =
        field_map json "errorEntries" BatchAlarmActionErrorEntries.of_json in
      make ?errorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchEnableAlarmRequest =
  struct
    type nonrec t = {
      enableActionRequests: EnableAlarmActionRequests.t }
    let context_ = "BatchEnableAlarmRequest"
    let make ~enableActionRequests = fun () -> { enableActionRequests }
    let to_value x =
      structure_to_value
        [("enableActionRequests",
           (Some (EnableAlarmActionRequests.to_value x.enableActionRequests)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableActionRequests =
        EnableAlarmActionRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "enableActionRequests") in
      make ~enableActionRequests ()
    let of_json json =
      let enableActionRequests =
        field_map_exn json "enableActionRequests"
          EnableAlarmActionRequests.of_json in
      make ~enableActionRequests ()
    let to_json v = composed_to_json to_value v
  end
module BatchDisableAlarmResponse =
  struct
    type nonrec t = {
      errorEntries: BatchAlarmActionErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errorEntries = fun () -> { errorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errorEntries",
           (Option.map x.errorEntries
              ~f:BatchAlarmActionErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorEntries =
        (Option.map ~f:BatchAlarmActionErrorEntries.of_xml)
          (Xml.child xml_arg0 "errorEntries") in
      make ?errorEntries ()
    let of_json json =
      let errorEntries =
        field_map json "errorEntries" BatchAlarmActionErrorEntries.of_json in
      make ?errorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchDisableAlarmRequest =
  struct
    type nonrec t = {
      disableActionRequests: DisableAlarmActionRequests.t }
    let context_ = "BatchDisableAlarmRequest"
    let make ~disableActionRequests = fun () -> { disableActionRequests }
    let to_value x =
      structure_to_value
        [("disableActionRequests",
           (Some
              (DisableAlarmActionRequests.to_value x.disableActionRequests)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let disableActionRequests =
        DisableAlarmActionRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "disableActionRequests") in
      make ~disableActionRequests ()
    let of_json json =
      let disableActionRequests =
        field_map_exn json "disableActionRequests"
          DisableAlarmActionRequests.of_json in
      make ~disableActionRequests ()
    let to_json v = composed_to_json to_value v
  end
module BatchAcknowledgeAlarmResponse =
  struct
    type nonrec t = {
      errorEntries: BatchAlarmActionErrorEntries.t option }
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errorEntries = fun () -> { errorEntries }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errorEntries",
           (Option.map x.errorEntries
              ~f:BatchAlarmActionErrorEntries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorEntries =
        (Option.map ~f:BatchAlarmActionErrorEntries.of_xml)
          (Xml.child xml_arg0 "errorEntries") in
      make ?errorEntries ()
    let of_json json =
      let errorEntries =
        field_map json "errorEntries" BatchAlarmActionErrorEntries.of_json in
      make ?errorEntries ()
    let to_json v = composed_to_json to_value v
  end
module BatchAcknowledgeAlarmRequest =
  struct
    type nonrec t =
      {
      acknowledgeActionRequests: AcknowledgeAlarmActionRequests.t }
    let context_ = "BatchAcknowledgeAlarmRequest"
    let make ~acknowledgeActionRequests =
      fun () -> { acknowledgeActionRequests }
    let to_value x =
      structure_to_value
        [("acknowledgeActionRequests",
           (Some
              (AcknowledgeAlarmActionRequests.to_value
                 x.acknowledgeActionRequests)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acknowledgeActionRequests =
        AcknowledgeAlarmActionRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "acknowledgeActionRequests") in
      make ~acknowledgeActionRequests ()
    let of_json json =
      let acknowledgeActionRequests =
        field_map_exn json "acknowledgeActionRequests"
          AcknowledgeAlarmActionRequests.of_json in
      make ~acknowledgeActionRequests ()
    let to_json v = composed_to_json to_value v
  end
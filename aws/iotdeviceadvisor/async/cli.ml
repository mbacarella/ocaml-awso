(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_suite_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionConfiguration =
         flag "suite-definition-configuration" (optional json_arg)
           ~doc:"JSON SuiteDefinitionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_suite_definition
           (Values.CreateSuiteDefinitionRequest.make
              ?suiteDefinitionConfiguration:(Option.map
                                               ~f:Values.SuiteDefinitionConfiguration.of_json
                                               suiteDefinitionConfiguration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ())
           (Some Values.CreateSuiteDefinitionResponse.to_json)
           (Some Values.CreateSuiteDefinitionResponse.error_to_json)])
let delete_suite_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_suite_definition
           (Values.DeleteSuiteDefinitionRequest.make ~suiteDefinitionId ())
           (Some Values.DeleteSuiteDefinitionResponse.to_json)
           (Some Values.DeleteSuiteDefinitionResponse.error_to_json)])
let get_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingArn =
         flag "thing-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and certificateArn =
         flag "certificate-arn" (optional string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_endpoint
           (Values.GetEndpointRequest.make ?thingArn ?certificateArn ())
           (Some Values.GetEndpointResponse.to_json)
           (Some Values.GetEndpointResponse.error_to_json)])
let get_suite_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionVersion =
         flag "suite-definition-version" (optional string)
           ~doc:"STRING SuiteDefinitionVersion"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_suite_definition
           (Values.GetSuiteDefinitionRequest.make ?suiteDefinitionVersion
              ~suiteDefinitionId ())
           (Some Values.GetSuiteDefinitionResponse.to_json)
           (Some Values.GetSuiteDefinitionResponse.error_to_json)])
let get_suite_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID"
       and suiteRunId =
         flag "suite-run-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_suite_run
           (Values.GetSuiteRunRequest.make ~suiteDefinitionId ~suiteRunId ())
           (Some Values.GetSuiteRunResponse.to_json)
           (Some Values.GetSuiteRunResponse.error_to_json)])
let get_suite_run_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID"
       and suiteRunId =
         flag "suite-run-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_suite_run_report
           (Values.GetSuiteRunReportRequest.make ~suiteDefinitionId
              ~suiteRunId ()) (Some Values.GetSuiteRunReportResponse.to_json)
           (Some Values.GetSuiteRunReportResponse.error_to_json)])
let list_suite_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_suite_definitions
           (Values.ListSuiteDefinitionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListSuiteDefinitionsResponse.to_json)
           (Some Values.ListSuiteDefinitionsResponse.error_to_json)])
let list_suite_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionId =
         flag "suite-definition-id" (optional string) ~doc:"STRING UUID"
       and suiteDefinitionVersion =
         flag "suite-definition-version" (optional string)
           ~doc:"STRING SuiteDefinitionVersion"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_suite_runs
           (Values.ListSuiteRunsRequest.make ?suiteDefinitionId
              ?suiteDefinitionVersion ?maxResults ?nextToken ())
           (Some Values.ListSuiteRunsResponse.to_json)
           (Some Values.ListSuiteRunsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_suite_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionVersion =
         flag "suite-definition-version" (optional string)
           ~doc:"STRING SuiteDefinitionVersion"
       and suiteRunConfiguration =
         flag "suite-run-configuration" (optional json_arg)
           ~doc:"JSON SuiteRunConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_suite_run
           (Values.StartSuiteRunRequest.make ?suiteDefinitionVersion
              ?suiteRunConfiguration:(Option.map
                                        ~f:Values.SuiteRunConfiguration.of_json
                                        suiteRunConfiguration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~suiteDefinitionId ())
           (Some Values.StartSuiteRunResponse.to_json)
           (Some Values.StartSuiteRunResponse.error_to_json)])
let stop_suite_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID"
       and suiteRunId =
         flag "suite-run-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_suite_run
           (Values.StopSuiteRunRequest.make ~suiteDefinitionId ~suiteRunId ())
           (Some Values.StopSuiteRunResponse.to_json)
           (Some Values.StopSuiteRunResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_suite_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suiteDefinitionConfiguration =
         flag "suite-definition-configuration" (optional json_arg)
           ~doc:"JSON SuiteDefinitionConfiguration"
       and suiteDefinitionId =
         flag "suite-definition-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_suite_definition
           (Values.UpdateSuiteDefinitionRequest.make
              ?suiteDefinitionConfiguration:(Option.map
                                               ~f:Values.SuiteDefinitionConfiguration.of_json
                                               suiteDefinitionConfiguration)
              ~suiteDefinitionId ())
           (Some Values.UpdateSuiteDefinitionResponse.to_json)
           (Some Values.UpdateSuiteDefinitionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-suite-definition", create_suite_definition);
    ("delete-suite-definition", delete_suite_definition);
    ("get-endpoint", get_endpoint);
    ("get-suite-definition", get_suite_definition);
    ("get-suite-run", get_suite_run);
    ("get-suite-run-report", get_suite_run_report);
    ("list-suite-definitions", list_suite_definitions);
    ("list-suite-runs", list_suite_runs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-suite-run", start_suite_run);
    ("stop-suite-run", stop_suite_run);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-suite-definition", update_suite_definition)]
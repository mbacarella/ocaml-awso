(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_evaluate_feature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef"
       and requests =
         flag "requests" (required json_arg)
           ~doc:"JSON EvaluationRequestsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_evaluate_feature
           (Values.BatchEvaluateFeatureRequest.make ~project
              ~requests:(Values.EvaluationRequestsList.of_json requests) ())
           (Some Values.BatchEvaluateFeatureResponse.to_json)
           (Some Values.BatchEvaluateFeatureResponse.error_to_json)])
let create_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and onlineAbConfig =
         flag "online-ab-config" (optional json_arg)
           ~doc:"JSON OnlineAbConfig"
       and randomizationSalt =
         flag "randomization-salt" (optional string)
           ~doc:"STRING RandomizationSalt"
       and samplingRate =
         flag "sampling-rate" (optional json_arg) ~doc:"JSON SplitWeight"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and metricGoals =
         flag "metric-goals" (required json_arg)
           ~doc:"JSON MetricGoalConfigList"
       and name = flag "name" (required string) ~doc:"STRING ExperimentName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef"
       and treatments =
         flag "treatments" (required json_arg)
           ~doc:"JSON TreatmentConfigList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_experiment
           (Values.CreateExperimentRequest.make ?description
              ?onlineAbConfig:(Option.map ~f:Values.OnlineAbConfig.of_json
                                 onlineAbConfig) ?randomizationSalt
              ?samplingRate:(Option.map ~f:Values.SplitWeight.of_json
                               samplingRate)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~metricGoals:(Values.MetricGoalConfigList.of_json metricGoals)
              ~name ~project
              ~treatments:(Values.TreatmentConfigList.of_json treatments) ())
           (Some Values.CreateExperimentResponse.to_json)
           (Some Values.CreateExperimentResponse.error_to_json)])
let create_feature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultVariation =
         flag "default-variation" (optional string)
           ~doc:"STRING VariationName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and entityOverrides =
         flag "entity-overrides" (optional json_arg)
           ~doc:"JSON EntityOverrideMap"
       and evaluationStrategy =
         flag "evaluation-strategy" (optional json_arg)
           ~doc:"JSON FeatureEvaluationStrategy"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING FeatureName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef"
       and variations =
         flag "variations" (required json_arg)
           ~doc:"JSON VariationConfigsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_feature
           (Values.CreateFeatureRequest.make ?defaultVariation ?description
              ?entityOverrides:(Option.map
                                  ~f:Values.EntityOverrideMap.of_json
                                  entityOverrides)
              ?evaluationStrategy:(Option.map
                                     ~f:Values.FeatureEvaluationStrategy.of_json
                                     evaluationStrategy)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ~project
              ~variations:(Values.VariationConfigsList.of_json variations) ())
           (Some Values.CreateFeatureResponse.to_json)
           (Some Values.CreateFeatureResponse.error_to_json)])
let create_launch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and metricMonitors =
         flag "metric-monitors" (optional json_arg)
           ~doc:"JSON MetricMonitorConfigList"
       and randomizationSalt =
         flag "randomization-salt" (optional string)
           ~doc:"STRING RandomizationSalt"
       and scheduledSplitsConfig =
         flag "scheduled-splits-config" (optional json_arg)
           ~doc:"JSON ScheduledSplitsLaunchConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and groups =
         flag "groups" (required json_arg) ~doc:"JSON LaunchGroupConfigList"
       and name = flag "name" (required string) ~doc:"STRING LaunchName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_launch
           (Values.CreateLaunchRequest.make ?description
              ?metricMonitors:(Option.map
                                 ~f:Values.MetricMonitorConfigList.of_json
                                 metricMonitors) ?randomizationSalt
              ?scheduledSplitsConfig:(Option.map
                                        ~f:Values.ScheduledSplitsLaunchConfig.of_json
                                        scheduledSplitsConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~groups:(Values.LaunchGroupConfigList.of_json groups) ~name
              ~project ()) (Some Values.CreateLaunchResponse.to_json)
           (Some Values.CreateLaunchResponse.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataDelivery =
         flag "data-delivery" (optional json_arg)
           ~doc:"JSON ProjectDataDeliveryConfig"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make
              ?dataDelivery:(Option.map
                               ~f:Values.ProjectDataDeliveryConfig.of_json
                               dataDelivery) ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreateProjectResponse.to_json)
           (Some Values.CreateProjectResponse.error_to_json)])
let delete_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and experiment =
         flag "experiment" (required string) ~doc:"STRING ExperimentName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_experiment
           (Values.DeleteExperimentRequest.make ~experiment ~project ())
           (Some Values.DeleteExperimentResponse.to_json)
           (Some Values.DeleteExperimentResponse.error_to_json)])
let delete_feature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and feature =
         flag "feature" (required string) ~doc:"STRING FeatureName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_feature
           (Values.DeleteFeatureRequest.make ~feature ~project ())
           (Some Values.DeleteFeatureResponse.to_json)
           (Some Values.DeleteFeatureResponse.error_to_json)])
let delete_launch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launch = flag "launch" (required string) ~doc:"STRING LaunchName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch
           (Values.DeleteLaunchRequest.make ~launch ~project ())
           (Some Values.DeleteLaunchResponse.to_json)
           (Some Values.DeleteLaunchResponse.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project (Values.DeleteProjectRequest.make ~project ())
           (Some Values.DeleteProjectResponse.to_json)
           (Some Values.DeleteProjectResponse.error_to_json)])
let evaluate_feature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluationContext =
         flag "evaluation-context" (optional string) ~doc:"STRING JsonValue"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityId"
       and feature =
         flag "feature" (required string) ~doc:"STRING FeatureName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.evaluate_feature
           (Values.EvaluateFeatureRequest.make ?evaluationContext ~entityId
              ~feature ~project ())
           (Some Values.EvaluateFeatureResponse.to_json)
           (Some Values.EvaluateFeatureResponse.error_to_json)])
let get_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and experiment =
         flag "experiment" (required string) ~doc:"STRING ExperimentName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_experiment
           (Values.GetExperimentRequest.make ~experiment ~project ())
           (Some Values.GetExperimentResponse.to_json)
           (Some Values.GetExperimentResponse.error_to_json)])
let get_experiment_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baseStat =
         flag "base-stat" (optional json_arg) ~doc:"JSON ExperimentBaseStat"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and period =
         flag "period" (optional json_arg) ~doc:"JSON ResultsPeriod"
       and reportNames =
         flag "report-names" (optional json_arg)
           ~doc:"JSON ExperimentReportNameList"
       and resultStats =
         flag "result-stats" (optional json_arg)
           ~doc:"JSON ExperimentResultRequestTypeList"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and experiment =
         flag "experiment" (required string) ~doc:"STRING ExperimentName"
       and metricNames =
         flag "metric-names" (required json_arg) ~doc:"JSON MetricNameList"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef"
       and treatmentNames =
         flag "treatment-names" (required json_arg)
           ~doc:"JSON TreatmentNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_experiment_results
           (Values.GetExperimentResultsRequest.make
              ?baseStat:(Option.map ~f:Values.ExperimentBaseStat.of_json
                           baseStat)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?period:(Option.map ~f:Values.ResultsPeriod.of_json period)
              ?reportNames:(Option.map
                              ~f:Values.ExperimentReportNameList.of_json
                              reportNames)
              ?resultStats:(Option.map
                              ~f:Values.ExperimentResultRequestTypeList.of_json
                              resultStats)
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ~experiment
              ~metricNames:(Values.MetricNameList.of_json metricNames)
              ~project
              ~treatmentNames:(Values.TreatmentNameList.of_json
                                 treatmentNames) ())
           (Some Values.GetExperimentResultsResponse.to_json)
           (Some Values.GetExperimentResultsResponse.error_to_json)])
let get_feature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and feature =
         flag "feature" (required string) ~doc:"STRING FeatureName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_feature
           (Values.GetFeatureRequest.make ~feature ~project ())
           (Some Values.GetFeatureResponse.to_json)
           (Some Values.GetFeatureResponse.error_to_json)])
let get_launch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launch = flag "launch" (required string) ~doc:"STRING LaunchName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_launch (Values.GetLaunchRequest.make ~launch ~project ())
           (Some Values.GetLaunchResponse.to_json)
           (Some Values.GetLaunchResponse.error_to_json)])
let get_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_project (Values.GetProjectRequest.make ~project ())
           (Some Values.GetProjectResponse.to_json)
           (Some Values.GetProjectResponse.error_to_json)])
let list_experiments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxExperiments"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ExperimentStatus"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_experiments
           (Values.ListExperimentsRequest.make ?maxResults ?nextToken
              ?status:(Option.map ~f:Values.ExperimentStatus.of_json status)
              ~project ()) (Some Values.ListExperimentsResponse.to_json)
           (Some Values.ListExperimentsResponse.error_to_json)])
let list_features =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxFeatures"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_features
           (Values.ListFeaturesRequest.make ?maxResults ?nextToken ~project
              ()) (Some Values.ListFeaturesResponse.to_json)
           (Some Values.ListFeaturesResponse.error_to_json)])
let list_launches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxLaunches"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON LaunchStatus"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_launches
           (Values.ListLaunchesRequest.make ?maxResults ?nextToken
              ?status:(Option.map ~f:Values.LaunchStatus.of_json status)
              ~project ()) (Some Values.ListLaunchesResponse.to_json)
           (Some Values.ListLaunchesResponse.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxProjects"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListProjectsResponse.to_json)
           (Some Values.ListProjectsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_project_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and events = flag "events" (required json_arg) ~doc:"JSON EventList"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_project_events
           (Values.PutProjectEventsRequest.make
              ~events:(Values.EventList.of_json events) ~project ())
           (Some Values.PutProjectEventsResponse.to_json)
           (Some Values.PutProjectEventsResponse.error_to_json)])
let start_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analysisCompleteTime =
         flag "analysis-complete-time" (required json_arg)
           ~doc:"JSON Timestamp"
       and experiment =
         flag "experiment" (required string) ~doc:"STRING ExperimentName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_experiment
           (Values.StartExperimentRequest.make
              ~analysisCompleteTime:(Values.Timestamp.of_json
                                       analysisCompleteTime) ~experiment
              ~project ()) (Some Values.StartExperimentResponse.to_json)
           (Some Values.StartExperimentResponse.error_to_json)])
let start_launch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launch = flag "launch" (required string) ~doc:"STRING LaunchName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_launch
           (Values.StartLaunchRequest.make ~launch ~project ())
           (Some Values.StartLaunchResponse.to_json)
           (Some Values.StartLaunchResponse.error_to_json)])
let stop_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and desiredState =
         flag "desired-state" (optional json_arg)
           ~doc:"JSON ExperimentStopDesiredState"
       and reason = flag "reason" (optional string) ~doc:"STRING Description"
       and experiment =
         flag "experiment" (required string) ~doc:"STRING ExperimentName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_experiment
           (Values.StopExperimentRequest.make
              ?desiredState:(Option.map
                               ~f:Values.ExperimentStopDesiredState.of_json
                               desiredState) ?reason ~experiment ~project ())
           (Some Values.StopExperimentResponse.to_json)
           (Some Values.StopExperimentResponse.error_to_json)])
let stop_launch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and desiredState =
         flag "desired-state" (optional json_arg)
           ~doc:"JSON LaunchStopDesiredState"
       and reason = flag "reason" (optional string) ~doc:"STRING Description"
       and launch = flag "launch" (required string) ~doc:"STRING LaunchName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_launch
           (Values.StopLaunchRequest.make
              ?desiredState:(Option.map
                               ~f:Values.LaunchStopDesiredState.of_json
                               desiredState) ?reason ~launch ~project ())
           (Some Values.StopLaunchResponse.to_json)
           (Some Values.StopLaunchResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and metricGoals =
         flag "metric-goals" (optional json_arg)
           ~doc:"JSON MetricGoalConfigList"
       and onlineAbConfig =
         flag "online-ab-config" (optional json_arg)
           ~doc:"JSON OnlineAbConfig"
       and randomizationSalt =
         flag "randomization-salt" (optional string)
           ~doc:"STRING RandomizationSalt"
       and samplingRate =
         flag "sampling-rate" (optional json_arg) ~doc:"JSON SplitWeight"
       and treatments =
         flag "treatments" (optional json_arg)
           ~doc:"JSON TreatmentConfigList"
       and experiment =
         flag "experiment" (required string) ~doc:"STRING ExperimentName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_experiment
           (Values.UpdateExperimentRequest.make ?description
              ?metricGoals:(Option.map ~f:Values.MetricGoalConfigList.of_json
                              metricGoals)
              ?onlineAbConfig:(Option.map ~f:Values.OnlineAbConfig.of_json
                                 onlineAbConfig) ?randomizationSalt
              ?samplingRate:(Option.map ~f:Values.SplitWeight.of_json
                               samplingRate)
              ?treatments:(Option.map ~f:Values.TreatmentConfigList.of_json
                             treatments) ~experiment ~project ())
           (Some Values.UpdateExperimentResponse.to_json)
           (Some Values.UpdateExperimentResponse.error_to_json)])
let update_feature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addOrUpdateVariations =
         flag "add-or-update-variations" (optional json_arg)
           ~doc:"JSON VariationConfigsList"
       and defaultVariation =
         flag "default-variation" (optional string)
           ~doc:"STRING VariationName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and entityOverrides =
         flag "entity-overrides" (optional json_arg)
           ~doc:"JSON EntityOverrideMap"
       and evaluationStrategy =
         flag "evaluation-strategy" (optional json_arg)
           ~doc:"JSON FeatureEvaluationStrategy"
       and removeVariations =
         flag "remove-variations" (optional json_arg)
           ~doc:"JSON VariationNameList"
       and feature =
         flag "feature" (required string) ~doc:"STRING FeatureName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_feature
           (Values.UpdateFeatureRequest.make
              ?addOrUpdateVariations:(Option.map
                                        ~f:Values.VariationConfigsList.of_json
                                        addOrUpdateVariations)
              ?defaultVariation ?description
              ?entityOverrides:(Option.map
                                  ~f:Values.EntityOverrideMap.of_json
                                  entityOverrides)
              ?evaluationStrategy:(Option.map
                                     ~f:Values.FeatureEvaluationStrategy.of_json
                                     evaluationStrategy)
              ?removeVariations:(Option.map
                                   ~f:Values.VariationNameList.of_json
                                   removeVariations) ~feature ~project ())
           (Some Values.UpdateFeatureResponse.to_json)
           (Some Values.UpdateFeatureResponse.error_to_json)])
let update_launch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and groups =
         flag "groups" (optional json_arg) ~doc:"JSON LaunchGroupConfigList"
       and metricMonitors =
         flag "metric-monitors" (optional json_arg)
           ~doc:"JSON MetricMonitorConfigList"
       and randomizationSalt =
         flag "randomization-salt" (optional string)
           ~doc:"STRING RandomizationSalt"
       and scheduledSplitsConfig =
         flag "scheduled-splits-config" (optional json_arg)
           ~doc:"JSON ScheduledSplitsLaunchConfig"
       and launch = flag "launch" (required string) ~doc:"STRING LaunchName"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_launch
           (Values.UpdateLaunchRequest.make ?description
              ?groups:(Option.map ~f:Values.LaunchGroupConfigList.of_json
                         groups)
              ?metricMonitors:(Option.map
                                 ~f:Values.MetricMonitorConfigList.of_json
                                 metricMonitors) ?randomizationSalt
              ?scheduledSplitsConfig:(Option.map
                                        ~f:Values.ScheduledSplitsLaunchConfig.of_json
                                        scheduledSplitsConfig) ~launch
              ~project ()) (Some Values.UpdateLaunchResponse.to_json)
           (Some Values.UpdateLaunchResponse.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make ?description ~project ())
           (Some Values.UpdateProjectResponse.to_json)
           (Some Values.UpdateProjectResponse.error_to_json)])
let update_project_data_delivery =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchLogs =
         flag "cloud-watch-logs" (optional json_arg)
           ~doc:"JSON CloudWatchLogsDestinationConfig"
       and s3Destination =
         flag "s3-destination" (optional json_arg)
           ~doc:"JSON S3DestinationConfig"
       and project =
         flag "project" (required string) ~doc:"STRING ProjectRef" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project_data_delivery
           (Values.UpdateProjectDataDeliveryRequest.make
              ?cloudWatchLogs:(Option.map
                                 ~f:Values.CloudWatchLogsDestinationConfig.of_json
                                 cloudWatchLogs)
              ?s3Destination:(Option.map
                                ~f:Values.S3DestinationConfig.of_json
                                s3Destination) ~project ())
           (Some Values.UpdateProjectDataDeliveryResponse.to_json)
           (Some Values.UpdateProjectDataDeliveryResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-evaluate-feature", batch_evaluate_feature);
    ("create-experiment", create_experiment);
    ("create-feature", create_feature);
    ("create-launch", create_launch);
    ("create-project", create_project);
    ("delete-experiment", delete_experiment);
    ("delete-feature", delete_feature);
    ("delete-launch", delete_launch);
    ("delete-project", delete_project);
    ("evaluate-feature", evaluate_feature);
    ("get-experiment", get_experiment);
    ("get-experiment-results", get_experiment_results);
    ("get-feature", get_feature);
    ("get-launch", get_launch);
    ("get-project", get_project);
    ("list-experiments", list_experiments);
    ("list-features", list_features);
    ("list-launches", list_launches);
    ("list-projects", list_projects);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-project-events", put_project_events);
    ("start-experiment", start_experiment);
    ("start-launch", start_launch);
    ("stop-experiment", stop_experiment);
    ("stop-launch", stop_launch);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-experiment", update_experiment);
    ("update-feature", update_feature);
    ("update-launch", update_launch);
    ("update-project", update_project);
    ("update-project-data-delivery", update_project_data_delivery)]
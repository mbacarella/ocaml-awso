(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let attach_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIds"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_instances
           (Values.AttachInstancesQuery.make
              ?instanceIds:(Option.map ~f:Values.InstanceIds.of_json
                              instanceIds) ~autoScalingGroupName ()) None
           None])
let attach_load_balancer_target_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and targetGroupARNs =
         flag "target-group-a-r-ns" (required json_arg)
           ~doc:"JSON TargetGroupARNs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_load_balancer_target_groups
           (Values.AttachLoadBalancerTargetGroupsType.make
              ~autoScalingGroupName
              ~targetGroupARNs:(Values.TargetGroupARNs.of_json
                                  targetGroupARNs) ())
           (Some Values.AttachLoadBalancerTargetGroupsResultType.to_json)
           (Some
              Values.AttachLoadBalancerTargetGroupsResultType.error_to_json)])
let attach_load_balancers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and loadBalancerNames =
         flag "load-balancer-names" (required json_arg)
           ~doc:"JSON LoadBalancerNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_load_balancers
           (Values.AttachLoadBalancersType.make ~autoScalingGroupName
              ~loadBalancerNames:(Values.LoadBalancerNames.of_json
                                    loadBalancerNames) ())
           (Some Values.AttachLoadBalancersResultType.to_json)
           (Some Values.AttachLoadBalancersResultType.error_to_json)])
let batch_delete_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and scheduledActionNames =
         flag "scheduled-action-names" (required json_arg)
           ~doc:"JSON ScheduledActionNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_scheduled_action
           (Values.BatchDeleteScheduledActionType.make ~autoScalingGroupName
              ~scheduledActionNames:(Values.ScheduledActionNames.of_json
                                       scheduledActionNames) ())
           (Some Values.BatchDeleteScheduledActionAnswer.to_json)
           (Some Values.BatchDeleteScheduledActionAnswer.error_to_json)])
let batch_put_scheduled_update_group_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and scheduledUpdateGroupActions =
         flag "scheduled-update-group-actions" (required json_arg)
           ~doc:"JSON ScheduledUpdateGroupActionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_scheduled_update_group_action
           (Values.BatchPutScheduledUpdateGroupActionType.make
              ~autoScalingGroupName
              ~scheduledUpdateGroupActions:(Values.ScheduledUpdateGroupActionRequests.of_json
                                              scheduledUpdateGroupActions) ())
           (Some Values.BatchPutScheduledUpdateGroupActionAnswer.to_json)
           (Some
              Values.BatchPutScheduledUpdateGroupActionAnswer.error_to_json)])
let cancel_instance_refresh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_instance_refresh
           (Values.CancelInstanceRefreshType.make ~autoScalingGroupName ())
           (Some Values.CancelInstanceRefreshAnswer.to_json)
           (Some Values.CancelInstanceRefreshAnswer.error_to_json)])
let complete_lifecycle_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lifecycleActionToken =
         flag "lifecycle-action-token" (optional string)
           ~doc:"STRING LifecycleActionToken"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING XmlStringMaxLen19"
       and lifecycleHookName =
         flag "lifecycle-hook-name" (required string)
           ~doc:"STRING AsciiStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING ResourceName"
       and lifecycleActionResult =
         flag "lifecycle-action-result" (required string)
           ~doc:"STRING LifecycleActionResult" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_lifecycle_action
           (Values.CompleteLifecycleActionType.make ?lifecycleActionToken
              ?instanceId ~lifecycleHookName ~autoScalingGroupName
              ~lifecycleActionResult ())
           (Some Values.CompleteLifecycleActionAnswer.to_json)
           (Some Values.CompleteLifecycleActionAnswer.error_to_json)])
let create_auto_scaling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchConfigurationName =
         flag "launch-configuration-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and launchTemplate =
         flag "launch-template" (optional json_arg)
           ~doc:"JSON LaunchTemplateSpecification"
       and mixedInstancesPolicy =
         flag "mixed-instances-policy" (optional json_arg)
           ~doc:"JSON MixedInstancesPolicy"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING XmlStringMaxLen19"
       and desiredCapacity =
         flag "desired-capacity" (optional int)
           ~doc:"INT AutoScalingGroupDesiredCapacity"
       and defaultCooldown =
         flag "default-cooldown" (optional int) ~doc:"INT Cooldown"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZones"
       and loadBalancerNames =
         flag "load-balancer-names" (optional json_arg)
           ~doc:"JSON LoadBalancerNames"
       and targetGroupARNs =
         flag "target-group-a-r-ns" (optional json_arg)
           ~doc:"JSON TargetGroupARNs"
       and healthCheckType =
         flag "health-check-type" (optional string)
           ~doc:"STRING XmlStringMaxLen32"
       and healthCheckGracePeriod =
         flag "health-check-grace-period" (optional int)
           ~doc:"INT HealthCheckGracePeriod"
       and placementGroup =
         flag "placement-group" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and vPCZoneIdentifier =
         flag "v-p-c-zone-identifier" (optional string)
           ~doc:"STRING XmlStringMaxLen2047"
       and terminationPolicies =
         flag "termination-policies" (optional json_arg)
           ~doc:"JSON TerminationPolicies"
       and newInstancesProtectedFromScaleIn =
         flag "new-instances-protected-from-scale-in" (optional bool)
           ~doc:"BOOL InstanceProtected"
       and capacityRebalance =
         flag "capacity-rebalance" (optional bool)
           ~doc:"BOOL CapacityRebalanceEnabled"
       and lifecycleHookSpecificationList =
         flag "lifecycle-hook-specification-list" (optional json_arg)
           ~doc:"JSON LifecycleHookSpecifications"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and serviceLinkedRoleARN =
         flag "service-linked-role-a-r-n" (optional string)
           ~doc:"STRING ResourceName"
       and maxInstanceLifetime =
         flag "max-instance-lifetime" (optional int)
           ~doc:"INT MaxInstanceLifetime"
       and context = flag "context" (optional string) ~doc:"STRING Context"
       and desiredCapacityType =
         flag "desired-capacity-type" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and minSize =
         flag "min-size" (required int) ~doc:"INT AutoScalingGroupMinSize"
       and maxSize =
         flag "max-size" (required int) ~doc:"INT AutoScalingGroupMaxSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_auto_scaling_group
           (Values.CreateAutoScalingGroupType.make ?launchConfigurationName
              ?launchTemplate:(Option.map
                                 ~f:Values.LaunchTemplateSpecification.of_json
                                 launchTemplate)
              ?mixedInstancesPolicy:(Option.map
                                       ~f:Values.MixedInstancesPolicy.of_json
                                       mixedInstancesPolicy) ?instanceId
              ?desiredCapacity ?defaultCooldown
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZones.of_json
                                    availabilityZones)
              ?loadBalancerNames:(Option.map
                                    ~f:Values.LoadBalancerNames.of_json
                                    loadBalancerNames)
              ?targetGroupARNs:(Option.map ~f:Values.TargetGroupARNs.of_json
                                  targetGroupARNs) ?healthCheckType
              ?healthCheckGracePeriod ?placementGroup ?vPCZoneIdentifier
              ?terminationPolicies:(Option.map
                                      ~f:Values.TerminationPolicies.of_json
                                      terminationPolicies)
              ?newInstancesProtectedFromScaleIn ?capacityRebalance
              ?lifecycleHookSpecificationList:(Option.map
                                                 ~f:Values.LifecycleHookSpecifications.of_json
                                                 lifecycleHookSpecificationList)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?serviceLinkedRoleARN ?maxInstanceLifetime ?context
              ?desiredCapacityType ~autoScalingGroupName ~minSize ~maxSize ())
           None None])
let create_launch_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageId =
         flag "image-id" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and keyName =
         flag "key-name" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON SecurityGroups"
       and classicLinkVPCId =
         flag "classic-link-v-p-c-id" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and classicLinkVPCSecurityGroups =
         flag "classic-link-v-p-c-security-groups" (optional json_arg)
           ~doc:"JSON ClassicLinkVPCSecurityGroups"
       and userData =
         flag "user-data" (optional string) ~doc:"STRING XmlStringUserData"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING XmlStringMaxLen19"
       and instanceType =
         flag "instance-type" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and kernelId =
         flag "kernel-id" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and ramdiskId =
         flag "ramdisk-id" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and blockDeviceMappings =
         flag "block-device-mappings" (optional json_arg)
           ~doc:"JSON BlockDeviceMappings"
       and instanceMonitoring =
         flag "instance-monitoring" (optional json_arg)
           ~doc:"JSON InstanceMonitoring"
       and spotPrice =
         flag "spot-price" (optional string) ~doc:"STRING SpotPrice"
       and iamInstanceProfile =
         flag "iam-instance-profile" (optional string)
           ~doc:"STRING XmlStringMaxLen1600"
       and ebsOptimized =
         flag "ebs-optimized" (optional bool) ~doc:"BOOL EbsOptimized"
       and associatePublicIpAddress =
         flag "associate-public-ip-address" (optional bool)
           ~doc:"BOOL AssociatePublicIpAddress"
       and placementTenancy =
         flag "placement-tenancy" (optional string)
           ~doc:"STRING XmlStringMaxLen64"
       and metadataOptions =
         flag "metadata-options" (optional json_arg)
           ~doc:"JSON InstanceMetadataOptions"
       and launchConfigurationName =
         flag "launch-configuration-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_launch_configuration
           (Values.CreateLaunchConfigurationType.make ?imageId ?keyName
              ?securityGroups:(Option.map ~f:Values.SecurityGroups.of_json
                                 securityGroups) ?classicLinkVPCId
              ?classicLinkVPCSecurityGroups:(Option.map
                                               ~f:Values.ClassicLinkVPCSecurityGroups.of_json
                                               classicLinkVPCSecurityGroups)
              ?userData ?instanceId ?instanceType ?kernelId ?ramdiskId
              ?blockDeviceMappings:(Option.map
                                      ~f:Values.BlockDeviceMappings.of_json
                                      blockDeviceMappings)
              ?instanceMonitoring:(Option.map
                                     ~f:Values.InstanceMonitoring.of_json
                                     instanceMonitoring) ?spotPrice
              ?iamInstanceProfile ?ebsOptimized ?associatePublicIpAddress
              ?placementTenancy
              ?metadataOptions:(Option.map
                                  ~f:Values.InstanceMetadataOptions.of_json
                                  metadataOptions) ~launchConfigurationName
              ()) None None])
let create_or_update_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_or_update_tags
           (Values.CreateOrUpdateTagsType.make
              ~tags:(Values.Tags.of_json tags) ()) None None])
let delete_auto_scaling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL ForceDelete"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_auto_scaling_group
           (Values.DeleteAutoScalingGroupType.make ?forceDelete
              ~autoScalingGroupName ()) None None])
let delete_launch_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchConfigurationName =
         flag "launch-configuration-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_configuration
           (Values.LaunchConfigurationNameType.make ~launchConfigurationName
              ()) None None])
let delete_lifecycle_hook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lifecycleHookName =
         flag "lifecycle-hook-name" (required string)
           ~doc:"STRING AsciiStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lifecycle_hook
           (Values.DeleteLifecycleHookType.make ~lifecycleHookName
              ~autoScalingGroupName ())
           (Some Values.DeleteLifecycleHookAnswer.to_json)
           (Some Values.DeleteLifecycleHookAnswer.error_to_json)])
let delete_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and topicARN =
         flag "topic-a-r-n" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification_configuration
           (Values.DeleteNotificationConfigurationType.make
              ~autoScalingGroupName ~topicARN ()) None None])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy
           (Values.DeletePolicyType.make ?autoScalingGroupName ~policyName ())
           None None])
let delete_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_action
           (Values.DeleteScheduledActionType.make ~autoScalingGroupName
              ~scheduledActionName ()) None None])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsType.make ~tags:(Values.Tags.of_json tags) ())
           None None])
let delete_warm_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL ForceDelete"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_warm_pool
           (Values.DeleteWarmPoolType.make ?forceDelete ~autoScalingGroupName
              ()) (Some Values.DeleteWarmPoolAnswer.to_json)
           (Some Values.DeleteWarmPoolAnswer.error_to_json)])
let describe_account_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_limits (Fn.id ())
           (Some Values.DescribeAccountLimitsAnswer.to_json)
           (Some Values.DescribeAccountLimitsAnswer.error_to_json)])
let describe_adjustment_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_adjustment_types (Fn.id ())
           (Some Values.DescribeAdjustmentTypesAnswer.to_json)
           (Some Values.DescribeAdjustmentTypesAnswer.error_to_json)])
let describe_auto_scaling_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupNames =
         flag "auto-scaling-group-names" (optional json_arg)
           ~doc:"JSON AutoScalingGroupNames"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_auto_scaling_groups
           (Values.AutoScalingGroupNamesType.make
              ?autoScalingGroupNames:(Option.map
                                        ~f:Values.AutoScalingGroupNames.of_json
                                        autoScalingGroupNames) ?nextToken
              ?maxRecords
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.AutoScalingGroupsType.to_json)
           (Some Values.AutoScalingGroupsType.error_to_json)])
let describe_auto_scaling_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIds"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_auto_scaling_instances
           (Values.DescribeAutoScalingInstancesType.make
              ?instanceIds:(Option.map ~f:Values.InstanceIds.of_json
                              instanceIds) ?maxRecords ?nextToken ())
           (Some Values.AutoScalingInstancesType.to_json)
           (Some Values.AutoScalingInstancesType.error_to_json)])
let describe_auto_scaling_notification_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_auto_scaling_notification_types (Fn.id ())
           (Some Values.DescribeAutoScalingNotificationTypesAnswer.to_json)
           (Some
              Values.DescribeAutoScalingNotificationTypesAnswer.error_to_json)])
let describe_instance_refreshes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceRefreshIds =
         flag "instance-refresh-ids" (optional json_arg)
           ~doc:"JSON InstanceRefreshIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_refreshes
           (Values.DescribeInstanceRefreshesType.make
              ?instanceRefreshIds:(Option.map
                                     ~f:Values.InstanceRefreshIds.of_json
                                     instanceRefreshIds) ?nextToken
              ?maxRecords ~autoScalingGroupName ())
           (Some Values.DescribeInstanceRefreshesAnswer.to_json)
           (Some Values.DescribeInstanceRefreshesAnswer.error_to_json)])
let describe_launch_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchConfigurationNames =
         flag "launch-configuration-names" (optional json_arg)
           ~doc:"JSON LaunchConfigurationNames"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_launch_configurations
           (Values.LaunchConfigurationNamesType.make
              ?launchConfigurationNames:(Option.map
                                           ~f:Values.LaunchConfigurationNames.of_json
                                           launchConfigurationNames)
              ?nextToken ?maxRecords ())
           (Some Values.LaunchConfigurationsType.to_json)
           (Some Values.LaunchConfigurationsType.error_to_json)])
let describe_lifecycle_hook_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_lifecycle_hook_types (Fn.id ())
           (Some Values.DescribeLifecycleHookTypesAnswer.to_json)
           (Some Values.DescribeLifecycleHookTypesAnswer.error_to_json)])
let describe_lifecycle_hooks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lifecycleHookNames =
         flag "lifecycle-hook-names" (optional json_arg)
           ~doc:"JSON LifecycleHookNames"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_lifecycle_hooks
           (Values.DescribeLifecycleHooksType.make
              ?lifecycleHookNames:(Option.map
                                     ~f:Values.LifecycleHookNames.of_json
                                     lifecycleHookNames)
              ~autoScalingGroupName ())
           (Some Values.DescribeLifecycleHooksAnswer.to_json)
           (Some Values.DescribeLifecycleHooksAnswer.error_to_json)])
let describe_load_balancer_target_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_load_balancer_target_groups
           (Values.DescribeLoadBalancerTargetGroupsRequest.make ?nextToken
              ?maxRecords ~autoScalingGroupName ())
           (Some Values.DescribeLoadBalancerTargetGroupsResponse.to_json)
           (Some
              Values.DescribeLoadBalancerTargetGroupsResponse.error_to_json)])
let describe_load_balancers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_load_balancers
           (Values.DescribeLoadBalancersRequest.make ?nextToken ?maxRecords
              ~autoScalingGroupName ())
           (Some Values.DescribeLoadBalancersResponse.to_json)
           (Some Values.DescribeLoadBalancersResponse.error_to_json)])
let describe_metric_collection_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_metric_collection_types (Fn.id ())
           (Some Values.DescribeMetricCollectionTypesAnswer.to_json)
           (Some Values.DescribeMetricCollectionTypesAnswer.error_to_json)])
let describe_notification_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupNames =
         flag "auto-scaling-group-names" (optional json_arg)
           ~doc:"JSON AutoScalingGroupNames"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_notification_configurations
           (Values.DescribeNotificationConfigurationsType.make
              ?autoScalingGroupNames:(Option.map
                                        ~f:Values.AutoScalingGroupNames.of_json
                                        autoScalingGroupNames) ?nextToken
              ?maxRecords ())
           (Some Values.DescribeNotificationConfigurationsAnswer.to_json)
           (Some
              Values.DescribeNotificationConfigurationsAnswer.error_to_json)])
let describe_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and policyNames =
         flag "policy-names" (optional json_arg) ~doc:"JSON PolicyNames"
       and policyTypes =
         flag "policy-types" (optional json_arg) ~doc:"JSON PolicyTypes"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_policies
           (Values.DescribePoliciesType.make ?autoScalingGroupName
              ?policyNames:(Option.map ~f:Values.PolicyNames.of_json
                              policyNames)
              ?policyTypes:(Option.map ~f:Values.PolicyTypes.of_json
                              policyTypes) ?nextToken ?maxRecords ())
           (Some Values.PoliciesType.to_json)
           (Some Values.PoliciesType.error_to_json)])
let describe_scaling_activities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activityIds =
         flag "activity-ids" (optional json_arg) ~doc:"JSON ActivityIds"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and includeDeletedGroups =
         flag "include-deleted-groups" (optional bool)
           ~doc:"BOOL IncludeDeletedGroups"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_activities
           (Values.DescribeScalingActivitiesType.make
              ?activityIds:(Option.map ~f:Values.ActivityIds.of_json
                              activityIds) ?autoScalingGroupName
              ?includeDeletedGroups ?maxRecords ?nextToken ())
           (Some Values.ActivitiesType.to_json)
           (Some Values.ActivitiesType.error_to_json)])
let describe_scaling_process_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_process_types (Fn.id ())
           (Some Values.ProcessesType.to_json)
           (Some Values.ProcessesType.error_to_json)])
let describe_scheduled_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and scheduledActionNames =
         flag "scheduled-action-names" (optional json_arg)
           ~doc:"JSON ScheduledActionNames"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimestampType"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimestampType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scheduled_actions
           (Values.DescribeScheduledActionsType.make ?autoScalingGroupName
              ?scheduledActionNames:(Option.map
                                       ~f:Values.ScheduledActionNames.of_json
                                       scheduledActionNames)
              ?startTime:(Option.map ~f:Values.TimestampType.of_json
                            startTime)
              ?endTime:(Option.map ~f:Values.TimestampType.of_json endTime)
              ?nextToken ?maxRecords ())
           (Some Values.ScheduledActionsType.to_json)
           (Some Values.ScheduledActionsType.error_to_json)])
let describe_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tags
           (Values.DescribeTagsType.make
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?nextToken ?maxRecords ()) (Some Values.TagsType.to_json)
           (Some Values.TagsType.error_to_json)])
let describe_termination_policy_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_termination_policy_types (Fn.id ())
           (Some Values.DescribeTerminationPolicyTypesAnswer.to_json)
           (Some Values.DescribeTerminationPolicyTypesAnswer.error_to_json)])
let describe_warm_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_warm_pool
           (Values.DescribeWarmPoolType.make ?maxRecords ?nextToken
              ~autoScalingGroupName ())
           (Some Values.DescribeWarmPoolAnswer.to_json)
           (Some Values.DescribeWarmPoolAnswer.error_to_json)])
let detach_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIds"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and shouldDecrementDesiredCapacity =
         flag "should-decrement-desired-capacity" (required bool)
           ~doc:"BOOL ShouldDecrementDesiredCapacity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_instances
           (Values.DetachInstancesQuery.make
              ?instanceIds:(Option.map ~f:Values.InstanceIds.of_json
                              instanceIds) ~autoScalingGroupName
              ~shouldDecrementDesiredCapacity ())
           (Some Values.DetachInstancesAnswer.to_json)
           (Some Values.DetachInstancesAnswer.error_to_json)])
let detach_load_balancer_target_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and targetGroupARNs =
         flag "target-group-a-r-ns" (required json_arg)
           ~doc:"JSON TargetGroupARNs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_load_balancer_target_groups
           (Values.DetachLoadBalancerTargetGroupsType.make
              ~autoScalingGroupName
              ~targetGroupARNs:(Values.TargetGroupARNs.of_json
                                  targetGroupARNs) ())
           (Some Values.DetachLoadBalancerTargetGroupsResultType.to_json)
           (Some
              Values.DetachLoadBalancerTargetGroupsResultType.error_to_json)])
let detach_load_balancers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and loadBalancerNames =
         flag "load-balancer-names" (required json_arg)
           ~doc:"JSON LoadBalancerNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_load_balancers
           (Values.DetachLoadBalancersType.make ~autoScalingGroupName
              ~loadBalancerNames:(Values.LoadBalancerNames.of_json
                                    loadBalancerNames) ())
           (Some Values.DetachLoadBalancersResultType.to_json)
           (Some Values.DetachLoadBalancersResultType.error_to_json)])
let disable_metrics_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metrics = flag "metrics" (optional json_arg) ~doc:"JSON Metrics"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_metrics_collection
           (Values.DisableMetricsCollectionQuery.make
              ?metrics:(Option.map ~f:Values.Metrics.of_json metrics)
              ~autoScalingGroupName ()) None None])
let enable_metrics_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metrics = flag "metrics" (optional json_arg) ~doc:"JSON Metrics"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and granularity =
         flag "granularity" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_metrics_collection
           (Values.EnableMetricsCollectionQuery.make
              ?metrics:(Option.map ~f:Values.Metrics.of_json metrics)
              ~autoScalingGroupName ~granularity ()) None None])
let enter_standby =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIds"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and shouldDecrementDesiredCapacity =
         flag "should-decrement-desired-capacity" (required bool)
           ~doc:"BOOL ShouldDecrementDesiredCapacity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enter_standby
           (Values.EnterStandbyQuery.make
              ?instanceIds:(Option.map ~f:Values.InstanceIds.of_json
                              instanceIds) ~autoScalingGroupName
              ~shouldDecrementDesiredCapacity ())
           (Some Values.EnterStandbyAnswer.to_json)
           (Some Values.EnterStandbyAnswer.error_to_json)])
let execute_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and honorCooldown =
         flag "honor-cooldown" (optional bool) ~doc:"BOOL HonorCooldown"
       and metricValue =
         flag "metric-value" (optional float) ~doc:"FLOAT MetricScale"
       and breachThreshold =
         flag "breach-threshold" (optional float) ~doc:"FLOAT MetricScale"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_policy
           (Values.ExecutePolicyType.make ?autoScalingGroupName
              ?honorCooldown ?metricValue ?breachThreshold ~policyName ())
           None None])
let exit_standby =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIds"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.exit_standby
           (Values.ExitStandbyQuery.make
              ?instanceIds:(Option.map ~f:Values.InstanceIds.of_json
                              instanceIds) ~autoScalingGroupName ())
           (Some Values.ExitStandbyAnswer.to_json)
           (Some Values.ExitStandbyAnswer.error_to_json)])
let get_predictive_scaling_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and policyName =
         flag "policy-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON TimestampType"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON TimestampType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_predictive_scaling_forecast
           (Values.GetPredictiveScalingForecastType.make
              ~autoScalingGroupName ~policyName
              ~startTime:(Values.TimestampType.of_json startTime)
              ~endTime:(Values.TimestampType.of_json endTime) ())
           (Some Values.GetPredictiveScalingForecastAnswer.to_json)
           (Some Values.GetPredictiveScalingForecastAnswer.error_to_json)])
let put_lifecycle_hook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lifecycleTransition =
         flag "lifecycle-transition" (optional string)
           ~doc:"STRING LifecycleTransition"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and notificationTargetARN =
         flag "notification-target-a-r-n" (optional string)
           ~doc:"STRING NotificationTargetResourceName"
       and notificationMetadata =
         flag "notification-metadata" (optional string)
           ~doc:"STRING XmlStringMaxLen1023"
       and heartbeatTimeout =
         flag "heartbeat-timeout" (optional int) ~doc:"INT HeartbeatTimeout"
       and defaultResult =
         flag "default-result" (optional string)
           ~doc:"STRING LifecycleActionResult"
       and lifecycleHookName =
         flag "lifecycle-hook-name" (required string)
           ~doc:"STRING AsciiStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_lifecycle_hook
           (Values.PutLifecycleHookType.make ?lifecycleTransition ?roleARN
              ?notificationTargetARN ?notificationMetadata ?heartbeatTimeout
              ?defaultResult ~lifecycleHookName ~autoScalingGroupName ())
           (Some Values.PutLifecycleHookAnswer.to_json)
           (Some Values.PutLifecycleHookAnswer.error_to_json)])
let put_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and topicARN =
         flag "topic-a-r-n" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and notificationTypes =
         flag "notification-types" (required json_arg)
           ~doc:"JSON AutoScalingNotificationTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_notification_configuration
           (Values.PutNotificationConfigurationType.make
              ~autoScalingGroupName ~topicARN
              ~notificationTypes:(Values.AutoScalingNotificationTypes.of_json
                                    notificationTypes) ()) None None])
let put_scaling_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyType =
         flag "policy-type" (optional string) ~doc:"STRING XmlStringMaxLen64"
       and adjustmentType =
         flag "adjustment-type" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and minAdjustmentStep =
         flag "min-adjustment-step" (optional int)
           ~doc:"INT MinAdjustmentStep"
       and minAdjustmentMagnitude =
         flag "min-adjustment-magnitude" (optional int)
           ~doc:"INT MinAdjustmentMagnitude"
       and scalingAdjustment =
         flag "scaling-adjustment" (optional int) ~doc:"INT PolicyIncrement"
       and cooldown = flag "cooldown" (optional int) ~doc:"INT Cooldown"
       and metricAggregationType =
         flag "metric-aggregation-type" (optional string)
           ~doc:"STRING XmlStringMaxLen32"
       and stepAdjustments =
         flag "step-adjustments" (optional json_arg)
           ~doc:"JSON StepAdjustments"
       and estimatedInstanceWarmup =
         flag "estimated-instance-warmup" (optional int)
           ~doc:"INT EstimatedInstanceWarmup"
       and targetTrackingConfiguration =
         flag "target-tracking-configuration" (optional json_arg)
           ~doc:"JSON TargetTrackingConfiguration"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL ScalingPolicyEnabled"
       and predictiveScalingConfiguration =
         flag "predictive-scaling-configuration" (optional json_arg)
           ~doc:"JSON PredictiveScalingConfiguration"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and policyName =
         flag "policy-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_scaling_policy
           (Values.PutScalingPolicyType.make ?policyType ?adjustmentType
              ?minAdjustmentStep ?minAdjustmentMagnitude ?scalingAdjustment
              ?cooldown ?metricAggregationType
              ?stepAdjustments:(Option.map ~f:Values.StepAdjustments.of_json
                                  stepAdjustments) ?estimatedInstanceWarmup
              ?targetTrackingConfiguration:(Option.map
                                              ~f:Values.TargetTrackingConfiguration.of_json
                                              targetTrackingConfiguration)
              ?enabled
              ?predictiveScalingConfiguration:(Option.map
                                                 ~f:Values.PredictiveScalingConfiguration.of_json
                                                 predictiveScalingConfiguration)
              ~autoScalingGroupName ~policyName ())
           (Some Values.PolicyARNType.to_json)
           (Some Values.PolicyARNType.error_to_json)])
let put_scheduled_update_group_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and time = flag "time" (optional json_arg) ~doc:"JSON TimestampType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimestampType"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimestampType"
       and recurrence =
         flag "recurrence" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and minSize =
         flag "min-size" (optional int) ~doc:"INT AutoScalingGroupMinSize"
       and maxSize =
         flag "max-size" (optional int) ~doc:"INT AutoScalingGroupMaxSize"
       and desiredCapacity =
         flag "desired-capacity" (optional int)
           ~doc:"INT AutoScalingGroupDesiredCapacity"
       and timeZone =
         flag "time-zone" (optional string) ~doc:"STRING XmlStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_scheduled_update_group_action
           (Values.PutScheduledUpdateGroupActionType.make
              ?time:(Option.map ~f:Values.TimestampType.of_json time)
              ?startTime:(Option.map ~f:Values.TimestampType.of_json
                            startTime)
              ?endTime:(Option.map ~f:Values.TimestampType.of_json endTime)
              ?recurrence ?minSize ?maxSize ?desiredCapacity ?timeZone
              ~autoScalingGroupName ~scheduledActionName ()) None None])
let put_warm_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxGroupPreparedCapacity =
         flag "max-group-prepared-capacity" (optional int)
           ~doc:"INT MaxGroupPreparedCapacity"
       and minSize =
         flag "min-size" (optional int) ~doc:"INT WarmPoolMinSize"
       and poolState =
         flag "pool-state" (optional json_arg) ~doc:"JSON WarmPoolState"
       and instanceReusePolicy =
         flag "instance-reuse-policy" (optional json_arg)
           ~doc:"JSON InstanceReusePolicy"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_warm_pool
           (Values.PutWarmPoolType.make ?maxGroupPreparedCapacity ?minSize
              ?poolState:(Option.map ~f:Values.WarmPoolState.of_json
                            poolState)
              ?instanceReusePolicy:(Option.map
                                      ~f:Values.InstanceReusePolicy.of_json
                                      instanceReusePolicy)
              ~autoScalingGroupName ())
           (Some Values.PutWarmPoolAnswer.to_json)
           (Some Values.PutWarmPoolAnswer.error_to_json)])
let record_lifecycle_action_heartbeat =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lifecycleActionToken =
         flag "lifecycle-action-token" (optional string)
           ~doc:"STRING LifecycleActionToken"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING XmlStringMaxLen19"
       and lifecycleHookName =
         flag "lifecycle-hook-name" (required string)
           ~doc:"STRING AsciiStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.record_lifecycle_action_heartbeat
           (Values.RecordLifecycleActionHeartbeatType.make
              ?lifecycleActionToken ?instanceId ~lifecycleHookName
              ~autoScalingGroupName ())
           (Some Values.RecordLifecycleActionHeartbeatAnswer.to_json)
           (Some Values.RecordLifecycleActionHeartbeatAnswer.error_to_json)])
let resume_processes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingProcesses =
         flag "scaling-processes" (optional json_arg)
           ~doc:"JSON ProcessNames"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_processes
           (Values.ScalingProcessQuery.make
              ?scalingProcesses:(Option.map ~f:Values.ProcessNames.of_json
                                   scalingProcesses) ~autoScalingGroupName ())
           None None])
let set_desired_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and honorCooldown =
         flag "honor-cooldown" (optional bool) ~doc:"BOOL HonorCooldown"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and desiredCapacity =
         flag "desired-capacity" (required int)
           ~doc:"INT AutoScalingGroupDesiredCapacity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_desired_capacity
           (Values.SetDesiredCapacityType.make ?honorCooldown
              ~autoScalingGroupName ~desiredCapacity ()) None None])
let set_instance_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shouldRespectGracePeriod =
         flag "should-respect-grace-period" (optional bool)
           ~doc:"BOOL ShouldRespectGracePeriod"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING XmlStringMaxLen19"
       and healthStatus =
         flag "health-status" (required string)
           ~doc:"STRING XmlStringMaxLen32" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_instance_health
           (Values.SetInstanceHealthQuery.make ?shouldRespectGracePeriod
              ~instanceId ~healthStatus ()) None None])
let set_instance_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (required json_arg) ~doc:"JSON InstanceIds"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255"
       and protectedFromScaleIn =
         flag "protected-from-scale-in" (required bool)
           ~doc:"BOOL ProtectedFromScaleIn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_instance_protection
           (Values.SetInstanceProtectionQuery.make
              ~instanceIds:(Values.InstanceIds.of_json instanceIds)
              ~autoScalingGroupName ~protectedFromScaleIn ())
           (Some Values.SetInstanceProtectionAnswer.to_json)
           (Some Values.SetInstanceProtectionAnswer.error_to_json)])
let start_instance_refresh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and strategy =
         flag "strategy" (optional json_arg) ~doc:"JSON RefreshStrategy"
       and desiredConfiguration =
         flag "desired-configuration" (optional json_arg)
           ~doc:"JSON DesiredConfiguration"
       and preferences =
         flag "preferences" (optional json_arg)
           ~doc:"JSON RefreshPreferences"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_instance_refresh
           (Values.StartInstanceRefreshType.make
              ?strategy:(Option.map ~f:Values.RefreshStrategy.of_json
                           strategy)
              ?desiredConfiguration:(Option.map
                                       ~f:Values.DesiredConfiguration.of_json
                                       desiredConfiguration)
              ?preferences:(Option.map ~f:Values.RefreshPreferences.of_json
                              preferences) ~autoScalingGroupName ())
           (Some Values.StartInstanceRefreshAnswer.to_json)
           (Some Values.StartInstanceRefreshAnswer.error_to_json)])
let suspend_processes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingProcesses =
         flag "scaling-processes" (optional json_arg)
           ~doc:"JSON ProcessNames"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.suspend_processes
           (Values.ScalingProcessQuery.make
              ?scalingProcesses:(Option.map ~f:Values.ProcessNames.of_json
                                   scalingProcesses) ~autoScalingGroupName ())
           None None])
let terminate_instance_in_auto_scaling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING XmlStringMaxLen19"
       and shouldDecrementDesiredCapacity =
         flag "should-decrement-desired-capacity" (required bool)
           ~doc:"BOOL ShouldDecrementDesiredCapacity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_instance_in_auto_scaling_group
           (Values.TerminateInstanceInAutoScalingGroupType.make ~instanceId
              ~shouldDecrementDesiredCapacity ())
           (Some Values.ActivityType.to_json)
           (Some Values.ActivityType.error_to_json)])
let update_auto_scaling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchConfigurationName =
         flag "launch-configuration-name" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and launchTemplate =
         flag "launch-template" (optional json_arg)
           ~doc:"JSON LaunchTemplateSpecification"
       and mixedInstancesPolicy =
         flag "mixed-instances-policy" (optional json_arg)
           ~doc:"JSON MixedInstancesPolicy"
       and minSize =
         flag "min-size" (optional int) ~doc:"INT AutoScalingGroupMinSize"
       and maxSize =
         flag "max-size" (optional int) ~doc:"INT AutoScalingGroupMaxSize"
       and desiredCapacity =
         flag "desired-capacity" (optional int)
           ~doc:"INT AutoScalingGroupDesiredCapacity"
       and defaultCooldown =
         flag "default-cooldown" (optional int) ~doc:"INT Cooldown"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZones"
       and healthCheckType =
         flag "health-check-type" (optional string)
           ~doc:"STRING XmlStringMaxLen32"
       and healthCheckGracePeriod =
         flag "health-check-grace-period" (optional int)
           ~doc:"INT HealthCheckGracePeriod"
       and placementGroup =
         flag "placement-group" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and vPCZoneIdentifier =
         flag "v-p-c-zone-identifier" (optional string)
           ~doc:"STRING XmlStringMaxLen2047"
       and terminationPolicies =
         flag "termination-policies" (optional json_arg)
           ~doc:"JSON TerminationPolicies"
       and newInstancesProtectedFromScaleIn =
         flag "new-instances-protected-from-scale-in" (optional bool)
           ~doc:"BOOL InstanceProtected"
       and serviceLinkedRoleARN =
         flag "service-linked-role-a-r-n" (optional string)
           ~doc:"STRING ResourceName"
       and maxInstanceLifetime =
         flag "max-instance-lifetime" (optional int)
           ~doc:"INT MaxInstanceLifetime"
       and capacityRebalance =
         flag "capacity-rebalance" (optional bool)
           ~doc:"BOOL CapacityRebalanceEnabled"
       and context = flag "context" (optional string) ~doc:"STRING Context"
       and desiredCapacityType =
         flag "desired-capacity-type" (optional string)
           ~doc:"STRING XmlStringMaxLen255"
       and autoScalingGroupName =
         flag "auto-scaling-group-name" (required string)
           ~doc:"STRING XmlStringMaxLen255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_auto_scaling_group
           (Values.UpdateAutoScalingGroupType.make ?launchConfigurationName
              ?launchTemplate:(Option.map
                                 ~f:Values.LaunchTemplateSpecification.of_json
                                 launchTemplate)
              ?mixedInstancesPolicy:(Option.map
                                       ~f:Values.MixedInstancesPolicy.of_json
                                       mixedInstancesPolicy) ?minSize
              ?maxSize ?desiredCapacity ?defaultCooldown
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZones.of_json
                                    availabilityZones) ?healthCheckType
              ?healthCheckGracePeriod ?placementGroup ?vPCZoneIdentifier
              ?terminationPolicies:(Option.map
                                      ~f:Values.TerminationPolicies.of_json
                                      terminationPolicies)
              ?newInstancesProtectedFromScaleIn ?serviceLinkedRoleARN
              ?maxInstanceLifetime ?capacityRebalance ?context
              ?desiredCapacityType ~autoScalingGroupName ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("attach-instances", attach_instances);
    ("attach-load-balancer-target-groups",
      attach_load_balancer_target_groups);
    ("attach-load-balancers", attach_load_balancers);
    ("batch-delete-scheduled-action", batch_delete_scheduled_action);
    ("batch-put-scheduled-update-group-action",
      batch_put_scheduled_update_group_action);
    ("cancel-instance-refresh", cancel_instance_refresh);
    ("complete-lifecycle-action", complete_lifecycle_action);
    ("create-auto-scaling-group", create_auto_scaling_group);
    ("create-launch-configuration", create_launch_configuration);
    ("create-or-update-tags", create_or_update_tags);
    ("delete-auto-scaling-group", delete_auto_scaling_group);
    ("delete-launch-configuration", delete_launch_configuration);
    ("delete-lifecycle-hook", delete_lifecycle_hook);
    ("delete-notification-configuration", delete_notification_configuration);
    ("delete-policy", delete_policy);
    ("delete-scheduled-action", delete_scheduled_action);
    ("delete-tags", delete_tags);
    ("delete-warm-pool", delete_warm_pool);
    ("describe-account-limits", describe_account_limits);
    ("describe-adjustment-types", describe_adjustment_types);
    ("describe-auto-scaling-groups", describe_auto_scaling_groups);
    ("describe-auto-scaling-instances", describe_auto_scaling_instances);
    ("describe-auto-scaling-notification-types",
      describe_auto_scaling_notification_types);
    ("describe-instance-refreshes", describe_instance_refreshes);
    ("describe-launch-configurations", describe_launch_configurations);
    ("describe-lifecycle-hook-types", describe_lifecycle_hook_types);
    ("describe-lifecycle-hooks", describe_lifecycle_hooks);
    ("describe-load-balancer-target-groups",
      describe_load_balancer_target_groups);
    ("describe-load-balancers", describe_load_balancers);
    ("describe-metric-collection-types", describe_metric_collection_types);
    ("describe-notification-configurations",
      describe_notification_configurations);
    ("describe-policies", describe_policies);
    ("describe-scaling-activities", describe_scaling_activities);
    ("describe-scaling-process-types", describe_scaling_process_types);
    ("describe-scheduled-actions", describe_scheduled_actions);
    ("describe-tags", describe_tags);
    ("describe-termination-policy-types", describe_termination_policy_types);
    ("describe-warm-pool", describe_warm_pool);
    ("detach-instances", detach_instances);
    ("detach-load-balancer-target-groups",
      detach_load_balancer_target_groups);
    ("detach-load-balancers", detach_load_balancers);
    ("disable-metrics-collection", disable_metrics_collection);
    ("enable-metrics-collection", enable_metrics_collection);
    ("enter-standby", enter_standby);
    ("execute-policy", execute_policy);
    ("exit-standby", exit_standby);
    ("get-predictive-scaling-forecast", get_predictive_scaling_forecast);
    ("put-lifecycle-hook", put_lifecycle_hook);
    ("put-notification-configuration", put_notification_configuration);
    ("put-scaling-policy", put_scaling_policy);
    ("put-scheduled-update-group-action", put_scheduled_update_group_action);
    ("put-warm-pool", put_warm_pool);
    ("record-lifecycle-action-heartbeat", record_lifecycle_action_heartbeat);
    ("resume-processes", resume_processes);
    ("set-desired-capacity", set_desired_capacity);
    ("set-instance-health", set_instance_health);
    ("set-instance-protection", set_instance_protection);
    ("start-instance-refresh", start_instance_refresh);
    ("suspend-processes", suspend_processes);
    ("terminate-instance-in-auto-scaling-group",
      terminate_instance_in_auto_scaling_group);
    ("update-auto-scaling-group", update_auto_scaling_group)]
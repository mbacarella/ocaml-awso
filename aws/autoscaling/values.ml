(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.autoscaling
let apiVersion = "2011-01-01"
let endpointPrefix = "autoscaling"
let serviceFullName = "Auto Scaling"
let signatureVersion = "v4"
let protocol = "query"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let xmlNamespace = "http://autoscaling.amazonaws.com/doc/2011-01-01/"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module MetricDimensionName =
  struct
    type nonrec t = string
    let context_ = "MetricDimensionName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricDimensionName" j
    let to_json = simple_to_json to_value
  end
module MetricDimensionValue =
  struct
    type nonrec t = string
    let context_ = "MetricDimensionValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricDimensionValue" j
    let to_json = simple_to_json to_value
  end
module MetricDimension =
  struct
    type nonrec t =
      {
      name: MetricDimensionName.t ;
      value: MetricDimensionValue.t }
    let context_ = "MetricDimension"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Some (MetricDimensionName.to_value x.name)));
        ("Value", (Some (MetricDimensionValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        MetricDimensionValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let name =
        MetricDimensionName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~value ~name ()
    let of_json json =
      let value = field_map_exn json "Value" MetricDimensionValue.of_json in
      let name = field_map_exn json "Name" MetricDimensionName.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end
module MetricDimensions =
  struct
    type nonrec t = MetricDimension.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MetricDimension.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricDimension.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricDimensions" ~of_json:MetricDimension.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module MetricName =
  struct
    type nonrec t = string
    let context_ = "MetricName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricName" j
    let to_json = simple_to_json to_value
  end
module MetricNamespace =
  struct
    type nonrec t = string
    let context_ = "MetricNamespace"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricNamespace" j
    let to_json = simple_to_json to_value
  end
module Metric =
  struct
    type nonrec t =
      {
      namespace: MetricNamespace.t ;
      metricName: MetricName.t ;
      dimensions: MetricDimensions.t option }
    let context_ = "Metric"
    let make ?dimensions =
      fun ~namespace ->
        fun ~metricName -> fun () -> { dimensions; namespace; metricName }
    let to_value x =
      structure_to_value
        [("Namespace", (Some (MetricNamespace.to_value x.namespace)));
        ("MetricName", (Some (MetricName.to_value x.metricName)));
        ("Dimensions",
          (Option.map x.dimensions ~f:MetricDimensions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dimensions =
        (Option.map ~f:MetricDimensions.of_xml)
          (Xml.child xml_arg0 "Dimensions") in
      let metricName =
        MetricName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricName") in
      let namespace =
        MetricNamespace.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Namespace") in
      make ?dimensions ~metricName ~namespace ()
    let of_json json =
      let dimensions = field_map json "Dimensions" MetricDimensions.of_json in
      let metricName = field_map_exn json "MetricName" MetricName.of_json in
      let namespace = field_map_exn json "Namespace" MetricNamespace.of_json in
      make ?dimensions ~metricName ~namespace ()
    let to_json v = composed_to_json to_value v
  end
module MetricUnit =
  struct
    type nonrec t = string
    let context_ = "MetricUnit"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricUnit" j
    let to_json = simple_to_json to_value
  end
module XmlStringMetricStat =
  struct
    type nonrec t = string
    let context_ = "XmlStringMetricStat"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMetricStat" j
    let to_json = simple_to_json to_value
  end
module NullablePositiveInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NullablePositiveInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AcceleratorManufacturer =
  struct
    type nonrec t =
      | Nvidia 
      | Amd 
      | Amazon_web_services 
      | Xilinx 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Nvidia -> "nvidia"
      | Amd -> "amd"
      | Amazon_web_services -> "amazon-web-services"
      | Xilinx -> "xilinx"
      | Non_static_id s -> s
    let of_string =
      function
      | "nvidia" -> Nvidia
      | "amd" -> Amd
      | "amazon-web-services" -> Amazon_web_services
      | "xilinx" -> Xilinx
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AcceleratorManufacturer" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AcceleratorManufacturer" j)
    let to_json = simple_to_json to_value
  end
module AcceleratorName =
  struct
    type nonrec t =
      | A100 
      | V100 
      | K80 
      | T4 
      | M60 
      | Radeon_pro_v520 
      | Vu9p 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | A100 -> "a100"
      | V100 -> "v100"
      | K80 -> "k80"
      | T4 -> "t4"
      | M60 -> "m60"
      | Radeon_pro_v520 -> "radeon-pro-v520"
      | Vu9p -> "vu9p"
      | Non_static_id s -> s
    let of_string =
      function
      | "a100" -> A100
      | "v100" -> V100
      | "k80" -> K80
      | "t4" -> T4
      | "m60" -> M60
      | "radeon-pro-v520" -> Radeon_pro_v520
      | "vu9p" -> Vu9p
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AcceleratorName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AcceleratorName" j)
    let to_json = simple_to_json to_value
  end
module AcceleratorType =
  struct
    type nonrec t =
      | Gpu 
      | Fpga 
      | Inference 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Gpu -> "gpu"
      | Fpga -> "fpga"
      | Inference -> "inference"
      | Non_static_id s -> s
    let of_string =
      function
      | "gpu" -> Gpu
      | "fpga" -> Fpga
      | "inference" -> Inference
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AcceleratorType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AcceleratorType" j)
    let to_json = simple_to_json to_value
  end
module CpuManufacturer =
  struct
    type nonrec t =
      | Intel 
      | Amd 
      | Amazon_web_services 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Intel -> "intel"
      | Amd -> "amd"
      | Amazon_web_services -> "amazon-web-services"
      | Non_static_id s -> s
    let of_string =
      function
      | "intel" -> Intel
      | "amd" -> Amd
      | "amazon-web-services" -> Amazon_web_services
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CpuManufacturer" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CpuManufacturer" j)
    let to_json = simple_to_json to_value
  end
module ExcludedInstance =
  struct
    type nonrec t = string
    let context_ = "ExcludedInstance"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:30) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9\\.\\*]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExcludedInstance" j
    let to_json = simple_to_json to_value
  end
module InstanceGeneration =
  struct
    type nonrec t =
      | Current 
      | Previous 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Current -> "current"
      | Previous -> "previous"
      | Non_static_id s -> s
    let of_string =
      function
      | "current" -> Current
      | "previous" -> Previous
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceGeneration" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceGeneration" j)
    let to_json = simple_to_json to_value
  end
module LocalStorageType =
  struct
    type nonrec t =
      | Hdd 
      | Ssd 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Hdd -> "hdd" | Ssd -> "ssd" | Non_static_id s -> s
    let of_string =
      function | "hdd" -> Hdd | "ssd" -> Ssd | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LocalStorageType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LocalStorageType" j)
    let to_json = simple_to_json to_value
  end
module NullablePositiveDouble =
  struct
    type nonrec t = float
    let make i =
      let open Result in ok_or_failwith (check_float_min i ~min:0.); i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module MetricStat =
  struct
    type nonrec t =
      {
      metric: Metric.t ;
      stat: XmlStringMetricStat.t ;
      unit: MetricUnit.t option }
    let context_ = "MetricStat"
    let make ?unit =
      fun ~metric -> fun ~stat -> fun () -> { unit; metric; stat }
    let to_value x =
      structure_to_value
        [("Metric", (Some (Metric.to_value x.metric)));
        ("Stat", (Some (XmlStringMetricStat.to_value x.stat)));
        ("Unit", (Option.map x.unit ~f:MetricUnit.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unit =
        (Option.map ~f:MetricUnit.of_xml) (Xml.child xml_arg0 "Unit") in
      let stat =
        XmlStringMetricStat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Stat") in
      let metric =
        Metric.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Metric") in
      make ?unit ~stat ~metric ()
    let of_json json =
      let unit = field_map json "Unit" MetricUnit.of_json in
      let stat = field_map_exn json "Stat" XmlStringMetricStat.of_json in
      let metric = field_map_exn json "Metric" Metric.of_json in
      make ?unit ~stat ~metric ()
    let to_json v = composed_to_json to_value v
  end
module ReturnData =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen1023 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen1023"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1023) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen1023" j
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen255 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen255"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen255" j
    let to_json = simple_to_json to_value
  end
module XmlStringMetricLabel =
  struct
    type nonrec t = string
    let context_ = "XmlStringMetricLabel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2047) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMetricLabel" j
    let to_json = simple_to_json to_value
  end
module AcceleratorCountRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveInteger.t option ;
      max: NullablePositiveInteger.t option }
    let make ?min = fun ?max -> fun () -> { min; max }
    let to_value x =
      structure_to_value
        [("Min", (Option.map x.min ~f:NullablePositiveInteger.to_value));
        ("Max", (Option.map x.max ~f:NullablePositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Min") in
      make ?max ?min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveInteger.of_json in
      let min = field_map json "Min" NullablePositiveInteger.of_json in
      make ?max ?min ()
    let to_json v = composed_to_json to_value v
  end
module AcceleratorManufacturers =
  struct
    type nonrec t = AcceleratorManufacturer.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AcceleratorManufacturer.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AcceleratorManufacturer.of_xml)
    let of_json j =
      list_of_json ~kind:"AcceleratorManufacturers"
        ~of_json:AcceleratorManufacturer.of_json j
    let to_json v = composed_to_json to_value v
  end
module AcceleratorNames =
  struct
    type nonrec t = AcceleratorName.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AcceleratorName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AcceleratorName.of_xml)
    let of_json j =
      list_of_json ~kind:"AcceleratorNames" ~of_json:AcceleratorName.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AcceleratorTotalMemoryMiBRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveInteger.t option ;
      max: NullablePositiveInteger.t option }
    let make ?min = fun ?max -> fun () -> { min; max }
    let to_value x =
      structure_to_value
        [("Min", (Option.map x.min ~f:NullablePositiveInteger.to_value));
        ("Max", (Option.map x.max ~f:NullablePositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Min") in
      make ?max ?min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveInteger.of_json in
      let min = field_map json "Min" NullablePositiveInteger.of_json in
      make ?max ?min ()
    let to_json v = composed_to_json to_value v
  end
module AcceleratorTypes =
  struct
    type nonrec t = AcceleratorType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AcceleratorType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AcceleratorType.of_xml)
    let of_json j =
      list_of_json ~kind:"AcceleratorTypes" ~of_json:AcceleratorType.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module BareMetal =
  struct
    type nonrec t =
      | Included 
      | Excluded 
      | Required 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Included -> "included"
      | Excluded -> "excluded"
      | Required -> "required"
      | Non_static_id s -> s
    let of_string =
      function
      | "included" -> Included
      | "excluded" -> Excluded
      | "required" -> Required
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BareMetal" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BareMetal" j)
    let to_json = simple_to_json to_value
  end
module BaselineEbsBandwidthMbpsRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveInteger.t option ;
      max: NullablePositiveInteger.t option }
    let make ?min = fun ?max -> fun () -> { min; max }
    let to_value x =
      structure_to_value
        [("Min", (Option.map x.min ~f:NullablePositiveInteger.to_value));
        ("Max", (Option.map x.max ~f:NullablePositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Min") in
      make ?max ?min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveInteger.of_json in
      let min = field_map json "Min" NullablePositiveInteger.of_json in
      make ?max ?min ()
    let to_json v = composed_to_json to_value v
  end
module BurstablePerformance =
  struct
    type nonrec t =
      | Included 
      | Excluded 
      | Required 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Included -> "included"
      | Excluded -> "excluded"
      | Required -> "required"
      | Non_static_id s -> s
    let of_string =
      function
      | "included" -> Included
      | "excluded" -> Excluded
      | "required" -> Required
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BurstablePerformance" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BurstablePerformance" j)
    let to_json = simple_to_json to_value
  end
module CpuManufacturers =
  struct
    type nonrec t = CpuManufacturer.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CpuManufacturer.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CpuManufacturer.of_xml)
    let of_json j =
      list_of_json ~kind:"CpuManufacturers" ~of_json:CpuManufacturer.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ExcludedInstanceTypes =
  struct
    type nonrec t = ExcludedInstance.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:400); i
    let to_value xs =
      (xs |> (List.map ~f:ExcludedInstance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExcludedInstance.of_xml)
    let of_json j =
      list_of_json ~kind:"ExcludedInstanceTypes"
        ~of_json:ExcludedInstance.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceGenerations =
  struct
    type nonrec t = InstanceGeneration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceGeneration.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceGeneration.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGenerations"
        ~of_json:InstanceGeneration.of_json j
    let to_json v = composed_to_json to_value v
  end
module LocalStorage =
  struct
    type nonrec t =
      | Included 
      | Excluded 
      | Required 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Included -> "included"
      | Excluded -> "excluded"
      | Required -> "required"
      | Non_static_id s -> s
    let of_string =
      function
      | "included" -> Included
      | "excluded" -> Excluded
      | "required" -> Required
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LocalStorage" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LocalStorage" j)
    let to_json = simple_to_json to_value
  end
module LocalStorageTypes =
  struct
    type nonrec t = LocalStorageType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LocalStorageType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocalStorageType.of_xml)
    let of_json j =
      list_of_json ~kind:"LocalStorageTypes"
        ~of_json:LocalStorageType.of_json j
    let to_json v = composed_to_json to_value v
  end
module MemoryGiBPerVCpuRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveDouble.t option ;
      max: NullablePositiveDouble.t option }
    let make ?min = fun ?max -> fun () -> { min; max }
    let to_value x =
      structure_to_value
        [("Min", (Option.map x.min ~f:NullablePositiveDouble.to_value));
        ("Max", (Option.map x.max ~f:NullablePositiveDouble.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveDouble.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        (Option.map ~f:NullablePositiveDouble.of_xml)
          (Xml.child xml_arg0 "Min") in
      make ?max ?min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveDouble.of_json in
      let min = field_map json "Min" NullablePositiveDouble.of_json in
      make ?max ?min ()
    let to_json v = composed_to_json to_value v
  end
module MemoryMiBRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveInteger.t ;
      max: NullablePositiveInteger.t option }
    let context_ = "MemoryMiBRequest"
    let make ?max = fun ~min -> fun () -> { max; min }
    let to_value x =
      structure_to_value
        [("Min", (Some (NullablePositiveInteger.to_value x.min)));
        ("Max", (Option.map x.max ~f:NullablePositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        NullablePositiveInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Min") in
      make ?max ~min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveInteger.of_json in
      let min = field_map_exn json "Min" NullablePositiveInteger.of_json in
      make ?max ~min ()
    let to_json v = composed_to_json to_value v
  end
module NetworkInterfaceCountRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveInteger.t option ;
      max: NullablePositiveInteger.t option }
    let make ?min = fun ?max -> fun () -> { min; max }
    let to_value x =
      structure_to_value
        [("Min", (Option.map x.min ~f:NullablePositiveInteger.to_value));
        ("Max", (Option.map x.max ~f:NullablePositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Min") in
      make ?max ?min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveInteger.of_json in
      let min = field_map json "Min" NullablePositiveInteger.of_json in
      make ?max ?min ()
    let to_json v = composed_to_json to_value v
  end
module NullableBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TotalLocalStorageGBRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveDouble.t option ;
      max: NullablePositiveDouble.t option }
    let make ?min = fun ?max -> fun () -> { min; max }
    let to_value x =
      structure_to_value
        [("Min", (Option.map x.min ~f:NullablePositiveDouble.to_value));
        ("Max", (Option.map x.max ~f:NullablePositiveDouble.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveDouble.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        (Option.map ~f:NullablePositiveDouble.of_xml)
          (Xml.child xml_arg0 "Min") in
      make ?max ?min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveDouble.of_json in
      let min = field_map json "Min" NullablePositiveDouble.of_json in
      make ?max ?min ()
    let to_json v = composed_to_json to_value v
  end
module VCpuCountRequest =
  struct
    type nonrec t =
      {
      min: NullablePositiveInteger.t ;
      max: NullablePositiveInteger.t option }
    let context_ = "VCpuCountRequest"
    let make ?max = fun ~min -> fun () -> { max; min }
    let to_value x =
      structure_to_value
        [("Min", (Some (NullablePositiveInteger.to_value x.min)));
        ("Max", (Option.map x.max ~f:NullablePositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "Max") in
      let min =
        NullablePositiveInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Min") in
      make ?max ~min ()
    let of_json json =
      let max = field_map json "Max" NullablePositiveInteger.of_json in
      let min = field_map_exn json "Min" NullablePositiveInteger.of_json in
      make ?max ~min ()
    let to_json v = composed_to_json to_value v
  end
module LaunchTemplateName =
  struct
    type nonrec t = string
    let context_ = "LaunchTemplateName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9\\(\\)\\.\\-/_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LaunchTemplateName" j
    let to_json = simple_to_json to_value
  end
module MetricDataQuery =
  struct
    type nonrec t =
      {
      id: XmlStringMaxLen255.t ;
      expression: XmlStringMaxLen1023.t option ;
      metricStat: MetricStat.t option ;
      label: XmlStringMetricLabel.t option ;
      returnData: ReturnData.t option }
    let context_ = "MetricDataQuery"
    let make ?expression =
      fun ?metricStat ->
        fun ?label ->
          fun ?returnData ->
            fun ~id ->
              fun () -> { expression; metricStat; label; returnData; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (XmlStringMaxLen255.to_value x.id)));
        ("Expression",
          (Option.map x.expression ~f:XmlStringMaxLen1023.to_value));
        ("MetricStat", (Option.map x.metricStat ~f:MetricStat.to_value));
        ("Label", (Option.map x.label ~f:XmlStringMetricLabel.to_value));
        ("ReturnData", (Option.map x.returnData ~f:ReturnData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnData =
        (Option.map ~f:ReturnData.of_xml) (Xml.child xml_arg0 "ReturnData") in
      let label =
        (Option.map ~f:XmlStringMetricLabel.of_xml)
          (Xml.child xml_arg0 "Label") in
      let metricStat =
        (Option.map ~f:MetricStat.of_xml) (Xml.child xml_arg0 "MetricStat") in
      let expression =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "Expression") in
      let id =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?returnData ?label ?metricStat ?expression ~id ()
    let of_json json =
      let returnData = field_map json "ReturnData" ReturnData.of_json in
      let label = field_map json "Label" XmlStringMetricLabel.of_json in
      let metricStat = field_map json "MetricStat" MetricStat.of_json in
      let expression =
        field_map json "Expression" XmlStringMaxLen1023.of_json in
      let id = field_map_exn json "Id" XmlStringMaxLen255.of_json in
      make ?returnData ?label ?metricStat ?expression ~id ()
    let to_json v = composed_to_json to_value v
  end
module InstanceRequirements =
  struct
    type nonrec t =
      {
      vCpuCount: VCpuCountRequest.t ;
      memoryMiB: MemoryMiBRequest.t ;
      cpuManufacturers: CpuManufacturers.t option ;
      memoryGiBPerVCpu: MemoryGiBPerVCpuRequest.t option ;
      excludedInstanceTypes: ExcludedInstanceTypes.t option ;
      instanceGenerations: InstanceGenerations.t option ;
      spotMaxPricePercentageOverLowestPrice: NullablePositiveInteger.t option ;
      onDemandMaxPricePercentageOverLowestPrice:
        NullablePositiveInteger.t option ;
      bareMetal: BareMetal.t option ;
      burstablePerformance: BurstablePerformance.t option ;
      requireHibernateSupport: NullableBoolean.t option ;
      networkInterfaceCount: NetworkInterfaceCountRequest.t option ;
      localStorage: LocalStorage.t option ;
      localStorageTypes: LocalStorageTypes.t option ;
      totalLocalStorageGB: TotalLocalStorageGBRequest.t option ;
      baselineEbsBandwidthMbps: BaselineEbsBandwidthMbpsRequest.t option ;
      acceleratorTypes: AcceleratorTypes.t option ;
      acceleratorCount: AcceleratorCountRequest.t option ;
      acceleratorManufacturers: AcceleratorManufacturers.t option ;
      acceleratorNames: AcceleratorNames.t option ;
      acceleratorTotalMemoryMiB: AcceleratorTotalMemoryMiBRequest.t option }
    let context_ = "InstanceRequirements"
    let make ?cpuManufacturers =
      fun ?memoryGiBPerVCpu ->
        fun ?excludedInstanceTypes ->
          fun ?instanceGenerations ->
            fun ?spotMaxPricePercentageOverLowestPrice ->
              fun ?onDemandMaxPricePercentageOverLowestPrice ->
                fun ?bareMetal ->
                  fun ?burstablePerformance ->
                    fun ?requireHibernateSupport ->
                      fun ?networkInterfaceCount ->
                        fun ?localStorage ->
                          fun ?localStorageTypes ->
                            fun ?totalLocalStorageGB ->
                              fun ?baselineEbsBandwidthMbps ->
                                fun ?acceleratorTypes ->
                                  fun ?acceleratorCount ->
                                    fun ?acceleratorManufacturers ->
                                      fun ?acceleratorNames ->
                                        fun ?acceleratorTotalMemoryMiB ->
                                          fun ~vCpuCount ->
                                            fun ~memoryMiB ->
                                              fun () ->
                                                {
                                                  cpuManufacturers;
                                                  memoryGiBPerVCpu;
                                                  excludedInstanceTypes;
                                                  instanceGenerations;
                                                  spotMaxPricePercentageOverLowestPrice;
                                                  onDemandMaxPricePercentageOverLowestPrice;
                                                  bareMetal;
                                                  burstablePerformance;
                                                  requireHibernateSupport;
                                                  networkInterfaceCount;
                                                  localStorage;
                                                  localStorageTypes;
                                                  totalLocalStorageGB;
                                                  baselineEbsBandwidthMbps;
                                                  acceleratorTypes;
                                                  acceleratorCount;
                                                  acceleratorManufacturers;
                                                  acceleratorNames;
                                                  acceleratorTotalMemoryMiB;
                                                  vCpuCount;
                                                  memoryMiB
                                                }
    let to_value x =
      structure_to_value
        [("VCpuCount", (Some (VCpuCountRequest.to_value x.vCpuCount)));
        ("MemoryMiB", (Some (MemoryMiBRequest.to_value x.memoryMiB)));
        ("CpuManufacturers",
          (Option.map x.cpuManufacturers ~f:CpuManufacturers.to_value));
        ("MemoryGiBPerVCpu",
          (Option.map x.memoryGiBPerVCpu ~f:MemoryGiBPerVCpuRequest.to_value));
        ("ExcludedInstanceTypes",
          (Option.map x.excludedInstanceTypes
             ~f:ExcludedInstanceTypes.to_value));
        ("InstanceGenerations",
          (Option.map x.instanceGenerations ~f:InstanceGenerations.to_value));
        ("SpotMaxPricePercentageOverLowestPrice",
          (Option.map x.spotMaxPricePercentageOverLowestPrice
             ~f:NullablePositiveInteger.to_value));
        ("OnDemandMaxPricePercentageOverLowestPrice",
          (Option.map x.onDemandMaxPricePercentageOverLowestPrice
             ~f:NullablePositiveInteger.to_value));
        ("BareMetal", (Option.map x.bareMetal ~f:BareMetal.to_value));
        ("BurstablePerformance",
          (Option.map x.burstablePerformance ~f:BurstablePerformance.to_value));
        ("RequireHibernateSupport",
          (Option.map x.requireHibernateSupport ~f:NullableBoolean.to_value));
        ("NetworkInterfaceCount",
          (Option.map x.networkInterfaceCount
             ~f:NetworkInterfaceCountRequest.to_value));
        ("LocalStorage",
          (Option.map x.localStorage ~f:LocalStorage.to_value));
        ("LocalStorageTypes",
          (Option.map x.localStorageTypes ~f:LocalStorageTypes.to_value));
        ("TotalLocalStorageGB",
          (Option.map x.totalLocalStorageGB
             ~f:TotalLocalStorageGBRequest.to_value));
        ("BaselineEbsBandwidthMbps",
          (Option.map x.baselineEbsBandwidthMbps
             ~f:BaselineEbsBandwidthMbpsRequest.to_value));
        ("AcceleratorTypes",
          (Option.map x.acceleratorTypes ~f:AcceleratorTypes.to_value));
        ("AcceleratorCount",
          (Option.map x.acceleratorCount ~f:AcceleratorCountRequest.to_value));
        ("AcceleratorManufacturers",
          (Option.map x.acceleratorManufacturers
             ~f:AcceleratorManufacturers.to_value));
        ("AcceleratorNames",
          (Option.map x.acceleratorNames ~f:AcceleratorNames.to_value));
        ("AcceleratorTotalMemoryMiB",
          (Option.map x.acceleratorTotalMemoryMiB
             ~f:AcceleratorTotalMemoryMiBRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceleratorTotalMemoryMiB =
        (Option.map ~f:AcceleratorTotalMemoryMiBRequest.of_xml)
          (Xml.child xml_arg0 "AcceleratorTotalMemoryMiB") in
      let acceleratorNames =
        (Option.map ~f:AcceleratorNames.of_xml)
          (Xml.child xml_arg0 "AcceleratorNames") in
      let acceleratorManufacturers =
        (Option.map ~f:AcceleratorManufacturers.of_xml)
          (Xml.child xml_arg0 "AcceleratorManufacturers") in
      let acceleratorCount =
        (Option.map ~f:AcceleratorCountRequest.of_xml)
          (Xml.child xml_arg0 "AcceleratorCount") in
      let acceleratorTypes =
        (Option.map ~f:AcceleratorTypes.of_xml)
          (Xml.child xml_arg0 "AcceleratorTypes") in
      let baselineEbsBandwidthMbps =
        (Option.map ~f:BaselineEbsBandwidthMbpsRequest.of_xml)
          (Xml.child xml_arg0 "BaselineEbsBandwidthMbps") in
      let totalLocalStorageGB =
        (Option.map ~f:TotalLocalStorageGBRequest.of_xml)
          (Xml.child xml_arg0 "TotalLocalStorageGB") in
      let localStorageTypes =
        (Option.map ~f:LocalStorageTypes.of_xml)
          (Xml.child xml_arg0 "LocalStorageTypes") in
      let localStorage =
        (Option.map ~f:LocalStorage.of_xml)
          (Xml.child xml_arg0 "LocalStorage") in
      let networkInterfaceCount =
        (Option.map ~f:NetworkInterfaceCountRequest.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceCount") in
      let requireHibernateSupport =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "RequireHibernateSupport") in
      let burstablePerformance =
        (Option.map ~f:BurstablePerformance.of_xml)
          (Xml.child xml_arg0 "BurstablePerformance") in
      let bareMetal =
        (Option.map ~f:BareMetal.of_xml) (Xml.child xml_arg0 "BareMetal") in
      let onDemandMaxPricePercentageOverLowestPrice =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "OnDemandMaxPricePercentageOverLowestPrice") in
      let spotMaxPricePercentageOverLowestPrice =
        (Option.map ~f:NullablePositiveInteger.of_xml)
          (Xml.child xml_arg0 "SpotMaxPricePercentageOverLowestPrice") in
      let instanceGenerations =
        (Option.map ~f:InstanceGenerations.of_xml)
          (Xml.child xml_arg0 "InstanceGenerations") in
      let excludedInstanceTypes =
        (Option.map ~f:ExcludedInstanceTypes.of_xml)
          (Xml.child xml_arg0 "ExcludedInstanceTypes") in
      let memoryGiBPerVCpu =
        (Option.map ~f:MemoryGiBPerVCpuRequest.of_xml)
          (Xml.child xml_arg0 "MemoryGiBPerVCpu") in
      let cpuManufacturers =
        (Option.map ~f:CpuManufacturers.of_xml)
          (Xml.child xml_arg0 "CpuManufacturers") in
      let memoryMiB =
        MemoryMiBRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MemoryMiB") in
      let vCpuCount =
        VCpuCountRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VCpuCount") in
      make ?acceleratorTotalMemoryMiB ?acceleratorNames
        ?acceleratorManufacturers ?acceleratorCount ?acceleratorTypes
        ?baselineEbsBandwidthMbps ?totalLocalStorageGB ?localStorageTypes
        ?localStorage ?networkInterfaceCount ?requireHibernateSupport
        ?burstablePerformance ?bareMetal
        ?onDemandMaxPricePercentageOverLowestPrice
        ?spotMaxPricePercentageOverLowestPrice ?instanceGenerations
        ?excludedInstanceTypes ?memoryGiBPerVCpu ?cpuManufacturers ~memoryMiB
        ~vCpuCount ()
    let of_json json =
      let acceleratorTotalMemoryMiB =
        field_map json "AcceleratorTotalMemoryMiB"
          AcceleratorTotalMemoryMiBRequest.of_json in
      let acceleratorNames =
        field_map json "AcceleratorNames" AcceleratorNames.of_json in
      let acceleratorManufacturers =
        field_map json "AcceleratorManufacturers"
          AcceleratorManufacturers.of_json in
      let acceleratorCount =
        field_map json "AcceleratorCount" AcceleratorCountRequest.of_json in
      let acceleratorTypes =
        field_map json "AcceleratorTypes" AcceleratorTypes.of_json in
      let baselineEbsBandwidthMbps =
        field_map json "BaselineEbsBandwidthMbps"
          BaselineEbsBandwidthMbpsRequest.of_json in
      let totalLocalStorageGB =
        field_map json "TotalLocalStorageGB"
          TotalLocalStorageGBRequest.of_json in
      let localStorageTypes =
        field_map json "LocalStorageTypes" LocalStorageTypes.of_json in
      let localStorage = field_map json "LocalStorage" LocalStorage.of_json in
      let networkInterfaceCount =
        field_map json "NetworkInterfaceCount"
          NetworkInterfaceCountRequest.of_json in
      let requireHibernateSupport =
        field_map json "RequireHibernateSupport" NullableBoolean.of_json in
      let burstablePerformance =
        field_map json "BurstablePerformance" BurstablePerformance.of_json in
      let bareMetal = field_map json "BareMetal" BareMetal.of_json in
      let onDemandMaxPricePercentageOverLowestPrice =
        field_map json "OnDemandMaxPricePercentageOverLowestPrice"
          NullablePositiveInteger.of_json in
      let spotMaxPricePercentageOverLowestPrice =
        field_map json "SpotMaxPricePercentageOverLowestPrice"
          NullablePositiveInteger.of_json in
      let instanceGenerations =
        field_map json "InstanceGenerations" InstanceGenerations.of_json in
      let excludedInstanceTypes =
        field_map json "ExcludedInstanceTypes" ExcludedInstanceTypes.of_json in
      let memoryGiBPerVCpu =
        field_map json "MemoryGiBPerVCpu" MemoryGiBPerVCpuRequest.of_json in
      let cpuManufacturers =
        field_map json "CpuManufacturers" CpuManufacturers.of_json in
      let memoryMiB = field_map_exn json "MemoryMiB" MemoryMiBRequest.of_json in
      let vCpuCount = field_map_exn json "VCpuCount" VCpuCountRequest.of_json in
      make ?acceleratorTotalMemoryMiB ?acceleratorNames
        ?acceleratorManufacturers ?acceleratorCount ?acceleratorTypes
        ?baselineEbsBandwidthMbps ?totalLocalStorageGB ?localStorageTypes
        ?localStorage ?networkInterfaceCount ?requireHibernateSupport
        ?burstablePerformance ?bareMetal
        ?onDemandMaxPricePercentageOverLowestPrice
        ?spotMaxPricePercentageOverLowestPrice ?instanceGenerations
        ?excludedInstanceTypes ?memoryGiBPerVCpu ?cpuManufacturers ~memoryMiB
        ~vCpuCount ()
    let to_json v = composed_to_json to_value v
  end
module LaunchTemplateSpecification =
  struct
    type nonrec t =
      {
      launchTemplateId: XmlStringMaxLen255.t option ;
      launchTemplateName: LaunchTemplateName.t option ;
      version: XmlStringMaxLen255.t option }
    let make ?launchTemplateId =
      fun ?launchTemplateName ->
        fun ?version ->
          fun () -> { launchTemplateId; launchTemplateName; version }
    let to_value x =
      structure_to_value
        [("LaunchTemplateId",
           (Option.map x.launchTemplateId ~f:XmlStringMaxLen255.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("Version", (Option.map x.version ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Version") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      make ?version ?launchTemplateName ?launchTemplateId ()
    let of_json json =
      let version = field_map json "Version" XmlStringMaxLen255.of_json in
      let launchTemplateName =
        field_map json "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json "LaunchTemplateId" XmlStringMaxLen255.of_json in
      make ?version ?launchTemplateName ?launchTemplateId ()
    let to_json v = composed_to_json to_value v
  end
module XmlStringMaxLen32 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen32"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen32" j
    let to_json = simple_to_json to_value
  end
module MetricDataQueries =
  struct
    type nonrec t = MetricDataQuery.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MetricDataQuery.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricDataQuery.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricDataQueries" ~of_json:MetricDataQuery.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module PredefinedLoadMetricType =
  struct
    type nonrec t =
      | ASGTotalCPUUtilization 
      | ASGTotalNetworkIn 
      | ASGTotalNetworkOut 
      | ALBTargetGroupRequestCount 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASGTotalCPUUtilization -> "ASGTotalCPUUtilization"
      | ASGTotalNetworkIn -> "ASGTotalNetworkIn"
      | ASGTotalNetworkOut -> "ASGTotalNetworkOut"
      | ALBTargetGroupRequestCount -> "ALBTargetGroupRequestCount"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASGTotalCPUUtilization" -> ASGTotalCPUUtilization
      | "ASGTotalNetworkIn" -> ASGTotalNetworkIn
      | "ASGTotalNetworkOut" -> ASGTotalNetworkOut
      | "ALBTargetGroupRequestCount" -> ALBTargetGroupRequestCount
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PredefinedLoadMetricType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PredefinedLoadMetricType" j)
    let to_json = simple_to_json to_value
  end
module PredefinedMetricPairType =
  struct
    type nonrec t =
      | ASGCPUUtilization 
      | ASGNetworkIn 
      | ASGNetworkOut 
      | ALBRequestCount 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASGCPUUtilization -> "ASGCPUUtilization"
      | ASGNetworkIn -> "ASGNetworkIn"
      | ASGNetworkOut -> "ASGNetworkOut"
      | ALBRequestCount -> "ALBRequestCount"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASGCPUUtilization" -> ASGCPUUtilization
      | "ASGNetworkIn" -> ASGNetworkIn
      | "ASGNetworkOut" -> ASGNetworkOut
      | "ALBRequestCount" -> ALBRequestCount
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PredefinedMetricPairType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PredefinedMetricPairType" j)
    let to_json = simple_to_json to_value
  end
module PredefinedScalingMetricType =
  struct
    type nonrec t =
      | ASGAverageCPUUtilization 
      | ASGAverageNetworkIn 
      | ASGAverageNetworkOut 
      | ALBRequestCountPerTarget 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASGAverageCPUUtilization -> "ASGAverageCPUUtilization"
      | ASGAverageNetworkIn -> "ASGAverageNetworkIn"
      | ASGAverageNetworkOut -> "ASGAverageNetworkOut"
      | ALBRequestCountPerTarget -> "ALBRequestCountPerTarget"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASGAverageCPUUtilization" -> ASGAverageCPUUtilization
      | "ASGAverageNetworkIn" -> ASGAverageNetworkIn
      | "ASGAverageNetworkOut" -> ASGAverageNetworkOut
      | "ALBRequestCountPerTarget" -> ALBRequestCountPerTarget
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PredefinedScalingMetricType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PredefinedScalingMetricType" j)
    let to_json = simple_to_json to_value
  end
module LaunchTemplateOverrides =
  struct
    type nonrec t =
      {
      instanceType: XmlStringMaxLen255.t option ;
      weightedCapacity: XmlStringMaxLen32.t option ;
      launchTemplateSpecification: LaunchTemplateSpecification.t option ;
      instanceRequirements: InstanceRequirements.t option }
    let make ?instanceType =
      fun ?weightedCapacity ->
        fun ?launchTemplateSpecification ->
          fun ?instanceRequirements ->
            fun () ->
              {
                instanceType;
                weightedCapacity;
                launchTemplateSpecification;
                instanceRequirements
              }
    let to_value x =
      structure_to_value
        [("InstanceType",
           (Option.map x.instanceType ~f:XmlStringMaxLen255.to_value));
        ("WeightedCapacity",
          (Option.map x.weightedCapacity ~f:XmlStringMaxLen32.to_value));
        ("LaunchTemplateSpecification",
          (Option.map x.launchTemplateSpecification
             ~f:LaunchTemplateSpecification.to_value));
        ("InstanceRequirements",
          (Option.map x.instanceRequirements ~f:InstanceRequirements.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceRequirements =
        (Option.map ~f:InstanceRequirements.of_xml)
          (Xml.child xml_arg0 "InstanceRequirements") in
      let launchTemplateSpecification =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateSpecification") in
      let weightedCapacity =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "WeightedCapacity") in
      let instanceType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      make ?instanceRequirements ?launchTemplateSpecification
        ?weightedCapacity ?instanceType ()
    let of_json json =
      let instanceRequirements =
        field_map json "InstanceRequirements" InstanceRequirements.of_json in
      let launchTemplateSpecification =
        field_map json "LaunchTemplateSpecification"
          LaunchTemplateSpecification.of_json in
      let weightedCapacity =
        field_map json "WeightedCapacity" XmlStringMaxLen32.of_json in
      let instanceType =
        field_map json "InstanceType" XmlStringMaxLen255.of_json in
      make ?instanceRequirements ?launchTemplateSpecification
        ?weightedCapacity ?instanceType ()
    let to_json v = composed_to_json to_value v
  end
module MetricScale =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module PredictiveScalingCustomizedCapacityMetric =
  struct
    type nonrec t = {
      metricDataQueries: MetricDataQueries.t }
    let context_ = "PredictiveScalingCustomizedCapacityMetric"
    let make ~metricDataQueries = fun () -> { metricDataQueries }
    let to_value x =
      structure_to_value
        [("MetricDataQueries",
           (Some (MetricDataQueries.to_value x.metricDataQueries)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricDataQueries =
        MetricDataQueries.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricDataQueries") in
      make ~metricDataQueries ()
    let of_json json =
      let metricDataQueries =
        field_map_exn json "MetricDataQueries" MetricDataQueries.of_json in
      make ~metricDataQueries ()
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingCustomizedLoadMetric =
  struct
    type nonrec t = {
      metricDataQueries: MetricDataQueries.t }
    let context_ = "PredictiveScalingCustomizedLoadMetric"
    let make ~metricDataQueries = fun () -> { metricDataQueries }
    let to_value x =
      structure_to_value
        [("MetricDataQueries",
           (Some (MetricDataQueries.to_value x.metricDataQueries)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricDataQueries =
        MetricDataQueries.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricDataQueries") in
      make ~metricDataQueries ()
    let of_json json =
      let metricDataQueries =
        field_map_exn json "MetricDataQueries" MetricDataQueries.of_json in
      make ~metricDataQueries ()
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingCustomizedScalingMetric =
  struct
    type nonrec t = {
      metricDataQueries: MetricDataQueries.t }
    let context_ = "PredictiveScalingCustomizedScalingMetric"
    let make ~metricDataQueries = fun () -> { metricDataQueries }
    let to_value x =
      structure_to_value
        [("MetricDataQueries",
           (Some (MetricDataQueries.to_value x.metricDataQueries)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricDataQueries =
        MetricDataQueries.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricDataQueries") in
      make ~metricDataQueries ()
    let of_json json =
      let metricDataQueries =
        field_map_exn json "MetricDataQueries" MetricDataQueries.of_json in
      make ~metricDataQueries ()
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingPredefinedLoadMetric =
  struct
    type nonrec t =
      {
      predefinedMetricType: PredefinedLoadMetricType.t ;
      resourceLabel: XmlStringMaxLen1023.t option }
    let context_ = "PredictiveScalingPredefinedLoadMetric"
    let make ?resourceLabel =
      fun ~predefinedMetricType ->
        fun () -> { resourceLabel; predefinedMetricType }
    let to_value x =
      structure_to_value
        [("PredefinedMetricType",
           (Some (PredefinedLoadMetricType.to_value x.predefinedMetricType)));
        ("ResourceLabel",
          (Option.map x.resourceLabel ~f:XmlStringMaxLen1023.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceLabel =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "ResourceLabel") in
      let predefinedMetricType =
        PredefinedLoadMetricType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PredefinedMetricType") in
      make ?resourceLabel ~predefinedMetricType ()
    let of_json json =
      let resourceLabel =
        field_map json "ResourceLabel" XmlStringMaxLen1023.of_json in
      let predefinedMetricType =
        field_map_exn json "PredefinedMetricType"
          PredefinedLoadMetricType.of_json in
      make ?resourceLabel ~predefinedMetricType ()
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingPredefinedMetricPair =
  struct
    type nonrec t =
      {
      predefinedMetricType: PredefinedMetricPairType.t ;
      resourceLabel: XmlStringMaxLen1023.t option }
    let context_ = "PredictiveScalingPredefinedMetricPair"
    let make ?resourceLabel =
      fun ~predefinedMetricType ->
        fun () -> { resourceLabel; predefinedMetricType }
    let to_value x =
      structure_to_value
        [("PredefinedMetricType",
           (Some (PredefinedMetricPairType.to_value x.predefinedMetricType)));
        ("ResourceLabel",
          (Option.map x.resourceLabel ~f:XmlStringMaxLen1023.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceLabel =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "ResourceLabel") in
      let predefinedMetricType =
        PredefinedMetricPairType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PredefinedMetricType") in
      make ?resourceLabel ~predefinedMetricType ()
    let of_json json =
      let resourceLabel =
        field_map json "ResourceLabel" XmlStringMaxLen1023.of_json in
      let predefinedMetricType =
        field_map_exn json "PredefinedMetricType"
          PredefinedMetricPairType.of_json in
      make ?resourceLabel ~predefinedMetricType ()
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingPredefinedScalingMetric =
  struct
    type nonrec t =
      {
      predefinedMetricType: PredefinedScalingMetricType.t ;
      resourceLabel: XmlStringMaxLen1023.t option }
    let context_ = "PredictiveScalingPredefinedScalingMetric"
    let make ?resourceLabel =
      fun ~predefinedMetricType ->
        fun () -> { resourceLabel; predefinedMetricType }
    let to_value x =
      structure_to_value
        [("PredefinedMetricType",
           (Some
              (PredefinedScalingMetricType.to_value x.predefinedMetricType)));
        ("ResourceLabel",
          (Option.map x.resourceLabel ~f:XmlStringMaxLen1023.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceLabel =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "ResourceLabel") in
      let predefinedMetricType =
        PredefinedScalingMetricType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PredefinedMetricType") in
      make ?resourceLabel ~predefinedMetricType ()
    let of_json json =
      let resourceLabel =
        field_map json "ResourceLabel" XmlStringMaxLen1023.of_json in
      let predefinedMetricType =
        field_map_exn json "PredefinedMetricType"
          PredefinedScalingMetricType.of_json in
      make ?resourceLabel ~predefinedMetricType ()
    let to_json v = composed_to_json to_value v
  end
module BlockDeviceEbsDeleteOnTermination =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module BlockDeviceEbsEncrypted =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module BlockDeviceEbsIops =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:20000) >>=
             (fun () -> check_int_min i ~min:100));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BlockDeviceEbsIops" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BlockDeviceEbsThroughput =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>=
             (fun () -> check_int_min i ~min:125));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BlockDeviceEbsThroughput"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BlockDeviceEbsVolumeSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:16384) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BlockDeviceEbsVolumeSize"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BlockDeviceEbsVolumeType =
  struct
    type nonrec t = string
    let context_ = "BlockDeviceEbsVolumeType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BlockDeviceEbsVolumeType" j
    let to_json = simple_to_json to_value
  end
module MixedInstanceSpotPrice =
  struct
    type nonrec t = string
    let context_ = "MixedInstanceSpotPrice"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MixedInstanceSpotPrice" j
    let to_json = simple_to_json to_value
  end
module OnDemandBaseCapacity =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for OnDemandBaseCapacity" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module OnDemandPercentageAboveBaseCapacity =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for OnDemandPercentageAboveBaseCapacity"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SpotInstancePools =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SpotInstancePools" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module XmlString =
  struct
    type nonrec t = string
    let context_ = "XmlString"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlString" j
    let to_json = simple_to_json to_value
  end
module Overrides =
  struct
    type nonrec t = LaunchTemplateOverrides.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LaunchTemplateOverrides.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LaunchTemplateOverrides.of_xml)
    let of_json j =
      list_of_json ~kind:"Overrides" ~of_json:LaunchTemplateOverrides.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1600) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module PredictiveScalingMetricSpecification =
  struct
    type nonrec t =
      {
      targetValue: MetricScale.t ;
      predefinedMetricPairSpecification:
        PredictiveScalingPredefinedMetricPair.t option ;
      predefinedScalingMetricSpecification:
        PredictiveScalingPredefinedScalingMetric.t option ;
      predefinedLoadMetricSpecification:
        PredictiveScalingPredefinedLoadMetric.t option ;
      customizedScalingMetricSpecification:
        PredictiveScalingCustomizedScalingMetric.t option ;
      customizedLoadMetricSpecification:
        PredictiveScalingCustomizedLoadMetric.t option ;
      customizedCapacityMetricSpecification:
        PredictiveScalingCustomizedCapacityMetric.t option }
    let context_ = "PredictiveScalingMetricSpecification"
    let make ?predefinedMetricPairSpecification =
      fun ?predefinedScalingMetricSpecification ->
        fun ?predefinedLoadMetricSpecification ->
          fun ?customizedScalingMetricSpecification ->
            fun ?customizedLoadMetricSpecification ->
              fun ?customizedCapacityMetricSpecification ->
                fun ~targetValue ->
                  fun () ->
                    {
                      predefinedMetricPairSpecification;
                      predefinedScalingMetricSpecification;
                      predefinedLoadMetricSpecification;
                      customizedScalingMetricSpecification;
                      customizedLoadMetricSpecification;
                      customizedCapacityMetricSpecification;
                      targetValue
                    }
    let to_value x =
      structure_to_value
        [("TargetValue", (Some (MetricScale.to_value x.targetValue)));
        ("PredefinedMetricPairSpecification",
          (Option.map x.predefinedMetricPairSpecification
             ~f:PredictiveScalingPredefinedMetricPair.to_value));
        ("PredefinedScalingMetricSpecification",
          (Option.map x.predefinedScalingMetricSpecification
             ~f:PredictiveScalingPredefinedScalingMetric.to_value));
        ("PredefinedLoadMetricSpecification",
          (Option.map x.predefinedLoadMetricSpecification
             ~f:PredictiveScalingPredefinedLoadMetric.to_value));
        ("CustomizedScalingMetricSpecification",
          (Option.map x.customizedScalingMetricSpecification
             ~f:PredictiveScalingCustomizedScalingMetric.to_value));
        ("CustomizedLoadMetricSpecification",
          (Option.map x.customizedLoadMetricSpecification
             ~f:PredictiveScalingCustomizedLoadMetric.to_value));
        ("CustomizedCapacityMetricSpecification",
          (Option.map x.customizedCapacityMetricSpecification
             ~f:PredictiveScalingCustomizedCapacityMetric.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customizedCapacityMetricSpecification =
        (Option.map ~f:PredictiveScalingCustomizedCapacityMetric.of_xml)
          (Xml.child xml_arg0 "CustomizedCapacityMetricSpecification") in
      let customizedLoadMetricSpecification =
        (Option.map ~f:PredictiveScalingCustomizedLoadMetric.of_xml)
          (Xml.child xml_arg0 "CustomizedLoadMetricSpecification") in
      let customizedScalingMetricSpecification =
        (Option.map ~f:PredictiveScalingCustomizedScalingMetric.of_xml)
          (Xml.child xml_arg0 "CustomizedScalingMetricSpecification") in
      let predefinedLoadMetricSpecification =
        (Option.map ~f:PredictiveScalingPredefinedLoadMetric.of_xml)
          (Xml.child xml_arg0 "PredefinedLoadMetricSpecification") in
      let predefinedScalingMetricSpecification =
        (Option.map ~f:PredictiveScalingPredefinedScalingMetric.of_xml)
          (Xml.child xml_arg0 "PredefinedScalingMetricSpecification") in
      let predefinedMetricPairSpecification =
        (Option.map ~f:PredictiveScalingPredefinedMetricPair.of_xml)
          (Xml.child xml_arg0 "PredefinedMetricPairSpecification") in
      let targetValue =
        MetricScale.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetValue") in
      make ?customizedCapacityMetricSpecification
        ?customizedLoadMetricSpecification
        ?customizedScalingMetricSpecification
        ?predefinedLoadMetricSpecification
        ?predefinedScalingMetricSpecification
        ?predefinedMetricPairSpecification ~targetValue ()
    let of_json json =
      let customizedCapacityMetricSpecification =
        field_map json "CustomizedCapacityMetricSpecification"
          PredictiveScalingCustomizedCapacityMetric.of_json in
      let customizedLoadMetricSpecification =
        field_map json "CustomizedLoadMetricSpecification"
          PredictiveScalingCustomizedLoadMetric.of_json in
      let customizedScalingMetricSpecification =
        field_map json "CustomizedScalingMetricSpecification"
          PredictiveScalingCustomizedScalingMetric.of_json in
      let predefinedLoadMetricSpecification =
        field_map json "PredefinedLoadMetricSpecification"
          PredictiveScalingPredefinedLoadMetric.of_json in
      let predefinedScalingMetricSpecification =
        field_map json "PredefinedScalingMetricSpecification"
          PredictiveScalingPredefinedScalingMetric.of_json in
      let predefinedMetricPairSpecification =
        field_map json "PredefinedMetricPairSpecification"
          PredictiveScalingPredefinedMetricPair.of_json in
      let targetValue = field_map_exn json "TargetValue" MetricScale.of_json in
      make ?customizedCapacityMetricSpecification
        ?customizedLoadMetricSpecification
        ?customizedScalingMetricSpecification
        ?predefinedLoadMetricSpecification
        ?predefinedScalingMetricSpecification
        ?predefinedMetricPairSpecification ~targetValue ()
    let to_json v = composed_to_json to_value v
  end
module PolicyIncrement =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PolicyIncrement" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MetricStatistic =
  struct
    type nonrec t =
      | Average 
      | Minimum 
      | Maximum 
      | SampleCount 
      | Sum 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Average -> "Average"
      | Minimum -> "Minimum"
      | Maximum -> "Maximum"
      | SampleCount -> "SampleCount"
      | Sum -> "Sum"
      | Non_static_id s -> s
    let of_string =
      function
      | "Average" -> Average
      | "Minimum" -> Minimum
      | "Maximum" -> Maximum
      | "SampleCount" -> SampleCount
      | "Sum" -> Sum
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MetricStatistic" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MetricStatistic" j)
    let to_json = simple_to_json to_value
  end
module MetricType =
  struct
    type nonrec t =
      | ASGAverageCPUUtilization 
      | ASGAverageNetworkIn 
      | ASGAverageNetworkOut 
      | ALBRequestCountPerTarget 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASGAverageCPUUtilization -> "ASGAverageCPUUtilization"
      | ASGAverageNetworkIn -> "ASGAverageNetworkIn"
      | ASGAverageNetworkOut -> "ASGAverageNetworkOut"
      | ALBRequestCountPerTarget -> "ALBRequestCountPerTarget"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASGAverageCPUUtilization" -> ASGAverageCPUUtilization
      | "ASGAverageNetworkIn" -> ASGAverageNetworkIn
      | "ASGAverageNetworkOut" -> ASGAverageNetworkOut
      | "ALBRequestCountPerTarget" -> ALBRequestCountPerTarget
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MetricType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MetricType" j)
    let to_json = simple_to_json to_value
  end
module Ebs =
  struct
    type nonrec t =
      {
      snapshotId: XmlStringMaxLen255.t option ;
      volumeSize: BlockDeviceEbsVolumeSize.t option ;
      volumeType: BlockDeviceEbsVolumeType.t option ;
      deleteOnTermination: BlockDeviceEbsDeleteOnTermination.t option ;
      iops: BlockDeviceEbsIops.t option ;
      encrypted: BlockDeviceEbsEncrypted.t option ;
      throughput: BlockDeviceEbsThroughput.t option }
    let make ?snapshotId =
      fun ?volumeSize ->
        fun ?volumeType ->
          fun ?deleteOnTermination ->
            fun ?iops ->
              fun ?encrypted ->
                fun ?throughput ->
                  fun () ->
                    {
                      snapshotId;
                      volumeSize;
                      volumeType;
                      deleteOnTermination;
                      iops;
                      encrypted;
                      throughput
                    }
    let to_value x =
      structure_to_value
        [("SnapshotId",
           (Option.map x.snapshotId ~f:XmlStringMaxLen255.to_value));
        ("VolumeSize",
          (Option.map x.volumeSize ~f:BlockDeviceEbsVolumeSize.to_value));
        ("VolumeType",
          (Option.map x.volumeType ~f:BlockDeviceEbsVolumeType.to_value));
        ("DeleteOnTermination",
          (Option.map x.deleteOnTermination
             ~f:BlockDeviceEbsDeleteOnTermination.to_value));
        ("Iops", (Option.map x.iops ~f:BlockDeviceEbsIops.to_value));
        ("Encrypted",
          (Option.map x.encrypted ~f:BlockDeviceEbsEncrypted.to_value));
        ("Throughput",
          (Option.map x.throughput ~f:BlockDeviceEbsThroughput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let throughput =
        (Option.map ~f:BlockDeviceEbsThroughput.of_xml)
          (Xml.child xml_arg0 "Throughput") in
      let encrypted =
        (Option.map ~f:BlockDeviceEbsEncrypted.of_xml)
          (Xml.child xml_arg0 "Encrypted") in
      let iops =
        (Option.map ~f:BlockDeviceEbsIops.of_xml) (Xml.child xml_arg0 "Iops") in
      let deleteOnTermination =
        (Option.map ~f:BlockDeviceEbsDeleteOnTermination.of_xml)
          (Xml.child xml_arg0 "DeleteOnTermination") in
      let volumeType =
        (Option.map ~f:BlockDeviceEbsVolumeType.of_xml)
          (Xml.child xml_arg0 "VolumeType") in
      let volumeSize =
        (Option.map ~f:BlockDeviceEbsVolumeSize.of_xml)
          (Xml.child xml_arg0 "VolumeSize") in
      let snapshotId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "SnapshotId") in
      make ?throughput ?encrypted ?iops ?deleteOnTermination ?volumeType
        ?volumeSize ?snapshotId ()
    let of_json json =
      let throughput =
        field_map json "Throughput" BlockDeviceEbsThroughput.of_json in
      let encrypted =
        field_map json "Encrypted" BlockDeviceEbsEncrypted.of_json in
      let iops = field_map json "Iops" BlockDeviceEbsIops.of_json in
      let deleteOnTermination =
        field_map json "DeleteOnTermination"
          BlockDeviceEbsDeleteOnTermination.of_json in
      let volumeType =
        field_map json "VolumeType" BlockDeviceEbsVolumeType.of_json in
      let volumeSize =
        field_map json "VolumeSize" BlockDeviceEbsVolumeSize.of_json in
      let snapshotId = field_map json "SnapshotId" XmlStringMaxLen255.of_json in
      make ?throughput ?encrypted ?iops ?deleteOnTermination ?volumeType
        ?volumeSize ?snapshotId ()
    let to_json v = composed_to_json to_value v
  end
module NoDevice =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module InstancesDistribution =
  struct
    type nonrec t =
      {
      onDemandAllocationStrategy: XmlString.t option ;
      onDemandBaseCapacity: OnDemandBaseCapacity.t option ;
      onDemandPercentageAboveBaseCapacity:
        OnDemandPercentageAboveBaseCapacity.t option ;
      spotAllocationStrategy: XmlString.t option ;
      spotInstancePools: SpotInstancePools.t option ;
      spotMaxPrice: MixedInstanceSpotPrice.t option }
    let make ?onDemandAllocationStrategy =
      fun ?onDemandBaseCapacity ->
        fun ?onDemandPercentageAboveBaseCapacity ->
          fun ?spotAllocationStrategy ->
            fun ?spotInstancePools ->
              fun ?spotMaxPrice ->
                fun () ->
                  {
                    onDemandAllocationStrategy;
                    onDemandBaseCapacity;
                    onDemandPercentageAboveBaseCapacity;
                    spotAllocationStrategy;
                    spotInstancePools;
                    spotMaxPrice
                  }
    let to_value x =
      structure_to_value
        [("OnDemandAllocationStrategy",
           (Option.map x.onDemandAllocationStrategy ~f:XmlString.to_value));
        ("OnDemandBaseCapacity",
          (Option.map x.onDemandBaseCapacity ~f:OnDemandBaseCapacity.to_value));
        ("OnDemandPercentageAboveBaseCapacity",
          (Option.map x.onDemandPercentageAboveBaseCapacity
             ~f:OnDemandPercentageAboveBaseCapacity.to_value));
        ("SpotAllocationStrategy",
          (Option.map x.spotAllocationStrategy ~f:XmlString.to_value));
        ("SpotInstancePools",
          (Option.map x.spotInstancePools ~f:SpotInstancePools.to_value));
        ("SpotMaxPrice",
          (Option.map x.spotMaxPrice ~f:MixedInstanceSpotPrice.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotMaxPrice =
        (Option.map ~f:MixedInstanceSpotPrice.of_xml)
          (Xml.child xml_arg0 "SpotMaxPrice") in
      let spotInstancePools =
        (Option.map ~f:SpotInstancePools.of_xml)
          (Xml.child xml_arg0 "SpotInstancePools") in
      let spotAllocationStrategy =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "SpotAllocationStrategy") in
      let onDemandPercentageAboveBaseCapacity =
        (Option.map ~f:OnDemandPercentageAboveBaseCapacity.of_xml)
          (Xml.child xml_arg0 "OnDemandPercentageAboveBaseCapacity") in
      let onDemandBaseCapacity =
        (Option.map ~f:OnDemandBaseCapacity.of_xml)
          (Xml.child xml_arg0 "OnDemandBaseCapacity") in
      let onDemandAllocationStrategy =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "OnDemandAllocationStrategy") in
      make ?spotMaxPrice ?spotInstancePools ?spotAllocationStrategy
        ?onDemandPercentageAboveBaseCapacity ?onDemandBaseCapacity
        ?onDemandAllocationStrategy ()
    let of_json json =
      let spotMaxPrice =
        field_map json "SpotMaxPrice" MixedInstanceSpotPrice.of_json in
      let spotInstancePools =
        field_map json "SpotInstancePools" SpotInstancePools.of_json in
      let spotAllocationStrategy =
        field_map json "SpotAllocationStrategy" XmlString.of_json in
      let onDemandPercentageAboveBaseCapacity =
        field_map json "OnDemandPercentageAboveBaseCapacity"
          OnDemandPercentageAboveBaseCapacity.of_json in
      let onDemandBaseCapacity =
        field_map json "OnDemandBaseCapacity" OnDemandBaseCapacity.of_json in
      let onDemandAllocationStrategy =
        field_map json "OnDemandAllocationStrategy" XmlString.of_json in
      make ?spotMaxPrice ?spotInstancePools ?spotAllocationStrategy
        ?onDemandPercentageAboveBaseCapacity ?onDemandBaseCapacity
        ?onDemandAllocationStrategy ()
    let to_json v = composed_to_json to_value v
  end
module LaunchTemplate =
  struct
    type nonrec t =
      {
      launchTemplateSpecification: LaunchTemplateSpecification.t option ;
      overrides: Overrides.t option }
    let make ?launchTemplateSpecification =
      fun ?overrides -> fun () -> { launchTemplateSpecification; overrides }
    let to_value x =
      structure_to_value
        [("LaunchTemplateSpecification",
           (Option.map x.launchTemplateSpecification
              ~f:LaunchTemplateSpecification.to_value));
        ("Overrides", (Option.map x.overrides ~f:Overrides.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let overrides =
        (Option.map ~f:Overrides.of_xml) (Xml.child xml_arg0 "Overrides") in
      let launchTemplateSpecification =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateSpecification") in
      make ?overrides ?launchTemplateSpecification ()
    let of_json json =
      let overrides = field_map json "Overrides" Overrides.of_json in
      let launchTemplateSpecification =
        field_map json "LaunchTemplateSpecification"
          LaunchTemplateSpecification.of_json in
      make ?overrides ?launchTemplateSpecification ()
    let to_json v = composed_to_json to_value v
  end
module InstancesToUpdate =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for InstancesToUpdate" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IntPercent =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for IntPercent" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NonZeroIntPercent =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NonZeroIntPercent" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InstanceProtected =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module LifecycleState =
  struct
    type nonrec t =
      | Pending 
      | Pending_Wait 
      | Pending_Proceed 
      | Quarantined 
      | InService 
      | Terminating 
      | Terminating_Wait 
      | Terminating_Proceed 
      | Terminated 
      | Detaching 
      | Detached 
      | EnteringStandby 
      | Standby 
      | Warmed_Pending 
      | Warmed_Pending_Wait 
      | Warmed_Pending_Proceed 
      | Warmed_Terminating 
      | Warmed_Terminating_Wait 
      | Warmed_Terminating_Proceed 
      | Warmed_Terminated 
      | Warmed_Stopped 
      | Warmed_Running 
      | Warmed_Hibernated 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "Pending"
      | Pending_Wait -> "Pending:Wait"
      | Pending_Proceed -> "Pending:Proceed"
      | Quarantined -> "Quarantined"
      | InService -> "InService"
      | Terminating -> "Terminating"
      | Terminating_Wait -> "Terminating:Wait"
      | Terminating_Proceed -> "Terminating:Proceed"
      | Terminated -> "Terminated"
      | Detaching -> "Detaching"
      | Detached -> "Detached"
      | EnteringStandby -> "EnteringStandby"
      | Standby -> "Standby"
      | Warmed_Pending -> "Warmed:Pending"
      | Warmed_Pending_Wait -> "Warmed:Pending:Wait"
      | Warmed_Pending_Proceed -> "Warmed:Pending:Proceed"
      | Warmed_Terminating -> "Warmed:Terminating"
      | Warmed_Terminating_Wait -> "Warmed:Terminating:Wait"
      | Warmed_Terminating_Proceed -> "Warmed:Terminating:Proceed"
      | Warmed_Terminated -> "Warmed:Terminated"
      | Warmed_Stopped -> "Warmed:Stopped"
      | Warmed_Running -> "Warmed:Running"
      | Warmed_Hibernated -> "Warmed:Hibernated"
      | Non_static_id s -> s
    let of_string =
      function
      | "Pending" -> Pending
      | "Pending:Wait" -> Pending_Wait
      | "Pending:Proceed" -> Pending_Proceed
      | "Quarantined" -> Quarantined
      | "InService" -> InService
      | "Terminating" -> Terminating
      | "Terminating:Wait" -> Terminating_Wait
      | "Terminating:Proceed" -> Terminating_Proceed
      | "Terminated" -> Terminated
      | "Detaching" -> Detaching
      | "Detached" -> Detached
      | "EnteringStandby" -> EnteringStandby
      | "Standby" -> Standby
      | "Warmed:Pending" -> Warmed_Pending
      | "Warmed:Pending:Wait" -> Warmed_Pending_Wait
      | "Warmed:Pending:Proceed" -> Warmed_Pending_Proceed
      | "Warmed:Terminating" -> Warmed_Terminating
      | "Warmed:Terminating:Wait" -> Warmed_Terminating_Wait
      | "Warmed:Terminating:Proceed" -> Warmed_Terminating_Proceed
      | "Warmed:Terminated" -> Warmed_Terminated
      | "Warmed:Stopped" -> Warmed_Stopped
      | "Warmed:Running" -> Warmed_Running
      | "Warmed:Hibernated" -> Warmed_Hibernated
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LifecycleState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LifecycleState" j)
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen19 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen19"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:19) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen19" j
    let to_json = simple_to_json to_value
  end
module PropagateAtLaunch =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ReuseOnScaleIn =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Alarm =
  struct
    type nonrec t =
      {
      alarmName: XmlStringMaxLen255.t option ;
      alarmARN: ResourceName.t option }
    let make ?alarmName = fun ?alarmARN -> fun () -> { alarmName; alarmARN }
    let to_value x =
      structure_to_value
        [("AlarmName",
           (Option.map x.alarmName ~f:XmlStringMaxLen255.to_value));
        ("AlarmARN", (Option.map x.alarmARN ~f:ResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let alarmARN =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "AlarmARN") in
      let alarmName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AlarmName") in
      make ?alarmARN ?alarmName ()
    let of_json json =
      let alarmARN = field_map json "AlarmARN" ResourceName.of_json in
      let alarmName = field_map json "AlarmName" XmlStringMaxLen255.of_json in
      make ?alarmARN ?alarmName ()
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingMaxCapacityBreachBehavior =
  struct
    type nonrec t =
      | HonorMaxCapacity 
      | IncreaseMaxCapacity 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HonorMaxCapacity -> "HonorMaxCapacity"
      | IncreaseMaxCapacity -> "IncreaseMaxCapacity"
      | Non_static_id s -> s
    let of_string =
      function
      | "HonorMaxCapacity" -> HonorMaxCapacity
      | "IncreaseMaxCapacity" -> IncreaseMaxCapacity
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration PredictiveScalingMaxCapacityBreachBehavior"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"PredictiveScalingMaxCapacityBreachBehavior" j)
    let to_json = simple_to_json to_value
  end
module PredictiveScalingMaxCapacityBuffer =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PredictiveScalingMaxCapacityBuffer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PredictiveScalingMetricSpecifications =
  struct
    type nonrec t = PredictiveScalingMetricSpecification.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PredictiveScalingMetricSpecification.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:PredictiveScalingMetricSpecification.of_xml)
    let of_json j =
      list_of_json ~kind:"PredictiveScalingMetricSpecifications"
        ~of_json:PredictiveScalingMetricSpecification.of_json j
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingMode =
  struct
    type nonrec t =
      | ForecastAndScale 
      | ForecastOnly 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ForecastAndScale -> "ForecastAndScale"
      | ForecastOnly -> "ForecastOnly"
      | Non_static_id s -> s
    let of_string =
      function
      | "ForecastAndScale" -> ForecastAndScale
      | "ForecastOnly" -> ForecastOnly
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PredictiveScalingMode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PredictiveScalingMode" j)
    let to_json = simple_to_json to_value
  end
module PredictiveScalingSchedulingBufferTime =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PredictiveScalingSchedulingBufferTime"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StepAdjustment =
  struct
    type nonrec t =
      {
      metricIntervalLowerBound: MetricScale.t option ;
      metricIntervalUpperBound: MetricScale.t option ;
      scalingAdjustment: PolicyIncrement.t }
    let context_ = "StepAdjustment"
    let make ?metricIntervalLowerBound =
      fun ?metricIntervalUpperBound ->
        fun ~scalingAdjustment ->
          fun () ->
            {
              metricIntervalLowerBound;
              metricIntervalUpperBound;
              scalingAdjustment
            }
    let to_value x =
      structure_to_value
        [("MetricIntervalLowerBound",
           (Option.map x.metricIntervalLowerBound ~f:MetricScale.to_value));
        ("MetricIntervalUpperBound",
          (Option.map x.metricIntervalUpperBound ~f:MetricScale.to_value));
        ("ScalingAdjustment",
          (Some (PolicyIncrement.to_value x.scalingAdjustment)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scalingAdjustment =
        PolicyIncrement.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScalingAdjustment") in
      let metricIntervalUpperBound =
        (Option.map ~f:MetricScale.of_xml)
          (Xml.child xml_arg0 "MetricIntervalUpperBound") in
      let metricIntervalLowerBound =
        (Option.map ~f:MetricScale.of_xml)
          (Xml.child xml_arg0 "MetricIntervalLowerBound") in
      make ~scalingAdjustment ?metricIntervalUpperBound
        ?metricIntervalLowerBound ()
    let of_json json =
      let scalingAdjustment =
        field_map_exn json "ScalingAdjustment" PolicyIncrement.of_json in
      let metricIntervalUpperBound =
        field_map json "MetricIntervalUpperBound" MetricScale.of_json in
      let metricIntervalLowerBound =
        field_map json "MetricIntervalLowerBound" MetricScale.of_json in
      make ~scalingAdjustment ?metricIntervalUpperBound
        ?metricIntervalLowerBound ()
    let to_json v = composed_to_json to_value v
  end
module CustomizedMetricSpecification =
  struct
    type nonrec t =
      {
      metricName: MetricName.t ;
      namespace: MetricNamespace.t ;
      dimensions: MetricDimensions.t option ;
      statistic: MetricStatistic.t ;
      unit: MetricUnit.t option }
    let context_ = "CustomizedMetricSpecification"
    let make ?dimensions =
      fun ?unit ->
        fun ~metricName ->
          fun ~namespace ->
            fun ~statistic ->
              fun () ->
                { dimensions; unit; metricName; namespace; statistic }
    let to_value x =
      structure_to_value
        [("MetricName", (Some (MetricName.to_value x.metricName)));
        ("Namespace", (Some (MetricNamespace.to_value x.namespace)));
        ("Dimensions",
          (Option.map x.dimensions ~f:MetricDimensions.to_value));
        ("Statistic", (Some (MetricStatistic.to_value x.statistic)));
        ("Unit", (Option.map x.unit ~f:MetricUnit.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unit =
        (Option.map ~f:MetricUnit.of_xml) (Xml.child xml_arg0 "Unit") in
      let statistic =
        MetricStatistic.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Statistic") in
      let dimensions =
        (Option.map ~f:MetricDimensions.of_xml)
          (Xml.child xml_arg0 "Dimensions") in
      let namespace =
        MetricNamespace.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Namespace") in
      let metricName =
        MetricName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricName") in
      make ?unit ~statistic ?dimensions ~namespace ~metricName ()
    let of_json json =
      let unit = field_map json "Unit" MetricUnit.of_json in
      let statistic = field_map_exn json "Statistic" MetricStatistic.of_json in
      let dimensions = field_map json "Dimensions" MetricDimensions.of_json in
      let namespace = field_map_exn json "Namespace" MetricNamespace.of_json in
      let metricName = field_map_exn json "MetricName" MetricName.of_json in
      make ?unit ~statistic ?dimensions ~namespace ~metricName ()
    let to_json v = composed_to_json to_value v
  end
module DisableScaleIn =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module PredefinedMetricSpecification =
  struct
    type nonrec t =
      {
      predefinedMetricType: MetricType.t ;
      resourceLabel: XmlStringMaxLen1023.t option }
    let context_ = "PredefinedMetricSpecification"
    let make ?resourceLabel =
      fun ~predefinedMetricType ->
        fun () -> { resourceLabel; predefinedMetricType }
    let to_value x =
      structure_to_value
        [("PredefinedMetricType",
           (Some (MetricType.to_value x.predefinedMetricType)));
        ("ResourceLabel",
          (Option.map x.resourceLabel ~f:XmlStringMaxLen1023.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceLabel =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "ResourceLabel") in
      let predefinedMetricType =
        MetricType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PredefinedMetricType") in
      make ?resourceLabel ~predefinedMetricType ()
    let of_json json =
      let resourceLabel =
        field_map json "ResourceLabel" XmlStringMaxLen1023.of_json in
      let predefinedMetricType =
        field_map_exn json "PredefinedMetricType" MetricType.of_json in
      make ?resourceLabel ~predefinedMetricType ()
    let to_json v = composed_to_json to_value v
  end
module BlockDeviceMapping =
  struct
    type nonrec t =
      {
      virtualName: XmlStringMaxLen255.t option ;
      deviceName: XmlStringMaxLen255.t ;
      ebs: Ebs.t option ;
      noDevice: NoDevice.t option }
    let context_ = "BlockDeviceMapping"
    let make ?virtualName =
      fun ?ebs ->
        fun ?noDevice ->
          fun ~deviceName ->
            fun () -> { virtualName; ebs; noDevice; deviceName }
    let to_value x =
      structure_to_value
        [("VirtualName",
           (Option.map x.virtualName ~f:XmlStringMaxLen255.to_value));
        ("DeviceName", (Some (XmlStringMaxLen255.to_value x.deviceName)));
        ("Ebs", (Option.map x.ebs ~f:Ebs.to_value));
        ("NoDevice", (Option.map x.noDevice ~f:NoDevice.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let noDevice =
        (Option.map ~f:NoDevice.of_xml) (Xml.child xml_arg0 "NoDevice") in
      let ebs = (Option.map ~f:Ebs.of_xml) (Xml.child xml_arg0 "Ebs") in
      let deviceName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DeviceName") in
      let virtualName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "VirtualName") in
      make ?noDevice ?ebs ~deviceName ?virtualName ()
    let of_json json =
      let noDevice = field_map json "NoDevice" NoDevice.of_json in
      let ebs = field_map json "Ebs" Ebs.of_json in
      let deviceName =
        field_map_exn json "DeviceName" XmlStringMaxLen255.of_json in
      let virtualName =
        field_map json "VirtualName" XmlStringMaxLen255.of_json in
      make ?noDevice ?ebs ~deviceName ?virtualName ()
    let to_json v = composed_to_json to_value v
  end
module InstanceMetadataEndpointState =
  struct
    type nonrec t =
      | Disabled 
      | Enabled 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Disabled -> "disabled"
      | Enabled -> "enabled"
      | Non_static_id s -> s
    let of_string =
      function
      | "disabled" -> Disabled
      | "enabled" -> Enabled
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceMetadataEndpointState"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceMetadataEndpointState" j)
    let to_json = simple_to_json to_value
  end
module InstanceMetadataHttpPutResponseHopLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:64) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for InstanceMetadataHttpPutResponseHopLimit"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InstanceMetadataHttpTokensState =
  struct
    type nonrec t =
      | Optional 
      | Required 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Optional -> "optional"
      | Required -> "required"
      | Non_static_id s -> s
    let of_string =
      function
      | "optional" -> Optional
      | "required" -> Required
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceMetadataHttpTokensState"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceMetadataHttpTokensState" j)
    let to_json = simple_to_json to_value
  end
module MonitoringEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TimestampType =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module MixedInstancesPolicy =
  struct
    type nonrec t =
      {
      launchTemplate: LaunchTemplate.t option ;
      instancesDistribution: InstancesDistribution.t option }
    let make ?launchTemplate =
      fun ?instancesDistribution ->
        fun () -> { launchTemplate; instancesDistribution }
    let to_value x =
      structure_to_value
        [("LaunchTemplate",
           (Option.map x.launchTemplate ~f:LaunchTemplate.to_value));
        ("InstancesDistribution",
          (Option.map x.instancesDistribution
             ~f:InstancesDistribution.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instancesDistribution =
        (Option.map ~f:InstancesDistribution.of_xml)
          (Xml.child xml_arg0 "InstancesDistribution") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplate.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      make ?instancesDistribution ?launchTemplate ()
    let of_json json =
      let instancesDistribution =
        field_map json "InstancesDistribution" InstancesDistribution.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplate.of_json in
      make ?instancesDistribution ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end
module InstanceRefreshLivePoolProgress =
  struct
    type nonrec t =
      {
      percentageComplete: IntPercent.t option ;
      instancesToUpdate: InstancesToUpdate.t option }
    let make ?percentageComplete =
      fun ?instancesToUpdate ->
        fun () -> { percentageComplete; instancesToUpdate }
    let to_value x =
      structure_to_value
        [("PercentageComplete",
           (Option.map x.percentageComplete ~f:IntPercent.to_value));
        ("InstancesToUpdate",
          (Option.map x.instancesToUpdate ~f:InstancesToUpdate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instancesToUpdate =
        (Option.map ~f:InstancesToUpdate.of_xml)
          (Xml.child xml_arg0 "InstancesToUpdate") in
      let percentageComplete =
        (Option.map ~f:IntPercent.of_xml)
          (Xml.child xml_arg0 "PercentageComplete") in
      make ?instancesToUpdate ?percentageComplete ()
    let of_json json =
      let instancesToUpdate =
        field_map json "InstancesToUpdate" InstancesToUpdate.of_json in
      let percentageComplete =
        field_map json "PercentageComplete" IntPercent.of_json in
      make ?instancesToUpdate ?percentageComplete ()
    let to_json v = composed_to_json to_value v
  end
module InstanceRefreshWarmPoolProgress =
  struct
    type nonrec t =
      {
      percentageComplete: IntPercent.t option ;
      instancesToUpdate: InstancesToUpdate.t option }
    let make ?percentageComplete =
      fun ?instancesToUpdate ->
        fun () -> { percentageComplete; instancesToUpdate }
    let to_value x =
      structure_to_value
        [("PercentageComplete",
           (Option.map x.percentageComplete ~f:IntPercent.to_value));
        ("InstancesToUpdate",
          (Option.map x.instancesToUpdate ~f:InstancesToUpdate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instancesToUpdate =
        (Option.map ~f:InstancesToUpdate.of_xml)
          (Xml.child xml_arg0 "InstancesToUpdate") in
      let percentageComplete =
        (Option.map ~f:IntPercent.of_xml)
          (Xml.child xml_arg0 "PercentageComplete") in
      make ?instancesToUpdate ?percentageComplete ()
    let of_json json =
      let instancesToUpdate =
        field_map json "InstancesToUpdate" InstancesToUpdate.of_json in
      let percentageComplete =
        field_map json "PercentageComplete" IntPercent.of_json in
      make ?instancesToUpdate ?percentageComplete ()
    let to_json v = composed_to_json to_value v
  end
module CheckpointDelay =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:172800) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for CheckpointDelay" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CheckpointPercentages =
  struct
    type nonrec t = NonZeroIntPercent.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NonZeroIntPercent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonZeroIntPercent.of_xml)
    let of_json j =
      list_of_json ~kind:"CheckpointPercentages"
        ~of_json:NonZeroIntPercent.of_json j
    let to_json v = composed_to_json to_value v
  end
module RefreshInstanceWarmup =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for RefreshInstanceWarmup" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SkipMatching =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module EnabledMetric =
  struct
    type nonrec t =
      {
      metric: XmlStringMaxLen255.t option ;
      granularity: XmlStringMaxLen255.t option }
    let make ?metric = fun ?granularity -> fun () -> { metric; granularity }
    let to_value x =
      structure_to_value
        [("Metric", (Option.map x.metric ~f:XmlStringMaxLen255.to_value));
        ("Granularity",
          (Option.map x.granularity ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let granularity =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Granularity") in
      let metric =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Metric") in
      make ?granularity ?metric ()
    let of_json json =
      let granularity =
        field_map json "Granularity" XmlStringMaxLen255.of_json in
      let metric = field_map json "Metric" XmlStringMaxLen255.of_json in
      make ?granularity ?metric ()
    let to_json v = composed_to_json to_value v
  end
module Instance =
  struct
    type nonrec t =
      {
      instanceId: XmlStringMaxLen19.t ;
      instanceType: XmlStringMaxLen255.t option ;
      availabilityZone: XmlStringMaxLen255.t ;
      lifecycleState: LifecycleState.t ;
      healthStatus: XmlStringMaxLen32.t ;
      launchConfigurationName: XmlStringMaxLen255.t option ;
      launchTemplate: LaunchTemplateSpecification.t option ;
      protectedFromScaleIn: InstanceProtected.t ;
      weightedCapacity: XmlStringMaxLen32.t option }
    let context_ = "Instance"
    let make ?instanceType =
      fun ?launchConfigurationName ->
        fun ?launchTemplate ->
          fun ?weightedCapacity ->
            fun ~instanceId ->
              fun ~availabilityZone ->
                fun ~lifecycleState ->
                  fun ~healthStatus ->
                    fun ~protectedFromScaleIn ->
                      fun () ->
                        {
                          instanceType;
                          launchConfigurationName;
                          launchTemplate;
                          weightedCapacity;
                          instanceId;
                          availabilityZone;
                          lifecycleState;
                          healthStatus;
                          protectedFromScaleIn
                        }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (XmlStringMaxLen19.to_value x.instanceId)));
        ("InstanceType",
          (Option.map x.instanceType ~f:XmlStringMaxLen255.to_value));
        ("AvailabilityZone",
          (Some (XmlStringMaxLen255.to_value x.availabilityZone)));
        ("LifecycleState", (Some (LifecycleState.to_value x.lifecycleState)));
        ("HealthStatus", (Some (XmlStringMaxLen32.to_value x.healthStatus)));
        ("LaunchConfigurationName",
          (Option.map x.launchConfigurationName
             ~f:XmlStringMaxLen255.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:LaunchTemplateSpecification.to_value));
        ("ProtectedFromScaleIn",
          (Some (InstanceProtected.to_value x.protectedFromScaleIn)));
        ("WeightedCapacity",
          (Option.map x.weightedCapacity ~f:XmlStringMaxLen32.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let weightedCapacity =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "WeightedCapacity") in
      let protectedFromScaleIn =
        InstanceProtected.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectedFromScaleIn") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let launchConfigurationName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationName") in
      let healthStatus =
        XmlStringMaxLen32.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthStatus") in
      let lifecycleState =
        LifecycleState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleState") in
      let availabilityZone =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZone") in
      let instanceType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let instanceId =
        XmlStringMaxLen19.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?weightedCapacity ~protectedFromScaleIn ?launchTemplate
        ?launchConfigurationName ~healthStatus ~lifecycleState
        ~availabilityZone ?instanceType ~instanceId ()
    let of_json json =
      let weightedCapacity =
        field_map json "WeightedCapacity" XmlStringMaxLen32.of_json in
      let protectedFromScaleIn =
        field_map_exn json "ProtectedFromScaleIn" InstanceProtected.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      let launchConfigurationName =
        field_map json "LaunchConfigurationName" XmlStringMaxLen255.of_json in
      let healthStatus =
        field_map_exn json "HealthStatus" XmlStringMaxLen32.of_json in
      let lifecycleState =
        field_map_exn json "LifecycleState" LifecycleState.of_json in
      let availabilityZone =
        field_map_exn json "AvailabilityZone" XmlStringMaxLen255.of_json in
      let instanceType =
        field_map json "InstanceType" XmlStringMaxLen255.of_json in
      let instanceId =
        field_map_exn json "InstanceId" XmlStringMaxLen19.of_json in
      make ?weightedCapacity ~protectedFromScaleIn ?launchTemplate
        ?launchConfigurationName ~healthStatus ~lifecycleState
        ~availabilityZone ?instanceType ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module SuspendedProcess =
  struct
    type nonrec t =
      {
      processName: XmlStringMaxLen255.t option ;
      suspensionReason: XmlStringMaxLen255.t option }
    let make ?processName =
      fun ?suspensionReason -> fun () -> { processName; suspensionReason }
    let to_value x =
      structure_to_value
        [("ProcessName",
           (Option.map x.processName ~f:XmlStringMaxLen255.to_value));
        ("SuspensionReason",
          (Option.map x.suspensionReason ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suspensionReason =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "SuspensionReason") in
      let processName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "ProcessName") in
      make ?suspensionReason ?processName ()
    let of_json json =
      let suspensionReason =
        field_map json "SuspensionReason" XmlStringMaxLen255.of_json in
      let processName =
        field_map json "ProcessName" XmlStringMaxLen255.of_json in
      make ?suspensionReason ?processName ()
    let to_json v = composed_to_json to_value v
  end
module TagDescription =
  struct
    type nonrec t =
      {
      resourceId: XmlString.t option ;
      resourceType: XmlString.t option ;
      key: TagKey.t option ;
      value: TagValue.t option ;
      propagateAtLaunch: PropagateAtLaunch.t option }
    let make ?resourceId =
      fun ?resourceType ->
        fun ?key ->
          fun ?value ->
            fun ?propagateAtLaunch ->
              fun () ->
                { resourceId; resourceType; key; value; propagateAtLaunch }
    let to_value x =
      structure_to_value
        [("ResourceId", (Option.map x.resourceId ~f:XmlString.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:XmlString.to_value));
        ("Key", (Option.map x.key ~f:TagKey.to_value));
        ("Value", (Option.map x.value ~f:TagValue.to_value));
        ("PropagateAtLaunch",
          (Option.map x.propagateAtLaunch ~f:PropagateAtLaunch.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let propagateAtLaunch =
        (Option.map ~f:PropagateAtLaunch.of_xml)
          (Xml.child xml_arg0 "PropagateAtLaunch") in
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:TagKey.of_xml) (Xml.child xml_arg0 "Key") in
      let resourceType =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ResourceId") in
      make ?propagateAtLaunch ?value ?key ?resourceType ?resourceId ()
    let of_json json =
      let propagateAtLaunch =
        field_map json "PropagateAtLaunch" PropagateAtLaunch.of_json in
      let value = field_map json "Value" TagValue.of_json in
      let key = field_map json "Key" TagKey.of_json in
      let resourceType = field_map json "ResourceType" XmlString.of_json in
      let resourceId = field_map json "ResourceId" XmlString.of_json in
      make ?propagateAtLaunch ?value ?key ?resourceType ?resourceId ()
    let to_json v = composed_to_json to_value v
  end
module XmlStringMaxLen511 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen511"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:511) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen511" j
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen1600 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen1600"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1600) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen1600" j
    let to_json = simple_to_json to_value
  end
module InstanceReusePolicy =
  struct
    type nonrec t = {
      reuseOnScaleIn: ReuseOnScaleIn.t option }
    let make ?reuseOnScaleIn = fun () -> { reuseOnScaleIn }
    let to_value x =
      structure_to_value
        [("ReuseOnScaleIn",
           (Option.map x.reuseOnScaleIn ~f:ReuseOnScaleIn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reuseOnScaleIn =
        (Option.map ~f:ReuseOnScaleIn.of_xml)
          (Xml.child xml_arg0 "ReuseOnScaleIn") in
      make ?reuseOnScaleIn ()
    let of_json json =
      let reuseOnScaleIn =
        field_map json "ReuseOnScaleIn" ReuseOnScaleIn.of_json in
      make ?reuseOnScaleIn ()
    let to_json v = composed_to_json to_value v
  end
module MaxGroupPreparedCapacity =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:(-1)); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxGroupPreparedCapacity"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module WarmPoolMinSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for WarmPoolMinSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module WarmPoolState =
  struct
    type nonrec t =
      | Stopped 
      | Running 
      | Hibernated 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Stopped -> "Stopped"
      | Running -> "Running"
      | Hibernated -> "Hibernated"
      | Non_static_id s -> s
    let of_string =
      function
      | "Stopped" -> Stopped
      | "Running" -> Running
      | "Hibernated" -> Hibernated
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration WarmPoolState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WarmPoolState" j)
    let to_json = simple_to_json to_value
  end
module WarmPoolStatus =
  struct
    type nonrec t =
      | PendingDelete 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | PendingDelete -> "PendingDelete" | Non_static_id s -> s
    let of_string =
      function | "PendingDelete" -> PendingDelete | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration WarmPoolStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WarmPoolStatus" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingGroupDesiredCapacity =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for AutoScalingGroupDesiredCapacity"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingGroupMaxSize =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for AutoScalingGroupMaxSize"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingGroupMinSize =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for AutoScalingGroupMinSize"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Alarms =
  struct
    type nonrec t = Alarm.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Alarm.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Alarm.of_xml)
    let of_json j = list_of_json ~kind:"Alarms" ~of_json:Alarm.of_json j
    let to_json v = composed_to_json to_value v
  end
module Cooldown =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Cooldown" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module EstimatedInstanceWarmup =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for EstimatedInstanceWarmup"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MinAdjustmentMagnitude =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MinAdjustmentMagnitude" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MinAdjustmentStep =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MinAdjustmentStep" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PredictiveScalingConfiguration =
  struct
    type nonrec t =
      {
      metricSpecifications: PredictiveScalingMetricSpecifications.t ;
      mode: PredictiveScalingMode.t option ;
      schedulingBufferTime: PredictiveScalingSchedulingBufferTime.t option ;
      maxCapacityBreachBehavior:
        PredictiveScalingMaxCapacityBreachBehavior.t option ;
      maxCapacityBuffer: PredictiveScalingMaxCapacityBuffer.t option }
    let context_ = "PredictiveScalingConfiguration"
    let make ?mode =
      fun ?schedulingBufferTime ->
        fun ?maxCapacityBreachBehavior ->
          fun ?maxCapacityBuffer ->
            fun ~metricSpecifications ->
              fun () ->
                {
                  mode;
                  schedulingBufferTime;
                  maxCapacityBreachBehavior;
                  maxCapacityBuffer;
                  metricSpecifications
                }
    let to_value x =
      structure_to_value
        [("MetricSpecifications",
           (Some
              (PredictiveScalingMetricSpecifications.to_value
                 x.metricSpecifications)));
        ("Mode", (Option.map x.mode ~f:PredictiveScalingMode.to_value));
        ("SchedulingBufferTime",
          (Option.map x.schedulingBufferTime
             ~f:PredictiveScalingSchedulingBufferTime.to_value));
        ("MaxCapacityBreachBehavior",
          (Option.map x.maxCapacityBreachBehavior
             ~f:PredictiveScalingMaxCapacityBreachBehavior.to_value));
        ("MaxCapacityBuffer",
          (Option.map x.maxCapacityBuffer
             ~f:PredictiveScalingMaxCapacityBuffer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxCapacityBuffer =
        (Option.map ~f:PredictiveScalingMaxCapacityBuffer.of_xml)
          (Xml.child xml_arg0 "MaxCapacityBuffer") in
      let maxCapacityBreachBehavior =
        (Option.map ~f:PredictiveScalingMaxCapacityBreachBehavior.of_xml)
          (Xml.child xml_arg0 "MaxCapacityBreachBehavior") in
      let schedulingBufferTime =
        (Option.map ~f:PredictiveScalingSchedulingBufferTime.of_xml)
          (Xml.child xml_arg0 "SchedulingBufferTime") in
      let mode =
        (Option.map ~f:PredictiveScalingMode.of_xml)
          (Xml.child xml_arg0 "Mode") in
      let metricSpecifications =
        PredictiveScalingMetricSpecifications.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricSpecifications") in
      make ?maxCapacityBuffer ?maxCapacityBreachBehavior
        ?schedulingBufferTime ?mode ~metricSpecifications ()
    let of_json json =
      let maxCapacityBuffer =
        field_map json "MaxCapacityBuffer"
          PredictiveScalingMaxCapacityBuffer.of_json in
      let maxCapacityBreachBehavior =
        field_map json "MaxCapacityBreachBehavior"
          PredictiveScalingMaxCapacityBreachBehavior.of_json in
      let schedulingBufferTime =
        field_map json "SchedulingBufferTime"
          PredictiveScalingSchedulingBufferTime.of_json in
      let mode = field_map json "Mode" PredictiveScalingMode.of_json in
      let metricSpecifications =
        field_map_exn json "MetricSpecifications"
          PredictiveScalingMetricSpecifications.of_json in
      make ?maxCapacityBuffer ?maxCapacityBreachBehavior
        ?schedulingBufferTime ?mode ~metricSpecifications ()
    let to_json v = composed_to_json to_value v
  end
module ScalingPolicyEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module StepAdjustments =
  struct
    type nonrec t = StepAdjustment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StepAdjustment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StepAdjustment.of_xml)
    let of_json j =
      list_of_json ~kind:"StepAdjustments" ~of_json:StepAdjustment.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetTrackingConfiguration =
  struct
    type nonrec t =
      {
      predefinedMetricSpecification: PredefinedMetricSpecification.t option ;
      customizedMetricSpecification: CustomizedMetricSpecification.t option ;
      targetValue: MetricScale.t ;
      disableScaleIn: DisableScaleIn.t option }
    let context_ = "TargetTrackingConfiguration"
    let make ?predefinedMetricSpecification =
      fun ?customizedMetricSpecification ->
        fun ?disableScaleIn ->
          fun ~targetValue ->
            fun () ->
              {
                predefinedMetricSpecification;
                customizedMetricSpecification;
                disableScaleIn;
                targetValue
              }
    let to_value x =
      structure_to_value
        [("PredefinedMetricSpecification",
           (Option.map x.predefinedMetricSpecification
              ~f:PredefinedMetricSpecification.to_value));
        ("CustomizedMetricSpecification",
          (Option.map x.customizedMetricSpecification
             ~f:CustomizedMetricSpecification.to_value));
        ("TargetValue", (Some (MetricScale.to_value x.targetValue)));
        ("DisableScaleIn",
          (Option.map x.disableScaleIn ~f:DisableScaleIn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let disableScaleIn =
        (Option.map ~f:DisableScaleIn.of_xml)
          (Xml.child xml_arg0 "DisableScaleIn") in
      let targetValue =
        MetricScale.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetValue") in
      let customizedMetricSpecification =
        (Option.map ~f:CustomizedMetricSpecification.of_xml)
          (Xml.child xml_arg0 "CustomizedMetricSpecification") in
      let predefinedMetricSpecification =
        (Option.map ~f:PredefinedMetricSpecification.of_xml)
          (Xml.child xml_arg0 "PredefinedMetricSpecification") in
      make ?disableScaleIn ~targetValue ?customizedMetricSpecification
        ?predefinedMetricSpecification ()
    let of_json json =
      let disableScaleIn =
        field_map json "DisableScaleIn" DisableScaleIn.of_json in
      let targetValue = field_map_exn json "TargetValue" MetricScale.of_json in
      let customizedMetricSpecification =
        field_map json "CustomizedMetricSpecification"
          CustomizedMetricSpecification.of_json in
      let predefinedMetricSpecification =
        field_map json "PredefinedMetricSpecification"
          PredefinedMetricSpecification.of_json in
      make ?disableScaleIn ~targetValue ?customizedMetricSpecification
        ?predefinedMetricSpecification ()
    let to_json v = composed_to_json to_value v
  end
module XmlStringMaxLen64 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen64"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen64" j
    let to_json = simple_to_json to_value
  end
module AssociatePublicIpAddress =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module BlockDeviceMappings =
  struct
    type nonrec t = BlockDeviceMapping.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BlockDeviceMapping.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BlockDeviceMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"BlockDeviceMappings"
        ~of_json:BlockDeviceMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClassicLinkVPCSecurityGroups =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"ClassicLinkVPCSecurityGroups"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module EbsOptimized =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module InstanceMetadataOptions =
  struct
    type nonrec t =
      {
      httpTokens: InstanceMetadataHttpTokensState.t option ;
      httpPutResponseHopLimit:
        InstanceMetadataHttpPutResponseHopLimit.t option ;
      httpEndpoint: InstanceMetadataEndpointState.t option }
    let make ?httpTokens =
      fun ?httpPutResponseHopLimit ->
        fun ?httpEndpoint ->
          fun () -> { httpTokens; httpPutResponseHopLimit; httpEndpoint }
    let to_value x =
      structure_to_value
        [("HttpTokens",
           (Option.map x.httpTokens
              ~f:InstanceMetadataHttpTokensState.to_value));
        ("HttpPutResponseHopLimit",
          (Option.map x.httpPutResponseHopLimit
             ~f:InstanceMetadataHttpPutResponseHopLimit.to_value));
        ("HttpEndpoint",
          (Option.map x.httpEndpoint
             ~f:InstanceMetadataEndpointState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let httpEndpoint =
        (Option.map ~f:InstanceMetadataEndpointState.of_xml)
          (Xml.child xml_arg0 "HttpEndpoint") in
      let httpPutResponseHopLimit =
        (Option.map ~f:InstanceMetadataHttpPutResponseHopLimit.of_xml)
          (Xml.child xml_arg0 "HttpPutResponseHopLimit") in
      let httpTokens =
        (Option.map ~f:InstanceMetadataHttpTokensState.of_xml)
          (Xml.child xml_arg0 "HttpTokens") in
      make ?httpEndpoint ?httpPutResponseHopLimit ?httpTokens ()
    let of_json json =
      let httpEndpoint =
        field_map json "HttpEndpoint" InstanceMetadataEndpointState.of_json in
      let httpPutResponseHopLimit =
        field_map json "HttpPutResponseHopLimit"
          InstanceMetadataHttpPutResponseHopLimit.of_json in
      let httpTokens =
        field_map json "HttpTokens" InstanceMetadataHttpTokensState.of_json in
      make ?httpEndpoint ?httpPutResponseHopLimit ?httpTokens ()
    let to_json v = composed_to_json to_value v
  end
module InstanceMonitoring =
  struct
    type nonrec t = {
      enabled: MonitoringEnabled.t option }
    let make ?enabled = fun () -> { enabled }
    let to_value x =
      structure_to_value
        [("Enabled", (Option.map x.enabled ~f:MonitoringEnabled.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        (Option.map ~f:MonitoringEnabled.of_xml)
          (Xml.child xml_arg0 "Enabled") in
      make ?enabled ()
    let of_json json =
      let enabled = field_map json "Enabled" MonitoringEnabled.of_json in
      make ?enabled ()
    let to_json v = composed_to_json to_value v
  end
module SecurityGroups =
  struct
    type nonrec t = XmlString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlString.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroups" ~of_json:XmlString.of_json j
    let to_json v = composed_to_json to_value v
  end
module SpotPrice =
  struct
    type nonrec t = string
    let context_ = "SpotPrice"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SpotPrice" j
    let to_json = simple_to_json to_value
  end
module XmlStringUserData =
  struct
    type nonrec t = string
    let context_ = "XmlStringUserData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:21847) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringUserData" j
    let to_json = simple_to_json to_value
  end
module PredictiveScalingForecastTimestamps =
  struct
    type nonrec t = TimestampType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TimestampType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TimestampType.of_xml)
    let of_json j =
      list_of_json ~kind:"PredictiveScalingForecastTimestamps"
        ~of_json:TimestampType.of_json j
    let to_json v = composed_to_json to_value v
  end
module PredictiveScalingForecastValues =
  struct
    type nonrec t = MetricScale.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MetricScale.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricScale.of_xml)
    let of_json j =
      list_of_json ~kind:"PredictiveScalingForecastValues"
        ~of_json:MetricScale.of_json j
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroupState =
  struct
    type nonrec t = string
    let context_ = "AutoScalingGroupState"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AutoScalingGroupState" j
    let to_json = simple_to_json to_value
  end
module Progress =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Progress" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ScalingActivityStatusCode =
  struct
    type nonrec t =
      | PendingSpotBidPlacement 
      | WaitingForSpotInstanceRequestId 
      | WaitingForSpotInstanceId 
      | WaitingForInstanceId 
      | PreInService 
      | InProgress 
      | WaitingForELBConnectionDraining 
      | MidLifecycleAction 
      | WaitingForInstanceWarmup 
      | Successful 
      | Failed 
      | Cancelled 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PendingSpotBidPlacement -> "PendingSpotBidPlacement"
      | WaitingForSpotInstanceRequestId -> "WaitingForSpotInstanceRequestId"
      | WaitingForSpotInstanceId -> "WaitingForSpotInstanceId"
      | WaitingForInstanceId -> "WaitingForInstanceId"
      | PreInService -> "PreInService"
      | InProgress -> "InProgress"
      | WaitingForELBConnectionDraining -> "WaitingForELBConnectionDraining"
      | MidLifecycleAction -> "MidLifecycleAction"
      | WaitingForInstanceWarmup -> "WaitingForInstanceWarmup"
      | Successful -> "Successful"
      | Failed -> "Failed"
      | Cancelled -> "Cancelled"
      | Non_static_id s -> s
    let of_string =
      function
      | "PendingSpotBidPlacement" -> PendingSpotBidPlacement
      | "WaitingForSpotInstanceRequestId" -> WaitingForSpotInstanceRequestId
      | "WaitingForSpotInstanceId" -> WaitingForSpotInstanceId
      | "WaitingForInstanceId" -> WaitingForInstanceId
      | "PreInService" -> PreInService
      | "InProgress" -> InProgress
      | "WaitingForELBConnectionDraining" -> WaitingForELBConnectionDraining
      | "MidLifecycleAction" -> MidLifecycleAction
      | "WaitingForInstanceWarmup" -> WaitingForInstanceWarmup
      | "Successful" -> Successful
      | "Failed" -> Failed
      | "Cancelled" -> Cancelled
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ScalingActivityStatusCode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ScalingActivityStatusCode" j)
    let to_json = simple_to_json to_value
  end
module Values =
  struct
    type nonrec t = XmlString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlString.of_xml)
    let of_json j = list_of_json ~kind:"Values" ~of_json:XmlString.of_json j
    let to_json v = composed_to_json to_value v
  end
module AsciiStringMaxLen255 =
  struct
    type nonrec t = string
    let context_ = "AsciiStringMaxLen255"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9\\-_\\/]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AsciiStringMaxLen255" j
    let to_json = simple_to_json to_value
  end
module GlobalTimeout =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for GlobalTimeout" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module HeartbeatTimeout =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for HeartbeatTimeout" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module LifecycleActionResult =
  struct
    type nonrec t = string
    let context_ = "LifecycleActionResult"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LifecycleActionResult" j
    let to_json = simple_to_json to_value
  end
module LifecycleTransition =
  struct
    type nonrec t = string
    let context_ = "LifecycleTransition"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LifecycleTransition" j
    let to_json = simple_to_json to_value
  end
module NotificationTargetResourceName =
  struct
    type nonrec t = string
    let context_ = "NotificationTargetResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NotificationTargetResourceName" j
    let to_json = simple_to_json to_value
  end
module DesiredConfiguration =
  struct
    type nonrec t =
      {
      launchTemplate: LaunchTemplateSpecification.t option ;
      mixedInstancesPolicy: MixedInstancesPolicy.t option }
    let make ?launchTemplate =
      fun ?mixedInstancesPolicy ->
        fun () -> { launchTemplate; mixedInstancesPolicy }
    let to_value x =
      structure_to_value
        [("LaunchTemplate",
           (Option.map x.launchTemplate
              ~f:LaunchTemplateSpecification.to_value));
        ("MixedInstancesPolicy",
          (Option.map x.mixedInstancesPolicy ~f:MixedInstancesPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mixedInstancesPolicy =
        (Option.map ~f:MixedInstancesPolicy.of_xml)
          (Xml.child xml_arg0 "MixedInstancesPolicy") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      make ?mixedInstancesPolicy ?launchTemplate ()
    let of_json json =
      let mixedInstancesPolicy =
        field_map json "MixedInstancesPolicy" MixedInstancesPolicy.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      make ?mixedInstancesPolicy ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end
module InstanceRefreshProgressDetails =
  struct
    type nonrec t =
      {
      livePoolProgress: InstanceRefreshLivePoolProgress.t option ;
      warmPoolProgress: InstanceRefreshWarmPoolProgress.t option }
    let make ?livePoolProgress =
      fun ?warmPoolProgress ->
        fun () -> { livePoolProgress; warmPoolProgress }
    let to_value x =
      structure_to_value
        [("LivePoolProgress",
           (Option.map x.livePoolProgress
              ~f:InstanceRefreshLivePoolProgress.to_value));
        ("WarmPoolProgress",
          (Option.map x.warmPoolProgress
             ~f:InstanceRefreshWarmPoolProgress.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let warmPoolProgress =
        (Option.map ~f:InstanceRefreshWarmPoolProgress.of_xml)
          (Xml.child xml_arg0 "WarmPoolProgress") in
      let livePoolProgress =
        (Option.map ~f:InstanceRefreshLivePoolProgress.of_xml)
          (Xml.child xml_arg0 "LivePoolProgress") in
      make ?warmPoolProgress ?livePoolProgress ()
    let of_json json =
      let warmPoolProgress =
        field_map json "WarmPoolProgress"
          InstanceRefreshWarmPoolProgress.of_json in
      let livePoolProgress =
        field_map json "LivePoolProgress"
          InstanceRefreshLivePoolProgress.of_json in
      make ?warmPoolProgress ?livePoolProgress ()
    let to_json v = composed_to_json to_value v
  end
module InstanceRefreshStatus =
  struct
    type nonrec t =
      | Pending 
      | InProgress 
      | Successful 
      | Failed 
      | Cancelling 
      | Cancelled 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "Pending"
      | InProgress -> "InProgress"
      | Successful -> "Successful"
      | Failed -> "Failed"
      | Cancelling -> "Cancelling"
      | Cancelled -> "Cancelled"
      | Non_static_id s -> s
    let of_string =
      function
      | "Pending" -> Pending
      | "InProgress" -> InProgress
      | "Successful" -> Successful
      | "Failed" -> Failed
      | "Cancelling" -> Cancelling
      | "Cancelled" -> Cancelled
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceRefreshStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceRefreshStatus" j)
    let to_json = simple_to_json to_value
  end
module RefreshPreferences =
  struct
    type nonrec t =
      {
      minHealthyPercentage: IntPercent.t option ;
      instanceWarmup: RefreshInstanceWarmup.t option ;
      checkpointPercentages: CheckpointPercentages.t option ;
      checkpointDelay: CheckpointDelay.t option ;
      skipMatching: SkipMatching.t option }
    let make ?minHealthyPercentage =
      fun ?instanceWarmup ->
        fun ?checkpointPercentages ->
          fun ?checkpointDelay ->
            fun ?skipMatching ->
              fun () ->
                {
                  minHealthyPercentage;
                  instanceWarmup;
                  checkpointPercentages;
                  checkpointDelay;
                  skipMatching
                }
    let to_value x =
      structure_to_value
        [("MinHealthyPercentage",
           (Option.map x.minHealthyPercentage ~f:IntPercent.to_value));
        ("InstanceWarmup",
          (Option.map x.instanceWarmup ~f:RefreshInstanceWarmup.to_value));
        ("CheckpointPercentages",
          (Option.map x.checkpointPercentages
             ~f:CheckpointPercentages.to_value));
        ("CheckpointDelay",
          (Option.map x.checkpointDelay ~f:CheckpointDelay.to_value));
        ("SkipMatching",
          (Option.map x.skipMatching ~f:SkipMatching.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let skipMatching =
        (Option.map ~f:SkipMatching.of_xml)
          (Xml.child xml_arg0 "SkipMatching") in
      let checkpointDelay =
        (Option.map ~f:CheckpointDelay.of_xml)
          (Xml.child xml_arg0 "CheckpointDelay") in
      let checkpointPercentages =
        (Option.map ~f:CheckpointPercentages.of_xml)
          (Xml.child xml_arg0 "CheckpointPercentages") in
      let instanceWarmup =
        (Option.map ~f:RefreshInstanceWarmup.of_xml)
          (Xml.child xml_arg0 "InstanceWarmup") in
      let minHealthyPercentage =
        (Option.map ~f:IntPercent.of_xml)
          (Xml.child xml_arg0 "MinHealthyPercentage") in
      make ?skipMatching ?checkpointDelay ?checkpointPercentages
        ?instanceWarmup ?minHealthyPercentage ()
    let of_json json =
      let skipMatching = field_map json "SkipMatching" SkipMatching.of_json in
      let checkpointDelay =
        field_map json "CheckpointDelay" CheckpointDelay.of_json in
      let checkpointPercentages =
        field_map json "CheckpointPercentages" CheckpointPercentages.of_json in
      let instanceWarmup =
        field_map json "InstanceWarmup" RefreshInstanceWarmup.of_json in
      let minHealthyPercentage =
        field_map json "MinHealthyPercentage" IntPercent.of_json in
      make ?skipMatching ?checkpointDelay ?checkpointPercentages
        ?instanceWarmup ?minHealthyPercentage ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroupPredictedCapacity =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for AutoScalingGroupPredictedCapacity" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AvailabilityZones =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"AvailabilityZones"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module CapacityRebalanceEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Context =
  struct
    type nonrec t = string
    let context_ = "Context"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Context" j
    let to_json = simple_to_json to_value
  end
module EnabledMetrics =
  struct
    type nonrec t = EnabledMetric.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EnabledMetric.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnabledMetric.of_xml)
    let of_json j =
      list_of_json ~kind:"EnabledMetrics" ~of_json:EnabledMetric.of_json j
    let to_json v = composed_to_json to_value v
  end
module HealthCheckGracePeriod =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for HealthCheckGracePeriod" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Instances =
  struct
    type nonrec t = Instance.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Instance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Instance.of_xml)
    let of_json j =
      list_of_json ~kind:"Instances" ~of_json:Instance.of_json j
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerNames =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancerNames"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxInstanceLifetime =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxInstanceLifetime" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SuspendedProcesses =
  struct
    type nonrec t = SuspendedProcess.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SuspendedProcess.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SuspendedProcess.of_xml)
    let of_json j =
      list_of_json ~kind:"SuspendedProcesses"
        ~of_json:SuspendedProcess.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagDescriptionList =
  struct
    type nonrec t = TagDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagDescription.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"TagDescriptionList" ~of_json:TagDescription.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module TargetGroupARNs =
  struct
    type nonrec t = XmlStringMaxLen511.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen511.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen511.of_xml)
    let of_json j =
      list_of_json ~kind:"TargetGroupARNs"
        ~of_json:XmlStringMaxLen511.of_json j
    let to_json v = composed_to_json to_value v
  end
module TerminationPolicies =
  struct
    type nonrec t = XmlStringMaxLen1600.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen1600.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen1600.of_xml)
    let of_json j =
      list_of_json ~kind:"TerminationPolicies"
        ~of_json:XmlStringMaxLen1600.of_json j
    let to_json v = composed_to_json to_value v
  end
module WarmPoolConfiguration =
  struct
    type nonrec t =
      {
      maxGroupPreparedCapacity: MaxGroupPreparedCapacity.t option ;
      minSize: WarmPoolMinSize.t option ;
      poolState: WarmPoolState.t option ;
      status: WarmPoolStatus.t option ;
      instanceReusePolicy: InstanceReusePolicy.t option }
    let make ?maxGroupPreparedCapacity =
      fun ?minSize ->
        fun ?poolState ->
          fun ?status ->
            fun ?instanceReusePolicy ->
              fun () ->
                {
                  maxGroupPreparedCapacity;
                  minSize;
                  poolState;
                  status;
                  instanceReusePolicy
                }
    let to_value x =
      structure_to_value
        [("MaxGroupPreparedCapacity",
           (Option.map x.maxGroupPreparedCapacity
              ~f:MaxGroupPreparedCapacity.to_value));
        ("MinSize", (Option.map x.minSize ~f:WarmPoolMinSize.to_value));
        ("PoolState", (Option.map x.poolState ~f:WarmPoolState.to_value));
        ("Status", (Option.map x.status ~f:WarmPoolStatus.to_value));
        ("InstanceReusePolicy",
          (Option.map x.instanceReusePolicy ~f:InstanceReusePolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceReusePolicy =
        (Option.map ~f:InstanceReusePolicy.of_xml)
          (Xml.child xml_arg0 "InstanceReusePolicy") in
      let status =
        (Option.map ~f:WarmPoolStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let poolState =
        (Option.map ~f:WarmPoolState.of_xml) (Xml.child xml_arg0 "PoolState") in
      let minSize =
        (Option.map ~f:WarmPoolMinSize.of_xml) (Xml.child xml_arg0 "MinSize") in
      let maxGroupPreparedCapacity =
        (Option.map ~f:MaxGroupPreparedCapacity.of_xml)
          (Xml.child xml_arg0 "MaxGroupPreparedCapacity") in
      make ?instanceReusePolicy ?status ?poolState ?minSize
        ?maxGroupPreparedCapacity ()
    let of_json json =
      let instanceReusePolicy =
        field_map json "InstanceReusePolicy" InstanceReusePolicy.of_json in
      let status = field_map json "Status" WarmPoolStatus.of_json in
      let poolState = field_map json "PoolState" WarmPoolState.of_json in
      let minSize = field_map json "MinSize" WarmPoolMinSize.of_json in
      let maxGroupPreparedCapacity =
        field_map json "MaxGroupPreparedCapacity"
          MaxGroupPreparedCapacity.of_json in
      make ?instanceReusePolicy ?status ?poolState ?minSize
        ?maxGroupPreparedCapacity ()
    let to_json v = composed_to_json to_value v
  end
module WarmPoolSize =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for WarmPoolSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen2047 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen2047"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2047) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen2047" j
    let to_json = simple_to_json to_value
  end
module ScheduledUpdateGroupAction =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      scheduledActionName: XmlStringMaxLen255.t option ;
      scheduledActionARN: ResourceName.t option ;
      time: TimestampType.t option ;
      startTime: TimestampType.t option ;
      endTime: TimestampType.t option ;
      recurrence: XmlStringMaxLen255.t option ;
      minSize: AutoScalingGroupMinSize.t option ;
      maxSize: AutoScalingGroupMaxSize.t option ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t option ;
      timeZone: XmlStringMaxLen255.t option }
    let make ?autoScalingGroupName =
      fun ?scheduledActionName ->
        fun ?scheduledActionARN ->
          fun ?time ->
            fun ?startTime ->
              fun ?endTime ->
                fun ?recurrence ->
                  fun ?minSize ->
                    fun ?maxSize ->
                      fun ?desiredCapacity ->
                        fun ?timeZone ->
                          fun () ->
                            {
                              autoScalingGroupName;
                              scheduledActionName;
                              scheduledActionARN;
                              time;
                              startTime;
                              endTime;
                              recurrence;
                              minSize;
                              maxSize;
                              desiredCapacity;
                              timeZone
                            }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("ScheduledActionName",
          (Option.map x.scheduledActionName ~f:XmlStringMaxLen255.to_value));
        ("ScheduledActionARN",
          (Option.map x.scheduledActionARN ~f:ResourceName.to_value));
        ("Time", (Option.map x.time ~f:TimestampType.to_value));
        ("StartTime", (Option.map x.startTime ~f:TimestampType.to_value));
        ("EndTime", (Option.map x.endTime ~f:TimestampType.to_value));
        ("Recurrence",
          (Option.map x.recurrence ~f:XmlStringMaxLen255.to_value));
        ("MinSize",
          (Option.map x.minSize ~f:AutoScalingGroupMinSize.to_value));
        ("MaxSize",
          (Option.map x.maxSize ~f:AutoScalingGroupMaxSize.to_value));
        ("DesiredCapacity",
          (Option.map x.desiredCapacity
             ~f:AutoScalingGroupDesiredCapacity.to_value));
        ("TimeZone", (Option.map x.timeZone ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeZone =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "TimeZone") in
      let desiredCapacity =
        (Option.map ~f:AutoScalingGroupDesiredCapacity.of_xml)
          (Xml.child xml_arg0 "DesiredCapacity") in
      let maxSize =
        (Option.map ~f:AutoScalingGroupMaxSize.of_xml)
          (Xml.child xml_arg0 "MaxSize") in
      let minSize =
        (Option.map ~f:AutoScalingGroupMinSize.of_xml)
          (Xml.child xml_arg0 "MinSize") in
      let recurrence =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Recurrence") in
      let endTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "StartTime") in
      let time =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "Time") in
      let scheduledActionARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ScheduledActionARN") in
      let scheduledActionName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "ScheduledActionName") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ?timeZone ?desiredCapacity ?maxSize ?minSize ?recurrence ?endTime
        ?startTime ?time ?scheduledActionARN ?scheduledActionName
        ?autoScalingGroupName ()
    let of_json json =
      let timeZone = field_map json "TimeZone" XmlStringMaxLen255.of_json in
      let desiredCapacity =
        field_map json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let maxSize = field_map json "MaxSize" AutoScalingGroupMaxSize.of_json in
      let minSize = field_map json "MinSize" AutoScalingGroupMinSize.of_json in
      let recurrence = field_map json "Recurrence" XmlStringMaxLen255.of_json in
      let endTime = field_map json "EndTime" TimestampType.of_json in
      let startTime = field_map json "StartTime" TimestampType.of_json in
      let time = field_map json "Time" TimestampType.of_json in
      let scheduledActionARN =
        field_map json "ScheduledActionARN" ResourceName.of_json in
      let scheduledActionName =
        field_map json "ScheduledActionName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?timeZone ?desiredCapacity ?maxSize ?minSize ?recurrence ?endTime
        ?startTime ?time ?scheduledActionARN ?scheduledActionName
        ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module ProcessType =
  struct
    type nonrec t = {
      processName: XmlStringMaxLen255.t }
    let context_ = "ProcessType"
    let make ~processName = fun () -> { processName }
    let to_value x =
      structure_to_value
        [("ProcessName", (Some (XmlStringMaxLen255.to_value x.processName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let processName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProcessName") in
      make ~processName ()
    let of_json json =
      let processName =
        field_map_exn json "ProcessName" XmlStringMaxLen255.of_json in
      make ~processName ()
    let to_json v = composed_to_json to_value v
  end
module ScalingPolicy =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      policyName: XmlStringMaxLen255.t option ;
      policyARN: ResourceName.t option ;
      policyType: XmlStringMaxLen64.t option ;
      adjustmentType: XmlStringMaxLen255.t option ;
      minAdjustmentStep: MinAdjustmentStep.t option ;
      minAdjustmentMagnitude: MinAdjustmentMagnitude.t option ;
      scalingAdjustment: PolicyIncrement.t option ;
      cooldown: Cooldown.t option ;
      stepAdjustments: StepAdjustments.t option ;
      metricAggregationType: XmlStringMaxLen32.t option ;
      estimatedInstanceWarmup: EstimatedInstanceWarmup.t option ;
      alarms: Alarms.t option ;
      targetTrackingConfiguration: TargetTrackingConfiguration.t option ;
      enabled: ScalingPolicyEnabled.t option ;
      predictiveScalingConfiguration: PredictiveScalingConfiguration.t option }
    let make ?autoScalingGroupName =
      fun ?policyName ->
        fun ?policyARN ->
          fun ?policyType ->
            fun ?adjustmentType ->
              fun ?minAdjustmentStep ->
                fun ?minAdjustmentMagnitude ->
                  fun ?scalingAdjustment ->
                    fun ?cooldown ->
                      fun ?stepAdjustments ->
                        fun ?metricAggregationType ->
                          fun ?estimatedInstanceWarmup ->
                            fun ?alarms ->
                              fun ?targetTrackingConfiguration ->
                                fun ?enabled ->
                                  fun ?predictiveScalingConfiguration ->
                                    fun () ->
                                      {
                                        autoScalingGroupName;
                                        policyName;
                                        policyARN;
                                        policyType;
                                        adjustmentType;
                                        minAdjustmentStep;
                                        minAdjustmentMagnitude;
                                        scalingAdjustment;
                                        cooldown;
                                        stepAdjustments;
                                        metricAggregationType;
                                        estimatedInstanceWarmup;
                                        alarms;
                                        targetTrackingConfiguration;
                                        enabled;
                                        predictiveScalingConfiguration
                                      }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("PolicyName",
          (Option.map x.policyName ~f:XmlStringMaxLen255.to_value));
        ("PolicyARN", (Option.map x.policyARN ~f:ResourceName.to_value));
        ("PolicyType",
          (Option.map x.policyType ~f:XmlStringMaxLen64.to_value));
        ("AdjustmentType",
          (Option.map x.adjustmentType ~f:XmlStringMaxLen255.to_value));
        ("MinAdjustmentStep",
          (Option.map x.minAdjustmentStep ~f:MinAdjustmentStep.to_value));
        ("MinAdjustmentMagnitude",
          (Option.map x.minAdjustmentMagnitude
             ~f:MinAdjustmentMagnitude.to_value));
        ("ScalingAdjustment",
          (Option.map x.scalingAdjustment ~f:PolicyIncrement.to_value));
        ("Cooldown", (Option.map x.cooldown ~f:Cooldown.to_value));
        ("StepAdjustments",
          (Option.map x.stepAdjustments ~f:StepAdjustments.to_value));
        ("MetricAggregationType",
          (Option.map x.metricAggregationType ~f:XmlStringMaxLen32.to_value));
        ("EstimatedInstanceWarmup",
          (Option.map x.estimatedInstanceWarmup
             ~f:EstimatedInstanceWarmup.to_value));
        ("Alarms", (Option.map x.alarms ~f:Alarms.to_value));
        ("TargetTrackingConfiguration",
          (Option.map x.targetTrackingConfiguration
             ~f:TargetTrackingConfiguration.to_value));
        ("Enabled", (Option.map x.enabled ~f:ScalingPolicyEnabled.to_value));
        ("PredictiveScalingConfiguration",
          (Option.map x.predictiveScalingConfiguration
             ~f:PredictiveScalingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let predictiveScalingConfiguration =
        (Option.map ~f:PredictiveScalingConfiguration.of_xml)
          (Xml.child xml_arg0 "PredictiveScalingConfiguration") in
      let enabled =
        (Option.map ~f:ScalingPolicyEnabled.of_xml)
          (Xml.child xml_arg0 "Enabled") in
      let targetTrackingConfiguration =
        (Option.map ~f:TargetTrackingConfiguration.of_xml)
          (Xml.child xml_arg0 "TargetTrackingConfiguration") in
      let alarms =
        (Option.map ~f:Alarms.of_xml) (Xml.child xml_arg0 "Alarms") in
      let estimatedInstanceWarmup =
        (Option.map ~f:EstimatedInstanceWarmup.of_xml)
          (Xml.child xml_arg0 "EstimatedInstanceWarmup") in
      let metricAggregationType =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "MetricAggregationType") in
      let stepAdjustments =
        (Option.map ~f:StepAdjustments.of_xml)
          (Xml.child xml_arg0 "StepAdjustments") in
      let cooldown =
        (Option.map ~f:Cooldown.of_xml) (Xml.child xml_arg0 "Cooldown") in
      let scalingAdjustment =
        (Option.map ~f:PolicyIncrement.of_xml)
          (Xml.child xml_arg0 "ScalingAdjustment") in
      let minAdjustmentMagnitude =
        (Option.map ~f:MinAdjustmentMagnitude.of_xml)
          (Xml.child xml_arg0 "MinAdjustmentMagnitude") in
      let minAdjustmentStep =
        (Option.map ~f:MinAdjustmentStep.of_xml)
          (Xml.child xml_arg0 "MinAdjustmentStep") in
      let adjustmentType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AdjustmentType") in
      let policyType =
        (Option.map ~f:XmlStringMaxLen64.of_xml)
          (Xml.child xml_arg0 "PolicyType") in
      let policyARN =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "PolicyARN") in
      let policyName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "PolicyName") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ?predictiveScalingConfiguration ?enabled
        ?targetTrackingConfiguration ?alarms ?estimatedInstanceWarmup
        ?metricAggregationType ?stepAdjustments ?cooldown ?scalingAdjustment
        ?minAdjustmentMagnitude ?minAdjustmentStep ?adjustmentType
        ?policyType ?policyARN ?policyName ?autoScalingGroupName ()
    let of_json json =
      let predictiveScalingConfiguration =
        field_map json "PredictiveScalingConfiguration"
          PredictiveScalingConfiguration.of_json in
      let enabled = field_map json "Enabled" ScalingPolicyEnabled.of_json in
      let targetTrackingConfiguration =
        field_map json "TargetTrackingConfiguration"
          TargetTrackingConfiguration.of_json in
      let alarms = field_map json "Alarms" Alarms.of_json in
      let estimatedInstanceWarmup =
        field_map json "EstimatedInstanceWarmup"
          EstimatedInstanceWarmup.of_json in
      let metricAggregationType =
        field_map json "MetricAggregationType" XmlStringMaxLen32.of_json in
      let stepAdjustments =
        field_map json "StepAdjustments" StepAdjustments.of_json in
      let cooldown = field_map json "Cooldown" Cooldown.of_json in
      let scalingAdjustment =
        field_map json "ScalingAdjustment" PolicyIncrement.of_json in
      let minAdjustmentMagnitude =
        field_map json "MinAdjustmentMagnitude"
          MinAdjustmentMagnitude.of_json in
      let minAdjustmentStep =
        field_map json "MinAdjustmentStep" MinAdjustmentStep.of_json in
      let adjustmentType =
        field_map json "AdjustmentType" XmlStringMaxLen255.of_json in
      let policyType = field_map json "PolicyType" XmlStringMaxLen64.of_json in
      let policyARN = field_map json "PolicyARN" ResourceName.of_json in
      let policyName = field_map json "PolicyName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?predictiveScalingConfiguration ?enabled
        ?targetTrackingConfiguration ?alarms ?estimatedInstanceWarmup
        ?metricAggregationType ?stepAdjustments ?cooldown ?scalingAdjustment
        ?minAdjustmentMagnitude ?minAdjustmentStep ?adjustmentType
        ?policyType ?policyARN ?policyName ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module LaunchConfiguration =
  struct
    type nonrec t =
      {
      launchConfigurationName: XmlStringMaxLen255.t ;
      launchConfigurationARN: ResourceName.t option ;
      imageId: XmlStringMaxLen255.t ;
      keyName: XmlStringMaxLen255.t option ;
      securityGroups: SecurityGroups.t option ;
      classicLinkVPCId: XmlStringMaxLen255.t option ;
      classicLinkVPCSecurityGroups: ClassicLinkVPCSecurityGroups.t option ;
      userData: XmlStringUserData.t option ;
      instanceType: XmlStringMaxLen255.t ;
      kernelId: XmlStringMaxLen255.t option ;
      ramdiskId: XmlStringMaxLen255.t option ;
      blockDeviceMappings: BlockDeviceMappings.t option ;
      instanceMonitoring: InstanceMonitoring.t option ;
      spotPrice: SpotPrice.t option ;
      iamInstanceProfile: XmlStringMaxLen1600.t option ;
      createdTime: TimestampType.t ;
      ebsOptimized: EbsOptimized.t option ;
      associatePublicIpAddress: AssociatePublicIpAddress.t option ;
      placementTenancy: XmlStringMaxLen64.t option ;
      metadataOptions: InstanceMetadataOptions.t option }
    let context_ = "LaunchConfiguration"
    let make ?launchConfigurationARN =
      fun ?keyName ->
        fun ?securityGroups ->
          fun ?classicLinkVPCId ->
            fun ?classicLinkVPCSecurityGroups ->
              fun ?userData ->
                fun ?kernelId ->
                  fun ?ramdiskId ->
                    fun ?blockDeviceMappings ->
                      fun ?instanceMonitoring ->
                        fun ?spotPrice ->
                          fun ?iamInstanceProfile ->
                            fun ?ebsOptimized ->
                              fun ?associatePublicIpAddress ->
                                fun ?placementTenancy ->
                                  fun ?metadataOptions ->
                                    fun ~launchConfigurationName ->
                                      fun ~imageId ->
                                        fun ~instanceType ->
                                          fun ~createdTime ->
                                            fun () ->
                                              {
                                                launchConfigurationARN;
                                                keyName;
                                                securityGroups;
                                                classicLinkVPCId;
                                                classicLinkVPCSecurityGroups;
                                                userData;
                                                kernelId;
                                                ramdiskId;
                                                blockDeviceMappings;
                                                instanceMonitoring;
                                                spotPrice;
                                                iamInstanceProfile;
                                                ebsOptimized;
                                                associatePublicIpAddress;
                                                placementTenancy;
                                                metadataOptions;
                                                launchConfigurationName;
                                                imageId;
                                                instanceType;
                                                createdTime
                                              }
    let to_value x =
      structure_to_value
        [("LaunchConfigurationName",
           (Some (XmlStringMaxLen255.to_value x.launchConfigurationName)));
        ("LaunchConfigurationARN",
          (Option.map x.launchConfigurationARN ~f:ResourceName.to_value));
        ("ImageId", (Some (XmlStringMaxLen255.to_value x.imageId)));
        ("KeyName", (Option.map x.keyName ~f:XmlStringMaxLen255.to_value));
        ("SecurityGroups",
          (Option.map x.securityGroups ~f:SecurityGroups.to_value));
        ("ClassicLinkVPCId",
          (Option.map x.classicLinkVPCId ~f:XmlStringMaxLen255.to_value));
        ("ClassicLinkVPCSecurityGroups",
          (Option.map x.classicLinkVPCSecurityGroups
             ~f:ClassicLinkVPCSecurityGroups.to_value));
        ("UserData", (Option.map x.userData ~f:XmlStringUserData.to_value));
        ("InstanceType", (Some (XmlStringMaxLen255.to_value x.instanceType)));
        ("KernelId", (Option.map x.kernelId ~f:XmlStringMaxLen255.to_value));
        ("RamdiskId",
          (Option.map x.ramdiskId ~f:XmlStringMaxLen255.to_value));
        ("BlockDeviceMappings",
          (Option.map x.blockDeviceMappings ~f:BlockDeviceMappings.to_value));
        ("InstanceMonitoring",
          (Option.map x.instanceMonitoring ~f:InstanceMonitoring.to_value));
        ("SpotPrice", (Option.map x.spotPrice ~f:SpotPrice.to_value));
        ("IamInstanceProfile",
          (Option.map x.iamInstanceProfile ~f:XmlStringMaxLen1600.to_value));
        ("CreatedTime", (Some (TimestampType.to_value x.createdTime)));
        ("EbsOptimized",
          (Option.map x.ebsOptimized ~f:EbsOptimized.to_value));
        ("AssociatePublicIpAddress",
          (Option.map x.associatePublicIpAddress
             ~f:AssociatePublicIpAddress.to_value));
        ("PlacementTenancy",
          (Option.map x.placementTenancy ~f:XmlStringMaxLen64.to_value));
        ("MetadataOptions",
          (Option.map x.metadataOptions ~f:InstanceMetadataOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadataOptions =
        (Option.map ~f:InstanceMetadataOptions.of_xml)
          (Xml.child xml_arg0 "MetadataOptions") in
      let placementTenancy =
        (Option.map ~f:XmlStringMaxLen64.of_xml)
          (Xml.child xml_arg0 "PlacementTenancy") in
      let associatePublicIpAddress =
        (Option.map ~f:AssociatePublicIpAddress.of_xml)
          (Xml.child xml_arg0 "AssociatePublicIpAddress") in
      let ebsOptimized =
        (Option.map ~f:EbsOptimized.of_xml)
          (Xml.child xml_arg0 "EbsOptimized") in
      let createdTime =
        TimestampType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreatedTime") in
      let iamInstanceProfile =
        (Option.map ~f:XmlStringMaxLen1600.of_xml)
          (Xml.child xml_arg0 "IamInstanceProfile") in
      let spotPrice =
        (Option.map ~f:SpotPrice.of_xml) (Xml.child xml_arg0 "SpotPrice") in
      let instanceMonitoring =
        (Option.map ~f:InstanceMonitoring.of_xml)
          (Xml.child xml_arg0 "InstanceMonitoring") in
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappings.of_xml)
          (Xml.child xml_arg0 "BlockDeviceMappings") in
      let ramdiskId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "RamdiskId") in
      let kernelId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "KernelId") in
      let instanceType =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      let userData =
        (Option.map ~f:XmlStringUserData.of_xml)
          (Xml.child xml_arg0 "UserData") in
      let classicLinkVPCSecurityGroups =
        (Option.map ~f:ClassicLinkVPCSecurityGroups.of_xml)
          (Xml.child xml_arg0 "ClassicLinkVPCSecurityGroups") in
      let classicLinkVPCId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "ClassicLinkVPCId") in
      let securityGroups =
        (Option.map ~f:SecurityGroups.of_xml)
          (Xml.child xml_arg0 "SecurityGroups") in
      let keyName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "KeyName") in
      let imageId =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      let launchConfigurationARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationARN") in
      let launchConfigurationName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchConfigurationName") in
      make ?metadataOptions ?placementTenancy ?associatePublicIpAddress
        ?ebsOptimized ~createdTime ?iamInstanceProfile ?spotPrice
        ?instanceMonitoring ?blockDeviceMappings ?ramdiskId ?kernelId
        ~instanceType ?userData ?classicLinkVPCSecurityGroups
        ?classicLinkVPCId ?securityGroups ?keyName ~imageId
        ?launchConfigurationARN ~launchConfigurationName ()
    let of_json json =
      let metadataOptions =
        field_map json "MetadataOptions" InstanceMetadataOptions.of_json in
      let placementTenancy =
        field_map json "PlacementTenancy" XmlStringMaxLen64.of_json in
      let associatePublicIpAddress =
        field_map json "AssociatePublicIpAddress"
          AssociatePublicIpAddress.of_json in
      let ebsOptimized = field_map json "EbsOptimized" EbsOptimized.of_json in
      let createdTime =
        field_map_exn json "CreatedTime" TimestampType.of_json in
      let iamInstanceProfile =
        field_map json "IamInstanceProfile" XmlStringMaxLen1600.of_json in
      let spotPrice = field_map json "SpotPrice" SpotPrice.of_json in
      let instanceMonitoring =
        field_map json "InstanceMonitoring" InstanceMonitoring.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings" BlockDeviceMappings.of_json in
      let ramdiskId = field_map json "RamdiskId" XmlStringMaxLen255.of_json in
      let kernelId = field_map json "KernelId" XmlStringMaxLen255.of_json in
      let instanceType =
        field_map_exn json "InstanceType" XmlStringMaxLen255.of_json in
      let userData = field_map json "UserData" XmlStringUserData.of_json in
      let classicLinkVPCSecurityGroups =
        field_map json "ClassicLinkVPCSecurityGroups"
          ClassicLinkVPCSecurityGroups.of_json in
      let classicLinkVPCId =
        field_map json "ClassicLinkVPCId" XmlStringMaxLen255.of_json in
      let securityGroups =
        field_map json "SecurityGroups" SecurityGroups.of_json in
      let keyName = field_map json "KeyName" XmlStringMaxLen255.of_json in
      let imageId = field_map_exn json "ImageId" XmlStringMaxLen255.of_json in
      let launchConfigurationARN =
        field_map json "LaunchConfigurationARN" ResourceName.of_json in
      let launchConfigurationName =
        field_map_exn json "LaunchConfigurationName"
          XmlStringMaxLen255.of_json in
      make ?metadataOptions ?placementTenancy ?associatePublicIpAddress
        ?ebsOptimized ~createdTime ?iamInstanceProfile ?spotPrice
        ?instanceMonitoring ?blockDeviceMappings ?ramdiskId ?kernelId
        ~instanceType ?userData ?classicLinkVPCSecurityGroups
        ?classicLinkVPCId ?securityGroups ?keyName ~imageId
        ?launchConfigurationARN ~launchConfigurationName ()
    let to_json v = composed_to_json to_value v
  end
module LoadForecast =
  struct
    type nonrec t =
      {
      timestamps: PredictiveScalingForecastTimestamps.t ;
      values: PredictiveScalingForecastValues.t ;
      metricSpecification: PredictiveScalingMetricSpecification.t }
    let context_ = "LoadForecast"
    let make ~timestamps =
      fun ~values ->
        fun ~metricSpecification ->
          fun () -> { timestamps; values; metricSpecification }
    let to_value x =
      structure_to_value
        [("Timestamps",
           (Some (PredictiveScalingForecastTimestamps.to_value x.timestamps)));
        ("Values",
          (Some (PredictiveScalingForecastValues.to_value x.values)));
        ("MetricSpecification",
          (Some
             (PredictiveScalingMetricSpecification.to_value
                x.metricSpecification)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricSpecification =
        PredictiveScalingMetricSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricSpecification") in
      let values =
        PredictiveScalingForecastValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Values") in
      let timestamps =
        PredictiveScalingForecastTimestamps.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Timestamps") in
      make ~metricSpecification ~values ~timestamps ()
    let of_json json =
      let metricSpecification =
        field_map_exn json "MetricSpecification"
          PredictiveScalingMetricSpecification.of_json in
      let values =
        field_map_exn json "Values" PredictiveScalingForecastValues.of_json in
      let timestamps =
        field_map_exn json "Timestamps"
          PredictiveScalingForecastTimestamps.of_json in
      make ~metricSpecification ~values ~timestamps ()
    let to_json v = composed_to_json to_value v
  end
module Activity =
  struct
    type nonrec t =
      {
      activityId: XmlString.t ;
      autoScalingGroupName: XmlStringMaxLen255.t ;
      description: XmlString.t option ;
      cause: XmlStringMaxLen1023.t ;
      startTime: TimestampType.t ;
      endTime: TimestampType.t option ;
      statusCode: ScalingActivityStatusCode.t ;
      statusMessage: XmlStringMaxLen255.t option ;
      progress: Progress.t option ;
      details: XmlString.t option ;
      autoScalingGroupState: AutoScalingGroupState.t option ;
      autoScalingGroupARN: ResourceName.t option }
    let context_ = "Activity"
    let make ?description =
      fun ?endTime ->
        fun ?statusMessage ->
          fun ?progress ->
            fun ?details ->
              fun ?autoScalingGroupState ->
                fun ?autoScalingGroupARN ->
                  fun ~activityId ->
                    fun ~autoScalingGroupName ->
                      fun ~cause ->
                        fun ~startTime ->
                          fun ~statusCode ->
                            fun () ->
                              {
                                description;
                                endTime;
                                statusMessage;
                                progress;
                                details;
                                autoScalingGroupState;
                                autoScalingGroupARN;
                                activityId;
                                autoScalingGroupName;
                                cause;
                                startTime;
                                statusCode
                              }
    let to_value x =
      structure_to_value
        [("ActivityId", (Some (XmlString.to_value x.activityId)));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("Description", (Option.map x.description ~f:XmlString.to_value));
        ("Cause", (Some (XmlStringMaxLen1023.to_value x.cause)));
        ("StartTime", (Some (TimestampType.to_value x.startTime)));
        ("EndTime", (Option.map x.endTime ~f:TimestampType.to_value));
        ("StatusCode",
          (Some (ScalingActivityStatusCode.to_value x.statusCode)));
        ("StatusMessage",
          (Option.map x.statusMessage ~f:XmlStringMaxLen255.to_value));
        ("Progress", (Option.map x.progress ~f:Progress.to_value));
        ("Details", (Option.map x.details ~f:XmlString.to_value));
        ("AutoScalingGroupState",
          (Option.map x.autoScalingGroupState
             ~f:AutoScalingGroupState.to_value));
        ("AutoScalingGroupARN",
          (Option.map x.autoScalingGroupARN ~f:ResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingGroupARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupARN") in
      let autoScalingGroupState =
        (Option.map ~f:AutoScalingGroupState.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupState") in
      let details =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Details") in
      let progress =
        (Option.map ~f:Progress.of_xml) (Xml.child xml_arg0 "Progress") in
      let statusMessage =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let statusCode =
        ScalingActivityStatusCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StatusCode") in
      let endTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        TimestampType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      let cause =
        XmlStringMaxLen1023.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Cause") in
      let description =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Description") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let activityId =
        XmlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ActivityId") in
      make ?autoScalingGroupARN ?autoScalingGroupState ?details ?progress
        ?statusMessage ~statusCode ?endTime ~startTime ~cause ?description
        ~autoScalingGroupName ~activityId ()
    let of_json json =
      let autoScalingGroupARN =
        field_map json "AutoScalingGroupARN" ResourceName.of_json in
      let autoScalingGroupState =
        field_map json "AutoScalingGroupState" AutoScalingGroupState.of_json in
      let details = field_map json "Details" XmlString.of_json in
      let progress = field_map json "Progress" Progress.of_json in
      let statusMessage =
        field_map json "StatusMessage" XmlStringMaxLen255.of_json in
      let statusCode =
        field_map_exn json "StatusCode" ScalingActivityStatusCode.of_json in
      let endTime = field_map json "EndTime" TimestampType.of_json in
      let startTime = field_map_exn json "StartTime" TimestampType.of_json in
      let cause = field_map_exn json "Cause" XmlStringMaxLen1023.of_json in
      let description = field_map json "Description" XmlString.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let activityId = field_map_exn json "ActivityId" XmlString.of_json in
      make ?autoScalingGroupARN ?autoScalingGroupState ?details ?progress
        ?statusMessage ~statusCode ?endTime ~startTime ~cause ?description
        ~autoScalingGroupName ~activityId ()
    let to_json v = composed_to_json to_value v
  end
module Filter =
  struct
    type nonrec t = {
      name: XmlString.t option ;
      values: Values.t option }
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:XmlString.to_value));
        ("Values", (Option.map x.values ~f:Values.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:Values.of_xml) (Xml.child xml_arg0 "Values") in
      let name = (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Name") in
      make ?values ?name ()
    let of_json json =
      let values = field_map json "Values" Values.of_json in
      let name = field_map json "Name" XmlString.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end
module NotificationConfiguration =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      topicARN: XmlStringMaxLen255.t option ;
      notificationType: XmlStringMaxLen255.t option }
    let make ?autoScalingGroupName =
      fun ?topicARN ->
        fun ?notificationType ->
          fun () -> { autoScalingGroupName; topicARN; notificationType }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("TopicARN", (Option.map x.topicARN ~f:XmlStringMaxLen255.to_value));
        ("NotificationType",
          (Option.map x.notificationType ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "NotificationType") in
      let topicARN =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "TopicARN") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ?notificationType ?topicARN ?autoScalingGroupName ()
    let of_json json =
      let notificationType =
        field_map json "NotificationType" XmlStringMaxLen255.of_json in
      let topicARN = field_map json "TopicARN" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?notificationType ?topicARN ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module MetricCollectionType =
  struct
    type nonrec t = {
      metric: XmlStringMaxLen255.t option }
    let make ?metric = fun () -> { metric }
    let to_value x =
      structure_to_value
        [("Metric", (Option.map x.metric ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metric =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Metric") in
      make ?metric ()
    let of_json json =
      let metric = field_map json "Metric" XmlStringMaxLen255.of_json in
      make ?metric ()
    let to_json v = composed_to_json to_value v
  end
module MetricGranularityType =
  struct
    type nonrec t = {
      granularity: XmlStringMaxLen255.t option }
    let make ?granularity = fun () -> { granularity }
    let to_value x =
      structure_to_value
        [("Granularity",
           (Option.map x.granularity ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let granularity =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Granularity") in
      make ?granularity ()
    let of_json json =
      let granularity =
        field_map json "Granularity" XmlStringMaxLen255.of_json in
      make ?granularity ()
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerState =
  struct
    type nonrec t =
      {
      loadBalancerName: XmlStringMaxLen255.t option ;
      state: XmlStringMaxLen255.t option }
    let make ?loadBalancerName =
      fun ?state -> fun () -> { loadBalancerName; state }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Option.map x.loadBalancerName ~f:XmlStringMaxLen255.to_value));
        ("State", (Option.map x.state ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "State") in
      let loadBalancerName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LoadBalancerName") in
      make ?state ?loadBalancerName ()
    let of_json json =
      let state = field_map json "State" XmlStringMaxLen255.of_json in
      let loadBalancerName =
        field_map json "LoadBalancerName" XmlStringMaxLen255.of_json in
      make ?state ?loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerTargetGroupState =
  struct
    type nonrec t =
      {
      loadBalancerTargetGroupARN: XmlStringMaxLen511.t option ;
      state: XmlStringMaxLen255.t option }
    let make ?loadBalancerTargetGroupARN =
      fun ?state -> fun () -> { loadBalancerTargetGroupARN; state }
    let to_value x =
      structure_to_value
        [("LoadBalancerTargetGroupARN",
           (Option.map x.loadBalancerTargetGroupARN
              ~f:XmlStringMaxLen511.to_value));
        ("State", (Option.map x.state ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "State") in
      let loadBalancerTargetGroupARN =
        (Option.map ~f:XmlStringMaxLen511.of_xml)
          (Xml.child xml_arg0 "LoadBalancerTargetGroupARN") in
      make ?state ?loadBalancerTargetGroupARN ()
    let of_json json =
      let state = field_map json "State" XmlStringMaxLen255.of_json in
      let loadBalancerTargetGroupARN =
        field_map json "LoadBalancerTargetGroupARN"
          XmlStringMaxLen511.of_json in
      make ?state ?loadBalancerTargetGroupARN ()
    let to_json v = composed_to_json to_value v
  end
module LifecycleHook =
  struct
    type nonrec t =
      {
      lifecycleHookName: AsciiStringMaxLen255.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      lifecycleTransition: LifecycleTransition.t option ;
      notificationTargetARN: NotificationTargetResourceName.t option ;
      roleARN: XmlStringMaxLen255.t option ;
      notificationMetadata: XmlStringMaxLen1023.t option ;
      heartbeatTimeout: HeartbeatTimeout.t option ;
      globalTimeout: GlobalTimeout.t option ;
      defaultResult: LifecycleActionResult.t option }
    let make ?lifecycleHookName =
      fun ?autoScalingGroupName ->
        fun ?lifecycleTransition ->
          fun ?notificationTargetARN ->
            fun ?roleARN ->
              fun ?notificationMetadata ->
                fun ?heartbeatTimeout ->
                  fun ?globalTimeout ->
                    fun ?defaultResult ->
                      fun () ->
                        {
                          lifecycleHookName;
                          autoScalingGroupName;
                          lifecycleTransition;
                          notificationTargetARN;
                          roleARN;
                          notificationMetadata;
                          heartbeatTimeout;
                          globalTimeout;
                          defaultResult
                        }
    let to_value x =
      structure_to_value
        [("LifecycleHookName",
           (Option.map x.lifecycleHookName ~f:AsciiStringMaxLen255.to_value));
        ("AutoScalingGroupName",
          (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("LifecycleTransition",
          (Option.map x.lifecycleTransition ~f:LifecycleTransition.to_value));
        ("NotificationTargetARN",
          (Option.map x.notificationTargetARN
             ~f:NotificationTargetResourceName.to_value));
        ("RoleARN", (Option.map x.roleARN ~f:XmlStringMaxLen255.to_value));
        ("NotificationMetadata",
          (Option.map x.notificationMetadata ~f:XmlStringMaxLen1023.to_value));
        ("HeartbeatTimeout",
          (Option.map x.heartbeatTimeout ~f:HeartbeatTimeout.to_value));
        ("GlobalTimeout",
          (Option.map x.globalTimeout ~f:GlobalTimeout.to_value));
        ("DefaultResult",
          (Option.map x.defaultResult ~f:LifecycleActionResult.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultResult =
        (Option.map ~f:LifecycleActionResult.of_xml)
          (Xml.child xml_arg0 "DefaultResult") in
      let globalTimeout =
        (Option.map ~f:GlobalTimeout.of_xml)
          (Xml.child xml_arg0 "GlobalTimeout") in
      let heartbeatTimeout =
        (Option.map ~f:HeartbeatTimeout.of_xml)
          (Xml.child xml_arg0 "HeartbeatTimeout") in
      let notificationMetadata =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "NotificationMetadata") in
      let roleARN =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "RoleARN") in
      let notificationTargetARN =
        (Option.map ~f:NotificationTargetResourceName.of_xml)
          (Xml.child xml_arg0 "NotificationTargetARN") in
      let lifecycleTransition =
        (Option.map ~f:LifecycleTransition.of_xml)
          (Xml.child xml_arg0 "LifecycleTransition") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      let lifecycleHookName =
        (Option.map ~f:AsciiStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LifecycleHookName") in
      make ?defaultResult ?globalTimeout ?heartbeatTimeout
        ?notificationMetadata ?roleARN ?notificationTargetARN
        ?lifecycleTransition ?autoScalingGroupName ?lifecycleHookName ()
    let of_json json =
      let defaultResult =
        field_map json "DefaultResult" LifecycleActionResult.of_json in
      let globalTimeout =
        field_map json "GlobalTimeout" GlobalTimeout.of_json in
      let heartbeatTimeout =
        field_map json "HeartbeatTimeout" HeartbeatTimeout.of_json in
      let notificationMetadata =
        field_map json "NotificationMetadata" XmlStringMaxLen1023.of_json in
      let roleARN = field_map json "RoleARN" XmlStringMaxLen255.of_json in
      let notificationTargetARN =
        field_map json "NotificationTargetARN"
          NotificationTargetResourceName.of_json in
      let lifecycleTransition =
        field_map json "LifecycleTransition" LifecycleTransition.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let lifecycleHookName =
        field_map json "LifecycleHookName" AsciiStringMaxLen255.of_json in
      make ?defaultResult ?globalTimeout ?heartbeatTimeout
        ?notificationMetadata ?roleARN ?notificationTargetARN
        ?lifecycleTransition ?autoScalingGroupName ?lifecycleHookName ()
    let to_json v = composed_to_json to_value v
  end
module InstanceRefresh =
  struct
    type nonrec t =
      {
      instanceRefreshId: XmlStringMaxLen255.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      status: InstanceRefreshStatus.t option ;
      statusReason: XmlStringMaxLen1023.t option ;
      startTime: TimestampType.t option ;
      endTime: TimestampType.t option ;
      percentageComplete: IntPercent.t option ;
      instancesToUpdate: InstancesToUpdate.t option ;
      progressDetails: InstanceRefreshProgressDetails.t option ;
      preferences: RefreshPreferences.t option ;
      desiredConfiguration: DesiredConfiguration.t option }
    let make ?instanceRefreshId =
      fun ?autoScalingGroupName ->
        fun ?status ->
          fun ?statusReason ->
            fun ?startTime ->
              fun ?endTime ->
                fun ?percentageComplete ->
                  fun ?instancesToUpdate ->
                    fun ?progressDetails ->
                      fun ?preferences ->
                        fun ?desiredConfiguration ->
                          fun () ->
                            {
                              instanceRefreshId;
                              autoScalingGroupName;
                              status;
                              statusReason;
                              startTime;
                              endTime;
                              percentageComplete;
                              instancesToUpdate;
                              progressDetails;
                              preferences;
                              desiredConfiguration
                            }
    let to_value x =
      structure_to_value
        [("InstanceRefreshId",
           (Option.map x.instanceRefreshId ~f:XmlStringMaxLen255.to_value));
        ("AutoScalingGroupName",
          (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("Status", (Option.map x.status ~f:InstanceRefreshStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:XmlStringMaxLen1023.to_value));
        ("StartTime", (Option.map x.startTime ~f:TimestampType.to_value));
        ("EndTime", (Option.map x.endTime ~f:TimestampType.to_value));
        ("PercentageComplete",
          (Option.map x.percentageComplete ~f:IntPercent.to_value));
        ("InstancesToUpdate",
          (Option.map x.instancesToUpdate ~f:InstancesToUpdate.to_value));
        ("ProgressDetails",
          (Option.map x.progressDetails
             ~f:InstanceRefreshProgressDetails.to_value));
        ("Preferences",
          (Option.map x.preferences ~f:RefreshPreferences.to_value));
        ("DesiredConfiguration",
          (Option.map x.desiredConfiguration ~f:DesiredConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let desiredConfiguration =
        (Option.map ~f:DesiredConfiguration.of_xml)
          (Xml.child xml_arg0 "DesiredConfiguration") in
      let preferences =
        (Option.map ~f:RefreshPreferences.of_xml)
          (Xml.child xml_arg0 "Preferences") in
      let progressDetails =
        (Option.map ~f:InstanceRefreshProgressDetails.of_xml)
          (Xml.child xml_arg0 "ProgressDetails") in
      let instancesToUpdate =
        (Option.map ~f:InstancesToUpdate.of_xml)
          (Xml.child xml_arg0 "InstancesToUpdate") in
      let percentageComplete =
        (Option.map ~f:IntPercent.of_xml)
          (Xml.child xml_arg0 "PercentageComplete") in
      let endTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "StartTime") in
      let statusReason =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:InstanceRefreshStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      let instanceRefreshId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceRefreshId") in
      make ?desiredConfiguration ?preferences ?progressDetails
        ?instancesToUpdate ?percentageComplete ?endTime ?startTime
        ?statusReason ?status ?autoScalingGroupName ?instanceRefreshId ()
    let of_json json =
      let desiredConfiguration =
        field_map json "DesiredConfiguration" DesiredConfiguration.of_json in
      let preferences =
        field_map json "Preferences" RefreshPreferences.of_json in
      let progressDetails =
        field_map json "ProgressDetails"
          InstanceRefreshProgressDetails.of_json in
      let instancesToUpdate =
        field_map json "InstancesToUpdate" InstancesToUpdate.of_json in
      let percentageComplete =
        field_map json "PercentageComplete" IntPercent.of_json in
      let endTime = field_map json "EndTime" TimestampType.of_json in
      let startTime = field_map json "StartTime" TimestampType.of_json in
      let statusReason =
        field_map json "StatusReason" XmlStringMaxLen1023.of_json in
      let status = field_map json "Status" InstanceRefreshStatus.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceRefreshId =
        field_map json "InstanceRefreshId" XmlStringMaxLen255.of_json in
      make ?desiredConfiguration ?preferences ?progressDetails
        ?instancesToUpdate ?percentageComplete ?endTime ?startTime
        ?statusReason ?status ?autoScalingGroupName ?instanceRefreshId ()
    let to_json v = composed_to_json to_value v
  end
module AdjustmentType =
  struct
    type nonrec t = {
      adjustmentType: XmlStringMaxLen255.t option }
    let make ?adjustmentType = fun () -> { adjustmentType }
    let to_value x =
      structure_to_value
        [("AdjustmentType",
           (Option.map x.adjustmentType ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let adjustmentType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AdjustmentType") in
      make ?adjustmentType ()
    let of_json json =
      let adjustmentType =
        field_map json "AdjustmentType" XmlStringMaxLen255.of_json in
      make ?adjustmentType ()
    let to_json v = composed_to_json to_value v
  end
module Tag =
  struct
    type nonrec t =
      {
      resourceId: XmlString.t option ;
      resourceType: XmlString.t option ;
      key: TagKey.t ;
      value: TagValue.t option ;
      propagateAtLaunch: PropagateAtLaunch.t option }
    let context_ = "Tag"
    let make ?resourceId =
      fun ?resourceType ->
        fun ?value ->
          fun ?propagateAtLaunch ->
            fun ~key ->
              fun () ->
                { resourceId; resourceType; value; propagateAtLaunch; key }
    let to_value x =
      structure_to_value
        [("ResourceId", (Option.map x.resourceId ~f:XmlString.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:XmlString.to_value));
        ("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Option.map x.value ~f:TagValue.to_value));
        ("PropagateAtLaunch",
          (Option.map x.propagateAtLaunch ~f:PropagateAtLaunch.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let propagateAtLaunch =
        (Option.map ~f:PropagateAtLaunch.of_xml)
          (Xml.child xml_arg0 "PropagateAtLaunch") in
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      let resourceType =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ResourceId") in
      make ?propagateAtLaunch ?value ~key ?resourceType ?resourceId ()
    let of_json json =
      let propagateAtLaunch =
        field_map json "PropagateAtLaunch" PropagateAtLaunch.of_json in
      let value = field_map json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      let resourceType = field_map json "ResourceType" XmlString.of_json in
      let resourceId = field_map json "ResourceId" XmlString.of_json in
      make ?propagateAtLaunch ?value ~key ?resourceType ?resourceId ()
    let to_json v = composed_to_json to_value v
  end
module LifecycleHookSpecification =
  struct
    type nonrec t =
      {
      lifecycleHookName: AsciiStringMaxLen255.t ;
      lifecycleTransition: LifecycleTransition.t ;
      notificationMetadata: XmlStringMaxLen1023.t option ;
      heartbeatTimeout: HeartbeatTimeout.t option ;
      defaultResult: LifecycleActionResult.t option ;
      notificationTargetARN: NotificationTargetResourceName.t option ;
      roleARN: XmlStringMaxLen255.t option }
    let context_ = "LifecycleHookSpecification"
    let make ?notificationMetadata =
      fun ?heartbeatTimeout ->
        fun ?defaultResult ->
          fun ?notificationTargetARN ->
            fun ?roleARN ->
              fun ~lifecycleHookName ->
                fun ~lifecycleTransition ->
                  fun () ->
                    {
                      notificationMetadata;
                      heartbeatTimeout;
                      defaultResult;
                      notificationTargetARN;
                      roleARN;
                      lifecycleHookName;
                      lifecycleTransition
                    }
    let to_value x =
      structure_to_value
        [("LifecycleHookName",
           (Some (AsciiStringMaxLen255.to_value x.lifecycleHookName)));
        ("LifecycleTransition",
          (Some (LifecycleTransition.to_value x.lifecycleTransition)));
        ("NotificationMetadata",
          (Option.map x.notificationMetadata ~f:XmlStringMaxLen1023.to_value));
        ("HeartbeatTimeout",
          (Option.map x.heartbeatTimeout ~f:HeartbeatTimeout.to_value));
        ("DefaultResult",
          (Option.map x.defaultResult ~f:LifecycleActionResult.to_value));
        ("NotificationTargetARN",
          (Option.map x.notificationTargetARN
             ~f:NotificationTargetResourceName.to_value));
        ("RoleARN", (Option.map x.roleARN ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let roleARN =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "RoleARN") in
      let notificationTargetARN =
        (Option.map ~f:NotificationTargetResourceName.of_xml)
          (Xml.child xml_arg0 "NotificationTargetARN") in
      let defaultResult =
        (Option.map ~f:LifecycleActionResult.of_xml)
          (Xml.child xml_arg0 "DefaultResult") in
      let heartbeatTimeout =
        (Option.map ~f:HeartbeatTimeout.of_xml)
          (Xml.child xml_arg0 "HeartbeatTimeout") in
      let notificationMetadata =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "NotificationMetadata") in
      let lifecycleTransition =
        LifecycleTransition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleTransition") in
      let lifecycleHookName =
        AsciiStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleHookName") in
      make ?roleARN ?notificationTargetARN ?defaultResult ?heartbeatTimeout
        ?notificationMetadata ~lifecycleTransition ~lifecycleHookName ()
    let of_json json =
      let roleARN = field_map json "RoleARN" XmlStringMaxLen255.of_json in
      let notificationTargetARN =
        field_map json "NotificationTargetARN"
          NotificationTargetResourceName.of_json in
      let defaultResult =
        field_map json "DefaultResult" LifecycleActionResult.of_json in
      let heartbeatTimeout =
        field_map json "HeartbeatTimeout" HeartbeatTimeout.of_json in
      let notificationMetadata =
        field_map json "NotificationMetadata" XmlStringMaxLen1023.of_json in
      let lifecycleTransition =
        field_map_exn json "LifecycleTransition" LifecycleTransition.of_json in
      let lifecycleHookName =
        field_map_exn json "LifecycleHookName" AsciiStringMaxLen255.of_json in
      make ?roleARN ?notificationTargetARN ?defaultResult ?heartbeatTimeout
        ?notificationMetadata ~lifecycleTransition ~lifecycleHookName ()
    let to_json v = composed_to_json to_value v
  end
module ScheduledUpdateGroupActionRequest =
  struct
    type nonrec t =
      {
      scheduledActionName: XmlStringMaxLen255.t ;
      startTime: TimestampType.t option ;
      endTime: TimestampType.t option ;
      recurrence: XmlStringMaxLen255.t option ;
      minSize: AutoScalingGroupMinSize.t option ;
      maxSize: AutoScalingGroupMaxSize.t option ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t option ;
      timeZone: XmlStringMaxLen255.t option }
    let context_ = "ScheduledUpdateGroupActionRequest"
    let make ?startTime =
      fun ?endTime ->
        fun ?recurrence ->
          fun ?minSize ->
            fun ?maxSize ->
              fun ?desiredCapacity ->
                fun ?timeZone ->
                  fun ~scheduledActionName ->
                    fun () ->
                      {
                        startTime;
                        endTime;
                        recurrence;
                        minSize;
                        maxSize;
                        desiredCapacity;
                        timeZone;
                        scheduledActionName
                      }
    let to_value x =
      structure_to_value
        [("ScheduledActionName",
           (Some (XmlStringMaxLen255.to_value x.scheduledActionName)));
        ("StartTime", (Option.map x.startTime ~f:TimestampType.to_value));
        ("EndTime", (Option.map x.endTime ~f:TimestampType.to_value));
        ("Recurrence",
          (Option.map x.recurrence ~f:XmlStringMaxLen255.to_value));
        ("MinSize",
          (Option.map x.minSize ~f:AutoScalingGroupMinSize.to_value));
        ("MaxSize",
          (Option.map x.maxSize ~f:AutoScalingGroupMaxSize.to_value));
        ("DesiredCapacity",
          (Option.map x.desiredCapacity
             ~f:AutoScalingGroupDesiredCapacity.to_value));
        ("TimeZone", (Option.map x.timeZone ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeZone =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "TimeZone") in
      let desiredCapacity =
        (Option.map ~f:AutoScalingGroupDesiredCapacity.of_xml)
          (Xml.child xml_arg0 "DesiredCapacity") in
      let maxSize =
        (Option.map ~f:AutoScalingGroupMaxSize.of_xml)
          (Xml.child xml_arg0 "MaxSize") in
      let minSize =
        (Option.map ~f:AutoScalingGroupMinSize.of_xml)
          (Xml.child xml_arg0 "MinSize") in
      let recurrence =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Recurrence") in
      let endTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "StartTime") in
      let scheduledActionName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduledActionName") in
      make ?timeZone ?desiredCapacity ?maxSize ?minSize ?recurrence ?endTime
        ?startTime ~scheduledActionName ()
    let of_json json =
      let timeZone = field_map json "TimeZone" XmlStringMaxLen255.of_json in
      let desiredCapacity =
        field_map json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let maxSize = field_map json "MaxSize" AutoScalingGroupMaxSize.of_json in
      let minSize = field_map json "MinSize" AutoScalingGroupMinSize.of_json in
      let recurrence = field_map json "Recurrence" XmlStringMaxLen255.of_json in
      let endTime = field_map json "EndTime" TimestampType.of_json in
      let startTime = field_map json "StartTime" TimestampType.of_json in
      let scheduledActionName =
        field_map_exn json "ScheduledActionName" XmlStringMaxLen255.of_json in
      make ?timeZone ?desiredCapacity ?maxSize ?minSize ?recurrence ?endTime
        ?startTime ~scheduledActionName ()
    let to_json v = composed_to_json to_value v
  end
module FailedScheduledUpdateGroupActionRequest =
  struct
    type nonrec t =
      {
      scheduledActionName: XmlStringMaxLen255.t ;
      errorCode: XmlStringMaxLen64.t option ;
      errorMessage: XmlString.t option }
    let context_ = "FailedScheduledUpdateGroupActionRequest"
    let make ?errorCode =
      fun ?errorMessage ->
        fun ~scheduledActionName ->
          fun () -> { errorCode; errorMessage; scheduledActionName }
    let to_value x =
      structure_to_value
        [("ScheduledActionName",
           (Some (XmlStringMaxLen255.to_value x.scheduledActionName)));
        ("ErrorCode", (Option.map x.errorCode ~f:XmlStringMaxLen64.to_value));
        ("ErrorMessage", (Option.map x.errorMessage ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ErrorMessage") in
      let errorCode =
        (Option.map ~f:XmlStringMaxLen64.of_xml)
          (Xml.child xml_arg0 "ErrorCode") in
      let scheduledActionName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduledActionName") in
      make ?errorMessage ?errorCode ~scheduledActionName ()
    let of_json json =
      let errorMessage = field_map json "ErrorMessage" XmlString.of_json in
      let errorCode = field_map json "ErrorCode" XmlStringMaxLen64.of_json in
      let scheduledActionName =
        field_map_exn json "ScheduledActionName" XmlStringMaxLen255.of_json in
      make ?errorMessage ?errorCode ~scheduledActionName ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingInstanceDetails =
  struct
    type nonrec t =
      {
      instanceId: XmlStringMaxLen19.t ;
      instanceType: XmlStringMaxLen255.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t ;
      availabilityZone: XmlStringMaxLen255.t ;
      lifecycleState: XmlStringMaxLen32.t ;
      healthStatus: XmlStringMaxLen32.t ;
      launchConfigurationName: XmlStringMaxLen255.t option ;
      launchTemplate: LaunchTemplateSpecification.t option ;
      protectedFromScaleIn: InstanceProtected.t ;
      weightedCapacity: XmlStringMaxLen32.t option }
    let context_ = "AutoScalingInstanceDetails"
    let make ?instanceType =
      fun ?launchConfigurationName ->
        fun ?launchTemplate ->
          fun ?weightedCapacity ->
            fun ~instanceId ->
              fun ~autoScalingGroupName ->
                fun ~availabilityZone ->
                  fun ~lifecycleState ->
                    fun ~healthStatus ->
                      fun ~protectedFromScaleIn ->
                        fun () ->
                          {
                            instanceType;
                            launchConfigurationName;
                            launchTemplate;
                            weightedCapacity;
                            instanceId;
                            autoScalingGroupName;
                            availabilityZone;
                            lifecycleState;
                            healthStatus;
                            protectedFromScaleIn
                          }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (XmlStringMaxLen19.to_value x.instanceId)));
        ("InstanceType",
          (Option.map x.instanceType ~f:XmlStringMaxLen255.to_value));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("AvailabilityZone",
          (Some (XmlStringMaxLen255.to_value x.availabilityZone)));
        ("LifecycleState",
          (Some (XmlStringMaxLen32.to_value x.lifecycleState)));
        ("HealthStatus", (Some (XmlStringMaxLen32.to_value x.healthStatus)));
        ("LaunchConfigurationName",
          (Option.map x.launchConfigurationName
             ~f:XmlStringMaxLen255.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:LaunchTemplateSpecification.to_value));
        ("ProtectedFromScaleIn",
          (Some (InstanceProtected.to_value x.protectedFromScaleIn)));
        ("WeightedCapacity",
          (Option.map x.weightedCapacity ~f:XmlStringMaxLen32.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let weightedCapacity =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "WeightedCapacity") in
      let protectedFromScaleIn =
        InstanceProtected.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectedFromScaleIn") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let launchConfigurationName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationName") in
      let healthStatus =
        XmlStringMaxLen32.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthStatus") in
      let lifecycleState =
        XmlStringMaxLen32.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleState") in
      let availabilityZone =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZone") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let instanceType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let instanceId =
        XmlStringMaxLen19.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?weightedCapacity ~protectedFromScaleIn ?launchTemplate
        ?launchConfigurationName ~healthStatus ~lifecycleState
        ~availabilityZone ~autoScalingGroupName ?instanceType ~instanceId ()
    let of_json json =
      let weightedCapacity =
        field_map json "WeightedCapacity" XmlStringMaxLen32.of_json in
      let protectedFromScaleIn =
        field_map_exn json "ProtectedFromScaleIn" InstanceProtected.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      let launchConfigurationName =
        field_map json "LaunchConfigurationName" XmlStringMaxLen255.of_json in
      let healthStatus =
        field_map_exn json "HealthStatus" XmlStringMaxLen32.of_json in
      let lifecycleState =
        field_map_exn json "LifecycleState" XmlStringMaxLen32.of_json in
      let availabilityZone =
        field_map_exn json "AvailabilityZone" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceType =
        field_map json "InstanceType" XmlStringMaxLen255.of_json in
      let instanceId =
        field_map_exn json "InstanceId" XmlStringMaxLen19.of_json in
      make ?weightedCapacity ~protectedFromScaleIn ?launchTemplate
        ?launchConfigurationName ~healthStatus ~lifecycleState
        ~availabilityZone ~autoScalingGroupName ?instanceType ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroup =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      autoScalingGroupARN: ResourceName.t option ;
      launchConfigurationName: XmlStringMaxLen255.t option ;
      launchTemplate: LaunchTemplateSpecification.t option ;
      mixedInstancesPolicy: MixedInstancesPolicy.t option ;
      minSize: AutoScalingGroupMinSize.t ;
      maxSize: AutoScalingGroupMaxSize.t ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t ;
      predictedCapacity: AutoScalingGroupPredictedCapacity.t option ;
      defaultCooldown: Cooldown.t ;
      availabilityZones: AvailabilityZones.t ;
      loadBalancerNames: LoadBalancerNames.t option ;
      targetGroupARNs: TargetGroupARNs.t option ;
      healthCheckType: XmlStringMaxLen32.t ;
      healthCheckGracePeriod: HealthCheckGracePeriod.t option ;
      instances: Instances.t option ;
      createdTime: TimestampType.t ;
      suspendedProcesses: SuspendedProcesses.t option ;
      placementGroup: XmlStringMaxLen255.t option ;
      vPCZoneIdentifier: XmlStringMaxLen2047.t option ;
      enabledMetrics: EnabledMetrics.t option ;
      status: XmlStringMaxLen255.t option ;
      tags: TagDescriptionList.t option ;
      terminationPolicies: TerminationPolicies.t option ;
      newInstancesProtectedFromScaleIn: InstanceProtected.t option ;
      serviceLinkedRoleARN: ResourceName.t option ;
      maxInstanceLifetime: MaxInstanceLifetime.t option ;
      capacityRebalance: CapacityRebalanceEnabled.t option ;
      warmPoolConfiguration: WarmPoolConfiguration.t option ;
      warmPoolSize: WarmPoolSize.t option ;
      context: Context.t option ;
      desiredCapacityType: XmlStringMaxLen255.t option }
    let context_ = "AutoScalingGroup"
    let make ?autoScalingGroupARN =
      fun ?launchConfigurationName ->
        fun ?launchTemplate ->
          fun ?mixedInstancesPolicy ->
            fun ?predictedCapacity ->
              fun ?loadBalancerNames ->
                fun ?targetGroupARNs ->
                  fun ?healthCheckGracePeriod ->
                    fun ?instances ->
                      fun ?suspendedProcesses ->
                        fun ?placementGroup ->
                          fun ?vPCZoneIdentifier ->
                            fun ?enabledMetrics ->
                              fun ?status ->
                                fun ?tags ->
                                  fun ?terminationPolicies ->
                                    fun ?newInstancesProtectedFromScaleIn ->
                                      fun ?serviceLinkedRoleARN ->
                                        fun ?maxInstanceLifetime ->
                                          fun ?capacityRebalance ->
                                            fun ?warmPoolConfiguration ->
                                              fun ?warmPoolSize ->
                                                fun ?context ->
                                                  fun ?desiredCapacityType ->
                                                    fun ~autoScalingGroupName
                                                      ->
                                                      fun ~minSize ->
                                                        fun ~maxSize ->
                                                          fun
                                                            ~desiredCapacity
                                                            ->
                                                            fun
                                                              ~defaultCooldown
                                                              ->
                                                              fun
                                                                ~availabilityZones
                                                                ->
                                                                fun
                                                                  ~healthCheckType
                                                                  ->
                                                                  fun
                                                                    ~createdTime
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    autoScalingGroupARN;
                                                                    launchConfigurationName;
                                                                    launchTemplate;
                                                                    mixedInstancesPolicy;
                                                                    predictedCapacity;
                                                                    loadBalancerNames;
                                                                    targetGroupARNs;
                                                                    healthCheckGracePeriod;
                                                                    instances;
                                                                    suspendedProcesses;
                                                                    placementGroup;
                                                                    vPCZoneIdentifier;
                                                                    enabledMetrics;
                                                                    status;
                                                                    tags;
                                                                    terminationPolicies;
                                                                    newInstancesProtectedFromScaleIn;
                                                                    serviceLinkedRoleARN;
                                                                    maxInstanceLifetime;
                                                                    capacityRebalance;
                                                                    warmPoolConfiguration;
                                                                    warmPoolSize;
                                                                    context;
                                                                    desiredCapacityType;
                                                                    autoScalingGroupName;
                                                                    minSize;
                                                                    maxSize;
                                                                    desiredCapacity;
                                                                    defaultCooldown;
                                                                    availabilityZones;
                                                                    healthCheckType;
                                                                    createdTime
                                                                    }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("AutoScalingGroupARN",
          (Option.map x.autoScalingGroupARN ~f:ResourceName.to_value));
        ("LaunchConfigurationName",
          (Option.map x.launchConfigurationName
             ~f:XmlStringMaxLen255.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:LaunchTemplateSpecification.to_value));
        ("MixedInstancesPolicy",
          (Option.map x.mixedInstancesPolicy ~f:MixedInstancesPolicy.to_value));
        ("MinSize", (Some (AutoScalingGroupMinSize.to_value x.minSize)));
        ("MaxSize", (Some (AutoScalingGroupMaxSize.to_value x.maxSize)));
        ("DesiredCapacity",
          (Some (AutoScalingGroupDesiredCapacity.to_value x.desiredCapacity)));
        ("PredictedCapacity",
          (Option.map x.predictedCapacity
             ~f:AutoScalingGroupPredictedCapacity.to_value));
        ("DefaultCooldown", (Some (Cooldown.to_value x.defaultCooldown)));
        ("AvailabilityZones",
          (Some (AvailabilityZones.to_value x.availabilityZones)));
        ("LoadBalancerNames",
          (Option.map x.loadBalancerNames ~f:LoadBalancerNames.to_value));
        ("TargetGroupARNs",
          (Option.map x.targetGroupARNs ~f:TargetGroupARNs.to_value));
        ("HealthCheckType",
          (Some (XmlStringMaxLen32.to_value x.healthCheckType)));
        ("HealthCheckGracePeriod",
          (Option.map x.healthCheckGracePeriod
             ~f:HealthCheckGracePeriod.to_value));
        ("Instances", (Option.map x.instances ~f:Instances.to_value));
        ("CreatedTime", (Some (TimestampType.to_value x.createdTime)));
        ("SuspendedProcesses",
          (Option.map x.suspendedProcesses ~f:SuspendedProcesses.to_value));
        ("PlacementGroup",
          (Option.map x.placementGroup ~f:XmlStringMaxLen255.to_value));
        ("VPCZoneIdentifier",
          (Option.map x.vPCZoneIdentifier ~f:XmlStringMaxLen2047.to_value));
        ("EnabledMetrics",
          (Option.map x.enabledMetrics ~f:EnabledMetrics.to_value));
        ("Status", (Option.map x.status ~f:XmlStringMaxLen255.to_value));
        ("Tags", (Option.map x.tags ~f:TagDescriptionList.to_value));
        ("TerminationPolicies",
          (Option.map x.terminationPolicies ~f:TerminationPolicies.to_value));
        ("NewInstancesProtectedFromScaleIn",
          (Option.map x.newInstancesProtectedFromScaleIn
             ~f:InstanceProtected.to_value));
        ("ServiceLinkedRoleARN",
          (Option.map x.serviceLinkedRoleARN ~f:ResourceName.to_value));
        ("MaxInstanceLifetime",
          (Option.map x.maxInstanceLifetime ~f:MaxInstanceLifetime.to_value));
        ("CapacityRebalance",
          (Option.map x.capacityRebalance
             ~f:CapacityRebalanceEnabled.to_value));
        ("WarmPoolConfiguration",
          (Option.map x.warmPoolConfiguration
             ~f:WarmPoolConfiguration.to_value));
        ("WarmPoolSize",
          (Option.map x.warmPoolSize ~f:WarmPoolSize.to_value));
        ("Context", (Option.map x.context ~f:Context.to_value));
        ("DesiredCapacityType",
          (Option.map x.desiredCapacityType ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let desiredCapacityType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "DesiredCapacityType") in
      let context =
        (Option.map ~f:Context.of_xml) (Xml.child xml_arg0 "Context") in
      let warmPoolSize =
        (Option.map ~f:WarmPoolSize.of_xml)
          (Xml.child xml_arg0 "WarmPoolSize") in
      let warmPoolConfiguration =
        (Option.map ~f:WarmPoolConfiguration.of_xml)
          (Xml.child xml_arg0 "WarmPoolConfiguration") in
      let capacityRebalance =
        (Option.map ~f:CapacityRebalanceEnabled.of_xml)
          (Xml.child xml_arg0 "CapacityRebalance") in
      let maxInstanceLifetime =
        (Option.map ~f:MaxInstanceLifetime.of_xml)
          (Xml.child xml_arg0 "MaxInstanceLifetime") in
      let serviceLinkedRoleARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ServiceLinkedRoleARN") in
      let newInstancesProtectedFromScaleIn =
        (Option.map ~f:InstanceProtected.of_xml)
          (Xml.child xml_arg0 "NewInstancesProtectedFromScaleIn") in
      let terminationPolicies =
        (Option.map ~f:TerminationPolicies.of_xml)
          (Xml.child xml_arg0 "TerminationPolicies") in
      let tags =
        (Option.map ~f:TagDescriptionList.of_xml) (Xml.child xml_arg0 "Tags") in
      let status =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Status") in
      let enabledMetrics =
        (Option.map ~f:EnabledMetrics.of_xml)
          (Xml.child xml_arg0 "EnabledMetrics") in
      let vPCZoneIdentifier =
        (Option.map ~f:XmlStringMaxLen2047.of_xml)
          (Xml.child xml_arg0 "VPCZoneIdentifier") in
      let placementGroup =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "PlacementGroup") in
      let suspendedProcesses =
        (Option.map ~f:SuspendedProcesses.of_xml)
          (Xml.child xml_arg0 "SuspendedProcesses") in
      let createdTime =
        TimestampType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreatedTime") in
      let instances =
        (Option.map ~f:Instances.of_xml) (Xml.child xml_arg0 "Instances") in
      let healthCheckGracePeriod =
        (Option.map ~f:HealthCheckGracePeriod.of_xml)
          (Xml.child xml_arg0 "HealthCheckGracePeriod") in
      let healthCheckType =
        XmlStringMaxLen32.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckType") in
      let targetGroupARNs =
        (Option.map ~f:TargetGroupARNs.of_xml)
          (Xml.child xml_arg0 "TargetGroupARNs") in
      let loadBalancerNames =
        (Option.map ~f:LoadBalancerNames.of_xml)
          (Xml.child xml_arg0 "LoadBalancerNames") in
      let availabilityZones =
        AvailabilityZones.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZones") in
      let defaultCooldown =
        Cooldown.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DefaultCooldown") in
      let predictedCapacity =
        (Option.map ~f:AutoScalingGroupPredictedCapacity.of_xml)
          (Xml.child xml_arg0 "PredictedCapacity") in
      let desiredCapacity =
        AutoScalingGroupDesiredCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DesiredCapacity") in
      let maxSize =
        AutoScalingGroupMaxSize.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxSize") in
      let minSize =
        AutoScalingGroupMinSize.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinSize") in
      let mixedInstancesPolicy =
        (Option.map ~f:MixedInstancesPolicy.of_xml)
          (Xml.child xml_arg0 "MixedInstancesPolicy") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let launchConfigurationName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationName") in
      let autoScalingGroupARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupARN") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?desiredCapacityType ?context ?warmPoolSize ?warmPoolConfiguration
        ?capacityRebalance ?maxInstanceLifetime ?serviceLinkedRoleARN
        ?newInstancesProtectedFromScaleIn ?terminationPolicies ?tags ?status
        ?enabledMetrics ?vPCZoneIdentifier ?placementGroup
        ?suspendedProcesses ~createdTime ?instances ?healthCheckGracePeriod
        ~healthCheckType ?targetGroupARNs ?loadBalancerNames
        ~availabilityZones ~defaultCooldown ?predictedCapacity
        ~desiredCapacity ~maxSize ~minSize ?mixedInstancesPolicy
        ?launchTemplate ?launchConfigurationName ?autoScalingGroupARN
        ~autoScalingGroupName ()
    let of_json json =
      let desiredCapacityType =
        field_map json "DesiredCapacityType" XmlStringMaxLen255.of_json in
      let context = field_map json "Context" Context.of_json in
      let warmPoolSize = field_map json "WarmPoolSize" WarmPoolSize.of_json in
      let warmPoolConfiguration =
        field_map json "WarmPoolConfiguration" WarmPoolConfiguration.of_json in
      let capacityRebalance =
        field_map json "CapacityRebalance" CapacityRebalanceEnabled.of_json in
      let maxInstanceLifetime =
        field_map json "MaxInstanceLifetime" MaxInstanceLifetime.of_json in
      let serviceLinkedRoleARN =
        field_map json "ServiceLinkedRoleARN" ResourceName.of_json in
      let newInstancesProtectedFromScaleIn =
        field_map json "NewInstancesProtectedFromScaleIn"
          InstanceProtected.of_json in
      let terminationPolicies =
        field_map json "TerminationPolicies" TerminationPolicies.of_json in
      let tags = field_map json "Tags" TagDescriptionList.of_json in
      let status = field_map json "Status" XmlStringMaxLen255.of_json in
      let enabledMetrics =
        field_map json "EnabledMetrics" EnabledMetrics.of_json in
      let vPCZoneIdentifier =
        field_map json "VPCZoneIdentifier" XmlStringMaxLen2047.of_json in
      let placementGroup =
        field_map json "PlacementGroup" XmlStringMaxLen255.of_json in
      let suspendedProcesses =
        field_map json "SuspendedProcesses" SuspendedProcesses.of_json in
      let createdTime =
        field_map_exn json "CreatedTime" TimestampType.of_json in
      let instances = field_map json "Instances" Instances.of_json in
      let healthCheckGracePeriod =
        field_map json "HealthCheckGracePeriod"
          HealthCheckGracePeriod.of_json in
      let healthCheckType =
        field_map_exn json "HealthCheckType" XmlStringMaxLen32.of_json in
      let targetGroupARNs =
        field_map json "TargetGroupARNs" TargetGroupARNs.of_json in
      let loadBalancerNames =
        field_map json "LoadBalancerNames" LoadBalancerNames.of_json in
      let availabilityZones =
        field_map_exn json "AvailabilityZones" AvailabilityZones.of_json in
      let defaultCooldown =
        field_map_exn json "DefaultCooldown" Cooldown.of_json in
      let predictedCapacity =
        field_map json "PredictedCapacity"
          AutoScalingGroupPredictedCapacity.of_json in
      let desiredCapacity =
        field_map_exn json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let maxSize =
        field_map_exn json "MaxSize" AutoScalingGroupMaxSize.of_json in
      let minSize =
        field_map_exn json "MinSize" AutoScalingGroupMinSize.of_json in
      let mixedInstancesPolicy =
        field_map json "MixedInstancesPolicy" MixedInstancesPolicy.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      let launchConfigurationName =
        field_map json "LaunchConfigurationName" XmlStringMaxLen255.of_json in
      let autoScalingGroupARN =
        field_map json "AutoScalingGroupARN" ResourceName.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?desiredCapacityType ?context ?warmPoolSize ?warmPoolConfiguration
        ?capacityRebalance ?maxInstanceLifetime ?serviceLinkedRoleARN
        ?newInstancesProtectedFromScaleIn ?terminationPolicies ?tags ?status
        ?enabledMetrics ?vPCZoneIdentifier ?placementGroup
        ?suspendedProcesses ~createdTime ?instances ?healthCheckGracePeriod
        ~healthCheckType ?targetGroupARNs ?loadBalancerNames
        ~availabilityZones ~defaultCooldown ?predictedCapacity
        ~desiredCapacity ~maxSize ~minSize ?mixedInstancesPolicy
        ?launchTemplate ?launchConfigurationName ?autoScalingGroupARN
        ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module ShouldDecrementDesiredCapacity =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module InvalidNextToken =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ResourceContentionFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module RefreshStrategy =
  struct
    type nonrec t =
      | Rolling 
      | Non_static_id of string 
    let make i = i
    let to_string = function | Rolling -> "Rolling" | Non_static_id s -> s
    let of_string = function | "Rolling" -> Rolling | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RefreshStrategy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RefreshStrategy" j)
    let to_json = simple_to_json to_value
  end
module InstanceRefreshInProgressFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module LimitExceededFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module InstanceIds =
  struct
    type nonrec t = XmlStringMaxLen19.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen19.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen19.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceIds" ~of_json:XmlStringMaxLen19.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProtectedFromScaleIn =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ShouldRespectGracePeriod =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module HonorCooldown =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ScheduledUpdateGroupActions =
  struct
    type nonrec t = ScheduledUpdateGroupAction.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ScheduledUpdateGroupAction.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScheduledUpdateGroupAction.of_xml)
    let of_json j =
      list_of_json ~kind:"ScheduledUpdateGroupActions"
        ~of_json:ScheduledUpdateGroupAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProcessNames =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"ProcessNames" ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module LifecycleActionToken =
  struct
    type nonrec t = string
    let context_ = "LifecycleActionToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:36) >>=
             (fun () -> check_string_min i ~min:36));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LifecycleActionToken" j
    let to_json = simple_to_json to_value
  end
module AutoScalingNotificationTypes =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"AutoScalingNotificationTypes"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module Processes =
  struct
    type nonrec t = ProcessType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProcessType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProcessType.of_xml)
    let of_json j =
      list_of_json ~kind:"Processes" ~of_json:ProcessType.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceLinkedRoleFailure =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ScalingPolicies =
  struct
    type nonrec t = ScalingPolicy.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ScalingPolicy.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScalingPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"ScalingPolicies" ~of_json:ScalingPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module LaunchConfigurations =
  struct
    type nonrec t = LaunchConfiguration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LaunchConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LaunchConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"LaunchConfigurations"
        ~of_json:LaunchConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module LaunchConfigurationNames =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"LaunchConfigurationNames"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxRecords =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxRecords" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CapacityForecast =
  struct
    type nonrec t =
      {
      timestamps: PredictiveScalingForecastTimestamps.t ;
      values: PredictiveScalingForecastValues.t }
    let context_ = "CapacityForecast"
    let make ~timestamps = fun ~values -> fun () -> { timestamps; values }
    let to_value x =
      structure_to_value
        [("Timestamps",
           (Some (PredictiveScalingForecastTimestamps.to_value x.timestamps)));
        ("Values",
          (Some (PredictiveScalingForecastValues.to_value x.values)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        PredictiveScalingForecastValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Values") in
      let timestamps =
        PredictiveScalingForecastTimestamps.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Timestamps") in
      make ~values ~timestamps ()
    let of_json json =
      let values =
        field_map_exn json "Values" PredictiveScalingForecastValues.of_json in
      let timestamps =
        field_map_exn json "Timestamps"
          PredictiveScalingForecastTimestamps.of_json in
      make ~values ~timestamps ()
    let to_json v = composed_to_json to_value v
  end
module LoadForecasts =
  struct
    type nonrec t = LoadForecast.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LoadForecast.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LoadForecast.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadForecasts" ~of_json:LoadForecast.of_json j
    let to_json v = composed_to_json to_value v
  end
module Activities =
  struct
    type nonrec t = Activity.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Activity.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Activity.of_xml)
    let of_json j =
      list_of_json ~kind:"Activities" ~of_json:Activity.of_json j
    let to_json v = composed_to_json to_value v
  end
module Metrics =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"Metrics" ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module Filters =
  struct
    type nonrec t = Filter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Filter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Filter.of_xml)
    let of_json j = list_of_json ~kind:"Filters" ~of_json:Filter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScheduledActionNames =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"ScheduledActionNames"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActivityIds =
  struct
    type nonrec t = XmlString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlString.of_xml)
    let of_json j =
      list_of_json ~kind:"ActivityIds" ~of_json:XmlString.of_json j
    let to_json v = composed_to_json to_value v
  end
module IncludeDeletedGroups =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module PolicyNames =
  struct
    type nonrec t = ResourceName.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceName.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyNames" ~of_json:ResourceName.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyTypes =
  struct
    type nonrec t = XmlStringMaxLen64.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen64.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen64.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyTypes" ~of_json:XmlStringMaxLen64.of_json j
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroupNames =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"AutoScalingGroupNames"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module NotificationConfigurations =
  struct
    type nonrec t = NotificationConfiguration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NotificationConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotificationConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationConfigurations"
        ~of_json:NotificationConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module MetricCollectionTypes =
  struct
    type nonrec t = MetricCollectionType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MetricCollectionType.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricCollectionType.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricCollectionTypes"
        ~of_json:MetricCollectionType.of_json j
    let to_json v = composed_to_json to_value v
  end
module MetricGranularityTypes =
  struct
    type nonrec t = MetricGranularityType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MetricGranularityType.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricGranularityType.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricGranularityTypes"
        ~of_json:MetricGranularityType.of_json j
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerStates =
  struct
    type nonrec t = LoadBalancerState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LoadBalancerState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LoadBalancerState.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancerStates"
        ~of_json:LoadBalancerState.of_json j
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerTargetGroupStates =
  struct
    type nonrec t = LoadBalancerTargetGroupState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LoadBalancerTargetGroupState.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LoadBalancerTargetGroupState.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancerTargetGroupStates"
        ~of_json:LoadBalancerTargetGroupState.of_json j
    let to_json v = composed_to_json to_value v
  end
module LifecycleHookNames =
  struct
    type nonrec t = AsciiStringMaxLen255.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let to_value xs =
      (xs |> (List.map ~f:AsciiStringMaxLen255.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AsciiStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"LifecycleHookNames"
        ~of_json:AsciiStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module LifecycleHooks =
  struct
    type nonrec t = LifecycleHook.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LifecycleHook.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LifecycleHook.of_xml)
    let of_json j =
      list_of_json ~kind:"LifecycleHooks" ~of_json:LifecycleHook.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceRefreshIds =
  struct
    type nonrec t = XmlStringMaxLen255.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen255.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen255.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceRefreshIds"
        ~of_json:XmlStringMaxLen255.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceRefreshes =
  struct
    type nonrec t = InstanceRefresh.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceRefresh.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceRefresh.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceRefreshes" ~of_json:InstanceRefresh.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AdjustmentTypes =
  struct
    type nonrec t = AdjustmentType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AdjustmentType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AdjustmentType.of_xml)
    let of_json j =
      list_of_json ~kind:"AdjustmentTypes" ~of_json:AdjustmentType.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxNumberOfAutoScalingGroups =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxNumberOfAutoScalingGroups"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MaxNumberOfLaunchConfigurations =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxNumberOfLaunchConfigurations"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NumberOfAutoScalingGroups =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NumberOfAutoScalingGroups"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NumberOfLaunchConfigurations =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NumberOfLaunchConfigurations"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ForceDelete =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ResourceInUseFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ScalingActivityInProgressFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module Tags =
  struct
    type nonrec t = Tag.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"Tags" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module LifecycleHookSpecifications =
  struct
    type nonrec t = LifecycleHookSpecification.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LifecycleHookSpecification.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LifecycleHookSpecification.of_xml)
    let of_json j =
      list_of_json ~kind:"LifecycleHookSpecifications"
        ~of_json:LifecycleHookSpecification.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActiveInstanceRefreshNotFoundFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ScheduledUpdateGroupActionRequests =
  struct
    type nonrec t = ScheduledUpdateGroupActionRequest.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ScheduledUpdateGroupActionRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ScheduledUpdateGroupActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"ScheduledUpdateGroupActionRequests"
        ~of_json:ScheduledUpdateGroupActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module AlreadyExistsFault =
  struct
    type nonrec t = {
      message: XmlStringMaxLen255.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" XmlStringMaxLen255.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module FailedScheduledUpdateGroupActionRequests =
  struct
    type nonrec t = FailedScheduledUpdateGroupActionRequest.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FailedScheduledUpdateGroupActionRequest.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:FailedScheduledUpdateGroupActionRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"FailedScheduledUpdateGroupActionRequests"
        ~of_json:FailedScheduledUpdateGroupActionRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module AutoScalingInstances =
  struct
    type nonrec t = AutoScalingInstanceDetails.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AutoScalingInstanceDetails.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AutoScalingInstanceDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"AutoScalingInstances"
        ~of_json:AutoScalingInstanceDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroups =
  struct
    type nonrec t = AutoScalingGroup.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AutoScalingGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AutoScalingGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"AutoScalingGroups"
        ~of_json:AutoScalingGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateAutoScalingGroupType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      launchConfigurationName: XmlStringMaxLen255.t option ;
      launchTemplate: LaunchTemplateSpecification.t option ;
      mixedInstancesPolicy: MixedInstancesPolicy.t option ;
      minSize: AutoScalingGroupMinSize.t option ;
      maxSize: AutoScalingGroupMaxSize.t option ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t option ;
      defaultCooldown: Cooldown.t option ;
      availabilityZones: AvailabilityZones.t option ;
      healthCheckType: XmlStringMaxLen32.t option ;
      healthCheckGracePeriod: HealthCheckGracePeriod.t option ;
      placementGroup: XmlStringMaxLen255.t option ;
      vPCZoneIdentifier: XmlStringMaxLen2047.t option ;
      terminationPolicies: TerminationPolicies.t option ;
      newInstancesProtectedFromScaleIn: InstanceProtected.t option ;
      serviceLinkedRoleARN: ResourceName.t option ;
      maxInstanceLifetime: MaxInstanceLifetime.t option ;
      capacityRebalance: CapacityRebalanceEnabled.t option ;
      context: Context.t option ;
      desiredCapacityType: XmlStringMaxLen255.t option }
    let context_ = "UpdateAutoScalingGroupType"
    let make ?launchConfigurationName =
      fun ?launchTemplate ->
        fun ?mixedInstancesPolicy ->
          fun ?minSize ->
            fun ?maxSize ->
              fun ?desiredCapacity ->
                fun ?defaultCooldown ->
                  fun ?availabilityZones ->
                    fun ?healthCheckType ->
                      fun ?healthCheckGracePeriod ->
                        fun ?placementGroup ->
                          fun ?vPCZoneIdentifier ->
                            fun ?terminationPolicies ->
                              fun ?newInstancesProtectedFromScaleIn ->
                                fun ?serviceLinkedRoleARN ->
                                  fun ?maxInstanceLifetime ->
                                    fun ?capacityRebalance ->
                                      fun ?context ->
                                        fun ?desiredCapacityType ->
                                          fun ~autoScalingGroupName ->
                                            fun () ->
                                              {
                                                launchConfigurationName;
                                                launchTemplate;
                                                mixedInstancesPolicy;
                                                minSize;
                                                maxSize;
                                                desiredCapacity;
                                                defaultCooldown;
                                                availabilityZones;
                                                healthCheckType;
                                                healthCheckGracePeriod;
                                                placementGroup;
                                                vPCZoneIdentifier;
                                                terminationPolicies;
                                                newInstancesProtectedFromScaleIn;
                                                serviceLinkedRoleARN;
                                                maxInstanceLifetime;
                                                capacityRebalance;
                                                context;
                                                desiredCapacityType;
                                                autoScalingGroupName
                                              }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("LaunchConfigurationName",
          (Option.map x.launchConfigurationName
             ~f:XmlStringMaxLen255.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:LaunchTemplateSpecification.to_value));
        ("MixedInstancesPolicy",
          (Option.map x.mixedInstancesPolicy ~f:MixedInstancesPolicy.to_value));
        ("MinSize",
          (Option.map x.minSize ~f:AutoScalingGroupMinSize.to_value));
        ("MaxSize",
          (Option.map x.maxSize ~f:AutoScalingGroupMaxSize.to_value));
        ("DesiredCapacity",
          (Option.map x.desiredCapacity
             ~f:AutoScalingGroupDesiredCapacity.to_value));
        ("DefaultCooldown",
          (Option.map x.defaultCooldown ~f:Cooldown.to_value));
        ("AvailabilityZones",
          (Option.map x.availabilityZones ~f:AvailabilityZones.to_value));
        ("HealthCheckType",
          (Option.map x.healthCheckType ~f:XmlStringMaxLen32.to_value));
        ("HealthCheckGracePeriod",
          (Option.map x.healthCheckGracePeriod
             ~f:HealthCheckGracePeriod.to_value));
        ("PlacementGroup",
          (Option.map x.placementGroup ~f:XmlStringMaxLen255.to_value));
        ("VPCZoneIdentifier",
          (Option.map x.vPCZoneIdentifier ~f:XmlStringMaxLen2047.to_value));
        ("TerminationPolicies",
          (Option.map x.terminationPolicies ~f:TerminationPolicies.to_value));
        ("NewInstancesProtectedFromScaleIn",
          (Option.map x.newInstancesProtectedFromScaleIn
             ~f:InstanceProtected.to_value));
        ("ServiceLinkedRoleARN",
          (Option.map x.serviceLinkedRoleARN ~f:ResourceName.to_value));
        ("MaxInstanceLifetime",
          (Option.map x.maxInstanceLifetime ~f:MaxInstanceLifetime.to_value));
        ("CapacityRebalance",
          (Option.map x.capacityRebalance
             ~f:CapacityRebalanceEnabled.to_value));
        ("Context", (Option.map x.context ~f:Context.to_value));
        ("DesiredCapacityType",
          (Option.map x.desiredCapacityType ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let desiredCapacityType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "DesiredCapacityType") in
      let context =
        (Option.map ~f:Context.of_xml) (Xml.child xml_arg0 "Context") in
      let capacityRebalance =
        (Option.map ~f:CapacityRebalanceEnabled.of_xml)
          (Xml.child xml_arg0 "CapacityRebalance") in
      let maxInstanceLifetime =
        (Option.map ~f:MaxInstanceLifetime.of_xml)
          (Xml.child xml_arg0 "MaxInstanceLifetime") in
      let serviceLinkedRoleARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ServiceLinkedRoleARN") in
      let newInstancesProtectedFromScaleIn =
        (Option.map ~f:InstanceProtected.of_xml)
          (Xml.child xml_arg0 "NewInstancesProtectedFromScaleIn") in
      let terminationPolicies =
        (Option.map ~f:TerminationPolicies.of_xml)
          (Xml.child xml_arg0 "TerminationPolicies") in
      let vPCZoneIdentifier =
        (Option.map ~f:XmlStringMaxLen2047.of_xml)
          (Xml.child xml_arg0 "VPCZoneIdentifier") in
      let placementGroup =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "PlacementGroup") in
      let healthCheckGracePeriod =
        (Option.map ~f:HealthCheckGracePeriod.of_xml)
          (Xml.child xml_arg0 "HealthCheckGracePeriod") in
      let healthCheckType =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "HealthCheckType") in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      let defaultCooldown =
        (Option.map ~f:Cooldown.of_xml)
          (Xml.child xml_arg0 "DefaultCooldown") in
      let desiredCapacity =
        (Option.map ~f:AutoScalingGroupDesiredCapacity.of_xml)
          (Xml.child xml_arg0 "DesiredCapacity") in
      let maxSize =
        (Option.map ~f:AutoScalingGroupMaxSize.of_xml)
          (Xml.child xml_arg0 "MaxSize") in
      let minSize =
        (Option.map ~f:AutoScalingGroupMinSize.of_xml)
          (Xml.child xml_arg0 "MinSize") in
      let mixedInstancesPolicy =
        (Option.map ~f:MixedInstancesPolicy.of_xml)
          (Xml.child xml_arg0 "MixedInstancesPolicy") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let launchConfigurationName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationName") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?desiredCapacityType ?context ?capacityRebalance
        ?maxInstanceLifetime ?serviceLinkedRoleARN
        ?newInstancesProtectedFromScaleIn ?terminationPolicies
        ?vPCZoneIdentifier ?placementGroup ?healthCheckGracePeriod
        ?healthCheckType ?availabilityZones ?defaultCooldown ?desiredCapacity
        ?maxSize ?minSize ?mixedInstancesPolicy ?launchTemplate
        ?launchConfigurationName ~autoScalingGroupName ()
    let of_json json =
      let desiredCapacityType =
        field_map json "DesiredCapacityType" XmlStringMaxLen255.of_json in
      let context = field_map json "Context" Context.of_json in
      let capacityRebalance =
        field_map json "CapacityRebalance" CapacityRebalanceEnabled.of_json in
      let maxInstanceLifetime =
        field_map json "MaxInstanceLifetime" MaxInstanceLifetime.of_json in
      let serviceLinkedRoleARN =
        field_map json "ServiceLinkedRoleARN" ResourceName.of_json in
      let newInstancesProtectedFromScaleIn =
        field_map json "NewInstancesProtectedFromScaleIn"
          InstanceProtected.of_json in
      let terminationPolicies =
        field_map json "TerminationPolicies" TerminationPolicies.of_json in
      let vPCZoneIdentifier =
        field_map json "VPCZoneIdentifier" XmlStringMaxLen2047.of_json in
      let placementGroup =
        field_map json "PlacementGroup" XmlStringMaxLen255.of_json in
      let healthCheckGracePeriod =
        field_map json "HealthCheckGracePeriod"
          HealthCheckGracePeriod.of_json in
      let healthCheckType =
        field_map json "HealthCheckType" XmlStringMaxLen32.of_json in
      let availabilityZones =
        field_map json "AvailabilityZones" AvailabilityZones.of_json in
      let defaultCooldown = field_map json "DefaultCooldown" Cooldown.of_json in
      let desiredCapacity =
        field_map json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let maxSize = field_map json "MaxSize" AutoScalingGroupMaxSize.of_json in
      let minSize = field_map json "MinSize" AutoScalingGroupMinSize.of_json in
      let mixedInstancesPolicy =
        field_map json "MixedInstancesPolicy" MixedInstancesPolicy.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      let launchConfigurationName =
        field_map json "LaunchConfigurationName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?desiredCapacityType ?context ?capacityRebalance
        ?maxInstanceLifetime ?serviceLinkedRoleARN
        ?newInstancesProtectedFromScaleIn ?terminationPolicies
        ?vPCZoneIdentifier ?placementGroup ?healthCheckGracePeriod
        ?healthCheckType ?availabilityZones ?defaultCooldown ?desiredCapacity
        ?maxSize ?minSize ?mixedInstancesPolicy ?launchTemplate
        ?launchConfigurationName ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module TerminateInstanceInAutoScalingGroupType =
  struct
    type nonrec t =
      {
      instanceId: XmlStringMaxLen19.t ;
      shouldDecrementDesiredCapacity: ShouldDecrementDesiredCapacity.t }
    let context_ = "TerminateInstanceInAutoScalingGroupType"
    let make ~instanceId =
      fun ~shouldDecrementDesiredCapacity ->
        fun () -> { instanceId; shouldDecrementDesiredCapacity }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (XmlStringMaxLen19.to_value x.instanceId)));
        ("ShouldDecrementDesiredCapacity",
          (Some
             (ShouldDecrementDesiredCapacity.to_value
                x.shouldDecrementDesiredCapacity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shouldDecrementDesiredCapacity =
        ShouldDecrementDesiredCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ShouldDecrementDesiredCapacity") in
      let instanceId =
        XmlStringMaxLen19.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ~shouldDecrementDesiredCapacity ~instanceId ()
    let of_json json =
      let shouldDecrementDesiredCapacity =
        field_map_exn json "ShouldDecrementDesiredCapacity"
          ShouldDecrementDesiredCapacity.of_json in
      let instanceId =
        field_map_exn json "InstanceId" XmlStringMaxLen19.of_json in
      make ~shouldDecrementDesiredCapacity ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module TagsType =
  struct
    type describeTagsResult =
      {
      tags: TagDescriptionList.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeTagsResult: describeTagsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "TagsType"
    let make ?tags =
      fun ?nextToken ->
        fun () ->
          { describeTagsResult = { tags; nextToken }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeTagsResult in
      structure_to_wrapped_value
        [("Tags", (Option.map x.tags ~f:TagDescriptionList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeTagsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "DescribeTagsResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let tags =
        (Option.map ~f:TagDescriptionList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?nextToken ?tags ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let tags = field_map json "Tags" TagDescriptionList.of_json in
      make ?nextToken ?tags ()
    let to_json v = composed_to_json to_value v
  end
module StartInstanceRefreshType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      strategy: RefreshStrategy.t option ;
      desiredConfiguration: DesiredConfiguration.t option ;
      preferences: RefreshPreferences.t option }
    let context_ = "StartInstanceRefreshType"
    let make ?strategy =
      fun ?desiredConfiguration ->
        fun ?preferences ->
          fun ~autoScalingGroupName ->
            fun () ->
              {
                strategy;
                desiredConfiguration;
                preferences;
                autoScalingGroupName
              }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("Strategy", (Option.map x.strategy ~f:RefreshStrategy.to_value));
        ("DesiredConfiguration",
          (Option.map x.desiredConfiguration ~f:DesiredConfiguration.to_value));
        ("Preferences",
          (Option.map x.preferences ~f:RefreshPreferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preferences =
        (Option.map ~f:RefreshPreferences.of_xml)
          (Xml.child xml_arg0 "Preferences") in
      let desiredConfiguration =
        (Option.map ~f:DesiredConfiguration.of_xml)
          (Xml.child xml_arg0 "DesiredConfiguration") in
      let strategy =
        (Option.map ~f:RefreshStrategy.of_xml)
          (Xml.child xml_arg0 "Strategy") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?preferences ?desiredConfiguration ?strategy ~autoScalingGroupName
        ()
    let of_json json =
      let preferences =
        field_map json "Preferences" RefreshPreferences.of_json in
      let desiredConfiguration =
        field_map json "DesiredConfiguration" DesiredConfiguration.of_json in
      let strategy = field_map json "Strategy" RefreshStrategy.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?preferences ?desiredConfiguration ?strategy ~autoScalingGroupName
        ()
    let to_json v = composed_to_json to_value v
  end
module StartInstanceRefreshAnswer =
  struct
    type startInstanceRefreshResult =
      {
      instanceRefreshId: XmlStringMaxLen255.t option }
    and responseMetaData = unit
    and t =
      {
      startInstanceRefreshResult: startInstanceRefreshResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InstanceRefreshInProgressFault of InstanceRefreshInProgressFault.t 
      | `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "StartInstanceRefreshAnswer"
    let make ?instanceRefreshId =
      fun () ->
        {
          startInstanceRefreshResult = { instanceRefreshId };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "InstanceRefreshInProgressFault" ->
          `InstanceRefreshInProgressFault
            (InstanceRefreshInProgressFault.of_json json)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InstanceRefreshInProgressFault" ->
          `InstanceRefreshInProgressFault
            (InstanceRefreshInProgressFault.of_xml xml)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InstanceRefreshInProgressFault e ->
          `Assoc
            [("error", (`String "InstanceRefreshInProgressFault"));
            ("details", (InstanceRefreshInProgressFault.to_json e))]
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.startInstanceRefreshResult in
      structure_to_wrapped_value
        [("InstanceRefreshId",
           (Option.map x.instanceRefreshId ~f:XmlStringMaxLen255.to_value))]
        ~wrapper:"StartInstanceRefreshResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "StartInstanceRefreshResult" in
      let instanceRefreshId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceRefreshId") in
      make ?instanceRefreshId ()
    let of_json json =
      let instanceRefreshId =
        field_map json "InstanceRefreshId" XmlStringMaxLen255.of_json in
      make ?instanceRefreshId ()
    let to_json v = composed_to_json to_value v
  end
module SetInstanceProtectionQuery =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIds.t ;
      autoScalingGroupName: XmlStringMaxLen255.t ;
      protectedFromScaleIn: ProtectedFromScaleIn.t }
    let context_ = "SetInstanceProtectionQuery"
    let make ~instanceIds =
      fun ~autoScalingGroupName ->
        fun ~protectedFromScaleIn ->
          fun () ->
            { instanceIds; autoScalingGroupName; protectedFromScaleIn }
    let to_value x =
      structure_to_value
        [("InstanceIds", (Some (InstanceIds.to_value x.instanceIds)));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ProtectedFromScaleIn",
          (Some (ProtectedFromScaleIn.to_value x.protectedFromScaleIn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectedFromScaleIn =
        ProtectedFromScaleIn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectedFromScaleIn") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let instanceIds =
        InstanceIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceIds") in
      make ~protectedFromScaleIn ~autoScalingGroupName ~instanceIds ()
    let of_json json =
      let protectedFromScaleIn =
        field_map_exn json "ProtectedFromScaleIn"
          ProtectedFromScaleIn.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceIds = field_map_exn json "InstanceIds" InstanceIds.of_json in
      make ~protectedFromScaleIn ~autoScalingGroupName ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end
module SetInstanceProtectionAnswer =
  struct
    type setInstanceProtectionResult = unit
    and responseMetaData = unit
    and t =
      {
      setInstanceProtectionResult: setInstanceProtectionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { setInstanceProtectionResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module SetInstanceHealthQuery =
  struct
    type nonrec t =
      {
      instanceId: XmlStringMaxLen19.t ;
      healthStatus: XmlStringMaxLen32.t ;
      shouldRespectGracePeriod: ShouldRespectGracePeriod.t option }
    let context_ = "SetInstanceHealthQuery"
    let make ?shouldRespectGracePeriod =
      fun ~instanceId ->
        fun ~healthStatus ->
          fun () -> { shouldRespectGracePeriod; instanceId; healthStatus }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (XmlStringMaxLen19.to_value x.instanceId)));
        ("HealthStatus", (Some (XmlStringMaxLen32.to_value x.healthStatus)));
        ("ShouldRespectGracePeriod",
          (Option.map x.shouldRespectGracePeriod
             ~f:ShouldRespectGracePeriod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shouldRespectGracePeriod =
        (Option.map ~f:ShouldRespectGracePeriod.of_xml)
          (Xml.child xml_arg0 "ShouldRespectGracePeriod") in
      let healthStatus =
        XmlStringMaxLen32.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthStatus") in
      let instanceId =
        XmlStringMaxLen19.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?shouldRespectGracePeriod ~healthStatus ~instanceId ()
    let of_json json =
      let shouldRespectGracePeriod =
        field_map json "ShouldRespectGracePeriod"
          ShouldRespectGracePeriod.of_json in
      let healthStatus =
        field_map_exn json "HealthStatus" XmlStringMaxLen32.of_json in
      let instanceId =
        field_map_exn json "InstanceId" XmlStringMaxLen19.of_json in
      make ?shouldRespectGracePeriod ~healthStatus ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module SetDesiredCapacityType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t ;
      honorCooldown: HonorCooldown.t option }
    let context_ = "SetDesiredCapacityType"
    let make ?honorCooldown =
      fun ~autoScalingGroupName ->
        fun ~desiredCapacity ->
          fun () -> { honorCooldown; autoScalingGroupName; desiredCapacity }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("DesiredCapacity",
          (Some (AutoScalingGroupDesiredCapacity.to_value x.desiredCapacity)));
        ("HonorCooldown",
          (Option.map x.honorCooldown ~f:HonorCooldown.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let honorCooldown =
        (Option.map ~f:HonorCooldown.of_xml)
          (Xml.child xml_arg0 "HonorCooldown") in
      let desiredCapacity =
        AutoScalingGroupDesiredCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DesiredCapacity") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?honorCooldown ~desiredCapacity ~autoScalingGroupName ()
    let of_json json =
      let honorCooldown =
        field_map json "HonorCooldown" HonorCooldown.of_json in
      let desiredCapacity =
        field_map_exn json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?honorCooldown ~desiredCapacity ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module ScheduledActionsType =
  struct
    type describeScheduledActionsResult =
      {
      scheduledUpdateGroupActions: ScheduledUpdateGroupActions.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeScheduledActionsResult: describeScheduledActionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ScheduledActionsType"
    let make ?scheduledUpdateGroupActions =
      fun ?nextToken ->
        fun () ->
          {
            describeScheduledActionsResult =
              { scheduledUpdateGroupActions; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeScheduledActionsResult in
      structure_to_wrapped_value
        [("ScheduledUpdateGroupActions",
           (Option.map x.scheduledUpdateGroupActions
              ~f:ScheduledUpdateGroupActions.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeScheduledActionsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeScheduledActionsResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let scheduledUpdateGroupActions =
        (Option.map ~f:ScheduledUpdateGroupActions.of_xml)
          (Xml.child xml_arg0 "ScheduledUpdateGroupActions") in
      make ?nextToken ?scheduledUpdateGroupActions ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let scheduledUpdateGroupActions =
        field_map json "ScheduledUpdateGroupActions"
          ScheduledUpdateGroupActions.of_json in
      make ?nextToken ?scheduledUpdateGroupActions ()
    let to_json v = composed_to_json to_value v
  end
module ScalingProcessQuery =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      scalingProcesses: ProcessNames.t option }
    let context_ = "ScalingProcessQuery"
    let make ?scalingProcesses =
      fun ~autoScalingGroupName ->
        fun () -> { scalingProcesses; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ScalingProcesses",
          (Option.map x.scalingProcesses ~f:ProcessNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scalingProcesses =
        (Option.map ~f:ProcessNames.of_xml)
          (Xml.child xml_arg0 "ScalingProcesses") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?scalingProcesses ~autoScalingGroupName ()
    let of_json json =
      let scalingProcesses =
        field_map json "ScalingProcesses" ProcessNames.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?scalingProcesses ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module RecordLifecycleActionHeartbeatType =
  struct
    type nonrec t =
      {
      lifecycleHookName: AsciiStringMaxLen255.t ;
      autoScalingGroupName: ResourceName.t ;
      lifecycleActionToken: LifecycleActionToken.t option ;
      instanceId: XmlStringMaxLen19.t option }
    let context_ = "RecordLifecycleActionHeartbeatType"
    let make ?lifecycleActionToken =
      fun ?instanceId ->
        fun ~lifecycleHookName ->
          fun ~autoScalingGroupName ->
            fun () ->
              {
                lifecycleActionToken;
                instanceId;
                lifecycleHookName;
                autoScalingGroupName
              }
    let to_value x =
      structure_to_value
        [("LifecycleHookName",
           (Some (AsciiStringMaxLen255.to_value x.lifecycleHookName)));
        ("AutoScalingGroupName",
          (Some (ResourceName.to_value x.autoScalingGroupName)));
        ("LifecycleActionToken",
          (Option.map x.lifecycleActionToken ~f:LifecycleActionToken.to_value));
        ("InstanceId",
          (Option.map x.instanceId ~f:XmlStringMaxLen19.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        (Option.map ~f:XmlStringMaxLen19.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let lifecycleActionToken =
        (Option.map ~f:LifecycleActionToken.of_xml)
          (Xml.child xml_arg0 "LifecycleActionToken") in
      let autoScalingGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let lifecycleHookName =
        AsciiStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleHookName") in
      make ?instanceId ?lifecycleActionToken ~autoScalingGroupName
        ~lifecycleHookName ()
    let of_json json =
      let instanceId = field_map json "InstanceId" XmlStringMaxLen19.of_json in
      let lifecycleActionToken =
        field_map json "LifecycleActionToken" LifecycleActionToken.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" ResourceName.of_json in
      let lifecycleHookName =
        field_map_exn json "LifecycleHookName" AsciiStringMaxLen255.of_json in
      make ?instanceId ?lifecycleActionToken ~autoScalingGroupName
        ~lifecycleHookName ()
    let to_json v = composed_to_json to_value v
  end
module RecordLifecycleActionHeartbeatAnswer =
  struct
    type recordLifecycleActionHeartbeatResult = unit
    and responseMetaData = unit
    and t =
      {
      recordLifecycleActionHeartbeatResult:
        recordLifecycleActionHeartbeatResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { recordLifecycleActionHeartbeatResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutWarmPoolType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      maxGroupPreparedCapacity: MaxGroupPreparedCapacity.t option ;
      minSize: WarmPoolMinSize.t option ;
      poolState: WarmPoolState.t option ;
      instanceReusePolicy: InstanceReusePolicy.t option }
    let context_ = "PutWarmPoolType"
    let make ?maxGroupPreparedCapacity =
      fun ?minSize ->
        fun ?poolState ->
          fun ?instanceReusePolicy ->
            fun ~autoScalingGroupName ->
              fun () ->
                {
                  maxGroupPreparedCapacity;
                  minSize;
                  poolState;
                  instanceReusePolicy;
                  autoScalingGroupName
                }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("MaxGroupPreparedCapacity",
          (Option.map x.maxGroupPreparedCapacity
             ~f:MaxGroupPreparedCapacity.to_value));
        ("MinSize", (Option.map x.minSize ~f:WarmPoolMinSize.to_value));
        ("PoolState", (Option.map x.poolState ~f:WarmPoolState.to_value));
        ("InstanceReusePolicy",
          (Option.map x.instanceReusePolicy ~f:InstanceReusePolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceReusePolicy =
        (Option.map ~f:InstanceReusePolicy.of_xml)
          (Xml.child xml_arg0 "InstanceReusePolicy") in
      let poolState =
        (Option.map ~f:WarmPoolState.of_xml) (Xml.child xml_arg0 "PoolState") in
      let minSize =
        (Option.map ~f:WarmPoolMinSize.of_xml) (Xml.child xml_arg0 "MinSize") in
      let maxGroupPreparedCapacity =
        (Option.map ~f:MaxGroupPreparedCapacity.of_xml)
          (Xml.child xml_arg0 "MaxGroupPreparedCapacity") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?instanceReusePolicy ?poolState ?minSize ?maxGroupPreparedCapacity
        ~autoScalingGroupName ()
    let of_json json =
      let instanceReusePolicy =
        field_map json "InstanceReusePolicy" InstanceReusePolicy.of_json in
      let poolState = field_map json "PoolState" WarmPoolState.of_json in
      let minSize = field_map json "MinSize" WarmPoolMinSize.of_json in
      let maxGroupPreparedCapacity =
        field_map json "MaxGroupPreparedCapacity"
          MaxGroupPreparedCapacity.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?instanceReusePolicy ?poolState ?minSize ?maxGroupPreparedCapacity
        ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module PutWarmPoolAnswer =
  struct
    type putWarmPoolResult = unit
    and responseMetaData = unit
    and t =
      {
      putWarmPoolResult: putWarmPoolResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { putWarmPoolResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutScheduledUpdateGroupActionType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      scheduledActionName: XmlStringMaxLen255.t ;
      time: TimestampType.t option ;
      startTime: TimestampType.t option ;
      endTime: TimestampType.t option ;
      recurrence: XmlStringMaxLen255.t option ;
      minSize: AutoScalingGroupMinSize.t option ;
      maxSize: AutoScalingGroupMaxSize.t option ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t option ;
      timeZone: XmlStringMaxLen255.t option }
    let context_ = "PutScheduledUpdateGroupActionType"
    let make ?time =
      fun ?startTime ->
        fun ?endTime ->
          fun ?recurrence ->
            fun ?minSize ->
              fun ?maxSize ->
                fun ?desiredCapacity ->
                  fun ?timeZone ->
                    fun ~autoScalingGroupName ->
                      fun ~scheduledActionName ->
                        fun () ->
                          {
                            time;
                            startTime;
                            endTime;
                            recurrence;
                            minSize;
                            maxSize;
                            desiredCapacity;
                            timeZone;
                            autoScalingGroupName;
                            scheduledActionName
                          }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ScheduledActionName",
          (Some (XmlStringMaxLen255.to_value x.scheduledActionName)));
        ("Time", (Option.map x.time ~f:TimestampType.to_value));
        ("StartTime", (Option.map x.startTime ~f:TimestampType.to_value));
        ("EndTime", (Option.map x.endTime ~f:TimestampType.to_value));
        ("Recurrence",
          (Option.map x.recurrence ~f:XmlStringMaxLen255.to_value));
        ("MinSize",
          (Option.map x.minSize ~f:AutoScalingGroupMinSize.to_value));
        ("MaxSize",
          (Option.map x.maxSize ~f:AutoScalingGroupMaxSize.to_value));
        ("DesiredCapacity",
          (Option.map x.desiredCapacity
             ~f:AutoScalingGroupDesiredCapacity.to_value));
        ("TimeZone", (Option.map x.timeZone ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeZone =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "TimeZone") in
      let desiredCapacity =
        (Option.map ~f:AutoScalingGroupDesiredCapacity.of_xml)
          (Xml.child xml_arg0 "DesiredCapacity") in
      let maxSize =
        (Option.map ~f:AutoScalingGroupMaxSize.of_xml)
          (Xml.child xml_arg0 "MaxSize") in
      let minSize =
        (Option.map ~f:AutoScalingGroupMinSize.of_xml)
          (Xml.child xml_arg0 "MinSize") in
      let recurrence =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "Recurrence") in
      let endTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "StartTime") in
      let time =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "Time") in
      let scheduledActionName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduledActionName") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?timeZone ?desiredCapacity ?maxSize ?minSize ?recurrence ?endTime
        ?startTime ?time ~scheduledActionName ~autoScalingGroupName ()
    let of_json json =
      let timeZone = field_map json "TimeZone" XmlStringMaxLen255.of_json in
      let desiredCapacity =
        field_map json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let maxSize = field_map json "MaxSize" AutoScalingGroupMaxSize.of_json in
      let minSize = field_map json "MinSize" AutoScalingGroupMinSize.of_json in
      let recurrence = field_map json "Recurrence" XmlStringMaxLen255.of_json in
      let endTime = field_map json "EndTime" TimestampType.of_json in
      let startTime = field_map json "StartTime" TimestampType.of_json in
      let time = field_map json "Time" TimestampType.of_json in
      let scheduledActionName =
        field_map_exn json "ScheduledActionName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?timeZone ?desiredCapacity ?maxSize ?minSize ?recurrence ?endTime
        ?startTime ?time ~scheduledActionName ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module PutScalingPolicyType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      policyName: XmlStringMaxLen255.t ;
      policyType: XmlStringMaxLen64.t option ;
      adjustmentType: XmlStringMaxLen255.t option ;
      minAdjustmentStep: MinAdjustmentStep.t option ;
      minAdjustmentMagnitude: MinAdjustmentMagnitude.t option ;
      scalingAdjustment: PolicyIncrement.t option ;
      cooldown: Cooldown.t option ;
      metricAggregationType: XmlStringMaxLen32.t option ;
      stepAdjustments: StepAdjustments.t option ;
      estimatedInstanceWarmup: EstimatedInstanceWarmup.t option ;
      targetTrackingConfiguration: TargetTrackingConfiguration.t option ;
      enabled: ScalingPolicyEnabled.t option ;
      predictiveScalingConfiguration: PredictiveScalingConfiguration.t option }
    let context_ = "PutScalingPolicyType"
    let make ?policyType =
      fun ?adjustmentType ->
        fun ?minAdjustmentStep ->
          fun ?minAdjustmentMagnitude ->
            fun ?scalingAdjustment ->
              fun ?cooldown ->
                fun ?metricAggregationType ->
                  fun ?stepAdjustments ->
                    fun ?estimatedInstanceWarmup ->
                      fun ?targetTrackingConfiguration ->
                        fun ?enabled ->
                          fun ?predictiveScalingConfiguration ->
                            fun ~autoScalingGroupName ->
                              fun ~policyName ->
                                fun () ->
                                  {
                                    policyType;
                                    adjustmentType;
                                    minAdjustmentStep;
                                    minAdjustmentMagnitude;
                                    scalingAdjustment;
                                    cooldown;
                                    metricAggregationType;
                                    stepAdjustments;
                                    estimatedInstanceWarmup;
                                    targetTrackingConfiguration;
                                    enabled;
                                    predictiveScalingConfiguration;
                                    autoScalingGroupName;
                                    policyName
                                  }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("PolicyName", (Some (XmlStringMaxLen255.to_value x.policyName)));
        ("PolicyType",
          (Option.map x.policyType ~f:XmlStringMaxLen64.to_value));
        ("AdjustmentType",
          (Option.map x.adjustmentType ~f:XmlStringMaxLen255.to_value));
        ("MinAdjustmentStep",
          (Option.map x.minAdjustmentStep ~f:MinAdjustmentStep.to_value));
        ("MinAdjustmentMagnitude",
          (Option.map x.minAdjustmentMagnitude
             ~f:MinAdjustmentMagnitude.to_value));
        ("ScalingAdjustment",
          (Option.map x.scalingAdjustment ~f:PolicyIncrement.to_value));
        ("Cooldown", (Option.map x.cooldown ~f:Cooldown.to_value));
        ("MetricAggregationType",
          (Option.map x.metricAggregationType ~f:XmlStringMaxLen32.to_value));
        ("StepAdjustments",
          (Option.map x.stepAdjustments ~f:StepAdjustments.to_value));
        ("EstimatedInstanceWarmup",
          (Option.map x.estimatedInstanceWarmup
             ~f:EstimatedInstanceWarmup.to_value));
        ("TargetTrackingConfiguration",
          (Option.map x.targetTrackingConfiguration
             ~f:TargetTrackingConfiguration.to_value));
        ("Enabled", (Option.map x.enabled ~f:ScalingPolicyEnabled.to_value));
        ("PredictiveScalingConfiguration",
          (Option.map x.predictiveScalingConfiguration
             ~f:PredictiveScalingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let predictiveScalingConfiguration =
        (Option.map ~f:PredictiveScalingConfiguration.of_xml)
          (Xml.child xml_arg0 "PredictiveScalingConfiguration") in
      let enabled =
        (Option.map ~f:ScalingPolicyEnabled.of_xml)
          (Xml.child xml_arg0 "Enabled") in
      let targetTrackingConfiguration =
        (Option.map ~f:TargetTrackingConfiguration.of_xml)
          (Xml.child xml_arg0 "TargetTrackingConfiguration") in
      let estimatedInstanceWarmup =
        (Option.map ~f:EstimatedInstanceWarmup.of_xml)
          (Xml.child xml_arg0 "EstimatedInstanceWarmup") in
      let stepAdjustments =
        (Option.map ~f:StepAdjustments.of_xml)
          (Xml.child xml_arg0 "StepAdjustments") in
      let metricAggregationType =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "MetricAggregationType") in
      let cooldown =
        (Option.map ~f:Cooldown.of_xml) (Xml.child xml_arg0 "Cooldown") in
      let scalingAdjustment =
        (Option.map ~f:PolicyIncrement.of_xml)
          (Xml.child xml_arg0 "ScalingAdjustment") in
      let minAdjustmentMagnitude =
        (Option.map ~f:MinAdjustmentMagnitude.of_xml)
          (Xml.child xml_arg0 "MinAdjustmentMagnitude") in
      let minAdjustmentStep =
        (Option.map ~f:MinAdjustmentStep.of_xml)
          (Xml.child xml_arg0 "MinAdjustmentStep") in
      let adjustmentType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AdjustmentType") in
      let policyType =
        (Option.map ~f:XmlStringMaxLen64.of_xml)
          (Xml.child xml_arg0 "PolicyType") in
      let policyName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?predictiveScalingConfiguration ?enabled
        ?targetTrackingConfiguration ?estimatedInstanceWarmup
        ?stepAdjustments ?metricAggregationType ?cooldown ?scalingAdjustment
        ?minAdjustmentMagnitude ?minAdjustmentStep ?adjustmentType
        ?policyType ~policyName ~autoScalingGroupName ()
    let of_json json =
      let predictiveScalingConfiguration =
        field_map json "PredictiveScalingConfiguration"
          PredictiveScalingConfiguration.of_json in
      let enabled = field_map json "Enabled" ScalingPolicyEnabled.of_json in
      let targetTrackingConfiguration =
        field_map json "TargetTrackingConfiguration"
          TargetTrackingConfiguration.of_json in
      let estimatedInstanceWarmup =
        field_map json "EstimatedInstanceWarmup"
          EstimatedInstanceWarmup.of_json in
      let stepAdjustments =
        field_map json "StepAdjustments" StepAdjustments.of_json in
      let metricAggregationType =
        field_map json "MetricAggregationType" XmlStringMaxLen32.of_json in
      let cooldown = field_map json "Cooldown" Cooldown.of_json in
      let scalingAdjustment =
        field_map json "ScalingAdjustment" PolicyIncrement.of_json in
      let minAdjustmentMagnitude =
        field_map json "MinAdjustmentMagnitude"
          MinAdjustmentMagnitude.of_json in
      let minAdjustmentStep =
        field_map json "MinAdjustmentStep" MinAdjustmentStep.of_json in
      let adjustmentType =
        field_map json "AdjustmentType" XmlStringMaxLen255.of_json in
      let policyType = field_map json "PolicyType" XmlStringMaxLen64.of_json in
      let policyName =
        field_map_exn json "PolicyName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?predictiveScalingConfiguration ?enabled
        ?targetTrackingConfiguration ?estimatedInstanceWarmup
        ?stepAdjustments ?metricAggregationType ?cooldown ?scalingAdjustment
        ?minAdjustmentMagnitude ?minAdjustmentStep ?adjustmentType
        ?policyType ~policyName ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module PutNotificationConfigurationType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      topicARN: XmlStringMaxLen255.t ;
      notificationTypes: AutoScalingNotificationTypes.t }
    let context_ = "PutNotificationConfigurationType"
    let make ~autoScalingGroupName =
      fun ~topicARN ->
        fun ~notificationTypes ->
          fun () -> { autoScalingGroupName; topicARN; notificationTypes }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("TopicARN", (Some (XmlStringMaxLen255.to_value x.topicARN)));
        ("NotificationTypes",
          (Some (AutoScalingNotificationTypes.to_value x.notificationTypes)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationTypes =
        AutoScalingNotificationTypes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotificationTypes") in
      let topicARN =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TopicARN") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~notificationTypes ~topicARN ~autoScalingGroupName ()
    let of_json json =
      let notificationTypes =
        field_map_exn json "NotificationTypes"
          AutoScalingNotificationTypes.of_json in
      let topicARN = field_map_exn json "TopicARN" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~notificationTypes ~topicARN ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module PutLifecycleHookType =
  struct
    type nonrec t =
      {
      lifecycleHookName: AsciiStringMaxLen255.t ;
      autoScalingGroupName: XmlStringMaxLen255.t ;
      lifecycleTransition: LifecycleTransition.t option ;
      roleARN: XmlStringMaxLen255.t option ;
      notificationTargetARN: NotificationTargetResourceName.t option ;
      notificationMetadata: XmlStringMaxLen1023.t option ;
      heartbeatTimeout: HeartbeatTimeout.t option ;
      defaultResult: LifecycleActionResult.t option }
    let context_ = "PutLifecycleHookType"
    let make ?lifecycleTransition =
      fun ?roleARN ->
        fun ?notificationTargetARN ->
          fun ?notificationMetadata ->
            fun ?heartbeatTimeout ->
              fun ?defaultResult ->
                fun ~lifecycleHookName ->
                  fun ~autoScalingGroupName ->
                    fun () ->
                      {
                        lifecycleTransition;
                        roleARN;
                        notificationTargetARN;
                        notificationMetadata;
                        heartbeatTimeout;
                        defaultResult;
                        lifecycleHookName;
                        autoScalingGroupName
                      }
    let to_value x =
      structure_to_value
        [("LifecycleHookName",
           (Some (AsciiStringMaxLen255.to_value x.lifecycleHookName)));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("LifecycleTransition",
          (Option.map x.lifecycleTransition ~f:LifecycleTransition.to_value));
        ("RoleARN", (Option.map x.roleARN ~f:XmlStringMaxLen255.to_value));
        ("NotificationTargetARN",
          (Option.map x.notificationTargetARN
             ~f:NotificationTargetResourceName.to_value));
        ("NotificationMetadata",
          (Option.map x.notificationMetadata ~f:XmlStringMaxLen1023.to_value));
        ("HeartbeatTimeout",
          (Option.map x.heartbeatTimeout ~f:HeartbeatTimeout.to_value));
        ("DefaultResult",
          (Option.map x.defaultResult ~f:LifecycleActionResult.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultResult =
        (Option.map ~f:LifecycleActionResult.of_xml)
          (Xml.child xml_arg0 "DefaultResult") in
      let heartbeatTimeout =
        (Option.map ~f:HeartbeatTimeout.of_xml)
          (Xml.child xml_arg0 "HeartbeatTimeout") in
      let notificationMetadata =
        (Option.map ~f:XmlStringMaxLen1023.of_xml)
          (Xml.child xml_arg0 "NotificationMetadata") in
      let notificationTargetARN =
        (Option.map ~f:NotificationTargetResourceName.of_xml)
          (Xml.child xml_arg0 "NotificationTargetARN") in
      let roleARN =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "RoleARN") in
      let lifecycleTransition =
        (Option.map ~f:LifecycleTransition.of_xml)
          (Xml.child xml_arg0 "LifecycleTransition") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let lifecycleHookName =
        AsciiStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleHookName") in
      make ?defaultResult ?heartbeatTimeout ?notificationMetadata
        ?notificationTargetARN ?roleARN ?lifecycleTransition
        ~autoScalingGroupName ~lifecycleHookName ()
    let of_json json =
      let defaultResult =
        field_map json "DefaultResult" LifecycleActionResult.of_json in
      let heartbeatTimeout =
        field_map json "HeartbeatTimeout" HeartbeatTimeout.of_json in
      let notificationMetadata =
        field_map json "NotificationMetadata" XmlStringMaxLen1023.of_json in
      let notificationTargetARN =
        field_map json "NotificationTargetARN"
          NotificationTargetResourceName.of_json in
      let roleARN = field_map json "RoleARN" XmlStringMaxLen255.of_json in
      let lifecycleTransition =
        field_map json "LifecycleTransition" LifecycleTransition.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let lifecycleHookName =
        field_map_exn json "LifecycleHookName" AsciiStringMaxLen255.of_json in
      make ?defaultResult ?heartbeatTimeout ?notificationMetadata
        ?notificationTargetARN ?roleARN ?lifecycleTransition
        ~autoScalingGroupName ~lifecycleHookName ()
    let to_json v = composed_to_json to_value v
  end
module PutLifecycleHookAnswer =
  struct
    type putLifecycleHookResult = unit
    and responseMetaData = unit
    and t =
      {
      putLifecycleHookResult: putLifecycleHookResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { putLifecycleHookResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ProcessesType =
  struct
    type describeScalingProcessTypesResult = {
      processes: Processes.t option }
    and responseMetaData = unit
    and t =
      {
      describeScalingProcessTypesResult: describeScalingProcessTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ProcessesType"
    let make ?processes =
      fun () ->
        {
          describeScalingProcessTypesResult = { processes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeScalingProcessTypesResult in
      structure_to_wrapped_value
        [("Processes", (Option.map x.processes ~f:Processes.to_value))]
        ~wrapper:"DescribeScalingProcessTypesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeScalingProcessTypesResult" in
      let processes =
        (Option.map ~f:Processes.of_xml) (Xml.child xml_arg0 "Processes") in
      make ?processes ()
    let of_json json =
      let processes = field_map json "Processes" Processes.of_json in
      make ?processes ()
    let to_json v = composed_to_json to_value v
  end
module PolicyARNType =
  struct
    type putScalingPolicyResult =
      {
      policyARN: ResourceName.t option ;
      alarms: Alarms.t option }
    and responseMetaData = unit
    and t =
      {
      putScalingPolicyResult: putScalingPolicyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `ServiceLinkedRoleFailure of ServiceLinkedRoleFailure.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "PolicyARNType"
    let make ?policyARN =
      fun ?alarms ->
        fun () ->
          {
            putScalingPolicyResult = { policyARN; alarms };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `ServiceLinkedRoleFailure e ->
          `Assoc
            [("error", (`String "ServiceLinkedRoleFailure"));
            ("details", (ServiceLinkedRoleFailure.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.putScalingPolicyResult in
      structure_to_wrapped_value
        [("PolicyARN", (Option.map x.policyARN ~f:ResourceName.to_value));
        ("Alarms", (Option.map x.alarms ~f:Alarms.to_value))]
        ~wrapper:"PutScalingPolicyResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "PutScalingPolicyResult" in
      let alarms =
        (Option.map ~f:Alarms.of_xml) (Xml.child xml_arg0 "Alarms") in
      let policyARN =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "PolicyARN") in
      make ?alarms ?policyARN ()
    let of_json json =
      let alarms = field_map json "Alarms" Alarms.of_json in
      let policyARN = field_map json "PolicyARN" ResourceName.of_json in
      make ?alarms ?policyARN ()
    let to_json v = composed_to_json to_value v
  end
module PoliciesType =
  struct
    type describePoliciesResult =
      {
      scalingPolicies: ScalingPolicies.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describePoliciesResult: describePoliciesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `ServiceLinkedRoleFailure of ServiceLinkedRoleFailure.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "PoliciesType"
    let make ?scalingPolicies =
      fun ?nextToken ->
        fun () ->
          {
            describePoliciesResult = { scalingPolicies; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `ServiceLinkedRoleFailure e ->
          `Assoc
            [("error", (`String "ServiceLinkedRoleFailure"));
            ("details", (ServiceLinkedRoleFailure.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describePoliciesResult in
      structure_to_wrapped_value
        [("ScalingPolicies",
           (Option.map x.scalingPolicies ~f:ScalingPolicies.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribePoliciesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribePoliciesResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let scalingPolicies =
        (Option.map ~f:ScalingPolicies.of_xml)
          (Xml.child xml_arg0 "ScalingPolicies") in
      make ?nextToken ?scalingPolicies ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let scalingPolicies =
        field_map json "ScalingPolicies" ScalingPolicies.of_json in
      make ?nextToken ?scalingPolicies ()
    let to_json v = composed_to_json to_value v
  end
module LaunchConfigurationsType =
  struct
    type describeLaunchConfigurationsResult =
      {
      launchConfigurations: LaunchConfigurations.t ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeLaunchConfigurationsResult: describeLaunchConfigurationsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "LaunchConfigurationsType"
    let make ?nextToken =
      fun ~launchConfigurations ->
        fun () ->
          {
            describeLaunchConfigurationsResult =
              { nextToken; launchConfigurations };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLaunchConfigurationsResult in
      structure_to_wrapped_value
        [("LaunchConfigurations",
           (Some (LaunchConfigurations.to_value x.launchConfigurations)));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeLaunchConfigurationsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeLaunchConfigurationsResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let launchConfigurations =
        LaunchConfigurations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchConfigurations") in
      make ?nextToken ~launchConfigurations ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let launchConfigurations =
        field_map_exn json "LaunchConfigurations"
          LaunchConfigurations.of_json in
      make ?nextToken ~launchConfigurations ()
    let to_json v = composed_to_json to_value v
  end
module LaunchConfigurationNamesType =
  struct
    type nonrec t =
      {
      launchConfigurationNames: LaunchConfigurationNames.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let make ?launchConfigurationNames =
      fun ?nextToken ->
        fun ?maxRecords ->
          fun () -> { launchConfigurationNames; nextToken; maxRecords }
    let to_value x =
      structure_to_value
        [("LaunchConfigurationNames",
           (Option.map x.launchConfigurationNames
              ~f:LaunchConfigurationNames.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let launchConfigurationNames =
        (Option.map ~f:LaunchConfigurationNames.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationNames") in
      make ?maxRecords ?nextToken ?launchConfigurationNames ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let launchConfigurationNames =
        field_map json "LaunchConfigurationNames"
          LaunchConfigurationNames.of_json in
      make ?maxRecords ?nextToken ?launchConfigurationNames ()
    let to_json v = composed_to_json to_value v
  end
module LaunchConfigurationNameType =
  struct
    type nonrec t = {
      launchConfigurationName: XmlStringMaxLen255.t }
    let context_ = "LaunchConfigurationNameType"
    let make ~launchConfigurationName = fun () -> { launchConfigurationName }
    let to_value x =
      structure_to_value
        [("LaunchConfigurationName",
           (Some (XmlStringMaxLen255.to_value x.launchConfigurationName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchConfigurationName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchConfigurationName") in
      make ~launchConfigurationName ()
    let of_json json =
      let launchConfigurationName =
        field_map_exn json "LaunchConfigurationName"
          XmlStringMaxLen255.of_json in
      make ~launchConfigurationName ()
    let to_json v = composed_to_json to_value v
  end
module GetPredictiveScalingForecastType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      policyName: XmlStringMaxLen255.t ;
      startTime: TimestampType.t ;
      endTime: TimestampType.t }
    let context_ = "GetPredictiveScalingForecastType"
    let make ~autoScalingGroupName =
      fun ~policyName ->
        fun ~startTime ->
          fun ~endTime ->
            fun () ->
              { autoScalingGroupName; policyName; startTime; endTime }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("PolicyName", (Some (XmlStringMaxLen255.to_value x.policyName)));
        ("StartTime", (Some (TimestampType.to_value x.startTime)));
        ("EndTime", (Some (TimestampType.to_value x.endTime)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endTime =
        TimestampType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        TimestampType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      let policyName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~endTime ~startTime ~policyName ~autoScalingGroupName ()
    let of_json json =
      let endTime = field_map_exn json "EndTime" TimestampType.of_json in
      let startTime = field_map_exn json "StartTime" TimestampType.of_json in
      let policyName =
        field_map_exn json "PolicyName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~endTime ~startTime ~policyName ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module GetPredictiveScalingForecastAnswer =
  struct
    type getPredictiveScalingForecastResult =
      {
      loadForecast: LoadForecasts.t ;
      capacityForecast: CapacityForecast.t ;
      updateTime: TimestampType.t }
    and responseMetaData = unit
    and t =
      {
      getPredictiveScalingForecastResult: getPredictiveScalingForecastResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetPredictiveScalingForecastAnswer"
    let make ~loadForecast =
      fun ~capacityForecast ->
        fun ~updateTime ->
          fun () ->
            {
              getPredictiveScalingForecastResult =
                { loadForecast; capacityForecast; updateTime };
              responseMetaData = ()
            }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.getPredictiveScalingForecastResult in
      structure_to_wrapped_value
        [("LoadForecast", (Some (LoadForecasts.to_value x.loadForecast)));
        ("CapacityForecast",
          (Some (CapacityForecast.to_value x.capacityForecast)));
        ("UpdateTime", (Some (TimestampType.to_value x.updateTime)))]
        ~wrapper:"GetPredictiveScalingForecastResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "GetPredictiveScalingForecastResult" in
      let updateTime =
        TimestampType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "UpdateTime") in
      let capacityForecast =
        CapacityForecast.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityForecast") in
      let loadForecast =
        LoadForecasts.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadForecast") in
      make ~updateTime ~capacityForecast ~loadForecast ()
    let of_json json =
      let updateTime = field_map_exn json "UpdateTime" TimestampType.of_json in
      let capacityForecast =
        field_map_exn json "CapacityForecast" CapacityForecast.of_json in
      let loadForecast =
        field_map_exn json "LoadForecast" LoadForecasts.of_json in
      make ~updateTime ~capacityForecast ~loadForecast ()
    let to_json v = composed_to_json to_value v
  end
module ExitStandbyQuery =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIds.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t }
    let context_ = "ExitStandbyQuery"
    let make ?instanceIds =
      fun ~autoScalingGroupName ->
        fun () -> { instanceIds; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("InstanceIds", (Option.map x.instanceIds ~f:InstanceIds.to_value));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let instanceIds =
        (Option.map ~f:InstanceIds.of_xml) (Xml.child xml_arg0 "InstanceIds") in
      make ~autoScalingGroupName ?instanceIds ()
    let of_json json =
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceIds = field_map json "InstanceIds" InstanceIds.of_json in
      make ~autoScalingGroupName ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end
module ExitStandbyAnswer =
  struct
    type exitStandbyResult = {
      activities: Activities.t option }
    and responseMetaData = unit
    and t =
      {
      exitStandbyResult: exitStandbyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ExitStandbyAnswer"
    let make ?activities =
      fun () -> { exitStandbyResult = { activities }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.exitStandbyResult in
      structure_to_wrapped_value
        [("Activities", (Option.map x.activities ~f:Activities.to_value))]
        ~wrapper:"ExitStandbyResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "ExitStandbyResult" in
      let activities =
        (Option.map ~f:Activities.of_xml) (Xml.child xml_arg0 "Activities") in
      make ?activities ()
    let of_json json =
      let activities = field_map json "Activities" Activities.of_json in
      make ?activities ()
    let to_json v = composed_to_json to_value v
  end
module ExecutePolicyType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      policyName: ResourceName.t ;
      honorCooldown: HonorCooldown.t option ;
      metricValue: MetricScale.t option ;
      breachThreshold: MetricScale.t option }
    let context_ = "ExecutePolicyType"
    let make ?autoScalingGroupName =
      fun ?honorCooldown ->
        fun ?metricValue ->
          fun ?breachThreshold ->
            fun ~policyName ->
              fun () ->
                {
                  autoScalingGroupName;
                  honorCooldown;
                  metricValue;
                  breachThreshold;
                  policyName
                }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("PolicyName", (Some (ResourceName.to_value x.policyName)));
        ("HonorCooldown",
          (Option.map x.honorCooldown ~f:HonorCooldown.to_value));
        ("MetricValue", (Option.map x.metricValue ~f:MetricScale.to_value));
        ("BreachThreshold",
          (Option.map x.breachThreshold ~f:MetricScale.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let breachThreshold =
        (Option.map ~f:MetricScale.of_xml)
          (Xml.child xml_arg0 "BreachThreshold") in
      let metricValue =
        (Option.map ~f:MetricScale.of_xml) (Xml.child xml_arg0 "MetricValue") in
      let honorCooldown =
        (Option.map ~f:HonorCooldown.of_xml)
          (Xml.child xml_arg0 "HonorCooldown") in
      let policyName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ?breachThreshold ?metricValue ?honorCooldown ~policyName
        ?autoScalingGroupName ()
    let of_json json =
      let breachThreshold =
        field_map json "BreachThreshold" MetricScale.of_json in
      let metricValue = field_map json "MetricValue" MetricScale.of_json in
      let honorCooldown =
        field_map json "HonorCooldown" HonorCooldown.of_json in
      let policyName = field_map_exn json "PolicyName" ResourceName.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?breachThreshold ?metricValue ?honorCooldown ~policyName
        ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module EnterStandbyQuery =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIds.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t ;
      shouldDecrementDesiredCapacity: ShouldDecrementDesiredCapacity.t }
    let context_ = "EnterStandbyQuery"
    let make ?instanceIds =
      fun ~autoScalingGroupName ->
        fun ~shouldDecrementDesiredCapacity ->
          fun () ->
            {
              instanceIds;
              autoScalingGroupName;
              shouldDecrementDesiredCapacity
            }
    let to_value x =
      structure_to_value
        [("InstanceIds", (Option.map x.instanceIds ~f:InstanceIds.to_value));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ShouldDecrementDesiredCapacity",
          (Some
             (ShouldDecrementDesiredCapacity.to_value
                x.shouldDecrementDesiredCapacity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shouldDecrementDesiredCapacity =
        ShouldDecrementDesiredCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ShouldDecrementDesiredCapacity") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let instanceIds =
        (Option.map ~f:InstanceIds.of_xml) (Xml.child xml_arg0 "InstanceIds") in
      make ~shouldDecrementDesiredCapacity ~autoScalingGroupName ?instanceIds
        ()
    let of_json json =
      let shouldDecrementDesiredCapacity =
        field_map_exn json "ShouldDecrementDesiredCapacity"
          ShouldDecrementDesiredCapacity.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceIds = field_map json "InstanceIds" InstanceIds.of_json in
      make ~shouldDecrementDesiredCapacity ~autoScalingGroupName ?instanceIds
        ()
    let to_json v = composed_to_json to_value v
  end
module EnterStandbyAnswer =
  struct
    type enterStandbyResult = {
      activities: Activities.t option }
    and responseMetaData = unit
    and t =
      {
      enterStandbyResult: enterStandbyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "EnterStandbyAnswer"
    let make ?activities =
      fun () ->
        { enterStandbyResult = { activities }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.enterStandbyResult in
      structure_to_wrapped_value
        [("Activities", (Option.map x.activities ~f:Activities.to_value))]
        ~wrapper:"EnterStandbyResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "EnterStandbyResult" in
      let activities =
        (Option.map ~f:Activities.of_xml) (Xml.child xml_arg0 "Activities") in
      make ?activities ()
    let of_json json =
      let activities = field_map json "Activities" Activities.of_json in
      make ?activities ()
    let to_json v = composed_to_json to_value v
  end
module EnableMetricsCollectionQuery =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      metrics: Metrics.t option ;
      granularity: XmlStringMaxLen255.t }
    let context_ = "EnableMetricsCollectionQuery"
    let make ?metrics =
      fun ~autoScalingGroupName ->
        fun ~granularity ->
          fun () -> { metrics; autoScalingGroupName; granularity }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("Metrics", (Option.map x.metrics ~f:Metrics.to_value));
        ("Granularity", (Some (XmlStringMaxLen255.to_value x.granularity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let granularity =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Granularity") in
      let metrics =
        (Option.map ~f:Metrics.of_xml) (Xml.child xml_arg0 "Metrics") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~granularity ?metrics ~autoScalingGroupName ()
    let of_json json =
      let granularity =
        field_map_exn json "Granularity" XmlStringMaxLen255.of_json in
      let metrics = field_map json "Metrics" Metrics.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~granularity ?metrics ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DisableMetricsCollectionQuery =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      metrics: Metrics.t option }
    let context_ = "DisableMetricsCollectionQuery"
    let make ?metrics =
      fun ~autoScalingGroupName ->
        fun () -> { metrics; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("Metrics", (Option.map x.metrics ~f:Metrics.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metrics =
        (Option.map ~f:Metrics.of_xml) (Xml.child xml_arg0 "Metrics") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?metrics ~autoScalingGroupName ()
    let of_json json =
      let metrics = field_map json "Metrics" Metrics.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?metrics ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DetachLoadBalancersType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      loadBalancerNames: LoadBalancerNames.t }
    let context_ = "DetachLoadBalancersType"
    let make ~autoScalingGroupName =
      fun ~loadBalancerNames ->
        fun () -> { autoScalingGroupName; loadBalancerNames }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("LoadBalancerNames",
          (Some (LoadBalancerNames.to_value x.loadBalancerNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerNames =
        LoadBalancerNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerNames") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~loadBalancerNames ~autoScalingGroupName ()
    let of_json json =
      let loadBalancerNames =
        field_map_exn json "LoadBalancerNames" LoadBalancerNames.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~loadBalancerNames ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DetachLoadBalancersResultType =
  struct
    type detachLoadBalancersResult = unit
    and responseMetaData = unit
    and t =
      {
      detachLoadBalancersResult: detachLoadBalancersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { detachLoadBalancersResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DetachLoadBalancerTargetGroupsType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      targetGroupARNs: TargetGroupARNs.t }
    let context_ = "DetachLoadBalancerTargetGroupsType"
    let make ~autoScalingGroupName =
      fun ~targetGroupARNs ->
        fun () -> { autoScalingGroupName; targetGroupARNs }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("TargetGroupARNs",
          (Some (TargetGroupARNs.to_value x.targetGroupARNs)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetGroupARNs =
        TargetGroupARNs.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetGroupARNs") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~targetGroupARNs ~autoScalingGroupName ()
    let of_json json =
      let targetGroupARNs =
        field_map_exn json "TargetGroupARNs" TargetGroupARNs.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~targetGroupARNs ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DetachLoadBalancerTargetGroupsResultType =
  struct
    type detachLoadBalancerTargetGroupsResult = unit
    and responseMetaData = unit
    and t =
      {
      detachLoadBalancerTargetGroupsResult:
        detachLoadBalancerTargetGroupsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { detachLoadBalancerTargetGroupsResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DetachInstancesQuery =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIds.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t ;
      shouldDecrementDesiredCapacity: ShouldDecrementDesiredCapacity.t }
    let context_ = "DetachInstancesQuery"
    let make ?instanceIds =
      fun ~autoScalingGroupName ->
        fun ~shouldDecrementDesiredCapacity ->
          fun () ->
            {
              instanceIds;
              autoScalingGroupName;
              shouldDecrementDesiredCapacity
            }
    let to_value x =
      structure_to_value
        [("InstanceIds", (Option.map x.instanceIds ~f:InstanceIds.to_value));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ShouldDecrementDesiredCapacity",
          (Some
             (ShouldDecrementDesiredCapacity.to_value
                x.shouldDecrementDesiredCapacity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shouldDecrementDesiredCapacity =
        ShouldDecrementDesiredCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ShouldDecrementDesiredCapacity") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let instanceIds =
        (Option.map ~f:InstanceIds.of_xml) (Xml.child xml_arg0 "InstanceIds") in
      make ~shouldDecrementDesiredCapacity ~autoScalingGroupName ?instanceIds
        ()
    let of_json json =
      let shouldDecrementDesiredCapacity =
        field_map_exn json "ShouldDecrementDesiredCapacity"
          ShouldDecrementDesiredCapacity.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceIds = field_map json "InstanceIds" InstanceIds.of_json in
      make ~shouldDecrementDesiredCapacity ~autoScalingGroupName ?instanceIds
        ()
    let to_json v = composed_to_json to_value v
  end
module DetachInstancesAnswer =
  struct
    type detachInstancesResult = {
      activities: Activities.t option }
    and responseMetaData = unit
    and t =
      {
      detachInstancesResult: detachInstancesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DetachInstancesAnswer"
    let make ?activities =
      fun () ->
        { detachInstancesResult = { activities }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.detachInstancesResult in
      structure_to_wrapped_value
        [("Activities", (Option.map x.activities ~f:Activities.to_value))]
        ~wrapper:"DetachInstancesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DetachInstancesResult" in
      let activities =
        (Option.map ~f:Activities.of_xml) (Xml.child xml_arg0 "Activities") in
      make ?activities ()
    let of_json json =
      let activities = field_map json "Activities" Activities.of_json in
      make ?activities ()
    let to_json v = composed_to_json to_value v
  end
module DescribeWarmPoolType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      maxRecords: MaxRecords.t option ;
      nextToken: XmlString.t option }
    let context_ = "DescribeWarmPoolType"
    let make ?maxRecords =
      fun ?nextToken ->
        fun ~autoScalingGroupName ->
          fun () -> { maxRecords; nextToken; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?nextToken ?maxRecords ~autoScalingGroupName ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?nextToken ?maxRecords ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeWarmPoolAnswer =
  struct
    type describeWarmPoolResult =
      {
      warmPoolConfiguration: WarmPoolConfiguration.t option ;
      instances: Instances.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeWarmPoolResult: describeWarmPoolResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeWarmPoolAnswer"
    let make ?warmPoolConfiguration =
      fun ?instances ->
        fun ?nextToken ->
          fun () ->
            {
              describeWarmPoolResult =
                { warmPoolConfiguration; instances; nextToken };
              responseMetaData = ()
            }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeWarmPoolResult in
      structure_to_wrapped_value
        [("WarmPoolConfiguration",
           (Option.map x.warmPoolConfiguration
              ~f:WarmPoolConfiguration.to_value));
        ("Instances", (Option.map x.instances ~f:Instances.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeWarmPoolResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeWarmPoolResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let instances =
        (Option.map ~f:Instances.of_xml) (Xml.child xml_arg0 "Instances") in
      let warmPoolConfiguration =
        (Option.map ~f:WarmPoolConfiguration.of_xml)
          (Xml.child xml_arg0 "WarmPoolConfiguration") in
      make ?nextToken ?instances ?warmPoolConfiguration ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let instances = field_map json "Instances" Instances.of_json in
      let warmPoolConfiguration =
        field_map json "WarmPoolConfiguration" WarmPoolConfiguration.of_json in
      make ?nextToken ?instances ?warmPoolConfiguration ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTerminationPolicyTypesAnswer =
  struct
    type describeTerminationPolicyTypesResult =
      {
      terminationPolicyTypes: TerminationPolicies.t option }
    and responseMetaData = unit
    and t =
      {
      describeTerminationPolicyTypesResult:
        describeTerminationPolicyTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeTerminationPolicyTypesAnswer"
    let make ?terminationPolicyTypes =
      fun () ->
        {
          describeTerminationPolicyTypesResult = { terminationPolicyTypes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeTerminationPolicyTypesResult in
      structure_to_wrapped_value
        [("TerminationPolicyTypes",
           (Option.map x.terminationPolicyTypes
              ~f:TerminationPolicies.to_value))]
        ~wrapper:"DescribeTerminationPolicyTypesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeTerminationPolicyTypesResult" in
      let terminationPolicyTypes =
        (Option.map ~f:TerminationPolicies.of_xml)
          (Xml.child xml_arg0 "TerminationPolicyTypes") in
      make ?terminationPolicyTypes ()
    let of_json json =
      let terminationPolicyTypes =
        field_map json "TerminationPolicyTypes" TerminationPolicies.of_json in
      make ?terminationPolicyTypes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTagsType =
  struct
    type nonrec t =
      {
      filters: Filters.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let make ?filters =
      fun ?nextToken ->
        fun ?maxRecords -> fun () -> { filters; nextToken; maxRecords }
    let to_value x =
      structure_to_value
        [("Filters", (Option.map x.filters ~f:Filters.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:Filters.of_xml) (Xml.child xml_arg0 "Filters") in
      make ?maxRecords ?nextToken ?filters ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let filters = field_map json "Filters" Filters.of_json in
      make ?maxRecords ?nextToken ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeScheduledActionsType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      scheduledActionNames: ScheduledActionNames.t option ;
      startTime: TimestampType.t option ;
      endTime: TimestampType.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let make ?autoScalingGroupName =
      fun ?scheduledActionNames ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?nextToken ->
              fun ?maxRecords ->
                fun () ->
                  {
                    autoScalingGroupName;
                    scheduledActionNames;
                    startTime;
                    endTime;
                    nextToken;
                    maxRecords
                  }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("ScheduledActionNames",
          (Option.map x.scheduledActionNames ~f:ScheduledActionNames.to_value));
        ("StartTime", (Option.map x.startTime ~f:TimestampType.to_value));
        ("EndTime", (Option.map x.endTime ~f:TimestampType.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let endTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:TimestampType.of_xml) (Xml.child xml_arg0 "StartTime") in
      let scheduledActionNames =
        (Option.map ~f:ScheduledActionNames.of_xml)
          (Xml.child xml_arg0 "ScheduledActionNames") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ?maxRecords ?nextToken ?endTime ?startTime ?scheduledActionNames
        ?autoScalingGroupName ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let endTime = field_map json "EndTime" TimestampType.of_json in
      let startTime = field_map json "StartTime" TimestampType.of_json in
      let scheduledActionNames =
        field_map json "ScheduledActionNames" ScheduledActionNames.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?maxRecords ?nextToken ?endTime ?startTime ?scheduledActionNames
        ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeScalingActivitiesType =
  struct
    type nonrec t =
      {
      activityIds: ActivityIds.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      includeDeletedGroups: IncludeDeletedGroups.t option ;
      maxRecords: MaxRecords.t option ;
      nextToken: XmlString.t option }
    let make ?activityIds =
      fun ?autoScalingGroupName ->
        fun ?includeDeletedGroups ->
          fun ?maxRecords ->
            fun ?nextToken ->
              fun () ->
                {
                  activityIds;
                  autoScalingGroupName;
                  includeDeletedGroups;
                  maxRecords;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("ActivityIds", (Option.map x.activityIds ~f:ActivityIds.to_value));
        ("AutoScalingGroupName",
          (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("IncludeDeletedGroups",
          (Option.map x.includeDeletedGroups ~f:IncludeDeletedGroups.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let includeDeletedGroups =
        (Option.map ~f:IncludeDeletedGroups.of_xml)
          (Xml.child xml_arg0 "IncludeDeletedGroups") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      let activityIds =
        (Option.map ~f:ActivityIds.of_xml) (Xml.child xml_arg0 "ActivityIds") in
      make ?nextToken ?maxRecords ?includeDeletedGroups ?autoScalingGroupName
        ?activityIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let includeDeletedGroups =
        field_map json "IncludeDeletedGroups" IncludeDeletedGroups.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let activityIds = field_map json "ActivityIds" ActivityIds.of_json in
      make ?nextToken ?maxRecords ?includeDeletedGroups ?autoScalingGroupName
        ?activityIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribePoliciesType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      policyNames: PolicyNames.t option ;
      policyTypes: PolicyTypes.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let make ?autoScalingGroupName =
      fun ?policyNames ->
        fun ?policyTypes ->
          fun ?nextToken ->
            fun ?maxRecords ->
              fun () ->
                {
                  autoScalingGroupName;
                  policyNames;
                  policyTypes;
                  nextToken;
                  maxRecords
                }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("PolicyNames", (Option.map x.policyNames ~f:PolicyNames.to_value));
        ("PolicyTypes", (Option.map x.policyTypes ~f:PolicyTypes.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let policyTypes =
        (Option.map ~f:PolicyTypes.of_xml) (Xml.child xml_arg0 "PolicyTypes") in
      let policyNames =
        (Option.map ~f:PolicyNames.of_xml) (Xml.child xml_arg0 "PolicyNames") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ?maxRecords ?nextToken ?policyTypes ?policyNames
        ?autoScalingGroupName ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let policyTypes = field_map json "PolicyTypes" PolicyTypes.of_json in
      let policyNames = field_map json "PolicyNames" PolicyNames.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?maxRecords ?nextToken ?policyTypes ?policyNames
        ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNotificationConfigurationsType =
  struct
    type nonrec t =
      {
      autoScalingGroupNames: AutoScalingGroupNames.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let make ?autoScalingGroupNames =
      fun ?nextToken ->
        fun ?maxRecords ->
          fun () -> { autoScalingGroupNames; nextToken; maxRecords }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupNames",
           (Option.map x.autoScalingGroupNames
              ~f:AutoScalingGroupNames.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let autoScalingGroupNames =
        (Option.map ~f:AutoScalingGroupNames.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupNames") in
      make ?maxRecords ?nextToken ?autoScalingGroupNames ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let autoScalingGroupNames =
        field_map json "AutoScalingGroupNames" AutoScalingGroupNames.of_json in
      make ?maxRecords ?nextToken ?autoScalingGroupNames ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNotificationConfigurationsAnswer =
  struct
    type describeNotificationConfigurationsResult =
      {
      notificationConfigurations: NotificationConfigurations.t ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeNotificationConfigurationsResult:
        describeNotificationConfigurationsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeNotificationConfigurationsAnswer"
    let make ?nextToken =
      fun ~notificationConfigurations ->
        fun () ->
          {
            describeNotificationConfigurationsResult =
              { nextToken; notificationConfigurations };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeNotificationConfigurationsResult in
      structure_to_wrapped_value
        [("NotificationConfigurations",
           (Some
              (NotificationConfigurations.to_value
                 x.notificationConfigurations)));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeNotificationConfigurationsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeNotificationConfigurationsResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let notificationConfigurations =
        NotificationConfigurations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NotificationConfigurations") in
      make ?nextToken ~notificationConfigurations ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let notificationConfigurations =
        field_map_exn json "NotificationConfigurations"
          NotificationConfigurations.of_json in
      make ?nextToken ~notificationConfigurations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeMetricCollectionTypesAnswer =
  struct
    type describeMetricCollectionTypesResult =
      {
      metrics: MetricCollectionTypes.t option ;
      granularities: MetricGranularityTypes.t option }
    and responseMetaData = unit
    and t =
      {
      describeMetricCollectionTypesResult:
        describeMetricCollectionTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeMetricCollectionTypesAnswer"
    let make ?metrics =
      fun ?granularities ->
        fun () ->
          {
            describeMetricCollectionTypesResult = { metrics; granularities };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeMetricCollectionTypesResult in
      structure_to_wrapped_value
        [("Metrics",
           (Option.map x.metrics ~f:MetricCollectionTypes.to_value));
        ("Granularities",
          (Option.map x.granularities ~f:MetricGranularityTypes.to_value))]
        ~wrapper:"DescribeMetricCollectionTypesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeMetricCollectionTypesResult" in
      let granularities =
        (Option.map ~f:MetricGranularityTypes.of_xml)
          (Xml.child xml_arg0 "Granularities") in
      let metrics =
        (Option.map ~f:MetricCollectionTypes.of_xml)
          (Xml.child xml_arg0 "Metrics") in
      make ?granularities ?metrics ()
    let of_json json =
      let granularities =
        field_map json "Granularities" MetricGranularityTypes.of_json in
      let metrics = field_map json "Metrics" MetricCollectionTypes.of_json in
      make ?granularities ?metrics ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLoadBalancersResponse =
  struct
    type describeLoadBalancersResult =
      {
      loadBalancers: LoadBalancerStates.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeLoadBalancersResult: describeLoadBalancersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLoadBalancersResponse"
    let make ?loadBalancers =
      fun ?nextToken ->
        fun () ->
          {
            describeLoadBalancersResult = { loadBalancers; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLoadBalancersResult in
      structure_to_wrapped_value
        [("LoadBalancers",
           (Option.map x.loadBalancers ~f:LoadBalancerStates.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeLoadBalancersResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeLoadBalancersResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let loadBalancers =
        (Option.map ~f:LoadBalancerStates.of_xml)
          (Xml.child xml_arg0 "LoadBalancers") in
      make ?nextToken ?loadBalancers ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let loadBalancers =
        field_map json "LoadBalancers" LoadBalancerStates.of_json in
      make ?nextToken ?loadBalancers ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLoadBalancersRequest =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let context_ = "DescribeLoadBalancersRequest"
    let make ?nextToken =
      fun ?maxRecords ->
        fun ~autoScalingGroupName ->
          fun () -> { nextToken; maxRecords; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?maxRecords ?nextToken ~autoScalingGroupName ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?maxRecords ?nextToken ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLoadBalancerTargetGroupsResponse =
  struct
    type describeLoadBalancerTargetGroupsResult =
      {
      loadBalancerTargetGroups: LoadBalancerTargetGroupStates.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeLoadBalancerTargetGroupsResult:
        describeLoadBalancerTargetGroupsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLoadBalancerTargetGroupsResponse"
    let make ?loadBalancerTargetGroups =
      fun ?nextToken ->
        fun () ->
          {
            describeLoadBalancerTargetGroupsResult =
              { loadBalancerTargetGroups; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLoadBalancerTargetGroupsResult in
      structure_to_wrapped_value
        [("LoadBalancerTargetGroups",
           (Option.map x.loadBalancerTargetGroups
              ~f:LoadBalancerTargetGroupStates.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeLoadBalancerTargetGroupsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeLoadBalancerTargetGroupsResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let loadBalancerTargetGroups =
        (Option.map ~f:LoadBalancerTargetGroupStates.of_xml)
          (Xml.child xml_arg0 "LoadBalancerTargetGroups") in
      make ?nextToken ?loadBalancerTargetGroups ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let loadBalancerTargetGroups =
        field_map json "LoadBalancerTargetGroups"
          LoadBalancerTargetGroupStates.of_json in
      make ?nextToken ?loadBalancerTargetGroups ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLoadBalancerTargetGroupsRequest =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let context_ = "DescribeLoadBalancerTargetGroupsRequest"
    let make ?nextToken =
      fun ?maxRecords ->
        fun ~autoScalingGroupName ->
          fun () -> { nextToken; maxRecords; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?maxRecords ?nextToken ~autoScalingGroupName ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?maxRecords ?nextToken ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLifecycleHooksType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      lifecycleHookNames: LifecycleHookNames.t option }
    let context_ = "DescribeLifecycleHooksType"
    let make ?lifecycleHookNames =
      fun ~autoScalingGroupName ->
        fun () -> { lifecycleHookNames; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("LifecycleHookNames",
          (Option.map x.lifecycleHookNames ~f:LifecycleHookNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lifecycleHookNames =
        (Option.map ~f:LifecycleHookNames.of_xml)
          (Xml.child xml_arg0 "LifecycleHookNames") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?lifecycleHookNames ~autoScalingGroupName ()
    let of_json json =
      let lifecycleHookNames =
        field_map json "LifecycleHookNames" LifecycleHookNames.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?lifecycleHookNames ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLifecycleHooksAnswer =
  struct
    type describeLifecycleHooksResult =
      {
      lifecycleHooks: LifecycleHooks.t option }
    and responseMetaData = unit
    and t =
      {
      describeLifecycleHooksResult: describeLifecycleHooksResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLifecycleHooksAnswer"
    let make ?lifecycleHooks =
      fun () ->
        {
          describeLifecycleHooksResult = { lifecycleHooks };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLifecycleHooksResult in
      structure_to_wrapped_value
        [("LifecycleHooks",
           (Option.map x.lifecycleHooks ~f:LifecycleHooks.to_value))]
        ~wrapper:"DescribeLifecycleHooksResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeLifecycleHooksResult" in
      let lifecycleHooks =
        (Option.map ~f:LifecycleHooks.of_xml)
          (Xml.child xml_arg0 "LifecycleHooks") in
      make ?lifecycleHooks ()
    let of_json json =
      let lifecycleHooks =
        field_map json "LifecycleHooks" LifecycleHooks.of_json in
      make ?lifecycleHooks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLifecycleHookTypesAnswer =
  struct
    type describeLifecycleHookTypesResult =
      {
      lifecycleHookTypes: AutoScalingNotificationTypes.t option }
    and responseMetaData = unit
    and t =
      {
      describeLifecycleHookTypesResult: describeLifecycleHookTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLifecycleHookTypesAnswer"
    let make ?lifecycleHookTypes =
      fun () ->
        {
          describeLifecycleHookTypesResult = { lifecycleHookTypes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLifecycleHookTypesResult in
      structure_to_wrapped_value
        [("LifecycleHookTypes",
           (Option.map x.lifecycleHookTypes
              ~f:AutoScalingNotificationTypes.to_value))]
        ~wrapper:"DescribeLifecycleHookTypesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeLifecycleHookTypesResult" in
      let lifecycleHookTypes =
        (Option.map ~f:AutoScalingNotificationTypes.of_xml)
          (Xml.child xml_arg0 "LifecycleHookTypes") in
      make ?lifecycleHookTypes ()
    let of_json json =
      let lifecycleHookTypes =
        field_map json "LifecycleHookTypes"
          AutoScalingNotificationTypes.of_json in
      make ?lifecycleHookTypes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceRefreshesType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      instanceRefreshIds: InstanceRefreshIds.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option }
    let context_ = "DescribeInstanceRefreshesType"
    let make ?instanceRefreshIds =
      fun ?nextToken ->
        fun ?maxRecords ->
          fun ~autoScalingGroupName ->
            fun () ->
              {
                instanceRefreshIds;
                nextToken;
                maxRecords;
                autoScalingGroupName
              }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("InstanceRefreshIds",
          (Option.map x.instanceRefreshIds ~f:InstanceRefreshIds.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let instanceRefreshIds =
        (Option.map ~f:InstanceRefreshIds.of_xml)
          (Xml.child xml_arg0 "InstanceRefreshIds") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?maxRecords ?nextToken ?instanceRefreshIds ~autoScalingGroupName
        ()
    let of_json json =
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let instanceRefreshIds =
        field_map json "InstanceRefreshIds" InstanceRefreshIds.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?maxRecords ?nextToken ?instanceRefreshIds ~autoScalingGroupName
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceRefreshesAnswer =
  struct
    type describeInstanceRefreshesResult =
      {
      instanceRefreshes: InstanceRefreshes.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeInstanceRefreshesResult: describeInstanceRefreshesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeInstanceRefreshesAnswer"
    let make ?instanceRefreshes =
      fun ?nextToken ->
        fun () ->
          {
            describeInstanceRefreshesResult =
              { instanceRefreshes; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeInstanceRefreshesResult in
      structure_to_wrapped_value
        [("InstanceRefreshes",
           (Option.map x.instanceRefreshes ~f:InstanceRefreshes.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeInstanceRefreshesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeInstanceRefreshesResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let instanceRefreshes =
        (Option.map ~f:InstanceRefreshes.of_xml)
          (Xml.child xml_arg0 "InstanceRefreshes") in
      make ?nextToken ?instanceRefreshes ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let instanceRefreshes =
        field_map json "InstanceRefreshes" InstanceRefreshes.of_json in
      make ?nextToken ?instanceRefreshes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAutoScalingNotificationTypesAnswer =
  struct
    type describeAutoScalingNotificationTypesResult =
      {
      autoScalingNotificationTypes: AutoScalingNotificationTypes.t option }
    and responseMetaData = unit
    and t =
      {
      describeAutoScalingNotificationTypesResult:
        describeAutoScalingNotificationTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAutoScalingNotificationTypesAnswer"
    let make ?autoScalingNotificationTypes =
      fun () ->
        {
          describeAutoScalingNotificationTypesResult =
            { autoScalingNotificationTypes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAutoScalingNotificationTypesResult in
      structure_to_wrapped_value
        [("AutoScalingNotificationTypes",
           (Option.map x.autoScalingNotificationTypes
              ~f:AutoScalingNotificationTypes.to_value))]
        ~wrapper:"DescribeAutoScalingNotificationTypesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeAutoScalingNotificationTypesResult" in
      let autoScalingNotificationTypes =
        (Option.map ~f:AutoScalingNotificationTypes.of_xml)
          (Xml.child xml_arg0 "AutoScalingNotificationTypes") in
      make ?autoScalingNotificationTypes ()
    let of_json json =
      let autoScalingNotificationTypes =
        field_map json "AutoScalingNotificationTypes"
          AutoScalingNotificationTypes.of_json in
      make ?autoScalingNotificationTypes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAutoScalingInstancesType =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIds.t option ;
      maxRecords: MaxRecords.t option ;
      nextToken: XmlString.t option }
    let make ?instanceIds =
      fun ?maxRecords ->
        fun ?nextToken -> fun () -> { instanceIds; maxRecords; nextToken }
    let to_value x =
      structure_to_value
        [("InstanceIds", (Option.map x.instanceIds ~f:InstanceIds.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let instanceIds =
        (Option.map ~f:InstanceIds.of_xml) (Xml.child xml_arg0 "InstanceIds") in
      make ?nextToken ?maxRecords ?instanceIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let instanceIds = field_map json "InstanceIds" InstanceIds.of_json in
      make ?nextToken ?maxRecords ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAdjustmentTypesAnswer =
  struct
    type describeAdjustmentTypesResult =
      {
      adjustmentTypes: AdjustmentTypes.t option }
    and responseMetaData = unit
    and t =
      {
      describeAdjustmentTypesResult: describeAdjustmentTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAdjustmentTypesAnswer"
    let make ?adjustmentTypes =
      fun () ->
        {
          describeAdjustmentTypesResult = { adjustmentTypes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAdjustmentTypesResult in
      structure_to_wrapped_value
        [("AdjustmentTypes",
           (Option.map x.adjustmentTypes ~f:AdjustmentTypes.to_value))]
        ~wrapper:"DescribeAdjustmentTypesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeAdjustmentTypesResult" in
      let adjustmentTypes =
        (Option.map ~f:AdjustmentTypes.of_xml)
          (Xml.child xml_arg0 "AdjustmentTypes") in
      make ?adjustmentTypes ()
    let of_json json =
      let adjustmentTypes =
        field_map json "AdjustmentTypes" AdjustmentTypes.of_json in
      make ?adjustmentTypes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAccountLimitsAnswer =
  struct
    type describeAccountLimitsResult =
      {
      maxNumberOfAutoScalingGroups: MaxNumberOfAutoScalingGroups.t option ;
      maxNumberOfLaunchConfigurations:
        MaxNumberOfLaunchConfigurations.t option ;
      numberOfAutoScalingGroups: NumberOfAutoScalingGroups.t option ;
      numberOfLaunchConfigurations: NumberOfLaunchConfigurations.t option }
    and responseMetaData = unit
    and t =
      {
      describeAccountLimitsResult: describeAccountLimitsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAccountLimitsAnswer"
    let make ?maxNumberOfAutoScalingGroups =
      fun ?maxNumberOfLaunchConfigurations ->
        fun ?numberOfAutoScalingGroups ->
          fun ?numberOfLaunchConfigurations ->
            fun () ->
              {
                describeAccountLimitsResult =
                  {
                    maxNumberOfAutoScalingGroups;
                    maxNumberOfLaunchConfigurations;
                    numberOfAutoScalingGroups;
                    numberOfLaunchConfigurations
                  };
                responseMetaData = ()
              }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAccountLimitsResult in
      structure_to_wrapped_value
        [("MaxNumberOfAutoScalingGroups",
           (Option.map x.maxNumberOfAutoScalingGroups
              ~f:MaxNumberOfAutoScalingGroups.to_value));
        ("MaxNumberOfLaunchConfigurations",
          (Option.map x.maxNumberOfLaunchConfigurations
             ~f:MaxNumberOfLaunchConfigurations.to_value));
        ("NumberOfAutoScalingGroups",
          (Option.map x.numberOfAutoScalingGroups
             ~f:NumberOfAutoScalingGroups.to_value));
        ("NumberOfLaunchConfigurations",
          (Option.map x.numberOfLaunchConfigurations
             ~f:NumberOfLaunchConfigurations.to_value))]
        ~wrapper:"DescribeAccountLimitsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeAccountLimitsResult" in
      let numberOfLaunchConfigurations =
        (Option.map ~f:NumberOfLaunchConfigurations.of_xml)
          (Xml.child xml_arg0 "NumberOfLaunchConfigurations") in
      let numberOfAutoScalingGroups =
        (Option.map ~f:NumberOfAutoScalingGroups.of_xml)
          (Xml.child xml_arg0 "NumberOfAutoScalingGroups") in
      let maxNumberOfLaunchConfigurations =
        (Option.map ~f:MaxNumberOfLaunchConfigurations.of_xml)
          (Xml.child xml_arg0 "MaxNumberOfLaunchConfigurations") in
      let maxNumberOfAutoScalingGroups =
        (Option.map ~f:MaxNumberOfAutoScalingGroups.of_xml)
          (Xml.child xml_arg0 "MaxNumberOfAutoScalingGroups") in
      make ?numberOfLaunchConfigurations ?numberOfAutoScalingGroups
        ?maxNumberOfLaunchConfigurations ?maxNumberOfAutoScalingGroups ()
    let of_json json =
      let numberOfLaunchConfigurations =
        field_map json "NumberOfLaunchConfigurations"
          NumberOfLaunchConfigurations.of_json in
      let numberOfAutoScalingGroups =
        field_map json "NumberOfAutoScalingGroups"
          NumberOfAutoScalingGroups.of_json in
      let maxNumberOfLaunchConfigurations =
        field_map json "MaxNumberOfLaunchConfigurations"
          MaxNumberOfLaunchConfigurations.of_json in
      let maxNumberOfAutoScalingGroups =
        field_map json "MaxNumberOfAutoScalingGroups"
          MaxNumberOfAutoScalingGroups.of_json in
      make ?numberOfLaunchConfigurations ?numberOfAutoScalingGroups
        ?maxNumberOfLaunchConfigurations ?maxNumberOfAutoScalingGroups ()
    let to_json v = composed_to_json to_value v
  end
module DeleteWarmPoolType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      forceDelete: ForceDelete.t option }
    let context_ = "DeleteWarmPoolType"
    let make ?forceDelete =
      fun ~autoScalingGroupName ->
        fun () -> { forceDelete; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ForceDelete", (Option.map x.forceDelete ~f:ForceDelete.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let forceDelete =
        (Option.map ~f:ForceDelete.of_xml) (Xml.child xml_arg0 "ForceDelete") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?forceDelete ~autoScalingGroupName ()
    let of_json json =
      let forceDelete = field_map json "ForceDelete" ForceDelete.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?forceDelete ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteWarmPoolAnswer =
  struct
    type deleteWarmPoolResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteWarmPoolResult: deleteWarmPoolResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `ResourceInUseFault of ResourceInUseFault.t 
      | `ScalingActivityInProgressFault of ScalingActivityInProgressFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { deleteWarmPoolResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | "ResourceInUseFault" ->
          `ResourceInUseFault (ResourceInUseFault.of_json json)
      | "ScalingActivityInProgressFault" ->
          `ScalingActivityInProgressFault
            (ScalingActivityInProgressFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | "ResourceInUseFault" ->
          `ResourceInUseFault (ResourceInUseFault.of_xml xml)
      | "ScalingActivityInProgressFault" ->
          `ScalingActivityInProgressFault
            (ScalingActivityInProgressFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `ResourceInUseFault e ->
          `Assoc
            [("error", (`String "ResourceInUseFault"));
            ("details", (ResourceInUseFault.to_json e))]
      | `ScalingActivityInProgressFault e ->
          `Assoc
            [("error", (`String "ScalingActivityInProgressFault"));
            ("details", (ScalingActivityInProgressFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTagsType =
  struct
    type nonrec t = {
      tags: Tags.t }
    let context_ = "DeleteTagsType"
    let make ~tags = fun () -> { tags }
    let to_value x =
      structure_to_value [("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      make ~tags ()
    let of_json json =
      let tags = field_map_exn json "Tags" Tags.of_json in make ~tags ()
    let to_json v = composed_to_json to_value v
  end
module DeleteScheduledActionType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      scheduledActionName: XmlStringMaxLen255.t }
    let context_ = "DeleteScheduledActionType"
    let make ~autoScalingGroupName =
      fun ~scheduledActionName ->
        fun () -> { autoScalingGroupName; scheduledActionName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ScheduledActionName",
          (Some (XmlStringMaxLen255.to_value x.scheduledActionName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledActionName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduledActionName") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~scheduledActionName ~autoScalingGroupName ()
    let of_json json =
      let scheduledActionName =
        field_map_exn json "ScheduledActionName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~scheduledActionName ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DeletePolicyType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t option ;
      policyName: ResourceName.t }
    let context_ = "DeletePolicyType"
    let make ?autoScalingGroupName =
      fun ~policyName -> fun () -> { autoScalingGroupName; policyName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Option.map x.autoScalingGroupName ~f:XmlStringMaxLen255.to_value));
        ("PolicyName", (Some (ResourceName.to_value x.policyName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let autoScalingGroupName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupName") in
      make ~policyName ?autoScalingGroupName ()
    let of_json json =
      let policyName = field_map_exn json "PolicyName" ResourceName.of_json in
      let autoScalingGroupName =
        field_map json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~policyName ?autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNotificationConfigurationType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      topicARN: XmlStringMaxLen255.t }
    let context_ = "DeleteNotificationConfigurationType"
    let make ~autoScalingGroupName =
      fun ~topicARN -> fun () -> { autoScalingGroupName; topicARN }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("TopicARN", (Some (XmlStringMaxLen255.to_value x.topicARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topicARN =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TopicARN") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~topicARN ~autoScalingGroupName ()
    let of_json json =
      let topicARN = field_map_exn json "TopicARN" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~topicARN ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLifecycleHookType =
  struct
    type nonrec t =
      {
      lifecycleHookName: AsciiStringMaxLen255.t ;
      autoScalingGroupName: XmlStringMaxLen255.t }
    let context_ = "DeleteLifecycleHookType"
    let make ~lifecycleHookName =
      fun ~autoScalingGroupName ->
        fun () -> { lifecycleHookName; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("LifecycleHookName",
           (Some (AsciiStringMaxLen255.to_value x.lifecycleHookName)));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let lifecycleHookName =
        AsciiStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleHookName") in
      make ~autoScalingGroupName ~lifecycleHookName ()
    let of_json json =
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let lifecycleHookName =
        field_map_exn json "LifecycleHookName" AsciiStringMaxLen255.of_json in
      make ~autoScalingGroupName ~lifecycleHookName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLifecycleHookAnswer =
  struct
    type deleteLifecycleHookResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteLifecycleHookResult: deleteLifecycleHookResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { deleteLifecycleHookResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAutoScalingGroupType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      forceDelete: ForceDelete.t option }
    let context_ = "DeleteAutoScalingGroupType"
    let make ?forceDelete =
      fun ~autoScalingGroupName ->
        fun () -> { forceDelete; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ForceDelete", (Option.map x.forceDelete ~f:ForceDelete.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let forceDelete =
        (Option.map ~f:ForceDelete.of_xml) (Xml.child xml_arg0 "ForceDelete") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?forceDelete ~autoScalingGroupName ()
    let of_json json =
      let forceDelete = field_map json "ForceDelete" ForceDelete.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?forceDelete ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module CreateOrUpdateTagsType =
  struct
    type nonrec t = {
      tags: Tags.t }
    let context_ = "CreateOrUpdateTagsType"
    let make ~tags = fun () -> { tags }
    let to_value x =
      structure_to_value [("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      make ~tags ()
    let of_json json =
      let tags = field_map_exn json "Tags" Tags.of_json in make ~tags ()
    let to_json v = composed_to_json to_value v
  end
module CreateLaunchConfigurationType =
  struct
    type nonrec t =
      {
      launchConfigurationName: XmlStringMaxLen255.t ;
      imageId: XmlStringMaxLen255.t option ;
      keyName: XmlStringMaxLen255.t option ;
      securityGroups: SecurityGroups.t option ;
      classicLinkVPCId: XmlStringMaxLen255.t option ;
      classicLinkVPCSecurityGroups: ClassicLinkVPCSecurityGroups.t option ;
      userData: XmlStringUserData.t option ;
      instanceId: XmlStringMaxLen19.t option ;
      instanceType: XmlStringMaxLen255.t option ;
      kernelId: XmlStringMaxLen255.t option ;
      ramdiskId: XmlStringMaxLen255.t option ;
      blockDeviceMappings: BlockDeviceMappings.t option ;
      instanceMonitoring: InstanceMonitoring.t option ;
      spotPrice: SpotPrice.t option ;
      iamInstanceProfile: XmlStringMaxLen1600.t option ;
      ebsOptimized: EbsOptimized.t option ;
      associatePublicIpAddress: AssociatePublicIpAddress.t option ;
      placementTenancy: XmlStringMaxLen64.t option ;
      metadataOptions: InstanceMetadataOptions.t option }
    let context_ = "CreateLaunchConfigurationType"
    let make ?imageId =
      fun ?keyName ->
        fun ?securityGroups ->
          fun ?classicLinkVPCId ->
            fun ?classicLinkVPCSecurityGroups ->
              fun ?userData ->
                fun ?instanceId ->
                  fun ?instanceType ->
                    fun ?kernelId ->
                      fun ?ramdiskId ->
                        fun ?blockDeviceMappings ->
                          fun ?instanceMonitoring ->
                            fun ?spotPrice ->
                              fun ?iamInstanceProfile ->
                                fun ?ebsOptimized ->
                                  fun ?associatePublicIpAddress ->
                                    fun ?placementTenancy ->
                                      fun ?metadataOptions ->
                                        fun ~launchConfigurationName ->
                                          fun () ->
                                            {
                                              imageId;
                                              keyName;
                                              securityGroups;
                                              classicLinkVPCId;
                                              classicLinkVPCSecurityGroups;
                                              userData;
                                              instanceId;
                                              instanceType;
                                              kernelId;
                                              ramdiskId;
                                              blockDeviceMappings;
                                              instanceMonitoring;
                                              spotPrice;
                                              iamInstanceProfile;
                                              ebsOptimized;
                                              associatePublicIpAddress;
                                              placementTenancy;
                                              metadataOptions;
                                              launchConfigurationName
                                            }
    let to_value x =
      structure_to_value
        [("LaunchConfigurationName",
           (Some (XmlStringMaxLen255.to_value x.launchConfigurationName)));
        ("ImageId", (Option.map x.imageId ~f:XmlStringMaxLen255.to_value));
        ("KeyName", (Option.map x.keyName ~f:XmlStringMaxLen255.to_value));
        ("SecurityGroups",
          (Option.map x.securityGroups ~f:SecurityGroups.to_value));
        ("ClassicLinkVPCId",
          (Option.map x.classicLinkVPCId ~f:XmlStringMaxLen255.to_value));
        ("ClassicLinkVPCSecurityGroups",
          (Option.map x.classicLinkVPCSecurityGroups
             ~f:ClassicLinkVPCSecurityGroups.to_value));
        ("UserData", (Option.map x.userData ~f:XmlStringUserData.to_value));
        ("InstanceId",
          (Option.map x.instanceId ~f:XmlStringMaxLen19.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:XmlStringMaxLen255.to_value));
        ("KernelId", (Option.map x.kernelId ~f:XmlStringMaxLen255.to_value));
        ("RamdiskId",
          (Option.map x.ramdiskId ~f:XmlStringMaxLen255.to_value));
        ("BlockDeviceMappings",
          (Option.map x.blockDeviceMappings ~f:BlockDeviceMappings.to_value));
        ("InstanceMonitoring",
          (Option.map x.instanceMonitoring ~f:InstanceMonitoring.to_value));
        ("SpotPrice", (Option.map x.spotPrice ~f:SpotPrice.to_value));
        ("IamInstanceProfile",
          (Option.map x.iamInstanceProfile ~f:XmlStringMaxLen1600.to_value));
        ("EbsOptimized",
          (Option.map x.ebsOptimized ~f:EbsOptimized.to_value));
        ("AssociatePublicIpAddress",
          (Option.map x.associatePublicIpAddress
             ~f:AssociatePublicIpAddress.to_value));
        ("PlacementTenancy",
          (Option.map x.placementTenancy ~f:XmlStringMaxLen64.to_value));
        ("MetadataOptions",
          (Option.map x.metadataOptions ~f:InstanceMetadataOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadataOptions =
        (Option.map ~f:InstanceMetadataOptions.of_xml)
          (Xml.child xml_arg0 "MetadataOptions") in
      let placementTenancy =
        (Option.map ~f:XmlStringMaxLen64.of_xml)
          (Xml.child xml_arg0 "PlacementTenancy") in
      let associatePublicIpAddress =
        (Option.map ~f:AssociatePublicIpAddress.of_xml)
          (Xml.child xml_arg0 "AssociatePublicIpAddress") in
      let ebsOptimized =
        (Option.map ~f:EbsOptimized.of_xml)
          (Xml.child xml_arg0 "EbsOptimized") in
      let iamInstanceProfile =
        (Option.map ~f:XmlStringMaxLen1600.of_xml)
          (Xml.child xml_arg0 "IamInstanceProfile") in
      let spotPrice =
        (Option.map ~f:SpotPrice.of_xml) (Xml.child xml_arg0 "SpotPrice") in
      let instanceMonitoring =
        (Option.map ~f:InstanceMonitoring.of_xml)
          (Xml.child xml_arg0 "InstanceMonitoring") in
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappings.of_xml)
          (Xml.child xml_arg0 "BlockDeviceMappings") in
      let ramdiskId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "RamdiskId") in
      let kernelId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "KernelId") in
      let instanceType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let instanceId =
        (Option.map ~f:XmlStringMaxLen19.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let userData =
        (Option.map ~f:XmlStringUserData.of_xml)
          (Xml.child xml_arg0 "UserData") in
      let classicLinkVPCSecurityGroups =
        (Option.map ~f:ClassicLinkVPCSecurityGroups.of_xml)
          (Xml.child xml_arg0 "ClassicLinkVPCSecurityGroups") in
      let classicLinkVPCId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "ClassicLinkVPCId") in
      let securityGroups =
        (Option.map ~f:SecurityGroups.of_xml)
          (Xml.child xml_arg0 "SecurityGroups") in
      let keyName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "KeyName") in
      let imageId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      let launchConfigurationName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchConfigurationName") in
      make ?metadataOptions ?placementTenancy ?associatePublicIpAddress
        ?ebsOptimized ?iamInstanceProfile ?spotPrice ?instanceMonitoring
        ?blockDeviceMappings ?ramdiskId ?kernelId ?instanceType ?instanceId
        ?userData ?classicLinkVPCSecurityGroups ?classicLinkVPCId
        ?securityGroups ?keyName ?imageId ~launchConfigurationName ()
    let of_json json =
      let metadataOptions =
        field_map json "MetadataOptions" InstanceMetadataOptions.of_json in
      let placementTenancy =
        field_map json "PlacementTenancy" XmlStringMaxLen64.of_json in
      let associatePublicIpAddress =
        field_map json "AssociatePublicIpAddress"
          AssociatePublicIpAddress.of_json in
      let ebsOptimized = field_map json "EbsOptimized" EbsOptimized.of_json in
      let iamInstanceProfile =
        field_map json "IamInstanceProfile" XmlStringMaxLen1600.of_json in
      let spotPrice = field_map json "SpotPrice" SpotPrice.of_json in
      let instanceMonitoring =
        field_map json "InstanceMonitoring" InstanceMonitoring.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings" BlockDeviceMappings.of_json in
      let ramdiskId = field_map json "RamdiskId" XmlStringMaxLen255.of_json in
      let kernelId = field_map json "KernelId" XmlStringMaxLen255.of_json in
      let instanceType =
        field_map json "InstanceType" XmlStringMaxLen255.of_json in
      let instanceId = field_map json "InstanceId" XmlStringMaxLen19.of_json in
      let userData = field_map json "UserData" XmlStringUserData.of_json in
      let classicLinkVPCSecurityGroups =
        field_map json "ClassicLinkVPCSecurityGroups"
          ClassicLinkVPCSecurityGroups.of_json in
      let classicLinkVPCId =
        field_map json "ClassicLinkVPCId" XmlStringMaxLen255.of_json in
      let securityGroups =
        field_map json "SecurityGroups" SecurityGroups.of_json in
      let keyName = field_map json "KeyName" XmlStringMaxLen255.of_json in
      let imageId = field_map json "ImageId" XmlStringMaxLen255.of_json in
      let launchConfigurationName =
        field_map_exn json "LaunchConfigurationName"
          XmlStringMaxLen255.of_json in
      make ?metadataOptions ?placementTenancy ?associatePublicIpAddress
        ?ebsOptimized ?iamInstanceProfile ?spotPrice ?instanceMonitoring
        ?blockDeviceMappings ?ramdiskId ?kernelId ?instanceType ?instanceId
        ?userData ?classicLinkVPCSecurityGroups ?classicLinkVPCId
        ?securityGroups ?keyName ?imageId ~launchConfigurationName ()
    let to_json v = composed_to_json to_value v
  end
module CreateAutoScalingGroupType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      launchConfigurationName: XmlStringMaxLen255.t option ;
      launchTemplate: LaunchTemplateSpecification.t option ;
      mixedInstancesPolicy: MixedInstancesPolicy.t option ;
      instanceId: XmlStringMaxLen19.t option ;
      minSize: AutoScalingGroupMinSize.t ;
      maxSize: AutoScalingGroupMaxSize.t ;
      desiredCapacity: AutoScalingGroupDesiredCapacity.t option ;
      defaultCooldown: Cooldown.t option ;
      availabilityZones: AvailabilityZones.t option ;
      loadBalancerNames: LoadBalancerNames.t option ;
      targetGroupARNs: TargetGroupARNs.t option ;
      healthCheckType: XmlStringMaxLen32.t option ;
      healthCheckGracePeriod: HealthCheckGracePeriod.t option ;
      placementGroup: XmlStringMaxLen255.t option ;
      vPCZoneIdentifier: XmlStringMaxLen2047.t option ;
      terminationPolicies: TerminationPolicies.t option ;
      newInstancesProtectedFromScaleIn: InstanceProtected.t option ;
      capacityRebalance: CapacityRebalanceEnabled.t option ;
      lifecycleHookSpecificationList: LifecycleHookSpecifications.t option ;
      tags: Tags.t option ;
      serviceLinkedRoleARN: ResourceName.t option ;
      maxInstanceLifetime: MaxInstanceLifetime.t option ;
      context: Context.t option ;
      desiredCapacityType: XmlStringMaxLen255.t option }
    let context_ = "CreateAutoScalingGroupType"
    let make ?launchConfigurationName =
      fun ?launchTemplate ->
        fun ?mixedInstancesPolicy ->
          fun ?instanceId ->
            fun ?desiredCapacity ->
              fun ?defaultCooldown ->
                fun ?availabilityZones ->
                  fun ?loadBalancerNames ->
                    fun ?targetGroupARNs ->
                      fun ?healthCheckType ->
                        fun ?healthCheckGracePeriod ->
                          fun ?placementGroup ->
                            fun ?vPCZoneIdentifier ->
                              fun ?terminationPolicies ->
                                fun ?newInstancesProtectedFromScaleIn ->
                                  fun ?capacityRebalance ->
                                    fun ?lifecycleHookSpecificationList ->
                                      fun ?tags ->
                                        fun ?serviceLinkedRoleARN ->
                                          fun ?maxInstanceLifetime ->
                                            fun ?context ->
                                              fun ?desiredCapacityType ->
                                                fun ~autoScalingGroupName ->
                                                  fun ~minSize ->
                                                    fun ~maxSize ->
                                                      fun () ->
                                                        {
                                                          launchConfigurationName;
                                                          launchTemplate;
                                                          mixedInstancesPolicy;
                                                          instanceId;
                                                          desiredCapacity;
                                                          defaultCooldown;
                                                          availabilityZones;
                                                          loadBalancerNames;
                                                          targetGroupARNs;
                                                          healthCheckType;
                                                          healthCheckGracePeriod;
                                                          placementGroup;
                                                          vPCZoneIdentifier;
                                                          terminationPolicies;
                                                          newInstancesProtectedFromScaleIn;
                                                          capacityRebalance;
                                                          lifecycleHookSpecificationList;
                                                          tags;
                                                          serviceLinkedRoleARN;
                                                          maxInstanceLifetime;
                                                          context;
                                                          desiredCapacityType;
                                                          autoScalingGroupName;
                                                          minSize;
                                                          maxSize
                                                        }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("LaunchConfigurationName",
          (Option.map x.launchConfigurationName
             ~f:XmlStringMaxLen255.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:LaunchTemplateSpecification.to_value));
        ("MixedInstancesPolicy",
          (Option.map x.mixedInstancesPolicy ~f:MixedInstancesPolicy.to_value));
        ("InstanceId",
          (Option.map x.instanceId ~f:XmlStringMaxLen19.to_value));
        ("MinSize", (Some (AutoScalingGroupMinSize.to_value x.minSize)));
        ("MaxSize", (Some (AutoScalingGroupMaxSize.to_value x.maxSize)));
        ("DesiredCapacity",
          (Option.map x.desiredCapacity
             ~f:AutoScalingGroupDesiredCapacity.to_value));
        ("DefaultCooldown",
          (Option.map x.defaultCooldown ~f:Cooldown.to_value));
        ("AvailabilityZones",
          (Option.map x.availabilityZones ~f:AvailabilityZones.to_value));
        ("LoadBalancerNames",
          (Option.map x.loadBalancerNames ~f:LoadBalancerNames.to_value));
        ("TargetGroupARNs",
          (Option.map x.targetGroupARNs ~f:TargetGroupARNs.to_value));
        ("HealthCheckType",
          (Option.map x.healthCheckType ~f:XmlStringMaxLen32.to_value));
        ("HealthCheckGracePeriod",
          (Option.map x.healthCheckGracePeriod
             ~f:HealthCheckGracePeriod.to_value));
        ("PlacementGroup",
          (Option.map x.placementGroup ~f:XmlStringMaxLen255.to_value));
        ("VPCZoneIdentifier",
          (Option.map x.vPCZoneIdentifier ~f:XmlStringMaxLen2047.to_value));
        ("TerminationPolicies",
          (Option.map x.terminationPolicies ~f:TerminationPolicies.to_value));
        ("NewInstancesProtectedFromScaleIn",
          (Option.map x.newInstancesProtectedFromScaleIn
             ~f:InstanceProtected.to_value));
        ("CapacityRebalance",
          (Option.map x.capacityRebalance
             ~f:CapacityRebalanceEnabled.to_value));
        ("LifecycleHookSpecificationList",
          (Option.map x.lifecycleHookSpecificationList
             ~f:LifecycleHookSpecifications.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("ServiceLinkedRoleARN",
          (Option.map x.serviceLinkedRoleARN ~f:ResourceName.to_value));
        ("MaxInstanceLifetime",
          (Option.map x.maxInstanceLifetime ~f:MaxInstanceLifetime.to_value));
        ("Context", (Option.map x.context ~f:Context.to_value));
        ("DesiredCapacityType",
          (Option.map x.desiredCapacityType ~f:XmlStringMaxLen255.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let desiredCapacityType =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "DesiredCapacityType") in
      let context =
        (Option.map ~f:Context.of_xml) (Xml.child xml_arg0 "Context") in
      let maxInstanceLifetime =
        (Option.map ~f:MaxInstanceLifetime.of_xml)
          (Xml.child xml_arg0 "MaxInstanceLifetime") in
      let serviceLinkedRoleARN =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ServiceLinkedRoleARN") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let lifecycleHookSpecificationList =
        (Option.map ~f:LifecycleHookSpecifications.of_xml)
          (Xml.child xml_arg0 "LifecycleHookSpecificationList") in
      let capacityRebalance =
        (Option.map ~f:CapacityRebalanceEnabled.of_xml)
          (Xml.child xml_arg0 "CapacityRebalance") in
      let newInstancesProtectedFromScaleIn =
        (Option.map ~f:InstanceProtected.of_xml)
          (Xml.child xml_arg0 "NewInstancesProtectedFromScaleIn") in
      let terminationPolicies =
        (Option.map ~f:TerminationPolicies.of_xml)
          (Xml.child xml_arg0 "TerminationPolicies") in
      let vPCZoneIdentifier =
        (Option.map ~f:XmlStringMaxLen2047.of_xml)
          (Xml.child xml_arg0 "VPCZoneIdentifier") in
      let placementGroup =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "PlacementGroup") in
      let healthCheckGracePeriod =
        (Option.map ~f:HealthCheckGracePeriod.of_xml)
          (Xml.child xml_arg0 "HealthCheckGracePeriod") in
      let healthCheckType =
        (Option.map ~f:XmlStringMaxLen32.of_xml)
          (Xml.child xml_arg0 "HealthCheckType") in
      let targetGroupARNs =
        (Option.map ~f:TargetGroupARNs.of_xml)
          (Xml.child xml_arg0 "TargetGroupARNs") in
      let loadBalancerNames =
        (Option.map ~f:LoadBalancerNames.of_xml)
          (Xml.child xml_arg0 "LoadBalancerNames") in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      let defaultCooldown =
        (Option.map ~f:Cooldown.of_xml)
          (Xml.child xml_arg0 "DefaultCooldown") in
      let desiredCapacity =
        (Option.map ~f:AutoScalingGroupDesiredCapacity.of_xml)
          (Xml.child xml_arg0 "DesiredCapacity") in
      let maxSize =
        AutoScalingGroupMaxSize.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxSize") in
      let minSize =
        AutoScalingGroupMinSize.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinSize") in
      let instanceId =
        (Option.map ~f:XmlStringMaxLen19.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let mixedInstancesPolicy =
        (Option.map ~f:MixedInstancesPolicy.of_xml)
          (Xml.child xml_arg0 "MixedInstancesPolicy") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let launchConfigurationName =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "LaunchConfigurationName") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ?desiredCapacityType ?context ?maxInstanceLifetime
        ?serviceLinkedRoleARN ?tags ?lifecycleHookSpecificationList
        ?capacityRebalance ?newInstancesProtectedFromScaleIn
        ?terminationPolicies ?vPCZoneIdentifier ?placementGroup
        ?healthCheckGracePeriod ?healthCheckType ?targetGroupARNs
        ?loadBalancerNames ?availabilityZones ?defaultCooldown
        ?desiredCapacity ~maxSize ~minSize ?instanceId ?mixedInstancesPolicy
        ?launchTemplate ?launchConfigurationName ~autoScalingGroupName ()
    let of_json json =
      let desiredCapacityType =
        field_map json "DesiredCapacityType" XmlStringMaxLen255.of_json in
      let context = field_map json "Context" Context.of_json in
      let maxInstanceLifetime =
        field_map json "MaxInstanceLifetime" MaxInstanceLifetime.of_json in
      let serviceLinkedRoleARN =
        field_map json "ServiceLinkedRoleARN" ResourceName.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let lifecycleHookSpecificationList =
        field_map json "LifecycleHookSpecificationList"
          LifecycleHookSpecifications.of_json in
      let capacityRebalance =
        field_map json "CapacityRebalance" CapacityRebalanceEnabled.of_json in
      let newInstancesProtectedFromScaleIn =
        field_map json "NewInstancesProtectedFromScaleIn"
          InstanceProtected.of_json in
      let terminationPolicies =
        field_map json "TerminationPolicies" TerminationPolicies.of_json in
      let vPCZoneIdentifier =
        field_map json "VPCZoneIdentifier" XmlStringMaxLen2047.of_json in
      let placementGroup =
        field_map json "PlacementGroup" XmlStringMaxLen255.of_json in
      let healthCheckGracePeriod =
        field_map json "HealthCheckGracePeriod"
          HealthCheckGracePeriod.of_json in
      let healthCheckType =
        field_map json "HealthCheckType" XmlStringMaxLen32.of_json in
      let targetGroupARNs =
        field_map json "TargetGroupARNs" TargetGroupARNs.of_json in
      let loadBalancerNames =
        field_map json "LoadBalancerNames" LoadBalancerNames.of_json in
      let availabilityZones =
        field_map json "AvailabilityZones" AvailabilityZones.of_json in
      let defaultCooldown = field_map json "DefaultCooldown" Cooldown.of_json in
      let desiredCapacity =
        field_map json "DesiredCapacity"
          AutoScalingGroupDesiredCapacity.of_json in
      let maxSize =
        field_map_exn json "MaxSize" AutoScalingGroupMaxSize.of_json in
      let minSize =
        field_map_exn json "MinSize" AutoScalingGroupMinSize.of_json in
      let instanceId = field_map json "InstanceId" XmlStringMaxLen19.of_json in
      let mixedInstancesPolicy =
        field_map json "MixedInstancesPolicy" MixedInstancesPolicy.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      let launchConfigurationName =
        field_map json "LaunchConfigurationName" XmlStringMaxLen255.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ?desiredCapacityType ?context ?maxInstanceLifetime
        ?serviceLinkedRoleARN ?tags ?lifecycleHookSpecificationList
        ?capacityRebalance ?newInstancesProtectedFromScaleIn
        ?terminationPolicies ?vPCZoneIdentifier ?placementGroup
        ?healthCheckGracePeriod ?healthCheckType ?targetGroupARNs
        ?loadBalancerNames ?availabilityZones ?defaultCooldown
        ?desiredCapacity ~maxSize ~minSize ?instanceId ?mixedInstancesPolicy
        ?launchTemplate ?launchConfigurationName ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module CompleteLifecycleActionType =
  struct
    type nonrec t =
      {
      lifecycleHookName: AsciiStringMaxLen255.t ;
      autoScalingGroupName: ResourceName.t ;
      lifecycleActionToken: LifecycleActionToken.t option ;
      lifecycleActionResult: LifecycleActionResult.t ;
      instanceId: XmlStringMaxLen19.t option }
    let context_ = "CompleteLifecycleActionType"
    let make ?lifecycleActionToken =
      fun ?instanceId ->
        fun ~lifecycleHookName ->
          fun ~autoScalingGroupName ->
            fun ~lifecycleActionResult ->
              fun () ->
                {
                  lifecycleActionToken;
                  instanceId;
                  lifecycleHookName;
                  autoScalingGroupName;
                  lifecycleActionResult
                }
    let to_value x =
      structure_to_value
        [("LifecycleHookName",
           (Some (AsciiStringMaxLen255.to_value x.lifecycleHookName)));
        ("AutoScalingGroupName",
          (Some (ResourceName.to_value x.autoScalingGroupName)));
        ("LifecycleActionToken",
          (Option.map x.lifecycleActionToken ~f:LifecycleActionToken.to_value));
        ("LifecycleActionResult",
          (Some (LifecycleActionResult.to_value x.lifecycleActionResult)));
        ("InstanceId",
          (Option.map x.instanceId ~f:XmlStringMaxLen19.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        (Option.map ~f:XmlStringMaxLen19.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let lifecycleActionResult =
        LifecycleActionResult.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleActionResult") in
      let lifecycleActionToken =
        (Option.map ~f:LifecycleActionToken.of_xml)
          (Xml.child xml_arg0 "LifecycleActionToken") in
      let autoScalingGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let lifecycleHookName =
        AsciiStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecycleHookName") in
      make ?instanceId ~lifecycleActionResult ?lifecycleActionToken
        ~autoScalingGroupName ~lifecycleHookName ()
    let of_json json =
      let instanceId = field_map json "InstanceId" XmlStringMaxLen19.of_json in
      let lifecycleActionResult =
        field_map_exn json "LifecycleActionResult"
          LifecycleActionResult.of_json in
      let lifecycleActionToken =
        field_map json "LifecycleActionToken" LifecycleActionToken.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" ResourceName.of_json in
      let lifecycleHookName =
        field_map_exn json "LifecycleHookName" AsciiStringMaxLen255.of_json in
      make ?instanceId ~lifecycleActionResult ?lifecycleActionToken
        ~autoScalingGroupName ~lifecycleHookName ()
    let to_json v = composed_to_json to_value v
  end
module CompleteLifecycleActionAnswer =
  struct
    type completeLifecycleActionResult = unit
    and responseMetaData = unit
    and t =
      {
      completeLifecycleActionResult: completeLifecycleActionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { completeLifecycleActionResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module CancelInstanceRefreshType =
  struct
    type nonrec t = {
      autoScalingGroupName: XmlStringMaxLen255.t }
    let context_ = "CancelInstanceRefreshType"
    let make ~autoScalingGroupName = fun () -> { autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~autoScalingGroupName ()
    let of_json json =
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module CancelInstanceRefreshAnswer =
  struct
    type cancelInstanceRefreshResult =
      {
      instanceRefreshId: XmlStringMaxLen255.t option }
    and responseMetaData = unit
    and t =
      {
      cancelInstanceRefreshResult: cancelInstanceRefreshResult ;
      responseMetaData: responseMetaData }
    type error =
      [
        `ActiveInstanceRefreshNotFoundFault of
          ActiveInstanceRefreshNotFoundFault.t 
      | `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CancelInstanceRefreshAnswer"
    let make ?instanceRefreshId =
      fun () ->
        {
          cancelInstanceRefreshResult = { instanceRefreshId };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ActiveInstanceRefreshNotFoundFault" ->
          `ActiveInstanceRefreshNotFoundFault
            (ActiveInstanceRefreshNotFoundFault.of_json json)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ActiveInstanceRefreshNotFoundFault" ->
          `ActiveInstanceRefreshNotFoundFault
            (ActiveInstanceRefreshNotFoundFault.of_xml xml)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ActiveInstanceRefreshNotFoundFault e ->
          `Assoc
            [("error", (`String "ActiveInstanceRefreshNotFoundFault"));
            ("details", (ActiveInstanceRefreshNotFoundFault.to_json e))]
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.cancelInstanceRefreshResult in
      structure_to_wrapped_value
        [("InstanceRefreshId",
           (Option.map x.instanceRefreshId ~f:XmlStringMaxLen255.to_value))]
        ~wrapper:"CancelInstanceRefreshResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "CancelInstanceRefreshResult" in
      let instanceRefreshId =
        (Option.map ~f:XmlStringMaxLen255.of_xml)
          (Xml.child xml_arg0 "InstanceRefreshId") in
      make ?instanceRefreshId ()
    let of_json json =
      let instanceRefreshId =
        field_map json "InstanceRefreshId" XmlStringMaxLen255.of_json in
      make ?instanceRefreshId ()
    let to_json v = composed_to_json to_value v
  end
module BatchPutScheduledUpdateGroupActionType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      scheduledUpdateGroupActions: ScheduledUpdateGroupActionRequests.t }
    let context_ = "BatchPutScheduledUpdateGroupActionType"
    let make ~autoScalingGroupName =
      fun ~scheduledUpdateGroupActions ->
        fun () -> { autoScalingGroupName; scheduledUpdateGroupActions }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ScheduledUpdateGroupActions",
          (Some
             (ScheduledUpdateGroupActionRequests.to_value
                x.scheduledUpdateGroupActions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledUpdateGroupActions =
        ScheduledUpdateGroupActionRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ScheduledUpdateGroupActions") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~scheduledUpdateGroupActions ~autoScalingGroupName ()
    let of_json json =
      let scheduledUpdateGroupActions =
        field_map_exn json "ScheduledUpdateGroupActions"
          ScheduledUpdateGroupActionRequests.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~scheduledUpdateGroupActions ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module BatchPutScheduledUpdateGroupActionAnswer =
  struct
    type batchPutScheduledUpdateGroupActionResult =
      {
      failedScheduledUpdateGroupActions:
        FailedScheduledUpdateGroupActionRequests.t option }
    and responseMetaData = unit
    and t =
      {
      batchPutScheduledUpdateGroupActionResult:
        batchPutScheduledUpdateGroupActionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AlreadyExistsFault of AlreadyExistsFault.t 
      | `LimitExceededFault of LimitExceededFault.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "BatchPutScheduledUpdateGroupActionAnswer"
    let make ?failedScheduledUpdateGroupActions =
      fun () ->
        {
          batchPutScheduledUpdateGroupActionResult =
            { failedScheduledUpdateGroupActions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AlreadyExistsFault" ->
          `AlreadyExistsFault (AlreadyExistsFault.of_json json)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsFault" ->
          `AlreadyExistsFault (AlreadyExistsFault.of_xml xml)
      | "LimitExceededFault" ->
          `LimitExceededFault (LimitExceededFault.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsFault e ->
          `Assoc
            [("error", (`String "AlreadyExistsFault"));
            ("details", (AlreadyExistsFault.to_json e))]
      | `LimitExceededFault e ->
          `Assoc
            [("error", (`String "LimitExceededFault"));
            ("details", (LimitExceededFault.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.batchPutScheduledUpdateGroupActionResult in
      structure_to_wrapped_value
        [("FailedScheduledUpdateGroupActions",
           (Option.map x.failedScheduledUpdateGroupActions
              ~f:FailedScheduledUpdateGroupActionRequests.to_value))]
        ~wrapper:"BatchPutScheduledUpdateGroupActionResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "BatchPutScheduledUpdateGroupActionResult" in
      let failedScheduledUpdateGroupActions =
        (Option.map ~f:FailedScheduledUpdateGroupActionRequests.of_xml)
          (Xml.child xml_arg0 "FailedScheduledUpdateGroupActions") in
      make ?failedScheduledUpdateGroupActions ()
    let of_json json =
      let failedScheduledUpdateGroupActions =
        field_map json "FailedScheduledUpdateGroupActions"
          FailedScheduledUpdateGroupActionRequests.of_json in
      make ?failedScheduledUpdateGroupActions ()
    let to_json v = composed_to_json to_value v
  end
module BatchDeleteScheduledActionType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      scheduledActionNames: ScheduledActionNames.t }
    let context_ = "BatchDeleteScheduledActionType"
    let make ~autoScalingGroupName =
      fun ~scheduledActionNames ->
        fun () -> { autoScalingGroupName; scheduledActionNames }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("ScheduledActionNames",
          (Some (ScheduledActionNames.to_value x.scheduledActionNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledActionNames =
        ScheduledActionNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduledActionNames") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~scheduledActionNames ~autoScalingGroupName ()
    let of_json json =
      let scheduledActionNames =
        field_map_exn json "ScheduledActionNames"
          ScheduledActionNames.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~scheduledActionNames ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module BatchDeleteScheduledActionAnswer =
  struct
    type batchDeleteScheduledActionResult =
      {
      failedScheduledActions:
        FailedScheduledUpdateGroupActionRequests.t option }
    and responseMetaData = unit
    and t =
      {
      batchDeleteScheduledActionResult: batchDeleteScheduledActionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "BatchDeleteScheduledActionAnswer"
    let make ?failedScheduledActions =
      fun () ->
        {
          batchDeleteScheduledActionResult = { failedScheduledActions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.batchDeleteScheduledActionResult in
      structure_to_wrapped_value
        [("FailedScheduledActions",
           (Option.map x.failedScheduledActions
              ~f:FailedScheduledUpdateGroupActionRequests.to_value))]
        ~wrapper:"BatchDeleteScheduledActionResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "BatchDeleteScheduledActionResult" in
      let failedScheduledActions =
        (Option.map ~f:FailedScheduledUpdateGroupActionRequests.of_xml)
          (Xml.child xml_arg0 "FailedScheduledActions") in
      make ?failedScheduledActions ()
    let of_json json =
      let failedScheduledActions =
        field_map json "FailedScheduledActions"
          FailedScheduledUpdateGroupActionRequests.of_json in
      make ?failedScheduledActions ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingInstancesType =
  struct
    type describeAutoScalingInstancesResult =
      {
      autoScalingInstances: AutoScalingInstances.t option ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeAutoScalingInstancesResult: describeAutoScalingInstancesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "AutoScalingInstancesType"
    let make ?autoScalingInstances =
      fun ?nextToken ->
        fun () ->
          {
            describeAutoScalingInstancesResult =
              { autoScalingInstances; nextToken };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAutoScalingInstancesResult in
      structure_to_wrapped_value
        [("AutoScalingInstances",
           (Option.map x.autoScalingInstances
              ~f:AutoScalingInstances.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeAutoScalingInstancesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeAutoScalingInstancesResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let autoScalingInstances =
        (Option.map ~f:AutoScalingInstances.of_xml)
          (Xml.child xml_arg0 "AutoScalingInstances") in
      make ?nextToken ?autoScalingInstances ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let autoScalingInstances =
        field_map json "AutoScalingInstances" AutoScalingInstances.of_json in
      make ?nextToken ?autoScalingInstances ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroupsType =
  struct
    type describeAutoScalingGroupsResult =
      {
      autoScalingGroups: AutoScalingGroups.t ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeAutoScalingGroupsResult: describeAutoScalingGroupsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "AutoScalingGroupsType"
    let make ?nextToken =
      fun ~autoScalingGroups ->
        fun () ->
          {
            describeAutoScalingGroupsResult =
              { nextToken; autoScalingGroups };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAutoScalingGroupsResult in
      structure_to_wrapped_value
        [("AutoScalingGroups",
           (Some (AutoScalingGroups.to_value x.autoScalingGroups)));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeAutoScalingGroupsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeAutoScalingGroupsResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let autoScalingGroups =
        AutoScalingGroups.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroups") in
      make ?nextToken ~autoScalingGroups ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let autoScalingGroups =
        field_map_exn json "AutoScalingGroups" AutoScalingGroups.of_json in
      make ?nextToken ~autoScalingGroups ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroupNamesType =
  struct
    type nonrec t =
      {
      autoScalingGroupNames: AutoScalingGroupNames.t option ;
      nextToken: XmlString.t option ;
      maxRecords: MaxRecords.t option ;
      filters: Filters.t option }
    let make ?autoScalingGroupNames =
      fun ?nextToken ->
        fun ?maxRecords ->
          fun ?filters ->
            fun () ->
              { autoScalingGroupNames; nextToken; maxRecords; filters }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupNames",
           (Option.map x.autoScalingGroupNames
              ~f:AutoScalingGroupNames.to_value));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value));
        ("MaxRecords", (Option.map x.maxRecords ~f:MaxRecords.to_value));
        ("Filters", (Option.map x.filters ~f:Filters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:Filters.of_xml) (Xml.child xml_arg0 "Filters") in
      let maxRecords =
        (Option.map ~f:MaxRecords.of_xml) (Xml.child xml_arg0 "MaxRecords") in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let autoScalingGroupNames =
        (Option.map ~f:AutoScalingGroupNames.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupNames") in
      make ?filters ?maxRecords ?nextToken ?autoScalingGroupNames ()
    let of_json json =
      let filters = field_map json "Filters" Filters.of_json in
      let maxRecords = field_map json "MaxRecords" MaxRecords.of_json in
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let autoScalingGroupNames =
        field_map json "AutoScalingGroupNames" AutoScalingGroupNames.of_json in
      make ?filters ?maxRecords ?nextToken ?autoScalingGroupNames ()
    let to_json v = composed_to_json to_value v
  end
module AttachLoadBalancersType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      loadBalancerNames: LoadBalancerNames.t }
    let context_ = "AttachLoadBalancersType"
    let make ~autoScalingGroupName =
      fun ~loadBalancerNames ->
        fun () -> { autoScalingGroupName; loadBalancerNames }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("LoadBalancerNames",
          (Some (LoadBalancerNames.to_value x.loadBalancerNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerNames =
        LoadBalancerNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerNames") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~loadBalancerNames ~autoScalingGroupName ()
    let of_json json =
      let loadBalancerNames =
        field_map_exn json "LoadBalancerNames" LoadBalancerNames.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~loadBalancerNames ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module AttachLoadBalancersResultType =
  struct
    type attachLoadBalancersResult = unit
    and responseMetaData = unit
    and t =
      {
      attachLoadBalancersResult: attachLoadBalancersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `ServiceLinkedRoleFailure of ServiceLinkedRoleFailure.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { attachLoadBalancersResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `ServiceLinkedRoleFailure e ->
          `Assoc
            [("error", (`String "ServiceLinkedRoleFailure"));
            ("details", (ServiceLinkedRoleFailure.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AttachLoadBalancerTargetGroupsType =
  struct
    type nonrec t =
      {
      autoScalingGroupName: XmlStringMaxLen255.t ;
      targetGroupARNs: TargetGroupARNs.t }
    let context_ = "AttachLoadBalancerTargetGroupsType"
    let make ~autoScalingGroupName =
      fun ~targetGroupARNs ->
        fun () -> { autoScalingGroupName; targetGroupARNs }
    let to_value x =
      structure_to_value
        [("AutoScalingGroupName",
           (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)));
        ("TargetGroupARNs",
          (Some (TargetGroupARNs.to_value x.targetGroupARNs)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetGroupARNs =
        TargetGroupARNs.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetGroupARNs") in
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      make ~targetGroupARNs ~autoScalingGroupName ()
    let of_json json =
      let targetGroupARNs =
        field_map_exn json "TargetGroupARNs" TargetGroupARNs.of_json in
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      make ~targetGroupARNs ~autoScalingGroupName ()
    let to_json v = composed_to_json to_value v
  end
module AttachLoadBalancerTargetGroupsResultType =
  struct
    type attachLoadBalancerTargetGroupsResult = unit
    and responseMetaData = unit
    and t =
      {
      attachLoadBalancerTargetGroupsResult:
        attachLoadBalancerTargetGroupsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `ServiceLinkedRoleFailure of ServiceLinkedRoleFailure.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { attachLoadBalancerTargetGroupsResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | "ServiceLinkedRoleFailure" ->
          `ServiceLinkedRoleFailure (ServiceLinkedRoleFailure.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `ServiceLinkedRoleFailure e ->
          `Assoc
            [("error", (`String "ServiceLinkedRoleFailure"));
            ("details", (ServiceLinkedRoleFailure.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AttachInstancesQuery =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIds.t option ;
      autoScalingGroupName: XmlStringMaxLen255.t }
    let context_ = "AttachInstancesQuery"
    let make ?instanceIds =
      fun ~autoScalingGroupName ->
        fun () -> { instanceIds; autoScalingGroupName }
    let to_value x =
      structure_to_value
        [("InstanceIds", (Option.map x.instanceIds ~f:InstanceIds.to_value));
        ("AutoScalingGroupName",
          (Some (XmlStringMaxLen255.to_value x.autoScalingGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingGroupName =
        XmlStringMaxLen255.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingGroupName") in
      let instanceIds =
        (Option.map ~f:InstanceIds.of_xml) (Xml.child xml_arg0 "InstanceIds") in
      make ~autoScalingGroupName ?instanceIds ()
    let of_json json =
      let autoScalingGroupName =
        field_map_exn json "AutoScalingGroupName" XmlStringMaxLen255.of_json in
      let instanceIds = field_map json "InstanceIds" InstanceIds.of_json in
      make ~autoScalingGroupName ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end
module ActivityType =
  struct
    type terminateInstanceInAutoScalingGroupResult =
      {
      activity: Activity.t option }
    and responseMetaData = unit
    and t =
      {
      terminateInstanceInAutoScalingGroupResult:
        terminateInstanceInAutoScalingGroupResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `ResourceContentionFault of ResourceContentionFault.t 
      | `ScalingActivityInProgressFault of ScalingActivityInProgressFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ActivityType"
    let make ?activity =
      fun () ->
        {
          terminateInstanceInAutoScalingGroupResult = { activity };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | "ScalingActivityInProgressFault" ->
          `ScalingActivityInProgressFault
            (ScalingActivityInProgressFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | "ScalingActivityInProgressFault" ->
          `ScalingActivityInProgressFault
            (ScalingActivityInProgressFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `ScalingActivityInProgressFault e ->
          `Assoc
            [("error", (`String "ScalingActivityInProgressFault"));
            ("details", (ScalingActivityInProgressFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.terminateInstanceInAutoScalingGroupResult in
      structure_to_wrapped_value
        [("Activity", (Option.map x.activity ~f:Activity.to_value))]
        ~wrapper:"TerminateInstanceInAutoScalingGroupResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "TerminateInstanceInAutoScalingGroupResult" in
      let activity =
        (Option.map ~f:Activity.of_xml) (Xml.child xml_arg0 "Activity") in
      make ?activity ()
    let of_json json =
      let activity = field_map json "Activity" Activity.of_json in
      make ?activity ()
    let to_json v = composed_to_json to_value v
  end
module ActivitiesType =
  struct
    type describeScalingActivitiesResult =
      {
      activities: Activities.t ;
      nextToken: XmlString.t option }
    and responseMetaData = unit
    and t =
      {
      describeScalingActivitiesResult: describeScalingActivitiesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `InvalidNextToken of InvalidNextToken.t 
      | `ResourceContentionFault of ResourceContentionFault.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ActivitiesType"
    let make ?nextToken =
      fun ~activities ->
        fun () ->
          {
            describeScalingActivitiesResult = { nextToken; activities };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "InvalidNextToken" ->
          `InvalidNextToken (InvalidNextToken.of_json json)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextToken" -> `InvalidNextToken (InvalidNextToken.of_xml xml)
      | "ResourceContentionFault" ->
          `ResourceContentionFault (ResourceContentionFault.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextToken e ->
          `Assoc
            [("error", (`String "InvalidNextToken"));
            ("details", (InvalidNextToken.to_json e))]
      | `ResourceContentionFault e ->
          `Assoc
            [("error", (`String "ResourceContentionFault"));
            ("details", (ResourceContentionFault.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeScalingActivitiesResult in
      structure_to_wrapped_value
        [("Activities", (Some (Activities.to_value x.activities)));
        ("NextToken", (Option.map x.nextToken ~f:XmlString.to_value))]
        ~wrapper:"DescribeScalingActivitiesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeScalingActivitiesResult" in
      let nextToken =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "NextToken") in
      let activities =
        Activities.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Activities") in
      make ?nextToken ~activities ()
    let of_json json =
      let nextToken = field_map json "NextToken" XmlString.of_json in
      let activities = field_map_exn json "Activities" Activities.of_json in
      make ?nextToken ~activities ()
    let to_json v = composed_to_json to_value v
  end
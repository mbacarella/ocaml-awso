(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | Autocomplete: (AutocompleteRequest.t, AutocompleteResponse.t,
  AutocompleteResponse.error) t 
  | Geocode: (GeocodeRequest.t, GeocodeResponse.t, GeocodeResponse.error) t 
  | GetPlace: (GetPlaceRequest.t, GetPlaceResponse.t, GetPlaceResponse.error)
  t 
  | ReverseGeocode: (ReverseGeocodeRequest.t, ReverseGeocodeResponse.t,
  ReverseGeocodeResponse.error) t 
  | SearchNearby: (SearchNearbyRequest.t, SearchNearbyResponse.t,
  SearchNearbyResponse.error) t 
  | SearchText: (SearchTextRequest.t, SearchTextResponse.t,
  SearchTextResponse.error) t 
  | Suggest: (SuggestRequest.t, SuggestResponse.t, SuggestResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | Autocomplete -> `POST
  | Geocode -> `POST
  | GetPlace -> `GET
  | ReverseGeocode -> `POST
  | SearchNearby -> `POST
  | SearchText -> `POST
  | Suggest -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | Autocomplete ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/autocomplete")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | Geocode ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/geocode")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | GetPlace ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/place/%s"
               (GetPlaceRequestPlaceIdString.to_header
                  x.GetPlaceRequest.placeId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("additional-features",
                          (GetPlaceAdditionalFeatureList.to_header v)))
                  x.additionalFeatures;
               Option.map
                 ~f:(fun v -> ("language", (LanguageTag.to_header v)))
                 x.language;
               Option.map
                 ~f:(fun v -> ("political-view", (CountryCode.to_header v)))
                 x.politicalView;
               Option.map
                 ~f:(fun v ->
                       ("intended-use", (GetPlaceIntendedUse.to_header v)))
                 x.intendedUse;
               Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | ReverseGeocode ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/reverse-geocode")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | SearchNearby ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/search-nearby")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | SearchText ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/search-text")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | Suggest ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v2/suggest")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | Autocomplete ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("QueryText",
                           (AutocompleteRequestQueryTextString.to_value
                              req.AutocompleteRequest.queryText));
                      Option.map req.AutocompleteRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (AutocompleteRequestMaxResultsInteger.to_value
                                   x)));
                      Option.map req.AutocompleteRequest.biasPosition
                        ~f:(fun x -> ("BiasPosition", (Position.to_value x)));
                      Option.map req.AutocompleteRequest.filter
                        ~f:(fun x ->
                              ("Filter", (AutocompleteFilter.to_value x)));
                      Option.map req.AutocompleteRequest.postalCodeMode
                        ~f:(fun x ->
                              ("PostalCodeMode", (PostalCodeMode.to_value x)));
                      Option.map req.AutocompleteRequest.additionalFeatures
                        ~f:(fun x ->
                              ("AdditionalFeatures",
                                (AutocompleteAdditionalFeatureList.to_value x)));
                      Option.map req.AutocompleteRequest.language
                        ~f:(fun x -> ("Language", (LanguageTag.to_value x)));
                      Option.map req.AutocompleteRequest.politicalView
                        ~f:(fun x ->
                              ("PoliticalView", (CountryCode.to_value x)));
                      Option.map req.AutocompleteRequest.intendedUse
                        ~f:(fun x ->
                              ("IntendedUse",
                                (AutocompleteIntendedUse.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Geocode ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GeocodeRequest.queryText
                         ~f:(fun x ->
                               ("QueryText",
                                 (GeocodeRequestQueryTextString.to_value x)));
                      Option.map req.GeocodeRequest.queryComponents
                        ~f:(fun x ->
                              ("QueryComponents",
                                (GeocodeQueryComponents.to_value x)));
                      Option.map req.GeocodeRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (GeocodeRequestMaxResultsInteger.to_value x)));
                      Option.map req.GeocodeRequest.biasPosition
                        ~f:(fun x -> ("BiasPosition", (Position.to_value x)));
                      Option.map req.GeocodeRequest.filter
                        ~f:(fun x -> ("Filter", (GeocodeFilter.to_value x)));
                      Option.map req.GeocodeRequest.additionalFeatures
                        ~f:(fun x ->
                              ("AdditionalFeatures",
                                (GeocodeAdditionalFeatureList.to_value x)));
                      Option.map req.GeocodeRequest.language
                        ~f:(fun x -> ("Language", (LanguageTag.to_value x)));
                      Option.map req.GeocodeRequest.politicalView
                        ~f:(fun x ->
                              ("PoliticalView", (CountryCode.to_value x)));
                      Option.map req.GeocodeRequest.intendedUse
                        ~f:(fun x ->
                              ("IntendedUse",
                                (GeocodeIntendedUse.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPlace ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ReverseGeocode ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("QueryPosition",
                           (Position.to_value
                              req.ReverseGeocodeRequest.queryPosition));
                      Option.map req.ReverseGeocodeRequest.queryRadius
                        ~f:(fun x ->
                              ("QueryRadius",
                                (ReverseGeocodeRequestQueryRadiusLong.to_value
                                   x)));
                      Option.map req.ReverseGeocodeRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ReverseGeocodeRequestMaxResultsInteger.to_value
                                   x)));
                      Option.map req.ReverseGeocodeRequest.filter
                        ~f:(fun x ->
                              ("Filter", (ReverseGeocodeFilter.to_value x)));
                      Option.map req.ReverseGeocodeRequest.additionalFeatures
                        ~f:(fun x ->
                              ("AdditionalFeatures",
                                (ReverseGeocodeAdditionalFeatureList.to_value
                                   x)));
                      Option.map req.ReverseGeocodeRequest.language
                        ~f:(fun x -> ("Language", (LanguageTag.to_value x)));
                      Option.map req.ReverseGeocodeRequest.politicalView
                        ~f:(fun x ->
                              ("PoliticalView", (CountryCode.to_value x)));
                      Option.map req.ReverseGeocodeRequest.intendedUse
                        ~f:(fun x ->
                              ("IntendedUse",
                                (ReverseGeocodeIntendedUse.to_value x)));
                      Option.map req.ReverseGeocodeRequest.heading
                        ~f:(fun x -> ("Heading", (Heading.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SearchNearby ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("QueryPosition",
                           (Position.to_value
                              req.SearchNearbyRequest.queryPosition));
                      Option.map req.SearchNearbyRequest.queryRadius
                        ~f:(fun x ->
                              ("QueryRadius",
                                (SearchNearbyRequestQueryRadiusLong.to_value
                                   x)));
                      Option.map req.SearchNearbyRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (SearchNearbyRequestMaxResultsInteger.to_value
                                   x)));
                      Option.map req.SearchNearbyRequest.filter
                        ~f:(fun x ->
                              ("Filter", (SearchNearbyFilter.to_value x)));
                      Option.map req.SearchNearbyRequest.additionalFeatures
                        ~f:(fun x ->
                              ("AdditionalFeatures",
                                (SearchNearbyAdditionalFeatureList.to_value x)));
                      Option.map req.SearchNearbyRequest.language
                        ~f:(fun x -> ("Language", (LanguageTag.to_value x)));
                      Option.map req.SearchNearbyRequest.politicalView
                        ~f:(fun x ->
                              ("PoliticalView", (CountryCode.to_value x)));
                      Option.map req.SearchNearbyRequest.intendedUse
                        ~f:(fun x ->
                              ("IntendedUse",
                                (SearchNearbyIntendedUse.to_value x)));
                      Option.map req.SearchNearbyRequest.nextToken
                        ~f:(fun x -> ("NextToken", (Token.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SearchText ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SearchTextRequest.queryText
                         ~f:(fun x ->
                               ("QueryText",
                                 (SearchTextRequestQueryTextString.to_value x)));
                      Option.map req.SearchTextRequest.queryId
                        ~f:(fun x ->
                              ("QueryId",
                                (SearchTextRequestQueryIdString.to_value x)));
                      Option.map req.SearchTextRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (SearchTextRequestMaxResultsInteger.to_value
                                   x)));
                      Option.map req.SearchTextRequest.biasPosition
                        ~f:(fun x -> ("BiasPosition", (Position.to_value x)));
                      Option.map req.SearchTextRequest.filter
                        ~f:(fun x ->
                              ("Filter", (SearchTextFilter.to_value x)));
                      Option.map req.SearchTextRequest.additionalFeatures
                        ~f:(fun x ->
                              ("AdditionalFeatures",
                                (SearchTextAdditionalFeatureList.to_value x)));
                      Option.map req.SearchTextRequest.language
                        ~f:(fun x -> ("Language", (LanguageTag.to_value x)));
                      Option.map req.SearchTextRequest.politicalView
                        ~f:(fun x ->
                              ("PoliticalView", (CountryCode.to_value x)));
                      Option.map req.SearchTextRequest.intendedUse
                        ~f:(fun x ->
                              ("IntendedUse",
                                (SearchTextIntendedUse.to_value x)));
                      Option.map req.SearchTextRequest.nextToken
                        ~f:(fun x -> ("NextToken", (Token.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Suggest ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("QueryText",
                           (SuggestRequestQueryTextString.to_value
                              req.SuggestRequest.queryText));
                      Option.map req.SuggestRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (SuggestRequestMaxResultsInteger.to_value x)));
                      Option.map req.SuggestRequest.maxQueryRefinements
                        ~f:(fun x ->
                              ("MaxQueryRefinements",
                                (SuggestRequestMaxQueryRefinementsInteger.to_value
                                   x)));
                      Option.map req.SuggestRequest.biasPosition
                        ~f:(fun x -> ("BiasPosition", (Position.to_value x)));
                      Option.map req.SuggestRequest.filter
                        ~f:(fun x -> ("Filter", (SuggestFilter.to_value x)));
                      Option.map req.SuggestRequest.additionalFeatures
                        ~f:(fun x ->
                              ("AdditionalFeatures",
                                (SuggestAdditionalFeatureList.to_value x)));
                      Option.map req.SuggestRequest.language
                        ~f:(fun x -> ("Language", (LanguageTag.to_value x)));
                      Option.map req.SuggestRequest.politicalView
                        ~f:(fun x ->
                              ("PoliticalView", (CountryCode.to_value x)));
                      Option.map req.SuggestRequest.intendedUse
                        ~f:(fun x ->
                              ("IntendedUse",
                                (SuggestIntendedUse.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | Autocomplete ->
      if is_success
      then Ok (AutocompleteResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some AutocompleteResponse.error_of_json))
  | Geocode ->
      if is_success
      then Ok (GeocodeResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GeocodeResponse.error_of_json))
  | GetPlace ->
      if is_success
      then Ok (GetPlaceResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetPlaceResponse.error_of_json))
  | ReverseGeocode ->
      if is_success
      then Ok (ReverseGeocodeResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ReverseGeocodeResponse.error_of_json))
  | SearchNearby ->
      if is_success
      then Ok (SearchNearbyResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchNearbyResponse.error_of_json))
  | SearchText ->
      if is_success
      then Ok (SearchTextResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchTextResponse.error_of_json))
  | Suggest ->
      if is_success
      then Ok (SuggestResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SuggestResponse.error_of_json))
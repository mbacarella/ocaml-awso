(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and resourceQuery =
         flag "resource-query" (optional json_arg) ~doc:"JSON ResourceQuery"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON GroupConfigurationList"
       and name = flag "name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupInput.make ?description
              ?resourceQuery:(Option.map ~f:Values.ResourceQuery.of_json
                                resourceQuery)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?configuration:(Option.map
                                ~f:Values.GroupConfigurationList.of_json
                                configuration) ~name ())
           (Some Values.CreateGroupOutput.to_json)
           (Some Values.CreateGroupOutput.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and group = flag "group" (optional string) ~doc:"STRING GroupString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupInput.make ?groupName ?group ())
           (Some Values.DeleteGroupOutput.to_json)
           (Some Values.DeleteGroupOutput.error_to_json)])
let get_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and group = flag "group" (optional string) ~doc:"STRING GroupString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group (Values.GetGroupInput.make ?groupName ?group ())
           (Some Values.GetGroupOutput.to_json)
           (Some Values.GetGroupOutput.error_to_json)])
let get_group_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and group = flag "group" (optional string) ~doc:"STRING GroupString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_configuration
           (Values.GetGroupConfigurationInput.make ?group ())
           (Some Values.GetGroupConfigurationOutput.to_json)
           (Some Values.GetGroupConfigurationOutput.error_to_json)])
let get_group_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and group = flag "group" (optional string) ~doc:"STRING GroupString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_query
           (Values.GetGroupQueryInput.make ?groupName ?group ())
           (Some Values.GetGroupQueryOutput.to_json)
           (Some Values.GetGroupQueryOutput.error_to_json)])
let get_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING GroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tags (Values.GetTagsInput.make ~arn ())
           (Some Values.GetTagsOutput.to_json)
           (Some Values.GetTagsOutput.error_to_json)])
let group_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and group = flag "group" (required string) ~doc:"STRING GroupString"
       and resourceArns =
         flag "resource-arns" (required json_arg) ~doc:"JSON ResourceArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.group_resources
           (Values.GroupResourcesInput.make ~group
              ~resourceArns:(Values.ResourceArnList.of_json resourceArns) ())
           (Some Values.GroupResourcesOutput.to_json)
           (Some Values.GroupResourcesOutput.error_to_json)])
let list_group_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and group = flag "group" (optional string) ~doc:"STRING GroupString"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ResourceFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_resources
           (Values.ListGroupResourcesInput.make ?groupName ?group
              ?filters:(Option.map ~f:Values.ResourceFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListGroupResourcesOutput.to_json)
           (Some Values.ListGroupResourcesOutput.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON GroupFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsInput.make
              ?filters:(Option.map ~f:Values.GroupFilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListGroupsOutput.to_json)
           (Some Values.ListGroupsOutput.error_to_json)])
let put_group_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and group = flag "group" (optional string) ~doc:"STRING GroupString"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON GroupConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_group_configuration
           (Values.PutGroupConfigurationInput.make ?group
              ?configuration:(Option.map
                                ~f:Values.GroupConfigurationList.of_json
                                configuration) ())
           (Some Values.PutGroupConfigurationOutput.to_json)
           (Some Values.PutGroupConfigurationOutput.error_to_json)])
let search_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceQuery =
         flag "resource-query" (required json_arg) ~doc:"JSON ResourceQuery" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_resources
           (Values.SearchResourcesInput.make ?maxResults ?nextToken
              ~resourceQuery:(Values.ResourceQuery.of_json resourceQuery) ())
           (Some Values.SearchResourcesOutput.to_json)
           (Some Values.SearchResourcesOutput.error_to_json)])
let tag =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING GroupArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.tag
           (Values.TagInput.make ~arn ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagOutput.to_json)
           (Some Values.TagOutput.error_to_json)])
let ungroup_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and group = flag "group" (required string) ~doc:"STRING GroupString"
       and resourceArns =
         flag "resource-arns" (required json_arg) ~doc:"JSON ResourceArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.ungroup_resources
           (Values.UngroupResourcesInput.make ~group
              ~resourceArns:(Values.ResourceArnList.of_json resourceArns) ())
           (Some Values.UngroupResourcesOutput.to_json)
           (Some Values.UngroupResourcesOutput.error_to_json)])
let untag =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING GroupArn"
       and keys = flag "keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.untag
           (Values.UntagInput.make ~arn
              ~keys:(Values.TagKeyList.of_json keys) ())
           (Some Values.UntagOutput.to_json)
           (Some Values.UntagOutput.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and group = flag "group" (optional string) ~doc:"STRING GroupString"
       and description =
         flag "description" (optional string) ~doc:"STRING Description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupInput.make ?groupName ?group ?description ())
           (Some Values.UpdateGroupOutput.to_json)
           (Some Values.UpdateGroupOutput.error_to_json)])
let update_group_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and group = flag "group" (optional string) ~doc:"STRING GroupString"
       and resourceQuery =
         flag "resource-query" (required json_arg) ~doc:"JSON ResourceQuery" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group_query
           (Values.UpdateGroupQueryInput.make ?groupName ?group
              ~resourceQuery:(Values.ResourceQuery.of_json resourceQuery) ())
           (Some Values.UpdateGroupQueryOutput.to_json)
           (Some Values.UpdateGroupQueryOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-group", create_group);
    ("delete-group", delete_group);
    ("get-group", get_group);
    ("get-group-configuration", get_group_configuration);
    ("get-group-query", get_group_query);
    ("get-tags", get_tags);
    ("group-resources", group_resources);
    ("list-group-resources", list_group_resources);
    ("list-groups", list_groups);
    ("put-group-configuration", put_group_configuration);
    ("search-resources", search_resources);
    ("tag", tag);
    ("ungroup-resources", ungroup_resources);
    ("untag", untag);
    ("update-group", update_group);
    ("update-group-query", update_group_query)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteHumanLoop: (DeleteHumanLoopRequest.t, DeleteHumanLoopResponse.t,
  DeleteHumanLoopResponse.error) t 
  | DescribeHumanLoop: (DescribeHumanLoopRequest.t,
  DescribeHumanLoopResponse.t, DescribeHumanLoopResponse.error) t 
  | ListHumanLoops: (ListHumanLoopsRequest.t, ListHumanLoopsResponse.t,
  ListHumanLoopsResponse.error) t 
  | StartHumanLoop: (StartHumanLoopRequest.t, StartHumanLoopResponse.t,
  StartHumanLoopResponse.error) t 
  | StopHumanLoop: (StopHumanLoopRequest.t, StopHumanLoopResponse.t,
  StopHumanLoopResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteHumanLoop -> `DELETE
  | DescribeHumanLoop -> `GET
  | ListHumanLoops -> `GET
  | StartHumanLoop -> `POST
  | StopHumanLoop -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteHumanLoop ->
          (Format.kasprintf Uri.of_string) "/human-loops/%s"
            (HumanLoopName.to_header x.DeleteHumanLoopRequest.humanLoopName)
      | DescribeHumanLoop ->
          (Format.kasprintf Uri.of_string) "/human-loops/%s"
            (HumanLoopName.to_header x.DescribeHumanLoopRequest.humanLoopName)
      | ListHumanLoops ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/human-loops")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("CreationTimeAfter", (Timestamp.to_header v)))
                  x.creationTimeAfter;
               Option.map
                 ~f:(fun v -> ("CreationTimeBefore", (Timestamp.to_header v)))
                 x.creationTimeBefore;
               Some
                 ("FlowDefinitionArn",
                   (FlowDefinitionArn.to_header x.flowDefinitionArn));
               Option.map
                 ~f:(fun v -> ("SortOrder", (SortOrder.to_header v)))
                 x.sortOrder;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("MaxResults", (MaxResults.to_header v)))
                 x.maxResults])
      | StartHumanLoop -> (Format.kasprintf Uri.of_string) "/human-loops"
      | StopHumanLoop -> (Format.kasprintf Uri.of_string) "/human-loops/stop")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteHumanLoop -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeHumanLoop ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListHumanLoops ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartHumanLoop ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("HumanLoopName",
                           (HumanLoopName.to_value
                              req.StartHumanLoopRequest.humanLoopName));
                      Some
                        ("FlowDefinitionArn",
                          (FlowDefinitionArn.to_value
                             req.StartHumanLoopRequest.flowDefinitionArn));
                      Some
                        ("HumanLoopInput",
                          (HumanLoopInput.to_value
                             req.StartHumanLoopRequest.humanLoopInput));
                      Option.map req.StartHumanLoopRequest.dataAttributes
                        ~f:(fun x ->
                              ("DataAttributes",
                                (HumanLoopDataAttributes.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopHumanLoop ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("HumanLoopName",
                           (HumanLoopName.to_value
                              req.StopHumanLoopRequest.humanLoopName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | DeleteHumanLoop ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteHumanLoopResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteHumanLoopResponse.of_header_and_body (headers, ())))
  | DescribeHumanLoop ->
      (match resp with
       | Error err ->
           handle_error err (Some DescribeHumanLoopResponse.error_of_json)
       | Ok resp ->
           Ok (DescribeHumanLoopResponse.of_json (response_to_json resp)))
  | ListHumanLoops ->
      (match resp with
       | Error err ->
           handle_error err (Some ListHumanLoopsResponse.error_of_json)
       | Ok resp ->
           Ok (ListHumanLoopsResponse.of_json (response_to_json resp)))
  | StartHumanLoop ->
      (match resp with
       | Error err ->
           handle_error err (Some StartHumanLoopResponse.error_of_json)
       | Ok resp ->
           Ok (StartHumanLoopResponse.of_json (response_to_json resp)))
  | StopHumanLoop ->
      (match resp with
       | Error err ->
           handle_error err (Some StopHumanLoopResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (StopHumanLoopResponse.of_header_and_body (headers, ())))
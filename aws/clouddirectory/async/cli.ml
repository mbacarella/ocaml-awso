(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_facet_to_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and objectAttributeList =
         flag "object-attribute-list" (optional json_arg)
           ~doc:"JSON AttributeKeyAndValueList"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and schemaFacet =
         flag "schema-facet" (required json_arg) ~doc:"JSON SchemaFacet"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_facet_to_object
           (Values.AddFacetToObjectRequest.make
              ?objectAttributeList:(Option.map
                                      ~f:Values.AttributeKeyAndValueList.of_json
                                      objectAttributeList) ~directoryArn
              ~schemaFacet:(Values.SchemaFacet.of_json schemaFacet)
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.AddFacetToObjectResponse.to_json)
           (Some Values.AddFacetToObjectResponse.error_to_json)])
let apply_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publishedSchemaArn =
         flag "published-schema-arn" (required string) ~doc:"STRING Arn"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_schema
           (Values.ApplySchemaRequest.make ~publishedSchemaArn ~directoryArn
              ()) (Some Values.ApplySchemaResponse.to_json)
           (Some Values.ApplySchemaResponse.error_to_json)])
let attach_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and parentReference =
         flag "parent-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and childReference =
         flag "child-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and linkName =
         flag "link-name" (required string) ~doc:"STRING LinkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_object
           (Values.AttachObjectRequest.make ~directoryArn
              ~parentReference:(Values.ObjectReference.of_json
                                  parentReference)
              ~childReference:(Values.ObjectReference.of_json childReference)
              ~linkName ()) (Some Values.AttachObjectResponse.to_json)
           (Some Values.AttachObjectResponse.error_to_json)])
let attach_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and policyReference =
         flag "policy-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_policy
           (Values.AttachPolicyRequest.make ~directoryArn
              ~policyReference:(Values.ObjectReference.of_json
                                  policyReference)
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.AttachPolicyResponse.to_json)
           (Some Values.AttachPolicyResponse.error_to_json)])
let attach_to_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and indexReference =
         flag "index-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and targetReference =
         flag "target-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_to_index
           (Values.AttachToIndexRequest.make ~directoryArn
              ~indexReference:(Values.ObjectReference.of_json indexReference)
              ~targetReference:(Values.ObjectReference.of_json
                                  targetReference) ())
           (Some Values.AttachToIndexResponse.to_json)
           (Some Values.AttachToIndexResponse.error_to_json)])
let attach_typed_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and sourceObjectReference =
         flag "source-object-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and targetObjectReference =
         flag "target-object-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and typedLinkFacet =
         flag "typed-link-facet" (required json_arg)
           ~doc:"JSON TypedLinkSchemaAndFacetName"
       and attributes =
         flag "attributes" (required json_arg)
           ~doc:"JSON AttributeNameAndValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_typed_link
           (Values.AttachTypedLinkRequest.make ~directoryArn
              ~sourceObjectReference:(Values.ObjectReference.of_json
                                        sourceObjectReference)
              ~targetObjectReference:(Values.ObjectReference.of_json
                                        targetObjectReference)
              ~typedLinkFacet:(Values.TypedLinkSchemaAndFacetName.of_json
                                 typedLinkFacet)
              ~attributes:(Values.AttributeNameAndValueList.of_json
                             attributes) ())
           (Some Values.AttachTypedLinkResponse.to_json)
           (Some Values.AttachTypedLinkResponse.error_to_json)])
let batch_read =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and operations =
         flag "operations" (required json_arg)
           ~doc:"JSON BatchReadOperationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_read
           (Values.BatchReadRequest.make
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~operations:(Values.BatchReadOperationList.of_json operations)
              ()) (Some Values.BatchReadResponse.to_json)
           (Some Values.BatchReadResponse.error_to_json)])
let batch_write =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and operations =
         flag "operations" (required json_arg)
           ~doc:"JSON BatchWriteOperationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_write
           (Values.BatchWriteRequest.make ~directoryArn
              ~operations:(Values.BatchWriteOperationList.of_json operations)
              ()) (Some Values.BatchWriteResponse.to_json)
           (Some Values.BatchWriteResponse.error_to_json)])
let create_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DirectoryName"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_directory
           (Values.CreateDirectoryRequest.make ~name ~schemaArn ())
           (Some Values.CreateDirectoryResponse.to_json)
           (Some Values.CreateDirectoryResponse.error_to_json)])
let create_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON FacetAttributeList"
       and objectType =
         flag "object-type" (optional json_arg) ~doc:"JSON ObjectType"
       and facetStyle =
         flag "facet-style" (optional json_arg) ~doc:"JSON FacetStyle"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING FacetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_facet
           (Values.CreateFacetRequest.make
              ?attributes:(Option.map ~f:Values.FacetAttributeList.of_json
                             attributes)
              ?objectType:(Option.map ~f:Values.ObjectType.of_json objectType)
              ?facetStyle:(Option.map ~f:Values.FacetStyle.of_json facetStyle)
              ~schemaArn ~name ()) (Some Values.CreateFacetResponse.to_json)
           (Some Values.CreateFacetResponse.error_to_json)])
let create_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parentReference =
         flag "parent-reference" (optional json_arg)
           ~doc:"JSON ObjectReference"
       and linkName =
         flag "link-name" (optional string) ~doc:"STRING LinkName"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and orderedIndexedAttributeList =
         flag "ordered-indexed-attribute-list" (required json_arg)
           ~doc:"JSON AttributeKeyList"
       and isUnique = flag "is-unique" (required bool) ~doc:"BOOL Bool" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_index
           (Values.CreateIndexRequest.make
              ?parentReference:(Option.map ~f:Values.ObjectReference.of_json
                                  parentReference) ?linkName ~directoryArn
              ~orderedIndexedAttributeList:(Values.AttributeKeyList.of_json
                                              orderedIndexedAttributeList)
              ~isUnique ()) (Some Values.CreateIndexResponse.to_json)
           (Some Values.CreateIndexResponse.error_to_json)])
let create_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and objectAttributeList =
         flag "object-attribute-list" (optional json_arg)
           ~doc:"JSON AttributeKeyAndValueList"
       and parentReference =
         flag "parent-reference" (optional json_arg)
           ~doc:"JSON ObjectReference"
       and linkName =
         flag "link-name" (optional string) ~doc:"STRING LinkName"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and schemaFacets =
         flag "schema-facets" (required json_arg) ~doc:"JSON SchemaFacetList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_object
           (Values.CreateObjectRequest.make
              ?objectAttributeList:(Option.map
                                      ~f:Values.AttributeKeyAndValueList.of_json
                                      objectAttributeList)
              ?parentReference:(Option.map ~f:Values.ObjectReference.of_json
                                  parentReference) ?linkName ~directoryArn
              ~schemaFacets:(Values.SchemaFacetList.of_json schemaFacets) ())
           (Some Values.CreateObjectResponse.to_json)
           (Some Values.CreateObjectResponse.error_to_json)])
let create_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING SchemaName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schema (Values.CreateSchemaRequest.make ~name ())
           (Some Values.CreateSchemaResponse.to_json)
           (Some Values.CreateSchemaResponse.error_to_json)])
let create_typed_link_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and facet =
         flag "facet" (required json_arg) ~doc:"JSON TypedLinkFacet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_typed_link_facet
           (Values.CreateTypedLinkFacetRequest.make ~schemaArn
              ~facet:(Values.TypedLinkFacet.of_json facet) ())
           (Some Values.CreateTypedLinkFacetResponse.to_json)
           (Some Values.CreateTypedLinkFacetResponse.error_to_json)])
let delete_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_directory
           (Values.DeleteDirectoryRequest.make ~directoryArn ())
           (Some Values.DeleteDirectoryResponse.to_json)
           (Some Values.DeleteDirectoryResponse.error_to_json)])
let delete_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING FacetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_facet
           (Values.DeleteFacetRequest.make ~schemaArn ~name ())
           (Some Values.DeleteFacetResponse.to_json)
           (Some Values.DeleteFacetResponse.error_to_json)])
let delete_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_object
           (Values.DeleteObjectRequest.make ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.DeleteObjectResponse.to_json)
           (Some Values.DeleteObjectResponse.error_to_json)])
let delete_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema (Values.DeleteSchemaRequest.make ~schemaArn ())
           (Some Values.DeleteSchemaResponse.to_json)
           (Some Values.DeleteSchemaResponse.error_to_json)])
let delete_typed_link_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING TypedLinkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_typed_link_facet
           (Values.DeleteTypedLinkFacetRequest.make ~schemaArn ~name ())
           (Some Values.DeleteTypedLinkFacetResponse.to_json)
           (Some Values.DeleteTypedLinkFacetResponse.error_to_json)])
let detach_from_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and indexReference =
         flag "index-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and targetReference =
         flag "target-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_from_index
           (Values.DetachFromIndexRequest.make ~directoryArn
              ~indexReference:(Values.ObjectReference.of_json indexReference)
              ~targetReference:(Values.ObjectReference.of_json
                                  targetReference) ())
           (Some Values.DetachFromIndexResponse.to_json)
           (Some Values.DetachFromIndexResponse.error_to_json)])
let detach_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and parentReference =
         flag "parent-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and linkName =
         flag "link-name" (required string) ~doc:"STRING LinkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_object
           (Values.DetachObjectRequest.make ~directoryArn
              ~parentReference:(Values.ObjectReference.of_json
                                  parentReference) ~linkName ())
           (Some Values.DetachObjectResponse.to_json)
           (Some Values.DetachObjectResponse.error_to_json)])
let detach_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and policyReference =
         flag "policy-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_policy
           (Values.DetachPolicyRequest.make ~directoryArn
              ~policyReference:(Values.ObjectReference.of_json
                                  policyReference)
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.DetachPolicyResponse.to_json)
           (Some Values.DetachPolicyResponse.error_to_json)])
let detach_typed_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and typedLinkSpecifier =
         flag "typed-link-specifier" (required json_arg)
           ~doc:"JSON TypedLinkSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_typed_link
           (Values.DetachTypedLinkRequest.make ~directoryArn
              ~typedLinkSpecifier:(Values.TypedLinkSpecifier.of_json
                                     typedLinkSpecifier) ()) None None])
let disable_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_directory
           (Values.DisableDirectoryRequest.make ~directoryArn ())
           (Some Values.DisableDirectoryResponse.to_json)
           (Some Values.DisableDirectoryResponse.error_to_json)])
let enable_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_directory
           (Values.EnableDirectoryRequest.make ~directoryArn ())
           (Some Values.EnableDirectoryResponse.to_json)
           (Some Values.EnableDirectoryResponse.error_to_json)])
let get_applied_schema_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_applied_schema_version
           (Values.GetAppliedSchemaVersionRequest.make ~schemaArn ())
           (Some Values.GetAppliedSchemaVersionResponse.to_json)
           (Some Values.GetAppliedSchemaVersionResponse.error_to_json)])
let get_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING DirectoryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_directory
           (Values.GetDirectoryRequest.make ~directoryArn ())
           (Some Values.GetDirectoryResponse.to_json)
           (Some Values.GetDirectoryResponse.error_to_json)])
let get_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING FacetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_facet (Values.GetFacetRequest.make ~schemaArn ~name ())
           (Some Values.GetFacetResponse.to_json)
           (Some Values.GetFacetResponse.error_to_json)])
let get_link_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and typedLinkSpecifier =
         flag "typed-link-specifier" (required json_arg)
           ~doc:"JSON TypedLinkSpecifier"
       and attributeNames =
         flag "attribute-names" (required json_arg)
           ~doc:"JSON AttributeNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_link_attributes
           (Values.GetLinkAttributesRequest.make
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~typedLinkSpecifier:(Values.TypedLinkSpecifier.of_json
                                     typedLinkSpecifier)
              ~attributeNames:(Values.AttributeNameList.of_json
                                 attributeNames) ())
           (Some Values.GetLinkAttributesResponse.to_json)
           (Some Values.GetLinkAttributesResponse.error_to_json)])
let get_object_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and schemaFacet =
         flag "schema-facet" (required json_arg) ~doc:"JSON SchemaFacet"
       and attributeNames =
         flag "attribute-names" (required json_arg)
           ~doc:"JSON AttributeNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_attributes
           (Values.GetObjectAttributesRequest.make
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference)
              ~schemaFacet:(Values.SchemaFacet.of_json schemaFacet)
              ~attributeNames:(Values.AttributeNameList.of_json
                                 attributeNames) ())
           (Some Values.GetObjectAttributesResponse.to_json)
           (Some Values.GetObjectAttributesResponse.error_to_json)])
let get_object_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_information
           (Values.GetObjectInformationRequest.make
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.GetObjectInformationResponse.to_json)
           (Some Values.GetObjectInformationResponse.error_to_json)])
let get_schema_as_json =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema_as_json
           (Values.GetSchemaAsJsonRequest.make ~schemaArn ())
           (Some Values.GetSchemaAsJsonResponse.to_json)
           (Some Values.GetSchemaAsJsonResponse.error_to_json)])
let get_typed_link_facet_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING TypedLinkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_typed_link_facet_information
           (Values.GetTypedLinkFacetInformationRequest.make ~schemaArn ~name
              ()) (Some Values.GetTypedLinkFacetInformationResponse.to_json)
           (Some Values.GetTypedLinkFacetInformationResponse.error_to_json)])
let list_applied_schema_arns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applied_schema_arns
           (Values.ListAppliedSchemaArnsRequest.make ?schemaArn ?nextToken
              ?maxResults ~directoryArn ())
           (Some Values.ListAppliedSchemaArnsResponse.to_json)
           (Some Values.ListAppliedSchemaArnsResponse.error_to_json)])
let list_attached_indices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and targetReference =
         flag "target-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attached_indices
           (Values.ListAttachedIndicesRequest.make ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~targetReference:(Values.ObjectReference.of_json
                                  targetReference) ())
           (Some Values.ListAttachedIndicesResponse.to_json)
           (Some Values.ListAttachedIndicesResponse.error_to_json)])
let list_development_schema_arns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_development_schema_arns
           (Values.ListDevelopmentSchemaArnsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListDevelopmentSchemaArnsResponse.to_json)
           (Some Values.ListDevelopmentSchemaArnsResponse.error_to_json)])
let list_directories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON DirectoryState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_directories
           (Values.ListDirectoriesRequest.make ?nextToken ?maxResults
              ?state:(Option.map ~f:Values.DirectoryState.of_json state) ())
           (Some Values.ListDirectoriesResponse.to_json)
           (Some Values.ListDirectoriesResponse.error_to_json)])
let list_facet_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING FacetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_facet_attributes
           (Values.ListFacetAttributesRequest.make ?nextToken ?maxResults
              ~schemaArn ~name ())
           (Some Values.ListFacetAttributesResponse.to_json)
           (Some Values.ListFacetAttributesResponse.error_to_json)])
let list_facet_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_facet_names
           (Values.ListFacetNamesRequest.make ?nextToken ?maxResults
              ~schemaArn ()) (Some Values.ListFacetNamesResponse.to_json)
           (Some Values.ListFacetNamesResponse.error_to_json)])
let list_incoming_typed_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterAttributeRanges =
         flag "filter-attribute-ranges" (optional json_arg)
           ~doc:"JSON TypedLinkAttributeRangeList"
       and filterTypedLink =
         flag "filter-typed-link" (optional json_arg)
           ~doc:"JSON TypedLinkSchemaAndFacetName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_incoming_typed_links
           (Values.ListIncomingTypedLinksRequest.make
              ?filterAttributeRanges:(Option.map
                                        ~f:Values.TypedLinkAttributeRangeList.of_json
                                        filterAttributeRanges)
              ?filterTypedLink:(Option.map
                                  ~f:Values.TypedLinkSchemaAndFacetName.of_json
                                  filterTypedLink) ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListIncomingTypedLinksResponse.to_json)
           (Some Values.ListIncomingTypedLinksResponse.error_to_json)])
let list_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rangesOnIndexedValues =
         flag "ranges-on-indexed-values" (optional json_arg)
           ~doc:"JSON ObjectAttributeRangeList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and indexReference =
         flag "index-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_index
           (Values.ListIndexRequest.make
              ?rangesOnIndexedValues:(Option.map
                                        ~f:Values.ObjectAttributeRangeList.of_json
                                        rangesOnIndexedValues) ?maxResults
              ?nextToken
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~indexReference:(Values.ObjectReference.of_json indexReference)
              ()) (Some Values.ListIndexResponse.to_json)
           (Some Values.ListIndexResponse.error_to_json)])
let list_managed_schema_arns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_schema_arns
           (Values.ListManagedSchemaArnsRequest.make ?schemaArn ?nextToken
              ?maxResults ())
           (Some Values.ListManagedSchemaArnsResponse.to_json)
           (Some Values.ListManagedSchemaArnsResponse.error_to_json)])
let list_object_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and facetFilter =
         flag "facet-filter" (optional json_arg) ~doc:"JSON SchemaFacet"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_object_attributes
           (Values.ListObjectAttributesRequest.make ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel)
              ?facetFilter:(Option.map ~f:Values.SchemaFacet.of_json
                              facetFilter) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListObjectAttributesResponse.to_json)
           (Some Values.ListObjectAttributesResponse.error_to_json)])
let list_object_children =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_object_children
           (Values.ListObjectChildrenRequest.make ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListObjectChildrenResponse.to_json)
           (Some Values.ListObjectChildrenResponse.error_to_json)])
let list_object_parent_paths =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_object_parent_paths
           (Values.ListObjectParentPathsRequest.make ?nextToken ?maxResults
              ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListObjectParentPathsResponse.to_json)
           (Some Values.ListObjectParentPathsResponse.error_to_json)])
let list_object_parents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and includeAllLinksToEachParent =
         flag "include-all-links-to-each-parent" (optional bool)
           ~doc:"BOOL Bool"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_object_parents
           (Values.ListObjectParentsRequest.make ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel)
              ?includeAllLinksToEachParent ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListObjectParentsResponse.to_json)
           (Some Values.ListObjectParentsResponse.error_to_json)])
let list_object_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_object_policies
           (Values.ListObjectPoliciesRequest.make ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListObjectPoliciesResponse.to_json)
           (Some Values.ListObjectPoliciesResponse.error_to_json)])
let list_outgoing_typed_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterAttributeRanges =
         flag "filter-attribute-ranges" (optional json_arg)
           ~doc:"JSON TypedLinkAttributeRangeList"
       and filterTypedLink =
         flag "filter-typed-link" (optional json_arg)
           ~doc:"JSON TypedLinkSchemaAndFacetName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_outgoing_typed_links
           (Values.ListOutgoingTypedLinksRequest.make
              ?filterAttributeRanges:(Option.map
                                        ~f:Values.TypedLinkAttributeRangeList.of_json
                                        filterAttributeRanges)
              ?filterTypedLink:(Option.map
                                  ~f:Values.TypedLinkSchemaAndFacetName.of_json
                                  filterTypedLink) ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.ListOutgoingTypedLinksResponse.to_json)
           (Some Values.ListOutgoingTypedLinksResponse.error_to_json)])
let list_policy_attachments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and consistencyLevel =
         flag "consistency-level" (optional json_arg)
           ~doc:"JSON ConsistencyLevel"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and policyReference =
         flag "policy-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_attachments
           (Values.ListPolicyAttachmentsRequest.make ?nextToken ?maxResults
              ?consistencyLevel:(Option.map
                                   ~f:Values.ConsistencyLevel.of_json
                                   consistencyLevel) ~directoryArn
              ~policyReference:(Values.ObjectReference.of_json
                                  policyReference) ())
           (Some Values.ListPolicyAttachmentsResponse.to_json)
           (Some Values.ListPolicyAttachmentsResponse.error_to_json)])
let list_published_schema_arns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_published_schema_arns
           (Values.ListPublishedSchemaArnsRequest.make ?schemaArn ?nextToken
              ?maxResults ())
           (Some Values.ListPublishedSchemaArnsResponse.to_json)
           (Some Values.ListPublishedSchemaArnsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT TagsNumberResults"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ?maxResults
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_typed_link_facet_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING TypedLinkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_typed_link_facet_attributes
           (Values.ListTypedLinkFacetAttributesRequest.make ?nextToken
              ?maxResults ~schemaArn ~name ())
           (Some Values.ListTypedLinkFacetAttributesResponse.to_json)
           (Some Values.ListTypedLinkFacetAttributesResponse.error_to_json)])
let list_typed_link_facet_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_typed_link_facet_names
           (Values.ListTypedLinkFacetNamesRequest.make ?nextToken ?maxResults
              ~schemaArn ())
           (Some Values.ListTypedLinkFacetNamesResponse.to_json)
           (Some Values.ListTypedLinkFacetNamesResponse.error_to_json)])
let lookup_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NumberResults"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.lookup_policy
           (Values.LookupPolicyRequest.make ?nextToken ?maxResults
              ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.LookupPolicyResponse.to_json)
           (Some Values.LookupPolicyResponse.error_to_json)])
let publish_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and minorVersion =
         flag "minor-version" (optional string) ~doc:"STRING Version"
       and name = flag "name" (optional string) ~doc:"STRING SchemaName"
       and developmentSchemaArn =
         flag "development-schema-arn" (required string) ~doc:"STRING Arn"
       and version = flag "version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_schema
           (Values.PublishSchemaRequest.make ?minorVersion ?name
              ~developmentSchemaArn ~version ())
           (Some Values.PublishSchemaResponse.to_json)
           (Some Values.PublishSchemaResponse.error_to_json)])
let put_schema_from_json =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and document =
         flag "document" (required string) ~doc:"STRING SchemaJsonDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_schema_from_json
           (Values.PutSchemaFromJsonRequest.make ~schemaArn ~document ())
           (Some Values.PutSchemaFromJsonResponse.to_json)
           (Some Values.PutSchemaFromJsonResponse.error_to_json)])
let remove_facet_from_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and schemaFacet =
         flag "schema-facet" (required json_arg) ~doc:"JSON SchemaFacet"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_facet_from_object
           (Values.RemoveFacetFromObjectRequest.make ~directoryArn
              ~schemaFacet:(Values.SchemaFacet.of_json schemaFacet)
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference) ())
           (Some Values.RemoveFacetFromObjectResponse.to_json)
           (Some Values.RemoveFacetFromObjectResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeUpdates =
         flag "attribute-updates" (optional json_arg)
           ~doc:"JSON FacetAttributeUpdateList"
       and objectType =
         flag "object-type" (optional json_arg) ~doc:"JSON ObjectType"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING FacetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_facet
           (Values.UpdateFacetRequest.make
              ?attributeUpdates:(Option.map
                                   ~f:Values.FacetAttributeUpdateList.of_json
                                   attributeUpdates)
              ?objectType:(Option.map ~f:Values.ObjectType.of_json objectType)
              ~schemaArn ~name ()) (Some Values.UpdateFacetResponse.to_json)
           (Some Values.UpdateFacetResponse.error_to_json)])
let update_link_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and typedLinkSpecifier =
         flag "typed-link-specifier" (required json_arg)
           ~doc:"JSON TypedLinkSpecifier"
       and attributeUpdates =
         flag "attribute-updates" (required json_arg)
           ~doc:"JSON LinkAttributeUpdateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_link_attributes
           (Values.UpdateLinkAttributesRequest.make ~directoryArn
              ~typedLinkSpecifier:(Values.TypedLinkSpecifier.of_json
                                     typedLinkSpecifier)
              ~attributeUpdates:(Values.LinkAttributeUpdateList.of_json
                                   attributeUpdates) ())
           (Some Values.UpdateLinkAttributesResponse.to_json)
           (Some Values.UpdateLinkAttributesResponse.error_to_json)])
let update_object_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn"
       and objectReference =
         flag "object-reference" (required json_arg)
           ~doc:"JSON ObjectReference"
       and attributeUpdates =
         flag "attribute-updates" (required json_arg)
           ~doc:"JSON ObjectAttributeUpdateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_object_attributes
           (Values.UpdateObjectAttributesRequest.make ~directoryArn
              ~objectReference:(Values.ObjectReference.of_json
                                  objectReference)
              ~attributeUpdates:(Values.ObjectAttributeUpdateList.of_json
                                   attributeUpdates) ())
           (Some Values.UpdateObjectAttributesResponse.to_json)
           (Some Values.UpdateObjectAttributesResponse.error_to_json)])
let update_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING SchemaName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_schema
           (Values.UpdateSchemaRequest.make ~schemaArn ~name ())
           (Some Values.UpdateSchemaResponse.to_json)
           (Some Values.UpdateSchemaResponse.error_to_json)])
let update_typed_link_facet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING TypedLinkName"
       and attributeUpdates =
         flag "attribute-updates" (required json_arg)
           ~doc:"JSON TypedLinkFacetAttributeUpdateList"
       and identityAttributeOrder =
         flag "identity-attribute-order" (required json_arg)
           ~doc:"JSON AttributeNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_typed_link_facet
           (Values.UpdateTypedLinkFacetRequest.make ~schemaArn ~name
              ~attributeUpdates:(Values.TypedLinkFacetAttributeUpdateList.of_json
                                   attributeUpdates)
              ~identityAttributeOrder:(Values.AttributeNameList.of_json
                                         identityAttributeOrder) ())
           (Some Values.UpdateTypedLinkFacetResponse.to_json)
           (Some Values.UpdateTypedLinkFacetResponse.error_to_json)])
let upgrade_applied_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Bool"
       and publishedSchemaArn =
         flag "published-schema-arn" (required string) ~doc:"STRING Arn"
       and directoryArn =
         flag "directory-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upgrade_applied_schema
           (Values.UpgradeAppliedSchemaRequest.make ?dryRun
              ~publishedSchemaArn ~directoryArn ())
           (Some Values.UpgradeAppliedSchemaResponse.to_json)
           (Some Values.UpgradeAppliedSchemaResponse.error_to_json)])
let upgrade_published_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Bool"
       and developmentSchemaArn =
         flag "development-schema-arn" (required string) ~doc:"STRING Arn"
       and publishedSchemaArn =
         flag "published-schema-arn" (required string) ~doc:"STRING Arn"
       and minorVersion =
         flag "minor-version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upgrade_published_schema
           (Values.UpgradePublishedSchemaRequest.make ?dryRun
              ~developmentSchemaArn ~publishedSchemaArn ~minorVersion ())
           (Some Values.UpgradePublishedSchemaResponse.to_json)
           (Some Values.UpgradePublishedSchemaResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-facet-to-object", add_facet_to_object);
    ("apply-schema", apply_schema);
    ("attach-object", attach_object);
    ("attach-policy", attach_policy);
    ("attach-to-index", attach_to_index);
    ("attach-typed-link", attach_typed_link);
    ("batch-read", batch_read);
    ("batch-write", batch_write);
    ("create-directory", create_directory);
    ("create-facet", create_facet);
    ("create-index", create_index);
    ("create-object", create_object);
    ("create-schema", create_schema);
    ("create-typed-link-facet", create_typed_link_facet);
    ("delete-directory", delete_directory);
    ("delete-facet", delete_facet);
    ("delete-object", delete_object);
    ("delete-schema", delete_schema);
    ("delete-typed-link-facet", delete_typed_link_facet);
    ("detach-from-index", detach_from_index);
    ("detach-object", detach_object);
    ("detach-policy", detach_policy);
    ("detach-typed-link", detach_typed_link);
    ("disable-directory", disable_directory);
    ("enable-directory", enable_directory);
    ("get-applied-schema-version", get_applied_schema_version);
    ("get-directory", get_directory);
    ("get-facet", get_facet);
    ("get-link-attributes", get_link_attributes);
    ("get-object-attributes", get_object_attributes);
    ("get-object-information", get_object_information);
    ("get-schema-as-json", get_schema_as_json);
    ("get-typed-link-facet-information", get_typed_link_facet_information);
    ("list-applied-schema-arns", list_applied_schema_arns);
    ("list-attached-indices", list_attached_indices);
    ("list-development-schema-arns", list_development_schema_arns);
    ("list-directories", list_directories);
    ("list-facet-attributes", list_facet_attributes);
    ("list-facet-names", list_facet_names);
    ("list-incoming-typed-links", list_incoming_typed_links);
    ("list-index", list_index);
    ("list-managed-schema-arns", list_managed_schema_arns);
    ("list-object-attributes", list_object_attributes);
    ("list-object-children", list_object_children);
    ("list-object-parent-paths", list_object_parent_paths);
    ("list-object-parents", list_object_parents);
    ("list-object-policies", list_object_policies);
    ("list-outgoing-typed-links", list_outgoing_typed_links);
    ("list-policy-attachments", list_policy_attachments);
    ("list-published-schema-arns", list_published_schema_arns);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-typed-link-facet-attributes", list_typed_link_facet_attributes);
    ("list-typed-link-facet-names", list_typed_link_facet_names);
    ("lookup-policy", lookup_policy);
    ("publish-schema", publish_schema);
    ("put-schema-from-json", put_schema_from_json);
    ("remove-facet-from-object", remove_facet_from_object);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-facet", update_facet);
    ("update-link-attributes", update_link_attributes);
    ("update-object-attributes", update_object_attributes);
    ("update-schema", update_schema);
    ("update-typed-link-facet", update_typed_link_facet);
    ("upgrade-applied-schema", upgrade_applied_schema);
    ("upgrade-published-schema", upgrade_published_schema)]
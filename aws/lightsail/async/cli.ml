(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let allocate_static_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and staticIpName =
         flag "static-ip-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.allocate_static_ip
           (Values.AllocateStaticIpRequest.make ~staticIpName ())
           (Some Values.AllocateStaticIpResult.to_json)
           (Some Values.AllocateStaticIpResult.error_to_json)])
let attach_certificate_to_distribution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (required string)
           ~doc:"STRING ResourceName"
       and certificateName =
         flag "certificate-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_certificate_to_distribution
           (Values.AttachCertificateToDistributionRequest.make
              ~distributionName ~certificateName ())
           (Some Values.AttachCertificateToDistributionResult.to_json)
           (Some Values.AttachCertificateToDistributionResult.error_to_json)])
let attach_disk =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskName =
         flag "disk-name" (required string) ~doc:"STRING ResourceName"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName"
       and diskPath =
         flag "disk-path" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_disk
           (Values.AttachDiskRequest.make ~diskName ~instanceName ~diskPath
              ()) (Some Values.AttachDiskResult.to_json)
           (Some Values.AttachDiskResult.error_to_json)])
let attach_instances_to_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and instanceNames =
         flag "instance-names" (required json_arg)
           ~doc:"JSON ResourceNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_instances_to_load_balancer
           (Values.AttachInstancesToLoadBalancerRequest.make
              ~loadBalancerName
              ~instanceNames:(Values.ResourceNameList.of_json instanceNames)
              ()) (Some Values.AttachInstancesToLoadBalancerResult.to_json)
           (Some Values.AttachInstancesToLoadBalancerResult.error_to_json)])
let attach_load_balancer_tls_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and certificateName =
         flag "certificate-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_load_balancer_tls_certificate
           (Values.AttachLoadBalancerTlsCertificateRequest.make
              ~loadBalancerName ~certificateName ())
           (Some Values.AttachLoadBalancerTlsCertificateResult.to_json)
           (Some Values.AttachLoadBalancerTlsCertificateResult.error_to_json)])
let attach_static_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and staticIpName =
         flag "static-ip-name" (required string) ~doc:"STRING ResourceName"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_static_ip
           (Values.AttachStaticIpRequest.make ~staticIpName ~instanceName ())
           (Some Values.AttachStaticIpResult.to_json)
           (Some Values.AttachStaticIpResult.error_to_json)])
let close_instance_public_ports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portInfo =
         flag "port-info" (required json_arg) ~doc:"JSON PortInfo"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.close_instance_public_ports
           (Values.CloseInstancePublicPortsRequest.make
              ~portInfo:(Values.PortInfo.of_json portInfo) ~instanceName ())
           (Some Values.CloseInstancePublicPortsResult.to_json)
           (Some Values.CloseInstancePublicPortsResult.error_to_json)])
let copy_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceSnapshotName =
         flag "source-snapshot-name" (optional string)
           ~doc:"STRING ResourceName"
       and sourceResourceName =
         flag "source-resource-name" (optional string) ~doc:"STRING string"
       and restoreDate =
         flag "restore-date" (optional string) ~doc:"STRING string"
       and useLatestRestorableAutoSnapshot =
         flag "use-latest-restorable-auto-snapshot" (optional bool)
           ~doc:"BOOL boolean"
       and targetSnapshotName =
         flag "target-snapshot-name" (required string)
           ~doc:"STRING ResourceName"
       and sourceRegion =
         flag "source-region" (required json_arg) ~doc:"JSON RegionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_snapshot
           (Values.CopySnapshotRequest.make ?sourceSnapshotName
              ?sourceResourceName ?restoreDate
              ?useLatestRestorableAutoSnapshot ~targetSnapshotName
              ~sourceRegion:(Values.RegionName.of_json sourceRegion) ())
           (Some Values.CopySnapshotResult.to_json)
           (Some Values.CopySnapshotResult.error_to_json)])
let create_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and enableObjectVersioning =
         flag "enable-object-versioning" (optional bool) ~doc:"BOOL boolean"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bucket
           (Values.CreateBucketRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?enableObjectVersioning ~bucketName ~bundleId ())
           (Some Values.CreateBucketResult.to_json)
           (Some Values.CreateBucketResult.error_to_json)])
let create_bucket_access_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bucket_access_key
           (Values.CreateBucketAccessKeyRequest.make ~bucketName ())
           (Some Values.CreateBucketAccessKeyResult.to_json)
           (Some Values.CreateBucketAccessKeyResult.error_to_json)])
let create_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subjectAlternativeNames =
         flag "subject-alternative-names" (optional json_arg)
           ~doc:"JSON SubjectAlternativeNameList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and certificateName =
         flag "certificate-name" (required string)
           ~doc:"STRING CertificateName"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_certificate
           (Values.CreateCertificateRequest.make
              ?subjectAlternativeNames:(Option.map
                                          ~f:Values.SubjectAlternativeNameList.of_json
                                          subjectAlternativeNames)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~certificateName ~domainName ())
           (Some Values.CreateCertificateResult.to_json)
           (Some Values.CreateCertificateResult.error_to_json)])
let create_cloud_formation_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instances =
         flag "instances" (required json_arg) ~doc:"JSON InstanceEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cloud_formation_stack
           (Values.CreateCloudFormationStackRequest.make
              ~instances:(Values.InstanceEntryList.of_json instances) ())
           (Some Values.CreateCloudFormationStackResult.to_json)
           (Some Values.CreateCloudFormationStackResult.error_to_json)])
let create_contact_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON ContactProtocol"
       and contactEndpoint =
         flag "contact-endpoint" (required string) ~doc:"STRING StringMax256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact_method
           (Values.CreateContactMethodRequest.make
              ~protocol:(Values.ContactProtocol.of_json protocol)
              ~contactEndpoint ())
           (Some Values.CreateContactMethodResult.to_json)
           (Some Values.CreateContactMethodResult.error_to_json)])
let create_container_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and publicDomainNames =
         flag "public-domain-names" (optional json_arg)
           ~doc:"JSON ContainerServicePublicDomains"
       and deployment =
         flag "deployment" (optional json_arg)
           ~doc:"JSON ContainerServiceDeploymentRequest"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName"
       and power =
         flag "power" (required json_arg)
           ~doc:"JSON ContainerServicePowerName"
       and scale =
         flag "scale" (required int) ~doc:"INT ContainerServiceScale" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_container_service
           (Values.CreateContainerServiceRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?publicDomainNames:(Option.map
                                    ~f:Values.ContainerServicePublicDomains.of_json
                                    publicDomainNames)
              ?deployment:(Option.map
                             ~f:Values.ContainerServiceDeploymentRequest.of_json
                             deployment) ~serviceName
              ~power:(Values.ContainerServicePowerName.of_json power) ~scale
              ()) (Some Values.CreateContainerServiceResult.to_json)
           (Some Values.CreateContainerServiceResult.error_to_json)])
let create_container_service_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containers =
         flag "containers" (optional json_arg) ~doc:"JSON ContainerMap"
       and publicEndpoint =
         flag "public-endpoint" (optional json_arg)
           ~doc:"JSON EndpointRequest"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_container_service_deployment
           (Values.CreateContainerServiceDeploymentRequest.make
              ?containers:(Option.map ~f:Values.ContainerMap.of_json
                             containers)
              ?publicEndpoint:(Option.map ~f:Values.EndpointRequest.of_json
                                 publicEndpoint) ~serviceName ())
           (Some Values.CreateContainerServiceDeploymentResult.to_json)
           (Some Values.CreateContainerServiceDeploymentResult.error_to_json)])
let create_container_service_registry_login =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_container_service_registry_login
           (Values.CreateContainerServiceRegistryLoginRequest.make ())
           (Some Values.CreateContainerServiceRegistryLoginResult.to_json)
           (Some
              Values.CreateContainerServiceRegistryLoginResult.error_to_json)])
let create_disk =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addOns =
         flag "add-ons" (optional json_arg) ~doc:"JSON AddOnRequestList"
       and diskName =
         flag "disk-name" (required string) ~doc:"STRING ResourceName"
       and availabilityZone =
         flag "availability-zone" (required string)
           ~doc:"STRING NonEmptyString"
       and sizeInGb = flag "size-in-gb" (required int) ~doc:"INT integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_disk
           (Values.CreateDiskRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?addOns:(Option.map ~f:Values.AddOnRequestList.of_json addOns)
              ~diskName ~availabilityZone ~sizeInGb ())
           (Some Values.CreateDiskResult.to_json)
           (Some Values.CreateDiskResult.error_to_json)])
let create_disk_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskSnapshotName =
         flag "disk-snapshot-name" (optional string)
           ~doc:"STRING ResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addOns =
         flag "add-ons" (optional json_arg) ~doc:"JSON AddOnRequestList"
       and sourceDiskName =
         flag "source-disk-name" (optional string) ~doc:"STRING string"
       and restoreDate =
         flag "restore-date" (optional string) ~doc:"STRING string"
       and useLatestRestorableAutoSnapshot =
         flag "use-latest-restorable-auto-snapshot" (optional bool)
           ~doc:"BOOL boolean"
       and diskName =
         flag "disk-name" (required string) ~doc:"STRING ResourceName"
       and availabilityZone =
         flag "availability-zone" (required string)
           ~doc:"STRING NonEmptyString"
       and sizeInGb = flag "size-in-gb" (required int) ~doc:"INT integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_disk_from_snapshot
           (Values.CreateDiskFromSnapshotRequest.make ?diskSnapshotName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?addOns:(Option.map ~f:Values.AddOnRequestList.of_json addOns)
              ?sourceDiskName ?restoreDate ?useLatestRestorableAutoSnapshot
              ~diskName ~availabilityZone ~sizeInGb ())
           (Some Values.CreateDiskFromSnapshotResult.to_json)
           (Some Values.CreateDiskFromSnapshotResult.error_to_json)])
let create_disk_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskName =
         flag "disk-name" (optional string) ~doc:"STRING ResourceName"
       and instanceName =
         flag "instance-name" (optional string) ~doc:"STRING ResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and diskSnapshotName =
         flag "disk-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_disk_snapshot
           (Values.CreateDiskSnapshotRequest.make ?diskName ?instanceName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~diskSnapshotName ())
           (Some Values.CreateDiskSnapshotResult.to_json)
           (Some Values.CreateDiskSnapshotResult.error_to_json)])
let create_distribution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheBehaviorSettings =
         flag "cache-behavior-settings" (optional json_arg)
           ~doc:"JSON CacheSettings"
       and cacheBehaviors =
         flag "cache-behaviors" (optional json_arg)
           ~doc:"JSON CacheBehaviorList"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and distributionName =
         flag "distribution-name" (required string)
           ~doc:"STRING ResourceName"
       and origin = flag "origin" (required json_arg) ~doc:"JSON InputOrigin"
       and defaultCacheBehavior =
         flag "default-cache-behavior" (required json_arg)
           ~doc:"JSON CacheBehavior"
       and bundleId = flag "bundle-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_distribution
           (Values.CreateDistributionRequest.make
              ?cacheBehaviorSettings:(Option.map
                                        ~f:Values.CacheSettings.of_json
                                        cacheBehaviorSettings)
              ?cacheBehaviors:(Option.map ~f:Values.CacheBehaviorList.of_json
                                 cacheBehaviors)
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~distributionName ~origin:(Values.InputOrigin.of_json origin)
              ~defaultCacheBehavior:(Values.CacheBehavior.of_json
                                       defaultCacheBehavior) ~bundleId ())
           (Some Values.CreateDistributionResult.to_json)
           (Some Values.CreateDistributionResult.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~domainName
              ()) (Some Values.CreateDomainResult.to_json)
           (Some Values.CreateDomainResult.error_to_json)])
let create_domain_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and domainEntry =
         flag "domain-entry" (required json_arg) ~doc:"JSON DomainEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_entry
           (Values.CreateDomainEntryRequest.make ~domainName
              ~domainEntry:(Values.DomainEntry.of_json domainEntry) ())
           (Some Values.CreateDomainEntryResult.to_json)
           (Some Values.CreateDomainEntryResult.error_to_json)])
let create_instance_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and instanceSnapshotName =
         flag "instance-snapshot-name" (required string)
           ~doc:"STRING ResourceName"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance_snapshot
           (Values.CreateInstanceSnapshotRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~instanceSnapshotName ~instanceName ())
           (Some Values.CreateInstanceSnapshotResult.to_json)
           (Some Values.CreateInstanceSnapshotResult.error_to_json)])
let create_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customImageName =
         flag "custom-image-name" (optional string)
           ~doc:"STRING ResourceName"
       and userData = flag "user-data" (optional string) ~doc:"STRING string"
       and keyPairName =
         flag "key-pair-name" (optional string) ~doc:"STRING ResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addOns =
         flag "add-ons" (optional json_arg) ~doc:"JSON AddOnRequestList"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and instanceNames =
         flag "instance-names" (required json_arg) ~doc:"JSON StringList"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING string"
       and blueprintId =
         flag "blueprint-id" (required string) ~doc:"STRING NonEmptyString"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instances
           (Values.CreateInstancesRequest.make ?customImageName ?userData
              ?keyPairName ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?addOns:(Option.map ~f:Values.AddOnRequestList.of_json addOns)
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType)
              ~instanceNames:(Values.StringList.of_json instanceNames)
              ~availabilityZone ~blueprintId ~bundleId ())
           (Some Values.CreateInstancesResult.to_json)
           (Some Values.CreateInstancesResult.error_to_json)])
let create_instances_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachedDiskMapping =
         flag "attached-disk-mapping" (optional json_arg)
           ~doc:"JSON AttachedDiskMap"
       and instanceSnapshotName =
         flag "instance-snapshot-name" (optional string)
           ~doc:"STRING ResourceName"
       and userData = flag "user-data" (optional string) ~doc:"STRING string"
       and keyPairName =
         flag "key-pair-name" (optional string) ~doc:"STRING ResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addOns =
         flag "add-ons" (optional json_arg) ~doc:"JSON AddOnRequestList"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and sourceInstanceName =
         flag "source-instance-name" (optional string) ~doc:"STRING string"
       and restoreDate =
         flag "restore-date" (optional string) ~doc:"STRING string"
       and useLatestRestorableAutoSnapshot =
         flag "use-latest-restorable-auto-snapshot" (optional bool)
           ~doc:"BOOL boolean"
       and instanceNames =
         flag "instance-names" (required json_arg) ~doc:"JSON StringList"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING string"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instances_from_snapshot
           (Values.CreateInstancesFromSnapshotRequest.make
              ?attachedDiskMapping:(Option.map
                                      ~f:Values.AttachedDiskMap.of_json
                                      attachedDiskMapping)
              ?instanceSnapshotName ?userData ?keyPairName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?addOns:(Option.map ~f:Values.AddOnRequestList.of_json addOns)
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?sourceInstanceName
              ?restoreDate ?useLatestRestorableAutoSnapshot
              ~instanceNames:(Values.StringList.of_json instanceNames)
              ~availabilityZone ~bundleId ())
           (Some Values.CreateInstancesFromSnapshotResult.to_json)
           (Some Values.CreateInstancesFromSnapshotResult.error_to_json)])
let create_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and keyPairName =
         flag "key-pair-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_key_pair
           (Values.CreateKeyPairRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~keyPairName
              ()) (Some Values.CreateKeyPairResult.to_json)
           (Some Values.CreateKeyPairResult.error_to_json)])
let create_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckPath =
         flag "health-check-path" (optional string) ~doc:"STRING string"
       and certificateName =
         flag "certificate-name" (optional string) ~doc:"STRING ResourceName"
       and certificateDomainName =
         flag "certificate-domain-name" (optional string)
           ~doc:"STRING DomainName"
       and certificateAlternativeNames =
         flag "certificate-alternative-names" (optional json_arg)
           ~doc:"JSON DomainNameList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and instancePort = flag "instance-port" (required int) ~doc:"INT Port" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_load_balancer
           (Values.CreateLoadBalancerRequest.make ?healthCheckPath
              ?certificateName ?certificateDomainName
              ?certificateAlternativeNames:(Option.map
                                              ~f:Values.DomainNameList.of_json
                                              certificateAlternativeNames)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ~loadBalancerName
              ~instancePort ())
           (Some Values.CreateLoadBalancerResult.to_json)
           (Some Values.CreateLoadBalancerResult.error_to_json)])
let create_load_balancer_tls_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAlternativeNames =
         flag "certificate-alternative-names" (optional json_arg)
           ~doc:"JSON DomainNameList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and certificateName =
         flag "certificate-name" (required string) ~doc:"STRING ResourceName"
       and certificateDomainName =
         flag "certificate-domain-name" (required string)
           ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_load_balancer_tls_certificate
           (Values.CreateLoadBalancerTlsCertificateRequest.make
              ?certificateAlternativeNames:(Option.map
                                              ~f:Values.DomainNameList.of_json
                                              certificateAlternativeNames)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~loadBalancerName ~certificateName ~certificateDomainName ())
           (Some Values.CreateLoadBalancerTlsCertificateResult.to_json)
           (Some Values.CreateLoadBalancerTlsCertificateResult.error_to_json)])
let create_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING string"
       and masterUserPassword =
         flag "master-user-password" (optional string)
           ~doc:"STRING SensitiveString"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING string"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING string"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName"
       and relationalDatabaseBlueprintId =
         flag "relational-database-blueprint-id" (required string)
           ~doc:"STRING string"
       and relationalDatabaseBundleId =
         flag "relational-database-bundle-id" (required string)
           ~doc:"STRING string"
       and masterDatabaseName =
         flag "master-database-name" (required string) ~doc:"STRING string"
       and masterUsername =
         flag "master-username" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_relational_database
           (Values.CreateRelationalDatabaseRequest.make ?availabilityZone
              ?masterUserPassword ?preferredBackupWindow
              ?preferredMaintenanceWindow ?publiclyAccessible
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~relationalDatabaseName ~relationalDatabaseBlueprintId
              ~relationalDatabaseBundleId ~masterDatabaseName ~masterUsername
              ()) (Some Values.CreateRelationalDatabaseResult.to_json)
           (Some Values.CreateRelationalDatabaseResult.error_to_json)])
let create_relational_database_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING string"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL boolean"
       and relationalDatabaseSnapshotName =
         flag "relational-database-snapshot-name" (optional string)
           ~doc:"STRING ResourceName"
       and relationalDatabaseBundleId =
         flag "relational-database-bundle-id" (optional string)
           ~doc:"STRING string"
       and sourceRelationalDatabaseName =
         flag "source-relational-database-name" (optional string)
           ~doc:"STRING ResourceName"
       and restoreTime =
         flag "restore-time" (optional json_arg) ~doc:"JSON IsoDate"
       and useLatestRestorableTime =
         flag "use-latest-restorable-time" (optional bool)
           ~doc:"BOOL boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_relational_database_from_snapshot
           (Values.CreateRelationalDatabaseFromSnapshotRequest.make
              ?availabilityZone ?publiclyAccessible
              ?relationalDatabaseSnapshotName ?relationalDatabaseBundleId
              ?sourceRelationalDatabaseName
              ?restoreTime:(Option.map ~f:Values.IsoDate.of_json restoreTime)
              ?useLatestRestorableTime
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~relationalDatabaseName ())
           (Some Values.CreateRelationalDatabaseFromSnapshotResult.to_json)
           (Some
              Values.CreateRelationalDatabaseFromSnapshotResult.error_to_json)])
let create_relational_database_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName"
       and relationalDatabaseSnapshotName =
         flag "relational-database-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_relational_database_snapshot
           (Values.CreateRelationalDatabaseSnapshotRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~relationalDatabaseName ~relationalDatabaseSnapshotName ())
           (Some Values.CreateRelationalDatabaseSnapshotResult.to_json)
           (Some Values.CreateRelationalDatabaseSnapshotResult.error_to_json)])
let delete_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alarm (Values.DeleteAlarmRequest.make ~alarmName ())
           (Some Values.DeleteAlarmResult.to_json)
           (Some Values.DeleteAlarmResult.error_to_json)])
let delete_auto_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName"
       and date =
         flag "date" (required string) ~doc:"STRING AutoSnapshotDate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_auto_snapshot
           (Values.DeleteAutoSnapshotRequest.make ~resourceName ~date ())
           (Some Values.DeleteAutoSnapshotResult.to_json)
           (Some Values.DeleteAutoSnapshotResult.error_to_json)])
let delete_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL boolean"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket
           (Values.DeleteBucketRequest.make ?forceDelete ~bucketName ())
           (Some Values.DeleteBucketResult.to_json)
           (Some Values.DeleteBucketResult.error_to_json)])
let delete_bucket_access_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName"
       and accessKeyId =
         flag "access-key-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_access_key
           (Values.DeleteBucketAccessKeyRequest.make ~bucketName ~accessKeyId
              ()) (Some Values.DeleteBucketAccessKeyResult.to_json)
           (Some Values.DeleteBucketAccessKeyResult.error_to_json)])
let delete_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateName =
         flag "certificate-name" (required string)
           ~doc:"STRING CertificateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_certificate
           (Values.DeleteCertificateRequest.make ~certificateName ())
           (Some Values.DeleteCertificateResult.to_json)
           (Some Values.DeleteCertificateResult.error_to_json)])
let delete_contact_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON ContactProtocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact_method
           (Values.DeleteContactMethodRequest.make
              ~protocol:(Values.ContactProtocol.of_json protocol) ())
           (Some Values.DeleteContactMethodResult.to_json)
           (Some Values.DeleteContactMethodResult.error_to_json)])
let delete_container_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName"
       and image = flag "image" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_container_image
           (Values.DeleteContainerImageRequest.make ~serviceName ~image ())
           (Some Values.DeleteContainerImageResult.to_json)
           (Some Values.DeleteContainerImageResult.error_to_json)])
let delete_container_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_container_service
           (Values.DeleteContainerServiceRequest.make ~serviceName ())
           (Some Values.DeleteContainerServiceResult.to_json)
           (Some Values.DeleteContainerServiceResult.error_to_json)])
let delete_disk =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDeleteAddOns =
         flag "force-delete-add-ons" (optional bool) ~doc:"BOOL boolean"
       and diskName =
         flag "disk-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_disk
           (Values.DeleteDiskRequest.make ?forceDeleteAddOns ~diskName ())
           (Some Values.DeleteDiskResult.to_json)
           (Some Values.DeleteDiskResult.error_to_json)])
let delete_disk_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskSnapshotName =
         flag "disk-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_disk_snapshot
           (Values.DeleteDiskSnapshotRequest.make ~diskSnapshotName ())
           (Some Values.DeleteDiskSnapshotResult.to_json)
           (Some Values.DeleteDiskSnapshotResult.error_to_json)])
let delete_distribution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (optional string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_distribution
           (Values.DeleteDistributionRequest.make ?distributionName ())
           (Some Values.DeleteDistributionResult.to_json)
           (Some Values.DeleteDistributionResult.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainName ())
           (Some Values.DeleteDomainResult.to_json)
           (Some Values.DeleteDomainResult.error_to_json)])
let delete_domain_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and domainEntry =
         flag "domain-entry" (required json_arg) ~doc:"JSON DomainEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_entry
           (Values.DeleteDomainEntryRequest.make ~domainName
              ~domainEntry:(Values.DomainEntry.of_json domainEntry) ())
           (Some Values.DeleteDomainEntryResult.to_json)
           (Some Values.DeleteDomainEntryResult.error_to_json)])
let delete_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDeleteAddOns =
         flag "force-delete-add-ons" (optional bool) ~doc:"BOOL boolean"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance
           (Values.DeleteInstanceRequest.make ?forceDeleteAddOns
              ~instanceName ()) (Some Values.DeleteInstanceResult.to_json)
           (Some Values.DeleteInstanceResult.error_to_json)])
let delete_instance_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceSnapshotName =
         flag "instance-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance_snapshot
           (Values.DeleteInstanceSnapshotRequest.make ~instanceSnapshotName
              ()) (Some Values.DeleteInstanceSnapshotResult.to_json)
           (Some Values.DeleteInstanceSnapshotResult.error_to_json)])
let delete_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedFingerprint =
         flag "expected-fingerprint" (optional string) ~doc:"STRING string"
       and keyPairName =
         flag "key-pair-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_key_pair
           (Values.DeleteKeyPairRequest.make ?expectedFingerprint
              ~keyPairName ()) (Some Values.DeleteKeyPairResult.to_json)
           (Some Values.DeleteKeyPairResult.error_to_json)])
let delete_known_host_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_known_host_keys
           (Values.DeleteKnownHostKeysRequest.make ~instanceName ())
           (Some Values.DeleteKnownHostKeysResult.to_json)
           (Some Values.DeleteKnownHostKeysResult.error_to_json)])
let delete_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_load_balancer
           (Values.DeleteLoadBalancerRequest.make ~loadBalancerName ())
           (Some Values.DeleteLoadBalancerResult.to_json)
           (Some Values.DeleteLoadBalancerResult.error_to_json)])
let delete_load_balancer_tls_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL boolean"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and certificateName =
         flag "certificate-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_load_balancer_tls_certificate
           (Values.DeleteLoadBalancerTlsCertificateRequest.make ?force
              ~loadBalancerName ~certificateName ())
           (Some Values.DeleteLoadBalancerTlsCertificateResult.to_json)
           (Some Values.DeleteLoadBalancerTlsCertificateResult.error_to_json)])
let delete_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipFinalSnapshot =
         flag "skip-final-snapshot" (optional bool) ~doc:"BOOL boolean"
       and finalRelationalDatabaseSnapshotName =
         flag "final-relational-database-snapshot-name" (optional string)
           ~doc:"STRING ResourceName"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_relational_database
           (Values.DeleteRelationalDatabaseRequest.make ?skipFinalSnapshot
              ?finalRelationalDatabaseSnapshotName ~relationalDatabaseName ())
           (Some Values.DeleteRelationalDatabaseResult.to_json)
           (Some Values.DeleteRelationalDatabaseResult.error_to_json)])
let delete_relational_database_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseSnapshotName =
         flag "relational-database-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_relational_database_snapshot
           (Values.DeleteRelationalDatabaseSnapshotRequest.make
              ~relationalDatabaseSnapshotName ())
           (Some Values.DeleteRelationalDatabaseSnapshotResult.to_json)
           (Some Values.DeleteRelationalDatabaseSnapshotResult.error_to_json)])
let detach_certificate_from_distribution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_certificate_from_distribution
           (Values.DetachCertificateFromDistributionRequest.make
              ~distributionName ())
           (Some Values.DetachCertificateFromDistributionResult.to_json)
           (Some Values.DetachCertificateFromDistributionResult.error_to_json)])
let detach_disk =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskName =
         flag "disk-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_disk (Values.DetachDiskRequest.make ~diskName ())
           (Some Values.DetachDiskResult.to_json)
           (Some Values.DetachDiskResult.error_to_json)])
let detach_instances_from_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and instanceNames =
         flag "instance-names" (required json_arg)
           ~doc:"JSON ResourceNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_instances_from_load_balancer
           (Values.DetachInstancesFromLoadBalancerRequest.make
              ~loadBalancerName
              ~instanceNames:(Values.ResourceNameList.of_json instanceNames)
              ()) (Some Values.DetachInstancesFromLoadBalancerResult.to_json)
           (Some Values.DetachInstancesFromLoadBalancerResult.error_to_json)])
let detach_static_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and staticIpName =
         flag "static-ip-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_static_ip
           (Values.DetachStaticIpRequest.make ~staticIpName ())
           (Some Values.DetachStaticIpResult.to_json)
           (Some Values.DetachStaticIpResult.error_to_json)])
let disable_add_on =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addOnType =
         flag "add-on-type" (required json_arg) ~doc:"JSON AddOnType"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_add_on
           (Values.DisableAddOnRequest.make
              ~addOnType:(Values.AddOnType.of_json addOnType) ~resourceName
              ()) (Some Values.DisableAddOnResult.to_json)
           (Some Values.DisableAddOnResult.error_to_json)])
let download_default_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.download_default_key_pair
           (Values.DownloadDefaultKeyPairRequest.make ())
           (Some Values.DownloadDefaultKeyPairResult.to_json)
           (Some Values.DownloadDefaultKeyPairResult.error_to_json)])
let enable_add_on =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName"
       and addOnRequest =
         flag "add-on-request" (required json_arg) ~doc:"JSON AddOnRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_add_on
           (Values.EnableAddOnRequest.make ~resourceName
              ~addOnRequest:(Values.AddOnRequest.of_json addOnRequest) ())
           (Some Values.EnableAddOnResult.to_json)
           (Some Values.EnableAddOnResult.error_to_json)])
let export_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceSnapshotName =
         flag "source-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_snapshot
           (Values.ExportSnapshotRequest.make ~sourceSnapshotName ())
           (Some Values.ExportSnapshotResult.to_json)
           (Some Values.ExportSnapshotResult.error_to_json)])
let get_active_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_active_names
           (Values.GetActiveNamesRequest.make ?pageToken ())
           (Some Values.GetActiveNamesResult.to_json)
           (Some Values.GetActiveNamesResult.error_to_json)])
let get_alarms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmName =
         flag "alarm-name" (optional string) ~doc:"STRING ResourceName"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and monitoredResourceName =
         flag "monitored-resource-name" (optional string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_alarms
           (Values.GetAlarmsRequest.make ?alarmName ?pageToken
              ?monitoredResourceName ())
           (Some Values.GetAlarmsResult.to_json)
           (Some Values.GetAlarmsResult.error_to_json)])
let get_auto_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_auto_snapshots
           (Values.GetAutoSnapshotsRequest.make ~resourceName ())
           (Some Values.GetAutoSnapshotsResult.to_json)
           (Some Values.GetAutoSnapshotsResult.error_to_json)])
let get_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeInactive =
         flag "include-inactive" (optional bool) ~doc:"BOOL boolean"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blueprints
           (Values.GetBlueprintsRequest.make ?includeInactive ?pageToken ())
           (Some Values.GetBlueprintsResult.to_json)
           (Some Values.GetBlueprintsResult.error_to_json)])
let get_bucket_access_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_access_keys
           (Values.GetBucketAccessKeysRequest.make ~bucketName ())
           (Some Values.GetBucketAccessKeysResult.to_json)
           (Some Values.GetBucketAccessKeysResult.error_to_json)])
let get_bucket_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeInactive =
         flag "include-inactive" (optional bool) ~doc:"BOOL boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_bundles
           (Values.GetBucketBundlesRequest.make ?includeInactive ())
           (Some Values.GetBucketBundlesResult.to_json)
           (Some Values.GetBucketBundlesResult.error_to_json)])
let get_bucket_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName"
       and metricName =
         flag "metric-name" (required json_arg) ~doc:"JSON BucketMetricName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON IsoDate"
       and endTime = flag "end-time" (required json_arg) ~doc:"JSON IsoDate"
       and period = flag "period" (required int) ~doc:"INT MetricPeriod"
       and statistics =
         flag "statistics" (required json_arg)
           ~doc:"JSON MetricStatisticList"
       and unit = flag "unit" (required json_arg) ~doc:"JSON MetricUnit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_metric_data
           (Values.GetBucketMetricDataRequest.make ~bucketName
              ~metricName:(Values.BucketMetricName.of_json metricName)
              ~startTime:(Values.IsoDate.of_json startTime)
              ~endTime:(Values.IsoDate.of_json endTime) ~period
              ~statistics:(Values.MetricStatisticList.of_json statistics)
              ~unit:(Values.MetricUnit.of_json unit) ())
           (Some Values.GetBucketMetricDataResult.to_json)
           (Some Values.GetBucketMetricDataResult.error_to_json)])
let get_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (optional string) ~doc:"STRING BucketName"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and includeConnectedResources =
         flag "include-connected-resources" (optional bool)
           ~doc:"BOOL boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_buckets
           (Values.GetBucketsRequest.make ?bucketName ?pageToken
              ?includeConnectedResources ())
           (Some Values.GetBucketsResult.to_json)
           (Some Values.GetBucketsResult.error_to_json)])
let get_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeInactive =
         flag "include-inactive" (optional bool) ~doc:"BOOL boolean"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bundles
           (Values.GetBundlesRequest.make ?includeInactive ?pageToken ())
           (Some Values.GetBundlesResult.to_json)
           (Some Values.GetBundlesResult.error_to_json)])
let get_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateStatuses =
         flag "certificate-statuses" (optional json_arg)
           ~doc:"JSON CertificateStatusList"
       and includeCertificateDetails =
         flag "include-certificate-details" (optional bool)
           ~doc:"BOOL IncludeCertificateDetails"
       and certificateName =
         flag "certificate-name" (optional string)
           ~doc:"STRING CertificateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificates
           (Values.GetCertificatesRequest.make
              ?certificateStatuses:(Option.map
                                      ~f:Values.CertificateStatusList.of_json
                                      certificateStatuses)
              ?includeCertificateDetails ?certificateName ())
           (Some Values.GetCertificatesResult.to_json)
           (Some Values.GetCertificatesResult.error_to_json)])
let get_cloud_formation_stack_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cloud_formation_stack_records
           (Values.GetCloudFormationStackRecordsRequest.make ?pageToken ())
           (Some Values.GetCloudFormationStackRecordsResult.to_json)
           (Some Values.GetCloudFormationStackRecordsResult.error_to_json)])
let get_contact_methods =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocols =
         flag "protocols" (optional json_arg)
           ~doc:"JSON ContactProtocolsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact_methods
           (Values.GetContactMethodsRequest.make
              ?protocols:(Option.map ~f:Values.ContactProtocolsList.of_json
                            protocols) ())
           (Some Values.GetContactMethodsResult.to_json)
           (Some Values.GetContactMethodsResult.error_to_json)])
let get_container_a_p_i_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_a_p_i_metadata
           (Values.GetContainerAPIMetadataRequest.make ())
           (Some Values.GetContainerAPIMetadataResult.to_json)
           (Some Values.GetContainerAPIMetadataResult.error_to_json)])
let get_container_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_images
           (Values.GetContainerImagesRequest.make ~serviceName ())
           (Some Values.GetContainerImagesResult.to_json)
           (Some Values.GetContainerImagesResult.error_to_json)])
let get_container_log =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON IsoDate"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON IsoDate"
       and filterPattern =
         flag "filter-pattern" (optional string) ~doc:"STRING string"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_log
           (Values.GetContainerLogRequest.make
              ?startTime:(Option.map ~f:Values.IsoDate.of_json startTime)
              ?endTime:(Option.map ~f:Values.IsoDate.of_json endTime)
              ?filterPattern ?pageToken ~serviceName ~containerName ())
           (Some Values.GetContainerLogResult.to_json)
           (Some Values.GetContainerLogResult.error_to_json)])
let get_container_service_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_service_deployments
           (Values.GetContainerServiceDeploymentsRequest.make ~serviceName ())
           (Some Values.GetContainerServiceDeploymentsResult.to_json)
           (Some Values.GetContainerServiceDeploymentsResult.error_to_json)])
let get_container_service_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON ContainerServiceMetricName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON IsoDate"
       and endTime = flag "end-time" (required json_arg) ~doc:"JSON IsoDate"
       and period = flag "period" (required int) ~doc:"INT MetricPeriod"
       and statistics =
         flag "statistics" (required json_arg)
           ~doc:"JSON MetricStatisticList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_service_metric_data
           (Values.GetContainerServiceMetricDataRequest.make ~serviceName
              ~metricName:(Values.ContainerServiceMetricName.of_json
                             metricName)
              ~startTime:(Values.IsoDate.of_json startTime)
              ~endTime:(Values.IsoDate.of_json endTime) ~period
              ~statistics:(Values.MetricStatisticList.of_json statistics) ())
           (Some Values.GetContainerServiceMetricDataResult.to_json)
           (Some Values.GetContainerServiceMetricDataResult.error_to_json)])
let get_container_service_powers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_service_powers
           (Values.GetContainerServicePowersRequest.make ())
           (Some Values.GetContainerServicePowersResult.to_json)
           (Some Values.GetContainerServicePowersResult.error_to_json)])
let get_container_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (optional string)
           ~doc:"STRING ContainerServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_services
           (Values.GetContainerServicesRequest.make ?serviceName ())
           (Some Values.ContainerServicesListResult.to_json)
           (Some Values.ContainerServicesListResult.error_to_json)])
let get_disk =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskName =
         flag "disk-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_disk (Values.GetDiskRequest.make ~diskName ())
           (Some Values.GetDiskResult.to_json)
           (Some Values.GetDiskResult.error_to_json)])
let get_disk_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and diskSnapshotName =
         flag "disk-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_disk_snapshot
           (Values.GetDiskSnapshotRequest.make ~diskSnapshotName ())
           (Some Values.GetDiskSnapshotResult.to_json)
           (Some Values.GetDiskSnapshotResult.error_to_json)])
let get_disk_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_disk_snapshots
           (Values.GetDiskSnapshotsRequest.make ?pageToken ())
           (Some Values.GetDiskSnapshotsResult.to_json)
           (Some Values.GetDiskSnapshotsResult.error_to_json)])
let get_disks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_disks (Values.GetDisksRequest.make ?pageToken ())
           (Some Values.GetDisksResult.to_json)
           (Some Values.GetDisksResult.error_to_json)])
let get_distribution_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_distribution_bundles
           (Values.GetDistributionBundlesRequest.make ())
           (Some Values.GetDistributionBundlesResult.to_json)
           (Some Values.GetDistributionBundlesResult.error_to_json)])
let get_distribution_latest_cache_reset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (optional string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_distribution_latest_cache_reset
           (Values.GetDistributionLatestCacheResetRequest.make
              ?distributionName ())
           (Some Values.GetDistributionLatestCacheResetResult.to_json)
           (Some Values.GetDistributionLatestCacheResetResult.error_to_json)])
let get_distribution_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (required string)
           ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON DistributionMetricName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON timestamp"
       and period = flag "period" (required int) ~doc:"INT MetricPeriod"
       and unit = flag "unit" (required json_arg) ~doc:"JSON MetricUnit"
       and statistics =
         flag "statistics" (required json_arg)
           ~doc:"JSON MetricStatisticList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_distribution_metric_data
           (Values.GetDistributionMetricDataRequest.make ~distributionName
              ~metricName:(Values.DistributionMetricName.of_json metricName)
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ~period
              ~unit:(Values.MetricUnit.of_json unit)
              ~statistics:(Values.MetricStatisticList.of_json statistics) ())
           (Some Values.GetDistributionMetricDataResult.to_json)
           (Some Values.GetDistributionMetricDataResult.error_to_json)])
let get_distributions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (optional string)
           ~doc:"STRING ResourceName"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_distributions
           (Values.GetDistributionsRequest.make ?distributionName ?pageToken
              ()) (Some Values.GetDistributionsResult.to_json)
           (Some Values.GetDistributionsResult.error_to_json)])
let get_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain (Values.GetDomainRequest.make ~domainName ())
           (Some Values.GetDomainResult.to_json)
           (Some Values.GetDomainResult.error_to_json)])
let get_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domains (Values.GetDomainsRequest.make ?pageToken ())
           (Some Values.GetDomainsResult.to_json)
           (Some Values.GetDomainsResult.error_to_json)])
let get_export_snapshot_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_export_snapshot_records
           (Values.GetExportSnapshotRecordsRequest.make ?pageToken ())
           (Some Values.GetExportSnapshotRecordsResult.to_json)
           (Some Values.GetExportSnapshotRecordsResult.error_to_json)])
let get_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance (Values.GetInstanceRequest.make ~instanceName ())
           (Some Values.GetInstanceResult.to_json)
           (Some Values.GetInstanceResult.error_to_json)])
let get_instance_access_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (optional json_arg)
           ~doc:"JSON InstanceAccessProtocol"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_access_details
           (Values.GetInstanceAccessDetailsRequest.make
              ?protocol:(Option.map ~f:Values.InstanceAccessProtocol.of_json
                           protocol) ~instanceName ())
           (Some Values.GetInstanceAccessDetailsResult.to_json)
           (Some Values.GetInstanceAccessDetailsResult.error_to_json)])
let get_instance_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON InstanceMetricName"
       and period = flag "period" (required int) ~doc:"INT MetricPeriod"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON timestamp"
       and unit = flag "unit" (required json_arg) ~doc:"JSON MetricUnit"
       and statistics =
         flag "statistics" (required json_arg)
           ~doc:"JSON MetricStatisticList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_metric_data
           (Values.GetInstanceMetricDataRequest.make ~instanceName
              ~metricName:(Values.InstanceMetricName.of_json metricName)
              ~period ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~unit:(Values.MetricUnit.of_json unit)
              ~statistics:(Values.MetricStatisticList.of_json statistics) ())
           (Some Values.GetInstanceMetricDataResult.to_json)
           (Some Values.GetInstanceMetricDataResult.error_to_json)])
let get_instance_port_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_port_states
           (Values.GetInstancePortStatesRequest.make ~instanceName ())
           (Some Values.GetInstancePortStatesResult.to_json)
           (Some Values.GetInstancePortStatesResult.error_to_json)])
let get_instance_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceSnapshotName =
         flag "instance-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_snapshot
           (Values.GetInstanceSnapshotRequest.make ~instanceSnapshotName ())
           (Some Values.GetInstanceSnapshotResult.to_json)
           (Some Values.GetInstanceSnapshotResult.error_to_json)])
let get_instance_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_snapshots
           (Values.GetInstanceSnapshotsRequest.make ?pageToken ())
           (Some Values.GetInstanceSnapshotsResult.to_json)
           (Some Values.GetInstanceSnapshotsResult.error_to_json)])
let get_instance_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_state
           (Values.GetInstanceStateRequest.make ~instanceName ())
           (Some Values.GetInstanceStateResult.to_json)
           (Some Values.GetInstanceStateResult.error_to_json)])
let get_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instances (Values.GetInstancesRequest.make ?pageToken ())
           (Some Values.GetInstancesResult.to_json)
           (Some Values.GetInstancesResult.error_to_json)])
let get_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyPairName =
         flag "key-pair-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_key_pair (Values.GetKeyPairRequest.make ~keyPairName ())
           (Some Values.GetKeyPairResult.to_json)
           (Some Values.GetKeyPairResult.error_to_json)])
let get_key_pairs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and includeDefaultKeyPair =
         flag "include-default-key-pair" (optional bool) ~doc:"BOOL boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_key_pairs
           (Values.GetKeyPairsRequest.make ?pageToken ?includeDefaultKeyPair
              ()) (Some Values.GetKeyPairsResult.to_json)
           (Some Values.GetKeyPairsResult.error_to_json)])
let get_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_load_balancer
           (Values.GetLoadBalancerRequest.make ~loadBalancerName ())
           (Some Values.GetLoadBalancerResult.to_json)
           (Some Values.GetLoadBalancerResult.error_to_json)])
let get_load_balancer_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON LoadBalancerMetricName"
       and period = flag "period" (required int) ~doc:"INT MetricPeriod"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON timestamp"
       and unit = flag "unit" (required json_arg) ~doc:"JSON MetricUnit"
       and statistics =
         flag "statistics" (required json_arg)
           ~doc:"JSON MetricStatisticList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_load_balancer_metric_data
           (Values.GetLoadBalancerMetricDataRequest.make ~loadBalancerName
              ~metricName:(Values.LoadBalancerMetricName.of_json metricName)
              ~period ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~unit:(Values.MetricUnit.of_json unit)
              ~statistics:(Values.MetricStatisticList.of_json statistics) ())
           (Some Values.GetLoadBalancerMetricDataResult.to_json)
           (Some Values.GetLoadBalancerMetricDataResult.error_to_json)])
let get_load_balancer_tls_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_load_balancer_tls_certificates
           (Values.GetLoadBalancerTlsCertificatesRequest.make
              ~loadBalancerName ())
           (Some Values.GetLoadBalancerTlsCertificatesResult.to_json)
           (Some Values.GetLoadBalancerTlsCertificatesResult.error_to_json)])
let get_load_balancers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_load_balancers
           (Values.GetLoadBalancersRequest.make ?pageToken ())
           (Some Values.GetLoadBalancersResult.to_json)
           (Some Values.GetLoadBalancersResult.error_to_json)])
let get_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operation (Values.GetOperationRequest.make ~operationId ())
           (Some Values.GetOperationResult.to_json)
           (Some Values.GetOperationResult.error_to_json)])
let get_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operations (Values.GetOperationsRequest.make ?pageToken ())
           (Some Values.GetOperationsResult.to_json)
           (Some Values.GetOperationsResult.error_to_json)])
let get_operations_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operations_for_resource
           (Values.GetOperationsForResourceRequest.make ?pageToken
              ~resourceName ())
           (Some Values.GetOperationsForResourceResult.to_json)
           (Some Values.GetOperationsForResourceResult.error_to_json)])
let get_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeAvailabilityZones =
         flag "include-availability-zones" (optional bool)
           ~doc:"BOOL boolean"
       and includeRelationalDatabaseAvailabilityZones =
         flag "include-relational-database-availability-zones"
           (optional bool) ~doc:"BOOL boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_regions
           (Values.GetRegionsRequest.make ?includeAvailabilityZones
              ?includeRelationalDatabaseAvailabilityZones ())
           (Some Values.GetRegionsResult.to_json)
           (Some Values.GetRegionsResult.error_to_json)])
let get_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database
           (Values.GetRelationalDatabaseRequest.make ~relationalDatabaseName
              ()) (Some Values.GetRelationalDatabaseResult.to_json)
           (Some Values.GetRelationalDatabaseResult.error_to_json)])
let get_relational_database_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_blueprints
           (Values.GetRelationalDatabaseBlueprintsRequest.make ?pageToken ())
           (Some Values.GetRelationalDatabaseBlueprintsResult.to_json)
           (Some Values.GetRelationalDatabaseBlueprintsResult.error_to_json)])
let get_relational_database_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_bundles
           (Values.GetRelationalDatabaseBundlesRequest.make ?pageToken ())
           (Some Values.GetRelationalDatabaseBundlesResult.to_json)
           (Some Values.GetRelationalDatabaseBundlesResult.error_to_json)])
let get_relational_database_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationInMinutes =
         flag "duration-in-minutes" (optional int) ~doc:"INT integer"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_events
           (Values.GetRelationalDatabaseEventsRequest.make ?durationInMinutes
              ?pageToken ~relationalDatabaseName ())
           (Some Values.GetRelationalDatabaseEventsResult.to_json)
           (Some Values.GetRelationalDatabaseEventsResult.error_to_json)])
let get_relational_database_log_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON IsoDate"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON IsoDate"
       and startFromHead =
         flag "start-from-head" (optional bool) ~doc:"BOOL boolean"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName"
       and logStreamName =
         flag "log-stream-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_log_events
           (Values.GetRelationalDatabaseLogEventsRequest.make
              ?startTime:(Option.map ~f:Values.IsoDate.of_json startTime)
              ?endTime:(Option.map ~f:Values.IsoDate.of_json endTime)
              ?startFromHead ?pageToken ~relationalDatabaseName
              ~logStreamName ())
           (Some Values.GetRelationalDatabaseLogEventsResult.to_json)
           (Some Values.GetRelationalDatabaseLogEventsResult.error_to_json)])
let get_relational_database_log_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_log_streams
           (Values.GetRelationalDatabaseLogStreamsRequest.make
              ~relationalDatabaseName ())
           (Some Values.GetRelationalDatabaseLogStreamsResult.to_json)
           (Some Values.GetRelationalDatabaseLogStreamsResult.error_to_json)])
let get_relational_database_master_user_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and passwordVersion =
         flag "password-version" (optional json_arg)
           ~doc:"JSON RelationalDatabasePasswordVersion"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_master_user_password
           (Values.GetRelationalDatabaseMasterUserPasswordRequest.make
              ?passwordVersion:(Option.map
                                  ~f:Values.RelationalDatabasePasswordVersion.of_json
                                  passwordVersion) ~relationalDatabaseName ())
           (Some Values.GetRelationalDatabaseMasterUserPasswordResult.to_json)
           (Some
              Values.GetRelationalDatabaseMasterUserPasswordResult.error_to_json)])
let get_relational_database_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON RelationalDatabaseMetricName"
       and period = flag "period" (required int) ~doc:"INT MetricPeriod"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON IsoDate"
       and endTime = flag "end-time" (required json_arg) ~doc:"JSON IsoDate"
       and unit = flag "unit" (required json_arg) ~doc:"JSON MetricUnit"
       and statistics =
         flag "statistics" (required json_arg)
           ~doc:"JSON MetricStatisticList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_metric_data
           (Values.GetRelationalDatabaseMetricDataRequest.make
              ~relationalDatabaseName
              ~metricName:(Values.RelationalDatabaseMetricName.of_json
                             metricName) ~period
              ~startTime:(Values.IsoDate.of_json startTime)
              ~endTime:(Values.IsoDate.of_json endTime)
              ~unit:(Values.MetricUnit.of_json unit)
              ~statistics:(Values.MetricStatisticList.of_json statistics) ())
           (Some Values.GetRelationalDatabaseMetricDataResult.to_json)
           (Some Values.GetRelationalDatabaseMetricDataResult.error_to_json)])
let get_relational_database_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_parameters
           (Values.GetRelationalDatabaseParametersRequest.make ?pageToken
              ~relationalDatabaseName ())
           (Some Values.GetRelationalDatabaseParametersResult.to_json)
           (Some Values.GetRelationalDatabaseParametersResult.error_to_json)])
let get_relational_database_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseSnapshotName =
         flag "relational-database-snapshot-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_snapshot
           (Values.GetRelationalDatabaseSnapshotRequest.make
              ~relationalDatabaseSnapshotName ())
           (Some Values.GetRelationalDatabaseSnapshotResult.to_json)
           (Some Values.GetRelationalDatabaseSnapshotResult.error_to_json)])
let get_relational_database_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_database_snapshots
           (Values.GetRelationalDatabaseSnapshotsRequest.make ?pageToken ())
           (Some Values.GetRelationalDatabaseSnapshotsResult.to_json)
           (Some Values.GetRelationalDatabaseSnapshotsResult.error_to_json)])
let get_relational_databases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relational_databases
           (Values.GetRelationalDatabasesRequest.make ?pageToken ())
           (Some Values.GetRelationalDatabasesResult.to_json)
           (Some Values.GetRelationalDatabasesResult.error_to_json)])
let get_static_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and staticIpName =
         flag "static-ip-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_static_ip (Values.GetStaticIpRequest.make ~staticIpName ())
           (Some Values.GetStaticIpResult.to_json)
           (Some Values.GetStaticIpResult.error_to_json)])
let get_static_ips =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_static_ips (Values.GetStaticIpsRequest.make ?pageToken ())
           (Some Values.GetStaticIpsResult.to_json)
           (Some Values.GetStaticIpsResult.error_to_json)])
let import_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyPairName =
         flag "key-pair-name" (required string) ~doc:"STRING ResourceName"
       and publicKeyBase64 =
         flag "public-key-base64" (required string) ~doc:"STRING Base64" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_key_pair
           (Values.ImportKeyPairRequest.make ~keyPairName ~publicKeyBase64 ())
           (Some Values.ImportKeyPairResult.to_json)
           (Some Values.ImportKeyPairResult.error_to_json)])
let is_vpc_peered =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.is_vpc_peered (Values.IsVpcPeeredRequest.make ())
           (Some Values.IsVpcPeeredResult.to_json)
           (Some Values.IsVpcPeeredResult.error_to_json)])
let open_instance_public_ports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portInfo =
         flag "port-info" (required json_arg) ~doc:"JSON PortInfo"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.open_instance_public_ports
           (Values.OpenInstancePublicPortsRequest.make
              ~portInfo:(Values.PortInfo.of_json portInfo) ~instanceName ())
           (Some Values.OpenInstancePublicPortsResult.to_json)
           (Some Values.OpenInstancePublicPortsResult.error_to_json)])
let peer_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.peer_vpc (Values.PeerVpcRequest.make ())
           (Some Values.PeerVpcResult.to_json)
           (Some Values.PeerVpcResult.error_to_json)])
let put_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datapointsToAlarm =
         flag "datapoints-to-alarm" (optional int) ~doc:"INT integer"
       and treatMissingData =
         flag "treat-missing-data" (optional json_arg)
           ~doc:"JSON TreatMissingData"
       and contactProtocols =
         flag "contact-protocols" (optional json_arg)
           ~doc:"JSON ContactProtocolsList"
       and notificationTriggers =
         flag "notification-triggers" (optional json_arg)
           ~doc:"JSON NotificationTriggerList"
       and notificationEnabled =
         flag "notification-enabled" (optional bool) ~doc:"BOOL boolean"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required json_arg) ~doc:"JSON MetricName"
       and monitoredResourceName =
         flag "monitored-resource-name" (required string)
           ~doc:"STRING ResourceName"
       and comparisonOperator =
         flag "comparison-operator" (required json_arg)
           ~doc:"JSON ComparisonOperator"
       and threshold = flag "threshold" (required float) ~doc:"FLOAT double"
       and evaluationPeriods =
         flag "evaluation-periods" (required int) ~doc:"INT integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_alarm
           (Values.PutAlarmRequest.make ?datapointsToAlarm
              ?treatMissingData:(Option.map
                                   ~f:Values.TreatMissingData.of_json
                                   treatMissingData)
              ?contactProtocols:(Option.map
                                   ~f:Values.ContactProtocolsList.of_json
                                   contactProtocols)
              ?notificationTriggers:(Option.map
                                       ~f:Values.NotificationTriggerList.of_json
                                       notificationTriggers)
              ?notificationEnabled ~alarmName
              ~metricName:(Values.MetricName.of_json metricName)
              ~monitoredResourceName
              ~comparisonOperator:(Values.ComparisonOperator.of_json
                                     comparisonOperator) ~threshold
              ~evaluationPeriods ()) (Some Values.PutAlarmResult.to_json)
           (Some Values.PutAlarmResult.error_to_json)])
let put_instance_public_ports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portInfos =
         flag "port-infos" (required json_arg) ~doc:"JSON PortInfoList"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_instance_public_ports
           (Values.PutInstancePublicPortsRequest.make
              ~portInfos:(Values.PortInfoList.of_json portInfos)
              ~instanceName ())
           (Some Values.PutInstancePublicPortsResult.to_json)
           (Some Values.PutInstancePublicPortsResult.error_to_json)])
let reboot_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_instance
           (Values.RebootInstanceRequest.make ~instanceName ())
           (Some Values.RebootInstanceResult.to_json)
           (Some Values.RebootInstanceResult.error_to_json)])
let reboot_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_relational_database
           (Values.RebootRelationalDatabaseRequest.make
              ~relationalDatabaseName ())
           (Some Values.RebootRelationalDatabaseResult.to_json)
           (Some Values.RebootRelationalDatabaseResult.error_to_json)])
let register_container_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName"
       and label =
         flag "label" (required string) ~doc:"STRING ContainerLabel"
       and digest = flag "digest" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_container_image
           (Values.RegisterContainerImageRequest.make ~serviceName ~label
              ~digest ()) (Some Values.RegisterContainerImageResult.to_json)
           (Some Values.RegisterContainerImageResult.error_to_json)])
let release_static_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and staticIpName =
         flag "static-ip-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.release_static_ip
           (Values.ReleaseStaticIpRequest.make ~staticIpName ())
           (Some Values.ReleaseStaticIpResult.to_json)
           (Some Values.ReleaseStaticIpResult.error_to_json)])
let reset_distribution_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (optional string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_distribution_cache
           (Values.ResetDistributionCacheRequest.make ?distributionName ())
           (Some Values.ResetDistributionCacheResult.to_json)
           (Some Values.ResetDistributionCacheResult.error_to_json)])
let send_contact_method_verification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (required json_arg)
           ~doc:"JSON ContactMethodVerificationProtocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_contact_method_verification
           (Values.SendContactMethodVerificationRequest.make
              ~protocol:(Values.ContactMethodVerificationProtocol.of_json
                           protocol) ())
           (Some Values.SendContactMethodVerificationResult.to_json)
           (Some Values.SendContactMethodVerificationResult.error_to_json)])
let set_ip_address_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName"
       and ipAddressType =
         flag "ip-address-type" (required json_arg) ~doc:"JSON IpAddressType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_ip_address_type
           (Values.SetIpAddressTypeRequest.make
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resourceName
              ~ipAddressType:(Values.IpAddressType.of_json ipAddressType) ())
           (Some Values.SetIpAddressTypeResult.to_json)
           (Some Values.SetIpAddressTypeResult.error_to_json)])
let set_resource_access_for_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName"
       and access =
         flag "access" (required json_arg) ~doc:"JSON ResourceBucketAccess" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_resource_access_for_bucket
           (Values.SetResourceAccessForBucketRequest.make ~resourceName
              ~bucketName
              ~access:(Values.ResourceBucketAccess.of_json access) ())
           (Some Values.SetResourceAccessForBucketResult.to_json)
           (Some Values.SetResourceAccessForBucketResult.error_to_json)])
let start_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_instance
           (Values.StartInstanceRequest.make ~instanceName ())
           (Some Values.StartInstanceResult.to_json)
           (Some Values.StartInstanceResult.error_to_json)])
let start_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_relational_database
           (Values.StartRelationalDatabaseRequest.make
              ~relationalDatabaseName ())
           (Some Values.StartRelationalDatabaseResult.to_json)
           (Some Values.StartRelationalDatabaseResult.error_to_json)])
let stop_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL boolean"
       and instanceName =
         flag "instance-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_instance
           (Values.StopInstanceRequest.make ?force ~instanceName ())
           (Some Values.StopInstanceResult.to_json)
           (Some Values.StopInstanceResult.error_to_json)])
let stop_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseSnapshotName =
         flag "relational-database-snapshot-name" (optional string)
           ~doc:"STRING ResourceName"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_relational_database
           (Values.StopRelationalDatabaseRequest.make
              ?relationalDatabaseSnapshotName ~relationalDatabaseName ())
           (Some Values.StopRelationalDatabaseResult.to_json)
           (Some Values.StopRelationalDatabaseResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ?resourceArn ~resourceName
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let test_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING ResourceName"
       and state = flag "state" (required json_arg) ~doc:"JSON AlarmState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_alarm
           (Values.TestAlarmRequest.make ~alarmName
              ~state:(Values.AlarmState.of_json state) ())
           (Some Values.TestAlarmResult.to_json)
           (Some Values.TestAlarmResult.error_to_json)])
let unpeer_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unpeer_vpc (Values.UnpeerVpcRequest.make ())
           (Some Values.UnpeerVpcResult.to_json)
           (Some Values.UnpeerVpcResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING ResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ?resourceArn ~resourceName
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessRules =
         flag "access-rules" (optional json_arg) ~doc:"JSON AccessRules"
       and versioning =
         flag "versioning" (optional string) ~doc:"STRING NonEmptyString"
       and readonlyAccessAccounts =
         flag "readonly-access-accounts" (optional json_arg)
           ~doc:"JSON PartnerIdList"
       and accessLogConfig =
         flag "access-log-config" (optional json_arg)
           ~doc:"JSON BucketAccessLogConfig"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bucket
           (Values.UpdateBucketRequest.make
              ?accessRules:(Option.map ~f:Values.AccessRules.of_json
                              accessRules) ?versioning
              ?readonlyAccessAccounts:(Option.map
                                         ~f:Values.PartnerIdList.of_json
                                         readonlyAccessAccounts)
              ?accessLogConfig:(Option.map
                                  ~f:Values.BucketAccessLogConfig.of_json
                                  accessLogConfig) ~bucketName ())
           (Some Values.UpdateBucketResult.to_json)
           (Some Values.UpdateBucketResult.error_to_json)])
let update_bucket_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING BucketName"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bucket_bundle
           (Values.UpdateBucketBundleRequest.make ~bucketName ~bundleId ())
           (Some Values.UpdateBucketBundleResult.to_json)
           (Some Values.UpdateBucketBundleResult.error_to_json)])
let update_container_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and power =
         flag "power" (optional json_arg)
           ~doc:"JSON ContainerServicePowerName"
       and scale =
         flag "scale" (optional int) ~doc:"INT ContainerServiceScale"
       and isDisabled =
         flag "is-disabled" (optional bool) ~doc:"BOOL boolean"
       and publicDomainNames =
         flag "public-domain-names" (optional json_arg)
           ~doc:"JSON ContainerServicePublicDomains"
       and serviceName =
         flag "service-name" (required string)
           ~doc:"STRING ContainerServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_container_service
           (Values.UpdateContainerServiceRequest.make
              ?power:(Option.map ~f:Values.ContainerServicePowerName.of_json
                        power) ?scale ?isDisabled
              ?publicDomainNames:(Option.map
                                    ~f:Values.ContainerServicePublicDomains.of_json
                                    publicDomainNames) ~serviceName ())
           (Some Values.UpdateContainerServiceResult.to_json)
           (Some Values.UpdateContainerServiceResult.error_to_json)])
let update_distribution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and origin = flag "origin" (optional json_arg) ~doc:"JSON InputOrigin"
       and defaultCacheBehavior =
         flag "default-cache-behavior" (optional json_arg)
           ~doc:"JSON CacheBehavior"
       and cacheBehaviorSettings =
         flag "cache-behavior-settings" (optional json_arg)
           ~doc:"JSON CacheSettings"
       and cacheBehaviors =
         flag "cache-behaviors" (optional json_arg)
           ~doc:"JSON CacheBehaviorList"
       and isEnabled = flag "is-enabled" (optional bool) ~doc:"BOOL boolean"
       and distributionName =
         flag "distribution-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_distribution
           (Values.UpdateDistributionRequest.make
              ?origin:(Option.map ~f:Values.InputOrigin.of_json origin)
              ?defaultCacheBehavior:(Option.map
                                       ~f:Values.CacheBehavior.of_json
                                       defaultCacheBehavior)
              ?cacheBehaviorSettings:(Option.map
                                        ~f:Values.CacheSettings.of_json
                                        cacheBehaviorSettings)
              ?cacheBehaviors:(Option.map ~f:Values.CacheBehaviorList.of_json
                                 cacheBehaviors) ?isEnabled ~distributionName
              ()) (Some Values.UpdateDistributionResult.to_json)
           (Some Values.UpdateDistributionResult.error_to_json)])
let update_distribution_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and distributionName =
         flag "distribution-name" (optional string)
           ~doc:"STRING ResourceName"
       and bundleId = flag "bundle-id" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_distribution_bundle
           (Values.UpdateDistributionBundleRequest.make ?distributionName
              ?bundleId ())
           (Some Values.UpdateDistributionBundleResult.to_json)
           (Some Values.UpdateDistributionBundleResult.error_to_json)])
let update_domain_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and domainEntry =
         flag "domain-entry" (required json_arg) ~doc:"JSON DomainEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_entry
           (Values.UpdateDomainEntryRequest.make ~domainName
              ~domainEntry:(Values.DomainEntry.of_json domainEntry) ())
           (Some Values.UpdateDomainEntryResult.to_json)
           (Some Values.UpdateDomainEntryResult.error_to_json)])
let update_load_balancer_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerName =
         flag "load-balancer-name" (required string)
           ~doc:"STRING ResourceName"
       and attributeName =
         flag "attribute-name" (required json_arg)
           ~doc:"JSON LoadBalancerAttributeName"
       and attributeValue =
         flag "attribute-value" (required string) ~doc:"STRING StringMax256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_load_balancer_attribute
           (Values.UpdateLoadBalancerAttributeRequest.make ~loadBalancerName
              ~attributeName:(Values.LoadBalancerAttributeName.of_json
                                attributeName) ~attributeValue ())
           (Some Values.UpdateLoadBalancerAttributeResult.to_json)
           (Some Values.UpdateLoadBalancerAttributeResult.error_to_json)])
let update_relational_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and masterUserPassword =
         flag "master-user-password" (optional string)
           ~doc:"STRING SensitiveString"
       and rotateMasterUserPassword =
         flag "rotate-master-user-password" (optional bool)
           ~doc:"BOOL boolean"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING string"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING string"
       and enableBackupRetention =
         flag "enable-backup-retention" (optional bool) ~doc:"BOOL boolean"
       and disableBackupRetention =
         flag "disable-backup-retention" (optional bool) ~doc:"BOOL boolean"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL boolean"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL boolean"
       and caCertificateIdentifier =
         flag "ca-certificate-identifier" (optional string)
           ~doc:"STRING string"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_relational_database
           (Values.UpdateRelationalDatabaseRequest.make ?masterUserPassword
              ?rotateMasterUserPassword ?preferredBackupWindow
              ?preferredMaintenanceWindow ?enableBackupRetention
              ?disableBackupRetention ?publiclyAccessible ?applyImmediately
              ?caCertificateIdentifier ~relationalDatabaseName ())
           (Some Values.UpdateRelationalDatabaseResult.to_json)
           (Some Values.UpdateRelationalDatabaseResult.error_to_json)])
let update_relational_database_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationalDatabaseName =
         flag "relational-database-name" (required string)
           ~doc:"STRING ResourceName"
       and parameters =
         flag "parameters" (required json_arg)
           ~doc:"JSON RelationalDatabaseParameterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_relational_database_parameters
           (Values.UpdateRelationalDatabaseParametersRequest.make
              ~relationalDatabaseName
              ~parameters:(Values.RelationalDatabaseParameterList.of_json
                             parameters) ())
           (Some Values.UpdateRelationalDatabaseParametersResult.to_json)
           (Some
              Values.UpdateRelationalDatabaseParametersResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("allocate-static-ip", allocate_static_ip);
    ("attach-certificate-to-distribution",
      attach_certificate_to_distribution);
    ("attach-disk", attach_disk);
    ("attach-instances-to-load-balancer", attach_instances_to_load_balancer);
    ("attach-load-balancer-tls-certificate",
      attach_load_balancer_tls_certificate);
    ("attach-static-ip", attach_static_ip);
    ("close-instance-public-ports", close_instance_public_ports);
    ("copy-snapshot", copy_snapshot);
    ("create-bucket", create_bucket);
    ("create-bucket-access-key", create_bucket_access_key);
    ("create-certificate", create_certificate);
    ("create-cloud-formation-stack", create_cloud_formation_stack);
    ("create-contact-method", create_contact_method);
    ("create-container-service", create_container_service);
    ("create-container-service-deployment",
      create_container_service_deployment);
    ("create-container-service-registry-login",
      create_container_service_registry_login);
    ("create-disk", create_disk);
    ("create-disk-from-snapshot", create_disk_from_snapshot);
    ("create-disk-snapshot", create_disk_snapshot);
    ("create-distribution", create_distribution);
    ("create-domain", create_domain);
    ("create-domain-entry", create_domain_entry);
    ("create-instance-snapshot", create_instance_snapshot);
    ("create-instances", create_instances);
    ("create-instances-from-snapshot", create_instances_from_snapshot);
    ("create-key-pair", create_key_pair);
    ("create-load-balancer", create_load_balancer);
    ("create-load-balancer-tls-certificate",
      create_load_balancer_tls_certificate);
    ("create-relational-database", create_relational_database);
    ("create-relational-database-from-snapshot",
      create_relational_database_from_snapshot);
    ("create-relational-database-snapshot",
      create_relational_database_snapshot);
    ("delete-alarm", delete_alarm);
    ("delete-auto-snapshot", delete_auto_snapshot);
    ("delete-bucket", delete_bucket);
    ("delete-bucket-access-key", delete_bucket_access_key);
    ("delete-certificate", delete_certificate);
    ("delete-contact-method", delete_contact_method);
    ("delete-container-image", delete_container_image);
    ("delete-container-service", delete_container_service);
    ("delete-disk", delete_disk);
    ("delete-disk-snapshot", delete_disk_snapshot);
    ("delete-distribution", delete_distribution);
    ("delete-domain", delete_domain);
    ("delete-domain-entry", delete_domain_entry);
    ("delete-instance", delete_instance);
    ("delete-instance-snapshot", delete_instance_snapshot);
    ("delete-key-pair", delete_key_pair);
    ("delete-known-host-keys", delete_known_host_keys);
    ("delete-load-balancer", delete_load_balancer);
    ("delete-load-balancer-tls-certificate",
      delete_load_balancer_tls_certificate);
    ("delete-relational-database", delete_relational_database);
    ("delete-relational-database-snapshot",
      delete_relational_database_snapshot);
    ("detach-certificate-from-distribution",
      detach_certificate_from_distribution);
    ("detach-disk", detach_disk);
    ("detach-instances-from-load-balancer",
      detach_instances_from_load_balancer);
    ("detach-static-ip", detach_static_ip);
    ("disable-add-on", disable_add_on);
    ("download-default-key-pair", download_default_key_pair);
    ("enable-add-on", enable_add_on);
    ("export-snapshot", export_snapshot);
    ("get-active-names", get_active_names);
    ("get-alarms", get_alarms);
    ("get-auto-snapshots", get_auto_snapshots);
    ("get-blueprints", get_blueprints);
    ("get-bucket-access-keys", get_bucket_access_keys);
    ("get-bucket-bundles", get_bucket_bundles);
    ("get-bucket-metric-data", get_bucket_metric_data);
    ("get-buckets", get_buckets);
    ("get-bundles", get_bundles);
    ("get-certificates", get_certificates);
    ("get-cloud-formation-stack-records", get_cloud_formation_stack_records);
    ("get-contact-methods", get_contact_methods);
    ("get-container-a-p-i-metadata", get_container_a_p_i_metadata);
    ("get-container-images", get_container_images);
    ("get-container-log", get_container_log);
    ("get-container-service-deployments", get_container_service_deployments);
    ("get-container-service-metric-data", get_container_service_metric_data);
    ("get-container-service-powers", get_container_service_powers);
    ("get-container-services", get_container_services);
    ("get-disk", get_disk);
    ("get-disk-snapshot", get_disk_snapshot);
    ("get-disk-snapshots", get_disk_snapshots);
    ("get-disks", get_disks);
    ("get-distribution-bundles", get_distribution_bundles);
    ("get-distribution-latest-cache-reset",
      get_distribution_latest_cache_reset);
    ("get-distribution-metric-data", get_distribution_metric_data);
    ("get-distributions", get_distributions);
    ("get-domain", get_domain);
    ("get-domains", get_domains);
    ("get-export-snapshot-records", get_export_snapshot_records);
    ("get-instance", get_instance);
    ("get-instance-access-details", get_instance_access_details);
    ("get-instance-metric-data", get_instance_metric_data);
    ("get-instance-port-states", get_instance_port_states);
    ("get-instance-snapshot", get_instance_snapshot);
    ("get-instance-snapshots", get_instance_snapshots);
    ("get-instance-state", get_instance_state);
    ("get-instances", get_instances);
    ("get-key-pair", get_key_pair);
    ("get-key-pairs", get_key_pairs);
    ("get-load-balancer", get_load_balancer);
    ("get-load-balancer-metric-data", get_load_balancer_metric_data);
    ("get-load-balancer-tls-certificates",
      get_load_balancer_tls_certificates);
    ("get-load-balancers", get_load_balancers);
    ("get-operation", get_operation);
    ("get-operations", get_operations);
    ("get-operations-for-resource", get_operations_for_resource);
    ("get-regions", get_regions);
    ("get-relational-database", get_relational_database);
    ("get-relational-database-blueprints",
      get_relational_database_blueprints);
    ("get-relational-database-bundles", get_relational_database_bundles);
    ("get-relational-database-events", get_relational_database_events);
    ("get-relational-database-log-events",
      get_relational_database_log_events);
    ("get-relational-database-log-streams",
      get_relational_database_log_streams);
    ("get-relational-database-master-user-password",
      get_relational_database_master_user_password);
    ("get-relational-database-metric-data",
      get_relational_database_metric_data);
    ("get-relational-database-parameters",
      get_relational_database_parameters);
    ("get-relational-database-snapshot", get_relational_database_snapshot);
    ("get-relational-database-snapshots", get_relational_database_snapshots);
    ("get-relational-databases", get_relational_databases);
    ("get-static-ip", get_static_ip);
    ("get-static-ips", get_static_ips);
    ("import-key-pair", import_key_pair);
    ("is-vpc-peered", is_vpc_peered);
    ("open-instance-public-ports", open_instance_public_ports);
    ("peer-vpc", peer_vpc);
    ("put-alarm", put_alarm);
    ("put-instance-public-ports", put_instance_public_ports);
    ("reboot-instance", reboot_instance);
    ("reboot-relational-database", reboot_relational_database);
    ("register-container-image", register_container_image);
    ("release-static-ip", release_static_ip);
    ("reset-distribution-cache", reset_distribution_cache);
    ("send-contact-method-verification", send_contact_method_verification);
    ("set-ip-address-type", set_ip_address_type);
    ("set-resource-access-for-bucket", set_resource_access_for_bucket);
    ("start-instance", start_instance);
    ("start-relational-database", start_relational_database);
    ("stop-instance", stop_instance);
    ("stop-relational-database", stop_relational_database);
    ("tag-resource", tag_resource);
    ("test-alarm", test_alarm);
    ("unpeer-vpc", unpeer_vpc);
    ("untag-resource", untag_resource);
    ("update-bucket", update_bucket);
    ("update-bucket-bundle", update_bucket_bundle);
    ("update-container-service", update_container_service);
    ("update-distribution", update_distribution);
    ("update-distribution-bundle", update_distribution_bundle);
    ("update-domain-entry", update_domain_entry);
    ("update-load-balancer-attribute", update_load_balancer_attribute);
    ("update-relational-database", update_relational_database);
    ("update-relational-database-parameters",
      update_relational_database_parameters)]
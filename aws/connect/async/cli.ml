(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_approved_origin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and origin = flag "origin" (required string) ~doc:"STRING Origin" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_approved_origin
           (Values.AssociateApprovedOriginRequest.make ~instanceId ~origin ())
           None None])
let associate_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lexBot = flag "lex-bot" (optional json_arg) ~doc:"JSON LexBot"
       and lexV2Bot =
         flag "lex-v2-bot" (optional json_arg) ~doc:"JSON LexV2Bot"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_bot
           (Values.AssociateBotRequest.make
              ?lexBot:(Option.map ~f:Values.LexBot.of_json lexBot)
              ?lexV2Bot:(Option.map ~f:Values.LexV2Bot.of_json lexV2Bot)
              ~instanceId ()) None None])
let associate_default_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyId =
         flag "vocabulary-id" (optional string) ~doc:"STRING VocabularyId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and languageCode =
         flag "language-code" (required json_arg)
           ~doc:"JSON VocabularyLanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_default_vocabulary
           (Values.AssociateDefaultVocabularyRequest.make ?vocabularyId
              ~instanceId
              ~languageCode:(Values.VocabularyLanguageCode.of_json
                               languageCode) ())
           (Some Values.AssociateDefaultVocabularyResponse.to_json)
           (Some Values.AssociateDefaultVocabularyResponse.error_to_json)])
let associate_instance_storage_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON InstanceStorageResourceType"
       and storageConfig =
         flag "storage-config" (required json_arg)
           ~doc:"JSON InstanceStorageConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_instance_storage_config
           (Values.AssociateInstanceStorageConfigRequest.make ~instanceId
              ~resourceType:(Values.InstanceStorageResourceType.of_json
                               resourceType)
              ~storageConfig:(Values.InstanceStorageConfig.of_json
                                storageConfig) ())
           (Some Values.AssociateInstanceStorageConfigResponse.to_json)
           (Some Values.AssociateInstanceStorageConfigResponse.error_to_json)])
let associate_lambda_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and functionArn =
         flag "function-arn" (required string) ~doc:"STRING FunctionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_lambda_function
           (Values.AssociateLambdaFunctionRequest.make ~instanceId
              ~functionArn ()) None None])
let associate_lex_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and lexBot = flag "lex-bot" (required json_arg) ~doc:"JSON LexBot" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_lex_bot
           (Values.AssociateLexBotRequest.make ~instanceId
              ~lexBot:(Values.LexBot.of_json lexBot) ()) None None])
let associate_queue_quick_connects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and quickConnectIds =
         flag "quick-connect-ids" (required json_arg)
           ~doc:"JSON QuickConnectsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_queue_quick_connects
           (Values.AssociateQueueQuickConnectsRequest.make ~instanceId
              ~queueId
              ~quickConnectIds:(Values.QuickConnectsList.of_json
                                  quickConnectIds) ()) None None])
let associate_routing_profile_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and queueConfigs =
         flag "queue-configs" (required json_arg)
           ~doc:"JSON RoutingProfileQueueConfigList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_routing_profile_queues
           (Values.AssociateRoutingProfileQueuesRequest.make ~instanceId
              ~routingProfileId
              ~queueConfigs:(Values.RoutingProfileQueueConfigList.of_json
                               queueConfigs) ()) None None])
let associate_security_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and key = flag "key" (required string) ~doc:"STRING PEM" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_security_key
           (Values.AssociateSecurityKeyRequest.make ~instanceId ~key ())
           (Some Values.AssociateSecurityKeyResponse.to_json)
           (Some Values.AssociateSecurityKeyResponse.error_to_json)])
let create_agent_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AgentStatusDescription"
       and displayOrder =
         flag "display-order" (optional int)
           ~doc:"INT AgentStatusOrderNumber"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name = flag "name" (required string) ~doc:"STRING AgentStatusName"
       and state =
         flag "state" (required json_arg) ~doc:"JSON AgentStatusState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_status
           (Values.CreateAgentStatusRequest.make ?description ?displayOrder
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name ~state:(Values.AgentStatusState.of_json state) ())
           (Some Values.CreateAgentStatusResponse.to_json)
           (Some Values.CreateAgentStatusResponse.error_to_json)])
let create_contact_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ContactFlowDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name = flag "name" (required string) ~doc:"STRING ContactFlowName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ContactFlowType"
       and content =
         flag "content" (required string) ~doc:"STRING ContactFlowContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact_flow
           (Values.CreateContactFlowRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name ~type_:(Values.ContactFlowType.of_json type_) ~content ())
           (Some Values.CreateContactFlowResponse.to_json)
           (Some Values.CreateContactFlowResponse.error_to_json)])
let create_contact_flow_module =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ContactFlowModuleDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name =
         flag "name" (required string) ~doc:"STRING ContactFlowModuleName"
       and content =
         flag "content" (required string)
           ~doc:"STRING ContactFlowModuleContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact_flow_module
           (Values.CreateContactFlowModuleRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ~instanceId ~name ~content ())
           (Some Values.CreateContactFlowModuleResponse.to_json)
           (Some Values.CreateContactFlowModuleResponse.error_to_json)])
let create_hours_of_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING HoursOfOperationDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name =
         flag "name" (required string) ~doc:"STRING CommonNameLength127"
       and timeZone =
         flag "time-zone" (required string) ~doc:"STRING TimeZone"
       and config =
         flag "config" (required json_arg)
           ~doc:"JSON HoursOfOperationConfigList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hours_of_operation
           (Values.CreateHoursOfOperationRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name ~timeZone
              ~config:(Values.HoursOfOperationConfigList.of_json config) ())
           (Some Values.CreateHoursOfOperationResponse.to_json)
           (Some Values.CreateHoursOfOperationResponse.error_to_json)])
let create_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and instanceAlias =
         flag "instance-alias" (optional string) ~doc:"STRING DirectoryAlias"
       and directoryId =
         flag "directory-id" (optional string) ~doc:"STRING DirectoryId"
       and identityManagementType =
         flag "identity-management-type" (required json_arg)
           ~doc:"JSON DirectoryType"
       and inboundCallsEnabled =
         flag "inbound-calls-enabled" (required bool)
           ~doc:"BOOL InboundCallsEnabled"
       and outboundCallsEnabled =
         flag "outbound-calls-enabled" (required bool)
           ~doc:"BOOL OutboundCallsEnabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance
           (Values.CreateInstanceRequest.make ?clientToken ?instanceAlias
              ?directoryId
              ~identityManagementType:(Values.DirectoryType.of_json
                                         identityManagementType)
              ~inboundCallsEnabled ~outboundCallsEnabled ())
           (Some Values.CreateInstanceResponse.to_json)
           (Some Values.CreateInstanceResponse.error_to_json)])
let create_integration_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceApplicationUrl =
         flag "source-application-url" (optional string) ~doc:"STRING URI"
       and sourceApplicationName =
         flag "source-application-name" (optional string)
           ~doc:"STRING SourceApplicationName"
       and sourceType =
         flag "source-type" (optional json_arg) ~doc:"JSON SourceType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and integrationType =
         flag "integration-type" (required json_arg)
           ~doc:"JSON IntegrationType"
       and integrationArn =
         flag "integration-arn" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration_association
           (Values.CreateIntegrationAssociationRequest.make
              ?sourceApplicationUrl ?sourceApplicationName
              ?sourceType:(Option.map ~f:Values.SourceType.of_json sourceType)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~integrationType:(Values.IntegrationType.of_json
                                  integrationType) ~integrationArn ())
           (Some Values.CreateIntegrationAssociationResponse.to_json)
           (Some Values.CreateIntegrationAssociationResponse.error_to_json)])
let create_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING QueueDescription"
       and outboundCallerConfig =
         flag "outbound-caller-config" (optional json_arg)
           ~doc:"JSON OutboundCallerConfig"
       and maxContacts =
         flag "max-contacts" (optional int) ~doc:"INT QueueMaxContacts"
       and quickConnectIds =
         flag "quick-connect-ids" (optional json_arg)
           ~doc:"JSON QuickConnectsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name =
         flag "name" (required string) ~doc:"STRING CommonNameLength127"
       and hoursOfOperationId =
         flag "hours-of-operation-id" (required string)
           ~doc:"STRING HoursOfOperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue
           (Values.CreateQueueRequest.make ?description
              ?outboundCallerConfig:(Option.map
                                       ~f:Values.OutboundCallerConfig.of_json
                                       outboundCallerConfig) ?maxContacts
              ?quickConnectIds:(Option.map
                                  ~f:Values.QuickConnectsList.of_json
                                  quickConnectIds)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name ~hoursOfOperationId ())
           (Some Values.CreateQueueResponse.to_json)
           (Some Values.CreateQueueResponse.error_to_json)])
let create_quick_connect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING QuickConnectDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name =
         flag "name" (required string) ~doc:"STRING QuickConnectName"
       and quickConnectConfig =
         flag "quick-connect-config" (required json_arg)
           ~doc:"JSON QuickConnectConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_quick_connect
           (Values.CreateQuickConnectRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name
              ~quickConnectConfig:(Values.QuickConnectConfig.of_json
                                     quickConnectConfig) ())
           (Some Values.CreateQuickConnectResponse.to_json)
           (Some Values.CreateQuickConnectResponse.error_to_json)])
let create_routing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueConfigs =
         flag "queue-configs" (optional json_arg)
           ~doc:"JSON RoutingProfileQueueConfigList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name =
         flag "name" (required string) ~doc:"STRING RoutingProfileName"
       and description =
         flag "description" (required string)
           ~doc:"STRING RoutingProfileDescription"
       and defaultOutboundQueueId =
         flag "default-outbound-queue-id" (required string)
           ~doc:"STRING QueueId"
       and mediaConcurrencies =
         flag "media-concurrencies" (required json_arg)
           ~doc:"JSON MediaConcurrencies" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_routing_profile
           (Values.CreateRoutingProfileRequest.make
              ?queueConfigs:(Option.map
                               ~f:Values.RoutingProfileQueueConfigList.of_json
                               queueConfigs)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name ~description ~defaultOutboundQueueId
              ~mediaConcurrencies:(Values.MediaConcurrencies.of_json
                                     mediaConcurrencies) ())
           (Some Values.CreateRoutingProfileResponse.to_json)
           (Some Values.CreateRoutingProfileResponse.error_to_json)])
let create_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SecurityProfileDescription"
       and permissions =
         flag "permissions" (optional json_arg) ~doc:"JSON PermissionsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_profile
           (Values.CreateSecurityProfileRequest.make ?description
              ?permissions:(Option.map ~f:Values.PermissionsList.of_json
                              permissions)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~securityProfileName ~instanceId ())
           (Some Values.CreateSecurityProfileResponse.to_json)
           (Some Values.CreateSecurityProfileResponse.error_to_json)])
let create_use_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and integrationAssociationId =
         flag "integration-association-id" (required string)
           ~doc:"STRING IntegrationAssociationId"
       and useCaseType =
         flag "use-case-type" (required json_arg) ~doc:"JSON UseCaseType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_use_case
           (Values.CreateUseCaseRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~integrationAssociationId
              ~useCaseType:(Values.UseCaseType.of_json useCaseType) ())
           (Some Values.CreateUseCaseResponse.to_json)
           (Some Values.CreateUseCaseResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and password =
         flag "password" (optional string) ~doc:"STRING Password"
       and identityInfo =
         flag "identity-info" (optional json_arg)
           ~doc:"JSON UserIdentityInfo"
       and directoryUserId =
         flag "directory-user-id" (optional string)
           ~doc:"STRING DirectoryUserId"
       and hierarchyGroupId =
         flag "hierarchy-group-id" (optional string)
           ~doc:"STRING HierarchyGroupId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and username =
         flag "username" (required string) ~doc:"STRING AgentUsername"
       and phoneConfig =
         flag "phone-config" (required json_arg) ~doc:"JSON UserPhoneConfig"
       and securityProfileIds =
         flag "security-profile-ids" (required json_arg)
           ~doc:"JSON SecurityProfileIds"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?password
              ?identityInfo:(Option.map ~f:Values.UserIdentityInfo.of_json
                               identityInfo) ?directoryUserId
              ?hierarchyGroupId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~username
              ~phoneConfig:(Values.UserPhoneConfig.of_json phoneConfig)
              ~securityProfileIds:(Values.SecurityProfileIds.of_json
                                     securityProfileIds) ~routingProfileId
              ~instanceId ()) (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let create_user_hierarchy_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parentGroupId =
         flag "parent-group-id" (optional string)
           ~doc:"STRING HierarchyGroupId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING HierarchyGroupName"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_hierarchy_group
           (Values.CreateUserHierarchyGroupRequest.make ?parentGroupId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~instanceId ())
           (Some Values.CreateUserHierarchyGroupResponse.to_json)
           (Some Values.CreateUserHierarchyGroupResponse.error_to_json)])
let create_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName"
       and languageCode =
         flag "language-code" (required json_arg)
           ~doc:"JSON VocabularyLanguageCode"
       and content =
         flag "content" (required string) ~doc:"STRING VocabularyContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vocabulary
           (Values.CreateVocabularyRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~vocabularyName
              ~languageCode:(Values.VocabularyLanguageCode.of_json
                               languageCode) ~content ())
           (Some Values.CreateVocabularyResponse.to_json)
           (Some Values.CreateVocabularyResponse.error_to_json)])
let delete_contact_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact_flow
           (Values.DeleteContactFlowRequest.make ~instanceId ~contactFlowId
              ()) None None])
let delete_contact_flow_module =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowModuleId =
         flag "contact-flow-module-id" (required string)
           ~doc:"STRING ContactFlowModuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact_flow_module
           (Values.DeleteContactFlowModuleRequest.make ~instanceId
              ~contactFlowModuleId ())
           (Some Values.DeleteContactFlowModuleResponse.to_json)
           (Some Values.DeleteContactFlowModuleResponse.error_to_json)])
let delete_hours_of_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and hoursOfOperationId =
         flag "hours-of-operation-id" (required string)
           ~doc:"STRING HoursOfOperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hours_of_operation
           (Values.DeleteHoursOfOperationRequest.make ~instanceId
              ~hoursOfOperationId ()) None None])
let delete_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance
           (Values.DeleteInstanceRequest.make ~instanceId ()) None None])
let delete_integration_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and integrationAssociationId =
         flag "integration-association-id" (required string)
           ~doc:"STRING IntegrationAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration_association
           (Values.DeleteIntegrationAssociationRequest.make ~instanceId
              ~integrationAssociationId ()) None None])
let delete_quick_connect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and quickConnectId =
         flag "quick-connect-id" (required string)
           ~doc:"STRING QuickConnectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_quick_connect
           (Values.DeleteQuickConnectRequest.make ~instanceId ~quickConnectId
              ()) None None])
let delete_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and securityProfileId =
         flag "security-profile-id" (required string)
           ~doc:"STRING SecurityProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_profile
           (Values.DeleteSecurityProfileRequest.make ~instanceId
              ~securityProfileId ()) None None])
let delete_use_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and integrationAssociationId =
         flag "integration-association-id" (required string)
           ~doc:"STRING IntegrationAssociationId"
       and useCaseId =
         flag "use-case-id" (required string) ~doc:"STRING UseCaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_use_case
           (Values.DeleteUseCaseRequest.make ~instanceId
              ~integrationAssociationId ~useCaseId ()) None None])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~instanceId ~userId ()) None None])
let delete_user_hierarchy_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hierarchyGroupId =
         flag "hierarchy-group-id" (required string)
           ~doc:"STRING HierarchyGroupId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_hierarchy_group
           (Values.DeleteUserHierarchyGroupRequest.make ~hierarchyGroupId
              ~instanceId ()) None None])
let delete_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and vocabularyId =
         flag "vocabulary-id" (required string) ~doc:"STRING VocabularyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vocabulary
           (Values.DeleteVocabularyRequest.make ~instanceId ~vocabularyId ())
           (Some Values.DeleteVocabularyResponse.to_json)
           (Some Values.DeleteVocabularyResponse.error_to_json)])
let describe_agent_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and agentStatusId =
         flag "agent-status-id" (required string) ~doc:"STRING AgentStatusId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_agent_status
           (Values.DescribeAgentStatusRequest.make ~instanceId ~agentStatusId
              ()) (Some Values.DescribeAgentStatusResponse.to_json)
           (Some Values.DescribeAgentStatusResponse.error_to_json)])
let describe_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_contact
           (Values.DescribeContactRequest.make ~instanceId ~contactId ())
           (Some Values.DescribeContactResponse.to_json)
           (Some Values.DescribeContactResponse.error_to_json)])
let describe_contact_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_contact_flow
           (Values.DescribeContactFlowRequest.make ~instanceId ~contactFlowId
              ()) (Some Values.DescribeContactFlowResponse.to_json)
           (Some Values.DescribeContactFlowResponse.error_to_json)])
let describe_contact_flow_module =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowModuleId =
         flag "contact-flow-module-id" (required string)
           ~doc:"STRING ContactFlowModuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_contact_flow_module
           (Values.DescribeContactFlowModuleRequest.make ~instanceId
              ~contactFlowModuleId ())
           (Some Values.DescribeContactFlowModuleResponse.to_json)
           (Some Values.DescribeContactFlowModuleResponse.error_to_json)])
let describe_hours_of_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and hoursOfOperationId =
         flag "hours-of-operation-id" (required string)
           ~doc:"STRING HoursOfOperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hours_of_operation
           (Values.DescribeHoursOfOperationRequest.make ~instanceId
              ~hoursOfOperationId ())
           (Some Values.DescribeHoursOfOperationResponse.to_json)
           (Some Values.DescribeHoursOfOperationResponse.error_to_json)])
let describe_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance
           (Values.DescribeInstanceRequest.make ~instanceId ())
           (Some Values.DescribeInstanceResponse.to_json)
           (Some Values.DescribeInstanceResponse.error_to_json)])
let describe_instance_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and attributeType =
         flag "attribute-type" (required json_arg)
           ~doc:"JSON InstanceAttributeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_attribute
           (Values.DescribeInstanceAttributeRequest.make ~instanceId
              ~attributeType:(Values.InstanceAttributeType.of_json
                                attributeType) ())
           (Some Values.DescribeInstanceAttributeResponse.to_json)
           (Some Values.DescribeInstanceAttributeResponse.error_to_json)])
let describe_instance_storage_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON InstanceStorageResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_storage_config
           (Values.DescribeInstanceStorageConfigRequest.make ~instanceId
              ~associationId
              ~resourceType:(Values.InstanceStorageResourceType.of_json
                               resourceType) ())
           (Some Values.DescribeInstanceStorageConfigResponse.to_json)
           (Some Values.DescribeInstanceStorageConfigResponse.error_to_json)])
let describe_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_queue
           (Values.DescribeQueueRequest.make ~instanceId ~queueId ())
           (Some Values.DescribeQueueResponse.to_json)
           (Some Values.DescribeQueueResponse.error_to_json)])
let describe_quick_connect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and quickConnectId =
         flag "quick-connect-id" (required string)
           ~doc:"STRING QuickConnectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_quick_connect
           (Values.DescribeQuickConnectRequest.make ~instanceId
              ~quickConnectId ())
           (Some Values.DescribeQuickConnectResponse.to_json)
           (Some Values.DescribeQuickConnectResponse.error_to_json)])
let describe_routing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_routing_profile
           (Values.DescribeRoutingProfileRequest.make ~instanceId
              ~routingProfileId ())
           (Some Values.DescribeRoutingProfileResponse.to_json)
           (Some Values.DescribeRoutingProfileResponse.error_to_json)])
let describe_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileId =
         flag "security-profile-id" (required string)
           ~doc:"STRING SecurityProfileId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_security_profile
           (Values.DescribeSecurityProfileRequest.make ~securityProfileId
              ~instanceId ())
           (Some Values.DescribeSecurityProfileResponse.to_json)
           (Some Values.DescribeSecurityProfileResponse.error_to_json)])
let describe_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user
           (Values.DescribeUserRequest.make ~userId ~instanceId ())
           (Some Values.DescribeUserResponse.to_json)
           (Some Values.DescribeUserResponse.error_to_json)])
let describe_user_hierarchy_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hierarchyGroupId =
         flag "hierarchy-group-id" (required string)
           ~doc:"STRING HierarchyGroupId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_hierarchy_group
           (Values.DescribeUserHierarchyGroupRequest.make ~hierarchyGroupId
              ~instanceId ())
           (Some Values.DescribeUserHierarchyGroupResponse.to_json)
           (Some Values.DescribeUserHierarchyGroupResponse.error_to_json)])
let describe_user_hierarchy_structure =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_hierarchy_structure
           (Values.DescribeUserHierarchyStructureRequest.make ~instanceId ())
           (Some Values.DescribeUserHierarchyStructureResponse.to_json)
           (Some Values.DescribeUserHierarchyStructureResponse.error_to_json)])
let describe_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and vocabularyId =
         flag "vocabulary-id" (required string) ~doc:"STRING VocabularyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_vocabulary
           (Values.DescribeVocabularyRequest.make ~instanceId ~vocabularyId
              ()) (Some Values.DescribeVocabularyResponse.to_json)
           (Some Values.DescribeVocabularyResponse.error_to_json)])
let disassociate_approved_origin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and origin = flag "origin" (required string) ~doc:"STRING Origin" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_approved_origin
           (Values.DisassociateApprovedOriginRequest.make ~instanceId ~origin
              ()) None None])
let disassociate_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lexBot = flag "lex-bot" (optional json_arg) ~doc:"JSON LexBot"
       and lexV2Bot =
         flag "lex-v2-bot" (optional json_arg) ~doc:"JSON LexV2Bot"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_bot
           (Values.DisassociateBotRequest.make
              ?lexBot:(Option.map ~f:Values.LexBot.of_json lexBot)
              ?lexV2Bot:(Option.map ~f:Values.LexV2Bot.of_json lexV2Bot)
              ~instanceId ()) None None])
let disassociate_instance_storage_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON InstanceStorageResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_instance_storage_config
           (Values.DisassociateInstanceStorageConfigRequest.make ~instanceId
              ~associationId
              ~resourceType:(Values.InstanceStorageResourceType.of_json
                               resourceType) ()) None None])
let disassociate_lambda_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and functionArn =
         flag "function-arn" (required string) ~doc:"STRING FunctionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_lambda_function
           (Values.DisassociateLambdaFunctionRequest.make ~instanceId
              ~functionArn ()) None None])
let disassociate_lex_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and botName = flag "bot-name" (required string) ~doc:"STRING BotName"
       and lexRegion =
         flag "lex-region" (required string) ~doc:"STRING LexRegion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_lex_bot
           (Values.DisassociateLexBotRequest.make ~instanceId ~botName
              ~lexRegion ()) None None])
let disassociate_queue_quick_connects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and quickConnectIds =
         flag "quick-connect-ids" (required json_arg)
           ~doc:"JSON QuickConnectsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_queue_quick_connects
           (Values.DisassociateQueueQuickConnectsRequest.make ~instanceId
              ~queueId
              ~quickConnectIds:(Values.QuickConnectsList.of_json
                                  quickConnectIds) ()) None None])
let disassociate_routing_profile_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and queueReferences =
         flag "queue-references" (required json_arg)
           ~doc:"JSON RoutingProfileQueueReferenceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_routing_profile_queues
           (Values.DisassociateRoutingProfileQueuesRequest.make ~instanceId
              ~routingProfileId
              ~queueReferences:(Values.RoutingProfileQueueReferenceList.of_json
                                  queueReferences) ()) None None])
let disassociate_security_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_security_key
           (Values.DisassociateSecurityKeyRequest.make ~instanceId
              ~associationId ()) None None])
let get_contact_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and initialContactId =
         flag "initial-contact-id" (required string) ~doc:"STRING ContactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact_attributes
           (Values.GetContactAttributesRequest.make ~instanceId
              ~initialContactId ())
           (Some Values.GetContactAttributesResponse.to_json)
           (Some Values.GetContactAttributesResponse.error_to_json)])
let get_current_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupings =
         flag "groupings" (optional json_arg) ~doc:"JSON Groupings"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and filters = flag "filters" (required json_arg) ~doc:"JSON Filters"
       and currentMetrics =
         flag "current-metrics" (required json_arg)
           ~doc:"JSON CurrentMetrics" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_current_metric_data
           (Values.GetCurrentMetricDataRequest.make
              ?groupings:(Option.map ~f:Values.Groupings.of_json groupings)
              ?nextToken ?maxResults ~instanceId
              ~filters:(Values.Filters.of_json filters)
              ~currentMetrics:(Values.CurrentMetrics.of_json currentMetrics)
              ()) (Some Values.GetCurrentMetricDataResponse.to_json)
           (Some Values.GetCurrentMetricDataResponse.error_to_json)])
let get_federation_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_federation_token
           (Values.GetFederationTokenRequest.make ~instanceId ())
           (Some Values.GetFederationTokenResponse.to_json)
           (Some Values.GetFederationTokenResponse.error_to_json)])
let get_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupings =
         flag "groupings" (optional json_arg) ~doc:"JSON Groupings"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp__lc1"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp__lc1"
       and filters = flag "filters" (required json_arg) ~doc:"JSON Filters"
       and historicalMetrics =
         flag "historical-metrics" (required json_arg)
           ~doc:"JSON HistoricalMetrics" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_data
           (Values.GetMetricDataRequest.make
              ?groupings:(Option.map ~f:Values.Groupings.of_json groupings)
              ?nextToken ?maxResults ~instanceId
              ~startTime:(Values.Timestamp__lc1.of_json startTime)
              ~endTime:(Values.Timestamp__lc1.of_json endTime)
              ~filters:(Values.Filters.of_json filters)
              ~historicalMetrics:(Values.HistoricalMetrics.of_json
                                    historicalMetrics) ())
           (Some Values.GetMetricDataResponse.to_json)
           (Some Values.GetMetricDataResponse.error_to_json)])
let list_agent_statuses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and agentStatusTypes =
         flag "agent-status-types" (optional json_arg)
           ~doc:"JSON AgentStatusTypes"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_statuses
           (Values.ListAgentStatusRequest.make ?nextToken ?maxResults
              ?agentStatusTypes:(Option.map
                                   ~f:Values.AgentStatusTypes.of_json
                                   agentStatusTypes) ~instanceId ())
           (Some Values.ListAgentStatusResponse.to_json)
           (Some Values.ListAgentStatusResponse.error_to_json)])
let list_approved_origins =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult25"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_approved_origins
           (Values.ListApprovedOriginsRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListApprovedOriginsResponse.to_json)
           (Some Values.ListApprovedOriginsResponse.error_to_json)])
let list_bots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult25"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and lexVersion =
         flag "lex-version" (required json_arg) ~doc:"JSON LexVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bots
           (Values.ListBotsRequest.make ?nextToken ?maxResults ~instanceId
              ~lexVersion:(Values.LexVersion.of_json lexVersion) ())
           (Some Values.ListBotsResponse.to_json)
           (Some Values.ListBotsResponse.error_to_json)])
let list_contact_flow_modules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and contactFlowModuleState =
         flag "contact-flow-module-state" (optional json_arg)
           ~doc:"JSON ContactFlowModuleState"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contact_flow_modules
           (Values.ListContactFlowModulesRequest.make ?nextToken ?maxResults
              ?contactFlowModuleState:(Option.map
                                         ~f:Values.ContactFlowModuleState.of_json
                                         contactFlowModuleState) ~instanceId
              ()) (Some Values.ListContactFlowModulesResponse.to_json)
           (Some Values.ListContactFlowModulesResponse.error_to_json)])
let list_contact_flows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactFlowTypes =
         flag "contact-flow-types" (optional json_arg)
           ~doc:"JSON ContactFlowTypes"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contact_flows
           (Values.ListContactFlowsRequest.make
              ?contactFlowTypes:(Option.map
                                   ~f:Values.ContactFlowTypes.of_json
                                   contactFlowTypes) ?nextToken ?maxResults
              ~instanceId ()) (Some Values.ListContactFlowsResponse.to_json)
           (Some Values.ListContactFlowsResponse.error_to_json)])
let list_contact_references =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and referenceTypes =
         flag "reference-types" (required json_arg)
           ~doc:"JSON ReferenceTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contact_references
           (Values.ListContactReferencesRequest.make ?nextToken ~instanceId
              ~contactId
              ~referenceTypes:(Values.ReferenceTypes.of_json referenceTypes)
              ()) (Some Values.ListContactReferencesResponse.to_json)
           (Some Values.ListContactReferencesResponse.error_to_json)])
let list_default_vocabularies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and languageCode =
         flag "language-code" (optional json_arg)
           ~doc:"JSON VocabularyLanguageCode"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING VocabularyNextToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_default_vocabularies
           (Values.ListDefaultVocabulariesRequest.make
              ?languageCode:(Option.map
                               ~f:Values.VocabularyLanguageCode.of_json
                               languageCode) ?maxResults ?nextToken
              ~instanceId ())
           (Some Values.ListDefaultVocabulariesResponse.to_json)
           (Some Values.ListDefaultVocabulariesResponse.error_to_json)])
let list_hours_of_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hours_of_operations
           (Values.ListHoursOfOperationsRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListHoursOfOperationsResponse.to_json)
           (Some Values.ListHoursOfOperationsResponse.error_to_json)])
let list_instance_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult7"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_attributes
           (Values.ListInstanceAttributesRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListInstanceAttributesResponse.to_json)
           (Some Values.ListInstanceAttributesResponse.error_to_json)])
let list_instance_storage_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult10"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON InstanceStorageResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_storage_configs
           (Values.ListInstanceStorageConfigsRequest.make ?nextToken
              ?maxResults ~instanceId
              ~resourceType:(Values.InstanceStorageResourceType.of_json
                               resourceType) ())
           (Some Values.ListInstanceStorageConfigsResponse.to_json)
           (Some Values.ListInstanceStorageConfigsResponse.error_to_json)])
let list_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult10" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instances
           (Values.ListInstancesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListInstancesResponse.to_json)
           (Some Values.ListInstancesResponse.error_to_json)])
let list_integration_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationType =
         flag "integration-type" (optional json_arg)
           ~doc:"JSON IntegrationType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_integration_associations
           (Values.ListIntegrationAssociationsRequest.make
              ?integrationType:(Option.map ~f:Values.IntegrationType.of_json
                                  integrationType) ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListIntegrationAssociationsResponse.to_json)
           (Some Values.ListIntegrationAssociationsResponse.error_to_json)])
let list_lambda_functions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult25"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lambda_functions
           (Values.ListLambdaFunctionsRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListLambdaFunctionsResponse.to_json)
           (Some Values.ListLambdaFunctionsResponse.error_to_json)])
let list_lex_bots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult25"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lex_bots
           (Values.ListLexBotsRequest.make ?nextToken ?maxResults ~instanceId
              ()) (Some Values.ListLexBotsResponse.to_json)
           (Some Values.ListLexBotsResponse.error_to_json)])
let list_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberTypes =
         flag "phone-number-types" (optional json_arg)
           ~doc:"JSON PhoneNumberTypes"
       and phoneNumberCountryCodes =
         flag "phone-number-country-codes" (optional json_arg)
           ~doc:"JSON PhoneNumberCountryCodes"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_phone_numbers
           (Values.ListPhoneNumbersRequest.make
              ?phoneNumberTypes:(Option.map
                                   ~f:Values.PhoneNumberTypes.of_json
                                   phoneNumberTypes)
              ?phoneNumberCountryCodes:(Option.map
                                          ~f:Values.PhoneNumberCountryCodes.of_json
                                          phoneNumberCountryCodes) ?nextToken
              ?maxResults ~instanceId ())
           (Some Values.ListPhoneNumbersResponse.to_json)
           (Some Values.ListPhoneNumbersResponse.error_to_json)])
let list_prompts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_prompts
           (Values.ListPromptsRequest.make ?nextToken ?maxResults ~instanceId
              ()) (Some Values.ListPromptsResponse.to_json)
           (Some Values.ListPromptsResponse.error_to_json)])
let list_queue_quick_connects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queue_quick_connects
           (Values.ListQueueQuickConnectsRequest.make ?nextToken ?maxResults
              ~instanceId ~queueId ())
           (Some Values.ListQueueQuickConnectsResponse.to_json)
           (Some Values.ListQueueQuickConnectsResponse.error_to_json)])
let list_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueTypes =
         flag "queue-types" (optional json_arg) ~doc:"JSON QueueTypes"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queues
           (Values.ListQueuesRequest.make
              ?queueTypes:(Option.map ~f:Values.QueueTypes.of_json queueTypes)
              ?nextToken ?maxResults ~instanceId ())
           (Some Values.ListQueuesResponse.to_json)
           (Some Values.ListQueuesResponse.error_to_json)])
let list_quick_connects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and quickConnectTypes =
         flag "quick-connect-types" (optional json_arg)
           ~doc:"JSON QuickConnectTypes"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_quick_connects
           (Values.ListQuickConnectsRequest.make ?nextToken ?maxResults
              ?quickConnectTypes:(Option.map
                                    ~f:Values.QuickConnectTypes.of_json
                                    quickConnectTypes) ~instanceId ())
           (Some Values.ListQuickConnectsResponse.to_json)
           (Some Values.ListQuickConnectsResponse.error_to_json)])
let list_routing_profile_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routing_profile_queues
           (Values.ListRoutingProfileQueuesRequest.make ?nextToken
              ?maxResults ~instanceId ~routingProfileId ())
           (Some Values.ListRoutingProfileQueuesResponse.to_json)
           (Some Values.ListRoutingProfileQueuesResponse.error_to_json)])
let list_routing_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routing_profiles
           (Values.ListRoutingProfilesRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListRoutingProfilesResponse.to_json)
           (Some Values.ListRoutingProfilesResponse.error_to_json)])
let list_security_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult2"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_keys
           (Values.ListSecurityKeysRequest.make ?nextToken ?maxResults
              ~instanceId ()) (Some Values.ListSecurityKeysResponse.to_json)
           (Some Values.ListSecurityKeysResponse.error_to_json)])
let list_security_profile_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and securityProfileId =
         flag "security-profile-id" (required string)
           ~doc:"STRING SecurityProfileId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_profile_permissions
           (Values.ListSecurityProfilePermissionsRequest.make ?nextToken
              ?maxResults ~securityProfileId ~instanceId ())
           (Some Values.ListSecurityProfilePermissionsResponse.to_json)
           (Some Values.ListSecurityProfilePermissionsResponse.error_to_json)])
let list_security_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_profiles
           (Values.ListSecurityProfilesRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListSecurityProfilesResponse.to_json)
           (Some Values.ListSecurityProfilesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_use_cases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and integrationAssociationId =
         flag "integration-association-id" (required string)
           ~doc:"STRING IntegrationAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_use_cases
           (Values.ListUseCasesRequest.make ?nextToken ?maxResults
              ~instanceId ~integrationAssociationId ())
           (Some Values.ListUseCasesResponse.to_json)
           (Some Values.ListUseCasesResponse.error_to_json)])
let list_user_hierarchy_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_hierarchy_groups
           (Values.ListUserHierarchyGroupsRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListUserHierarchyGroupsResponse.to_json)
           (Some Values.ListUserHierarchyGroupsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult1000"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?nextToken ?maxResults ~instanceId
              ()) (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let resume_contact_recording =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and initialContactId =
         flag "initial-contact-id" (required string) ~doc:"STRING ContactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_contact_recording
           (Values.ResumeContactRecordingRequest.make ~instanceId ~contactId
              ~initialContactId ())
           (Some Values.ResumeContactRecordingResponse.to_json)
           (Some Values.ResumeContactRecordingResponse.error_to_json)])
let search_vocabularies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResult100"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING VocabularyNextToken"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON VocabularyState"
       and nameStartsWith =
         flag "name-starts-with" (optional string)
           ~doc:"STRING VocabularyName"
       and languageCode =
         flag "language-code" (optional json_arg)
           ~doc:"JSON VocabularyLanguageCode"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_vocabularies
           (Values.SearchVocabulariesRequest.make ?maxResults ?nextToken
              ?state:(Option.map ~f:Values.VocabularyState.of_json state)
              ?nameStartsWith
              ?languageCode:(Option.map
                               ~f:Values.VocabularyLanguageCode.of_json
                               languageCode) ~instanceId ())
           (Some Values.SearchVocabulariesResponse.to_json)
           (Some Values.SearchVocabulariesResponse.error_to_json)])
let start_chat_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON Attributes"
       and initialMessage =
         flag "initial-message" (optional json_arg) ~doc:"JSON ChatMessage"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and chatDurationInMinutes =
         flag "chat-duration-in-minutes" (optional int)
           ~doc:"INT ChatDurationInMinutes"
       and supportedMessagingContentTypes =
         flag "supported-messaging-content-types" (optional json_arg)
           ~doc:"JSON SupportedMessagingContentTypes"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId"
       and participantDetails =
         flag "participant-details" (required json_arg)
           ~doc:"JSON ParticipantDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_chat_contact
           (Values.StartChatContactRequest.make
              ?attributes:(Option.map ~f:Values.Attributes.of_json attributes)
              ?initialMessage:(Option.map ~f:Values.ChatMessage.of_json
                                 initialMessage) ?clientToken
              ?chatDurationInMinutes
              ?supportedMessagingContentTypes:(Option.map
                                                 ~f:Values.SupportedMessagingContentTypes.of_json
                                                 supportedMessagingContentTypes)
              ~instanceId ~contactFlowId
              ~participantDetails:(Values.ParticipantDetails.of_json
                                     participantDetails) ())
           (Some Values.StartChatContactResponse.to_json)
           (Some Values.StartChatContactResponse.error_to_json)])
let start_contact_recording =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and initialContactId =
         flag "initial-contact-id" (required string) ~doc:"STRING ContactId"
       and voiceRecordingConfiguration =
         flag "voice-recording-configuration" (required json_arg)
           ~doc:"JSON VoiceRecordingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_contact_recording
           (Values.StartContactRecordingRequest.make ~instanceId ~contactId
              ~initialContactId
              ~voiceRecordingConfiguration:(Values.VoiceRecordingConfiguration.of_json
                                              voiceRecordingConfiguration) ())
           (Some Values.StartContactRecordingResponse.to_json)
           (Some Values.StartContactRecordingResponse.error_to_json)])
let start_contact_streaming =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and chatStreamingConfiguration =
         flag "chat-streaming-configuration" (required json_arg)
           ~doc:"JSON ChatStreamingConfiguration"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_contact_streaming
           (Values.StartContactStreamingRequest.make ~instanceId ~contactId
              ~chatStreamingConfiguration:(Values.ChatStreamingConfiguration.of_json
                                             chatStreamingConfiguration)
              ~clientToken ())
           (Some Values.StartContactStreamingResponse.to_json)
           (Some Values.StartContactStreamingResponse.error_to_json)])
let start_outbound_voice_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sourcePhoneNumber =
         flag "source-phone-number" (optional string)
           ~doc:"STRING PhoneNumber"
       and queueId = flag "queue-id" (optional string) ~doc:"STRING QueueId"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON Attributes"
       and answerMachineDetectionConfig =
         flag "answer-machine-detection-config" (optional json_arg)
           ~doc:"JSON AnswerMachineDetectionConfig"
       and campaignId =
         flag "campaign-id" (optional string) ~doc:"STRING CampaignId"
       and trafficType =
         flag "traffic-type" (optional json_arg) ~doc:"JSON TrafficType"
       and destinationPhoneNumber =
         flag "destination-phone-number" (required string)
           ~doc:"STRING PhoneNumber"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_outbound_voice_contact
           (Values.StartOutboundVoiceContactRequest.make ?clientToken
              ?sourcePhoneNumber ?queueId
              ?attributes:(Option.map ~f:Values.Attributes.of_json attributes)
              ?answerMachineDetectionConfig:(Option.map
                                               ~f:Values.AnswerMachineDetectionConfig.of_json
                                               answerMachineDetectionConfig)
              ?campaignId
              ?trafficType:(Option.map ~f:Values.TrafficType.of_json
                              trafficType) ~destinationPhoneNumber
              ~contactFlowId ~instanceId ())
           (Some Values.StartOutboundVoiceContactResponse.to_json)
           (Some Values.StartOutboundVoiceContactResponse.error_to_json)])
let start_task_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and previousContactId =
         flag "previous-contact-id" (optional string) ~doc:"STRING ContactId"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON Attributes"
       and references =
         flag "references" (optional json_arg) ~doc:"JSON ContactReferences"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and scheduledTime =
         flag "scheduled-time" (optional json_arg) ~doc:"JSON Timestamp"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_task_contact
           (Values.StartTaskContactRequest.make ?previousContactId
              ?attributes:(Option.map ~f:Values.Attributes.of_json attributes)
              ?references:(Option.map ~f:Values.ContactReferences.of_json
                             references) ?description ?clientToken
              ?scheduledTime:(Option.map ~f:Values.Timestamp.of_json
                                scheduledTime) ~instanceId ~contactFlowId
              ~name ()) (Some Values.StartTaskContactResponse.to_json)
           (Some Values.StartTaskContactResponse.error_to_json)])
let stop_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_contact
           (Values.StopContactRequest.make ~contactId ~instanceId ())
           (Some Values.StopContactResponse.to_json)
           (Some Values.StopContactResponse.error_to_json)])
let stop_contact_recording =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and initialContactId =
         flag "initial-contact-id" (required string) ~doc:"STRING ContactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_contact_recording
           (Values.StopContactRecordingRequest.make ~instanceId ~contactId
              ~initialContactId ())
           (Some Values.StopContactRecordingResponse.to_json)
           (Some Values.StopContactRecordingResponse.error_to_json)])
let stop_contact_streaming =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and streamingId =
         flag "streaming-id" (required string) ~doc:"STRING StreamingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_contact_streaming
           (Values.StopContactStreamingRequest.make ~instanceId ~contactId
              ~streamingId ())
           (Some Values.StopContactStreamingResponse.to_json)
           (Some Values.StopContactStreamingResponse.error_to_json)])
let suspend_contact_recording =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and initialContactId =
         flag "initial-contact-id" (required string) ~doc:"STRING ContactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.suspend_contact_recording
           (Values.SuspendContactRecordingRequest.make ~instanceId ~contactId
              ~initialContactId ())
           (Some Values.SuspendContactRecordingResponse.to_json)
           (Some Values.SuspendContactRecordingResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_agent_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AgentStatusName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateAgentStatusDescription"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON AgentStatusState"
       and displayOrder =
         flag "display-order" (optional int)
           ~doc:"INT AgentStatusOrderNumber"
       and resetOrderNumber =
         flag "reset-order-number" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and agentStatusId =
         flag "agent-status-id" (required string) ~doc:"STRING AgentStatusId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_status
           (Values.UpdateAgentStatusRequest.make ?name ?description
              ?state:(Option.map ~f:Values.AgentStatusState.of_json state)
              ?displayOrder ?resetOrderNumber ~instanceId ~agentStatusId ())
           None None])
let update_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and references =
         flag "references" (optional json_arg) ~doc:"JSON ContactReferences"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact
           (Values.UpdateContactRequest.make ?name ?description
              ?references:(Option.map ~f:Values.ContactReferences.of_json
                             references) ~instanceId ~contactId ())
           (Some Values.UpdateContactResponse.to_json)
           (Some Values.UpdateContactResponse.error_to_json)])
let update_contact_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and initialContactId =
         flag "initial-contact-id" (required string) ~doc:"STRING ContactId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_attributes
           (Values.UpdateContactAttributesRequest.make ~initialContactId
              ~instanceId ~attributes:(Values.Attributes.of_json attributes)
              ()) (Some Values.UpdateContactAttributesResponse.to_json)
           (Some Values.UpdateContactAttributesResponse.error_to_json)])
let update_contact_flow_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId"
       and content =
         flag "content" (required string) ~doc:"STRING ContactFlowContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_flow_content
           (Values.UpdateContactFlowContentRequest.make ~instanceId
              ~contactFlowId ~content ()) None None])
let update_contact_flow_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ContactFlowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ContactFlowDescription"
       and contactFlowState =
         flag "contact-flow-state" (optional json_arg)
           ~doc:"JSON ContactFlowState"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_flow_metadata
           (Values.UpdateContactFlowMetadataRequest.make ?name ?description
              ?contactFlowState:(Option.map
                                   ~f:Values.ContactFlowState.of_json
                                   contactFlowState) ~instanceId
              ~contactFlowId ()) None None])
let update_contact_flow_module_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowModuleId =
         flag "contact-flow-module-id" (required string)
           ~doc:"STRING ContactFlowModuleId"
       and content =
         flag "content" (required string)
           ~doc:"STRING ContactFlowModuleContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_flow_module_content
           (Values.UpdateContactFlowModuleContentRequest.make ~instanceId
              ~contactFlowModuleId ~content ())
           (Some Values.UpdateContactFlowModuleContentResponse.to_json)
           (Some Values.UpdateContactFlowModuleContentResponse.error_to_json)])
let update_contact_flow_module_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ContactFlowModuleName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ContactFlowModuleDescription"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON ContactFlowModuleState"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowModuleId =
         flag "contact-flow-module-id" (required string)
           ~doc:"STRING ContactFlowModuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_flow_module_metadata
           (Values.UpdateContactFlowModuleMetadataRequest.make ?name
              ?description
              ?state:(Option.map ~f:Values.ContactFlowModuleState.of_json
                        state) ~instanceId ~contactFlowModuleId ())
           (Some Values.UpdateContactFlowModuleMetadataResponse.to_json)
           (Some Values.UpdateContactFlowModuleMetadataResponse.error_to_json)])
let update_contact_flow_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ContactFlowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ContactFlowDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactFlowId =
         flag "contact-flow-id" (required string) ~doc:"STRING ContactFlowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_flow_name
           (Values.UpdateContactFlowNameRequest.make ?name ?description
              ~instanceId ~contactFlowId ()) None None])
let update_contact_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING ContactId"
       and scheduledTime =
         flag "scheduled-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_schedule
           (Values.UpdateContactScheduleRequest.make ~instanceId ~contactId
              ~scheduledTime:(Values.Timestamp.of_json scheduledTime) ())
           (Some Values.UpdateContactScheduleResponse.to_json)
           (Some Values.UpdateContactScheduleResponse.error_to_json)])
let update_hours_of_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING CommonNameLength127"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateHoursOfOperationDescription"
       and timeZone =
         flag "time-zone" (optional string) ~doc:"STRING TimeZone"
       and config =
         flag "config" (optional json_arg)
           ~doc:"JSON HoursOfOperationConfigList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and hoursOfOperationId =
         flag "hours-of-operation-id" (required string)
           ~doc:"STRING HoursOfOperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_hours_of_operation
           (Values.UpdateHoursOfOperationRequest.make ?name ?description
              ?timeZone
              ?config:(Option.map
                         ~f:Values.HoursOfOperationConfigList.of_json config)
              ~instanceId ~hoursOfOperationId ()) None None])
let update_instance_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and attributeType =
         flag "attribute-type" (required json_arg)
           ~doc:"JSON InstanceAttributeType"
       and value =
         flag "value" (required string) ~doc:"STRING InstanceAttributeValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance_attribute
           (Values.UpdateInstanceAttributeRequest.make ~instanceId
              ~attributeType:(Values.InstanceAttributeType.of_json
                                attributeType) ~value ()) None None])
let update_instance_storage_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON InstanceStorageResourceType"
       and storageConfig =
         flag "storage-config" (required json_arg)
           ~doc:"JSON InstanceStorageConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance_storage_config
           (Values.UpdateInstanceStorageConfigRequest.make ~instanceId
              ~associationId
              ~resourceType:(Values.InstanceStorageResourceType.of_json
                               resourceType)
              ~storageConfig:(Values.InstanceStorageConfig.of_json
                                storageConfig) ()) None None])
let update_queue_hours_of_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and hoursOfOperationId =
         flag "hours-of-operation-id" (required string)
           ~doc:"STRING HoursOfOperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_hours_of_operation
           (Values.UpdateQueueHoursOfOperationRequest.make ~instanceId
              ~queueId ~hoursOfOperationId ()) None None])
let update_queue_max_contacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxContacts =
         flag "max-contacts" (optional int) ~doc:"INT QueueMaxContacts"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_max_contacts
           (Values.UpdateQueueMaxContactsRequest.make ?maxContacts
              ~instanceId ~queueId ()) None None])
let update_queue_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING CommonNameLength127"
       and description =
         flag "description" (optional string) ~doc:"STRING QueueDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_name
           (Values.UpdateQueueNameRequest.make ?name ?description ~instanceId
              ~queueId ()) None None])
let update_queue_outbound_caller_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and outboundCallerConfig =
         flag "outbound-caller-config" (required json_arg)
           ~doc:"JSON OutboundCallerConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_outbound_caller_config
           (Values.UpdateQueueOutboundCallerConfigRequest.make ~instanceId
              ~queueId
              ~outboundCallerConfig:(Values.OutboundCallerConfig.of_json
                                       outboundCallerConfig) ()) None None])
let update_queue_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and status = flag "status" (required json_arg) ~doc:"JSON QueueStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_status
           (Values.UpdateQueueStatusRequest.make ~instanceId ~queueId
              ~status:(Values.QueueStatus.of_json status) ()) None None])
let update_quick_connect_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and quickConnectId =
         flag "quick-connect-id" (required string)
           ~doc:"STRING QuickConnectId"
       and quickConnectConfig =
         flag "quick-connect-config" (required json_arg)
           ~doc:"JSON QuickConnectConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_quick_connect_config
           (Values.UpdateQuickConnectConfigRequest.make ~instanceId
              ~quickConnectId
              ~quickConnectConfig:(Values.QuickConnectConfig.of_json
                                     quickConnectConfig) ()) None None])
let update_quick_connect_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING QuickConnectName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateQuickConnectDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and quickConnectId =
         flag "quick-connect-id" (required string)
           ~doc:"STRING QuickConnectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_quick_connect_name
           (Values.UpdateQuickConnectNameRequest.make ?name ?description
              ~instanceId ~quickConnectId ()) None None])
let update_routing_profile_concurrency =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and mediaConcurrencies =
         flag "media-concurrencies" (required json_arg)
           ~doc:"JSON MediaConcurrencies" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_profile_concurrency
           (Values.UpdateRoutingProfileConcurrencyRequest.make ~instanceId
              ~routingProfileId
              ~mediaConcurrencies:(Values.MediaConcurrencies.of_json
                                     mediaConcurrencies) ()) None None])
let update_routing_profile_default_outbound_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and defaultOutboundQueueId =
         flag "default-outbound-queue-id" (required string)
           ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_profile_default_outbound_queue
           (Values.UpdateRoutingProfileDefaultOutboundQueueRequest.make
              ~instanceId ~routingProfileId ~defaultOutboundQueueId ()) None
           None])
let update_routing_profile_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING RoutingProfileName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING RoutingProfileDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_profile_name
           (Values.UpdateRoutingProfileNameRequest.make ?name ?description
              ~instanceId ~routingProfileId ()) None None])
let update_routing_profile_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and queueConfigs =
         flag "queue-configs" (required json_arg)
           ~doc:"JSON RoutingProfileQueueConfigList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_profile_queues
           (Values.UpdateRoutingProfileQueuesRequest.make ~instanceId
              ~routingProfileId
              ~queueConfigs:(Values.RoutingProfileQueueConfigList.of_json
                               queueConfigs) ()) None None])
let update_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SecurityProfileDescription"
       and permissions =
         flag "permissions" (optional json_arg) ~doc:"JSON PermissionsList"
       and securityProfileId =
         flag "security-profile-id" (required string)
           ~doc:"STRING SecurityProfileId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security_profile
           (Values.UpdateSecurityProfileRequest.make ?description
              ?permissions:(Option.map ~f:Values.PermissionsList.of_json
                              permissions) ~securityProfileId ~instanceId ())
           None None])
let update_user_hierarchy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hierarchyGroupId =
         flag "hierarchy-group-id" (optional string)
           ~doc:"STRING HierarchyGroupId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_hierarchy
           (Values.UpdateUserHierarchyRequest.make ?hierarchyGroupId ~userId
              ~instanceId ()) None None])
let update_user_hierarchy_group_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING HierarchyGroupName"
       and hierarchyGroupId =
         flag "hierarchy-group-id" (required string)
           ~doc:"STRING HierarchyGroupId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_hierarchy_group_name
           (Values.UpdateUserHierarchyGroupNameRequest.make ~name
              ~hierarchyGroupId ~instanceId ()) None None])
let update_user_hierarchy_structure =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hierarchyStructure =
         flag "hierarchy-structure" (required json_arg)
           ~doc:"JSON HierarchyStructureUpdate"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_hierarchy_structure
           (Values.UpdateUserHierarchyStructureRequest.make
              ~hierarchyStructure:(Values.HierarchyStructureUpdate.of_json
                                     hierarchyStructure) ~instanceId ()) None
           None])
let update_user_identity_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityInfo =
         flag "identity-info" (required json_arg)
           ~doc:"JSON UserIdentityInfo"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_identity_info
           (Values.UpdateUserIdentityInfoRequest.make
              ~identityInfo:(Values.UserIdentityInfo.of_json identityInfo)
              ~userId ~instanceId ()) None None])
let update_user_phone_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneConfig =
         flag "phone-config" (required json_arg) ~doc:"JSON UserPhoneConfig"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_phone_config
           (Values.UpdateUserPhoneConfigRequest.make
              ~phoneConfig:(Values.UserPhoneConfig.of_json phoneConfig)
              ~userId ~instanceId ()) None None])
let update_user_routing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingProfileId =
         flag "routing-profile-id" (required string)
           ~doc:"STRING RoutingProfileId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_routing_profile
           (Values.UpdateUserRoutingProfileRequest.make ~routingProfileId
              ~userId ~instanceId ()) None None])
let update_user_security_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileIds =
         flag "security-profile-ids" (required json_arg)
           ~doc:"JSON SecurityProfileIds"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_security_profiles
           (Values.UpdateUserSecurityProfilesRequest.make
              ~securityProfileIds:(Values.SecurityProfileIds.of_json
                                     securityProfileIds) ~userId ~instanceId
              ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-approved-origin", associate_approved_origin);
    ("associate-bot", associate_bot);
    ("associate-default-vocabulary", associate_default_vocabulary);
    ("associate-instance-storage-config", associate_instance_storage_config);
    ("associate-lambda-function", associate_lambda_function);
    ("associate-lex-bot", associate_lex_bot);
    ("associate-queue-quick-connects", associate_queue_quick_connects);
    ("associate-routing-profile-queues", associate_routing_profile_queues);
    ("associate-security-key", associate_security_key);
    ("create-agent-status", create_agent_status);
    ("create-contact-flow", create_contact_flow);
    ("create-contact-flow-module", create_contact_flow_module);
    ("create-hours-of-operation", create_hours_of_operation);
    ("create-instance", create_instance);
    ("create-integration-association", create_integration_association);
    ("create-queue", create_queue);
    ("create-quick-connect", create_quick_connect);
    ("create-routing-profile", create_routing_profile);
    ("create-security-profile", create_security_profile);
    ("create-use-case", create_use_case);
    ("create-user", create_user);
    ("create-user-hierarchy-group", create_user_hierarchy_group);
    ("create-vocabulary", create_vocabulary);
    ("delete-contact-flow", delete_contact_flow);
    ("delete-contact-flow-module", delete_contact_flow_module);
    ("delete-hours-of-operation", delete_hours_of_operation);
    ("delete-instance", delete_instance);
    ("delete-integration-association", delete_integration_association);
    ("delete-quick-connect", delete_quick_connect);
    ("delete-security-profile", delete_security_profile);
    ("delete-use-case", delete_use_case);
    ("delete-user", delete_user);
    ("delete-user-hierarchy-group", delete_user_hierarchy_group);
    ("delete-vocabulary", delete_vocabulary);
    ("describe-agent-status", describe_agent_status);
    ("describe-contact", describe_contact);
    ("describe-contact-flow", describe_contact_flow);
    ("describe-contact-flow-module", describe_contact_flow_module);
    ("describe-hours-of-operation", describe_hours_of_operation);
    ("describe-instance", describe_instance);
    ("describe-instance-attribute", describe_instance_attribute);
    ("describe-instance-storage-config", describe_instance_storage_config);
    ("describe-queue", describe_queue);
    ("describe-quick-connect", describe_quick_connect);
    ("describe-routing-profile", describe_routing_profile);
    ("describe-security-profile", describe_security_profile);
    ("describe-user", describe_user);
    ("describe-user-hierarchy-group", describe_user_hierarchy_group);
    ("describe-user-hierarchy-structure", describe_user_hierarchy_structure);
    ("describe-vocabulary", describe_vocabulary);
    ("disassociate-approved-origin", disassociate_approved_origin);
    ("disassociate-bot", disassociate_bot);
    ("disassociate-instance-storage-config",
      disassociate_instance_storage_config);
    ("disassociate-lambda-function", disassociate_lambda_function);
    ("disassociate-lex-bot", disassociate_lex_bot);
    ("disassociate-queue-quick-connects", disassociate_queue_quick_connects);
    ("disassociate-routing-profile-queues",
      disassociate_routing_profile_queues);
    ("disassociate-security-key", disassociate_security_key);
    ("get-contact-attributes", get_contact_attributes);
    ("get-current-metric-data", get_current_metric_data);
    ("get-federation-token", get_federation_token);
    ("get-metric-data", get_metric_data);
    ("list-agent-statuses", list_agent_statuses);
    ("list-approved-origins", list_approved_origins);
    ("list-bots", list_bots);
    ("list-contact-flow-modules", list_contact_flow_modules);
    ("list-contact-flows", list_contact_flows);
    ("list-contact-references", list_contact_references);
    ("list-default-vocabularies", list_default_vocabularies);
    ("list-hours-of-operations", list_hours_of_operations);
    ("list-instance-attributes", list_instance_attributes);
    ("list-instance-storage-configs", list_instance_storage_configs);
    ("list-instances", list_instances);
    ("list-integration-associations", list_integration_associations);
    ("list-lambda-functions", list_lambda_functions);
    ("list-lex-bots", list_lex_bots);
    ("list-phone-numbers", list_phone_numbers);
    ("list-prompts", list_prompts);
    ("list-queue-quick-connects", list_queue_quick_connects);
    ("list-queues", list_queues);
    ("list-quick-connects", list_quick_connects);
    ("list-routing-profile-queues", list_routing_profile_queues);
    ("list-routing-profiles", list_routing_profiles);
    ("list-security-keys", list_security_keys);
    ("list-security-profile-permissions", list_security_profile_permissions);
    ("list-security-profiles", list_security_profiles);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-use-cases", list_use_cases);
    ("list-user-hierarchy-groups", list_user_hierarchy_groups);
    ("list-users", list_users);
    ("resume-contact-recording", resume_contact_recording);
    ("search-vocabularies", search_vocabularies);
    ("start-chat-contact", start_chat_contact);
    ("start-contact-recording", start_contact_recording);
    ("start-contact-streaming", start_contact_streaming);
    ("start-outbound-voice-contact", start_outbound_voice_contact);
    ("start-task-contact", start_task_contact);
    ("stop-contact", stop_contact);
    ("stop-contact-recording", stop_contact_recording);
    ("stop-contact-streaming", stop_contact_streaming);
    ("suspend-contact-recording", suspend_contact_recording);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-agent-status", update_agent_status);
    ("update-contact", update_contact);
    ("update-contact-attributes", update_contact_attributes);
    ("update-contact-flow-content", update_contact_flow_content);
    ("update-contact-flow-metadata", update_contact_flow_metadata);
    ("update-contact-flow-module-content",
      update_contact_flow_module_content);
    ("update-contact-flow-module-metadata",
      update_contact_flow_module_metadata);
    ("update-contact-flow-name", update_contact_flow_name);
    ("update-contact-schedule", update_contact_schedule);
    ("update-hours-of-operation", update_hours_of_operation);
    ("update-instance-attribute", update_instance_attribute);
    ("update-instance-storage-config", update_instance_storage_config);
    ("update-queue-hours-of-operation", update_queue_hours_of_operation);
    ("update-queue-max-contacts", update_queue_max_contacts);
    ("update-queue-name", update_queue_name);
    ("update-queue-outbound-caller-config",
      update_queue_outbound_caller_config);
    ("update-queue-status", update_queue_status);
    ("update-quick-connect-config", update_quick_connect_config);
    ("update-quick-connect-name", update_quick_connect_name);
    ("update-routing-profile-concurrency",
      update_routing_profile_concurrency);
    ("update-routing-profile-default-outbound-queue",
      update_routing_profile_default_outbound_queue);
    ("update-routing-profile-name", update_routing_profile_name);
    ("update-routing-profile-queues", update_routing_profile_queues);
    ("update-security-profile", update_security_profile);
    ("update-user-hierarchy", update_user_hierarchy);
    ("update-user-hierarchy-group-name", update_user_hierarchy_group_name);
    ("update-user-hierarchy-structure", update_user_hierarchy_structure);
    ("update-user-identity-info", update_user_identity_info);
    ("update-user-phone-config", update_user_phone_config);
    ("update-user-routing-profile", update_user_routing_profile);
    ("update-user-security-profiles", update_user_security_profiles)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_agent_collaborator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relayConversationHistory =
         flag "relay-conversation-history" (optional json_arg)
           ~doc:"JSON RelayConversationHistory"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and agentDescriptor =
         flag "agent-descriptor" (required json_arg)
           ~doc:"JSON AgentDescriptor"
       and collaboratorName =
         flag "collaborator-name" (required string) ~doc:"STRING Name"
       and collaborationInstruction =
         flag "collaboration-instruction" (required string)
           ~doc:"STRING CollaborationInstruction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_agent_collaborator
           (Values.AssociateAgentCollaboratorRequest.make
              ?relayConversationHistory:(Option.map
                                           ~f:Values.RelayConversationHistory.of_json
                                           relayConversationHistory)
              ?clientToken ~agentId ~agentVersion
              ~agentDescriptor:(Values.AgentDescriptor.of_json
                                  agentDescriptor) ~collaboratorName
              ~collaborationInstruction ())
           (Some Values.AssociateAgentCollaboratorResponse.to_json)
           (Some Values.AssociateAgentCollaboratorResponse.error_to_json)])
let associate_agent_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseState =
         flag "knowledge-base-state" (optional json_arg)
           ~doc:"JSON KnowledgeBaseState"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and description =
         flag "description" (required string) ~doc:"STRING Description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_agent_knowledge_base
           (Values.AssociateAgentKnowledgeBaseRequest.make
              ?knowledgeBaseState:(Option.map
                                     ~f:Values.KnowledgeBaseState.of_json
                                     knowledgeBaseState) ~agentId
              ~agentVersion ~knowledgeBaseId ~description ())
           (Some Values.AssociateAgentKnowledgeBaseResponse.to_json)
           (Some Values.AssociateAgentKnowledgeBaseResponse.error_to_json)])
let create_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and instruction =
         flag "instruction" (optional string) ~doc:"STRING Instruction"
       and foundationModel =
         flag "foundation-model" (optional string)
           ~doc:"STRING ModelIdentifier"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and orchestrationType =
         flag "orchestration-type" (optional json_arg)
           ~doc:"JSON OrchestrationType"
       and customOrchestration =
         flag "custom-orchestration" (optional json_arg)
           ~doc:"JSON CustomOrchestration"
       and idleSessionTTLInSeconds =
         flag "idle-session-t-t-l-in-seconds" (optional int)
           ~doc:"INT SessionTTL"
       and agentResourceRoleArn =
         flag "agent-resource-role-arn" (optional string)
           ~doc:"STRING AgentRoleArn"
       and customerEncryptionKeyArn =
         flag "customer-encryption-key-arn" (optional string)
           ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and promptOverrideConfiguration =
         flag "prompt-override-configuration" (optional json_arg)
           ~doc:"JSON PromptOverrideConfiguration"
       and guardrailConfiguration =
         flag "guardrail-configuration" (optional json_arg)
           ~doc:"JSON GuardrailConfiguration"
       and memoryConfiguration =
         flag "memory-configuration" (optional json_arg)
           ~doc:"JSON MemoryConfiguration"
       and agentCollaboration =
         flag "agent-collaboration" (optional json_arg)
           ~doc:"JSON AgentCollaboration"
       and agentName = flag "agent-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent
           (Values.CreateAgentRequest.make ?clientToken ?instruction
              ?foundationModel ?description
              ?orchestrationType:(Option.map
                                    ~f:Values.OrchestrationType.of_json
                                    orchestrationType)
              ?customOrchestration:(Option.map
                                      ~f:Values.CustomOrchestration.of_json
                                      customOrchestration)
              ?idleSessionTTLInSeconds ?agentResourceRoleArn
              ?customerEncryptionKeyArn
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?promptOverrideConfiguration:(Option.map
                                              ~f:Values.PromptOverrideConfiguration.of_json
                                              promptOverrideConfiguration)
              ?guardrailConfiguration:(Option.map
                                         ~f:Values.GuardrailConfiguration.of_json
                                         guardrailConfiguration)
              ?memoryConfiguration:(Option.map
                                      ~f:Values.MemoryConfiguration.of_json
                                      memoryConfiguration)
              ?agentCollaboration:(Option.map
                                     ~f:Values.AgentCollaboration.of_json
                                     agentCollaboration) ~agentName ())
           (Some Values.CreateAgentResponse.to_json)
           (Some Values.CreateAgentResponse.error_to_json)])
let create_agent_action_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and parentActionGroupSignature =
         flag "parent-action-group-signature" (optional json_arg)
           ~doc:"JSON ActionGroupSignature"
       and parentActionGroupSignatureParams =
         flag "parent-action-group-signature-params" (optional json_arg)
           ~doc:"JSON ActionGroupSignatureParams"
       and actionGroupExecutor =
         flag "action-group-executor" (optional json_arg)
           ~doc:"JSON ActionGroupExecutor"
       and apiSchema =
         flag "api-schema" (optional json_arg) ~doc:"JSON APISchema"
       and actionGroupState =
         flag "action-group-state" (optional json_arg)
           ~doc:"JSON ActionGroupState"
       and functionSchema =
         flag "function-schema" (optional json_arg)
           ~doc:"JSON FunctionSchema"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and actionGroupName =
         flag "action-group-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_action_group
           (Values.CreateAgentActionGroupRequest.make ?clientToken
              ?description
              ?parentActionGroupSignature:(Option.map
                                             ~f:Values.ActionGroupSignature.of_json
                                             parentActionGroupSignature)
              ?parentActionGroupSignatureParams:(Option.map
                                                   ~f:Values.ActionGroupSignatureParams.of_json
                                                   parentActionGroupSignatureParams)
              ?actionGroupExecutor:(Option.map
                                      ~f:Values.ActionGroupExecutor.of_json
                                      actionGroupExecutor)
              ?apiSchema:(Option.map ~f:Values.APISchema.of_json apiSchema)
              ?actionGroupState:(Option.map
                                   ~f:Values.ActionGroupState.of_json
                                   actionGroupState)
              ?functionSchema:(Option.map ~f:Values.FunctionSchema.of_json
                                 functionSchema) ~agentId ~agentVersion
              ~actionGroupName ())
           (Some Values.CreateAgentActionGroupResponse.to_json)
           (Some Values.CreateAgentActionGroupResponse.error_to_json)])
let create_agent_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and routingConfiguration =
         flag "routing-configuration" (optional json_arg)
           ~doc:"JSON AgentAliasRoutingConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentAliasName =
         flag "agent-alias-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_alias
           (Values.CreateAgentAliasRequest.make ?clientToken ?description
              ?routingConfiguration:(Option.map
                                       ~f:Values.AgentAliasRoutingConfiguration.of_json
                                       routingConfiguration)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~agentId
              ~agentAliasName ())
           (Some Values.CreateAgentAliasResponse.to_json)
           (Some Values.CreateAgentAliasResponse.error_to_json)])
let create_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and dataDeletionPolicy =
         flag "data-deletion-policy" (optional json_arg)
           ~doc:"JSON DataDeletionPolicy"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (optional json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration"
       and vectorIngestionConfiguration =
         flag "vector-ingestion-configuration" (optional json_arg)
           ~doc:"JSON VectorIngestionConfiguration"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and dataSourceConfiguration =
         flag "data-source-configuration" (required json_arg)
           ~doc:"JSON DataSourceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source
           (Values.CreateDataSourceRequest.make ?clientToken ?description
              ?dataDeletionPolicy:(Option.map
                                     ~f:Values.DataDeletionPolicy.of_json
                                     dataDeletionPolicy)
              ?serverSideEncryptionConfiguration:(Option.map
                                                    ~f:Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ?vectorIngestionConfiguration:(Option.map
                                               ~f:Values.VectorIngestionConfiguration.of_json
                                               vectorIngestionConfiguration)
              ~knowledgeBaseId ~name
              ~dataSourceConfiguration:(Values.DataSourceConfiguration.of_json
                                          dataSourceConfiguration) ())
           (Some Values.CreateDataSourceResponse.to_json)
           (Some Values.CreateDataSourceResponse.error_to_json)])
let create_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FlowDescription"
       and customerEncryptionKeyArn =
         flag "customer-encryption-key-arn" (optional string)
           ~doc:"STRING KmsKeyArn"
       and definition =
         flag "definition" (optional json_arg) ~doc:"JSON FlowDefinition"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING FlowName"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING FlowExecutionRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow
           (Values.CreateFlowRequest.make ?description
              ?customerEncryptionKeyArn
              ?definition:(Option.map ~f:Values.FlowDefinition.of_json
                             definition) ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~executionRoleArn ()) (Some Values.CreateFlowResponse.to_json)
           (Some Values.CreateFlowResponse.error_to_json)])
let create_flow_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and concurrencyConfiguration =
         flag "concurrency-configuration" (optional json_arg)
           ~doc:"JSON FlowAliasConcurrencyConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and routingConfiguration =
         flag "routing-configuration" (required json_arg)
           ~doc:"JSON FlowAliasRoutingConfiguration"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow_alias
           (Values.CreateFlowAliasRequest.make ?description
              ?concurrencyConfiguration:(Option.map
                                           ~f:Values.FlowAliasConcurrencyConfiguration.of_json
                                           concurrencyConfiguration)
              ?clientToken ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~name
              ~routingConfiguration:(Values.FlowAliasRoutingConfiguration.of_json
                                       routingConfiguration) ~flowIdentifier
              ()) (Some Values.CreateFlowAliasResponse.to_json)
           (Some Values.CreateFlowAliasResponse.error_to_json)])
let create_flow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FlowDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow_version
           (Values.CreateFlowVersionRequest.make ?description ?clientToken
              ~flowIdentifier ())
           (Some Values.CreateFlowVersionResponse.to_json)
           (Some Values.CreateFlowVersionResponse.error_to_json)])
let create_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and storageConfiguration =
         flag "storage-configuration" (optional json_arg)
           ~doc:"JSON StorageConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING KnowledgeBaseRoleArn"
       and knowledgeBaseConfiguration =
         flag "knowledge-base-configuration" (required json_arg)
           ~doc:"JSON KnowledgeBaseConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_knowledge_base
           (Values.CreateKnowledgeBaseRequest.make ?clientToken ?description
              ?storageConfiguration:(Option.map
                                       ~f:Values.StorageConfiguration.of_json
                                       storageConfiguration)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~roleArn
              ~knowledgeBaseConfiguration:(Values.KnowledgeBaseConfiguration.of_json
                                             knowledgeBaseConfiguration) ())
           (Some Values.CreateKnowledgeBaseResponse.to_json)
           (Some Values.CreateKnowledgeBaseResponse.error_to_json)])
let create_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PromptDescription"
       and customerEncryptionKeyArn =
         flag "customer-encryption-key-arn" (optional string)
           ~doc:"STRING KmsKeyArn"
       and defaultVariant =
         flag "default-variant" (optional string)
           ~doc:"STRING PromptVariantName"
       and variants =
         flag "variants" (optional json_arg) ~doc:"JSON PromptVariantList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING PromptName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_prompt
           (Values.CreatePromptRequest.make ?description
              ?customerEncryptionKeyArn ?defaultVariant
              ?variants:(Option.map ~f:Values.PromptVariantList.of_json
                           variants) ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name ())
           (Some Values.CreatePromptResponse.to_json)
           (Some Values.CreatePromptResponse.error_to_json)])
let create_prompt_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PromptDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and promptIdentifier =
         flag "prompt-identifier" (required string)
           ~doc:"STRING PromptIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_prompt_version
           (Values.CreatePromptVersionRequest.make ?description ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~promptIdentifier ())
           (Some Values.CreatePromptVersionResponse.to_json)
           (Some Values.CreatePromptVersionResponse.error_to_json)])
let delete_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL Boolean"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent
           (Values.DeleteAgentRequest.make ?skipResourceInUseCheck ~agentId
              ()) (Some Values.DeleteAgentResponse.to_json)
           (Some Values.DeleteAgentResponse.error_to_json)])
let delete_agent_action_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL Boolean"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and actionGroupId =
         flag "action-group-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_action_group
           (Values.DeleteAgentActionGroupRequest.make ?skipResourceInUseCheck
              ~agentId ~agentVersion ~actionGroupId ())
           (Some Values.DeleteAgentActionGroupResponse.to_json)
           (Some Values.DeleteAgentActionGroupResponse.error_to_json)])
let delete_agent_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentAliasId =
         flag "agent-alias-id" (required string) ~doc:"STRING AgentAliasId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_alias
           (Values.DeleteAgentAliasRequest.make ~agentId ~agentAliasId ())
           (Some Values.DeleteAgentAliasResponse.to_json)
           (Some Values.DeleteAgentAliasResponse.error_to_json)])
let delete_agent_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL Boolean"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string)
           ~doc:"STRING NumericalVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_version
           (Values.DeleteAgentVersionRequest.make ?skipResourceInUseCheck
              ~agentId ~agentVersion ())
           (Some Values.DeleteAgentVersionResponse.to_json)
           (Some Values.DeleteAgentVersionResponse.error_to_json)])
let delete_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_source
           (Values.DeleteDataSourceRequest.make ~knowledgeBaseId
              ~dataSourceId ())
           (Some Values.DeleteDataSourceResponse.to_json)
           (Some Values.DeleteDataSourceResponse.error_to_json)])
let delete_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL Boolean"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow
           (Values.DeleteFlowRequest.make ?skipResourceInUseCheck
              ~flowIdentifier ()) (Some Values.DeleteFlowResponse.to_json)
           (Some Values.DeleteFlowResponse.error_to_json)])
let delete_flow_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier"
       and aliasIdentifier =
         flag "alias-identifier" (required string)
           ~doc:"STRING FlowAliasIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow_alias
           (Values.DeleteFlowAliasRequest.make ~flowIdentifier
              ~aliasIdentifier ())
           (Some Values.DeleteFlowAliasResponse.to_json)
           (Some Values.DeleteFlowAliasResponse.error_to_json)])
let delete_flow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL Boolean"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier"
       and flowVersion =
         flag "flow-version" (required string) ~doc:"STRING NumericalVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow_version
           (Values.DeleteFlowVersionRequest.make ?skipResourceInUseCheck
              ~flowIdentifier ~flowVersion ())
           (Some Values.DeleteFlowVersionResponse.to_json)
           (Some Values.DeleteFlowVersionResponse.error_to_json)])
let delete_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_knowledge_base
           (Values.DeleteKnowledgeBaseRequest.make ~knowledgeBaseId ())
           (Some Values.DeleteKnowledgeBaseResponse.to_json)
           (Some Values.DeleteKnowledgeBaseResponse.error_to_json)])
let delete_knowledge_base_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id"
       and documentIdentifiers =
         flag "document-identifiers" (required json_arg)
           ~doc:"JSON DocumentIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_knowledge_base_documents
           (Values.DeleteKnowledgeBaseDocumentsRequest.make ?clientToken
              ~knowledgeBaseId ~dataSourceId
              ~documentIdentifiers:(Values.DocumentIdentifiers.of_json
                                      documentIdentifiers) ())
           (Some Values.DeleteKnowledgeBaseDocumentsResponse.to_json)
           (Some Values.DeleteKnowledgeBaseDocumentsResponse.error_to_json)])
let delete_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and promptVersion =
         flag "prompt-version" (optional string)
           ~doc:"STRING NumericalVersion"
       and promptIdentifier =
         flag "prompt-identifier" (required string)
           ~doc:"STRING PromptIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_prompt
           (Values.DeletePromptRequest.make ?promptVersion ~promptIdentifier
              ()) (Some Values.DeletePromptResponse.to_json)
           (Some Values.DeletePromptResponse.error_to_json)])
let disassociate_agent_collaborator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and collaboratorId =
         flag "collaborator-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_agent_collaborator
           (Values.DisassociateAgentCollaboratorRequest.make ~agentId
              ~agentVersion ~collaboratorId ())
           (Some Values.DisassociateAgentCollaboratorResponse.to_json)
           (Some Values.DisassociateAgentCollaboratorResponse.error_to_json)])
let disassociate_agent_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_agent_knowledge_base
           (Values.DisassociateAgentKnowledgeBaseRequest.make ~agentId
              ~agentVersion ~knowledgeBaseId ())
           (Some Values.DisassociateAgentKnowledgeBaseResponse.to_json)
           (Some Values.DisassociateAgentKnowledgeBaseResponse.error_to_json)])
let get_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent (Values.GetAgentRequest.make ~agentId ())
           (Some Values.GetAgentResponse.to_json)
           (Some Values.GetAgentResponse.error_to_json)])
let get_agent_action_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version"
       and actionGroupId =
         flag "action-group-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_action_group
           (Values.GetAgentActionGroupRequest.make ~agentId ~agentVersion
              ~actionGroupId ())
           (Some Values.GetAgentActionGroupResponse.to_json)
           (Some Values.GetAgentActionGroupResponse.error_to_json)])
let get_agent_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentAliasId =
         flag "agent-alias-id" (required string) ~doc:"STRING AgentAliasId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_alias
           (Values.GetAgentAliasRequest.make ~agentId ~agentAliasId ())
           (Some Values.GetAgentAliasResponse.to_json)
           (Some Values.GetAgentAliasResponse.error_to_json)])
let get_agent_collaborator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version"
       and collaboratorId =
         flag "collaborator-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_collaborator
           (Values.GetAgentCollaboratorRequest.make ~agentId ~agentVersion
              ~collaboratorId ())
           (Some Values.GetAgentCollaboratorResponse.to_json)
           (Some Values.GetAgentCollaboratorResponse.error_to_json)])
let get_agent_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_knowledge_base
           (Values.GetAgentKnowledgeBaseRequest.make ~agentId ~agentVersion
              ~knowledgeBaseId ())
           (Some Values.GetAgentKnowledgeBaseResponse.to_json)
           (Some Values.GetAgentKnowledgeBaseResponse.error_to_json)])
let get_agent_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string)
           ~doc:"STRING NumericalVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_version
           (Values.GetAgentVersionRequest.make ~agentId ~agentVersion ())
           (Some Values.GetAgentVersionResponse.to_json)
           (Some Values.GetAgentVersionResponse.error_to_json)])
let get_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_source
           (Values.GetDataSourceRequest.make ~knowledgeBaseId ~dataSourceId
              ()) (Some Values.GetDataSourceResponse.to_json)
           (Some Values.GetDataSourceResponse.error_to_json)])
let get_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_flow (Values.GetFlowRequest.make ~flowIdentifier ())
           (Some Values.GetFlowResponse.to_json)
           (Some Values.GetFlowResponse.error_to_json)])
let get_flow_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier"
       and aliasIdentifier =
         flag "alias-identifier" (required string)
           ~doc:"STRING FlowAliasIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_flow_alias
           (Values.GetFlowAliasRequest.make ~flowIdentifier ~aliasIdentifier
              ()) (Some Values.GetFlowAliasResponse.to_json)
           (Some Values.GetFlowAliasResponse.error_to_json)])
let get_flow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier"
       and flowVersion =
         flag "flow-version" (required string) ~doc:"STRING NumericalVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_flow_version
           (Values.GetFlowVersionRequest.make ~flowIdentifier ~flowVersion ())
           (Some Values.GetFlowVersionResponse.to_json)
           (Some Values.GetFlowVersionResponse.error_to_json)])
let get_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id"
       and ingestionJobId =
         flag "ingestion-job-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ingestion_job
           (Values.GetIngestionJobRequest.make ~knowledgeBaseId ~dataSourceId
              ~ingestionJobId ())
           (Some Values.GetIngestionJobResponse.to_json)
           (Some Values.GetIngestionJobResponse.error_to_json)])
let get_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_knowledge_base
           (Values.GetKnowledgeBaseRequest.make ~knowledgeBaseId ())
           (Some Values.GetKnowledgeBaseResponse.to_json)
           (Some Values.GetKnowledgeBaseResponse.error_to_json)])
let get_knowledge_base_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id"
       and documentIdentifiers =
         flag "document-identifiers" (required json_arg)
           ~doc:"JSON DocumentIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_knowledge_base_documents
           (Values.GetKnowledgeBaseDocumentsRequest.make ~knowledgeBaseId
              ~dataSourceId
              ~documentIdentifiers:(Values.DocumentIdentifiers.of_json
                                      documentIdentifiers) ())
           (Some Values.GetKnowledgeBaseDocumentsResponse.to_json)
           (Some Values.GetKnowledgeBaseDocumentsResponse.error_to_json)])
let get_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and promptVersion =
         flag "prompt-version" (optional string) ~doc:"STRING Version"
       and promptIdentifier =
         flag "prompt-identifier" (required string)
           ~doc:"STRING PromptIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_prompt
           (Values.GetPromptRequest.make ?promptVersion ~promptIdentifier ())
           (Some Values.GetPromptResponse.to_json)
           (Some Values.GetPromptResponse.error_to_json)])
let ingest_knowledge_base_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id"
       and documents =
         flag "documents" (required json_arg)
           ~doc:"JSON KnowledgeBaseDocuments" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.ingest_knowledge_base_documents
           (Values.IngestKnowledgeBaseDocumentsRequest.make ?clientToken
              ~knowledgeBaseId ~dataSourceId
              ~documents:(Values.KnowledgeBaseDocuments.of_json documents) ())
           (Some Values.IngestKnowledgeBaseDocumentsResponse.to_json)
           (Some Values.IngestKnowledgeBaseDocumentsResponse.error_to_json)])
let list_agent_action_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_action_groups
           (Values.ListAgentActionGroupsRequest.make ?maxResults ?nextToken
              ~agentId ~agentVersion ())
           (Some Values.ListAgentActionGroupsResponse.to_json)
           (Some Values.ListAgentActionGroupsResponse.error_to_json)])
let list_agent_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_aliases
           (Values.ListAgentAliasesRequest.make ?maxResults ?nextToken
              ~agentId ()) (Some Values.ListAgentAliasesResponse.to_json)
           (Some Values.ListAgentAliasesResponse.error_to_json)])
let list_agent_collaborators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_collaborators
           (Values.ListAgentCollaboratorsRequest.make ?maxResults ?nextToken
              ~agentId ~agentVersion ())
           (Some Values.ListAgentCollaboratorsResponse.to_json)
           (Some Values.ListAgentCollaboratorsResponse.error_to_json)])
let list_agent_knowledge_bases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_knowledge_bases
           (Values.ListAgentKnowledgeBasesRequest.make ?maxResults ?nextToken
              ~agentId ~agentVersion ())
           (Some Values.ListAgentKnowledgeBasesResponse.to_json)
           (Some Values.ListAgentKnowledgeBasesResponse.error_to_json)])
let list_agent_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_versions
           (Values.ListAgentVersionsRequest.make ?maxResults ?nextToken
              ~agentId ()) (Some Values.ListAgentVersionsResponse.to_json)
           (Some Values.ListAgentVersionsResponse.error_to_json)])
let list_agents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agents
           (Values.ListAgentsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAgentsResponse.to_json)
           (Some Values.ListAgentsResponse.error_to_json)])
let list_data_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sources
           (Values.ListDataSourcesRequest.make ?maxResults ?nextToken
              ~knowledgeBaseId ())
           (Some Values.ListDataSourcesResponse.to_json)
           (Some Values.ListDataSourcesResponse.error_to_json)])
let list_flow_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flow_aliases
           (Values.ListFlowAliasesRequest.make ?maxResults ?nextToken
              ~flowIdentifier ())
           (Some Values.ListFlowAliasesResponse.to_json)
           (Some Values.ListFlowAliasesResponse.error_to_json)])
let list_flow_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flow_versions
           (Values.ListFlowVersionsRequest.make ?maxResults ?nextToken
              ~flowIdentifier ())
           (Some Values.ListFlowVersionsResponse.to_json)
           (Some Values.ListFlowVersionsResponse.error_to_json)])
let list_flows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flows
           (Values.ListFlowsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListFlowsResponse.to_json)
           (Some Values.ListFlowsResponse.error_to_json)])
let list_ingestion_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON IngestionJobFilters"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON IngestionJobSortBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ingestion_jobs
           (Values.ListIngestionJobsRequest.make
              ?filters:(Option.map ~f:Values.IngestionJobFilters.of_json
                          filters)
              ?sortBy:(Option.map ~f:Values.IngestionJobSortBy.of_json sortBy)
              ?maxResults ?nextToken ~knowledgeBaseId ~dataSourceId ())
           (Some Values.ListIngestionJobsResponse.to_json)
           (Some Values.ListIngestionJobsResponse.error_to_json)])
let list_knowledge_base_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_knowledge_base_documents
           (Values.ListKnowledgeBaseDocumentsRequest.make ?maxResults
              ?nextToken ~knowledgeBaseId ~dataSourceId ())
           (Some Values.ListKnowledgeBaseDocumentsResponse.to_json)
           (Some Values.ListKnowledgeBaseDocumentsResponse.error_to_json)])
let list_knowledge_bases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_knowledge_bases
           (Values.ListKnowledgeBasesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListKnowledgeBasesResponse.to_json)
           (Some Values.ListKnowledgeBasesResponse.error_to_json)])
let list_prompts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and promptIdentifier =
         flag "prompt-identifier" (optional string)
           ~doc:"STRING PromptIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_prompts
           (Values.ListPromptsRequest.make ?promptIdentifier ?maxResults
              ?nextToken ()) (Some Values.ListPromptsResponse.to_json)
           (Some Values.ListPromptsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourcesArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let prepare_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.prepare_agent (Values.PrepareAgentRequest.make ~agentId ())
           (Some Values.PrepareAgentResponse.to_json)
           (Some Values.PrepareAgentResponse.error_to_json)])
let prepare_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.prepare_flow
           (Values.PrepareFlowRequest.make ~flowIdentifier ())
           (Some Values.PrepareFlowResponse.to_json)
           (Some Values.PrepareFlowResponse.error_to_json)])
let start_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_ingestion_job
           (Values.StartIngestionJobRequest.make ?clientToken ?description
              ~knowledgeBaseId ~dataSourceId ())
           (Some Values.StartIngestionJobResponse.to_json)
           (Some Values.StartIngestionJobResponse.error_to_json)])
let stop_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id"
       and ingestionJobId =
         flag "ingestion-job-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_ingestion_job
           (Values.StopIngestionJobRequest.make ~knowledgeBaseId
              ~dataSourceId ~ingestionJobId ())
           (Some Values.StopIngestionJobResponse.to_json)
           (Some Values.StopIngestionJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourcesArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourcesArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instruction =
         flag "instruction" (optional string) ~doc:"STRING Instruction"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and orchestrationType =
         flag "orchestration-type" (optional json_arg)
           ~doc:"JSON OrchestrationType"
       and customOrchestration =
         flag "custom-orchestration" (optional json_arg)
           ~doc:"JSON CustomOrchestration"
       and idleSessionTTLInSeconds =
         flag "idle-session-t-t-l-in-seconds" (optional int)
           ~doc:"INT SessionTTL"
       and customerEncryptionKeyArn =
         flag "customer-encryption-key-arn" (optional string)
           ~doc:"STRING KmsKeyArn"
       and promptOverrideConfiguration =
         flag "prompt-override-configuration" (optional json_arg)
           ~doc:"JSON PromptOverrideConfiguration"
       and guardrailConfiguration =
         flag "guardrail-configuration" (optional json_arg)
           ~doc:"JSON GuardrailConfiguration"
       and memoryConfiguration =
         flag "memory-configuration" (optional json_arg)
           ~doc:"JSON MemoryConfiguration"
       and agentCollaboration =
         flag "agent-collaboration" (optional json_arg)
           ~doc:"JSON AgentCollaboration"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentName = flag "agent-name" (required string) ~doc:"STRING Name"
       and foundationModel =
         flag "foundation-model" (required string)
           ~doc:"STRING ModelIdentifier"
       and agentResourceRoleArn =
         flag "agent-resource-role-arn" (required string)
           ~doc:"STRING AgentRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent
           (Values.UpdateAgentRequest.make ?instruction ?description
              ?orchestrationType:(Option.map
                                    ~f:Values.OrchestrationType.of_json
                                    orchestrationType)
              ?customOrchestration:(Option.map
                                      ~f:Values.CustomOrchestration.of_json
                                      customOrchestration)
              ?idleSessionTTLInSeconds ?customerEncryptionKeyArn
              ?promptOverrideConfiguration:(Option.map
                                              ~f:Values.PromptOverrideConfiguration.of_json
                                              promptOverrideConfiguration)
              ?guardrailConfiguration:(Option.map
                                         ~f:Values.GuardrailConfiguration.of_json
                                         guardrailConfiguration)
              ?memoryConfiguration:(Option.map
                                      ~f:Values.MemoryConfiguration.of_json
                                      memoryConfiguration)
              ?agentCollaboration:(Option.map
                                     ~f:Values.AgentCollaboration.of_json
                                     agentCollaboration) ~agentId ~agentName
              ~foundationModel ~agentResourceRoleArn ())
           (Some Values.UpdateAgentResponse.to_json)
           (Some Values.UpdateAgentResponse.error_to_json)])
let update_agent_action_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and parentActionGroupSignature =
         flag "parent-action-group-signature" (optional json_arg)
           ~doc:"JSON ActionGroupSignature"
       and parentActionGroupSignatureParams =
         flag "parent-action-group-signature-params" (optional json_arg)
           ~doc:"JSON ActionGroupSignatureParams"
       and actionGroupExecutor =
         flag "action-group-executor" (optional json_arg)
           ~doc:"JSON ActionGroupExecutor"
       and actionGroupState =
         flag "action-group-state" (optional json_arg)
           ~doc:"JSON ActionGroupState"
       and apiSchema =
         flag "api-schema" (optional json_arg) ~doc:"JSON APISchema"
       and functionSchema =
         flag "function-schema" (optional json_arg)
           ~doc:"JSON FunctionSchema"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and actionGroupId =
         flag "action-group-id" (required string) ~doc:"STRING Id"
       and actionGroupName =
         flag "action-group-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_action_group
           (Values.UpdateAgentActionGroupRequest.make ?description
              ?parentActionGroupSignature:(Option.map
                                             ~f:Values.ActionGroupSignature.of_json
                                             parentActionGroupSignature)
              ?parentActionGroupSignatureParams:(Option.map
                                                   ~f:Values.ActionGroupSignatureParams.of_json
                                                   parentActionGroupSignatureParams)
              ?actionGroupExecutor:(Option.map
                                      ~f:Values.ActionGroupExecutor.of_json
                                      actionGroupExecutor)
              ?actionGroupState:(Option.map
                                   ~f:Values.ActionGroupState.of_json
                                   actionGroupState)
              ?apiSchema:(Option.map ~f:Values.APISchema.of_json apiSchema)
              ?functionSchema:(Option.map ~f:Values.FunctionSchema.of_json
                                 functionSchema) ~agentId ~agentVersion
              ~actionGroupId ~actionGroupName ())
           (Some Values.UpdateAgentActionGroupResponse.to_json)
           (Some Values.UpdateAgentActionGroupResponse.error_to_json)])
let update_agent_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and routingConfiguration =
         flag "routing-configuration" (optional json_arg)
           ~doc:"JSON AgentAliasRoutingConfiguration"
       and aliasInvocationState =
         flag "alias-invocation-state" (optional json_arg)
           ~doc:"JSON AliasInvocationState"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentAliasId =
         flag "agent-alias-id" (required string) ~doc:"STRING AgentAliasId"
       and agentAliasName =
         flag "agent-alias-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_alias
           (Values.UpdateAgentAliasRequest.make ?description
              ?routingConfiguration:(Option.map
                                       ~f:Values.AgentAliasRoutingConfiguration.of_json
                                       routingConfiguration)
              ?aliasInvocationState:(Option.map
                                       ~f:Values.AliasInvocationState.of_json
                                       aliasInvocationState) ~agentId
              ~agentAliasId ~agentAliasName ())
           (Some Values.UpdateAgentAliasResponse.to_json)
           (Some Values.UpdateAgentAliasResponse.error_to_json)])
let update_agent_collaborator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relayConversationHistory =
         flag "relay-conversation-history" (optional json_arg)
           ~doc:"JSON RelayConversationHistory"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and collaboratorId =
         flag "collaborator-id" (required string) ~doc:"STRING Id"
       and agentDescriptor =
         flag "agent-descriptor" (required json_arg)
           ~doc:"JSON AgentDescriptor"
       and collaboratorName =
         flag "collaborator-name" (required string) ~doc:"STRING Name"
       and collaborationInstruction =
         flag "collaboration-instruction" (required string)
           ~doc:"STRING CollaborationInstruction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_collaborator
           (Values.UpdateAgentCollaboratorRequest.make
              ?relayConversationHistory:(Option.map
                                           ~f:Values.RelayConversationHistory.of_json
                                           relayConversationHistory) ~agentId
              ~agentVersion ~collaboratorId
              ~agentDescriptor:(Values.AgentDescriptor.of_json
                                  agentDescriptor) ~collaboratorName
              ~collaborationInstruction ())
           (Some Values.UpdateAgentCollaboratorResponse.to_json)
           (Some Values.UpdateAgentCollaboratorResponse.error_to_json)])
let update_agent_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and knowledgeBaseState =
         flag "knowledge-base-state" (optional json_arg)
           ~doc:"JSON KnowledgeBaseState"
       and agentId = flag "agent-id" (required string) ~doc:"STRING Id"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING DraftVersion"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_knowledge_base
           (Values.UpdateAgentKnowledgeBaseRequest.make ?description
              ?knowledgeBaseState:(Option.map
                                     ~f:Values.KnowledgeBaseState.of_json
                                     knowledgeBaseState) ~agentId
              ~agentVersion ~knowledgeBaseId ())
           (Some Values.UpdateAgentKnowledgeBaseResponse.to_json)
           (Some Values.UpdateAgentKnowledgeBaseResponse.error_to_json)])
let update_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and dataDeletionPolicy =
         flag "data-deletion-policy" (optional json_arg)
           ~doc:"JSON DataDeletionPolicy"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (optional json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration"
       and vectorIngestionConfiguration =
         flag "vector-ingestion-configuration" (optional json_arg)
           ~doc:"JSON VectorIngestionConfiguration"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING Id"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and dataSourceConfiguration =
         flag "data-source-configuration" (required json_arg)
           ~doc:"JSON DataSourceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source
           (Values.UpdateDataSourceRequest.make ?description
              ?dataDeletionPolicy:(Option.map
                                     ~f:Values.DataDeletionPolicy.of_json
                                     dataDeletionPolicy)
              ?serverSideEncryptionConfiguration:(Option.map
                                                    ~f:Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ?vectorIngestionConfiguration:(Option.map
                                               ~f:Values.VectorIngestionConfiguration.of_json
                                               vectorIngestionConfiguration)
              ~knowledgeBaseId ~dataSourceId ~name
              ~dataSourceConfiguration:(Values.DataSourceConfiguration.of_json
                                          dataSourceConfiguration) ())
           (Some Values.UpdateDataSourceResponse.to_json)
           (Some Values.UpdateDataSourceResponse.error_to_json)])
let update_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FlowDescription"
       and customerEncryptionKeyArn =
         flag "customer-encryption-key-arn" (optional string)
           ~doc:"STRING KmsKeyArn"
       and definition =
         flag "definition" (optional json_arg) ~doc:"JSON FlowDefinition"
       and name = flag "name" (required string) ~doc:"STRING FlowName"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING FlowExecutionRoleArn"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow
           (Values.UpdateFlowRequest.make ?description
              ?customerEncryptionKeyArn
              ?definition:(Option.map ~f:Values.FlowDefinition.of_json
                             definition) ~name ~executionRoleArn
              ~flowIdentifier ()) (Some Values.UpdateFlowResponse.to_json)
           (Some Values.UpdateFlowResponse.error_to_json)])
let update_flow_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and concurrencyConfiguration =
         flag "concurrency-configuration" (optional json_arg)
           ~doc:"JSON FlowAliasConcurrencyConfiguration"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and routingConfiguration =
         flag "routing-configuration" (required json_arg)
           ~doc:"JSON FlowAliasRoutingConfiguration"
       and flowIdentifier =
         flag "flow-identifier" (required string)
           ~doc:"STRING FlowIdentifier"
       and aliasIdentifier =
         flag "alias-identifier" (required string)
           ~doc:"STRING FlowAliasIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow_alias
           (Values.UpdateFlowAliasRequest.make ?description
              ?concurrencyConfiguration:(Option.map
                                           ~f:Values.FlowAliasConcurrencyConfiguration.of_json
                                           concurrencyConfiguration) ~name
              ~routingConfiguration:(Values.FlowAliasRoutingConfiguration.of_json
                                       routingConfiguration) ~flowIdentifier
              ~aliasIdentifier ())
           (Some Values.UpdateFlowAliasResponse.to_json)
           (Some Values.UpdateFlowAliasResponse.error_to_json)])
let update_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and storageConfiguration =
         flag "storage-configuration" (optional json_arg)
           ~doc:"JSON StorageConfiguration"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING Id"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING KnowledgeBaseRoleArn"
       and knowledgeBaseConfiguration =
         flag "knowledge-base-configuration" (required json_arg)
           ~doc:"JSON KnowledgeBaseConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_knowledge_base
           (Values.UpdateKnowledgeBaseRequest.make ?description
              ?storageConfiguration:(Option.map
                                       ~f:Values.StorageConfiguration.of_json
                                       storageConfiguration) ~knowledgeBaseId
              ~name ~roleArn
              ~knowledgeBaseConfiguration:(Values.KnowledgeBaseConfiguration.of_json
                                             knowledgeBaseConfiguration) ())
           (Some Values.UpdateKnowledgeBaseResponse.to_json)
           (Some Values.UpdateKnowledgeBaseResponse.error_to_json)])
let update_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PromptDescription"
       and customerEncryptionKeyArn =
         flag "customer-encryption-key-arn" (optional string)
           ~doc:"STRING KmsKeyArn"
       and defaultVariant =
         flag "default-variant" (optional string)
           ~doc:"STRING PromptVariantName"
       and variants =
         flag "variants" (optional json_arg) ~doc:"JSON PromptVariantList"
       and name = flag "name" (required string) ~doc:"STRING PromptName"
       and promptIdentifier =
         flag "prompt-identifier" (required string)
           ~doc:"STRING PromptIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_prompt
           (Values.UpdatePromptRequest.make ?description
              ?customerEncryptionKeyArn ?defaultVariant
              ?variants:(Option.map ~f:Values.PromptVariantList.of_json
                           variants) ~name ~promptIdentifier ())
           (Some Values.UpdatePromptResponse.to_json)
           (Some Values.UpdatePromptResponse.error_to_json)])
let validate_flow_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON FlowDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_flow_definition
           (Values.ValidateFlowDefinitionRequest.make
              ~definition:(Values.FlowDefinition.of_json definition) ())
           (Some Values.ValidateFlowDefinitionResponse.to_json)
           (Some Values.ValidateFlowDefinitionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-agent-collaborator", associate_agent_collaborator);
    ("associate-agent-knowledge-base", associate_agent_knowledge_base);
    ("create-agent", create_agent);
    ("create-agent-action-group", create_agent_action_group);
    ("create-agent-alias", create_agent_alias);
    ("create-data-source", create_data_source);
    ("create-flow", create_flow);
    ("create-flow-alias", create_flow_alias);
    ("create-flow-version", create_flow_version);
    ("create-knowledge-base", create_knowledge_base);
    ("create-prompt", create_prompt);
    ("create-prompt-version", create_prompt_version);
    ("delete-agent", delete_agent);
    ("delete-agent-action-group", delete_agent_action_group);
    ("delete-agent-alias", delete_agent_alias);
    ("delete-agent-version", delete_agent_version);
    ("delete-data-source", delete_data_source);
    ("delete-flow", delete_flow);
    ("delete-flow-alias", delete_flow_alias);
    ("delete-flow-version", delete_flow_version);
    ("delete-knowledge-base", delete_knowledge_base);
    ("delete-knowledge-base-documents", delete_knowledge_base_documents);
    ("delete-prompt", delete_prompt);
    ("disassociate-agent-collaborator", disassociate_agent_collaborator);
    ("disassociate-agent-knowledge-base", disassociate_agent_knowledge_base);
    ("get-agent", get_agent);
    ("get-agent-action-group", get_agent_action_group);
    ("get-agent-alias", get_agent_alias);
    ("get-agent-collaborator", get_agent_collaborator);
    ("get-agent-knowledge-base", get_agent_knowledge_base);
    ("get-agent-version", get_agent_version);
    ("get-data-source", get_data_source);
    ("get-flow", get_flow);
    ("get-flow-alias", get_flow_alias);
    ("get-flow-version", get_flow_version);
    ("get-ingestion-job", get_ingestion_job);
    ("get-knowledge-base", get_knowledge_base);
    ("get-knowledge-base-documents", get_knowledge_base_documents);
    ("get-prompt", get_prompt);
    ("ingest-knowledge-base-documents", ingest_knowledge_base_documents);
    ("list-agent-action-groups", list_agent_action_groups);
    ("list-agent-aliases", list_agent_aliases);
    ("list-agent-collaborators", list_agent_collaborators);
    ("list-agent-knowledge-bases", list_agent_knowledge_bases);
    ("list-agent-versions", list_agent_versions);
    ("list-agents", list_agents);
    ("list-data-sources", list_data_sources);
    ("list-flow-aliases", list_flow_aliases);
    ("list-flow-versions", list_flow_versions);
    ("list-flows", list_flows);
    ("list-ingestion-jobs", list_ingestion_jobs);
    ("list-knowledge-base-documents", list_knowledge_base_documents);
    ("list-knowledge-bases", list_knowledge_bases);
    ("list-prompts", list_prompts);
    ("list-tags-for-resource", list_tags_for_resource);
    ("prepare-agent", prepare_agent);
    ("prepare-flow", prepare_flow);
    ("start-ingestion-job", start_ingestion_job);
    ("stop-ingestion-job", stop_ingestion_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-agent", update_agent);
    ("update-agent-action-group", update_agent_action_group);
    ("update-agent-alias", update_agent_alias);
    ("update-agent-collaborator", update_agent_collaborator);
    ("update-agent-knowledge-base", update_agent_knowledge_base);
    ("update-data-source", update_data_source);
    ("update-flow", update_flow);
    ("update-flow-alias", update_flow_alias);
    ("update-knowledge-base", update_knowledge_base);
    ("update-prompt", update_prompt);
    ("validate-flow-definition", validate_flow_definition)]
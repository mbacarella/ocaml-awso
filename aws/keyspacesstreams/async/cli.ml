(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetRecordsInputMaxResultsInteger"
       and shardIterator =
         flag "shard-iterator" (required string) ~doc:"STRING ShardIterator" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_records
           (Values.GetRecordsInput.make ?maxResults ~shardIterator ())
           (Some Values.GetRecordsOutput.to_json)
           (Some Values.GetRecordsOutput.error_to_json)])
let get_shard_iterator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sequenceNumber =
         flag "sequence-number" (optional string)
           ~doc:"STRING SequenceNumber"
       and streamArn =
         flag "stream-arn" (required string) ~doc:"STRING StreamArn"
       and shardId = flag "shard-id" (required string) ~doc:"STRING ShardId"
       and shardIteratorType =
         flag "shard-iterator-type" (required json_arg)
           ~doc:"JSON ShardIteratorType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_shard_iterator
           (Values.GetShardIteratorInput.make ?sequenceNumber ~streamArn
              ~shardId
              ~shardIteratorType:(Values.ShardIteratorType.of_json
                                    shardIteratorType) ())
           (Some Values.GetShardIteratorOutput.to_json)
           (Some Values.GetShardIteratorOutput.error_to_json)])
let get_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetStreamInputMaxResultsInteger"
       and shardFilter =
         flag "shard-filter" (optional json_arg) ~doc:"JSON ShardFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ShardIdToken"
       and streamArn =
         flag "stream-arn" (required string) ~doc:"STRING StreamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stream
           (Values.GetStreamInput.make ?maxResults
              ?shardFilter:(Option.map ~f:Values.ShardFilter.of_json
                              shardFilter) ?nextToken ~streamArn ())
           (Some Values.GetStreamOutput.to_json)
           (Some Values.GetStreamOutput.error_to_json)])
let list_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyspaceName =
         flag "keyspace-name" (optional string) ~doc:"STRING KeyspaceName"
       and tableName =
         flag "table-name" (optional string) ~doc:"STRING TableName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListStreamsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING StreamArnToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_streams
           (Values.ListStreamsInput.make ?keyspaceName ?tableName ?maxResults
              ?nextToken ()) (Some Values.ListStreamsOutput.to_json)
           (Some Values.ListStreamsOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-records", get_records);
    ("get-shard-iterator", get_shard_iterator);
    ("get-stream", get_stream);
    ("list-streams", list_streams)]
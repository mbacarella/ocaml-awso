(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.keyspacesstreams
let apiVersion = "2024-09-09"
let endpointPrefix = "cassandra-streams"
let serviceFullName = "Amazon Keyspaces Streams"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "KeyspacesStreams"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "You don't have sufficient permissions to perform this action."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You don't have sufficient access permissions to perform this operation. This exception occurs when your IAM user or role lacks the required permissions to access the Amazon Keyspaces resource or perform the requested action. Check your IAM policies and ensure they grant the necessary permissions."]
module Blob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Date =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ShardIterator =
  struct
    type nonrec t = string
    let context_ = "ShardIterator"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4096) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShardIterator" j
    let to_json = simple_to_json to_value
  end
module GetRecordsInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GetRecordsInputMaxResultsInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GetRecordsInput =
  struct
    type nonrec t =
      {
      shardIterator: ShardIterator.t
        [@ocaml.doc
          "The unique identifier of the shard iterator. A shard iterator specifies the position in the shard from which you want to start reading data records sequentially. You obtain this value by calling the GetShardIterator operation. Each shard iterator is valid for 15 minutes after creation."];
      maxResults: GetRecordsInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of records to return in a single GetRecords request. The default value is 100. You can specify a limit between 1 and 1000, but the actual number returned might be less than the specified maximum if the size of the data for the returned records exceeds the internal size limit."]}
    let context_ = "GetRecordsInput"
    let make ?maxResults =
      fun ~shardIterator -> fun () -> { maxResults; shardIterator }
    let to_value x =
      structure_to_value
        [("shardIterator", (Some (ShardIterator.to_value x.shardIterator)));
        ("maxResults",
          (Option.map x.maxResults
             ~f:GetRecordsInputMaxResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:GetRecordsInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let shardIterator =
        ShardIterator.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "shardIterator") in
      make ?maxResults ~shardIterator ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults"
          GetRecordsInputMaxResultsInteger.of_json in
      let shardIterator =
        field_map_exn json__ "shardIterator" ShardIterator.of_json in
      make ?maxResults ~shardIterator ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves data records from a specified shard in an Amazon Keyspaces data stream. This operation returns a collection of data records from the shard, including the primary key columns and information about modifications made to the captured table data. Each record represents a single data modification in the Amazon Keyspaces table and includes metadata about when the change occurred."]
module ValidationExceptionType =
  struct
    type nonrec t =
      | InvalidFormat 
      | TrimmedDataAccess 
      | ExpiredIterator 
      | ExpiredNextToken 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | InvalidFormat -> "InvalidFormat"
      | TrimmedDataAccess -> "TrimmedDataAccess"
      | ExpiredIterator -> "ExpiredIterator"
      | ExpiredNextToken -> "ExpiredNextToken"
      | Non_static_id s -> s
    let of_string =
      function
      | "InvalidFormat" -> InvalidFormat
      | "TrimmedDataAccess" -> TrimmedDataAccess
      | "ExpiredIterator" -> ExpiredIterator
      | "ExpiredNextToken" -> ExpiredNextToken
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionType" j)
    let to_json = simple_to_json to_value
  end
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "The input fails to satisfy the constraints specified by the service. Check the error details and modify your request."];
      errorCode: ValidationExceptionType.t option
        [@ocaml.doc
          "An error occurred validating your request. See the error message for details."]}
    let make ?message = fun ?errorCode -> fun () -> { message; errorCode }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("errorCode",
          (Option.map x.errorCode ~f:ValidationExceptionType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorCode =
        (Option.map ~f:ValidationExceptionType.of_xml)
          (Xml.child xml_arg0 "errorCode") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?errorCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorCode =
        field_map json__ "errorCode" ValidationExceptionType.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?errorCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request validation failed because one or more input parameters failed validation. This exception occurs when there are syntax errors in the request, field constraints are violated, or required parameters are missing. To help you fix the issue, the exception message provides details about which parameter failed and why."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "The request was denied due to request throttling. Reduce the frequency of requests and try again."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request rate is too high and exceeds the service's throughput limits. This exception occurs when you send too many requests in a short period of time. Implement exponential backoff in your retry strategy to handle this exception. Reducing your request frequency or distributing requests more evenly can help avoid throughput exceptions. This exception can also occur when more than two processes are reading from the same stream shard at the same time. Ensure that only one process reads from a stream shard at the same time."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "The requested resource wasn't found. Verify that the resource exists and try again."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested resource doesn't exist or could not be found. This exception occurs when you attempt to access a keyspace, table, stream, or other Amazon Keyspaces resource that doesn't exist or that has been deleted. Verify that the resource identifier is correct and that the resource exists in your account."]
module SequenceNumber =
  struct
    type nonrec t = string
    let context_ = "SequenceNumber"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:48) >>=
             (fun () -> check_string_min i ~min:21));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SequenceNumber" j
    let to_json = simple_to_json to_value
  end
module OriginType =
  struct
    type nonrec t =
      | USER 
      | REPLICATION 
      | TTL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | USER -> "USER"
      | REPLICATION -> "REPLICATION"
      | TTL -> "TTL"
      | Non_static_id s -> s
    let of_string =
      function
      | "USER" -> USER
      | "REPLICATION" -> REPLICATION
      | "TTL" -> TTL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OriginType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OriginType" j)
    let to_json = simple_to_json to_value
  end
module KeyspacesMetadata =
  struct
    type nonrec t =
      {
      expirationTime: String_.t option
        [@ocaml.doc
          "The time at which the associated data will expire, based on the time-to-live (TTL) setting."];
      writeTime: String_.t option
        [@ocaml.doc
          "The timestamp at which the associated data was written to the database."]}
    let make ?expirationTime =
      fun ?writeTime -> fun () -> { expirationTime; writeTime }
    let to_value x =
      structure_to_value
        [("expirationTime",
           (Option.map x.expirationTime ~f:String_.to_value));
        ("writeTime", (Option.map x.writeTime ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let writeTime =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "writeTime") in
      let expirationTime =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "expirationTime") in
      make ?writeTime ?expirationTime ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let writeTime = field_map json__ "writeTime" String_.of_json in
      let expirationTime = field_map json__ "expirationTime" String_.of_json in
      make ?writeTime ?expirationTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains metadata information associated with Amazon Keyspaces cells and rows."]
module rec
  KeyspacesCell:sig
                  type nonrec t =
                    {
                    value: KeyspacesCellValue.t option
                      [@ocaml.doc
                        "The value stored in this cell, which can be of various data types supported by Amazon Keyspaces."];
                    metadata: KeyspacesMetadata.t option
                      [@ocaml.doc
                        "Metadata associated with this cell, such as time-to-live (TTL) expiration time and write timestamp."]}
                  val make :
                    ?value:KeyspacesCellValue.t ->
                      ?metadata:KeyspacesMetadata.t -> unit -> t
                  val to_value : t -> Botodata.value
                  val to_query : t -> Client.Query.t
                  val of_xml : Xml.t -> t
                  val of_json : Yojson.Safe.t -> t
                  val to_json : t -> Yojson.Safe.t
                end =
  struct
    type nonrec t =
      {
      value: KeyspacesCellValue.t option
        [@ocaml.doc
          "The value stored in this cell, which can be of various data types supported by Amazon Keyspaces."];
      metadata: KeyspacesMetadata.t option
        [@ocaml.doc
          "Metadata associated with this cell, such as time-to-live (TTL) expiration time and write timestamp."]}
    let make ?value = fun ?metadata -> fun () -> { value; metadata }
    let to_value x =
      structure_to_value
        [("value", (Option.map x.value ~f:KeyspacesCellValue.to_value));
        ("metadata", (Option.map x.metadata ~f:KeyspacesMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        (Option.map ~f:KeyspacesMetadata.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let value =
        (Option.map ~f:KeyspacesCellValue.of_xml)
          (Xml.child xml_arg0 "value") in
      make ?metadata ?value ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let metadata = field_map json__ "metadata" KeyspacesMetadata.of_json in
      let value = field_map json__ "value" KeyspacesCellValue.of_json in
      make ?metadata ?value ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a cell in an Amazon Keyspaces table, containing both the value and metadata about the cell."]
 and
  KeyspacesCellList:sig
                      type nonrec t = KeyspacesCell.t list
                      val make : KeyspacesCell.t list -> t
                      val to_value : t -> Botodata.value
                      val to_query : t -> Client.Query.t
                      val of_xml : Xml.t -> KeyspacesCell.t list
                      val of_json : Yojson.Safe.t -> t
                      val to_json : t -> Yojson.Safe.t
                      val to_header : t -> string
                    end =
  struct
    type nonrec t = KeyspacesCell.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:KeyspacesCell.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeyspacesCell.of_xml)
    let of_json j =
      list_of_json ~kind:"KeyspacesCellList" ~of_json:KeyspacesCell.of_json j
    let to_json v = composed_to_json to_value v
  end and
       KeyspacesCellMap:sig
                          type nonrec t = KeyspacesCellMapDefinition.t list
                          val make : KeyspacesCellMapDefinition.t list -> t
                          val to_value : t -> Botodata.value
                          val to_query : t -> Client.Query.t
                          val of_xml :
                            Xml.t -> KeyspacesCellMapDefinition.t list
                          val of_json : Yojson.Safe.t -> t
                          val to_json : t -> Yojson.Safe.t
                          val to_header : t -> string
                        end =
       struct
         type nonrec t = KeyspacesCellMapDefinition.t list
         let make i = i
         let of_string _ =
           failwithf "of_string is not implemented for List_shape objects" ()
           [@@warning "-32"]
         let to_value xs =
           (xs |> (List.map ~f:KeyspacesCellMapDefinition.to_value)) |>
             (fun x -> `List x)
         let to_query v = to_query to_value v
         let to_header _ =
           failwithf "to_header is not implemented for List_shape objects" ()
         let of_xml x =
           make
             (List.map
                ((Xml.all_children x) |>
                   (List.filter
                      ~f:(function
                          | `Data s ->
                              (match Stdlib.String.trim s with
                               | "" -> false
                               | _ -> true)
                          | _ -> true))) ~f:KeyspacesCellMapDefinition.of_xml)
         let of_json j =
           list_of_json ~kind:"KeyspacesCellMap"
             ~of_json:KeyspacesCellMapDefinition.of_json j
         let to_json v = composed_to_json to_value v
       end and
            KeyspacesCellMapDefinition:sig
                                         type nonrec t =
                                           {
                                           key: KeyspacesCellValue.t option
                                             [@ocaml.doc
                                               "The key of this map entry in the Amazon Keyspaces cell."];
                                           value: KeyspacesCellValue.t option
                                             [@ocaml.doc
                                               "The value associated with the key in this map entry."];
                                           metadata:
                                             KeyspacesMetadata.t option
                                             [@ocaml.doc
                                               "Metadata for this specific key-value pair within the map, such as timestamps and TTL information."]}
                                         val make :
                                           ?key:KeyspacesCellValue.t ->
                                             ?value:KeyspacesCellValue.t ->
                                               ?metadata:KeyspacesMetadata.t
                                                 -> unit -> t
                                         val to_value : t -> Botodata.value
                                         val to_query : t -> Client.Query.t
                                         val of_xml : Xml.t -> t
                                         val of_json : Yojson.Safe.t -> t
                                         val to_json : t -> Yojson.Safe.t
                                       end =
            struct
              type nonrec t =
                {
                key: KeyspacesCellValue.t option
                  [@ocaml.doc
                    "The key of this map entry in the Amazon Keyspaces cell."];
                value: KeyspacesCellValue.t option
                  [@ocaml.doc
                    "The value associated with the key in this map entry."];
                metadata: KeyspacesMetadata.t option
                  [@ocaml.doc
                    "Metadata for this specific key-value pair within the map, such as timestamps and TTL information."]}
              let make ?key =
                fun ?value ->
                  fun ?metadata -> fun () -> { key; value; metadata }
              let to_value x =
                structure_to_value
                  [("key", (Option.map x.key ~f:KeyspacesCellValue.to_value));
                  ("value",
                    (Option.map x.value ~f:KeyspacesCellValue.to_value));
                  ("metadata",
                    (Option.map x.metadata ~f:KeyspacesMetadata.to_value))]
              let to_query v = to_query to_value v
              let of_xml xml_arg0 =
                let metadata =
                  (Option.map ~f:KeyspacesMetadata.of_xml)
                    (Xml.child xml_arg0 "metadata") in
                let value =
                  (Option.map ~f:KeyspacesCellValue.of_xml)
                    (Xml.child xml_arg0 "value") in
                let key =
                  (Option.map ~f:KeyspacesCellValue.of_xml)
                    (Xml.child xml_arg0 "key") in
                make ?metadata ?value ?key ()
              let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning
                                                                    "-32"]
              let of_json json__ =
                let metadata =
                  field_map json__ "metadata" KeyspacesMetadata.of_json in
                let value =
                  field_map json__ "value" KeyspacesCellValue.of_json in
                let key = field_map json__ "key" KeyspacesCellValue.of_json in
                make ?metadata ?value ?key ()
              let to_json v = composed_to_json to_value v
            end[@@ocaml.doc
                 "Represents a key-value pair within a map data type in Amazon Keyspaces, including the associated metadata."]
 and
  KeyspacesCellValue:sig
                       type nonrec t =
                         {
                         asciiT: String_.t option
                           [@ocaml.doc
                             "A value of ASCII text type, containing US-ASCII characters."];
                         bigintT: String_.t option
                           [@ocaml.doc "A 64-bit signed integer value."];
                         blobT: Blob.t option
                           [@ocaml.doc
                             "A binary large object (BLOB) value stored as a Base64-encoded string."];
                         boolT: Boolean.t option
                           [@ocaml.doc
                             "A Boolean value, either true or false."];
                         counterT: String_.t option
                           [@ocaml.doc
                             "A distributed counter value that can be incremented and decremented."];
                         dateT: String_.t option
                           [@ocaml.doc
                             "A date value without a time component, represented as days since epoch (January 1, 1970)."];
                         decimalT: String_.t option
                           [@ocaml.doc
                             "A variable-precision decimal number value."];
                         doubleT: String_.t option
                           [@ocaml.doc
                             "A 64-bit double-precision floating point value."];
                         durationT: String_.t option
                           [@ocaml.doc
                             "A duration value with nanosecond precision, representing a period of time encoded as 32-bit months, 32-bit days, and 64-bit nanoseconds."];
                         floatT: String_.t option
                           [@ocaml.doc
                             "A 32-bit single-precision floating point value."];
                         inetT: String_.t option
                           [@ocaml.doc
                             "An IP address value, either IPv4 or IPv6 format."];
                         intT: String_.t option
                           [@ocaml.doc "A 32-bit signed integer value."];
                         listT: KeyspacesCellList.t option
                           [@ocaml.doc
                             "An ordered collection of elements that can contain duplicate values."];
                         mapT: KeyspacesCellMap.t option
                           [@ocaml.doc
                             "A collection of key-value pairs where each key is unique."];
                         setT: KeyspacesCellList.t option
                           [@ocaml.doc
                             "An unordered collection of unique elements."];
                         smallintT: String_.t option
                           [@ocaml.doc "A 16-bit signed integer value."];
                         textT: String_.t option
                           [@ocaml.doc "A UTF-8 encoded string value."];
                         timeT: String_.t option
                           [@ocaml.doc
                             "A time value without a date component, with nanosecond precision."];
                         timestampT: String_.t option
                           [@ocaml.doc
                             "A timestamp value representing date and time with millisecond precision."];
                         timeuuidT: String_.t option
                           [@ocaml.doc
                             "A universally unique identifier (UUID) that includes a timestamp component, ensuring both uniqueness and time ordering."];
                         tinyintT: String_.t option
                           [@ocaml.doc "An 8-bit signed integer value."];
                         tupleT: KeyspacesCellList.t option
                           [@ocaml.doc
                             "A fixed-length ordered list of elements, where each element can be of a different data type."];
                         uuidT: String_.t option
                           [@ocaml.doc
                             "A universally unique identifier (UUID) value."];
                         varcharT: String_.t option
                           [@ocaml.doc
                             "A UTF-8 encoded string value, functionally equivalent to text type."];
                         varintT: String_.t option
                           [@ocaml.doc
                             "An integer value within the +/-10^38 range."];
                         udtT: KeyspacesUdtMap.t option
                           [@ocaml.doc
                             "A user-defined type (UDT) value consisting of named fields, each with its own data type."]}
                       val make :
                         ?asciiT:String_.t ->
                           ?bigintT:String_.t ->
                             ?blobT:Blob.t ->
                               ?boolT:Boolean.t ->
                                 ?counterT:String_.t ->
                                   ?dateT:String_.t ->
                                     ?decimalT:String_.t ->
                                       ?doubleT:String_.t ->
                                         ?durationT:String_.t ->
                                           ?floatT:String_.t ->
                                             ?inetT:String_.t ->
                                               ?intT:String_.t ->
                                                 ?listT:KeyspacesCellList.t
                                                   ->
                                                   ?mapT:KeyspacesCellMap.t
                                                     ->
                                                     ?setT:KeyspacesCellList.t
                                                       ->
                                                       ?smallintT:String_.t
                                                         ->
                                                         ?textT:String_.t ->
                                                           ?timeT:String_.t
                                                             ->
                                                             ?timestampT:String_.t
                                                               ->
                                                               ?timeuuidT:String_.t
                                                                 ->
                                                                 ?tinyintT:String_.t
                                                                   ->
                                                                   ?tupleT:KeyspacesCellList.t
                                                                    ->
                                                                    ?uuidT:String_.t
                                                                    ->
                                                                    ?varcharT:String_.t
                                                                    ->
                                                                    ?varintT:String_.t
                                                                    ->
                                                                    ?udtT:KeyspacesUdtMap.t
                                                                    ->
                                                                    unit -> t
                       val to_value : t -> Botodata.value
                       val to_query : t -> Client.Query.t
                       val of_xml : Xml.t -> t
                       val of_json : Yojson.Safe.t -> t
                       val to_json : t -> Yojson.Safe.t
                     end =
  struct
    type nonrec t =
      {
      asciiT: String_.t option
        [@ocaml.doc
          "A value of ASCII text type, containing US-ASCII characters."];
      bigintT: String_.t option [@ocaml.doc "A 64-bit signed integer value."];
      blobT: Blob.t option
        [@ocaml.doc
          "A binary large object (BLOB) value stored as a Base64-encoded string."];
      boolT: Boolean.t option
        [@ocaml.doc "A Boolean value, either true or false."];
      counterT: String_.t option
        [@ocaml.doc
          "A distributed counter value that can be incremented and decremented."];
      dateT: String_.t option
        [@ocaml.doc
          "A date value without a time component, represented as days since epoch (January 1, 1970)."];
      decimalT: String_.t option
        [@ocaml.doc "A variable-precision decimal number value."];
      doubleT: String_.t option
        [@ocaml.doc "A 64-bit double-precision floating point value."];
      durationT: String_.t option
        [@ocaml.doc
          "A duration value with nanosecond precision, representing a period of time encoded as 32-bit months, 32-bit days, and 64-bit nanoseconds."];
      floatT: String_.t option
        [@ocaml.doc "A 32-bit single-precision floating point value."];
      inetT: String_.t option
        [@ocaml.doc "An IP address value, either IPv4 or IPv6 format."];
      intT: String_.t option [@ocaml.doc "A 32-bit signed integer value."];
      listT: KeyspacesCellList.t option
        [@ocaml.doc
          "An ordered collection of elements that can contain duplicate values."];
      mapT: KeyspacesCellMap.t option
        [@ocaml.doc
          "A collection of key-value pairs where each key is unique."];
      setT: KeyspacesCellList.t option
        [@ocaml.doc "An unordered collection of unique elements."];
      smallintT: String_.t option
        [@ocaml.doc "A 16-bit signed integer value."];
      textT: String_.t option [@ocaml.doc "A UTF-8 encoded string value."];
      timeT: String_.t option
        [@ocaml.doc
          "A time value without a date component, with nanosecond precision."];
      timestampT: String_.t option
        [@ocaml.doc
          "A timestamp value representing date and time with millisecond precision."];
      timeuuidT: String_.t option
        [@ocaml.doc
          "A universally unique identifier (UUID) that includes a timestamp component, ensuring both uniqueness and time ordering."];
      tinyintT: String_.t option
        [@ocaml.doc "An 8-bit signed integer value."];
      tupleT: KeyspacesCellList.t option
        [@ocaml.doc
          "A fixed-length ordered list of elements, where each element can be of a different data type."];
      uuidT: String_.t option
        [@ocaml.doc "A universally unique identifier (UUID) value."];
      varcharT: String_.t option
        [@ocaml.doc
          "A UTF-8 encoded string value, functionally equivalent to text type."];
      varintT: String_.t option
        [@ocaml.doc "An integer value within the +/-10^38 range."];
      udtT: KeyspacesUdtMap.t option
        [@ocaml.doc
          "A user-defined type (UDT) value consisting of named fields, each with its own data type."]}
    let make ?asciiT =
      fun ?bigintT ->
        fun ?blobT ->
          fun ?boolT ->
            fun ?counterT ->
              fun ?dateT ->
                fun ?decimalT ->
                  fun ?doubleT ->
                    fun ?durationT ->
                      fun ?floatT ->
                        fun ?inetT ->
                          fun ?intT ->
                            fun ?listT ->
                              fun ?mapT ->
                                fun ?setT ->
                                  fun ?smallintT ->
                                    fun ?textT ->
                                      fun ?timeT ->
                                        fun ?timestampT ->
                                          fun ?timeuuidT ->
                                            fun ?tinyintT ->
                                              fun ?tupleT ->
                                                fun ?uuidT ->
                                                  fun ?varcharT ->
                                                    fun ?varintT ->
                                                      fun ?udtT ->
                                                        fun () ->
                                                          {
                                                            asciiT;
                                                            bigintT;
                                                            blobT;
                                                            boolT;
                                                            counterT;
                                                            dateT;
                                                            decimalT;
                                                            doubleT;
                                                            durationT;
                                                            floatT;
                                                            inetT;
                                                            intT;
                                                            listT;
                                                            mapT;
                                                            setT;
                                                            smallintT;
                                                            textT;
                                                            timeT;
                                                            timestampT;
                                                            timeuuidT;
                                                            tinyintT;
                                                            tupleT;
                                                            uuidT;
                                                            varcharT;
                                                            varintT;
                                                            udtT
                                                          }
    let to_value x =
      structure_to_value
        [("asciiT", (Option.map x.asciiT ~f:String_.to_value));
        ("bigintT", (Option.map x.bigintT ~f:String_.to_value));
        ("blobT", (Option.map x.blobT ~f:Blob.to_value));
        ("boolT", (Option.map x.boolT ~f:Boolean.to_value));
        ("counterT", (Option.map x.counterT ~f:String_.to_value));
        ("dateT", (Option.map x.dateT ~f:String_.to_value));
        ("decimalT", (Option.map x.decimalT ~f:String_.to_value));
        ("doubleT", (Option.map x.doubleT ~f:String_.to_value));
        ("durationT", (Option.map x.durationT ~f:String_.to_value));
        ("floatT", (Option.map x.floatT ~f:String_.to_value));
        ("inetT", (Option.map x.inetT ~f:String_.to_value));
        ("intT", (Option.map x.intT ~f:String_.to_value));
        ("listT", (Option.map x.listT ~f:KeyspacesCellList.to_value));
        ("mapT", (Option.map x.mapT ~f:KeyspacesCellMap.to_value));
        ("setT", (Option.map x.setT ~f:KeyspacesCellList.to_value));
        ("smallintT", (Option.map x.smallintT ~f:String_.to_value));
        ("textT", (Option.map x.textT ~f:String_.to_value));
        ("timeT", (Option.map x.timeT ~f:String_.to_value));
        ("timestampT", (Option.map x.timestampT ~f:String_.to_value));
        ("timeuuidT", (Option.map x.timeuuidT ~f:String_.to_value));
        ("tinyintT", (Option.map x.tinyintT ~f:String_.to_value));
        ("tupleT", (Option.map x.tupleT ~f:KeyspacesCellList.to_value));
        ("uuidT", (Option.map x.uuidT ~f:String_.to_value));
        ("varcharT", (Option.map x.varcharT ~f:String_.to_value));
        ("varintT", (Option.map x.varintT ~f:String_.to_value));
        ("udtT", (Option.map x.udtT ~f:KeyspacesUdtMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let udtT =
        (Option.map ~f:KeyspacesUdtMap.of_xml) (Xml.child xml_arg0 "udtT") in
      let varintT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "varintT") in
      let varcharT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "varcharT") in
      let uuidT = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "uuidT") in
      let tupleT =
        (Option.map ~f:KeyspacesCellList.of_xml)
          (Xml.child xml_arg0 "tupleT") in
      let tinyintT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "tinyintT") in
      let timeuuidT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "timeuuidT") in
      let timestampT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "timestampT") in
      let timeT = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "timeT") in
      let textT = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "textT") in
      let smallintT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "smallintT") in
      let setT =
        (Option.map ~f:KeyspacesCellList.of_xml) (Xml.child xml_arg0 "setT") in
      let mapT =
        (Option.map ~f:KeyspacesCellMap.of_xml) (Xml.child xml_arg0 "mapT") in
      let listT =
        (Option.map ~f:KeyspacesCellList.of_xml) (Xml.child xml_arg0 "listT") in
      let intT = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "intT") in
      let inetT = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "inetT") in
      let floatT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "floatT") in
      let durationT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "durationT") in
      let doubleT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "doubleT") in
      let decimalT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "decimalT") in
      let dateT = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dateT") in
      let counterT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "counterT") in
      let boolT = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "boolT") in
      let blobT = (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "blobT") in
      let bigintT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "bigintT") in
      let asciiT =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "asciiT") in
      make ?udtT ?varintT ?varcharT ?uuidT ?tupleT ?tinyintT ?timeuuidT
        ?timestampT ?timeT ?textT ?smallintT ?setT ?mapT ?listT ?intT ?inetT
        ?floatT ?durationT ?doubleT ?decimalT ?dateT ?counterT ?boolT ?blobT
        ?bigintT ?asciiT ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let udtT = field_map json__ "udtT" KeyspacesUdtMap.of_json in
      let varintT = field_map json__ "varintT" String_.of_json in
      let varcharT = field_map json__ "varcharT" String_.of_json in
      let uuidT = field_map json__ "uuidT" String_.of_json in
      let tupleT = field_map json__ "tupleT" KeyspacesCellList.of_json in
      let tinyintT = field_map json__ "tinyintT" String_.of_json in
      let timeuuidT = field_map json__ "timeuuidT" String_.of_json in
      let timestampT = field_map json__ "timestampT" String_.of_json in
      let timeT = field_map json__ "timeT" String_.of_json in
      let textT = field_map json__ "textT" String_.of_json in
      let smallintT = field_map json__ "smallintT" String_.of_json in
      let setT = field_map json__ "setT" KeyspacesCellList.of_json in
      let mapT = field_map json__ "mapT" KeyspacesCellMap.of_json in
      let listT = field_map json__ "listT" KeyspacesCellList.of_json in
      let intT = field_map json__ "intT" String_.of_json in
      let inetT = field_map json__ "inetT" String_.of_json in
      let floatT = field_map json__ "floatT" String_.of_json in
      let durationT = field_map json__ "durationT" String_.of_json in
      let doubleT = field_map json__ "doubleT" String_.of_json in
      let decimalT = field_map json__ "decimalT" String_.of_json in
      let dateT = field_map json__ "dateT" String_.of_json in
      let counterT = field_map json__ "counterT" String_.of_json in
      let boolT = field_map json__ "boolT" Boolean.of_json in
      let blobT = field_map json__ "blobT" Blob.of_json in
      let bigintT = field_map json__ "bigintT" String_.of_json in
      let asciiT = field_map json__ "asciiT" String_.of_json in
      make ?udtT ?varintT ?varcharT ?uuidT ?tupleT ?tinyintT ?timeuuidT
        ?timestampT ?timeT ?textT ?smallintT ?setT ?mapT ?listT ?intT ?inetT
        ?floatT ?durationT ?doubleT ?decimalT ?dateT ?counterT ?boolT ?blobT
        ?bigintT ?asciiT ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the value of a cell in an Amazon Keyspaces table, supporting various data types with type-specific fields."]
 and
  KeyspacesUdtMap:sig
                    type nonrec t = (String_.t * KeyspacesCell.t) list
                    val make : (String_.t * KeyspacesCell.t) list -> t
                    val to_value : t -> Botodata.value
                    val to_query : t -> Client.Query.t
                    val of_xml : Xml.t -> t
                    val of_json : Yojson.Safe.t -> t
                    val to_json : t -> Yojson.Safe.t
                    val of_header :
                      (string, string) List.Assoc.t ->
                        (String.t, KeyspacesCell.t) List.Assoc.t
                    val to_header : t -> string
                  end =
  struct
    type nonrec t = (String_.t * KeyspacesCell.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String KeyspacesCell"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (KeyspacesCell.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:KeyspacesCell.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeyspacesCells =
  struct
    type nonrec t = (String_.t * KeyspacesCell.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String KeyspacesCell"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (KeyspacesCell.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:KeyspacesCell.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeyspacesRow =
  struct
    type nonrec t =
      {
      valueCells: KeyspacesCells.t option
        [@ocaml.doc
          "A map of regular (non-static) column cells in the row, where keys are column names and values are the corresponding cells."];
      staticCells: KeyspacesCells.t option
        [@ocaml.doc
          "A map of static column cells shared by all rows with the same partition key, where keys are column names and values are the corresponding cells."];
      rowMetadata: KeyspacesMetadata.t option
        [@ocaml.doc
          "Metadata that applies to the entire row, such as timestamps and TTL information."]}
    let make ?valueCells =
      fun ?staticCells ->
        fun ?rowMetadata ->
          fun () -> { valueCells; staticCells; rowMetadata }
    let to_value x =
      structure_to_value
        [("valueCells", (Option.map x.valueCells ~f:KeyspacesCells.to_value));
        ("staticCells",
          (Option.map x.staticCells ~f:KeyspacesCells.to_value));
        ("rowMetadata",
          (Option.map x.rowMetadata ~f:KeyspacesMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rowMetadata =
        (Option.map ~f:KeyspacesMetadata.of_xml)
          (Xml.child xml_arg0 "rowMetadata") in
      let staticCells =
        (Option.map ~f:KeyspacesCells.of_xml)
          (Xml.child xml_arg0 "staticCells") in
      let valueCells =
        (Option.map ~f:KeyspacesCells.of_xml)
          (Xml.child xml_arg0 "valueCells") in
      make ?rowMetadata ?staticCells ?valueCells ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rowMetadata =
        field_map json__ "rowMetadata" KeyspacesMetadata.of_json in
      let staticCells = field_map json__ "staticCells" KeyspacesCells.of_json in
      let valueCells = field_map json__ "valueCells" KeyspacesCells.of_json in
      make ?rowMetadata ?staticCells ?valueCells ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a row in an Amazon Keyspaces table, containing regular column values, static column values, and row-level metadata."]
module KeyspacesKeysMap =
  struct
    type nonrec t = (String_.t * KeyspacesCellValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String KeyspacesCellValue"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x ->
                       (KeyspacesCellValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:KeyspacesCellValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module Record =
  struct
    type nonrec t =
      {
      eventVersion: String_.t option
        [@ocaml.doc
          "The version of the record format, used to track the evolution of the record structure over time."];
      createdAt: Date.t option
        [@ocaml.doc
          "The timestamp indicating when this change data capture record was created."];
      origin: OriginType.t option
        [@ocaml.doc
          "The origin or source of this change data capture record."];
      partitionKeys: KeyspacesKeysMap.t option
        [@ocaml.doc
          "The partition key columns and their values for the affected row."];
      clusteringKeys: KeyspacesKeysMap.t option
        [@ocaml.doc
          "The clustering key columns and their values for the affected row, which determine the order of rows within a partition."];
      newImage: KeyspacesRow.t option
        [@ocaml.doc
          "The state of the row after the change operation that generated this record."];
      oldImage: KeyspacesRow.t option
        [@ocaml.doc
          "The state of the row before the change operation that generated this record."];
      sequenceNumber: SequenceNumber.t option
        [@ocaml.doc
          "A unique identifier assigned to this record within the shard, used for ordering and tracking purposes."]}
    let make ?eventVersion =
      fun ?createdAt ->
        fun ?origin ->
          fun ?partitionKeys ->
            fun ?clusteringKeys ->
              fun ?newImage ->
                fun ?oldImage ->
                  fun ?sequenceNumber ->
                    fun () ->
                      {
                        eventVersion;
                        createdAt;
                        origin;
                        partitionKeys;
                        clusteringKeys;
                        newImage;
                        oldImage;
                        sequenceNumber
                      }
    let to_value x =
      structure_to_value
        [("eventVersion", (Option.map x.eventVersion ~f:String_.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Date.to_value));
        ("origin", (Option.map x.origin ~f:OriginType.to_value));
        ("partitionKeys",
          (Option.map x.partitionKeys ~f:KeyspacesKeysMap.to_value));
        ("clusteringKeys",
          (Option.map x.clusteringKeys ~f:KeyspacesKeysMap.to_value));
        ("newImage", (Option.map x.newImage ~f:KeyspacesRow.to_value));
        ("oldImage", (Option.map x.oldImage ~f:KeyspacesRow.to_value));
        ("sequenceNumber",
          (Option.map x.sequenceNumber ~f:SequenceNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sequenceNumber =
        (Option.map ~f:SequenceNumber.of_xml)
          (Xml.child xml_arg0 "sequenceNumber") in
      let oldImage =
        (Option.map ~f:KeyspacesRow.of_xml) (Xml.child xml_arg0 "oldImage") in
      let newImage =
        (Option.map ~f:KeyspacesRow.of_xml) (Xml.child xml_arg0 "newImage") in
      let clusteringKeys =
        (Option.map ~f:KeyspacesKeysMap.of_xml)
          (Xml.child xml_arg0 "clusteringKeys") in
      let partitionKeys =
        (Option.map ~f:KeyspacesKeysMap.of_xml)
          (Xml.child xml_arg0 "partitionKeys") in
      let origin =
        (Option.map ~f:OriginType.of_xml) (Xml.child xml_arg0 "origin") in
      let createdAt =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "createdAt") in
      let eventVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "eventVersion") in
      make ?sequenceNumber ?oldImage ?newImage ?clusteringKeys ?partitionKeys
        ?origin ?createdAt ?eventVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sequenceNumber =
        field_map json__ "sequenceNumber" SequenceNumber.of_json in
      let oldImage = field_map json__ "oldImage" KeyspacesRow.of_json in
      let newImage = field_map json__ "newImage" KeyspacesRow.of_json in
      let clusteringKeys =
        field_map json__ "clusteringKeys" KeyspacesKeysMap.of_json in
      let partitionKeys =
        field_map json__ "partitionKeys" KeyspacesKeysMap.of_json in
      let origin = field_map json__ "origin" OriginType.of_json in
      let createdAt = field_map json__ "createdAt" Date.of_json in
      let eventVersion = field_map json__ "eventVersion" String_.of_json in
      make ?sequenceNumber ?oldImage ?newImage ?clusteringKeys ?partitionKeys
        ?origin ?createdAt ?eventVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a change data capture record for a row in an Amazon Keyspaces table, containing both the new and old states of the row."]
module RecordList =
  struct
    type nonrec t = Record.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Record.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Record.of_xml)
    let of_json j = list_of_json ~kind:"RecordList" ~of_json:Record.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "The service encountered an internal error. Try your request again."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon Keyspaces service encountered an unexpected error while processing the request. This internal server error is not related to your request parameters. Retry your request after a brief delay. If the issue persists, contact Amazon Web Services Support with details of your request to help identify and resolve the problem."]
module GetRecordsOutput =
  struct
    type nonrec t =
      {
      changeRecords: RecordList.t option
        [@ocaml.doc
          "An array of change data records retrieved from the specified shard. Each record represents a single data modification (insert, update, or delete) to a row in the Amazon Keyspaces table. Records include the primary key columns and information about what data was modified."];
      nextShardIterator: ShardIterator.t option
        [@ocaml.doc
          "The next position in the shard from which to start sequentially reading data records. If null, the shard has been closed and the requested iterator will not return any more data."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?changeRecords =
      fun ?nextShardIterator ->
        fun () -> { changeRecords; nextShardIterator }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("changeRecords",
           (Option.map x.changeRecords ~f:RecordList.to_value));
        ("nextShardIterator",
          (Option.map x.nextShardIterator ~f:ShardIterator.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextShardIterator =
        (Option.map ~f:ShardIterator.of_xml)
          (Xml.child xml_arg0 "nextShardIterator") in
      let changeRecords =
        (Option.map ~f:RecordList.of_xml)
          (Xml.child xml_arg0 "changeRecords") in
      make ?nextShardIterator ?changeRecords ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextShardIterator =
        field_map json__ "nextShardIterator" ShardIterator.of_json in
      let changeRecords = field_map json__ "changeRecords" RecordList.of_json in
      make ?nextShardIterator ?changeRecords ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves data records from a specified shard in an Amazon Keyspaces data stream. This operation returns a collection of data records from the shard, including the primary key columns and information about modifications made to the captured table data. Each record represents a single data modification in the Amazon Keyspaces table and includes metadata about when the change occurred."]
module StreamArn =
  struct
    type nonrec t = string
    let context_ = "StreamArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:37));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamArn" j
    let to_json = simple_to_json to_value
  end
module ShardIteratorType =
  struct
    type nonrec t =
      | TRIM_HORIZON 
      | LATEST 
      | AT_SEQUENCE_NUMBER 
      | AFTER_SEQUENCE_NUMBER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TRIM_HORIZON -> "TRIM_HORIZON"
      | LATEST -> "LATEST"
      | AT_SEQUENCE_NUMBER -> "AT_SEQUENCE_NUMBER"
      | AFTER_SEQUENCE_NUMBER -> "AFTER_SEQUENCE_NUMBER"
      | Non_static_id s -> s
    let of_string =
      function
      | "TRIM_HORIZON" -> TRIM_HORIZON
      | "LATEST" -> LATEST
      | "AT_SEQUENCE_NUMBER" -> AT_SEQUENCE_NUMBER
      | "AFTER_SEQUENCE_NUMBER" -> AFTER_SEQUENCE_NUMBER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ShardIteratorType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShardIteratorType" j)
    let to_json = simple_to_json to_value
  end
module ShardId =
  struct
    type nonrec t = string
    let context_ = "ShardId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:65) >>=
             (fun () -> check_string_min i ~min:28));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShardId" j
    let to_json = simple_to_json to_value
  end
module GetShardIteratorInput =
  struct
    type nonrec t =
      {
      streamArn: StreamArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the stream for which to get the shard iterator. The ARN uniquely identifies the stream within Amazon Keyspaces."];
      shardId: ShardId.t
        [@ocaml.doc
          "The identifier of the shard within the stream. The shard ID uniquely identifies a subset of the stream's data records that you want to access."];
      shardIteratorType: ShardIteratorType.t
        [@ocaml.doc
          "Determines how the shard iterator is positioned. Must be one of the following: TRIM_HORIZON - Start reading at the last untrimmed record in the shard, which is the oldest data record in the shard. AT_SEQUENCE_NUMBER - Start reading exactly from the specified sequence number. AFTER_SEQUENCE_NUMBER - Start reading right after the specified sequence number. LATEST - Start reading just after the most recent record in the shard, so that you always read the most recent data."];
      sequenceNumber: SequenceNumber.t option
        [@ocaml.doc
          "The sequence number of the data record in the shard from which to start reading. Required if ShardIteratorType is AT_SEQUENCE_NUMBER or AFTER_SEQUENCE_NUMBER. This parameter is ignored for other iterator types."]}
    let context_ = "GetShardIteratorInput"
    let make ?sequenceNumber =
      fun ~streamArn ->
        fun ~shardId ->
          fun ~shardIteratorType ->
            fun () ->
              { sequenceNumber; streamArn; shardId; shardIteratorType }
    let to_value x =
      structure_to_value
        [("streamArn", (Some (StreamArn.to_value x.streamArn)));
        ("shardId", (Some (ShardId.to_value x.shardId)));
        ("shardIteratorType",
          (Some (ShardIteratorType.to_value x.shardIteratorType)));
        ("sequenceNumber",
          (Option.map x.sequenceNumber ~f:SequenceNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sequenceNumber =
        (Option.map ~f:SequenceNumber.of_xml)
          (Xml.child xml_arg0 "sequenceNumber") in
      let shardIteratorType =
        ShardIteratorType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "shardIteratorType") in
      let shardId =
        ShardId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "shardId") in
      let streamArn =
        StreamArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamArn") in
      make ?sequenceNumber ~shardIteratorType ~shardId ~streamArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sequenceNumber =
        field_map json__ "sequenceNumber" SequenceNumber.of_json in
      let shardIteratorType =
        field_map_exn json__ "shardIteratorType" ShardIteratorType.of_json in
      let shardId = field_map_exn json__ "shardId" ShardId.of_json in
      let streamArn = field_map_exn json__ "streamArn" StreamArn.of_json in
      make ?sequenceNumber ~shardIteratorType ~shardId ~streamArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a shard iterator that serves as a bookmark for reading data from a specific position in an Amazon Keyspaces data stream's shard. The shard iterator specifies the shard position from which to start reading data records sequentially. You can specify whether to begin reading at the latest record, the oldest record, or at a particular sequence number within the shard."]
module GetShardIteratorOutput =
  struct
    type nonrec t =
      {
      shardIterator: ShardIterator.t option
        [@ocaml.doc
          "The unique identifier for the shard iterator. This value is used in the GetRecords operation to retrieve data records from the specified shard. Each shard iterator expires 15 minutes after it is returned to the requester."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?shardIterator = fun () -> { shardIterator }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("shardIterator",
           (Option.map x.shardIterator ~f:ShardIterator.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shardIterator =
        (Option.map ~f:ShardIterator.of_xml)
          (Xml.child xml_arg0 "shardIterator") in
      make ?shardIterator ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let shardIterator =
        field_map json__ "shardIterator" ShardIterator.of_json in
      make ?shardIterator ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a shard iterator that serves as a bookmark for reading data from a specific position in an Amazon Keyspaces data stream's shard. The shard iterator specifies the shard position from which to start reading data records sequentially. You can specify whether to begin reading at the latest record, the oldest record, or at a particular sequence number within the shard."]
module ShardIdToken =
  struct
    type nonrec t = string
    let context_ = "ShardIdToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:3000) >>=
             (fun () -> check_string_min i ~min:80));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShardIdToken" j
    let to_json = simple_to_json to_value
  end
module ShardFilterType =
  struct
    type nonrec t =
      | CHILD_SHARDS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | CHILD_SHARDS -> "CHILD_SHARDS" | Non_static_id s -> s
    let of_string =
      function | "CHILD_SHARDS" -> CHILD_SHARDS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ShardFilterType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShardFilterType" j)
    let to_json = simple_to_json to_value
  end
module ShardFilter =
  struct
    type nonrec t =
      {
      type_: ShardFilterType.t option
        [@ocaml.doc
          "The type of shard filter to use, which determines how the shardId parameter is interpreted."];
      shardId: ShardId.t option
        [@ocaml.doc
          "The identifier of a specific shard used to filter results based on the specified filter type."]}
    let make ?type_ = fun ?shardId -> fun () -> { type_; shardId }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:ShardFilterType.to_value));
        ("shardId", (Option.map x.shardId ~f:ShardId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shardId =
        (Option.map ~f:ShardId.of_xml) (Xml.child xml_arg0 "shardId") in
      let type_ =
        (Option.map ~f:ShardFilterType.of_xml) (Xml.child xml_arg0 "type") in
      make ?shardId ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let shardId = field_map json__ "shardId" ShardId.of_json in
      let type_ = field_map json__ "type" ShardFilterType.of_json in
      make ?shardId ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A filter used to limit the shards returned by a GetStream operation."]
module GetStreamInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for GetStreamInputMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GetStreamInput =
  struct
    type nonrec t =
      {
      streamArn: StreamArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the stream for which detailed information is requested. This uniquely identifies the specific stream you want to get information about."];
      maxResults: GetStreamInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of shard objects to return in a single GetStream request. The default value is 100. The minimum value is 1 and the maximum value is 100."];
      shardFilter: ShardFilter.t option
        [@ocaml.doc
          "Optional filter criteria to apply when retrieving shards. You can filter shards based on their parent shardID to get a list of children shards to narrow down the results returned by the GetStream operation."];
      nextToken: ShardIdToken.t option
        [@ocaml.doc
          "An optional pagination token provided by a previous GetStream operation. If this parameter is specified, the response includes only records beyond the token, up to the value specified by MaxResults."]}
    let context_ = "GetStreamInput"
    let make ?maxResults =
      fun ?shardFilter ->
        fun ?nextToken ->
          fun ~streamArn ->
            fun () -> { maxResults; shardFilter; nextToken; streamArn }
    let to_value x =
      structure_to_value
        [("streamArn", (Some (StreamArn.to_value x.streamArn)));
        ("maxResults",
          (Option.map x.maxResults
             ~f:GetStreamInputMaxResultsInteger.to_value));
        ("shardFilter", (Option.map x.shardFilter ~f:ShardFilter.to_value));
        ("nextToken", (Option.map x.nextToken ~f:ShardIdToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:ShardIdToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let shardFilter =
        (Option.map ~f:ShardFilter.of_xml) (Xml.child xml_arg0 "shardFilter") in
      let maxResults =
        (Option.map ~f:GetStreamInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let streamArn =
        StreamArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamArn") in
      make ?nextToken ?shardFilter ?maxResults ~streamArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" ShardIdToken.of_json in
      let shardFilter = field_map json__ "shardFilter" ShardFilter.of_json in
      let maxResults =
        field_map json__ "maxResults" GetStreamInputMaxResultsInteger.of_json in
      let streamArn = field_map_exn json__ "streamArn" StreamArn.of_json in
      make ?nextToken ?shardFilter ?maxResults ~streamArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about a specific data capture stream for an Amazon Keyspaces table. The information includes the stream's Amazon Resource Name (ARN), creation time, current status, retention period, shard composition, and associated table details. This operation helps you monitor and manage the configuration of your Amazon Keyspaces data streams."]
module TableName =
  struct
    type nonrec t = string
    let context_ = "TableName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:48) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9][a-zA-Z0-9_]{0,47}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TableName" j
    let to_json = simple_to_json to_value
  end
module StreamViewType =
  struct
    type nonrec t =
      | NEW_IMAGE 
      | OLD_IMAGE 
      | NEW_AND_OLD_IMAGES 
      | KEYS_ONLY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NEW_IMAGE -> "NEW_IMAGE"
      | OLD_IMAGE -> "OLD_IMAGE"
      | NEW_AND_OLD_IMAGES -> "NEW_AND_OLD_IMAGES"
      | KEYS_ONLY -> "KEYS_ONLY"
      | Non_static_id s -> s
    let of_string =
      function
      | "NEW_IMAGE" -> NEW_IMAGE
      | "OLD_IMAGE" -> OLD_IMAGE
      | "NEW_AND_OLD_IMAGES" -> NEW_AND_OLD_IMAGES
      | "KEYS_ONLY" -> KEYS_ONLY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamViewType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamViewType" j)
    let to_json = simple_to_json to_value
  end
module StreamStatus =
  struct
    type nonrec t =
      | ENABLING 
      | ENABLED 
      | DISABLING 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLING -> "ENABLING"
      | ENABLED -> "ENABLED"
      | DISABLING -> "DISABLING"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLING" -> ENABLING
      | "ENABLED" -> ENABLED
      | "DISABLING" -> DISABLING
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamStatus" j)
    let to_json = simple_to_json to_value
  end
module ShardIdList =
  struct
    type nonrec t = ShardId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ShardId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ShardId.of_xml)
    let of_json j =
      list_of_json ~kind:"ShardIdList" ~of_json:ShardId.of_json j
    let to_json v = composed_to_json to_value v
  end
module SequenceNumberRange =
  struct
    type nonrec t =
      {
      startingSequenceNumber: SequenceNumber.t option
        [@ocaml.doc "The starting sequence number of the range."];
      endingSequenceNumber: SequenceNumber.t option
        [@ocaml.doc
          "The ending sequence number of the range, which may be null for open-ended ranges."]}
    let make ?startingSequenceNumber =
      fun ?endingSequenceNumber ->
        fun () -> { startingSequenceNumber; endingSequenceNumber }
    let to_value x =
      structure_to_value
        [("startingSequenceNumber",
           (Option.map x.startingSequenceNumber ~f:SequenceNumber.to_value));
        ("endingSequenceNumber",
          (Option.map x.endingSequenceNumber ~f:SequenceNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endingSequenceNumber =
        (Option.map ~f:SequenceNumber.of_xml)
          (Xml.child xml_arg0 "endingSequenceNumber") in
      let startingSequenceNumber =
        (Option.map ~f:SequenceNumber.of_xml)
          (Xml.child xml_arg0 "startingSequenceNumber") in
      make ?endingSequenceNumber ?startingSequenceNumber ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endingSequenceNumber =
        field_map json__ "endingSequenceNumber" SequenceNumber.of_json in
      let startingSequenceNumber =
        field_map json__ "startingSequenceNumber" SequenceNumber.of_json in
      make ?endingSequenceNumber ?startingSequenceNumber ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a range of sequence numbers within a change data capture stream's shard for Amazon Keyspaces."]
module Shard =
  struct
    type nonrec t =
      {
      shardId: ShardId.t option
        [@ocaml.doc "A unique identifier for this shard within the stream."];
      sequenceNumberRange: SequenceNumberRange.t option
        [@ocaml.doc
          "The range of sequence numbers contained within this shard."];
      parentShardIds: ShardIdList.t option
        [@ocaml.doc
          "The identifiers of parent shards that this shard evolved from, if this shard was created through resharding."]}
    let make ?shardId =
      fun ?sequenceNumberRange ->
        fun ?parentShardIds ->
          fun () -> { shardId; sequenceNumberRange; parentShardIds }
    let to_value x =
      structure_to_value
        [("shardId", (Option.map x.shardId ~f:ShardId.to_value));
        ("sequenceNumberRange",
          (Option.map x.sequenceNumberRange ~f:SequenceNumberRange.to_value));
        ("parentShardIds",
          (Option.map x.parentShardIds ~f:ShardIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parentShardIds =
        (Option.map ~f:ShardIdList.of_xml)
          (Xml.child xml_arg0 "parentShardIds") in
      let sequenceNumberRange =
        (Option.map ~f:SequenceNumberRange.of_xml)
          (Xml.child xml_arg0 "sequenceNumberRange") in
      let shardId =
        (Option.map ~f:ShardId.of_xml) (Xml.child xml_arg0 "shardId") in
      make ?parentShardIds ?sequenceNumberRange ?shardId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parentShardIds =
        field_map json__ "parentShardIds" ShardIdList.of_json in
      let sequenceNumberRange =
        field_map json__ "sequenceNumberRange" SequenceNumberRange.of_json in
      let shardId = field_map json__ "shardId" ShardId.of_json in
      make ?parentShardIds ?sequenceNumberRange ?shardId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a uniquely identified group of change records within a change data capture stream for Amazon Keyspaces."]
module ShardDescriptionList =
  struct
    type nonrec t = Shard.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Shard.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Shard.of_xml)
    let of_json j =
      list_of_json ~kind:"ShardDescriptionList" ~of_json:Shard.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeyspaceName =
  struct
    type nonrec t = string
    let context_ = "KeyspaceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:48) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9][a-zA-Z0-9_]{0,47}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KeyspaceName" j
    let to_json = simple_to_json to_value
  end
module GetStreamOutput =
  struct
    type nonrec t =
      {
      streamArn: StreamArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the stream within Amazon Keyspaces. This ARN can be used in other API operations to reference this specific stream."];
      streamLabel: String_.t option
        [@ocaml.doc
          "A timestamp that serves as a unique identifier for this stream, used for debugging and monitoring purposes. The stream label represents the point in time when the stream was created."];
      streamStatus: StreamStatus.t option
        [@ocaml.doc
          "The current status of the stream. Values can be ENABLING, ENABLED, DISABLING, or DISABLED. Operations on the stream depend on its current status."];
      streamViewType: StreamViewType.t option
        [@ocaml.doc
          "The format of the data records in this stream. Currently, this can be one of the following options: NEW_AND_OLD_IMAGES - both versions of the row, before and after the change. This is the default. NEW_IMAGE - the version of the row after the change. OLD_IMAGE - the version of the row before the change. KEYS_ONLY - the partition and clustering keys of the row that was changed."];
      creationRequestDateTime: Date.t option
        [@ocaml.doc
          "The date and time when the request to create this stream was issued. The value is represented in ISO 8601 format."];
      keyspaceName: KeyspaceName.t option
        [@ocaml.doc
          "The name of the keyspace containing the table associated with this stream. The keyspace name is part of the table's hierarchical identifier in Amazon Keyspaces."];
      tableName: TableName.t option
        [@ocaml.doc
          "The name of the table associated with this stream. The stream captures changes to rows in this Amazon Keyspaces table."];
      shards: ShardDescriptionList.t option
        [@ocaml.doc
          "An array of shard objects associated with this stream. Each shard contains a subset of the stream's data records and has its own unique identifier. The collection of shards represents the complete stream data."];
      nextToken: ShardIdToken.t option
        [@ocaml.doc
          "A pagination token that can be used in a subsequent GetStream request. This token is returned if the response contains more shards than can be returned in a single response."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streamArn =
      fun ?streamLabel ->
        fun ?streamStatus ->
          fun ?streamViewType ->
            fun ?creationRequestDateTime ->
              fun ?keyspaceName ->
                fun ?tableName ->
                  fun ?shards ->
                    fun ?nextToken ->
                      fun () ->
                        {
                          streamArn;
                          streamLabel;
                          streamStatus;
                          streamViewType;
                          creationRequestDateTime;
                          keyspaceName;
                          tableName;
                          shards;
                          nextToken
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("streamArn", (Option.map x.streamArn ~f:StreamArn.to_value));
        ("streamLabel", (Option.map x.streamLabel ~f:String_.to_value));
        ("streamStatus",
          (Option.map x.streamStatus ~f:StreamStatus.to_value));
        ("streamViewType",
          (Option.map x.streamViewType ~f:StreamViewType.to_value));
        ("creationRequestDateTime",
          (Option.map x.creationRequestDateTime ~f:Date.to_value));
        ("keyspaceName",
          (Option.map x.keyspaceName ~f:KeyspaceName.to_value));
        ("tableName", (Option.map x.tableName ~f:TableName.to_value));
        ("shards", (Option.map x.shards ~f:ShardDescriptionList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:ShardIdToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:ShardIdToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let shards =
        (Option.map ~f:ShardDescriptionList.of_xml)
          (Xml.child xml_arg0 "shards") in
      let tableName =
        (Option.map ~f:TableName.of_xml) (Xml.child xml_arg0 "tableName") in
      let keyspaceName =
        (Option.map ~f:KeyspaceName.of_xml)
          (Xml.child xml_arg0 "keyspaceName") in
      let creationRequestDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "creationRequestDateTime") in
      let streamViewType =
        (Option.map ~f:StreamViewType.of_xml)
          (Xml.child xml_arg0 "streamViewType") in
      let streamStatus =
        (Option.map ~f:StreamStatus.of_xml)
          (Xml.child xml_arg0 "streamStatus") in
      let streamLabel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "streamLabel") in
      let streamArn =
        (Option.map ~f:StreamArn.of_xml) (Xml.child xml_arg0 "streamArn") in
      make ?nextToken ?shards ?tableName ?keyspaceName
        ?creationRequestDateTime ?streamViewType ?streamStatus ?streamLabel
        ?streamArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" ShardIdToken.of_json in
      let shards = field_map json__ "shards" ShardDescriptionList.of_json in
      let tableName = field_map json__ "tableName" TableName.of_json in
      let keyspaceName = field_map json__ "keyspaceName" KeyspaceName.of_json in
      let creationRequestDateTime =
        field_map json__ "creationRequestDateTime" Date.of_json in
      let streamViewType =
        field_map json__ "streamViewType" StreamViewType.of_json in
      let streamStatus = field_map json__ "streamStatus" StreamStatus.of_json in
      let streamLabel = field_map json__ "streamLabel" String_.of_json in
      let streamArn = field_map json__ "streamArn" StreamArn.of_json in
      make ?nextToken ?shards ?tableName ?keyspaceName
        ?creationRequestDateTime ?streamViewType ?streamStatus ?streamLabel
        ?streamArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about a specific data capture stream for an Amazon Keyspaces table. The information includes the stream's Amazon Resource Name (ARN), creation time, current status, retention period, shard composition, and associated table details. This operation helps you monitor and manage the configuration of your Amazon Keyspaces data streams."]
module StreamArnToken =
  struct
    type nonrec t = string
    let context_ = "StreamArnToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:3000) >>=
             (fun () -> check_string_min i ~min:80));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamArnToken" j
    let to_json = simple_to_json to_value
  end
module ListStreamsInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListStreamsInputMaxResultsInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ListStreamsInput =
  struct
    type nonrec t =
      {
      keyspaceName: KeyspaceName.t option
        [@ocaml.doc
          "The name of the keyspace for which to list streams. If specified, only streams associated with tables in this keyspace are returned. If omitted, streams from all keyspaces are included in the results."];
      tableName: TableName.t option
        [@ocaml.doc
          "The name of the table for which to list streams. Must be used together with keyspaceName. If specified, only streams associated with this specific table are returned."];
      maxResults: ListStreamsInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of streams to return in a single ListStreams request. The default value is 100. The minimum value is 1 and the maximum value is 100."];
      nextToken: StreamArnToken.t option
        [@ocaml.doc
          "An optional pagination token provided by a previous ListStreams operation. If this parameter is specified, the response includes only records beyond the token, up to the value specified by maxResults."]}
    let make ?keyspaceName =
      fun ?tableName ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { keyspaceName; tableName; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("keyspaceName",
           (Option.map x.keyspaceName ~f:KeyspaceName.to_value));
        ("tableName", (Option.map x.tableName ~f:TableName.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListStreamsInputMaxResultsInteger.to_value));
        ("nextToken", (Option.map x.nextToken ~f:StreamArnToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:StreamArnToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListStreamsInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let tableName =
        (Option.map ~f:TableName.of_xml) (Xml.child xml_arg0 "tableName") in
      let keyspaceName =
        (Option.map ~f:KeyspaceName.of_xml)
          (Xml.child xml_arg0 "keyspaceName") in
      make ?nextToken ?maxResults ?tableName ?keyspaceName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" StreamArnToken.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListStreamsInputMaxResultsInteger.of_json in
      let tableName = field_map json__ "tableName" TableName.of_json in
      let keyspaceName = field_map json__ "keyspaceName" KeyspaceName.of_json in
      make ?nextToken ?maxResults ?tableName ?keyspaceName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all data capture streams associated with your Amazon Keyspaces account or for a specific keyspace or table. The response includes information such as stream ARNs, table associations, creation timestamps, and current status. This operation helps you discover and manage all active data streams in your Amazon Keyspaces environment."]
module Stream =
  struct
    type nonrec t =
      {
      streamArn: StreamArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies this stream."];
      keyspaceName: KeyspaceName.t option
        [@ocaml.doc
          "The name of the keyspace containing the table associated with this stream."];
      tableName: TableName.t option
        [@ocaml.doc "The name of the table associated with this stream."];
      streamLabel: String_.t option
        [@ocaml.doc
          "A unique identifier for this stream that can be used in stream operations."]}
    let make ?streamArn =
      fun ?keyspaceName ->
        fun ?tableName ->
          fun ?streamLabel ->
            fun () -> { streamArn; keyspaceName; tableName; streamLabel }
    let to_value x =
      structure_to_value
        [("streamArn", (Option.map x.streamArn ~f:StreamArn.to_value));
        ("keyspaceName",
          (Option.map x.keyspaceName ~f:KeyspaceName.to_value));
        ("tableName", (Option.map x.tableName ~f:TableName.to_value));
        ("streamLabel", (Option.map x.streamLabel ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamLabel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "streamLabel") in
      let tableName =
        (Option.map ~f:TableName.of_xml) (Xml.child xml_arg0 "tableName") in
      let keyspaceName =
        (Option.map ~f:KeyspaceName.of_xml)
          (Xml.child xml_arg0 "keyspaceName") in
      let streamArn =
        (Option.map ~f:StreamArn.of_xml) (Xml.child xml_arg0 "streamArn") in
      make ?streamLabel ?tableName ?keyspaceName ?streamArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamLabel = field_map json__ "streamLabel" String_.of_json in
      let tableName = field_map json__ "tableName" TableName.of_json in
      let keyspaceName = field_map json__ "keyspaceName" KeyspaceName.of_json in
      let streamArn = field_map json__ "streamArn" StreamArn.of_json in
      make ?streamLabel ?tableName ?keyspaceName ?streamArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a change data capture stream for an Amazon Keyspaces table, which enables tracking and processing of data changes."]
module StreamList =
  struct
    type nonrec t = Stream.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Stream.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Stream.of_xml)
    let of_json j = list_of_json ~kind:"StreamList" ~of_json:Stream.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListStreamsOutput =
  struct
    type nonrec t =
      {
      streams: StreamList.t option
        [@ocaml.doc
          "An array of stream objects, each containing summary information about a stream including its ARN, status, and associated table information. This list includes all streams that match the request criteria."];
      nextToken: StreamArnToken.t option
        [@ocaml.doc
          "A pagination token that can be used in a subsequent ListStreams request. This token is returned if the response contains more streams than can be returned in a single response based on the maxResults parameter."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streams = fun ?nextToken -> fun () -> { streams; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("streams", (Option.map x.streams ~f:StreamList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:StreamArnToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:StreamArnToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let streams =
        (Option.map ~f:StreamList.of_xml) (Xml.child xml_arg0 "streams") in
      make ?nextToken ?streams ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" StreamArnToken.of_json in
      let streams = field_map json__ "streams" StreamList.of_json in
      make ?nextToken ?streams ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all data capture streams associated with your Amazon Keyspaces account or for a specific keyspace or table. The response includes information such as stream ARNs, table associations, creation timestamps, and current status. This operation helps you discover and manage all active data streams in your Amazon Keyspaces environment."]
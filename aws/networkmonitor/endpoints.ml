(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateMonitor: (CreateMonitorInput.t, CreateMonitorOutput.t,
  CreateMonitorOutput.error) t 
  | CreateProbe: (CreateProbeInput.t, CreateProbeOutput.t,
  CreateProbeOutput.error) t 
  | DeleteMonitor: (DeleteMonitorInput.t, DeleteMonitorOutput.t,
  DeleteMonitorOutput.error) t 
  | DeleteProbe: (DeleteProbeInput.t, DeleteProbeOutput.t,
  DeleteProbeOutput.error) t 
  | GetMonitor: (GetMonitorInput.t, GetMonitorOutput.t,
  GetMonitorOutput.error) t 
  | GetProbe: (GetProbeInput.t, GetProbeOutput.t, GetProbeOutput.error) t 
  | ListMonitors: (ListMonitorsInput.t, ListMonitorsOutput.t,
  ListMonitorsOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateMonitor: (UpdateMonitorInput.t, UpdateMonitorOutput.t,
  UpdateMonitorOutput.error) t 
  | UpdateProbe: (UpdateProbeInput.t, UpdateProbeOutput.t,
  UpdateProbeOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateMonitor -> `POST
  | CreateProbe -> `POST
  | DeleteMonitor -> `DELETE
  | DeleteProbe -> `DELETE
  | GetMonitor -> `GET
  | GetProbe -> `GET
  | ListMonitors -> `GET
  | ListTagsForResource -> `GET
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateMonitor -> `PATCH
  | UpdateProbe -> `PATCH
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateMonitor -> (Format.kasprintf Uri.of_string) "/monitors"
      | CreateProbe ->
          (Format.kasprintf Uri.of_string) "/monitors/%s/probes"
            (ResourceName.to_header x.CreateProbeInput.monitorName)
      | DeleteMonitor ->
          (Format.kasprintf Uri.of_string) "/monitors/%s"
            (ResourceName.to_header x.DeleteMonitorInput.monitorName)
      | DeleteProbe ->
          (Format.kasprintf Uri.of_string) "/monitors/%s/probes/%s"
            (ResourceName.to_header x.DeleteProbeInput.monitorName)
            (ProbeId.to_header x.DeleteProbeInput.probeId)
      | GetMonitor ->
          (Format.kasprintf Uri.of_string) "/monitors/%s"
            (ResourceName.to_header x.GetMonitorInput.monitorName)
      | GetProbe ->
          (Format.kasprintf Uri.of_string) "/monitors/%s/probes/%s"
            (ResourceName.to_header x.GetProbeInput.monitorName)
            (ProbeId.to_header x.GetProbeInput.probeId)
      | ListMonitors ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/monitors")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map ~f:(fun v -> ("state", (String_.to_header v)))
                 x.state])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.ListTagsForResourceInput.resourceArn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (Arn.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateMonitor ->
          (Format.kasprintf Uri.of_string) "/monitors/%s"
            (ResourceName.to_header x.UpdateMonitorInput.monitorName)
      | UpdateProbe ->
          (Format.kasprintf Uri.of_string) "/monitors/%s/probes/%s"
            (ResourceName.to_header x.UpdateProbeInput.monitorName)
            (ProbeId.to_header x.UpdateProbeInput.probeId))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateMonitor ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("monitorName",
                           (ResourceName.to_value
                              req.CreateMonitorInput.monitorName));
                      Option.map req.CreateMonitorInput.probes
                        ~f:(fun x ->
                              ("probes",
                                (CreateMonitorProbeInputList.to_value x)));
                      Option.map req.CreateMonitorInput.aggregationPeriod
                        ~f:(fun x ->
                              ("aggregationPeriod",
                                (AggregationPeriod.to_value x)));
                      Option.map req.CreateMonitorInput.clientToken
                        ~f:(fun x -> ("clientToken", (String_.to_value x)));
                      Option.map req.CreateMonitorInput.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateProbe ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("probe",
                           (ProbeInput.to_value req.CreateProbeInput.probe));
                      Option.map req.CreateProbeInput.clientToken
                        ~f:(fun x -> ("clientToken", (String_.to_value x)));
                      Option.map req.CreateProbeInput.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteMonitor -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteProbe -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetMonitor ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetProbe ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListMonitors ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceInput.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateMonitor -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateProbe -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateMonitor ->
      if is_success
      then Ok (CreateMonitorOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateMonitorOutput.error_of_json))
  | CreateProbe ->
      if is_success
      then Ok (CreateProbeOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateProbeOutput.error_of_json))
  | DeleteMonitor ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteMonitorOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteMonitorOutput.error_of_json))
  | DeleteProbe ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteProbeOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteProbeOutput.error_of_json))
  | GetMonitor ->
      if is_success
      then Ok (GetMonitorOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetMonitorOutput.error_of_json))
  | GetProbe ->
      if is_success
      then Ok (GetProbeOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetProbeOutput.error_of_json))
  | ListMonitors ->
      if is_success
      then Ok (ListMonitorsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListMonitorsOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateMonitor ->
      if is_success
      then Ok (UpdateMonitorOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateMonitorOutput.error_of_json))
  | UpdateProbe ->
      if is_success
      then Ok (UpdateProbeOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateProbeOutput.error_of_json))
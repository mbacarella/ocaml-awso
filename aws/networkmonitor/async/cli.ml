(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and probes =
         flag "probes" (optional json_arg)
           ~doc:"JSON CreateMonitorProbeInputList"
       and aggregationPeriod =
         flag "aggregation-period" (optional json_arg)
           ~doc:"JSON AggregationPeriod"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_monitor
           (Values.CreateMonitorInput.make
              ?probes:(Option.map
                         ~f:Values.CreateMonitorProbeInputList.of_json probes)
              ?aggregationPeriod:(Option.map
                                    ~f:Values.AggregationPeriod.of_json
                                    aggregationPeriod) ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~monitorName
              ()) (Some Values.CreateMonitorOutput.to_json)
           (Some Values.CreateMonitorOutput.error_to_json)])
let create_probe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and probe = flag "probe" (required json_arg) ~doc:"JSON ProbeInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_probe
           (Values.CreateProbeInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~monitorName
              ~probe:(Values.ProbeInput.of_json probe) ())
           (Some Values.CreateProbeOutput.to_json)
           (Some Values.CreateProbeOutput.error_to_json)])
let delete_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_monitor (Values.DeleteMonitorInput.make ~monitorName ())
           (Some Values.DeleteMonitorOutput.to_json)
           (Some Values.DeleteMonitorOutput.error_to_json)])
let delete_probe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and probeId = flag "probe-id" (required string) ~doc:"STRING ProbeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_probe
           (Values.DeleteProbeInput.make ~monitorName ~probeId ())
           (Some Values.DeleteProbeOutput.to_json)
           (Some Values.DeleteProbeOutput.error_to_json)])
let get_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_monitor (Values.GetMonitorInput.make ~monitorName ())
           (Some Values.GetMonitorOutput.to_json)
           (Some Values.GetMonitorOutput.error_to_json)])
let get_probe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and probeId = flag "probe-id" (required string) ~doc:"STRING ProbeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_probe (Values.GetProbeInput.make ~monitorName ~probeId ())
           (Some Values.GetProbeOutput.to_json)
           (Some Values.GetProbeOutput.error_to_json)])
let list_monitors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and state = flag "state" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_monitors
           (Values.ListMonitorsInput.make ?nextToken ?maxResults ?state ())
           (Some Values.ListMonitorsOutput.to_json)
           (Some Values.ListMonitorsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and aggregationPeriod =
         flag "aggregation-period" (required json_arg)
           ~doc:"JSON AggregationPeriod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_monitor
           (Values.UpdateMonitorInput.make ~monitorName
              ~aggregationPeriod:(Values.AggregationPeriod.of_json
                                    aggregationPeriod) ())
           (Some Values.UpdateMonitorOutput.to_json)
           (Some Values.UpdateMonitorOutput.error_to_json)])
let update_probe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and state = flag "state" (optional json_arg) ~doc:"JSON ProbeState"
       and destination =
         flag "destination" (optional string) ~doc:"STRING Destination"
       and destinationPort =
         flag "destination-port" (optional int) ~doc:"INT Port"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON Protocol"
       and packetSize =
         flag "packet-size" (optional int) ~doc:"INT PacketSize"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and probeId = flag "probe-id" (required string) ~doc:"STRING ProbeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_probe
           (Values.UpdateProbeInput.make
              ?state:(Option.map ~f:Values.ProbeState.of_json state)
              ?destination ?destinationPort
              ?protocol:(Option.map ~f:Values.Protocol.of_json protocol)
              ?packetSize ~monitorName ~probeId ())
           (Some Values.UpdateProbeOutput.to_json)
           (Some Values.UpdateProbeOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-monitor", create_monitor);
    ("create-probe", create_probe);
    ("delete-monitor", delete_monitor);
    ("delete-probe", delete_probe);
    ("get-monitor", get_monitor);
    ("get-probe", get_probe);
    ("list-monitors", list_monitors);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-monitor", update_monitor);
    ("update-probe", update_probe)]
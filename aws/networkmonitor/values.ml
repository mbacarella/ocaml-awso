(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.networkmonitor
let apiVersion = "2023-08-01"
let endpointPrefix = "networkmonitor"
let serviceFullName = "Amazon CloudWatch Network Monitor"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module AggregationPeriod =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:30L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module MonitorArn =
  struct
    type nonrec t = string
    let context_ = "MonitorArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () -> check_pattern i ~pattern:"arn:.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MonitorArn" j
    let to_json = simple_to_json to_value
  end
module MonitorState =
  struct
    type nonrec t =
      | PENDING 
      | ACTIVE 
      | INACTIVE 
      | ERROR 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | ERROR -> "ERROR"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | "ERROR" -> ERROR
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MonitorState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MonitorState" j)
    let to_json = simple_to_json to_value
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AddressFamily =
  struct
    type nonrec t =
      | IPV4 
      | IPV6 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | IPV4 -> "IPV4" | IPV6 -> "IPV6" | Non_static_id s -> s
    let of_string =
      function | "IPV4" -> IPV4 | "IPV6" -> IPV6 | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AddressFamily" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AddressFamily" j)
    let to_json = simple_to_json to_value
  end
module Arn =
  struct
    type nonrec t = string
    let context_ = "Arn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"arn:.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Arn" j
    let to_json = simple_to_json to_value
  end
module Destination =
  struct
    type nonrec t = string
    let context_ = "Destination"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Destination" j
    let to_json = simple_to_json to_value
  end
module Iso8601Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module PacketSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:8500) >>=
             (fun () -> check_int_min i ~min:56));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PacketSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Port =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:65536) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Port" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ProbeId =
  struct
    type nonrec t = string
    let context_ = "ProbeId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"probe-[a-z0-9A-Z-]{21,64}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProbeId" j
    let to_json = simple_to_json to_value
  end
module ProbeState =
  struct
    type nonrec t =
      | PENDING 
      | ACTIVE 
      | INACTIVE 
      | ERROR 
      | DELETING 
      | DELETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | ERROR -> "ERROR"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | "ERROR" -> ERROR
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ProbeState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProbeState" j)
    let to_json = simple_to_json to_value
  end
module Protocol =
  struct
    type nonrec t =
      | TCP 
      | ICMP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | TCP -> "TCP" | ICMP -> "ICMP" | Non_static_id s -> s
    let of_string =
      function | "TCP" -> TCP | "ICMP" -> ICMP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Protocol" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Protocol" j)
    let to_json = simple_to_json to_value
  end
module VpcId =
  struct
    type nonrec t = string
    let context_ = "VpcId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"vpc-[a-zA-Z0-9]{8,32}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VpcId" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module MonitorSummary =
  struct
    type nonrec t =
      {
      monitorArn: MonitorArn.t option [@ocaml.doc "The ARN of the monitor."];
      monitorName: ResourceName.t option
        [@ocaml.doc "The name of the monitor."];
      state: MonitorState.t option [@ocaml.doc "The state of the monitor."];
      aggregationPeriod: AggregationPeriod.t option
        [@ocaml.doc
          "The time, in seconds, that metrics are collected and sent to Amazon CloudWatch. Valid values are either 30 or 60."];
      tags: TagMap.t option
        [@ocaml.doc "The list of key-value pairs assigned to the monitor."]}
    let make ?monitorArn =
      fun ?monitorName ->
        fun ?state ->
          fun ?aggregationPeriod ->
            fun ?tags ->
              fun () ->
                { monitorArn; monitorName; state; aggregationPeriod; tags }
    let to_value x =
      structure_to_value
        [("monitorArn", (Option.map x.monitorArn ~f:MonitorArn.to_value));
        ("monitorName", (Option.map x.monitorName ~f:ResourceName.to_value));
        ("state", (Option.map x.state ~f:MonitorState.to_value));
        ("aggregationPeriod",
          (Option.map x.aggregationPeriod ~f:AggregationPeriod.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let aggregationPeriod =
        (Option.map ~f:AggregationPeriod.of_xml)
          (Xml.child xml_arg0 "aggregationPeriod") in
      let state =
        (Option.map ~f:MonitorState.of_xml) (Xml.child xml_arg0 "state") in
      let monitorName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "monitorName") in
      let monitorArn =
        (Option.map ~f:MonitorArn.of_xml) (Xml.child xml_arg0 "monitorArn") in
      make ?tags ?aggregationPeriod ?state ?monitorName ?monitorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let aggregationPeriod =
        field_map json__ "aggregationPeriod" AggregationPeriod.of_json in
      let state = field_map json__ "state" MonitorState.of_json in
      let monitorName = field_map json__ "monitorName" ResourceName.of_json in
      let monitorArn = field_map json__ "monitorArn" MonitorArn.of_json in
      make ?tags ?aggregationPeriod ?state ?monitorName ?monitorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Displays summary information about a monitor."]
module Probe =
  struct
    type nonrec t =
      {
      probeId: ProbeId.t option [@ocaml.doc "The ID of the probe."];
      probeArn: Arn.t option [@ocaml.doc "The ARN of the probe."];
      sourceArn: Arn.t option
        [@ocaml.doc "The ARN of the probe source subnet."];
      destination: Destination.t option
        [@ocaml.doc
          "The destination for the probe. This should be either an IPV4 or IPV6."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The destination port for the probe. This is required only if the protocol is TCP and must be a number between 1 and 65536."];
      protocol: Protocol.t option
        [@ocaml.doc
          "The network protocol for the destination. This can be either TCP or ICMP. If the protocol is TCP, then port is also required."];
      packetSize: PacketSize.t option
        [@ocaml.doc
          "The size of the packets traveling between the source and destination. This must be a number between 56 and"];
      addressFamily: AddressFamily.t option
        [@ocaml.doc "The IPv4 or IPv6 address for the probe."];
      vpcId: VpcId.t option [@ocaml.doc "The ID of the source VPC subnet."];
      state: ProbeState.t option [@ocaml.doc "The state of the probe."];
      createdAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date the probe was created."];
      modifiedAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date that the probe was last modified."];
      tags: TagMap.t option
        [@ocaml.doc
          "The list of key-value pairs created and assigned to the probe."]}
    let make ?probeId =
      fun ?probeArn ->
        fun ?sourceArn ->
          fun ?destination ->
            fun ?destinationPort ->
              fun ?protocol ->
                fun ?packetSize ->
                  fun ?addressFamily ->
                    fun ?vpcId ->
                      fun ?state ->
                        fun ?createdAt ->
                          fun ?modifiedAt ->
                            fun ?tags ->
                              fun () ->
                                {
                                  probeId;
                                  probeArn;
                                  sourceArn;
                                  destination;
                                  destinationPort;
                                  protocol;
                                  packetSize;
                                  addressFamily;
                                  vpcId;
                                  state;
                                  createdAt;
                                  modifiedAt;
                                  tags
                                }
    let to_value x =
      structure_to_value
        [("probeId", (Option.map x.probeId ~f:ProbeId.to_value));
        ("probeArn", (Option.map x.probeArn ~f:Arn.to_value));
        ("sourceArn", (Option.map x.sourceArn ~f:Arn.to_value));
        ("destination", (Option.map x.destination ~f:Destination.to_value));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Option.map x.protocol ~f:Protocol.to_value));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value));
        ("addressFamily",
          (Option.map x.addressFamily ~f:AddressFamily.to_value));
        ("vpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("state", (Option.map x.state ~f:ProbeState.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Iso8601Timestamp.to_value));
        ("modifiedAt",
          (Option.map x.modifiedAt ~f:Iso8601Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let modifiedAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "modifiedAt") in
      let createdAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let state =
        (Option.map ~f:ProbeState.of_xml) (Xml.child xml_arg0 "state") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "vpcId") in
      let addressFamily =
        (Option.map ~f:AddressFamily.of_xml)
          (Xml.child xml_arg0 "addressFamily") in
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        (Option.map ~f:Protocol.of_xml) (Xml.child xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "destination") in
      let sourceArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "sourceArn") in
      let probeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "probeArn") in
      let probeId =
        (Option.map ~f:ProbeId.of_xml) (Xml.child xml_arg0 "probeId") in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let modifiedAt = field_map json__ "modifiedAt" Iso8601Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Iso8601Timestamp.of_json in
      let state = field_map json__ "state" ProbeState.of_json in
      let vpcId = field_map json__ "vpcId" VpcId.of_json in
      let addressFamily =
        field_map json__ "addressFamily" AddressFamily.of_json in
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination = field_map json__ "destination" Destination.of_json in
      let sourceArn = field_map json__ "sourceArn" Arn.of_json in
      let probeArn = field_map json__ "probeArn" Arn.of_json in
      let probeId = field_map json__ "probeId" ProbeId.of_json in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes information about a network monitor probe."]
module CreateMonitorProbeInput =
  struct
    type nonrec t =
      {
      sourceArn: Arn.t [@ocaml.doc "The ARN of the subnet."];
      destination: Destination.t
        [@ocaml.doc
          "The destination IP address. This must be either IPV4 or IPV6."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The port associated with the destination. This is required only if the protocol is TCP and must be a number between 1 and 65536."];
      protocol: Protocol.t
        [@ocaml.doc
          "The protocol used for the network traffic between the source and destination. This must be either TCP or ICMP."];
      packetSize: PacketSize.t option
        [@ocaml.doc
          "The size of the packets sent between the source and destination. This must be a number between 56 and 8500."];
      probeTags: TagMap.t option
        [@ocaml.doc
          "The list of key-value pairs created and assigned to the monitor."]}
    let context_ = "CreateMonitorProbeInput"
    let make ?destinationPort =
      fun ?packetSize ->
        fun ?probeTags ->
          fun ~sourceArn ->
            fun ~destination ->
              fun ~protocol ->
                fun () ->
                  {
                    destinationPort;
                    packetSize;
                    probeTags;
                    sourceArn;
                    destination;
                    protocol
                  }
    let to_value x =
      structure_to_value
        [("sourceArn", (Some (Arn.to_value x.sourceArn)));
        ("destination", (Some (Destination.to_value x.destination)));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Some (Protocol.to_value x.protocol)));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value));
        ("probeTags", (Option.map x.probeTags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let probeTags =
        (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "probeTags") in
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        Protocol.of_xml (Xml.child_exn ~context:context_ xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        Destination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "destination") in
      let sourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "sourceArn") in
      make ?probeTags ?packetSize ~protocol ?destinationPort ~destination
        ~sourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let probeTags = field_map json__ "probeTags" TagMap.of_json in
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map_exn json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination =
        field_map_exn json__ "destination" Destination.of_json in
      let sourceArn = field_map_exn json__ "sourceArn" Arn.of_json in
      make ?probeTags ?packetSize ~protocol ?destinationPort ~destination
        ~sourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a monitor probe."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request processing has failed because of an unknown error, exception or failure."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource does not exist."]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This request exceeds a service quota."]
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling"]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One of the parameters for the request is not valid."]
module ConflictException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This operation attempted to create a resource that already exists."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module MonitorList =
  struct
    type nonrec t = MonitorSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MonitorSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MonitorSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"MonitorList" ~of_json:MonitorSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:25) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4096) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module ProbeList =
  struct
    type nonrec t = Probe.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Probe.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Probe.of_xml)
    let of_json j = list_of_json ~kind:"ProbeList" ~of_json:Probe.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProbeInput =
  struct
    type nonrec t =
      {
      sourceArn: Arn.t [@ocaml.doc "The ARN of the subnet."];
      destination: Destination.t
        [@ocaml.doc
          "The destination IP address. This must be either IPV4 or IPV6."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The port associated with the destination. This is required only if the protocol is TCP and must be a number between 1 and 65536."];
      protocol: Protocol.t
        [@ocaml.doc
          "The protocol used for the network traffic between the source and destination. This must be either TCP or ICMP."];
      packetSize: PacketSize.t option
        [@ocaml.doc
          "The size of the packets sent between the source and destination. This must be a number between 56 and 8500."];
      tags: TagMap.t option
        [@ocaml.doc
          "The list of key-value pairs created and assigned to the monitor."]}
    let context_ = "ProbeInput"
    let make ?destinationPort =
      fun ?packetSize ->
        fun ?tags ->
          fun ~sourceArn ->
            fun ~destination ->
              fun ~protocol ->
                fun () ->
                  {
                    destinationPort;
                    packetSize;
                    tags;
                    sourceArn;
                    destination;
                    protocol
                  }
    let to_value x =
      structure_to_value
        [("sourceArn", (Some (Arn.to_value x.sourceArn)));
        ("destination", (Some (Destination.to_value x.destination)));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Some (Protocol.to_value x.protocol)));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        Protocol.of_xml (Xml.child_exn ~context:context_ xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        Destination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "destination") in
      let sourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "sourceArn") in
      make ?tags ?packetSize ~protocol ?destinationPort ~destination
        ~sourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map_exn json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination =
        field_map_exn json__ "destination" Destination.of_json in
      let sourceArn = field_map_exn json__ "sourceArn" Arn.of_json in
      make ?tags ?packetSize ~protocol ?destinationPort ~destination
        ~sourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Defines a probe when creating a probe or monitor."]
module CreateMonitorProbeInputList =
  struct
    type nonrec t = CreateMonitorProbeInput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CreateMonitorProbeInput.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateMonitorProbeInput.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateMonitorProbeInputList"
        ~of_json:CreateMonitorProbeInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateProbeOutput =
  struct
    type nonrec t =
      {
      probeId: ProbeId.t option [@ocaml.doc "The updated ID of the probe."];
      probeArn: Arn.t option [@ocaml.doc "The updated ARN of the probe."];
      sourceArn: Arn.t option
        [@ocaml.doc "The updated ARN of the source subnet."];
      destination: Destination.t option
        [@ocaml.doc "The updated destination IP address for the probe."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The updated destination port. This must be a number between 1 and 65536."];
      protocol: Protocol.t option
        [@ocaml.doc "The updated protocol for the probe."];
      packetSize: PacketSize.t option
        [@ocaml.doc "The updated packet size for the probe."];
      addressFamily: AddressFamily.t option
        [@ocaml.doc
          "The updated IP address family. This must be either IPV4 or IPV6."];
      vpcId: VpcId.t option
        [@ocaml.doc "The updated ID of the source VPC subnet ID."];
      state: ProbeState.t option
        [@ocaml.doc "The state of the updated probe."];
      createdAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date that the probe was created."];
      modifiedAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date that the probe was last updated."];
      tags: TagMap.t option [@ocaml.doc "Update tags for a probe."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?probeId =
      fun ?probeArn ->
        fun ?sourceArn ->
          fun ?destination ->
            fun ?destinationPort ->
              fun ?protocol ->
                fun ?packetSize ->
                  fun ?addressFamily ->
                    fun ?vpcId ->
                      fun ?state ->
                        fun ?createdAt ->
                          fun ?modifiedAt ->
                            fun ?tags ->
                              fun () ->
                                {
                                  probeId;
                                  probeArn;
                                  sourceArn;
                                  destination;
                                  destinationPort;
                                  protocol;
                                  packetSize;
                                  addressFamily;
                                  vpcId;
                                  state;
                                  createdAt;
                                  modifiedAt;
                                  tags
                                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("probeId", (Option.map x.probeId ~f:ProbeId.to_value));
        ("probeArn", (Option.map x.probeArn ~f:Arn.to_value));
        ("sourceArn", (Option.map x.sourceArn ~f:Arn.to_value));
        ("destination", (Option.map x.destination ~f:Destination.to_value));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Option.map x.protocol ~f:Protocol.to_value));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value));
        ("addressFamily",
          (Option.map x.addressFamily ~f:AddressFamily.to_value));
        ("vpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("state", (Option.map x.state ~f:ProbeState.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Iso8601Timestamp.to_value));
        ("modifiedAt",
          (Option.map x.modifiedAt ~f:Iso8601Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let modifiedAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "modifiedAt") in
      let createdAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let state =
        (Option.map ~f:ProbeState.of_xml) (Xml.child xml_arg0 "state") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "vpcId") in
      let addressFamily =
        (Option.map ~f:AddressFamily.of_xml)
          (Xml.child xml_arg0 "addressFamily") in
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        (Option.map ~f:Protocol.of_xml) (Xml.child xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "destination") in
      let sourceArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "sourceArn") in
      let probeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "probeArn") in
      let probeId =
        (Option.map ~f:ProbeId.of_xml) (Xml.child xml_arg0 "probeId") in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let modifiedAt = field_map json__ "modifiedAt" Iso8601Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Iso8601Timestamp.of_json in
      let state = field_map json__ "state" ProbeState.of_json in
      let vpcId = field_map json__ "vpcId" VpcId.of_json in
      let addressFamily =
        field_map json__ "addressFamily" AddressFamily.of_json in
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination = field_map json__ "destination" Destination.of_json in
      let sourceArn = field_map json__ "sourceArn" Arn.of_json in
      let probeArn = field_map json__ "probeArn" Arn.of_json in
      let probeId = field_map json__ "probeId" ProbeId.of_json in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a monitor probe. This action requires both the monitorName and probeId parameters. Run ListMonitors to get a list of monitor names. Run GetMonitor to get a list of probes and probe IDs. You can update the following para create a monitor with probes using this command. For each probe, you define the following: state\226\128\148The state of the probe. destination\226\128\148 The target destination IP address for the probe. destinationPort\226\128\148Required only if the protocol is TCP. protocol\226\128\148The communication protocol between the source and destination. This will be either TCP or ICMP. packetSize\226\128\148The size of the packets. This must be a number between 56 and 8500. (Optional) tags \226\128\148Key-value pairs created and assigned to the probe."]
module UpdateProbeInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc
          "The name of the monitor that the probe was updated for."];
      probeId: ProbeId.t [@ocaml.doc "The ID of the probe to update."];
      state: ProbeState.t option
        [@ocaml.doc "The state of the probe update."];
      destination: Destination.t option
        [@ocaml.doc
          "The updated IP address for the probe destination. This must be either an IPv4 or IPv6 address."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The updated port for the probe destination. This is required only if the protocol is TCP and must be a number between 1 and 65536."];
      protocol: Protocol.t option
        [@ocaml.doc
          "The updated network protocol for the destination. This can be either TCP or ICMP. If the protocol is TCP, then port is also required."];
      packetSize: PacketSize.t option
        [@ocaml.doc
          "he updated packets size for network traffic between the source and destination. This must be a number between 56 and 8500."]}
    let context_ = "UpdateProbeInput"
    let make ?state =
      fun ?destination ->
        fun ?destinationPort ->
          fun ?protocol ->
            fun ?packetSize ->
              fun ~monitorName ->
                fun ~probeId ->
                  fun () ->
                    {
                      state;
                      destination;
                      destinationPort;
                      protocol;
                      packetSize;
                      monitorName;
                      probeId
                    }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)));
        ("probeId", (Some (ProbeId.to_value x.probeId)));
        ("state", (Option.map x.state ~f:ProbeState.to_value));
        ("destination", (Option.map x.destination ~f:Destination.to_value));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Option.map x.protocol ~f:Protocol.to_value));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        (Option.map ~f:Protocol.of_xml) (Xml.child xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "destination") in
      let state =
        (Option.map ~f:ProbeState.of_xml) (Xml.child xml_arg0 "state") in
      let probeId =
        ProbeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "probeId") in
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ?packetSize ?protocol ?destinationPort ?destination ?state
        ~probeId ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination = field_map json__ "destination" Destination.of_json in
      let state = field_map json__ "state" ProbeState.of_json in
      let probeId = field_map_exn json__ "probeId" ProbeId.of_json in
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ?packetSize ?protocol ?destinationPort ?destination ?state
        ~probeId ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a monitor probe. This action requires both the monitorName and probeId parameters. Run ListMonitors to get a list of monitor names. Run GetMonitor to get a list of probes and probe IDs. You can update the following para create a monitor with probes using this command. For each probe, you define the following: state\226\128\148The state of the probe. destination\226\128\148 The target destination IP address for the probe. destinationPort\226\128\148Required only if the protocol is TCP. protocol\226\128\148The communication protocol between the source and destination. This will be either TCP or ICMP. packetSize\226\128\148The size of the packets. This must be a number between 56 and 8500. (Optional) tags \226\128\148Key-value pairs created and assigned to the probe."]
module UpdateMonitorOutput =
  struct
    type nonrec t =
      {
      monitorArn: MonitorArn.t option
        [@ocaml.doc "The ARN of the monitor that was updated."];
      monitorName: ResourceName.t option
        [@ocaml.doc "The name of the monitor that was updated."];
      state: MonitorState.t option
        [@ocaml.doc "The state of the updated monitor."];
      aggregationPeriod: AggregationPeriod.t option
        [@ocaml.doc "The changed aggregation period."];
      tags: TagMap.t option
        [@ocaml.doc
          "The list of key-value pairs associated with the monitor."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?monitorArn =
      fun ?monitorName ->
        fun ?state ->
          fun ?aggregationPeriod ->
            fun ?tags ->
              fun () ->
                { monitorArn; monitorName; state; aggregationPeriod; tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("monitorArn", (Option.map x.monitorArn ~f:MonitorArn.to_value));
        ("monitorName", (Option.map x.monitorName ~f:ResourceName.to_value));
        ("state", (Option.map x.state ~f:MonitorState.to_value));
        ("aggregationPeriod",
          (Option.map x.aggregationPeriod ~f:AggregationPeriod.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let aggregationPeriod =
        (Option.map ~f:AggregationPeriod.of_xml)
          (Xml.child xml_arg0 "aggregationPeriod") in
      let state =
        (Option.map ~f:MonitorState.of_xml) (Xml.child xml_arg0 "state") in
      let monitorName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "monitorName") in
      let monitorArn =
        (Option.map ~f:MonitorArn.of_xml) (Xml.child xml_arg0 "monitorArn") in
      make ?tags ?aggregationPeriod ?state ?monitorName ?monitorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let aggregationPeriod =
        field_map json__ "aggregationPeriod" AggregationPeriod.of_json in
      let state = field_map json__ "state" MonitorState.of_json in
      let monitorName = field_map json__ "monitorName" ResourceName.of_json in
      let monitorArn = field_map json__ "monitorArn" MonitorArn.of_json in
      make ?tags ?aggregationPeriod ?state ?monitorName ?monitorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the aggregationPeriod for a monitor. Monitors support an aggregationPeriod of either 30 or 60 seconds. This action requires the monitorName and probeId parameter. Run ListMonitors to get a list of monitor names."]
module UpdateMonitorInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc "The name of the monitor to update."];
      aggregationPeriod: AggregationPeriod.t
        [@ocaml.doc
          "The aggregation time, in seconds, to change to. This must be either 30 or 60."]}
    let context_ = "UpdateMonitorInput"
    let make ~monitorName =
      fun ~aggregationPeriod -> fun () -> { monitorName; aggregationPeriod }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)));
        ("aggregationPeriod",
          (Some (AggregationPeriod.to_value x.aggregationPeriod)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aggregationPeriod =
        AggregationPeriod.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "aggregationPeriod") in
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ~aggregationPeriod ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let aggregationPeriod =
        field_map_exn json__ "aggregationPeriod" AggregationPeriod.of_json in
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ~aggregationPeriod ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the aggregationPeriod for a monitor. Monitors support an aggregationPeriod of either 30 or 60 seconds. This action requires the monitorName and probeId parameter. Run ListMonitors to get a list of monitor names."]
module UntagResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes a key-value pair from a monitor or probe."]
module UntagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc
          "The ARN of the monitor or probe that the tag should be removed from."];
      tagKeys: TagKeyList.t [@ocaml.doc "The key-value pa"]}
    let context_ = "UntagResourceInput"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json__ "resourceArn" Arn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes a key-value pair from a monitor or probe."]
module TagResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds key-value pairs to a monitor or probe."]
module TagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc "The ARN of the monitor or probe to tag."];
      tags: TagMap.t
        [@ocaml.doc
          "The list of key-value pairs assigned to the monitor or probe."]}
    let context_ = "TagResourceInput"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagMap.of_json in
      let resourceArn = field_map_exn json__ "resourceArn" Arn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds key-value pairs to a monitor or probe."]
module ListTagsForResourceOutput =
  struct
    type nonrec t =
      {
      tags: TagMap.t option
        [@ocaml.doc "Lists the tags assigned to the resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags assigned to this resource."]
module ListTagsForResourceInput =
  struct
    type nonrec t = {
      resourceArn: Arn.t [@ocaml.doc "The"]}
    let context_ = "ListTagsForResourceInput"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn = field_map_exn json__ "resourceArn" Arn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags assigned to this resource."]
module ListMonitorsOutput =
  struct
    type nonrec t =
      {
      monitors: MonitorList.t option
        [@ocaml.doc "Lists individual details about each of your monitors."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?monitors = fun ?nextToken -> fun () -> { monitors; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("monitors", (Option.map x.monitors ~f:MonitorList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let monitors =
        (Option.map ~f:MonitorList.of_xml) (Xml.child xml_arg0 "monitors") in
      make ?nextToken ?monitors ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let monitors = field_map json__ "monitors" MonitorList.of_json in
      make ?nextToken ?monitors ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of all of your monitors."]
module ListMonitorsInput =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value. If MaxResults is given a value larger than 100, only 100 results are returned."];
      state: String_.t option
        [@ocaml.doc "The list of all monitors and their states."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?state -> fun () -> { nextToken; maxResults; state }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("state", (Option.map x.state ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "state") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?state ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let state = field_map json__ "state" String_.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let nextToken = field_map json__ "nextToken" PaginationToken.of_json in
      make ?state ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of all of your monitors."]
module GetProbeOutput =
  struct
    type nonrec t =
      {
      probeId: ProbeId.t option
        [@ocaml.doc "The ID of the probe for which details are returned."];
      probeArn: Arn.t option [@ocaml.doc "The ARN of the probe."];
      sourceArn: Arn.t option [@ocaml.doc "The ARN of the probe."];
      destination: Destination.t option
        [@ocaml.doc
          "The destination IP address for the monitor. This must be either an IPv4 or IPv6 address."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The port associated with the destination. This is required only if the protocol is TCP and must be a number between 1 and 65536."];
      protocol: Protocol.t option
        [@ocaml.doc
          "The protocol used for the network traffic between the source and destination. This must be either TCP or ICMP."];
      packetSize: PacketSize.t option
        [@ocaml.doc
          "The size of the packets sent between the source and destination. This must be a number between 56 and 8500."];
      addressFamily: AddressFamily.t option
        [@ocaml.doc "Indicates whether the IP address is IPV4 or IPV6."];
      vpcId: VpcId.t option
        [@ocaml.doc "The ID of the source VPC or subnet."];
      state: ProbeState.t option [@ocaml.doc "The state of the probe."];
      createdAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date that the probe was created."];
      modifiedAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date that the probe was last modified."];
      tags: TagMap.t option
        [@ocaml.doc "The list of key-value pairs assigned to the probe."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?probeId =
      fun ?probeArn ->
        fun ?sourceArn ->
          fun ?destination ->
            fun ?destinationPort ->
              fun ?protocol ->
                fun ?packetSize ->
                  fun ?addressFamily ->
                    fun ?vpcId ->
                      fun ?state ->
                        fun ?createdAt ->
                          fun ?modifiedAt ->
                            fun ?tags ->
                              fun () ->
                                {
                                  probeId;
                                  probeArn;
                                  sourceArn;
                                  destination;
                                  destinationPort;
                                  protocol;
                                  packetSize;
                                  addressFamily;
                                  vpcId;
                                  state;
                                  createdAt;
                                  modifiedAt;
                                  tags
                                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("probeId", (Option.map x.probeId ~f:ProbeId.to_value));
        ("probeArn", (Option.map x.probeArn ~f:Arn.to_value));
        ("sourceArn", (Option.map x.sourceArn ~f:Arn.to_value));
        ("destination", (Option.map x.destination ~f:Destination.to_value));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Option.map x.protocol ~f:Protocol.to_value));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value));
        ("addressFamily",
          (Option.map x.addressFamily ~f:AddressFamily.to_value));
        ("vpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("state", (Option.map x.state ~f:ProbeState.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Iso8601Timestamp.to_value));
        ("modifiedAt",
          (Option.map x.modifiedAt ~f:Iso8601Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let modifiedAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "modifiedAt") in
      let createdAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let state =
        (Option.map ~f:ProbeState.of_xml) (Xml.child xml_arg0 "state") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "vpcId") in
      let addressFamily =
        (Option.map ~f:AddressFamily.of_xml)
          (Xml.child xml_arg0 "addressFamily") in
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        (Option.map ~f:Protocol.of_xml) (Xml.child xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "destination") in
      let sourceArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "sourceArn") in
      let probeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "probeArn") in
      let probeId =
        (Option.map ~f:ProbeId.of_xml) (Xml.child xml_arg0 "probeId") in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let modifiedAt = field_map json__ "modifiedAt" Iso8601Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Iso8601Timestamp.of_json in
      let state = field_map json__ "state" ProbeState.of_json in
      let vpcId = field_map json__ "vpcId" VpcId.of_json in
      let addressFamily =
        field_map json__ "addressFamily" AddressFamily.of_json in
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination = field_map json__ "destination" Destination.of_json in
      let sourceArn = field_map json__ "sourceArn" Arn.of_json in
      let probeArn = field_map json__ "probeArn" Arn.of_json in
      let probeId = field_map json__ "probeId" ProbeId.of_json in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the details about a probe. This action requires both the monitorName and probeId parameters. Run ListMonitors to get a list of monitor names. Run GetMonitor to get a list of probes and probe IDs."]
module GetProbeInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc
          "The name of the monitor associated with the probe. Run ListMonitors to get a list of monitor names."];
      probeId: ProbeId.t
        [@ocaml.doc
          "The ID of the probe to get information about. Run GetMonitor action to get a list of probes and probe IDs for the monitor."]}
    let context_ = "GetProbeInput"
    let make ~monitorName =
      fun ~probeId -> fun () -> { monitorName; probeId }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)));
        ("probeId", (Some (ProbeId.to_value x.probeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let probeId =
        ProbeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "probeId") in
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ~probeId ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let probeId = field_map_exn json__ "probeId" ProbeId.of_json in
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ~probeId ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the details about a probe. This action requires both the monitorName and probeId parameters. Run ListMonitors to get a list of monitor names. Run GetMonitor to get a list of probes and probe IDs."]
module GetMonitorOutput =
  struct
    type nonrec t =
      {
      monitorArn: MonitorArn.t option
        [@ocaml.doc "The ARN of the selected monitor."];
      monitorName: ResourceName.t option
        [@ocaml.doc "The name of the monitor."];
      state: MonitorState.t option
        [@ocaml.doc "Lists the status of the state of each monitor."];
      aggregationPeriod: AggregationPeriod.t option
        [@ocaml.doc "The aggregation period for the specified monitor."];
      tags: TagMap.t option
        [@ocaml.doc "The list of key-value pairs assigned to the monitor."];
      probes: ProbeList.t option
        [@ocaml.doc
          "The details about each probe associated with that monitor."];
      createdAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date when the monitor was created."];
      modifiedAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date when the monitor was last modified."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?monitorArn =
      fun ?monitorName ->
        fun ?state ->
          fun ?aggregationPeriod ->
            fun ?tags ->
              fun ?probes ->
                fun ?createdAt ->
                  fun ?modifiedAt ->
                    fun () ->
                      {
                        monitorArn;
                        monitorName;
                        state;
                        aggregationPeriod;
                        tags;
                        probes;
                        createdAt;
                        modifiedAt
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("monitorArn", (Option.map x.monitorArn ~f:MonitorArn.to_value));
        ("monitorName", (Option.map x.monitorName ~f:ResourceName.to_value));
        ("state", (Option.map x.state ~f:MonitorState.to_value));
        ("aggregationPeriod",
          (Option.map x.aggregationPeriod ~f:AggregationPeriod.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("probes", (Option.map x.probes ~f:ProbeList.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Iso8601Timestamp.to_value));
        ("modifiedAt",
          (Option.map x.modifiedAt ~f:Iso8601Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modifiedAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "modifiedAt") in
      let createdAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let probes =
        (Option.map ~f:ProbeList.of_xml) (Xml.child xml_arg0 "probes") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let aggregationPeriod =
        (Option.map ~f:AggregationPeriod.of_xml)
          (Xml.child xml_arg0 "aggregationPeriod") in
      let state =
        (Option.map ~f:MonitorState.of_xml) (Xml.child xml_arg0 "state") in
      let monitorName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "monitorName") in
      let monitorArn =
        (Option.map ~f:MonitorArn.of_xml) (Xml.child xml_arg0 "monitorArn") in
      make ?modifiedAt ?createdAt ?probes ?tags ?aggregationPeriod ?state
        ?monitorName ?monitorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modifiedAt = field_map json__ "modifiedAt" Iso8601Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Iso8601Timestamp.of_json in
      let probes = field_map json__ "probes" ProbeList.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let aggregationPeriod =
        field_map json__ "aggregationPeriod" AggregationPeriod.of_json in
      let state = field_map json__ "state" MonitorState.of_json in
      let monitorName = field_map json__ "monitorName" ResourceName.of_json in
      let monitorArn = field_map json__ "monitorArn" MonitorArn.of_json in
      make ?modifiedAt ?createdAt ?probes ?tags ?aggregationPeriod ?state
        ?monitorName ?monitorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns details about a specific monitor. This action requires the monitorName parameter. Run ListMonitors to get a list of monitor names."]
module GetMonitorInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc "The name of the monitor that details are returned for."]}
    let context_ = "GetMonitorInput"
    let make ~monitorName = fun () -> { monitorName }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns details about a specific monitor. This action requires the monitorName parameter. Run ListMonitors to get a list of monitor names."]
module DeleteProbeOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified probe. Once a probe is deleted you'll no longer incur any billing fees for that probe. This action requires both the monitorName and probeId parameters. Run ListMonitors to get a list of monitor names. Run GetMonitor to get a list of probes and probe IDs. You can only delete a single probe at a time using this action."]
module DeleteProbeInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc "The name of the monitor to delete."];
      probeId: ProbeId.t [@ocaml.doc "The ID of the probe to delete."]}
    let context_ = "DeleteProbeInput"
    let make ~monitorName =
      fun ~probeId -> fun () -> { monitorName; probeId }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)));
        ("probeId", (Some (ProbeId.to_value x.probeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let probeId =
        ProbeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "probeId") in
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ~probeId ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let probeId = field_map_exn json__ "probeId" ProbeId.of_json in
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ~probeId ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified probe. Once a probe is deleted you'll no longer incur any billing fees for that probe. This action requires both the monitorName and probeId parameters. Run ListMonitors to get a list of monitor names. Run GetMonitor to get a list of probes and probe IDs. You can only delete a single probe at a time using this action."]
module DeleteMonitorOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified monitor. This action requires the monitorName parameter. Run ListMonitors to get a list of monitor names."]
module DeleteMonitorInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc "The name of the monitor to delete."]}
    let context_ = "DeleteMonitorInput"
    let make ~monitorName = fun () -> { monitorName }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified monitor. This action requires the monitorName parameter. Run ListMonitors to get a list of monitor names."]
module CreateProbeOutput =
  struct
    type nonrec t =
      {
      probeId: ProbeId.t option
        [@ocaml.doc "The ID of the probe for which details are returned."];
      probeArn: Arn.t option [@ocaml.doc "The ARN of the probe."];
      sourceArn: Arn.t option [@ocaml.doc "The ARN of the probe."];
      destination: Destination.t option
        [@ocaml.doc
          "The destination IP address for the monitor. This must be either an IPv4 or IPv6 address."];
      destinationPort: Port.t option
        [@ocaml.doc
          "The port associated with the destination. This is required only if the protocol is TCP and must be a number between 1 and 65536."];
      protocol: Protocol.t option
        [@ocaml.doc
          "The protocol used for the network traffic between the source and destination. This must be either TCP or ICMP."];
      packetSize: PacketSize.t option
        [@ocaml.doc
          "The size of the packets sent between the source and destination. This must be a number between 56 and 8500."];
      addressFamily: AddressFamily.t option
        [@ocaml.doc "Indicates whether the IP address is IPV4 or IPV6."];
      vpcId: VpcId.t option
        [@ocaml.doc "The ID of the source VPC or subnet."];
      state: ProbeState.t option [@ocaml.doc "The state of the probe."];
      createdAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date that the probe was created."];
      modifiedAt: Iso8601Timestamp.t option
        [@ocaml.doc "The time and date when the probe was last modified."];
      tags: TagMap.t option
        [@ocaml.doc "The list of key-value pairs assigned to the probe."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?probeId =
      fun ?probeArn ->
        fun ?sourceArn ->
          fun ?destination ->
            fun ?destinationPort ->
              fun ?protocol ->
                fun ?packetSize ->
                  fun ?addressFamily ->
                    fun ?vpcId ->
                      fun ?state ->
                        fun ?createdAt ->
                          fun ?modifiedAt ->
                            fun ?tags ->
                              fun () ->
                                {
                                  probeId;
                                  probeArn;
                                  sourceArn;
                                  destination;
                                  destinationPort;
                                  protocol;
                                  packetSize;
                                  addressFamily;
                                  vpcId;
                                  state;
                                  createdAt;
                                  modifiedAt;
                                  tags
                                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("probeId", (Option.map x.probeId ~f:ProbeId.to_value));
        ("probeArn", (Option.map x.probeArn ~f:Arn.to_value));
        ("sourceArn", (Option.map x.sourceArn ~f:Arn.to_value));
        ("destination", (Option.map x.destination ~f:Destination.to_value));
        ("destinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("protocol", (Option.map x.protocol ~f:Protocol.to_value));
        ("packetSize", (Option.map x.packetSize ~f:PacketSize.to_value));
        ("addressFamily",
          (Option.map x.addressFamily ~f:AddressFamily.to_value));
        ("vpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("state", (Option.map x.state ~f:ProbeState.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Iso8601Timestamp.to_value));
        ("modifiedAt",
          (Option.map x.modifiedAt ~f:Iso8601Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let modifiedAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "modifiedAt") in
      let createdAt =
        (Option.map ~f:Iso8601Timestamp.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let state =
        (Option.map ~f:ProbeState.of_xml) (Xml.child xml_arg0 "state") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "vpcId") in
      let addressFamily =
        (Option.map ~f:AddressFamily.of_xml)
          (Xml.child xml_arg0 "addressFamily") in
      let packetSize =
        (Option.map ~f:PacketSize.of_xml) (Xml.child xml_arg0 "packetSize") in
      let protocol =
        (Option.map ~f:Protocol.of_xml) (Xml.child xml_arg0 "protocol") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "destinationPort") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "destination") in
      let sourceArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "sourceArn") in
      let probeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "probeArn") in
      let probeId =
        (Option.map ~f:ProbeId.of_xml) (Xml.child xml_arg0 "probeId") in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let modifiedAt = field_map json__ "modifiedAt" Iso8601Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Iso8601Timestamp.of_json in
      let state = field_map json__ "state" ProbeState.of_json in
      let vpcId = field_map json__ "vpcId" VpcId.of_json in
      let addressFamily =
        field_map json__ "addressFamily" AddressFamily.of_json in
      let packetSize = field_map json__ "packetSize" PacketSize.of_json in
      let protocol = field_map json__ "protocol" Protocol.of_json in
      let destinationPort = field_map json__ "destinationPort" Port.of_json in
      let destination = field_map json__ "destination" Destination.of_json in
      let sourceArn = field_map json__ "sourceArn" Arn.of_json in
      let probeArn = field_map json__ "probeArn" Arn.of_json in
      let probeId = field_map json__ "probeId" ProbeId.of_json in
      make ?tags ?modifiedAt ?createdAt ?state ?vpcId ?addressFamily
        ?packetSize ?protocol ?destinationPort ?destination ?sourceArn
        ?probeArn ?probeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a probe within a monitor. Once you create a probe, and it begins monitoring your network traffic, you'll incur billing charges for that probe. This action requires the monitorName parameter. Run ListMonitors to get a list of monitor names. Note the name of the monitorName you want to create the probe for."]
module CreateProbeInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc "The name of the monitor to associated with the probe."];
      probe: ProbeInput.t
        [@ocaml.doc
          "Describes the details of an individual probe for a monitor."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."];
      tags: TagMap.t option
        [@ocaml.doc
          "The list of key-value pairs created and assigned to the probe."]}
    let context_ = "CreateProbeInput"
    let make ?clientToken =
      fun ?tags ->
        fun ~monitorName ->
          fun ~probe -> fun () -> { clientToken; tags; monitorName; probe }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)));
        ("probe", (Some (ProbeInput.to_value x.probe)));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let probe =
        ProbeInput.of_xml (Xml.child_exn ~context:context_ xml_arg0 "probe") in
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ?tags ?clientToken ~probe ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let clientToken = field_map json__ "clientToken" String_.of_json in
      let probe = field_map_exn json__ "probe" ProbeInput.of_json in
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ?tags ?clientToken ~probe ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a probe within a monitor. Once you create a probe, and it begins monitoring your network traffic, you'll incur billing charges for that probe. This action requires the monitorName parameter. Run ListMonitors to get a list of monitor names. Note the name of the monitorName you want to create the probe for."]
module CreateMonitorOutput =
  struct
    type nonrec t =
      {
      monitorArn: MonitorArn.t option [@ocaml.doc "The ARN of the monitor."];
      monitorName: ResourceName.t option
        [@ocaml.doc "The name of the monitor."];
      state: MonitorState.t option [@ocaml.doc "The state of the monitor."];
      aggregationPeriod: AggregationPeriod.t option
        [@ocaml.doc
          "The number of seconds that metrics are aggregated by and sent to Amazon CloudWatch. This will be either 30 or 60."];
      tags: TagMap.t option
        [@ocaml.doc "The list of key-value pairs assigned to the monitor."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?monitorArn =
      fun ?monitorName ->
        fun ?state ->
          fun ?aggregationPeriod ->
            fun ?tags ->
              fun () ->
                { monitorArn; monitorName; state; aggregationPeriod; tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("monitorArn", (Option.map x.monitorArn ~f:MonitorArn.to_value));
        ("monitorName", (Option.map x.monitorName ~f:ResourceName.to_value));
        ("state", (Option.map x.state ~f:MonitorState.to_value));
        ("aggregationPeriod",
          (Option.map x.aggregationPeriod ~f:AggregationPeriod.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let aggregationPeriod =
        (Option.map ~f:AggregationPeriod.of_xml)
          (Xml.child xml_arg0 "aggregationPeriod") in
      let state =
        (Option.map ~f:MonitorState.of_xml) (Xml.child xml_arg0 "state") in
      let monitorName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "monitorName") in
      let monitorArn =
        (Option.map ~f:MonitorArn.of_xml) (Xml.child xml_arg0 "monitorArn") in
      make ?tags ?aggregationPeriod ?state ?monitorName ?monitorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let aggregationPeriod =
        field_map json__ "aggregationPeriod" AggregationPeriod.of_json in
      let state = field_map json__ "state" MonitorState.of_json in
      let monitorName = field_map json__ "monitorName" ResourceName.of_json in
      let monitorArn = field_map json__ "monitorArn" MonitorArn.of_json in
      make ?tags ?aggregationPeriod ?state ?monitorName ?monitorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a monitor between a source subnet and destination IP address. Within a monitor you'll create one or more probes that monitor network traffic between your source Amazon Web Services VPC subnets and your destination IP addresses. Each probe then aggregates and sends metrics to Amazon CloudWatch. You can also create a monitor with probes using this command. For each probe, you define the following: source\226\128\148The subnet IDs where the probes will be created. destination\226\128\148 The target destination IP address for the probe. destinationPort\226\128\148Required only if the protocol is TCP. protocol\226\128\148The communication protocol between the source and destination. This will be either TCP or ICMP. packetSize\226\128\148The size of the packets. This must be a number between 56 and 8500. (Optional) tags \226\128\148Key-value pairs created and assigned to the probe."]
module CreateMonitorInput =
  struct
    type nonrec t =
      {
      monitorName: ResourceName.t
        [@ocaml.doc
          "The name identifying the monitor. It can contain only letters, underscores (_), or dashes (-), and can be up to 200 characters."];
      probes: CreateMonitorProbeInputList.t option
        [@ocaml.doc
          "Displays a list of all of the probes created for a monitor."];
      aggregationPeriod: AggregationPeriod.t option
        [@ocaml.doc
          "The time, in seconds, that metrics are aggregated and sent to Amazon CloudWatch. Valid values are either 30 or 60. 60 is the default if no period is chosen."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."];
      tags: TagMap.t option
        [@ocaml.doc
          "The list of key-value pairs created and assigned to the monitor."]}
    let context_ = "CreateMonitorInput"
    let make ?probes =
      fun ?aggregationPeriod ->
        fun ?clientToken ->
          fun ?tags ->
            fun ~monitorName ->
              fun () ->
                { probes; aggregationPeriod; clientToken; tags; monitorName }
    let to_value x =
      structure_to_value
        [("monitorName", (Some (ResourceName.to_value x.monitorName)));
        ("probes",
          (Option.map x.probes ~f:CreateMonitorProbeInputList.to_value));
        ("aggregationPeriod",
          (Option.map x.aggregationPeriod ~f:AggregationPeriod.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let aggregationPeriod =
        (Option.map ~f:AggregationPeriod.of_xml)
          (Xml.child xml_arg0 "aggregationPeriod") in
      let probes =
        (Option.map ~f:CreateMonitorProbeInputList.of_xml)
          (Xml.child xml_arg0 "probes") in
      let monitorName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "monitorName") in
      make ?tags ?clientToken ?aggregationPeriod ?probes ~monitorName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let clientToken = field_map json__ "clientToken" String_.of_json in
      let aggregationPeriod =
        field_map json__ "aggregationPeriod" AggregationPeriod.of_json in
      let probes =
        field_map json__ "probes" CreateMonitorProbeInputList.of_json in
      let monitorName =
        field_map_exn json__ "monitorName" ResourceName.of_json in
      make ?tags ?clientToken ?aggregationPeriod ?probes ~monitorName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a monitor between a source subnet and destination IP address. Within a monitor you'll create one or more probes that monitor network traffic between your source Amazon Web Services VPC subnets and your destination IP addresses. Each probe then aggregates and sends metrics to Amazon CloudWatch. You can also create a monitor with probes using this command. For each probe, you define the following: source\226\128\148The subnet IDs where the probes will be created. destination\226\128\148 The target destination IP address for the probe. destinationPort\226\128\148Required only if the protocol is TCP. protocol\226\128\148The communication protocol between the source and destination. This will be either TCP or ICMP. packetSize\226\128\148The size of the packets. This must be a number between 56 and 8500. (Optional) tags \226\128\148Key-value pairs created and assigned to the probe."]
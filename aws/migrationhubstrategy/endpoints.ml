(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetApplicationComponentDetails: (GetApplicationComponentDetailsRequest.t,
  GetApplicationComponentDetailsResponse.t,
  GetApplicationComponentDetailsResponse.error) t 
  | GetApplicationComponentStrategies:
  (GetApplicationComponentStrategiesRequest.t,
  GetApplicationComponentStrategiesResponse.t,
  GetApplicationComponentStrategiesResponse.error) t 
  | GetAssessment: (GetAssessmentRequest.t, GetAssessmentResponse.t,
  GetAssessmentResponse.error) t 
  | GetImportFileTask: (GetImportFileTaskRequest.t,
  GetImportFileTaskResponse.t, GetImportFileTaskResponse.error) t 
  | GetPortfolioPreferences: (GetPortfolioPreferencesRequest.t,
  GetPortfolioPreferencesResponse.t, GetPortfolioPreferencesResponse.error) t
  
  | GetPortfolioSummary: (GetPortfolioSummaryRequest.t,
  GetPortfolioSummaryResponse.t, GetPortfolioSummaryResponse.error) t 
  | GetRecommendationReportDetails: (GetRecommendationReportDetailsRequest.t,
  GetRecommendationReportDetailsResponse.t,
  GetRecommendationReportDetailsResponse.error) t 
  | GetServerDetails: (GetServerDetailsRequest.t, GetServerDetailsResponse.t,
  GetServerDetailsResponse.error) t 
  | GetServerStrategies: (GetServerStrategiesRequest.t,
  GetServerStrategiesResponse.t, GetServerStrategiesResponse.error) t 
  | ListApplicationComponents: (ListApplicationComponentsRequest.t,
  ListApplicationComponentsResponse.t,
  ListApplicationComponentsResponse.error) t 
  | ListCollectors: (ListCollectorsRequest.t, ListCollectorsResponse.t,
  ListCollectorsResponse.error) t 
  | ListImportFileTask: (ListImportFileTaskRequest.t,
  ListImportFileTaskResponse.t, ListImportFileTaskResponse.error) t 
  | ListServers: (ListServersRequest.t, ListServersResponse.t,
  ListServersResponse.error) t 
  | PutPortfolioPreferences: (PutPortfolioPreferencesRequest.t,
  PutPortfolioPreferencesResponse.t, PutPortfolioPreferencesResponse.error) t
  
  | StartAssessment: (StartAssessmentRequest.t, StartAssessmentResponse.t,
  StartAssessmentResponse.error) t 
  | StartImportFileTask: (StartImportFileTaskRequest.t,
  StartImportFileTaskResponse.t, StartImportFileTaskResponse.error) t 
  | StartRecommendationReportGeneration:
  (StartRecommendationReportGenerationRequest.t,
  StartRecommendationReportGenerationResponse.t,
  StartRecommendationReportGenerationResponse.error) t 
  | StopAssessment: (StopAssessmentRequest.t, StopAssessmentResponse.t,
  StopAssessmentResponse.error) t 
  | UpdateApplicationComponentConfig:
  (UpdateApplicationComponentConfigRequest.t,
  UpdateApplicationComponentConfigResponse.t,
  UpdateApplicationComponentConfigResponse.error) t 
  | UpdateServerConfig: (UpdateServerConfigRequest.t,
  UpdateServerConfigResponse.t, UpdateServerConfigResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetApplicationComponentDetails -> `GET
  | GetApplicationComponentStrategies -> `GET
  | GetAssessment -> `GET
  | GetImportFileTask -> `GET
  | GetPortfolioPreferences -> `GET
  | GetPortfolioSummary -> `GET
  | GetRecommendationReportDetails -> `GET
  | GetServerDetails -> `GET
  | GetServerStrategies -> `GET
  | ListApplicationComponents -> `POST
  | ListCollectors -> `GET
  | ListImportFileTask -> `GET
  | ListServers -> `POST
  | PutPortfolioPreferences -> `POST
  | StartAssessment -> `POST
  | StartImportFileTask -> `POST
  | StartRecommendationReportGeneration -> `POST
  | StopAssessment -> `POST
  | UpdateApplicationComponentConfig -> `POST
  | UpdateServerConfig -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetApplicationComponentDetails ->
          (Format.kasprintf Uri.of_string)
            "/get-applicationcomponent-details/%s"
            (ApplicationComponentId.to_header
               x.GetApplicationComponentDetailsRequest.applicationComponentId)
      | GetApplicationComponentStrategies ->
          (Format.kasprintf Uri.of_string)
            "/get-applicationcomponent-strategies/%s"
            (ApplicationComponentId.to_header
               x.GetApplicationComponentStrategiesRequest.applicationComponentId)
      | GetAssessment ->
          (Format.kasprintf Uri.of_string) "/get-assessment/%s"
            (AsyncTaskId.to_header x.GetAssessmentRequest.id)
      | GetImportFileTask ->
          (Format.kasprintf Uri.of_string) "/get-import-file-task/%s"
            (String_.to_header x.GetImportFileTaskRequest.id)
      | GetPortfolioPreferences ->
          (Format.kasprintf Uri.of_string) "/get-portfolio-preferences"
      | GetPortfolioSummary ->
          (Format.kasprintf Uri.of_string) "/get-portfolio-summary"
      | GetRecommendationReportDetails ->
          (Format.kasprintf Uri.of_string)
            "/get-recommendation-report-details/%s"
            (RecommendationTaskId.to_header
               x.GetRecommendationReportDetailsRequest.id)
      | GetServerDetails ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/get-server-details/%s"
               (ServerId.to_header x.GetServerDetailsRequest.serverId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResult.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | GetServerStrategies ->
          (Format.kasprintf Uri.of_string) "/get-server-strategies/%s"
            (ServerId.to_header x.GetServerStrategiesRequest.serverId)
      | ListApplicationComponents ->
          (Format.kasprintf Uri.of_string) "/list-applicationcomponents"
      | ListCollectors ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/list-collectors")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResult.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListImportFileTask ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/list-import-file-task")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (Integer.to_header v)))
                  x.maxResults;
               Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                 x.nextToken])
      | ListServers -> (Format.kasprintf Uri.of_string) "/list-servers"
      | PutPortfolioPreferences ->
          (Format.kasprintf Uri.of_string) "/put-portfolio-preferences"
      | StartAssessment ->
          (Format.kasprintf Uri.of_string) "/start-assessment"
      | StartImportFileTask ->
          (Format.kasprintf Uri.of_string) "/start-import-file-task"
      | StartRecommendationReportGeneration ->
          (Format.kasprintf Uri.of_string)
            "/start-recommendation-report-generation"
      | StopAssessment -> (Format.kasprintf Uri.of_string) "/stop-assessment"
      | UpdateApplicationComponentConfig ->
          (Format.kasprintf Uri.of_string)
            "/update-applicationcomponent-config/"
      | UpdateServerConfig ->
          (Format.kasprintf Uri.of_string) "/update-server-config/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetApplicationComponentDetails ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetApplicationComponentStrategies ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAssessment ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetImportFileTask ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPortfolioPreferences ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPortfolioSummary ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetRecommendationReportDetails ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetServerDetails ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetServerStrategies ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListApplicationComponents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListApplicationComponentsRequest.applicationComponentCriteria
                         ~f:(fun x ->
                               ("applicationComponentCriteria",
                                 (ApplicationComponentCriteria.to_value x)));
                      Option.map
                        req.ListApplicationComponentsRequest.filterValue
                        ~f:(fun x ->
                              ("filterValue",
                                (ListApplicationComponentsRequestFilterValueString.to_value
                                   x)));
                      Option.map
                        req.ListApplicationComponentsRequest.groupIdFilter
                        ~f:(fun x -> ("groupIdFilter", (GroupIds.to_value x)));
                      Option.map
                        req.ListApplicationComponentsRequest.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResult.to_value x)));
                      Option.map
                        req.ListApplicationComponentsRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListApplicationComponentsRequest.sort
                        ~f:(fun x -> ("sort", (SortOrder.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListCollectors ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListImportFileTask ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListServers ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListServersRequest.filterValue
                         ~f:(fun x -> ("filterValue", (String_.to_value x)));
                      Option.map req.ListServersRequest.groupIdFilter
                        ~f:(fun x -> ("groupIdFilter", (GroupIds.to_value x)));
                      Option.map req.ListServersRequest.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResult.to_value x)));
                      Option.map req.ListServersRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListServersRequest.serverCriteria
                        ~f:(fun x ->
                              ("serverCriteria", (ServerCriteria.to_value x)));
                      Option.map req.ListServersRequest.sort
                        ~f:(fun x -> ("sort", (SortOrder.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutPortfolioPreferences ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.PutPortfolioPreferencesRequest.applicationPreferences
                         ~f:(fun x ->
                               ("applicationPreferences",
                                 (ApplicationPreferences.to_value x)));
                      Option.map
                        req.PutPortfolioPreferencesRequest.databasePreferences
                        ~f:(fun x ->
                              ("databasePreferences",
                                (DatabasePreferences.to_value x)));
                      Option.map
                        req.PutPortfolioPreferencesRequest.prioritizeBusinessGoals
                        ~f:(fun x ->
                              ("prioritizeBusinessGoals",
                                (PrioritizeBusinessGoals.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartAssessment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.StartAssessmentRequest.s3bucketForAnalysisData
                         ~f:(fun x ->
                               ("s3bucketForAnalysisData",
                                 (StartAssessmentRequestS3bucketForAnalysisDataString.to_value
                                    x)));
                      Option.map
                        req.StartAssessmentRequest.s3bucketForReportData
                        ~f:(fun x ->
                              ("s3bucketForReportData",
                                (StartAssessmentRequestS3bucketForReportDataString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartImportFileTask ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("S3Bucket",
                           (ImportS3Bucket.to_value
                              req.StartImportFileTaskRequest.s3Bucket));
                      Option.map
                        req.StartImportFileTaskRequest.dataSourceType
                        ~f:(fun x ->
                              ("dataSourceType", (DataSourceType.to_value x)));
                      Option.map req.StartImportFileTaskRequest.groupId
                        ~f:(fun x -> ("groupId", (GroupIds.to_value x)));
                      Some
                        ("name",
                          (StartImportFileTaskRequestNameString.to_value
                             req.StartImportFileTaskRequest.name));
                      Option.map
                        req.StartImportFileTaskRequest.s3bucketForReportData
                        ~f:(fun x ->
                              ("s3bucketForReportData",
                                (StartImportFileTaskRequestS3bucketForReportDataString.to_value
                                   x)));
                      Some
                        ("s3key",
                          (String_.to_value
                             req.StartImportFileTaskRequest.s3key))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartRecommendationReportGeneration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.StartRecommendationReportGenerationRequest.groupIdFilter
                         ~f:(fun x ->
                               ("groupIdFilter", (GroupIds.to_value x)));
                      Option.map
                        req.StartRecommendationReportGenerationRequest.outputFormat
                        ~f:(fun x ->
                              ("outputFormat", (OutputFormat.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopAssessment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("assessmentId",
                           (AsyncTaskId.to_value
                              req.StopAssessmentRequest.assessmentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateApplicationComponentConfig ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("applicationComponentId",
                           (ApplicationComponentId.to_value
                              req.UpdateApplicationComponentConfigRequest.applicationComponentId));
                      Option.map
                        req.UpdateApplicationComponentConfigRequest.inclusionStatus
                        ~f:(fun x ->
                              ("inclusionStatus",
                                (InclusionStatus.to_value x)));
                      Option.map
                        req.UpdateApplicationComponentConfigRequest.secretsManagerKey
                        ~f:(fun x ->
                              ("secretsManagerKey",
                                (SecretsManagerKey.to_value x)));
                      Option.map
                        req.UpdateApplicationComponentConfigRequest.sourceCodeList
                        ~f:(fun x ->
                              ("sourceCodeList", (SourceCodeList.to_value x)));
                      Option.map
                        req.UpdateApplicationComponentConfigRequest.strategyOption
                        ~f:(fun x ->
                              ("strategyOption", (StrategyOption.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateServerConfig ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("serverId",
                           (ServerId.to_value
                              req.UpdateServerConfigRequest.serverId));
                      Option.map req.UpdateServerConfigRequest.strategyOption
                        ~f:(fun x ->
                              ("strategyOption", (StrategyOption.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | GetApplicationComponentDetails ->
      if is_success
      then
        Ok
          (GetApplicationComponentDetailsResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetApplicationComponentDetailsResponse.error_of_json))
  | GetApplicationComponentStrategies ->
      if is_success
      then
        Ok
          (GetApplicationComponentStrategiesResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetApplicationComponentStrategiesResponse.error_of_json))
  | GetAssessment ->
      if is_success
      then Ok (GetAssessmentResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetAssessmentResponse.error_of_json))
  | GetImportFileTask ->
      if is_success
      then Ok (GetImportFileTaskResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetImportFileTaskResponse.error_of_json))
  | GetPortfolioPreferences ->
      if is_success
      then
        Ok (GetPortfolioPreferencesResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetPortfolioPreferencesResponse.error_of_json))
  | GetPortfolioSummary ->
      if is_success
      then Ok (GetPortfolioSummaryResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetPortfolioSummaryResponse.error_of_json))
  | GetRecommendationReportDetails ->
      if is_success
      then
        Ok
          (GetRecommendationReportDetailsResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetRecommendationReportDetailsResponse.error_of_json))
  | GetServerDetails ->
      if is_success
      then Ok (GetServerDetailsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetServerDetailsResponse.error_of_json))
  | GetServerStrategies ->
      if is_success
      then Ok (GetServerStrategiesResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetServerStrategiesResponse.error_of_json))
  | ListApplicationComponents ->
      if is_success
      then
        Ok
          (ListApplicationComponentsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListApplicationComponentsResponse.error_of_json))
  | ListCollectors ->
      if is_success
      then Ok (ListCollectorsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListCollectorsResponse.error_of_json))
  | ListImportFileTask ->
      if is_success
      then Ok (ListImportFileTaskResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListImportFileTaskResponse.error_of_json))
  | ListServers ->
      if is_success
      then Ok (ListServersResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListServersResponse.error_of_json))
  | PutPortfolioPreferences ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutPortfolioPreferencesResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error
             (Some PutPortfolioPreferencesResponse.error_of_json))
  | StartAssessment ->
      if is_success
      then Ok (StartAssessmentResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some StartAssessmentResponse.error_of_json))
  | StartImportFileTask ->
      if is_success
      then Ok (StartImportFileTaskResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartImportFileTaskResponse.error_of_json))
  | StartRecommendationReportGeneration ->
      if is_success
      then
        Ok
          (StartRecommendationReportGenerationResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StartRecommendationReportGenerationResponse.error_of_json))
  | StopAssessment ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StopAssessmentResponse.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some StopAssessmentResponse.error_of_json))
  | UpdateApplicationComponentConfig ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (UpdateApplicationComponentConfigResponse.of_header_and_body
             (headers, ()))
      else
        Error
          (parse_aws_error
             (Some UpdateApplicationComponentConfigResponse.error_of_json))
  | UpdateServerConfig ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UpdateServerConfigResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some UpdateServerConfigResponse.error_of_json))
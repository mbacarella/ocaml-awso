(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_group_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and memberRealm =
         flag "member-realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and memberName =
         flag "member-name" (required string) ~doc:"STRING MemberName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_group_member
           (Values.AddGroupMemberRequest.make ?clientToken ?memberRealm
              ~directoryId ~groupName ~memberName ())
           (Some Values.AddGroupMemberResult.to_json)
           (Some Values.AddGroupMemberResult.error_to_json)])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and groupScope =
         flag "group-scope" (optional json_arg) ~doc:"JSON GroupScope"
       and groupType =
         flag "group-type" (optional json_arg) ~doc:"JSON GroupType"
       and otherAttributes =
         flag "other-attributes" (optional json_arg) ~doc:"JSON Attributes"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?clientToken
              ?groupScope:(Option.map ~f:Values.GroupScope.of_json groupScope)
              ?groupType:(Option.map ~f:Values.GroupType.of_json groupType)
              ?otherAttributes:(Option.map ~f:Values.Attributes.of_json
                                  otherAttributes) ~directoryId
              ~sAMAccountName ()) (Some Values.CreateGroupResult.to_json)
           (Some Values.CreateGroupResult.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and emailAddress =
         flag "email-address" (optional string) ~doc:"STRING EmailAddress"
       and givenName =
         flag "given-name" (optional string) ~doc:"STRING GivenName"
       and otherAttributes =
         flag "other-attributes" (optional json_arg) ~doc:"JSON Attributes"
       and surname = flag "surname" (optional string) ~doc:"STRING Surname"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?clientToken ?emailAddress
              ?givenName
              ?otherAttributes:(Option.map ~f:Values.Attributes.of_json
                                  otherAttributes) ?surname ~directoryId
              ~sAMAccountName ()) (Some Values.CreateUserResult.to_json)
           (Some Values.CreateUserResult.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupRequest.make ?clientToken ~directoryId
              ~sAMAccountName ()) (Some Values.DeleteGroupResult.to_json)
           (Some Values.DeleteGroupResult.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ?clientToken ~directoryId
              ~sAMAccountName ()) (Some Values.DeleteUserResult.to_json)
           (Some Values.DeleteUserResult.error_to_json)])
let describe_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and otherAttributes =
         flag "other-attributes" (optional json_arg)
           ~doc:"JSON LdapDisplayNameList"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_group
           (Values.DescribeGroupRequest.make
              ?otherAttributes:(Option.map
                                  ~f:Values.LdapDisplayNameList.of_json
                                  otherAttributes) ?realm ~directoryId
              ~sAMAccountName ()) (Some Values.DescribeGroupResult.to_json)
           (Some Values.DescribeGroupResult.error_to_json)])
let describe_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and otherAttributes =
         flag "other-attributes" (optional json_arg)
           ~doc:"JSON LdapDisplayNameList"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user
           (Values.DescribeUserRequest.make
              ?otherAttributes:(Option.map
                                  ~f:Values.LdapDisplayNameList.of_json
                                  otherAttributes) ?realm ~directoryId
              ~sAMAccountName ()) (Some Values.DescribeUserResult.to_json)
           (Some Values.DescribeUserResult.error_to_json)])
let disable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_user
           (Values.DisableUserRequest.make ?clientToken ~directoryId
              ~sAMAccountName ()) (Some Values.DisableUserResult.to_json)
           (Some Values.DisableUserResult.error_to_json)])
let list_group_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and memberRealm =
         flag "member-realm" (optional string) ~doc:"STRING Realm"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_members
           (Values.ListGroupMembersRequest.make ?maxResults ?memberRealm
              ?nextToken ?realm ~directoryId ~sAMAccountName ())
           (Some Values.ListGroupMembersResult.to_json)
           (Some Values.ListGroupMembersResult.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?maxResults ?nextToken ?realm
              ~directoryId ()) (Some Values.ListGroupsResult.to_json)
           (Some Values.ListGroupsResult.error_to_json)])
let list_groups_for_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and memberRealm =
         flag "member-realm" (optional string) ~doc:"STRING Realm"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING MemberName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups_for_member
           (Values.ListGroupsForMemberRequest.make ?maxResults ?memberRealm
              ?nextToken ?realm ~directoryId ~sAMAccountName ())
           (Some Values.ListGroupsForMemberResult.to_json)
           (Some Values.ListGroupsForMemberResult.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?maxResults ?nextToken ?realm
              ~directoryId ()) (Some Values.ListUsersResult.to_json)
           (Some Values.ListUsersResult.error_to_json)])
let remove_group_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and memberRealm =
         flag "member-realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and memberName =
         flag "member-name" (required string) ~doc:"STRING MemberName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_group_member
           (Values.RemoveGroupMemberRequest.make ?clientToken ?memberRealm
              ~directoryId ~groupName ~memberName ())
           (Some Values.RemoveGroupMemberResult.to_json)
           (Some Values.RemoveGroupMemberResult.error_to_json)])
let search_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and searchAttributes =
         flag "search-attributes" (required json_arg)
           ~doc:"JSON LdapDisplayNameList"
       and searchString =
         flag "search-string" (required string) ~doc:"STRING SearchString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_groups
           (Values.SearchGroupsRequest.make ?maxResults ?nextToken ?realm
              ~directoryId
              ~searchAttributes:(Values.LdapDisplayNameList.of_json
                                   searchAttributes) ~searchString ())
           (Some Values.SearchGroupsResult.to_json)
           (Some Values.SearchGroupsResult.error_to_json)])
let search_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and realm = flag "realm" (optional string) ~doc:"STRING Realm"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and searchAttributes =
         flag "search-attributes" (required json_arg)
           ~doc:"JSON LdapDisplayNameList"
       and searchString =
         flag "search-string" (required string) ~doc:"STRING SearchString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_users
           (Values.SearchUsersRequest.make ?maxResults ?nextToken ?realm
              ~directoryId
              ~searchAttributes:(Values.LdapDisplayNameList.of_json
                                   searchAttributes) ~searchString ())
           (Some Values.SearchUsersResult.to_json)
           (Some Values.SearchUsersResult.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and groupScope =
         flag "group-scope" (optional json_arg) ~doc:"JSON GroupScope"
       and groupType =
         flag "group-type" (optional json_arg) ~doc:"JSON GroupType"
       and otherAttributes =
         flag "other-attributes" (optional json_arg) ~doc:"JSON Attributes"
       and updateType =
         flag "update-type" (optional json_arg) ~doc:"JSON UpdateType"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupRequest.make ?clientToken
              ?groupScope:(Option.map ~f:Values.GroupScope.of_json groupScope)
              ?groupType:(Option.map ~f:Values.GroupType.of_json groupType)
              ?otherAttributes:(Option.map ~f:Values.Attributes.of_json
                                  otherAttributes)
              ?updateType:(Option.map ~f:Values.UpdateType.of_json updateType)
              ~directoryId ~sAMAccountName ())
           (Some Values.UpdateGroupResult.to_json)
           (Some Values.UpdateGroupResult.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and emailAddress =
         flag "email-address" (optional string) ~doc:"STRING EmailAddress"
       and givenName =
         flag "given-name" (optional string) ~doc:"STRING GivenName"
       and otherAttributes =
         flag "other-attributes" (optional json_arg) ~doc:"JSON Attributes"
       and surname = flag "surname" (optional string) ~doc:"STRING Surname"
       and updateType =
         flag "update-type" (optional json_arg) ~doc:"JSON UpdateType"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and sAMAccountName =
         flag "s-a-m-account-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make ?clientToken ?emailAddress
              ?givenName
              ?otherAttributes:(Option.map ~f:Values.Attributes.of_json
                                  otherAttributes) ?surname
              ?updateType:(Option.map ~f:Values.UpdateType.of_json updateType)
              ~directoryId ~sAMAccountName ())
           (Some Values.UpdateUserResult.to_json)
           (Some Values.UpdateUserResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-group-member", add_group_member);
    ("create-group", create_group);
    ("create-user", create_user);
    ("delete-group", delete_group);
    ("delete-user", delete_user);
    ("describe-group", describe_group);
    ("describe-user", describe_user);
    ("disable-user", disable_user);
    ("list-group-members", list_group_members);
    ("list-groups", list_groups);
    ("list-groups-for-member", list_groups_for_member);
    ("list-users", list_users);
    ("remove-group-member", remove_group_member);
    ("search-groups", search_groups);
    ("search-users", search_users);
    ("update-group", update_group);
    ("update-user", update_user)]
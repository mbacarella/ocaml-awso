(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AddGroupMember: (AddGroupMemberRequest.t, AddGroupMemberResult.t,
  AddGroupMemberResult.error) t 
  | CreateGroup: (CreateGroupRequest.t, CreateGroupResult.t,
  CreateGroupResult.error) t 
  | CreateUser: (CreateUserRequest.t, CreateUserResult.t,
  CreateUserResult.error) t 
  | DeleteGroup: (DeleteGroupRequest.t, DeleteGroupResult.t,
  DeleteGroupResult.error) t 
  | DeleteUser: (DeleteUserRequest.t, DeleteUserResult.t,
  DeleteUserResult.error) t 
  | DescribeGroup: (DescribeGroupRequest.t, DescribeGroupResult.t,
  DescribeGroupResult.error) t 
  | DescribeUser: (DescribeUserRequest.t, DescribeUserResult.t,
  DescribeUserResult.error) t 
  | DisableUser: (DisableUserRequest.t, DisableUserResult.t,
  DisableUserResult.error) t 
  | ListGroupMembers: (ListGroupMembersRequest.t, ListGroupMembersResult.t,
  ListGroupMembersResult.error) t 
  | ListGroups: (ListGroupsRequest.t, ListGroupsResult.t,
  ListGroupsResult.error) t 
  | ListGroupsForMember: (ListGroupsForMemberRequest.t,
  ListGroupsForMemberResult.t, ListGroupsForMemberResult.error) t 
  | ListUsers: (ListUsersRequest.t, ListUsersResult.t, ListUsersResult.error)
  t 
  | RemoveGroupMember: (RemoveGroupMemberRequest.t,
  RemoveGroupMemberResult.t, RemoveGroupMemberResult.error) t 
  | SearchGroups: (SearchGroupsRequest.t, SearchGroupsResult.t,
  SearchGroupsResult.error) t 
  | SearchUsers: (SearchUsersRequest.t, SearchUsersResult.t,
  SearchUsersResult.error) t 
  | UpdateGroup: (UpdateGroupRequest.t, UpdateGroupResult.t,
  UpdateGroupResult.error) t 
  | UpdateUser: (UpdateUserRequest.t, UpdateUserResult.t,
  UpdateUserResult.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AddGroupMember -> `POST
  | CreateGroup -> `POST
  | CreateUser -> `POST
  | DeleteGroup -> `POST
  | DeleteUser -> `POST
  | DescribeGroup -> `POST
  | DescribeUser -> `POST
  | DisableUser -> `POST
  | ListGroupMembers -> `POST
  | ListGroups -> `POST
  | ListGroupsForMember -> `POST
  | ListUsers -> `POST
  | RemoveGroupMember -> `POST
  | SearchGroups -> `POST
  | SearchUsers -> `POST
  | UpdateGroup -> `POST
  | UpdateUser -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AddGroupMember ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/GroupMemberships/AddGroupMember")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | CreateGroup ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Groups/CreateGroup")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | CreateUser ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/CreateUser")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | DeleteGroup ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Groups/DeleteGroup")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | DeleteUser ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/DeleteUser")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | DescribeGroup ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Groups/DescribeGroup")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | DescribeUser ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/DescribeUser")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | DisableUser ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/DisableUser")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | ListGroupMembers ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/GroupMemberships/ListGroupMembers")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | ListGroups ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Groups/ListGroups")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | ListGroupsForMember ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/GroupMemberships/ListGroupsForMember")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | ListUsers ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/ListUsers")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | RemoveGroupMember ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/GroupMemberships/RemoveGroupMember")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | SearchGroups ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Groups/SearchGroups")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | SearchUsers ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/SearchUsers")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | UpdateGroup ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Groups/UpdateGroup")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))])
      | UpdateUser ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/Users/UpdateUser")
            (List.filter_opt
               [Some ("DirectoryId", (DirectoryId.to_header x.directoryId))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | AddGroupMember ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.AddGroupMemberRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Some
                        ("GroupName",
                          (GroupName.to_value
                             req.AddGroupMemberRequest.groupName));
                      Some
                        ("MemberName",
                          (MemberName.to_value
                             req.AddGroupMemberRequest.memberName));
                      Option.map req.AddGroupMemberRequest.memberRealm
                        ~f:(fun x -> ("MemberRealm", (Realm.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateGroup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateGroupRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.CreateGroupRequest.groupScope
                        ~f:(fun x -> ("GroupScope", (GroupScope.to_value x)));
                      Option.map req.CreateGroupRequest.groupType
                        ~f:(fun x -> ("GroupType", (GroupType.to_value x)));
                      Option.map req.CreateGroupRequest.otherAttributes
                        ~f:(fun x ->
                              ("OtherAttributes", (Attributes.to_value x)));
                      Some
                        ("SAMAccountName",
                          (GroupName.to_value
                             req.CreateGroupRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateUser ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateUserRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.CreateUserRequest.emailAddress
                        ~f:(fun x ->
                              ("EmailAddress", (EmailAddress.to_value x)));
                      Option.map req.CreateUserRequest.givenName
                        ~f:(fun x -> ("GivenName", (GivenName.to_value x)));
                      Option.map req.CreateUserRequest.otherAttributes
                        ~f:(fun x ->
                              ("OtherAttributes", (Attributes.to_value x)));
                      Some
                        ("SAMAccountName",
                          (UserName.to_value
                             req.CreateUserRequest.sAMAccountName));
                      Option.map req.CreateUserRequest.surname
                        ~f:(fun x -> ("Surname", (Surname.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteGroup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DeleteGroupRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Some
                        ("SAMAccountName",
                          (GroupName.to_value
                             req.DeleteGroupRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteUser ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DeleteUserRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Some
                        ("SAMAccountName",
                          (UserName.to_value
                             req.DeleteUserRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeGroup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DescribeGroupRequest.otherAttributes
                         ~f:(fun x ->
                               ("OtherAttributes",
                                 (LdapDisplayNameList.to_value x)));
                      Option.map req.DescribeGroupRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)));
                      Some
                        ("SAMAccountName",
                          (GroupName.to_value
                             req.DescribeGroupRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeUser ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DescribeUserRequest.otherAttributes
                         ~f:(fun x ->
                               ("OtherAttributes",
                                 (LdapDisplayNameList.to_value x)));
                      Option.map req.DescribeUserRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)));
                      Some
                        ("SAMAccountName",
                          (UserName.to_value
                             req.DescribeUserRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DisableUser ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DisableUserRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Some
                        ("SAMAccountName",
                          (UserName.to_value
                             req.DisableUserRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListGroupMembers ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListGroupMembersRequest.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListGroupMembersRequest.memberRealm
                        ~f:(fun x -> ("MemberRealm", (Realm.to_value x)));
                      Option.map req.ListGroupMembersRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListGroupMembersRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)));
                      Some
                        ("SAMAccountName",
                          (GroupName.to_value
                             req.ListGroupMembersRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListGroups ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListGroupsRequest.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListGroupsRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListGroupsRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListGroupsForMember ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListGroupsForMemberRequest.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListGroupsForMemberRequest.memberRealm
                        ~f:(fun x -> ("MemberRealm", (Realm.to_value x)));
                      Option.map req.ListGroupsForMemberRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListGroupsForMemberRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)));
                      Some
                        ("SAMAccountName",
                          (MemberName.to_value
                             req.ListGroupsForMemberRequest.sAMAccountName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListUsers ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListUsersRequest.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListUsersRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListUsersRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RemoveGroupMember ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.RemoveGroupMemberRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Some
                        ("GroupName",
                          (GroupName.to_value
                             req.RemoveGroupMemberRequest.groupName));
                      Some
                        ("MemberName",
                          (MemberName.to_value
                             req.RemoveGroupMemberRequest.memberName));
                      Option.map req.RemoveGroupMemberRequest.memberRealm
                        ~f:(fun x -> ("MemberRealm", (Realm.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SearchGroups ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SearchGroupsRequest.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.SearchGroupsRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.SearchGroupsRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)));
                      Some
                        ("SearchAttributes",
                          (LdapDisplayNameList.to_value
                             req.SearchGroupsRequest.searchAttributes));
                      Some
                        ("SearchString",
                          (SearchString.to_value
                             req.SearchGroupsRequest.searchString))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SearchUsers ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SearchUsersRequest.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.SearchUsersRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.SearchUsersRequest.realm
                        ~f:(fun x -> ("Realm", (Realm.to_value x)));
                      Some
                        ("SearchAttributes",
                          (LdapDisplayNameList.to_value
                             req.SearchUsersRequest.searchAttributes));
                      Some
                        ("SearchString",
                          (SearchString.to_value
                             req.SearchUsersRequest.searchString))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateGroup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.UpdateGroupRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.UpdateGroupRequest.groupScope
                        ~f:(fun x -> ("GroupScope", (GroupScope.to_value x)));
                      Option.map req.UpdateGroupRequest.groupType
                        ~f:(fun x -> ("GroupType", (GroupType.to_value x)));
                      Option.map req.UpdateGroupRequest.otherAttributes
                        ~f:(fun x ->
                              ("OtherAttributes", (Attributes.to_value x)));
                      Some
                        ("SAMAccountName",
                          (GroupName.to_value
                             req.UpdateGroupRequest.sAMAccountName));
                      Option.map req.UpdateGroupRequest.updateType
                        ~f:(fun x -> ("UpdateType", (UpdateType.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateUser ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.UpdateUserRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.UpdateUserRequest.emailAddress
                        ~f:(fun x ->
                              ("EmailAddress", (EmailAddress.to_value x)));
                      Option.map req.UpdateUserRequest.givenName
                        ~f:(fun x -> ("GivenName", (GivenName.to_value x)));
                      Option.map req.UpdateUserRequest.otherAttributes
                        ~f:(fun x ->
                              ("OtherAttributes", (Attributes.to_value x)));
                      Some
                        ("SAMAccountName",
                          (UserName.to_value
                             req.UpdateUserRequest.sAMAccountName));
                      Option.map req.UpdateUserRequest.surname
                        ~f:(fun x -> ("Surname", (Surname.to_value x)));
                      Option.map req.UpdateUserRequest.updateType
                        ~f:(fun x -> ("UpdateType", (UpdateType.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | AddGroupMember ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (AddGroupMemberResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some AddGroupMemberResult.error_of_json))
  | CreateGroup ->
      if is_success
      then Ok (CreateGroupResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateGroupResult.error_of_json))
  | CreateUser ->
      if is_success
      then Ok (CreateUserResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateUserResult.error_of_json))
  | DeleteGroup ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteGroupResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteGroupResult.error_of_json))
  | DeleteUser ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteUserResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteUserResult.error_of_json))
  | DescribeGroup ->
      if is_success
      then Ok (DescribeGroupResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeGroupResult.error_of_json))
  | DescribeUser ->
      if is_success
      then Ok (DescribeUserResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeUserResult.error_of_json))
  | DisableUser ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DisableUserResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DisableUserResult.error_of_json))
  | ListGroupMembers ->
      if is_success
      then Ok (ListGroupMembersResult.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListGroupMembersResult.error_of_json))
  | ListGroups ->
      if is_success
      then Ok (ListGroupsResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListGroupsResult.error_of_json))
  | ListGroupsForMember ->
      if is_success
      then Ok (ListGroupsForMemberResult.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListGroupsForMemberResult.error_of_json))
  | ListUsers ->
      if is_success
      then Ok (ListUsersResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListUsersResult.error_of_json))
  | RemoveGroupMember ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (RemoveGroupMemberResult.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some RemoveGroupMemberResult.error_of_json))
  | SearchGroups ->
      if is_success
      then Ok (SearchGroupsResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchGroupsResult.error_of_json))
  | SearchUsers ->
      if is_success
      then Ok (SearchUsersResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchUsersResult.error_of_json))
  | UpdateGroup ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UpdateGroupResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UpdateGroupResult.error_of_json))
  | UpdateUser ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UpdateUserResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UpdateUserResult.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.ds_data
let apiVersion = "2023-05-31"
let endpointPrefix = "ds-data"
let serviceFullName = "AWS Directory Service Data"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module StringAttributeValue =
  struct
    type nonrec t = string
    let context_ = "StringAttributeValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringAttributeValue" j
    let to_json = simple_to_json to_value
  end
module BooleanAttributeValue =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NumberAttributeValue =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module StringSetAttributeValue =
  struct
    type nonrec t = StringAttributeValue.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:StringAttributeValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StringAttributeValue.of_xml)
    let of_json j =
      list_of_json ~kind:"StringSetAttributeValue"
        ~of_json:StringAttributeValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AttributeValue =
  struct
    type nonrec t =
      {
      bOOL: BooleanAttributeValue.t option
        [@ocaml.doc
          "Indicates that the attribute type value is a boolean. For example: \"BOOL\": true"];
      n: NumberAttributeValue.t option
        [@ocaml.doc
          "Indicates that the attribute type value is a number. For example: \"N\": \"16\""];
      s: StringAttributeValue.t option
        [@ocaml.doc
          "Indicates that the attribute type value is a string. For example: \"S\": \"S Group\""];
      sS: StringSetAttributeValue.t option
        [@ocaml.doc
          "Indicates that the attribute type value is a string set. For example: \"SS\": \\[\"sample_service_class/host.sample.com:1234/sample_service_name_1\", \"sample_service_class/host.sample.com:1234/sample_service_name_2\"\\]"]}
    let make ?bOOL =
      fun ?n -> fun ?s -> fun ?sS -> fun () -> { bOOL; n; s; sS }
    let to_value x =
      structure_to_value
        [("BOOL", (Option.map x.bOOL ~f:BooleanAttributeValue.to_value));
        ("N", (Option.map x.n ~f:NumberAttributeValue.to_value));
        ("S", (Option.map x.s ~f:StringAttributeValue.to_value));
        ("SS", (Option.map x.sS ~f:StringSetAttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sS =
        (Option.map ~f:StringSetAttributeValue.of_xml)
          (Xml.child xml_arg0 "SS") in
      let s =
        (Option.map ~f:StringAttributeValue.of_xml) (Xml.child xml_arg0 "S") in
      let n =
        (Option.map ~f:NumberAttributeValue.of_xml) (Xml.child xml_arg0 "N") in
      let bOOL =
        (Option.map ~f:BooleanAttributeValue.of_xml)
          (Xml.child xml_arg0 "BOOL") in
      make ?sS ?s ?n ?bOOL ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sS = field_map json__ "SS" StringSetAttributeValue.of_json in
      let s = field_map json__ "S" StringAttributeValue.of_json in
      let n = field_map json__ "N" NumberAttributeValue.of_json in
      let bOOL = field_map json__ "BOOL" BooleanAttributeValue.of_json in
      make ?sS ?s ?n ?bOOL ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The data type for an attribute. Each attribute value is described as a name-value pair. The name is the AD schema name, and the value is the data itself. For a list of supported attributes, see Directory Service Data Attributes."]
module LdapDisplayName =
  struct
    type nonrec t = string
    let context_ = "LdapDisplayName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[A-Za-z*][A-Za-z-*]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LdapDisplayName" j
    let to_json = simple_to_json to_value
  end
module Attributes =
  struct
    type nonrec t = (LdapDisplayName.t * AttributeValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types LdapDisplayName AttributeValue"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (LdapDisplayName.to_value x) |>
                    (fun x ->
                       (AttributeValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:LdapDisplayName.of_string
        ~of_json:AttributeValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module DistinguishedName =
  struct
    type nonrec t = string
    let context_ = "DistinguishedName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DistinguishedName" j
    let to_json = simple_to_json to_value
  end
module EmailAddress =
  struct
    type nonrec t = string
    let context_ = "EmailAddress"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EmailAddress" j
    let to_json = simple_to_json to_value
  end
module GivenName =
  struct
    type nonrec t = string
    let context_ = "GivenName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GivenName" j
    let to_json = simple_to_json to_value
  end
module SID =
  struct
    type nonrec t = string
    let context_ = "SID"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SID" j
    let to_json = simple_to_json to_value
  end
module Surname =
  struct
    type nonrec t = string
    let context_ = "Surname"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Surname" j
    let to_json = simple_to_json to_value
  end
module UserName =
  struct
    type nonrec t = string
    let context_ = "UserName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:20) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w\\-.]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UserName" j
    let to_json = simple_to_json to_value
  end
module UserPrincipalName =
  struct
    type nonrec t = string
    let context_ = "UserPrincipalName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UserPrincipalName" j
    let to_json = simple_to_json to_value
  end
module GroupName =
  struct
    type nonrec t = string
    let context_ = "GroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[^:;|=+\"*?<>/\\\\,\\[\\]@]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupName" j
    let to_json = simple_to_json to_value
  end
module GroupScope =
  struct
    type nonrec t =
      | DomainLocal 
      | Global 
      | Universal 
      | BuiltinLocal 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DomainLocal -> "DomainLocal"
      | Global -> "Global"
      | Universal -> "Universal"
      | BuiltinLocal -> "BuiltinLocal"
      | Non_static_id s -> s
    let of_string =
      function
      | "DomainLocal" -> DomainLocal
      | "Global" -> Global
      | "Universal" -> Universal
      | "BuiltinLocal" -> BuiltinLocal
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GroupScope" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GroupScope" j)
    let to_json = simple_to_json to_value
  end
module GroupType =
  struct
    type nonrec t =
      | Distribution 
      | Security 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Distribution -> "Distribution"
      | Security -> "Security"
      | Non_static_id s -> s
    let of_string =
      function
      | "Distribution" -> Distribution
      | "Security" -> Security
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GroupType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GroupType" j)
    let to_json = simple_to_json to_value
  end
module MemberName =
  struct
    type nonrec t = string
    let context_ = "MemberName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[^:;|=+\"*?<>/\\\\,\\[\\]@]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MemberName" j
    let to_json = simple_to_json to_value
  end
module MemberType =
  struct
    type nonrec t =
      | USER 
      | GROUP 
      | COMPUTER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | USER -> "USER"
      | GROUP -> "GROUP"
      | COMPUTER -> "COMPUTER"
      | Non_static_id s -> s
    let of_string =
      function
      | "USER" -> USER
      | "GROUP" -> GROUP
      | "COMPUTER" -> COMPUTER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MemberType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MemberType" j)
    let to_json = simple_to_json to_value
  end
module AccessDeniedReason =
  struct
    type nonrec t =
      | IAM_AUTH 
      | DIRECTORY_AUTH 
      | DATA_DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IAM_AUTH -> "IAM_AUTH"
      | DIRECTORY_AUTH -> "DIRECTORY_AUTH"
      | DATA_DISABLED -> "DATA_DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "IAM_AUTH" -> IAM_AUTH
      | "DIRECTORY_AUTH" -> DIRECTORY_AUTH
      | "DATA_DISABLED" -> DATA_DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AccessDeniedReason" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccessDeniedReason" j)
    let to_json = simple_to_json to_value
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module DirectoryUnavailableReason =
  struct
    type nonrec t =
      | INVALID_DIRECTORY_STATE 
      | DIRECTORY_TIMEOUT 
      | DIRECTORY_RESOURCES_EXCEEDED 
      | NO_DISK_SPACE 
      | TRUST_AUTH_FAILURE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INVALID_DIRECTORY_STATE -> "INVALID_DIRECTORY_STATE"
      | DIRECTORY_TIMEOUT -> "DIRECTORY_TIMEOUT"
      | DIRECTORY_RESOURCES_EXCEEDED -> "DIRECTORY_RESOURCES_EXCEEDED"
      | NO_DISK_SPACE -> "NO_DISK_SPACE"
      | TRUST_AUTH_FAILURE -> "TRUST_AUTH_FAILURE"
      | Non_static_id s -> s
    let of_string =
      function
      | "INVALID_DIRECTORY_STATE" -> INVALID_DIRECTORY_STATE
      | "DIRECTORY_TIMEOUT" -> DIRECTORY_TIMEOUT
      | "DIRECTORY_RESOURCES_EXCEEDED" -> DIRECTORY_RESOURCES_EXCEEDED
      | "NO_DISK_SPACE" -> NO_DISK_SPACE
      | "TRUST_AUTH_FAILURE" -> TRUST_AUTH_FAILURE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DirectoryUnavailableReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DirectoryUnavailableReason" j)
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | INVALID_REALM 
      | INVALID_DIRECTORY_TYPE 
      | INVALID_SECONDARY_REGION 
      | INVALID_NEXT_TOKEN 
      | INVALID_ATTRIBUTE_VALUE 
      | INVALID_ATTRIBUTE_NAME 
      | INVALID_ATTRIBUTE_FOR_USER 
      | INVALID_ATTRIBUTE_FOR_GROUP 
      | INVALID_ATTRIBUTE_FOR_SEARCH 
      | INVALID_ATTRIBUTE_FOR_MODIFY 
      | DUPLICATE_ATTRIBUTE 
      | MISSING_ATTRIBUTE 
      | ATTRIBUTE_EXISTS 
      | LDAP_SIZE_LIMIT_EXCEEDED 
      | LDAP_UNSUPPORTED_OPERATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INVALID_REALM -> "INVALID_REALM"
      | INVALID_DIRECTORY_TYPE -> "INVALID_DIRECTORY_TYPE"
      | INVALID_SECONDARY_REGION -> "INVALID_SECONDARY_REGION"
      | INVALID_NEXT_TOKEN -> "INVALID_NEXT_TOKEN"
      | INVALID_ATTRIBUTE_VALUE -> "INVALID_ATTRIBUTE_VALUE"
      | INVALID_ATTRIBUTE_NAME -> "INVALID_ATTRIBUTE_NAME"
      | INVALID_ATTRIBUTE_FOR_USER -> "INVALID_ATTRIBUTE_FOR_USER"
      | INVALID_ATTRIBUTE_FOR_GROUP -> "INVALID_ATTRIBUTE_FOR_GROUP"
      | INVALID_ATTRIBUTE_FOR_SEARCH -> "INVALID_ATTRIBUTE_FOR_SEARCH"
      | INVALID_ATTRIBUTE_FOR_MODIFY -> "INVALID_ATTRIBUTE_FOR_MODIFY"
      | DUPLICATE_ATTRIBUTE -> "DUPLICATE_ATTRIBUTE"
      | MISSING_ATTRIBUTE -> "MISSING_ATTRIBUTE"
      | ATTRIBUTE_EXISTS -> "ATTRIBUTE_EXISTS"
      | LDAP_SIZE_LIMIT_EXCEEDED -> "LDAP_SIZE_LIMIT_EXCEEDED"
      | LDAP_UNSUPPORTED_OPERATION -> "LDAP_UNSUPPORTED_OPERATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "INVALID_REALM" -> INVALID_REALM
      | "INVALID_DIRECTORY_TYPE" -> INVALID_DIRECTORY_TYPE
      | "INVALID_SECONDARY_REGION" -> INVALID_SECONDARY_REGION
      | "INVALID_NEXT_TOKEN" -> INVALID_NEXT_TOKEN
      | "INVALID_ATTRIBUTE_VALUE" -> INVALID_ATTRIBUTE_VALUE
      | "INVALID_ATTRIBUTE_NAME" -> INVALID_ATTRIBUTE_NAME
      | "INVALID_ATTRIBUTE_FOR_USER" -> INVALID_ATTRIBUTE_FOR_USER
      | "INVALID_ATTRIBUTE_FOR_GROUP" -> INVALID_ATTRIBUTE_FOR_GROUP
      | "INVALID_ATTRIBUTE_FOR_SEARCH" -> INVALID_ATTRIBUTE_FOR_SEARCH
      | "INVALID_ATTRIBUTE_FOR_MODIFY" -> INVALID_ATTRIBUTE_FOR_MODIFY
      | "DUPLICATE_ATTRIBUTE" -> DUPLICATE_ATTRIBUTE
      | "MISSING_ATTRIBUTE" -> MISSING_ATTRIBUTE
      | "ATTRIBUTE_EXISTS" -> ATTRIBUTE_EXISTS
      | "LDAP_SIZE_LIMIT_EXCEEDED" -> LDAP_SIZE_LIMIT_EXCEEDED
      | "LDAP_UNSUPPORTED_OPERATION" -> LDAP_UNSUPPORTED_OPERATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module User =
  struct
    type nonrec t =
      {
      distinguishedName: DistinguishedName.t option
        [@ocaml.doc "The distinguished name of the object."];
      emailAddress: EmailAddress.t option
        [@ocaml.doc "The email address of the user."];
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the user account is active."];
      givenName: GivenName.t option
        [@ocaml.doc "The first name of the user."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "An expression that includes one or more attributes, data types, and values of a user."];
      sAMAccountName: UserName.t option [@ocaml.doc "The name of the user."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the user."];
      surname: Surname.t option [@ocaml.doc "The last name of the user."];
      userPrincipalName: UserPrincipalName.t option
        [@ocaml.doc
          "The UPN that is an internet-style login name for a user and based on the internet standard RFC 822. The UPN is shorter than the distinguished name and easier to remember."]}
    let make ?distinguishedName =
      fun ?emailAddress ->
        fun ?enabled ->
          fun ?givenName ->
            fun ?otherAttributes ->
              fun ?sAMAccountName ->
                fun ?sID ->
                  fun ?surname ->
                    fun ?userPrincipalName ->
                      fun () ->
                        {
                          distinguishedName;
                          emailAddress;
                          enabled;
                          givenName;
                          otherAttributes;
                          sAMAccountName;
                          sID;
                          surname;
                          userPrincipalName
                        }
    let to_value x =
      structure_to_value
        [("DistinguishedName",
           (Option.map x.distinguishedName ~f:DistinguishedName.to_value));
        ("EmailAddress",
          (Option.map x.emailAddress ~f:EmailAddress.to_value));
        ("Enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("GivenName", (Option.map x.givenName ~f:GivenName.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:UserName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value));
        ("Surname", (Option.map x.surname ~f:Surname.to_value));
        ("UserPrincipalName",
          (Option.map x.userPrincipalName ~f:UserPrincipalName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userPrincipalName =
        (Option.map ~f:UserPrincipalName.of_xml)
          (Xml.child xml_arg0 "UserPrincipalName") in
      let surname =
        (Option.map ~f:Surname.of_xml) (Xml.child xml_arg0 "Surname") in
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:UserName.of_xml) (Xml.child xml_arg0 "SAMAccountName") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let givenName =
        (Option.map ~f:GivenName.of_xml) (Xml.child xml_arg0 "GivenName") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Enabled") in
      let emailAddress =
        (Option.map ~f:EmailAddress.of_xml)
          (Xml.child xml_arg0 "EmailAddress") in
      let distinguishedName =
        (Option.map ~f:DistinguishedName.of_xml)
          (Xml.child xml_arg0 "DistinguishedName") in
      make ?userPrincipalName ?surname ?sID ?sAMAccountName ?otherAttributes
        ?givenName ?enabled ?emailAddress ?distinguishedName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userPrincipalName =
        field_map json__ "UserPrincipalName" UserPrincipalName.of_json in
      let surname = field_map json__ "Surname" Surname.of_json in
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName = field_map json__ "SAMAccountName" UserName.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let givenName = field_map json__ "GivenName" GivenName.of_json in
      let enabled = field_map json__ "Enabled" Boolean.of_json in
      let emailAddress = field_map json__ "EmailAddress" EmailAddress.of_json in
      let distinguishedName =
        field_map json__ "DistinguishedName" DistinguishedName.of_json in
      make ?userPrincipalName ?surname ?sID ?sAMAccountName ?otherAttributes
        ?givenName ?enabled ?emailAddress ?distinguishedName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A user object that contains identifying information and attributes for a specified user."]
module Group =
  struct
    type nonrec t =
      {
      distinguishedName: DistinguishedName.t option
        [@ocaml.doc "The distinguished name of the object."];
      groupScope: GroupScope.t option
        [@ocaml.doc
          "The scope of the AD group. For details, see Active Directory security groups"];
      groupType: GroupType.t option
        [@ocaml.doc
          "The AD group type. For details, see Active Directory security group type."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "An expression of one or more attributes, data types, and the values of a group."];
      sAMAccountName: GroupName.t option
        [@ocaml.doc "The name of the group."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the group."]}
    let make ?distinguishedName =
      fun ?groupScope ->
        fun ?groupType ->
          fun ?otherAttributes ->
            fun ?sAMAccountName ->
              fun ?sID ->
                fun () ->
                  {
                    distinguishedName;
                    groupScope;
                    groupType;
                    otherAttributes;
                    sAMAccountName;
                    sID
                  }
    let to_value x =
      structure_to_value
        [("DistinguishedName",
           (Option.map x.distinguishedName ~f:DistinguishedName.to_value));
        ("GroupScope", (Option.map x.groupScope ~f:GroupScope.to_value));
        ("GroupType", (Option.map x.groupType ~f:GroupType.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:GroupName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:GroupName.of_xml)
          (Xml.child xml_arg0 "SAMAccountName") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let groupType =
        (Option.map ~f:GroupType.of_xml) (Xml.child xml_arg0 "GroupType") in
      let groupScope =
        (Option.map ~f:GroupScope.of_xml) (Xml.child xml_arg0 "GroupScope") in
      let distinguishedName =
        (Option.map ~f:DistinguishedName.of_xml)
          (Xml.child xml_arg0 "DistinguishedName") in
      make ?sID ?sAMAccountName ?otherAttributes ?groupType ?groupScope
        ?distinguishedName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName =
        field_map json__ "SAMAccountName" GroupName.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let groupType = field_map json__ "GroupType" GroupType.of_json in
      let groupScope = field_map json__ "GroupScope" GroupScope.of_json in
      let distinguishedName =
        field_map json__ "DistinguishedName" DistinguishedName.of_json in
      make ?sID ?sAMAccountName ?otherAttributes ?groupType ?groupScope
        ?distinguishedName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A group object that contains identifying information and attributes for a specified group."]
module UserSummary =
  struct
    type nonrec t =
      {
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the user account is active."];
      givenName: GivenName.t option
        [@ocaml.doc "The first name of the user."];
      sAMAccountName: UserName.t option [@ocaml.doc "The name of the user."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the user."];
      surname: Surname.t option [@ocaml.doc "The last name of the user."]}
    let make ?enabled =
      fun ?givenName ->
        fun ?sAMAccountName ->
          fun ?sID ->
            fun ?surname ->
              fun () -> { enabled; givenName; sAMAccountName; sID; surname }
    let to_value x =
      structure_to_value
        [("Enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("GivenName", (Option.map x.givenName ~f:GivenName.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:UserName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value));
        ("Surname", (Option.map x.surname ~f:Surname.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let surname =
        (Option.map ~f:Surname.of_xml) (Xml.child xml_arg0 "Surname") in
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:UserName.of_xml) (Xml.child xml_arg0 "SAMAccountName") in
      let givenName =
        (Option.map ~f:GivenName.of_xml) (Xml.child xml_arg0 "GivenName") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Enabled") in
      make ?surname ?sID ?sAMAccountName ?givenName ?enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let surname = field_map json__ "Surname" Surname.of_json in
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName = field_map json__ "SAMAccountName" UserName.of_json in
      let givenName = field_map json__ "GivenName" GivenName.of_json in
      let enabled = field_map json__ "Enabled" Boolean.of_json in
      make ?surname ?sID ?sAMAccountName ?givenName ?enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure containing a subset of the fields of a user object from a directory."]
module GroupSummary =
  struct
    type nonrec t =
      {
      groupScope: GroupScope.t option
        [@ocaml.doc
          "The scope of the AD group. For details, see Active Directory security groups."];
      groupType: GroupType.t option
        [@ocaml.doc
          "The AD group type. For details, see Active Directory security group type."];
      sAMAccountName: GroupName.t option
        [@ocaml.doc "The name of the group."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the group."]}
    let make ?groupScope =
      fun ?groupType ->
        fun ?sAMAccountName ->
          fun ?sID ->
            fun () -> { groupScope; groupType; sAMAccountName; sID }
    let to_value x =
      structure_to_value
        [("GroupScope", (Option.map x.groupScope ~f:GroupScope.to_value));
        ("GroupType", (Option.map x.groupType ~f:GroupType.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:GroupName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:GroupName.of_xml)
          (Xml.child xml_arg0 "SAMAccountName") in
      let groupType =
        (Option.map ~f:GroupType.of_xml) (Xml.child xml_arg0 "GroupType") in
      let groupScope =
        (Option.map ~f:GroupScope.of_xml) (Xml.child xml_arg0 "GroupScope") in
      make ?sID ?sAMAccountName ?groupType ?groupScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName =
        field_map json__ "SAMAccountName" GroupName.of_json in
      let groupType = field_map json__ "GroupType" GroupType.of_json in
      let groupScope = field_map json__ "GroupScope" GroupScope.of_json in
      make ?sID ?sAMAccountName ?groupType ?groupScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure containing a subset of fields of a group object from a directory."]
module Member =
  struct
    type nonrec t =
      {
      memberType: MemberType.t option
        [@ocaml.doc "The AD type of the member object."];
      sAMAccountName: MemberName.t option
        [@ocaml.doc "The name of the group member."];
      sID: SID.t option
        [@ocaml.doc
          "The unique security identifier (SID) of the group member."]}
    let make ?memberType =
      fun ?sAMAccountName ->
        fun ?sID -> fun () -> { memberType; sAMAccountName; sID }
    let to_value x =
      structure_to_value
        [("MemberType", (Option.map x.memberType ~f:MemberType.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:MemberName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:MemberName.of_xml)
          (Xml.child xml_arg0 "SAMAccountName") in
      let memberType =
        (Option.map ~f:MemberType.of_xml) (Xml.child xml_arg0 "MemberType") in
      make ?sID ?sAMAccountName ?memberType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName =
        field_map json__ "SAMAccountName" MemberName.of_json in
      let memberType = field_map json__ "MemberType" MemberType.of_json in
      make ?sID ?sAMAccountName ?memberType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A member object that contains identifying information for a specified member."]
module AccessDeniedException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      reason: AccessDeniedReason.t option
        [@ocaml.doc "Reason the request was unauthorized."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("Reason", (Option.map x.reason ~f:AccessDeniedReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:AccessDeniedReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason = field_map json__ "Reason" AccessDeniedReason.of_json in
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You don't have permission to perform the request or access the directory. It can also occur when the DirectoryId doesn't exist or the user, member, or group might be outside of your organizational unit (OU). Make sure that you have the authentication and authorization to perform the action. Review the directory information in the request, and make sure that the object isn't outside of your OU."]
module ConflictException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This error will occur when you try to create a resource that conflicts with an existing object. It can also occur when adding a member to a group that the member is already in. This error can be caused by a request sent within the 8-hour idempotency window with the same client token but different input parameters. Client tokens should not be re-used across different requests. After 8 hours, any request with the same client token is treated as a new request."]
module DirectoryUnavailableException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      reason: DirectoryUnavailableReason.t option
        [@ocaml.doc "Reason the request failed for the specified directory."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("Reason",
          (Option.map x.reason ~f:DirectoryUnavailableReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:DirectoryUnavailableReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map json__ "Reason" DirectoryUnavailableReason.of_json in
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request could not be completed due to a problem in the configuration or current state of the specified directory."]
module InternalServerException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The operation didn't succeed because an internal error occurred. Try again later."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource couldn't be found."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      retryAfterSeconds: Integer.t option
        [@ocaml.doc
          "The recommended amount of seconds to retry after a throttling exception."]}
    let make ?message =
      fun ?retryAfterSeconds -> fun () -> { message; retryAfterSeconds }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("Retry-After", (Option.map x.retryAfterSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryAfterSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Retry-After") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?retryAfterSeconds ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryAfterSeconds =
        field_map json__ "RetryAfterSeconds" Integer.of_json in
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?retryAfterSeconds ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit on the number of requests per second has been exceeded."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "Reason the request failed validation."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("Reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map json__ "Reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "Message" ExceptionMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request isn't valid. Review the details in the error message to update the invalid parameters or values in your request."]
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\x00-\\x7F]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module DirectoryId =
  struct
    type nonrec t = string
    let context_ = "DirectoryId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^d-[0-9a-f]{10}$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DirectoryId" j
    let to_json = simple_to_json to_value
  end
module UpdateType =
  struct
    type nonrec t =
      | ADD 
      | REPLACE 
      | REMOVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ADD -> "ADD"
      | REPLACE -> "REPLACE"
      | REMOVE -> "REMOVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ADD" -> ADD
      | "REPLACE" -> REPLACE
      | "REMOVE" -> REMOVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UpdateType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UpdateType" j)
    let to_json = simple_to_json to_value
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:6144) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module Realm =
  struct
    type nonrec t = string
    let context_ = "Realm"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([a-zA-Z0-9]+[\\\\.-])+([a-zA-Z0-9])+[.]?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Realm" j
    let to_json = simple_to_json to_value
  end
module UserList =
  struct
    type nonrec t = User.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:User.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:User.of_xml)
    let of_json j = list_of_json ~kind:"UserList" ~of_json:User.of_json j
    let to_json v = composed_to_json to_value v
  end
module LdapDisplayNameList =
  struct
    type nonrec t = LdapDisplayName.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LdapDisplayName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LdapDisplayName.of_xml)
    let of_json j =
      list_of_json ~kind:"LdapDisplayNameList"
        ~of_json:LdapDisplayName.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SearchString =
  struct
    type nonrec t = string
    let context_ = "SearchString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SearchString" j
    let to_json = simple_to_json to_value
  end
module GroupList =
  struct
    type nonrec t = Group.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Group.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Group.of_xml)
    let of_json j = list_of_json ~kind:"GroupList" ~of_json:Group.of_json j
    let to_json v = composed_to_json to_value v
  end
module UserSummaryList =
  struct
    type nonrec t = UserSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:UserSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UserSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"UserSummaryList" ~of_json:UserSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupSummaryList =
  struct
    type nonrec t = GroupSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GroupSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupSummaryList" ~of_json:GroupSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MemberList =
  struct
    type nonrec t = Member.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Member.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Member.of_xml)
    let of_json j = list_of_json ~kind:"MemberList" ~of_json:Member.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateUserResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates user information."]
module UpdateUserRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      emailAddress: EmailAddress.t option
        [@ocaml.doc "The email address of the user."];
      givenName: GivenName.t option
        [@ocaml.doc "The first name of the user."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "An expression that defines one or more attribute names with the data type and value of each attribute. A key is an attribute name, and the value is a list of maps. For a list of supported attributes, see Directory Service Data Attributes. Attribute names are case insensitive."];
      sAMAccountName: UserName.t [@ocaml.doc "The name of the user."];
      surname: Surname.t option [@ocaml.doc "The last name of the user."];
      updateType: UpdateType.t option
        [@ocaml.doc
          "The type of update to be performed. If no value exists for the attribute, use ADD. Otherwise, use REPLACE to change an attribute value or REMOVE to clear the attribute value."]}
    let context_ = "UpdateUserRequest"
    let make ?clientToken =
      fun ?emailAddress ->
        fun ?givenName ->
          fun ?otherAttributes ->
            fun ?surname ->
              fun ?updateType ->
                fun ~directoryId ->
                  fun ~sAMAccountName ->
                    fun () ->
                      {
                        clientToken;
                        emailAddress;
                        givenName;
                        otherAttributes;
                        surname;
                        updateType;
                        directoryId;
                        sAMAccountName
                      }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("EmailAddress",
          (Option.map x.emailAddress ~f:EmailAddress.to_value));
        ("GivenName", (Option.map x.givenName ~f:GivenName.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("SAMAccountName", (Some (UserName.to_value x.sAMAccountName)));
        ("Surname", (Option.map x.surname ~f:Surname.to_value));
        ("UpdateType", (Option.map x.updateType ~f:UpdateType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updateType =
        (Option.map ~f:UpdateType.of_xml) (Xml.child xml_arg0 "UpdateType") in
      let surname =
        (Option.map ~f:Surname.of_xml) (Xml.child xml_arg0 "Surname") in
      let sAMAccountName =
        UserName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let givenName =
        (Option.map ~f:GivenName.of_xml) (Xml.child xml_arg0 "GivenName") in
      let emailAddress =
        (Option.map ~f:EmailAddress.of_xml)
          (Xml.child xml_arg0 "EmailAddress") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?updateType ?surname ~sAMAccountName ?otherAttributes ?givenName
        ?emailAddress ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updateType = field_map json__ "UpdateType" UpdateType.of_json in
      let surname = field_map json__ "Surname" Surname.of_json in
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" UserName.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let givenName = field_map json__ "GivenName" GivenName.of_json in
      let emailAddress = field_map json__ "EmailAddress" EmailAddress.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?updateType ?surname ~sAMAccountName ?otherAttributes ?givenName
        ?emailAddress ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates user information."]
module UpdateGroupResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates group information."]
module UpdateGroupRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      groupScope: GroupScope.t option
        [@ocaml.doc
          "The scope of the AD group. For details, see Active Directory security groups."];
      groupType: GroupType.t option
        [@ocaml.doc
          "The AD group type. For details, see Active Directory security group type."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "An expression that defines one or more attributes with the data type and the value of each attribute."];
      sAMAccountName: GroupName.t [@ocaml.doc "The name of the group."];
      updateType: UpdateType.t option
        [@ocaml.doc
          "The type of update to be performed. If no value exists for the attribute, use ADD. Otherwise, use REPLACE to change an attribute value or REMOVE to clear the attribute value."]}
    let context_ = "UpdateGroupRequest"
    let make ?clientToken =
      fun ?groupScope ->
        fun ?groupType ->
          fun ?otherAttributes ->
            fun ?updateType ->
              fun ~directoryId ->
                fun ~sAMAccountName ->
                  fun () ->
                    {
                      clientToken;
                      groupScope;
                      groupType;
                      otherAttributes;
                      updateType;
                      directoryId;
                      sAMAccountName
                    }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("GroupScope", (Option.map x.groupScope ~f:GroupScope.to_value));
        ("GroupType", (Option.map x.groupType ~f:GroupType.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("SAMAccountName", (Some (GroupName.to_value x.sAMAccountName)));
        ("UpdateType", (Option.map x.updateType ~f:UpdateType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updateType =
        (Option.map ~f:UpdateType.of_xml) (Xml.child xml_arg0 "UpdateType") in
      let sAMAccountName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let groupType =
        (Option.map ~f:GroupType.of_xml) (Xml.child xml_arg0 "GroupType") in
      let groupScope =
        (Option.map ~f:GroupScope.of_xml) (Xml.child xml_arg0 "GroupScope") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?updateType ~sAMAccountName ?otherAttributes ?groupType
        ?groupScope ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updateType = field_map json__ "UpdateType" UpdateType.of_json in
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" GroupName.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let groupType = field_map json__ "GroupType" GroupType.of_json in
      let groupScope = field_map json__ "GroupScope" GroupScope.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?updateType ~sAMAccountName ?otherAttributes ?groupType
        ?groupScope ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates group information."]
module SearchUsersResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory where the address block is added."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc "The domain that's associated with the user."];
      users: UserList.t option
        [@ocaml.doc "The user information that the request returns."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?nextToken ->
        fun ?realm ->
          fun ?users -> fun () -> { directoryId; nextToken; realm; users }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("Users", (Option.map x.users ~f:UserList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let users =
        (Option.map ~f:UserList.of_xml) (Xml.child xml_arg0 "Users") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?users ?realm ?nextToken ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let users = field_map json__ "Users" UserList.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?users ?realm ?nextToken ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches the specified directory for a user. You can find users that match the SearchString parameter with the value of their attributes included in the SearchString parameter. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the SearchUsers.NextToken member contains a token that you pass in the next call to SearchUsers. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module SearchUsersRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to be returned per request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the user. This parameter is optional, so you can return users outside of your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD users are returned. This value is case insensitive."];
      searchAttributes: LdapDisplayNameList.t
        [@ocaml.doc
          "One or more data attributes that are used to search for a user. For a list of supported attributes, see Directory Service Data Attributes."];
      searchString: SearchString.t
        [@ocaml.doc
          "The attribute value that you want to search for. Wildcard (*) searches aren't supported. For a list of supported attributes, see Directory Service Data Attributes."]}
    let context_ = "SearchUsersRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?realm ->
          fun ~directoryId ->
            fun ~searchAttributes ->
              fun ~searchString ->
                fun () ->
                  {
                    maxResults;
                    nextToken;
                    realm;
                    directoryId;
                    searchAttributes;
                    searchString
                  }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SearchAttributes",
          (Some (LdapDisplayNameList.to_value x.searchAttributes)));
        ("SearchString", (Some (SearchString.to_value x.searchString)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let searchString =
        SearchString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SearchString") in
      let searchAttributes =
        LdapDisplayNameList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SearchAttributes") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ~searchString ~searchAttributes ?realm ?nextToken ?maxResults
        ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let searchString =
        field_map_exn json__ "SearchString" SearchString.of_json in
      let searchAttributes =
        field_map_exn json__ "SearchAttributes" LdapDisplayNameList.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ~searchString ~searchAttributes ?realm ?nextToken ?maxResults
        ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches the specified directory for a user. You can find users that match the SearchString parameter with the value of their attributes included in the SearchString parameter. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the SearchUsers.NextToken member contains a token that you pass in the next call to SearchUsers. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module SearchGroupsResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      groups: GroupList.t option
        [@ocaml.doc "The group information that the request returns."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc "The domain that's associated with the group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?groups ->
        fun ?nextToken ->
          fun ?realm -> fun () -> { directoryId; groups; nextToken; realm }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Groups", (Option.map x.groups ~f:GroupList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let groups =
        (Option.map ~f:GroupList.of_xml) (Xml.child xml_arg0 "Groups") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?realm ?nextToken ?groups ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let groups = field_map json__ "Groups" GroupList.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?realm ?nextToken ?groups ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches the specified directory for a group. You can find groups that match the SearchString parameter with the value of their attributes included in the SearchString parameter. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the SearchGroups.NextToken member contains a token that you pass in the next call to SearchGroups. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module SearchGroupsRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to be returned per request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group. This parameter is optional, so you can return groups outside of your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD groups are returned. This value is case insensitive."];
      searchAttributes: LdapDisplayNameList.t
        [@ocaml.doc
          "One or more data attributes that are used to search for a group. For a list of supported attributes, see Directory Service Data Attributes."];
      searchString: SearchString.t
        [@ocaml.doc
          "The attribute value that you want to search for. Wildcard (*) searches aren't supported. For a list of supported attributes, see Directory Service Data Attributes."]}
    let context_ = "SearchGroupsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?realm ->
          fun ~directoryId ->
            fun ~searchAttributes ->
              fun ~searchString ->
                fun () ->
                  {
                    maxResults;
                    nextToken;
                    realm;
                    directoryId;
                    searchAttributes;
                    searchString
                  }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SearchAttributes",
          (Some (LdapDisplayNameList.to_value x.searchAttributes)));
        ("SearchString", (Some (SearchString.to_value x.searchString)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let searchString =
        SearchString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SearchString") in
      let searchAttributes =
        LdapDisplayNameList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SearchAttributes") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ~searchString ~searchAttributes ?realm ?nextToken ?maxResults
        ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let searchString =
        field_map_exn json__ "SearchString" SearchString.of_json in
      let searchAttributes =
        field_map_exn json__ "SearchAttributes" LdapDisplayNameList.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ~searchString ~searchAttributes ?realm ?nextToken ?maxResults
        ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches the specified directory for a group. You can find groups that match the SearchString parameter with the value of their attributes included in the SearchString parameter. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the SearchGroups.NextToken member contains a token that you pass in the next call to SearchGroups. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module RemoveGroupMemberResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes a member from a group."]
module RemoveGroupMemberRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the member."];
      groupName: GroupName.t [@ocaml.doc "The name of the group."];
      memberName: MemberName.t
        [@ocaml.doc
          "The SAMAccountName of the user, group, or computer to remove from the group."];
      memberRealm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group member. This parameter defaults to the Managed Microsoft AD domain. This parameter is optional and case insensitive."]}
    let context_ = "RemoveGroupMemberRequest"
    let make ?clientToken =
      fun ?memberRealm ->
        fun ~directoryId ->
          fun ~groupName ->
            fun ~memberName ->
              fun () ->
                {
                  clientToken;
                  memberRealm;
                  directoryId;
                  groupName;
                  memberName
                }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("GroupName", (Some (GroupName.to_value x.groupName)));
        ("MemberName", (Some (MemberName.to_value x.memberName)));
        ("MemberRealm", (Option.map x.memberRealm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let memberRealm =
        (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "MemberRealm") in
      let memberName =
        MemberName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MemberName") in
      let groupName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?memberRealm ~memberName ~groupName ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let memberRealm = field_map json__ "MemberRealm" Realm.of_json in
      let memberName = field_map_exn json__ "MemberName" MemberName.of_json in
      let groupName = field_map_exn json__ "GroupName" GroupName.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?memberRealm ~memberName ~groupName ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes a member from a group."]
module ListUsersResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc "The domain that's associated with the user."];
      users: UserSummaryList.t option
        [@ocaml.doc "The user information that the request returns."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?nextToken ->
        fun ?realm ->
          fun ?users -> fun () -> { directoryId; nextToken; realm; users }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("Users", (Option.map x.users ~f:UserSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let users =
        (Option.map ~f:UserSummaryList.of_xml) (Xml.child xml_arg0 "Users") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?users ?realm ?nextToken ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let users = field_map json__ "Users" UserSummaryList.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?users ?realm ?nextToken ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns user information for the specified directory. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListUsers.NextToken member contains a token that you pass in the next call to ListUsers. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListUsersRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to be returned per request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the user. This parameter is optional, so you can return users outside of your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD users are returned. This value is case insensitive."]}
    let context_ = "ListUsersRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?realm ->
          fun ~directoryId ->
            fun () -> { maxResults; nextToken; realm; directoryId }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ?realm ?nextToken ?maxResults ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ?realm ?nextToken ?maxResults ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns user information for the specified directory. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListUsers.NextToken member contains a token that you pass in the next call to ListUsers. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListGroupsResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      groups: GroupSummaryList.t option
        [@ocaml.doc "The group information that the request returns."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc "The domain name associated with the group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?groups ->
        fun ?nextToken ->
          fun ?realm -> fun () -> { directoryId; groups; nextToken; realm }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Groups", (Option.map x.groups ~f:GroupSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let groups =
        (Option.map ~f:GroupSummaryList.of_xml) (Xml.child xml_arg0 "Groups") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?realm ?nextToken ?groups ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let groups = field_map json__ "Groups" GroupSummaryList.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?realm ?nextToken ?groups ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns group information for the specified directory. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListGroups.NextToken member contains a token that you pass in the next call to ListGroups. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListGroupsRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to be returned per request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name associated with the directory. This parameter is optional, so you can return groups outside of your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD groups are returned. This value is case insensitive."]}
    let context_ = "ListGroupsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?realm ->
          fun ~directoryId ->
            fun () -> { maxResults; nextToken; realm; directoryId }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ?realm ?nextToken ?maxResults ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ?realm ?nextToken ?maxResults ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns group information for the specified directory. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListGroups.NextToken member contains a token that you pass in the next call to ListGroups. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListGroupsForMemberResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the member."];
      groups: GroupSummaryList.t option
        [@ocaml.doc "The group information that the request returns."];
      memberRealm: Realm.t option
        [@ocaml.doc "The domain that's associated with the member."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc "The domain that's associated with the group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?groups ->
        fun ?memberRealm ->
          fun ?nextToken ->
            fun ?realm ->
              fun () ->
                { directoryId; groups; memberRealm; nextToken; realm }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Groups", (Option.map x.groups ~f:GroupSummaryList.to_value));
        ("MemberRealm", (Option.map x.memberRealm ~f:Realm.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let memberRealm =
        (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "MemberRealm") in
      let groups =
        (Option.map ~f:GroupSummaryList.of_xml) (Xml.child xml_arg0 "Groups") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?realm ?nextToken ?memberRealm ?groups ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let memberRealm = field_map json__ "MemberRealm" Realm.of_json in
      let groups = field_map json__ "Groups" GroupSummaryList.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?realm ?nextToken ?memberRealm ?groups ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns group information for the specified member. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListGroupsForMember.NextToken member contains a token that you pass in the next call to ListGroupsForMember. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListGroupsForMemberRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the member."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to be returned per request."];
      memberRealm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group member. This parameter is optional, so you can limit your results to the group members in a specific domain. This parameter is case insensitive and defaults to Realm"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group. This parameter is optional, so you can return groups outside of your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD groups are returned. This value is case insensitive and defaults to your Managed Microsoft AD domain."];
      sAMAccountName: MemberName.t
        [@ocaml.doc
          "The SAMAccountName of the user, group, or computer that's a member of the group."]}
    let context_ = "ListGroupsForMemberRequest"
    let make ?maxResults =
      fun ?memberRealm ->
        fun ?nextToken ->
          fun ?realm ->
            fun ~directoryId ->
              fun ~sAMAccountName ->
                fun () ->
                  {
                    maxResults;
                    memberRealm;
                    nextToken;
                    realm;
                    directoryId;
                    sAMAccountName
                  }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("MemberRealm", (Option.map x.memberRealm ~f:Realm.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SAMAccountName", (Some (MemberName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        MemberName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let memberRealm =
        (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "MemberRealm") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ~sAMAccountName ?realm ?nextToken ?memberRealm ?maxResults
        ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" MemberName.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let memberRealm = field_map json__ "MemberRealm" Realm.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ~sAMAccountName ?realm ?nextToken ?memberRealm ?maxResults
        ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns group information for the specified member. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListGroupsForMember.NextToken member contains a token that you pass in the next call to ListGroupsForMember. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListGroupMembersResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "Identifier (ID) of the directory associated with the group."];
      memberRealm: Realm.t option
        [@ocaml.doc "The domain name that's associated with the member."];
      members: MemberList.t option
        [@ocaml.doc "The member information that the request returns."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc "The domain name that's associated with the group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?memberRealm ->
        fun ?members ->
          fun ?nextToken ->
            fun ?realm ->
              fun () ->
                { directoryId; memberRealm; members; nextToken; realm }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("MemberRealm", (Option.map x.memberRealm ~f:Realm.to_value));
        ("Members", (Option.map x.members ~f:MemberList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let members =
        (Option.map ~f:MemberList.of_xml) (Xml.child xml_arg0 "Members") in
      let memberRealm =
        (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "MemberRealm") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?realm ?nextToken ?members ?memberRealm ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let members = field_map json__ "Members" MemberList.of_json in
      let memberRealm = field_map json__ "MemberRealm" Realm.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?realm ?nextToken ?members ?memberRealm ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns member information for the specified group. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListGroupMembers.NextToken member contains a token that you pass in the next call to ListGroupMembers. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module ListGroupMembersRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to be returned per request."];
      memberRealm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group member. This parameter defaults to the Managed Microsoft AD domain. This parameter is optional and case insensitive."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "An encoded paging token for paginated calls that can be passed back to retrieve the next page."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group. This parameter is optional, so you can return members from a group outside of your Managed Microsoft AD domain. When no value is defined, only members of your Managed Microsoft AD groups are returned. This value is case insensitive."];
      sAMAccountName: GroupName.t [@ocaml.doc "The name of the group."]}
    let context_ = "ListGroupMembersRequest"
    let make ?maxResults =
      fun ?memberRealm ->
        fun ?nextToken ->
          fun ?realm ->
            fun ~directoryId ->
              fun ~sAMAccountName ->
                fun () ->
                  {
                    maxResults;
                    memberRealm;
                    nextToken;
                    realm;
                    directoryId;
                    sAMAccountName
                  }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("MemberRealm", (Option.map x.memberRealm ~f:Realm.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SAMAccountName", (Some (GroupName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let memberRealm =
        (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "MemberRealm") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ~sAMAccountName ?realm ?nextToken ?memberRealm ?maxResults
        ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" GroupName.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let memberRealm = field_map json__ "MemberRealm" Realm.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ~sAMAccountName ?realm ?nextToken ?memberRealm ?maxResults
        ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns member information for the specified group. This operation supports pagination with the use of the NextToken request and response parameters. If more results are available, the ListGroupMembers.NextToken member contains a token that you pass in the next call to ListGroupMembers. This retrieves the next set of items. You can also specify a maximum number of return results with the MaxResults parameter."]
module DisableUserResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deactivates an active user account. For information about how to enable an inactive user account, see ResetUserPassword in the Directory Service API Reference."]
module DisableUserRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      sAMAccountName: UserName.t [@ocaml.doc "The name of the user."]}
    let context_ = "DisableUserRequest"
    let make ?clientToken =
      fun ~directoryId ->
        fun ~sAMAccountName ->
          fun () -> { clientToken; directoryId; sAMAccountName }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("SAMAccountName", (Some (UserName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        UserName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~sAMAccountName ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" UserName.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ~sAMAccountName ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deactivates an active user account. For information about how to enable an inactive user account, see ResetUserPassword in the Directory Service API Reference."]
module DescribeUserResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      distinguishedName: DistinguishedName.t option
        [@ocaml.doc "The distinguished name of the object."];
      emailAddress: EmailAddress.t option
        [@ocaml.doc "The email address of the user."];
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the user account is active."];
      givenName: GivenName.t option
        [@ocaml.doc "The first name of the user."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "The attribute values that are returned for the attribute names that are included in the request. Attribute names are case insensitive."];
      realm: Realm.t option
        [@ocaml.doc "The domain name that's associated with the user."];
      sAMAccountName: UserName.t option [@ocaml.doc "The name of the user."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the user."];
      surname: Surname.t option [@ocaml.doc "The last name of the user."];
      userPrincipalName: UserPrincipalName.t option
        [@ocaml.doc
          "The UPN that is an Internet-style login name for a user and is based on the Internet standard RFC 822. The UPN is shorter than the distinguished name and easier to remember."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?distinguishedName ->
        fun ?emailAddress ->
          fun ?enabled ->
            fun ?givenName ->
              fun ?otherAttributes ->
                fun ?realm ->
                  fun ?sAMAccountName ->
                    fun ?sID ->
                      fun ?surname ->
                        fun ?userPrincipalName ->
                          fun () ->
                            {
                              directoryId;
                              distinguishedName;
                              emailAddress;
                              enabled;
                              givenName;
                              otherAttributes;
                              realm;
                              sAMAccountName;
                              sID;
                              surname;
                              userPrincipalName
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("DistinguishedName",
          (Option.map x.distinguishedName ~f:DistinguishedName.to_value));
        ("EmailAddress",
          (Option.map x.emailAddress ~f:EmailAddress.to_value));
        ("Enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("GivenName", (Option.map x.givenName ~f:GivenName.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:UserName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value));
        ("Surname", (Option.map x.surname ~f:Surname.to_value));
        ("UserPrincipalName",
          (Option.map x.userPrincipalName ~f:UserPrincipalName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userPrincipalName =
        (Option.map ~f:UserPrincipalName.of_xml)
          (Xml.child xml_arg0 "UserPrincipalName") in
      let surname =
        (Option.map ~f:Surname.of_xml) (Xml.child xml_arg0 "Surname") in
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:UserName.of_xml) (Xml.child xml_arg0 "SAMAccountName") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let givenName =
        (Option.map ~f:GivenName.of_xml) (Xml.child xml_arg0 "GivenName") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Enabled") in
      let emailAddress =
        (Option.map ~f:EmailAddress.of_xml)
          (Xml.child xml_arg0 "EmailAddress") in
      let distinguishedName =
        (Option.map ~f:DistinguishedName.of_xml)
          (Xml.child xml_arg0 "DistinguishedName") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?userPrincipalName ?surname ?sID ?sAMAccountName ?realm
        ?otherAttributes ?givenName ?enabled ?emailAddress ?distinguishedName
        ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userPrincipalName =
        field_map json__ "UserPrincipalName" UserPrincipalName.of_json in
      let surname = field_map json__ "Surname" Surname.of_json in
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName = field_map json__ "SAMAccountName" UserName.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let givenName = field_map json__ "GivenName" GivenName.of_json in
      let enabled = field_map json__ "Enabled" Boolean.of_json in
      let emailAddress = field_map json__ "EmailAddress" EmailAddress.of_json in
      let distinguishedName =
        field_map json__ "DistinguishedName" DistinguishedName.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?userPrincipalName ?surname ?sID ?sAMAccountName ?realm
        ?otherAttributes ?givenName ?enabled ?emailAddress ?distinguishedName
        ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns information about a specific user."]
module DescribeUserRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      otherAttributes: LdapDisplayNameList.t option
        [@ocaml.doc
          "One or more attribute names to be returned for the user. A key is an attribute name, and the value is a list of maps. For a list of supported attributes, see Directory Service Data Attributes."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the user. This parameter is optional, so you can return users outside your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD users are returned. This value is case insensitive."];
      sAMAccountName: UserName.t [@ocaml.doc "The name of the user."]}
    let context_ = "DescribeUserRequest"
    let make ?otherAttributes =
      fun ?realm ->
        fun ~directoryId ->
          fun ~sAMAccountName ->
            fun () -> { otherAttributes; realm; directoryId; sAMAccountName }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:LdapDisplayNameList.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SAMAccountName", (Some (UserName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        UserName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let otherAttributes =
        (Option.map ~f:LdapDisplayNameList.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ~sAMAccountName ?realm ?otherAttributes ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" UserName.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" LdapDisplayNameList.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ~sAMAccountName ?realm ?otherAttributes ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns information about a specific user."]
module DescribeGroupResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      distinguishedName: DistinguishedName.t option
        [@ocaml.doc "The distinguished name of the object."];
      groupScope: GroupScope.t option
        [@ocaml.doc
          "The scope of the AD group. For details, see Active Directory security groups."];
      groupType: GroupType.t option
        [@ocaml.doc
          "The AD group type. For details, see Active Directory security group type."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "The attribute values that are returned for the attribute names that are included in the request."];
      realm: Realm.t option
        [@ocaml.doc "The domain name that's associated with the group."];
      sAMAccountName: GroupName.t option
        [@ocaml.doc "The name of the group."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?distinguishedName ->
        fun ?groupScope ->
          fun ?groupType ->
            fun ?otherAttributes ->
              fun ?realm ->
                fun ?sAMAccountName ->
                  fun ?sID ->
                    fun () ->
                      {
                        directoryId;
                        distinguishedName;
                        groupScope;
                        groupType;
                        otherAttributes;
                        realm;
                        sAMAccountName;
                        sID
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("DistinguishedName",
          (Option.map x.distinguishedName ~f:DistinguishedName.to_value));
        ("GroupScope", (Option.map x.groupScope ~f:GroupScope.to_value));
        ("GroupType", (Option.map x.groupType ~f:GroupType.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:GroupName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:GroupName.of_xml)
          (Xml.child xml_arg0 "SAMAccountName") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let groupType =
        (Option.map ~f:GroupType.of_xml) (Xml.child xml_arg0 "GroupType") in
      let groupScope =
        (Option.map ~f:GroupScope.of_xml) (Xml.child xml_arg0 "GroupScope") in
      let distinguishedName =
        (Option.map ~f:DistinguishedName.of_xml)
          (Xml.child xml_arg0 "DistinguishedName") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?sID ?sAMAccountName ?realm ?otherAttributes ?groupType
        ?groupScope ?distinguishedName ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName =
        field_map json__ "SAMAccountName" GroupName.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let groupType = field_map json__ "GroupType" GroupType.of_json in
      let groupScope = field_map json__ "GroupScope" GroupScope.of_json in
      let distinguishedName =
        field_map json__ "DistinguishedName" DistinguishedName.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?sID ?sAMAccountName ?realm ?otherAttributes ?groupType
        ?groupScope ?distinguishedName ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns information about a specific group."]
module DescribeGroupRequest =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The Identifier (ID) of the directory associated with the group."];
      otherAttributes: LdapDisplayNameList.t option
        [@ocaml.doc
          "One or more attributes to be returned for the group. For a list of supported attributes, see Directory Service Data Attributes."];
      realm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group. This parameter is optional, so you can return groups outside of your Managed Microsoft AD domain. When no value is defined, only your Managed Microsoft AD groups are returned. This value is case insensitive."];
      sAMAccountName: GroupName.t [@ocaml.doc "The name of the group."]}
    let context_ = "DescribeGroupRequest"
    let make ?otherAttributes =
      fun ?realm ->
        fun ~directoryId ->
          fun ~sAMAccountName ->
            fun () -> { otherAttributes; realm; directoryId; sAMAccountName }
    let to_value x =
      structure_to_value
        [("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:LdapDisplayNameList.to_value));
        ("Realm", (Option.map x.realm ~f:Realm.to_value));
        ("SAMAccountName", (Some (GroupName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let realm = (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "Realm") in
      let otherAttributes =
        (Option.map ~f:LdapDisplayNameList.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      make ~sAMAccountName ?realm ?otherAttributes ~directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" GroupName.of_json in
      let realm = field_map json__ "Realm" Realm.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" LdapDisplayNameList.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      make ~sAMAccountName ?realm ?otherAttributes ~directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns information about a specific group."]
module DeleteUserResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a user."]
module DeleteUserRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the user."];
      sAMAccountName: UserName.t [@ocaml.doc "The name of the user."]}
    let context_ = "DeleteUserRequest"
    let make ?clientToken =
      fun ~directoryId ->
        fun ~sAMAccountName ->
          fun () -> { clientToken; directoryId; sAMAccountName }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("SAMAccountName", (Some (UserName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        UserName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~sAMAccountName ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" UserName.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ~sAMAccountName ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a user."]
module DeleteGroupResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a group."]
module DeleteGroupRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      sAMAccountName: GroupName.t [@ocaml.doc "The name of the group."]}
    let context_ = "DeleteGroupRequest"
    let make ?clientToken =
      fun ~directoryId ->
        fun ~sAMAccountName ->
          fun () -> { clientToken; directoryId; sAMAccountName }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("SAMAccountName", (Some (GroupName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~sAMAccountName ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" GroupName.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ~sAMAccountName ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a group."]
module CreateUserResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory where the address block is added."];
      sAMAccountName: UserName.t option [@ocaml.doc "The name of the user."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the user."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?sAMAccountName ->
        fun ?sID -> fun () -> { directoryId; sAMAccountName; sID }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:UserName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:UserName.of_xml) (Xml.child xml_arg0 "SAMAccountName") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?sID ?sAMAccountName ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName = field_map json__ "SAMAccountName" UserName.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?sID ?sAMAccountName ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a new user."]
module CreateUserRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that\226\128\153s associated with the user."];
      emailAddress: EmailAddress.t option
        [@ocaml.doc "The email address of the user."];
      givenName: GivenName.t option
        [@ocaml.doc "The first name of the user."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "An expression that defines one or more attribute names with the data type and value of each attribute. A key is an attribute name, and the value is a list of maps. For a list of supported attributes, see Directory Service Data Attributes. Attribute names are case insensitive."];
      sAMAccountName: UserName.t [@ocaml.doc "The name of the user."];
      surname: Surname.t option [@ocaml.doc "The last name of the user."]}
    let context_ = "CreateUserRequest"
    let make ?clientToken =
      fun ?emailAddress ->
        fun ?givenName ->
          fun ?otherAttributes ->
            fun ?surname ->
              fun ~directoryId ->
                fun ~sAMAccountName ->
                  fun () ->
                    {
                      clientToken;
                      emailAddress;
                      givenName;
                      otherAttributes;
                      surname;
                      directoryId;
                      sAMAccountName
                    }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("EmailAddress",
          (Option.map x.emailAddress ~f:EmailAddress.to_value));
        ("GivenName", (Option.map x.givenName ~f:GivenName.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("SAMAccountName", (Some (UserName.to_value x.sAMAccountName)));
        ("Surname", (Option.map x.surname ~f:Surname.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let surname =
        (Option.map ~f:Surname.of_xml) (Xml.child xml_arg0 "Surname") in
      let sAMAccountName =
        UserName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let givenName =
        (Option.map ~f:GivenName.of_xml) (Xml.child xml_arg0 "GivenName") in
      let emailAddress =
        (Option.map ~f:EmailAddress.of_xml)
          (Xml.child xml_arg0 "EmailAddress") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?surname ~sAMAccountName ?otherAttributes ?givenName ?emailAddress
        ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let surname = field_map json__ "Surname" Surname.of_json in
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" UserName.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let givenName = field_map json__ "GivenName" GivenName.of_json in
      let emailAddress = field_map json__ "EmailAddress" EmailAddress.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?surname ~sAMAccountName ?otherAttributes ?givenName ?emailAddress
        ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a new user."]
module CreateGroupResult =
  struct
    type nonrec t =
      {
      directoryId: DirectoryId.t option
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      sAMAccountName: GroupName.t option
        [@ocaml.doc "The name of the group."];
      sID: SID.t option
        [@ocaml.doc "The unique security identifier (SID) of the group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryId =
      fun ?sAMAccountName ->
        fun ?sID -> fun () -> { directoryId; sAMAccountName; sID }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("SAMAccountName",
          (Option.map x.sAMAccountName ~f:GroupName.to_value));
        ("SID", (Option.map x.sID ~f:SID.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sID = (Option.map ~f:SID.of_xml) (Xml.child xml_arg0 "SID") in
      let sAMAccountName =
        (Option.map ~f:GroupName.of_xml)
          (Xml.child xml_arg0 "SAMAccountName") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      make ?sID ?sAMAccountName ?directoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sID = field_map json__ "SID" SID.of_json in
      let sAMAccountName =
        field_map json__ "SAMAccountName" GroupName.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      make ?sID ?sAMAccountName ?directoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a new group."]
module CreateGroupRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      groupScope: GroupScope.t option
        [@ocaml.doc
          "The scope of the AD group. For details, see Active Directory security group scope."];
      groupType: GroupType.t option
        [@ocaml.doc
          "The AD group type. For details, see Active Directory security group type."];
      otherAttributes: Attributes.t option
        [@ocaml.doc
          "An expression that defines one or more attributes with the data type and value of each attribute."];
      sAMAccountName: GroupName.t [@ocaml.doc "The name of the group."]}
    let context_ = "CreateGroupRequest"
    let make ?clientToken =
      fun ?groupScope ->
        fun ?groupType ->
          fun ?otherAttributes ->
            fun ~directoryId ->
              fun ~sAMAccountName ->
                fun () ->
                  {
                    clientToken;
                    groupScope;
                    groupType;
                    otherAttributes;
                    directoryId;
                    sAMAccountName
                  }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("GroupScope", (Option.map x.groupScope ~f:GroupScope.to_value));
        ("GroupType", (Option.map x.groupType ~f:GroupType.to_value));
        ("OtherAttributes",
          (Option.map x.otherAttributes ~f:Attributes.to_value));
        ("SAMAccountName", (Some (GroupName.to_value x.sAMAccountName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sAMAccountName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SAMAccountName") in
      let otherAttributes =
        (Option.map ~f:Attributes.of_xml)
          (Xml.child xml_arg0 "OtherAttributes") in
      let groupType =
        (Option.map ~f:GroupType.of_xml) (Xml.child xml_arg0 "GroupType") in
      let groupScope =
        (Option.map ~f:GroupScope.of_xml) (Xml.child xml_arg0 "GroupScope") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~sAMAccountName ?otherAttributes ?groupType ?groupScope
        ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sAMAccountName =
        field_map_exn json__ "SAMAccountName" GroupName.of_json in
      let otherAttributes =
        field_map json__ "OtherAttributes" Attributes.of_json in
      let groupType = field_map json__ "GroupType" GroupType.of_json in
      let groupScope = field_map json__ "GroupScope" GroupScope.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ~sAMAccountName ?otherAttributes ?groupType ?groupScope
        ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a new group."]
module AddGroupMemberResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `DirectoryUnavailableException of DirectoryUnavailableException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DirectoryUnavailableException" ->
          `DirectoryUnavailableException
            (DirectoryUnavailableException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DirectoryUnavailableException e ->
          `Assoc
            [("error", (`String "DirectoryUnavailableException"));
            ("details", (DirectoryUnavailableException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds an existing user, group, or computer as a group member."]
module AddGroupMemberRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique and case-sensitive identifier that you provide to make sure the idempotency of the request, so multiple identical calls have the same effect as one single call. A client token is valid for 8 hours after the first request that uses it completes. After 8 hours, any request with the same client token is treated as a new request. If the request succeeds, any future uses of that token will be idempotent for another 8 hours. If you submit a request with the same client token but change one of the other parameters within the 8-hour idempotency window, Directory Service Data returns an ConflictException. This parameter is optional when using the CLI or SDK."];
      directoryId: DirectoryId.t
        [@ocaml.doc
          "The identifier (ID) of the directory that's associated with the group."];
      groupName: GroupName.t [@ocaml.doc "The name of the group."];
      memberName: MemberName.t
        [@ocaml.doc
          "The SAMAccountName of the user, group, or computer to add as a group member."];
      memberRealm: Realm.t option
        [@ocaml.doc
          "The domain name that's associated with the group member. This parameter is required only when adding a member outside of your Managed Microsoft AD domain to a group inside of your Managed Microsoft AD domain. This parameter defaults to the Managed Microsoft AD domain. This parameter is case insensitive."]}
    let context_ = "AddGroupMemberRequest"
    let make ?clientToken =
      fun ?memberRealm ->
        fun ~directoryId ->
          fun ~groupName ->
            fun ~memberName ->
              fun () ->
                {
                  clientToken;
                  memberRealm;
                  directoryId;
                  groupName;
                  memberName
                }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("GroupName", (Some (GroupName.to_value x.groupName)));
        ("MemberName", (Some (MemberName.to_value x.memberName)));
        ("MemberRealm", (Option.map x.memberRealm ~f:Realm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let memberRealm =
        (Option.map ~f:Realm.of_xml) (Xml.child xml_arg0 "MemberRealm") in
      let memberName =
        MemberName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MemberName") in
      let groupName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?memberRealm ~memberName ~groupName ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let memberRealm = field_map json__ "MemberRealm" Realm.of_json in
      let memberName = field_map_exn json__ "MemberName" MemberName.of_json in
      let groupName = field_map_exn json__ "GroupName" GroupName.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?memberRealm ~memberName ~groupName ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds an existing user, group, or computer as a group member."]
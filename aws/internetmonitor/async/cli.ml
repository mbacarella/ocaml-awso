(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resources =
         flag "resources" (optional json_arg) ~doc:"JSON SetOfARNs"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and maxCityNetworksToMonitor =
         flag "max-city-networks-to-monitor" (optional int)
           ~doc:"INT MaxCityNetworksToMonitor"
       and internetMeasurementsLogDelivery =
         flag "internet-measurements-log-delivery" (optional json_arg)
           ~doc:"JSON InternetMeasurementsLogDelivery"
       and trafficPercentageToMonitor =
         flag "traffic-percentage-to-monitor" (optional int)
           ~doc:"INT TrafficPercentageToMonitor"
       and healthEventsConfig =
         flag "health-events-config" (optional json_arg)
           ~doc:"JSON HealthEventsConfig"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_monitor
           (Values.CreateMonitorInput.make
              ?resources:(Option.map ~f:Values.SetOfARNs.of_json resources)
              ?clientToken ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?maxCityNetworksToMonitor
              ?internetMeasurementsLogDelivery:(Option.map
                                                  ~f:Values.InternetMeasurementsLogDelivery.of_json
                                                  internetMeasurementsLogDelivery)
              ?trafficPercentageToMonitor
              ?healthEventsConfig:(Option.map
                                     ~f:Values.HealthEventsConfig.of_json
                                     healthEventsConfig) ~monitorName ())
           (Some Values.CreateMonitorOutput.to_json)
           (Some Values.CreateMonitorOutput.error_to_json)])
let delete_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_monitor (Values.DeleteMonitorInput.make ~monitorName ())
           (Some Values.DeleteMonitorOutput.to_json)
           (Some Values.DeleteMonitorOutput.error_to_json)])
let get_health_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkedAccountId =
         flag "linked-account-id" (optional string) ~doc:"STRING AccountId"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING HealthEventName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_health_event
           (Values.GetHealthEventInput.make ?linkedAccountId ~monitorName
              ~eventId ()) (Some Values.GetHealthEventOutput.to_json)
           (Some Values.GetHealthEventOutput.error_to_json)])
let get_internet_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING InternetEventId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_internet_event
           (Values.GetInternetEventInput.make ~eventId ())
           (Some Values.GetInternetEventOutput.to_json)
           (Some Values.GetInternetEventOutput.error_to_json)])
let get_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkedAccountId =
         flag "linked-account-id" (optional string) ~doc:"STRING AccountId"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_monitor
           (Values.GetMonitorInput.make ?linkedAccountId ~monitorName ())
           (Some Values.GetMonitorOutput.to_json)
           (Some Values.GetMonitorOutput.error_to_json)])
let get_query_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT QueryMaxResults"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_results
           (Values.GetQueryResultsInput.make ?nextToken ?maxResults
              ~monitorName ~queryId ())
           (Some Values.GetQueryResultsOutput.to_json)
           (Some Values.GetQueryResultsOutput.error_to_json)])
let get_query_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_status
           (Values.GetQueryStatusInput.make ~monitorName ~queryId ())
           (Some Values.GetQueryStatusOutput.to_json)
           (Some Values.GetQueryStatusOutput.error_to_json)])
let list_health_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and eventStatus =
         flag "event-status" (optional json_arg)
           ~doc:"JSON HealthEventStatus"
       and linkedAccountId =
         flag "linked-account-id" (optional string) ~doc:"STRING AccountId"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_health_events
           (Values.ListHealthEventsInput.make
              ?startTime:(Option.map
                            ~f:Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ?endTime:(Option.map
                          ~f:Values.SyntheticTimestamp_date_time.of_json
                          endTime) ?nextToken ?maxResults
              ?eventStatus:(Option.map ~f:Values.HealthEventStatus.of_json
                              eventStatus) ?linkedAccountId ~monitorName ())
           (Some Values.ListHealthEventsOutput.to_json)
           (Some Values.ListHealthEventsOutput.error_to_json)])
let list_internet_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT InternetEventMaxResults"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and eventStatus =
         flag "event-status" (optional string) ~doc:"STRING String"
       and eventType =
         flag "event-type" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_internet_events
           (Values.ListInternetEventsInput.make ?nextToken ?maxResults
              ?startTime:(Option.map
                            ~f:Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ?endTime:(Option.map
                          ~f:Values.SyntheticTimestamp_date_time.of_json
                          endTime) ?eventStatus ?eventType ())
           (Some Values.ListInternetEventsOutput.to_json)
           (Some Values.ListInternetEventsOutput.error_to_json)])
let list_monitors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and monitorStatus =
         flag "monitor-status" (optional string) ~doc:"STRING String"
       and includeLinkedAccounts =
         flag "include-linked-accounts" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_monitors
           (Values.ListMonitorsInput.make ?nextToken ?maxResults
              ?monitorStatus ?includeLinkedAccounts ())
           (Some Values.ListMonitorsOutput.to_json)
           (Some Values.ListMonitorsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING MonitorArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let start_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterParameters =
         flag "filter-parameters" (optional json_arg)
           ~doc:"JSON FilterParameters"
       and linkedAccountId =
         flag "linked-account-id" (optional string) ~doc:"STRING AccountId"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and startTime =
         flag "start-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and queryType =
         flag "query-type" (required json_arg) ~doc:"JSON QueryType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query
           (Values.StartQueryInput.make
              ?filterParameters:(Option.map
                                   ~f:Values.FilterParameters.of_json
                                   filterParameters) ?linkedAccountId
              ~monitorName
              ~startTime:(Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ~endTime:(Values.SyntheticTimestamp_date_time.of_json endTime)
              ~queryType:(Values.QueryType.of_json queryType) ())
           (Some Values.StartQueryOutput.to_json)
           (Some Values.StartQueryOutput.error_to_json)])
let stop_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_query
           (Values.StopQueryInput.make ~monitorName ~queryId ())
           (Some Values.StopQueryOutput.to_json)
           (Some Values.StopQueryOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING MonitorArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING MonitorArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourcesToAdd =
         flag "resources-to-add" (optional json_arg) ~doc:"JSON SetOfARNs"
       and resourcesToRemove =
         flag "resources-to-remove" (optional json_arg) ~doc:"JSON SetOfARNs"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON MonitorConfigState"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and maxCityNetworksToMonitor =
         flag "max-city-networks-to-monitor" (optional int)
           ~doc:"INT MaxCityNetworksToMonitor"
       and internetMeasurementsLogDelivery =
         flag "internet-measurements-log-delivery" (optional json_arg)
           ~doc:"JSON InternetMeasurementsLogDelivery"
       and trafficPercentageToMonitor =
         flag "traffic-percentage-to-monitor" (optional int)
           ~doc:"INT TrafficPercentageToMonitor"
       and healthEventsConfig =
         flag "health-events-config" (optional json_arg)
           ~doc:"JSON HealthEventsConfig"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_monitor
           (Values.UpdateMonitorInput.make
              ?resourcesToAdd:(Option.map ~f:Values.SetOfARNs.of_json
                                 resourcesToAdd)
              ?resourcesToRemove:(Option.map ~f:Values.SetOfARNs.of_json
                                    resourcesToRemove)
              ?status:(Option.map ~f:Values.MonitorConfigState.of_json status)
              ?clientToken ?maxCityNetworksToMonitor
              ?internetMeasurementsLogDelivery:(Option.map
                                                  ~f:Values.InternetMeasurementsLogDelivery.of_json
                                                  internetMeasurementsLogDelivery)
              ?trafficPercentageToMonitor
              ?healthEventsConfig:(Option.map
                                     ~f:Values.HealthEventsConfig.of_json
                                     healthEventsConfig) ~monitorName ())
           (Some Values.UpdateMonitorOutput.to_json)
           (Some Values.UpdateMonitorOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-monitor", create_monitor);
    ("delete-monitor", delete_monitor);
    ("get-health-event", get_health_event);
    ("get-internet-event", get_internet_event);
    ("get-monitor", get_monitor);
    ("get-query-results", get_query_results);
    ("get-query-status", get_query_status);
    ("list-health-events", list_health_events);
    ("list-internet-events", list_internet_events);
    ("list-monitors", list_monitors);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-query", start_query);
    ("stop-query", stop_query);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-monitor", update_monitor)]
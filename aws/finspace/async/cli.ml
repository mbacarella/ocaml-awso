(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and federationMode =
         flag "federation-mode" (optional json_arg)
           ~doc:"JSON FederationMode"
       and federationParameters =
         flag "federation-parameters" (optional json_arg)
           ~doc:"JSON FederationParameters"
       and superuserParameters =
         flag "superuser-parameters" (optional json_arg)
           ~doc:"JSON SuperuserParameters"
       and dataBundles =
         flag "data-bundles" (optional json_arg) ~doc:"JSON DataBundleArns"
       and name = flag "name" (required string) ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentRequest.make ?description ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?federationMode:(Option.map ~f:Values.FederationMode.of_json
                                 federationMode)
              ?federationParameters:(Option.map
                                       ~f:Values.FederationParameters.of_json
                                       federationParameters)
              ?superuserParameters:(Option.map
                                      ~f:Values.SuperuserParameters.of_json
                                      superuserParameters)
              ?dataBundles:(Option.map ~f:Values.DataBundleArns.of_json
                              dataBundles) ~name ())
           (Some Values.CreateEnvironmentResponse.to_json)
           (Some Values.CreateEnvironmentResponse.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentRequest.make ~environmentId ())
           (Some Values.DeleteEnvironmentResponse.to_json)
           (Some Values.DeleteEnvironmentResponse.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment
           (Values.GetEnvironmentRequest.make ~environmentId ())
           (Some Values.GetEnvironmentResponse.to_json)
           (Some Values.GetEnvironmentResponse.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListEnvironmentsResponse.to_json)
           (Some Values.ListEnvironmentsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING EnvironmentArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING EnvironmentArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING EnvironmentArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING EnvironmentName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and federationMode =
         flag "federation-mode" (optional json_arg)
           ~doc:"JSON FederationMode"
       and federationParameters =
         flag "federation-parameters" (optional json_arg)
           ~doc:"JSON FederationParameters"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentRequest.make ?name ?description
              ?federationMode:(Option.map ~f:Values.FederationMode.of_json
                                 federationMode)
              ?federationParameters:(Option.map
                                       ~f:Values.FederationParameters.of_json
                                       federationParameters) ~environmentId
              ()) (Some Values.UpdateEnvironmentResponse.to_json)
           (Some Values.UpdateEnvironmentResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-environment", create_environment);
    ("delete-environment", delete_environment);
    ("get-environment", get_environment);
    ("list-environments", list_environments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-environment", update_environment)]
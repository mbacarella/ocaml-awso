(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_external_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and externalConnection =
         flag "external-connection" (required string)
           ~doc:"STRING ExternalConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_external_connection
           (Values.AssociateExternalConnectionRequest.make ?domainOwner
              ~domain ~repository ~externalConnection ())
           (Some Values.AssociateExternalConnectionResult.to_json)
           (Some Values.AssociateExternalConnectionResult.error_to_json)])
let copy_package_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and versions =
         flag "versions" (optional json_arg) ~doc:"JSON PackageVersionList"
       and versionRevisions =
         flag "version-revisions" (optional json_arg)
           ~doc:"JSON PackageVersionRevisionMap"
       and allowOverwrite =
         flag "allow-overwrite" (optional bool) ~doc:"BOOL BooleanOptional"
       and includeFromUpstream =
         flag "include-from-upstream" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and sourceRepository =
         flag "source-repository" (required string)
           ~doc:"STRING RepositoryName"
       and destinationRepository =
         flag "destination-repository" (required string)
           ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_package_versions
           (Values.CopyPackageVersionsRequest.make ?domainOwner ?namespace
              ?versions:(Option.map ~f:Values.PackageVersionList.of_json
                           versions)
              ?versionRevisions:(Option.map
                                   ~f:Values.PackageVersionRevisionMap.of_json
                                   versionRevisions) ?allowOverwrite
              ?includeFromUpstream ~domain ~sourceRepository
              ~destinationRepository
              ~format:(Values.PackageFormat.of_json format) ~package ())
           (Some Values.CopyPackageVersionsResult.to_json)
           (Some Values.CopyPackageVersionsResult.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING Arn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainRequest.make ?encryptionKey
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~domain ())
           (Some Values.CreateDomainResult.to_json)
           (Some Values.CreateDomainResult.error_to_json)])
let create_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and upstreams =
         flag "upstreams" (optional json_arg)
           ~doc:"JSON UpstreamRepositoryList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_repository
           (Values.CreateRepositoryRequest.make ?domainOwner ?description
              ?upstreams:(Option.map ~f:Values.UpstreamRepositoryList.of_json
                            upstreams)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~domain
              ~repository ()) (Some Values.CreateRepositoryResult.to_json)
           (Some Values.CreateRepositoryResult.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain
           (Values.DeleteDomainRequest.make ?domainOwner ~domain ())
           (Some Values.DeleteDomainResult.to_json)
           (Some Values.DeleteDomainResult.error_to_json)])
let delete_domain_permissions_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and policyRevision =
         flag "policy-revision" (optional string)
           ~doc:"STRING PolicyRevision"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_permissions_policy
           (Values.DeleteDomainPermissionsPolicyRequest.make ?domainOwner
              ?policyRevision ~domain ())
           (Some Values.DeleteDomainPermissionsPolicyResult.to_json)
           (Some Values.DeleteDomainPermissionsPolicyResult.error_to_json)])
let delete_package_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and expectedStatus =
         flag "expected-status" (optional json_arg)
           ~doc:"JSON PackageVersionStatus"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and versions =
         flag "versions" (required json_arg) ~doc:"JSON PackageVersionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_package_versions
           (Values.DeletePackageVersionsRequest.make ?domainOwner ?namespace
              ?expectedStatus:(Option.map
                                 ~f:Values.PackageVersionStatus.of_json
                                 expectedStatus) ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~versions:(Values.PackageVersionList.of_json versions) ())
           (Some Values.DeletePackageVersionsResult.to_json)
           (Some Values.DeletePackageVersionsResult.error_to_json)])
let delete_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_repository
           (Values.DeleteRepositoryRequest.make ?domainOwner ~domain
              ~repository ()) (Some Values.DeleteRepositoryResult.to_json)
           (Some Values.DeleteRepositoryResult.error_to_json)])
let delete_repository_permissions_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and policyRevision =
         flag "policy-revision" (optional string)
           ~doc:"STRING PolicyRevision"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_repository_permissions_policy
           (Values.DeleteRepositoryPermissionsPolicyRequest.make ?domainOwner
              ?policyRevision ~domain ~repository ())
           (Some Values.DeleteRepositoryPermissionsPolicyResult.to_json)
           (Some Values.DeleteRepositoryPermissionsPolicyResult.error_to_json)])
let describe_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain
           (Values.DescribeDomainRequest.make ?domainOwner ~domain ())
           (Some Values.DescribeDomainResult.to_json)
           (Some Values.DescribeDomainResult.error_to_json)])
let describe_package_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING PackageVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_package_version
           (Values.DescribePackageVersionRequest.make ?domainOwner ?namespace
              ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~packageVersion ())
           (Some Values.DescribePackageVersionResult.to_json)
           (Some Values.DescribePackageVersionResult.error_to_json)])
let describe_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_repository
           (Values.DescribeRepositoryRequest.make ?domainOwner ~domain
              ~repository ()) (Some Values.DescribeRepositoryResult.to_json)
           (Some Values.DescribeRepositoryResult.error_to_json)])
let disassociate_external_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and externalConnection =
         flag "external-connection" (required string)
           ~doc:"STRING ExternalConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_external_connection
           (Values.DisassociateExternalConnectionRequest.make ?domainOwner
              ~domain ~repository ~externalConnection ())
           (Some Values.DisassociateExternalConnectionResult.to_json)
           (Some Values.DisassociateExternalConnectionResult.error_to_json)])
let dispose_package_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and versionRevisions =
         flag "version-revisions" (optional json_arg)
           ~doc:"JSON PackageVersionRevisionMap"
       and expectedStatus =
         flag "expected-status" (optional json_arg)
           ~doc:"JSON PackageVersionStatus"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and versions =
         flag "versions" (required json_arg) ~doc:"JSON PackageVersionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.dispose_package_versions
           (Values.DisposePackageVersionsRequest.make ?domainOwner ?namespace
              ?versionRevisions:(Option.map
                                   ~f:Values.PackageVersionRevisionMap.of_json
                                   versionRevisions)
              ?expectedStatus:(Option.map
                                 ~f:Values.PackageVersionStatus.of_json
                                 expectedStatus) ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~versions:(Values.PackageVersionList.of_json versions) ())
           (Some Values.DisposePackageVersionsResult.to_json)
           (Some Values.DisposePackageVersionsResult.error_to_json)])
let get_authorization_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and durationSeconds =
         flag "duration-seconds" (optional json_arg)
           ~doc:"JSON AuthorizationTokenDurationSeconds"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_authorization_token
           (Values.GetAuthorizationTokenRequest.make ?domainOwner
              ?durationSeconds:(Option.map
                                  ~f:Values.AuthorizationTokenDurationSeconds.of_json
                                  durationSeconds) ~domain ())
           (Some Values.GetAuthorizationTokenResult.to_json)
           (Some Values.GetAuthorizationTokenResult.error_to_json)])
let get_domain_permissions_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_permissions_policy
           (Values.GetDomainPermissionsPolicyRequest.make ?domainOwner
              ~domain ())
           (Some Values.GetDomainPermissionsPolicyResult.to_json)
           (Some Values.GetDomainPermissionsPolicyResult.error_to_json)])
let get_package_version_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and packageVersionRevision =
         flag "package-version-revision" (optional string)
           ~doc:"STRING PackageVersionRevision"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING PackageVersion"
       and asset = flag "asset" (required string) ~doc:"STRING AssetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_package_version_asset
           (Values.GetPackageVersionAssetRequest.make ?domainOwner ?namespace
              ?packageVersionRevision ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~packageVersion ~asset ())
           (Some Values.GetPackageVersionAssetResult.to_json)
           (Some Values.GetPackageVersionAssetResult.error_to_json)])
let get_package_version_readme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING PackageVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_package_version_readme
           (Values.GetPackageVersionReadmeRequest.make ?domainOwner
              ?namespace ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~packageVersion ())
           (Some Values.GetPackageVersionReadmeResult.to_json)
           (Some Values.GetPackageVersionReadmeResult.error_to_json)])
let get_repository_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository_endpoint
           (Values.GetRepositoryEndpointRequest.make ?domainOwner ~domain
              ~repository ~format:(Values.PackageFormat.of_json format) ())
           (Some Values.GetRepositoryEndpointResult.to_json)
           (Some Values.GetRepositoryEndpointResult.error_to_json)])
let get_repository_permissions_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository_permissions_policy
           (Values.GetRepositoryPermissionsPolicyRequest.make ?domainOwner
              ~domain ~repository ())
           (Some Values.GetRepositoryPermissionsPolicyResult.to_json)
           (Some Values.GetRepositoryPermissionsPolicyResult.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListDomainsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDomainsResult.to_json)
           (Some Values.ListDomainsResult.error_to_json)])
let list_package_version_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPackageVersionAssetsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING PackageVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_package_version_assets
           (Values.ListPackageVersionAssetsRequest.make ?domainOwner
              ?namespace ?maxResults ?nextToken ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~packageVersion ())
           (Some Values.ListPackageVersionAssetsResult.to_json)
           (Some Values.ListPackageVersionAssetsResult.error_to_json)])
let list_package_version_dependencies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING PackageVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_package_version_dependencies
           (Values.ListPackageVersionDependenciesRequest.make ?domainOwner
              ?namespace ?nextToken ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~packageVersion ())
           (Some Values.ListPackageVersionDependenciesResult.to_json)
           (Some Values.ListPackageVersionDependenciesResult.error_to_json)])
let list_package_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON PackageVersionStatus"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON PackageVersionSortType"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPackageVersionsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_package_versions
           (Values.ListPackageVersionsRequest.make ?domainOwner ?namespace
              ?status:(Option.map ~f:Values.PackageVersionStatus.of_json
                         status)
              ?sortBy:(Option.map ~f:Values.PackageVersionSortType.of_json
                         sortBy) ?maxResults ?nextToken ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package ())
           (Some Values.ListPackageVersionsResult.to_json)
           (Some Values.ListPackageVersionsResult.error_to_json)])
let list_packages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and format =
         flag "format" (optional json_arg) ~doc:"JSON PackageFormat"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and packagePrefix =
         flag "package-prefix" (optional string) ~doc:"STRING PackageName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListPackagesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_packages
           (Values.ListPackagesRequest.make ?domainOwner
              ?format:(Option.map ~f:Values.PackageFormat.of_json format)
              ?namespace ?packagePrefix ?maxResults ?nextToken ~domain
              ~repository ()) (Some Values.ListPackagesResult.to_json)
           (Some Values.ListPackagesResult.error_to_json)])
let list_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryPrefix =
         flag "repository-prefix" (optional string)
           ~doc:"STRING RepositoryName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRepositoriesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repositories
           (Values.ListRepositoriesRequest.make ?repositoryPrefix ?maxResults
              ?nextToken ()) (Some Values.ListRepositoriesResult.to_json)
           (Some Values.ListRepositoriesResult.error_to_json)])
let list_repositories_in_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and administratorAccount =
         flag "administrator-account" (optional string)
           ~doc:"STRING AccountId"
       and repositoryPrefix =
         flag "repository-prefix" (optional string)
           ~doc:"STRING RepositoryName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRepositoriesInDomainMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repositories_in_domain
           (Values.ListRepositoriesInDomainRequest.make ?domainOwner
              ?administratorAccount ?repositoryPrefix ?maxResults ?nextToken
              ~domain ())
           (Some Values.ListRepositoriesInDomainResult.to_json)
           (Some Values.ListRepositoriesInDomainResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let put_domain_permissions_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and policyRevision =
         flag "policy-revision" (optional string)
           ~doc:"STRING PolicyRevision"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_domain_permissions_policy
           (Values.PutDomainPermissionsPolicyRequest.make ?domainOwner
              ?policyRevision ~domain ~policyDocument ())
           (Some Values.PutDomainPermissionsPolicyResult.to_json)
           (Some Values.PutDomainPermissionsPolicyResult.error_to_json)])
let put_repository_permissions_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and policyRevision =
         flag "policy-revision" (optional string)
           ~doc:"STRING PolicyRevision"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_repository_permissions_policy
           (Values.PutRepositoryPermissionsPolicyRequest.make ?domainOwner
              ?policyRevision ~domain ~repository ~policyDocument ())
           (Some Values.PutRepositoryPermissionsPolicyResult.to_json)
           (Some Values.PutRepositoryPermissionsPolicyResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_package_versions_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING PackageNamespace"
       and versionRevisions =
         flag "version-revisions" (optional json_arg)
           ~doc:"JSON PackageVersionRevisionMap"
       and expectedStatus =
         flag "expected-status" (optional json_arg)
           ~doc:"JSON PackageVersionStatus"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON PackageFormat"
       and package =
         flag "package" (required string) ~doc:"STRING PackageName"
       and versions =
         flag "versions" (required json_arg) ~doc:"JSON PackageVersionList"
       and targetStatus =
         flag "target-status" (required json_arg)
           ~doc:"JSON PackageVersionStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_package_versions_status
           (Values.UpdatePackageVersionsStatusRequest.make ?domainOwner
              ?namespace
              ?versionRevisions:(Option.map
                                   ~f:Values.PackageVersionRevisionMap.of_json
                                   versionRevisions)
              ?expectedStatus:(Option.map
                                 ~f:Values.PackageVersionStatus.of_json
                                 expectedStatus) ~domain ~repository
              ~format:(Values.PackageFormat.of_json format) ~package
              ~versions:(Values.PackageVersionList.of_json versions)
              ~targetStatus:(Values.PackageVersionStatus.of_json targetStatus)
              ()) (Some Values.UpdatePackageVersionsStatusResult.to_json)
           (Some Values.UpdatePackageVersionsStatusResult.error_to_json)])
let update_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainOwner =
         flag "domain-owner" (optional string) ~doc:"STRING AccountId"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and upstreams =
         flag "upstreams" (optional json_arg)
           ~doc:"JSON UpstreamRepositoryList"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and repository =
         flag "repository" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_repository
           (Values.UpdateRepositoryRequest.make ?domainOwner ?description
              ?upstreams:(Option.map ~f:Values.UpstreamRepositoryList.of_json
                            upstreams) ~domain ~repository ())
           (Some Values.UpdateRepositoryResult.to_json)
           (Some Values.UpdateRepositoryResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-external-connection", associate_external_connection);
    ("copy-package-versions", copy_package_versions);
    ("create-domain", create_domain);
    ("create-repository", create_repository);
    ("delete-domain", delete_domain);
    ("delete-domain-permissions-policy", delete_domain_permissions_policy);
    ("delete-package-versions", delete_package_versions);
    ("delete-repository", delete_repository);
    ("delete-repository-permissions-policy",
      delete_repository_permissions_policy);
    ("describe-domain", describe_domain);
    ("describe-package-version", describe_package_version);
    ("describe-repository", describe_repository);
    ("disassociate-external-connection", disassociate_external_connection);
    ("dispose-package-versions", dispose_package_versions);
    ("get-authorization-token", get_authorization_token);
    ("get-domain-permissions-policy", get_domain_permissions_policy);
    ("get-package-version-asset", get_package_version_asset);
    ("get-package-version-readme", get_package_version_readme);
    ("get-repository-endpoint", get_repository_endpoint);
    ("get-repository-permissions-policy", get_repository_permissions_policy);
    ("list-domains", list_domains);
    ("list-package-version-assets", list_package_version_assets);
    ("list-package-version-dependencies", list_package_version_dependencies);
    ("list-package-versions", list_package_versions);
    ("list-packages", list_packages);
    ("list-repositories", list_repositories);
    ("list-repositories-in-domain", list_repositories_in_domain);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-domain-permissions-policy", put_domain_permissions_policy);
    ("put-repository-permissions-policy", put_repository_permissions_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-package-versions-status", update_package_versions_status);
    ("update-repository", update_repository)]
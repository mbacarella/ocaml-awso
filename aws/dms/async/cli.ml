(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceMessage.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.AddTagsToResourceResponse.to_json)
           (Some Values.AddTagsToResourceResponse.error_to_json)])
let apply_pending_maintenance_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String"
       and applyAction =
         flag "apply-action" (required string) ~doc:"STRING String"
       and optInType =
         flag "opt-in-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_pending_maintenance_action
           (Values.ApplyPendingMaintenanceActionMessage.make
              ~replicationInstanceArn ~applyAction ~optInType ())
           (Some Values.ApplyPendingMaintenanceActionResponse.to_json)
           (Some Values.ApplyPendingMaintenanceActionResponse.error_to_json)])
let cancel_replication_task_assessment_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskAssessmentRunArn =
         flag "replication-task-assessment-run-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_replication_task_assessment_run
           (Values.CancelReplicationTaskAssessmentRunMessage.make
              ~replicationTaskAssessmentRunArn ())
           (Some Values.CancelReplicationTaskAssessmentRunResponse.to_json)
           (Some
              Values.CancelReplicationTaskAssessmentRunResponse.error_to_json)])
let create_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username = flag "username" (optional string) ~doc:"STRING String"
       and password =
         flag "password" (optional string) ~doc:"STRING SecretString"
       and serverName =
         flag "server-name" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING String"
       and extraConnectionAttributes =
         flag "extra-connection-attributes" (optional string)
           ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING String"
       and sslMode =
         flag "ssl-mode" (optional json_arg) ~doc:"JSON DmsSslModeValue"
       and serviceAccessRoleArn =
         flag "service-access-role-arn" (optional string)
           ~doc:"STRING String"
       and externalTableDefinition =
         flag "external-table-definition" (optional string)
           ~doc:"STRING String"
       and dynamoDbSettings =
         flag "dynamo-db-settings" (optional json_arg)
           ~doc:"JSON DynamoDbSettings"
       and s3Settings =
         flag "s3-settings" (optional json_arg) ~doc:"JSON S3Settings"
       and dmsTransferSettings =
         flag "dms-transfer-settings" (optional json_arg)
           ~doc:"JSON DmsTransferSettings"
       and mongoDbSettings =
         flag "mongo-db-settings" (optional json_arg)
           ~doc:"JSON MongoDbSettings"
       and kinesisSettings =
         flag "kinesis-settings" (optional json_arg)
           ~doc:"JSON KinesisSettings"
       and kafkaSettings =
         flag "kafka-settings" (optional json_arg) ~doc:"JSON KafkaSettings"
       and elasticsearchSettings =
         flag "elasticsearch-settings" (optional json_arg)
           ~doc:"JSON ElasticsearchSettings"
       and neptuneSettings =
         flag "neptune-settings" (optional json_arg)
           ~doc:"JSON NeptuneSettings"
       and redshiftSettings =
         flag "redshift-settings" (optional json_arg)
           ~doc:"JSON RedshiftSettings"
       and postgreSQLSettings =
         flag "postgre-s-q-l-settings" (optional json_arg)
           ~doc:"JSON PostgreSQLSettings"
       and mySQLSettings =
         flag "my-s-q-l-settings" (optional json_arg)
           ~doc:"JSON MySQLSettings"
       and oracleSettings =
         flag "oracle-settings" (optional json_arg)
           ~doc:"JSON OracleSettings"
       and sybaseSettings =
         flag "sybase-settings" (optional json_arg)
           ~doc:"JSON SybaseSettings"
       and microsoftSQLServerSettings =
         flag "microsoft-s-q-l-server-settings" (optional json_arg)
           ~doc:"JSON MicrosoftSQLServerSettings"
       and iBMDb2Settings =
         flag "i-b-m-db2-settings" (optional json_arg)
           ~doc:"JSON IBMDb2Settings"
       and resourceIdentifier =
         flag "resource-identifier" (optional string) ~doc:"STRING String"
       and docDbSettings =
         flag "doc-db-settings" (optional json_arg) ~doc:"JSON DocDbSettings"
       and redisSettings =
         flag "redis-settings" (optional json_arg) ~doc:"JSON RedisSettings"
       and gcpMySQLSettings =
         flag "gcp-my-s-q-l-settings" (optional json_arg)
           ~doc:"JSON GcpMySQLSettings"
       and endpointIdentifier =
         flag "endpoint-identifier" (required string) ~doc:"STRING String"
       and endpointType =
         flag "endpoint-type" (required json_arg)
           ~doc:"JSON ReplicationEndpointTypeValue"
       and engineName =
         flag "engine-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint
           (Values.CreateEndpointMessage.make ?username ?password ?serverName
              ?port ?databaseName ?extraConnectionAttributes ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?certificateArn
              ?sslMode:(Option.map ~f:Values.DmsSslModeValue.of_json sslMode)
              ?serviceAccessRoleArn ?externalTableDefinition
              ?dynamoDbSettings:(Option.map
                                   ~f:Values.DynamoDbSettings.of_json
                                   dynamoDbSettings)
              ?s3Settings:(Option.map ~f:Values.S3Settings.of_json s3Settings)
              ?dmsTransferSettings:(Option.map
                                      ~f:Values.DmsTransferSettings.of_json
                                      dmsTransferSettings)
              ?mongoDbSettings:(Option.map ~f:Values.MongoDbSettings.of_json
                                  mongoDbSettings)
              ?kinesisSettings:(Option.map ~f:Values.KinesisSettings.of_json
                                  kinesisSettings)
              ?kafkaSettings:(Option.map ~f:Values.KafkaSettings.of_json
                                kafkaSettings)
              ?elasticsearchSettings:(Option.map
                                        ~f:Values.ElasticsearchSettings.of_json
                                        elasticsearchSettings)
              ?neptuneSettings:(Option.map ~f:Values.NeptuneSettings.of_json
                                  neptuneSettings)
              ?redshiftSettings:(Option.map
                                   ~f:Values.RedshiftSettings.of_json
                                   redshiftSettings)
              ?postgreSQLSettings:(Option.map
                                     ~f:Values.PostgreSQLSettings.of_json
                                     postgreSQLSettings)
              ?mySQLSettings:(Option.map ~f:Values.MySQLSettings.of_json
                                mySQLSettings)
              ?oracleSettings:(Option.map ~f:Values.OracleSettings.of_json
                                 oracleSettings)
              ?sybaseSettings:(Option.map ~f:Values.SybaseSettings.of_json
                                 sybaseSettings)
              ?microsoftSQLServerSettings:(Option.map
                                             ~f:Values.MicrosoftSQLServerSettings.of_json
                                             microsoftSQLServerSettings)
              ?iBMDb2Settings:(Option.map ~f:Values.IBMDb2Settings.of_json
                                 iBMDb2Settings) ?resourceIdentifier
              ?docDbSettings:(Option.map ~f:Values.DocDbSettings.of_json
                                docDbSettings)
              ?redisSettings:(Option.map ~f:Values.RedisSettings.of_json
                                redisSettings)
              ?gcpMySQLSettings:(Option.map
                                   ~f:Values.GcpMySQLSettings.of_json
                                   gcpMySQLSettings) ~endpointIdentifier
              ~endpointType:(Values.ReplicationEndpointTypeValue.of_json
                               endpointType) ~engineName ())
           (Some Values.CreateEndpointResponse.to_json)
           (Some Values.CreateEndpointResponse.error_to_json)])
let create_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and sourceIds =
         flag "source-ids" (optional json_arg) ~doc:"JSON SourceIdsList"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String"
       and snsTopicArn =
         flag "sns-topic-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_subscription
           (Values.CreateEventSubscriptionMessage.make ?sourceType
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories)
              ?sourceIds:(Option.map ~f:Values.SourceIdsList.of_json
                            sourceIds) ?enabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~subscriptionName ~snsTopicArn ())
           (Some Values.CreateEventSubscriptionResponse.to_json)
           (Some Values.CreateEventSubscriptionResponse.error_to_json)])
let create_replication_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and replicationSubnetGroupIdentifier =
         flag "replication-subnet-group-identifier" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dnsNameServers =
         flag "dns-name-servers" (optional string) ~doc:"STRING String"
       and resourceIdentifier =
         flag "resource-identifier" (optional string) ~doc:"STRING String"
       and replicationInstanceIdentifier =
         flag "replication-instance-identifier" (required string)
           ~doc:"STRING String"
       and replicationInstanceClass =
         flag "replication-instance-class" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_instance
           (Values.CreateReplicationInstanceMessage.make ?allocatedStorage
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?availabilityZone
              ?replicationSubnetGroupIdentifier ?preferredMaintenanceWindow
              ?multiAZ ?engineVersion ?autoMinorVersionUpgrade
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?kmsKeyId
              ?publiclyAccessible ?dnsNameServers ?resourceIdentifier
              ~replicationInstanceIdentifier ~replicationInstanceClass ())
           (Some Values.CreateReplicationInstanceResponse.to_json)
           (Some Values.CreateReplicationInstanceResponse.error_to_json)])
let create_replication_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and replicationSubnetGroupIdentifier =
         flag "replication-subnet-group-identifier" (required string)
           ~doc:"STRING String"
       and replicationSubnetGroupDescription =
         flag "replication-subnet-group-description" (required string)
           ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_subnet_group
           (Values.CreateReplicationSubnetGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~replicationSubnetGroupIdentifier
              ~replicationSubnetGroupDescription
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.CreateReplicationSubnetGroupResponse.to_json)
           (Some Values.CreateReplicationSubnetGroupResponse.error_to_json)])
let create_replication_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskSettings =
         flag "replication-task-settings" (optional string)
           ~doc:"STRING String"
       and cdcStartTime =
         flag "cdc-start-time" (optional json_arg) ~doc:"JSON TStamp"
       and cdcStartPosition =
         flag "cdc-start-position" (optional string) ~doc:"STRING String"
       and cdcStopPosition =
         flag "cdc-stop-position" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and taskData = flag "task-data" (optional string) ~doc:"STRING String"
       and resourceIdentifier =
         flag "resource-identifier" (optional string) ~doc:"STRING String"
       and replicationTaskIdentifier =
         flag "replication-task-identifier" (required string)
           ~doc:"STRING String"
       and sourceEndpointArn =
         flag "source-endpoint-arn" (required string) ~doc:"STRING String"
       and targetEndpointArn =
         flag "target-endpoint-arn" (required string) ~doc:"STRING String"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String"
       and migrationType =
         flag "migration-type" (required json_arg)
           ~doc:"JSON MigrationTypeValue"
       and tableMappings =
         flag "table-mappings" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_task
           (Values.CreateReplicationTaskMessage.make ?replicationTaskSettings
              ?cdcStartTime:(Option.map ~f:Values.TStamp.of_json cdcStartTime)
              ?cdcStartPosition ?cdcStopPosition
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?taskData
              ?resourceIdentifier ~replicationTaskIdentifier
              ~sourceEndpointArn ~targetEndpointArn ~replicationInstanceArn
              ~migrationType:(Values.MigrationTypeValue.of_json migrationType)
              ~tableMappings ())
           (Some Values.CreateReplicationTaskResponse.to_json)
           (Some Values.CreateReplicationTaskResponse.error_to_json)])
let delete_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_certificate
           (Values.DeleteCertificateMessage.make ~certificateArn ())
           (Some Values.DeleteCertificateResponse.to_json)
           (Some Values.DeleteCertificateResponse.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionMessage.make ~endpointArn
              ~replicationInstanceArn ())
           (Some Values.DeleteConnectionResponse.to_json)
           (Some Values.DeleteConnectionResponse.error_to_json)])
let delete_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint
           (Values.DeleteEndpointMessage.make ~endpointArn ())
           (Some Values.DeleteEndpointResponse.to_json)
           (Some Values.DeleteEndpointResponse.error_to_json)])
let delete_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_subscription
           (Values.DeleteEventSubscriptionMessage.make ~subscriptionName ())
           (Some Values.DeleteEventSubscriptionResponse.to_json)
           (Some Values.DeleteEventSubscriptionResponse.error_to_json)])
let delete_replication_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_instance
           (Values.DeleteReplicationInstanceMessage.make
              ~replicationInstanceArn ())
           (Some Values.DeleteReplicationInstanceResponse.to_json)
           (Some Values.DeleteReplicationInstanceResponse.error_to_json)])
let delete_replication_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationSubnetGroupIdentifier =
         flag "replication-subnet-group-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_subnet_group
           (Values.DeleteReplicationSubnetGroupMessage.make
              ~replicationSubnetGroupIdentifier ())
           (Some Values.DeleteReplicationSubnetGroupResponse.to_json)
           (Some Values.DeleteReplicationSubnetGroupResponse.error_to_json)])
let delete_replication_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_task
           (Values.DeleteReplicationTaskMessage.make ~replicationTaskArn ())
           (Some Values.DeleteReplicationTaskResponse.to_json)
           (Some Values.DeleteReplicationTaskResponse.error_to_json)])
let delete_replication_task_assessment_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskAssessmentRunArn =
         flag "replication-task-assessment-run-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_task_assessment_run
           (Values.DeleteReplicationTaskAssessmentRunMessage.make
              ~replicationTaskAssessmentRunArn ())
           (Some Values.DeleteReplicationTaskAssessmentRunResponse.to_json)
           (Some
              Values.DeleteReplicationTaskAssessmentRunResponse.error_to_json)])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes
           (Values.DescribeAccountAttributesMessage.make ())
           (Some Values.DescribeAccountAttributesResponse.to_json)
           (Some Values.DescribeAccountAttributesResponse.error_to_json)])
let describe_applicable_individual_assessments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskArn =
         flag "replication-task-arn" (optional string) ~doc:"STRING String"
       and replicationInstanceArn =
         flag "replication-instance-arn" (optional string)
           ~doc:"STRING String"
       and sourceEngineName =
         flag "source-engine-name" (optional string) ~doc:"STRING String"
       and targetEngineName =
         flag "target-engine-name" (optional string) ~doc:"STRING String"
       and migrationType =
         flag "migration-type" (optional json_arg)
           ~doc:"JSON MigrationTypeValue"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_applicable_individual_assessments
           (Values.DescribeApplicableIndividualAssessmentsMessage.make
              ?replicationTaskArn ?replicationInstanceArn ?sourceEngineName
              ?targetEngineName
              ?migrationType:(Option.map ~f:Values.MigrationTypeValue.of_json
                                migrationType) ?maxRecords ?marker ())
           (Some
              Values.DescribeApplicableIndividualAssessmentsResponse.to_json)
           (Some
              Values.DescribeApplicableIndividualAssessmentsResponse.error_to_json)])
let describe_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificates
           (Values.DescribeCertificatesMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeCertificatesResponse.to_json)
           (Some Values.DescribeCertificatesResponse.error_to_json)])
let describe_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connections
           (Values.DescribeConnectionsMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeConnectionsResponse.to_json)
           (Some Values.DescribeConnectionsResponse.error_to_json)])
let describe_endpoint_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and engineName =
         flag "engine-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint_settings
           (Values.DescribeEndpointSettingsMessage.make ?maxRecords ?marker
              ~engineName ())
           (Some Values.DescribeEndpointSettingsResponse.to_json)
           (Some Values.DescribeEndpointSettingsResponse.error_to_json)])
let describe_endpoint_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint_types
           (Values.DescribeEndpointTypesMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeEndpointTypesResponse.to_json)
           (Some Values.DescribeEndpointTypesResponse.error_to_json)])
let describe_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoints
           (Values.DescribeEndpointsMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeEndpointsResponse.to_json)
           (Some Values.DescribeEndpointsResponse.error_to_json)])
let describe_event_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_categories
           (Values.DescribeEventCategoriesMessage.make ?sourceType
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeEventCategoriesResponse.to_json)
           (Some Values.DescribeEventCategoriesResponse.error_to_json)])
let describe_event_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_subscriptions
           (Values.DescribeEventSubscriptionsMessage.make ?subscriptionName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeEventSubscriptionsResponse.to_json)
           (Some Values.DescribeEventSubscriptionsResponse.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceIdentifier =
         flag "source-identifier" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional json_arg) ~doc:"JSON SourceType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and duration =
         flag "duration" (optional int) ~doc:"INT IntegerOptional"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsMessage.make ?sourceIdentifier
              ?sourceType:(Option.map ~f:Values.SourceType.of_json sourceType)
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime)
              ?duration
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeEventsResponse.to_json)
           (Some Values.DescribeEventsResponse.error_to_json)])
let describe_orderable_replication_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_orderable_replication_instances
           (Values.DescribeOrderableReplicationInstancesMessage.make
              ?maxRecords ?marker ())
           (Some Values.DescribeOrderableReplicationInstancesResponse.to_json)
           (Some
              Values.DescribeOrderableReplicationInstancesResponse.error_to_json)])
let describe_pending_maintenance_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationInstanceArn =
         flag "replication-instance-arn" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pending_maintenance_actions
           (Values.DescribePendingMaintenanceActionsMessage.make
              ?replicationInstanceArn
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ())
           (Some Values.DescribePendingMaintenanceActionsResponse.to_json)
           (Some
              Values.DescribePendingMaintenanceActionsResponse.error_to_json)])
let describe_refresh_schemas_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_refresh_schemas_status
           (Values.DescribeRefreshSchemasStatusMessage.make ~endpointArn ())
           (Some Values.DescribeRefreshSchemasStatusResponse.to_json)
           (Some Values.DescribeRefreshSchemasStatusResponse.error_to_json)])
let describe_replication_instance_task_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_instance_task_logs
           (Values.DescribeReplicationInstanceTaskLogsMessage.make
              ?maxRecords ?marker ~replicationInstanceArn ())
           (Some Values.DescribeReplicationInstanceTaskLogsResponse.to_json)
           (Some
              Values.DescribeReplicationInstanceTaskLogsResponse.error_to_json)])
let describe_replication_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_instances
           (Values.DescribeReplicationInstancesMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeReplicationInstancesResponse.to_json)
           (Some Values.DescribeReplicationInstancesResponse.error_to_json)])
let describe_replication_subnet_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_subnet_groups
           (Values.DescribeReplicationSubnetGroupsMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeReplicationSubnetGroupsResponse.to_json)
           (Some Values.DescribeReplicationSubnetGroupsResponse.error_to_json)])
let describe_replication_task_assessment_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskArn =
         flag "replication-task-arn" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_task_assessment_results
           (Values.DescribeReplicationTaskAssessmentResultsMessage.make
              ?replicationTaskArn ?maxRecords ?marker ())
           (Some
              Values.DescribeReplicationTaskAssessmentResultsResponse.to_json)
           (Some
              Values.DescribeReplicationTaskAssessmentResultsResponse.error_to_json)])
let describe_replication_task_assessment_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_task_assessment_runs
           (Values.DescribeReplicationTaskAssessmentRunsMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeReplicationTaskAssessmentRunsResponse.to_json)
           (Some
              Values.DescribeReplicationTaskAssessmentRunsResponse.error_to_json)])
let describe_replication_task_individual_assessments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_task_individual_assessments
           (Values.DescribeReplicationTaskIndividualAssessmentsMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some
              Values.DescribeReplicationTaskIndividualAssessmentsResponse.to_json)
           (Some
              Values.DescribeReplicationTaskIndividualAssessmentsResponse.error_to_json)])
let describe_replication_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and withoutSettings =
         flag "without-settings" (optional bool) ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_tasks
           (Values.DescribeReplicationTasksMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ?withoutSettings ())
           (Some Values.DescribeReplicationTasksResponse.to_json)
           (Some Values.DescribeReplicationTasksResponse.error_to_json)])
let describe_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_schemas
           (Values.DescribeSchemasMessage.make ?maxRecords ?marker
              ~endpointArn ()) (Some Values.DescribeSchemasResponse.to_json)
           (Some Values.DescribeSchemasResponse.error_to_json)])
let describe_table_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_table_statistics
           (Values.DescribeTableStatisticsMessage.make ?maxRecords ?marker
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~replicationTaskArn ())
           (Some Values.DescribeTableStatisticsResponse.to_json)
           (Some Values.DescribeTableStatisticsResponse.error_to_json)])
let import_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificatePem =
         flag "certificate-pem" (optional string) ~doc:"STRING SecretString"
       and certificateWallet =
         flag "certificate-wallet" (optional json_arg)
           ~doc:"JSON CertificateWallet"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and certificateIdentifier =
         flag "certificate-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_certificate
           (Values.ImportCertificateMessage.make ?certificatePem
              ?certificateWallet:(Option.map
                                    ~f:Values.CertificateWallet.of_json
                                    certificateWallet)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~certificateIdentifier ())
           (Some Values.ImportCertificateResponse.to_json)
           (Some Values.ImportCertificateResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING String"
       and resourceArnList =
         flag "resource-arn-list" (optional json_arg) ~doc:"JSON ArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceMessage.make ?resourceArn
              ?resourceArnList:(Option.map ~f:Values.ArnList.of_json
                                  resourceArnList) ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let modify_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointIdentifier =
         flag "endpoint-identifier" (optional string) ~doc:"STRING String"
       and endpointType =
         flag "endpoint-type" (optional json_arg)
           ~doc:"JSON ReplicationEndpointTypeValue"
       and engineName =
         flag "engine-name" (optional string) ~doc:"STRING String"
       and username = flag "username" (optional string) ~doc:"STRING String"
       and password =
         flag "password" (optional string) ~doc:"STRING SecretString"
       and serverName =
         flag "server-name" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING String"
       and extraConnectionAttributes =
         flag "extra-connection-attributes" (optional string)
           ~doc:"STRING String"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING String"
       and sslMode =
         flag "ssl-mode" (optional json_arg) ~doc:"JSON DmsSslModeValue"
       and serviceAccessRoleArn =
         flag "service-access-role-arn" (optional string)
           ~doc:"STRING String"
       and externalTableDefinition =
         flag "external-table-definition" (optional string)
           ~doc:"STRING String"
       and dynamoDbSettings =
         flag "dynamo-db-settings" (optional json_arg)
           ~doc:"JSON DynamoDbSettings"
       and s3Settings =
         flag "s3-settings" (optional json_arg) ~doc:"JSON S3Settings"
       and dmsTransferSettings =
         flag "dms-transfer-settings" (optional json_arg)
           ~doc:"JSON DmsTransferSettings"
       and mongoDbSettings =
         flag "mongo-db-settings" (optional json_arg)
           ~doc:"JSON MongoDbSettings"
       and kinesisSettings =
         flag "kinesis-settings" (optional json_arg)
           ~doc:"JSON KinesisSettings"
       and kafkaSettings =
         flag "kafka-settings" (optional json_arg) ~doc:"JSON KafkaSettings"
       and elasticsearchSettings =
         flag "elasticsearch-settings" (optional json_arg)
           ~doc:"JSON ElasticsearchSettings"
       and neptuneSettings =
         flag "neptune-settings" (optional json_arg)
           ~doc:"JSON NeptuneSettings"
       and redshiftSettings =
         flag "redshift-settings" (optional json_arg)
           ~doc:"JSON RedshiftSettings"
       and postgreSQLSettings =
         flag "postgre-s-q-l-settings" (optional json_arg)
           ~doc:"JSON PostgreSQLSettings"
       and mySQLSettings =
         flag "my-s-q-l-settings" (optional json_arg)
           ~doc:"JSON MySQLSettings"
       and oracleSettings =
         flag "oracle-settings" (optional json_arg)
           ~doc:"JSON OracleSettings"
       and sybaseSettings =
         flag "sybase-settings" (optional json_arg)
           ~doc:"JSON SybaseSettings"
       and microsoftSQLServerSettings =
         flag "microsoft-s-q-l-server-settings" (optional json_arg)
           ~doc:"JSON MicrosoftSQLServerSettings"
       and iBMDb2Settings =
         flag "i-b-m-db2-settings" (optional json_arg)
           ~doc:"JSON IBMDb2Settings"
       and docDbSettings =
         flag "doc-db-settings" (optional json_arg) ~doc:"JSON DocDbSettings"
       and redisSettings =
         flag "redis-settings" (optional json_arg) ~doc:"JSON RedisSettings"
       and exactSettings =
         flag "exact-settings" (optional bool) ~doc:"BOOL BooleanOptional"
       and gcpMySQLSettings =
         flag "gcp-my-s-q-l-settings" (optional json_arg)
           ~doc:"JSON GcpMySQLSettings"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_endpoint
           (Values.ModifyEndpointMessage.make ?endpointIdentifier
              ?endpointType:(Option.map
                               ~f:Values.ReplicationEndpointTypeValue.of_json
                               endpointType) ?engineName ?username ?password
              ?serverName ?port ?databaseName ?extraConnectionAttributes
              ?certificateArn
              ?sslMode:(Option.map ~f:Values.DmsSslModeValue.of_json sslMode)
              ?serviceAccessRoleArn ?externalTableDefinition
              ?dynamoDbSettings:(Option.map
                                   ~f:Values.DynamoDbSettings.of_json
                                   dynamoDbSettings)
              ?s3Settings:(Option.map ~f:Values.S3Settings.of_json s3Settings)
              ?dmsTransferSettings:(Option.map
                                      ~f:Values.DmsTransferSettings.of_json
                                      dmsTransferSettings)
              ?mongoDbSettings:(Option.map ~f:Values.MongoDbSettings.of_json
                                  mongoDbSettings)
              ?kinesisSettings:(Option.map ~f:Values.KinesisSettings.of_json
                                  kinesisSettings)
              ?kafkaSettings:(Option.map ~f:Values.KafkaSettings.of_json
                                kafkaSettings)
              ?elasticsearchSettings:(Option.map
                                        ~f:Values.ElasticsearchSettings.of_json
                                        elasticsearchSettings)
              ?neptuneSettings:(Option.map ~f:Values.NeptuneSettings.of_json
                                  neptuneSettings)
              ?redshiftSettings:(Option.map
                                   ~f:Values.RedshiftSettings.of_json
                                   redshiftSettings)
              ?postgreSQLSettings:(Option.map
                                     ~f:Values.PostgreSQLSettings.of_json
                                     postgreSQLSettings)
              ?mySQLSettings:(Option.map ~f:Values.MySQLSettings.of_json
                                mySQLSettings)
              ?oracleSettings:(Option.map ~f:Values.OracleSettings.of_json
                                 oracleSettings)
              ?sybaseSettings:(Option.map ~f:Values.SybaseSettings.of_json
                                 sybaseSettings)
              ?microsoftSQLServerSettings:(Option.map
                                             ~f:Values.MicrosoftSQLServerSettings.of_json
                                             microsoftSQLServerSettings)
              ?iBMDb2Settings:(Option.map ~f:Values.IBMDb2Settings.of_json
                                 iBMDb2Settings)
              ?docDbSettings:(Option.map ~f:Values.DocDbSettings.of_json
                                docDbSettings)
              ?redisSettings:(Option.map ~f:Values.RedisSettings.of_json
                                redisSettings) ?exactSettings
              ?gcpMySQLSettings:(Option.map
                                   ~f:Values.GcpMySQLSettings.of_json
                                   gcpMySQLSettings) ~endpointArn ())
           (Some Values.ModifyEndpointResponse.to_json)
           (Some Values.ModifyEndpointResponse.error_to_json)])
let modify_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snsTopicArn =
         flag "sns-topic-arn" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_event_subscription
           (Values.ModifyEventSubscriptionMessage.make ?snsTopicArn
              ?sourceType
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories) ?enabled ~subscriptionName
              ()) (Some Values.ModifyEventSubscriptionResponse.to_json)
           (Some Values.ModifyEventSubscriptionResponse.error_to_json)])
let modify_replication_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL Boolean"
       and replicationInstanceClass =
         flag "replication-instance-class" (optional string)
           ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and allowMajorVersionUpgrade =
         flag "allow-major-version-upgrade" (optional bool)
           ~doc:"BOOL Boolean"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and replicationInstanceIdentifier =
         flag "replication-instance-identifier" (optional string)
           ~doc:"STRING String"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_replication_instance
           (Values.ModifyReplicationInstanceMessage.make ?allocatedStorage
              ?applyImmediately ?replicationInstanceClass
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds)
              ?preferredMaintenanceWindow ?multiAZ ?engineVersion
              ?allowMajorVersionUpgrade ?autoMinorVersionUpgrade
              ?replicationInstanceIdentifier ~replicationInstanceArn ())
           (Some Values.ModifyReplicationInstanceResponse.to_json)
           (Some Values.ModifyReplicationInstanceResponse.error_to_json)])
let modify_replication_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationSubnetGroupDescription =
         flag "replication-subnet-group-description" (optional string)
           ~doc:"STRING String"
       and replicationSubnetGroupIdentifier =
         flag "replication-subnet-group-identifier" (required string)
           ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_replication_subnet_group
           (Values.ModifyReplicationSubnetGroupMessage.make
              ?replicationSubnetGroupDescription
              ~replicationSubnetGroupIdentifier
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.ModifyReplicationSubnetGroupResponse.to_json)
           (Some Values.ModifyReplicationSubnetGroupResponse.error_to_json)])
let modify_replication_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskIdentifier =
         flag "replication-task-identifier" (optional string)
           ~doc:"STRING String"
       and migrationType =
         flag "migration-type" (optional json_arg)
           ~doc:"JSON MigrationTypeValue"
       and tableMappings =
         flag "table-mappings" (optional string) ~doc:"STRING String"
       and replicationTaskSettings =
         flag "replication-task-settings" (optional string)
           ~doc:"STRING String"
       and cdcStartTime =
         flag "cdc-start-time" (optional json_arg) ~doc:"JSON TStamp"
       and cdcStartPosition =
         flag "cdc-start-position" (optional string) ~doc:"STRING String"
       and cdcStopPosition =
         flag "cdc-stop-position" (optional string) ~doc:"STRING String"
       and taskData = flag "task-data" (optional string) ~doc:"STRING String"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_replication_task
           (Values.ModifyReplicationTaskMessage.make
              ?replicationTaskIdentifier
              ?migrationType:(Option.map ~f:Values.MigrationTypeValue.of_json
                                migrationType) ?tableMappings
              ?replicationTaskSettings
              ?cdcStartTime:(Option.map ~f:Values.TStamp.of_json cdcStartTime)
              ?cdcStartPosition ?cdcStopPosition ?taskData
              ~replicationTaskArn ())
           (Some Values.ModifyReplicationTaskResponse.to_json)
           (Some Values.ModifyReplicationTaskResponse.error_to_json)])
let move_replication_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String"
       and targetReplicationInstanceArn =
         flag "target-replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.move_replication_task
           (Values.MoveReplicationTaskMessage.make ~replicationTaskArn
              ~targetReplicationInstanceArn ())
           (Some Values.MoveReplicationTaskResponse.to_json)
           (Some Values.MoveReplicationTaskResponse.error_to_json)])
let reboot_replication_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceFailover =
         flag "force-failover" (optional bool) ~doc:"BOOL BooleanOptional"
       and forcePlannedFailover =
         flag "force-planned-failover" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_replication_instance
           (Values.RebootReplicationInstanceMessage.make ?forceFailover
              ?forcePlannedFailover ~replicationInstanceArn ())
           (Some Values.RebootReplicationInstanceResponse.to_json)
           (Some Values.RebootReplicationInstanceResponse.error_to_json)])
let refresh_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.refresh_schemas
           (Values.RefreshSchemasMessage.make ~endpointArn
              ~replicationInstanceArn ())
           (Some Values.RefreshSchemasResponse.to_json)
           (Some Values.RefreshSchemasResponse.error_to_json)])
let reload_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reloadOption =
         flag "reload-option" (optional json_arg)
           ~doc:"JSON ReloadOptionValue"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String"
       and tablesToReload =
         flag "tables-to-reload" (required json_arg)
           ~doc:"JSON TableListToReload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reload_tables
           (Values.ReloadTablesMessage.make
              ?reloadOption:(Option.map ~f:Values.ReloadOptionValue.of_json
                               reloadOption) ~replicationTaskArn
              ~tablesToReload:(Values.TableListToReload.of_json
                                 tablesToReload) ())
           (Some Values.ReloadTablesResponse.to_json)
           (Some Values.ReloadTablesResponse.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON KeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceMessage.make ~resourceArn
              ~tagKeys:(Values.KeyList.of_json tagKeys) ())
           (Some Values.RemoveTagsFromResourceResponse.to_json)
           (Some Values.RemoveTagsFromResourceResponse.error_to_json)])
let start_replication_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cdcStartTime =
         flag "cdc-start-time" (optional json_arg) ~doc:"JSON TStamp"
       and cdcStartPosition =
         flag "cdc-start-position" (optional string) ~doc:"STRING String"
       and cdcStopPosition =
         flag "cdc-stop-position" (optional string) ~doc:"STRING String"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String"
       and startReplicationTaskType =
         flag "start-replication-task-type" (required json_arg)
           ~doc:"JSON StartReplicationTaskTypeValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_replication_task
           (Values.StartReplicationTaskMessage.make
              ?cdcStartTime:(Option.map ~f:Values.TStamp.of_json cdcStartTime)
              ?cdcStartPosition ?cdcStopPosition ~replicationTaskArn
              ~startReplicationTaskType:(Values.StartReplicationTaskTypeValue.of_json
                                           startReplicationTaskType) ())
           (Some Values.StartReplicationTaskResponse.to_json)
           (Some Values.StartReplicationTaskResponse.error_to_json)])
let start_replication_task_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_replication_task_assessment
           (Values.StartReplicationTaskAssessmentMessage.make
              ~replicationTaskArn ())
           (Some Values.StartReplicationTaskAssessmentResponse.to_json)
           (Some Values.StartReplicationTaskAssessmentResponse.error_to_json)])
let start_replication_task_assessment_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resultLocationFolder =
         flag "result-location-folder" (optional string) ~doc:"STRING String"
       and resultEncryptionMode =
         flag "result-encryption-mode" (optional string) ~doc:"STRING String"
       and resultKmsKeyArn =
         flag "result-kms-key-arn" (optional string) ~doc:"STRING String"
       and includeOnly =
         flag "include-only" (optional json_arg) ~doc:"JSON IncludeTestList"
       and exclude =
         flag "exclude" (optional json_arg) ~doc:"JSON ExcludeTestList"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String"
       and serviceAccessRoleArn =
         flag "service-access-role-arn" (required string)
           ~doc:"STRING String"
       and resultLocationBucket =
         flag "result-location-bucket" (required string) ~doc:"STRING String"
       and assessmentRunName =
         flag "assessment-run-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_replication_task_assessment_run
           (Values.StartReplicationTaskAssessmentRunMessage.make
              ?resultLocationFolder ?resultEncryptionMode ?resultKmsKeyArn
              ?includeOnly:(Option.map ~f:Values.IncludeTestList.of_json
                              includeOnly)
              ?exclude:(Option.map ~f:Values.ExcludeTestList.of_json exclude)
              ~replicationTaskArn ~serviceAccessRoleArn ~resultLocationBucket
              ~assessmentRunName ())
           (Some Values.StartReplicationTaskAssessmentRunResponse.to_json)
           (Some
              Values.StartReplicationTaskAssessmentRunResponse.error_to_json)])
let stop_replication_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationTaskArn =
         flag "replication-task-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_replication_task
           (Values.StopReplicationTaskMessage.make ~replicationTaskArn ())
           (Some Values.StopReplicationTaskResponse.to_json)
           (Some Values.StopReplicationTaskResponse.error_to_json)])
let test_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationInstanceArn =
         flag "replication-instance-arn" (required string)
           ~doc:"STRING String"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_connection
           (Values.TestConnectionMessage.make ~replicationInstanceArn
              ~endpointArn ()) (Some Values.TestConnectionResponse.to_json)
           (Some Values.TestConnectionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-tags-to-resource", add_tags_to_resource);
    ("apply-pending-maintenance-action", apply_pending_maintenance_action);
    ("cancel-replication-task-assessment-run",
      cancel_replication_task_assessment_run);
    ("create-endpoint", create_endpoint);
    ("create-event-subscription", create_event_subscription);
    ("create-replication-instance", create_replication_instance);
    ("create-replication-subnet-group", create_replication_subnet_group);
    ("create-replication-task", create_replication_task);
    ("delete-certificate", delete_certificate);
    ("delete-connection", delete_connection);
    ("delete-endpoint", delete_endpoint);
    ("delete-event-subscription", delete_event_subscription);
    ("delete-replication-instance", delete_replication_instance);
    ("delete-replication-subnet-group", delete_replication_subnet_group);
    ("delete-replication-task", delete_replication_task);
    ("delete-replication-task-assessment-run",
      delete_replication_task_assessment_run);
    ("describe-account-attributes", describe_account_attributes);
    ("describe-applicable-individual-assessments",
      describe_applicable_individual_assessments);
    ("describe-certificates", describe_certificates);
    ("describe-connections", describe_connections);
    ("describe-endpoint-settings", describe_endpoint_settings);
    ("describe-endpoint-types", describe_endpoint_types);
    ("describe-endpoints", describe_endpoints);
    ("describe-event-categories", describe_event_categories);
    ("describe-event-subscriptions", describe_event_subscriptions);
    ("describe-events", describe_events);
    ("describe-orderable-replication-instances",
      describe_orderable_replication_instances);
    ("describe-pending-maintenance-actions",
      describe_pending_maintenance_actions);
    ("describe-refresh-schemas-status", describe_refresh_schemas_status);
    ("describe-replication-instance-task-logs",
      describe_replication_instance_task_logs);
    ("describe-replication-instances", describe_replication_instances);
    ("describe-replication-subnet-groups",
      describe_replication_subnet_groups);
    ("describe-replication-task-assessment-results",
      describe_replication_task_assessment_results);
    ("describe-replication-task-assessment-runs",
      describe_replication_task_assessment_runs);
    ("describe-replication-task-individual-assessments",
      describe_replication_task_individual_assessments);
    ("describe-replication-tasks", describe_replication_tasks);
    ("describe-schemas", describe_schemas);
    ("describe-table-statistics", describe_table_statistics);
    ("import-certificate", import_certificate);
    ("list-tags-for-resource", list_tags_for_resource);
    ("modify-endpoint", modify_endpoint);
    ("modify-event-subscription", modify_event_subscription);
    ("modify-replication-instance", modify_replication_instance);
    ("modify-replication-subnet-group", modify_replication_subnet_group);
    ("modify-replication-task", modify_replication_task);
    ("move-replication-task", move_replication_task);
    ("reboot-replication-instance", reboot_replication_instance);
    ("refresh-schemas", refresh_schemas);
    ("reload-tables", reload_tables);
    ("remove-tags-from-resource", remove_tags_from_resource);
    ("start-replication-task", start_replication_task);
    ("start-replication-task-assessment", start_replication_task_assessment);
    ("start-replication-task-assessment-run",
      start_replication_task_assessment_run);
    ("stop-replication-task", stop_replication_task);
    ("test-connection", test_connection)]
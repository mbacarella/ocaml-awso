(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_l_f_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and resource =
         flag "resource" (required json_arg) ~doc:"JSON Resource"
       and lFTags =
         flag "l-f-tags" (required json_arg) ~doc:"JSON LFTagsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_l_f_tags_to_resource
           (Values.AddLFTagsToResourceRequest.make ?catalogId
              ~resource:(Values.Resource.of_json resource)
              ~lFTags:(Values.LFTagsList.of_json lFTags) ())
           (Some Values.AddLFTagsToResourceResponse.to_json)
           (Some Values.AddLFTagsToResourceResponse.error_to_json)])
let batch_grant_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON BatchPermissionsRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_grant_permissions
           (Values.BatchGrantPermissionsRequest.make ?catalogId
              ~entries:(Values.BatchPermissionsRequestEntryList.of_json
                          entries) ())
           (Some Values.BatchGrantPermissionsResponse.to_json)
           (Some Values.BatchGrantPermissionsResponse.error_to_json)])
let batch_revoke_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON BatchPermissionsRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_revoke_permissions
           (Values.BatchRevokePermissionsRequest.make ?catalogId
              ~entries:(Values.BatchPermissionsRequestEntryList.of_json
                          entries) ())
           (Some Values.BatchRevokePermissionsResponse.to_json)
           (Some Values.BatchRevokePermissionsResponse.error_to_json)])
let cancel_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionId =
         flag "transaction-id" (required string)
           ~doc:"STRING TransactionIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_transaction
           (Values.CancelTransactionRequest.make ~transactionId ())
           (Some Values.CancelTransactionResponse.to_json)
           (Some Values.CancelTransactionResponse.error_to_json)])
let commit_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionId =
         flag "transaction-id" (required string)
           ~doc:"STRING TransactionIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.commit_transaction
           (Values.CommitTransactionRequest.make ~transactionId ())
           (Some Values.CommitTransactionResponse.to_json)
           (Some Values.CommitTransactionResponse.error_to_json)])
let create_data_cells_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableData =
         flag "table-data" (required json_arg) ~doc:"JSON DataCellsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_cells_filter
           (Values.CreateDataCellsFilterRequest.make
              ~tableData:(Values.DataCellsFilter.of_json tableData) ())
           (Some Values.CreateDataCellsFilterResponse.to_json)
           (Some Values.CreateDataCellsFilterResponse.error_to_json)])
let create_l_f_tag =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and tagKey = flag "tag-key" (required string) ~doc:"STRING LFTagKey"
       and tagValues =
         flag "tag-values" (required json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_l_f_tag
           (Values.CreateLFTagRequest.make ?catalogId ~tagKey
              ~tagValues:(Values.TagValueList.of_json tagValues) ())
           (Some Values.CreateLFTagResponse.to_json)
           (Some Values.CreateLFTagResponse.error_to_json)])
let delete_data_cells_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableCatalogId =
         flag "table-catalog-id" (optional string)
           ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (optional string) ~doc:"STRING NameString"
       and name = flag "name" (optional string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_cells_filter
           (Values.DeleteDataCellsFilterRequest.make ?tableCatalogId
              ?databaseName ?tableName ?name ())
           (Some Values.DeleteDataCellsFilterResponse.to_json)
           (Some Values.DeleteDataCellsFilterResponse.error_to_json)])
let delete_l_f_tag =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and tagKey = flag "tag-key" (required string) ~doc:"STRING LFTagKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_l_f_tag
           (Values.DeleteLFTagRequest.make ?catalogId ~tagKey ())
           (Some Values.DeleteLFTagResponse.to_json)
           (Some Values.DeleteLFTagResponse.error_to_json)])
let delete_objects_on_cancel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and transactionId =
         flag "transaction-id" (required string)
           ~doc:"STRING TransactionIdString"
       and objects =
         flag "objects" (required json_arg) ~doc:"JSON VirtualObjectList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_objects_on_cancel
           (Values.DeleteObjectsOnCancelRequest.make ?catalogId ~databaseName
              ~tableName ~transactionId
              ~objects:(Values.VirtualObjectList.of_json objects) ())
           (Some Values.DeleteObjectsOnCancelResponse.to_json)
           (Some Values.DeleteObjectsOnCancelResponse.error_to_json)])
let deregister_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_resource
           (Values.DeregisterResourceRequest.make ~resourceArn ())
           (Some Values.DeregisterResourceResponse.to_json)
           (Some Values.DeregisterResourceResponse.error_to_json)])
let describe_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource
           (Values.DescribeResourceRequest.make ~resourceArn ())
           (Some Values.DescribeResourceResponse.to_json)
           (Some Values.DescribeResourceResponse.error_to_json)])
let describe_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionId =
         flag "transaction-id" (required string)
           ~doc:"STRING TransactionIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_transaction
           (Values.DescribeTransactionRequest.make ~transactionId ())
           (Some Values.DescribeTransactionResponse.to_json)
           (Some Values.DescribeTransactionResponse.error_to_json)])
let extend_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.extend_transaction
           (Values.ExtendTransactionRequest.make ?transactionId ())
           (Some Values.ExtendTransactionResponse.to_json)
           (Some Values.ExtendTransactionResponse.error_to_json)])
let get_data_lake_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_lake_settings
           (Values.GetDataLakeSettingsRequest.make ?catalogId ())
           (Some Values.GetDataLakeSettingsResponse.to_json)
           (Some Values.GetDataLakeSettingsResponse.error_to_json)])
let get_effective_permissions_for_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_effective_permissions_for_path
           (Values.GetEffectivePermissionsForPathRequest.make ?catalogId
              ?nextToken ?maxResults ~resourceArn ())
           (Some Values.GetEffectivePermissionsForPathResponse.to_json)
           (Some Values.GetEffectivePermissionsForPathResponse.error_to_json)])
let get_l_f_tag =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and tagKey = flag "tag-key" (required string) ~doc:"STRING LFTagKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_l_f_tag (Values.GetLFTagRequest.make ?catalogId ~tagKey ())
           (Some Values.GetLFTagResponse.to_json)
           (Some Values.GetLFTagResponse.error_to_json)])
let get_query_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryId =
         flag "query-id" (required string)
           ~doc:"STRING GetQueryStateRequestQueryIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_state (Values.GetQueryStateRequest.make ~queryId ())
           (Some Values.GetQueryStateResponse.to_json)
           (Some Values.GetQueryStateResponse.error_to_json)])
let get_query_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryId =
         flag "query-id" (required string)
           ~doc:"STRING GetQueryStatisticsRequestQueryIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_statistics
           (Values.GetQueryStatisticsRequest.make ~queryId ())
           (Some Values.GetQueryStatisticsResponse.to_json)
           (Some Values.GetQueryStatisticsResponse.error_to_json)])
let get_resource_l_f_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and showAssignedLFTags =
         flag "show-assigned-l-f-tags" (optional bool)
           ~doc:"BOOL BooleanNullable"
       and resource =
         flag "resource" (required json_arg) ~doc:"JSON Resource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_l_f_tags
           (Values.GetResourceLFTagsRequest.make ?catalogId
              ?showAssignedLFTags
              ~resource:(Values.Resource.of_json resource) ())
           (Some Values.GetResourceLFTagsResponse.to_json)
           (Some Values.GetResourceLFTagsResponse.error_to_json)])
let get_table_objects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and queryAsOfTime =
         flag "query-as-of-time" (optional json_arg) ~doc:"JSON Timestamp"
       and partitionPredicate =
         flag "partition-predicate" (optional string)
           ~doc:"STRING PredicateString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING TokenString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_objects
           (Values.GetTableObjectsRequest.make ?catalogId ?transactionId
              ?queryAsOfTime:(Option.map ~f:Values.Timestamp.of_json
                                queryAsOfTime) ?partitionPredicate
              ?maxResults ?nextToken ~databaseName ~tableName ())
           (Some Values.GetTableObjectsResponse.to_json)
           (Some Values.GetTableObjectsResponse.error_to_json)])
let get_temporary_glue_partition_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and permissions =
         flag "permissions" (optional json_arg) ~doc:"JSON PermissionList"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT CredentialTimeoutDurationSecondInteger"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING ResourceArnString"
       and partition =
         flag "partition" (required json_arg) ~doc:"JSON PartitionValueList"
       and supportedPermissionTypes =
         flag "supported-permission-types" (required json_arg)
           ~doc:"JSON PermissionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_temporary_glue_partition_credentials
           (Values.GetTemporaryGluePartitionCredentialsRequest.make
              ?permissions:(Option.map ~f:Values.PermissionList.of_json
                              permissions) ?durationSeconds
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ~tableArn
              ~partition:(Values.PartitionValueList.of_json partition)
              ~supportedPermissionTypes:(Values.PermissionTypeList.of_json
                                           supportedPermissionTypes) ())
           (Some Values.GetTemporaryGluePartitionCredentialsResponse.to_json)
           (Some
              Values.GetTemporaryGluePartitionCredentialsResponse.error_to_json)])
let get_temporary_glue_table_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and permissions =
         flag "permissions" (optional json_arg) ~doc:"JSON PermissionList"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT CredentialTimeoutDurationSecondInteger"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING ResourceArnString"
       and supportedPermissionTypes =
         flag "supported-permission-types" (required json_arg)
           ~doc:"JSON PermissionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_temporary_glue_table_credentials
           (Values.GetTemporaryGlueTableCredentialsRequest.make
              ?permissions:(Option.map ~f:Values.PermissionList.of_json
                              permissions) ?durationSeconds
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ~tableArn
              ~supportedPermissionTypes:(Values.PermissionTypeList.of_json
                                           supportedPermissionTypes) ())
           (Some Values.GetTemporaryGlueTableCredentialsResponse.to_json)
           (Some
              Values.GetTemporaryGlueTableCredentialsResponse.error_to_json)])
let get_work_unit_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryId =
         flag "query-id" (required string)
           ~doc:"STRING GetWorkUnitResultsRequestQueryIdString"
       and workUnitId =
         flag "work-unit-id" (required json_arg)
           ~doc:"JSON GetWorkUnitResultsRequestWorkUnitIdLong"
       and workUnitToken =
         flag "work-unit-token" (required string)
           ~doc:"STRING SyntheticGetWorkUnitResultsRequestWorkUnitTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_work_unit_results
           (Values.GetWorkUnitResultsRequest.make ~queryId
              ~workUnitId:(Values.GetWorkUnitResultsRequestWorkUnitIdLong.of_json
                             workUnitId) ~workUnitToken ())
           (Some Values.GetWorkUnitResultsResponse.to_json)
           (Some Values.GetWorkUnitResultsResponse.error_to_json)])
let get_work_units =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer"
       and queryId =
         flag "query-id" (required string)
           ~doc:"STRING GetWorkUnitsRequestQueryIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_work_units
           (Values.GetWorkUnitsRequest.make ?nextToken ?pageSize ~queryId ())
           (Some Values.GetWorkUnitsResponse.to_json)
           (Some Values.GetWorkUnitsResponse.error_to_json)])
let grant_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and permissionsWithGrantOption =
         flag "permissions-with-grant-option" (optional json_arg)
           ~doc:"JSON PermissionList"
       and principal =
         flag "principal" (required json_arg) ~doc:"JSON DataLakePrincipal"
       and resource =
         flag "resource" (required json_arg) ~doc:"JSON Resource"
       and permissions =
         flag "permissions" (required json_arg) ~doc:"JSON PermissionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.grant_permissions
           (Values.GrantPermissionsRequest.make ?catalogId
              ?permissionsWithGrantOption:(Option.map
                                             ~f:Values.PermissionList.of_json
                                             permissionsWithGrantOption)
              ~principal:(Values.DataLakePrincipal.of_json principal)
              ~resource:(Values.Resource.of_json resource)
              ~permissions:(Values.PermissionList.of_json permissions) ())
           (Some Values.GrantPermissionsResponse.to_json)
           (Some Values.GrantPermissionsResponse.error_to_json)])
let list_data_cells_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and table = flag "table" (optional json_arg) ~doc:"JSON TableResource"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_cells_filter
           (Values.ListDataCellsFilterRequest.make
              ?table:(Option.map ~f:Values.TableResource.of_json table)
              ?nextToken ?maxResults ())
           (Some Values.ListDataCellsFilterResponse.to_json)
           (Some Values.ListDataCellsFilterResponse.error_to_json)])
let list_l_f_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and resourceShareType =
         flag "resource-share-type" (optional json_arg)
           ~doc:"JSON ResourceShareType"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_l_f_tags
           (Values.ListLFTagsRequest.make ?catalogId
              ?resourceShareType:(Option.map
                                    ~f:Values.ResourceShareType.of_json
                                    resourceShareType) ?maxResults ?nextToken
              ()) (Some Values.ListLFTagsResponse.to_json)
           (Some Values.ListLFTagsResponse.error_to_json)])
let list_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and principal =
         flag "principal" (optional json_arg) ~doc:"JSON DataLakePrincipal"
       and resourceType =
         flag "resource-type" (optional json_arg)
           ~doc:"JSON DataLakeResourceType"
       and resource =
         flag "resource" (optional json_arg) ~doc:"JSON Resource"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and includeRelated =
         flag "include-related" (optional string)
           ~doc:"STRING TrueFalseString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permissions
           (Values.ListPermissionsRequest.make ?catalogId
              ?principal:(Option.map ~f:Values.DataLakePrincipal.of_json
                            principal)
              ?resourceType:(Option.map
                               ~f:Values.DataLakeResourceType.of_json
                               resourceType)
              ?resource:(Option.map ~f:Values.Resource.of_json resource)
              ?nextToken ?maxResults ?includeRelated ())
           (Some Values.ListPermissionsResponse.to_json)
           (Some Values.ListPermissionsResponse.error_to_json)])
let list_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterConditionList =
         flag "filter-condition-list" (optional json_arg)
           ~doc:"JSON FilterConditionList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources
           (Values.ListResourcesRequest.make
              ?filterConditionList:(Option.map
                                      ~f:Values.FilterConditionList.of_json
                                      filterConditionList) ?maxResults
              ?nextToken ()) (Some Values.ListResourcesResponse.to_json)
           (Some Values.ListResourcesResponse.error_to_json)])
let list_table_storage_optimizers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and storageOptimizerType =
         flag "storage-optimizer-type" (optional json_arg)
           ~doc:"JSON OptimizerType"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_storage_optimizers
           (Values.ListTableStorageOptimizersRequest.make ?catalogId
              ?storageOptimizerType:(Option.map
                                       ~f:Values.OptimizerType.of_json
                                       storageOptimizerType) ?maxResults
              ?nextToken ~databaseName ~tableName ())
           (Some Values.ListTableStorageOptimizersResponse.to_json)
           (Some Values.ListTableStorageOptimizersResponse.error_to_json)])
let list_transactions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and statusFilter =
         flag "status-filter" (optional json_arg)
           ~doc:"JSON TransactionStatusFilter"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING TokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_transactions
           (Values.ListTransactionsRequest.make ?catalogId
              ?statusFilter:(Option.map
                               ~f:Values.TransactionStatusFilter.of_json
                               statusFilter) ?maxResults ?nextToken ())
           (Some Values.ListTransactionsResponse.to_json)
           (Some Values.ListTransactionsResponse.error_to_json)])
let put_data_lake_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and dataLakeSettings =
         flag "data-lake-settings" (required json_arg)
           ~doc:"JSON DataLakeSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_data_lake_settings
           (Values.PutDataLakeSettingsRequest.make ?catalogId
              ~dataLakeSettings:(Values.DataLakeSettings.of_json
                                   dataLakeSettings) ())
           (Some Values.PutDataLakeSettingsResponse.to_json)
           (Some Values.PutDataLakeSettingsResponse.error_to_json)])
let register_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and useServiceLinkedRole =
         flag "use-service-linked-role" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IAMRoleArn"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_resource
           (Values.RegisterResourceRequest.make ?useServiceLinkedRole
              ?roleArn ~resourceArn ())
           (Some Values.RegisterResourceResponse.to_json)
           (Some Values.RegisterResourceResponse.error_to_json)])
let remove_l_f_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and resource =
         flag "resource" (required json_arg) ~doc:"JSON Resource"
       and lFTags =
         flag "l-f-tags" (required json_arg) ~doc:"JSON LFTagsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_l_f_tags_from_resource
           (Values.RemoveLFTagsFromResourceRequest.make ?catalogId
              ~resource:(Values.Resource.of_json resource)
              ~lFTags:(Values.LFTagsList.of_json lFTags) ())
           (Some Values.RemoveLFTagsFromResourceResponse.to_json)
           (Some Values.RemoveLFTagsFromResourceResponse.error_to_json)])
let revoke_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and permissionsWithGrantOption =
         flag "permissions-with-grant-option" (optional json_arg)
           ~doc:"JSON PermissionList"
       and principal =
         flag "principal" (required json_arg) ~doc:"JSON DataLakePrincipal"
       and resource =
         flag "resource" (required json_arg) ~doc:"JSON Resource"
       and permissions =
         flag "permissions" (required json_arg) ~doc:"JSON PermissionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_permissions
           (Values.RevokePermissionsRequest.make ?catalogId
              ?permissionsWithGrantOption:(Option.map
                                             ~f:Values.PermissionList.of_json
                                             permissionsWithGrantOption)
              ~principal:(Values.DataLakePrincipal.of_json principal)
              ~resource:(Values.Resource.of_json resource)
              ~permissions:(Values.PermissionList.of_json permissions) ())
           (Some Values.RevokePermissionsResponse.to_json)
           (Some Values.RevokePermissionsResponse.error_to_json)])
let search_databases_by_l_f_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and expression =
         flag "expression" (required json_arg) ~doc:"JSON Expression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_databases_by_l_f_tags
           (Values.SearchDatabasesByLFTagsRequest.make ?nextToken ?maxResults
              ?catalogId ~expression:(Values.Expression.of_json expression)
              ()) (Some Values.SearchDatabasesByLFTagsResponse.to_json)
           (Some Values.SearchDatabasesByLFTagsResponse.error_to_json)])
let search_tables_by_l_f_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and expression =
         flag "expression" (required json_arg) ~doc:"JSON Expression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_tables_by_l_f_tags
           (Values.SearchTablesByLFTagsRequest.make ?nextToken ?maxResults
              ?catalogId ~expression:(Values.Expression.of_json expression)
              ()) (Some Values.SearchTablesByLFTagsResponse.to_json)
           (Some Values.SearchTablesByLFTagsResponse.error_to_json)])
let start_query_planning =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryPlanningContext =
         flag "query-planning-context" (required json_arg)
           ~doc:"JSON QueryPlanningContext"
       and queryString =
         flag "query-string" (required string)
           ~doc:"STRING SyntheticStartQueryPlanningRequestQueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query_planning
           (Values.StartQueryPlanningRequest.make
              ~queryPlanningContext:(Values.QueryPlanningContext.of_json
                                       queryPlanningContext) ~queryString ())
           (Some Values.StartQueryPlanningResponse.to_json)
           (Some Values.StartQueryPlanningResponse.error_to_json)])
let start_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionType =
         flag "transaction-type" (optional json_arg)
           ~doc:"JSON TransactionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_transaction
           (Values.StartTransactionRequest.make
              ?transactionType:(Option.map ~f:Values.TransactionType.of_json
                                  transactionType) ())
           (Some Values.StartTransactionResponse.to_json)
           (Some Values.StartTransactionResponse.error_to_json)])
let update_l_f_tag =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and tagValuesToDelete =
         flag "tag-values-to-delete" (optional json_arg)
           ~doc:"JSON TagValueList"
       and tagValuesToAdd =
         flag "tag-values-to-add" (optional json_arg)
           ~doc:"JSON TagValueList"
       and tagKey = flag "tag-key" (required string) ~doc:"STRING LFTagKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_l_f_tag
           (Values.UpdateLFTagRequest.make ?catalogId
              ?tagValuesToDelete:(Option.map ~f:Values.TagValueList.of_json
                                    tagValuesToDelete)
              ?tagValuesToAdd:(Option.map ~f:Values.TagValueList.of_json
                                 tagValuesToAdd) ~tagKey ())
           (Some Values.UpdateLFTagResponse.to_json)
           (Some Values.UpdateLFTagResponse.error_to_json)])
let update_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IAMRoleArn"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource
           (Values.UpdateResourceRequest.make ~roleArn ~resourceArn ())
           (Some Values.UpdateResourceResponse.to_json)
           (Some Values.UpdateResourceResponse.error_to_json)])
let update_table_objects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and writeOperations =
         flag "write-operations" (required json_arg)
           ~doc:"JSON WriteOperationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table_objects
           (Values.UpdateTableObjectsRequest.make ?catalogId ?transactionId
              ~databaseName ~tableName
              ~writeOperations:(Values.WriteOperationList.of_json
                                  writeOperations) ())
           (Some Values.UpdateTableObjectsResponse.to_json)
           (Some Values.UpdateTableObjectsResponse.error_to_json)])
let update_table_storage_optimizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and storageOptimizerConfig =
         flag "storage-optimizer-config" (required json_arg)
           ~doc:"JSON StorageOptimizerConfigMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table_storage_optimizer
           (Values.UpdateTableStorageOptimizerRequest.make ?catalogId
              ~databaseName ~tableName
              ~storageOptimizerConfig:(Values.StorageOptimizerConfigMap.of_json
                                         storageOptimizerConfig) ())
           (Some Values.UpdateTableStorageOptimizerResponse.to_json)
           (Some Values.UpdateTableStorageOptimizerResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-l-f-tags-to-resource", add_l_f_tags_to_resource);
    ("batch-grant-permissions", batch_grant_permissions);
    ("batch-revoke-permissions", batch_revoke_permissions);
    ("cancel-transaction", cancel_transaction);
    ("commit-transaction", commit_transaction);
    ("create-data-cells-filter", create_data_cells_filter);
    ("create-l-f-tag", create_l_f_tag);
    ("delete-data-cells-filter", delete_data_cells_filter);
    ("delete-l-f-tag", delete_l_f_tag);
    ("delete-objects-on-cancel", delete_objects_on_cancel);
    ("deregister-resource", deregister_resource);
    ("describe-resource", describe_resource);
    ("describe-transaction", describe_transaction);
    ("extend-transaction", extend_transaction);
    ("get-data-lake-settings", get_data_lake_settings);
    ("get-effective-permissions-for-path",
      get_effective_permissions_for_path);
    ("get-l-f-tag", get_l_f_tag);
    ("get-query-state", get_query_state);
    ("get-query-statistics", get_query_statistics);
    ("get-resource-l-f-tags", get_resource_l_f_tags);
    ("get-table-objects", get_table_objects);
    ("get-temporary-glue-partition-credentials",
      get_temporary_glue_partition_credentials);
    ("get-temporary-glue-table-credentials",
      get_temporary_glue_table_credentials);
    ("get-work-unit-results", get_work_unit_results);
    ("get-work-units", get_work_units);
    ("grant-permissions", grant_permissions);
    ("list-data-cells-filter", list_data_cells_filter);
    ("list-l-f-tags", list_l_f_tags);
    ("list-permissions", list_permissions);
    ("list-resources", list_resources);
    ("list-table-storage-optimizers", list_table_storage_optimizers);
    ("list-transactions", list_transactions);
    ("put-data-lake-settings", put_data_lake_settings);
    ("register-resource", register_resource);
    ("remove-l-f-tags-from-resource", remove_l_f_tags_from_resource);
    ("revoke-permissions", revoke_permissions);
    ("search-databases-by-l-f-tags", search_databases_by_l_f_tags);
    ("search-tables-by-l-f-tags", search_tables_by_l_f_tags);
    ("start-query-planning", start_query_planning);
    ("start-transaction", start_transaction);
    ("update-l-f-tag", update_l_f_tag);
    ("update-resource", update_resource);
    ("update-table-objects", update_table_objects);
    ("update-table-storage-optimizer", update_table_storage_optimizer)]
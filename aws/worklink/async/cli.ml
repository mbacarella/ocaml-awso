(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and acmCertificateArn =
         flag "acm-certificate-arn" (required string)
           ~doc:"STRING AcmCertificateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_domain
           (Values.AssociateDomainRequest.make ?displayName ~fleetArn
              ~domainName ~acmCertificateArn ())
           (Some Values.AssociateDomainResponse.to_json)
           (Some Values.AssociateDomainResponse.error_to_json)])
let associate_website_authorization_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and authorizationProviderType =
         flag "authorization-provider-type" (required json_arg)
           ~doc:"JSON AuthorizationProviderType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_website_authorization_provider
           (Values.AssociateWebsiteAuthorizationProviderRequest.make
              ?domainName ~fleetArn
              ~authorizationProviderType:(Values.AuthorizationProviderType.of_json
                                            authorizationProviderType) ())
           (Some Values.AssociateWebsiteAuthorizationProviderResponse.to_json)
           (Some
              Values.AssociateWebsiteAuthorizationProviderResponse.error_to_json)])
let associate_website_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and certificate =
         flag "certificate" (required string) ~doc:"STRING Certificate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_website_certificate_authority
           (Values.AssociateWebsiteCertificateAuthorityRequest.make
              ?displayName ~fleetArn ~certificate ())
           (Some Values.AssociateWebsiteCertificateAuthorityResponse.to_json)
           (Some
              Values.AssociateWebsiteCertificateAuthorityResponse.error_to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and optimizeForEndUserLocation =
         flag "optimize-for-end-user-location" (optional bool)
           ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and fleetName =
         flag "fleet-name" (required string) ~doc:"STRING FleetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetRequest.make ?displayName
              ?optimizeForEndUserLocation
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~fleetName ())
           (Some Values.CreateFleetResponse.to_json)
           (Some Values.CreateFleetResponse.error_to_json)])
let delete_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet (Values.DeleteFleetRequest.make ~fleetArn ())
           (Some Values.DeleteFleetResponse.to_json)
           (Some Values.DeleteFleetResponse.error_to_json)])
let describe_audit_stream_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_audit_stream_configuration
           (Values.DescribeAuditStreamConfigurationRequest.make ~fleetArn ())
           (Some Values.DescribeAuditStreamConfigurationResponse.to_json)
           (Some
              Values.DescribeAuditStreamConfigurationResponse.error_to_json)])
let describe_company_network_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_company_network_configuration
           (Values.DescribeCompanyNetworkConfigurationRequest.make ~fleetArn
              ())
           (Some Values.DescribeCompanyNetworkConfigurationResponse.to_json)
           (Some
              Values.DescribeCompanyNetworkConfigurationResponse.error_to_json)])
let describe_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and deviceId = flag "device-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device
           (Values.DescribeDeviceRequest.make ~fleetArn ~deviceId ())
           (Some Values.DescribeDeviceResponse.to_json)
           (Some Values.DescribeDeviceResponse.error_to_json)])
let describe_device_policy_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device_policy_configuration
           (Values.DescribeDevicePolicyConfigurationRequest.make ~fleetArn ())
           (Some Values.DescribeDevicePolicyConfigurationResponse.to_json)
           (Some
              Values.DescribeDevicePolicyConfigurationResponse.error_to_json)])
let describe_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain
           (Values.DescribeDomainRequest.make ~fleetArn ~domainName ())
           (Some Values.DescribeDomainResponse.to_json)
           (Some Values.DescribeDomainResponse.error_to_json)])
let describe_fleet_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_metadata
           (Values.DescribeFleetMetadataRequest.make ~fleetArn ())
           (Some Values.DescribeFleetMetadataResponse.to_json)
           (Some Values.DescribeFleetMetadataResponse.error_to_json)])
let describe_identity_provider_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_provider_configuration
           (Values.DescribeIdentityProviderConfigurationRequest.make
              ~fleetArn ())
           (Some Values.DescribeIdentityProviderConfigurationResponse.to_json)
           (Some
              Values.DescribeIdentityProviderConfigurationResponse.error_to_json)])
let describe_website_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and websiteCaId =
         flag "website-ca-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_website_certificate_authority
           (Values.DescribeWebsiteCertificateAuthorityRequest.make ~fleetArn
              ~websiteCaId ())
           (Some Values.DescribeWebsiteCertificateAuthorityResponse.to_json)
           (Some
              Values.DescribeWebsiteCertificateAuthorityResponse.error_to_json)])
let disassociate_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_domain
           (Values.DisassociateDomainRequest.make ~fleetArn ~domainName ())
           (Some Values.DisassociateDomainResponse.to_json)
           (Some Values.DisassociateDomainResponse.error_to_json)])
let disassociate_website_authorization_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and authorizationProviderId =
         flag "authorization-provider-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_website_authorization_provider
           (Values.DisassociateWebsiteAuthorizationProviderRequest.make
              ~fleetArn ~authorizationProviderId ())
           (Some
              Values.DisassociateWebsiteAuthorizationProviderResponse.to_json)
           (Some
              Values.DisassociateWebsiteAuthorizationProviderResponse.error_to_json)])
let disassociate_website_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and websiteCaId =
         flag "website-ca-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_website_certificate_authority
           (Values.DisassociateWebsiteCertificateAuthorityRequest.make
              ~fleetArn ~websiteCaId ())
           (Some
              Values.DisassociateWebsiteCertificateAuthorityResponse.to_json)
           (Some
              Values.DisassociateWebsiteCertificateAuthorityResponse.error_to_json)])
let list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices
           (Values.ListDevicesRequest.make ?nextToken ?maxResults ~fleetArn
              ()) (Some Values.ListDevicesResponse.to_json)
           (Some Values.ListDevicesResponse.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?nextToken ?maxResults ~fleetArn
              ()) (Some Values.ListDomainsResponse.to_json)
           (Some Values.ListDomainsResponse.error_to_json)])
let list_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleets
           (Values.ListFleetsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListFleetsResponse.to_json)
           (Some Values.ListFleetsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_website_authorization_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_website_authorization_providers
           (Values.ListWebsiteAuthorizationProvidersRequest.make ?nextToken
              ?maxResults ~fleetArn ())
           (Some Values.ListWebsiteAuthorizationProvidersResponse.to_json)
           (Some
              Values.ListWebsiteAuthorizationProvidersResponse.error_to_json)])
let list_website_certificate_authorities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_website_certificate_authorities
           (Values.ListWebsiteCertificateAuthoritiesRequest.make ?maxResults
              ?nextToken ~fleetArn ())
           (Some Values.ListWebsiteCertificateAuthoritiesResponse.to_json)
           (Some
              Values.ListWebsiteCertificateAuthoritiesResponse.error_to_json)])
let restore_domain_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_domain_access
           (Values.RestoreDomainAccessRequest.make ~fleetArn ~domainName ())
           (Some Values.RestoreDomainAccessResponse.to_json)
           (Some Values.RestoreDomainAccessResponse.error_to_json)])
let revoke_domain_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_domain_access
           (Values.RevokeDomainAccessRequest.make ~fleetArn ~domainName ())
           (Some Values.RevokeDomainAccessResponse.to_json)
           (Some Values.RevokeDomainAccessResponse.error_to_json)])
let sign_out_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and username =
         flag "username" (required string) ~doc:"STRING Username" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.sign_out_user
           (Values.SignOutUserRequest.make ~fleetArn ~username ())
           (Some Values.SignOutUserResponse.to_json)
           (Some Values.SignOutUserResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING FleetArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING FleetArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_audit_stream_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and auditStreamArn =
         flag "audit-stream-arn" (optional string)
           ~doc:"STRING AuditStreamArn"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_audit_stream_configuration
           (Values.UpdateAuditStreamConfigurationRequest.make ?auditStreamArn
              ~fleetArn ())
           (Some Values.UpdateAuditStreamConfigurationResponse.to_json)
           (Some Values.UpdateAuditStreamConfigurationResponse.error_to_json)])
let update_company_network_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON SubnetIds"
       and securityGroupIds =
         flag "security-group-ids" (required json_arg)
           ~doc:"JSON SecurityGroupIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_company_network_configuration
           (Values.UpdateCompanyNetworkConfigurationRequest.make ~fleetArn
              ~vpcId ~subnetIds:(Values.SubnetIds.of_json subnetIds)
              ~securityGroupIds:(Values.SecurityGroupIds.of_json
                                   securityGroupIds) ())
           (Some Values.UpdateCompanyNetworkConfigurationResponse.to_json)
           (Some
              Values.UpdateCompanyNetworkConfigurationResponse.error_to_json)])
let update_device_policy_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceCaCertificate =
         flag "device-ca-certificate" (optional string)
           ~doc:"STRING CertificateChain"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device_policy_configuration
           (Values.UpdateDevicePolicyConfigurationRequest.make
              ?deviceCaCertificate ~fleetArn ())
           (Some Values.UpdateDevicePolicyConfigurationResponse.to_json)
           (Some Values.UpdateDevicePolicyConfigurationResponse.error_to_json)])
let update_domain_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_metadata
           (Values.UpdateDomainMetadataRequest.make ?displayName ~fleetArn
              ~domainName ())
           (Some Values.UpdateDomainMetadataResponse.to_json)
           (Some Values.UpdateDomainMetadataResponse.error_to_json)])
let update_fleet_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and optimizeForEndUserLocation =
         flag "optimize-for-end-user-location" (optional bool)
           ~doc:"BOOL Boolean"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet_metadata
           (Values.UpdateFleetMetadataRequest.make ?displayName
              ?optimizeForEndUserLocation ~fleetArn ())
           (Some Values.UpdateFleetMetadataResponse.to_json)
           (Some Values.UpdateFleetMetadataResponse.error_to_json)])
let update_identity_provider_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityProviderSamlMetadata =
         flag "identity-provider-saml-metadata" (optional string)
           ~doc:"STRING SamlMetadata"
       and fleetArn =
         flag "fleet-arn" (required string) ~doc:"STRING FleetArn"
       and identityProviderType =
         flag "identity-provider-type" (required json_arg)
           ~doc:"JSON IdentityProviderType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_identity_provider_configuration
           (Values.UpdateIdentityProviderConfigurationRequest.make
              ?identityProviderSamlMetadata ~fleetArn
              ~identityProviderType:(Values.IdentityProviderType.of_json
                                       identityProviderType) ())
           (Some Values.UpdateIdentityProviderConfigurationResponse.to_json)
           (Some
              Values.UpdateIdentityProviderConfigurationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-domain", associate_domain);
    ("associate-website-authorization-provider",
      associate_website_authorization_provider);
    ("associate-website-certificate-authority",
      associate_website_certificate_authority);
    ("create-fleet", create_fleet);
    ("delete-fleet", delete_fleet);
    ("describe-audit-stream-configuration",
      describe_audit_stream_configuration);
    ("describe-company-network-configuration",
      describe_company_network_configuration);
    ("describe-device", describe_device);
    ("describe-device-policy-configuration",
      describe_device_policy_configuration);
    ("describe-domain", describe_domain);
    ("describe-fleet-metadata", describe_fleet_metadata);
    ("describe-identity-provider-configuration",
      describe_identity_provider_configuration);
    ("describe-website-certificate-authority",
      describe_website_certificate_authority);
    ("disassociate-domain", disassociate_domain);
    ("disassociate-website-authorization-provider",
      disassociate_website_authorization_provider);
    ("disassociate-website-certificate-authority",
      disassociate_website_certificate_authority);
    ("list-devices", list_devices);
    ("list-domains", list_domains);
    ("list-fleets", list_fleets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-website-authorization-providers",
      list_website_authorization_providers);
    ("list-website-certificate-authorities",
      list_website_certificate_authorities);
    ("restore-domain-access", restore_domain_access);
    ("revoke-domain-access", revoke_domain_access);
    ("sign-out-user", sign_out_user);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-audit-stream-configuration", update_audit_stream_configuration);
    ("update-company-network-configuration",
      update_company_network_configuration);
    ("update-device-policy-configuration",
      update_device_policy_configuration);
    ("update-domain-metadata", update_domain_metadata);
    ("update-fleet-metadata", update_fleet_metadata);
    ("update-identity-provider-configuration",
      update_identity_provider_configuration)]
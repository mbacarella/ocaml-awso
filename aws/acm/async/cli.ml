(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags_to_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_certificate
           (Values.AddTagsToCertificateRequest.make ~certificateArn
              ~tags:(Values.TagList.of_json tags) ()) None None])
let delete_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_certificate
           (Values.DeleteCertificateRequest.make ~certificateArn ()) None
           None])
let describe_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificate
           (Values.DescribeCertificateRequest.make ~certificateArn ())
           (Some Values.DescribeCertificateResponse.to_json)
           (Some Values.DescribeCertificateResponse.error_to_json)])
let export_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn"
       and passphrase =
         flag "passphrase" (required json_arg) ~doc:"JSON PassphraseBlob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_certificate
           (Values.ExportCertificateRequest.make ~certificateArn
              ~passphrase:(Values.PassphraseBlob.of_json passphrase) ())
           (Some Values.ExportCertificateResponse.to_json)
           (Some Values.ExportCertificateResponse.error_to_json)])
let get_account_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_configuration (Fn.id ())
           (Some Values.GetAccountConfigurationResponse.to_json)
           (Some Values.GetAccountConfigurationResponse.error_to_json)])
let get_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificate
           (Values.GetCertificateRequest.make ~certificateArn ())
           (Some Values.GetCertificateResponse.to_json)
           (Some Values.GetCertificateResponse.error_to_json)])
let import_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING Arn"
       and certificateChain =
         flag "certificate-chain" (optional json_arg)
           ~doc:"JSON CertificateChainBlob"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and certificate =
         flag "certificate" (required json_arg)
           ~doc:"JSON CertificateBodyBlob"
       and privateKey =
         flag "private-key" (required json_arg) ~doc:"JSON PrivateKeyBlob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_certificate
           (Values.ImportCertificateRequest.make ?certificateArn
              ?certificateChain:(Option.map
                                   ~f:Values.CertificateChainBlob.of_json
                                   certificateChain)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~certificate:(Values.CertificateBodyBlob.of_json certificate)
              ~privateKey:(Values.PrivateKeyBlob.of_json privateKey) ())
           (Some Values.ImportCertificateResponse.to_json)
           (Some Values.ImportCertificateResponse.error_to_json)])
let list_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateStatuses =
         flag "certificate-statuses" (optional json_arg)
           ~doc:"JSON CertificateStatuses"
       and includes = flag "includes" (optional json_arg) ~doc:"JSON Filters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_certificates
           (Values.ListCertificatesRequest.make
              ?certificateStatuses:(Option.map
                                      ~f:Values.CertificateStatuses.of_json
                                      certificateStatuses)
              ?includes:(Option.map ~f:Values.Filters.of_json includes)
              ?nextToken ?maxItems
              ?sortBy:(Option.map ~f:Values.SortBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListCertificatesResponse.to_json)
           (Some Values.ListCertificatesResponse.error_to_json)])
let list_tags_for_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_certificate
           (Values.ListTagsForCertificateRequest.make ~certificateArn ())
           (Some Values.ListTagsForCertificateResponse.to_json)
           (Some Values.ListTagsForCertificateResponse.error_to_json)])
let put_account_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expiryEvents =
         flag "expiry-events" (optional json_arg)
           ~doc:"JSON ExpiryEventsConfiguration"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_configuration
           (Values.PutAccountConfigurationRequest.make
              ?expiryEvents:(Option.map
                               ~f:Values.ExpiryEventsConfiguration.of_json
                               expiryEvents) ~idempotencyToken ()) None None])
let remove_tags_from_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_certificate
           (Values.RemoveTagsFromCertificateRequest.make ~certificateArn
              ~tags:(Values.TagList.of_json tags) ()) None None])
let renew_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.renew_certificate
           (Values.RenewCertificateRequest.make ~certificateArn ()) None None])
let request_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and validationMethod =
         flag "validation-method" (optional json_arg)
           ~doc:"JSON ValidationMethod"
       and subjectAlternativeNames =
         flag "subject-alternative-names" (optional json_arg)
           ~doc:"JSON DomainList"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and domainValidationOptions =
         flag "domain-validation-options" (optional json_arg)
           ~doc:"JSON DomainValidationOptionList"
       and options =
         flag "options" (optional json_arg) ~doc:"JSON CertificateOptions"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (optional string)
           ~doc:"STRING PcaArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and keyAlgorithm =
         flag "key-algorithm" (optional json_arg) ~doc:"JSON KeyAlgorithm"
       and managedBy =
         flag "managed-by" (optional json_arg)
           ~doc:"JSON CertificateManagedBy"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.request_certificate
           (Values.RequestCertificateRequest.make
              ?validationMethod:(Option.map
                                   ~f:Values.ValidationMethod.of_json
                                   validationMethod)
              ?subjectAlternativeNames:(Option.map
                                          ~f:Values.DomainList.of_json
                                          subjectAlternativeNames)
              ?idempotencyToken
              ?domainValidationOptions:(Option.map
                                          ~f:Values.DomainValidationOptionList.of_json
                                          domainValidationOptions)
              ?options:(Option.map ~f:Values.CertificateOptions.of_json
                          options) ?certificateAuthorityArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?keyAlgorithm:(Option.map ~f:Values.KeyAlgorithm.of_json
                               keyAlgorithm)
              ?managedBy:(Option.map ~f:Values.CertificateManagedBy.of_json
                            managedBy) ~domainName ())
           (Some Values.RequestCertificateResponse.to_json)
           (Some Values.RequestCertificateResponse.error_to_json)])
let resend_validation_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn"
       and domain =
         flag "domain" (required string) ~doc:"STRING DomainNameString"
       and validationDomain =
         flag "validation-domain" (required string)
           ~doc:"STRING DomainNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resend_validation_email
           (Values.ResendValidationEmailRequest.make ~certificateArn ~domain
              ~validationDomain ()) None None])
let revoke_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn"
       and revocationReason =
         flag "revocation-reason" (required json_arg)
           ~doc:"JSON RevocationReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_certificate
           (Values.RevokeCertificateRequest.make ~certificateArn
              ~revocationReason:(Values.RevocationReason.of_json
                                   revocationReason) ())
           (Some Values.RevokeCertificateResponse.to_json)
           (Some Values.RevokeCertificateResponse.error_to_json)])
let search_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterStatement =
         flag "filter-statement" (optional json_arg)
           ~doc:"JSON CertificateFilterStatement"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT SearchMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON SearchCertificatesSortBy"
       and sortOrder =
         flag "sort-order" (optional json_arg)
           ~doc:"JSON SearchCertificatesSortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_certificates
           (Values.SearchCertificatesRequest.make
              ?filterStatement:(Option.map
                                  ~f:Values.CertificateFilterStatement.of_json
                                  filterStatement) ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.SearchCertificatesSortBy.of_json
                         sortBy)
              ?sortOrder:(Option.map
                            ~f:Values.SearchCertificatesSortOrder.of_json
                            sortOrder) ())
           (Some Values.SearchCertificatesResponse.to_json)
           (Some Values.SearchCertificatesResponse.error_to_json)])
let update_certificate_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn"
       and options =
         flag "options" (required json_arg) ~doc:"JSON CertificateOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_certificate_options
           (Values.UpdateCertificateOptionsRequest.make ~certificateArn
              ~options:(Values.CertificateOptions.of_json options) ()) None
           None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-tags-to-certificate", add_tags_to_certificate);
    ("delete-certificate", delete_certificate);
    ("describe-certificate", describe_certificate);
    ("export-certificate", export_certificate);
    ("get-account-configuration", get_account_configuration);
    ("get-certificate", get_certificate);
    ("import-certificate", import_certificate);
    ("list-certificates", list_certificates);
    ("list-tags-for-certificate", list_tags_for_certificate);
    ("put-account-configuration", put_account_configuration);
    ("remove-tags-from-certificate", remove_tags_from_certificate);
    ("renew-certificate", renew_certificate);
    ("request-certificate", request_certificate);
    ("resend-validation-email", resend_validation_email);
    ("revoke-certificate", revoke_certificate);
    ("search-certificates", search_certificates);
    ("update-certificate-options", update_certificate_options)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AddTagsToCertificate: (AddTagsToCertificateRequest.t, unit, unit) t 
  | DeleteCertificate: (DeleteCertificateRequest.t, unit, unit) t 
  | DescribeCertificate: (DescribeCertificateRequest.t,
  DescribeCertificateResponse.t, DescribeCertificateResponse.error) t 
  | ExportCertificate: (ExportCertificateRequest.t,
  ExportCertificateResponse.t, ExportCertificateResponse.error) t 
  | GetAccountConfiguration: (unit, GetAccountConfigurationResponse.t,
  GetAccountConfigurationResponse.error) t 
  | GetCertificate: (GetCertificateRequest.t, GetCertificateResponse.t,
  GetCertificateResponse.error) t 
  | ImportCertificate: (ImportCertificateRequest.t,
  ImportCertificateResponse.t, ImportCertificateResponse.error) t 
  | ListCertificates: (ListCertificatesRequest.t, ListCertificatesResponse.t,
  ListCertificatesResponse.error) t 
  | ListTagsForCertificate: (ListTagsForCertificateRequest.t,
  ListTagsForCertificateResponse.t, ListTagsForCertificateResponse.error) t 
  | PutAccountConfiguration: (PutAccountConfigurationRequest.t, unit, 
  unit) t 
  | RemoveTagsFromCertificate: (RemoveTagsFromCertificateRequest.t, unit,
  unit) t 
  | RenewCertificate: (RenewCertificateRequest.t, unit, unit) t 
  | RequestCertificate: (RequestCertificateRequest.t,
  RequestCertificateResponse.t, RequestCertificateResponse.error) t 
  | ResendValidationEmail: (ResendValidationEmailRequest.t, unit, unit) t 
  | UpdateCertificateOptions: (UpdateCertificateOptionsRequest.t, unit, 
  unit) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AddTagsToCertificate -> `POST
  | DeleteCertificate -> `POST
  | DescribeCertificate -> `POST
  | ExportCertificate -> `POST
  | GetAccountConfiguration -> `POST
  | GetCertificate -> `POST
  | ImportCertificate -> `POST
  | ListCertificates -> `POST
  | ListTagsForCertificate -> `POST
  | PutAccountConfiguration -> `POST
  | RemoveTagsFromCertificate -> `POST
  | RenewCertificate -> `POST
  | RequestCertificate -> `POST
  | ResendValidationEmail -> `POST
  | UpdateCertificateOptions -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AddTagsToCertificate -> (Format.kasprintf Uri.of_string) "/"
      | DeleteCertificate -> (Format.kasprintf Uri.of_string) "/"
      | DescribeCertificate -> (Format.kasprintf Uri.of_string) "/"
      | ExportCertificate -> (Format.kasprintf Uri.of_string) "/"
      | GetAccountConfiguration -> (Format.kasprintf Uri.of_string) "/"
      | GetCertificate -> (Format.kasprintf Uri.of_string) "/"
      | ImportCertificate -> (Format.kasprintf Uri.of_string) "/"
      | ListCertificates -> (Format.kasprintf Uri.of_string) "/"
      | ListTagsForCertificate -> (Format.kasprintf Uri.of_string) "/"
      | PutAccountConfiguration -> (Format.kasprintf Uri.of_string) "/"
      | RemoveTagsFromCertificate -> (Format.kasprintf Uri.of_string) "/"
      | RenewCertificate -> (Format.kasprintf Uri.of_string) "/"
      | RequestCertificate -> (Format.kasprintf Uri.of_string) "/"
      | ResendValidationEmail -> (Format.kasprintf Uri.of_string) "/"
      | UpdateCertificateOptions -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | AddTagsToCertificate ->
      let json = AddTagsToCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.AddTagsToCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteCertificate ->
      let json = DeleteCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.DeleteCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DescribeCertificate ->
      let json = DescribeCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.DescribeCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ExportCertificate ->
      let json = ExportCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.ExportCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetAccountConfiguration ->
      let json = (fun () -> `Assoc []) req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.GetAccountConfiguration")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetCertificate ->
      let json = GetCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.GetCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ImportCertificate ->
      let json = ImportCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.ImportCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListCertificates ->
      let json = ListCertificatesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.ListCertificates")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListTagsForCertificate ->
      let json = ListTagsForCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.ListTagsForCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | PutAccountConfiguration ->
      let json = PutAccountConfigurationRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.PutAccountConfiguration")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RemoveTagsFromCertificate ->
      let json = RemoveTagsFromCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.RemoveTagsFromCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RenewCertificate ->
      let json = RenewCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.RenewCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RequestCertificate ->
      let json = RequestCertificateRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.RequestCertificate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ResendValidationEmail ->
      let json = ResendValidationEmailRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.ResendValidationEmail")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateCertificateOptions ->
      let json = UpdateCertificateOptionsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "CertificateManager.UpdateCertificateOptions")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | AddTagsToCertificate ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DeleteCertificate ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DescribeCertificate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DescribeCertificateResponse.of_json json)
      else
        Error
          (parse_aws_error (Some DescribeCertificateResponse.error_of_json))
  | ExportCertificate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ExportCertificateResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ExportCertificateResponse.error_of_json))
  | GetAccountConfiguration ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetAccountConfigurationResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some GetAccountConfigurationResponse.error_of_json))
  | GetCertificate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetCertificateResponse.of_json json)
      else
        Error (parse_aws_error (Some GetCertificateResponse.error_of_json))
  | ImportCertificate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ImportCertificateResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ImportCertificateResponse.error_of_json))
  | ListCertificates ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListCertificatesResponse.of_json json)
      else
        Error (parse_aws_error (Some ListCertificatesResponse.error_of_json))
  | ListTagsForCertificate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListTagsForCertificateResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ListTagsForCertificateResponse.error_of_json))
  | PutAccountConfiguration ->
      if is_success then Ok () else Error (parse_aws_error None)
  | RemoveTagsFromCertificate ->
      if is_success then Ok () else Error (parse_aws_error None)
  | RenewCertificate ->
      if is_success then Ok () else Error (parse_aws_error None)
  | RequestCertificate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RequestCertificateResponse.of_json json)
      else
        Error
          (parse_aws_error (Some RequestCertificateResponse.error_of_json))
  | ResendValidationEmail ->
      if is_success then Ok () else Error (parse_aws_error None)
  | UpdateCertificateOptions ->
      if is_success then Ok () else Error (parse_aws_error None)
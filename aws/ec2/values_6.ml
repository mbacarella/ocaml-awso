(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
open Values_0[@@warning "-33"]
open Values_1[@@warning "-33"]
open Values_2[@@warning "-33"]
open Values_3[@@warning "-33"]
open Values_4[@@warning "-33"]
open Values_5[@@warning "-33"]
module EnableVgwRoutePropagationRequest =
  struct
    type nonrec t =
      {
      gatewayId: VpnGatewayId.t ;
      routeTableId: RouteTableId.t ;
      dryRun: Boolean.t option }
    let context_ = "EnableVgwRoutePropagationRequest"
    let make ?dryRun =
      fun ~gatewayId ->
        fun ~routeTableId -> fun () -> { dryRun; gatewayId; routeTableId }
    let to_value x =
      structure_to_value
        [("GatewayId", (Some (VpnGatewayId.to_value x.gatewayId)));
        ("RouteTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteTableId") in
      let gatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GatewayId") in
      make ?dryRun ~routeTableId ~gatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let gatewayId = field_map_exn json "GatewayId" VpnGatewayId.of_json in
      make ?dryRun ~routeTableId ~gatewayId ()
    let to_json v = composed_to_json to_value v
  end
module EnableTransitGatewayRouteTablePropagationResult =
  struct
    type nonrec t = {
      propagation: TransitGatewayPropagation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?propagation = fun () -> { propagation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("propagation",
           (Option.map x.propagation ~f:TransitGatewayPropagation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let propagation =
        (Option.map ~f:TransitGatewayPropagation.of_xml)
          (Xml.child xml_arg0 "propagation") in
      make ?propagation ()
    let of_json json =
      let propagation =
        field_map json "Propagation" TransitGatewayPropagation.of_json in
      make ?propagation ()
    let to_json v = composed_to_json to_value v
  end
module EnableTransitGatewayRouteTablePropagationRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "EnableTransitGatewayRouteTablePropagationRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~transitGatewayAttachmentId ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; transitGatewayAttachmentId
            }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module EnableSerialConsoleAccessResult =
  struct
    type nonrec t = {
      serialConsoleAccessEnabled: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serialConsoleAccessEnabled =
      fun () -> { serialConsoleAccessEnabled }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serialConsoleAccessEnabled",
           (Option.map x.serialConsoleAccessEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serialConsoleAccessEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "serialConsoleAccessEnabled") in
      make ?serialConsoleAccessEnabled ()
    let of_json json =
      let serialConsoleAccessEnabled =
        field_map json "SerialConsoleAccessEnabled" Boolean.of_json in
      make ?serialConsoleAccessEnabled ()
    let to_json v = composed_to_json to_value v
  end
module EnableSerialConsoleAccessRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module EnableIpamOrganizationAdminAccountResult =
  struct
    type nonrec t = {
      success: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?success = fun () -> { success }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("success", (Option.map x.success ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let success =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "success") in
      make ?success ()
    let of_json json =
      let success = field_map json "Success" Boolean.of_json in
      make ?success ()
    let to_json v = composed_to_json to_value v
  end
module EnableIpamOrganizationAdminAccountRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      delegatedAdminAccountId: String_.t }
    let context_ = "EnableIpamOrganizationAdminAccountRequest"
    let make ?dryRun =
      fun ~delegatedAdminAccountId ->
        fun () -> { dryRun; delegatedAdminAccountId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("DelegatedAdminAccountId",
          (Some (String_.to_value x.delegatedAdminAccountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegatedAdminAccountId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DelegatedAdminAccountId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~delegatedAdminAccountId ?dryRun ()
    let of_json json =
      let delegatedAdminAccountId =
        field_map_exn json "DelegatedAdminAccountId" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~delegatedAdminAccountId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module EnableImageDeprecationResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module EnableImageDeprecationRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t ;
      deprecateAt: MillisecondDateTime.t ;
      dryRun: Boolean.t option }
    let context_ = "EnableImageDeprecationRequest"
    let make ?dryRun =
      fun ~imageId ->
        fun ~deprecateAt -> fun () -> { dryRun; imageId; deprecateAt }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("DeprecateAt", (Some (MillisecondDateTime.to_value x.deprecateAt)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let deprecateAt =
        MillisecondDateTime.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DeprecateAt") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ~deprecateAt ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let deprecateAt =
        field_map_exn json "DeprecateAt" MillisecondDateTime.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ~deprecateAt ~imageId ()
    let to_json v = composed_to_json to_value v
  end
module EnableFastSnapshotRestoresResult =
  struct
    type nonrec t =
      {
      successful: EnableFastSnapshotRestoreSuccessSet.t option ;
      unsuccessful: EnableFastSnapshotRestoreErrorSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successful =
      fun ?unsuccessful -> fun () -> { successful; unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successful",
           (Option.map x.successful
              ~f:EnableFastSnapshotRestoreSuccessSet.to_value));
        ("unsuccessful",
          (Option.map x.unsuccessful
             ~f:EnableFastSnapshotRestoreErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:EnableFastSnapshotRestoreErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      let successful =
        (Option.map ~f:EnableFastSnapshotRestoreSuccessSet.of_xml)
          (Xml.child xml_arg0 "successful") in
      make ?unsuccessful ?successful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful"
          EnableFastSnapshotRestoreErrorSet.of_json in
      let successful =
        field_map json "Successful"
          EnableFastSnapshotRestoreSuccessSet.of_json in
      make ?unsuccessful ?successful ()
    let to_json v = composed_to_json to_value v
  end
module EnableFastSnapshotRestoresRequest =
  struct
    type nonrec t =
      {
      availabilityZones: AvailabilityZoneStringList.t ;
      sourceSnapshotIds: SnapshotIdStringList.t ;
      dryRun: Boolean.t option }
    let context_ = "EnableFastSnapshotRestoresRequest"
    let make ?dryRun =
      fun ~availabilityZones ->
        fun ~sourceSnapshotIds ->
          fun () -> { dryRun; availabilityZones; sourceSnapshotIds }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Some (AvailabilityZoneStringList.to_value x.availabilityZones)));
        ("SourceSnapshotId",
          (Some (SnapshotIdStringList.to_value x.sourceSnapshotIds)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let sourceSnapshotIds =
        SnapshotIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceSnapshotId") in
      let availabilityZones =
        AvailabilityZoneStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZone") in
      make ?dryRun ~sourceSnapshotIds ~availabilityZones ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let sourceSnapshotIds =
        field_map_exn json "SourceSnapshotIds" SnapshotIdStringList.of_json in
      let availabilityZones =
        field_map_exn json "AvailabilityZones"
          AvailabilityZoneStringList.of_json in
      make ?dryRun ~sourceSnapshotIds ~availabilityZones ()
    let to_json v = composed_to_json to_value v
  end
module EnableFastLaunchResult =
  struct
    type nonrec t =
      {
      imageId: ImageId.t option ;
      resourceType: FastLaunchResourceType.t option ;
      snapshotConfiguration: FastLaunchSnapshotConfigurationResponse.t option ;
      launchTemplate: FastLaunchLaunchTemplateSpecificationResponse.t option ;
      maxParallelLaunches: Integer.t option ;
      ownerId: String_.t option ;
      state: FastLaunchStateCode.t option ;
      stateTransitionReason: String_.t option ;
      stateTransitionTime: MillisecondDateTime.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId =
      fun ?resourceType ->
        fun ?snapshotConfiguration ->
          fun ?launchTemplate ->
            fun ?maxParallelLaunches ->
              fun ?ownerId ->
                fun ?state ->
                  fun ?stateTransitionReason ->
                    fun ?stateTransitionTime ->
                      fun () ->
                        {
                          imageId;
                          resourceType;
                          snapshotConfiguration;
                          launchTemplate;
                          maxParallelLaunches;
                          ownerId;
                          state;
                          stateTransitionReason;
                          stateTransitionTime
                        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:ImageId.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:FastLaunchResourceType.to_value));
        ("snapshotConfiguration",
          (Option.map x.snapshotConfiguration
             ~f:FastLaunchSnapshotConfigurationResponse.to_value));
        ("launchTemplate",
          (Option.map x.launchTemplate
             ~f:FastLaunchLaunchTemplateSpecificationResponse.to_value));
        ("maxParallelLaunches",
          (Option.map x.maxParallelLaunches ~f:Integer.to_value));
        ("ownerId", (Option.map x.ownerId ~f:String_.to_value));
        ("state", (Option.map x.state ~f:FastLaunchStateCode.to_value));
        ("stateTransitionReason",
          (Option.map x.stateTransitionReason ~f:String_.to_value));
        ("stateTransitionTime",
          (Option.map x.stateTransitionTime ~f:MillisecondDateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stateTransitionTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "stateTransitionTime") in
      let stateTransitionReason =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "stateTransitionReason") in
      let state =
        (Option.map ~f:FastLaunchStateCode.of_xml)
          (Xml.child xml_arg0 "state") in
      let ownerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ownerId") in
      let maxParallelLaunches =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "maxParallelLaunches") in
      let launchTemplate =
        (Option.map ~f:FastLaunchLaunchTemplateSpecificationResponse.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      let snapshotConfiguration =
        (Option.map ~f:FastLaunchSnapshotConfigurationResponse.of_xml)
          (Xml.child xml_arg0 "snapshotConfiguration") in
      let resourceType =
        (Option.map ~f:FastLaunchResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let imageId =
        (Option.map ~f:ImageId.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?stateTransitionTime ?stateTransitionReason ?state ?ownerId
        ?maxParallelLaunches ?launchTemplate ?snapshotConfiguration
        ?resourceType ?imageId ()
    let of_json json =
      let stateTransitionTime =
        field_map json "StateTransitionTime" MillisecondDateTime.of_json in
      let stateTransitionReason =
        field_map json "StateTransitionReason" String_.of_json in
      let state = field_map json "State" FastLaunchStateCode.of_json in
      let ownerId = field_map json "OwnerId" String_.of_json in
      let maxParallelLaunches =
        field_map json "MaxParallelLaunches" Integer.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate"
          FastLaunchLaunchTemplateSpecificationResponse.of_json in
      let snapshotConfiguration =
        field_map json "SnapshotConfiguration"
          FastLaunchSnapshotConfigurationResponse.of_json in
      let resourceType =
        field_map json "ResourceType" FastLaunchResourceType.of_json in
      let imageId = field_map json "ImageId" ImageId.of_json in
      make ?stateTransitionTime ?stateTransitionReason ?state ?ownerId
        ?maxParallelLaunches ?launchTemplate ?snapshotConfiguration
        ?resourceType ?imageId ()
    let to_json v = composed_to_json to_value v
  end
module EnableFastLaunchRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t ;
      resourceType: String_.t option ;
      snapshotConfiguration: FastLaunchSnapshotConfigurationRequest.t option ;
      launchTemplate: FastLaunchLaunchTemplateSpecificationRequest.t option ;
      maxParallelLaunches: Integer.t option ;
      dryRun: Boolean.t option }
    let context_ = "EnableFastLaunchRequest"
    let make ?resourceType =
      fun ?snapshotConfiguration ->
        fun ?launchTemplate ->
          fun ?maxParallelLaunches ->
            fun ?dryRun ->
              fun ~imageId ->
                fun () ->
                  {
                    resourceType;
                    snapshotConfiguration;
                    launchTemplate;
                    maxParallelLaunches;
                    dryRun;
                    imageId
                  }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value));
        ("SnapshotConfiguration",
          (Option.map x.snapshotConfiguration
             ~f:FastLaunchSnapshotConfigurationRequest.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:FastLaunchLaunchTemplateSpecificationRequest.to_value));
        ("MaxParallelLaunches",
          (Option.map x.maxParallelLaunches ~f:Integer.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxParallelLaunches =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxParallelLaunches") in
      let launchTemplate =
        (Option.map ~f:FastLaunchLaunchTemplateSpecificationRequest.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let snapshotConfiguration =
        (Option.map ~f:FastLaunchSnapshotConfigurationRequest.of_xml)
          (Xml.child xml_arg0 "SnapshotConfiguration") in
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ?maxParallelLaunches ?launchTemplate
        ?snapshotConfiguration ?resourceType ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxParallelLaunches =
        field_map json "MaxParallelLaunches" Integer.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate"
          FastLaunchLaunchTemplateSpecificationRequest.of_json in
      let snapshotConfiguration =
        field_map json "SnapshotConfiguration"
          FastLaunchSnapshotConfigurationRequest.of_json in
      let resourceType = field_map json "ResourceType" String_.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ?maxParallelLaunches ?launchTemplate
        ?snapshotConfiguration ?resourceType ~imageId ()
    let to_json v = composed_to_json to_value v
  end
module EnableEbsEncryptionByDefaultResult =
  struct
    type nonrec t = {
      ebsEncryptionByDefault: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ebsEncryptionByDefault = fun () -> { ebsEncryptionByDefault }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ebsEncryptionByDefault",
           (Option.map x.ebsEncryptionByDefault ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ebsEncryptionByDefault =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ebsEncryptionByDefault") in
      make ?ebsEncryptionByDefault ()
    let of_json json =
      let ebsEncryptionByDefault =
        field_map json "EbsEncryptionByDefault" Boolean.of_json in
      make ?ebsEncryptionByDefault ()
    let to_json v = composed_to_json to_value v
  end
module EnableEbsEncryptionByDefaultRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateVpcCidrBlockResult =
  struct
    type nonrec t =
      {
      ipv6CidrBlockAssociation: VpcIpv6CidrBlockAssociation.t option ;
      cidrBlockAssociation: VpcCidrBlockAssociation.t option ;
      vpcId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrBlockAssociation =
      fun ?cidrBlockAssociation ->
        fun ?vpcId ->
          fun () -> { ipv6CidrBlockAssociation; cidrBlockAssociation; vpcId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrBlockAssociation",
           (Option.map x.ipv6CidrBlockAssociation
              ~f:VpcIpv6CidrBlockAssociation.to_value));
        ("cidrBlockAssociation",
          (Option.map x.cidrBlockAssociation
             ~f:VpcCidrBlockAssociation.to_value));
        ("vpcId", (Option.map x.vpcId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "vpcId") in
      let cidrBlockAssociation =
        (Option.map ~f:VpcCidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "cidrBlockAssociation") in
      let ipv6CidrBlockAssociation =
        (Option.map ~f:VpcIpv6CidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "ipv6CidrBlockAssociation") in
      make ?vpcId ?cidrBlockAssociation ?ipv6CidrBlockAssociation ()
    let of_json json =
      let vpcId = field_map json "VpcId" String_.of_json in
      let cidrBlockAssociation =
        field_map json "CidrBlockAssociation" VpcCidrBlockAssociation.of_json in
      let ipv6CidrBlockAssociation =
        field_map json "Ipv6CidrBlockAssociation"
          VpcIpv6CidrBlockAssociation.of_json in
      make ?vpcId ?cidrBlockAssociation ?ipv6CidrBlockAssociation ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateVpcCidrBlockRequest =
  struct
    type nonrec t = {
      associationId: VpcCidrAssociationId.t }
    let context_ = "DisassociateVpcCidrBlockRequest"
    let make ~associationId = fun () -> { associationId }
    let to_value x =
      structure_to_value
        [("associationId",
           (Some (VpcCidrAssociationId.to_value x.associationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationId =
        VpcCidrAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associationId") in
      make ~associationId ()
    let of_json json =
      let associationId =
        field_map_exn json "AssociationId" VpcCidrAssociationId.of_json in
      make ~associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTrunkInterfaceResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun ?clientToken -> fun () -> { return; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?clientToken ?return ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let return = field_map json "Return" Boolean.of_json in
      make ?clientToken ?return ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTrunkInterfaceRequest =
  struct
    type nonrec t =
      {
      associationId: TrunkInterfaceAssociationId.t ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "DisassociateTrunkInterfaceRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~associationId ->
          fun () -> { clientToken; dryRun; associationId }
    let to_value x =
      structure_to_value
        [("AssociationId",
           (Some (TrunkInterfaceAssociationId.to_value x.associationId)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let associationId =
        TrunkInterfaceAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationId") in
      make ?dryRun ?clientToken ~associationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let associationId =
        field_map_exn json "AssociationId"
          TrunkInterfaceAssociationId.of_json in
      make ?dryRun ?clientToken ~associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTransitGatewayRouteTableResult =
  struct
    type nonrec t = {
      association: TransitGatewayAssociation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?association = fun () -> { association }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("association",
           (Option.map x.association ~f:TransitGatewayAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let association =
        (Option.map ~f:TransitGatewayAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      make ?association ()
    let of_json json =
      let association =
        field_map json "Association" TransitGatewayAssociation.of_json in
      make ?association ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "DisassociateTransitGatewayRouteTableRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~transitGatewayAttachmentId ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; transitGatewayAttachmentId
            }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayMulticastDomainAssociations.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations = fun () -> { associations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayMulticastDomainAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociations.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?associations ()
    let of_json json =
      let associations =
        field_map json "Associations"
          TransitGatewayMulticastDomainAssociations.of_json in
      make ?associations ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option ;
      subnetIds: TransitGatewaySubnetIdList.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayMulticastDomainId =
      fun ?transitGatewayAttachmentId ->
        fun ?subnetIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                transitGatewayAttachmentId;
                subnetIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("SubnetIds",
          (Option.map x.subnetIds ~f:TransitGatewaySubnetIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetIds =
        (Option.map ~f:TransitGatewaySubnetIdList.of_xml)
          (Xml.child xml_arg0 "SubnetIds") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetIds =
        field_map json "SubnetIds" TransitGatewaySubnetIdList.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateSubnetCidrBlockResult =
  struct
    type nonrec t =
      {
      ipv6CidrBlockAssociation: SubnetIpv6CidrBlockAssociation.t option ;
      subnetId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrBlockAssociation =
      fun ?subnetId -> fun () -> { ipv6CidrBlockAssociation; subnetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrBlockAssociation",
           (Option.map x.ipv6CidrBlockAssociation
              ~f:SubnetIpv6CidrBlockAssociation.to_value));
        ("subnetId", (Option.map x.subnetId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnetId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "subnetId") in
      let ipv6CidrBlockAssociation =
        (Option.map ~f:SubnetIpv6CidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "ipv6CidrBlockAssociation") in
      make ?subnetId ?ipv6CidrBlockAssociation ()
    let of_json json =
      let subnetId = field_map json "SubnetId" String_.of_json in
      let ipv6CidrBlockAssociation =
        field_map json "Ipv6CidrBlockAssociation"
          SubnetIpv6CidrBlockAssociation.of_json in
      make ?subnetId ?ipv6CidrBlockAssociation ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateSubnetCidrBlockRequest =
  struct
    type nonrec t = {
      associationId: SubnetCidrAssociationId.t }
    let context_ = "DisassociateSubnetCidrBlockRequest"
    let make ~associationId = fun () -> { associationId }
    let to_value x =
      structure_to_value
        [("associationId",
           (Some (SubnetCidrAssociationId.to_value x.associationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationId =
        SubnetCidrAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associationId") in
      make ~associationId ()
    let of_json json =
      let associationId =
        field_map_exn json "AssociationId" SubnetCidrAssociationId.of_json in
      make ~associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateRouteTableRequest =
  struct
    type nonrec t =
      {
      associationId: RouteTableAssociationId.t ;
      dryRun: Boolean.t option }
    let context_ = "DisassociateRouteTableRequest"
    let make ?dryRun =
      fun ~associationId -> fun () -> { dryRun; associationId }
    let to_value x =
      structure_to_value
        [("associationId",
           (Some (RouteTableAssociationId.to_value x.associationId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let associationId =
        RouteTableAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associationId") in
      make ?dryRun ~associationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let associationId =
        field_map_exn json "AssociationId" RouteTableAssociationId.of_json in
      make ?dryRun ~associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateInstanceEventWindowResult =
  struct
    type nonrec t = {
      instanceEventWindow: InstanceEventWindow.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindow = fun () -> { instanceEventWindow }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindow",
           (Option.map x.instanceEventWindow ~f:InstanceEventWindow.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindow =
        (Option.map ~f:InstanceEventWindow.of_xml)
          (Xml.child xml_arg0 "instanceEventWindow") in
      make ?instanceEventWindow ()
    let of_json json =
      let instanceEventWindow =
        field_map json "InstanceEventWindow" InstanceEventWindow.of_json in
      make ?instanceEventWindow ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      instanceEventWindowId: InstanceEventWindowId.t ;
      associationTarget: InstanceEventWindowDisassociationRequest.t }
    let context_ = "DisassociateInstanceEventWindowRequest"
    let make ?dryRun =
      fun ~instanceEventWindowId ->
        fun ~associationTarget ->
          fun () -> { dryRun; instanceEventWindowId; associationTarget }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Some (InstanceEventWindowId.to_value x.instanceEventWindowId)));
        ("AssociationTarget",
          (Some
             (InstanceEventWindowDisassociationRequest.to_value
                x.associationTarget)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationTarget =
        InstanceEventWindowDisassociationRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationTarget") in
      let instanceEventWindowId =
        InstanceEventWindowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventWindowId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~associationTarget ~instanceEventWindowId ?dryRun ()
    let of_json json =
      let associationTarget =
        field_map_exn json "AssociationTarget"
          InstanceEventWindowDisassociationRequest.of_json in
      let instanceEventWindowId =
        field_map_exn json "InstanceEventWindowId"
          InstanceEventWindowId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~associationTarget ~instanceEventWindowId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateIamInstanceProfileResult =
  struct
    type nonrec t =
      {
      iamInstanceProfileAssociation: IamInstanceProfileAssociation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?iamInstanceProfileAssociation =
      fun () -> { iamInstanceProfileAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("iamInstanceProfileAssociation",
           (Option.map x.iamInstanceProfileAssociation
              ~f:IamInstanceProfileAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let iamInstanceProfileAssociation =
        (Option.map ~f:IamInstanceProfileAssociation.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfileAssociation") in
      make ?iamInstanceProfileAssociation ()
    let of_json json =
      let iamInstanceProfileAssociation =
        field_map json "IamInstanceProfileAssociation"
          IamInstanceProfileAssociation.of_json in
      make ?iamInstanceProfileAssociation ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateIamInstanceProfileRequest =
  struct
    type nonrec t = {
      associationId: IamInstanceProfileAssociationId.t }
    let context_ = "DisassociateIamInstanceProfileRequest"
    let make ~associationId = fun () -> { associationId }
    let to_value x =
      structure_to_value
        [("AssociationId",
           (Some (IamInstanceProfileAssociationId.to_value x.associationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationId =
        IamInstanceProfileAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationId") in
      make ~associationId ()
    let of_json json =
      let associationId =
        field_map_exn json "AssociationId"
          IamInstanceProfileAssociationId.of_json in
      make ~associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateEnclaveCertificateIamRoleResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateEnclaveCertificateIamRoleRequest =
  struct
    type nonrec t =
      {
      certificateArn: ResourceArn.t option ;
      roleArn: ResourceArn.t option ;
      dryRun: Boolean.t option }
    let make ?certificateArn =
      fun ?roleArn ->
        fun ?dryRun -> fun () -> { certificateArn; roleArn; dryRun }
    let to_value x =
      structure_to_value
        [("CertificateArn",
           (Option.map x.certificateArn ~f:ResourceArn.to_value));
        ("RoleArn", (Option.map x.roleArn ~f:ResourceArn.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let roleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RoleArn") in
      let certificateArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "CertificateArn") in
      make ?dryRun ?roleArn ?certificateArn ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let roleArn = field_map json "RoleArn" ResourceArn.of_json in
      let certificateArn =
        field_map json "CertificateArn" ResourceArn.of_json in
      make ?dryRun ?roleArn ?certificateArn ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateClientVpnTargetNetworkResult =
  struct
    type nonrec t =
      {
      associationId: String_.t option ;
      status: AssociationStatus.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId =
      fun ?status -> fun () -> { associationId; status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:AssociationStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AssociationStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?status ?associationId ()
    let of_json json =
      let status = field_map json "Status" AssociationStatus.of_json in
      let associationId = field_map json "AssociationId" String_.of_json in
      make ?status ?associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateClientVpnTargetNetworkRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      associationId: ClientVpnAssociationId.t ;
      dryRun: Boolean.t option }
    let context_ = "DisassociateClientVpnTargetNetworkRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId ->
        fun ~associationId ->
          fun () -> { dryRun; clientVpnEndpointId; associationId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("AssociationId",
          (Some (ClientVpnAssociationId.to_value x.associationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let associationId =
        ClientVpnAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~associationId ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let associationId =
        field_map_exn json "AssociationId" ClientVpnAssociationId.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~associationId ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateAddressRequest =
  struct
    type nonrec t =
      {
      associationId: ElasticIpAssociationId.t option ;
      publicIp: String_.t option ;
      dryRun: Boolean.t option }
    let make ?associationId =
      fun ?publicIp ->
        fun ?dryRun -> fun () -> { associationId; publicIp; dryRun }
    let to_value x =
      structure_to_value
        [("AssociationId",
           (Option.map x.associationId ~f:ElasticIpAssociationId.to_value));
        ("PublicIp", (Option.map x.publicIp ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PublicIp") in
      let associationId =
        (Option.map ~f:ElasticIpAssociationId.of_xml)
          (Xml.child xml_arg0 "AssociationId") in
      make ?dryRun ?publicIp ?associationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let publicIp = field_map json "PublicIp" String_.of_json in
      let associationId =
        field_map json "AssociationId" ElasticIpAssociationId.of_json in
      make ?dryRun ?publicIp ?associationId ()
    let to_json v = composed_to_json to_value v
  end
module DisableVpcClassicLinkResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DisableVpcClassicLinkRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      vpcId: VpcId.t }
    let context_ = "DisableVpcClassicLinkRequest"
    let make ?dryRun = fun ~vpcId -> fun () -> { dryRun; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DisableVpcClassicLinkDnsSupportResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DisableVpcClassicLinkDnsSupportRequest =
  struct
    type nonrec t = {
      vpcId: VpcId.t option }
    let make ?vpcId = fun () -> { vpcId }
    let to_value x =
      structure_to_value [("VpcId", (Option.map x.vpcId ~f:VpcId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      make ?vpcId ()
    let of_json json =
      let vpcId = field_map json "VpcId" VpcId.of_json in make ?vpcId ()
    let to_json v = composed_to_json to_value v
  end
module DisableVgwRoutePropagationRequest =
  struct
    type nonrec t =
      {
      gatewayId: VpnGatewayId.t ;
      routeTableId: RouteTableId.t ;
      dryRun: Boolean.t option }
    let context_ = "DisableVgwRoutePropagationRequest"
    let make ?dryRun =
      fun ~gatewayId ->
        fun ~routeTableId -> fun () -> { dryRun; gatewayId; routeTableId }
    let to_value x =
      structure_to_value
        [("GatewayId", (Some (VpnGatewayId.to_value x.gatewayId)));
        ("RouteTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteTableId") in
      let gatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GatewayId") in
      make ?dryRun ~routeTableId ~gatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let gatewayId = field_map_exn json "GatewayId" VpnGatewayId.of_json in
      make ?dryRun ~routeTableId ~gatewayId ()
    let to_json v = composed_to_json to_value v
  end
module DisableTransitGatewayRouteTablePropagationResult =
  struct
    type nonrec t = {
      propagation: TransitGatewayPropagation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?propagation = fun () -> { propagation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("propagation",
           (Option.map x.propagation ~f:TransitGatewayPropagation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let propagation =
        (Option.map ~f:TransitGatewayPropagation.of_xml)
          (Xml.child xml_arg0 "propagation") in
      make ?propagation ()
    let of_json json =
      let propagation =
        field_map json "Propagation" TransitGatewayPropagation.of_json in
      make ?propagation ()
    let to_json v = composed_to_json to_value v
  end
module DisableTransitGatewayRouteTablePropagationRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "DisableTransitGatewayRouteTablePropagationRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~transitGatewayAttachmentId ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; transitGatewayAttachmentId
            }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module DisableSerialConsoleAccessResult =
  struct
    type nonrec t = {
      serialConsoleAccessEnabled: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serialConsoleAccessEnabled =
      fun () -> { serialConsoleAccessEnabled }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serialConsoleAccessEnabled",
           (Option.map x.serialConsoleAccessEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serialConsoleAccessEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "serialConsoleAccessEnabled") in
      make ?serialConsoleAccessEnabled ()
    let of_json json =
      let serialConsoleAccessEnabled =
        field_map json "SerialConsoleAccessEnabled" Boolean.of_json in
      make ?serialConsoleAccessEnabled ()
    let to_json v = composed_to_json to_value v
  end
module DisableSerialConsoleAccessRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DisableIpamOrganizationAdminAccountResult =
  struct
    type nonrec t = {
      success: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?success = fun () -> { success }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("success", (Option.map x.success ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let success =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "success") in
      make ?success ()
    let of_json json =
      let success = field_map json "Success" Boolean.of_json in
      make ?success ()
    let to_json v = composed_to_json to_value v
  end
module DisableIpamOrganizationAdminAccountRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      delegatedAdminAccountId: String_.t }
    let context_ = "DisableIpamOrganizationAdminAccountRequest"
    let make ?dryRun =
      fun ~delegatedAdminAccountId ->
        fun () -> { dryRun; delegatedAdminAccountId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("DelegatedAdminAccountId",
          (Some (String_.to_value x.delegatedAdminAccountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegatedAdminAccountId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DelegatedAdminAccountId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~delegatedAdminAccountId ?dryRun ()
    let of_json json =
      let delegatedAdminAccountId =
        field_map_exn json "DelegatedAdminAccountId" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~delegatedAdminAccountId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DisableImageDeprecationResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DisableImageDeprecationRequest =
  struct
    type nonrec t = {
      imageId: ImageId.t ;
      dryRun: Boolean.t option }
    let context_ = "DisableImageDeprecationRequest"
    let make ?dryRun = fun ~imageId -> fun () -> { dryRun; imageId }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ~imageId ()
    let to_json v = composed_to_json to_value v
  end
module DisableFastSnapshotRestoresResult =
  struct
    type nonrec t =
      {
      successful: DisableFastSnapshotRestoreSuccessSet.t option ;
      unsuccessful: DisableFastSnapshotRestoreErrorSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successful =
      fun ?unsuccessful -> fun () -> { successful; unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successful",
           (Option.map x.successful
              ~f:DisableFastSnapshotRestoreSuccessSet.to_value));
        ("unsuccessful",
          (Option.map x.unsuccessful
             ~f:DisableFastSnapshotRestoreErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:DisableFastSnapshotRestoreErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      let successful =
        (Option.map ~f:DisableFastSnapshotRestoreSuccessSet.of_xml)
          (Xml.child xml_arg0 "successful") in
      make ?unsuccessful ?successful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful"
          DisableFastSnapshotRestoreErrorSet.of_json in
      let successful =
        field_map json "Successful"
          DisableFastSnapshotRestoreSuccessSet.of_json in
      make ?unsuccessful ?successful ()
    let to_json v = composed_to_json to_value v
  end
module DisableFastSnapshotRestoresRequest =
  struct
    type nonrec t =
      {
      availabilityZones: AvailabilityZoneStringList.t ;
      sourceSnapshotIds: SnapshotIdStringList.t ;
      dryRun: Boolean.t option }
    let context_ = "DisableFastSnapshotRestoresRequest"
    let make ?dryRun =
      fun ~availabilityZones ->
        fun ~sourceSnapshotIds ->
          fun () -> { dryRun; availabilityZones; sourceSnapshotIds }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Some (AvailabilityZoneStringList.to_value x.availabilityZones)));
        ("SourceSnapshotId",
          (Some (SnapshotIdStringList.to_value x.sourceSnapshotIds)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let sourceSnapshotIds =
        SnapshotIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceSnapshotId") in
      let availabilityZones =
        AvailabilityZoneStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZone") in
      make ?dryRun ~sourceSnapshotIds ~availabilityZones ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let sourceSnapshotIds =
        field_map_exn json "SourceSnapshotIds" SnapshotIdStringList.of_json in
      let availabilityZones =
        field_map_exn json "AvailabilityZones"
          AvailabilityZoneStringList.of_json in
      make ?dryRun ~sourceSnapshotIds ~availabilityZones ()
    let to_json v = composed_to_json to_value v
  end
module DisableFastLaunchResult =
  struct
    type nonrec t =
      {
      imageId: ImageId.t option ;
      resourceType: FastLaunchResourceType.t option ;
      snapshotConfiguration: FastLaunchSnapshotConfigurationResponse.t option ;
      launchTemplate: FastLaunchLaunchTemplateSpecificationResponse.t option ;
      maxParallelLaunches: Integer.t option ;
      ownerId: String_.t option ;
      state: FastLaunchStateCode.t option ;
      stateTransitionReason: String_.t option ;
      stateTransitionTime: MillisecondDateTime.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId =
      fun ?resourceType ->
        fun ?snapshotConfiguration ->
          fun ?launchTemplate ->
            fun ?maxParallelLaunches ->
              fun ?ownerId ->
                fun ?state ->
                  fun ?stateTransitionReason ->
                    fun ?stateTransitionTime ->
                      fun () ->
                        {
                          imageId;
                          resourceType;
                          snapshotConfiguration;
                          launchTemplate;
                          maxParallelLaunches;
                          ownerId;
                          state;
                          stateTransitionReason;
                          stateTransitionTime
                        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:ImageId.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:FastLaunchResourceType.to_value));
        ("snapshotConfiguration",
          (Option.map x.snapshotConfiguration
             ~f:FastLaunchSnapshotConfigurationResponse.to_value));
        ("launchTemplate",
          (Option.map x.launchTemplate
             ~f:FastLaunchLaunchTemplateSpecificationResponse.to_value));
        ("maxParallelLaunches",
          (Option.map x.maxParallelLaunches ~f:Integer.to_value));
        ("ownerId", (Option.map x.ownerId ~f:String_.to_value));
        ("state", (Option.map x.state ~f:FastLaunchStateCode.to_value));
        ("stateTransitionReason",
          (Option.map x.stateTransitionReason ~f:String_.to_value));
        ("stateTransitionTime",
          (Option.map x.stateTransitionTime ~f:MillisecondDateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stateTransitionTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "stateTransitionTime") in
      let stateTransitionReason =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "stateTransitionReason") in
      let state =
        (Option.map ~f:FastLaunchStateCode.of_xml)
          (Xml.child xml_arg0 "state") in
      let ownerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ownerId") in
      let maxParallelLaunches =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "maxParallelLaunches") in
      let launchTemplate =
        (Option.map ~f:FastLaunchLaunchTemplateSpecificationResponse.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      let snapshotConfiguration =
        (Option.map ~f:FastLaunchSnapshotConfigurationResponse.of_xml)
          (Xml.child xml_arg0 "snapshotConfiguration") in
      let resourceType =
        (Option.map ~f:FastLaunchResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let imageId =
        (Option.map ~f:ImageId.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?stateTransitionTime ?stateTransitionReason ?state ?ownerId
        ?maxParallelLaunches ?launchTemplate ?snapshotConfiguration
        ?resourceType ?imageId ()
    let of_json json =
      let stateTransitionTime =
        field_map json "StateTransitionTime" MillisecondDateTime.of_json in
      let stateTransitionReason =
        field_map json "StateTransitionReason" String_.of_json in
      let state = field_map json "State" FastLaunchStateCode.of_json in
      let ownerId = field_map json "OwnerId" String_.of_json in
      let maxParallelLaunches =
        field_map json "MaxParallelLaunches" Integer.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate"
          FastLaunchLaunchTemplateSpecificationResponse.of_json in
      let snapshotConfiguration =
        field_map json "SnapshotConfiguration"
          FastLaunchSnapshotConfigurationResponse.of_json in
      let resourceType =
        field_map json "ResourceType" FastLaunchResourceType.of_json in
      let imageId = field_map json "ImageId" ImageId.of_json in
      make ?stateTransitionTime ?stateTransitionReason ?state ?ownerId
        ?maxParallelLaunches ?launchTemplate ?snapshotConfiguration
        ?resourceType ?imageId ()
    let to_json v = composed_to_json to_value v
  end
module DisableFastLaunchRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t ;
      force: Boolean.t option ;
      dryRun: Boolean.t option }
    let context_ = "DisableFastLaunchRequest"
    let make ?force =
      fun ?dryRun -> fun ~imageId -> fun () -> { force; dryRun; imageId }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("Force", (Option.map x.force ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let force = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Force") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ?force ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let force = field_map json "Force" Boolean.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ?force ~imageId ()
    let to_json v = composed_to_json to_value v
  end
module DisableEbsEncryptionByDefaultResult =
  struct
    type nonrec t = {
      ebsEncryptionByDefault: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ebsEncryptionByDefault = fun () -> { ebsEncryptionByDefault }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ebsEncryptionByDefault",
           (Option.map x.ebsEncryptionByDefault ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ebsEncryptionByDefault =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ebsEncryptionByDefault") in
      make ?ebsEncryptionByDefault ()
    let of_json json =
      let ebsEncryptionByDefault =
        field_map json "EbsEncryptionByDefault" Boolean.of_json in
      make ?ebsEncryptionByDefault ()
    let to_json v = composed_to_json to_value v
  end
module DisableEbsEncryptionByDefaultRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DetachVpnGatewayRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t ;
      vpnGatewayId: VpnGatewayId.t ;
      dryRun: Boolean.t option }
    let context_ = "DetachVpnGatewayRequest"
    let make ?dryRun =
      fun ~vpcId ->
        fun ~vpnGatewayId -> fun () -> { dryRun; vpcId; vpnGatewayId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("VpnGatewayId", (Some (VpnGatewayId.to_value x.vpnGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnGatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnGatewayId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?dryRun ~vpnGatewayId ~vpcId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnGatewayId =
        field_map_exn json "VpnGatewayId" VpnGatewayId.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      make ?dryRun ~vpnGatewayId ~vpcId ()
    let to_json v = composed_to_json to_value v
  end
module DetachVolumeRequest =
  struct
    type nonrec t =
      {
      device: String_.t option ;
      force: Boolean.t option ;
      instanceId: InstanceId.t option ;
      volumeId: VolumeId.t ;
      dryRun: Boolean.t option }
    let context_ = "DetachVolumeRequest"
    let make ?device =
      fun ?force ->
        fun ?instanceId ->
          fun ?dryRun ->
            fun ~volumeId ->
              fun () -> { device; force; instanceId; dryRun; volumeId }
    let to_value x =
      structure_to_value
        [("Device", (Option.map x.device ~f:String_.to_value));
        ("Force", (Option.map x.force ~f:Boolean.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let force = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Force") in
      let device =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Device") in
      make ?dryRun ~volumeId ?instanceId ?force ?device ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let instanceId = field_map json "InstanceId" InstanceId.of_json in
      let force = field_map json "Force" Boolean.of_json in
      let device = field_map json "Device" String_.of_json in
      make ?dryRun ~volumeId ?instanceId ?force ?device ()
    let to_json v = composed_to_json to_value v
  end
module DetachNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      attachmentId: NetworkInterfaceAttachmentId.t ;
      dryRun: Boolean.t option ;
      force: Boolean.t option }
    let context_ = "DetachNetworkInterfaceRequest"
    let make ?dryRun =
      fun ?force ->
        fun ~attachmentId -> fun () -> { dryRun; force; attachmentId }
    let to_value x =
      structure_to_value
        [("attachmentId",
           (Some (NetworkInterfaceAttachmentId.to_value x.attachmentId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("force", (Option.map x.force ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let force = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "force") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let attachmentId =
        NetworkInterfaceAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attachmentId") in
      make ?force ?dryRun ~attachmentId ()
    let of_json json =
      let force = field_map json "Force" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let attachmentId =
        field_map_exn json "AttachmentId"
          NetworkInterfaceAttachmentId.of_json in
      make ?force ?dryRun ~attachmentId ()
    let to_json v = composed_to_json to_value v
  end
module DetachInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      internetGatewayId: InternetGatewayId.t ;
      vpcId: VpcId.t }
    let context_ = "DetachInternetGatewayRequest"
    let make ?dryRun =
      fun ~internetGatewayId ->
        fun ~vpcId -> fun () -> { dryRun; internetGatewayId; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Some (InternetGatewayId.to_value x.internetGatewayId)));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let internetGatewayId =
        InternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ~internetGatewayId ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let internetGatewayId =
        field_map_exn json "InternetGatewayId" InternetGatewayId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ~internetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DetachClassicLinkVpcResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DetachClassicLinkVpcRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      instanceId: InstanceId.t ;
      vpcId: VpcId.t }
    let context_ = "DetachClassicLinkVpcRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~vpcId -> fun () -> { dryRun; instanceId; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ~instanceId ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ~instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpnGatewaysResult =
  struct
    type nonrec t = {
      vpnGateways: VpnGatewayList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnGateways = fun () -> { vpnGateways }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnGatewaySet",
           (Option.map x.vpnGateways ~f:VpnGatewayList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnGateways =
        (Option.map ~f:VpnGatewayList.of_xml)
          (Xml.child xml_arg0 "vpnGatewaySet") in
      make ?vpnGateways ()
    let of_json json =
      let vpnGateways = field_map json "VpnGateways" VpnGatewayList.of_json in
      make ?vpnGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpnGatewaysRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      vpnGatewayIds: VpnGatewayIdStringList.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?vpnGatewayIds ->
        fun ?dryRun -> fun () -> { filters; vpnGatewayIds; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("VpnGatewayId",
          (Option.map x.vpnGatewayIds ~f:VpnGatewayIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnGatewayIds =
        (Option.map ~f:VpnGatewayIdStringList.of_xml)
          (Xml.child xml_arg0 "VpnGatewayId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?vpnGatewayIds ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnGatewayIds =
        field_map json "VpnGatewayIds" VpnGatewayIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?vpnGatewayIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpnConnectionsResult =
  struct
    type nonrec t = {
      vpnConnections: VpnConnectionList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnections = fun () -> { vpnConnections }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnectionSet",
           (Option.map x.vpnConnections ~f:VpnConnectionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnections =
        (Option.map ~f:VpnConnectionList.of_xml)
          (Xml.child xml_arg0 "vpnConnectionSet") in
      make ?vpnConnections ()
    let of_json json =
      let vpnConnections =
        field_map json "VpnConnections" VpnConnectionList.of_json in
      make ?vpnConnections ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpnConnectionsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      vpnConnectionIds: VpnConnectionIdStringList.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?vpnConnectionIds ->
        fun ?dryRun -> fun () -> { filters; vpnConnectionIds; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("VpnConnectionId",
          (Option.map x.vpnConnectionIds
             ~f:VpnConnectionIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnConnectionIds =
        (Option.map ~f:VpnConnectionIdStringList.of_xml)
          (Xml.child xml_arg0 "VpnConnectionId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?vpnConnectionIds ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnConnectionIds =
        field_map json "VpnConnectionIds" VpnConnectionIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?vpnConnectionIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcsResult =
  struct
    type nonrec t = {
      vpcs: VpcList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcs = fun ?nextToken -> fun () -> { vpcs; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcSet", (Option.map x.vpcs ~f:VpcList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let vpcs = (Option.map ~f:VpcList.of_xml) (Xml.child xml_arg0 "vpcSet") in
      make ?nextToken ?vpcs ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let vpcs = field_map json "Vpcs" VpcList.of_json in
      make ?nextToken ?vpcs ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      vpcIds: VpcIdStringList.t option ;
      dryRun: Boolean.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeVpcsMaxResults.t option }
    let make ?filters =
      fun ?vpcIds ->
        fun ?dryRun ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () -> { filters; vpcIds; dryRun; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("VpcId", (Option.map x.vpcIds ~f:VpcIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeVpcsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeVpcsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcIds =
        (Option.map ~f:VpcIdStringList.of_xml) (Xml.child xml_arg0 "VpcId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ?vpcIds ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeVpcsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpcIds = field_map json "VpcIds" VpcIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ?vpcIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcPeeringConnectionsResult =
  struct
    type nonrec t =
      {
      vpcPeeringConnections: VpcPeeringConnectionList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringConnections =
      fun ?nextToken -> fun () -> { vpcPeeringConnections; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcPeeringConnectionSet",
           (Option.map x.vpcPeeringConnections
              ~f:VpcPeeringConnectionList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let vpcPeeringConnections =
        (Option.map ~f:VpcPeeringConnectionList.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnectionSet") in
      make ?nextToken ?vpcPeeringConnections ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let vpcPeeringConnections =
        field_map json "VpcPeeringConnections"
          VpcPeeringConnectionList.of_json in
      make ?nextToken ?vpcPeeringConnections ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcPeeringConnectionsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      vpcPeeringConnectionIds: VpcPeeringConnectionIdList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeVpcPeeringConnectionsMaxResults.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?vpcPeeringConnectionIds ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                {
                  filters;
                  dryRun;
                  vpcPeeringConnectionIds;
                  nextToken;
                  maxResults
                }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcPeeringConnectionId",
          (Option.map x.vpcPeeringConnectionIds
             ~f:VpcPeeringConnectionIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeVpcPeeringConnectionsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeVpcPeeringConnectionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let vpcPeeringConnectionIds =
        (Option.map ~f:VpcPeeringConnectionIdList.of_xml)
          (Xml.child xml_arg0 "VpcPeeringConnectionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?vpcPeeringConnectionIds ?dryRun ?filters
        ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeVpcPeeringConnectionsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let vpcPeeringConnectionIds =
        field_map json "VpcPeeringConnectionIds"
          VpcPeeringConnectionIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?vpcPeeringConnectionIds ?dryRun ?filters
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointsResult =
  struct
    type nonrec t =
      {
      vpcEndpoints: VpcEndpointSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcEndpoints =
      fun ?nextToken -> fun () -> { vpcEndpoints; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcEndpointSet",
           (Option.map x.vpcEndpoints ~f:VpcEndpointSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let vpcEndpoints =
        (Option.map ~f:VpcEndpointSet.of_xml)
          (Xml.child xml_arg0 "vpcEndpointSet") in
      make ?nextToken ?vpcEndpoints ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let vpcEndpoints = field_map json "VpcEndpoints" VpcEndpointSet.of_json in
      make ?nextToken ?vpcEndpoints ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcEndpointIds: VpcEndpointIdList.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?vpcEndpointIds ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; vpcEndpointIds; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointIds ~f:VpcEndpointIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let vpcEndpointIds =
        (Option.map ~f:VpcEndpointIdList.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?vpcEndpointIds ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let vpcEndpointIds =
        field_map json "VpcEndpointIds" VpcEndpointIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?vpcEndpointIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointServicesResult =
  struct
    type nonrec t =
      {
      serviceNames: ValueStringList.t option ;
      serviceDetails: ServiceDetailSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serviceNames =
      fun ?serviceDetails ->
        fun ?nextToken ->
          fun () -> { serviceNames; serviceDetails; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceNameSet",
           (Option.map x.serviceNames ~f:ValueStringList.to_value));
        ("serviceDetailSet",
          (Option.map x.serviceDetails ~f:ServiceDetailSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let serviceDetails =
        (Option.map ~f:ServiceDetailSet.of_xml)
          (Xml.child xml_arg0 "serviceDetailSet") in
      let serviceNames =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "serviceNameSet") in
      make ?nextToken ?serviceDetails ?serviceNames ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let serviceDetails =
        field_map json "ServiceDetails" ServiceDetailSet.of_json in
      let serviceNames =
        field_map json "ServiceNames" ValueStringList.of_json in
      make ?nextToken ?serviceDetails ?serviceNames ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointServicesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      serviceNames: ValueStringList.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?serviceNames ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; serviceNames; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceName",
          (Option.map x.serviceNames ~f:ValueStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let serviceNames =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "ServiceName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?serviceNames ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let serviceNames =
        field_map json "ServiceNames" ValueStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?serviceNames ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointServicePermissionsResult =
  struct
    type nonrec t =
      {
      allowedPrincipals: AllowedPrincipalSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?allowedPrincipals =
      fun ?nextToken -> fun () -> { allowedPrincipals; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("allowedPrincipals",
           (Option.map x.allowedPrincipals ~f:AllowedPrincipalSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let allowedPrincipals =
        (Option.map ~f:AllowedPrincipalSet.of_xml)
          (Xml.child xml_arg0 "allowedPrincipals") in
      make ?nextToken ?allowedPrincipals ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let allowedPrincipals =
        field_map json "AllowedPrincipals" AllowedPrincipalSet.of_json in
      make ?nextToken ?allowedPrincipals ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointServicePermissionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      serviceId: VpcEndpointServiceId.t ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let context_ = "DescribeVpcEndpointServicePermissionsRequest"
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ~serviceId ->
              fun () -> { dryRun; filters; maxResults; nextToken; serviceId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ~serviceId ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ~serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointServiceConfigurationsResult =
  struct
    type nonrec t =
      {
      serviceConfigurations: ServiceConfigurationSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serviceConfigurations =
      fun ?nextToken -> fun () -> { serviceConfigurations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceConfigurationSet",
           (Option.map x.serviceConfigurations
              ~f:ServiceConfigurationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let serviceConfigurations =
        (Option.map ~f:ServiceConfigurationSet.of_xml)
          (Xml.child xml_arg0 "serviceConfigurationSet") in
      make ?nextToken ?serviceConfigurations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let serviceConfigurations =
        field_map json "ServiceConfigurations"
          ServiceConfigurationSet.of_json in
      make ?nextToken ?serviceConfigurations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointServiceConfigurationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      serviceIds: VpcEndpointServiceIdList.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?serviceIds ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; serviceIds; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId",
          (Option.map x.serviceIds ~f:VpcEndpointServiceIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let serviceIds =
        (Option.map ~f:VpcEndpointServiceIdList.of_xml)
          (Xml.child xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?serviceIds ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let serviceIds =
        field_map json "ServiceIds" VpcEndpointServiceIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?serviceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointConnectionsResult =
  struct
    type nonrec t =
      {
      vpcEndpointConnections: VpcEndpointConnectionSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcEndpointConnections =
      fun ?nextToken -> fun () -> { vpcEndpointConnections; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcEndpointConnectionSet",
           (Option.map x.vpcEndpointConnections
              ~f:VpcEndpointConnectionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let vpcEndpointConnections =
        (Option.map ~f:VpcEndpointConnectionSet.of_xml)
          (Xml.child xml_arg0 "vpcEndpointConnectionSet") in
      make ?nextToken ?vpcEndpointConnections ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let vpcEndpointConnections =
        field_map json "VpcEndpointConnections"
          VpcEndpointConnectionSet.of_json in
      make ?nextToken ?vpcEndpointConnections ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointConnectionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { dryRun; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointConnectionNotificationsResult =
  struct
    type nonrec t =
      {
      connectionNotificationSet: ConnectionNotificationSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?connectionNotificationSet =
      fun ?nextToken -> fun () -> { connectionNotificationSet; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("connectionNotificationSet",
           (Option.map x.connectionNotificationSet
              ~f:ConnectionNotificationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let connectionNotificationSet =
        (Option.map ~f:ConnectionNotificationSet.of_xml)
          (Xml.child xml_arg0 "connectionNotificationSet") in
      make ?nextToken ?connectionNotificationSet ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let connectionNotificationSet =
        field_map json "ConnectionNotificationSet"
          ConnectionNotificationSet.of_json in
      make ?nextToken ?connectionNotificationSet ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcEndpointConnectionNotificationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      connectionNotificationId: ConnectionNotificationId.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?connectionNotificationId ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  dryRun;
                  connectionNotificationId;
                  filters;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ConnectionNotificationId",
          (Option.map x.connectionNotificationId
             ~f:ConnectionNotificationId.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let connectionNotificationId =
        (Option.map ~f:ConnectionNotificationId.of_xml)
          (Xml.child xml_arg0 "ConnectionNotificationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?connectionNotificationId ?dryRun
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let connectionNotificationId =
        field_map json "ConnectionNotificationId"
          ConnectionNotificationId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?connectionNotificationId ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcClassicLinkResult =
  struct
    type nonrec t = {
      vpcs: VpcClassicLinkList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcs = fun () -> { vpcs }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcSet", (Option.map x.vpcs ~f:VpcClassicLinkList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcs =
        (Option.map ~f:VpcClassicLinkList.of_xml)
          (Xml.child xml_arg0 "vpcSet") in
      make ?vpcs ()
    let of_json json =
      let vpcs = field_map json "Vpcs" VpcClassicLinkList.of_json in
      make ?vpcs ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcClassicLinkRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      vpcIds: VpcClassicLinkIdList.t option }
    let make ?filters =
      fun ?dryRun -> fun ?vpcIds -> fun () -> { filters; dryRun; vpcIds }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcId", (Option.map x.vpcIds ~f:VpcClassicLinkIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcIds =
        (Option.map ~f:VpcClassicLinkIdList.of_xml)
          (Xml.child xml_arg0 "VpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?vpcIds ?dryRun ?filters ()
    let of_json json =
      let vpcIds = field_map json "VpcIds" VpcClassicLinkIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?vpcIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcClassicLinkDnsSupportResult =
  struct
    type nonrec t =
      {
      nextToken: DescribeVpcClassicLinkDnsSupportNextToken.t option ;
      vpcs: ClassicLinkDnsSupportList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?vpcs -> fun () -> { nextToken; vpcs }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken",
           (Option.map x.nextToken
              ~f:DescribeVpcClassicLinkDnsSupportNextToken.to_value));
        ("vpcs", (Option.map x.vpcs ~f:ClassicLinkDnsSupportList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcs =
        (Option.map ~f:ClassicLinkDnsSupportList.of_xml)
          (Xml.child xml_arg0 "vpcs") in
      let nextToken =
        (Option.map ~f:DescribeVpcClassicLinkDnsSupportNextToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?vpcs ?nextToken ()
    let of_json json =
      let vpcs = field_map json "Vpcs" ClassicLinkDnsSupportList.of_json in
      let nextToken =
        field_map json "NextToken"
          DescribeVpcClassicLinkDnsSupportNextToken.of_json in
      make ?vpcs ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcClassicLinkDnsSupportRequest =
  struct
    type nonrec t =
      {
      maxResults: DescribeVpcClassicLinkDnsSupportMaxResults.t option ;
      nextToken: DescribeVpcClassicLinkDnsSupportNextToken.t option ;
      vpcIds: VpcClassicLinkIdList.t option }
    let make ?maxResults =
      fun ?nextToken ->
        fun ?vpcIds -> fun () -> { maxResults; nextToken; vpcIds }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults
              ~f:DescribeVpcClassicLinkDnsSupportMaxResults.to_value));
        ("nextToken",
          (Option.map x.nextToken
             ~f:DescribeVpcClassicLinkDnsSupportNextToken.to_value));
        ("VpcIds", (Option.map x.vpcIds ~f:VpcClassicLinkIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcIds =
        (Option.map ~f:VpcClassicLinkIdList.of_xml)
          (Xml.child xml_arg0 "VpcIds") in
      let nextToken =
        (Option.map ~f:DescribeVpcClassicLinkDnsSupportNextToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:DescribeVpcClassicLinkDnsSupportMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?vpcIds ?nextToken ?maxResults ()
    let of_json json =
      let vpcIds = field_map json "VpcIds" VpcClassicLinkIdList.of_json in
      let nextToken =
        field_map json "NextToken"
          DescribeVpcClassicLinkDnsSupportNextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeVpcClassicLinkDnsSupportMaxResults.of_json in
      make ?vpcIds ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcAttributeResult =
  struct
    type nonrec t =
      {
      vpcId: String_.t option ;
      enableDnsHostnames: AttributeBooleanValue.t option ;
      enableDnsSupport: AttributeBooleanValue.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcId =
      fun ?enableDnsHostnames ->
        fun ?enableDnsSupport ->
          fun () -> { vpcId; enableDnsHostnames; enableDnsSupport }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcId", (Option.map x.vpcId ~f:String_.to_value));
        ("enableDnsHostnames",
          (Option.map x.enableDnsHostnames ~f:AttributeBooleanValue.to_value));
        ("enableDnsSupport",
          (Option.map x.enableDnsSupport ~f:AttributeBooleanValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableDnsSupport =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "enableDnsSupport") in
      let enableDnsHostnames =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "enableDnsHostnames") in
      let vpcId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "vpcId") in
      make ?enableDnsSupport ?enableDnsHostnames ?vpcId ()
    let of_json json =
      let enableDnsSupport =
        field_map json "EnableDnsSupport" AttributeBooleanValue.of_json in
      let enableDnsHostnames =
        field_map json "EnableDnsHostnames" AttributeBooleanValue.of_json in
      let vpcId = field_map json "VpcId" String_.of_json in
      make ?enableDnsSupport ?enableDnsHostnames ?vpcId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVpcAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: VpcAttributeName.t ;
      vpcId: VpcId.t ;
      dryRun: Boolean.t option }
    let context_ = "DescribeVpcAttributeRequest"
    let make ?dryRun =
      fun ~attribute -> fun ~vpcId -> fun () -> { dryRun; attribute; vpcId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (VpcAttributeName.to_value x.attribute)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let attribute =
        VpcAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~vpcId ~attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let attribute = field_map_exn json "Attribute" VpcAttributeName.of_json in
      make ?dryRun ~vpcId ~attribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumesResult =
  struct
    type nonrec t =
      {
      volumes: VolumeList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?volumes = fun ?nextToken -> fun () -> { volumes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("volumeSet", (Option.map x.volumes ~f:VolumeList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let volumes =
        (Option.map ~f:VolumeList.of_xml) (Xml.child xml_arg0 "volumeSet") in
      make ?nextToken ?volumes ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let volumes = field_map json "Volumes" VolumeList.of_json in
      make ?nextToken ?volumes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      volumeIds: VolumeIdStringList.t option ;
      dryRun: Boolean.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?filters =
      fun ?volumeIds ->
        fun ?dryRun ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () -> { filters; volumeIds; dryRun; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("VolumeId", (Option.map x.volumeIds ~f:VolumeIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeIds =
        (Option.map ~f:VolumeIdStringList.of_xml)
          (Xml.child xml_arg0 "VolumeId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?maxResults ?dryRun ?volumeIds ?filters ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeIds = field_map json "VolumeIds" VolumeIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?nextToken ?maxResults ?dryRun ?volumeIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumesModificationsResult =
  struct
    type nonrec t =
      {
      volumesModifications: VolumeModificationList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?volumesModifications =
      fun ?nextToken -> fun () -> { volumesModifications; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("volumeModificationSet",
           (Option.map x.volumesModifications
              ~f:VolumeModificationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let volumesModifications =
        (Option.map ~f:VolumeModificationList.of_xml)
          (Xml.child xml_arg0 "volumeModificationSet") in
      make ?nextToken ?volumesModifications ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let volumesModifications =
        field_map json "VolumesModifications" VolumeModificationList.of_json in
      make ?nextToken ?volumesModifications ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumesModificationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      volumeIds: VolumeIdStringList.t option ;
      filters: FilterList.t option ;
      nextToken: String_.t option ;
      maxResults: Integer.t option }
    let make ?dryRun =
      fun ?volumeIds ->
        fun ?filters ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () -> { dryRun; volumeIds; filters; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VolumeId", (Option.map x.volumeIds ~f:VolumeIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let volumeIds =
        (Option.map ~f:VolumeIdStringList.of_xml)
          (Xml.child xml_arg0 "VolumeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?maxResults ?nextToken ?filters ?volumeIds ?dryRun ()
    let of_json json =
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let volumeIds = field_map json "VolumeIds" VolumeIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?maxResults ?nextToken ?filters ?volumeIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumeStatusResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      volumeStatuses: VolumeStatusList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?volumeStatuses -> fun () -> { nextToken; volumeStatuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("volumeStatusSet",
          (Option.map x.volumeStatuses ~f:VolumeStatusList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeStatuses =
        (Option.map ~f:VolumeStatusList.of_xml)
          (Xml.child xml_arg0 "volumeStatusSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?volumeStatuses ?nextToken ()
    let of_json json =
      let volumeStatuses =
        field_map json "VolumeStatuses" VolumeStatusList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?volumeStatuses ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumeStatusRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      volumeIds: VolumeIdStringList.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?volumeIds ->
            fun ?dryRun ->
              fun () -> { filters; maxResults; nextToken; volumeIds; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("VolumeId", (Option.map x.volumeIds ~f:VolumeIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeIds =
        (Option.map ~f:VolumeIdStringList.of_xml)
          (Xml.child xml_arg0 "VolumeId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?volumeIds ?nextToken ?maxResults ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeIds = field_map json "VolumeIds" VolumeIdStringList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?volumeIds ?nextToken ?maxResults ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumeAttributeResult =
  struct
    type nonrec t =
      {
      autoEnableIO: AttributeBooleanValue.t option ;
      productCodes: ProductCodeList.t option ;
      volumeId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?autoEnableIO =
      fun ?productCodes ->
        fun ?volumeId -> fun () -> { autoEnableIO; productCodes; volumeId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("autoEnableIO",
           (Option.map x.autoEnableIO ~f:AttributeBooleanValue.to_value));
        ("productCodes",
          (Option.map x.productCodes ~f:ProductCodeList.to_value));
        ("volumeId", (Option.map x.volumeId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "volumeId") in
      let productCodes =
        (Option.map ~f:ProductCodeList.of_xml)
          (Xml.child xml_arg0 "productCodes") in
      let autoEnableIO =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "autoEnableIO") in
      make ?volumeId ?productCodes ?autoEnableIO ()
    let of_json json =
      let volumeId = field_map json "VolumeId" String_.of_json in
      let productCodes =
        field_map json "ProductCodes" ProductCodeList.of_json in
      let autoEnableIO =
        field_map json "AutoEnableIO" AttributeBooleanValue.of_json in
      make ?volumeId ?productCodes ?autoEnableIO ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVolumeAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: VolumeAttributeName.t ;
      volumeId: VolumeId.t ;
      dryRun: Boolean.t option }
    let context_ = "DescribeVolumeAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~volumeId -> fun () -> { dryRun; attribute; volumeId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (VolumeAttributeName.to_value x.attribute)));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let attribute =
        VolumeAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~volumeId ~attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let attribute =
        field_map_exn json "Attribute" VolumeAttributeName.of_json in
      make ?dryRun ~volumeId ~attribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrunkInterfaceAssociationsResult =
  struct
    type nonrec t =
      {
      interfaceAssociations: TrunkInterfaceAssociationList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?interfaceAssociations =
      fun ?nextToken -> fun () -> { interfaceAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("interfaceAssociationSet",
           (Option.map x.interfaceAssociations
              ~f:TrunkInterfaceAssociationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let interfaceAssociations =
        (Option.map ~f:TrunkInterfaceAssociationList.of_xml)
          (Xml.child xml_arg0 "interfaceAssociationSet") in
      make ?nextToken ?interfaceAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let interfaceAssociations =
        field_map json "InterfaceAssociations"
          TrunkInterfaceAssociationList.of_json in
      make ?nextToken ?interfaceAssociations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrunkInterfaceAssociationsRequest =
  struct
    type nonrec t =
      {
      associationIds: TrunkInterfaceAssociationIdList.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeTrunkInterfaceAssociationsMaxResults.t option }
    let make ?associationIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { associationIds; dryRun; filters; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("AssociationId",
           (Option.map x.associationIds
              ~f:TrunkInterfaceAssociationIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeTrunkInterfaceAssociationsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeTrunkInterfaceAssociationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let associationIds =
        (Option.map ~f:TrunkInterfaceAssociationIdList.of_xml)
          (Xml.child xml_arg0 "AssociationId") in
      make ?maxResults ?nextToken ?filters ?dryRun ?associationIds ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeTrunkInterfaceAssociationsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let associationIds =
        field_map json "AssociationIds"
          TrunkInterfaceAssociationIdList.of_json in
      make ?maxResults ?nextToken ?filters ?dryRun ?associationIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewaysResult =
  struct
    type nonrec t =
      {
      transitGateways: TransitGatewayList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGateways =
      fun ?nextToken -> fun () -> { transitGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewaySet",
           (Option.map x.transitGateways ~f:TransitGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGateways =
        (Option.map ~f:TransitGatewayList.of_xml)
          (Xml.child xml_arg0 "transitGatewaySet") in
      make ?nextToken ?transitGateways ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGateways =
        field_map json "TransitGateways" TransitGatewayList.of_json in
      make ?nextToken ?transitGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewaysRequest =
  struct
    type nonrec t =
      {
      transitGatewayIds: TransitGatewayIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { transitGatewayIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("TransitGatewayIds",
           (Option.map x.transitGatewayIds
              ~f:TransitGatewayIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayIds =
        (Option.map ~f:TransitGatewayIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayIds") in
      make ?dryRun ?nextToken ?maxResults ?filters ?transitGatewayIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayIds =
        field_map json "TransitGatewayIds" TransitGatewayIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?transitGatewayIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayVpcAttachmentsResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachments: TransitGatewayVpcAttachmentList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachments =
      fun ?nextToken -> fun () -> { transitGatewayVpcAttachments; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachments",
           (Option.map x.transitGatewayVpcAttachments
              ~f:TransitGatewayVpcAttachmentList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayVpcAttachments =
        (Option.map ~f:TransitGatewayVpcAttachmentList.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachments") in
      make ?nextToken ?transitGatewayVpcAttachments ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayVpcAttachments =
        field_map json "TransitGatewayVpcAttachments"
          TransitGatewayVpcAttachmentList.of_json in
      make ?nextToken ?transitGatewayVpcAttachments ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayVpcAttachmentsRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentIds:
        TransitGatewayAttachmentIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayAttachmentIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayAttachmentIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentIds",
           (Option.map x.transitGatewayAttachmentIds
              ~f:TransitGatewayAttachmentIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayAttachmentIds =
        (Option.map ~f:TransitGatewayAttachmentIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayAttachmentIds =
        field_map json "TransitGatewayAttachmentIds"
          TransitGatewayAttachmentIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayRouteTablesResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTables: TransitGatewayRouteTableList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTables =
      fun ?nextToken -> fun () -> { transitGatewayRouteTables; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTables",
           (Option.map x.transitGatewayRouteTables
              ~f:TransitGatewayRouteTableList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayRouteTables =
        (Option.map ~f:TransitGatewayRouteTableList.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTables") in
      make ?nextToken ?transitGatewayRouteTables ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayRouteTables =
        field_map json "TransitGatewayRouteTables"
          TransitGatewayRouteTableList.of_json in
      make ?nextToken ?transitGatewayRouteTables ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayRouteTablesRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableIds:
        TransitGatewayRouteTableIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayRouteTableIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayRouteTableIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableIds",
           (Option.map x.transitGatewayRouteTableIds
              ~f:TransitGatewayRouteTableIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayRouteTableIds =
        (Option.map ~f:TransitGatewayRouteTableIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayRouteTableIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayRouteTableIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayRouteTableIds =
        field_map json "TransitGatewayRouteTableIds"
          TransitGatewayRouteTableIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayRouteTableIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayPeeringAttachmentsResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachments:
        TransitGatewayPeeringAttachmentList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachments =
      fun ?nextToken ->
        fun () -> { transitGatewayPeeringAttachments; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachments",
           (Option.map x.transitGatewayPeeringAttachments
              ~f:TransitGatewayPeeringAttachmentList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayPeeringAttachments =
        (Option.map ~f:TransitGatewayPeeringAttachmentList.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachments") in
      make ?nextToken ?transitGatewayPeeringAttachments ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayPeeringAttachments =
        field_map json "TransitGatewayPeeringAttachments"
          TransitGatewayPeeringAttachmentList.of_json in
      make ?nextToken ?transitGatewayPeeringAttachments ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayPeeringAttachmentsRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentIds:
        TransitGatewayAttachmentIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayAttachmentIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayAttachmentIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentIds",
           (Option.map x.transitGatewayAttachmentIds
              ~f:TransitGatewayAttachmentIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayAttachmentIds =
        (Option.map ~f:TransitGatewayAttachmentIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayAttachmentIds =
        field_map json "TransitGatewayAttachmentIds"
          TransitGatewayAttachmentIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayMulticastDomainsResult =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomains:
        TransitGatewayMulticastDomainList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMulticastDomains =
      fun ?nextToken ->
        fun () -> { transitGatewayMulticastDomains; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMulticastDomains",
           (Option.map x.transitGatewayMulticastDomains
              ~f:TransitGatewayMulticastDomainList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayMulticastDomains =
        (Option.map ~f:TransitGatewayMulticastDomainList.of_xml)
          (Xml.child xml_arg0 "transitGatewayMulticastDomains") in
      make ?nextToken ?transitGatewayMulticastDomains ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayMulticastDomains =
        field_map json "TransitGatewayMulticastDomains"
          TransitGatewayMulticastDomainList.of_json in
      make ?nextToken ?transitGatewayMulticastDomains ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayMulticastDomainsRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainIds:
        TransitGatewayMulticastDomainIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayMulticastDomainIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayMulticastDomainIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainIds",
           (Option.map x.transitGatewayMulticastDomainIds
              ~f:TransitGatewayMulticastDomainIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayMulticastDomainIds =
        (Option.map ~f:TransitGatewayMulticastDomainIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayMulticastDomainIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayMulticastDomainIds =
        field_map json "TransitGatewayMulticastDomainIds"
          TransitGatewayMulticastDomainIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayMulticastDomainIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayConnectsResult =
  struct
    type nonrec t =
      {
      transitGatewayConnects: TransitGatewayConnectList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnects =
      fun ?nextToken -> fun () -> { transitGatewayConnects; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnectSet",
           (Option.map x.transitGatewayConnects
              ~f:TransitGatewayConnectList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayConnects =
        (Option.map ~f:TransitGatewayConnectList.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnectSet") in
      make ?nextToken ?transitGatewayConnects ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayConnects =
        field_map json "TransitGatewayConnects"
          TransitGatewayConnectList.of_json in
      make ?nextToken ?transitGatewayConnects ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayConnectsRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentIds:
        TransitGatewayAttachmentIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayAttachmentIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayAttachmentIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentIds",
           (Option.map x.transitGatewayAttachmentIds
              ~f:TransitGatewayAttachmentIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayAttachmentIds =
        (Option.map ~f:TransitGatewayAttachmentIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayAttachmentIds =
        field_map json "TransitGatewayAttachmentIds"
          TransitGatewayAttachmentIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayConnectPeersResult =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeers: TransitGatewayConnectPeerList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnectPeers =
      fun ?nextToken -> fun () -> { transitGatewayConnectPeers; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnectPeerSet",
           (Option.map x.transitGatewayConnectPeers
              ~f:TransitGatewayConnectPeerList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayConnectPeers =
        (Option.map ~f:TransitGatewayConnectPeerList.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnectPeerSet") in
      make ?nextToken ?transitGatewayConnectPeers ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayConnectPeers =
        field_map json "TransitGatewayConnectPeers"
          TransitGatewayConnectPeerList.of_json in
      make ?nextToken ?transitGatewayConnectPeers ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayConnectPeersRequest =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeerIds:
        TransitGatewayConnectPeerIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayConnectPeerIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayConnectPeerIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayConnectPeerIds",
           (Option.map x.transitGatewayConnectPeerIds
              ~f:TransitGatewayConnectPeerIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayConnectPeerIds =
        (Option.map ~f:TransitGatewayConnectPeerIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayConnectPeerIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayConnectPeerIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayConnectPeerIds =
        field_map json "TransitGatewayConnectPeerIds"
          TransitGatewayConnectPeerIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayConnectPeerIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayAttachmentsResult =
  struct
    type nonrec t =
      {
      transitGatewayAttachments: TransitGatewayAttachmentList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayAttachments =
      fun ?nextToken -> fun () -> { transitGatewayAttachments; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayAttachments",
           (Option.map x.transitGatewayAttachments
              ~f:TransitGatewayAttachmentList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayAttachments =
        (Option.map ~f:TransitGatewayAttachmentList.of_xml)
          (Xml.child xml_arg0 "transitGatewayAttachments") in
      make ?nextToken ?transitGatewayAttachments ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayAttachments =
        field_map json "TransitGatewayAttachments"
          TransitGatewayAttachmentList.of_json in
      make ?nextToken ?transitGatewayAttachments ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTransitGatewayAttachmentsRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentIds:
        TransitGatewayAttachmentIdStringList.t option ;
      filters: FilterList.t option ;
      maxResults: TransitGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayAttachmentIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayAttachmentIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentIds",
           (Option.map x.transitGatewayAttachmentIds
              ~f:TransitGatewayAttachmentIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayAttachmentIds =
        (Option.map ~f:TransitGatewayAttachmentIdStringList.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentIds") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayAttachmentIds =
        field_map json "TransitGatewayAttachmentIds"
          TransitGatewayAttachmentIdStringList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayAttachmentIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrafficMirrorTargetsResult =
  struct
    type nonrec t =
      {
      trafficMirrorTargets: TrafficMirrorTargetSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorTargets =
      fun ?nextToken -> fun () -> { trafficMirrorTargets; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorTargetSet",
           (Option.map x.trafficMirrorTargets
              ~f:TrafficMirrorTargetSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let trafficMirrorTargets =
        (Option.map ~f:TrafficMirrorTargetSet.of_xml)
          (Xml.child xml_arg0 "trafficMirrorTargetSet") in
      make ?nextToken ?trafficMirrorTargets ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let trafficMirrorTargets =
        field_map json "TrafficMirrorTargets" TrafficMirrorTargetSet.of_json in
      make ?nextToken ?trafficMirrorTargets ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrafficMirrorTargetsRequest =
  struct
    type nonrec t =
      {
      trafficMirrorTargetIds: TrafficMirrorTargetIdList.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: TrafficMirroringMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?trafficMirrorTargetIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  trafficMirrorTargetIds;
                  dryRun;
                  filters;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("TrafficMirrorTargetId",
           (Option.map x.trafficMirrorTargetIds
              ~f:TrafficMirrorTargetIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TrafficMirroringMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TrafficMirroringMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorTargetIds =
        (Option.map ~f:TrafficMirrorTargetIdList.of_xml)
          (Xml.child xml_arg0 "TrafficMirrorTargetId") in
      make ?nextToken ?maxResults ?filters ?dryRun ?trafficMirrorTargetIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" TrafficMirroringMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorTargetIds =
        field_map json "TrafficMirrorTargetIds"
          TrafficMirrorTargetIdList.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ?trafficMirrorTargetIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrafficMirrorSessionsResult =
  struct
    type nonrec t =
      {
      trafficMirrorSessions: TrafficMirrorSessionSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorSessions =
      fun ?nextToken -> fun () -> { trafficMirrorSessions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorSessionSet",
           (Option.map x.trafficMirrorSessions
              ~f:TrafficMirrorSessionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let trafficMirrorSessions =
        (Option.map ~f:TrafficMirrorSessionSet.of_xml)
          (Xml.child xml_arg0 "trafficMirrorSessionSet") in
      make ?nextToken ?trafficMirrorSessions ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let trafficMirrorSessions =
        field_map json "TrafficMirrorSessions"
          TrafficMirrorSessionSet.of_json in
      make ?nextToken ?trafficMirrorSessions ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrafficMirrorSessionsRequest =
  struct
    type nonrec t =
      {
      trafficMirrorSessionIds: TrafficMirrorSessionIdList.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: TrafficMirroringMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?trafficMirrorSessionIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  trafficMirrorSessionIds;
                  dryRun;
                  filters;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("TrafficMirrorSessionId",
           (Option.map x.trafficMirrorSessionIds
              ~f:TrafficMirrorSessionIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TrafficMirroringMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TrafficMirroringMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorSessionIds =
        (Option.map ~f:TrafficMirrorSessionIdList.of_xml)
          (Xml.child xml_arg0 "TrafficMirrorSessionId") in
      make ?nextToken ?maxResults ?filters ?dryRun ?trafficMirrorSessionIds
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" TrafficMirroringMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorSessionIds =
        field_map json "TrafficMirrorSessionIds"
          TrafficMirrorSessionIdList.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ?trafficMirrorSessionIds
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrafficMirrorFiltersResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilters: TrafficMirrorFilterSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilters =
      fun ?nextToken -> fun () -> { trafficMirrorFilters; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterSet",
           (Option.map x.trafficMirrorFilters
              ~f:TrafficMirrorFilterSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let trafficMirrorFilters =
        (Option.map ~f:TrafficMirrorFilterSet.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterSet") in
      make ?nextToken ?trafficMirrorFilters ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let trafficMirrorFilters =
        field_map json "TrafficMirrorFilters" TrafficMirrorFilterSet.of_json in
      make ?nextToken ?trafficMirrorFilters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrafficMirrorFiltersRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterIds: TrafficMirrorFilterIdList.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: TrafficMirroringMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?trafficMirrorFilterIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  trafficMirrorFilterIds;
                  dryRun;
                  filters;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterId",
           (Option.map x.trafficMirrorFilterIds
              ~f:TrafficMirrorFilterIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TrafficMirroringMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TrafficMirroringMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorFilterIds =
        (Option.map ~f:TrafficMirrorFilterIdList.of_xml)
          (Xml.child xml_arg0 "TrafficMirrorFilterId") in
      make ?nextToken ?maxResults ?filters ?dryRun ?trafficMirrorFilterIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" TrafficMirroringMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorFilterIds =
        field_map json "TrafficMirrorFilterIds"
          TrafficMirrorFilterIdList.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ?trafficMirrorFilterIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTagsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      tags: TagDescriptionList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?tags -> fun () -> { nextToken; tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagDescriptionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagDescriptionList.of_xml)
          (Xml.child xml_arg0 "tagSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?tags ?nextToken ()
    let of_json json =
      let tags = field_map json "Tags" TagDescriptionList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?tags ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTagsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { dryRun; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSubnetsResult =
  struct
    type nonrec t =
      {
      subnets: SubnetList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnets = fun ?nextToken -> fun () -> { subnets; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnetSet", (Option.map x.subnets ~f:SubnetList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let subnets =
        (Option.map ~f:SubnetList.of_xml) (Xml.child xml_arg0 "subnetSet") in
      make ?nextToken ?subnets ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let subnets = field_map json "Subnets" SubnetList.of_json in
      make ?nextToken ?subnets ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSubnetsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      subnetIds: SubnetIdStringList.t option ;
      dryRun: Boolean.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeSubnetsMaxResults.t option }
    let make ?filters =
      fun ?subnetIds ->
        fun ?dryRun ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () -> { filters; subnetIds; dryRun; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("SubnetId", (Option.map x.subnetIds ~f:SubnetIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeSubnetsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeSubnetsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let subnetIds =
        (Option.map ~f:SubnetIdStringList.of_xml)
          (Xml.child xml_arg0 "SubnetId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ?subnetIds ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeSubnetsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetIds = field_map json "SubnetIds" SubnetIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ?subnetIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStoreImageTasksResult =
  struct
    type nonrec t =
      {
      storeImageTaskResults: StoreImageTaskResultSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?storeImageTaskResults =
      fun ?nextToken -> fun () -> { storeImageTaskResults; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("storeImageTaskResultSet",
           (Option.map x.storeImageTaskResults
              ~f:StoreImageTaskResultSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let storeImageTaskResults =
        (Option.map ~f:StoreImageTaskResultSet.of_xml)
          (Xml.child xml_arg0 "storeImageTaskResultSet") in
      make ?nextToken ?storeImageTaskResults ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let storeImageTaskResults =
        field_map json "StoreImageTaskResults"
          StoreImageTaskResultSet.of_json in
      make ?nextToken ?storeImageTaskResults ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStoreImageTasksRequest =
  struct
    type nonrec t =
      {
      imageIds: ImageIdList.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeStoreImageTasksRequestMaxResults.t option }
    let make ?imageIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () -> { imageIds; dryRun; filters; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("ImageId", (Option.map x.imageIds ~f:ImageIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeStoreImageTasksRequestMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeStoreImageTasksRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let imageIds =
        (Option.map ~f:ImageIdList.of_xml) (Xml.child xml_arg0 "ImageId") in
      make ?maxResults ?nextToken ?filters ?dryRun ?imageIds ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeStoreImageTasksRequestMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let imageIds = field_map json "ImageIds" ImageIdList.of_json in
      make ?maxResults ?nextToken ?filters ?dryRun ?imageIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStaleSecurityGroupsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      staleSecurityGroupSet: StaleSecurityGroupSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?staleSecurityGroupSet ->
        fun () -> { nextToken; staleSecurityGroupSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("staleSecurityGroupSet",
          (Option.map x.staleSecurityGroupSet
             ~f:StaleSecurityGroupSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let staleSecurityGroupSet =
        (Option.map ~f:StaleSecurityGroupSet.of_xml)
          (Xml.child xml_arg0 "staleSecurityGroupSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?staleSecurityGroupSet ?nextToken ()
    let of_json json =
      let staleSecurityGroupSet =
        field_map json "StaleSecurityGroupSet" StaleSecurityGroupSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?staleSecurityGroupSet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStaleSecurityGroupsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      maxResults: DescribeStaleSecurityGroupsMaxResults.t option ;
      nextToken: DescribeStaleSecurityGroupsNextToken.t option ;
      vpcId: VpcId.t }
    let context_ = "DescribeStaleSecurityGroupsRequest"
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~vpcId -> fun () -> { dryRun; maxResults; nextToken; vpcId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeStaleSecurityGroupsMaxResults.to_value));
        ("NextToken",
          (Option.map x.nextToken
             ~f:DescribeStaleSecurityGroupsNextToken.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let nextToken =
        (Option.map ~f:DescribeStaleSecurityGroupsNextToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeStaleSecurityGroupsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcId ?nextToken ?maxResults ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let nextToken =
        field_map json "NextToken"
          DescribeStaleSecurityGroupsNextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeStaleSecurityGroupsMaxResults.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotPriceHistoryResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      spotPriceHistory: SpotPriceHistoryList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?spotPriceHistory -> fun () -> { nextToken; spotPriceHistory }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotPriceHistorySet",
          (Option.map x.spotPriceHistory ~f:SpotPriceHistoryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotPriceHistory =
        (Option.map ~f:SpotPriceHistoryList.of_xml)
          (Xml.child xml_arg0 "spotPriceHistorySet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?spotPriceHistory ?nextToken ()
    let of_json json =
      let spotPriceHistory =
        field_map json "SpotPriceHistory" SpotPriceHistoryList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?spotPriceHistory ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotPriceHistoryRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      availabilityZone: String_.t option ;
      dryRun: Boolean.t option ;
      endTime: DateTime.t option ;
      instanceTypes: InstanceTypeList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      productDescriptions: ProductDescriptionList.t option ;
      startTime: DateTime.t option }
    let make ?filters =
      fun ?availabilityZone ->
        fun ?dryRun ->
          fun ?endTime ->
            fun ?instanceTypes ->
              fun ?maxResults ->
                fun ?nextToken ->
                  fun ?productDescriptions ->
                    fun ?startTime ->
                      fun () ->
                        {
                          filters;
                          availabilityZone;
                          dryRun;
                          endTime;
                          instanceTypes;
                          maxResults;
                          nextToken;
                          productDescriptions;
                          startTime
                        }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("availabilityZone",
          (Option.map x.availabilityZone ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("endTime", (Option.map x.endTime ~f:DateTime.to_value));
        ("InstanceType",
          (Option.map x.instanceTypes ~f:InstanceTypeList.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("ProductDescription",
          (Option.map x.productDescriptions
             ~f:ProductDescriptionList.to_value));
        ("startTime", (Option.map x.startTime ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "startTime") in
      let productDescriptions =
        (Option.map ~f:ProductDescriptionList.of_xml)
          (Xml.child xml_arg0 "ProductDescription") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let instanceTypes =
        (Option.map ~f:InstanceTypeList.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let endTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "endTime") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "availabilityZone") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?startTime ?productDescriptions ?nextToken ?maxResults
        ?instanceTypes ?endTime ?dryRun ?availabilityZone ?filters ()
    let of_json json =
      let startTime = field_map json "StartTime" DateTime.of_json in
      let productDescriptions =
        field_map json "ProductDescriptions" ProductDescriptionList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let instanceTypes =
        field_map json "InstanceTypes" InstanceTypeList.of_json in
      let endTime = field_map json "EndTime" DateTime.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let availabilityZone =
        field_map json "AvailabilityZone" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?startTime ?productDescriptions ?nextToken ?maxResults
        ?instanceTypes ?endTime ?dryRun ?availabilityZone ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotInstanceRequestsResult =
  struct
    type nonrec t =
      {
      spotInstanceRequests: SpotInstanceRequestList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotInstanceRequests =
      fun ?nextToken -> fun () -> { spotInstanceRequests; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotInstanceRequestSet",
           (Option.map x.spotInstanceRequests
              ~f:SpotInstanceRequestList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let spotInstanceRequests =
        (Option.map ~f:SpotInstanceRequestList.of_xml)
          (Xml.child xml_arg0 "spotInstanceRequestSet") in
      make ?nextToken ?spotInstanceRequests ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let spotInstanceRequests =
        field_map json "SpotInstanceRequests" SpotInstanceRequestList.of_json in
      make ?nextToken ?spotInstanceRequests ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotInstanceRequestsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      spotInstanceRequestIds: SpotInstanceRequestIdList.t option ;
      nextToken: String_.t option ;
      maxResults: Integer.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?spotInstanceRequestIds ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                {
                  filters;
                  dryRun;
                  spotInstanceRequestIds;
                  nextToken;
                  maxResults
                }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SpotInstanceRequestId",
          (Option.map x.spotInstanceRequestIds
             ~f:SpotInstanceRequestIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let spotInstanceRequestIds =
        (Option.map ~f:SpotInstanceRequestIdList.of_xml)
          (Xml.child xml_arg0 "SpotInstanceRequestId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?spotInstanceRequestIds ?dryRun ?filters ()
    let of_json json =
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let spotInstanceRequestIds =
        field_map json "SpotInstanceRequestIds"
          SpotInstanceRequestIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?spotInstanceRequestIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotFleetRequestsResponse =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      spotFleetRequestConfigs: SpotFleetRequestConfigSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?spotFleetRequestConfigs ->
        fun () -> { nextToken; spotFleetRequestConfigs }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotFleetRequestConfigSet",
          (Option.map x.spotFleetRequestConfigs
             ~f:SpotFleetRequestConfigSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotFleetRequestConfigs =
        (Option.map ~f:SpotFleetRequestConfigSet.of_xml)
          (Xml.child xml_arg0 "spotFleetRequestConfigSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?spotFleetRequestConfigs ?nextToken ()
    let of_json json =
      let spotFleetRequestConfigs =
        field_map json "SpotFleetRequestConfigs"
          SpotFleetRequestConfigSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?spotFleetRequestConfigs ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotFleetRequestsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      spotFleetRequestIds: SpotFleetRequestIdList.t option }
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?spotFleetRequestIds ->
            fun () -> { dryRun; maxResults; nextToken; spotFleetRequestIds }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotFleetRequestId",
          (Option.map x.spotFleetRequestIds
             ~f:SpotFleetRequestIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotFleetRequestIds =
        (Option.map ~f:SpotFleetRequestIdList.of_xml)
          (Xml.child xml_arg0 "spotFleetRequestId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?spotFleetRequestIds ?nextToken ?maxResults ?dryRun ()
    let of_json json =
      let spotFleetRequestIds =
        field_map json "SpotFleetRequestIds" SpotFleetRequestIdList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?spotFleetRequestIds ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotFleetRequestHistoryResponse =
  struct
    type nonrec t =
      {
      historyRecords: HistoryRecords.t option ;
      lastEvaluatedTime: DateTime.t option ;
      nextToken: String_.t option ;
      spotFleetRequestId: String_.t option ;
      startTime: DateTime.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?historyRecords =
      fun ?lastEvaluatedTime ->
        fun ?nextToken ->
          fun ?spotFleetRequestId ->
            fun ?startTime ->
              fun () ->
                {
                  historyRecords;
                  lastEvaluatedTime;
                  nextToken;
                  spotFleetRequestId;
                  startTime
                }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("historyRecordSet",
           (Option.map x.historyRecords ~f:HistoryRecords.to_value));
        ("lastEvaluatedTime",
          (Option.map x.lastEvaluatedTime ~f:DateTime.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotFleetRequestId",
          (Option.map x.spotFleetRequestId ~f:String_.to_value));
        ("startTime", (Option.map x.startTime ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "startTime") in
      let spotFleetRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "spotFleetRequestId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let lastEvaluatedTime =
        (Option.map ~f:DateTime.of_xml)
          (Xml.child xml_arg0 "lastEvaluatedTime") in
      let historyRecords =
        (Option.map ~f:HistoryRecords.of_xml)
          (Xml.child xml_arg0 "historyRecordSet") in
      make ?startTime ?spotFleetRequestId ?nextToken ?lastEvaluatedTime
        ?historyRecords ()
    let of_json json =
      let startTime = field_map json "StartTime" DateTime.of_json in
      let spotFleetRequestId =
        field_map json "SpotFleetRequestId" String_.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let lastEvaluatedTime =
        field_map json "LastEvaluatedTime" DateTime.of_json in
      let historyRecords =
        field_map json "HistoryRecords" HistoryRecords.of_json in
      make ?startTime ?spotFleetRequestId ?nextToken ?lastEvaluatedTime
        ?historyRecords ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotFleetRequestHistoryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      eventType: EventType.t option ;
      maxResults: DescribeSpotFleetRequestHistoryMaxResults.t option ;
      nextToken: String_.t option ;
      spotFleetRequestId: SpotFleetRequestId.t ;
      startTime: DateTime.t }
    let context_ = "DescribeSpotFleetRequestHistoryRequest"
    let make ?dryRun =
      fun ?eventType ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ~spotFleetRequestId ->
              fun ~startTime ->
                fun () ->
                  {
                    dryRun;
                    eventType;
                    maxResults;
                    nextToken;
                    spotFleetRequestId;
                    startTime
                  }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("eventType", (Option.map x.eventType ~f:EventType.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:DescribeSpotFleetRequestHistoryMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotFleetRequestId",
          (Some (SpotFleetRequestId.to_value x.spotFleetRequestId)));
        ("startTime", (Some (DateTime.to_value x.startTime)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        DateTime.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "startTime") in
      let spotFleetRequestId =
        SpotFleetRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "spotFleetRequestId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:DescribeSpotFleetRequestHistoryMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let eventType =
        (Option.map ~f:EventType.of_xml) (Xml.child xml_arg0 "eventType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~startTime ~spotFleetRequestId ?nextToken ?maxResults ?eventType
        ?dryRun ()
    let of_json json =
      let startTime = field_map_exn json "StartTime" DateTime.of_json in
      let spotFleetRequestId =
        field_map_exn json "SpotFleetRequestId" SpotFleetRequestId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeSpotFleetRequestHistoryMaxResults.of_json in
      let eventType = field_map json "EventType" EventType.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~startTime ~spotFleetRequestId ?nextToken ?maxResults ?eventType
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotFleetInstancesResponse =
  struct
    type nonrec t =
      {
      activeInstances: ActiveInstanceSet.t option ;
      nextToken: String_.t option ;
      spotFleetRequestId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?activeInstances =
      fun ?nextToken ->
        fun ?spotFleetRequestId ->
          fun () -> { activeInstances; nextToken; spotFleetRequestId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("activeInstanceSet",
           (Option.map x.activeInstances ~f:ActiveInstanceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotFleetRequestId",
          (Option.map x.spotFleetRequestId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotFleetRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "spotFleetRequestId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let activeInstances =
        (Option.map ~f:ActiveInstanceSet.of_xml)
          (Xml.child xml_arg0 "activeInstanceSet") in
      make ?spotFleetRequestId ?nextToken ?activeInstances ()
    let of_json json =
      let spotFleetRequestId =
        field_map json "SpotFleetRequestId" String_.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let activeInstances =
        field_map json "ActiveInstances" ActiveInstanceSet.of_json in
      make ?spotFleetRequestId ?nextToken ?activeInstances ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotFleetInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      maxResults: DescribeSpotFleetInstancesMaxResults.t option ;
      nextToken: String_.t option ;
      spotFleetRequestId: SpotFleetRequestId.t }
    let context_ = "DescribeSpotFleetInstancesRequest"
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~spotFleetRequestId ->
            fun () -> { dryRun; maxResults; nextToken; spotFleetRequestId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:DescribeSpotFleetInstancesMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("spotFleetRequestId",
          (Some (SpotFleetRequestId.to_value x.spotFleetRequestId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotFleetRequestId =
        SpotFleetRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "spotFleetRequestId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:DescribeSpotFleetInstancesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~spotFleetRequestId ?nextToken ?maxResults ?dryRun ()
    let of_json json =
      let spotFleetRequestId =
        field_map_exn json "SpotFleetRequestId" SpotFleetRequestId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeSpotFleetInstancesMaxResults.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~spotFleetRequestId ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotDatafeedSubscriptionResult =
  struct
    type nonrec t =
      {
      spotDatafeedSubscription: SpotDatafeedSubscription.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotDatafeedSubscription =
      fun () -> { spotDatafeedSubscription }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotDatafeedSubscription",
           (Option.map x.spotDatafeedSubscription
              ~f:SpotDatafeedSubscription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotDatafeedSubscription =
        (Option.map ~f:SpotDatafeedSubscription.of_xml)
          (Xml.child xml_arg0 "spotDatafeedSubscription") in
      make ?spotDatafeedSubscription ()
    let of_json json =
      let spotDatafeedSubscription =
        field_map json "SpotDatafeedSubscription"
          SpotDatafeedSubscription.of_json in
      make ?spotDatafeedSubscription ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSpotDatafeedSubscriptionRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSnapshotsResult =
  struct
    type nonrec t =
      {
      snapshots: SnapshotList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshots =
      fun ?nextToken -> fun () -> { snapshots; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotSet", (Option.map x.snapshots ~f:SnapshotList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let snapshots =
        (Option.map ~f:SnapshotList.of_xml)
          (Xml.child xml_arg0 "snapshotSet") in
      make ?nextToken ?snapshots ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let snapshots = field_map json "Snapshots" SnapshotList.of_json in
      make ?nextToken ?snapshots ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSnapshotsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      ownerIds: OwnerStringList.t option ;
      restorableByUserIds: RestorableByStringList.t option ;
      snapshotIds: SnapshotIdStringList.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?ownerIds ->
            fun ?restorableByUserIds ->
              fun ?snapshotIds ->
                fun ?dryRun ->
                  fun () ->
                    {
                      filters;
                      maxResults;
                      nextToken;
                      ownerIds;
                      restorableByUserIds;
                      snapshotIds;
                      dryRun
                    }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Owner", (Option.map x.ownerIds ~f:OwnerStringList.to_value));
        ("RestorableBy",
          (Option.map x.restorableByUserIds
             ~f:RestorableByStringList.to_value));
        ("SnapshotId",
          (Option.map x.snapshotIds ~f:SnapshotIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let snapshotIds =
        (Option.map ~f:SnapshotIdStringList.of_xml)
          (Xml.child xml_arg0 "SnapshotId") in
      let restorableByUserIds =
        (Option.map ~f:RestorableByStringList.of_xml)
          (Xml.child xml_arg0 "RestorableBy") in
      let ownerIds =
        (Option.map ~f:OwnerStringList.of_xml) (Xml.child xml_arg0 "Owner") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?snapshotIds ?restorableByUserIds ?ownerIds ?nextToken
        ?maxResults ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let snapshotIds =
        field_map json "SnapshotIds" SnapshotIdStringList.of_json in
      let restorableByUserIds =
        field_map json "RestorableByUserIds" RestorableByStringList.of_json in
      let ownerIds = field_map json "OwnerIds" OwnerStringList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?snapshotIds ?restorableByUserIds ?ownerIds ?nextToken
        ?maxResults ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSnapshotTierStatusResult =
  struct
    type nonrec t =
      {
      snapshotTierStatuses: SnapshotTierStatusSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshotTierStatuses =
      fun ?nextToken -> fun () -> { snapshotTierStatuses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotTierStatusSet",
           (Option.map x.snapshotTierStatuses
              ~f:SnapshotTierStatusSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let snapshotTierStatuses =
        (Option.map ~f:SnapshotTierStatusSet.of_xml)
          (Xml.child xml_arg0 "snapshotTierStatusSet") in
      make ?nextToken ?snapshotTierStatuses ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let snapshotTierStatuses =
        field_map json "SnapshotTierStatuses" SnapshotTierStatusSet.of_json in
      make ?nextToken ?snapshotTierStatuses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSnapshotTierStatusRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeSnapshotTierStatusMaxResults.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () -> { filters; dryRun; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSnapshotTierStatusMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeSnapshotTierStatusMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeSnapshotTierStatusMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSnapshotAttributeResult =
  struct
    type nonrec t =
      {
      createVolumePermissions: CreateVolumePermissionList.t option ;
      productCodes: ProductCodeList.t option ;
      snapshotId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?createVolumePermissions =
      fun ?productCodes ->
        fun ?snapshotId ->
          fun () -> { createVolumePermissions; productCodes; snapshotId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("createVolumePermission",
           (Option.map x.createVolumePermissions
              ~f:CreateVolumePermissionList.to_value));
        ("productCodes",
          (Option.map x.productCodes ~f:ProductCodeList.to_value));
        ("snapshotId", (Option.map x.snapshotId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snapshotId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "snapshotId") in
      let productCodes =
        (Option.map ~f:ProductCodeList.of_xml)
          (Xml.child xml_arg0 "productCodes") in
      let createVolumePermissions =
        (Option.map ~f:CreateVolumePermissionList.of_xml)
          (Xml.child xml_arg0 "createVolumePermission") in
      make ?snapshotId ?productCodes ?createVolumePermissions ()
    let of_json json =
      let snapshotId = field_map json "SnapshotId" String_.of_json in
      let productCodes =
        field_map json "ProductCodes" ProductCodeList.of_json in
      let createVolumePermissions =
        field_map json "CreateVolumePermissions"
          CreateVolumePermissionList.of_json in
      make ?snapshotId ?productCodes ?createVolumePermissions ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSnapshotAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: SnapshotAttributeName.t ;
      snapshotId: SnapshotId.t ;
      dryRun: Boolean.t option }
    let context_ = "DescribeSnapshotAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~snapshotId -> fun () -> { dryRun; attribute; snapshotId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (SnapshotAttributeName.to_value x.attribute)));
        ("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      let attribute =
        SnapshotAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~snapshotId ~attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      let attribute =
        field_map_exn json "Attribute" SnapshotAttributeName.of_json in
      make ?dryRun ~snapshotId ~attribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityGroupsResult =
  struct
    type nonrec t =
      {
      securityGroups: SecurityGroupList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroups =
      fun ?nextToken -> fun () -> { securityGroups; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupInfo",
           (Option.map x.securityGroups ~f:SecurityGroupList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let securityGroups =
        (Option.map ~f:SecurityGroupList.of_xml)
          (Xml.child xml_arg0 "securityGroupInfo") in
      make ?nextToken ?securityGroups ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let securityGroups =
        field_map json "SecurityGroups" SecurityGroupList.of_json in
      make ?nextToken ?securityGroups ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityGroupsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      groupIds: GroupIdStringList.t option ;
      groupNames: GroupNameStringList.t option ;
      dryRun: Boolean.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeSecurityGroupsMaxResults.t option }
    let make ?filters =
      fun ?groupIds ->
        fun ?groupNames ->
          fun ?dryRun ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun () ->
                  {
                    filters;
                    groupIds;
                    groupNames;
                    dryRun;
                    nextToken;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("GroupId", (Option.map x.groupIds ~f:GroupIdStringList.to_value));
        ("GroupName",
          (Option.map x.groupNames ~f:GroupNameStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSecurityGroupsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeSecurityGroupsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let groupNames =
        (Option.map ~f:GroupNameStringList.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupIds =
        (Option.map ~f:GroupIdStringList.of_xml)
          (Xml.child xml_arg0 "GroupId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ?groupNames ?groupIds ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeSecurityGroupsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let groupNames =
        field_map json "GroupNames" GroupNameStringList.of_json in
      let groupIds = field_map json "GroupIds" GroupIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ?groupNames ?groupIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityGroupRulesResult =
  struct
    type nonrec t =
      {
      securityGroupRules: SecurityGroupRuleList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroupRules =
      fun ?nextToken -> fun () -> { securityGroupRules; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupRuleSet",
           (Option.map x.securityGroupRules ~f:SecurityGroupRuleList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let securityGroupRules =
        (Option.map ~f:SecurityGroupRuleList.of_xml)
          (Xml.child xml_arg0 "securityGroupRuleSet") in
      make ?nextToken ?securityGroupRules ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let securityGroupRules =
        field_map json "SecurityGroupRules" SecurityGroupRuleList.of_json in
      make ?nextToken ?securityGroupRules ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityGroupRulesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      securityGroupRuleIds: SecurityGroupRuleIdList.t option ;
      dryRun: Boolean.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeSecurityGroupRulesMaxResults.t option }
    let make ?filters =
      fun ?securityGroupRuleIds ->
        fun ?dryRun ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                {
                  filters;
                  securityGroupRuleIds;
                  dryRun;
                  nextToken;
                  maxResults
                }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("SecurityGroupRuleId",
          (Option.map x.securityGroupRuleIds
             ~f:SecurityGroupRuleIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSecurityGroupRulesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeSecurityGroupRulesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let securityGroupRuleIds =
        (Option.map ~f:SecurityGroupRuleIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupRuleId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ?securityGroupRuleIds ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeSecurityGroupRulesMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let securityGroupRuleIds =
        field_map json "SecurityGroupRuleIds" SecurityGroupRuleIdList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ?securityGroupRuleIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityGroupReferencesResult =
  struct
    type nonrec t =
      {
      securityGroupReferenceSet: SecurityGroupReferences.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroupReferenceSet =
      fun () -> { securityGroupReferenceSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupReferenceSet",
           (Option.map x.securityGroupReferenceSet
              ~f:SecurityGroupReferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupReferenceSet =
        (Option.map ~f:SecurityGroupReferences.of_xml)
          (Xml.child xml_arg0 "securityGroupReferenceSet") in
      make ?securityGroupReferenceSet ()
    let of_json json =
      let securityGroupReferenceSet =
        field_map json "SecurityGroupReferenceSet"
          SecurityGroupReferences.of_json in
      make ?securityGroupReferenceSet ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityGroupReferencesRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      groupId: GroupIds.t }
    let context_ = "DescribeSecurityGroupReferencesRequest"
    let make ?dryRun = fun ~groupId -> fun () -> { dryRun; groupId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("GroupId", (Some (GroupIds.to_value x.groupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        GroupIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "GroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~groupId ?dryRun ()
    let of_json json =
      let groupId = field_map_exn json "GroupId" GroupIds.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~groupId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeScheduledInstancesResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      scheduledInstanceSet: ScheduledInstanceSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?scheduledInstanceSet ->
        fun () -> { nextToken; scheduledInstanceSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("scheduledInstanceSet",
          (Option.map x.scheduledInstanceSet ~f:ScheduledInstanceSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledInstanceSet =
        (Option.map ~f:ScheduledInstanceSet.of_xml)
          (Xml.child xml_arg0 "scheduledInstanceSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?scheduledInstanceSet ?nextToken ()
    let of_json json =
      let scheduledInstanceSet =
        field_map json "ScheduledInstanceSet" ScheduledInstanceSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?scheduledInstanceSet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeScheduledInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      scheduledInstanceIds: ScheduledInstanceIdRequestSet.t option ;
      slotStartTimeRange: SlotStartTimeRangeRequest.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?scheduledInstanceIds ->
              fun ?slotStartTimeRange ->
                fun () ->
                  {
                    dryRun;
                    filters;
                    maxResults;
                    nextToken;
                    scheduledInstanceIds;
                    slotStartTimeRange
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("ScheduledInstanceId",
          (Option.map x.scheduledInstanceIds
             ~f:ScheduledInstanceIdRequestSet.to_value));
        ("SlotStartTimeRange",
          (Option.map x.slotStartTimeRange
             ~f:SlotStartTimeRangeRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let slotStartTimeRange =
        (Option.map ~f:SlotStartTimeRangeRequest.of_xml)
          (Xml.child xml_arg0 "SlotStartTimeRange") in
      let scheduledInstanceIds =
        (Option.map ~f:ScheduledInstanceIdRequestSet.of_xml)
          (Xml.child xml_arg0 "ScheduledInstanceId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?slotStartTimeRange ?scheduledInstanceIds ?nextToken ?maxResults
        ?filters ?dryRun ()
    let of_json json =
      let slotStartTimeRange =
        field_map json "SlotStartTimeRange" SlotStartTimeRangeRequest.of_json in
      let scheduledInstanceIds =
        field_map json "ScheduledInstanceIds"
          ScheduledInstanceIdRequestSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?slotStartTimeRange ?scheduledInstanceIds ?nextToken ?maxResults
        ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeScheduledInstanceAvailabilityResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      scheduledInstanceAvailabilitySet:
        ScheduledInstanceAvailabilitySet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?scheduledInstanceAvailabilitySet ->
        fun () -> { nextToken; scheduledInstanceAvailabilitySet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("scheduledInstanceAvailabilitySet",
          (Option.map x.scheduledInstanceAvailabilitySet
             ~f:ScheduledInstanceAvailabilitySet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledInstanceAvailabilitySet =
        (Option.map ~f:ScheduledInstanceAvailabilitySet.of_xml)
          (Xml.child xml_arg0 "scheduledInstanceAvailabilitySet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?scheduledInstanceAvailabilitySet ?nextToken ()
    let of_json json =
      let scheduledInstanceAvailabilitySet =
        field_map json "ScheduledInstanceAvailabilitySet"
          ScheduledInstanceAvailabilitySet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?scheduledInstanceAvailabilitySet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeScheduledInstanceAvailabilityRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      firstSlotStartTimeRange: SlotDateTimeRangeRequest.t ;
      maxResults: DescribeScheduledInstanceAvailabilityMaxResults.t option ;
      maxSlotDurationInHours: Integer.t option ;
      minSlotDurationInHours: Integer.t option ;
      nextToken: String_.t option ;
      recurrence: ScheduledInstanceRecurrenceRequest.t }
    let context_ = "DescribeScheduledInstanceAvailabilityRequest"
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?maxSlotDurationInHours ->
            fun ?minSlotDurationInHours ->
              fun ?nextToken ->
                fun ~firstSlotStartTimeRange ->
                  fun ~recurrence ->
                    fun () ->
                      {
                        dryRun;
                        filters;
                        maxResults;
                        maxSlotDurationInHours;
                        minSlotDurationInHours;
                        nextToken;
                        firstSlotStartTimeRange;
                        recurrence
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("FirstSlotStartTimeRange",
          (Some (SlotDateTimeRangeRequest.to_value x.firstSlotStartTimeRange)));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeScheduledInstanceAvailabilityMaxResults.to_value));
        ("MaxSlotDurationInHours",
          (Option.map x.maxSlotDurationInHours ~f:Integer.to_value));
        ("MinSlotDurationInHours",
          (Option.map x.minSlotDurationInHours ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Recurrence",
          (Some (ScheduledInstanceRecurrenceRequest.to_value x.recurrence)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recurrence =
        ScheduledInstanceRecurrenceRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Recurrence") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let minSlotDurationInHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MinSlotDurationInHours") in
      let maxSlotDurationInHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxSlotDurationInHours") in
      let maxResults =
        (Option.map ~f:DescribeScheduledInstanceAvailabilityMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let firstSlotStartTimeRange =
        SlotDateTimeRangeRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FirstSlotStartTimeRange") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~recurrence ?nextToken ?minSlotDurationInHours
        ?maxSlotDurationInHours ?maxResults ~firstSlotStartTimeRange ?filters
        ?dryRun ()
    let of_json json =
      let recurrence =
        field_map_exn json "Recurrence"
          ScheduledInstanceRecurrenceRequest.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let minSlotDurationInHours =
        field_map json "MinSlotDurationInHours" Integer.of_json in
      let maxSlotDurationInHours =
        field_map json "MaxSlotDurationInHours" Integer.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeScheduledInstanceAvailabilityMaxResults.of_json in
      let firstSlotStartTimeRange =
        field_map_exn json "FirstSlotStartTimeRange"
          SlotDateTimeRangeRequest.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~recurrence ?nextToken ?minSlotDurationInHours
        ?maxSlotDurationInHours ?maxResults ~firstSlotStartTimeRange ?filters
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeRouteTablesResult =
  struct
    type nonrec t =
      {
      routeTables: RouteTableList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeTables =
      fun ?nextToken -> fun () -> { routeTables; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeTableSet",
           (Option.map x.routeTables ~f:RouteTableList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routeTables =
        (Option.map ~f:RouteTableList.of_xml)
          (Xml.child xml_arg0 "routeTableSet") in
      make ?nextToken ?routeTables ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let routeTables = field_map json "RouteTables" RouteTableList.of_json in
      make ?nextToken ?routeTables ()
    let to_json v = composed_to_json to_value v
  end
module DescribeRouteTablesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      routeTableIds: RouteTableIdStringList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeRouteTablesMaxResults.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?routeTableIds ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { filters; dryRun; routeTableIds; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("RouteTableId",
          (Option.map x.routeTableIds ~f:RouteTableIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeRouteTablesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeRouteTablesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let routeTableIds =
        (Option.map ~f:RouteTableIdStringList.of_xml)
          (Xml.child xml_arg0 "RouteTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?routeTableIds ?dryRun ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeRouteTablesMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let routeTableIds =
        field_map json "RouteTableIds" RouteTableIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?routeTableIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesResult =
  struct
    type nonrec t = {
      reservedInstances: ReservedInstancesList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstances = fun () -> { reservedInstances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesSet",
           (Option.map x.reservedInstances ~f:ReservedInstancesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstances =
        (Option.map ~f:ReservedInstancesList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesSet") in
      make ?reservedInstances ()
    let of_json json =
      let reservedInstances =
        field_map json "ReservedInstances" ReservedInstancesList.of_json in
      make ?reservedInstances ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      offeringClass: OfferingClassType.t option ;
      reservedInstancesIds: ReservedInstancesIdStringList.t option ;
      dryRun: Boolean.t option ;
      offeringType: OfferingTypeValues.t option }
    let make ?filters =
      fun ?offeringClass ->
        fun ?reservedInstancesIds ->
          fun ?dryRun ->
            fun ?offeringType ->
              fun () ->
                {
                  filters;
                  offeringClass;
                  reservedInstancesIds;
                  dryRun;
                  offeringType
                }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("OfferingClass",
          (Option.map x.offeringClass ~f:OfferingClassType.to_value));
        ("ReservedInstancesId",
          (Option.map x.reservedInstancesIds
             ~f:ReservedInstancesIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("offeringType",
          (Option.map x.offeringType ~f:OfferingTypeValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringType =
        (Option.map ~f:OfferingTypeValues.of_xml)
          (Xml.child xml_arg0 "offeringType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let reservedInstancesIds =
        (Option.map ~f:ReservedInstancesIdStringList.of_xml)
          (Xml.child xml_arg0 "ReservedInstancesId") in
      let offeringClass =
        (Option.map ~f:OfferingClassType.of_xml)
          (Xml.child xml_arg0 "OfferingClass") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?offeringType ?dryRun ?reservedInstancesIds ?offeringClass
        ?filters ()
    let of_json json =
      let offeringType =
        field_map json "OfferingType" OfferingTypeValues.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let reservedInstancesIds =
        field_map json "ReservedInstancesIds"
          ReservedInstancesIdStringList.of_json in
      let offeringClass =
        field_map json "OfferingClass" OfferingClassType.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?offeringType ?dryRun ?reservedInstancesIds ?offeringClass
        ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesOfferingsResult =
  struct
    type nonrec t =
      {
      reservedInstancesOfferings: ReservedInstancesOfferingList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesOfferings =
      fun ?nextToken -> fun () -> { reservedInstancesOfferings; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesOfferingsSet",
           (Option.map x.reservedInstancesOfferings
              ~f:ReservedInstancesOfferingList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reservedInstancesOfferings =
        (Option.map ~f:ReservedInstancesOfferingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesOfferingsSet") in
      make ?nextToken ?reservedInstancesOfferings ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let reservedInstancesOfferings =
        field_map json "ReservedInstancesOfferings"
          ReservedInstancesOfferingList.of_json in
      make ?nextToken ?reservedInstancesOfferings ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesOfferingsRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t option ;
      filters: FilterList.t option ;
      includeMarketplace: Boolean.t option ;
      instanceType: InstanceType.t option ;
      maxDuration: Long.t option ;
      maxInstanceCount: Integer.t option ;
      minDuration: Long.t option ;
      offeringClass: OfferingClassType.t option ;
      productDescription: RIProductDescription.t option ;
      reservedInstancesOfferingIds:
        ReservedInstancesOfferingIdStringList.t option ;
      dryRun: Boolean.t option ;
      instanceTenancy: Tenancy.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      offeringType: OfferingTypeValues.t option }
    let make ?availabilityZone =
      fun ?filters ->
        fun ?includeMarketplace ->
          fun ?instanceType ->
            fun ?maxDuration ->
              fun ?maxInstanceCount ->
                fun ?minDuration ->
                  fun ?offeringClass ->
                    fun ?productDescription ->
                      fun ?reservedInstancesOfferingIds ->
                        fun ?dryRun ->
                          fun ?instanceTenancy ->
                            fun ?maxResults ->
                              fun ?nextToken ->
                                fun ?offeringType ->
                                  fun () ->
                                    {
                                      availabilityZone;
                                      filters;
                                      includeMarketplace;
                                      instanceType;
                                      maxDuration;
                                      maxInstanceCount;
                                      minDuration;
                                      offeringClass;
                                      productDescription;
                                      reservedInstancesOfferingIds;
                                      dryRun;
                                      instanceTenancy;
                                      maxResults;
                                      nextToken;
                                      offeringType
                                    }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("IncludeMarketplace",
          (Option.map x.includeMarketplace ~f:Boolean.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:InstanceType.to_value));
        ("MaxDuration", (Option.map x.maxDuration ~f:Long.to_value));
        ("MaxInstanceCount",
          (Option.map x.maxInstanceCount ~f:Integer.to_value));
        ("MinDuration", (Option.map x.minDuration ~f:Long.to_value));
        ("OfferingClass",
          (Option.map x.offeringClass ~f:OfferingClassType.to_value));
        ("ProductDescription",
          (Option.map x.productDescription ~f:RIProductDescription.to_value));
        ("ReservedInstancesOfferingId",
          (Option.map x.reservedInstancesOfferingIds
             ~f:ReservedInstancesOfferingIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceTenancy",
          (Option.map x.instanceTenancy ~f:Tenancy.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("offeringType",
          (Option.map x.offeringType ~f:OfferingTypeValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringType =
        (Option.map ~f:OfferingTypeValues.of_xml)
          (Xml.child xml_arg0 "offeringType") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let instanceTenancy =
        (Option.map ~f:Tenancy.of_xml) (Xml.child xml_arg0 "instanceTenancy") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let reservedInstancesOfferingIds =
        (Option.map ~f:ReservedInstancesOfferingIdStringList.of_xml)
          (Xml.child xml_arg0 "ReservedInstancesOfferingId") in
      let productDescription =
        (Option.map ~f:RIProductDescription.of_xml)
          (Xml.child xml_arg0 "ProductDescription") in
      let offeringClass =
        (Option.map ~f:OfferingClassType.of_xml)
          (Xml.child xml_arg0 "OfferingClass") in
      let minDuration =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MinDuration") in
      let maxInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxInstanceCount") in
      let maxDuration =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MaxDuration") in
      let instanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let includeMarketplace =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeMarketplace") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?offeringType ?nextToken ?maxResults ?instanceTenancy ?dryRun
        ?reservedInstancesOfferingIds ?productDescription ?offeringClass
        ?minDuration ?maxInstanceCount ?maxDuration ?instanceType
        ?includeMarketplace ?filters ?availabilityZone ()
    let of_json json =
      let offeringType =
        field_map json "OfferingType" OfferingTypeValues.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let instanceTenancy = field_map json "InstanceTenancy" Tenancy.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let reservedInstancesOfferingIds =
        field_map json "ReservedInstancesOfferingIds"
          ReservedInstancesOfferingIdStringList.of_json in
      let productDescription =
        field_map json "ProductDescription" RIProductDescription.of_json in
      let offeringClass =
        field_map json "OfferingClass" OfferingClassType.of_json in
      let minDuration = field_map json "MinDuration" Long.of_json in
      let maxInstanceCount =
        field_map json "MaxInstanceCount" Integer.of_json in
      let maxDuration = field_map json "MaxDuration" Long.of_json in
      let instanceType = field_map json "InstanceType" InstanceType.of_json in
      let includeMarketplace =
        field_map json "IncludeMarketplace" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let availabilityZone =
        field_map json "AvailabilityZone" String_.of_json in
      make ?offeringType ?nextToken ?maxResults ?instanceTenancy ?dryRun
        ?reservedInstancesOfferingIds ?productDescription ?offeringClass
        ?minDuration ?maxInstanceCount ?maxDuration ?instanceType
        ?includeMarketplace ?filters ?availabilityZone ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesModificationsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      reservedInstancesModifications:
        ReservedInstancesModificationList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?reservedInstancesModifications ->
        fun () -> { nextToken; reservedInstancesModifications }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reservedInstancesModificationsSet",
          (Option.map x.reservedInstancesModifications
             ~f:ReservedInstancesModificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesModifications =
        (Option.map ~f:ReservedInstancesModificationList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesModificationsSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reservedInstancesModifications ?nextToken ()
    let of_json json =
      let reservedInstancesModifications =
        field_map json "ReservedInstancesModifications"
          ReservedInstancesModificationList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?reservedInstancesModifications ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesModificationsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      reservedInstancesModificationIds:
        ReservedInstancesModificationIdStringList.t option ;
      nextToken: String_.t option }
    let make ?filters =
      fun ?reservedInstancesModificationIds ->
        fun ?nextToken ->
          fun () -> { filters; reservedInstancesModificationIds; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("ReservedInstancesModificationId",
          (Option.map x.reservedInstancesModificationIds
             ~f:ReservedInstancesModificationIdStringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reservedInstancesModificationIds =
        (Option.map ~f:ReservedInstancesModificationIdStringList.of_xml)
          (Xml.child xml_arg0 "ReservedInstancesModificationId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?reservedInstancesModificationIds ?filters ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let reservedInstancesModificationIds =
        field_map json "ReservedInstancesModificationIds"
          ReservedInstancesModificationIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?nextToken ?reservedInstancesModificationIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesListingsResult =
  struct
    type nonrec t =
      {
      reservedInstancesListings: ReservedInstancesListingList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesListings =
      fun () -> { reservedInstancesListings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesListingsSet",
           (Option.map x.reservedInstancesListings
              ~f:ReservedInstancesListingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListings =
        (Option.map ~f:ReservedInstancesListingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingsSet") in
      make ?reservedInstancesListings ()
    let of_json json =
      let reservedInstancesListings =
        field_map json "ReservedInstancesListings"
          ReservedInstancesListingList.of_json in
      make ?reservedInstancesListings ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReservedInstancesListingsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      reservedInstancesId: ReservationId.t option ;
      reservedInstancesListingId: ReservedInstancesListingId.t option }
    let make ?filters =
      fun ?reservedInstancesId ->
        fun ?reservedInstancesListingId ->
          fun () ->
            { filters; reservedInstancesId; reservedInstancesListingId }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("reservedInstancesId",
          (Option.map x.reservedInstancesId ~f:ReservationId.to_value));
        ("reservedInstancesListingId",
          (Option.map x.reservedInstancesListingId
             ~f:ReservedInstancesListingId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListingId =
        (Option.map ~f:ReservedInstancesListingId.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingId") in
      let reservedInstancesId =
        (Option.map ~f:ReservationId.of_xml)
          (Xml.child xml_arg0 "reservedInstancesId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?reservedInstancesListingId ?reservedInstancesId ?filters ()
    let of_json json =
      let reservedInstancesListingId =
        field_map json "ReservedInstancesListingId"
          ReservedInstancesListingId.of_json in
      let reservedInstancesId =
        field_map json "ReservedInstancesId" ReservationId.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?reservedInstancesListingId ?reservedInstancesId ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReplaceRootVolumeTasksResult =
  struct
    type nonrec t =
      {
      replaceRootVolumeTasks: ReplaceRootVolumeTasks.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?replaceRootVolumeTasks =
      fun ?nextToken -> fun () -> { replaceRootVolumeTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("replaceRootVolumeTaskSet",
           (Option.map x.replaceRootVolumeTasks
              ~f:ReplaceRootVolumeTasks.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let replaceRootVolumeTasks =
        (Option.map ~f:ReplaceRootVolumeTasks.of_xml)
          (Xml.child xml_arg0 "replaceRootVolumeTaskSet") in
      make ?nextToken ?replaceRootVolumeTasks ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let replaceRootVolumeTasks =
        field_map json "ReplaceRootVolumeTasks"
          ReplaceRootVolumeTasks.of_json in
      make ?nextToken ?replaceRootVolumeTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeReplaceRootVolumeTasksRequest =
  struct
    type nonrec t =
      {
      replaceRootVolumeTaskIds: ReplaceRootVolumeTaskIds.t option ;
      filters: FilterList.t option ;
      maxResults: DescribeReplaceRootVolumeTasksMaxResults.t option ;
      nextToken: NextToken.t option ;
      dryRun: Boolean.t option }
    let make ?replaceRootVolumeTaskIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  replaceRootVolumeTaskIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("ReplaceRootVolumeTaskId",
           (Option.map x.replaceRootVolumeTaskIds
              ~f:ReplaceRootVolumeTaskIds.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeReplaceRootVolumeTasksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeReplaceRootVolumeTasksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let replaceRootVolumeTaskIds =
        (Option.map ~f:ReplaceRootVolumeTaskIds.of_xml)
          (Xml.child xml_arg0 "ReplaceRootVolumeTaskId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?replaceRootVolumeTaskIds
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeReplaceRootVolumeTasksMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let replaceRootVolumeTaskIds =
        field_map json "ReplaceRootVolumeTaskIds"
          ReplaceRootVolumeTaskIds.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?replaceRootVolumeTaskIds
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeRegionsResult =
  struct
    type nonrec t = {
      regions: RegionList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?regions = fun () -> { regions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("regionInfo", (Option.map x.regions ~f:RegionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regions =
        (Option.map ~f:RegionList.of_xml) (Xml.child xml_arg0 "regionInfo") in
      make ?regions ()
    let of_json json =
      let regions = field_map json "Regions" RegionList.of_json in
      make ?regions ()
    let to_json v = composed_to_json to_value v
  end
module DescribeRegionsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      regionNames: RegionNameStringList.t option ;
      dryRun: Boolean.t option ;
      allRegions: Boolean.t option }
    let make ?filters =
      fun ?regionNames ->
        fun ?dryRun ->
          fun ?allRegions ->
            fun () -> { filters; regionNames; dryRun; allRegions }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("RegionName",
          (Option.map x.regionNames ~f:RegionNameStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("AllRegions", (Option.map x.allRegions ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allRegions =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AllRegions") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let regionNames =
        (Option.map ~f:RegionNameStringList.of_xml)
          (Xml.child xml_arg0 "RegionName") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?allRegions ?dryRun ?regionNames ?filters ()
    let of_json json =
      let allRegions = field_map json "AllRegions" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let regionNames =
        field_map json "RegionNames" RegionNameStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?allRegions ?dryRun ?regionNames ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribePublicIpv4PoolsResult =
  struct
    type nonrec t =
      {
      publicIpv4Pools: PublicIpv4PoolSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?publicIpv4Pools =
      fun ?nextToken -> fun () -> { publicIpv4Pools; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("publicIpv4PoolSet",
           (Option.map x.publicIpv4Pools ~f:PublicIpv4PoolSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let publicIpv4Pools =
        (Option.map ~f:PublicIpv4PoolSet.of_xml)
          (Xml.child xml_arg0 "publicIpv4PoolSet") in
      make ?nextToken ?publicIpv4Pools ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let publicIpv4Pools =
        field_map json "PublicIpv4Pools" PublicIpv4PoolSet.of_json in
      make ?nextToken ?publicIpv4Pools ()
    let to_json v = composed_to_json to_value v
  end
module DescribePublicIpv4PoolsRequest =
  struct
    type nonrec t =
      {
      poolIds: PublicIpv4PoolIdStringList.t option ;
      nextToken: NextToken.t option ;
      maxResults: PoolMaxResults.t option ;
      filters: FilterList.t option }
    let make ?poolIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun () -> { poolIds; nextToken; maxResults; filters }
    let to_value x =
      structure_to_value
        [("PoolId",
           (Option.map x.poolIds ~f:PublicIpv4PoolIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:PoolMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:PoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let poolIds =
        (Option.map ~f:PublicIpv4PoolIdStringList.of_xml)
          (Xml.child xml_arg0 "PoolId") in
      make ?filters ?maxResults ?nextToken ?poolIds ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let maxResults = field_map json "MaxResults" PoolMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let poolIds =
        field_map json "PoolIds" PublicIpv4PoolIdStringList.of_json in
      make ?filters ?maxResults ?nextToken ?poolIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribePrincipalIdFormatResult =
  struct
    type nonrec t =
      {
      principals: PrincipalIdFormatList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?principals =
      fun ?nextToken -> fun () -> { principals; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("principalSet",
           (Option.map x.principals ~f:PrincipalIdFormatList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let principals =
        (Option.map ~f:PrincipalIdFormatList.of_xml)
          (Xml.child xml_arg0 "principalSet") in
      make ?nextToken ?principals ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let principals =
        field_map json "Principals" PrincipalIdFormatList.of_json in
      make ?nextToken ?principals ()
    let to_json v = composed_to_json to_value v
  end
module DescribePrincipalIdFormatRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      resources: ResourceList.t option ;
      maxResults: DescribePrincipalIdFormatMaxResults.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?resources ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { dryRun; resources; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Resource", (Option.map x.resources ~f:ResourceList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribePrincipalIdFormatMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribePrincipalIdFormatMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let resources =
        (Option.map ~f:ResourceList.of_xml) (Xml.child xml_arg0 "Resource") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?resources ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribePrincipalIdFormatMaxResults.of_json in
      let resources = field_map json "Resources" ResourceList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?resources ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribePrefixListsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      prefixLists: PrefixListSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?prefixLists -> fun () -> { nextToken; prefixLists }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("prefixListSet",
          (Option.map x.prefixLists ~f:PrefixListSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixLists =
        (Option.map ~f:PrefixListSet.of_xml)
          (Xml.child xml_arg0 "prefixListSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?prefixLists ?nextToken ()
    let of_json json =
      let prefixLists = field_map json "PrefixLists" PrefixListSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?prefixLists ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribePrefixListsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      prefixListIds: PrefixListResourceIdStringList.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?prefixListIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; prefixListIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("PrefixListId",
          (Option.map x.prefixListIds
             ~f:PrefixListResourceIdStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixListIds =
        (Option.map ~f:PrefixListResourceIdStringList.of_xml)
          (Xml.child xml_arg0 "PrefixListId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let prefixListIds =
        field_map json "PrefixListIds" PrefixListResourceIdStringList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribePlacementGroupsResult =
  struct
    type nonrec t = {
      placementGroups: PlacementGroupList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?placementGroups = fun () -> { placementGroups }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("placementGroupSet",
           (Option.map x.placementGroups ~f:PlacementGroupList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementGroups =
        (Option.map ~f:PlacementGroupList.of_xml)
          (Xml.child xml_arg0 "placementGroupSet") in
      make ?placementGroups ()
    let of_json json =
      let placementGroups =
        field_map json "PlacementGroups" PlacementGroupList.of_json in
      make ?placementGroups ()
    let to_json v = composed_to_json to_value v
  end
module DescribePlacementGroupsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      groupNames: PlacementGroupStringList.t option ;
      groupIds: PlacementGroupIdStringList.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?groupNames ->
          fun ?groupIds ->
            fun () -> { filters; dryRun; groupNames; groupIds }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupName",
          (Option.map x.groupNames ~f:PlacementGroupStringList.to_value));
        ("GroupId",
          (Option.map x.groupIds ~f:PlacementGroupIdStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupIds =
        (Option.map ~f:PlacementGroupIdStringList.of_xml)
          (Xml.child xml_arg0 "GroupId") in
      let groupNames =
        (Option.map ~f:PlacementGroupStringList.of_xml)
          (Xml.child xml_arg0 "groupName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?groupIds ?groupNames ?dryRun ?filters ()
    let of_json json =
      let groupIds =
        field_map json "GroupIds" PlacementGroupIdStringList.of_json in
      let groupNames =
        field_map json "GroupNames" PlacementGroupStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?groupIds ?groupNames ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInterfacesResult =
  struct
    type nonrec t =
      {
      networkInterfaces: NetworkInterfaceList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfaces =
      fun ?nextToken -> fun () -> { networkInterfaces; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfaceSet",
           (Option.map x.networkInterfaces ~f:NetworkInterfaceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInterfaces =
        (Option.map ~f:NetworkInterfaceList.of_xml)
          (Xml.child xml_arg0 "networkInterfaceSet") in
      make ?nextToken ?networkInterfaces ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInterfaces =
        field_map json "NetworkInterfaces" NetworkInterfaceList.of_json in
      make ?nextToken ?networkInterfaces ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInterfacesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      networkInterfaceIds: NetworkInterfaceIdList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeNetworkInterfacesMaxResults.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?networkInterfaceIds ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { filters; dryRun; networkInterfaceIds; nextToken; maxResults
                }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInterfaceId",
          (Option.map x.networkInterfaceIds
             ~f:NetworkInterfaceIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeNetworkInterfacesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeNetworkInterfacesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let networkInterfaceIds =
        (Option.map ~f:NetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filter") in
      make ?maxResults ?nextToken ?networkInterfaceIds ?dryRun ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeNetworkInterfacesMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInterfaceIds =
        field_map json "NetworkInterfaceIds" NetworkInterfaceIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?networkInterfaceIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInterfacePermissionsResult =
  struct
    type nonrec t =
      {
      networkInterfacePermissions: NetworkInterfacePermissionList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfacePermissions =
      fun ?nextToken -> fun () -> { networkInterfacePermissions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfacePermissions",
           (Option.map x.networkInterfacePermissions
              ~f:NetworkInterfacePermissionList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInterfacePermissions =
        (Option.map ~f:NetworkInterfacePermissionList.of_xml)
          (Xml.child xml_arg0 "networkInterfacePermissions") in
      make ?nextToken ?networkInterfacePermissions ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInterfacePermissions =
        field_map json "NetworkInterfacePermissions"
          NetworkInterfacePermissionList.of_json in
      make ?nextToken ?networkInterfacePermissions ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInterfacePermissionsRequest =
  struct
    type nonrec t =
      {
      networkInterfacePermissionIds:
        NetworkInterfacePermissionIdList.t option ;
      filters: FilterList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeNetworkInterfacePermissionsMaxResults.t option }
    let make ?networkInterfacePermissionIds =
      fun ?filters ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () ->
              { networkInterfacePermissionIds; filters; nextToken; maxResults
              }
    let to_value x =
      structure_to_value
        [("NetworkInterfacePermissionId",
           (Option.map x.networkInterfacePermissionIds
              ~f:NetworkInterfacePermissionIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeNetworkInterfacePermissionsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeNetworkInterfacePermissionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkInterfacePermissionIds =
        (Option.map ~f:NetworkInterfacePermissionIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfacePermissionId") in
      make ?maxResults ?nextToken ?filters ?networkInterfacePermissionIds ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeNetworkInterfacePermissionsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let networkInterfacePermissionIds =
        field_map json "NetworkInterfacePermissionIds"
          NetworkInterfacePermissionIdList.of_json in
      make ?maxResults ?nextToken ?filters ?networkInterfacePermissionIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInterfaceAttributeResult =
  struct
    type nonrec t =
      {
      attachment: NetworkInterfaceAttachment.t option ;
      description: AttributeValue.t option ;
      groups: GroupIdentifierList.t option ;
      networkInterfaceId: String_.t option ;
      sourceDestCheck: AttributeBooleanValue.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?attachment =
      fun ?description ->
        fun ?groups ->
          fun ?networkInterfaceId ->
            fun ?sourceDestCheck ->
              fun () ->
                {
                  attachment;
                  description;
                  groups;
                  networkInterfaceId;
                  sourceDestCheck
                }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachment",
           (Option.map x.attachment ~f:NetworkInterfaceAttachment.to_value));
        ("description",
          (Option.map x.description ~f:AttributeValue.to_value));
        ("groupSet", (Option.map x.groups ~f:GroupIdentifierList.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:String_.to_value));
        ("sourceDestCheck",
          (Option.map x.sourceDestCheck ~f:AttributeBooleanValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceDestCheck =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "sourceDestCheck") in
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let groups =
        (Option.map ~f:GroupIdentifierList.of_xml)
          (Xml.child xml_arg0 "groupSet") in
      let description =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "description") in
      let attachment =
        (Option.map ~f:NetworkInterfaceAttachment.of_xml)
          (Xml.child xml_arg0 "attachment") in
      make ?sourceDestCheck ?networkInterfaceId ?groups ?description
        ?attachment ()
    let of_json json =
      let sourceDestCheck =
        field_map json "SourceDestCheck" AttributeBooleanValue.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" String_.of_json in
      let groups = field_map json "Groups" GroupIdentifierList.of_json in
      let description = field_map json "Description" AttributeValue.of_json in
      let attachment =
        field_map json "Attachment" NetworkInterfaceAttachment.of_json in
      make ?sourceDestCheck ?networkInterfaceId ?groups ?description
        ?attachment ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInterfaceAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: NetworkInterfaceAttribute.t option ;
      dryRun: Boolean.t option ;
      networkInterfaceId: NetworkInterfaceId.t }
    let context_ = "DescribeNetworkInterfaceAttributeRequest"
    let make ?attribute =
      fun ?dryRun ->
        fun ~networkInterfaceId ->
          fun () -> { attribute; dryRun; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("attribute",
           (Option.map x.attribute ~f:NetworkInterfaceAttribute.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let attribute =
        (Option.map ~f:NetworkInterfaceAttribute.of_xml)
          (Xml.child xml_arg0 "attribute") in
      make ~networkInterfaceId ?dryRun ?attribute ()
    let of_json json =
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let attribute =
        field_map json "Attribute" NetworkInterfaceAttribute.of_json in
      make ~networkInterfaceId ?dryRun ?attribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsPathsResult =
  struct
    type nonrec t =
      {
      networkInsightsPaths: NetworkInsightsPathList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsPaths =
      fun ?nextToken -> fun () -> { networkInsightsPaths; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsPathSet",
           (Option.map x.networkInsightsPaths
              ~f:NetworkInsightsPathList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsPaths =
        (Option.map ~f:NetworkInsightsPathList.of_xml)
          (Xml.child xml_arg0 "networkInsightsPathSet") in
      make ?nextToken ?networkInsightsPaths ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInsightsPaths =
        field_map json "NetworkInsightsPaths" NetworkInsightsPathList.of_json in
      make ?nextToken ?networkInsightsPaths ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsPathsRequest =
  struct
    type nonrec t =
      {
      networkInsightsPathIds: NetworkInsightsPathIdList.t option ;
      filters: FilterList.t option ;
      maxResults: NetworkInsightsMaxResults.t option ;
      dryRun: Boolean.t option ;
      nextToken: NextToken.t option }
    let make ?networkInsightsPathIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?nextToken ->
              fun () ->
                {
                  networkInsightsPathIds;
                  filters;
                  maxResults;
                  dryRun;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("NetworkInsightsPathId",
           (Option.map x.networkInsightsPathIds
              ~f:NetworkInsightsPathIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkInsightsPathIds =
        (Option.map ~f:NetworkInsightsPathIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsPathId") in
      make ?nextToken ?dryRun ?maxResults ?filters ?networkInsightsPathIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let networkInsightsPathIds =
        field_map json "NetworkInsightsPathIds"
          NetworkInsightsPathIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters ?networkInsightsPathIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsAnalysesResult =
  struct
    type nonrec t =
      {
      networkInsightsAnalyses: NetworkInsightsAnalysisList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAnalyses =
      fun ?nextToken -> fun () -> { networkInsightsAnalyses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAnalysisSet",
           (Option.map x.networkInsightsAnalyses
              ~f:NetworkInsightsAnalysisList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsAnalyses =
        (Option.map ~f:NetworkInsightsAnalysisList.of_xml)
          (Xml.child xml_arg0 "networkInsightsAnalysisSet") in
      make ?nextToken ?networkInsightsAnalyses ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInsightsAnalyses =
        field_map json "NetworkInsightsAnalyses"
          NetworkInsightsAnalysisList.of_json in
      make ?nextToken ?networkInsightsAnalyses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsAnalysesRequest =
  struct
    type nonrec t =
      {
      networkInsightsAnalysisIds: NetworkInsightsAnalysisIdList.t option ;
      networkInsightsPathId: NetworkInsightsPathId.t option ;
      analysisStartTime: MillisecondDateTime.t option ;
      analysisEndTime: MillisecondDateTime.t option ;
      filters: FilterList.t option ;
      maxResults: NetworkInsightsMaxResults.t option ;
      dryRun: Boolean.t option ;
      nextToken: NextToken.t option }
    let make ?networkInsightsAnalysisIds =
      fun ?networkInsightsPathId ->
        fun ?analysisStartTime ->
          fun ?analysisEndTime ->
            fun ?filters ->
              fun ?maxResults ->
                fun ?dryRun ->
                  fun ?nextToken ->
                    fun () ->
                      {
                        networkInsightsAnalysisIds;
                        networkInsightsPathId;
                        analysisStartTime;
                        analysisEndTime;
                        filters;
                        maxResults;
                        dryRun;
                        nextToken
                      }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAnalysisId",
           (Option.map x.networkInsightsAnalysisIds
              ~f:NetworkInsightsAnalysisIdList.to_value));
        ("NetworkInsightsPathId",
          (Option.map x.networkInsightsPathId
             ~f:NetworkInsightsPathId.to_value));
        ("AnalysisStartTime",
          (Option.map x.analysisStartTime ~f:MillisecondDateTime.to_value));
        ("AnalysisEndTime",
          (Option.map x.analysisEndTime ~f:MillisecondDateTime.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let analysisEndTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisEndTime") in
      let analysisStartTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisStartTime") in
      let networkInsightsPathId =
        (Option.map ~f:NetworkInsightsPathId.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsPathId") in
      let networkInsightsAnalysisIds =
        (Option.map ~f:NetworkInsightsAnalysisIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAnalysisId") in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisEndTime
        ?analysisStartTime ?networkInsightsPathId ?networkInsightsAnalysisIds
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let analysisEndTime =
        field_map json "AnalysisEndTime" MillisecondDateTime.of_json in
      let analysisStartTime =
        field_map json "AnalysisStartTime" MillisecondDateTime.of_json in
      let networkInsightsPathId =
        field_map json "NetworkInsightsPathId" NetworkInsightsPathId.of_json in
      let networkInsightsAnalysisIds =
        field_map json "NetworkInsightsAnalysisIds"
          NetworkInsightsAnalysisIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisEndTime
        ?analysisStartTime ?networkInsightsPathId ?networkInsightsAnalysisIds
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsAccessScopesResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopes: NetworkInsightsAccessScopeList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopes =
      fun ?nextToken -> fun () -> { networkInsightsAccessScopes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeSet",
           (Option.map x.networkInsightsAccessScopes
              ~f:NetworkInsightsAccessScopeList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsAccessScopes =
        (Option.map ~f:NetworkInsightsAccessScopeList.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeSet") in
      make ?nextToken ?networkInsightsAccessScopes ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInsightsAccessScopes =
        field_map json "NetworkInsightsAccessScopes"
          NetworkInsightsAccessScopeList.of_json in
      make ?nextToken ?networkInsightsAccessScopes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsAccessScopesRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeIds:
        NetworkInsightsAccessScopeIdList.t option ;
      filters: FilterList.t option ;
      maxResults: NetworkInsightsMaxResults.t option ;
      dryRun: Boolean.t option ;
      nextToken: NextToken.t option }
    let make ?networkInsightsAccessScopeIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?nextToken ->
              fun () ->
                {
                  networkInsightsAccessScopeIds;
                  filters;
                  maxResults;
                  dryRun;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeId",
           (Option.map x.networkInsightsAccessScopeIds
              ~f:NetworkInsightsAccessScopeIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkInsightsAccessScopeIds =
        (Option.map ~f:NetworkInsightsAccessScopeIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAccessScopeId") in
      make ?nextToken ?dryRun ?maxResults ?filters
        ?networkInsightsAccessScopeIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let networkInsightsAccessScopeIds =
        field_map json "NetworkInsightsAccessScopeIds"
          NetworkInsightsAccessScopeIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters
        ?networkInsightsAccessScopeIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsAccessScopeAnalysesResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalyses:
        NetworkInsightsAccessScopeAnalysisList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeAnalyses =
      fun ?nextToken ->
        fun () -> { networkInsightsAccessScopeAnalyses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeAnalysisSet",
           (Option.map x.networkInsightsAccessScopeAnalyses
              ~f:NetworkInsightsAccessScopeAnalysisList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsAccessScopeAnalyses =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisList.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeAnalysisSet") in
      make ?nextToken ?networkInsightsAccessScopeAnalyses ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkInsightsAccessScopeAnalyses =
        field_map json "NetworkInsightsAccessScopeAnalyses"
          NetworkInsightsAccessScopeAnalysisList.of_json in
      make ?nextToken ?networkInsightsAccessScopeAnalyses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkInsightsAccessScopeAnalysesRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisIds:
        NetworkInsightsAccessScopeAnalysisIdList.t option ;
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t option ;
      analysisStartTimeBegin: MillisecondDateTime.t option ;
      analysisStartTimeEnd: MillisecondDateTime.t option ;
      filters: FilterList.t option ;
      maxResults: NetworkInsightsMaxResults.t option ;
      dryRun: Boolean.t option ;
      nextToken: NextToken.t option }
    let make ?networkInsightsAccessScopeAnalysisIds =
      fun ?networkInsightsAccessScopeId ->
        fun ?analysisStartTimeBegin ->
          fun ?analysisStartTimeEnd ->
            fun ?filters ->
              fun ?maxResults ->
                fun ?dryRun ->
                  fun ?nextToken ->
                    fun () ->
                      {
                        networkInsightsAccessScopeAnalysisIds;
                        networkInsightsAccessScopeId;
                        analysisStartTimeBegin;
                        analysisStartTimeEnd;
                        filters;
                        maxResults;
                        dryRun;
                        nextToken
                      }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeAnalysisId",
           (Option.map x.networkInsightsAccessScopeAnalysisIds
              ~f:NetworkInsightsAccessScopeAnalysisIdList.to_value));
        ("NetworkInsightsAccessScopeId",
          (Option.map x.networkInsightsAccessScopeId
             ~f:NetworkInsightsAccessScopeId.to_value));
        ("AnalysisStartTimeBegin",
          (Option.map x.analysisStartTimeBegin
             ~f:MillisecondDateTime.to_value));
        ("AnalysisStartTimeEnd",
          (Option.map x.analysisStartTimeEnd ~f:MillisecondDateTime.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let analysisStartTimeEnd =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisStartTimeEnd") in
      let analysisStartTimeBegin =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisStartTimeBegin") in
      let networkInsightsAccessScopeId =
        (Option.map ~f:NetworkInsightsAccessScopeId.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAccessScopeId") in
      let networkInsightsAccessScopeAnalysisIds =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAccessScopeAnalysisId") in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisStartTimeEnd
        ?analysisStartTimeBegin ?networkInsightsAccessScopeId
        ?networkInsightsAccessScopeAnalysisIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let analysisStartTimeEnd =
        field_map json "AnalysisStartTimeEnd" MillisecondDateTime.of_json in
      let analysisStartTimeBegin =
        field_map json "AnalysisStartTimeBegin" MillisecondDateTime.of_json in
      let networkInsightsAccessScopeId =
        field_map json "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      let networkInsightsAccessScopeAnalysisIds =
        field_map json "NetworkInsightsAccessScopeAnalysisIds"
          NetworkInsightsAccessScopeAnalysisIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisStartTimeEnd
        ?analysisStartTimeBegin ?networkInsightsAccessScopeId
        ?networkInsightsAccessScopeAnalysisIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkAclsResult =
  struct
    type nonrec t =
      {
      networkAcls: NetworkAclList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkAcls =
      fun ?nextToken -> fun () -> { networkAcls; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkAclSet",
           (Option.map x.networkAcls ~f:NetworkAclList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkAcls =
        (Option.map ~f:NetworkAclList.of_xml)
          (Xml.child xml_arg0 "networkAclSet") in
      make ?nextToken ?networkAcls ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkAcls = field_map json "NetworkAcls" NetworkAclList.of_json in
      make ?nextToken ?networkAcls ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNetworkAclsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      networkAclIds: NetworkAclIdStringList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeNetworkAclsMaxResults.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?networkAclIds ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { filters; dryRun; networkAclIds; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkAclId",
          (Option.map x.networkAclIds ~f:NetworkAclIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeNetworkAclsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeNetworkAclsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let networkAclIds =
        (Option.map ~f:NetworkAclIdStringList.of_xml)
          (Xml.child xml_arg0 "NetworkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?networkAclIds ?dryRun ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeNetworkAclsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let networkAclIds =
        field_map json "NetworkAclIds" NetworkAclIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?networkAclIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNatGatewaysResult =
  struct
    type nonrec t =
      {
      natGateways: NatGatewayList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?natGateways =
      fun ?nextToken -> fun () -> { natGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("natGatewaySet",
           (Option.map x.natGateways ~f:NatGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let natGateways =
        (Option.map ~f:NatGatewayList.of_xml)
          (Xml.child xml_arg0 "natGatewaySet") in
      make ?nextToken ?natGateways ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let natGateways = field_map json "NatGateways" NatGatewayList.of_json in
      make ?nextToken ?natGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNatGatewaysRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filter: FilterList.t option ;
      maxResults: DescribeNatGatewaysMaxResults.t option ;
      natGatewayIds: NatGatewayIdStringList.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filter ->
        fun ?maxResults ->
          fun ?natGatewayIds ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filter; maxResults; natGatewayIds; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeNatGatewaysMaxResults.to_value));
        ("NatGatewayId",
          (Option.map x.natGatewayIds ~f:NatGatewayIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let natGatewayIds =
        (Option.map ~f:NatGatewayIdStringList.of_xml)
          (Xml.child xml_arg0 "NatGatewayId") in
      let maxResults =
        (Option.map ~f:DescribeNatGatewaysMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?natGatewayIds ?maxResults ?filter ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let natGatewayIds =
        field_map json "NatGatewayIds" NatGatewayIdStringList.of_json in
      let maxResults =
        field_map json "MaxResults" DescribeNatGatewaysMaxResults.of_json in
      let filter = field_map json "Filter" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?natGatewayIds ?maxResults ?filter ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeMovingAddressesResult =
  struct
    type nonrec t =
      {
      movingAddressStatuses: MovingAddressStatusSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?movingAddressStatuses =
      fun ?nextToken -> fun () -> { movingAddressStatuses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("movingAddressStatusSet",
           (Option.map x.movingAddressStatuses
              ~f:MovingAddressStatusSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let movingAddressStatuses =
        (Option.map ~f:MovingAddressStatusSet.of_xml)
          (Xml.child xml_arg0 "movingAddressStatusSet") in
      make ?nextToken ?movingAddressStatuses ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let movingAddressStatuses =
        field_map json "MovingAddressStatuses" MovingAddressStatusSet.of_json in
      make ?nextToken ?movingAddressStatuses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeMovingAddressesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      maxResults: DescribeMovingAddressesMaxResults.t option ;
      nextToken: String_.t option ;
      publicIps: ValueStringList.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?publicIps ->
              fun () -> { filters; dryRun; maxResults; nextToken; publicIps }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:DescribeMovingAddressesMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("publicIp", (Option.map x.publicIps ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let publicIps =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "publicIp") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:DescribeMovingAddressesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filter") in
      make ?publicIps ?nextToken ?maxResults ?dryRun ?filters ()
    let of_json json =
      let publicIps = field_map json "PublicIps" ValueStringList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" DescribeMovingAddressesMaxResults.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?publicIps ?nextToken ?maxResults ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeManagedPrefixListsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option ;
      prefixLists: ManagedPrefixListSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?prefixLists -> fun () -> { nextToken; prefixLists }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("prefixListSet",
          (Option.map x.prefixLists ~f:ManagedPrefixListSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixLists =
        (Option.map ~f:ManagedPrefixListSet.of_xml)
          (Xml.child xml_arg0 "prefixListSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?prefixLists ?nextToken ()
    let of_json json =
      let prefixLists =
        field_map json "PrefixLists" ManagedPrefixListSet.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?prefixLists ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeManagedPrefixListsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: PrefixListMaxResults.t option ;
      nextToken: NextToken.t option ;
      prefixListIds: ValueStringList.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?prefixListIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; prefixListIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:PrefixListMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("PrefixListId",
          (Option.map x.prefixListIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixListIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "PrefixListId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:PrefixListMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let prefixListIds =
        field_map json "PrefixListIds" ValueStringList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" PrefixListMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewaysResult =
  struct
    type nonrec t =
      {
      localGateways: LocalGatewaySet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGateways =
      fun ?nextToken -> fun () -> { localGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewaySet",
           (Option.map x.localGateways ~f:LocalGatewaySet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGateways =
        (Option.map ~f:LocalGatewaySet.of_xml)
          (Xml.child xml_arg0 "localGatewaySet") in
      make ?nextToken ?localGateways ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let localGateways =
        field_map json "LocalGateways" LocalGatewaySet.of_json in
      make ?nextToken ?localGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewaysRequest =
  struct
    type nonrec t =
      {
      localGatewayIds: LocalGatewayIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: LocalGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?localGatewayIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { localGatewayIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("LocalGatewayId",
           (Option.map x.localGatewayIds ~f:LocalGatewayIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayIds =
        (Option.map ~f:LocalGatewayIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayIds =
        field_map json "LocalGatewayIds" LocalGatewayIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayVirtualInterfacesResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaces: LocalGatewayVirtualInterfaceSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterfaces =
      fun ?nextToken ->
        fun () -> { localGatewayVirtualInterfaces; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterfaceSet",
           (Option.map x.localGatewayVirtualInterfaces
              ~f:LocalGatewayVirtualInterfaceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayVirtualInterfaces =
        (Option.map ~f:LocalGatewayVirtualInterfaceSet.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterfaceSet") in
      make ?nextToken ?localGatewayVirtualInterfaces ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let localGatewayVirtualInterfaces =
        field_map json "LocalGatewayVirtualInterfaces"
          LocalGatewayVirtualInterfaceSet.of_json in
      make ?nextToken ?localGatewayVirtualInterfaces ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayVirtualInterfacesRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceIds:
        LocalGatewayVirtualInterfaceIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: LocalGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?localGatewayVirtualInterfaceIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayVirtualInterfaceIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceId",
           (Option.map x.localGatewayVirtualInterfaceIds
              ~f:LocalGatewayVirtualInterfaceIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayVirtualInterfaceIds =
        (Option.map ~f:LocalGatewayVirtualInterfaceIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayVirtualInterfaceId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayVirtualInterfaceIds =
        field_map json "LocalGatewayVirtualInterfaceIds"
          LocalGatewayVirtualInterfaceIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayVirtualInterfaceGroupsResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroups:
        LocalGatewayVirtualInterfaceGroupSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterfaceGroups =
      fun ?nextToken ->
        fun () -> { localGatewayVirtualInterfaceGroups; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterfaceGroupSet",
           (Option.map x.localGatewayVirtualInterfaceGroups
              ~f:LocalGatewayVirtualInterfaceGroupSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayVirtualInterfaceGroups =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroupSet.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterfaceGroupSet") in
      make ?nextToken ?localGatewayVirtualInterfaceGroups ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let localGatewayVirtualInterfaceGroups =
        field_map json "LocalGatewayVirtualInterfaceGroups"
          LocalGatewayVirtualInterfaceGroupSet.of_json in
      make ?nextToken ?localGatewayVirtualInterfaceGroups ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayVirtualInterfaceGroupsRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroupIds:
        LocalGatewayVirtualInterfaceGroupIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: LocalGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?localGatewayVirtualInterfaceGroupIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayVirtualInterfaceGroupIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceGroupId",
           (Option.map x.localGatewayVirtualInterfaceGroupIds
              ~f:LocalGatewayVirtualInterfaceGroupIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayVirtualInterfaceGroupIds =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroupIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayVirtualInterfaceGroupId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceGroupIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayVirtualInterfaceGroupIds =
        field_map json "LocalGatewayVirtualInterfaceGroupIds"
          LocalGatewayVirtualInterfaceGroupIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceGroupIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayRouteTablesResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTables: LocalGatewayRouteTableSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTables =
      fun ?nextToken -> fun () -> { localGatewayRouteTables; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableSet",
           (Option.map x.localGatewayRouteTables
              ~f:LocalGatewayRouteTableSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayRouteTables =
        (Option.map ~f:LocalGatewayRouteTableSet.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableSet") in
      make ?nextToken ?localGatewayRouteTables ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let localGatewayRouteTables =
        field_map json "LocalGatewayRouteTables"
          LocalGatewayRouteTableSet.of_json in
      make ?nextToken ?localGatewayRouteTables ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayRouteTablesRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableIds: LocalGatewayRouteTableIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: LocalGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?localGatewayRouteTableIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayRouteTableIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Option.map x.localGatewayRouteTableIds
              ~f:LocalGatewayRouteTableIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableIds =
        (Option.map ~f:LocalGatewayRouteTableIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayRouteTableId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayRouteTableIds
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayRouteTableIds =
        field_map json "LocalGatewayRouteTableIds"
          LocalGatewayRouteTableIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayRouteTableIds
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayRouteTableVpcAssociationsResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociations:
        LocalGatewayRouteTableVpcAssociationSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVpcAssociations =
      fun ?nextToken ->
        fun () -> { localGatewayRouteTableVpcAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVpcAssociationSet",
           (Option.map x.localGatewayRouteTableVpcAssociations
              ~f:LocalGatewayRouteTableVpcAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayRouteTableVpcAssociations =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociationSet.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableVpcAssociationSet") in
      make ?nextToken ?localGatewayRouteTableVpcAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let localGatewayRouteTableVpcAssociations =
        field_map json "LocalGatewayRouteTableVpcAssociations"
          LocalGatewayRouteTableVpcAssociationSet.of_json in
      make ?nextToken ?localGatewayRouteTableVpcAssociations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayRouteTableVpcAssociationsRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociationIds:
        LocalGatewayRouteTableVpcAssociationIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: LocalGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?localGatewayRouteTableVpcAssociationIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayRouteTableVpcAssociationIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVpcAssociationId",
           (Option.map x.localGatewayRouteTableVpcAssociationIds
              ~f:LocalGatewayRouteTableVpcAssociationIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableVpcAssociationIds =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociationIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayRouteTableVpcAssociationId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVpcAssociationIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayRouteTableVpcAssociationIds =
        field_map json "LocalGatewayRouteTableVpcAssociationIds"
          LocalGatewayRouteTableVpcAssociationIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVpcAssociationIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociations:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVirtualInterfaceGroupAssociations =
      fun ?nextToken ->
        fun () ->
          {
            localGatewayRouteTableVirtualInterfaceGroupAssociations;
            nextToken
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVirtualInterfaceGroupAssociationSet",
           (Option.map
              x.localGatewayRouteTableVirtualInterfaceGroupAssociations
              ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayRouteTableVirtualInterfaceGroupAssociations =
        (Option.map
           ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.of_xml)
          (Xml.child xml_arg0
             "localGatewayRouteTableVirtualInterfaceGroupAssociationSet") in
      make ?nextToken
        ?localGatewayRouteTableVirtualInterfaceGroupAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let localGatewayRouteTableVirtualInterfaceGroupAssociations =
        field_map json
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociations"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.of_json in
      make ?nextToken
        ?localGatewayRouteTableVirtualInterfaceGroupAssociations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociationIds:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: LocalGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayRouteTableVirtualInterfaceGroupAssociationIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVirtualInterfaceGroupAssociationId",
           (Option.map
              x.localGatewayRouteTableVirtualInterfaceGroupAssociationIds
              ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
        (Option.map
           ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.of_xml)
          (Xml.child xml_arg0
             "LocalGatewayRouteTableVirtualInterfaceGroupAssociationId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
        field_map json
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociationIds"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLaunchTemplatesResult =
  struct
    type nonrec t =
      {
      launchTemplates: LaunchTemplateSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplates =
      fun ?nextToken -> fun () -> { launchTemplates; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplates",
           (Option.map x.launchTemplates ~f:LaunchTemplateSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let launchTemplates =
        (Option.map ~f:LaunchTemplateSet.of_xml)
          (Xml.child xml_arg0 "launchTemplates") in
      make ?nextToken ?launchTemplates ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let launchTemplates =
        field_map json "LaunchTemplates" LaunchTemplateSet.of_json in
      make ?nextToken ?launchTemplates ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLaunchTemplatesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      launchTemplateIds: LaunchTemplateIdStringList.t option ;
      launchTemplateNames: LaunchTemplateNameStringList.t option ;
      filters: FilterList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeLaunchTemplatesMaxResults.t option }
    let make ?dryRun =
      fun ?launchTemplateIds ->
        fun ?launchTemplateNames ->
          fun ?filters ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun () ->
                  {
                    dryRun;
                    launchTemplateIds;
                    launchTemplateNames;
                    filters;
                    nextToken;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateIds
             ~f:LaunchTemplateIdStringList.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateNames
             ~f:LaunchTemplateNameStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeLaunchTemplatesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeLaunchTemplatesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let launchTemplateNames =
        (Option.map ~f:LaunchTemplateNameStringList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateIds =
        (Option.map ~f:LaunchTemplateIdStringList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?maxResults ?nextToken ?filters ?launchTemplateNames
        ?launchTemplateIds ?dryRun ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeLaunchTemplatesMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let launchTemplateNames =
        field_map json "LaunchTemplateNames"
          LaunchTemplateNameStringList.of_json in
      let launchTemplateIds =
        field_map json "LaunchTemplateIds" LaunchTemplateIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?maxResults ?nextToken ?filters ?launchTemplateNames
        ?launchTemplateIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLaunchTemplateVersionsResult =
  struct
    type nonrec t =
      {
      launchTemplateVersions: LaunchTemplateVersionSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplateVersions =
      fun ?nextToken -> fun () -> { launchTemplateVersions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplateVersionSet",
           (Option.map x.launchTemplateVersions
              ~f:LaunchTemplateVersionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let launchTemplateVersions =
        (Option.map ~f:LaunchTemplateVersionSet.of_xml)
          (Xml.child xml_arg0 "launchTemplateVersionSet") in
      make ?nextToken ?launchTemplateVersions ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let launchTemplateVersions =
        field_map json "LaunchTemplateVersions"
          LaunchTemplateVersionSet.of_json in
      make ?nextToken ?launchTemplateVersions ()
    let to_json v = composed_to_json to_value v
  end
module DescribeLaunchTemplateVersionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      launchTemplateId: LaunchTemplateId.t option ;
      launchTemplateName: LaunchTemplateName.t option ;
      versions: VersionStringList.t option ;
      minVersion: String_.t option ;
      maxVersion: String_.t option ;
      nextToken: String_.t option ;
      maxResults: Integer.t option ;
      filters: FilterList.t option }
    let make ?dryRun =
      fun ?launchTemplateId ->
        fun ?launchTemplateName ->
          fun ?versions ->
            fun ?minVersion ->
              fun ?maxVersion ->
                fun ?nextToken ->
                  fun ?maxResults ->
                    fun ?filters ->
                      fun () ->
                        {
                          dryRun;
                          launchTemplateId;
                          launchTemplateName;
                          versions;
                          minVersion;
                          maxVersion;
                          nextToken;
                          maxResults;
                          filters
                        }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("LaunchTemplateVersion",
          (Option.map x.versions ~f:VersionStringList.to_value));
        ("MinVersion", (Option.map x.minVersion ~f:String_.to_value));
        ("MaxVersion", (Option.map x.maxVersion ~f:String_.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MaxVersion") in
      let minVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MinVersion") in
      let versions =
        (Option.map ~f:VersionStringList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?maxResults ?nextToken ?maxVersion ?minVersion ?versions
        ?launchTemplateName ?launchTemplateId ?dryRun ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxVersion = field_map json "MaxVersion" String_.of_json in
      let minVersion = field_map json "MinVersion" String_.of_json in
      let versions = field_map json "Versions" VersionStringList.of_json in
      let launchTemplateName =
        field_map json "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json "LaunchTemplateId" LaunchTemplateId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?filters ?maxResults ?nextToken ?maxVersion ?minVersion ?versions
        ?launchTemplateName ?launchTemplateId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeKeyPairsResult =
  struct
    type nonrec t = {
      keyPairs: KeyPairList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?keyPairs = fun () -> { keyPairs }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keySet", (Option.map x.keyPairs ~f:KeyPairList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyPairs =
        (Option.map ~f:KeyPairList.of_xml) (Xml.child xml_arg0 "keySet") in
      make ?keyPairs ()
    let of_json json =
      let keyPairs = field_map json "KeyPairs" KeyPairList.of_json in
      make ?keyPairs ()
    let to_json v = composed_to_json to_value v
  end
module DescribeKeyPairsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      keyNames: KeyNameStringList.t option ;
      keyPairIds: KeyPairIdStringList.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?keyNames ->
        fun ?keyPairIds ->
          fun ?dryRun -> fun () -> { filters; keyNames; keyPairIds; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("KeyName", (Option.map x.keyNames ~f:KeyNameStringList.to_value));
        ("KeyPairId",
          (Option.map x.keyPairIds ~f:KeyPairIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let keyPairIds =
        (Option.map ~f:KeyPairIdStringList.of_xml)
          (Xml.child xml_arg0 "KeyPairId") in
      let keyNames =
        (Option.map ~f:KeyNameStringList.of_xml)
          (Xml.child xml_arg0 "KeyName") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?keyPairIds ?keyNames ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let keyPairIds =
        field_map json "KeyPairIds" KeyPairIdStringList.of_json in
      let keyNames = field_map json "KeyNames" KeyNameStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?keyPairIds ?keyNames ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpv6PoolsResult =
  struct
    type nonrec t =
      {
      ipv6Pools: Ipv6PoolSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6Pools =
      fun ?nextToken -> fun () -> { ipv6Pools; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6PoolSet", (Option.map x.ipv6Pools ~f:Ipv6PoolSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipv6Pools =
        (Option.map ~f:Ipv6PoolSet.of_xml) (Xml.child xml_arg0 "ipv6PoolSet") in
      make ?nextToken ?ipv6Pools ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let ipv6Pools = field_map json "Ipv6Pools" Ipv6PoolSet.of_json in
      make ?nextToken ?ipv6Pools ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpv6PoolsRequest =
  struct
    type nonrec t =
      {
      poolIds: Ipv6PoolIdList.t option ;
      nextToken: NextToken.t option ;
      maxResults: Ipv6PoolMaxResults.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option }
    let make ?poolIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?filters ->
              fun () -> { poolIds; nextToken; maxResults; dryRun; filters }
    let to_value x =
      structure_to_value
        [("PoolId", (Option.map x.poolIds ~f:Ipv6PoolIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:Ipv6PoolMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:Ipv6PoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let poolIds =
        (Option.map ~f:Ipv6PoolIdList.of_xml) (Xml.child xml_arg0 "PoolId") in
      make ?filters ?dryRun ?maxResults ?nextToken ?poolIds ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults = field_map json "MaxResults" Ipv6PoolMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let poolIds = field_map json "PoolIds" Ipv6PoolIdList.of_json in
      make ?filters ?dryRun ?maxResults ?nextToken ?poolIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpamsResult =
  struct
    type nonrec t = {
      nextToken: NextToken.t option ;
      ipams: IpamSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?ipams -> fun () -> { nextToken; ipams }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamSet", (Option.map x.ipams ~f:IpamSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipams =
        (Option.map ~f:IpamSet.of_xml) (Xml.child xml_arg0 "ipamSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipams ?nextToken ()
    let of_json json =
      let ipams = field_map json "Ipams" IpamSet.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?ipams ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpamsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: IpamMaxResults.t option ;
      nextToken: NextToken.t option ;
      ipamIds: ValueStringList.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamIds ->
              fun () -> { dryRun; filters; maxResults; nextToken; ipamIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamId", (Option.map x.ipamIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamIds =
        (Option.map ~f:ValueStringList.of_xml) (Xml.child xml_arg0 "IpamId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let ipamIds = field_map json "IpamIds" ValueStringList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?ipamIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpamScopesResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option ;
      ipamScopes: IpamScopeSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamScopes -> fun () -> { nextToken; ipamScopes }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamScopeSet", (Option.map x.ipamScopes ~f:IpamScopeSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScopes =
        (Option.map ~f:IpamScopeSet.of_xml)
          (Xml.child xml_arg0 "ipamScopeSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamScopes ?nextToken ()
    let of_json json =
      let ipamScopes = field_map json "IpamScopes" IpamScopeSet.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?ipamScopes ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpamScopesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: IpamMaxResults.t option ;
      nextToken: NextToken.t option ;
      ipamScopeIds: ValueStringList.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamScopeIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; ipamScopeIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamScopeId",
          (Option.map x.ipamScopeIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScopeIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamScopeId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamScopeIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let ipamScopeIds =
        field_map json "IpamScopeIds" ValueStringList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?ipamScopeIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpamPoolsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option ;
      ipamPools: IpamPoolSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamPools -> fun () -> { nextToken; ipamPools }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamPoolSet", (Option.map x.ipamPools ~f:IpamPoolSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPools =
        (Option.map ~f:IpamPoolSet.of_xml) (Xml.child xml_arg0 "ipamPoolSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamPools ?nextToken ()
    let of_json json =
      let ipamPools = field_map json "IpamPools" IpamPoolSet.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?ipamPools ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIpamPoolsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: IpamMaxResults.t option ;
      nextToken: NextToken.t option ;
      ipamPoolIds: ValueStringList.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamPoolIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; ipamPoolIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamPoolId",
          (Option.map x.ipamPoolIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamPoolId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamPoolIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let ipamPoolIds = field_map json "IpamPoolIds" ValueStringList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?ipamPoolIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInternetGatewaysResult =
  struct
    type nonrec t =
      {
      internetGateways: InternetGatewayList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?internetGateways =
      fun ?nextToken -> fun () -> { internetGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("internetGatewaySet",
           (Option.map x.internetGateways ~f:InternetGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let internetGateways =
        (Option.map ~f:InternetGatewayList.of_xml)
          (Xml.child xml_arg0 "internetGatewaySet") in
      make ?nextToken ?internetGateways ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let internetGateways =
        field_map json "InternetGateways" InternetGatewayList.of_json in
      make ?nextToken ?internetGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInternetGatewaysRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      internetGatewayIds: InternetGatewayIdList.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeInternetGatewaysMaxResults.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?internetGatewayIds ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { filters; dryRun; internetGatewayIds; nextToken; maxResults
                }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Option.map x.internetGatewayIds ~f:InternetGatewayIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInternetGatewaysMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeInternetGatewaysMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let internetGatewayIds =
        (Option.map ~f:InternetGatewayIdList.of_xml)
          (Xml.child xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?internetGatewayIds ?dryRun ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeInternetGatewaysMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let internetGatewayIds =
        field_map json "InternetGatewayIds" InternetGatewayIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?internetGatewayIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstancesResult =
  struct
    type nonrec t =
      {
      reservations: ReservationList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservations =
      fun ?nextToken -> fun () -> { reservations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservationSet",
           (Option.map x.reservations ~f:ReservationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reservations =
        (Option.map ~f:ReservationList.of_xml)
          (Xml.child xml_arg0 "reservationSet") in
      make ?nextToken ?reservations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let reservations =
        field_map json "Reservations" ReservationList.of_json in
      make ?nextToken ?reservations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstancesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      instanceIds: InstanceIdStringList.t option ;
      dryRun: Boolean.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?filters =
      fun ?instanceIds ->
        fun ?dryRun ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { filters; instanceIds; dryRun; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?maxResults ?dryRun ?instanceIds ?filters ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceIds =
        field_map json "InstanceIds" InstanceIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?nextToken ?maxResults ?dryRun ?instanceIds ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceTypesResult =
  struct
    type nonrec t =
      {
      instanceTypes: InstanceTypeInfoList.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTypes =
      fun ?nextToken -> fun () -> { instanceTypes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTypeSet",
           (Option.map x.instanceTypes ~f:InstanceTypeInfoList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceTypes =
        (Option.map ~f:InstanceTypeInfoList.of_xml)
          (Xml.child xml_arg0 "instanceTypeSet") in
      make ?nextToken ?instanceTypes ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let instanceTypes =
        field_map json "InstanceTypes" InstanceTypeInfoList.of_json in
      make ?nextToken ?instanceTypes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceTypesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      instanceTypes: RequestInstanceTypeList.t option ;
      filters: FilterList.t option ;
      maxResults: DITMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?dryRun =
      fun ?instanceTypes ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; instanceTypes; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceType",
          (Option.map x.instanceTypes ~f:RequestInstanceTypeList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:DITMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DITMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let instanceTypes =
        (Option.map ~f:RequestInstanceTypeList.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?instanceTypes ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" DITMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let instanceTypes =
        field_map json "InstanceTypes" RequestInstanceTypeList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?instanceTypes ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceTypeOfferingsResult =
  struct
    type nonrec t =
      {
      instanceTypeOfferings: InstanceTypeOfferingsList.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTypeOfferings =
      fun ?nextToken -> fun () -> { instanceTypeOfferings; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTypeOfferingSet",
           (Option.map x.instanceTypeOfferings
              ~f:InstanceTypeOfferingsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceTypeOfferings =
        (Option.map ~f:InstanceTypeOfferingsList.of_xml)
          (Xml.child xml_arg0 "instanceTypeOfferingSet") in
      make ?nextToken ?instanceTypeOfferings ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let instanceTypeOfferings =
        field_map json "InstanceTypeOfferings"
          InstanceTypeOfferingsList.of_json in
      make ?nextToken ?instanceTypeOfferings ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceTypeOfferingsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      locationType: LocationType.t option ;
      filters: FilterList.t option ;
      maxResults: DITOMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?dryRun =
      fun ?locationType ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; locationType; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LocationType",
          (Option.map x.locationType ~f:LocationType.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:DITOMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DITOMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let locationType =
        (Option.map ~f:LocationType.of_xml)
          (Xml.child xml_arg0 "LocationType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?locationType ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" DITOMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let locationType = field_map json "LocationType" LocationType.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?locationType ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceStatusResult =
  struct
    type nonrec t =
      {
      instanceStatuses: InstanceStatusList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceStatuses =
      fun ?nextToken -> fun () -> { instanceStatuses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceStatusSet",
           (Option.map x.instanceStatuses ~f:InstanceStatusList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceStatuses =
        (Option.map ~f:InstanceStatusList.of_xml)
          (Xml.child xml_arg0 "instanceStatusSet") in
      make ?nextToken ?instanceStatuses ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let instanceStatuses =
        field_map json "InstanceStatuses" InstanceStatusList.of_json in
      make ?nextToken ?instanceStatuses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceStatusRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      instanceIds: InstanceIdStringList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option ;
      includeAllInstances: Boolean.t option }
    let make ?filters =
      fun ?instanceIds ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun ?includeAllInstances ->
                fun () ->
                  {
                    filters;
                    instanceIds;
                    maxResults;
                    nextToken;
                    dryRun;
                    includeAllInstances
                  }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("includeAllInstances",
          (Option.map x.includeAllInstances ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeAllInstances =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "includeAllInstances") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?includeAllInstances ?dryRun ?nextToken ?maxResults ?instanceIds
        ?filters ()
    let of_json json =
      let includeAllInstances =
        field_map json "IncludeAllInstances" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let instanceIds =
        field_map json "InstanceIds" InstanceIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?includeAllInstances ?dryRun ?nextToken ?maxResults ?instanceIds
        ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceEventWindowsResult =
  struct
    type nonrec t =
      {
      instanceEventWindows: InstanceEventWindowSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindows =
      fun ?nextToken -> fun () -> { instanceEventWindows; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindowSet",
           (Option.map x.instanceEventWindows
              ~f:InstanceEventWindowSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceEventWindows =
        (Option.map ~f:InstanceEventWindowSet.of_xml)
          (Xml.child xml_arg0 "instanceEventWindowSet") in
      make ?nextToken ?instanceEventWindows ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let instanceEventWindows =
        field_map json "InstanceEventWindows" InstanceEventWindowSet.of_json in
      make ?nextToken ?instanceEventWindows ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceEventWindowsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      instanceEventWindowIds: InstanceEventWindowIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: ResultRange.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?instanceEventWindowIds ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  dryRun;
                  instanceEventWindowIds;
                  filters;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Option.map x.instanceEventWindowIds
             ~f:InstanceEventWindowIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:ResultRange.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ResultRange.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let instanceEventWindowIds =
        (Option.map ~f:InstanceEventWindowIdSet.of_xml)
          (Xml.child xml_arg0 "InstanceEventWindowId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?instanceEventWindowIds ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" ResultRange.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let instanceEventWindowIds =
        field_map json "InstanceEventWindowIds"
          InstanceEventWindowIdSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?instanceEventWindowIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceEventNotificationAttributesResult =
  struct
    type nonrec t =
      {
      instanceTagAttribute: InstanceTagNotificationAttribute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTagAttribute = fun () -> { instanceTagAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTagAttribute",
           (Option.map x.instanceTagAttribute
              ~f:InstanceTagNotificationAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:InstanceTagNotificationAttribute.of_xml)
          (Xml.child xml_arg0 "instanceTagAttribute") in
      make ?instanceTagAttribute ()
    let of_json json =
      let instanceTagAttribute =
        field_map json "InstanceTagAttribute"
          InstanceTagNotificationAttribute.of_json in
      make ?instanceTagAttribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceEventNotificationAttributesRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceCreditSpecificationsResult =
  struct
    type nonrec t =
      {
      instanceCreditSpecifications: InstanceCreditSpecificationList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceCreditSpecifications =
      fun ?nextToken -> fun () -> { instanceCreditSpecifications; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceCreditSpecificationSet",
           (Option.map x.instanceCreditSpecifications
              ~f:InstanceCreditSpecificationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceCreditSpecifications =
        (Option.map ~f:InstanceCreditSpecificationList.of_xml)
          (Xml.child xml_arg0 "instanceCreditSpecificationSet") in
      make ?nextToken ?instanceCreditSpecifications ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let instanceCreditSpecifications =
        field_map json "InstanceCreditSpecifications"
          InstanceCreditSpecificationList.of_json in
      make ?nextToken ?instanceCreditSpecifications ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceCreditSpecificationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      instanceIds: InstanceIdStringList.t option ;
      maxResults: DescribeInstanceCreditSpecificationsMaxResults.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?instanceIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; instanceIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInstanceCreditSpecificationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeInstanceCreditSpecificationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?instanceIds ?filters ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeInstanceCreditSpecificationsMaxResults.of_json in
      let instanceIds =
        field_map json "InstanceIds" InstanceIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?instanceIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeInstanceAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: InstanceAttributeName.t ;
      dryRun: Boolean.t option ;
      instanceId: InstanceId.t }
    let context_ = "DescribeInstanceAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~instanceId -> fun () -> { dryRun; attribute; instanceId }
    let to_value x =
      structure_to_value
        [("attribute", (Some (InstanceAttributeName.to_value x.attribute)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let attribute =
        InstanceAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attribute") in
      make ~instanceId ?dryRun ~attribute ()
    let of_json json =
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let attribute =
        field_map_exn json "Attribute" InstanceAttributeName.of_json in
      make ~instanceId ?dryRun ~attribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImportSnapshotTasksResult =
  struct
    type nonrec t =
      {
      importSnapshotTasks: ImportSnapshotTaskList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?importSnapshotTasks =
      fun ?nextToken -> fun () -> { importSnapshotTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("importSnapshotTaskSet",
           (Option.map x.importSnapshotTasks
              ~f:ImportSnapshotTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let importSnapshotTasks =
        (Option.map ~f:ImportSnapshotTaskList.of_xml)
          (Xml.child xml_arg0 "importSnapshotTaskSet") in
      make ?nextToken ?importSnapshotTasks ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let importSnapshotTasks =
        field_map json "ImportSnapshotTasks" ImportSnapshotTaskList.of_json in
      make ?nextToken ?importSnapshotTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImportSnapshotTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      importTaskIds: ImportSnapshotTaskIdList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?importTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; importTaskIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filters", (Option.map x.filters ~f:FilterList.to_value));
        ("ImportTaskId",
          (Option.map x.importTaskIds ~f:ImportSnapshotTaskIdList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let importTaskIds =
        (Option.map ~f:ImportSnapshotTaskIdList.of_xml)
          (Xml.child xml_arg0 "ImportTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filters") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let importTaskIds =
        field_map json "ImportTaskIds" ImportSnapshotTaskIdList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImportImageTasksResult =
  struct
    type nonrec t =
      {
      importImageTasks: ImportImageTaskList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?importImageTasks =
      fun ?nextToken -> fun () -> { importImageTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("importImageTaskSet",
           (Option.map x.importImageTasks ~f:ImportImageTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let importImageTasks =
        (Option.map ~f:ImportImageTaskList.of_xml)
          (Xml.child xml_arg0 "importImageTaskSet") in
      make ?nextToken ?importImageTasks ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let importImageTasks =
        field_map json "ImportImageTasks" ImportImageTaskList.of_json in
      make ?nextToken ?importImageTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImportImageTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      importTaskIds: ImportTaskIdList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?importTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; importTaskIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filters", (Option.map x.filters ~f:FilterList.to_value));
        ("ImportTaskId",
          (Option.map x.importTaskIds ~f:ImportTaskIdList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let importTaskIds =
        (Option.map ~f:ImportTaskIdList.of_xml)
          (Xml.child xml_arg0 "ImportTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filters") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let importTaskIds =
        field_map json "ImportTaskIds" ImportTaskIdList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImagesResult =
  struct
    type nonrec t = {
      images: ImageList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?images = fun () -> { images }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imagesSet", (Option.map x.images ~f:ImageList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let images =
        (Option.map ~f:ImageList.of_xml) (Xml.child xml_arg0 "imagesSet") in
      make ?images ()
    let of_json json =
      let images = field_map json "Images" ImageList.of_json in
      make ?images ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImagesRequest =
  struct
    type nonrec t =
      {
      executableUsers: ExecutableByStringList.t option ;
      filters: FilterList.t option ;
      imageIds: ImageIdStringList.t option ;
      owners: OwnerStringList.t option ;
      includeDeprecated: Boolean.t option ;
      dryRun: Boolean.t option }
    let make ?executableUsers =
      fun ?filters ->
        fun ?imageIds ->
          fun ?owners ->
            fun ?includeDeprecated ->
              fun ?dryRun ->
                fun () ->
                  {
                    executableUsers;
                    filters;
                    imageIds;
                    owners;
                    includeDeprecated;
                    dryRun
                  }
    let to_value x =
      structure_to_value
        [("ExecutableBy",
           (Option.map x.executableUsers ~f:ExecutableByStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("ImageId", (Option.map x.imageIds ~f:ImageIdStringList.to_value));
        ("Owner", (Option.map x.owners ~f:OwnerStringList.to_value));
        ("IncludeDeprecated",
          (Option.map x.includeDeprecated ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let includeDeprecated =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeDeprecated") in
      let owners =
        (Option.map ~f:OwnerStringList.of_xml) (Xml.child xml_arg0 "Owner") in
      let imageIds =
        (Option.map ~f:ImageIdStringList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let executableUsers =
        (Option.map ~f:ExecutableByStringList.of_xml)
          (Xml.child xml_arg0 "ExecutableBy") in
      make ?dryRun ?includeDeprecated ?owners ?imageIds ?filters
        ?executableUsers ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let includeDeprecated =
        field_map json "IncludeDeprecated" Boolean.of_json in
      let owners = field_map json "Owners" OwnerStringList.of_json in
      let imageIds = field_map json "ImageIds" ImageIdStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let executableUsers =
        field_map json "ExecutableUsers" ExecutableByStringList.of_json in
      make ?dryRun ?includeDeprecated ?owners ?imageIds ?filters
        ?executableUsers ()
    let to_json v = composed_to_json to_value v
  end
module DescribeImageAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: ImageAttributeName.t ;
      imageId: ImageId.t ;
      dryRun: Boolean.t option }
    let context_ = "DescribeImageAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~imageId -> fun () -> { dryRun; attribute; imageId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (ImageAttributeName.to_value x.attribute)));
        ("ImageId", (Some (ImageId.to_value x.imageId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      let attribute =
        ImageAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~imageId ~attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      let attribute =
        field_map_exn json "Attribute" ImageAttributeName.of_json in
      make ?dryRun ~imageId ~attribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIdentityIdFormatResult =
  struct
    type nonrec t = {
      statuses: IdFormatList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?statuses = fun () -> { statuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("statusSet", (Option.map x.statuses ~f:IdFormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:IdFormatList.of_xml) (Xml.child xml_arg0 "statusSet") in
      make ?statuses ()
    let of_json json =
      let statuses = field_map json "Statuses" IdFormatList.of_json in
      make ?statuses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIdentityIdFormatRequest =
  struct
    type nonrec t = {
      principalArn: String_.t ;
      resource: String_.t option }
    let context_ = "DescribeIdentityIdFormatRequest"
    let make ?resource =
      fun ~principalArn -> fun () -> { resource; principalArn }
    let to_value x =
      structure_to_value
        [("principalArn", (Some (String_.to_value x.principalArn)));
        ("resource", (Option.map x.resource ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resource") in
      let principalArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "principalArn") in
      make ?resource ~principalArn ()
    let of_json json =
      let resource = field_map json "Resource" String_.of_json in
      let principalArn = field_map_exn json "PrincipalArn" String_.of_json in
      make ?resource ~principalArn ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIdFormatResult =
  struct
    type nonrec t = {
      statuses: IdFormatList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?statuses = fun () -> { statuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("statusSet", (Option.map x.statuses ~f:IdFormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:IdFormatList.of_xml) (Xml.child xml_arg0 "statusSet") in
      make ?statuses ()
    let of_json json =
      let statuses = field_map json "Statuses" IdFormatList.of_json in
      make ?statuses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIdFormatRequest =
  struct
    type nonrec t = {
      resource: String_.t option }
    let make ?resource = fun () -> { resource }
    let to_value x =
      structure_to_value
        [("Resource", (Option.map x.resource ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Resource") in
      make ?resource ()
    let of_json json =
      let resource = field_map json "Resource" String_.of_json in
      make ?resource ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIamInstanceProfileAssociationsResult =
  struct
    type nonrec t =
      {
      iamInstanceProfileAssociations:
        IamInstanceProfileAssociationSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?iamInstanceProfileAssociations =
      fun ?nextToken ->
        fun () -> { iamInstanceProfileAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("iamInstanceProfileAssociationSet",
           (Option.map x.iamInstanceProfileAssociations
              ~f:IamInstanceProfileAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let iamInstanceProfileAssociations =
        (Option.map ~f:IamInstanceProfileAssociationSet.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfileAssociationSet") in
      make ?nextToken ?iamInstanceProfileAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let iamInstanceProfileAssociations =
        field_map json "IamInstanceProfileAssociations"
          IamInstanceProfileAssociationSet.of_json in
      make ?nextToken ?iamInstanceProfileAssociations ()
    let to_json v = composed_to_json to_value v
  end
module DescribeIamInstanceProfileAssociationsRequest =
  struct
    type nonrec t =
      {
      associationIds: AssociationIdList.t option ;
      filters: FilterList.t option ;
      maxResults: DescribeIamInstanceProfileAssociationsMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?associationIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { associationIds; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("AssociationId",
           (Option.map x.associationIds ~f:AssociationIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeIamInstanceProfileAssociationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map
           ~f:DescribeIamInstanceProfileAssociationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let associationIds =
        (Option.map ~f:AssociationIdList.of_xml)
          (Xml.child xml_arg0 "AssociationId") in
      make ?nextToken ?maxResults ?filters ?associationIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeIamInstanceProfileAssociationsMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let associationIds =
        field_map json "AssociationIds" AssociationIdList.of_json in
      make ?nextToken ?maxResults ?filters ?associationIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeHostsResult =
  struct
    type nonrec t = {
      hosts: HostList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?hosts = fun ?nextToken -> fun () -> { hosts; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("hostSet", (Option.map x.hosts ~f:HostList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let hosts =
        (Option.map ~f:HostList.of_xml) (Xml.child xml_arg0 "hostSet") in
      make ?nextToken ?hosts ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let hosts = field_map json "Hosts" HostList.of_json in
      make ?nextToken ?hosts ()
    let to_json v = composed_to_json to_value v
  end
module DescribeHostsRequest =
  struct
    type nonrec t =
      {
      filter: FilterList.t option ;
      hostIds: RequestHostIdList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?filter =
      fun ?hostIds ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { filter; hostIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:FilterList.to_value));
        ("hostId", (Option.map x.hostIds ~f:RequestHostIdList.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let hostIds =
        (Option.map ~f:RequestHostIdList.of_xml)
          (Xml.child xml_arg0 "hostId") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filter") in
      make ?nextToken ?maxResults ?hostIds ?filter ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let hostIds = field_map json "HostIds" RequestHostIdList.of_json in
      let filter = field_map json "Filter" FilterList.of_json in
      make ?nextToken ?maxResults ?hostIds ?filter ()
    let to_json v = composed_to_json to_value v
  end
module DescribeHostReservationsResult =
  struct
    type nonrec t =
      {
      hostReservationSet: HostReservationSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?hostReservationSet =
      fun ?nextToken -> fun () -> { hostReservationSet; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("hostReservationSet",
           (Option.map x.hostReservationSet ~f:HostReservationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let hostReservationSet =
        (Option.map ~f:HostReservationSet.of_xml)
          (Xml.child xml_arg0 "hostReservationSet") in
      make ?nextToken ?hostReservationSet ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let hostReservationSet =
        field_map json "HostReservationSet" HostReservationSet.of_json in
      make ?nextToken ?hostReservationSet ()
    let to_json v = composed_to_json to_value v
  end
module DescribeHostReservationsRequest =
  struct
    type nonrec t =
      {
      filter: FilterList.t option ;
      hostReservationIdSet: HostReservationIdSet.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?filter =
      fun ?hostReservationIdSet ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { filter; hostReservationIdSet; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("HostReservationIdSet",
          (Option.map x.hostReservationIdSet ~f:HostReservationIdSet.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let hostReservationIdSet =
        (Option.map ~f:HostReservationIdSet.of_xml)
          (Xml.child xml_arg0 "HostReservationIdSet") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?maxResults ?hostReservationIdSet ?filter ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let hostReservationIdSet =
        field_map json "HostReservationIdSet" HostReservationIdSet.of_json in
      let filter = field_map json "Filter" FilterList.of_json in
      make ?nextToken ?maxResults ?hostReservationIdSet ?filter ()
    let to_json v = composed_to_json to_value v
  end
module DescribeHostReservationOfferingsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      offeringSet: HostOfferingSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?offeringSet -> fun () -> { nextToken; offeringSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("offeringSet",
          (Option.map x.offeringSet ~f:HostOfferingSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringSet =
        (Option.map ~f:HostOfferingSet.of_xml)
          (Xml.child xml_arg0 "offeringSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?offeringSet ?nextToken ()
    let of_json json =
      let offeringSet = field_map json "OfferingSet" HostOfferingSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?offeringSet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeHostReservationOfferingsRequest =
  struct
    type nonrec t =
      {
      filter: FilterList.t option ;
      maxDuration: Integer.t option ;
      maxResults: DescribeHostReservationsMaxResults.t option ;
      minDuration: Integer.t option ;
      nextToken: String_.t option ;
      offeringId: OfferingId.t option }
    let make ?filter =
      fun ?maxDuration ->
        fun ?maxResults ->
          fun ?minDuration ->
            fun ?nextToken ->
              fun ?offeringId ->
                fun () ->
                  {
                    filter;
                    maxDuration;
                    maxResults;
                    minDuration;
                    nextToken;
                    offeringId
                  }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("MaxDuration", (Option.map x.maxDuration ~f:Integer.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeHostReservationsMaxResults.to_value));
        ("MinDuration", (Option.map x.minDuration ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("OfferingId", (Option.map x.offeringId ~f:OfferingId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringId =
        (Option.map ~f:OfferingId.of_xml) (Xml.child xml_arg0 "OfferingId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let minDuration =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MinDuration") in
      let maxResults =
        (Option.map ~f:DescribeHostReservationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let maxDuration =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxDuration") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?offeringId ?nextToken ?minDuration ?maxResults ?maxDuration
        ?filter ()
    let of_json json =
      let offeringId = field_map json "OfferingId" OfferingId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let minDuration = field_map json "MinDuration" Integer.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeHostReservationsMaxResults.of_json in
      let maxDuration = field_map json "MaxDuration" Integer.of_json in
      let filter = field_map json "Filter" FilterList.of_json in
      make ?offeringId ?nextToken ?minDuration ?maxResults ?maxDuration
        ?filter ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFpgaImagesResult =
  struct
    type nonrec t =
      {
      fpgaImages: FpgaImageList.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImages =
      fun ?nextToken -> fun () -> { fpgaImages; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageSet",
           (Option.map x.fpgaImages ~f:FpgaImageList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let fpgaImages =
        (Option.map ~f:FpgaImageList.of_xml)
          (Xml.child xml_arg0 "fpgaImageSet") in
      make ?nextToken ?fpgaImages ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let fpgaImages = field_map json "FpgaImages" FpgaImageList.of_json in
      make ?nextToken ?fpgaImages ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFpgaImagesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      fpgaImageIds: FpgaImageIdList.t option ;
      owners: OwnerStringList.t option ;
      filters: FilterList.t option ;
      nextToken: NextToken.t option ;
      maxResults: DescribeFpgaImagesMaxResults.t option }
    let make ?dryRun =
      fun ?fpgaImageIds ->
        fun ?owners ->
          fun ?filters ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun () ->
                  {
                    dryRun;
                    fpgaImageIds;
                    owners;
                    filters;
                    nextToken;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId",
          (Option.map x.fpgaImageIds ~f:FpgaImageIdList.to_value));
        ("Owner", (Option.map x.owners ~f:OwnerStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeFpgaImagesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeFpgaImagesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let owners =
        (Option.map ~f:OwnerStringList.of_xml) (Xml.child xml_arg0 "Owner") in
      let fpgaImageIds =
        (Option.map ~f:FpgaImageIdList.of_xml)
          (Xml.child xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?maxResults ?nextToken ?filters ?owners ?fpgaImageIds ?dryRun ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeFpgaImagesMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let owners = field_map json "Owners" OwnerStringList.of_json in
      let fpgaImageIds =
        field_map json "FpgaImageIds" FpgaImageIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?maxResults ?nextToken ?filters ?owners ?fpgaImageIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFpgaImageAttributeResult =
  struct
    type nonrec t = {
      fpgaImageAttribute: FpgaImageAttribute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageAttribute = fun () -> { fpgaImageAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageAttribute",
           (Option.map x.fpgaImageAttribute ~f:FpgaImageAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageAttribute =
        (Option.map ~f:FpgaImageAttribute.of_xml)
          (Xml.child xml_arg0 "fpgaImageAttribute") in
      make ?fpgaImageAttribute ()
    let of_json json =
      let fpgaImageAttribute =
        field_map json "FpgaImageAttribute" FpgaImageAttribute.of_json in
      make ?fpgaImageAttribute ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFpgaImageAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      fpgaImageId: FpgaImageId.t ;
      attribute: FpgaImageAttributeName.t }
    let context_ = "DescribeFpgaImageAttributeRequest"
    let make ?dryRun =
      fun ~fpgaImageId ->
        fun ~attribute -> fun () -> { dryRun; fpgaImageId; attribute }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId", (Some (FpgaImageId.to_value x.fpgaImageId)));
        ("Attribute", (Some (FpgaImageAttributeName.to_value x.attribute)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attribute =
        FpgaImageAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      let fpgaImageId =
        FpgaImageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~attribute ~fpgaImageId ?dryRun ()
    let of_json json =
      let attribute =
        field_map_exn json "Attribute" FpgaImageAttributeName.of_json in
      let fpgaImageId = field_map_exn json "FpgaImageId" FpgaImageId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~attribute ~fpgaImageId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFlowLogsResult =
  struct
    type nonrec t =
      {
      flowLogs: FlowLogSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?flowLogs = fun ?nextToken -> fun () -> { flowLogs; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("flowLogSet", (Option.map x.flowLogs ~f:FlowLogSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let flowLogs =
        (Option.map ~f:FlowLogSet.of_xml) (Xml.child xml_arg0 "flowLogSet") in
      make ?nextToken ?flowLogs ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let flowLogs = field_map json "FlowLogs" FlowLogSet.of_json in
      make ?nextToken ?flowLogs ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFlowLogsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filter: FilterList.t option ;
      flowLogIds: FlowLogIdList.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    let make ?dryRun =
      fun ?filter ->
        fun ?flowLogIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () -> { dryRun; filter; flowLogIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("FlowLogId", (Option.map x.flowLogIds ~f:FlowLogIdList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let flowLogIds =
        (Option.map ~f:FlowLogIdList.of_xml) (Xml.child xml_arg0 "FlowLogId") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?flowLogIds ?filter ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let flowLogIds = field_map json "FlowLogIds" FlowLogIdList.of_json in
      let filter = field_map json "Filter" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?flowLogIds ?filter ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFleetsResult =
  struct
    type nonrec t = {
      nextToken: String_.t option ;
      fleets: FleetSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?fleets -> fun () -> { nextToken; fleets }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleetSet", (Option.map x.fleets ~f:FleetSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleets =
        (Option.map ~f:FleetSet.of_xml) (Xml.child xml_arg0 "fleetSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?fleets ?nextToken ()
    let of_json json =
      let fleets = field_map json "Fleets" FleetSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?fleets ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFleetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      fleetIds: FleetIdSet.t option ;
      filters: FilterList.t option }
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?fleetIds ->
            fun ?filters ->
              fun () -> { dryRun; maxResults; nextToken; fleetIds; filters }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("FleetId", (Option.map x.fleetIds ~f:FleetIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let fleetIds =
        (Option.map ~f:FleetIdSet.of_xml) (Xml.child xml_arg0 "FleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?fleetIds ?nextToken ?maxResults ?dryRun ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let fleetIds = field_map json "FleetIds" FleetIdSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?filters ?fleetIds ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFleetInstancesResult =
  struct
    type nonrec t =
      {
      activeInstances: ActiveInstanceSet.t option ;
      nextToken: String_.t option ;
      fleetId: FleetId.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?activeInstances =
      fun ?nextToken ->
        fun ?fleetId -> fun () -> { activeInstances; nextToken; fleetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("activeInstanceSet",
           (Option.map x.activeInstances ~f:ActiveInstanceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleetId", (Option.map x.fleetId ~f:FleetId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "fleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let activeInstances =
        (Option.map ~f:ActiveInstanceSet.of_xml)
          (Xml.child xml_arg0 "activeInstanceSet") in
      make ?fleetId ?nextToken ?activeInstances ()
    let of_json json =
      let fleetId = field_map json "FleetId" FleetId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let activeInstances =
        field_map json "ActiveInstances" ActiveInstanceSet.of_json in
      make ?fleetId ?nextToken ?activeInstances ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFleetInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      fleetId: FleetId.t ;
      filters: FilterList.t option }
    let context_ = "DescribeFleetInstancesRequest"
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ~fleetId ->
              fun () -> { dryRun; maxResults; nextToken; filters; fleetId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ~fleetId ?nextToken ?maxResults ?dryRun ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let fleetId = field_map_exn json "FleetId" FleetId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?filters ~fleetId ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFleetHistoryResult =
  struct
    type nonrec t =
      {
      historyRecords: HistoryRecordSet.t option ;
      lastEvaluatedTime: DateTime.t option ;
      nextToken: String_.t option ;
      fleetId: FleetId.t option ;
      startTime: DateTime.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?historyRecords =
      fun ?lastEvaluatedTime ->
        fun ?nextToken ->
          fun ?fleetId ->
            fun ?startTime ->
              fun () ->
                {
                  historyRecords;
                  lastEvaluatedTime;
                  nextToken;
                  fleetId;
                  startTime
                }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("historyRecordSet",
           (Option.map x.historyRecords ~f:HistoryRecordSet.to_value));
        ("lastEvaluatedTime",
          (Option.map x.lastEvaluatedTime ~f:DateTime.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("startTime", (Option.map x.startTime ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "startTime") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "fleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let lastEvaluatedTime =
        (Option.map ~f:DateTime.of_xml)
          (Xml.child xml_arg0 "lastEvaluatedTime") in
      let historyRecords =
        (Option.map ~f:HistoryRecordSet.of_xml)
          (Xml.child xml_arg0 "historyRecordSet") in
      make ?startTime ?fleetId ?nextToken ?lastEvaluatedTime ?historyRecords
        ()
    let of_json json =
      let startTime = field_map json "StartTime" DateTime.of_json in
      let fleetId = field_map json "FleetId" FleetId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let lastEvaluatedTime =
        field_map json "LastEvaluatedTime" DateTime.of_json in
      let historyRecords =
        field_map json "HistoryRecords" HistoryRecordSet.of_json in
      make ?startTime ?fleetId ?nextToken ?lastEvaluatedTime ?historyRecords
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFleetHistoryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      eventType: FleetEventType.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option ;
      fleetId: FleetId.t ;
      startTime: DateTime.t }
    let context_ = "DescribeFleetHistoryRequest"
    let make ?dryRun =
      fun ?eventType ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ~fleetId ->
              fun ~startTime ->
                fun () ->
                  {
                    dryRun;
                    eventType;
                    maxResults;
                    nextToken;
                    fleetId;
                    startTime
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("EventType", (Option.map x.eventType ~f:FleetEventType.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("StartTime", (Some (DateTime.to_value x.startTime)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        DateTime.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let eventType =
        (Option.map ~f:FleetEventType.of_xml)
          (Xml.child xml_arg0 "EventType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~startTime ~fleetId ?nextToken ?maxResults ?eventType ?dryRun ()
    let of_json json =
      let startTime = field_map_exn json "StartTime" DateTime.of_json in
      let fleetId = field_map_exn json "FleetId" FleetId.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let eventType = field_map json "EventType" FleetEventType.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~startTime ~fleetId ?nextToken ?maxResults ?eventType ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFastSnapshotRestoresResult =
  struct
    type nonrec t =
      {
      fastSnapshotRestores: DescribeFastSnapshotRestoreSuccessSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fastSnapshotRestores =
      fun ?nextToken -> fun () -> { fastSnapshotRestores; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fastSnapshotRestoreSet",
           (Option.map x.fastSnapshotRestores
              ~f:DescribeFastSnapshotRestoreSuccessSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let fastSnapshotRestores =
        (Option.map ~f:DescribeFastSnapshotRestoreSuccessSet.of_xml)
          (Xml.child xml_arg0 "fastSnapshotRestoreSet") in
      make ?nextToken ?fastSnapshotRestores ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let fastSnapshotRestores =
        field_map json "FastSnapshotRestores"
          DescribeFastSnapshotRestoreSuccessSet.of_json in
      make ?nextToken ?fastSnapshotRestores ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFastSnapshotRestoresRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      maxResults: DescribeFastSnapshotRestoresMaxResults.t option ;
      nextToken: NextToken.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun -> fun () -> { filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeFastSnapshotRestoresMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeFastSnapshotRestoresMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?nextToken ?maxResults ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeFastSnapshotRestoresMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFastLaunchImagesResult =
  struct
    type nonrec t =
      {
      fastLaunchImages: DescribeFastLaunchImagesSuccessSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fastLaunchImages =
      fun ?nextToken -> fun () -> { fastLaunchImages; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fastLaunchImageSet",
           (Option.map x.fastLaunchImages
              ~f:DescribeFastLaunchImagesSuccessSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let fastLaunchImages =
        (Option.map ~f:DescribeFastLaunchImagesSuccessSet.of_xml)
          (Xml.child xml_arg0 "fastLaunchImageSet") in
      make ?nextToken ?fastLaunchImages ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let fastLaunchImages =
        field_map json "FastLaunchImages"
          DescribeFastLaunchImagesSuccessSet.of_json in
      make ?nextToken ?fastLaunchImages ()
    let to_json v = composed_to_json to_value v
  end
module DescribeFastLaunchImagesRequest =
  struct
    type nonrec t =
      {
      imageIds: FastLaunchImageIdList.t option ;
      filters: FilterList.t option ;
      maxResults: DescribeFastLaunchImagesRequestMaxResults.t option ;
      nextToken: NextToken.t option ;
      dryRun: Boolean.t option }
    let make ?imageIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () -> { imageIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("ImageId",
           (Option.map x.imageIds ~f:FastLaunchImageIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeFastLaunchImagesRequestMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeFastLaunchImagesRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let imageIds =
        (Option.map ~f:FastLaunchImageIdList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?imageIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeFastLaunchImagesRequestMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let imageIds = field_map json "ImageIds" FastLaunchImageIdList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?imageIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeExportTasksResult =
  struct
    type nonrec t = {
      exportTasks: ExportTaskList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exportTasks = fun () -> { exportTasks }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exportTaskSet",
           (Option.map x.exportTasks ~f:ExportTaskList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTasks =
        (Option.map ~f:ExportTaskList.of_xml)
          (Xml.child xml_arg0 "exportTaskSet") in
      make ?exportTasks ()
    let of_json json =
      let exportTasks = field_map json "ExportTasks" ExportTaskList.of_json in
      make ?exportTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeExportTasksRequest =
  struct
    type nonrec t =
      {
      exportTaskIds: ExportTaskIdStringList.t option ;
      filters: FilterList.t option }
    let make ?exportTaskIds =
      fun ?filters -> fun () -> { exportTaskIds; filters }
    let to_value x =
      structure_to_value
        [("exportTaskId",
           (Option.map x.exportTaskIds ~f:ExportTaskIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let exportTaskIds =
        (Option.map ~f:ExportTaskIdStringList.of_xml)
          (Xml.child xml_arg0 "exportTaskId") in
      make ?filters ?exportTaskIds ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let exportTaskIds =
        field_map json "ExportTaskIds" ExportTaskIdStringList.of_json in
      make ?filters ?exportTaskIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeExportImageTasksResult =
  struct
    type nonrec t =
      {
      exportImageTasks: ExportImageTaskList.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exportImageTasks =
      fun ?nextToken -> fun () -> { exportImageTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exportImageTaskSet",
           (Option.map x.exportImageTasks ~f:ExportImageTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let exportImageTasks =
        (Option.map ~f:ExportImageTaskList.of_xml)
          (Xml.child xml_arg0 "exportImageTaskSet") in
      make ?nextToken ?exportImageTasks ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let exportImageTasks =
        field_map json "ExportImageTasks" ExportImageTaskList.of_json in
      make ?nextToken ?exportImageTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeExportImageTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      exportImageTaskIds: ExportImageTaskIdList.t option ;
      maxResults: DescribeExportImageTasksMaxResults.t option ;
      nextToken: NextToken.t option }
    let make ?dryRun =
      fun ?filters ->
        fun ?exportImageTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; exportImageTaskIds; maxResults; nextToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("ExportImageTaskId",
          (Option.map x.exportImageTaskIds ~f:ExportImageTaskIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeExportImageTasksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeExportImageTasksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let exportImageTaskIds =
        (Option.map ~f:ExportImageTaskIdList.of_xml)
          (Xml.child xml_arg0 "ExportImageTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?exportImageTaskIds ?filters ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeExportImageTasksMaxResults.of_json in
      let exportImageTaskIds =
        field_map json "ExportImageTaskIds" ExportImageTaskIdList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?exportImageTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeElasticGpusResult =
  struct
    type nonrec t =
      {
      elasticGpuSet: ElasticGpuSet.t option ;
      maxResults: Integer.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?elasticGpuSet =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { elasticGpuSet; maxResults; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("elasticGpuSet",
           (Option.map x.elasticGpuSet ~f:ElasticGpuSet.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let elasticGpuSet =
        (Option.map ~f:ElasticGpuSet.of_xml)
          (Xml.child xml_arg0 "elasticGpuSet") in
      make ?nextToken ?maxResults ?elasticGpuSet ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let elasticGpuSet =
        field_map json "ElasticGpuSet" ElasticGpuSet.of_json in
      make ?nextToken ?maxResults ?elasticGpuSet ()
    let to_json v = composed_to_json to_value v
  end
module DescribeElasticGpusRequest =
  struct
    type nonrec t =
      {
      elasticGpuIds: ElasticGpuIdSet.t option ;
      dryRun: Boolean.t option ;
      filters: FilterList.t option ;
      maxResults: DescribeElasticGpusMaxResults.t option ;
      nextToken: String_.t option }
    let make ?elasticGpuIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { elasticGpuIds; dryRun; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("ElasticGpuId",
           (Option.map x.elasticGpuIds ~f:ElasticGpuIdSet.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeElasticGpusMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeElasticGpusMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let elasticGpuIds =
        (Option.map ~f:ElasticGpuIdSet.of_xml)
          (Xml.child xml_arg0 "ElasticGpuId") in
      make ?nextToken ?maxResults ?filters ?dryRun ?elasticGpuIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" DescribeElasticGpusMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let elasticGpuIds =
        field_map json "ElasticGpuIds" ElasticGpuIdSet.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ?elasticGpuIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeEgressOnlyInternetGatewaysResult =
  struct
    type nonrec t =
      {
      egressOnlyInternetGateways: EgressOnlyInternetGatewayList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?egressOnlyInternetGateways =
      fun ?nextToken -> fun () -> { egressOnlyInternetGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("egressOnlyInternetGatewaySet",
           (Option.map x.egressOnlyInternetGateways
              ~f:EgressOnlyInternetGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let egressOnlyInternetGateways =
        (Option.map ~f:EgressOnlyInternetGatewayList.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGatewaySet") in
      make ?nextToken ?egressOnlyInternetGateways ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let egressOnlyInternetGateways =
        field_map json "EgressOnlyInternetGateways"
          EgressOnlyInternetGatewayList.of_json in
      make ?nextToken ?egressOnlyInternetGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeEgressOnlyInternetGatewaysRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      egressOnlyInternetGatewayIds: EgressOnlyInternetGatewayIdList.t option ;
      maxResults: DescribeEgressOnlyInternetGatewaysMaxResults.t option ;
      nextToken: String_.t option ;
      filters: FilterList.t option }
    let make ?dryRun =
      fun ?egressOnlyInternetGatewayIds ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?filters ->
              fun () ->
                {
                  dryRun;
                  egressOnlyInternetGatewayIds;
                  maxResults;
                  nextToken;
                  filters
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("EgressOnlyInternetGatewayId",
          (Option.map x.egressOnlyInternetGatewayIds
             ~f:EgressOnlyInternetGatewayIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeEgressOnlyInternetGatewaysMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeEgressOnlyInternetGatewaysMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let egressOnlyInternetGatewayIds =
        (Option.map ~f:EgressOnlyInternetGatewayIdList.of_xml)
          (Xml.child xml_arg0 "EgressOnlyInternetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?nextToken ?maxResults ?egressOnlyInternetGatewayIds
        ?dryRun ()
    let of_json json =
      let filters = field_map json "Filters" FilterList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeEgressOnlyInternetGatewaysMaxResults.of_json in
      let egressOnlyInternetGatewayIds =
        field_map json "EgressOnlyInternetGatewayIds"
          EgressOnlyInternetGatewayIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?filters ?nextToken ?maxResults ?egressOnlyInternetGatewayIds
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeDhcpOptionsResult =
  struct
    type nonrec t =
      {
      dhcpOptions: DhcpOptionsList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?dhcpOptions =
      fun ?nextToken -> fun () -> { dhcpOptions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("dhcpOptionsSet",
           (Option.map x.dhcpOptions ~f:DhcpOptionsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let dhcpOptions =
        (Option.map ~f:DhcpOptionsList.of_xml)
          (Xml.child xml_arg0 "dhcpOptionsSet") in
      make ?nextToken ?dhcpOptions ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let dhcpOptions = field_map json "DhcpOptions" DhcpOptionsList.of_json in
      make ?nextToken ?dhcpOptions ()
    let to_json v = composed_to_json to_value v
  end
module DescribeDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpOptionsIds: DhcpOptionsIdStringList.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeDhcpOptionsMaxResults.t option }
    let make ?dhcpOptionsIds =
      fun ?filters ->
        fun ?dryRun ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { dhcpOptionsIds; filters; dryRun; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("DhcpOptionsId",
           (Option.map x.dhcpOptionsIds ~f:DhcpOptionsIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeDhcpOptionsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeDhcpOptionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dhcpOptionsIds =
        (Option.map ~f:DhcpOptionsIdStringList.of_xml)
          (Xml.child xml_arg0 "DhcpOptionsId") in
      make ?maxResults ?nextToken ?dryRun ?filters ?dhcpOptionsIds ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" DescribeDhcpOptionsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dhcpOptionsIds =
        field_map json "DhcpOptionsIds" DhcpOptionsIdStringList.of_json in
      make ?maxResults ?nextToken ?dryRun ?filters ?dhcpOptionsIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCustomerGatewaysResult =
  struct
    type nonrec t = {
      customerGateways: CustomerGatewayList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?customerGateways = fun () -> { customerGateways }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("customerGatewaySet",
           (Option.map x.customerGateways ~f:CustomerGatewayList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customerGateways =
        (Option.map ~f:CustomerGatewayList.of_xml)
          (Xml.child xml_arg0 "customerGatewaySet") in
      make ?customerGateways ()
    let of_json json =
      let customerGateways =
        field_map json "CustomerGateways" CustomerGatewayList.of_json in
      make ?customerGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCustomerGatewaysRequest =
  struct
    type nonrec t =
      {
      customerGatewayIds: CustomerGatewayIdStringList.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option }
    let make ?customerGatewayIds =
      fun ?filters ->
        fun ?dryRun -> fun () -> { customerGatewayIds; filters; dryRun }
    let to_value x =
      structure_to_value
        [("CustomerGatewayId",
           (Option.map x.customerGatewayIds
              ~f:CustomerGatewayIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let customerGatewayIds =
        (Option.map ~f:CustomerGatewayIdStringList.of_xml)
          (Xml.child xml_arg0 "CustomerGatewayId") in
      make ?dryRun ?filters ?customerGatewayIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let customerGatewayIds =
        field_map json "CustomerGatewayIds"
          CustomerGatewayIdStringList.of_json in
      make ?dryRun ?filters ?customerGatewayIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeConversionTasksResult =
  struct
    type nonrec t = {
      conversionTasks: DescribeConversionTaskList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?conversionTasks = fun () -> { conversionTasks }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("conversionTasks",
           (Option.map x.conversionTasks
              ~f:DescribeConversionTaskList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conversionTasks =
        (Option.map ~f:DescribeConversionTaskList.of_xml)
          (Xml.child xml_arg0 "conversionTasks") in
      make ?conversionTasks ()
    let of_json json =
      let conversionTasks =
        field_map json "ConversionTasks" DescribeConversionTaskList.of_json in
      make ?conversionTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeConversionTasksRequest =
  struct
    type nonrec t =
      {
      conversionTaskIds: ConversionIdStringList.t option ;
      dryRun: Boolean.t option }
    let make ?conversionTaskIds =
      fun ?dryRun -> fun () -> { conversionTaskIds; dryRun }
    let to_value x =
      structure_to_value
        [("conversionTaskId",
           (Option.map x.conversionTaskIds ~f:ConversionIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let conversionTaskIds =
        (Option.map ~f:ConversionIdStringList.of_xml)
          (Xml.child xml_arg0 "conversionTaskId") in
      make ?dryRun ?conversionTaskIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let conversionTaskIds =
        field_map json "ConversionTaskIds" ConversionIdStringList.of_json in
      make ?dryRun ?conversionTaskIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCoipPoolsResult =
  struct
    type nonrec t =
      {
      coipPools: CoipPoolSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipPools =
      fun ?nextToken -> fun () -> { coipPools; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipPoolSet", (Option.map x.coipPools ~f:CoipPoolSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let coipPools =
        (Option.map ~f:CoipPoolSet.of_xml) (Xml.child xml_arg0 "coipPoolSet") in
      make ?nextToken ?coipPools ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let coipPools = field_map json "CoipPools" CoipPoolSet.of_json in
      make ?nextToken ?coipPools ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCoipPoolsRequest =
  struct
    type nonrec t =
      {
      poolIds: CoipPoolIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: CoipPoolMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?poolIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () -> { poolIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("PoolId", (Option.map x.poolIds ~f:CoipPoolIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:CoipPoolMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:CoipPoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let poolIds =
        (Option.map ~f:CoipPoolIdSet.of_xml) (Xml.child xml_arg0 "PoolId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?poolIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" CoipPoolMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let poolIds = field_map json "PoolIds" CoipPoolIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?poolIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnTargetNetworksResult =
  struct
    type nonrec t =
      {
      clientVpnTargetNetworks: TargetNetworkSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnTargetNetworks =
      fun ?nextToken -> fun () -> { clientVpnTargetNetworks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnTargetNetworks",
           (Option.map x.clientVpnTargetNetworks ~f:TargetNetworkSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let clientVpnTargetNetworks =
        (Option.map ~f:TargetNetworkSet.of_xml)
          (Xml.child xml_arg0 "clientVpnTargetNetworks") in
      make ?nextToken ?clientVpnTargetNetworks ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let clientVpnTargetNetworks =
        field_map json "ClientVpnTargetNetworks" TargetNetworkSet.of_json in
      make ?nextToken ?clientVpnTargetNetworks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnTargetNetworksRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      associationIds: ValueStringList.t option ;
      maxResults: DescribeClientVpnTargetNetworksMaxResults.t option ;
      nextToken: NextToken.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option }
    let context_ = "DescribeClientVpnTargetNetworksRequest"
    let make ?associationIds =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?dryRun ->
              fun ~clientVpnEndpointId ->
                fun () ->
                  {
                    associationIds;
                    maxResults;
                    nextToken;
                    filters;
                    dryRun;
                    clientVpnEndpointId
                  }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("AssociationIds",
          (Option.map x.associationIds ~f:ValueStringList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnTargetNetworksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnTargetNetworksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let associationIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "AssociationIds") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?filters ?nextToken ?maxResults ?associationIds
        ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeClientVpnTargetNetworksMaxResults.of_json in
      let associationIds =
        field_map json "AssociationIds" ValueStringList.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?filters ?nextToken ?maxResults ?associationIds
        ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnRoutesResult =
  struct
    type nonrec t =
      {
      routes: ClientVpnRouteSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routes = fun ?nextToken -> fun () -> { routes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routes", (Option.map x.routes ~f:ClientVpnRouteSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routes =
        (Option.map ~f:ClientVpnRouteSet.of_xml)
          (Xml.child xml_arg0 "routes") in
      make ?nextToken ?routes ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let routes = field_map json "Routes" ClientVpnRouteSet.of_json in
      make ?nextToken ?routes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnRoutesRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      filters: FilterList.t option ;
      maxResults: DescribeClientVpnRoutesMaxResults.t option ;
      nextToken: NextToken.t option ;
      dryRun: Boolean.t option }
    let context_ = "DescribeClientVpnRoutesRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~clientVpnEndpointId ->
              fun () ->
                { filters; maxResults; nextToken; dryRun; clientVpnEndpointId
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnRoutesMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnRoutesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?nextToken ?maxResults ?filters ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" DescribeClientVpnRoutesMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnEndpointsResult =
  struct
    type nonrec t =
      {
      clientVpnEndpoints: EndpointSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnEndpoints =
      fun ?nextToken -> fun () -> { clientVpnEndpoints; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnEndpoint",
           (Option.map x.clientVpnEndpoints ~f:EndpointSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let clientVpnEndpoints =
        (Option.map ~f:EndpointSet.of_xml)
          (Xml.child xml_arg0 "clientVpnEndpoint") in
      make ?nextToken ?clientVpnEndpoints ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let clientVpnEndpoints =
        field_map json "ClientVpnEndpoints" EndpointSet.of_json in
      make ?nextToken ?clientVpnEndpoints ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnEndpointsRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointIds: ClientVpnEndpointIdList.t option ;
      maxResults: DescribeClientVpnEndpointMaxResults.t option ;
      nextToken: NextToken.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option }
    let make ?clientVpnEndpointIds =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  clientVpnEndpointIds;
                  maxResults;
                  nextToken;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Option.map x.clientVpnEndpointIds
              ~f:ClientVpnEndpointIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnEndpointMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnEndpointMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let clientVpnEndpointIds =
        (Option.map ~f:ClientVpnEndpointIdList.of_xml)
          (Xml.child xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?filters ?nextToken ?maxResults ?clientVpnEndpointIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeClientVpnEndpointMaxResults.of_json in
      let clientVpnEndpointIds =
        field_map json "ClientVpnEndpointIds" ClientVpnEndpointIdList.of_json in
      make ?dryRun ?filters ?nextToken ?maxResults ?clientVpnEndpointIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnConnectionsResult =
  struct
    type nonrec t =
      {
      connections: ClientVpnConnectionSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?connections =
      fun ?nextToken -> fun () -> { connections; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("connections",
           (Option.map x.connections ~f:ClientVpnConnectionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let connections =
        (Option.map ~f:ClientVpnConnectionSet.of_xml)
          (Xml.child xml_arg0 "connections") in
      make ?nextToken ?connections ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let connections =
        field_map json "Connections" ClientVpnConnectionSet.of_json in
      make ?nextToken ?connections ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnConnectionsRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      filters: FilterList.t option ;
      nextToken: NextToken.t option ;
      maxResults: DescribeClientVpnConnectionsMaxResults.t option ;
      dryRun: Boolean.t option }
    let context_ = "DescribeClientVpnConnectionsRequest"
    let make ?filters =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ~clientVpnEndpointId ->
              fun () ->
                { filters; nextToken; maxResults; dryRun; clientVpnEndpointId
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnConnectionsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnConnectionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?maxResults ?nextToken ?filters ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeClientVpnConnectionsMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?maxResults ?nextToken ?filters ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnAuthorizationRulesResult =
  struct
    type nonrec t =
      {
      authorizationRules: AuthorizationRuleSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?authorizationRules =
      fun ?nextToken -> fun () -> { authorizationRules; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("authorizationRule",
           (Option.map x.authorizationRules ~f:AuthorizationRuleSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let authorizationRules =
        (Option.map ~f:AuthorizationRuleSet.of_xml)
          (Xml.child xml_arg0 "authorizationRule") in
      make ?nextToken ?authorizationRules ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let authorizationRules =
        field_map json "AuthorizationRules" AuthorizationRuleSet.of_json in
      make ?nextToken ?authorizationRules ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClientVpnAuthorizationRulesRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      dryRun: Boolean.t option ;
      nextToken: NextToken.t option ;
      filters: FilterList.t option ;
      maxResults: DescribeClientVpnAuthorizationRulesMaxResults.t option }
    let context_ = "DescribeClientVpnAuthorizationRulesRequest"
    let make ?dryRun =
      fun ?nextToken ->
        fun ?filters ->
          fun ?maxResults ->
            fun ~clientVpnEndpointId ->
              fun () ->
                { dryRun; nextToken; filters; maxResults; clientVpnEndpointId
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnAuthorizationRulesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeClientVpnAuthorizationRulesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?maxResults ?filters ?nextToken ?dryRun ~clientVpnEndpointId ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          DescribeClientVpnAuthorizationRulesMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?maxResults ?filters ?nextToken ?dryRun ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClassicLinkInstancesResult =
  struct
    type nonrec t =
      {
      instances: ClassicLinkInstanceList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instances =
      fun ?nextToken -> fun () -> { instances; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.instances ~f:ClassicLinkInstanceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instances =
        (Option.map ~f:ClassicLinkInstanceList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?nextToken ?instances ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let instances =
        field_map json "Instances" ClassicLinkInstanceList.of_json in
      make ?nextToken ?instances ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClassicLinkInstancesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      dryRun: Boolean.t option ;
      instanceIds: InstanceIdStringList.t option ;
      maxResults: DescribeClassicLinkInstancesMaxResults.t option ;
      nextToken: String_.t option }
    let make ?filters =
      fun ?dryRun ->
        fun ?instanceIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { filters; dryRun; instanceIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:DescribeClassicLinkInstancesMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:DescribeClassicLinkInstancesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?maxResults ?instanceIds ?dryRun ?filters ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeClassicLinkInstancesMaxResults.of_json in
      let instanceIds =
        field_map json "InstanceIds" InstanceIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?nextToken ?maxResults ?instanceIds ?dryRun ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCarrierGatewaysResult =
  struct
    type nonrec t =
      {
      carrierGateways: CarrierGatewaySet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?carrierGateways =
      fun ?nextToken -> fun () -> { carrierGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("carrierGatewaySet",
           (Option.map x.carrierGateways ~f:CarrierGatewaySet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let carrierGateways =
        (Option.map ~f:CarrierGatewaySet.of_xml)
          (Xml.child xml_arg0 "carrierGatewaySet") in
      make ?nextToken ?carrierGateways ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let carrierGateways =
        field_map json "CarrierGateways" CarrierGatewaySet.of_json in
      make ?nextToken ?carrierGateways ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCarrierGatewaysRequest =
  struct
    type nonrec t =
      {
      carrierGatewayIds: CarrierGatewayIdSet.t option ;
      filters: FilterList.t option ;
      maxResults: CarrierGatewayMaxResults.t option ;
      nextToken: String_.t option ;
      dryRun: Boolean.t option }
    let make ?carrierGatewayIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { carrierGatewayIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("CarrierGatewayId",
           (Option.map x.carrierGatewayIds ~f:CarrierGatewayIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:CarrierGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:CarrierGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let carrierGatewayIds =
        (Option.map ~f:CarrierGatewayIdSet.of_xml)
          (Xml.child xml_arg0 "CarrierGatewayId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?carrierGatewayIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" CarrierGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let carrierGatewayIds =
        field_map json "CarrierGatewayIds" CarrierGatewayIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?carrierGatewayIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCapacityReservationsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option ;
      capacityReservations: CapacityReservationSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?capacityReservations ->
        fun () -> { nextToken; capacityReservations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("capacityReservationSet",
          (Option.map x.capacityReservations
             ~f:CapacityReservationSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservations =
        (Option.map ~f:CapacityReservationSet.of_xml)
          (Xml.child xml_arg0 "capacityReservationSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?capacityReservations ?nextToken ()
    let of_json json =
      let capacityReservations =
        field_map json "CapacityReservations" CapacityReservationSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?capacityReservations ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCapacityReservationsRequest =
  struct
    type nonrec t =
      {
      capacityReservationIds: CapacityReservationIdSet.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeCapacityReservationsMaxResults.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option }
    let make ?capacityReservationIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  capacityReservationIds;
                  nextToken;
                  maxResults;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Option.map x.capacityReservationIds
              ~f:CapacityReservationIdSet.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityReservationsMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeCapacityReservationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationIds =
        (Option.map ~f:CapacityReservationIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityReservationIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeCapacityReservationsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let capacityReservationIds =
        field_map json "CapacityReservationIds"
          CapacityReservationIdSet.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityReservationIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCapacityReservationFleetsResult =
  struct
    type nonrec t =
      {
      capacityReservationFleets: CapacityReservationFleetSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityReservationFleets =
      fun ?nextToken -> fun () -> { capacityReservationFleets; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityReservationFleetSet",
           (Option.map x.capacityReservationFleets
              ~f:CapacityReservationFleetSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityReservationFleets =
        (Option.map ~f:CapacityReservationFleetSet.of_xml)
          (Xml.child xml_arg0 "capacityReservationFleetSet") in
      make ?nextToken ?capacityReservationFleets ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let capacityReservationFleets =
        field_map json "CapacityReservationFleets"
          CapacityReservationFleetSet.of_json in
      make ?nextToken ?capacityReservationFleets ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCapacityReservationFleetsRequest =
  struct
    type nonrec t =
      {
      capacityReservationFleetIds: CapacityReservationFleetIdSet.t option ;
      nextToken: String_.t option ;
      maxResults: DescribeCapacityReservationFleetsMaxResults.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option }
    let make ?capacityReservationFleetIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  capacityReservationFleetIds;
                  nextToken;
                  maxResults;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationFleetId",
           (Option.map x.capacityReservationFleetIds
              ~f:CapacityReservationFleetIdSet.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityReservationFleetsMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeCapacityReservationFleetsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationFleetIds =
        (Option.map ~f:CapacityReservationFleetIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationFleetId") in
      make ?dryRun ?filters ?maxResults ?nextToken
        ?capacityReservationFleetIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let maxResults =
        field_map json "MaxResults"
          DescribeCapacityReservationFleetsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let capacityReservationFleetIds =
        field_map json "CapacityReservationFleetIds"
          CapacityReservationFleetIdSet.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken
        ?capacityReservationFleetIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeByoipCidrsResult =
  struct
    type nonrec t =
      {
      byoipCidrs: ByoipCidrSet.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidrs =
      fun ?nextToken -> fun () -> { byoipCidrs; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidrSet", (Option.map x.byoipCidrs ~f:ByoipCidrSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let byoipCidrs =
        (Option.map ~f:ByoipCidrSet.of_xml)
          (Xml.child xml_arg0 "byoipCidrSet") in
      make ?nextToken ?byoipCidrs ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let byoipCidrs = field_map json "ByoipCidrs" ByoipCidrSet.of_json in
      make ?nextToken ?byoipCidrs ()
    let to_json v = composed_to_json to_value v
  end
module DescribeByoipCidrsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      maxResults: DescribeByoipCidrsMaxResults.t ;
      nextToken: NextToken.t option }
    let context_ = "DescribeByoipCidrsRequest"
    let make ?dryRun =
      fun ?nextToken ->
        fun ~maxResults -> fun () -> { dryRun; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Some (DescribeByoipCidrsMaxResults.to_value x.maxResults)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        DescribeByoipCidrsMaxResults.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ~maxResults ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map_exn json "MaxResults" DescribeByoipCidrsMaxResults.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ~maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeBundleTasksResult =
  struct
    type nonrec t = {
      bundleTasks: BundleTaskList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?bundleTasks = fun () -> { bundleTasks }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleInstanceTasksSet",
           (Option.map x.bundleTasks ~f:BundleTaskList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleTasks =
        (Option.map ~f:BundleTaskList.of_xml)
          (Xml.child xml_arg0 "bundleInstanceTasksSet") in
      make ?bundleTasks ()
    let of_json json =
      let bundleTasks = field_map json "BundleTasks" BundleTaskList.of_json in
      make ?bundleTasks ()
    let to_json v = composed_to_json to_value v
  end
module DescribeBundleTasksRequest =
  struct
    type nonrec t =
      {
      bundleIds: BundleIdStringList.t option ;
      filters: FilterList.t option ;
      dryRun: Boolean.t option }
    let make ?bundleIds =
      fun ?filters -> fun ?dryRun -> fun () -> { bundleIds; filters; dryRun }
    let to_value x =
      structure_to_value
        [("BundleId",
           (Option.map x.bundleIds ~f:BundleIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let bundleIds =
        (Option.map ~f:BundleIdStringList.of_xml)
          (Xml.child xml_arg0 "BundleId") in
      make ?dryRun ?filters ?bundleIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let bundleIds = field_map json "BundleIds" BundleIdStringList.of_json in
      make ?dryRun ?filters ?bundleIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAvailabilityZonesResult =
  struct
    type nonrec t = {
      availabilityZones: AvailabilityZoneList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?availabilityZones = fun () -> { availabilityZones }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("availabilityZoneInfo",
           (Option.map x.availabilityZones ~f:AvailabilityZoneList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZones =
        (Option.map ~f:AvailabilityZoneList.of_xml)
          (Xml.child xml_arg0 "availabilityZoneInfo") in
      make ?availabilityZones ()
    let of_json json =
      let availabilityZones =
        field_map json "AvailabilityZones" AvailabilityZoneList.of_json in
      make ?availabilityZones ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAvailabilityZonesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      zoneNames: ZoneNameStringList.t option ;
      zoneIds: ZoneIdStringList.t option ;
      allAvailabilityZones: Boolean.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?zoneNames ->
        fun ?zoneIds ->
          fun ?allAvailabilityZones ->
            fun ?dryRun ->
              fun () ->
                { filters; zoneNames; zoneIds; allAvailabilityZones; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("ZoneName", (Option.map x.zoneNames ~f:ZoneNameStringList.to_value));
        ("ZoneId", (Option.map x.zoneIds ~f:ZoneIdStringList.to_value));
        ("AllAvailabilityZones",
          (Option.map x.allAvailabilityZones ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let allAvailabilityZones =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AllAvailabilityZones") in
      let zoneIds =
        (Option.map ~f:ZoneIdStringList.of_xml) (Xml.child xml_arg0 "ZoneId") in
      let zoneNames =
        (Option.map ~f:ZoneNameStringList.of_xml)
          (Xml.child xml_arg0 "ZoneName") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?allAvailabilityZones ?zoneIds ?zoneNames ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let allAvailabilityZones =
        field_map json "AllAvailabilityZones" Boolean.of_json in
      let zoneIds = field_map json "ZoneIds" ZoneIdStringList.of_json in
      let zoneNames = field_map json "ZoneNames" ZoneNameStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?allAvailabilityZones ?zoneIds ?zoneNames ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAggregateIdFormatResult =
  struct
    type nonrec t =
      {
      useLongIdsAggregated: Boolean.t option ;
      statuses: IdFormatList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?useLongIdsAggregated =
      fun ?statuses -> fun () -> { useLongIdsAggregated; statuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("useLongIdsAggregated",
           (Option.map x.useLongIdsAggregated ~f:Boolean.to_value));
        ("statusSet", (Option.map x.statuses ~f:IdFormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:IdFormatList.of_xml) (Xml.child xml_arg0 "statusSet") in
      let useLongIdsAggregated =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "useLongIdsAggregated") in
      make ?statuses ?useLongIdsAggregated ()
    let of_json json =
      let statuses = field_map json "Statuses" IdFormatList.of_json in
      let useLongIdsAggregated =
        field_map json "UseLongIdsAggregated" Boolean.of_json in
      make ?statuses ?useLongIdsAggregated ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAggregateIdFormatRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAddressesResult =
  struct
    type nonrec t = {
      addresses: AddressList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?addresses = fun () -> { addresses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("addressesSet", (Option.map x.addresses ~f:AddressList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let addresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "addressesSet") in
      make ?addresses ()
    let of_json json =
      let addresses = field_map json "Addresses" AddressList.of_json in
      make ?addresses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAddressesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option ;
      publicIps: PublicIpStringList.t option ;
      allocationIds: AllocationIdList.t option ;
      dryRun: Boolean.t option }
    let make ?filters =
      fun ?publicIps ->
        fun ?allocationIds ->
          fun ?dryRun ->
            fun () -> { filters; publicIps; allocationIds; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("PublicIp", (Option.map x.publicIps ~f:PublicIpStringList.to_value));
        ("AllocationId",
          (Option.map x.allocationIds ~f:AllocationIdList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let allocationIds =
        (Option.map ~f:AllocationIdList.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      let publicIps =
        (Option.map ~f:PublicIpStringList.of_xml)
          (Xml.child xml_arg0 "PublicIp") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?allocationIds ?publicIps ?filters ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let allocationIds =
        field_map json "AllocationIds" AllocationIdList.of_json in
      let publicIps = field_map json "PublicIps" PublicIpStringList.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?dryRun ?allocationIds ?publicIps ?filters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAddressesAttributeResult =
  struct
    type nonrec t =
      {
      addresses: AddressSet.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?addresses =
      fun ?nextToken -> fun () -> { addresses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("addressSet", (Option.map x.addresses ~f:AddressSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let addresses =
        (Option.map ~f:AddressSet.of_xml) (Xml.child xml_arg0 "addressSet") in
      make ?nextToken ?addresses ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let addresses = field_map json "Addresses" AddressSet.of_json in
      make ?nextToken ?addresses ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAddressesAttributeRequest =
  struct
    type nonrec t =
      {
      allocationIds: AllocationIds.t option ;
      attribute: AddressAttributeName.t option ;
      nextToken: NextToken.t option ;
      maxResults: AddressMaxResults.t option ;
      dryRun: Boolean.t option }
    let make ?allocationIds =
      fun ?attribute ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?dryRun ->
              fun () ->
                { allocationIds; attribute; nextToken; maxResults; dryRun }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationIds ~f:AllocationIds.to_value));
        ("Attribute",
          (Option.map x.attribute ~f:AddressAttributeName.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:AddressMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:AddressMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let attribute =
        (Option.map ~f:AddressAttributeName.of_xml)
          (Xml.child xml_arg0 "Attribute") in
      let allocationIds =
        (Option.map ~f:AllocationIds.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?dryRun ?maxResults ?nextToken ?attribute ?allocationIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults = field_map json "MaxResults" AddressMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let attribute = field_map json "Attribute" AddressAttributeName.of_json in
      let allocationIds =
        field_map json "AllocationIds" AllocationIds.of_json in
      make ?dryRun ?maxResults ?nextToken ?attribute ?allocationIds ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAccountAttributesResult =
  struct
    type nonrec t = {
      accountAttributes: AccountAttributeList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?accountAttributes = fun () -> { accountAttributes }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("accountAttributeSet",
           (Option.map x.accountAttributes ~f:AccountAttributeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountAttributes =
        (Option.map ~f:AccountAttributeList.of_xml)
          (Xml.child xml_arg0 "accountAttributeSet") in
      make ?accountAttributes ()
    let of_json json =
      let accountAttributes =
        field_map json "AccountAttributes" AccountAttributeList.of_json in
      make ?accountAttributes ()
    let to_json v = composed_to_json to_value v
  end
module DescribeAccountAttributesRequest =
  struct
    type nonrec t =
      {
      attributeNames: AccountAttributeNameStringList.t option ;
      dryRun: Boolean.t option }
    let make ?attributeNames =
      fun ?dryRun -> fun () -> { attributeNames; dryRun }
    let to_value x =
      structure_to_value
        [("attributeName",
           (Option.map x.attributeNames
              ~f:AccountAttributeNameStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let attributeNames =
        (Option.map ~f:AccountAttributeNameStringList.of_xml)
          (Xml.child xml_arg0 "attributeName") in
      make ?dryRun ?attributeNames ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let attributeNames =
        field_map json "AttributeNames"
          AccountAttributeNameStringList.of_json in
      make ?dryRun ?attributeNames ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterTransitGatewayMulticastGroupSourcesResult =
  struct
    type nonrec t =
      {
      deregisteredMulticastGroupSources:
        TransitGatewayMulticastDeregisteredGroupSources.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?deregisteredMulticastGroupSources =
      fun () -> { deregisteredMulticastGroupSources }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deregisteredMulticastGroupSources",
           (Option.map x.deregisteredMulticastGroupSources
              ~f:TransitGatewayMulticastDeregisteredGroupSources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deregisteredMulticastGroupSources =
        (Option.map ~f:TransitGatewayMulticastDeregisteredGroupSources.of_xml)
          (Xml.child xml_arg0 "deregisteredMulticastGroupSources") in
      make ?deregisteredMulticastGroupSources ()
    let of_json json =
      let deregisteredMulticastGroupSources =
        field_map json "DeregisteredMulticastGroupSources"
          TransitGatewayMulticastDeregisteredGroupSources.of_json in
      make ?deregisteredMulticastGroupSources ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterTransitGatewayMulticastGroupSourcesRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option ;
      groupIpAddress: String_.t option ;
      networkInterfaceIds: TransitGatewayNetworkInterfaceIdList.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayMulticastDomainId =
      fun ?groupIpAddress ->
        fun ?networkInterfaceIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                groupIpAddress;
                networkInterfaceIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("GroupIpAddress", (Option.map x.groupIpAddress ~f:String_.to_value));
        ("NetworkInterfaceIds",
          (Option.map x.networkInterfaceIds
             ~f:TransitGatewayNetworkInterfaceIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInterfaceIds =
        (Option.map ~f:TransitGatewayNetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceIds") in
      let groupIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupIpAddress") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInterfaceIds =
        field_map json "NetworkInterfaceIds"
          TransitGatewayNetworkInterfaceIdList.of_json in
      let groupIpAddress = field_map json "GroupIpAddress" String_.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterTransitGatewayMulticastGroupMembersResult =
  struct
    type nonrec t =
      {
      deregisteredMulticastGroupMembers:
        TransitGatewayMulticastDeregisteredGroupMembers.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?deregisteredMulticastGroupMembers =
      fun () -> { deregisteredMulticastGroupMembers }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deregisteredMulticastGroupMembers",
           (Option.map x.deregisteredMulticastGroupMembers
              ~f:TransitGatewayMulticastDeregisteredGroupMembers.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deregisteredMulticastGroupMembers =
        (Option.map ~f:TransitGatewayMulticastDeregisteredGroupMembers.of_xml)
          (Xml.child xml_arg0 "deregisteredMulticastGroupMembers") in
      make ?deregisteredMulticastGroupMembers ()
    let of_json json =
      let deregisteredMulticastGroupMembers =
        field_map json "DeregisteredMulticastGroupMembers"
          TransitGatewayMulticastDeregisteredGroupMembers.of_json in
      make ?deregisteredMulticastGroupMembers ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterTransitGatewayMulticastGroupMembersRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option ;
      groupIpAddress: String_.t option ;
      networkInterfaceIds: TransitGatewayNetworkInterfaceIdList.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayMulticastDomainId =
      fun ?groupIpAddress ->
        fun ?networkInterfaceIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                groupIpAddress;
                networkInterfaceIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("GroupIpAddress", (Option.map x.groupIpAddress ~f:String_.to_value));
        ("NetworkInterfaceIds",
          (Option.map x.networkInterfaceIds
             ~f:TransitGatewayNetworkInterfaceIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInterfaceIds =
        (Option.map ~f:TransitGatewayNetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceIds") in
      let groupIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupIpAddress") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInterfaceIds =
        field_map json "NetworkInterfaceIds"
          TransitGatewayNetworkInterfaceIdList.of_json in
      let groupIpAddress = field_map json "GroupIpAddress" String_.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterInstanceEventNotificationAttributesResult =
  struct
    type nonrec t =
      {
      instanceTagAttribute: InstanceTagNotificationAttribute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTagAttribute = fun () -> { instanceTagAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTagAttribute",
           (Option.map x.instanceTagAttribute
              ~f:InstanceTagNotificationAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:InstanceTagNotificationAttribute.of_xml)
          (Xml.child xml_arg0 "instanceTagAttribute") in
      make ?instanceTagAttribute ()
    let of_json json =
      let instanceTagAttribute =
        field_map json "InstanceTagAttribute"
          InstanceTagNotificationAttribute.of_json in
      make ?instanceTagAttribute ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterInstanceEventNotificationAttributesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      instanceTagAttribute: DeregisterInstanceTagAttributeRequest.t option }
    let make ?dryRun =
      fun ?instanceTagAttribute -> fun () -> { dryRun; instanceTagAttribute }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceTagAttribute",
          (Option.map x.instanceTagAttribute
             ~f:DeregisterInstanceTagAttributeRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:DeregisterInstanceTagAttributeRequest.of_xml)
          (Xml.child xml_arg0 "InstanceTagAttribute") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?instanceTagAttribute ?dryRun ()
    let of_json json =
      let instanceTagAttribute =
        field_map json "InstanceTagAttribute"
          DeregisterInstanceTagAttributeRequest.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?instanceTagAttribute ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterImageRequest =
  struct
    type nonrec t = {
      imageId: ImageId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeregisterImageRequest"
    let make ?dryRun = fun ~imageId -> fun () -> { dryRun; imageId }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ~imageId ()
    let to_json v = composed_to_json to_value v
  end
module DeprovisionPublicIpv4PoolCidrResult =
  struct
    type nonrec t =
      {
      poolId: Ipv4PoolEc2Id.t option ;
      deprovisionedAddresses: DeprovisionedAddressSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?poolId =
      fun ?deprovisionedAddresses ->
        fun () -> { poolId; deprovisionedAddresses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("poolId", (Option.map x.poolId ~f:Ipv4PoolEc2Id.to_value));
        ("deprovisionedAddressSet",
          (Option.map x.deprovisionedAddresses
             ~f:DeprovisionedAddressSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deprovisionedAddresses =
        (Option.map ~f:DeprovisionedAddressSet.of_xml)
          (Xml.child xml_arg0 "deprovisionedAddressSet") in
      let poolId =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml) (Xml.child xml_arg0 "poolId") in
      make ?deprovisionedAddresses ?poolId ()
    let of_json json =
      let deprovisionedAddresses =
        field_map json "DeprovisionedAddresses"
          DeprovisionedAddressSet.of_json in
      let poolId = field_map json "PoolId" Ipv4PoolEc2Id.of_json in
      make ?deprovisionedAddresses ?poolId ()
    let to_json v = composed_to_json to_value v
  end
module DeprovisionPublicIpv4PoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      poolId: Ipv4PoolEc2Id.t ;
      cidr: String_.t }
    let context_ = "DeprovisionPublicIpv4PoolCidrRequest"
    let make ?dryRun =
      fun ~poolId -> fun ~cidr -> fun () -> { dryRun; poolId; cidr }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PoolId", (Some (Ipv4PoolEc2Id.to_value x.poolId)));
        ("Cidr", (Some (String_.to_value x.cidr)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let poolId =
        Ipv4PoolEc2Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~cidr ~poolId ?dryRun ()
    let of_json json =
      let cidr = field_map_exn json "Cidr" String_.of_json in
      let poolId = field_map_exn json "PoolId" Ipv4PoolEc2Id.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~cidr ~poolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeprovisionIpamPoolCidrResult =
  struct
    type nonrec t = {
      ipamPoolCidr: IpamPoolCidr.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolCidr = fun () -> { ipamPoolCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolCidr",
           (Option.map x.ipamPoolCidr ~f:IpamPoolCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolCidr =
        (Option.map ~f:IpamPoolCidr.of_xml)
          (Xml.child xml_arg0 "ipamPoolCidr") in
      make ?ipamPoolCidr ()
    let of_json json =
      let ipamPoolCidr = field_map json "IpamPoolCidr" IpamPoolCidr.of_json in
      make ?ipamPoolCidr ()
    let to_json v = composed_to_json to_value v
  end
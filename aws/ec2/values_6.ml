(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
open Values_0[@@warning "-33"]
open Values_1[@@warning "-33"]
open Values_2[@@warning "-33"]
open Values_3[@@warning "-33"]
open Values_4[@@warning "-33"]
open Values_5[@@warning "-33"]
module DescribeSecurityGroupRulesResult =
  struct
    type nonrec t =
      {
      securityGroupRules: SecurityGroupRuleList.t option
        [@ocaml.doc "Information about security group rules."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroupRules =
      fun ?nextToken -> fun () -> { securityGroupRules; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupRuleSet",
           (Option.map x.securityGroupRules ~f:SecurityGroupRuleList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let securityGroupRules =
        (Option.map ~f:SecurityGroupRuleList.of_xml)
          (Xml.child xml_arg0 "securityGroupRuleSet") in
      make ?nextToken ?securityGroupRules ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let securityGroupRules =
        field_map json__ "SecurityGroupRules" SecurityGroupRuleList.of_json in
      make ?nextToken ?securityGroupRules ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your security group rules."]
module DescribeSecurityGroupRulesRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. group-id - The ID of the security group. security-group-rule-id - The ID of the security group rule. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."];
      securityGroupRuleIds: SecurityGroupRuleIdList.t option
        [@ocaml.doc "The IDs of the security group rules."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeSecurityGroupRulesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. This value can be between 5 and 1000. If this parameter is not specified, then all items are returned. For more information, see Pagination."]}
    let make ?filters =
      fun ?securityGroupRuleIds ->
        fun ?dryRun ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                {
                  filters;
                  securityGroupRuleIds;
                  dryRun;
                  nextToken;
                  maxResults
                }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("SecurityGroupRuleId",
          (Option.map x.securityGroupRuleIds
             ~f:SecurityGroupRuleIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSecurityGroupRulesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeSecurityGroupRulesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let securityGroupRuleIds =
        (Option.map ~f:SecurityGroupRuleIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupRuleId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ?securityGroupRuleIds ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeSecurityGroupRulesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let securityGroupRuleIds =
        field_map json__ "SecurityGroupRuleIds"
          SecurityGroupRuleIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ?securityGroupRuleIds ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your security group rules."]
module DescribeSecurityGroupReferencesResult =
  struct
    type nonrec t =
      {
      securityGroupReferenceSet: SecurityGroupReferences.t option
        [@ocaml.doc
          "Information about the VPCs with the referencing security groups."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroupReferenceSet =
      fun () -> { securityGroupReferenceSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupReferenceSet",
           (Option.map x.securityGroupReferenceSet
              ~f:SecurityGroupReferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupReferenceSet =
        (Option.map ~f:SecurityGroupReferences.of_xml)
          (Xml.child xml_arg0 "securityGroupReferenceSet") in
      make ?securityGroupReferenceSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupReferenceSet =
        field_map json__ "SecurityGroupReferenceSet"
          SecurityGroupReferences.of_json in
      make ?securityGroupReferenceSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the VPCs on the other side of a VPC peering or Transit Gateway connection that are referencing the security groups you've specified in this request."]
module DescribeSecurityGroupReferencesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupId: GroupIds.t
        [@ocaml.doc "The IDs of the security groups in your account."]}
    let context_ = "DescribeSecurityGroupReferencesRequest"
    let make ?dryRun = fun ~groupId -> fun () -> { dryRun; groupId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("GroupId", (Some (GroupIds.to_value x.groupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        GroupIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "GroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~groupId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map_exn json__ "GroupId" GroupIds.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~groupId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the VPCs on the other side of a VPC peering or Transit Gateway connection that are referencing the security groups you've specified in this request."]
module DescribeSecondarySubnetsResult =
  struct
    type nonrec t =
      {
      secondarySubnets: SecondarySubnetList.t option
        [@ocaml.doc "Information about the secondary subnets."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondarySubnets =
      fun ?nextToken -> fun () -> { secondarySubnets; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondarySubnetSet",
           (Option.map x.secondarySubnets ~f:SecondarySubnetList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let secondarySubnets =
        (Option.map ~f:SecondarySubnetList.of_xml)
          (Xml.child xml_arg0 "secondarySubnetSet") in
      make ?nextToken ?secondarySubnets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let secondarySubnets =
        field_map json__ "SecondarySubnets" SecondarySubnetList.of_json in
      make ?nextToken ?secondarySubnets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your secondary subnets."]
module DescribeSecondarySubnetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. ipv4-cidr-block-association.association-id - The association ID for an IPv4 CIDR block associated with the secondary subnet. ipv4-cidr-block-association.cidr-block - An IPv4 CIDR block associated with the secondary subnet. ipv4-cidr-block-association.state - The state of an IPv4 CIDR block associated with the secondary subnet. owner-id - The ID of the Amazon Web Services account that owns the secondary subnet. secondary-network-id - The ID of the secondary network. secondary-network-type - The type of the secondary network (rdma). secondary-subnet-id - The ID of the secondary subnet. secondary-subnet-arn - The ARN of the secondary subnet. state - The state of the secondary subnet (create-in-progress | create-complete | create-failed | delete-in-progress | delete-complete | delete-failed). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      maxResults: DescribeSecondarySubnetsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      secondarySubnetIds: SecondarySubnetIdList.t option
        [@ocaml.doc "The IDs of the secondary subnets."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?secondarySubnetIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; secondarySubnetIds
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSecondarySubnetsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("SecondarySubnetId",
          (Option.map x.secondarySubnetIds ~f:SecondarySubnetIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondarySubnetIds =
        (Option.map ~f:SecondarySubnetIdList.of_xml)
          (Xml.child xml_arg0 "SecondarySubnetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeSecondarySubnetsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?secondarySubnetIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondarySubnetIds =
        field_map json__ "SecondarySubnetIds" SecondarySubnetIdList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeSecondarySubnetsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?secondarySubnetIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your secondary subnets."]
module DescribeSecondaryNetworksResult =
  struct
    type nonrec t =
      {
      secondaryNetworks: SecondaryNetworkList.t option
        [@ocaml.doc "Information about the secondary networks."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondaryNetworks =
      fun ?nextToken -> fun () -> { secondaryNetworks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondaryNetworkSet",
           (Option.map x.secondaryNetworks ~f:SecondaryNetworkList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let secondaryNetworks =
        (Option.map ~f:SecondaryNetworkList.of_xml)
          (Xml.child xml_arg0 "secondaryNetworkSet") in
      make ?nextToken ?secondaryNetworks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let secondaryNetworks =
        field_map json__ "SecondaryNetworks" SecondaryNetworkList.of_json in
      make ?nextToken ?secondaryNetworks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more secondary networks."]
module DescribeSecondaryNetworksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. The following are the possible values: ipv4-cidr-block-association.association-id - The association ID for an IPv4 CIDR block associated with the secondary network. ipv4-cidr-block-association.cidr-block - An IPv4 CIDR block associated with the secondary network. ipv4-cidr-block-association.state - The state of an IPv4 CIDR block associated with the secondary network. owner-id - The ID of the Amazon Web Services account that owns the secondary network. secondary-network-id - The ID of the secondary network. secondary-network-arn - The ARN of the secondary network. state - The state of the secondary network (create-in-progress | create-complete | create-failed | delete-in-progress | delete-complete | delete-failed). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. type - The type of the secondary network (rdma)."];
      maxResults: DescribeSecondaryNetworksMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      secondaryNetworkIds: SecondaryNetworkIdList.t option
        [@ocaml.doc "The IDs of the secondary networks."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?secondaryNetworkIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; secondaryNetworkIds
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSecondaryNetworksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("SecondaryNetworkId",
          (Option.map x.secondaryNetworkIds
             ~f:SecondaryNetworkIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondaryNetworkIds =
        (Option.map ~f:SecondaryNetworkIdList.of_xml)
          (Xml.child xml_arg0 "SecondaryNetworkId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeSecondaryNetworksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?secondaryNetworkIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondaryNetworkIds =
        field_map json__ "SecondaryNetworkIds" SecondaryNetworkIdList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeSecondaryNetworksMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?secondaryNetworkIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more secondary networks."]
module DescribeSecondaryInterfacesResult =
  struct
    type nonrec t =
      {
      secondaryInterfaces: SecondaryInterfaceList.t option
        [@ocaml.doc "Information about the secondary interfaces."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondaryInterfaces =
      fun ?nextToken -> fun () -> { secondaryInterfaces; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondaryInterfaceSet",
           (Option.map x.secondaryInterfaces
              ~f:SecondaryInterfaceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let secondaryInterfaces =
        (Option.map ~f:SecondaryInterfaceList.of_xml)
          (Xml.child xml_arg0 "secondaryInterfaceSet") in
      make ?nextToken ?secondaryInterfaces ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let secondaryInterfaces =
        field_map json__ "SecondaryInterfaces" SecondaryInterfaceList.of_json in
      make ?nextToken ?secondaryInterfaces ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your secondary interfaces."]
module DescribeSecondaryInterfacesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. attachment.attachment-id - The ID of the secondary interface attachment. attachment.instance-id - The ID of the instance to which the secondary interface is attached. attachment.instance-owner-id - The ID of the Amazon Web Services account that owns the instance to which the secondary interface is attached. attachment.status - The attachment status (attaching | attached | detaching | detached). private-ipv4-addresses.private-ip-address - The private IPv4 address associated with the secondary interface. owner-id - The ID of the Amazon Web Services account that owns the secondary interface. secondary-interface-arn - The ARN of the secondary interface. secondary-interface-id - The ID of the secondary interface. secondary-interface-type - The type of secondary interface (secondary). secondary-network-id - The ID of the secondary network. secondary-network-type - The type of the secondary network (rdma). secondary-subnet-id - The ID of the secondary subnet. status - The status of the secondary interface (available | in-use). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      maxResults: DescribeSecondaryInterfacesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      secondaryInterfaceIds: SecondaryInterfaceIdList.t option
        [@ocaml.doc "The IDs of the secondary interfaces."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?secondaryInterfaceIds ->
              fun () ->
                {
                  dryRun;
                  filters;
                  maxResults;
                  nextToken;
                  secondaryInterfaceIds
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeSecondaryInterfacesMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("SecondaryInterfaceId",
          (Option.map x.secondaryInterfaceIds
             ~f:SecondaryInterfaceIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondaryInterfaceIds =
        (Option.map ~f:SecondaryInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "SecondaryInterfaceId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeSecondaryInterfacesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?secondaryInterfaceIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondaryInterfaceIds =
        field_map json__ "SecondaryInterfaceIds"
          SecondaryInterfaceIdList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeSecondaryInterfacesMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?secondaryInterfaceIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your secondary interfaces."]
module DescribeScheduledInstancesResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token required to retrieve the next set of results. This value is null when there are no more results to return."];
      scheduledInstanceSet: ScheduledInstanceSet.t option
        [@ocaml.doc "Information about the Scheduled Instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?scheduledInstanceSet ->
        fun () -> { nextToken; scheduledInstanceSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("scheduledInstanceSet",
          (Option.map x.scheduledInstanceSet ~f:ScheduledInstanceSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledInstanceSet =
        (Option.map ~f:ScheduledInstanceSet.of_xml)
          (Xml.child xml_arg0 "scheduledInstanceSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?scheduledInstanceSet ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scheduledInstanceSet =
        field_map json__ "ScheduledInstanceSet" ScheduledInstanceSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?scheduledInstanceSet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DescribeScheduledInstances."]
module DescribeScheduledInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The Availability Zone (for example, us-west-2a). instance-type - The instance type (for example, c4.large). platform - The platform (Linux/UNIX or Windows)."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. This value can be between 5 and 300. The default value is 100. To retrieve the remaining results, make another call with the returned NextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next set of results."];
      scheduledInstanceIds: ScheduledInstanceIdRequestSet.t option
        [@ocaml.doc "The Scheduled Instance IDs."];
      slotStartTimeRange: SlotStartTimeRangeRequest.t option
        [@ocaml.doc "The time period for the first schedule to start."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?scheduledInstanceIds ->
              fun ?slotStartTimeRange ->
                fun () ->
                  {
                    dryRun;
                    filters;
                    maxResults;
                    nextToken;
                    scheduledInstanceIds;
                    slotStartTimeRange
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("ScheduledInstanceId",
          (Option.map x.scheduledInstanceIds
             ~f:ScheduledInstanceIdRequestSet.to_value));
        ("SlotStartTimeRange",
          (Option.map x.slotStartTimeRange
             ~f:SlotStartTimeRangeRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let slotStartTimeRange =
        (Option.map ~f:SlotStartTimeRangeRequest.of_xml)
          (Xml.child xml_arg0 "SlotStartTimeRange") in
      let scheduledInstanceIds =
        (Option.map ~f:ScheduledInstanceIdRequestSet.of_xml)
          (Xml.child xml_arg0 "ScheduledInstanceId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?slotStartTimeRange ?scheduledInstanceIds ?nextToken ?maxResults
        ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let slotStartTimeRange =
        field_map json__ "SlotStartTimeRange"
          SlotStartTimeRangeRequest.of_json in
      let scheduledInstanceIds =
        field_map json__ "ScheduledInstanceIds"
          ScheduledInstanceIdRequestSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?slotStartTimeRange ?scheduledInstanceIds ?nextToken ?maxResults
        ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeScheduledInstances."]
module DescribeScheduledInstanceAvailabilityResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token required to retrieve the next set of results. This value is null when there are no more results to return."];
      scheduledInstanceAvailabilitySet:
        ScheduledInstanceAvailabilitySet.t option
        [@ocaml.doc "Information about the available Scheduled Instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?scheduledInstanceAvailabilitySet ->
        fun () -> { nextToken; scheduledInstanceAvailabilitySet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("scheduledInstanceAvailabilitySet",
          (Option.map x.scheduledInstanceAvailabilitySet
             ~f:ScheduledInstanceAvailabilitySet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledInstanceAvailabilitySet =
        (Option.map ~f:ScheduledInstanceAvailabilitySet.of_xml)
          (Xml.child xml_arg0 "scheduledInstanceAvailabilitySet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?scheduledInstanceAvailabilitySet ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scheduledInstanceAvailabilitySet =
        field_map json__ "ScheduledInstanceAvailabilitySet"
          ScheduledInstanceAvailabilitySet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?scheduledInstanceAvailabilitySet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of DescribeScheduledInstanceAvailability."]
module DescribeScheduledInstanceAvailabilityRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The Availability Zone (for example, us-west-2a). instance-type - The instance type (for example, c4.large). platform - The platform (Linux/UNIX or Windows)."];
      firstSlotStartTimeRange: SlotDateTimeRangeRequest.t
        [@ocaml.doc "The time period for the first schedule to start."];
      maxResults: DescribeScheduledInstanceAvailabilityMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. This value can be between 5 and 300. The default value is 300. To retrieve the remaining results, make another call with the returned NextToken value."];
      maxSlotDurationInHours: Integer.t option
        [@ocaml.doc
          "The maximum available duration, in hours. This value must be greater than MinSlotDurationInHours and less than 1,720."];
      minSlotDurationInHours: Integer.t option
        [@ocaml.doc
          "The minimum available duration, in hours. The minimum required duration is 1,200 hours per year. For example, the minimum daily schedule is 4 hours, the minimum weekly schedule is 24 hours, and the minimum monthly schedule is 100 hours."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next set of results."];
      recurrence: ScheduledInstanceRecurrenceRequest.t
        [@ocaml.doc "The schedule recurrence."]}
    let context_ = "DescribeScheduledInstanceAvailabilityRequest"
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?maxSlotDurationInHours ->
            fun ?minSlotDurationInHours ->
              fun ?nextToken ->
                fun ~firstSlotStartTimeRange ->
                  fun ~recurrence ->
                    fun () ->
                      {
                        dryRun;
                        filters;
                        maxResults;
                        maxSlotDurationInHours;
                        minSlotDurationInHours;
                        nextToken;
                        firstSlotStartTimeRange;
                        recurrence
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("FirstSlotStartTimeRange",
          (Some (SlotDateTimeRangeRequest.to_value x.firstSlotStartTimeRange)));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeScheduledInstanceAvailabilityMaxResults.to_value));
        ("MaxSlotDurationInHours",
          (Option.map x.maxSlotDurationInHours ~f:Integer.to_value));
        ("MinSlotDurationInHours",
          (Option.map x.minSlotDurationInHours ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Recurrence",
          (Some (ScheduledInstanceRecurrenceRequest.to_value x.recurrence)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recurrence =
        ScheduledInstanceRecurrenceRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Recurrence") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let minSlotDurationInHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MinSlotDurationInHours") in
      let maxSlotDurationInHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxSlotDurationInHours") in
      let maxResults =
        (Option.map ~f:DescribeScheduledInstanceAvailabilityMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let firstSlotStartTimeRange =
        SlotDateTimeRangeRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FirstSlotStartTimeRange") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~recurrence ?nextToken ?minSlotDurationInHours
        ?maxSlotDurationInHours ?maxResults ~firstSlotStartTimeRange ?filters
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let recurrence =
        field_map_exn json__ "Recurrence"
          ScheduledInstanceRecurrenceRequest.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let minSlotDurationInHours =
        field_map json__ "MinSlotDurationInHours" Integer.of_json in
      let maxSlotDurationInHours =
        field_map json__ "MaxSlotDurationInHours" Integer.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeScheduledInstanceAvailabilityMaxResults.of_json in
      let firstSlotStartTimeRange =
        field_map_exn json__ "FirstSlotStartTimeRange"
          SlotDateTimeRangeRequest.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~recurrence ?nextToken ?minSlotDurationInHours
        ?maxSlotDurationInHours ?maxResults ~firstSlotStartTimeRange ?filters
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeScheduledInstanceAvailability."]
module DescribeRouteTablesResult =
  struct
    type nonrec t =
      {
      routeTables: RouteTableList.t option
        [@ocaml.doc "Information about the route tables."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeTables =
      fun ?nextToken -> fun () -> { routeTables; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeTableSet",
           (Option.map x.routeTables ~f:RouteTableList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routeTables =
        (Option.map ~f:RouteTableList.of_xml)
          (Xml.child xml_arg0 "routeTableSet") in
      make ?nextToken ?routeTables ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeTables = field_map json__ "RouteTables" RouteTableList.of_json in
      make ?nextToken ?routeTables ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DescribeRouteTables."]
module DescribeRouteTablesRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeRouteTablesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      routeTableIds: RouteTableIdStringList.t option
        [@ocaml.doc "The IDs of the route tables."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. association.gateway-id - The ID of the gateway involved in the association. association.route-table-association-id - The ID of an association ID for the route table. association.route-table-id - The ID of the route table involved in the association. association.subnet-id - The ID of the subnet involved in the association. association.main - Indicates whether the route table is the main route table for the VPC (true | false). Route tables that do not have an association ID are not returned in the response. owner-id - The ID of the Amazon Web Services account that owns the route table. route-table-id - The ID of the route table. route.destination-cidr-block - The IPv4 CIDR range specified in a route in the table. route.destination-ipv6-cidr-block - The IPv6 CIDR range specified in a route in the route table. route.destination-prefix-list-id - The ID (prefix) of the Amazon Web Services service specified in a route in the table. route.egress-only-internet-gateway-id - The ID of an egress-only Internet gateway specified in a route in the route table. route.gateway-id - The ID of a gateway specified in a route in the table. route.instance-id - The ID of an instance specified in a route in the table. route.nat-gateway-id - The ID of a NAT gateway. route.transit-gateway-id - The ID of a transit gateway. route.origin - Describes how the route was created. CreateRouteTable indicates that the route was automatically created when the route table was created; CreateRoute indicates that the route was manually added to the route table; EnableVgwRoutePropagation indicates that the route was propagated by route propagation. route.state - The state of a route in the route table (active | blackhole). The blackhole state indicates that the route's target isn't available (for example, the specified gateway isn't attached to the VPC, the specified NAT instance has been terminated, and so on). route.vpc-peering-connection-id - The ID of a VPC peering connection specified in a route in the table. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. vpc-id - The ID of the VPC for the route table."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?dryRun ->
          fun ?routeTableIds ->
            fun ?filters ->
              fun () ->
                { nextToken; maxResults; dryRun; routeTableIds; filters }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeRouteTablesMaxResults.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("RouteTableId",
          (Option.map x.routeTableIds ~f:RouteTableIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let routeTableIds =
        (Option.map ~f:RouteTableIdStringList.of_xml)
          (Xml.child xml_arg0 "RouteTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let maxResults =
        (Option.map ~f:DescribeRouteTablesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?filters ?routeTableIds ?dryRun ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let routeTableIds =
        field_map json__ "RouteTableIds" RouteTableIdStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" DescribeRouteTablesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?filters ?routeTableIds ?dryRun ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your route tables. The default is to describe all your route tables. Alternatively, you can specify specific route table IDs or filter the results to include only the route tables that match specific criteria. Each subnet in your VPC must be associated with a route table. If a subnet is not explicitly associated with any route table, it is implicitly associated with the main route table. This command does not return the subnet ID for implicit associations. For more information, see Route tables in the Amazon VPC User Guide."]
module DescribeRouteServersResult =
  struct
    type nonrec t =
      {
      routeServers: RouteServersList.t option
        [@ocaml.doc "Information about the described route servers."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServers =
      fun ?nextToken -> fun () -> { routeServers; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerSet",
           (Option.map x.routeServers ~f:RouteServersList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routeServers =
        (Option.map ~f:RouteServersList.of_xml)
          (Xml.child xml_arg0 "routeServerSet") in
      make ?nextToken ?routeServers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeServers =
        field_map json__ "RouteServers" RouteServersList.of_json in
      make ?nextToken ?routeServers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more route servers. Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability with third-party workloads. Route server supports the follow route table types: VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route server does not support route tables associated with virtual private gateways. To propagate routes into a transit gateway route table, use Transit Gateway Connect. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DescribeRouteServersRequest =
  struct
    type nonrec t =
      {
      routeServerIds: RouteServerIdsList.t option
        [@ocaml.doc "The IDs of the route servers to describe."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: RouteServerMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call."];
      filters: FilterList.t option
        [@ocaml.doc "One or more filters to apply to the describe request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?routeServerIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                { routeServerIds; nextToken; maxResults; filters; dryRun }
    let to_value x =
      structure_to_value
        [("RouteServerId",
           (Option.map x.routeServerIds ~f:RouteServerIdsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:RouteServerMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:RouteServerMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let routeServerIds =
        (Option.map ~f:RouteServerIdsList.of_xml)
          (Xml.child xml_arg0 "RouteServerId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?routeServerIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults" RouteServerMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeServerIds =
        field_map json__ "RouteServerIds" RouteServerIdsList.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?routeServerIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more route servers. Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability with third-party workloads. Route server supports the follow route table types: VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route server does not support route tables associated with virtual private gateways. To propagate routes into a transit gateway route table, use Transit Gateway Connect. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DescribeRouteServerPeersResult =
  struct
    type nonrec t =
      {
      routeServerPeers: RouteServerPeersList.t option
        [@ocaml.doc "Information about the described route server peers."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerPeers =
      fun ?nextToken -> fun () -> { routeServerPeers; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerPeerSet",
           (Option.map x.routeServerPeers ~f:RouteServerPeersList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routeServerPeers =
        (Option.map ~f:RouteServerPeersList.of_xml)
          (Xml.child xml_arg0 "routeServerPeerSet") in
      make ?nextToken ?routeServerPeers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeServerPeers =
        field_map json__ "RouteServerPeers" RouteServerPeersList.of_json in
      make ?nextToken ?routeServerPeers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more route server peers. A route server peer is a session between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other network security function running on an EC2 instance). The device must meet these requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP sessions For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DescribeRouteServerPeersRequest =
  struct
    type nonrec t =
      {
      routeServerPeerIds: RouteServerPeerIdsList.t option
        [@ocaml.doc "The IDs of the route server peers to describe."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: RouteServerMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call."];
      filters: FilterList.t option
        [@ocaml.doc "One or more filters to apply to the describe request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?routeServerPeerIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                { routeServerPeerIds; nextToken; maxResults; filters; dryRun
                }
    let to_value x =
      structure_to_value
        [("RouteServerPeerId",
           (Option.map x.routeServerPeerIds
              ~f:RouteServerPeerIdsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:RouteServerMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:RouteServerMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let routeServerPeerIds =
        (Option.map ~f:RouteServerPeerIdsList.of_xml)
          (Xml.child xml_arg0 "RouteServerPeerId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?routeServerPeerIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults" RouteServerMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeServerPeerIds =
        field_map json__ "RouteServerPeerIds" RouteServerPeerIdsList.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?routeServerPeerIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more route server peers. A route server peer is a session between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other network security function running on an EC2 instance). The device must meet these requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP sessions For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DescribeRouteServerEndpointsResult =
  struct
    type nonrec t =
      {
      routeServerEndpoints: RouteServerEndpointsList.t option
        [@ocaml.doc
          "Information about the described route server endpoints."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerEndpoints =
      fun ?nextToken -> fun () -> { routeServerEndpoints; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerEndpointSet",
           (Option.map x.routeServerEndpoints
              ~f:RouteServerEndpointsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routeServerEndpoints =
        (Option.map ~f:RouteServerEndpointsList.of_xml)
          (Xml.child xml_arg0 "routeServerEndpointSet") in
      make ?nextToken ?routeServerEndpoints ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeServerEndpoints =
        field_map json__ "RouteServerEndpoints"
          RouteServerEndpointsList.of_json in
      make ?nextToken ?routeServerEndpoints ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more route server endpoints. A route server endpoint is an Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol) connections between your route server and your BGP peers. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DescribeRouteServerEndpointsRequest =
  struct
    type nonrec t =
      {
      routeServerEndpointIds: RouteServerEndpointIdsList.t option
        [@ocaml.doc "The IDs of the route server endpoints to describe."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: RouteServerMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call."];
      filters: FilterList.t option
        [@ocaml.doc "One or more filters to apply to the describe request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?routeServerEndpointIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  routeServerEndpointIds;
                  nextToken;
                  maxResults;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("RouteServerEndpointId",
           (Option.map x.routeServerEndpointIds
              ~f:RouteServerEndpointIdsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:RouteServerMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:RouteServerMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let routeServerEndpointIds =
        (Option.map ~f:RouteServerEndpointIdsList.of_xml)
          (Xml.child xml_arg0 "RouteServerEndpointId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?routeServerEndpointIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults" RouteServerMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let routeServerEndpointIds =
        field_map json__ "RouteServerEndpointIds"
          RouteServerEndpointIdsList.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?routeServerEndpointIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more route server endpoints. A route server endpoint is an Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol) connections between your route server and your BGP peers. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DescribeReservedInstancesResult =
  struct
    type nonrec t =
      {
      reservedInstances: ReservedInstancesList.t option
        [@ocaml.doc "A list of Reserved Instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstances = fun () -> { reservedInstances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesSet",
           (Option.map x.reservedInstances ~f:ReservedInstancesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstances =
        (Option.map ~f:ReservedInstancesList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesSet") in
      make ?reservedInstances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstances =
        field_map json__ "ReservedInstances" ReservedInstancesList.of_json in
      make ?reservedInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output for DescribeReservedInstances."]
module DescribeReservedInstancesRequest =
  struct
    type nonrec t =
      {
      offeringClass: OfferingClassType.t option
        [@ocaml.doc
          "Describes whether the Reserved Instance is Standard or Convertible."];
      reservedInstancesIds: ReservedInstancesIdStringList.t option
        [@ocaml.doc
          "One or more Reserved Instance IDs. Default: Describes all your Reserved Instances, or only those otherwise specified."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. availability-zone - The Availability Zone where the Reserved Instance can be used. availability-zone-id - The ID of the Availability Zone where the Reserved Instance can be used. duration - The duration of the Reserved Instance (one year or three years), in seconds (31536000 | 94608000). end - The time when the Reserved Instance expires (for example, 2015-08-07T11:54:42.000Z). fixed-price - The purchase price of the Reserved Instance (for example, 9800.0). instance-type - The instance type that is covered by the reservation. scope - The scope of the Reserved Instance (Region or Availability Zone). product-description - The Reserved Instance product platform description (Linux/UNIX | Linux with SQL Server Standard | Linux with SQL Server Web | Linux with SQL Server Enterprise | SUSE Linux | Red Hat Enterprise Linux | Red Hat Enterprise Linux with HA | Windows | Windows with SQL Server Standard | Windows with SQL Server Web | Windows with SQL Server Enterprise). reserved-instances-id - The ID of the Reserved Instance. start - The time at which the Reserved Instance purchase request was placed (for example, 2014-08-07T11:54:42.000Z). state - The state of the Reserved Instance (payment-pending | active | payment-failed | retired). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. usage-price - The usage price of the Reserved Instance, per hour (for example, 0.84)."];
      offeringType: OfferingTypeValues.t option
        [@ocaml.doc
          "The Reserved Instance offering type. If you are using tools that predate the 2011-11-01 API version, you only have access to the Medium Utilization Reserved Instance offering type."]}
    let make ?offeringClass =
      fun ?reservedInstancesIds ->
        fun ?dryRun ->
          fun ?filters ->
            fun ?offeringType ->
              fun () ->
                {
                  offeringClass;
                  reservedInstancesIds;
                  dryRun;
                  filters;
                  offeringType
                }
    let to_value x =
      structure_to_value
        [("OfferingClass",
           (Option.map x.offeringClass ~f:OfferingClassType.to_value));
        ("ReservedInstancesId",
          (Option.map x.reservedInstancesIds
             ~f:ReservedInstancesIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("offeringType",
          (Option.map x.offeringType ~f:OfferingTypeValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringType =
        (Option.map ~f:OfferingTypeValues.of_xml)
          (Xml.child xml_arg0 "offeringType") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let reservedInstancesIds =
        (Option.map ~f:ReservedInstancesIdStringList.of_xml)
          (Xml.child xml_arg0 "ReservedInstancesId") in
      let offeringClass =
        (Option.map ~f:OfferingClassType.of_xml)
          (Xml.child xml_arg0 "OfferingClass") in
      make ?offeringType ?filters ?dryRun ?reservedInstancesIds
        ?offeringClass ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let offeringType =
        field_map json__ "OfferingType" OfferingTypeValues.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let reservedInstancesIds =
        field_map json__ "ReservedInstancesIds"
          ReservedInstancesIdStringList.of_json in
      let offeringClass =
        field_map json__ "OfferingClass" OfferingClassType.of_json in
      make ?offeringType ?filters ?dryRun ?reservedInstancesIds
        ?offeringClass ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeReservedInstances."]
module DescribeReservedInstancesOfferingsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      reservedInstancesOfferings: ReservedInstancesOfferingList.t option
        [@ocaml.doc "A list of Reserved Instances offerings."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?reservedInstancesOfferings ->
        fun () -> { nextToken; reservedInstancesOfferings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reservedInstancesOfferingsSet",
          (Option.map x.reservedInstancesOfferings
             ~f:ReservedInstancesOfferingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesOfferings =
        (Option.map ~f:ReservedInstancesOfferingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesOfferingsSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reservedInstancesOfferings ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesOfferings =
        field_map json__ "ReservedInstancesOfferings"
          ReservedInstancesOfferingList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?reservedInstancesOfferings ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of DescribeReservedInstancesOfferings."]
module DescribeReservedInstancesOfferingsRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t option
        [@ocaml.doc
          "The Availability Zone in which the Reserved Instance can be used. Either AvailabilityZone or AvailabilityZoneId can be specified, but not both."];
      includeMarketplace: Boolean.t option
        [@ocaml.doc
          "Include Reserved Instance Marketplace offerings in the response."];
      instanceType: InstanceType.t option
        [@ocaml.doc
          "The instance type that the reservation will cover (for example, m1.small). For more information, see Amazon EC2 instance types in the Amazon EC2 User Guide."];
      maxDuration: Long.t option
        [@ocaml.doc
          "The maximum duration (in seconds) to filter when searching for offerings. Default: 94608000 (3 years)"];
      maxInstanceCount: Integer.t option
        [@ocaml.doc
          "The maximum number of instances to filter when searching for offerings. Default: 20"];
      minDuration: Long.t option
        [@ocaml.doc
          "The minimum duration (in seconds) to filter when searching for offerings. Default: 2592000 (1 month)"];
      offeringClass: OfferingClassType.t option
        [@ocaml.doc
          "The offering class of the Reserved Instance. Can be standard or convertible."];
      productDescription: RIProductDescription.t option
        [@ocaml.doc
          "The Reserved Instance product platform description. Instances that include (Amazon VPC) in the description are for use with Amazon VPC."];
      reservedInstancesOfferingIds:
        ReservedInstancesOfferingIdStringList.t option
        [@ocaml.doc "One or more Reserved Instances offering IDs."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "The ID of the Availability Zone. Either AvailabilityZone or AvailabilityZoneId can be specified, but not both."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. availability-zone - The Availability Zone where the Reserved Instance can be used. availability-zone-id - The ID of the Availability Zone where the Reserved Instance can be used. duration - The duration of the Reserved Instance (for example, one year or three years), in seconds (31536000 | 94608000). fixed-price - The purchase price of the Reserved Instance (for example, 9800.0). instance-type - The instance type that is covered by the reservation. marketplace - Set to true to show only Reserved Instance Marketplace offerings. When this filter is not used, which is the default behavior, all offerings from both Amazon Web Services and the Reserved Instance Marketplace are listed. product-description - The Reserved Instance product platform description (Linux/UNIX | Linux with SQL Server Standard | Linux with SQL Server Web | Linux with SQL Server Enterprise | SUSE Linux | Red Hat Enterprise Linux | Red Hat Enterprise Linux with HA | Windows | Windows with SQL Server Standard | Windows with SQL Server Web | Windows with SQL Server Enterprise). reserved-instances-offering-id - The Reserved Instances offering ID. scope - The scope of the Reserved Instance (Availability Zone or Region). usage-price - The usage price of the Reserved Instance, per hour (for example, 0.84)."];
      instanceTenancy: Tenancy.t option
        [@ocaml.doc
          "The tenancy of the instances covered by the reservation. A Reserved Instance with a tenancy of dedicated is applied to instances that run in a VPC on single-tenant hardware (i.e., Dedicated Instances). Important: The host value cannot be used with this parameter. Use the default or dedicated values only. Default: default"];
      offeringType: OfferingTypeValues.t option
        [@ocaml.doc
          "The Reserved Instance offering type. If you are using tools that predate the 2011-11-01 API version, you only have access to the Medium Utilization Reserved Instance offering type."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results of the initial request can be seen by sending another request with the returned NextToken value. The maximum is 100. Default: 100"]}
    let make ?availabilityZone =
      fun ?includeMarketplace ->
        fun ?instanceType ->
          fun ?maxDuration ->
            fun ?maxInstanceCount ->
              fun ?minDuration ->
                fun ?offeringClass ->
                  fun ?productDescription ->
                    fun ?reservedInstancesOfferingIds ->
                      fun ?availabilityZoneId ->
                        fun ?dryRun ->
                          fun ?filters ->
                            fun ?instanceTenancy ->
                              fun ?offeringType ->
                                fun ?nextToken ->
                                  fun ?maxResults ->
                                    fun () ->
                                      {
                                        availabilityZone;
                                        includeMarketplace;
                                        instanceType;
                                        maxDuration;
                                        maxInstanceCount;
                                        minDuration;
                                        offeringClass;
                                        productDescription;
                                        reservedInstancesOfferingIds;
                                        availabilityZoneId;
                                        dryRun;
                                        filters;
                                        instanceTenancy;
                                        offeringType;
                                        nextToken;
                                        maxResults
                                      }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:String_.to_value));
        ("IncludeMarketplace",
          (Option.map x.includeMarketplace ~f:Boolean.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:InstanceType.to_value));
        ("MaxDuration", (Option.map x.maxDuration ~f:Long.to_value));
        ("MaxInstanceCount",
          (Option.map x.maxInstanceCount ~f:Integer.to_value));
        ("MinDuration", (Option.map x.minDuration ~f:Long.to_value));
        ("OfferingClass",
          (Option.map x.offeringClass ~f:OfferingClassType.to_value));
        ("ProductDescription",
          (Option.map x.productDescription ~f:RIProductDescription.to_value));
        ("ReservedInstancesOfferingId",
          (Option.map x.reservedInstancesOfferingIds
             ~f:ReservedInstancesOfferingIdStringList.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("instanceTenancy",
          (Option.map x.instanceTenancy ~f:Tenancy.to_value));
        ("offeringType",
          (Option.map x.offeringType ~f:OfferingTypeValues.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let offeringType =
        (Option.map ~f:OfferingTypeValues.of_xml)
          (Xml.child xml_arg0 "offeringType") in
      let instanceTenancy =
        (Option.map ~f:Tenancy.of_xml) (Xml.child xml_arg0 "instanceTenancy") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let reservedInstancesOfferingIds =
        (Option.map ~f:ReservedInstancesOfferingIdStringList.of_xml)
          (Xml.child xml_arg0 "ReservedInstancesOfferingId") in
      let productDescription =
        (Option.map ~f:RIProductDescription.of_xml)
          (Xml.child xml_arg0 "ProductDescription") in
      let offeringClass =
        (Option.map ~f:OfferingClassType.of_xml)
          (Xml.child xml_arg0 "OfferingClass") in
      let minDuration =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MinDuration") in
      let maxInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxInstanceCount") in
      let maxDuration =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MaxDuration") in
      let instanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let includeMarketplace =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeMarketplace") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?maxResults ?nextToken ?offeringType ?instanceTenancy ?filters
        ?dryRun ?availabilityZoneId ?reservedInstancesOfferingIds
        ?productDescription ?offeringClass ?minDuration ?maxInstanceCount
        ?maxDuration ?instanceType ?includeMarketplace ?availabilityZone ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let offeringType =
        field_map json__ "OfferingType" OfferingTypeValues.of_json in
      let instanceTenancy =
        field_map json__ "InstanceTenancy" Tenancy.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let reservedInstancesOfferingIds =
        field_map json__ "ReservedInstancesOfferingIds"
          ReservedInstancesOfferingIdStringList.of_json in
      let productDescription =
        field_map json__ "ProductDescription" RIProductDescription.of_json in
      let offeringClass =
        field_map json__ "OfferingClass" OfferingClassType.of_json in
      let minDuration = field_map json__ "MinDuration" Long.of_json in
      let maxInstanceCount =
        field_map json__ "MaxInstanceCount" Integer.of_json in
      let maxDuration = field_map json__ "MaxDuration" Long.of_json in
      let instanceType = field_map json__ "InstanceType" InstanceType.of_json in
      let includeMarketplace =
        field_map json__ "IncludeMarketplace" Boolean.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" String_.of_json in
      make ?maxResults ?nextToken ?offeringType ?instanceTenancy ?filters
        ?dryRun ?availabilityZoneId ?reservedInstancesOfferingIds
        ?productDescription ?offeringClass ?minDuration ?maxInstanceCount
        ?maxDuration ?instanceType ?includeMarketplace ?availabilityZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeReservedInstancesOfferings."]
module DescribeReservedInstancesModificationsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      reservedInstancesModifications:
        ReservedInstancesModificationList.t option
        [@ocaml.doc "The Reserved Instance modification information."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?reservedInstancesModifications ->
        fun () -> { nextToken; reservedInstancesModifications }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reservedInstancesModificationsSet",
          (Option.map x.reservedInstancesModifications
             ~f:ReservedInstancesModificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesModifications =
        (Option.map ~f:ReservedInstancesModificationList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesModificationsSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reservedInstancesModifications ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesModifications =
        field_map json__ "ReservedInstancesModifications"
          ReservedInstancesModificationList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?reservedInstancesModifications ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of DescribeReservedInstancesModifications."]
module DescribeReservedInstancesModificationsRequest =
  struct
    type nonrec t =
      {
      reservedInstancesModificationIds:
        ReservedInstancesModificationIdStringList.t option
        [@ocaml.doc "IDs for the submitted modification request."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. client-token - The idempotency token for the modification request. create-date - The time when the modification request was created. effective-date - The time when the modification becomes effective. modification-result.reserved-instances-id - The ID for the Reserved Instances created as part of the modification request. This ID is only available when the status of the modification is fulfilled. modification-result.target-configuration.availability-zone - The Availability Zone for the new Reserved Instances. modification-result.target-configuration.availability-zone-id - The ID of the Availability Zone for the new Reserved Instances. modification-result.target-configuration.instance-count - The number of new Reserved Instances. modification-result.target-configuration.instance-type - The instance type of the new Reserved Instances. reserved-instances-id - The ID of the Reserved Instances modified. reserved-instances-modification-id - The ID of the modification request. status - The status of the Reserved Instances modification request (processing | fulfilled | failed). status-message - The reason for the status. update-date - The time when the modification request was last updated."]}
    let make ?reservedInstancesModificationIds =
      fun ?nextToken ->
        fun ?filters ->
          fun () -> { reservedInstancesModificationIds; nextToken; filters }
    let to_value x =
      structure_to_value
        [("ReservedInstancesModificationId",
           (Option.map x.reservedInstancesModificationIds
              ~f:ReservedInstancesModificationIdStringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reservedInstancesModificationIds =
        (Option.map ~f:ReservedInstancesModificationIdStringList.of_xml)
          (Xml.child xml_arg0 "ReservedInstancesModificationId") in
      make ?filters ?nextToken ?reservedInstancesModificationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let reservedInstancesModificationIds =
        field_map json__ "ReservedInstancesModificationIds"
          ReservedInstancesModificationIdStringList.of_json in
      make ?filters ?nextToken ?reservedInstancesModificationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeReservedInstancesModifications."]
module DescribeReservedInstancesListingsResult =
  struct
    type nonrec t =
      {
      reservedInstancesListings: ReservedInstancesListingList.t option
        [@ocaml.doc "Information about the Reserved Instance listing."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesListings =
      fun () -> { reservedInstancesListings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesListingsSet",
           (Option.map x.reservedInstancesListings
              ~f:ReservedInstancesListingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListings =
        (Option.map ~f:ReservedInstancesListingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingsSet") in
      make ?reservedInstancesListings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesListings =
        field_map json__ "ReservedInstancesListings"
          ReservedInstancesListingList.of_json in
      make ?reservedInstancesListings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of DescribeReservedInstancesListings."]
module DescribeReservedInstancesListingsRequest =
  struct
    type nonrec t =
      {
      reservedInstancesId: ReservationId.t option
        [@ocaml.doc "One or more Reserved Instance IDs."];
      reservedInstancesListingId: ReservedInstancesListingId.t option
        [@ocaml.doc "One or more Reserved Instance listing IDs."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. reserved-instances-id - The ID of the Reserved Instances. reserved-instances-listing-id - The ID of the Reserved Instances listing. status - The status of the Reserved Instance listing (pending | active | cancelled | closed). status-message - The reason for the status."]}
    let make ?reservedInstancesId =
      fun ?reservedInstancesListingId ->
        fun ?filters ->
          fun () ->
            { reservedInstancesId; reservedInstancesListingId; filters }
    let to_value x =
      structure_to_value
        [("reservedInstancesId",
           (Option.map x.reservedInstancesId ~f:ReservationId.to_value));
        ("reservedInstancesListingId",
          (Option.map x.reservedInstancesListingId
             ~f:ReservedInstancesListingId.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let reservedInstancesListingId =
        (Option.map ~f:ReservedInstancesListingId.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingId") in
      let reservedInstancesId =
        (Option.map ~f:ReservationId.of_xml)
          (Xml.child xml_arg0 "reservedInstancesId") in
      make ?filters ?reservedInstancesListingId ?reservedInstancesId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let reservedInstancesListingId =
        field_map json__ "ReservedInstancesListingId"
          ReservedInstancesListingId.of_json in
      let reservedInstancesId =
        field_map json__ "ReservedInstancesId" ReservationId.of_json in
      make ?filters ?reservedInstancesListingId ?reservedInstancesId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeReservedInstancesListings."]
module DescribeReplaceRootVolumeTasksResult =
  struct
    type nonrec t =
      {
      replaceRootVolumeTasks: ReplaceRootVolumeTasks.t option
        [@ocaml.doc "Information about the root volume replacement task."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?replaceRootVolumeTasks =
      fun ?nextToken -> fun () -> { replaceRootVolumeTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("replaceRootVolumeTaskSet",
           (Option.map x.replaceRootVolumeTasks
              ~f:ReplaceRootVolumeTasks.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let replaceRootVolumeTasks =
        (Option.map ~f:ReplaceRootVolumeTasks.of_xml)
          (Xml.child xml_arg0 "replaceRootVolumeTaskSet") in
      make ?nextToken ?replaceRootVolumeTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let replaceRootVolumeTasks =
        field_map json__ "ReplaceRootVolumeTasks"
          ReplaceRootVolumeTasks.of_json in
      make ?nextToken ?replaceRootVolumeTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a root volume replacement task. For more information, see Replace a root volume in the Amazon EC2 User Guide."]
module DescribeReplaceRootVolumeTasksRequest =
  struct
    type nonrec t =
      {
      replaceRootVolumeTaskIds: ReplaceRootVolumeTaskIds.t option
        [@ocaml.doc "The ID of the root volume replacement task to view."];
      filters: FilterList.t option
        [@ocaml.doc
          "Filter to use: instance-id - The ID of the instance for which the root volume replacement task was created."];
      maxResults: DescribeReplaceRootVolumeTasksMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?replaceRootVolumeTaskIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  replaceRootVolumeTaskIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("ReplaceRootVolumeTaskId",
           (Option.map x.replaceRootVolumeTaskIds
              ~f:ReplaceRootVolumeTaskIds.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeReplaceRootVolumeTasksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeReplaceRootVolumeTasksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let replaceRootVolumeTaskIds =
        (Option.map ~f:ReplaceRootVolumeTaskIds.of_xml)
          (Xml.child xml_arg0 "ReplaceRootVolumeTaskId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?replaceRootVolumeTaskIds
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeReplaceRootVolumeTasksMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let replaceRootVolumeTaskIds =
        field_map json__ "ReplaceRootVolumeTaskIds"
          ReplaceRootVolumeTaskIds.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?replaceRootVolumeTaskIds
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a root volume replacement task. For more information, see Replace a root volume in the Amazon EC2 User Guide."]
module DescribeRegionsResult =
  struct
    type nonrec t =
      {
      regions: RegionList.t option
        [@ocaml.doc "Information about the Regions."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?regions = fun () -> { regions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("regionInfo", (Option.map x.regions ~f:RegionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regions =
        (Option.map ~f:RegionList.of_xml) (Xml.child xml_arg0 "regionInfo") in
      make ?regions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regions = field_map json__ "Regions" RegionList.of_json in
      make ?regions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Regions that are enabled for your account, or all Regions. For a list of the Regions supported by Amazon EC2, see Amazon EC2 service endpoints. For information about enabling and disabling Regions for your account, see Specify which Amazon Web Services Regions your account can use in the Amazon Web Services Account Management Reference Guide. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeRegionsRequest =
  struct
    type nonrec t =
      {
      regionNames: RegionNameStringList.t option
        [@ocaml.doc
          "The names of the Regions. You can specify any Regions, whether they are enabled and disabled for your account."];
      allRegions: Boolean.t option
        [@ocaml.doc
          "Indicates whether to display all Regions, including Regions that are disabled for your account."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. endpoint - The endpoint of the Region (for example, ec2.us-east-1.amazonaws.com). opt-in-status - The opt-in status of the Region (opt-in-not-required | opted-in | not-opted-in). region-name - The name of the Region (for example, us-east-1)."]}
    let make ?regionNames =
      fun ?allRegions ->
        fun ?dryRun ->
          fun ?filters ->
            fun () -> { regionNames; allRegions; dryRun; filters }
    let to_value x =
      structure_to_value
        [("RegionName",
           (Option.map x.regionNames ~f:RegionNameStringList.to_value));
        ("AllRegions", (Option.map x.allRegions ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let allRegions =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AllRegions") in
      let regionNames =
        (Option.map ~f:RegionNameStringList.of_xml)
          (Xml.child xml_arg0 "RegionName") in
      make ?filters ?dryRun ?allRegions ?regionNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let allRegions = field_map json__ "AllRegions" Boolean.of_json in
      let regionNames =
        field_map json__ "RegionNames" RegionNameStringList.of_json in
      make ?filters ?dryRun ?allRegions ?regionNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Regions that are enabled for your account, or all Regions. For a list of the Regions supported by Amazon EC2, see Amazon EC2 service endpoints. For information about enabling and disabling Regions for your account, see Specify which Amazon Web Services Regions your account can use in the Amazon Web Services Account Management Reference Guide. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribePublicIpv4PoolsResult =
  struct
    type nonrec t =
      {
      publicIpv4Pools: PublicIpv4PoolSet.t option
        [@ocaml.doc "Information about the address pools."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?publicIpv4Pools =
      fun ?nextToken -> fun () -> { publicIpv4Pools; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("publicIpv4PoolSet",
           (Option.map x.publicIpv4Pools ~f:PublicIpv4PoolSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let publicIpv4Pools =
        (Option.map ~f:PublicIpv4PoolSet.of_xml)
          (Xml.child xml_arg0 "publicIpv4PoolSet") in
      make ?nextToken ?publicIpv4Pools ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let publicIpv4Pools =
        field_map json__ "PublicIpv4Pools" PublicIpv4PoolSet.of_json in
      make ?nextToken ?publicIpv4Pools ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified IPv4 address pools."]
module DescribePublicIpv4PoolsRequest =
  struct
    type nonrec t =
      {
      poolIds: PublicIpv4PoolIdStringList.t option
        [@ocaml.doc "The IDs of the address pools."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: PoolMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."]}
    let make ?poolIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun () -> { poolIds; nextToken; maxResults; filters }
    let to_value x =
      structure_to_value
        [("PoolId",
           (Option.map x.poolIds ~f:PublicIpv4PoolIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:PoolMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:PoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let poolIds =
        (Option.map ~f:PublicIpv4PoolIdStringList.of_xml)
          (Xml.child xml_arg0 "PoolId") in
      make ?filters ?maxResults ?nextToken ?poolIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults = field_map json__ "MaxResults" PoolMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let poolIds =
        field_map json__ "PoolIds" PublicIpv4PoolIdStringList.of_json in
      make ?filters ?maxResults ?nextToken ?poolIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified IPv4 address pools."]
module DescribePrincipalIdFormatResult =
  struct
    type nonrec t =
      {
      principals: PrincipalIdFormatList.t option
        [@ocaml.doc "Information about the ID format settings for the ARN."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?principals =
      fun ?nextToken -> fun () -> { principals; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("principalSet",
           (Option.map x.principals ~f:PrincipalIdFormatList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let principals =
        (Option.map ~f:PrincipalIdFormatList.of_xml)
          (Xml.child xml_arg0 "principalSet") in
      make ?nextToken ?principals ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let principals =
        field_map json__ "Principals" PrincipalIdFormatList.of_json in
      make ?nextToken ?principals ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the ID format settings for the root user and all IAM roles and IAM users that have explicitly specified a longer ID (17-character ID) preference. By default, all IAM roles and IAM users default to the same ID settings as the root user, unless they explicitly override the settings. This request is useful for identifying those IAM users and IAM roles that have overridden the default ID settings. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway."]
module DescribePrincipalIdFormatRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      resources: ResourceList.t option
        [@ocaml.doc
          "The type of resource: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway"];
      maxResults: DescribePrincipalIdFormatMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned NextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token to request the next page of results."]}
    let make ?dryRun =
      fun ?resources ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { dryRun; resources; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Resource", (Option.map x.resources ~f:ResourceList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribePrincipalIdFormatMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribePrincipalIdFormatMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let resources =
        (Option.map ~f:ResourceList.of_xml) (Xml.child xml_arg0 "Resource") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?resources ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribePrincipalIdFormatMaxResults.of_json in
      let resources = field_map json__ "Resources" ResourceList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?resources ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the ID format settings for the root user and all IAM roles and IAM users that have explicitly specified a longer ID (17-character ID) preference. By default, all IAM roles and IAM users default to the same ID settings as the root user, unless they explicitly override the settings. This request is useful for identifying those IAM users and IAM roles that have overridden the default ID settings. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway."]
module DescribePrefixListsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      prefixLists: PrefixListSet.t option
        [@ocaml.doc "All available prefix lists."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?prefixLists -> fun () -> { nextToken; prefixLists }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("prefixListSet",
          (Option.map x.prefixLists ~f:PrefixListSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixLists =
        (Option.map ~f:PrefixListSet.of_xml)
          (Xml.child xml_arg0 "prefixListSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?prefixLists ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixLists = field_map json__ "PrefixLists" PrefixListSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?prefixLists ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes available Amazon Web Services services in a prefix list format, which includes the prefix list name and prefix list ID of the service and the IP address range for the service."]
module DescribePrefixListsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. prefix-list-id: The ID of a prefix list. prefix-list-name: The name of a prefix list."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      prefixListIds: PrefixListResourceIdStringList.t option
        [@ocaml.doc "One or more prefix list IDs."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?prefixListIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; prefixListIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("PrefixListId",
          (Option.map x.prefixListIds
             ~f:PrefixListResourceIdStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixListIds =
        (Option.map ~f:PrefixListResourceIdStringList.of_xml)
          (Xml.child xml_arg0 "PrefixListId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixListIds =
        field_map json__ "PrefixListIds"
          PrefixListResourceIdStringList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes available Amazon Web Services services in a prefix list format, which includes the prefix list name and prefix list ID of the service and the IP address range for the service."]
module DescribePlacementGroupsResult =
  struct
    type nonrec t =
      {
      placementGroups: PlacementGroupList.t option
        [@ocaml.doc "Information about the placement groups."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?placementGroups = fun () -> { placementGroups }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("placementGroupSet",
           (Option.map x.placementGroups ~f:PlacementGroupList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementGroups =
        (Option.map ~f:PlacementGroupList.of_xml)
          (Xml.child xml_arg0 "placementGroupSet") in
      make ?placementGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let placementGroups =
        field_map json__ "PlacementGroups" PlacementGroupList.of_json in
      make ?placementGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified placement groups or all of your placement groups. To describe a specific placement group that is shared with your account, you must specify the ID of the placement group using the GroupId parameter. Specifying the name of a shared placement group using the GroupNames parameter will result in an error. For more information, see Placement groups in the Amazon EC2 User Guide."]
module DescribePlacementGroupsRequest =
  struct
    type nonrec t =
      {
      groupIds: PlacementGroupIdStringList.t option
        [@ocaml.doc "The IDs of the placement groups."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupNames: PlacementGroupStringList.t option
        [@ocaml.doc
          "The names of the placement groups. Constraints: You can specify a name only if the placement group is owned by your account. If a placement group is shared with your account, specifying the name results in an error. You must use the GroupId parameter instead."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. group-name - The name of the placement group. group-arn - The Amazon Resource Name (ARN) of the placement group. spread-level - The spread level for the placement group (host | rack). state - The state of the placement group (pending | available | deleting | deleted). strategy - The strategy of the placement group (cluster | spread | partition). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources that have a tag with a specific key, regardless of the tag value."]}
    let make ?groupIds =
      fun ?dryRun ->
        fun ?groupNames ->
          fun ?filters -> fun () -> { groupIds; dryRun; groupNames; filters }
    let to_value x =
      structure_to_value
        [("GroupId",
           (Option.map x.groupIds ~f:PlacementGroupIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupName",
          (Option.map x.groupNames ~f:PlacementGroupStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let groupNames =
        (Option.map ~f:PlacementGroupStringList.of_xml)
          (Xml.child xml_arg0 "groupName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let groupIds =
        (Option.map ~f:PlacementGroupIdStringList.of_xml)
          (Xml.child xml_arg0 "GroupId") in
      make ?filters ?groupNames ?dryRun ?groupIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let groupNames =
        field_map json__ "GroupNames" PlacementGroupStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let groupIds =
        field_map json__ "GroupIds" PlacementGroupIdStringList.of_json in
      make ?filters ?groupNames ?dryRun ?groupIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified placement groups or all of your placement groups. To describe a specific placement group that is shared with your account, you must specify the ID of the placement group using the GroupId parameter. Specifying the name of a shared placement group using the GroupNames parameter will result in an error. For more information, see Placement groups in the Amazon EC2 User Guide."]
module DescribeOutpostLagsResult =
  struct
    type nonrec t =
      {
      outpostLags: OutpostLagSet.t option [@ocaml.doc "The Outpost LAGs."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?outpostLags =
      fun ?nextToken -> fun () -> { outpostLags; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("outpostLagSet",
           (Option.map x.outpostLags ~f:OutpostLagSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let outpostLags =
        (Option.map ~f:OutpostLagSet.of_xml)
          (Xml.child xml_arg0 "outpostLagSet") in
      make ?nextToken ?outpostLags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let outpostLags = field_map json__ "OutpostLags" OutpostLagSet.of_json in
      make ?nextToken ?outpostLags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Outposts link aggregation groups (LAGs). LAGs are only available for second-generation Outposts racks at this time."]
module DescribeOutpostLagsRequest =
  struct
    type nonrec t =
      {
      outpostLagIds: OutpostLagIdSet.t option
        [@ocaml.doc "The IDs of the Outpost LAGs."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters to use for narrowing down the request. The following filters are supported: service-link-virtual-interface-id - The ID of the service link virtual interface. service-link-virtual-interface-arn - The ARN of the service link virtual interface. outpost-id - The Outpost ID. outpost-arn - The Outpost ARN. owner-id - The ID of the Amazon Web Services account that owns the service link virtual interface. vlan - The ID of the address pool. local-address - The local address. peer-address - The peer address. peer-bgp-asn - The peer BGP ASN. outpost-lag-id - The Outpost LAG ID. configuration-state - The configuration state of the service link virtual interface."];
      maxResults: OutpostLagMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?outpostLagIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { outpostLagIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("OutpostLagId",
           (Option.map x.outpostLagIds ~f:OutpostLagIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:OutpostLagMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:OutpostLagMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let outpostLagIds =
        (Option.map ~f:OutpostLagIdSet.of_xml)
          (Xml.child xml_arg0 "OutpostLagId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?outpostLagIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" OutpostLagMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let outpostLagIds =
        field_map json__ "OutpostLagIds" OutpostLagIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?outpostLagIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Outposts link aggregation groups (LAGs). LAGs are only available for second-generation Outposts racks at this time."]
module DescribeNetworkInterfacesResult =
  struct
    type nonrec t =
      {
      networkInterfaces: NetworkInterfaceList.t option
        [@ocaml.doc "Information about the network interfaces."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfaces =
      fun ?nextToken -> fun () -> { networkInterfaces; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfaceSet",
           (Option.map x.networkInterfaces ~f:NetworkInterfaceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInterfaces =
        (Option.map ~f:NetworkInterfaceList.of_xml)
          (Xml.child xml_arg0 "networkInterfaceSet") in
      make ?nextToken ?networkInterfaces ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkInterfaces =
        field_map json__ "NetworkInterfaces" NetworkInterfaceList.of_json in
      make ?nextToken ?networkInterfaces ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified network interfaces or all your network interfaces. If you have a large number of network interfaces, the operation fails unless you use pagination or one of the following filters: group-id, mac-address, private-dns-name, private-ip-address, subnet-id, or vpc-id. We strongly recommend using only paginated requests. Unpaginated requests are susceptible to throttling and timeouts."]
module DescribeNetworkInterfacesRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeNetworkInterfacesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. You cannot specify this parameter and the network interface IDs parameter in the same request. For more information, see Pagination."];
      includeManagedResources: Boolean.t option
        [@ocaml.doc
          "Indicates whether to include managed resources in the output. If this parameter is set to true, the output includes resources that are managed by Amazon Web Services services, even if managed resource visibility is set to hidden."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceIds: NetworkInterfaceIdList.t option
        [@ocaml.doc
          "The network interface IDs. Default: Describes all your network interfaces."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. association.allocation-id - The allocation ID returned when you allocated the Elastic IP address (IPv4) for your network interface. association.association-id - The association ID returned when the network interface was associated with an IPv4 address. addresses.association.owner-id - The owner ID of the addresses associated with the network interface. addresses.association.public-ip - The association ID returned when the network interface was associated with the Elastic IP address (IPv4). addresses.primary - Whether the private IPv4 address is the primary IP address associated with the network interface. addresses.private-ip-address - The private IPv4 addresses associated with the network interface. association.ip-owner-id - The owner of the Elastic IP address (IPv4) associated with the network interface. association.public-ip - The address of the Elastic IP address (IPv4) bound to the network interface. association.public-dns-name - The public DNS name for the network interface (IPv4). attachment.attach-time - The time that the network interface was attached to an instance. attachment.attachment-id - The ID of the interface attachment. attachment.delete-on-termination - Indicates whether the attachment is deleted when an instance is terminated. attachment.device-index - The device index to which the network interface is attached. attachment.instance-id - The ID of the instance to which the network interface is attached. attachment.instance-owner-id - The owner ID of the instance to which the network interface is attached. attachment.status - The status of the attachment (attaching | attached | detaching | detached). availability-zone - The Availability Zone of the network interface. availability-zone-id - The ID of the Availability Zone of the network interface. description - The description of the network interface. group-id - The ID of a security group associated with the network interface. ipv6-addresses.ipv6-address - An IPv6 address associated with the network interface. interface-type - The type of network interface (api_gateway_managed | aws_codestar_connections_managed | branch | ec2_instance_connect_endpoint | efa | efa-only | efs | evs | gateway_load_balancer | gateway_load_balancer_endpoint | global_accelerator_managed | interface | iot_rules_managed | lambda | load_balancer | nat_gateway | network_load_balancer | quicksight | transit_gateway | trunk | vpc_endpoint). mac-address - The MAC address of the network interface. network-interface-id - The ID of the network interface. operator.managed - A Boolean that indicates whether this is a managed network interface. operator.principal - The principal that manages the network interface. Only valid for managed network interfaces, where managed is true. owner-id - The Amazon Web Services account ID of the network interface owner. private-dns-name - The private DNS name of the network interface (IPv4). private-ip-address - The private IPv4 address or addresses of the network interface. requester-id - The alias or Amazon Web Services account ID of the principal or service that created the network interface. requester-managed - Indicates whether the network interface is being managed by an Amazon Web Services service (for example, Amazon Web Services Management Console, Auto Scaling, and so on). source-dest-check - Indicates whether the network interface performs source/destination checking. A value of true means checking is enabled, and false means checking is disabled. The value must be false for the network interface to perform network address translation (NAT) in your VPC. status - The status of the network interface. If the network interface is not attached to an instance, the status is available; if a network interface is attached to an instance the status is in-use. subnet-id - The ID of the subnet for the network interface. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. vpc-id - The ID of the VPC for the network interface."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?includeManagedResources ->
          fun ?dryRun ->
            fun ?networkInterfaceIds ->
              fun ?filters ->
                fun () ->
                  {
                    nextToken;
                    maxResults;
                    includeManagedResources;
                    dryRun;
                    networkInterfaceIds;
                    filters
                  }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeNetworkInterfacesMaxResults.to_value));
        ("IncludeManagedResources",
          (Option.map x.includeManagedResources ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInterfaceId",
          (Option.map x.networkInterfaceIds
             ~f:NetworkInterfaceIdList.to_value));
        ("filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filter") in
      let networkInterfaceIds =
        (Option.map ~f:NetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let includeManagedResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeManagedResources") in
      let maxResults =
        (Option.map ~f:DescribeNetworkInterfacesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?filters ?networkInterfaceIds ?dryRun ?includeManagedResources
        ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let networkInterfaceIds =
        field_map json__ "NetworkInterfaceIds" NetworkInterfaceIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let includeManagedResources =
        field_map json__ "IncludeManagedResources" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeNetworkInterfacesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?filters ?networkInterfaceIds ?dryRun ?includeManagedResources
        ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeNetworkInterfaces."]
module DescribeNetworkInterfacePermissionsResult =
  struct
    type nonrec t =
      {
      networkInterfacePermissions: NetworkInterfacePermissionList.t option
        [@ocaml.doc "The network interface permissions."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfacePermissions =
      fun ?nextToken -> fun () -> { networkInterfacePermissions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfacePermissions",
           (Option.map x.networkInterfacePermissions
              ~f:NetworkInterfacePermissionList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInterfacePermissions =
        (Option.map ~f:NetworkInterfacePermissionList.of_xml)
          (Xml.child xml_arg0 "networkInterfacePermissions") in
      make ?nextToken ?networkInterfacePermissions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkInterfacePermissions =
        field_map json__ "NetworkInterfacePermissions"
          NetworkInterfacePermissionList.of_json in
      make ?nextToken ?networkInterfacePermissions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output for DescribeNetworkInterfacePermissions."]
module DescribeNetworkInterfacePermissionsRequest =
  struct
    type nonrec t =
      {
      networkInterfacePermissionIds:
        NetworkInterfacePermissionIdList.t option
        [@ocaml.doc "The network interface permission IDs."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. network-interface-permission.network-interface-permission-id - The ID of the permission. network-interface-permission.network-interface-id - The ID of the network interface. network-interface-permission.aws-account-id - The Amazon Web Services account ID. network-interface-permission.aws-service - The Amazon Web Services service. network-interface-permission.permission - The type of permission (INSTANCE-ATTACH | EIP-ASSOCIATE)."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeNetworkInterfacePermissionsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. If this parameter is not specified, up to 50 results are returned by default. For more information, see Pagination."]}
    let make ?networkInterfacePermissionIds =
      fun ?filters ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () ->
              { networkInterfacePermissionIds; filters; nextToken; maxResults
              }
    let to_value x =
      structure_to_value
        [("NetworkInterfacePermissionId",
           (Option.map x.networkInterfacePermissionIds
              ~f:NetworkInterfacePermissionIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeNetworkInterfacePermissionsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeNetworkInterfacePermissionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkInterfacePermissionIds =
        (Option.map ~f:NetworkInterfacePermissionIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfacePermissionId") in
      make ?maxResults ?nextToken ?filters ?networkInterfacePermissionIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeNetworkInterfacePermissionsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let networkInterfacePermissionIds =
        field_map json__ "NetworkInterfacePermissionIds"
          NetworkInterfacePermissionIdList.of_json in
      make ?maxResults ?nextToken ?filters ?networkInterfacePermissionIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeNetworkInterfacePermissions."]
module DescribeNetworkInterfaceAttributeResult =
  struct
    type nonrec t =
      {
      attachment: NetworkInterfaceAttachment.t option
        [@ocaml.doc "The attachment (if any) of the network interface."];
      description: AttributeValue.t option
        [@ocaml.doc "The description of the network interface."];
      groups: GroupIdentifierList.t option
        [@ocaml.doc
          "The security groups associated with the network interface."];
      networkInterfaceId: String_.t option
        [@ocaml.doc "The ID of the network interface."];
      sourceDestCheck: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether source/destination checking is enabled."];
      associatePublicIpAddress: Boolean.t option
        [@ocaml.doc
          "Indicates whether to assign a public IPv4 address to a network interface. This option can be enabled for any network interface but will only apply to the primary network interface (eth0)."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?attachment =
      fun ?description ->
        fun ?groups ->
          fun ?networkInterfaceId ->
            fun ?sourceDestCheck ->
              fun ?associatePublicIpAddress ->
                fun () ->
                  {
                    attachment;
                    description;
                    groups;
                    networkInterfaceId;
                    sourceDestCheck;
                    associatePublicIpAddress
                  }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachment",
           (Option.map x.attachment ~f:NetworkInterfaceAttachment.to_value));
        ("description",
          (Option.map x.description ~f:AttributeValue.to_value));
        ("groupSet", (Option.map x.groups ~f:GroupIdentifierList.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:String_.to_value));
        ("sourceDestCheck",
          (Option.map x.sourceDestCheck ~f:AttributeBooleanValue.to_value));
        ("associatePublicIpAddress",
          (Option.map x.associatePublicIpAddress ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associatePublicIpAddress =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "associatePublicIpAddress") in
      let sourceDestCheck =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "sourceDestCheck") in
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let groups =
        (Option.map ~f:GroupIdentifierList.of_xml)
          (Xml.child xml_arg0 "groupSet") in
      let description =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "description") in
      let attachment =
        (Option.map ~f:NetworkInterfaceAttachment.of_xml)
          (Xml.child xml_arg0 "attachment") in
      make ?associatePublicIpAddress ?sourceDestCheck ?networkInterfaceId
        ?groups ?description ?attachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associatePublicIpAddress =
        field_map json__ "AssociatePublicIpAddress" Boolean.of_json in
      let sourceDestCheck =
        field_map json__ "SourceDestCheck" AttributeBooleanValue.of_json in
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" String_.of_json in
      let groups = field_map json__ "Groups" GroupIdentifierList.of_json in
      let description = field_map json__ "Description" AttributeValue.of_json in
      let attachment =
        field_map json__ "Attachment" NetworkInterfaceAttachment.of_json in
      make ?associatePublicIpAddress ?sourceDestCheck ?networkInterfaceId
        ?groups ?description ?attachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of DescribeNetworkInterfaceAttribute."]
module DescribeNetworkInterfaceAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      attribute: NetworkInterfaceAttribute.t option
        [@ocaml.doc
          "The attribute of the network interface. This parameter is required."]}
    let context_ = "DescribeNetworkInterfaceAttributeRequest"
    let make ?dryRun =
      fun ?attribute ->
        fun ~networkInterfaceId ->
          fun () -> { dryRun; attribute; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("attribute",
          (Option.map x.attribute ~f:NetworkInterfaceAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attribute =
        (Option.map ~f:NetworkInterfaceAttribute.of_xml)
          (Xml.child xml_arg0 "attribute") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?attribute ~networkInterfaceId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attribute =
        field_map json__ "Attribute" NetworkInterfaceAttribute.of_json in
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?attribute ~networkInterfaceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeNetworkInterfaceAttribute."]
module DescribeNetworkInsightsPathsResult =
  struct
    type nonrec t =
      {
      networkInsightsPaths: NetworkInsightsPathList.t option
        [@ocaml.doc "Information about the paths."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsPaths =
      fun ?nextToken -> fun () -> { networkInsightsPaths; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsPathSet",
           (Option.map x.networkInsightsPaths
              ~f:NetworkInsightsPathList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsPaths =
        (Option.map ~f:NetworkInsightsPathList.of_xml)
          (Xml.child xml_arg0 "networkInsightsPathSet") in
      make ?nextToken ?networkInsightsPaths ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkInsightsPaths =
        field_map json__ "NetworkInsightsPaths"
          NetworkInsightsPathList.of_json in
      make ?nextToken ?networkInsightsPaths ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your paths."]
module DescribeNetworkInsightsPathsRequest =
  struct
    type nonrec t =
      {
      networkInsightsPathIds: NetworkInsightsPathIdList.t option
        [@ocaml.doc "The IDs of the paths."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. The following are the possible values: destination - The ID of the resource. filter-at-source.source-address - The source IPv4 address at the source. filter-at-source.source-port-range - The source port range at the source. filter-at-source.destination-address - The destination IPv4 address at the source. filter-at-source.destination-port-range - The destination port range at the source. filter-at-destination.source-address - The source IPv4 address at the destination. filter-at-destination.source-port-range - The source port range at the destination. filter-at-destination.destination-address - The destination IPv4 address at the destination. filter-at-destination.destination-port-range - The destination port range at the destination. protocol - The protocol. source - The ID of the resource."];
      maxResults: NetworkInsightsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?networkInsightsPathIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?nextToken ->
              fun () ->
                {
                  networkInsightsPathIds;
                  filters;
                  maxResults;
                  dryRun;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("NetworkInsightsPathId",
           (Option.map x.networkInsightsPathIds
              ~f:NetworkInsightsPathIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkInsightsPathIds =
        (Option.map ~f:NetworkInsightsPathIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsPathId") in
      make ?nextToken ?dryRun ?maxResults ?filters ?networkInsightsPathIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let networkInsightsPathIds =
        field_map json__ "NetworkInsightsPathIds"
          NetworkInsightsPathIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters ?networkInsightsPathIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your paths."]
module DescribeNetworkInsightsAnalysesResult =
  struct
    type nonrec t =
      {
      networkInsightsAnalyses: NetworkInsightsAnalysisList.t option
        [@ocaml.doc "Information about the network insights analyses."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAnalyses =
      fun ?nextToken -> fun () -> { networkInsightsAnalyses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAnalysisSet",
           (Option.map x.networkInsightsAnalyses
              ~f:NetworkInsightsAnalysisList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsAnalyses =
        (Option.map ~f:NetworkInsightsAnalysisList.of_xml)
          (Xml.child xml_arg0 "networkInsightsAnalysisSet") in
      make ?nextToken ?networkInsightsAnalyses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkInsightsAnalyses =
        field_map json__ "NetworkInsightsAnalyses"
          NetworkInsightsAnalysisList.of_json in
      make ?nextToken ?networkInsightsAnalyses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your network insights analyses."]
module DescribeNetworkInsightsAnalysesRequest =
  struct
    type nonrec t =
      {
      networkInsightsAnalysisIds: NetworkInsightsAnalysisIdList.t option
        [@ocaml.doc
          "The ID of the network insights analyses. You must specify either analysis IDs or a path ID."];
      networkInsightsPathId: NetworkInsightsPathId.t option
        [@ocaml.doc
          "The ID of the path. You must specify either a path ID or analysis IDs."];
      analysisStartTime: MillisecondDateTime.t option
        [@ocaml.doc "The time when the network insights analyses started."];
      analysisEndTime: MillisecondDateTime.t option
        [@ocaml.doc "The time when the network insights analyses ended."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. The following are the possible values: path-found - A Boolean value that indicates whether a feasible path is found. status - The status of the analysis (running | succeeded | failed)."];
      maxResults: NetworkInsightsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?networkInsightsAnalysisIds =
      fun ?networkInsightsPathId ->
        fun ?analysisStartTime ->
          fun ?analysisEndTime ->
            fun ?filters ->
              fun ?maxResults ->
                fun ?dryRun ->
                  fun ?nextToken ->
                    fun () ->
                      {
                        networkInsightsAnalysisIds;
                        networkInsightsPathId;
                        analysisStartTime;
                        analysisEndTime;
                        filters;
                        maxResults;
                        dryRun;
                        nextToken
                      }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAnalysisId",
           (Option.map x.networkInsightsAnalysisIds
              ~f:NetworkInsightsAnalysisIdList.to_value));
        ("NetworkInsightsPathId",
          (Option.map x.networkInsightsPathId
             ~f:NetworkInsightsPathId.to_value));
        ("AnalysisStartTime",
          (Option.map x.analysisStartTime ~f:MillisecondDateTime.to_value));
        ("AnalysisEndTime",
          (Option.map x.analysisEndTime ~f:MillisecondDateTime.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let analysisEndTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisEndTime") in
      let analysisStartTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisStartTime") in
      let networkInsightsPathId =
        (Option.map ~f:NetworkInsightsPathId.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsPathId") in
      let networkInsightsAnalysisIds =
        (Option.map ~f:NetworkInsightsAnalysisIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAnalysisId") in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisEndTime
        ?analysisStartTime ?networkInsightsPathId ?networkInsightsAnalysisIds
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let analysisEndTime =
        field_map json__ "AnalysisEndTime" MillisecondDateTime.of_json in
      let analysisStartTime =
        field_map json__ "AnalysisStartTime" MillisecondDateTime.of_json in
      let networkInsightsPathId =
        field_map json__ "NetworkInsightsPathId"
          NetworkInsightsPathId.of_json in
      let networkInsightsAnalysisIds =
        field_map json__ "NetworkInsightsAnalysisIds"
          NetworkInsightsAnalysisIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisEndTime
        ?analysisStartTime ?networkInsightsPathId ?networkInsightsAnalysisIds
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your network insights analyses."]
module DescribeNetworkInsightsAccessScopesResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopes: NetworkInsightsAccessScopeList.t option
        [@ocaml.doc "The Network Access Scopes."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopes =
      fun ?nextToken -> fun () -> { networkInsightsAccessScopes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeSet",
           (Option.map x.networkInsightsAccessScopes
              ~f:NetworkInsightsAccessScopeList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsAccessScopes =
        (Option.map ~f:NetworkInsightsAccessScopeList.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeSet") in
      make ?nextToken ?networkInsightsAccessScopes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkInsightsAccessScopes =
        field_map json__ "NetworkInsightsAccessScopes"
          NetworkInsightsAccessScopeList.of_json in
      make ?nextToken ?networkInsightsAccessScopes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Network Access Scopes."]
module DescribeNetworkInsightsAccessScopesRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeIds:
        NetworkInsightsAccessScopeIdList.t option
        [@ocaml.doc "The IDs of the Network Access Scopes."];
      filters: FilterList.t option
        [@ocaml.doc "There are no supported filters."];
      maxResults: NetworkInsightsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?networkInsightsAccessScopeIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?nextToken ->
              fun () ->
                {
                  networkInsightsAccessScopeIds;
                  filters;
                  maxResults;
                  dryRun;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeId",
           (Option.map x.networkInsightsAccessScopeIds
              ~f:NetworkInsightsAccessScopeIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkInsightsAccessScopeIds =
        (Option.map ~f:NetworkInsightsAccessScopeIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAccessScopeId") in
      make ?nextToken ?dryRun ?maxResults ?filters
        ?networkInsightsAccessScopeIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let networkInsightsAccessScopeIds =
        field_map json__ "NetworkInsightsAccessScopeIds"
          NetworkInsightsAccessScopeIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters
        ?networkInsightsAccessScopeIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Network Access Scopes."]
module DescribeNetworkInsightsAccessScopeAnalysesResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalyses:
        NetworkInsightsAccessScopeAnalysisList.t option
        [@ocaml.doc "The Network Access Scope analyses."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeAnalyses =
      fun ?nextToken ->
        fun () -> { networkInsightsAccessScopeAnalyses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeAnalysisSet",
           (Option.map x.networkInsightsAccessScopeAnalyses
              ~f:NetworkInsightsAccessScopeAnalysisList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkInsightsAccessScopeAnalyses =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisList.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeAnalysisSet") in
      make ?nextToken ?networkInsightsAccessScopeAnalyses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkInsightsAccessScopeAnalyses =
        field_map json__ "NetworkInsightsAccessScopeAnalyses"
          NetworkInsightsAccessScopeAnalysisList.of_json in
      make ?nextToken ?networkInsightsAccessScopeAnalyses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Network Access Scope analyses."]
module DescribeNetworkInsightsAccessScopeAnalysesRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisIds:
        NetworkInsightsAccessScopeAnalysisIdList.t option
        [@ocaml.doc "The IDs of the Network Access Scope analyses."];
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t option
        [@ocaml.doc "The ID of the Network Access Scope."];
      analysisStartTimeBegin: MillisecondDateTime.t option
        [@ocaml.doc
          "Filters the results based on the start time. The analysis must have started on or after this time."];
      analysisStartTimeEnd: MillisecondDateTime.t option
        [@ocaml.doc
          "Filters the results based on the start time. The analysis must have started on or before this time."];
      filters: FilterList.t option
        [@ocaml.doc "There are no supported filters."];
      maxResults: NetworkInsightsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?networkInsightsAccessScopeAnalysisIds =
      fun ?networkInsightsAccessScopeId ->
        fun ?analysisStartTimeBegin ->
          fun ?analysisStartTimeEnd ->
            fun ?filters ->
              fun ?maxResults ->
                fun ?dryRun ->
                  fun ?nextToken ->
                    fun () ->
                      {
                        networkInsightsAccessScopeAnalysisIds;
                        networkInsightsAccessScopeId;
                        analysisStartTimeBegin;
                        analysisStartTimeEnd;
                        filters;
                        maxResults;
                        dryRun;
                        nextToken
                      }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeAnalysisId",
           (Option.map x.networkInsightsAccessScopeAnalysisIds
              ~f:NetworkInsightsAccessScopeAnalysisIdList.to_value));
        ("NetworkInsightsAccessScopeId",
          (Option.map x.networkInsightsAccessScopeId
             ~f:NetworkInsightsAccessScopeId.to_value));
        ("AnalysisStartTimeBegin",
          (Option.map x.analysisStartTimeBegin
             ~f:MillisecondDateTime.to_value));
        ("AnalysisStartTimeEnd",
          (Option.map x.analysisStartTimeEnd ~f:MillisecondDateTime.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let analysisStartTimeEnd =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisStartTimeEnd") in
      let analysisStartTimeBegin =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "AnalysisStartTimeBegin") in
      let networkInsightsAccessScopeId =
        (Option.map ~f:NetworkInsightsAccessScopeId.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAccessScopeId") in
      let networkInsightsAccessScopeAnalysisIds =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInsightsAccessScopeAnalysisId") in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisStartTimeEnd
        ?analysisStartTimeBegin ?networkInsightsAccessScopeId
        ?networkInsightsAccessScopeAnalysisIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" NetworkInsightsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let analysisStartTimeEnd =
        field_map json__ "AnalysisStartTimeEnd" MillisecondDateTime.of_json in
      let analysisStartTimeBegin =
        field_map json__ "AnalysisStartTimeBegin" MillisecondDateTime.of_json in
      let networkInsightsAccessScopeId =
        field_map json__ "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      let networkInsightsAccessScopeAnalysisIds =
        field_map json__ "NetworkInsightsAccessScopeAnalysisIds"
          NetworkInsightsAccessScopeAnalysisIdList.of_json in
      make ?nextToken ?dryRun ?maxResults ?filters ?analysisStartTimeEnd
        ?analysisStartTimeBegin ?networkInsightsAccessScopeId
        ?networkInsightsAccessScopeAnalysisIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Network Access Scope analyses."]
module DescribeNetworkAclsResult =
  struct
    type nonrec t =
      {
      networkAcls: NetworkAclList.t option
        [@ocaml.doc "Information about the network ACLs."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkAcls =
      fun ?nextToken -> fun () -> { networkAcls; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkAclSet",
           (Option.map x.networkAcls ~f:NetworkAclList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkAcls =
        (Option.map ~f:NetworkAclList.of_xml)
          (Xml.child xml_arg0 "networkAclSet") in
      make ?nextToken ?networkAcls ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let networkAcls = field_map json__ "NetworkAcls" NetworkAclList.of_json in
      make ?nextToken ?networkAcls ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your network ACLs. The default is to describe all your network ACLs. Alternatively, you can specify specific network ACL IDs or filter the results to include only the network ACLs that match specific criteria. For more information, see Network ACLs in the Amazon VPC User Guide."]
module DescribeNetworkAclsRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeNetworkAclsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkAclIds: NetworkAclIdStringList.t option
        [@ocaml.doc "The IDs of the network ACLs."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. association.association-id - The ID of an association ID for the ACL. association.network-acl-id - The ID of the network ACL involved in the association. association.subnet-id - The ID of the subnet involved in the association. default - Indicates whether the ACL is the default network ACL for the VPC. entry.cidr - The IPv4 CIDR range specified in the entry. entry.icmp.code - The ICMP code specified in the entry, if any. entry.icmp.type - The ICMP type specified in the entry, if any. entry.ipv6-cidr - The IPv6 CIDR range specified in the entry. entry.port-range.from - The start of the port range specified in the entry. entry.port-range.to - The end of the port range specified in the entry. entry.protocol - The protocol specified in the entry (tcp | udp | icmp or a protocol number). entry.rule-action - Allows or denies the matching traffic (allow | deny). entry.egress - A Boolean that indicates the type of rule. Specify true for egress rules, or false for ingress rules. entry.rule-number - The number of an entry (in other words, rule) in the set of ACL entries. network-acl-id - The ID of the network ACL. owner-id - The ID of the Amazon Web Services account that owns the network ACL. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. vpc-id - The ID of the VPC for the network ACL."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?dryRun ->
          fun ?networkAclIds ->
            fun ?filters ->
              fun () ->
                { nextToken; maxResults; dryRun; networkAclIds; filters }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeNetworkAclsMaxResults.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkAclId",
          (Option.map x.networkAclIds ~f:NetworkAclIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let networkAclIds =
        (Option.map ~f:NetworkAclIdStringList.of_xml)
          (Xml.child xml_arg0 "NetworkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let maxResults =
        (Option.map ~f:DescribeNetworkAclsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?filters ?networkAclIds ?dryRun ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let networkAclIds =
        field_map json__ "NetworkAclIds" NetworkAclIdStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" DescribeNetworkAclsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?filters ?networkAclIds ?dryRun ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your network ACLs. The default is to describe all your network ACLs. Alternatively, you can specify specific network ACL IDs or filter the results to include only the network ACLs that match specific criteria. For more information, see Network ACLs in the Amazon VPC User Guide."]
module DescribeNatGatewaysResult =
  struct
    type nonrec t =
      {
      natGateways: NatGatewayList.t option
        [@ocaml.doc "Information about the NAT gateways."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?natGateways =
      fun ?nextToken -> fun () -> { natGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("natGatewaySet",
           (Option.map x.natGateways ~f:NatGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let natGateways =
        (Option.map ~f:NatGatewayList.of_xml)
          (Xml.child xml_arg0 "natGatewaySet") in
      make ?nextToken ?natGateways ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let natGateways = field_map json__ "NatGateways" NatGatewayList.of_json in
      make ?nextToken ?natGateways ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your NAT gateways. The default is to describe all your NAT gateways. Alternatively, you can specify specific NAT gateway IDs or filter the results to include only the NAT gateways that match specific criteria."]
module DescribeNatGatewaysRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filter: FilterList.t option
        [@ocaml.doc
          "The filters. nat-gateway-id - The ID of the NAT gateway. state - The state of the NAT gateway (pending | failed | available | deleting | deleted). subnet-id - The ID of the subnet in which the NAT gateway resides. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. vpc-id - The ID of the VPC in which the NAT gateway resides."];
      maxResults: DescribeNatGatewaysMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      natGatewayIds: NatGatewayIdStringList.t option
        [@ocaml.doc "The IDs of the NAT gateways."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."]}
    let make ?dryRun =
      fun ?filter ->
        fun ?maxResults ->
          fun ?natGatewayIds ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filter; maxResults; natGatewayIds; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeNatGatewaysMaxResults.to_value));
        ("NatGatewayId",
          (Option.map x.natGatewayIds ~f:NatGatewayIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let natGatewayIds =
        (Option.map ~f:NatGatewayIdStringList.of_xml)
          (Xml.child xml_arg0 "NatGatewayId") in
      let maxResults =
        (Option.map ~f:DescribeNatGatewaysMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?natGatewayIds ?maxResults ?filter ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let natGatewayIds =
        field_map json__ "NatGatewayIds" NatGatewayIdStringList.of_json in
      let maxResults =
        field_map json__ "MaxResults" DescribeNatGatewaysMaxResults.of_json in
      let filter = field_map json__ "Filter" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?natGatewayIds ?maxResults ?filter ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your NAT gateways. The default is to describe all your NAT gateways. Alternatively, you can specify specific NAT gateway IDs or filter the results to include only the NAT gateways that match specific criteria."]
module DescribeMovingAddressesResult =
  struct
    type nonrec t =
      {
      movingAddressStatuses: MovingAddressStatusSet.t option
        [@ocaml.doc "The status for each Elastic IP address."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?movingAddressStatuses =
      fun ?nextToken -> fun () -> { movingAddressStatuses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("movingAddressStatusSet",
           (Option.map x.movingAddressStatuses
              ~f:MovingAddressStatusSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let movingAddressStatuses =
        (Option.map ~f:MovingAddressStatusSet.of_xml)
          (Xml.child xml_arg0 "movingAddressStatusSet") in
      make ?nextToken ?movingAddressStatuses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let movingAddressStatuses =
        field_map json__ "MovingAddressStatuses"
          MovingAddressStatusSet.of_json in
      make ?nextToken ?movingAddressStatuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is deprecated. Describes your Elastic IP addresses that are being moved from or being restored to the EC2-Classic platform. This request does not return information about any other Elastic IP addresses in your account."]
module DescribeMovingAddressesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      publicIps: ValueStringList.t option
        [@ocaml.doc "One or more Elastic IP addresses."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. moving-status - The status of the Elastic IP address (MovingToVpc | RestoringToClassic)."];
      maxResults: DescribeMovingAddressesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results of the initial request can be seen by sending another request with the returned NextToken value. This value can be between 5 and 1000; if MaxResults is given a value outside of this range, an error is returned. Default: If no value is provided, the default is 1000."]}
    let make ?dryRun =
      fun ?publicIps ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?maxResults ->
              fun () -> { dryRun; publicIps; nextToken; filters; maxResults }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("publicIp", (Option.map x.publicIps ~f:ValueStringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("filter", (Option.map x.filters ~f:FilterList.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:DescribeMovingAddressesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeMovingAddressesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let publicIps =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "publicIp") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?maxResults ?filters ?nextToken ?publicIps ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeMovingAddressesMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let publicIps = field_map json__ "PublicIps" ValueStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?maxResults ?filters ?nextToken ?publicIps ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is deprecated. Describes your Elastic IP addresses that are being moved from or being restored to the EC2-Classic platform. This request does not return information about any other Elastic IP addresses in your account."]
module DescribeManagedPrefixListsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      prefixLists: ManagedPrefixListSet.t option
        [@ocaml.doc "Information about the prefix lists."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?prefixLists -> fun () -> { nextToken; prefixLists }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("prefixListSet",
          (Option.map x.prefixLists ~f:ManagedPrefixListSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixLists =
        (Option.map ~f:ManagedPrefixListSet.of_xml)
          (Xml.child xml_arg0 "prefixListSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?prefixLists ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixLists =
        field_map json__ "PrefixLists" ManagedPrefixListSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?prefixLists ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your managed prefix lists and any Amazon Web Services-managed prefix lists."]
module DescribeManagedPrefixListsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. owner-id - The ID of the prefix list owner. prefix-list-id - The ID of the prefix list. prefix-list-name - The name of the prefix list."];
      maxResults: PrefixListMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      prefixListIds: ValueStringList.t option
        [@ocaml.doc "One or more prefix list IDs."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?prefixListIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; prefixListIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:PrefixListMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("PrefixListId",
          (Option.map x.prefixListIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixListIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "PrefixListId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:PrefixListMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixListIds =
        field_map json__ "PrefixListIds" ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" PrefixListMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?prefixListIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your managed prefix lists and any Amazon Web Services-managed prefix lists."]
module DescribeMacModificationTasksResult =
  struct
    type nonrec t =
      {
      macModificationTasks: MacModificationTaskList.t option
        [@ocaml.doc "Information about the tasks."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?macModificationTasks =
      fun ?nextToken -> fun () -> { macModificationTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("macModificationTaskSet",
           (Option.map x.macModificationTasks
              ~f:MacModificationTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let macModificationTasks =
        (Option.map ~f:MacModificationTaskList.of_xml)
          (Xml.child xml_arg0 "macModificationTaskSet") in
      make ?nextToken ?macModificationTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let macModificationTasks =
        field_map json__ "MacModificationTasks"
          MacModificationTaskList.of_json in
      make ?nextToken ?macModificationTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a System Integrity Protection (SIP) modification task or volume ownership delegation task for an Amazon EC2 Mac instance. For more information, see Configure SIP for Amazon EC2 instances in the Amazon EC2 User Guide."]
module DescribeMacModificationTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "Specifies one or more filters for the request: instance-id - The ID of the instance for which the task was created. task-state - The state of the task (successful | failed | in-progress | pending). mac-system-integrity-protection-configuration.sip-status - The overall SIP state requested in the task (enabled | disabled). start-time - The date and time the task was created. task-type - The type of task (sip-modification | volume-ownership-delegation)."];
      macModificationTaskIds: MacModificationTaskIdList.t option
        [@ocaml.doc "The ID of task."];
      maxResults: DescribeMacModificationTasksMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?macModificationTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  dryRun;
                  filters;
                  macModificationTaskIds;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MacModificationTaskId",
          (Option.map x.macModificationTaskIds
             ~f:MacModificationTaskIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeMacModificationTasksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeMacModificationTasksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let macModificationTaskIds =
        (Option.map ~f:MacModificationTaskIdList.of_xml)
          (Xml.child xml_arg0 "MacModificationTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?macModificationTaskIds ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeMacModificationTasksMaxResults.of_json in
      let macModificationTaskIds =
        field_map json__ "MacModificationTaskIds"
          MacModificationTaskIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?macModificationTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a System Integrity Protection (SIP) modification task or volume ownership delegation task for an Amazon EC2 Mac instance. For more information, see Configure SIP for Amazon EC2 instances in the Amazon EC2 User Guide."]
module DescribeMacHostsResult =
  struct
    type nonrec t =
      {
      macHosts: MacHostList.t option
        [@ocaml.doc "Information about the EC2 Mac Dedicated Hosts."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?macHosts = fun ?nextToken -> fun () -> { macHosts; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("macHostSet", (Option.map x.macHosts ~f:MacHostList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let macHosts =
        (Option.map ~f:MacHostList.of_xml) (Xml.child xml_arg0 "macHostSet") in
      make ?nextToken ?macHosts ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let macHosts = field_map json__ "MacHosts" MacHostList.of_json in
      make ?nextToken ?macHosts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified EC2 Mac Dedicated Host or all of your EC2 Mac Dedicated Hosts."]
module DescribeMacHostsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The Availability Zone of the EC2 Mac Dedicated Host. instance-type - The instance type size that the EC2 Mac Dedicated Host is configured to support."];
      hostIds: RequestHostIdList.t option
        [@ocaml.doc "The IDs of the EC2 Mac Dedicated Hosts."];
      maxResults: DescribeMacHostsRequestMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."]}
    let make ?filters =
      fun ?hostIds ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { filters; hostIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("HostId", (Option.map x.hostIds ~f:RequestHostIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeMacHostsRequestMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeMacHostsRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let hostIds =
        (Option.map ~f:RequestHostIdList.of_xml)
          (Xml.child xml_arg0 "HostId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?maxResults ?hostIds ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeMacHostsRequestMaxResults.of_json in
      let hostIds = field_map json__ "HostIds" RequestHostIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      make ?nextToken ?maxResults ?hostIds ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified EC2 Mac Dedicated Host or all of your EC2 Mac Dedicated Hosts."]
module DescribeLockedSnapshotsResult =
  struct
    type nonrec t =
      {
      snapshots: LockedSnapshotsInfoList.t option
        [@ocaml.doc "Information about the snapshots."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshots =
      fun ?nextToken -> fun () -> { snapshots; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotSet",
           (Option.map x.snapshots ~f:LockedSnapshotsInfoList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let snapshots =
        (Option.map ~f:LockedSnapshotsInfoList.of_xml)
          (Xml.child xml_arg0 "snapshotSet") in
      make ?nextToken ?snapshots ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let snapshots =
        field_map json__ "Snapshots" LockedSnapshotsInfoList.of_json in
      make ?nextToken ?snapshots ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the lock status for a snapshot."]
module DescribeLockedSnapshotsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. lock-state - The state of the snapshot lock (compliance-cooloff | governance | compliance | expired)."];
      maxResults: DescribeLockedSnapshotsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      snapshotIds: SnapshotIdStringList.t option
        [@ocaml.doc
          "The IDs of the snapshots for which to view the lock status."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?snapshotIds ->
            fun ?dryRun ->
              fun () ->
                { filters; maxResults; nextToken; snapshotIds; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeLockedSnapshotsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("SnapshotId",
          (Option.map x.snapshotIds ~f:SnapshotIdStringList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let snapshotIds =
        (Option.map ~f:SnapshotIdStringList.of_xml)
          (Xml.child xml_arg0 "SnapshotId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeLockedSnapshotsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?snapshotIds ?nextToken ?maxResults ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let snapshotIds =
        field_map json__ "SnapshotIds" SnapshotIdStringList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeLockedSnapshotsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      make ?dryRun ?snapshotIds ?nextToken ?maxResults ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the lock status for a snapshot."]
module DescribeLocalGatewaysResult =
  struct
    type nonrec t =
      {
      localGateways: LocalGatewaySet.t option
        [@ocaml.doc "Information about the local gateways."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGateways =
      fun ?nextToken -> fun () -> { localGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewaySet",
           (Option.map x.localGateways ~f:LocalGatewaySet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGateways =
        (Option.map ~f:LocalGatewaySet.of_xml)
          (Xml.child xml_arg0 "localGatewaySet") in
      make ?nextToken ?localGateways ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let localGateways =
        field_map json__ "LocalGateways" LocalGatewaySet.of_json in
      make ?nextToken ?localGateways ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more local gateways. By default, all local gateways are described. Alternatively, you can filter the results."]
module DescribeLocalGatewaysRequest =
  struct
    type nonrec t =
      {
      localGatewayIds: LocalGatewayIdSet.t option
        [@ocaml.doc "The IDs of the local gateways."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. local-gateway-id - The ID of a local gateway. outpost-arn - The Amazon Resource Name (ARN) of the Outpost. owner-id - The ID of the Amazon Web Services account that owns the local gateway. state - The state of the association."];
      maxResults: LocalGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?localGatewayIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { localGatewayIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("LocalGatewayId",
           (Option.map x.localGatewayIds ~f:LocalGatewayIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayIds =
        (Option.map ~f:LocalGatewayIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let localGatewayIds =
        field_map json__ "LocalGatewayIds" LocalGatewayIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more local gateways. By default, all local gateways are described. Alternatively, you can filter the results."]
module DescribeLocalGatewayVirtualInterfacesResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaces: LocalGatewayVirtualInterfaceSet.t option
        [@ocaml.doc "Information about the virtual interfaces."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterfaces =
      fun ?nextToken ->
        fun () -> { localGatewayVirtualInterfaces; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterfaceSet",
           (Option.map x.localGatewayVirtualInterfaces
              ~f:LocalGatewayVirtualInterfaceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayVirtualInterfaces =
        (Option.map ~f:LocalGatewayVirtualInterfaceSet.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterfaceSet") in
      make ?nextToken ?localGatewayVirtualInterfaces ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let localGatewayVirtualInterfaces =
        field_map json__ "LocalGatewayVirtualInterfaces"
          LocalGatewayVirtualInterfaceSet.of_json in
      make ?nextToken ?localGatewayVirtualInterfaces ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified local gateway virtual interfaces."]
module DescribeLocalGatewayVirtualInterfacesRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceIds:
        LocalGatewayVirtualInterfaceIdSet.t option
        [@ocaml.doc "The IDs of the virtual interfaces."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. local-address - The local address. local-bgp-asn - The Border Gateway Protocol (BGP) Autonomous System Number (ASN) of the local gateway. local-gateway-id - The ID of the local gateway. local-gateway-virtual-interface-id - The ID of the virtual interface. owner-id - The ID of the Amazon Web Services account that owns the local gateway virtual interface. peer-address - The peer address. peer-bgp-asn - The peer BGP ASN. vlan - The ID of the VLAN."];
      maxResults: LocalGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?localGatewayVirtualInterfaceIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayVirtualInterfaceIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceId",
           (Option.map x.localGatewayVirtualInterfaceIds
              ~f:LocalGatewayVirtualInterfaceIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayVirtualInterfaceIds =
        (Option.map ~f:LocalGatewayVirtualInterfaceIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayVirtualInterfaceId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let localGatewayVirtualInterfaceIds =
        field_map json__ "LocalGatewayVirtualInterfaceIds"
          LocalGatewayVirtualInterfaceIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified local gateway virtual interfaces."]
module DescribeLocalGatewayVirtualInterfaceGroupsResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroups:
        LocalGatewayVirtualInterfaceGroupSet.t option
        [@ocaml.doc "The virtual interface groups."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterfaceGroups =
      fun ?nextToken ->
        fun () -> { localGatewayVirtualInterfaceGroups; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterfaceGroupSet",
           (Option.map x.localGatewayVirtualInterfaceGroups
              ~f:LocalGatewayVirtualInterfaceGroupSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayVirtualInterfaceGroups =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroupSet.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterfaceGroupSet") in
      make ?nextToken ?localGatewayVirtualInterfaceGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let localGatewayVirtualInterfaceGroups =
        field_map json__ "LocalGatewayVirtualInterfaceGroups"
          LocalGatewayVirtualInterfaceGroupSet.of_json in
      make ?nextToken ?localGatewayVirtualInterfaceGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified local gateway virtual interface groups."]
module DescribeLocalGatewayVirtualInterfaceGroupsRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroupIds:
        LocalGatewayVirtualInterfaceGroupIdSet.t option
        [@ocaml.doc "The IDs of the virtual interface groups."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. local-gateway-id - The ID of a local gateway. local-gateway-virtual-interface-group-id - The ID of the virtual interface group. local-gateway-virtual-interface-id - The ID of the virtual interface. owner-id - The ID of the Amazon Web Services account that owns the local gateway virtual interface group."];
      maxResults: LocalGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?localGatewayVirtualInterfaceGroupIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayVirtualInterfaceGroupIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceGroupId",
           (Option.map x.localGatewayVirtualInterfaceGroupIds
              ~f:LocalGatewayVirtualInterfaceGroupIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayVirtualInterfaceGroupIds =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroupIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayVirtualInterfaceGroupId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceGroupIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let localGatewayVirtualInterfaceGroupIds =
        field_map json__ "LocalGatewayVirtualInterfaceGroupIds"
          LocalGatewayVirtualInterfaceGroupIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayVirtualInterfaceGroupIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified local gateway virtual interface groups."]
module DescribeLocalGatewayRouteTablesResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTables: LocalGatewayRouteTableSet.t option
        [@ocaml.doc "Information about the local gateway route tables."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTables =
      fun ?nextToken -> fun () -> { localGatewayRouteTables; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableSet",
           (Option.map x.localGatewayRouteTables
              ~f:LocalGatewayRouteTableSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayRouteTables =
        (Option.map ~f:LocalGatewayRouteTableSet.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableSet") in
      make ?nextToken ?localGatewayRouteTables ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let localGatewayRouteTables =
        field_map json__ "LocalGatewayRouteTables"
          LocalGatewayRouteTableSet.of_json in
      make ?nextToken ?localGatewayRouteTables ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more local gateway route tables. By default, all local gateway route tables are described. Alternatively, you can filter the results."]
module DescribeLocalGatewayRouteTablesRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableIds: LocalGatewayRouteTableIdSet.t option
        [@ocaml.doc "The IDs of the local gateway route tables."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. local-gateway-id - The ID of a local gateway. local-gateway-route-table-arn - The Amazon Resource Name (ARN) of the local gateway route table. local-gateway-route-table-id - The ID of a local gateway route table. outpost-arn - The Amazon Resource Name (ARN) of the Outpost. owner-id - The ID of the Amazon Web Services account that owns the local gateway route table. state - The state of the local gateway route table."];
      maxResults: LocalGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?localGatewayRouteTableIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayRouteTableIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Option.map x.localGatewayRouteTableIds
              ~f:LocalGatewayRouteTableIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableIds =
        (Option.map ~f:LocalGatewayRouteTableIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayRouteTableId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayRouteTableIds
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let localGatewayRouteTableIds =
        field_map json__ "LocalGatewayRouteTableIds"
          LocalGatewayRouteTableIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?localGatewayRouteTableIds
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more local gateway route tables. By default, all local gateway route tables are described. Alternatively, you can filter the results."]
module DescribeLocalGatewayRouteTableVpcAssociationsResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociations:
        LocalGatewayRouteTableVpcAssociationSet.t option
        [@ocaml.doc "Information about the associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVpcAssociations =
      fun ?nextToken ->
        fun () -> { localGatewayRouteTableVpcAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVpcAssociationSet",
           (Option.map x.localGatewayRouteTableVpcAssociations
              ~f:LocalGatewayRouteTableVpcAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayRouteTableVpcAssociations =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociationSet.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableVpcAssociationSet") in
      make ?nextToken ?localGatewayRouteTableVpcAssociations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let localGatewayRouteTableVpcAssociations =
        field_map json__ "LocalGatewayRouteTableVpcAssociations"
          LocalGatewayRouteTableVpcAssociationSet.of_json in
      make ?nextToken ?localGatewayRouteTableVpcAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified associations between VPCs and local gateway route tables."]
module DescribeLocalGatewayRouteTableVpcAssociationsRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociationIds:
        LocalGatewayRouteTableVpcAssociationIdSet.t option
        [@ocaml.doc "The IDs of the associations."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. local-gateway-id - The ID of a local gateway. local-gateway-route-table-arn - The Amazon Resource Name (ARN) of the local gateway route table for the association. local-gateway-route-table-id - The ID of the local gateway route table. local-gateway-route-table-vpc-association-id - The ID of the association. owner-id - The ID of the Amazon Web Services account that owns the local gateway route table for the association. state - The state of the association. vpc-id - The ID of the VPC."];
      maxResults: LocalGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?localGatewayRouteTableVpcAssociationIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayRouteTableVpcAssociationIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVpcAssociationId",
           (Option.map x.localGatewayRouteTableVpcAssociationIds
              ~f:LocalGatewayRouteTableVpcAssociationIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableVpcAssociationIds =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociationIdSet.of_xml)
          (Xml.child xml_arg0 "LocalGatewayRouteTableVpcAssociationId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVpcAssociationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let localGatewayRouteTableVpcAssociationIds =
        field_map json__ "LocalGatewayRouteTableVpcAssociationIds"
          LocalGatewayRouteTableVpcAssociationIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVpcAssociationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified associations between VPCs and local gateway route tables."]
module DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociations:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.t option
        [@ocaml.doc "Information about the associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVirtualInterfaceGroupAssociations =
      fun ?nextToken ->
        fun () ->
          {
            localGatewayRouteTableVirtualInterfaceGroupAssociations;
            nextToken
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVirtualInterfaceGroupAssociationSet",
           (Option.map
              x.localGatewayRouteTableVirtualInterfaceGroupAssociations
              ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let localGatewayRouteTableVirtualInterfaceGroupAssociations =
        (Option.map
           ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.of_xml)
          (Xml.child xml_arg0
             "localGatewayRouteTableVirtualInterfaceGroupAssociationSet") in
      make ?nextToken
        ?localGatewayRouteTableVirtualInterfaceGroupAssociations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let localGatewayRouteTableVirtualInterfaceGroupAssociations =
        field_map json__
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociations"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociationSet.of_json in
      make ?nextToken
        ?localGatewayRouteTableVirtualInterfaceGroupAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the associations between virtual interface groups and local gateway route tables."]
module DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociationIds:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.t option
        [@ocaml.doc "The IDs of the associations."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. local-gateway-id - The ID of a local gateway. local-gateway-route-table-arn - The Amazon Resource Name (ARN) of the local gateway route table for the virtual interface group. local-gateway-route-table-id - The ID of the local gateway route table. local-gateway-route-table-virtual-interface-group-association-id - The ID of the association. local-gateway-route-table-virtual-interface-group-id - The ID of the virtual interface group. owner-id - The ID of the Amazon Web Services account that owns the local gateway virtual interface group association. state - The state of the association."];
      maxResults: LocalGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  localGatewayRouteTableVirtualInterfaceGroupAssociationIds;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVirtualInterfaceGroupAssociationId",
           (Option.map
              x.localGatewayRouteTableVirtualInterfaceGroupAssociationIds
              ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:LocalGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:LocalGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
        (Option.map
           ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.of_xml)
          (Xml.child xml_arg0
             "LocalGatewayRouteTableVirtualInterfaceGroupAssociationId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" LocalGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
        field_map json__
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociationIds"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the associations between virtual interface groups and local gateway route tables."]
module DescribeLaunchTemplatesResult =
  struct
    type nonrec t =
      {
      launchTemplates: LaunchTemplateSet.t option
        [@ocaml.doc "Information about the launch templates."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplates =
      fun ?nextToken -> fun () -> { launchTemplates; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplates",
           (Option.map x.launchTemplates ~f:LaunchTemplateSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let launchTemplates =
        (Option.map ~f:LaunchTemplateSet.of_xml)
          (Xml.child xml_arg0 "launchTemplates") in
      make ?nextToken ?launchTemplates ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let launchTemplates =
        field_map json__ "LaunchTemplates" LaunchTemplateSet.of_json in
      make ?nextToken ?launchTemplates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more launch templates."]
module DescribeLaunchTemplatesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      launchTemplateIds: LaunchTemplateIdStringList.t option
        [@ocaml.doc "One or more launch template IDs."];
      launchTemplateNames: LaunchTemplateNameStringList.t option
        [@ocaml.doc "One or more launch template names."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. create-time - The time the launch template was created. launch-template-name - The name of the launch template. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      nextToken: String_.t option
        [@ocaml.doc "The token to request the next page of results."];
      maxResults: DescribeLaunchTemplatesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned NextToken value. This value can be between 1 and 200."];
      includeManagedResources: Boolean.t option
        [@ocaml.doc
          "Indicates whether to include managed resources in the output. If this parameter is set to true, the output includes resources that are managed by Amazon Web Services services, even if managed resource visibility is set to hidden."]}
    let make ?dryRun =
      fun ?launchTemplateIds ->
        fun ?launchTemplateNames ->
          fun ?filters ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun ?includeManagedResources ->
                  fun () ->
                    {
                      dryRun;
                      launchTemplateIds;
                      launchTemplateNames;
                      filters;
                      nextToken;
                      maxResults;
                      includeManagedResources
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateIds
             ~f:LaunchTemplateIdStringList.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateNames
             ~f:LaunchTemplateNameStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeLaunchTemplatesMaxResults.to_value));
        ("IncludeManagedResources",
          (Option.map x.includeManagedResources ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeManagedResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeManagedResources") in
      let maxResults =
        (Option.map ~f:DescribeLaunchTemplatesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let launchTemplateNames =
        (Option.map ~f:LaunchTemplateNameStringList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateIds =
        (Option.map ~f:LaunchTemplateIdStringList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?includeManagedResources ?maxResults ?nextToken ?filters
        ?launchTemplateNames ?launchTemplateIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let includeManagedResources =
        field_map json__ "IncludeManagedResources" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeLaunchTemplatesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let launchTemplateNames =
        field_map json__ "LaunchTemplateNames"
          LaunchTemplateNameStringList.of_json in
      let launchTemplateIds =
        field_map json__ "LaunchTemplateIds"
          LaunchTemplateIdStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?includeManagedResources ?maxResults ?nextToken ?filters
        ?launchTemplateNames ?launchTemplateIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more launch templates."]
module DescribeLaunchTemplateVersionsResult =
  struct
    type nonrec t =
      {
      launchTemplateVersions: LaunchTemplateVersionSet.t option
        [@ocaml.doc "Information about the launch template versions."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplateVersions =
      fun ?nextToken -> fun () -> { launchTemplateVersions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplateVersionSet",
           (Option.map x.launchTemplateVersions
              ~f:LaunchTemplateVersionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let launchTemplateVersions =
        (Option.map ~f:LaunchTemplateVersionSet.of_xml)
          (Xml.child xml_arg0 "launchTemplateVersionSet") in
      make ?nextToken ?launchTemplateVersions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let launchTemplateVersions =
        field_map json__ "LaunchTemplateVersions"
          LaunchTemplateVersionSet.of_json in
      make ?nextToken ?launchTemplateVersions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more versions of a specified launch template. You can describe all versions, individual versions, or a range of versions. You can also describe all the latest versions or all the default versions of all the launch templates in your account."]
module DescribeLaunchTemplateVersionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      launchTemplateId: LaunchTemplateId.t option
        [@ocaml.doc
          "The ID of the launch template. To describe one or more versions of a specified launch template, you must specify either the launch template ID or the launch template name, but not both. To describe all the latest or default launch template versions in your account, you must omit this parameter."];
      launchTemplateName: LaunchTemplateName.t option
        [@ocaml.doc
          "The name of the launch template. To describe one or more versions of a specified launch template, you must specify either the launch template name or the launch template ID, but not both. To describe all the latest or default launch template versions in your account, you must omit this parameter."];
      versions: VersionStringList.t option
        [@ocaml.doc
          "One or more versions of the launch template. Valid values depend on whether you are describing a specified launch template (by ID or name) or all launch templates in your account. To describe one or more versions of a specified launch template, valid values are $Latest, $Default, and numbers. To describe all launch templates in your account that are defined as the latest version, the valid value is $Latest. To describe all launch templates in your account that are defined as the default version, the valid value is $Default. You can specify $Latest and $Default in the same request. You cannot specify numbers."];
      minVersion: String_.t option
        [@ocaml.doc
          "The version number after which to describe launch template versions."];
      maxVersion: String_.t option
        [@ocaml.doc
          "The version number up to which to describe launch template versions."];
      nextToken: String_.t option
        [@ocaml.doc "The token to request the next page of results."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned NextToken value. This value can be between 1 and 200."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. create-time - The time the launch template version was created. ebs-optimized - A boolean that indicates whether the instance is optimized for Amazon EBS I/O. http-endpoint - Indicates whether the HTTP metadata endpoint on your instances is enabled (enabled | disabled). http-protocol-ipv4 - Indicates whether the IPv4 endpoint for the instance metadata service is enabled (enabled | disabled). host-resource-group-arn - The ARN of the host resource group in which to launch the instances. http-tokens - The state of token usage for your instance metadata requests (optional | required). iam-instance-profile - The ARN of the IAM instance profile. image-id - The ID of the AMI. instance-type - The instance type. is-default-version - A boolean that indicates whether the launch template version is the default version. kernel-id - The kernel ID. license-configuration-arn - The ARN of the license configuration. network-card-index - The index of the network card. ram-disk-id - The RAM disk ID."];
      resolveAlias: Boolean.t option
        [@ocaml.doc
          "If true, and if a Systems Manager parameter is specified for ImageId, the AMI ID is displayed in the response for imageId. If false, and if a Systems Manager parameter is specified for ImageId, the parameter is displayed in the response for imageId. For more information, see Use a Systems Manager parameter instead of an AMI ID in the Amazon EC2 User Guide. Default: false"];
      includeManagedResources: Boolean.t option
        [@ocaml.doc
          "Indicates whether to include managed resources in the output. If this parameter is set to true, the output includes resources that are managed by Amazon Web Services services, even if managed resource visibility is set to hidden."]}
    let make ?dryRun =
      fun ?launchTemplateId ->
        fun ?launchTemplateName ->
          fun ?versions ->
            fun ?minVersion ->
              fun ?maxVersion ->
                fun ?nextToken ->
                  fun ?maxResults ->
                    fun ?filters ->
                      fun ?resolveAlias ->
                        fun ?includeManagedResources ->
                          fun () ->
                            {
                              dryRun;
                              launchTemplateId;
                              launchTemplateName;
                              versions;
                              minVersion;
                              maxVersion;
                              nextToken;
                              maxResults;
                              filters;
                              resolveAlias;
                              includeManagedResources
                            }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("LaunchTemplateVersion",
          (Option.map x.versions ~f:VersionStringList.to_value));
        ("MinVersion", (Option.map x.minVersion ~f:String_.to_value));
        ("MaxVersion", (Option.map x.maxVersion ~f:String_.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("ResolveAlias", (Option.map x.resolveAlias ~f:Boolean.to_value));
        ("IncludeManagedResources",
          (Option.map x.includeManagedResources ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeManagedResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeManagedResources") in
      let resolveAlias =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ResolveAlias") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MaxVersion") in
      let minVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MinVersion") in
      let versions =
        (Option.map ~f:VersionStringList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?includeManagedResources ?resolveAlias ?filters ?maxResults
        ?nextToken ?maxVersion ?minVersion ?versions ?launchTemplateName
        ?launchTemplateId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let includeManagedResources =
        field_map json__ "IncludeManagedResources" Boolean.of_json in
      let resolveAlias = field_map json__ "ResolveAlias" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxVersion = field_map json__ "MaxVersion" String_.of_json in
      let minVersion = field_map json__ "MinVersion" String_.of_json in
      let versions = field_map json__ "Versions" VersionStringList.of_json in
      let launchTemplateName =
        field_map json__ "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json__ "LaunchTemplateId" LaunchTemplateId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?includeManagedResources ?resolveAlias ?filters ?maxResults
        ?nextToken ?maxVersion ?minVersion ?versions ?launchTemplateName
        ?launchTemplateId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more versions of a specified launch template. You can describe all versions, individual versions, or a range of versions. You can also describe all the latest versions or all the default versions of all the launch templates in your account."]
module DescribeKeyPairsResult =
  struct
    type nonrec t =
      {
      keyPairs: KeyPairList.t option
        [@ocaml.doc "Information about the key pairs."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?keyPairs = fun () -> { keyPairs }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keySet", (Option.map x.keyPairs ~f:KeyPairList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyPairs =
        (Option.map ~f:KeyPairList.of_xml) (Xml.child xml_arg0 "keySet") in
      make ?keyPairs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let keyPairs = field_map json__ "KeyPairs" KeyPairList.of_json in
      make ?keyPairs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified key pairs or all of your key pairs. For more information about key pairs, see Amazon EC2 key pairs in the Amazon EC2 User Guide."]
module DescribeKeyPairsRequest =
  struct
    type nonrec t =
      {
      keyNames: KeyNameStringList.t option
        [@ocaml.doc
          "The key pair names. Default: Describes all of your key pairs."];
      keyPairIds: KeyPairIdStringList.t option
        [@ocaml.doc "The IDs of the key pairs."];
      includePublicKey: Boolean.t option
        [@ocaml.doc
          "If true, the public key material is included in the response. Default: false"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. key-pair-id - The ID of the key pair. fingerprint - The fingerprint of the key pair. key-name - The name of the key pair. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."]}
    let make ?keyNames =
      fun ?keyPairIds ->
        fun ?includePublicKey ->
          fun ?dryRun ->
            fun ?filters ->
              fun () ->
                { keyNames; keyPairIds; includePublicKey; dryRun; filters }
    let to_value x =
      structure_to_value
        [("KeyName", (Option.map x.keyNames ~f:KeyNameStringList.to_value));
        ("KeyPairId",
          (Option.map x.keyPairIds ~f:KeyPairIdStringList.to_value));
        ("IncludePublicKey",
          (Option.map x.includePublicKey ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let includePublicKey =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludePublicKey") in
      let keyPairIds =
        (Option.map ~f:KeyPairIdStringList.of_xml)
          (Xml.child xml_arg0 "KeyPairId") in
      let keyNames =
        (Option.map ~f:KeyNameStringList.of_xml)
          (Xml.child xml_arg0 "KeyName") in
      make ?filters ?dryRun ?includePublicKey ?keyPairIds ?keyNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let includePublicKey =
        field_map json__ "IncludePublicKey" Boolean.of_json in
      let keyPairIds =
        field_map json__ "KeyPairIds" KeyPairIdStringList.of_json in
      let keyNames = field_map json__ "KeyNames" KeyNameStringList.of_json in
      make ?filters ?dryRun ?includePublicKey ?keyPairIds ?keyNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified key pairs or all of your key pairs. For more information about key pairs, see Amazon EC2 key pairs in the Amazon EC2 User Guide."]
module DescribeIpv6PoolsResult =
  struct
    type nonrec t =
      {
      ipv6Pools: Ipv6PoolSet.t option
        [@ocaml.doc "Information about the IPv6 address pools."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6Pools =
      fun ?nextToken -> fun () -> { ipv6Pools; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6PoolSet", (Option.map x.ipv6Pools ~f:Ipv6PoolSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipv6Pools =
        (Option.map ~f:Ipv6PoolSet.of_xml) (Xml.child xml_arg0 "ipv6PoolSet") in
      make ?nextToken ?ipv6Pools ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let ipv6Pools = field_map json__ "Ipv6Pools" Ipv6PoolSet.of_json in
      make ?nextToken ?ipv6Pools ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes your IPv6 address pools."]
module DescribeIpv6PoolsRequest =
  struct
    type nonrec t =
      {
      poolIds: Ipv6PoolIdList.t option
        [@ocaml.doc "The IDs of the IPv6 address pools."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: Ipv6PoolMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."]}
    let make ?poolIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?filters ->
              fun () -> { poolIds; nextToken; maxResults; dryRun; filters }
    let to_value x =
      structure_to_value
        [("PoolId", (Option.map x.poolIds ~f:Ipv6PoolIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:Ipv6PoolMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:Ipv6PoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let poolIds =
        (Option.map ~f:Ipv6PoolIdList.of_xml) (Xml.child xml_arg0 "PoolId") in
      make ?filters ?dryRun ?maxResults ?nextToken ?poolIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" Ipv6PoolMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let poolIds = field_map json__ "PoolIds" Ipv6PoolIdList.of_json in
      make ?filters ?dryRun ?maxResults ?nextToken ?poolIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes your IPv6 address pools."]
module DescribeIpamsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipams: IpamSet.t option [@ocaml.doc "Information about the IPAMs."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?ipams -> fun () -> { nextToken; ipams }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamSet", (Option.map x.ipams ~f:IpamSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipams =
        (Option.map ~f:IpamSet.of_xml) (Xml.child xml_arg0 "ipamSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipams ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipams = field_map json__ "Ipams" IpamSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipams ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Get information about your IPAM pools. For more information, see What is IPAM? in the Amazon VPC IPAM User Guide."]
module DescribeIpamsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the request."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamIds: ValueStringList.t option
        [@ocaml.doc "The IDs of the IPAMs you want information on."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamIds ->
              fun () -> { dryRun; filters; maxResults; nextToken; ipamIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamId", (Option.map x.ipamIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamIds =
        (Option.map ~f:ValueStringList.of_xml) (Xml.child xml_arg0 "IpamId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamIds = field_map json__ "IpamIds" ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Get information about your IPAM pools. For more information, see What is IPAM? in the Amazon VPC IPAM User Guide."]
module DescribeIpamScopesResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamScopes: IpamScopeSet.t option
        [@ocaml.doc "The scopes you want information on."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamScopes -> fun () -> { nextToken; ipamScopes }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamScopeSet", (Option.map x.ipamScopes ~f:IpamScopeSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScopes =
        (Option.map ~f:IpamScopeSet.of_xml)
          (Xml.child xml_arg0 "ipamScopeSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamScopes ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamScopes = field_map json__ "IpamScopes" IpamScopeSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipamScopes ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get information about your IPAM scopes."]
module DescribeIpamScopesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the request."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamScopeIds: ValueStringList.t option
        [@ocaml.doc "The IDs of the scopes you want information on."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamScopeIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; ipamScopeIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamScopeId",
          (Option.map x.ipamScopeIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScopeIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamScopeId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamScopeIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamScopeIds =
        field_map json__ "IpamScopeIds" ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamScopeIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get information about your IPAM scopes."]
module DescribeIpamResourceDiscoveryAssociationsResult =
  struct
    type nonrec t =
      {
      ipamResourceDiscoveryAssociations:
        IpamResourceDiscoveryAssociationSet.t option
        [@ocaml.doc "The resource discovery associations."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamResourceDiscoveryAssociations =
      fun ?nextToken ->
        fun () -> { ipamResourceDiscoveryAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamResourceDiscoveryAssociationSet",
           (Option.map x.ipamResourceDiscoveryAssociations
              ~f:IpamResourceDiscoveryAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipamResourceDiscoveryAssociations =
        (Option.map ~f:IpamResourceDiscoveryAssociationSet.of_xml)
          (Xml.child xml_arg0 "ipamResourceDiscoveryAssociationSet") in
      make ?nextToken ?ipamResourceDiscoveryAssociations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let ipamResourceDiscoveryAssociations =
        field_map json__ "IpamResourceDiscoveryAssociations"
          IpamResourceDiscoveryAssociationSet.of_json in
      make ?nextToken ?ipamResourceDiscoveryAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes resource discovery association with an Amazon VPC IPAM. An associated resource discovery is a resource discovery that has been associated with an IPAM.."]
module DescribeIpamResourceDiscoveryAssociationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamResourceDiscoveryAssociationIds: ValueStringList.t option
        [@ocaml.doc "The resource discovery association IDs."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of resource discovery associations to return in one page of results."];
      filters: FilterList.t option
        [@ocaml.doc "The resource discovery association filters."]}
    let make ?dryRun =
      fun ?ipamResourceDiscoveryAssociationIds ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?filters ->
              fun () ->
                {
                  dryRun;
                  ipamResourceDiscoveryAssociationIds;
                  nextToken;
                  maxResults;
                  filters
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamResourceDiscoveryAssociationId",
          (Option.map x.ipamResourceDiscoveryAssociationIds
             ~f:ValueStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let ipamResourceDiscoveryAssociationIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamResourceDiscoveryAssociationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?maxResults ?nextToken
        ?ipamResourceDiscoveryAssociationIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let ipamResourceDiscoveryAssociationIds =
        field_map json__ "IpamResourceDiscoveryAssociationIds"
          ValueStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ?maxResults ?nextToken
        ?ipamResourceDiscoveryAssociationIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes resource discovery association with an Amazon VPC IPAM. An associated resource discovery is a resource discovery that has been associated with an IPAM.."]
module DescribeIpamResourceDiscoveriesResult =
  struct
    type nonrec t =
      {
      ipamResourceDiscoveries: IpamResourceDiscoverySet.t option
        [@ocaml.doc "The resource discoveries."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamResourceDiscoveries =
      fun ?nextToken -> fun () -> { ipamResourceDiscoveries; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamResourceDiscoverySet",
           (Option.map x.ipamResourceDiscoveries
              ~f:IpamResourceDiscoverySet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipamResourceDiscoveries =
        (Option.map ~f:IpamResourceDiscoverySet.of_xml)
          (Xml.child xml_arg0 "ipamResourceDiscoverySet") in
      make ?nextToken ?ipamResourceDiscoveries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let ipamResourceDiscoveries =
        field_map json__ "IpamResourceDiscoveries"
          IpamResourceDiscoverySet.of_json in
      make ?nextToken ?ipamResourceDiscoveries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes IPAM resource discoveries. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module DescribeIpamResourceDiscoveriesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamResourceDiscoveryIds: ValueStringList.t option
        [@ocaml.doc "The IPAM resource discovery IDs."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of resource discoveries to return in one page of results."];
      filters: FilterList.t option
        [@ocaml.doc "The resource discovery filters."]}
    let make ?dryRun =
      fun ?ipamResourceDiscoveryIds ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?filters ->
              fun () ->
                {
                  dryRun;
                  ipamResourceDiscoveryIds;
                  nextToken;
                  maxResults;
                  filters
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamResourceDiscoveryId",
          (Option.map x.ipamResourceDiscoveryIds ~f:ValueStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let ipamResourceDiscoveryIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamResourceDiscoveryId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?maxResults ?nextToken ?ipamResourceDiscoveryIds ?dryRun
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let ipamResourceDiscoveryIds =
        field_map json__ "IpamResourceDiscoveryIds" ValueStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ?maxResults ?nextToken ?ipamResourceDiscoveryIds ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes IPAM resource discoveries. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module DescribeIpamPrefixListResolversResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamPrefixListResolvers: IpamPrefixListResolverSet.t option
        [@ocaml.doc "Information about the IPAM prefix list resolvers."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamPrefixListResolvers ->
        fun () -> { nextToken; ipamPrefixListResolvers }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamPrefixListResolverSet",
          (Option.map x.ipamPrefixListResolvers
             ~f:IpamPrefixListResolverSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolvers =
        (Option.map ~f:IpamPrefixListResolverSet.of_xml)
          (Xml.child xml_arg0 "ipamPrefixListResolverSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamPrefixListResolvers ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolvers =
        field_map json__ "IpamPrefixListResolvers"
          IpamPrefixListResolverSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipamPrefixListResolvers ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more IPAM prefix list resolvers. Use this operation to view the configuration, status, and properties of your resolvers."]
module DescribeIpamPrefixListResolversRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc "One or more filters to limit the results."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamPrefixListResolverIds: ValueStringList.t option
        [@ocaml.doc
          "The IDs of the IPAM prefix list resolvers to describe. If not specified, all resolvers in your account are described."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamPrefixListResolverIds ->
              fun () ->
                {
                  dryRun;
                  filters;
                  maxResults;
                  nextToken;
                  ipamPrefixListResolverIds
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamPrefixListResolverId",
          (Option.map x.ipamPrefixListResolverIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamPrefixListResolverId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamPrefixListResolverIds ?nextToken ?maxResults ?filters ?dryRun
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverIds =
        field_map json__ "IpamPrefixListResolverIds" ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamPrefixListResolverIds ?nextToken ?maxResults ?filters ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more IPAM prefix list resolvers. Use this operation to view the configuration, status, and properties of your resolvers."]
module DescribeIpamPrefixListResolverTargetsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamPrefixListResolverTargets: IpamPrefixListResolverTargetSet.t option
        [@ocaml.doc
          "Information about the IPAM prefix list resolver Targets."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamPrefixListResolverTargets ->
        fun () -> { nextToken; ipamPrefixListResolverTargets }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamPrefixListResolverTargetSet",
          (Option.map x.ipamPrefixListResolverTargets
             ~f:IpamPrefixListResolverTargetSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverTargets =
        (Option.map ~f:IpamPrefixListResolverTargetSet.of_xml)
          (Xml.child xml_arg0 "ipamPrefixListResolverTargetSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamPrefixListResolverTargets ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverTargets =
        field_map json__ "IpamPrefixListResolverTargets"
          IpamPrefixListResolverTargetSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipamPrefixListResolverTargets ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more IPAM prefix list resolver Targets. Use this operation to view the configuration and status of resolver targets."]
module DescribeIpamPrefixListResolverTargetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc "One or more filters to limit the results."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamPrefixListResolverTargetIds: ValueStringList.t option
        [@ocaml.doc
          "The IDs of the IPAM prefix list resolver Targets to describe. If not specified, all targets in your account are described."];
      ipamPrefixListResolverId: IpamPrefixListResolverId.t option
        [@ocaml.doc
          "The ID of the IPAM prefix list resolver to filter targets by. Only targets associated with this resolver will be returned."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamPrefixListResolverTargetIds ->
              fun ?ipamPrefixListResolverId ->
                fun () ->
                  {
                    dryRun;
                    filters;
                    maxResults;
                    nextToken;
                    ipamPrefixListResolverTargetIds;
                    ipamPrefixListResolverId
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamPrefixListResolverTargetId",
          (Option.map x.ipamPrefixListResolverTargetIds
             ~f:ValueStringList.to_value));
        ("IpamPrefixListResolverId",
          (Option.map x.ipamPrefixListResolverId
             ~f:IpamPrefixListResolverId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverId =
        (Option.map ~f:IpamPrefixListResolverId.of_xml)
          (Xml.child xml_arg0 "IpamPrefixListResolverId") in
      let ipamPrefixListResolverTargetIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamPrefixListResolverTargetId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamPrefixListResolverId ?ipamPrefixListResolverTargetIds
        ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverId =
        field_map json__ "IpamPrefixListResolverId"
          IpamPrefixListResolverId.of_json in
      let ipamPrefixListResolverTargetIds =
        field_map json__ "IpamPrefixListResolverTargetIds"
          ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamPrefixListResolverId ?ipamPrefixListResolverTargetIds
        ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more IPAM prefix list resolver Targets. Use this operation to view the configuration and status of resolver targets."]
module DescribeIpamPoolsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamPools: IpamPoolSet.t option
        [@ocaml.doc "Information about the IPAM pools."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamPools -> fun () -> { nextToken; ipamPools }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamPoolSet", (Option.map x.ipamPools ~f:IpamPoolSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPools =
        (Option.map ~f:IpamPoolSet.of_xml) (Xml.child xml_arg0 "ipamPoolSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamPools ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPools = field_map json__ "IpamPools" IpamPoolSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipamPools ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get information about your IPAM pools."]
module DescribeIpamPoolsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the request."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamPoolIds: ValueStringList.t option
        [@ocaml.doc
          "The IDs of the IPAM pools you would like information on."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamPoolIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; ipamPoolIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamPoolId",
          (Option.map x.ipamPoolIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamPoolId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamPoolIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPoolIds =
        field_map json__ "IpamPoolIds" ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamPoolIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get information about your IPAM pools."]
module DescribeIpamPoliciesResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamPolicies: IpamPolicySet.t option
        [@ocaml.doc
          "Information about the IPAM policies. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamPolicies -> fun () -> { nextToken; ipamPolicies }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamPolicySet",
          (Option.map x.ipamPolicies ~f:IpamPolicySet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPolicies =
        (Option.map ~f:IpamPolicySet.of_xml)
          (Xml.child xml_arg0 "ipamPolicySet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamPolicies ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPolicies =
        field_map json__ "IpamPolicies" IpamPolicySet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipamPolicies ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more IPAM policies. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]
module DescribeIpamPoliciesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc "One or more filters for the IPAM policy description."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamPolicyIds: ValueStringList.t option
        [@ocaml.doc "The IDs of the IPAM policies to describe."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamPolicyIds ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; ipamPolicyIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamPolicyId",
          (Option.map x.ipamPolicyIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPolicyIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamPolicyId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamPolicyIds ?nextToken ?maxResults ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPolicyIds =
        field_map json__ "IpamPolicyIds" ValueStringList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamPolicyIds ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more IPAM policies. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]
module DescribeIpamExternalResourceVerificationTokensResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamExternalResourceVerificationTokens:
        IpamExternalResourceVerificationTokenSet.t option
        [@ocaml.doc "Verification tokens."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamExternalResourceVerificationTokens ->
        fun () -> { nextToken; ipamExternalResourceVerificationTokens }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamExternalResourceVerificationTokenSet",
          (Option.map x.ipamExternalResourceVerificationTokens
             ~f:IpamExternalResourceVerificationTokenSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamExternalResourceVerificationTokens =
        (Option.map ~f:IpamExternalResourceVerificationTokenSet.of_xml)
          (Xml.child xml_arg0 "ipamExternalResourceVerificationTokenSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamExternalResourceVerificationTokens ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamExternalResourceVerificationTokens =
        field_map json__ "IpamExternalResourceVerificationTokens"
          IpamExternalResourceVerificationTokenSet.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?ipamExternalResourceVerificationTokens ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describe verification tokens. A verification token is an Amazon Web Services-generated random value that you can use to prove ownership of an external resource. For example, you can use a verification token to validate that you control a public IP address range when you bring an IP address range to Amazon Web Services (BYOIP)."]
module DescribeIpamExternalResourceVerificationTokensRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output. Available filters: ipam-arn ipam-external-resource-verification-token-arn ipam-external-resource-verification-token-id ipam-id ipam-region state status token-name token-value"];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of tokens to return in one page of results."];
      ipamExternalResourceVerificationTokenIds: ValueStringList.t option
        [@ocaml.doc "Verification token IDs."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?ipamExternalResourceVerificationTokenIds ->
              fun () ->
                {
                  dryRun;
                  filters;
                  nextToken;
                  maxResults;
                  ipamExternalResourceVerificationTokenIds
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("IpamExternalResourceVerificationTokenId",
          (Option.map x.ipamExternalResourceVerificationTokenIds
             ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamExternalResourceVerificationTokenIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "IpamExternalResourceVerificationTokenId") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipamExternalResourceVerificationTokenIds ?maxResults ?nextToken
        ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamExternalResourceVerificationTokenIds =
        field_map json__ "IpamExternalResourceVerificationTokenIds"
          ValueStringList.of_json in
      let maxResults = field_map json__ "MaxResults" IpamMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipamExternalResourceVerificationTokenIds ?maxResults ?nextToken
        ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describe verification tokens. A verification token is an Amazon Web Services-generated random value that you can use to prove ownership of an external resource. For example, you can use a verification token to validate that you control a public IP address range when you bring an IP address range to Amazon Web Services (BYOIP)."]
module DescribeIpamByoasnResult =
  struct
    type nonrec t =
      {
      byoasns: ByoasnSet.t option
        [@ocaml.doc "ASN and BYOIP CIDR associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoasns = fun ?nextToken -> fun () -> { byoasns; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoasnSet", (Option.map x.byoasns ~f:ByoasnSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let byoasns =
        (Option.map ~f:ByoasnSet.of_xml) (Xml.child xml_arg0 "byoasnSet") in
      make ?nextToken ?byoasns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let byoasns = field_map json__ "Byoasns" ByoasnSet.of_json in
      make ?nextToken ?byoasns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your Autonomous System Numbers (ASNs), their provisioning statuses, and the BYOIP CIDRs with which they are associated. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide."]
module DescribeIpamByoasnRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: DescribeIpamByoasnMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { dryRun; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeIpamByoasnMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeIpamByoasnMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" DescribeIpamByoasnMaxResults.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your Autonomous System Numbers (ASNs), their provisioning statuses, and the BYOIP CIDRs with which they are associated. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide."]
module DescribeInternetGatewaysResult =
  struct
    type nonrec t =
      {
      internetGateways: InternetGatewayList.t option
        [@ocaml.doc "Information about the internet gateways."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?internetGateways =
      fun ?nextToken -> fun () -> { internetGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("internetGatewaySet",
           (Option.map x.internetGateways ~f:InternetGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let internetGateways =
        (Option.map ~f:InternetGatewayList.of_xml)
          (Xml.child xml_arg0 "internetGatewaySet") in
      make ?nextToken ?internetGateways ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let internetGateways =
        field_map json__ "InternetGateways" InternetGatewayList.of_json in
      make ?nextToken ?internetGateways ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your internet gateways. The default is to describe all your internet gateways. Alternatively, you can specify specific internet gateway IDs or filter the results to include only the internet gateways that match specific criteria."]
module DescribeInternetGatewaysRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeInternetGatewaysMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      internetGatewayIds: InternetGatewayIdList.t option
        [@ocaml.doc
          "The IDs of the internet gateways. Default: Describes all your internet gateways."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. attachment.state - The current state of the attachment between the gateway and the VPC (available). Present only if a VPC is attached. attachment.vpc-id - The ID of an attached VPC. internet-gateway-id - The ID of the Internet gateway. owner-id - The ID of the Amazon Web Services account that owns the internet gateway. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?dryRun ->
          fun ?internetGatewayIds ->
            fun ?filters ->
              fun () ->
                { nextToken; maxResults; dryRun; internetGatewayIds; filters
                }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInternetGatewaysMaxResults.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Option.map x.internetGatewayIds ~f:InternetGatewayIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let internetGatewayIds =
        (Option.map ~f:InternetGatewayIdList.of_xml)
          (Xml.child xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let maxResults =
        (Option.map ~f:DescribeInternetGatewaysMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?filters ?internetGatewayIds ?dryRun ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let internetGatewayIds =
        field_map json__ "InternetGatewayIds" InternetGatewayIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeInternetGatewaysMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?filters ?internetGatewayIds ?dryRun ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your internet gateways. The default is to describe all your internet gateways. Alternatively, you can specify specific internet gateway IDs or filter the results to include only the internet gateways that match specific criteria."]
module DescribeInstancesResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      reservations: ReservationList.t option
        [@ocaml.doc "Information about the reservations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?reservations -> fun () -> { nextToken; reservations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reservationSet",
          (Option.map x.reservations ~f:ReservationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservations =
        (Option.map ~f:ReservationList.of_xml)
          (Xml.child xml_arg0 "reservationSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reservations ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservations =
        field_map json__ "Reservations" ReservationList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?reservations ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified instances or all instances. If you specify instance IDs, the output includes information for only the specified instances. If you specify filters, the output includes information for only those instances that meet the filter criteria. If you do not specify instance IDs or filters, the output includes information for all instances, which can affect performance. We recommend that you use pagination to ensure that the operation returns quickly and successfully. The response includes SQL license exemption status information for instances registered with the SQL LE service, providing visibility into license exemption configuration and status. If you specify an instance ID that is not valid, an error is returned. If you specify an instance that you do not own, it is not included in the output. Recently terminated instances might appear in the returned results. This interval is usually less than one hour. If you describe instances in the rare case where an Availability Zone is experiencing a service disruption and you specify instance IDs that are in the affected zone, or do not specify any instance IDs at all, the call fails. If you describe instances and specify only instance IDs that are in an unaffected zone, the call works normally. The Amazon EC2 API follows an eventual consistency model. This means that the result of an API command you run that creates or modifies resources might not be immediately available to all subsequent commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. We strongly recommend using only paginated requests. Unpaginated requests are susceptible to throttling and timeouts. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The instance IDs. Default: Describes all your instances."];
      includeManagedResources: Boolean.t option
        [@ocaml.doc
          "Indicates whether to include managed resources in the output. If this parameter is set to true, the output includes resources that are managed by Amazon Web Services services, even if managed resource visibility is set to hidden."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. affinity - The affinity setting for an instance running on a Dedicated Host (default | host). architecture - The instance architecture (i386 | x86_64 | arm64). availability-zone - The Availability Zone of the instance. availability-zone-id - The ID of the Availability Zone of the instance. block-device-mapping.attach-time - The attach time for an EBS volume mapped to the instance, for example, 2022-09-15T17:15:20.000Z. block-device-mapping.delete-on-termination - A Boolean that indicates whether the EBS volume is deleted on instance termination. block-device-mapping.device-name - The device name specified in the block device mapping (for example, /dev/sdh or xvdh). block-device-mapping.status - The status for the EBS volume (attaching | attached | detaching | detached). block-device-mapping.volume-id - The volume ID of the EBS volume. boot-mode - The boot mode that was specified by the AMI (legacy-bios | uefi | uefi-preferred). capacity-reservation-id - The ID of the Capacity Reservation into which the instance was launched. capacity-reservation-specification.capacity-reservation-preference - The instance's Capacity Reservation preference (open | none). capacity-reservation-specification.capacity-reservation-target.capacity-reservation-id - The ID of the targeted Capacity Reservation. capacity-reservation-specification.capacity-reservation-target.capacity-reservation-resource-group-arn - The ARN of the targeted Capacity Reservation group. client-token - The idempotency token you provided when you launched the instance. current-instance-boot-mode - The boot mode that is used to launch the instance at launch or start (legacy-bios | uefi). dns-name - The public DNS name of the instance. ebs-optimized - A Boolean that indicates whether the instance is optimized for Amazon EBS I/O. ena-support - A Boolean that indicates whether the instance is enabled for enhanced networking with ENA. enclave-options.enabled - A Boolean that indicates whether the instance is enabled for Amazon Web Services Nitro Enclaves. hibernation-options.configured - A Boolean that indicates whether the instance is enabled for hibernation. A value of true means that the instance is enabled for hibernation. host-id - The ID of the Dedicated Host on which the instance is running, if applicable. hypervisor - The hypervisor type of the instance (ovm | xen). The value xen is used for both Xen and Nitro hypervisors. iam-instance-profile.arn - The instance profile associated with the instance. Specified as an ARN. iam-instance-profile.id - The instance profile associated with the instance. Specified as an ID. image-id - The ID of the image used to launch the instance. instance-id - The ID of the instance. instance-lifecycle - Indicates whether this is a Spot Instance, a Scheduled Instance, or a Capacity Block (spot | scheduled | capacity-block). instance-state-code - The state of the instance, as a 16-bit unsigned integer. The high byte is used for internal purposes and should be ignored. The low byte is set based on the state represented. The valid values are: 0 (pending), 16 (running), 32 (shutting-down), 48 (terminated), 64 (stopping), and 80 (stopped). instance-state-name - The state of the instance (pending | running | shutting-down | terminated | stopping | stopped). instance-type - The type of instance (for example, t2.micro). instance.group-id - The ID of the security group for the instance. instance.group-name - The name of the security group for the instance. ip-address - The public IPv4 address of the instance. ipv6-address - The IPv6 address of the instance. kernel-id - The kernel ID. key-name - The name of the key pair used when the instance was launched. launch-index - When launching multiple instances, this is the index for the instance in the launch group (for example, 0, 1, 2, and so on). launch-time - The time when the instance was launched, in the ISO 8601 format in the UTC time zone (YYYY-MM-DDThh:mm:ss.sssZ), for example, 2021-09-29T11:04:43.305Z. You can use a wildcard (*), for example, 2021-09-29T*, which matches an entire day. maintenance-options.auto-recovery - The current automatic recovery behavior of the instance (disabled | default). metadata-options.http-endpoint - The status of access to the HTTP metadata endpoint on your instance (enabled | disabled) metadata-options.http-protocol-ipv4 - Indicates whether the IPv4 endpoint is enabled (disabled | enabled). metadata-options.http-protocol-ipv6 - Indicates whether the IPv6 endpoint is enabled (disabled | enabled). metadata-options.http-put-response-hop-limit - The HTTP metadata request put response hop limit (integer, possible values 1 to 64) metadata-options.http-tokens - The metadata request authorization state (optional | required) metadata-options.instance-metadata-tags - The status of access to instance tags from the instance metadata (enabled | disabled) metadata-options.state - The state of the metadata option changes (pending | applied). monitoring-state - Indicates whether detailed monitoring is enabled (disabled | enabled). network-interface.addresses.association.allocation-id - The allocation ID. network-interface.addresses.association.association-id - The association ID. network-interface.addresses.association.carrier-ip - The carrier IP address. network-interface.addresses.association.customer-owned-ip - The customer-owned IP address. network-interface.addresses.association.ip-owner-id - The owner ID of the private IPv4 address associated with the network interface. network-interface.addresses.association.public-dns-name - The public DNS name. network-interface.addresses.association.public-ip - The ID of the association of an Elastic IP address (IPv4) with a network interface. network-interface.addresses.primary - Specifies whether the IPv4 address of the network interface is the primary private IPv4 address. network-interface.addresses.private-dns-name - The private DNS name. network-interface.addresses.private-ip-address - The private IPv4 address associated with the network interface. network-interface.association.allocation-id - The allocation ID returned when you allocated the Elastic IP address (IPv4) for your network interface. network-interface.association.association-id - The association ID returned when the network interface was associated with an IPv4 address. network-interface.association.carrier-ip - The customer-owned IP address. network-interface.association.customer-owned-ip - The customer-owned IP address. network-interface.association.ip-owner-id - The owner of the Elastic IP address (IPv4) associated with the network interface. network-interface.association.public-dns-name - The public DNS name. network-interface.association.public-ip - The address of the Elastic IP address (IPv4) bound to the network interface. network-interface.attachment.attach-time - The time that the network interface was attached to an instance. network-interface.attachment.attachment-id - The ID of the interface attachment. network-interface.attachment.delete-on-termination - Specifies whether the attachment is deleted when an instance is terminated. network-interface.attachment.device-index - The device index to which the network interface is attached. network-interface.attachment.instance-id - The ID of the instance to which the network interface is attached. network-interface.attachment.instance-owner-id - The owner ID of the instance to which the network interface is attached. network-interface.attachment.network-card-index - The index of the network card. network-interface.attachment.status - The status of the attachment (attaching | attached | detaching | detached). network-interface.availability-zone - The Availability Zone for the network interface. network-interface.deny-all-igw-traffic - A Boolean that indicates whether a network interface with an IPv6 address is unreachable from the public internet. network-interface.description - The description of the network interface. network-interface.group-id - The ID of a security group associated with the network interface. network-interface.group-name - The name of a security group associated with the network interface. network-interface.ipv4-prefixes.ipv4-prefix - The IPv4 prefixes that are assigned to the network interface. network-interface.ipv6-address - The IPv6 address associated with the network interface. network-interface.ipv6-addresses.ipv6-address - The IPv6 address associated with the network interface. network-interface.ipv6-addresses.is-primary-ipv6 - A Boolean that indicates whether this is the primary IPv6 address. network-interface.ipv6-native - A Boolean that indicates whether this is an IPv6 only network interface. network-interface.ipv6-prefixes.ipv6-prefix - The IPv6 prefix assigned to the network interface. network-interface.mac-address - The MAC address of the network interface. network-interface.network-interface-id - The ID of the network interface. network-interface.operator.managed - A Boolean that indicates whether the instance has a managed network interface. network-interface.operator.principal - The principal that manages the network interface. Only valid for instances with managed network interfaces, where managed is true. network-interface.outpost-arn - The ARN of the Outpost. network-interface.owner-id - The ID of the owner of the network interface. network-interface.private-dns-name - The private DNS name of the network interface. network-interface.private-ip-address - The private IPv4 address. network-interface.public-dns-name - The public DNS name. network-interface.requester-id - The requester ID for the network interface. network-interface.requester-managed - Indicates whether the network interface is being managed by Amazon Web Services. network-interface.status - The status of the network interface (available) | in-use). network-interface.source-dest-check - Whether the network interface performs source/destination checking. A value of true means that checking is enabled, and false means that checking is disabled. The value must be false for the network interface to perform network address translation (NAT) in your VPC. network-interface.subnet-id - The ID of the subnet for the network interface. network-interface.tag-key - The key of a tag assigned to the network interface. network-interface.tag-value - The value of a tag assigned to the network interface. network-interface.vpc-id - The ID of the VPC for the network interface. network-performance-options.bandwidth-weighting - Where the performance boost is applied, if applicable. Valid values: default, vpc-1, ebs-1. operator.managed - A Boolean that indicates whether this is a managed instance. operator.principal - The principal that manages the instance. Only valid for managed instances, where managed is true. outpost-arn - The Amazon Resource Name (ARN) of the Outpost. owner-id - The Amazon Web Services account ID of the instance owner. placement-group-name - The name of the placement group for the instance. placement-partition-number - The partition in which the instance is located. platform - The platform. To list only Windows instances, use windows. platform-details - The platform (Linux/UNIX | Red Hat BYOL Linux | Red Hat Enterprise Linux | Red Hat Enterprise Linux with HA | Red Hat Enterprise Linux with High Availability | Red Hat Enterprise Linux with SQL Server Standard and HA | Red Hat Enterprise Linux with SQL Server Enterprise and HA | Red Hat Enterprise Linux with SQL Server Standard | Red Hat Enterprise Linux with SQL Server Web | Red Hat Enterprise Linux with SQL Server Enterprise | SQL Server Enterprise | SQL Server Standard | SQL Server Web | SUSE Linux | Ubuntu Pro | Windows | Windows BYOL | Windows with SQL Server Enterprise | Windows with SQL Server Standard | Windows with SQL Server Web). private-dns-name - The private IPv4 DNS name of the instance. private-dns-name-options.enable-resource-name-dns-a-record - A Boolean that indicates whether to respond to DNS queries for instance hostnames with DNS A records. private-dns-name-options.enable-resource-name-dns-aaaa-record - A Boolean that indicates whether to respond to DNS queries for instance hostnames with DNS AAAA records. private-dns-name-options.hostname-type - The type of hostname (ip-name | resource-name). private-ip-address - The private IPv4 address of the instance. This can only be used to filter by the primary IP address of the network interface attached to the instance. To filter by additional IP addresses assigned to the network interface, use the filter network-interface.addresses.private-ip-address. product-code - The product code associated with the AMI used to launch the instance. product-code.type - The type of product code (devpay | marketplace). ramdisk-id - The RAM disk ID. reason - The reason for the current state of the instance (for example, shows \"User Initiated \\[date\\]\" when you stop or terminate the instance). Similar to the state-reason-code filter. requester-id - The ID of the entity that launched the instance on your behalf (for example, Amazon Web Services Management Console, Auto Scaling, and so on). reservation-id - The ID of the instance's reservation. A reservation ID is created any time you launch an instance. A reservation ID has a one-to-one relationship with an instance launch request, but can be associated with more than one instance if you launch multiple instances using the same launch request. For example, if you launch one instance, you get one reservation ID. If you launch ten instances using the same launch request, you also get one reservation ID. root-device-name - The device name of the root device volume (for example, /dev/sda1). root-device-type - The type of the root device volume (ebs | instance-store). source-dest-check - Indicates whether the instance performs source/destination checking. A value of true means that checking is enabled, and false means that checking is disabled. The value must be false for the instance to perform network address translation (NAT) in your VPC. spot-instance-request-id - The ID of the Spot Instance request. state-reason-code - The reason code for the state change. state-reason-message - A message that describes the state change. subnet-id - The ID of the subnet for the instance. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources that have a tag with a specific key, regardless of the tag value. tenancy - The tenancy of an instance (dedicated | default | host). tpm-support - Indicates if the instance is configured for NitroTPM support (v2.0). usage-operation - The usage operation value for the instance (RunInstances | RunInstances:00g0 | RunInstances:0010 | RunInstances:1010 | RunInstances:1014 | RunInstances:1110 | RunInstances:0014 | RunInstances:0210 | RunInstances:0110 | RunInstances:0100 | RunInstances:0004 | RunInstances:0200 | RunInstances:000g | RunInstances:0g00 | RunInstances:0002 | RunInstances:0800 | RunInstances:0102 | RunInstances:0006 | RunInstances:0202). usage-operation-update-time - The time that the usage operation was last updated, for example, 2022-09-15T17:15:20.000Z. virtualization-type - The virtualization type of the instance (paravirtual | hvm). vpc-id - The ID of the VPC that the instance is running in."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. You cannot specify this parameter and the instance IDs parameter in the same request."]}
    let make ?instanceIds =
      fun ?includeManagedResources ->
        fun ?dryRun ->
          fun ?filters ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun () ->
                  {
                    instanceIds;
                    includeManagedResources;
                    dryRun;
                    filters;
                    nextToken;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("InstanceId",
           (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("IncludeManagedResources",
          (Option.map x.includeManagedResources ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let includeManagedResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeManagedResources") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      make ?maxResults ?nextToken ?filters ?dryRun ?includeManagedResources
        ?instanceIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let includeManagedResources =
        field_map json__ "IncludeManagedResources" Boolean.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      make ?maxResults ?nextToken ?filters ?dryRun ?includeManagedResources
        ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified instances or all instances. If you specify instance IDs, the output includes information for only the specified instances. If you specify filters, the output includes information for only those instances that meet the filter criteria. If you do not specify instance IDs or filters, the output includes information for all instances, which can affect performance. We recommend that you use pagination to ensure that the operation returns quickly and successfully. The response includes SQL license exemption status information for instances registered with the SQL LE service, providing visibility into license exemption configuration and status. If you specify an instance ID that is not valid, an error is returned. If you specify an instance that you do not own, it is not included in the output. Recently terminated instances might appear in the returned results. This interval is usually less than one hour. If you describe instances in the rare case where an Availability Zone is experiencing a service disruption and you specify instance IDs that are in the affected zone, or do not specify any instance IDs at all, the call fails. If you describe instances and specify only instance IDs that are in an unaffected zone, the call works normally. The Amazon EC2 API follows an eventual consistency model. This means that the result of an API command you run that creates or modifies resources might not be immediately available to all subsequent commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. We strongly recommend using only paginated requests. Unpaginated requests are susceptible to throttling and timeouts. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeInstanceTypesResult =
  struct
    type nonrec t =
      {
      instanceTypes: InstanceTypeInfoList.t option
        [@ocaml.doc "The instance type."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTypes =
      fun ?nextToken -> fun () -> { instanceTypes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTypeSet",
           (Option.map x.instanceTypes ~f:InstanceTypeInfoList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceTypes =
        (Option.map ~f:InstanceTypeInfoList.of_xml)
          (Xml.child xml_arg0 "instanceTypeSet") in
      make ?nextToken ?instanceTypes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let instanceTypes =
        field_map json__ "InstanceTypes" InstanceTypeInfoList.of_json in
      make ?nextToken ?instanceTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified instance types. By default, all instance types for the current Region are described. Alternatively, you can filter the results. To include instance types that are not supported in the current Region, set IncludeUnsupportedInRegion to true."]
module DescribeInstanceTypesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceTypes: RequestInstanceTypeList.t option
        [@ocaml.doc "The instance types."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. auto-recovery-supported - Indicates whether Amazon CloudWatch action based recovery is supported (true | false). bare-metal - Indicates whether it is a bare metal instance type (true | false). burstable-performance-supported - Indicates whether the instance type is a burstable performance T instance type (true | false). current-generation - Indicates whether this instance type is the latest generation instance type of an instance family (true | false). dedicated-hosts-supported - Indicates whether the instance type supports Dedicated Hosts. (true | false) ebs-info.attachment-limit-type - The type of Amazon EBS volume attachment limit (shared | dedicated). ebs-info.maximum-ebs-attachments - The maximum number of Amazon EBS volumes that can be attached to the instance type. ebs-info.ebs-optimized-info.baseline-bandwidth-in-mbps - The baseline bandwidth performance for an EBS-optimized instance type, in Mbps. ebs-info.ebs-optimized-info.baseline-iops - The baseline input/output storage operations per second for an EBS-optimized instance type. ebs-info.ebs-optimized-info.baseline-throughput-in-mbps - The baseline throughput performance for an EBS-optimized instance type, in MB/s. ebs-info.ebs-optimized-info.maximum-bandwidth-in-mbps - The maximum bandwidth performance for an EBS-optimized instance type, in Mbps. ebs-info.ebs-optimized-info.maximum-iops - The maximum input/output storage operations per second for an EBS-optimized instance type. ebs-info.ebs-optimized-info.maximum-throughput-in-mbps - The maximum throughput performance for an EBS-optimized instance type, in MB/s. ebs-info.ebs-optimized-support - Indicates whether the instance type is EBS-optimized (supported | unsupported | default). ebs-info.encryption-support - Indicates whether EBS encryption is supported (supported | unsupported). ebs-info.nvme-support - Indicates whether non-volatile memory express (NVMe) is supported for EBS volumes (required | supported | unsupported). free-tier-eligible - A Boolean that indicates whether this instance type can be used under the Amazon Web Services Free Tier (true | false). hibernation-supported - Indicates whether On-Demand hibernation is supported (true | false). hypervisor - The hypervisor (nitro | xen). instance-storage-info.disk.count - The number of local disks. instance-storage-info.disk.size-in-gb - The storage size of each instance storage disk, in GB. instance-storage-info.disk.type - The storage technology for the local instance storage disks (hdd | ssd). instance-storage-info.encryption-support - Indicates whether data is encrypted at rest (required | supported | unsupported). instance-storage-info.nvme-support - Indicates whether non-volatile memory express (NVMe) is supported for instance store (required | supported | unsupported). instance-storage-info.total-size-in-gb - The total amount of storage available from all local instance storage, in GB. instance-storage-supported - Indicates whether the instance type has local instance storage (true | false). instance-type - The instance type (for example c5.2xlarge or c5*). memory-info.size-in-mib - The memory size. network-info.bandwidth-weightings - For instances that support bandwidth weighting to boost performance (default, vpc-1, ebs-1). network-info.efa-info.maximum-efa-interfaces - The maximum number of Elastic Fabric Adapters (EFAs) per instance. network-info.efa-supported - Indicates whether the instance type supports Elastic Fabric Adapter (EFA) (true | false). network-info.ena-support - Indicates whether Elastic Network Adapter (ENA) is supported or required (required | supported | unsupported). network-info.flexible-ena-queues-support - Indicates whether an instance supports flexible ENA queues (supported | unsupported). network-info.encryption-in-transit-supported - Indicates whether the instance type automatically encrypts in-transit traffic between instances (true | false). network-info.ipv4-addresses-per-interface - The maximum number of private IPv4 addresses per network interface. network-info.ipv6-addresses-per-interface - The maximum number of private IPv6 addresses per network interface. network-info.ipv6-supported - Indicates whether the instance type supports IPv6 (true | false). network-info.maximum-network-cards - The maximum number of network cards per instance. network-info.maximum-network-interfaces - The maximum number of network interfaces per instance. network-info.network-performance - The network performance (for example, \"25 Gigabit\"). nitro-enclaves-support - Indicates whether Nitro Enclaves is supported (supported | unsupported). nitro-tpm-support - Indicates whether NitroTPM is supported (supported | unsupported). nitro-tpm-info.supported-versions - The supported NitroTPM version (2.0). processor-info.supported-architecture - The CPU architecture (arm64 | i386 | x86_64). processor-info.sustained-clock-speed-in-ghz - The CPU clock speed, in GHz. processor-info.supported-features - The supported CPU features (amd-sev-snp). reboot-migration-support - Indicates whether enabling reboot migration is supported (supported | unsupported). supported-boot-mode - The boot mode (legacy-bios | uefi). supported-root-device-type - The root device type (ebs | instance-store). supported-usage-class - The usage class (on-demand | spot | capacity-block). supported-virtualization-type - The virtualization type (hvm | paravirtual). vcpu-info.default-cores - The default number of cores for the instance type. vcpu-info.default-threads-per-core - The default number of threads per core for the instance type. vcpu-info.default-vcpus - The default number of vCPUs for the instance type. vcpu-info.valid-cores - The number of cores that can be configured for the instance type. vcpu-info.valid-threads-per-core - The number of threads per core that can be configured for the instance type. For example, \"1\" or \"1,2\"."];
      maxResults: DITMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      includeUnsupportedInRegion: IncludeUnsupportedInRegion.t option
        [@ocaml.doc
          "If true, the response includes instance types that are not supported in the current Region, in addition to the supported types. Default: false."]}
    let make ?dryRun =
      fun ?instanceTypes ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun ?includeUnsupportedInRegion ->
                fun () ->
                  {
                    dryRun;
                    instanceTypes;
                    filters;
                    maxResults;
                    nextToken;
                    includeUnsupportedInRegion
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceType",
          (Option.map x.instanceTypes ~f:RequestInstanceTypeList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:DITMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IncludeUnsupportedInRegion",
          (Option.map x.includeUnsupportedInRegion
             ~f:IncludeUnsupportedInRegion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeUnsupportedInRegion =
        (Option.map ~f:IncludeUnsupportedInRegion.of_xml)
          (Xml.child xml_arg0 "IncludeUnsupportedInRegion") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DITMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let instanceTypes =
        (Option.map ~f:RequestInstanceTypeList.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?includeUnsupportedInRegion ?nextToken ?maxResults ?filters
        ?instanceTypes ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let includeUnsupportedInRegion =
        field_map json__ "IncludeUnsupportedInRegion"
          IncludeUnsupportedInRegion.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" DITMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let instanceTypes =
        field_map json__ "InstanceTypes" RequestInstanceTypeList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?includeUnsupportedInRegion ?nextToken ?maxResults ?filters
        ?instanceTypes ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified instance types. By default, all instance types for the current Region are described. Alternatively, you can filter the results. To include instance types that are not supported in the current Region, set IncludeUnsupportedInRegion to true."]
module DescribeInstanceTypeOfferingsResult =
  struct
    type nonrec t =
      {
      instanceTypeOfferings: InstanceTypeOfferingsList.t option
        [@ocaml.doc "The instance types offered in the location."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTypeOfferings =
      fun ?nextToken -> fun () -> { instanceTypeOfferings; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTypeOfferingSet",
           (Option.map x.instanceTypeOfferings
              ~f:InstanceTypeOfferingsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceTypeOfferings =
        (Option.map ~f:InstanceTypeOfferingsList.of_xml)
          (Xml.child xml_arg0 "instanceTypeOfferingSet") in
      make ?nextToken ?instanceTypeOfferings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let instanceTypeOfferings =
        field_map json__ "InstanceTypeOfferings"
          InstanceTypeOfferingsList.of_json in
      make ?nextToken ?instanceTypeOfferings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the instance types that are offered for the specified location. If no location is specified, the default is to list the instance types that are offered in the current Region."]
module DescribeInstanceTypeOfferingsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      locationType: LocationType.t option
        [@ocaml.doc
          "The location type. availability-zone - The Availability Zone. When you specify a location filter, it must be an Availability Zone for the current Region. availability-zone-id - The AZ ID. When you specify a location filter, it must be an AZ ID for the current Region. outpost - The Outpost ARN. When you specify a location filter, it must be an Outpost ARN for the current Region. region - The current Region. If you specify a location filter, it must match the current Region."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. instance-type - The instance type. For a list of possible values, see Instance. location - The location. For a list of possible identifiers, see Regions and Zones."];
      maxResults: DITOMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."]}
    let make ?dryRun =
      fun ?locationType ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; locationType; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LocationType",
          (Option.map x.locationType ~f:LocationType.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:DITOMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DITOMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let locationType =
        (Option.map ~f:LocationType.of_xml)
          (Xml.child xml_arg0 "LocationType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?locationType ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" DITOMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let locationType = field_map json__ "LocationType" LocationType.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?locationType ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the instance types that are offered for the specified location. If no location is specified, the default is to list the instance types that are offered in the current Region."]
module DescribeInstanceTopologyResult =
  struct
    type nonrec t =
      {
      instances: InstanceSet.t option
        [@ocaml.doc "Information about the topology of each instance."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instances =
      fun ?nextToken -> fun () -> { instances; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceSet", (Option.map x.instances ~f:InstanceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instances =
        (Option.map ~f:InstanceSet.of_xml) (Xml.child xml_arg0 "instanceSet") in
      make ?nextToken ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let instances = field_map json__ "Instances" InstanceSet.of_json in
      make ?nextToken ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a tree-based hierarchy that represents the physical host placement of your EC2 instances within an Availability Zone or Local Zone. You can use this information to determine the relative proximity of your EC2 instances within the Amazon Web Services network to support your tightly coupled workloads. Instance topology is supported for specific instance types only. For more information, see Prerequisites for Amazon EC2 instance topology in the Amazon EC2 User Guide. The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the system supporting it. As a result, when you call the DescribeInstanceTopology API command immediately after launching instances, the response might return a null value for capacityBlockId because the data might not have fully propagated across all subsystems. For more information, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. For more information, see Amazon EC2 topology in the Amazon EC2 User Guide."]
module DescribeInstanceTopologyRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeInstanceTopologyMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. You can't specify this parameter and the instance IDs parameter in the same request. Default: 20"];
      instanceIds: DescribeInstanceTopologyInstanceIdSet.t option
        [@ocaml.doc
          "The instance IDs. Default: Describes all your instances. Constraints: Maximum 100 explicitly specified instance IDs."];
      groupNames: DescribeInstanceTopologyGroupNameSet.t option
        [@ocaml.doc
          "The name of the placement group that each instance is in. Constraints: Maximum 100 explicitly specified placement group names."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The name of the Availability Zone (for example, us-west-2a) or Local Zone (for example, us-west-2-lax-1b) that the instance is in. instance-type - The instance type (for example, p4d.24xlarge) or instance family (for example, p4d*). You can use the * wildcard to match zero or more characters, or the ? wildcard to match zero or one character. zone-id - The ID of the Availability Zone (for example, usw2-az2) or Local Zone (for example, usw2-lax1-az1) that the instance is in."]}
    let make ?dryRun =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?instanceIds ->
            fun ?groupNames ->
              fun ?filters ->
                fun () ->
                  {
                    dryRun;
                    nextToken;
                    maxResults;
                    instanceIds;
                    groupNames;
                    filters
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInstanceTopologyMaxResults.to_value));
        ("InstanceId",
          (Option.map x.instanceIds
             ~f:DescribeInstanceTopologyInstanceIdSet.to_value));
        ("GroupName",
          (Option.map x.groupNames
             ~f:DescribeInstanceTopologyGroupNameSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let groupNames =
        (Option.map ~f:DescribeInstanceTopologyGroupNameSet.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let instanceIds =
        (Option.map ~f:DescribeInstanceTopologyInstanceIdSet.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let maxResults =
        (Option.map ~f:DescribeInstanceTopologyMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?groupNames ?instanceIds ?maxResults ?nextToken ?dryRun
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let groupNames =
        field_map json__ "GroupNames"
          DescribeInstanceTopologyGroupNameSet.of_json in
      let instanceIds =
        field_map json__ "InstanceIds"
          DescribeInstanceTopologyInstanceIdSet.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeInstanceTopologyMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ?groupNames ?instanceIds ?maxResults ?nextToken ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a tree-based hierarchy that represents the physical host placement of your EC2 instances within an Availability Zone or Local Zone. You can use this information to determine the relative proximity of your EC2 instances within the Amazon Web Services network to support your tightly coupled workloads. Instance topology is supported for specific instance types only. For more information, see Prerequisites for Amazon EC2 instance topology in the Amazon EC2 User Guide. The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the system supporting it. As a result, when you call the DescribeInstanceTopology API command immediately after launching instances, the response might return a null value for capacityBlockId because the data might not have fully propagated across all subsystems. For more information, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. For more information, see Amazon EC2 topology in the Amazon EC2 User Guide."]
module DescribeInstanceStatusResult =
  struct
    type nonrec t =
      {
      instanceStatuses: InstanceStatusList.t option
        [@ocaml.doc "Information about the status of the instances."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceStatuses =
      fun ?nextToken -> fun () -> { instanceStatuses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceStatusSet",
           (Option.map x.instanceStatuses ~f:InstanceStatusList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceStatuses =
        (Option.map ~f:InstanceStatusList.of_xml)
          (Xml.child xml_arg0 "instanceStatusSet") in
      make ?nextToken ?instanceStatuses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let instanceStatuses =
        field_map json__ "InstanceStatuses" InstanceStatusList.of_json in
      make ?nextToken ?instanceStatuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the status of the specified instances or all of your instances. By default, only running instances are described, unless you specifically indicate to return the status of all instances. Instance status includes the following components: Status checks - Amazon EC2 performs status checks on running EC2 instances to identify hardware and software issues. For more information, see Status checks for your instances and Troubleshoot instances with failed status checks in the Amazon EC2 User Guide. Scheduled events - Amazon EC2 can schedule events (such as reboot, stop, or terminate) for your instances related to hardware issues, software updates, or system maintenance. For more information, see Scheduled events for your instances in the Amazon EC2 User Guide. Instance state - You can manage your instances from the moment you launch them through their termination. For more information, see Instance lifecycle in the Amazon EC2 User Guide. The Amazon EC2 API follows an eventual consistency model. This means that the result of an API command you run that creates or modifies resources might not be immediately available to all subsequent commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeInstanceStatusRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The instance IDs. Default: Describes all your instances. Constraints: Maximum 100 explicitly specified instance IDs."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. You cannot specify this parameter and the instance IDs parameter in the same request."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      includeManagedResources: Boolean.t option
        [@ocaml.doc
          "Indicates whether to include managed resources in the output. If this parameter is set to true, the output includes resources that are managed by Amazon Web Services services, even if managed resource visibility is set to hidden."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The Availability Zone of the instance. availability-zone-id - The ID of the Availability Zone of the instance. event.code - The code for the scheduled event (instance-reboot | system-reboot | system-maintenance | instance-retirement | instance-stop). event.description - A description of the event. event.instance-event-id - The ID of the event whose date and time you are modifying. event.not-after - The latest end time for the scheduled event (for example, 2014-09-15T17:15:20.000Z). event.not-before - The earliest start time for the scheduled event (for example, 2014-09-15T17:15:20.000Z). event.not-before-deadline - The deadline for starting the event (for example, 2014-09-15T17:15:20.000Z). instance-state-code - The code for the instance state, as a 16-bit unsigned integer. The high byte is used for internal purposes and should be ignored. The low byte is set based on the state represented. The valid values are 0 (pending), 16 (running), 32 (shutting-down), 48 (terminated), 64 (stopping), and 80 (stopped). instance-state-name - The state of the instance (pending | running | shutting-down | terminated | stopping | stopped). instance-status.reachability - Filters on instance status where the name is reachability (passed | failed | initializing | insufficient-data). instance-status.status - The status of the instance (ok | impaired | initializing | insufficient-data | not-applicable). operator.managed - A Boolean that indicates whether this is a managed instance. operator.principal - The principal that manages the instance. Only valid for managed instances, where managed is true. system-status.reachability - Filters on system status where the name is reachability (passed | failed | initializing | insufficient-data). system-status.status - The system status of the instance (ok | impaired | initializing | insufficient-data | not-applicable). attached-ebs-status.status - The status of the attached EBS volume for the instance (ok | impaired | initializing | insufficient-data | not-applicable)."];
      includeAllInstances: Boolean.t option
        [@ocaml.doc
          "When true, includes the health status for all instances. When false, includes the health status for running instances only. Default: false"]}
    let make ?instanceIds =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?includeManagedResources ->
            fun ?dryRun ->
              fun ?filters ->
                fun ?includeAllInstances ->
                  fun () ->
                    {
                      instanceIds;
                      maxResults;
                      nextToken;
                      includeManagedResources;
                      dryRun;
                      filters;
                      includeAllInstances
                    }
    let to_value x =
      structure_to_value
        [("InstanceId",
           (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("IncludeManagedResources",
          (Option.map x.includeManagedResources ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("includeAllInstances",
          (Option.map x.includeAllInstances ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeAllInstances =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "includeAllInstances") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let includeManagedResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeManagedResources") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      make ?includeAllInstances ?filters ?dryRun ?includeManagedResources
        ?nextToken ?maxResults ?instanceIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let includeAllInstances =
        field_map json__ "IncludeAllInstances" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let includeManagedResources =
        field_map json__ "IncludeManagedResources" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      make ?includeAllInstances ?filters ?dryRun ?includeManagedResources
        ?nextToken ?maxResults ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the status of the specified instances or all of your instances. By default, only running instances are described, unless you specifically indicate to return the status of all instances. Instance status includes the following components: Status checks - Amazon EC2 performs status checks on running EC2 instances to identify hardware and software issues. For more information, see Status checks for your instances and Troubleshoot instances with failed status checks in the Amazon EC2 User Guide. Scheduled events - Amazon EC2 can schedule events (such as reboot, stop, or terminate) for your instances related to hardware issues, software updates, or system maintenance. For more information, see Scheduled events for your instances in the Amazon EC2 User Guide. Instance state - You can manage your instances from the moment you launch them through their termination. For more information, see Instance lifecycle in the Amazon EC2 User Guide. The Amazon EC2 API follows an eventual consistency model. This means that the result of an API command you run that creates or modifies resources might not be immediately available to all subsequent commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeInstanceSqlHaStatesResult =
  struct
    type nonrec t =
      {
      instances: RegisteredInstanceList.t option
        [@ocaml.doc
          "Information about the SQL Server High Availability instances."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instances =
      fun ?nextToken -> fun () -> { instances; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceSet",
           (Option.map x.instances ~f:RegisteredInstanceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instances =
        (Option.map ~f:RegisteredInstanceList.of_xml)
          (Xml.child xml_arg0 "instanceSet") in
      make ?nextToken ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let instances =
        field_map json__ "Instances" RegisteredInstanceList.of_json in
      make ?nextToken ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the SQL Server High Availability states for Amazon EC2 instances that are enabled for Amazon EC2 High Availability for SQL Server monitoring."]
module DescribeInstanceSqlHaStatesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The IDs of the SQL Server High Availability instances to describe. If omitted, the API returns SQL Server High Availability states for all SQL Server High Availability instances."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults:
        DescribeInstanceSqlHaStatesRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters to apply to the results. Supported filters include: tag:<key> - The tag key and value pair assigned to the instance. For example, to find all instances tagged with Owner:TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The tag key assigned to the instance. haStatus - The SQL Server High Availability status of the SQL Server High Availability instance (processing | active | standby | invalid). sqlServerLicenseUsage - The license type for the SQL Server license (full | waived)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?instanceIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                { instanceIds; nextToken; maxResults; filters; dryRun }
    let to_value x =
      structure_to_value
        [("InstanceId",
           (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInstanceSqlHaStatesRequestMaxResultsInteger.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map
           ~f:DescribeInstanceSqlHaStatesRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?instanceIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeInstanceSqlHaStatesRequestMaxResultsInteger.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the SQL Server High Availability states for Amazon EC2 instances that are enabled for Amazon EC2 High Availability for SQL Server monitoring."]
module DescribeInstanceSqlHaHistoryStatesResult =
  struct
    type nonrec t =
      {
      instances: RegisteredInstanceList.t option
        [@ocaml.doc
          "Information about the historical SQL Server High Availability states of the SQL Server High Availability instances."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instances =
      fun ?nextToken -> fun () -> { instances; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceSet",
           (Option.map x.instances ~f:RegisteredInstanceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instances =
        (Option.map ~f:RegisteredInstanceList.of_xml)
          (Xml.child xml_arg0 "instanceSet") in
      make ?nextToken ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let instances =
        field_map json__ "Instances" RegisteredInstanceList.of_json in
      make ?nextToken ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the historical SQL Server High Availability states for Amazon EC2 instances that are enabled for Amazon EC2 High Availability for SQL Server monitoring."]
module DescribeInstanceSqlHaHistoryStatesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The IDs of the SQL Server High Availability instances to describe. If omitted, the API returns historical states for all SQL Server High Availability instances."];
      startTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The start data and time of the period for which to get the historical SQL Server High Availability states. If omitted, the API returns all available historical states. Timezone: UTC Format: YYYY-MM-DDThh:mm:ss.sssZ"];
      endTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The end data and time of the period for which to get historical SQL Server High Availability states. If omitted, the API returns historical states up to the current date and time. Timezone: UTC Format: YYYY-MM-DDThh:mm:ss.sssZ"];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults:
        DescribeInstanceSqlHaStatesRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters to apply to the results. Supported filters include: tag:<key> - The tag key and value pair assigned to the instance. For example, to find all instances tagged with Owner:TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The tag key assigned to the instance. haStatus - The SQL Server High Availability status of the SQL Server High Availability instance (processing | active | standby | invalid). sqlServerLicenseUsage - The license type for the SQL Server license (full | waived)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?instanceIds =
      fun ?startTime ->
        fun ?endTime ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun ?filters ->
                fun ?dryRun ->
                  fun () ->
                    {
                      instanceIds;
                      startTime;
                      endTime;
                      nextToken;
                      maxResults;
                      filters;
                      dryRun
                    }
    let to_value x =
      structure_to_value
        [("InstanceId",
           (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("StartTime",
          (Option.map x.startTime ~f:MillisecondDateTime.to_value));
        ("EndTime", (Option.map x.endTime ~f:MillisecondDateTime.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInstanceSqlHaStatesRequestMaxResultsInteger.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map
           ~f:DescribeInstanceSqlHaStatesRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let endTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "StartTime") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?endTime ?startTime
        ?instanceIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeInstanceSqlHaStatesRequestMaxResultsInteger.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let endTime = field_map json__ "EndTime" MillisecondDateTime.of_json in
      let startTime =
        field_map json__ "StartTime" MillisecondDateTime.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?endTime ?startTime
        ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the historical SQL Server High Availability states for Amazon EC2 instances that are enabled for Amazon EC2 High Availability for SQL Server monitoring."]
module DescribeInstanceImageMetadataResult =
  struct
    type nonrec t =
      {
      instanceImageMetadata: InstanceImageMetadataList.t option
        [@ocaml.doc
          "Information about the instance and the AMI used to launch the instance."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceImageMetadata =
      fun ?nextToken -> fun () -> { instanceImageMetadata; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceImageMetadataSet",
           (Option.map x.instanceImageMetadata
              ~f:InstanceImageMetadataList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceImageMetadata =
        (Option.map ~f:InstanceImageMetadataList.of_xml)
          (Xml.child xml_arg0 "instanceImageMetadataSet") in
      make ?nextToken ?instanceImageMetadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let instanceImageMetadata =
        field_map json__ "InstanceImageMetadata"
          InstanceImageMetadataList.of_json in
      make ?nextToken ?instanceImageMetadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the AMI that was used to launch an instance, even if the AMI is deprecated, deregistered, made private (no longer public or shared with your account), or not allowed. If you specify instance IDs, the output includes information for only the specified instances. If you specify filters, the output includes information for only those instances that meet the filter criteria. If you do not specify instance IDs or filters, the output includes information for all instances, which can affect performance. If you specify an instance ID that is not valid, an instance that doesn't exist, or an instance that you do not own, an error (InvalidInstanceID.NotFound) is returned. Recently terminated instances might appear in the returned results. This interval is usually less than one hour. In the rare case where an Availability Zone is experiencing a service disruption and you specify instance IDs that are in the affected Availability Zone, or do not specify any instance IDs at all, the call fails. If you specify only instance IDs that are in an unaffected Availability Zone, the call works normally. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeInstanceImageMetadataRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The name of the Availability Zone (for example, us-west-2a) or Local Zone (for example, us-west-2-lax-1b) of the instance. instance-id - The ID of the instance. image-allowed - A Boolean that indicates whether the image meets the criteria specified for Allowed AMIs. instance-state-name - The state of the instance (pending | running | shutting-down | terminated | stopping | stopped). instance-type - The type of instance (for example, t3.micro). launch-time - The time when the instance was launched, in the ISO 8601 format in the UTC time zone (YYYY-MM-DDThh:mm:ss.sssZ), for example, 2023-09-29T11:04:43.305Z. You can use a wildcard (*), for example, 2023-09-29T*, which matches an entire day. owner-alias - The owner alias (amazon | aws-marketplace | aws-backup-vault). The valid aliases are defined in an Amazon-maintained list. This is not the Amazon Web Services account alias that can be set using the IAM console. We recommend that you use the Owner request parameter instead of this filter. owner-id - The Amazon Web Services account ID of the owner. We recommend that you use the Owner request parameter instead of this filter. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. zone-id - The ID of the Availability Zone (for example, usw2-az2) or Local Zone (for example, usw2-lax1-az1) of the instance."];
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The instance IDs. If you don't specify an instance ID or filters, the output includes information for all instances."];
      maxResults: DescribeInstanceImageMetadataMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. Default: 1000"];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?filters =
      fun ?instanceIds ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { filters; instanceIds; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInstanceImageMetadataMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeInstanceImageMetadataMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?nextToken ?maxResults ?instanceIds ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeInstanceImageMetadataMaxResults.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      make ?dryRun ?nextToken ?maxResults ?instanceIds ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the AMI that was used to launch an instance, even if the AMI is deprecated, deregistered, made private (no longer public or shared with your account), or not allowed. If you specify instance IDs, the output includes information for only the specified instances. If you specify filters, the output includes information for only those instances that meet the filter criteria. If you do not specify instance IDs or filters, the output includes information for all instances, which can affect performance. If you specify an instance ID that is not valid, an instance that doesn't exist, or an instance that you do not own, an error (InvalidInstanceID.NotFound) is returned. Recently terminated instances might appear in the returned results. This interval is usually less than one hour. In the rare case where an Availability Zone is experiencing a service disruption and you specify instance IDs that are in the affected Availability Zone, or do not specify any instance IDs at all, the call fails. If you specify only instance IDs that are in an unaffected Availability Zone, the call works normally. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeInstanceEventWindowsResult =
  struct
    type nonrec t =
      {
      instanceEventWindows: InstanceEventWindowSet.t option
        [@ocaml.doc "Information about the event windows."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindows =
      fun ?nextToken -> fun () -> { instanceEventWindows; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindowSet",
           (Option.map x.instanceEventWindows
              ~f:InstanceEventWindowSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceEventWindows =
        (Option.map ~f:InstanceEventWindowSet.of_xml)
          (Xml.child xml_arg0 "instanceEventWindowSet") in
      make ?nextToken ?instanceEventWindows ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let instanceEventWindows =
        field_map json__ "InstanceEventWindows"
          InstanceEventWindowSet.of_json in
      make ?nextToken ?instanceEventWindows ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified event windows or all event windows. If you specify event window IDs, the output includes information for only the specified event windows. If you specify filters, the output includes information for only those event windows that meet the filter criteria. If you do not specify event windows IDs or filters, the output includes information for all event windows, which can affect performance. We recommend that you use pagination to ensure that the operation returns quickly and successfully. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module DescribeInstanceEventWindowsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceEventWindowIds: InstanceEventWindowIdSet.t option
        [@ocaml.doc "The IDs of the event windows."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. dedicated-host-id - The event windows associated with the specified Dedicated Host ID. event-window-name - The event windows associated with the specified names. instance-id - The event windows associated with the specified instance ID. instance-tag - The event windows associated with the specified tag and value. instance-tag-key - The event windows associated with the specified tag key, regardless of the value. instance-tag-value - The event windows associated with the specified tag value, regardless of the key. tag:<key> - The key/value combination of a tag assigned to the event window. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value CMX, specify tag:Owner for the filter name and CMX for the filter value. tag-key - The key of a tag assigned to the event window. Use this filter to find all event windows that have a tag with a specific key, regardless of the tag value. tag-value - The value of a tag assigned to the event window. Use this filter to find all event windows that have a tag with a specific value, regardless of the tag key."];
      maxResults: ResultRange.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned NextToken value. This value can be between 20 and 500. You cannot specify this parameter and the event window IDs parameter in the same call."];
      nextToken: String_.t option
        [@ocaml.doc "The token to request the next page of results."]}
    let make ?dryRun =
      fun ?instanceEventWindowIds ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  dryRun;
                  instanceEventWindowIds;
                  filters;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Option.map x.instanceEventWindowIds
             ~f:InstanceEventWindowIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:ResultRange.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ResultRange.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let instanceEventWindowIds =
        (Option.map ~f:InstanceEventWindowIdSet.of_xml)
          (Xml.child xml_arg0 "InstanceEventWindowId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?instanceEventWindowIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" ResultRange.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let instanceEventWindowIds =
        field_map json__ "InstanceEventWindowIds"
          InstanceEventWindowIdSet.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?instanceEventWindowIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describe instance event windows by InstanceEventWindow."]
module DescribeInstanceEventNotificationAttributesResult =
  struct
    type nonrec t =
      {
      instanceTagAttribute: InstanceTagNotificationAttribute.t option
        [@ocaml.doc "Information about the registered tag keys."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTagAttribute = fun () -> { instanceTagAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTagAttribute",
           (Option.map x.instanceTagAttribute
              ~f:InstanceTagNotificationAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:InstanceTagNotificationAttribute.of_xml)
          (Xml.child xml_arg0 "instanceTagAttribute") in
      make ?instanceTagAttribute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceTagAttribute =
        field_map json__ "InstanceTagAttribute"
          InstanceTagNotificationAttribute.of_json in
      make ?instanceTagAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the tag keys that are registered to appear in scheduled event notifications for resources in the current Region."]
module DescribeInstanceEventNotificationAttributesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the tag keys that are registered to appear in scheduled event notifications for resources in the current Region."]
module DescribeInstanceCreditSpecificationsResult =
  struct
    type nonrec t =
      {
      instanceCreditSpecifications: InstanceCreditSpecificationList.t option
        [@ocaml.doc
          "Information about the credit option for CPU usage of an instance."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceCreditSpecifications =
      fun ?nextToken -> fun () -> { instanceCreditSpecifications; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceCreditSpecificationSet",
           (Option.map x.instanceCreditSpecifications
              ~f:InstanceCreditSpecificationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceCreditSpecifications =
        (Option.map ~f:InstanceCreditSpecificationList.of_xml)
          (Xml.child xml_arg0 "instanceCreditSpecificationSet") in
      make ?nextToken ?instanceCreditSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let instanceCreditSpecifications =
        field_map json__ "InstanceCreditSpecifications"
          InstanceCreditSpecificationList.of_json in
      make ?nextToken ?instanceCreditSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the credit option for CPU usage of the specified burstable performance instances. The credit options are standard and unlimited. If you do not specify an instance ID, Amazon EC2 returns burstable performance instances with the unlimited credit option, as well as instances that were previously configured as T2, T3, and T3a with the unlimited credit option. For example, if you resize a T2 instance, while it is configured as unlimited, to an M4 instance, Amazon EC2 returns the M4 instance. If you specify one or more instance IDs, Amazon EC2 returns the credit option (standard or unlimited) of those instances. If you specify an instance ID that is not valid, such as an instance that is not a burstable performance instance, an error is returned. Recently terminated instances might appear in the returned results. This interval is usually less than one hour. If an Availability Zone is experiencing a service disruption and you specify instance IDs in the affected zone, or do not specify any instance IDs at all, the call fails. If you specify only instance IDs in an unaffected zone, the call works normally. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module DescribeInstanceCreditSpecificationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc "The filters. instance-id - The ID of the instance."];
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The instance IDs. Default: Describes all your instances. Constraints: Maximum 1000 explicitly specified instance IDs."];
      maxResults: DescribeInstanceCreditSpecificationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. You cannot specify this parameter and the instance IDs parameter in the same call."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?instanceIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; instanceIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeInstanceCreditSpecificationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeInstanceCreditSpecificationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?instanceIds ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeInstanceCreditSpecificationsMaxResults.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?instanceIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the credit option for CPU usage of the specified burstable performance instances. The credit options are standard and unlimited. If you do not specify an instance ID, Amazon EC2 returns burstable performance instances with the unlimited credit option, as well as instances that were previously configured as T2, T3, and T3a with the unlimited credit option. For example, if you resize a T2 instance, while it is configured as unlimited, to an M4 instance, Amazon EC2 returns the M4 instance. If you specify one or more instance IDs, Amazon EC2 returns the credit option (standard or unlimited) of those instances. If you specify an instance ID that is not valid, such as an instance that is not a burstable performance instance, an error is returned. Recently terminated instances might appear in the returned results. This interval is usually less than one hour. If an Availability Zone is experiencing a service disruption and you specify instance IDs in the affected zone, or do not specify any instance IDs at all, the call fails. If you specify only instance IDs in an unaffected zone, the call works normally. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module DescribeInstanceConnectEndpointsResult =
  struct
    type nonrec t =
      {
      instanceConnectEndpoints: InstanceConnectEndpointSet.t option
        [@ocaml.doc "Information about the EC2 Instance Connect Endpoints."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceConnectEndpoints =
      fun ?nextToken -> fun () -> { instanceConnectEndpoints; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceConnectEndpointSet",
           (Option.map x.instanceConnectEndpoints
              ~f:InstanceConnectEndpointSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceConnectEndpoints =
        (Option.map ~f:InstanceConnectEndpointSet.of_xml)
          (Xml.child xml_arg0 "instanceConnectEndpointSet") in
      make ?nextToken ?instanceConnectEndpoints ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let instanceConnectEndpoints =
        field_map json__ "InstanceConnectEndpoints"
          InstanceConnectEndpointSet.of_json in
      make ?nextToken ?instanceConnectEndpoints ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified EC2 Instance Connect Endpoints or all EC2 Instance Connect Endpoints."]
module DescribeInstanceConnectEndpointsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: InstanceConnectEndpointMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. instance-connect-endpoint-id - The ID of the EC2 Instance Connect Endpoint. state - The state of the EC2 Instance Connect Endpoint (create-in-progress | create-complete | create-failed | delete-in-progress | delete-complete | delete-failed). subnet-id - The ID of the subnet in which the EC2 Instance Connect Endpoint was created. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. tag-value - The value of a tag assigned to the resource. Use this filter to find all resources that have a tag with a specific value, regardless of tag key. vpc-id - The ID of the VPC in which the EC2 Instance Connect Endpoint was created."];
      instanceConnectEndpointIds: ValueStringList.t option
        [@ocaml.doc "One or more EC2 Instance Connect Endpoint IDs."]}
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?instanceConnectEndpointIds ->
              fun () ->
                {
                  dryRun;
                  maxResults;
                  nextToken;
                  filters;
                  instanceConnectEndpointIds
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:InstanceConnectEndpointMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("InstanceConnectEndpointId",
          (Option.map x.instanceConnectEndpointIds
             ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceConnectEndpointIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "InstanceConnectEndpointId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:InstanceConnectEndpointMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?instanceConnectEndpointIds ?filters ?nextToken ?maxResults
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceConnectEndpointIds =
        field_map json__ "InstanceConnectEndpointIds" ValueStringList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          InstanceConnectEndpointMaxResults.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?instanceConnectEndpointIds ?filters ?nextToken ?maxResults
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified EC2 Instance Connect Endpoints or all EC2 Instance Connect Endpoints."]
module DescribeInstanceAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      attribute: InstanceAttributeName.t
        [@ocaml.doc
          "The instance attribute. Note that the enaSupport attribute is not supported."]}
    let context_ = "DescribeInstanceAttributeRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~attribute -> fun () -> { dryRun; instanceId; attribute }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("attribute", (Some (InstanceAttributeName.to_value x.attribute)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attribute =
        InstanceAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attribute") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~attribute ~instanceId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attribute =
        field_map_exn json__ "Attribute" InstanceAttributeName.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~attribute ~instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified attribute of the specified instance. You can specify only one attribute at a time. Available attributes include SQL license exemption configuration for instances registered with the SQL LE service."]
module DescribeImportSnapshotTasksResult =
  struct
    type nonrec t =
      {
      importSnapshotTasks: ImportSnapshotTaskList.t option
        [@ocaml.doc
          "A list of zero or more import snapshot tasks that are currently active or were completed or canceled in the previous 7 days."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to get the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?importSnapshotTasks =
      fun ?nextToken -> fun () -> { importSnapshotTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("importSnapshotTaskSet",
           (Option.map x.importSnapshotTasks
              ~f:ImportSnapshotTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let importSnapshotTasks =
        (Option.map ~f:ImportSnapshotTaskList.of_xml)
          (Xml.child xml_arg0 "importSnapshotTaskSet") in
      make ?nextToken ?importSnapshotTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let importSnapshotTasks =
        field_map json__ "ImportSnapshotTasks" ImportSnapshotTaskList.of_json in
      make ?nextToken ?importSnapshotTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes your import snapshot tasks."]
module DescribeImportSnapshotTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option [@ocaml.doc "The filters."];
      importTaskIds: ImportSnapshotTaskIdList.t option
        [@ocaml.doc "A list of import snapshot task IDs."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned NextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "A token that indicates the next page of results."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?importTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; importTaskIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filters", (Option.map x.filters ~f:FilterList.to_value));
        ("ImportTaskId",
          (Option.map x.importTaskIds ~f:ImportSnapshotTaskIdList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let importTaskIds =
        (Option.map ~f:ImportSnapshotTaskIdList.of_xml)
          (Xml.child xml_arg0 "ImportTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filters") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let importTaskIds =
        field_map json__ "ImportTaskIds" ImportSnapshotTaskIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes your import snapshot tasks."]
module DescribeImportImageTasksResult =
  struct
    type nonrec t =
      {
      importImageTasks: ImportImageTaskList.t option
        [@ocaml.doc
          "A list of zero or more import image tasks that are currently active or were completed or canceled in the previous 7 days."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to get the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?importImageTasks =
      fun ?nextToken -> fun () -> { importImageTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("importImageTaskSet",
           (Option.map x.importImageTasks ~f:ImportImageTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let importImageTasks =
        (Option.map ~f:ImportImageTaskList.of_xml)
          (Xml.child xml_arg0 "importImageTaskSet") in
      make ?nextToken ?importImageTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let importImageTasks =
        field_map json__ "ImportImageTasks" ImportImageTaskList.of_json in
      make ?nextToken ?importImageTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Displays details about an import virtual machine or import snapshot tasks that are already created."]
module DescribeImportImageTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "Filter tasks using the task-state filter and one of the following values: active, completed, deleting, or deleted."];
      importTaskIds: ImportTaskIdList.t option
        [@ocaml.doc "The IDs of the import image tasks."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call."];
      nextToken: String_.t option
        [@ocaml.doc "A token that indicates the next page of results."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?importTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; importTaskIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filters", (Option.map x.filters ~f:FilterList.to_value));
        ("ImportTaskId",
          (Option.map x.importTaskIds ~f:ImportTaskIdList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let importTaskIds =
        (Option.map ~f:ImportTaskIdList.of_xml)
          (Xml.child xml_arg0 "ImportTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filters") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let importTaskIds =
        field_map json__ "ImportTaskIds" ImportTaskIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?importTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Displays details about an import virtual machine or import snapshot tasks that are already created."]
module DescribeImagesResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      images: ImageList.t option [@ocaml.doc "Information about the images."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?images -> fun () -> { nextToken; images }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("imagesSet", (Option.map x.images ~f:ImageList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let images =
        (Option.map ~f:ImageList.of_xml) (Xml.child xml_arg0 "imagesSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?images ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let images = field_map json__ "Images" ImageList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?images ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified images (AMIs, AKIs, and ARIs) available to you or all of the images available to you. The images available to you include public images, private images that you own, and private images owned by other Amazon Web Services accounts for which you have explicit launch permissions. Recently deregistered images appear in the returned results for a short interval and then return empty results. After all instances that reference a deregistered AMI are terminated, specifying the ID of the image will eventually return an error indicating that the AMI ID cannot be found. When Allowed AMIs is set to enabled, only allowed images are returned in the results, with the imageAllowed field set to true for each image. In audit-mode, the imageAllowed field is set to true for images that meet the account's Allowed AMIs criteria, and false for images that don't meet the criteria. For more information, see Allowed AMIs. The Amazon EC2 API follows an eventual consistency model. This means that the result of an API command you run that creates or modifies resources might not be immediately available to all subsequent commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. We strongly recommend using only paginated requests. Unpaginated requests are susceptible to throttling and timeouts. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeImagesRequest =
  struct
    type nonrec t =
      {
      executableUsers: ExecutableByStringList.t option
        [@ocaml.doc
          "Scopes the images by users with explicit launch permissions. Specify an Amazon Web Services account ID, self (the sender of the request), or all (public AMIs). If you specify an Amazon Web Services account ID that is not your own, only AMIs shared with that specific Amazon Web Services account ID are returned. However, AMIs that are shared with the account\226\128\153s organization or organizational unit (OU) are not returned. If you specify self or your own Amazon Web Services account ID, AMIs shared with your account are returned. In addition, AMIs that are shared with the organization or OU of which you are member are also returned. If you specify all, all public AMIs are returned."];
      imageIds: ImageIdStringList.t option
        [@ocaml.doc
          "The image IDs. Default: Describes all images available to you."];
      owners: OwnerStringList.t option
        [@ocaml.doc
          "Scopes the results to images with the specified owners. You can specify a combination of Amazon Web Services account IDs, self, amazon, aws-backup-vault, and aws-marketplace. If you omit this parameter, the results include all images for which you have launch permissions, regardless of ownership."];
      includeDeprecated: Boolean.t option
        [@ocaml.doc
          "Specifies whether to include deprecated AMIs. Default: No deprecated AMIs are included in the response. If you are the AMI owner, all deprecated AMIs appear in the response regardless of what you specify for this parameter."];
      includeDisabled: Boolean.t option
        [@ocaml.doc
          "Specifies whether to include disabled AMIs. Default: No disabled AMIs are included in the response."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. architecture - The image architecture (i386 | x86_64 | arm64 | x86_64_mac | arm64_mac). block-device-mapping.delete-on-termination - A Boolean value that indicates whether the Amazon EBS volume is deleted on instance termination. block-device-mapping.device-name - The device name specified in the block device mapping (for example, /dev/sdh or xvdh). block-device-mapping.snapshot-id - The ID of the snapshot used for the Amazon EBS volume. block-device-mapping.volume-size - The volume size of the Amazon EBS volume, in GiB. block-device-mapping.volume-type - The volume type of the Amazon EBS volume (io1 | io2 | gp2 | gp3 | sc1 | st1 | standard). block-device-mapping.encrypted - A Boolean that indicates whether the Amazon EBS volume is encrypted. creation-date - The time when the image was created, in the ISO 8601 format in the UTC time zone (YYYY-MM-DDThh:mm:ss.sssZ), for example, 2021-09-29T11:04:43.305Z. You can use a wildcard (*), for example, 2021-09-29T*, which matches an entire day. description - The description of the image (provided during image creation). ena-support - A Boolean that indicates whether enhanced networking with ENA is enabled. free-tier-eligible - A Boolean that indicates whether this image can be used under the Amazon Web Services Free Tier (true | false). hypervisor - The hypervisor type (ovm | xen). image-allowed - A Boolean that indicates whether the image meets the criteria specified for Allowed AMIs. image-id - The ID of the image. image-type - The image type (machine | kernel | ramdisk). is-public - A Boolean that indicates whether the image is public. kernel-id - The kernel ID. manifest-location - The location of the image manifest. name - The name of the AMI (provided during image creation). owner-alias - The owner alias (amazon | aws-backup-vault | aws-marketplace). The valid aliases are defined in an Amazon-maintained list. This is not the Amazon Web Services account alias that can be set using the IAM console. We recommend that you use the Owner request parameter instead of this filter. owner-id - The Amazon Web Services account ID of the owner. We recommend that you use the Owner request parameter instead of this filter. platform - The platform. The only supported value is windows. product-code - The product code. product-code.type - The type of the product code (marketplace). ramdisk-id - The RAM disk ID. root-device-name - The device name of the root device volume (for example, /dev/sda1). root-device-type - The type of the root device volume (ebs | instance-store). source-image-id - The ID of the source AMI from which the AMI was created. source-image-region - The Region of the source AMI. source-instance-id - The ID of the instance that the AMI was created from if the AMI was created using CreateImage. This filter is applicable only if the AMI was created using CreateImage. state - The state of the image (available | pending | failed). state-reason-code - The reason code for the state change. state-reason-message - The message for the state change. sriov-net-support - A value of simple indicates that enhanced networking with the Intel 82599 VF interface is enabled. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. virtualization-type - The virtualization type (paravirtual | hvm)."]}
    let make ?executableUsers =
      fun ?imageIds ->
        fun ?owners ->
          fun ?includeDeprecated ->
            fun ?includeDisabled ->
              fun ?maxResults ->
                fun ?nextToken ->
                  fun ?dryRun ->
                    fun ?filters ->
                      fun () ->
                        {
                          executableUsers;
                          imageIds;
                          owners;
                          includeDeprecated;
                          includeDisabled;
                          maxResults;
                          nextToken;
                          dryRun;
                          filters
                        }
    let to_value x =
      structure_to_value
        [("ExecutableBy",
           (Option.map x.executableUsers ~f:ExecutableByStringList.to_value));
        ("ImageId", (Option.map x.imageIds ~f:ImageIdStringList.to_value));
        ("Owner", (Option.map x.owners ~f:OwnerStringList.to_value));
        ("IncludeDeprecated",
          (Option.map x.includeDeprecated ~f:Boolean.to_value));
        ("IncludeDisabled",
          (Option.map x.includeDisabled ~f:Boolean.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let includeDisabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "IncludeDisabled") in
      let includeDeprecated =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeDeprecated") in
      let owners =
        (Option.map ~f:OwnerStringList.of_xml) (Xml.child xml_arg0 "Owner") in
      let imageIds =
        (Option.map ~f:ImageIdStringList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      let executableUsers =
        (Option.map ~f:ExecutableByStringList.of_xml)
          (Xml.child xml_arg0 "ExecutableBy") in
      make ?filters ?dryRun ?nextToken ?maxResults ?includeDisabled
        ?includeDeprecated ?owners ?imageIds ?executableUsers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let includeDisabled =
        field_map json__ "IncludeDisabled" Boolean.of_json in
      let includeDeprecated =
        field_map json__ "IncludeDeprecated" Boolean.of_json in
      let owners = field_map json__ "Owners" OwnerStringList.of_json in
      let imageIds = field_map json__ "ImageIds" ImageIdStringList.of_json in
      let executableUsers =
        field_map json__ "ExecutableUsers" ExecutableByStringList.of_json in
      make ?filters ?dryRun ?nextToken ?maxResults ?includeDisabled
        ?includeDeprecated ?owners ?imageIds ?executableUsers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified images (AMIs, AKIs, and ARIs) available to you or all of the images available to you. The images available to you include public images, private images that you own, and private images owned by other Amazon Web Services accounts for which you have explicit launch permissions. Recently deregistered images appear in the returned results for a short interval and then return empty results. After all instances that reference a deregistered AMI are terminated, specifying the ID of the image will eventually return an error indicating that the AMI ID cannot be found. When Allowed AMIs is set to enabled, only allowed images are returned in the results, with the imageAllowed field set to true for each image. In audit-mode, the imageAllowed field is set to true for images that meet the account's Allowed AMIs criteria, and false for images that don't meet the criteria. For more information, see Allowed AMIs. The Amazon EC2 API follows an eventual consistency model. This means that the result of an API command you run that creates or modifies resources might not be immediately available to all subsequent commands you run. For guidance on how to manage eventual consistency, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. We strongly recommend using only paginated requests. Unpaginated requests are susceptible to throttling and timeouts. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeImageUsageReportsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      imageUsageReports: ImageUsageReportList.t option
        [@ocaml.doc "The image usage reports."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?imageUsageReports -> fun () -> { nextToken; imageUsageReports }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("imageUsageReportSet",
          (Option.map x.imageUsageReports ~f:ImageUsageReportList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageUsageReports =
        (Option.map ~f:ImageUsageReportList.of_xml)
          (Xml.child xml_arg0 "imageUsageReportSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?imageUsageReports ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let imageUsageReports =
        field_map json__ "ImageUsageReports" ImageUsageReportList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?imageUsageReports ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration and status of image usage reports, filtered by report IDs or image IDs. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module DescribeImageUsageReportsRequest =
  struct
    type nonrec t =
      {
      imageIds: DescribeImageUsageReportsImageIdStringList.t option
        [@ocaml.doc
          "The IDs of the images for filtering the reports. If specified, only reports containing these images are returned."];
      reportIds: ImageUsageReportIdStringList.t option
        [@ocaml.doc "The IDs of the image usage reports."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. creation-time - The time when the report was created, in the ISO 8601 format in the UTC time zone (YYYY-MM-DDThh:mm:ss.sssZ), for example, 2025-11-29T11:04:43.305Z. You can use a wildcard (*), for example, 2025-11-29T*, which matches an entire day. state - The state of the report (available | pending | error). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: DescribeImageUsageReportsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."]}
    let make ?imageIds =
      fun ?reportIds ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?dryRun ->
              fun ?maxResults ->
                fun () ->
                  {
                    imageIds;
                    reportIds;
                    nextToken;
                    filters;
                    dryRun;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("ImageId",
           (Option.map x.imageIds
              ~f:DescribeImageUsageReportsImageIdStringList.to_value));
        ("ReportId",
          (Option.map x.reportIds ~f:ImageUsageReportIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeImageUsageReportsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeImageUsageReportsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let reportIds =
        (Option.map ~f:ImageUsageReportIdStringList.of_xml)
          (Xml.child xml_arg0 "ReportId") in
      let imageIds =
        (Option.map ~f:DescribeImageUsageReportsImageIdStringList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      make ?maxResults ?dryRun ?filters ?nextToken ?reportIds ?imageIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeImageUsageReportsMaxResults.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let reportIds =
        field_map json__ "ReportIds" ImageUsageReportIdStringList.of_json in
      let imageIds =
        field_map json__ "ImageIds"
          DescribeImageUsageReportsImageIdStringList.of_json in
      make ?maxResults ?dryRun ?filters ?nextToken ?reportIds ?imageIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration and status of image usage reports, filtered by report IDs or image IDs. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module DescribeImageUsageReportEntriesResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      imageUsageReportEntries: ImageUsageReportEntryList.t option
        [@ocaml.doc "The content of the usage reports."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?imageUsageReportEntries ->
        fun () -> { nextToken; imageUsageReportEntries }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("imageUsageReportEntrySet",
          (Option.map x.imageUsageReportEntries
             ~f:ImageUsageReportEntryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageUsageReportEntries =
        (Option.map ~f:ImageUsageReportEntryList.of_xml)
          (Xml.child xml_arg0 "imageUsageReportEntrySet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?imageUsageReportEntries ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let imageUsageReportEntries =
        field_map json__ "ImageUsageReportEntries"
          ImageUsageReportEntryList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?imageUsageReportEntries ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the entries in image usage reports, showing how your images are used across other Amazon Web Services accounts. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module DescribeImageUsageReportEntriesRequest =
  struct
    type nonrec t =
      {
      imageIds: DescribeImageUsageReportsImageIdStringList.t option
        [@ocaml.doc
          "The IDs of the images for filtering the report entries. If specified, only report entries containing these images are returned."];
      reportIds: ImageUsageReportIdStringList.t option
        [@ocaml.doc "The IDs of the usage reports."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. account-id - A 12-digit Amazon Web Services account ID. creation-time - The time when the report was created, in the ISO 8601 format in the UTC time zone (YYYY-MM-DDThh:mm:ss.sssZ), for example, 2025-11-29T11:04:43.305Z. You can use a wildcard (*), for example, 2025-11-29T*, which matches an entire day. resource-type - The resource type (ec2:Instance | ec2:LaunchTemplate)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: DescribeImageUsageReportEntriesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."]}
    let make ?imageIds =
      fun ?reportIds ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?dryRun ->
              fun ?maxResults ->
                fun () ->
                  {
                    imageIds;
                    reportIds;
                    nextToken;
                    filters;
                    dryRun;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("ImageId",
           (Option.map x.imageIds
              ~f:DescribeImageUsageReportsImageIdStringList.to_value));
        ("ReportId",
          (Option.map x.reportIds ~f:ImageUsageReportIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeImageUsageReportEntriesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeImageUsageReportEntriesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let reportIds =
        (Option.map ~f:ImageUsageReportIdStringList.of_xml)
          (Xml.child xml_arg0 "ReportId") in
      let imageIds =
        (Option.map ~f:DescribeImageUsageReportsImageIdStringList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      make ?maxResults ?dryRun ?filters ?nextToken ?reportIds ?imageIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeImageUsageReportEntriesMaxResults.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let reportIds =
        field_map json__ "ReportIds" ImageUsageReportIdStringList.of_json in
      let imageIds =
        field_map json__ "ImageIds"
          DescribeImageUsageReportsImageIdStringList.of_json in
      make ?maxResults ?dryRun ?filters ?nextToken ?reportIds ?imageIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the entries in image usage reports, showing how your images are used across other Amazon Web Services accounts. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module DescribeImageReferencesResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      imageReferences: ImageReferenceList.t option
        [@ocaml.doc
          "The resources that are referencing the specified images."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?imageReferences -> fun () -> { nextToken; imageReferences }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("imageReferenceSet",
          (Option.map x.imageReferences ~f:ImageReferenceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageReferences =
        (Option.map ~f:ImageReferenceList.of_xml)
          (Xml.child xml_arg0 "imageReferenceSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?imageReferences ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let imageReferences =
        field_map json__ "ImageReferences" ImageReferenceList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?imageReferences ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your Amazon Web Services resources that are referencing the specified images. For more information, see Identify your resources referencing specified AMIs in the Amazon EC2 User Guide."]
module DescribeImageReferencesRequest =
  struct
    type nonrec t =
      {
      imageIds: DescribeImageReferencesImageIdStringList.t
        [@ocaml.doc
          "The IDs of the images to check for resource references."];
      includeAllResourceTypes: Boolean.t option
        [@ocaml.doc
          "Specifies whether to check all supported Amazon Web Services resource types for image references. When specified, default values are applied for ResourceTypeOptions. For the default values, see How AMI reference checks work in the Amazon EC2 User Guide. If you also specify ResourceTypes with ResourceTypeOptions, your specified values override the default values. Supported resource types: ec2:Instance | ec2:LaunchTemplate | ssm:Parameter | imagebuilder:ImageRecipe | imagebuilder:ContainerRecipe Either IncludeAllResourceTypes or ResourceTypes must be specified."];
      resourceTypes: ResourceTypeRequestList.t option
        [@ocaml.doc
          "The Amazon Web Services resource types to check for image references. Either IncludeAllResourceTypes or ResourceTypes must be specified."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: DescribeImageReferencesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."]}
    let context_ = "DescribeImageReferencesRequest"
    let make ?includeAllResourceTypes =
      fun ?resourceTypes ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ?maxResults ->
              fun ~imageIds ->
                fun () ->
                  {
                    includeAllResourceTypes;
                    resourceTypes;
                    nextToken;
                    dryRun;
                    maxResults;
                    imageIds
                  }
    let to_value x =
      structure_to_value
        [("ImageId",
           (Some
              (DescribeImageReferencesImageIdStringList.to_value x.imageIds)));
        ("IncludeAllResourceTypes",
          (Option.map x.includeAllResourceTypes ~f:Boolean.to_value));
        ("ResourceType",
          (Option.map x.resourceTypes ~f:ResourceTypeRequestList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeImageReferencesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeImageReferencesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let resourceTypes =
        (Option.map ~f:ResourceTypeRequestList.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let includeAllResourceTypes =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeAllResourceTypes") in
      let imageIds =
        DescribeImageReferencesImageIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?maxResults ?dryRun ?nextToken ?resourceTypes
        ?includeAllResourceTypes ~imageIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeImageReferencesMaxResults.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let resourceTypes =
        field_map json__ "ResourceTypes" ResourceTypeRequestList.of_json in
      let includeAllResourceTypes =
        field_map json__ "IncludeAllResourceTypes" Boolean.of_json in
      let imageIds =
        field_map_exn json__ "ImageIds"
          DescribeImageReferencesImageIdStringList.of_json in
      make ?maxResults ?dryRun ?nextToken ?resourceTypes
        ?includeAllResourceTypes ~imageIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your Amazon Web Services resources that are referencing the specified images. For more information, see Identify your resources referencing specified AMIs in the Amazon EC2 User Guide."]
module DescribeImageAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: ImageAttributeName.t
        [@ocaml.doc
          "The AMI attribute. Note: The blockDeviceMapping attribute is deprecated. Using this attribute returns the Client.AuthFailure error. To get information about the block device mappings for an AMI, describe the image instead."];
      imageId: ImageId.t [@ocaml.doc "The ID of the AMI."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DescribeImageAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~imageId -> fun () -> { dryRun; attribute; imageId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (ImageAttributeName.to_value x.attribute)));
        ("ImageId", (Some (ImageId.to_value x.imageId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      let attribute =
        ImageAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~imageId ~attribute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let imageId = field_map_exn json__ "ImageId" ImageId.of_json in
      let attribute =
        field_map_exn json__ "Attribute" ImageAttributeName.of_json in
      make ?dryRun ~imageId ~attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeImageAttribute."]
module DescribeIdentityIdFormatResult =
  struct
    type nonrec t =
      {
      statuses: IdFormatList.t option
        [@ocaml.doc "Information about the ID format for the resources."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?statuses = fun () -> { statuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("statusSet", (Option.map x.statuses ~f:IdFormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:IdFormatList.of_xml) (Xml.child xml_arg0 "statusSet") in
      make ?statuses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statuses = field_map json__ "Statuses" IdFormatList.of_json in
      make ?statuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the ID format settings for resources for the specified IAM user, IAM role, or root user. For example, you can view the resource types that are enabled for longer IDs. This request only returns information about resource types whose ID formats can be modified; it does not return information about other resource types. For more information, see Resource IDs in the Amazon Elastic Compute Cloud User Guide. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. These settings apply to the principal specified in the request. They do not apply to the principal that makes the request."]
module DescribeIdentityIdFormatRequest =
  struct
    type nonrec t =
      {
      resource: String_.t option
        [@ocaml.doc
          "The type of resource: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway"];
      principalArn: String_.t
        [@ocaml.doc
          "The ARN of the principal, which can be an IAM role, IAM user, or the root user."]}
    let context_ = "DescribeIdentityIdFormatRequest"
    let make ?resource =
      fun ~principalArn -> fun () -> { resource; principalArn }
    let to_value x =
      structure_to_value
        [("resource", (Option.map x.resource ~f:String_.to_value));
        ("principalArn", (Some (String_.to_value x.principalArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "principalArn") in
      let resource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resource") in
      make ~principalArn ?resource ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let principalArn = field_map_exn json__ "PrincipalArn" String_.of_json in
      let resource = field_map json__ "Resource" String_.of_json in
      make ~principalArn ?resource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the ID format settings for resources for the specified IAM user, IAM role, or root user. For example, you can view the resource types that are enabled for longer IDs. This request only returns information about resource types whose ID formats can be modified; it does not return information about other resource types. For more information, see Resource IDs in the Amazon Elastic Compute Cloud User Guide. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. These settings apply to the principal specified in the request. They do not apply to the principal that makes the request."]
module DescribeIdFormatResult =
  struct
    type nonrec t =
      {
      statuses: IdFormatList.t option
        [@ocaml.doc "Information about the ID format for the resource."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?statuses = fun () -> { statuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("statusSet", (Option.map x.statuses ~f:IdFormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:IdFormatList.of_xml) (Xml.child xml_arg0 "statusSet") in
      make ?statuses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statuses = field_map json__ "Statuses" IdFormatList.of_json in
      make ?statuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the ID format settings for your resources on a per-Region basis, for example, to view which resource types are enabled for longer IDs. This request only returns information about resource types whose ID formats can be modified; it does not return information about other resource types. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. These settings apply to the IAM user who makes the request; they do not apply to the entire Amazon Web Services account. By default, an IAM user defaults to the same settings as the root user, unless they explicitly override the settings by running the ModifyIdFormat command. Resources created with longer IDs are visible to all IAM users, regardless of these settings and provided that they have permission to use the relevant Describe command for the resource type."]
module DescribeIdFormatRequest =
  struct
    type nonrec t =
      {
      resource: String_.t option
        [@ocaml.doc
          "The type of resource: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway"]}
    let make ?resource = fun () -> { resource }
    let to_value x =
      structure_to_value
        [("Resource", (Option.map x.resource ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Resource") in
      make ?resource ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resource = field_map json__ "Resource" String_.of_json in
      make ?resource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the ID format settings for your resources on a per-Region basis, for example, to view which resource types are enabled for longer IDs. This request only returns information about resource types whose ID formats can be modified; it does not return information about other resource types. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. These settings apply to the IAM user who makes the request; they do not apply to the entire Amazon Web Services account. By default, an IAM user defaults to the same settings as the root user, unless they explicitly override the settings by running the ModifyIdFormat command. Resources created with longer IDs are visible to all IAM users, regardless of these settings and provided that they have permission to use the relevant Describe command for the resource type."]
module DescribeIamInstanceProfileAssociationsResult =
  struct
    type nonrec t =
      {
      iamInstanceProfileAssociations:
        IamInstanceProfileAssociationSet.t option
        [@ocaml.doc
          "Information about the IAM instance profile associations."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?iamInstanceProfileAssociations =
      fun ?nextToken ->
        fun () -> { iamInstanceProfileAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("iamInstanceProfileAssociationSet",
           (Option.map x.iamInstanceProfileAssociations
              ~f:IamInstanceProfileAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let iamInstanceProfileAssociations =
        (Option.map ~f:IamInstanceProfileAssociationSet.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfileAssociationSet") in
      make ?nextToken ?iamInstanceProfileAssociations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let iamInstanceProfileAssociations =
        field_map json__ "IamInstanceProfileAssociations"
          IamInstanceProfileAssociationSet.of_json in
      make ?nextToken ?iamInstanceProfileAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes your IAM instance profile associations."]
module DescribeIamInstanceProfileAssociationsRequest =
  struct
    type nonrec t =
      {
      associationIds: AssociationIdList.t option
        [@ocaml.doc "The IAM instance profile associations."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. instance-id - The ID of the instance. state - The state of the association (associating | associated | disassociating)."];
      maxResults: DescribeIamInstanceProfileAssociationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."]}
    let make ?associationIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { associationIds; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("AssociationId",
           (Option.map x.associationIds ~f:AssociationIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeIamInstanceProfileAssociationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map
           ~f:DescribeIamInstanceProfileAssociationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let associationIds =
        (Option.map ~f:AssociationIdList.of_xml)
          (Xml.child xml_arg0 "AssociationId") in
      make ?nextToken ?maxResults ?filters ?associationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeIamInstanceProfileAssociationsMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let associationIds =
        field_map json__ "AssociationIds" AssociationIdList.of_json in
      make ?nextToken ?maxResults ?filters ?associationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes your IAM instance profile associations."]
module DescribeHostsResult =
  struct
    type nonrec t =
      {
      hosts: HostList.t option
        [@ocaml.doc "Information about the Dedicated Hosts."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?hosts = fun ?nextToken -> fun () -> { hosts; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("hostSet", (Option.map x.hosts ~f:HostList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let hosts =
        (Option.map ~f:HostList.of_xml) (Xml.child xml_arg0 "hostSet") in
      make ?nextToken ?hosts ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let hosts = field_map json__ "Hosts" HostList.of_json in
      make ?nextToken ?hosts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified Dedicated Hosts or all your Dedicated Hosts. The results describe only the Dedicated Hosts in the Region you're currently using. All listed instances consume capacity on your Dedicated Host. Dedicated Hosts that have recently been released are listed with the state released."]
module DescribeHostsRequest =
  struct
    type nonrec t =
      {
      hostIds: RequestHostIdList.t option
        [@ocaml.doc
          "The IDs of the Dedicated Hosts. The IDs are used for targeted instance launches."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error. You cannot specify this parameter and the host IDs parameter in the same request."];
      filter: FilterList.t option
        [@ocaml.doc
          "The filters. auto-placement - Whether auto-placement is enabled or disabled (on | off). availability-zone - The Availability Zone of the host. client-token - The idempotency token that you provided when you allocated the host. host-reservation-id - The ID of the reservation assigned to this host. instance-type - The instance type size that the Dedicated Host is configured to support. state - The allocation state of the Dedicated Host (available | under-assessment | permanent-failure | released | released-permanent-failure). tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."]}
    let make ?hostIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filter -> fun () -> { hostIds; nextToken; maxResults; filter }
    let to_value x =
      structure_to_value
        [("hostId", (Option.map x.hostIds ~f:RequestHostIdList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("filter", (Option.map x.filter ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filter") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let hostIds =
        (Option.map ~f:RequestHostIdList.of_xml)
          (Xml.child xml_arg0 "hostId") in
      make ?filter ?maxResults ?nextToken ?hostIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filter = field_map json__ "Filter" FilterList.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let hostIds = field_map json__ "HostIds" RequestHostIdList.of_json in
      make ?filter ?maxResults ?nextToken ?hostIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified Dedicated Hosts or all your Dedicated Hosts. The results describe only the Dedicated Hosts in the Region you're currently using. All listed instances consume capacity on your Dedicated Host. Dedicated Hosts that have recently been released are listed with the state released."]
module DescribeHostReservationsResult =
  struct
    type nonrec t =
      {
      hostReservationSet: HostReservationSet.t option
        [@ocaml.doc "Details about the reservation's configuration."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?hostReservationSet =
      fun ?nextToken -> fun () -> { hostReservationSet; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("hostReservationSet",
           (Option.map x.hostReservationSet ~f:HostReservationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let hostReservationSet =
        (Option.map ~f:HostReservationSet.of_xml)
          (Xml.child xml_arg0 "hostReservationSet") in
      make ?nextToken ?hostReservationSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let hostReservationSet =
        field_map json__ "HostReservationSet" HostReservationSet.of_json in
      make ?nextToken ?hostReservationSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes reservations that are associated with Dedicated Hosts in your account."]
module DescribeHostReservationsRequest =
  struct
    type nonrec t =
      {
      filter: FilterList.t option
        [@ocaml.doc
          "The filters. instance-family - The instance family (for example, m4). payment-option - The payment option (NoUpfront | PartialUpfront | AllUpfront). state - The state of the reservation (payment-pending | payment-failed | active | retired). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      hostReservationIdSet: HostReservationIdSet.t option
        [@ocaml.doc "The host reservation IDs."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."]}
    let make ?filter =
      fun ?hostReservationIdSet ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { filter; hostReservationIdSet; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("HostReservationIdSet",
          (Option.map x.hostReservationIdSet ~f:HostReservationIdSet.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let hostReservationIdSet =
        (Option.map ~f:HostReservationIdSet.of_xml)
          (Xml.child xml_arg0 "HostReservationIdSet") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?nextToken ?maxResults ?hostReservationIdSet ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let hostReservationIdSet =
        field_map json__ "HostReservationIdSet" HostReservationIdSet.of_json in
      let filter = field_map json__ "Filter" FilterList.of_json in
      make ?nextToken ?maxResults ?hostReservationIdSet ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes reservations that are associated with Dedicated Hosts in your account."]
module DescribeHostReservationOfferingsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      offeringSet: HostOfferingSet.t option
        [@ocaml.doc "Information about the offerings."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?offeringSet -> fun () -> { nextToken; offeringSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("offeringSet",
          (Option.map x.offeringSet ~f:HostOfferingSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringSet =
        (Option.map ~f:HostOfferingSet.of_xml)
          (Xml.child xml_arg0 "offeringSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?offeringSet ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let offeringSet =
        field_map json__ "OfferingSet" HostOfferingSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?offeringSet ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Dedicated Host reservations that are available to purchase. The results describe all of the Dedicated Host reservation offerings, including offerings that might not match the instance family and Region of your Dedicated Hosts. When purchasing an offering, ensure that the instance family and Region of the offering matches that of the Dedicated Hosts with which it is to be associated. For more information about supported instance types, see Dedicated Hosts in the Amazon EC2 User Guide."]
module DescribeHostReservationOfferingsRequest =
  struct
    type nonrec t =
      {
      filter: FilterList.t option
        [@ocaml.doc
          "The filters. instance-family - The instance family of the offering (for example, m4). payment-option - The payment option (NoUpfront | PartialUpfront | AllUpfront)."];
      maxDuration: Integer.t option
        [@ocaml.doc
          "This is the maximum duration of the reservation to purchase, specified in seconds. Reservations are available in one-year and three-year terms. The number of seconds specified must be the number of seconds in a year (365x24x60x60) times one of the supported durations (1 or 3). For example, specify 94608000 for three years."];
      maxResults: DescribeHostReservationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error."];
      minDuration: Integer.t option
        [@ocaml.doc
          "This is the minimum duration of the reservation you'd like to purchase, specified in seconds. Reservations are available in one-year and three-year terms. The number of seconds specified must be the number of seconds in a year (365x24x60x60) times one of the supported durations (1 or 3). For example, specify 31536000 for one year."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      offeringId: OfferingId.t option
        [@ocaml.doc "The ID of the reservation offering."]}
    let make ?filter =
      fun ?maxDuration ->
        fun ?maxResults ->
          fun ?minDuration ->
            fun ?nextToken ->
              fun ?offeringId ->
                fun () ->
                  {
                    filter;
                    maxDuration;
                    maxResults;
                    minDuration;
                    nextToken;
                    offeringId
                  }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("MaxDuration", (Option.map x.maxDuration ~f:Integer.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeHostReservationsMaxResults.to_value));
        ("MinDuration", (Option.map x.minDuration ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("OfferingId", (Option.map x.offeringId ~f:OfferingId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringId =
        (Option.map ~f:OfferingId.of_xml) (Xml.child xml_arg0 "OfferingId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let minDuration =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MinDuration") in
      let maxResults =
        (Option.map ~f:DescribeHostReservationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let maxDuration =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxDuration") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?offeringId ?nextToken ?minDuration ?maxResults ?maxDuration
        ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let offeringId = field_map json__ "OfferingId" OfferingId.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let minDuration = field_map json__ "MinDuration" Integer.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeHostReservationsMaxResults.of_json in
      let maxDuration = field_map json__ "MaxDuration" Integer.of_json in
      let filter = field_map json__ "Filter" FilterList.of_json in
      make ?offeringId ?nextToken ?minDuration ?maxResults ?maxDuration
        ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Dedicated Host reservations that are available to purchase. The results describe all of the Dedicated Host reservation offerings, including offerings that might not match the instance family and Region of your Dedicated Hosts. When purchasing an offering, ensure that the instance family and Region of the offering matches that of the Dedicated Hosts with which it is to be associated. For more information about supported instance types, see Dedicated Hosts in the Amazon EC2 User Guide."]
module DescribeFpgaImagesResult =
  struct
    type nonrec t =
      {
      fpgaImages: FpgaImageList.t option
        [@ocaml.doc "Information about the FPGA images."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImages =
      fun ?nextToken -> fun () -> { fpgaImages; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageSet",
           (Option.map x.fpgaImages ~f:FpgaImageList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let fpgaImages =
        (Option.map ~f:FpgaImageList.of_xml)
          (Xml.child xml_arg0 "fpgaImageSet") in
      make ?nextToken ?fpgaImages ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let fpgaImages = field_map json__ "FpgaImages" FpgaImageList.of_json in
      make ?nextToken ?fpgaImages ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Amazon FPGA Images (AFIs) available to you. These include public AFIs, private AFIs that you own, and AFIs owned by other Amazon Web Services accounts for which you have load permissions."]
module DescribeFpgaImagesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fpgaImageIds: FpgaImageIdList.t option [@ocaml.doc "The AFI IDs."];
      owners: OwnerStringList.t option
        [@ocaml.doc
          "Filters the AFI by owner. Specify an Amazon Web Services account ID, self (owner is the sender of the request), or an Amazon Web Services owner alias (valid values are amazon | aws-marketplace)."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. create-time - The creation time of the AFI. fpga-image-id - The FPGA image identifier (AFI ID). fpga-image-global-id - The global FPGA image identifier (AGFI ID). name - The name of the AFI. owner-id - The Amazon Web Services account ID of the AFI owner. product-code - The product code. shell-version - The version of the Amazon Web Services Shell that was used to create the bitstream. state - The state of the AFI (pending | failed | available | unavailable). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. update-time - The time of the most recent update."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      maxResults: DescribeFpgaImagesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call."]}
    let make ?dryRun =
      fun ?fpgaImageIds ->
        fun ?owners ->
          fun ?filters ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun () ->
                  {
                    dryRun;
                    fpgaImageIds;
                    owners;
                    filters;
                    nextToken;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId",
          (Option.map x.fpgaImageIds ~f:FpgaImageIdList.to_value));
        ("Owner", (Option.map x.owners ~f:OwnerStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeFpgaImagesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeFpgaImagesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let owners =
        (Option.map ~f:OwnerStringList.of_xml) (Xml.child xml_arg0 "Owner") in
      let fpgaImageIds =
        (Option.map ~f:FpgaImageIdList.of_xml)
          (Xml.child xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?maxResults ?nextToken ?filters ?owners ?fpgaImageIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults" DescribeFpgaImagesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let owners = field_map json__ "Owners" OwnerStringList.of_json in
      let fpgaImageIds =
        field_map json__ "FpgaImageIds" FpgaImageIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?maxResults ?nextToken ?filters ?owners ?fpgaImageIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Amazon FPGA Images (AFIs) available to you. These include public AFIs, private AFIs that you own, and AFIs owned by other Amazon Web Services accounts for which you have load permissions."]
module DescribeFpgaImageAttributeResult =
  struct
    type nonrec t =
      {
      fpgaImageAttribute: FpgaImageAttribute.t option
        [@ocaml.doc "Information about the attribute."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageAttribute = fun () -> { fpgaImageAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageAttribute",
           (Option.map x.fpgaImageAttribute ~f:FpgaImageAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageAttribute =
        (Option.map ~f:FpgaImageAttribute.of_xml)
          (Xml.child xml_arg0 "fpgaImageAttribute") in
      make ?fpgaImageAttribute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fpgaImageAttribute =
        field_map json__ "FpgaImageAttribute" FpgaImageAttribute.of_json in
      make ?fpgaImageAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified attribute of the specified Amazon FPGA Image (AFI)."]
module DescribeFpgaImageAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fpgaImageId: FpgaImageId.t [@ocaml.doc "The ID of the AFI."];
      attribute: FpgaImageAttributeName.t [@ocaml.doc "The AFI attribute."]}
    let context_ = "DescribeFpgaImageAttributeRequest"
    let make ?dryRun =
      fun ~fpgaImageId ->
        fun ~attribute -> fun () -> { dryRun; fpgaImageId; attribute }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId", (Some (FpgaImageId.to_value x.fpgaImageId)));
        ("Attribute", (Some (FpgaImageAttributeName.to_value x.attribute)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attribute =
        FpgaImageAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      let fpgaImageId =
        FpgaImageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~attribute ~fpgaImageId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attribute =
        field_map_exn json__ "Attribute" FpgaImageAttributeName.of_json in
      let fpgaImageId =
        field_map_exn json__ "FpgaImageId" FpgaImageId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~attribute ~fpgaImageId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified attribute of the specified Amazon FPGA Image (AFI)."]
module DescribeFlowLogsResult =
  struct
    type nonrec t =
      {
      flowLogs: FlowLogSet.t option
        [@ocaml.doc "Information about the flow logs."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to request the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?flowLogs = fun ?nextToken -> fun () -> { flowLogs; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("flowLogSet", (Option.map x.flowLogs ~f:FlowLogSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let flowLogs =
        (Option.map ~f:FlowLogSet.of_xml) (Xml.child xml_arg0 "flowLogSet") in
      make ?nextToken ?flowLogs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let flowLogs = field_map json__ "FlowLogs" FlowLogSet.of_json in
      make ?nextToken ?flowLogs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more flow logs. To view the published flow log records, you must view the log destination. For example, the CloudWatch Logs log group, the Amazon S3 bucket, or the Kinesis Data Firehose delivery stream."]
module DescribeFlowLogsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filter: FilterList.t option
        [@ocaml.doc
          "One or more filters. deliver-log-status - The status of the logs delivery (SUCCESS | FAILED). log-destination-type - The type of destination for the flow log data (cloud-watch-logs | s3 | kinesis-data-firehose). flow-log-id - The ID of the flow log. log-group-name - The name of the log group. resource-id - The ID of the VPC, subnet, or network interface. traffic-type - The type of traffic (ACCEPT | REJECT | ALL). tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      flowLogIds: FlowLogIdList.t option
        [@ocaml.doc
          "One or more flow log IDs. Constraint: Maximum of 1000 flow log IDs."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to request the next page of items. Pagination continues from the end of the items returned by the previous request."]}
    let make ?dryRun =
      fun ?filter ->
        fun ?flowLogIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () -> { dryRun; filter; flowLogIds; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filter ~f:FilterList.to_value));
        ("FlowLogId", (Option.map x.flowLogIds ~f:FlowLogIdList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let flowLogIds =
        (Option.map ~f:FlowLogIdList.of_xml) (Xml.child xml_arg0 "FlowLogId") in
      let filter =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?flowLogIds ?filter ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let flowLogIds = field_map json__ "FlowLogIds" FlowLogIdList.of_json in
      let filter = field_map json__ "Filter" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?flowLogIds ?filter ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more flow logs. To view the published flow log records, you must view the log destination. For example, the CloudWatch Logs log group, the Amazon S3 bucket, or the Kinesis Data Firehose delivery stream."]
module DescribeFleetsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      fleets: FleetSet.t option
        [@ocaml.doc "Information about the EC2 Fleets."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?fleets -> fun () -> { nextToken; fleets }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleetSet", (Option.map x.fleets ~f:FleetSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleets =
        (Option.map ~f:FleetSet.of_xml) (Xml.child xml_arg0 "fleetSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?fleets ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleets = field_map json__ "Fleets" FleetSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?fleets ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified EC2 Fleet or all of your EC2 Fleets. If a fleet is of type instant, you must specify the fleet ID in the request, otherwise the fleet does not appear in the response. For more information, see Describe your EC2 Fleet in the Amazon EC2 User Guide."]
module DescribeFleetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      fleetIds: FleetIdSet.t option
        [@ocaml.doc
          "The IDs of the EC2 Fleets. If a fleet is of type instant, you must specify the fleet ID, otherwise it does not appear in the response."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. activity-status - The progress of the EC2 Fleet ( error | pending-fulfillment | pending-termination | fulfilled). excess-capacity-termination-policy - Indicates whether to terminate running instances if the target capacity is decreased below the current EC2 Fleet size (true | false). fleet-state - The state of the EC2 Fleet (submitted | active | deleted | failed | deleted-running | deleted-terminating | modifying). replace-unhealthy-instances - Indicates whether EC2 Fleet should replace unhealthy instances (true | false). type - The type of request (instant | request | maintain)."]}
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?fleetIds ->
            fun ?filters ->
              fun () -> { dryRun; maxResults; nextToken; fleetIds; filters }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("FleetId", (Option.map x.fleetIds ~f:FleetIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let fleetIds =
        (Option.map ~f:FleetIdSet.of_xml) (Xml.child xml_arg0 "FleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?fleetIds ?nextToken ?maxResults ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let fleetIds = field_map json__ "FleetIds" FleetIdSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ?fleetIds ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified EC2 Fleet or all of your EC2 Fleets. If a fleet is of type instant, you must specify the fleet ID in the request, otherwise the fleet does not appear in the response. For more information, see Describe your EC2 Fleet in the Amazon EC2 User Guide."]
module DescribeFleetInstancesResult =
  struct
    type nonrec t =
      {
      activeInstances: ActiveInstanceSet.t option
        [@ocaml.doc
          "The running instances. This list is refreshed periodically and might be out of date."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      fleetId: FleetId.t option [@ocaml.doc "The ID of the EC2 Fleet."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?activeInstances =
      fun ?nextToken ->
        fun ?fleetId -> fun () -> { activeInstances; nextToken; fleetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("activeInstanceSet",
           (Option.map x.activeInstances ~f:ActiveInstanceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleetId", (Option.map x.fleetId ~f:FleetId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "fleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let activeInstances =
        (Option.map ~f:ActiveInstanceSet.of_xml)
          (Xml.child xml_arg0 "activeInstanceSet") in
      make ?fleetId ?nextToken ?activeInstances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let activeInstances =
        field_map json__ "ActiveInstances" ActiveInstanceSet.of_json in
      make ?fleetId ?nextToken ?activeInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the running instances for the specified EC2 Fleet. Currently, DescribeFleetInstances does not support fleets of type instant. Instead, use DescribeFleets, specifying the instant fleet ID in the request. For more information, see Describe your EC2 Fleet in the Amazon EC2 User Guide."]
module DescribeFleetInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      fleetId: FleetId.t [@ocaml.doc "The ID of the EC2 Fleet."];
      filters: FilterList.t option
        [@ocaml.doc "The filters. instance-type - The instance type."]}
    let context_ = "DescribeFleetInstancesRequest"
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ~fleetId ->
              fun () -> { dryRun; maxResults; nextToken; filters; fleetId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ~fleetId ?nextToken ?maxResults ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetId.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ~fleetId ?nextToken ?maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the running instances for the specified EC2 Fleet. Currently, DescribeFleetInstances does not support fleets of type instant. Instead, use DescribeFleets, specifying the instant fleet ID in the request. For more information, see Describe your EC2 Fleet in the Amazon EC2 User Guide."]
module DescribeFleetHistoryResult =
  struct
    type nonrec t =
      {
      historyRecords: HistoryRecordSet.t option
        [@ocaml.doc
          "Information about the events in the history of the EC2 Fleet."];
      lastEvaluatedTime: DateTime.t option
        [@ocaml.doc
          "The last date and time for the events, in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ). All records up to this time were retrieved. If nextToken indicates that there are more items, this value is not present."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      fleetId: FleetId.t option [@ocaml.doc "The ID of the EC Fleet."];
      startTime: DateTime.t option
        [@ocaml.doc
          "The start date and time for the events, in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ)."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?historyRecords =
      fun ?lastEvaluatedTime ->
        fun ?nextToken ->
          fun ?fleetId ->
            fun ?startTime ->
              fun () ->
                {
                  historyRecords;
                  lastEvaluatedTime;
                  nextToken;
                  fleetId;
                  startTime
                }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("historyRecordSet",
           (Option.map x.historyRecords ~f:HistoryRecordSet.to_value));
        ("lastEvaluatedTime",
          (Option.map x.lastEvaluatedTime ~f:DateTime.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("startTime", (Option.map x.startTime ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "startTime") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "fleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let lastEvaluatedTime =
        (Option.map ~f:DateTime.of_xml)
          (Xml.child xml_arg0 "lastEvaluatedTime") in
      let historyRecords =
        (Option.map ~f:HistoryRecordSet.of_xml)
          (Xml.child xml_arg0 "historyRecordSet") in
      make ?startTime ?fleetId ?nextToken ?lastEvaluatedTime ?historyRecords
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let startTime = field_map json__ "StartTime" DateTime.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let lastEvaluatedTime =
        field_map json__ "LastEvaluatedTime" DateTime.of_json in
      let historyRecords =
        field_map json__ "HistoryRecords" HistoryRecordSet.of_json in
      make ?startTime ?fleetId ?nextToken ?lastEvaluatedTime ?historyRecords
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the events for the specified EC2 Fleet during the specified time. EC2 Fleet events are delayed by up to 30 seconds before they can be described. This ensures that you can query by the last evaluated time and not miss a recorded event. EC2 Fleet events are available for 48 hours. For more information, see Monitor fleet events using Amazon EventBridge in the Amazon EC2 User Guide."]
module DescribeFleetHistoryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      eventType: FleetEventType.t option
        [@ocaml.doc
          "The type of events to describe. By default, all events are described."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      fleetId: FleetId.t [@ocaml.doc "The ID of the EC2 Fleet."];
      startTime: DateTime.t
        [@ocaml.doc
          "The start date and time for the events, in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ)."]}
    let context_ = "DescribeFleetHistoryRequest"
    let make ?dryRun =
      fun ?eventType ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ~fleetId ->
              fun ~startTime ->
                fun () ->
                  {
                    dryRun;
                    eventType;
                    maxResults;
                    nextToken;
                    fleetId;
                    startTime
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("EventType", (Option.map x.eventType ~f:FleetEventType.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("StartTime", (Some (DateTime.to_value x.startTime)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startTime =
        DateTime.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let eventType =
        (Option.map ~f:FleetEventType.of_xml)
          (Xml.child xml_arg0 "EventType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~startTime ~fleetId ?nextToken ?maxResults ?eventType ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let startTime = field_map_exn json__ "StartTime" DateTime.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetId.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let eventType = field_map json__ "EventType" FleetEventType.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~startTime ~fleetId ?nextToken ?maxResults ?eventType ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the events for the specified EC2 Fleet during the specified time. EC2 Fleet events are delayed by up to 30 seconds before they can be described. This ensures that you can query by the last evaluated time and not miss a recorded event. EC2 Fleet events are available for 48 hours. For more information, see Monitor fleet events using Amazon EventBridge in the Amazon EC2 User Guide."]
module DescribeFastSnapshotRestoresResult =
  struct
    type nonrec t =
      {
      fastSnapshotRestores: DescribeFastSnapshotRestoreSuccessSet.t option
        [@ocaml.doc "Information about the state of fast snapshot restores."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fastSnapshotRestores =
      fun ?nextToken -> fun () -> { fastSnapshotRestores; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fastSnapshotRestoreSet",
           (Option.map x.fastSnapshotRestores
              ~f:DescribeFastSnapshotRestoreSuccessSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let fastSnapshotRestores =
        (Option.map ~f:DescribeFastSnapshotRestoreSuccessSet.of_xml)
          (Xml.child xml_arg0 "fastSnapshotRestoreSet") in
      make ?nextToken ?fastSnapshotRestores ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let fastSnapshotRestores =
        field_map json__ "FastSnapshotRestores"
          DescribeFastSnapshotRestoreSuccessSet.of_json in
      make ?nextToken ?fastSnapshotRestores ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the state of fast snapshot restores for your snapshots."]
module DescribeFastSnapshotRestoresRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. The possible values are: availability-zone: The Availability Zone of the snapshot. For example, us-east-2a. availability-zone-id: The ID of the Availability Zone of the snapshot. For example, use2-az1. owner-id: The ID of the Amazon Web Services account that enabled fast snapshot restore on the snapshot. snapshot-id: The ID of the snapshot. state: The state of fast snapshot restores for the snapshot (enabling | optimizing | enabled | disabling | disabled)."];
      maxResults: DescribeFastSnapshotRestoresMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun -> fun () -> { filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeFastSnapshotRestoresMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeFastSnapshotRestoresMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?dryRun ?nextToken ?maxResults ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeFastSnapshotRestoresMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the state of fast snapshot restores for your snapshots."]
module DescribeFastLaunchImagesResult =
  struct
    type nonrec t =
      {
      fastLaunchImages: DescribeFastLaunchImagesSuccessSet.t option
        [@ocaml.doc
          "A collection of details about the fast-launch enabled Windows images that meet the requested criteria."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fastLaunchImages =
      fun ?nextToken -> fun () -> { fastLaunchImages; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fastLaunchImageSet",
           (Option.map x.fastLaunchImages
              ~f:DescribeFastLaunchImagesSuccessSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let fastLaunchImages =
        (Option.map ~f:DescribeFastLaunchImagesSuccessSet.of_xml)
          (Xml.child xml_arg0 "fastLaunchImageSet") in
      make ?nextToken ?fastLaunchImages ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let fastLaunchImages =
        field_map json__ "FastLaunchImages"
          DescribeFastLaunchImagesSuccessSet.of_json in
      make ?nextToken ?fastLaunchImages ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describe details for Windows AMIs that are configured for Windows fast launch."]
module DescribeFastLaunchImagesRequest =
  struct
    type nonrec t =
      {
      imageIds: FastLaunchImageIdList.t option
        [@ocaml.doc
          "Specify one or more Windows AMI image IDs for the request."];
      filters: FilterList.t option
        [@ocaml.doc
          "Use the following filters to streamline results. resource-type - The resource type for pre-provisioning. owner-id - The owner ID for the pre-provisioning resource. state - The current state of fast launching for the Windows AMI."];
      maxResults: DescribeFastLaunchImagesRequestMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?imageIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () -> { imageIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("ImageId",
           (Option.map x.imageIds ~f:FastLaunchImageIdList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeFastLaunchImagesRequestMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeFastLaunchImagesRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let imageIds =
        (Option.map ~f:FastLaunchImageIdList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?imageIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeFastLaunchImagesRequestMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let imageIds =
        field_map json__ "ImageIds" FastLaunchImageIdList.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?imageIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describe details for Windows AMIs that are configured for Windows fast launch."]
module DescribeExportTasksResult =
  struct
    type nonrec t =
      {
      exportTasks: ExportTaskList.t option
        [@ocaml.doc "Information about the export tasks."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exportTasks = fun () -> { exportTasks }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exportTaskSet",
           (Option.map x.exportTasks ~f:ExportTaskList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTasks =
        (Option.map ~f:ExportTaskList.of_xml)
          (Xml.child xml_arg0 "exportTaskSet") in
      make ?exportTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exportTasks = field_map json__ "ExportTasks" ExportTaskList.of_json in
      make ?exportTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified export instance tasks or all of your export instance tasks."]
module DescribeExportTasksRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc "the filters for the export tasks."];
      exportTaskIds: ExportTaskIdStringList.t option
        [@ocaml.doc "The export task IDs."]}
    let make ?filters =
      fun ?exportTaskIds -> fun () -> { filters; exportTaskIds }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("exportTaskId",
          (Option.map x.exportTaskIds ~f:ExportTaskIdStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTaskIds =
        (Option.map ~f:ExportTaskIdStringList.of_xml)
          (Xml.child xml_arg0 "exportTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?exportTaskIds ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exportTaskIds =
        field_map json__ "ExportTaskIds" ExportTaskIdStringList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      make ?exportTaskIds ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified export instance tasks or all of your export instance tasks."]
module DescribeExportImageTasksResult =
  struct
    type nonrec t =
      {
      exportImageTasks: ExportImageTaskList.t option
        [@ocaml.doc "Information about the export image tasks."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to get the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exportImageTasks =
      fun ?nextToken -> fun () -> { exportImageTasks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exportImageTaskSet",
           (Option.map x.exportImageTasks ~f:ExportImageTaskList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let exportImageTasks =
        (Option.map ~f:ExportImageTaskList.of_xml)
          (Xml.child xml_arg0 "exportImageTaskSet") in
      make ?nextToken ?exportImageTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let exportImageTasks =
        field_map json__ "ExportImageTasks" ExportImageTaskList.of_json in
      make ?nextToken ?exportImageTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified export image tasks or all of your export image tasks."]
module DescribeExportImageTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "Filter tasks using the task-state filter and one of the following values: active, completed, deleting, or deleted."];
      exportImageTaskIds: ExportImageTaskIdList.t option
        [@ocaml.doc "The IDs of the export image tasks."];
      maxResults: DescribeExportImageTasksMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token that indicates the next page of results."]}
    let make ?dryRun =
      fun ?filters ->
        fun ?exportImageTaskIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { dryRun; filters; exportImageTaskIds; maxResults; nextToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("ExportImageTaskId",
          (Option.map x.exportImageTaskIds ~f:ExportImageTaskIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeExportImageTasksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeExportImageTasksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let exportImageTaskIds =
        (Option.map ~f:ExportImageTaskIdList.of_xml)
          (Xml.child xml_arg0 "ExportImageTaskId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?exportImageTaskIds ?filters ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeExportImageTasksMaxResults.of_json in
      let exportImageTaskIds =
        field_map json__ "ExportImageTaskIds" ExportImageTaskIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?exportImageTaskIds ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified export image tasks or all of your export image tasks."]
module DescribeElasticGpusResult =
  struct
    type nonrec t =
      {
      elasticGpuSet: ElasticGpuSet.t option
        [@ocaml.doc "Information about the Elastic Graphics accelerators."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The total number of items to return. If the total number of items available is more than the value specified in max-items then a Next-Token will be provided in the output that you can use to resume pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?elasticGpuSet =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { elasticGpuSet; maxResults; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("elasticGpuSet",
           (Option.map x.elasticGpuSet ~f:ElasticGpuSet.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let elasticGpuSet =
        (Option.map ~f:ElasticGpuSet.of_xml)
          (Xml.child xml_arg0 "elasticGpuSet") in
      make ?nextToken ?maxResults ?elasticGpuSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" Integer.of_json in
      let elasticGpuSet =
        field_map json__ "ElasticGpuSet" ElasticGpuSet.of_json in
      make ?nextToken ?maxResults ?elasticGpuSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon Elastic Graphics reached end of life on January 8, 2024. Describes the Elastic Graphics accelerator associated with your instances."]
module DescribeElasticGpusRequest =
  struct
    type nonrec t =
      {
      elasticGpuIds: ElasticGpuIdSet.t option
        [@ocaml.doc "The Elastic Graphics accelerator IDs."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The Availability Zone in which the Elastic Graphics accelerator resides. elastic-gpu-health - The status of the Elastic Graphics accelerator (OK | IMPAIRED). elastic-gpu-state - The state of the Elastic Graphics accelerator (ATTACHED). elastic-gpu-type - The type of Elastic Graphics accelerator; for example, eg1.medium. instance-id - The ID of the instance to which the Elastic Graphics accelerator is associated."];
      maxResults: DescribeElasticGpusMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned NextToken value. This value can be between 5 and 1000."];
      nextToken: String_.t option
        [@ocaml.doc "The token to request the next page of results."]}
    let make ?elasticGpuIds =
      fun ?dryRun ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { elasticGpuIds; dryRun; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("ElasticGpuId",
           (Option.map x.elasticGpuIds ~f:ElasticGpuIdSet.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeElasticGpusMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeElasticGpusMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let elasticGpuIds =
        (Option.map ~f:ElasticGpuIdSet.of_xml)
          (Xml.child xml_arg0 "ElasticGpuId") in
      make ?nextToken ?maxResults ?filters ?dryRun ?elasticGpuIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" DescribeElasticGpusMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let elasticGpuIds =
        field_map json__ "ElasticGpuIds" ElasticGpuIdSet.of_json in
      make ?nextToken ?maxResults ?filters ?dryRun ?elasticGpuIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon Elastic Graphics reached end of life on January 8, 2024. Describes the Elastic Graphics accelerator associated with your instances."]
module DescribeEgressOnlyInternetGatewaysResult =
  struct
    type nonrec t =
      {
      egressOnlyInternetGateways: EgressOnlyInternetGatewayList.t option
        [@ocaml.doc "Information about the egress-only internet gateways."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?egressOnlyInternetGateways =
      fun ?nextToken -> fun () -> { egressOnlyInternetGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("egressOnlyInternetGatewaySet",
           (Option.map x.egressOnlyInternetGateways
              ~f:EgressOnlyInternetGatewayList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let egressOnlyInternetGateways =
        (Option.map ~f:EgressOnlyInternetGatewayList.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGatewaySet") in
      make ?nextToken ?egressOnlyInternetGateways ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let egressOnlyInternetGateways =
        field_map json__ "EgressOnlyInternetGateways"
          EgressOnlyInternetGatewayList.of_json in
      make ?nextToken ?egressOnlyInternetGateways ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your egress-only internet gateways. The default is to describe all your egress-only internet gateways. Alternatively, you can specify specific egress-only internet gateway IDs or filter the results to include only the egress-only internet gateways that match specific criteria."]
module DescribeEgressOnlyInternetGatewaysRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      egressOnlyInternetGatewayIds: EgressOnlyInternetGatewayIdList.t option
        [@ocaml.doc "The IDs of the egress-only internet gateways."];
      maxResults: DescribeEgressOnlyInternetGatewaysMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."]}
    let make ?dryRun =
      fun ?egressOnlyInternetGatewayIds ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?filters ->
              fun () ->
                {
                  dryRun;
                  egressOnlyInternetGatewayIds;
                  maxResults;
                  nextToken;
                  filters
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("EgressOnlyInternetGatewayId",
          (Option.map x.egressOnlyInternetGatewayIds
             ~f:EgressOnlyInternetGatewayIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeEgressOnlyInternetGatewaysMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeEgressOnlyInternetGatewaysMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let egressOnlyInternetGatewayIds =
        (Option.map ~f:EgressOnlyInternetGatewayIdList.of_xml)
          (Xml.child xml_arg0 "EgressOnlyInternetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?nextToken ?maxResults ?egressOnlyInternetGatewayIds
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeEgressOnlyInternetGatewaysMaxResults.of_json in
      let egressOnlyInternetGatewayIds =
        field_map json__ "EgressOnlyInternetGatewayIds"
          EgressOnlyInternetGatewayIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ?nextToken ?maxResults ?egressOnlyInternetGatewayIds
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your egress-only internet gateways. The default is to describe all your egress-only internet gateways. Alternatively, you can specify specific egress-only internet gateway IDs or filter the results to include only the egress-only internet gateways that match specific criteria."]
module DescribeDhcpOptionsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      dhcpOptions: DhcpOptionsList.t option
        [@ocaml.doc "Information about the DHCP options sets."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?dhcpOptions -> fun () -> { nextToken; dhcpOptions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("dhcpOptionsSet",
          (Option.map x.dhcpOptions ~f:DhcpOptionsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dhcpOptions =
        (Option.map ~f:DhcpOptionsList.of_xml)
          (Xml.child xml_arg0 "dhcpOptionsSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?dhcpOptions ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dhcpOptions =
        field_map json__ "DhcpOptions" DhcpOptionsList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?dhcpOptions ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your DHCP option sets. The default is to describe all your DHCP option sets. Alternatively, you can specify specific DHCP option set IDs or filter the results to include only the DHCP option sets that match specific criteria. For more information, see DHCP option sets in the Amazon VPC User Guide."]
module DescribeDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpOptionsIds: DhcpOptionsIdStringList.t option
        [@ocaml.doc "The IDs of DHCP option sets."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeDhcpOptionsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. dhcp-options-id - The ID of a DHCP options set. key - The key for one of the options (for example, domain-name). value - The value for one of the options. owner-id - The ID of the Amazon Web Services account that owns the DHCP options set. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."]}
    let make ?dhcpOptionsIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ?filters ->
              fun () ->
                { dhcpOptionsIds; nextToken; maxResults; dryRun; filters }
    let to_value x =
      structure_to_value
        [("DhcpOptionsId",
           (Option.map x.dhcpOptionsIds ~f:DhcpOptionsIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DescribeDhcpOptionsMaxResults.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let maxResults =
        (Option.map ~f:DescribeDhcpOptionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dhcpOptionsIds =
        (Option.map ~f:DhcpOptionsIdStringList.of_xml)
          (Xml.child xml_arg0 "DhcpOptionsId") in
      make ?filters ?dryRun ?maxResults ?nextToken ?dhcpOptionsIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" DescribeDhcpOptionsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let dhcpOptionsIds =
        field_map json__ "DhcpOptionsIds" DhcpOptionsIdStringList.of_json in
      make ?filters ?dryRun ?maxResults ?nextToken ?dhcpOptionsIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your DHCP option sets. The default is to describe all your DHCP option sets. Alternatively, you can specify specific DHCP option set IDs or filter the results to include only the DHCP option sets that match specific criteria. For more information, see DHCP option sets in the Amazon VPC User Guide."]
module DescribeDeclarativePoliciesReportsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      reports: DeclarativePoliciesReportList.t option
        [@ocaml.doc "The report metadata."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?reports -> fun () -> { nextToken; reports }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reportSet",
          (Option.map x.reports ~f:DeclarativePoliciesReportList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reports =
        (Option.map ~f:DeclarativePoliciesReportList.of_xml)
          (Xml.child xml_arg0 "reportSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reports ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reports =
        field_map json__ "Reports" DeclarativePoliciesReportList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?reports ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the metadata of an account status report, including the status of the report. To view the full report, download it from the Amazon S3 bucket where it was saved. Reports are accessible only when they have the complete status. Reports with other statuses (running, cancelled, or error) are not available in the S3 bucket. For more information about downloading objects from an S3 bucket, see Downloading objects in the Amazon Simple Storage Service User Guide. For more information, see Generating the account status report for declarative policies in the Amazon Web Services Organizations User Guide."]
module DescribeDeclarativePoliciesReportsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DeclarativePoliciesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      reportIds: ValueStringList.t option
        [@ocaml.doc "One or more report IDs."]}
    let make ?dryRun =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?reportIds ->
            fun () -> { dryRun; nextToken; maxResults; reportIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:DeclarativePoliciesMaxResults.to_value));
        ("ReportId", (Option.map x.reportIds ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "ReportId") in
      let maxResults =
        (Option.map ~f:DeclarativePoliciesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?reportIds ?maxResults ?nextToken ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportIds = field_map json__ "ReportIds" ValueStringList.of_json in
      let maxResults =
        field_map json__ "MaxResults" DeclarativePoliciesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?reportIds ?maxResults ?nextToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the metadata of an account status report, including the status of the report. To view the full report, download it from the Amazon S3 bucket where it was saved. Reports are accessible only when they have the complete status. Reports with other statuses (running, cancelled, or error) are not available in the S3 bucket. For more information about downloading objects from an S3 bucket, see Downloading objects in the Amazon Simple Storage Service User Guide. For more information, see Generating the account status report for declarative policies in the Amazon Web Services Organizations User Guide."]
module DescribeCustomerGatewaysResult =
  struct
    type nonrec t =
      {
      customerGateways: CustomerGatewayList.t option
        [@ocaml.doc "Information about one or more customer gateways."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?customerGateways = fun () -> { customerGateways }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("customerGatewaySet",
           (Option.map x.customerGateways ~f:CustomerGatewayList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customerGateways =
        (Option.map ~f:CustomerGatewayList.of_xml)
          (Xml.child xml_arg0 "customerGatewaySet") in
      make ?customerGateways ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let customerGateways =
        field_map json__ "CustomerGateways" CustomerGatewayList.of_json in
      make ?customerGateways ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DescribeCustomerGateways."]
module DescribeCustomerGatewaysRequest =
  struct
    type nonrec t =
      {
      customerGatewayIds: CustomerGatewayIdStringList.t option
        [@ocaml.doc
          "One or more customer gateway IDs. Default: Describes all your customer gateways."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. bgp-asn - The customer gateway's Border Gateway Protocol (BGP) Autonomous System Number (ASN). customer-gateway-id - The ID of the customer gateway. ip-address - The IP address of the customer gateway device's external interface. state - The state of the customer gateway (pending | available | deleting | deleted). type - The type of customer gateway. Currently, the only supported type is ipsec.1. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?customerGatewayIds =
      fun ?filters ->
        fun ?dryRun -> fun () -> { customerGatewayIds; filters; dryRun }
    let to_value x =
      structure_to_value
        [("CustomerGatewayId",
           (Option.map x.customerGatewayIds
              ~f:CustomerGatewayIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let customerGatewayIds =
        (Option.map ~f:CustomerGatewayIdStringList.of_xml)
          (Xml.child xml_arg0 "CustomerGatewayId") in
      make ?dryRun ?filters ?customerGatewayIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let customerGatewayIds =
        field_map json__ "CustomerGatewayIds"
          CustomerGatewayIdStringList.of_json in
      make ?dryRun ?filters ?customerGatewayIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeCustomerGateways."]
module DescribeConversionTasksResult =
  struct
    type nonrec t =
      {
      conversionTasks: DescribeConversionTaskList.t option
        [@ocaml.doc "Information about the conversion tasks."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?conversionTasks = fun () -> { conversionTasks }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("conversionTasks",
           (Option.map x.conversionTasks
              ~f:DescribeConversionTaskList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conversionTasks =
        (Option.map ~f:DescribeConversionTaskList.of_xml)
          (Xml.child xml_arg0 "conversionTasks") in
      make ?conversionTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let conversionTasks =
        field_map json__ "ConversionTasks" DescribeConversionTaskList.of_json in
      make ?conversionTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified conversion tasks or all your conversion tasks. For more information, see the VM Import/Export User Guide. For information about the import manifest referenced by this API action, see VM Import Manifest."]
module DescribeConversionTasksRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      conversionTaskIds: ConversionIdStringList.t option
        [@ocaml.doc "The conversion task IDs."]}
    let make ?dryRun =
      fun ?conversionTaskIds -> fun () -> { dryRun; conversionTaskIds }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("conversionTaskId",
          (Option.map x.conversionTaskIds ~f:ConversionIdStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conversionTaskIds =
        (Option.map ~f:ConversionIdStringList.of_xml)
          (Xml.child xml_arg0 "conversionTaskId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?conversionTaskIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let conversionTaskIds =
        field_map json__ "ConversionTaskIds" ConversionIdStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?conversionTaskIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified conversion tasks or all your conversion tasks. For more information, see the VM Import/Export User Guide. For information about the import manifest referenced by this API action, see VM Import Manifest."]
module DescribeCoipPoolsResult =
  struct
    type nonrec t =
      {
      coipPools: CoipPoolSet.t option
        [@ocaml.doc "Information about the address pools."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipPools =
      fun ?nextToken -> fun () -> { coipPools; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipPoolSet", (Option.map x.coipPools ~f:CoipPoolSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let coipPools =
        (Option.map ~f:CoipPoolSet.of_xml) (Xml.child xml_arg0 "coipPoolSet") in
      make ?nextToken ?coipPools ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let coipPools = field_map json__ "CoipPools" CoipPoolSet.of_json in
      make ?nextToken ?coipPools ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified customer-owned address pools or all of your customer-owned address pools."]
module DescribeCoipPoolsRequest =
  struct
    type nonrec t =
      {
      poolIds: CoipPoolIdSet.t option
        [@ocaml.doc "The IDs of the address pools."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. coip-pool.local-gateway-route-table-id - The ID of the local gateway route table. coip-pool.pool-id - The ID of the address pool."];
      maxResults: CoipPoolMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?poolIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () -> { poolIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("PoolId", (Option.map x.poolIds ~f:CoipPoolIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:CoipPoolMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:CoipPoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let poolIds =
        (Option.map ~f:CoipPoolIdSet.of_xml) (Xml.child xml_arg0 "PoolId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?poolIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" CoipPoolMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let poolIds = field_map json__ "PoolIds" CoipPoolIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?poolIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified customer-owned address pools or all of your customer-owned address pools."]
module DescribeClientVpnTargetNetworksResult =
  struct
    type nonrec t =
      {
      clientVpnTargetNetworks: TargetNetworkSet.t option
        [@ocaml.doc "Information about the associated target networks."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnTargetNetworks =
      fun ?nextToken -> fun () -> { clientVpnTargetNetworks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnTargetNetworks",
           (Option.map x.clientVpnTargetNetworks ~f:TargetNetworkSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let clientVpnTargetNetworks =
        (Option.map ~f:TargetNetworkSet.of_xml)
          (Xml.child xml_arg0 "clientVpnTargetNetworks") in
      make ?nextToken ?clientVpnTargetNetworks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let clientVpnTargetNetworks =
        field_map json__ "ClientVpnTargetNetworks" TargetNetworkSet.of_json in
      make ?nextToken ?clientVpnTargetNetworks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the target networks associated with the specified Client VPN endpoint."]
module DescribeClientVpnTargetNetworksRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      associationIds: ValueStringList.t option
        [@ocaml.doc "The IDs of the target network associations."];
      maxResults: DescribeClientVpnTargetNetworksMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. association-id - The ID of the association. target-network-id - The ID of the subnet specified as the target network. vpc-id - The ID of the VPC in which the target network is located."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DescribeClientVpnTargetNetworksRequest"
    let make ?associationIds =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?dryRun ->
              fun ~clientVpnEndpointId ->
                fun () ->
                  {
                    associationIds;
                    maxResults;
                    nextToken;
                    filters;
                    dryRun;
                    clientVpnEndpointId
                  }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("AssociationIds",
          (Option.map x.associationIds ~f:ValueStringList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnTargetNetworksMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnTargetNetworksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let associationIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "AssociationIds") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?filters ?nextToken ?maxResults ?associationIds
        ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeClientVpnTargetNetworksMaxResults.of_json in
      let associationIds =
        field_map json__ "AssociationIds" ValueStringList.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ?filters ?nextToken ?maxResults ?associationIds
        ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the target networks associated with the specified Client VPN endpoint."]
module DescribeClientVpnRoutesResult =
  struct
    type nonrec t =
      {
      routes: ClientVpnRouteSet.t option
        [@ocaml.doc "Information about the Client VPN endpoint routes."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routes = fun ?nextToken -> fun () -> { routes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routes", (Option.map x.routes ~f:ClientVpnRouteSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routes =
        (Option.map ~f:ClientVpnRouteSet.of_xml)
          (Xml.child xml_arg0 "routes") in
      make ?nextToken ?routes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let routes = field_map json__ "Routes" ClientVpnRouteSet.of_json in
      make ?nextToken ?routes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the routes for the specified Client VPN endpoint."]
module DescribeClientVpnRoutesRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. destination-cidr - The CIDR of the route destination. origin - How the route was associated with the Client VPN endpoint (associate | add-route). target-subnet - The ID of the subnet through which traffic is routed."];
      maxResults: DescribeClientVpnRoutesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DescribeClientVpnRoutesRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~clientVpnEndpointId ->
              fun () ->
                { filters; maxResults; nextToken; dryRun; clientVpnEndpointId
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnRoutesMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnRoutesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?nextToken ?maxResults ?filters ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeClientVpnRoutesMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the routes for the specified Client VPN endpoint."]
module DescribeClientVpnEndpointsResult =
  struct
    type nonrec t =
      {
      clientVpnEndpoints: EndpointSet.t option
        [@ocaml.doc "Information about the Client VPN endpoints."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnEndpoints =
      fun ?nextToken -> fun () -> { clientVpnEndpoints; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnEndpoint",
           (Option.map x.clientVpnEndpoints ~f:EndpointSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let clientVpnEndpoints =
        (Option.map ~f:EndpointSet.of_xml)
          (Xml.child xml_arg0 "clientVpnEndpoint") in
      make ?nextToken ?clientVpnEndpoints ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let clientVpnEndpoints =
        field_map json__ "ClientVpnEndpoints" EndpointSet.of_json in
      make ?nextToken ?clientVpnEndpoints ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more Client VPN endpoints in the account."]
module DescribeClientVpnEndpointsRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointIds: ClientVpnEndpointIdList.t option
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      maxResults: DescribeClientVpnEndpointMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. endpoint-id - The ID of the Client VPN endpoint. transport-protocol - The transport protocol (tcp | udp)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?clientVpnEndpointIds =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  clientVpnEndpointIds;
                  maxResults;
                  nextToken;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Option.map x.clientVpnEndpointIds
              ~f:ClientVpnEndpointIdList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnEndpointMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnEndpointMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let clientVpnEndpointIds =
        (Option.map ~f:ClientVpnEndpointIdList.of_xml)
          (Xml.child xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?filters ?nextToken ?maxResults ?clientVpnEndpointIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeClientVpnEndpointMaxResults.of_json in
      let clientVpnEndpointIds =
        field_map json__ "ClientVpnEndpointIds"
          ClientVpnEndpointIdList.of_json in
      make ?dryRun ?filters ?nextToken ?maxResults ?clientVpnEndpointIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more Client VPN endpoints in the account."]
module DescribeClientVpnConnectionsResult =
  struct
    type nonrec t =
      {
      connections: ClientVpnConnectionSet.t option
        [@ocaml.doc
          "Information about the active and terminated client connections."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?connections =
      fun ?nextToken -> fun () -> { connections; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("connections",
           (Option.map x.connections ~f:ClientVpnConnectionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let connections =
        (Option.map ~f:ClientVpnConnectionSet.of_xml)
          (Xml.child xml_arg0 "connections") in
      make ?nextToken ?connections ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let connections =
        field_map json__ "Connections" ClientVpnConnectionSet.of_json in
      make ?nextToken ?connections ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes active client connections and connections that have been terminated within the last 60 minutes for the specified Client VPN endpoint."]
module DescribeClientVpnConnectionsRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. connection-id - The ID of the connection. username - For Active Directory client authentication, the user name of the client who established the client connection."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      maxResults: DescribeClientVpnConnectionsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DescribeClientVpnConnectionsRequest"
    let make ?filters =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun ~clientVpnEndpointId ->
              fun () ->
                { filters; nextToken; maxResults; dryRun; clientVpnEndpointId
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnConnectionsMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:DescribeClientVpnConnectionsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?maxResults ?nextToken ?filters ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeClientVpnConnectionsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ?maxResults ?nextToken ?filters ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes active client connections and connections that have been terminated within the last 60 minutes for the specified Client VPN endpoint."]
module DescribeClientVpnAuthorizationRulesResult =
  struct
    type nonrec t =
      {
      authorizationRules: AuthorizationRuleSet.t option
        [@ocaml.doc "Information about the authorization rules."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?authorizationRules =
      fun ?nextToken -> fun () -> { authorizationRules; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("authorizationRule",
           (Option.map x.authorizationRules ~f:AuthorizationRuleSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let authorizationRules =
        (Option.map ~f:AuthorizationRuleSet.of_xml)
          (Xml.child xml_arg0 "authorizationRule") in
      make ?nextToken ?authorizationRules ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let authorizationRules =
        field_map json__ "AuthorizationRules" AuthorizationRuleSet.of_json in
      make ?nextToken ?authorizationRules ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the authorization rules for a specified Client VPN endpoint."]
module DescribeClientVpnAuthorizationRulesRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next page of results."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. description - The description of the authorization rule. destination-cidr - The CIDR of the network to which the authorization rule applies. group-id - The ID of the Active Directory group to which the authorization rule grants access."];
      maxResults: DescribeClientVpnAuthorizationRulesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the nextToken value."]}
    let context_ = "DescribeClientVpnAuthorizationRulesRequest"
    let make ?dryRun =
      fun ?nextToken ->
        fun ?filters ->
          fun ?maxResults ->
            fun ~clientVpnEndpointId ->
              fun () ->
                { dryRun; nextToken; filters; maxResults; clientVpnEndpointId
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeClientVpnAuthorizationRulesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeClientVpnAuthorizationRulesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?maxResults ?filters ?nextToken ?dryRun ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeClientVpnAuthorizationRulesMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?maxResults ?filters ?nextToken ?dryRun ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the authorization rules for a specified Client VPN endpoint."]
module DescribeClassicLinkInstancesResult =
  struct
    type nonrec t =
      {
      instances: ClassicLinkInstanceList.t option
        [@ocaml.doc
          "Information about one or more linked EC2-Classic instances."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instances =
      fun ?nextToken -> fun () -> { instances; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.instances ~f:ClassicLinkInstanceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instances =
        (Option.map ~f:ClassicLinkInstanceList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?nextToken ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let instances =
        field_map json__ "Instances" ClassicLinkInstanceList.of_json in
      make ?nextToken ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is deprecated. Describes your linked EC2-Classic instances. This request only returns information about EC2-Classic instances linked to a VPC through ClassicLink. You cannot use this request to return information about other instances."]
module DescribeClassicLinkInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceIds: InstanceIdStringList.t option
        [@ocaml.doc
          "The instance IDs. Must be instances linked to a VPC through ClassicLink."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. group-id - The ID of a VPC security group that's associated with the instance. instance-id - The ID of the instance. tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. vpc-id - The ID of the VPC to which the instance is linked."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeClassicLinkInstancesMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. Constraint: If the value is greater than 1000, we return only 1000 items."]}
    let make ?dryRun =
      fun ?instanceIds ->
        fun ?filters ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { dryRun; instanceIds; filters; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId",
          (Option.map x.instanceIds ~f:InstanceIdStringList.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:DescribeClassicLinkInstancesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:DescribeClassicLinkInstancesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let instanceIds =
        (Option.map ~f:InstanceIdStringList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?maxResults ?nextToken ?filters ?instanceIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeClassicLinkInstancesMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" InstanceIdStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?maxResults ?nextToken ?filters ?instanceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is deprecated. Describes your linked EC2-Classic instances. This request only returns information about EC2-Classic instances linked to a VPC through ClassicLink. You cannot use this request to return information about other instances."]
module DescribeCarrierGatewaysResult =
  struct
    type nonrec t =
      {
      carrierGateways: CarrierGatewaySet.t option
        [@ocaml.doc "Information about the carrier gateway."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?carrierGateways =
      fun ?nextToken -> fun () -> { carrierGateways; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("carrierGatewaySet",
           (Option.map x.carrierGateways ~f:CarrierGatewaySet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let carrierGateways =
        (Option.map ~f:CarrierGatewaySet.of_xml)
          (Xml.child xml_arg0 "carrierGatewaySet") in
      make ?nextToken ?carrierGateways ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let carrierGateways =
        field_map json__ "CarrierGateways" CarrierGatewaySet.of_json in
      make ?nextToken ?carrierGateways ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your carrier gateways."]
module DescribeCarrierGatewaysRequest =
  struct
    type nonrec t =
      {
      carrierGatewayIds: CarrierGatewayIdSet.t option
        [@ocaml.doc "One or more carrier gateway IDs."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. carrier-gateway-id - The ID of the carrier gateway. state - The state of the carrier gateway (pending | failed | available | deleting | deleted). owner-id - The Amazon Web Services account ID of the owner of the carrier gateway. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value. vpc-id - The ID of the VPC associated with the carrier gateway."];
      maxResults: CarrierGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?carrierGatewayIds =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                { carrierGatewayIds; filters; maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("CarrierGatewayId",
           (Option.map x.carrierGatewayIds ~f:CarrierGatewayIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:CarrierGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:CarrierGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let carrierGatewayIds =
        (Option.map ~f:CarrierGatewayIdSet.of_xml)
          (Xml.child xml_arg0 "CarrierGatewayId") in
      make ?dryRun ?nextToken ?maxResults ?filters ?carrierGatewayIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" CarrierGatewayMaxResults.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let carrierGatewayIds =
        field_map json__ "CarrierGatewayIds" CarrierGatewayIdSet.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ?carrierGatewayIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your carrier gateways."]
module DescribeCapacityReservationsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      capacityReservations: CapacityReservationSet.t option
        [@ocaml.doc "Information about the Capacity Reservations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?capacityReservations ->
        fun () -> { nextToken; capacityReservations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("capacityReservationSet",
          (Option.map x.capacityReservations
             ~f:CapacityReservationSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservations =
        (Option.map ~f:CapacityReservationSet.of_xml)
          (Xml.child xml_arg0 "capacityReservationSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?capacityReservations ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityReservations =
        field_map json__ "CapacityReservations"
          CapacityReservationSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?capacityReservations ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more of your Capacity Reservations. The results describe only the Capacity Reservations in the Amazon Web Services Region that you're currently using."]
module DescribeCapacityReservationsRequest =
  struct
    type nonrec t =
      {
      capacityReservationIds: CapacityReservationIdSet.t option
        [@ocaml.doc "The ID of the Capacity Reservation."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeCapacityReservationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. instance-type - The type of instance for which the Capacity Reservation reserves capacity. owner-id - The ID of the Amazon Web Services account that owns the Capacity Reservation. instance-platform - The type of operating system for which the Capacity Reservation reserves capacity. availability-zone - The Availability Zone of the Capacity Reservation. tenancy - Indicates the tenancy of the Capacity Reservation. A Capacity Reservation can have one of the following tenancy settings: default - The Capacity Reservation is created on hardware that is shared with other Amazon Web Services accounts. dedicated - The Capacity Reservation is created on single-tenant hardware that is dedicated to a single Amazon Web Services account. outpost-arn - The Amazon Resource Name (ARN) of the Outpost on which the Capacity Reservation was created. state - The current state of the Capacity Reservation. A Capacity Reservation can be in one of the following states: active- The Capacity Reservation is active and the capacity is available for your use. expired - The Capacity Reservation expired automatically at the date and time specified in your request. The reserved capacity is no longer available for your use. cancelled - The Capacity Reservation was cancelled. The reserved capacity is no longer available for your use. pending - The Capacity Reservation request was successful but the capacity provisioning is still pending. failed - The Capacity Reservation request has failed. A request might fail due to invalid request parameters, capacity constraints, or instance limit constraints. Failed requests are retained for 60 minutes. start-date - The date and time at which the Capacity Reservation was started. end-date - The date and time at which the Capacity Reservation expires. When a Capacity Reservation expires, the reserved capacity is released and you can no longer launch instances into it. The Capacity Reservation's state changes to expired when it reaches its end date and time. end-date-type - Indicates the way in which the Capacity Reservation ends. A Capacity Reservation can have one of the following end types: unlimited - The Capacity Reservation remains active until you explicitly cancel it. limited - The Capacity Reservation expires automatically at a specified date and time. instance-match-criteria - Indicates the type of instance launches that the Capacity Reservation accepts. The options include: open - The Capacity Reservation accepts all instances that have matching attributes (instance type, platform, and Availability Zone). Instances that have matching attributes launch into the Capacity Reservation automatically without specifying any additional parameters. targeted - The Capacity Reservation only accepts instances that have matching attributes (instance type, platform, and Availability Zone), and explicitly target the Capacity Reservation. This ensures that only permitted instances can use the reserved capacity. placement-group-arn - The ARN of the cluster placement group in which the Capacity Reservation was created."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?capacityReservationIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  capacityReservationIds;
                  nextToken;
                  maxResults;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Option.map x.capacityReservationIds
              ~f:CapacityReservationIdSet.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityReservationsMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeCapacityReservationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationIds =
        (Option.map ~f:CapacityReservationIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityReservationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityReservationsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityReservationIds =
        field_map json__ "CapacityReservationIds"
          CapacityReservationIdSet.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityReservationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more of your Capacity Reservations. The results describe only the Capacity Reservations in the Amazon Web Services Region that you're currently using."]
module DescribeCapacityReservationTopologyResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to include in another request to get the next page of items. This value is null when there are no more items to return."];
      capacityReservations: CapacityReservationTopologySet.t option
        [@ocaml.doc
          "Information about the topology of each Capacity Reservation."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?capacityReservations ->
        fun () -> { nextToken; capacityReservations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("capacityReservationSet",
          (Option.map x.capacityReservations
             ~f:CapacityReservationTopologySet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservations =
        (Option.map ~f:CapacityReservationTopologySet.of_xml)
          (Xml.child xml_arg0 "capacityReservationSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?capacityReservations ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityReservations =
        field_map json__ "CapacityReservations"
          CapacityReservationTopologySet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?capacityReservations ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a tree-based hierarchy that represents the physical host placement of your pending or active Capacity Reservations within an Availability Zone or Local Zone. You can use this information to determine the relative proximity of your capacity within the Amazon Web Services network before it is launched and use this information to allocate capacity together to support your tightly coupled workloads. Capacity Reservation topology is supported for specific instance types only. For more information, see Prerequisites for Amazon EC2 instance topology in the Amazon EC2 User Guide. The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the system supporting it. As a result, when you call the DescribeCapacityReservationTopology API command immediately after launching instances, the response might return a null value for capacityBlockId because the data might not have fully propagated across all subsystems. For more information, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. For more information, see Amazon EC2 topology in the Amazon EC2 User Guide."]
module DescribeCapacityReservationTopologyRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request."];
      maxResults: DescribeCapacityReservationTopologyMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination. You can't specify this parameter and the Capacity Reservation IDs parameter in the same request. Default: 10"];
      capacityReservationIds: CapacityReservationIdSet.t option
        [@ocaml.doc
          "The Capacity Reservation IDs. Default: Describes all your Capacity Reservations. Constraints: Maximum 100 explicitly specified Capacity Reservation IDs."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. availability-zone - The name of the Availability Zone (for example, us-west-2a) or Local Zone (for example, us-west-2-lax-1b) that the Capacity Reservation is in. instance-type - The instance type (for example, p4d.24xlarge) or instance family (for example, p4d*). You can use the * wildcard to match zero or more characters, or the ? wildcard to match zero or one character."]}
    let make ?dryRun =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?capacityReservationIds ->
            fun ?filters ->
              fun () ->
                {
                  dryRun;
                  nextToken;
                  maxResults;
                  capacityReservationIds;
                  filters
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityReservationTopologyMaxResults.to_value));
        ("CapacityReservationId",
          (Option.map x.capacityReservationIds
             ~f:CapacityReservationIdSet.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let capacityReservationIds =
        (Option.map ~f:CapacityReservationIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationId") in
      let maxResults =
        (Option.map ~f:DescribeCapacityReservationTopologyMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?filters ?capacityReservationIds ?maxResults ?nextToken ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let capacityReservationIds =
        field_map json__ "CapacityReservationIds"
          CapacityReservationIdSet.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityReservationTopologyMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?filters ?capacityReservationIds ?maxResults ?nextToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a tree-based hierarchy that represents the physical host placement of your pending or active Capacity Reservations within an Availability Zone or Local Zone. You can use this information to determine the relative proximity of your capacity within the Amazon Web Services network before it is launched and use this information to allocate capacity together to support your tightly coupled workloads. Capacity Reservation topology is supported for specific instance types only. For more information, see Prerequisites for Amazon EC2 instance topology in the Amazon EC2 User Guide. The Amazon EC2 API follows an eventual consistency model due to the distributed nature of the system supporting it. As a result, when you call the DescribeCapacityReservationTopology API command immediately after launching instances, the response might return a null value for capacityBlockId because the data might not have fully propagated across all subsystems. For more information, see Eventual consistency in the Amazon EC2 API in the Amazon EC2 Developer Guide. For more information, see Amazon EC2 topology in the Amazon EC2 User Guide."]
module DescribeCapacityReservationFleetsResult =
  struct
    type nonrec t =
      {
      capacityReservationFleets: CapacityReservationFleetSet.t option
        [@ocaml.doc "Information about the Capacity Reservation Fleets."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityReservationFleets =
      fun ?nextToken -> fun () -> { capacityReservationFleets; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityReservationFleetSet",
           (Option.map x.capacityReservationFleets
              ~f:CapacityReservationFleetSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityReservationFleets =
        (Option.map ~f:CapacityReservationFleetSet.of_xml)
          (Xml.child xml_arg0 "capacityReservationFleetSet") in
      make ?nextToken ?capacityReservationFleets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityReservationFleets =
        field_map json__ "CapacityReservationFleets"
          CapacityReservationFleetSet.of_json in
      make ?nextToken ?capacityReservationFleets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more Capacity Reservation Fleets."]
module DescribeCapacityReservationFleetsRequest =
  struct
    type nonrec t =
      {
      capacityReservationFleetIds: CapacityReservationFleetIdSet.t option
        [@ocaml.doc
          "The IDs of the Capacity Reservation Fleets to describe."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeCapacityReservationFleetsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. state - The state of the Fleet (submitted | modifying | active | partially_fulfilled | expiring | expired | cancelling | cancelled | failed). instance-match-criteria - The instance matching criteria for the Fleet. Only open is supported. tenancy - The tenancy of the Fleet (default | dedicated). allocation-strategy - The allocation strategy used by the Fleet. Only prioritized is supported."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?capacityReservationFleetIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  capacityReservationFleetIds;
                  nextToken;
                  maxResults;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationFleetId",
           (Option.map x.capacityReservationFleetIds
              ~f:CapacityReservationFleetIdSet.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityReservationFleetsMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeCapacityReservationFleetsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationFleetIds =
        (Option.map ~f:CapacityReservationFleetIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationFleetId") in
      make ?dryRun ?filters ?maxResults ?nextToken
        ?capacityReservationFleetIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityReservationFleetsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityReservationFleetIds =
        field_map json__ "CapacityReservationFleetIds"
          CapacityReservationFleetIdSet.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken
        ?capacityReservationFleetIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more Capacity Reservation Fleets."]
module DescribeCapacityReservationBillingRequestsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      capacityReservationBillingRequests:
        CapacityReservationBillingRequestSet.t option
        [@ocaml.doc "Information about the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?capacityReservationBillingRequests ->
        fun () -> { nextToken; capacityReservationBillingRequests }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("capacityReservationBillingRequestSet",
          (Option.map x.capacityReservationBillingRequests
             ~f:CapacityReservationBillingRequestSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationBillingRequests =
        (Option.map ~f:CapacityReservationBillingRequestSet.of_xml)
          (Xml.child xml_arg0 "capacityReservationBillingRequestSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?capacityReservationBillingRequests ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityReservationBillingRequests =
        field_map json__ "CapacityReservationBillingRequests"
          CapacityReservationBillingRequestSet.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?capacityReservationBillingRequests ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a request to assign the billing of the unused capacity of a Capacity Reservation. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations."]
module DescribeCapacityReservationBillingRequestsRequest =
  struct
    type nonrec t =
      {
      capacityReservationIds: CapacityReservationIdSet.t option
        [@ocaml.doc "The ID of the Capacity Reservation."];
      role: CallerRole.t
        [@ocaml.doc
          "Specify one of the following: odcr-owner - If you are the Capacity Reservation owner, specify this value to view requests that you have initiated. Not supported with the requested-by filter. unused-reservation-billing-owner - If you are the consumer account, specify this value to view requests that have been sent to you. Not supported with the unused-reservation-billing-owner filter."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults:
        DescribeCapacityReservationBillingRequestsRequestMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. status - The state of the request (pending | accepted | rejected | cancelled | revoked | expired). requested-by - The account ID of the Capacity Reservation owner that initiated the request. Not supported if you specify requested-by for Role. unused-reservation-billing-owner - The ID of the consumer account to which the request was sent. Not supported if you specify unused-reservation-billing-owner for Role."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DescribeCapacityReservationBillingRequestsRequest"
    let make ?capacityReservationIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun ~role ->
                fun () ->
                  {
                    capacityReservationIds;
                    nextToken;
                    maxResults;
                    filters;
                    dryRun;
                    role
                  }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Option.map x.capacityReservationIds
              ~f:CapacityReservationIdSet.to_value));
        ("Role", (Some (CallerRole.to_value x.role)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityReservationBillingRequestsRequestMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map
           ~f:DescribeCapacityReservationBillingRequestsRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let role =
        CallerRole.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Role") in
      let capacityReservationIds =
        (Option.map ~f:CapacityReservationIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationId") in
      make ?dryRun ?filters ?maxResults ?nextToken ~role
        ?capacityReservationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityReservationBillingRequestsRequestMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let role = field_map_exn json__ "Role" CallerRole.of_json in
      let capacityReservationIds =
        field_map json__ "CapacityReservationIds"
          CapacityReservationIdSet.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ~role
        ?capacityReservationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a request to assign the billing of the unused capacity of a Capacity Reservation. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations."]
module DescribeCapacityManagerDataExportsResult =
  struct
    type nonrec t =
      {
      capacityManagerDataExports:
        CapacityManagerDataExportResponseSet.t option
        [@ocaml.doc
          "Information about the data export configurations, including export settings, delivery status, and recent activity."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityManagerDataExports =
      fun ?nextToken -> fun () -> { capacityManagerDataExports; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityManagerDataExportSet",
           (Option.map x.capacityManagerDataExports
              ~f:CapacityManagerDataExportResponseSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityManagerDataExports =
        (Option.map ~f:CapacityManagerDataExportResponseSet.of_xml)
          (Xml.child xml_arg0 "capacityManagerDataExportSet") in
      make ?nextToken ?capacityManagerDataExports ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityManagerDataExports =
        field_map json__ "CapacityManagerDataExports"
          CapacityManagerDataExportResponseSet.of_json in
      make ?nextToken ?capacityManagerDataExports ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more Capacity Manager data export configurations. Returns information about export settings, delivery status, and recent export activity."]
module DescribeCapacityManagerDataExportsRequest =
  struct
    type nonrec t =
      {
      capacityManagerDataExportIds: CapacityManagerDataExportIdSet.t option
        [@ocaml.doc
          "The IDs of the data export configurations to describe. If not specified, all export configurations are returned."];
      maxResults:
        DescribeCapacityManagerDataExportsRequestMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. If not specified, up to 1000 results are returned."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token for the next page of results. Use this value in a subsequent call to retrieve additional results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters to narrow the results. Supported filters include export status, creation date, and S3 bucket name."]}
    let make ?capacityManagerDataExportIds =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ?filters ->
              fun () ->
                {
                  capacityManagerDataExportIds;
                  maxResults;
                  nextToken;
                  dryRun;
                  filters
                }
    let to_value x =
      structure_to_value
        [("CapacityManagerDataExportId",
           (Option.map x.capacityManagerDataExportIds
              ~f:CapacityManagerDataExportIdSet.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityManagerDataExportsRequestMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map
           ~f:DescribeCapacityManagerDataExportsRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let capacityManagerDataExportIds =
        (Option.map ~f:CapacityManagerDataExportIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityManagerDataExportId") in
      make ?filters ?dryRun ?nextToken ?maxResults
        ?capacityManagerDataExportIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityManagerDataExportsRequestMaxResults.of_json in
      let capacityManagerDataExportIds =
        field_map json__ "CapacityManagerDataExportIds"
          CapacityManagerDataExportIdSet.of_json in
      make ?filters ?dryRun ?nextToken ?maxResults
        ?capacityManagerDataExportIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more Capacity Manager data export configurations. Returns information about export settings, delivery status, and recent export activity."]
module DescribeCapacityBlocksResult =
  struct
    type nonrec t =
      {
      capacityBlocks: CapacityBlockSet.t option
        [@ocaml.doc "The Capacity Blocks."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityBlocks =
      fun ?nextToken -> fun () -> { capacityBlocks; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityBlockSet",
           (Option.map x.capacityBlocks ~f:CapacityBlockSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityBlocks =
        (Option.map ~f:CapacityBlockSet.of_xml)
          (Xml.child xml_arg0 "capacityBlockSet") in
      make ?nextToken ?capacityBlocks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlocks =
        field_map json__ "CapacityBlocks" CapacityBlockSet.of_json in
      make ?nextToken ?capacityBlocks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes details about Capacity Blocks in the Amazon Web Services Region that you're currently using."]
module DescribeCapacityBlocksRequest =
  struct
    type nonrec t =
      {
      capacityBlockIds: CapacityBlockIds.t option
        [@ocaml.doc "The IDs of the Capacity Blocks."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeCapacityBlocksMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. capacity-block-id - The ID of the Capacity Block. ultraserver-type - The Capacity Block type. The type can be instances or ultraservers. availability-zone - The Availability Zone of the Capacity Block. start-date - The date and time at which the Capacity Block was started. end-date - The date and time at which the Capacity Block expires. When a Capacity Block expires, all instances in the Capacity Block are terminated. create-date - The date and time at which the Capacity Block was created. state - The state of the Capacity Block (active | expired | unavailable | cancelled | failed | scheduled | payment-pending | payment-failed). tags - The tags assigned to the Capacity Block."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?capacityBlockIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                { capacityBlockIds; nextToken; maxResults; filters; dryRun }
    let to_value x =
      structure_to_value
        [("CapacityBlockId",
           (Option.map x.capacityBlockIds ~f:CapacityBlockIds.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityBlocksMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeCapacityBlocksMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityBlockIds =
        (Option.map ~f:CapacityBlockIds.of_xml)
          (Xml.child xml_arg0 "CapacityBlockId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityBlockIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityBlocksMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlockIds =
        field_map json__ "CapacityBlockIds" CapacityBlockIds.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityBlockIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes details about Capacity Blocks in the Amazon Web Services Region that you're currently using."]
module DescribeCapacityBlockStatusResult =
  struct
    type nonrec t =
      {
      capacityBlockStatuses: CapacityBlockStatusSet.t option
        [@ocaml.doc "The availability of capacity for a Capacity Block."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityBlockStatuses =
      fun ?nextToken -> fun () -> { capacityBlockStatuses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityBlockStatusSet",
           (Option.map x.capacityBlockStatuses
              ~f:CapacityBlockStatusSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityBlockStatuses =
        (Option.map ~f:CapacityBlockStatusSet.of_xml)
          (Xml.child xml_arg0 "capacityBlockStatusSet") in
      make ?nextToken ?capacityBlockStatuses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlockStatuses =
        field_map json__ "CapacityBlockStatuses"
          CapacityBlockStatusSet.of_json in
      make ?nextToken ?capacityBlockStatuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the availability of capacity for the specified Capacity blocks, or all of your Capacity Blocks."]
module DescribeCapacityBlockStatusRequest =
  struct
    type nonrec t =
      {
      capacityBlockIds: CapacityBlockIds.t option
        [@ocaml.doc "The ID of the Capacity Block."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeCapacityBlockStatusMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. interconnect-status - The status of the interconnect for the Capacity Block (ok | impaired | insufficient-data)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?capacityBlockIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                { capacityBlockIds; nextToken; maxResults; filters; dryRun }
    let to_value x =
      structure_to_value
        [("CapacityBlockId",
           (Option.map x.capacityBlockIds ~f:CapacityBlockIds.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityBlockStatusMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeCapacityBlockStatusMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityBlockIds =
        (Option.map ~f:CapacityBlockIds.of_xml)
          (Xml.child xml_arg0 "CapacityBlockId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityBlockIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityBlockStatusMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlockIds =
        field_map json__ "CapacityBlockIds" CapacityBlockIds.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityBlockIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the availability of capacity for the specified Capacity blocks, or all of your Capacity Blocks."]
module DescribeCapacityBlockOfferingsResult =
  struct
    type nonrec t =
      {
      capacityBlockOfferings: CapacityBlockOfferingSet.t option
        [@ocaml.doc
          "The recommended Capacity Block offering for the dates specified."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityBlockOfferings =
      fun ?nextToken -> fun () -> { capacityBlockOfferings; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityBlockOfferingSet",
           (Option.map x.capacityBlockOfferings
              ~f:CapacityBlockOfferingSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityBlockOfferings =
        (Option.map ~f:CapacityBlockOfferingSet.of_xml)
          (Xml.child xml_arg0 "capacityBlockOfferingSet") in
      make ?nextToken ?capacityBlockOfferings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlockOfferings =
        field_map json__ "CapacityBlockOfferings"
          CapacityBlockOfferingSet.of_json in
      make ?nextToken ?capacityBlockOfferings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes Capacity Block offerings available for purchase in the Amazon Web Services Region that you're currently using. With Capacity Blocks, you can purchase a specific GPU instance type or EC2 UltraServer for a period of time. To search for an available Capacity Block offering, you specify a reservation duration and instance count."]
module DescribeCapacityBlockOfferingsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance for which the Capacity Block offering reserves capacity."];
      instanceCount: Integer.t option
        [@ocaml.doc
          "The number of instances for which to reserve capacity. Each Capacity Block can have up to 64 instances, and you can have up to 256 instances across Capacity Blocks."];
      startDateRange: MillisecondDateTime.t option
        [@ocaml.doc
          "The earliest start date for the Capacity Block offering."];
      endDateRange: MillisecondDateTime.t option
        [@ocaml.doc "The latest end date for the Capacity Block offering."];
      capacityDurationHours: Integer.t
        [@ocaml.doc
          "The reservation duration for the Capacity Block, in hours. You must specify the duration in 1-day increments up 14 days, and in 7-day increments up to 182 days."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeCapacityBlockOfferingsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      ultraserverType: String_.t option
        [@ocaml.doc
          "The EC2 UltraServer type of the Capacity Block offerings."];
      ultraserverCount: Integer.t option
        [@ocaml.doc "The number of EC2 UltraServers in the offerings."];
      allAvailabilityZones: Boolean.t option
        [@ocaml.doc
          "Include all Availability Zones and Local Zones, regardless of your opt-in status. If you do not use this parameter, the results include available offerings from all Availability Zones in the Amazon Web Services Region and Local Zones you are opted into."]}
    let context_ = "DescribeCapacityBlockOfferingsRequest"
    let make ?dryRun =
      fun ?instanceType ->
        fun ?instanceCount ->
          fun ?startDateRange ->
            fun ?endDateRange ->
              fun ?nextToken ->
                fun ?maxResults ->
                  fun ?ultraserverType ->
                    fun ?ultraserverCount ->
                      fun ?allAvailabilityZones ->
                        fun ~capacityDurationHours ->
                          fun () ->
                            {
                              dryRun;
                              instanceType;
                              instanceCount;
                              startDateRange;
                              endDateRange;
                              nextToken;
                              maxResults;
                              ultraserverType;
                              ultraserverCount;
                              allAvailabilityZones;
                              capacityDurationHours
                            }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("InstanceCount", (Option.map x.instanceCount ~f:Integer.to_value));
        ("StartDateRange",
          (Option.map x.startDateRange ~f:MillisecondDateTime.to_value));
        ("EndDateRange",
          (Option.map x.endDateRange ~f:MillisecondDateTime.to_value));
        ("CapacityDurationHours",
          (Some (Integer.to_value x.capacityDurationHours)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityBlockOfferingsMaxResults.to_value));
        ("UltraserverType",
          (Option.map x.ultraserverType ~f:String_.to_value));
        ("UltraserverCount",
          (Option.map x.ultraserverCount ~f:Integer.to_value));
        ("AllAvailabilityZones",
          (Option.map x.allAvailabilityZones ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allAvailabilityZones =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AllAvailabilityZones") in
      let ultraserverCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "UltraserverCount") in
      let ultraserverType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UltraserverType") in
      let maxResults =
        (Option.map ~f:DescribeCapacityBlockOfferingsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityDurationHours =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityDurationHours") in
      let endDateRange =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "EndDateRange") in
      let startDateRange =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "StartDateRange") in
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "InstanceCount") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?allAvailabilityZones ?ultraserverCount ?ultraserverType
        ?maxResults ?nextToken ~capacityDurationHours ?endDateRange
        ?startDateRange ?instanceCount ?instanceType ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allAvailabilityZones =
        field_map json__ "AllAvailabilityZones" Boolean.of_json in
      let ultraserverCount =
        field_map json__ "UltraserverCount" Integer.of_json in
      let ultraserverType =
        field_map json__ "UltraserverType" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityBlockOfferingsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityDurationHours =
        field_map_exn json__ "CapacityDurationHours" Integer.of_json in
      let endDateRange =
        field_map json__ "EndDateRange" MillisecondDateTime.of_json in
      let startDateRange =
        field_map json__ "StartDateRange" MillisecondDateTime.of_json in
      let instanceCount = field_map json__ "InstanceCount" Integer.of_json in
      let instanceType = field_map json__ "InstanceType" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?allAvailabilityZones ?ultraserverCount ?ultraserverType
        ?maxResults ?nextToken ~capacityDurationHours ?endDateRange
        ?startDateRange ?instanceCount ?instanceType ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes Capacity Block offerings available for purchase in the Amazon Web Services Region that you're currently using. With Capacity Blocks, you can purchase a specific GPU instance type or EC2 UltraServer for a period of time. To search for an available Capacity Block offering, you specify a reservation duration and instance count."]
module DescribeCapacityBlockExtensionOfferingsResult =
  struct
    type nonrec t =
      {
      capacityBlockExtensionOfferings:
        CapacityBlockExtensionOfferingSet.t option
        [@ocaml.doc
          "The recommended Capacity Block extension offerings for the dates specified."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityBlockExtensionOfferings =
      fun ?nextToken ->
        fun () -> { capacityBlockExtensionOfferings; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityBlockExtensionOfferingSet",
           (Option.map x.capacityBlockExtensionOfferings
              ~f:CapacityBlockExtensionOfferingSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityBlockExtensionOfferings =
        (Option.map ~f:CapacityBlockExtensionOfferingSet.of_xml)
          (Xml.child xml_arg0 "capacityBlockExtensionOfferingSet") in
      make ?nextToken ?capacityBlockExtensionOfferings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlockExtensionOfferings =
        field_map json__ "CapacityBlockExtensionOfferings"
          CapacityBlockExtensionOfferingSet.of_json in
      make ?nextToken ?capacityBlockExtensionOfferings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes Capacity Block extension offerings available for purchase in the Amazon Web Services Region that you're currently using."]
module DescribeCapacityBlockExtensionOfferingsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      capacityBlockExtensionDurationHours: Integer.t
        [@ocaml.doc
          "The duration of the Capacity Block extension offering in hours."];
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc "The ID of the Capacity reservation to be extended."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeCapacityBlockExtensionOfferingsMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."]}
    let context_ = "DescribeCapacityBlockExtensionOfferingsRequest"
    let make ?dryRun =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~capacityBlockExtensionDurationHours ->
            fun ~capacityReservationId ->
              fun () ->
                {
                  dryRun;
                  nextToken;
                  maxResults;
                  capacityBlockExtensionDurationHours;
                  capacityReservationId
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CapacityBlockExtensionDurationHours",
          (Some (Integer.to_value x.capacityBlockExtensionDurationHours)));
        ("CapacityReservationId",
          (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeCapacityBlockExtensionOfferingsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map
           ~f:DescribeCapacityBlockExtensionOfferingsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      let capacityBlockExtensionDurationHours =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CapacityBlockExtensionDurationHours") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?maxResults ?nextToken ~capacityReservationId
        ~capacityBlockExtensionDurationHours ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "MaxResults"
          DescribeCapacityBlockExtensionOfferingsMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityReservationId =
        field_map_exn json__ "CapacityReservationId"
          CapacityReservationId.of_json in
      let capacityBlockExtensionDurationHours =
        field_map_exn json__ "CapacityBlockExtensionDurationHours"
          Integer.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?maxResults ?nextToken ~capacityReservationId
        ~capacityBlockExtensionDurationHours ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes Capacity Block extension offerings available for purchase in the Amazon Web Services Region that you're currently using."]
module DescribeCapacityBlockExtensionHistoryResult =
  struct
    type nonrec t =
      {
      capacityBlockExtensions: CapacityBlockExtensionSet.t option
        [@ocaml.doc
          "Describes one or more of your Capacity Block extensions. The results describe only the Capacity Block extensions in the Amazon Web Services Region that you're currently using."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityBlockExtensions =
      fun ?nextToken -> fun () -> { capacityBlockExtensions; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityBlockExtensionSet",
           (Option.map x.capacityBlockExtensions
              ~f:CapacityBlockExtensionSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let capacityBlockExtensions =
        (Option.map ~f:CapacityBlockExtensionSet.of_xml)
          (Xml.child xml_arg0 "capacityBlockExtensionSet") in
      make ?nextToken ?capacityBlockExtensions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityBlockExtensions =
        field_map json__ "CapacityBlockExtensions"
          CapacityBlockExtensionSet.of_json in
      make ?nextToken ?capacityBlockExtensions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the events for the specified Capacity Block extension during the specified time."]
module DescribeCapacityBlockExtensionHistoryRequest =
  struct
    type nonrec t =
      {
      capacityReservationIds: CapacityReservationIdSet.t option
        [@ocaml.doc
          "The IDs of Capacity Block reservations that you want to display the history for."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: DescribeFutureCapacityMaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. For more information, see Pagination."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters availability-zone - The Availability Zone of the extension. availability-zone-id - The Availability Zone ID of the extension. capacity-block-extension-offering-id - The ID of the extension offering. capacity-block-extension-status - The status of the extension (payment-pending | payment-failed | payment-succeeded). capacity-reservation-id - The reservation ID of the extension. instance-type - The instance type of the extension."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?capacityReservationIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filters ->
            fun ?dryRun ->
              fun () ->
                {
                  capacityReservationIds;
                  nextToken;
                  maxResults;
                  filters;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Option.map x.capacityReservationIds
              ~f:CapacityReservationIdSet.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeFutureCapacityMaxResults.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:DescribeFutureCapacityMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationIds =
        (Option.map ~f:CapacityReservationIdSet.of_xml)
          (Xml.child xml_arg0 "CapacityReservationId") in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityReservationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeFutureCapacityMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let capacityReservationIds =
        field_map json__ "CapacityReservationIds"
          CapacityReservationIdSet.of_json in
      make ?dryRun ?filters ?maxResults ?nextToken ?capacityReservationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the events for the specified Capacity Block extension during the specified time."]
module DescribeByoipCidrsResult =
  struct
    type nonrec t =
      {
      byoipCidrs: ByoipCidrSet.t option
        [@ocaml.doc "Information about your address ranges."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidrs =
      fun ?nextToken -> fun () -> { byoipCidrs; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidrSet", (Option.map x.byoipCidrs ~f:ByoipCidrSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let byoipCidrs =
        (Option.map ~f:ByoipCidrSet.of_xml)
          (Xml.child xml_arg0 "byoipCidrSet") in
      make ?nextToken ?byoipCidrs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let byoipCidrs = field_map json__ "ByoipCidrs" ByoipCidrSet.of_json in
      make ?nextToken ?byoipCidrs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the IP address ranges that were provisioned for use with Amazon Web Services resources through through bring your own IP addresses (BYOIP)."]
module DescribeByoipCidrsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: DescribeByoipCidrsMaxResults.t
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "DescribeByoipCidrsRequest"
    let make ?dryRun =
      fun ?nextToken ->
        fun ~maxResults -> fun () -> { dryRun; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Some (DescribeByoipCidrsMaxResults.to_value x.maxResults)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        DescribeByoipCidrsMaxResults.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ~maxResults ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map_exn json__ "MaxResults"
          DescribeByoipCidrsMaxResults.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?nextToken ~maxResults ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the IP address ranges that were provisioned for use with Amazon Web Services resources through through bring your own IP addresses (BYOIP)."]
module DescribeBundleTasksResult =
  struct
    type nonrec t =
      {
      bundleTasks: BundleTaskList.t option
        [@ocaml.doc "Information about the bundle tasks."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?bundleTasks = fun () -> { bundleTasks }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleInstanceTasksSet",
           (Option.map x.bundleTasks ~f:BundleTaskList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleTasks =
        (Option.map ~f:BundleTaskList.of_xml)
          (Xml.child xml_arg0 "bundleInstanceTasksSet") in
      make ?bundleTasks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bundleTasks = field_map json__ "BundleTasks" BundleTaskList.of_json in
      make ?bundleTasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified bundle tasks or all of your bundle tasks. Completed bundle tasks are listed for only a limited time. If your bundle task is no longer in the list, you can still register an AMI from it. Just use RegisterImage with the Amazon S3 bucket name and image manifest name you provided to the bundle task. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeBundleTasksRequest =
  struct
    type nonrec t =
      {
      bundleIds: BundleIdStringList.t option
        [@ocaml.doc
          "The bundle task IDs. Default: Describes all your bundle tasks."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. bundle-id - The ID of the bundle task. error-code - If the task failed, the error code returned. error-message - If the task failed, the error message returned. instance-id - The ID of the instance. progress - The level of task completion, as a percentage (for example, 20%). s3-bucket - The Amazon S3 bucket to store the AMI. s3-prefix - The beginning of the AMI name. start-time - The time the task started (for example, 2013-09-15T17:15:20.000Z). state - The state of the task (pending | waiting-for-shutdown | bundling | storing | cancelling | complete | failed). update-time - The time of the most recent update for the task."]}
    let make ?bundleIds =
      fun ?dryRun -> fun ?filters -> fun () -> { bundleIds; dryRun; filters }
    let to_value x =
      structure_to_value
        [("BundleId",
           (Option.map x.bundleIds ~f:BundleIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let bundleIds =
        (Option.map ~f:BundleIdStringList.of_xml)
          (Xml.child xml_arg0 "BundleId") in
      make ?filters ?dryRun ?bundleIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let bundleIds = field_map json__ "BundleIds" BundleIdStringList.of_json in
      make ?filters ?dryRun ?bundleIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified bundle tasks or all of your bundle tasks. Completed bundle tasks are listed for only a limited time. If your bundle task is no longer in the list, you can still register an AMI from it. Just use RegisterImage with the Amazon S3 bucket name and image manifest name you provided to the bundle task. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeAwsNetworkPerformanceMetricSubscriptionsResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      subscriptions: SubscriptionList.t option
        [@ocaml.doc
          "Describes the current Infrastructure Performance subscriptions."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?subscriptions -> fun () -> { nextToken; subscriptions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("subscriptionSet",
          (Option.map x.subscriptions ~f:SubscriptionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subscriptions =
        (Option.map ~f:SubscriptionList.of_xml)
          (Xml.child xml_arg0 "subscriptionSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?subscriptions ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subscriptions =
        field_map json__ "Subscriptions" SubscriptionList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      make ?subscriptions ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the current Infrastructure Performance metric subscriptions."]
module DescribeAwsNetworkPerformanceMetricSubscriptionsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResultsParam.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      filters: FilterList.t option [@ocaml.doc "One or more filters."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?filters ->
          fun ?dryRun -> fun () -> { maxResults; nextToken; filters; dryRun }
    let to_value x =
      structure_to_value
        [("MaxResults",
           (Option.map x.maxResults ~f:MaxResultsParam.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResultsParam.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?dryRun ?filters ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResultsParam.of_json in
      make ?dryRun ?filters ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the current Infrastructure Performance metric subscriptions."]
module DescribeAvailabilityZonesResult =
  struct
    type nonrec t =
      {
      availabilityZones: AvailabilityZoneList.t option
        [@ocaml.doc
          "Information about the Availability Zones, Local Zones, and Wavelength Zones."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?availabilityZones = fun () -> { availabilityZones }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("availabilityZoneInfo",
           (Option.map x.availabilityZones ~f:AvailabilityZoneList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZones =
        (Option.map ~f:AvailabilityZoneList.of_xml)
          (Xml.child xml_arg0 "availabilityZoneInfo") in
      make ?availabilityZones ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZones =
        field_map json__ "AvailabilityZones" AvailabilityZoneList.of_json in
      make ?availabilityZones ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Availability Zones, Local Zones, and Wavelength Zones that are available to you. For more information about Availability Zones, Local Zones, and Wavelength Zones, see Regions and zones in the Amazon EC2 User Guide. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeAvailabilityZonesRequest =
  struct
    type nonrec t =
      {
      zoneNames: ZoneNameStringList.t option
        [@ocaml.doc
          "The names of the Availability Zones, Local Zones, and Wavelength Zones."];
      zoneIds: ZoneIdStringList.t option
        [@ocaml.doc
          "The IDs of the Availability Zones, Local Zones, and Wavelength Zones."];
      allAvailabilityZones: Boolean.t option
        [@ocaml.doc
          "Include all Availability Zones, Local Zones, and Wavelength Zones regardless of your opt-in status. If you do not use this parameter, the results include only the zones for the Regions where you have chosen the option to opt in."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filters. group-long-name - The long name of the zone group for the Availability Zone (for example, US West (Oregon) 1), the Local Zone (for example, for Zone group us-west-2-lax-1, it is US West (Los Angeles), or the Wavelength Zone (for example, for Zone group us-east-1-wl1, it is US East (Verizon). group-name - The name of the zone group for the Availability Zone (for example, us-east-1-zg-1), the Local Zone (for example, us-west-2-lax-1), or the Wavelength Zone (for example, us-east-1-wl1). message - The Zone message. opt-in-status - The opt-in status (opted-in | not-opted-in | opt-in-not-required). parent-zone-id - The ID of the zone that handles some of the Local Zone and Wavelength Zone control plane operations, such as API calls. parent-zone-name - The ID of the zone that handles some of the Local Zone and Wavelength Zone control plane operations, such as API calls. region-name - The name of the Region for the Zone (for example, us-east-1). state - The state of the Availability Zone, the Local Zone, or the Wavelength Zone (available | unavailable | constrained). zone-id - The ID of the Availability Zone (for example, use1-az1), the Local Zone (for example, usw2-lax1-az1), or the Wavelength Zone (for example, us-east-1-wl1-bos-wlz-1). zone-name - The name of the Availability Zone (for example, us-east-1a), the Local Zone (for example, us-west-2-lax-1a), or the Wavelength Zone (for example, us-east-1-wl1-bos-wlz-1). zone-type - The type of zone (availability-zone | local-zone | wavelength-zone)."]}
    let make ?zoneNames =
      fun ?zoneIds ->
        fun ?allAvailabilityZones ->
          fun ?dryRun ->
            fun ?filters ->
              fun () ->
                { zoneNames; zoneIds; allAvailabilityZones; dryRun; filters }
    let to_value x =
      structure_to_value
        [("ZoneName",
           (Option.map x.zoneNames ~f:ZoneNameStringList.to_value));
        ("ZoneId", (Option.map x.zoneIds ~f:ZoneIdStringList.to_value));
        ("AllAvailabilityZones",
          (Option.map x.allAvailabilityZones ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let allAvailabilityZones =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AllAvailabilityZones") in
      let zoneIds =
        (Option.map ~f:ZoneIdStringList.of_xml) (Xml.child xml_arg0 "ZoneId") in
      let zoneNames =
        (Option.map ~f:ZoneNameStringList.of_xml)
          (Xml.child xml_arg0 "ZoneName") in
      make ?filters ?dryRun ?allAvailabilityZones ?zoneIds ?zoneNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let allAvailabilityZones =
        field_map json__ "AllAvailabilityZones" Boolean.of_json in
      let zoneIds = field_map json__ "ZoneIds" ZoneIdStringList.of_json in
      let zoneNames = field_map json__ "ZoneNames" ZoneNameStringList.of_json in
      make ?filters ?dryRun ?allAvailabilityZones ?zoneIds ?zoneNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Availability Zones, Local Zones, and Wavelength Zones that are available to you. For more information about Availability Zones, Local Zones, and Wavelength Zones, see Regions and zones in the Amazon EC2 User Guide. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeAggregateIdFormatResult =
  struct
    type nonrec t =
      {
      useLongIdsAggregated: Boolean.t option
        [@ocaml.doc
          "Indicates whether all resource types in the Region are configured to use longer IDs. This value is only true if all users are configured to use longer IDs for all resources types in the Region."];
      statuses: IdFormatList.t option
        [@ocaml.doc "Information about each resource's ID format."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?useLongIdsAggregated =
      fun ?statuses -> fun () -> { useLongIdsAggregated; statuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("useLongIdsAggregated",
           (Option.map x.useLongIdsAggregated ~f:Boolean.to_value));
        ("statusSet", (Option.map x.statuses ~f:IdFormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:IdFormatList.of_xml) (Xml.child xml_arg0 "statusSet") in
      let useLongIdsAggregated =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "useLongIdsAggregated") in
      make ?statuses ?useLongIdsAggregated ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statuses = field_map json__ "Statuses" IdFormatList.of_json in
      let useLongIdsAggregated =
        field_map json__ "UseLongIdsAggregated" Boolean.of_json in
      make ?statuses ?useLongIdsAggregated ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the longer ID format settings for all resource types in a specific Region. This request is useful for performing a quick audit to determine whether a specific Region is fully opted in for longer IDs (17-character IDs). This request only returns information about resource types that support longer IDs. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway."]
module DescribeAggregateIdFormatRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the longer ID format settings for all resource types in a specific Region. This request is useful for performing a quick audit to determine whether a specific Region is fully opted in for longer IDs (17-character IDs). This request only returns information about resource types that support longer IDs. The following resource types support longer IDs: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | instance | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | reservation | route-table | route-table-association | security-group | snapshot | subnet | subnet-cidr-block-association | volume | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway."]
module DescribeAddressesResult =
  struct
    type nonrec t =
      {
      addresses: AddressList.t option
        [@ocaml.doc "Information about the Elastic IP addresses."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?addresses = fun () -> { addresses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("addressesSet", (Option.map x.addresses ~f:AddressList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let addresses =
        (Option.map ~f:AddressList.of_xml)
          (Xml.child xml_arg0 "addressesSet") in
      make ?addresses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let addresses = field_map json__ "Addresses" AddressList.of_json in
      make ?addresses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified Elastic IP addresses or all of your Elastic IP addresses."]
module DescribeAddressesRequest =
  struct
    type nonrec t =
      {
      publicIps: PublicIpStringList.t option
        [@ocaml.doc
          "One or more Elastic IP addresses. Default: Describes all your Elastic IP addresses."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. Filter names and values are case-sensitive. allocation-id - The allocation ID for the address. association-id - The association ID for the address. instance-id - The ID of the instance the address is associated with, if any. network-border-group - A unique set of Availability Zones, Local Zones, or Wavelength Zones from where Amazon Web Services advertises IP addresses. network-interface-id - The ID of the network interface that the address is associated with, if any. network-interface-owner-id - The Amazon Web Services account ID of the owner. private-ip-address - The private IP address associated with the Elastic IP address. public-ip - The Elastic IP address, or the carrier IP address. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      allocationIds: AllocationIdList.t option
        [@ocaml.doc "Information about the allocation IDs."]}
    let make ?publicIps =
      fun ?dryRun ->
        fun ?filters ->
          fun ?allocationIds ->
            fun () -> { publicIps; dryRun; filters; allocationIds }
    let to_value x =
      structure_to_value
        [("PublicIp",
           (Option.map x.publicIps ~f:PublicIpStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("AllocationId",
          (Option.map x.allocationIds ~f:AllocationIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allocationIds =
        (Option.map ~f:AllocationIdList.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let publicIps =
        (Option.map ~f:PublicIpStringList.of_xml)
          (Xml.child xml_arg0 "PublicIp") in
      make ?allocationIds ?filters ?dryRun ?publicIps ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allocationIds =
        field_map json__ "AllocationIds" AllocationIdList.of_json in
      let filters = field_map json__ "Filters" FilterList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let publicIps = field_map json__ "PublicIps" PublicIpStringList.of_json in
      make ?allocationIds ?filters ?dryRun ?publicIps ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified Elastic IP addresses or all of your Elastic IP addresses."]
module DescribeAddressesAttributeResult =
  struct
    type nonrec t =
      {
      addresses: AddressSet.t option
        [@ocaml.doc "Information about the IP addresses."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?addresses =
      fun ?nextToken -> fun () -> { addresses; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("addressSet", (Option.map x.addresses ~f:AddressSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let addresses =
        (Option.map ~f:AddressSet.of_xml) (Xml.child xml_arg0 "addressSet") in
      make ?nextToken ?addresses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let addresses = field_map json__ "Addresses" AddressSet.of_json in
      make ?nextToken ?addresses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the attributes of the specified Elastic IP addresses. For requirements, see Using reverse DNS for email applications."]
module DescribeAddressesAttributeRequest =
  struct
    type nonrec t =
      {
      allocationIds: AllocationIds.t option
        [@ocaml.doc "\\[EC2-VPC\\] The allocation IDs."];
      attribute: AddressAttributeName.t option
        [@ocaml.doc "The attribute of the IP address."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: AddressMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?allocationIds =
      fun ?attribute ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?dryRun ->
              fun () ->
                { allocationIds; attribute; nextToken; maxResults; dryRun }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationIds ~f:AllocationIds.to_value));
        ("Attribute",
          (Option.map x.attribute ~f:AddressAttributeName.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:AddressMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:AddressMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let attribute =
        (Option.map ~f:AddressAttributeName.of_xml)
          (Xml.child xml_arg0 "Attribute") in
      let allocationIds =
        (Option.map ~f:AllocationIds.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?dryRun ?maxResults ?nextToken ?attribute ?allocationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults" AddressMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let attribute =
        field_map json__ "Attribute" AddressAttributeName.of_json in
      let allocationIds =
        field_map json__ "AllocationIds" AllocationIds.of_json in
      make ?dryRun ?maxResults ?nextToken ?attribute ?allocationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the attributes of the specified Elastic IP addresses. For requirements, see Using reverse DNS for email applications."]
module DescribeAddressTransfersResult =
  struct
    type nonrec t =
      {
      addressTransfers: AddressTransferList.t option
        [@ocaml.doc "The Elastic IP address transfer."];
      nextToken: String_.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?addressTransfers =
      fun ?nextToken -> fun () -> { addressTransfers; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("addressTransferSet",
           (Option.map x.addressTransfers ~f:AddressTransferList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let addressTransfers =
        (Option.map ~f:AddressTransferList.of_xml)
          (Xml.child xml_arg0 "addressTransferSet") in
      make ?nextToken ?addressTransfers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let addressTransfers =
        field_map json__ "AddressTransfers" AddressTransferList.of_json in
      make ?nextToken ?addressTransfers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an Elastic IP address transfer. For more information, see Transfer Elastic IP addresses in the Amazon VPC User Guide. When you transfer an Elastic IP address, there is a two-step handshake between the source and transfer Amazon Web Services accounts. When the source account starts the transfer, the transfer account has seven days to accept the Elastic IP address transfer. During those seven days, the source account can view the pending transfer by using this action. After seven days, the transfer expires and ownership of the Elastic IP address returns to the source account. Accepted transfers are visible to the source account for 14 days after the transfers have been accepted."]
module DescribeAddressTransfersRequest =
  struct
    type nonrec t =
      {
      allocationIds: AllocationIdList.t option
        [@ocaml.doc "The allocation IDs of Elastic IP addresses."];
      nextToken: String_.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."];
      maxResults: DescribeAddressTransfersMaxResults.t option
        [@ocaml.doc
          "The maximum number of address transfers to return in one page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?allocationIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun () -> { allocationIds; nextToken; maxResults; dryRun }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationIds ~f:AllocationIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:DescribeAddressTransfersMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:DescribeAddressTransfersMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let allocationIds =
        (Option.map ~f:AllocationIdList.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?dryRun ?maxResults ?nextToken ?allocationIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          DescribeAddressTransfersMaxResults.of_json in
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let allocationIds =
        field_map json__ "AllocationIds" AllocationIdList.of_json in
      make ?dryRun ?maxResults ?nextToken ?allocationIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an Elastic IP address transfer. For more information, see Transfer Elastic IP addresses in the Amazon VPC User Guide. When you transfer an Elastic IP address, there is a two-step handshake between the source and transfer Amazon Web Services accounts. When the source account starts the transfer, the transfer account has seven days to accept the Elastic IP address transfer. During those seven days, the source account can view the pending transfer by using this action. After seven days, the transfer expires and ownership of the Elastic IP address returns to the source account. Accepted transfers are visible to the source account for 14 days after the transfers have been accepted."]
module DescribeAccountAttributesResult =
  struct
    type nonrec t =
      {
      accountAttributes: AccountAttributeList.t option
        [@ocaml.doc "Information about the account attributes."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?accountAttributes = fun () -> { accountAttributes }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("accountAttributeSet",
           (Option.map x.accountAttributes ~f:AccountAttributeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountAttributes =
        (Option.map ~f:AccountAttributeList.of_xml)
          (Xml.child xml_arg0 "accountAttributeSet") in
      make ?accountAttributes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accountAttributes =
        field_map json__ "AccountAttributes" AccountAttributeList.of_json in
      make ?accountAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes attributes of your Amazon Web Services account. The following are the supported account attributes: default-vpc: The ID of the default VPC for your account, or none. max-instances: This attribute is no longer supported. The returned value does not reflect your actual vCPU limit for running On-Demand Instances. For more information, see On-Demand Instance Limits in the Amazon Elastic Compute Cloud User Guide. max-elastic-ips: The maximum number of Elastic IP addresses that you can allocate. supported-platforms: This attribute is deprecated. vpc-max-elastic-ips: The maximum number of Elastic IP addresses that you can allocate. vpc-max-security-groups-per-interface: The maximum number of security groups that you can assign to a network interface. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DescribeAccountAttributesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      attributeNames: AccountAttributeNameStringList.t option
        [@ocaml.doc "The account attribute names."]}
    let make ?dryRun =
      fun ?attributeNames -> fun () -> { dryRun; attributeNames }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("attributeName",
          (Option.map x.attributeNames
             ~f:AccountAttributeNameStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributeNames =
        (Option.map ~f:AccountAttributeNameStringList.of_xml)
          (Xml.child xml_arg0 "attributeName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?attributeNames ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attributeNames =
        field_map json__ "AttributeNames"
          AccountAttributeNameStringList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?attributeNames ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes attributes of your Amazon Web Services account. The following are the supported account attributes: default-vpc: The ID of the default VPC for your account, or none. max-instances: This attribute is no longer supported. The returned value does not reflect your actual vCPU limit for running On-Demand Instances. For more information, see On-Demand Instance Limits in the Amazon Elastic Compute Cloud User Guide. max-elastic-ips: The maximum number of Elastic IP addresses that you can allocate. supported-platforms: This attribute is deprecated. vpc-max-elastic-ips: The maximum number of Elastic IP addresses that you can allocate. vpc-max-security-groups-per-interface: The maximum number of security groups that you can assign to a network interface. The order of the elements in the response, including those within nested structures, might vary. Applications should not assume the elements appear in a particular order."]
module DeregisterTransitGatewayMulticastGroupSourcesResult =
  struct
    type nonrec t =
      {
      deregisteredMulticastGroupSources:
        TransitGatewayMulticastDeregisteredGroupSources.t option
        [@ocaml.doc "Information about the deregistered group sources."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?deregisteredMulticastGroupSources =
      fun () -> { deregisteredMulticastGroupSources }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deregisteredMulticastGroupSources",
           (Option.map x.deregisteredMulticastGroupSources
              ~f:TransitGatewayMulticastDeregisteredGroupSources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deregisteredMulticastGroupSources =
        (Option.map ~f:TransitGatewayMulticastDeregisteredGroupSources.of_xml)
          (Xml.child xml_arg0 "deregisteredMulticastGroupSources") in
      make ?deregisteredMulticastGroupSources ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deregisteredMulticastGroupSources =
        field_map json__ "DeregisteredMulticastGroupSources"
          TransitGatewayMulticastDeregisteredGroupSources.of_json in
      make ?deregisteredMulticastGroupSources ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified sources (network interfaces) from the transit gateway multicast group."]
module DeregisterTransitGatewayMulticastGroupSourcesRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      groupIpAddress: String_.t option
        [@ocaml.doc
          "The IP address assigned to the transit gateway multicast group."];
      networkInterfaceIds: TransitGatewayNetworkInterfaceIdList.t option
        [@ocaml.doc "The IDs of the group sources' network interfaces."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?groupIpAddress ->
        fun ?networkInterfaceIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                groupIpAddress;
                networkInterfaceIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("GroupIpAddress", (Option.map x.groupIpAddress ~f:String_.to_value));
        ("NetworkInterfaceIds",
          (Option.map x.networkInterfaceIds
             ~f:TransitGatewayNetworkInterfaceIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInterfaceIds =
        (Option.map ~f:TransitGatewayNetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceIds") in
      let groupIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupIpAddress") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let networkInterfaceIds =
        field_map json__ "NetworkInterfaceIds"
          TransitGatewayNetworkInterfaceIdList.of_json in
      let groupIpAddress = field_map json__ "GroupIpAddress" String_.of_json in
      let transitGatewayMulticastDomainId =
        field_map json__ "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified sources (network interfaces) from the transit gateway multicast group."]
module DeregisterTransitGatewayMulticastGroupMembersResult =
  struct
    type nonrec t =
      {
      deregisteredMulticastGroupMembers:
        TransitGatewayMulticastDeregisteredGroupMembers.t option
        [@ocaml.doc "Information about the deregistered members."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?deregisteredMulticastGroupMembers =
      fun () -> { deregisteredMulticastGroupMembers }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deregisteredMulticastGroupMembers",
           (Option.map x.deregisteredMulticastGroupMembers
              ~f:TransitGatewayMulticastDeregisteredGroupMembers.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deregisteredMulticastGroupMembers =
        (Option.map ~f:TransitGatewayMulticastDeregisteredGroupMembers.of_xml)
          (Xml.child xml_arg0 "deregisteredMulticastGroupMembers") in
      make ?deregisteredMulticastGroupMembers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deregisteredMulticastGroupMembers =
        field_map json__ "DeregisteredMulticastGroupMembers"
          TransitGatewayMulticastDeregisteredGroupMembers.of_json in
      make ?deregisteredMulticastGroupMembers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified members (network interfaces) from the transit gateway multicast group."]
module DeregisterTransitGatewayMulticastGroupMembersRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      groupIpAddress: String_.t option
        [@ocaml.doc
          "The IP address assigned to the transit gateway multicast group."];
      networkInterfaceIds: TransitGatewayNetworkInterfaceIdList.t option
        [@ocaml.doc "The IDs of the group members' network interfaces."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?groupIpAddress ->
        fun ?networkInterfaceIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                groupIpAddress;
                networkInterfaceIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("GroupIpAddress", (Option.map x.groupIpAddress ~f:String_.to_value));
        ("NetworkInterfaceIds",
          (Option.map x.networkInterfaceIds
             ~f:TransitGatewayNetworkInterfaceIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInterfaceIds =
        (Option.map ~f:TransitGatewayNetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceIds") in
      let groupIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupIpAddress") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let networkInterfaceIds =
        field_map json__ "NetworkInterfaceIds"
          TransitGatewayNetworkInterfaceIdList.of_json in
      let groupIpAddress = field_map json__ "GroupIpAddress" String_.of_json in
      let transitGatewayMulticastDomainId =
        field_map json__ "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified members (network interfaces) from the transit gateway multicast group."]
module DeregisterInstanceEventNotificationAttributesResult =
  struct
    type nonrec t =
      {
      instanceTagAttribute: InstanceTagNotificationAttribute.t option
        [@ocaml.doc "The resulting set of tag keys."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTagAttribute = fun () -> { instanceTagAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTagAttribute",
           (Option.map x.instanceTagAttribute
              ~f:InstanceTagNotificationAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:InstanceTagNotificationAttribute.of_xml)
          (Xml.child xml_arg0 "instanceTagAttribute") in
      make ?instanceTagAttribute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceTagAttribute =
        field_map json__ "InstanceTagAttribute"
          InstanceTagNotificationAttribute.of_json in
      make ?instanceTagAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters tag keys to prevent tags that have the specified tag keys from being included in scheduled event notifications for resources in the Region."]
module DeregisterInstanceEventNotificationAttributesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceTagAttribute: DeregisterInstanceTagAttributeRequest.t
        [@ocaml.doc "Information about the tag keys to deregister."]}
    let context_ = "DeregisterInstanceEventNotificationAttributesRequest"
    let make ?dryRun =
      fun ~instanceTagAttribute -> fun () -> { dryRun; instanceTagAttribute }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceTagAttribute",
          (Some
             (DeregisterInstanceTagAttributeRequest.to_value
                x.instanceTagAttribute)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        DeregisterInstanceTagAttributeRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceTagAttribute") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceTagAttribute ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceTagAttribute =
        field_map_exn json__ "InstanceTagAttribute"
          DeregisterInstanceTagAttributeRequest.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~instanceTagAttribute ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters tag keys to prevent tags that have the specified tag keys from being included in scheduled event notifications for resources in the Region."]
module DeregisterImageResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."];
      deleteSnapshotResults: DeleteSnapshotResultSet.t option
        [@ocaml.doc
          "The deletion result for each snapshot associated with the AMI, including the snapshot ID and its success or error code."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?deleteSnapshotResults ->
        fun () -> { return; deleteSnapshotResults }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("deleteSnapshotResultSet",
          (Option.map x.deleteSnapshotResults
             ~f:DeleteSnapshotResultSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deleteSnapshotResults =
        (Option.map ~f:DeleteSnapshotResultSet.of_xml)
          (Xml.child xml_arg0 "deleteSnapshotResultSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?deleteSnapshotResults ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deleteSnapshotResults =
        field_map json__ "DeleteSnapshotResults"
          DeleteSnapshotResultSet.of_json in
      let return = field_map json__ "Return" Boolean.of_json in
      make ?deleteSnapshotResults ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified AMI. A deregistered AMI can't be used to launch new instances. If a deregistered EBS-backed AMI matches a Recycle Bin retention rule, it moves to the Recycle Bin for the specified retention period. It can be restored before its retention period expires, after which it is permanently deleted. If the deregistered AMI doesn't match a retention rule, it is permanently deleted immediately. For more information, see Recover deleted Amazon EBS snapshots and EBS-backed AMIs with Recycle Bin in the Amazon EBS User Guide. When deregistering an EBS-backed AMI, you can optionally delete its associated snapshots at the same time. However, if a snapshot is associated with multiple AMIs, it won't be deleted even if specified for deletion, although the AMI will still be deregistered. Deregistering an AMI does not delete the following: Instances already launched from the AMI. You'll continue to incur usage costs for the instances until you terminate them. For EBS-backed AMIs: Snapshots that are associated with multiple AMIs. You'll continue to incur snapshot storage costs. For instance store-backed AMIs: The files uploaded to Amazon S3 during AMI creation. You'll continue to incur S3 storage costs. For more information, see Deregister an Amazon EC2 AMI in the Amazon EC2 User Guide."]
module DeregisterImageRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t [@ocaml.doc "The ID of the AMI."];
      deleteAssociatedSnapshots: Boolean.t option
        [@ocaml.doc
          "Specifies whether to delete the snapshots associated with the AMI during deregistration. If a snapshot is associated with multiple AMIs, it is not deleted, regardless of this setting. Default: The snapshots are not deleted."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeregisterImageRequest"
    let make ?deleteAssociatedSnapshots =
      fun ?dryRun ->
        fun ~imageId ->
          fun () -> { deleteAssociatedSnapshots; dryRun; imageId }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("DeleteAssociatedSnapshots",
          (Option.map x.deleteAssociatedSnapshots ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let deleteAssociatedSnapshots =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DeleteAssociatedSnapshots") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ?deleteAssociatedSnapshots ~imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let deleteAssociatedSnapshots =
        field_map json__ "DeleteAssociatedSnapshots" Boolean.of_json in
      let imageId = field_map_exn json__ "ImageId" ImageId.of_json in
      make ?dryRun ?deleteAssociatedSnapshots ~imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeregisterImage."]
module DeprovisionPublicIpv4PoolCidrResult =
  struct
    type nonrec t =
      {
      poolId: Ipv4PoolEc2Id.t option
        [@ocaml.doc
          "The ID of the pool that you deprovisioned the CIDR from."];
      deprovisionedAddresses: DeprovisionedAddressSet.t option
        [@ocaml.doc "The deprovisioned CIDRs."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?poolId =
      fun ?deprovisionedAddresses ->
        fun () -> { poolId; deprovisionedAddresses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("poolId", (Option.map x.poolId ~f:Ipv4PoolEc2Id.to_value));
        ("deprovisionedAddressSet",
          (Option.map x.deprovisionedAddresses
             ~f:DeprovisionedAddressSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deprovisionedAddresses =
        (Option.map ~f:DeprovisionedAddressSet.of_xml)
          (Xml.child xml_arg0 "deprovisionedAddressSet") in
      let poolId =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml) (Xml.child xml_arg0 "poolId") in
      make ?deprovisionedAddresses ?poolId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deprovisionedAddresses =
        field_map json__ "DeprovisionedAddresses"
          DeprovisionedAddressSet.of_json in
      let poolId = field_map json__ "PoolId" Ipv4PoolEc2Id.of_json in
      make ?deprovisionedAddresses ?poolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deprovision a CIDR from a public IPv4 pool."]
module DeprovisionPublicIpv4PoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      poolId: Ipv4PoolEc2Id.t
        [@ocaml.doc
          "The ID of the pool that you want to deprovision the CIDR from."];
      cidr: String_.t
        [@ocaml.doc "The CIDR you want to deprovision from the pool."]}
    let context_ = "DeprovisionPublicIpv4PoolCidrRequest"
    let make ?dryRun =
      fun ~poolId -> fun ~cidr -> fun () -> { dryRun; poolId; cidr }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PoolId", (Some (Ipv4PoolEc2Id.to_value x.poolId)));
        ("Cidr", (Some (String_.to_value x.cidr)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let poolId =
        Ipv4PoolEc2Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~cidr ~poolId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      let poolId = field_map_exn json__ "PoolId" Ipv4PoolEc2Id.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~cidr ~poolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deprovision a CIDR from a public IPv4 pool."]
module DeprovisionIpamPoolCidrResult =
  struct
    type nonrec t =
      {
      ipamPoolCidr: IpamPoolCidr.t option
        [@ocaml.doc "The deprovisioned pool CIDR."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolCidr = fun () -> { ipamPoolCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolCidr",
           (Option.map x.ipamPoolCidr ~f:IpamPoolCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolCidr =
        (Option.map ~f:IpamPoolCidr.of_xml)
          (Xml.child xml_arg0 "ipamPoolCidr") in
      make ?ipamPoolCidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPoolCidr = field_map json__ "IpamPoolCidr" IpamPoolCidr.of_json in
      make ?ipamPoolCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deprovision a CIDR provisioned from an IPAM pool. If you deprovision a CIDR from a pool that has a source pool, the CIDR is recycled back into the source pool. For more information, see Deprovision pool CIDRs in the Amazon VPC IPAM User Guide."]
module DeprovisionIpamPoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc
          "The ID of the pool that has the CIDR you want to deprovision."];
      cidr: String_.t option
        [@ocaml.doc "The CIDR which you want to deprovision from the pool."]}
    let context_ = "DeprovisionIpamPoolCidrRequest"
    let make ?dryRun =
      fun ?cidr -> fun ~ipamPoolId -> fun () -> { dryRun; cidr; ipamPoolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cidr", (Option.map x.cidr ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidr = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Cidr") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cidr ~ipamPoolId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cidr = field_map json__ "Cidr" String_.of_json in
      let ipamPoolId = field_map_exn json__ "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?cidr ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deprovision a CIDR provisioned from an IPAM pool. If you deprovision a CIDR from a pool that has a source pool, the CIDR is recycled back into the source pool. For more information, see Deprovision pool CIDRs in the Amazon VPC IPAM User Guide."]
module DeprovisionIpamByoasnResult =
  struct
    type nonrec t =
      {
      byoasn: Byoasn.t option
        [@ocaml.doc "An ASN and BYOIP CIDR association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoasn = fun () -> { byoasn }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoasn", (Option.map x.byoasn ~f:Byoasn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoasn =
        (Option.map ~f:Byoasn.of_xml) (Xml.child xml_arg0 "byoasn") in
      make ?byoasn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let byoasn = field_map json__ "Byoasn" Byoasn.of_json in
      make ?byoasn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deprovisions your Autonomous System Number (ASN) from your Amazon Web Services account. This action can only be called after any BYOIP CIDR associations are removed from your Amazon Web Services account with DisassociateIpamByoasn. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide."]
module DeprovisionIpamByoasnRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t [@ocaml.doc "The IPAM ID."];
      asn: String_.t [@ocaml.doc "An ASN."]}
    let context_ = "DeprovisionIpamByoasnRequest"
    let make ?dryRun =
      fun ~ipamId -> fun ~asn -> fun () -> { dryRun; ipamId; asn }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Asn", (Some (String_.to_value x.asn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let asn =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Asn") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~asn ~ipamId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let asn = field_map_exn json__ "Asn" String_.of_json in
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~asn ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deprovisions your Autonomous System Number (ASN) from your Amazon Web Services account. This action can only be called after any BYOIP CIDR associations are removed from your Amazon Web Services account with DisassociateIpamByoasn. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide."]
module DeprovisionByoipCidrResult =
  struct
    type nonrec t =
      {
      byoipCidr: ByoipCidr.t option
        [@ocaml.doc "Information about the address range."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let byoipCidr = field_map json__ "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Releases the specified address range that you provisioned for use with your Amazon Web Services resources through bring your own IP addresses (BYOIP) and deletes the corresponding address pool. Before you can release an address range, you must stop advertising it and you must not have any IP addresses allocated from its address range."]
module DeprovisionByoipCidrRequest =
  struct
    type nonrec t =
      {
      cidr: String_.t
        [@ocaml.doc
          "The address range, in CIDR notation. The prefix must be the same prefix that you specified when you provisioned the address range."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeprovisionByoipCidrRequest"
    let make ?dryRun = fun ~cidr -> fun () -> { dryRun; cidr }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?dryRun ~cidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      make ?dryRun ~cidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Releases the specified address range that you provisioned for use with your Amazon Web Services resources through bring your own IP addresses (BYOIP) and deletes the corresponding address pool. Before you can release an address range, you must stop advertising it and you must not have any IP addresses allocated from its address range."]
module DeleteVpnGatewayRequest =
  struct
    type nonrec t =
      {
      vpnGatewayId: VpnGatewayId.t
        [@ocaml.doc "The ID of the virtual private gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVpnGatewayRequest"
    let make ?dryRun =
      fun ~vpnGatewayId -> fun () -> { dryRun; vpnGatewayId }
    let to_value x =
      structure_to_value
        [("VpnGatewayId", (Some (VpnGatewayId.to_value x.vpnGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnGatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnGatewayId") in
      make ?dryRun ~vpnGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpnGatewayId =
        field_map_exn json__ "VpnGatewayId" VpnGatewayId.of_json in
      make ?dryRun ~vpnGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteVpnGateway."]
module DeleteVpnConnectionRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The CIDR block associated with the local subnet of the customer network."];
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc "The ID of the VPN connection."]}
    let context_ = "DeleteVpnConnectionRouteRequest"
    let make ~destinationCidrBlock =
      fun ~vpnConnectionId ->
        fun () -> { destinationCidrBlock; vpnConnectionId }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("VpnConnectionId",
          (Some (VpnConnectionId.to_value x.vpnConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpnConnectionId =
        field_map_exn json__ "VpnConnectionId" VpnConnectionId.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteVpnConnectionRoute."]
module DeleteVpnConnectionRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc "The ID of the VPN connection."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVpnConnectionRequest"
    let make ?dryRun =
      fun ~vpnConnectionId -> fun () -> { dryRun; vpnConnectionId }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ~vpnConnectionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpnConnectionId =
        field_map_exn json__ "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ~vpnConnectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteVpnConnection."]
module DeleteVpnConcentratorResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified VPN concentrator."]
module DeleteVpnConcentratorRequest =
  struct
    type nonrec t =
      {
      vpnConcentratorId: VpnConcentratorId.t
        [@ocaml.doc "The ID of the VPN concentrator to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVpnConcentratorRequest"
    let make ?dryRun =
      fun ~vpnConcentratorId -> fun () -> { dryRun; vpnConcentratorId }
    let to_value x =
      structure_to_value
        [("VpnConcentratorId",
           (Some (VpnConcentratorId.to_value x.vpnConcentratorId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let vpnConcentratorId =
        VpnConcentratorId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConcentratorId") in
      make ?dryRun ~vpnConcentratorId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpnConcentratorId =
        field_map_exn json__ "VpnConcentratorId" VpnConcentratorId.of_json in
      make ?dryRun ~vpnConcentratorId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified VPN concentrator."]
module DeleteVpcRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVpcRequest"
    let make ?dryRun = fun ~vpcId -> fun () -> { dryRun; vpcId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?dryRun ~vpcId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      make ?dryRun ~vpcId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC. You must detach or delete all gateways and resources that are associated with the VPC before you can delete it. For example, you must terminate all instances running in the VPC, delete all security groups associated with the VPC (except the default one), delete all route tables associated with the VPC (except the default one), and so on. When you delete the VPC, it deletes the default security group, network ACL, and route table for the VPC. If you created a flow log for the VPC that you are deleting, note that flow logs for deleted VPCs are eventually automatically removed."]
module DeleteVpcPeeringConnectionResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a VPC peering connection. Either the owner of the requester VPC or the owner of the accepter VPC can delete the VPC peering connection if it's in the active state. The owner of the requester VPC can delete a VPC peering connection in the pending-acceptance state. You cannot delete a VPC peering connection that's in the failed or rejected state."]
module DeleteVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcPeeringConnectionId: VpcPeeringConnectionId.t
        [@ocaml.doc "The ID of the VPC peering connection."]}
    let context_ = "DeleteVpcPeeringConnectionRequest"
    let make ?dryRun =
      fun ~vpcPeeringConnectionId ->
        fun () -> { dryRun; vpcPeeringConnectionId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcPeeringConnectionId",
          (Some (VpcPeeringConnectionId.to_value x.vpcPeeringConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        VpcPeeringConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpcPeeringConnectionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcPeeringConnectionId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringConnectionId =
        field_map_exn json__ "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~vpcPeeringConnectionId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a VPC peering connection. Either the owner of the requester VPC or the owner of the accepter VPC can delete the VPC peering connection if it's in the active state. The owner of the requester VPC can delete a VPC peering connection in the pending-acceptance state. You cannot delete a VPC peering connection that's in the failed or rejected state."]
module DeleteVpcEndpointsResult =
  struct
    type nonrec t =
      {
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the VPC endpoints that were not successfully deleted."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessful =
        field_map json__ "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC endpoints. When you delete a gateway endpoint, we delete the endpoint routes in the route tables for the endpoint. When you delete a Gateway Load Balancer endpoint, we delete its endpoint network interfaces. You can only delete Gateway Load Balancer endpoints when the routes that are associated with the endpoint are deleted. When you delete an interface endpoint, we delete its endpoint network interfaces."]
module DeleteVpcEndpointsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcEndpointIds: VpcEndpointIdList.t
        [@ocaml.doc "The IDs of the VPC endpoints."]}
    let context_ = "DeleteVpcEndpointsRequest"
    let make ?dryRun =
      fun ~vpcEndpointIds -> fun () -> { dryRun; vpcEndpointIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointId",
          (Some (VpcEndpointIdList.to_value x.vpcEndpointIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEndpointIds =
        VpcEndpointIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcEndpointIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcEndpointIds =
        field_map_exn json__ "VpcEndpointIds" VpcEndpointIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~vpcEndpointIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC endpoints. When you delete a gateway endpoint, we delete the endpoint routes in the route tables for the endpoint. When you delete a Gateway Load Balancer endpoint, we delete its endpoint network interfaces. You can only delete Gateway Load Balancer endpoints when the routes that are associated with the endpoint are deleted. When you delete an interface endpoint, we delete its endpoint network interfaces."]
module DeleteVpcEndpointServiceConfigurationsResult =
  struct
    type nonrec t =
      {
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the service configurations that were not deleted, if applicable."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessful =
        field_map json__ "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC endpoint service configurations. Before you can delete an endpoint service configuration, you must reject any Available or PendingAcceptance interface endpoint connections that are attached to the service."]
module DeleteVpcEndpointServiceConfigurationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceIds: VpcEndpointServiceIdList.t
        [@ocaml.doc "The IDs of the services."]}
    let context_ = "DeleteVpcEndpointServiceConfigurationsRequest"
    let make ?dryRun = fun ~serviceIds -> fun () -> { dryRun; serviceIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId",
          (Some (VpcEndpointServiceIdList.to_value x.serviceIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceIds =
        VpcEndpointServiceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~serviceIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceIds =
        field_map_exn json__ "ServiceIds" VpcEndpointServiceIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~serviceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC endpoint service configurations. Before you can delete an endpoint service configuration, you must reject any Available or PendingAcceptance interface endpoint connections that are attached to the service."]
module DeleteVpcEndpointConnectionNotificationsResult =
  struct
    type nonrec t =
      {
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the notifications that could not be deleted successfully."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessful =
        field_map json__ "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC endpoint connection notifications."]
module DeleteVpcEndpointConnectionNotificationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      connectionNotificationIds: ConnectionNotificationIdsList.t
        [@ocaml.doc "The IDs of the notifications."]}
    let context_ = "DeleteVpcEndpointConnectionNotificationsRequest"
    let make ?dryRun =
      fun ~connectionNotificationIds ->
        fun () -> { dryRun; connectionNotificationIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ConnectionNotificationId",
          (Some
             (ConnectionNotificationIdsList.to_value
                x.connectionNotificationIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectionNotificationIds =
        ConnectionNotificationIdsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ConnectionNotificationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~connectionNotificationIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectionNotificationIds =
        field_map_exn json__ "ConnectionNotificationIds"
          ConnectionNotificationIdsList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~connectionNotificationIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified VPC endpoint connection notifications."]
module DeleteVpcEncryptionControlResult =
  struct
    type nonrec t =
      {
      vpcEncryptionControl: VpcEncryptionControl.t option
        [@ocaml.doc
          "Information about the deleted VPC Encryption Control configuration."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcEncryptionControl = fun () -> { vpcEncryptionControl }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcEncryptionControl",
           (Option.map x.vpcEncryptionControl
              ~f:VpcEncryptionControl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEncryptionControl =
        (Option.map ~f:VpcEncryptionControl.of_xml)
          (Xml.child xml_arg0 "vpcEncryptionControl") in
      make ?vpcEncryptionControl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcEncryptionControl =
        field_map json__ "VpcEncryptionControl" VpcEncryptionControl.of_json in
      make ?vpcEncryptionControl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a VPC Encryption Control configuration. This removes the encryption policy enforcement from the specified VPC. For more information, see Enforce VPC encryption in transit in the Amazon VPC User Guide."]
module DeleteVpcEncryptionControlRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcEncryptionControlId: VpcEncryptionControlId.t
        [@ocaml.doc
          "The ID of the VPC Encryption Control resource to delete."]}
    let context_ = "DeleteVpcEncryptionControlRequest"
    let make ?dryRun =
      fun ~vpcEncryptionControlId ->
        fun () -> { dryRun; vpcEncryptionControlId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEncryptionControlId",
          (Some (VpcEncryptionControlId.to_value x.vpcEncryptionControlId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEncryptionControlId =
        VpcEncryptionControlId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEncryptionControlId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcEncryptionControlId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcEncryptionControlId =
        field_map_exn json__ "VpcEncryptionControlId"
          VpcEncryptionControlId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~vpcEncryptionControlId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a VPC Encryption Control configuration. This removes the encryption policy enforcement from the specified VPC. For more information, see Enforce VPC encryption in transit in the Amazon VPC User Guide."]
module DeleteVpcBlockPublicAccessExclusionResult =
  struct
    type nonrec t =
      {
      vpcBlockPublicAccessExclusion: VpcBlockPublicAccessExclusion.t option
        [@ocaml.doc "Details about an exclusion."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcBlockPublicAccessExclusion =
      fun () -> { vpcBlockPublicAccessExclusion }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcBlockPublicAccessExclusion",
           (Option.map x.vpcBlockPublicAccessExclusion
              ~f:VpcBlockPublicAccessExclusion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcBlockPublicAccessExclusion =
        (Option.map ~f:VpcBlockPublicAccessExclusion.of_xml)
          (Xml.child xml_arg0 "vpcBlockPublicAccessExclusion") in
      make ?vpcBlockPublicAccessExclusion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcBlockPublicAccessExclusion =
        field_map json__ "VpcBlockPublicAccessExclusion"
          VpcBlockPublicAccessExclusion.of_json in
      make ?vpcBlockPublicAccessExclusion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be applied to a single VPC or subnet that exempts it from the account\226\128\153s BPA mode and will allow bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon VPC User Guide."]
module DeleteVpcBlockPublicAccessExclusionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      exclusionId: VpcBlockPublicAccessExclusionId.t
        [@ocaml.doc "The ID of the exclusion."]}
    let context_ = "DeleteVpcBlockPublicAccessExclusionRequest"
    let make ?dryRun = fun ~exclusionId -> fun () -> { dryRun; exclusionId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ExclusionId",
          (Some (VpcBlockPublicAccessExclusionId.to_value x.exclusionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exclusionId =
        VpcBlockPublicAccessExclusionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExclusionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~exclusionId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exclusionId =
        field_map_exn json__ "ExclusionId"
          VpcBlockPublicAccessExclusionId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~exclusionId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be applied to a single VPC or subnet that exempts it from the account\226\128\153s BPA mode and will allow bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon VPC User Guide."]
module DeleteVolumeRequest =
  struct
    type nonrec t =
      {
      volumeId: VolumeId.t [@ocaml.doc "The ID of the volume."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVolumeRequest"
    let make ?dryRun = fun ~volumeId -> fun () -> { dryRun; volumeId }
    let to_value x =
      structure_to_value
        [("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      make ?dryRun ~volumeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let volumeId = field_map_exn json__ "VolumeId" VolumeId.of_json in
      make ?dryRun ~volumeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified EBS volume. The volume must be in the available state (not attached to an instance). The volume can remain in the deleting state for several minutes. For more information, see Delete an Amazon EBS volume in the Amazon EBS User Guide."]
module DeleteVerifiedAccessTrustProviderResult =
  struct
    type nonrec t =
      {
      verifiedAccessTrustProvider: VerifiedAccessTrustProvider.t option
        [@ocaml.doc "Details about the Verified Access trust provider."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessTrustProvider =
      fun () -> { verifiedAccessTrustProvider }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessTrustProvider",
           (Option.map x.verifiedAccessTrustProvider
              ~f:VerifiedAccessTrustProvider.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessTrustProvider =
        (Option.map ~f:VerifiedAccessTrustProvider.of_xml)
          (Xml.child xml_arg0 "verifiedAccessTrustProvider") in
      make ?verifiedAccessTrustProvider ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessTrustProvider =
        field_map json__ "VerifiedAccessTrustProvider"
          VerifiedAccessTrustProvider.of_json in
      make ?verifiedAccessTrustProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an Amazon Web Services Verified Access trust provider."]
module DeleteVerifiedAccessTrustProviderRequest =
  struct
    type nonrec t =
      {
      verifiedAccessTrustProviderId: VerifiedAccessTrustProviderId.t
        [@ocaml.doc "The ID of the Verified Access trust provider."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."]}
    let context_ = "DeleteVerifiedAccessTrustProviderRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ~verifiedAccessTrustProviderId ->
          fun () -> { dryRun; clientToken; verifiedAccessTrustProviderId }
    let to_value x =
      structure_to_value
        [("VerifiedAccessTrustProviderId",
           (Some
              (VerifiedAccessTrustProviderId.to_value
                 x.verifiedAccessTrustProviderId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let verifiedAccessTrustProviderId =
        VerifiedAccessTrustProviderId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VerifiedAccessTrustProviderId") in
      make ?clientToken ?dryRun ~verifiedAccessTrustProviderId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let verifiedAccessTrustProviderId =
        field_map_exn json__ "VerifiedAccessTrustProviderId"
          VerifiedAccessTrustProviderId.of_json in
      make ?clientToken ?dryRun ~verifiedAccessTrustProviderId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an Amazon Web Services Verified Access trust provider."]
module DeleteVerifiedAccessInstanceResult =
  struct
    type nonrec t =
      {
      verifiedAccessInstance: VerifiedAccessInstance.t option
        [@ocaml.doc "Details about the Verified Access instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessInstance = fun () -> { verifiedAccessInstance }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessInstance",
           (Option.map x.verifiedAccessInstance
              ~f:VerifiedAccessInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessInstance =
        (Option.map ~f:VerifiedAccessInstance.of_xml)
          (Xml.child xml_arg0 "verifiedAccessInstance") in
      make ?verifiedAccessInstance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessInstance =
        field_map json__ "VerifiedAccessInstance"
          VerifiedAccessInstance.of_json in
      make ?verifiedAccessInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an Amazon Web Services Verified Access instance."]
module DeleteVerifiedAccessInstanceRequest =
  struct
    type nonrec t =
      {
      verifiedAccessInstanceId: VerifiedAccessInstanceId.t
        [@ocaml.doc "The ID of the Verified Access instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."]}
    let context_ = "DeleteVerifiedAccessInstanceRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ~verifiedAccessInstanceId ->
          fun () -> { dryRun; clientToken; verifiedAccessInstanceId }
    let to_value x =
      structure_to_value
        [("VerifiedAccessInstanceId",
           (Some
              (VerifiedAccessInstanceId.to_value x.verifiedAccessInstanceId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let verifiedAccessInstanceId =
        VerifiedAccessInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VerifiedAccessInstanceId") in
      make ?clientToken ?dryRun ~verifiedAccessInstanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let verifiedAccessInstanceId =
        field_map_exn json__ "VerifiedAccessInstanceId"
          VerifiedAccessInstanceId.of_json in
      make ?clientToken ?dryRun ~verifiedAccessInstanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an Amazon Web Services Verified Access instance."]
module DeleteVerifiedAccessGroupResult =
  struct
    type nonrec t =
      {
      verifiedAccessGroup: VerifiedAccessGroup.t option
        [@ocaml.doc "Details about the Verified Access group."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessGroup = fun () -> { verifiedAccessGroup }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessGroup",
           (Option.map x.verifiedAccessGroup ~f:VerifiedAccessGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessGroup =
        (Option.map ~f:VerifiedAccessGroup.of_xml)
          (Xml.child xml_arg0 "verifiedAccessGroup") in
      make ?verifiedAccessGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessGroup =
        field_map json__ "VerifiedAccessGroup" VerifiedAccessGroup.of_json in
      make ?verifiedAccessGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an Amazon Web Services Verified Access group."]
module DeleteVerifiedAccessGroupRequest =
  struct
    type nonrec t =
      {
      verifiedAccessGroupId: VerifiedAccessGroupId.t
        [@ocaml.doc "The ID of the Verified Access group."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVerifiedAccessGroupRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~verifiedAccessGroupId ->
          fun () -> { clientToken; dryRun; verifiedAccessGroupId }
    let to_value x =
      structure_to_value
        [("VerifiedAccessGroupId",
           (Some (VerifiedAccessGroupId.to_value x.verifiedAccessGroupId)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let verifiedAccessGroupId =
        VerifiedAccessGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VerifiedAccessGroupId") in
      make ?dryRun ?clientToken ~verifiedAccessGroupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let verifiedAccessGroupId =
        field_map_exn json__ "VerifiedAccessGroupId"
          VerifiedAccessGroupId.of_json in
      make ?dryRun ?clientToken ~verifiedAccessGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an Amazon Web Services Verified Access group."]
module DeleteVerifiedAccessEndpointResult =
  struct
    type nonrec t =
      {
      verifiedAccessEndpoint: VerifiedAccessEndpoint.t option
        [@ocaml.doc "Details about the Verified Access endpoint."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessEndpoint = fun () -> { verifiedAccessEndpoint }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessEndpoint",
           (Option.map x.verifiedAccessEndpoint
              ~f:VerifiedAccessEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessEndpoint =
        (Option.map ~f:VerifiedAccessEndpoint.of_xml)
          (Xml.child xml_arg0 "verifiedAccessEndpoint") in
      make ?verifiedAccessEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessEndpoint =
        field_map json__ "VerifiedAccessEndpoint"
          VerifiedAccessEndpoint.of_json in
      make ?verifiedAccessEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an Amazon Web Services Verified Access endpoint."]
module DeleteVerifiedAccessEndpointRequest =
  struct
    type nonrec t =
      {
      verifiedAccessEndpointId: VerifiedAccessEndpointId.t
        [@ocaml.doc "The ID of the Verified Access endpoint."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteVerifiedAccessEndpointRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~verifiedAccessEndpointId ->
          fun () -> { clientToken; dryRun; verifiedAccessEndpointId }
    let to_value x =
      structure_to_value
        [("VerifiedAccessEndpointId",
           (Some
              (VerifiedAccessEndpointId.to_value x.verifiedAccessEndpointId)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let verifiedAccessEndpointId =
        VerifiedAccessEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VerifiedAccessEndpointId") in
      make ?dryRun ?clientToken ~verifiedAccessEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let verifiedAccessEndpointId =
        field_map_exn json__ "VerifiedAccessEndpointId"
          VerifiedAccessEndpointId.of_json in
      make ?dryRun ?clientToken ~verifiedAccessEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an Amazon Web Services Verified Access endpoint."]
module DeleteTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option
        [@ocaml.doc "Information about the deleted VPC attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayVpcAttachment =
        field_map json__ "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified VPC attachment."]
module DeleteTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayVpcAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified VPC attachment."]
module DeleteTransitGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTable: TransitGatewayRouteTable.t option
        [@ocaml.doc
          "Information about the deleted transit gateway route table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTable =
      fun () -> { transitGatewayRouteTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTable",
           (Option.map x.transitGatewayRouteTable
              ~f:TransitGatewayRouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayRouteTable =
        (Option.map ~f:TransitGatewayRouteTable.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTable") in
      make ?transitGatewayRouteTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayRouteTable =
        field_map json__ "TransitGatewayRouteTable"
          TransitGatewayRouteTable.of_json in
      make ?transitGatewayRouteTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified transit gateway route table. If there are any route tables associated with the transit gateway route table, you must first run DisassociateRouteTable before you can delete the transit gateway route table. This removes any route tables associated with the transit gateway route table."]
module DeleteTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayRouteTableRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun () -> { dryRun; transitGatewayRouteTableId }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified transit gateway route table. If there are any route tables associated with the transit gateway route table, you must first run DisassociateRouteTable before you can delete the transit gateway route table. This removes any route tables associated with the transit gateway route table."]
module DeleteTransitGatewayRouteTableAnnouncementResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableAnnouncement:
        TransitGatewayRouteTableAnnouncement.t option
        [@ocaml.doc
          "Provides details about a deleted transit gateway route table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTableAnnouncement =
      fun () -> { transitGatewayRouteTableAnnouncement }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTableAnnouncement",
           (Option.map x.transitGatewayRouteTableAnnouncement
              ~f:TransitGatewayRouteTableAnnouncement.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayRouteTableAnnouncement =
        (Option.map ~f:TransitGatewayRouteTableAnnouncement.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTableAnnouncement") in
      make ?transitGatewayRouteTableAnnouncement ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayRouteTableAnnouncement =
        field_map json__ "TransitGatewayRouteTableAnnouncement"
          TransitGatewayRouteTableAnnouncement.of_json in
      make ?transitGatewayRouteTableAnnouncement ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Advertises to the transit gateway that a transit gateway route table is deleted."]
module DeleteTransitGatewayRouteTableAnnouncementRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableAnnouncementId:
        TransitGatewayRouteTableAnnouncementId.t
        [@ocaml.doc
          "The transit gateway route table ID that's being deleted."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayRouteTableAnnouncementRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableAnnouncementId ->
        fun () -> { dryRun; transitGatewayRouteTableAnnouncementId }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableAnnouncementId",
           (Some
              (TransitGatewayRouteTableAnnouncementId.to_value
                 x.transitGatewayRouteTableAnnouncementId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayRouteTableAnnouncementId =
        TransitGatewayRouteTableAnnouncementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableAnnouncementId") in
      make ?dryRun ~transitGatewayRouteTableAnnouncementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayRouteTableAnnouncementId =
        field_map_exn json__ "TransitGatewayRouteTableAnnouncementId"
          TransitGatewayRouteTableAnnouncementId.of_json in
      make ?dryRun ~transitGatewayRouteTableAnnouncementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Advertises to the transit gateway that a transit gateway route table is deleted."]
module DeleteTransitGatewayRouteResult =
  struct
    type nonrec t =
      {
      route: TransitGatewayRoute.t option
        [@ocaml.doc "Information about the route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:TransitGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:TransitGatewayRoute.of_xml)
          (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let route = field_map json__ "Route" TransitGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route from the specified transit gateway route table."]
module DeleteTransitGatewayRouteRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The CIDR range for the route. This must match the CIDR for the route exactly."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayRouteRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~destinationCidrBlock ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; destinationCidrBlock }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~destinationCidrBlock ~transitGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~destinationCidrBlock ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route from the specified transit gateway route table."]
module DeleteTransitGatewayResult =
  struct
    type nonrec t =
      {
      transitGateway: TransitGateway.t option
        [@ocaml.doc "Information about the deleted transit gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGateway = fun () -> { transitGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGateway",
           (Option.map x.transitGateway ~f:TransitGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGateway =
        (Option.map ~f:TransitGateway.of_xml)
          (Xml.child xml_arg0 "transitGateway") in
      make ?transitGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGateway =
        field_map json__ "TransitGateway" TransitGateway.of_json in
      make ?transitGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified transit gateway."]
module DeleteTransitGatewayRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc "The ID of the transit gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayRequest"
    let make ?dryRun =
      fun ~transitGatewayId -> fun () -> { dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified transit gateway."]
module DeleteTransitGatewayPrefixListReferenceResult =
  struct
    type nonrec t =
      {
      transitGatewayPrefixListReference:
        TransitGatewayPrefixListReference.t option
        [@ocaml.doc "Information about the deleted prefix list reference."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPrefixListReference =
      fun () -> { transitGatewayPrefixListReference }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPrefixListReference",
           (Option.map x.transitGatewayPrefixListReference
              ~f:TransitGatewayPrefixListReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPrefixListReference =
        (Option.map ~f:TransitGatewayPrefixListReference.of_xml)
          (Xml.child xml_arg0 "transitGatewayPrefixListReference") in
      make ?transitGatewayPrefixListReference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPrefixListReference =
        field_map json__ "TransitGatewayPrefixListReference"
          TransitGatewayPrefixListReference.of_json in
      make ?transitGatewayPrefixListReference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a reference (route) to a prefix list in a specified transit gateway route table."]
module DeleteTransitGatewayPrefixListReferenceRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the route table."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayPrefixListReferenceRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~prefixListId ->
          fun () -> { dryRun; transitGatewayRouteTableId; prefixListId }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~prefixListId ~transitGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let prefixListId =
        field_map_exn json__ "PrefixListId" PrefixListResourceId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~prefixListId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a reference (route) to a prefix list in a specified transit gateway route table."]
module DeleteTransitGatewayPolicyTableResult =
  struct
    type nonrec t =
      {
      transitGatewayPolicyTable: TransitGatewayPolicyTable.t option
        [@ocaml.doc
          "Provides details about the deleted transit gateway policy table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPolicyTable =
      fun () -> { transitGatewayPolicyTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPolicyTable",
           (Option.map x.transitGatewayPolicyTable
              ~f:TransitGatewayPolicyTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPolicyTable =
        (Option.map ~f:TransitGatewayPolicyTable.of_xml)
          (Xml.child xml_arg0 "transitGatewayPolicyTable") in
      make ?transitGatewayPolicyTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPolicyTable =
        field_map json__ "TransitGatewayPolicyTable"
          TransitGatewayPolicyTable.of_json in
      make ?transitGatewayPolicyTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified transit gateway policy table."]
module DeleteTransitGatewayPolicyTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayPolicyTableId: TransitGatewayPolicyTableId.t
        [@ocaml.doc "The transit gateway policy table to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayPolicyTableRequest"
    let make ?dryRun =
      fun ~transitGatewayPolicyTableId ->
        fun () -> { dryRun; transitGatewayPolicyTableId }
    let to_value x =
      structure_to_value
        [("TransitGatewayPolicyTableId",
           (Some
              (TransitGatewayPolicyTableId.to_value
                 x.transitGatewayPolicyTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayPolicyTableId =
        TransitGatewayPolicyTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayPolicyTableId") in
      make ?dryRun ~transitGatewayPolicyTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayPolicyTableId =
        field_map_exn json__ "TransitGatewayPolicyTableId"
          TransitGatewayPolicyTableId.of_json in
      make ?dryRun ~transitGatewayPolicyTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified transit gateway policy table."]
module DeleteTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option
        [@ocaml.doc "The transit gateway peering attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPeeringAttachment =
        field_map json__ "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a transit gateway peering attachment."]
module DeleteTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the transit gateway peering attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayPeeringAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a transit gateway peering attachment."]
module DeleteTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomain: TransitGatewayMulticastDomain.t option
        [@ocaml.doc
          "Information about the deleted transit gateway multicast domain."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMulticastDomain =
      fun () -> { transitGatewayMulticastDomain }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMulticastDomain",
           (Option.map x.transitGatewayMulticastDomain
              ~f:TransitGatewayMulticastDomain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMulticastDomain =
        (Option.map ~f:TransitGatewayMulticastDomain.of_xml)
          (Xml.child xml_arg0 "transitGatewayMulticastDomain") in
      make ?transitGatewayMulticastDomain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayMulticastDomain =
        field_map json__ "TransitGatewayMulticastDomain"
          TransitGatewayMulticastDomain.of_json in
      make ?transitGatewayMulticastDomain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified transit gateway multicast domain."]
module DeleteTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId: TransitGatewayMulticastDomainId.t
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayMulticastDomainRequest"
    let make ?dryRun =
      fun ~transitGatewayMulticastDomainId ->
        fun () -> { dryRun; transitGatewayMulticastDomainId }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Some
              (TransitGatewayMulticastDomainId.to_value
                 x.transitGatewayMulticastDomainId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayMulticastDomainId =
        TransitGatewayMulticastDomainId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayMulticastDomainId") in
      make ?dryRun ~transitGatewayMulticastDomainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayMulticastDomainId =
        field_map_exn json__ "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ~transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified transit gateway multicast domain."]
module DeleteTransitGatewayMeteringPolicyResult =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicy: TransitGatewayMeteringPolicy.t option
        [@ocaml.doc
          "Information about the deleted transit gateway metering policy."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMeteringPolicy =
      fun () -> { transitGatewayMeteringPolicy }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMeteringPolicy",
           (Option.map x.transitGatewayMeteringPolicy
              ~f:TransitGatewayMeteringPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMeteringPolicy =
        (Option.map ~f:TransitGatewayMeteringPolicy.of_xml)
          (Xml.child xml_arg0 "transitGatewayMeteringPolicy") in
      make ?transitGatewayMeteringPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayMeteringPolicy =
        field_map json__ "TransitGatewayMeteringPolicy"
          TransitGatewayMeteringPolicy.of_json in
      make ?transitGatewayMeteringPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a transit gateway metering policy."]
module DeleteTransitGatewayMeteringPolicyRequest =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicyId: TransitGatewayMeteringPolicyId.t
        [@ocaml.doc
          "The ID of the transit gateway metering policy to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayMeteringPolicyRequest"
    let make ?dryRun =
      fun ~transitGatewayMeteringPolicyId ->
        fun () -> { dryRun; transitGatewayMeteringPolicyId }
    let to_value x =
      structure_to_value
        [("TransitGatewayMeteringPolicyId",
           (Some
              (TransitGatewayMeteringPolicyId.to_value
                 x.transitGatewayMeteringPolicyId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayMeteringPolicyId =
        TransitGatewayMeteringPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayMeteringPolicyId") in
      make ?dryRun ~transitGatewayMeteringPolicyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayMeteringPolicyId =
        field_map_exn json__ "TransitGatewayMeteringPolicyId"
          TransitGatewayMeteringPolicyId.of_json in
      make ?dryRun ~transitGatewayMeteringPolicyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a transit gateway metering policy."]
module DeleteTransitGatewayMeteringPolicyEntryResult =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicyEntry:
        TransitGatewayMeteringPolicyEntry.t option
        [@ocaml.doc
          "Information about the deleted transit gateway metering policy entry."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMeteringPolicyEntry =
      fun () -> { transitGatewayMeteringPolicyEntry }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMeteringPolicyEntry",
           (Option.map x.transitGatewayMeteringPolicyEntry
              ~f:TransitGatewayMeteringPolicyEntry.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMeteringPolicyEntry =
        (Option.map ~f:TransitGatewayMeteringPolicyEntry.of_xml)
          (Xml.child xml_arg0 "transitGatewayMeteringPolicyEntry") in
      make ?transitGatewayMeteringPolicyEntry ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayMeteringPolicyEntry =
        field_map json__ "TransitGatewayMeteringPolicyEntry"
          TransitGatewayMeteringPolicyEntry.of_json in
      make ?transitGatewayMeteringPolicyEntry ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes an entry from a transit gateway metering policy."]
module DeleteTransitGatewayMeteringPolicyEntryRequest =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicyId: TransitGatewayMeteringPolicyId.t
        [@ocaml.doc
          "The ID of the transit gateway metering policy containing the entry to delete."];
      policyRuleNumber: Integer.t
        [@ocaml.doc
          "The rule number of the metering policy entry to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayMeteringPolicyEntryRequest"
    let make ?dryRun =
      fun ~transitGatewayMeteringPolicyId ->
        fun ~policyRuleNumber ->
          fun () ->
            { dryRun; transitGatewayMeteringPolicyId; policyRuleNumber }
    let to_value x =
      structure_to_value
        [("TransitGatewayMeteringPolicyId",
           (Some
              (TransitGatewayMeteringPolicyId.to_value
                 x.transitGatewayMeteringPolicyId)));
        ("PolicyRuleNumber", (Some (Integer.to_value x.policyRuleNumber)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let policyRuleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyRuleNumber") in
      let transitGatewayMeteringPolicyId =
        TransitGatewayMeteringPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayMeteringPolicyId") in
      make ?dryRun ~policyRuleNumber ~transitGatewayMeteringPolicyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let policyRuleNumber =
        field_map_exn json__ "PolicyRuleNumber" Integer.of_json in
      let transitGatewayMeteringPolicyId =
        field_map_exn json__ "TransitGatewayMeteringPolicyId"
          TransitGatewayMeteringPolicyId.of_json in
      make ?dryRun ~policyRuleNumber ~transitGatewayMeteringPolicyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes an entry from a transit gateway metering policy."]
module DeleteTransitGatewayConnectResult =
  struct
    type nonrec t =
      {
      transitGatewayConnect: TransitGatewayConnect.t option
        [@ocaml.doc "Information about the deleted Connect attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnect = fun () -> { transitGatewayConnect }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnect",
           (Option.map x.transitGatewayConnect
              ~f:TransitGatewayConnect.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnect =
        (Option.map ~f:TransitGatewayConnect.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnect") in
      make ?transitGatewayConnect ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayConnect =
        field_map json__ "TransitGatewayConnect"
          TransitGatewayConnect.of_json in
      make ?transitGatewayConnect ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Connect attachment. You must first delete any Connect peers for the attachment."]
module DeleteTransitGatewayConnectRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the Connect attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayConnectRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Connect attachment. You must first delete any Connect peers for the attachment."]
module DeleteTransitGatewayConnectPeerResult =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeer: TransitGatewayConnectPeer.t option
        [@ocaml.doc "Information about the deleted Connect peer."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnectPeer =
      fun () -> { transitGatewayConnectPeer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnectPeer",
           (Option.map x.transitGatewayConnectPeer
              ~f:TransitGatewayConnectPeer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnectPeer =
        (Option.map ~f:TransitGatewayConnectPeer.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnectPeer") in
      make ?transitGatewayConnectPeer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayConnectPeer =
        field_map json__ "TransitGatewayConnectPeer"
          TransitGatewayConnectPeer.of_json in
      make ?transitGatewayConnectPeer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Connect peer."]
module DeleteTransitGatewayConnectPeerRequest =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeerId: TransitGatewayConnectPeerId.t
        [@ocaml.doc "The ID of the Connect peer."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayConnectPeerRequest"
    let make ?dryRun =
      fun ~transitGatewayConnectPeerId ->
        fun () -> { dryRun; transitGatewayConnectPeerId }
    let to_value x =
      structure_to_value
        [("TransitGatewayConnectPeerId",
           (Some
              (TransitGatewayConnectPeerId.to_value
                 x.transitGatewayConnectPeerId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayConnectPeerId =
        TransitGatewayConnectPeerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayConnectPeerId") in
      make ?dryRun ~transitGatewayConnectPeerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayConnectPeerId =
        field_map_exn json__ "TransitGatewayConnectPeerId"
          TransitGatewayConnectPeerId.of_json in
      make ?dryRun ~transitGatewayConnectPeerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Connect peer."]
module DeleteTransitGatewayClientVpnAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayClientVpnAttachment:
        TransitGatewayClientVpnAttachment.t option
        [@ocaml.doc
          "Information about the Transit Gateway Client VPN attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayClientVpnAttachment =
      fun () -> { transitGatewayClientVpnAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayClientVpnAttachment",
           (Option.map x.transitGatewayClientVpnAttachment
              ~f:TransitGatewayClientVpnAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayClientVpnAttachment =
        (Option.map ~f:TransitGatewayClientVpnAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayClientVpnAttachment") in
      make ?transitGatewayClientVpnAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayClientVpnAttachment =
        field_map json__ "TransitGatewayClientVpnAttachment"
          TransitGatewayClientVpnAttachment.of_json in
      make ?transitGatewayClientVpnAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a Transit Gateway attachment for a Client VPN endpoint. The Transit Gateway owner can delete the attachment to remove the association between the Client VPN endpoint and the Transit Gateway."]
module DeleteTransitGatewayClientVpnAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the Transit Gateway attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTransitGatewayClientVpnAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a Transit Gateway attachment for a Client VPN endpoint. The Transit Gateway owner can delete the attachment to remove the association between the Client VPN endpoint and the Transit Gateway."]
module DeleteTrafficMirrorTargetResult =
  struct
    type nonrec t =
      {
      trafficMirrorTargetId: String_.t option
        [@ocaml.doc "The ID of the deleted Traffic Mirror target."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorTargetId = fun () -> { trafficMirrorTargetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorTargetId",
           (Option.map x.trafficMirrorTargetId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorTargetId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorTargetId") in
      make ?trafficMirrorTargetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trafficMirrorTargetId =
        field_map json__ "TrafficMirrorTargetId" String_.of_json in
      make ?trafficMirrorTargetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Traffic Mirror target. You cannot delete a Traffic Mirror target that is in use by a Traffic Mirror session."]
module DeleteTrafficMirrorTargetRequest =
  struct
    type nonrec t =
      {
      trafficMirrorTargetId: TrafficMirrorTargetId.t
        [@ocaml.doc "The ID of the Traffic Mirror target."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTrafficMirrorTargetRequest"
    let make ?dryRun =
      fun ~trafficMirrorTargetId ->
        fun () -> { dryRun; trafficMirrorTargetId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorTargetId",
           (Some (TrafficMirrorTargetId.to_value x.trafficMirrorTargetId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorTargetId =
        TrafficMirrorTargetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorTargetId") in
      make ?dryRun ~trafficMirrorTargetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let trafficMirrorTargetId =
        field_map_exn json__ "TrafficMirrorTargetId"
          TrafficMirrorTargetId.of_json in
      make ?dryRun ~trafficMirrorTargetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Traffic Mirror target. You cannot delete a Traffic Mirror target that is in use by a Traffic Mirror session."]
module DeleteTrafficMirrorSessionResult =
  struct
    type nonrec t =
      {
      trafficMirrorSessionId: String_.t option
        [@ocaml.doc "The ID of the deleted Traffic Mirror session."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorSessionId = fun () -> { trafficMirrorSessionId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorSessionId",
           (Option.map x.trafficMirrorSessionId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorSessionId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorSessionId") in
      make ?trafficMirrorSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trafficMirrorSessionId =
        field_map json__ "TrafficMirrorSessionId" String_.of_json in
      make ?trafficMirrorSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Traffic Mirror session."]
module DeleteTrafficMirrorSessionRequest =
  struct
    type nonrec t =
      {
      trafficMirrorSessionId: TrafficMirrorSessionId.t
        [@ocaml.doc "The ID of the Traffic Mirror session."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTrafficMirrorSessionRequest"
    let make ?dryRun =
      fun ~trafficMirrorSessionId ->
        fun () -> { dryRun; trafficMirrorSessionId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorSessionId",
           (Some (TrafficMirrorSessionId.to_value x.trafficMirrorSessionId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorSessionId =
        TrafficMirrorSessionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorSessionId") in
      make ?dryRun ~trafficMirrorSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let trafficMirrorSessionId =
        field_map_exn json__ "TrafficMirrorSessionId"
          TrafficMirrorSessionId.of_json in
      make ?dryRun ~trafficMirrorSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Traffic Mirror session."]
module DeleteTrafficMirrorFilterRuleResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRuleId: String_.t option
        [@ocaml.doc "The ID of the deleted Traffic Mirror rule."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterRuleId =
      fun () -> { trafficMirrorFilterRuleId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterRuleId",
           (Option.map x.trafficMirrorFilterRuleId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorFilterRuleId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterRuleId") in
      make ?trafficMirrorFilterRuleId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trafficMirrorFilterRuleId =
        field_map json__ "TrafficMirrorFilterRuleId" String_.of_json in
      make ?trafficMirrorFilterRuleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Traffic Mirror rule."]
module DeleteTrafficMirrorFilterRuleRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRuleId: TrafficMirrorFilterRuleIdWithResolver.t
        [@ocaml.doc "The ID of the Traffic Mirror rule."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTrafficMirrorFilterRuleRequest"
    let make ?dryRun =
      fun ~trafficMirrorFilterRuleId ->
        fun () -> { dryRun; trafficMirrorFilterRuleId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterRuleId",
           (Some
              (TrafficMirrorFilterRuleIdWithResolver.to_value
                 x.trafficMirrorFilterRuleId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorFilterRuleId =
        TrafficMirrorFilterRuleIdWithResolver.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TrafficMirrorFilterRuleId") in
      make ?dryRun ~trafficMirrorFilterRuleId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let trafficMirrorFilterRuleId =
        field_map_exn json__ "TrafficMirrorFilterRuleId"
          TrafficMirrorFilterRuleIdWithResolver.of_json in
      make ?dryRun ~trafficMirrorFilterRuleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Traffic Mirror rule."]
module DeleteTrafficMirrorFilterResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilterId: String_.t option
        [@ocaml.doc "The ID of the Traffic Mirror filter."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterId = fun () -> { trafficMirrorFilterId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterId",
           (Option.map x.trafficMirrorFilterId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorFilterId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterId") in
      make ?trafficMirrorFilterId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trafficMirrorFilterId =
        field_map json__ "TrafficMirrorFilterId" String_.of_json in
      make ?trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Traffic Mirror filter. You cannot delete a Traffic Mirror filter that is in use by a Traffic Mirror session."]
module DeleteTrafficMirrorFilterRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterId: TrafficMirrorFilterId.t
        [@ocaml.doc "The ID of the Traffic Mirror filter."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteTrafficMirrorFilterRequest"
    let make ?dryRun =
      fun ~trafficMirrorFilterId ->
        fun () -> { dryRun; trafficMirrorFilterId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterId",
           (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      make ?dryRun ~trafficMirrorFilterId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let trafficMirrorFilterId =
        field_map_exn json__ "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      make ?dryRun ~trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Traffic Mirror filter. You cannot delete a Traffic Mirror filter that is in use by a Traffic Mirror session."]
module DeleteTagsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      resources: ResourceIdList.t
        [@ocaml.doc
          "The IDs of the resources, separated by spaces. Constraints: Up to 1000 resource IDs. We recommend breaking up this request into smaller batches."];
      tags: TagList.t option
        [@ocaml.doc
          "The tags to delete. Specify a tag key and an optional tag value to delete specific tags. If you specify a tag key without a tag value, we delete any tag with this key regardless of its value. If you specify a tag key with an empty string as the tag value, we delete the tag only if its value is an empty string. If you omit this parameter, we delete all user-defined tags for the specified resources. We do not delete Amazon Web Services-generated tags (tags that have the aws: prefix). Constraints: Up to 1000 tags."]}
    let context_ = "DeleteTagsRequest"
    let make ?dryRun =
      fun ?tags -> fun ~resources -> fun () -> { dryRun; tags; resources }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("resourceId", (Some (ResourceIdList.to_value x.resources)));
        ("tag", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tag") in
      let resources =
        ResourceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tags ~resources ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let resources = field_map_exn json__ "Resources" ResourceIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tags ~resources ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified set of tags from the specified set of resources. To list the current tags, use DescribeTags. For more information about tags, see Tag your Amazon EC2 resources in the Amazon Elastic Compute Cloud User Guide."]
module DeleteSubnetRequest =
  struct
    type nonrec t =
      {
      subnetId: SubnetId.t [@ocaml.doc "The ID of the subnet."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteSubnetRequest"
    let make ?dryRun = fun ~subnetId -> fun () -> { dryRun; subnetId }
    let to_value x =
      structure_to_value
        [("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      make ?dryRun ~subnetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let subnetId = field_map_exn json__ "SubnetId" SubnetId.of_json in
      make ?dryRun ~subnetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified subnet. You must terminate all running instances in the subnet before you can delete the subnet."]
module DeleteSubnetCidrReservationResult =
  struct
    type nonrec t =
      {
      deletedSubnetCidrReservation: SubnetCidrReservation.t option
        [@ocaml.doc "Information about the deleted subnet CIDR reservation."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?deletedSubnetCidrReservation =
      fun () -> { deletedSubnetCidrReservation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deletedSubnetCidrReservation",
           (Option.map x.deletedSubnetCidrReservation
              ~f:SubnetCidrReservation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deletedSubnetCidrReservation =
        (Option.map ~f:SubnetCidrReservation.of_xml)
          (Xml.child xml_arg0 "deletedSubnetCidrReservation") in
      make ?deletedSubnetCidrReservation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deletedSubnetCidrReservation =
        field_map json__ "DeletedSubnetCidrReservation"
          SubnetCidrReservation.of_json in
      make ?deletedSubnetCidrReservation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a subnet CIDR reservation."]
module DeleteSubnetCidrReservationRequest =
  struct
    type nonrec t =
      {
      subnetCidrReservationId: SubnetCidrReservationId.t
        [@ocaml.doc "The ID of the subnet CIDR reservation."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteSubnetCidrReservationRequest"
    let make ?dryRun =
      fun ~subnetCidrReservationId ->
        fun () -> { dryRun; subnetCidrReservationId }
    let to_value x =
      structure_to_value
        [("SubnetCidrReservationId",
           (Some (SubnetCidrReservationId.to_value x.subnetCidrReservationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetCidrReservationId =
        SubnetCidrReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubnetCidrReservationId") in
      make ?dryRun ~subnetCidrReservationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let subnetCidrReservationId =
        field_map_exn json__ "SubnetCidrReservationId"
          SubnetCidrReservationId.of_json in
      make ?dryRun ~subnetCidrReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a subnet CIDR reservation."]
module DeleteSpotDatafeedSubscriptionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DeleteSpotDatafeedSubscription."]
module DeleteSnapshotRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t [@ocaml.doc "The ID of the EBS snapshot."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteSnapshotRequest"
    let make ?dryRun = fun ~snapshotId -> fun () -> { dryRun; snapshotId }
    let to_value x =
      structure_to_value
        [("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      make ?dryRun ~snapshotId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let snapshotId = field_map_exn json__ "SnapshotId" SnapshotId.of_json in
      make ?dryRun ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified snapshot. When you make periodic snapshots of a volume, the snapshots are incremental, and only the blocks on the device that have changed since your last snapshot are saved in the new snapshot. When you delete a snapshot, only the data not needed for any other snapshot is removed. So regardless of which prior snapshots have been deleted, all active snapshots will have access to all the information needed to restore the volume. You cannot delete a snapshot of the root device of an EBS volume used by a registered AMI. You must first deregister the AMI before you can delete the snapshot. For more information, see Delete an Amazon EBS snapshot in the Amazon EBS User Guide."]
module DeleteSecurityGroupResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."];
      groupId: SecurityGroupId.t option
        [@ocaml.doc "The ID of the deleted security group."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun ?groupId -> fun () -> { return; groupId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("groupId", (Option.map x.groupId ~f:SecurityGroupId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "groupId") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?groupId ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map json__ "GroupId" SecurityGroupId.of_json in
      let return = field_map json__ "Return" Boolean.of_json in
      make ?groupId ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a security group. If you attempt to delete a security group that is associated with an instance or network interface, is referenced by another security group in the same VPC, or has a VPC association, the operation fails with DependencyViolation."]
module DeleteSecurityGroupRequest =
  struct
    type nonrec t =
      {
      groupId: SecurityGroupId.t option
        [@ocaml.doc "The ID of the security group."];
      groupName: SecurityGroupName.t option
        [@ocaml.doc
          "\\[Default VPC\\] The name of the security group. You can specify either the security group name or the security group ID. For security groups in a nondefault VPC, you must specify the security group ID."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?groupId =
      fun ?groupName ->
        fun ?dryRun -> fun () -> { groupId; groupName; dryRun }
    let to_value x =
      structure_to_value
        [("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      make ?dryRun ?groupName ?groupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let groupName = field_map json__ "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json__ "GroupId" SecurityGroupId.of_json in
      make ?dryRun ?groupName ?groupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a security group. If you attempt to delete a security group that is associated with an instance or network interface, is referenced by another security group in the same VPC, or has a VPC association, the operation fails with DependencyViolation."]
module DeleteSecondarySubnetResult =
  struct
    type nonrec t =
      {
      secondarySubnet: SecondarySubnet.t option
        [@ocaml.doc "Information about the secondary subnet being deleted."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondarySubnet =
      fun ?clientToken -> fun () -> { secondarySubnet; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondarySubnet",
           (Option.map x.secondarySubnet ~f:SecondarySubnet.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let secondarySubnet =
        (Option.map ~f:SecondarySubnet.of_xml)
          (Xml.child xml_arg0 "secondarySubnet") in
      make ?clientToken ?secondarySubnet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let secondarySubnet =
        field_map json__ "SecondarySubnet" SecondarySubnet.of_json in
      make ?clientToken ?secondarySubnet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a secondary subnet. A secondary subnet must not contain any secondary interfaces prior to deletion."]
module DeleteSecondarySubnetRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      secondarySubnetId: SecondarySubnetId.t
        [@ocaml.doc "The ID of the secondary subnet to delete."]}
    let context_ = "DeleteSecondarySubnetRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~secondarySubnetId ->
          fun () -> { clientToken; dryRun; secondarySubnetId }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecondarySubnetId",
          (Some (SecondarySubnetId.to_value x.secondarySubnetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondarySubnetId =
        SecondarySubnetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecondarySubnetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~secondarySubnetId ?dryRun ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondarySubnetId =
        field_map_exn json__ "SecondarySubnetId" SecondarySubnetId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ~secondarySubnetId ?dryRun ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a secondary subnet. A secondary subnet must not contain any secondary interfaces prior to deletion."]
module DeleteSecondaryNetworkResult =
  struct
    type nonrec t =
      {
      secondaryNetwork: SecondaryNetwork.t option
        [@ocaml.doc "Information about the secondary network."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondaryNetwork =
      fun ?clientToken -> fun () -> { secondaryNetwork; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondaryNetwork",
           (Option.map x.secondaryNetwork ~f:SecondaryNetwork.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let secondaryNetwork =
        (Option.map ~f:SecondaryNetwork.of_xml)
          (Xml.child xml_arg0 "secondaryNetwork") in
      make ?clientToken ?secondaryNetwork ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let secondaryNetwork =
        field_map json__ "SecondaryNetwork" SecondaryNetwork.of_json in
      make ?clientToken ?secondaryNetwork ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a secondary network. You must delete all secondary subnets in the secondary network before you can delete the secondary network."]
module DeleteSecondaryNetworkRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      secondaryNetworkId: SecondaryNetworkId.t
        [@ocaml.doc "The ID of the secondary network."]}
    let context_ = "DeleteSecondaryNetworkRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~secondaryNetworkId ->
          fun () -> { clientToken; dryRun; secondaryNetworkId }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecondaryNetworkId",
          (Some (SecondaryNetworkId.to_value x.secondaryNetworkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondaryNetworkId =
        SecondaryNetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecondaryNetworkId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~secondaryNetworkId ?dryRun ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondaryNetworkId =
        field_map_exn json__ "SecondaryNetworkId" SecondaryNetworkId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ~secondaryNetworkId ?dryRun ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a secondary network. You must delete all secondary subnets in the secondary network before you can delete the secondary network."]
module DeleteRouteTableRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      routeTableId: RouteTableId.t [@ocaml.doc "The ID of the route table."]}
    let context_ = "DeleteRouteTableRequest"
    let make ?dryRun =
      fun ~routeTableId -> fun () -> { dryRun; routeTableId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~routeTableId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeTableId =
        field_map_exn json__ "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~routeTableId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route table. You must disassociate the route table from any subnets before you can delete it. You can't delete the main route table."]
module DeleteRouteServerResult =
  struct
    type nonrec t =
      {
      routeServer: RouteServer.t option
        [@ocaml.doc "Information about the deleted route server."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServer = fun () -> { routeServer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServer", (Option.map x.routeServer ~f:RouteServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServer =
        (Option.map ~f:RouteServer.of_xml) (Xml.child xml_arg0 "routeServer") in
      make ?routeServer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServer = field_map json__ "RouteServer" RouteServer.of_json in
      make ?routeServer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route server. Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability with third-party workloads. Route server supports the follow route table types: VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route server does not support route tables associated with virtual private gateways. To propagate routes into a transit gateway route table, use Transit Gateway Connect. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DeleteRouteServerRequest =
  struct
    type nonrec t =
      {
      routeServerId: RouteServerId.t
        [@ocaml.doc "The ID of the route server to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteRouteServerRequest"
    let make ?dryRun =
      fun ~routeServerId -> fun () -> { dryRun; routeServerId }
    let to_value x =
      structure_to_value
        [("RouteServerId", (Some (RouteServerId.to_value x.routeServerId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let routeServerId =
        RouteServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteServerId") in
      make ?dryRun ~routeServerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let routeServerId =
        field_map_exn json__ "RouteServerId" RouteServerId.of_json in
      make ?dryRun ~routeServerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route server. Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability with third-party workloads. Route server supports the follow route table types: VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route server does not support route tables associated with virtual private gateways. To propagate routes into a transit gateway route table, use Transit Gateway Connect. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module DeleteRouteServerPeerResult =
  struct
    type nonrec t =
      {
      routeServerPeer: RouteServerPeer.t option
        [@ocaml.doc "Information about the deleted route server peer."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerPeer = fun () -> { routeServerPeer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerPeer",
           (Option.map x.routeServerPeer ~f:RouteServerPeer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServerPeer =
        (Option.map ~f:RouteServerPeer.of_xml)
          (Xml.child xml_arg0 "routeServerPeer") in
      make ?routeServerPeer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServerPeer =
        field_map json__ "RouteServerPeer" RouteServerPeer.of_json in
      make ?routeServerPeer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified BGP peer from a route server. A route server peer is a session between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other network security function running on an EC2 instance). The device must meet these requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP sessions"]
module DeleteRouteServerPeerRequest =
  struct
    type nonrec t =
      {
      routeServerPeerId: RouteServerPeerId.t
        [@ocaml.doc "The ID of the route server peer to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteRouteServerPeerRequest"
    let make ?dryRun =
      fun ~routeServerPeerId -> fun () -> { dryRun; routeServerPeerId }
    let to_value x =
      structure_to_value
        [("RouteServerPeerId",
           (Some (RouteServerPeerId.to_value x.routeServerPeerId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let routeServerPeerId =
        RouteServerPeerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteServerPeerId") in
      make ?dryRun ~routeServerPeerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let routeServerPeerId =
        field_map_exn json__ "RouteServerPeerId" RouteServerPeerId.of_json in
      make ?dryRun ~routeServerPeerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified BGP peer from a route server. A route server peer is a session between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other network security function running on an EC2 instance). The device must meet these requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP sessions"]
module DeleteRouteServerEndpointResult =
  struct
    type nonrec t =
      {
      routeServerEndpoint: RouteServerEndpoint.t option
        [@ocaml.doc "Information about the deleted route server endpoint."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerEndpoint = fun () -> { routeServerEndpoint }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerEndpoint",
           (Option.map x.routeServerEndpoint ~f:RouteServerEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServerEndpoint =
        (Option.map ~f:RouteServerEndpoint.of_xml)
          (Xml.child xml_arg0 "routeServerEndpoint") in
      make ?routeServerEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServerEndpoint =
        field_map json__ "RouteServerEndpoint" RouteServerEndpoint.of_json in
      make ?routeServerEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route server endpoint. A route server endpoint is an Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol) connections between your route server and your BGP peers."]
module DeleteRouteServerEndpointRequest =
  struct
    type nonrec t =
      {
      routeServerEndpointId: RouteServerEndpointId.t
        [@ocaml.doc "The ID of the route server endpoint to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteRouteServerEndpointRequest"
    let make ?dryRun =
      fun ~routeServerEndpointId ->
        fun () -> { dryRun; routeServerEndpointId }
    let to_value x =
      structure_to_value
        [("RouteServerEndpointId",
           (Some (RouteServerEndpointId.to_value x.routeServerEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let routeServerEndpointId =
        RouteServerEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteServerEndpointId") in
      make ?dryRun ~routeServerEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let routeServerEndpointId =
        field_map_exn json__ "RouteServerEndpointId"
          RouteServerEndpointId.of_json in
      make ?dryRun ~routeServerEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route server endpoint. A route server endpoint is an Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol) connections between your route server and your BGP peers."]
module DeleteRouteRequest =
  struct
    type nonrec t =
      {
      destinationPrefixListId: PrefixListResourceId.t option
        [@ocaml.doc "The ID of the prefix list for the route."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      routeTableId: RouteTableId.t [@ocaml.doc "The ID of the route table."];
      destinationCidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 CIDR range for the route. The value you specify must match the CIDR for the route exactly."];
      destinationIpv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 CIDR range for the route. The value you specify must match the CIDR for the route exactly."]}
    let context_ = "DeleteRouteRequest"
    let make ?destinationPrefixListId =
      fun ?dryRun ->
        fun ?destinationCidrBlock ->
          fun ?destinationIpv6CidrBlock ->
            fun ~routeTableId ->
              fun () ->
                {
                  destinationPrefixListId;
                  dryRun;
                  destinationCidrBlock;
                  destinationIpv6CidrBlock;
                  routeTableId
                }
    let to_value x =
      structure_to_value
        [("DestinationPrefixListId",
           (Option.map x.destinationPrefixListId
              ~f:PrefixListResourceId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("destinationCidrBlock",
          (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("destinationIpv6CidrBlock",
          (Option.map x.destinationIpv6CidrBlock ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationIpv6CidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationIpv6CidrBlock") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationCidrBlock") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      make ?destinationIpv6CidrBlock ?destinationCidrBlock ~routeTableId
        ?dryRun ?destinationPrefixListId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationIpv6CidrBlock =
        field_map json__ "DestinationIpv6CidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map json__ "DestinationCidrBlock" String_.of_json in
      let routeTableId =
        field_map_exn json__ "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let destinationPrefixListId =
        field_map json__ "DestinationPrefixListId"
          PrefixListResourceId.of_json in
      make ?destinationIpv6CidrBlock ?destinationCidrBlock ~routeTableId
        ?dryRun ?destinationPrefixListId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route from the specified route table."]
module DeleteQueuedReservedInstancesResult =
  struct
    type nonrec t =
      {
      successfulQueuedPurchaseDeletions:
        SuccessfulQueuedPurchaseDeletionSet.t option
        [@ocaml.doc
          "Information about the queued purchases that were successfully deleted."];
      failedQueuedPurchaseDeletions: FailedQueuedPurchaseDeletionSet.t option
        [@ocaml.doc
          "Information about the queued purchases that could not be deleted."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulQueuedPurchaseDeletions =
      fun ?failedQueuedPurchaseDeletions ->
        fun () ->
          { successfulQueuedPurchaseDeletions; failedQueuedPurchaseDeletions
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulQueuedPurchaseDeletionSet",
           (Option.map x.successfulQueuedPurchaseDeletions
              ~f:SuccessfulQueuedPurchaseDeletionSet.to_value));
        ("failedQueuedPurchaseDeletionSet",
          (Option.map x.failedQueuedPurchaseDeletions
             ~f:FailedQueuedPurchaseDeletionSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedQueuedPurchaseDeletions =
        (Option.map ~f:FailedQueuedPurchaseDeletionSet.of_xml)
          (Xml.child xml_arg0 "failedQueuedPurchaseDeletionSet") in
      let successfulQueuedPurchaseDeletions =
        (Option.map ~f:SuccessfulQueuedPurchaseDeletionSet.of_xml)
          (Xml.child xml_arg0 "successfulQueuedPurchaseDeletionSet") in
      make ?failedQueuedPurchaseDeletions ?successfulQueuedPurchaseDeletions
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failedQueuedPurchaseDeletions =
        field_map json__ "FailedQueuedPurchaseDeletions"
          FailedQueuedPurchaseDeletionSet.of_json in
      let successfulQueuedPurchaseDeletions =
        field_map json__ "SuccessfulQueuedPurchaseDeletions"
          SuccessfulQueuedPurchaseDeletionSet.of_json in
      make ?failedQueuedPurchaseDeletions ?successfulQueuedPurchaseDeletions
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the queued purchases for the specified Reserved Instances."]
module DeleteQueuedReservedInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      reservedInstancesIds: DeleteQueuedReservedInstancesIdList.t
        [@ocaml.doc "The IDs of the Reserved Instances."]}
    let context_ = "DeleteQueuedReservedInstancesRequest"
    let make ?dryRun =
      fun ~reservedInstancesIds -> fun () -> { dryRun; reservedInstancesIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ReservedInstancesId",
          (Some
             (DeleteQueuedReservedInstancesIdList.to_value
                x.reservedInstancesIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesIds =
        DeleteQueuedReservedInstancesIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservedInstancesId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~reservedInstancesIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesIds =
        field_map_exn json__ "ReservedInstancesIds"
          DeleteQueuedReservedInstancesIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~reservedInstancesIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the queued purchases for the specified Reserved Instances."]
module DeletePublicIpv4PoolResult =
  struct
    type nonrec t =
      {
      returnValue: Boolean.t option
        [@ocaml.doc
          "Information about the result of deleting the public IPv4 pool."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("returnValue", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "returnValue") in
      make ?returnValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnValue = field_map json__ "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a public IPv4 pool. A public IPv4 pool is an EC2 IP address pool required for the public IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses you bring to Amazon Web Services, however, use IPAM pools only."]
module DeletePublicIpv4PoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      poolId: Ipv4PoolEc2Id.t
        [@ocaml.doc "The ID of the public IPv4 pool you want to delete."];
      networkBorderGroup: String_.t option
        [@ocaml.doc
          "The Availability Zone (AZ) or Local Zone (LZ) network border group that the resource that the IP address is assigned to is in. Defaults to an AZ network border group. For more information on available Local Zones, see Local Zone availability in the Amazon EC2 User Guide."]}
    let context_ = "DeletePublicIpv4PoolRequest"
    let make ?dryRun =
      fun ?networkBorderGroup ->
        fun ~poolId -> fun () -> { dryRun; networkBorderGroup; poolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PoolId", (Some (Ipv4PoolEc2Id.to_value x.poolId)));
        ("NetworkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkBorderGroup") in
      let poolId =
        Ipv4PoolEc2Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?networkBorderGroup ~poolId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkBorderGroup =
        field_map json__ "NetworkBorderGroup" String_.of_json in
      let poolId = field_map_exn json__ "PoolId" Ipv4PoolEc2Id.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?networkBorderGroup ~poolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a public IPv4 pool. A public IPv4 pool is an EC2 IP address pool required for the public IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses you bring to Amazon Web Services, however, use IPAM pools only."]
module DeletePlacementGroupRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupName: PlacementGroupNameWithResolver.t
        [@ocaml.doc "The name of the placement group."]}
    let context_ = "DeletePlacementGroupRequest"
    let make ?dryRun = fun ~groupName -> fun () -> { dryRun; groupName }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupName",
          (Some (PlacementGroupNameWithResolver.to_value x.groupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupName =
        PlacementGroupNameWithResolver.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~groupName ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupName =
        field_map_exn json__ "GroupName"
          PlacementGroupNameWithResolver.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~groupName ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified placement group. You must terminate all instances in the placement group before you can delete the placement group. For more information, see Placement groups in the Amazon EC2 User Guide."]
module DeleteNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."]}
    let context_ = "DeleteNetworkInterfaceRequest"
    let make ?dryRun =
      fun ~networkInterfaceId -> fun () -> { dryRun; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~networkInterfaceId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~networkInterfaceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteNetworkInterface."]
module DeleteNetworkInterfacePermissionResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds and an error otherwise."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output for DeleteNetworkInterfacePermission."]
module DeleteNetworkInterfacePermissionRequest =
  struct
    type nonrec t =
      {
      networkInterfacePermissionId: NetworkInterfacePermissionId.t
        [@ocaml.doc "The ID of the network interface permission."];
      force: Boolean.t option
        [@ocaml.doc
          "Specify true to remove the permission even if the network interface is attached to an instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteNetworkInterfacePermissionRequest"
    let make ?force =
      fun ?dryRun ->
        fun ~networkInterfacePermissionId ->
          fun () -> { force; dryRun; networkInterfacePermissionId }
    let to_value x =
      structure_to_value
        [("NetworkInterfacePermissionId",
           (Some
              (NetworkInterfacePermissionId.to_value
                 x.networkInterfacePermissionId)));
        ("Force", (Option.map x.force ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let force = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Force") in
      let networkInterfacePermissionId =
        NetworkInterfacePermissionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInterfacePermissionId") in
      make ?dryRun ?force ~networkInterfacePermissionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let force = field_map json__ "Force" Boolean.of_json in
      let networkInterfacePermissionId =
        field_map_exn json__ "NetworkInterfacePermissionId"
          NetworkInterfacePermissionId.of_json in
      make ?dryRun ?force ~networkInterfacePermissionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DeleteNetworkInterfacePermission."]
module DeleteNetworkInsightsPathResult =
  struct
    type nonrec t =
      {
      networkInsightsPathId: NetworkInsightsPathId.t option
        [@ocaml.doc "The ID of the path."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsPathId = fun () -> { networkInsightsPathId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsPathId",
           (Option.map x.networkInsightsPathId
              ~f:NetworkInsightsPathId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsPathId =
        (Option.map ~f:NetworkInsightsPathId.of_xml)
          (Xml.child xml_arg0 "networkInsightsPathId") in
      make ?networkInsightsPathId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsPathId =
        field_map json__ "NetworkInsightsPathId"
          NetworkInsightsPathId.of_json in
      make ?networkInsightsPathId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified path."]
module DeleteNetworkInsightsPathRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInsightsPathId: NetworkInsightsPathId.t
        [@ocaml.doc "The ID of the path."]}
    let context_ = "DeleteNetworkInsightsPathRequest"
    let make ?dryRun =
      fun ~networkInsightsPathId ->
        fun () -> { dryRun; networkInsightsPathId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInsightsPathId",
          (Some (NetworkInsightsPathId.to_value x.networkInsightsPathId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsPathId =
        NetworkInsightsPathId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInsightsPathId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~networkInsightsPathId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsPathId =
        field_map_exn json__ "NetworkInsightsPathId"
          NetworkInsightsPathId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~networkInsightsPathId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified path."]
module DeleteNetworkInsightsAnalysisResult =
  struct
    type nonrec t =
      {
      networkInsightsAnalysisId: NetworkInsightsAnalysisId.t option
        [@ocaml.doc "The ID of the network insights analysis."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAnalysisId =
      fun () -> { networkInsightsAnalysisId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAnalysisId",
           (Option.map x.networkInsightsAnalysisId
              ~f:NetworkInsightsAnalysisId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAnalysisId =
        (Option.map ~f:NetworkInsightsAnalysisId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAnalysisId") in
      make ?networkInsightsAnalysisId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsAnalysisId =
        field_map json__ "NetworkInsightsAnalysisId"
          NetworkInsightsAnalysisId.of_json in
      make ?networkInsightsAnalysisId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified network insights analysis."]
module DeleteNetworkInsightsAnalysisRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInsightsAnalysisId: NetworkInsightsAnalysisId.t
        [@ocaml.doc "The ID of the network insights analysis."]}
    let context_ = "DeleteNetworkInsightsAnalysisRequest"
    let make ?dryRun =
      fun ~networkInsightsAnalysisId ->
        fun () -> { dryRun; networkInsightsAnalysisId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInsightsAnalysisId",
          (Some
             (NetworkInsightsAnalysisId.to_value x.networkInsightsAnalysisId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAnalysisId =
        NetworkInsightsAnalysisId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAnalysisId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~networkInsightsAnalysisId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsAnalysisId =
        field_map_exn json__ "NetworkInsightsAnalysisId"
          NetworkInsightsAnalysisId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~networkInsightsAnalysisId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified network insights analysis."]
module DeleteNetworkInsightsAccessScopeResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t option
        [@ocaml.doc "The ID of the Network Access Scope."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeId =
      fun () -> { networkInsightsAccessScopeId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeId",
           (Option.map x.networkInsightsAccessScopeId
              ~f:NetworkInsightsAccessScopeId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeId =
        (Option.map ~f:NetworkInsightsAccessScopeId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeId") in
      make ?networkInsightsAccessScopeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsAccessScopeId =
        field_map json__ "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      make ?networkInsightsAccessScopeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Network Access Scope."]
module DeleteNetworkInsightsAccessScopeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t
        [@ocaml.doc "The ID of the Network Access Scope."]}
    let context_ = "DeleteNetworkInsightsAccessScopeRequest"
    let make ?dryRun =
      fun ~networkInsightsAccessScopeId ->
        fun () -> { dryRun; networkInsightsAccessScopeId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInsightsAccessScopeId",
          (Some
             (NetworkInsightsAccessScopeId.to_value
                x.networkInsightsAccessScopeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeId =
        NetworkInsightsAccessScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~networkInsightsAccessScopeId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsAccessScopeId =
        field_map_exn json__ "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~networkInsightsAccessScopeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Network Access Scope."]
module DeleteNetworkInsightsAccessScopeAnalysisResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisId:
        NetworkInsightsAccessScopeAnalysisId.t option
        [@ocaml.doc "The ID of the Network Access Scope analysis."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeAnalysisId =
      fun () -> { networkInsightsAccessScopeAnalysisId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeAnalysisId",
           (Option.map x.networkInsightsAccessScopeAnalysisId
              ~f:NetworkInsightsAccessScopeAnalysisId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeAnalysisId =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeAnalysisId") in
      make ?networkInsightsAccessScopeAnalysisId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsAccessScopeAnalysisId =
        field_map json__ "NetworkInsightsAccessScopeAnalysisId"
          NetworkInsightsAccessScopeAnalysisId.of_json in
      make ?networkInsightsAccessScopeAnalysisId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Network Access Scope analysis."]
module DeleteNetworkInsightsAccessScopeAnalysisRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisId:
        NetworkInsightsAccessScopeAnalysisId.t
        [@ocaml.doc "The ID of the Network Access Scope analysis."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteNetworkInsightsAccessScopeAnalysisRequest"
    let make ?dryRun =
      fun ~networkInsightsAccessScopeAnalysisId ->
        fun () -> { dryRun; networkInsightsAccessScopeAnalysisId }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeAnalysisId",
           (Some
              (NetworkInsightsAccessScopeAnalysisId.to_value
                 x.networkInsightsAccessScopeAnalysisId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInsightsAccessScopeAnalysisId =
        NetworkInsightsAccessScopeAnalysisId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeAnalysisId") in
      make ?dryRun ~networkInsightsAccessScopeAnalysisId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let networkInsightsAccessScopeAnalysisId =
        field_map_exn json__ "NetworkInsightsAccessScopeAnalysisId"
          NetworkInsightsAccessScopeAnalysisId.of_json in
      make ?dryRun ~networkInsightsAccessScopeAnalysisId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Network Access Scope analysis."]
module DeleteNetworkAclRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkAclId: NetworkAclId.t [@ocaml.doc "The ID of the network ACL."]}
    let context_ = "DeleteNetworkAclRequest"
    let make ?dryRun =
      fun ~networkAclId -> fun () -> { dryRun; networkAclId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~networkAclId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkAclId =
        field_map_exn json__ "NetworkAclId" NetworkAclId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~networkAclId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified network ACL. You can't delete the ACL if it's associated with any subnets. You can't delete the default network ACL."]
module DeleteNetworkAclEntryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkAclId: NetworkAclId.t [@ocaml.doc "The ID of the network ACL."];
      ruleNumber: Integer.t
        [@ocaml.doc "The rule number of the entry to delete."];
      egress: Boolean.t
        [@ocaml.doc "Indicates whether the rule is an egress rule."]}
    let context_ = "DeleteNetworkAclEntryRequest"
    let make ?dryRun =
      fun ~networkAclId ->
        fun ~ruleNumber ->
          fun ~egress ->
            fun () -> { dryRun; networkAclId; ruleNumber; egress }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)));
        ("ruleNumber", (Some (Integer.to_value x.ruleNumber)));
        ("egress", (Some (Boolean.to_value x.egress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let egress =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "egress") in
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleNumber") in
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~egress ~ruleNumber ~networkAclId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let egress = field_map_exn json__ "Egress" Boolean.of_json in
      let ruleNumber = field_map_exn json__ "RuleNumber" Integer.of_json in
      let networkAclId =
        field_map_exn json__ "NetworkAclId" NetworkAclId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~egress ~ruleNumber ~networkAclId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified ingress or egress entry (rule) from the specified network ACL."]
module DeleteNatGatewayResult =
  struct
    type nonrec t =
      {
      natGatewayId: String_.t option
        [@ocaml.doc "The ID of the NAT gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?natGatewayId = fun () -> { natGatewayId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("natGatewayId", (Option.map x.natGatewayId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "natGatewayId") in
      make ?natGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGatewayId = field_map json__ "NatGatewayId" String_.of_json in
      make ?natGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified NAT gateway. Deleting a public NAT gateway disassociates its Elastic IP address, but does not release the address from your account. Deleting a NAT gateway does not delete any NAT gateway routes in your route tables."]
module DeleteNatGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      natGatewayId: NatGatewayId.t [@ocaml.doc "The ID of the NAT gateway."]}
    let context_ = "DeleteNatGatewayRequest"
    let make ?dryRun =
      fun ~natGatewayId -> fun () -> { dryRun; natGatewayId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NatGatewayId", (Some (NatGatewayId.to_value x.natGatewayId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayId =
        NatGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NatGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~natGatewayId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGatewayId =
        field_map_exn json__ "NatGatewayId" NatGatewayId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~natGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified NAT gateway. Deleting a public NAT gateway disassociates its Elastic IP address, but does not release the address from your account. Deleting a NAT gateway does not delete any NAT gateway routes in your route tables."]
module DeleteManagedPrefixListResult =
  struct
    type nonrec t =
      {
      prefixList: ManagedPrefixList.t option
        [@ocaml.doc "Information about the prefix list."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixList = fun () -> { prefixList }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixList",
           (Option.map x.prefixList ~f:ManagedPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixList =
        (Option.map ~f:ManagedPrefixList.of_xml)
          (Xml.child xml_arg0 "prefixList") in
      make ?prefixList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixList =
        field_map json__ "PrefixList" ManagedPrefixList.of_json in
      make ?prefixList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified managed prefix list. You must first remove all references to the prefix list in your resources."]
module DeleteManagedPrefixListRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."]}
    let context_ = "DeleteManagedPrefixListRequest"
    let make ?dryRun =
      fun ~prefixListId -> fun () -> { dryRun; prefixListId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~prefixListId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixListId =
        field_map_exn json__ "PrefixListId" PrefixListResourceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~prefixListId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified managed prefix list. You must first remove all references to the prefix list in your resources."]
module DeleteLocalGatewayVirtualInterfaceResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterface: LocalGatewayVirtualInterface.t option
        [@ocaml.doc
          "Information about the deleted local gateway virtual interface."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterface =
      fun () -> { localGatewayVirtualInterface }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterface",
           (Option.map x.localGatewayVirtualInterface
              ~f:LocalGatewayVirtualInterface.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayVirtualInterface =
        (Option.map ~f:LocalGatewayVirtualInterface.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterface") in
      make ?localGatewayVirtualInterface ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayVirtualInterface =
        field_map json__ "LocalGatewayVirtualInterface"
          LocalGatewayVirtualInterface.of_json in
      make ?localGatewayVirtualInterface ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified local gateway virtual interface."]
module DeleteLocalGatewayVirtualInterfaceRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceId: LocalGatewayVirtualInterfaceId.t
        [@ocaml.doc "The ID of the local virtual interface to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteLocalGatewayVirtualInterfaceRequest"
    let make ?dryRun =
      fun ~localGatewayVirtualInterfaceId ->
        fun () -> { dryRun; localGatewayVirtualInterfaceId }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceId",
           (Some
              (LocalGatewayVirtualInterfaceId.to_value
                 x.localGatewayVirtualInterfaceId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayVirtualInterfaceId =
        LocalGatewayVirtualInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayVirtualInterfaceId") in
      make ?dryRun ~localGatewayVirtualInterfaceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayVirtualInterfaceId =
        field_map_exn json__ "LocalGatewayVirtualInterfaceId"
          LocalGatewayVirtualInterfaceId.of_json in
      make ?dryRun ~localGatewayVirtualInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified local gateway virtual interface."]
module DeleteLocalGatewayVirtualInterfaceGroupResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroup:
        LocalGatewayVirtualInterfaceGroup.t option
        [@ocaml.doc
          "Information about the deleted local gateway virtual interface group."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterfaceGroup =
      fun () -> { localGatewayVirtualInterfaceGroup }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterfaceGroup",
           (Option.map x.localGatewayVirtualInterfaceGroup
              ~f:LocalGatewayVirtualInterfaceGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayVirtualInterfaceGroup =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroup.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterfaceGroup") in
      make ?localGatewayVirtualInterfaceGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayVirtualInterfaceGroup =
        field_map json__ "LocalGatewayVirtualInterfaceGroup"
          LocalGatewayVirtualInterfaceGroup.of_json in
      make ?localGatewayVirtualInterfaceGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete the specified local gateway interface group."]
module DeleteLocalGatewayVirtualInterfaceGroupRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroupId:
        LocalGatewayVirtualInterfaceGroupId.t
        [@ocaml.doc
          "The ID of the local gateway virtual interface group to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteLocalGatewayVirtualInterfaceGroupRequest"
    let make ?dryRun =
      fun ~localGatewayVirtualInterfaceGroupId ->
        fun () -> { dryRun; localGatewayVirtualInterfaceGroupId }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceGroupId",
           (Some
              (LocalGatewayVirtualInterfaceGroupId.to_value
                 x.localGatewayVirtualInterfaceGroupId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayVirtualInterfaceGroupId =
        LocalGatewayVirtualInterfaceGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayVirtualInterfaceGroupId") in
      make ?dryRun ~localGatewayVirtualInterfaceGroupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayVirtualInterfaceGroupId =
        field_map_exn json__ "LocalGatewayVirtualInterfaceGroupId"
          LocalGatewayVirtualInterfaceGroupId.of_json in
      make ?dryRun ~localGatewayVirtualInterfaceGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete the specified local gateway interface group."]
module DeleteLocalGatewayRouteTableVpcAssociationResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociation:
        LocalGatewayRouteTableVpcAssociation.t option
        [@ocaml.doc "Information about the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVpcAssociation =
      fun () -> { localGatewayRouteTableVpcAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVpcAssociation",
           (Option.map x.localGatewayRouteTableVpcAssociation
              ~f:LocalGatewayRouteTableVpcAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableVpcAssociation =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociation.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableVpcAssociation") in
      make ?localGatewayRouteTableVpcAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayRouteTableVpcAssociation =
        field_map json__ "LocalGatewayRouteTableVpcAssociation"
          LocalGatewayRouteTableVpcAssociation.of_json in
      make ?localGatewayRouteTableVpcAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified association between a VPC and local gateway route table."]
module DeleteLocalGatewayRouteTableVpcAssociationRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociationId:
        LocalGatewayRouteTableVpcAssociationId.t
        [@ocaml.doc "The ID of the association."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteLocalGatewayRouteTableVpcAssociationRequest"
    let make ?dryRun =
      fun ~localGatewayRouteTableVpcAssociationId ->
        fun () -> { dryRun; localGatewayRouteTableVpcAssociationId }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVpcAssociationId",
           (Some
              (LocalGatewayRouteTableVpcAssociationId.to_value
                 x.localGatewayRouteTableVpcAssociationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayRouteTableVpcAssociationId =
        LocalGatewayRouteTableVpcAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableVpcAssociationId") in
      make ?dryRun ~localGatewayRouteTableVpcAssociationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayRouteTableVpcAssociationId =
        field_map_exn json__ "LocalGatewayRouteTableVpcAssociationId"
          LocalGatewayRouteTableVpcAssociationId.of_json in
      make ?dryRun ~localGatewayRouteTableVpcAssociationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified association between a VPC and local gateway route table."]
module DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociation:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociation.t option
        [@ocaml.doc "Information about the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVirtualInterfaceGroupAssociation =
      fun () -> { localGatewayRouteTableVirtualInterfaceGroupAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVirtualInterfaceGroupAssociation",
           (Option.map
              x.localGatewayRouteTableVirtualInterfaceGroupAssociation
              ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableVirtualInterfaceGroupAssociation =
        (Option.map
           ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociation.of_xml)
          (Xml.child xml_arg0
             "localGatewayRouteTableVirtualInterfaceGroupAssociation") in
      make ?localGatewayRouteTableVirtualInterfaceGroupAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayRouteTableVirtualInterfaceGroupAssociation =
        field_map json__
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociation"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociation.of_json in
      make ?localGatewayRouteTableVirtualInterfaceGroupAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a local gateway route table virtual interface group association."]
module DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociationId:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociationId.t
        [@ocaml.doc
          "The ID of the local gateway route table virtual interface group association."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ =
      "DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest"
    let make ?dryRun =
      fun ~localGatewayRouteTableVirtualInterfaceGroupAssociationId ->
        fun () ->
          { dryRun; localGatewayRouteTableVirtualInterfaceGroupAssociationId
          }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVirtualInterfaceGroupAssociationId",
           (Some
              (LocalGatewayRouteTableVirtualInterfaceGroupAssociationId.to_value
                 x.localGatewayRouteTableVirtualInterfaceGroupAssociationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayRouteTableVirtualInterfaceGroupAssociationId =
        LocalGatewayRouteTableVirtualInterfaceGroupAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableVirtualInterfaceGroupAssociationId") in
      make ?dryRun ~localGatewayRouteTableVirtualInterfaceGroupAssociationId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayRouteTableVirtualInterfaceGroupAssociationId =
        field_map_exn json__
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociationId"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociationId.of_json in
      make ?dryRun ~localGatewayRouteTableVirtualInterfaceGroupAssociationId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a local gateway route table virtual interface group association."]
module DeleteLocalGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTable: LocalGatewayRouteTable.t option
        [@ocaml.doc "Information about the local gateway route table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTable = fun () -> { localGatewayRouteTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTable",
           (Option.map x.localGatewayRouteTable
              ~f:LocalGatewayRouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTable =
        (Option.map ~f:LocalGatewayRouteTable.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTable") in
      make ?localGatewayRouteTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayRouteTable =
        field_map json__ "LocalGatewayRouteTable"
          LocalGatewayRouteTable.of_json in
      make ?localGatewayRouteTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a local gateway route table."]
module DeleteLocalGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteLocalGatewayRouteTableRequest"
    let make ?dryRun =
      fun ~localGatewayRouteTableId ->
        fun () -> { dryRun; localGatewayRouteTableId }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Some
              (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      make ?dryRun ~localGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayRouteTableId =
        field_map_exn json__ "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      make ?dryRun ~localGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a local gateway route table."]
module DeleteLocalGatewayRouteResult =
  struct
    type nonrec t =
      {
      route: LocalGatewayRoute.t option
        [@ocaml.doc "Information about the route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:LocalGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:LocalGatewayRoute.of_xml) (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let route = field_map json__ "Route" LocalGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route from the specified local gateway route table."]
module DeleteLocalGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t option
        [@ocaml.doc
          "The CIDR range for the route. This must match the CIDR for the route exactly."];
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      destinationPrefixListId: PrefixListResourceId.t option
        [@ocaml.doc
          "Use a prefix list in place of DestinationCidrBlock. You cannot use DestinationPrefixListId and DestinationCidrBlock in the same request."]}
    let context_ = "DeleteLocalGatewayRouteRequest"
    let make ?destinationCidrBlock =
      fun ?dryRun ->
        fun ?destinationPrefixListId ->
          fun ~localGatewayRouteTableId ->
            fun () ->
              {
                destinationCidrBlock;
                dryRun;
                destinationPrefixListId;
                localGatewayRouteTableId
              }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("LocalGatewayRouteTableId",
          (Some
             (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("DestinationPrefixListId",
          (Option.map x.destinationPrefixListId
             ~f:PrefixListResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationCidrBlock") in
      make ?destinationPrefixListId ?dryRun ~localGatewayRouteTableId
        ?destinationCidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationPrefixListId =
        field_map json__ "DestinationPrefixListId"
          PrefixListResourceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayRouteTableId =
        field_map_exn json__ "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      let destinationCidrBlock =
        field_map json__ "DestinationCidrBlock" String_.of_json in
      make ?destinationPrefixListId ?dryRun ~localGatewayRouteTableId
        ?destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified route from the specified local gateway route table."]
module DeleteLaunchTemplateVersionsResult =
  struct
    type nonrec t =
      {
      successfullyDeletedLaunchTemplateVersions:
        DeleteLaunchTemplateVersionsResponseSuccessSet.t option
        [@ocaml.doc
          "Information about the launch template versions that were successfully deleted."];
      unsuccessfullyDeletedLaunchTemplateVersions:
        DeleteLaunchTemplateVersionsResponseErrorSet.t option
        [@ocaml.doc
          "Information about the launch template versions that could not be deleted."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfullyDeletedLaunchTemplateVersions =
      fun ?unsuccessfullyDeletedLaunchTemplateVersions ->
        fun () ->
          {
            successfullyDeletedLaunchTemplateVersions;
            unsuccessfullyDeletedLaunchTemplateVersions
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfullyDeletedLaunchTemplateVersionSet",
           (Option.map x.successfullyDeletedLaunchTemplateVersions
              ~f:DeleteLaunchTemplateVersionsResponseSuccessSet.to_value));
        ("unsuccessfullyDeletedLaunchTemplateVersionSet",
          (Option.map x.unsuccessfullyDeletedLaunchTemplateVersions
             ~f:DeleteLaunchTemplateVersionsResponseErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfullyDeletedLaunchTemplateVersions =
        (Option.map ~f:DeleteLaunchTemplateVersionsResponseErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfullyDeletedLaunchTemplateVersionSet") in
      let successfullyDeletedLaunchTemplateVersions =
        (Option.map ~f:DeleteLaunchTemplateVersionsResponseSuccessSet.of_xml)
          (Xml.child xml_arg0 "successfullyDeletedLaunchTemplateVersionSet") in
      make ?unsuccessfullyDeletedLaunchTemplateVersions
        ?successfullyDeletedLaunchTemplateVersions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessfullyDeletedLaunchTemplateVersions =
        field_map json__ "UnsuccessfullyDeletedLaunchTemplateVersions"
          DeleteLaunchTemplateVersionsResponseErrorSet.of_json in
      let successfullyDeletedLaunchTemplateVersions =
        field_map json__ "SuccessfullyDeletedLaunchTemplateVersions"
          DeleteLaunchTemplateVersionsResponseSuccessSet.of_json in
      make ?unsuccessfullyDeletedLaunchTemplateVersions
        ?successfullyDeletedLaunchTemplateVersions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes one or more versions of a launch template. You can't delete the default version of a launch template; you must first assign a different version as the default. If the default version is the only version for the launch template, you must delete the entire launch template using DeleteLaunchTemplate. You can delete up to 200 launch template versions in a single request. To delete more than 200 versions in a single request, use DeleteLaunchTemplate, which deletes the launch template and all of its versions. For more information, see Delete a launch template version in the Amazon EC2 User Guide."]
module DeleteLaunchTemplateVersionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      launchTemplateId: LaunchTemplateId.t option
        [@ocaml.doc
          "The ID of the launch template. You must specify either the launch template ID or the launch template name, but not both."];
      launchTemplateName: LaunchTemplateName.t option
        [@ocaml.doc
          "The name of the launch template. You must specify either the launch template ID or the launch template name, but not both."];
      versions: VersionStringList.t
        [@ocaml.doc
          "The version numbers of one or more launch template versions to delete. You can specify up to 200 launch template version numbers."]}
    let context_ = "DeleteLaunchTemplateVersionsRequest"
    let make ?dryRun =
      fun ?launchTemplateId ->
        fun ?launchTemplateName ->
          fun ~versions ->
            fun () ->
              { dryRun; launchTemplateId; launchTemplateName; versions }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("LaunchTemplateVersion",
          (Some (VersionStringList.to_value x.versions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versions =
        VersionStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~versions ?launchTemplateName ?launchTemplateId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let versions =
        field_map_exn json__ "Versions" VersionStringList.of_json in
      let launchTemplateName =
        field_map json__ "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json__ "LaunchTemplateId" LaunchTemplateId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~versions ?launchTemplateName ?launchTemplateId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes one or more versions of a launch template. You can't delete the default version of a launch template; you must first assign a different version as the default. If the default version is the only version for the launch template, you must delete the entire launch template using DeleteLaunchTemplate. You can delete up to 200 launch template versions in a single request. To delete more than 200 versions in a single request, use DeleteLaunchTemplate, which deletes the launch template and all of its versions. For more information, see Delete a launch template version in the Amazon EC2 User Guide."]
module DeleteLaunchTemplateResult =
  struct
    type nonrec t =
      {
      launchTemplate: LaunchTemplate.t option
        [@ocaml.doc "Information about the launch template."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplate = fun () -> { launchTemplate }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplate",
           (Option.map x.launchTemplate ~f:LaunchTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplate =
        (Option.map ~f:LaunchTemplate.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      make ?launchTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let launchTemplate =
        field_map json__ "LaunchTemplate" LaunchTemplate.of_json in
      make ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a launch template. Deleting a launch template deletes all of its versions."]
module DeleteLaunchTemplateRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      launchTemplateId: LaunchTemplateId.t option
        [@ocaml.doc
          "The ID of the launch template. You must specify either the launch template ID or the launch template name, but not both."];
      launchTemplateName: LaunchTemplateName.t option
        [@ocaml.doc
          "The name of the launch template. You must specify either the launch template ID or the launch template name, but not both."]}
    let make ?dryRun =
      fun ?launchTemplateId ->
        fun ?launchTemplateName ->
          fun () -> { dryRun; launchTemplateId; launchTemplateName }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?launchTemplateName ?launchTemplateId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let launchTemplateName =
        field_map json__ "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json__ "LaunchTemplateId" LaunchTemplateId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?launchTemplateName ?launchTemplateId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a launch template. Deleting a launch template deletes all of its versions."]
module DeleteKeyPairResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds, and an error otherwise."];
      keyPairId: String_.t option [@ocaml.doc "The ID of the key pair."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun ?keyPairId -> fun () -> { return; keyPairId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("keyPairId", (Option.map x.keyPairId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyPairId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyPairId") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?keyPairId ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let keyPairId = field_map json__ "KeyPairId" String_.of_json in
      let return = field_map json__ "Return" Boolean.of_json in
      make ?keyPairId ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified key pair, by removing the public key from Amazon EC2."]
module DeleteKeyPairRequest =
  struct
    type nonrec t =
      {
      keyName: KeyPairNameWithResolver.t option
        [@ocaml.doc "The name of the key pair."];
      keyPairId: KeyPairId.t option [@ocaml.doc "The ID of the key pair."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?keyName =
      fun ?keyPairId ->
        fun ?dryRun -> fun () -> { keyName; keyPairId; dryRun }
    let to_value x =
      structure_to_value
        [("KeyName",
           (Option.map x.keyName ~f:KeyPairNameWithResolver.to_value));
        ("KeyPairId", (Option.map x.keyPairId ~f:KeyPairId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let keyPairId =
        (Option.map ~f:KeyPairId.of_xml) (Xml.child xml_arg0 "KeyPairId") in
      let keyName =
        (Option.map ~f:KeyPairNameWithResolver.of_xml)
          (Xml.child xml_arg0 "KeyName") in
      make ?dryRun ?keyPairId ?keyName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let keyPairId = field_map json__ "KeyPairId" KeyPairId.of_json in
      let keyName =
        field_map json__ "KeyName" KeyPairNameWithResolver.of_json in
      make ?dryRun ?keyPairId ?keyName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified key pair, by removing the public key from Amazon EC2."]
module DeleteIpamScopeResult =
  struct
    type nonrec t =
      {
      ipamScope: IpamScope.t option
        [@ocaml.doc "Information about the results of the deletion."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamScope = fun () -> { ipamScope }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamScope", (Option.map x.ipamScope ~f:IpamScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScope =
        (Option.map ~f:IpamScope.of_xml) (Xml.child xml_arg0 "ipamScope") in
      make ?ipamScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamScope = field_map json__ "IpamScope" IpamScope.of_json in
      make ?ipamScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete the scope for an IPAM. You cannot delete the default scopes. For more information, see Delete a scope in the Amazon VPC IPAM User Guide."]
module DeleteIpamScopeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamScopeId: IpamScopeId.t
        [@ocaml.doc "The ID of the scope to delete."]}
    let context_ = "DeleteIpamScopeRequest"
    let make ?dryRun = fun ~ipamScopeId -> fun () -> { dryRun; ipamScopeId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamScopeId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamScopeId =
        field_map_exn json__ "IpamScopeId" IpamScopeId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamScopeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete the scope for an IPAM. You cannot delete the default scopes. For more information, see Delete a scope in the Amazon VPC IPAM User Guide."]
module DeleteIpamResult =
  struct
    type nonrec t =
      {
      ipam: Ipam.t option
        [@ocaml.doc "Information about the results of the deletion."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipam = fun () -> { ipam }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("ipam", (Option.map x.ipam ~f:Ipam.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipam = (Option.map ~f:Ipam.of_xml) (Xml.child xml_arg0 "ipam") in
      make ?ipam ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipam = field_map json__ "Ipam" Ipam.of_json in make ?ipam ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an IPAM. Deleting an IPAM removes all monitored data associated with the IPAM including the historical data for CIDRs. For more information, see Delete an IPAM in the Amazon VPC IPAM User Guide."]
module DeleteIpamResourceDiscoveryResult =
  struct
    type nonrec t =
      {
      ipamResourceDiscovery: IpamResourceDiscovery.t option
        [@ocaml.doc "The IPAM resource discovery."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamResourceDiscovery = fun () -> { ipamResourceDiscovery }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamResourceDiscovery",
           (Option.map x.ipamResourceDiscovery
              ~f:IpamResourceDiscovery.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamResourceDiscovery =
        (Option.map ~f:IpamResourceDiscovery.of_xml)
          (Xml.child xml_arg0 "ipamResourceDiscovery") in
      make ?ipamResourceDiscovery ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamResourceDiscovery =
        field_map json__ "IpamResourceDiscovery"
          IpamResourceDiscovery.of_json in
      make ?ipamResourceDiscovery ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module DeleteIpamResourceDiscoveryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamResourceDiscoveryId: IpamResourceDiscoveryId.t
        [@ocaml.doc "The IPAM resource discovery ID."]}
    let context_ = "DeleteIpamResourceDiscoveryRequest"
    let make ?dryRun =
      fun ~ipamResourceDiscoveryId ->
        fun () -> { dryRun; ipamResourceDiscoveryId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamResourceDiscoveryId",
          (Some (IpamResourceDiscoveryId.to_value x.ipamResourceDiscoveryId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamResourceDiscoveryId =
        IpamResourceDiscoveryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamResourceDiscoveryId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamResourceDiscoveryId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamResourceDiscoveryId =
        field_map_exn json__ "IpamResourceDiscoveryId"
          IpamResourceDiscoveryId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamResourceDiscoveryId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module DeleteIpamRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t [@ocaml.doc "The ID of the IPAM to delete."];
      cascade: Boolean.t option
        [@ocaml.doc
          "Enables you to quickly delete an IPAM, private scopes, pools in private scopes, and any allocations in the pools in private scopes. You cannot delete the IPAM with this option if there is a pool in your public scope. If you use this option, IPAM does the following: Deallocates any CIDRs allocated to VPC resources (such as VPCs) in pools in private scopes. No VPC resources are deleted as a result of enabling this option. The CIDR associated with the resource will no longer be allocated from an IPAM pool, but the CIDR itself will remain unchanged. Deprovisions all IPv4 CIDRs provisioned to IPAM pools in private scopes. Deletes all IPAM pools in private scopes. Deletes all non-default private scopes in the IPAM. Deletes the default public and private scopes and the IPAM."]}
    let context_ = "DeleteIpamRequest"
    let make ?dryRun =
      fun ?cascade -> fun ~ipamId -> fun () -> { dryRun; cascade; ipamId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Cascade", (Option.map x.cascade ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cascade =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Cascade") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cascade ~ipamId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cascade = field_map json__ "Cascade" Boolean.of_json in
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?cascade ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an IPAM. Deleting an IPAM removes all monitored data associated with the IPAM including the historical data for CIDRs. For more information, see Delete an IPAM in the Amazon VPC IPAM User Guide."]
module DeleteIpamPrefixListResolverTargetResult =
  struct
    type nonrec t =
      {
      ipamPrefixListResolverTarget: IpamPrefixListResolverTarget.t option
        [@ocaml.doc
          "Information about the IPAM prefix list resolver target that was deleted."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPrefixListResolverTarget =
      fun () -> { ipamPrefixListResolverTarget }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPrefixListResolverTarget",
           (Option.map x.ipamPrefixListResolverTarget
              ~f:IpamPrefixListResolverTarget.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverTarget =
        (Option.map ~f:IpamPrefixListResolverTarget.of_xml)
          (Xml.child xml_arg0 "ipamPrefixListResolverTarget") in
      make ?ipamPrefixListResolverTarget ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverTarget =
        field_map json__ "IpamPrefixListResolverTarget"
          IpamPrefixListResolverTarget.of_json in
      make ?ipamPrefixListResolverTarget ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM prefix list resolver target. This removes the association between the resolver and the managed prefix list, stopping automatic CIDR synchronization. For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide."]
module DeleteIpamPrefixListResolverTargetRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPrefixListResolverTargetId: IpamPrefixListResolverTargetId.t
        [@ocaml.doc
          "The ID of the IPAM prefix list resolver target to delete."]}
    let context_ = "DeleteIpamPrefixListResolverTargetRequest"
    let make ?dryRun =
      fun ~ipamPrefixListResolverTargetId ->
        fun () -> { dryRun; ipamPrefixListResolverTargetId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPrefixListResolverTargetId",
          (Some
             (IpamPrefixListResolverTargetId.to_value
                x.ipamPrefixListResolverTargetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverTargetId =
        IpamPrefixListResolverTargetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "IpamPrefixListResolverTargetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamPrefixListResolverTargetId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverTargetId =
        field_map_exn json__ "IpamPrefixListResolverTargetId"
          IpamPrefixListResolverTargetId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamPrefixListResolverTargetId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM prefix list resolver target. This removes the association between the resolver and the managed prefix list, stopping automatic CIDR synchronization. For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide."]
module DeleteIpamPrefixListResolverResult =
  struct
    type nonrec t =
      {
      ipamPrefixListResolver: IpamPrefixListResolver.t option
        [@ocaml.doc
          "Information about the IPAM prefix list resolver that was deleted."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPrefixListResolver = fun () -> { ipamPrefixListResolver }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPrefixListResolver",
           (Option.map x.ipamPrefixListResolver
              ~f:IpamPrefixListResolver.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolver =
        (Option.map ~f:IpamPrefixListResolver.of_xml)
          (Xml.child xml_arg0 "ipamPrefixListResolver") in
      make ?ipamPrefixListResolver ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolver =
        field_map json__ "IpamPrefixListResolver"
          IpamPrefixListResolver.of_json in
      make ?ipamPrefixListResolver ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM prefix list resolver. Before deleting a resolver, you must first delete all resolver targets associated with it."]
module DeleteIpamPrefixListResolverRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPrefixListResolverId: IpamPrefixListResolverId.t
        [@ocaml.doc "The ID of the IPAM prefix list resolver to delete."]}
    let context_ = "DeleteIpamPrefixListResolverRequest"
    let make ?dryRun =
      fun ~ipamPrefixListResolverId ->
        fun () -> { dryRun; ipamPrefixListResolverId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPrefixListResolverId",
          (Some
             (IpamPrefixListResolverId.to_value x.ipamPrefixListResolverId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverId =
        IpamPrefixListResolverId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "IpamPrefixListResolverId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamPrefixListResolverId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverId =
        field_map_exn json__ "IpamPrefixListResolverId"
          IpamPrefixListResolverId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamPrefixListResolverId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM prefix list resolver. Before deleting a resolver, you must first delete all resolver targets associated with it."]
module DeleteIpamPoolResult =
  struct
    type nonrec t =
      {
      ipamPool: IpamPool.t option
        [@ocaml.doc "Information about the results of the deletion."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPool = fun () -> { ipamPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPool", (Option.map x.ipamPool ~f:IpamPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPool =
        (Option.map ~f:IpamPool.of_xml) (Xml.child xml_arg0 "ipamPool") in
      make ?ipamPool ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPool = field_map json__ "IpamPool" IpamPool.of_json in
      make ?ipamPool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an IPAM pool. You cannot delete an IPAM pool if there are allocations in it or CIDRs provisioned to it. To release allocations, see ReleaseIpamPoolAllocation. To deprovision pool CIDRs, see DeprovisionIpamPoolCidr. For more information, see Delete a pool in the Amazon VPC IPAM User Guide."]
module DeleteIpamPoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t [@ocaml.doc "The ID of the pool to delete."];
      cascade: Boolean.t option
        [@ocaml.doc
          "Enables you to quickly delete an IPAM pool and all resources within that pool, including provisioned CIDRs, allocations, and other pools. You can only use this option to delete pools in the private scope or pools in the public scope with a source resource. A source resource is a resource used to provision CIDRs to a resource planning pool."]}
    let context_ = "DeleteIpamPoolRequest"
    let make ?dryRun =
      fun ?cascade ->
        fun ~ipamPoolId -> fun () -> { dryRun; cascade; ipamPoolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cascade", (Option.map x.cascade ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cascade =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Cascade") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cascade ~ipamPoolId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cascade = field_map json__ "Cascade" Boolean.of_json in
      let ipamPoolId = field_map_exn json__ "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?cascade ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an IPAM pool. You cannot delete an IPAM pool if there are allocations in it or CIDRs provisioned to it. To release allocations, see ReleaseIpamPoolAllocation. To deprovision pool CIDRs, see DeprovisionIpamPoolCidr. For more information, see Delete a pool in the Amazon VPC IPAM User Guide."]
module DeleteIpamPolicyResult =
  struct
    type nonrec t =
      {
      ipamPolicy: IpamPolicy.t option
        [@ocaml.doc
          "Information about the deleted IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPolicy = fun () -> { ipamPolicy }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPolicy", (Option.map x.ipamPolicy ~f:IpamPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPolicy =
        (Option.map ~f:IpamPolicy.of_xml) (Xml.child xml_arg0 "ipamPolicy") in
      make ?ipamPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPolicy = field_map json__ "IpamPolicy" IpamPolicy.of_json in
      make ?ipamPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]
module DeleteIpamPolicyRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPolicyId: IpamPolicyId.t
        [@ocaml.doc "The ID of the IPAM policy to delete."]}
    let context_ = "DeleteIpamPolicyRequest"
    let make ?dryRun =
      fun ~ipamPolicyId -> fun () -> { dryRun; ipamPolicyId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPolicyId", (Some (IpamPolicyId.to_value x.ipamPolicyId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPolicyId =
        IpamPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPolicyId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamPolicyId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPolicyId =
        field_map_exn json__ "IpamPolicyId" IpamPolicyId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamPolicyId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]
module DeleteIpamExternalResourceVerificationTokenResult =
  struct
    type nonrec t =
      {
      ipamExternalResourceVerificationToken:
        IpamExternalResourceVerificationToken.t option
        [@ocaml.doc "The verification token."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamExternalResourceVerificationToken =
      fun () -> { ipamExternalResourceVerificationToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamExternalResourceVerificationToken",
           (Option.map x.ipamExternalResourceVerificationToken
              ~f:IpamExternalResourceVerificationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamExternalResourceVerificationToken =
        (Option.map ~f:IpamExternalResourceVerificationToken.of_xml)
          (Xml.child xml_arg0 "ipamExternalResourceVerificationToken") in
      make ?ipamExternalResourceVerificationToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamExternalResourceVerificationToken =
        field_map json__ "IpamExternalResourceVerificationToken"
          IpamExternalResourceVerificationToken.of_json in
      make ?ipamExternalResourceVerificationToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a verification token. A verification token is an Amazon Web Services-generated random value that you can use to prove ownership of an external resource. For example, you can use a verification token to validate that you control a public IP address range when you bring an IP address range to Amazon Web Services (BYOIP)."]
module DeleteIpamExternalResourceVerificationTokenRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamExternalResourceVerificationTokenId:
        IpamExternalResourceVerificationTokenId.t
        [@ocaml.doc "The token ID."]}
    let context_ = "DeleteIpamExternalResourceVerificationTokenRequest"
    let make ?dryRun =
      fun ~ipamExternalResourceVerificationTokenId ->
        fun () -> { dryRun; ipamExternalResourceVerificationTokenId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamExternalResourceVerificationTokenId",
          (Some
             (IpamExternalResourceVerificationTokenId.to_value
                x.ipamExternalResourceVerificationTokenId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamExternalResourceVerificationTokenId =
        IpamExternalResourceVerificationTokenId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "IpamExternalResourceVerificationTokenId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamExternalResourceVerificationTokenId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamExternalResourceVerificationTokenId =
        field_map_exn json__ "IpamExternalResourceVerificationTokenId"
          IpamExternalResourceVerificationTokenId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamExternalResourceVerificationTokenId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a verification token. A verification token is an Amazon Web Services-generated random value that you can use to prove ownership of an external resource. For example, you can use a verification token to validate that you control a public IP address range when you bring an IP address range to Amazon Web Services (BYOIP)."]
module DeleteInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      internetGatewayId: InternetGatewayId.t
        [@ocaml.doc "The ID of the internet gateway."]}
    let context_ = "DeleteInternetGatewayRequest"
    let make ?dryRun =
      fun ~internetGatewayId -> fun () -> { dryRun; internetGatewayId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Some (InternetGatewayId.to_value x.internetGatewayId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let internetGatewayId =
        InternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~internetGatewayId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let internetGatewayId =
        field_map_exn json__ "InternetGatewayId" InternetGatewayId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~internetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified internet gateway. You must detach the internet gateway from the VPC before you can delete it."]
module DeleteInstanceEventWindowResult =
  struct
    type nonrec t =
      {
      instanceEventWindowState: InstanceEventWindowStateChange.t option
        [@ocaml.doc "The state of the event window."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindowState =
      fun () -> { instanceEventWindowState }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindowState",
           (Option.map x.instanceEventWindowState
              ~f:InstanceEventWindowStateChange.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindowState =
        (Option.map ~f:InstanceEventWindowStateChange.of_xml)
          (Xml.child xml_arg0 "instanceEventWindowState") in
      make ?instanceEventWindowState ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceEventWindowState =
        field_map json__ "InstanceEventWindowState"
          InstanceEventWindowStateChange.of_json in
      make ?instanceEventWindowState ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified event window. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module DeleteInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      forceDelete: Boolean.t option
        [@ocaml.doc
          "Specify true to force delete the event window. Use the force delete parameter if the event window is currently associated with targets."];
      instanceEventWindowId: InstanceEventWindowId.t
        [@ocaml.doc "The ID of the event window."]}
    let context_ = "DeleteInstanceEventWindowRequest"
    let make ?dryRun =
      fun ?forceDelete ->
        fun ~instanceEventWindowId ->
          fun () -> { dryRun; forceDelete; instanceEventWindowId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ForceDelete", (Option.map x.forceDelete ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Some (InstanceEventWindowId.to_value x.instanceEventWindowId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindowId =
        InstanceEventWindowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventWindowId") in
      let forceDelete =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ForceDelete") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceEventWindowId ?forceDelete ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceEventWindowId =
        field_map_exn json__ "InstanceEventWindowId"
          InstanceEventWindowId.of_json in
      let forceDelete = field_map json__ "ForceDelete" Boolean.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~instanceEventWindowId ?forceDelete ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified event window. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module DeleteInstanceConnectEndpointResult =
  struct
    type nonrec t =
      {
      instanceConnectEndpoint: Ec2InstanceConnectEndpoint.t option
        [@ocaml.doc "Information about the EC2 Instance Connect Endpoint."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceConnectEndpoint = fun () -> { instanceConnectEndpoint }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceConnectEndpoint",
           (Option.map x.instanceConnectEndpoint
              ~f:Ec2InstanceConnectEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceConnectEndpoint =
        (Option.map ~f:Ec2InstanceConnectEndpoint.of_xml)
          (Xml.child xml_arg0 "instanceConnectEndpoint") in
      make ?instanceConnectEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceConnectEndpoint =
        field_map json__ "InstanceConnectEndpoint"
          Ec2InstanceConnectEndpoint.of_json in
      make ?instanceConnectEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified EC2 Instance Connect Endpoint."]
module DeleteInstanceConnectEndpointRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceConnectEndpointId: InstanceConnectEndpointId.t
        [@ocaml.doc "The ID of the EC2 Instance Connect Endpoint to delete."]}
    let context_ = "DeleteInstanceConnectEndpointRequest"
    let make ?dryRun =
      fun ~instanceConnectEndpointId ->
        fun () -> { dryRun; instanceConnectEndpointId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceConnectEndpointId",
          (Some
             (InstanceConnectEndpointId.to_value x.instanceConnectEndpointId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceConnectEndpointId =
        InstanceConnectEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "InstanceConnectEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceConnectEndpointId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceConnectEndpointId =
        field_map_exn json__ "InstanceConnectEndpointId"
          InstanceConnectEndpointId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~instanceConnectEndpointId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified EC2 Instance Connect Endpoint."]
module DeleteImageUsageReportResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified image usage report. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module DeleteImageUsageReportRequest =
  struct
    type nonrec t =
      {
      reportId: ImageUsageReportId.t
        [@ocaml.doc "The ID of the report to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteImageUsageReportRequest"
    let make ?dryRun = fun ~reportId -> fun () -> { dryRun; reportId }
    let to_value x =
      structure_to_value
        [("ReportId", (Some (ImageUsageReportId.to_value x.reportId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let reportId =
        ImageUsageReportId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportId") in
      make ?dryRun ~reportId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let reportId =
        field_map_exn json__ "ReportId" ImageUsageReportId.of_json in
      make ?dryRun ~reportId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified image usage report. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module DeleteFpgaImageResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds, and an error otherwise."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Amazon FPGA Image (AFI)."]
module DeleteFpgaImageRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fpgaImageId: FpgaImageId.t [@ocaml.doc "The ID of the AFI."]}
    let context_ = "DeleteFpgaImageRequest"
    let make ?dryRun = fun ~fpgaImageId -> fun () -> { dryRun; fpgaImageId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId", (Some (FpgaImageId.to_value x.fpgaImageId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageId =
        FpgaImageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~fpgaImageId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fpgaImageId =
        field_map_exn json__ "FpgaImageId" FpgaImageId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~fpgaImageId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Amazon FPGA Image (AFI)."]
module DeleteFlowLogsResult =
  struct
    type nonrec t =
      {
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the flow logs that could not be deleted successfully."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessful =
        field_map json__ "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes one or more flow logs."]
module DeleteFlowLogsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      flowLogIds: FlowLogIdList.t
        [@ocaml.doc
          "One or more flow log IDs. Constraint: Maximum of 1000 flow log IDs."]}
    let context_ = "DeleteFlowLogsRequest"
    let make ?dryRun = fun ~flowLogIds -> fun () -> { dryRun; flowLogIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FlowLogId", (Some (FlowLogIdList.to_value x.flowLogIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flowLogIds =
        FlowLogIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FlowLogId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~flowLogIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flowLogIds =
        field_map_exn json__ "FlowLogIds" FlowLogIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~flowLogIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes one or more flow logs."]
module DeleteFleetsResult =
  struct
    type nonrec t =
      {
      successfulFleetDeletions: DeleteFleetSuccessSet.t option
        [@ocaml.doc
          "Information about the EC2 Fleets that are successfully deleted."];
      unsuccessfulFleetDeletions: DeleteFleetErrorSet.t option
        [@ocaml.doc
          "Information about the EC2 Fleets that are not successfully deleted."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulFleetDeletions =
      fun ?unsuccessfulFleetDeletions ->
        fun () -> { successfulFleetDeletions; unsuccessfulFleetDeletions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulFleetDeletionSet",
           (Option.map x.successfulFleetDeletions
              ~f:DeleteFleetSuccessSet.to_value));
        ("unsuccessfulFleetDeletionSet",
          (Option.map x.unsuccessfulFleetDeletions
             ~f:DeleteFleetErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfulFleetDeletions =
        (Option.map ~f:DeleteFleetErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfulFleetDeletionSet") in
      let successfulFleetDeletions =
        (Option.map ~f:DeleteFleetSuccessSet.of_xml)
          (Xml.child xml_arg0 "successfulFleetDeletionSet") in
      make ?unsuccessfulFleetDeletions ?successfulFleetDeletions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessfulFleetDeletions =
        field_map json__ "UnsuccessfulFleetDeletions"
          DeleteFleetErrorSet.of_json in
      let successfulFleetDeletions =
        field_map json__ "SuccessfulFleetDeletions"
          DeleteFleetSuccessSet.of_json in
      make ?unsuccessfulFleetDeletions ?successfulFleetDeletions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified EC2 Fleet request. After you delete an EC2 Fleet request, it launches no new instances. You must also specify whether a deleted EC2 Fleet request should terminate its instances. If you choose to terminate the instances, the EC2 Fleet request enters the deleted_terminating state. Otherwise, it enters the deleted_running state, and the instances continue to run until they are interrupted or you terminate them manually. A deleted instant fleet with running instances is not supported. When you delete an instant fleet, Amazon EC2 automatically terminates all its instances. For fleets with more than 1000 instances, the deletion request might fail. If your fleet has more than 1000 instances, first terminate most of the instances manually, leaving 1000 or fewer. Then delete the fleet, and the remaining instances will be terminated automatically. Terminating an instance is permanent and irreversible. After you terminate an instance, you can no longer connect to it, and it can't be recovered. All attached Amazon EBS volumes that are configured to be deleted on termination are also permanently deleted and can't be recovered. All data stored on instance store volumes is permanently lost. For more information, see How instance termination works. Before you terminate an instance, ensure that you have backed up all data that you need to retain after the termination to persistent storage. Restrictions You can delete up to 25 fleets of type instant in a single request. You can delete up to 100 fleets of type maintain or request in a single request. You can delete up to 125 fleets in a single request, provided you do not exceed the quota for each fleet type, as specified above. If you exceed the specified number of fleets to delete, no fleets are deleted. For more information, see Delete an EC2 Fleet request and the instances in the fleet in the Amazon EC2 User Guide."]
module DeleteFleetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fleetIds: FleetIdSet.t
        [@ocaml.doc
          "The IDs of the EC2 Fleets. Constraints: In a single request, you can specify up to 25 instant fleet IDs and up to 100 maintain or request fleet IDs."];
      terminateInstances: Boolean.t
        [@ocaml.doc
          "Indicates whether to terminate the associated instances when the EC2 Fleet is deleted. The default is to terminate the instances. To let the instances continue to run after the EC2 Fleet is deleted, specify no-terminate-instances. Supported only for fleets of type maintain and request. For instant fleets, you cannot specify NoTerminateInstances. A deleted instant fleet with running instances is not supported."]}
    let context_ = "DeleteFleetsRequest"
    let make ?dryRun =
      fun ~fleetIds ->
        fun ~terminateInstances ->
          fun () -> { dryRun; fleetIds; terminateInstances }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FleetId", (Some (FleetIdSet.to_value x.fleetIds)));
        ("TerminateInstances",
          (Some (Boolean.to_value x.terminateInstances)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminateInstances =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TerminateInstances") in
      let fleetIds =
        FleetIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~terminateInstances ~fleetIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let terminateInstances =
        field_map_exn json__ "TerminateInstances" Boolean.of_json in
      let fleetIds = field_map_exn json__ "FleetIds" FleetIdSet.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~terminateInstances ~fleetIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified EC2 Fleet request. After you delete an EC2 Fleet request, it launches no new instances. You must also specify whether a deleted EC2 Fleet request should terminate its instances. If you choose to terminate the instances, the EC2 Fleet request enters the deleted_terminating state. Otherwise, it enters the deleted_running state, and the instances continue to run until they are interrupted or you terminate them manually. A deleted instant fleet with running instances is not supported. When you delete an instant fleet, Amazon EC2 automatically terminates all its instances. For fleets with more than 1000 instances, the deletion request might fail. If your fleet has more than 1000 instances, first terminate most of the instances manually, leaving 1000 or fewer. Then delete the fleet, and the remaining instances will be terminated automatically. Terminating an instance is permanent and irreversible. After you terminate an instance, you can no longer connect to it, and it can't be recovered. All attached Amazon EBS volumes that are configured to be deleted on termination are also permanently deleted and can't be recovered. All data stored on instance store volumes is permanently lost. For more information, see How instance termination works. Before you terminate an instance, ensure that you have backed up all data that you need to retain after the termination to persistent storage. Restrictions You can delete up to 25 fleets of type instant in a single request. You can delete up to 100 fleets of type maintain or request in a single request. You can delete up to 125 fleets in a single request, provided you do not exceed the quota for each fleet type, as specified above. If you exceed the specified number of fleets to delete, no fleets are deleted. For more information, see Delete an EC2 Fleet request and the instances in the fleet in the Amazon EC2 User Guide."]
module DeleteEgressOnlyInternetGatewayResult =
  struct
    type nonrec t =
      {
      returnCode: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnCode = fun () -> { returnCode }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("returnCode", (Option.map x.returnCode ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnCode =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "returnCode") in
      make ?returnCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnCode = field_map json__ "ReturnCode" Boolean.of_json in
      make ?returnCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes an egress-only internet gateway."]
module DeleteEgressOnlyInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      egressOnlyInternetGatewayId: EgressOnlyInternetGatewayId.t
        [@ocaml.doc "The ID of the egress-only internet gateway."]}
    let context_ = "DeleteEgressOnlyInternetGatewayRequest"
    let make ?dryRun =
      fun ~egressOnlyInternetGatewayId ->
        fun () -> { dryRun; egressOnlyInternetGatewayId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("EgressOnlyInternetGatewayId",
          (Some
             (EgressOnlyInternetGatewayId.to_value
                x.egressOnlyInternetGatewayId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let egressOnlyInternetGatewayId =
        EgressOnlyInternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "EgressOnlyInternetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~egressOnlyInternetGatewayId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let egressOnlyInternetGatewayId =
        field_map_exn json__ "EgressOnlyInternetGatewayId"
          EgressOnlyInternetGatewayId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~egressOnlyInternetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes an egress-only internet gateway."]
module DeleteDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpOptionsId: DhcpOptionsId.t
        [@ocaml.doc "The ID of the DHCP options set."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteDhcpOptionsRequest"
    let make ?dryRun =
      fun ~dhcpOptionsId -> fun () -> { dryRun; dhcpOptionsId }
    let to_value x =
      structure_to_value
        [("DhcpOptionsId", (Some (DhcpOptionsId.to_value x.dhcpOptionsId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let dhcpOptionsId =
        DhcpOptionsId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DhcpOptionsId") in
      make ?dryRun ~dhcpOptionsId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let dhcpOptionsId =
        field_map_exn json__ "DhcpOptionsId" DhcpOptionsId.of_json in
      make ?dryRun ~dhcpOptionsId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified set of DHCP options. You must disassociate the set of DHCP options before you can delete it. You can disassociate the set of DHCP options by associating either a new set of options or the default set of options with the VPC."]
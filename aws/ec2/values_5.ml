(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
open Values_0[@@warning "-33"]
open Values_1[@@warning "-33"]
open Values_2[@@warning "-33"]
open Values_3[@@warning "-33"]
open Values_4[@@warning "-33"]
module SpotOptionsRequest =
  struct
    type nonrec t =
      {
      allocationStrategy: SpotAllocationStrategy.t option
        [@ocaml.doc
          "The strategy that determines how to allocate the target Spot Instance capacity across the Spot Instance pools specified by the EC2 Fleet. lowest-price - EC2 Fleet launches instances from the Spot Instance pools with the lowest price. diversified - EC2 Fleet launches instances from all of the Spot Instance pools that you specify. capacity-optimized (recommended) - EC2 Fleet launches instances from Spot Instance pools with optimal capacity for the number of instances that are launching. To give certain instance types a higher chance of launching first, use capacity-optimized-prioritized. Set a priority for each instance type by using the Priority parameter for LaunchTemplateOverrides. You can assign the same priority to different LaunchTemplateOverrides. EC2 implements the priorities on a best-effort basis, but optimizes for capacity first. capacity-optimized-prioritized is supported only if your fleet uses a launch template. Note that if the On-Demand AllocationStrategy is set to prioritized, the same priority is applied when fulfilling On-Demand capacity. Default: lowest-price"];
      maintenanceStrategies: FleetSpotMaintenanceStrategiesRequest.t option
        [@ocaml.doc
          "The strategies for managing your Spot Instances that are at an elevated risk of being interrupted."];
      instanceInterruptionBehavior: SpotInstanceInterruptionBehavior.t option
        [@ocaml.doc
          "The behavior when a Spot Instance is interrupted. Default: terminate"];
      instancePoolsToUseCount: Integer.t option
        [@ocaml.doc
          "The number of Spot pools across which to allocate your target Spot capacity. Supported only when Spot AllocationStrategy is set to lowest-price. EC2 Fleet selects the cheapest Spot pools and evenly allocates your target Spot capacity across the number of Spot pools that you specify. Note that EC2 Fleet attempts to draw Spot Instances from the number of pools that you specify on a best effort basis. If a pool runs out of Spot capacity before fulfilling your target capacity, EC2 Fleet will continue to fulfill your request by drawing from the next cheapest pool. To ensure that your target capacity is met, you might receive Spot Instances from more than the number of pools that you specified. Similarly, if most of the pools have no Spot capacity, you might receive your full target capacity from fewer than the number of pools that you specified."];
      singleInstanceType: Boolean.t option
        [@ocaml.doc
          "Indicates that the fleet uses a single instance type to launch all Spot Instances in the fleet. Supported only for fleets of type instant."];
      singleAvailabilityZone: Boolean.t option
        [@ocaml.doc
          "Indicates that the fleet launches all Spot Instances into a single Availability Zone. Supported only for fleets of type instant."];
      minTargetCapacity: Integer.t option
        [@ocaml.doc
          "The minimum target capacity for Spot Instances in the fleet. If the minimum target capacity is not reached, the fleet launches no instances. Supported only for fleets of type instant. At least one of the following must be specified: SingleAvailabilityZone | SingleInstanceType"];
      maxTotalPrice: String_.t option
        [@ocaml.doc
          "The maximum amount per hour for Spot Instances that you're willing to pay."]}
    let make ?allocationStrategy =
      fun ?maintenanceStrategies ->
        fun ?instanceInterruptionBehavior ->
          fun ?instancePoolsToUseCount ->
            fun ?singleInstanceType ->
              fun ?singleAvailabilityZone ->
                fun ?minTargetCapacity ->
                  fun ?maxTotalPrice ->
                    fun () ->
                      {
                        allocationStrategy;
                        maintenanceStrategies;
                        instanceInterruptionBehavior;
                        instancePoolsToUseCount;
                        singleInstanceType;
                        singleAvailabilityZone;
                        minTargetCapacity;
                        maxTotalPrice
                      }
    let to_value x =
      structure_to_value
        [("AllocationStrategy",
           (Option.map x.allocationStrategy
              ~f:SpotAllocationStrategy.to_value));
        ("MaintenanceStrategies",
          (Option.map x.maintenanceStrategies
             ~f:FleetSpotMaintenanceStrategiesRequest.to_value));
        ("InstanceInterruptionBehavior",
          (Option.map x.instanceInterruptionBehavior
             ~f:SpotInstanceInterruptionBehavior.to_value));
        ("InstancePoolsToUseCount",
          (Option.map x.instancePoolsToUseCount ~f:Integer.to_value));
        ("SingleInstanceType",
          (Option.map x.singleInstanceType ~f:Boolean.to_value));
        ("SingleAvailabilityZone",
          (Option.map x.singleAvailabilityZone ~f:Boolean.to_value));
        ("MinTargetCapacity",
          (Option.map x.minTargetCapacity ~f:Integer.to_value));
        ("MaxTotalPrice", (Option.map x.maxTotalPrice ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxTotalPrice =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MaxTotalPrice") in
      let minTargetCapacity =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MinTargetCapacity") in
      let singleAvailabilityZone =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SingleAvailabilityZone") in
      let singleInstanceType =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SingleInstanceType") in
      let instancePoolsToUseCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "InstancePoolsToUseCount") in
      let instanceInterruptionBehavior =
        (Option.map ~f:SpotInstanceInterruptionBehavior.of_xml)
          (Xml.child xml_arg0 "InstanceInterruptionBehavior") in
      let maintenanceStrategies =
        (Option.map ~f:FleetSpotMaintenanceStrategiesRequest.of_xml)
          (Xml.child xml_arg0 "MaintenanceStrategies") in
      let allocationStrategy =
        (Option.map ~f:SpotAllocationStrategy.of_xml)
          (Xml.child xml_arg0 "AllocationStrategy") in
      make ?maxTotalPrice ?minTargetCapacity ?singleAvailabilityZone
        ?singleInstanceType ?instancePoolsToUseCount
        ?instanceInterruptionBehavior ?maintenanceStrategies
        ?allocationStrategy ()
    let of_json json =
      let maxTotalPrice = field_map json "MaxTotalPrice" String_.of_json in
      let minTargetCapacity =
        field_map json "MinTargetCapacity" Integer.of_json in
      let singleAvailabilityZone =
        field_map json "SingleAvailabilityZone" Boolean.of_json in
      let singleInstanceType =
        field_map json "SingleInstanceType" Boolean.of_json in
      let instancePoolsToUseCount =
        field_map json "InstancePoolsToUseCount" Integer.of_json in
      let instanceInterruptionBehavior =
        field_map json "InstanceInterruptionBehavior"
          SpotInstanceInterruptionBehavior.of_json in
      let maintenanceStrategies =
        field_map json "MaintenanceStrategies"
          FleetSpotMaintenanceStrategiesRequest.of_json in
      let allocationStrategy =
        field_map json "AllocationStrategy" SpotAllocationStrategy.of_json in
      make ?maxTotalPrice ?minTargetCapacity ?singleAvailabilityZone
        ?singleInstanceType ?instancePoolsToUseCount
        ?instanceInterruptionBehavior ?maintenanceStrategies
        ?allocationStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration of Spot Instances in an EC2 Fleet request."]
module NewDhcpConfigurationList =
  struct
    type nonrec t = NewDhcpConfiguration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NewDhcpConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NewDhcpConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"NewDhcpConfigurationList"
        ~of_json:NewDhcpConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClientVpnAuthenticationRequestList =
  struct
    type nonrec t = ClientVpnAuthenticationRequest.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ClientVpnAuthenticationRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClientVpnAuthenticationRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"ClientVpnAuthenticationRequestList"
        ~of_json:ClientVpnAuthenticationRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReservationFleetInstanceSpecificationList =
  struct
    type nonrec t = ReservationFleetInstanceSpecification.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReservationFleetInstanceSpecification.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ReservationFleetInstanceSpecification.of_xml)
    let of_json j =
      list_of_json ~kind:"ReservationFleetInstanceSpecificationList"
        ~of_json:ReservationFleetInstanceSpecification.of_json j
    let to_json v = composed_to_json to_value v
  end
module CancelledSpotInstanceRequestList =
  struct
    type nonrec t = CancelledSpotInstanceRequest.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CancelledSpotInstanceRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CancelledSpotInstanceRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"CancelledSpotInstanceRequestList"
        ~of_json:CancelledSpotInstanceRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module CancelSpotFleetRequestsErrorSet =
  struct
    type nonrec t = CancelSpotFleetRequestsErrorItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CancelSpotFleetRequestsErrorItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CancelSpotFleetRequestsErrorItem.of_xml)
    let of_json j =
      list_of_json ~kind:"CancelSpotFleetRequestsErrorSet"
        ~of_json:CancelSpotFleetRequestsErrorItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module CancelSpotFleetRequestsSuccessSet =
  struct
    type nonrec t = CancelSpotFleetRequestsSuccessItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CancelSpotFleetRequestsSuccessItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CancelSpotFleetRequestsSuccessItem.of_xml)
    let of_json j =
      list_of_json ~kind:"CancelSpotFleetRequestsSuccessSet"
        ~of_json:CancelSpotFleetRequestsSuccessItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImportTaskId =
  struct
    type nonrec t = string
    let context_ = "ImportTaskId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ImportTaskId" j
    let to_json = simple_to_json to_value
  end
module ExportVmTaskId =
  struct
    type nonrec t = string
    let context_ = "ExportVmTaskId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExportVmTaskId" j
    let to_json = simple_to_json to_value
  end
module CapacityReservationFleetCancellationStateSet =
  struct
    type nonrec t = CapacityReservationFleetCancellationState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CapacityReservationFleetCancellationState.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CapacityReservationFleetCancellationState.of_xml)
    let of_json j =
      list_of_json ~kind:"CapacityReservationFleetCancellationStateSet"
        ~of_json:CapacityReservationFleetCancellationState.of_json j
    let to_json v = composed_to_json to_value v
  end
module FailedCapacityReservationFleetCancellationResultSet =
  struct
    type nonrec t = FailedCapacityReservationFleetCancellationResult.t list
    let make i = i
    let to_value xs =
      (xs |>
         (List.map
            ~f:FailedCapacityReservationFleetCancellationResult.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:FailedCapacityReservationFleetCancellationResult.of_xml)
    let of_json j =
      list_of_json
        ~kind:"FailedCapacityReservationFleetCancellationResultSet"
        ~of_json:FailedCapacityReservationFleetCancellationResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceEventWindowAssociationRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdList.t option
        [@ocaml.doc
          "The IDs of the instances to associate with the event window. If the instance is on a Dedicated Host, you can't specify the Instance ID parameter; you must use the Dedicated Host ID parameter."];
      instanceTags: TagList.t option
        [@ocaml.doc
          "The instance tags to associate with the event window. Any instances associated with the tags will be associated with the event window."];
      dedicatedHostIds: DedicatedHostIdList.t option
        [@ocaml.doc
          "The IDs of the Dedicated Hosts to associate with the event window."]}
    let make ?instanceIds =
      fun ?instanceTags ->
        fun ?dedicatedHostIds ->
          fun () -> { instanceIds; instanceTags; dedicatedHostIds }
    let to_value x =
      structure_to_value
        [("InstanceId",
           (Option.map x.instanceIds ~f:InstanceIdList.to_value));
        ("InstanceTag", (Option.map x.instanceTags ~f:TagList.to_value));
        ("DedicatedHostId",
          (Option.map x.dedicatedHostIds ~f:DedicatedHostIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dedicatedHostIds =
        (Option.map ~f:DedicatedHostIdList.of_xml)
          (Xml.child xml_arg0 "DedicatedHostId") in
      let instanceTags =
        (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "InstanceTag") in
      let instanceIds =
        (Option.map ~f:InstanceIdList.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      make ?dedicatedHostIds ?instanceTags ?instanceIds ()
    let of_json json =
      let dedicatedHostIds =
        field_map json "DedicatedHostIds" DedicatedHostIdList.of_json in
      let instanceTags = field_map json "InstanceTags" TagList.of_json in
      let instanceIds = field_map json "InstanceIds" InstanceIdList.of_json in
      make ?dedicatedHostIds ?instanceTags ?instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One or more targets associated with the specified event window. Only one type of target (instance ID, instance tag, or Dedicated Host ID) can be associated with an event window."]
module DefaultingDhcpOptionsId =
  struct
    type nonrec t = string
    let context_ = "DefaultingDhcpOptionsId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DefaultingDhcpOptionsId" j
    let to_json = simple_to_json to_value
  end
module AssignedPrivateIpAddressList =
  struct
    type nonrec t = AssignedPrivateIpAddress.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssignedPrivateIpAddress.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssignedPrivateIpAddress.of_xml)
    let of_json j =
      list_of_json ~kind:"AssignedPrivateIpAddressList"
        ~of_json:AssignedPrivateIpAddress.of_json j
    let to_json v = composed_to_json to_value v
  end
module IpamPoolAllocationDisallowedCidrs =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"IpamPoolAllocationDisallowedCidrs"
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module WithdrawByoipCidrResult =
  struct
    type nonrec t =
      {
      byoipCidr: ByoipCidr.t option
        [@ocaml.doc "Information about the address pool."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_json json =
      let byoipCidr = field_map json "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops advertising an address range that is provisioned as an address pool. You can perform this operation at most once every 10 seconds, even if you specify different address ranges each time. It can take a few minutes before traffic to the specified addresses stops routing to Amazon Web Services because of BGP propagation delays."]
module WithdrawByoipCidrRequest =
  struct
    type nonrec t =
      {
      cidr: String_.t [@ocaml.doc "The address range, in CIDR notation."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "WithdrawByoipCidrRequest"
    let make ?dryRun = fun ~cidr -> fun () -> { dryRun; cidr }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?dryRun ~cidr ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      make ?dryRun ~cidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops advertising an address range that is provisioned as an address pool. You can perform this operation at most once every 10 seconds, even if you specify different address ranges each time. It can take a few minutes before traffic to the specified addresses stops routing to Amazon Web Services because of BGP propagation delays."]
module UpdateSecurityGroupRuleDescriptionsIngressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the description of an ingress (inbound) security group rule. You can replace an existing description, or add a description to a rule that did not have one previously. You can remove a description for a security group rule by omitting the description parameter in the request."]
module UpdateSecurityGroupRuleDescriptionsIngressRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupId: SecurityGroupId.t option
        [@ocaml.doc
          "The ID of the security group. You must specify either the security group ID or the security group name in the request. For security groups in a nondefault VPC, you must specify the security group ID."];
      groupName: SecurityGroupName.t option
        [@ocaml.doc
          "\\[EC2-Classic, default VPC\\] The name of the security group. You must specify either the security group ID or the security group name in the request."];
      ipPermissions: IpPermissionList.t option
        [@ocaml.doc
          "The IP permissions for the security group rule. You must specify either IP permissions or a description."];
      securityGroupRuleDescriptions:
        SecurityGroupRuleDescriptionList.t option
        [@ocaml.doc
          "\\[VPC only\\] The description for the ingress security group rules. You must specify either a description or IP permissions."]}
    let make ?dryRun =
      fun ?groupId ->
        fun ?groupName ->
          fun ?ipPermissions ->
            fun ?securityGroupRuleDescriptions ->
              fun () ->
                {
                  dryRun;
                  groupId;
                  groupName;
                  ipPermissions;
                  securityGroupRuleDescriptions
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("IpPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("SecurityGroupRuleDescription",
          (Option.map x.securityGroupRuleDescriptions
             ~f:SecurityGroupRuleDescriptionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRuleDescriptions =
        (Option.map ~f:SecurityGroupRuleDescriptionList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupRuleDescription") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "IpPermissions") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?securityGroupRuleDescriptions ?ipPermissions ?groupName ?groupId
        ?dryRun ()
    let of_json json =
      let securityGroupRuleDescriptions =
        field_map json "SecurityGroupRuleDescriptions"
          SecurityGroupRuleDescriptionList.of_json in
      let ipPermissions =
        field_map json "IpPermissions" IpPermissionList.of_json in
      let groupName = field_map json "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json "GroupId" SecurityGroupId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?securityGroupRuleDescriptions ?ipPermissions ?groupName ?groupId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the description of an ingress (inbound) security group rule. You can replace an existing description, or add a description to a rule that did not have one previously. You can remove a description for a security group rule by omitting the description parameter in the request."]
module UpdateSecurityGroupRuleDescriptionsEgressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "\\[VPC only\\] Updates the description of an egress (outbound) security group rule. You can replace an existing description, or add a description to a rule that did not have one previously. You can remove a description for a security group rule by omitting the description parameter in the request."]
module UpdateSecurityGroupRuleDescriptionsEgressRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupId: SecurityGroupId.t option
        [@ocaml.doc
          "The ID of the security group. You must specify either the security group ID or the security group name in the request. For security groups in a nondefault VPC, you must specify the security group ID."];
      groupName: SecurityGroupName.t option
        [@ocaml.doc
          "\\[Default VPC\\] The name of the security group. You must specify either the security group ID or the security group name in the request."];
      ipPermissions: IpPermissionList.t option
        [@ocaml.doc
          "The IP permissions for the security group rule. You must specify either the IP permissions or the description."];
      securityGroupRuleDescriptions:
        SecurityGroupRuleDescriptionList.t option
        [@ocaml.doc
          "The description for the egress security group rules. You must specify either the description or the IP permissions."]}
    let make ?dryRun =
      fun ?groupId ->
        fun ?groupName ->
          fun ?ipPermissions ->
            fun ?securityGroupRuleDescriptions ->
              fun () ->
                {
                  dryRun;
                  groupId;
                  groupName;
                  ipPermissions;
                  securityGroupRuleDescriptions
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("IpPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("SecurityGroupRuleDescription",
          (Option.map x.securityGroupRuleDescriptions
             ~f:SecurityGroupRuleDescriptionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRuleDescriptions =
        (Option.map ~f:SecurityGroupRuleDescriptionList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupRuleDescription") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "IpPermissions") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?securityGroupRuleDescriptions ?ipPermissions ?groupName ?groupId
        ?dryRun ()
    let of_json json =
      let securityGroupRuleDescriptions =
        field_map json "SecurityGroupRuleDescriptions"
          SecurityGroupRuleDescriptionList.of_json in
      let ipPermissions =
        field_map json "IpPermissions" IpPermissionList.of_json in
      let groupName = field_map json "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json "GroupId" SecurityGroupId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?securityGroupRuleDescriptions ?ipPermissions ?groupName ?groupId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "\\[VPC only\\] Updates the description of an egress (outbound) security group rule. You can replace an existing description, or add a description to a rule that did not have one previously. You can remove a description for a security group rule by omitting the description parameter in the request."]
module UnmonitorInstancesResult =
  struct
    type nonrec t =
      {
      instanceMonitorings: InstanceMonitoringList.t option
        [@ocaml.doc "The monitoring information."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceMonitorings = fun () -> { instanceMonitorings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.instanceMonitorings
              ~f:InstanceMonitoringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceMonitorings =
        (Option.map ~f:InstanceMonitoringList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?instanceMonitorings ()
    let of_json json =
      let instanceMonitorings =
        field_map json "InstanceMonitorings" InstanceMonitoringList.of_json in
      make ?instanceMonitorings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables detailed monitoring for a running instance. For more information, see Monitoring your instances and volumes in the Amazon EC2 User Guide."]
module UnmonitorInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t
        [@ocaml.doc "The IDs of the instances."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "UnmonitorInstancesRequest"
    let make ?dryRun = fun ~instanceIds -> fun () -> { dryRun; instanceIds }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceIdStringList.to_value x.instanceIds)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceIds =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~instanceIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceIds =
        field_map_exn json "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables detailed monitoring for a running instance. For more information, see Monitoring your instances and volumes in the Amazon EC2 User Guide."]
module UnassignPrivateIpAddressesRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      privateIpAddresses: PrivateIpAddressStringList.t option
        [@ocaml.doc
          "The secondary private IP addresses to unassign from the network interface. You can specify this option multiple times to unassign more than one IP address."];
      ipv4Prefixes: IpPrefixList.t option
        [@ocaml.doc
          "The IPv4 prefixes to unassign from the network interface."]}
    let context_ = "UnassignPrivateIpAddressesRequest"
    let make ?privateIpAddresses =
      fun ?ipv4Prefixes ->
        fun ~networkInterfaceId ->
          fun () -> { privateIpAddresses; ipv4Prefixes; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("networkInterfaceId",
           (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("privateIpAddress",
          (Option.map x.privateIpAddresses
             ~f:PrivateIpAddressStringList.to_value));
        ("Ipv4Prefix", (Option.map x.ipv4Prefixes ~f:IpPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv4Prefixes =
        (Option.map ~f:IpPrefixList.of_xml) (Xml.child xml_arg0 "Ipv4Prefix") in
      let privateIpAddresses =
        (Option.map ~f:PrivateIpAddressStringList.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      make ?ipv4Prefixes ?privateIpAddresses ~networkInterfaceId ()
    let of_json json =
      let ipv4Prefixes = field_map json "Ipv4Prefixes" IpPrefixList.of_json in
      let privateIpAddresses =
        field_map json "PrivateIpAddresses"
          PrivateIpAddressStringList.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?ipv4Prefixes ?privateIpAddresses ~networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for UnassignPrivateIpAddresses."]
module UnassignIpv6AddressesResult =
  struct
    type nonrec t =
      {
      networkInterfaceId: String_.t option
        [@ocaml.doc "The ID of the network interface."];
      unassignedIpv6Addresses: Ipv6AddressList.t option
        [@ocaml.doc
          "The IPv6 addresses that have been unassigned from the network interface."];
      unassignedIpv6Prefixes: IpPrefixList.t option
        [@ocaml.doc
          "The IPv4 prefixes that have been unassigned from the network interface."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfaceId =
      fun ?unassignedIpv6Addresses ->
        fun ?unassignedIpv6Prefixes ->
          fun () ->
            {
              networkInterfaceId;
              unassignedIpv6Addresses;
              unassignedIpv6Prefixes
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfaceId",
           (Option.map x.networkInterfaceId ~f:String_.to_value));
        ("unassignedIpv6Addresses",
          (Option.map x.unassignedIpv6Addresses ~f:Ipv6AddressList.to_value));
        ("unassignedIpv6PrefixSet",
          (Option.map x.unassignedIpv6Prefixes ~f:IpPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unassignedIpv6Prefixes =
        (Option.map ~f:IpPrefixList.of_xml)
          (Xml.child xml_arg0 "unassignedIpv6PrefixSet") in
      let unassignedIpv6Addresses =
        (Option.map ~f:Ipv6AddressList.of_xml)
          (Xml.child xml_arg0 "unassignedIpv6Addresses") in
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      make ?unassignedIpv6Prefixes ?unassignedIpv6Addresses
        ?networkInterfaceId ()
    let of_json json =
      let unassignedIpv6Prefixes =
        field_map json "UnassignedIpv6Prefixes" IpPrefixList.of_json in
      let unassignedIpv6Addresses =
        field_map json "UnassignedIpv6Addresses" Ipv6AddressList.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" String_.of_json in
      make ?unassignedIpv6Prefixes ?unassignedIpv6Addresses
        ?networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Unassigns one or more IPv6 addresses IPv4 Prefix Delegation prefixes from a network interface."]
module UnassignIpv6AddressesRequest =
  struct
    type nonrec t =
      {
      ipv6Addresses: Ipv6AddressList.t option
        [@ocaml.doc
          "The IPv6 addresses to unassign from the network interface."];
      ipv6Prefixes: IpPrefixList.t option
        [@ocaml.doc
          "One or more IPv6 prefixes to unassign from the network interface."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."]}
    let context_ = "UnassignIpv6AddressesRequest"
    let make ?ipv6Addresses =
      fun ?ipv6Prefixes ->
        fun ~networkInterfaceId ->
          fun () -> { ipv6Addresses; ipv6Prefixes; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("ipv6Addresses",
           (Option.map x.ipv6Addresses ~f:Ipv6AddressList.to_value));
        ("Ipv6Prefix", (Option.map x.ipv6Prefixes ~f:IpPrefixList.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let ipv6Prefixes =
        (Option.map ~f:IpPrefixList.of_xml) (Xml.child xml_arg0 "Ipv6Prefix") in
      let ipv6Addresses =
        (Option.map ~f:Ipv6AddressList.of_xml)
          (Xml.child xml_arg0 "ipv6Addresses") in
      make ~networkInterfaceId ?ipv6Prefixes ?ipv6Addresses ()
    let of_json json =
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let ipv6Prefixes = field_map json "Ipv6Prefixes" IpPrefixList.of_json in
      let ipv6Addresses =
        field_map json "Ipv6Addresses" Ipv6AddressList.of_json in
      make ~networkInterfaceId ?ipv6Prefixes ?ipv6Addresses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Unassigns one or more IPv6 addresses IPv4 Prefix Delegation prefixes from a network interface."]
module TerminateInstancesResult =
  struct
    type nonrec t =
      {
      terminatingInstances: InstanceStateChangeList.t option
        [@ocaml.doc "Information about the terminated instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?terminatingInstances = fun () -> { terminatingInstances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.terminatingInstances
              ~f:InstanceStateChangeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminatingInstances =
        (Option.map ~f:InstanceStateChangeList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?terminatingInstances ()
    let of_json json =
      let terminatingInstances =
        field_map json "TerminatingInstances" InstanceStateChangeList.of_json in
      make ?terminatingInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Shuts down the specified instances. This operation is idempotent; if you terminate an instance more than once, each call succeeds. If you specify multiple instances and the request fails (for example, because of a single incorrect instance ID), none of the instances are terminated. If you terminate multiple instances across multiple Availability Zones, and one or more of the specified instances are enabled for termination protection, the request fails with the following results: The specified instances that are in the same Availability Zone as the protected instance are not terminated. The specified instances that are in different Availability Zones, where no other specified instances are protected, are successfully terminated. For example, say you have the following instances: Instance A: us-east-1a; Not protected Instance B: us-east-1a; Not protected Instance C: us-east-1b; Protected Instance D: us-east-1b; not protected If you attempt to terminate all of these instances in the same request, the request reports failure with the following results: Instance A and Instance B are successfully terminated because none of the specified instances in us-east-1a are enabled for termination protection. Instance C and Instance D fail to terminate because at least one of the specified instances in us-east-1b (Instance C) is enabled for termination protection. Terminated instances remain visible after termination (for approximately one hour). By default, Amazon EC2 deletes all EBS volumes that were attached when the instance launched. Volumes attached after instance launch continue running. You can stop, start, and terminate EBS-backed instances. You can only terminate instance store-backed instances. What happens to an instance differs if you stop it or terminate it. For example, when you stop an instance, the root device and any other devices attached to the instance persist. When you terminate an instance, any attached EBS volumes with the DeleteOnTermination block device mapping parameter set to true are automatically deleted. For more information about the differences between stopping and terminating instances, see Instance lifecycle in the Amazon EC2 User Guide. For more information about troubleshooting, see Troubleshooting terminating your instance in the Amazon EC2 User Guide."]
module TerminateInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t
        [@ocaml.doc
          "The IDs of the instances. Constraints: Up to 1000 instance IDs. We recommend breaking up this request into smaller batches."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "TerminateInstancesRequest"
    let make ?dryRun = fun ~instanceIds -> fun () -> { dryRun; instanceIds }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceIdStringList.to_value x.instanceIds)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceIds =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~instanceIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceIds =
        field_map_exn json "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Shuts down the specified instances. This operation is idempotent; if you terminate an instance more than once, each call succeeds. If you specify multiple instances and the request fails (for example, because of a single incorrect instance ID), none of the instances are terminated. If you terminate multiple instances across multiple Availability Zones, and one or more of the specified instances are enabled for termination protection, the request fails with the following results: The specified instances that are in the same Availability Zone as the protected instance are not terminated. The specified instances that are in different Availability Zones, where no other specified instances are protected, are successfully terminated. For example, say you have the following instances: Instance A: us-east-1a; Not protected Instance B: us-east-1a; Not protected Instance C: us-east-1b; Protected Instance D: us-east-1b; not protected If you attempt to terminate all of these instances in the same request, the request reports failure with the following results: Instance A and Instance B are successfully terminated because none of the specified instances in us-east-1a are enabled for termination protection. Instance C and Instance D fail to terminate because at least one of the specified instances in us-east-1b (Instance C) is enabled for termination protection. Terminated instances remain visible after termination (for approximately one hour). By default, Amazon EC2 deletes all EBS volumes that were attached when the instance launched. Volumes attached after instance launch continue running. You can stop, start, and terminate EBS-backed instances. You can only terminate instance store-backed instances. What happens to an instance differs if you stop it or terminate it. For example, when you stop an instance, the root device and any other devices attached to the instance persist. When you terminate an instance, any attached EBS volumes with the DeleteOnTermination block device mapping parameter set to true are automatically deleted. For more information about the differences between stopping and terminating instances, see Instance lifecycle in the Amazon EC2 User Guide. For more information about troubleshooting, see Troubleshooting terminating your instance in the Amazon EC2 User Guide."]
module TerminateClientVpnConnectionsResult =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: String_.t option
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      username: String_.t option
        [@ocaml.doc
          "The user who established the terminated client connections."];
      connectionStatuses: TerminateConnectionStatusSet.t option
        [@ocaml.doc "The current state of the client connections."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnEndpointId =
      fun ?username ->
        fun ?connectionStatuses ->
          fun () -> { clientVpnEndpointId; username; connectionStatuses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnEndpointId",
           (Option.map x.clientVpnEndpointId ~f:String_.to_value));
        ("username", (Option.map x.username ~f:String_.to_value));
        ("connectionStatuses",
          (Option.map x.connectionStatuses
             ~f:TerminateConnectionStatusSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectionStatuses =
        (Option.map ~f:TerminateConnectionStatusSet.of_xml)
          (Xml.child xml_arg0 "connectionStatuses") in
      let username =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "username") in
      let clientVpnEndpointId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "clientVpnEndpointId") in
      make ?connectionStatuses ?username ?clientVpnEndpointId ()
    let of_json json =
      let connectionStatuses =
        field_map json "ConnectionStatuses"
          TerminateConnectionStatusSet.of_json in
      let username = field_map json "Username" String_.of_json in
      let clientVpnEndpointId =
        field_map json "ClientVpnEndpointId" String_.of_json in
      make ?connectionStatuses ?username ?clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Terminates active Client VPN endpoint connections. This action can be used to terminate a specific client connection, or up to five connections established by a specific user."]
module TerminateClientVpnConnectionsRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc
          "The ID of the Client VPN endpoint to which the client is connected."];
      connectionId: VpnConnectionId.t option
        [@ocaml.doc "The ID of the client connection to be terminated."];
      username: String_.t option
        [@ocaml.doc
          "The name of the user who initiated the connection. Use this option to terminate all active connections for the specified user. This option can only be used if the user has established up to five connections."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "TerminateClientVpnConnectionsRequest"
    let make ?connectionId =
      fun ?username ->
        fun ?dryRun ->
          fun ~clientVpnEndpointId ->
            fun () -> { connectionId; username; dryRun; clientVpnEndpointId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("ConnectionId",
          (Option.map x.connectionId ~f:VpnConnectionId.to_value));
        ("Username", (Option.map x.username ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let username =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Username") in
      let connectionId =
        (Option.map ~f:VpnConnectionId.of_xml)
          (Xml.child xml_arg0 "ConnectionId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?username ?connectionId ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let username = field_map json "Username" String_.of_json in
      let connectionId =
        field_map json "ConnectionId" VpnConnectionId.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?username ?connectionId ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Terminates active Client VPN endpoint connections. This action can be used to terminate a specific client connection, or up to five connections established by a specific user."]
module StopInstancesResult =
  struct
    type nonrec t =
      {
      stoppingInstances: InstanceStateChangeList.t option
        [@ocaml.doc "Information about the stopped instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?stoppingInstances = fun () -> { stoppingInstances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.stoppingInstances
              ~f:InstanceStateChangeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stoppingInstances =
        (Option.map ~f:InstanceStateChangeList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?stoppingInstances ()
    let of_json json =
      let stoppingInstances =
        field_map json "StoppingInstances" InstanceStateChangeList.of_json in
      make ?stoppingInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops an Amazon EBS-backed instance. You can use the Stop action to hibernate an instance if the instance is enabled for hibernation and it meets the hibernation prerequisites. For more information, see Hibernate your instance in the Amazon EC2 User Guide. We don't charge usage for a stopped instance, or data transfer fees; however, your root partition Amazon EBS volume remains and continues to persist your data, and you are charged for Amazon EBS volume usage. Every time you start your instance, Amazon EC2 charges a one-minute minimum for instance usage, and thereafter charges per second for instance usage. You can't stop or hibernate instance store-backed instances. You can't use the Stop action to hibernate Spot Instances, but you can specify that Amazon EC2 should hibernate Spot Instances when they are interrupted. For more information, see Hibernating interrupted Spot Instances in the Amazon EC2 User Guide. When you stop or hibernate an instance, we shut it down. You can restart your instance at any time. Before stopping or hibernating an instance, make sure it is in a state from which it can be restarted. Stopping an instance does not preserve data stored in RAM, but hibernating an instance does preserve data stored in RAM. If an instance cannot hibernate successfully, a normal shutdown occurs. Stopping and hibernating an instance is different to rebooting or terminating it. For example, when you stop or hibernate an instance, the root device and any other devices attached to the instance persist. When you terminate an instance, the root device and any other devices attached during the instance launch are automatically deleted. For more information about the differences between rebooting, stopping, hibernating, and terminating instances, see Instance lifecycle in the Amazon EC2 User Guide. When you stop an instance, we attempt to shut it down forcibly after a short while. If your instance appears stuck in the stopping state after a period of time, there may be an issue with the underlying host computer. For more information, see Troubleshoot stopping your instance in the Amazon EC2 User Guide."]
module StopInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t
        [@ocaml.doc "The IDs of the instances."];
      hibernate: Boolean.t option
        [@ocaml.doc
          "Hibernates the instance if the instance was enabled for hibernation at launch. If the instance cannot hibernate successfully, a normal shutdown occurs. For more information, see Hibernate your instance in the Amazon EC2 User Guide. Default: false"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      force: Boolean.t option
        [@ocaml.doc
          "Forces the instances to stop. The instances do not have an opportunity to flush file system caches or file system metadata. If you use this option, you must perform file system check and repair procedures. This option is not recommended for Windows instances. Default: false"]}
    let context_ = "StopInstancesRequest"
    let make ?hibernate =
      fun ?dryRun ->
        fun ?force ->
          fun ~instanceIds ->
            fun () -> { hibernate; dryRun; force; instanceIds }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceIdStringList.to_value x.instanceIds)));
        ("Hibernate", (Option.map x.hibernate ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("force", (Option.map x.force ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let force = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "force") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let hibernate =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Hibernate") in
      let instanceIds =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?force ?dryRun ?hibernate ~instanceIds ()
    let of_json json =
      let force = field_map json "Force" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let hibernate = field_map json "Hibernate" Boolean.of_json in
      let instanceIds =
        field_map_exn json "InstanceIds" InstanceIdStringList.of_json in
      make ?force ?dryRun ?hibernate ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops an Amazon EBS-backed instance. You can use the Stop action to hibernate an instance if the instance is enabled for hibernation and it meets the hibernation prerequisites. For more information, see Hibernate your instance in the Amazon EC2 User Guide. We don't charge usage for a stopped instance, or data transfer fees; however, your root partition Amazon EBS volume remains and continues to persist your data, and you are charged for Amazon EBS volume usage. Every time you start your instance, Amazon EC2 charges a one-minute minimum for instance usage, and thereafter charges per second for instance usage. You can't stop or hibernate instance store-backed instances. You can't use the Stop action to hibernate Spot Instances, but you can specify that Amazon EC2 should hibernate Spot Instances when they are interrupted. For more information, see Hibernating interrupted Spot Instances in the Amazon EC2 User Guide. When you stop or hibernate an instance, we shut it down. You can restart your instance at any time. Before stopping or hibernating an instance, make sure it is in a state from which it can be restarted. Stopping an instance does not preserve data stored in RAM, but hibernating an instance does preserve data stored in RAM. If an instance cannot hibernate successfully, a normal shutdown occurs. Stopping and hibernating an instance is different to rebooting or terminating it. For example, when you stop or hibernate an instance, the root device and any other devices attached to the instance persist. When you terminate an instance, the root device and any other devices attached during the instance launch are automatically deleted. For more information about the differences between rebooting, stopping, hibernating, and terminating instances, see Instance lifecycle in the Amazon EC2 User Guide. When you stop an instance, we attempt to shut it down forcibly after a short while. If your instance appears stuck in the stopping state after a period of time, there may be an issue with the underlying host computer. For more information, see Troubleshoot stopping your instance in the Amazon EC2 User Guide."]
module StartVpcEndpointServicePrivateDnsVerificationResult =
  struct
    type nonrec t =
      {
      returnValue: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?returnValue ()
    let of_json json =
      let returnValue = field_map json "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initiates the verification process to prove that the service provider owns the private DNS name domain for the endpoint service. The service provider must successfully perform the verification before the consumer can use the name to access the service. Before the service provider runs this command, they must add a record to the DNS server. For more information, see Adding a TXT Record to Your Domain's DNS Server in the Amazon VPC User Guide."]
module StartVpcEndpointServicePrivateDnsVerificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t
        [@ocaml.doc "The ID of the endpoint service."]}
    let context_ = "StartVpcEndpointServicePrivateDnsVerificationRequest"
    let make ?dryRun = fun ~serviceId -> fun () -> { dryRun; serviceId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~serviceId ?dryRun ()
    let of_json json =
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initiates the verification process to prove that the service provider owns the private DNS name domain for the endpoint service. The service provider must successfully perform the verification before the consumer can use the name to access the service. Before the service provider runs this command, they must add a record to the DNS server. For more information, see Adding a TXT Record to Your Domain's DNS Server in the Amazon VPC User Guide."]
module StartNetworkInsightsAnalysisResult =
  struct
    type nonrec t =
      {
      networkInsightsAnalysis: NetworkInsightsAnalysis.t option
        [@ocaml.doc "Information about the network insights analysis."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAnalysis = fun () -> { networkInsightsAnalysis }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAnalysis",
           (Option.map x.networkInsightsAnalysis
              ~f:NetworkInsightsAnalysis.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAnalysis =
        (Option.map ~f:NetworkInsightsAnalysis.of_xml)
          (Xml.child xml_arg0 "networkInsightsAnalysis") in
      make ?networkInsightsAnalysis ()
    let of_json json =
      let networkInsightsAnalysis =
        field_map json "NetworkInsightsAnalysis"
          NetworkInsightsAnalysis.of_json in
      make ?networkInsightsAnalysis ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts analyzing the specified path. If the path is reachable, the operation returns the shortest feasible path."]
module StartNetworkInsightsAnalysisRequest =
  struct
    type nonrec t =
      {
      networkInsightsPathId: NetworkInsightsPathId.t
        [@ocaml.doc "The ID of the path."];
      filterInArns: ArnList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARN) of the resources that the path must traverse."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply."];
      clientToken: String_.t
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    let context_ = "StartNetworkInsightsAnalysisRequest"
    let make ?filterInArns =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~networkInsightsPathId ->
            fun ~clientToken ->
              fun () ->
                {
                  filterInArns;
                  dryRun;
                  tagSpecifications;
                  networkInsightsPathId;
                  clientToken
                }
    let to_value x =
      structure_to_value
        [("NetworkInsightsPathId",
           (Some (NetworkInsightsPathId.to_value x.networkInsightsPathId)));
        ("FilterInArn", (Option.map x.filterInArns ~f:ArnList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Some (String_.to_value x.clientToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let filterInArns =
        (Option.map ~f:ArnList.of_xml) (Xml.child xml_arg0 "FilterInArn") in
      let networkInsightsPathId =
        NetworkInsightsPathId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInsightsPathId") in
      make ~clientToken ?tagSpecifications ?dryRun ?filterInArns
        ~networkInsightsPathId ()
    let of_json json =
      let clientToken = field_map_exn json "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let filterInArns = field_map json "FilterInArns" ArnList.of_json in
      let networkInsightsPathId =
        field_map_exn json "NetworkInsightsPathId"
          NetworkInsightsPathId.of_json in
      make ~clientToken ?tagSpecifications ?dryRun ?filterInArns
        ~networkInsightsPathId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts analyzing the specified path. If the path is reachable, the operation returns the shortest feasible path."]
module StartNetworkInsightsAccessScopeAnalysisResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysis:
        NetworkInsightsAccessScopeAnalysis.t option
        [@ocaml.doc "The Network Access Scope analysis."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeAnalysis =
      fun () -> { networkInsightsAccessScopeAnalysis }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeAnalysis",
           (Option.map x.networkInsightsAccessScopeAnalysis
              ~f:NetworkInsightsAccessScopeAnalysis.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeAnalysis =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysis.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeAnalysis") in
      make ?networkInsightsAccessScopeAnalysis ()
    let of_json json =
      let networkInsightsAccessScopeAnalysis =
        field_map json "NetworkInsightsAccessScopeAnalysis"
          NetworkInsightsAccessScopeAnalysis.of_json in
      make ?networkInsightsAccessScopeAnalysis ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts analyzing the specified Network Access Scope."]
module StartNetworkInsightsAccessScopeAnalysisRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t
        [@ocaml.doc "The ID of the Network Access Scope."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply."];
      clientToken: String_.t
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    let context_ = "StartNetworkInsightsAccessScopeAnalysisRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ~networkInsightsAccessScopeId ->
          fun ~clientToken ->
            fun () ->
              {
                dryRun;
                tagSpecifications;
                networkInsightsAccessScopeId;
                clientToken
              }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeId",
           (Some
              (NetworkInsightsAccessScopeId.to_value
                 x.networkInsightsAccessScopeId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Some (String_.to_value x.clientToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInsightsAccessScopeId =
        NetworkInsightsAccessScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeId") in
      make ~clientToken ?tagSpecifications ?dryRun
        ~networkInsightsAccessScopeId ()
    let of_json json =
      let clientToken = field_map_exn json "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInsightsAccessScopeId =
        field_map_exn json "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      make ~clientToken ?tagSpecifications ?dryRun
        ~networkInsightsAccessScopeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts analyzing the specified Network Access Scope."]
module StartInstancesResult =
  struct
    type nonrec t =
      {
      startingInstances: InstanceStateChangeList.t option
        [@ocaml.doc "Information about the started instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?startingInstances = fun () -> { startingInstances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.startingInstances
              ~f:InstanceStateChangeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startingInstances =
        (Option.map ~f:InstanceStateChangeList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?startingInstances ()
    let of_json json =
      let startingInstances =
        field_map json "StartingInstances" InstanceStateChangeList.of_json in
      make ?startingInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an Amazon EBS-backed instance that you've previously stopped. Instances that use Amazon EBS volumes as their root devices can be quickly stopped and started. When an instance is stopped, the compute resources are released and you are not billed for instance usage. However, your root partition Amazon EBS volume remains and continues to persist your data, and you are charged for Amazon EBS volume usage. You can restart your instance at any time. Every time you start your instance, Amazon EC2 charges a one-minute minimum for instance usage, and thereafter charges per second for instance usage. Before stopping an instance, make sure it is in a state from which it can be restarted. Stopping an instance does not preserve data stored in RAM. Performing this operation on an instance that uses an instance store as its root device returns an error. If you attempt to start a T3 instance with host tenancy and the unlimted CPU credit option, the request fails. The unlimited CPU credit option is not supported on Dedicated Hosts. Before you start the instance, either change its CPU credit option to standard, or change its tenancy to default or dedicated. For more information, see Stop and start your instance in the Amazon EC2 User Guide."]
module StartInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t
        [@ocaml.doc "The IDs of the instances."];
      additionalInfo: String_.t option [@ocaml.doc "Reserved."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "StartInstancesRequest"
    let make ?additionalInfo =
      fun ?dryRun ->
        fun ~instanceIds -> fun () -> { additionalInfo; dryRun; instanceIds }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceIdStringList.to_value x.instanceIds)));
        ("additionalInfo", (Option.map x.additionalInfo ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let additionalInfo =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "additionalInfo") in
      let instanceIds =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ?additionalInfo ~instanceIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let additionalInfo = field_map json "AdditionalInfo" String_.of_json in
      let instanceIds =
        field_map_exn json "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ?additionalInfo ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an Amazon EBS-backed instance that you've previously stopped. Instances that use Amazon EBS volumes as their root devices can be quickly stopped and started. When an instance is stopped, the compute resources are released and you are not billed for instance usage. However, your root partition Amazon EBS volume remains and continues to persist your data, and you are charged for Amazon EBS volume usage. You can restart your instance at any time. Every time you start your instance, Amazon EC2 charges a one-minute minimum for instance usage, and thereafter charges per second for instance usage. Before stopping an instance, make sure it is in a state from which it can be restarted. Stopping an instance does not preserve data stored in RAM. Performing this operation on an instance that uses an instance store as its root device returns an error. If you attempt to start a T3 instance with host tenancy and the unlimted CPU credit option, the request fails. The unlimited CPU credit option is not supported on Dedicated Hosts. Before you start the instance, either change its CPU credit option to standard, or change its tenancy to default or dedicated. For more information, see Stop and start your instance in the Amazon EC2 User Guide."]
module SendDiagnosticInterruptRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "SendDiagnosticInterruptRequest"
    let make ?dryRun = fun ~instanceId -> fun () -> { dryRun; instanceId }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~instanceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?dryRun ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a diagnostic interrupt to the specified Amazon EC2 instance to trigger a kernel panic (on Linux instances), or a blue screen/stop error (on Windows instances). For instances based on Intel and AMD processors, the interrupt is received as a non-maskable interrupt (NMI). In general, the operating system crashes and reboots when a kernel panic or stop error is triggered. The operating system can also be configured to perform diagnostic tasks, such as generating a memory dump file, loading a secondary kernel, or obtaining a call trace. Before sending a diagnostic interrupt to your instance, ensure that its operating system is configured to perform the required diagnostic tasks. For more information about configuring your operating system to generate a crash dump when a kernel panic or stop error occurs, see Send a diagnostic interrupt (for advanced users) (Linux instances) or Send a diagnostic interrupt (for advanced users) (Windows instances)."]
module SearchTransitGatewayRoutesResult =
  struct
    type nonrec t =
      {
      routes: TransitGatewayRouteList.t option
        [@ocaml.doc "Information about the routes."];
      additionalRoutesAvailable: Boolean.t option
        [@ocaml.doc
          "Indicates whether there are additional routes available."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routes =
      fun ?additionalRoutesAvailable ->
        fun () -> { routes; additionalRoutesAvailable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeSet",
           (Option.map x.routes ~f:TransitGatewayRouteList.to_value));
        ("additionalRoutesAvailable",
          (Option.map x.additionalRoutesAvailable ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalRoutesAvailable =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "additionalRoutesAvailable") in
      let routes =
        (Option.map ~f:TransitGatewayRouteList.of_xml)
          (Xml.child xml_arg0 "routeSet") in
      make ?additionalRoutesAvailable ?routes ()
    let of_json json =
      let additionalRoutesAvailable =
        field_map json "AdditionalRoutesAvailable" Boolean.of_json in
      let routes = field_map json "Routes" TransitGatewayRouteList.of_json in
      make ?additionalRoutesAvailable ?routes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches for routes in the specified transit gateway route table."]
module SearchTransitGatewayRoutesRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      filters: FilterList.t
        [@ocaml.doc
          "One or more filters. The possible values are: attachment.transit-gateway-attachment-id- The id of the transit gateway attachment. attachment.resource-id - The resource id of the transit gateway attachment. attachment.resource-type - The attachment resource type. Valid values are vpc | vpn | direct-connect-gateway | peering | connect. prefix-list-id - The ID of the prefix list. route-search.exact-match - The exact match of the specified filter. route-search.longest-prefix-match - The longest prefix that matches the route. route-search.subnet-of-match - The routes with a subnet that match the specified CIDR filter. route-search.supernet-of-match - The routes with a CIDR that encompass the CIDR filter. For example, if you have 10.0.1.0/29 and 10.0.1.0/31 routes in your route table and you specify supernet-of-match as 10.0.1.0/30, then the result returns 10.0.1.0/29. state - The state of the route (active | blackhole). type - The type of route (propagated | static)."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc "The maximum number of routes to return."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "SearchTransitGatewayRoutesRequest"
    let make ?maxResults =
      fun ?dryRun ->
        fun ~transitGatewayRouteTableId ->
          fun ~filters ->
            fun () ->
              { maxResults; dryRun; transitGatewayRouteTableId; filters }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("Filter", (Some (FilterList.to_value x.filters)));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        FilterList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Filter") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?maxResults ~filters ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map_exn json "Filters" FilterList.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?maxResults ~filters ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches for routes in the specified transit gateway route table."]
module SearchTransitGatewayMulticastGroupsResult =
  struct
    type nonrec t =
      {
      multicastGroups: TransitGatewayMulticastGroupList.t option
        [@ocaml.doc "Information about the transit gateway multicast group."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?multicastGroups =
      fun ?nextToken -> fun () -> { multicastGroups; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("multicastGroups",
           (Option.map x.multicastGroups
              ~f:TransitGatewayMulticastGroupList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let multicastGroups =
        (Option.map ~f:TransitGatewayMulticastGroupList.of_xml)
          (Xml.child xml_arg0 "multicastGroups") in
      make ?nextToken ?multicastGroups ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let multicastGroups =
        field_map json "MulticastGroups"
          TransitGatewayMulticastGroupList.of_json in
      make ?nextToken ?multicastGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches one or more transit gateway multicast groups and returns the group membership information."]
module SearchTransitGatewayMulticastGroupsRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: group-ip-address - The IP address of the transit gateway multicast group. is-group-member - The resource is a group member. Valid values are true | false. is-group-source - The resource is a group source. Valid values are true | false. member-type - The member type. Valid values are igmp | static. resource-id - The ID of the resource. resource-type - The type of resource. Valid values are vpc | vpn | direct-connect-gateway | tgw-peering. source-type - The source type. Valid values are igmp | static. subnet-id - The ID of the subnet. transit-gateway-attachment-id - The id of the transit gateway attachment."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayMulticastDomainId;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches one or more transit gateway multicast groups and returns the group membership information."]
module SearchLocalGatewayRoutesResult =
  struct
    type nonrec t =
      {
      routes: LocalGatewayRouteList.t option
        [@ocaml.doc "Information about the routes."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routes = fun ?nextToken -> fun () -> { routes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeSet",
           (Option.map x.routes ~f:LocalGatewayRouteList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let routes =
        (Option.map ~f:LocalGatewayRouteList.of_xml)
          (Xml.child xml_arg0 "routeSet") in
      make ?nextToken ?routes ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let routes = field_map json "Routes" LocalGatewayRouteList.of_json in
      make ?nextToken ?routes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches for routes in the specified local gateway route table."]
module SearchLocalGatewayRoutesRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. route-search.exact-match - The exact match of the specified filter. route-search.longest-prefix-match - The longest prefix that matches the route. route-search.subnet-of-match - The routes with a subnet that match the specified CIDR filter. route-search.supernet-of-match - The routes with a CIDR that encompass the CIDR filter. For example, if you have 10.0.1.0/29 and 10.0.1.0/31 routes in your route table and you specify supernet-of-match as 10.0.1.0/30, then the result returns 10.0.1.0/29. state - The state of the route. type - The route type."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "SearchLocalGatewayRoutesRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~localGatewayRouteTableId ->
              fun () ->
                {
                  filters;
                  maxResults;
                  nextToken;
                  dryRun;
                  localGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Some
              (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      make ?dryRun ?nextToken ?maxResults ?filters ~localGatewayRouteTableId
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let localGatewayRouteTableId =
        field_map_exn json "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ~localGatewayRouteTableId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches for routes in the specified local gateway route table."]
module RunScheduledInstancesResult =
  struct
    type nonrec t =
      {
      instanceIdSet: InstanceIdSet.t option
        [@ocaml.doc "The IDs of the newly launched instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceIdSet = fun () -> { instanceIdSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceIdSet",
           (Option.map x.instanceIdSet ~f:InstanceIdSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceIdSet =
        (Option.map ~f:InstanceIdSet.of_xml)
          (Xml.child xml_arg0 "instanceIdSet") in
      make ?instanceIdSet ()
    let of_json json =
      let instanceIdSet =
        field_map json "InstanceIdSet" InstanceIdSet.of_json in
      make ?instanceIdSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of RunScheduledInstances."]
module RunScheduledInstancesRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that ensures the idempotency of the request. For more information, see Ensuring Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceCount: Integer.t option
        [@ocaml.doc "The number of instances. Default: 1"];
      launchSpecification: ScheduledInstancesLaunchSpecification.t
        [@ocaml.doc
          "The launch specification. You must match the instance type, Availability Zone, network, and platform of the schedule that you purchased."];
      scheduledInstanceId: ScheduledInstanceId.t
        [@ocaml.doc "The Scheduled Instance ID."]}
    let context_ = "RunScheduledInstancesRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?instanceCount ->
          fun ~launchSpecification ->
            fun ~scheduledInstanceId ->
              fun () ->
                {
                  clientToken;
                  dryRun;
                  instanceCount;
                  launchSpecification;
                  scheduledInstanceId
                }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceCount", (Option.map x.instanceCount ~f:Integer.to_value));
        ("LaunchSpecification",
          (Some
             (ScheduledInstancesLaunchSpecification.to_value
                x.launchSpecification)));
        ("ScheduledInstanceId",
          (Some (ScheduledInstanceId.to_value x.scheduledInstanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledInstanceId =
        ScheduledInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduledInstanceId") in
      let launchSpecification =
        ScheduledInstancesLaunchSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchSpecification") in
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "InstanceCount") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~scheduledInstanceId ~launchSpecification ?instanceCount ?dryRun
        ?clientToken ()
    let of_json json =
      let scheduledInstanceId =
        field_map_exn json "ScheduledInstanceId" ScheduledInstanceId.of_json in
      let launchSpecification =
        field_map_exn json "LaunchSpecification"
          ScheduledInstancesLaunchSpecification.of_json in
      let instanceCount = field_map json "InstanceCount" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ~scheduledInstanceId ~launchSpecification ?instanceCount ?dryRun
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for RunScheduledInstances."]
module RunInstancesRequest =
  struct
    type nonrec t =
      {
      blockDeviceMappings: BlockDeviceMappingRequestList.t option
        [@ocaml.doc
          "The block device mapping, which defines the EBS volumes and instance store volumes to attach to the instance at launch. For more information, see Block device mappings in the Amazon EC2 User Guide."];
      imageId: ImageId.t option
        [@ocaml.doc
          "The ID of the AMI. An AMI ID is required to launch an instance and must be specified here or in a launch template."];
      instanceType: InstanceType.t option
        [@ocaml.doc
          "The instance type. For more information, see Instance types in the Amazon EC2 User Guide. Default: m1.small"];
      ipv6AddressCount: Integer.t option
        [@ocaml.doc
          "\\[EC2-VPC\\] The number of IPv6 addresses to associate with the primary network interface. Amazon EC2 chooses the IPv6 addresses from the range of your subnet. You cannot specify this option and the option to assign specific IPv6 addresses in the same request. You can specify this option if you've specified a minimum number of instances to launch. You cannot specify this option and the network interfaces option in the same request."];
      ipv6Addresses: InstanceIpv6AddressList.t option
        [@ocaml.doc
          "\\[EC2-VPC\\] The IPv6 addresses from the range of the subnet to associate with the primary network interface. You cannot specify this option and the option to assign a number of IPv6 addresses in the same request. You cannot specify this option if you've specified a minimum number of instances to launch. You cannot specify this option and the network interfaces option in the same request."];
      kernelId: KernelId.t option
        [@ocaml.doc
          "The ID of the kernel. We recommend that you use PV-GRUB instead of kernels and RAM disks. For more information, see PV-GRUB in the Amazon EC2 User Guide."];
      keyName: KeyPairName.t option
        [@ocaml.doc
          "The name of the key pair. You can create a key pair using CreateKeyPair or ImportKeyPair. If you do not specify a key pair, you can't connect to the instance unless you choose an AMI that is configured to allow users another way to log in."];
      maxCount: Integer.t
        [@ocaml.doc
          "The maximum number of instances to launch. If you specify more instances than Amazon EC2 can launch in the target Availability Zone, Amazon EC2 launches the largest possible number of instances above MinCount. Constraints: Between 1 and the maximum number you're allowed for the specified instance type. For more information about the default limits, and how to request an increase, see How many instances can I run in Amazon EC2 in the Amazon EC2 FAQ."];
      minCount: Integer.t
        [@ocaml.doc
          "The minimum number of instances to launch. If you specify a minimum that is more instances than Amazon EC2 can launch in the target Availability Zone, Amazon EC2 launches no instances. Constraints: Between 1 and the maximum number you're allowed for the specified instance type. For more information about the default limits, and how to request an increase, see How many instances can I run in Amazon EC2 in the Amazon EC2 General FAQ."];
      monitoring: RunInstancesMonitoringEnabled.t option
        [@ocaml.doc
          "Specifies whether detailed monitoring is enabled for the instance."];
      placement: Placement.t option
        [@ocaml.doc "The placement for the instance."];
      ramdiskId: RamdiskId.t option
        [@ocaml.doc
          "The ID of the RAM disk to select. Some kernels require additional drivers at launch. Check the kernel requirements for information about whether you need to specify a RAM disk. To find kernel requirements, go to the Amazon Web Services Resource Center and search for the kernel ID. We recommend that you use PV-GRUB instead of kernels and RAM disks. For more information, see PV-GRUB in the Amazon EC2 User Guide."];
      securityGroupIds: SecurityGroupIdStringList.t option
        [@ocaml.doc
          "The IDs of the security groups. You can create a security group using CreateSecurityGroup. If you specify a network interface, you must specify any security groups as part of the network interface."];
      securityGroups: SecurityGroupStringList.t option
        [@ocaml.doc
          "\\[EC2-Classic, default VPC\\] The names of the security groups. For a nondefault VPC, you must use security group IDs instead. If you specify a network interface, you must specify any security groups as part of the network interface. Default: Amazon EC2 uses the default security group."];
      subnetId: SubnetId.t option
        [@ocaml.doc
          "\\[EC2-VPC\\] The ID of the subnet to launch the instance into. If you specify a network interface, you must specify any subnets as part of the network interface."];
      userData: String_.t option
        [@ocaml.doc
          "The user data to make available to the instance. For more information, see Run commands on your Linux instance at launch and Run commands on your Windows instance at launch. If you are using a command line tool, base64-encoding is performed for you, and you can load the text from a file. Otherwise, you must provide base64-encoded text. User data is limited to 16 KB."];
      additionalInfo: String_.t option [@ocaml.doc "Reserved."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure the idempotency of the request. If you do not specify a client token, a randomly generated token is used for the request to ensure idempotency. For more information, see Ensuring Idempotency. Constraints: Maximum 64 ASCII characters"];
      disableApiTermination: Boolean.t option
        [@ocaml.doc
          "If you set this parameter to true, you can't terminate the instance using the Amazon EC2 console, CLI, or API; otherwise, you can. To change this attribute after launch, use ModifyInstanceAttribute. Alternatively, if you set InstanceInitiatedShutdownBehavior to terminate, you can terminate the instance by running the shutdown command from the instance. Default: false"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ebsOptimized: Boolean.t option
        [@ocaml.doc
          "Indicates whether the instance is optimized for Amazon EBS I/O. This optimization provides dedicated throughput to Amazon EBS and an optimized configuration stack to provide optimal Amazon EBS I/O performance. This optimization isn't available with all instance types. Additional usage charges apply when using an EBS-optimized instance. Default: false"];
      iamInstanceProfile: IamInstanceProfileSpecification.t option
        [@ocaml.doc
          "The name or Amazon Resource Name (ARN) of an IAM instance profile."];
      instanceInitiatedShutdownBehavior: ShutdownBehavior.t option
        [@ocaml.doc
          "Indicates whether an instance stops or terminates when you initiate shutdown from the instance (using the operating system command for system shutdown). Default: stop"];
      networkInterfaces: InstanceNetworkInterfaceSpecificationList.t option
        [@ocaml.doc
          "The network interfaces to associate with the instance. If you specify a network interface, you must specify any security groups and subnets as part of the network interface."];
      privateIpAddress: String_.t option
        [@ocaml.doc
          "\\[EC2-VPC\\] The primary IPv4 address. You must specify a value from the IPv4 address range of the subnet. Only one private IP address can be designated as primary. You can't specify this option if you've specified the option to designate a private IP address as the primary IP address in a network interface specification. You cannot specify this option if you're launching more than one instance in the request. You cannot specify this option and the network interfaces option in the same request."];
      elasticGpuSpecification: ElasticGpuSpecifications.t option
        [@ocaml.doc
          "An elastic GPU to associate with the instance. An Elastic GPU is a GPU resource that you can attach to your Windows instance to accelerate the graphics performance of your applications. For more information, see Amazon EC2 Elastic GPUs in the Amazon EC2 User Guide."];
      elasticInferenceAccelerators: ElasticInferenceAccelerators.t option
        [@ocaml.doc
          "An elastic inference accelerator to associate with the instance. Elastic inference accelerators are a resource you can attach to your Amazon EC2 instances to accelerate your Deep Learning (DL) inference workloads. You cannot specify accelerators from different generations in the same request."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the resources during launch. You can only tag instances and volumes on launch. The specified tags are applied to all instances or volumes that are created during launch. To tag a resource after it has been created, see CreateTags."];
      launchTemplate: LaunchTemplateSpecification.t option
        [@ocaml.doc
          "The launch template to use to launch the instances. Any parameters that you specify in RunInstances override the same parameters in the launch template. You can specify either the name or ID of a launch template, but not both."];
      instanceMarketOptions: InstanceMarketOptionsRequest.t option
        [@ocaml.doc
          "The market (purchasing) option for the instances. For RunInstances, persistent Spot Instance requests are only supported when InstanceInterruptionBehavior is set to either hibernate or stop."];
      creditSpecification: CreditSpecificationRequest.t option
        [@ocaml.doc
          "The credit option for CPU usage of the burstable performance instance. Valid values are standard and unlimited. To change this attribute after launch, use ModifyInstanceCreditSpecification. For more information, see Burstable performance instances in the Amazon EC2 User Guide. Default: standard (T2 instances) or unlimited (T3/T3a instances) For T3 instances with host tenancy, only standard is supported."];
      cpuOptions: CpuOptionsRequest.t option
        [@ocaml.doc
          "The CPU options for the instance. For more information, see Optimize CPU options in the Amazon EC2 User Guide."];
      capacityReservationSpecification:
        CapacityReservationSpecification.t option
        [@ocaml.doc
          "Information about the Capacity Reservation targeting option. If you do not specify this parameter, the instance's Capacity Reservation preference defaults to open, which enables it to run in any open Capacity Reservation that has matching attributes (instance type, platform, Availability Zone)."];
      hibernationOptions: HibernationOptionsRequest.t option
        [@ocaml.doc
          "Indicates whether an instance is enabled for hibernation. For more information, see Hibernate your instance in the Amazon EC2 User Guide. You can't enable hibernation and Amazon Web Services Nitro Enclaves on the same instance."];
      licenseSpecifications: LicenseSpecificationListRequest.t option
        [@ocaml.doc "The license configurations."];
      metadataOptions: InstanceMetadataOptionsRequest.t option
        [@ocaml.doc
          "The metadata options for the instance. For more information, see Instance metadata and user data."];
      enclaveOptions: EnclaveOptionsRequest.t option
        [@ocaml.doc
          "Indicates whether the instance is enabled for Amazon Web Services Nitro Enclaves. For more information, see What is Amazon Web Services Nitro Enclaves? in the Amazon Web Services Nitro Enclaves User Guide. You can't enable Amazon Web Services Nitro Enclaves and hibernation on the same instance."];
      privateDnsNameOptions: PrivateDnsNameOptionsRequest.t option
        [@ocaml.doc
          "The options for the instance hostname. The default values are inherited from the subnet."];
      maintenanceOptions: InstanceMaintenanceOptionsRequest.t option
        [@ocaml.doc "The maintenance and recovery options for the instance."]}
    let context_ = "RunInstancesRequest"
    let make ?blockDeviceMappings =
      fun ?imageId ->
        fun ?instanceType ->
          fun ?ipv6AddressCount ->
            fun ?ipv6Addresses ->
              fun ?kernelId ->
                fun ?keyName ->
                  fun ?monitoring ->
                    fun ?placement ->
                      fun ?ramdiskId ->
                        fun ?securityGroupIds ->
                          fun ?securityGroups ->
                            fun ?subnetId ->
                              fun ?userData ->
                                fun ?additionalInfo ->
                                  fun ?clientToken ->
                                    fun ?disableApiTermination ->
                                      fun ?dryRun ->
                                        fun ?ebsOptimized ->
                                          fun ?iamInstanceProfile ->
                                            fun
                                              ?instanceInitiatedShutdownBehavior
                                              ->
                                              fun ?networkInterfaces ->
                                                fun ?privateIpAddress ->
                                                  fun
                                                    ?elasticGpuSpecification
                                                    ->
                                                    fun
                                                      ?elasticInferenceAccelerators
                                                      ->
                                                      fun ?tagSpecifications
                                                        ->
                                                        fun ?launchTemplate
                                                          ->
                                                          fun
                                                            ?instanceMarketOptions
                                                            ->
                                                            fun
                                                              ?creditSpecification
                                                              ->
                                                              fun ?cpuOptions
                                                                ->
                                                                fun
                                                                  ?capacityReservationSpecification
                                                                  ->
                                                                  fun
                                                                    ?hibernationOptions
                                                                    ->
                                                                    fun
                                                                    ?licenseSpecifications
                                                                    ->
                                                                    fun
                                                                    ?metadataOptions
                                                                    ->
                                                                    fun
                                                                    ?enclaveOptions
                                                                    ->
                                                                    fun
                                                                    ?privateDnsNameOptions
                                                                    ->
                                                                    fun
                                                                    ?maintenanceOptions
                                                                    ->
                                                                    fun
                                                                    ~maxCount
                                                                    ->
                                                                    fun
                                                                    ~minCount
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    blockDeviceMappings;
                                                                    imageId;
                                                                    instanceType;
                                                                    ipv6AddressCount;
                                                                    ipv6Addresses;
                                                                    kernelId;
                                                                    keyName;
                                                                    monitoring;
                                                                    placement;
                                                                    ramdiskId;
                                                                    securityGroupIds;
                                                                    securityGroups;
                                                                    subnetId;
                                                                    userData;
                                                                    additionalInfo;
                                                                    clientToken;
                                                                    disableApiTermination;
                                                                    dryRun;
                                                                    ebsOptimized;
                                                                    iamInstanceProfile;
                                                                    instanceInitiatedShutdownBehavior;
                                                                    networkInterfaces;
                                                                    privateIpAddress;
                                                                    elasticGpuSpecification;
                                                                    elasticInferenceAccelerators;
                                                                    tagSpecifications;
                                                                    launchTemplate;
                                                                    instanceMarketOptions;
                                                                    creditSpecification;
                                                                    cpuOptions;
                                                                    capacityReservationSpecification;
                                                                    hibernationOptions;
                                                                    licenseSpecifications;
                                                                    metadataOptions;
                                                                    enclaveOptions;
                                                                    privateDnsNameOptions;
                                                                    maintenanceOptions;
                                                                    maxCount;
                                                                    minCount
                                                                    }
    let to_value x =
      structure_to_value
        [("BlockDeviceMapping",
           (Option.map x.blockDeviceMappings
              ~f:BlockDeviceMappingRequestList.to_value));
        ("ImageId", (Option.map x.imageId ~f:ImageId.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:InstanceType.to_value));
        ("Ipv6AddressCount",
          (Option.map x.ipv6AddressCount ~f:Integer.to_value));
        ("Ipv6Address",
          (Option.map x.ipv6Addresses ~f:InstanceIpv6AddressList.to_value));
        ("KernelId", (Option.map x.kernelId ~f:KernelId.to_value));
        ("KeyName", (Option.map x.keyName ~f:KeyPairName.to_value));
        ("MaxCount", (Some (Integer.to_value x.maxCount)));
        ("MinCount", (Some (Integer.to_value x.minCount)));
        ("Monitoring",
          (Option.map x.monitoring ~f:RunInstancesMonitoringEnabled.to_value));
        ("Placement", (Option.map x.placement ~f:Placement.to_value));
        ("RamdiskId", (Option.map x.ramdiskId ~f:RamdiskId.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:SecurityGroupIdStringList.to_value));
        ("SecurityGroup",
          (Option.map x.securityGroups ~f:SecurityGroupStringList.to_value));
        ("SubnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("UserData", (Option.map x.userData ~f:String_.to_value));
        ("additionalInfo", (Option.map x.additionalInfo ~f:String_.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("disableApiTermination",
          (Option.map x.disableApiTermination ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ebsOptimized", (Option.map x.ebsOptimized ~f:Boolean.to_value));
        ("iamInstanceProfile",
          (Option.map x.iamInstanceProfile
             ~f:IamInstanceProfileSpecification.to_value));
        ("instanceInitiatedShutdownBehavior",
          (Option.map x.instanceInitiatedShutdownBehavior
             ~f:ShutdownBehavior.to_value));
        ("networkInterface",
          (Option.map x.networkInterfaces
             ~f:InstanceNetworkInterfaceSpecificationList.to_value));
        ("privateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("ElasticGpuSpecification",
          (Option.map x.elasticGpuSpecification
             ~f:ElasticGpuSpecifications.to_value));
        ("ElasticInferenceAccelerator",
          (Option.map x.elasticInferenceAccelerators
             ~f:ElasticInferenceAccelerators.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("LaunchTemplate",
          (Option.map x.launchTemplate
             ~f:LaunchTemplateSpecification.to_value));
        ("InstanceMarketOptions",
          (Option.map x.instanceMarketOptions
             ~f:InstanceMarketOptionsRequest.to_value));
        ("CreditSpecification",
          (Option.map x.creditSpecification
             ~f:CreditSpecificationRequest.to_value));
        ("CpuOptions",
          (Option.map x.cpuOptions ~f:CpuOptionsRequest.to_value));
        ("CapacityReservationSpecification",
          (Option.map x.capacityReservationSpecification
             ~f:CapacityReservationSpecification.to_value));
        ("HibernationOptions",
          (Option.map x.hibernationOptions
             ~f:HibernationOptionsRequest.to_value));
        ("LicenseSpecification",
          (Option.map x.licenseSpecifications
             ~f:LicenseSpecificationListRequest.to_value));
        ("MetadataOptions",
          (Option.map x.metadataOptions
             ~f:InstanceMetadataOptionsRequest.to_value));
        ("EnclaveOptions",
          (Option.map x.enclaveOptions ~f:EnclaveOptionsRequest.to_value));
        ("PrivateDnsNameOptions",
          (Option.map x.privateDnsNameOptions
             ~f:PrivateDnsNameOptionsRequest.to_value));
        ("MaintenanceOptions",
          (Option.map x.maintenanceOptions
             ~f:InstanceMaintenanceOptionsRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maintenanceOptions =
        (Option.map ~f:InstanceMaintenanceOptionsRequest.of_xml)
          (Xml.child xml_arg0 "MaintenanceOptions") in
      let privateDnsNameOptions =
        (Option.map ~f:PrivateDnsNameOptionsRequest.of_xml)
          (Xml.child xml_arg0 "PrivateDnsNameOptions") in
      let enclaveOptions =
        (Option.map ~f:EnclaveOptionsRequest.of_xml)
          (Xml.child xml_arg0 "EnclaveOptions") in
      let metadataOptions =
        (Option.map ~f:InstanceMetadataOptionsRequest.of_xml)
          (Xml.child xml_arg0 "MetadataOptions") in
      let licenseSpecifications =
        (Option.map ~f:LicenseSpecificationListRequest.of_xml)
          (Xml.child xml_arg0 "LicenseSpecification") in
      let hibernationOptions =
        (Option.map ~f:HibernationOptionsRequest.of_xml)
          (Xml.child xml_arg0 "HibernationOptions") in
      let capacityReservationSpecification =
        (Option.map ~f:CapacityReservationSpecification.of_xml)
          (Xml.child xml_arg0 "CapacityReservationSpecification") in
      let cpuOptions =
        (Option.map ~f:CpuOptionsRequest.of_xml)
          (Xml.child xml_arg0 "CpuOptions") in
      let creditSpecification =
        (Option.map ~f:CreditSpecificationRequest.of_xml)
          (Xml.child xml_arg0 "CreditSpecification") in
      let instanceMarketOptions =
        (Option.map ~f:InstanceMarketOptionsRequest.of_xml)
          (Xml.child xml_arg0 "InstanceMarketOptions") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplateSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchTemplate") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let elasticInferenceAccelerators =
        (Option.map ~f:ElasticInferenceAccelerators.of_xml)
          (Xml.child xml_arg0 "ElasticInferenceAccelerator") in
      let elasticGpuSpecification =
        (Option.map ~f:ElasticGpuSpecifications.of_xml)
          (Xml.child xml_arg0 "ElasticGpuSpecification") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let networkInterfaces =
        (Option.map ~f:InstanceNetworkInterfaceSpecificationList.of_xml)
          (Xml.child xml_arg0 "networkInterface") in
      let instanceInitiatedShutdownBehavior =
        (Option.map ~f:ShutdownBehavior.of_xml)
          (Xml.child xml_arg0 "instanceInitiatedShutdownBehavior") in
      let iamInstanceProfile =
        (Option.map ~f:IamInstanceProfileSpecification.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfile") in
      let ebsOptimized =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ebsOptimized") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let disableApiTermination =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "disableApiTermination") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let additionalInfo =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "additionalInfo") in
      let userData =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UserData") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "SubnetId") in
      let securityGroups =
        (Option.map ~f:SecurityGroupStringList.of_xml)
          (Xml.child xml_arg0 "SecurityGroup") in
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIdStringList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let ramdiskId =
        (Option.map ~f:RamdiskId.of_xml) (Xml.child xml_arg0 "RamdiskId") in
      let placement =
        (Option.map ~f:Placement.of_xml) (Xml.child xml_arg0 "Placement") in
      let monitoring =
        (Option.map ~f:RunInstancesMonitoringEnabled.of_xml)
          (Xml.child xml_arg0 "Monitoring") in
      let minCount =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "MinCount") in
      let maxCount =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "MaxCount") in
      let keyName =
        (Option.map ~f:KeyPairName.of_xml) (Xml.child xml_arg0 "KeyName") in
      let kernelId =
        (Option.map ~f:KernelId.of_xml) (Xml.child xml_arg0 "KernelId") in
      let ipv6Addresses =
        (Option.map ~f:InstanceIpv6AddressList.of_xml)
          (Xml.child xml_arg0 "Ipv6Address") in
      let ipv6AddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "Ipv6AddressCount") in
      let instanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let imageId =
        (Option.map ~f:ImageId.of_xml) (Xml.child xml_arg0 "ImageId") in
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappingRequestList.of_xml)
          (Xml.child xml_arg0 "BlockDeviceMapping") in
      make ?maintenanceOptions ?privateDnsNameOptions ?enclaveOptions
        ?metadataOptions ?licenseSpecifications ?hibernationOptions
        ?capacityReservationSpecification ?cpuOptions ?creditSpecification
        ?instanceMarketOptions ?launchTemplate ?tagSpecifications
        ?elasticInferenceAccelerators ?elasticGpuSpecification
        ?privateIpAddress ?networkInterfaces
        ?instanceInitiatedShutdownBehavior ?iamInstanceProfile ?ebsOptimized
        ?dryRun ?disableApiTermination ?clientToken ?additionalInfo ?userData
        ?subnetId ?securityGroups ?securityGroupIds ?ramdiskId ?placement
        ?monitoring ~minCount ~maxCount ?keyName ?kernelId ?ipv6Addresses
        ?ipv6AddressCount ?instanceType ?imageId ?blockDeviceMappings ()
    let of_json json =
      let maintenanceOptions =
        field_map json "MaintenanceOptions"
          InstanceMaintenanceOptionsRequest.of_json in
      let privateDnsNameOptions =
        field_map json "PrivateDnsNameOptions"
          PrivateDnsNameOptionsRequest.of_json in
      let enclaveOptions =
        field_map json "EnclaveOptions" EnclaveOptionsRequest.of_json in
      let metadataOptions =
        field_map json "MetadataOptions"
          InstanceMetadataOptionsRequest.of_json in
      let licenseSpecifications =
        field_map json "LicenseSpecifications"
          LicenseSpecificationListRequest.of_json in
      let hibernationOptions =
        field_map json "HibernationOptions" HibernationOptionsRequest.of_json in
      let capacityReservationSpecification =
        field_map json "CapacityReservationSpecification"
          CapacityReservationSpecification.of_json in
      let cpuOptions = field_map json "CpuOptions" CpuOptionsRequest.of_json in
      let creditSpecification =
        field_map json "CreditSpecification"
          CreditSpecificationRequest.of_json in
      let instanceMarketOptions =
        field_map json "InstanceMarketOptions"
          InstanceMarketOptionsRequest.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplateSpecification.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let elasticInferenceAccelerators =
        field_map json "ElasticInferenceAccelerators"
          ElasticInferenceAccelerators.of_json in
      let elasticGpuSpecification =
        field_map json "ElasticGpuSpecification"
          ElasticGpuSpecifications.of_json in
      let privateIpAddress =
        field_map json "PrivateIpAddress" String_.of_json in
      let networkInterfaces =
        field_map json "NetworkInterfaces"
          InstanceNetworkInterfaceSpecificationList.of_json in
      let instanceInitiatedShutdownBehavior =
        field_map json "InstanceInitiatedShutdownBehavior"
          ShutdownBehavior.of_json in
      let iamInstanceProfile =
        field_map json "IamInstanceProfile"
          IamInstanceProfileSpecification.of_json in
      let ebsOptimized = field_map json "EbsOptimized" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let disableApiTermination =
        field_map json "DisableApiTermination" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let additionalInfo = field_map json "AdditionalInfo" String_.of_json in
      let userData = field_map json "UserData" String_.of_json in
      let subnetId = field_map json "SubnetId" SubnetId.of_json in
      let securityGroups =
        field_map json "SecurityGroups" SecurityGroupStringList.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds" SecurityGroupIdStringList.of_json in
      let ramdiskId = field_map json "RamdiskId" RamdiskId.of_json in
      let placement = field_map json "Placement" Placement.of_json in
      let monitoring =
        field_map json "Monitoring" RunInstancesMonitoringEnabled.of_json in
      let minCount = field_map_exn json "MinCount" Integer.of_json in
      let maxCount = field_map_exn json "MaxCount" Integer.of_json in
      let keyName = field_map json "KeyName" KeyPairName.of_json in
      let kernelId = field_map json "KernelId" KernelId.of_json in
      let ipv6Addresses =
        field_map json "Ipv6Addresses" InstanceIpv6AddressList.of_json in
      let ipv6AddressCount =
        field_map json "Ipv6AddressCount" Integer.of_json in
      let instanceType = field_map json "InstanceType" InstanceType.of_json in
      let imageId = field_map json "ImageId" ImageId.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings"
          BlockDeviceMappingRequestList.of_json in
      make ?maintenanceOptions ?privateDnsNameOptions ?enclaveOptions
        ?metadataOptions ?licenseSpecifications ?hibernationOptions
        ?capacityReservationSpecification ?cpuOptions ?creditSpecification
        ?instanceMarketOptions ?launchTemplate ?tagSpecifications
        ?elasticInferenceAccelerators ?elasticGpuSpecification
        ?privateIpAddress ?networkInterfaces
        ?instanceInitiatedShutdownBehavior ?iamInstanceProfile ?ebsOptimized
        ?dryRun ?disableApiTermination ?clientToken ?additionalInfo ?userData
        ?subnetId ?securityGroups ?securityGroupIds ?ramdiskId ?placement
        ?monitoring ~minCount ~maxCount ?keyName ?kernelId ?ipv6Addresses
        ?ipv6AddressCount ?instanceType ?imageId ?blockDeviceMappings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Launches the specified number of instances using an AMI for which you have permissions. You can specify a number of options, or leave the default options. The following rules apply: \\[EC2-VPC\\] If you don't specify a subnet ID, we choose a default subnet from your default VPC for you. If you don't have a default VPC, you must specify a subnet ID in the request. \\[EC2-Classic\\] If don't specify an Availability Zone, we choose one for you. Some instance types must be launched into a VPC. If you do not have a default VPC, or if you do not specify a subnet ID, the request fails. For more information, see Instance types available only in a VPC. \\[EC2-VPC\\] All instances have a network interface with a primary private IPv4 address. If you don't specify this address, we choose one from the IPv4 range of your subnet. Not all instance types support IPv6 addresses. For more information, see Instance types. If you don't specify a security group ID, we use the default security group. For more information, see Security groups. If any of the AMIs have a product code attached for which the user has not subscribed, the request fails. You can create a launch template, which is a resource that contains the parameters to launch an instance. When you launch an instance using RunInstances, you can specify the launch template instead of specifying the launch parameters. To ensure faster instance launches, break up large requests into smaller batches. For example, create five separate launch requests for 100 instances each instead of one launch request for 500 instances. An instance is ready for you to use when it's in the running state. You can check the state of your instance using DescribeInstances. You can tag instances and EBS volumes during launch, after launch, or both. For more information, see CreateTags and Tagging your Amazon EC2 resources. Linux instances have access to the public key of the key pair at boot. You can use this key to provide secure access to the instance. Amazon EC2 public images use this feature to provide secure access without passwords. For more information, see Key pairs. For troubleshooting, see What to do if an instance immediately terminates, and Troubleshooting connecting to your instance."]
module RevokeSecurityGroupIngressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."];
      unknownIpPermissions: IpPermissionList.t option
        [@ocaml.doc
          "The inbound rules that were unknown to the service. In some cases, unknownIpPermissionSet might be in a different format from the request parameter."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?unknownIpPermissions -> fun () -> { return; unknownIpPermissions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("unknownIpPermissionSet",
          (Option.map x.unknownIpPermissions ~f:IpPermissionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unknownIpPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "unknownIpPermissionSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?unknownIpPermissions ?return ()
    let of_json json =
      let unknownIpPermissions =
        field_map json "UnknownIpPermissions" IpPermissionList.of_json in
      let return = field_map json "Return" Boolean.of_json in
      make ?unknownIpPermissions ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the specified inbound (ingress) rules from a security group. You can specify rules using either rule IDs or security group rule properties. If you use rule properties, the values that you specify (for example, ports) must match the existing rule's values exactly. Each rule has a protocol, from and to ports, and source (CIDR range, security group, or prefix list). For the TCP and UDP protocols, you must also specify the destination port or range of ports. For the ICMP protocol, you must also specify the ICMP type and code. If the security group rule has a description, you do not need to specify the description to revoke the rule. \\[EC2-Classic, default VPC\\] If the values you specify do not match the existing rule's values, no error is returned, and the output describes the security group rules that were not revoked. Amazon Web Services recommends that you describe the security group to verify that the rules were removed. Rule changes are propagated to instances within the security group as quickly as possible. However, a small delay might occur."]
module RevokeSecurityGroupIngressRequest =
  struct
    type nonrec t =
      {
      cidrIp: String_.t option
        [@ocaml.doc
          "The CIDR IP address range. You can't specify this parameter when specifying a source security group."];
      fromPort: Integer.t option
        [@ocaml.doc
          "The start of port range for the TCP and UDP protocols, or an ICMP type number. For the ICMP type number, use -1 to specify all ICMP types."];
      groupId: SecurityGroupId.t option
        [@ocaml.doc
          "The ID of the security group. You must specify either the security group ID or the security group name in the request. For security groups in a nondefault VPC, you must specify the security group ID."];
      groupName: SecurityGroupName.t option
        [@ocaml.doc
          "\\[EC2-Classic, default VPC\\] The name of the security group. You must specify either the security group ID or the security group name in the request."];
      ipPermissions: IpPermissionList.t option
        [@ocaml.doc
          "The sets of IP permissions. You can't specify a source security group and a CIDR IP address range in the same set of permissions."];
      ipProtocol: String_.t option
        [@ocaml.doc
          "The IP protocol name (tcp, udp, icmp) or number (see Protocol Numbers). Use -1 to specify all."];
      sourceSecurityGroupName: String_.t option
        [@ocaml.doc
          "\\[EC2-Classic, default VPC\\] The name of the source security group. You can't specify this parameter in combination with the following parameters: the CIDR IP address range, the start of the port range, the IP protocol, and the end of the port range. For EC2-VPC, the source security group must be in the same VPC. To revoke a specific rule for an IP protocol and port range, use a set of IP permissions instead."];
      sourceSecurityGroupOwnerId: String_.t option
        [@ocaml.doc
          "\\[EC2-Classic\\] The Amazon Web Services account ID of the source security group, if the source security group is in a different account. You can't specify this parameter in combination with the following parameters: the CIDR IP address range, the IP protocol, the start of the port range, and the end of the port range. To revoke a specific rule for an IP protocol and port range, use a set of IP permissions instead."];
      toPort: Integer.t option
        [@ocaml.doc
          "The end of port range for the TCP and UDP protocols, or an ICMP code number. For the ICMP code number, use -1 to specify all ICMP codes for the ICMP type."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      securityGroupRuleIds: SecurityGroupRuleIdList.t option
        [@ocaml.doc "The IDs of the security group rules."]}
    let make ?cidrIp =
      fun ?fromPort ->
        fun ?groupId ->
          fun ?groupName ->
            fun ?ipPermissions ->
              fun ?ipProtocol ->
                fun ?sourceSecurityGroupName ->
                  fun ?sourceSecurityGroupOwnerId ->
                    fun ?toPort ->
                      fun ?dryRun ->
                        fun ?securityGroupRuleIds ->
                          fun () ->
                            {
                              cidrIp;
                              fromPort;
                              groupId;
                              groupName;
                              ipPermissions;
                              ipProtocol;
                              sourceSecurityGroupName;
                              sourceSecurityGroupOwnerId;
                              toPort;
                              dryRun;
                              securityGroupRuleIds
                            }
    let to_value x =
      structure_to_value
        [("CidrIp", (Option.map x.cidrIp ~f:String_.to_value));
        ("FromPort", (Option.map x.fromPort ~f:Integer.to_value));
        ("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("IpPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("IpProtocol", (Option.map x.ipProtocol ~f:String_.to_value));
        ("SourceSecurityGroupName",
          (Option.map x.sourceSecurityGroupName ~f:String_.to_value));
        ("SourceSecurityGroupOwnerId",
          (Option.map x.sourceSecurityGroupOwnerId ~f:String_.to_value));
        ("ToPort", (Option.map x.toPort ~f:Integer.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecurityGroupRuleId",
          (Option.map x.securityGroupRuleIds
             ~f:SecurityGroupRuleIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRuleIds =
        (Option.map ~f:SecurityGroupRuleIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupRuleId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let toPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "ToPort") in
      let sourceSecurityGroupOwnerId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroupOwnerId") in
      let sourceSecurityGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroupName") in
      let ipProtocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "IpProtocol") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "IpPermissions") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      let fromPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "FromPort") in
      let cidrIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrIp") in
      make ?securityGroupRuleIds ?dryRun ?toPort ?sourceSecurityGroupOwnerId
        ?sourceSecurityGroupName ?ipProtocol ?ipPermissions ?groupName
        ?groupId ?fromPort ?cidrIp ()
    let of_json json =
      let securityGroupRuleIds =
        field_map json "SecurityGroupRuleIds" SecurityGroupRuleIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let toPort = field_map json "ToPort" Integer.of_json in
      let sourceSecurityGroupOwnerId =
        field_map json "SourceSecurityGroupOwnerId" String_.of_json in
      let sourceSecurityGroupName =
        field_map json "SourceSecurityGroupName" String_.of_json in
      let ipProtocol = field_map json "IpProtocol" String_.of_json in
      let ipPermissions =
        field_map json "IpPermissions" IpPermissionList.of_json in
      let groupName = field_map json "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json "GroupId" SecurityGroupId.of_json in
      let fromPort = field_map json "FromPort" Integer.of_json in
      let cidrIp = field_map json "CidrIp" String_.of_json in
      make ?securityGroupRuleIds ?dryRun ?toPort ?sourceSecurityGroupOwnerId
        ?sourceSecurityGroupName ?ipProtocol ?ipPermissions ?groupName
        ?groupId ?fromPort ?cidrIp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the specified inbound (ingress) rules from a security group. You can specify rules using either rule IDs or security group rule properties. If you use rule properties, the values that you specify (for example, ports) must match the existing rule's values exactly. Each rule has a protocol, from and to ports, and source (CIDR range, security group, or prefix list). For the TCP and UDP protocols, you must also specify the destination port or range of ports. For the ICMP protocol, you must also specify the ICMP type and code. If the security group rule has a description, you do not need to specify the description to revoke the rule. \\[EC2-Classic, default VPC\\] If the values you specify do not match the existing rule's values, no error is returned, and the output describes the security group rules that were not revoked. Amazon Web Services recommends that you describe the security group to verify that the rules were removed. Rule changes are propagated to instances within the security group as quickly as possible. However, a small delay might occur."]
module RevokeSecurityGroupEgressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."];
      unknownIpPermissions: IpPermissionList.t option
        [@ocaml.doc
          "The outbound rules that were unknown to the service. In some cases, unknownIpPermissionSet might be in a different format from the request parameter."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?unknownIpPermissions -> fun () -> { return; unknownIpPermissions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("unknownIpPermissionSet",
          (Option.map x.unknownIpPermissions ~f:IpPermissionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unknownIpPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "unknownIpPermissionSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?unknownIpPermissions ?return ()
    let of_json json =
      let unknownIpPermissions =
        field_map json "UnknownIpPermissions" IpPermissionList.of_json in
      let return = field_map json "Return" Boolean.of_json in
      make ?unknownIpPermissions ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "\\[VPC only\\] Removes the specified outbound (egress) rules from a security group for EC2-VPC. This action does not apply to security groups for use in EC2-Classic. You can specify rules using either rule IDs or security group rule properties. If you use rule properties, the values that you specify (for example, ports) must match the existing rule's values exactly. Each rule has a protocol, from and to ports, and destination (CIDR range, security group, or prefix list). For the TCP and UDP protocols, you must also specify the destination port or range of ports. For the ICMP protocol, you must also specify the ICMP type and code. If the security group rule has a description, you do not need to specify the description to revoke the rule. \\[Default VPC\\] If the values you specify do not match the existing rule's values, no error is returned, and the output describes the security group rules that were not revoked. Amazon Web Services recommends that you describe the security group to verify that the rules were removed. Rule changes are propagated to instances within the security group as quickly as possible. However, a small delay might occur."]
module RevokeSecurityGroupEgressRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupId: SecurityGroupId.t [@ocaml.doc "The ID of the security group."];
      ipPermissions: IpPermissionList.t option
        [@ocaml.doc
          "The sets of IP permissions. You can't specify a destination security group and a CIDR IP address range in the same set of permissions."];
      securityGroupRuleIds: SecurityGroupRuleIdList.t option
        [@ocaml.doc "The IDs of the security group rules."];
      cidrIp: String_.t option
        [@ocaml.doc
          "Not supported. Use a set of IP permissions to specify the CIDR."];
      fromPort: Integer.t option
        [@ocaml.doc
          "Not supported. Use a set of IP permissions to specify the port."];
      ipProtocol: String_.t option
        [@ocaml.doc
          "Not supported. Use a set of IP permissions to specify the protocol name or number."];
      toPort: Integer.t option
        [@ocaml.doc
          "Not supported. Use a set of IP permissions to specify the port."];
      sourceSecurityGroupName: String_.t option
        [@ocaml.doc
          "Not supported. Use a set of IP permissions to specify a destination security group."];
      sourceSecurityGroupOwnerId: String_.t option
        [@ocaml.doc
          "Not supported. Use a set of IP permissions to specify a destination security group."]}
    let context_ = "RevokeSecurityGroupEgressRequest"
    let make ?dryRun =
      fun ?ipPermissions ->
        fun ?securityGroupRuleIds ->
          fun ?cidrIp ->
            fun ?fromPort ->
              fun ?ipProtocol ->
                fun ?toPort ->
                  fun ?sourceSecurityGroupName ->
                    fun ?sourceSecurityGroupOwnerId ->
                      fun ~groupId ->
                        fun () ->
                          {
                            dryRun;
                            ipPermissions;
                            securityGroupRuleIds;
                            cidrIp;
                            fromPort;
                            ipProtocol;
                            toPort;
                            sourceSecurityGroupName;
                            sourceSecurityGroupOwnerId;
                            groupId
                          }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupId", (Some (SecurityGroupId.to_value x.groupId)));
        ("ipPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("SecurityGroupRuleId",
          (Option.map x.securityGroupRuleIds
             ~f:SecurityGroupRuleIdList.to_value));
        ("cidrIp", (Option.map x.cidrIp ~f:String_.to_value));
        ("fromPort", (Option.map x.fromPort ~f:Integer.to_value));
        ("ipProtocol", (Option.map x.ipProtocol ~f:String_.to_value));
        ("toPort", (Option.map x.toPort ~f:Integer.to_value));
        ("sourceSecurityGroupName",
          (Option.map x.sourceSecurityGroupName ~f:String_.to_value));
        ("sourceSecurityGroupOwnerId",
          (Option.map x.sourceSecurityGroupOwnerId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceSecurityGroupOwnerId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceSecurityGroupOwnerId") in
      let sourceSecurityGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceSecurityGroupName") in
      let toPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "toPort") in
      let ipProtocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipProtocol") in
      let fromPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "fromPort") in
      let cidrIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cidrIp") in
      let securityGroupRuleIds =
        (Option.map ~f:SecurityGroupRuleIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupRuleId") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "ipPermissions") in
      let groupId =
        SecurityGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?sourceSecurityGroupOwnerId ?sourceSecurityGroupName ?toPort
        ?ipProtocol ?fromPort ?cidrIp ?securityGroupRuleIds ?ipPermissions
        ~groupId ?dryRun ()
    let of_json json =
      let sourceSecurityGroupOwnerId =
        field_map json "SourceSecurityGroupOwnerId" String_.of_json in
      let sourceSecurityGroupName =
        field_map json "SourceSecurityGroupName" String_.of_json in
      let toPort = field_map json "ToPort" Integer.of_json in
      let ipProtocol = field_map json "IpProtocol" String_.of_json in
      let fromPort = field_map json "FromPort" Integer.of_json in
      let cidrIp = field_map json "CidrIp" String_.of_json in
      let securityGroupRuleIds =
        field_map json "SecurityGroupRuleIds" SecurityGroupRuleIdList.of_json in
      let ipPermissions =
        field_map json "IpPermissions" IpPermissionList.of_json in
      let groupId = field_map_exn json "GroupId" SecurityGroupId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?sourceSecurityGroupOwnerId ?sourceSecurityGroupName ?toPort
        ?ipProtocol ?fromPort ?cidrIp ?securityGroupRuleIds ?ipPermissions
        ~groupId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "\\[VPC only\\] Removes the specified outbound (egress) rules from a security group for EC2-VPC. This action does not apply to security groups for use in EC2-Classic. You can specify rules using either rule IDs or security group rule properties. If you use rule properties, the values that you specify (for example, ports) must match the existing rule's values exactly. Each rule has a protocol, from and to ports, and destination (CIDR range, security group, or prefix list). For the TCP and UDP protocols, you must also specify the destination port or range of ports. For the ICMP protocol, you must also specify the ICMP type and code. If the security group rule has a description, you do not need to specify the description to revoke the rule. \\[Default VPC\\] If the values you specify do not match the existing rule's values, no error is returned, and the output describes the security group rules that were not revoked. Amazon Web Services recommends that you describe the security group to verify that the rules were removed. Rule changes are propagated to instances within the security group as quickly as possible. However, a small delay might occur."]
module RevokeClientVpnIngressResult =
  struct
    type nonrec t =
      {
      status: ClientVpnAuthorizationRuleStatus.t option
        [@ocaml.doc "The current state of the authorization rule."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status",
           (Option.map x.status ~f:ClientVpnAuthorizationRuleStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnAuthorizationRuleStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status =
        field_map json "Status" ClientVpnAuthorizationRuleStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes an ingress authorization rule from a Client VPN endpoint."]
module RevokeClientVpnIngressRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc
          "The ID of the Client VPN endpoint with which the authorization rule is associated."];
      targetNetworkCidr: String_.t
        [@ocaml.doc
          "The IPv4 address range, in CIDR notation, of the network for which access is being removed."];
      accessGroupId: String_.t option
        [@ocaml.doc
          "The ID of the Active Directory group for which to revoke access."];
      revokeAllGroups: Boolean.t option
        [@ocaml.doc
          "Indicates whether access should be revoked for all clients."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RevokeClientVpnIngressRequest"
    let make ?accessGroupId =
      fun ?revokeAllGroups ->
        fun ?dryRun ->
          fun ~clientVpnEndpointId ->
            fun ~targetNetworkCidr ->
              fun () ->
                {
                  accessGroupId;
                  revokeAllGroups;
                  dryRun;
                  clientVpnEndpointId;
                  targetNetworkCidr
                }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("TargetNetworkCidr", (Some (String_.to_value x.targetNetworkCidr)));
        ("AccessGroupId", (Option.map x.accessGroupId ~f:String_.to_value));
        ("RevokeAllGroups",
          (Option.map x.revokeAllGroups ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let revokeAllGroups =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RevokeAllGroups") in
      let accessGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccessGroupId") in
      let targetNetworkCidr =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetNetworkCidr") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?revokeAllGroups ?accessGroupId ~targetNetworkCidr
        ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let revokeAllGroups = field_map json "RevokeAllGroups" Boolean.of_json in
      let accessGroupId = field_map json "AccessGroupId" String_.of_json in
      let targetNetworkCidr =
        field_map_exn json "TargetNetworkCidr" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?revokeAllGroups ?accessGroupId ~targetNetworkCidr
        ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes an ingress authorization rule from a Client VPN endpoint."]
module RestoreSnapshotTierResult =
  struct
    type nonrec t =
      {
      snapshotId: String_.t option [@ocaml.doc "The ID of the snapshot."];
      restoreStartTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The date and time when the snapshot restore process started."];
      restoreDuration: Integer.t option
        [@ocaml.doc
          "For temporary restores only. The number of days for which the archived snapshot is temporarily restored."];
      isPermanentRestore: Boolean.t option
        [@ocaml.doc
          "Indicates whether the snapshot is permanently restored. true indicates a permanent restore. false indicates a temporary restore."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshotId =
      fun ?restoreStartTime ->
        fun ?restoreDuration ->
          fun ?isPermanentRestore ->
            fun () ->
              {
                snapshotId;
                restoreStartTime;
                restoreDuration;
                isPermanentRestore
              }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotId", (Option.map x.snapshotId ~f:String_.to_value));
        ("restoreStartTime",
          (Option.map x.restoreStartTime ~f:MillisecondDateTime.to_value));
        ("restoreDuration",
          (Option.map x.restoreDuration ~f:Integer.to_value));
        ("isPermanentRestore",
          (Option.map x.isPermanentRestore ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let isPermanentRestore =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "isPermanentRestore") in
      let restoreDuration =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "restoreDuration") in
      let restoreStartTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "restoreStartTime") in
      let snapshotId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "snapshotId") in
      make ?isPermanentRestore ?restoreDuration ?restoreStartTime ?snapshotId
        ()
    let of_json json =
      let isPermanentRestore =
        field_map json "IsPermanentRestore" Boolean.of_json in
      let restoreDuration = field_map json "RestoreDuration" Integer.of_json in
      let restoreStartTime =
        field_map json "RestoreStartTime" MillisecondDateTime.of_json in
      let snapshotId = field_map json "SnapshotId" String_.of_json in
      make ?isPermanentRestore ?restoreDuration ?restoreStartTime ?snapshotId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores an archived Amazon EBS snapshot for use temporarily or permanently, or modifies the restore period or restore type for a snapshot that was previously temporarily restored. For more information see Restore an archived snapshot and modify the restore period or restore type for a temporarily restored snapshot in the Amazon Elastic Compute Cloud User Guide."]
module RestoreSnapshotTierRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t
        [@ocaml.doc "The ID of the snapshot to restore."];
      temporaryRestoreDays:
        RestoreSnapshotTierRequestTemporaryRestoreDays.t option
        [@ocaml.doc
          "Specifies the number of days for which to temporarily restore an archived snapshot. Required for temporary restores only. The snapshot will be automatically re-archived after this period. To temporarily restore an archived snapshot, specify the number of days and omit the PermanentRestore parameter or set it to false."];
      permanentRestore: Boolean.t option
        [@ocaml.doc
          "Indicates whether to permanently restore an archived snapshot. To permanently restore an archived snapshot, specify true and omit the RestoreSnapshotTierRequest$TemporaryRestoreDays parameter."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RestoreSnapshotTierRequest"
    let make ?temporaryRestoreDays =
      fun ?permanentRestore ->
        fun ?dryRun ->
          fun ~snapshotId ->
            fun () ->
              { temporaryRestoreDays; permanentRestore; dryRun; snapshotId }
    let to_value x =
      structure_to_value
        [("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("TemporaryRestoreDays",
          (Option.map x.temporaryRestoreDays
             ~f:RestoreSnapshotTierRequestTemporaryRestoreDays.to_value));
        ("PermanentRestore",
          (Option.map x.permanentRestore ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let permanentRestore =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PermanentRestore") in
      let temporaryRestoreDays =
        (Option.map ~f:RestoreSnapshotTierRequestTemporaryRestoreDays.of_xml)
          (Xml.child xml_arg0 "TemporaryRestoreDays") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      make ?dryRun ?permanentRestore ?temporaryRestoreDays ~snapshotId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let permanentRestore =
        field_map json "PermanentRestore" Boolean.of_json in
      let temporaryRestoreDays =
        field_map json "TemporaryRestoreDays"
          RestoreSnapshotTierRequestTemporaryRestoreDays.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ?dryRun ?permanentRestore ?temporaryRestoreDays ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores an archived Amazon EBS snapshot for use temporarily or permanently, or modifies the restore period or restore type for a snapshot that was previously temporarily restored. For more information see Restore an archived snapshot and modify the restore period or restore type for a temporarily restored snapshot in the Amazon Elastic Compute Cloud User Guide."]
module RestoreSnapshotFromRecycleBinResult =
  struct
    type nonrec t =
      {
      snapshotId: String_.t option [@ocaml.doc "The ID of the snapshot."];
      outpostArn: String_.t option
        [@ocaml.doc
          "The ARN of the Outpost on which the snapshot is stored. For more information, see Amazon EBS local snapshots on Outposts in the Amazon Elastic Compute Cloud User Guide."];
      description: String_.t option
        [@ocaml.doc "The description for the snapshot."];
      encrypted: Boolean.t option
        [@ocaml.doc "Indicates whether the snapshot is encrypted."];
      ownerId: String_.t option
        [@ocaml.doc
          "The ID of the Amazon Web Services account that owns the EBS snapshot."];
      progress: String_.t option
        [@ocaml.doc "The progress of the snapshot, as a percentage."];
      startTime: MillisecondDateTime.t option
        [@ocaml.doc "The time stamp when the snapshot was initiated."];
      state: SnapshotState.t option [@ocaml.doc "The state of the snapshot."];
      volumeId: String_.t option
        [@ocaml.doc
          "The ID of the volume that was used to create the snapshot."];
      volumeSize: Integer.t option
        [@ocaml.doc "The size of the volume, in GiB."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshotId =
      fun ?outpostArn ->
        fun ?description ->
          fun ?encrypted ->
            fun ?ownerId ->
              fun ?progress ->
                fun ?startTime ->
                  fun ?state ->
                    fun ?volumeId ->
                      fun ?volumeSize ->
                        fun () ->
                          {
                            snapshotId;
                            outpostArn;
                            description;
                            encrypted;
                            ownerId;
                            progress;
                            startTime;
                            state;
                            volumeId;
                            volumeSize
                          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotId", (Option.map x.snapshotId ~f:String_.to_value));
        ("outpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("ownerId", (Option.map x.ownerId ~f:String_.to_value));
        ("progress", (Option.map x.progress ~f:String_.to_value));
        ("startTime",
          (Option.map x.startTime ~f:MillisecondDateTime.to_value));
        ("status", (Option.map x.state ~f:SnapshotState.to_value));
        ("volumeId", (Option.map x.volumeId ~f:String_.to_value));
        ("volumeSize", (Option.map x.volumeSize ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeSize =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "volumeSize") in
      let volumeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "volumeId") in
      let state =
        (Option.map ~f:SnapshotState.of_xml) (Xml.child xml_arg0 "status") in
      let startTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let progress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "progress") in
      let ownerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ownerId") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "outpostArn") in
      let snapshotId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "snapshotId") in
      make ?volumeSize ?volumeId ?state ?startTime ?progress ?ownerId
        ?encrypted ?description ?outpostArn ?snapshotId ()
    let of_json json =
      let volumeSize = field_map json "VolumeSize" Integer.of_json in
      let volumeId = field_map json "VolumeId" String_.of_json in
      let state = field_map json "State" SnapshotState.of_json in
      let startTime = field_map json "StartTime" MillisecondDateTime.of_json in
      let progress = field_map json "Progress" String_.of_json in
      let ownerId = field_map json "OwnerId" String_.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let outpostArn = field_map json "OutpostArn" String_.of_json in
      let snapshotId = field_map json "SnapshotId" String_.of_json in
      make ?volumeSize ?volumeId ?state ?startTime ?progress ?ownerId
        ?encrypted ?description ?outpostArn ?snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores a snapshot from the Recycle Bin. For more information, see Restore snapshots from the Recycle Bin in the Amazon Elastic Compute Cloud User Guide."]
module RestoreSnapshotFromRecycleBinRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t
        [@ocaml.doc "The ID of the snapshot to restore."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RestoreSnapshotFromRecycleBinRequest"
    let make ?dryRun = fun ~snapshotId -> fun () -> { dryRun; snapshotId }
    let to_value x =
      structure_to_value
        [("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      make ?dryRun ~snapshotId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ?dryRun ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores a snapshot from the Recycle Bin. For more information, see Restore snapshots from the Recycle Bin in the Amazon Elastic Compute Cloud User Guide."]
module RestoreManagedPrefixListVersionResult =
  struct
    type nonrec t =
      {
      prefixList: ManagedPrefixList.t option
        [@ocaml.doc "Information about the prefix list."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixList = fun () -> { prefixList }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixList",
           (Option.map x.prefixList ~f:ManagedPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixList =
        (Option.map ~f:ManagedPrefixList.of_xml)
          (Xml.child xml_arg0 "prefixList") in
      make ?prefixList ()
    let of_json json =
      let prefixList = field_map json "PrefixList" ManagedPrefixList.of_json in
      make ?prefixList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores the entries from a previous version of a managed prefix list to a new version of the prefix list."]
module RestoreManagedPrefixListVersionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."];
      previousVersion: Long.t [@ocaml.doc "The version to restore."];
      currentVersion: Long.t
        [@ocaml.doc "The current version number for the prefix list."]}
    let context_ = "RestoreManagedPrefixListVersionRequest"
    let make ?dryRun =
      fun ~prefixListId ->
        fun ~previousVersion ->
          fun ~currentVersion ->
            fun () ->
              { dryRun; prefixListId; previousVersion; currentVersion }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("PreviousVersion", (Some (Long.to_value x.previousVersion)));
        ("CurrentVersion", (Some (Long.to_value x.currentVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let currentVersion =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CurrentVersion") in
      let previousVersion =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PreviousVersion") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~currentVersion ~previousVersion ~prefixListId ?dryRun ()
    let of_json json =
      let currentVersion = field_map_exn json "CurrentVersion" Long.of_json in
      let previousVersion = field_map_exn json "PreviousVersion" Long.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~currentVersion ~previousVersion ~prefixListId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores the entries from a previous version of a managed prefix list to a new version of the prefix list."]
module RestoreImageFromRecycleBinResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores an AMI from the Recycle Bin. For more information, see Recycle Bin in the Amazon Elastic Compute Cloud User Guide."]
module RestoreImageFromRecycleBinRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t [@ocaml.doc "The ID of the AMI to restore."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RestoreImageFromRecycleBinRequest"
    let make ?dryRun = fun ~imageId -> fun () -> { dryRun; imageId }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ~imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores an AMI from the Recycle Bin. For more information, see Recycle Bin in the Amazon Elastic Compute Cloud User Guide."]
module RestoreAddressToClassicResult =
  struct
    type nonrec t =
      {
      publicIp: String_.t option [@ocaml.doc "The Elastic IP address."];
      status: Status.t option
        [@ocaml.doc "The move status for the IP address."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?publicIp = fun ?status -> fun () -> { publicIp; status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("publicIp", (Option.map x.publicIp ~f:String_.to_value));
        ("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "publicIp") in
      make ?status ?publicIp ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let publicIp = field_map json "PublicIp" String_.of_json in
      make ?status ?publicIp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores an Elastic IP address that was previously moved to the EC2-VPC platform back to the EC2-Classic platform. You cannot move an Elastic IP address that was originally allocated for use in EC2-VPC. The Elastic IP address must not be associated with an instance or network interface."]
module RestoreAddressToClassicRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      publicIp: String_.t [@ocaml.doc "The Elastic IP address."]}
    let context_ = "RestoreAddressToClassicRequest"
    let make ?dryRun = fun ~publicIp -> fun () -> { dryRun; publicIp }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("publicIp", (Some (String_.to_value x.publicIp)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let publicIp =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "publicIp") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~publicIp ?dryRun ()
    let of_json json =
      let publicIp = field_map_exn json "PublicIp" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~publicIp ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores an Elastic IP address that was previously moved to the EC2-VPC platform back to the EC2-Classic platform. You cannot move an Elastic IP address that was originally allocated for use in EC2-VPC. The Elastic IP address must not be associated with an instance or network interface."]
module ResetSnapshotAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: SnapshotAttributeName.t
        [@ocaml.doc
          "The attribute to reset. Currently, only the attribute for permission to create volumes can be reset."];
      snapshotId: SnapshotId.t [@ocaml.doc "The ID of the snapshot."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ResetSnapshotAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~snapshotId -> fun () -> { dryRun; attribute; snapshotId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (SnapshotAttributeName.to_value x.attribute)));
        ("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      let attribute =
        SnapshotAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~snapshotId ~attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      let attribute =
        field_map_exn json "Attribute" SnapshotAttributeName.of_json in
      make ?dryRun ~snapshotId ~attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets permission settings for the specified snapshot. For more information about modifying snapshot permissions, see Share a snapshot in the Amazon Elastic Compute Cloud User Guide."]
module ResetNetworkInterfaceAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      sourceDestCheck: String_.t option
        [@ocaml.doc
          "The source/destination checking attribute. Resets the value to true."]}
    let context_ = "ResetNetworkInterfaceAttributeRequest"
    let make ?dryRun =
      fun ?sourceDestCheck ->
        fun ~networkInterfaceId ->
          fun () -> { dryRun; sourceDestCheck; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("sourceDestCheck",
          (Option.map x.sourceDestCheck ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceDestCheck =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceDestCheck") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?sourceDestCheck ~networkInterfaceId ?dryRun ()
    let of_json json =
      let sourceDestCheck = field_map json "SourceDestCheck" String_.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?sourceDestCheck ~networkInterfaceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for ResetNetworkInterfaceAttribute."]
module ResetInstanceAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: InstanceAttributeName.t
        [@ocaml.doc
          "The attribute to reset. You can only reset the following attributes: kernel | ramdisk | sourceDestCheck."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."]}
    let context_ = "ResetInstanceAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~instanceId -> fun () -> { dryRun; attribute; instanceId }
    let to_value x =
      structure_to_value
        [("attribute", (Some (InstanceAttributeName.to_value x.attribute)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let attribute =
        InstanceAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attribute") in
      make ~instanceId ?dryRun ~attribute ()
    let of_json json =
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let attribute =
        field_map_exn json "Attribute" InstanceAttributeName.of_json in
      make ~instanceId ?dryRun ~attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets an attribute of an instance to its default value. To reset the kernel or ramdisk, the instance must be in a stopped state. To reset the sourceDestCheck, the instance can be either running or stopped. The sourceDestCheck attribute controls whether source/destination checking is enabled. The default value is true, which means checking is enabled. This value must be false for a NAT instance to perform NAT. For more information, see NAT Instances in the Amazon VPC User Guide."]
module ResetImageAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: ResetImageAttributeName.t
        [@ocaml.doc
          "The attribute to reset (currently you can only reset the launch permission attribute)."];
      imageId: ImageId.t [@ocaml.doc "The ID of the AMI."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ResetImageAttributeRequest"
    let make ?dryRun =
      fun ~attribute ->
        fun ~imageId -> fun () -> { dryRun; attribute; imageId }
    let to_value x =
      structure_to_value
        [("Attribute", (Some (ResetImageAttributeName.to_value x.attribute)));
        ("ImageId", (Some (ImageId.to_value x.imageId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      let attribute =
        ResetImageAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      make ?dryRun ~imageId ~attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      let attribute =
        field_map_exn json "Attribute" ResetImageAttributeName.of_json in
      make ?dryRun ~imageId ~attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for ResetImageAttribute."]
module ResetFpgaImageAttributeResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds, and an error otherwise."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the specified attribute of the specified Amazon FPGA Image (AFI) to its default value. You can only reset the load permission attribute."]
module ResetFpgaImageAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fpgaImageId: FpgaImageId.t [@ocaml.doc "The ID of the AFI."];
      attribute: ResetFpgaImageAttributeName.t option
        [@ocaml.doc "The attribute."]}
    let context_ = "ResetFpgaImageAttributeRequest"
    let make ?dryRun =
      fun ?attribute ->
        fun ~fpgaImageId -> fun () -> { dryRun; attribute; fpgaImageId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId", (Some (FpgaImageId.to_value x.fpgaImageId)));
        ("Attribute",
          (Option.map x.attribute ~f:ResetFpgaImageAttributeName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attribute =
        (Option.map ~f:ResetFpgaImageAttributeName.of_xml)
          (Xml.child xml_arg0 "Attribute") in
      let fpgaImageId =
        FpgaImageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?attribute ~fpgaImageId ?dryRun ()
    let of_json json =
      let attribute =
        field_map json "Attribute" ResetFpgaImageAttributeName.of_json in
      let fpgaImageId = field_map_exn json "FpgaImageId" FpgaImageId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?attribute ~fpgaImageId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the specified attribute of the specified Amazon FPGA Image (AFI) to its default value. You can only reset the load permission attribute."]
module ResetEbsDefaultKmsKeyIdResult =
  struct
    type nonrec t =
      {
      kmsKeyId: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the default KMS key for EBS encryption by default."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?kmsKeyId = fun () -> { kmsKeyId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("kmsKeyId", (Option.map x.kmsKeyId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      make ?kmsKeyId ()
    let of_json json =
      let kmsKeyId = field_map json "KmsKeyId" String_.of_json in
      make ?kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the default KMS key for EBS encryption for your account in this Region to the Amazon Web Services managed KMS key for EBS. After resetting the default KMS key to the Amazon Web Services managed KMS key, you can continue to encrypt by a customer managed KMS key by specifying it when you create the volume. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module ResetEbsDefaultKmsKeyIdRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the default KMS key for EBS encryption for your account in this Region to the Amazon Web Services managed KMS key for EBS. After resetting the default KMS key to the Amazon Web Services managed KMS key, you can continue to encrypt by a customer managed KMS key by specifying it when you create the volume. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module ResetAddressAttributeResult =
  struct
    type nonrec t =
      {
      address: AddressAttribute.t option
        [@ocaml.doc "Information about the IP address."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?address = fun () -> { address }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("address", (Option.map x.address ~f:AddressAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let address =
        (Option.map ~f:AddressAttribute.of_xml)
          (Xml.child xml_arg0 "address") in
      make ?address ()
    let of_json json =
      let address = field_map json "Address" AddressAttribute.of_json in
      make ?address ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the attribute of the specified IP address. For requirements, see Using reverse DNS for email applications."]
module ResetAddressAttributeRequest =
  struct
    type nonrec t =
      {
      allocationId: AllocationId.t
        [@ocaml.doc "\\[EC2-VPC\\] The allocation ID."];
      attribute: AddressAttributeName.t
        [@ocaml.doc "The attribute of the IP address."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ResetAddressAttributeRequest"
    let make ?dryRun =
      fun ~allocationId ->
        fun ~attribute -> fun () -> { dryRun; allocationId; attribute }
    let to_value x =
      structure_to_value
        [("AllocationId", (Some (AllocationId.to_value x.allocationId)));
        ("Attribute", (Some (AddressAttributeName.to_value x.attribute)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let attribute =
        AddressAttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Attribute") in
      let allocationId =
        AllocationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AllocationId") in
      make ?dryRun ~attribute ~allocationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let attribute =
        field_map_exn json "Attribute" AddressAttributeName.of_json in
      let allocationId =
        field_map_exn json "AllocationId" AllocationId.of_json in
      make ?dryRun ~attribute ~allocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the attribute of the specified IP address. For requirements, see Using reverse DNS for email applications."]
module RequestSpotInstancesResult =
  struct
    type nonrec t =
      {
      spotInstanceRequests: SpotInstanceRequestList.t option
        [@ocaml.doc "One or more Spot Instance requests."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotInstanceRequests = fun () -> { spotInstanceRequests }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotInstanceRequestSet",
           (Option.map x.spotInstanceRequests
              ~f:SpotInstanceRequestList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotInstanceRequests =
        (Option.map ~f:SpotInstanceRequestList.of_xml)
          (Xml.child xml_arg0 "spotInstanceRequestSet") in
      make ?spotInstanceRequests ()
    let of_json json =
      let spotInstanceRequests =
        field_map json "SpotInstanceRequests" SpotInstanceRequestList.of_json in
      make ?spotInstanceRequests ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of RequestSpotInstances."]
module RequestSpotInstancesRequest =
  struct
    type nonrec t =
      {
      availabilityZoneGroup: String_.t option
        [@ocaml.doc
          "The user-specified name for a logical grouping of requests. When you specify an Availability Zone group in a Spot Instance request, all Spot Instances in the request are launched in the same Availability Zone. Instance proximity is maintained with this parameter, but the choice of Availability Zone is not. The group applies only to requests for Spot Instances of the same instance type. Any additional Spot Instance requests that are specified with the same Availability Zone group name are launched in that same Availability Zone, as long as at least one instance from the group is still active. If there is no active instance running in the Availability Zone group that you specify for a new Spot Instance request (all instances are terminated, the request is expired, or the maximum price you specified falls below current Spot price), then Amazon EC2 launches the instance in any Availability Zone where the constraint can be met. Consequently, the subsequent set of Spot Instances could be placed in a different zone from the original request, even if you specified the same Availability Zone group. Default: Instances are launched in any available Availability Zone."];
      blockDurationMinutes: Integer.t option [@ocaml.doc "Deprecated."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to Ensure Idempotency in the Amazon EC2 User Guide for Linux Instances."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceCount: Integer.t option
        [@ocaml.doc
          "The maximum number of Spot Instances to launch. Default: 1"];
      launchGroup: String_.t option
        [@ocaml.doc
          "The instance launch group. Launch groups are Spot Instances that launch together and terminate together. Default: Instances are launched and terminated individually"];
      launchSpecification: RequestSpotLaunchSpecification.t option
        [@ocaml.doc "The launch specification."];
      spotPrice: String_.t option
        [@ocaml.doc
          "The maximum price per hour that you are willing to pay for a Spot Instance. The default is the On-Demand price."];
      type_: SpotInstanceType.t option
        [@ocaml.doc "The Spot Instance request type. Default: one-time"];
      validFrom: DateTime.t option
        [@ocaml.doc
          "The start date of the request. If this is a one-time request, the request becomes active at this date and time and remains active until all instances launch, the request expires, or the request is canceled. If the request is persistent, the request becomes active at this date and time and remains active until it expires or is canceled. The specified start date and time cannot be equal to the current date and time. You must specify a start date and time that occurs after the current date and time."];
      validUntil: DateTime.t option
        [@ocaml.doc
          "The end date of the request, in UTC format (YYYY-MM-DDTHH:MM:SSZ). For a persistent request, the request remains active until the ValidUntil date and time is reached. Otherwise, the request remains active until you cancel it. For a one-time request, the request remains active until all instances launch, the request is canceled, or the ValidUntil date and time is reached. By default, the request is valid for 7 days from the date the request was created."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The key-value pair for tagging the Spot Instance request on creation. The value for ResourceType must be spot-instances-request, otherwise the Spot Instance request fails. To tag the Spot Instance request after it has been created, see CreateTags."];
      instanceInterruptionBehavior: InstanceInterruptionBehavior.t option
        [@ocaml.doc
          "The behavior when a Spot Instance is interrupted. The default is terminate."]}
    let make ?availabilityZoneGroup =
      fun ?blockDurationMinutes ->
        fun ?clientToken ->
          fun ?dryRun ->
            fun ?instanceCount ->
              fun ?launchGroup ->
                fun ?launchSpecification ->
                  fun ?spotPrice ->
                    fun ?type_ ->
                      fun ?validFrom ->
                        fun ?validUntil ->
                          fun ?tagSpecifications ->
                            fun ?instanceInterruptionBehavior ->
                              fun () ->
                                {
                                  availabilityZoneGroup;
                                  blockDurationMinutes;
                                  clientToken;
                                  dryRun;
                                  instanceCount;
                                  launchGroup;
                                  launchSpecification;
                                  spotPrice;
                                  type_;
                                  validFrom;
                                  validUntil;
                                  tagSpecifications;
                                  instanceInterruptionBehavior
                                }
    let to_value x =
      structure_to_value
        [("availabilityZoneGroup",
           (Option.map x.availabilityZoneGroup ~f:String_.to_value));
        ("blockDurationMinutes",
          (Option.map x.blockDurationMinutes ~f:Integer.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceCount", (Option.map x.instanceCount ~f:Integer.to_value));
        ("launchGroup", (Option.map x.launchGroup ~f:String_.to_value));
        ("LaunchSpecification",
          (Option.map x.launchSpecification
             ~f:RequestSpotLaunchSpecification.to_value));
        ("spotPrice", (Option.map x.spotPrice ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:SpotInstanceType.to_value));
        ("validFrom", (Option.map x.validFrom ~f:DateTime.to_value));
        ("validUntil", (Option.map x.validUntil ~f:DateTime.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("InstanceInterruptionBehavior",
          (Option.map x.instanceInterruptionBehavior
             ~f:InstanceInterruptionBehavior.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceInterruptionBehavior =
        (Option.map ~f:InstanceInterruptionBehavior.of_xml)
          (Xml.child xml_arg0 "InstanceInterruptionBehavior") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let validUntil =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "validUntil") in
      let validFrom =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "validFrom") in
      let type_ =
        (Option.map ~f:SpotInstanceType.of_xml) (Xml.child xml_arg0 "type") in
      let spotPrice =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "spotPrice") in
      let launchSpecification =
        (Option.map ~f:RequestSpotLaunchSpecification.of_xml)
          (Xml.child xml_arg0 "LaunchSpecification") in
      let launchGroup =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "launchGroup") in
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "instanceCount") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let blockDurationMinutes =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "blockDurationMinutes") in
      let availabilityZoneGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "availabilityZoneGroup") in
      make ?instanceInterruptionBehavior ?tagSpecifications ?validUntil
        ?validFrom ?type_ ?spotPrice ?launchSpecification ?launchGroup
        ?instanceCount ?dryRun ?clientToken ?blockDurationMinutes
        ?availabilityZoneGroup ()
    let of_json json =
      let instanceInterruptionBehavior =
        field_map json "InstanceInterruptionBehavior"
          InstanceInterruptionBehavior.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let validUntil = field_map json "ValidUntil" DateTime.of_json in
      let validFrom = field_map json "ValidFrom" DateTime.of_json in
      let type_ = field_map json "Type" SpotInstanceType.of_json in
      let spotPrice = field_map json "SpotPrice" String_.of_json in
      let launchSpecification =
        field_map json "LaunchSpecification"
          RequestSpotLaunchSpecification.of_json in
      let launchGroup = field_map json "LaunchGroup" String_.of_json in
      let instanceCount = field_map json "InstanceCount" Integer.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let blockDurationMinutes =
        field_map json "BlockDurationMinutes" Integer.of_json in
      let availabilityZoneGroup =
        field_map json "AvailabilityZoneGroup" String_.of_json in
      make ?instanceInterruptionBehavior ?tagSpecifications ?validUntil
        ?validFrom ?type_ ?spotPrice ?launchSpecification ?launchGroup
        ?instanceCount ?dryRun ?clientToken ?blockDurationMinutes
        ?availabilityZoneGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for RequestSpotInstances."]
module RequestSpotFleetResponse =
  struct
    type nonrec t =
      {
      spotFleetRequestId: String_.t option
        [@ocaml.doc "The ID of the Spot Fleet request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotFleetRequestId = fun () -> { spotFleetRequestId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotFleetRequestId",
           (Option.map x.spotFleetRequestId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotFleetRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "spotFleetRequestId") in
      make ?spotFleetRequestId ()
    let of_json json =
      let spotFleetRequestId =
        field_map json "SpotFleetRequestId" String_.of_json in
      make ?spotFleetRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of RequestSpotFleet."]
module RequestSpotFleetRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      spotFleetRequestConfig: SpotFleetRequestConfigData.t
        [@ocaml.doc "The configuration for the Spot Fleet request."]}
    let context_ = "RequestSpotFleetRequest"
    let make ?dryRun =
      fun ~spotFleetRequestConfig ->
        fun () -> { dryRun; spotFleetRequestConfig }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("spotFleetRequestConfig",
          (Some
             (SpotFleetRequestConfigData.to_value x.spotFleetRequestConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotFleetRequestConfig =
        SpotFleetRequestConfigData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "spotFleetRequestConfig") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~spotFleetRequestConfig ?dryRun ()
    let of_json json =
      let spotFleetRequestConfig =
        field_map_exn json "SpotFleetRequestConfig"
          SpotFleetRequestConfigData.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~spotFleetRequestConfig ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for RequestSpotFleet."]
module ReportInstanceStatusRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc
          "Descriptive text about the health state of your instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      endTime: DateTime.t option
        [@ocaml.doc
          "The time at which the reported instance health state ended."];
      instances: InstanceIdStringList.t [@ocaml.doc "The instances."];
      reasonCodes: ReasonCodesList.t
        [@ocaml.doc
          "The reason codes that describe the health state of your instance. instance-stuck-in-state: My instance is stuck in a state. unresponsive: My instance is unresponsive. not-accepting-credentials: My instance is not accepting my credentials. password-not-available: A password is not available for my instance. performance-network: My instance is experiencing performance problems that I believe are network related. performance-instance-store: My instance is experiencing performance problems that I believe are related to the instance stores. performance-ebs-volume: My instance is experiencing performance problems that I believe are related to an EBS volume. performance-other: My instance is experiencing performance problems. other: \\[explain using the description parameter\\]"];
      startTime: DateTime.t option
        [@ocaml.doc
          "The time at which the reported instance health state began."];
      status: ReportStatusType.t
        [@ocaml.doc "The status of all instances listed."]}
    let context_ = "ReportInstanceStatusRequest"
    let make ?description =
      fun ?dryRun ->
        fun ?endTime ->
          fun ?startTime ->
            fun ~instances ->
              fun ~reasonCodes ->
                fun ~status ->
                  fun () ->
                    {
                      description;
                      dryRun;
                      endTime;
                      startTime;
                      instances;
                      reasonCodes;
                      status
                    }
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("endTime", (Option.map x.endTime ~f:DateTime.to_value));
        ("instanceId", (Some (InstanceIdStringList.to_value x.instances)));
        ("reasonCode", (Some (ReasonCodesList.to_value x.reasonCodes)));
        ("startTime", (Option.map x.startTime ~f:DateTime.to_value));
        ("status", (Some (ReportStatusType.to_value x.status)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        ReportStatusType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let startTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "startTime") in
      let reasonCodes =
        ReasonCodesList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reasonCode") in
      let instances =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let endTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "endTime") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ~status ?startTime ~reasonCodes ~instances ?endTime ?dryRun
        ?description ()
    let of_json json =
      let status = field_map_exn json "Status" ReportStatusType.of_json in
      let startTime = field_map json "StartTime" DateTime.of_json in
      let reasonCodes =
        field_map_exn json "ReasonCodes" ReasonCodesList.of_json in
      let instances =
        field_map_exn json "Instances" InstanceIdStringList.of_json in
      let endTime = field_map json "EndTime" DateTime.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      make ~status ?startTime ~reasonCodes ~instances ?endTime ?dryRun
        ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Submits feedback about the status of an instance. The instance must be in the running state. If your experience with the instance differs from the instance status returned by DescribeInstanceStatus, use ReportInstanceStatus to report your experience with the instance. Amazon EC2 collects this information to improve the accuracy of status checks. Use of this action does not change the value returned by DescribeInstanceStatus."]
module ReplaceTransitGatewayRouteResult =
  struct
    type nonrec t =
      {
      route: TransitGatewayRoute.t option
        [@ocaml.doc "Information about the modified route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:TransitGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:TransitGatewayRoute.of_xml)
          (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_json json =
      let route = field_map json "Route" TransitGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces the specified route in the specified transit gateway route table."]
module ReplaceTransitGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The CIDR range used for the destination match. Routing decisions are based on the most specific match."];
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the route table."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc "The ID of the attachment."];
      blackhole: Boolean.t option
        [@ocaml.doc
          "Indicates whether traffic matching this route is to be dropped."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ReplaceTransitGatewayRouteRequest"
    let make ?transitGatewayAttachmentId =
      fun ?blackhole ->
        fun ?dryRun ->
          fun ~destinationCidrBlock ->
            fun ~transitGatewayRouteTableId ->
              fun () ->
                {
                  transitGatewayAttachmentId;
                  blackhole;
                  dryRun;
                  destinationCidrBlock;
                  transitGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("TransitGatewayRouteTableId",
          (Some
             (TransitGatewayRouteTableId.to_value
                x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("Blackhole", (Option.map x.blackhole ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let blackhole =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Blackhole") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId
        ~transitGatewayRouteTableId ~destinationCidrBlock ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let blackhole = field_map json "Blackhole" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId
        ~transitGatewayRouteTableId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces the specified route in the specified transit gateway route table."]
module ReplaceRouteTableAssociationResult =
  struct
    type nonrec t =
      {
      newAssociationId: String_.t option
        [@ocaml.doc "The ID of the new association."];
      associationState: RouteTableAssociationState.t option
        [@ocaml.doc "The state of the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?newAssociationId =
      fun ?associationState ->
        fun () -> { newAssociationId; associationState }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("newAssociationId",
           (Option.map x.newAssociationId ~f:String_.to_value));
        ("associationState",
          (Option.map x.associationState
             ~f:RouteTableAssociationState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationState =
        (Option.map ~f:RouteTableAssociationState.of_xml)
          (Xml.child xml_arg0 "associationState") in
      let newAssociationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "newAssociationId") in
      make ?associationState ?newAssociationId ()
    let of_json json =
      let associationState =
        field_map json "AssociationState" RouteTableAssociationState.of_json in
      let newAssociationId =
        field_map json "NewAssociationId" String_.of_json in
      make ?associationState ?newAssociationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the route table associated with a given subnet, internet gateway, or virtual private gateway in a VPC. After the operation completes, the subnet or gateway uses the routes in the new route table. For more information about route tables, see Route tables in the Amazon Virtual Private Cloud User Guide. You can also use this operation to change which table is the main route table in the VPC. Specify the main route table's association ID and the route table ID of the new main route table."]
module ReplaceRouteTableAssociationRequest =
  struct
    type nonrec t =
      {
      associationId: RouteTableAssociationId.t
        [@ocaml.doc "The association ID."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      routeTableId: RouteTableId.t
        [@ocaml.doc
          "The ID of the new route table to associate with the subnet."]}
    let context_ = "ReplaceRouteTableAssociationRequest"
    let make ?dryRun =
      fun ~associationId ->
        fun ~routeTableId ->
          fun () -> { dryRun; associationId; routeTableId }
    let to_value x =
      structure_to_value
        [("associationId",
           (Some (RouteTableAssociationId.to_value x.associationId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let associationId =
        RouteTableAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associationId") in
      make ~routeTableId ?dryRun ~associationId ()
    let of_json json =
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let associationId =
        field_map_exn json "AssociationId" RouteTableAssociationId.of_json in
      make ~routeTableId ?dryRun ~associationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the route table associated with a given subnet, internet gateway, or virtual private gateway in a VPC. After the operation completes, the subnet or gateway uses the routes in the new route table. For more information about route tables, see Route tables in the Amazon Virtual Private Cloud User Guide. You can also use this operation to change which table is the main route table in the VPC. Specify the main route table's association ID and the route table ID of the new main route table."]
module ReplaceRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 CIDR address block used for the destination match. The value that you provide must match the CIDR of an existing route in the table."];
      destinationIpv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 CIDR address block used for the destination match. The value that you provide must match the CIDR of an existing route in the table."];
      destinationPrefixListId: PrefixListResourceId.t option
        [@ocaml.doc "The ID of the prefix list for the route."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcEndpointId: VpcEndpointId.t option
        [@ocaml.doc
          "The ID of a VPC endpoint. Supported for Gateway Load Balancer endpoints only."];
      egressOnlyInternetGatewayId: EgressOnlyInternetGatewayId.t option
        [@ocaml.doc
          "\\[IPv6 traffic only\\] The ID of an egress-only internet gateway."];
      gatewayId: RouteGatewayId.t option
        [@ocaml.doc
          "The ID of an internet gateway or virtual private gateway."];
      instanceId: InstanceId.t option
        [@ocaml.doc "The ID of a NAT instance in your VPC."];
      localTarget: Boolean.t option
        [@ocaml.doc
          "Specifies whether to reset the local route to its default target (local)."];
      natGatewayId: NatGatewayId.t option
        [@ocaml.doc "\\[IPv4 traffic only\\] The ID of a NAT gateway."];
      transitGatewayId: TransitGatewayId.t option
        [@ocaml.doc "The ID of a transit gateway."];
      localGatewayId: LocalGatewayId.t option
        [@ocaml.doc "The ID of the local gateway."];
      carrierGatewayId: CarrierGatewayId.t option
        [@ocaml.doc "\\[IPv4 traffic only\\] The ID of a carrier gateway."];
      networkInterfaceId: NetworkInterfaceId.t option
        [@ocaml.doc "The ID of a network interface."];
      routeTableId: RouteTableId.t [@ocaml.doc "The ID of the route table."];
      vpcPeeringConnectionId: VpcPeeringConnectionId.t option
        [@ocaml.doc "The ID of a VPC peering connection."];
      coreNetworkArn: CoreNetworkArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the core network."]}
    let context_ = "ReplaceRouteRequest"
    let make ?destinationCidrBlock =
      fun ?destinationIpv6CidrBlock ->
        fun ?destinationPrefixListId ->
          fun ?dryRun ->
            fun ?vpcEndpointId ->
              fun ?egressOnlyInternetGatewayId ->
                fun ?gatewayId ->
                  fun ?instanceId ->
                    fun ?localTarget ->
                      fun ?natGatewayId ->
                        fun ?transitGatewayId ->
                          fun ?localGatewayId ->
                            fun ?carrierGatewayId ->
                              fun ?networkInterfaceId ->
                                fun ?vpcPeeringConnectionId ->
                                  fun ?coreNetworkArn ->
                                    fun ~routeTableId ->
                                      fun () ->
                                        {
                                          destinationCidrBlock;
                                          destinationIpv6CidrBlock;
                                          destinationPrefixListId;
                                          dryRun;
                                          vpcEndpointId;
                                          egressOnlyInternetGatewayId;
                                          gatewayId;
                                          instanceId;
                                          localTarget;
                                          natGatewayId;
                                          transitGatewayId;
                                          localGatewayId;
                                          carrierGatewayId;
                                          networkInterfaceId;
                                          vpcPeeringConnectionId;
                                          coreNetworkArn;
                                          routeTableId
                                        }
    let to_value x =
      structure_to_value
        [("destinationCidrBlock",
           (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("destinationIpv6CidrBlock",
          (Option.map x.destinationIpv6CidrBlock ~f:String_.to_value));
        ("DestinationPrefixListId",
          (Option.map x.destinationPrefixListId
             ~f:PrefixListResourceId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointId ~f:VpcEndpointId.to_value));
        ("egressOnlyInternetGatewayId",
          (Option.map x.egressOnlyInternetGatewayId
             ~f:EgressOnlyInternetGatewayId.to_value));
        ("gatewayId", (Option.map x.gatewayId ~f:RouteGatewayId.to_value));
        ("instanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("LocalTarget", (Option.map x.localTarget ~f:Boolean.to_value));
        ("natGatewayId",
          (Option.map x.natGatewayId ~f:NatGatewayId.to_value));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("LocalGatewayId",
          (Option.map x.localGatewayId ~f:LocalGatewayId.to_value));
        ("CarrierGatewayId",
          (Option.map x.carrierGatewayId ~f:CarrierGatewayId.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("vpcPeeringConnectionId",
          (Option.map x.vpcPeeringConnectionId
             ~f:VpcPeeringConnectionId.to_value));
        ("CoreNetworkArn",
          (Option.map x.coreNetworkArn ~f:CoreNetworkArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coreNetworkArn =
        (Option.map ~f:CoreNetworkArn.of_xml)
          (Xml.child xml_arg0 "CoreNetworkArn") in
      let vpcPeeringConnectionId =
        (Option.map ~f:VpcPeeringConnectionId.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnectionId") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let carrierGatewayId =
        (Option.map ~f:CarrierGatewayId.of_xml)
          (Xml.child xml_arg0 "CarrierGatewayId") in
      let localGatewayId =
        (Option.map ~f:LocalGatewayId.of_xml)
          (Xml.child xml_arg0 "LocalGatewayId") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let natGatewayId =
        (Option.map ~f:NatGatewayId.of_xml)
          (Xml.child xml_arg0 "natGatewayId") in
      let localTarget =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "LocalTarget") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "instanceId") in
      let gatewayId =
        (Option.map ~f:RouteGatewayId.of_xml)
          (Xml.child xml_arg0 "gatewayId") in
      let egressOnlyInternetGatewayId =
        (Option.map ~f:EgressOnlyInternetGatewayId.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGatewayId") in
      let vpcEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      let destinationIpv6CidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationIpv6CidrBlock") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationCidrBlock") in
      make ?coreNetworkArn ?vpcPeeringConnectionId ~routeTableId
        ?networkInterfaceId ?carrierGatewayId ?localGatewayId
        ?transitGatewayId ?natGatewayId ?localTarget ?instanceId ?gatewayId
        ?egressOnlyInternetGatewayId ?vpcEndpointId ?dryRun
        ?destinationPrefixListId ?destinationIpv6CidrBlock
        ?destinationCidrBlock ()
    let of_json json =
      let coreNetworkArn =
        field_map json "CoreNetworkArn" CoreNetworkArn.of_json in
      let vpcPeeringConnectionId =
        field_map json "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let carrierGatewayId =
        field_map json "CarrierGatewayId" CarrierGatewayId.of_json in
      let localGatewayId =
        field_map json "LocalGatewayId" LocalGatewayId.of_json in
      let transitGatewayId =
        field_map json "TransitGatewayId" TransitGatewayId.of_json in
      let natGatewayId = field_map json "NatGatewayId" NatGatewayId.of_json in
      let localTarget = field_map json "LocalTarget" Boolean.of_json in
      let instanceId = field_map json "InstanceId" InstanceId.of_json in
      let gatewayId = field_map json "GatewayId" RouteGatewayId.of_json in
      let egressOnlyInternetGatewayId =
        field_map json "EgressOnlyInternetGatewayId"
          EgressOnlyInternetGatewayId.of_json in
      let vpcEndpointId =
        field_map json "VpcEndpointId" VpcEndpointId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let destinationPrefixListId =
        field_map json "DestinationPrefixListId" PrefixListResourceId.of_json in
      let destinationIpv6CidrBlock =
        field_map json "DestinationIpv6CidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map json "DestinationCidrBlock" String_.of_json in
      make ?coreNetworkArn ?vpcPeeringConnectionId ~routeTableId
        ?networkInterfaceId ?carrierGatewayId ?localGatewayId
        ?transitGatewayId ?natGatewayId ?localTarget ?instanceId ?gatewayId
        ?egressOnlyInternetGatewayId ?vpcEndpointId ?dryRun
        ?destinationPrefixListId ?destinationIpv6CidrBlock
        ?destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces an existing route within a route table in a VPC. You must provide only one of the following: internet gateway, virtual private gateway, NAT instance, NAT gateway, VPC peering connection, network interface, egress-only internet gateway, or transit gateway. For more information, see Route tables in the Amazon Virtual Private Cloud User Guide."]
module ReplaceNetworkAclEntryRequest =
  struct
    type nonrec t =
      {
      cidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 network range to allow or deny, in CIDR notation (for example 172.16.0.0/24)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      egress: Boolean.t
        [@ocaml.doc
          "Indicates whether to replace the egress rule. Default: If no value is specified, we replace the ingress rule."];
      icmpTypeCode: IcmpTypeCode.t option
        [@ocaml.doc
          "ICMP protocol: The ICMP or ICMPv6 type and code. Required if specifying protocol 1 (ICMP) or protocol 58 (ICMPv6) with an IPv6 CIDR block."];
      ipv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 network range to allow or deny, in CIDR notation (for example 2001:bd8:1234:1a00::/64)."];
      networkAclId: NetworkAclId.t [@ocaml.doc "The ID of the ACL."];
      portRange: PortRange.t option
        [@ocaml.doc
          "TCP or UDP protocols: The range of ports the rule applies to. Required if specifying protocol 6 (TCP) or 17 (UDP)."];
      protocol: String_.t
        [@ocaml.doc
          "The protocol number. A value of \"-1\" means all protocols. If you specify \"-1\" or a protocol number other than \"6\" (TCP), \"17\" (UDP), or \"1\" (ICMP), traffic on all ports is allowed, regardless of any ports or ICMP types or codes that you specify. If you specify protocol \"58\" (ICMPv6) and specify an IPv4 CIDR block, traffic for all ICMP types and codes allowed, regardless of any that you specify. If you specify protocol \"58\" (ICMPv6) and specify an IPv6 CIDR block, you must specify an ICMP type and code."];
      ruleAction: RuleAction.t
        [@ocaml.doc
          "Indicates whether to allow or deny the traffic that matches the rule."];
      ruleNumber: Integer.t
        [@ocaml.doc "The rule number of the entry to replace."]}
    let context_ = "ReplaceNetworkAclEntryRequest"
    let make ?cidrBlock =
      fun ?dryRun ->
        fun ?icmpTypeCode ->
          fun ?ipv6CidrBlock ->
            fun ?portRange ->
              fun ~egress ->
                fun ~networkAclId ->
                  fun ~protocol ->
                    fun ~ruleAction ->
                      fun ~ruleNumber ->
                        fun () ->
                          {
                            cidrBlock;
                            dryRun;
                            icmpTypeCode;
                            ipv6CidrBlock;
                            portRange;
                            egress;
                            networkAclId;
                            protocol;
                            ruleAction;
                            ruleNumber
                          }
    let to_value x =
      structure_to_value
        [("cidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("egress", (Some (Boolean.to_value x.egress)));
        ("Icmp", (Option.map x.icmpTypeCode ~f:IcmpTypeCode.to_value));
        ("ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)));
        ("portRange", (Option.map x.portRange ~f:PortRange.to_value));
        ("protocol", (Some (String_.to_value x.protocol)));
        ("ruleAction", (Some (RuleAction.to_value x.ruleAction)));
        ("ruleNumber", (Some (Integer.to_value x.ruleNumber)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleNumber") in
      let ruleAction =
        RuleAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleAction") in
      let protocol =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "protocol") in
      let portRange =
        (Option.map ~f:PortRange.of_xml) (Xml.child xml_arg0 "portRange") in
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipv6CidrBlock") in
      let icmpTypeCode =
        (Option.map ~f:IcmpTypeCode.of_xml) (Xml.child xml_arg0 "Icmp") in
      let egress =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "egress") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cidrBlock") in
      make ~ruleNumber ~ruleAction ~protocol ?portRange ~networkAclId
        ?ipv6CidrBlock ?icmpTypeCode ~egress ?dryRun ?cidrBlock ()
    let of_json json =
      let ruleNumber = field_map_exn json "RuleNumber" Integer.of_json in
      let ruleAction = field_map_exn json "RuleAction" RuleAction.of_json in
      let protocol = field_map_exn json "Protocol" String_.of_json in
      let portRange = field_map json "PortRange" PortRange.of_json in
      let networkAclId =
        field_map_exn json "NetworkAclId" NetworkAclId.of_json in
      let ipv6CidrBlock = field_map json "Ipv6CidrBlock" String_.of_json in
      let icmpTypeCode = field_map json "IcmpTypeCode" IcmpTypeCode.of_json in
      let egress = field_map_exn json "Egress" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let cidrBlock = field_map json "CidrBlock" String_.of_json in
      make ~ruleNumber ~ruleAction ~protocol ?portRange ~networkAclId
        ?ipv6CidrBlock ?icmpTypeCode ~egress ?dryRun ?cidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces an entry (rule) in a network ACL. For more information, see Network ACLs in the Amazon Virtual Private Cloud User Guide."]
module ReplaceNetworkAclAssociationResult =
  struct
    type nonrec t =
      {
      newAssociationId: String_.t option
        [@ocaml.doc "The ID of the new association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?newAssociationId = fun () -> { newAssociationId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("newAssociationId",
           (Option.map x.newAssociationId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let newAssociationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "newAssociationId") in
      make ?newAssociationId ()
    let of_json json =
      let newAssociationId =
        field_map json "NewAssociationId" String_.of_json in
      make ?newAssociationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes which network ACL a subnet is associated with. By default when you create a subnet, it's automatically associated with the default network ACL. For more information, see Network ACLs in the Amazon Virtual Private Cloud User Guide. This is an idempotent operation."]
module ReplaceNetworkAclAssociationRequest =
  struct
    type nonrec t =
      {
      associationId: NetworkAclAssociationId.t
        [@ocaml.doc
          "The ID of the current association between the original network ACL and the subnet."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkAclId: NetworkAclId.t
        [@ocaml.doc
          "The ID of the new network ACL to associate with the subnet."]}
    let context_ = "ReplaceNetworkAclAssociationRequest"
    let make ?dryRun =
      fun ~associationId ->
        fun ~networkAclId ->
          fun () -> { dryRun; associationId; networkAclId }
    let to_value x =
      structure_to_value
        [("associationId",
           (Some (NetworkAclAssociationId.to_value x.associationId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let associationId =
        NetworkAclAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associationId") in
      make ~networkAclId ?dryRun ~associationId ()
    let of_json json =
      let networkAclId =
        field_map_exn json "NetworkAclId" NetworkAclId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let associationId =
        field_map_exn json "AssociationId" NetworkAclAssociationId.of_json in
      make ~networkAclId ?dryRun ~associationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes which network ACL a subnet is associated with. By default when you create a subnet, it's automatically associated with the default network ACL. For more information, see Network ACLs in the Amazon Virtual Private Cloud User Guide. This is an idempotent operation."]
module ReplaceIamInstanceProfileAssociationResult =
  struct
    type nonrec t =
      {
      iamInstanceProfileAssociation: IamInstanceProfileAssociation.t option
        [@ocaml.doc
          "Information about the IAM instance profile association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?iamInstanceProfileAssociation =
      fun () -> { iamInstanceProfileAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("iamInstanceProfileAssociation",
           (Option.map x.iamInstanceProfileAssociation
              ~f:IamInstanceProfileAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let iamInstanceProfileAssociation =
        (Option.map ~f:IamInstanceProfileAssociation.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfileAssociation") in
      make ?iamInstanceProfileAssociation ()
    let of_json json =
      let iamInstanceProfileAssociation =
        field_map json "IamInstanceProfileAssociation"
          IamInstanceProfileAssociation.of_json in
      make ?iamInstanceProfileAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces an IAM instance profile for the specified running instance. You can use this action to change the IAM instance profile that's associated with an instance without having to disassociate the existing IAM instance profile first. Use DescribeIamInstanceProfileAssociations to get the association ID."]
module ReplaceIamInstanceProfileAssociationRequest =
  struct
    type nonrec t =
      {
      iamInstanceProfile: IamInstanceProfileSpecification.t
        [@ocaml.doc "The IAM instance profile."];
      associationId: IamInstanceProfileAssociationId.t
        [@ocaml.doc
          "The ID of the existing IAM instance profile association."]}
    let context_ = "ReplaceIamInstanceProfileAssociationRequest"
    let make ~iamInstanceProfile =
      fun ~associationId -> fun () -> { iamInstanceProfile; associationId }
    let to_value x =
      structure_to_value
        [("IamInstanceProfile",
           (Some
              (IamInstanceProfileSpecification.to_value x.iamInstanceProfile)));
        ("AssociationId",
          (Some (IamInstanceProfileAssociationId.to_value x.associationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationId =
        IamInstanceProfileAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationId") in
      let iamInstanceProfile =
        IamInstanceProfileSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamInstanceProfile") in
      make ~associationId ~iamInstanceProfile ()
    let of_json json =
      let associationId =
        field_map_exn json "AssociationId"
          IamInstanceProfileAssociationId.of_json in
      let iamInstanceProfile =
        field_map_exn json "IamInstanceProfile"
          IamInstanceProfileSpecification.of_json in
      make ~associationId ~iamInstanceProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces an IAM instance profile for the specified running instance. You can use this action to change the IAM instance profile that's associated with an instance without having to disassociate the existing IAM instance profile first. Use DescribeIamInstanceProfileAssociations to get the association ID."]
module ReleaseIpamPoolAllocationResult =
  struct
    type nonrec t =
      {
      success: Boolean.t option
        [@ocaml.doc "Indicates if the release was successful."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?success = fun () -> { success }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("success", (Option.map x.success ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let success =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "success") in
      make ?success ()
    let of_json json =
      let success = field_map json "Success" Boolean.of_json in
      make ?success ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Release an allocation within an IPAM pool. You can only use this action to release manual allocations. To remove an allocation for a resource without deleting the resource, set its monitored state to false using ModifyIpamResourceCidr. For more information, see Release an allocation in the Amazon VPC IPAM User Guide."]
module ReleaseIpamPoolAllocationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc
          "The ID of the IPAM pool which contains the allocation you want to release."];
      cidr: String_.t
        [@ocaml.doc "The CIDR of the allocation you want to release."];
      ipamPoolAllocationId: IpamPoolAllocationId.t
        [@ocaml.doc "The ID of the allocation."]}
    let context_ = "ReleaseIpamPoolAllocationRequest"
    let make ?dryRun =
      fun ~ipamPoolId ->
        fun ~cidr ->
          fun ~ipamPoolAllocationId ->
            fun () -> { dryRun; ipamPoolId; cidr; ipamPoolAllocationId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cidr", (Some (String_.to_value x.cidr)));
        ("IpamPoolAllocationId",
          (Some (IpamPoolAllocationId.to_value x.ipamPoolAllocationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolAllocationId =
        IpamPoolAllocationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolAllocationId") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamPoolAllocationId ~cidr ~ipamPoolId ?dryRun ()
    let of_json json =
      let ipamPoolAllocationId =
        field_map_exn json "IpamPoolAllocationId"
          IpamPoolAllocationId.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~ipamPoolAllocationId ~cidr ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Release an allocation within an IPAM pool. You can only use this action to release manual allocations. To remove an allocation for a resource without deleting the resource, set its monitored state to false using ModifyIpamResourceCidr. For more information, see Release an allocation in the Amazon VPC IPAM User Guide."]
module ReleaseHostsResult =
  struct
    type nonrec t =
      {
      successful: ResponseHostIdList.t option
        [@ocaml.doc
          "The IDs of the Dedicated Hosts that were successfully released."];
      unsuccessful: UnsuccessfulItemList.t option
        [@ocaml.doc
          "The IDs of the Dedicated Hosts that could not be released, including an error message."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successful =
      fun ?unsuccessful -> fun () -> { successful; unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successful",
           (Option.map x.successful ~f:ResponseHostIdList.to_value));
        ("unsuccessful",
          (Option.map x.unsuccessful ~f:UnsuccessfulItemList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemList.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      let successful =
        (Option.map ~f:ResponseHostIdList.of_xml)
          (Xml.child xml_arg0 "successful") in
      make ?unsuccessful ?successful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemList.of_json in
      let successful = field_map json "Successful" ResponseHostIdList.of_json in
      make ?unsuccessful ?successful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When you no longer want to use an On-Demand Dedicated Host it can be released. On-Demand billing is stopped and the host goes into released state. The host ID of Dedicated Hosts that have been released can no longer be specified in another request, for example, to modify the host. You must stop or terminate all instances on a host before it can be released. When Dedicated Hosts are released, it may take some time for them to stop counting toward your limit and you may receive capacity errors when trying to allocate new Dedicated Hosts. Wait a few minutes and then try again. Released hosts still appear in a DescribeHosts response."]
module ReleaseHostsRequest =
  struct
    type nonrec t =
      {
      hostIds: RequestHostIdList.t
        [@ocaml.doc "The IDs of the Dedicated Hosts to release."]}
    let context_ = "ReleaseHostsRequest"
    let make ~hostIds = fun () -> { hostIds }
    let to_value x =
      structure_to_value
        [("hostId", (Some (RequestHostIdList.to_value x.hostIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostIds =
        RequestHostIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "hostId") in
      make ~hostIds ()
    let of_json json =
      let hostIds = field_map_exn json "HostIds" RequestHostIdList.of_json in
      make ~hostIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When you no longer want to use an On-Demand Dedicated Host it can be released. On-Demand billing is stopped and the host goes into released state. The host ID of Dedicated Hosts that have been released can no longer be specified in another request, for example, to modify the host. You must stop or terminate all instances on a host before it can be released. When Dedicated Hosts are released, it may take some time for them to stop counting toward your limit and you may receive capacity errors when trying to allocate new Dedicated Hosts. Wait a few minutes and then try again. Released hosts still appear in a DescribeHosts response."]
module ReleaseAddressRequest =
  struct
    type nonrec t =
      {
      allocationId: AllocationId.t option
        [@ocaml.doc "\\[EC2-VPC\\] The allocation ID. Required for EC2-VPC."];
      publicIp: String_.t option
        [@ocaml.doc
          "\\[EC2-Classic\\] The Elastic IP address. Required for EC2-Classic."];
      networkBorderGroup: String_.t option
        [@ocaml.doc
          "The set of Availability Zones, Local Zones, or Wavelength Zones from which Amazon Web Services advertises IP addresses. If you provide an incorrect network border group, you receive an InvalidAddress.NotFound error. You cannot use a network border group with EC2 Classic. If you attempt this operation on EC2 classic, you receive an InvalidParameterCombination error."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?allocationId =
      fun ?publicIp ->
        fun ?networkBorderGroup ->
          fun ?dryRun ->
            fun () -> { allocationId; publicIp; networkBorderGroup; dryRun }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationId ~f:AllocationId.to_value));
        ("PublicIp", (Option.map x.publicIp ~f:String_.to_value));
        ("NetworkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkBorderGroup") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PublicIp") in
      let allocationId =
        (Option.map ~f:AllocationId.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?dryRun ?networkBorderGroup ?publicIp ?allocationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkBorderGroup =
        field_map json "NetworkBorderGroup" String_.of_json in
      let publicIp = field_map json "PublicIp" String_.of_json in
      let allocationId = field_map json "AllocationId" AllocationId.of_json in
      make ?dryRun ?networkBorderGroup ?publicIp ?allocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Releases the specified Elastic IP address. \\[EC2-Classic, default VPC\\] Releasing an Elastic IP address automatically disassociates it from any instance that it's associated with. To disassociate an Elastic IP address without releasing it, use DisassociateAddress. \\[Nondefault VPC\\] You must use DisassociateAddress to disassociate the Elastic IP address before you can release it. Otherwise, Amazon EC2 returns an error (InvalidIPAddress.InUse). After releasing an Elastic IP address, it is released to the IP address pool. Be sure to update your DNS records and any servers or devices that communicate with the address. If you attempt to release an Elastic IP address that you already released, you'll get an AuthFailure error if the address is already allocated to another Amazon Web Services account. \\[EC2-VPC\\] After you release an Elastic IP address for use in a VPC, you might be able to recover it. For more information, see AllocateAddress."]
module RejectVpcPeeringConnectionResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects a VPC peering connection request. The VPC peering connection must be in the pending-acceptance state. Use the DescribeVpcPeeringConnections request to view your outstanding VPC peering connection requests. To delete an active VPC peering connection, or to delete a VPC peering connection request that you initiated, use DeleteVpcPeeringConnection."]
module RejectVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcPeeringConnectionId: VpcPeeringConnectionId.t
        [@ocaml.doc "The ID of the VPC peering connection."]}
    let context_ = "RejectVpcPeeringConnectionRequest"
    let make ?dryRun =
      fun ~vpcPeeringConnectionId ->
        fun () -> { dryRun; vpcPeeringConnectionId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcPeeringConnectionId",
          (Some (VpcPeeringConnectionId.to_value x.vpcPeeringConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        VpcPeeringConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpcPeeringConnectionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcPeeringConnectionId ?dryRun ()
    let of_json json =
      let vpcPeeringConnectionId =
        field_map_exn json "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcPeeringConnectionId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects a VPC peering connection request. The VPC peering connection must be in the pending-acceptance state. Use the DescribeVpcPeeringConnections request to view your outstanding VPC peering connection requests. To delete an active VPC peering connection, or to delete a VPC peering connection request that you initiated, use DeleteVpcPeeringConnection."]
module RejectVpcEndpointConnectionsResult =
  struct
    type nonrec t =
      {
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the endpoints that were not rejected, if applicable."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects one or more VPC endpoint connection requests to your VPC endpoint service."]
module RejectVpcEndpointConnectionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t [@ocaml.doc "The ID of the service."];
      vpcEndpointIds: VpcEndpointIdList.t
        [@ocaml.doc "The IDs of one or more VPC endpoints."]}
    let context_ = "RejectVpcEndpointConnectionsRequest"
    let make ?dryRun =
      fun ~serviceId ->
        fun ~vpcEndpointIds ->
          fun () -> { dryRun; serviceId; vpcEndpointIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("VpcEndpointId",
          (Some (VpcEndpointIdList.to_value x.vpcEndpointIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEndpointIds =
        VpcEndpointIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEndpointId") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcEndpointIds ~serviceId ?dryRun ()
    let of_json json =
      let vpcEndpointIds =
        field_map_exn json "VpcEndpointIds" VpcEndpointIdList.of_json in
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcEndpointIds ~serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects one or more VPC endpoint connection requests to your VPC endpoint service."]
module RejectTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option
        [@ocaml.doc "Information about the attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_json json =
      let transitGatewayVpcAttachment =
        field_map json "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects a request to attach a VPC to a transit gateway. The VPC attachment must be in the pendingAcceptance state. Use DescribeTransitGatewayVpcAttachments to view your pending VPC attachment requests. Use AcceptTransitGatewayVpcAttachment to accept a VPC attachment request."]
module RejectTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RejectTransitGatewayVpcAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects a request to attach a VPC to a transit gateway. The VPC attachment must be in the pendingAcceptance state. Use DescribeTransitGatewayVpcAttachments to view your pending VPC attachment requests. Use AcceptTransitGatewayVpcAttachment to accept a VPC attachment request."]
module RejectTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option
        [@ocaml.doc "The transit gateway peering attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_json json =
      let transitGatewayPeeringAttachment =
        field_map json "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Rejects a transit gateway peering attachment request."]
module RejectTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the transit gateway peering attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RejectTransitGatewayPeeringAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Rejects a transit gateway peering attachment request."]
module RejectTransitGatewayMulticastDomainAssociationsResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayMulticastDomainAssociations.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations = fun () -> { associations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayMulticastDomainAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociations.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?associations ()
    let of_json json =
      let associations =
        field_map json "Associations"
          TransitGatewayMulticastDomainAssociations.of_json in
      make ?associations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects a request to associate cross-account subnets with a transit gateway multicast domain."]
module RejectTransitGatewayMulticastDomainAssociationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc "The ID of the transit gateway attachment."];
      subnetIds: ValueStringList.t option
        [@ocaml.doc
          "The IDs of the subnets to associate with the transit gateway multicast domain."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?transitGatewayAttachmentId ->
        fun ?subnetIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                transitGatewayAttachmentId;
                subnetIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:ValueStringList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "SubnetIds") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetIds = field_map json "SubnetIds" ValueStringList.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rejects a request to associate cross-account subnets with a transit gateway multicast domain."]
module RegisterTransitGatewayMulticastGroupSourcesResult =
  struct
    type nonrec t =
      {
      registeredMulticastGroupSources:
        TransitGatewayMulticastRegisteredGroupSources.t option
        [@ocaml.doc
          "Information about the transit gateway multicast group sources."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?registeredMulticastGroupSources =
      fun () -> { registeredMulticastGroupSources }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("registeredMulticastGroupSources",
           (Option.map x.registeredMulticastGroupSources
              ~f:TransitGatewayMulticastRegisteredGroupSources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let registeredMulticastGroupSources =
        (Option.map ~f:TransitGatewayMulticastRegisteredGroupSources.of_xml)
          (Xml.child xml_arg0 "registeredMulticastGroupSources") in
      make ?registeredMulticastGroupSources ()
    let of_json json =
      let registeredMulticastGroupSources =
        field_map json "RegisteredMulticastGroupSources"
          TransitGatewayMulticastRegisteredGroupSources.of_json in
      make ?registeredMulticastGroupSources ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers sources (network interfaces) with the specified transit gateway multicast group. A multicast source is a network interface attached to a supported instance that sends multicast traffic. For information about supported instances, see Multicast Considerations in Amazon VPC Transit Gateways. After you add the source, use SearchTransitGatewayMulticastGroups to verify that the source was added to the multicast group."]
module RegisterTransitGatewayMulticastGroupSourcesRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      groupIpAddress: String_.t option
        [@ocaml.doc
          "The IP address assigned to the transit gateway multicast group."];
      networkInterfaceIds: TransitGatewayNetworkInterfaceIdList.t option
        [@ocaml.doc
          "The group sources' network interface IDs to register with the transit gateway multicast group."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?groupIpAddress ->
        fun ?networkInterfaceIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                groupIpAddress;
                networkInterfaceIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("GroupIpAddress", (Option.map x.groupIpAddress ~f:String_.to_value));
        ("NetworkInterfaceIds",
          (Option.map x.networkInterfaceIds
             ~f:TransitGatewayNetworkInterfaceIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInterfaceIds =
        (Option.map ~f:TransitGatewayNetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceIds") in
      let groupIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupIpAddress") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInterfaceIds =
        field_map json "NetworkInterfaceIds"
          TransitGatewayNetworkInterfaceIdList.of_json in
      let groupIpAddress = field_map json "GroupIpAddress" String_.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers sources (network interfaces) with the specified transit gateway multicast group. A multicast source is a network interface attached to a supported instance that sends multicast traffic. For information about supported instances, see Multicast Considerations in Amazon VPC Transit Gateways. After you add the source, use SearchTransitGatewayMulticastGroups to verify that the source was added to the multicast group."]
module RegisterTransitGatewayMulticastGroupMembersResult =
  struct
    type nonrec t =
      {
      registeredMulticastGroupMembers:
        TransitGatewayMulticastRegisteredGroupMembers.t option
        [@ocaml.doc
          "Information about the registered transit gateway multicast group members."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?registeredMulticastGroupMembers =
      fun () -> { registeredMulticastGroupMembers }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("registeredMulticastGroupMembers",
           (Option.map x.registeredMulticastGroupMembers
              ~f:TransitGatewayMulticastRegisteredGroupMembers.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let registeredMulticastGroupMembers =
        (Option.map ~f:TransitGatewayMulticastRegisteredGroupMembers.of_xml)
          (Xml.child xml_arg0 "registeredMulticastGroupMembers") in
      make ?registeredMulticastGroupMembers ()
    let of_json json =
      let registeredMulticastGroupMembers =
        field_map json "RegisteredMulticastGroupMembers"
          TransitGatewayMulticastRegisteredGroupMembers.of_json in
      make ?registeredMulticastGroupMembers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers members (network interfaces) with the transit gateway multicast group. A member is a network interface associated with a supported EC2 instance that receives multicast traffic. For information about supported instances, see Multicast Consideration in Amazon VPC Transit Gateways. After you add the members, use SearchTransitGatewayMulticastGroups to verify that the members were added to the transit gateway multicast group."]
module RegisterTransitGatewayMulticastGroupMembersRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      groupIpAddress: String_.t option
        [@ocaml.doc
          "The IP address assigned to the transit gateway multicast group."];
      networkInterfaceIds: TransitGatewayNetworkInterfaceIdList.t option
        [@ocaml.doc
          "The group members' network interface IDs to register with the transit gateway multicast group."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?groupIpAddress ->
        fun ?networkInterfaceIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                groupIpAddress;
                networkInterfaceIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("GroupIpAddress", (Option.map x.groupIpAddress ~f:String_.to_value));
        ("NetworkInterfaceIds",
          (Option.map x.networkInterfaceIds
             ~f:TransitGatewayNetworkInterfaceIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInterfaceIds =
        (Option.map ~f:TransitGatewayNetworkInterfaceIdList.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceIds") in
      let groupIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupIpAddress") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInterfaceIds =
        field_map json "NetworkInterfaceIds"
          TransitGatewayNetworkInterfaceIdList.of_json in
      let groupIpAddress = field_map json "GroupIpAddress" String_.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?networkInterfaceIds ?groupIpAddress
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers members (network interfaces) with the transit gateway multicast group. A member is a network interface associated with a supported EC2 instance that receives multicast traffic. For information about supported instances, see Multicast Consideration in Amazon VPC Transit Gateways. After you add the members, use SearchTransitGatewayMulticastGroups to verify that the members were added to the transit gateway multicast group."]
module RegisterInstanceEventNotificationAttributesResult =
  struct
    type nonrec t =
      {
      instanceTagAttribute: InstanceTagNotificationAttribute.t option
        [@ocaml.doc "The resulting set of tag keys."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTagAttribute = fun () -> { instanceTagAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTagAttribute",
           (Option.map x.instanceTagAttribute
              ~f:InstanceTagNotificationAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:InstanceTagNotificationAttribute.of_xml)
          (Xml.child xml_arg0 "instanceTagAttribute") in
      make ?instanceTagAttribute ()
    let of_json json =
      let instanceTagAttribute =
        field_map json "InstanceTagAttribute"
          InstanceTagNotificationAttribute.of_json in
      make ?instanceTagAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers a set of tag keys to include in scheduled event notifications for your resources. To remove tags, use DeregisterInstanceEventNotificationAttributes."]
module RegisterInstanceEventNotificationAttributesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceTagAttribute: RegisterInstanceTagAttributeRequest.t option
        [@ocaml.doc "Information about the tag keys to register."]}
    let make ?dryRun =
      fun ?instanceTagAttribute -> fun () -> { dryRun; instanceTagAttribute }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceTagAttribute",
          (Option.map x.instanceTagAttribute
             ~f:RegisterInstanceTagAttributeRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTagAttribute =
        (Option.map ~f:RegisterInstanceTagAttributeRequest.of_xml)
          (Xml.child xml_arg0 "InstanceTagAttribute") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?instanceTagAttribute ?dryRun ()
    let of_json json =
      let instanceTagAttribute =
        field_map json "InstanceTagAttribute"
          RegisterInstanceTagAttributeRequest.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?instanceTagAttribute ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers a set of tag keys to include in scheduled event notifications for your resources. To remove tags, use DeregisterInstanceEventNotificationAttributes."]
module RegisterImageResult =
  struct
    type nonrec t =
      {
      imageId: String_.t option
        [@ocaml.doc "The ID of the newly registered AMI."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_json json =
      let imageId = field_map json "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of RegisterImage."]
module RegisterImageRequest =
  struct
    type nonrec t =
      {
      imageLocation: String_.t option
        [@ocaml.doc
          "The full path to your AMI manifest in Amazon S3 storage. The specified bucket must have the aws-exec-read canned access control list (ACL) to ensure that it can be accessed by Amazon EC2. For more information, see Canned ACLs in the Amazon S3 Service Developer Guide."];
      architecture: ArchitectureValues.t option
        [@ocaml.doc
          "The architecture of the AMI. Default: For Amazon EBS-backed AMIs, i386. For instance store-backed AMIs, the architecture specified in the manifest file."];
      blockDeviceMappings: BlockDeviceMappingRequestList.t option
        [@ocaml.doc
          "The block device mapping entries. If you specify an Amazon EBS volume using the ID of an Amazon EBS snapshot, you can't specify the encryption state of the volume. If you create an AMI on an Outpost, then all backing snapshots must be on the same Outpost or in the Region of that Outpost. AMIs on an Outpost that include local snapshots can be used to launch instances on the same Outpost only. For more information, Amazon EBS local snapshots on Outposts in the Amazon Elastic Compute Cloud User Guide."];
      description: String_.t option
        [@ocaml.doc "A description for your AMI."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      enaSupport: Boolean.t option
        [@ocaml.doc
          "Set to true to enable enhanced networking with ENA for the AMI and any instances that you launch from the AMI. This option is supported only for HVM AMIs. Specifying this option with a PV AMI can make instances launched from the AMI unreachable."];
      kernelId: KernelId.t option [@ocaml.doc "The ID of the kernel."];
      name: String_.t
        [@ocaml.doc
          "A name for your AMI. Constraints: 3-128 alphanumeric characters, parentheses (()), square brackets (\\[\\]), spaces ( ), periods (.), slashes (/), dashes (-), single quotes ('), at-signs (\\@), or underscores(_)"];
      billingProducts: BillingProductList.t option
        [@ocaml.doc
          "The billing product codes. Your account must be authorized to specify billing product codes. Otherwise, you can use the Amazon Web Services Marketplace to bill for the use of an AMI."];
      ramdiskId: RamdiskId.t option [@ocaml.doc "The ID of the RAM disk."];
      rootDeviceName: String_.t option
        [@ocaml.doc
          "The device name of the root device volume (for example, /dev/sda1)."];
      sriovNetSupport: String_.t option
        [@ocaml.doc
          "Set to simple to enable enhanced networking with the Intel 82599 Virtual Function interface for the AMI and any instances that you launch from the AMI. There is no way to disable sriovNetSupport at this time. This option is supported only for HVM AMIs. Specifying this option with a PV AMI can make instances launched from the AMI unreachable."];
      virtualizationType: String_.t option
        [@ocaml.doc
          "The type of virtualization (hvm | paravirtual). Default: paravirtual"];
      bootMode: BootModeValues.t option
        [@ocaml.doc
          "The boot mode of the AMI. For more information, see Boot modes in the Amazon Elastic Compute Cloud User Guide."]}
    let context_ = "RegisterImageRequest"
    let make ?imageLocation =
      fun ?architecture ->
        fun ?blockDeviceMappings ->
          fun ?description ->
            fun ?dryRun ->
              fun ?enaSupport ->
                fun ?kernelId ->
                  fun ?billingProducts ->
                    fun ?ramdiskId ->
                      fun ?rootDeviceName ->
                        fun ?sriovNetSupport ->
                          fun ?virtualizationType ->
                            fun ?bootMode ->
                              fun ~name ->
                                fun () ->
                                  {
                                    imageLocation;
                                    architecture;
                                    blockDeviceMappings;
                                    description;
                                    dryRun;
                                    enaSupport;
                                    kernelId;
                                    billingProducts;
                                    ramdiskId;
                                    rootDeviceName;
                                    sriovNetSupport;
                                    virtualizationType;
                                    bootMode;
                                    name
                                  }
    let to_value x =
      structure_to_value
        [("ImageLocation", (Option.map x.imageLocation ~f:String_.to_value));
        ("architecture",
          (Option.map x.architecture ~f:ArchitectureValues.to_value));
        ("BlockDeviceMapping",
          (Option.map x.blockDeviceMappings
             ~f:BlockDeviceMappingRequestList.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("enaSupport", (Option.map x.enaSupport ~f:Boolean.to_value));
        ("kernelId", (Option.map x.kernelId ~f:KernelId.to_value));
        ("name", (Some (String_.to_value x.name)));
        ("BillingProduct",
          (Option.map x.billingProducts ~f:BillingProductList.to_value));
        ("ramdiskId", (Option.map x.ramdiskId ~f:RamdiskId.to_value));
        ("rootDeviceName", (Option.map x.rootDeviceName ~f:String_.to_value));
        ("sriovNetSupport",
          (Option.map x.sriovNetSupport ~f:String_.to_value));
        ("virtualizationType",
          (Option.map x.virtualizationType ~f:String_.to_value));
        ("BootMode", (Option.map x.bootMode ~f:BootModeValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bootMode =
        (Option.map ~f:BootModeValues.of_xml) (Xml.child xml_arg0 "BootMode") in
      let virtualizationType =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "virtualizationType") in
      let sriovNetSupport =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sriovNetSupport") in
      let rootDeviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "rootDeviceName") in
      let ramdiskId =
        (Option.map ~f:RamdiskId.of_xml) (Xml.child xml_arg0 "ramdiskId") in
      let billingProducts =
        (Option.map ~f:BillingProductList.of_xml)
          (Xml.child xml_arg0 "BillingProduct") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let kernelId =
        (Option.map ~f:KernelId.of_xml) (Xml.child xml_arg0 "kernelId") in
      let enaSupport =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enaSupport") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappingRequestList.of_xml)
          (Xml.child xml_arg0 "BlockDeviceMapping") in
      let architecture =
        (Option.map ~f:ArchitectureValues.of_xml)
          (Xml.child xml_arg0 "architecture") in
      let imageLocation =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ImageLocation") in
      make ?bootMode ?virtualizationType ?sriovNetSupport ?rootDeviceName
        ?ramdiskId ?billingProducts ~name ?kernelId ?enaSupport ?dryRun
        ?description ?blockDeviceMappings ?architecture ?imageLocation ()
    let of_json json =
      let bootMode = field_map json "BootMode" BootModeValues.of_json in
      let virtualizationType =
        field_map json "VirtualizationType" String_.of_json in
      let sriovNetSupport = field_map json "SriovNetSupport" String_.of_json in
      let rootDeviceName = field_map json "RootDeviceName" String_.of_json in
      let ramdiskId = field_map json "RamdiskId" RamdiskId.of_json in
      let billingProducts =
        field_map json "BillingProducts" BillingProductList.of_json in
      let name = field_map_exn json "Name" String_.of_json in
      let kernelId = field_map json "KernelId" KernelId.of_json in
      let enaSupport = field_map json "EnaSupport" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings"
          BlockDeviceMappingRequestList.of_json in
      let architecture =
        field_map json "Architecture" ArchitectureValues.of_json in
      let imageLocation = field_map json "ImageLocation" String_.of_json in
      make ?bootMode ?virtualizationType ?sriovNetSupport ?rootDeviceName
        ?ramdiskId ?billingProducts ~name ?kernelId ?enaSupport ?dryRun
        ?description ?blockDeviceMappings ?architecture ?imageLocation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for RegisterImage."]
module RebootInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t [@ocaml.doc "The instance IDs."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "RebootInstancesRequest"
    let make ?dryRun = fun ~instanceIds -> fun () -> { dryRun; instanceIds }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceIdStringList.to_value x.instanceIds)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceIds =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~instanceIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceIds =
        field_map_exn json "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a reboot of the specified instances. This operation is asynchronous; it only queues a request to reboot the specified instances. The operation succeeds if the instances are valid and belong to you. Requests to reboot terminated instances are ignored. If an instance does not cleanly shut down within a few minutes, Amazon EC2 performs a hard reboot. For more information about troubleshooting, see Troubleshoot an unreachable instance in the Amazon EC2 User Guide."]
module PurchaseScheduledInstancesResult =
  struct
    type nonrec t =
      {
      scheduledInstanceSet: PurchasedScheduledInstanceSet.t option
        [@ocaml.doc "Information about the Scheduled Instances."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?scheduledInstanceSet = fun () -> { scheduledInstanceSet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("scheduledInstanceSet",
           (Option.map x.scheduledInstanceSet
              ~f:PurchasedScheduledInstanceSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheduledInstanceSet =
        (Option.map ~f:PurchasedScheduledInstanceSet.of_xml)
          (Xml.child xml_arg0 "scheduledInstanceSet") in
      make ?scheduledInstanceSet ()
    let of_json json =
      let scheduledInstanceSet =
        field_map json "ScheduledInstanceSet"
          PurchasedScheduledInstanceSet.of_json in
      make ?scheduledInstanceSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of PurchaseScheduledInstances."]
module PurchaseScheduledInstancesRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that ensures the idempotency of the request. For more information, see Ensuring Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      purchaseRequests: PurchaseRequestSet.t
        [@ocaml.doc "The purchase requests."]}
    let context_ = "PurchaseScheduledInstancesRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~purchaseRequests ->
          fun () -> { clientToken; dryRun; purchaseRequests }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PurchaseRequest",
          (Some (PurchaseRequestSet.to_value x.purchaseRequests)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let purchaseRequests =
        PurchaseRequestSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PurchaseRequest") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~purchaseRequests ?dryRun ?clientToken ()
    let of_json json =
      let purchaseRequests =
        field_map_exn json "PurchaseRequests" PurchaseRequestSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ~purchaseRequests ?dryRun ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for PurchaseScheduledInstances."]
module PurchaseReservedInstancesOfferingResult =
  struct
    type nonrec t =
      {
      reservedInstancesId: String_.t option
        [@ocaml.doc
          "The IDs of the purchased Reserved Instances. If your purchase crosses into a discounted pricing tier, the final Reserved Instances IDs might change. For more information, see Crossing pricing tiers in the Amazon Elastic Compute Cloud User Guide."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesId = fun () -> { reservedInstancesId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesId",
           (Option.map x.reservedInstancesId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesId") in
      make ?reservedInstancesId ()
    let of_json json =
      let reservedInstancesId =
        field_map json "ReservedInstancesId" String_.of_json in
      make ?reservedInstancesId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of PurchaseReservedInstancesOffering."]
module PurchaseReservedInstancesOfferingRequest =
  struct
    type nonrec t =
      {
      instanceCount: Integer.t
        [@ocaml.doc "The number of Reserved Instances to purchase."];
      reservedInstancesOfferingId: ReservedInstancesOfferingId.t
        [@ocaml.doc "The ID of the Reserved Instance offering to purchase."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      limitPrice: ReservedInstanceLimitPrice.t option
        [@ocaml.doc
          "Specified for Reserved Instance Marketplace offerings to limit the total order and ensure that the Reserved Instances are not purchased at unexpected prices."];
      purchaseTime: DateTime.t option
        [@ocaml.doc
          "The time at which to purchase the Reserved Instance, in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ)."]}
    let context_ = "PurchaseReservedInstancesOfferingRequest"
    let make ?dryRun =
      fun ?limitPrice ->
        fun ?purchaseTime ->
          fun ~instanceCount ->
            fun ~reservedInstancesOfferingId ->
              fun () ->
                {
                  dryRun;
                  limitPrice;
                  purchaseTime;
                  instanceCount;
                  reservedInstancesOfferingId
                }
    let to_value x =
      structure_to_value
        [("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("ReservedInstancesOfferingId",
          (Some
             (ReservedInstancesOfferingId.to_value
                x.reservedInstancesOfferingId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("limitPrice",
          (Option.map x.limitPrice ~f:ReservedInstanceLimitPrice.to_value));
        ("PurchaseTime", (Option.map x.purchaseTime ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let purchaseTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "PurchaseTime") in
      let limitPrice =
        (Option.map ~f:ReservedInstanceLimitPrice.of_xml)
          (Xml.child xml_arg0 "limitPrice") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let reservedInstancesOfferingId =
        ReservedInstancesOfferingId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ReservedInstancesOfferingId") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      make ?purchaseTime ?limitPrice ?dryRun ~reservedInstancesOfferingId
        ~instanceCount ()
    let of_json json =
      let purchaseTime = field_map json "PurchaseTime" DateTime.of_json in
      let limitPrice =
        field_map json "LimitPrice" ReservedInstanceLimitPrice.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let reservedInstancesOfferingId =
        field_map_exn json "ReservedInstancesOfferingId"
          ReservedInstancesOfferingId.of_json in
      let instanceCount = field_map_exn json "InstanceCount" Integer.of_json in
      make ?purchaseTime ?limitPrice ?dryRun ~reservedInstancesOfferingId
        ~instanceCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for PurchaseReservedInstancesOffering."]
module PurchaseHostReservationResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency."];
      currencyCode: CurrencyCodeValues.t option
        [@ocaml.doc
          "The currency in which the totalUpfrontPrice and totalHourlyPrice amounts are specified. At this time, the only supported currency is USD."];
      purchase: PurchaseSet.t option
        [@ocaml.doc "Describes the details of the purchase."];
      totalHourlyPrice: String_.t option
        [@ocaml.doc
          "The total hourly price of the reservation calculated per hour."];
      totalUpfrontPrice: String_.t option
        [@ocaml.doc
          "The total amount charged to your account when you purchase the reservation."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?currencyCode ->
        fun ?purchase ->
          fun ?totalHourlyPrice ->
            fun ?totalUpfrontPrice ->
              fun () ->
                {
                  clientToken;
                  currencyCode;
                  purchase;
                  totalHourlyPrice;
                  totalUpfrontPrice
                }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCodeValues.to_value));
        ("purchase", (Option.map x.purchase ~f:PurchaseSet.to_value));
        ("totalHourlyPrice",
          (Option.map x.totalHourlyPrice ~f:String_.to_value));
        ("totalUpfrontPrice",
          (Option.map x.totalUpfrontPrice ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalUpfrontPrice =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "totalUpfrontPrice") in
      let totalHourlyPrice =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "totalHourlyPrice") in
      let purchase =
        (Option.map ~f:PurchaseSet.of_xml) (Xml.child xml_arg0 "purchase") in
      let currencyCode =
        (Option.map ~f:CurrencyCodeValues.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?totalUpfrontPrice ?totalHourlyPrice ?purchase ?currencyCode
        ?clientToken ()
    let of_json json =
      let totalUpfrontPrice =
        field_map json "TotalUpfrontPrice" String_.of_json in
      let totalHourlyPrice =
        field_map json "TotalHourlyPrice" String_.of_json in
      let purchase = field_map json "Purchase" PurchaseSet.of_json in
      let currencyCode =
        field_map json "CurrencyCode" CurrencyCodeValues.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?totalUpfrontPrice ?totalHourlyPrice ?purchase ?currencyCode
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Purchase a reservation with configurations that match those of your Dedicated Host. You must have active Dedicated Hosts in your account before you purchase a reservation. This action results in the specified reservation being purchased and charged to your account."]
module PurchaseHostReservationRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency."];
      currencyCode: CurrencyCodeValues.t option
        [@ocaml.doc
          "The currency in which the totalUpfrontPrice, LimitPrice, and totalHourlyPrice amounts are specified. At this time, the only supported currency is USD."];
      hostIdSet: RequestHostIdSet.t
        [@ocaml.doc
          "The IDs of the Dedicated Hosts with which the reservation will be associated."];
      limitPrice: String_.t option
        [@ocaml.doc
          "The specified limit is checked against the total upfront cost of the reservation (calculated as the offering's upfront cost multiplied by the host count). If the total upfront cost is greater than the specified price limit, the request fails. This is used to ensure that the purchase does not exceed the expected upfront cost of the purchase. At this time, the only supported currency is USD. For example, to indicate a limit price of USD 100, specify 100.00."];
      offeringId: OfferingId.t [@ocaml.doc "The ID of the offering."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the Dedicated Host Reservation during purchase."]}
    let context_ = "PurchaseHostReservationRequest"
    let make ?clientToken =
      fun ?currencyCode ->
        fun ?limitPrice ->
          fun ?tagSpecifications ->
            fun ~hostIdSet ->
              fun ~offeringId ->
                fun () ->
                  {
                    clientToken;
                    currencyCode;
                    limitPrice;
                    tagSpecifications;
                    hostIdSet;
                    offeringId
                  }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("CurrencyCode",
          (Option.map x.currencyCode ~f:CurrencyCodeValues.to_value));
        ("HostIdSet", (Some (RequestHostIdSet.to_value x.hostIdSet)));
        ("LimitPrice", (Option.map x.limitPrice ~f:String_.to_value));
        ("OfferingId", (Some (OfferingId.to_value x.offeringId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let offeringId =
        OfferingId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OfferingId") in
      let limitPrice =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LimitPrice") in
      let hostIdSet =
        RequestHostIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostIdSet") in
      let currencyCode =
        (Option.map ~f:CurrencyCodeValues.of_xml)
          (Xml.child xml_arg0 "CurrencyCode") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~offeringId ?limitPrice ~hostIdSet
        ?currencyCode ?clientToken ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let offeringId = field_map_exn json "OfferingId" OfferingId.of_json in
      let limitPrice = field_map json "LimitPrice" String_.of_json in
      let hostIdSet = field_map_exn json "HostIdSet" RequestHostIdSet.of_json in
      let currencyCode =
        field_map json "CurrencyCode" CurrencyCodeValues.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?tagSpecifications ~offeringId ?limitPrice ~hostIdSet
        ?currencyCode ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Purchase a reservation with configurations that match those of your Dedicated Host. You must have active Dedicated Hosts in your account before you purchase a reservation. This action results in the specified reservation being purchased and charged to your account."]
module ProvisionPublicIpv4PoolCidrResult =
  struct
    type nonrec t =
      {
      poolId: Ipv4PoolEc2Id.t option
        [@ocaml.doc
          "The ID of the pool that you want to provision the CIDR to."];
      poolAddressRange: PublicIpv4PoolRange.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?poolId =
      fun ?poolAddressRange -> fun () -> { poolId; poolAddressRange }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("poolId", (Option.map x.poolId ~f:Ipv4PoolEc2Id.to_value));
        ("poolAddressRange",
          (Option.map x.poolAddressRange ~f:PublicIpv4PoolRange.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let poolAddressRange =
        (Option.map ~f:PublicIpv4PoolRange.of_xml)
          (Xml.child xml_arg0 "poolAddressRange") in
      let poolId =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml) (Xml.child xml_arg0 "poolId") in
      make ?poolAddressRange ?poolId ()
    let of_json json =
      let poolAddressRange =
        field_map json "PoolAddressRange" PublicIpv4PoolRange.of_json in
      let poolId = field_map json "PoolId" Ipv4PoolEc2Id.of_json in
      make ?poolAddressRange ?poolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provision a CIDR to a public IPv4 pool. For more information about IPAM, see What is IPAM? in the Amazon VPC IPAM User Guide."]
module ProvisionPublicIpv4PoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc
          "The ID of the IPAM pool you would like to use to allocate this CIDR."];
      poolId: Ipv4PoolEc2Id.t
        [@ocaml.doc
          "The ID of the public IPv4 pool you would like to use for this CIDR."];
      netmaskLength: Integer.t
        [@ocaml.doc
          "The netmask length of the CIDR you would like to allocate to the public IPv4 pool."]}
    let context_ = "ProvisionPublicIpv4PoolCidrRequest"
    let make ?dryRun =
      fun ~ipamPoolId ->
        fun ~poolId ->
          fun ~netmaskLength ->
            fun () -> { dryRun; ipamPoolId; poolId; netmaskLength }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("PoolId", (Some (Ipv4PoolEc2Id.to_value x.poolId)));
        ("NetmaskLength", (Some (Integer.to_value x.netmaskLength)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let netmaskLength =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetmaskLength") in
      let poolId =
        Ipv4PoolEc2Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~netmaskLength ~poolId ~ipamPoolId ?dryRun ()
    let of_json json =
      let netmaskLength = field_map_exn json "NetmaskLength" Integer.of_json in
      let poolId = field_map_exn json "PoolId" Ipv4PoolEc2Id.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~netmaskLength ~poolId ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provision a CIDR to a public IPv4 pool. For more information about IPAM, see What is IPAM? in the Amazon VPC IPAM User Guide."]
module ProvisionIpamPoolCidrResult =
  struct
    type nonrec t =
      {
      ipamPoolCidr: IpamPoolCidr.t option
        [@ocaml.doc "Information about the provisioned CIDR."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolCidr = fun () -> { ipamPoolCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolCidr",
           (Option.map x.ipamPoolCidr ~f:IpamPoolCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolCidr =
        (Option.map ~f:IpamPoolCidr.of_xml)
          (Xml.child xml_arg0 "ipamPoolCidr") in
      make ?ipamPoolCidr ()
    let of_json json =
      let ipamPoolCidr = field_map json "IpamPoolCidr" IpamPoolCidr.of_json in
      make ?ipamPoolCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provision a CIDR to an IPAM pool. You can use this action to provision new CIDRs to a top-level pool or to transfer a CIDR from a top-level pool to a pool within it. For more information, see Provision CIDRs to pools in the Amazon VPC IPAM User Guide."]
module ProvisionIpamPoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc
          "The ID of the IPAM pool to which you want to assign a CIDR."];
      cidr: String_.t option
        [@ocaml.doc "The CIDR you want to assign to the IPAM pool."];
      cidrAuthorizationContext: IpamCidrAuthorizationContext.t option
        [@ocaml.doc
          "A signed document that proves that you are authorized to bring a specified IP address range to Amazon using BYOIP. This option applies to public pools only."]}
    let context_ = "ProvisionIpamPoolCidrRequest"
    let make ?dryRun =
      fun ?cidr ->
        fun ?cidrAuthorizationContext ->
          fun ~ipamPoolId ->
            fun () -> { dryRun; cidr; cidrAuthorizationContext; ipamPoolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cidr", (Option.map x.cidr ~f:String_.to_value));
        ("CidrAuthorizationContext",
          (Option.map x.cidrAuthorizationContext
             ~f:IpamCidrAuthorizationContext.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidrAuthorizationContext =
        (Option.map ~f:IpamCidrAuthorizationContext.of_xml)
          (Xml.child xml_arg0 "CidrAuthorizationContext") in
      let cidr = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Cidr") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cidrAuthorizationContext ?cidr ~ipamPoolId ?dryRun ()
    let of_json json =
      let cidrAuthorizationContext =
        field_map json "CidrAuthorizationContext"
          IpamCidrAuthorizationContext.of_json in
      let cidr = field_map json "Cidr" String_.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?cidrAuthorizationContext ?cidr ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provision a CIDR to an IPAM pool. You can use this action to provision new CIDRs to a top-level pool or to transfer a CIDR from a top-level pool to a pool within it. For more information, see Provision CIDRs to pools in the Amazon VPC IPAM User Guide."]
module ProvisionByoipCidrResult =
  struct
    type nonrec t =
      {
      byoipCidr: ByoipCidr.t option
        [@ocaml.doc "Information about the address range."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_json json =
      let byoipCidr = field_map json "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provisions an IPv4 or IPv6 address range for use with your Amazon Web Services resources through bring your own IP addresses (BYOIP) and creates a corresponding address pool. After the address range is provisioned, it is ready to be advertised using AdvertiseByoipCidr. Amazon Web Services verifies that you own the address range and are authorized to advertise it. You must ensure that the address range is registered to you and that you created an RPKI ROA to authorize Amazon ASNs 16509 and 14618 to advertise the address range. For more information, see Bring your own IP addresses (BYOIP) in the Amazon Elastic Compute Cloud User Guide. Provisioning an address range is an asynchronous operation, so the call returns immediately, but the address range is not ready to use until its status changes from pending-provision to provisioned. To monitor the status of an address range, use DescribeByoipCidrs. To allocate an Elastic IP address from your IPv4 address pool, use AllocateAddress with either the specific address from the address pool or the ID of the address pool."]
module ProvisionByoipCidrRequest =
  struct
    type nonrec t =
      {
      cidr: String_.t
        [@ocaml.doc
          "The public IPv4 or IPv6 address range, in CIDR notation. The most specific IPv4 prefix that you can specify is /24. The most specific IPv6 prefix you can specify is /56. The address range cannot overlap with another address range that you've brought to this or another Region."];
      cidrAuthorizationContext: CidrAuthorizationContext.t option
        [@ocaml.doc
          "A signed document that proves that you are authorized to bring the specified IP address range to Amazon using BYOIP."];
      publiclyAdvertisable: Boolean.t option
        [@ocaml.doc
          "(IPv6 only) Indicate whether the address range will be publicly advertised to the internet. Default: true"];
      description: String_.t option
        [@ocaml.doc
          "A description for the address range and the address pool."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      poolTagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the address pool."];
      multiRegion: Boolean.t option [@ocaml.doc "Reserved."]}
    let context_ = "ProvisionByoipCidrRequest"
    let make ?cidrAuthorizationContext =
      fun ?publiclyAdvertisable ->
        fun ?description ->
          fun ?dryRun ->
            fun ?poolTagSpecifications ->
              fun ?multiRegion ->
                fun ~cidr ->
                  fun () ->
                    {
                      cidrAuthorizationContext;
                      publiclyAdvertisable;
                      description;
                      dryRun;
                      poolTagSpecifications;
                      multiRegion;
                      cidr
                    }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("CidrAuthorizationContext",
          (Option.map x.cidrAuthorizationContext
             ~f:CidrAuthorizationContext.to_value));
        ("PubliclyAdvertisable",
          (Option.map x.publiclyAdvertisable ~f:Boolean.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PoolTagSpecification",
          (Option.map x.poolTagSpecifications
             ~f:TagSpecificationList.to_value));
        ("MultiRegion", (Option.map x.multiRegion ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let multiRegion =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "MultiRegion") in
      let poolTagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "PoolTagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let publiclyAdvertisable =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PubliclyAdvertisable") in
      let cidrAuthorizationContext =
        (Option.map ~f:CidrAuthorizationContext.of_xml)
          (Xml.child xml_arg0 "CidrAuthorizationContext") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?multiRegion ?poolTagSpecifications ?dryRun ?description
        ?publiclyAdvertisable ?cidrAuthorizationContext ~cidr ()
    let of_json json =
      let multiRegion = field_map json "MultiRegion" Boolean.of_json in
      let poolTagSpecifications =
        field_map json "PoolTagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let publiclyAdvertisable =
        field_map json "PubliclyAdvertisable" Boolean.of_json in
      let cidrAuthorizationContext =
        field_map json "CidrAuthorizationContext"
          CidrAuthorizationContext.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      make ?multiRegion ?poolTagSpecifications ?dryRun ?description
        ?publiclyAdvertisable ?cidrAuthorizationContext ~cidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provisions an IPv4 or IPv6 address range for use with your Amazon Web Services resources through bring your own IP addresses (BYOIP) and creates a corresponding address pool. After the address range is provisioned, it is ready to be advertised using AdvertiseByoipCidr. Amazon Web Services verifies that you own the address range and are authorized to advertise it. You must ensure that the address range is registered to you and that you created an RPKI ROA to authorize Amazon ASNs 16509 and 14618 to advertise the address range. For more information, see Bring your own IP addresses (BYOIP) in the Amazon Elastic Compute Cloud User Guide. Provisioning an address range is an asynchronous operation, so the call returns immediately, but the address range is not ready to use until its status changes from pending-provision to provisioned. To monitor the status of an address range, use DescribeByoipCidrs. To allocate an Elastic IP address from your IPv4 address pool, use AllocateAddress with either the specific address from the address pool or the ID of the address pool."]
module MoveByoipCidrToIpamResult =
  struct
    type nonrec t = {
      byoipCidr: ByoipCidr.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_json json =
      let byoipCidr = field_map json "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Move an BYOIP IPv4 CIDR to IPAM from a public IPv4 pool. If you already have an IPv4 BYOIP CIDR with Amazon Web Services, you can move the CIDR to IPAM from a public IPv4 pool. You cannot move an IPv6 CIDR to IPAM. If you are bringing a new IP address to Amazon Web Services for the first time, complete the steps in Tutorial: BYOIP address CIDRs to IPAM."]
module MoveByoipCidrToIpamRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      cidr: String_.t [@ocaml.doc "The BYOIP CIDR."];
      ipamPoolId: IpamPoolId.t [@ocaml.doc "The IPAM pool ID."];
      ipamPoolOwner: String_.t
        [@ocaml.doc
          "The Amazon Web Services account ID of the owner of the IPAM pool."]}
    let context_ = "MoveByoipCidrToIpamRequest"
    let make ?dryRun =
      fun ~cidr ->
        fun ~ipamPoolId ->
          fun ~ipamPoolOwner ->
            fun () -> { dryRun; cidr; ipamPoolId; ipamPoolOwner }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Cidr", (Some (String_.to_value x.cidr)));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("IpamPoolOwner", (Some (String_.to_value x.ipamPoolOwner)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolOwner =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolOwner") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamPoolOwner ~ipamPoolId ~cidr ?dryRun ()
    let of_json json =
      let ipamPoolOwner = field_map_exn json "IpamPoolOwner" String_.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~ipamPoolOwner ~ipamPoolId ~cidr ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Move an BYOIP IPv4 CIDR to IPAM from a public IPv4 pool. If you already have an IPv4 BYOIP CIDR with Amazon Web Services, you can move the CIDR to IPAM from a public IPv4 pool. You cannot move an IPv6 CIDR to IPAM. If you are bringing a new IP address to Amazon Web Services for the first time, complete the steps in Tutorial: BYOIP address CIDRs to IPAM."]
module MoveAddressToVpcResult =
  struct
    type nonrec t =
      {
      allocationId: String_.t option
        [@ocaml.doc "The allocation ID for the Elastic IP address."];
      status: Status.t option
        [@ocaml.doc "The status of the move of the IP address."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?allocationId =
      fun ?status -> fun () -> { allocationId; status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("allocationId", (Option.map x.allocationId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let allocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "allocationId") in
      make ?status ?allocationId ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let allocationId = field_map json "AllocationId" String_.of_json in
      make ?status ?allocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Moves an Elastic IP address from the EC2-Classic platform to the EC2-VPC platform. The Elastic IP address must be allocated to your account for more than 24 hours, and it must not be associated with an instance. After the Elastic IP address is moved, it is no longer available for use in the EC2-Classic platform, unless you move it back using the RestoreAddressToClassic request. You cannot move an Elastic IP address that was originally allocated for use in the EC2-VPC platform to the EC2-Classic platform."]
module MoveAddressToVpcRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      publicIp: String_.t [@ocaml.doc "The Elastic IP address."]}
    let context_ = "MoveAddressToVpcRequest"
    let make ?dryRun = fun ~publicIp -> fun () -> { dryRun; publicIp }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("publicIp", (Some (String_.to_value x.publicIp)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let publicIp =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "publicIp") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~publicIp ?dryRun ()
    let of_json json =
      let publicIp = field_map_exn json "PublicIp" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~publicIp ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Moves an Elastic IP address from the EC2-Classic platform to the EC2-VPC platform. The Elastic IP address must be allocated to your account for more than 24 hours, and it must not be associated with an instance. After the Elastic IP address is moved, it is no longer available for use in the EC2-Classic platform, unless you move it back using the RestoreAddressToClassic request. You cannot move an Elastic IP address that was originally allocated for use in the EC2-VPC platform to the EC2-Classic platform."]
module MonitorInstancesResult =
  struct
    type nonrec t =
      {
      instanceMonitorings: InstanceMonitoringList.t option
        [@ocaml.doc "The monitoring information."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceMonitorings = fun () -> { instanceMonitorings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instancesSet",
           (Option.map x.instanceMonitorings
              ~f:InstanceMonitoringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceMonitorings =
        (Option.map ~f:InstanceMonitoringList.of_xml)
          (Xml.child xml_arg0 "instancesSet") in
      make ?instanceMonitorings ()
    let of_json json =
      let instanceMonitorings =
        field_map json "InstanceMonitorings" InstanceMonitoringList.of_json in
      make ?instanceMonitorings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables detailed monitoring for a running instance. Otherwise, basic monitoring is enabled. For more information, see Monitor your instances using CloudWatch in the Amazon EC2 User Guide. To disable detailed monitoring, see UnmonitorInstances."]
module MonitorInstancesRequest =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdStringList.t
        [@ocaml.doc "The IDs of the instances."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "MonitorInstancesRequest"
    let make ?dryRun = fun ~instanceIds -> fun () -> { dryRun; instanceIds }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceIdStringList.to_value x.instanceIds)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceIds =
        InstanceIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~instanceIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceIds =
        field_map_exn json "InstanceIds" InstanceIdStringList.of_json in
      make ?dryRun ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables detailed monitoring for a running instance. Otherwise, basic monitoring is enabled. For more information, see Monitor your instances using CloudWatch in the Amazon EC2 User Guide. To disable detailed monitoring, see UnmonitorInstances."]
module ModifyVpnTunnelOptionsResult =
  struct
    type nonrec t = {
      vpnConnection: VpnConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnection = fun () -> { vpnConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnection",
           (Option.map x.vpnConnection ~f:VpnConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnection =
        (Option.map ~f:VpnConnection.of_xml)
          (Xml.child xml_arg0 "vpnConnection") in
      make ?vpnConnection ()
    let of_json json =
      let vpnConnection =
        field_map json "VpnConnection" VpnConnection.of_json in
      make ?vpnConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the options for a VPN tunnel in an Amazon Web Services Site-to-Site VPN connection. You can modify multiple options for a tunnel in a single request, but you can only modify one tunnel at a time. For more information, see Site-to-Site VPN tunnel options for your Site-to-Site VPN connection in the Amazon Web Services Site-to-Site VPN User Guide."]
module ModifyVpnTunnelOptionsRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc
          "The ID of the Amazon Web Services Site-to-Site VPN connection."];
      vpnTunnelOutsideIpAddress: String_.t
        [@ocaml.doc "The external IP address of the VPN tunnel."];
      tunnelOptions: ModifyVpnTunnelOptionsSpecification.t
        [@ocaml.doc "The tunnel options to modify."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyVpnTunnelOptionsRequest"
    let make ?dryRun =
      fun ~vpnConnectionId ->
        fun ~vpnTunnelOutsideIpAddress ->
          fun ~tunnelOptions ->
            fun () ->
              {
                dryRun;
                vpnConnectionId;
                vpnTunnelOutsideIpAddress;
                tunnelOptions
              }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("VpnTunnelOutsideIpAddress",
          (Some (String_.to_value x.vpnTunnelOutsideIpAddress)));
        ("TunnelOptions",
          (Some
             (ModifyVpnTunnelOptionsSpecification.to_value x.tunnelOptions)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tunnelOptions =
        ModifyVpnTunnelOptionsSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TunnelOptions") in
      let vpnTunnelOutsideIpAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VpnTunnelOutsideIpAddress") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ~tunnelOptions ~vpnTunnelOutsideIpAddress ~vpnConnectionId
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tunnelOptions =
        field_map_exn json "TunnelOptions"
          ModifyVpnTunnelOptionsSpecification.of_json in
      let vpnTunnelOutsideIpAddress =
        field_map_exn json "VpnTunnelOutsideIpAddress" String_.of_json in
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ~tunnelOptions ~vpnTunnelOutsideIpAddress ~vpnConnectionId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the options for a VPN tunnel in an Amazon Web Services Site-to-Site VPN connection. You can modify multiple options for a tunnel in a single request, but you can only modify one tunnel at a time. For more information, see Site-to-Site VPN tunnel options for your Site-to-Site VPN connection in the Amazon Web Services Site-to-Site VPN User Guide."]
module ModifyVpnTunnelCertificateResult =
  struct
    type nonrec t = {
      vpnConnection: VpnConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnection = fun () -> { vpnConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnection",
           (Option.map x.vpnConnection ~f:VpnConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnection =
        (Option.map ~f:VpnConnection.of_xml)
          (Xml.child xml_arg0 "vpnConnection") in
      make ?vpnConnection ()
    let of_json json =
      let vpnConnection =
        field_map json "VpnConnection" VpnConnection.of_json in
      make ?vpnConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the VPN tunnel endpoint certificate."]
module ModifyVpnTunnelCertificateRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc
          "The ID of the Amazon Web Services Site-to-Site VPN connection."];
      vpnTunnelOutsideIpAddress: String_.t
        [@ocaml.doc "The external IP address of the VPN tunnel."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyVpnTunnelCertificateRequest"
    let make ?dryRun =
      fun ~vpnConnectionId ->
        fun ~vpnTunnelOutsideIpAddress ->
          fun () -> { dryRun; vpnConnectionId; vpnTunnelOutsideIpAddress }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("VpnTunnelOutsideIpAddress",
          (Some (String_.to_value x.vpnTunnelOutsideIpAddress)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let vpnTunnelOutsideIpAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VpnTunnelOutsideIpAddress") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ~vpnTunnelOutsideIpAddress ~vpnConnectionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnTunnelOutsideIpAddress =
        field_map_exn json "VpnTunnelOutsideIpAddress" String_.of_json in
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ~vpnTunnelOutsideIpAddress ~vpnConnectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the VPN tunnel endpoint certificate."]
module ModifyVpnConnectionResult =
  struct
    type nonrec t = {
      vpnConnection: VpnConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnection = fun () -> { vpnConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnection",
           (Option.map x.vpnConnection ~f:VpnConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnection =
        (Option.map ~f:VpnConnection.of_xml)
          (Xml.child xml_arg0 "vpnConnection") in
      make ?vpnConnection ()
    let of_json json =
      let vpnConnection =
        field_map json "VpnConnection" VpnConnection.of_json in
      make ?vpnConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the customer gateway or the target gateway of an Amazon Web Services Site-to-Site VPN connection. To modify the target gateway, the following migration options are available: An existing virtual private gateway to a new virtual private gateway An existing virtual private gateway to a transit gateway An existing transit gateway to a new transit gateway An existing transit gateway to a virtual private gateway Before you perform the migration to the new gateway, you must configure the new gateway. Use CreateVpnGateway to create a virtual private gateway, or CreateTransitGateway to create a transit gateway. This step is required when you migrate from a virtual private gateway with static routes to a transit gateway. You must delete the static routes before you migrate to the new gateway. Keep a copy of the static route before you delete it. You will need to add back these routes to the transit gateway after the VPN connection migration is complete. After you migrate to the new gateway, you might need to modify your VPC route table. Use CreateRoute and DeleteRoute to make the changes described in Update VPC route tables in the Amazon Web Services Site-to-Site VPN User Guide. When the new gateway is a transit gateway, modify the transit gateway route table to allow traffic between the VPC and the Amazon Web Services Site-to-Site VPN connection. Use CreateTransitGatewayRoute to add the routes. If you deleted VPN static routes, you must add the static routes to the transit gateway route table. After you perform this operation, the VPN endpoint's IP addresses on the Amazon Web Services side and the tunnel options remain intact. Your Amazon Web Services Site-to-Site VPN connection will be temporarily unavailable for a brief period while we provision the new endpoints."]
module ModifyVpnConnectionRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc "The ID of the VPN connection."];
      transitGatewayId: TransitGatewayId.t option
        [@ocaml.doc "The ID of the transit gateway."];
      customerGatewayId: CustomerGatewayId.t option
        [@ocaml.doc
          "The ID of the customer gateway at your end of the VPN connection."];
      vpnGatewayId: VpnGatewayId.t option
        [@ocaml.doc
          "The ID of the virtual private gateway at the Amazon Web Services side of the VPN connection."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyVpnConnectionRequest"
    let make ?transitGatewayId =
      fun ?customerGatewayId ->
        fun ?vpnGatewayId ->
          fun ?dryRun ->
            fun ~vpnConnectionId ->
              fun () ->
                {
                  transitGatewayId;
                  customerGatewayId;
                  vpnGatewayId;
                  dryRun;
                  vpnConnectionId
                }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("CustomerGatewayId",
          (Option.map x.customerGatewayId ~f:CustomerGatewayId.to_value));
        ("VpnGatewayId",
          (Option.map x.vpnGatewayId ~f:VpnGatewayId.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let vpnGatewayId =
        (Option.map ~f:VpnGatewayId.of_xml)
          (Xml.child xml_arg0 "VpnGatewayId") in
      let customerGatewayId =
        (Option.map ~f:CustomerGatewayId.of_xml)
          (Xml.child xml_arg0 "CustomerGatewayId") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ?vpnGatewayId ?customerGatewayId ?transitGatewayId
        ~vpnConnectionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnGatewayId = field_map json "VpnGatewayId" VpnGatewayId.of_json in
      let customerGatewayId =
        field_map json "CustomerGatewayId" CustomerGatewayId.of_json in
      let transitGatewayId =
        field_map json "TransitGatewayId" TransitGatewayId.of_json in
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ?vpnGatewayId ?customerGatewayId ?transitGatewayId
        ~vpnConnectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the customer gateway or the target gateway of an Amazon Web Services Site-to-Site VPN connection. To modify the target gateway, the following migration options are available: An existing virtual private gateway to a new virtual private gateway An existing virtual private gateway to a transit gateway An existing transit gateway to a new transit gateway An existing transit gateway to a virtual private gateway Before you perform the migration to the new gateway, you must configure the new gateway. Use CreateVpnGateway to create a virtual private gateway, or CreateTransitGateway to create a transit gateway. This step is required when you migrate from a virtual private gateway with static routes to a transit gateway. You must delete the static routes before you migrate to the new gateway. Keep a copy of the static route before you delete it. You will need to add back these routes to the transit gateway after the VPN connection migration is complete. After you migrate to the new gateway, you might need to modify your VPC route table. Use CreateRoute and DeleteRoute to make the changes described in Update VPC route tables in the Amazon Web Services Site-to-Site VPN User Guide. When the new gateway is a transit gateway, modify the transit gateway route table to allow traffic between the VPC and the Amazon Web Services Site-to-Site VPN connection. Use CreateTransitGatewayRoute to add the routes. If you deleted VPN static routes, you must add the static routes to the transit gateway route table. After you perform this operation, the VPN endpoint's IP addresses on the Amazon Web Services side and the tunnel options remain intact. Your Amazon Web Services Site-to-Site VPN connection will be temporarily unavailable for a brief period while we provision the new endpoints."]
module ModifyVpnConnectionOptionsResult =
  struct
    type nonrec t = {
      vpnConnection: VpnConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnection = fun () -> { vpnConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnection",
           (Option.map x.vpnConnection ~f:VpnConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnection =
        (Option.map ~f:VpnConnection.of_xml)
          (Xml.child xml_arg0 "vpnConnection") in
      make ?vpnConnection ()
    let of_json json =
      let vpnConnection =
        field_map json "VpnConnection" VpnConnection.of_json in
      make ?vpnConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the connection options for your Site-to-Site VPN connection. When you modify the VPN connection options, the VPN endpoint IP addresses on the Amazon Web Services side do not change, and the tunnel options do not change. Your VPN connection will be temporarily unavailable for a brief period while the VPN connection is updated."]
module ModifyVpnConnectionOptionsRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc "The ID of the Site-to-Site VPN connection."];
      localIpv4NetworkCidr: String_.t option
        [@ocaml.doc
          "The IPv4 CIDR on the customer gateway (on-premises) side of the VPN connection. Default: 0.0.0.0/0"];
      remoteIpv4NetworkCidr: String_.t option
        [@ocaml.doc
          "The IPv4 CIDR on the Amazon Web Services side of the VPN connection. Default: 0.0.0.0/0"];
      localIpv6NetworkCidr: String_.t option
        [@ocaml.doc
          "The IPv6 CIDR on the customer gateway (on-premises) side of the VPN connection. Default: ::/0"];
      remoteIpv6NetworkCidr: String_.t option
        [@ocaml.doc
          "The IPv6 CIDR on the Amazon Web Services side of the VPN connection. Default: ::/0"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyVpnConnectionOptionsRequest"
    let make ?localIpv4NetworkCidr =
      fun ?remoteIpv4NetworkCidr ->
        fun ?localIpv6NetworkCidr ->
          fun ?remoteIpv6NetworkCidr ->
            fun ?dryRun ->
              fun ~vpnConnectionId ->
                fun () ->
                  {
                    localIpv4NetworkCidr;
                    remoteIpv4NetworkCidr;
                    localIpv6NetworkCidr;
                    remoteIpv6NetworkCidr;
                    dryRun;
                    vpnConnectionId
                  }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("LocalIpv4NetworkCidr",
          (Option.map x.localIpv4NetworkCidr ~f:String_.to_value));
        ("RemoteIpv4NetworkCidr",
          (Option.map x.remoteIpv4NetworkCidr ~f:String_.to_value));
        ("LocalIpv6NetworkCidr",
          (Option.map x.localIpv6NetworkCidr ~f:String_.to_value));
        ("RemoteIpv6NetworkCidr",
          (Option.map x.remoteIpv6NetworkCidr ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let remoteIpv6NetworkCidr =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "RemoteIpv6NetworkCidr") in
      let localIpv6NetworkCidr =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "LocalIpv6NetworkCidr") in
      let remoteIpv4NetworkCidr =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "RemoteIpv4NetworkCidr") in
      let localIpv4NetworkCidr =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "LocalIpv4NetworkCidr") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ?remoteIpv6NetworkCidr ?localIpv6NetworkCidr
        ?remoteIpv4NetworkCidr ?localIpv4NetworkCidr ~vpnConnectionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let remoteIpv6NetworkCidr =
        field_map json "RemoteIpv6NetworkCidr" String_.of_json in
      let localIpv6NetworkCidr =
        field_map json "LocalIpv6NetworkCidr" String_.of_json in
      let remoteIpv4NetworkCidr =
        field_map json "RemoteIpv4NetworkCidr" String_.of_json in
      let localIpv4NetworkCidr =
        field_map json "LocalIpv4NetworkCidr" String_.of_json in
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ?remoteIpv6NetworkCidr ?localIpv6NetworkCidr
        ?remoteIpv4NetworkCidr ?localIpv4NetworkCidr ~vpnConnectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the connection options for your Site-to-Site VPN connection. When you modify the VPN connection options, the VPN endpoint IP addresses on the Amazon Web Services side do not change, and the tunnel options do not change. Your VPN connection will be temporarily unavailable for a brief period while the VPN connection is updated."]
module ModifyVpcTenancyResult =
  struct
    type nonrec t =
      {
      returnValue: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?returnValue ()
    let of_json json =
      let returnValue = field_map json "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the instance tenancy attribute of the specified VPC. You can change the instance tenancy attribute of a VPC to default only. You cannot change the instance tenancy attribute to dedicated. After you modify the tenancy of the VPC, any new instances that you launch into the VPC have a tenancy of default, unless you specify otherwise during launch. The tenancy of any existing instances in the VPC is not affected. For more information, see Dedicated Instances in the Amazon Elastic Compute Cloud User Guide."]
module ModifyVpcTenancyRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      instanceTenancy: VpcTenancy.t
        [@ocaml.doc "The instance tenancy attribute for the VPC."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyVpcTenancyRequest"
    let make ?dryRun =
      fun ~vpcId ->
        fun ~instanceTenancy -> fun () -> { dryRun; vpcId; instanceTenancy }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("InstanceTenancy", (Some (VpcTenancy.to_value x.instanceTenancy)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let instanceTenancy =
        VpcTenancy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceTenancy") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?dryRun ~instanceTenancy ~vpcId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceTenancy =
        field_map_exn json "InstanceTenancy" VpcTenancy.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      make ?dryRun ~instanceTenancy ~vpcId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the instance tenancy attribute of the specified VPC. You can change the instance tenancy attribute of a VPC to default only. You cannot change the instance tenancy attribute to dedicated. After you modify the tenancy of the VPC, any new instances that you launch into the VPC have a tenancy of default, unless you specify otherwise during launch. The tenancy of any existing instances in the VPC is not affected. For more information, see Dedicated Instances in the Amazon Elastic Compute Cloud User Guide."]
module ModifyVpcPeeringConnectionOptionsResult =
  struct
    type nonrec t =
      {
      accepterPeeringConnectionOptions: PeeringConnectionOptions.t option
        [@ocaml.doc
          "Information about the VPC peering connection options for the accepter VPC."];
      requesterPeeringConnectionOptions: PeeringConnectionOptions.t option
        [@ocaml.doc
          "Information about the VPC peering connection options for the requester VPC."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?accepterPeeringConnectionOptions =
      fun ?requesterPeeringConnectionOptions ->
        fun () ->
          {
            accepterPeeringConnectionOptions;
            requesterPeeringConnectionOptions
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("accepterPeeringConnectionOptions",
           (Option.map x.accepterPeeringConnectionOptions
              ~f:PeeringConnectionOptions.to_value));
        ("requesterPeeringConnectionOptions",
          (Option.map x.requesterPeeringConnectionOptions
             ~f:PeeringConnectionOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requesterPeeringConnectionOptions =
        (Option.map ~f:PeeringConnectionOptions.of_xml)
          (Xml.child xml_arg0 "requesterPeeringConnectionOptions") in
      let accepterPeeringConnectionOptions =
        (Option.map ~f:PeeringConnectionOptions.of_xml)
          (Xml.child xml_arg0 "accepterPeeringConnectionOptions") in
      make ?requesterPeeringConnectionOptions
        ?accepterPeeringConnectionOptions ()
    let of_json json =
      let requesterPeeringConnectionOptions =
        field_map json "RequesterPeeringConnectionOptions"
          PeeringConnectionOptions.of_json in
      let accepterPeeringConnectionOptions =
        field_map json "AccepterPeeringConnectionOptions"
          PeeringConnectionOptions.of_json in
      make ?requesterPeeringConnectionOptions
        ?accepterPeeringConnectionOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the VPC peering connection options on one side of a VPC peering connection. You can do the following: Enable/disable communication over the peering connection between an EC2-Classic instance that's linked to your VPC (using ClassicLink) and instances in the peer VPC. Enable/disable communication over the peering connection between instances in your VPC and an EC2-Classic instance that's linked to the peer VPC. Enable/disable the ability to resolve public DNS hostnames to private IP addresses when queried from instances in the peer VPC. If the peered VPCs are in the same Amazon Web Services account, you can enable DNS resolution for queries from the local VPC. This ensures that queries from the local VPC resolve to private IP addresses in the peer VPC. This option is not available if the peered VPCs are in different different Amazon Web Services accounts or different Regions. For peered VPCs in different Amazon Web Services accounts, each Amazon Web Services account owner must initiate a separate request to modify the peering connection options. For inter-region peering connections, you must use the Region for the requester VPC to modify the requester VPC peering options and the Region for the accepter VPC to modify the accepter VPC peering options. To verify which VPCs are the accepter and the requester for a VPC peering connection, use the DescribeVpcPeeringConnections command."]
module ModifyVpcPeeringConnectionOptionsRequest =
  struct
    type nonrec t =
      {
      accepterPeeringConnectionOptions:
        PeeringConnectionOptionsRequest.t option
        [@ocaml.doc
          "The VPC peering connection options for the accepter VPC."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      requesterPeeringConnectionOptions:
        PeeringConnectionOptionsRequest.t option
        [@ocaml.doc
          "The VPC peering connection options for the requester VPC."];
      vpcPeeringConnectionId: VpcPeeringConnectionId.t
        [@ocaml.doc "The ID of the VPC peering connection."]}
    let context_ = "ModifyVpcPeeringConnectionOptionsRequest"
    let make ?accepterPeeringConnectionOptions =
      fun ?dryRun ->
        fun ?requesterPeeringConnectionOptions ->
          fun ~vpcPeeringConnectionId ->
            fun () ->
              {
                accepterPeeringConnectionOptions;
                dryRun;
                requesterPeeringConnectionOptions;
                vpcPeeringConnectionId
              }
    let to_value x =
      structure_to_value
        [("AccepterPeeringConnectionOptions",
           (Option.map x.accepterPeeringConnectionOptions
              ~f:PeeringConnectionOptionsRequest.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("RequesterPeeringConnectionOptions",
          (Option.map x.requesterPeeringConnectionOptions
             ~f:PeeringConnectionOptionsRequest.to_value));
        ("VpcPeeringConnectionId",
          (Some (VpcPeeringConnectionId.to_value x.vpcPeeringConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        VpcPeeringConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcPeeringConnectionId") in
      let requesterPeeringConnectionOptions =
        (Option.map ~f:PeeringConnectionOptionsRequest.of_xml)
          (Xml.child xml_arg0 "RequesterPeeringConnectionOptions") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let accepterPeeringConnectionOptions =
        (Option.map ~f:PeeringConnectionOptionsRequest.of_xml)
          (Xml.child xml_arg0 "AccepterPeeringConnectionOptions") in
      make ~vpcPeeringConnectionId ?requesterPeeringConnectionOptions ?dryRun
        ?accepterPeeringConnectionOptions ()
    let of_json json =
      let vpcPeeringConnectionId =
        field_map_exn json "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let requesterPeeringConnectionOptions =
        field_map json "RequesterPeeringConnectionOptions"
          PeeringConnectionOptionsRequest.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let accepterPeeringConnectionOptions =
        field_map json "AccepterPeeringConnectionOptions"
          PeeringConnectionOptionsRequest.of_json in
      make ~vpcPeeringConnectionId ?requesterPeeringConnectionOptions ?dryRun
        ?accepterPeeringConnectionOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the VPC peering connection options on one side of a VPC peering connection. You can do the following: Enable/disable communication over the peering connection between an EC2-Classic instance that's linked to your VPC (using ClassicLink) and instances in the peer VPC. Enable/disable communication over the peering connection between instances in your VPC and an EC2-Classic instance that's linked to the peer VPC. Enable/disable the ability to resolve public DNS hostnames to private IP addresses when queried from instances in the peer VPC. If the peered VPCs are in the same Amazon Web Services account, you can enable DNS resolution for queries from the local VPC. This ensures that queries from the local VPC resolve to private IP addresses in the peer VPC. This option is not available if the peered VPCs are in different different Amazon Web Services accounts or different Regions. For peered VPCs in different Amazon Web Services accounts, each Amazon Web Services account owner must initiate a separate request to modify the peering connection options. For inter-region peering connections, you must use the Region for the requester VPC to modify the requester VPC peering options and the Region for the accepter VPC to modify the accepter VPC peering options. To verify which VPCs are the accepter and the requester for a VPC peering connection, use the DescribeVpcPeeringConnections command."]
module ModifyVpcEndpointServicePermissionsResult =
  struct
    type nonrec t =
      {
      returnValue: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?returnValue ()
    let of_json json =
      let returnValue = field_map json "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the permissions for your VPC endpoint service. You can add or remove permissions for service consumers (IAM users, IAM roles, and Amazon Web Services accounts) to connect to your endpoint service. If you grant permissions to all principals, the service is public. Any users who know the name of a public service can send a request to attach an endpoint. If the service does not require manual approval, attachments are automatically approved."]
module ModifyVpcEndpointServicePermissionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t [@ocaml.doc "The ID of the service."];
      addAllowedPrincipals: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARN) of one or more principals. Permissions are granted to the principals in this list. To grant permissions to all principals, specify an asterisk (*)."];
      removeAllowedPrincipals: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARN) of one or more principals. Permissions are revoked for principals in this list."]}
    let context_ = "ModifyVpcEndpointServicePermissionsRequest"
    let make ?dryRun =
      fun ?addAllowedPrincipals ->
        fun ?removeAllowedPrincipals ->
          fun ~serviceId ->
            fun () ->
              {
                dryRun;
                addAllowedPrincipals;
                removeAllowedPrincipals;
                serviceId
              }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("AddAllowedPrincipals",
          (Option.map x.addAllowedPrincipals ~f:ValueStringList.to_value));
        ("RemoveAllowedPrincipals",
          (Option.map x.removeAllowedPrincipals ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeAllowedPrincipals =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "RemoveAllowedPrincipals") in
      let addAllowedPrincipals =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "AddAllowedPrincipals") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?removeAllowedPrincipals ?addAllowedPrincipals ~serviceId ?dryRun
        ()
    let of_json json =
      let removeAllowedPrincipals =
        field_map json "RemoveAllowedPrincipals" ValueStringList.of_json in
      let addAllowedPrincipals =
        field_map json "AddAllowedPrincipals" ValueStringList.of_json in
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?removeAllowedPrincipals ?addAllowedPrincipals ~serviceId ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the permissions for your VPC endpoint service. You can add or remove permissions for service consumers (IAM users, IAM roles, and Amazon Web Services accounts) to connect to your endpoint service. If you grant permissions to all principals, the service is public. Any users who know the name of a public service can send a request to attach an endpoint. If the service does not require manual approval, attachments are automatically approved."]
module ModifyVpcEndpointServicePayerResponsibilityResult =
  struct
    type nonrec t =
      {
      returnValue: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?returnValue ()
    let of_json json =
      let returnValue = field_map json "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the payer responsibility for your VPC endpoint service."]
module ModifyVpcEndpointServicePayerResponsibilityRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t [@ocaml.doc "The ID of the service."];
      payerResponsibility: PayerResponsibility.t
        [@ocaml.doc
          "The entity that is responsible for the endpoint costs. The default is the endpoint owner. If you set the payer responsibility to the service owner, you cannot set it back to the endpoint owner."]}
    let context_ = "ModifyVpcEndpointServicePayerResponsibilityRequest"
    let make ?dryRun =
      fun ~serviceId ->
        fun ~payerResponsibility ->
          fun () -> { dryRun; serviceId; payerResponsibility }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("PayerResponsibility",
          (Some (PayerResponsibility.to_value x.payerResponsibility)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let payerResponsibility =
        PayerResponsibility.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PayerResponsibility") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~payerResponsibility ~serviceId ?dryRun ()
    let of_json json =
      let payerResponsibility =
        field_map_exn json "PayerResponsibility" PayerResponsibility.of_json in
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~payerResponsibility ~serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the payer responsibility for your VPC endpoint service."]
module ModifyVpcEndpointServiceConfigurationResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the attributes of your VPC endpoint service configuration. You can change the Network Load Balancers or Gateway Load Balancers for your service, and you can specify whether acceptance is required for requests to connect to your endpoint service through an interface VPC endpoint. If you set or modify the private DNS name, you must prove that you own the private DNS domain name. For more information, see VPC Endpoint Service Private DNS Name Verification in the Amazon Virtual Private Cloud User Guide."]
module ModifyVpcEndpointServiceConfigurationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t [@ocaml.doc "The ID of the service."];
      privateDnsName: String_.t option
        [@ocaml.doc
          "(Interface endpoint configuration) The private DNS name to assign to the endpoint service."];
      removePrivateDnsName: Boolean.t option
        [@ocaml.doc
          "(Interface endpoint configuration) Removes the private DNS name of the endpoint service."];
      acceptanceRequired: Boolean.t option
        [@ocaml.doc
          "Indicates whether requests to create an endpoint to your service must be accepted."];
      addNetworkLoadBalancerArns: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of Network Load Balancers to add to your service configuration."];
      removeNetworkLoadBalancerArns: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of Network Load Balancers to remove from your service configuration."];
      addGatewayLoadBalancerArns: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of Gateway Load Balancers to add to your service configuration."];
      removeGatewayLoadBalancerArns: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of Gateway Load Balancers to remove from your service configuration."]}
    let context_ = "ModifyVpcEndpointServiceConfigurationRequest"
    let make ?dryRun =
      fun ?privateDnsName ->
        fun ?removePrivateDnsName ->
          fun ?acceptanceRequired ->
            fun ?addNetworkLoadBalancerArns ->
              fun ?removeNetworkLoadBalancerArns ->
                fun ?addGatewayLoadBalancerArns ->
                  fun ?removeGatewayLoadBalancerArns ->
                    fun ~serviceId ->
                      fun () ->
                        {
                          dryRun;
                          privateDnsName;
                          removePrivateDnsName;
                          acceptanceRequired;
                          addNetworkLoadBalancerArns;
                          removeNetworkLoadBalancerArns;
                          addGatewayLoadBalancerArns;
                          removeGatewayLoadBalancerArns;
                          serviceId
                        }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("PrivateDnsName", (Option.map x.privateDnsName ~f:String_.to_value));
        ("RemovePrivateDnsName",
          (Option.map x.removePrivateDnsName ~f:Boolean.to_value));
        ("AcceptanceRequired",
          (Option.map x.acceptanceRequired ~f:Boolean.to_value));
        ("AddNetworkLoadBalancerArn",
          (Option.map x.addNetworkLoadBalancerArns
             ~f:ValueStringList.to_value));
        ("RemoveNetworkLoadBalancerArn",
          (Option.map x.removeNetworkLoadBalancerArns
             ~f:ValueStringList.to_value));
        ("AddGatewayLoadBalancerArn",
          (Option.map x.addGatewayLoadBalancerArns
             ~f:ValueStringList.to_value));
        ("RemoveGatewayLoadBalancerArn",
          (Option.map x.removeGatewayLoadBalancerArns
             ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeGatewayLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "RemoveGatewayLoadBalancerArn") in
      let addGatewayLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "AddGatewayLoadBalancerArn") in
      let removeNetworkLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "RemoveNetworkLoadBalancerArn") in
      let addNetworkLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "AddNetworkLoadBalancerArn") in
      let acceptanceRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AcceptanceRequired") in
      let removePrivateDnsName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RemovePrivateDnsName") in
      let privateDnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PrivateDnsName") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?removeGatewayLoadBalancerArns ?addGatewayLoadBalancerArns
        ?removeNetworkLoadBalancerArns ?addNetworkLoadBalancerArns
        ?acceptanceRequired ?removePrivateDnsName ?privateDnsName ~serviceId
        ?dryRun ()
    let of_json json =
      let removeGatewayLoadBalancerArns =
        field_map json "RemoveGatewayLoadBalancerArns"
          ValueStringList.of_json in
      let addGatewayLoadBalancerArns =
        field_map json "AddGatewayLoadBalancerArns" ValueStringList.of_json in
      let removeNetworkLoadBalancerArns =
        field_map json "RemoveNetworkLoadBalancerArns"
          ValueStringList.of_json in
      let addNetworkLoadBalancerArns =
        field_map json "AddNetworkLoadBalancerArns" ValueStringList.of_json in
      let acceptanceRequired =
        field_map json "AcceptanceRequired" Boolean.of_json in
      let removePrivateDnsName =
        field_map json "RemovePrivateDnsName" Boolean.of_json in
      let privateDnsName = field_map json "PrivateDnsName" String_.of_json in
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?removeGatewayLoadBalancerArns ?addGatewayLoadBalancerArns
        ?removeNetworkLoadBalancerArns ?addNetworkLoadBalancerArns
        ?acceptanceRequired ?removePrivateDnsName ?privateDnsName ~serviceId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the attributes of your VPC endpoint service configuration. You can change the Network Load Balancers or Gateway Load Balancers for your service, and you can specify whether acceptance is required for requests to connect to your endpoint service through an interface VPC endpoint. If you set or modify the private DNS name, you must prove that you own the private DNS domain name. For more information, see VPC Endpoint Service Private DNS Name Verification in the Amazon Virtual Private Cloud User Guide."]
module ModifyVpcEndpointResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies attributes of a specified VPC endpoint. The attributes that you can modify depend on the type of VPC endpoint (interface, gateway, or Gateway Load Balancer). For more information, see VPC Endpoints in the Amazon Virtual Private Cloud User Guide."]
module ModifyVpcEndpointRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcEndpointId: VpcEndpointId.t [@ocaml.doc "The ID of the endpoint."];
      resetPolicy: Boolean.t option
        [@ocaml.doc
          "(Gateway endpoint) Specify true to reset the policy document to the default policy. The default policy allows full access to the service."];
      policyDocument: String_.t option
        [@ocaml.doc
          "(Interface and gateway endpoints) A policy to attach to the endpoint that controls access to the service. The policy must be in valid JSON format."];
      addRouteTableIds: VpcEndpointRouteTableIdList.t option
        [@ocaml.doc
          "(Gateway endpoint) One or more route tables IDs to associate with the endpoint."];
      removeRouteTableIds: VpcEndpointRouteTableIdList.t option
        [@ocaml.doc
          "(Gateway endpoint) One or more route table IDs to disassociate from the endpoint."];
      addSubnetIds: VpcEndpointSubnetIdList.t option
        [@ocaml.doc
          "(Interface and Gateway Load Balancer endpoints) One or more subnet IDs in which to serve the endpoint. For a Gateway Load Balancer endpoint, you can specify only one subnet."];
      removeSubnetIds: VpcEndpointSubnetIdList.t option
        [@ocaml.doc
          "(Interface endpoint) One or more subnets IDs in which to remove the endpoint."];
      addSecurityGroupIds: VpcEndpointSecurityGroupIdList.t option
        [@ocaml.doc
          "(Interface endpoint) One or more security group IDs to associate with the network interface."];
      removeSecurityGroupIds: VpcEndpointSecurityGroupIdList.t option
        [@ocaml.doc
          "(Interface endpoint) One or more security group IDs to disassociate from the network interface."];
      privateDnsEnabled: Boolean.t option
        [@ocaml.doc
          "(Interface endpoint) Indicates whether a private hosted zone is associated with the VPC."]}
    let context_ = "ModifyVpcEndpointRequest"
    let make ?dryRun =
      fun ?resetPolicy ->
        fun ?policyDocument ->
          fun ?addRouteTableIds ->
            fun ?removeRouteTableIds ->
              fun ?addSubnetIds ->
                fun ?removeSubnetIds ->
                  fun ?addSecurityGroupIds ->
                    fun ?removeSecurityGroupIds ->
                      fun ?privateDnsEnabled ->
                        fun ~vpcEndpointId ->
                          fun () ->
                            {
                              dryRun;
                              resetPolicy;
                              policyDocument;
                              addRouteTableIds;
                              removeRouteTableIds;
                              addSubnetIds;
                              removeSubnetIds;
                              addSecurityGroupIds;
                              removeSecurityGroupIds;
                              privateDnsEnabled;
                              vpcEndpointId
                            }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointId", (Some (VpcEndpointId.to_value x.vpcEndpointId)));
        ("ResetPolicy", (Option.map x.resetPolicy ~f:Boolean.to_value));
        ("PolicyDocument", (Option.map x.policyDocument ~f:String_.to_value));
        ("AddRouteTableId",
          (Option.map x.addRouteTableIds
             ~f:VpcEndpointRouteTableIdList.to_value));
        ("RemoveRouteTableId",
          (Option.map x.removeRouteTableIds
             ~f:VpcEndpointRouteTableIdList.to_value));
        ("AddSubnetId",
          (Option.map x.addSubnetIds ~f:VpcEndpointSubnetIdList.to_value));
        ("RemoveSubnetId",
          (Option.map x.removeSubnetIds ~f:VpcEndpointSubnetIdList.to_value));
        ("AddSecurityGroupId",
          (Option.map x.addSecurityGroupIds
             ~f:VpcEndpointSecurityGroupIdList.to_value));
        ("RemoveSecurityGroupId",
          (Option.map x.removeSecurityGroupIds
             ~f:VpcEndpointSecurityGroupIdList.to_value));
        ("PrivateDnsEnabled",
          (Option.map x.privateDnsEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let privateDnsEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PrivateDnsEnabled") in
      let removeSecurityGroupIds =
        (Option.map ~f:VpcEndpointSecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "RemoveSecurityGroupId") in
      let addSecurityGroupIds =
        (Option.map ~f:VpcEndpointSecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "AddSecurityGroupId") in
      let removeSubnetIds =
        (Option.map ~f:VpcEndpointSubnetIdList.of_xml)
          (Xml.child xml_arg0 "RemoveSubnetId") in
      let addSubnetIds =
        (Option.map ~f:VpcEndpointSubnetIdList.of_xml)
          (Xml.child xml_arg0 "AddSubnetId") in
      let removeRouteTableIds =
        (Option.map ~f:VpcEndpointRouteTableIdList.of_xml)
          (Xml.child xml_arg0 "RemoveRouteTableId") in
      let addRouteTableIds =
        (Option.map ~f:VpcEndpointRouteTableIdList.of_xml)
          (Xml.child xml_arg0 "AddRouteTableId") in
      let policyDocument =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PolicyDocument") in
      let resetPolicy =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ResetPolicy") in
      let vpcEndpointId =
        VpcEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?privateDnsEnabled ?removeSecurityGroupIds ?addSecurityGroupIds
        ?removeSubnetIds ?addSubnetIds ?removeRouteTableIds ?addRouteTableIds
        ?policyDocument ?resetPolicy ~vpcEndpointId ?dryRun ()
    let of_json json =
      let privateDnsEnabled =
        field_map json "PrivateDnsEnabled" Boolean.of_json in
      let removeSecurityGroupIds =
        field_map json "RemoveSecurityGroupIds"
          VpcEndpointSecurityGroupIdList.of_json in
      let addSecurityGroupIds =
        field_map json "AddSecurityGroupIds"
          VpcEndpointSecurityGroupIdList.of_json in
      let removeSubnetIds =
        field_map json "RemoveSubnetIds" VpcEndpointSubnetIdList.of_json in
      let addSubnetIds =
        field_map json "AddSubnetIds" VpcEndpointSubnetIdList.of_json in
      let removeRouteTableIds =
        field_map json "RemoveRouteTableIds"
          VpcEndpointRouteTableIdList.of_json in
      let addRouteTableIds =
        field_map json "AddRouteTableIds" VpcEndpointRouteTableIdList.of_json in
      let policyDocument = field_map json "PolicyDocument" String_.of_json in
      let resetPolicy = field_map json "ResetPolicy" Boolean.of_json in
      let vpcEndpointId =
        field_map_exn json "VpcEndpointId" VpcEndpointId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?privateDnsEnabled ?removeSecurityGroupIds ?addSecurityGroupIds
        ?removeSubnetIds ?addSubnetIds ?removeRouteTableIds ?addRouteTableIds
        ?policyDocument ?resetPolicy ~vpcEndpointId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for ModifyVpcEndpoint."]
module ModifyVpcEndpointConnectionNotificationResult =
  struct
    type nonrec t =
      {
      returnValue: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?returnValue ()
    let of_json json =
      let returnValue = field_map json "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a connection notification for VPC endpoint or VPC endpoint service. You can change the SNS topic for the notification, or the events for which to be notified."]
module ModifyVpcEndpointConnectionNotificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      connectionNotificationId: ConnectionNotificationId.t
        [@ocaml.doc "The ID of the notification."];
      connectionNotificationArn: String_.t option
        [@ocaml.doc "The ARN for the SNS topic for the notification."];
      connectionEvents: ValueStringList.t option
        [@ocaml.doc
          "One or more events for the endpoint. Valid values are Accept, Connect, Delete, and Reject."]}
    let context_ = "ModifyVpcEndpointConnectionNotificationRequest"
    let make ?dryRun =
      fun ?connectionNotificationArn ->
        fun ?connectionEvents ->
          fun ~connectionNotificationId ->
            fun () ->
              {
                dryRun;
                connectionNotificationArn;
                connectionEvents;
                connectionNotificationId
              }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ConnectionNotificationId",
          (Some
             (ConnectionNotificationId.to_value x.connectionNotificationId)));
        ("ConnectionNotificationArn",
          (Option.map x.connectionNotificationArn ~f:String_.to_value));
        ("ConnectionEvents",
          (Option.map x.connectionEvents ~f:ValueStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectionEvents =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "ConnectionEvents") in
      let connectionNotificationArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ConnectionNotificationArn") in
      let connectionNotificationId =
        ConnectionNotificationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ConnectionNotificationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?connectionEvents ?connectionNotificationArn
        ~connectionNotificationId ?dryRun ()
    let of_json json =
      let connectionEvents =
        field_map json "ConnectionEvents" ValueStringList.of_json in
      let connectionNotificationArn =
        field_map json "ConnectionNotificationArn" String_.of_json in
      let connectionNotificationId =
        field_map_exn json "ConnectionNotificationId"
          ConnectionNotificationId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?connectionEvents ?connectionNotificationArn
        ~connectionNotificationId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a connection notification for VPC endpoint or VPC endpoint service. You can change the SNS topic for the notification, or the events for which to be notified."]
module ModifyVpcAttributeRequest =
  struct
    type nonrec t =
      {
      enableDnsHostnames: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether the instances launched in the VPC get DNS hostnames. If enabled, instances in the VPC get DNS hostnames; otherwise, they do not. You cannot modify the DNS resolution and DNS hostnames attributes in the same request. Use separate requests for each attribute. You can only enable DNS hostnames if you've enabled DNS support."];
      enableDnsSupport: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether the DNS resolution is supported for the VPC. If enabled, queries to the Amazon provided DNS server at the 169.254.169.253 IP address, or the reserved IP address at the base of the VPC network range \"plus two\" succeed. If disabled, the Amazon provided DNS service in the VPC that resolves public DNS hostnames to IP addresses is not enabled. You cannot modify the DNS resolution and DNS hostnames attributes in the same request. Use separate requests for each attribute."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."]}
    let context_ = "ModifyVpcAttributeRequest"
    let make ?enableDnsHostnames =
      fun ?enableDnsSupport ->
        fun ~vpcId ->
          fun () -> { enableDnsHostnames; enableDnsSupport; vpcId }
    let to_value x =
      structure_to_value
        [("EnableDnsHostnames",
           (Option.map x.enableDnsHostnames ~f:AttributeBooleanValue.to_value));
        ("EnableDnsSupport",
          (Option.map x.enableDnsSupport ~f:AttributeBooleanValue.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let enableDnsSupport =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "EnableDnsSupport") in
      let enableDnsHostnames =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "EnableDnsHostnames") in
      make ~vpcId ?enableDnsSupport ?enableDnsHostnames ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let enableDnsSupport =
        field_map json "EnableDnsSupport" AttributeBooleanValue.of_json in
      let enableDnsHostnames =
        field_map json "EnableDnsHostnames" AttributeBooleanValue.of_json in
      make ~vpcId ?enableDnsSupport ?enableDnsHostnames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the specified attribute of the specified VPC."]
module ModifyVolumeResult =
  struct
    type nonrec t =
      {
      volumeModification: VolumeModification.t option
        [@ocaml.doc "Information about the volume modification."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?volumeModification = fun () -> { volumeModification }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("volumeModification",
           (Option.map x.volumeModification ~f:VolumeModification.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeModification =
        (Option.map ~f:VolumeModification.of_xml)
          (Xml.child xml_arg0 "volumeModification") in
      make ?volumeModification ()
    let of_json json =
      let volumeModification =
        field_map json "VolumeModification" VolumeModification.of_json in
      make ?volumeModification ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can modify several parameters of an existing EBS volume, including volume size, volume type, and IOPS capacity. If your EBS volume is attached to a current-generation EC2 instance type, you might be able to apply these changes without stopping the instance or detaching the volume from it. For more information about modifying EBS volumes, see Amazon EBS Elastic Volumes (Linux instances) or Amazon EBS Elastic Volumes (Windows instances). When you complete a resize operation on your volume, you need to extend the volume's file-system size to take advantage of the new storage capacity. For more information, see Extend a Linux file system or Extend a Windows file system. You can use CloudWatch Events to check the status of a modification to an EBS volume. For information about CloudWatch Events, see the Amazon CloudWatch Events User Guide. You can also track the status of a modification using DescribeVolumesModifications. For information about tracking status changes using either method, see Monitor the progress of volume modifications. With previous-generation instance types, resizing an EBS volume might require detaching and reattaching the volume or stopping and restarting the instance. If you reach the maximum volume modification rate per volume limit, you must wait at least six hours before applying further modifications to the affected EBS volume."]
module ModifyVolumeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      volumeId: VolumeId.t [@ocaml.doc "The ID of the volume."];
      size: Integer.t option
        [@ocaml.doc
          "The target size of the volume, in GiB. The target volume size must be greater than or equal to the existing size of the volume. The following are the supported volumes sizes for each volume type: gp2 and gp3: 1-16,384 io1 and io2: 4-16,384 st1 and sc1: 125-16,384 standard: 1-1,024 Default: The existing size is retained."];
      volumeType: VolumeType.t option
        [@ocaml.doc
          "The target EBS volume type of the volume. For more information, see Amazon EBS volume types in the Amazon Elastic Compute Cloud User Guide. Default: The existing type is retained."];
      iops: Integer.t option
        [@ocaml.doc
          "The target IOPS rate of the volume. This parameter is valid only for gp3, io1, and io2 volumes. The following are the supported values for each volume type: gp3: 3,000-16,000 IOPS io1: 100-64,000 IOPS io2: 100-64,000 IOPS Default: The existing value is retained if you keep the same volume type. If you change the volume type to io1, io2, or gp3, the default is 3,000."];
      throughput: Integer.t option
        [@ocaml.doc
          "The target throughput of the volume, in MiB/s. This parameter is valid only for gp3 volumes. The maximum value is 1,000. Default: The existing value is retained if the source and target volume type is gp3. Otherwise, the default value is 125. Valid Range: Minimum value of 125. Maximum value of 1000."];
      multiAttachEnabled: Boolean.t option
        [@ocaml.doc
          "Specifies whether to enable Amazon EBS Multi-Attach. If you enable Multi-Attach, you can attach the volume to up to 16 Nitro-based instances in the same Availability Zone. This parameter is supported with io1 and io2 volumes only. For more information, see Amazon EBS Multi-Attach in the Amazon Elastic Compute Cloud User Guide."]}
    let context_ = "ModifyVolumeRequest"
    let make ?dryRun =
      fun ?size ->
        fun ?volumeType ->
          fun ?iops ->
            fun ?throughput ->
              fun ?multiAttachEnabled ->
                fun ~volumeId ->
                  fun () ->
                    {
                      dryRun;
                      size;
                      volumeType;
                      iops;
                      throughput;
                      multiAttachEnabled;
                      volumeId
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("Size", (Option.map x.size ~f:Integer.to_value));
        ("VolumeType", (Option.map x.volumeType ~f:VolumeType.to_value));
        ("Iops", (Option.map x.iops ~f:Integer.to_value));
        ("Throughput", (Option.map x.throughput ~f:Integer.to_value));
        ("MultiAttachEnabled",
          (Option.map x.multiAttachEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let multiAttachEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "MultiAttachEnabled") in
      let throughput =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Throughput") in
      let iops = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Iops") in
      let volumeType =
        (Option.map ~f:VolumeType.of_xml) (Xml.child xml_arg0 "VolumeType") in
      let size = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Size") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?multiAttachEnabled ?throughput ?iops ?volumeType ?size ~volumeId
        ?dryRun ()
    let of_json json =
      let multiAttachEnabled =
        field_map json "MultiAttachEnabled" Boolean.of_json in
      let throughput = field_map json "Throughput" Integer.of_json in
      let iops = field_map json "Iops" Integer.of_json in
      let volumeType = field_map json "VolumeType" VolumeType.of_json in
      let size = field_map json "Size" Integer.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?multiAttachEnabled ?throughput ?iops ?volumeType ?size ~volumeId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can modify several parameters of an existing EBS volume, including volume size, volume type, and IOPS capacity. If your EBS volume is attached to a current-generation EC2 instance type, you might be able to apply these changes without stopping the instance or detaching the volume from it. For more information about modifying EBS volumes, see Amazon EBS Elastic Volumes (Linux instances) or Amazon EBS Elastic Volumes (Windows instances). When you complete a resize operation on your volume, you need to extend the volume's file-system size to take advantage of the new storage capacity. For more information, see Extend a Linux file system or Extend a Windows file system. You can use CloudWatch Events to check the status of a modification to an EBS volume. For information about CloudWatch Events, see the Amazon CloudWatch Events User Guide. You can also track the status of a modification using DescribeVolumesModifications. For information about tracking status changes using either method, see Monitor the progress of volume modifications. With previous-generation instance types, resizing an EBS volume might require detaching and reattaching the volume or stopping and restarting the instance. If you reach the maximum volume modification rate per volume limit, you must wait at least six hours before applying further modifications to the affected EBS volume."]
module ModifyVolumeAttributeRequest =
  struct
    type nonrec t =
      {
      autoEnableIO: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether the volume should be auto-enabled for I/O operations."];
      volumeId: VolumeId.t [@ocaml.doc "The ID of the volume."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyVolumeAttributeRequest"
    let make ?autoEnableIO =
      fun ?dryRun ->
        fun ~volumeId -> fun () -> { autoEnableIO; dryRun; volumeId }
    let to_value x =
      structure_to_value
        [("AutoEnableIO",
           (Option.map x.autoEnableIO ~f:AttributeBooleanValue.to_value));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let autoEnableIO =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "AutoEnableIO") in
      make ?dryRun ~volumeId ?autoEnableIO ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let autoEnableIO =
        field_map json "AutoEnableIO" AttributeBooleanValue.of_json in
      make ?dryRun ~volumeId ?autoEnableIO ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a volume attribute. By default, all I/O operations for the volume are suspended when the data on the volume is determined to be potentially inconsistent, to prevent undetectable, latent data corruption. The I/O access to the volume can be resumed by first enabling I/O access and then checking the data consistency on your volume. You can change the default behavior to resume I/O operations. We recommend that you change this only for boot volumes or for volumes that are stateless or disposable."]
module ModifyTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option
        [@ocaml.doc "Information about the modified attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_json json =
      let transitGatewayVpcAttachment =
        field_map json "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the specified VPC attachment."]
module ModifyTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the attachment."];
      addSubnetIds: TransitGatewaySubnetIdList.t option
        [@ocaml.doc
          "The IDs of one or more subnets to add. You can specify at most one subnet per Availability Zone."];
      removeSubnetIds: TransitGatewaySubnetIdList.t option
        [@ocaml.doc "The IDs of one or more subnets to remove."];
      options: ModifyTransitGatewayVpcAttachmentRequestOptions.t option
        [@ocaml.doc "The new VPC attachment options."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyTransitGatewayVpcAttachmentRequest"
    let make ?addSubnetIds =
      fun ?removeSubnetIds ->
        fun ?options ->
          fun ?dryRun ->
            fun ~transitGatewayAttachmentId ->
              fun () ->
                {
                  addSubnetIds;
                  removeSubnetIds;
                  options;
                  dryRun;
                  transitGatewayAttachmentId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("AddSubnetIds",
          (Option.map x.addSubnetIds ~f:TransitGatewaySubnetIdList.to_value));
        ("RemoveSubnetIds",
          (Option.map x.removeSubnetIds
             ~f:TransitGatewaySubnetIdList.to_value));
        ("Options",
          (Option.map x.options
             ~f:ModifyTransitGatewayVpcAttachmentRequestOptions.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let options =
        (Option.map ~f:ModifyTransitGatewayVpcAttachmentRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let removeSubnetIds =
        (Option.map ~f:TransitGatewaySubnetIdList.of_xml)
          (Xml.child xml_arg0 "RemoveSubnetIds") in
      let addSubnetIds =
        (Option.map ~f:TransitGatewaySubnetIdList.of_xml)
          (Xml.child xml_arg0 "AddSubnetIds") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ?options ?removeSubnetIds ?addSubnetIds
        ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let options =
        field_map json "Options"
          ModifyTransitGatewayVpcAttachmentRequestOptions.of_json in
      let removeSubnetIds =
        field_map json "RemoveSubnetIds" TransitGatewaySubnetIdList.of_json in
      let addSubnetIds =
        field_map json "AddSubnetIds" TransitGatewaySubnetIdList.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ?options ?removeSubnetIds ?addSubnetIds
        ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the specified VPC attachment."]
module ModifyTransitGatewayResult =
  struct
    type nonrec t = {
      transitGateway: TransitGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGateway = fun () -> { transitGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGateway",
           (Option.map x.transitGateway ~f:TransitGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGateway =
        (Option.map ~f:TransitGateway.of_xml)
          (Xml.child xml_arg0 "transitGateway") in
      make ?transitGateway ()
    let of_json json =
      let transitGateway =
        field_map json "TransitGateway" TransitGateway.of_json in
      make ?transitGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified transit gateway. When you modify a transit gateway, the modified options are applied to new transit gateway attachments only. Your existing transit gateway attachments are not modified."]
module ModifyTransitGatewayRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc "The ID of the transit gateway."];
      description: String_.t option
        [@ocaml.doc "The description for the transit gateway."];
      options: ModifyTransitGatewayOptions.t option
        [@ocaml.doc "The options to modify."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyTransitGatewayRequest"
    let make ?description =
      fun ?options ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun () -> { description; options; dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Options",
          (Option.map x.options ~f:ModifyTransitGatewayOptions.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let options =
        (Option.map ~f:ModifyTransitGatewayOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?options ?description ~transitGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let options =
        field_map json "Options" ModifyTransitGatewayOptions.of_json in
      let description = field_map json "Description" String_.of_json in
      let transitGatewayId =
        field_map_exn json "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?options ?description ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified transit gateway. When you modify a transit gateway, the modified options are applied to new transit gateway attachments only. Your existing transit gateway attachments are not modified."]
module ModifyTransitGatewayPrefixListReferenceResult =
  struct
    type nonrec t =
      {
      transitGatewayPrefixListReference:
        TransitGatewayPrefixListReference.t option
        [@ocaml.doc "Information about the prefix list reference."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPrefixListReference =
      fun () -> { transitGatewayPrefixListReference }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPrefixListReference",
           (Option.map x.transitGatewayPrefixListReference
              ~f:TransitGatewayPrefixListReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPrefixListReference =
        (Option.map ~f:TransitGatewayPrefixListReference.of_xml)
          (Xml.child xml_arg0 "transitGatewayPrefixListReference") in
      make ?transitGatewayPrefixListReference ()
    let of_json json =
      let transitGatewayPrefixListReference =
        field_map json "TransitGatewayPrefixListReference"
          TransitGatewayPrefixListReference.of_json in
      make ?transitGatewayPrefixListReference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a reference (route) to a prefix list in a specified transit gateway route table."]
module ModifyTransitGatewayPrefixListReferenceRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc "The ID of the attachment to which traffic is routed."];
      blackhole: Boolean.t option
        [@ocaml.doc
          "Indicates whether to drop traffic that matches this route."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyTransitGatewayPrefixListReferenceRequest"
    let make ?transitGatewayAttachmentId =
      fun ?blackhole ->
        fun ?dryRun ->
          fun ~transitGatewayRouteTableId ->
            fun ~prefixListId ->
              fun () ->
                {
                  transitGatewayAttachmentId;
                  blackhole;
                  dryRun;
                  transitGatewayRouteTableId;
                  prefixListId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("Blackhole", (Option.map x.blackhole ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let blackhole =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Blackhole") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId ~prefixListId
        ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let blackhole = field_map json "Blackhole" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId ~prefixListId
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a reference (route) to a prefix list in a specified transit gateway route table."]
module ModifyTrafficMirrorSessionResult =
  struct
    type nonrec t =
      {
      trafficMirrorSession: TrafficMirrorSession.t option
        [@ocaml.doc "Information about the Traffic Mirror session."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorSession = fun () -> { trafficMirrorSession }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorSession",
           (Option.map x.trafficMirrorSession
              ~f:TrafficMirrorSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorSession =
        (Option.map ~f:TrafficMirrorSession.of_xml)
          (Xml.child xml_arg0 "trafficMirrorSession") in
      make ?trafficMirrorSession ()
    let of_json json =
      let trafficMirrorSession =
        field_map json "TrafficMirrorSession" TrafficMirrorSession.of_json in
      make ?trafficMirrorSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies a Traffic Mirror session."]
module ModifyTrafficMirrorSessionRequest =
  struct
    type nonrec t =
      {
      trafficMirrorSessionId: TrafficMirrorSessionId.t
        [@ocaml.doc "The ID of the Traffic Mirror session."];
      trafficMirrorTargetId: TrafficMirrorTargetId.t option
        [@ocaml.doc
          "The Traffic Mirror target. The target must be in the same VPC as the source, or have a VPC peering connection with the source."];
      trafficMirrorFilterId: TrafficMirrorFilterId.t option
        [@ocaml.doc "The ID of the Traffic Mirror filter."];
      packetLength: Integer.t option
        [@ocaml.doc
          "The number of bytes in each packet to mirror. These are bytes after the VXLAN header. To mirror a subset, set this to the length (in bytes) to mirror. For example, if you set this value to 100, then the first 100 bytes that meet the filter criteria are copied to the target. Do not specify this parameter when you want to mirror the entire packet."];
      sessionNumber: Integer.t option
        [@ocaml.doc
          "The session number determines the order in which sessions are evaluated when an interface is used by multiple sessions. The first session with a matching filter is the one that mirrors the packets. Valid values are 1-32766."];
      virtualNetworkId: Integer.t option
        [@ocaml.doc "The virtual network ID of the Traffic Mirror session."];
      description: String_.t option
        [@ocaml.doc
          "The description to assign to the Traffic Mirror session."];
      removeFields: TrafficMirrorSessionFieldList.t option
        [@ocaml.doc
          "The properties that you want to remove from the Traffic Mirror session. When you remove a property from a Traffic Mirror session, the property is set to the default."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyTrafficMirrorSessionRequest"
    let make ?trafficMirrorTargetId =
      fun ?trafficMirrorFilterId ->
        fun ?packetLength ->
          fun ?sessionNumber ->
            fun ?virtualNetworkId ->
              fun ?description ->
                fun ?removeFields ->
                  fun ?dryRun ->
                    fun ~trafficMirrorSessionId ->
                      fun () ->
                        {
                          trafficMirrorTargetId;
                          trafficMirrorFilterId;
                          packetLength;
                          sessionNumber;
                          virtualNetworkId;
                          description;
                          removeFields;
                          dryRun;
                          trafficMirrorSessionId
                        }
    let to_value x =
      structure_to_value
        [("TrafficMirrorSessionId",
           (Some (TrafficMirrorSessionId.to_value x.trafficMirrorSessionId)));
        ("TrafficMirrorTargetId",
          (Option.map x.trafficMirrorTargetId
             ~f:TrafficMirrorTargetId.to_value));
        ("TrafficMirrorFilterId",
          (Option.map x.trafficMirrorFilterId
             ~f:TrafficMirrorFilterId.to_value));
        ("PacketLength", (Option.map x.packetLength ~f:Integer.to_value));
        ("SessionNumber", (Option.map x.sessionNumber ~f:Integer.to_value));
        ("VirtualNetworkId",
          (Option.map x.virtualNetworkId ~f:Integer.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("RemoveField",
          (Option.map x.removeFields
             ~f:TrafficMirrorSessionFieldList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let removeFields =
        (Option.map ~f:TrafficMirrorSessionFieldList.of_xml)
          (Xml.child xml_arg0 "RemoveField") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let virtualNetworkId =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "VirtualNetworkId") in
      let sessionNumber =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "SessionNumber") in
      let packetLength =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PacketLength") in
      let trafficMirrorFilterId =
        (Option.map ~f:TrafficMirrorFilterId.of_xml)
          (Xml.child xml_arg0 "TrafficMirrorFilterId") in
      let trafficMirrorTargetId =
        (Option.map ~f:TrafficMirrorTargetId.of_xml)
          (Xml.child xml_arg0 "TrafficMirrorTargetId") in
      let trafficMirrorSessionId =
        TrafficMirrorSessionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorSessionId") in
      make ?dryRun ?removeFields ?description ?virtualNetworkId
        ?sessionNumber ?packetLength ?trafficMirrorFilterId
        ?trafficMirrorTargetId ~trafficMirrorSessionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let removeFields =
        field_map json "RemoveFields" TrafficMirrorSessionFieldList.of_json in
      let description = field_map json "Description" String_.of_json in
      let virtualNetworkId =
        field_map json "VirtualNetworkId" Integer.of_json in
      let sessionNumber = field_map json "SessionNumber" Integer.of_json in
      let packetLength = field_map json "PacketLength" Integer.of_json in
      let trafficMirrorFilterId =
        field_map json "TrafficMirrorFilterId" TrafficMirrorFilterId.of_json in
      let trafficMirrorTargetId =
        field_map json "TrafficMirrorTargetId" TrafficMirrorTargetId.of_json in
      let trafficMirrorSessionId =
        field_map_exn json "TrafficMirrorSessionId"
          TrafficMirrorSessionId.of_json in
      make ?dryRun ?removeFields ?description ?virtualNetworkId
        ?sessionNumber ?packetLength ?trafficMirrorFilterId
        ?trafficMirrorTargetId ~trafficMirrorSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies a Traffic Mirror session."]
module ModifyTrafficMirrorFilterRuleResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRule: TrafficMirrorFilterRule.t option
        [@ocaml.doc "Modifies a Traffic Mirror rule."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterRule = fun () -> { trafficMirrorFilterRule }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterRule",
           (Option.map x.trafficMirrorFilterRule
              ~f:TrafficMirrorFilterRule.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorFilterRule =
        (Option.map ~f:TrafficMirrorFilterRule.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterRule") in
      make ?trafficMirrorFilterRule ()
    let of_json json =
      let trafficMirrorFilterRule =
        field_map json "TrafficMirrorFilterRule"
          TrafficMirrorFilterRule.of_json in
      make ?trafficMirrorFilterRule ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified Traffic Mirror rule. DestinationCidrBlock and SourceCidrBlock must both be an IPv4 range or an IPv6 range."]
module ModifyTrafficMirrorFilterRuleRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRuleId: TrafficMirrorFilterRuleId.t
        [@ocaml.doc "The ID of the Traffic Mirror rule."];
      trafficDirection: TrafficDirection.t option
        [@ocaml.doc "The type of traffic to assign to the rule."];
      ruleNumber: Integer.t option
        [@ocaml.doc
          "The number of the Traffic Mirror rule. This number must be unique for each Traffic Mirror rule in a given direction. The rules are processed in ascending order by rule number."];
      ruleAction: TrafficMirrorRuleAction.t option
        [@ocaml.doc "The action to assign to the rule."];
      destinationPortRange: TrafficMirrorPortRangeRequest.t option
        [@ocaml.doc
          "The destination ports that are associated with the Traffic Mirror rule."];
      sourcePortRange: TrafficMirrorPortRangeRequest.t option
        [@ocaml.doc "The port range to assign to the Traffic Mirror rule."];
      protocol: Integer.t option
        [@ocaml.doc
          "The protocol, for example TCP, to assign to the Traffic Mirror rule."];
      destinationCidrBlock: String_.t option
        [@ocaml.doc
          "The destination CIDR block to assign to the Traffic Mirror rule."];
      sourceCidrBlock: String_.t option
        [@ocaml.doc
          "The source CIDR block to assign to the Traffic Mirror rule."];
      description: String_.t option
        [@ocaml.doc "The description to assign to the Traffic Mirror rule."];
      removeFields: TrafficMirrorFilterRuleFieldList.t option
        [@ocaml.doc
          "The properties that you want to remove from the Traffic Mirror filter rule. When you remove a property from a Traffic Mirror filter rule, the property is set to the default."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyTrafficMirrorFilterRuleRequest"
    let make ?trafficDirection =
      fun ?ruleNumber ->
        fun ?ruleAction ->
          fun ?destinationPortRange ->
            fun ?sourcePortRange ->
              fun ?protocol ->
                fun ?destinationCidrBlock ->
                  fun ?sourceCidrBlock ->
                    fun ?description ->
                      fun ?removeFields ->
                        fun ?dryRun ->
                          fun ~trafficMirrorFilterRuleId ->
                            fun () ->
                              {
                                trafficDirection;
                                ruleNumber;
                                ruleAction;
                                destinationPortRange;
                                sourcePortRange;
                                protocol;
                                destinationCidrBlock;
                                sourceCidrBlock;
                                description;
                                removeFields;
                                dryRun;
                                trafficMirrorFilterRuleId
                              }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterRuleId",
           (Some
              (TrafficMirrorFilterRuleId.to_value x.trafficMirrorFilterRuleId)));
        ("TrafficDirection",
          (Option.map x.trafficDirection ~f:TrafficDirection.to_value));
        ("RuleNumber", (Option.map x.ruleNumber ~f:Integer.to_value));
        ("RuleAction",
          (Option.map x.ruleAction ~f:TrafficMirrorRuleAction.to_value));
        ("DestinationPortRange",
          (Option.map x.destinationPortRange
             ~f:TrafficMirrorPortRangeRequest.to_value));
        ("SourcePortRange",
          (Option.map x.sourcePortRange
             ~f:TrafficMirrorPortRangeRequest.to_value));
        ("Protocol", (Option.map x.protocol ~f:Integer.to_value));
        ("DestinationCidrBlock",
          (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("SourceCidrBlock",
          (Option.map x.sourceCidrBlock ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("RemoveField",
          (Option.map x.removeFields
             ~f:TrafficMirrorFilterRuleFieldList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let removeFields =
        (Option.map ~f:TrafficMirrorFilterRuleFieldList.of_xml)
          (Xml.child xml_arg0 "RemoveField") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceCidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SourceCidrBlock") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationCidrBlock") in
      let protocol =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Protocol") in
      let sourcePortRange =
        (Option.map ~f:TrafficMirrorPortRangeRequest.of_xml)
          (Xml.child xml_arg0 "SourcePortRange") in
      let destinationPortRange =
        (Option.map ~f:TrafficMirrorPortRangeRequest.of_xml)
          (Xml.child xml_arg0 "DestinationPortRange") in
      let ruleAction =
        (Option.map ~f:TrafficMirrorRuleAction.of_xml)
          (Xml.child xml_arg0 "RuleAction") in
      let ruleNumber =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "RuleNumber") in
      let trafficDirection =
        (Option.map ~f:TrafficDirection.of_xml)
          (Xml.child xml_arg0 "TrafficDirection") in
      let trafficMirrorFilterRuleId =
        TrafficMirrorFilterRuleId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TrafficMirrorFilterRuleId") in
      make ?dryRun ?removeFields ?description ?sourceCidrBlock
        ?destinationCidrBlock ?protocol ?sourcePortRange
        ?destinationPortRange ?ruleAction ?ruleNumber ?trafficDirection
        ~trafficMirrorFilterRuleId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let removeFields =
        field_map json "RemoveFields"
          TrafficMirrorFilterRuleFieldList.of_json in
      let description = field_map json "Description" String_.of_json in
      let sourceCidrBlock = field_map json "SourceCidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map json "DestinationCidrBlock" String_.of_json in
      let protocol = field_map json "Protocol" Integer.of_json in
      let sourcePortRange =
        field_map json "SourcePortRange"
          TrafficMirrorPortRangeRequest.of_json in
      let destinationPortRange =
        field_map json "DestinationPortRange"
          TrafficMirrorPortRangeRequest.of_json in
      let ruleAction =
        field_map json "RuleAction" TrafficMirrorRuleAction.of_json in
      let ruleNumber = field_map json "RuleNumber" Integer.of_json in
      let trafficDirection =
        field_map json "TrafficDirection" TrafficDirection.of_json in
      let trafficMirrorFilterRuleId =
        field_map_exn json "TrafficMirrorFilterRuleId"
          TrafficMirrorFilterRuleId.of_json in
      make ?dryRun ?removeFields ?description ?sourceCidrBlock
        ?destinationCidrBlock ?protocol ?sourcePortRange
        ?destinationPortRange ?ruleAction ?ruleNumber ?trafficDirection
        ~trafficMirrorFilterRuleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified Traffic Mirror rule. DestinationCidrBlock and SourceCidrBlock must both be an IPv4 range or an IPv6 range."]
module ModifyTrafficMirrorFilterNetworkServicesResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilter: TrafficMirrorFilter.t option
        [@ocaml.doc
          "The Traffic Mirror filter that the network service is associated with."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilter = fun () -> { trafficMirrorFilter }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilter",
           (Option.map x.trafficMirrorFilter ~f:TrafficMirrorFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorFilter =
        (Option.map ~f:TrafficMirrorFilter.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilter") in
      make ?trafficMirrorFilter ()
    let of_json json =
      let trafficMirrorFilter =
        field_map json "TrafficMirrorFilter" TrafficMirrorFilter.of_json in
      make ?trafficMirrorFilter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows or restricts mirroring network services. By default, Amazon DNS network services are not eligible for Traffic Mirror. Use AddNetworkServices to add network services to a Traffic Mirror filter. When a network service is added to the Traffic Mirror filter, all traffic related to that network service will be mirrored. When you no longer want to mirror network services, use RemoveNetworkServices to remove the network services from the Traffic Mirror filter."]
module ModifyTrafficMirrorFilterNetworkServicesRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterId: TrafficMirrorFilterId.t
        [@ocaml.doc "The ID of the Traffic Mirror filter."];
      addNetworkServices: TrafficMirrorNetworkServiceList.t option
        [@ocaml.doc
          "The network service, for example Amazon DNS, that you want to mirror."];
      removeNetworkServices: TrafficMirrorNetworkServiceList.t option
        [@ocaml.doc
          "The network service, for example Amazon DNS, that you no longer want to mirror."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyTrafficMirrorFilterNetworkServicesRequest"
    let make ?addNetworkServices =
      fun ?removeNetworkServices ->
        fun ?dryRun ->
          fun ~trafficMirrorFilterId ->
            fun () ->
              {
                addNetworkServices;
                removeNetworkServices;
                dryRun;
                trafficMirrorFilterId
              }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterId",
           (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("AddNetworkService",
          (Option.map x.addNetworkServices
             ~f:TrafficMirrorNetworkServiceList.to_value));
        ("RemoveNetworkService",
          (Option.map x.removeNetworkServices
             ~f:TrafficMirrorNetworkServiceList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let removeNetworkServices =
        (Option.map ~f:TrafficMirrorNetworkServiceList.of_xml)
          (Xml.child xml_arg0 "RemoveNetworkService") in
      let addNetworkServices =
        (Option.map ~f:TrafficMirrorNetworkServiceList.of_xml)
          (Xml.child xml_arg0 "AddNetworkService") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      make ?dryRun ?removeNetworkServices ?addNetworkServices
        ~trafficMirrorFilterId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let removeNetworkServices =
        field_map json "RemoveNetworkServices"
          TrafficMirrorNetworkServiceList.of_json in
      let addNetworkServices =
        field_map json "AddNetworkServices"
          TrafficMirrorNetworkServiceList.of_json in
      let trafficMirrorFilterId =
        field_map_exn json "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      make ?dryRun ?removeNetworkServices ?addNetworkServices
        ~trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows or restricts mirroring network services. By default, Amazon DNS network services are not eligible for Traffic Mirror. Use AddNetworkServices to add network services to a Traffic Mirror filter. When a network service is added to the Traffic Mirror filter, all traffic related to that network service will be mirrored. When you no longer want to mirror network services, use RemoveNetworkServices to remove the network services from the Traffic Mirror filter."]
module ModifySubnetAttributeRequest =
  struct
    type nonrec t =
      {
      assignIpv6AddressOnCreation: AttributeBooleanValue.t option
        [@ocaml.doc
          "Specify true to indicate that network interfaces created in the specified subnet should be assigned an IPv6 address. This includes a network interface that's created when launching an instance into the subnet (the instance therefore receives an IPv6 address). If you enable the IPv6 addressing feature for your subnet, your network interface or instance only receives an IPv6 address if it's created using version 2016-11-15 or later of the Amazon EC2 API."];
      mapPublicIpOnLaunch: AttributeBooleanValue.t option
        [@ocaml.doc
          "Specify true to indicate that network interfaces attached to instances created in the specified subnet should be assigned a public IPv4 address."];
      subnetId: SubnetId.t [@ocaml.doc "The ID of the subnet."];
      mapCustomerOwnedIpOnLaunch: AttributeBooleanValue.t option
        [@ocaml.doc
          "Specify true to indicate that network interfaces attached to instances created in the specified subnet should be assigned a customer-owned IPv4 address. When this value is true, you must specify the customer-owned IP pool using CustomerOwnedIpv4Pool."];
      customerOwnedIpv4Pool: CoipPoolId.t option
        [@ocaml.doc
          "The customer-owned IPv4 address pool associated with the subnet. You must set this value when you specify true for MapCustomerOwnedIpOnLaunch."];
      enableDns64: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether DNS queries made to the Amazon-provided DNS Resolver in this subnet should return synthetic IPv6 addresses for IPv4-only destinations."];
      privateDnsHostnameTypeOnLaunch: HostnameType.t option
        [@ocaml.doc
          "The type of hostnames to assign to instances in the subnet at launch. For IPv4 only subnets, an instance DNS name must be based on the instance IPv4 address. For IPv6 only subnets, an instance DNS name must be based on the instance ID. For dual-stack subnets, you can specify whether DNS names use the instance IPv4 address or the instance ID."];
      enableResourceNameDnsARecordOnLaunch: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether to respond to DNS queries for instance hostnames with DNS A records."];
      enableResourceNameDnsAAAARecordOnLaunch: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether to respond to DNS queries for instance hostnames with DNS AAAA records."];
      enableLniAtDeviceIndex: Integer.t option
        [@ocaml.doc
          "Indicates the device position for local network interfaces in this subnet. For example, 1 indicates local network interfaces in this subnet are the secondary network interface (eth1). A local network interface cannot be the primary network interface (eth0)."];
      disableLniAtDeviceIndex: AttributeBooleanValue.t option
        [@ocaml.doc
          "Specify true to indicate that local network interfaces at the current position should be disabled."]}
    let context_ = "ModifySubnetAttributeRequest"
    let make ?assignIpv6AddressOnCreation =
      fun ?mapPublicIpOnLaunch ->
        fun ?mapCustomerOwnedIpOnLaunch ->
          fun ?customerOwnedIpv4Pool ->
            fun ?enableDns64 ->
              fun ?privateDnsHostnameTypeOnLaunch ->
                fun ?enableResourceNameDnsARecordOnLaunch ->
                  fun ?enableResourceNameDnsAAAARecordOnLaunch ->
                    fun ?enableLniAtDeviceIndex ->
                      fun ?disableLniAtDeviceIndex ->
                        fun ~subnetId ->
                          fun () ->
                            {
                              assignIpv6AddressOnCreation;
                              mapPublicIpOnLaunch;
                              mapCustomerOwnedIpOnLaunch;
                              customerOwnedIpv4Pool;
                              enableDns64;
                              privateDnsHostnameTypeOnLaunch;
                              enableResourceNameDnsARecordOnLaunch;
                              enableResourceNameDnsAAAARecordOnLaunch;
                              enableLniAtDeviceIndex;
                              disableLniAtDeviceIndex;
                              subnetId
                            }
    let to_value x =
      structure_to_value
        [("AssignIpv6AddressOnCreation",
           (Option.map x.assignIpv6AddressOnCreation
              ~f:AttributeBooleanValue.to_value));
        ("MapPublicIpOnLaunch",
          (Option.map x.mapPublicIpOnLaunch ~f:AttributeBooleanValue.to_value));
        ("subnetId", (Some (SubnetId.to_value x.subnetId)));
        ("MapCustomerOwnedIpOnLaunch",
          (Option.map x.mapCustomerOwnedIpOnLaunch
             ~f:AttributeBooleanValue.to_value));
        ("CustomerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool ~f:CoipPoolId.to_value));
        ("EnableDns64",
          (Option.map x.enableDns64 ~f:AttributeBooleanValue.to_value));
        ("PrivateDnsHostnameTypeOnLaunch",
          (Option.map x.privateDnsHostnameTypeOnLaunch
             ~f:HostnameType.to_value));
        ("EnableResourceNameDnsARecordOnLaunch",
          (Option.map x.enableResourceNameDnsARecordOnLaunch
             ~f:AttributeBooleanValue.to_value));
        ("EnableResourceNameDnsAAAARecordOnLaunch",
          (Option.map x.enableResourceNameDnsAAAARecordOnLaunch
             ~f:AttributeBooleanValue.to_value));
        ("EnableLniAtDeviceIndex",
          (Option.map x.enableLniAtDeviceIndex ~f:Integer.to_value));
        ("DisableLniAtDeviceIndex",
          (Option.map x.disableLniAtDeviceIndex
             ~f:AttributeBooleanValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let disableLniAtDeviceIndex =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "DisableLniAtDeviceIndex") in
      let enableLniAtDeviceIndex =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "EnableLniAtDeviceIndex") in
      let enableResourceNameDnsAAAARecordOnLaunch =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "EnableResourceNameDnsAAAARecordOnLaunch") in
      let enableResourceNameDnsARecordOnLaunch =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "EnableResourceNameDnsARecordOnLaunch") in
      let privateDnsHostnameTypeOnLaunch =
        (Option.map ~f:HostnameType.of_xml)
          (Xml.child xml_arg0 "PrivateDnsHostnameTypeOnLaunch") in
      let enableDns64 =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "EnableDns64") in
      let customerOwnedIpv4Pool =
        (Option.map ~f:CoipPoolId.of_xml)
          (Xml.child xml_arg0 "CustomerOwnedIpv4Pool") in
      let mapCustomerOwnedIpOnLaunch =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "MapCustomerOwnedIpOnLaunch") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subnetId") in
      let mapPublicIpOnLaunch =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "MapPublicIpOnLaunch") in
      let assignIpv6AddressOnCreation =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "AssignIpv6AddressOnCreation") in
      make ?disableLniAtDeviceIndex ?enableLniAtDeviceIndex
        ?enableResourceNameDnsAAAARecordOnLaunch
        ?enableResourceNameDnsARecordOnLaunch ?privateDnsHostnameTypeOnLaunch
        ?enableDns64 ?customerOwnedIpv4Pool ?mapCustomerOwnedIpOnLaunch
        ~subnetId ?mapPublicIpOnLaunch ?assignIpv6AddressOnCreation ()
    let of_json json =
      let disableLniAtDeviceIndex =
        field_map json "DisableLniAtDeviceIndex"
          AttributeBooleanValue.of_json in
      let enableLniAtDeviceIndex =
        field_map json "EnableLniAtDeviceIndex" Integer.of_json in
      let enableResourceNameDnsAAAARecordOnLaunch =
        field_map json "EnableResourceNameDnsAAAARecordOnLaunch"
          AttributeBooleanValue.of_json in
      let enableResourceNameDnsARecordOnLaunch =
        field_map json "EnableResourceNameDnsARecordOnLaunch"
          AttributeBooleanValue.of_json in
      let privateDnsHostnameTypeOnLaunch =
        field_map json "PrivateDnsHostnameTypeOnLaunch" HostnameType.of_json in
      let enableDns64 =
        field_map json "EnableDns64" AttributeBooleanValue.of_json in
      let customerOwnedIpv4Pool =
        field_map json "CustomerOwnedIpv4Pool" CoipPoolId.of_json in
      let mapCustomerOwnedIpOnLaunch =
        field_map json "MapCustomerOwnedIpOnLaunch"
          AttributeBooleanValue.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let mapPublicIpOnLaunch =
        field_map json "MapPublicIpOnLaunch" AttributeBooleanValue.of_json in
      let assignIpv6AddressOnCreation =
        field_map json "AssignIpv6AddressOnCreation"
          AttributeBooleanValue.of_json in
      make ?disableLniAtDeviceIndex ?enableLniAtDeviceIndex
        ?enableResourceNameDnsAAAARecordOnLaunch
        ?enableResourceNameDnsARecordOnLaunch ?privateDnsHostnameTypeOnLaunch
        ?enableDns64 ?customerOwnedIpv4Pool ?mapCustomerOwnedIpOnLaunch
        ~subnetId ?mapPublicIpOnLaunch ?assignIpv6AddressOnCreation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a subnet attribute. You can only modify one attribute at a time. Use this action to modify subnets on Amazon Web Services Outposts. To modify a subnet on an Outpost rack, set both MapCustomerOwnedIpOnLaunch and CustomerOwnedIpv4Pool. These two parameters act as a single attribute. To modify a subnet on an Outpost server, set either EnableLniAtDeviceIndex or DisableLniAtDeviceIndex. For more information about Amazon Web Services Outposts, see the following: Outpost servers Outpost racks"]
module ModifySpotFleetRequestResponse =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "If the request succeeds, the response returns true. If the request fails, no response is returned, and instead an error message is returned."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of ModifySpotFleetRequest."]
module ModifySpotFleetRequestRequest =
  struct
    type nonrec t =
      {
      excessCapacityTerminationPolicy:
        ExcessCapacityTerminationPolicy.t option
        [@ocaml.doc
          "Indicates whether running Spot Instances should be terminated if the target capacity of the Spot Fleet request is decreased below the current size of the Spot Fleet."];
      launchTemplateConfigs: LaunchTemplateConfigList.t option
        [@ocaml.doc
          "The launch template and overrides. You can only use this parameter if you specified a launch template (LaunchTemplateConfigs) in your Spot Fleet request. If you specified LaunchSpecifications in your Spot Fleet request, then omit this parameter."];
      spotFleetRequestId: SpotFleetRequestId.t
        [@ocaml.doc "The ID of the Spot Fleet request."];
      targetCapacity: Integer.t option [@ocaml.doc "The size of the fleet."];
      onDemandTargetCapacity: Integer.t option
        [@ocaml.doc "The number of On-Demand Instances in the fleet."];
      context: String_.t option [@ocaml.doc "Reserved."]}
    let context_ = "ModifySpotFleetRequestRequest"
    let make ?excessCapacityTerminationPolicy =
      fun ?launchTemplateConfigs ->
        fun ?targetCapacity ->
          fun ?onDemandTargetCapacity ->
            fun ?context ->
              fun ~spotFleetRequestId ->
                fun () ->
                  {
                    excessCapacityTerminationPolicy;
                    launchTemplateConfigs;
                    targetCapacity;
                    onDemandTargetCapacity;
                    context;
                    spotFleetRequestId
                  }
    let to_value x =
      structure_to_value
        [("excessCapacityTerminationPolicy",
           (Option.map x.excessCapacityTerminationPolicy
              ~f:ExcessCapacityTerminationPolicy.to_value));
        ("LaunchTemplateConfig",
          (Option.map x.launchTemplateConfigs
             ~f:LaunchTemplateConfigList.to_value));
        ("spotFleetRequestId",
          (Some (SpotFleetRequestId.to_value x.spotFleetRequestId)));
        ("targetCapacity", (Option.map x.targetCapacity ~f:Integer.to_value));
        ("OnDemandTargetCapacity",
          (Option.map x.onDemandTargetCapacity ~f:Integer.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let onDemandTargetCapacity =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "OnDemandTargetCapacity") in
      let targetCapacity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "targetCapacity") in
      let spotFleetRequestId =
        SpotFleetRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "spotFleetRequestId") in
      let launchTemplateConfigs =
        (Option.map ~f:LaunchTemplateConfigList.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateConfig") in
      let excessCapacityTerminationPolicy =
        (Option.map ~f:ExcessCapacityTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "excessCapacityTerminationPolicy") in
      make ?context ?onDemandTargetCapacity ?targetCapacity
        ~spotFleetRequestId ?launchTemplateConfigs
        ?excessCapacityTerminationPolicy ()
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let onDemandTargetCapacity =
        field_map json "OnDemandTargetCapacity" Integer.of_json in
      let targetCapacity = field_map json "TargetCapacity" Integer.of_json in
      let spotFleetRequestId =
        field_map_exn json "SpotFleetRequestId" SpotFleetRequestId.of_json in
      let launchTemplateConfigs =
        field_map json "LaunchTemplateConfigs"
          LaunchTemplateConfigList.of_json in
      let excessCapacityTerminationPolicy =
        field_map json "ExcessCapacityTerminationPolicy"
          ExcessCapacityTerminationPolicy.of_json in
      make ?context ?onDemandTargetCapacity ?targetCapacity
        ~spotFleetRequestId ?launchTemplateConfigs
        ?excessCapacityTerminationPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for ModifySpotFleetRequest."]
module ModifySnapshotTierResult =
  struct
    type nonrec t =
      {
      snapshotId: String_.t option [@ocaml.doc "The ID of the snapshot."];
      tieringStartTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The date and time when the archive process was started."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshotId =
      fun ?tieringStartTime -> fun () -> { snapshotId; tieringStartTime }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotId", (Option.map x.snapshotId ~f:String_.to_value));
        ("tieringStartTime",
          (Option.map x.tieringStartTime ~f:MillisecondDateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tieringStartTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "tieringStartTime") in
      let snapshotId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "snapshotId") in
      make ?tieringStartTime ?snapshotId ()
    let of_json json =
      let tieringStartTime =
        field_map json "TieringStartTime" MillisecondDateTime.of_json in
      let snapshotId = field_map json "SnapshotId" String_.of_json in
      make ?tieringStartTime ?snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Archives an Amazon EBS snapshot. When you archive a snapshot, it is converted to a full snapshot that includes all of the blocks of data that were written to the volume at the time the snapshot was created, and moved from the standard tier to the archive tier. For more information, see Archive Amazon EBS snapshots in the Amazon Elastic Compute Cloud User Guide."]
module ModifySnapshotTierRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t [@ocaml.doc "The ID of the snapshot."];
      storageTier: TargetStorageTier.t option
        [@ocaml.doc
          "The name of the storage tier. You must specify archive."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifySnapshotTierRequest"
    let make ?storageTier =
      fun ?dryRun ->
        fun ~snapshotId -> fun () -> { storageTier; dryRun; snapshotId }
    let to_value x =
      structure_to_value
        [("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("StorageTier",
          (Option.map x.storageTier ~f:TargetStorageTier.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let storageTier =
        (Option.map ~f:TargetStorageTier.of_xml)
          (Xml.child xml_arg0 "StorageTier") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      make ?dryRun ?storageTier ~snapshotId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let storageTier =
        field_map json "StorageTier" TargetStorageTier.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ?dryRun ?storageTier ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Archives an Amazon EBS snapshot. When you archive a snapshot, it is converted to a full snapshot that includes all of the blocks of data that were written to the volume at the time the snapshot was created, and moved from the standard tier to the archive tier. For more information, see Archive Amazon EBS snapshots in the Amazon Elastic Compute Cloud User Guide."]
module ModifySnapshotAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: SnapshotAttributeName.t option
        [@ocaml.doc
          "The snapshot attribute to modify. Only volume creation permissions can be modified."];
      createVolumePermission: CreateVolumePermissionModifications.t option
        [@ocaml.doc
          "A JSON representation of the snapshot attribute modification."];
      groupNames: GroupNameStringList.t option
        [@ocaml.doc "The group to modify for the snapshot."];
      operationType: OperationType.t option
        [@ocaml.doc "The type of operation to perform to the attribute."];
      snapshotId: SnapshotId.t [@ocaml.doc "The ID of the snapshot."];
      userIds: UserIdStringList.t option
        [@ocaml.doc "The account ID to modify for the snapshot."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifySnapshotAttributeRequest"
    let make ?attribute =
      fun ?createVolumePermission ->
        fun ?groupNames ->
          fun ?operationType ->
            fun ?userIds ->
              fun ?dryRun ->
                fun ~snapshotId ->
                  fun () ->
                    {
                      attribute;
                      createVolumePermission;
                      groupNames;
                      operationType;
                      userIds;
                      dryRun;
                      snapshotId
                    }
    let to_value x =
      structure_to_value
        [("Attribute",
           (Option.map x.attribute ~f:SnapshotAttributeName.to_value));
        ("CreateVolumePermission",
          (Option.map x.createVolumePermission
             ~f:CreateVolumePermissionModifications.to_value));
        ("UserGroup",
          (Option.map x.groupNames ~f:GroupNameStringList.to_value));
        ("OperationType",
          (Option.map x.operationType ~f:OperationType.to_value));
        ("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("UserId", (Option.map x.userIds ~f:UserIdStringList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let userIds =
        (Option.map ~f:UserIdStringList.of_xml) (Xml.child xml_arg0 "UserId") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      let operationType =
        (Option.map ~f:OperationType.of_xml)
          (Xml.child xml_arg0 "OperationType") in
      let groupNames =
        (Option.map ~f:GroupNameStringList.of_xml)
          (Xml.child xml_arg0 "UserGroup") in
      let createVolumePermission =
        (Option.map ~f:CreateVolumePermissionModifications.of_xml)
          (Xml.child xml_arg0 "CreateVolumePermission") in
      let attribute =
        (Option.map ~f:SnapshotAttributeName.of_xml)
          (Xml.child xml_arg0 "Attribute") in
      make ?dryRun ?userIds ~snapshotId ?operationType ?groupNames
        ?createVolumePermission ?attribute ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let userIds = field_map json "UserIds" UserIdStringList.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      let operationType =
        field_map json "OperationType" OperationType.of_json in
      let groupNames =
        field_map json "GroupNames" GroupNameStringList.of_json in
      let createVolumePermission =
        field_map json "CreateVolumePermission"
          CreateVolumePermissionModifications.of_json in
      let attribute =
        field_map json "Attribute" SnapshotAttributeName.of_json in
      make ?dryRun ?userIds ~snapshotId ?operationType ?groupNames
        ?createVolumePermission ?attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds or removes permission settings for the specified snapshot. You may add or remove specified Amazon Web Services account IDs from a snapshot's list of create volume permissions, but you cannot do both in a single operation. If you need to both add and remove account IDs for a snapshot, you must use multiple operations. You can make up to 500 modifications to a snapshot in a single operation. Encrypted snapshots and snapshots with Amazon Web Services Marketplace product codes cannot be made public. Snapshots encrypted with your default KMS key cannot be shared with other accounts. For more information about modifying snapshot permissions, see Share a snapshot in the Amazon Elastic Compute Cloud User Guide."]
module ModifySecurityGroupRulesResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the rules of a security group."]
module ModifySecurityGroupRulesRequest =
  struct
    type nonrec t =
      {
      groupId: SecurityGroupId.t [@ocaml.doc "The ID of the security group."];
      securityGroupRules: SecurityGroupRuleUpdateList.t
        [@ocaml.doc
          "Information about the security group properties to update."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifySecurityGroupRulesRequest"
    let make ?dryRun =
      fun ~groupId ->
        fun ~securityGroupRules ->
          fun () -> { dryRun; groupId; securityGroupRules }
    let to_value x =
      structure_to_value
        [("GroupId", (Some (SecurityGroupId.to_value x.groupId)));
        ("SecurityGroupRule",
          (Some (SecurityGroupRuleUpdateList.to_value x.securityGroupRules)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let securityGroupRules =
        SecurityGroupRuleUpdateList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupRule") in
      let groupId =
        SecurityGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupId") in
      make ?dryRun ~securityGroupRules ~groupId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let securityGroupRules =
        field_map_exn json "SecurityGroupRules"
          SecurityGroupRuleUpdateList.of_json in
      let groupId = field_map_exn json "GroupId" SecurityGroupId.of_json in
      make ?dryRun ~securityGroupRules ~groupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the rules of a security group."]
module ModifyReservedInstancesResult =
  struct
    type nonrec t =
      {
      reservedInstancesModificationId: String_.t option
        [@ocaml.doc "The ID for the modification."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesModificationId =
      fun () -> { reservedInstancesModificationId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesModificationId",
           (Option.map x.reservedInstancesModificationId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesModificationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesModificationId") in
      make ?reservedInstancesModificationId ()
    let of_json json =
      let reservedInstancesModificationId =
        field_map json "ReservedInstancesModificationId" String_.of_json in
      make ?reservedInstancesModificationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of ModifyReservedInstances."]
module ModifyReservedInstancesRequest =
  struct
    type nonrec t =
      {
      reservedInstancesIds: ReservedInstancesIdStringList.t
        [@ocaml.doc "The IDs of the Reserved Instances to modify."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token you provide to ensure idempotency of your modification request. For more information, see Ensuring Idempotency."];
      targetConfigurations: ReservedInstancesConfigurationList.t
        [@ocaml.doc
          "The configuration settings for the Reserved Instances to modify."]}
    let context_ = "ModifyReservedInstancesRequest"
    let make ?clientToken =
      fun ~reservedInstancesIds ->
        fun ~targetConfigurations ->
          fun () ->
            { clientToken; reservedInstancesIds; targetConfigurations }
    let to_value x =
      structure_to_value
        [("ReservedInstancesId",
           (Some
              (ReservedInstancesIdStringList.to_value x.reservedInstancesIds)));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("ReservedInstancesConfigurationSetItemType",
          (Some
             (ReservedInstancesConfigurationList.to_value
                x.targetConfigurations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetConfigurations =
        ReservedInstancesConfigurationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ReservedInstancesConfigurationSetItemType") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let reservedInstancesIds =
        ReservedInstancesIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservedInstancesId") in
      make ~targetConfigurations ?clientToken ~reservedInstancesIds ()
    let of_json json =
      let targetConfigurations =
        field_map_exn json "TargetConfigurations"
          ReservedInstancesConfigurationList.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let reservedInstancesIds =
        field_map_exn json "ReservedInstancesIds"
          ReservedInstancesIdStringList.of_json in
      make ~targetConfigurations ?clientToken ~reservedInstancesIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for ModifyReservedInstances."]
module ModifyPrivateDnsNameOptionsResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the options for instance hostnames for the specified instance."]
module ModifyPrivateDnsNameOptionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t option [@ocaml.doc "The ID of the instance."];
      privateDnsHostnameType: HostnameType.t option
        [@ocaml.doc
          "The type of hostname for EC2 instances. For IPv4 only subnets, an instance DNS name must be based on the instance IPv4 address. For IPv6 only subnets, an instance DNS name must be based on the instance ID. For dual-stack subnets, you can specify whether DNS names use the instance IPv4 address or the instance ID."];
      enableResourceNameDnsARecord: Boolean.t option
        [@ocaml.doc
          "Indicates whether to respond to DNS queries for instance hostnames with DNS A records."];
      enableResourceNameDnsAAAARecord: Boolean.t option
        [@ocaml.doc
          "Indicates whether to respond to DNS queries for instance hostnames with DNS AAAA records."]}
    let make ?dryRun =
      fun ?instanceId ->
        fun ?privateDnsHostnameType ->
          fun ?enableResourceNameDnsARecord ->
            fun ?enableResourceNameDnsAAAARecord ->
              fun () ->
                {
                  dryRun;
                  instanceId;
                  privateDnsHostnameType;
                  enableResourceNameDnsARecord;
                  enableResourceNameDnsAAAARecord
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("PrivateDnsHostnameType",
          (Option.map x.privateDnsHostnameType ~f:HostnameType.to_value));
        ("EnableResourceNameDnsARecord",
          (Option.map x.enableResourceNameDnsARecord ~f:Boolean.to_value));
        ("EnableResourceNameDnsAAAARecord",
          (Option.map x.enableResourceNameDnsAAAARecord ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableResourceNameDnsAAAARecord =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableResourceNameDnsAAAARecord") in
      let enableResourceNameDnsARecord =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableResourceNameDnsARecord") in
      let privateDnsHostnameType =
        (Option.map ~f:HostnameType.of_xml)
          (Xml.child xml_arg0 "PrivateDnsHostnameType") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?enableResourceNameDnsAAAARecord ?enableResourceNameDnsARecord
        ?privateDnsHostnameType ?instanceId ?dryRun ()
    let of_json json =
      let enableResourceNameDnsAAAARecord =
        field_map json "EnableResourceNameDnsAAAARecord" Boolean.of_json in
      let enableResourceNameDnsARecord =
        field_map json "EnableResourceNameDnsARecord" Boolean.of_json in
      let privateDnsHostnameType =
        field_map json "PrivateDnsHostnameType" HostnameType.of_json in
      let instanceId = field_map json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?enableResourceNameDnsAAAARecord ?enableResourceNameDnsARecord
        ?privateDnsHostnameType ?instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the options for instance hostnames for the specified instance."]
module ModifyNetworkInterfaceAttributeRequest =
  struct
    type nonrec t =
      {
      attachment: NetworkInterfaceAttachmentChanges.t option
        [@ocaml.doc
          "Information about the interface attachment. If modifying the 'delete on termination' attribute, you must specify the ID of the interface attachment."];
      description: AttributeValue.t option
        [@ocaml.doc "A description for the network interface."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groups: SecurityGroupIdStringList.t option
        [@ocaml.doc
          "Changes the security groups for the network interface. The new set of groups you specify replaces the current set. You must specify at least one group, even if it's just the default security group in the VPC. You must specify the ID of the security group, not the name."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      sourceDestCheck: AttributeBooleanValue.t option
        [@ocaml.doc
          "Enable or disable source/destination checks, which ensure that the instance is either the source or the destination of any traffic that it receives. If the value is true, source/destination checks are enabled; otherwise, they are disabled. The default value is true. You must disable source/destination checks if the instance runs services such as network address translation, routing, or firewalls."]}
    let context_ = "ModifyNetworkInterfaceAttributeRequest"
    let make ?attachment =
      fun ?description ->
        fun ?dryRun ->
          fun ?groups ->
            fun ?sourceDestCheck ->
              fun ~networkInterfaceId ->
                fun () ->
                  {
                    attachment;
                    description;
                    dryRun;
                    groups;
                    sourceDestCheck;
                    networkInterfaceId
                  }
    let to_value x =
      structure_to_value
        [("attachment",
           (Option.map x.attachment
              ~f:NetworkInterfaceAttachmentChanges.to_value));
        ("description",
          (Option.map x.description ~f:AttributeValue.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecurityGroupId",
          (Option.map x.groups ~f:SecurityGroupIdStringList.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("sourceDestCheck",
          (Option.map x.sourceDestCheck ~f:AttributeBooleanValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceDestCheck =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "sourceDestCheck") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let groups =
        (Option.map ~f:SecurityGroupIdStringList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let description =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "description") in
      let attachment =
        (Option.map ~f:NetworkInterfaceAttachmentChanges.of_xml)
          (Xml.child xml_arg0 "attachment") in
      make ?sourceDestCheck ~networkInterfaceId ?groups ?dryRun ?description
        ?attachment ()
    let of_json json =
      let sourceDestCheck =
        field_map json "SourceDestCheck" AttributeBooleanValue.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let groups = field_map json "Groups" SecurityGroupIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" AttributeValue.of_json in
      let attachment =
        field_map json "Attachment" NetworkInterfaceAttachmentChanges.of_json in
      make ?sourceDestCheck ~networkInterfaceId ?groups ?dryRun ?description
        ?attachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for ModifyNetworkInterfaceAttribute."]
module ModifyManagedPrefixListResult =
  struct
    type nonrec t =
      {
      prefixList: ManagedPrefixList.t option
        [@ocaml.doc "Information about the prefix list."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixList = fun () -> { prefixList }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixList",
           (Option.map x.prefixList ~f:ManagedPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixList =
        (Option.map ~f:ManagedPrefixList.of_xml)
          (Xml.child xml_arg0 "prefixList") in
      make ?prefixList ()
    let of_json json =
      let prefixList = field_map json "PrefixList" ManagedPrefixList.of_json in
      make ?prefixList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified managed prefix list. Adding or removing entries in a prefix list creates a new version of the prefix list. Changing the name of the prefix list does not affect the version. If you specify a current version number that does not match the true current version number, the request fails."]
module ModifyManagedPrefixListRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."];
      currentVersion: Long.t option
        [@ocaml.doc "The current version of the prefix list."];
      prefixListName: String_.t option
        [@ocaml.doc "A name for the prefix list."];
      addEntries: AddPrefixListEntries.t option
        [@ocaml.doc "One or more entries to add to the prefix list."];
      removeEntries: RemovePrefixListEntries.t option
        [@ocaml.doc "One or more entries to remove from the prefix list."];
      maxEntries: Integer.t option
        [@ocaml.doc
          "The maximum number of entries for the prefix list. You cannot modify the entries of a prefix list and modify the size of a prefix list at the same time. If any of the resources that reference the prefix list cannot support the new maximum size, the modify operation fails. Check the state message for the IDs of the first ten resources that do not support the new maximum size."]}
    let context_ = "ModifyManagedPrefixListRequest"
    let make ?dryRun =
      fun ?currentVersion ->
        fun ?prefixListName ->
          fun ?addEntries ->
            fun ?removeEntries ->
              fun ?maxEntries ->
                fun ~prefixListId ->
                  fun () ->
                    {
                      dryRun;
                      currentVersion;
                      prefixListName;
                      addEntries;
                      removeEntries;
                      maxEntries;
                      prefixListId
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("CurrentVersion", (Option.map x.currentVersion ~f:Long.to_value));
        ("PrefixListName", (Option.map x.prefixListName ~f:String_.to_value));
        ("AddEntry",
          (Option.map x.addEntries ~f:AddPrefixListEntries.to_value));
        ("RemoveEntry",
          (Option.map x.removeEntries ~f:RemovePrefixListEntries.to_value));
        ("MaxEntries", (Option.map x.maxEntries ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxEntries =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxEntries") in
      let removeEntries =
        (Option.map ~f:RemovePrefixListEntries.of_xml)
          (Xml.child xml_arg0 "RemoveEntry") in
      let addEntries =
        (Option.map ~f:AddPrefixListEntries.of_xml)
          (Xml.child xml_arg0 "AddEntry") in
      let prefixListName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PrefixListName") in
      let currentVersion =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CurrentVersion") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?maxEntries ?removeEntries ?addEntries ?prefixListName
        ?currentVersion ~prefixListId ?dryRun ()
    let of_json json =
      let maxEntries = field_map json "MaxEntries" Integer.of_json in
      let removeEntries =
        field_map json "RemoveEntries" RemovePrefixListEntries.of_json in
      let addEntries =
        field_map json "AddEntries" AddPrefixListEntries.of_json in
      let prefixListName = field_map json "PrefixListName" String_.of_json in
      let currentVersion = field_map json "CurrentVersion" Long.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?maxEntries ?removeEntries ?addEntries ?prefixListName
        ?currentVersion ~prefixListId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified managed prefix list. Adding or removing entries in a prefix list creates a new version of the prefix list. Changing the name of the prefix list does not affect the version. If you specify a current version number that does not match the true current version number, the request fails."]
module ModifyLaunchTemplateResult =
  struct
    type nonrec t =
      {
      launchTemplate: LaunchTemplate.t option
        [@ocaml.doc "Information about the launch template."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplate = fun () -> { launchTemplate }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplate",
           (Option.map x.launchTemplate ~f:LaunchTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplate =
        (Option.map ~f:LaunchTemplate.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      make ?launchTemplate ()
    let of_json json =
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplate.of_json in
      make ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a launch template. You can specify which version of the launch template to set as the default version. When launching an instance, the default version applies when a launch template version is not specified."]
module ModifyLaunchTemplateRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency. Constraint: Maximum 128 ASCII characters."];
      launchTemplateId: LaunchTemplateId.t option
        [@ocaml.doc
          "The ID of the launch template. You must specify either the launch template ID or launch template name in the request."];
      launchTemplateName: LaunchTemplateName.t option
        [@ocaml.doc
          "The name of the launch template. You must specify either the launch template ID or launch template name in the request."];
      defaultVersion: String_.t option
        [@ocaml.doc
          "The version number of the launch template to set as the default version."]}
    let make ?dryRun =
      fun ?clientToken ->
        fun ?launchTemplateId ->
          fun ?launchTemplateName ->
            fun ?defaultVersion ->
              fun () ->
                {
                  dryRun;
                  clientToken;
                  launchTemplateId;
                  launchTemplateName;
                  defaultVersion
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("SetDefaultVersion",
          (Option.map x.defaultVersion ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SetDefaultVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?defaultVersion ?launchTemplateName ?launchTemplateId ?clientToken
        ?dryRun ()
    let of_json json =
      let defaultVersion = field_map json "DefaultVersion" String_.of_json in
      let launchTemplateName =
        field_map json "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json "LaunchTemplateId" LaunchTemplateId.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?defaultVersion ?launchTemplateName ?launchTemplateId ?clientToken
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a launch template. You can specify which version of the launch template to set as the default version. When launching an instance, the default version applies when a launch template version is not specified."]
module ModifyIpamScopeResult =
  struct
    type nonrec t =
      {
      ipamScope: IpamScope.t option
        [@ocaml.doc "The results of the modification."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamScope = fun () -> { ipamScope }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamScope", (Option.map x.ipamScope ~f:IpamScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScope =
        (Option.map ~f:IpamScope.of_xml) (Xml.child xml_arg0 "ipamScope") in
      make ?ipamScope ()
    let of_json json =
      let ipamScope = field_map json "IpamScope" IpamScope.of_json in
      make ?ipamScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modify an IPAM scope."]
module ModifyIpamScopeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamScopeId: IpamScopeId.t
        [@ocaml.doc "The ID of the scope you want to modify."];
      description: String_.t option
        [@ocaml.doc "The description of the scope you want to modify."]}
    let context_ = "ModifyIpamScopeRequest"
    let make ?dryRun =
      fun ?description ->
        fun ~ipamScopeId -> fun () -> { dryRun; description; ipamScopeId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)));
        ("Description", (Option.map x.description ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?description ~ipamScopeId ?dryRun ()
    let of_json json =
      let description = field_map json "Description" String_.of_json in
      let ipamScopeId = field_map_exn json "IpamScopeId" IpamScopeId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?description ~ipamScopeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modify an IPAM scope."]
module ModifyIpamResult =
  struct
    type nonrec t =
      {
      ipam: Ipam.t option [@ocaml.doc "The results of the modification."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipam = fun () -> { ipam }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("ipam", (Option.map x.ipam ~f:Ipam.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipam = (Option.map ~f:Ipam.of_xml) (Xml.child xml_arg0 "ipam") in
      make ?ipam ()
    let of_json json =
      let ipam = field_map json "Ipam" Ipam.of_json in make ?ipam ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modify the configurations of an IPAM."]
module ModifyIpamResourceCidrResult =
  struct
    type nonrec t = {
      ipamResourceCidr: IpamResourceCidr.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamResourceCidr = fun () -> { ipamResourceCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamResourceCidr",
           (Option.map x.ipamResourceCidr ~f:IpamResourceCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamResourceCidr =
        (Option.map ~f:IpamResourceCidr.of_xml)
          (Xml.child xml_arg0 "ipamResourceCidr") in
      make ?ipamResourceCidr ()
    let of_json json =
      let ipamResourceCidr =
        field_map json "IpamResourceCidr" IpamResourceCidr.of_json in
      make ?ipamResourceCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify a resource CIDR. You can use this action to transfer resource CIDRs between scopes and ignore resource CIDRs that you do not want to manage. If set to false, the resource will not be tracked for overlap, it cannot be auto-imported into a pool, and it will be removed from any pool it has an allocation in. For more information, see Move resource CIDRs between scopes and Change the monitoring state of resource CIDRs in the Amazon VPC IPAM User Guide."]
module ModifyIpamResourceCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      resourceId: String_.t
        [@ocaml.doc "The ID of the resource you want to modify."];
      resourceCidr: String_.t
        [@ocaml.doc "The CIDR of the resource you want to modify."];
      resourceRegion: String_.t
        [@ocaml.doc
          "The Amazon Web Services Region of the resource you want to modify."];
      currentIpamScopeId: IpamScopeId.t
        [@ocaml.doc
          "The ID of the current scope that the resource CIDR is in."];
      destinationIpamScopeId: IpamScopeId.t option
        [@ocaml.doc
          "The ID of the scope you want to transfer the resource CIDR to."];
      monitored: Boolean.t
        [@ocaml.doc
          "Determines if the resource is monitored by IPAM. If a resource is monitored, the resource is discovered by IPAM and you can view details about the resource\226\128\153s CIDR."]}
    let context_ = "ModifyIpamResourceCidrRequest"
    let make ?dryRun =
      fun ?destinationIpamScopeId ->
        fun ~resourceId ->
          fun ~resourceCidr ->
            fun ~resourceRegion ->
              fun ~currentIpamScopeId ->
                fun ~monitored ->
                  fun () ->
                    {
                      dryRun;
                      destinationIpamScopeId;
                      resourceId;
                      resourceCidr;
                      resourceRegion;
                      currentIpamScopeId;
                      monitored
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ResourceId", (Some (String_.to_value x.resourceId)));
        ("ResourceCidr", (Some (String_.to_value x.resourceCidr)));
        ("ResourceRegion", (Some (String_.to_value x.resourceRegion)));
        ("CurrentIpamScopeId",
          (Some (IpamScopeId.to_value x.currentIpamScopeId)));
        ("DestinationIpamScopeId",
          (Option.map x.destinationIpamScopeId ~f:IpamScopeId.to_value));
        ("Monitored", (Some (Boolean.to_value x.monitored)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let monitored =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Monitored") in
      let destinationIpamScopeId =
        (Option.map ~f:IpamScopeId.of_xml)
          (Xml.child xml_arg0 "DestinationIpamScopeId") in
      let currentIpamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CurrentIpamScopeId") in
      let resourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceRegion") in
      let resourceCidr =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceCidr") in
      let resourceId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~monitored ?destinationIpamScopeId ~currentIpamScopeId
        ~resourceRegion ~resourceCidr ~resourceId ?dryRun ()
    let of_json json =
      let monitored = field_map_exn json "Monitored" Boolean.of_json in
      let destinationIpamScopeId =
        field_map json "DestinationIpamScopeId" IpamScopeId.of_json in
      let currentIpamScopeId =
        field_map_exn json "CurrentIpamScopeId" IpamScopeId.of_json in
      let resourceRegion =
        field_map_exn json "ResourceRegion" String_.of_json in
      let resourceCidr = field_map_exn json "ResourceCidr" String_.of_json in
      let resourceId = field_map_exn json "ResourceId" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~monitored ?destinationIpamScopeId ~currentIpamScopeId
        ~resourceRegion ~resourceCidr ~resourceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify a resource CIDR. You can use this action to transfer resource CIDRs between scopes and ignore resource CIDRs that you do not want to manage. If set to false, the resource will not be tracked for overlap, it cannot be auto-imported into a pool, and it will be removed from any pool it has an allocation in. For more information, see Move resource CIDRs between scopes and Change the monitoring state of resource CIDRs in the Amazon VPC IPAM User Guide."]
module ModifyIpamRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t [@ocaml.doc "The ID of the IPAM you want to modify."];
      description: String_.t option
        [@ocaml.doc "The description of the IPAM you want to modify."];
      addOperatingRegions: AddIpamOperatingRegionSet.t option
        [@ocaml.doc
          "Choose the operating Regions for the IPAM. Operating Regions are Amazon Web Services Regions where the IPAM is allowed to manage IP address CIDRs. IPAM only discovers and monitors resources in the Amazon Web Services Regions you select as operating Regions. For more information about operating Regions, see Create an IPAM in the Amazon VPC IPAM User Guide."];
      removeOperatingRegions: RemoveIpamOperatingRegionSet.t option
        [@ocaml.doc "The operating Regions to remove."]}
    let context_ = "ModifyIpamRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?addOperatingRegions ->
          fun ?removeOperatingRegions ->
            fun ~ipamId ->
              fun () ->
                {
                  dryRun;
                  description;
                  addOperatingRegions;
                  removeOperatingRegions;
                  ipamId
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("AddOperatingRegion",
          (Option.map x.addOperatingRegions
             ~f:AddIpamOperatingRegionSet.to_value));
        ("RemoveOperatingRegion",
          (Option.map x.removeOperatingRegions
             ~f:RemoveIpamOperatingRegionSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeOperatingRegions =
        (Option.map ~f:RemoveIpamOperatingRegionSet.of_xml)
          (Xml.child xml_arg0 "RemoveOperatingRegion") in
      let addOperatingRegions =
        (Option.map ~f:AddIpamOperatingRegionSet.of_xml)
          (Xml.child xml_arg0 "AddOperatingRegion") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?removeOperatingRegions ?addOperatingRegions ?description ~ipamId
        ?dryRun ()
    let of_json json =
      let removeOperatingRegions =
        field_map json "RemoveOperatingRegions"
          RemoveIpamOperatingRegionSet.of_json in
      let addOperatingRegions =
        field_map json "AddOperatingRegions"
          AddIpamOperatingRegionSet.of_json in
      let description = field_map json "Description" String_.of_json in
      let ipamId = field_map_exn json "IpamId" IpamId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?removeOperatingRegions ?addOperatingRegions ?description ~ipamId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modify the configurations of an IPAM."]
module ModifyIpamPoolResult =
  struct
    type nonrec t =
      {
      ipamPool: IpamPool.t option
        [@ocaml.doc "The results of the modification."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPool = fun () -> { ipamPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPool", (Option.map x.ipamPool ~f:IpamPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPool =
        (Option.map ~f:IpamPool.of_xml) (Xml.child xml_arg0 "ipamPool") in
      make ?ipamPool ()
    let of_json json =
      let ipamPool = field_map json "IpamPool" IpamPool.of_json in
      make ?ipamPool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify the configurations of an IPAM pool. For more information, see Modify a pool in the Amazon VPC IPAM User Guide."]
module ModifyIpamPoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc "The ID of the IPAM pool you want to modify."];
      description: String_.t option
        [@ocaml.doc "The description of the IPAM pool you want to modify."];
      autoImport: Boolean.t option
        [@ocaml.doc
          "If true, IPAM will continuously look for resources within the CIDR range of this pool and automatically import them as allocations into your IPAM. The CIDRs that will be allocated for these resources must not already be allocated to other resources in order for the import to succeed. IPAM will import a CIDR regardless of its compliance with the pool's allocation rules, so a resource might be imported and subsequently marked as noncompliant. If IPAM discovers multiple CIDRs that overlap, IPAM will import the largest CIDR only. If IPAM discovers multiple CIDRs with matching CIDRs, IPAM will randomly import one of them only. A locale must be set on the pool for this feature to work."];
      allocationMinNetmaskLength: IpamNetmaskLength.t option
        [@ocaml.doc
          "The minimum netmask length required for CIDR allocations in this IPAM pool to be compliant. Possible netmask lengths for IPv4 addresses are 0 - 32. Possible netmask lengths for IPv6 addresses are 0 - 128. The minimum netmask length must be less than the maximum netmask length."];
      allocationMaxNetmaskLength: IpamNetmaskLength.t option
        [@ocaml.doc
          "The maximum netmask length possible for CIDR allocations in this IPAM pool to be compliant. Possible netmask lengths for IPv4 addresses are 0 - 32. Possible netmask lengths for IPv6 addresses are 0 - 128.The maximum netmask length must be greater than the minimum netmask length."];
      allocationDefaultNetmaskLength: IpamNetmaskLength.t option
        [@ocaml.doc
          "The default netmask length for allocations added to this pool. If, for example, the CIDR assigned to this pool is 10.0.0.0/8 and you enter 16 here, new allocations will default to 10.0.0.0/16."];
      clearAllocationDefaultNetmaskLength: Boolean.t option
        [@ocaml.doc
          "Clear the default netmask length allocation rule for this pool."];
      addAllocationResourceTags: RequestIpamResourceTagList.t option
        [@ocaml.doc
          "Add tag allocation rules to a pool. For more information about allocation rules, see Create a top-level pool in the Amazon VPC IPAM User Guide."];
      removeAllocationResourceTags: RequestIpamResourceTagList.t option
        [@ocaml.doc "Remove tag allocation rules from a pool."]}
    let context_ = "ModifyIpamPoolRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?autoImport ->
          fun ?allocationMinNetmaskLength ->
            fun ?allocationMaxNetmaskLength ->
              fun ?allocationDefaultNetmaskLength ->
                fun ?clearAllocationDefaultNetmaskLength ->
                  fun ?addAllocationResourceTags ->
                    fun ?removeAllocationResourceTags ->
                      fun ~ipamPoolId ->
                        fun () ->
                          {
                            dryRun;
                            description;
                            autoImport;
                            allocationMinNetmaskLength;
                            allocationMaxNetmaskLength;
                            allocationDefaultNetmaskLength;
                            clearAllocationDefaultNetmaskLength;
                            addAllocationResourceTags;
                            removeAllocationResourceTags;
                            ipamPoolId
                          }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("AutoImport", (Option.map x.autoImport ~f:Boolean.to_value));
        ("AllocationMinNetmaskLength",
          (Option.map x.allocationMinNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationMaxNetmaskLength",
          (Option.map x.allocationMaxNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationDefaultNetmaskLength",
          (Option.map x.allocationDefaultNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("ClearAllocationDefaultNetmaskLength",
          (Option.map x.clearAllocationDefaultNetmaskLength
             ~f:Boolean.to_value));
        ("AddAllocationResourceTag",
          (Option.map x.addAllocationResourceTags
             ~f:RequestIpamResourceTagList.to_value));
        ("RemoveAllocationResourceTag",
          (Option.map x.removeAllocationResourceTags
             ~f:RequestIpamResourceTagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeAllocationResourceTags =
        (Option.map ~f:RequestIpamResourceTagList.of_xml)
          (Xml.child xml_arg0 "RemoveAllocationResourceTag") in
      let addAllocationResourceTags =
        (Option.map ~f:RequestIpamResourceTagList.of_xml)
          (Xml.child xml_arg0 "AddAllocationResourceTag") in
      let clearAllocationDefaultNetmaskLength =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ClearAllocationDefaultNetmaskLength") in
      let allocationDefaultNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationDefaultNetmaskLength") in
      let allocationMaxNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationMaxNetmaskLength") in
      let allocationMinNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationMinNetmaskLength") in
      let autoImport =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoImport") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?removeAllocationResourceTags ?addAllocationResourceTags
        ?clearAllocationDefaultNetmaskLength ?allocationDefaultNetmaskLength
        ?allocationMaxNetmaskLength ?allocationMinNetmaskLength ?autoImport
        ?description ~ipamPoolId ?dryRun ()
    let of_json json =
      let removeAllocationResourceTags =
        field_map json "RemoveAllocationResourceTags"
          RequestIpamResourceTagList.of_json in
      let addAllocationResourceTags =
        field_map json "AddAllocationResourceTags"
          RequestIpamResourceTagList.of_json in
      let clearAllocationDefaultNetmaskLength =
        field_map json "ClearAllocationDefaultNetmaskLength" Boolean.of_json in
      let allocationDefaultNetmaskLength =
        field_map json "AllocationDefaultNetmaskLength"
          IpamNetmaskLength.of_json in
      let allocationMaxNetmaskLength =
        field_map json "AllocationMaxNetmaskLength" IpamNetmaskLength.of_json in
      let allocationMinNetmaskLength =
        field_map json "AllocationMinNetmaskLength" IpamNetmaskLength.of_json in
      let autoImport = field_map json "AutoImport" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?removeAllocationResourceTags ?addAllocationResourceTags
        ?clearAllocationDefaultNetmaskLength ?allocationDefaultNetmaskLength
        ?allocationMaxNetmaskLength ?allocationMinNetmaskLength ?autoImport
        ?description ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify the configurations of an IPAM pool. For more information, see Modify a pool in the Amazon VPC IPAM User Guide."]
module ModifyInstancePlacementResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds, and an error otherwise."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the placement attributes for a specified instance. You can do the following: Modify the affinity between an instance and a Dedicated Host. When affinity is set to host and the instance is not associated with a specific Dedicated Host, the next time the instance is launched, it is automatically associated with the host on which it lands. If the instance is restarted or rebooted, this relationship persists. Change the Dedicated Host with which an instance is associated. Change the instance tenancy of an instance. Move an instance to or from a placement group. At least one attribute for affinity, host ID, tenancy, or placement group name must be specified in the request. Affinity and tenancy can be modified in the same request. To modify the host ID, tenancy, placement group, or partition for an instance, the instance must be in the stopped state."]
module ModifyInstancePlacementRequest =
  struct
    type nonrec t =
      {
      affinity: Affinity.t option
        [@ocaml.doc "The affinity setting for the instance."];
      groupName: PlacementGroupName.t option
        [@ocaml.doc
          "The name of the placement group in which to place the instance. For spread placement groups, the instance must have a tenancy of default. For cluster and partition placement groups, the instance must have a tenancy of default or dedicated. To remove an instance from a placement group, specify an empty string (\"\")."];
      hostId: DedicatedHostId.t option
        [@ocaml.doc
          "The ID of the Dedicated Host with which to associate the instance."];
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the instance that you are modifying."];
      tenancy: HostTenancy.t option
        [@ocaml.doc
          "The tenancy for the instance. For T3 instances, you can't change the tenancy from dedicated to host, or from host to dedicated. Attempting to make one of these unsupported tenancy changes results in the InvalidTenancy error code."];
      partitionNumber: Integer.t option
        [@ocaml.doc
          "The number of the partition in which to place the instance. Valid only if the placement group strategy is set to partition."];
      hostResourceGroupArn: String_.t option
        [@ocaml.doc
          "The ARN of the host resource group in which to place the instance."]}
    let context_ = "ModifyInstancePlacementRequest"
    let make ?affinity =
      fun ?groupName ->
        fun ?hostId ->
          fun ?tenancy ->
            fun ?partitionNumber ->
              fun ?hostResourceGroupArn ->
                fun ~instanceId ->
                  fun () ->
                    {
                      affinity;
                      groupName;
                      hostId;
                      tenancy;
                      partitionNumber;
                      hostResourceGroupArn;
                      instanceId
                    }
    let to_value x =
      structure_to_value
        [("affinity", (Option.map x.affinity ~f:Affinity.to_value));
        ("GroupName",
          (Option.map x.groupName ~f:PlacementGroupName.to_value));
        ("hostId", (Option.map x.hostId ~f:DedicatedHostId.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("tenancy", (Option.map x.tenancy ~f:HostTenancy.to_value));
        ("PartitionNumber",
          (Option.map x.partitionNumber ~f:Integer.to_value));
        ("HostResourceGroupArn",
          (Option.map x.hostResourceGroupArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostResourceGroupArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "HostResourceGroupArn") in
      let partitionNumber =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PartitionNumber") in
      let tenancy =
        (Option.map ~f:HostTenancy.of_xml) (Xml.child xml_arg0 "tenancy") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let hostId =
        (Option.map ~f:DedicatedHostId.of_xml) (Xml.child xml_arg0 "hostId") in
      let groupName =
        (Option.map ~f:PlacementGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let affinity =
        (Option.map ~f:Affinity.of_xml) (Xml.child xml_arg0 "affinity") in
      make ?hostResourceGroupArn ?partitionNumber ?tenancy ~instanceId
        ?hostId ?groupName ?affinity ()
    let of_json json =
      let hostResourceGroupArn =
        field_map json "HostResourceGroupArn" String_.of_json in
      let partitionNumber = field_map json "PartitionNumber" Integer.of_json in
      let tenancy = field_map json "Tenancy" HostTenancy.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let hostId = field_map json "HostId" DedicatedHostId.of_json in
      let groupName = field_map json "GroupName" PlacementGroupName.of_json in
      let affinity = field_map json "Affinity" Affinity.of_json in
      make ?hostResourceGroupArn ?partitionNumber ?tenancy ~instanceId
        ?hostId ?groupName ?affinity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the placement attributes for a specified instance. You can do the following: Modify the affinity between an instance and a Dedicated Host. When affinity is set to host and the instance is not associated with a specific Dedicated Host, the next time the instance is launched, it is automatically associated with the host on which it lands. If the instance is restarted or rebooted, this relationship persists. Change the Dedicated Host with which an instance is associated. Change the instance tenancy of an instance. Move an instance to or from a placement group. At least one attribute for affinity, host ID, tenancy, or placement group name must be specified in the request. Affinity and tenancy can be modified in the same request. To modify the host ID, tenancy, placement group, or partition for an instance, the instance must be in the stopped state."]
module ModifyInstanceMetadataOptionsResult =
  struct
    type nonrec t =
      {
      instanceId: String_.t option [@ocaml.doc "The ID of the instance."];
      instanceMetadataOptions: InstanceMetadataOptionsResponse.t option
        [@ocaml.doc "The metadata options for the instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceId =
      fun ?instanceMetadataOptions ->
        fun () -> { instanceId; instanceMetadataOptions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceId", (Option.map x.instanceId ~f:String_.to_value));
        ("instanceMetadataOptions",
          (Option.map x.instanceMetadataOptions
             ~f:InstanceMetadataOptionsResponse.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceMetadataOptions =
        (Option.map ~f:InstanceMetadataOptionsResponse.of_xml)
          (Xml.child xml_arg0 "instanceMetadataOptions") in
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      make ?instanceMetadataOptions ?instanceId ()
    let of_json json =
      let instanceMetadataOptions =
        field_map json "InstanceMetadataOptions"
          InstanceMetadataOptionsResponse.of_json in
      let instanceId = field_map json "InstanceId" String_.of_json in
      make ?instanceMetadataOptions ?instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify the instance metadata parameters on a running or stopped instance. When you modify the parameters on a stopped instance, they are applied when the instance is started. When you modify the parameters on a running instance, the API responds with a state of \226\128\156pending\226\128\157. After the parameter modifications are successfully applied to the instance, the state of the modifications changes from \226\128\156pending\226\128\157 to \226\128\156applied\226\128\157 in subsequent describe-instances API calls. For more information, see Instance metadata and user data in the Amazon EC2 User Guide."]
module ModifyInstanceMetadataOptionsRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      httpTokens: HttpTokensState.t option
        [@ocaml.doc
          "The state of token usage for your instance metadata requests. If the parameter is not specified in the request, the default state is optional. If the state is optional, you can choose to retrieve instance metadata with or without a signed token header on your request. If you retrieve the IAM role credentials without a token, the version 1.0 role credentials are returned. If you retrieve the IAM role credentials using a valid signed token, the version 2.0 role credentials are returned. If the state is required, you must send a signed token header with any instance metadata retrieval requests. In this state, retrieving the IAM role credential always returns the version 2.0 credentials; the version 1.0 credentials are not available."];
      httpPutResponseHopLimit: Integer.t option
        [@ocaml.doc
          "The desired HTTP PUT response hop limit for instance metadata requests. The larger the number, the further instance metadata requests can travel. If no parameter is specified, the existing state is maintained. Possible values: Integers from 1 to 64"];
      httpEndpoint: InstanceMetadataEndpointState.t option
        [@ocaml.doc
          "Enables or disables the HTTP metadata endpoint on your instances. If this parameter is not specified, the existing state is maintained. If you specify a value of disabled, you cannot access your instance metadata."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      httpProtocolIpv6: InstanceMetadataProtocolState.t option
        [@ocaml.doc
          "Enables or disables the IPv6 endpoint for the instance metadata service. This setting applies only if you have enabled the HTTP metadata endpoint."];
      instanceMetadataTags: InstanceMetadataTagsState.t option
        [@ocaml.doc
          "Set to enabled to allow access to instance tags from the instance metadata. Set to disabled to turn off access to instance tags from the instance metadata. For more information, see Work with instance tags using the instance metadata. Default: disabled"]}
    let context_ = "ModifyInstanceMetadataOptionsRequest"
    let make ?httpTokens =
      fun ?httpPutResponseHopLimit ->
        fun ?httpEndpoint ->
          fun ?dryRun ->
            fun ?httpProtocolIpv6 ->
              fun ?instanceMetadataTags ->
                fun ~instanceId ->
                  fun () ->
                    {
                      httpTokens;
                      httpPutResponseHopLimit;
                      httpEndpoint;
                      dryRun;
                      httpProtocolIpv6;
                      instanceMetadataTags;
                      instanceId
                    }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("HttpTokens", (Option.map x.httpTokens ~f:HttpTokensState.to_value));
        ("HttpPutResponseHopLimit",
          (Option.map x.httpPutResponseHopLimit ~f:Integer.to_value));
        ("HttpEndpoint",
          (Option.map x.httpEndpoint
             ~f:InstanceMetadataEndpointState.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("HttpProtocolIpv6",
          (Option.map x.httpProtocolIpv6
             ~f:InstanceMetadataProtocolState.to_value));
        ("InstanceMetadataTags",
          (Option.map x.instanceMetadataTags
             ~f:InstanceMetadataTagsState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceMetadataTags =
        (Option.map ~f:InstanceMetadataTagsState.of_xml)
          (Xml.child xml_arg0 "InstanceMetadataTags") in
      let httpProtocolIpv6 =
        (Option.map ~f:InstanceMetadataProtocolState.of_xml)
          (Xml.child xml_arg0 "HttpProtocolIpv6") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let httpEndpoint =
        (Option.map ~f:InstanceMetadataEndpointState.of_xml)
          (Xml.child xml_arg0 "HttpEndpoint") in
      let httpPutResponseHopLimit =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "HttpPutResponseHopLimit") in
      let httpTokens =
        (Option.map ~f:HttpTokensState.of_xml)
          (Xml.child xml_arg0 "HttpTokens") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?instanceMetadataTags ?httpProtocolIpv6 ?dryRun ?httpEndpoint
        ?httpPutResponseHopLimit ?httpTokens ~instanceId ()
    let of_json json =
      let instanceMetadataTags =
        field_map json "InstanceMetadataTags"
          InstanceMetadataTagsState.of_json in
      let httpProtocolIpv6 =
        field_map json "HttpProtocolIpv6"
          InstanceMetadataProtocolState.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let httpEndpoint =
        field_map json "HttpEndpoint" InstanceMetadataEndpointState.of_json in
      let httpPutResponseHopLimit =
        field_map json "HttpPutResponseHopLimit" Integer.of_json in
      let httpTokens = field_map json "HttpTokens" HttpTokensState.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?instanceMetadataTags ?httpProtocolIpv6 ?dryRun ?httpEndpoint
        ?httpPutResponseHopLimit ?httpTokens ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify the instance metadata parameters on a running or stopped instance. When you modify the parameters on a stopped instance, they are applied when the instance is started. When you modify the parameters on a running instance, the API responds with a state of \226\128\156pending\226\128\157. After the parameter modifications are successfully applied to the instance, the state of the modifications changes from \226\128\156pending\226\128\157 to \226\128\156applied\226\128\157 in subsequent describe-instances API calls. For more information, see Instance metadata and user data in the Amazon EC2 User Guide."]
module ModifyInstanceMaintenanceOptionsResult =
  struct
    type nonrec t =
      {
      instanceId: String_.t option [@ocaml.doc "The ID of the instance."];
      autoRecovery: InstanceAutoRecoveryState.t option
        [@ocaml.doc
          "Provides information on the current automatic recovery behavior of your instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceId =
      fun ?autoRecovery -> fun () -> { instanceId; autoRecovery }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceId", (Option.map x.instanceId ~f:String_.to_value));
        ("autoRecovery",
          (Option.map x.autoRecovery ~f:InstanceAutoRecoveryState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRecovery =
        (Option.map ~f:InstanceAutoRecoveryState.of_xml)
          (Xml.child xml_arg0 "autoRecovery") in
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      make ?autoRecovery ?instanceId ()
    let of_json json =
      let autoRecovery =
        field_map json "AutoRecovery" InstanceAutoRecoveryState.of_json in
      let instanceId = field_map json "InstanceId" String_.of_json in
      make ?autoRecovery ?instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the recovery behavior of your instance to disable simplified automatic recovery or set the recovery behavior to default. The default configuration will not enable simplified automatic recovery for an unsupported instance type. For more information, see Simplified automatic recovery."]
module ModifyInstanceMaintenanceOptionsRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      autoRecovery: InstanceAutoRecoveryState.t option
        [@ocaml.doc
          "Disables the automatic recovery behavior of your instance or sets it to default."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyInstanceMaintenanceOptionsRequest"
    let make ?autoRecovery =
      fun ?dryRun ->
        fun ~instanceId -> fun () -> { autoRecovery; dryRun; instanceId }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("AutoRecovery",
          (Option.map x.autoRecovery ~f:InstanceAutoRecoveryState.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let autoRecovery =
        (Option.map ~f:InstanceAutoRecoveryState.of_xml)
          (Xml.child xml_arg0 "AutoRecovery") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ?autoRecovery ~instanceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let autoRecovery =
        field_map json "AutoRecovery" InstanceAutoRecoveryState.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?dryRun ?autoRecovery ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the recovery behavior of your instance to disable simplified automatic recovery or set the recovery behavior to default. The default configuration will not enable simplified automatic recovery for an unsupported instance type. For more information, see Simplified automatic recovery."]
module ModifyInstanceEventWindowResult =
  struct
    type nonrec t =
      {
      instanceEventWindow: InstanceEventWindow.t option
        [@ocaml.doc "Information about the event window."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindow = fun () -> { instanceEventWindow }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindow",
           (Option.map x.instanceEventWindow ~f:InstanceEventWindow.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindow =
        (Option.map ~f:InstanceEventWindow.of_xml)
          (Xml.child xml_arg0 "instanceEventWindow") in
      make ?instanceEventWindow ()
    let of_json json =
      let instanceEventWindow =
        field_map json "InstanceEventWindow" InstanceEventWindow.of_json in
      make ?instanceEventWindow ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified event window. You can define either a set of time ranges or a cron expression when modifying the event window, but not both. To modify the targets associated with the event window, use the AssociateInstanceEventWindow and DisassociateInstanceEventWindow API. If Amazon Web Services has already scheduled an event, modifying an event window won't change the time of the scheduled event. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module ModifyInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      name: String_.t option [@ocaml.doc "The name of the event window."];
      instanceEventWindowId: InstanceEventWindowId.t
        [@ocaml.doc "The ID of the event window."];
      timeRanges: InstanceEventWindowTimeRangeRequestSet.t option
        [@ocaml.doc "The time ranges of the event window."];
      cronExpression: InstanceEventWindowCronExpression.t option
        [@ocaml.doc
          "The cron expression of the event window, for example, * 0-4,20-23 * * 1,5. Constraints: Only hour and day of the week values are supported. For day of the week values, you can specify either integers 0 through 6, or alternative single values SUN through SAT. The minute, month, and year must be specified by *. The hour value must be one or a multiple range, for example, 0-4 or 0-4,20-23. Each hour range must be >= 2 hours, for example, 0-2 or 20-23. The event window must be >= 4 hours. The combined total time ranges in the event window must be >= 4 hours. For more information about cron expressions, see cron on the Wikipedia website."]}
    let context_ = "ModifyInstanceEventWindowRequest"
    let make ?dryRun =
      fun ?name ->
        fun ?timeRanges ->
          fun ?cronExpression ->
            fun ~instanceEventWindowId ->
              fun () ->
                {
                  dryRun;
                  name;
                  timeRanges;
                  cronExpression;
                  instanceEventWindowId
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("InstanceEventWindowId",
          (Some (InstanceEventWindowId.to_value x.instanceEventWindowId)));
        ("TimeRange",
          (Option.map x.timeRanges
             ~f:InstanceEventWindowTimeRangeRequestSet.to_value));
        ("CronExpression",
          (Option.map x.cronExpression
             ~f:InstanceEventWindowCronExpression.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cronExpression =
        (Option.map ~f:InstanceEventWindowCronExpression.of_xml)
          (Xml.child xml_arg0 "CronExpression") in
      let timeRanges =
        (Option.map ~f:InstanceEventWindowTimeRangeRequestSet.of_xml)
          (Xml.child xml_arg0 "TimeRange") in
      let instanceEventWindowId =
        InstanceEventWindowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventWindowId") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cronExpression ?timeRanges ~instanceEventWindowId ?name ?dryRun
        ()
    let of_json json =
      let cronExpression =
        field_map json "CronExpression"
          InstanceEventWindowCronExpression.of_json in
      let timeRanges =
        field_map json "TimeRanges"
          InstanceEventWindowTimeRangeRequestSet.of_json in
      let instanceEventWindowId =
        field_map_exn json "InstanceEventWindowId"
          InstanceEventWindowId.of_json in
      let name = field_map json "Name" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?cronExpression ?timeRanges ~instanceEventWindowId ?name ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified event window. You can define either a set of time ranges or a cron expression when modifying the event window, but not both. To modify the targets associated with the event window, use the AssociateInstanceEventWindow and DisassociateInstanceEventWindow API. If Amazon Web Services has already scheduled an event, modifying an event window won't change the time of the scheduled event. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module ModifyInstanceEventStartTimeResult =
  struct
    type nonrec t = {
      event: InstanceStatusEvent.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?event = fun () -> { event }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("event", (Option.map x.event ~f:InstanceStatusEvent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let event =
        (Option.map ~f:InstanceStatusEvent.of_xml)
          (Xml.child xml_arg0 "event") in
      make ?event ()
    let of_json json =
      let event = field_map json "Event" InstanceStatusEvent.of_json in
      make ?event ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the start time for a scheduled Amazon EC2 instance event."]
module ModifyInstanceEventStartTimeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the instance with the scheduled event."];
      instanceEventId: String_.t
        [@ocaml.doc
          "The ID of the event whose date and time you are modifying."];
      notBefore: DateTime.t
        [@ocaml.doc "The new date and time when the event will take place."]}
    let context_ = "ModifyInstanceEventStartTimeRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~instanceEventId ->
          fun ~notBefore ->
            fun () -> { dryRun; instanceId; instanceEventId; notBefore }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("InstanceEventId", (Some (String_.to_value x.instanceEventId)));
        ("NotBefore", (Some (DateTime.to_value x.notBefore)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notBefore =
        DateTime.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotBefore") in
      let instanceEventId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~notBefore ~instanceEventId ~instanceId ?dryRun ()
    let of_json json =
      let notBefore = field_map_exn json "NotBefore" DateTime.of_json in
      let instanceEventId =
        field_map_exn json "InstanceEventId" String_.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~notBefore ~instanceEventId ~instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the start time for a scheduled Amazon EC2 instance event."]
module ModifyInstanceCreditSpecificationResult =
  struct
    type nonrec t =
      {
      successfulInstanceCreditSpecifications:
        SuccessfulInstanceCreditSpecificationSet.t option
        [@ocaml.doc
          "Information about the instances whose credit option for CPU usage was successfully modified."];
      unsuccessfulInstanceCreditSpecifications:
        UnsuccessfulInstanceCreditSpecificationSet.t option
        [@ocaml.doc
          "Information about the instances whose credit option for CPU usage was not modified."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulInstanceCreditSpecifications =
      fun ?unsuccessfulInstanceCreditSpecifications ->
        fun () ->
          {
            successfulInstanceCreditSpecifications;
            unsuccessfulInstanceCreditSpecifications
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulInstanceCreditSpecificationSet",
           (Option.map x.successfulInstanceCreditSpecifications
              ~f:SuccessfulInstanceCreditSpecificationSet.to_value));
        ("unsuccessfulInstanceCreditSpecificationSet",
          (Option.map x.unsuccessfulInstanceCreditSpecifications
             ~f:UnsuccessfulInstanceCreditSpecificationSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfulInstanceCreditSpecifications =
        (Option.map ~f:UnsuccessfulInstanceCreditSpecificationSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfulInstanceCreditSpecificationSet") in
      let successfulInstanceCreditSpecifications =
        (Option.map ~f:SuccessfulInstanceCreditSpecificationSet.of_xml)
          (Xml.child xml_arg0 "successfulInstanceCreditSpecificationSet") in
      make ?unsuccessfulInstanceCreditSpecifications
        ?successfulInstanceCreditSpecifications ()
    let of_json json =
      let unsuccessfulInstanceCreditSpecifications =
        field_map json "UnsuccessfulInstanceCreditSpecifications"
          UnsuccessfulInstanceCreditSpecificationSet.of_json in
      let successfulInstanceCreditSpecifications =
        field_map json "SuccessfulInstanceCreditSpecifications"
          SuccessfulInstanceCreditSpecificationSet.of_json in
      make ?unsuccessfulInstanceCreditSpecifications
        ?successfulInstanceCreditSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the credit option for CPU usage on a running or stopped burstable performance instance. The credit options are standard and unlimited. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module ModifyInstanceCreditSpecificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring Idempotency."];
      instanceCreditSpecifications: InstanceCreditSpecificationListRequest.t
        [@ocaml.doc "Information about the credit option for CPU usage."]}
    let context_ = "ModifyInstanceCreditSpecificationRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ~instanceCreditSpecifications ->
          fun () -> { dryRun; clientToken; instanceCreditSpecifications }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("InstanceCreditSpecification",
          (Some
             (InstanceCreditSpecificationListRequest.to_value
                x.instanceCreditSpecifications)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceCreditSpecifications =
        InstanceCreditSpecificationListRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "InstanceCreditSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceCreditSpecifications ?clientToken ?dryRun ()
    let of_json json =
      let instanceCreditSpecifications =
        field_map_exn json "InstanceCreditSpecifications"
          InstanceCreditSpecificationListRequest.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~instanceCreditSpecifications ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the credit option for CPU usage on a running or stopped burstable performance instance. The credit options are standard and unlimited. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module ModifyInstanceCapacityReservationAttributesResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the Capacity Reservation settings for a stopped instance. Use this action to configure an instance to target a specific Capacity Reservation, run in any open Capacity Reservation with matching attributes, or run On-Demand Instance capacity."]
module ModifyInstanceCapacityReservationAttributesRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the instance to be modified."];
      capacityReservationSpecification: CapacityReservationSpecification.t
        [@ocaml.doc
          "Information about the Capacity Reservation targeting option."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyInstanceCapacityReservationAttributesRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~capacityReservationSpecification ->
          fun () -> { dryRun; instanceId; capacityReservationSpecification }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("CapacityReservationSpecification",
          (Some
             (CapacityReservationSpecification.to_value
                x.capacityReservationSpecification)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let capacityReservationSpecification =
        CapacityReservationSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CapacityReservationSpecification") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~capacityReservationSpecification ~instanceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let capacityReservationSpecification =
        field_map_exn json "CapacityReservationSpecification"
          CapacityReservationSpecification.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?dryRun ~capacityReservationSpecification ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the Capacity Reservation settings for a stopped instance. Use this action to configure an instance to target a specific Capacity Reservation, run in any open Capacity Reservation with matching attributes, or run On-Demand Instance capacity."]
module ModifyInstanceAttributeRequest =
  struct
    type nonrec t =
      {
      sourceDestCheck: AttributeBooleanValue.t option
        [@ocaml.doc
          "Enable or disable source/destination checks, which ensure that the instance is either the source or the destination of any traffic that it receives. If the value is true, source/destination checks are enabled; otherwise, they are disabled. The default value is true. You must disable source/destination checks if the instance runs services such as network address translation, routing, or firewalls."];
      attribute: InstanceAttributeName.t option
        [@ocaml.doc "The name of the attribute."];
      blockDeviceMappings:
        InstanceBlockDeviceMappingSpecificationList.t option
        [@ocaml.doc
          "Modifies the DeleteOnTermination attribute for volumes that are currently attached. The volume must be owned by the caller. If no value is specified for DeleteOnTermination, the default is true and the volume is deleted when the instance is terminated. To add instance store volumes to an Amazon EBS-backed instance, you must add them when you launch the instance. For more information, see Update the block device mapping when launching an instance in the Amazon EC2 User Guide."];
      disableApiTermination: AttributeBooleanValue.t option
        [@ocaml.doc
          "If the value is true, you can't terminate the instance using the Amazon EC2 console, CLI, or API; otherwise, you can. You cannot use this parameter for Spot Instances."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ebsOptimized: AttributeBooleanValue.t option
        [@ocaml.doc
          "Specifies whether the instance is optimized for Amazon EBS I/O. This optimization provides dedicated throughput to Amazon EBS and an optimized configuration stack to provide optimal EBS I/O performance. This optimization isn't available with all instance types. Additional usage charges apply when using an EBS Optimized instance."];
      enaSupport: AttributeBooleanValue.t option
        [@ocaml.doc
          "Set to true to enable enhanced networking with ENA for the instance. This option is supported only for HVM instances. Specifying this option with a PV instance can make it unreachable."];
      groups: GroupIdStringList.t option
        [@ocaml.doc
          "\\[EC2-VPC\\] Replaces the security groups of the instance with the specified security groups. You must specify at least one security group, even if it's just the default security group for the VPC. You must specify the security group ID, not the security group name."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      instanceInitiatedShutdownBehavior: AttributeValue.t option
        [@ocaml.doc
          "Specifies whether an instance stops or terminates when you initiate shutdown from the instance (using the operating system command for system shutdown)."];
      instanceType: AttributeValue.t option
        [@ocaml.doc
          "Changes the instance type to the specified value. For more information, see Instance types in the Amazon EC2 User Guide. If the instance type is not valid, the error returned is InvalidInstanceAttributeValue."];
      kernel: AttributeValue.t option
        [@ocaml.doc
          "Changes the instance's kernel to the specified value. We recommend that you use PV-GRUB instead of kernels and RAM disks. For more information, see PV-GRUB."];
      ramdisk: AttributeValue.t option
        [@ocaml.doc
          "Changes the instance's RAM disk to the specified value. We recommend that you use PV-GRUB instead of kernels and RAM disks. For more information, see PV-GRUB."];
      sriovNetSupport: AttributeValue.t option
        [@ocaml.doc
          "Set to simple to enable enhanced networking with the Intel 82599 Virtual Function interface for the instance. There is no way to disable enhanced networking with the Intel 82599 Virtual Function interface at this time. This option is supported only for HVM instances. Specifying this option with a PV instance can make it unreachable."];
      userData: BlobAttributeValue.t option
        [@ocaml.doc
          "Changes the instance's user data to the specified value. If you are using an Amazon Web Services SDK or command line tool, base64-encoding is performed for you, and you can load the text from a file. Otherwise, you must provide base64-encoded text."];
      value: String_.t option
        [@ocaml.doc
          "A new value for the attribute. Use only with the kernel, ramdisk, userData, disableApiTermination, or instanceInitiatedShutdownBehavior attribute."]}
    let context_ = "ModifyInstanceAttributeRequest"
    let make ?sourceDestCheck =
      fun ?attribute ->
        fun ?blockDeviceMappings ->
          fun ?disableApiTermination ->
            fun ?dryRun ->
              fun ?ebsOptimized ->
                fun ?enaSupport ->
                  fun ?groups ->
                    fun ?instanceInitiatedShutdownBehavior ->
                      fun ?instanceType ->
                        fun ?kernel ->
                          fun ?ramdisk ->
                            fun ?sriovNetSupport ->
                              fun ?userData ->
                                fun ?value ->
                                  fun ~instanceId ->
                                    fun () ->
                                      {
                                        sourceDestCheck;
                                        attribute;
                                        blockDeviceMappings;
                                        disableApiTermination;
                                        dryRun;
                                        ebsOptimized;
                                        enaSupport;
                                        groups;
                                        instanceInitiatedShutdownBehavior;
                                        instanceType;
                                        kernel;
                                        ramdisk;
                                        sriovNetSupport;
                                        userData;
                                        value;
                                        instanceId
                                      }
    let to_value x =
      structure_to_value
        [("SourceDestCheck",
           (Option.map x.sourceDestCheck ~f:AttributeBooleanValue.to_value));
        ("attribute",
          (Option.map x.attribute ~f:InstanceAttributeName.to_value));
        ("blockDeviceMapping",
          (Option.map x.blockDeviceMappings
             ~f:InstanceBlockDeviceMappingSpecificationList.to_value));
        ("disableApiTermination",
          (Option.map x.disableApiTermination
             ~f:AttributeBooleanValue.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ebsOptimized",
          (Option.map x.ebsOptimized ~f:AttributeBooleanValue.to_value));
        ("enaSupport",
          (Option.map x.enaSupport ~f:AttributeBooleanValue.to_value));
        ("GroupId", (Option.map x.groups ~f:GroupIdStringList.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("instanceInitiatedShutdownBehavior",
          (Option.map x.instanceInitiatedShutdownBehavior
             ~f:AttributeValue.to_value));
        ("instanceType",
          (Option.map x.instanceType ~f:AttributeValue.to_value));
        ("kernel", (Option.map x.kernel ~f:AttributeValue.to_value));
        ("ramdisk", (Option.map x.ramdisk ~f:AttributeValue.to_value));
        ("sriovNetSupport",
          (Option.map x.sriovNetSupport ~f:AttributeValue.to_value));
        ("userData", (Option.map x.userData ~f:BlobAttributeValue.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let userData =
        (Option.map ~f:BlobAttributeValue.of_xml)
          (Xml.child xml_arg0 "userData") in
      let sriovNetSupport =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "sriovNetSupport") in
      let ramdisk =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "ramdisk") in
      let kernel =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "kernel") in
      let instanceType =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "instanceType") in
      let instanceInitiatedShutdownBehavior =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "instanceInitiatedShutdownBehavior") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let groups =
        (Option.map ~f:GroupIdStringList.of_xml)
          (Xml.child xml_arg0 "GroupId") in
      let enaSupport =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "enaSupport") in
      let ebsOptimized =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "ebsOptimized") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let disableApiTermination =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "disableApiTermination") in
      let blockDeviceMappings =
        (Option.map ~f:InstanceBlockDeviceMappingSpecificationList.of_xml)
          (Xml.child xml_arg0 "blockDeviceMapping") in
      let attribute =
        (Option.map ~f:InstanceAttributeName.of_xml)
          (Xml.child xml_arg0 "attribute") in
      let sourceDestCheck =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "SourceDestCheck") in
      make ?value ?userData ?sriovNetSupport ?ramdisk ?kernel ?instanceType
        ?instanceInitiatedShutdownBehavior ~instanceId ?groups ?enaSupport
        ?ebsOptimized ?dryRun ?disableApiTermination ?blockDeviceMappings
        ?attribute ?sourceDestCheck ()
    let of_json json =
      let value = field_map json "Value" String_.of_json in
      let userData = field_map json "UserData" BlobAttributeValue.of_json in
      let sriovNetSupport =
        field_map json "SriovNetSupport" AttributeValue.of_json in
      let ramdisk = field_map json "Ramdisk" AttributeValue.of_json in
      let kernel = field_map json "Kernel" AttributeValue.of_json in
      let instanceType = field_map json "InstanceType" AttributeValue.of_json in
      let instanceInitiatedShutdownBehavior =
        field_map json "InstanceInitiatedShutdownBehavior"
          AttributeValue.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let groups = field_map json "Groups" GroupIdStringList.of_json in
      let enaSupport =
        field_map json "EnaSupport" AttributeBooleanValue.of_json in
      let ebsOptimized =
        field_map json "EbsOptimized" AttributeBooleanValue.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let disableApiTermination =
        field_map json "DisableApiTermination" AttributeBooleanValue.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings"
          InstanceBlockDeviceMappingSpecificationList.of_json in
      let attribute =
        field_map json "Attribute" InstanceAttributeName.of_json in
      let sourceDestCheck =
        field_map json "SourceDestCheck" AttributeBooleanValue.of_json in
      make ?value ?userData ?sriovNetSupport ?ramdisk ?kernel ?instanceType
        ?instanceInitiatedShutdownBehavior ~instanceId ?groups ?enaSupport
        ?ebsOptimized ?dryRun ?disableApiTermination ?blockDeviceMappings
        ?attribute ?sourceDestCheck ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified attribute of the specified instance. You can specify only one attribute at a time. Note: Using this action to change the security groups associated with an elastic network interface (ENI) attached to an instance in a VPC can result in an error if the instance has more than one ENI. To change the security groups associated with an ENI attached to an instance that has multiple ENIs, we recommend that you use the ModifyNetworkInterfaceAttribute action. To modify some attributes, the instance must be stopped. For more information, see Modify a stopped instance in the Amazon EC2 User Guide."]
module ModifyImageAttributeRequest =
  struct
    type nonrec t =
      {
      attribute: String_.t option
        [@ocaml.doc
          "The name of the attribute to modify. Valid values: description | launchPermission"];
      description: AttributeValue.t option
        [@ocaml.doc "A new description for the AMI."];
      imageId: ImageId.t [@ocaml.doc "The ID of the AMI."];
      launchPermission: LaunchPermissionModifications.t option
        [@ocaml.doc "A new launch permission for the AMI."];
      operationType: OperationType.t option
        [@ocaml.doc
          "The operation type. This parameter can be used only when the Attribute parameter is launchPermission."];
      productCodes: ProductCodeStringList.t option
        [@ocaml.doc "Not supported."];
      userGroups: UserGroupStringList.t option
        [@ocaml.doc
          "The user groups. This parameter can be used only when the Attribute parameter is launchPermission."];
      userIds: UserIdStringList.t option
        [@ocaml.doc
          "The Amazon Web Services account IDs. This parameter can be used only when the Attribute parameter is launchPermission."];
      value: String_.t option
        [@ocaml.doc
          "The value of the attribute being modified. This parameter can be used only when the Attribute parameter is description."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      organizationArns: OrganizationArnStringList.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an organization. This parameter can be used only when the Attribute parameter is launchPermission."];
      organizationalUnitArns: OrganizationalUnitArnStringList.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an organizational unit (OU). This parameter can be used only when the Attribute parameter is launchPermission."]}
    let context_ = "ModifyImageAttributeRequest"
    let make ?attribute =
      fun ?description ->
        fun ?launchPermission ->
          fun ?operationType ->
            fun ?productCodes ->
              fun ?userGroups ->
                fun ?userIds ->
                  fun ?value ->
                    fun ?dryRun ->
                      fun ?organizationArns ->
                        fun ?organizationalUnitArns ->
                          fun ~imageId ->
                            fun () ->
                              {
                                attribute;
                                description;
                                launchPermission;
                                operationType;
                                productCodes;
                                userGroups;
                                userIds;
                                value;
                                dryRun;
                                organizationArns;
                                organizationalUnitArns;
                                imageId
                              }
    let to_value x =
      structure_to_value
        [("Attribute", (Option.map x.attribute ~f:String_.to_value));
        ("Description",
          (Option.map x.description ~f:AttributeValue.to_value));
        ("ImageId", (Some (ImageId.to_value x.imageId)));
        ("LaunchPermission",
          (Option.map x.launchPermission
             ~f:LaunchPermissionModifications.to_value));
        ("OperationType",
          (Option.map x.operationType ~f:OperationType.to_value));
        ("ProductCode",
          (Option.map x.productCodes ~f:ProductCodeStringList.to_value));
        ("UserGroup",
          (Option.map x.userGroups ~f:UserGroupStringList.to_value));
        ("UserId", (Option.map x.userIds ~f:UserIdStringList.to_value));
        ("Value", (Option.map x.value ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("OrganizationArn",
          (Option.map x.organizationArns
             ~f:OrganizationArnStringList.to_value));
        ("OrganizationalUnitArn",
          (Option.map x.organizationalUnitArns
             ~f:OrganizationalUnitArnStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationalUnitArns =
        (Option.map ~f:OrganizationalUnitArnStringList.of_xml)
          (Xml.child xml_arg0 "OrganizationalUnitArn") in
      let organizationArns =
        (Option.map ~f:OrganizationArnStringList.of_xml)
          (Xml.child xml_arg0 "OrganizationArn") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Value") in
      let userIds =
        (Option.map ~f:UserIdStringList.of_xml) (Xml.child xml_arg0 "UserId") in
      let userGroups =
        (Option.map ~f:UserGroupStringList.of_xml)
          (Xml.child xml_arg0 "UserGroup") in
      let productCodes =
        (Option.map ~f:ProductCodeStringList.of_xml)
          (Xml.child xml_arg0 "ProductCode") in
      let operationType =
        (Option.map ~f:OperationType.of_xml)
          (Xml.child xml_arg0 "OperationType") in
      let launchPermission =
        (Option.map ~f:LaunchPermissionModifications.of_xml)
          (Xml.child xml_arg0 "LaunchPermission") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      let description =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "Description") in
      let attribute =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Attribute") in
      make ?organizationalUnitArns ?organizationArns ?dryRun ?value ?userIds
        ?userGroups ?productCodes ?operationType ?launchPermission ~imageId
        ?description ?attribute ()
    let of_json json =
      let organizationalUnitArns =
        field_map json "OrganizationalUnitArns"
          OrganizationalUnitArnStringList.of_json in
      let organizationArns =
        field_map json "OrganizationArns" OrganizationArnStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let value = field_map json "Value" String_.of_json in
      let userIds = field_map json "UserIds" UserIdStringList.of_json in
      let userGroups =
        field_map json "UserGroups" UserGroupStringList.of_json in
      let productCodes =
        field_map json "ProductCodes" ProductCodeStringList.of_json in
      let operationType =
        field_map json "OperationType" OperationType.of_json in
      let launchPermission =
        field_map json "LaunchPermission"
          LaunchPermissionModifications.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      let description = field_map json "Description" AttributeValue.of_json in
      let attribute = field_map json "Attribute" String_.of_json in
      make ?organizationalUnitArns ?organizationArns ?dryRun ?value ?userIds
        ?userGroups ?productCodes ?operationType ?launchPermission ~imageId
        ?description ?attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for ModifyImageAttribute."]
module ModifyIdentityIdFormatRequest =
  struct
    type nonrec t =
      {
      principalArn: String_.t
        [@ocaml.doc
          "The ARN of the principal, which can be an IAM user, IAM role, or the root user. Specify all to modify the ID format for all IAM users, IAM roles, and the root user of the account."];
      resource: String_.t
        [@ocaml.doc
          "The type of resource: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | route-table | route-table-association | security-group | subnet | subnet-cidr-block-association | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. Alternatively, use the all-current option to include all resource types that are currently within their opt-in period for longer IDs."];
      useLongIds: Boolean.t
        [@ocaml.doc
          "Indicates whether the resource should use longer IDs (17-character IDs)"]}
    let context_ = "ModifyIdentityIdFormatRequest"
    let make ~principalArn =
      fun ~resource ->
        fun ~useLongIds -> fun () -> { principalArn; resource; useLongIds }
    let to_value x =
      structure_to_value
        [("principalArn", (Some (String_.to_value x.principalArn)));
        ("resource", (Some (String_.to_value x.resource)));
        ("useLongIds", (Some (Boolean.to_value x.useLongIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let useLongIds =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "useLongIds") in
      let resource =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resource") in
      let principalArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "principalArn") in
      make ~useLongIds ~resource ~principalArn ()
    let of_json json =
      let useLongIds = field_map_exn json "UseLongIds" Boolean.of_json in
      let resource = field_map_exn json "Resource" String_.of_json in
      let principalArn = field_map_exn json "PrincipalArn" String_.of_json in
      make ~useLongIds ~resource ~principalArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the ID format of a resource for a specified IAM user, IAM role, or the root user for an account; or all IAM users, IAM roles, and the root user for an account. You can specify that resources should receive longer IDs (17-character IDs) when they are created. This request can only be used to modify longer ID settings for resource types that are within the opt-in period. Resources currently in their opt-in period include: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | route-table | route-table-association | security-group | subnet | subnet-cidr-block-association | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. For more information, see Resource IDs in the Amazon Elastic Compute Cloud User Guide. This setting applies to the principal specified in the request; it does not apply to the principal that makes the request. Resources created with longer IDs are visible to all IAM roles and users, regardless of these settings and provided that they have permission to use the relevant Describe command for the resource type."]
module ModifyIdFormatRequest =
  struct
    type nonrec t =
      {
      resource: String_.t
        [@ocaml.doc
          "The type of resource: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | route-table | route-table-association | security-group | subnet | subnet-cidr-block-association | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. Alternatively, use the all-current option to include all resource types that are currently within their opt-in period for longer IDs."];
      useLongIds: Boolean.t
        [@ocaml.doc
          "Indicate whether the resource should use longer IDs (17-character IDs)."]}
    let context_ = "ModifyIdFormatRequest"
    let make ~resource =
      fun ~useLongIds -> fun () -> { resource; useLongIds }
    let to_value x =
      structure_to_value
        [("Resource", (Some (String_.to_value x.resource)));
        ("UseLongIds", (Some (Boolean.to_value x.useLongIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let useLongIds =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "UseLongIds") in
      let resource =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Resource") in
      make ~useLongIds ~resource ()
    let of_json json =
      let useLongIds = field_map_exn json "UseLongIds" Boolean.of_json in
      let resource = field_map_exn json "Resource" String_.of_json in
      make ~useLongIds ~resource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the ID format for the specified resource on a per-Region basis. You can specify that resources should receive longer IDs (17-character IDs) when they are created. This request can only be used to modify longer ID settings for resource types that are within the opt-in period. Resources currently in their opt-in period include: bundle | conversion-task | customer-gateway | dhcp-options | elastic-ip-allocation | elastic-ip-association | export-task | flow-log | image | import-task | internet-gateway | network-acl | network-acl-association | network-interface | network-interface-attachment | prefix-list | route-table | route-table-association | security-group | subnet | subnet-cidr-block-association | vpc | vpc-cidr-block-association | vpc-endpoint | vpc-peering-connection | vpn-connection | vpn-gateway. This setting applies to the IAM user who makes the request; it does not apply to the entire Amazon Web Services account. By default, an IAM user defaults to the same settings as the root user. If you're using this action as the root user, then these settings apply to the entire account, unless an IAM user explicitly overrides these settings for themselves. For more information, see Resource IDs in the Amazon Elastic Compute Cloud User Guide. Resources created with longer IDs are visible to all IAM roles and users, regardless of these settings and provided that they have permission to use the relevant Describe command for the resource type."]
module ModifyHostsResult =
  struct
    type nonrec t =
      {
      successful: ResponseHostIdList.t option
        [@ocaml.doc
          "The IDs of the Dedicated Hosts that were successfully modified."];
      unsuccessful: UnsuccessfulItemList.t option
        [@ocaml.doc
          "The IDs of the Dedicated Hosts that could not be modified. Check whether the setting you requested can be used."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successful =
      fun ?unsuccessful -> fun () -> { successful; unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successful",
           (Option.map x.successful ~f:ResponseHostIdList.to_value));
        ("unsuccessful",
          (Option.map x.unsuccessful ~f:UnsuccessfulItemList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemList.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      let successful =
        (Option.map ~f:ResponseHostIdList.of_xml)
          (Xml.child xml_arg0 "successful") in
      make ?unsuccessful ?successful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemList.of_json in
      let successful = field_map json "Successful" ResponseHostIdList.of_json in
      make ?unsuccessful ?successful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify the auto-placement setting of a Dedicated Host. When auto-placement is enabled, any instances that you launch with a tenancy of host but without a specific host ID are placed onto any available Dedicated Host in your account that has auto-placement enabled. When auto-placement is disabled, you need to provide a host ID to have the instance launch onto a specific host. If no host ID is provided, the instance is launched onto a suitable host with auto-placement enabled. You can also use this API action to modify a Dedicated Host to support either multiple instance types in an instance family, or to support a specific instance type only."]
module ModifyHostsRequest =
  struct
    type nonrec t =
      {
      autoPlacement: AutoPlacement.t option
        [@ocaml.doc "Specify whether to enable or disable auto-placement."];
      hostIds: RequestHostIdList.t
        [@ocaml.doc "The IDs of the Dedicated Hosts to modify."];
      hostRecovery: HostRecovery.t option
        [@ocaml.doc
          "Indicates whether to enable or disable host recovery for the Dedicated Host. For more information, see Host recovery in the Amazon EC2 User Guide."];
      instanceType: String_.t option
        [@ocaml.doc
          "Specifies the instance type to be supported by the Dedicated Host. Specify this parameter to modify a Dedicated Host to support only a specific instance type. If you want to modify a Dedicated Host to support multiple instance types in its current instance family, omit this parameter and specify InstanceFamily instead. You cannot specify InstanceType and InstanceFamily in the same request."];
      instanceFamily: String_.t option
        [@ocaml.doc
          "Specifies the instance family to be supported by the Dedicated Host. Specify this parameter to modify a Dedicated Host to support multiple instance types within its current instance family. If you want to modify a Dedicated Host to support a specific instance type only, omit this parameter and specify InstanceType instead. You cannot specify InstanceFamily and InstanceType in the same request."]}
    let context_ = "ModifyHostsRequest"
    let make ?autoPlacement =
      fun ?hostRecovery ->
        fun ?instanceType ->
          fun ?instanceFamily ->
            fun ~hostIds ->
              fun () ->
                {
                  autoPlacement;
                  hostRecovery;
                  instanceType;
                  instanceFamily;
                  hostIds
                }
    let to_value x =
      structure_to_value
        [("autoPlacement",
           (Option.map x.autoPlacement ~f:AutoPlacement.to_value));
        ("hostId", (Some (RequestHostIdList.to_value x.hostIds)));
        ("HostRecovery",
          (Option.map x.hostRecovery ~f:HostRecovery.to_value));
        ("InstanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("InstanceFamily", (Option.map x.instanceFamily ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceFamily") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceType") in
      let hostRecovery =
        (Option.map ~f:HostRecovery.of_xml)
          (Xml.child xml_arg0 "HostRecovery") in
      let hostIds =
        RequestHostIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "hostId") in
      let autoPlacement =
        (Option.map ~f:AutoPlacement.of_xml)
          (Xml.child xml_arg0 "autoPlacement") in
      make ?instanceFamily ?instanceType ?hostRecovery ~hostIds
        ?autoPlacement ()
    let of_json json =
      let instanceFamily = field_map json "InstanceFamily" String_.of_json in
      let instanceType = field_map json "InstanceType" String_.of_json in
      let hostRecovery = field_map json "HostRecovery" HostRecovery.of_json in
      let hostIds = field_map_exn json "HostIds" RequestHostIdList.of_json in
      let autoPlacement =
        field_map json "AutoPlacement" AutoPlacement.of_json in
      make ?instanceFamily ?instanceType ?hostRecovery ~hostIds
        ?autoPlacement ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modify the auto-placement setting of a Dedicated Host. When auto-placement is enabled, any instances that you launch with a tenancy of host but without a specific host ID are placed onto any available Dedicated Host in your account that has auto-placement enabled. When auto-placement is disabled, you need to provide a host ID to have the instance launch onto a specific host. If no host ID is provided, the instance is launched onto a suitable host with auto-placement enabled. You can also use this API action to modify a Dedicated Host to support either multiple instance types in an instance family, or to support a specific instance type only."]
module ModifyFpgaImageAttributeResult =
  struct
    type nonrec t =
      {
      fpgaImageAttribute: FpgaImageAttribute.t option
        [@ocaml.doc "Information about the attribute."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageAttribute = fun () -> { fpgaImageAttribute }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageAttribute",
           (Option.map x.fpgaImageAttribute ~f:FpgaImageAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageAttribute =
        (Option.map ~f:FpgaImageAttribute.of_xml)
          (Xml.child xml_arg0 "fpgaImageAttribute") in
      make ?fpgaImageAttribute ()
    let of_json json =
      let fpgaImageAttribute =
        field_map json "FpgaImageAttribute" FpgaImageAttribute.of_json in
      make ?fpgaImageAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified attribute of the specified Amazon FPGA Image (AFI)."]
module ModifyFpgaImageAttributeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fpgaImageId: FpgaImageId.t [@ocaml.doc "The ID of the AFI."];
      attribute: FpgaImageAttributeName.t option
        [@ocaml.doc "The name of the attribute."];
      operationType: OperationType.t option
        [@ocaml.doc "The operation type."];
      userIds: UserIdStringList.t option
        [@ocaml.doc
          "The Amazon Web Services account IDs. This parameter is valid only when modifying the loadPermission attribute."];
      userGroups: UserGroupStringList.t option
        [@ocaml.doc
          "The user groups. This parameter is valid only when modifying the loadPermission attribute."];
      productCodes: ProductCodeStringList.t option
        [@ocaml.doc
          "The product codes. After you add a product code to an AFI, it can't be removed. This parameter is valid only when modifying the productCodes attribute."];
      loadPermission: LoadPermissionModifications.t option
        [@ocaml.doc "The load permission for the AFI."];
      description: String_.t option [@ocaml.doc "A description for the AFI."];
      name: String_.t option [@ocaml.doc "A name for the AFI."]}
    let context_ = "ModifyFpgaImageAttributeRequest"
    let make ?dryRun =
      fun ?attribute ->
        fun ?operationType ->
          fun ?userIds ->
            fun ?userGroups ->
              fun ?productCodes ->
                fun ?loadPermission ->
                  fun ?description ->
                    fun ?name ->
                      fun ~fpgaImageId ->
                        fun () ->
                          {
                            dryRun;
                            attribute;
                            operationType;
                            userIds;
                            userGroups;
                            productCodes;
                            loadPermission;
                            description;
                            name;
                            fpgaImageId
                          }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId", (Some (FpgaImageId.to_value x.fpgaImageId)));
        ("Attribute",
          (Option.map x.attribute ~f:FpgaImageAttributeName.to_value));
        ("OperationType",
          (Option.map x.operationType ~f:OperationType.to_value));
        ("UserId", (Option.map x.userIds ~f:UserIdStringList.to_value));
        ("UserGroup",
          (Option.map x.userGroups ~f:UserGroupStringList.to_value));
        ("ProductCode",
          (Option.map x.productCodes ~f:ProductCodeStringList.to_value));
        ("LoadPermission",
          (Option.map x.loadPermission
             ~f:LoadPermissionModifications.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let loadPermission =
        (Option.map ~f:LoadPermissionModifications.of_xml)
          (Xml.child xml_arg0 "LoadPermission") in
      let productCodes =
        (Option.map ~f:ProductCodeStringList.of_xml)
          (Xml.child xml_arg0 "ProductCode") in
      let userGroups =
        (Option.map ~f:UserGroupStringList.of_xml)
          (Xml.child xml_arg0 "UserGroup") in
      let userIds =
        (Option.map ~f:UserIdStringList.of_xml) (Xml.child xml_arg0 "UserId") in
      let operationType =
        (Option.map ~f:OperationType.of_xml)
          (Xml.child xml_arg0 "OperationType") in
      let attribute =
        (Option.map ~f:FpgaImageAttributeName.of_xml)
          (Xml.child xml_arg0 "Attribute") in
      let fpgaImageId =
        FpgaImageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?name ?description ?loadPermission ?productCodes ?userGroups
        ?userIds ?operationType ?attribute ~fpgaImageId ?dryRun ()
    let of_json json =
      let name = field_map json "Name" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let loadPermission =
        field_map json "LoadPermission" LoadPermissionModifications.of_json in
      let productCodes =
        field_map json "ProductCodes" ProductCodeStringList.of_json in
      let userGroups =
        field_map json "UserGroups" UserGroupStringList.of_json in
      let userIds = field_map json "UserIds" UserIdStringList.of_json in
      let operationType =
        field_map json "OperationType" OperationType.of_json in
      let attribute =
        field_map json "Attribute" FpgaImageAttributeName.of_json in
      let fpgaImageId = field_map_exn json "FpgaImageId" FpgaImageId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?name ?description ?loadPermission ?productCodes ?userGroups
        ?userIds ?operationType ?attribute ~fpgaImageId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified attribute of the specified Amazon FPGA Image (AFI)."]
module ModifyFleetResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "If the request succeeds, the response returns true. If the request fails, no response is returned, and instead an error message is returned."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified EC2 Fleet. You can only modify an EC2 Fleet request of type maintain. While the EC2 Fleet is being modified, it is in the modifying state. To scale up your EC2 Fleet, increase its target capacity. The EC2 Fleet launches the additional Spot Instances according to the allocation strategy for the EC2 Fleet request. If the allocation strategy is lowest-price, the EC2 Fleet launches instances using the Spot Instance pool with the lowest price. If the allocation strategy is diversified, the EC2 Fleet distributes the instances across the Spot Instance pools. If the allocation strategy is capacity-optimized, EC2 Fleet launches instances from Spot Instance pools with optimal capacity for the number of instances that are launching. To scale down your EC2 Fleet, decrease its target capacity. First, the EC2 Fleet cancels any open requests that exceed the new target capacity. You can request that the EC2 Fleet terminate Spot Instances until the size of the fleet no longer exceeds the new target capacity. If the allocation strategy is lowest-price, the EC2 Fleet terminates the instances with the highest price per unit. If the allocation strategy is capacity-optimized, the EC2 Fleet terminates the instances in the Spot Instance pools that have the least available Spot Instance capacity. If the allocation strategy is diversified, the EC2 Fleet terminates instances across the Spot Instance pools. Alternatively, you can request that the EC2 Fleet keep the fleet at its current size, but not replace any Spot Instances that are interrupted or that you terminate manually. If you are finished with your EC2 Fleet for now, but will use it again later, you can set the target capacity to 0."]
module ModifyFleetRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      excessCapacityTerminationPolicy:
        FleetExcessCapacityTerminationPolicy.t option
        [@ocaml.doc
          "Indicates whether running instances should be terminated if the total target capacity of the EC2 Fleet is decreased below the current size of the EC2 Fleet."];
      launchTemplateConfigs: FleetLaunchTemplateConfigListRequest.t option
        [@ocaml.doc "The launch template and overrides."];
      fleetId: FleetId.t [@ocaml.doc "The ID of the EC2 Fleet."];
      targetCapacitySpecification:
        TargetCapacitySpecificationRequest.t option
        [@ocaml.doc "The size of the EC2 Fleet."];
      context: String_.t option [@ocaml.doc "Reserved."]}
    let context_ = "ModifyFleetRequest"
    let make ?dryRun =
      fun ?excessCapacityTerminationPolicy ->
        fun ?launchTemplateConfigs ->
          fun ?targetCapacitySpecification ->
            fun ?context ->
              fun ~fleetId ->
                fun () ->
                  {
                    dryRun;
                    excessCapacityTerminationPolicy;
                    launchTemplateConfigs;
                    targetCapacitySpecification;
                    context;
                    fleetId
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ExcessCapacityTerminationPolicy",
          (Option.map x.excessCapacityTerminationPolicy
             ~f:FleetExcessCapacityTerminationPolicy.to_value));
        ("LaunchTemplateConfig",
          (Option.map x.launchTemplateConfigs
             ~f:FleetLaunchTemplateConfigListRequest.to_value));
        ("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("TargetCapacitySpecification",
          (Option.map x.targetCapacitySpecification
             ~f:TargetCapacitySpecificationRequest.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let targetCapacitySpecification =
        (Option.map ~f:TargetCapacitySpecificationRequest.of_xml)
          (Xml.child xml_arg0 "TargetCapacitySpecification") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let launchTemplateConfigs =
        (Option.map ~f:FleetLaunchTemplateConfigListRequest.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateConfig") in
      let excessCapacityTerminationPolicy =
        (Option.map ~f:FleetExcessCapacityTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "ExcessCapacityTerminationPolicy") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?context ?targetCapacitySpecification ~fleetId
        ?launchTemplateConfigs ?excessCapacityTerminationPolicy ?dryRun ()
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let targetCapacitySpecification =
        field_map json "TargetCapacitySpecification"
          TargetCapacitySpecificationRequest.of_json in
      let fleetId = field_map_exn json "FleetId" FleetId.of_json in
      let launchTemplateConfigs =
        field_map json "LaunchTemplateConfigs"
          FleetLaunchTemplateConfigListRequest.of_json in
      let excessCapacityTerminationPolicy =
        field_map json "ExcessCapacityTerminationPolicy"
          FleetExcessCapacityTerminationPolicy.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?context ?targetCapacitySpecification ~fleetId
        ?launchTemplateConfigs ?excessCapacityTerminationPolicy ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified EC2 Fleet. You can only modify an EC2 Fleet request of type maintain. While the EC2 Fleet is being modified, it is in the modifying state. To scale up your EC2 Fleet, increase its target capacity. The EC2 Fleet launches the additional Spot Instances according to the allocation strategy for the EC2 Fleet request. If the allocation strategy is lowest-price, the EC2 Fleet launches instances using the Spot Instance pool with the lowest price. If the allocation strategy is diversified, the EC2 Fleet distributes the instances across the Spot Instance pools. If the allocation strategy is capacity-optimized, EC2 Fleet launches instances from Spot Instance pools with optimal capacity for the number of instances that are launching. To scale down your EC2 Fleet, decrease its target capacity. First, the EC2 Fleet cancels any open requests that exceed the new target capacity. You can request that the EC2 Fleet terminate Spot Instances until the size of the fleet no longer exceeds the new target capacity. If the allocation strategy is lowest-price, the EC2 Fleet terminates the instances with the highest price per unit. If the allocation strategy is capacity-optimized, the EC2 Fleet terminates the instances in the Spot Instance pools that have the least available Spot Instance capacity. If the allocation strategy is diversified, the EC2 Fleet terminates instances across the Spot Instance pools. Alternatively, you can request that the EC2 Fleet keep the fleet at its current size, but not replace any Spot Instances that are interrupted or that you terminate manually. If you are finished with your EC2 Fleet for now, but will use it again later, you can set the target capacity to 0."]
module ModifyEbsDefaultKmsKeyIdResult =
  struct
    type nonrec t =
      {
      kmsKeyId: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the default KMS key for encryption by default."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?kmsKeyId = fun () -> { kmsKeyId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("kmsKeyId", (Option.map x.kmsKeyId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      make ?kmsKeyId ()
    let of_json json =
      let kmsKeyId = field_map json "KmsKeyId" String_.of_json in
      make ?kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the default KMS key for EBS encryption by default for your account in this Region. Amazon Web Services creates a unique Amazon Web Services managed KMS key in each Region for use with encryption by default. If you change the default KMS key to a symmetric customer managed KMS key, it is used instead of the Amazon Web Services managed KMS key. To reset the default KMS key to the Amazon Web Services managed KMS key for EBS, use ResetEbsDefaultKmsKeyId. Amazon EBS does not support asymmetric KMS keys. If you delete or disable the customer managed KMS key that you specified for use with encryption by default, your instances will fail to launch. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module ModifyEbsDefaultKmsKeyIdRequest =
  struct
    type nonrec t =
      {
      kmsKeyId: KmsKeyId.t
        [@ocaml.doc
          "The identifier of the Key Management Service (KMS) KMS key to use for Amazon EBS encryption. If this parameter is not specified, your KMS key for Amazon EBS is used. If KmsKeyId is specified, the encrypted state must be true. You can specify the KMS key using any of the following: Key ID. For example, 1234abcd-12ab-34cd-56ef-1234567890ab. Key alias. For example, alias/ExampleAlias. Key ARN. For example, arn:aws:kms:us-east-1:012345678910:key/1234abcd-12ab-34cd-56ef-1234567890ab. Alias ARN. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. Amazon Web Services authenticates the KMS key asynchronously. Therefore, if you specify an ID, alias, or ARN that is not valid, the action can appear to complete, but eventually fails. Amazon EBS does not support asymmetric KMS keys."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyEbsDefaultKmsKeyIdRequest"
    let make ?dryRun = fun ~kmsKeyId -> fun () -> { dryRun; kmsKeyId }
    let to_value x =
      structure_to_value
        [("KmsKeyId", (Some (KmsKeyId.to_value x.kmsKeyId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let kmsKeyId =
        KmsKeyId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KmsKeyId") in
      make ?dryRun ~kmsKeyId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let kmsKeyId = field_map_exn json "KmsKeyId" KmsKeyId.of_json in
      make ?dryRun ~kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the default KMS key for EBS encryption by default for your account in this Region. Amazon Web Services creates a unique Amazon Web Services managed KMS key in each Region for use with encryption by default. If you change the default KMS key to a symmetric customer managed KMS key, it is used instead of the Amazon Web Services managed KMS key. To reset the default KMS key to the Amazon Web Services managed KMS key for EBS, use ResetEbsDefaultKmsKeyId. Amazon EBS does not support asymmetric KMS keys. If you delete or disable the customer managed KMS key that you specified for use with encryption by default, your instances will fail to launch. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module ModifyDefaultCreditSpecificationResult =
  struct
    type nonrec t =
      {
      instanceFamilyCreditSpecification:
        InstanceFamilyCreditSpecification.t option
        [@ocaml.doc
          "The default credit option for CPU usage of the instance family."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceFamilyCreditSpecification =
      fun () -> { instanceFamilyCreditSpecification }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceFamilyCreditSpecification",
           (Option.map x.instanceFamilyCreditSpecification
              ~f:InstanceFamilyCreditSpecification.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceFamilyCreditSpecification =
        (Option.map ~f:InstanceFamilyCreditSpecification.of_xml)
          (Xml.child xml_arg0 "instanceFamilyCreditSpecification") in
      make ?instanceFamilyCreditSpecification ()
    let of_json json =
      let instanceFamilyCreditSpecification =
        field_map json "InstanceFamilyCreditSpecification"
          InstanceFamilyCreditSpecification.of_json in
      make ?instanceFamilyCreditSpecification ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the default credit option for CPU usage of burstable performance instances. The default credit option is set at the account level per Amazon Web Services Region, and is specified per instance family. All new burstable performance instances in the account launch using the default credit option. ModifyDefaultCreditSpecification is an asynchronous operation, which works at an Amazon Web Services Region level and modifies the credit option for each Availability Zone. All zones in a Region are updated within five minutes. But if instances are launched during this operation, they might not get the new credit option until the zone is updated. To verify whether the update has occurred, you can call GetDefaultCreditSpecification and check DefaultCreditSpecification for updates. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module ModifyDefaultCreditSpecificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceFamily: UnlimitedSupportedInstanceFamily.t
        [@ocaml.doc "The instance family."];
      cpuCredits: String_.t
        [@ocaml.doc
          "The credit option for CPU usage of the instance family. Valid Values: standard | unlimited"]}
    let context_ = "ModifyDefaultCreditSpecificationRequest"
    let make ?dryRun =
      fun ~instanceFamily ->
        fun ~cpuCredits -> fun () -> { dryRun; instanceFamily; cpuCredits }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceFamily",
          (Some (UnlimitedSupportedInstanceFamily.to_value x.instanceFamily)));
        ("CpuCredits", (Some (String_.to_value x.cpuCredits)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cpuCredits =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CpuCredits") in
      let instanceFamily =
        UnlimitedSupportedInstanceFamily.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceFamily") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~cpuCredits ~instanceFamily ?dryRun ()
    let of_json json =
      let cpuCredits = field_map_exn json "CpuCredits" String_.of_json in
      let instanceFamily =
        field_map_exn json "InstanceFamily"
          UnlimitedSupportedInstanceFamily.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~cpuCredits ~instanceFamily ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the default credit option for CPU usage of burstable performance instances. The default credit option is set at the account level per Amazon Web Services Region, and is specified per instance family. All new burstable performance instances in the account launch using the default credit option. ModifyDefaultCreditSpecification is an asynchronous operation, which works at an Amazon Web Services Region level and modifies the credit option for each Availability Zone. All zones in a Region are updated within five minutes. But if instances are launched during this operation, they might not get the new credit option until the zone is updated. To verify whether the update has occurred, you can call GetDefaultCreditSpecification and check DefaultCreditSpecification for updates. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module ModifyClientVpnEndpointResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified Client VPN endpoint. Modifying the DNS server resets existing client connections."]
module ModifyClientVpnEndpointRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint to modify."];
      serverCertificateArn: String_.t option
        [@ocaml.doc
          "The ARN of the server certificate to be used. The server certificate must be provisioned in Certificate Manager (ACM)."];
      connectionLogOptions: ConnectionLogOptions.t option
        [@ocaml.doc
          "Information about the client connection logging options. If you enable client connection logging, data about client connections is sent to a Cloudwatch Logs log stream. The following information is logged: Client connection requests Client connection results (successful and unsuccessful) Reasons for unsuccessful client connection requests Client connection termination time"];
      dnsServers: DnsServersOptionsModifyStructure.t option
        [@ocaml.doc
          "Information about the DNS servers to be used by Client VPN connections. A Client VPN endpoint can have up to two DNS servers."];
      vpnPort: Integer.t option
        [@ocaml.doc
          "The port number to assign to the Client VPN endpoint for TCP and UDP traffic. Valid Values: 443 | 1194 Default Value: 443"];
      description: String_.t option
        [@ocaml.doc "A brief description of the Client VPN endpoint."];
      splitTunnel: Boolean.t option
        [@ocaml.doc
          "Indicates whether the VPN is split-tunnel. For information about split-tunnel VPN endpoints, see Split-tunnel Client VPN endpoint in the Client VPN Administrator Guide."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      securityGroupIds: ClientVpnSecurityGroupIdSet.t option
        [@ocaml.doc
          "The IDs of one or more security groups to apply to the target network."];
      vpcId: VpcId.t option
        [@ocaml.doc
          "The ID of the VPC to associate with the Client VPN endpoint."];
      selfServicePortal: SelfServicePortal.t option
        [@ocaml.doc
          "Specify whether to enable the self-service portal for the Client VPN endpoint."];
      clientConnectOptions: ClientConnectOptions.t option
        [@ocaml.doc
          "The options for managing connection authorization for new client connections."];
      sessionTimeoutHours: Integer.t option
        [@ocaml.doc
          "The maximum VPN session duration time in hours. Valid values: 8 | 10 | 12 | 24 Default value: 24"];
      clientLoginBannerOptions: ClientLoginBannerOptions.t option
        [@ocaml.doc
          "Options for enabling a customizable text banner that will be displayed on Amazon Web Services provided clients when a VPN session is established."]}
    let context_ = "ModifyClientVpnEndpointRequest"
    let make ?serverCertificateArn =
      fun ?connectionLogOptions ->
        fun ?dnsServers ->
          fun ?vpnPort ->
            fun ?description ->
              fun ?splitTunnel ->
                fun ?dryRun ->
                  fun ?securityGroupIds ->
                    fun ?vpcId ->
                      fun ?selfServicePortal ->
                        fun ?clientConnectOptions ->
                          fun ?sessionTimeoutHours ->
                            fun ?clientLoginBannerOptions ->
                              fun ~clientVpnEndpointId ->
                                fun () ->
                                  {
                                    serverCertificateArn;
                                    connectionLogOptions;
                                    dnsServers;
                                    vpnPort;
                                    description;
                                    splitTunnel;
                                    dryRun;
                                    securityGroupIds;
                                    vpcId;
                                    selfServicePortal;
                                    clientConnectOptions;
                                    sessionTimeoutHours;
                                    clientLoginBannerOptions;
                                    clientVpnEndpointId
                                  }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("ServerCertificateArn",
          (Option.map x.serverCertificateArn ~f:String_.to_value));
        ("ConnectionLogOptions",
          (Option.map x.connectionLogOptions ~f:ConnectionLogOptions.to_value));
        ("DnsServers",
          (Option.map x.dnsServers
             ~f:DnsServersOptionsModifyStructure.to_value));
        ("VpnPort", (Option.map x.vpnPort ~f:Integer.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("SplitTunnel", (Option.map x.splitTunnel ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:ClientVpnSecurityGroupIdSet.to_value));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("SelfServicePortal",
          (Option.map x.selfServicePortal ~f:SelfServicePortal.to_value));
        ("ClientConnectOptions",
          (Option.map x.clientConnectOptions ~f:ClientConnectOptions.to_value));
        ("SessionTimeoutHours",
          (Option.map x.sessionTimeoutHours ~f:Integer.to_value));
        ("ClientLoginBannerOptions",
          (Option.map x.clientLoginBannerOptions
             ~f:ClientLoginBannerOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientLoginBannerOptions =
        (Option.map ~f:ClientLoginBannerOptions.of_xml)
          (Xml.child xml_arg0 "ClientLoginBannerOptions") in
      let sessionTimeoutHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "SessionTimeoutHours") in
      let clientConnectOptions =
        (Option.map ~f:ClientConnectOptions.of_xml)
          (Xml.child xml_arg0 "ClientConnectOptions") in
      let selfServicePortal =
        (Option.map ~f:SelfServicePortal.of_xml)
          (Xml.child xml_arg0 "SelfServicePortal") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let securityGroupIds =
        (Option.map ~f:ClientVpnSecurityGroupIdSet.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let splitTunnel =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SplitTunnel") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let vpnPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "VpnPort") in
      let dnsServers =
        (Option.map ~f:DnsServersOptionsModifyStructure.of_xml)
          (Xml.child xml_arg0 "DnsServers") in
      let connectionLogOptions =
        (Option.map ~f:ConnectionLogOptions.of_xml)
          (Xml.child xml_arg0 "ConnectionLogOptions") in
      let serverCertificateArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ServerCertificateArn") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?clientLoginBannerOptions ?sessionTimeoutHours
        ?clientConnectOptions ?selfServicePortal ?vpcId ?securityGroupIds
        ?dryRun ?splitTunnel ?description ?vpnPort ?dnsServers
        ?connectionLogOptions ?serverCertificateArn ~clientVpnEndpointId ()
    let of_json json =
      let clientLoginBannerOptions =
        field_map json "ClientLoginBannerOptions"
          ClientLoginBannerOptions.of_json in
      let sessionTimeoutHours =
        field_map json "SessionTimeoutHours" Integer.of_json in
      let clientConnectOptions =
        field_map json "ClientConnectOptions" ClientConnectOptions.of_json in
      let selfServicePortal =
        field_map json "SelfServicePortal" SelfServicePortal.of_json in
      let vpcId = field_map json "VpcId" VpcId.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds" ClientVpnSecurityGroupIdSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let splitTunnel = field_map json "SplitTunnel" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let vpnPort = field_map json "VpnPort" Integer.of_json in
      let dnsServers =
        field_map json "DnsServers" DnsServersOptionsModifyStructure.of_json in
      let connectionLogOptions =
        field_map json "ConnectionLogOptions" ConnectionLogOptions.of_json in
      let serverCertificateArn =
        field_map json "ServerCertificateArn" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?clientLoginBannerOptions ?sessionTimeoutHours
        ?clientConnectOptions ?selfServicePortal ?vpcId ?securityGroupIds
        ?dryRun ?splitTunnel ?description ?vpnPort ?dnsServers
        ?connectionLogOptions ?serverCertificateArn ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the specified Client VPN endpoint. Modifying the DNS server resets existing client connections."]
module ModifyCapacityReservationResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a Capacity Reservation's capacity and the conditions under which it is to be released. You cannot change a Capacity Reservation's instance type, EBS optimization, instance store settings, platform, Availability Zone, or instance eligibility. If you need to modify any of these attributes, we recommend that you cancel the Capacity Reservation, and then create a new one with the required attributes."]
module ModifyCapacityReservationRequest =
  struct
    type nonrec t =
      {
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc "The ID of the Capacity Reservation."];
      instanceCount: Integer.t option
        [@ocaml.doc
          "The number of instances for which to reserve capacity. The number of instances can't be increased or decreased by more than 1000 in a single request."];
      endDate: DateTime.t option
        [@ocaml.doc
          "The date and time at which the Capacity Reservation expires. When a Capacity Reservation expires, the reserved capacity is released and you can no longer launch instances into it. The Capacity Reservation's state changes to expired when it reaches its end date and time. The Capacity Reservation is cancelled within an hour from the specified time. For example, if you specify 5/31/2019, 13:30:55, the Capacity Reservation is guaranteed to end between 13:30:55 and 14:30:55 on 5/31/2019. You must provide an EndDate value if EndDateType is limited. Omit EndDate if EndDateType is unlimited."];
      endDateType: EndDateType.t option
        [@ocaml.doc
          "Indicates the way in which the Capacity Reservation ends. A Capacity Reservation can have one of the following end types: unlimited - The Capacity Reservation remains active until you explicitly cancel it. Do not provide an EndDate value if EndDateType is unlimited. limited - The Capacity Reservation expires automatically at a specified date and time. You must provide an EndDate value if EndDateType is limited."];
      accept: Boolean.t option
        [@ocaml.doc
          "Reserved. Capacity Reservations you have created are accepted by default."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      additionalInfo: String_.t option
        [@ocaml.doc "Reserved for future use."]}
    let context_ = "ModifyCapacityReservationRequest"
    let make ?instanceCount =
      fun ?endDate ->
        fun ?endDateType ->
          fun ?accept ->
            fun ?dryRun ->
              fun ?additionalInfo ->
                fun ~capacityReservationId ->
                  fun () ->
                    {
                      instanceCount;
                      endDate;
                      endDateType;
                      accept;
                      dryRun;
                      additionalInfo;
                      capacityReservationId
                    }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("InstanceCount", (Option.map x.instanceCount ~f:Integer.to_value));
        ("EndDate", (Option.map x.endDate ~f:DateTime.to_value));
        ("EndDateType", (Option.map x.endDateType ~f:EndDateType.to_value));
        ("Accept", (Option.map x.accept ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("AdditionalInfo", (Option.map x.additionalInfo ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalInfo =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AdditionalInfo") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let accept =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Accept") in
      let endDateType =
        (Option.map ~f:EndDateType.of_xml) (Xml.child xml_arg0 "EndDateType") in
      let endDate =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "EndDate") in
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "InstanceCount") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      make ?additionalInfo ?dryRun ?accept ?endDateType ?endDate
        ?instanceCount ~capacityReservationId ()
    let of_json json =
      let additionalInfo = field_map json "AdditionalInfo" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let accept = field_map json "Accept" Boolean.of_json in
      let endDateType = field_map json "EndDateType" EndDateType.of_json in
      let endDate = field_map json "EndDate" DateTime.of_json in
      let instanceCount = field_map json "InstanceCount" Integer.of_json in
      let capacityReservationId =
        field_map_exn json "CapacityReservationId"
          CapacityReservationId.of_json in
      make ?additionalInfo ?dryRun ?accept ?endDateType ?endDate
        ?instanceCount ~capacityReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a Capacity Reservation's capacity and the conditions under which it is to be released. You cannot change a Capacity Reservation's instance type, EBS optimization, instance store settings, platform, Availability Zone, or instance eligibility. If you need to modify any of these attributes, we recommend that you cancel the Capacity Reservation, and then create a new one with the required attributes."]
module ModifyCapacityReservationFleetResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a Capacity Reservation Fleet. When you modify the total target capacity of a Capacity Reservation Fleet, the Fleet automatically creates new Capacity Reservations, or modifies or cancels existing Capacity Reservations in the Fleet to meet the new total target capacity. When you modify the end date for the Fleet, the end dates for all of the individual Capacity Reservations in the Fleet are updated accordingly."]
module ModifyCapacityReservationFleetRequest =
  struct
    type nonrec t =
      {
      capacityReservationFleetId: CapacityReservationFleetId.t
        [@ocaml.doc "The ID of the Capacity Reservation Fleet to modify."];
      totalTargetCapacity: Integer.t option
        [@ocaml.doc
          "The total number of capacity units to be reserved by the Capacity Reservation Fleet. This value, together with the instance type weights that you assign to each instance type used by the Fleet determine the number of instances for which the Fleet reserves capacity. Both values are based on units that make sense for your workload. For more information, see Total target capacity in the Amazon EC2 User Guide."];
      endDate: MillisecondDateTime.t option
        [@ocaml.doc
          "The date and time at which the Capacity Reservation Fleet expires. When the Capacity Reservation Fleet expires, its state changes to expired and all of the Capacity Reservations in the Fleet expire. The Capacity Reservation Fleet expires within an hour after the specified time. For example, if you specify 5/31/2019, 13:30:55, the Capacity Reservation Fleet is guaranteed to expire between 13:30:55 and 14:30:55 on 5/31/2019. You can't specify EndDate and RemoveEndDate in the same request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      removeEndDate: Boolean.t option
        [@ocaml.doc
          "Indicates whether to remove the end date from the Capacity Reservation Fleet. If you remove the end date, the Capacity Reservation Fleet does not expire and it remains active until you explicitly cancel it using the CancelCapacityReservationFleet action. You can't specify RemoveEndDate and EndDate in the same request."]}
    let context_ = "ModifyCapacityReservationFleetRequest"
    let make ?totalTargetCapacity =
      fun ?endDate ->
        fun ?dryRun ->
          fun ?removeEndDate ->
            fun ~capacityReservationFleetId ->
              fun () ->
                {
                  totalTargetCapacity;
                  endDate;
                  dryRun;
                  removeEndDate;
                  capacityReservationFleetId
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationFleetId",
           (Some
              (CapacityReservationFleetId.to_value
                 x.capacityReservationFleetId)));
        ("TotalTargetCapacity",
          (Option.map x.totalTargetCapacity ~f:Integer.to_value));
        ("EndDate", (Option.map x.endDate ~f:MillisecondDateTime.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("RemoveEndDate", (Option.map x.removeEndDate ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeEndDate =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RemoveEndDate") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let endDate =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "EndDate") in
      let totalTargetCapacity =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "TotalTargetCapacity") in
      let capacityReservationFleetId =
        CapacityReservationFleetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CapacityReservationFleetId") in
      make ?removeEndDate ?dryRun ?endDate ?totalTargetCapacity
        ~capacityReservationFleetId ()
    let of_json json =
      let removeEndDate = field_map json "RemoveEndDate" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let endDate = field_map json "EndDate" MillisecondDateTime.of_json in
      let totalTargetCapacity =
        field_map json "TotalTargetCapacity" Integer.of_json in
      let capacityReservationFleetId =
        field_map_exn json "CapacityReservationFleetId"
          CapacityReservationFleetId.of_json in
      make ?removeEndDate ?dryRun ?endDate ?totalTargetCapacity
        ~capacityReservationFleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a Capacity Reservation Fleet. When you modify the total target capacity of a Capacity Reservation Fleet, the Fleet automatically creates new Capacity Reservations, or modifies or cancels existing Capacity Reservations in the Fleet to meet the new total target capacity. When you modify the end date for the Fleet, the end dates for all of the individual Capacity Reservations in the Fleet are updated accordingly."]
module ModifyAvailabilityZoneGroupResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds, and an error otherwise."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the opt-in status of the Local Zone and Wavelength Zone group for your account. Use DescribeAvailabilityZones to view the value for GroupName."]
module ModifyAvailabilityZoneGroupRequest =
  struct
    type nonrec t =
      {
      groupName: String_.t
        [@ocaml.doc
          "The name of the Availability Zone group, Local Zone group, or Wavelength Zone group."];
      optInStatus: ModifyAvailabilityZoneOptInStatus.t
        [@ocaml.doc
          "Indicates whether you are opted in to the Local Zone group or Wavelength Zone group. The only valid value is opted-in. You must contact Amazon Web Services Support to opt out of a Local Zone or Wavelength Zone group."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyAvailabilityZoneGroupRequest"
    let make ?dryRun =
      fun ~groupName ->
        fun ~optInStatus -> fun () -> { dryRun; groupName; optInStatus }
    let to_value x =
      structure_to_value
        [("GroupName", (Some (String_.to_value x.groupName)));
        ("OptInStatus",
          (Some (ModifyAvailabilityZoneOptInStatus.to_value x.optInStatus)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let optInStatus =
        ModifyAvailabilityZoneOptInStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OptInStatus") in
      let groupName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      make ?dryRun ~optInStatus ~groupName ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let optInStatus =
        field_map_exn json "OptInStatus"
          ModifyAvailabilityZoneOptInStatus.of_json in
      let groupName = field_map_exn json "GroupName" String_.of_json in
      make ?dryRun ~optInStatus ~groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the opt-in status of the Local Zone and Wavelength Zone group for your account. Use DescribeAvailabilityZones to view the value for GroupName."]
module ModifyAddressAttributeResult =
  struct
    type nonrec t =
      {
      address: AddressAttribute.t option
        [@ocaml.doc "Information about the Elastic IP address."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?address = fun () -> { address }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("address", (Option.map x.address ~f:AddressAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let address =
        (Option.map ~f:AddressAttribute.of_xml)
          (Xml.child xml_arg0 "address") in
      make ?address ()
    let of_json json =
      let address = field_map json "Address" AddressAttribute.of_json in
      make ?address ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies an attribute of the specified Elastic IP address. For requirements, see Using reverse DNS for email applications."]
module ModifyAddressAttributeRequest =
  struct
    type nonrec t =
      {
      allocationId: AllocationId.t
        [@ocaml.doc "\\[EC2-VPC\\] The allocation ID."];
      domainName: String_.t option
        [@ocaml.doc "The domain name to modify for the IP address."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ModifyAddressAttributeRequest"
    let make ?domainName =
      fun ?dryRun ->
        fun ~allocationId -> fun () -> { domainName; dryRun; allocationId }
    let to_value x =
      structure_to_value
        [("AllocationId", (Some (AllocationId.to_value x.allocationId)));
        ("DomainName", (Option.map x.domainName ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let domainName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "DomainName") in
      let allocationId =
        AllocationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AllocationId") in
      make ?dryRun ?domainName ~allocationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let domainName = field_map json "DomainName" String_.of_json in
      let allocationId =
        field_map_exn json "AllocationId" AllocationId.of_json in
      make ?dryRun ?domainName ~allocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies an attribute of the specified Elastic IP address. For requirements, see Using reverse DNS for email applications."]
module ListSnapshotsInRecycleBinResult =
  struct
    type nonrec t =
      {
      snapshots: SnapshotRecycleBinInfoList.t option
        [@ocaml.doc "Information about the snapshots."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshots =
      fun ?nextToken -> fun () -> { snapshots; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotSet",
           (Option.map x.snapshots ~f:SnapshotRecycleBinInfoList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let snapshots =
        (Option.map ~f:SnapshotRecycleBinInfoList.of_xml)
          (Xml.child xml_arg0 "snapshotSet") in
      make ?nextToken ?snapshots ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let snapshots =
        field_map json "Snapshots" SnapshotRecycleBinInfoList.of_json in
      make ?nextToken ?snapshots ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists one or more snapshots that are currently in the Recycle Bin."]
module ListSnapshotsInRecycleBinRequest =
  struct
    type nonrec t =
      {
      maxResults: ListSnapshotsInRecycleBinMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      snapshotIds: SnapshotIdStringList.t option
        [@ocaml.doc
          "The IDs of the snapshots to list. Omit this parameter to list all of the snapshots that are in the Recycle Bin."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?snapshotIds ->
          fun ?dryRun ->
            fun () -> { maxResults; nextToken; snapshotIds; dryRun }
    let to_value x =
      structure_to_value
        [("MaxResults",
           (Option.map x.maxResults
              ~f:ListSnapshotsInRecycleBinMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("SnapshotId",
          (Option.map x.snapshotIds ~f:SnapshotIdStringList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let snapshotIds =
        (Option.map ~f:SnapshotIdStringList.of_xml)
          (Xml.child xml_arg0 "SnapshotId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListSnapshotsInRecycleBinMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?dryRun ?snapshotIds ?nextToken ?maxResults ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let snapshotIds =
        field_map json "SnapshotIds" SnapshotIdStringList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults"
          ListSnapshotsInRecycleBinMaxResults.of_json in
      make ?dryRun ?snapshotIds ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists one or more snapshots that are currently in the Recycle Bin."]
module ListImagesInRecycleBinResult =
  struct
    type nonrec t =
      {
      images: ImageRecycleBinInfoList.t option
        [@ocaml.doc "Information about the AMIs."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?images = fun ?nextToken -> fun () -> { images; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageSet",
           (Option.map x.images ~f:ImageRecycleBinInfoList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let images =
        (Option.map ~f:ImageRecycleBinInfoList.of_xml)
          (Xml.child xml_arg0 "imageSet") in
      make ?nextToken ?images ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let images = field_map json "Images" ImageRecycleBinInfoList.of_json in
      make ?nextToken ?images ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists one or more AMIs that are currently in the Recycle Bin. For more information, see Recycle Bin in the Amazon Elastic Compute Cloud User Guide."]
module ListImagesInRecycleBinRequest =
  struct
    type nonrec t =
      {
      imageIds: ImageIdStringList.t option
        [@ocaml.doc
          "The IDs of the AMIs to list. Omit this parameter to list all of the AMIs that are in the Recycle Bin. You can specify up to 20 IDs in a single request."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: ListImagesInRecycleBinMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value. If you do not specify a value for MaxResults, the request returns 1,000 items per page by default. For more information, see Pagination."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?imageIds =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?dryRun ->
            fun () -> { imageIds; nextToken; maxResults; dryRun }
    let to_value x =
      structure_to_value
        [("ImageId", (Option.map x.imageIds ~f:ImageIdStringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:ListImagesInRecycleBinMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:ListImagesInRecycleBinMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let imageIds =
        (Option.map ~f:ImageIdStringList.of_xml)
          (Xml.child xml_arg0 "ImageId") in
      make ?dryRun ?maxResults ?nextToken ?imageIds ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults" ListImagesInRecycleBinMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let imageIds = field_map json "ImageIds" ImageIdStringList.of_json in
      make ?dryRun ?maxResults ?nextToken ?imageIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists one or more AMIs that are currently in the Recycle Bin. For more information, see Recycle Bin in the Amazon Elastic Compute Cloud User Guide."]
module KeyPair =
  struct
    type nonrec t =
      {
      keyFingerprint: String_.t option
        [@ocaml.doc "The SHA-1 digest of the DER encoded private key."];
      keyMaterial: SensitiveUserData.t option
        [@ocaml.doc "An unencrypted PEM encoded RSA or ED25519 private key."];
      keyName: String_.t option [@ocaml.doc "The name of the key pair."];
      keyPairId: String_.t option [@ocaml.doc "The ID of the key pair."];
      tags: TagList.t option [@ocaml.doc "Any tags applied to the key pair."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?keyFingerprint =
      fun ?keyMaterial ->
        fun ?keyName ->
          fun ?keyPairId ->
            fun ?tags ->
              fun () ->
                { keyFingerprint; keyMaterial; keyName; keyPairId; tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keyFingerprint",
           (Option.map x.keyFingerprint ~f:String_.to_value));
        ("keyMaterial",
          (Option.map x.keyMaterial ~f:SensitiveUserData.to_value));
        ("keyName", (Option.map x.keyName ~f:String_.to_value));
        ("keyPairId", (Option.map x.keyPairId ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let keyPairId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyPairId") in
      let keyName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyName") in
      let keyMaterial =
        (Option.map ~f:SensitiveUserData.of_xml)
          (Xml.child xml_arg0 "keyMaterial") in
      let keyFingerprint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyFingerprint") in
      make ?tags ?keyPairId ?keyName ?keyMaterial ?keyFingerprint ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let keyPairId = field_map json "KeyPairId" String_.of_json in
      let keyName = field_map json "KeyName" String_.of_json in
      let keyMaterial =
        field_map json "KeyMaterial" SensitiveUserData.of_json in
      let keyFingerprint = field_map json "KeyFingerprint" String_.of_json in
      make ?tags ?keyPairId ?keyName ?keyMaterial ?keyFingerprint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a key pair."]
module InstanceAttribute =
  struct
    type nonrec t =
      {
      groups: GroupIdentifierList.t option
        [@ocaml.doc "The security groups associated with the instance."];
      blockDeviceMappings: InstanceBlockDeviceMappingList.t option
        [@ocaml.doc "The block device mapping of the instance."];
      disableApiTermination: AttributeBooleanValue.t option
        [@ocaml.doc
          "If the value is true, you can't terminate the instance through the Amazon EC2 console, CLI, or API; otherwise, you can."];
      enaSupport: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether enhanced networking with ENA is enabled."];
      enclaveOptions: EnclaveOptions.t option
        [@ocaml.doc
          "To enable the instance for Amazon Web Services Nitro Enclaves, set this parameter to true; otherwise, set it to false."];
      ebsOptimized: AttributeBooleanValue.t option
        [@ocaml.doc
          "Indicates whether the instance is optimized for Amazon EBS I/O."];
      instanceId: String_.t option [@ocaml.doc "The ID of the instance."];
      instanceInitiatedShutdownBehavior: AttributeValue.t option
        [@ocaml.doc
          "Indicates whether an instance stops or terminates when you initiate shutdown from the instance (using the operating system command for system shutdown)."];
      instanceType: AttributeValue.t option [@ocaml.doc "The instance type."];
      kernelId: AttributeValue.t option [@ocaml.doc "The kernel ID."];
      productCodes: ProductCodeList.t option
        [@ocaml.doc "A list of product codes."];
      ramdiskId: AttributeValue.t option [@ocaml.doc "The RAM disk ID."];
      rootDeviceName: AttributeValue.t option
        [@ocaml.doc
          "The device name of the root device volume (for example, /dev/sda1)."];
      sourceDestCheck: AttributeBooleanValue.t option
        [@ocaml.doc
          "Enable or disable source/destination checks, which ensure that the instance is either the source or the destination of any traffic that it receives. If the value is true, source/destination checks are enabled; otherwise, they are disabled. The default value is true. You must disable source/destination checks if the instance runs services such as network address translation, routing, or firewalls."];
      sriovNetSupport: AttributeValue.t option
        [@ocaml.doc
          "Indicates whether enhanced networking with the Intel 82599 Virtual Function interface is enabled."];
      userData: AttributeValue.t option [@ocaml.doc "The user data."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?groups =
      fun ?blockDeviceMappings ->
        fun ?disableApiTermination ->
          fun ?enaSupport ->
            fun ?enclaveOptions ->
              fun ?ebsOptimized ->
                fun ?instanceId ->
                  fun ?instanceInitiatedShutdownBehavior ->
                    fun ?instanceType ->
                      fun ?kernelId ->
                        fun ?productCodes ->
                          fun ?ramdiskId ->
                            fun ?rootDeviceName ->
                              fun ?sourceDestCheck ->
                                fun ?sriovNetSupport ->
                                  fun ?userData ->
                                    fun () ->
                                      {
                                        groups;
                                        blockDeviceMappings;
                                        disableApiTermination;
                                        enaSupport;
                                        enclaveOptions;
                                        ebsOptimized;
                                        instanceId;
                                        instanceInitiatedShutdownBehavior;
                                        instanceType;
                                        kernelId;
                                        productCodes;
                                        ramdiskId;
                                        rootDeviceName;
                                        sourceDestCheck;
                                        sriovNetSupport;
                                        userData
                                      }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("groupSet", (Option.map x.groups ~f:GroupIdentifierList.to_value));
        ("blockDeviceMapping",
          (Option.map x.blockDeviceMappings
             ~f:InstanceBlockDeviceMappingList.to_value));
        ("disableApiTermination",
          (Option.map x.disableApiTermination
             ~f:AttributeBooleanValue.to_value));
        ("enaSupport",
          (Option.map x.enaSupport ~f:AttributeBooleanValue.to_value));
        ("enclaveOptions",
          (Option.map x.enclaveOptions ~f:EnclaveOptions.to_value));
        ("ebsOptimized",
          (Option.map x.ebsOptimized ~f:AttributeBooleanValue.to_value));
        ("instanceId", (Option.map x.instanceId ~f:String_.to_value));
        ("instanceInitiatedShutdownBehavior",
          (Option.map x.instanceInitiatedShutdownBehavior
             ~f:AttributeValue.to_value));
        ("instanceType",
          (Option.map x.instanceType ~f:AttributeValue.to_value));
        ("kernel", (Option.map x.kernelId ~f:AttributeValue.to_value));
        ("productCodes",
          (Option.map x.productCodes ~f:ProductCodeList.to_value));
        ("ramdisk", (Option.map x.ramdiskId ~f:AttributeValue.to_value));
        ("rootDeviceName",
          (Option.map x.rootDeviceName ~f:AttributeValue.to_value));
        ("sourceDestCheck",
          (Option.map x.sourceDestCheck ~f:AttributeBooleanValue.to_value));
        ("sriovNetSupport",
          (Option.map x.sriovNetSupport ~f:AttributeValue.to_value));
        ("userData", (Option.map x.userData ~f:AttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userData =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "userData") in
      let sriovNetSupport =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "sriovNetSupport") in
      let sourceDestCheck =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "sourceDestCheck") in
      let rootDeviceName =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "rootDeviceName") in
      let ramdiskId =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "ramdisk") in
      let productCodes =
        (Option.map ~f:ProductCodeList.of_xml)
          (Xml.child xml_arg0 "productCodes") in
      let kernelId =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "kernel") in
      let instanceType =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "instanceType") in
      let instanceInitiatedShutdownBehavior =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "instanceInitiatedShutdownBehavior") in
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      let ebsOptimized =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "ebsOptimized") in
      let enclaveOptions =
        (Option.map ~f:EnclaveOptions.of_xml)
          (Xml.child xml_arg0 "enclaveOptions") in
      let enaSupport =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "enaSupport") in
      let disableApiTermination =
        (Option.map ~f:AttributeBooleanValue.of_xml)
          (Xml.child xml_arg0 "disableApiTermination") in
      let blockDeviceMappings =
        (Option.map ~f:InstanceBlockDeviceMappingList.of_xml)
          (Xml.child xml_arg0 "blockDeviceMapping") in
      let groups =
        (Option.map ~f:GroupIdentifierList.of_xml)
          (Xml.child xml_arg0 "groupSet") in
      make ?userData ?sriovNetSupport ?sourceDestCheck ?rootDeviceName
        ?ramdiskId ?productCodes ?kernelId ?instanceType
        ?instanceInitiatedShutdownBehavior ?instanceId ?ebsOptimized
        ?enclaveOptions ?enaSupport ?disableApiTermination
        ?blockDeviceMappings ?groups ()
    let of_json json =
      let userData = field_map json "UserData" AttributeValue.of_json in
      let sriovNetSupport =
        field_map json "SriovNetSupport" AttributeValue.of_json in
      let sourceDestCheck =
        field_map json "SourceDestCheck" AttributeBooleanValue.of_json in
      let rootDeviceName =
        field_map json "RootDeviceName" AttributeValue.of_json in
      let ramdiskId = field_map json "RamdiskId" AttributeValue.of_json in
      let productCodes =
        field_map json "ProductCodes" ProductCodeList.of_json in
      let kernelId = field_map json "KernelId" AttributeValue.of_json in
      let instanceType = field_map json "InstanceType" AttributeValue.of_json in
      let instanceInitiatedShutdownBehavior =
        field_map json "InstanceInitiatedShutdownBehavior"
          AttributeValue.of_json in
      let instanceId = field_map json "InstanceId" String_.of_json in
      let ebsOptimized =
        field_map json "EbsOptimized" AttributeBooleanValue.of_json in
      let enclaveOptions =
        field_map json "EnclaveOptions" EnclaveOptions.of_json in
      let enaSupport =
        field_map json "EnaSupport" AttributeBooleanValue.of_json in
      let disableApiTermination =
        field_map json "DisableApiTermination" AttributeBooleanValue.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings"
          InstanceBlockDeviceMappingList.of_json in
      let groups = field_map json "Groups" GroupIdentifierList.of_json in
      make ?userData ?sriovNetSupport ?sourceDestCheck ?rootDeviceName
        ?ramdiskId ?productCodes ?kernelId ?instanceType
        ?instanceInitiatedShutdownBehavior ?instanceId ?ebsOptimized
        ?enclaveOptions ?enaSupport ?disableApiTermination
        ?blockDeviceMappings ?groups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes an instance attribute."]
module ImportVolumeResult =
  struct
    type nonrec t =
      {
      conversionTask: ConversionTask.t option
        [@ocaml.doc "Information about the conversion task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?conversionTask = fun () -> { conversionTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("conversionTask",
           (Option.map x.conversionTask ~f:ConversionTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conversionTask =
        (Option.map ~f:ConversionTask.of_xml)
          (Xml.child xml_arg0 "conversionTask") in
      make ?conversionTask ()
    let of_json json =
      let conversionTask =
        field_map json "ConversionTask" ConversionTask.of_json in
      make ?conversionTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an import volume task using metadata from the specified disk image. This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage instead. To import a disk to a snapshot, use ImportSnapshot instead. This API action is not supported by the Command Line Interface (CLI). For information about using the Amazon EC2 CLI, which is deprecated, see Importing Disks to Amazon EBS in the Amazon EC2 CLI Reference PDF file. For information about the import manifest referenced by this API action, see VM Import Manifest."]
module ImportVolumeRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t
        [@ocaml.doc "The Availability Zone for the resulting EBS volume."];
      description: String_.t option
        [@ocaml.doc "A description of the volume."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      image: DiskImageDetail.t [@ocaml.doc "The disk image."];
      volume: VolumeDetail.t [@ocaml.doc "The volume size."]}
    let context_ = "ImportVolumeRequest"
    let make ?description =
      fun ?dryRun ->
        fun ~availabilityZone ->
          fun ~image ->
            fun ~volume ->
              fun () ->
                { description; dryRun; availabilityZone; image; volume }
    let to_value x =
      structure_to_value
        [("availabilityZone", (Some (String_.to_value x.availabilityZone)));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("image", (Some (DiskImageDetail.to_value x.image)));
        ("volume", (Some (VolumeDetail.to_value x.volume)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volume =
        VolumeDetail.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "volume") in
      let image =
        DiskImageDetail.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "image") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let availabilityZone =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "availabilityZone") in
      make ~volume ~image ?dryRun ?description ~availabilityZone ()
    let of_json json =
      let volume = field_map_exn json "Volume" VolumeDetail.of_json in
      let image = field_map_exn json "Image" DiskImageDetail.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let availabilityZone =
        field_map_exn json "AvailabilityZone" String_.of_json in
      make ~volume ~image ?dryRun ?description ~availabilityZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an import volume task using metadata from the specified disk image. This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage instead. To import a disk to a snapshot, use ImportSnapshot instead. This API action is not supported by the Command Line Interface (CLI). For information about using the Amazon EC2 CLI, which is deprecated, see Importing Disks to Amazon EBS in the Amazon EC2 CLI Reference PDF file. For information about the import manifest referenced by this API action, see VM Import Manifest."]
module ImportSnapshotResult =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description of the import snapshot task."];
      importTaskId: String_.t option
        [@ocaml.doc "The ID of the import snapshot task."];
      snapshotTaskDetail: SnapshotTaskDetail.t option
        [@ocaml.doc "Information about the import snapshot task."];
      tags: TagList.t option
        [@ocaml.doc "Any tags assigned to the import snapshot task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?description =
      fun ?importTaskId ->
        fun ?snapshotTaskDetail ->
          fun ?tags ->
            fun () -> { description; importTaskId; snapshotTaskDetail; tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("importTaskId", (Option.map x.importTaskId ~f:String_.to_value));
        ("snapshotTaskDetail",
          (Option.map x.snapshotTaskDetail ~f:SnapshotTaskDetail.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let snapshotTaskDetail =
        (Option.map ~f:SnapshotTaskDetail.of_xml)
          (Xml.child xml_arg0 "snapshotTaskDetail") in
      let importTaskId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "importTaskId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ?tags ?snapshotTaskDetail ?importTaskId ?description ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let snapshotTaskDetail =
        field_map json "SnapshotTaskDetail" SnapshotTaskDetail.of_json in
      let importTaskId = field_map json "ImportTaskId" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?tags ?snapshotTaskDetail ?importTaskId ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports a disk into an EBS snapshot. For more information, see Importing a disk as a snapshot using VM Import/Export in the VM Import/Export User Guide."]
module ImportSnapshotRequest =
  struct
    type nonrec t =
      {
      clientData: ClientData.t option
        [@ocaml.doc "The client-specific data."];
      clientToken: String_.t option
        [@ocaml.doc "Token to enable idempotency for VM import requests."];
      description: String_.t option
        [@ocaml.doc "The description string for the import snapshot task."];
      diskContainer: SnapshotDiskContainer.t option
        [@ocaml.doc "Information about the disk container."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      encrypted: Boolean.t option
        [@ocaml.doc
          "Specifies whether the destination snapshot of the imported image should be encrypted. The default KMS key for EBS is used unless you specify a non-default KMS key using KmsKeyId. For more information, see Amazon EBS Encryption in the Amazon Elastic Compute Cloud User Guide."];
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "An identifier for the symmetric KMS key to use when creating the encrypted snapshot. This parameter is only required if you want to use a non-default KMS key; if this parameter is not specified, the default KMS key for EBS is used. If a KmsKeyId is specified, the Encrypted flag must also be set. The KMS key identifier may be provided in any of the following formats: Key ID Key alias. The alias ARN contains the arn:aws:kms namespace, followed by the Region of the key, the Amazon Web Services account ID of the key owner, the alias namespace, and then the key alias. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. ARN using key ID. The ID ARN contains the arn:aws:kms namespace, followed by the Region of the key, the Amazon Web Services account ID of the key owner, the key namespace, and then the key ID. For example, arn:aws:kms:us-east-1:012345678910:key/abcd1234-a123-456a-a12b-a123b4cd56ef. ARN using key alias. The alias ARN contains the arn:aws:kms namespace, followed by the Region of the key, the Amazon Web Services account ID of the key owner, the alias namespace, and then the key alias. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. Amazon Web Services parses KmsKeyId asynchronously, meaning that the action you call may appear to complete even though you provided an invalid identifier. This action will eventually report failure. The specified KMS key must exist in the Region that the snapshot is being copied to. Amazon EBS does not support asymmetric KMS keys."];
      roleName: String_.t option
        [@ocaml.doc
          "The name of the role to use when not using the default role, 'vmimport'."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the import snapshot task during creation."]}
    let make ?clientData =
      fun ?clientToken ->
        fun ?description ->
          fun ?diskContainer ->
            fun ?dryRun ->
              fun ?encrypted ->
                fun ?kmsKeyId ->
                  fun ?roleName ->
                    fun ?tagSpecifications ->
                      fun () ->
                        {
                          clientData;
                          clientToken;
                          description;
                          diskContainer;
                          dryRun;
                          encrypted;
                          kmsKeyId;
                          roleName;
                          tagSpecifications
                        }
    let to_value x =
      structure_to_value
        [("ClientData", (Option.map x.clientData ~f:ClientData.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DiskContainer",
          (Option.map x.diskContainer ~f:SnapshotDiskContainer.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("KmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("RoleName", (Option.map x.roleName ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let roleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RoleName") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "KmsKeyId") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Encrypted") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let diskContainer =
        (Option.map ~f:SnapshotDiskContainer.of_xml)
          (Xml.child xml_arg0 "DiskContainer") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let clientData =
        (Option.map ~f:ClientData.of_xml) (Xml.child xml_arg0 "ClientData") in
      make ?tagSpecifications ?roleName ?kmsKeyId ?encrypted ?dryRun
        ?diskContainer ?description ?clientToken ?clientData ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let roleName = field_map json "RoleName" String_.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let diskContainer =
        field_map json "DiskContainer" SnapshotDiskContainer.of_json in
      let description = field_map json "Description" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let clientData = field_map json "ClientData" ClientData.of_json in
      make ?tagSpecifications ?roleName ?kmsKeyId ?encrypted ?dryRun
        ?diskContainer ?description ?clientToken ?clientData ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports a disk into an EBS snapshot. For more information, see Importing a disk as a snapshot using VM Import/Export in the VM Import/Export User Guide."]
module ImportKeyPairResult =
  struct
    type nonrec t =
      {
      keyFingerprint: String_.t option
        [@ocaml.doc
          "The MD5 public key fingerprint as specified in section 4 of RFC 4716."];
      keyName: String_.t option
        [@ocaml.doc "The key pair name that you provided."];
      keyPairId: String_.t option
        [@ocaml.doc "The ID of the resulting key pair."];
      tags: TagList.t option
        [@ocaml.doc "The tags applied to the imported key pair."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?keyFingerprint =
      fun ?keyName ->
        fun ?keyPairId ->
          fun ?tags -> fun () -> { keyFingerprint; keyName; keyPairId; tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keyFingerprint",
           (Option.map x.keyFingerprint ~f:String_.to_value));
        ("keyName", (Option.map x.keyName ~f:String_.to_value));
        ("keyPairId", (Option.map x.keyPairId ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let keyPairId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyPairId") in
      let keyName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyName") in
      let keyFingerprint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyFingerprint") in
      make ?tags ?keyPairId ?keyName ?keyFingerprint ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let keyPairId = field_map json "KeyPairId" String_.of_json in
      let keyName = field_map json "KeyName" String_.of_json in
      let keyFingerprint = field_map json "KeyFingerprint" String_.of_json in
      make ?tags ?keyPairId ?keyName ?keyFingerprint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the public key from an RSA or ED25519 key pair that you created with a third-party tool. Compare this with CreateKeyPair, in which Amazon Web Services creates the key pair and gives the keys to you (Amazon Web Services keeps a copy of the public key). With ImportKeyPair, you create the key pair and give Amazon Web Services just the public key. The private key is never transferred between you and Amazon Web Services. For more information about key pairs, see Amazon EC2 key pairs in the Amazon Elastic Compute Cloud User Guide."]
module ImportKeyPairRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      keyName: String_.t [@ocaml.doc "A unique name for the key pair."];
      publicKeyMaterial: Blob.t
        [@ocaml.doc
          "The public key. For API calls, the text must be base64-encoded. For command line tools, base64 encoding is performed for you."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the imported key pair."]}
    let context_ = "ImportKeyPairRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ~keyName ->
          fun ~publicKeyMaterial ->
            fun () ->
              { dryRun; tagSpecifications; keyName; publicKeyMaterial }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("keyName", (Some (String_.to_value x.keyName)));
        ("publicKeyMaterial", (Some (Blob.to_value x.publicKeyMaterial)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let publicKeyMaterial =
        Blob.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "publicKeyMaterial") in
      let keyName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "keyName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tagSpecifications ~publicKeyMaterial ~keyName ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let publicKeyMaterial =
        field_map_exn json "PublicKeyMaterial" Blob.of_json in
      let keyName = field_map_exn json "KeyName" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ~publicKeyMaterial ~keyName ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the public key from an RSA or ED25519 key pair that you created with a third-party tool. Compare this with CreateKeyPair, in which Amazon Web Services creates the key pair and gives the keys to you (Amazon Web Services keeps a copy of the public key). With ImportKeyPair, you create the key pair and give Amazon Web Services just the public key. The private key is never transferred between you and Amazon Web Services. For more information about key pairs, see Amazon EC2 key pairs in the Amazon Elastic Compute Cloud User Guide."]
module ImportInstanceResult =
  struct
    type nonrec t =
      {
      conversionTask: ConversionTask.t option
        [@ocaml.doc "Information about the conversion task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?conversionTask = fun () -> { conversionTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("conversionTask",
           (Option.map x.conversionTask ~f:ConversionTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conversionTask =
        (Option.map ~f:ConversionTask.of_xml)
          (Xml.child xml_arg0 "conversionTask") in
      make ?conversionTask ()
    let of_json json =
      let conversionTask =
        field_map json "ConversionTask" ConversionTask.of_json in
      make ?conversionTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an import instance task using metadata from the specified disk image. This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage instead. This API action is not supported by the Command Line Interface (CLI). For information about using the Amazon EC2 CLI, which is deprecated, see Importing a VM to Amazon EC2 in the Amazon EC2 CLI Reference PDF file. For information about the import manifest referenced by this API action, see VM Import Manifest."]
module ImportInstanceRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description for the instance being imported."];
      diskImages: DiskImageList.t option [@ocaml.doc "The disk image."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      launchSpecification: ImportInstanceLaunchSpecification.t option
        [@ocaml.doc "The launch specification."];
      platform: PlatformValues.t
        [@ocaml.doc "The instance operating system."]}
    let context_ = "ImportInstanceRequest"
    let make ?description =
      fun ?diskImages ->
        fun ?dryRun ->
          fun ?launchSpecification ->
            fun ~platform ->
              fun () ->
                {
                  description;
                  diskImages;
                  dryRun;
                  launchSpecification;
                  platform
                }
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("diskImage", (Option.map x.diskImages ~f:DiskImageList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("launchSpecification",
          (Option.map x.launchSpecification
             ~f:ImportInstanceLaunchSpecification.to_value));
        ("platform", (Some (PlatformValues.to_value x.platform)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let platform =
        PlatformValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "platform") in
      let launchSpecification =
        (Option.map ~f:ImportInstanceLaunchSpecification.of_xml)
          (Xml.child xml_arg0 "launchSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let diskImages =
        (Option.map ~f:DiskImageList.of_xml) (Xml.child xml_arg0 "diskImage") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ~platform ?launchSpecification ?dryRun ?diskImages ?description ()
    let of_json json =
      let platform = field_map_exn json "Platform" PlatformValues.of_json in
      let launchSpecification =
        field_map json "LaunchSpecification"
          ImportInstanceLaunchSpecification.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let diskImages = field_map json "DiskImages" DiskImageList.of_json in
      let description = field_map json "Description" String_.of_json in
      make ~platform ?launchSpecification ?dryRun ?diskImages ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an import instance task using metadata from the specified disk image. This API action supports only single-volume VMs. To import multi-volume VMs, use ImportImage instead. This API action is not supported by the Command Line Interface (CLI). For information about using the Amazon EC2 CLI, which is deprecated, see Importing a VM to Amazon EC2 in the Amazon EC2 CLI Reference PDF file. For information about the import manifest referenced by this API action, see VM Import Manifest."]
module ImportImageResult =
  struct
    type nonrec t =
      {
      architecture: String_.t option
        [@ocaml.doc "The architecture of the virtual machine."];
      description: String_.t option
        [@ocaml.doc "A description of the import task."];
      encrypted: Boolean.t option
        [@ocaml.doc "Indicates whether the AMI is encrypted."];
      hypervisor: String_.t option
        [@ocaml.doc "The target hypervisor of the import task."];
      imageId: String_.t option
        [@ocaml.doc
          "The ID of the Amazon Machine Image (AMI) created by the import task."];
      importTaskId: ImportImageTaskId.t option
        [@ocaml.doc "The task ID of the import image task."];
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "The identifier for the symmetric KMS key that was used to create the encrypted AMI."];
      licenseType: String_.t option
        [@ocaml.doc "The license type of the virtual machine."];
      platform: String_.t option
        [@ocaml.doc "The operating system of the virtual machine."];
      progress: String_.t option [@ocaml.doc "The progress of the task."];
      snapshotDetails: SnapshotDetailList.t option
        [@ocaml.doc "Information about the snapshots."];
      status: String_.t option [@ocaml.doc "A brief status of the task."];
      statusMessage: String_.t option
        [@ocaml.doc "A detailed status message of the import task."];
      licenseSpecifications:
        ImportImageLicenseSpecificationListResponse.t option
        [@ocaml.doc "The ARNs of the license configurations."];
      tags: TagList.t option
        [@ocaml.doc "Any tags assigned to the import image task."];
      usageOperation: String_.t option
        [@ocaml.doc "The usage operation value."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?architecture =
      fun ?description ->
        fun ?encrypted ->
          fun ?hypervisor ->
            fun ?imageId ->
              fun ?importTaskId ->
                fun ?kmsKeyId ->
                  fun ?licenseType ->
                    fun ?platform ->
                      fun ?progress ->
                        fun ?snapshotDetails ->
                          fun ?status ->
                            fun ?statusMessage ->
                              fun ?licenseSpecifications ->
                                fun ?tags ->
                                  fun ?usageOperation ->
                                    fun () ->
                                      {
                                        architecture;
                                        description;
                                        encrypted;
                                        hypervisor;
                                        imageId;
                                        importTaskId;
                                        kmsKeyId;
                                        licenseType;
                                        platform;
                                        progress;
                                        snapshotDetails;
                                        status;
                                        statusMessage;
                                        licenseSpecifications;
                                        tags;
                                        usageOperation
                                      }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("architecture", (Option.map x.architecture ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("hypervisor", (Option.map x.hypervisor ~f:String_.to_value));
        ("imageId", (Option.map x.imageId ~f:String_.to_value));
        ("importTaskId",
          (Option.map x.importTaskId ~f:ImportImageTaskId.to_value));
        ("kmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("licenseType", (Option.map x.licenseType ~f:String_.to_value));
        ("platform", (Option.map x.platform ~f:String_.to_value));
        ("progress", (Option.map x.progress ~f:String_.to_value));
        ("snapshotDetailSet",
          (Option.map x.snapshotDetails ~f:SnapshotDetailList.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("licenseSpecifications",
          (Option.map x.licenseSpecifications
             ~f:ImportImageLicenseSpecificationListResponse.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value));
        ("usageOperation", (Option.map x.usageOperation ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let usageOperation =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "usageOperation") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let licenseSpecifications =
        (Option.map ~f:ImportImageLicenseSpecificationListResponse.of_xml)
          (Xml.child xml_arg0 "licenseSpecifications") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let snapshotDetails =
        (Option.map ~f:SnapshotDetailList.of_xml)
          (Xml.child xml_arg0 "snapshotDetailSet") in
      let progress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "progress") in
      let platform =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platform") in
      let licenseType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "licenseType") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      let importTaskId =
        (Option.map ~f:ImportImageTaskId.of_xml)
          (Xml.child xml_arg0 "importTaskId") in
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      let hypervisor =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "hypervisor") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let architecture =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "architecture") in
      make ?usageOperation ?tags ?licenseSpecifications ?statusMessage
        ?status ?snapshotDetails ?progress ?platform ?licenseType ?kmsKeyId
        ?importTaskId ?imageId ?hypervisor ?encrypted ?description
        ?architecture ()
    let of_json json =
      let usageOperation = field_map json "UsageOperation" String_.of_json in
      let tags = field_map json "Tags" TagList.of_json in
      let licenseSpecifications =
        field_map json "LicenseSpecifications"
          ImportImageLicenseSpecificationListResponse.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" String_.of_json in
      let snapshotDetails =
        field_map json "SnapshotDetails" SnapshotDetailList.of_json in
      let progress = field_map json "Progress" String_.of_json in
      let platform = field_map json "Platform" String_.of_json in
      let licenseType = field_map json "LicenseType" String_.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      let importTaskId =
        field_map json "ImportTaskId" ImportImageTaskId.of_json in
      let imageId = field_map json "ImageId" String_.of_json in
      let hypervisor = field_map json "Hypervisor" String_.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let architecture = field_map json "Architecture" String_.of_json in
      make ?usageOperation ?tags ?licenseSpecifications ?statusMessage
        ?status ?snapshotDetails ?progress ?platform ?licenseType ?kmsKeyId
        ?importTaskId ?imageId ?hypervisor ?encrypted ?description
        ?architecture ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Import single or multi-volume disk images or EBS snapshots into an Amazon Machine Image (AMI). For more information, see Importing a VM as an image using VM Import/Export in the VM Import/Export User Guide."]
module ImportImageRequest =
  struct
    type nonrec t =
      {
      architecture: String_.t option
        [@ocaml.doc
          "The architecture of the virtual machine. Valid values: i386 | x86_64"];
      clientData: ClientData.t option
        [@ocaml.doc "The client-specific data."];
      clientToken: String_.t option
        [@ocaml.doc
          "The token to enable idempotency for VM import requests."];
      description: String_.t option
        [@ocaml.doc "A description string for the import image task."];
      diskContainers: ImageDiskContainerList.t option
        [@ocaml.doc "Information about the disk containers."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      encrypted: Boolean.t option
        [@ocaml.doc
          "Specifies whether the destination AMI of the imported image should be encrypted. The default KMS key for EBS is used unless you specify a non-default KMS key using KmsKeyId. For more information, see Amazon EBS Encryption in the Amazon Elastic Compute Cloud User Guide."];
      hypervisor: String_.t option
        [@ocaml.doc "The target hypervisor platform. Valid values: xen"];
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "An identifier for the symmetric KMS key to use when creating the encrypted AMI. This parameter is only required if you want to use a non-default KMS key; if this parameter is not specified, the default KMS key for EBS is used. If a KmsKeyId is specified, the Encrypted flag must also be set. The KMS key identifier may be provided in any of the following formats: Key ID Key alias. The alias ARN contains the arn:aws:kms namespace, followed by the Region of the key, the Amazon Web Services account ID of the key owner, the alias namespace, and then the key alias. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. ARN using key ID. The ID ARN contains the arn:aws:kms namespace, followed by the Region of the key, the Amazon Web Services account ID of the key owner, the key namespace, and then the key ID. For example, arn:aws:kms:us-east-1:012345678910:key/abcd1234-a123-456a-a12b-a123b4cd56ef. ARN using key alias. The alias ARN contains the arn:aws:kms namespace, followed by the Region of the key, the Amazon Web Services account ID of the key owner, the alias namespace, and then the key alias. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. Amazon Web Services parses KmsKeyId asynchronously, meaning that the action you call may appear to complete even though you provided an invalid identifier. This action will eventually report failure. The specified KMS key must exist in the Region that the AMI is being copied to. Amazon EBS does not support asymmetric KMS keys."];
      licenseType: String_.t option
        [@ocaml.doc
          "The license type to be used for the Amazon Machine Image (AMI) after importing. By default, we detect the source-system operating system (OS) and apply the appropriate license. Specify AWS to replace the source-system license with an Amazon Web Services license, if appropriate. Specify BYOL to retain the source-system license, if appropriate. To use BYOL, you must have existing licenses with rights to use these licenses in a third party cloud, such as Amazon Web Services. For more information, see Prerequisites in the VM Import/Export User Guide."];
      platform: String_.t option
        [@ocaml.doc
          "The operating system of the virtual machine. Valid values: Windows | Linux"];
      roleName: String_.t option
        [@ocaml.doc
          "The name of the role to use when not using the default role, 'vmimport'."];
      licenseSpecifications:
        ImportImageLicenseSpecificationListRequest.t option
        [@ocaml.doc "The ARNs of the license configurations."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the import image task during creation."];
      usageOperation: String_.t option
        [@ocaml.doc
          "The usage operation value. For more information, see Licensing options in the VM Import/Export User Guide."];
      bootMode: BootModeValues.t option
        [@ocaml.doc "The boot mode of the virtual machine."]}
    let make ?architecture =
      fun ?clientData ->
        fun ?clientToken ->
          fun ?description ->
            fun ?diskContainers ->
              fun ?dryRun ->
                fun ?encrypted ->
                  fun ?hypervisor ->
                    fun ?kmsKeyId ->
                      fun ?licenseType ->
                        fun ?platform ->
                          fun ?roleName ->
                            fun ?licenseSpecifications ->
                              fun ?tagSpecifications ->
                                fun ?usageOperation ->
                                  fun ?bootMode ->
                                    fun () ->
                                      {
                                        architecture;
                                        clientData;
                                        clientToken;
                                        description;
                                        diskContainers;
                                        dryRun;
                                        encrypted;
                                        hypervisor;
                                        kmsKeyId;
                                        licenseType;
                                        platform;
                                        roleName;
                                        licenseSpecifications;
                                        tagSpecifications;
                                        usageOperation;
                                        bootMode
                                      }
    let to_value x =
      structure_to_value
        [("Architecture", (Option.map x.architecture ~f:String_.to_value));
        ("ClientData", (Option.map x.clientData ~f:ClientData.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DiskContainer",
          (Option.map x.diskContainers ~f:ImageDiskContainerList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("Hypervisor", (Option.map x.hypervisor ~f:String_.to_value));
        ("KmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("LicenseType", (Option.map x.licenseType ~f:String_.to_value));
        ("Platform", (Option.map x.platform ~f:String_.to_value));
        ("RoleName", (Option.map x.roleName ~f:String_.to_value));
        ("LicenseSpecifications",
          (Option.map x.licenseSpecifications
             ~f:ImportImageLicenseSpecificationListRequest.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("UsageOperation", (Option.map x.usageOperation ~f:String_.to_value));
        ("BootMode", (Option.map x.bootMode ~f:BootModeValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bootMode =
        (Option.map ~f:BootModeValues.of_xml) (Xml.child xml_arg0 "BootMode") in
      let usageOperation =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UsageOperation") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let licenseSpecifications =
        (Option.map ~f:ImportImageLicenseSpecificationListRequest.of_xml)
          (Xml.child xml_arg0 "LicenseSpecifications") in
      let roleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RoleName") in
      let platform =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Platform") in
      let licenseType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LicenseType") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "KmsKeyId") in
      let hypervisor =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Hypervisor") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Encrypted") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let diskContainers =
        (Option.map ~f:ImageDiskContainerList.of_xml)
          (Xml.child xml_arg0 "DiskContainer") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let clientData =
        (Option.map ~f:ClientData.of_xml) (Xml.child xml_arg0 "ClientData") in
      let architecture =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Architecture") in
      make ?bootMode ?usageOperation ?tagSpecifications
        ?licenseSpecifications ?roleName ?platform ?licenseType ?kmsKeyId
        ?hypervisor ?encrypted ?dryRun ?diskContainers ?description
        ?clientToken ?clientData ?architecture ()
    let of_json json =
      let bootMode = field_map json "BootMode" BootModeValues.of_json in
      let usageOperation = field_map json "UsageOperation" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let licenseSpecifications =
        field_map json "LicenseSpecifications"
          ImportImageLicenseSpecificationListRequest.of_json in
      let roleName = field_map json "RoleName" String_.of_json in
      let platform = field_map json "Platform" String_.of_json in
      let licenseType = field_map json "LicenseType" String_.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      let hypervisor = field_map json "Hypervisor" String_.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let diskContainers =
        field_map json "DiskContainers" ImageDiskContainerList.of_json in
      let description = field_map json "Description" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let clientData = field_map json "ClientData" ClientData.of_json in
      let architecture = field_map json "Architecture" String_.of_json in
      make ?bootMode ?usageOperation ?tagSpecifications
        ?licenseSpecifications ?roleName ?platform ?licenseType ?kmsKeyId
        ?hypervisor ?encrypted ?dryRun ?diskContainers ?description
        ?clientToken ?clientData ?architecture ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Import single or multi-volume disk images or EBS snapshots into an Amazon Machine Image (AMI). For more information, see Importing a VM as an image using VM Import/Export in the VM Import/Export User Guide."]
module ImportClientVpnClientCertificateRevocationListResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Uploads a client certificate revocation list to the specified Client VPN endpoint. Uploading a client certificate revocation list overwrites the existing client certificate revocation list. Uploading a client certificate revocation list resets existing client connections."]
module ImportClientVpnClientCertificateRevocationListRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc
          "The ID of the Client VPN endpoint to which the client certificate revocation list applies."];
      certificateRevocationList: String_.t
        [@ocaml.doc
          "The client certificate revocation list file. For more information, see Generate a Client Certificate Revocation List in the Client VPN Administrator Guide."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ImportClientVpnClientCertificateRevocationListRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId ->
        fun ~certificateRevocationList ->
          fun () ->
            { dryRun; clientVpnEndpointId; certificateRevocationList }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("CertificateRevocationList",
          (Some (String_.to_value x.certificateRevocationList)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let certificateRevocationList =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CertificateRevocationList") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~certificateRevocationList ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let certificateRevocationList =
        field_map_exn json "CertificateRevocationList" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~certificateRevocationList ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Uploads a client certificate revocation list to the specified Client VPN endpoint. Uploading a client certificate revocation list overwrites the existing client certificate revocation list. Uploading a client certificate revocation list resets existing client connections."]
module ImageAttribute =
  struct
    type nonrec t =
      {
      blockDeviceMappings: BlockDeviceMappingList.t option
        [@ocaml.doc "The block device mapping entries."];
      imageId: String_.t option [@ocaml.doc "The ID of the AMI."];
      launchPermissions: LaunchPermissionList.t option
        [@ocaml.doc "The launch permissions."];
      productCodes: ProductCodeList.t option
        [@ocaml.doc "The product codes."];
      description: AttributeValue.t option
        [@ocaml.doc "A description for the AMI."];
      kernelId: AttributeValue.t option [@ocaml.doc "The kernel ID."];
      ramdiskId: AttributeValue.t option [@ocaml.doc "The RAM disk ID."];
      sriovNetSupport: AttributeValue.t option
        [@ocaml.doc
          "Indicates whether enhanced networking with the Intel 82599 Virtual Function interface is enabled."];
      bootMode: AttributeValue.t option [@ocaml.doc "The boot mode."];
      lastLaunchedTime: AttributeValue.t option
        [@ocaml.doc
          "The date and time, in ISO 8601 date-time format, when the AMI was last used to launch an EC2 instance. When the AMI is used, there is a 24-hour delay before that usage is reported. lastLaunchedTime data is available starting April 2017."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?blockDeviceMappings =
      fun ?imageId ->
        fun ?launchPermissions ->
          fun ?productCodes ->
            fun ?description ->
              fun ?kernelId ->
                fun ?ramdiskId ->
                  fun ?sriovNetSupport ->
                    fun ?bootMode ->
                      fun ?lastLaunchedTime ->
                        fun () ->
                          {
                            blockDeviceMappings;
                            imageId;
                            launchPermissions;
                            productCodes;
                            description;
                            kernelId;
                            ramdiskId;
                            sriovNetSupport;
                            bootMode;
                            lastLaunchedTime
                          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("blockDeviceMapping",
           (Option.map x.blockDeviceMappings
              ~f:BlockDeviceMappingList.to_value));
        ("imageId", (Option.map x.imageId ~f:String_.to_value));
        ("launchPermission",
          (Option.map x.launchPermissions ~f:LaunchPermissionList.to_value));
        ("productCodes",
          (Option.map x.productCodes ~f:ProductCodeList.to_value));
        ("description",
          (Option.map x.description ~f:AttributeValue.to_value));
        ("kernel", (Option.map x.kernelId ~f:AttributeValue.to_value));
        ("ramdisk", (Option.map x.ramdiskId ~f:AttributeValue.to_value));
        ("sriovNetSupport",
          (Option.map x.sriovNetSupport ~f:AttributeValue.to_value));
        ("bootMode", (Option.map x.bootMode ~f:AttributeValue.to_value));
        ("lastLaunchedTime",
          (Option.map x.lastLaunchedTime ~f:AttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastLaunchedTime =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "lastLaunchedTime") in
      let bootMode =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "bootMode") in
      let sriovNetSupport =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "sriovNetSupport") in
      let ramdiskId =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "ramdisk") in
      let kernelId =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "kernel") in
      let description =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "description") in
      let productCodes =
        (Option.map ~f:ProductCodeList.of_xml)
          (Xml.child xml_arg0 "productCodes") in
      let launchPermissions =
        (Option.map ~f:LaunchPermissionList.of_xml)
          (Xml.child xml_arg0 "launchPermission") in
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappingList.of_xml)
          (Xml.child xml_arg0 "blockDeviceMapping") in
      make ?lastLaunchedTime ?bootMode ?sriovNetSupport ?ramdiskId ?kernelId
        ?description ?productCodes ?launchPermissions ?imageId
        ?blockDeviceMappings ()
    let of_json json =
      let lastLaunchedTime =
        field_map json "LastLaunchedTime" AttributeValue.of_json in
      let bootMode = field_map json "BootMode" AttributeValue.of_json in
      let sriovNetSupport =
        field_map json "SriovNetSupport" AttributeValue.of_json in
      let ramdiskId = field_map json "RamdiskId" AttributeValue.of_json in
      let kernelId = field_map json "KernelId" AttributeValue.of_json in
      let description = field_map json "Description" AttributeValue.of_json in
      let productCodes =
        field_map json "ProductCodes" ProductCodeList.of_json in
      let launchPermissions =
        field_map json "LaunchPermissions" LaunchPermissionList.of_json in
      let imageId = field_map json "ImageId" String_.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings" BlockDeviceMappingList.of_json in
      make ?lastLaunchedTime ?bootMode ?sriovNetSupport ?ramdiskId ?kernelId
        ?description ?productCodes ?launchPermissions ?imageId
        ?blockDeviceMappings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes an image attribute."]
module GetVpnConnectionDeviceTypesResult =
  struct
    type nonrec t =
      {
      vpnConnectionDeviceTypes: VpnConnectionDeviceTypeList.t option
        [@ocaml.doc
          "List of customer gateway devices that have a sample configuration file available for use."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The NextToken value to include in a future GetVpnConnectionDeviceTypes request. When the results of a GetVpnConnectionDeviceTypes request exceed MaxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnectionDeviceTypes =
      fun ?nextToken -> fun () -> { vpnConnectionDeviceTypes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnectionDeviceTypeSet",
           (Option.map x.vpnConnectionDeviceTypes
              ~f:VpnConnectionDeviceTypeList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let vpnConnectionDeviceTypes =
        (Option.map ~f:VpnConnectionDeviceTypeList.of_xml)
          (Xml.child xml_arg0 "vpnConnectionDeviceTypeSet") in
      make ?nextToken ?vpnConnectionDeviceTypes ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let vpnConnectionDeviceTypes =
        field_map json "VpnConnectionDeviceTypes"
          VpnConnectionDeviceTypeList.of_json in
      make ?nextToken ?vpnConnectionDeviceTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Obtain a list of customer gateway devices for which sample configuration files can be provided. The request has no additional parameters. You can also see the list of device types with sample configuration files available under Your customer gateway device in the Amazon Web Services Site-to-Site VPN User Guide."]
module GetVpnConnectionDeviceTypesRequest =
  struct
    type nonrec t =
      {
      maxResults: GVCDMaxResults.t option
        [@ocaml.doc
          "The maximum number of results returned by GetVpnConnectionDeviceTypes in paginated output. When this parameter is used, GetVpnConnectionDeviceTypes only returns MaxResults results in a single page along with a NextToken response element. The remaining results of the initial request can be seen by sending another GetVpnConnectionDeviceTypes request with the returned NextToken value. This value can be between 200 and 1000. If this parameter is not used, then GetVpnConnectionDeviceTypes returns all results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The NextToken value returned from a previous paginated GetVpnConnectionDeviceTypes request where MaxResults was used and the results exceeded the value of that parameter. Pagination continues from the end of the previous results that returned the NextToken value. This value is null when there are no more results to return."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?dryRun -> fun () -> { maxResults; nextToken; dryRun }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:GVCDMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:GVCDMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?dryRun ?nextToken ?maxResults ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" GVCDMaxResults.of_json in
      make ?dryRun ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Obtain a list of customer gateway devices for which sample configuration files can be provided. The request has no additional parameters. You can also see the list of device types with sample configuration files available under Your customer gateway device in the Amazon Web Services Site-to-Site VPN User Guide."]
module GetVpnConnectionDeviceSampleConfigurationResult =
  struct
    type nonrec t =
      {
      vpnConnectionDeviceSampleConfiguration:
        VpnConnectionDeviceSampleConfiguration.t option
        [@ocaml.doc
          "Sample configuration file for the specified customer gateway device."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnectionDeviceSampleConfiguration =
      fun () -> { vpnConnectionDeviceSampleConfiguration }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnectionDeviceSampleConfiguration",
           (Option.map x.vpnConnectionDeviceSampleConfiguration
              ~f:VpnConnectionDeviceSampleConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnectionDeviceSampleConfiguration =
        (Option.map ~f:VpnConnectionDeviceSampleConfiguration.of_xml)
          (Xml.child xml_arg0 "vpnConnectionDeviceSampleConfiguration") in
      make ?vpnConnectionDeviceSampleConfiguration ()
    let of_json json =
      let vpnConnectionDeviceSampleConfiguration =
        field_map json "VpnConnectionDeviceSampleConfiguration"
          VpnConnectionDeviceSampleConfiguration.of_json in
      make ?vpnConnectionDeviceSampleConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Download an Amazon Web Services-provided sample configuration file to be used with the customer gateway device specified for your Site-to-Site VPN connection."]
module GetVpnConnectionDeviceSampleConfigurationRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc
          "The VpnConnectionId specifies the Site-to-Site VPN connection used for the sample configuration."];
      vpnConnectionDeviceTypeId: VpnConnectionDeviceTypeId.t
        [@ocaml.doc
          "Device identifier provided by the GetVpnConnectionDeviceTypes API."];
      internetKeyExchangeVersion: String_.t option
        [@ocaml.doc
          "The IKE version to be used in the sample configuration file for your customer gateway device. You can specify one of the following versions: ikev1 or ikev2."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetVpnConnectionDeviceSampleConfigurationRequest"
    let make ?internetKeyExchangeVersion =
      fun ?dryRun ->
        fun ~vpnConnectionId ->
          fun ~vpnConnectionDeviceTypeId ->
            fun () ->
              {
                internetKeyExchangeVersion;
                dryRun;
                vpnConnectionId;
                vpnConnectionDeviceTypeId
              }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("VpnConnectionDeviceTypeId",
          (Some
             (VpnConnectionDeviceTypeId.to_value x.vpnConnectionDeviceTypeId)));
        ("InternetKeyExchangeVersion",
          (Option.map x.internetKeyExchangeVersion ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let internetKeyExchangeVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "InternetKeyExchangeVersion") in
      let vpnConnectionDeviceTypeId =
        VpnConnectionDeviceTypeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VpnConnectionDeviceTypeId") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ?internetKeyExchangeVersion ~vpnConnectionDeviceTypeId
        ~vpnConnectionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let internetKeyExchangeVersion =
        field_map json "InternetKeyExchangeVersion" String_.of_json in
      let vpnConnectionDeviceTypeId =
        field_map_exn json "VpnConnectionDeviceTypeId"
          VpnConnectionDeviceTypeId.of_json in
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ?internetKeyExchangeVersion ~vpnConnectionDeviceTypeId
        ~vpnConnectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Download an Amazon Web Services-provided sample configuration file to be used with the customer gateway device specified for your Site-to-Site VPN connection."]
module GetTransitGatewayRouteTablePropagationsResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTablePropagations:
        TransitGatewayRouteTablePropagationList.t option
        [@ocaml.doc "Information about the route table propagations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTablePropagations =
      fun ?nextToken ->
        fun () -> { transitGatewayRouteTablePropagations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTablePropagations",
           (Option.map x.transitGatewayRouteTablePropagations
              ~f:TransitGatewayRouteTablePropagationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayRouteTablePropagations =
        (Option.map ~f:TransitGatewayRouteTablePropagationList.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTablePropagations") in
      make ?nextToken ?transitGatewayRouteTablePropagations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayRouteTablePropagations =
        field_map json "TransitGatewayRouteTablePropagations"
          TransitGatewayRouteTablePropagationList.of_json in
      make ?nextToken ?transitGatewayRouteTablePropagations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the route table propagations for the specified transit gateway route table."]
module GetTransitGatewayRouteTablePropagationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: resource-id - The ID of the resource. resource-type - The resource type. Valid values are vpc | vpn | direct-connect-gateway | peering | connect. transit-gateway-attachment-id - The ID of the attachment."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetTransitGatewayRouteTablePropagationsRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~transitGatewayRouteTableId ->
              fun () ->
                {
                  filters;
                  maxResults;
                  nextToken;
                  dryRun;
                  transitGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the route table propagations for the specified transit gateway route table."]
module GetTransitGatewayRouteTableAssociationsResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayRouteTableAssociationList.t option
        [@ocaml.doc "Information about the associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations =
      fun ?nextToken -> fun () -> { associations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayRouteTableAssociationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let associations =
        (Option.map ~f:TransitGatewayRouteTableAssociationList.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?nextToken ?associations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let associations =
        field_map json "Associations"
          TransitGatewayRouteTableAssociationList.of_json in
      make ?nextToken ?associations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the associations for the specified transit gateway route table."]
module GetTransitGatewayRouteTableAssociationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: resource-id - The ID of the resource. resource-type - The resource type. Valid values are vpc | vpn | direct-connect-gateway | peering | connect. transit-gateway-attachment-id - The ID of the attachment."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetTransitGatewayRouteTableAssociationsRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~transitGatewayRouteTableId ->
              fun () ->
                {
                  filters;
                  maxResults;
                  nextToken;
                  dryRun;
                  transitGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the associations for the specified transit gateway route table."]
module GetTransitGatewayPrefixListReferencesResult =
  struct
    type nonrec t =
      {
      transitGatewayPrefixListReferences:
        TransitGatewayPrefixListReferenceSet.t option
        [@ocaml.doc "Information about the prefix list references."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPrefixListReferences =
      fun ?nextToken ->
        fun () -> { transitGatewayPrefixListReferences; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPrefixListReferenceSet",
           (Option.map x.transitGatewayPrefixListReferences
              ~f:TransitGatewayPrefixListReferenceSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayPrefixListReferences =
        (Option.map ~f:TransitGatewayPrefixListReferenceSet.of_xml)
          (Xml.child xml_arg0 "transitGatewayPrefixListReferenceSet") in
      make ?nextToken ?transitGatewayPrefixListReferences ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayPrefixListReferences =
        field_map json "TransitGatewayPrefixListReferences"
          TransitGatewayPrefixListReferenceSet.of_json in
      make ?nextToken ?transitGatewayPrefixListReferences ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the prefix list references in a specified transit gateway route table."]
module GetTransitGatewayPrefixListReferencesRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: attachment.resource-id - The ID of the resource for the attachment. attachment.resource-type - The type of resource for the attachment. Valid values are vpc | vpn | direct-connect-gateway | peering. attachment.transit-gateway-attachment-id - The ID of the attachment. is-blackhole - Whether traffic matching the route is blocked (true | false). prefix-list-id - The ID of the prefix list. prefix-list-owner-id - The ID of the owner of the prefix list. state - The state of the prefix list reference (pending | available | modifying | deleting)."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetTransitGatewayPrefixListReferencesRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~transitGatewayRouteTableId ->
              fun () ->
                {
                  filters;
                  maxResults;
                  nextToken;
                  dryRun;
                  transitGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the prefix list references in a specified transit gateway route table."]
module GetTransitGatewayMulticastDomainAssociationsResult =
  struct
    type nonrec t =
      {
      multicastDomainAssociations:
        TransitGatewayMulticastDomainAssociationList.t option
        [@ocaml.doc "Information about the multicast domain associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?multicastDomainAssociations =
      fun ?nextToken -> fun () -> { multicastDomainAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("multicastDomainAssociations",
           (Option.map x.multicastDomainAssociations
              ~f:TransitGatewayMulticastDomainAssociationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let multicastDomainAssociations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociationList.of_xml)
          (Xml.child xml_arg0 "multicastDomainAssociations") in
      make ?nextToken ?multicastDomainAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let multicastDomainAssociations =
        field_map json "MulticastDomainAssociations"
          TransitGatewayMulticastDomainAssociationList.of_json in
      make ?nextToken ?multicastDomainAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the associations for the transit gateway multicast domain."]
module GetTransitGatewayMulticastDomainAssociationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: resource-id - The ID of the resource. resource-type - The type of resource. The valid value is: vpc. state - The state of the subnet association. Valid values are associated | associating | disassociated | disassociating. subnet-id - The ID of the subnet. transit-gateway-attachment-id - The id of the transit gateway attachment."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?dryRun ->
              fun () ->
                {
                  transitGatewayMulticastDomainId;
                  filters;
                  maxResults;
                  nextToken;
                  dryRun
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the associations for the transit gateway multicast domain."]
module GetTransitGatewayAttachmentPropagationsResult =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentPropagations:
        TransitGatewayAttachmentPropagationList.t option
        [@ocaml.doc "Information about the propagation route tables."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayAttachmentPropagations =
      fun ?nextToken ->
        fun () -> { transitGatewayAttachmentPropagations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayAttachmentPropagations",
           (Option.map x.transitGatewayAttachmentPropagations
              ~f:TransitGatewayAttachmentPropagationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transitGatewayAttachmentPropagations =
        (Option.map ~f:TransitGatewayAttachmentPropagationList.of_xml)
          (Xml.child xml_arg0 "transitGatewayAttachmentPropagations") in
      make ?nextToken ?transitGatewayAttachmentPropagations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let transitGatewayAttachmentPropagations =
        field_map json "TransitGatewayAttachmentPropagations"
          TransitGatewayAttachmentPropagationList.of_json in
      make ?nextToken ?transitGatewayAttachmentPropagations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the route tables to which the specified resource attachment propagates routes."]
module GetTransitGatewayAttachmentPropagationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the attachment."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: transit-gateway-route-table-id - The ID of the transit gateway route table."];
      maxResults: TransitGatewayMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetTransitGatewayAttachmentPropagationsRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~transitGatewayAttachmentId ->
              fun () ->
                {
                  filters;
                  maxResults;
                  nextToken;
                  dryRun;
                  transitGatewayAttachmentId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:TransitGatewayMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:TransitGatewayMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" TransitGatewayMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters
        ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the route tables to which the specified resource attachment propagates routes."]
module GetSubnetCidrReservationsResult =
  struct
    type nonrec t =
      {
      subnetIpv4CidrReservations: SubnetCidrReservationList.t option
        [@ocaml.doc "Information about the IPv4 subnet CIDR reservations."];
      subnetIpv6CidrReservations: SubnetCidrReservationList.t option
        [@ocaml.doc "Information about the IPv6 subnet CIDR reservations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnetIpv4CidrReservations =
      fun ?subnetIpv6CidrReservations ->
        fun ?nextToken ->
          fun () ->
            {
              subnetIpv4CidrReservations;
              subnetIpv6CidrReservations;
              nextToken
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnetIpv4CidrReservationSet",
           (Option.map x.subnetIpv4CidrReservations
              ~f:SubnetCidrReservationList.to_value));
        ("subnetIpv6CidrReservationSet",
          (Option.map x.subnetIpv6CidrReservations
             ~f:SubnetCidrReservationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let subnetIpv6CidrReservations =
        (Option.map ~f:SubnetCidrReservationList.of_xml)
          (Xml.child xml_arg0 "subnetIpv6CidrReservationSet") in
      let subnetIpv4CidrReservations =
        (Option.map ~f:SubnetCidrReservationList.of_xml)
          (Xml.child xml_arg0 "subnetIpv4CidrReservationSet") in
      make ?nextToken ?subnetIpv6CidrReservations ?subnetIpv4CidrReservations
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let subnetIpv6CidrReservations =
        field_map json "SubnetIpv6CidrReservations"
          SubnetCidrReservationList.of_json in
      let subnetIpv4CidrReservations =
        field_map json "SubnetIpv4CidrReservations"
          SubnetCidrReservationList.of_json in
      make ?nextToken ?subnetIpv6CidrReservations ?subnetIpv4CidrReservations
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the subnet CIDR reservations."]
module GetSubnetCidrReservationsRequest =
  struct
    type nonrec t =
      {
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. reservationType - The type of reservation (prefix | explicit). subnet-id - The ID of the subnet. tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value. tag-key - The key of a tag assigned to the resource. Use this filter to find all resources assigned a tag with a specific key, regardless of the tag value."];
      subnetId: SubnetId.t [@ocaml.doc "The ID of the subnet."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: GetSubnetCidrReservationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."]}
    let context_ = "GetSubnetCidrReservationsRequest"
    let make ?filters =
      fun ?dryRun ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ~subnetId ->
              fun () -> { filters; dryRun; nextToken; maxResults; subnetId }
    let to_value x =
      structure_to_value
        [("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:GetSubnetCidrReservationsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:GetSubnetCidrReservationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      make ?maxResults ?nextToken ?dryRun ~subnetId ?filters ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          GetSubnetCidrReservationsMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      make ?maxResults ?nextToken ?dryRun ~subnetId ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the subnet CIDR reservations."]
module GetSpotPlacementScoresResult =
  struct
    type nonrec t =
      {
      spotPlacementScores: SpotPlacementScores.t option
        [@ocaml.doc
          "The Spot placement score for the top 10 Regions or Availability Zones, scored on a scale from 1 to 10. Each score&#x2028; reflects how likely it is that each Region or Availability Zone will succeed at fulfilling the specified target capacity&#x2028; at the time of the Spot placement score request. A score of 10 means that your Spot capacity request is highly likely to succeed in that Region or Availability Zone. If you request a Spot placement score for Regions, a high score assumes that your fleet request will be configured to use all Availability Zones and the capacity-optimized allocation strategy. If you request a Spot placement score for Availability Zones, a high score assumes that your fleet request will be configured to use a single Availability Zone and the capacity-optimized allocation strategy. Different&#x2028; Regions or Availability Zones might return the same score. The Spot placement score serves as a recommendation only. No score guarantees that your Spot request will be fully or partially fulfilled."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next set of results."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotPlacementScores =
      fun ?nextToken -> fun () -> { spotPlacementScores; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotPlacementScoreSet",
           (Option.map x.spotPlacementScores ~f:SpotPlacementScores.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let spotPlacementScores =
        (Option.map ~f:SpotPlacementScores.of_xml)
          (Xml.child xml_arg0 "spotPlacementScoreSet") in
      make ?nextToken ?spotPlacementScores ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let spotPlacementScores =
        field_map json "SpotPlacementScores" SpotPlacementScores.of_json in
      make ?nextToken ?spotPlacementScores ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Calculates the Spot placement score for a Region or Availability Zone based on the specified target capacity and compute requirements. You can specify your compute requirements either by using InstanceRequirementsWithMetadata and letting Amazon EC2 choose the optimal instance types to fulfill your Spot request, or you can specify the instance types by using InstanceTypes. For more information, see Spot placement score in the Amazon EC2 User Guide."]
module GetSpotPlacementScoresRequest =
  struct
    type nonrec t =
      {
      instanceTypes: InstanceTypes.t option
        [@ocaml.doc
          "The instance types. We recommend that you specify at least three instance types. If you specify one or two instance types, or specify variations of a single instance type (for example, an m3.xlarge with and without instance storage), the returned placement score will always be low. If you specify InstanceTypes, you can't specify InstanceRequirementsWithMetadata."];
      targetCapacity: SpotPlacementScoresTargetCapacity.t
        [@ocaml.doc "The target capacity."];
      targetCapacityUnitType: TargetCapacityUnitType.t option
        [@ocaml.doc
          "The unit for the target capacity. Default: units (translates to number of instances)"];
      singleAvailabilityZone: Boolean.t option
        [@ocaml.doc
          "Specify true so that the response returns a list of scored Availability Zones. Otherwise, the response returns a list of scored Regions. A list of scored Availability Zones is useful if you want to launch all of your Spot capacity into a single Availability Zone."];
      regionNames: RegionNames.t option
        [@ocaml.doc
          "The Regions used to narrow down the list of Regions to be scored. Enter the Region code, for example, us-east-1."];
      instanceRequirementsWithMetadata:
        InstanceRequirementsWithMetadataRequest.t option
        [@ocaml.doc
          "The attributes for the instance types. When you specify instance attributes, Amazon EC2 will identify instance types with those attributes. If you specify InstanceRequirementsWithMetadata, you can't specify InstanceTypes."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      maxResults: SpotPlacementScoresMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. Specify a value between 1 and&#x2028; 1000. The default value is 1000. To retrieve the remaining results, make another call with&#x2028; the returned NextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next set of results."]}
    let context_ = "GetSpotPlacementScoresRequest"
    let make ?instanceTypes =
      fun ?targetCapacityUnitType ->
        fun ?singleAvailabilityZone ->
          fun ?regionNames ->
            fun ?instanceRequirementsWithMetadata ->
              fun ?dryRun ->
                fun ?maxResults ->
                  fun ?nextToken ->
                    fun ~targetCapacity ->
                      fun () ->
                        {
                          instanceTypes;
                          targetCapacityUnitType;
                          singleAvailabilityZone;
                          regionNames;
                          instanceRequirementsWithMetadata;
                          dryRun;
                          maxResults;
                          nextToken;
                          targetCapacity
                        }
    let to_value x =
      structure_to_value
        [("InstanceType",
           (Option.map x.instanceTypes ~f:InstanceTypes.to_value));
        ("TargetCapacity",
          (Some (SpotPlacementScoresTargetCapacity.to_value x.targetCapacity)));
        ("TargetCapacityUnitType",
          (Option.map x.targetCapacityUnitType
             ~f:TargetCapacityUnitType.to_value));
        ("SingleAvailabilityZone",
          (Option.map x.singleAvailabilityZone ~f:Boolean.to_value));
        ("RegionName", (Option.map x.regionNames ~f:RegionNames.to_value));
        ("InstanceRequirementsWithMetadata",
          (Option.map x.instanceRequirementsWithMetadata
             ~f:InstanceRequirementsWithMetadataRequest.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:SpotPlacementScoresMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:SpotPlacementScoresMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let instanceRequirementsWithMetadata =
        (Option.map ~f:InstanceRequirementsWithMetadataRequest.of_xml)
          (Xml.child xml_arg0 "InstanceRequirementsWithMetadata") in
      let regionNames =
        (Option.map ~f:RegionNames.of_xml) (Xml.child xml_arg0 "RegionName") in
      let singleAvailabilityZone =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SingleAvailabilityZone") in
      let targetCapacityUnitType =
        (Option.map ~f:TargetCapacityUnitType.of_xml)
          (Xml.child xml_arg0 "TargetCapacityUnitType") in
      let targetCapacity =
        SpotPlacementScoresTargetCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetCapacity") in
      let instanceTypes =
        (Option.map ~f:InstanceTypes.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      make ?nextToken ?maxResults ?dryRun ?instanceRequirementsWithMetadata
        ?regionNames ?singleAvailabilityZone ?targetCapacityUnitType
        ~targetCapacity ?instanceTypes ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults =
        field_map json "MaxResults" SpotPlacementScoresMaxResults.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceRequirementsWithMetadata =
        field_map json "InstanceRequirementsWithMetadata"
          InstanceRequirementsWithMetadataRequest.of_json in
      let regionNames = field_map json "RegionNames" RegionNames.of_json in
      let singleAvailabilityZone =
        field_map json "SingleAvailabilityZone" Boolean.of_json in
      let targetCapacityUnitType =
        field_map json "TargetCapacityUnitType"
          TargetCapacityUnitType.of_json in
      let targetCapacity =
        field_map_exn json "TargetCapacity"
          SpotPlacementScoresTargetCapacity.of_json in
      let instanceTypes =
        field_map json "InstanceTypes" InstanceTypes.of_json in
      make ?nextToken ?maxResults ?dryRun ?instanceRequirementsWithMetadata
        ?regionNames ?singleAvailabilityZone ?targetCapacityUnitType
        ~targetCapacity ?instanceTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Calculates the Spot placement score for a Region or Availability Zone based on the specified target capacity and compute requirements. You can specify your compute requirements either by using InstanceRequirementsWithMetadata and letting Amazon EC2 choose the optimal instance types to fulfill your Spot request, or you can specify the instance types by using InstanceTypes. For more information, see Spot placement score in the Amazon EC2 User Guide."]
module GetSerialConsoleAccessStatusResult =
  struct
    type nonrec t =
      {
      serialConsoleAccessEnabled: Boolean.t option
        [@ocaml.doc
          "If true, access to the EC2 serial console of all instances is enabled for your account. If false, access to the EC2 serial console of all instances is disabled for your account."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serialConsoleAccessEnabled =
      fun () -> { serialConsoleAccessEnabled }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serialConsoleAccessEnabled",
           (Option.map x.serialConsoleAccessEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serialConsoleAccessEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "serialConsoleAccessEnabled") in
      make ?serialConsoleAccessEnabled ()
    let of_json json =
      let serialConsoleAccessEnabled =
        field_map json "SerialConsoleAccessEnabled" Boolean.of_json in
      make ?serialConsoleAccessEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the access status of your account to the EC2 serial console of all instances. By default, access to the EC2 serial console is disabled for your account. For more information, see Manage account access to the EC2 serial console in the Amazon EC2 User Guide."]
module GetSerialConsoleAccessStatusRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the access status of your account to the EC2 serial console of all instances. By default, access to the EC2 serial console is disabled for your account. For more information, see Manage account access to the EC2 serial console in the Amazon EC2 User Guide."]
module GetReservedInstancesExchangeQuoteResult =
  struct
    type nonrec t =
      {
      currencyCode: String_.t option
        [@ocaml.doc "The currency of the transaction."];
      isValidExchange: Boolean.t option
        [@ocaml.doc
          "If true, the exchange is valid. If false, the exchange cannot be completed."];
      outputReservedInstancesWillExpireAt: DateTime.t option
        [@ocaml.doc "The new end date of the reservation term."];
      paymentDue: String_.t option
        [@ocaml.doc "The total true upfront charge for the exchange."];
      reservedInstanceValueRollup: ReservationValue.t option
        [@ocaml.doc "The cost associated with the Reserved Instance."];
      reservedInstanceValueSet: ReservedInstanceReservationValueSet.t option
        [@ocaml.doc
          "The configuration of your Convertible Reserved Instances."];
      targetConfigurationValueRollup: ReservationValue.t option
        [@ocaml.doc "The cost associated with the Reserved Instance."];
      targetConfigurationValueSet: TargetReservationValueSet.t option
        [@ocaml.doc
          "The values of the target Convertible Reserved Instances."];
      validationFailureReason: String_.t option
        [@ocaml.doc
          "Describes the reason why the exchange cannot be completed."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?currencyCode =
      fun ?isValidExchange ->
        fun ?outputReservedInstancesWillExpireAt ->
          fun ?paymentDue ->
            fun ?reservedInstanceValueRollup ->
              fun ?reservedInstanceValueSet ->
                fun ?targetConfigurationValueRollup ->
                  fun ?targetConfigurationValueSet ->
                    fun ?validationFailureReason ->
                      fun () ->
                        {
                          currencyCode;
                          isValidExchange;
                          outputReservedInstancesWillExpireAt;
                          paymentDue;
                          reservedInstanceValueRollup;
                          reservedInstanceValueSet;
                          targetConfigurationValueRollup;
                          targetConfigurationValueSet;
                          validationFailureReason
                        }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("currencyCode", (Option.map x.currencyCode ~f:String_.to_value));
        ("isValidExchange",
          (Option.map x.isValidExchange ~f:Boolean.to_value));
        ("outputReservedInstancesWillExpireAt",
          (Option.map x.outputReservedInstancesWillExpireAt
             ~f:DateTime.to_value));
        ("paymentDue", (Option.map x.paymentDue ~f:String_.to_value));
        ("reservedInstanceValueRollup",
          (Option.map x.reservedInstanceValueRollup
             ~f:ReservationValue.to_value));
        ("reservedInstanceValueSet",
          (Option.map x.reservedInstanceValueSet
             ~f:ReservedInstanceReservationValueSet.to_value));
        ("targetConfigurationValueRollup",
          (Option.map x.targetConfigurationValueRollup
             ~f:ReservationValue.to_value));
        ("targetConfigurationValueSet",
          (Option.map x.targetConfigurationValueSet
             ~f:TargetReservationValueSet.to_value));
        ("validationFailureReason",
          (Option.map x.validationFailureReason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationFailureReason =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "validationFailureReason") in
      let targetConfigurationValueSet =
        (Option.map ~f:TargetReservationValueSet.of_xml)
          (Xml.child xml_arg0 "targetConfigurationValueSet") in
      let targetConfigurationValueRollup =
        (Option.map ~f:ReservationValue.of_xml)
          (Xml.child xml_arg0 "targetConfigurationValueRollup") in
      let reservedInstanceValueSet =
        (Option.map ~f:ReservedInstanceReservationValueSet.of_xml)
          (Xml.child xml_arg0 "reservedInstanceValueSet") in
      let reservedInstanceValueRollup =
        (Option.map ~f:ReservationValue.of_xml)
          (Xml.child xml_arg0 "reservedInstanceValueRollup") in
      let paymentDue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentDue") in
      let outputReservedInstancesWillExpireAt =
        (Option.map ~f:DateTime.of_xml)
          (Xml.child xml_arg0 "outputReservedInstancesWillExpireAt") in
      let isValidExchange =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isValidExchange") in
      let currencyCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currencyCode") in
      make ?validationFailureReason ?targetConfigurationValueSet
        ?targetConfigurationValueRollup ?reservedInstanceValueSet
        ?reservedInstanceValueRollup ?paymentDue
        ?outputReservedInstancesWillExpireAt ?isValidExchange ?currencyCode
        ()
    let of_json json =
      let validationFailureReason =
        field_map json "ValidationFailureReason" String_.of_json in
      let targetConfigurationValueSet =
        field_map json "TargetConfigurationValueSet"
          TargetReservationValueSet.of_json in
      let targetConfigurationValueRollup =
        field_map json "TargetConfigurationValueRollup"
          ReservationValue.of_json in
      let reservedInstanceValueSet =
        field_map json "ReservedInstanceValueSet"
          ReservedInstanceReservationValueSet.of_json in
      let reservedInstanceValueRollup =
        field_map json "ReservedInstanceValueRollup" ReservationValue.of_json in
      let paymentDue = field_map json "PaymentDue" String_.of_json in
      let outputReservedInstancesWillExpireAt =
        field_map json "OutputReservedInstancesWillExpireAt" DateTime.of_json in
      let isValidExchange = field_map json "IsValidExchange" Boolean.of_json in
      let currencyCode = field_map json "CurrencyCode" String_.of_json in
      make ?validationFailureReason ?targetConfigurationValueSet
        ?targetConfigurationValueRollup ?reservedInstanceValueSet
        ?reservedInstanceValueRollup ?paymentDue
        ?outputReservedInstancesWillExpireAt ?isValidExchange ?currencyCode
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of GetReservedInstancesExchangeQuote."]
module GetReservedInstancesExchangeQuoteRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      reservedInstanceIds: ReservedInstanceIdSet.t
        [@ocaml.doc
          "The IDs of the Convertible Reserved Instances to exchange."];
      targetConfigurations: TargetConfigurationRequestSet.t option
        [@ocaml.doc
          "The configuration of the target Convertible Reserved Instance to exchange for your current Convertible Reserved Instances."]}
    let context_ = "GetReservedInstancesExchangeQuoteRequest"
    let make ?dryRun =
      fun ?targetConfigurations ->
        fun ~reservedInstanceIds ->
          fun () -> { dryRun; targetConfigurations; reservedInstanceIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ReservedInstanceId",
          (Some (ReservedInstanceIdSet.to_value x.reservedInstanceIds)));
        ("TargetConfiguration",
          (Option.map x.targetConfigurations
             ~f:TargetConfigurationRequestSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetConfigurations =
        (Option.map ~f:TargetConfigurationRequestSet.of_xml)
          (Xml.child xml_arg0 "TargetConfiguration") in
      let reservedInstanceIds =
        ReservedInstanceIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservedInstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?targetConfigurations ~reservedInstanceIds ?dryRun ()
    let of_json json =
      let targetConfigurations =
        field_map json "TargetConfigurations"
          TargetConfigurationRequestSet.of_json in
      let reservedInstanceIds =
        field_map_exn json "ReservedInstanceIds"
          ReservedInstanceIdSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?targetConfigurations ~reservedInstanceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for GetReservedInstanceExchangeQuote."]
module GetPasswordDataResult =
  struct
    type nonrec t =
      {
      instanceId: String_.t option
        [@ocaml.doc "The ID of the Windows instance."];
      passwordData: String_.t option
        [@ocaml.doc
          "The password of the instance. Returns an empty string if the password is not available."];
      timestamp: DateTime.t option
        [@ocaml.doc "The time the data was last updated."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceId =
      fun ?passwordData ->
        fun ?timestamp -> fun () -> { instanceId; passwordData; timestamp }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceId", (Option.map x.instanceId ~f:String_.to_value));
        ("passwordData", (Option.map x.passwordData ~f:String_.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "timestamp") in
      let passwordData =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "passwordData") in
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      make ?timestamp ?passwordData ?instanceId ()
    let of_json json =
      let timestamp = field_map json "Timestamp" DateTime.of_json in
      let passwordData = field_map json "PasswordData" String_.of_json in
      let instanceId = field_map json "InstanceId" String_.of_json in
      make ?timestamp ?passwordData ?instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the encrypted administrator password for a running Windows instance. The Windows password is generated at boot by the EC2Config service or EC2Launch scripts (Windows Server 2016 and later). This usually only happens the first time an instance is launched. For more information, see EC2Config and EC2Launch in the Amazon EC2 User Guide. For the EC2Config service, the password is not generated for rebundled AMIs unless Ec2SetPassword is enabled before bundling. The password is encrypted using the key pair that you specified when you launched the instance. You must provide the corresponding key pair file. When you launch an instance, password generation and encryption may take a few minutes. If you try to retrieve the password before it's available, the output returns an empty string. We recommend that you wait up to 15 minutes after launching an instance before trying to retrieve the generated password."]
module GetPasswordDataRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t [@ocaml.doc "The ID of the Windows instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetPasswordDataRequest"
    let make ?dryRun = fun ~instanceId -> fun () -> { dryRun; instanceId }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~instanceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?dryRun ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the encrypted administrator password for a running Windows instance. The Windows password is generated at boot by the EC2Config service or EC2Launch scripts (Windows Server 2016 and later). This usually only happens the first time an instance is launched. For more information, see EC2Config and EC2Launch in the Amazon EC2 User Guide. For the EC2Config service, the password is not generated for rebundled AMIs unless Ec2SetPassword is enabled before bundling. The password is encrypted using the key pair that you specified when you launched the instance. You must provide the corresponding key pair file. When you launch an instance, password generation and encryption may take a few minutes. If you try to retrieve the password before it's available, the output returns an empty string. We recommend that you wait up to 15 minutes after launching an instance before trying to retrieve the generated password."]
module GetNetworkInsightsAccessScopeContentResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeContent:
        NetworkInsightsAccessScopeContent.t option
        [@ocaml.doc "The Network Access Scope content."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeContent =
      fun () -> { networkInsightsAccessScopeContent }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeContent",
           (Option.map x.networkInsightsAccessScopeContent
              ~f:NetworkInsightsAccessScopeContent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeContent =
        (Option.map ~f:NetworkInsightsAccessScopeContent.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeContent") in
      make ?networkInsightsAccessScopeContent ()
    let of_json json =
      let networkInsightsAccessScopeContent =
        field_map json "NetworkInsightsAccessScopeContent"
          NetworkInsightsAccessScopeContent.of_json in
      make ?networkInsightsAccessScopeContent ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the content for the specified Network Access Scope."]
module GetNetworkInsightsAccessScopeContentRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t
        [@ocaml.doc "The ID of the Network Access Scope."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetNetworkInsightsAccessScopeContentRequest"
    let make ?dryRun =
      fun ~networkInsightsAccessScopeId ->
        fun () -> { dryRun; networkInsightsAccessScopeId }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeId",
           (Some
              (NetworkInsightsAccessScopeId.to_value
                 x.networkInsightsAccessScopeId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInsightsAccessScopeId =
        NetworkInsightsAccessScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeId") in
      make ?dryRun ~networkInsightsAccessScopeId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInsightsAccessScopeId =
        field_map_exn json "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      make ?dryRun ~networkInsightsAccessScopeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the content for the specified Network Access Scope."]
module GetNetworkInsightsAccessScopeAnalysisFindingsResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisId:
        NetworkInsightsAccessScopeAnalysisId.t option
        [@ocaml.doc "The ID of the Network Access Scope analysis."];
      analysisStatus: AnalysisStatus.t option
        [@ocaml.doc "The status of Network Access Scope Analysis."];
      analysisFindings: AccessScopeAnalysisFindingList.t option
        [@ocaml.doc
          "The findings associated with Network Access Scope Analysis."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeAnalysisId =
      fun ?analysisStatus ->
        fun ?analysisFindings ->
          fun ?nextToken ->
            fun () ->
              {
                networkInsightsAccessScopeAnalysisId;
                analysisStatus;
                analysisFindings;
                nextToken
              }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeAnalysisId",
           (Option.map x.networkInsightsAccessScopeAnalysisId
              ~f:NetworkInsightsAccessScopeAnalysisId.to_value));
        ("analysisStatus",
          (Option.map x.analysisStatus ~f:AnalysisStatus.to_value));
        ("analysisFindingSet",
          (Option.map x.analysisFindings
             ~f:AccessScopeAnalysisFindingList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let analysisFindings =
        (Option.map ~f:AccessScopeAnalysisFindingList.of_xml)
          (Xml.child xml_arg0 "analysisFindingSet") in
      let analysisStatus =
        (Option.map ~f:AnalysisStatus.of_xml)
          (Xml.child xml_arg0 "analysisStatus") in
      let networkInsightsAccessScopeAnalysisId =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeAnalysisId") in
      make ?nextToken ?analysisFindings ?analysisStatus
        ?networkInsightsAccessScopeAnalysisId ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let analysisFindings =
        field_map json "AnalysisFindings"
          AccessScopeAnalysisFindingList.of_json in
      let analysisStatus =
        field_map json "AnalysisStatus" AnalysisStatus.of_json in
      let networkInsightsAccessScopeAnalysisId =
        field_map json "NetworkInsightsAccessScopeAnalysisId"
          NetworkInsightsAccessScopeAnalysisId.of_json in
      make ?nextToken ?analysisFindings ?analysisStatus
        ?networkInsightsAccessScopeAnalysisId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the findings for the specified Network Access Scope analysis."]
module GetNetworkInsightsAccessScopeAnalysisFindingsRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisId:
        NetworkInsightsAccessScopeAnalysisId.t
        [@ocaml.doc "The ID of the Network Access Scope analysis."];
      maxResults: NetworkInsightsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetNetworkInsightsAccessScopeAnalysisFindingsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?dryRun ->
          fun ~networkInsightsAccessScopeAnalysisId ->
            fun () ->
              {
                maxResults;
                nextToken;
                dryRun;
                networkInsightsAccessScopeAnalysisId
              }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeAnalysisId",
           (Some
              (NetworkInsightsAccessScopeAnalysisId.to_value
                 x.networkInsightsAccessScopeAnalysisId)));
        ("MaxResults",
          (Option.map x.maxResults ~f:NetworkInsightsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:NetworkInsightsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let networkInsightsAccessScopeAnalysisId =
        NetworkInsightsAccessScopeAnalysisId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeAnalysisId") in
      make ?dryRun ?nextToken ?maxResults
        ~networkInsightsAccessScopeAnalysisId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" NetworkInsightsMaxResults.of_json in
      let networkInsightsAccessScopeAnalysisId =
        field_map_exn json "NetworkInsightsAccessScopeAnalysisId"
          NetworkInsightsAccessScopeAnalysisId.of_json in
      make ?dryRun ?nextToken ?maxResults
        ~networkInsightsAccessScopeAnalysisId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the findings for the specified Network Access Scope analysis."]
module GetManagedPrefixListEntriesResult =
  struct
    type nonrec t =
      {
      entries: PrefixListEntrySet.t option
        [@ocaml.doc "Information about the prefix list entries."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?entries = fun ?nextToken -> fun () -> { entries; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("entrySet", (Option.map x.entries ~f:PrefixListEntrySet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let entries =
        (Option.map ~f:PrefixListEntrySet.of_xml)
          (Xml.child xml_arg0 "entrySet") in
      make ?nextToken ?entries ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let entries = field_map json "Entries" PrefixListEntrySet.of_json in
      make ?nextToken ?entries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the entries for a specified managed prefix list."]
module GetManagedPrefixListEntriesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."];
      targetVersion: Long.t option
        [@ocaml.doc
          "The version of the prefix list for which to return the entries. The default is the current version."];
      maxResults: PrefixListMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "GetManagedPrefixListEntriesRequest"
    let make ?dryRun =
      fun ?targetVersion ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ~prefixListId ->
              fun () ->
                { dryRun; targetVersion; maxResults; nextToken; prefixListId
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("TargetVersion", (Option.map x.targetVersion ~f:Long.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:PrefixListMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:PrefixListMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let targetVersion =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "TargetVersion") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?targetVersion ~prefixListId ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" PrefixListMaxResults.of_json in
      let targetVersion = field_map json "TargetVersion" Long.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?targetVersion ~prefixListId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the entries for a specified managed prefix list."]
module GetManagedPrefixListAssociationsResult =
  struct
    type nonrec t =
      {
      prefixListAssociations: PrefixListAssociationSet.t option
        [@ocaml.doc "Information about the associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixListAssociations =
      fun ?nextToken -> fun () -> { prefixListAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixListAssociationSet",
           (Option.map x.prefixListAssociations
              ~f:PrefixListAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let prefixListAssociations =
        (Option.map ~f:PrefixListAssociationSet.of_xml)
          (Xml.child xml_arg0 "prefixListAssociationSet") in
      make ?nextToken ?prefixListAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let prefixListAssociations =
        field_map json "PrefixListAssociations"
          PrefixListAssociationSet.of_json in
      make ?nextToken ?prefixListAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the resources that are associated with the specified managed prefix list."]
module GetManagedPrefixListAssociationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc "The ID of the prefix list."];
      maxResults: GetManagedPrefixListAssociationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "GetManagedPrefixListAssociationsRequest"
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~prefixListId ->
            fun () -> { dryRun; maxResults; nextToken; prefixListId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:GetManagedPrefixListAssociationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:GetManagedPrefixListAssociationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ~prefixListId ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults"
          GetManagedPrefixListAssociationsMaxResults.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ~prefixListId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the resources that are associated with the specified managed prefix list."]
module GetLaunchTemplateDataResult =
  struct
    type nonrec t =
      {
      launchTemplateData: ResponseLaunchTemplateData.t option
        [@ocaml.doc "The instance data."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplateData = fun () -> { launchTemplateData }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplateData",
           (Option.map x.launchTemplateData
              ~f:ResponseLaunchTemplateData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplateData =
        (Option.map ~f:ResponseLaunchTemplateData.of_xml)
          (Xml.child xml_arg0 "launchTemplateData") in
      make ?launchTemplateData ()
    let of_json json =
      let launchTemplateData =
        field_map json "LaunchTemplateData"
          ResponseLaunchTemplateData.of_json in
      make ?launchTemplateData ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the configuration data of the specified instance. You can use this data to create a launch template. This action calls on other describe actions to get instance information. Depending on your instance configuration, you may need to allow the following actions in your IAM policy: DescribeSpotInstanceRequests, DescribeInstanceCreditSpecifications, DescribeVolumes, DescribeInstanceAttribute, and DescribeElasticGpus. Or, you can allow describe* depending on your instance requirements."]
module GetLaunchTemplateDataRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."]}
    let context_ = "GetLaunchTemplateDataRequest"
    let make ?dryRun = fun ~instanceId -> fun () -> { dryRun; instanceId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceId ?dryRun ()
    let of_json json =
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the configuration data of the specified instance. You can use this data to create a launch template. This action calls on other describe actions to get instance information. Depending on your instance configuration, you may need to allow the following actions in your IAM policy: DescribeSpotInstanceRequests, DescribeInstanceCreditSpecifications, DescribeVolumes, DescribeInstanceAttribute, and DescribeElasticGpus. Or, you can allow describe* depending on your instance requirements."]
module GetIpamResourceCidrsResult =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      ipamResourceCidrs: IpamResourceCidrSet.t option
        [@ocaml.doc "The resource CIDRs."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?ipamResourceCidrs -> fun () -> { nextToken; ipamResourceCidrs }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ipamResourceCidrSet",
          (Option.map x.ipamResourceCidrs ~f:IpamResourceCidrSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamResourceCidrs =
        (Option.map ~f:IpamResourceCidrSet.of_xml)
          (Xml.child xml_arg0 "ipamResourceCidrSet") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?ipamResourceCidrs ?nextToken ()
    let of_json json =
      let ipamResourceCidrs =
        field_map json "IpamResourceCidrs" IpamResourceCidrSet.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?ipamResourceCidrs ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get information about the resources in a scope."]
module GetIpamResourceCidrsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the request."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      ipamScopeId: IpamScopeId.t
        [@ocaml.doc "The ID of the scope that the resource is in."];
      ipamPoolId: IpamPoolId.t option
        [@ocaml.doc "The ID of the IPAM pool that the resource is in."];
      resourceId: String_.t option [@ocaml.doc "The ID of the resource."];
      resourceType: IpamResourceType.t option
        [@ocaml.doc "The resource type."];
      resourceTag: RequestIpamResourceTag.t option ;
      resourceOwner: String_.t option
        [@ocaml.doc
          "The ID of the Amazon Web Services account that owns the resource."]}
    let context_ = "GetIpamResourceCidrsRequest"
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ?ipamPoolId ->
              fun ?resourceId ->
                fun ?resourceType ->
                  fun ?resourceTag ->
                    fun ?resourceOwner ->
                      fun ~ipamScopeId ->
                        fun () ->
                          {
                            dryRun;
                            filters;
                            maxResults;
                            nextToken;
                            ipamPoolId;
                            resourceId;
                            resourceType;
                            resourceTag;
                            resourceOwner;
                            ipamScopeId
                          }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)));
        ("IpamPoolId", (Option.map x.ipamPoolId ~f:IpamPoolId.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:IpamResourceType.to_value));
        ("ResourceTag",
          (Option.map x.resourceTag ~f:RequestIpamResourceTag.to_value));
        ("ResourceOwner", (Option.map x.resourceOwner ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceOwner =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceOwner") in
      let resourceTag =
        (Option.map ~f:RequestIpamResourceTag.of_xml)
          (Xml.child xml_arg0 "ResourceTag") in
      let resourceType =
        (Option.map ~f:IpamResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let ipamPoolId =
        (Option.map ~f:IpamPoolId.of_xml) (Xml.child xml_arg0 "IpamPoolId") in
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?resourceOwner ?resourceTag ?resourceType ?resourceId ?ipamPoolId
        ~ipamScopeId ?nextToken ?maxResults ?filters ?dryRun ()
    let of_json json =
      let resourceOwner = field_map json "ResourceOwner" String_.of_json in
      let resourceTag =
        field_map json "ResourceTag" RequestIpamResourceTag.of_json in
      let resourceType =
        field_map json "ResourceType" IpamResourceType.of_json in
      let resourceId = field_map json "ResourceId" String_.of_json in
      let ipamPoolId = field_map json "IpamPoolId" IpamPoolId.of_json in
      let ipamScopeId = field_map_exn json "IpamScopeId" IpamScopeId.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?resourceOwner ?resourceTag ?resourceType ?resourceId ?ipamPoolId
        ~ipamScopeId ?nextToken ?maxResults ?filters ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get information about the resources in a scope."]
module GetIpamPoolCidrsResult =
  struct
    type nonrec t =
      {
      ipamPoolCidrs: IpamPoolCidrSet.t option
        [@ocaml.doc
          "Information about the CIDRs provisioned to an IPAM pool."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolCidrs =
      fun ?nextToken -> fun () -> { ipamPoolCidrs; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolCidrSet",
           (Option.map x.ipamPoolCidrs ~f:IpamPoolCidrSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipamPoolCidrs =
        (Option.map ~f:IpamPoolCidrSet.of_xml)
          (Xml.child xml_arg0 "ipamPoolCidrSet") in
      make ?nextToken ?ipamPoolCidrs ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let ipamPoolCidrs =
        field_map json "IpamPoolCidrs" IpamPoolCidrSet.of_json in
      make ?nextToken ?ipamPoolCidrs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get the CIDRs provisioned to an IPAM pool."]
module GetIpamPoolCidrsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc "The ID of the IPAM pool you want the CIDR for."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output."];
      maxResults: IpamMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the request."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "GetIpamPoolCidrsRequest"
    let make ?dryRun =
      fun ?filters ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun ~ipamPoolId ->
              fun () ->
                { dryRun; filters; maxResults; nextToken; ipamPoolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:IpamMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ~ipamPoolId ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" IpamMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get the CIDRs provisioned to an IPAM pool."]
module GetIpamPoolAllocationsResult =
  struct
    type nonrec t =
      {
      ipamPoolAllocations: IpamPoolAllocationSet.t option
        [@ocaml.doc "The IPAM pool allocations you want information on."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolAllocations =
      fun ?nextToken -> fun () -> { ipamPoolAllocations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolAllocationSet",
           (Option.map x.ipamPoolAllocations
              ~f:IpamPoolAllocationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipamPoolAllocations =
        (Option.map ~f:IpamPoolAllocationSet.of_xml)
          (Xml.child xml_arg0 "ipamPoolAllocationSet") in
      make ?nextToken ?ipamPoolAllocations ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let ipamPoolAllocations =
        field_map json "IpamPoolAllocations" IpamPoolAllocationSet.of_json in
      make ?nextToken ?ipamPoolAllocations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a list of all the CIDR allocations in an IPAM pool."]
module GetIpamPoolAllocationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc
          "The ID of the IPAM pool you want to see the allocations for."];
      ipamPoolAllocationId: IpamPoolAllocationId.t option
        [@ocaml.doc "The ID of the allocation."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters for the request. For more information about filtering, see Filtering CLI output."];
      maxResults: GetIpamPoolAllocationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results you would like returned per page."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "GetIpamPoolAllocationsRequest"
    let make ?dryRun =
      fun ?ipamPoolAllocationId ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun ~ipamPoolId ->
                fun () ->
                  {
                    dryRun;
                    ipamPoolAllocationId;
                    filters;
                    maxResults;
                    nextToken;
                    ipamPoolId
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("IpamPoolAllocationId",
          (Option.map x.ipamPoolAllocationId ~f:IpamPoolAllocationId.to_value));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:GetIpamPoolAllocationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:GetIpamPoolAllocationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let ipamPoolAllocationId =
        (Option.map ~f:IpamPoolAllocationId.of_xml)
          (Xml.child xml_arg0 "IpamPoolAllocationId") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?filters ?ipamPoolAllocationId ~ipamPoolId
        ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" GetIpamPoolAllocationsMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let ipamPoolAllocationId =
        field_map json "IpamPoolAllocationId" IpamPoolAllocationId.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?filters ?ipamPoolAllocationId ~ipamPoolId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a list of all the CIDR allocations in an IPAM pool."]
module GetIpamAddressHistoryResult =
  struct
    type nonrec t =
      {
      historyRecords: IpamAddressHistoryRecordSet.t option
        [@ocaml.doc
          "A historical record for a CIDR within an IPAM scope. If the CIDR is associated with an EC2 instance, you will see an object in the response for the instance and one for the network interface."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?historyRecords =
      fun ?nextToken -> fun () -> { historyRecords; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("historyRecordSet",
           (Option.map x.historyRecords
              ~f:IpamAddressHistoryRecordSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let historyRecords =
        (Option.map ~f:IpamAddressHistoryRecordSet.of_xml)
          (Xml.child xml_arg0 "historyRecordSet") in
      make ?nextToken ?historyRecords ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let historyRecords =
        field_map json "HistoryRecords" IpamAddressHistoryRecordSet.of_json in
      make ?nextToken ?historyRecords ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieve historical information about a CIDR within an IPAM scope. For more information, see View the history of IP addresses in the Amazon VPC IPAM User Guide."]
module GetIpamAddressHistoryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      cidr: String_.t
        [@ocaml.doc
          "The CIDR you want the history of. The CIDR can be an IPv4 or IPv6 IP address range. If you enter a /16 IPv4 CIDR, you will get records that match it exactly. You will not get records for any subnets within the /16 CIDR."];
      ipamScopeId: IpamScopeId.t
        [@ocaml.doc "The ID of the IPAM scope that the CIDR is in."];
      vpcId: String_.t option
        [@ocaml.doc
          "The ID of the VPC you want your history records filtered by."];
      startTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The start of the time period for which you are looking for history. If you omit this option, it will default to the value of EndTime."];
      endTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The end of the time period for which you are looking for history. If you omit this option, it will default to the current time."];
      maxResults: IpamAddressHistoryMaxResults.t option
        [@ocaml.doc
          "The maximum number of historical results you would like returned per page. Defaults to 100."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "GetIpamAddressHistoryRequest"
    let make ?dryRun =
      fun ?vpcId ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?maxResults ->
              fun ?nextToken ->
                fun ~cidr ->
                  fun ~ipamScopeId ->
                    fun () ->
                      {
                        dryRun;
                        vpcId;
                        startTime;
                        endTime;
                        maxResults;
                        nextToken;
                        cidr;
                        ipamScopeId
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Cidr", (Some (String_.to_value x.cidr)));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)));
        ("VpcId", (Option.map x.vpcId ~f:String_.to_value));
        ("StartTime",
          (Option.map x.startTime ~f:MillisecondDateTime.to_value));
        ("EndTime", (Option.map x.endTime ~f:MillisecondDateTime.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:IpamAddressHistoryMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:IpamAddressHistoryMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let endTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "StartTime") in
      let vpcId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VpcId") in
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ?endTime ?startTime ?vpcId ~ipamScopeId
        ~cidr ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" IpamAddressHistoryMaxResults.of_json in
      let endTime = field_map json "EndTime" MillisecondDateTime.of_json in
      let startTime = field_map json "StartTime" MillisecondDateTime.of_json in
      let vpcId = field_map json "VpcId" String_.of_json in
      let ipamScopeId = field_map_exn json "IpamScopeId" IpamScopeId.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ?endTime ?startTime ?vpcId ~ipamScopeId
        ~cidr ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieve historical information about a CIDR within an IPAM scope. For more information, see View the history of IP addresses in the Amazon VPC IPAM User Guide."]
module GetInstanceTypesFromInstanceRequirementsResult =
  struct
    type nonrec t =
      {
      instanceTypes: InstanceTypeInfoFromInstanceRequirementsSet.t option
        [@ocaml.doc
          "The instance types with the specified instance attributes."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next set of results."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceTypes =
      fun ?nextToken -> fun () -> { instanceTypes; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceTypeSet",
           (Option.map x.instanceTypes
              ~f:InstanceTypeInfoFromInstanceRequirementsSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let instanceTypes =
        (Option.map ~f:InstanceTypeInfoFromInstanceRequirementsSet.of_xml)
          (Xml.child xml_arg0 "instanceTypeSet") in
      make ?nextToken ?instanceTypes ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let instanceTypes =
        field_map json "InstanceTypes"
          InstanceTypeInfoFromInstanceRequirementsSet.of_json in
      make ?nextToken ?instanceTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of instance types with the specified instance attributes. You can use the response to preview the instance types without launching instances. Note that the response does not consider capacity. When you specify multiple parameters, you get instance types that satisfy all of the specified parameters. If you specify multiple values for a parameter, you get instance types that satisfy any of the specified values. For more information, see Preview instance types with specified attributes, Attribute-based instance type selection for EC2 Fleet, Attribute-based instance type selection for Spot Fleet, and Spot placement score in the Amazon EC2 User Guide, and Creating an Auto Scaling group using attribute-based instance type selection in the Amazon EC2 Auto Scaling User Guide."]
module GetInstanceTypesFromInstanceRequirementsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      architectureTypes: ArchitectureTypeSet.t
        [@ocaml.doc "The processor architecture type."];
      virtualizationTypes: VirtualizationTypeSet.t
        [@ocaml.doc "The virtualization type."];
      instanceRequirements: InstanceRequirementsRequest.t
        [@ocaml.doc "The attributes required for the instance types."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of results to return in a single call. Specify a value between 1 and&#x2028; 1000. The default value is 1000. To retrieve the remaining results, make another call with&#x2028; the returned NextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next set of results."]}
    let context_ = "GetInstanceTypesFromInstanceRequirementsRequest"
    let make ?dryRun =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~architectureTypes ->
            fun ~virtualizationTypes ->
              fun ~instanceRequirements ->
                fun () ->
                  {
                    dryRun;
                    maxResults;
                    nextToken;
                    architectureTypes;
                    virtualizationTypes;
                    instanceRequirements
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ArchitectureType",
          (Some (ArchitectureTypeSet.to_value x.architectureTypes)));
        ("VirtualizationType",
          (Some (VirtualizationTypeSet.to_value x.virtualizationTypes)));
        ("InstanceRequirements",
          (Some (InstanceRequirementsRequest.to_value x.instanceRequirements)));
        ("MaxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let instanceRequirements =
        InstanceRequirementsRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceRequirements") in
      let virtualizationTypes =
        VirtualizationTypeSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VirtualizationType") in
      let architectureTypes =
        ArchitectureTypeSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ArchitectureType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?nextToken ?maxResults ~instanceRequirements ~virtualizationTypes
        ~architectureTypes ?dryRun ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" Integer.of_json in
      let instanceRequirements =
        field_map_exn json "InstanceRequirements"
          InstanceRequirementsRequest.of_json in
      let virtualizationTypes =
        field_map_exn json "VirtualizationTypes"
          VirtualizationTypeSet.of_json in
      let architectureTypes =
        field_map_exn json "ArchitectureTypes" ArchitectureTypeSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?nextToken ?maxResults ~instanceRequirements ~virtualizationTypes
        ~architectureTypes ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of instance types with the specified instance attributes. You can use the response to preview the instance types without launching instances. Note that the response does not consider capacity. When you specify multiple parameters, you get instance types that satisfy all of the specified parameters. If you specify multiple values for a parameter, you get instance types that satisfy any of the specified values. For more information, see Preview instance types with specified attributes, Attribute-based instance type selection for EC2 Fleet, Attribute-based instance type selection for Spot Fleet, and Spot placement score in the Amazon EC2 User Guide, and Creating an Auto Scaling group using attribute-based instance type selection in the Amazon EC2 Auto Scaling User Guide."]
module GetHostReservationPurchasePreviewResult =
  struct
    type nonrec t =
      {
      currencyCode: CurrencyCodeValues.t option
        [@ocaml.doc
          "The currency in which the totalUpfrontPrice and totalHourlyPrice amounts are specified. At this time, the only supported currency is USD."];
      purchase: PurchaseSet.t option
        [@ocaml.doc
          "The purchase information of the Dedicated Host reservation and the Dedicated Hosts associated with it."];
      totalHourlyPrice: String_.t option
        [@ocaml.doc
          "The potential total hourly price of the reservation per hour."];
      totalUpfrontPrice: String_.t option
        [@ocaml.doc
          "The potential total upfront price. This is billed immediately."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?currencyCode =
      fun ?purchase ->
        fun ?totalHourlyPrice ->
          fun ?totalUpfrontPrice ->
            fun () ->
              { currencyCode; purchase; totalHourlyPrice; totalUpfrontPrice }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("currencyCode",
           (Option.map x.currencyCode ~f:CurrencyCodeValues.to_value));
        ("purchase", (Option.map x.purchase ~f:PurchaseSet.to_value));
        ("totalHourlyPrice",
          (Option.map x.totalHourlyPrice ~f:String_.to_value));
        ("totalUpfrontPrice",
          (Option.map x.totalUpfrontPrice ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalUpfrontPrice =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "totalUpfrontPrice") in
      let totalHourlyPrice =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "totalHourlyPrice") in
      let purchase =
        (Option.map ~f:PurchaseSet.of_xml) (Xml.child xml_arg0 "purchase") in
      let currencyCode =
        (Option.map ~f:CurrencyCodeValues.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      make ?totalUpfrontPrice ?totalHourlyPrice ?purchase ?currencyCode ()
    let of_json json =
      let totalUpfrontPrice =
        field_map json "TotalUpfrontPrice" String_.of_json in
      let totalHourlyPrice =
        field_map json "TotalHourlyPrice" String_.of_json in
      let purchase = field_map json "Purchase" PurchaseSet.of_json in
      let currencyCode =
        field_map json "CurrencyCode" CurrencyCodeValues.of_json in
      make ?totalUpfrontPrice ?totalHourlyPrice ?purchase ?currencyCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Preview a reservation purchase with configurations that match those of your Dedicated Host. You must have active Dedicated Hosts in your account before you purchase a reservation. This is a preview of the PurchaseHostReservation action and does not result in the offering being purchased."]
module GetHostReservationPurchasePreviewRequest =
  struct
    type nonrec t =
      {
      hostIdSet: RequestHostIdSet.t
        [@ocaml.doc
          "The IDs of the Dedicated Hosts with which the reservation is associated."];
      offeringId: OfferingId.t
        [@ocaml.doc "The offering ID of the reservation."]}
    let context_ = "GetHostReservationPurchasePreviewRequest"
    let make ~hostIdSet =
      fun ~offeringId -> fun () -> { hostIdSet; offeringId }
    let to_value x =
      structure_to_value
        [("HostIdSet", (Some (RequestHostIdSet.to_value x.hostIdSet)));
        ("OfferingId", (Some (OfferingId.to_value x.offeringId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringId =
        OfferingId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OfferingId") in
      let hostIdSet =
        RequestHostIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostIdSet") in
      make ~offeringId ~hostIdSet ()
    let of_json json =
      let offeringId = field_map_exn json "OfferingId" OfferingId.of_json in
      let hostIdSet = field_map_exn json "HostIdSet" RequestHostIdSet.of_json in
      make ~offeringId ~hostIdSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Preview a reservation purchase with configurations that match those of your Dedicated Host. You must have active Dedicated Hosts in your account before you purchase a reservation. This is a preview of the PurchaseHostReservation action and does not result in the offering being purchased."]
module GetGroupsForCapacityReservationResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      capacityReservationGroups: CapacityReservationGroupSet.t option
        [@ocaml.doc
          "Information about the resource groups to which the Capacity Reservation has been added."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?capacityReservationGroups ->
        fun () -> { nextToken; capacityReservationGroups }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("capacityReservationGroupSet",
          (Option.map x.capacityReservationGroups
             ~f:CapacityReservationGroupSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationGroups =
        (Option.map ~f:CapacityReservationGroupSet.of_xml)
          (Xml.child xml_arg0 "capacityReservationGroupSet") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?capacityReservationGroups ?nextToken ()
    let of_json json =
      let capacityReservationGroups =
        field_map json "CapacityReservationGroups"
          CapacityReservationGroupSet.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?capacityReservationGroups ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the resource groups to which a Capacity Reservation has been added."]
module GetGroupsForCapacityReservationRequest =
  struct
    type nonrec t =
      {
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc "The ID of the Capacity Reservation."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: GetGroupsForCapacityReservationRequestMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetGroupsForCapacityReservationRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?dryRun ->
          fun ~capacityReservationId ->
            fun () ->
              { nextToken; maxResults; dryRun; capacityReservationId }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:GetGroupsForCapacityReservationRequestMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map
           ~f:GetGroupsForCapacityReservationRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      make ?dryRun ?maxResults ?nextToken ~capacityReservationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults"
          GetGroupsForCapacityReservationRequestMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let capacityReservationId =
        field_map_exn json "CapacityReservationId"
          CapacityReservationId.of_json in
      make ?dryRun ?maxResults ?nextToken ~capacityReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the resource groups to which a Capacity Reservation has been added."]
module GetFlowLogsIntegrationTemplateResult =
  struct
    type nonrec t =
      {
      result: String_.t option
        [@ocaml.doc "The generated CloudFormation template."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?result = fun () -> { result }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("result", (Option.map x.result ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let result =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "result") in
      make ?result ()
    let of_json json =
      let result = field_map json "Result" String_.of_json in make ?result ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Generates a CloudFormation template that streamlines and automates the integration of VPC flow logs with Amazon Athena. This make it easier for you to query and gain insights from VPC flow logs data. Based on the information that you provide, we configure resources in the template to do the following: Create a table in Athena that maps fields to a custom log format Create a Lambda function that updates the table with new partitions on a daily, weekly, or monthly basis Create a table partitioned between two timestamps in the past Create a set of named queries in Athena that you can use to get started quickly"]
module GetFlowLogsIntegrationTemplateRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      flowLogId: VpcFlowLogId.t [@ocaml.doc "The ID of the flow log."];
      configDeliveryS3DestinationArn: String_.t
        [@ocaml.doc
          "To store the CloudFormation template in Amazon S3, specify the location in Amazon S3."];
      integrateServices: IntegrateServices.t
        [@ocaml.doc "Information about the service integration."]}
    let context_ = "GetFlowLogsIntegrationTemplateRequest"
    let make ?dryRun =
      fun ~flowLogId ->
        fun ~configDeliveryS3DestinationArn ->
          fun ~integrateServices ->
            fun () ->
              {
                dryRun;
                flowLogId;
                configDeliveryS3DestinationArn;
                integrateServices
              }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FlowLogId", (Some (VpcFlowLogId.to_value x.flowLogId)));
        ("ConfigDeliveryS3DestinationArn",
          (Some (String_.to_value x.configDeliveryS3DestinationArn)));
        ("IntegrateService",
          (Some (IntegrateServices.to_value x.integrateServices)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let integrateServices =
        IntegrateServices.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IntegrateService") in
      let configDeliveryS3DestinationArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ConfigDeliveryS3DestinationArn") in
      let flowLogId =
        VpcFlowLogId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FlowLogId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~integrateServices ~configDeliveryS3DestinationArn ~flowLogId
        ?dryRun ()
    let of_json json =
      let integrateServices =
        field_map_exn json "IntegrateServices" IntegrateServices.of_json in
      let configDeliveryS3DestinationArn =
        field_map_exn json "ConfigDeliveryS3DestinationArn" String_.of_json in
      let flowLogId = field_map_exn json "FlowLogId" VpcFlowLogId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~integrateServices ~configDeliveryS3DestinationArn ~flowLogId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Generates a CloudFormation template that streamlines and automates the integration of VPC flow logs with Amazon Athena. This make it easier for you to query and gain insights from VPC flow logs data. Based on the information that you provide, we configure resources in the template to do the following: Create a table in Athena that maps fields to a custom log format Create a Lambda function that updates the table with new partitions on a daily, weekly, or monthly basis Create a table partitioned between two timestamps in the past Create a set of named queries in Athena that you can use to get started quickly"]
module GetEbsEncryptionByDefaultResult =
  struct
    type nonrec t =
      {
      ebsEncryptionByDefault: Boolean.t option
        [@ocaml.doc "Indicates whether encryption by default is enabled."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ebsEncryptionByDefault = fun () -> { ebsEncryptionByDefault }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ebsEncryptionByDefault",
           (Option.map x.ebsEncryptionByDefault ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ebsEncryptionByDefault =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ebsEncryptionByDefault") in
      make ?ebsEncryptionByDefault ()
    let of_json json =
      let ebsEncryptionByDefault =
        field_map json "EbsEncryptionByDefault" Boolean.of_json in
      make ?ebsEncryptionByDefault ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes whether EBS encryption by default is enabled for your account in the current Region. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module GetEbsEncryptionByDefaultRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes whether EBS encryption by default is enabled for your account in the current Region. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module GetEbsDefaultKmsKeyIdResult =
  struct
    type nonrec t =
      {
      kmsKeyId: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the default KMS key for encryption by default."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?kmsKeyId = fun () -> { kmsKeyId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("kmsKeyId", (Option.map x.kmsKeyId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      make ?kmsKeyId ()
    let of_json json =
      let kmsKeyId = field_map json "KmsKeyId" String_.of_json in
      make ?kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the default KMS key for EBS encryption by default for your account in this Region. You can change the default KMS key for encryption by default using ModifyEbsDefaultKmsKeyId or ResetEbsDefaultKmsKeyId. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module GetEbsDefaultKmsKeyIdRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the default KMS key for EBS encryption by default for your account in this Region. You can change the default KMS key for encryption by default using ModifyEbsDefaultKmsKeyId or ResetEbsDefaultKmsKeyId. For more information, see Amazon EBS encryption in the Amazon Elastic Compute Cloud User Guide."]
module GetDefaultCreditSpecificationResult =
  struct
    type nonrec t =
      {
      instanceFamilyCreditSpecification:
        InstanceFamilyCreditSpecification.t option
        [@ocaml.doc
          "The default credit option for CPU usage of the instance family."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceFamilyCreditSpecification =
      fun () -> { instanceFamilyCreditSpecification }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceFamilyCreditSpecification",
           (Option.map x.instanceFamilyCreditSpecification
              ~f:InstanceFamilyCreditSpecification.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceFamilyCreditSpecification =
        (Option.map ~f:InstanceFamilyCreditSpecification.of_xml)
          (Xml.child xml_arg0 "instanceFamilyCreditSpecification") in
      make ?instanceFamilyCreditSpecification ()
    let of_json json =
      let instanceFamilyCreditSpecification =
        field_map json "InstanceFamilyCreditSpecification"
          InstanceFamilyCreditSpecification.of_json in
      make ?instanceFamilyCreditSpecification ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the default credit option for CPU usage of a burstable performance instance family. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module GetDefaultCreditSpecificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceFamily: UnlimitedSupportedInstanceFamily.t
        [@ocaml.doc "The instance family."]}
    let context_ = "GetDefaultCreditSpecificationRequest"
    let make ?dryRun =
      fun ~instanceFamily -> fun () -> { dryRun; instanceFamily }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceFamily",
          (Some (UnlimitedSupportedInstanceFamily.to_value x.instanceFamily)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceFamily =
        UnlimitedSupportedInstanceFamily.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceFamily") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceFamily ?dryRun ()
    let of_json json =
      let instanceFamily =
        field_map_exn json "InstanceFamily"
          UnlimitedSupportedInstanceFamily.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~instanceFamily ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the default credit option for CPU usage of a burstable performance instance family. For more information, see Burstable performance instances in the Amazon EC2 User Guide."]
module GetConsoleScreenshotResult =
  struct
    type nonrec t =
      {
      imageData: String_.t option
        [@ocaml.doc "The data that comprises the image."];
      instanceId: String_.t option [@ocaml.doc "The ID of the instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageData =
      fun ?instanceId -> fun () -> { imageData; instanceId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageData", (Option.map x.imageData ~f:String_.to_value));
        ("instanceId", (Option.map x.instanceId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      let imageData =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageData") in
      make ?instanceId ?imageData ()
    let of_json json =
      let instanceId = field_map json "InstanceId" String_.of_json in
      let imageData = field_map json "ImageData" String_.of_json in
      make ?instanceId ?imageData ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieve a JPG-format screenshot of a running instance to help with troubleshooting. The returned content is Base64-encoded."]
module GetConsoleScreenshotRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      wakeUp: Boolean.t option
        [@ocaml.doc
          "When set to true, acts as keystroke input and wakes up an instance that's in standby or \"sleep\" mode."]}
    let context_ = "GetConsoleScreenshotRequest"
    let make ?dryRun =
      fun ?wakeUp ->
        fun ~instanceId -> fun () -> { dryRun; wakeUp; instanceId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("WakeUp", (Option.map x.wakeUp ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let wakeUp =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "WakeUp") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?wakeUp ~instanceId ?dryRun ()
    let of_json json =
      let wakeUp = field_map json "WakeUp" Boolean.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?wakeUp ~instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieve a JPG-format screenshot of a running instance to help with troubleshooting. The returned content is Base64-encoded."]
module GetConsoleOutputResult =
  struct
    type nonrec t =
      {
      instanceId: String_.t option [@ocaml.doc "The ID of the instance."];
      output: String_.t option
        [@ocaml.doc
          "The console output, base64-encoded. If you are using a command line tool, the tool decodes the output for you."];
      timestamp: DateTime.t option
        [@ocaml.doc "The time at which the output was last updated."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceId =
      fun ?output ->
        fun ?timestamp -> fun () -> { instanceId; output; timestamp }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceId", (Option.map x.instanceId ~f:String_.to_value));
        ("output", (Option.map x.output ~f:String_.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "timestamp") in
      let output =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "output") in
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      make ?timestamp ?output ?instanceId ()
    let of_json json =
      let timestamp = field_map json "Timestamp" DateTime.of_json in
      let output = field_map json "Output" String_.of_json in
      let instanceId = field_map json "InstanceId" String_.of_json in
      make ?timestamp ?output ?instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the console output for the specified instance. For Linux instances, the instance console output displays the exact console output that would normally be displayed on a physical monitor attached to a computer. For Windows instances, the instance console output includes the last three system event log errors. By default, the console output returns buffered information that was posted shortly after an instance transition state (start, stop, reboot, or terminate). This information is available for at least one hour after the most recent post. Only the most recent 64 KB of console output is available. You can optionally retrieve the latest serial console output at any time during the instance lifecycle. This option is supported on instance types that use the Nitro hypervisor. For more information, see Instance console output in the Amazon EC2 User Guide."]
module GetConsoleOutputRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      latest: Boolean.t option
        [@ocaml.doc
          "When enabled, retrieves the latest console output for the instance. Default: disabled (false)"]}
    let context_ = "GetConsoleOutputRequest"
    let make ?dryRun =
      fun ?latest ->
        fun ~instanceId -> fun () -> { dryRun; latest; instanceId }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Latest", (Option.map x.latest ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latest =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Latest") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?latest ?dryRun ~instanceId ()
    let of_json json =
      let latest = field_map json "Latest" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?latest ?dryRun ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the console output for the specified instance. For Linux instances, the instance console output displays the exact console output that would normally be displayed on a physical monitor attached to a computer. For Windows instances, the instance console output includes the last three system event log errors. By default, the console output returns buffered information that was posted shortly after an instance transition state (start, stop, reboot, or terminate). This information is available for at least one hour after the most recent post. Only the most recent 64 KB of console output is available. You can optionally retrieve the latest serial console output at any time during the instance lifecycle. This option is supported on instance types that use the Nitro hypervisor. For more information, see Instance console output in the Amazon EC2 User Guide."]
module GetCoipPoolUsageResult =
  struct
    type nonrec t =
      {
      coipPoolId: String_.t option
        [@ocaml.doc "The ID of the customer-owned address pool."];
      coipAddressUsages: CoipAddressUsageSet.t option
        [@ocaml.doc "Information about the address usage."];
      localGatewayRouteTableId: String_.t option
        [@ocaml.doc "The ID of the local gateway route table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipPoolId =
      fun ?coipAddressUsages ->
        fun ?localGatewayRouteTableId ->
          fun () ->
            { coipPoolId; coipAddressUsages; localGatewayRouteTableId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipPoolId", (Option.map x.coipPoolId ~f:String_.to_value));
        ("coipAddressUsageSet",
          (Option.map x.coipAddressUsages ~f:CoipAddressUsageSet.to_value));
        ("localGatewayRouteTableId",
          (Option.map x.localGatewayRouteTableId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableId") in
      let coipAddressUsages =
        (Option.map ~f:CoipAddressUsageSet.of_xml)
          (Xml.child xml_arg0 "coipAddressUsageSet") in
      let coipPoolId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "coipPoolId") in
      make ?localGatewayRouteTableId ?coipAddressUsages ?coipPoolId ()
    let of_json json =
      let localGatewayRouteTableId =
        field_map json "LocalGatewayRouteTableId" String_.of_json in
      let coipAddressUsages =
        field_map json "CoipAddressUsages" CoipAddressUsageSet.of_json in
      let coipPoolId = field_map json "CoipPoolId" String_.of_json in
      make ?localGatewayRouteTableId ?coipAddressUsages ?coipPoolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the allocations from the specified customer-owned address pool."]
module GetCoipPoolUsageRequest =
  struct
    type nonrec t =
      {
      poolId: Ipv4PoolCoipId.t [@ocaml.doc "The ID of the address pool."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. coip-address-usage.allocation-id - The allocation ID of the address. coip-address-usage.aws-account-id - The ID of the Amazon Web Services account that is using the customer-owned IP address. coip-address-usage.aws-service - The Amazon Web Services service that is using the customer-owned IP address. coip-address-usage.co-ip - The customer-owned IP address."];
      maxResults: CoipPoolMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: String_.t option
        [@ocaml.doc "The token for the next page of results."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetCoipPoolUsageRequest"
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ?dryRun ->
            fun ~poolId ->
              fun () -> { filters; maxResults; nextToken; dryRun; poolId }
    let to_value x =
      structure_to_value
        [("PoolId", (Some (Ipv4PoolCoipId.to_value x.poolId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:CoipPoolMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:CoipPoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let poolId =
        Ipv4PoolCoipId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      make ?dryRun ?nextToken ?maxResults ?filters ~poolId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" CoipPoolMaxResults.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let poolId = field_map_exn json "PoolId" Ipv4PoolCoipId.of_json in
      make ?dryRun ?nextToken ?maxResults ?filters ~poolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the allocations from the specified customer-owned address pool."]
module GetCapacityReservationUsageResult =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."];
      capacityReservationId: String_.t option
        [@ocaml.doc "The ID of the Capacity Reservation."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance for which the Capacity Reservation reserves capacity."];
      totalInstanceCount: Integer.t option
        [@ocaml.doc
          "The number of instances for which the Capacity Reservation reserves capacity."];
      availableInstanceCount: Integer.t option
        [@ocaml.doc
          "The remaining capacity. Indicates the number of instances that can be launched in the Capacity Reservation."];
      state: CapacityReservationState.t option
        [@ocaml.doc
          "The current state of the Capacity Reservation. A Capacity Reservation can be in one of the following states: active - The Capacity Reservation is active and the capacity is available for your use. expired - The Capacity Reservation expired automatically at the date and time specified in your request. The reserved capacity is no longer available for your use. cancelled - The Capacity Reservation was cancelled. The reserved capacity is no longer available for your use. pending - The Capacity Reservation request was successful but the capacity provisioning is still pending. failed - The Capacity Reservation request has failed. A request might fail due to invalid request parameters, capacity constraints, or instance limit constraints. Failed requests are retained for 60 minutes."];
      instanceUsages: InstanceUsageSet.t option
        [@ocaml.doc "Information about the Capacity Reservation usage."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?capacityReservationId ->
        fun ?instanceType ->
          fun ?totalInstanceCount ->
            fun ?availableInstanceCount ->
              fun ?state ->
                fun ?instanceUsages ->
                  fun () ->
                    {
                      nextToken;
                      capacityReservationId;
                      instanceType;
                      totalInstanceCount;
                      availableInstanceCount;
                      state;
                      instanceUsages
                    }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("capacityReservationId",
          (Option.map x.capacityReservationId ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("totalInstanceCount",
          (Option.map x.totalInstanceCount ~f:Integer.to_value));
        ("availableInstanceCount",
          (Option.map x.availableInstanceCount ~f:Integer.to_value));
        ("state", (Option.map x.state ~f:CapacityReservationState.to_value));
        ("instanceUsageSet",
          (Option.map x.instanceUsages ~f:InstanceUsageSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceUsages =
        (Option.map ~f:InstanceUsageSet.of_xml)
          (Xml.child xml_arg0 "instanceUsageSet") in
      let state =
        (Option.map ~f:CapacityReservationState.of_xml)
          (Xml.child xml_arg0 "state") in
      let availableInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "availableInstanceCount") in
      let totalInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "totalInstanceCount") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let capacityReservationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "capacityReservationId") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?instanceUsages ?state ?availableInstanceCount ?totalInstanceCount
        ?instanceType ?capacityReservationId ?nextToken ()
    let of_json json =
      let instanceUsages =
        field_map json "InstanceUsages" InstanceUsageSet.of_json in
      let state = field_map json "State" CapacityReservationState.of_json in
      let availableInstanceCount =
        field_map json "AvailableInstanceCount" Integer.of_json in
      let totalInstanceCount =
        field_map json "TotalInstanceCount" Integer.of_json in
      let instanceType = field_map json "InstanceType" String_.of_json in
      let capacityReservationId =
        field_map json "CapacityReservationId" String_.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?instanceUsages ?state ?availableInstanceCount ?totalInstanceCount
        ?instanceType ?capacityReservationId ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets usage information about a Capacity Reservation. If the Capacity Reservation is shared, it shows usage information for the Capacity Reservation owner and each Amazon Web Services account that is currently using the shared capacity. If the Capacity Reservation is not shared, it shows only the Capacity Reservation owner's usage."]
module GetCapacityReservationUsageRequest =
  struct
    type nonrec t =
      {
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc "The ID of the Capacity Reservation."];
      nextToken: String_.t option
        [@ocaml.doc "The token to use to retrieve the next page of results."];
      maxResults: GetCapacityReservationUsageRequestMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return for the request in a single page. The remaining results can be seen by sending another request with the returned nextToken value. This value can be between 5 and 500. If maxResults is given a larger value than 500, you receive an error. Valid range: Minimum value of 1. Maximum value of 1000."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetCapacityReservationUsageRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?dryRun ->
          fun ~capacityReservationId ->
            fun () ->
              { nextToken; maxResults; dryRun; capacityReservationId }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:GetCapacityReservationUsageRequestMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:GetCapacityReservationUsageRequestMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      make ?dryRun ?maxResults ?nextToken ~capacityReservationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults =
        field_map json "MaxResults"
          GetCapacityReservationUsageRequestMaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let capacityReservationId =
        field_map_exn json "CapacityReservationId"
          CapacityReservationId.of_json in
      make ?dryRun ?maxResults ?nextToken ~capacityReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets usage information about a Capacity Reservation. If the Capacity Reservation is shared, it shows usage information for the Capacity Reservation owner and each Amazon Web Services account that is currently using the shared capacity. If the Capacity Reservation is not shared, it shows only the Capacity Reservation owner's usage."]
module GetAssociatedIpv6PoolCidrsResult =
  struct
    type nonrec t =
      {
      ipv6CidrAssociations: Ipv6CidrAssociationSet.t option
        [@ocaml.doc "Information about the IPv6 CIDR block associations."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrAssociations =
      fun ?nextToken -> fun () -> { ipv6CidrAssociations; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrAssociationSet",
           (Option.map x.ipv6CidrAssociations
              ~f:Ipv6CidrAssociationSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ipv6CidrAssociations =
        (Option.map ~f:Ipv6CidrAssociationSet.of_xml)
          (Xml.child xml_arg0 "ipv6CidrAssociationSet") in
      make ?nextToken ?ipv6CidrAssociations ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let ipv6CidrAssociations =
        field_map json "Ipv6CidrAssociations" Ipv6CidrAssociationSet.of_json in
      make ?nextToken ?ipv6CidrAssociations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the IPv6 CIDR block associations for a specified IPv6 address pool."]
module GetAssociatedIpv6PoolCidrsRequest =
  struct
    type nonrec t =
      {
      poolId: Ipv6PoolEc2Id.t [@ocaml.doc "The ID of the IPv6 address pool."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: Ipv6PoolMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "GetAssociatedIpv6PoolCidrsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?dryRun ->
          fun ~poolId -> fun () -> { nextToken; maxResults; dryRun; poolId }
    let to_value x =
      structure_to_value
        [("PoolId", (Some (Ipv6PoolEc2Id.to_value x.poolId)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:Ipv6PoolMaxResults.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let maxResults =
        (Option.map ~f:Ipv6PoolMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let poolId =
        Ipv6PoolEc2Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      make ?dryRun ?maxResults ?nextToken ~poolId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let maxResults = field_map json "MaxResults" Ipv6PoolMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let poolId = field_map_exn json "PoolId" Ipv6PoolEc2Id.of_json in
      make ?dryRun ?maxResults ?nextToken ~poolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the IPv6 CIDR block associations for a specified IPv6 address pool."]
module GetAssociatedEnclaveCertificateIamRolesResult =
  struct
    type nonrec t =
      {
      associatedRoles: AssociatedRolesList.t option
        [@ocaml.doc "Information about the associated IAM roles."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associatedRoles = fun () -> { associatedRoles }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associatedRoleSet",
           (Option.map x.associatedRoles ~f:AssociatedRolesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associatedRoles =
        (Option.map ~f:AssociatedRolesList.of_xml)
          (Xml.child xml_arg0 "associatedRoleSet") in
      make ?associatedRoles ()
    let of_json json =
      let associatedRoles =
        field_map json "AssociatedRoles" AssociatedRolesList.of_json in
      make ?associatedRoles ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the IAM roles that are associated with the specified ACM (ACM) certificate. It also returns the name of the Amazon S3 bucket and the Amazon S3 object key where the certificate, certificate chain, and encrypted private key bundle are stored, and the ARN of the KMS key that's used to encrypt the private key."]
module GetAssociatedEnclaveCertificateIamRolesRequest =
  struct
    type nonrec t =
      {
      certificateArn: ResourceArn.t option
        [@ocaml.doc
          "The ARN of the ACM certificate for which to view the associated IAM roles, encryption keys, and Amazon S3 object information."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?certificateArn =
      fun ?dryRun -> fun () -> { certificateArn; dryRun }
    let to_value x =
      structure_to_value
        [("CertificateArn",
           (Option.map x.certificateArn ~f:ResourceArn.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let certificateArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "CertificateArn") in
      make ?dryRun ?certificateArn ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let certificateArn =
        field_map json "CertificateArn" ResourceArn.of_json in
      make ?dryRun ?certificateArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the IAM roles that are associated with the specified ACM (ACM) certificate. It also returns the name of the Amazon S3 bucket and the Amazon S3 object key where the certificate, certificate chain, and encrypted private key bundle are stored, and the ARN of the KMS key that's used to encrypt the private key."]
module ExportTransitGatewayRoutesResult =
  struct
    type nonrec t =
      {
      s3Location: String_.t option
        [@ocaml.doc
          "The URL of the exported file in Amazon S3. For example, s3://bucket_name/VPCTransitGateway/TransitGatewayRouteTables/file_name."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?s3Location = fun () -> { s3Location }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("s3Location", (Option.map x.s3Location ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "s3Location") in
      make ?s3Location ()
    let of_json json =
      let s3Location = field_map json "S3Location" String_.of_json in
      make ?s3Location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports routes from the specified transit gateway route table to the specified S3 bucket. By default, all routes are exported. Alternatively, you can filter by CIDR range. The routes are saved to the specified bucket in a JSON file. For more information, see Export Route Tables to Amazon S3 in Transit Gateways."]
module ExportTransitGatewayRoutesRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the route table."];
      filters: FilterList.t option
        [@ocaml.doc
          "One or more filters. The possible values are: attachment.transit-gateway-attachment-id - The id of the transit gateway attachment. attachment.resource-id - The resource id of the transit gateway attachment. route-search.exact-match - The exact match of the specified filter. route-search.longest-prefix-match - The longest prefix that matches the route. route-search.subnet-of-match - The routes with a subnet that match the specified CIDR filter. route-search.supernet-of-match - The routes with a CIDR that encompass the CIDR filter. For example, if you have 10.0.1.0/29 and 10.0.1.0/31 routes in your route table and you specify supernet-of-match as 10.0.1.0/30, then the result returns 10.0.1.0/29. state - The state of the route (active | blackhole). transit-gateway-route-destination-cidr-block - The CIDR range. type - The type of route (propagated | static)."];
      s3Bucket: String_.t [@ocaml.doc "The name of the S3 bucket."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ExportTransitGatewayRoutesRequest"
    let make ?filters =
      fun ?dryRun ->
        fun ~transitGatewayRouteTableId ->
          fun ~s3Bucket ->
            fun () ->
              { filters; dryRun; transitGatewayRouteTableId; s3Bucket }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("Filter", (Option.map x.filters ~f:FilterList.to_value));
        ("S3Bucket", (Some (String_.to_value x.s3Bucket)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let s3Bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "S3Bucket") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "Filter") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~s3Bucket ?filters ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let s3Bucket = field_map_exn json "S3Bucket" String_.of_json in
      let filters = field_map json "Filters" FilterList.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~s3Bucket ?filters ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports routes from the specified transit gateway route table to the specified S3 bucket. By default, all routes are exported. Alternatively, you can filter by CIDR range. The routes are saved to the specified bucket in a JSON file. For more information, see Export Route Tables to Amazon S3 in Transit Gateways."]
module ExportImageResult =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description of the image being exported."];
      diskImageFormat: DiskImageFormat.t option
        [@ocaml.doc "The disk image format for the exported image."];
      exportImageTaskId: String_.t option
        [@ocaml.doc "The ID of the export image task."];
      imageId: String_.t option [@ocaml.doc "The ID of the image."];
      roleName: String_.t option
        [@ocaml.doc
          "The name of the role that grants VM Import/Export permission to export images to your Amazon S3 bucket."];
      progress: String_.t option
        [@ocaml.doc "The percent complete of the export image task."];
      s3ExportLocation: ExportTaskS3Location.t option
        [@ocaml.doc "Information about the destination Amazon S3 bucket."];
      status: String_.t option
        [@ocaml.doc
          "The status of the export image task. The possible values are active, completed, deleting, and deleted."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message for the export image task."];
      tags: TagList.t option
        [@ocaml.doc "Any tags assigned to the export image task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?description =
      fun ?diskImageFormat ->
        fun ?exportImageTaskId ->
          fun ?imageId ->
            fun ?roleName ->
              fun ?progress ->
                fun ?s3ExportLocation ->
                  fun ?status ->
                    fun ?statusMessage ->
                      fun ?tags ->
                        fun () ->
                          {
                            description;
                            diskImageFormat;
                            exportImageTaskId;
                            imageId;
                            roleName;
                            progress;
                            s3ExportLocation;
                            status;
                            statusMessage;
                            tags
                          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("diskImageFormat",
          (Option.map x.diskImageFormat ~f:DiskImageFormat.to_value));
        ("exportImageTaskId",
          (Option.map x.exportImageTaskId ~f:String_.to_value));
        ("imageId", (Option.map x.imageId ~f:String_.to_value));
        ("roleName", (Option.map x.roleName ~f:String_.to_value));
        ("progress", (Option.map x.progress ~f:String_.to_value));
        ("s3ExportLocation",
          (Option.map x.s3ExportLocation ~f:ExportTaskS3Location.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let s3ExportLocation =
        (Option.map ~f:ExportTaskS3Location.of_xml)
          (Xml.child xml_arg0 "s3ExportLocation") in
      let progress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "progress") in
      let roleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "roleName") in
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      let exportImageTaskId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "exportImageTaskId") in
      let diskImageFormat =
        (Option.map ~f:DiskImageFormat.of_xml)
          (Xml.child xml_arg0 "diskImageFormat") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ?tags ?statusMessage ?status ?s3ExportLocation ?progress ?roleName
        ?imageId ?exportImageTaskId ?diskImageFormat ?description ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" String_.of_json in
      let s3ExportLocation =
        field_map json "S3ExportLocation" ExportTaskS3Location.of_json in
      let progress = field_map json "Progress" String_.of_json in
      let roleName = field_map json "RoleName" String_.of_json in
      let imageId = field_map json "ImageId" String_.of_json in
      let exportImageTaskId =
        field_map json "ExportImageTaskId" String_.of_json in
      let diskImageFormat =
        field_map json "DiskImageFormat" DiskImageFormat.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?tags ?statusMessage ?status ?s3ExportLocation ?progress ?roleName
        ?imageId ?exportImageTaskId ?diskImageFormat ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports an Amazon Machine Image (AMI) to a VM file. For more information, see Exporting a VM directly from an Amazon Machine Image (AMI) in the VM Import/Export User Guide."]
module ExportImageRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc "Token to enable idempotency for export image requests."];
      description: String_.t option
        [@ocaml.doc
          "A description of the image being exported. The maximum length is 255 characters."];
      diskImageFormat: DiskImageFormat.t
        [@ocaml.doc "The disk image format."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      imageId: ImageId.t [@ocaml.doc "The ID of the image."];
      s3ExportLocation: ExportTaskS3LocationRequest.t
        [@ocaml.doc
          "Information about the destination Amazon S3 bucket. The bucket must exist and grant WRITE and READ_ACP permissions to the Amazon Web Services account vm-import-export\\@amazon.com."];
      roleName: String_.t option
        [@ocaml.doc
          "The name of the role that grants VM Import/Export permission to export images to your Amazon S3 bucket. If this parameter is not specified, the default role is named 'vmimport'."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the export image task during creation."]}
    let context_ = "ExportImageRequest"
    let make ?clientToken =
      fun ?description ->
        fun ?dryRun ->
          fun ?roleName ->
            fun ?tagSpecifications ->
              fun ~diskImageFormat ->
                fun ~imageId ->
                  fun ~s3ExportLocation ->
                    fun () ->
                      {
                        clientToken;
                        description;
                        dryRun;
                        roleName;
                        tagSpecifications;
                        diskImageFormat;
                        imageId;
                        s3ExportLocation
                      }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DiskImageFormat",
          (Some (DiskImageFormat.to_value x.diskImageFormat)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ImageId", (Some (ImageId.to_value x.imageId)));
        ("S3ExportLocation",
          (Some (ExportTaskS3LocationRequest.to_value x.s3ExportLocation)));
        ("RoleName", (Option.map x.roleName ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let roleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RoleName") in
      let s3ExportLocation =
        ExportTaskS3LocationRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "S3ExportLocation") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let diskImageFormat =
        DiskImageFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DiskImageFormat") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ?roleName ~s3ExportLocation ~imageId ?dryRun
        ~diskImageFormat ?description ?clientToken ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let roleName = field_map json "RoleName" String_.of_json in
      let s3ExportLocation =
        field_map_exn json "S3ExportLocation"
          ExportTaskS3LocationRequest.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let diskImageFormat =
        field_map_exn json "DiskImageFormat" DiskImageFormat.of_json in
      let description = field_map json "Description" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?tagSpecifications ?roleName ~s3ExportLocation ~imageId ?dryRun
        ~diskImageFormat ?description ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports an Amazon Machine Image (AMI) to a VM file. For more information, see Exporting a VM directly from an Amazon Machine Image (AMI) in the VM Import/Export User Guide."]
module ExportClientVpnClientConfigurationResult =
  struct
    type nonrec t =
      {
      clientConfiguration: String_.t option
        [@ocaml.doc
          "The contents of the Client VPN endpoint configuration file."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientConfiguration = fun () -> { clientConfiguration }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientConfiguration",
           (Option.map x.clientConfiguration ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientConfiguration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "clientConfiguration") in
      make ?clientConfiguration ()
    let of_json json =
      let clientConfiguration =
        field_map json "ClientConfiguration" String_.of_json in
      make ?clientConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Downloads the contents of the Client VPN endpoint configuration file for the specified Client VPN endpoint. The Client VPN endpoint configuration file includes the Client VPN endpoint and certificate information clients need to establish a connection with the Client VPN endpoint."]
module ExportClientVpnClientConfigurationRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ExportClientVpnClientConfigurationRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId -> fun () -> { dryRun; clientVpnEndpointId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Downloads the contents of the Client VPN endpoint configuration file for the specified Client VPN endpoint. The Client VPN endpoint configuration file includes the Client VPN endpoint and certificate information clients need to establish a connection with the Client VPN endpoint."]
module ExportClientVpnClientCertificateRevocationListResult =
  struct
    type nonrec t =
      {
      certificateRevocationList: String_.t option
        [@ocaml.doc
          "Information about the client certificate revocation list."];
      status: ClientCertificateRevocationListStatus.t option
        [@ocaml.doc
          "The current state of the client certificate revocation list."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?certificateRevocationList =
      fun ?status -> fun () -> { certificateRevocationList; status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("certificateRevocationList",
           (Option.map x.certificateRevocationList ~f:String_.to_value));
        ("status",
          (Option.map x.status
             ~f:ClientCertificateRevocationListStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientCertificateRevocationListStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let certificateRevocationList =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateRevocationList") in
      make ?status ?certificateRevocationList ()
    let of_json json =
      let status =
        field_map json "Status" ClientCertificateRevocationListStatus.of_json in
      let certificateRevocationList =
        field_map json "CertificateRevocationList" String_.of_json in
      make ?status ?certificateRevocationList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Downloads the client certificate revocation list for the specified Client VPN endpoint."]
module ExportClientVpnClientCertificateRevocationListRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ExportClientVpnClientCertificateRevocationListRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId -> fun () -> { dryRun; clientVpnEndpointId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Downloads the client certificate revocation list for the specified Client VPN endpoint."]
module EnableVpcClassicLinkResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables a VPC for ClassicLink. You can then link EC2-Classic instances to your ClassicLink-enabled VPC to allow communication over private IP addresses. You cannot enable your VPC for ClassicLink if any of your VPC route tables have existing routes for address ranges within the 10.0.0.0/8 IP address range, excluding local routes for VPCs in the 10.0.0.0/16 and 10.1.0.0/16 IP address ranges. For more information, see ClassicLink in the Amazon Elastic Compute Cloud User Guide."]
module EnableVpcClassicLinkRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."]}
    let context_ = "EnableVpcClassicLinkRequest"
    let make ?dryRun = fun ~vpcId -> fun () -> { dryRun; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables a VPC for ClassicLink. You can then link EC2-Classic instances to your ClassicLink-enabled VPC to allow communication over private IP addresses. You cannot enable your VPC for ClassicLink if any of your VPC route tables have existing routes for address ranges within the 10.0.0.0/8 IP address range, excluding local routes for VPCs in the 10.0.0.0/16 and 10.1.0.0/16 IP address ranges. For more information, see ClassicLink in the Amazon Elastic Compute Cloud User Guide."]
module EnableVpcClassicLinkDnsSupportResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables a VPC to support DNS hostname resolution for ClassicLink. If enabled, the DNS hostname of a linked EC2-Classic instance resolves to its private IP address when addressed from an instance in the VPC to which it's linked. Similarly, the DNS hostname of an instance in a VPC resolves to its private IP address when addressed from a linked EC2-Classic instance. For more information, see ClassicLink in the Amazon Elastic Compute Cloud User Guide. You must specify a VPC ID in the request."]
module EnableVpcClassicLinkDnsSupportRequest =
  struct
    type nonrec t = {
      vpcId: VpcId.t option [@ocaml.doc "The ID of the VPC."]}
    let make ?vpcId = fun () -> { vpcId }
    let to_value x =
      structure_to_value [("VpcId", (Option.map x.vpcId ~f:VpcId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      make ?vpcId ()
    let of_json json =
      let vpcId = field_map json "VpcId" VpcId.of_json in make ?vpcId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables a VPC to support DNS hostname resolution for ClassicLink. If enabled, the DNS hostname of a linked EC2-Classic instance resolves to its private IP address when addressed from an instance in the VPC to which it's linked. Similarly, the DNS hostname of an instance in a VPC resolves to its private IP address when addressed from a linked EC2-Classic instance. For more information, see ClassicLink in the Amazon Elastic Compute Cloud User Guide. You must specify a VPC ID in the request."]
module EnableVolumeIORequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      volumeId: VolumeId.t [@ocaml.doc "The ID of the volume."]}
    let context_ = "EnableVolumeIORequest"
    let make ?dryRun = fun ~volumeId -> fun () -> { dryRun; volumeId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("volumeId", (Some (VolumeId.to_value x.volumeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "volumeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~volumeId ?dryRun ()
    let of_json json =
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~volumeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables I/O operations for a volume that had I/O operations disabled because the data on the volume was potentially inconsistent."]
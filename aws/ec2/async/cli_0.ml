(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_reserved_instances_exchange_quote =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and targetConfigurations =
         flag "target-configurations" (optional json_arg)
           ~doc:"JSON TargetConfigurationRequestSet"
       and reservedInstanceIds =
         flag "reserved-instance-ids" (required json_arg)
           ~doc:"JSON ReservedInstanceIdSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_reserved_instances_exchange_quote
           (Values.AcceptReservedInstancesExchangeQuoteRequest.make ?dryRun
              ?targetConfigurations:(Option.map
                                       ~f:Values.TargetConfigurationRequestSet.of_json
                                       targetConfigurations)
              ~reservedInstanceIds:(Values.ReservedInstanceIdSet.of_json
                                      reservedInstanceIds) ())
           (Some Values.AcceptReservedInstancesExchangeQuoteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let accept_transit_gateway_multicast_domain_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayMulticastDomainId =
         flag "transit-gateway-multicast-domain-id" (optional string)
           ~doc:"STRING TransitGatewayMulticastDomainId"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (optional string)
           ~doc:"STRING TransitGatewayAttachmentId"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON ValueStringList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_transit_gateway_multicast_domain_associations
           (Values.AcceptTransitGatewayMulticastDomainAssociationsRequest.make
              ?transitGatewayMulticastDomainId ?transitGatewayAttachmentId
              ?subnetIds:(Option.map ~f:Values.ValueStringList.of_json
                            subnetIds) ?dryRun ())
           (Some
              Values.AcceptTransitGatewayMulticastDomainAssociationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let accept_transit_gateway_peering_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_transit_gateway_peering_attachment
           (Values.AcceptTransitGatewayPeeringAttachmentRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.AcceptTransitGatewayPeeringAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let accept_transit_gateway_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_transit_gateway_vpc_attachment
           (Values.AcceptTransitGatewayVpcAttachmentRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.AcceptTransitGatewayVpcAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let accept_vpc_endpoint_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and serviceId =
         flag "service-id" (required string)
           ~doc:"STRING VpcEndpointServiceId"
       and vpcEndpointIds =
         flag "vpc-endpoint-ids" (required json_arg)
           ~doc:"JSON VpcEndpointIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_vpc_endpoint_connections
           (Values.AcceptVpcEndpointConnectionsRequest.make ?dryRun
              ~serviceId
              ~vpcEndpointIds:(Values.VpcEndpointIdList.of_json
                                 vpcEndpointIds) ())
           (Some Values.AcceptVpcEndpointConnectionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let accept_vpc_peering_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcPeeringConnectionId =
         flag "vpc-peering-connection-id" (optional string)
           ~doc:"STRING VpcPeeringConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_vpc_peering_connection
           (Values.AcceptVpcPeeringConnectionRequest.make ?dryRun
              ?vpcPeeringConnectionId ())
           (Some Values.AcceptVpcPeeringConnectionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let advertise_byoip_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and cidr = flag "cidr" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.advertise_byoip_cidr
           (Values.AdvertiseByoipCidrRequest.make ?dryRun ~cidr ())
           (Some Values.AdvertiseByoipCidrResult.to_json)
           (Some Values.Ec2_error.to_json)])
let allocate_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (optional json_arg) ~doc:"JSON DomainType"
       and address =
         flag "address" (optional string) ~doc:"STRING PublicIpAddress"
       and publicIpv4Pool =
         flag "public-ipv4-pool" (optional string)
           ~doc:"STRING Ipv4PoolEc2Id"
       and networkBorderGroup =
         flag "network-border-group" (optional string) ~doc:"STRING String"
       and customerOwnedIpv4Pool =
         flag "customer-owned-ipv4-pool" (optional string)
           ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.allocate_address
           (Values.AllocateAddressRequest.make
              ?domain:(Option.map ~f:Values.DomainType.of_json domain)
              ?address ?publicIpv4Pool ?networkBorderGroup
              ?customerOwnedIpv4Pool ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.AllocateAddressResult.to_json)
           (Some Values.Ec2_error.to_json)])
let allocate_hosts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoPlacement =
         flag "auto-placement" (optional json_arg) ~doc:"JSON AutoPlacement"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and instanceType =
         flag "instance-type" (optional string) ~doc:"STRING String"
       and instanceFamily =
         flag "instance-family" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and hostRecovery =
         flag "host-recovery" (optional json_arg) ~doc:"JSON HostRecovery"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING String"
       and quantity = flag "quantity" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.allocate_hosts
           (Values.AllocateHostsRequest.make
              ?autoPlacement:(Option.map ~f:Values.AutoPlacement.of_json
                                autoPlacement) ?clientToken ?instanceType
              ?instanceFamily
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ?hostRecovery:(Option.map ~f:Values.HostRecovery.of_json
                               hostRecovery) ~availabilityZone ~quantity ())
           (Some Values.AllocateHostsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let allocate_ipam_pool_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and cidr = flag "cidr" (optional string) ~doc:"STRING String"
       and netmaskLength =
         flag "netmask-length" (optional int) ~doc:"INT Integer"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and previewNextCidr =
         flag "preview-next-cidr" (optional bool) ~doc:"BOOL Boolean"
       and disallowedCidrs =
         flag "disallowed-cidrs" (optional json_arg)
           ~doc:"JSON IpamPoolAllocationDisallowedCidrs"
       and ipamPoolId =
         flag "ipam-pool-id" (required string) ~doc:"STRING IpamPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.allocate_ipam_pool_cidr
           (Values.AllocateIpamPoolCidrRequest.make ?dryRun ?cidr
              ?netmaskLength ?clientToken ?description ?previewNextCidr
              ?disallowedCidrs:(Option.map
                                  ~f:Values.IpamPoolAllocationDisallowedCidrs.of_json
                                  disallowedCidrs) ~ipamPoolId ())
           (Some Values.AllocateIpamPoolCidrResult.to_json)
           (Some Values.Ec2_error.to_json)])
let apply_security_groups_to_client_vpn_target_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and securityGroupIds =
         flag "security-group-ids" (required json_arg)
           ~doc:"JSON ClientVpnSecurityGroupIdSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_security_groups_to_client_vpn_target_network
           (Values.ApplySecurityGroupsToClientVpnTargetNetworkRequest.make
              ?dryRun ~clientVpnEndpointId ~vpcId
              ~securityGroupIds:(Values.ClientVpnSecurityGroupIdSet.of_json
                                   securityGroupIds) ())
           (Some
              Values.ApplySecurityGroupsToClientVpnTargetNetworkResult.to_json)
           (Some Values.Ec2_error.to_json)])
let assign_ipv6_addresses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipv6AddressCount =
         flag "ipv6-address-count" (optional int) ~doc:"INT Integer"
       and ipv6Addresses =
         flag "ipv6-addresses" (optional json_arg)
           ~doc:"JSON Ipv6AddressList"
       and ipv6PrefixCount =
         flag "ipv6-prefix-count" (optional int) ~doc:"INT Integer"
       and ipv6Prefixes =
         flag "ipv6-prefixes" (optional json_arg) ~doc:"JSON IpPrefixList"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assign_ipv6_addresses
           (Values.AssignIpv6AddressesRequest.make ?ipv6AddressCount
              ?ipv6Addresses:(Option.map ~f:Values.Ipv6AddressList.of_json
                                ipv6Addresses) ?ipv6PrefixCount
              ?ipv6Prefixes:(Option.map ~f:Values.IpPrefixList.of_json
                               ipv6Prefixes) ~networkInterfaceId ())
           (Some Values.AssignIpv6AddressesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let assign_private_ip_addresses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowReassignment =
         flag "allow-reassignment" (optional bool) ~doc:"BOOL Boolean"
       and privateIpAddresses =
         flag "private-ip-addresses" (optional json_arg)
           ~doc:"JSON PrivateIpAddressStringList"
       and secondaryPrivateIpAddressCount =
         flag "secondary-private-ip-address-count" (optional int)
           ~doc:"INT Integer"
       and ipv4Prefixes =
         flag "ipv4-prefixes" (optional json_arg) ~doc:"JSON IpPrefixList"
       and ipv4PrefixCount =
         flag "ipv4-prefix-count" (optional int) ~doc:"INT Integer"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assign_private_ip_addresses
           (Values.AssignPrivateIpAddressesRequest.make ?allowReassignment
              ?privateIpAddresses:(Option.map
                                     ~f:Values.PrivateIpAddressStringList.of_json
                                     privateIpAddresses)
              ?secondaryPrivateIpAddressCount
              ?ipv4Prefixes:(Option.map ~f:Values.IpPrefixList.of_json
                               ipv4Prefixes) ?ipv4PrefixCount
              ~networkInterfaceId ())
           (Some Values.AssignPrivateIpAddressesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocationId =
         flag "allocation-id" (optional string) ~doc:"STRING AllocationId"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and publicIp = flag "public-ip" (optional string) ~doc:"STRING String"
       and allowReassociation =
         flag "allow-reassociation" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfaceId =
         flag "network-interface-id" (optional string)
           ~doc:"STRING NetworkInterfaceId"
       and privateIpAddress =
         flag "private-ip-address" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_address
           (Values.AssociateAddressRequest.make ?allocationId ?instanceId
              ?publicIp ?allowReassociation ?dryRun ?networkInterfaceId
              ?privateIpAddress ())
           (Some Values.AssociateAddressResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_client_vpn_target_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_client_vpn_target_network
           (Values.AssociateClientVpnTargetNetworkRequest.make ?clientToken
              ?dryRun ~clientVpnEndpointId ~subnetId ())
           (Some Values.AssociateClientVpnTargetNetworkResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_dhcp_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and dhcpOptionsId =
         flag "dhcp-options-id" (required string)
           ~doc:"STRING DefaultingDhcpOptionsId"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_dhcp_options
           (Values.AssociateDhcpOptionsRequest.make ?dryRun ~dhcpOptionsId
              ~vpcId ()) None (Some Values.Ec2_error.to_json)])
let associate_enclave_certificate_iam_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING ResourceArn"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING ResourceArn"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_enclave_certificate_iam_role
           (Values.AssociateEnclaveCertificateIamRoleRequest.make
              ?certificateArn ?roleArn ?dryRun ())
           (Some Values.AssociateEnclaveCertificateIamRoleResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_iam_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iamInstanceProfile =
         flag "iam-instance-profile" (required json_arg)
           ~doc:"JSON IamInstanceProfileSpecification"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_iam_instance_profile
           (Values.AssociateIamInstanceProfileRequest.make
              ~iamInstanceProfile:(Values.IamInstanceProfileSpecification.of_json
                                     iamInstanceProfile) ~instanceId ())
           (Some Values.AssociateIamInstanceProfileResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_instance_event_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceEventWindowId =
         flag "instance-event-window-id" (required string)
           ~doc:"STRING InstanceEventWindowId"
       and associationTarget =
         flag "association-target" (required json_arg)
           ~doc:"JSON InstanceEventWindowAssociationRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_instance_event_window
           (Values.AssociateInstanceEventWindowRequest.make ?dryRun
              ~instanceEventWindowId
              ~associationTarget:(Values.InstanceEventWindowAssociationRequest.of_json
                                    associationTarget) ())
           (Some Values.AssociateInstanceEventWindowResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and subnetId =
         flag "subnet-id" (optional string) ~doc:"STRING SubnetId"
       and gatewayId =
         flag "gateway-id" (optional string) ~doc:"STRING RouteGatewayId"
       and routeTableId =
         flag "route-table-id" (required string) ~doc:"STRING RouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_route_table
           (Values.AssociateRouteTableRequest.make ?dryRun ?subnetId
              ?gatewayId ~routeTableId ())
           (Some Values.AssociateRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_subnet_cidr_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipv6CidrBlock =
         flag "ipv6-cidr-block" (required string) ~doc:"STRING String"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_subnet_cidr_block
           (Values.AssociateSubnetCidrBlockRequest.make ~ipv6CidrBlock
              ~subnetId ())
           (Some Values.AssociateSubnetCidrBlockResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_transit_gateway_multicast_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayMulticastDomainId =
         flag "transit-gateway-multicast-domain-id" (optional string)
           ~doc:"STRING TransitGatewayMulticastDomainId"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (optional string)
           ~doc:"STRING TransitGatewayAttachmentId"
       and subnetIds =
         flag "subnet-ids" (optional json_arg)
           ~doc:"JSON TransitGatewaySubnetIdList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_transit_gateway_multicast_domain
           (Values.AssociateTransitGatewayMulticastDomainRequest.make
              ?transitGatewayMulticastDomainId ?transitGatewayAttachmentId
              ?subnetIds:(Option.map
                            ~f:Values.TransitGatewaySubnetIdList.of_json
                            subnetIds) ?dryRun ())
           (Some Values.AssociateTransitGatewayMulticastDomainResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_transit_gateway_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_transit_gateway_route_table
           (Values.AssociateTransitGatewayRouteTableRequest.make ?dryRun
              ~transitGatewayRouteTableId ~transitGatewayAttachmentId ())
           (Some Values.AssociateTransitGatewayRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_trunk_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vlanId = flag "vlan-id" (optional int) ~doc:"INT Integer"
       and greKey = flag "gre-key" (optional int) ~doc:"INT Integer"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and branchInterfaceId =
         flag "branch-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId"
       and trunkInterfaceId =
         flag "trunk-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_trunk_interface
           (Values.AssociateTrunkInterfaceRequest.make ?vlanId ?greKey
              ?clientToken ?dryRun ~branchInterfaceId ~trunkInterfaceId ())
           (Some Values.AssociateTrunkInterfaceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let associate_vpc_cidr_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amazonProvidedIpv6CidrBlock =
         flag "amazon-provided-ipv6-cidr-block" (optional bool)
           ~doc:"BOOL Boolean"
       and cidrBlock =
         flag "cidr-block" (optional string) ~doc:"STRING String"
       and ipv6CidrBlockNetworkBorderGroup =
         flag "ipv6-cidr-block-network-border-group" (optional string)
           ~doc:"STRING String"
       and ipv6Pool =
         flag "ipv6-pool" (optional string) ~doc:"STRING Ipv6PoolEc2Id"
       and ipv6CidrBlock =
         flag "ipv6-cidr-block" (optional string) ~doc:"STRING String"
       and ipv4IpamPoolId =
         flag "ipv4-ipam-pool-id" (optional string) ~doc:"STRING IpamPoolId"
       and ipv4NetmaskLength =
         flag "ipv4-netmask-length" (optional int) ~doc:"INT NetmaskLength"
       and ipv6IpamPoolId =
         flag "ipv6-ipam-pool-id" (optional string) ~doc:"STRING IpamPoolId"
       and ipv6NetmaskLength =
         flag "ipv6-netmask-length" (optional int) ~doc:"INT NetmaskLength"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_vpc_cidr_block
           (Values.AssociateVpcCidrBlockRequest.make
              ?amazonProvidedIpv6CidrBlock ?cidrBlock
              ?ipv6CidrBlockNetworkBorderGroup ?ipv6Pool ?ipv6CidrBlock
              ?ipv4IpamPoolId ?ipv4NetmaskLength ?ipv6IpamPoolId
              ?ipv6NetmaskLength ~vpcId ())
           (Some Values.AssociateVpcCidrBlockResult.to_json)
           (Some Values.Ec2_error.to_json)])
let attach_classic_link_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and groups =
         flag "groups" (required json_arg) ~doc:"JSON GroupIdStringList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_classic_link_vpc
           (Values.AttachClassicLinkVpcRequest.make ?dryRun
              ~groups:(Values.GroupIdStringList.of_json groups) ~instanceId
              ~vpcId ()) (Some Values.AttachClassicLinkVpcResult.to_json)
           (Some Values.Ec2_error.to_json)])
let attach_internet_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and internetGatewayId =
         flag "internet-gateway-id" (required string)
           ~doc:"STRING InternetGatewayId"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_internet_gateway
           (Values.AttachInternetGatewayRequest.make ?dryRun
              ~internetGatewayId ~vpcId ()) None
           (Some Values.Ec2_error.to_json)])
let attach_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkCardIndex =
         flag "network-card-index" (optional int) ~doc:"INT Integer"
       and deviceIndex =
         flag "device-index" (required int) ~doc:"INT Integer"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_network_interface
           (Values.AttachNetworkInterfaceRequest.make ?dryRun
              ?networkCardIndex ~deviceIndex ~instanceId ~networkInterfaceId
              ()) (Some Values.AttachNetworkInterfaceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let attach_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and device = flag "device" (required string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_volume
           (Values.AttachVolumeRequest.make ?dryRun ~device ~instanceId
              ~volumeId ()) (Some Values.VolumeAttachment.to_json)
           (Some Values.Ec2_error.to_json)])
let attach_vpn_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and vpnGatewayId =
         flag "vpn-gateway-id" (required string) ~doc:"STRING VpnGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_vpn_gateway
           (Values.AttachVpnGatewayRequest.make ?dryRun ~vpcId ~vpnGatewayId
              ()) (Some Values.AttachVpnGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let authorize_client_vpn_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessGroupId =
         flag "access-group-id" (optional string) ~doc:"STRING String"
       and authorizeAllGroups =
         flag "authorize-all-groups" (optional bool) ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId"
       and targetNetworkCidr =
         flag "target-network-cidr" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_client_vpn_ingress
           (Values.AuthorizeClientVpnIngressRequest.make ?accessGroupId
              ?authorizeAllGroups ?description ?clientToken ?dryRun
              ~clientVpnEndpointId ~targetNetworkCidr ())
           (Some Values.AuthorizeClientVpnIngressResult.to_json)
           (Some Values.Ec2_error.to_json)])
let authorize_security_group_egress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipPermissions =
         flag "ip-permissions" (optional json_arg)
           ~doc:"JSON IpPermissionList"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and cidrIp = flag "cidr-ip" (optional string) ~doc:"STRING String"
       and fromPort = flag "from-port" (optional int) ~doc:"INT Integer"
       and ipProtocol =
         flag "ip-protocol" (optional string) ~doc:"STRING String"
       and toPort = flag "to-port" (optional int) ~doc:"INT Integer"
       and sourceSecurityGroupName =
         flag "source-security-group-name" (optional string)
           ~doc:"STRING String"
       and sourceSecurityGroupOwnerId =
         flag "source-security-group-owner-id" (optional string)
           ~doc:"STRING String"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING SecurityGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_security_group_egress
           (Values.AuthorizeSecurityGroupEgressRequest.make ?dryRun
              ?ipPermissions:(Option.map ~f:Values.IpPermissionList.of_json
                                ipPermissions)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?cidrIp ?fromPort
              ?ipProtocol ?toPort ?sourceSecurityGroupName
              ?sourceSecurityGroupOwnerId ~groupId ())
           (Some Values.AuthorizeSecurityGroupEgressResult.to_json)
           (Some Values.Ec2_error.to_json)])
let authorize_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidrIp = flag "cidr-ip" (optional string) ~doc:"STRING String"
       and fromPort = flag "from-port" (optional int) ~doc:"INT Integer"
       and groupId =
         flag "group-id" (optional string) ~doc:"STRING SecurityGroupId"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING SecurityGroupName"
       and ipPermissions =
         flag "ip-permissions" (optional json_arg)
           ~doc:"JSON IpPermissionList"
       and ipProtocol =
         flag "ip-protocol" (optional string) ~doc:"STRING String"
       and sourceSecurityGroupName =
         flag "source-security-group-name" (optional string)
           ~doc:"STRING String"
       and sourceSecurityGroupOwnerId =
         flag "source-security-group-owner-id" (optional string)
           ~doc:"STRING String"
       and toPort = flag "to-port" (optional int) ~doc:"INT Integer"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_security_group_ingress
           (Values.AuthorizeSecurityGroupIngressRequest.make ?cidrIp
              ?fromPort ?groupId ?groupName
              ?ipPermissions:(Option.map ~f:Values.IpPermissionList.of_json
                                ipPermissions) ?ipProtocol
              ?sourceSecurityGroupName ?sourceSecurityGroupOwnerId ?toPort
              ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.AuthorizeSecurityGroupIngressResult.to_json)
           (Some Values.Ec2_error.to_json)])
let bundle_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and storage = flag "storage" (required json_arg) ~doc:"JSON Storage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.bundle_instance
           (Values.BundleInstanceRequest.make ?dryRun ~instanceId
              ~storage:(Values.Storage.of_json storage) ())
           (Some Values.BundleInstanceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_bundle_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING BundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_bundle_task
           (Values.CancelBundleTaskRequest.make ?dryRun ~bundleId ())
           (Some Values.CancelBundleTaskResult.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_capacity_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and capacityReservationId =
         flag "capacity-reservation-id" (required string)
           ~doc:"STRING CapacityReservationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_capacity_reservation
           (Values.CancelCapacityReservationRequest.make ?dryRun
              ~capacityReservationId ())
           (Some Values.CancelCapacityReservationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_capacity_reservation_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and capacityReservationFleetIds =
         flag "capacity-reservation-fleet-ids" (required json_arg)
           ~doc:"JSON CapacityReservationFleetIdSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_capacity_reservation_fleets
           (Values.CancelCapacityReservationFleetsRequest.make ?dryRun
              ~capacityReservationFleetIds:(Values.CapacityReservationFleetIdSet.of_json
                                              capacityReservationFleetIds) ())
           (Some Values.CancelCapacityReservationFleetsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_conversion_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and reasonMessage =
         flag "reason-message" (optional string) ~doc:"STRING String"
       and conversionTaskId =
         flag "conversion-task-id" (required string)
           ~doc:"STRING ConversionTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_conversion_task
           (Values.CancelConversionRequest.make ?dryRun ?reasonMessage
              ~conversionTaskId ()) None (Some Values.Ec2_error.to_json)])
let cancel_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportTaskId =
         flag "export-task-id" (required string) ~doc:"STRING ExportVmTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_export_task
           (Values.CancelExportTaskRequest.make ~exportTaskId ()) None
           (Some Values.Ec2_error.to_json)])
let cancel_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cancelReason =
         flag "cancel-reason" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and importTaskId =
         flag "import-task-id" (optional string) ~doc:"STRING ImportTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_import_task
           (Values.CancelImportTaskRequest.make ?cancelReason ?dryRun
              ?importTaskId ()) (Some Values.CancelImportTaskResult.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_reserved_instances_listing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedInstancesListingId =
         flag "reserved-instances-listing-id" (required string)
           ~doc:"STRING ReservedInstancesListingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_reserved_instances_listing
           (Values.CancelReservedInstancesListingRequest.make
              ~reservedInstancesListingId ())
           (Some Values.CancelReservedInstancesListingResult.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_spot_fleet_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and spotFleetRequestIds =
         flag "spot-fleet-request-ids" (required json_arg)
           ~doc:"JSON SpotFleetRequestIdList"
       and terminateInstances =
         flag "terminate-instances" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_spot_fleet_requests
           (Values.CancelSpotFleetRequestsRequest.make ?dryRun
              ~spotFleetRequestIds:(Values.SpotFleetRequestIdList.of_json
                                      spotFleetRequestIds)
              ~terminateInstances ())
           (Some Values.CancelSpotFleetRequestsResponse.to_json)
           (Some Values.Ec2_error.to_json)])
let cancel_spot_instance_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and spotInstanceRequestIds =
         flag "spot-instance-request-ids" (required json_arg)
           ~doc:"JSON SpotInstanceRequestIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_spot_instance_requests
           (Values.CancelSpotInstanceRequestsRequest.make ?dryRun
              ~spotInstanceRequestIds:(Values.SpotInstanceRequestIdList.of_json
                                         spotInstanceRequestIds) ())
           (Some Values.CancelSpotInstanceRequestsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let confirm_product_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and productCode =
         flag "product-code" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.confirm_product_instance
           (Values.ConfirmProductInstanceRequest.make ?dryRun ~instanceId
              ~productCode ())
           (Some Values.ConfirmProductInstanceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let copy_fpga_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and sourceFpgaImageId =
         flag "source-fpga-image-id" (required string) ~doc:"STRING String"
       and sourceRegion =
         flag "source-region" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_fpga_image
           (Values.CopyFpgaImageRequest.make ?dryRun ?description ?name
              ?clientToken ~sourceFpgaImageId ~sourceRegion ())
           (Some Values.CopyFpgaImageResult.to_json)
           (Some Values.Ec2_error.to_json)])
let copy_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and destinationOutpostArn =
         flag "destination-outpost-arn" (optional string)
           ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING String"
       and sourceImageId =
         flag "source-image-id" (required string) ~doc:"STRING String"
       and sourceRegion =
         flag "source-region" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_image
           (Values.CopyImageRequest.make ?clientToken ?description ?encrypted
              ?kmsKeyId ?destinationOutpostArn ?dryRun ~name ~sourceImageId
              ~sourceRegion ()) (Some Values.CopyImageResult.to_json)
           (Some Values.Ec2_error.to_json)])
let copy_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and destinationOutpostArn =
         flag "destination-outpost-arn" (optional string)
           ~doc:"STRING String"
       and destinationRegion =
         flag "destination-region" (optional string) ~doc:"STRING String"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and presignedUrl =
         flag "presigned-url" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and sourceRegion =
         flag "source-region" (required string) ~doc:"STRING String"
       and sourceSnapshotId =
         flag "source-snapshot-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_snapshot
           (Values.CopySnapshotRequest.make ?description
              ?destinationOutpostArn ?destinationRegion ?encrypted ?kmsKeyId
              ?presignedUrl
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ~sourceRegion
              ~sourceSnapshotId ()) (Some Values.CopySnapshotResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_capacity_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and availabilityZoneId =
         flag "availability-zone-id" (optional string) ~doc:"STRING String"
       and tenancy =
         flag "tenancy" (optional json_arg)
           ~doc:"JSON CapacityReservationTenancy"
       and ebsOptimized =
         flag "ebs-optimized" (optional bool) ~doc:"BOOL Boolean"
       and ephemeralStorage =
         flag "ephemeral-storage" (optional bool) ~doc:"BOOL Boolean"
       and endDate = flag "end-date" (optional json_arg) ~doc:"JSON DateTime"
       and endDateType =
         flag "end-date-type" (optional json_arg) ~doc:"JSON EndDateType"
       and instanceMatchCriteria =
         flag "instance-match-criteria" (optional json_arg)
           ~doc:"JSON InstanceMatchCriteria"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and outpostArn =
         flag "outpost-arn" (optional string) ~doc:"STRING OutpostArn"
       and placementGroupArn =
         flag "placement-group-arn" (optional string)
           ~doc:"STRING PlacementGroupArn"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING String"
       and instancePlatform =
         flag "instance-platform" (required json_arg)
           ~doc:"JSON CapacityReservationInstancePlatform"
       and instanceCount =
         flag "instance-count" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_capacity_reservation
           (Values.CreateCapacityReservationRequest.make ?clientToken
              ?availabilityZone ?availabilityZoneId
              ?tenancy:(Option.map
                          ~f:Values.CapacityReservationTenancy.of_json
                          tenancy) ?ebsOptimized ?ephemeralStorage
              ?endDate:(Option.map ~f:Values.DateTime.of_json endDate)
              ?endDateType:(Option.map ~f:Values.EndDateType.of_json
                              endDateType)
              ?instanceMatchCriteria:(Option.map
                                        ~f:Values.InstanceMatchCriteria.of_json
                                        instanceMatchCriteria)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ?outpostArn
              ?placementGroupArn ~instanceType
              ~instancePlatform:(Values.CapacityReservationInstancePlatform.of_json
                                   instancePlatform) ~instanceCount ())
           (Some Values.CreateCapacityReservationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_capacity_reservation_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocationStrategy =
         flag "allocation-strategy" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tenancy =
         flag "tenancy" (optional json_arg)
           ~doc:"JSON FleetCapacityReservationTenancy"
       and endDate =
         flag "end-date" (optional json_arg) ~doc:"JSON MillisecondDateTime"
       and instanceMatchCriteria =
         flag "instance-match-criteria" (optional json_arg)
           ~doc:"JSON FleetInstanceMatchCriteria"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceTypeSpecifications =
         flag "instance-type-specifications" (required json_arg)
           ~doc:"JSON ReservationFleetInstanceSpecificationList"
       and totalTargetCapacity =
         flag "total-target-capacity" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_capacity_reservation_fleet
           (Values.CreateCapacityReservationFleetRequest.make
              ?allocationStrategy ?clientToken
              ?tenancy:(Option.map
                          ~f:Values.FleetCapacityReservationTenancy.of_json
                          tenancy)
              ?endDate:(Option.map ~f:Values.MillisecondDateTime.of_json
                          endDate)
              ?instanceMatchCriteria:(Option.map
                                        ~f:Values.FleetInstanceMatchCriteria.of_json
                                        instanceMatchCriteria)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~instanceTypeSpecifications:(Values.ReservationFleetInstanceSpecificationList.of_json
                                             instanceTypeSpecifications)
              ~totalTargetCapacity ())
           (Some Values.CreateCapacityReservationFleetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_carrier_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_carrier_gateway
           (Values.CreateCarrierGatewayRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ?clientToken
              ~vpcId ()) (Some Values.CreateCarrierGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_client_vpn_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dnsServers =
         flag "dns-servers" (optional json_arg) ~doc:"JSON ValueStringList"
       and transportProtocol =
         flag "transport-protocol" (optional json_arg)
           ~doc:"JSON TransportProtocol"
       and vpnPort = flag "vpn-port" (optional int) ~doc:"INT Integer"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and splitTunnel =
         flag "split-tunnel" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON ClientVpnSecurityGroupIdSet"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING VpcId"
       and selfServicePortal =
         flag "self-service-portal" (optional json_arg)
           ~doc:"JSON SelfServicePortal"
       and clientConnectOptions =
         flag "client-connect-options" (optional json_arg)
           ~doc:"JSON ClientConnectOptions"
       and sessionTimeoutHours =
         flag "session-timeout-hours" (optional int) ~doc:"INT Integer"
       and clientLoginBannerOptions =
         flag "client-login-banner-options" (optional json_arg)
           ~doc:"JSON ClientLoginBannerOptions"
       and clientCidrBlock =
         flag "client-cidr-block" (required string) ~doc:"STRING String"
       and serverCertificateArn =
         flag "server-certificate-arn" (required string) ~doc:"STRING String"
       and authenticationOptions =
         flag "authentication-options" (required json_arg)
           ~doc:"JSON ClientVpnAuthenticationRequestList"
       and connectionLogOptions =
         flag "connection-log-options" (required json_arg)
           ~doc:"JSON ConnectionLogOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_client_vpn_endpoint
           (Values.CreateClientVpnEndpointRequest.make
              ?dnsServers:(Option.map ~f:Values.ValueStringList.of_json
                             dnsServers)
              ?transportProtocol:(Option.map
                                    ~f:Values.TransportProtocol.of_json
                                    transportProtocol) ?vpnPort ?description
              ?splitTunnel ?dryRun ?clientToken
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ?securityGroupIds:(Option.map
                                   ~f:Values.ClientVpnSecurityGroupIdSet.of_json
                                   securityGroupIds) ?vpcId
              ?selfServicePortal:(Option.map
                                    ~f:Values.SelfServicePortal.of_json
                                    selfServicePortal)
              ?clientConnectOptions:(Option.map
                                       ~f:Values.ClientConnectOptions.of_json
                                       clientConnectOptions)
              ?sessionTimeoutHours
              ?clientLoginBannerOptions:(Option.map
                                           ~f:Values.ClientLoginBannerOptions.of_json
                                           clientLoginBannerOptions)
              ~clientCidrBlock ~serverCertificateArn
              ~authenticationOptions:(Values.ClientVpnAuthenticationRequestList.of_json
                                        authenticationOptions)
              ~connectionLogOptions:(Values.ConnectionLogOptions.of_json
                                       connectionLogOptions) ())
           (Some Values.CreateClientVpnEndpointResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_client_vpn_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and targetVpcSubnetId =
         flag "target-vpc-subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_client_vpn_route
           (Values.CreateClientVpnRouteRequest.make ?description ?clientToken
              ?dryRun ~clientVpnEndpointId ~destinationCidrBlock
              ~targetVpcSubnetId ())
           (Some Values.CreateClientVpnRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_customer_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publicIp = flag "public-ip" (optional string) ~doc:"STRING String"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and deviceName =
         flag "device-name" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and bgpAsn = flag "bgp-asn" (required int) ~doc:"INT Integer"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON GatewayType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_customer_gateway
           (Values.CreateCustomerGatewayRequest.make ?publicIp
              ?certificateArn
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?deviceName ?dryRun
              ~bgpAsn ~type_:(Values.GatewayType.of_json type_) ())
           (Some Values.CreateCustomerGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_default_subnet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipv6Native =
         flag "ipv6-native" (optional bool) ~doc:"BOOL Boolean"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_default_subnet
           (Values.CreateDefaultSubnetRequest.make ?dryRun ?ipv6Native
              ~availabilityZone ())
           (Some Values.CreateDefaultSubnetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_default_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_default_vpc
           (Values.CreateDefaultVpcRequest.make ?dryRun ())
           (Some Values.CreateDefaultVpcResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_dhcp_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and dhcpConfigurations =
         flag "dhcp-configurations" (required json_arg)
           ~doc:"JSON NewDhcpConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dhcp_options
           (Values.CreateDhcpOptionsRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~dhcpConfigurations:(Values.NewDhcpConfigurationList.of_json
                                     dhcpConfigurations) ())
           (Some Values.CreateDhcpOptionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_egress_only_internet_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_egress_only_internet_gateway
           (Values.CreateEgressOnlyInternetGatewayRequest.make ?clientToken
              ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~vpcId ())
           (Some Values.CreateEgressOnlyInternetGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and spotOptions =
         flag "spot-options" (optional json_arg)
           ~doc:"JSON SpotOptionsRequest"
       and onDemandOptions =
         flag "on-demand-options" (optional json_arg)
           ~doc:"JSON OnDemandOptionsRequest"
       and excessCapacityTerminationPolicy =
         flag "excess-capacity-termination-policy" (optional json_arg)
           ~doc:"JSON FleetExcessCapacityTerminationPolicy"
       and terminateInstancesWithExpiration =
         flag "terminate-instances-with-expiration" (optional bool)
           ~doc:"BOOL Boolean"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON FleetType"
       and validFrom =
         flag "valid-from" (optional json_arg) ~doc:"JSON DateTime"
       and validUntil =
         flag "valid-until" (optional json_arg) ~doc:"JSON DateTime"
       and replaceUnhealthyInstances =
         flag "replace-unhealthy-instances" (optional bool)
           ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and context = flag "context" (optional string) ~doc:"STRING String"
       and launchTemplateConfigs =
         flag "launch-template-configs" (required json_arg)
           ~doc:"JSON FleetLaunchTemplateConfigListRequest"
       and targetCapacitySpecification =
         flag "target-capacity-specification" (required json_arg)
           ~doc:"JSON TargetCapacitySpecificationRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetRequest.make ?dryRun ?clientToken
              ?spotOptions:(Option.map ~f:Values.SpotOptionsRequest.of_json
                              spotOptions)
              ?onDemandOptions:(Option.map
                                  ~f:Values.OnDemandOptionsRequest.of_json
                                  onDemandOptions)
              ?excessCapacityTerminationPolicy:(Option.map
                                                  ~f:Values.FleetExcessCapacityTerminationPolicy.of_json
                                                  excessCapacityTerminationPolicy)
              ?terminateInstancesWithExpiration
              ?type_:(Option.map ~f:Values.FleetType.of_json type_)
              ?validFrom:(Option.map ~f:Values.DateTime.of_json validFrom)
              ?validUntil:(Option.map ~f:Values.DateTime.of_json validUntil)
              ?replaceUnhealthyInstances
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?context
              ~launchTemplateConfigs:(Values.FleetLaunchTemplateConfigListRequest.of_json
                                        launchTemplateConfigs)
              ~targetCapacitySpecification:(Values.TargetCapacitySpecificationRequest.of_json
                                              targetCapacitySpecification) ())
           (Some Values.CreateFleetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_flow_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and deliverLogsPermissionArn =
         flag "deliver-logs-permission-arn" (optional string)
           ~doc:"STRING String"
       and logGroupName =
         flag "log-group-name" (optional string) ~doc:"STRING String"
       and logDestinationType =
         flag "log-destination-type" (optional json_arg)
           ~doc:"JSON LogDestinationType"
       and logDestination =
         flag "log-destination" (optional string) ~doc:"STRING String"
       and logFormat =
         flag "log-format" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and maxAggregationInterval =
         flag "max-aggregation-interval" (optional int) ~doc:"INT Integer"
       and destinationOptions =
         flag "destination-options" (optional json_arg)
           ~doc:"JSON DestinationOptionsRequest"
       and resourceIds =
         flag "resource-ids" (required json_arg)
           ~doc:"JSON FlowLogResourceIds"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON FlowLogsResourceType"
       and trafficType =
         flag "traffic-type" (required json_arg) ~doc:"JSON TrafficType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow_logs
           (Values.CreateFlowLogsRequest.make ?dryRun ?clientToken
              ?deliverLogsPermissionArn ?logGroupName
              ?logDestinationType:(Option.map
                                     ~f:Values.LogDestinationType.of_json
                                     logDestinationType) ?logDestination
              ?logFormat
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ?maxAggregationInterval
              ?destinationOptions:(Option.map
                                     ~f:Values.DestinationOptionsRequest.of_json
                                     destinationOptions)
              ~resourceIds:(Values.FlowLogResourceIds.of_json resourceIds)
              ~resourceType:(Values.FlowLogsResourceType.of_json resourceType)
              ~trafficType:(Values.TrafficType.of_json trafficType) ())
           (Some Values.CreateFlowLogsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_fpga_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and logsStorageLocation =
         flag "logs-storage-location" (optional json_arg)
           ~doc:"JSON StorageLocation"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and inputStorageLocation =
         flag "input-storage-location" (required json_arg)
           ~doc:"JSON StorageLocation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fpga_image
           (Values.CreateFpgaImageRequest.make ?dryRun
              ?logsStorageLocation:(Option.map
                                      ~f:Values.StorageLocation.of_json
                                      logsStorageLocation) ?description ?name
              ?clientToken
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ~inputStorageLocation:(Values.StorageLocation.of_json
                                       inputStorageLocation) ())
           (Some Values.CreateFpgaImageResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blockDeviceMappings =
         flag "block-device-mappings" (optional json_arg)
           ~doc:"JSON BlockDeviceMappingRequestList"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and noReboot = flag "no-reboot" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_image
           (Values.CreateImageRequest.make
              ?blockDeviceMappings:(Option.map
                                      ~f:Values.BlockDeviceMappingRequestList.of_json
                                      blockDeviceMappings) ?description
              ?dryRun ?noReboot
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~instanceId ~name ())
           (Some Values.CreateImageResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_instance_event_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and timeRanges =
         flag "time-ranges" (optional json_arg)
           ~doc:"JSON InstanceEventWindowTimeRangeRequestSet"
       and cronExpression =
         flag "cron-expression" (optional string)
           ~doc:"STRING InstanceEventWindowCronExpression"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance_event_window
           (Values.CreateInstanceEventWindowRequest.make ?dryRun ?name
              ?timeRanges:(Option.map
                             ~f:Values.InstanceEventWindowTimeRangeRequestSet.of_json
                             timeRanges) ?cronExpression
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.CreateInstanceEventWindowResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_instance_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and exportToS3Task =
         flag "export-to-s3-task" (required json_arg)
           ~doc:"JSON ExportToS3TaskSpecification"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and targetEnvironment =
         flag "target-environment" (required json_arg)
           ~doc:"JSON ExportEnvironment" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance_export_task
           (Values.CreateInstanceExportTaskRequest.make ?description
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ~exportToS3Task:(Values.ExportToS3TaskSpecification.of_json
                                 exportToS3Task) ~instanceId
              ~targetEnvironment:(Values.ExportEnvironment.of_json
                                    targetEnvironment) ())
           (Some Values.CreateInstanceExportTaskResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_internet_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_internet_gateway
           (Values.CreateInternetGatewayRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ())
           (Some Values.CreateInternetGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_ipam =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and operatingRegions =
         flag "operating-regions" (optional json_arg)
           ~doc:"JSON AddIpamOperatingRegionSet"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ipam
           (Values.CreateIpamRequest.make ?dryRun ?description
              ?operatingRegions:(Option.map
                                   ~f:Values.AddIpamOperatingRegionSet.of_json
                                   operatingRegions)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?clientToken ())
           (Some Values.CreateIpamResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_ipam_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and locale = flag "locale" (optional string) ~doc:"STRING String"
       and sourceIpamPoolId =
         flag "source-ipam-pool-id" (optional string)
           ~doc:"STRING IpamPoolId"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and autoImport =
         flag "auto-import" (optional bool) ~doc:"BOOL Boolean"
       and publiclyAdvertisable =
         flag "publicly-advertisable" (optional bool) ~doc:"BOOL Boolean"
       and allocationMinNetmaskLength =
         flag "allocation-min-netmask-length" (optional int)
           ~doc:"INT IpamNetmaskLength"
       and allocationMaxNetmaskLength =
         flag "allocation-max-netmask-length" (optional int)
           ~doc:"INT IpamNetmaskLength"
       and allocationDefaultNetmaskLength =
         flag "allocation-default-netmask-length" (optional int)
           ~doc:"INT IpamNetmaskLength"
       and allocationResourceTags =
         flag "allocation-resource-tags" (optional json_arg)
           ~doc:"JSON RequestIpamResourceTagList"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and awsService =
         flag "aws-service" (optional json_arg)
           ~doc:"JSON IpamPoolAwsService"
       and ipamScopeId =
         flag "ipam-scope-id" (required string) ~doc:"STRING IpamScopeId"
       and addressFamily =
         flag "address-family" (required json_arg) ~doc:"JSON AddressFamily" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ipam_pool
           (Values.CreateIpamPoolRequest.make ?dryRun ?locale
              ?sourceIpamPoolId ?description ?autoImport
              ?publiclyAdvertisable ?allocationMinNetmaskLength
              ?allocationMaxNetmaskLength ?allocationDefaultNetmaskLength
              ?allocationResourceTags:(Option.map
                                         ~f:Values.RequestIpamResourceTagList.of_json
                                         allocationResourceTags)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?clientToken
              ?awsService:(Option.map ~f:Values.IpamPoolAwsService.of_json
                             awsService) ~ipamScopeId
              ~addressFamily:(Values.AddressFamily.of_json addressFamily) ())
           (Some Values.CreateIpamPoolResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_ipam_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and ipamId = flag "ipam-id" (required string) ~doc:"STRING IpamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ipam_scope
           (Values.CreateIpamScopeRequest.make ?dryRun ?description
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?clientToken ~ipamId
              ()) (Some Values.CreateIpamScopeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and keyType = flag "key-type" (optional json_arg) ~doc:"JSON KeyType"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and keyName = flag "key-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_key_pair
           (Values.CreateKeyPairRequest.make ?dryRun
              ?keyType:(Option.map ~f:Values.KeyType.of_json keyType)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~keyName ())
           (Some Values.KeyPair.to_json) (Some Values.Ec2_error.to_json)])
let create_launch_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and launchTemplateName =
         flag "launch-template-name" (required string)
           ~doc:"STRING LaunchTemplateName"
       and launchTemplateData =
         flag "launch-template-data" (required json_arg)
           ~doc:"JSON RequestLaunchTemplateData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_launch_template
           (Values.CreateLaunchTemplateRequest.make ?dryRun ?clientToken
              ?versionDescription
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~launchTemplateName
              ~launchTemplateData:(Values.RequestLaunchTemplateData.of_json
                                     launchTemplateData) ())
           (Some Values.CreateLaunchTemplateResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_launch_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and launchTemplateId =
         flag "launch-template-id" (optional string)
           ~doc:"STRING LaunchTemplateId"
       and launchTemplateName =
         flag "launch-template-name" (optional string)
           ~doc:"STRING LaunchTemplateName"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and launchTemplateData =
         flag "launch-template-data" (required json_arg)
           ~doc:"JSON RequestLaunchTemplateData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_launch_template_version
           (Values.CreateLaunchTemplateVersionRequest.make ?dryRun
              ?clientToken ?launchTemplateId ?launchTemplateName
              ?sourceVersion ?versionDescription
              ~launchTemplateData:(Values.RequestLaunchTemplateData.of_json
                                     launchTemplateData) ())
           (Some Values.CreateLaunchTemplateVersionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_local_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and localGatewayRouteTableId =
         flag "local-gateway-route-table-id" (required string)
           ~doc:"STRING LocalGatewayRoutetableId"
       and localGatewayVirtualInterfaceGroupId =
         flag "local-gateway-virtual-interface-group-id" (required string)
           ~doc:"STRING LocalGatewayVirtualInterfaceGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_local_gateway_route
           (Values.CreateLocalGatewayRouteRequest.make ?dryRun
              ~destinationCidrBlock ~localGatewayRouteTableId
              ~localGatewayVirtualInterfaceGroupId ())
           (Some Values.CreateLocalGatewayRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_local_gateway_route_table_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayRouteTableId =
         flag "local-gateway-route-table-id" (required string)
           ~doc:"STRING LocalGatewayRoutetableId"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_local_gateway_route_table_vpc_association
           (Values.CreateLocalGatewayRouteTableVpcAssociationRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~localGatewayRouteTableId ~vpcId ())
           (Some
              Values.CreateLocalGatewayRouteTableVpcAssociationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_managed_prefix_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and entries =
         flag "entries" (optional json_arg) ~doc:"JSON AddPrefixListEntries"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and prefixListName =
         flag "prefix-list-name" (required string) ~doc:"STRING String"
       and maxEntries = flag "max-entries" (required int) ~doc:"INT Integer"
       and addressFamily =
         flag "address-family" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_managed_prefix_list
           (Values.CreateManagedPrefixListRequest.make ?dryRun
              ?entries:(Option.map ~f:Values.AddPrefixListEntries.of_json
                          entries)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?clientToken
              ~prefixListName ~maxEntries ~addressFamily ())
           (Some Values.CreateManagedPrefixListResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_nat_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocationId =
         flag "allocation-id" (optional string) ~doc:"STRING AllocationId"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and connectivityType =
         flag "connectivity-type" (optional json_arg)
           ~doc:"JSON ConnectivityType"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_nat_gateway
           (Values.CreateNatGatewayRequest.make ?allocationId ?clientToken
              ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ?connectivityType:(Option.map
                                   ~f:Values.ConnectivityType.of_json
                                   connectivityType) ~subnetId ())
           (Some Values.CreateNatGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_network_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_acl
           (Values.CreateNetworkAclRequest.make ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~vpcId ())
           (Some Values.CreateNetworkAclResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_network_acl_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidrBlock =
         flag "cidr-block" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and icmpTypeCode =
         flag "icmp-type-code" (optional json_arg) ~doc:"JSON IcmpTypeCode"
       and ipv6CidrBlock =
         flag "ipv6-cidr-block" (optional string) ~doc:"STRING String"
       and portRange =
         flag "port-range" (optional json_arg) ~doc:"JSON PortRange"
       and egress = flag "egress" (required bool) ~doc:"BOOL Boolean"
       and networkAclId =
         flag "network-acl-id" (required string) ~doc:"STRING NetworkAclId"
       and protocol = flag "protocol" (required string) ~doc:"STRING String"
       and ruleAction =
         flag "rule-action" (required json_arg) ~doc:"JSON RuleAction"
       and ruleNumber = flag "rule-number" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_acl_entry
           (Values.CreateNetworkAclEntryRequest.make ?cidrBlock ?dryRun
              ?icmpTypeCode:(Option.map ~f:Values.IcmpTypeCode.of_json
                               icmpTypeCode) ?ipv6CidrBlock
              ?portRange:(Option.map ~f:Values.PortRange.of_json portRange)
              ~egress ~networkAclId ~protocol
              ~ruleAction:(Values.RuleAction.of_json ruleAction) ~ruleNumber
              ()) None (Some Values.Ec2_error.to_json)])
let create_network_insights_access_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and matchPaths =
         flag "match-paths" (optional json_arg)
           ~doc:"JSON AccessScopePathListRequest"
       and excludePaths =
         flag "exclude-paths" (optional json_arg)
           ~doc:"JSON AccessScopePathListRequest"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_insights_access_scope
           (Values.CreateNetworkInsightsAccessScopeRequest.make
              ?matchPaths:(Option.map
                             ~f:Values.AccessScopePathListRequest.of_json
                             matchPaths)
              ?excludePaths:(Option.map
                               ~f:Values.AccessScopePathListRequest.of_json
                               excludePaths)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ~clientToken
              ())
           (Some Values.CreateNetworkInsightsAccessScopeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_network_insights_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceIp =
         flag "source-ip" (optional string) ~doc:"STRING IpAddress"
       and destinationIp =
         flag "destination-ip" (optional string) ~doc:"STRING IpAddress"
       and destinationPort =
         flag "destination-port" (optional int) ~doc:"INT Port"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and source =
         flag "source" (required string)
           ~doc:"STRING NetworkInsightsResourceId"
       and destination =
         flag "destination" (required string)
           ~doc:"STRING NetworkInsightsResourceId"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON Protocol"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_insights_path
           (Values.CreateNetworkInsightsPathRequest.make ?sourceIp
              ?destinationIp ?destinationPort
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ~source
              ~destination ~protocol:(Values.Protocol.of_json protocol)
              ~clientToken ())
           (Some Values.CreateNetworkInsightsPathResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and groups =
         flag "groups" (optional json_arg)
           ~doc:"JSON SecurityGroupIdStringList"
       and ipv6AddressCount =
         flag "ipv6-address-count" (optional int) ~doc:"INT Integer"
       and ipv6Addresses =
         flag "ipv6-addresses" (optional json_arg)
           ~doc:"JSON InstanceIpv6AddressList"
       and privateIpAddress =
         flag "private-ip-address" (optional string) ~doc:"STRING String"
       and privateIpAddresses =
         flag "private-ip-addresses" (optional json_arg)
           ~doc:"JSON PrivateIpAddressSpecificationList"
       and secondaryPrivateIpAddressCount =
         flag "secondary-private-ip-address-count" (optional int)
           ~doc:"INT Integer"
       and ipv4Prefixes =
         flag "ipv4-prefixes" (optional json_arg) ~doc:"JSON Ipv4PrefixList"
       and ipv4PrefixCount =
         flag "ipv4-prefix-count" (optional int) ~doc:"INT Integer"
       and ipv6Prefixes =
         flag "ipv6-prefixes" (optional json_arg) ~doc:"JSON Ipv6PrefixList"
       and ipv6PrefixCount =
         flag "ipv6-prefix-count" (optional int) ~doc:"INT Integer"
       and interfaceType =
         flag "interface-type" (optional json_arg)
           ~doc:"JSON NetworkInterfaceCreationType"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_interface
           (Values.CreateNetworkInterfaceRequest.make ?description ?dryRun
              ?groups:(Option.map ~f:Values.SecurityGroupIdStringList.of_json
                         groups) ?ipv6AddressCount
              ?ipv6Addresses:(Option.map
                                ~f:Values.InstanceIpv6AddressList.of_json
                                ipv6Addresses) ?privateIpAddress
              ?privateIpAddresses:(Option.map
                                     ~f:Values.PrivateIpAddressSpecificationList.of_json
                                     privateIpAddresses)
              ?secondaryPrivateIpAddressCount
              ?ipv4Prefixes:(Option.map ~f:Values.Ipv4PrefixList.of_json
                               ipv4Prefixes) ?ipv4PrefixCount
              ?ipv6Prefixes:(Option.map ~f:Values.Ipv6PrefixList.of_json
                               ipv6Prefixes) ?ipv6PrefixCount
              ?interfaceType:(Option.map
                                ~f:Values.NetworkInterfaceCreationType.of_json
                                interfaceType)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?clientToken ~subnetId
              ()) (Some Values.CreateNetworkInterfaceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_network_interface_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING String"
       and awsService =
         flag "aws-service" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId"
       and permission =
         flag "permission" (required json_arg)
           ~doc:"JSON InterfacePermissionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_interface_permission
           (Values.CreateNetworkInterfacePermissionRequest.make ?awsAccountId
              ?awsService ?dryRun ~networkInterfaceId
              ~permission:(Values.InterfacePermissionType.of_json permission)
              ())
           (Some Values.CreateNetworkInterfacePermissionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING String"
       and strategy =
         flag "strategy" (optional json_arg) ~doc:"JSON PlacementStrategy"
       and partitionCount =
         flag "partition-count" (optional int) ~doc:"INT Integer"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_placement_group
           (Values.CreatePlacementGroupRequest.make ?dryRun ?groupName
              ?strategy:(Option.map ~f:Values.PlacementStrategy.of_json
                           strategy) ?partitionCount
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.CreatePlacementGroupResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_public_ipv4_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_public_ipv4_pool
           (Values.CreatePublicIpv4PoolRequest.make ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.CreatePublicIpv4PoolResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_replace_root_volume_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotId =
         flag "snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replace_root_volume_task
           (Values.CreateReplaceRootVolumeTaskRequest.make ?snapshotId
              ?clientToken ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~instanceId ())
           (Some Values.CreateReplaceRootVolumeTaskResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_reserved_instances_listing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and instanceCount =
         flag "instance-count" (required int) ~doc:"INT Integer"
       and priceSchedules =
         flag "price-schedules" (required json_arg)
           ~doc:"JSON PriceScheduleSpecificationList"
       and reservedInstancesId =
         flag "reserved-instances-id" (required string)
           ~doc:"STRING ReservationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_reserved_instances_listing
           (Values.CreateReservedInstancesListingRequest.make ~clientToken
              ~instanceCount
              ~priceSchedules:(Values.PriceScheduleSpecificationList.of_json
                                 priceSchedules) ~reservedInstancesId ())
           (Some Values.CreateReservedInstancesListingResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_restore_image_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and bucket = flag "bucket" (required string) ~doc:"STRING String"
       and objectKey =
         flag "object-key" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_restore_image_task
           (Values.CreateRestoreImageTaskRequest.make ?name
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ~bucket
              ~objectKey ())
           (Some Values.CreateRestoreImageTaskResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationCidrBlock =
         flag "destination-cidr-block" (optional string) ~doc:"STRING String"
       and destinationIpv6CidrBlock =
         flag "destination-ipv6-cidr-block" (optional string)
           ~doc:"STRING String"
       and destinationPrefixListId =
         flag "destination-prefix-list-id" (optional string)
           ~doc:"STRING PrefixListResourceId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcEndpointId =
         flag "vpc-endpoint-id" (optional string) ~doc:"STRING VpcEndpointId"
       and egressOnlyInternetGatewayId =
         flag "egress-only-internet-gateway-id" (optional string)
           ~doc:"STRING EgressOnlyInternetGatewayId"
       and gatewayId =
         flag "gateway-id" (optional string) ~doc:"STRING RouteGatewayId"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and natGatewayId =
         flag "nat-gateway-id" (optional string) ~doc:"STRING NatGatewayId"
       and transitGatewayId =
         flag "transit-gateway-id" (optional string)
           ~doc:"STRING TransitGatewayId"
       and localGatewayId =
         flag "local-gateway-id" (optional string)
           ~doc:"STRING LocalGatewayId"
       and carrierGatewayId =
         flag "carrier-gateway-id" (optional string)
           ~doc:"STRING CarrierGatewayId"
       and networkInterfaceId =
         flag "network-interface-id" (optional string)
           ~doc:"STRING NetworkInterfaceId"
       and vpcPeeringConnectionId =
         flag "vpc-peering-connection-id" (optional string)
           ~doc:"STRING VpcPeeringConnectionId"
       and coreNetworkArn =
         flag "core-network-arn" (optional string)
           ~doc:"STRING CoreNetworkArn"
       and routeTableId =
         flag "route-table-id" (required string) ~doc:"STRING RouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route
           (Values.CreateRouteRequest.make ?destinationCidrBlock
              ?destinationIpv6CidrBlock ?destinationPrefixListId ?dryRun
              ?vpcEndpointId ?egressOnlyInternetGatewayId ?gatewayId
              ?instanceId ?natGatewayId ?transitGatewayId ?localGatewayId
              ?carrierGatewayId ?networkInterfaceId ?vpcPeeringConnectionId
              ?coreNetworkArn ~routeTableId ())
           (Some Values.CreateRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route_table
           (Values.CreateRouteTableRequest.make ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~vpcId ())
           (Some Values.CreateRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING VpcId"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and description =
         flag "description" (required string) ~doc:"STRING String"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_group
           (Values.CreateSecurityGroupRequest.make ?vpcId
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ~description
              ~groupName ()) (Some Values.CreateSecurityGroupResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and outpostArn =
         flag "outpost-arn" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotRequest.make ?description ?outpostArn
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ~volumeId ())
           (Some Values.Snapshot.to_json) (Some Values.Ec2_error.to_json)])
let create_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and outpostArn =
         flag "outpost-arn" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and copyTagsFromSource =
         flag "copy-tags-from-source" (optional json_arg)
           ~doc:"JSON CopyTagsFromSource"
       and instanceSpecification =
         flag "instance-specification" (required json_arg)
           ~doc:"JSON InstanceSpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshots
           (Values.CreateSnapshotsRequest.make ?description ?outpostArn
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ?copyTagsFromSource:(Option.map
                                     ~f:Values.CopyTagsFromSource.of_json
                                     copyTagsFromSource)
              ~instanceSpecification:(Values.InstanceSpecification.of_json
                                        instanceSpecification) ())
           (Some Values.CreateSnapshotsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_spot_datafeed_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and prefix = flag "prefix" (optional string) ~doc:"STRING String"
       and bucket = flag "bucket" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_spot_datafeed_subscription
           (Values.CreateSpotDatafeedSubscriptionRequest.make ?dryRun ?prefix
              ~bucket ())
           (Some Values.CreateSpotDatafeedSubscriptionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_store_image_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3ObjectTags =
         flag "s3-object-tags" (optional json_arg)
           ~doc:"JSON S3ObjectTagList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and imageId = flag "image-id" (required string) ~doc:"STRING ImageId"
       and bucket = flag "bucket" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_store_image_task
           (Values.CreateStoreImageTaskRequest.make
              ?s3ObjectTags:(Option.map ~f:Values.S3ObjectTagList.of_json
                               s3ObjectTags) ?dryRun ~imageId ~bucket ())
           (Some Values.CreateStoreImageTaskResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_subnet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and availabilityZoneId =
         flag "availability-zone-id" (optional string) ~doc:"STRING String"
       and cidrBlock =
         flag "cidr-block" (optional string) ~doc:"STRING String"
       and ipv6CidrBlock =
         flag "ipv6-cidr-block" (optional string) ~doc:"STRING String"
       and outpostArn =
         flag "outpost-arn" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipv6Native =
         flag "ipv6-native" (optional bool) ~doc:"BOOL Boolean"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subnet
           (Values.CreateSubnetRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?availabilityZone
              ?availabilityZoneId ?cidrBlock ?ipv6CidrBlock ?outpostArn
              ?dryRun ?ipv6Native ~vpcId ())
           (Some Values.CreateSubnetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_subnet_cidr_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId"
       and cidr = flag "cidr" (required string) ~doc:"STRING String"
       and reservationType =
         flag "reservation-type" (required json_arg)
           ~doc:"JSON SubnetCidrReservationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subnet_cidr_reservation
           (Values.CreateSubnetCidrReservationRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?description ?dryRun
              ~subnetId ~cidr
              ~reservationType:(Values.SubnetCidrReservationType.of_json
                                  reservationType) ())
           (Some Values.CreateSubnetCidrReservationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and resources =
         flag "resources" (required json_arg) ~doc:"JSON ResourceIdList"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tags
           (Values.CreateTagsRequest.make ?dryRun
              ~resources:(Values.ResourceIdList.of_json resources)
              ~tags:(Values.TagList.of_json tags) ()) None
           (Some Values.Ec2_error.to_json)])
let create_traffic_mirror_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_mirror_filter
           (Values.CreateTrafficMirrorFilterRequest.make ?description
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ?clientToken
              ()) (Some Values.CreateTrafficMirrorFilterResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_traffic_mirror_filter_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationPortRange =
         flag "destination-port-range" (optional json_arg)
           ~doc:"JSON TrafficMirrorPortRangeRequest"
       and sourcePortRange =
         flag "source-port-range" (optional json_arg)
           ~doc:"JSON TrafficMirrorPortRangeRequest"
       and protocol = flag "protocol" (optional int) ~doc:"INT Integer"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and trafficMirrorFilterId =
         flag "traffic-mirror-filter-id" (required string)
           ~doc:"STRING TrafficMirrorFilterId"
       and trafficDirection =
         flag "traffic-direction" (required json_arg)
           ~doc:"JSON TrafficDirection"
       and ruleNumber = flag "rule-number" (required int) ~doc:"INT Integer"
       and ruleAction =
         flag "rule-action" (required json_arg)
           ~doc:"JSON TrafficMirrorRuleAction"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and sourceCidrBlock =
         flag "source-cidr-block" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_mirror_filter_rule
           (Values.CreateTrafficMirrorFilterRuleRequest.make
              ?destinationPortRange:(Option.map
                                       ~f:Values.TrafficMirrorPortRangeRequest.of_json
                                       destinationPortRange)
              ?sourcePortRange:(Option.map
                                  ~f:Values.TrafficMirrorPortRangeRequest.of_json
                                  sourcePortRange) ?protocol ?description
              ?dryRun ?clientToken ~trafficMirrorFilterId
              ~trafficDirection:(Values.TrafficDirection.of_json
                                   trafficDirection) ~ruleNumber
              ~ruleAction:(Values.TrafficMirrorRuleAction.of_json ruleAction)
              ~destinationCidrBlock ~sourceCidrBlock ())
           (Some Values.CreateTrafficMirrorFilterRuleResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_traffic_mirror_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packetLength =
         flag "packet-length" (optional int) ~doc:"INT Integer"
       and virtualNetworkId =
         flag "virtual-network-id" (optional int) ~doc:"INT Integer"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId"
       and trafficMirrorTargetId =
         flag "traffic-mirror-target-id" (required string)
           ~doc:"STRING TrafficMirrorTargetId"
       and trafficMirrorFilterId =
         flag "traffic-mirror-filter-id" (required string)
           ~doc:"STRING TrafficMirrorFilterId"
       and sessionNumber =
         flag "session-number" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_mirror_session
           (Values.CreateTrafficMirrorSessionRequest.make ?packetLength
              ?virtualNetworkId ?description
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ?clientToken
              ~networkInterfaceId ~trafficMirrorTargetId
              ~trafficMirrorFilterId ~sessionNumber ())
           (Some Values.CreateTrafficMirrorSessionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_traffic_mirror_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkInterfaceId =
         flag "network-interface-id" (optional string)
           ~doc:"STRING NetworkInterfaceId"
       and networkLoadBalancerArn =
         flag "network-load-balancer-arn" (optional string)
           ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_mirror_target
           (Values.CreateTrafficMirrorTargetRequest.make ?networkInterfaceId
              ?networkLoadBalancerArn ?description
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ?clientToken
              ()) (Some Values.CreateTrafficMirrorTargetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and options =
         flag "options" (optional json_arg)
           ~doc:"JSON TransitGatewayRequestOptions"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway
           (Values.CreateTransitGatewayRequest.make ?description
              ?options:(Option.map
                          ~f:Values.TransitGatewayRequestOptions.of_json
                          options)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun ())
           (Some Values.CreateTransitGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_connect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transportTransitGatewayAttachmentId =
         flag "transport-transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId"
       and options =
         flag "options" (required json_arg)
           ~doc:"JSON CreateTransitGatewayConnectRequestOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_connect
           (Values.CreateTransitGatewayConnectRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~transportTransitGatewayAttachmentId
              ~options:(Values.CreateTransitGatewayConnectRequestOptions.of_json
                          options) ())
           (Some Values.CreateTransitGatewayConnectResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayAddress =
         flag "transit-gateway-address" (optional string)
           ~doc:"STRING String"
       and bgpOptions =
         flag "bgp-options" (optional json_arg)
           ~doc:"JSON TransitGatewayConnectRequestBgpOptions"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId"
       and peerAddress =
         flag "peer-address" (required string) ~doc:"STRING String"
       and insideCidrBlocks =
         flag "inside-cidr-blocks" (required json_arg)
           ~doc:"JSON InsideCidrBlocksStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_connect_peer
           (Values.CreateTransitGatewayConnectPeerRequest.make
              ?transitGatewayAddress
              ?bgpOptions:(Option.map
                             ~f:Values.TransitGatewayConnectRequestBgpOptions.of_json
                             bgpOptions)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~transitGatewayAttachmentId ~peerAddress
              ~insideCidrBlocks:(Values.InsideCidrBlocksStringList.of_json
                                   insideCidrBlocks) ())
           (Some Values.CreateTransitGatewayConnectPeerResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_multicast_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and options =
         flag "options" (optional json_arg)
           ~doc:"JSON CreateTransitGatewayMulticastDomainRequestOptions"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayId =
         flag "transit-gateway-id" (required string)
           ~doc:"STRING TransitGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_multicast_domain
           (Values.CreateTransitGatewayMulticastDomainRequest.make
              ?options:(Option.map
                          ~f:Values.CreateTransitGatewayMulticastDomainRequestOptions.of_json
                          options)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~transitGatewayId ())
           (Some Values.CreateTransitGatewayMulticastDomainResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_peering_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayId =
         flag "transit-gateway-id" (required string)
           ~doc:"STRING TransitGatewayId"
       and peerTransitGatewayId =
         flag "peer-transit-gateway-id" (required string)
           ~doc:"STRING TransitAssociationGatewayId"
       and peerAccountId =
         flag "peer-account-id" (required string) ~doc:"STRING String"
       and peerRegion =
         flag "peer-region" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_peering_attachment
           (Values.CreateTransitGatewayPeeringAttachmentRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~transitGatewayId ~peerTransitGatewayId ~peerAccountId
              ~peerRegion ())
           (Some Values.CreateTransitGatewayPeeringAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_prefix_list_reference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (optional string)
           ~doc:"STRING TransitGatewayAttachmentId"
       and blackhole = flag "blackhole" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId"
       and prefixListId =
         flag "prefix-list-id" (required string)
           ~doc:"STRING PrefixListResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_prefix_list_reference
           (Values.CreateTransitGatewayPrefixListReferenceRequest.make
              ?transitGatewayAttachmentId ?blackhole ?dryRun
              ~transitGatewayRouteTableId ~prefixListId ())
           (Some Values.CreateTransitGatewayPrefixListReferenceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (optional string)
           ~doc:"STRING TransitGatewayAttachmentId"
       and blackhole = flag "blackhole" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_route
           (Values.CreateTransitGatewayRouteRequest.make
              ?transitGatewayAttachmentId ?blackhole ?dryRun
              ~destinationCidrBlock ~transitGatewayRouteTableId ())
           (Some Values.CreateTransitGatewayRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayId =
         flag "transit-gateway-id" (required string)
           ~doc:"STRING TransitGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_route_table
           (Values.CreateTransitGatewayRouteTableRequest.make
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~transitGatewayId ())
           (Some Values.CreateTransitGatewayRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_transit_gateway_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and options =
         flag "options" (optional json_arg)
           ~doc:"JSON CreateTransitGatewayVpcAttachmentRequestOptions"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayId =
         flag "transit-gateway-id" (required string)
           ~doc:"STRING TransitGatewayId"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON TransitGatewaySubnetIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_vpc_attachment
           (Values.CreateTransitGatewayVpcAttachmentRequest.make
              ?options:(Option.map
                          ~f:Values.CreateTransitGatewayVpcAttachmentRequestOptions.of_json
                          options)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?dryRun
              ~transitGatewayId ~vpcId
              ~subnetIds:(Values.TransitGatewaySubnetIdList.of_json subnetIds)
              ())
           (Some Values.CreateTransitGatewayVpcAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Boolean"
       and iops = flag "iops" (optional int) ~doc:"INT Integer"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and outpostArn =
         flag "outpost-arn" (optional string) ~doc:"STRING String"
       and size = flag "size" (optional int) ~doc:"INT Integer"
       and snapshotId =
         flag "snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and volumeType =
         flag "volume-type" (optional json_arg) ~doc:"JSON VolumeType"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and multiAttachEnabled =
         flag "multi-attach-enabled" (optional bool) ~doc:"BOOL Boolean"
       and throughput = flag "throughput" (optional int) ~doc:"INT Integer"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_volume
           (Values.CreateVolumeRequest.make ?encrypted ?iops ?kmsKeyId
              ?outpostArn ?size ?snapshotId
              ?volumeType:(Option.map ~f:Values.VolumeType.of_json volumeType)
              ?dryRun
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ?multiAttachEnabled
              ?throughput ?clientToken ~availabilityZone ())
           (Some Values.Volume.to_json) (Some Values.Ec2_error.to_json)])
let create_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidrBlock =
         flag "cidr-block" (optional string) ~doc:"STRING String"
       and amazonProvidedIpv6CidrBlock =
         flag "amazon-provided-ipv6-cidr-block" (optional bool)
           ~doc:"BOOL Boolean"
       and ipv6Pool =
         flag "ipv6-pool" (optional string) ~doc:"STRING Ipv6PoolEc2Id"
       and ipv6CidrBlock =
         flag "ipv6-cidr-block" (optional string) ~doc:"STRING String"
       and ipv4IpamPoolId =
         flag "ipv4-ipam-pool-id" (optional string) ~doc:"STRING IpamPoolId"
       and ipv4NetmaskLength =
         flag "ipv4-netmask-length" (optional int) ~doc:"INT NetmaskLength"
       and ipv6IpamPoolId =
         flag "ipv6-ipam-pool-id" (optional string) ~doc:"STRING IpamPoolId"
       and ipv6NetmaskLength =
         flag "ipv6-netmask-length" (optional int) ~doc:"INT NetmaskLength"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceTenancy =
         flag "instance-tenancy" (optional json_arg) ~doc:"JSON Tenancy"
       and ipv6CidrBlockNetworkBorderGroup =
         flag "ipv6-cidr-block-network-border-group" (optional string)
           ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc
           (Values.CreateVpcRequest.make ?cidrBlock
              ?amazonProvidedIpv6CidrBlock ?ipv6Pool ?ipv6CidrBlock
              ?ipv4IpamPoolId ?ipv4NetmaskLength ?ipv6IpamPoolId
              ?ipv6NetmaskLength ?dryRun
              ?instanceTenancy:(Option.map ~f:Values.Tenancy.of_json
                                  instanceTenancy)
              ?ipv6CidrBlockNetworkBorderGroup
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.CreateVpcResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_vpc_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcEndpointType =
         flag "vpc-endpoint-type" (optional json_arg)
           ~doc:"JSON VpcEndpointType"
       and policyDocument =
         flag "policy-document" (optional string) ~doc:"STRING String"
       and routeTableIds =
         flag "route-table-ids" (optional json_arg)
           ~doc:"JSON VpcEndpointRouteTableIdList"
       and subnetIds =
         flag "subnet-ids" (optional json_arg)
           ~doc:"JSON VpcEndpointSubnetIdList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON VpcEndpointSecurityGroupIdList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and privateDnsEnabled =
         flag "private-dns-enabled" (optional bool) ~doc:"BOOL Boolean"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_endpoint
           (Values.CreateVpcEndpointRequest.make ?dryRun
              ?vpcEndpointType:(Option.map ~f:Values.VpcEndpointType.of_json
                                  vpcEndpointType) ?policyDocument
              ?routeTableIds:(Option.map
                                ~f:Values.VpcEndpointRouteTableIdList.of_json
                                routeTableIds)
              ?subnetIds:(Option.map
                            ~f:Values.VpcEndpointSubnetIdList.of_json
                            subnetIds)
              ?securityGroupIds:(Option.map
                                   ~f:Values.VpcEndpointSecurityGroupIdList.of_json
                                   securityGroupIds) ?clientToken
              ?privateDnsEnabled
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~vpcId ~serviceName ())
           (Some Values.CreateVpcEndpointResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_vpc_endpoint_connection_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and serviceId =
         flag "service-id" (optional string)
           ~doc:"STRING VpcEndpointServiceId"
       and vpcEndpointId =
         flag "vpc-endpoint-id" (optional string) ~doc:"STRING VpcEndpointId"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and connectionNotificationArn =
         flag "connection-notification-arn" (required string)
           ~doc:"STRING String"
       and connectionEvents =
         flag "connection-events" (required json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_endpoint_connection_notification
           (Values.CreateVpcEndpointConnectionNotificationRequest.make
              ?dryRun ?serviceId ?vpcEndpointId ?clientToken
              ~connectionNotificationArn
              ~connectionEvents:(Values.ValueStringList.of_json
                                   connectionEvents) ())
           (Some Values.CreateVpcEndpointConnectionNotificationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_vpc_endpoint_service_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and acceptanceRequired =
         flag "acceptance-required" (optional bool) ~doc:"BOOL Boolean"
       and privateDnsName =
         flag "private-dns-name" (optional string) ~doc:"STRING String"
       and networkLoadBalancerArns =
         flag "network-load-balancer-arns" (optional json_arg)
           ~doc:"JSON ValueStringList"
       and gatewayLoadBalancerArns =
         flag "gateway-load-balancer-arns" (optional json_arg)
           ~doc:"JSON ValueStringList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_endpoint_service_configuration
           (Values.CreateVpcEndpointServiceConfigurationRequest.make ?dryRun
              ?acceptanceRequired ?privateDnsName
              ?networkLoadBalancerArns:(Option.map
                                          ~f:Values.ValueStringList.of_json
                                          networkLoadBalancerArns)
              ?gatewayLoadBalancerArns:(Option.map
                                          ~f:Values.ValueStringList.of_json
                                          gatewayLoadBalancerArns)
              ?clientToken
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.CreateVpcEndpointServiceConfigurationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_vpc_peering_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and peerOwnerId =
         flag "peer-owner-id" (optional string) ~doc:"STRING String"
       and peerVpcId =
         flag "peer-vpc-id" (optional string) ~doc:"STRING String"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING VpcId"
       and peerRegion =
         flag "peer-region" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_peering_connection
           (Values.CreateVpcPeeringConnectionRequest.make ?dryRun
              ?peerOwnerId ?peerVpcId ?vpcId ?peerRegion
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ())
           (Some Values.CreateVpcPeeringConnectionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_vpn_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpnGatewayId =
         flag "vpn-gateway-id" (optional string) ~doc:"STRING VpnGatewayId"
       and transitGatewayId =
         flag "transit-gateway-id" (optional string)
           ~doc:"STRING TransitGatewayId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and options =
         flag "options" (optional json_arg)
           ~doc:"JSON VpnConnectionOptionsSpecification"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and customerGatewayId =
         flag "customer-gateway-id" (required string)
           ~doc:"STRING CustomerGatewayId"
       and type_ = flag "type-" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpn_connection
           (Values.CreateVpnConnectionRequest.make ?vpnGatewayId
              ?transitGatewayId ?dryRun
              ?options:(Option.map
                          ~f:Values.VpnConnectionOptionsSpecification.of_json
                          options)
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications) ~customerGatewayId
              ~type_ ()) (Some Values.CreateVpnConnectionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let create_vpn_connection_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and vpnConnectionId =
         flag "vpn-connection-id" (required string)
           ~doc:"STRING VpnConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpn_connection_route
           (Values.CreateVpnConnectionRouteRequest.make ~destinationCidrBlock
              ~vpnConnectionId ()) None (Some Values.Ec2_error.to_json)])
let create_vpn_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecificationList"
       and amazonSideAsn =
         flag "amazon-side-asn" (optional json_arg) ~doc:"JSON Long"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON GatewayType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpn_gateway
           (Values.CreateVpnGatewayRequest.make ?availabilityZone
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecificationList.of_json
                                    tagSpecifications)
              ?amazonSideAsn:(Option.map ~f:Values.Long.of_json amazonSideAsn)
              ?dryRun ~type_:(Values.GatewayType.of_json type_) ())
           (Some Values.CreateVpnGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_carrier_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and carrierGatewayId =
         flag "carrier-gateway-id" (required string)
           ~doc:"STRING CarrierGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_carrier_gateway
           (Values.DeleteCarrierGatewayRequest.make ?dryRun ~carrierGatewayId
              ()) (Some Values.DeleteCarrierGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_client_vpn_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_client_vpn_endpoint
           (Values.DeleteClientVpnEndpointRequest.make ?dryRun
              ~clientVpnEndpointId ())
           (Some Values.DeleteClientVpnEndpointResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_client_vpn_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetVpcSubnetId =
         flag "target-vpc-subnet-id" (optional string) ~doc:"STRING SubnetId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_client_vpn_route
           (Values.DeleteClientVpnRouteRequest.make ?targetVpcSubnetId
              ?dryRun ~clientVpnEndpointId ~destinationCidrBlock ())
           (Some Values.DeleteClientVpnRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_customer_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and customerGatewayId =
         flag "customer-gateway-id" (required string)
           ~doc:"STRING CustomerGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_customer_gateway
           (Values.DeleteCustomerGatewayRequest.make ?dryRun
              ~customerGatewayId ()) None (Some Values.Ec2_error.to_json)])
let delete_dhcp_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and dhcpOptionsId =
         flag "dhcp-options-id" (required string) ~doc:"STRING DhcpOptionsId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dhcp_options
           (Values.DeleteDhcpOptionsRequest.make ?dryRun ~dhcpOptionsId ())
           None (Some Values.Ec2_error.to_json)])
let delete_egress_only_internet_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and egressOnlyInternetGatewayId =
         flag "egress-only-internet-gateway-id" (required string)
           ~doc:"STRING EgressOnlyInternetGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_egress_only_internet_gateway
           (Values.DeleteEgressOnlyInternetGatewayRequest.make ?dryRun
              ~egressOnlyInternetGatewayId ())
           (Some Values.DeleteEgressOnlyInternetGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and fleetIds =
         flag "fleet-ids" (required json_arg) ~doc:"JSON FleetIdSet"
       and terminateInstances =
         flag "terminate-instances" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleets
           (Values.DeleteFleetsRequest.make ?dryRun
              ~fleetIds:(Values.FleetIdSet.of_json fleetIds)
              ~terminateInstances ())
           (Some Values.DeleteFleetsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_flow_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and flowLogIds =
         flag "flow-log-ids" (required json_arg) ~doc:"JSON FlowLogIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow_logs
           (Values.DeleteFlowLogsRequest.make ?dryRun
              ~flowLogIds:(Values.FlowLogIdList.of_json flowLogIds) ())
           (Some Values.DeleteFlowLogsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_fpga_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and fpgaImageId =
         flag "fpga-image-id" (required string) ~doc:"STRING FpgaImageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fpga_image
           (Values.DeleteFpgaImageRequest.make ?dryRun ~fpgaImageId ())
           (Some Values.DeleteFpgaImageResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_instance_event_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and instanceEventWindowId =
         flag "instance-event-window-id" (required string)
           ~doc:"STRING InstanceEventWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance_event_window
           (Values.DeleteInstanceEventWindowRequest.make ?dryRun ?forceDelete
              ~instanceEventWindowId ())
           (Some Values.DeleteInstanceEventWindowResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_internet_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and internetGatewayId =
         flag "internet-gateway-id" (required string)
           ~doc:"STRING InternetGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_internet_gateway
           (Values.DeleteInternetGatewayRequest.make ?dryRun
              ~internetGatewayId ()) None (Some Values.Ec2_error.to_json)])
let delete_ipam =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and cascade = flag "cascade" (optional bool) ~doc:"BOOL Boolean"
       and ipamId = flag "ipam-id" (required string) ~doc:"STRING IpamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ipam
           (Values.DeleteIpamRequest.make ?dryRun ?cascade ~ipamId ())
           (Some Values.DeleteIpamResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_ipam_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipamPoolId =
         flag "ipam-pool-id" (required string) ~doc:"STRING IpamPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ipam_pool
           (Values.DeleteIpamPoolRequest.make ?dryRun ~ipamPoolId ())
           (Some Values.DeleteIpamPoolResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_ipam_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipamScopeId =
         flag "ipam-scope-id" (required string) ~doc:"STRING IpamScopeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ipam_scope
           (Values.DeleteIpamScopeRequest.make ?dryRun ~ipamScopeId ())
           (Some Values.DeleteIpamScopeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyName =
         flag "key-name" (optional string) ~doc:"STRING KeyPairName"
       and keyPairId =
         flag "key-pair-id" (optional string) ~doc:"STRING KeyPairId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_key_pair
           (Values.DeleteKeyPairRequest.make ?keyName ?keyPairId ?dryRun ())
           None (Some Values.Ec2_error.to_json)])
let delete_launch_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and launchTemplateId =
         flag "launch-template-id" (optional string)
           ~doc:"STRING LaunchTemplateId"
       and launchTemplateName =
         flag "launch-template-name" (optional string)
           ~doc:"STRING LaunchTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_template
           (Values.DeleteLaunchTemplateRequest.make ?dryRun ?launchTemplateId
              ?launchTemplateName ())
           (Some Values.DeleteLaunchTemplateResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_launch_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and launchTemplateId =
         flag "launch-template-id" (optional string)
           ~doc:"STRING LaunchTemplateId"
       and launchTemplateName =
         flag "launch-template-name" (optional string)
           ~doc:"STRING LaunchTemplateName"
       and versions =
         flag "versions" (required json_arg) ~doc:"JSON VersionStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_template_versions
           (Values.DeleteLaunchTemplateVersionsRequest.make ?dryRun
              ?launchTemplateId ?launchTemplateName
              ~versions:(Values.VersionStringList.of_json versions) ())
           (Some Values.DeleteLaunchTemplateVersionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and localGatewayRouteTableId =
         flag "local-gateway-route-table-id" (required string)
           ~doc:"STRING LocalGatewayRoutetableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_route
           (Values.DeleteLocalGatewayRouteRequest.make ?dryRun
              ~destinationCidrBlock ~localGatewayRouteTableId ())
           (Some Values.DeleteLocalGatewayRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_route_table_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayRouteTableVpcAssociationId =
         flag "local-gateway-route-table-vpc-association-id"
           (required string)
           ~doc:"STRING LocalGatewayRouteTableVpcAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_route_table_vpc_association
           (Values.DeleteLocalGatewayRouteTableVpcAssociationRequest.make
              ?dryRun ~localGatewayRouteTableVpcAssociationId ())
           (Some
              Values.DeleteLocalGatewayRouteTableVpcAssociationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_managed_prefix_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and prefixListId =
         flag "prefix-list-id" (required string)
           ~doc:"STRING PrefixListResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_managed_prefix_list
           (Values.DeleteManagedPrefixListRequest.make ?dryRun ~prefixListId
              ()) (Some Values.DeleteManagedPrefixListResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_nat_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and natGatewayId =
         flag "nat-gateway-id" (required string) ~doc:"STRING NatGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_nat_gateway
           (Values.DeleteNatGatewayRequest.make ?dryRun ~natGatewayId ())
           (Some Values.DeleteNatGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkAclId =
         flag "network-acl-id" (required string) ~doc:"STRING NetworkAclId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_acl
           (Values.DeleteNetworkAclRequest.make ?dryRun ~networkAclId ())
           None (Some Values.Ec2_error.to_json)])
let delete_network_acl_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and egress = flag "egress" (required bool) ~doc:"BOOL Boolean"
       and networkAclId =
         flag "network-acl-id" (required string) ~doc:"STRING NetworkAclId"
       and ruleNumber = flag "rule-number" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_acl_entry
           (Values.DeleteNetworkAclEntryRequest.make ?dryRun ~egress
              ~networkAclId ~ruleNumber ()) None
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_access_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsAccessScopeId =
         flag "network-insights-access-scope-id" (required string)
           ~doc:"STRING NetworkInsightsAccessScopeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_access_scope
           (Values.DeleteNetworkInsightsAccessScopeRequest.make ?dryRun
              ~networkInsightsAccessScopeId ())
           (Some Values.DeleteNetworkInsightsAccessScopeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_access_scope_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsAccessScopeAnalysisId =
         flag "network-insights-access-scope-analysis-id" (required string)
           ~doc:"STRING NetworkInsightsAccessScopeAnalysisId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_access_scope_analysis
           (Values.DeleteNetworkInsightsAccessScopeAnalysisRequest.make
              ?dryRun ~networkInsightsAccessScopeAnalysisId ())
           (Some
              Values.DeleteNetworkInsightsAccessScopeAnalysisResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsAnalysisId =
         flag "network-insights-analysis-id" (required string)
           ~doc:"STRING NetworkInsightsAnalysisId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_analysis
           (Values.DeleteNetworkInsightsAnalysisRequest.make ?dryRun
              ~networkInsightsAnalysisId ())
           (Some Values.DeleteNetworkInsightsAnalysisResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsPathId =
         flag "network-insights-path-id" (required string)
           ~doc:"STRING NetworkInsightsPathId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_path
           (Values.DeleteNetworkInsightsPathRequest.make ?dryRun
              ~networkInsightsPathId ())
           (Some Values.DeleteNetworkInsightsPathResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_interface
           (Values.DeleteNetworkInterfaceRequest.make ?dryRun
              ~networkInterfaceId ()) None (Some Values.Ec2_error.to_json)])
let delete_network_interface_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfacePermissionId =
         flag "network-interface-permission-id" (required string)
           ~doc:"STRING NetworkInterfacePermissionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_interface_permission
           (Values.DeleteNetworkInterfacePermissionRequest.make ?force
              ?dryRun ~networkInterfacePermissionId ())
           (Some Values.DeleteNetworkInterfacePermissionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING PlacementGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_placement_group
           (Values.DeletePlacementGroupRequest.make ?dryRun ~groupName ())
           None (Some Values.Ec2_error.to_json)])
let delete_public_ipv4_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING Ipv4PoolEc2Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_public_ipv4_pool
           (Values.DeletePublicIpv4PoolRequest.make ?dryRun ~poolId ())
           (Some Values.DeletePublicIpv4PoolResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_queued_reserved_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and reservedInstancesIds =
         flag "reserved-instances-ids" (required json_arg)
           ~doc:"JSON DeleteQueuedReservedInstancesIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queued_reserved_instances
           (Values.DeleteQueuedReservedInstancesRequest.make ?dryRun
              ~reservedInstancesIds:(Values.DeleteQueuedReservedInstancesIdList.of_json
                                       reservedInstancesIds) ())
           (Some Values.DeleteQueuedReservedInstancesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationCidrBlock =
         flag "destination-cidr-block" (optional string) ~doc:"STRING String"
       and destinationIpv6CidrBlock =
         flag "destination-ipv6-cidr-block" (optional string)
           ~doc:"STRING String"
       and destinationPrefixListId =
         flag "destination-prefix-list-id" (optional string)
           ~doc:"STRING PrefixListResourceId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and routeTableId =
         flag "route-table-id" (required string) ~doc:"STRING RouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route
           (Values.DeleteRouteRequest.make ?destinationCidrBlock
              ?destinationIpv6CidrBlock ?destinationPrefixListId ?dryRun
              ~routeTableId ()) None (Some Values.Ec2_error.to_json)])
let delete_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and routeTableId =
         flag "route-table-id" (required string) ~doc:"STRING RouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_table
           (Values.DeleteRouteTableRequest.make ?dryRun ~routeTableId ())
           None (Some Values.Ec2_error.to_json)])
let delete_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId =
         flag "group-id" (optional string) ~doc:"STRING SecurityGroupId"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING SecurityGroupName"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_group
           (Values.DeleteSecurityGroupRequest.make ?groupId ?groupName
              ?dryRun ()) None (Some Values.Ec2_error.to_json)])
let delete_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot
           (Values.DeleteSnapshotRequest.make ?dryRun ~snapshotId ()) None
           (Some Values.Ec2_error.to_json)])
let delete_spot_datafeed_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_spot_datafeed_subscription
           (Values.DeleteSpotDatafeedSubscriptionRequest.make ?dryRun ())
           None (Some Values.Ec2_error.to_json)])
let delete_subnet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subnet
           (Values.DeleteSubnetRequest.make ?dryRun ~subnetId ()) None
           (Some Values.Ec2_error.to_json)])
let delete_subnet_cidr_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and subnetCidrReservationId =
         flag "subnet-cidr-reservation-id" (required string)
           ~doc:"STRING SubnetCidrReservationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subnet_cidr_reservation
           (Values.DeleteSubnetCidrReservationRequest.make ?dryRun
              ~subnetCidrReservationId ())
           (Some Values.DeleteSubnetCidrReservationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and resources =
         flag "resources" (required json_arg) ~doc:"JSON ResourceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsRequest.make ?dryRun
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~resources:(Values.ResourceIdList.of_json resources) ()) None
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorFilterId =
         flag "traffic-mirror-filter-id" (required string)
           ~doc:"STRING TrafficMirrorFilterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_filter
           (Values.DeleteTrafficMirrorFilterRequest.make ?dryRun
              ~trafficMirrorFilterId ())
           (Some Values.DeleteTrafficMirrorFilterResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_filter_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorFilterRuleId =
         flag "traffic-mirror-filter-rule-id" (required string)
           ~doc:"STRING TrafficMirrorFilterRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_filter_rule
           (Values.DeleteTrafficMirrorFilterRuleRequest.make ?dryRun
              ~trafficMirrorFilterRuleId ())
           (Some Values.DeleteTrafficMirrorFilterRuleResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorSessionId =
         flag "traffic-mirror-session-id" (required string)
           ~doc:"STRING TrafficMirrorSessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_session
           (Values.DeleteTrafficMirrorSessionRequest.make ?dryRun
              ~trafficMirrorSessionId ())
           (Some Values.DeleteTrafficMirrorSessionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorTargetId =
         flag "traffic-mirror-target-id" (required string)
           ~doc:"STRING TrafficMirrorTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_target
           (Values.DeleteTrafficMirrorTargetRequest.make ?dryRun
              ~trafficMirrorTargetId ())
           (Some Values.DeleteTrafficMirrorTargetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayId =
         flag "transit-gateway-id" (required string)
           ~doc:"STRING TransitGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway
           (Values.DeleteTransitGatewayRequest.make ?dryRun ~transitGatewayId
              ()) (Some Values.DeleteTransitGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_connect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_connect
           (Values.DeleteTransitGatewayConnectRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayConnectResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayConnectPeerId =
         flag "transit-gateway-connect-peer-id" (required string)
           ~doc:"STRING TransitGatewayConnectPeerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_connect_peer
           (Values.DeleteTransitGatewayConnectPeerRequest.make ?dryRun
              ~transitGatewayConnectPeerId ())
           (Some Values.DeleteTransitGatewayConnectPeerResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_multicast_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayMulticastDomainId =
         flag "transit-gateway-multicast-domain-id" (required string)
           ~doc:"STRING TransitGatewayMulticastDomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_multicast_domain
           (Values.DeleteTransitGatewayMulticastDomainRequest.make ?dryRun
              ~transitGatewayMulticastDomainId ())
           (Some Values.DeleteTransitGatewayMulticastDomainResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_peering_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_peering_attachment
           (Values.DeleteTransitGatewayPeeringAttachmentRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayPeeringAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_prefix_list_reference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId"
       and prefixListId =
         flag "prefix-list-id" (required string)
           ~doc:"STRING PrefixListResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_prefix_list_reference
           (Values.DeleteTransitGatewayPrefixListReferenceRequest.make
              ?dryRun ~transitGatewayRouteTableId ~prefixListId ())
           (Some Values.DeleteTransitGatewayPrefixListReferenceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_route
           (Values.DeleteTransitGatewayRouteRequest.make ?dryRun
              ~transitGatewayRouteTableId ~destinationCidrBlock ())
           (Some Values.DeleteTransitGatewayRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_route_table
           (Values.DeleteTransitGatewayRouteTableRequest.make ?dryRun
              ~transitGatewayRouteTableId ())
           (Some Values.DeleteTransitGatewayRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_vpc_attachment
           (Values.DeleteTransitGatewayVpcAttachmentRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayVpcAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_volume
           (Values.DeleteVolumeRequest.make ?dryRun ~volumeId ()) None
           (Some Values.Ec2_error.to_json)])
let delete_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc (Values.DeleteVpcRequest.make ?dryRun ~vpcId ())
           None (Some Values.Ec2_error.to_json)])
let delete_vpc_endpoint_connection_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and connectionNotificationIds =
         flag "connection-notification-ids" (required json_arg)
           ~doc:"JSON ConnectionNotificationIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_endpoint_connection_notifications
           (Values.DeleteVpcEndpointConnectionNotificationsRequest.make
              ?dryRun
              ~connectionNotificationIds:(Values.ConnectionNotificationIdsList.of_json
                                            connectionNotificationIds) ())
           (Some
              Values.DeleteVpcEndpointConnectionNotificationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpc_endpoint_service_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and serviceIds =
         flag "service-ids" (required json_arg)
           ~doc:"JSON VpcEndpointServiceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_endpoint_service_configurations
           (Values.DeleteVpcEndpointServiceConfigurationsRequest.make ?dryRun
              ~serviceIds:(Values.VpcEndpointServiceIdList.of_json serviceIds)
              ())
           (Some Values.DeleteVpcEndpointServiceConfigurationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
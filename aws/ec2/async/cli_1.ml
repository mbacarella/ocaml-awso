(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
open Cli_0[@@warning "-33"]
let delete_launch_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and launchTemplateId =
         flag "launch-template-id" (optional string)
           ~doc:"STRING LaunchTemplateId"
       and launchTemplateName =
         flag "launch-template-name" (optional string)
           ~doc:"STRING LaunchTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_template
           (Values.DeleteLaunchTemplateRequest.make ?dryRun ?launchTemplateId
              ?launchTemplateName ())
           (Some Values.DeleteLaunchTemplateResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_launch_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and launchTemplateId =
         flag "launch-template-id" (optional string)
           ~doc:"STRING LaunchTemplateId"
       and launchTemplateName =
         flag "launch-template-name" (optional string)
           ~doc:"STRING LaunchTemplateName"
       and versions =
         flag "versions" (required json_arg) ~doc:"JSON VersionStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_template_versions
           (Values.DeleteLaunchTemplateVersionsRequest.make ?dryRun
              ?launchTemplateId ?launchTemplateName
              ~versions:(Values.VersionStringList.of_json versions) ())
           (Some Values.DeleteLaunchTemplateVersionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationCidrBlock =
         flag "destination-cidr-block" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and destinationPrefixListId =
         flag "destination-prefix-list-id" (optional string)
           ~doc:"STRING PrefixListResourceId"
       and localGatewayRouteTableId =
         flag "local-gateway-route-table-id" (required string)
           ~doc:"STRING LocalGatewayRoutetableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_route
           (Values.DeleteLocalGatewayRouteRequest.make ?destinationCidrBlock
              ?dryRun ?destinationPrefixListId ~localGatewayRouteTableId ())
           (Some Values.DeleteLocalGatewayRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayRouteTableId =
         flag "local-gateway-route-table-id" (required string)
           ~doc:"STRING LocalGatewayRoutetableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_route_table
           (Values.DeleteLocalGatewayRouteTableRequest.make ?dryRun
              ~localGatewayRouteTableId ())
           (Some Values.DeleteLocalGatewayRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_route_table_virtual_interface_group_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayRouteTableVirtualInterfaceGroupAssociationId =
         flag
           "local-gateway-route-table-virtual-interface-group-association-id"
           (required string)
           ~doc:"STRING LocalGatewayRouteTableVirtualInterfaceGroupAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_route_table_virtual_interface_group_association
           (Values.DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest.make
              ?dryRun
              ~localGatewayRouteTableVirtualInterfaceGroupAssociationId ())
           (Some
              Values.DeleteLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_route_table_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayRouteTableVpcAssociationId =
         flag "local-gateway-route-table-vpc-association-id"
           (required string)
           ~doc:"STRING LocalGatewayRouteTableVpcAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_route_table_vpc_association
           (Values.DeleteLocalGatewayRouteTableVpcAssociationRequest.make
              ?dryRun ~localGatewayRouteTableVpcAssociationId ())
           (Some
              Values.DeleteLocalGatewayRouteTableVpcAssociationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_virtual_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayVirtualInterfaceId =
         flag "local-gateway-virtual-interface-id" (required string)
           ~doc:"STRING LocalGatewayVirtualInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_virtual_interface
           (Values.DeleteLocalGatewayVirtualInterfaceRequest.make ?dryRun
              ~localGatewayVirtualInterfaceId ())
           (Some Values.DeleteLocalGatewayVirtualInterfaceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_local_gateway_virtual_interface_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and localGatewayVirtualInterfaceGroupId =
         flag "local-gateway-virtual-interface-group-id" (required string)
           ~doc:"STRING LocalGatewayVirtualInterfaceGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_local_gateway_virtual_interface_group
           (Values.DeleteLocalGatewayVirtualInterfaceGroupRequest.make
              ?dryRun ~localGatewayVirtualInterfaceGroupId ())
           (Some Values.DeleteLocalGatewayVirtualInterfaceGroupResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_managed_prefix_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and prefixListId =
         flag "prefix-list-id" (required string)
           ~doc:"STRING PrefixListResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_managed_prefix_list
           (Values.DeleteManagedPrefixListRequest.make ?dryRun ~prefixListId
              ()) (Some Values.DeleteManagedPrefixListResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_nat_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and natGatewayId =
         flag "nat-gateway-id" (required string) ~doc:"STRING NatGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_nat_gateway
           (Values.DeleteNatGatewayRequest.make ?dryRun ~natGatewayId ())
           (Some Values.DeleteNatGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkAclId =
         flag "network-acl-id" (required string) ~doc:"STRING NetworkAclId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_acl
           (Values.DeleteNetworkAclRequest.make ?dryRun ~networkAclId ())
           None (Some Values.Ec2_error.to_json)])
let delete_network_acl_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkAclId =
         flag "network-acl-id" (required string) ~doc:"STRING NetworkAclId"
       and ruleNumber = flag "rule-number" (required int) ~doc:"INT Integer"
       and egress = flag "egress" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_acl_entry
           (Values.DeleteNetworkAclEntryRequest.make ?dryRun ~networkAclId
              ~ruleNumber ~egress ()) None (Some Values.Ec2_error.to_json)])
let delete_network_insights_access_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsAccessScopeId =
         flag "network-insights-access-scope-id" (required string)
           ~doc:"STRING NetworkInsightsAccessScopeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_access_scope
           (Values.DeleteNetworkInsightsAccessScopeRequest.make ?dryRun
              ~networkInsightsAccessScopeId ())
           (Some Values.DeleteNetworkInsightsAccessScopeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_access_scope_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsAccessScopeAnalysisId =
         flag "network-insights-access-scope-analysis-id" (required string)
           ~doc:"STRING NetworkInsightsAccessScopeAnalysisId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_access_scope_analysis
           (Values.DeleteNetworkInsightsAccessScopeAnalysisRequest.make
              ?dryRun ~networkInsightsAccessScopeAnalysisId ())
           (Some
              Values.DeleteNetworkInsightsAccessScopeAnalysisResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsAnalysisId =
         flag "network-insights-analysis-id" (required string)
           ~doc:"STRING NetworkInsightsAnalysisId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_analysis
           (Values.DeleteNetworkInsightsAnalysisRequest.make ?dryRun
              ~networkInsightsAnalysisId ())
           (Some Values.DeleteNetworkInsightsAnalysisResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_insights_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInsightsPathId =
         flag "network-insights-path-id" (required string)
           ~doc:"STRING NetworkInsightsPathId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_insights_path
           (Values.DeleteNetworkInsightsPathRequest.make ?dryRun
              ~networkInsightsPathId ())
           (Some Values.DeleteNetworkInsightsPathResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_interface
           (Values.DeleteNetworkInterfaceRequest.make ?dryRun
              ~networkInterfaceId ()) None (Some Values.Ec2_error.to_json)])
let delete_network_interface_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfacePermissionId =
         flag "network-interface-permission-id" (required string)
           ~doc:"STRING NetworkInterfacePermissionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_interface_permission
           (Values.DeleteNetworkInterfacePermissionRequest.make ?force
              ?dryRun ~networkInterfacePermissionId ())
           (Some Values.DeleteNetworkInterfacePermissionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and groupName =
         flag "group-name" (required string)
           ~doc:"STRING PlacementGroupNameWithResolver" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_placement_group
           (Values.DeletePlacementGroupRequest.make ?dryRun ~groupName ())
           None (Some Values.Ec2_error.to_json)])
let delete_public_ipv4_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkBorderGroup =
         flag "network-border-group" (optional string) ~doc:"STRING String"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING Ipv4PoolEc2Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_public_ipv4_pool
           (Values.DeletePublicIpv4PoolRequest.make ?dryRun
              ?networkBorderGroup ~poolId ())
           (Some Values.DeletePublicIpv4PoolResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_queued_reserved_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and reservedInstancesIds =
         flag "reserved-instances-ids" (required json_arg)
           ~doc:"JSON DeleteQueuedReservedInstancesIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queued_reserved_instances
           (Values.DeleteQueuedReservedInstancesRequest.make ?dryRun
              ~reservedInstancesIds:(Values.DeleteQueuedReservedInstancesIdList.of_json
                                       reservedInstancesIds) ())
           (Some Values.DeleteQueuedReservedInstancesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationPrefixListId =
         flag "destination-prefix-list-id" (optional string)
           ~doc:"STRING PrefixListResourceId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and destinationCidrBlock =
         flag "destination-cidr-block" (optional string) ~doc:"STRING String"
       and destinationIpv6CidrBlock =
         flag "destination-ipv6-cidr-block" (optional string)
           ~doc:"STRING String"
       and routeTableId =
         flag "route-table-id" (required string) ~doc:"STRING RouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route
           (Values.DeleteRouteRequest.make ?destinationPrefixListId ?dryRun
              ?destinationCidrBlock ?destinationIpv6CidrBlock ~routeTableId
              ()) None (Some Values.Ec2_error.to_json)])
let delete_route_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and routeServerId =
         flag "route-server-id" (required string) ~doc:"STRING RouteServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_server
           (Values.DeleteRouteServerRequest.make ?dryRun ~routeServerId ())
           (Some Values.DeleteRouteServerResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_route_server_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and routeServerEndpointId =
         flag "route-server-endpoint-id" (required string)
           ~doc:"STRING RouteServerEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_server_endpoint
           (Values.DeleteRouteServerEndpointRequest.make ?dryRun
              ~routeServerEndpointId ())
           (Some Values.DeleteRouteServerEndpointResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_route_server_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and routeServerPeerId =
         flag "route-server-peer-id" (required string)
           ~doc:"STRING RouteServerPeerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_server_peer
           (Values.DeleteRouteServerPeerRequest.make ?dryRun
              ~routeServerPeerId ())
           (Some Values.DeleteRouteServerPeerResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and routeTableId =
         flag "route-table-id" (required string) ~doc:"STRING RouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_table
           (Values.DeleteRouteTableRequest.make ?dryRun ~routeTableId ())
           None (Some Values.Ec2_error.to_json)])
let delete_secondary_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and secondaryNetworkId =
         flag "secondary-network-id" (required string)
           ~doc:"STRING SecondaryNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_secondary_network
           (Values.DeleteSecondaryNetworkRequest.make ?clientToken ?dryRun
              ~secondaryNetworkId ())
           (Some Values.DeleteSecondaryNetworkResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_secondary_subnet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and secondarySubnetId =
         flag "secondary-subnet-id" (required string)
           ~doc:"STRING SecondarySubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_secondary_subnet
           (Values.DeleteSecondarySubnetRequest.make ?clientToken ?dryRun
              ~secondarySubnetId ())
           (Some Values.DeleteSecondarySubnetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId =
         flag "group-id" (optional string) ~doc:"STRING SecurityGroupId"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING SecurityGroupName"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_group
           (Values.DeleteSecurityGroupRequest.make ?groupId ?groupName
              ?dryRun ()) (Some Values.DeleteSecurityGroupResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot
           (Values.DeleteSnapshotRequest.make ?dryRun ~snapshotId ()) None
           (Some Values.Ec2_error.to_json)])
let delete_spot_datafeed_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_spot_datafeed_subscription
           (Values.DeleteSpotDatafeedSubscriptionRequest.make ?dryRun ())
           None (Some Values.Ec2_error.to_json)])
let delete_subnet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subnet
           (Values.DeleteSubnetRequest.make ?dryRun ~subnetId ()) None
           (Some Values.Ec2_error.to_json)])
let delete_subnet_cidr_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and subnetCidrReservationId =
         flag "subnet-cidr-reservation-id" (required string)
           ~doc:"STRING SubnetCidrReservationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subnet_cidr_reservation
           (Values.DeleteSubnetCidrReservationRequest.make ?dryRun
              ~subnetCidrReservationId ())
           (Some Values.DeleteSubnetCidrReservationResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and resources =
         flag "resources" (required json_arg) ~doc:"JSON ResourceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsRequest.make ?dryRun
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~resources:(Values.ResourceIdList.of_json resources) ()) None
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorFilterId =
         flag "traffic-mirror-filter-id" (required string)
           ~doc:"STRING TrafficMirrorFilterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_filter
           (Values.DeleteTrafficMirrorFilterRequest.make ?dryRun
              ~trafficMirrorFilterId ())
           (Some Values.DeleteTrafficMirrorFilterResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_filter_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorFilterRuleId =
         flag "traffic-mirror-filter-rule-id" (required string)
           ~doc:"STRING TrafficMirrorFilterRuleIdWithResolver" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_filter_rule
           (Values.DeleteTrafficMirrorFilterRuleRequest.make ?dryRun
              ~trafficMirrorFilterRuleId ())
           (Some Values.DeleteTrafficMirrorFilterRuleResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorSessionId =
         flag "traffic-mirror-session-id" (required string)
           ~doc:"STRING TrafficMirrorSessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_session
           (Values.DeleteTrafficMirrorSessionRequest.make ?dryRun
              ~trafficMirrorSessionId ())
           (Some Values.DeleteTrafficMirrorSessionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_traffic_mirror_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and trafficMirrorTargetId =
         flag "traffic-mirror-target-id" (required string)
           ~doc:"STRING TrafficMirrorTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_mirror_target
           (Values.DeleteTrafficMirrorTargetRequest.make ?dryRun
              ~trafficMirrorTargetId ())
           (Some Values.DeleteTrafficMirrorTargetResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayId =
         flag "transit-gateway-id" (required string)
           ~doc:"STRING TransitGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway
           (Values.DeleteTransitGatewayRequest.make ?dryRun ~transitGatewayId
              ()) (Some Values.DeleteTransitGatewayResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_client_vpn_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_client_vpn_attachment
           (Values.DeleteTransitGatewayClientVpnAttachmentRequest.make
              ?dryRun ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayClientVpnAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_connect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_connect
           (Values.DeleteTransitGatewayConnectRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayConnectResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayConnectPeerId =
         flag "transit-gateway-connect-peer-id" (required string)
           ~doc:"STRING TransitGatewayConnectPeerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_connect_peer
           (Values.DeleteTransitGatewayConnectPeerRequest.make ?dryRun
              ~transitGatewayConnectPeerId ())
           (Some Values.DeleteTransitGatewayConnectPeerResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_metering_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayMeteringPolicyId =
         flag "transit-gateway-metering-policy-id" (required string)
           ~doc:"STRING TransitGatewayMeteringPolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_metering_policy
           (Values.DeleteTransitGatewayMeteringPolicyRequest.make ?dryRun
              ~transitGatewayMeteringPolicyId ())
           (Some Values.DeleteTransitGatewayMeteringPolicyResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_metering_policy_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayMeteringPolicyId =
         flag "transit-gateway-metering-policy-id" (required string)
           ~doc:"STRING TransitGatewayMeteringPolicyId"
       and policyRuleNumber =
         flag "policy-rule-number" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_metering_policy_entry
           (Values.DeleteTransitGatewayMeteringPolicyEntryRequest.make
              ?dryRun ~transitGatewayMeteringPolicyId ~policyRuleNumber ())
           (Some Values.DeleteTransitGatewayMeteringPolicyEntryResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_multicast_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayMulticastDomainId =
         flag "transit-gateway-multicast-domain-id" (required string)
           ~doc:"STRING TransitGatewayMulticastDomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_multicast_domain
           (Values.DeleteTransitGatewayMulticastDomainRequest.make ?dryRun
              ~transitGatewayMulticastDomainId ())
           (Some Values.DeleteTransitGatewayMulticastDomainResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_peering_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_peering_attachment
           (Values.DeleteTransitGatewayPeeringAttachmentRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayPeeringAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_policy_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayPolicyTableId =
         flag "transit-gateway-policy-table-id" (required string)
           ~doc:"STRING TransitGatewayPolicyTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_policy_table
           (Values.DeleteTransitGatewayPolicyTableRequest.make ?dryRun
              ~transitGatewayPolicyTableId ())
           (Some Values.DeleteTransitGatewayPolicyTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_prefix_list_reference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId"
       and prefixListId =
         flag "prefix-list-id" (required string)
           ~doc:"STRING PrefixListResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_prefix_list_reference
           (Values.DeleteTransitGatewayPrefixListReferenceRequest.make
              ?dryRun ~transitGatewayRouteTableId ~prefixListId ())
           (Some Values.DeleteTransitGatewayPrefixListReferenceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_route
           (Values.DeleteTransitGatewayRouteRequest.make ?dryRun
              ~transitGatewayRouteTableId ~destinationCidrBlock ())
           (Some Values.DeleteTransitGatewayRouteResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_route_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableId =
         flag "transit-gateway-route-table-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_route_table
           (Values.DeleteTransitGatewayRouteTableRequest.make ?dryRun
              ~transitGatewayRouteTableId ())
           (Some Values.DeleteTransitGatewayRouteTableResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_route_table_announcement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayRouteTableAnnouncementId =
         flag "transit-gateway-route-table-announcement-id" (required string)
           ~doc:"STRING TransitGatewayRouteTableAnnouncementId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_route_table_announcement
           (Values.DeleteTransitGatewayRouteTableAnnouncementRequest.make
              ?dryRun ~transitGatewayRouteTableAnnouncementId ())
           (Some
              Values.DeleteTransitGatewayRouteTableAnnouncementResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_transit_gateway_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and transitGatewayAttachmentId =
         flag "transit-gateway-attachment-id" (required string)
           ~doc:"STRING TransitGatewayAttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transit_gateway_vpc_attachment
           (Values.DeleteTransitGatewayVpcAttachmentRequest.make ?dryRun
              ~transitGatewayAttachmentId ())
           (Some Values.DeleteTransitGatewayVpcAttachmentResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_verified_access_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and verifiedAccessEndpointId =
         flag "verified-access-endpoint-id" (required string)
           ~doc:"STRING VerifiedAccessEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_verified_access_endpoint
           (Values.DeleteVerifiedAccessEndpointRequest.make ?clientToken
              ?dryRun ~verifiedAccessEndpointId ())
           (Some Values.DeleteVerifiedAccessEndpointResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_verified_access_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and verifiedAccessGroupId =
         flag "verified-access-group-id" (required string)
           ~doc:"STRING VerifiedAccessGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_verified_access_group
           (Values.DeleteVerifiedAccessGroupRequest.make ?clientToken ?dryRun
              ~verifiedAccessGroupId ())
           (Some Values.DeleteVerifiedAccessGroupResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_verified_access_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and verifiedAccessInstanceId =
         flag "verified-access-instance-id" (required string)
           ~doc:"STRING VerifiedAccessInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_verified_access_instance
           (Values.DeleteVerifiedAccessInstanceRequest.make ?dryRun
              ?clientToken ~verifiedAccessInstanceId ())
           (Some Values.DeleteVerifiedAccessInstanceResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_verified_access_trust_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and verifiedAccessTrustProviderId =
         flag "verified-access-trust-provider-id" (required string)
           ~doc:"STRING VerifiedAccessTrustProviderId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_verified_access_trust_provider
           (Values.DeleteVerifiedAccessTrustProviderRequest.make ?dryRun
              ?clientToken ~verifiedAccessTrustProviderId ())
           (Some Values.DeleteVerifiedAccessTrustProviderResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_volume
           (Values.DeleteVolumeRequest.make ?dryRun ~volumeId ()) None
           (Some Values.Ec2_error.to_json)])
let delete_vpc =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc (Values.DeleteVpcRequest.make ?dryRun ~vpcId ())
           None (Some Values.Ec2_error.to_json)])
let delete_vpc_block_public_access_exclusion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and exclusionId =
         flag "exclusion-id" (required string)
           ~doc:"STRING VpcBlockPublicAccessExclusionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_block_public_access_exclusion
           (Values.DeleteVpcBlockPublicAccessExclusionRequest.make ?dryRun
              ~exclusionId ())
           (Some Values.DeleteVpcBlockPublicAccessExclusionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpc_encryption_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcEncryptionControlId =
         flag "vpc-encryption-control-id" (required string)
           ~doc:"STRING VpcEncryptionControlId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_encryption_control
           (Values.DeleteVpcEncryptionControlRequest.make ?dryRun
              ~vpcEncryptionControlId ())
           (Some Values.DeleteVpcEncryptionControlResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpc_endpoint_connection_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and connectionNotificationIds =
         flag "connection-notification-ids" (required json_arg)
           ~doc:"JSON ConnectionNotificationIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_endpoint_connection_notifications
           (Values.DeleteVpcEndpointConnectionNotificationsRequest.make
              ?dryRun
              ~connectionNotificationIds:(Values.ConnectionNotificationIdsList.of_json
                                            connectionNotificationIds) ())
           (Some
              Values.DeleteVpcEndpointConnectionNotificationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpc_endpoint_service_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and serviceIds =
         flag "service-ids" (required json_arg)
           ~doc:"JSON VpcEndpointServiceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_endpoint_service_configurations
           (Values.DeleteVpcEndpointServiceConfigurationsRequest.make ?dryRun
              ~serviceIds:(Values.VpcEndpointServiceIdList.of_json serviceIds)
              ())
           (Some Values.DeleteVpcEndpointServiceConfigurationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpc_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcEndpointIds =
         flag "vpc-endpoint-ids" (required json_arg)
           ~doc:"JSON VpcEndpointIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_endpoints
           (Values.DeleteVpcEndpointsRequest.make ?dryRun
              ~vpcEndpointIds:(Values.VpcEndpointIdList.of_json
                                 vpcEndpointIds) ())
           (Some Values.DeleteVpcEndpointsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpc_peering_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpcPeeringConnectionId =
         flag "vpc-peering-connection-id" (required string)
           ~doc:"STRING VpcPeeringConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_peering_connection
           (Values.DeleteVpcPeeringConnectionRequest.make ?dryRun
              ~vpcPeeringConnectionId ())
           (Some Values.DeleteVpcPeeringConnectionResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpn_concentrator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpnConcentratorId =
         flag "vpn-concentrator-id" (required string)
           ~doc:"STRING VpnConcentratorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpn_concentrator
           (Values.DeleteVpnConcentratorRequest.make ?dryRun
              ~vpnConcentratorId ())
           (Some Values.DeleteVpnConcentratorResult.to_json)
           (Some Values.Ec2_error.to_json)])
let delete_vpn_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpnConnectionId =
         flag "vpn-connection-id" (required string)
           ~doc:"STRING VpnConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpn_connection
           (Values.DeleteVpnConnectionRequest.make ?dryRun ~vpnConnectionId
              ()) None (Some Values.Ec2_error.to_json)])
let delete_vpn_connection_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationCidrBlock =
         flag "destination-cidr-block" (required string) ~doc:"STRING String"
       and vpnConnectionId =
         flag "vpn-connection-id" (required string)
           ~doc:"STRING VpnConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpn_connection_route
           (Values.DeleteVpnConnectionRouteRequest.make ~destinationCidrBlock
              ~vpnConnectionId ()) None (Some Values.Ec2_error.to_json)])
let delete_vpn_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and vpnGatewayId =
         flag "vpn-gateway-id" (required string) ~doc:"STRING VpnGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpn_gateway
           (Values.DeleteVpnGatewayRequest.make ?dryRun ~vpnGatewayId ())
           None (Some Values.Ec2_error.to_json)])
let deprovision_byoip_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and cidr = flag "cidr" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprovision_byoip_cidr
           (Values.DeprovisionByoipCidrRequest.make ?dryRun ~cidr ())
           (Some Values.DeprovisionByoipCidrResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deprovision_ipam_byoasn =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipamId = flag "ipam-id" (required string) ~doc:"STRING IpamId"
       and asn = flag "asn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprovision_ipam_byoasn
           (Values.DeprovisionIpamByoasnRequest.make ?dryRun ~ipamId ~asn ())
           (Some Values.DeprovisionIpamByoasnResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deprovision_ipam_pool_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and cidr = flag "cidr" (optional string) ~doc:"STRING String"
       and ipamPoolId =
         flag "ipam-pool-id" (required string) ~doc:"STRING IpamPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprovision_ipam_pool_cidr
           (Values.DeprovisionIpamPoolCidrRequest.make ?dryRun ?cidr
              ~ipamPoolId ())
           (Some Values.DeprovisionIpamPoolCidrResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deprovision_public_ipv4_pool_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING Ipv4PoolEc2Id"
       and cidr = flag "cidr" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprovision_public_ipv4_pool_cidr
           (Values.DeprovisionPublicIpv4PoolCidrRequest.make ?dryRun ~poolId
              ~cidr ())
           (Some Values.DeprovisionPublicIpv4PoolCidrResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deregister_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteAssociatedSnapshots =
         flag "delete-associated-snapshots" (optional bool)
           ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and imageId = flag "image-id" (required string) ~doc:"STRING ImageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_image
           (Values.DeregisterImageRequest.make ?deleteAssociatedSnapshots
              ?dryRun ~imageId ())
           (Some Values.DeregisterImageResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deregister_instance_event_notification_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceTagAttribute =
         flag "instance-tag-attribute" (required json_arg)
           ~doc:"JSON DeregisterInstanceTagAttributeRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_instance_event_notification_attributes
           (Values.DeregisterInstanceEventNotificationAttributesRequest.make
              ?dryRun
              ~instanceTagAttribute:(Values.DeregisterInstanceTagAttributeRequest.of_json
                                       instanceTagAttribute) ())
           (Some
              Values.DeregisterInstanceEventNotificationAttributesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deregister_transit_gateway_multicast_group_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayMulticastDomainId =
         flag "transit-gateway-multicast-domain-id" (optional string)
           ~doc:"STRING TransitGatewayMulticastDomainId"
       and groupIpAddress =
         flag "group-ip-address" (optional string) ~doc:"STRING String"
       and networkInterfaceIds =
         flag "network-interface-ids" (optional json_arg)
           ~doc:"JSON TransitGatewayNetworkInterfaceIdList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_transit_gateway_multicast_group_members
           (Values.DeregisterTransitGatewayMulticastGroupMembersRequest.make
              ?transitGatewayMulticastDomainId ?groupIpAddress
              ?networkInterfaceIds:(Option.map
                                      ~f:Values.TransitGatewayNetworkInterfaceIdList.of_json
                                      networkInterfaceIds) ?dryRun ())
           (Some
              Values.DeregisterTransitGatewayMulticastGroupMembersResult.to_json)
           (Some Values.Ec2_error.to_json)])
let deregister_transit_gateway_multicast_group_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayMulticastDomainId =
         flag "transit-gateway-multicast-domain-id" (optional string)
           ~doc:"STRING TransitGatewayMulticastDomainId"
       and groupIpAddress =
         flag "group-ip-address" (optional string) ~doc:"STRING String"
       and networkInterfaceIds =
         flag "network-interface-ids" (optional json_arg)
           ~doc:"JSON TransitGatewayNetworkInterfaceIdList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_transit_gateway_multicast_group_sources
           (Values.DeregisterTransitGatewayMulticastGroupSourcesRequest.make
              ?transitGatewayMulticastDomainId ?groupIpAddress
              ?networkInterfaceIds:(Option.map
                                      ~f:Values.TransitGatewayNetworkInterfaceIdList.of_json
                                      networkInterfaceIds) ?dryRun ())
           (Some
              Values.DeregisterTransitGatewayMulticastGroupSourcesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON AccountAttributeNameStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes
           (Values.DescribeAccountAttributesRequest.make ?dryRun
              ?attributeNames:(Option.map
                                 ~f:Values.AccountAttributeNameStringList.of_json
                                 attributeNames) ())
           (Some Values.DescribeAccountAttributesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_address_transfers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocationIds =
         flag "allocation-ids" (optional json_arg)
           ~doc:"JSON AllocationIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeAddressTransfersMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_address_transfers
           (Values.DescribeAddressTransfersRequest.make
              ?allocationIds:(Option.map ~f:Values.AllocationIdList.of_json
                                allocationIds) ?nextToken ?maxResults ?dryRun
              ()) (Some Values.DescribeAddressTransfersResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_addresses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publicIps =
         flag "public-ips" (optional json_arg) ~doc:"JSON PublicIpStringList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and allocationIds =
         flag "allocation-ids" (optional json_arg)
           ~doc:"JSON AllocationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_addresses
           (Values.DescribeAddressesRequest.make
              ?publicIps:(Option.map ~f:Values.PublicIpStringList.of_json
                            publicIps) ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?allocationIds:(Option.map ~f:Values.AllocationIdList.of_json
                                allocationIds) ())
           (Some Values.DescribeAddressesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_addresses_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocationIds =
         flag "allocation-ids" (optional json_arg) ~doc:"JSON AllocationIds"
       and attribute =
         flag "attribute" (optional json_arg)
           ~doc:"JSON AddressAttributeName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT AddressMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_addresses_attribute
           (Values.DescribeAddressesAttributeRequest.make
              ?allocationIds:(Option.map ~f:Values.AllocationIds.of_json
                                allocationIds)
              ?attribute:(Option.map ~f:Values.AddressAttributeName.of_json
                            attribute) ?nextToken ?maxResults ?dryRun ())
           (Some Values.DescribeAddressesAttributeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_aggregate_id_format =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_aggregate_id_format
           (Values.DescribeAggregateIdFormatRequest.make ?dryRun ())
           (Some Values.DescribeAggregateIdFormatResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_availability_zones =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and zoneNames =
         flag "zone-names" (optional json_arg) ~doc:"JSON ZoneNameStringList"
       and zoneIds =
         flag "zone-ids" (optional json_arg) ~doc:"JSON ZoneIdStringList"
       and allAvailabilityZones =
         flag "all-availability-zones" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_availability_zones
           (Values.DescribeAvailabilityZonesRequest.make
              ?zoneNames:(Option.map ~f:Values.ZoneNameStringList.of_json
                            zoneNames)
              ?zoneIds:(Option.map ~f:Values.ZoneIdStringList.of_json zoneIds)
              ?allAvailabilityZones ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeAvailabilityZonesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_aws_network_performance_metric_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsParam"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_aws_network_performance_metric_subscriptions
           (Values.DescribeAwsNetworkPerformanceMetricSubscriptionsRequest.make
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some
              Values.DescribeAwsNetworkPerformanceMetricSubscriptionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_bundle_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bundleIds =
         flag "bundle-ids" (optional json_arg) ~doc:"JSON BundleIdStringList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bundle_tasks
           (Values.DescribeBundleTasksRequest.make
              ?bundleIds:(Option.map ~f:Values.BundleIdStringList.of_json
                            bundleIds) ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeBundleTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_byoip_cidrs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (required int)
           ~doc:"INT DescribeByoipCidrsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_byoip_cidrs
           (Values.DescribeByoipCidrsRequest.make ?dryRun ?nextToken
              ~maxResults ()) (Some Values.DescribeByoipCidrsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_block_extension_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityReservationIds =
         flag "capacity-reservation-ids" (optional json_arg)
           ~doc:"JSON CapacityReservationIdSet"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeFutureCapacityMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_block_extension_history
           (Values.DescribeCapacityBlockExtensionHistoryRequest.make
              ?capacityReservationIds:(Option.map
                                         ~f:Values.CapacityReservationIdSet.of_json
                                         capacityReservationIds) ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeCapacityBlockExtensionHistoryResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_block_extension_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityBlockExtensionOfferingsMaxResults"
       and capacityBlockExtensionDurationHours =
         flag "capacity-block-extension-duration-hours" (required int)
           ~doc:"INT Integer"
       and capacityReservationId =
         flag "capacity-reservation-id" (required string)
           ~doc:"STRING CapacityReservationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_block_extension_offerings
           (Values.DescribeCapacityBlockExtensionOfferingsRequest.make
              ?dryRun ?nextToken ?maxResults
              ~capacityBlockExtensionDurationHours ~capacityReservationId ())
           (Some Values.DescribeCapacityBlockExtensionOfferingsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_block_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceType =
         flag "instance-type" (optional string) ~doc:"STRING String"
       and instanceCount =
         flag "instance-count" (optional int) ~doc:"INT Integer"
       and startDateRange =
         flag "start-date-range" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and endDateRange =
         flag "end-date-range" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityBlockOfferingsMaxResults"
       and ultraserverType =
         flag "ultraserver-type" (optional string) ~doc:"STRING String"
       and ultraserverCount =
         flag "ultraserver-count" (optional int) ~doc:"INT Integer"
       and allAvailabilityZones =
         flag "all-availability-zones" (optional bool) ~doc:"BOOL Boolean"
       and capacityDurationHours =
         flag "capacity-duration-hours" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_block_offerings
           (Values.DescribeCapacityBlockOfferingsRequest.make ?dryRun
              ?instanceType ?instanceCount
              ?startDateRange:(Option.map
                                 ~f:Values.MillisecondDateTime.of_json
                                 startDateRange)
              ?endDateRange:(Option.map ~f:Values.MillisecondDateTime.of_json
                               endDateRange) ?nextToken ?maxResults
              ?ultraserverType ?ultraserverCount ?allAvailabilityZones
              ~capacityDurationHours ())
           (Some Values.DescribeCapacityBlockOfferingsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_block_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityBlockIds =
         flag "capacity-block-ids" (optional json_arg)
           ~doc:"JSON CapacityBlockIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityBlockStatusMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_block_status
           (Values.DescribeCapacityBlockStatusRequest.make
              ?capacityBlockIds:(Option.map
                                   ~f:Values.CapacityBlockIds.of_json
                                   capacityBlockIds) ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeCapacityBlockStatusResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_blocks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityBlockIds =
         flag "capacity-block-ids" (optional json_arg)
           ~doc:"JSON CapacityBlockIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityBlocksMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_blocks
           (Values.DescribeCapacityBlocksRequest.make
              ?capacityBlockIds:(Option.map
                                   ~f:Values.CapacityBlockIds.of_json
                                   capacityBlockIds) ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ()) (Some Values.DescribeCapacityBlocksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_manager_data_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityManagerDataExportIds =
         flag "capacity-manager-data-export-ids" (optional json_arg)
           ~doc:"JSON CapacityManagerDataExportIdSet"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityManagerDataExportsRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_manager_data_exports
           (Values.DescribeCapacityManagerDataExportsRequest.make
              ?capacityManagerDataExportIds:(Option.map
                                               ~f:Values.CapacityManagerDataExportIdSet.of_json
                                               capacityManagerDataExportIds)
              ?maxResults ?nextToken ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeCapacityManagerDataExportsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_reservation_billing_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityReservationIds =
         flag "capacity-reservation-ids" (optional json_arg)
           ~doc:"JSON CapacityReservationIdSet"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityReservationBillingRequestsRequestMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and role = flag "role" (required json_arg) ~doc:"JSON CallerRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_reservation_billing_requests
           (Values.DescribeCapacityReservationBillingRequestsRequest.make
              ?capacityReservationIds:(Option.map
                                         ~f:Values.CapacityReservationIdSet.of_json
                                         capacityReservationIds) ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ~role:(Values.CallerRole.of_json role) ())
           (Some
              Values.DescribeCapacityReservationBillingRequestsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_reservation_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityReservationFleetIds =
         flag "capacity-reservation-fleet-ids" (optional json_arg)
           ~doc:"JSON CapacityReservationFleetIdSet"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityReservationFleetsMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_reservation_fleets
           (Values.DescribeCapacityReservationFleetsRequest.make
              ?capacityReservationFleetIds:(Option.map
                                              ~f:Values.CapacityReservationFleetIdSet.of_json
                                              capacityReservationFleetIds)
              ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeCapacityReservationFleetsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_reservation_topology =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityReservationTopologyMaxResults"
       and capacityReservationIds =
         flag "capacity-reservation-ids" (optional json_arg)
           ~doc:"JSON CapacityReservationIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_reservation_topology
           (Values.DescribeCapacityReservationTopologyRequest.make ?dryRun
              ?nextToken ?maxResults
              ?capacityReservationIds:(Option.map
                                         ~f:Values.CapacityReservationIdSet.of_json
                                         capacityReservationIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeCapacityReservationTopologyResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_capacity_reservations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityReservationIds =
         flag "capacity-reservation-ids" (optional json_arg)
           ~doc:"JSON CapacityReservationIdSet"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCapacityReservationsMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_reservations
           (Values.DescribeCapacityReservationsRequest.make
              ?capacityReservationIds:(Option.map
                                         ~f:Values.CapacityReservationIdSet.of_json
                                         capacityReservationIds) ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeCapacityReservationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_carrier_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and carrierGatewayIds =
         flag "carrier-gateway-ids" (optional json_arg)
           ~doc:"JSON CarrierGatewayIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT CarrierGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_carrier_gateways
           (Values.DescribeCarrierGatewaysRequest.make
              ?carrierGatewayIds:(Option.map
                                    ~f:Values.CarrierGatewayIdSet.of_json
                                    carrierGatewayIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeCarrierGatewaysResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_classic_link_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeClassicLinkInstancesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_classic_link_instances
           (Values.DescribeClassicLinkInstancesRequest.make ?dryRun
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribeClassicLinkInstancesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_client_vpn_authorization_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeClientVpnAuthorizationRulesMaxResults"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_client_vpn_authorization_rules
           (Values.DescribeClientVpnAuthorizationRulesRequest.make ?dryRun
              ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ~clientVpnEndpointId ())
           (Some Values.DescribeClientVpnAuthorizationRulesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_client_vpn_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeClientVpnConnectionsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_client_vpn_connections
           (Values.DescribeClientVpnConnectionsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ?dryRun ~clientVpnEndpointId ())
           (Some Values.DescribeClientVpnConnectionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_client_vpn_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientVpnEndpointIds =
         flag "client-vpn-endpoint-ids" (optional json_arg)
           ~doc:"JSON ClientVpnEndpointIdList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeClientVpnEndpointMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_client_vpn_endpoints
           (Values.DescribeClientVpnEndpointsRequest.make
              ?clientVpnEndpointIds:(Option.map
                                       ~f:Values.ClientVpnEndpointIdList.of_json
                                       clientVpnEndpointIds) ?maxResults
              ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeClientVpnEndpointsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_client_vpn_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeClientVpnRoutesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_client_vpn_routes
           (Values.DescribeClientVpnRoutesRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ~clientVpnEndpointId ())
           (Some Values.DescribeClientVpnRoutesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_client_vpn_target_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationIds =
         flag "association-ids" (optional json_arg)
           ~doc:"JSON ValueStringList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeClientVpnTargetNetworksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and clientVpnEndpointId =
         flag "client-vpn-endpoint-id" (required string)
           ~doc:"STRING ClientVpnEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_client_vpn_target_networks
           (Values.DescribeClientVpnTargetNetworksRequest.make
              ?associationIds:(Option.map ~f:Values.ValueStringList.of_json
                                 associationIds) ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ~clientVpnEndpointId ())
           (Some Values.DescribeClientVpnTargetNetworksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_coip_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolIds =
         flag "pool-ids" (optional json_arg) ~doc:"JSON CoipPoolIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CoipPoolMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_coip_pools
           (Values.DescribeCoipPoolsRequest.make
              ?poolIds:(Option.map ~f:Values.CoipPoolIdSet.of_json poolIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeCoipPoolsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_conversion_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and conversionTaskIds =
         flag "conversion-task-ids" (optional json_arg)
           ~doc:"JSON ConversionIdStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_conversion_tasks
           (Values.DescribeConversionTasksRequest.make ?dryRun
              ?conversionTaskIds:(Option.map
                                    ~f:Values.ConversionIdStringList.of_json
                                    conversionTaskIds) ())
           (Some Values.DescribeConversionTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_customer_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customerGatewayIds =
         flag "customer-gateway-ids" (optional json_arg)
           ~doc:"JSON CustomerGatewayIdStringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_customer_gateways
           (Values.DescribeCustomerGatewaysRequest.make
              ?customerGatewayIds:(Option.map
                                     ~f:Values.CustomerGatewayIdStringList.of_json
                                     customerGatewayIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeCustomerGatewaysResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_declarative_policies_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DeclarativePoliciesMaxResults"
       and reportIds =
         flag "report-ids" (optional json_arg) ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_declarative_policies_reports
           (Values.DescribeDeclarativePoliciesReportsRequest.make ?dryRun
              ?nextToken ?maxResults
              ?reportIds:(Option.map ~f:Values.ValueStringList.of_json
                            reportIds) ())
           (Some Values.DescribeDeclarativePoliciesReportsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_dhcp_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dhcpOptionsIds =
         flag "dhcp-options-ids" (optional json_arg)
           ~doc:"JSON DhcpOptionsIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeDhcpOptionsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dhcp_options
           (Values.DescribeDhcpOptionsRequest.make
              ?dhcpOptionsIds:(Option.map
                                 ~f:Values.DhcpOptionsIdStringList.of_json
                                 dhcpOptionsIds) ?nextToken ?maxResults
              ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeDhcpOptionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_egress_only_internet_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and egressOnlyInternetGatewayIds =
         flag "egress-only-internet-gateway-ids" (optional json_arg)
           ~doc:"JSON EgressOnlyInternetGatewayIdList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeEgressOnlyInternetGatewaysMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_egress_only_internet_gateways
           (Values.DescribeEgressOnlyInternetGatewaysRequest.make ?dryRun
              ?egressOnlyInternetGatewayIds:(Option.map
                                               ~f:Values.EgressOnlyInternetGatewayIdList.of_json
                                               egressOnlyInternetGatewayIds)
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeEgressOnlyInternetGatewaysResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_elastic_gpus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticGpuIds =
         flag "elastic-gpu-ids" (optional json_arg)
           ~doc:"JSON ElasticGpuIdSet"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeElasticGpusMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elastic_gpus
           (Values.DescribeElasticGpusRequest.make
              ?elasticGpuIds:(Option.map ~f:Values.ElasticGpuIdSet.of_json
                                elasticGpuIds) ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeElasticGpusResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_export_image_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and exportImageTaskIds =
         flag "export-image-task-ids" (optional json_arg)
           ~doc:"JSON ExportImageTaskIdList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeExportImageTasksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export_image_tasks
           (Values.DescribeExportImageTasksRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?exportImageTaskIds:(Option.map
                                     ~f:Values.ExportImageTaskIdList.of_json
                                     exportImageTaskIds) ?maxResults
              ?nextToken ())
           (Some Values.DescribeExportImageTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_export_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and exportTaskIds =
         flag "export-task-ids" (optional json_arg)
           ~doc:"JSON ExportTaskIdStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export_tasks
           (Values.DescribeExportTasksRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?exportTaskIds:(Option.map
                                ~f:Values.ExportTaskIdStringList.of_json
                                exportTaskIds) ())
           (Some Values.DescribeExportTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fast_launch_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageIds =
         flag "image-ids" (optional json_arg)
           ~doc:"JSON FastLaunchImageIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeFastLaunchImagesRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fast_launch_images
           (Values.DescribeFastLaunchImagesRequest.make
              ?imageIds:(Option.map ~f:Values.FastLaunchImageIdList.of_json
                           imageIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeFastLaunchImagesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fast_snapshot_restores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeFastSnapshotRestoresMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fast_snapshot_restores
           (Values.DescribeFastSnapshotRestoresRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeFastSnapshotRestoresResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fleet_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and eventType =
         flag "event-type" (optional json_arg) ~doc:"JSON FleetEventType"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON DateTime" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_history
           (Values.DescribeFleetHistoryRequest.make ?dryRun
              ?eventType:(Option.map ~f:Values.FleetEventType.of_json
                            eventType) ?maxResults ?nextToken ~fleetId
              ~startTime:(Values.DateTime.of_json startTime) ())
           (Some Values.DescribeFleetHistoryResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fleet_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_instances
           (Values.DescribeFleetInstancesRequest.make ?dryRun ?maxResults
              ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~fleetId ()) (Some Values.DescribeFleetInstancesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and fleetIds =
         flag "fleet-ids" (optional json_arg) ~doc:"JSON FleetIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleets
           (Values.DescribeFleetsRequest.make ?dryRun ?maxResults ?nextToken
              ?fleetIds:(Option.map ~f:Values.FleetIdSet.of_json fleetIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeFleetsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_flow_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON FilterList"
       and flowLogIds =
         flag "flow-log-ids" (optional json_arg) ~doc:"JSON FlowLogIdList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow_logs
           (Values.DescribeFlowLogsRequest.make ?dryRun
              ?filter:(Option.map ~f:Values.FilterList.of_json filter)
              ?flowLogIds:(Option.map ~f:Values.FlowLogIdList.of_json
                             flowLogIds) ?maxResults ?nextToken ())
           (Some Values.DescribeFlowLogsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fpga_image_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and fpgaImageId =
         flag "fpga-image-id" (required string) ~doc:"STRING FpgaImageId"
       and attribute =
         flag "attribute" (required json_arg)
           ~doc:"JSON FpgaImageAttributeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fpga_image_attribute
           (Values.DescribeFpgaImageAttributeRequest.make ?dryRun
              ~fpgaImageId
              ~attribute:(Values.FpgaImageAttributeName.of_json attribute) ())
           (Some Values.DescribeFpgaImageAttributeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_fpga_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and fpgaImageIds =
         flag "fpga-image-ids" (optional json_arg)
           ~doc:"JSON FpgaImageIdList"
       and owners =
         flag "owners" (optional json_arg) ~doc:"JSON OwnerStringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeFpgaImagesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fpga_images
           (Values.DescribeFpgaImagesRequest.make ?dryRun
              ?fpgaImageIds:(Option.map ~f:Values.FpgaImageIdList.of_json
                               fpgaImageIds)
              ?owners:(Option.map ~f:Values.OwnerStringList.of_json owners)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribeFpgaImagesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_host_reservation_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON FilterList"
       and maxDuration =
         flag "max-duration" (optional int) ~doc:"INT Integer"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeHostReservationsMaxResults"
       and minDuration =
         flag "min-duration" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and offeringId =
         flag "offering-id" (optional string) ~doc:"STRING OfferingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_host_reservation_offerings
           (Values.DescribeHostReservationOfferingsRequest.make
              ?filter:(Option.map ~f:Values.FilterList.of_json filter)
              ?maxDuration ?maxResults ?minDuration ?nextToken ?offeringId ())
           (Some Values.DescribeHostReservationOfferingsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_host_reservations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON FilterList"
       and hostReservationIdSet =
         flag "host-reservation-id-set" (optional json_arg)
           ~doc:"JSON HostReservationIdSet"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_host_reservations
           (Values.DescribeHostReservationsRequest.make
              ?filter:(Option.map ~f:Values.FilterList.of_json filter)
              ?hostReservationIdSet:(Option.map
                                       ~f:Values.HostReservationIdSet.of_json
                                       hostReservationIdSet) ?maxResults
              ?nextToken ())
           (Some Values.DescribeHostReservationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_hosts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostIds =
         flag "host-ids" (optional json_arg) ~doc:"JSON RequestHostIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hosts
           (Values.DescribeHostsRequest.make
              ?hostIds:(Option.map ~f:Values.RequestHostIdList.of_json
                          hostIds) ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.FilterList.of_json filter) ())
           (Some Values.DescribeHostsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_iam_instance_profile_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationIds =
         flag "association-ids" (optional json_arg)
           ~doc:"JSON AssociationIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeIamInstanceProfileAssociationsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_iam_instance_profile_associations
           (Values.DescribeIamInstanceProfileAssociationsRequest.make
              ?associationIds:(Option.map ~f:Values.AssociationIdList.of_json
                                 associationIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeIamInstanceProfileAssociationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_id_format =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource = flag "resource" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_id_format
           (Values.DescribeIdFormatRequest.make ?resource ())
           (Some Values.DescribeIdFormatResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_identity_id_format =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource = flag "resource" (optional string) ~doc:"STRING String"
       and principalArn =
         flag "principal-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_id_format
           (Values.DescribeIdentityIdFormatRequest.make ?resource
              ~principalArn ())
           (Some Values.DescribeIdentityIdFormatResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_image_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and attribute =
         flag "attribute" (required json_arg) ~doc:"JSON ImageAttributeName"
       and imageId = flag "image-id" (required string) ~doc:"STRING ImageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_attribute
           (Values.DescribeImageAttributeRequest.make ?dryRun
              ~attribute:(Values.ImageAttributeName.of_json attribute)
              ~imageId ()) (Some Values.ImageAttribute.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_image_references =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeAllResourceTypes =
         flag "include-all-resource-types" (optional bool)
           ~doc:"BOOL Boolean"
       and resourceTypes =
         flag "resource-types" (optional json_arg)
           ~doc:"JSON ResourceTypeRequestList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeImageReferencesMaxResults"
       and imageIds =
         flag "image-ids" (required json_arg)
           ~doc:"JSON DescribeImageReferencesImageIdStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_references
           (Values.DescribeImageReferencesRequest.make
              ?includeAllResourceTypes
              ?resourceTypes:(Option.map
                                ~f:Values.ResourceTypeRequestList.of_json
                                resourceTypes) ?nextToken ?dryRun ?maxResults
              ~imageIds:(Values.DescribeImageReferencesImageIdStringList.of_json
                           imageIds) ())
           (Some Values.DescribeImageReferencesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_image_usage_report_entries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageIds =
         flag "image-ids" (optional json_arg)
           ~doc:"JSON DescribeImageUsageReportsImageIdStringList"
       and reportIds =
         flag "report-ids" (optional json_arg)
           ~doc:"JSON ImageUsageReportIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeImageUsageReportEntriesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_usage_report_entries
           (Values.DescribeImageUsageReportEntriesRequest.make
              ?imageIds:(Option.map
                           ~f:Values.DescribeImageUsageReportsImageIdStringList.of_json
                           imageIds)
              ?reportIds:(Option.map
                            ~f:Values.ImageUsageReportIdStringList.of_json
                            reportIds) ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ?maxResults ())
           (Some Values.DescribeImageUsageReportEntriesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_image_usage_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageIds =
         flag "image-ids" (optional json_arg)
           ~doc:"JSON DescribeImageUsageReportsImageIdStringList"
       and reportIds =
         flag "report-ids" (optional json_arg)
           ~doc:"JSON ImageUsageReportIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeImageUsageReportsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_usage_reports
           (Values.DescribeImageUsageReportsRequest.make
              ?imageIds:(Option.map
                           ~f:Values.DescribeImageUsageReportsImageIdStringList.of_json
                           imageIds)
              ?reportIds:(Option.map
                            ~f:Values.ImageUsageReportIdStringList.of_json
                            reportIds) ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ?maxResults ())
           (Some Values.DescribeImageUsageReportsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executableUsers =
         flag "executable-users" (optional json_arg)
           ~doc:"JSON ExecutableByStringList"
       and imageIds =
         flag "image-ids" (optional json_arg) ~doc:"JSON ImageIdStringList"
       and owners =
         flag "owners" (optional json_arg) ~doc:"JSON OwnerStringList"
       and includeDeprecated =
         flag "include-deprecated" (optional bool) ~doc:"BOOL Boolean"
       and includeDisabled =
         flag "include-disabled" (optional bool) ~doc:"BOOL Boolean"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_images
           (Values.DescribeImagesRequest.make
              ?executableUsers:(Option.map
                                  ~f:Values.ExecutableByStringList.of_json
                                  executableUsers)
              ?imageIds:(Option.map ~f:Values.ImageIdStringList.of_json
                           imageIds)
              ?owners:(Option.map ~f:Values.OwnerStringList.of_json owners)
              ?includeDeprecated ?includeDisabled ?maxResults ?nextToken
              ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeImagesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_import_image_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and importTaskIds =
         flag "import-task-ids" (optional json_arg)
           ~doc:"JSON ImportTaskIdList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_import_image_tasks
           (Values.DescribeImportImageTasksRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?importTaskIds:(Option.map ~f:Values.ImportTaskIdList.of_json
                                importTaskIds) ?maxResults ?nextToken ())
           (Some Values.DescribeImportImageTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_import_snapshot_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and importTaskIds =
         flag "import-task-ids" (optional json_arg)
           ~doc:"JSON ImportSnapshotTaskIdList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_import_snapshot_tasks
           (Values.DescribeImportSnapshotTasksRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?importTaskIds:(Option.map
                                ~f:Values.ImportSnapshotTaskIdList.of_json
                                importTaskIds) ?maxResults ?nextToken ())
           (Some Values.DescribeImportSnapshotTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and attribute =
         flag "attribute" (required json_arg)
           ~doc:"JSON InstanceAttributeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_attribute
           (Values.DescribeInstanceAttributeRequest.make ?dryRun ~instanceId
              ~attribute:(Values.InstanceAttributeName.of_json attribute) ())
           (Some Values.InstanceAttribute.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_connect_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT InstanceConnectEndpointMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and instanceConnectEndpointIds =
         flag "instance-connect-endpoint-ids" (optional json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_connect_endpoints
           (Values.DescribeInstanceConnectEndpointsRequest.make ?dryRun
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?instanceConnectEndpointIds:(Option.map
                                             ~f:Values.ValueStringList.of_json
                                             instanceConnectEndpointIds) ())
           (Some Values.DescribeInstanceConnectEndpointsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_credit_specifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeInstanceCreditSpecificationsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_credit_specifications
           (Values.DescribeInstanceCreditSpecificationsRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds) ?maxResults ?nextToken ())
           (Some Values.DescribeInstanceCreditSpecificationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_event_notification_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_event_notification_attributes
           (Values.DescribeInstanceEventNotificationAttributesRequest.make
              ?dryRun ())
           (Some
              Values.DescribeInstanceEventNotificationAttributesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_event_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceEventWindowIds =
         flag "instance-event-window-ids" (optional json_arg)
           ~doc:"JSON InstanceEventWindowIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultRange"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_event_windows
           (Values.DescribeInstanceEventWindowsRequest.make ?dryRun
              ?instanceEventWindowIds:(Option.map
                                         ~f:Values.InstanceEventWindowIdSet.of_json
                                         instanceEventWindowIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeInstanceEventWindowsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_image_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeInstanceImageMetadataMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_image_metadata
           (Values.DescribeInstanceImageMetadataRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds) ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeInstanceImageMetadataResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_sql_ha_history_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON MillisecondDateTime"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeInstanceSqlHaStatesRequestMaxResultsInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_sql_ha_history_states
           (Values.DescribeInstanceSqlHaHistoryStatesRequest.make
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds)
              ?startTime:(Option.map ~f:Values.MillisecondDateTime.of_json
                            startTime)
              ?endTime:(Option.map ~f:Values.MillisecondDateTime.of_json
                          endTime) ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeInstanceSqlHaHistoryStatesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_sql_ha_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeInstanceSqlHaStatesRequestMaxResultsInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_sql_ha_states
           (Values.DescribeInstanceSqlHaStatesRequest.make
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds) ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?dryRun ())
           (Some Values.DescribeInstanceSqlHaStatesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and includeManagedResources =
         flag "include-managed-resources" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and includeAllInstances =
         flag "include-all-instances" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_status
           (Values.DescribeInstanceStatusRequest.make
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds) ?maxResults ?nextToken
              ?includeManagedResources ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?includeAllInstances ())
           (Some Values.DescribeInstanceStatusResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_topology =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeInstanceTopologyMaxResults"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON DescribeInstanceTopologyInstanceIdSet"
       and groupNames =
         flag "group-names" (optional json_arg)
           ~doc:"JSON DescribeInstanceTopologyGroupNameSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_topology
           (Values.DescribeInstanceTopologyRequest.make ?dryRun ?nextToken
              ?maxResults
              ?instanceIds:(Option.map
                              ~f:Values.DescribeInstanceTopologyInstanceIdSet.of_json
                              instanceIds)
              ?groupNames:(Option.map
                             ~f:Values.DescribeInstanceTopologyGroupNameSet.of_json
                             groupNames)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeInstanceTopologyResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_type_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and locationType =
         flag "location-type" (optional json_arg) ~doc:"JSON LocationType"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DITOMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_type_offerings
           (Values.DescribeInstanceTypeOfferingsRequest.make ?dryRun
              ?locationType:(Option.map ~f:Values.LocationType.of_json
                               locationType)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeInstanceTypeOfferingsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instance_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and instanceTypes =
         flag "instance-types" (optional json_arg)
           ~doc:"JSON RequestInstanceTypeList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DITMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and includeUnsupportedInRegion =
         flag "include-unsupported-in-region" (optional bool)
           ~doc:"BOOL IncludeUnsupportedInRegion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_types
           (Values.DescribeInstanceTypesRequest.make ?dryRun
              ?instanceTypes:(Option.map
                                ~f:Values.RequestInstanceTypeList.of_json
                                instanceTypes)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?includeUnsupportedInRegion ())
           (Some Values.DescribeInstanceTypesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON InstanceIdStringList"
       and includeManagedResources =
         flag "include-managed-resources" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instances
           (Values.DescribeInstancesRequest.make
              ?instanceIds:(Option.map ~f:Values.InstanceIdStringList.of_json
                              instanceIds) ?includeManagedResources ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribeInstancesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_internet_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeInternetGatewaysMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and internetGatewayIds =
         flag "internet-gateway-ids" (optional json_arg)
           ~doc:"JSON InternetGatewayIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_internet_gateways
           (Values.DescribeInternetGatewaysRequest.make ?nextToken
              ?maxResults ?dryRun
              ?internetGatewayIds:(Option.map
                                     ~f:Values.InternetGatewayIdList.of_json
                                     internetGatewayIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeInternetGatewaysResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_byoasn =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeIpamByoasnMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_byoasn
           (Values.DescribeIpamByoasnRequest.make ?dryRun ?maxResults
              ?nextToken ()) (Some Values.DescribeIpamByoasnResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_external_resource_verification_tokens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and ipamExternalResourceVerificationTokenIds =
         flag "ipam-external-resource-verification-token-ids"
           (optional json_arg) ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_external_resource_verification_tokens
           (Values.DescribeIpamExternalResourceVerificationTokensRequest.make
              ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults
              ?ipamExternalResourceVerificationTokenIds:(Option.map
                                                           ~f:Values.ValueStringList.of_json
                                                           ipamExternalResourceVerificationTokenIds)
              ())
           (Some
              Values.DescribeIpamExternalResourceVerificationTokensResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ipamPolicyIds =
         flag "ipam-policy-ids" (optional json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_policies
           (Values.DescribeIpamPoliciesRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?ipamPolicyIds:(Option.map ~f:Values.ValueStringList.of_json
                                ipamPolicyIds) ())
           (Some Values.DescribeIpamPoliciesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ipamPoolIds =
         flag "ipam-pool-ids" (optional json_arg) ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_pools
           (Values.DescribeIpamPoolsRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?ipamPoolIds:(Option.map ~f:Values.ValueStringList.of_json
                              ipamPoolIds) ())
           (Some Values.DescribeIpamPoolsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_prefix_list_resolver_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ipamPrefixListResolverTargetIds =
         flag "ipam-prefix-list-resolver-target-ids" (optional json_arg)
           ~doc:"JSON ValueStringList"
       and ipamPrefixListResolverId =
         flag "ipam-prefix-list-resolver-id" (optional string)
           ~doc:"STRING IpamPrefixListResolverId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_prefix_list_resolver_targets
           (Values.DescribeIpamPrefixListResolverTargetsRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?ipamPrefixListResolverTargetIds:(Option.map
                                                  ~f:Values.ValueStringList.of_json
                                                  ipamPrefixListResolverTargetIds)
              ?ipamPrefixListResolverId ())
           (Some Values.DescribeIpamPrefixListResolverTargetsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_prefix_list_resolvers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ipamPrefixListResolverIds =
         flag "ipam-prefix-list-resolver-ids" (optional json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_prefix_list_resolvers
           (Values.DescribeIpamPrefixListResolversRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?ipamPrefixListResolverIds:(Option.map
                                            ~f:Values.ValueStringList.of_json
                                            ipamPrefixListResolverIds) ())
           (Some Values.DescribeIpamPrefixListResolversResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_resource_discoveries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipamResourceDiscoveryIds =
         flag "ipam-resource-discovery-ids" (optional json_arg)
           ~doc:"JSON ValueStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_resource_discoveries
           (Values.DescribeIpamResourceDiscoveriesRequest.make ?dryRun
              ?ipamResourceDiscoveryIds:(Option.map
                                           ~f:Values.ValueStringList.of_json
                                           ipamResourceDiscoveryIds)
              ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeIpamResourceDiscoveriesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_resource_discovery_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ipamResourceDiscoveryAssociationIds =
         flag "ipam-resource-discovery-association-ids" (optional json_arg)
           ~doc:"JSON ValueStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_resource_discovery_associations
           (Values.DescribeIpamResourceDiscoveryAssociationsRequest.make
              ?dryRun
              ?ipamResourceDiscoveryAssociationIds:(Option.map
                                                      ~f:Values.ValueStringList.of_json
                                                      ipamResourceDiscoveryAssociationIds)
              ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some
              Values.DescribeIpamResourceDiscoveryAssociationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipam_scopes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ipamScopeIds =
         flag "ipam-scope-ids" (optional json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipam_scopes
           (Values.DescribeIpamScopesRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?ipamScopeIds:(Option.map ~f:Values.ValueStringList.of_json
                               ipamScopeIds) ())
           (Some Values.DescribeIpamScopesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IpamMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ipamIds =
         flag "ipam-ids" (optional json_arg) ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipams
           (Values.DescribeIpamsRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?ipamIds:(Option.map ~f:Values.ValueStringList.of_json ipamIds)
              ()) (Some Values.DescribeIpamsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_ipv6_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolIds =
         flag "pool-ids" (optional json_arg) ~doc:"JSON Ipv6PoolIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT Ipv6PoolMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ipv6_pools
           (Values.DescribeIpv6PoolsRequest.make
              ?poolIds:(Option.map ~f:Values.Ipv6PoolIdList.of_json poolIds)
              ?nextToken ?maxResults ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeIpv6PoolsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_key_pairs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyNames =
         flag "key-names" (optional json_arg) ~doc:"JSON KeyNameStringList"
       and keyPairIds =
         flag "key-pair-ids" (optional json_arg)
           ~doc:"JSON KeyPairIdStringList"
       and includePublicKey =
         flag "include-public-key" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_key_pairs
           (Values.DescribeKeyPairsRequest.make
              ?keyNames:(Option.map ~f:Values.KeyNameStringList.of_json
                           keyNames)
              ?keyPairIds:(Option.map ~f:Values.KeyPairIdStringList.of_json
                             keyPairIds) ?includePublicKey ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeKeyPairsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_launch_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and launchTemplateId =
         flag "launch-template-id" (optional string)
           ~doc:"STRING LaunchTemplateId"
       and launchTemplateName =
         flag "launch-template-name" (optional string)
           ~doc:"STRING LaunchTemplateName"
       and versions =
         flag "versions" (optional json_arg) ~doc:"JSON VersionStringList"
       and minVersion =
         flag "min-version" (optional string) ~doc:"STRING String"
       and maxVersion =
         flag "max-version" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and resolveAlias =
         flag "resolve-alias" (optional bool) ~doc:"BOOL Boolean"
       and includeManagedResources =
         flag "include-managed-resources" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_launch_template_versions
           (Values.DescribeLaunchTemplateVersionsRequest.make ?dryRun
              ?launchTemplateId ?launchTemplateName
              ?versions:(Option.map ~f:Values.VersionStringList.of_json
                           versions) ?minVersion ?maxVersion ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?resolveAlias ?includeManagedResources ())
           (Some Values.DescribeLaunchTemplateVersionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_launch_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and launchTemplateIds =
         flag "launch-template-ids" (optional json_arg)
           ~doc:"JSON LaunchTemplateIdStringList"
       and launchTemplateNames =
         flag "launch-template-names" (optional json_arg)
           ~doc:"JSON LaunchTemplateNameStringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeLaunchTemplatesMaxResults"
       and includeManagedResources =
         flag "include-managed-resources" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_launch_templates
           (Values.DescribeLaunchTemplatesRequest.make ?dryRun
              ?launchTemplateIds:(Option.map
                                    ~f:Values.LaunchTemplateIdStringList.of_json
                                    launchTemplateIds)
              ?launchTemplateNames:(Option.map
                                      ~f:Values.LaunchTemplateNameStringList.of_json
                                      launchTemplateNames)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ?includeManagedResources ())
           (Some Values.DescribeLaunchTemplatesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_local_gateway_route_table_virtual_interface_group_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localGatewayRouteTableVirtualInterfaceGroupAssociationIds =
         flag
           "local-gateway-route-table-virtual-interface-group-association-ids"
           (optional json_arg)
           ~doc:"JSON LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LocalGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_local_gateway_route_table_virtual_interface_group_associations
           (Values.DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsRequest.make
              ?localGatewayRouteTableVirtualInterfaceGroupAssociationIds:(
              Option.map
                ~f:Values.LocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet.of_json
                localGatewayRouteTableVirtualInterfaceGroupAssociationIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some
              Values.DescribeLocalGatewayRouteTableVirtualInterfaceGroupAssociationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_local_gateway_route_table_vpc_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localGatewayRouteTableVpcAssociationIds =
         flag "local-gateway-route-table-vpc-association-ids"
           (optional json_arg)
           ~doc:"JSON LocalGatewayRouteTableVpcAssociationIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LocalGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_local_gateway_route_table_vpc_associations
           (Values.DescribeLocalGatewayRouteTableVpcAssociationsRequest.make
              ?localGatewayRouteTableVpcAssociationIds:(Option.map
                                                          ~f:Values.LocalGatewayRouteTableVpcAssociationIdSet.of_json
                                                          localGatewayRouteTableVpcAssociationIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some
              Values.DescribeLocalGatewayRouteTableVpcAssociationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_local_gateway_route_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localGatewayRouteTableIds =
         flag "local-gateway-route-table-ids" (optional json_arg)
           ~doc:"JSON LocalGatewayRouteTableIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LocalGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_local_gateway_route_tables
           (Values.DescribeLocalGatewayRouteTablesRequest.make
              ?localGatewayRouteTableIds:(Option.map
                                            ~f:Values.LocalGatewayRouteTableIdSet.of_json
                                            localGatewayRouteTableIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeLocalGatewayRouteTablesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_local_gateway_virtual_interface_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localGatewayVirtualInterfaceGroupIds =
         flag "local-gateway-virtual-interface-group-ids" (optional json_arg)
           ~doc:"JSON LocalGatewayVirtualInterfaceGroupIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LocalGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_local_gateway_virtual_interface_groups
           (Values.DescribeLocalGatewayVirtualInterfaceGroupsRequest.make
              ?localGatewayVirtualInterfaceGroupIds:(Option.map
                                                       ~f:Values.LocalGatewayVirtualInterfaceGroupIdSet.of_json
                                                       localGatewayVirtualInterfaceGroupIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some
              Values.DescribeLocalGatewayVirtualInterfaceGroupsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_local_gateway_virtual_interfaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localGatewayVirtualInterfaceIds =
         flag "local-gateway-virtual-interface-ids" (optional json_arg)
           ~doc:"JSON LocalGatewayVirtualInterfaceIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LocalGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_local_gateway_virtual_interfaces
           (Values.DescribeLocalGatewayVirtualInterfacesRequest.make
              ?localGatewayVirtualInterfaceIds:(Option.map
                                                  ~f:Values.LocalGatewayVirtualInterfaceIdSet.of_json
                                                  localGatewayVirtualInterfaceIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeLocalGatewayVirtualInterfacesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_local_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localGatewayIds =
         flag "local-gateway-ids" (optional json_arg)
           ~doc:"JSON LocalGatewayIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LocalGatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_local_gateways
           (Values.DescribeLocalGatewaysRequest.make
              ?localGatewayIds:(Option.map
                                  ~f:Values.LocalGatewayIdSet.of_json
                                  localGatewayIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeLocalGatewaysResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_locked_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeLockedSnapshotsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and snapshotIds =
         flag "snapshot-ids" (optional json_arg)
           ~doc:"JSON SnapshotIdStringList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_locked_snapshots
           (Values.DescribeLockedSnapshotsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?snapshotIds:(Option.map ~f:Values.SnapshotIdStringList.of_json
                              snapshotIds) ?dryRun ())
           (Some Values.DescribeLockedSnapshotsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_mac_hosts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and hostIds =
         flag "host-ids" (optional json_arg) ~doc:"JSON RequestHostIdList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeMacHostsRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mac_hosts
           (Values.DescribeMacHostsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?hostIds:(Option.map ~f:Values.RequestHostIdList.of_json
                          hostIds) ?maxResults ?nextToken ())
           (Some Values.DescribeMacHostsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_mac_modification_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and macModificationTaskIds =
         flag "mac-modification-task-ids" (optional json_arg)
           ~doc:"JSON MacModificationTaskIdList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeMacModificationTasksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mac_modification_tasks
           (Values.DescribeMacModificationTasksRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?macModificationTaskIds:(Option.map
                                         ~f:Values.MacModificationTaskIdList.of_json
                                         macModificationTaskIds) ?maxResults
              ?nextToken ())
           (Some Values.DescribeMacModificationTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_managed_prefix_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PrefixListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and prefixListIds =
         flag "prefix-list-ids" (optional json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_managed_prefix_lists
           (Values.DescribeManagedPrefixListsRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?prefixListIds:(Option.map ~f:Values.ValueStringList.of_json
                                prefixListIds) ())
           (Some Values.DescribeManagedPrefixListsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_moving_addresses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and publicIps =
         flag "public-ips" (optional json_arg) ~doc:"JSON ValueStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeMovingAddressesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_moving_addresses
           (Values.DescribeMovingAddressesRequest.make ?dryRun
              ?publicIps:(Option.map ~f:Values.ValueStringList.of_json
                            publicIps) ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ())
           (Some Values.DescribeMovingAddressesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_nat_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeNatGatewaysMaxResults"
       and natGatewayIds =
         flag "nat-gateway-ids" (optional json_arg)
           ~doc:"JSON NatGatewayIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_nat_gateways
           (Values.DescribeNatGatewaysRequest.make ?dryRun
              ?filter:(Option.map ~f:Values.FilterList.of_json filter)
              ?maxResults
              ?natGatewayIds:(Option.map
                                ~f:Values.NatGatewayIdStringList.of_json
                                natGatewayIds) ?nextToken ())
           (Some Values.DescribeNatGatewaysResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_acls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeNetworkAclsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkAclIds =
         flag "network-acl-ids" (optional json_arg)
           ~doc:"JSON NetworkAclIdStringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_acls
           (Values.DescribeNetworkAclsRequest.make ?nextToken ?maxResults
              ?dryRun
              ?networkAclIds:(Option.map
                                ~f:Values.NetworkAclIdStringList.of_json
                                networkAclIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeNetworkAclsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_insights_access_scope_analyses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkInsightsAccessScopeAnalysisIds =
         flag "network-insights-access-scope-analysis-ids"
           (optional json_arg)
           ~doc:"JSON NetworkInsightsAccessScopeAnalysisIdList"
       and networkInsightsAccessScopeId =
         flag "network-insights-access-scope-id" (optional string)
           ~doc:"STRING NetworkInsightsAccessScopeId"
       and analysisStartTimeBegin =
         flag "analysis-start-time-begin" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and analysisStartTimeEnd =
         flag "analysis-start-time-end" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT NetworkInsightsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_insights_access_scope_analyses
           (Values.DescribeNetworkInsightsAccessScopeAnalysesRequest.make
              ?networkInsightsAccessScopeAnalysisIds:(Option.map
                                                        ~f:Values.NetworkInsightsAccessScopeAnalysisIdList.of_json
                                                        networkInsightsAccessScopeAnalysisIds)
              ?networkInsightsAccessScopeId
              ?analysisStartTimeBegin:(Option.map
                                         ~f:Values.MillisecondDateTime.of_json
                                         analysisStartTimeBegin)
              ?analysisStartTimeEnd:(Option.map
                                       ~f:Values.MillisecondDateTime.of_json
                                       analysisStartTimeEnd)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?dryRun ?nextToken ())
           (Some
              Values.DescribeNetworkInsightsAccessScopeAnalysesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_insights_access_scopes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkInsightsAccessScopeIds =
         flag "network-insights-access-scope-ids" (optional json_arg)
           ~doc:"JSON NetworkInsightsAccessScopeIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT NetworkInsightsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_insights_access_scopes
           (Values.DescribeNetworkInsightsAccessScopesRequest.make
              ?networkInsightsAccessScopeIds:(Option.map
                                                ~f:Values.NetworkInsightsAccessScopeIdList.of_json
                                                networkInsightsAccessScopeIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?dryRun ?nextToken ())
           (Some Values.DescribeNetworkInsightsAccessScopesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_insights_analyses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkInsightsAnalysisIds =
         flag "network-insights-analysis-ids" (optional json_arg)
           ~doc:"JSON NetworkInsightsAnalysisIdList"
       and networkInsightsPathId =
         flag "network-insights-path-id" (optional string)
           ~doc:"STRING NetworkInsightsPathId"
       and analysisStartTime =
         flag "analysis-start-time" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and analysisEndTime =
         flag "analysis-end-time" (optional json_arg)
           ~doc:"JSON MillisecondDateTime"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT NetworkInsightsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_insights_analyses
           (Values.DescribeNetworkInsightsAnalysesRequest.make
              ?networkInsightsAnalysisIds:(Option.map
                                             ~f:Values.NetworkInsightsAnalysisIdList.of_json
                                             networkInsightsAnalysisIds)
              ?networkInsightsPathId
              ?analysisStartTime:(Option.map
                                    ~f:Values.MillisecondDateTime.of_json
                                    analysisStartTime)
              ?analysisEndTime:(Option.map
                                  ~f:Values.MillisecondDateTime.of_json
                                  analysisEndTime)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?dryRun ?nextToken ())
           (Some Values.DescribeNetworkInsightsAnalysesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_insights_paths =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkInsightsPathIds =
         flag "network-insights-path-ids" (optional json_arg)
           ~doc:"JSON NetworkInsightsPathIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT NetworkInsightsMaxResults"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_insights_paths
           (Values.DescribeNetworkInsightsPathsRequest.make
              ?networkInsightsPathIds:(Option.map
                                         ~f:Values.NetworkInsightsPathIdList.of_json
                                         networkInsightsPathIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?dryRun ?nextToken ())
           (Some Values.DescribeNetworkInsightsPathsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_interface_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and attribute =
         flag "attribute" (optional json_arg)
           ~doc:"JSON NetworkInterfaceAttribute"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_interface_attribute
           (Values.DescribeNetworkInterfaceAttributeRequest.make ?dryRun
              ?attribute:(Option.map
                            ~f:Values.NetworkInterfaceAttribute.of_json
                            attribute) ~networkInterfaceId ())
           (Some Values.DescribeNetworkInterfaceAttributeResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_interface_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkInterfacePermissionIds =
         flag "network-interface-permission-ids" (optional json_arg)
           ~doc:"JSON NetworkInterfacePermissionIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeNetworkInterfacePermissionsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_interface_permissions
           (Values.DescribeNetworkInterfacePermissionsRequest.make
              ?networkInterfacePermissionIds:(Option.map
                                                ~f:Values.NetworkInterfacePermissionIdList.of_json
                                                networkInterfacePermissionIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribeNetworkInterfacePermissionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_network_interfaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeNetworkInterfacesMaxResults"
       and includeManagedResources =
         flag "include-managed-resources" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkInterfaceIds =
         flag "network-interface-ids" (optional json_arg)
           ~doc:"JSON NetworkInterfaceIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network_interfaces
           (Values.DescribeNetworkInterfacesRequest.make ?nextToken
              ?maxResults ?includeManagedResources ?dryRun
              ?networkInterfaceIds:(Option.map
                                      ~f:Values.NetworkInterfaceIdList.of_json
                                      networkInterfaceIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeNetworkInterfacesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_outpost_lags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outpostLagIds =
         flag "outpost-lag-ids" (optional json_arg)
           ~doc:"JSON OutpostLagIdSet"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT OutpostLagMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_outpost_lags
           (Values.DescribeOutpostLagsRequest.make
              ?outpostLagIds:(Option.map ~f:Values.OutpostLagIdSet.of_json
                                outpostLagIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeOutpostLagsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_placement_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupIds =
         flag "group-ids" (optional json_arg)
           ~doc:"JSON PlacementGroupIdStringList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and groupNames =
         flag "group-names" (optional json_arg)
           ~doc:"JSON PlacementGroupStringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_placement_groups
           (Values.DescribePlacementGroupsRequest.make
              ?groupIds:(Option.map
                           ~f:Values.PlacementGroupIdStringList.of_json
                           groupIds) ?dryRun
              ?groupNames:(Option.map
                             ~f:Values.PlacementGroupStringList.of_json
                             groupNames)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribePlacementGroupsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_prefix_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and prefixListIds =
         flag "prefix-list-ids" (optional json_arg)
           ~doc:"JSON PrefixListResourceIdStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_prefix_lists
           (Values.DescribePrefixListsRequest.make ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?prefixListIds:(Option.map
                                ~f:Values.PrefixListResourceIdStringList.of_json
                                prefixListIds) ())
           (Some Values.DescribePrefixListsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_principal_id_format =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and resources =
         flag "resources" (optional json_arg) ~doc:"JSON ResourceList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribePrincipalIdFormatMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_principal_id_format
           (Values.DescribePrincipalIdFormatRequest.make ?dryRun
              ?resources:(Option.map ~f:Values.ResourceList.of_json resources)
              ?maxResults ?nextToken ())
           (Some Values.DescribePrincipalIdFormatResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_public_ipv4_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolIds =
         flag "pool-ids" (optional json_arg)
           ~doc:"JSON PublicIpv4PoolIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PoolMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_public_ipv4_pools
           (Values.DescribePublicIpv4PoolsRequest.make
              ?poolIds:(Option.map
                          ~f:Values.PublicIpv4PoolIdStringList.of_json
                          poolIds) ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribePublicIpv4PoolsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionNames =
         flag "region-names" (optional json_arg)
           ~doc:"JSON RegionNameStringList"
       and allRegions =
         flag "all-regions" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_regions
           (Values.DescribeRegionsRequest.make
              ?regionNames:(Option.map ~f:Values.RegionNameStringList.of_json
                              regionNames) ?allRegions ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeRegionsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_replace_root_volume_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replaceRootVolumeTaskIds =
         flag "replace-root-volume-task-ids" (optional json_arg)
           ~doc:"JSON ReplaceRootVolumeTaskIds"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeReplaceRootVolumeTasksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replace_root_volume_tasks
           (Values.DescribeReplaceRootVolumeTasksRequest.make
              ?replaceRootVolumeTaskIds:(Option.map
                                           ~f:Values.ReplaceRootVolumeTaskIds.of_json
                                           replaceRootVolumeTaskIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ?dryRun ())
           (Some Values.DescribeReplaceRootVolumeTasksResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_reserved_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringClass =
         flag "offering-class" (optional json_arg)
           ~doc:"JSON OfferingClassType"
       and reservedInstancesIds =
         flag "reserved-instances-ids" (optional json_arg)
           ~doc:"JSON ReservedInstancesIdStringList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and offeringType =
         flag "offering-type" (optional json_arg)
           ~doc:"JSON OfferingTypeValues" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_instances
           (Values.DescribeReservedInstancesRequest.make
              ?offeringClass:(Option.map ~f:Values.OfferingClassType.of_json
                                offeringClass)
              ?reservedInstancesIds:(Option.map
                                       ~f:Values.ReservedInstancesIdStringList.of_json
                                       reservedInstancesIds) ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?offeringType:(Option.map ~f:Values.OfferingTypeValues.of_json
                               offeringType) ())
           (Some Values.DescribeReservedInstancesResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_reserved_instances_listings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedInstancesId =
         flag "reserved-instances-id" (optional string)
           ~doc:"STRING ReservationId"
       and reservedInstancesListingId =
         flag "reserved-instances-listing-id" (optional string)
           ~doc:"STRING ReservedInstancesListingId"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_instances_listings
           (Values.DescribeReservedInstancesListingsRequest.make
              ?reservedInstancesId ?reservedInstancesListingId
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeReservedInstancesListingsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_reserved_instances_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedInstancesModificationIds =
         flag "reserved-instances-modification-ids" (optional json_arg)
           ~doc:"JSON ReservedInstancesModificationIdStringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_instances_modifications
           (Values.DescribeReservedInstancesModificationsRequest.make
              ?reservedInstancesModificationIds:(Option.map
                                                   ~f:Values.ReservedInstancesModificationIdStringList.of_json
                                                   reservedInstancesModificationIds)
              ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.DescribeReservedInstancesModificationsResult.to_json)
           (Some Values.Ec2_error.to_json)])
let describe_reserved_instances_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and includeMarketplace =
         flag "include-marketplace" (optional bool) ~doc:"BOOL Boolean"
       and instanceType =
         flag "instance-type" (optional json_arg) ~doc:"JSON InstanceType"
       and maxDuration =
         flag "max-duration" (optional json_arg) ~doc:"JSON Long"
       and maxInstanceCount =
         flag "max-instance-count" (optional int) ~doc:"INT Integer"
       and minDuration =
         flag "min-duration" (optional json_arg) ~doc:"JSON Long"
       and offeringClass =
         flag "offering-class" (optional json_arg)
           ~doc:"JSON OfferingClassType"
       and productDescription =
         flag "product-description" (optional json_arg)
           ~doc:"JSON RIProductDescription"
       and reservedInstancesOfferingIds =
         flag "reserved-instances-offering-ids" (optional json_arg)
           ~doc:"JSON ReservedInstancesOfferingIdStringList"
       and availabilityZoneId =
         flag "availability-zone-id" (optional string)
           ~doc:"STRING AvailabilityZoneId"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and instanceTenancy =
         flag "instance-tenancy" (optional json_arg) ~doc:"JSON Tenancy"
       and offeringType =
         flag "offering-type" (optional json_arg)
           ~doc:"JSON OfferingTypeValues"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_instances_offerings
           (Values.DescribeReservedInstancesOfferingsRequest.make
              ?availabilityZone ?includeMarketplace
              ?instanceType:(Option.map ~f:Values.InstanceType.of_json
                               instanceType)
              ?maxDuration:(Option.map ~f:Values.Long.of_json maxDuration)
              ?maxInstanceCount
              ?minDuration:(Option.map ~f:Values.Long.of_json minDuration)
              ?offeringClass:(Option.map ~f:Values.OfferingClassType.of_json
                                offeringClass)
              ?productDescription:(Option.map
                                     ~f:Values.RIProductDescription.of_json
                                     productDescription)
              ?reservedInstancesOfferingIds:(Option.map
                                               ~f:Values.ReservedInstancesOfferingIdStringList.of_json
                                               reservedInstancesOfferingIds)
              ?availabilityZoneId ?dryRun
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?instanceTenancy:(Option.map ~f:Values.Tenancy.of_json
                                  instanceTenancy)
              ?offeringType:(Option.map ~f:Values.OfferingTypeValues.of_json
                               offeringType) ?nextToken ?maxResults ())
           (Some Values.DescribeReservedInstancesOfferingsResult.to_json)
           (Some Values.Ec2_error.to_json)])
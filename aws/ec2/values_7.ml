(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
open Values_0[@@warning "-33"]
open Values_1[@@warning "-33"]
open Values_2[@@warning "-33"]
open Values_3[@@warning "-33"]
open Values_4[@@warning "-33"]
open Values_5[@@warning "-33"]
open Values_6[@@warning "-33"]
module DeprovisionIpamPoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      ipamPoolId: IpamPoolId.t ;
      cidr: String_.t option }
    let context_ = "DeprovisionIpamPoolCidrRequest"
    let make ?dryRun =
      fun ?cidr -> fun ~ipamPoolId -> fun () -> { dryRun; cidr; ipamPoolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cidr", (Option.map x.cidr ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidr = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Cidr") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cidr ~ipamPoolId ?dryRun ()
    let of_json json =
      let cidr = field_map json "Cidr" String_.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?cidr ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeprovisionByoipCidrResult =
  struct
    type nonrec t = {
      byoipCidr: ByoipCidr.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_json json =
      let byoipCidr = field_map json "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end
module DeprovisionByoipCidrRequest =
  struct
    type nonrec t = {
      cidr: String_.t ;
      dryRun: Boolean.t option }
    let context_ = "DeprovisionByoipCidrRequest"
    let make ?dryRun = fun ~cidr -> fun () -> { dryRun; cidr }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?dryRun ~cidr ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      make ?dryRun ~cidr ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpnGatewayRequest =
  struct
    type nonrec t = {
      vpnGatewayId: VpnGatewayId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteVpnGatewayRequest"
    let make ?dryRun =
      fun ~vpnGatewayId -> fun () -> { dryRun; vpnGatewayId }
    let to_value x =
      structure_to_value
        [("VpnGatewayId", (Some (VpnGatewayId.to_value x.vpnGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnGatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnGatewayId") in
      make ?dryRun ~vpnGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnGatewayId =
        field_map_exn json "VpnGatewayId" VpnGatewayId.of_json in
      make ?dryRun ~vpnGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpnConnectionRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t ;
      vpnConnectionId: VpnConnectionId.t }
    let context_ = "DeleteVpnConnectionRouteRequest"
    let make ~destinationCidrBlock =
      fun ~vpnConnectionId ->
        fun () -> { destinationCidrBlock; vpnConnectionId }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("VpnConnectionId",
          (Some (VpnConnectionId.to_value x.vpnConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let of_json json =
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpnConnectionRequest =
  struct
    type nonrec t =
      {
      vpnConnectionId: VpnConnectionId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteVpnConnectionRequest"
    let make ?dryRun =
      fun ~vpnConnectionId -> fun () -> { dryRun; vpnConnectionId }
    let to_value x =
      structure_to_value
        [("VpnConnectionId",
           (Some (VpnConnectionId.to_value x.vpnConnectionId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      make ?dryRun ~vpnConnectionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      make ?dryRun ~vpnConnectionId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcRequest =
  struct
    type nonrec t = {
      vpcId: VpcId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteVpcRequest"
    let make ?dryRun = fun ~vpcId -> fun () -> { dryRun; vpcId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?dryRun ~vpcId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      make ?dryRun ~vpcId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcPeeringConnectionResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcPeeringConnectionId: VpcPeeringConnectionId.t }
    let context_ = "DeleteVpcPeeringConnectionRequest"
    let make ?dryRun =
      fun ~vpcPeeringConnectionId ->
        fun () -> { dryRun; vpcPeeringConnectionId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcPeeringConnectionId",
          (Some (VpcPeeringConnectionId.to_value x.vpcPeeringConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        VpcPeeringConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpcPeeringConnectionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcPeeringConnectionId ?dryRun ()
    let of_json json =
      let vpcPeeringConnectionId =
        field_map_exn json "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcPeeringConnectionId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcEndpointsResult =
  struct
    type nonrec t = {
      unsuccessful: UnsuccessfulItemSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcEndpointsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcEndpointIds: VpcEndpointIdList.t }
    let context_ = "DeleteVpcEndpointsRequest"
    let make ?dryRun =
      fun ~vpcEndpointIds -> fun () -> { dryRun; vpcEndpointIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointId",
          (Some (VpcEndpointIdList.to_value x.vpcEndpointIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEndpointIds =
        VpcEndpointIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcEndpointIds ?dryRun ()
    let of_json json =
      let vpcEndpointIds =
        field_map_exn json "VpcEndpointIds" VpcEndpointIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcEndpointIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcEndpointServiceConfigurationsResult =
  struct
    type nonrec t = {
      unsuccessful: UnsuccessfulItemSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcEndpointServiceConfigurationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      serviceIds: VpcEndpointServiceIdList.t }
    let context_ = "DeleteVpcEndpointServiceConfigurationsRequest"
    let make ?dryRun = fun ~serviceIds -> fun () -> { dryRun; serviceIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId",
          (Some (VpcEndpointServiceIdList.to_value x.serviceIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceIds =
        VpcEndpointServiceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~serviceIds ?dryRun ()
    let of_json json =
      let serviceIds =
        field_map_exn json "ServiceIds" VpcEndpointServiceIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~serviceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcEndpointConnectionNotificationsResult =
  struct
    type nonrec t = {
      unsuccessful: UnsuccessfulItemSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVpcEndpointConnectionNotificationsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      connectionNotificationIds: ConnectionNotificationIdsList.t }
    let context_ = "DeleteVpcEndpointConnectionNotificationsRequest"
    let make ?dryRun =
      fun ~connectionNotificationIds ->
        fun () -> { dryRun; connectionNotificationIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ConnectionNotificationId",
          (Some
             (ConnectionNotificationIdsList.to_value
                x.connectionNotificationIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectionNotificationIds =
        ConnectionNotificationIdsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ConnectionNotificationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~connectionNotificationIds ?dryRun ()
    let of_json json =
      let connectionNotificationIds =
        field_map_exn json "ConnectionNotificationIds"
          ConnectionNotificationIdsList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~connectionNotificationIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVolumeRequest =
  struct
    type nonrec t = {
      volumeId: VolumeId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteVolumeRequest"
    let make ?dryRun = fun ~volumeId -> fun () -> { dryRun; volumeId }
    let to_value x =
      structure_to_value
        [("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      make ?dryRun ~volumeId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      make ?dryRun ~volumeId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_json json =
      let transitGatewayVpcAttachment =
        field_map json "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayVpcAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTable: TransitGatewayRouteTable.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTable =
      fun () -> { transitGatewayRouteTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTable",
           (Option.map x.transitGatewayRouteTable
              ~f:TransitGatewayRouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayRouteTable =
        (Option.map ~f:TransitGatewayRouteTable.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTable") in
      make ?transitGatewayRouteTable ()
    let of_json json =
      let transitGatewayRouteTable =
        field_map json "TransitGatewayRouteTable"
          TransitGatewayRouteTable.of_json in
      make ?transitGatewayRouteTable ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayRouteTableRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun () -> { dryRun; transitGatewayRouteTableId }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayRouteResult =
  struct
    type nonrec t = {
      route: TransitGatewayRoute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:TransitGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:TransitGatewayRoute.of_xml)
          (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_json json =
      let route = field_map json "Route" TransitGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayRouteRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      destinationCidrBlock: String_.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayRouteRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~destinationCidrBlock ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; destinationCidrBlock }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~destinationCidrBlock ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~destinationCidrBlock ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayResult =
  struct
    type nonrec t = {
      transitGateway: TransitGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGateway = fun () -> { transitGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGateway",
           (Option.map x.transitGateway ~f:TransitGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGateway =
        (Option.map ~f:TransitGateway.of_xml)
          (Xml.child xml_arg0 "transitGateway") in
      make ?transitGateway ()
    let of_json json =
      let transitGateway =
        field_map json "TransitGateway" TransitGateway.of_json in
      make ?transitGateway ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayRequest"
    let make ?dryRun =
      fun ~transitGatewayId -> fun () -> { dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ~transitGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayId =
        field_map_exn json "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayPrefixListReferenceResult =
  struct
    type nonrec t =
      {
      transitGatewayPrefixListReference:
        TransitGatewayPrefixListReference.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPrefixListReference =
      fun () -> { transitGatewayPrefixListReference }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPrefixListReference",
           (Option.map x.transitGatewayPrefixListReference
              ~f:TransitGatewayPrefixListReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPrefixListReference =
        (Option.map ~f:TransitGatewayPrefixListReference.of_xml)
          (Xml.child xml_arg0 "transitGatewayPrefixListReference") in
      make ?transitGatewayPrefixListReference ()
    let of_json json =
      let transitGatewayPrefixListReference =
        field_map json "TransitGatewayPrefixListReference"
          TransitGatewayPrefixListReference.of_json in
      make ?transitGatewayPrefixListReference ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayPrefixListReferenceRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      prefixListId: PrefixListResourceId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayPrefixListReferenceRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~prefixListId ->
          fun () -> { dryRun; transitGatewayRouteTableId; prefixListId }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~prefixListId ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~prefixListId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_json json =
      let transitGatewayPeeringAttachment =
        field_map json "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayPeeringAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomain: TransitGatewayMulticastDomain.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMulticastDomain =
      fun () -> { transitGatewayMulticastDomain }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMulticastDomain",
           (Option.map x.transitGatewayMulticastDomain
              ~f:TransitGatewayMulticastDomain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMulticastDomain =
        (Option.map ~f:TransitGatewayMulticastDomain.of_xml)
          (Xml.child xml_arg0 "transitGatewayMulticastDomain") in
      make ?transitGatewayMulticastDomain ()
    let of_json json =
      let transitGatewayMulticastDomain =
        field_map json "TransitGatewayMulticastDomain"
          TransitGatewayMulticastDomain.of_json in
      make ?transitGatewayMulticastDomain ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId: TransitGatewayMulticastDomainId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayMulticastDomainRequest"
    let make ?dryRun =
      fun ~transitGatewayMulticastDomainId ->
        fun () -> { dryRun; transitGatewayMulticastDomainId }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Some
              (TransitGatewayMulticastDomainId.to_value
                 x.transitGatewayMulticastDomainId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayMulticastDomainId =
        TransitGatewayMulticastDomainId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayMulticastDomainId") in
      make ?dryRun ~transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayMulticastDomainId =
        field_map_exn json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ~transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayConnectResult =
  struct
    type nonrec t = {
      transitGatewayConnect: TransitGatewayConnect.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnect = fun () -> { transitGatewayConnect }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnect",
           (Option.map x.transitGatewayConnect
              ~f:TransitGatewayConnect.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnect =
        (Option.map ~f:TransitGatewayConnect.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnect") in
      make ?transitGatewayConnect ()
    let of_json json =
      let transitGatewayConnect =
        field_map json "TransitGatewayConnect" TransitGatewayConnect.of_json in
      make ?transitGatewayConnect ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayConnectRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayConnectRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayConnectPeerResult =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeer: TransitGatewayConnectPeer.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnectPeer =
      fun () -> { transitGatewayConnectPeer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnectPeer",
           (Option.map x.transitGatewayConnectPeer
              ~f:TransitGatewayConnectPeer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnectPeer =
        (Option.map ~f:TransitGatewayConnectPeer.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnectPeer") in
      make ?transitGatewayConnectPeer ()
    let of_json json =
      let transitGatewayConnectPeer =
        field_map json "TransitGatewayConnectPeer"
          TransitGatewayConnectPeer.of_json in
      make ?transitGatewayConnectPeer ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTransitGatewayConnectPeerRequest =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeerId: TransitGatewayConnectPeerId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTransitGatewayConnectPeerRequest"
    let make ?dryRun =
      fun ~transitGatewayConnectPeerId ->
        fun () -> { dryRun; transitGatewayConnectPeerId }
    let to_value x =
      structure_to_value
        [("TransitGatewayConnectPeerId",
           (Some
              (TransitGatewayConnectPeerId.to_value
                 x.transitGatewayConnectPeerId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayConnectPeerId =
        TransitGatewayConnectPeerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayConnectPeerId") in
      make ?dryRun ~transitGatewayConnectPeerId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayConnectPeerId =
        field_map_exn json "TransitGatewayConnectPeerId"
          TransitGatewayConnectPeerId.of_json in
      make ?dryRun ~transitGatewayConnectPeerId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorTargetResult =
  struct
    type nonrec t = {
      trafficMirrorTargetId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorTargetId = fun () -> { trafficMirrorTargetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorTargetId",
           (Option.map x.trafficMirrorTargetId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorTargetId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorTargetId") in
      make ?trafficMirrorTargetId ()
    let of_json json =
      let trafficMirrorTargetId =
        field_map json "TrafficMirrorTargetId" String_.of_json in
      make ?trafficMirrorTargetId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorTargetRequest =
  struct
    type nonrec t =
      {
      trafficMirrorTargetId: TrafficMirrorTargetId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTrafficMirrorTargetRequest"
    let make ?dryRun =
      fun ~trafficMirrorTargetId ->
        fun () -> { dryRun; trafficMirrorTargetId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorTargetId",
           (Some (TrafficMirrorTargetId.to_value x.trafficMirrorTargetId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorTargetId =
        TrafficMirrorTargetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorTargetId") in
      make ?dryRun ~trafficMirrorTargetId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorTargetId =
        field_map_exn json "TrafficMirrorTargetId"
          TrafficMirrorTargetId.of_json in
      make ?dryRun ~trafficMirrorTargetId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorSessionResult =
  struct
    type nonrec t = {
      trafficMirrorSessionId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorSessionId = fun () -> { trafficMirrorSessionId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorSessionId",
           (Option.map x.trafficMirrorSessionId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorSessionId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorSessionId") in
      make ?trafficMirrorSessionId ()
    let of_json json =
      let trafficMirrorSessionId =
        field_map json "TrafficMirrorSessionId" String_.of_json in
      make ?trafficMirrorSessionId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorSessionRequest =
  struct
    type nonrec t =
      {
      trafficMirrorSessionId: TrafficMirrorSessionId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTrafficMirrorSessionRequest"
    let make ?dryRun =
      fun ~trafficMirrorSessionId ->
        fun () -> { dryRun; trafficMirrorSessionId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorSessionId",
           (Some (TrafficMirrorSessionId.to_value x.trafficMirrorSessionId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorSessionId =
        TrafficMirrorSessionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorSessionId") in
      make ?dryRun ~trafficMirrorSessionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorSessionId =
        field_map_exn json "TrafficMirrorSessionId"
          TrafficMirrorSessionId.of_json in
      make ?dryRun ~trafficMirrorSessionId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorFilterRuleResult =
  struct
    type nonrec t = {
      trafficMirrorFilterRuleId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterRuleId =
      fun () -> { trafficMirrorFilterRuleId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterRuleId",
           (Option.map x.trafficMirrorFilterRuleId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorFilterRuleId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterRuleId") in
      make ?trafficMirrorFilterRuleId ()
    let of_json json =
      let trafficMirrorFilterRuleId =
        field_map json "TrafficMirrorFilterRuleId" String_.of_json in
      make ?trafficMirrorFilterRuleId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorFilterRuleRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRuleId: TrafficMirrorFilterRuleId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTrafficMirrorFilterRuleRequest"
    let make ?dryRun =
      fun ~trafficMirrorFilterRuleId ->
        fun () -> { dryRun; trafficMirrorFilterRuleId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterRuleId",
           (Some
              (TrafficMirrorFilterRuleId.to_value x.trafficMirrorFilterRuleId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorFilterRuleId =
        TrafficMirrorFilterRuleId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TrafficMirrorFilterRuleId") in
      make ?dryRun ~trafficMirrorFilterRuleId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorFilterRuleId =
        field_map_exn json "TrafficMirrorFilterRuleId"
          TrafficMirrorFilterRuleId.of_json in
      make ?dryRun ~trafficMirrorFilterRuleId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorFilterResult =
  struct
    type nonrec t = {
      trafficMirrorFilterId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterId = fun () -> { trafficMirrorFilterId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterId",
           (Option.map x.trafficMirrorFilterId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficMirrorFilterId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterId") in
      make ?trafficMirrorFilterId ()
    let of_json json =
      let trafficMirrorFilterId =
        field_map json "TrafficMirrorFilterId" String_.of_json in
      make ?trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTrafficMirrorFilterRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterId: TrafficMirrorFilterId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteTrafficMirrorFilterRequest"
    let make ?dryRun =
      fun ~trafficMirrorFilterId ->
        fun () -> { dryRun; trafficMirrorFilterId }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterId",
           (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      make ?dryRun ~trafficMirrorFilterId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let trafficMirrorFilterId =
        field_map_exn json "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      make ?dryRun ~trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTagsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      resources: ResourceIdList.t ;
      tags: TagList.t option }
    let context_ = "DeleteTagsRequest"
    let make ?dryRun =
      fun ?tags -> fun ~resources -> fun () -> { dryRun; tags; resources }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("resourceId", (Some (ResourceIdList.to_value x.resources)));
        ("tag", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tag") in
      let resources =
        ResourceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tags ~resources ?dryRun ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let resources = field_map_exn json "Resources" ResourceIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tags ~resources ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSubnetRequest =
  struct
    type nonrec t = {
      subnetId: SubnetId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteSubnetRequest"
    let make ?dryRun = fun ~subnetId -> fun () -> { dryRun; subnetId }
    let to_value x =
      structure_to_value
        [("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      make ?dryRun ~subnetId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      make ?dryRun ~subnetId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSubnetCidrReservationResult =
  struct
    type nonrec t =
      {
      deletedSubnetCidrReservation: SubnetCidrReservation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?deletedSubnetCidrReservation =
      fun () -> { deletedSubnetCidrReservation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deletedSubnetCidrReservation",
           (Option.map x.deletedSubnetCidrReservation
              ~f:SubnetCidrReservation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deletedSubnetCidrReservation =
        (Option.map ~f:SubnetCidrReservation.of_xml)
          (Xml.child xml_arg0 "deletedSubnetCidrReservation") in
      make ?deletedSubnetCidrReservation ()
    let of_json json =
      let deletedSubnetCidrReservation =
        field_map json "DeletedSubnetCidrReservation"
          SubnetCidrReservation.of_json in
      make ?deletedSubnetCidrReservation ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSubnetCidrReservationRequest =
  struct
    type nonrec t =
      {
      subnetCidrReservationId: SubnetCidrReservationId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteSubnetCidrReservationRequest"
    let make ?dryRun =
      fun ~subnetCidrReservationId ->
        fun () -> { dryRun; subnetCidrReservationId }
    let to_value x =
      structure_to_value
        [("SubnetCidrReservationId",
           (Some (SubnetCidrReservationId.to_value x.subnetCidrReservationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetCidrReservationId =
        SubnetCidrReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubnetCidrReservationId") in
      make ?dryRun ~subnetCidrReservationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetCidrReservationId =
        field_map_exn json "SubnetCidrReservationId"
          SubnetCidrReservationId.of_json in
      make ?dryRun ~subnetCidrReservationId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSpotDatafeedSubscriptionRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSnapshotRequest =
  struct
    type nonrec t = {
      snapshotId: SnapshotId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteSnapshotRequest"
    let make ?dryRun = fun ~snapshotId -> fun () -> { dryRun; snapshotId }
    let to_value x =
      structure_to_value
        [("SnapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotId") in
      make ?dryRun ~snapshotId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ?dryRun ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSecurityGroupRequest =
  struct
    type nonrec t =
      {
      groupId: SecurityGroupId.t option ;
      groupName: SecurityGroupName.t option ;
      dryRun: Boolean.t option }
    let make ?groupId =
      fun ?groupName ->
        fun ?dryRun -> fun () -> { groupId; groupName; dryRun }
    let to_value x =
      structure_to_value
        [("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      make ?dryRun ?groupName ?groupId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let groupName = field_map json "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json "GroupId" SecurityGroupId.of_json in
      make ?dryRun ?groupName ?groupId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteRouteTableRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      routeTableId: RouteTableId.t }
    let context_ = "DeleteRouteTableRequest"
    let make ?dryRun =
      fun ~routeTableId -> fun () -> { dryRun; routeTableId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~routeTableId ?dryRun ()
    let of_json json =
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~routeTableId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t option ;
      destinationIpv6CidrBlock: String_.t option ;
      destinationPrefixListId: PrefixListResourceId.t option ;
      dryRun: Boolean.t option ;
      routeTableId: RouteTableId.t }
    let context_ = "DeleteRouteRequest"
    let make ?destinationCidrBlock =
      fun ?destinationIpv6CidrBlock ->
        fun ?destinationPrefixListId ->
          fun ?dryRun ->
            fun ~routeTableId ->
              fun () ->
                {
                  destinationCidrBlock;
                  destinationIpv6CidrBlock;
                  destinationPrefixListId;
                  dryRun;
                  routeTableId
                }
    let to_value x =
      structure_to_value
        [("destinationCidrBlock",
           (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("destinationIpv6CidrBlock",
          (Option.map x.destinationIpv6CidrBlock ~f:String_.to_value));
        ("DestinationPrefixListId",
          (Option.map x.destinationPrefixListId
             ~f:PrefixListResourceId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      let destinationIpv6CidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationIpv6CidrBlock") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationCidrBlock") in
      make ~routeTableId ?dryRun ?destinationPrefixListId
        ?destinationIpv6CidrBlock ?destinationCidrBlock ()
    let of_json json =
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let destinationPrefixListId =
        field_map json "DestinationPrefixListId" PrefixListResourceId.of_json in
      let destinationIpv6CidrBlock =
        field_map json "DestinationIpv6CidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map json "DestinationCidrBlock" String_.of_json in
      make ~routeTableId ?dryRun ?destinationPrefixListId
        ?destinationIpv6CidrBlock ?destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module DeleteQueuedReservedInstancesResult =
  struct
    type nonrec t =
      {
      successfulQueuedPurchaseDeletions:
        SuccessfulQueuedPurchaseDeletionSet.t option ;
      failedQueuedPurchaseDeletions: FailedQueuedPurchaseDeletionSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulQueuedPurchaseDeletions =
      fun ?failedQueuedPurchaseDeletions ->
        fun () ->
          { successfulQueuedPurchaseDeletions; failedQueuedPurchaseDeletions
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulQueuedPurchaseDeletionSet",
           (Option.map x.successfulQueuedPurchaseDeletions
              ~f:SuccessfulQueuedPurchaseDeletionSet.to_value));
        ("failedQueuedPurchaseDeletionSet",
          (Option.map x.failedQueuedPurchaseDeletions
             ~f:FailedQueuedPurchaseDeletionSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedQueuedPurchaseDeletions =
        (Option.map ~f:FailedQueuedPurchaseDeletionSet.of_xml)
          (Xml.child xml_arg0 "failedQueuedPurchaseDeletionSet") in
      let successfulQueuedPurchaseDeletions =
        (Option.map ~f:SuccessfulQueuedPurchaseDeletionSet.of_xml)
          (Xml.child xml_arg0 "successfulQueuedPurchaseDeletionSet") in
      make ?failedQueuedPurchaseDeletions ?successfulQueuedPurchaseDeletions
        ()
    let of_json json =
      let failedQueuedPurchaseDeletions =
        field_map json "FailedQueuedPurchaseDeletions"
          FailedQueuedPurchaseDeletionSet.of_json in
      let successfulQueuedPurchaseDeletions =
        field_map json "SuccessfulQueuedPurchaseDeletions"
          SuccessfulQueuedPurchaseDeletionSet.of_json in
      make ?failedQueuedPurchaseDeletions ?successfulQueuedPurchaseDeletions
        ()
    let to_json v = composed_to_json to_value v
  end
module DeleteQueuedReservedInstancesRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      reservedInstancesIds: DeleteQueuedReservedInstancesIdList.t }
    let context_ = "DeleteQueuedReservedInstancesRequest"
    let make ?dryRun =
      fun ~reservedInstancesIds -> fun () -> { dryRun; reservedInstancesIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ReservedInstancesId",
          (Some
             (DeleteQueuedReservedInstancesIdList.to_value
                x.reservedInstancesIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesIds =
        DeleteQueuedReservedInstancesIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservedInstancesId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~reservedInstancesIds ?dryRun ()
    let of_json json =
      let reservedInstancesIds =
        field_map_exn json "ReservedInstancesIds"
          DeleteQueuedReservedInstancesIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~reservedInstancesIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeletePublicIpv4PoolResult =
  struct
    type nonrec t = {
      returnValue: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnValue = fun () -> { returnValue }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("returnValue", (Option.map x.returnValue ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnValue =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "returnValue") in
      make ?returnValue ()
    let of_json json =
      let returnValue = field_map json "ReturnValue" Boolean.of_json in
      make ?returnValue ()
    let to_json v = composed_to_json to_value v
  end
module DeletePublicIpv4PoolRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      poolId: Ipv4PoolEc2Id.t }
    let context_ = "DeletePublicIpv4PoolRequest"
    let make ?dryRun = fun ~poolId -> fun () -> { dryRun; poolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PoolId", (Some (Ipv4PoolEc2Id.to_value x.poolId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let poolId =
        Ipv4PoolEc2Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~poolId ?dryRun ()
    let of_json json =
      let poolId = field_map_exn json "PoolId" Ipv4PoolEc2Id.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~poolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeletePlacementGroupRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      groupName: PlacementGroupName.t }
    let context_ = "DeletePlacementGroupRequest"
    let make ?dryRun = fun ~groupName -> fun () -> { dryRun; groupName }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupName", (Some (PlacementGroupName.to_value x.groupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupName =
        PlacementGroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~groupName ?dryRun ()
    let of_json json =
      let groupName =
        field_map_exn json "GroupName" PlacementGroupName.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~groupName ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      networkInterfaceId: NetworkInterfaceId.t }
    let context_ = "DeleteNetworkInterfaceRequest"
    let make ?dryRun =
      fun ~networkInterfaceId -> fun () -> { dryRun; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~networkInterfaceId ?dryRun ()
    let of_json json =
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~networkInterfaceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInterfacePermissionResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInterfacePermissionRequest =
  struct
    type nonrec t =
      {
      networkInterfacePermissionId: NetworkInterfacePermissionId.t ;
      force: Boolean.t option ;
      dryRun: Boolean.t option }
    let context_ = "DeleteNetworkInterfacePermissionRequest"
    let make ?force =
      fun ?dryRun ->
        fun ~networkInterfacePermissionId ->
          fun () -> { force; dryRun; networkInterfacePermissionId }
    let to_value x =
      structure_to_value
        [("NetworkInterfacePermissionId",
           (Some
              (NetworkInterfacePermissionId.to_value
                 x.networkInterfacePermissionId)));
        ("Force", (Option.map x.force ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let force = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Force") in
      let networkInterfacePermissionId =
        NetworkInterfacePermissionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInterfacePermissionId") in
      make ?dryRun ?force ~networkInterfacePermissionId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let force = field_map json "Force" Boolean.of_json in
      let networkInterfacePermissionId =
        field_map_exn json "NetworkInterfacePermissionId"
          NetworkInterfacePermissionId.of_json in
      make ?dryRun ?force ~networkInterfacePermissionId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsPathResult =
  struct
    type nonrec t = {
      networkInsightsPathId: NetworkInsightsPathId.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsPathId = fun () -> { networkInsightsPathId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsPathId",
           (Option.map x.networkInsightsPathId
              ~f:NetworkInsightsPathId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsPathId =
        (Option.map ~f:NetworkInsightsPathId.of_xml)
          (Xml.child xml_arg0 "networkInsightsPathId") in
      make ?networkInsightsPathId ()
    let of_json json =
      let networkInsightsPathId =
        field_map json "NetworkInsightsPathId" NetworkInsightsPathId.of_json in
      make ?networkInsightsPathId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsPathRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      networkInsightsPathId: NetworkInsightsPathId.t }
    let context_ = "DeleteNetworkInsightsPathRequest"
    let make ?dryRun =
      fun ~networkInsightsPathId ->
        fun () -> { dryRun; networkInsightsPathId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInsightsPathId",
          (Some (NetworkInsightsPathId.to_value x.networkInsightsPathId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsPathId =
        NetworkInsightsPathId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInsightsPathId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~networkInsightsPathId ?dryRun ()
    let of_json json =
      let networkInsightsPathId =
        field_map_exn json "NetworkInsightsPathId"
          NetworkInsightsPathId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~networkInsightsPathId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsAnalysisResult =
  struct
    type nonrec t =
      {
      networkInsightsAnalysisId: NetworkInsightsAnalysisId.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAnalysisId =
      fun () -> { networkInsightsAnalysisId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAnalysisId",
           (Option.map x.networkInsightsAnalysisId
              ~f:NetworkInsightsAnalysisId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAnalysisId =
        (Option.map ~f:NetworkInsightsAnalysisId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAnalysisId") in
      make ?networkInsightsAnalysisId ()
    let of_json json =
      let networkInsightsAnalysisId =
        field_map json "NetworkInsightsAnalysisId"
          NetworkInsightsAnalysisId.of_json in
      make ?networkInsightsAnalysisId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsAnalysisRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      networkInsightsAnalysisId: NetworkInsightsAnalysisId.t }
    let context_ = "DeleteNetworkInsightsAnalysisRequest"
    let make ?dryRun =
      fun ~networkInsightsAnalysisId ->
        fun () -> { dryRun; networkInsightsAnalysisId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInsightsAnalysisId",
          (Some
             (NetworkInsightsAnalysisId.to_value x.networkInsightsAnalysisId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAnalysisId =
        NetworkInsightsAnalysisId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAnalysisId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~networkInsightsAnalysisId ?dryRun ()
    let of_json json =
      let networkInsightsAnalysisId =
        field_map_exn json "NetworkInsightsAnalysisId"
          NetworkInsightsAnalysisId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~networkInsightsAnalysisId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsAccessScopeResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeId =
      fun () -> { networkInsightsAccessScopeId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeId",
           (Option.map x.networkInsightsAccessScopeId
              ~f:NetworkInsightsAccessScopeId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeId =
        (Option.map ~f:NetworkInsightsAccessScopeId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeId") in
      make ?networkInsightsAccessScopeId ()
    let of_json json =
      let networkInsightsAccessScopeId =
        field_map json "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      make ?networkInsightsAccessScopeId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsAccessScopeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      networkInsightsAccessScopeId: NetworkInsightsAccessScopeId.t }
    let context_ = "DeleteNetworkInsightsAccessScopeRequest"
    let make ?dryRun =
      fun ~networkInsightsAccessScopeId ->
        fun () -> { dryRun; networkInsightsAccessScopeId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInsightsAccessScopeId",
          (Some
             (NetworkInsightsAccessScopeId.to_value
                x.networkInsightsAccessScopeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeId =
        NetworkInsightsAccessScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~networkInsightsAccessScopeId ?dryRun ()
    let of_json json =
      let networkInsightsAccessScopeId =
        field_map_exn json "NetworkInsightsAccessScopeId"
          NetworkInsightsAccessScopeId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~networkInsightsAccessScopeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsAccessScopeAnalysisResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisId:
        NetworkInsightsAccessScopeAnalysisId.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScopeAnalysisId =
      fun () -> { networkInsightsAccessScopeAnalysisId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScopeAnalysisId",
           (Option.map x.networkInsightsAccessScopeAnalysisId
              ~f:NetworkInsightsAccessScopeAnalysisId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeAnalysisId =
        (Option.map ~f:NetworkInsightsAccessScopeAnalysisId.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeAnalysisId") in
      make ?networkInsightsAccessScopeAnalysisId ()
    let of_json json =
      let networkInsightsAccessScopeAnalysisId =
        field_map json "NetworkInsightsAccessScopeAnalysisId"
          NetworkInsightsAccessScopeAnalysisId.of_json in
      make ?networkInsightsAccessScopeAnalysisId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkInsightsAccessScopeAnalysisRequest =
  struct
    type nonrec t =
      {
      networkInsightsAccessScopeAnalysisId:
        NetworkInsightsAccessScopeAnalysisId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteNetworkInsightsAccessScopeAnalysisRequest"
    let make ?dryRun =
      fun ~networkInsightsAccessScopeAnalysisId ->
        fun () -> { dryRun; networkInsightsAccessScopeAnalysisId }
    let to_value x =
      structure_to_value
        [("NetworkInsightsAccessScopeAnalysisId",
           (Some
              (NetworkInsightsAccessScopeAnalysisId.to_value
                 x.networkInsightsAccessScopeAnalysisId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let networkInsightsAccessScopeAnalysisId =
        NetworkInsightsAccessScopeAnalysisId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NetworkInsightsAccessScopeAnalysisId") in
      make ?dryRun ~networkInsightsAccessScopeAnalysisId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let networkInsightsAccessScopeAnalysisId =
        field_map_exn json "NetworkInsightsAccessScopeAnalysisId"
          NetworkInsightsAccessScopeAnalysisId.of_json in
      make ?dryRun ~networkInsightsAccessScopeAnalysisId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkAclRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      networkAclId: NetworkAclId.t }
    let context_ = "DeleteNetworkAclRequest"
    let make ?dryRun =
      fun ~networkAclId -> fun () -> { dryRun; networkAclId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~networkAclId ?dryRun ()
    let of_json json =
      let networkAclId =
        field_map_exn json "NetworkAclId" NetworkAclId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~networkAclId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNetworkAclEntryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      egress: Boolean.t ;
      networkAclId: NetworkAclId.t ;
      ruleNumber: Integer.t }
    let context_ = "DeleteNetworkAclEntryRequest"
    let make ?dryRun =
      fun ~egress ->
        fun ~networkAclId ->
          fun ~ruleNumber ->
            fun () -> { dryRun; egress; networkAclId; ruleNumber }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("egress", (Some (Boolean.to_value x.egress)));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)));
        ("ruleNumber", (Some (Integer.to_value x.ruleNumber)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleNumber") in
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let egress =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "egress") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~ruleNumber ~networkAclId ~egress ?dryRun ()
    let of_json json =
      let ruleNumber = field_map_exn json "RuleNumber" Integer.of_json in
      let networkAclId =
        field_map_exn json "NetworkAclId" NetworkAclId.of_json in
      let egress = field_map_exn json "Egress" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~ruleNumber ~networkAclId ~egress ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNatGatewayResult =
  struct
    type nonrec t = {
      natGatewayId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?natGatewayId = fun () -> { natGatewayId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("natGatewayId", (Option.map x.natGatewayId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "natGatewayId") in
      make ?natGatewayId ()
    let of_json json =
      let natGatewayId = field_map json "NatGatewayId" String_.of_json in
      make ?natGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteNatGatewayRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      natGatewayId: NatGatewayId.t }
    let context_ = "DeleteNatGatewayRequest"
    let make ?dryRun =
      fun ~natGatewayId -> fun () -> { dryRun; natGatewayId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NatGatewayId", (Some (NatGatewayId.to_value x.natGatewayId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayId =
        NatGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NatGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~natGatewayId ?dryRun ()
    let of_json json =
      let natGatewayId =
        field_map_exn json "NatGatewayId" NatGatewayId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~natGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteManagedPrefixListResult =
  struct
    type nonrec t = {
      prefixList: ManagedPrefixList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixList = fun () -> { prefixList }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixList",
           (Option.map x.prefixList ~f:ManagedPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixList =
        (Option.map ~f:ManagedPrefixList.of_xml)
          (Xml.child xml_arg0 "prefixList") in
      make ?prefixList ()
    let of_json json =
      let prefixList = field_map json "PrefixList" ManagedPrefixList.of_json in
      make ?prefixList ()
    let to_json v = composed_to_json to_value v
  end
module DeleteManagedPrefixListRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      prefixListId: PrefixListResourceId.t }
    let context_ = "DeleteManagedPrefixListRequest"
    let make ?dryRun =
      fun ~prefixListId -> fun () -> { dryRun; prefixListId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~prefixListId ?dryRun ()
    let of_json json =
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~prefixListId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLocalGatewayRouteTableVpcAssociationResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociation:
        LocalGatewayRouteTableVpcAssociation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVpcAssociation =
      fun () -> { localGatewayRouteTableVpcAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVpcAssociation",
           (Option.map x.localGatewayRouteTableVpcAssociation
              ~f:LocalGatewayRouteTableVpcAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableVpcAssociation =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociation.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableVpcAssociation") in
      make ?localGatewayRouteTableVpcAssociation ()
    let of_json json =
      let localGatewayRouteTableVpcAssociation =
        field_map json "LocalGatewayRouteTableVpcAssociation"
          LocalGatewayRouteTableVpcAssociation.of_json in
      make ?localGatewayRouteTableVpcAssociation ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLocalGatewayRouteTableVpcAssociationRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociationId:
        LocalGatewayRouteTableVpcAssociationId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteLocalGatewayRouteTableVpcAssociationRequest"
    let make ?dryRun =
      fun ~localGatewayRouteTableVpcAssociationId ->
        fun () -> { dryRun; localGatewayRouteTableVpcAssociationId }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableVpcAssociationId",
           (Some
              (LocalGatewayRouteTableVpcAssociationId.to_value
                 x.localGatewayRouteTableVpcAssociationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayRouteTableVpcAssociationId =
        LocalGatewayRouteTableVpcAssociationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableVpcAssociationId") in
      make ?dryRun ~localGatewayRouteTableVpcAssociationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let localGatewayRouteTableVpcAssociationId =
        field_map_exn json "LocalGatewayRouteTableVpcAssociationId"
          LocalGatewayRouteTableVpcAssociationId.of_json in
      make ?dryRun ~localGatewayRouteTableVpcAssociationId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLocalGatewayRouteResult =
  struct
    type nonrec t = {
      route: LocalGatewayRoute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:LocalGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:LocalGatewayRoute.of_xml) (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_json json =
      let route = field_map json "Route" LocalGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLocalGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t ;
      localGatewayRouteTableId: LocalGatewayRoutetableId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteLocalGatewayRouteRequest"
    let make ?dryRun =
      fun ~destinationCidrBlock ->
        fun ~localGatewayRouteTableId ->
          fun () ->
            { dryRun; destinationCidrBlock; localGatewayRouteTableId }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("LocalGatewayRouteTableId",
          (Some
             (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ?dryRun ~localGatewayRouteTableId ~destinationCidrBlock ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let localGatewayRouteTableId =
        field_map_exn json "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      make ?dryRun ~localGatewayRouteTableId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLaunchTemplateVersionsResult =
  struct
    type nonrec t =
      {
      successfullyDeletedLaunchTemplateVersions:
        DeleteLaunchTemplateVersionsResponseSuccessSet.t option ;
      unsuccessfullyDeletedLaunchTemplateVersions:
        DeleteLaunchTemplateVersionsResponseErrorSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfullyDeletedLaunchTemplateVersions =
      fun ?unsuccessfullyDeletedLaunchTemplateVersions ->
        fun () ->
          {
            successfullyDeletedLaunchTemplateVersions;
            unsuccessfullyDeletedLaunchTemplateVersions
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfullyDeletedLaunchTemplateVersionSet",
           (Option.map x.successfullyDeletedLaunchTemplateVersions
              ~f:DeleteLaunchTemplateVersionsResponseSuccessSet.to_value));
        ("unsuccessfullyDeletedLaunchTemplateVersionSet",
          (Option.map x.unsuccessfullyDeletedLaunchTemplateVersions
             ~f:DeleteLaunchTemplateVersionsResponseErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfullyDeletedLaunchTemplateVersions =
        (Option.map ~f:DeleteLaunchTemplateVersionsResponseErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfullyDeletedLaunchTemplateVersionSet") in
      let successfullyDeletedLaunchTemplateVersions =
        (Option.map ~f:DeleteLaunchTemplateVersionsResponseSuccessSet.of_xml)
          (Xml.child xml_arg0 "successfullyDeletedLaunchTemplateVersionSet") in
      make ?unsuccessfullyDeletedLaunchTemplateVersions
        ?successfullyDeletedLaunchTemplateVersions ()
    let of_json json =
      let unsuccessfullyDeletedLaunchTemplateVersions =
        field_map json "UnsuccessfullyDeletedLaunchTemplateVersions"
          DeleteLaunchTemplateVersionsResponseErrorSet.of_json in
      let successfullyDeletedLaunchTemplateVersions =
        field_map json "SuccessfullyDeletedLaunchTemplateVersions"
          DeleteLaunchTemplateVersionsResponseSuccessSet.of_json in
      make ?unsuccessfullyDeletedLaunchTemplateVersions
        ?successfullyDeletedLaunchTemplateVersions ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLaunchTemplateVersionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      launchTemplateId: LaunchTemplateId.t option ;
      launchTemplateName: LaunchTemplateName.t option ;
      versions: VersionStringList.t }
    let context_ = "DeleteLaunchTemplateVersionsRequest"
    let make ?dryRun =
      fun ?launchTemplateId ->
        fun ?launchTemplateName ->
          fun ~versions ->
            fun () ->
              { dryRun; launchTemplateId; launchTemplateName; versions }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("LaunchTemplateVersion",
          (Some (VersionStringList.to_value x.versions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versions =
        VersionStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~versions ?launchTemplateName ?launchTemplateId ?dryRun ()
    let of_json json =
      let versions = field_map_exn json "Versions" VersionStringList.of_json in
      let launchTemplateName =
        field_map json "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json "LaunchTemplateId" LaunchTemplateId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~versions ?launchTemplateName ?launchTemplateId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLaunchTemplateResult =
  struct
    type nonrec t = {
      launchTemplate: LaunchTemplate.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplate = fun () -> { launchTemplate }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplate",
           (Option.map x.launchTemplate ~f:LaunchTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplate =
        (Option.map ~f:LaunchTemplate.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      make ?launchTemplate ()
    let of_json json =
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplate.of_json in
      make ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLaunchTemplateRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      launchTemplateId: LaunchTemplateId.t option ;
      launchTemplateName: LaunchTemplateName.t option }
    let make ?dryRun =
      fun ?launchTemplateId ->
        fun ?launchTemplateName ->
          fun () -> { dryRun; launchTemplateId; launchTemplateName }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?launchTemplateName ?launchTemplateId ?dryRun ()
    let of_json json =
      let launchTemplateName =
        field_map json "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json "LaunchTemplateId" LaunchTemplateId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?launchTemplateName ?launchTemplateId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteKeyPairRequest =
  struct
    type nonrec t =
      {
      keyName: KeyPairName.t option ;
      keyPairId: KeyPairId.t option ;
      dryRun: Boolean.t option }
    let make ?keyName =
      fun ?keyPairId ->
        fun ?dryRun -> fun () -> { keyName; keyPairId; dryRun }
    let to_value x =
      structure_to_value
        [("KeyName", (Option.map x.keyName ~f:KeyPairName.to_value));
        ("KeyPairId", (Option.map x.keyPairId ~f:KeyPairId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let keyPairId =
        (Option.map ~f:KeyPairId.of_xml) (Xml.child xml_arg0 "KeyPairId") in
      let keyName =
        (Option.map ~f:KeyPairName.of_xml) (Xml.child xml_arg0 "KeyName") in
      make ?dryRun ?keyPairId ?keyName ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let keyPairId = field_map json "KeyPairId" KeyPairId.of_json in
      let keyName = field_map json "KeyName" KeyPairName.of_json in
      make ?dryRun ?keyPairId ?keyName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteIpamScopeResult =
  struct
    type nonrec t = {
      ipamScope: IpamScope.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamScope = fun () -> { ipamScope }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamScope", (Option.map x.ipamScope ~f:IpamScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScope =
        (Option.map ~f:IpamScope.of_xml) (Xml.child xml_arg0 "ipamScope") in
      make ?ipamScope ()
    let of_json json =
      let ipamScope = field_map json "IpamScope" IpamScope.of_json in
      make ?ipamScope ()
    let to_json v = composed_to_json to_value v
  end
module DeleteIpamScopeRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      ipamScopeId: IpamScopeId.t }
    let context_ = "DeleteIpamScopeRequest"
    let make ?dryRun = fun ~ipamScopeId -> fun () -> { dryRun; ipamScopeId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamScopeId ?dryRun ()
    let of_json json =
      let ipamScopeId = field_map_exn json "IpamScopeId" IpamScopeId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~ipamScopeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteIpamResult =
  struct
    type nonrec t = {
      ipam: Ipam.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipam = fun () -> { ipam }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("ipam", (Option.map x.ipam ~f:Ipam.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipam = (Option.map ~f:Ipam.of_xml) (Xml.child xml_arg0 "ipam") in
      make ?ipam ()
    let of_json json =
      let ipam = field_map json "Ipam" Ipam.of_json in make ?ipam ()
    let to_json v = composed_to_json to_value v
  end
module DeleteIpamRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      ipamId: IpamId.t ;
      cascade: Boolean.t option }
    let context_ = "DeleteIpamRequest"
    let make ?dryRun =
      fun ?cascade -> fun ~ipamId -> fun () -> { dryRun; cascade; ipamId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Cascade", (Option.map x.cascade ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cascade =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Cascade") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?cascade ~ipamId ?dryRun ()
    let of_json json =
      let cascade = field_map json "Cascade" Boolean.of_json in
      let ipamId = field_map_exn json "IpamId" IpamId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?cascade ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteIpamPoolResult =
  struct
    type nonrec t = {
      ipamPool: IpamPool.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPool = fun () -> { ipamPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPool", (Option.map x.ipamPool ~f:IpamPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPool =
        (Option.map ~f:IpamPool.of_xml) (Xml.child xml_arg0 "ipamPool") in
      make ?ipamPool ()
    let of_json json =
      let ipamPool = field_map json "IpamPool" IpamPool.of_json in
      make ?ipamPool ()
    let to_json v = composed_to_json to_value v
  end
module DeleteIpamPoolRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      ipamPoolId: IpamPoolId.t }
    let context_ = "DeleteIpamPoolRequest"
    let make ?dryRun = fun ~ipamPoolId -> fun () -> { dryRun; ipamPoolId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamPoolId ?dryRun ()
    let of_json json =
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      internetGatewayId: InternetGatewayId.t }
    let context_ = "DeleteInternetGatewayRequest"
    let make ?dryRun =
      fun ~internetGatewayId -> fun () -> { dryRun; internetGatewayId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Some (InternetGatewayId.to_value x.internetGatewayId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let internetGatewayId =
        InternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~internetGatewayId ?dryRun ()
    let of_json json =
      let internetGatewayId =
        field_map_exn json "InternetGatewayId" InternetGatewayId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~internetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteInstanceEventWindowResult =
  struct
    type nonrec t =
      {
      instanceEventWindowState: InstanceEventWindowStateChange.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindowState =
      fun () -> { instanceEventWindowState }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindowState",
           (Option.map x.instanceEventWindowState
              ~f:InstanceEventWindowStateChange.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindowState =
        (Option.map ~f:InstanceEventWindowStateChange.of_xml)
          (Xml.child xml_arg0 "instanceEventWindowState") in
      make ?instanceEventWindowState ()
    let of_json json =
      let instanceEventWindowState =
        field_map json "InstanceEventWindowState"
          InstanceEventWindowStateChange.of_json in
      make ?instanceEventWindowState ()
    let to_json v = composed_to_json to_value v
  end
module DeleteInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      forceDelete: Boolean.t option ;
      instanceEventWindowId: InstanceEventWindowId.t }
    let context_ = "DeleteInstanceEventWindowRequest"
    let make ?dryRun =
      fun ?forceDelete ->
        fun ~instanceEventWindowId ->
          fun () -> { dryRun; forceDelete; instanceEventWindowId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ForceDelete", (Option.map x.forceDelete ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Some (InstanceEventWindowId.to_value x.instanceEventWindowId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindowId =
        InstanceEventWindowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventWindowId") in
      let forceDelete =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ForceDelete") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~instanceEventWindowId ?forceDelete ?dryRun ()
    let of_json json =
      let instanceEventWindowId =
        field_map_exn json "InstanceEventWindowId"
          InstanceEventWindowId.of_json in
      let forceDelete = field_map json "ForceDelete" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~instanceEventWindowId ?forceDelete ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteFpgaImageResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module DeleteFpgaImageRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      fpgaImageId: FpgaImageId.t }
    let context_ = "DeleteFpgaImageRequest"
    let make ?dryRun = fun ~fpgaImageId -> fun () -> { dryRun; fpgaImageId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FpgaImageId", (Some (FpgaImageId.to_value x.fpgaImageId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageId =
        FpgaImageId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~fpgaImageId ?dryRun ()
    let of_json json =
      let fpgaImageId = field_map_exn json "FpgaImageId" FpgaImageId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~fpgaImageId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteFlowLogsResult =
  struct
    type nonrec t = {
      unsuccessful: UnsuccessfulItemSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end
module DeleteFlowLogsRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option ;
      flowLogIds: FlowLogIdList.t }
    let context_ = "DeleteFlowLogsRequest"
    let make ?dryRun = fun ~flowLogIds -> fun () -> { dryRun; flowLogIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FlowLogId", (Some (FlowLogIdList.to_value x.flowLogIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flowLogIds =
        FlowLogIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FlowLogId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~flowLogIds ?dryRun ()
    let of_json json =
      let flowLogIds = field_map_exn json "FlowLogIds" FlowLogIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~flowLogIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteFleetsResult =
  struct
    type nonrec t =
      {
      successfulFleetDeletions: DeleteFleetSuccessSet.t option ;
      unsuccessfulFleetDeletions: DeleteFleetErrorSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulFleetDeletions =
      fun ?unsuccessfulFleetDeletions ->
        fun () -> { successfulFleetDeletions; unsuccessfulFleetDeletions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulFleetDeletionSet",
           (Option.map x.successfulFleetDeletions
              ~f:DeleteFleetSuccessSet.to_value));
        ("unsuccessfulFleetDeletionSet",
          (Option.map x.unsuccessfulFleetDeletions
             ~f:DeleteFleetErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfulFleetDeletions =
        (Option.map ~f:DeleteFleetErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfulFleetDeletionSet") in
      let successfulFleetDeletions =
        (Option.map ~f:DeleteFleetSuccessSet.of_xml)
          (Xml.child xml_arg0 "successfulFleetDeletionSet") in
      make ?unsuccessfulFleetDeletions ?successfulFleetDeletions ()
    let of_json json =
      let unsuccessfulFleetDeletions =
        field_map json "UnsuccessfulFleetDeletions"
          DeleteFleetErrorSet.of_json in
      let successfulFleetDeletions =
        field_map json "SuccessfulFleetDeletions"
          DeleteFleetSuccessSet.of_json in
      make ?unsuccessfulFleetDeletions ?successfulFleetDeletions ()
    let to_json v = composed_to_json to_value v
  end
module DeleteFleetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      fleetIds: FleetIdSet.t ;
      terminateInstances: Boolean.t }
    let context_ = "DeleteFleetsRequest"
    let make ?dryRun =
      fun ~fleetIds ->
        fun ~terminateInstances ->
          fun () -> { dryRun; fleetIds; terminateInstances }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FleetId", (Some (FleetIdSet.to_value x.fleetIds)));
        ("TerminateInstances",
          (Some (Boolean.to_value x.terminateInstances)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminateInstances =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TerminateInstances") in
      let fleetIds =
        FleetIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~terminateInstances ~fleetIds ?dryRun ()
    let of_json json =
      let terminateInstances =
        field_map_exn json "TerminateInstances" Boolean.of_json in
      let fleetIds = field_map_exn json "FleetIds" FleetIdSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~terminateInstances ~fleetIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteEgressOnlyInternetGatewayResult =
  struct
    type nonrec t = {
      returnCode: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?returnCode = fun () -> { returnCode }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("returnCode", (Option.map x.returnCode ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnCode =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "returnCode") in
      make ?returnCode ()
    let of_json json =
      let returnCode = field_map json "ReturnCode" Boolean.of_json in
      make ?returnCode ()
    let to_json v = composed_to_json to_value v
  end
module DeleteEgressOnlyInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      egressOnlyInternetGatewayId: EgressOnlyInternetGatewayId.t }
    let context_ = "DeleteEgressOnlyInternetGatewayRequest"
    let make ?dryRun =
      fun ~egressOnlyInternetGatewayId ->
        fun () -> { dryRun; egressOnlyInternetGatewayId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("EgressOnlyInternetGatewayId",
          (Some
             (EgressOnlyInternetGatewayId.to_value
                x.egressOnlyInternetGatewayId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let egressOnlyInternetGatewayId =
        EgressOnlyInternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "EgressOnlyInternetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~egressOnlyInternetGatewayId ?dryRun ()
    let of_json json =
      let egressOnlyInternetGatewayId =
        field_map_exn json "EgressOnlyInternetGatewayId"
          EgressOnlyInternetGatewayId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~egressOnlyInternetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module DeleteDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpOptionsId: DhcpOptionsId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteDhcpOptionsRequest"
    let make ?dryRun =
      fun ~dhcpOptionsId -> fun () -> { dryRun; dhcpOptionsId }
    let to_value x =
      structure_to_value
        [("DhcpOptionsId", (Some (DhcpOptionsId.to_value x.dhcpOptionsId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let dhcpOptionsId =
        DhcpOptionsId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DhcpOptionsId") in
      make ?dryRun ~dhcpOptionsId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let dhcpOptionsId =
        field_map_exn json "DhcpOptionsId" DhcpOptionsId.of_json in
      make ?dryRun ~dhcpOptionsId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteCustomerGatewayRequest =
  struct
    type nonrec t =
      {
      customerGatewayId: CustomerGatewayId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteCustomerGatewayRequest"
    let make ?dryRun =
      fun ~customerGatewayId -> fun () -> { dryRun; customerGatewayId }
    let to_value x =
      structure_to_value
        [("CustomerGatewayId",
           (Some (CustomerGatewayId.to_value x.customerGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let customerGatewayId =
        CustomerGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CustomerGatewayId") in
      make ?dryRun ~customerGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let customerGatewayId =
        field_map_exn json "CustomerGatewayId" CustomerGatewayId.of_json in
      make ?dryRun ~customerGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteClientVpnRouteResult =
  struct
    type nonrec t = {
      status: ClientVpnRouteStatus.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ClientVpnRouteStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnRouteStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status = field_map json "Status" ClientVpnRouteStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module DeleteClientVpnRouteRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      targetVpcSubnetId: SubnetId.t option ;
      destinationCidrBlock: String_.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteClientVpnRouteRequest"
    let make ?targetVpcSubnetId =
      fun ?dryRun ->
        fun ~clientVpnEndpointId ->
          fun ~destinationCidrBlock ->
            fun () ->
              {
                targetVpcSubnetId;
                dryRun;
                clientVpnEndpointId;
                destinationCidrBlock
              }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("TargetVpcSubnetId",
          (Option.map x.targetVpcSubnetId ~f:SubnetId.to_value));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let targetVpcSubnetId =
        (Option.map ~f:SubnetId.of_xml)
          (Xml.child xml_arg0 "TargetVpcSubnetId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~destinationCidrBlock ?targetVpcSubnetId
        ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      let targetVpcSubnetId =
        field_map json "TargetVpcSubnetId" SubnetId.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~destinationCidrBlock ?targetVpcSubnetId
        ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteClientVpnEndpointResult =
  struct
    type nonrec t = {
      status: ClientVpnEndpointStatus.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status",
           (Option.map x.status ~f:ClientVpnEndpointStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnEndpointStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status = field_map json "Status" ClientVpnEndpointStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module DeleteClientVpnEndpointRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteClientVpnEndpointRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId -> fun () -> { dryRun; clientVpnEndpointId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteCarrierGatewayResult =
  struct
    type nonrec t = {
      carrierGateway: CarrierGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?carrierGateway = fun () -> { carrierGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("carrierGateway",
           (Option.map x.carrierGateway ~f:CarrierGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let carrierGateway =
        (Option.map ~f:CarrierGateway.of_xml)
          (Xml.child xml_arg0 "carrierGateway") in
      make ?carrierGateway ()
    let of_json json =
      let carrierGateway =
        field_map json "CarrierGateway" CarrierGateway.of_json in
      make ?carrierGateway ()
    let to_json v = composed_to_json to_value v
  end
module DeleteCarrierGatewayRequest =
  struct
    type nonrec t =
      {
      carrierGatewayId: CarrierGatewayId.t ;
      dryRun: Boolean.t option }
    let context_ = "DeleteCarrierGatewayRequest"
    let make ?dryRun =
      fun ~carrierGatewayId -> fun () -> { dryRun; carrierGatewayId }
    let to_value x =
      structure_to_value
        [("CarrierGatewayId",
           (Some (CarrierGatewayId.to_value x.carrierGatewayId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let carrierGatewayId =
        CarrierGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CarrierGatewayId") in
      make ?dryRun ~carrierGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let carrierGatewayId =
        field_map_exn json "CarrierGatewayId" CarrierGatewayId.of_json in
      make ?dryRun ~carrierGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpnGatewayResult =
  struct
    type nonrec t = {
      vpnGateway: VpnGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnGateway = fun () -> { vpnGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnGateway", (Option.map x.vpnGateway ~f:VpnGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnGateway =
        (Option.map ~f:VpnGateway.of_xml) (Xml.child xml_arg0 "vpnGateway") in
      make ?vpnGateway ()
    let of_json json =
      let vpnGateway = field_map json "VpnGateway" VpnGateway.of_json in
      make ?vpnGateway ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpnGatewayRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t option ;
      type_: GatewayType.t ;
      tagSpecifications: TagSpecificationList.t option ;
      amazonSideAsn: Long.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateVpnGatewayRequest"
    let make ?availabilityZone =
      fun ?tagSpecifications ->
        fun ?amazonSideAsn ->
          fun ?dryRun ->
            fun ~type_ ->
              fun () ->
                {
                  availabilityZone;
                  tagSpecifications;
                  amazonSideAsn;
                  dryRun;
                  type_
                }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:String_.to_value));
        ("Type", (Some (GatewayType.to_value x.type_)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("AmazonSideAsn", (Option.map x.amazonSideAsn ~f:Long.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let amazonSideAsn =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "AmazonSideAsn") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let type_ =
        GatewayType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?dryRun ?amazonSideAsn ?tagSpecifications ~type_ ?availabilityZone
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let amazonSideAsn = field_map json "AmazonSideAsn" Long.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let type_ = field_map_exn json "Type" GatewayType.of_json in
      let availabilityZone =
        field_map json "AvailabilityZone" String_.of_json in
      make ?dryRun ?amazonSideAsn ?tagSpecifications ~type_ ?availabilityZone
        ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpnConnectionRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t ;
      vpnConnectionId: VpnConnectionId.t }
    let context_ = "CreateVpnConnectionRouteRequest"
    let make ~destinationCidrBlock =
      fun ~vpnConnectionId ->
        fun () -> { destinationCidrBlock; vpnConnectionId }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("VpnConnectionId",
          (Some (VpnConnectionId.to_value x.vpnConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let of_json json =
      let vpnConnectionId =
        field_map_exn json "VpnConnectionId" VpnConnectionId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpnConnectionResult =
  struct
    type nonrec t = {
      vpnConnection: VpnConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnection = fun () -> { vpnConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnection",
           (Option.map x.vpnConnection ~f:VpnConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnection =
        (Option.map ~f:VpnConnection.of_xml)
          (Xml.child xml_arg0 "vpnConnection") in
      make ?vpnConnection ()
    let of_json json =
      let vpnConnection =
        field_map json "VpnConnection" VpnConnection.of_json in
      make ?vpnConnection ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpnConnectionRequest =
  struct
    type nonrec t =
      {
      customerGatewayId: CustomerGatewayId.t ;
      type_: String_.t ;
      vpnGatewayId: VpnGatewayId.t option ;
      transitGatewayId: TransitGatewayId.t option ;
      dryRun: Boolean.t option ;
      options: VpnConnectionOptionsSpecification.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateVpnConnectionRequest"
    let make ?vpnGatewayId =
      fun ?transitGatewayId ->
        fun ?dryRun ->
          fun ?options ->
            fun ?tagSpecifications ->
              fun ~customerGatewayId ->
                fun ~type_ ->
                  fun () ->
                    {
                      vpnGatewayId;
                      transitGatewayId;
                      dryRun;
                      options;
                      tagSpecifications;
                      customerGatewayId;
                      type_
                    }
    let to_value x =
      structure_to_value
        [("CustomerGatewayId",
           (Some (CustomerGatewayId.to_value x.customerGatewayId)));
        ("Type", (Some (String_.to_value x.type_)));
        ("VpnGatewayId",
          (Option.map x.vpnGatewayId ~f:VpnGatewayId.to_value));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("options",
          (Option.map x.options ~f:VpnConnectionOptionsSpecification.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        (Option.map ~f:VpnConnectionOptionsSpecification.of_xml)
          (Xml.child xml_arg0 "options") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let vpnGatewayId =
        (Option.map ~f:VpnGatewayId.of_xml)
          (Xml.child xml_arg0 "VpnGatewayId") in
      let type_ =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let customerGatewayId =
        CustomerGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CustomerGatewayId") in
      make ?tagSpecifications ?options ?dryRun ?transitGatewayId
        ?vpnGatewayId ~type_ ~customerGatewayId ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json "Options" VpnConnectionOptionsSpecification.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayId =
        field_map json "TransitGatewayId" TransitGatewayId.of_json in
      let vpnGatewayId = field_map json "VpnGatewayId" VpnGatewayId.of_json in
      let type_ = field_map_exn json "Type" String_.of_json in
      let customerGatewayId =
        field_map_exn json "CustomerGatewayId" CustomerGatewayId.of_json in
      make ?tagSpecifications ?options ?dryRun ?transitGatewayId
        ?vpnGatewayId ~type_ ~customerGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcResult =
  struct
    type nonrec t = {
      vpc: Vpc.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpc = fun () -> { vpc }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("vpc", (Option.map x.vpc ~f:Vpc.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpc = (Option.map ~f:Vpc.of_xml) (Xml.child xml_arg0 "vpc") in
      make ?vpc ()
    let of_json json =
      let vpc = field_map json "Vpc" Vpc.of_json in make ?vpc ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcRequest =
  struct
    type nonrec t =
      {
      cidrBlock: String_.t option ;
      amazonProvidedIpv6CidrBlock: Boolean.t option ;
      ipv6Pool: Ipv6PoolEc2Id.t option ;
      ipv6CidrBlock: String_.t option ;
      ipv4IpamPoolId: IpamPoolId.t option ;
      ipv4NetmaskLength: NetmaskLength.t option ;
      ipv6IpamPoolId: IpamPoolId.t option ;
      ipv6NetmaskLength: NetmaskLength.t option ;
      dryRun: Boolean.t option ;
      instanceTenancy: Tenancy.t option ;
      ipv6CidrBlockNetworkBorderGroup: String_.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?cidrBlock =
      fun ?amazonProvidedIpv6CidrBlock ->
        fun ?ipv6Pool ->
          fun ?ipv6CidrBlock ->
            fun ?ipv4IpamPoolId ->
              fun ?ipv4NetmaskLength ->
                fun ?ipv6IpamPoolId ->
                  fun ?ipv6NetmaskLength ->
                    fun ?dryRun ->
                      fun ?instanceTenancy ->
                        fun ?ipv6CidrBlockNetworkBorderGroup ->
                          fun ?tagSpecifications ->
                            fun () ->
                              {
                                cidrBlock;
                                amazonProvidedIpv6CidrBlock;
                                ipv6Pool;
                                ipv6CidrBlock;
                                ipv4IpamPoolId;
                                ipv4NetmaskLength;
                                ipv6IpamPoolId;
                                ipv6NetmaskLength;
                                dryRun;
                                instanceTenancy;
                                ipv6CidrBlockNetworkBorderGroup;
                                tagSpecifications
                              }
    let to_value x =
      structure_to_value
        [("CidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("amazonProvidedIpv6CidrBlock",
          (Option.map x.amazonProvidedIpv6CidrBlock ~f:Boolean.to_value));
        ("Ipv6Pool", (Option.map x.ipv6Pool ~f:Ipv6PoolEc2Id.to_value));
        ("Ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("Ipv4IpamPoolId",
          (Option.map x.ipv4IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv4NetmaskLength",
          (Option.map x.ipv4NetmaskLength ~f:NetmaskLength.to_value));
        ("Ipv6IpamPoolId",
          (Option.map x.ipv6IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv6NetmaskLength",
          (Option.map x.ipv6NetmaskLength ~f:NetmaskLength.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceTenancy",
          (Option.map x.instanceTenancy ~f:Tenancy.to_value));
        ("Ipv6CidrBlockNetworkBorderGroup",
          (Option.map x.ipv6CidrBlockNetworkBorderGroup ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let ipv6CidrBlockNetworkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "Ipv6CidrBlockNetworkBorderGroup") in
      let instanceTenancy =
        (Option.map ~f:Tenancy.of_xml) (Xml.child xml_arg0 "instanceTenancy") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let ipv6NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv6NetmaskLength") in
      let ipv6IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv6IpamPoolId") in
      let ipv4NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv4NetmaskLength") in
      let ipv4IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv4IpamPoolId") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ipv6CidrBlock") in
      let ipv6Pool =
        (Option.map ~f:Ipv6PoolEc2Id.of_xml) (Xml.child xml_arg0 "Ipv6Pool") in
      let amazonProvidedIpv6CidrBlock =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "amazonProvidedIpv6CidrBlock") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      make ?tagSpecifications ?ipv6CidrBlockNetworkBorderGroup
        ?instanceTenancy ?dryRun ?ipv6NetmaskLength ?ipv6IpamPoolId
        ?ipv4NetmaskLength ?ipv4IpamPoolId ?ipv6CidrBlock ?ipv6Pool
        ?amazonProvidedIpv6CidrBlock ?cidrBlock ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let ipv6CidrBlockNetworkBorderGroup =
        field_map json "Ipv6CidrBlockNetworkBorderGroup" String_.of_json in
      let instanceTenancy = field_map json "InstanceTenancy" Tenancy.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let ipv6NetmaskLength =
        field_map json "Ipv6NetmaskLength" NetmaskLength.of_json in
      let ipv6IpamPoolId = field_map json "Ipv6IpamPoolId" IpamPoolId.of_json in
      let ipv4NetmaskLength =
        field_map json "Ipv4NetmaskLength" NetmaskLength.of_json in
      let ipv4IpamPoolId = field_map json "Ipv4IpamPoolId" IpamPoolId.of_json in
      let ipv6CidrBlock = field_map json "Ipv6CidrBlock" String_.of_json in
      let ipv6Pool = field_map json "Ipv6Pool" Ipv6PoolEc2Id.of_json in
      let amazonProvidedIpv6CidrBlock =
        field_map json "AmazonProvidedIpv6CidrBlock" Boolean.of_json in
      let cidrBlock = field_map json "CidrBlock" String_.of_json in
      make ?tagSpecifications ?ipv6CidrBlockNetworkBorderGroup
        ?instanceTenancy ?dryRun ?ipv6NetmaskLength ?ipv6IpamPoolId
        ?ipv4NetmaskLength ?ipv4IpamPoolId ?ipv6CidrBlock ?ipv6Pool
        ?amazonProvidedIpv6CidrBlock ?cidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcPeeringConnectionResult =
  struct
    type nonrec t = {
      vpcPeeringConnection: VpcPeeringConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringConnection = fun () -> { vpcPeeringConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcPeeringConnection",
           (Option.map x.vpcPeeringConnection
              ~f:VpcPeeringConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnection =
        (Option.map ~f:VpcPeeringConnection.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnection") in
      make ?vpcPeeringConnection ()
    let of_json json =
      let vpcPeeringConnection =
        field_map json "VpcPeeringConnection" VpcPeeringConnection.of_json in
      make ?vpcPeeringConnection ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      peerOwnerId: String_.t option ;
      peerVpcId: String_.t option ;
      vpcId: VpcId.t option ;
      peerRegion: String_.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?dryRun =
      fun ?peerOwnerId ->
        fun ?peerVpcId ->
          fun ?vpcId ->
            fun ?peerRegion ->
              fun ?tagSpecifications ->
                fun () ->
                  {
                    dryRun;
                    peerOwnerId;
                    peerVpcId;
                    vpcId;
                    peerRegion;
                    tagSpecifications
                  }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("peerOwnerId", (Option.map x.peerOwnerId ~f:String_.to_value));
        ("peerVpcId", (Option.map x.peerVpcId ~f:String_.to_value));
        ("vpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("PeerRegion", (Option.map x.peerRegion ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let peerRegion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PeerRegion") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "vpcId") in
      let peerVpcId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "peerVpcId") in
      let peerOwnerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "peerOwnerId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tagSpecifications ?peerRegion ?vpcId ?peerVpcId ?peerOwnerId
        ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let peerRegion = field_map json "PeerRegion" String_.of_json in
      let vpcId = field_map json "VpcId" VpcId.of_json in
      let peerVpcId = field_map json "PeerVpcId" String_.of_json in
      let peerOwnerId = field_map json "PeerOwnerId" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?peerRegion ?vpcId ?peerVpcId ?peerOwnerId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcEndpointServiceConfigurationResult =
  struct
    type nonrec t =
      {
      serviceConfiguration: ServiceConfiguration.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serviceConfiguration =
      fun ?clientToken -> fun () -> { serviceConfiguration; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceConfiguration",
           (Option.map x.serviceConfiguration
              ~f:ServiceConfiguration.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let serviceConfiguration =
        (Option.map ~f:ServiceConfiguration.of_xml)
          (Xml.child xml_arg0 "serviceConfiguration") in
      make ?clientToken ?serviceConfiguration ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let serviceConfiguration =
        field_map json "ServiceConfiguration" ServiceConfiguration.of_json in
      make ?clientToken ?serviceConfiguration ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcEndpointServiceConfigurationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      acceptanceRequired: Boolean.t option ;
      privateDnsName: String_.t option ;
      networkLoadBalancerArns: ValueStringList.t option ;
      gatewayLoadBalancerArns: ValueStringList.t option ;
      clientToken: String_.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?dryRun =
      fun ?acceptanceRequired ->
        fun ?privateDnsName ->
          fun ?networkLoadBalancerArns ->
            fun ?gatewayLoadBalancerArns ->
              fun ?clientToken ->
                fun ?tagSpecifications ->
                  fun () ->
                    {
                      dryRun;
                      acceptanceRequired;
                      privateDnsName;
                      networkLoadBalancerArns;
                      gatewayLoadBalancerArns;
                      clientToken;
                      tagSpecifications
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("AcceptanceRequired",
          (Option.map x.acceptanceRequired ~f:Boolean.to_value));
        ("PrivateDnsName", (Option.map x.privateDnsName ~f:String_.to_value));
        ("NetworkLoadBalancerArn",
          (Option.map x.networkLoadBalancerArns ~f:ValueStringList.to_value));
        ("GatewayLoadBalancerArn",
          (Option.map x.gatewayLoadBalancerArns ~f:ValueStringList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let gatewayLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "GatewayLoadBalancerArn") in
      let networkLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "NetworkLoadBalancerArn") in
      let privateDnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PrivateDnsName") in
      let acceptanceRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AcceptanceRequired") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?clientToken ?gatewayLoadBalancerArns
        ?networkLoadBalancerArns ?privateDnsName ?acceptanceRequired ?dryRun
        ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let gatewayLoadBalancerArns =
        field_map json "GatewayLoadBalancerArns" ValueStringList.of_json in
      let networkLoadBalancerArns =
        field_map json "NetworkLoadBalancerArns" ValueStringList.of_json in
      let privateDnsName = field_map json "PrivateDnsName" String_.of_json in
      let acceptanceRequired =
        field_map json "AcceptanceRequired" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?clientToken ?gatewayLoadBalancerArns
        ?networkLoadBalancerArns ?privateDnsName ?acceptanceRequired ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcEndpointResult =
  struct
    type nonrec t =
      {
      vpcEndpoint: VpcEndpoint.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcEndpoint =
      fun ?clientToken -> fun () -> { vpcEndpoint; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcEndpoint", (Option.map x.vpcEndpoint ~f:VpcEndpoint.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let vpcEndpoint =
        (Option.map ~f:VpcEndpoint.of_xml) (Xml.child xml_arg0 "vpcEndpoint") in
      make ?clientToken ?vpcEndpoint ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let vpcEndpoint = field_map json "VpcEndpoint" VpcEndpoint.of_json in
      make ?clientToken ?vpcEndpoint ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcEndpointRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcEndpointType: VpcEndpointType.t option ;
      vpcId: VpcId.t ;
      serviceName: String_.t ;
      policyDocument: String_.t option ;
      routeTableIds: VpcEndpointRouteTableIdList.t option ;
      subnetIds: VpcEndpointSubnetIdList.t option ;
      securityGroupIds: VpcEndpointSecurityGroupIdList.t option ;
      clientToken: String_.t option ;
      privateDnsEnabled: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateVpcEndpointRequest"
    let make ?dryRun =
      fun ?vpcEndpointType ->
        fun ?policyDocument ->
          fun ?routeTableIds ->
            fun ?subnetIds ->
              fun ?securityGroupIds ->
                fun ?clientToken ->
                  fun ?privateDnsEnabled ->
                    fun ?tagSpecifications ->
                      fun ~vpcId ->
                        fun ~serviceName ->
                          fun () ->
                            {
                              dryRun;
                              vpcEndpointType;
                              policyDocument;
                              routeTableIds;
                              subnetIds;
                              securityGroupIds;
                              clientToken;
                              privateDnsEnabled;
                              tagSpecifications;
                              vpcId;
                              serviceName
                            }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointType",
          (Option.map x.vpcEndpointType ~f:VpcEndpointType.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("ServiceName", (Some (String_.to_value x.serviceName)));
        ("PolicyDocument", (Option.map x.policyDocument ~f:String_.to_value));
        ("RouteTableId",
          (Option.map x.routeTableIds ~f:VpcEndpointRouteTableIdList.to_value));
        ("SubnetId",
          (Option.map x.subnetIds ~f:VpcEndpointSubnetIdList.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:VpcEndpointSecurityGroupIdList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("PrivateDnsEnabled",
          (Option.map x.privateDnsEnabled ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let privateDnsEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PrivateDnsEnabled") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let securityGroupIds =
        (Option.map ~f:VpcEndpointSecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let subnetIds =
        (Option.map ~f:VpcEndpointSubnetIdList.of_xml)
          (Xml.child xml_arg0 "SubnetId") in
      let routeTableIds =
        (Option.map ~f:VpcEndpointRouteTableIdList.of_xml)
          (Xml.child xml_arg0 "RouteTableId") in
      let policyDocument =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PolicyDocument") in
      let serviceName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceName") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let vpcEndpointType =
        (Option.map ~f:VpcEndpointType.of_xml)
          (Xml.child xml_arg0 "VpcEndpointType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?privateDnsEnabled ?clientToken
        ?securityGroupIds ?subnetIds ?routeTableIds ?policyDocument
        ~serviceName ~vpcId ?vpcEndpointType ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let privateDnsEnabled =
        field_map json "PrivateDnsEnabled" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds"
          VpcEndpointSecurityGroupIdList.of_json in
      let subnetIds =
        field_map json "SubnetIds" VpcEndpointSubnetIdList.of_json in
      let routeTableIds =
        field_map json "RouteTableIds" VpcEndpointRouteTableIdList.of_json in
      let policyDocument = field_map json "PolicyDocument" String_.of_json in
      let serviceName = field_map_exn json "ServiceName" String_.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let vpcEndpointType =
        field_map json "VpcEndpointType" VpcEndpointType.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?privateDnsEnabled ?clientToken
        ?securityGroupIds ?subnetIds ?routeTableIds ?policyDocument
        ~serviceName ~vpcId ?vpcEndpointType ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcEndpointConnectionNotificationResult =
  struct
    type nonrec t =
      {
      connectionNotification: ConnectionNotification.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?connectionNotification =
      fun ?clientToken -> fun () -> { connectionNotification; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("connectionNotification",
           (Option.map x.connectionNotification
              ~f:ConnectionNotification.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let connectionNotification =
        (Option.map ~f:ConnectionNotification.of_xml)
          (Xml.child xml_arg0 "connectionNotification") in
      make ?clientToken ?connectionNotification ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let connectionNotification =
        field_map json "ConnectionNotification"
          ConnectionNotification.of_json in
      make ?clientToken ?connectionNotification ()
    let to_json v = composed_to_json to_value v
  end
module CreateVpcEndpointConnectionNotificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      serviceId: VpcEndpointServiceId.t option ;
      vpcEndpointId: VpcEndpointId.t option ;
      connectionNotificationArn: String_.t ;
      connectionEvents: ValueStringList.t ;
      clientToken: String_.t option }
    let context_ = "CreateVpcEndpointConnectionNotificationRequest"
    let make ?dryRun =
      fun ?serviceId ->
        fun ?vpcEndpointId ->
          fun ?clientToken ->
            fun ~connectionNotificationArn ->
              fun ~connectionEvents ->
                fun () ->
                  {
                    dryRun;
                    serviceId;
                    vpcEndpointId;
                    clientToken;
                    connectionNotificationArn;
                    connectionEvents
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId",
          (Option.map x.serviceId ~f:VpcEndpointServiceId.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointId ~f:VpcEndpointId.to_value));
        ("ConnectionNotificationArn",
          (Some (String_.to_value x.connectionNotificationArn)));
        ("ConnectionEvents",
          (Some (ValueStringList.to_value x.connectionEvents)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let connectionEvents =
        ValueStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectionEvents") in
      let connectionNotificationArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ConnectionNotificationArn") in
      let vpcEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let serviceId =
        (Option.map ~f:VpcEndpointServiceId.of_xml)
          (Xml.child xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ~connectionEvents ~connectionNotificationArn
        ?vpcEndpointId ?serviceId ?dryRun ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let connectionEvents =
        field_map_exn json "ConnectionEvents" ValueStringList.of_json in
      let connectionNotificationArn =
        field_map_exn json "ConnectionNotificationArn" String_.of_json in
      let vpcEndpointId =
        field_map json "VpcEndpointId" VpcEndpointId.of_json in
      let serviceId = field_map json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?clientToken ~connectionEvents ~connectionNotificationArn
        ?vpcEndpointId ?serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateVolumeRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t ;
      encrypted: Boolean.t option ;
      iops: Integer.t option ;
      kmsKeyId: KmsKeyId.t option ;
      outpostArn: String_.t option ;
      size: Integer.t option ;
      snapshotId: SnapshotId.t option ;
      volumeType: VolumeType.t option ;
      dryRun: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      multiAttachEnabled: Boolean.t option ;
      throughput: Integer.t option ;
      clientToken: String_.t option }
    let context_ = "CreateVolumeRequest"
    let make ?encrypted =
      fun ?iops ->
        fun ?kmsKeyId ->
          fun ?outpostArn ->
            fun ?size ->
              fun ?snapshotId ->
                fun ?volumeType ->
                  fun ?dryRun ->
                    fun ?tagSpecifications ->
                      fun ?multiAttachEnabled ->
                        fun ?throughput ->
                          fun ?clientToken ->
                            fun ~availabilityZone ->
                              fun () ->
                                {
                                  encrypted;
                                  iops;
                                  kmsKeyId;
                                  outpostArn;
                                  size;
                                  snapshotId;
                                  volumeType;
                                  dryRun;
                                  tagSpecifications;
                                  multiAttachEnabled;
                                  throughput;
                                  clientToken;
                                  availabilityZone
                                }
    let to_value x =
      structure_to_value
        [("AvailabilityZone", (Some (String_.to_value x.availabilityZone)));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("Iops", (Option.map x.iops ~f:Integer.to_value));
        ("KmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("Size", (Option.map x.size ~f:Integer.to_value));
        ("SnapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value));
        ("VolumeType", (Option.map x.volumeType ~f:VolumeType.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("MultiAttachEnabled",
          (Option.map x.multiAttachEnabled ~f:Boolean.to_value));
        ("Throughput", (Option.map x.throughput ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let throughput =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Throughput") in
      let multiAttachEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "MultiAttachEnabled") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeType =
        (Option.map ~f:VolumeType.of_xml) (Xml.child xml_arg0 "VolumeType") in
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "SnapshotId") in
      let size = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Size") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "KmsKeyId") in
      let iops = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Iops") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let availabilityZone =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZone") in
      make ?clientToken ?throughput ?multiAttachEnabled ?tagSpecifications
        ?dryRun ?volumeType ?snapshotId ?size ?outpostArn ?kmsKeyId ?iops
        ?encrypted ~availabilityZone ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let throughput = field_map json "Throughput" Integer.of_json in
      let multiAttachEnabled =
        field_map json "MultiAttachEnabled" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeType = field_map json "VolumeType" VolumeType.of_json in
      let snapshotId = field_map json "SnapshotId" SnapshotId.of_json in
      let size = field_map json "Size" Integer.of_json in
      let outpostArn = field_map json "OutpostArn" String_.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      let iops = field_map json "Iops" Integer.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let availabilityZone =
        field_map_exn json "AvailabilityZone" String_.of_json in
      make ?clientToken ?throughput ?multiAttachEnabled ?tagSpecifications
        ?dryRun ?volumeType ?snapshotId ?size ?outpostArn ?kmsKeyId ?iops
        ?encrypted ~availabilityZone ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_json json =
      let transitGatewayVpcAttachment =
        field_map json "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t ;
      vpcId: VpcId.t ;
      subnetIds: TransitGatewaySubnetIdList.t ;
      options: CreateTransitGatewayVpcAttachmentRequestOptions.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayVpcAttachmentRequest"
    let make ?options =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun ~vpcId ->
              fun ~subnetIds ->
                fun () ->
                  {
                    options;
                    tagSpecifications;
                    dryRun;
                    transitGatewayId;
                    vpcId;
                    subnetIds
                  }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("SubnetIds",
          (Some (TransitGatewaySubnetIdList.to_value x.subnetIds)));
        ("Options",
          (Option.map x.options
             ~f:CreateTransitGatewayVpcAttachmentRequestOptions.to_value));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let options =
        (Option.map ~f:CreateTransitGatewayVpcAttachmentRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let subnetIds =
        TransitGatewaySubnetIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubnetIds") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ?options ~subnetIds ~vpcId
        ~transitGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json "Options"
          CreateTransitGatewayVpcAttachmentRequestOptions.of_json in
      let subnetIds =
        field_map_exn json "SubnetIds" TransitGatewaySubnetIdList.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let transitGatewayId =
        field_map_exn json "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?options ~subnetIds ~vpcId
        ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTable: TransitGatewayRouteTable.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTable =
      fun () -> { transitGatewayRouteTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTable",
           (Option.map x.transitGatewayRouteTable
              ~f:TransitGatewayRouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayRouteTable =
        (Option.map ~f:TransitGatewayRouteTable.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTable") in
      make ?transitGatewayRouteTable ()
    let of_json json =
      let transitGatewayRouteTable =
        field_map json "TransitGatewayRouteTable"
          TransitGatewayRouteTable.of_json in
      make ?transitGatewayRouteTable ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayRouteTableRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transitGatewayId ->
          fun () -> { tagSpecifications; dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ~transitGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let transitGatewayId =
        field_map_exn json "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayRouteResult =
  struct
    type nonrec t = {
      route: TransitGatewayRoute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:TransitGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:TransitGatewayRoute.of_xml)
          (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_json json =
      let route = field_map json "Route" TransitGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t ;
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option ;
      blackhole: Boolean.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayRouteRequest"
    let make ?transitGatewayAttachmentId =
      fun ?blackhole ->
        fun ?dryRun ->
          fun ~destinationCidrBlock ->
            fun ~transitGatewayRouteTableId ->
              fun () ->
                {
                  transitGatewayAttachmentId;
                  blackhole;
                  dryRun;
                  destinationCidrBlock;
                  transitGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("TransitGatewayRouteTableId",
          (Some
             (TransitGatewayRouteTableId.to_value
                x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("Blackhole", (Option.map x.blackhole ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let blackhole =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Blackhole") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId
        ~transitGatewayRouteTableId ~destinationCidrBlock ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let blackhole = field_map json "Blackhole" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId
        ~transitGatewayRouteTableId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayResult =
  struct
    type nonrec t = {
      transitGateway: TransitGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGateway = fun () -> { transitGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGateway",
           (Option.map x.transitGateway ~f:TransitGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGateway =
        (Option.map ~f:TransitGateway.of_xml)
          (Xml.child xml_arg0 "transitGateway") in
      make ?transitGateway ()
    let of_json json =
      let transitGateway =
        field_map json "TransitGateway" TransitGateway.of_json in
      make ?transitGateway ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      options: TransitGatewayRequestOptions.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let make ?description =
      fun ?options ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun () -> { description; options; tagSpecifications; dryRun }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("Options",
          (Option.map x.options ~f:TransitGatewayRequestOptions.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        (Option.map ~f:TransitGatewayRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?dryRun ?tagSpecifications ?options ?description ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json "Options" TransitGatewayRequestOptions.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?dryRun ?tagSpecifications ?options ?description ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayPrefixListReferenceResult =
  struct
    type nonrec t =
      {
      transitGatewayPrefixListReference:
        TransitGatewayPrefixListReference.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPrefixListReference =
      fun () -> { transitGatewayPrefixListReference }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPrefixListReference",
           (Option.map x.transitGatewayPrefixListReference
              ~f:TransitGatewayPrefixListReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPrefixListReference =
        (Option.map ~f:TransitGatewayPrefixListReference.of_xml)
          (Xml.child xml_arg0 "transitGatewayPrefixListReference") in
      make ?transitGatewayPrefixListReference ()
    let of_json json =
      let transitGatewayPrefixListReference =
        field_map json "TransitGatewayPrefixListReference"
          TransitGatewayPrefixListReference.of_json in
      make ?transitGatewayPrefixListReference ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayPrefixListReferenceRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      prefixListId: PrefixListResourceId.t ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option ;
      blackhole: Boolean.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayPrefixListReferenceRequest"
    let make ?transitGatewayAttachmentId =
      fun ?blackhole ->
        fun ?dryRun ->
          fun ~transitGatewayRouteTableId ->
            fun ~prefixListId ->
              fun () ->
                {
                  transitGatewayAttachmentId;
                  blackhole;
                  dryRun;
                  transitGatewayRouteTableId;
                  prefixListId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("Blackhole", (Option.map x.blackhole ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let blackhole =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Blackhole") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId ~prefixListId
        ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let blackhole = field_map json "Blackhole" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let prefixListId =
        field_map_exn json "PrefixListId" PrefixListResourceId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId ~prefixListId
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_json json =
      let transitGatewayPeeringAttachment =
        field_map json "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t ;
      peerTransitGatewayId: TransitAssociationGatewayId.t ;
      peerAccountId: String_.t ;
      peerRegion: String_.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayPeeringAttachmentRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transitGatewayId ->
          fun ~peerTransitGatewayId ->
            fun ~peerAccountId ->
              fun ~peerRegion ->
                fun () ->
                  {
                    tagSpecifications;
                    dryRun;
                    transitGatewayId;
                    peerTransitGatewayId;
                    peerAccountId;
                    peerRegion
                  }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("PeerTransitGatewayId",
          (Some (TransitAssociationGatewayId.to_value x.peerTransitGatewayId)));
        ("PeerAccountId", (Some (String_.to_value x.peerAccountId)));
        ("PeerRegion", (Some (String_.to_value x.peerRegion)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let peerRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerRegion") in
      let peerAccountId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerAccountId") in
      let peerTransitGatewayId =
        TransitAssociationGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerTransitGatewayId") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ~peerRegion ~peerAccountId
        ~peerTransitGatewayId ~transitGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let peerRegion = field_map_exn json "PeerRegion" String_.of_json in
      let peerAccountId = field_map_exn json "PeerAccountId" String_.of_json in
      let peerTransitGatewayId =
        field_map_exn json "PeerTransitGatewayId"
          TransitAssociationGatewayId.of_json in
      let transitGatewayId =
        field_map_exn json "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ~peerRegion ~peerAccountId
        ~peerTransitGatewayId ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomain: TransitGatewayMulticastDomain.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMulticastDomain =
      fun () -> { transitGatewayMulticastDomain }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMulticastDomain",
           (Option.map x.transitGatewayMulticastDomain
              ~f:TransitGatewayMulticastDomain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMulticastDomain =
        (Option.map ~f:TransitGatewayMulticastDomain.of_xml)
          (Xml.child xml_arg0 "transitGatewayMulticastDomain") in
      make ?transitGatewayMulticastDomain ()
    let of_json json =
      let transitGatewayMulticastDomain =
        field_map json "TransitGatewayMulticastDomain"
          TransitGatewayMulticastDomain.of_json in
      make ?transitGatewayMulticastDomain ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t ;
      options: CreateTransitGatewayMulticastDomainRequestOptions.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayMulticastDomainRequest"
    let make ?options =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun () ->
              { options; tagSpecifications; dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("Options",
          (Option.map x.options
             ~f:CreateTransitGatewayMulticastDomainRequestOptions.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        (Option.map
           ~f:CreateTransitGatewayMulticastDomainRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ?options ~transitGatewayId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json "Options"
          CreateTransitGatewayMulticastDomainRequestOptions.of_json in
      let transitGatewayId =
        field_map_exn json "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?options ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayConnectResult =
  struct
    type nonrec t = {
      transitGatewayConnect: TransitGatewayConnect.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnect = fun () -> { transitGatewayConnect }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnect",
           (Option.map x.transitGatewayConnect
              ~f:TransitGatewayConnect.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnect =
        (Option.map ~f:TransitGatewayConnect.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnect") in
      make ?transitGatewayConnect ()
    let of_json json =
      let transitGatewayConnect =
        field_map json "TransitGatewayConnect" TransitGatewayConnect.of_json in
      make ?transitGatewayConnect ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayConnectRequest =
  struct
    type nonrec t =
      {
      transportTransitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      options: CreateTransitGatewayConnectRequestOptions.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayConnectRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transportTransitGatewayAttachmentId ->
          fun ~options ->
            fun () ->
              {
                tagSpecifications;
                dryRun;
                transportTransitGatewayAttachmentId;
                options
              }
    let to_value x =
      structure_to_value
        [("TransportTransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transportTransitGatewayAttachmentId)));
        ("Options",
          (Some
             (CreateTransitGatewayConnectRequestOptions.to_value x.options)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        CreateTransitGatewayConnectRequestOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Options") in
      let transportTransitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransportTransitGatewayAttachmentId") in
      make ?dryRun ?tagSpecifications ~options
        ~transportTransitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map_exn json "Options"
          CreateTransitGatewayConnectRequestOptions.of_json in
      let transportTransitGatewayAttachmentId =
        field_map_exn json "TransportTransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ?tagSpecifications ~options
        ~transportTransitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayConnectPeerResult =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeer: TransitGatewayConnectPeer.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnectPeer =
      fun () -> { transitGatewayConnectPeer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnectPeer",
           (Option.map x.transitGatewayConnectPeer
              ~f:TransitGatewayConnectPeer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnectPeer =
        (Option.map ~f:TransitGatewayConnectPeer.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnectPeer") in
      make ?transitGatewayConnectPeer ()
    let of_json json =
      let transitGatewayConnectPeer =
        field_map json "TransitGatewayConnectPeer"
          TransitGatewayConnectPeer.of_json in
      make ?transitGatewayConnectPeer ()
    let to_json v = composed_to_json to_value v
  end
module CreateTransitGatewayConnectPeerRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      transitGatewayAddress: String_.t option ;
      peerAddress: String_.t ;
      bgpOptions: TransitGatewayConnectRequestBgpOptions.t option ;
      insideCidrBlocks: InsideCidrBlocksStringList.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateTransitGatewayConnectPeerRequest"
    let make ?transitGatewayAddress =
      fun ?bgpOptions ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ~transitGatewayAttachmentId ->
              fun ~peerAddress ->
                fun ~insideCidrBlocks ->
                  fun () ->
                    {
                      transitGatewayAddress;
                      bgpOptions;
                      tagSpecifications;
                      dryRun;
                      transitGatewayAttachmentId;
                      peerAddress;
                      insideCidrBlocks
                    }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("TransitGatewayAddress",
          (Option.map x.transitGatewayAddress ~f:String_.to_value));
        ("PeerAddress", (Some (String_.to_value x.peerAddress)));
        ("BgpOptions",
          (Option.map x.bgpOptions
             ~f:TransitGatewayConnectRequestBgpOptions.to_value));
        ("InsideCidrBlocks",
          (Some (InsideCidrBlocksStringList.to_value x.insideCidrBlocks)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let insideCidrBlocks =
        InsideCidrBlocksStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InsideCidrBlocks") in
      let bgpOptions =
        (Option.map ~f:TransitGatewayConnectRequestBgpOptions.of_xml)
          (Xml.child xml_arg0 "BgpOptions") in
      let peerAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerAddress") in
      let transitGatewayAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAddress") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ?tagSpecifications ~insideCidrBlocks ?bgpOptions
        ~peerAddress ?transitGatewayAddress ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let insideCidrBlocks =
        field_map_exn json "InsideCidrBlocks"
          InsideCidrBlocksStringList.of_json in
      let bgpOptions =
        field_map json "BgpOptions"
          TransitGatewayConnectRequestBgpOptions.of_json in
      let peerAddress = field_map_exn json "PeerAddress" String_.of_json in
      let transitGatewayAddress =
        field_map json "TransitGatewayAddress" String_.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ?tagSpecifications ~insideCidrBlocks ?bgpOptions
        ~peerAddress ?transitGatewayAddress ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorTargetResult =
  struct
    type nonrec t =
      {
      trafficMirrorTarget: TrafficMirrorTarget.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorTarget =
      fun ?clientToken -> fun () -> { trafficMirrorTarget; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorTarget",
           (Option.map x.trafficMirrorTarget ~f:TrafficMirrorTarget.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorTarget =
        (Option.map ~f:TrafficMirrorTarget.of_xml)
          (Xml.child xml_arg0 "trafficMirrorTarget") in
      make ?clientToken ?trafficMirrorTarget ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let trafficMirrorTarget =
        field_map json "TrafficMirrorTarget" TrafficMirrorTarget.of_json in
      make ?clientToken ?trafficMirrorTarget ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorTargetRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t option ;
      networkLoadBalancerArn: String_.t option ;
      description: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t option }
    let make ?networkInterfaceId =
      fun ?networkLoadBalancerArn ->
        fun ?description ->
          fun ?tagSpecifications ->
            fun ?dryRun ->
              fun ?clientToken ->
                fun () ->
                  {
                    networkInterfaceId;
                    networkLoadBalancerArn;
                    description;
                    tagSpecifications;
                    dryRun;
                    clientToken
                  }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("NetworkLoadBalancerArn",
          (Option.map x.networkLoadBalancerArn ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let networkLoadBalancerArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkLoadBalancerArn") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceId") in
      make ?clientToken ?dryRun ?tagSpecifications ?description
        ?networkLoadBalancerArn ?networkInterfaceId ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json "Description" String_.of_json in
      let networkLoadBalancerArn =
        field_map json "NetworkLoadBalancerArn" String_.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ?description
        ?networkLoadBalancerArn ?networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorSessionResult =
  struct
    type nonrec t =
      {
      trafficMirrorSession: TrafficMirrorSession.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorSession =
      fun ?clientToken -> fun () -> { trafficMirrorSession; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorSession",
           (Option.map x.trafficMirrorSession
              ~f:TrafficMirrorSession.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorSession =
        (Option.map ~f:TrafficMirrorSession.of_xml)
          (Xml.child xml_arg0 "trafficMirrorSession") in
      make ?clientToken ?trafficMirrorSession ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let trafficMirrorSession =
        field_map json "TrafficMirrorSession" TrafficMirrorSession.of_json in
      make ?clientToken ?trafficMirrorSession ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorSessionRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t ;
      trafficMirrorTargetId: TrafficMirrorTargetId.t ;
      trafficMirrorFilterId: TrafficMirrorFilterId.t ;
      packetLength: Integer.t option ;
      sessionNumber: Integer.t ;
      virtualNetworkId: Integer.t option ;
      description: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t option }
    let context_ = "CreateTrafficMirrorSessionRequest"
    let make ?packetLength =
      fun ?virtualNetworkId ->
        fun ?description ->
          fun ?tagSpecifications ->
            fun ?dryRun ->
              fun ?clientToken ->
                fun ~networkInterfaceId ->
                  fun ~trafficMirrorTargetId ->
                    fun ~trafficMirrorFilterId ->
                      fun ~sessionNumber ->
                        fun () ->
                          {
                            packetLength;
                            virtualNetworkId;
                            description;
                            tagSpecifications;
                            dryRun;
                            clientToken;
                            networkInterfaceId;
                            trafficMirrorTargetId;
                            trafficMirrorFilterId;
                            sessionNumber
                          }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("TrafficMirrorTargetId",
          (Some (TrafficMirrorTargetId.to_value x.trafficMirrorTargetId)));
        ("TrafficMirrorFilterId",
          (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("PacketLength", (Option.map x.packetLength ~f:Integer.to_value));
        ("SessionNumber", (Some (Integer.to_value x.sessionNumber)));
        ("VirtualNetworkId",
          (Option.map x.virtualNetworkId ~f:Integer.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let virtualNetworkId =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "VirtualNetworkId") in
      let sessionNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SessionNumber") in
      let packetLength =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PacketLength") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      let trafficMirrorTargetId =
        TrafficMirrorTargetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorTargetId") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInterfaceId") in
      make ?clientToken ?dryRun ?tagSpecifications ?description
        ?virtualNetworkId ~sessionNumber ?packetLength ~trafficMirrorFilterId
        ~trafficMirrorTargetId ~networkInterfaceId ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json "Description" String_.of_json in
      let virtualNetworkId =
        field_map json "VirtualNetworkId" Integer.of_json in
      let sessionNumber = field_map_exn json "SessionNumber" Integer.of_json in
      let packetLength = field_map json "PacketLength" Integer.of_json in
      let trafficMirrorFilterId =
        field_map_exn json "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      let trafficMirrorTargetId =
        field_map_exn json "TrafficMirrorTargetId"
          TrafficMirrorTargetId.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ?description
        ?virtualNetworkId ~sessionNumber ?packetLength ~trafficMirrorFilterId
        ~trafficMirrorTargetId ~networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorFilterRuleResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRule: TrafficMirrorFilterRule.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterRule =
      fun ?clientToken -> fun () -> { trafficMirrorFilterRule; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterRule",
           (Option.map x.trafficMirrorFilterRule
              ~f:TrafficMirrorFilterRule.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorFilterRule =
        (Option.map ~f:TrafficMirrorFilterRule.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterRule") in
      make ?clientToken ?trafficMirrorFilterRule ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let trafficMirrorFilterRule =
        field_map json "TrafficMirrorFilterRule"
          TrafficMirrorFilterRule.of_json in
      make ?clientToken ?trafficMirrorFilterRule ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorFilterRuleRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterId: TrafficMirrorFilterId.t ;
      trafficDirection: TrafficDirection.t ;
      ruleNumber: Integer.t ;
      ruleAction: TrafficMirrorRuleAction.t ;
      destinationPortRange: TrafficMirrorPortRangeRequest.t option ;
      sourcePortRange: TrafficMirrorPortRangeRequest.t option ;
      protocol: Integer.t option ;
      destinationCidrBlock: String_.t ;
      sourceCidrBlock: String_.t ;
      description: String_.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t option }
    let context_ = "CreateTrafficMirrorFilterRuleRequest"
    let make ?destinationPortRange =
      fun ?sourcePortRange ->
        fun ?protocol ->
          fun ?description ->
            fun ?dryRun ->
              fun ?clientToken ->
                fun ~trafficMirrorFilterId ->
                  fun ~trafficDirection ->
                    fun ~ruleNumber ->
                      fun ~ruleAction ->
                        fun ~destinationCidrBlock ->
                          fun ~sourceCidrBlock ->
                            fun () ->
                              {
                                destinationPortRange;
                                sourcePortRange;
                                protocol;
                                description;
                                dryRun;
                                clientToken;
                                trafficMirrorFilterId;
                                trafficDirection;
                                ruleNumber;
                                ruleAction;
                                destinationCidrBlock;
                                sourceCidrBlock
                              }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterId",
           (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("TrafficDirection",
          (Some (TrafficDirection.to_value x.trafficDirection)));
        ("RuleNumber", (Some (Integer.to_value x.ruleNumber)));
        ("RuleAction",
          (Some (TrafficMirrorRuleAction.to_value x.ruleAction)));
        ("DestinationPortRange",
          (Option.map x.destinationPortRange
             ~f:TrafficMirrorPortRangeRequest.to_value));
        ("SourcePortRange",
          (Option.map x.sourcePortRange
             ~f:TrafficMirrorPortRangeRequest.to_value));
        ("Protocol", (Option.map x.protocol ~f:Integer.to_value));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("SourceCidrBlock", (Some (String_.to_value x.sourceCidrBlock)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceCidrBlock") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let protocol =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Protocol") in
      let sourcePortRange =
        (Option.map ~f:TrafficMirrorPortRangeRequest.of_xml)
          (Xml.child xml_arg0 "SourcePortRange") in
      let destinationPortRange =
        (Option.map ~f:TrafficMirrorPortRangeRequest.of_xml)
          (Xml.child xml_arg0 "DestinationPortRange") in
      let ruleAction =
        TrafficMirrorRuleAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleAction") in
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleNumber") in
      let trafficDirection =
        TrafficDirection.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficDirection") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      make ?clientToken ?dryRun ?description ~sourceCidrBlock
        ~destinationCidrBlock ?protocol ?sourcePortRange
        ?destinationPortRange ~ruleAction ~ruleNumber ~trafficDirection
        ~trafficMirrorFilterId ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let sourceCidrBlock =
        field_map_exn json "SourceCidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      let protocol = field_map json "Protocol" Integer.of_json in
      let sourcePortRange =
        field_map json "SourcePortRange"
          TrafficMirrorPortRangeRequest.of_json in
      let destinationPortRange =
        field_map json "DestinationPortRange"
          TrafficMirrorPortRangeRequest.of_json in
      let ruleAction =
        field_map_exn json "RuleAction" TrafficMirrorRuleAction.of_json in
      let ruleNumber = field_map_exn json "RuleNumber" Integer.of_json in
      let trafficDirection =
        field_map_exn json "TrafficDirection" TrafficDirection.of_json in
      let trafficMirrorFilterId =
        field_map_exn json "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      make ?clientToken ?dryRun ?description ~sourceCidrBlock
        ~destinationCidrBlock ?protocol ?sourcePortRange
        ?destinationPortRange ~ruleAction ~ruleNumber ~trafficDirection
        ~trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorFilterResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilter: TrafficMirrorFilter.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilter =
      fun ?clientToken -> fun () -> { trafficMirrorFilter; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilter",
           (Option.map x.trafficMirrorFilter ~f:TrafficMirrorFilter.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorFilter =
        (Option.map ~f:TrafficMirrorFilter.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilter") in
      make ?clientToken ?trafficMirrorFilter ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let trafficMirrorFilter =
        field_map json "TrafficMirrorFilter" TrafficMirrorFilter.of_json in
      make ?clientToken ?trafficMirrorFilter ()
    let to_json v = composed_to_json to_value v
  end
module CreateTrafficMirrorFilterRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t option }
    let make ?description =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ?clientToken ->
            fun () -> { description; tagSpecifications; dryRun; clientToken }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?clientToken ?dryRun ?tagSpecifications ?description ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ?description ()
    let to_json v = composed_to_json to_value v
  end
module CreateTagsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      resources: ResourceIdList.t ;
      tags: TagList.t }
    let context_ = "CreateTagsRequest"
    let make ?dryRun =
      fun ~resources -> fun ~tags -> fun () -> { dryRun; resources; tags }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ResourceId", (Some (ResourceIdList.to_value x.resources)));
        ("Tag", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tag") in
      let resources =
        ResourceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~tags ~resources ?dryRun ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resources = field_map_exn json "Resources" ResourceIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~tags ~resources ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateSubnetResult =
  struct
    type nonrec t = {
      subnet: Subnet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnet = fun () -> { subnet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnet", (Option.map x.subnet ~f:Subnet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnet =
        (Option.map ~f:Subnet.of_xml) (Xml.child xml_arg0 "subnet") in
      make ?subnet ()
    let of_json json =
      let subnet = field_map json "Subnet" Subnet.of_json in make ?subnet ()
    let to_json v = composed_to_json to_value v
  end
module CreateSubnetRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option ;
      availabilityZone: String_.t option ;
      availabilityZoneId: String_.t option ;
      cidrBlock: String_.t option ;
      ipv6CidrBlock: String_.t option ;
      outpostArn: String_.t option ;
      vpcId: VpcId.t ;
      dryRun: Boolean.t option ;
      ipv6Native: Boolean.t option }
    let context_ = "CreateSubnetRequest"
    let make ?tagSpecifications =
      fun ?availabilityZone ->
        fun ?availabilityZoneId ->
          fun ?cidrBlock ->
            fun ?ipv6CidrBlock ->
              fun ?outpostArn ->
                fun ?dryRun ->
                  fun ?ipv6Native ->
                    fun ~vpcId ->
                      fun () ->
                        {
                          tagSpecifications;
                          availabilityZone;
                          availabilityZoneId;
                          cidrBlock;
                          ipv6CidrBlock;
                          outpostArn;
                          dryRun;
                          ipv6Native;
                          vpcId
                        }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:String_.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:String_.to_value));
        ("CidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("Ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Ipv6Native", (Option.map x.ipv6Native ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv6Native =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Ipv6Native") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ipv6CidrBlock") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      let availabilityZoneId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?ipv6Native ?dryRun ~vpcId ?outpostArn ?ipv6CidrBlock ?cidrBlock
        ?availabilityZoneId ?availabilityZone ?tagSpecifications ()
    let of_json json =
      let ipv6Native = field_map json "Ipv6Native" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let outpostArn = field_map json "OutpostArn" String_.of_json in
      let ipv6CidrBlock = field_map json "Ipv6CidrBlock" String_.of_json in
      let cidrBlock = field_map json "CidrBlock" String_.of_json in
      let availabilityZoneId =
        field_map json "AvailabilityZoneId" String_.of_json in
      let availabilityZone =
        field_map json "AvailabilityZone" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      make ?ipv6Native ?dryRun ~vpcId ?outpostArn ?ipv6CidrBlock ?cidrBlock
        ?availabilityZoneId ?availabilityZone ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end
module CreateSubnetCidrReservationResult =
  struct
    type nonrec t = {
      subnetCidrReservation: SubnetCidrReservation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnetCidrReservation = fun () -> { subnetCidrReservation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnetCidrReservation",
           (Option.map x.subnetCidrReservation
              ~f:SubnetCidrReservation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnetCidrReservation =
        (Option.map ~f:SubnetCidrReservation.of_xml)
          (Xml.child xml_arg0 "subnetCidrReservation") in
      make ?subnetCidrReservation ()
    let of_json json =
      let subnetCidrReservation =
        field_map json "SubnetCidrReservation" SubnetCidrReservation.of_json in
      make ?subnetCidrReservation ()
    let to_json v = composed_to_json to_value v
  end
module CreateSubnetCidrReservationRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option ;
      subnetId: SubnetId.t ;
      cidr: String_.t ;
      reservationType: SubnetCidrReservationType.t ;
      description: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateSubnetCidrReservationRequest"
    let make ?tagSpecifications =
      fun ?description ->
        fun ?dryRun ->
          fun ~subnetId ->
            fun ~cidr ->
              fun ~reservationType ->
                fun () ->
                  {
                    tagSpecifications;
                    description;
                    dryRun;
                    subnetId;
                    cidr;
                    reservationType
                  }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("Cidr", (Some (String_.to_value x.cidr)));
        ("ReservationType",
          (Some (SubnetCidrReservationType.to_value x.reservationType)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let reservationType =
        SubnetCidrReservationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservationType") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?dryRun ?description ~reservationType ~cidr ~subnetId
        ?tagSpecifications ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let reservationType =
        field_map_exn json "ReservationType"
          SubnetCidrReservationType.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      make ?dryRun ?description ~reservationType ~cidr ~subnetId
        ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end
module CreateStoreImageTaskResult =
  struct
    type nonrec t = {
      objectKey: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?objectKey = fun () -> { objectKey }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("objectKey", (Option.map x.objectKey ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectKey =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "objectKey") in
      make ?objectKey ()
    let of_json json =
      let objectKey = field_map json "ObjectKey" String_.of_json in
      make ?objectKey ()
    let to_json v = composed_to_json to_value v
  end
module CreateStoreImageTaskRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t ;
      bucket: String_.t ;
      s3ObjectTags: S3ObjectTagList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateStoreImageTaskRequest"
    let make ?s3ObjectTags =
      fun ?dryRun ->
        fun ~imageId ->
          fun ~bucket -> fun () -> { s3ObjectTags; dryRun; imageId; bucket }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("Bucket", (Some (String_.to_value x.bucket)));
        ("S3ObjectTag",
          (Option.map x.s3ObjectTags ~f:S3ObjectTagList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let s3ObjectTags =
        (Option.map ~f:S3ObjectTagList.of_xml)
          (Xml.child xml_arg0 "S3ObjectTag") in
      let bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Bucket") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ?s3ObjectTags ~bucket ~imageId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let s3ObjectTags =
        field_map json "S3ObjectTags" S3ObjectTagList.of_json in
      let bucket = field_map_exn json "Bucket" String_.of_json in
      let imageId = field_map_exn json "ImageId" ImageId.of_json in
      make ?dryRun ?s3ObjectTags ~bucket ~imageId ()
    let to_json v = composed_to_json to_value v
  end
module CreateSpotDatafeedSubscriptionResult =
  struct
    type nonrec t =
      {
      spotDatafeedSubscription: SpotDatafeedSubscription.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotDatafeedSubscription =
      fun () -> { spotDatafeedSubscription }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotDatafeedSubscription",
           (Option.map x.spotDatafeedSubscription
              ~f:SpotDatafeedSubscription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotDatafeedSubscription =
        (Option.map ~f:SpotDatafeedSubscription.of_xml)
          (Xml.child xml_arg0 "spotDatafeedSubscription") in
      make ?spotDatafeedSubscription ()
    let of_json json =
      let spotDatafeedSubscription =
        field_map json "SpotDatafeedSubscription"
          SpotDatafeedSubscription.of_json in
      make ?spotDatafeedSubscription ()
    let to_json v = composed_to_json to_value v
  end
module CreateSpotDatafeedSubscriptionRequest =
  struct
    type nonrec t =
      {
      bucket: String_.t ;
      dryRun: Boolean.t option ;
      prefix: String_.t option }
    let context_ = "CreateSpotDatafeedSubscriptionRequest"
    let make ?dryRun =
      fun ?prefix -> fun ~bucket -> fun () -> { dryRun; prefix; bucket }
    let to_value x =
      structure_to_value
        [("bucket", (Some (String_.to_value x.bucket)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("prefix", (Option.map x.prefix ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "prefix") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "bucket") in
      make ?prefix ?dryRun ~bucket ()
    let of_json json =
      let prefix = field_map json "Prefix" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let bucket = field_map_exn json "Bucket" String_.of_json in
      make ?prefix ?dryRun ~bucket ()
    let to_json v = composed_to_json to_value v
  end
module CreateSnapshotsResult =
  struct
    type nonrec t = {
      snapshots: SnapshotSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshots = fun () -> { snapshots }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotSet", (Option.map x.snapshots ~f:SnapshotSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snapshots =
        (Option.map ~f:SnapshotSet.of_xml) (Xml.child xml_arg0 "snapshotSet") in
      make ?snapshots ()
    let of_json json =
      let snapshots = field_map json "Snapshots" SnapshotSet.of_json in
      make ?snapshots ()
    let to_json v = composed_to_json to_value v
  end
module CreateSnapshotsRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      instanceSpecification: InstanceSpecification.t ;
      outpostArn: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      copyTagsFromSource: CopyTagsFromSource.t option }
    let context_ = "CreateSnapshotsRequest"
    let make ?description =
      fun ?outpostArn ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ?copyTagsFromSource ->
              fun ~instanceSpecification ->
                fun () ->
                  {
                    description;
                    outpostArn;
                    tagSpecifications;
                    dryRun;
                    copyTagsFromSource;
                    instanceSpecification
                  }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("InstanceSpecification",
          (Some (InstanceSpecification.to_value x.instanceSpecification)));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CopyTagsFromSource",
          (Option.map x.copyTagsFromSource ~f:CopyTagsFromSource.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let copyTagsFromSource =
        (Option.map ~f:CopyTagsFromSource.of_xml)
          (Xml.child xml_arg0 "CopyTagsFromSource") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let instanceSpecification =
        InstanceSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?copyTagsFromSource ?dryRun ?tagSpecifications ?outpostArn
        ~instanceSpecification ?description ()
    let of_json json =
      let copyTagsFromSource =
        field_map json "CopyTagsFromSource" CopyTagsFromSource.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let outpostArn = field_map json "OutpostArn" String_.of_json in
      let instanceSpecification =
        field_map_exn json "InstanceSpecification"
          InstanceSpecification.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?copyTagsFromSource ?dryRun ?tagSpecifications ?outpostArn
        ~instanceSpecification ?description ()
    let to_json v = composed_to_json to_value v
  end
module CreateSnapshotRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      outpostArn: String_.t option ;
      volumeId: VolumeId.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateSnapshotRequest"
    let make ?description =
      fun ?outpostArn ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ~volumeId ->
              fun () ->
                {
                  description;
                  outpostArn;
                  tagSpecifications;
                  dryRun;
                  volumeId
                }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?dryRun ?tagSpecifications ~volumeId ?outpostArn ?description ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let outpostArn = field_map json "OutpostArn" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?dryRun ?tagSpecifications ~volumeId ?outpostArn ?description ()
    let to_json v = composed_to_json to_value v
  end
module CreateSecurityGroupResult =
  struct
    type nonrec t = {
      groupId: String_.t option ;
      tags: TagList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?groupId = fun ?tags -> fun () -> { groupId; tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("groupId", (Option.map x.groupId ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let groupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupId") in
      make ?tags ?groupId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let groupId = field_map json "GroupId" String_.of_json in
      make ?tags ?groupId ()
    let to_json v = composed_to_json to_value v
  end
module CreateSecurityGroupRequest =
  struct
    type nonrec t =
      {
      description: String_.t ;
      groupName: String_.t ;
      vpcId: VpcId.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateSecurityGroupRequest"
    let make ?vpcId =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~description ->
            fun ~groupName ->
              fun () ->
                { vpcId; tagSpecifications; dryRun; description; groupName }
    let to_value x =
      structure_to_value
        [("GroupDescription", (Some (String_.to_value x.description)));
        ("GroupName", (Some (String_.to_value x.groupName)));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let groupName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      let description =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupDescription") in
      make ?dryRun ?tagSpecifications ?vpcId ~groupName ~description ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map json "VpcId" VpcId.of_json in
      let groupName = field_map_exn json "GroupName" String_.of_json in
      let description = field_map_exn json "Description" String_.of_json in
      make ?dryRun ?tagSpecifications ?vpcId ~groupName ~description ()
    let to_json v = composed_to_json to_value v
  end
module CreateRouteTableResult =
  struct
    type nonrec t = {
      routeTable: RouteTable.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeTable = fun () -> { routeTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeTable", (Option.map x.routeTable ~f:RouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeTable =
        (Option.map ~f:RouteTable.of_xml) (Xml.child xml_arg0 "routeTable") in
      make ?routeTable ()
    let of_json json =
      let routeTable = field_map json "RouteTable" RouteTable.of_json in
      make ?routeTable ()
    let to_json v = composed_to_json to_value v
  end
module CreateRouteTableRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcId: VpcId.t ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateRouteTableRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ~vpcId -> fun () -> { dryRun; tagSpecifications; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tagSpecifications ~vpcId ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ~vpcId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateRouteResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module CreateRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t option ;
      destinationIpv6CidrBlock: String_.t option ;
      destinationPrefixListId: PrefixListResourceId.t option ;
      dryRun: Boolean.t option ;
      vpcEndpointId: VpcEndpointId.t option ;
      egressOnlyInternetGatewayId: EgressOnlyInternetGatewayId.t option ;
      gatewayId: RouteGatewayId.t option ;
      instanceId: InstanceId.t option ;
      natGatewayId: NatGatewayId.t option ;
      transitGatewayId: TransitGatewayId.t option ;
      localGatewayId: LocalGatewayId.t option ;
      carrierGatewayId: CarrierGatewayId.t option ;
      networkInterfaceId: NetworkInterfaceId.t option ;
      routeTableId: RouteTableId.t ;
      vpcPeeringConnectionId: VpcPeeringConnectionId.t option ;
      coreNetworkArn: CoreNetworkArn.t option }
    let context_ = "CreateRouteRequest"
    let make ?destinationCidrBlock =
      fun ?destinationIpv6CidrBlock ->
        fun ?destinationPrefixListId ->
          fun ?dryRun ->
            fun ?vpcEndpointId ->
              fun ?egressOnlyInternetGatewayId ->
                fun ?gatewayId ->
                  fun ?instanceId ->
                    fun ?natGatewayId ->
                      fun ?transitGatewayId ->
                        fun ?localGatewayId ->
                          fun ?carrierGatewayId ->
                            fun ?networkInterfaceId ->
                              fun ?vpcPeeringConnectionId ->
                                fun ?coreNetworkArn ->
                                  fun ~routeTableId ->
                                    fun () ->
                                      {
                                        destinationCidrBlock;
                                        destinationIpv6CidrBlock;
                                        destinationPrefixListId;
                                        dryRun;
                                        vpcEndpointId;
                                        egressOnlyInternetGatewayId;
                                        gatewayId;
                                        instanceId;
                                        natGatewayId;
                                        transitGatewayId;
                                        localGatewayId;
                                        carrierGatewayId;
                                        networkInterfaceId;
                                        vpcPeeringConnectionId;
                                        coreNetworkArn;
                                        routeTableId
                                      }
    let to_value x =
      structure_to_value
        [("destinationCidrBlock",
           (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("destinationIpv6CidrBlock",
          (Option.map x.destinationIpv6CidrBlock ~f:String_.to_value));
        ("DestinationPrefixListId",
          (Option.map x.destinationPrefixListId
             ~f:PrefixListResourceId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointId ~f:VpcEndpointId.to_value));
        ("egressOnlyInternetGatewayId",
          (Option.map x.egressOnlyInternetGatewayId
             ~f:EgressOnlyInternetGatewayId.to_value));
        ("gatewayId", (Option.map x.gatewayId ~f:RouteGatewayId.to_value));
        ("instanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("natGatewayId",
          (Option.map x.natGatewayId ~f:NatGatewayId.to_value));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("LocalGatewayId",
          (Option.map x.localGatewayId ~f:LocalGatewayId.to_value));
        ("CarrierGatewayId",
          (Option.map x.carrierGatewayId ~f:CarrierGatewayId.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("vpcPeeringConnectionId",
          (Option.map x.vpcPeeringConnectionId
             ~f:VpcPeeringConnectionId.to_value));
        ("CoreNetworkArn",
          (Option.map x.coreNetworkArn ~f:CoreNetworkArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coreNetworkArn =
        (Option.map ~f:CoreNetworkArn.of_xml)
          (Xml.child xml_arg0 "CoreNetworkArn") in
      let vpcPeeringConnectionId =
        (Option.map ~f:VpcPeeringConnectionId.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnectionId") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let carrierGatewayId =
        (Option.map ~f:CarrierGatewayId.of_xml)
          (Xml.child xml_arg0 "CarrierGatewayId") in
      let localGatewayId =
        (Option.map ~f:LocalGatewayId.of_xml)
          (Xml.child xml_arg0 "LocalGatewayId") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let natGatewayId =
        (Option.map ~f:NatGatewayId.of_xml)
          (Xml.child xml_arg0 "natGatewayId") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "instanceId") in
      let gatewayId =
        (Option.map ~f:RouteGatewayId.of_xml)
          (Xml.child xml_arg0 "gatewayId") in
      let egressOnlyInternetGatewayId =
        (Option.map ~f:EgressOnlyInternetGatewayId.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGatewayId") in
      let vpcEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      let destinationIpv6CidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationIpv6CidrBlock") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationCidrBlock") in
      make ?coreNetworkArn ?vpcPeeringConnectionId ~routeTableId
        ?networkInterfaceId ?carrierGatewayId ?localGatewayId
        ?transitGatewayId ?natGatewayId ?instanceId ?gatewayId
        ?egressOnlyInternetGatewayId ?vpcEndpointId ?dryRun
        ?destinationPrefixListId ?destinationIpv6CidrBlock
        ?destinationCidrBlock ()
    let of_json json =
      let coreNetworkArn =
        field_map json "CoreNetworkArn" CoreNetworkArn.of_json in
      let vpcPeeringConnectionId =
        field_map json "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let carrierGatewayId =
        field_map json "CarrierGatewayId" CarrierGatewayId.of_json in
      let localGatewayId =
        field_map json "LocalGatewayId" LocalGatewayId.of_json in
      let transitGatewayId =
        field_map json "TransitGatewayId" TransitGatewayId.of_json in
      let natGatewayId = field_map json "NatGatewayId" NatGatewayId.of_json in
      let instanceId = field_map json "InstanceId" InstanceId.of_json in
      let gatewayId = field_map json "GatewayId" RouteGatewayId.of_json in
      let egressOnlyInternetGatewayId =
        field_map json "EgressOnlyInternetGatewayId"
          EgressOnlyInternetGatewayId.of_json in
      let vpcEndpointId =
        field_map json "VpcEndpointId" VpcEndpointId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let destinationPrefixListId =
        field_map json "DestinationPrefixListId" PrefixListResourceId.of_json in
      let destinationIpv6CidrBlock =
        field_map json "DestinationIpv6CidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map json "DestinationCidrBlock" String_.of_json in
      make ?coreNetworkArn ?vpcPeeringConnectionId ~routeTableId
        ?networkInterfaceId ?carrierGatewayId ?localGatewayId
        ?transitGatewayId ?natGatewayId ?instanceId ?gatewayId
        ?egressOnlyInternetGatewayId ?vpcEndpointId ?dryRun
        ?destinationPrefixListId ?destinationIpv6CidrBlock
        ?destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateRestoreImageTaskResult =
  struct
    type nonrec t = {
      imageId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_json json =
      let imageId = field_map json "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end
module CreateRestoreImageTaskRequest =
  struct
    type nonrec t =
      {
      bucket: String_.t ;
      objectKey: String_.t ;
      name: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateRestoreImageTaskRequest"
    let make ?name =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~bucket ->
            fun ~objectKey ->
              fun () ->
                { name; tagSpecifications; dryRun; bucket; objectKey }
    let to_value x =
      structure_to_value
        [("Bucket", (Some (String_.to_value x.bucket)));
        ("ObjectKey", (Some (String_.to_value x.objectKey)));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let objectKey =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ObjectKey") in
      let bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Bucket") in
      make ?dryRun ?tagSpecifications ?name ~objectKey ~bucket ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let name = field_map json "Name" String_.of_json in
      let objectKey = field_map_exn json "ObjectKey" String_.of_json in
      let bucket = field_map_exn json "Bucket" String_.of_json in
      make ?dryRun ?tagSpecifications ?name ~objectKey ~bucket ()
    let to_json v = composed_to_json to_value v
  end
module CreateReservedInstancesListingResult =
  struct
    type nonrec t =
      {
      reservedInstancesListings: ReservedInstancesListingList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesListings =
      fun () -> { reservedInstancesListings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesListingsSet",
           (Option.map x.reservedInstancesListings
              ~f:ReservedInstancesListingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListings =
        (Option.map ~f:ReservedInstancesListingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingsSet") in
      make ?reservedInstancesListings ()
    let of_json json =
      let reservedInstancesListings =
        field_map json "ReservedInstancesListings"
          ReservedInstancesListingList.of_json in
      make ?reservedInstancesListings ()
    let to_json v = composed_to_json to_value v
  end
module CreateReservedInstancesListingRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t ;
      instanceCount: Integer.t ;
      priceSchedules: PriceScheduleSpecificationList.t ;
      reservedInstancesId: ReservationId.t }
    let context_ = "CreateReservedInstancesListingRequest"
    let make ~clientToken =
      fun ~instanceCount ->
        fun ~priceSchedules ->
          fun ~reservedInstancesId ->
            fun () ->
              {
                clientToken;
                instanceCount;
                priceSchedules;
                reservedInstancesId
              }
    let to_value x =
      structure_to_value
        [("clientToken", (Some (String_.to_value x.clientToken)));
        ("instanceCount", (Some (Integer.to_value x.instanceCount)));
        ("priceSchedules",
          (Some (PriceScheduleSpecificationList.to_value x.priceSchedules)));
        ("reservedInstancesId",
          (Some (ReservationId.to_value x.reservedInstancesId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesId =
        ReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reservedInstancesId") in
      let priceSchedules =
        PriceScheduleSpecificationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "priceSchedules") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceCount") in
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientToken") in
      make ~reservedInstancesId ~priceSchedules ~instanceCount ~clientToken
        ()
    let of_json json =
      let reservedInstancesId =
        field_map_exn json "ReservedInstancesId" ReservationId.of_json in
      let priceSchedules =
        field_map_exn json "PriceSchedules"
          PriceScheduleSpecificationList.of_json in
      let instanceCount = field_map_exn json "InstanceCount" Integer.of_json in
      let clientToken = field_map_exn json "ClientToken" String_.of_json in
      make ~reservedInstancesId ~priceSchedules ~instanceCount ~clientToken
        ()
    let to_json v = composed_to_json to_value v
  end
module CreateReplaceRootVolumeTaskResult =
  struct
    type nonrec t = {
      replaceRootVolumeTask: ReplaceRootVolumeTask.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?replaceRootVolumeTask = fun () -> { replaceRootVolumeTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("replaceRootVolumeTask",
           (Option.map x.replaceRootVolumeTask
              ~f:ReplaceRootVolumeTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let replaceRootVolumeTask =
        (Option.map ~f:ReplaceRootVolumeTask.of_xml)
          (Xml.child xml_arg0 "replaceRootVolumeTask") in
      make ?replaceRootVolumeTask ()
    let of_json json =
      let replaceRootVolumeTask =
        field_map json "ReplaceRootVolumeTask" ReplaceRootVolumeTask.of_json in
      make ?replaceRootVolumeTask ()
    let to_json v = composed_to_json to_value v
  end
module CreateReplaceRootVolumeTaskRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t ;
      snapshotId: SnapshotId.t option ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateReplaceRootVolumeTaskRequest"
    let make ?snapshotId =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ?tagSpecifications ->
            fun ~instanceId ->
              fun () ->
                {
                  snapshotId;
                  clientToken;
                  dryRun;
                  tagSpecifications;
                  instanceId
                }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("SnapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "SnapshotId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?tagSpecifications ?dryRun ?clientToken ?snapshotId ~instanceId ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let snapshotId = field_map json "SnapshotId" SnapshotId.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?tagSpecifications ?dryRun ?clientToken ?snapshotId ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module CreatePublicIpv4PoolResult =
  struct
    type nonrec t = {
      poolId: Ipv4PoolEc2Id.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?poolId = fun () -> { poolId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("poolId", (Option.map x.poolId ~f:Ipv4PoolEc2Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let poolId =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml) (Xml.child xml_arg0 "poolId") in
      make ?poolId ()
    let of_json json =
      let poolId = field_map json "PoolId" Ipv4PoolEc2Id.of_json in
      make ?poolId ()
    let to_json v = composed_to_json to_value v
  end
module CreatePublicIpv4PoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?dryRun =
      fun ?tagSpecifications -> fun () -> { dryRun; tagSpecifications }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreatePlacementGroupResult =
  struct
    type nonrec t = {
      placementGroup: PlacementGroup.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?placementGroup = fun () -> { placementGroup }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("placementGroup",
           (Option.map x.placementGroup ~f:PlacementGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementGroup =
        (Option.map ~f:PlacementGroup.of_xml)
          (Xml.child xml_arg0 "placementGroup") in
      make ?placementGroup ()
    let of_json json =
      let placementGroup =
        field_map json "PlacementGroup" PlacementGroup.of_json in
      make ?placementGroup ()
    let to_json v = composed_to_json to_value v
  end
module CreatePlacementGroupRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      groupName: String_.t option ;
      strategy: PlacementStrategy.t option ;
      partitionCount: Integer.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?dryRun =
      fun ?groupName ->
        fun ?strategy ->
          fun ?partitionCount ->
            fun ?tagSpecifications ->
              fun () ->
                {
                  dryRun;
                  groupName;
                  strategy;
                  partitionCount;
                  tagSpecifications
                }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupName", (Option.map x.groupName ~f:String_.to_value));
        ("strategy", (Option.map x.strategy ~f:PlacementStrategy.to_value));
        ("PartitionCount", (Option.map x.partitionCount ~f:Integer.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let partitionCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PartitionCount") in
      let strategy =
        (Option.map ~f:PlacementStrategy.of_xml)
          (Xml.child xml_arg0 "strategy") in
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tagSpecifications ?partitionCount ?strategy ?groupName ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let partitionCount = field_map json "PartitionCount" Integer.of_json in
      let strategy = field_map json "Strategy" PlacementStrategy.of_json in
      let groupName = field_map json "GroupName" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?partitionCount ?strategy ?groupName ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInterfaceResult =
  struct
    type nonrec t =
      {
      networkInterface: NetworkInterface.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterface =
      fun ?clientToken -> fun () -> { networkInterface; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterface",
           (Option.map x.networkInterface ~f:NetworkInterface.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let networkInterface =
        (Option.map ~f:NetworkInterface.of_xml)
          (Xml.child xml_arg0 "networkInterface") in
      make ?clientToken ?networkInterface ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let networkInterface =
        field_map json "NetworkInterface" NetworkInterface.of_json in
      make ?clientToken ?networkInterface ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      dryRun: Boolean.t option ;
      groups: SecurityGroupIdStringList.t option ;
      ipv6AddressCount: Integer.t option ;
      ipv6Addresses: InstanceIpv6AddressList.t option ;
      privateIpAddress: String_.t option ;
      privateIpAddresses: PrivateIpAddressSpecificationList.t option ;
      secondaryPrivateIpAddressCount: Integer.t option ;
      ipv4Prefixes: Ipv4PrefixList.t option ;
      ipv4PrefixCount: Integer.t option ;
      ipv6Prefixes: Ipv6PrefixList.t option ;
      ipv6PrefixCount: Integer.t option ;
      interfaceType: NetworkInterfaceCreationType.t option ;
      subnetId: SubnetId.t ;
      tagSpecifications: TagSpecificationList.t option ;
      clientToken: String_.t option }
    let context_ = "CreateNetworkInterfaceRequest"
    let make ?description =
      fun ?dryRun ->
        fun ?groups ->
          fun ?ipv6AddressCount ->
            fun ?ipv6Addresses ->
              fun ?privateIpAddress ->
                fun ?privateIpAddresses ->
                  fun ?secondaryPrivateIpAddressCount ->
                    fun ?ipv4Prefixes ->
                      fun ?ipv4PrefixCount ->
                        fun ?ipv6Prefixes ->
                          fun ?ipv6PrefixCount ->
                            fun ?interfaceType ->
                              fun ?tagSpecifications ->
                                fun ?clientToken ->
                                  fun ~subnetId ->
                                    fun () ->
                                      {
                                        description;
                                        dryRun;
                                        groups;
                                        ipv6AddressCount;
                                        ipv6Addresses;
                                        privateIpAddress;
                                        privateIpAddresses;
                                        secondaryPrivateIpAddressCount;
                                        ipv4Prefixes;
                                        ipv4PrefixCount;
                                        ipv6Prefixes;
                                        ipv6PrefixCount;
                                        interfaceType;
                                        tagSpecifications;
                                        clientToken;
                                        subnetId
                                      }
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecurityGroupId",
          (Option.map x.groups ~f:SecurityGroupIdStringList.to_value));
        ("ipv6AddressCount",
          (Option.map x.ipv6AddressCount ~f:Integer.to_value));
        ("ipv6Addresses",
          (Option.map x.ipv6Addresses ~f:InstanceIpv6AddressList.to_value));
        ("privateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("privateIpAddresses",
          (Option.map x.privateIpAddresses
             ~f:PrivateIpAddressSpecificationList.to_value));
        ("secondaryPrivateIpAddressCount",
          (Option.map x.secondaryPrivateIpAddressCount ~f:Integer.to_value));
        ("Ipv4Prefix",
          (Option.map x.ipv4Prefixes ~f:Ipv4PrefixList.to_value));
        ("Ipv4PrefixCount",
          (Option.map x.ipv4PrefixCount ~f:Integer.to_value));
        ("Ipv6Prefix",
          (Option.map x.ipv6Prefixes ~f:Ipv6PrefixList.to_value));
        ("Ipv6PrefixCount",
          (Option.map x.ipv6PrefixCount ~f:Integer.to_value));
        ("InterfaceType",
          (Option.map x.interfaceType
             ~f:NetworkInterfaceCreationType.to_value));
        ("subnetId", (Some (SubnetId.to_value x.subnetId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subnetId") in
      let interfaceType =
        (Option.map ~f:NetworkInterfaceCreationType.of_xml)
          (Xml.child xml_arg0 "InterfaceType") in
      let ipv6PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv6PrefixCount") in
      let ipv6Prefixes =
        (Option.map ~f:Ipv6PrefixList.of_xml)
          (Xml.child xml_arg0 "Ipv6Prefix") in
      let ipv4PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv4PrefixCount") in
      let ipv4Prefixes =
        (Option.map ~f:Ipv4PrefixList.of_xml)
          (Xml.child xml_arg0 "Ipv4Prefix") in
      let secondaryPrivateIpAddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "secondaryPrivateIpAddressCount") in
      let privateIpAddresses =
        (Option.map ~f:PrivateIpAddressSpecificationList.of_xml)
          (Xml.child xml_arg0 "privateIpAddresses") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let ipv6Addresses =
        (Option.map ~f:InstanceIpv6AddressList.of_xml)
          (Xml.child xml_arg0 "ipv6Addresses") in
      let ipv6AddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ipv6AddressCount") in
      let groups =
        (Option.map ~f:SecurityGroupIdStringList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ?clientToken ?tagSpecifications ~subnetId ?interfaceType
        ?ipv6PrefixCount ?ipv6Prefixes ?ipv4PrefixCount ?ipv4Prefixes
        ?secondaryPrivateIpAddressCount ?privateIpAddresses ?privateIpAddress
        ?ipv6Addresses ?ipv6AddressCount ?groups ?dryRun ?description ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let interfaceType =
        field_map json "InterfaceType" NetworkInterfaceCreationType.of_json in
      let ipv6PrefixCount = field_map json "Ipv6PrefixCount" Integer.of_json in
      let ipv6Prefixes = field_map json "Ipv6Prefixes" Ipv6PrefixList.of_json in
      let ipv4PrefixCount = field_map json "Ipv4PrefixCount" Integer.of_json in
      let ipv4Prefixes = field_map json "Ipv4Prefixes" Ipv4PrefixList.of_json in
      let secondaryPrivateIpAddressCount =
        field_map json "SecondaryPrivateIpAddressCount" Integer.of_json in
      let privateIpAddresses =
        field_map json "PrivateIpAddresses"
          PrivateIpAddressSpecificationList.of_json in
      let privateIpAddress =
        field_map json "PrivateIpAddress" String_.of_json in
      let ipv6Addresses =
        field_map json "Ipv6Addresses" InstanceIpv6AddressList.of_json in
      let ipv6AddressCount =
        field_map json "Ipv6AddressCount" Integer.of_json in
      let groups = field_map json "Groups" SecurityGroupIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?clientToken ?tagSpecifications ~subnetId ?interfaceType
        ?ipv6PrefixCount ?ipv6Prefixes ?ipv4PrefixCount ?ipv4Prefixes
        ?secondaryPrivateIpAddressCount ?privateIpAddresses ?privateIpAddress
        ?ipv6Addresses ?ipv6AddressCount ?groups ?dryRun ?description ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInterfacePermissionResult =
  struct
    type nonrec t =
      {
      interfacePermission: NetworkInterfacePermission.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?interfacePermission = fun () -> { interfacePermission }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("interfacePermission",
           (Option.map x.interfacePermission
              ~f:NetworkInterfacePermission.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let interfacePermission =
        (Option.map ~f:NetworkInterfacePermission.of_xml)
          (Xml.child xml_arg0 "interfacePermission") in
      make ?interfacePermission ()
    let of_json json =
      let interfacePermission =
        field_map json "InterfacePermission"
          NetworkInterfacePermission.of_json in
      make ?interfacePermission ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInterfacePermissionRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t ;
      awsAccountId: String_.t option ;
      awsService: String_.t option ;
      permission: InterfacePermissionType.t ;
      dryRun: Boolean.t option }
    let context_ = "CreateNetworkInterfacePermissionRequest"
    let make ?awsAccountId =
      fun ?awsService ->
        fun ?dryRun ->
          fun ~networkInterfaceId ->
            fun ~permission ->
              fun () ->
                {
                  awsAccountId;
                  awsService;
                  dryRun;
                  networkInterfaceId;
                  permission
                }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("AwsAccountId", (Option.map x.awsAccountId ~f:String_.to_value));
        ("AwsService", (Option.map x.awsService ~f:String_.to_value));
        ("Permission",
          (Some (InterfacePermissionType.to_value x.permission)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let permission =
        InterfacePermissionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Permission") in
      let awsService =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AwsService") in
      let awsAccountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AwsAccountId") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInterfaceId") in
      make ?dryRun ~permission ?awsService ?awsAccountId ~networkInterfaceId
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let permission =
        field_map_exn json "Permission" InterfacePermissionType.of_json in
      let awsService = field_map json "AwsService" String_.of_json in
      let awsAccountId = field_map json "AwsAccountId" String_.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?dryRun ~permission ?awsService ?awsAccountId ~networkInterfaceId
        ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInsightsPathResult =
  struct
    type nonrec t = {
      networkInsightsPath: NetworkInsightsPath.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsPath = fun () -> { networkInsightsPath }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsPath",
           (Option.map x.networkInsightsPath ~f:NetworkInsightsPath.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsPath =
        (Option.map ~f:NetworkInsightsPath.of_xml)
          (Xml.child xml_arg0 "networkInsightsPath") in
      make ?networkInsightsPath ()
    let of_json json =
      let networkInsightsPath =
        field_map json "NetworkInsightsPath" NetworkInsightsPath.of_json in
      make ?networkInsightsPath ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInsightsPathRequest =
  struct
    type nonrec t =
      {
      sourceIp: IpAddress.t option ;
      destinationIp: IpAddress.t option ;
      source: NetworkInsightsResourceId.t ;
      destination: NetworkInsightsResourceId.t ;
      protocol: Protocol.t ;
      destinationPort: Port.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t }
    let context_ = "CreateNetworkInsightsPathRequest"
    let make ?sourceIp =
      fun ?destinationIp ->
        fun ?destinationPort ->
          fun ?tagSpecifications ->
            fun ?dryRun ->
              fun ~source ->
                fun ~destination ->
                  fun ~protocol ->
                    fun ~clientToken ->
                      fun () ->
                        {
                          sourceIp;
                          destinationIp;
                          destinationPort;
                          tagSpecifications;
                          dryRun;
                          source;
                          destination;
                          protocol;
                          clientToken
                        }
    let to_value x =
      structure_to_value
        [("SourceIp", (Option.map x.sourceIp ~f:IpAddress.to_value));
        ("DestinationIp", (Option.map x.destinationIp ~f:IpAddress.to_value));
        ("Source", (Some (NetworkInsightsResourceId.to_value x.source)));
        ("Destination",
          (Some (NetworkInsightsResourceId.to_value x.destination)));
        ("Protocol", (Some (Protocol.to_value x.protocol)));
        ("DestinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Some (String_.to_value x.clientToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "DestinationPort") in
      let protocol =
        Protocol.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Protocol") in
      let destination =
        NetworkInsightsResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destination") in
      let source =
        NetworkInsightsResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Source") in
      let destinationIp =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "DestinationIp") in
      let sourceIp =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "SourceIp") in
      make ~clientToken ?dryRun ?tagSpecifications ?destinationPort ~protocol
        ~destination ~source ?destinationIp ?sourceIp ()
    let of_json json =
      let clientToken = field_map_exn json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let destinationPort = field_map json "DestinationPort" Port.of_json in
      let protocol = field_map_exn json "Protocol" Protocol.of_json in
      let destination =
        field_map_exn json "Destination" NetworkInsightsResourceId.of_json in
      let source =
        field_map_exn json "Source" NetworkInsightsResourceId.of_json in
      let destinationIp = field_map json "DestinationIp" IpAddress.of_json in
      let sourceIp = field_map json "SourceIp" IpAddress.of_json in
      make ~clientToken ?dryRun ?tagSpecifications ?destinationPort ~protocol
        ~destination ~source ?destinationIp ?sourceIp ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInsightsAccessScopeResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScope: NetworkInsightsAccessScope.t option ;
      networkInsightsAccessScopeContent:
        NetworkInsightsAccessScopeContent.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScope =
      fun ?networkInsightsAccessScopeContent ->
        fun () ->
          { networkInsightsAccessScope; networkInsightsAccessScopeContent }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScope",
           (Option.map x.networkInsightsAccessScope
              ~f:NetworkInsightsAccessScope.to_value));
        ("networkInsightsAccessScopeContent",
          (Option.map x.networkInsightsAccessScopeContent
             ~f:NetworkInsightsAccessScopeContent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeContent =
        (Option.map ~f:NetworkInsightsAccessScopeContent.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeContent") in
      let networkInsightsAccessScope =
        (Option.map ~f:NetworkInsightsAccessScope.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScope") in
      make ?networkInsightsAccessScopeContent ?networkInsightsAccessScope ()
    let of_json json =
      let networkInsightsAccessScopeContent =
        field_map json "NetworkInsightsAccessScopeContent"
          NetworkInsightsAccessScopeContent.of_json in
      let networkInsightsAccessScope =
        field_map json "NetworkInsightsAccessScope"
          NetworkInsightsAccessScope.of_json in
      make ?networkInsightsAccessScopeContent ?networkInsightsAccessScope ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkInsightsAccessScopeRequest =
  struct
    type nonrec t =
      {
      matchPaths: AccessScopePathListRequest.t option ;
      excludePaths: AccessScopePathListRequest.t option ;
      clientToken: String_.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateNetworkInsightsAccessScopeRequest"
    let make ?matchPaths =
      fun ?excludePaths ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ~clientToken ->
              fun () ->
                {
                  matchPaths;
                  excludePaths;
                  tagSpecifications;
                  dryRun;
                  clientToken
                }
    let to_value x =
      structure_to_value
        [("MatchPath",
           (Option.map x.matchPaths ~f:AccessScopePathListRequest.to_value));
        ("ExcludePath",
          (Option.map x.excludePaths ~f:AccessScopePathListRequest.to_value));
        ("ClientToken", (Some (String_.to_value x.clientToken)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientToken") in
      let excludePaths =
        (Option.map ~f:AccessScopePathListRequest.of_xml)
          (Xml.child xml_arg0 "ExcludePath") in
      let matchPaths =
        (Option.map ~f:AccessScopePathListRequest.of_xml)
          (Xml.child xml_arg0 "MatchPath") in
      make ?dryRun ?tagSpecifications ~clientToken ?excludePaths ?matchPaths
        ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map_exn json "ClientToken" String_.of_json in
      let excludePaths =
        field_map json "ExcludePaths" AccessScopePathListRequest.of_json in
      let matchPaths =
        field_map json "MatchPaths" AccessScopePathListRequest.of_json in
      make ?dryRun ?tagSpecifications ~clientToken ?excludePaths ?matchPaths
        ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkAclResult =
  struct
    type nonrec t = {
      networkAcl: NetworkAcl.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkAcl = fun () -> { networkAcl }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkAcl", (Option.map x.networkAcl ~f:NetworkAcl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkAcl =
        (Option.map ~f:NetworkAcl.of_xml) (Xml.child xml_arg0 "networkAcl") in
      make ?networkAcl ()
    let of_json json =
      let networkAcl = field_map json "NetworkAcl" NetworkAcl.of_json in
      make ?networkAcl ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkAclRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcId: VpcId.t ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateNetworkAclRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ~vpcId -> fun () -> { dryRun; tagSpecifications; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?tagSpecifications ~vpcId ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ~vpcId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateNetworkAclEntryRequest =
  struct
    type nonrec t =
      {
      cidrBlock: String_.t option ;
      dryRun: Boolean.t option ;
      egress: Boolean.t ;
      icmpTypeCode: IcmpTypeCode.t option ;
      ipv6CidrBlock: String_.t option ;
      networkAclId: NetworkAclId.t ;
      portRange: PortRange.t option ;
      protocol: String_.t ;
      ruleAction: RuleAction.t ;
      ruleNumber: Integer.t }
    let context_ = "CreateNetworkAclEntryRequest"
    let make ?cidrBlock =
      fun ?dryRun ->
        fun ?icmpTypeCode ->
          fun ?ipv6CidrBlock ->
            fun ?portRange ->
              fun ~egress ->
                fun ~networkAclId ->
                  fun ~protocol ->
                    fun ~ruleAction ->
                      fun ~ruleNumber ->
                        fun () ->
                          {
                            cidrBlock;
                            dryRun;
                            icmpTypeCode;
                            ipv6CidrBlock;
                            portRange;
                            egress;
                            networkAclId;
                            protocol;
                            ruleAction;
                            ruleNumber
                          }
    let to_value x =
      structure_to_value
        [("cidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("egress", (Some (Boolean.to_value x.egress)));
        ("Icmp", (Option.map x.icmpTypeCode ~f:IcmpTypeCode.to_value));
        ("ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)));
        ("portRange", (Option.map x.portRange ~f:PortRange.to_value));
        ("protocol", (Some (String_.to_value x.protocol)));
        ("ruleAction", (Some (RuleAction.to_value x.ruleAction)));
        ("ruleNumber", (Some (Integer.to_value x.ruleNumber)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleNumber") in
      let ruleAction =
        RuleAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleAction") in
      let protocol =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "protocol") in
      let portRange =
        (Option.map ~f:PortRange.of_xml) (Xml.child xml_arg0 "portRange") in
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipv6CidrBlock") in
      let icmpTypeCode =
        (Option.map ~f:IcmpTypeCode.of_xml) (Xml.child xml_arg0 "Icmp") in
      let egress =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "egress") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cidrBlock") in
      make ~ruleNumber ~ruleAction ~protocol ?portRange ~networkAclId
        ?ipv6CidrBlock ?icmpTypeCode ~egress ?dryRun ?cidrBlock ()
    let of_json json =
      let ruleNumber = field_map_exn json "RuleNumber" Integer.of_json in
      let ruleAction = field_map_exn json "RuleAction" RuleAction.of_json in
      let protocol = field_map_exn json "Protocol" String_.of_json in
      let portRange = field_map json "PortRange" PortRange.of_json in
      let networkAclId =
        field_map_exn json "NetworkAclId" NetworkAclId.of_json in
      let ipv6CidrBlock = field_map json "Ipv6CidrBlock" String_.of_json in
      let icmpTypeCode = field_map json "IcmpTypeCode" IcmpTypeCode.of_json in
      let egress = field_map_exn json "Egress" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let cidrBlock = field_map json "CidrBlock" String_.of_json in
      make ~ruleNumber ~ruleAction ~protocol ?portRange ~networkAclId
        ?ipv6CidrBlock ?icmpTypeCode ~egress ?dryRun ?cidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateNatGatewayResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option ;
      natGateway: NatGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?natGateway -> fun () -> { clientToken; natGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("natGateway", (Option.map x.natGateway ~f:NatGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGateway =
        (Option.map ~f:NatGateway.of_xml) (Xml.child xml_arg0 "natGateway") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?natGateway ?clientToken ()
    let of_json json =
      let natGateway = field_map json "NatGateway" NatGateway.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?natGateway ?clientToken ()
    let to_json v = composed_to_json to_value v
  end
module CreateNatGatewayRequest =
  struct
    type nonrec t =
      {
      allocationId: AllocationId.t option ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option ;
      subnetId: SubnetId.t ;
      tagSpecifications: TagSpecificationList.t option ;
      connectivityType: ConnectivityType.t option }
    let context_ = "CreateNatGatewayRequest"
    let make ?allocationId =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ?tagSpecifications ->
            fun ?connectivityType ->
              fun ~subnetId ->
                fun () ->
                  {
                    allocationId;
                    clientToken;
                    dryRun;
                    tagSpecifications;
                    connectivityType;
                    subnetId
                  }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationId ~f:AllocationId.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ConnectivityType",
          (Option.map x.connectivityType ~f:ConnectivityType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectivityType =
        (Option.map ~f:ConnectivityType.of_xml)
          (Xml.child xml_arg0 "ConnectivityType") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let allocationId =
        (Option.map ~f:AllocationId.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?connectivityType ?tagSpecifications ~subnetId ?dryRun
        ?clientToken ?allocationId ()
    let of_json json =
      let connectivityType =
        field_map json "ConnectivityType" ConnectivityType.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let allocationId = field_map json "AllocationId" AllocationId.of_json in
      make ?connectivityType ?tagSpecifications ~subnetId ?dryRun
        ?clientToken ?allocationId ()
    let to_json v = composed_to_json to_value v
  end
module CreateManagedPrefixListResult =
  struct
    type nonrec t = {
      prefixList: ManagedPrefixList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixList = fun () -> { prefixList }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixList",
           (Option.map x.prefixList ~f:ManagedPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixList =
        (Option.map ~f:ManagedPrefixList.of_xml)
          (Xml.child xml_arg0 "prefixList") in
      make ?prefixList ()
    let of_json json =
      let prefixList = field_map json "PrefixList" ManagedPrefixList.of_json in
      make ?prefixList ()
    let to_json v = composed_to_json to_value v
  end
module CreateManagedPrefixListRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      prefixListName: String_.t ;
      entries: AddPrefixListEntries.t option ;
      maxEntries: Integer.t ;
      tagSpecifications: TagSpecificationList.t option ;
      addressFamily: String_.t ;
      clientToken: String_.t option }
    let context_ = "CreateManagedPrefixListRequest"
    let make ?dryRun =
      fun ?entries ->
        fun ?tagSpecifications ->
          fun ?clientToken ->
            fun ~prefixListName ->
              fun ~maxEntries ->
                fun ~addressFamily ->
                  fun () ->
                    {
                      dryRun;
                      entries;
                      tagSpecifications;
                      clientToken;
                      prefixListName;
                      maxEntries;
                      addressFamily
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListName", (Some (String_.to_value x.prefixListName)));
        ("Entry", (Option.map x.entries ~f:AddPrefixListEntries.to_value));
        ("MaxEntries", (Some (Integer.to_value x.maxEntries)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("AddressFamily", (Some (String_.to_value x.addressFamily)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let addressFamily =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AddressFamily") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let maxEntries =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxEntries") in
      let entries =
        (Option.map ~f:AddPrefixListEntries.of_xml)
          (Xml.child xml_arg0 "Entry") in
      let prefixListName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ~addressFamily ?tagSpecifications ~maxEntries
        ?entries ~prefixListName ?dryRun ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let addressFamily = field_map_exn json "AddressFamily" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let maxEntries = field_map_exn json "MaxEntries" Integer.of_json in
      let entries = field_map json "Entries" AddPrefixListEntries.of_json in
      let prefixListName =
        field_map_exn json "PrefixListName" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?clientToken ~addressFamily ?tagSpecifications ~maxEntries
        ?entries ~prefixListName ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateLocalGatewayRouteTableVpcAssociationResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociation:
        LocalGatewayRouteTableVpcAssociation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVpcAssociation =
      fun () -> { localGatewayRouteTableVpcAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVpcAssociation",
           (Option.map x.localGatewayRouteTableVpcAssociation
              ~f:LocalGatewayRouteTableVpcAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableVpcAssociation =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociation.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableVpcAssociation") in
      make ?localGatewayRouteTableVpcAssociation ()
    let of_json json =
      let localGatewayRouteTableVpcAssociation =
        field_map json "LocalGatewayRouteTableVpcAssociation"
          LocalGatewayRouteTableVpcAssociation.of_json in
      make ?localGatewayRouteTableVpcAssociation ()
    let to_json v = composed_to_json to_value v
  end
module CreateLocalGatewayRouteTableVpcAssociationRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableId: LocalGatewayRoutetableId.t ;
      vpcId: VpcId.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateLocalGatewayRouteTableVpcAssociationRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~localGatewayRouteTableId ->
          fun ~vpcId ->
            fun () ->
              { tagSpecifications; dryRun; localGatewayRouteTableId; vpcId }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Some
              (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      make ?dryRun ?tagSpecifications ~vpcId ~localGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let localGatewayRouteTableId =
        field_map_exn json "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      make ?dryRun ?tagSpecifications ~vpcId ~localGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module CreateLocalGatewayRouteResult =
  struct
    type nonrec t = {
      route: LocalGatewayRoute.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:LocalGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:LocalGatewayRoute.of_xml) (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_json json =
      let route = field_map json "Route" LocalGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end
module CreateLocalGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t ;
      localGatewayRouteTableId: LocalGatewayRoutetableId.t ;
      localGatewayVirtualInterfaceGroupId:
        LocalGatewayVirtualInterfaceGroupId.t ;
      dryRun: Boolean.t option }
    let context_ = "CreateLocalGatewayRouteRequest"
    let make ?dryRun =
      fun ~destinationCidrBlock ->
        fun ~localGatewayRouteTableId ->
          fun ~localGatewayVirtualInterfaceGroupId ->
            fun () ->
              {
                dryRun;
                destinationCidrBlock;
                localGatewayRouteTableId;
                localGatewayVirtualInterfaceGroupId
              }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("LocalGatewayRouteTableId",
          (Some
             (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("LocalGatewayVirtualInterfaceGroupId",
          (Some
             (LocalGatewayVirtualInterfaceGroupId.to_value
                x.localGatewayVirtualInterfaceGroupId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayVirtualInterfaceGroupId =
        LocalGatewayVirtualInterfaceGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayVirtualInterfaceGroupId") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ?dryRun ~localGatewayVirtualInterfaceGroupId
        ~localGatewayRouteTableId ~destinationCidrBlock ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let localGatewayVirtualInterfaceGroupId =
        field_map_exn json "LocalGatewayVirtualInterfaceGroupId"
          LocalGatewayVirtualInterfaceGroupId.of_json in
      let localGatewayRouteTableId =
        field_map_exn json "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      make ?dryRun ~localGatewayVirtualInterfaceGroupId
        ~localGatewayRouteTableId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateLaunchTemplateVersionResult =
  struct
    type nonrec t =
      {
      launchTemplateVersion: LaunchTemplateVersion.t option ;
      warning: ValidationWarning.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplateVersion =
      fun ?warning -> fun () -> { launchTemplateVersion; warning }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplateVersion",
           (Option.map x.launchTemplateVersion
              ~f:LaunchTemplateVersion.to_value));
        ("warning", (Option.map x.warning ~f:ValidationWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let warning =
        (Option.map ~f:ValidationWarning.of_xml)
          (Xml.child xml_arg0 "warning") in
      let launchTemplateVersion =
        (Option.map ~f:LaunchTemplateVersion.of_xml)
          (Xml.child xml_arg0 "launchTemplateVersion") in
      make ?warning ?launchTemplateVersion ()
    let of_json json =
      let warning = field_map json "Warning" ValidationWarning.of_json in
      let launchTemplateVersion =
        field_map json "LaunchTemplateVersion" LaunchTemplateVersion.of_json in
      make ?warning ?launchTemplateVersion ()
    let to_json v = composed_to_json to_value v
  end
module CreateLaunchTemplateVersionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      clientToken: String_.t option ;
      launchTemplateId: LaunchTemplateId.t option ;
      launchTemplateName: LaunchTemplateName.t option ;
      sourceVersion: String_.t option ;
      versionDescription: VersionDescription.t option ;
      launchTemplateData: RequestLaunchTemplateData.t }
    let context_ = "CreateLaunchTemplateVersionRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?launchTemplateId ->
          fun ?launchTemplateName ->
            fun ?sourceVersion ->
              fun ?versionDescription ->
                fun ~launchTemplateData ->
                  fun () ->
                    {
                      dryRun;
                      clientToken;
                      launchTemplateId;
                      launchTemplateName;
                      sourceVersion;
                      versionDescription;
                      launchTemplateData
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("SourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:VersionDescription.to_value));
        ("LaunchTemplateData",
          (Some (RequestLaunchTemplateData.to_value x.launchTemplateData)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchTemplateData =
        RequestLaunchTemplateData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateData") in
      let versionDescription =
        (Option.map ~f:VersionDescription.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SourceVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~launchTemplateData ?versionDescription ?sourceVersion
        ?launchTemplateName ?launchTemplateId ?clientToken ?dryRun ()
    let of_json json =
      let launchTemplateData =
        field_map_exn json "LaunchTemplateData"
          RequestLaunchTemplateData.of_json in
      let versionDescription =
        field_map json "VersionDescription" VersionDescription.of_json in
      let sourceVersion = field_map json "SourceVersion" String_.of_json in
      let launchTemplateName =
        field_map json "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json "LaunchTemplateId" LaunchTemplateId.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~launchTemplateData ?versionDescription ?sourceVersion
        ?launchTemplateName ?launchTemplateId ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateLaunchTemplateResult =
  struct
    type nonrec t =
      {
      launchTemplate: LaunchTemplate.t option ;
      warning: ValidationWarning.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplate =
      fun ?warning -> fun () -> { launchTemplate; warning }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplate",
           (Option.map x.launchTemplate ~f:LaunchTemplate.to_value));
        ("warning", (Option.map x.warning ~f:ValidationWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let warning =
        (Option.map ~f:ValidationWarning.of_xml)
          (Xml.child xml_arg0 "warning") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplate.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      make ?warning ?launchTemplate ()
    let of_json json =
      let warning = field_map json "Warning" ValidationWarning.of_json in
      let launchTemplate =
        field_map json "LaunchTemplate" LaunchTemplate.of_json in
      make ?warning ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end
module CreateLaunchTemplateRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      clientToken: String_.t option ;
      launchTemplateName: LaunchTemplateName.t ;
      versionDescription: VersionDescription.t option ;
      launchTemplateData: RequestLaunchTemplateData.t ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateLaunchTemplateRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?versionDescription ->
          fun ?tagSpecifications ->
            fun ~launchTemplateName ->
              fun ~launchTemplateData ->
                fun () ->
                  {
                    dryRun;
                    clientToken;
                    versionDescription;
                    tagSpecifications;
                    launchTemplateName;
                    launchTemplateData
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("LaunchTemplateName",
          (Some (LaunchTemplateName.to_value x.launchTemplateName)));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:VersionDescription.to_value));
        ("LaunchTemplateData",
          (Some (RequestLaunchTemplateData.to_value x.launchTemplateData)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let launchTemplateData =
        RequestLaunchTemplateData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateData") in
      let versionDescription =
        (Option.map ~f:VersionDescription.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let launchTemplateName =
        LaunchTemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateName") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ~launchTemplateData ?versionDescription
        ~launchTemplateName ?clientToken ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let launchTemplateData =
        field_map_exn json "LaunchTemplateData"
          RequestLaunchTemplateData.of_json in
      let versionDescription =
        field_map json "VersionDescription" VersionDescription.of_json in
      let launchTemplateName =
        field_map_exn json "LaunchTemplateName" LaunchTemplateName.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ~launchTemplateData ?versionDescription
        ~launchTemplateName ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateKeyPairRequest =
  struct
    type nonrec t =
      {
      keyName: String_.t ;
      dryRun: Boolean.t option ;
      keyType: KeyType.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateKeyPairRequest"
    let make ?dryRun =
      fun ?keyType ->
        fun ?tagSpecifications ->
          fun ~keyName ->
            fun () -> { dryRun; keyType; tagSpecifications; keyName }
    let to_value x =
      structure_to_value
        [("KeyName", (Some (String_.to_value x.keyName)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("KeyType", (Option.map x.keyType ~f:KeyType.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let keyType =
        (Option.map ~f:KeyType.of_xml) (Xml.child xml_arg0 "KeyType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let keyName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeyName") in
      make ?tagSpecifications ?keyType ?dryRun ~keyName ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let keyType = field_map json "KeyType" KeyType.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let keyName = field_map_exn json "KeyName" String_.of_json in
      make ?tagSpecifications ?keyType ?dryRun ~keyName ()
    let to_json v = composed_to_json to_value v
  end
module CreateIpamScopeResult =
  struct
    type nonrec t = {
      ipamScope: IpamScope.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamScope = fun () -> { ipamScope }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamScope", (Option.map x.ipamScope ~f:IpamScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScope =
        (Option.map ~f:IpamScope.of_xml) (Xml.child xml_arg0 "ipamScope") in
      make ?ipamScope ()
    let of_json json =
      let ipamScope = field_map json "IpamScope" IpamScope.of_json in
      make ?ipamScope ()
    let to_json v = composed_to_json to_value v
  end
module CreateIpamScopeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      ipamId: IpamId.t ;
      description: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      clientToken: String_.t option }
    let context_ = "CreateIpamScopeRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?tagSpecifications ->
          fun ?clientToken ->
            fun ~ipamId ->
              fun () ->
                { dryRun; description; tagSpecifications; clientToken; ipamId
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ?description ~ipamId ?dryRun ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json "Description" String_.of_json in
      let ipamId = field_map_exn json "IpamId" IpamId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ?description ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateIpamResult =
  struct
    type nonrec t = {
      ipam: Ipam.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipam = fun () -> { ipam }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("ipam", (Option.map x.ipam ~f:Ipam.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipam = (Option.map ~f:Ipam.of_xml) (Xml.child xml_arg0 "ipam") in
      make ?ipam ()
    let of_json json =
      let ipam = field_map json "Ipam" Ipam.of_json in make ?ipam ()
    let to_json v = composed_to_json to_value v
  end
module CreateIpamRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      description: String_.t option ;
      operatingRegions: AddIpamOperatingRegionSet.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      clientToken: String_.t option }
    let make ?dryRun =
      fun ?description ->
        fun ?operatingRegions ->
          fun ?tagSpecifications ->
            fun ?clientToken ->
              fun () ->
                {
                  dryRun;
                  description;
                  operatingRegions;
                  tagSpecifications;
                  clientToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("OperatingRegion",
          (Option.map x.operatingRegions
             ~f:AddIpamOperatingRegionSet.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let operatingRegions =
        (Option.map ~f:AddIpamOperatingRegionSet.of_xml)
          (Xml.child xml_arg0 "OperatingRegion") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ?operatingRegions ?description
        ?dryRun ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let operatingRegions =
        field_map json "OperatingRegions" AddIpamOperatingRegionSet.of_json in
      let description = field_map json "Description" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ?operatingRegions ?description
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateIpamPoolResult =
  struct
    type nonrec t = {
      ipamPool: IpamPool.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPool = fun () -> { ipamPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPool", (Option.map x.ipamPool ~f:IpamPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPool =
        (Option.map ~f:IpamPool.of_xml) (Xml.child xml_arg0 "ipamPool") in
      make ?ipamPool ()
    let of_json json =
      let ipamPool = field_map json "IpamPool" IpamPool.of_json in
      make ?ipamPool ()
    let to_json v = composed_to_json to_value v
  end
module CreateIpamPoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      ipamScopeId: IpamScopeId.t ;
      locale: String_.t option ;
      sourceIpamPoolId: IpamPoolId.t option ;
      description: String_.t option ;
      addressFamily: AddressFamily.t ;
      autoImport: Boolean.t option ;
      publiclyAdvertisable: Boolean.t option ;
      allocationMinNetmaskLength: IpamNetmaskLength.t option ;
      allocationMaxNetmaskLength: IpamNetmaskLength.t option ;
      allocationDefaultNetmaskLength: IpamNetmaskLength.t option ;
      allocationResourceTags: RequestIpamResourceTagList.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      clientToken: String_.t option ;
      awsService: IpamPoolAwsService.t option }
    let context_ = "CreateIpamPoolRequest"
    let make ?dryRun =
      fun ?locale ->
        fun ?sourceIpamPoolId ->
          fun ?description ->
            fun ?autoImport ->
              fun ?publiclyAdvertisable ->
                fun ?allocationMinNetmaskLength ->
                  fun ?allocationMaxNetmaskLength ->
                    fun ?allocationDefaultNetmaskLength ->
                      fun ?allocationResourceTags ->
                        fun ?tagSpecifications ->
                          fun ?clientToken ->
                            fun ?awsService ->
                              fun ~ipamScopeId ->
                                fun ~addressFamily ->
                                  fun () ->
                                    {
                                      dryRun;
                                      locale;
                                      sourceIpamPoolId;
                                      description;
                                      autoImport;
                                      publiclyAdvertisable;
                                      allocationMinNetmaskLength;
                                      allocationMaxNetmaskLength;
                                      allocationDefaultNetmaskLength;
                                      allocationResourceTags;
                                      tagSpecifications;
                                      clientToken;
                                      awsService;
                                      ipamScopeId;
                                      addressFamily
                                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)));
        ("Locale", (Option.map x.locale ~f:String_.to_value));
        ("SourceIpamPoolId",
          (Option.map x.sourceIpamPoolId ~f:IpamPoolId.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("AddressFamily", (Some (AddressFamily.to_value x.addressFamily)));
        ("AutoImport", (Option.map x.autoImport ~f:Boolean.to_value));
        ("PubliclyAdvertisable",
          (Option.map x.publiclyAdvertisable ~f:Boolean.to_value));
        ("AllocationMinNetmaskLength",
          (Option.map x.allocationMinNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationMaxNetmaskLength",
          (Option.map x.allocationMaxNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationDefaultNetmaskLength",
          (Option.map x.allocationDefaultNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationResourceTag",
          (Option.map x.allocationResourceTags
             ~f:RequestIpamResourceTagList.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("AwsService",
          (Option.map x.awsService ~f:IpamPoolAwsService.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsService =
        (Option.map ~f:IpamPoolAwsService.of_xml)
          (Xml.child xml_arg0 "AwsService") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let allocationResourceTags =
        (Option.map ~f:RequestIpamResourceTagList.of_xml)
          (Xml.child xml_arg0 "AllocationResourceTag") in
      let allocationDefaultNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationDefaultNetmaskLength") in
      let allocationMaxNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationMaxNetmaskLength") in
      let allocationMinNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationMinNetmaskLength") in
      let publiclyAdvertisable =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PubliclyAdvertisable") in
      let autoImport =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoImport") in
      let addressFamily =
        AddressFamily.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AddressFamily") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceIpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "SourceIpamPoolId") in
      let locale =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Locale") in
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?awsService ?clientToken ?tagSpecifications
        ?allocationResourceTags ?allocationDefaultNetmaskLength
        ?allocationMaxNetmaskLength ?allocationMinNetmaskLength
        ?publiclyAdvertisable ?autoImport ~addressFamily ?description
        ?sourceIpamPoolId ?locale ~ipamScopeId ?dryRun ()
    let of_json json =
      let awsService = field_map json "AwsService" IpamPoolAwsService.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let allocationResourceTags =
        field_map json "AllocationResourceTags"
          RequestIpamResourceTagList.of_json in
      let allocationDefaultNetmaskLength =
        field_map json "AllocationDefaultNetmaskLength"
          IpamNetmaskLength.of_json in
      let allocationMaxNetmaskLength =
        field_map json "AllocationMaxNetmaskLength" IpamNetmaskLength.of_json in
      let allocationMinNetmaskLength =
        field_map json "AllocationMinNetmaskLength" IpamNetmaskLength.of_json in
      let publiclyAdvertisable =
        field_map json "PubliclyAdvertisable" Boolean.of_json in
      let autoImport = field_map json "AutoImport" Boolean.of_json in
      let addressFamily =
        field_map_exn json "AddressFamily" AddressFamily.of_json in
      let description = field_map json "Description" String_.of_json in
      let sourceIpamPoolId =
        field_map json "SourceIpamPoolId" IpamPoolId.of_json in
      let locale = field_map json "Locale" String_.of_json in
      let ipamScopeId = field_map_exn json "IpamScopeId" IpamScopeId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?awsService ?clientToken ?tagSpecifications
        ?allocationResourceTags ?allocationDefaultNetmaskLength
        ?allocationMaxNetmaskLength ?allocationMinNetmaskLength
        ?publiclyAdvertisable ?autoImport ~addressFamily ?description
        ?sourceIpamPoolId ?locale ~ipamScopeId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateInternetGatewayResult =
  struct
    type nonrec t = {
      internetGateway: InternetGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?internetGateway = fun () -> { internetGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("internetGateway",
           (Option.map x.internetGateway ~f:InternetGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let internetGateway =
        (Option.map ~f:InternetGateway.of_xml)
          (Xml.child xml_arg0 "internetGateway") in
      make ?internetGateway ()
    let of_json json =
      let internetGateway =
        field_map json "InternetGateway" InternetGateway.of_json in
      make ?internetGateway ()
    let to_json v = composed_to_json to_value v
  end
module CreateInternetGatewayRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let make ?tagSpecifications =
      fun ?dryRun -> fun () -> { tagSpecifications; dryRun }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?dryRun ?tagSpecifications ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      make ?dryRun ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end
module CreateInstanceExportTaskResult =
  struct
    type nonrec t = {
      exportTask: ExportTask.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exportTask = fun () -> { exportTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exportTask", (Option.map x.exportTask ~f:ExportTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTask =
        (Option.map ~f:ExportTask.of_xml) (Xml.child xml_arg0 "exportTask") in
      make ?exportTask ()
    let of_json json =
      let exportTask = field_map json "ExportTask" ExportTask.of_json in
      make ?exportTask ()
    let to_json v = composed_to_json to_value v
  end
module CreateInstanceExportTaskRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      exportToS3Task: ExportToS3TaskSpecification.t ;
      instanceId: InstanceId.t ;
      targetEnvironment: ExportEnvironment.t ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateInstanceExportTaskRequest"
    let make ?description =
      fun ?tagSpecifications ->
        fun ~exportToS3Task ->
          fun ~instanceId ->
            fun ~targetEnvironment ->
              fun () ->
                {
                  description;
                  tagSpecifications;
                  exportToS3Task;
                  instanceId;
                  targetEnvironment
                }
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("exportToS3",
          (Some (ExportToS3TaskSpecification.to_value x.exportToS3Task)));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("targetEnvironment",
          (Some (ExportEnvironment.to_value x.targetEnvironment)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let targetEnvironment =
        ExportEnvironment.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "targetEnvironment") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let exportToS3Task =
        ExportToS3TaskSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "exportToS3") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ?tagSpecifications ~targetEnvironment ~instanceId ~exportToS3Task
        ?description ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let targetEnvironment =
        field_map_exn json "TargetEnvironment" ExportEnvironment.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let exportToS3Task =
        field_map_exn json "ExportToS3Task"
          ExportToS3TaskSpecification.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?tagSpecifications ~targetEnvironment ~instanceId ~exportToS3Task
        ?description ()
    let to_json v = composed_to_json to_value v
  end
module CreateInstanceEventWindowResult =
  struct
    type nonrec t = {
      instanceEventWindow: InstanceEventWindow.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindow = fun () -> { instanceEventWindow }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindow",
           (Option.map x.instanceEventWindow ~f:InstanceEventWindow.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindow =
        (Option.map ~f:InstanceEventWindow.of_xml)
          (Xml.child xml_arg0 "instanceEventWindow") in
      make ?instanceEventWindow ()
    let of_json json =
      let instanceEventWindow =
        field_map json "InstanceEventWindow" InstanceEventWindow.of_json in
      make ?instanceEventWindow ()
    let to_json v = composed_to_json to_value v
  end
module CreateInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      name: String_.t option ;
      timeRanges: InstanceEventWindowTimeRangeRequestSet.t option ;
      cronExpression: InstanceEventWindowCronExpression.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?dryRun =
      fun ?name ->
        fun ?timeRanges ->
          fun ?cronExpression ->
            fun ?tagSpecifications ->
              fun () ->
                { dryRun; name; timeRanges; cronExpression; tagSpecifications
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("TimeRange",
          (Option.map x.timeRanges
             ~f:InstanceEventWindowTimeRangeRequestSet.to_value));
        ("CronExpression",
          (Option.map x.cronExpression
             ~f:InstanceEventWindowCronExpression.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let cronExpression =
        (Option.map ~f:InstanceEventWindowCronExpression.of_xml)
          (Xml.child xml_arg0 "CronExpression") in
      let timeRanges =
        (Option.map ~f:InstanceEventWindowTimeRangeRequestSet.of_xml)
          (Xml.child xml_arg0 "TimeRange") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?cronExpression ?timeRanges ?name ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let cronExpression =
        field_map json "CronExpression"
          InstanceEventWindowCronExpression.of_json in
      let timeRanges =
        field_map json "TimeRanges"
          InstanceEventWindowTimeRangeRequestSet.of_json in
      let name = field_map json "Name" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?cronExpression ?timeRanges ?name ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateImageResult =
  struct
    type nonrec t = {
      imageId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_json json =
      let imageId = field_map json "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end
module CreateImageRequest =
  struct
    type nonrec t =
      {
      blockDeviceMappings: BlockDeviceMappingRequestList.t option ;
      description: String_.t option ;
      dryRun: Boolean.t option ;
      instanceId: InstanceId.t ;
      name: String_.t ;
      noReboot: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateImageRequest"
    let make ?blockDeviceMappings =
      fun ?description ->
        fun ?dryRun ->
          fun ?noReboot ->
            fun ?tagSpecifications ->
              fun ~instanceId ->
                fun ~name ->
                  fun () ->
                    {
                      blockDeviceMappings;
                      description;
                      dryRun;
                      noReboot;
                      tagSpecifications;
                      instanceId;
                      name
                    }
    let to_value x =
      structure_to_value
        [("blockDeviceMapping",
           (Option.map x.blockDeviceMappings
              ~f:BlockDeviceMappingRequestList.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("name", (Some (String_.to_value x.name)));
        ("noReboot", (Option.map x.noReboot ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let noReboot =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "noReboot") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappingRequestList.of_xml)
          (Xml.child xml_arg0 "blockDeviceMapping") in
      make ?tagSpecifications ?noReboot ~name ~instanceId ?dryRun
        ?description ?blockDeviceMappings ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let noReboot = field_map json "NoReboot" Boolean.of_json in
      let name = field_map_exn json "Name" String_.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let blockDeviceMappings =
        field_map json "BlockDeviceMappings"
          BlockDeviceMappingRequestList.of_json in
      make ?tagSpecifications ?noReboot ~name ~instanceId ?dryRun
        ?description ?blockDeviceMappings ()
    let to_json v = composed_to_json to_value v
  end
module CreateFpgaImageResult =
  struct
    type nonrec t =
      {
      fpgaImageId: String_.t option ;
      fpgaImageGlobalId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageId =
      fun ?fpgaImageGlobalId -> fun () -> { fpgaImageId; fpgaImageGlobalId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageId", (Option.map x.fpgaImageId ~f:String_.to_value));
        ("fpgaImageGlobalId",
          (Option.map x.fpgaImageGlobalId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageGlobalId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "fpgaImageGlobalId") in
      let fpgaImageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fpgaImageId") in
      make ?fpgaImageGlobalId ?fpgaImageId ()
    let of_json json =
      let fpgaImageGlobalId =
        field_map json "FpgaImageGlobalId" String_.of_json in
      let fpgaImageId = field_map json "FpgaImageId" String_.of_json in
      make ?fpgaImageGlobalId ?fpgaImageId ()
    let to_json v = composed_to_json to_value v
  end
module CreateFpgaImageRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      inputStorageLocation: StorageLocation.t ;
      logsStorageLocation: StorageLocation.t option ;
      description: String_.t option ;
      name: String_.t option ;
      clientToken: String_.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateFpgaImageRequest"
    let make ?dryRun =
      fun ?logsStorageLocation ->
        fun ?description ->
          fun ?name ->
            fun ?clientToken ->
              fun ?tagSpecifications ->
                fun ~inputStorageLocation ->
                  fun () ->
                    {
                      dryRun;
                      logsStorageLocation;
                      description;
                      name;
                      clientToken;
                      tagSpecifications;
                      inputStorageLocation
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InputStorageLocation",
          (Some (StorageLocation.to_value x.inputStorageLocation)));
        ("LogsStorageLocation",
          (Option.map x.logsStorageLocation ~f:StorageLocation.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let logsStorageLocation =
        (Option.map ~f:StorageLocation.of_xml)
          (Xml.child xml_arg0 "LogsStorageLocation") in
      let inputStorageLocation =
        StorageLocation.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InputStorageLocation") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?clientToken ?name ?description
        ?logsStorageLocation ~inputStorageLocation ?dryRun ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let name = field_map json "Name" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let logsStorageLocation =
        field_map json "LogsStorageLocation" StorageLocation.of_json in
      let inputStorageLocation =
        field_map_exn json "InputStorageLocation" StorageLocation.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?tagSpecifications ?clientToken ?name ?description
        ?logsStorageLocation ~inputStorageLocation ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateFlowLogsResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option ;
      flowLogIds: ValueStringList.t option ;
      unsuccessful: UnsuccessfulItemSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?flowLogIds ->
        fun ?unsuccessful ->
          fun () -> { clientToken; flowLogIds; unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("flowLogIdSet",
          (Option.map x.flowLogIds ~f:ValueStringList.to_value));
        ("unsuccessful",
          (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      let flowLogIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "flowLogIdSet") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?unsuccessful ?flowLogIds ?clientToken ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      let flowLogIds = field_map json "FlowLogIds" ValueStringList.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?unsuccessful ?flowLogIds ?clientToken ()
    let to_json v = composed_to_json to_value v
  end
module CreateFlowLogsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      clientToken: String_.t option ;
      deliverLogsPermissionArn: String_.t option ;
      logGroupName: String_.t option ;
      resourceIds: FlowLogResourceIds.t ;
      resourceType: FlowLogsResourceType.t ;
      trafficType: TrafficType.t ;
      logDestinationType: LogDestinationType.t option ;
      logDestination: String_.t option ;
      logFormat: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      maxAggregationInterval: Integer.t option ;
      destinationOptions: DestinationOptionsRequest.t option }
    let context_ = "CreateFlowLogsRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?deliverLogsPermissionArn ->
          fun ?logGroupName ->
            fun ?logDestinationType ->
              fun ?logDestination ->
                fun ?logFormat ->
                  fun ?tagSpecifications ->
                    fun ?maxAggregationInterval ->
                      fun ?destinationOptions ->
                        fun ~resourceIds ->
                          fun ~resourceType ->
                            fun ~trafficType ->
                              fun () ->
                                {
                                  dryRun;
                                  clientToken;
                                  deliverLogsPermissionArn;
                                  logGroupName;
                                  logDestinationType;
                                  logDestination;
                                  logFormat;
                                  tagSpecifications;
                                  maxAggregationInterval;
                                  destinationOptions;
                                  resourceIds;
                                  resourceType;
                                  trafficType
                                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DeliverLogsPermissionArn",
          (Option.map x.deliverLogsPermissionArn ~f:String_.to_value));
        ("LogGroupName", (Option.map x.logGroupName ~f:String_.to_value));
        ("ResourceId", (Some (FlowLogResourceIds.to_value x.resourceIds)));
        ("ResourceType",
          (Some (FlowLogsResourceType.to_value x.resourceType)));
        ("TrafficType", (Some (TrafficType.to_value x.trafficType)));
        ("LogDestinationType",
          (Option.map x.logDestinationType ~f:LogDestinationType.to_value));
        ("LogDestination", (Option.map x.logDestination ~f:String_.to_value));
        ("LogFormat", (Option.map x.logFormat ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("MaxAggregationInterval",
          (Option.map x.maxAggregationInterval ~f:Integer.to_value));
        ("DestinationOptions",
          (Option.map x.destinationOptions
             ~f:DestinationOptionsRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationOptions =
        (Option.map ~f:DestinationOptionsRequest.of_xml)
          (Xml.child xml_arg0 "DestinationOptions") in
      let maxAggregationInterval =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxAggregationInterval") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let logFormat =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogFormat") in
      let logDestination =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogDestination") in
      let logDestinationType =
        (Option.map ~f:LogDestinationType.of_xml)
          (Xml.child xml_arg0 "LogDestinationType") in
      let trafficType =
        TrafficType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficType") in
      let resourceType =
        FlowLogsResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      let resourceIds =
        FlowLogResourceIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let logGroupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogGroupName") in
      let deliverLogsPermissionArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DeliverLogsPermissionArn") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?destinationOptions ?maxAggregationInterval ?tagSpecifications
        ?logFormat ?logDestination ?logDestinationType ~trafficType
        ~resourceType ~resourceIds ?logGroupName ?deliverLogsPermissionArn
        ?clientToken ?dryRun ()
    let of_json json =
      let destinationOptions =
        field_map json "DestinationOptions" DestinationOptionsRequest.of_json in
      let maxAggregationInterval =
        field_map json "MaxAggregationInterval" Integer.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let logFormat = field_map json "LogFormat" String_.of_json in
      let logDestination = field_map json "LogDestination" String_.of_json in
      let logDestinationType =
        field_map json "LogDestinationType" LogDestinationType.of_json in
      let trafficType = field_map_exn json "TrafficType" TrafficType.of_json in
      let resourceType =
        field_map_exn json "ResourceType" FlowLogsResourceType.of_json in
      let resourceIds =
        field_map_exn json "ResourceIds" FlowLogResourceIds.of_json in
      let logGroupName = field_map json "LogGroupName" String_.of_json in
      let deliverLogsPermissionArn =
        field_map json "DeliverLogsPermissionArn" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?destinationOptions ?maxAggregationInterval ?tagSpecifications
        ?logFormat ?logDestination ?logDestinationType ~trafficType
        ~resourceType ~resourceIds ?logGroupName ?deliverLogsPermissionArn
        ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateFleetResult =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option ;
      errors: CreateFleetErrorsSet.t option ;
      instances: CreateFleetInstancesSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?errors ->
        fun ?instances -> fun () -> { fleetId; errors; instances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("errorSet", (Option.map x.errors ~f:CreateFleetErrorsSet.to_value));
        ("fleetInstanceSet",
          (Option.map x.instances ~f:CreateFleetInstancesSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instances =
        (Option.map ~f:CreateFleetInstancesSet.of_xml)
          (Xml.child xml_arg0 "fleetInstanceSet") in
      let errors =
        (Option.map ~f:CreateFleetErrorsSet.of_xml)
          (Xml.child xml_arg0 "errorSet") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "fleetId") in
      make ?instances ?errors ?fleetId ()
    let of_json json =
      let instances =
        field_map json "Instances" CreateFleetInstancesSet.of_json in
      let errors = field_map json "Errors" CreateFleetErrorsSet.of_json in
      let fleetId = field_map json "FleetId" FleetId.of_json in
      make ?instances ?errors ?fleetId ()
    let to_json v = composed_to_json to_value v
  end
module CreateFleetRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      clientToken: String_.t option ;
      spotOptions: SpotOptionsRequest.t option ;
      onDemandOptions: OnDemandOptionsRequest.t option ;
      excessCapacityTerminationPolicy:
        FleetExcessCapacityTerminationPolicy.t option ;
      launchTemplateConfigs: FleetLaunchTemplateConfigListRequest.t ;
      targetCapacitySpecification: TargetCapacitySpecificationRequest.t ;
      terminateInstancesWithExpiration: Boolean.t option ;
      type_: FleetType.t option ;
      validFrom: DateTime.t option ;
      validUntil: DateTime.t option ;
      replaceUnhealthyInstances: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      context: String_.t option }
    let context_ = "CreateFleetRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?spotOptions ->
          fun ?onDemandOptions ->
            fun ?excessCapacityTerminationPolicy ->
              fun ?terminateInstancesWithExpiration ->
                fun ?type_ ->
                  fun ?validFrom ->
                    fun ?validUntil ->
                      fun ?replaceUnhealthyInstances ->
                        fun ?tagSpecifications ->
                          fun ?context ->
                            fun ~launchTemplateConfigs ->
                              fun ~targetCapacitySpecification ->
                                fun () ->
                                  {
                                    dryRun;
                                    clientToken;
                                    spotOptions;
                                    onDemandOptions;
                                    excessCapacityTerminationPolicy;
                                    terminateInstancesWithExpiration;
                                    type_;
                                    validFrom;
                                    validUntil;
                                    replaceUnhealthyInstances;
                                    tagSpecifications;
                                    context;
                                    launchTemplateConfigs;
                                    targetCapacitySpecification
                                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("SpotOptions",
          (Option.map x.spotOptions ~f:SpotOptionsRequest.to_value));
        ("OnDemandOptions",
          (Option.map x.onDemandOptions ~f:OnDemandOptionsRequest.to_value));
        ("ExcessCapacityTerminationPolicy",
          (Option.map x.excessCapacityTerminationPolicy
             ~f:FleetExcessCapacityTerminationPolicy.to_value));
        ("LaunchTemplateConfigs",
          (Some
             (FleetLaunchTemplateConfigListRequest.to_value
                x.launchTemplateConfigs)));
        ("TargetCapacitySpecification",
          (Some
             (TargetCapacitySpecificationRequest.to_value
                x.targetCapacitySpecification)));
        ("TerminateInstancesWithExpiration",
          (Option.map x.terminateInstancesWithExpiration ~f:Boolean.to_value));
        ("Type", (Option.map x.type_ ~f:FleetType.to_value));
        ("ValidFrom", (Option.map x.validFrom ~f:DateTime.to_value));
        ("ValidUntil", (Option.map x.validUntil ~f:DateTime.to_value));
        ("ReplaceUnhealthyInstances",
          (Option.map x.replaceUnhealthyInstances ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let replaceUnhealthyInstances =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ReplaceUnhealthyInstances") in
      let validUntil =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "ValidUntil") in
      let validFrom =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "ValidFrom") in
      let type_ =
        (Option.map ~f:FleetType.of_xml) (Xml.child xml_arg0 "Type") in
      let terminateInstancesWithExpiration =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TerminateInstancesWithExpiration") in
      let targetCapacitySpecification =
        TargetCapacitySpecificationRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TargetCapacitySpecification") in
      let launchTemplateConfigs =
        FleetLaunchTemplateConfigListRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateConfigs") in
      let excessCapacityTerminationPolicy =
        (Option.map ~f:FleetExcessCapacityTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "ExcessCapacityTerminationPolicy") in
      let onDemandOptions =
        (Option.map ~f:OnDemandOptionsRequest.of_xml)
          (Xml.child xml_arg0 "OnDemandOptions") in
      let spotOptions =
        (Option.map ~f:SpotOptionsRequest.of_xml)
          (Xml.child xml_arg0 "SpotOptions") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?context ?tagSpecifications ?replaceUnhealthyInstances ?validUntil
        ?validFrom ?type_ ?terminateInstancesWithExpiration
        ~targetCapacitySpecification ~launchTemplateConfigs
        ?excessCapacityTerminationPolicy ?onDemandOptions ?spotOptions
        ?clientToken ?dryRun ()
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let replaceUnhealthyInstances =
        field_map json "ReplaceUnhealthyInstances" Boolean.of_json in
      let validUntil = field_map json "ValidUntil" DateTime.of_json in
      let validFrom = field_map json "ValidFrom" DateTime.of_json in
      let type_ = field_map json "Type" FleetType.of_json in
      let terminateInstancesWithExpiration =
        field_map json "TerminateInstancesWithExpiration" Boolean.of_json in
      let targetCapacitySpecification =
        field_map_exn json "TargetCapacitySpecification"
          TargetCapacitySpecificationRequest.of_json in
      let launchTemplateConfigs =
        field_map_exn json "LaunchTemplateConfigs"
          FleetLaunchTemplateConfigListRequest.of_json in
      let excessCapacityTerminationPolicy =
        field_map json "ExcessCapacityTerminationPolicy"
          FleetExcessCapacityTerminationPolicy.of_json in
      let onDemandOptions =
        field_map json "OnDemandOptions" OnDemandOptionsRequest.of_json in
      let spotOptions =
        field_map json "SpotOptions" SpotOptionsRequest.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?context ?tagSpecifications ?replaceUnhealthyInstances ?validUntil
        ?validFrom ?type_ ?terminateInstancesWithExpiration
        ~targetCapacitySpecification ~launchTemplateConfigs
        ?excessCapacityTerminationPolicy ?onDemandOptions ?spotOptions
        ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateEgressOnlyInternetGatewayResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option ;
      egressOnlyInternetGateway: EgressOnlyInternetGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?egressOnlyInternetGateway ->
        fun () -> { clientToken; egressOnlyInternetGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("egressOnlyInternetGateway",
          (Option.map x.egressOnlyInternetGateway
             ~f:EgressOnlyInternetGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let egressOnlyInternetGateway =
        (Option.map ~f:EgressOnlyInternetGateway.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGateway") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?egressOnlyInternetGateway ?clientToken ()
    let of_json json =
      let egressOnlyInternetGateway =
        field_map json "EgressOnlyInternetGateway"
          EgressOnlyInternetGateway.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?egressOnlyInternetGateway ?clientToken ()
    let to_json v = composed_to_json to_value v
  end
module CreateEgressOnlyInternetGatewayRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option ;
      dryRun: Boolean.t option ;
      vpcId: VpcId.t ;
      tagSpecifications: TagSpecificationList.t option }
    let context_ = "CreateEgressOnlyInternetGatewayRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~vpcId ->
            fun () -> { clientToken; dryRun; tagSpecifications; vpcId }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~vpcId ?dryRun ?clientToken ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?tagSpecifications ~vpcId ?dryRun ?clientToken ()
    let to_json v = composed_to_json to_value v
  end
module CreateDhcpOptionsResult =
  struct
    type nonrec t = {
      dhcpOptions: DhcpOptions.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?dhcpOptions = fun () -> { dhcpOptions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("dhcpOptions", (Option.map x.dhcpOptions ~f:DhcpOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dhcpOptions =
        (Option.map ~f:DhcpOptions.of_xml) (Xml.child xml_arg0 "dhcpOptions") in
      make ?dhcpOptions ()
    let of_json json =
      let dhcpOptions = field_map json "DhcpOptions" DhcpOptions.of_json in
      make ?dhcpOptions ()
    let to_json v = composed_to_json to_value v
  end
module CreateDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpConfigurations: NewDhcpConfigurationList.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateDhcpOptionsRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~dhcpConfigurations ->
          fun () -> { tagSpecifications; dryRun; dhcpConfigurations }
    let to_value x =
      structure_to_value
        [("dhcpConfiguration",
           (Some (NewDhcpConfigurationList.to_value x.dhcpConfigurations)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dhcpConfigurations =
        NewDhcpConfigurationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dhcpConfiguration") in
      make ?dryRun ?tagSpecifications ~dhcpConfigurations ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dhcpConfigurations =
        field_map_exn json "DhcpConfigurations"
          NewDhcpConfigurationList.of_json in
      make ?dryRun ?tagSpecifications ~dhcpConfigurations ()
    let to_json v = composed_to_json to_value v
  end
module CreateDefaultVpcResult =
  struct
    type nonrec t = {
      vpc: Vpc.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpc = fun () -> { vpc }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("vpc", (Option.map x.vpc ~f:Vpc.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpc = (Option.map ~f:Vpc.of_xml) (Xml.child xml_arg0 "vpc") in
      make ?vpc ()
    let of_json json =
      let vpc = field_map json "Vpc" Vpc.of_json in make ?vpc ()
    let to_json v = composed_to_json to_value v
  end
module CreateDefaultVpcRequest =
  struct
    type nonrec t = {
      dryRun: Boolean.t option }
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CreateDefaultSubnetResult =
  struct
    type nonrec t = {
      subnet: Subnet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnet = fun () -> { subnet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnet", (Option.map x.subnet ~f:Subnet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnet =
        (Option.map ~f:Subnet.of_xml) (Xml.child xml_arg0 "subnet") in
      make ?subnet ()
    let of_json json =
      let subnet = field_map json "Subnet" Subnet.of_json in make ?subnet ()
    let to_json v = composed_to_json to_value v
  end
module CreateDefaultSubnetRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t ;
      dryRun: Boolean.t option ;
      ipv6Native: Boolean.t option }
    let context_ = "CreateDefaultSubnetRequest"
    let make ?dryRun =
      fun ?ipv6Native ->
        fun ~availabilityZone ->
          fun () -> { dryRun; ipv6Native; availabilityZone }
    let to_value x =
      structure_to_value
        [("AvailabilityZone", (Some (String_.to_value x.availabilityZone)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Ipv6Native", (Option.map x.ipv6Native ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv6Native =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Ipv6Native") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let availabilityZone =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZone") in
      make ?ipv6Native ?dryRun ~availabilityZone ()
    let of_json json =
      let ipv6Native = field_map json "Ipv6Native" Boolean.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let availabilityZone =
        field_map_exn json "AvailabilityZone" String_.of_json in
      make ?ipv6Native ?dryRun ~availabilityZone ()
    let to_json v = composed_to_json to_value v
  end
module CreateCustomerGatewayResult =
  struct
    type nonrec t = {
      customerGateway: CustomerGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?customerGateway = fun () -> { customerGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("customerGateway",
           (Option.map x.customerGateway ~f:CustomerGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customerGateway =
        (Option.map ~f:CustomerGateway.of_xml)
          (Xml.child xml_arg0 "customerGateway") in
      make ?customerGateway ()
    let of_json json =
      let customerGateway =
        field_map json "CustomerGateway" CustomerGateway.of_json in
      make ?customerGateway ()
    let to_json v = composed_to_json to_value v
  end
module CreateCustomerGatewayRequest =
  struct
    type nonrec t =
      {
      bgpAsn: Integer.t ;
      publicIp: String_.t option ;
      certificateArn: String_.t option ;
      type_: GatewayType.t ;
      tagSpecifications: TagSpecificationList.t option ;
      deviceName: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateCustomerGatewayRequest"
    let make ?publicIp =
      fun ?certificateArn ->
        fun ?tagSpecifications ->
          fun ?deviceName ->
            fun ?dryRun ->
              fun ~bgpAsn ->
                fun ~type_ ->
                  fun () ->
                    {
                      publicIp;
                      certificateArn;
                      tagSpecifications;
                      deviceName;
                      dryRun;
                      bgpAsn;
                      type_
                    }
    let to_value x =
      structure_to_value
        [("BgpAsn", (Some (Integer.to_value x.bgpAsn)));
        ("IpAddress", (Option.map x.publicIp ~f:String_.to_value));
        ("CertificateArn", (Option.map x.certificateArn ~f:String_.to_value));
        ("Type", (Some (GatewayType.to_value x.type_)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DeviceName", (Option.map x.deviceName ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let deviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "DeviceName") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let type_ =
        GatewayType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let certificateArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CertificateArn") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let bgpAsn =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "BgpAsn") in
      make ?dryRun ?deviceName ?tagSpecifications ~type_ ?certificateArn
        ?publicIp ~bgpAsn ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let deviceName = field_map json "DeviceName" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let type_ = field_map_exn json "Type" GatewayType.of_json in
      let certificateArn = field_map json "CertificateArn" String_.of_json in
      let publicIp = field_map json "PublicIp" String_.of_json in
      let bgpAsn = field_map_exn json "BgpAsn" Integer.of_json in
      make ?dryRun ?deviceName ?tagSpecifications ~type_ ?certificateArn
        ?publicIp ~bgpAsn ()
    let to_json v = composed_to_json to_value v
  end
module CreateClientVpnRouteResult =
  struct
    type nonrec t = {
      status: ClientVpnRouteStatus.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ClientVpnRouteStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnRouteStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status = field_map json "Status" ClientVpnRouteStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module CreateClientVpnRouteRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      destinationCidrBlock: String_.t ;
      targetVpcSubnetId: SubnetId.t ;
      description: String_.t option ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateClientVpnRouteRequest"
    let make ?description =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ~clientVpnEndpointId ->
            fun ~destinationCidrBlock ->
              fun ~targetVpcSubnetId ->
                fun () ->
                  {
                    description;
                    clientToken;
                    dryRun;
                    clientVpnEndpointId;
                    destinationCidrBlock;
                    targetVpcSubnetId
                  }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("TargetVpcSubnetId", (Some (SubnetId.to_value x.targetVpcSubnetId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let targetVpcSubnetId =
        SubnetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetVpcSubnetId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?clientToken ?description ~targetVpcSubnetId
        ~destinationCidrBlock ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let targetVpcSubnetId =
        field_map_exn json "TargetVpcSubnetId" SubnetId.of_json in
      let destinationCidrBlock =
        field_map_exn json "DestinationCidrBlock" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?clientToken ?description ~targetVpcSubnetId
        ~destinationCidrBlock ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module CreateClientVpnEndpointResult =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: String_.t option ;
      status: ClientVpnEndpointStatus.t option ;
      dnsName: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnEndpointId =
      fun ?status ->
        fun ?dnsName -> fun () -> { clientVpnEndpointId; status; dnsName }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnEndpointId",
           (Option.map x.clientVpnEndpointId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:ClientVpnEndpointStatus.to_value));
        ("dnsName", (Option.map x.dnsName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dnsName") in
      let status =
        (Option.map ~f:ClientVpnEndpointStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let clientVpnEndpointId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "clientVpnEndpointId") in
      make ?dnsName ?status ?clientVpnEndpointId ()
    let of_json json =
      let dnsName = field_map json "DnsName" String_.of_json in
      let status = field_map json "Status" ClientVpnEndpointStatus.of_json in
      let clientVpnEndpointId =
        field_map json "ClientVpnEndpointId" String_.of_json in
      make ?dnsName ?status ?clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module CreateClientVpnEndpointRequest =
  struct
    type nonrec t =
      {
      clientCidrBlock: String_.t ;
      serverCertificateArn: String_.t ;
      authenticationOptions: ClientVpnAuthenticationRequestList.t ;
      connectionLogOptions: ConnectionLogOptions.t ;
      dnsServers: ValueStringList.t option ;
      transportProtocol: TransportProtocol.t option ;
      vpnPort: Integer.t option ;
      description: String_.t option ;
      splitTunnel: Boolean.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      securityGroupIds: ClientVpnSecurityGroupIdSet.t option ;
      vpcId: VpcId.t option ;
      selfServicePortal: SelfServicePortal.t option ;
      clientConnectOptions: ClientConnectOptions.t option ;
      sessionTimeoutHours: Integer.t option ;
      clientLoginBannerOptions: ClientLoginBannerOptions.t option }
    let context_ = "CreateClientVpnEndpointRequest"
    let make ?dnsServers =
      fun ?transportProtocol ->
        fun ?vpnPort ->
          fun ?description ->
            fun ?splitTunnel ->
              fun ?dryRun ->
                fun ?clientToken ->
                  fun ?tagSpecifications ->
                    fun ?securityGroupIds ->
                      fun ?vpcId ->
                        fun ?selfServicePortal ->
                          fun ?clientConnectOptions ->
                            fun ?sessionTimeoutHours ->
                              fun ?clientLoginBannerOptions ->
                                fun ~clientCidrBlock ->
                                  fun ~serverCertificateArn ->
                                    fun ~authenticationOptions ->
                                      fun ~connectionLogOptions ->
                                        fun () ->
                                          {
                                            dnsServers;
                                            transportProtocol;
                                            vpnPort;
                                            description;
                                            splitTunnel;
                                            dryRun;
                                            clientToken;
                                            tagSpecifications;
                                            securityGroupIds;
                                            vpcId;
                                            selfServicePortal;
                                            clientConnectOptions;
                                            sessionTimeoutHours;
                                            clientLoginBannerOptions;
                                            clientCidrBlock;
                                            serverCertificateArn;
                                            authenticationOptions;
                                            connectionLogOptions
                                          }
    let to_value x =
      structure_to_value
        [("ClientCidrBlock", (Some (String_.to_value x.clientCidrBlock)));
        ("ServerCertificateArn",
          (Some (String_.to_value x.serverCertificateArn)));
        ("Authentication",
          (Some
             (ClientVpnAuthenticationRequestList.to_value
                x.authenticationOptions)));
        ("ConnectionLogOptions",
          (Some (ConnectionLogOptions.to_value x.connectionLogOptions)));
        ("DnsServers", (Option.map x.dnsServers ~f:ValueStringList.to_value));
        ("TransportProtocol",
          (Option.map x.transportProtocol ~f:TransportProtocol.to_value));
        ("VpnPort", (Option.map x.vpnPort ~f:Integer.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("SplitTunnel", (Option.map x.splitTunnel ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:ClientVpnSecurityGroupIdSet.to_value));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("SelfServicePortal",
          (Option.map x.selfServicePortal ~f:SelfServicePortal.to_value));
        ("ClientConnectOptions",
          (Option.map x.clientConnectOptions ~f:ClientConnectOptions.to_value));
        ("SessionTimeoutHours",
          (Option.map x.sessionTimeoutHours ~f:Integer.to_value));
        ("ClientLoginBannerOptions",
          (Option.map x.clientLoginBannerOptions
             ~f:ClientLoginBannerOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientLoginBannerOptions =
        (Option.map ~f:ClientLoginBannerOptions.of_xml)
          (Xml.child xml_arg0 "ClientLoginBannerOptions") in
      let sessionTimeoutHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "SessionTimeoutHours") in
      let clientConnectOptions =
        (Option.map ~f:ClientConnectOptions.of_xml)
          (Xml.child xml_arg0 "ClientConnectOptions") in
      let selfServicePortal =
        (Option.map ~f:SelfServicePortal.of_xml)
          (Xml.child xml_arg0 "SelfServicePortal") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let securityGroupIds =
        (Option.map ~f:ClientVpnSecurityGroupIdSet.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let splitTunnel =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SplitTunnel") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let vpnPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "VpnPort") in
      let transportProtocol =
        (Option.map ~f:TransportProtocol.of_xml)
          (Xml.child xml_arg0 "TransportProtocol") in
      let dnsServers =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "DnsServers") in
      let connectionLogOptions =
        ConnectionLogOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectionLogOptions") in
      let authenticationOptions =
        ClientVpnAuthenticationRequestList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Authentication") in
      let serverCertificateArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerCertificateArn") in
      let clientCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientCidrBlock") in
      make ?clientLoginBannerOptions ?sessionTimeoutHours
        ?clientConnectOptions ?selfServicePortal ?vpcId ?securityGroupIds
        ?tagSpecifications ?clientToken ?dryRun ?splitTunnel ?description
        ?vpnPort ?transportProtocol ?dnsServers ~connectionLogOptions
        ~authenticationOptions ~serverCertificateArn ~clientCidrBlock ()
    let of_json json =
      let clientLoginBannerOptions =
        field_map json "ClientLoginBannerOptions"
          ClientLoginBannerOptions.of_json in
      let sessionTimeoutHours =
        field_map json "SessionTimeoutHours" Integer.of_json in
      let clientConnectOptions =
        field_map json "ClientConnectOptions" ClientConnectOptions.of_json in
      let selfServicePortal =
        field_map json "SelfServicePortal" SelfServicePortal.of_json in
      let vpcId = field_map json "VpcId" VpcId.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds" ClientVpnSecurityGroupIdSet.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let splitTunnel = field_map json "SplitTunnel" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let vpnPort = field_map json "VpnPort" Integer.of_json in
      let transportProtocol =
        field_map json "TransportProtocol" TransportProtocol.of_json in
      let dnsServers = field_map json "DnsServers" ValueStringList.of_json in
      let connectionLogOptions =
        field_map_exn json "ConnectionLogOptions"
          ConnectionLogOptions.of_json in
      let authenticationOptions =
        field_map_exn json "AuthenticationOptions"
          ClientVpnAuthenticationRequestList.of_json in
      let serverCertificateArn =
        field_map_exn json "ServerCertificateArn" String_.of_json in
      let clientCidrBlock =
        field_map_exn json "ClientCidrBlock" String_.of_json in
      make ?clientLoginBannerOptions ?sessionTimeoutHours
        ?clientConnectOptions ?selfServicePortal ?vpcId ?securityGroupIds
        ?tagSpecifications ?clientToken ?dryRun ?splitTunnel ?description
        ?vpnPort ?transportProtocol ?dnsServers ~connectionLogOptions
        ~authenticationOptions ~serverCertificateArn ~clientCidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module CreateCarrierGatewayResult =
  struct
    type nonrec t = {
      carrierGateway: CarrierGateway.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?carrierGateway = fun () -> { carrierGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("carrierGateway",
           (Option.map x.carrierGateway ~f:CarrierGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let carrierGateway =
        (Option.map ~f:CarrierGateway.of_xml)
          (Xml.child xml_arg0 "carrierGateway") in
      make ?carrierGateway ()
    let of_json json =
      let carrierGateway =
        field_map json "CarrierGateway" CarrierGateway.of_json in
      make ?carrierGateway ()
    let to_json v = composed_to_json to_value v
  end
module CreateCarrierGatewayRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      clientToken: String_.t option }
    let context_ = "CreateCarrierGatewayRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ?clientToken ->
          fun ~vpcId ->
            fun () -> { tagSpecifications; dryRun; clientToken; vpcId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?clientToken ?dryRun ?tagSpecifications ~vpcId ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ~vpcId ()
    let to_json v = composed_to_json to_value v
  end
module CreateCapacityReservationResult =
  struct
    type nonrec t = {
      capacityReservation: CapacityReservation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityReservation = fun () -> { capacityReservation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityReservation",
           (Option.map x.capacityReservation ~f:CapacityReservation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservation =
        (Option.map ~f:CapacityReservation.of_xml)
          (Xml.child xml_arg0 "capacityReservation") in
      make ?capacityReservation ()
    let of_json json =
      let capacityReservation =
        field_map json "CapacityReservation" CapacityReservation.of_json in
      make ?capacityReservation ()
    let to_json v = composed_to_json to_value v
  end
module CreateCapacityReservationRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option ;
      instanceType: String_.t ;
      instancePlatform: CapacityReservationInstancePlatform.t ;
      availabilityZone: String_.t option ;
      availabilityZoneId: String_.t option ;
      tenancy: CapacityReservationTenancy.t option ;
      instanceCount: Integer.t ;
      ebsOptimized: Boolean.t option ;
      ephemeralStorage: Boolean.t option ;
      endDate: DateTime.t option ;
      endDateType: EndDateType.t option ;
      instanceMatchCriteria: InstanceMatchCriteria.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option ;
      outpostArn: OutpostArn.t option ;
      placementGroupArn: PlacementGroupArn.t option }
    let context_ = "CreateCapacityReservationRequest"
    let make ?clientToken =
      fun ?availabilityZone ->
        fun ?availabilityZoneId ->
          fun ?tenancy ->
            fun ?ebsOptimized ->
              fun ?ephemeralStorage ->
                fun ?endDate ->
                  fun ?endDateType ->
                    fun ?instanceMatchCriteria ->
                      fun ?tagSpecifications ->
                        fun ?dryRun ->
                          fun ?outpostArn ->
                            fun ?placementGroupArn ->
                              fun ~instanceType ->
                                fun ~instancePlatform ->
                                  fun ~instanceCount ->
                                    fun () ->
                                      {
                                        clientToken;
                                        availabilityZone;
                                        availabilityZoneId;
                                        tenancy;
                                        ebsOptimized;
                                        ephemeralStorage;
                                        endDate;
                                        endDateType;
                                        instanceMatchCriteria;
                                        tagSpecifications;
                                        dryRun;
                                        outpostArn;
                                        placementGroupArn;
                                        instanceType;
                                        instancePlatform;
                                        instanceCount
                                      }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("InstanceType", (Some (String_.to_value x.instanceType)));
        ("InstancePlatform",
          (Some
             (CapacityReservationInstancePlatform.to_value x.instancePlatform)));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:String_.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:String_.to_value));
        ("Tenancy",
          (Option.map x.tenancy ~f:CapacityReservationTenancy.to_value));
        ("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("EbsOptimized", (Option.map x.ebsOptimized ~f:Boolean.to_value));
        ("EphemeralStorage",
          (Option.map x.ephemeralStorage ~f:Boolean.to_value));
        ("EndDate", (Option.map x.endDate ~f:DateTime.to_value));
        ("EndDateType", (Option.map x.endDateType ~f:EndDateType.to_value));
        ("InstanceMatchCriteria",
          (Option.map x.instanceMatchCriteria
             ~f:InstanceMatchCriteria.to_value));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:OutpostArn.to_value));
        ("PlacementGroupArn",
          (Option.map x.placementGroupArn ~f:PlacementGroupArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementGroupArn =
        (Option.map ~f:PlacementGroupArn.of_xml)
          (Xml.child xml_arg0 "PlacementGroupArn") in
      let outpostArn =
        (Option.map ~f:OutpostArn.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let instanceMatchCriteria =
        (Option.map ~f:InstanceMatchCriteria.of_xml)
          (Xml.child xml_arg0 "InstanceMatchCriteria") in
      let endDateType =
        (Option.map ~f:EndDateType.of_xml) (Xml.child xml_arg0 "EndDateType") in
      let endDate =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "EndDate") in
      let ephemeralStorage =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EphemeralStorage") in
      let ebsOptimized =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "EbsOptimized") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      let tenancy =
        (Option.map ~f:CapacityReservationTenancy.of_xml)
          (Xml.child xml_arg0 "Tenancy") in
      let availabilityZoneId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let instancePlatform =
        CapacityReservationInstancePlatform.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstancePlatform") in
      let instanceType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?placementGroupArn ?outpostArn ?dryRun ?tagSpecifications
        ?instanceMatchCriteria ?endDateType ?endDate ?ephemeralStorage
        ?ebsOptimized ~instanceCount ?tenancy ?availabilityZoneId
        ?availabilityZone ~instancePlatform ~instanceType ?clientToken ()
    let of_json json =
      let placementGroupArn =
        field_map json "PlacementGroupArn" PlacementGroupArn.of_json in
      let outpostArn = field_map json "OutpostArn" OutpostArn.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let instanceMatchCriteria =
        field_map json "InstanceMatchCriteria" InstanceMatchCriteria.of_json in
      let endDateType = field_map json "EndDateType" EndDateType.of_json in
      let endDate = field_map json "EndDate" DateTime.of_json in
      let ephemeralStorage =
        field_map json "EphemeralStorage" Boolean.of_json in
      let ebsOptimized = field_map json "EbsOptimized" Boolean.of_json in
      let instanceCount = field_map_exn json "InstanceCount" Integer.of_json in
      let tenancy =
        field_map json "Tenancy" CapacityReservationTenancy.of_json in
      let availabilityZoneId =
        field_map json "AvailabilityZoneId" String_.of_json in
      let availabilityZone =
        field_map json "AvailabilityZone" String_.of_json in
      let instancePlatform =
        field_map_exn json "InstancePlatform"
          CapacityReservationInstancePlatform.of_json in
      let instanceType = field_map_exn json "InstanceType" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?placementGroupArn ?outpostArn ?dryRun ?tagSpecifications
        ?instanceMatchCriteria ?endDateType ?endDate ?ephemeralStorage
        ?ebsOptimized ~instanceCount ?tenancy ?availabilityZoneId
        ?availabilityZone ~instancePlatform ~instanceType ?clientToken ()
    let to_json v = composed_to_json to_value v
  end
module CreateCapacityReservationFleetResult =
  struct
    type nonrec t =
      {
      capacityReservationFleetId: CapacityReservationFleetId.t option ;
      state: CapacityReservationFleetState.t option ;
      totalTargetCapacity: Integer.t option ;
      totalFulfilledCapacity: Double.t option ;
      instanceMatchCriteria: FleetInstanceMatchCriteria.t option ;
      allocationStrategy: String_.t option ;
      createTime: MillisecondDateTime.t option ;
      endDate: MillisecondDateTime.t option ;
      tenancy: FleetCapacityReservationTenancy.t option ;
      fleetCapacityReservations: FleetCapacityReservationSet.t option ;
      tags: TagList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityReservationFleetId =
      fun ?state ->
        fun ?totalTargetCapacity ->
          fun ?totalFulfilledCapacity ->
            fun ?instanceMatchCriteria ->
              fun ?allocationStrategy ->
                fun ?createTime ->
                  fun ?endDate ->
                    fun ?tenancy ->
                      fun ?fleetCapacityReservations ->
                        fun ?tags ->
                          fun () ->
                            {
                              capacityReservationFleetId;
                              state;
                              totalTargetCapacity;
                              totalFulfilledCapacity;
                              instanceMatchCriteria;
                              allocationStrategy;
                              createTime;
                              endDate;
                              tenancy;
                              fleetCapacityReservations;
                              tags
                            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityReservationFleetId",
           (Option.map x.capacityReservationFleetId
              ~f:CapacityReservationFleetId.to_value));
        ("state",
          (Option.map x.state ~f:CapacityReservationFleetState.to_value));
        ("totalTargetCapacity",
          (Option.map x.totalTargetCapacity ~f:Integer.to_value));
        ("totalFulfilledCapacity",
          (Option.map x.totalFulfilledCapacity ~f:Double.to_value));
        ("instanceMatchCriteria",
          (Option.map x.instanceMatchCriteria
             ~f:FleetInstanceMatchCriteria.to_value));
        ("allocationStrategy",
          (Option.map x.allocationStrategy ~f:String_.to_value));
        ("createTime",
          (Option.map x.createTime ~f:MillisecondDateTime.to_value));
        ("endDate", (Option.map x.endDate ~f:MillisecondDateTime.to_value));
        ("tenancy",
          (Option.map x.tenancy ~f:FleetCapacityReservationTenancy.to_value));
        ("fleetCapacityReservationSet",
          (Option.map x.fleetCapacityReservations
             ~f:FleetCapacityReservationSet.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let fleetCapacityReservations =
        (Option.map ~f:FleetCapacityReservationSet.of_xml)
          (Xml.child xml_arg0 "fleetCapacityReservationSet") in
      let tenancy =
        (Option.map ~f:FleetCapacityReservationTenancy.of_xml)
          (Xml.child xml_arg0 "tenancy") in
      let endDate =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "endDate") in
      let createTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "createTime") in
      let allocationStrategy =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "allocationStrategy") in
      let instanceMatchCriteria =
        (Option.map ~f:FleetInstanceMatchCriteria.of_xml)
          (Xml.child xml_arg0 "instanceMatchCriteria") in
      let totalFulfilledCapacity =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "totalFulfilledCapacity") in
      let totalTargetCapacity =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "totalTargetCapacity") in
      let state =
        (Option.map ~f:CapacityReservationFleetState.of_xml)
          (Xml.child xml_arg0 "state") in
      let capacityReservationFleetId =
        (Option.map ~f:CapacityReservationFleetId.of_xml)
          (Xml.child xml_arg0 "capacityReservationFleetId") in
      make ?tags ?fleetCapacityReservations ?tenancy ?endDate ?createTime
        ?allocationStrategy ?instanceMatchCriteria ?totalFulfilledCapacity
        ?totalTargetCapacity ?state ?capacityReservationFleetId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let fleetCapacityReservations =
        field_map json "FleetCapacityReservations"
          FleetCapacityReservationSet.of_json in
      let tenancy =
        field_map json "Tenancy" FleetCapacityReservationTenancy.of_json in
      let endDate = field_map json "EndDate" MillisecondDateTime.of_json in
      let createTime =
        field_map json "CreateTime" MillisecondDateTime.of_json in
      let allocationStrategy =
        field_map json "AllocationStrategy" String_.of_json in
      let instanceMatchCriteria =
        field_map json "InstanceMatchCriteria"
          FleetInstanceMatchCriteria.of_json in
      let totalFulfilledCapacity =
        field_map json "TotalFulfilledCapacity" Double.of_json in
      let totalTargetCapacity =
        field_map json "TotalTargetCapacity" Integer.of_json in
      let state =
        field_map json "State" CapacityReservationFleetState.of_json in
      let capacityReservationFleetId =
        field_map json "CapacityReservationFleetId"
          CapacityReservationFleetId.of_json in
      make ?tags ?fleetCapacityReservations ?tenancy ?endDate ?createTime
        ?allocationStrategy ?instanceMatchCriteria ?totalFulfilledCapacity
        ?totalTargetCapacity ?state ?capacityReservationFleetId ()
    let to_json v = composed_to_json to_value v
  end
module CreateCapacityReservationFleetRequest =
  struct
    type nonrec t =
      {
      allocationStrategy: String_.t option ;
      clientToken: String_.t option ;
      instanceTypeSpecifications: ReservationFleetInstanceSpecificationList.t ;
      tenancy: FleetCapacityReservationTenancy.t option ;
      totalTargetCapacity: Integer.t ;
      endDate: MillisecondDateTime.t option ;
      instanceMatchCriteria: FleetInstanceMatchCriteria.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CreateCapacityReservationFleetRequest"
    let make ?allocationStrategy =
      fun ?clientToken ->
        fun ?tenancy ->
          fun ?endDate ->
            fun ?instanceMatchCriteria ->
              fun ?tagSpecifications ->
                fun ?dryRun ->
                  fun ~instanceTypeSpecifications ->
                    fun ~totalTargetCapacity ->
                      fun () ->
                        {
                          allocationStrategy;
                          clientToken;
                          tenancy;
                          endDate;
                          instanceMatchCriteria;
                          tagSpecifications;
                          dryRun;
                          instanceTypeSpecifications;
                          totalTargetCapacity
                        }
    let to_value x =
      structure_to_value
        [("AllocationStrategy",
           (Option.map x.allocationStrategy ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("InstanceTypeSpecification",
          (Some
             (ReservationFleetInstanceSpecificationList.to_value
                x.instanceTypeSpecifications)));
        ("Tenancy",
          (Option.map x.tenancy ~f:FleetCapacityReservationTenancy.to_value));
        ("TotalTargetCapacity",
          (Some (Integer.to_value x.totalTargetCapacity)));
        ("EndDate", (Option.map x.endDate ~f:MillisecondDateTime.to_value));
        ("InstanceMatchCriteria",
          (Option.map x.instanceMatchCriteria
             ~f:FleetInstanceMatchCriteria.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let instanceMatchCriteria =
        (Option.map ~f:FleetInstanceMatchCriteria.of_xml)
          (Xml.child xml_arg0 "InstanceMatchCriteria") in
      let endDate =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "EndDate") in
      let totalTargetCapacity =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TotalTargetCapacity") in
      let tenancy =
        (Option.map ~f:FleetCapacityReservationTenancy.of_xml)
          (Xml.child xml_arg0 "Tenancy") in
      let instanceTypeSpecifications =
        ReservationFleetInstanceSpecificationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "InstanceTypeSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let allocationStrategy =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AllocationStrategy") in
      make ?dryRun ?tagSpecifications ?instanceMatchCriteria ?endDate
        ~totalTargetCapacity ?tenancy ~instanceTypeSpecifications
        ?clientToken ?allocationStrategy ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let instanceMatchCriteria =
        field_map json "InstanceMatchCriteria"
          FleetInstanceMatchCriteria.of_json in
      let endDate = field_map json "EndDate" MillisecondDateTime.of_json in
      let totalTargetCapacity =
        field_map_exn json "TotalTargetCapacity" Integer.of_json in
      let tenancy =
        field_map json "Tenancy" FleetCapacityReservationTenancy.of_json in
      let instanceTypeSpecifications =
        field_map_exn json "InstanceTypeSpecifications"
          ReservationFleetInstanceSpecificationList.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let allocationStrategy =
        field_map json "AllocationStrategy" String_.of_json in
      make ?dryRun ?tagSpecifications ?instanceMatchCriteria ?endDate
        ~totalTargetCapacity ?tenancy ~instanceTypeSpecifications
        ?clientToken ?allocationStrategy ()
    let to_json v = composed_to_json to_value v
  end
module CopySnapshotResult =
  struct
    type nonrec t = {
      snapshotId: String_.t option ;
      tags: TagList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshotId = fun ?tags -> fun () -> { snapshotId; tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotId", (Option.map x.snapshotId ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let snapshotId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "snapshotId") in
      make ?tags ?snapshotId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let snapshotId = field_map json "SnapshotId" String_.of_json in
      make ?tags ?snapshotId ()
    let to_json v = composed_to_json to_value v
  end
module CopySnapshotRequest =
  struct
    type nonrec t =
      {
      description: String_.t option ;
      destinationOutpostArn: String_.t option ;
      destinationRegion: String_.t option ;
      encrypted: Boolean.t option ;
      kmsKeyId: KmsKeyId.t option ;
      presignedUrl: String_.t option ;
      sourceRegion: String_.t ;
      sourceSnapshotId: String_.t ;
      tagSpecifications: TagSpecificationList.t option ;
      dryRun: Boolean.t option }
    let context_ = "CopySnapshotRequest"
    let make ?description =
      fun ?destinationOutpostArn ->
        fun ?destinationRegion ->
          fun ?encrypted ->
            fun ?kmsKeyId ->
              fun ?presignedUrl ->
                fun ?tagSpecifications ->
                  fun ?dryRun ->
                    fun ~sourceRegion ->
                      fun ~sourceSnapshotId ->
                        fun () ->
                          {
                            description;
                            destinationOutpostArn;
                            destinationRegion;
                            encrypted;
                            kmsKeyId;
                            presignedUrl;
                            tagSpecifications;
                            dryRun;
                            sourceRegion;
                            sourceSnapshotId
                          }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("DestinationOutpostArn",
          (Option.map x.destinationOutpostArn ~f:String_.to_value));
        ("destinationRegion",
          (Option.map x.destinationRegion ~f:String_.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("kmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("presignedUrl", (Option.map x.presignedUrl ~f:String_.to_value));
        ("SourceRegion", (Some (String_.to_value x.sourceRegion)));
        ("SourceSnapshotId", (Some (String_.to_value x.sourceSnapshotId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let sourceSnapshotId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceSnapshotId") in
      let sourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceRegion") in
      let presignedUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "presignedUrl") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let destinationRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationRegion") in
      let destinationOutpostArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationOutpostArn") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?dryRun ?tagSpecifications ~sourceSnapshotId ~sourceRegion
        ?presignedUrl ?kmsKeyId ?encrypted ?destinationRegion
        ?destinationOutpostArn ?description ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let sourceSnapshotId =
        field_map_exn json "SourceSnapshotId" String_.of_json in
      let sourceRegion = field_map_exn json "SourceRegion" String_.of_json in
      let presignedUrl = field_map json "PresignedUrl" String_.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let destinationRegion =
        field_map json "DestinationRegion" String_.of_json in
      let destinationOutpostArn =
        field_map json "DestinationOutpostArn" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      make ?dryRun ?tagSpecifications ~sourceSnapshotId ~sourceRegion
        ?presignedUrl ?kmsKeyId ?encrypted ?destinationRegion
        ?destinationOutpostArn ?description ()
    let to_json v = composed_to_json to_value v
  end
module CopyImageResult =
  struct
    type nonrec t = {
      imageId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_json json =
      let imageId = field_map json "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end
module CopyImageRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option ;
      description: String_.t option ;
      encrypted: Boolean.t option ;
      kmsKeyId: KmsKeyId.t option ;
      name: String_.t ;
      sourceImageId: String_.t ;
      sourceRegion: String_.t ;
      destinationOutpostArn: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "CopyImageRequest"
    let make ?clientToken =
      fun ?description ->
        fun ?encrypted ->
          fun ?kmsKeyId ->
            fun ?destinationOutpostArn ->
              fun ?dryRun ->
                fun ~name ->
                  fun ~sourceImageId ->
                    fun ~sourceRegion ->
                      fun () ->
                        {
                          clientToken;
                          description;
                          encrypted;
                          kmsKeyId;
                          destinationOutpostArn;
                          dryRun;
                          name;
                          sourceImageId;
                          sourceRegion
                        }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("kmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("Name", (Some (String_.to_value x.name)));
        ("SourceImageId", (Some (String_.to_value x.sourceImageId)));
        ("SourceRegion", (Some (String_.to_value x.sourceRegion)));
        ("DestinationOutpostArn",
          (Option.map x.destinationOutpostArn ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationOutpostArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationOutpostArn") in
      let sourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceRegion") in
      let sourceImageId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceImageId") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?dryRun ?destinationOutpostArn ~sourceRegion ~sourceImageId ~name
        ?kmsKeyId ?encrypted ?description ?clientToken ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let destinationOutpostArn =
        field_map json "DestinationOutpostArn" String_.of_json in
      let sourceRegion = field_map_exn json "SourceRegion" String_.of_json in
      let sourceImageId = field_map_exn json "SourceImageId" String_.of_json in
      let name = field_map_exn json "Name" String_.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      make ?dryRun ?destinationOutpostArn ~sourceRegion ~sourceImageId ~name
        ?kmsKeyId ?encrypted ?description ?clientToken ()
    let to_json v = composed_to_json to_value v
  end
module CopyFpgaImageResult =
  struct
    type nonrec t = {
      fpgaImageId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageId = fun () -> { fpgaImageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageId", (Option.map x.fpgaImageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fpgaImageId") in
      make ?fpgaImageId ()
    let of_json json =
      let fpgaImageId = field_map json "FpgaImageId" String_.of_json in
      make ?fpgaImageId ()
    let to_json v = composed_to_json to_value v
  end
module CopyFpgaImageRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      sourceFpgaImageId: String_.t ;
      description: String_.t option ;
      name: String_.t option ;
      sourceRegion: String_.t ;
      clientToken: String_.t option }
    let context_ = "CopyFpgaImageRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?name ->
          fun ?clientToken ->
            fun ~sourceFpgaImageId ->
              fun ~sourceRegion ->
                fun () ->
                  {
                    dryRun;
                    description;
                    name;
                    clientToken;
                    sourceFpgaImageId;
                    sourceRegion
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SourceFpgaImageId", (Some (String_.to_value x.sourceFpgaImageId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("SourceRegion", (Some (String_.to_value x.sourceRegion)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let sourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceRegion") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceFpgaImageId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceFpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ~sourceRegion ?name ?description ~sourceFpgaImageId
        ?dryRun ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let sourceRegion = field_map_exn json "SourceRegion" String_.of_json in
      let name = field_map json "Name" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let sourceFpgaImageId =
        field_map_exn json "SourceFpgaImageId" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?clientToken ~sourceRegion ?name ?description ~sourceFpgaImageId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module ConfirmProductInstanceResult =
  struct
    type nonrec t = {
      ownerId: String_.t option ;
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ownerId = fun ?return -> fun () -> { ownerId; return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ownerId", (Option.map x.ownerId ~f:String_.to_value));
        ("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      let ownerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ownerId") in
      make ?return ?ownerId ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in
      let ownerId = field_map json "OwnerId" String_.of_json in
      make ?return ?ownerId ()
    let to_json v = composed_to_json to_value v
  end
module ConfirmProductInstanceRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t ;
      productCode: String_.t ;
      dryRun: Boolean.t option }
    let context_ = "ConfirmProductInstanceRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~productCode -> fun () -> { dryRun; instanceId; productCode }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("ProductCode", (Some (String_.to_value x.productCode)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let productCode =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProductCode") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~productCode ~instanceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let productCode = field_map_exn json "ProductCode" String_.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?dryRun ~productCode ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module CancelSpotInstanceRequestsResult =
  struct
    type nonrec t =
      {
      cancelledSpotInstanceRequests:
        CancelledSpotInstanceRequestList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?cancelledSpotInstanceRequests =
      fun () -> { cancelledSpotInstanceRequests }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotInstanceRequestSet",
           (Option.map x.cancelledSpotInstanceRequests
              ~f:CancelledSpotInstanceRequestList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cancelledSpotInstanceRequests =
        (Option.map ~f:CancelledSpotInstanceRequestList.of_xml)
          (Xml.child xml_arg0 "spotInstanceRequestSet") in
      make ?cancelledSpotInstanceRequests ()
    let of_json json =
      let cancelledSpotInstanceRequests =
        field_map json "CancelledSpotInstanceRequests"
          CancelledSpotInstanceRequestList.of_json in
      make ?cancelledSpotInstanceRequests ()
    let to_json v = composed_to_json to_value v
  end
module CancelSpotInstanceRequestsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      spotInstanceRequestIds: SpotInstanceRequestIdList.t }
    let context_ = "CancelSpotInstanceRequestsRequest"
    let make ?dryRun =
      fun ~spotInstanceRequestIds ->
        fun () -> { dryRun; spotInstanceRequestIds }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SpotInstanceRequestId",
          (Some (SpotInstanceRequestIdList.to_value x.spotInstanceRequestIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotInstanceRequestIds =
        SpotInstanceRequestIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SpotInstanceRequestId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~spotInstanceRequestIds ?dryRun ()
    let of_json json =
      let spotInstanceRequestIds =
        field_map_exn json "SpotInstanceRequestIds"
          SpotInstanceRequestIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~spotInstanceRequestIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CancelSpotFleetRequestsResponse =
  struct
    type nonrec t =
      {
      successfulFleetRequests: CancelSpotFleetRequestsSuccessSet.t option ;
      unsuccessfulFleetRequests: CancelSpotFleetRequestsErrorSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulFleetRequests =
      fun ?unsuccessfulFleetRequests ->
        fun () -> { successfulFleetRequests; unsuccessfulFleetRequests }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulFleetRequestSet",
           (Option.map x.successfulFleetRequests
              ~f:CancelSpotFleetRequestsSuccessSet.to_value));
        ("unsuccessfulFleetRequestSet",
          (Option.map x.unsuccessfulFleetRequests
             ~f:CancelSpotFleetRequestsErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfulFleetRequests =
        (Option.map ~f:CancelSpotFleetRequestsErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfulFleetRequestSet") in
      let successfulFleetRequests =
        (Option.map ~f:CancelSpotFleetRequestsSuccessSet.of_xml)
          (Xml.child xml_arg0 "successfulFleetRequestSet") in
      make ?unsuccessfulFleetRequests ?successfulFleetRequests ()
    let of_json json =
      let unsuccessfulFleetRequests =
        field_map json "UnsuccessfulFleetRequests"
          CancelSpotFleetRequestsErrorSet.of_json in
      let successfulFleetRequests =
        field_map json "SuccessfulFleetRequests"
          CancelSpotFleetRequestsSuccessSet.of_json in
      make ?unsuccessfulFleetRequests ?successfulFleetRequests ()
    let to_json v = composed_to_json to_value v
  end
module CancelSpotFleetRequestsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      spotFleetRequestIds: SpotFleetRequestIdList.t ;
      terminateInstances: Boolean.t }
    let context_ = "CancelSpotFleetRequestsRequest"
    let make ?dryRun =
      fun ~spotFleetRequestIds ->
        fun ~terminateInstances ->
          fun () -> { dryRun; spotFleetRequestIds; terminateInstances }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("spotFleetRequestId",
          (Some (SpotFleetRequestIdList.to_value x.spotFleetRequestIds)));
        ("terminateInstances",
          (Some (Boolean.to_value x.terminateInstances)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminateInstances =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "terminateInstances") in
      let spotFleetRequestIds =
        SpotFleetRequestIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "spotFleetRequestId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~terminateInstances ~spotFleetRequestIds ?dryRun ()
    let of_json json =
      let terminateInstances =
        field_map_exn json "TerminateInstances" Boolean.of_json in
      let spotFleetRequestIds =
        field_map_exn json "SpotFleetRequestIds"
          SpotFleetRequestIdList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~terminateInstances ~spotFleetRequestIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CancelReservedInstancesListingResult =
  struct
    type nonrec t =
      {
      reservedInstancesListings: ReservedInstancesListingList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesListings =
      fun () -> { reservedInstancesListings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesListingsSet",
           (Option.map x.reservedInstancesListings
              ~f:ReservedInstancesListingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListings =
        (Option.map ~f:ReservedInstancesListingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingsSet") in
      make ?reservedInstancesListings ()
    let of_json json =
      let reservedInstancesListings =
        field_map json "ReservedInstancesListings"
          ReservedInstancesListingList.of_json in
      make ?reservedInstancesListings ()
    let to_json v = composed_to_json to_value v
  end
module CancelReservedInstancesListingRequest =
  struct
    type nonrec t =
      {
      reservedInstancesListingId: ReservedInstancesListingId.t }
    let context_ = "CancelReservedInstancesListingRequest"
    let make ~reservedInstancesListingId =
      fun () -> { reservedInstancesListingId }
    let to_value x =
      structure_to_value
        [("reservedInstancesListingId",
           (Some
              (ReservedInstancesListingId.to_value
                 x.reservedInstancesListingId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListingId =
        ReservedInstancesListingId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "reservedInstancesListingId") in
      make ~reservedInstancesListingId ()
    let of_json json =
      let reservedInstancesListingId =
        field_map_exn json "ReservedInstancesListingId"
          ReservedInstancesListingId.of_json in
      make ~reservedInstancesListingId ()
    let to_json v = composed_to_json to_value v
  end
module CancelImportTaskResult =
  struct
    type nonrec t =
      {
      importTaskId: String_.t option ;
      previousState: String_.t option ;
      state: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?importTaskId =
      fun ?previousState ->
        fun ?state -> fun () -> { importTaskId; previousState; state }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("importTaskId", (Option.map x.importTaskId ~f:String_.to_value));
        ("previousState", (Option.map x.previousState ~f:String_.to_value));
        ("state", (Option.map x.state ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "state") in
      let previousState =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "previousState") in
      let importTaskId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "importTaskId") in
      make ?state ?previousState ?importTaskId ()
    let of_json json =
      let state = field_map json "State" String_.of_json in
      let previousState = field_map json "PreviousState" String_.of_json in
      let importTaskId = field_map json "ImportTaskId" String_.of_json in
      make ?state ?previousState ?importTaskId ()
    let to_json v = composed_to_json to_value v
  end
module CancelImportTaskRequest =
  struct
    type nonrec t =
      {
      cancelReason: String_.t option ;
      dryRun: Boolean.t option ;
      importTaskId: ImportTaskId.t option }
    let make ?cancelReason =
      fun ?dryRun ->
        fun ?importTaskId -> fun () -> { cancelReason; dryRun; importTaskId }
    let to_value x =
      structure_to_value
        [("CancelReason", (Option.map x.cancelReason ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ImportTaskId",
          (Option.map x.importTaskId ~f:ImportTaskId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let importTaskId =
        (Option.map ~f:ImportTaskId.of_xml)
          (Xml.child xml_arg0 "ImportTaskId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let cancelReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CancelReason") in
      make ?importTaskId ?dryRun ?cancelReason ()
    let of_json json =
      let importTaskId = field_map json "ImportTaskId" ImportTaskId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let cancelReason = field_map json "CancelReason" String_.of_json in
      make ?importTaskId ?dryRun ?cancelReason ()
    let to_json v = composed_to_json to_value v
  end
module CancelExportTaskRequest =
  struct
    type nonrec t = {
      exportTaskId: ExportVmTaskId.t }
    let context_ = "CancelExportTaskRequest"
    let make ~exportTaskId = fun () -> { exportTaskId }
    let to_value x =
      structure_to_value
        [("exportTaskId", (Some (ExportVmTaskId.to_value x.exportTaskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTaskId =
        ExportVmTaskId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "exportTaskId") in
      make ~exportTaskId ()
    let of_json json =
      let exportTaskId =
        field_map_exn json "ExportTaskId" ExportVmTaskId.of_json in
      make ~exportTaskId ()
    let to_json v = composed_to_json to_value v
  end
module CancelConversionRequest =
  struct
    type nonrec t =
      {
      conversionTaskId: ConversionTaskId.t ;
      dryRun: Boolean.t option ;
      reasonMessage: String_.t option }
    let context_ = "CancelConversionRequest"
    let make ?dryRun =
      fun ?reasonMessage ->
        fun ~conversionTaskId ->
          fun () -> { dryRun; reasonMessage; conversionTaskId }
    let to_value x =
      structure_to_value
        [("conversionTaskId",
           (Some (ConversionTaskId.to_value x.conversionTaskId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("reasonMessage", (Option.map x.reasonMessage ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reasonMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reasonMessage") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let conversionTaskId =
        ConversionTaskId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "conversionTaskId") in
      make ?reasonMessage ?dryRun ~conversionTaskId ()
    let of_json json =
      let reasonMessage = field_map json "ReasonMessage" String_.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let conversionTaskId =
        field_map_exn json "ConversionTaskId" ConversionTaskId.of_json in
      make ?reasonMessage ?dryRun ~conversionTaskId ()
    let to_json v = composed_to_json to_value v
  end
module CancelCapacityReservationResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module CancelCapacityReservationRequest =
  struct
    type nonrec t =
      {
      capacityReservationId: CapacityReservationId.t ;
      dryRun: Boolean.t option }
    let context_ = "CancelCapacityReservationRequest"
    let make ?dryRun =
      fun ~capacityReservationId ->
        fun () -> { dryRun; capacityReservationId }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      make ?dryRun ~capacityReservationId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let capacityReservationId =
        field_map_exn json "CapacityReservationId"
          CapacityReservationId.of_json in
      make ?dryRun ~capacityReservationId ()
    let to_json v = composed_to_json to_value v
  end
module CancelCapacityReservationFleetsResult =
  struct
    type nonrec t =
      {
      successfulFleetCancellations:
        CapacityReservationFleetCancellationStateSet.t option ;
      failedFleetCancellations:
        FailedCapacityReservationFleetCancellationResultSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulFleetCancellations =
      fun ?failedFleetCancellations ->
        fun () -> { successfulFleetCancellations; failedFleetCancellations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulFleetCancellationSet",
           (Option.map x.successfulFleetCancellations
              ~f:CapacityReservationFleetCancellationStateSet.to_value));
        ("failedFleetCancellationSet",
          (Option.map x.failedFleetCancellations
             ~f:FailedCapacityReservationFleetCancellationResultSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedFleetCancellations =
        (Option.map
           ~f:FailedCapacityReservationFleetCancellationResultSet.of_xml)
          (Xml.child xml_arg0 "failedFleetCancellationSet") in
      let successfulFleetCancellations =
        (Option.map ~f:CapacityReservationFleetCancellationStateSet.of_xml)
          (Xml.child xml_arg0 "successfulFleetCancellationSet") in
      make ?failedFleetCancellations ?successfulFleetCancellations ()
    let of_json json =
      let failedFleetCancellations =
        field_map json "FailedFleetCancellations"
          FailedCapacityReservationFleetCancellationResultSet.of_json in
      let successfulFleetCancellations =
        field_map json "SuccessfulFleetCancellations"
          CapacityReservationFleetCancellationStateSet.of_json in
      make ?failedFleetCancellations ?successfulFleetCancellations ()
    let to_json v = composed_to_json to_value v
  end
module CancelCapacityReservationFleetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      capacityReservationFleetIds: CapacityReservationFleetIdSet.t }
    let context_ = "CancelCapacityReservationFleetsRequest"
    let make ?dryRun =
      fun ~capacityReservationFleetIds ->
        fun () -> { dryRun; capacityReservationFleetIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CapacityReservationFleetId",
          (Some
             (CapacityReservationFleetIdSet.to_value
                x.capacityReservationFleetIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationFleetIds =
        CapacityReservationFleetIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CapacityReservationFleetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~capacityReservationFleetIds ?dryRun ()
    let of_json json =
      let capacityReservationFleetIds =
        field_map_exn json "CapacityReservationFleetIds"
          CapacityReservationFleetIdSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~capacityReservationFleetIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module CancelBundleTaskResult =
  struct
    type nonrec t = {
      bundleTask: BundleTask.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?bundleTask = fun () -> { bundleTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleInstanceTask",
           (Option.map x.bundleTask ~f:BundleTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleTask =
        (Option.map ~f:BundleTask.of_xml)
          (Xml.child xml_arg0 "bundleInstanceTask") in
      make ?bundleTask ()
    let of_json json =
      let bundleTask = field_map json "BundleTask" BundleTask.of_json in
      make ?bundleTask ()
    let to_json v = composed_to_json to_value v
  end
module CancelBundleTaskRequest =
  struct
    type nonrec t = {
      bundleId: BundleId.t ;
      dryRun: Boolean.t option }
    let context_ = "CancelBundleTaskRequest"
    let make ?dryRun = fun ~bundleId -> fun () -> { dryRun; bundleId }
    let to_value x =
      structure_to_value
        [("BundleId", (Some (BundleId.to_value x.bundleId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let bundleId =
        BundleId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "BundleId") in
      make ?dryRun ~bundleId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let bundleId = field_map_exn json "BundleId" BundleId.of_json in
      make ?dryRun ~bundleId ()
    let to_json v = composed_to_json to_value v
  end
module BundleInstanceResult =
  struct
    type nonrec t = {
      bundleTask: BundleTask.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?bundleTask = fun () -> { bundleTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleInstanceTask",
           (Option.map x.bundleTask ~f:BundleTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleTask =
        (Option.map ~f:BundleTask.of_xml)
          (Xml.child xml_arg0 "bundleInstanceTask") in
      make ?bundleTask ()
    let of_json json =
      let bundleTask = field_map json "BundleTask" BundleTask.of_json in
      make ?bundleTask ()
    let to_json v = composed_to_json to_value v
  end
module BundleInstanceRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t ;
      storage: Storage.t ;
      dryRun: Boolean.t option }
    let context_ = "BundleInstanceRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~storage -> fun () -> { dryRun; instanceId; storage }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("Storage", (Some (Storage.to_value x.storage)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let storage =
        Storage.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Storage") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~storage ~instanceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let storage = field_map_exn json "Storage" Storage.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      make ?dryRun ~storage ~instanceId ()
    let to_json v = composed_to_json to_value v
  end
module AuthorizeSecurityGroupIngressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option ;
      securityGroupRules: SecurityGroupRuleList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?securityGroupRules -> fun () -> { return; securityGroupRules }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("securityGroupRuleSet",
          (Option.map x.securityGroupRules ~f:SecurityGroupRuleList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRules =
        (Option.map ~f:SecurityGroupRuleList.of_xml)
          (Xml.child xml_arg0 "securityGroupRuleSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?securityGroupRules ?return ()
    let of_json json =
      let securityGroupRules =
        field_map json "SecurityGroupRules" SecurityGroupRuleList.of_json in
      let return = field_map json "Return" Boolean.of_json in
      make ?securityGroupRules ?return ()
    let to_json v = composed_to_json to_value v
  end
module AuthorizeSecurityGroupIngressRequest =
  struct
    type nonrec t =
      {
      cidrIp: String_.t option ;
      fromPort: Integer.t option ;
      groupId: SecurityGroupId.t option ;
      groupName: SecurityGroupName.t option ;
      ipPermissions: IpPermissionList.t option ;
      ipProtocol: String_.t option ;
      sourceSecurityGroupName: String_.t option ;
      sourceSecurityGroupOwnerId: String_.t option ;
      toPort: Integer.t option ;
      dryRun: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?cidrIp =
      fun ?fromPort ->
        fun ?groupId ->
          fun ?groupName ->
            fun ?ipPermissions ->
              fun ?ipProtocol ->
                fun ?sourceSecurityGroupName ->
                  fun ?sourceSecurityGroupOwnerId ->
                    fun ?toPort ->
                      fun ?dryRun ->
                        fun ?tagSpecifications ->
                          fun () ->
                            {
                              cidrIp;
                              fromPort;
                              groupId;
                              groupName;
                              ipPermissions;
                              ipProtocol;
                              sourceSecurityGroupName;
                              sourceSecurityGroupOwnerId;
                              toPort;
                              dryRun;
                              tagSpecifications
                            }
    let to_value x =
      structure_to_value
        [("CidrIp", (Option.map x.cidrIp ~f:String_.to_value));
        ("FromPort", (Option.map x.fromPort ~f:Integer.to_value));
        ("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("IpPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("IpProtocol", (Option.map x.ipProtocol ~f:String_.to_value));
        ("SourceSecurityGroupName",
          (Option.map x.sourceSecurityGroupName ~f:String_.to_value));
        ("SourceSecurityGroupOwnerId",
          (Option.map x.sourceSecurityGroupOwnerId ~f:String_.to_value));
        ("ToPort", (Option.map x.toPort ~f:Integer.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let toPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "ToPort") in
      let sourceSecurityGroupOwnerId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroupOwnerId") in
      let sourceSecurityGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroupName") in
      let ipProtocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "IpProtocol") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "IpPermissions") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      let fromPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "FromPort") in
      let cidrIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrIp") in
      make ?tagSpecifications ?dryRun ?toPort ?sourceSecurityGroupOwnerId
        ?sourceSecurityGroupName ?ipProtocol ?ipPermissions ?groupName
        ?groupId ?fromPort ?cidrIp ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let toPort = field_map json "ToPort" Integer.of_json in
      let sourceSecurityGroupOwnerId =
        field_map json "SourceSecurityGroupOwnerId" String_.of_json in
      let sourceSecurityGroupName =
        field_map json "SourceSecurityGroupName" String_.of_json in
      let ipProtocol = field_map json "IpProtocol" String_.of_json in
      let ipPermissions =
        field_map json "IpPermissions" IpPermissionList.of_json in
      let groupName = field_map json "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json "GroupId" SecurityGroupId.of_json in
      let fromPort = field_map json "FromPort" Integer.of_json in
      let cidrIp = field_map json "CidrIp" String_.of_json in
      make ?tagSpecifications ?dryRun ?toPort ?sourceSecurityGroupOwnerId
        ?sourceSecurityGroupName ?ipProtocol ?ipPermissions ?groupName
        ?groupId ?fromPort ?cidrIp ()
    let to_json v = composed_to_json to_value v
  end
module AuthorizeSecurityGroupEgressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option ;
      securityGroupRules: SecurityGroupRuleList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?securityGroupRules -> fun () -> { return; securityGroupRules }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("securityGroupRuleSet",
          (Option.map x.securityGroupRules ~f:SecurityGroupRuleList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRules =
        (Option.map ~f:SecurityGroupRuleList.of_xml)
          (Xml.child xml_arg0 "securityGroupRuleSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?securityGroupRules ?return ()
    let of_json json =
      let securityGroupRules =
        field_map json "SecurityGroupRules" SecurityGroupRuleList.of_json in
      let return = field_map json "Return" Boolean.of_json in
      make ?securityGroupRules ?return ()
    let to_json v = composed_to_json to_value v
  end
module AuthorizeSecurityGroupEgressRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      groupId: SecurityGroupId.t ;
      ipPermissions: IpPermissionList.t option ;
      tagSpecifications: TagSpecificationList.t option ;
      cidrIp: String_.t option ;
      fromPort: Integer.t option ;
      ipProtocol: String_.t option ;
      toPort: Integer.t option ;
      sourceSecurityGroupName: String_.t option ;
      sourceSecurityGroupOwnerId: String_.t option }
    let context_ = "AuthorizeSecurityGroupEgressRequest"
    let make ?dryRun =
      fun ?ipPermissions ->
        fun ?tagSpecifications ->
          fun ?cidrIp ->
            fun ?fromPort ->
              fun ?ipProtocol ->
                fun ?toPort ->
                  fun ?sourceSecurityGroupName ->
                    fun ?sourceSecurityGroupOwnerId ->
                      fun ~groupId ->
                        fun () ->
                          {
                            dryRun;
                            ipPermissions;
                            tagSpecifications;
                            cidrIp;
                            fromPort;
                            ipProtocol;
                            toPort;
                            sourceSecurityGroupName;
                            sourceSecurityGroupOwnerId;
                            groupId
                          }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupId", (Some (SecurityGroupId.to_value x.groupId)));
        ("ipPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("cidrIp", (Option.map x.cidrIp ~f:String_.to_value));
        ("fromPort", (Option.map x.fromPort ~f:Integer.to_value));
        ("ipProtocol", (Option.map x.ipProtocol ~f:String_.to_value));
        ("toPort", (Option.map x.toPort ~f:Integer.to_value));
        ("sourceSecurityGroupName",
          (Option.map x.sourceSecurityGroupName ~f:String_.to_value));
        ("sourceSecurityGroupOwnerId",
          (Option.map x.sourceSecurityGroupOwnerId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceSecurityGroupOwnerId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceSecurityGroupOwnerId") in
      let sourceSecurityGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceSecurityGroupName") in
      let toPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "toPort") in
      let ipProtocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipProtocol") in
      let fromPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "fromPort") in
      let cidrIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cidrIp") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "ipPermissions") in
      let groupId =
        SecurityGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?sourceSecurityGroupOwnerId ?sourceSecurityGroupName ?toPort
        ?ipProtocol ?fromPort ?cidrIp ?tagSpecifications ?ipPermissions
        ~groupId ?dryRun ()
    let of_json json =
      let sourceSecurityGroupOwnerId =
        field_map json "SourceSecurityGroupOwnerId" String_.of_json in
      let sourceSecurityGroupName =
        field_map json "SourceSecurityGroupName" String_.of_json in
      let toPort = field_map json "ToPort" Integer.of_json in
      let ipProtocol = field_map json "IpProtocol" String_.of_json in
      let fromPort = field_map json "FromPort" Integer.of_json in
      let cidrIp = field_map json "CidrIp" String_.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let ipPermissions =
        field_map json "IpPermissions" IpPermissionList.of_json in
      let groupId = field_map_exn json "GroupId" SecurityGroupId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?sourceSecurityGroupOwnerId ?sourceSecurityGroupName ?toPort
        ?ipProtocol ?fromPort ?cidrIp ?tagSpecifications ?ipPermissions
        ~groupId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AuthorizeClientVpnIngressResult =
  struct
    type nonrec t = {
      status: ClientVpnAuthorizationRuleStatus.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status",
           (Option.map x.status ~f:ClientVpnAuthorizationRuleStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnAuthorizationRuleStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status =
        field_map json "Status" ClientVpnAuthorizationRuleStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module AuthorizeClientVpnIngressRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      targetNetworkCidr: String_.t ;
      accessGroupId: String_.t option ;
      authorizeAllGroups: Boolean.t option ;
      description: String_.t option ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "AuthorizeClientVpnIngressRequest"
    let make ?accessGroupId =
      fun ?authorizeAllGroups ->
        fun ?description ->
          fun ?clientToken ->
            fun ?dryRun ->
              fun ~clientVpnEndpointId ->
                fun ~targetNetworkCidr ->
                  fun () ->
                    {
                      accessGroupId;
                      authorizeAllGroups;
                      description;
                      clientToken;
                      dryRun;
                      clientVpnEndpointId;
                      targetNetworkCidr
                    }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("TargetNetworkCidr", (Some (String_.to_value x.targetNetworkCidr)));
        ("AccessGroupId", (Option.map x.accessGroupId ~f:String_.to_value));
        ("AuthorizeAllGroups",
          (Option.map x.authorizeAllGroups ~f:Boolean.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let authorizeAllGroups =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AuthorizeAllGroups") in
      let accessGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccessGroupId") in
      let targetNetworkCidr =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetNetworkCidr") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?clientToken ?description ?authorizeAllGroups
        ?accessGroupId ~targetNetworkCidr ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let authorizeAllGroups =
        field_map json "AuthorizeAllGroups" Boolean.of_json in
      let accessGroupId = field_map json "AccessGroupId" String_.of_json in
      let targetNetworkCidr =
        field_map_exn json "TargetNetworkCidr" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?clientToken ?description ?authorizeAllGroups
        ?accessGroupId ~targetNetworkCidr ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module AttachVpnGatewayResult =
  struct
    type nonrec t = {
      vpcAttachment: VpcAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcAttachment = fun () -> { vpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachment",
           (Option.map x.vpcAttachment ~f:VpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcAttachment =
        (Option.map ~f:VpcAttachment.of_xml)
          (Xml.child xml_arg0 "attachment") in
      make ?vpcAttachment ()
    let of_json json =
      let vpcAttachment =
        field_map json "VpcAttachment" VpcAttachment.of_json in
      make ?vpcAttachment ()
    let to_json v = composed_to_json to_value v
  end
module AttachVpnGatewayRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t ;
      vpnGatewayId: VpnGatewayId.t ;
      dryRun: Boolean.t option }
    let context_ = "AttachVpnGatewayRequest"
    let make ?dryRun =
      fun ~vpcId ->
        fun ~vpnGatewayId -> fun () -> { dryRun; vpcId; vpnGatewayId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("VpnGatewayId", (Some (VpnGatewayId.to_value x.vpnGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnGatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnGatewayId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?dryRun ~vpnGatewayId ~vpcId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpnGatewayId =
        field_map_exn json "VpnGatewayId" VpnGatewayId.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      make ?dryRun ~vpnGatewayId ~vpcId ()
    let to_json v = composed_to_json to_value v
  end
module AttachVolumeRequest =
  struct
    type nonrec t =
      {
      device: String_.t ;
      instanceId: InstanceId.t ;
      volumeId: VolumeId.t ;
      dryRun: Boolean.t option }
    let context_ = "AttachVolumeRequest"
    let make ?dryRun =
      fun ~device ->
        fun ~instanceId ->
          fun ~volumeId -> fun () -> { dryRun; device; instanceId; volumeId }
    let to_value x =
      structure_to_value
        [("Device", (Some (String_.to_value x.device)));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let device =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Device") in
      make ?dryRun ~volumeId ~instanceId ~device ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let volumeId = field_map_exn json "VolumeId" VolumeId.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let device = field_map_exn json "Device" String_.of_json in
      make ?dryRun ~volumeId ~instanceId ~device ()
    let to_json v = composed_to_json to_value v
  end
module AttachNetworkInterfaceResult =
  struct
    type nonrec t =
      {
      attachmentId: String_.t option ;
      networkCardIndex: Integer.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?attachmentId =
      fun ?networkCardIndex -> fun () -> { attachmentId; networkCardIndex }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachmentId", (Option.map x.attachmentId ~f:String_.to_value));
        ("networkCardIndex",
          (Option.map x.networkCardIndex ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkCardIndex =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "networkCardIndex") in
      let attachmentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "attachmentId") in
      make ?networkCardIndex ?attachmentId ()
    let of_json json =
      let networkCardIndex =
        field_map json "NetworkCardIndex" Integer.of_json in
      let attachmentId = field_map json "AttachmentId" String_.of_json in
      make ?networkCardIndex ?attachmentId ()
    let to_json v = composed_to_json to_value v
  end
module AttachNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      deviceIndex: Integer.t ;
      dryRun: Boolean.t option ;
      instanceId: InstanceId.t ;
      networkInterfaceId: NetworkInterfaceId.t ;
      networkCardIndex: Integer.t option }
    let context_ = "AttachNetworkInterfaceRequest"
    let make ?dryRun =
      fun ?networkCardIndex ->
        fun ~deviceIndex ->
          fun ~instanceId ->
            fun ~networkInterfaceId ->
              fun () ->
                {
                  dryRun;
                  networkCardIndex;
                  deviceIndex;
                  instanceId;
                  networkInterfaceId
                }
    let to_value x =
      structure_to_value
        [("deviceIndex", (Some (Integer.to_value x.deviceIndex)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("NetworkCardIndex",
          (Option.map x.networkCardIndex ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkCardIndex =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NetworkCardIndex") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let deviceIndex =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "deviceIndex") in
      make ?networkCardIndex ~networkInterfaceId ~instanceId ?dryRun
        ~deviceIndex ()
    let of_json json =
      let networkCardIndex =
        field_map json "NetworkCardIndex" Integer.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let deviceIndex = field_map_exn json "DeviceIndex" Integer.of_json in
      make ?networkCardIndex ~networkInterfaceId ~instanceId ?dryRun
        ~deviceIndex ()
    let to_json v = composed_to_json to_value v
  end
module AttachInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      internetGatewayId: InternetGatewayId.t ;
      vpcId: VpcId.t }
    let context_ = "AttachInternetGatewayRequest"
    let make ?dryRun =
      fun ~internetGatewayId ->
        fun ~vpcId -> fun () -> { dryRun; internetGatewayId; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Some (InternetGatewayId.to_value x.internetGatewayId)));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let internetGatewayId =
        InternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ~internetGatewayId ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let internetGatewayId =
        field_map_exn json "InternetGatewayId" InternetGatewayId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ~internetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AttachClassicLinkVpcResult =
  struct
    type nonrec t = {
      return: Boolean.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_json json =
      let return = field_map json "Return" Boolean.of_json in make ?return ()
    let to_json v = composed_to_json to_value v
  end
module AttachClassicLinkVpcRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      groups: GroupIdStringList.t ;
      instanceId: InstanceId.t ;
      vpcId: VpcId.t }
    let context_ = "AttachClassicLinkVpcRequest"
    let make ?dryRun =
      fun ~groups ->
        fun ~instanceId ->
          fun ~vpcId -> fun () -> { dryRun; groups; instanceId; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SecurityGroupId", (Some (GroupIdStringList.to_value x.groups)));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let groups =
        GroupIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ~instanceId ~groups ?dryRun ()
    let of_json json =
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let groups = field_map_exn json "Groups" GroupIdStringList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcId ~instanceId ~groups ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AssociateVpcCidrBlockResult =
  struct
    type nonrec t =
      {
      ipv6CidrBlockAssociation: VpcIpv6CidrBlockAssociation.t option ;
      cidrBlockAssociation: VpcCidrBlockAssociation.t option ;
      vpcId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrBlockAssociation =
      fun ?cidrBlockAssociation ->
        fun ?vpcId ->
          fun () -> { ipv6CidrBlockAssociation; cidrBlockAssociation; vpcId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrBlockAssociation",
           (Option.map x.ipv6CidrBlockAssociation
              ~f:VpcIpv6CidrBlockAssociation.to_value));
        ("cidrBlockAssociation",
          (Option.map x.cidrBlockAssociation
             ~f:VpcCidrBlockAssociation.to_value));
        ("vpcId", (Option.map x.vpcId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "vpcId") in
      let cidrBlockAssociation =
        (Option.map ~f:VpcCidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "cidrBlockAssociation") in
      let ipv6CidrBlockAssociation =
        (Option.map ~f:VpcIpv6CidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "ipv6CidrBlockAssociation") in
      make ?vpcId ?cidrBlockAssociation ?ipv6CidrBlockAssociation ()
    let of_json json =
      let vpcId = field_map json "VpcId" String_.of_json in
      let cidrBlockAssociation =
        field_map json "CidrBlockAssociation" VpcCidrBlockAssociation.of_json in
      let ipv6CidrBlockAssociation =
        field_map json "Ipv6CidrBlockAssociation"
          VpcIpv6CidrBlockAssociation.of_json in
      make ?vpcId ?cidrBlockAssociation ?ipv6CidrBlockAssociation ()
    let to_json v = composed_to_json to_value v
  end
module AssociateVpcCidrBlockRequest =
  struct
    type nonrec t =
      {
      amazonProvidedIpv6CidrBlock: Boolean.t option ;
      cidrBlock: String_.t option ;
      vpcId: VpcId.t ;
      ipv6CidrBlockNetworkBorderGroup: String_.t option ;
      ipv6Pool: Ipv6PoolEc2Id.t option ;
      ipv6CidrBlock: String_.t option ;
      ipv4IpamPoolId: IpamPoolId.t option ;
      ipv4NetmaskLength: NetmaskLength.t option ;
      ipv6IpamPoolId: IpamPoolId.t option ;
      ipv6NetmaskLength: NetmaskLength.t option }
    let context_ = "AssociateVpcCidrBlockRequest"
    let make ?amazonProvidedIpv6CidrBlock =
      fun ?cidrBlock ->
        fun ?ipv6CidrBlockNetworkBorderGroup ->
          fun ?ipv6Pool ->
            fun ?ipv6CidrBlock ->
              fun ?ipv4IpamPoolId ->
                fun ?ipv4NetmaskLength ->
                  fun ?ipv6IpamPoolId ->
                    fun ?ipv6NetmaskLength ->
                      fun ~vpcId ->
                        fun () ->
                          {
                            amazonProvidedIpv6CidrBlock;
                            cidrBlock;
                            ipv6CidrBlockNetworkBorderGroup;
                            ipv6Pool;
                            ipv6CidrBlock;
                            ipv4IpamPoolId;
                            ipv4NetmaskLength;
                            ipv6IpamPoolId;
                            ipv6NetmaskLength;
                            vpcId
                          }
    let to_value x =
      structure_to_value
        [("amazonProvidedIpv6CidrBlock",
           (Option.map x.amazonProvidedIpv6CidrBlock ~f:Boolean.to_value));
        ("CidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)));
        ("Ipv6CidrBlockNetworkBorderGroup",
          (Option.map x.ipv6CidrBlockNetworkBorderGroup ~f:String_.to_value));
        ("Ipv6Pool", (Option.map x.ipv6Pool ~f:Ipv6PoolEc2Id.to_value));
        ("Ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("Ipv4IpamPoolId",
          (Option.map x.ipv4IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv4NetmaskLength",
          (Option.map x.ipv4NetmaskLength ~f:NetmaskLength.to_value));
        ("Ipv6IpamPoolId",
          (Option.map x.ipv6IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv6NetmaskLength",
          (Option.map x.ipv6NetmaskLength ~f:NetmaskLength.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv6NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv6NetmaskLength") in
      let ipv6IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv6IpamPoolId") in
      let ipv4NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv4NetmaskLength") in
      let ipv4IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv4IpamPoolId") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ipv6CidrBlock") in
      let ipv6Pool =
        (Option.map ~f:Ipv6PoolEc2Id.of_xml) (Xml.child xml_arg0 "Ipv6Pool") in
      let ipv6CidrBlockNetworkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "Ipv6CidrBlockNetworkBorderGroup") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      let amazonProvidedIpv6CidrBlock =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "amazonProvidedIpv6CidrBlock") in
      make ?ipv6NetmaskLength ?ipv6IpamPoolId ?ipv4NetmaskLength
        ?ipv4IpamPoolId ?ipv6CidrBlock ?ipv6Pool
        ?ipv6CidrBlockNetworkBorderGroup ~vpcId ?cidrBlock
        ?amazonProvidedIpv6CidrBlock ()
    let of_json json =
      let ipv6NetmaskLength =
        field_map json "Ipv6NetmaskLength" NetmaskLength.of_json in
      let ipv6IpamPoolId = field_map json "Ipv6IpamPoolId" IpamPoolId.of_json in
      let ipv4NetmaskLength =
        field_map json "Ipv4NetmaskLength" NetmaskLength.of_json in
      let ipv4IpamPoolId = field_map json "Ipv4IpamPoolId" IpamPoolId.of_json in
      let ipv6CidrBlock = field_map json "Ipv6CidrBlock" String_.of_json in
      let ipv6Pool = field_map json "Ipv6Pool" Ipv6PoolEc2Id.of_json in
      let ipv6CidrBlockNetworkBorderGroup =
        field_map json "Ipv6CidrBlockNetworkBorderGroup" String_.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let cidrBlock = field_map json "CidrBlock" String_.of_json in
      let amazonProvidedIpv6CidrBlock =
        field_map json "AmazonProvidedIpv6CidrBlock" Boolean.of_json in
      make ?ipv6NetmaskLength ?ipv6IpamPoolId ?ipv4NetmaskLength
        ?ipv4IpamPoolId ?ipv6CidrBlock ?ipv6Pool
        ?ipv6CidrBlockNetworkBorderGroup ~vpcId ?cidrBlock
        ?amazonProvidedIpv6CidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTrunkInterfaceResult =
  struct
    type nonrec t =
      {
      interfaceAssociation: TrunkInterfaceAssociation.t option ;
      clientToken: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?interfaceAssociation =
      fun ?clientToken -> fun () -> { interfaceAssociation; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("interfaceAssociation",
           (Option.map x.interfaceAssociation
              ~f:TrunkInterfaceAssociation.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let interfaceAssociation =
        (Option.map ~f:TrunkInterfaceAssociation.of_xml)
          (Xml.child xml_arg0 "interfaceAssociation") in
      make ?clientToken ?interfaceAssociation ()
    let of_json json =
      let clientToken = field_map json "ClientToken" String_.of_json in
      let interfaceAssociation =
        field_map json "InterfaceAssociation"
          TrunkInterfaceAssociation.of_json in
      make ?clientToken ?interfaceAssociation ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTrunkInterfaceRequest =
  struct
    type nonrec t =
      {
      branchInterfaceId: NetworkInterfaceId.t ;
      trunkInterfaceId: NetworkInterfaceId.t ;
      vlanId: Integer.t option ;
      greKey: Integer.t option ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "AssociateTrunkInterfaceRequest"
    let make ?vlanId =
      fun ?greKey ->
        fun ?clientToken ->
          fun ?dryRun ->
            fun ~branchInterfaceId ->
              fun ~trunkInterfaceId ->
                fun () ->
                  {
                    vlanId;
                    greKey;
                    clientToken;
                    dryRun;
                    branchInterfaceId;
                    trunkInterfaceId
                  }
    let to_value x =
      structure_to_value
        [("BranchInterfaceId",
           (Some (NetworkInterfaceId.to_value x.branchInterfaceId)));
        ("TrunkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.trunkInterfaceId)));
        ("VlanId", (Option.map x.vlanId ~f:Integer.to_value));
        ("GreKey", (Option.map x.greKey ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let greKey =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "GreKey") in
      let vlanId =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "VlanId") in
      let trunkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrunkInterfaceId") in
      let branchInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BranchInterfaceId") in
      make ?dryRun ?clientToken ?greKey ?vlanId ~trunkInterfaceId
        ~branchInterfaceId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let greKey = field_map json "GreKey" Integer.of_json in
      let vlanId = field_map json "VlanId" Integer.of_json in
      let trunkInterfaceId =
        field_map_exn json "TrunkInterfaceId" NetworkInterfaceId.of_json in
      let branchInterfaceId =
        field_map_exn json "BranchInterfaceId" NetworkInterfaceId.of_json in
      make ?dryRun ?clientToken ?greKey ?vlanId ~trunkInterfaceId
        ~branchInterfaceId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTransitGatewayRouteTableResult =
  struct
    type nonrec t = {
      association: TransitGatewayAssociation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?association = fun () -> { association }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("association",
           (Option.map x.association ~f:TransitGatewayAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let association =
        (Option.map ~f:TransitGatewayAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      make ?association ()
    let of_json json =
      let association =
        field_map json "Association" TransitGatewayAssociation.of_json in
      make ?association ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "AssociateTransitGatewayRouteTableRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~transitGatewayAttachmentId ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; transitGatewayAttachmentId
            }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayMulticastDomainAssociations.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations = fun () -> { associations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayMulticastDomainAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociations.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?associations ()
    let of_json json =
      let associations =
        field_map json "Associations"
          TransitGatewayMulticastDomainAssociations.of_json in
      make ?associations ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option ;
      subnetIds: TransitGatewaySubnetIdList.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayMulticastDomainId =
      fun ?transitGatewayAttachmentId ->
        fun ?subnetIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                transitGatewayAttachmentId;
                subnetIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("SubnetIds",
          (Option.map x.subnetIds ~f:TransitGatewaySubnetIdList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetIds =
        (Option.map ~f:TransitGatewaySubnetIdList.of_xml)
          (Xml.child xml_arg0 "SubnetIds") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetIds =
        field_map json "SubnetIds" TransitGatewaySubnetIdList.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateSubnetCidrBlockResult =
  struct
    type nonrec t =
      {
      ipv6CidrBlockAssociation: SubnetIpv6CidrBlockAssociation.t option ;
      subnetId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrBlockAssociation =
      fun ?subnetId -> fun () -> { ipv6CidrBlockAssociation; subnetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrBlockAssociation",
           (Option.map x.ipv6CidrBlockAssociation
              ~f:SubnetIpv6CidrBlockAssociation.to_value));
        ("subnetId", (Option.map x.subnetId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnetId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "subnetId") in
      let ipv6CidrBlockAssociation =
        (Option.map ~f:SubnetIpv6CidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "ipv6CidrBlockAssociation") in
      make ?subnetId ?ipv6CidrBlockAssociation ()
    let of_json json =
      let subnetId = field_map json "SubnetId" String_.of_json in
      let ipv6CidrBlockAssociation =
        field_map json "Ipv6CidrBlockAssociation"
          SubnetIpv6CidrBlockAssociation.of_json in
      make ?subnetId ?ipv6CidrBlockAssociation ()
    let to_json v = composed_to_json to_value v
  end
module AssociateSubnetCidrBlockRequest =
  struct
    type nonrec t = {
      ipv6CidrBlock: String_.t ;
      subnetId: SubnetId.t }
    let context_ = "AssociateSubnetCidrBlockRequest"
    let make ~ipv6CidrBlock =
      fun ~subnetId -> fun () -> { ipv6CidrBlock; subnetId }
    let to_value x =
      structure_to_value
        [("ipv6CidrBlock", (Some (String_.to_value x.ipv6CidrBlock)));
        ("subnetId", (Some (SubnetId.to_value x.subnetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subnetId") in
      let ipv6CidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ipv6CidrBlock") in
      make ~subnetId ~ipv6CidrBlock ()
    let of_json json =
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let ipv6CidrBlock = field_map_exn json "Ipv6CidrBlock" String_.of_json in
      make ~subnetId ~ipv6CidrBlock ()
    let to_json v = composed_to_json to_value v
  end
module AssociateRouteTableResult =
  struct
    type nonrec t =
      {
      associationId: String_.t option ;
      associationState: RouteTableAssociationState.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId =
      fun ?associationState -> fun () -> { associationId; associationState }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value));
        ("associationState",
          (Option.map x.associationState
             ~f:RouteTableAssociationState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationState =
        (Option.map ~f:RouteTableAssociationState.of_xml)
          (Xml.child xml_arg0 "associationState") in
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?associationState ?associationId ()
    let of_json json =
      let associationState =
        field_map json "AssociationState" RouteTableAssociationState.of_json in
      let associationId = field_map json "AssociationId" String_.of_json in
      make ?associationState ?associationId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateRouteTableRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      routeTableId: RouteTableId.t ;
      subnetId: SubnetId.t option ;
      gatewayId: RouteGatewayId.t option }
    let context_ = "AssociateRouteTableRequest"
    let make ?dryRun =
      fun ?subnetId ->
        fun ?gatewayId ->
          fun ~routeTableId ->
            fun () -> { dryRun; subnetId; gatewayId; routeTableId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("subnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("GatewayId", (Option.map x.gatewayId ~f:RouteGatewayId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gatewayId =
        (Option.map ~f:RouteGatewayId.of_xml)
          (Xml.child xml_arg0 "GatewayId") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "subnetId") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?gatewayId ?subnetId ~routeTableId ?dryRun ()
    let of_json json =
      let gatewayId = field_map json "GatewayId" RouteGatewayId.of_json in
      let subnetId = field_map json "SubnetId" SubnetId.of_json in
      let routeTableId =
        field_map_exn json "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?gatewayId ?subnetId ~routeTableId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AssociateInstanceEventWindowResult =
  struct
    type nonrec t = {
      instanceEventWindow: InstanceEventWindow.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindow = fun () -> { instanceEventWindow }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindow",
           (Option.map x.instanceEventWindow ~f:InstanceEventWindow.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindow =
        (Option.map ~f:InstanceEventWindow.of_xml)
          (Xml.child xml_arg0 "instanceEventWindow") in
      make ?instanceEventWindow ()
    let of_json json =
      let instanceEventWindow =
        field_map json "InstanceEventWindow" InstanceEventWindow.of_json in
      make ?instanceEventWindow ()
    let to_json v = composed_to_json to_value v
  end
module AssociateInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      instanceEventWindowId: InstanceEventWindowId.t ;
      associationTarget: InstanceEventWindowAssociationRequest.t }
    let context_ = "AssociateInstanceEventWindowRequest"
    let make ?dryRun =
      fun ~instanceEventWindowId ->
        fun ~associationTarget ->
          fun () -> { dryRun; instanceEventWindowId; associationTarget }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Some (InstanceEventWindowId.to_value x.instanceEventWindowId)));
        ("AssociationTarget",
          (Some
             (InstanceEventWindowAssociationRequest.to_value
                x.associationTarget)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationTarget =
        InstanceEventWindowAssociationRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationTarget") in
      let instanceEventWindowId =
        InstanceEventWindowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventWindowId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~associationTarget ~instanceEventWindowId ?dryRun ()
    let of_json json =
      let associationTarget =
        field_map_exn json "AssociationTarget"
          InstanceEventWindowAssociationRequest.of_json in
      let instanceEventWindowId =
        field_map_exn json "InstanceEventWindowId"
          InstanceEventWindowId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~associationTarget ~instanceEventWindowId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AssociateIamInstanceProfileResult =
  struct
    type nonrec t =
      {
      iamInstanceProfileAssociation: IamInstanceProfileAssociation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?iamInstanceProfileAssociation =
      fun () -> { iamInstanceProfileAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("iamInstanceProfileAssociation",
           (Option.map x.iamInstanceProfileAssociation
              ~f:IamInstanceProfileAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let iamInstanceProfileAssociation =
        (Option.map ~f:IamInstanceProfileAssociation.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfileAssociation") in
      make ?iamInstanceProfileAssociation ()
    let of_json json =
      let iamInstanceProfileAssociation =
        field_map json "IamInstanceProfileAssociation"
          IamInstanceProfileAssociation.of_json in
      make ?iamInstanceProfileAssociation ()
    let to_json v = composed_to_json to_value v
  end
module AssociateIamInstanceProfileRequest =
  struct
    type nonrec t =
      {
      iamInstanceProfile: IamInstanceProfileSpecification.t ;
      instanceId: InstanceId.t }
    let context_ = "AssociateIamInstanceProfileRequest"
    let make ~iamInstanceProfile =
      fun ~instanceId -> fun () -> { iamInstanceProfile; instanceId }
    let to_value x =
      structure_to_value
        [("IamInstanceProfile",
           (Some
              (IamInstanceProfileSpecification.to_value x.iamInstanceProfile)));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let iamInstanceProfile =
        IamInstanceProfileSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamInstanceProfile") in
      make ~instanceId ~iamInstanceProfile ()
    let of_json json =
      let instanceId = field_map_exn json "InstanceId" InstanceId.of_json in
      let iamInstanceProfile =
        field_map_exn json "IamInstanceProfile"
          IamInstanceProfileSpecification.of_json in
      make ~instanceId ~iamInstanceProfile ()
    let to_json v = composed_to_json to_value v
  end
module AssociateEnclaveCertificateIamRoleResult =
  struct
    type nonrec t =
      {
      certificateS3BucketName: String_.t option ;
      certificateS3ObjectKey: String_.t option ;
      encryptionKmsKeyId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?certificateS3BucketName =
      fun ?certificateS3ObjectKey ->
        fun ?encryptionKmsKeyId ->
          fun () ->
            {
              certificateS3BucketName;
              certificateS3ObjectKey;
              encryptionKmsKeyId
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("certificateS3BucketName",
           (Option.map x.certificateS3BucketName ~f:String_.to_value));
        ("certificateS3ObjectKey",
          (Option.map x.certificateS3ObjectKey ~f:String_.to_value));
        ("encryptionKmsKeyId",
          (Option.map x.encryptionKmsKeyId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKmsKeyId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "encryptionKmsKeyId") in
      let certificateS3ObjectKey =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateS3ObjectKey") in
      let certificateS3BucketName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateS3BucketName") in
      make ?encryptionKmsKeyId ?certificateS3ObjectKey
        ?certificateS3BucketName ()
    let of_json json =
      let encryptionKmsKeyId =
        field_map json "EncryptionKmsKeyId" String_.of_json in
      let certificateS3ObjectKey =
        field_map json "CertificateS3ObjectKey" String_.of_json in
      let certificateS3BucketName =
        field_map json "CertificateS3BucketName" String_.of_json in
      make ?encryptionKmsKeyId ?certificateS3ObjectKey
        ?certificateS3BucketName ()
    let to_json v = composed_to_json to_value v
  end
module AssociateEnclaveCertificateIamRoleRequest =
  struct
    type nonrec t =
      {
      certificateArn: ResourceArn.t option ;
      roleArn: ResourceArn.t option ;
      dryRun: Boolean.t option }
    let make ?certificateArn =
      fun ?roleArn ->
        fun ?dryRun -> fun () -> { certificateArn; roleArn; dryRun }
    let to_value x =
      structure_to_value
        [("CertificateArn",
           (Option.map x.certificateArn ~f:ResourceArn.to_value));
        ("RoleArn", (Option.map x.roleArn ~f:ResourceArn.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let roleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RoleArn") in
      let certificateArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "CertificateArn") in
      make ?dryRun ?roleArn ?certificateArn ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let roleArn = field_map json "RoleArn" ResourceArn.of_json in
      let certificateArn =
        field_map json "CertificateArn" ResourceArn.of_json in
      make ?dryRun ?roleArn ?certificateArn ()
    let to_json v = composed_to_json to_value v
  end
module AssociateDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpOptionsId: DefaultingDhcpOptionsId.t ;
      vpcId: VpcId.t ;
      dryRun: Boolean.t option }
    let context_ = "AssociateDhcpOptionsRequest"
    let make ?dryRun =
      fun ~dhcpOptionsId ->
        fun ~vpcId -> fun () -> { dryRun; dhcpOptionsId; vpcId }
    let to_value x =
      structure_to_value
        [("DhcpOptionsId",
           (Some (DefaultingDhcpOptionsId.to_value x.dhcpOptionsId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let dhcpOptionsId =
        DefaultingDhcpOptionsId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DhcpOptionsId") in
      make ?dryRun ~vpcId ~dhcpOptionsId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let dhcpOptionsId =
        field_map_exn json "DhcpOptionsId" DefaultingDhcpOptionsId.of_json in
      make ?dryRun ~vpcId ~dhcpOptionsId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateClientVpnTargetNetworkResult =
  struct
    type nonrec t =
      {
      associationId: String_.t option ;
      status: AssociationStatus.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId =
      fun ?status -> fun () -> { associationId; status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:AssociationStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AssociationStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?status ?associationId ()
    let of_json json =
      let status = field_map json "Status" AssociationStatus.of_json in
      let associationId = field_map json "AssociationId" String_.of_json in
      make ?status ?associationId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateClientVpnTargetNetworkRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      subnetId: SubnetId.t ;
      clientToken: String_.t option ;
      dryRun: Boolean.t option }
    let context_ = "AssociateClientVpnTargetNetworkRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~clientVpnEndpointId ->
          fun ~subnetId ->
            fun () -> { clientToken; dryRun; clientVpnEndpointId; subnetId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?clientToken ~subnetId ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ?clientToken ~subnetId ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateAddressResult =
  struct
    type nonrec t = {
      associationId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId = fun () -> { associationId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?associationId ()
    let of_json json =
      let associationId = field_map json "AssociationId" String_.of_json in
      make ?associationId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateAddressRequest =
  struct
    type nonrec t =
      {
      allocationId: AllocationId.t option ;
      instanceId: InstanceId.t option ;
      publicIp: String_.t option ;
      allowReassociation: Boolean.t option ;
      dryRun: Boolean.t option ;
      networkInterfaceId: NetworkInterfaceId.t option ;
      privateIpAddress: String_.t option }
    let make ?allocationId =
      fun ?instanceId ->
        fun ?publicIp ->
          fun ?allowReassociation ->
            fun ?dryRun ->
              fun ?networkInterfaceId ->
                fun ?privateIpAddress ->
                  fun () ->
                    {
                      allocationId;
                      instanceId;
                      publicIp;
                      allowReassociation;
                      dryRun;
                      networkInterfaceId;
                      privateIpAddress
                    }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationId ~f:AllocationId.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("PublicIp", (Option.map x.publicIp ~f:String_.to_value));
        ("allowReassociation",
          (Option.map x.allowReassociation ~f:Boolean.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("privateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let allowReassociation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "allowReassociation") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PublicIp") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let allocationId =
        (Option.map ~f:AllocationId.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?privateIpAddress ?networkInterfaceId ?dryRun ?allowReassociation
        ?publicIp ?instanceId ?allocationId ()
    let of_json json =
      let privateIpAddress =
        field_map json "PrivateIpAddress" String_.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let allowReassociation =
        field_map json "AllowReassociation" Boolean.of_json in
      let publicIp = field_map json "PublicIp" String_.of_json in
      let instanceId = field_map json "InstanceId" InstanceId.of_json in
      let allocationId = field_map json "AllocationId" AllocationId.of_json in
      make ?privateIpAddress ?networkInterfaceId ?dryRun ?allowReassociation
        ?publicIp ?instanceId ?allocationId ()
    let to_json v = composed_to_json to_value v
  end
module AssignPrivateIpAddressesResult =
  struct
    type nonrec t =
      {
      networkInterfaceId: String_.t option ;
      assignedPrivateIpAddresses: AssignedPrivateIpAddressList.t option ;
      assignedIpv4Prefixes: Ipv4PrefixesList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfaceId =
      fun ?assignedPrivateIpAddresses ->
        fun ?assignedIpv4Prefixes ->
          fun () ->
            {
              networkInterfaceId;
              assignedPrivateIpAddresses;
              assignedIpv4Prefixes
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfaceId",
           (Option.map x.networkInterfaceId ~f:String_.to_value));
        ("assignedPrivateIpAddressesSet",
          (Option.map x.assignedPrivateIpAddresses
             ~f:AssignedPrivateIpAddressList.to_value));
        ("assignedIpv4PrefixSet",
          (Option.map x.assignedIpv4Prefixes ~f:Ipv4PrefixesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assignedIpv4Prefixes =
        (Option.map ~f:Ipv4PrefixesList.of_xml)
          (Xml.child xml_arg0 "assignedIpv4PrefixSet") in
      let assignedPrivateIpAddresses =
        (Option.map ~f:AssignedPrivateIpAddressList.of_xml)
          (Xml.child xml_arg0 "assignedPrivateIpAddressesSet") in
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      make ?assignedIpv4Prefixes ?assignedPrivateIpAddresses
        ?networkInterfaceId ()
    let of_json json =
      let assignedIpv4Prefixes =
        field_map json "AssignedIpv4Prefixes" Ipv4PrefixesList.of_json in
      let assignedPrivateIpAddresses =
        field_map json "AssignedPrivateIpAddresses"
          AssignedPrivateIpAddressList.of_json in
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" String_.of_json in
      make ?assignedIpv4Prefixes ?assignedPrivateIpAddresses
        ?networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end
module AssignPrivateIpAddressesRequest =
  struct
    type nonrec t =
      {
      allowReassignment: Boolean.t option ;
      networkInterfaceId: NetworkInterfaceId.t ;
      privateIpAddresses: PrivateIpAddressStringList.t option ;
      secondaryPrivateIpAddressCount: Integer.t option ;
      ipv4Prefixes: IpPrefixList.t option ;
      ipv4PrefixCount: Integer.t option }
    let context_ = "AssignPrivateIpAddressesRequest"
    let make ?allowReassignment =
      fun ?privateIpAddresses ->
        fun ?secondaryPrivateIpAddressCount ->
          fun ?ipv4Prefixes ->
            fun ?ipv4PrefixCount ->
              fun ~networkInterfaceId ->
                fun () ->
                  {
                    allowReassignment;
                    privateIpAddresses;
                    secondaryPrivateIpAddressCount;
                    ipv4Prefixes;
                    ipv4PrefixCount;
                    networkInterfaceId
                  }
    let to_value x =
      structure_to_value
        [("allowReassignment",
           (Option.map x.allowReassignment ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("privateIpAddress",
          (Option.map x.privateIpAddresses
             ~f:PrivateIpAddressStringList.to_value));
        ("secondaryPrivateIpAddressCount",
          (Option.map x.secondaryPrivateIpAddressCount ~f:Integer.to_value));
        ("Ipv4Prefix", (Option.map x.ipv4Prefixes ~f:IpPrefixList.to_value));
        ("Ipv4PrefixCount",
          (Option.map x.ipv4PrefixCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv4PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv4PrefixCount") in
      let ipv4Prefixes =
        (Option.map ~f:IpPrefixList.of_xml) (Xml.child xml_arg0 "Ipv4Prefix") in
      let secondaryPrivateIpAddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "secondaryPrivateIpAddressCount") in
      let privateIpAddresses =
        (Option.map ~f:PrivateIpAddressStringList.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let allowReassignment =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "allowReassignment") in
      make ?ipv4PrefixCount ?ipv4Prefixes ?secondaryPrivateIpAddressCount
        ?privateIpAddresses ~networkInterfaceId ?allowReassignment ()
    let of_json json =
      let ipv4PrefixCount = field_map json "Ipv4PrefixCount" Integer.of_json in
      let ipv4Prefixes = field_map json "Ipv4Prefixes" IpPrefixList.of_json in
      let secondaryPrivateIpAddressCount =
        field_map json "SecondaryPrivateIpAddressCount" Integer.of_json in
      let privateIpAddresses =
        field_map json "PrivateIpAddresses"
          PrivateIpAddressStringList.of_json in
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let allowReassignment =
        field_map json "AllowReassignment" Boolean.of_json in
      make ?ipv4PrefixCount ?ipv4Prefixes ?secondaryPrivateIpAddressCount
        ?privateIpAddresses ~networkInterfaceId ?allowReassignment ()
    let to_json v = composed_to_json to_value v
  end
module AssignIpv6AddressesResult =
  struct
    type nonrec t =
      {
      assignedIpv6Addresses: Ipv6AddressList.t option ;
      assignedIpv6Prefixes: IpPrefixList.t option ;
      networkInterfaceId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?assignedIpv6Addresses =
      fun ?assignedIpv6Prefixes ->
        fun ?networkInterfaceId ->
          fun () ->
            { assignedIpv6Addresses; assignedIpv6Prefixes; networkInterfaceId
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assignedIpv6Addresses",
           (Option.map x.assignedIpv6Addresses ~f:Ipv6AddressList.to_value));
        ("assignedIpv6PrefixSet",
          (Option.map x.assignedIpv6Prefixes ~f:IpPrefixList.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let assignedIpv6Prefixes =
        (Option.map ~f:IpPrefixList.of_xml)
          (Xml.child xml_arg0 "assignedIpv6PrefixSet") in
      let assignedIpv6Addresses =
        (Option.map ~f:Ipv6AddressList.of_xml)
          (Xml.child xml_arg0 "assignedIpv6Addresses") in
      make ?networkInterfaceId ?assignedIpv6Prefixes ?assignedIpv6Addresses
        ()
    let of_json json =
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" String_.of_json in
      let assignedIpv6Prefixes =
        field_map json "AssignedIpv6Prefixes" IpPrefixList.of_json in
      let assignedIpv6Addresses =
        field_map json "AssignedIpv6Addresses" Ipv6AddressList.of_json in
      make ?networkInterfaceId ?assignedIpv6Prefixes ?assignedIpv6Addresses
        ()
    let to_json v = composed_to_json to_value v
  end
module AssignIpv6AddressesRequest =
  struct
    type nonrec t =
      {
      ipv6AddressCount: Integer.t option ;
      ipv6Addresses: Ipv6AddressList.t option ;
      ipv6PrefixCount: Integer.t option ;
      ipv6Prefixes: IpPrefixList.t option ;
      networkInterfaceId: NetworkInterfaceId.t }
    let context_ = "AssignIpv6AddressesRequest"
    let make ?ipv6AddressCount =
      fun ?ipv6Addresses ->
        fun ?ipv6PrefixCount ->
          fun ?ipv6Prefixes ->
            fun ~networkInterfaceId ->
              fun () ->
                {
                  ipv6AddressCount;
                  ipv6Addresses;
                  ipv6PrefixCount;
                  ipv6Prefixes;
                  networkInterfaceId
                }
    let to_value x =
      structure_to_value
        [("ipv6AddressCount",
           (Option.map x.ipv6AddressCount ~f:Integer.to_value));
        ("ipv6Addresses",
          (Option.map x.ipv6Addresses ~f:Ipv6AddressList.to_value));
        ("Ipv6PrefixCount",
          (Option.map x.ipv6PrefixCount ~f:Integer.to_value));
        ("Ipv6Prefix", (Option.map x.ipv6Prefixes ~f:IpPrefixList.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let ipv6Prefixes =
        (Option.map ~f:IpPrefixList.of_xml) (Xml.child xml_arg0 "Ipv6Prefix") in
      let ipv6PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv6PrefixCount") in
      let ipv6Addresses =
        (Option.map ~f:Ipv6AddressList.of_xml)
          (Xml.child xml_arg0 "ipv6Addresses") in
      let ipv6AddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ipv6AddressCount") in
      make ~networkInterfaceId ?ipv6Prefixes ?ipv6PrefixCount ?ipv6Addresses
        ?ipv6AddressCount ()
    let of_json json =
      let networkInterfaceId =
        field_map_exn json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let ipv6Prefixes = field_map json "Ipv6Prefixes" IpPrefixList.of_json in
      let ipv6PrefixCount = field_map json "Ipv6PrefixCount" Integer.of_json in
      let ipv6Addresses =
        field_map json "Ipv6Addresses" Ipv6AddressList.of_json in
      let ipv6AddressCount =
        field_map json "Ipv6AddressCount" Integer.of_json in
      make ~networkInterfaceId ?ipv6Prefixes ?ipv6PrefixCount ?ipv6Addresses
        ?ipv6AddressCount ()
    let to_json v = composed_to_json to_value v
  end
module ApplySecurityGroupsToClientVpnTargetNetworkResult =
  struct
    type nonrec t = {
      securityGroupIds: ClientVpnSecurityGroupIdSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroupIds = fun () -> { securityGroupIds }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupIds",
           (Option.map x.securityGroupIds
              ~f:ClientVpnSecurityGroupIdSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupIds =
        (Option.map ~f:ClientVpnSecurityGroupIdSet.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      make ?securityGroupIds ()
    let of_json json =
      let securityGroupIds =
        field_map json "SecurityGroupIds" ClientVpnSecurityGroupIdSet.of_json in
      make ?securityGroupIds ()
    let to_json v = composed_to_json to_value v
  end
module ApplySecurityGroupsToClientVpnTargetNetworkRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t ;
      vpcId: VpcId.t ;
      securityGroupIds: ClientVpnSecurityGroupIdSet.t ;
      dryRun: Boolean.t option }
    let context_ = "ApplySecurityGroupsToClientVpnTargetNetworkRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId ->
        fun ~vpcId ->
          fun ~securityGroupIds ->
            fun () ->
              { dryRun; clientVpnEndpointId; vpcId; securityGroupIds }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("SecurityGroupId",
          (Some (ClientVpnSecurityGroupIdSet.to_value x.securityGroupIds)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let securityGroupIds =
        ClientVpnSecurityGroupIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~securityGroupIds ~vpcId ~clientVpnEndpointId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let securityGroupIds =
        field_map_exn json "SecurityGroupIds"
          ClientVpnSecurityGroupIdSet.of_json in
      let vpcId = field_map_exn json "VpcId" VpcId.of_json in
      let clientVpnEndpointId =
        field_map_exn json "ClientVpnEndpointId" ClientVpnEndpointId.of_json in
      make ?dryRun ~securityGroupIds ~vpcId ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end
module AllocateIpamPoolCidrResult =
  struct
    type nonrec t = {
      ipamPoolAllocation: IpamPoolAllocation.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolAllocation = fun () -> { ipamPoolAllocation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolAllocation",
           (Option.map x.ipamPoolAllocation ~f:IpamPoolAllocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolAllocation =
        (Option.map ~f:IpamPoolAllocation.of_xml)
          (Xml.child xml_arg0 "ipamPoolAllocation") in
      make ?ipamPoolAllocation ()
    let of_json json =
      let ipamPoolAllocation =
        field_map json "IpamPoolAllocation" IpamPoolAllocation.of_json in
      make ?ipamPoolAllocation ()
    let to_json v = composed_to_json to_value v
  end
module AllocateIpamPoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      ipamPoolId: IpamPoolId.t ;
      cidr: String_.t option ;
      netmaskLength: Integer.t option ;
      clientToken: String_.t option ;
      description: String_.t option ;
      previewNextCidr: Boolean.t option ;
      disallowedCidrs: IpamPoolAllocationDisallowedCidrs.t option }
    let context_ = "AllocateIpamPoolCidrRequest"
    let make ?dryRun =
      fun ?cidr ->
        fun ?netmaskLength ->
          fun ?clientToken ->
            fun ?description ->
              fun ?previewNextCidr ->
                fun ?disallowedCidrs ->
                  fun ~ipamPoolId ->
                    fun () ->
                      {
                        dryRun;
                        cidr;
                        netmaskLength;
                        clientToken;
                        description;
                        previewNextCidr;
                        disallowedCidrs;
                        ipamPoolId
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cidr", (Option.map x.cidr ~f:String_.to_value));
        ("NetmaskLength", (Option.map x.netmaskLength ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("PreviewNextCidr",
          (Option.map x.previewNextCidr ~f:Boolean.to_value));
        ("DisallowedCidr",
          (Option.map x.disallowedCidrs
             ~f:IpamPoolAllocationDisallowedCidrs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let disallowedCidrs =
        (Option.map ~f:IpamPoolAllocationDisallowedCidrs.of_xml)
          (Xml.child xml_arg0 "DisallowedCidr") in
      let previewNextCidr =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "PreviewNextCidr") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let netmaskLength =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "NetmaskLength") in
      let cidr = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Cidr") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?disallowedCidrs ?previewNextCidr ?description ?clientToken
        ?netmaskLength ?cidr ~ipamPoolId ?dryRun ()
    let of_json json =
      let disallowedCidrs =
        field_map json "DisallowedCidrs"
          IpamPoolAllocationDisallowedCidrs.of_json in
      let previewNextCidr = field_map json "PreviewNextCidr" Boolean.of_json in
      let description = field_map json "Description" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let netmaskLength = field_map json "NetmaskLength" Integer.of_json in
      let cidr = field_map json "Cidr" String_.of_json in
      let ipamPoolId = field_map_exn json "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?disallowedCidrs ?previewNextCidr ?description ?clientToken
        ?netmaskLength ?cidr ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AllocateHostsResult =
  struct
    type nonrec t = {
      hostIds: ResponseHostIdList.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?hostIds = fun () -> { hostIds }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("hostIdSet", (Option.map x.hostIds ~f:ResponseHostIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostIds =
        (Option.map ~f:ResponseHostIdList.of_xml)
          (Xml.child xml_arg0 "hostIdSet") in
      make ?hostIds ()
    let of_json json =
      let hostIds = field_map json "HostIds" ResponseHostIdList.of_json in
      make ?hostIds ()
    let to_json v = composed_to_json to_value v
  end
module AllocateHostsRequest =
  struct
    type nonrec t =
      {
      autoPlacement: AutoPlacement.t option ;
      availabilityZone: String_.t ;
      clientToken: String_.t option ;
      instanceType: String_.t option ;
      instanceFamily: String_.t option ;
      quantity: Integer.t ;
      tagSpecifications: TagSpecificationList.t option ;
      hostRecovery: HostRecovery.t option }
    let context_ = "AllocateHostsRequest"
    let make ?autoPlacement =
      fun ?clientToken ->
        fun ?instanceType ->
          fun ?instanceFamily ->
            fun ?tagSpecifications ->
              fun ?hostRecovery ->
                fun ~availabilityZone ->
                  fun ~quantity ->
                    fun () ->
                      {
                        autoPlacement;
                        clientToken;
                        instanceType;
                        instanceFamily;
                        tagSpecifications;
                        hostRecovery;
                        availabilityZone;
                        quantity
                      }
    let to_value x =
      structure_to_value
        [("autoPlacement",
           (Option.map x.autoPlacement ~f:AutoPlacement.to_value));
        ("availabilityZone", (Some (String_.to_value x.availabilityZone)));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("InstanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("quantity", (Some (Integer.to_value x.quantity)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("HostRecovery",
          (Option.map x.hostRecovery ~f:HostRecovery.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostRecovery =
        (Option.map ~f:HostRecovery.of_xml)
          (Xml.child xml_arg0 "HostRecovery") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let quantity =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "quantity") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceFamily") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let availabilityZone =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "availabilityZone") in
      let autoPlacement =
        (Option.map ~f:AutoPlacement.of_xml)
          (Xml.child xml_arg0 "autoPlacement") in
      make ?hostRecovery ?tagSpecifications ~quantity ?instanceFamily
        ?instanceType ?clientToken ~availabilityZone ?autoPlacement ()
    let of_json json =
      let hostRecovery = field_map json "HostRecovery" HostRecovery.of_json in
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let quantity = field_map_exn json "Quantity" Integer.of_json in
      let instanceFamily = field_map json "InstanceFamily" String_.of_json in
      let instanceType = field_map json "InstanceType" String_.of_json in
      let clientToken = field_map json "ClientToken" String_.of_json in
      let availabilityZone =
        field_map_exn json "AvailabilityZone" String_.of_json in
      let autoPlacement =
        field_map json "AutoPlacement" AutoPlacement.of_json in
      make ?hostRecovery ?tagSpecifications ~quantity ?instanceFamily
        ?instanceType ?clientToken ~availabilityZone ?autoPlacement ()
    let to_json v = composed_to_json to_value v
  end
module AllocateAddressResult =
  struct
    type nonrec t =
      {
      publicIp: String_.t option ;
      allocationId: String_.t option ;
      publicIpv4Pool: String_.t option ;
      networkBorderGroup: String_.t option ;
      domain: DomainType.t option ;
      customerOwnedIp: String_.t option ;
      customerOwnedIpv4Pool: String_.t option ;
      carrierIp: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?publicIp =
      fun ?allocationId ->
        fun ?publicIpv4Pool ->
          fun ?networkBorderGroup ->
            fun ?domain ->
              fun ?customerOwnedIp ->
                fun ?customerOwnedIpv4Pool ->
                  fun ?carrierIp ->
                    fun () ->
                      {
                        publicIp;
                        allocationId;
                        publicIpv4Pool;
                        networkBorderGroup;
                        domain;
                        customerOwnedIp;
                        customerOwnedIpv4Pool;
                        carrierIp
                      }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("publicIp", (Option.map x.publicIp ~f:String_.to_value));
        ("allocationId", (Option.map x.allocationId ~f:String_.to_value));
        ("publicIpv4Pool", (Option.map x.publicIpv4Pool ~f:String_.to_value));
        ("networkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value));
        ("domain", (Option.map x.domain ~f:DomainType.to_value));
        ("customerOwnedIp",
          (Option.map x.customerOwnedIp ~f:String_.to_value));
        ("customerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool ~f:String_.to_value));
        ("carrierIp", (Option.map x.carrierIp ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let carrierIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "carrierIp") in
      let customerOwnedIpv4Pool =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "customerOwnedIpv4Pool") in
      let customerOwnedIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "customerOwnedIp") in
      let domain =
        (Option.map ~f:DomainType.of_xml) (Xml.child xml_arg0 "domain") in
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkBorderGroup") in
      let publicIpv4Pool =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "publicIpv4Pool") in
      let allocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "allocationId") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "publicIp") in
      make ?carrierIp ?customerOwnedIpv4Pool ?customerOwnedIp ?domain
        ?networkBorderGroup ?publicIpv4Pool ?allocationId ?publicIp ()
    let of_json json =
      let carrierIp = field_map json "CarrierIp" String_.of_json in
      let customerOwnedIpv4Pool =
        field_map json "CustomerOwnedIpv4Pool" String_.of_json in
      let customerOwnedIp = field_map json "CustomerOwnedIp" String_.of_json in
      let domain = field_map json "Domain" DomainType.of_json in
      let networkBorderGroup =
        field_map json "NetworkBorderGroup" String_.of_json in
      let publicIpv4Pool = field_map json "PublicIpv4Pool" String_.of_json in
      let allocationId = field_map json "AllocationId" String_.of_json in
      let publicIp = field_map json "PublicIp" String_.of_json in
      make ?carrierIp ?customerOwnedIpv4Pool ?customerOwnedIp ?domain
        ?networkBorderGroup ?publicIpv4Pool ?allocationId ?publicIp ()
    let to_json v = composed_to_json to_value v
  end
module AllocateAddressRequest =
  struct
    type nonrec t =
      {
      domain: DomainType.t option ;
      address: PublicIpAddress.t option ;
      publicIpv4Pool: Ipv4PoolEc2Id.t option ;
      networkBorderGroup: String_.t option ;
      customerOwnedIpv4Pool: String_.t option ;
      dryRun: Boolean.t option ;
      tagSpecifications: TagSpecificationList.t option }
    let make ?domain =
      fun ?address ->
        fun ?publicIpv4Pool ->
          fun ?networkBorderGroup ->
            fun ?customerOwnedIpv4Pool ->
              fun ?dryRun ->
                fun ?tagSpecifications ->
                  fun () ->
                    {
                      domain;
                      address;
                      publicIpv4Pool;
                      networkBorderGroup;
                      customerOwnedIpv4Pool;
                      dryRun;
                      tagSpecifications
                    }
    let to_value x =
      structure_to_value
        [("Domain", (Option.map x.domain ~f:DomainType.to_value));
        ("Address", (Option.map x.address ~f:PublicIpAddress.to_value));
        ("PublicIpv4Pool",
          (Option.map x.publicIpv4Pool ~f:Ipv4PoolEc2Id.to_value));
        ("NetworkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value));
        ("CustomerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let customerOwnedIpv4Pool =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CustomerOwnedIpv4Pool") in
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkBorderGroup") in
      let publicIpv4Pool =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml)
          (Xml.child xml_arg0 "PublicIpv4Pool") in
      let address =
        (Option.map ~f:PublicIpAddress.of_xml) (Xml.child xml_arg0 "Address") in
      let domain =
        (Option.map ~f:DomainType.of_xml) (Xml.child xml_arg0 "Domain") in
      make ?tagSpecifications ?dryRun ?customerOwnedIpv4Pool
        ?networkBorderGroup ?publicIpv4Pool ?address ?domain ()
    let of_json json =
      let tagSpecifications =
        field_map json "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let customerOwnedIpv4Pool =
        field_map json "CustomerOwnedIpv4Pool" String_.of_json in
      let networkBorderGroup =
        field_map json "NetworkBorderGroup" String_.of_json in
      let publicIpv4Pool =
        field_map json "PublicIpv4Pool" Ipv4PoolEc2Id.of_json in
      let address = field_map json "Address" PublicIpAddress.of_json in
      let domain = field_map json "Domain" DomainType.of_json in
      make ?tagSpecifications ?dryRun ?customerOwnedIpv4Pool
        ?networkBorderGroup ?publicIpv4Pool ?address ?domain ()
    let to_json v = composed_to_json to_value v
  end
module AdvertiseByoipCidrResult =
  struct
    type nonrec t = {
      byoipCidr: ByoipCidr.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_json json =
      let byoipCidr = field_map json "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end
module AdvertiseByoipCidrRequest =
  struct
    type nonrec t = {
      cidr: String_.t ;
      dryRun: Boolean.t option }
    let context_ = "AdvertiseByoipCidrRequest"
    let make ?dryRun = fun ~cidr -> fun () -> { dryRun; cidr }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?dryRun ~cidr ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let cidr = field_map_exn json "Cidr" String_.of_json in
      make ?dryRun ~cidr ()
    let to_json v = composed_to_json to_value v
  end
module AcceptVpcPeeringConnectionResult =
  struct
    type nonrec t = {
      vpcPeeringConnection: VpcPeeringConnection.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringConnection = fun () -> { vpcPeeringConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcPeeringConnection",
           (Option.map x.vpcPeeringConnection
              ~f:VpcPeeringConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnection =
        (Option.map ~f:VpcPeeringConnection.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnection") in
      make ?vpcPeeringConnection ()
    let of_json json =
      let vpcPeeringConnection =
        field_map json "VpcPeeringConnection" VpcPeeringConnection.of_json in
      make ?vpcPeeringConnection ()
    let to_json v = composed_to_json to_value v
  end
module AcceptVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      vpcPeeringConnectionId: VpcPeeringConnectionId.t option }
    let make ?dryRun =
      fun ?vpcPeeringConnectionId ->
        fun () -> { dryRun; vpcPeeringConnectionId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcPeeringConnectionId",
          (Option.map x.vpcPeeringConnectionId
             ~f:VpcPeeringConnectionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        (Option.map ~f:VpcPeeringConnectionId.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnectionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?vpcPeeringConnectionId ?dryRun ()
    let of_json json =
      let vpcPeeringConnectionId =
        field_map json "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?vpcPeeringConnectionId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AcceptVpcEndpointConnectionsResult =
  struct
    type nonrec t = {
      unsuccessful: UnsuccessfulItemSet.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_json json =
      let unsuccessful =
        field_map json "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end
module AcceptVpcEndpointConnectionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      serviceId: VpcEndpointServiceId.t ;
      vpcEndpointIds: VpcEndpointIdList.t }
    let context_ = "AcceptVpcEndpointConnectionsRequest"
    let make ?dryRun =
      fun ~serviceId ->
        fun ~vpcEndpointIds ->
          fun () -> { dryRun; serviceId; vpcEndpointIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("VpcEndpointId",
          (Some (VpcEndpointIdList.to_value x.vpcEndpointIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEndpointIds =
        VpcEndpointIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEndpointId") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcEndpointIds ~serviceId ?dryRun ()
    let of_json json =
      let vpcEndpointIds =
        field_map_exn json "VpcEndpointIds" VpcEndpointIdList.of_json in
      let serviceId =
        field_map_exn json "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ~vpcEndpointIds ~serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
module AcceptTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_json json =
      let transitGatewayVpcAttachment =
        field_map json "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end
module AcceptTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "AcceptTransitGatewayVpcAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module AcceptTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_json json =
      let transitGatewayPeeringAttachment =
        field_map json "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end
module AcceptTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t ;
      dryRun: Boolean.t option }
    let context_ = "AcceptTransitGatewayPeeringAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end
module AcceptTransitGatewayMulticastDomainAssociationsResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayMulticastDomainAssociations.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations = fun () -> { associations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayMulticastDomainAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociations.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?associations ()
    let of_json json =
      let associations =
        field_map json "Associations"
          TransitGatewayMulticastDomainAssociations.of_json in
      make ?associations ()
    let to_json v = composed_to_json to_value v
  end
module AcceptTransitGatewayMulticastDomainAssociationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option ;
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option ;
      subnetIds: ValueStringList.t option ;
      dryRun: Boolean.t option }
    let make ?transitGatewayMulticastDomainId =
      fun ?transitGatewayAttachmentId ->
        fun ?subnetIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                transitGatewayAttachmentId;
                subnetIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:ValueStringList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "SubnetIds") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let of_json json =
      let dryRun = field_map json "DryRun" Boolean.of_json in
      let subnetIds = field_map json "SubnetIds" ValueStringList.of_json in
      let transitGatewayAttachmentId =
        field_map json "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayMulticastDomainId =
        field_map json "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end
module AcceptReservedInstancesExchangeQuoteResult =
  struct
    type nonrec t = {
      exchangeId: String_.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exchangeId = fun () -> { exchangeId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exchangeId", (Option.map x.exchangeId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exchangeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "exchangeId") in
      make ?exchangeId ()
    let of_json json =
      let exchangeId = field_map json "ExchangeId" String_.of_json in
      make ?exchangeId ()
    let to_json v = composed_to_json to_value v
  end
module AcceptReservedInstancesExchangeQuoteRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option ;
      reservedInstanceIds: ReservedInstanceIdSet.t ;
      targetConfigurations: TargetConfigurationRequestSet.t option }
    let context_ = "AcceptReservedInstancesExchangeQuoteRequest"
    let make ?dryRun =
      fun ?targetConfigurations ->
        fun ~reservedInstanceIds ->
          fun () -> { dryRun; targetConfigurations; reservedInstanceIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ReservedInstanceId",
          (Some (ReservedInstanceIdSet.to_value x.reservedInstanceIds)));
        ("TargetConfiguration",
          (Option.map x.targetConfigurations
             ~f:TargetConfigurationRequestSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetConfigurations =
        (Option.map ~f:TargetConfigurationRequestSet.of_xml)
          (Xml.child xml_arg0 "TargetConfiguration") in
      let reservedInstanceIds =
        ReservedInstanceIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservedInstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?targetConfigurations ~reservedInstanceIds ?dryRun ()
    let of_json json =
      let targetConfigurations =
        field_map json "TargetConfigurations"
          TargetConfigurationRequestSet.of_json in
      let reservedInstanceIds =
        field_map_exn json "ReservedInstanceIds"
          ReservedInstanceIdSet.of_json in
      let dryRun = field_map json "DryRun" Boolean.of_json in
      make ?targetConfigurations ~reservedInstanceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end
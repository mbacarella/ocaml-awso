(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
open Values_0[@@warning "-33"]
open Values_1[@@warning "-33"]
open Values_2[@@warning "-33"]
open Values_3[@@warning "-33"]
open Values_4[@@warning "-33"]
open Values_5[@@warning "-33"]
open Values_6[@@warning "-33"]
module DeleteCustomerGatewayRequest =
  struct
    type nonrec t =
      {
      customerGatewayId: CustomerGatewayId.t
        [@ocaml.doc "The ID of the customer gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteCustomerGatewayRequest"
    let make ?dryRun =
      fun ~customerGatewayId -> fun () -> { dryRun; customerGatewayId }
    let to_value x =
      structure_to_value
        [("CustomerGatewayId",
           (Some (CustomerGatewayId.to_value x.customerGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let customerGatewayId =
        CustomerGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CustomerGatewayId") in
      make ?dryRun ~customerGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let customerGatewayId =
        field_map_exn json__ "CustomerGatewayId" CustomerGatewayId.of_json in
      make ?dryRun ~customerGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteCustomerGateway."]
module DeleteCoipPoolResult =
  struct
    type nonrec t =
      {
      coipPool: CoipPool.t option
        [@ocaml.doc "Information about the CoIP address pool."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipPool = fun () -> { coipPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipPool", (Option.map x.coipPool ~f:CoipPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coipPool =
        (Option.map ~f:CoipPool.of_xml) (Xml.child xml_arg0 "coipPool") in
      make ?coipPool ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let coipPool = field_map json__ "CoipPool" CoipPool.of_json in
      make ?coipPool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a pool of customer-owned IP (CoIP) addresses."]
module DeleteCoipPoolRequest =
  struct
    type nonrec t =
      {
      coipPoolId: Ipv4PoolCoipId.t
        [@ocaml.doc "The ID of the CoIP pool that you want to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteCoipPoolRequest"
    let make ?dryRun = fun ~coipPoolId -> fun () -> { dryRun; coipPoolId }
    let to_value x =
      structure_to_value
        [("CoipPoolId", (Some (Ipv4PoolCoipId.to_value x.coipPoolId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let coipPoolId =
        Ipv4PoolCoipId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CoipPoolId") in
      make ?dryRun ~coipPoolId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let coipPoolId =
        field_map_exn json__ "CoipPoolId" Ipv4PoolCoipId.of_json in
      make ?dryRun ~coipPoolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a pool of customer-owned IP (CoIP) addresses."]
module DeleteCoipCidrResult =
  struct
    type nonrec t =
      {
      coipCidr: CoipCidr.t option
        [@ocaml.doc
          "Information about a range of customer-owned IP addresses."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipCidr = fun () -> { coipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipCidr", (Option.map x.coipCidr ~f:CoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coipCidr =
        (Option.map ~f:CoipCidr.of_xml) (Xml.child xml_arg0 "coipCidr") in
      make ?coipCidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let coipCidr = field_map json__ "CoipCidr" CoipCidr.of_json in
      make ?coipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a range of customer-owned IP addresses."]
module DeleteCoipCidrRequest =
  struct
    type nonrec t =
      {
      cidr: String_.t
        [@ocaml.doc
          "A customer-owned IP address range that you want to delete."];
      coipPoolId: Ipv4PoolCoipId.t
        [@ocaml.doc "The ID of the customer-owned address pool."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteCoipCidrRequest"
    let make ?dryRun =
      fun ~cidr -> fun ~coipPoolId -> fun () -> { dryRun; cidr; coipPoolId }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("CoipPoolId", (Some (Ipv4PoolCoipId.to_value x.coipPoolId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let coipPoolId =
        Ipv4PoolCoipId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CoipPoolId") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?dryRun ~coipPoolId ~cidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let coipPoolId =
        field_map_exn json__ "CoipPoolId" Ipv4PoolCoipId.of_json in
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      make ?dryRun ~coipPoolId ~cidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a range of customer-owned IP addresses."]
module DeleteClientVpnRouteResult =
  struct
    type nonrec t =
      {
      status: ClientVpnRouteStatus.t option
        [@ocaml.doc "The current state of the route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ClientVpnRouteStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnRouteStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" ClientVpnRouteStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a route from a Client VPN endpoint. You can only delete routes that you manually added using the CreateClientVpnRoute action. You cannot delete routes that were automatically added when associating a subnet. To remove routes that have been automatically added, disassociate the target subnet from the Client VPN endpoint."]
module DeleteClientVpnRouteRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc
          "The ID of the Client VPN endpoint from which the route is to be deleted."];
      targetVpcSubnetId: SubnetId.t option
        [@ocaml.doc "The ID of the target subnet used by the route."];
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The IPv4 address range, in CIDR notation, of the route to be deleted."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteClientVpnRouteRequest"
    let make ?targetVpcSubnetId =
      fun ?dryRun ->
        fun ~clientVpnEndpointId ->
          fun ~destinationCidrBlock ->
            fun () ->
              {
                targetVpcSubnetId;
                dryRun;
                clientVpnEndpointId;
                destinationCidrBlock
              }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("TargetVpcSubnetId",
          (Option.map x.targetVpcSubnetId ~f:SubnetId.to_value));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let targetVpcSubnetId =
        (Option.map ~f:SubnetId.of_xml)
          (Xml.child xml_arg0 "TargetVpcSubnetId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~destinationCidrBlock ?targetVpcSubnetId
        ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      let targetVpcSubnetId =
        field_map json__ "TargetVpcSubnetId" SubnetId.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ~destinationCidrBlock ?targetVpcSubnetId
        ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a route from a Client VPN endpoint. You can only delete routes that you manually added using the CreateClientVpnRoute action. You cannot delete routes that were automatically added when associating a subnet. To remove routes that have been automatically added, disassociate the target subnet from the Client VPN endpoint."]
module DeleteClientVpnEndpointResult =
  struct
    type nonrec t =
      {
      status: ClientVpnEndpointStatus.t option
        [@ocaml.doc "The current state of the Client VPN endpoint."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status",
           (Option.map x.status ~f:ClientVpnEndpointStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnEndpointStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" ClientVpnEndpointStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Client VPN endpoint. You must disassociate all target networks before you can delete a Client VPN endpoint."]
module DeleteClientVpnEndpointRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN to be deleted."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteClientVpnEndpointRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId -> fun () -> { dryRun; clientVpnEndpointId }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Client VPN endpoint. You must disassociate all target networks before you can delete a Client VPN endpoint."]
module DeleteCarrierGatewayResult =
  struct
    type nonrec t =
      {
      carrierGateway: CarrierGateway.t option
        [@ocaml.doc "Information about the carrier gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?carrierGateway = fun () -> { carrierGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("carrierGateway",
           (Option.map x.carrierGateway ~f:CarrierGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let carrierGateway =
        (Option.map ~f:CarrierGateway.of_xml)
          (Xml.child xml_arg0 "carrierGateway") in
      make ?carrierGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let carrierGateway =
        field_map json__ "CarrierGateway" CarrierGateway.of_json in
      make ?carrierGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a carrier gateway. If you do not delete the route that contains the carrier gateway as the Target, the route is a blackhole route. For information about how to delete a route, see DeleteRoute."]
module DeleteCarrierGatewayRequest =
  struct
    type nonrec t =
      {
      carrierGatewayId: CarrierGatewayId.t
        [@ocaml.doc "The ID of the carrier gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteCarrierGatewayRequest"
    let make ?dryRun =
      fun ~carrierGatewayId -> fun () -> { dryRun; carrierGatewayId }
    let to_value x =
      structure_to_value
        [("CarrierGatewayId",
           (Some (CarrierGatewayId.to_value x.carrierGatewayId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let carrierGatewayId =
        CarrierGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CarrierGatewayId") in
      make ?dryRun ~carrierGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let carrierGatewayId =
        field_map_exn json__ "CarrierGatewayId" CarrierGatewayId.of_json in
      make ?dryRun ~carrierGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a carrier gateway. If you do not delete the route that contains the carrier gateway as the Target, the route is a blackhole route. For information about how to delete a route, see DeleteRoute."]
module DeleteCapacityManagerDataExportResult =
  struct
    type nonrec t =
      {
      capacityManagerDataExportId: CapacityManagerDataExportId.t option
        [@ocaml.doc
          "The unique identifier of the deleted data export configuration."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityManagerDataExportId =
      fun () -> { capacityManagerDataExportId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityManagerDataExportId",
           (Option.map x.capacityManagerDataExportId
              ~f:CapacityManagerDataExportId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityManagerDataExportId =
        (Option.map ~f:CapacityManagerDataExportId.of_xml)
          (Xml.child xml_arg0 "capacityManagerDataExportId") in
      make ?capacityManagerDataExportId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityManagerDataExportId =
        field_map json__ "CapacityManagerDataExportId"
          CapacityManagerDataExportId.of_json in
      make ?capacityManagerDataExportId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an existing Capacity Manager data export configuration. This stops future scheduled exports but does not delete previously exported files from S3."]
module DeleteCapacityManagerDataExportRequest =
  struct
    type nonrec t =
      {
      capacityManagerDataExportId: CapacityManagerDataExportId.t
        [@ocaml.doc
          "The unique identifier of the data export configuration to delete."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "DeleteCapacityManagerDataExportRequest"
    let make ?dryRun =
      fun ~capacityManagerDataExportId ->
        fun () -> { dryRun; capacityManagerDataExportId }
    let to_value x =
      structure_to_value
        [("CapacityManagerDataExportId",
           (Some
              (CapacityManagerDataExportId.to_value
                 x.capacityManagerDataExportId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let capacityManagerDataExportId =
        CapacityManagerDataExportId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CapacityManagerDataExportId") in
      make ?dryRun ~capacityManagerDataExportId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let capacityManagerDataExportId =
        field_map_exn json__ "CapacityManagerDataExportId"
          CapacityManagerDataExportId.of_json in
      make ?dryRun ~capacityManagerDataExportId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an existing Capacity Manager data export configuration. This stops future scheduled exports but does not delete previously exported files from S3."]
module CreateVpnGatewayResult =
  struct
    type nonrec t =
      {
      vpnGateway: VpnGateway.t option
        [@ocaml.doc "Information about the virtual private gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnGateway = fun () -> { vpnGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnGateway", (Option.map x.vpnGateway ~f:VpnGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnGateway =
        (Option.map ~f:VpnGateway.of_xml) (Xml.child xml_arg0 "vpnGateway") in
      make ?vpnGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpnGateway = field_map json__ "VpnGateway" VpnGateway.of_json in
      make ?vpnGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateVpnGateway."]
module CreateVpnGatewayRequest =
  struct
    type nonrec t =
      {
      availabilityZone: String_.t option
        [@ocaml.doc "The Availability Zone for the virtual private gateway."];
      type_: GatewayType.t
        [@ocaml.doc
          "The type of VPN connection this virtual private gateway supports."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the virtual private gateway."];
      amazonSideAsn: Long.t option
        [@ocaml.doc
          "A private Autonomous System Number (ASN) for the Amazon side of a BGP session. If you're using a 16-bit ASN, it must be in the 64512 to 65534 range. If you're using a 32-bit ASN, it must be in the 4200000000 to 4294967294 range. Default: 64512"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateVpnGatewayRequest"
    let make ?availabilityZone =
      fun ?tagSpecifications ->
        fun ?amazonSideAsn ->
          fun ?dryRun ->
            fun ~type_ ->
              fun () ->
                {
                  availabilityZone;
                  tagSpecifications;
                  amazonSideAsn;
                  dryRun;
                  type_
                }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:String_.to_value));
        ("Type", (Some (GatewayType.to_value x.type_)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("AmazonSideAsn", (Option.map x.amazonSideAsn ~f:Long.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let amazonSideAsn =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "AmazonSideAsn") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let type_ =
        GatewayType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?dryRun ?amazonSideAsn ?tagSpecifications ~type_ ?availabilityZone
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let amazonSideAsn = field_map json__ "AmazonSideAsn" Long.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let type_ = field_map_exn json__ "Type" GatewayType.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" String_.of_json in
      make ?dryRun ?amazonSideAsn ?tagSpecifications ~type_ ?availabilityZone
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateVpnGateway."]
module CreateVpnConnectionRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The CIDR block associated with the local subnet of the customer network."];
      vpnConnectionId: VpnConnectionId.t
        [@ocaml.doc "The ID of the VPN connection."]}
    let context_ = "CreateVpnConnectionRouteRequest"
    let make ~destinationCidrBlock =
      fun ~vpnConnectionId ->
        fun () -> { destinationCidrBlock; vpnConnectionId }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("VpnConnectionId",
          (Some (VpnConnectionId.to_value x.vpnConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnectionId =
        VpnConnectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnConnectionId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpnConnectionId =
        field_map_exn json__ "VpnConnectionId" VpnConnectionId.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      make ~vpnConnectionId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateVpnConnectionRoute."]
module CreateVpnConnectionResult =
  struct
    type nonrec t =
      {
      vpnConnection: VpnConnection.t option
        [@ocaml.doc "Information about the VPN connection."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConnection = fun () -> { vpnConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConnection",
           (Option.map x.vpnConnection ~f:VpnConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConnection =
        (Option.map ~f:VpnConnection.of_xml)
          (Xml.child xml_arg0 "vpnConnection") in
      make ?vpnConnection ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpnConnection =
        field_map json__ "VpnConnection" VpnConnection.of_json in
      make ?vpnConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateVpnConnection."]
module CreateVpnConnectionRequest =
  struct
    type nonrec t =
      {
      customerGatewayId: CustomerGatewayId.t
        [@ocaml.doc "The ID of the customer gateway."];
      type_: String_.t [@ocaml.doc "The type of VPN connection (ipsec.1)."];
      vpnGatewayId: VpnGatewayId.t option
        [@ocaml.doc
          "The ID of the virtual private gateway. If you specify a virtual private gateway, you cannot specify a transit gateway."];
      transitGatewayId: TransitGatewayId.t option
        [@ocaml.doc
          "The ID of the transit gateway. If you specify a transit gateway, you cannot specify a virtual private gateway."];
      vpnConcentratorId: VpnConcentratorId.t option
        [@ocaml.doc
          "The ID of the VPN concentrator to associate with the VPN connection."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the VPN connection."];
      preSharedKeyStorage: String_.t option
        [@ocaml.doc
          "Specifies the storage mode for the pre-shared key (PSK). Valid values are Standard\" (stored in the Site-to-Site VPN service) or SecretsManager (stored in Amazon Web Services Secrets Manager)."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      options: VpnConnectionOptionsSpecification.t option
        [@ocaml.doc "The options for the VPN connection."]}
    let context_ = "CreateVpnConnectionRequest"
    let make ?vpnGatewayId =
      fun ?transitGatewayId ->
        fun ?vpnConcentratorId ->
          fun ?tagSpecifications ->
            fun ?preSharedKeyStorage ->
              fun ?dryRun ->
                fun ?options ->
                  fun ~customerGatewayId ->
                    fun ~type_ ->
                      fun () ->
                        {
                          vpnGatewayId;
                          transitGatewayId;
                          vpnConcentratorId;
                          tagSpecifications;
                          preSharedKeyStorage;
                          dryRun;
                          options;
                          customerGatewayId;
                          type_
                        }
    let to_value x =
      structure_to_value
        [("CustomerGatewayId",
           (Some (CustomerGatewayId.to_value x.customerGatewayId)));
        ("Type", (Some (String_.to_value x.type_)));
        ("VpnGatewayId",
          (Option.map x.vpnGatewayId ~f:VpnGatewayId.to_value));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("VpnConcentratorId",
          (Option.map x.vpnConcentratorId ~f:VpnConcentratorId.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("PreSharedKeyStorage",
          (Option.map x.preSharedKeyStorage ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("options",
          (Option.map x.options ~f:VpnConnectionOptionsSpecification.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let options =
        (Option.map ~f:VpnConnectionOptionsSpecification.of_xml)
          (Xml.child xml_arg0 "options") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let preSharedKeyStorage =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "PreSharedKeyStorage") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpnConcentratorId =
        (Option.map ~f:VpnConcentratorId.of_xml)
          (Xml.child xml_arg0 "VpnConcentratorId") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let vpnGatewayId =
        (Option.map ~f:VpnGatewayId.of_xml)
          (Xml.child xml_arg0 "VpnGatewayId") in
      let type_ =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let customerGatewayId =
        CustomerGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CustomerGatewayId") in
      make ?options ?dryRun ?preSharedKeyStorage ?tagSpecifications
        ?vpnConcentratorId ?transitGatewayId ?vpnGatewayId ~type_
        ~customerGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let options =
        field_map json__ "Options" VpnConnectionOptionsSpecification.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let preSharedKeyStorage =
        field_map json__ "PreSharedKeyStorage" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpnConcentratorId =
        field_map json__ "VpnConcentratorId" VpnConcentratorId.of_json in
      let transitGatewayId =
        field_map json__ "TransitGatewayId" TransitGatewayId.of_json in
      let vpnGatewayId = field_map json__ "VpnGatewayId" VpnGatewayId.of_json in
      let type_ = field_map_exn json__ "Type" String_.of_json in
      let customerGatewayId =
        field_map_exn json__ "CustomerGatewayId" CustomerGatewayId.of_json in
      make ?options ?dryRun ?preSharedKeyStorage ?tagSpecifications
        ?vpnConcentratorId ?transitGatewayId ?vpnGatewayId ~type_
        ~customerGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateVpnConnection."]
module CreateVpnConcentratorResult =
  struct
    type nonrec t =
      {
      vpnConcentrator: VpnConcentrator.t option
        [@ocaml.doc "Information about the VPN concentrator."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpnConcentrator = fun () -> { vpnConcentrator }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpnConcentrator",
           (Option.map x.vpnConcentrator ~f:VpnConcentrator.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpnConcentrator =
        (Option.map ~f:VpnConcentrator.of_xml)
          (Xml.child xml_arg0 "vpnConcentrator") in
      make ?vpnConcentrator ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpnConcentrator =
        field_map json__ "VpnConcentrator" VpnConcentrator.of_json in
      make ?vpnConcentrator ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPN concentrator that aggregates multiple VPN connections to a transit gateway."]
module CreateVpnConcentratorRequest =
  struct
    type nonrec t =
      {
      type_: VpnConcentratorType.t
        [@ocaml.doc "The type of VPN concentrator to create."];
      transitGatewayId: TransitGatewayId.t option
        [@ocaml.doc
          "The ID of the transit gateway to attach the VPN concentrator to."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the VPN concentrator during creation."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateVpnConcentratorRequest"
    let make ?transitGatewayId =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~type_ ->
            fun () -> { transitGatewayId; tagSpecifications; dryRun; type_ }
    let to_value x =
      structure_to_value
        [("Type", (Some (VpnConcentratorType.to_value x.type_)));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let type_ =
        VpnConcentratorType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ?dryRun ?tagSpecifications ?transitGatewayId ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let transitGatewayId =
        field_map json__ "TransitGatewayId" TransitGatewayId.of_json in
      let type_ = field_map_exn json__ "Type" VpnConcentratorType.of_json in
      make ?dryRun ?tagSpecifications ?transitGatewayId ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPN concentrator that aggregates multiple VPN connections to a transit gateway."]
module CreateVpcResult =
  struct
    type nonrec t =
      {
      vpc: Vpc.t option [@ocaml.doc "Information about the VPC."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpc = fun () -> { vpc }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("vpc", (Option.map x.vpc ~f:Vpc.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpc = (Option.map ~f:Vpc.of_xml) (Xml.child xml_arg0 "vpc") in
      make ?vpc ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpc = field_map json__ "Vpc" Vpc.of_json in make ?vpc ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC with the specified CIDR blocks. A VPC must have an associated IPv4 CIDR block. You can choose an IPv4 CIDR block or an IPAM-allocated IPv4 CIDR block. You can optionally associate an IPv6 CIDR block with a VPC. You can choose an IPv6 CIDR block, an Amazon-provided IPv6 CIDR block, an IPAM-allocated IPv6 CIDR block, or an IPv6 CIDR block that you brought to Amazon Web Services. For more information, see IP addressing for your VPCs and subnets in the Amazon VPC User Guide. By default, each instance that you launch in the VPC has the default DHCP options, which include only a default DNS server that we provide (AmazonProvidedDNS). For more information, see DHCP option sets in the Amazon VPC User Guide. You can specify DNS options and tenancy for a VPC when you create it. You can't change the tenancy of a VPC after you create it. For more information, see VPC configuration options in the Amazon VPC User Guide."]
module CreateVpcRequest =
  struct
    type nonrec t =
      {
      cidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 network range for the VPC, in CIDR notation. For example, 10.0.0.0/16. We modify the specified CIDR block to its canonical form; for example, if you specify 100.68.0.18/18, we modify it to 100.68.0.0/18."];
      ipv6Pool: Ipv6PoolEc2Id.t option
        [@ocaml.doc
          "The ID of an IPv6 address pool from which to allocate the IPv6 CIDR block."];
      ipv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 CIDR block from the IPv6 address pool. You must also specify Ipv6Pool in the request. To let Amazon choose the IPv6 CIDR block for you, omit this parameter."];
      ipv4IpamPoolId: IpamPoolId.t option
        [@ocaml.doc
          "The ID of an IPv4 IPAM pool you want to use for allocating this VPC's CIDR. For more information, see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv4NetmaskLength: NetmaskLength.t option
        [@ocaml.doc
          "The netmask length of the IPv4 CIDR you want to allocate to this VPC from an Amazon VPC IP Address Manager (IPAM) pool. For more information about IPAM, see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv6IpamPoolId: IpamPoolId.t option
        [@ocaml.doc
          "The ID of an IPv6 IPAM pool which will be used to allocate this VPC an IPv6 CIDR. IPAM is a VPC feature that you can use to automate your IP address management workflows including assigning, tracking, troubleshooting, and auditing IP addresses across Amazon Web Services Regions and accounts throughout your Amazon Web Services Organization. For more information, see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv6NetmaskLength: NetmaskLength.t option
        [@ocaml.doc
          "The netmask length of the IPv6 CIDR you want to allocate to this VPC from an Amazon VPC IP Address Manager (IPAM) pool. For more information about IPAM, see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv6CidrBlockNetworkBorderGroup: String_.t option
        [@ocaml.doc
          "The name of the location from which we advertise the IPV6 CIDR block. Use this parameter to limit the address to this location. You must set AmazonProvidedIpv6CidrBlock to true to use this parameter."];
      vpcEncryptionControl: VpcEncryptionControlConfiguration.t option
        [@ocaml.doc
          "Specifies the encryption control configuration to apply to the VPC during creation. VPC Encryption Control enables you to enforce encryption for all data in transit within and between VPCs to meet compliance requirements. For more information, see Enforce VPC encryption in transit in the Amazon VPC User Guide."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the VPC."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceTenancy: Tenancy.t option
        [@ocaml.doc
          "The tenancy options for instances launched into the VPC. For default, instances are launched with shared tenancy by default. You can launch instances with any tenancy into a shared tenancy VPC. For dedicated, instances are launched as dedicated tenancy instances by default. You can only launch instances with a tenancy of dedicated or host into a dedicated tenancy VPC. Important: The host value cannot be used with this parameter. Use the default or dedicated values only. Default: default"];
      amazonProvidedIpv6CidrBlock: Boolean.t option
        [@ocaml.doc
          "Requests an Amazon-provided IPv6 CIDR block with a /56 prefix length for the VPC. You cannot specify the range of IP addresses, or the size of the CIDR block."]}
    let make ?cidrBlock =
      fun ?ipv6Pool ->
        fun ?ipv6CidrBlock ->
          fun ?ipv4IpamPoolId ->
            fun ?ipv4NetmaskLength ->
              fun ?ipv6IpamPoolId ->
                fun ?ipv6NetmaskLength ->
                  fun ?ipv6CidrBlockNetworkBorderGroup ->
                    fun ?vpcEncryptionControl ->
                      fun ?tagSpecifications ->
                        fun ?dryRun ->
                          fun ?instanceTenancy ->
                            fun ?amazonProvidedIpv6CidrBlock ->
                              fun () ->
                                {
                                  cidrBlock;
                                  ipv6Pool;
                                  ipv6CidrBlock;
                                  ipv4IpamPoolId;
                                  ipv4NetmaskLength;
                                  ipv6IpamPoolId;
                                  ipv6NetmaskLength;
                                  ipv6CidrBlockNetworkBorderGroup;
                                  vpcEncryptionControl;
                                  tagSpecifications;
                                  dryRun;
                                  instanceTenancy;
                                  amazonProvidedIpv6CidrBlock
                                }
    let to_value x =
      structure_to_value
        [("CidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("Ipv6Pool", (Option.map x.ipv6Pool ~f:Ipv6PoolEc2Id.to_value));
        ("Ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("Ipv4IpamPoolId",
          (Option.map x.ipv4IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv4NetmaskLength",
          (Option.map x.ipv4NetmaskLength ~f:NetmaskLength.to_value));
        ("Ipv6IpamPoolId",
          (Option.map x.ipv6IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv6NetmaskLength",
          (Option.map x.ipv6NetmaskLength ~f:NetmaskLength.to_value));
        ("Ipv6CidrBlockNetworkBorderGroup",
          (Option.map x.ipv6CidrBlockNetworkBorderGroup ~f:String_.to_value));
        ("VpcEncryptionControl",
          (Option.map x.vpcEncryptionControl
             ~f:VpcEncryptionControlConfiguration.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceTenancy",
          (Option.map x.instanceTenancy ~f:Tenancy.to_value));
        ("amazonProvidedIpv6CidrBlock",
          (Option.map x.amazonProvidedIpv6CidrBlock ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let amazonProvidedIpv6CidrBlock =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "amazonProvidedIpv6CidrBlock") in
      let instanceTenancy =
        (Option.map ~f:Tenancy.of_xml) (Xml.child xml_arg0 "instanceTenancy") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcEncryptionControl =
        (Option.map ~f:VpcEncryptionControlConfiguration.of_xml)
          (Xml.child xml_arg0 "VpcEncryptionControl") in
      let ipv6CidrBlockNetworkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "Ipv6CidrBlockNetworkBorderGroup") in
      let ipv6NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv6NetmaskLength") in
      let ipv6IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv6IpamPoolId") in
      let ipv4NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv4NetmaskLength") in
      let ipv4IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv4IpamPoolId") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ipv6CidrBlock") in
      let ipv6Pool =
        (Option.map ~f:Ipv6PoolEc2Id.of_xml) (Xml.child xml_arg0 "Ipv6Pool") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      make ?amazonProvidedIpv6CidrBlock ?instanceTenancy ?dryRun
        ?tagSpecifications ?vpcEncryptionControl
        ?ipv6CidrBlockNetworkBorderGroup ?ipv6NetmaskLength ?ipv6IpamPoolId
        ?ipv4NetmaskLength ?ipv4IpamPoolId ?ipv6CidrBlock ?ipv6Pool
        ?cidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let amazonProvidedIpv6CidrBlock =
        field_map json__ "AmazonProvidedIpv6CidrBlock" Boolean.of_json in
      let instanceTenancy =
        field_map json__ "InstanceTenancy" Tenancy.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpcEncryptionControl =
        field_map json__ "VpcEncryptionControl"
          VpcEncryptionControlConfiguration.of_json in
      let ipv6CidrBlockNetworkBorderGroup =
        field_map json__ "Ipv6CidrBlockNetworkBorderGroup" String_.of_json in
      let ipv6NetmaskLength =
        field_map json__ "Ipv6NetmaskLength" NetmaskLength.of_json in
      let ipv6IpamPoolId =
        field_map json__ "Ipv6IpamPoolId" IpamPoolId.of_json in
      let ipv4NetmaskLength =
        field_map json__ "Ipv4NetmaskLength" NetmaskLength.of_json in
      let ipv4IpamPoolId =
        field_map json__ "Ipv4IpamPoolId" IpamPoolId.of_json in
      let ipv6CidrBlock = field_map json__ "Ipv6CidrBlock" String_.of_json in
      let ipv6Pool = field_map json__ "Ipv6Pool" Ipv6PoolEc2Id.of_json in
      let cidrBlock = field_map json__ "CidrBlock" String_.of_json in
      make ?amazonProvidedIpv6CidrBlock ?instanceTenancy ?dryRun
        ?tagSpecifications ?vpcEncryptionControl
        ?ipv6CidrBlockNetworkBorderGroup ?ipv6NetmaskLength ?ipv6IpamPoolId
        ?ipv4NetmaskLength ?ipv4IpamPoolId ?ipv6CidrBlock ?ipv6Pool
        ?cidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC with the specified CIDR blocks. A VPC must have an associated IPv4 CIDR block. You can choose an IPv4 CIDR block or an IPAM-allocated IPv4 CIDR block. You can optionally associate an IPv6 CIDR block with a VPC. You can choose an IPv6 CIDR block, an Amazon-provided IPv6 CIDR block, an IPAM-allocated IPv6 CIDR block, or an IPv6 CIDR block that you brought to Amazon Web Services. For more information, see IP addressing for your VPCs and subnets in the Amazon VPC User Guide. By default, each instance that you launch in the VPC has the default DHCP options, which include only a default DNS server that we provide (AmazonProvidedDNS). For more information, see DHCP option sets in the Amazon VPC User Guide. You can specify DNS options and tenancy for a VPC when you create it. You can't change the tenancy of a VPC after you create it. For more information, see VPC configuration options in the Amazon VPC User Guide."]
module CreateVpcPeeringConnectionResult =
  struct
    type nonrec t =
      {
      vpcPeeringConnection: VpcPeeringConnection.t option
        [@ocaml.doc "Information about the VPC peering connection."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringConnection = fun () -> { vpcPeeringConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcPeeringConnection",
           (Option.map x.vpcPeeringConnection
              ~f:VpcPeeringConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnection =
        (Option.map ~f:VpcPeeringConnection.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnection") in
      make ?vpcPeeringConnection ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringConnection =
        field_map json__ "VpcPeeringConnection" VpcPeeringConnection.of_json in
      make ?vpcPeeringConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a VPC peering connection between two VPCs: a requester VPC that you own and an accepter VPC with which to create the connection. The accepter VPC can belong to another Amazon Web Services account and can be in a different Region to the requester VPC. The requester VPC and accepter VPC cannot have overlapping CIDR blocks. Limitations and rules apply to a VPC peering connection. For more information, see the VPC peering limitations in the VPC Peering Guide. The owner of the accepter VPC must accept the peering request to activate the peering connection. The VPC peering connection request expires after 7 days, after which it cannot be accepted or rejected. If you create a VPC peering connection request between VPCs with overlapping CIDR blocks, the VPC peering connection has a status of failed."]
module CreateVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      peerRegion: String_.t option
        [@ocaml.doc
          "The Region code for the accepter VPC, if the accepter VPC is located in a Region other than the Region in which you make the request. Default: The Region in which you make the request."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the peering connection."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcId: VpcId.t
        [@ocaml.doc
          "The ID of the requester VPC. You must specify this parameter in the request."];
      peerVpcId: String_.t option
        [@ocaml.doc
          "The ID of the VPC with which you are creating the VPC peering connection. You must specify this parameter in the request."];
      peerOwnerId: String_.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the owner of the accepter VPC. Default: Your Amazon Web Services account ID"]}
    let context_ = "CreateVpcPeeringConnectionRequest"
    let make ?peerRegion =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ?peerVpcId ->
            fun ?peerOwnerId ->
              fun ~vpcId ->
                fun () ->
                  {
                    peerRegion;
                    tagSpecifications;
                    dryRun;
                    peerVpcId;
                    peerOwnerId;
                    vpcId
                  }
    let to_value x =
      structure_to_value
        [("PeerRegion", (Option.map x.peerRegion ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)));
        ("peerVpcId", (Option.map x.peerVpcId ~f:String_.to_value));
        ("peerOwnerId", (Option.map x.peerOwnerId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let peerOwnerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "peerOwnerId") in
      let peerVpcId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "peerVpcId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let peerRegion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PeerRegion") in
      make ?peerOwnerId ?peerVpcId ~vpcId ?dryRun ?tagSpecifications
        ?peerRegion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let peerOwnerId = field_map json__ "PeerOwnerId" String_.of_json in
      let peerVpcId = field_map json__ "PeerVpcId" String_.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let peerRegion = field_map json__ "PeerRegion" String_.of_json in
      make ?peerOwnerId ?peerVpcId ~vpcId ?dryRun ?tagSpecifications
        ?peerRegion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a VPC peering connection between two VPCs: a requester VPC that you own and an accepter VPC with which to create the connection. The accepter VPC can belong to another Amazon Web Services account and can be in a different Region to the requester VPC. The requester VPC and accepter VPC cannot have overlapping CIDR blocks. Limitations and rules apply to a VPC peering connection. For more information, see the VPC peering limitations in the VPC Peering Guide. The owner of the accepter VPC must accept the peering request to activate the peering connection. The VPC peering connection request expires after 7 days, after which it cannot be accepted or rejected. If you create a VPC peering connection request between VPCs with overlapping CIDR blocks, the VPC peering connection has a status of failed."]
module CreateVpcEndpointServiceConfigurationResult =
  struct
    type nonrec t =
      {
      serviceConfiguration: ServiceConfiguration.t option
        [@ocaml.doc "Information about the service configuration."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?serviceConfiguration =
      fun ?clientToken -> fun () -> { serviceConfiguration; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceConfiguration",
           (Option.map x.serviceConfiguration
              ~f:ServiceConfiguration.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let serviceConfiguration =
        (Option.map ~f:ServiceConfiguration.of_xml)
          (Xml.child xml_arg0 "serviceConfiguration") in
      make ?clientToken ?serviceConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let serviceConfiguration =
        field_map json__ "ServiceConfiguration" ServiceConfiguration.of_json in
      make ?clientToken ?serviceConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC endpoint service to which service consumers (Amazon Web Services accounts, users, and IAM roles) can connect. Before you create an endpoint service, you must create one of the following for your service: A Network Load Balancer. Service consumers connect to your service using an interface endpoint. A Gateway Load Balancer. Service consumers connect to your service using a Gateway Load Balancer endpoint. If you set the private DNS name, you must prove that you own the private DNS domain name. For more information, see the Amazon Web Services PrivateLink Guide."]
module CreateVpcEndpointServiceConfigurationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      acceptanceRequired: Boolean.t option
        [@ocaml.doc
          "Indicates whether requests from service consumers to create an endpoint to your service must be accepted manually."];
      privateDnsName: String_.t option
        [@ocaml.doc
          "(Interface endpoint configuration) The private DNS name to assign to the VPC endpoint service."];
      networkLoadBalancerArns: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the Network Load Balancers."];
      gatewayLoadBalancerArns: ValueStringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the Gateway Load Balancers."];
      supportedIpAddressTypes: ValueStringList.t option
        [@ocaml.doc
          "The supported IP address types. The possible values are ipv4 and ipv6."];
      supportedRegions: ValueStringList.t option
        [@ocaml.doc
          "The Regions from which service consumers can access the service."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to associate with the service."]}
    let make ?dryRun =
      fun ?acceptanceRequired ->
        fun ?privateDnsName ->
          fun ?networkLoadBalancerArns ->
            fun ?gatewayLoadBalancerArns ->
              fun ?supportedIpAddressTypes ->
                fun ?supportedRegions ->
                  fun ?clientToken ->
                    fun ?tagSpecifications ->
                      fun () ->
                        {
                          dryRun;
                          acceptanceRequired;
                          privateDnsName;
                          networkLoadBalancerArns;
                          gatewayLoadBalancerArns;
                          supportedIpAddressTypes;
                          supportedRegions;
                          clientToken;
                          tagSpecifications
                        }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("AcceptanceRequired",
          (Option.map x.acceptanceRequired ~f:Boolean.to_value));
        ("PrivateDnsName", (Option.map x.privateDnsName ~f:String_.to_value));
        ("NetworkLoadBalancerArn",
          (Option.map x.networkLoadBalancerArns ~f:ValueStringList.to_value));
        ("GatewayLoadBalancerArn",
          (Option.map x.gatewayLoadBalancerArns ~f:ValueStringList.to_value));
        ("SupportedIpAddressType",
          (Option.map x.supportedIpAddressTypes ~f:ValueStringList.to_value));
        ("SupportedRegion",
          (Option.map x.supportedRegions ~f:ValueStringList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let supportedRegions =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "SupportedRegion") in
      let supportedIpAddressTypes =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "SupportedIpAddressType") in
      let gatewayLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "GatewayLoadBalancerArn") in
      let networkLoadBalancerArns =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "NetworkLoadBalancerArn") in
      let privateDnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PrivateDnsName") in
      let acceptanceRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AcceptanceRequired") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?clientToken ?supportedRegions
        ?supportedIpAddressTypes ?gatewayLoadBalancerArns
        ?networkLoadBalancerArns ?privateDnsName ?acceptanceRequired ?dryRun
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let supportedRegions =
        field_map json__ "SupportedRegions" ValueStringList.of_json in
      let supportedIpAddressTypes =
        field_map json__ "SupportedIpAddressTypes" ValueStringList.of_json in
      let gatewayLoadBalancerArns =
        field_map json__ "GatewayLoadBalancerArns" ValueStringList.of_json in
      let networkLoadBalancerArns =
        field_map json__ "NetworkLoadBalancerArns" ValueStringList.of_json in
      let privateDnsName = field_map json__ "PrivateDnsName" String_.of_json in
      let acceptanceRequired =
        field_map json__ "AcceptanceRequired" Boolean.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ?clientToken ?supportedRegions
        ?supportedIpAddressTypes ?gatewayLoadBalancerArns
        ?networkLoadBalancerArns ?privateDnsName ?acceptanceRequired ?dryRun
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC endpoint service to which service consumers (Amazon Web Services accounts, users, and IAM roles) can connect. Before you create an endpoint service, you must create one of the following for your service: A Network Load Balancer. Service consumers connect to your service using an interface endpoint. A Gateway Load Balancer. Service consumers connect to your service using a Gateway Load Balancer endpoint. If you set the private DNS name, you must prove that you own the private DNS domain name. For more information, see the Amazon Web Services PrivateLink Guide."]
module CreateVpcEndpointResult =
  struct
    type nonrec t =
      {
      vpcEndpoint: VpcEndpoint.t option
        [@ocaml.doc "Information about the endpoint."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcEndpoint =
      fun ?clientToken -> fun () -> { vpcEndpoint; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcEndpoint", (Option.map x.vpcEndpoint ~f:VpcEndpoint.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let vpcEndpoint =
        (Option.map ~f:VpcEndpoint.of_xml) (Xml.child xml_arg0 "vpcEndpoint") in
      make ?clientToken ?vpcEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let vpcEndpoint = field_map json__ "VpcEndpoint" VpcEndpoint.of_json in
      make ?clientToken ?vpcEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC endpoint. A VPC endpoint provides a private connection between the specified VPC and the specified endpoint service. You can use an endpoint service provided by Amazon Web Services, an Amazon Web Services Marketplace Partner, or another Amazon Web Services account. For more information, see the Amazon Web Services PrivateLink User Guide."]
module CreateVpcEndpointRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcEndpointType: VpcEndpointType.t option
        [@ocaml.doc "The type of endpoint. Default: Gateway"];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      serviceName: String_.t option
        [@ocaml.doc "The name of the endpoint service."];
      policyDocument: String_.t option
        [@ocaml.doc
          "(Interface and gateway endpoints) A policy to attach to the endpoint that controls access to the service. The policy must be in valid JSON format. If this parameter is not specified, we attach a default policy that allows full access to the service."];
      routeTableIds: VpcEndpointRouteTableIdList.t option
        [@ocaml.doc "(Gateway endpoint) The route table IDs."];
      subnetIds: VpcEndpointSubnetIdList.t option
        [@ocaml.doc
          "(Interface and Gateway Load Balancer endpoints) The IDs of the subnets in which to create endpoint network interfaces. For a Gateway Load Balancer endpoint, you can specify only one subnet."];
      securityGroupIds: VpcEndpointSecurityGroupIdList.t option
        [@ocaml.doc
          "(Interface endpoint) The IDs of the security groups to associate with the endpoint network interfaces. If this parameter is not specified, we use the default security group for the VPC."];
      ipAddressType: IpAddressType.t option
        [@ocaml.doc "The IP address type for the endpoint."];
      dnsOptions: DnsOptionsSpecification.t option
        [@ocaml.doc "The DNS options for the endpoint."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      privateDnsEnabled: Boolean.t option
        [@ocaml.doc
          "(Interface endpoint) Indicates whether to associate a private hosted zone with the specified VPC. The private hosted zone contains a record set for the default public DNS name for the service for the Region (for example, kinesis.us-east-1.amazonaws.com), which resolves to the private IP addresses of the endpoint network interfaces in the VPC. This enables you to make requests to the default public DNS name for the service instead of the public DNS names that are automatically generated by the VPC endpoint service. To use a private hosted zone, you must set the following VPC attributes to true: enableDnsHostnames and enableDnsSupport. Use ModifyVpcAttribute to set the VPC attributes."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to associate with the endpoint."];
      subnetConfigurations: SubnetConfigurationsList.t option
        [@ocaml.doc "The subnet configurations for the endpoint."];
      serviceNetworkArn: ServiceNetworkArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of a service network that will be associated with the VPC endpoint of type service-network."];
      resourceConfigurationArn: ResourceConfigurationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of a resource configuration that will be associated with the VPC endpoint of type resource."];
      serviceRegion: String_.t option
        [@ocaml.doc
          "The Region where the service is hosted. The default is the current Region."]}
    let context_ = "CreateVpcEndpointRequest"
    let make ?dryRun =
      fun ?vpcEndpointType ->
        fun ?serviceName ->
          fun ?policyDocument ->
            fun ?routeTableIds ->
              fun ?subnetIds ->
                fun ?securityGroupIds ->
                  fun ?ipAddressType ->
                    fun ?dnsOptions ->
                      fun ?clientToken ->
                        fun ?privateDnsEnabled ->
                          fun ?tagSpecifications ->
                            fun ?subnetConfigurations ->
                              fun ?serviceNetworkArn ->
                                fun ?resourceConfigurationArn ->
                                  fun ?serviceRegion ->
                                    fun ~vpcId ->
                                      fun () ->
                                        {
                                          dryRun;
                                          vpcEndpointType;
                                          serviceName;
                                          policyDocument;
                                          routeTableIds;
                                          subnetIds;
                                          securityGroupIds;
                                          ipAddressType;
                                          dnsOptions;
                                          clientToken;
                                          privateDnsEnabled;
                                          tagSpecifications;
                                          subnetConfigurations;
                                          serviceNetworkArn;
                                          resourceConfigurationArn;
                                          serviceRegion;
                                          vpcId
                                        }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcEndpointType",
          (Option.map x.vpcEndpointType ~f:VpcEndpointType.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("ServiceName", (Option.map x.serviceName ~f:String_.to_value));
        ("PolicyDocument", (Option.map x.policyDocument ~f:String_.to_value));
        ("RouteTableId",
          (Option.map x.routeTableIds ~f:VpcEndpointRouteTableIdList.to_value));
        ("SubnetId",
          (Option.map x.subnetIds ~f:VpcEndpointSubnetIdList.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:VpcEndpointSecurityGroupIdList.to_value));
        ("IpAddressType",
          (Option.map x.ipAddressType ~f:IpAddressType.to_value));
        ("DnsOptions",
          (Option.map x.dnsOptions ~f:DnsOptionsSpecification.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("PrivateDnsEnabled",
          (Option.map x.privateDnsEnabled ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SubnetConfiguration",
          (Option.map x.subnetConfigurations
             ~f:SubnetConfigurationsList.to_value));
        ("ServiceNetworkArn",
          (Option.map x.serviceNetworkArn ~f:ServiceNetworkArn.to_value));
        ("ResourceConfigurationArn",
          (Option.map x.resourceConfigurationArn
             ~f:ResourceConfigurationArn.to_value));
        ("ServiceRegion", (Option.map x.serviceRegion ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceRegion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceRegion") in
      let resourceConfigurationArn =
        (Option.map ~f:ResourceConfigurationArn.of_xml)
          (Xml.child xml_arg0 "ResourceConfigurationArn") in
      let serviceNetworkArn =
        (Option.map ~f:ServiceNetworkArn.of_xml)
          (Xml.child xml_arg0 "ServiceNetworkArn") in
      let subnetConfigurations =
        (Option.map ~f:SubnetConfigurationsList.of_xml)
          (Xml.child xml_arg0 "SubnetConfiguration") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let privateDnsEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PrivateDnsEnabled") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dnsOptions =
        (Option.map ~f:DnsOptionsSpecification.of_xml)
          (Xml.child xml_arg0 "DnsOptions") in
      let ipAddressType =
        (Option.map ~f:IpAddressType.of_xml)
          (Xml.child xml_arg0 "IpAddressType") in
      let securityGroupIds =
        (Option.map ~f:VpcEndpointSecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let subnetIds =
        (Option.map ~f:VpcEndpointSubnetIdList.of_xml)
          (Xml.child xml_arg0 "SubnetId") in
      let routeTableIds =
        (Option.map ~f:VpcEndpointRouteTableIdList.of_xml)
          (Xml.child xml_arg0 "RouteTableId") in
      let policyDocument =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PolicyDocument") in
      let serviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceName") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let vpcEndpointType =
        (Option.map ~f:VpcEndpointType.of_xml)
          (Xml.child xml_arg0 "VpcEndpointType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?serviceRegion ?resourceConfigurationArn ?serviceNetworkArn
        ?subnetConfigurations ?tagSpecifications ?privateDnsEnabled
        ?clientToken ?dnsOptions ?ipAddressType ?securityGroupIds ?subnetIds
        ?routeTableIds ?policyDocument ?serviceName ~vpcId ?vpcEndpointType
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceRegion = field_map json__ "ServiceRegion" String_.of_json in
      let resourceConfigurationArn =
        field_map json__ "ResourceConfigurationArn"
          ResourceConfigurationArn.of_json in
      let serviceNetworkArn =
        field_map json__ "ServiceNetworkArn" ServiceNetworkArn.of_json in
      let subnetConfigurations =
        field_map json__ "SubnetConfigurations"
          SubnetConfigurationsList.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let privateDnsEnabled =
        field_map json__ "PrivateDnsEnabled" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dnsOptions =
        field_map json__ "DnsOptions" DnsOptionsSpecification.of_json in
      let ipAddressType =
        field_map json__ "IpAddressType" IpAddressType.of_json in
      let securityGroupIds =
        field_map json__ "SecurityGroupIds"
          VpcEndpointSecurityGroupIdList.of_json in
      let subnetIds =
        field_map json__ "SubnetIds" VpcEndpointSubnetIdList.of_json in
      let routeTableIds =
        field_map json__ "RouteTableIds" VpcEndpointRouteTableIdList.of_json in
      let policyDocument = field_map json__ "PolicyDocument" String_.of_json in
      let serviceName = field_map json__ "ServiceName" String_.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let vpcEndpointType =
        field_map json__ "VpcEndpointType" VpcEndpointType.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?serviceRegion ?resourceConfigurationArn ?serviceNetworkArn
        ?subnetConfigurations ?tagSpecifications ?privateDnsEnabled
        ?clientToken ?dnsOptions ?ipAddressType ?securityGroupIds ?subnetIds
        ?routeTableIds ?policyDocument ?serviceName ~vpcId ?vpcEndpointType
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC endpoint. A VPC endpoint provides a private connection between the specified VPC and the specified endpoint service. You can use an endpoint service provided by Amazon Web Services, an Amazon Web Services Marketplace Partner, or another Amazon Web Services account. For more information, see the Amazon Web Services PrivateLink User Guide."]
module CreateVpcEndpointConnectionNotificationResult =
  struct
    type nonrec t =
      {
      connectionNotification: ConnectionNotification.t option
        [@ocaml.doc "Information about the notification."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?connectionNotification =
      fun ?clientToken -> fun () -> { connectionNotification; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("connectionNotification",
           (Option.map x.connectionNotification
              ~f:ConnectionNotification.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let connectionNotification =
        (Option.map ~f:ConnectionNotification.of_xml)
          (Xml.child xml_arg0 "connectionNotification") in
      make ?clientToken ?connectionNotification ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let connectionNotification =
        field_map json__ "ConnectionNotification"
          ConnectionNotification.of_json in
      make ?clientToken ?connectionNotification ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a connection notification for a specified VPC endpoint or VPC endpoint service. A connection notification notifies you of specific endpoint events. You must create an SNS topic to receive notifications. For more information, see Creating an Amazon SNS topic in the Amazon SNS Developer Guide. You can create a connection notification for interface endpoints only."]
module CreateVpcEndpointConnectionNotificationRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t option
        [@ocaml.doc "The ID of the endpoint service."];
      vpcEndpointId: VpcEndpointId.t option
        [@ocaml.doc "The ID of the endpoint."];
      connectionNotificationArn: String_.t
        [@ocaml.doc "The ARN of the SNS topic for the notifications."];
      connectionEvents: ValueStringList.t
        [@ocaml.doc
          "The endpoint events for which to receive notifications. Valid values are Accept, Connect, Delete, and Reject."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    let context_ = "CreateVpcEndpointConnectionNotificationRequest"
    let make ?dryRun =
      fun ?serviceId ->
        fun ?vpcEndpointId ->
          fun ?clientToken ->
            fun ~connectionNotificationArn ->
              fun ~connectionEvents ->
                fun () ->
                  {
                    dryRun;
                    serviceId;
                    vpcEndpointId;
                    clientToken;
                    connectionNotificationArn;
                    connectionEvents
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId",
          (Option.map x.serviceId ~f:VpcEndpointServiceId.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointId ~f:VpcEndpointId.to_value));
        ("ConnectionNotificationArn",
          (Some (String_.to_value x.connectionNotificationArn)));
        ("ConnectionEvents",
          (Some (ValueStringList.to_value x.connectionEvents)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let connectionEvents =
        ValueStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectionEvents") in
      let connectionNotificationArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ConnectionNotificationArn") in
      let vpcEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let serviceId =
        (Option.map ~f:VpcEndpointServiceId.of_xml)
          (Xml.child xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ~connectionEvents ~connectionNotificationArn
        ?vpcEndpointId ?serviceId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let connectionEvents =
        field_map_exn json__ "ConnectionEvents" ValueStringList.of_json in
      let connectionNotificationArn =
        field_map_exn json__ "ConnectionNotificationArn" String_.of_json in
      let vpcEndpointId =
        field_map json__ "VpcEndpointId" VpcEndpointId.of_json in
      let serviceId =
        field_map json__ "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ~connectionEvents ~connectionNotificationArn
        ?vpcEndpointId ?serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a connection notification for a specified VPC endpoint or VPC endpoint service. A connection notification notifies you of specific endpoint events. You must create an SNS topic to receive notifications. For more information, see Creating an Amazon SNS topic in the Amazon SNS Developer Guide. You can create a connection notification for interface endpoints only."]
module CreateVpcEncryptionControlResult =
  struct
    type nonrec t =
      {
      vpcEncryptionControl: VpcEncryptionControl.t option
        [@ocaml.doc
          "Information about the VPC Encryption Control configuration."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcEncryptionControl = fun () -> { vpcEncryptionControl }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcEncryptionControl",
           (Option.map x.vpcEncryptionControl
              ~f:VpcEncryptionControl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEncryptionControl =
        (Option.map ~f:VpcEncryptionControl.of_xml)
          (Xml.child xml_arg0 "vpcEncryptionControl") in
      make ?vpcEncryptionControl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcEncryptionControl =
        field_map json__ "VpcEncryptionControl" VpcEncryptionControl.of_json in
      make ?vpcEncryptionControl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC Encryption Control configuration for a specified VPC. VPC Encryption Control enables you to enforce encryption for all data in transit within and between VPCs to meet compliance requirements for standards like HIPAA, FedRAMP, and PCI DSS. For more information, see Enforce VPC encryption in transit in the Amazon VPC User Guide."]
module CreateVpcEncryptionControlRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcId: VpcId.t
        [@ocaml.doc
          "The ID of the VPC for which to create the encryption control configuration."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the VPC Encryption Control resource."]}
    let context_ = "CreateVpcEncryptionControlRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ~vpcId -> fun () -> { dryRun; tagSpecifications; vpcId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ~vpcId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ~vpcId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a VPC Encryption Control configuration for a specified VPC. VPC Encryption Control enables you to enforce encryption for all data in transit within and between VPCs to meet compliance requirements for standards like HIPAA, FedRAMP, and PCI DSS. For more information, see Enforce VPC encryption in transit in the Amazon VPC User Guide."]
module CreateVpcBlockPublicAccessExclusionResult =
  struct
    type nonrec t =
      {
      vpcBlockPublicAccessExclusion: VpcBlockPublicAccessExclusion.t option
        [@ocaml.doc "Details about an exclusion."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcBlockPublicAccessExclusion =
      fun () -> { vpcBlockPublicAccessExclusion }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcBlockPublicAccessExclusion",
           (Option.map x.vpcBlockPublicAccessExclusion
              ~f:VpcBlockPublicAccessExclusion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcBlockPublicAccessExclusion =
        (Option.map ~f:VpcBlockPublicAccessExclusion.of_xml)
          (Xml.child xml_arg0 "vpcBlockPublicAccessExclusion") in
      make ?vpcBlockPublicAccessExclusion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcBlockPublicAccessExclusion =
        field_map json__ "VpcBlockPublicAccessExclusion"
          VpcBlockPublicAccessExclusion.of_json in
      make ?vpcBlockPublicAccessExclusion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be applied to a single VPC or subnet that exempts it from the account\226\128\153s BPA mode and will allow bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon VPC User Guide."]
module CreateVpcBlockPublicAccessExclusionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      subnetId: SubnetId.t option [@ocaml.doc "A subnet ID."];
      vpcId: VpcId.t option [@ocaml.doc "A VPC ID."];
      internetGatewayExclusionMode: InternetGatewayExclusionMode.t
        [@ocaml.doc
          "The exclusion mode for internet gateway traffic. allow-bidirectional: Allow all internet traffic to and from the excluded VPCs and subnets. allow-egress: Allow outbound internet traffic from the excluded VPCs and subnets. Block inbound internet traffic to the excluded VPCs and subnets. Only applies when VPC Block Public Access is set to Bidirectional."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "tag - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."]}
    let context_ = "CreateVpcBlockPublicAccessExclusionRequest"
    let make ?dryRun =
      fun ?subnetId ->
        fun ?vpcId ->
          fun ?tagSpecifications ->
            fun ~internetGatewayExclusionMode ->
              fun () ->
                {
                  dryRun;
                  subnetId;
                  vpcId;
                  tagSpecifications;
                  internetGatewayExclusionMode
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SubnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("InternetGatewayExclusionMode",
          (Some
             (InternetGatewayExclusionMode.to_value
                x.internetGatewayExclusionMode)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let internetGatewayExclusionMode =
        InternetGatewayExclusionMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "InternetGatewayExclusionMode") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "SubnetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ~internetGatewayExclusionMode ?vpcId ?subnetId
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let internetGatewayExclusionMode =
        field_map_exn json__ "InternetGatewayExclusionMode"
          InternetGatewayExclusionMode.of_json in
      let vpcId = field_map json__ "VpcId" VpcId.of_json in
      let subnetId = field_map json__ "SubnetId" SubnetId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ~internetGatewayExclusionMode ?vpcId ?subnetId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a VPC Block Public Access (BPA) exclusion. A VPC BPA exclusion is a mode that can be applied to a single VPC or subnet that exempts it from the account\226\128\153s BPA mode and will allow bidirectional or egress-only access. You can create BPA exclusions for VPCs and subnets even when BPA is not enabled on the account to ensure that there is no traffic disruption to the exclusions when VPC BPA is turned on. To learn more about VPC BPA, see Block public access to VPCs and subnets in the Amazon VPC User Guide."]
module CreateVolumeRequest =
  struct
    type nonrec t =
      {
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "The ID of the Availability Zone in which to create the volume. For example, us-east-1a. Either AvailabilityZone or AvailabilityZoneId must be specified, but not both."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "The ID of the Availability Zone in which to create the volume. For example, use1-az1. Either AvailabilityZone or AvailabilityZoneId must be specified, but not both."];
      encrypted: Boolean.t option
        [@ocaml.doc
          "Indicates whether the volume should be encrypted. The effect of setting the encryption state to true depends on the volume origin (new or from a snapshot), starting encryption state, ownership, and whether encryption by default is enabled. For more information, see Encryption by default in the Amazon EBS User Guide. Encrypted Amazon EBS volumes must be attached to instances that support Amazon EBS encryption. For more information, see Supported instance types."];
      iops: Integer.t option
        [@ocaml.doc
          "The number of I/O operations per second (IOPS) to provision for the volume. Required for io1 and io2 volumes. Optional for gp3 volumes. Omit for all other volume types. Valid ranges: gp3: 3,000 (default) - 80,000 IOPS io1: 100 - 64,000 IOPS io2: 100 - 256,000 IOPS Instances built on the Nitro System can support up to 256,000 IOPS. Other instances can support up to 32,000 IOPS."];
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "The identifier of the KMS key to use for Amazon EBS encryption. If this parameter is not specified, your KMS key for Amazon EBS is used. If KmsKeyId is specified, the encrypted state must be true. You can specify the KMS key using any of the following: Key ID. For example, 1234abcd-12ab-34cd-56ef-1234567890ab. Key alias. For example, alias/ExampleAlias. Key ARN. For example, arn:aws:kms:us-east-1:012345678910:key/1234abcd-12ab-34cd-56ef-1234567890ab. Alias ARN. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. Amazon Web Services authenticates the KMS key asynchronously. Therefore, if you specify an ID, alias, or ARN that is not valid, the action can appear to complete, but eventually fails."];
      outpostArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Outpost on which to create the volume. If you intend to use a volume with an instance running on an outpost, then you must create the volume on the same outpost as the instance. You can't use a volume created in an Amazon Web Services Region with an instance on an Amazon Web Services outpost, or the other way around."];
      size: Integer.t option
        [@ocaml.doc
          "The size of the volume, in GiBs. You must specify either a snapshot ID or a volume size. If you specify a snapshot, the default is the snapshot size, and you can specify a volume size that is equal to or larger than the snapshot size. Valid sizes: gp2: 1 - 16,384 GiB gp3: 1 - 65,536 GiB io1: 4 - 16,384 GiB io2: 4 - 65,536 GiB st1 and sc1: 125 - 16,384 GiB standard: 1 - 1024 GiB"];
      snapshotId: SnapshotId.t option
        [@ocaml.doc
          "The snapshot from which to create the volume. You must specify either a snapshot ID or a volume size."];
      volumeType: VolumeType.t option
        [@ocaml.doc
          "The volume type. This parameter can be one of the following values: General Purpose SSD: gp2 | gp3 Provisioned IOPS SSD: io1 | io2 Throughput Optimized HDD: st1 Cold HDD: sc1 Magnetic: standard Throughput Optimized HDD (st1) and Cold HDD (sc1) volumes can't be used as boot volumes. For more information, see Amazon EBS volume types in the Amazon EBS User Guide. Default: gp2"];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the volume during creation."];
      multiAttachEnabled: Boolean.t option
        [@ocaml.doc
          "Indicates whether to enable Amazon EBS Multi-Attach. If you enable Multi-Attach, you can attach the volume to up to 16 Instances built on the Nitro System in the same Availability Zone. This parameter is supported with io1 and io2 volumes only. For more information, see Amazon EBS Multi-Attach in the Amazon EBS User Guide."];
      throughput: Integer.t option
        [@ocaml.doc
          "The throughput to provision for the volume, in MiB/s. Supported for gp3 volumes only. Omit for all other volume types. Valid Range: 125 - 2000 MiB/s"];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      volumeInitializationRate: Integer.t option
        [@ocaml.doc
          "Specifies the Amazon EBS Provisioned Rate for Volume Initialization (volume initialization rate), in MiB/s, at which to download the snapshot blocks from Amazon S3 to the volume. This is also known as volume initialization. Specifying a volume initialization rate ensures that the volume is initialized at a predictable and consistent rate after creation. This parameter is supported only for volumes created from snapshots. Omit this parameter if: You want to create the volume using fast snapshot restore. You must specify a snapshot that is enabled for fast snapshot restore. In this case, the volume is fully initialized at creation. If you specify a snapshot that is enabled for fast snapshot restore and a volume initialization rate, the volume will be initialized at the specified rate instead of fast snapshot restore. You want to create a volume that is initialized at the default rate. For more information, see Initialize Amazon EBS volumes in the Amazon EC2 User Guide. Valid range: 100 - 300 MiB/s"];
      operator: OperatorRequest.t option
        [@ocaml.doc "Reserved for internal use."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?availabilityZone =
      fun ?availabilityZoneId ->
        fun ?encrypted ->
          fun ?iops ->
            fun ?kmsKeyId ->
              fun ?outpostArn ->
                fun ?size ->
                  fun ?snapshotId ->
                    fun ?volumeType ->
                      fun ?tagSpecifications ->
                        fun ?multiAttachEnabled ->
                          fun ?throughput ->
                            fun ?clientToken ->
                              fun ?volumeInitializationRate ->
                                fun ?operator ->
                                  fun ?dryRun ->
                                    fun () ->
                                      {
                                        availabilityZone;
                                        availabilityZoneId;
                                        encrypted;
                                        iops;
                                        kmsKeyId;
                                        outpostArn;
                                        size;
                                        snapshotId;
                                        volumeType;
                                        tagSpecifications;
                                        multiAttachEnabled;
                                        throughput;
                                        clientToken;
                                        volumeInitializationRate;
                                        operator;
                                        dryRun
                                      }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("Iops", (Option.map x.iops ~f:Integer.to_value));
        ("KmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("Size", (Option.map x.size ~f:Integer.to_value));
        ("SnapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value));
        ("VolumeType", (Option.map x.volumeType ~f:VolumeType.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("MultiAttachEnabled",
          (Option.map x.multiAttachEnabled ~f:Boolean.to_value));
        ("Throughput", (Option.map x.throughput ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("VolumeInitializationRate",
          (Option.map x.volumeInitializationRate ~f:Integer.to_value));
        ("Operator", (Option.map x.operator ~f:OperatorRequest.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let operator =
        (Option.map ~f:OperatorRequest.of_xml)
          (Xml.child xml_arg0 "Operator") in
      let volumeInitializationRate =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "VolumeInitializationRate") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let throughput =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Throughput") in
      let multiAttachEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "MultiAttachEnabled") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let volumeType =
        (Option.map ~f:VolumeType.of_xml) (Xml.child xml_arg0 "VolumeType") in
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "SnapshotId") in
      let size = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Size") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "KmsKeyId") in
      let iops = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Iops") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?dryRun ?operator ?volumeInitializationRate ?clientToken
        ?throughput ?multiAttachEnabled ?tagSpecifications ?volumeType
        ?snapshotId ?size ?outpostArn ?kmsKeyId ?iops ?encrypted
        ?availabilityZoneId ?availabilityZone ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let operator = field_map json__ "Operator" OperatorRequest.of_json in
      let volumeInitializationRate =
        field_map json__ "VolumeInitializationRate" Integer.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let throughput = field_map json__ "Throughput" Integer.of_json in
      let multiAttachEnabled =
        field_map json__ "MultiAttachEnabled" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let volumeType = field_map json__ "VolumeType" VolumeType.of_json in
      let snapshotId = field_map json__ "SnapshotId" SnapshotId.of_json in
      let size = field_map json__ "Size" Integer.of_json in
      let outpostArn = field_map json__ "OutpostArn" String_.of_json in
      let kmsKeyId = field_map json__ "KmsKeyId" KmsKeyId.of_json in
      let iops = field_map json__ "Iops" Integer.of_json in
      let encrypted = field_map json__ "Encrypted" Boolean.of_json in
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      make ?dryRun ?operator ?volumeInitializationRate ?clientToken
        ?throughput ?multiAttachEnabled ?tagSpecifications ?volumeType
        ?snapshotId ?size ?outpostArn ?kmsKeyId ?iops ?encrypted
        ?availabilityZoneId ?availabilityZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an EBS volume that can be attached to an instance in the same Availability Zone. You can create a new empty volume or restore a volume from an EBS snapshot. Any Amazon Web Services Marketplace product codes from the snapshot are propagated to the volume. You can create encrypted volumes. Encrypted volumes must be attached to instances that support Amazon EBS encryption. Volumes that are created from encrypted snapshots are also automatically encrypted. For more information, see Amazon EBS encryption in the Amazon EBS User Guide. You can tag your volumes during creation. For more information, see Tag your Amazon EC2 resources in the Amazon EC2 User Guide. For more information, see Create an Amazon EBS volume in the Amazon EBS User Guide."]
module CreateVerifiedAccessTrustProviderResult =
  struct
    type nonrec t =
      {
      verifiedAccessTrustProvider: VerifiedAccessTrustProvider.t option
        [@ocaml.doc "Details about the Verified Access trust provider."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessTrustProvider =
      fun () -> { verifiedAccessTrustProvider }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessTrustProvider",
           (Option.map x.verifiedAccessTrustProvider
              ~f:VerifiedAccessTrustProvider.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessTrustProvider =
        (Option.map ~f:VerifiedAccessTrustProvider.of_xml)
          (Xml.child xml_arg0 "verifiedAccessTrustProvider") in
      make ?verifiedAccessTrustProvider ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessTrustProvider =
        field_map json__ "VerifiedAccessTrustProvider"
          VerifiedAccessTrustProvider.of_json in
      make ?verifiedAccessTrustProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A trust provider is a third-party entity that creates, maintains, and manages identity information for users and devices. When an application request is made, the identity information sent by the trust provider is evaluated by Verified Access before allowing or denying the application request."]
module CreateVerifiedAccessTrustProviderRequest =
  struct
    type nonrec t =
      {
      trustProviderType: TrustProviderType.t
        [@ocaml.doc "The type of trust provider."];
      userTrustProviderType: UserTrustProviderType.t option
        [@ocaml.doc
          "The type of user-based trust provider. This parameter is required when the provider type is user."];
      deviceTrustProviderType: DeviceTrustProviderType.t option
        [@ocaml.doc
          "The type of device-based trust provider. This parameter is required when the provider type is device."];
      oidcOptions: CreateVerifiedAccessTrustProviderOidcOptions.t option
        [@ocaml.doc
          "The options for a OpenID Connect-compatible user-identity trust provider. This parameter is required when the provider type is user."];
      deviceOptions: CreateVerifiedAccessTrustProviderDeviceOptions.t option
        [@ocaml.doc
          "The options for a device-based trust provider. This parameter is required when the provider type is device."];
      policyReferenceName: String_.t
        [@ocaml.doc
          "The identifier to be used when working with policy rules."];
      description: String_.t option
        [@ocaml.doc "A description for the Verified Access trust provider."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to assign to the Verified Access trust provider."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      sseSpecification: VerifiedAccessSseSpecificationRequest.t option
        [@ocaml.doc "The options for server side encryption."];
      nativeApplicationOidcOptions:
        CreateVerifiedAccessNativeApplicationOidcOptions.t option
        [@ocaml.doc "The OpenID Connect (OIDC) options."]}
    let context_ = "CreateVerifiedAccessTrustProviderRequest"
    let make ?userTrustProviderType =
      fun ?deviceTrustProviderType ->
        fun ?oidcOptions ->
          fun ?deviceOptions ->
            fun ?description ->
              fun ?tagSpecifications ->
                fun ?clientToken ->
                  fun ?dryRun ->
                    fun ?sseSpecification ->
                      fun ?nativeApplicationOidcOptions ->
                        fun ~trustProviderType ->
                          fun ~policyReferenceName ->
                            fun () ->
                              {
                                userTrustProviderType;
                                deviceTrustProviderType;
                                oidcOptions;
                                deviceOptions;
                                description;
                                tagSpecifications;
                                clientToken;
                                dryRun;
                                sseSpecification;
                                nativeApplicationOidcOptions;
                                trustProviderType;
                                policyReferenceName
                              }
    let to_value x =
      structure_to_value
        [("TrustProviderType",
           (Some (TrustProviderType.to_value x.trustProviderType)));
        ("UserTrustProviderType",
          (Option.map x.userTrustProviderType
             ~f:UserTrustProviderType.to_value));
        ("DeviceTrustProviderType",
          (Option.map x.deviceTrustProviderType
             ~f:DeviceTrustProviderType.to_value));
        ("OidcOptions",
          (Option.map x.oidcOptions
             ~f:CreateVerifiedAccessTrustProviderOidcOptions.to_value));
        ("DeviceOptions",
          (Option.map x.deviceOptions
             ~f:CreateVerifiedAccessTrustProviderDeviceOptions.to_value));
        ("PolicyReferenceName",
          (Some (String_.to_value x.policyReferenceName)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SseSpecification",
          (Option.map x.sseSpecification
             ~f:VerifiedAccessSseSpecificationRequest.to_value));
        ("NativeApplicationOidcOptions",
          (Option.map x.nativeApplicationOidcOptions
             ~f:CreateVerifiedAccessNativeApplicationOidcOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nativeApplicationOidcOptions =
        (Option.map
           ~f:CreateVerifiedAccessNativeApplicationOidcOptions.of_xml)
          (Xml.child xml_arg0 "NativeApplicationOidcOptions") in
      let sseSpecification =
        (Option.map ~f:VerifiedAccessSseSpecificationRequest.of_xml)
          (Xml.child xml_arg0 "SseSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let policyReferenceName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyReferenceName") in
      let deviceOptions =
        (Option.map ~f:CreateVerifiedAccessTrustProviderDeviceOptions.of_xml)
          (Xml.child xml_arg0 "DeviceOptions") in
      let oidcOptions =
        (Option.map ~f:CreateVerifiedAccessTrustProviderOidcOptions.of_xml)
          (Xml.child xml_arg0 "OidcOptions") in
      let deviceTrustProviderType =
        (Option.map ~f:DeviceTrustProviderType.of_xml)
          (Xml.child xml_arg0 "DeviceTrustProviderType") in
      let userTrustProviderType =
        (Option.map ~f:UserTrustProviderType.of_xml)
          (Xml.child xml_arg0 "UserTrustProviderType") in
      let trustProviderType =
        TrustProviderType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrustProviderType") in
      make ?nativeApplicationOidcOptions ?sseSpecification ?dryRun
        ?clientToken ?tagSpecifications ?description ~policyReferenceName
        ?deviceOptions ?oidcOptions ?deviceTrustProviderType
        ?userTrustProviderType ~trustProviderType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nativeApplicationOidcOptions =
        field_map json__ "NativeApplicationOidcOptions"
          CreateVerifiedAccessNativeApplicationOidcOptions.of_json in
      let sseSpecification =
        field_map json__ "SseSpecification"
          VerifiedAccessSseSpecificationRequest.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let policyReferenceName =
        field_map_exn json__ "PolicyReferenceName" String_.of_json in
      let deviceOptions =
        field_map json__ "DeviceOptions"
          CreateVerifiedAccessTrustProviderDeviceOptions.of_json in
      let oidcOptions =
        field_map json__ "OidcOptions"
          CreateVerifiedAccessTrustProviderOidcOptions.of_json in
      let deviceTrustProviderType =
        field_map json__ "DeviceTrustProviderType"
          DeviceTrustProviderType.of_json in
      let userTrustProviderType =
        field_map json__ "UserTrustProviderType"
          UserTrustProviderType.of_json in
      let trustProviderType =
        field_map_exn json__ "TrustProviderType" TrustProviderType.of_json in
      make ?nativeApplicationOidcOptions ?sseSpecification ?dryRun
        ?clientToken ?tagSpecifications ?description ~policyReferenceName
        ?deviceOptions ?oidcOptions ?deviceTrustProviderType
        ?userTrustProviderType ~trustProviderType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A trust provider is a third-party entity that creates, maintains, and manages identity information for users and devices. When an application request is made, the identity information sent by the trust provider is evaluated by Verified Access before allowing or denying the application request."]
module CreateVerifiedAccessInstanceResult =
  struct
    type nonrec t =
      {
      verifiedAccessInstance: VerifiedAccessInstance.t option
        [@ocaml.doc "Details about the Verified Access instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessInstance = fun () -> { verifiedAccessInstance }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessInstance",
           (Option.map x.verifiedAccessInstance
              ~f:VerifiedAccessInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessInstance =
        (Option.map ~f:VerifiedAccessInstance.of_xml)
          (Xml.child xml_arg0 "verifiedAccessInstance") in
      make ?verifiedAccessInstance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessInstance =
        field_map json__ "VerifiedAccessInstance"
          VerifiedAccessInstance.of_json in
      make ?verifiedAccessInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services Verified Access instance is a regional entity that evaluates application requests and grants access only when your security requirements are met."]
module CreateVerifiedAccessInstanceRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description for the Verified Access instance."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the Verified Access instance."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      fIPSEnabled: Boolean.t option
        [@ocaml.doc
          "Enable or disable support for Federal Information Processing Standards (FIPS) on the instance."];
      cidrEndpointsCustomSubDomain: String_.t option
        [@ocaml.doc "The custom subdomain."]}
    let make ?description =
      fun ?tagSpecifications ->
        fun ?clientToken ->
          fun ?dryRun ->
            fun ?fIPSEnabled ->
              fun ?cidrEndpointsCustomSubDomain ->
                fun () ->
                  {
                    description;
                    tagSpecifications;
                    clientToken;
                    dryRun;
                    fIPSEnabled;
                    cidrEndpointsCustomSubDomain
                  }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("FIPSEnabled", (Option.map x.fIPSEnabled ~f:Boolean.to_value));
        ("CidrEndpointsCustomSubDomain",
          (Option.map x.cidrEndpointsCustomSubDomain ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidrEndpointsCustomSubDomain =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CidrEndpointsCustomSubDomain") in
      let fIPSEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FIPSEnabled") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?cidrEndpointsCustomSubDomain ?fIPSEnabled ?dryRun ?clientToken
        ?tagSpecifications ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cidrEndpointsCustomSubDomain =
        field_map json__ "CidrEndpointsCustomSubDomain" String_.of_json in
      let fIPSEnabled = field_map json__ "FIPSEnabled" Boolean.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json__ "Description" String_.of_json in
      make ?cidrEndpointsCustomSubDomain ?fIPSEnabled ?dryRun ?clientToken
        ?tagSpecifications ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services Verified Access instance is a regional entity that evaluates application requests and grants access only when your security requirements are met."]
module CreateVerifiedAccessGroupResult =
  struct
    type nonrec t =
      {
      verifiedAccessGroup: VerifiedAccessGroup.t option
        [@ocaml.doc "Details about the Verified Access group."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessGroup = fun () -> { verifiedAccessGroup }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessGroup",
           (Option.map x.verifiedAccessGroup ~f:VerifiedAccessGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessGroup =
        (Option.map ~f:VerifiedAccessGroup.of_xml)
          (Xml.child xml_arg0 "verifiedAccessGroup") in
      make ?verifiedAccessGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessGroup =
        field_map json__ "VerifiedAccessGroup" VerifiedAccessGroup.of_json in
      make ?verifiedAccessGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services Verified Access group is a collection of Amazon Web Services Verified Access endpoints who's associated applications have similar security requirements. Each instance within a Verified Access group shares an Verified Access policy. For example, you can group all Verified Access instances associated with \"sales\" applications together and use one common Verified Access policy."]
module CreateVerifiedAccessGroupRequest =
  struct
    type nonrec t =
      {
      verifiedAccessInstanceId: VerifiedAccessInstanceId.t
        [@ocaml.doc "The ID of the Verified Access instance."];
      description: String_.t option
        [@ocaml.doc "A description for the Verified Access group."];
      policyDocument: String_.t option
        [@ocaml.doc "The Verified Access policy document."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the Verified Access group."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      sseSpecification: VerifiedAccessSseSpecificationRequest.t option
        [@ocaml.doc "The options for server side encryption."]}
    let context_ = "CreateVerifiedAccessGroupRequest"
    let make ?description =
      fun ?policyDocument ->
        fun ?tagSpecifications ->
          fun ?clientToken ->
            fun ?dryRun ->
              fun ?sseSpecification ->
                fun ~verifiedAccessInstanceId ->
                  fun () ->
                    {
                      description;
                      policyDocument;
                      tagSpecifications;
                      clientToken;
                      dryRun;
                      sseSpecification;
                      verifiedAccessInstanceId
                    }
    let to_value x =
      structure_to_value
        [("VerifiedAccessInstanceId",
           (Some
              (VerifiedAccessInstanceId.to_value x.verifiedAccessInstanceId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("PolicyDocument", (Option.map x.policyDocument ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SseSpecification",
          (Option.map x.sseSpecification
             ~f:VerifiedAccessSseSpecificationRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sseSpecification =
        (Option.map ~f:VerifiedAccessSseSpecificationRequest.of_xml)
          (Xml.child xml_arg0 "SseSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let policyDocument =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PolicyDocument") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let verifiedAccessInstanceId =
        VerifiedAccessInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VerifiedAccessInstanceId") in
      make ?sseSpecification ?dryRun ?clientToken ?tagSpecifications
        ?policyDocument ?description ~verifiedAccessInstanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sseSpecification =
        field_map json__ "SseSpecification"
          VerifiedAccessSseSpecificationRequest.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let policyDocument = field_map json__ "PolicyDocument" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let verifiedAccessInstanceId =
        field_map_exn json__ "VerifiedAccessInstanceId"
          VerifiedAccessInstanceId.of_json in
      make ?sseSpecification ?dryRun ?clientToken ?tagSpecifications
        ?policyDocument ?description ~verifiedAccessInstanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services Verified Access group is a collection of Amazon Web Services Verified Access endpoints who's associated applications have similar security requirements. Each instance within a Verified Access group shares an Verified Access policy. For example, you can group all Verified Access instances associated with \"sales\" applications together and use one common Verified Access policy."]
module CreateVerifiedAccessEndpointResult =
  struct
    type nonrec t =
      {
      verifiedAccessEndpoint: VerifiedAccessEndpoint.t option
        [@ocaml.doc "Details about the Verified Access endpoint."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessEndpoint = fun () -> { verifiedAccessEndpoint }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessEndpoint",
           (Option.map x.verifiedAccessEndpoint
              ~f:VerifiedAccessEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessEndpoint =
        (Option.map ~f:VerifiedAccessEndpoint.of_xml)
          (Xml.child xml_arg0 "verifiedAccessEndpoint") in
      make ?verifiedAccessEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessEndpoint =
        field_map json__ "VerifiedAccessEndpoint"
          VerifiedAccessEndpoint.of_json in
      make ?verifiedAccessEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services Verified Access endpoint is where you define your application along with an optional endpoint-level access policy."]
module CreateVerifiedAccessEndpointRequest =
  struct
    type nonrec t =
      {
      verifiedAccessGroupId: VerifiedAccessGroupId.t
        [@ocaml.doc
          "The ID of the Verified Access group to associate the endpoint with."];
      endpointType: VerifiedAccessEndpointType.t
        [@ocaml.doc "The type of Verified Access endpoint to create."];
      attachmentType: VerifiedAccessEndpointAttachmentType.t
        [@ocaml.doc "The type of attachment."];
      domainCertificateArn: CertificateArn.t option
        [@ocaml.doc
          "The ARN of the public TLS/SSL certificate in Amazon Web Services Certificate Manager to associate with the endpoint. The CN in the certificate must match the DNS name your end users will use to reach your application."];
      applicationDomain: String_.t option
        [@ocaml.doc "The DNS name for users to reach your application."];
      endpointDomainPrefix: String_.t option
        [@ocaml.doc
          "A custom identifier that is prepended to the DNS name that is generated for the endpoint."];
      securityGroupIds: SecurityGroupIdList.t option
        [@ocaml.doc
          "The IDs of the security groups to associate with the Verified Access endpoint. Required if AttachmentType is set to vpc."];
      loadBalancerOptions:
        CreateVerifiedAccessEndpointLoadBalancerOptions.t option
        [@ocaml.doc
          "The load balancer details. This parameter is required if the endpoint type is load-balancer."];
      networkInterfaceOptions:
        CreateVerifiedAccessEndpointEniOptions.t option
        [@ocaml.doc
          "The network interface details. This parameter is required if the endpoint type is network-interface."];
      description: String_.t option
        [@ocaml.doc "A description for the Verified Access endpoint."];
      policyDocument: String_.t option
        [@ocaml.doc "The Verified Access policy document."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the Verified Access endpoint."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      sseSpecification: VerifiedAccessSseSpecificationRequest.t option
        [@ocaml.doc "The options for server side encryption."];
      rdsOptions: CreateVerifiedAccessEndpointRdsOptions.t option
        [@ocaml.doc
          "The RDS details. This parameter is required if the endpoint type is rds."];
      cidrOptions: CreateVerifiedAccessEndpointCidrOptions.t option
        [@ocaml.doc
          "The CIDR options. This parameter is required if the endpoint type is cidr."]}
    let context_ = "CreateVerifiedAccessEndpointRequest"
    let make ?domainCertificateArn =
      fun ?applicationDomain ->
        fun ?endpointDomainPrefix ->
          fun ?securityGroupIds ->
            fun ?loadBalancerOptions ->
              fun ?networkInterfaceOptions ->
                fun ?description ->
                  fun ?policyDocument ->
                    fun ?tagSpecifications ->
                      fun ?clientToken ->
                        fun ?dryRun ->
                          fun ?sseSpecification ->
                            fun ?rdsOptions ->
                              fun ?cidrOptions ->
                                fun ~verifiedAccessGroupId ->
                                  fun ~endpointType ->
                                    fun ~attachmentType ->
                                      fun () ->
                                        {
                                          domainCertificateArn;
                                          applicationDomain;
                                          endpointDomainPrefix;
                                          securityGroupIds;
                                          loadBalancerOptions;
                                          networkInterfaceOptions;
                                          description;
                                          policyDocument;
                                          tagSpecifications;
                                          clientToken;
                                          dryRun;
                                          sseSpecification;
                                          rdsOptions;
                                          cidrOptions;
                                          verifiedAccessGroupId;
                                          endpointType;
                                          attachmentType
                                        }
    let to_value x =
      structure_to_value
        [("VerifiedAccessGroupId",
           (Some (VerifiedAccessGroupId.to_value x.verifiedAccessGroupId)));
        ("EndpointType",
          (Some (VerifiedAccessEndpointType.to_value x.endpointType)));
        ("AttachmentType",
          (Some
             (VerifiedAccessEndpointAttachmentType.to_value x.attachmentType)));
        ("DomainCertificateArn",
          (Option.map x.domainCertificateArn ~f:CertificateArn.to_value));
        ("ApplicationDomain",
          (Option.map x.applicationDomain ~f:String_.to_value));
        ("EndpointDomainPrefix",
          (Option.map x.endpointDomainPrefix ~f:String_.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds ~f:SecurityGroupIdList.to_value));
        ("LoadBalancerOptions",
          (Option.map x.loadBalancerOptions
             ~f:CreateVerifiedAccessEndpointLoadBalancerOptions.to_value));
        ("NetworkInterfaceOptions",
          (Option.map x.networkInterfaceOptions
             ~f:CreateVerifiedAccessEndpointEniOptions.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("PolicyDocument", (Option.map x.policyDocument ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SseSpecification",
          (Option.map x.sseSpecification
             ~f:VerifiedAccessSseSpecificationRequest.to_value));
        ("RdsOptions",
          (Option.map x.rdsOptions
             ~f:CreateVerifiedAccessEndpointRdsOptions.to_value));
        ("CidrOptions",
          (Option.map x.cidrOptions
             ~f:CreateVerifiedAccessEndpointCidrOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidrOptions =
        (Option.map ~f:CreateVerifiedAccessEndpointCidrOptions.of_xml)
          (Xml.child xml_arg0 "CidrOptions") in
      let rdsOptions =
        (Option.map ~f:CreateVerifiedAccessEndpointRdsOptions.of_xml)
          (Xml.child xml_arg0 "RdsOptions") in
      let sseSpecification =
        (Option.map ~f:VerifiedAccessSseSpecificationRequest.of_xml)
          (Xml.child xml_arg0 "SseSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let policyDocument =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PolicyDocument") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let networkInterfaceOptions =
        (Option.map ~f:CreateVerifiedAccessEndpointEniOptions.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceOptions") in
      let loadBalancerOptions =
        (Option.map ~f:CreateVerifiedAccessEndpointLoadBalancerOptions.of_xml)
          (Xml.child xml_arg0 "LoadBalancerOptions") in
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let endpointDomainPrefix =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "EndpointDomainPrefix") in
      let applicationDomain =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ApplicationDomain") in
      let domainCertificateArn =
        (Option.map ~f:CertificateArn.of_xml)
          (Xml.child xml_arg0 "DomainCertificateArn") in
      let attachmentType =
        VerifiedAccessEndpointAttachmentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AttachmentType") in
      let endpointType =
        VerifiedAccessEndpointType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EndpointType") in
      let verifiedAccessGroupId =
        VerifiedAccessGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VerifiedAccessGroupId") in
      make ?cidrOptions ?rdsOptions ?sseSpecification ?dryRun ?clientToken
        ?tagSpecifications ?policyDocument ?description
        ?networkInterfaceOptions ?loadBalancerOptions ?securityGroupIds
        ?endpointDomainPrefix ?applicationDomain ?domainCertificateArn
        ~attachmentType ~endpointType ~verifiedAccessGroupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cidrOptions =
        field_map json__ "CidrOptions"
          CreateVerifiedAccessEndpointCidrOptions.of_json in
      let rdsOptions =
        field_map json__ "RdsOptions"
          CreateVerifiedAccessEndpointRdsOptions.of_json in
      let sseSpecification =
        field_map json__ "SseSpecification"
          VerifiedAccessSseSpecificationRequest.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let policyDocument = field_map json__ "PolicyDocument" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let networkInterfaceOptions =
        field_map json__ "NetworkInterfaceOptions"
          CreateVerifiedAccessEndpointEniOptions.of_json in
      let loadBalancerOptions =
        field_map json__ "LoadBalancerOptions"
          CreateVerifiedAccessEndpointLoadBalancerOptions.of_json in
      let securityGroupIds =
        field_map json__ "SecurityGroupIds" SecurityGroupIdList.of_json in
      let endpointDomainPrefix =
        field_map json__ "EndpointDomainPrefix" String_.of_json in
      let applicationDomain =
        field_map json__ "ApplicationDomain" String_.of_json in
      let domainCertificateArn =
        field_map json__ "DomainCertificateArn" CertificateArn.of_json in
      let attachmentType =
        field_map_exn json__ "AttachmentType"
          VerifiedAccessEndpointAttachmentType.of_json in
      let endpointType =
        field_map_exn json__ "EndpointType"
          VerifiedAccessEndpointType.of_json in
      let verifiedAccessGroupId =
        field_map_exn json__ "VerifiedAccessGroupId"
          VerifiedAccessGroupId.of_json in
      make ?cidrOptions ?rdsOptions ?sseSpecification ?dryRun ?clientToken
        ?tagSpecifications ?policyDocument ?description
        ?networkInterfaceOptions ?loadBalancerOptions ?securityGroupIds
        ?endpointDomainPrefix ?applicationDomain ?domainCertificateArn
        ~attachmentType ~endpointType ~verifiedAccessGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services Verified Access endpoint is where you define your application along with an optional endpoint-level access policy."]
module CreateTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option
        [@ocaml.doc "Information about the VPC attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayVpcAttachment =
        field_map json__ "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches the specified VPC to the specified transit gateway. If you attach a VPC with a CIDR range that overlaps the CIDR range of a VPC that is already attached, the new VPC CIDR range is not propagated to the default propagation route table. To send VPC traffic to an attached transit gateway, add a route to the VPC route table using CreateRoute."]
module CreateTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc "The ID of the transit gateway."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      subnetIds: TransitGatewaySubnetIdList.t
        [@ocaml.doc
          "The IDs of one or more subnets. You can specify only one subnet per Availability Zone. You must specify at least one subnet, but we recommend that you specify two subnets for better availability. The transit gateway uses one IP address from each specified subnet."];
      options: CreateTransitGatewayVpcAttachmentRequestOptions.t option
        [@ocaml.doc "The VPC attachment options."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the VPC attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayVpcAttachmentRequest"
    let make ?options =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun ~vpcId ->
              fun ~subnetIds ->
                fun () ->
                  {
                    options;
                    tagSpecifications;
                    dryRun;
                    transitGatewayId;
                    vpcId;
                    subnetIds
                  }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("SubnetIds",
          (Some (TransitGatewaySubnetIdList.to_value x.subnetIds)));
        ("Options",
          (Option.map x.options
             ~f:CreateTransitGatewayVpcAttachmentRequestOptions.to_value));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let options =
        (Option.map ~f:CreateTransitGatewayVpcAttachmentRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let subnetIds =
        TransitGatewaySubnetIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubnetIds") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ?options ~subnetIds ~vpcId
        ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json__ "Options"
          CreateTransitGatewayVpcAttachmentRequestOptions.of_json in
      let subnetIds =
        field_map_exn json__ "SubnetIds" TransitGatewaySubnetIdList.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?options ~subnetIds ~vpcId
        ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches the specified VPC to the specified transit gateway. If you attach a VPC with a CIDR range that overlaps the CIDR range of a VPC that is already attached, the new VPC CIDR range is not propagated to the default propagation route table. To send VPC traffic to an attached transit gateway, add a route to the VPC route table using CreateRoute."]
module CreateTransitGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTable: TransitGatewayRouteTable.t option
        [@ocaml.doc "Information about the transit gateway route table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTable =
      fun () -> { transitGatewayRouteTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTable",
           (Option.map x.transitGatewayRouteTable
              ~f:TransitGatewayRouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayRouteTable =
        (Option.map ~f:TransitGatewayRouteTable.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTable") in
      make ?transitGatewayRouteTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayRouteTable =
        field_map json__ "TransitGatewayRouteTable"
          TransitGatewayRouteTable.of_json in
      make ?transitGatewayRouteTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a route table for the specified transit gateway."]
module CreateTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc "The ID of the transit gateway."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the transit gateway route table."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayRouteTableRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transitGatewayId ->
          fun () -> { tagSpecifications; dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a route table for the specified transit gateway."]
module CreateTransitGatewayRouteTableAnnouncementResult =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableAnnouncement:
        TransitGatewayRouteTableAnnouncement.t option
        [@ocaml.doc
          "Provides details about the transit gateway route table announcement."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayRouteTableAnnouncement =
      fun () -> { transitGatewayRouteTableAnnouncement }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayRouteTableAnnouncement",
           (Option.map x.transitGatewayRouteTableAnnouncement
              ~f:TransitGatewayRouteTableAnnouncement.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayRouteTableAnnouncement =
        (Option.map ~f:TransitGatewayRouteTableAnnouncement.of_xml)
          (Xml.child xml_arg0 "transitGatewayRouteTableAnnouncement") in
      make ?transitGatewayRouteTableAnnouncement ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayRouteTableAnnouncement =
        field_map json__ "TransitGatewayRouteTableAnnouncement"
          TransitGatewayRouteTableAnnouncement.of_json in
      make ?transitGatewayRouteTableAnnouncement ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Advertises a new transit gateway route table."]
module CreateTransitGatewayRouteTableAnnouncementRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      peeringAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the peering attachment."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags specifications applied to the transit gateway route table announcement."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayRouteTableAnnouncementRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transitGatewayRouteTableId ->
          fun ~peeringAttachmentId ->
            fun () ->
              {
                tagSpecifications;
                dryRun;
                transitGatewayRouteTableId;
                peeringAttachmentId
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("PeeringAttachmentId",
          (Some (TransitGatewayAttachmentId.to_value x.peeringAttachmentId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let peeringAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeeringAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?tagSpecifications ~peeringAttachmentId
        ~transitGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let peeringAttachmentId =
        field_map_exn json__ "PeeringAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?tagSpecifications ~peeringAttachmentId
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Advertises a new transit gateway route table."]
module CreateTransitGatewayRouteResult =
  struct
    type nonrec t =
      {
      route: TransitGatewayRoute.t option
        [@ocaml.doc "Information about the route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:TransitGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:TransitGatewayRoute.of_xml)
          (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let route = field_map json__ "Route" TransitGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a static route for the specified transit gateway route table."]
module CreateTransitGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The CIDR range used for destination matches. Routing decisions are based on the most specific match."];
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc "The ID of the attachment."];
      blackhole: Boolean.t option
        [@ocaml.doc
          "Indicates whether to drop traffic that matches this route."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayRouteRequest"
    let make ?transitGatewayAttachmentId =
      fun ?blackhole ->
        fun ?dryRun ->
          fun ~destinationCidrBlock ->
            fun ~transitGatewayRouteTableId ->
              fun () ->
                {
                  transitGatewayAttachmentId;
                  blackhole;
                  dryRun;
                  destinationCidrBlock;
                  transitGatewayRouteTableId
                }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Some (String_.to_value x.destinationCidrBlock)));
        ("TransitGatewayRouteTableId",
          (Some
             (TransitGatewayRouteTableId.to_value
                x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("Blackhole", (Option.map x.blackhole ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let blackhole =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Blackhole") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId
        ~transitGatewayRouteTableId ~destinationCidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let blackhole = field_map json__ "Blackhole" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId
        ~transitGatewayRouteTableId ~destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a static route for the specified transit gateway route table."]
module CreateTransitGatewayResult =
  struct
    type nonrec t =
      {
      transitGateway: TransitGateway.t option
        [@ocaml.doc "Information about the transit gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGateway = fun () -> { transitGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGateway",
           (Option.map x.transitGateway ~f:TransitGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGateway =
        (Option.map ~f:TransitGateway.of_xml)
          (Xml.child xml_arg0 "transitGateway") in
      make ?transitGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGateway =
        field_map json__ "TransitGateway" TransitGateway.of_json in
      make ?transitGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a transit gateway. You can use a transit gateway to interconnect your virtual private clouds (VPC) and on-premises networks. After the transit gateway enters the available state, you can attach your VPCs and VPN connections to the transit gateway. To attach your VPCs, use CreateTransitGatewayVpcAttachment. To attach a VPN connection, use CreateCustomerGateway to create a customer gateway and specify the ID of the customer gateway and the ID of the transit gateway in a call to CreateVpnConnection. When you create a transit gateway, we create a default transit gateway route table and use it as the default association route table and the default propagation route table. You can use CreateTransitGatewayRouteTable to create additional transit gateway route tables. If you disable automatic route propagation, we do not create a default transit gateway route table. You can use EnableTransitGatewayRouteTablePropagation to propagate routes from a resource attachment to a transit gateway route table. If you disable automatic associations, you can use AssociateTransitGatewayRouteTable to associate a resource attachment with a transit gateway route table."]
module CreateTransitGatewayRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description of the transit gateway."];
      options: TransitGatewayRequestOptions.t option
        [@ocaml.doc "The transit gateway options."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the transit gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?description =
      fun ?options ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun () -> { description; options; tagSpecifications; dryRun }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("Options",
          (Option.map x.options ~f:TransitGatewayRequestOptions.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        (Option.map ~f:TransitGatewayRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?dryRun ?tagSpecifications ?options ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json__ "Options" TransitGatewayRequestOptions.of_json in
      let description = field_map json__ "Description" String_.of_json in
      make ?dryRun ?tagSpecifications ?options ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a transit gateway. You can use a transit gateway to interconnect your virtual private clouds (VPC) and on-premises networks. After the transit gateway enters the available state, you can attach your VPCs and VPN connections to the transit gateway. To attach your VPCs, use CreateTransitGatewayVpcAttachment. To attach a VPN connection, use CreateCustomerGateway to create a customer gateway and specify the ID of the customer gateway and the ID of the transit gateway in a call to CreateVpnConnection. When you create a transit gateway, we create a default transit gateway route table and use it as the default association route table and the default propagation route table. You can use CreateTransitGatewayRouteTable to create additional transit gateway route tables. If you disable automatic route propagation, we do not create a default transit gateway route table. You can use EnableTransitGatewayRouteTablePropagation to propagate routes from a resource attachment to a transit gateway route table. If you disable automatic associations, you can use AssociateTransitGatewayRouteTable to associate a resource attachment with a transit gateway route table."]
module CreateTransitGatewayPrefixListReferenceResult =
  struct
    type nonrec t =
      {
      transitGatewayPrefixListReference:
        TransitGatewayPrefixListReference.t option
        [@ocaml.doc "Information about the prefix list reference."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPrefixListReference =
      fun () -> { transitGatewayPrefixListReference }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPrefixListReference",
           (Option.map x.transitGatewayPrefixListReference
              ~f:TransitGatewayPrefixListReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPrefixListReference =
        (Option.map ~f:TransitGatewayPrefixListReference.of_xml)
          (Xml.child xml_arg0 "transitGatewayPrefixListReference") in
      make ?transitGatewayPrefixListReference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPrefixListReference =
        field_map json__ "TransitGatewayPrefixListReference"
          TransitGatewayPrefixListReference.of_json in
      make ?transitGatewayPrefixListReference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a reference (route) to a prefix list in a specified transit gateway route table."]
module CreateTransitGatewayPrefixListReferenceRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      prefixListId: PrefixListResourceId.t
        [@ocaml.doc
          "The ID of the prefix list that is used for destination matches."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc "The ID of the attachment to which traffic is routed."];
      blackhole: Boolean.t option
        [@ocaml.doc
          "Indicates whether to drop traffic that matches this route."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayPrefixListReferenceRequest"
    let make ?transitGatewayAttachmentId =
      fun ?blackhole ->
        fun ?dryRun ->
          fun ~transitGatewayRouteTableId ->
            fun ~prefixListId ->
              fun () ->
                {
                  transitGatewayAttachmentId;
                  blackhole;
                  dryRun;
                  transitGatewayRouteTableId;
                  prefixListId
                }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("PrefixListId",
          (Some (PrefixListResourceId.to_value x.prefixListId)));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("Blackhole", (Option.map x.blackhole ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let blackhole =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Blackhole") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let prefixListId =
        PrefixListResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId ~prefixListId
        ~transitGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let blackhole = field_map json__ "Blackhole" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let prefixListId =
        field_map_exn json__ "PrefixListId" PrefixListResourceId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ?blackhole ?transitGatewayAttachmentId ~prefixListId
        ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a reference (route) to a prefix list in a specified transit gateway route table."]
module CreateTransitGatewayPolicyTableResult =
  struct
    type nonrec t =
      {
      transitGatewayPolicyTable: TransitGatewayPolicyTable.t option
        [@ocaml.doc "Describes the created transit gateway policy table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPolicyTable =
      fun () -> { transitGatewayPolicyTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPolicyTable",
           (Option.map x.transitGatewayPolicyTable
              ~f:TransitGatewayPolicyTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPolicyTable =
        (Option.map ~f:TransitGatewayPolicyTable.of_xml)
          (Xml.child xml_arg0 "transitGatewayPolicyTable") in
      make ?transitGatewayPolicyTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPolicyTable =
        field_map json__ "TransitGatewayPolicyTable"
          TransitGatewayPolicyTable.of_json in
      make ?transitGatewayPolicyTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a transit gateway policy table."]
module CreateTransitGatewayPolicyTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc
          "The ID of the transit gateway used for the policy table."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags specification for the transit gateway policy table created during the request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayPolicyTableRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transitGatewayId ->
          fun () -> { tagSpecifications; dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a transit gateway policy table."]
module CreateTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option
        [@ocaml.doc "The transit gateway peering attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPeeringAttachment =
        field_map json__ "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a transit gateway peering attachment between the specified transit gateway (requester) and a peer transit gateway (accepter). The peer transit gateway can be in your account or a different Amazon Web Services account. After you create the peering attachment, the owner of the accepter transit gateway must accept the attachment request."]
module CreateTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc "The ID of the transit gateway."];
      peerTransitGatewayId: TransitAssociationGatewayId.t
        [@ocaml.doc
          "The ID of the peer transit gateway with which to create the peering attachment."];
      peerAccountId: String_.t
        [@ocaml.doc
          "The ID of the Amazon Web Services account that owns the peer transit gateway."];
      peerRegion: String_.t
        [@ocaml.doc "The Region where the peer transit gateway is located."];
      options: CreateTransitGatewayPeeringAttachmentRequestOptions.t option
        [@ocaml.doc "Requests a transit gateway peering attachment."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the transit gateway peering attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayPeeringAttachmentRequest"
    let make ?options =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun ~peerTransitGatewayId ->
              fun ~peerAccountId ->
                fun ~peerRegion ->
                  fun () ->
                    {
                      options;
                      tagSpecifications;
                      dryRun;
                      transitGatewayId;
                      peerTransitGatewayId;
                      peerAccountId;
                      peerRegion
                    }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("PeerTransitGatewayId",
          (Some (TransitAssociationGatewayId.to_value x.peerTransitGatewayId)));
        ("PeerAccountId", (Some (String_.to_value x.peerAccountId)));
        ("PeerRegion", (Some (String_.to_value x.peerRegion)));
        ("Options",
          (Option.map x.options
             ~f:CreateTransitGatewayPeeringAttachmentRequestOptions.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        (Option.map
           ~f:CreateTransitGatewayPeeringAttachmentRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let peerRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerRegion") in
      let peerAccountId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerAccountId") in
      let peerTransitGatewayId =
        TransitAssociationGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerTransitGatewayId") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ?options ~peerRegion ~peerAccountId
        ~peerTransitGatewayId ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json__ "Options"
          CreateTransitGatewayPeeringAttachmentRequestOptions.of_json in
      let peerRegion = field_map_exn json__ "PeerRegion" String_.of_json in
      let peerAccountId =
        field_map_exn json__ "PeerAccountId" String_.of_json in
      let peerTransitGatewayId =
        field_map_exn json__ "PeerTransitGatewayId"
          TransitAssociationGatewayId.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?options ~peerRegion ~peerAccountId
        ~peerTransitGatewayId ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a transit gateway peering attachment between the specified transit gateway (requester) and a peer transit gateway (accepter). The peer transit gateway can be in your account or a different Amazon Web Services account. After you create the peering attachment, the owner of the accepter transit gateway must accept the attachment request."]
module CreateTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomain: TransitGatewayMulticastDomain.t option
        [@ocaml.doc
          "Information about the transit gateway multicast domain."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMulticastDomain =
      fun () -> { transitGatewayMulticastDomain }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMulticastDomain",
           (Option.map x.transitGatewayMulticastDomain
              ~f:TransitGatewayMulticastDomain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMulticastDomain =
        (Option.map ~f:TransitGatewayMulticastDomain.of_xml)
          (Xml.child xml_arg0 "transitGatewayMulticastDomain") in
      make ?transitGatewayMulticastDomain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayMulticastDomain =
        field_map json__ "TransitGatewayMulticastDomain"
          TransitGatewayMulticastDomain.of_json in
      make ?transitGatewayMulticastDomain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a multicast domain using the specified transit gateway. The transit gateway must be in the available state before you create a domain. Use DescribeTransitGateways to see the state of transit gateway."]
module CreateTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc "The ID of the transit gateway."];
      options: CreateTransitGatewayMulticastDomainRequestOptions.t option
        [@ocaml.doc "The options for the transit gateway multicast domain."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags for the transit gateway multicast domain."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayMulticastDomainRequest"
    let make ?options =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun () ->
              { options; tagSpecifications; dryRun; transitGatewayId }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("Options",
          (Option.map x.options
             ~f:CreateTransitGatewayMulticastDomainRequestOptions.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        (Option.map
           ~f:CreateTransitGatewayMulticastDomainRequestOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ?options ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map json__ "Options"
          CreateTransitGatewayMulticastDomainRequestOptions.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?options ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a multicast domain using the specified transit gateway. The transit gateway must be in the available state before you create a domain. Use DescribeTransitGateways to see the state of transit gateway."]
module CreateTransitGatewayMeteringPolicyResult =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicy: TransitGatewayMeteringPolicy.t option
        [@ocaml.doc
          "Information about the created transit gateway metering policy."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMeteringPolicy =
      fun () -> { transitGatewayMeteringPolicy }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMeteringPolicy",
           (Option.map x.transitGatewayMeteringPolicy
              ~f:TransitGatewayMeteringPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMeteringPolicy =
        (Option.map ~f:TransitGatewayMeteringPolicy.of_xml)
          (Xml.child xml_arg0 "transitGatewayMeteringPolicy") in
      make ?transitGatewayMeteringPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayMeteringPolicy =
        field_map json__ "TransitGatewayMeteringPolicy"
          TransitGatewayMeteringPolicy.of_json in
      make ?transitGatewayMeteringPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a metering policy for a transit gateway to track and measure network traffic."]
module CreateTransitGatewayMeteringPolicyRequest =
  struct
    type nonrec t =
      {
      transitGatewayId: TransitGatewayId.t
        [@ocaml.doc
          "The ID of the transit gateway for which to create the metering policy."];
      middleboxAttachmentIds: TransitGatewayAttachmentIdStringList.t option
        [@ocaml.doc
          "The IDs of the middlebox attachments to include in the metering policy."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the metering policy."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayMeteringPolicyRequest"
    let make ?middleboxAttachmentIds =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~transitGatewayId ->
            fun () ->
              {
                middleboxAttachmentIds;
                tagSpecifications;
                dryRun;
                transitGatewayId
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayId",
           (Some (TransitGatewayId.to_value x.transitGatewayId)));
        ("MiddleboxAttachmentId",
          (Option.map x.middleboxAttachmentIds
             ~f:TransitGatewayAttachmentIdStringList.to_value));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let middleboxAttachmentIds =
        (Option.map ~f:TransitGatewayAttachmentIdStringList.of_xml)
          (Xml.child xml_arg0 "MiddleboxAttachmentId") in
      let transitGatewayId =
        TransitGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransitGatewayId") in
      make ?dryRun ?tagSpecifications ?middleboxAttachmentIds
        ~transitGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let middleboxAttachmentIds =
        field_map json__ "MiddleboxAttachmentIds"
          TransitGatewayAttachmentIdStringList.of_json in
      let transitGatewayId =
        field_map_exn json__ "TransitGatewayId" TransitGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?middleboxAttachmentIds
        ~transitGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a metering policy for a transit gateway to track and measure network traffic."]
module CreateTransitGatewayMeteringPolicyEntryResult =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicyEntry:
        TransitGatewayMeteringPolicyEntry.t option
        [@ocaml.doc
          "Information about the created transit gateway metering policy entry."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayMeteringPolicyEntry =
      fun () -> { transitGatewayMeteringPolicyEntry }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayMeteringPolicyEntry",
           (Option.map x.transitGatewayMeteringPolicyEntry
              ~f:TransitGatewayMeteringPolicyEntry.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayMeteringPolicyEntry =
        (Option.map ~f:TransitGatewayMeteringPolicyEntry.of_xml)
          (Xml.child xml_arg0 "transitGatewayMeteringPolicyEntry") in
      make ?transitGatewayMeteringPolicyEntry ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayMeteringPolicyEntry =
        field_map json__ "TransitGatewayMeteringPolicyEntry"
          TransitGatewayMeteringPolicyEntry.of_json in
      make ?transitGatewayMeteringPolicyEntry ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an entry in a transit gateway metering policy to define traffic measurement rules."]
module CreateTransitGatewayMeteringPolicyEntryRequest =
  struct
    type nonrec t =
      {
      transitGatewayMeteringPolicyId: TransitGatewayMeteringPolicyId.t
        [@ocaml.doc
          "The ID of the transit gateway metering policy to add the entry to."];
      policyRuleNumber: Integer.t
        [@ocaml.doc
          "The rule number for the metering policy entry. Rules are processed in order from lowest to highest number."];
      sourceTransitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc
          "The ID of the source transit gateway attachment for traffic matching."];
      sourceTransitGatewayAttachmentType:
        TransitGatewayAttachmentResourceType.t option
        [@ocaml.doc
          "The type of the source transit gateway attachment for traffic matching. Note that the tgw-peering resource type has been deprecated. To configure metering policies for Connect, use the transport attachment type."];
      sourceCidrBlock: String_.t option
        [@ocaml.doc "The source CIDR block for traffic matching."];
      sourcePortRange: String_.t option
        [@ocaml.doc "The source port range for traffic matching."];
      destinationTransitGatewayAttachmentId:
        TransitGatewayAttachmentId.t option
        [@ocaml.doc
          "The ID of the destination transit gateway attachment for traffic matching."];
      destinationTransitGatewayAttachmentType:
        TransitGatewayAttachmentResourceType.t option
        [@ocaml.doc
          "The type of the destination transit gateway attachment for traffic matching. Note that the tgw-peering resource type has been deprecated. To configure metering policies for Connect, use the transport attachment type."];
      destinationCidrBlock: String_.t option
        [@ocaml.doc "The destination CIDR block for traffic matching."];
      destinationPortRange: String_.t option
        [@ocaml.doc "The destination port range for traffic matching."];
      protocol: String_.t option
        [@ocaml.doc "The protocol for traffic matching (1, 6, 17, etc.)."];
      meteredAccount: TransitGatewayMeteringPayerType.t
        [@ocaml.doc
          "The Amazon Web Services account ID to which the metered traffic should be attributed."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayMeteringPolicyEntryRequest"
    let make ?sourceTransitGatewayAttachmentId =
      fun ?sourceTransitGatewayAttachmentType ->
        fun ?sourceCidrBlock ->
          fun ?sourcePortRange ->
            fun ?destinationTransitGatewayAttachmentId ->
              fun ?destinationTransitGatewayAttachmentType ->
                fun ?destinationCidrBlock ->
                  fun ?destinationPortRange ->
                    fun ?protocol ->
                      fun ?dryRun ->
                        fun ~transitGatewayMeteringPolicyId ->
                          fun ~policyRuleNumber ->
                            fun ~meteredAccount ->
                              fun () ->
                                {
                                  sourceTransitGatewayAttachmentId;
                                  sourceTransitGatewayAttachmentType;
                                  sourceCidrBlock;
                                  sourcePortRange;
                                  destinationTransitGatewayAttachmentId;
                                  destinationTransitGatewayAttachmentType;
                                  destinationCidrBlock;
                                  destinationPortRange;
                                  protocol;
                                  dryRun;
                                  transitGatewayMeteringPolicyId;
                                  policyRuleNumber;
                                  meteredAccount
                                }
    let to_value x =
      structure_to_value
        [("TransitGatewayMeteringPolicyId",
           (Some
              (TransitGatewayMeteringPolicyId.to_value
                 x.transitGatewayMeteringPolicyId)));
        ("PolicyRuleNumber", (Some (Integer.to_value x.policyRuleNumber)));
        ("SourceTransitGatewayAttachmentId",
          (Option.map x.sourceTransitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("SourceTransitGatewayAttachmentType",
          (Option.map x.sourceTransitGatewayAttachmentType
             ~f:TransitGatewayAttachmentResourceType.to_value));
        ("SourceCidrBlock",
          (Option.map x.sourceCidrBlock ~f:String_.to_value));
        ("SourcePortRange",
          (Option.map x.sourcePortRange ~f:String_.to_value));
        ("DestinationTransitGatewayAttachmentId",
          (Option.map x.destinationTransitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("DestinationTransitGatewayAttachmentType",
          (Option.map x.destinationTransitGatewayAttachmentType
             ~f:TransitGatewayAttachmentResourceType.to_value));
        ("DestinationCidrBlock",
          (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("DestinationPortRange",
          (Option.map x.destinationPortRange ~f:String_.to_value));
        ("Protocol", (Option.map x.protocol ~f:String_.to_value));
        ("MeteredAccount",
          (Some (TransitGatewayMeteringPayerType.to_value x.meteredAccount)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let meteredAccount =
        TransitGatewayMeteringPayerType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MeteredAccount") in
      let protocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Protocol") in
      let destinationPortRange =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationPortRange") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationCidrBlock") in
      let destinationTransitGatewayAttachmentType =
        (Option.map ~f:TransitGatewayAttachmentResourceType.of_xml)
          (Xml.child xml_arg0 "DestinationTransitGatewayAttachmentType") in
      let destinationTransitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "DestinationTransitGatewayAttachmentId") in
      let sourcePortRange =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SourcePortRange") in
      let sourceCidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SourceCidrBlock") in
      let sourceTransitGatewayAttachmentType =
        (Option.map ~f:TransitGatewayAttachmentResourceType.of_xml)
          (Xml.child xml_arg0 "SourceTransitGatewayAttachmentType") in
      let sourceTransitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "SourceTransitGatewayAttachmentId") in
      let policyRuleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyRuleNumber") in
      let transitGatewayMeteringPolicyId =
        TransitGatewayMeteringPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayMeteringPolicyId") in
      make ?dryRun ~meteredAccount ?protocol ?destinationPortRange
        ?destinationCidrBlock ?destinationTransitGatewayAttachmentType
        ?destinationTransitGatewayAttachmentId ?sourcePortRange
        ?sourceCidrBlock ?sourceTransitGatewayAttachmentType
        ?sourceTransitGatewayAttachmentId ~policyRuleNumber
        ~transitGatewayMeteringPolicyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let meteredAccount =
        field_map_exn json__ "MeteredAccount"
          TransitGatewayMeteringPayerType.of_json in
      let protocol = field_map json__ "Protocol" String_.of_json in
      let destinationPortRange =
        field_map json__ "DestinationPortRange" String_.of_json in
      let destinationCidrBlock =
        field_map json__ "DestinationCidrBlock" String_.of_json in
      let destinationTransitGatewayAttachmentType =
        field_map json__ "DestinationTransitGatewayAttachmentType"
          TransitGatewayAttachmentResourceType.of_json in
      let destinationTransitGatewayAttachmentId =
        field_map json__ "DestinationTransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let sourcePortRange =
        field_map json__ "SourcePortRange" String_.of_json in
      let sourceCidrBlock =
        field_map json__ "SourceCidrBlock" String_.of_json in
      let sourceTransitGatewayAttachmentType =
        field_map json__ "SourceTransitGatewayAttachmentType"
          TransitGatewayAttachmentResourceType.of_json in
      let sourceTransitGatewayAttachmentId =
        field_map json__ "SourceTransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let policyRuleNumber =
        field_map_exn json__ "PolicyRuleNumber" Integer.of_json in
      let transitGatewayMeteringPolicyId =
        field_map_exn json__ "TransitGatewayMeteringPolicyId"
          TransitGatewayMeteringPolicyId.of_json in
      make ?dryRun ~meteredAccount ?protocol ?destinationPortRange
        ?destinationCidrBlock ?destinationTransitGatewayAttachmentType
        ?destinationTransitGatewayAttachmentId ?sourcePortRange
        ?sourceCidrBlock ?sourceTransitGatewayAttachmentType
        ?sourceTransitGatewayAttachmentId ~policyRuleNumber
        ~transitGatewayMeteringPolicyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an entry in a transit gateway metering policy to define traffic measurement rules."]
module CreateTransitGatewayConnectResult =
  struct
    type nonrec t =
      {
      transitGatewayConnect: TransitGatewayConnect.t option
        [@ocaml.doc "Information about the Connect attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnect = fun () -> { transitGatewayConnect }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnect",
           (Option.map x.transitGatewayConnect
              ~f:TransitGatewayConnect.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnect =
        (Option.map ~f:TransitGatewayConnect.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnect") in
      make ?transitGatewayConnect ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayConnect =
        field_map json__ "TransitGatewayConnect"
          TransitGatewayConnect.of_json in
      make ?transitGatewayConnect ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Connect attachment from a specified transit gateway attachment. A Connect attachment is a GRE-based tunnel attachment that you can use to establish a connection between a transit gateway and an appliance. A Connect attachment uses an existing VPC or Amazon Web Services Direct Connect attachment as the underlying transport mechanism."]
module CreateTransitGatewayConnectRequest =
  struct
    type nonrec t =
      {
      transportTransitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc
          "The ID of the transit gateway attachment. You can specify a VPC attachment or Amazon Web Services Direct Connect attachment."];
      options: CreateTransitGatewayConnectRequestOptions.t
        [@ocaml.doc "The Connect attachment options."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the Connect attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayConnectRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~transportTransitGatewayAttachmentId ->
          fun ~options ->
            fun () ->
              {
                tagSpecifications;
                dryRun;
                transportTransitGatewayAttachmentId;
                options
              }
    let to_value x =
      structure_to_value
        [("TransportTransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transportTransitGatewayAttachmentId)));
        ("Options",
          (Some
             (CreateTransitGatewayConnectRequestOptions.to_value x.options)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let options =
        CreateTransitGatewayConnectRequestOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Options") in
      let transportTransitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransportTransitGatewayAttachmentId") in
      make ?dryRun ?tagSpecifications ~options
        ~transportTransitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let options =
        field_map_exn json__ "Options"
          CreateTransitGatewayConnectRequestOptions.of_json in
      let transportTransitGatewayAttachmentId =
        field_map_exn json__ "TransportTransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ?tagSpecifications ~options
        ~transportTransitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Connect attachment from a specified transit gateway attachment. A Connect attachment is a GRE-based tunnel attachment that you can use to establish a connection between a transit gateway and an appliance. A Connect attachment uses an existing VPC or Amazon Web Services Direct Connect attachment as the underlying transport mechanism."]
module CreateTransitGatewayConnectPeerResult =
  struct
    type nonrec t =
      {
      transitGatewayConnectPeer: TransitGatewayConnectPeer.t option
        [@ocaml.doc "Information about the Connect peer."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayConnectPeer =
      fun () -> { transitGatewayConnectPeer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayConnectPeer",
           (Option.map x.transitGatewayConnectPeer
              ~f:TransitGatewayConnectPeer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConnectPeer =
        (Option.map ~f:TransitGatewayConnectPeer.of_xml)
          (Xml.child xml_arg0 "transitGatewayConnectPeer") in
      make ?transitGatewayConnectPeer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayConnectPeer =
        field_map json__ "TransitGatewayConnectPeer"
          TransitGatewayConnectPeer.of_json in
      make ?transitGatewayConnectPeer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Connect peer for a specified transit gateway Connect attachment between a transit gateway and an appliance. The peer address and transit gateway address must be the same IP address family (IPv4 or IPv6). For more information, see Connect peers in the Amazon Web Services Transit Gateways Guide."]
module CreateTransitGatewayConnectPeerRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the Connect attachment."];
      transitGatewayAddress: String_.t option
        [@ocaml.doc
          "The peer IP address (GRE outer IP address) on the transit gateway side of the Connect peer, which must be specified from a transit gateway CIDR block. If not specified, Amazon automatically assigns the first available IP address from the transit gateway CIDR block."];
      peerAddress: String_.t
        [@ocaml.doc
          "The peer IP address (GRE outer IP address) on the appliance side of the Connect peer."];
      bgpOptions: TransitGatewayConnectRequestBgpOptions.t option
        [@ocaml.doc "The BGP options for the Connect peer."];
      insideCidrBlocks: InsideCidrBlocksStringList.t
        [@ocaml.doc
          "The range of inside IP addresses that are used for BGP peering. You must specify a size /29 IPv4 CIDR block from the 169.254.0.0/16 range. The first address from the range must be configured on the appliance as the BGP IP address. You can also optionally specify a size /125 IPv6 CIDR block from the fd00::/8 range."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the Connect peer."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateTransitGatewayConnectPeerRequest"
    let make ?transitGatewayAddress =
      fun ?bgpOptions ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ~transitGatewayAttachmentId ->
              fun ~peerAddress ->
                fun ~insideCidrBlocks ->
                  fun () ->
                    {
                      transitGatewayAddress;
                      bgpOptions;
                      tagSpecifications;
                      dryRun;
                      transitGatewayAttachmentId;
                      peerAddress;
                      insideCidrBlocks
                    }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("TransitGatewayAddress",
          (Option.map x.transitGatewayAddress ~f:String_.to_value));
        ("PeerAddress", (Some (String_.to_value x.peerAddress)));
        ("BgpOptions",
          (Option.map x.bgpOptions
             ~f:TransitGatewayConnectRequestBgpOptions.to_value));
        ("InsideCidrBlocks",
          (Some (InsideCidrBlocksStringList.to_value x.insideCidrBlocks)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let insideCidrBlocks =
        InsideCidrBlocksStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InsideCidrBlocks") in
      let bgpOptions =
        (Option.map ~f:TransitGatewayConnectRequestBgpOptions.of_xml)
          (Xml.child xml_arg0 "BgpOptions") in
      let peerAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerAddress") in
      let transitGatewayAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAddress") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ?tagSpecifications ~insideCidrBlocks ?bgpOptions
        ~peerAddress ?transitGatewayAddress ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let insideCidrBlocks =
        field_map_exn json__ "InsideCidrBlocks"
          InsideCidrBlocksStringList.of_json in
      let bgpOptions =
        field_map json__ "BgpOptions"
          TransitGatewayConnectRequestBgpOptions.of_json in
      let peerAddress = field_map_exn json__ "PeerAddress" String_.of_json in
      let transitGatewayAddress =
        field_map json__ "TransitGatewayAddress" String_.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ?tagSpecifications ~insideCidrBlocks ?bgpOptions
        ~peerAddress ?transitGatewayAddress ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Connect peer for a specified transit gateway Connect attachment between a transit gateway and an appliance. The peer address and transit gateway address must be the same IP address family (IPv4 or IPv6). For more information, see Connect peers in the Amazon Web Services Transit Gateways Guide."]
module CreateTrafficMirrorTargetResult =
  struct
    type nonrec t =
      {
      trafficMirrorTarget: TrafficMirrorTarget.t option
        [@ocaml.doc "Information about the Traffic Mirror target."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorTarget =
      fun ?clientToken -> fun () -> { trafficMirrorTarget; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorTarget",
           (Option.map x.trafficMirrorTarget ~f:TrafficMirrorTarget.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorTarget =
        (Option.map ~f:TrafficMirrorTarget.of_xml)
          (Xml.child xml_arg0 "trafficMirrorTarget") in
      make ?clientToken ?trafficMirrorTarget ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let trafficMirrorTarget =
        field_map json__ "TrafficMirrorTarget" TrafficMirrorTarget.of_json in
      make ?clientToken ?trafficMirrorTarget ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a target for your Traffic Mirror session. A Traffic Mirror target is the destination for mirrored traffic. The Traffic Mirror source and the Traffic Mirror target (monitoring appliances) can be in the same VPC, or in different VPCs connected via VPC peering or a transit gateway. A Traffic Mirror target can be a network interface, a Network Load Balancer, or a Gateway Load Balancer endpoint. To use the target in a Traffic Mirror session, use CreateTrafficMirrorSession."]
module CreateTrafficMirrorTargetRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t option
        [@ocaml.doc
          "The network interface ID that is associated with the target."];
      networkLoadBalancerArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Network Load Balancer that is associated with the target."];
      description: String_.t option
        [@ocaml.doc "The description of the Traffic Mirror target."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the Traffic Mirror target."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      gatewayLoadBalancerEndpointId: VpcEndpointId.t option
        [@ocaml.doc "The ID of the Gateway Load Balancer endpoint."]}
    let make ?networkInterfaceId =
      fun ?networkLoadBalancerArn ->
        fun ?description ->
          fun ?tagSpecifications ->
            fun ?dryRun ->
              fun ?clientToken ->
                fun ?gatewayLoadBalancerEndpointId ->
                  fun () ->
                    {
                      networkInterfaceId;
                      networkLoadBalancerArn;
                      description;
                      tagSpecifications;
                      dryRun;
                      clientToken;
                      gatewayLoadBalancerEndpointId
                    }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("NetworkLoadBalancerArn",
          (Option.map x.networkLoadBalancerArn ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("GatewayLoadBalancerEndpointId",
          (Option.map x.gatewayLoadBalancerEndpointId
             ~f:VpcEndpointId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gatewayLoadBalancerEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "GatewayLoadBalancerEndpointId") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let networkLoadBalancerArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkLoadBalancerArn") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceId") in
      make ?gatewayLoadBalancerEndpointId ?clientToken ?dryRun
        ?tagSpecifications ?description ?networkLoadBalancerArn
        ?networkInterfaceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gatewayLoadBalancerEndpointId =
        field_map json__ "GatewayLoadBalancerEndpointId"
          VpcEndpointId.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let networkLoadBalancerArn =
        field_map json__ "NetworkLoadBalancerArn" String_.of_json in
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?gatewayLoadBalancerEndpointId ?clientToken ?dryRun
        ?tagSpecifications ?description ?networkLoadBalancerArn
        ?networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a target for your Traffic Mirror session. A Traffic Mirror target is the destination for mirrored traffic. The Traffic Mirror source and the Traffic Mirror target (monitoring appliances) can be in the same VPC, or in different VPCs connected via VPC peering or a transit gateway. A Traffic Mirror target can be a network interface, a Network Load Balancer, or a Gateway Load Balancer endpoint. To use the target in a Traffic Mirror session, use CreateTrafficMirrorSession."]
module CreateTrafficMirrorSessionResult =
  struct
    type nonrec t =
      {
      trafficMirrorSession: TrafficMirrorSession.t option
        [@ocaml.doc "Information about the Traffic Mirror session."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorSession =
      fun ?clientToken -> fun () -> { trafficMirrorSession; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorSession",
           (Option.map x.trafficMirrorSession
              ~f:TrafficMirrorSession.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorSession =
        (Option.map ~f:TrafficMirrorSession.of_xml)
          (Xml.child xml_arg0 "trafficMirrorSession") in
      make ?clientToken ?trafficMirrorSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let trafficMirrorSession =
        field_map json__ "TrafficMirrorSession" TrafficMirrorSession.of_json in
      make ?clientToken ?trafficMirrorSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Traffic Mirror session. A Traffic Mirror session actively copies packets from a Traffic Mirror source to a Traffic Mirror target. Create a filter, and then assign it to the session to define a subset of the traffic to mirror, for example all TCP traffic. The Traffic Mirror source and the Traffic Mirror target (monitoring appliances) can be in the same VPC, or in a different VPC connected via VPC peering or a transit gateway. By default, no traffic is mirrored. Use CreateTrafficMirrorFilter to create filter rules that specify the traffic to mirror."]
module CreateTrafficMirrorSessionRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the source network interface."];
      trafficMirrorTargetId: TrafficMirrorTargetId.t
        [@ocaml.doc "The ID of the Traffic Mirror target."];
      trafficMirrorFilterId: TrafficMirrorFilterId.t
        [@ocaml.doc "The ID of the Traffic Mirror filter."];
      packetLength: Integer.t option
        [@ocaml.doc
          "The number of bytes in each packet to mirror. These are bytes after the VXLAN header. Do not specify this parameter when you want to mirror the entire packet. To mirror a subset of the packet, set this to the length (in bytes) that you want to mirror. For example, if you set this value to 100, then the first 100 bytes that meet the filter criteria are copied to the target. If you do not want to mirror the entire packet, use the PacketLength parameter to specify the number of bytes in each packet to mirror. For sessions with Network Load Balancer (NLB) Traffic Mirror targets the default PacketLength will be set to 8500. Valid values are 1-8500. Setting a PacketLength greater than 8500 will result in an error response."];
      sessionNumber: Integer.t
        [@ocaml.doc
          "The session number determines the order in which sessions are evaluated when an interface is used by multiple sessions. The first session with a matching filter is the one that mirrors the packets. Valid values are 1-32766."];
      virtualNetworkId: Integer.t option
        [@ocaml.doc
          "The VXLAN ID for the Traffic Mirror session. For more information about the VXLAN protocol, see RFC 7348. If you do not specify a VirtualNetworkId, an account-wide unique ID is chosen at random."];
      description: String_.t option
        [@ocaml.doc "The description of the Traffic Mirror session."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to a Traffic Mirror session."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    let context_ = "CreateTrafficMirrorSessionRequest"
    let make ?packetLength =
      fun ?virtualNetworkId ->
        fun ?description ->
          fun ?tagSpecifications ->
            fun ?dryRun ->
              fun ?clientToken ->
                fun ~networkInterfaceId ->
                  fun ~trafficMirrorTargetId ->
                    fun ~trafficMirrorFilterId ->
                      fun ~sessionNumber ->
                        fun () ->
                          {
                            packetLength;
                            virtualNetworkId;
                            description;
                            tagSpecifications;
                            dryRun;
                            clientToken;
                            networkInterfaceId;
                            trafficMirrorTargetId;
                            trafficMirrorFilterId;
                            sessionNumber
                          }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("TrafficMirrorTargetId",
          (Some (TrafficMirrorTargetId.to_value x.trafficMirrorTargetId)));
        ("TrafficMirrorFilterId",
          (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("PacketLength", (Option.map x.packetLength ~f:Integer.to_value));
        ("SessionNumber", (Some (Integer.to_value x.sessionNumber)));
        ("VirtualNetworkId",
          (Option.map x.virtualNetworkId ~f:Integer.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let virtualNetworkId =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "VirtualNetworkId") in
      let sessionNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SessionNumber") in
      let packetLength =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PacketLength") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      let trafficMirrorTargetId =
        TrafficMirrorTargetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorTargetId") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInterfaceId") in
      make ?clientToken ?dryRun ?tagSpecifications ?description
        ?virtualNetworkId ~sessionNumber ?packetLength ~trafficMirrorFilterId
        ~trafficMirrorTargetId ~networkInterfaceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let virtualNetworkId =
        field_map json__ "VirtualNetworkId" Integer.of_json in
      let sessionNumber =
        field_map_exn json__ "SessionNumber" Integer.of_json in
      let packetLength = field_map json__ "PacketLength" Integer.of_json in
      let trafficMirrorFilterId =
        field_map_exn json__ "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      let trafficMirrorTargetId =
        field_map_exn json__ "TrafficMirrorTargetId"
          TrafficMirrorTargetId.of_json in
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ?description
        ?virtualNetworkId ~sessionNumber ?packetLength ~trafficMirrorFilterId
        ~trafficMirrorTargetId ~networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Traffic Mirror session. A Traffic Mirror session actively copies packets from a Traffic Mirror source to a Traffic Mirror target. Create a filter, and then assign it to the session to define a subset of the traffic to mirror, for example all TCP traffic. The Traffic Mirror source and the Traffic Mirror target (monitoring appliances) can be in the same VPC, or in a different VPC connected via VPC peering or a transit gateway. By default, no traffic is mirrored. Use CreateTrafficMirrorFilter to create filter rules that specify the traffic to mirror."]
module CreateTrafficMirrorFilterRuleResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilterRule: TrafficMirrorFilterRule.t option
        [@ocaml.doc "The Traffic Mirror rule."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilterRule =
      fun ?clientToken -> fun () -> { trafficMirrorFilterRule; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilterRule",
           (Option.map x.trafficMirrorFilterRule
              ~f:TrafficMirrorFilterRule.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorFilterRule =
        (Option.map ~f:TrafficMirrorFilterRule.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilterRule") in
      make ?clientToken ?trafficMirrorFilterRule ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let trafficMirrorFilterRule =
        field_map json__ "TrafficMirrorFilterRule"
          TrafficMirrorFilterRule.of_json in
      make ?clientToken ?trafficMirrorFilterRule ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Traffic Mirror filter rule. A Traffic Mirror rule defines the Traffic Mirror source traffic to mirror. You need the Traffic Mirror filter ID when you create the rule."]
module CreateTrafficMirrorFilterRuleRequest =
  struct
    type nonrec t =
      {
      trafficMirrorFilterId: TrafficMirrorFilterId.t
        [@ocaml.doc
          "The ID of the filter that this rule is associated with."];
      trafficDirection: TrafficDirection.t
        [@ocaml.doc "The type of traffic."];
      ruleNumber: Integer.t
        [@ocaml.doc
          "The number of the Traffic Mirror rule. This number must be unique for each Traffic Mirror rule in a given direction. The rules are processed in ascending order by rule number."];
      ruleAction: TrafficMirrorRuleAction.t
        [@ocaml.doc "The action to take on the filtered traffic."];
      destinationPortRange: TrafficMirrorPortRangeRequest.t option
        [@ocaml.doc "The destination port range."];
      sourcePortRange: TrafficMirrorPortRangeRequest.t option
        [@ocaml.doc "The source port range."];
      protocol: Integer.t option
        [@ocaml.doc
          "The protocol, for example UDP, to assign to the Traffic Mirror rule. For information about the protocol value, see Protocol Numbers on the Internet Assigned Numbers Authority (IANA) website."];
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The destination CIDR block to assign to the Traffic Mirror rule."];
      sourceCidrBlock: String_.t
        [@ocaml.doc
          "The source CIDR block to assign to the Traffic Mirror rule."];
      description: String_.t option
        [@ocaml.doc "The description of the Traffic Mirror rule."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "Traffic Mirroring tags specifications."]}
    let context_ = "CreateTrafficMirrorFilterRuleRequest"
    let make ?destinationPortRange =
      fun ?sourcePortRange ->
        fun ?protocol ->
          fun ?description ->
            fun ?dryRun ->
              fun ?clientToken ->
                fun ?tagSpecifications ->
                  fun ~trafficMirrorFilterId ->
                    fun ~trafficDirection ->
                      fun ~ruleNumber ->
                        fun ~ruleAction ->
                          fun ~destinationCidrBlock ->
                            fun ~sourceCidrBlock ->
                              fun () ->
                                {
                                  destinationPortRange;
                                  sourcePortRange;
                                  protocol;
                                  description;
                                  dryRun;
                                  clientToken;
                                  tagSpecifications;
                                  trafficMirrorFilterId;
                                  trafficDirection;
                                  ruleNumber;
                                  ruleAction;
                                  destinationCidrBlock;
                                  sourceCidrBlock
                                }
    let to_value x =
      structure_to_value
        [("TrafficMirrorFilterId",
           (Some (TrafficMirrorFilterId.to_value x.trafficMirrorFilterId)));
        ("TrafficDirection",
          (Some (TrafficDirection.to_value x.trafficDirection)));
        ("RuleNumber", (Some (Integer.to_value x.ruleNumber)));
        ("RuleAction",
          (Some (TrafficMirrorRuleAction.to_value x.ruleAction)));
        ("DestinationPortRange",
          (Option.map x.destinationPortRange
             ~f:TrafficMirrorPortRangeRequest.to_value));
        ("SourcePortRange",
          (Option.map x.sourcePortRange
             ~f:TrafficMirrorPortRangeRequest.to_value));
        ("Protocol", (Option.map x.protocol ~f:Integer.to_value));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("SourceCidrBlock", (Some (String_.to_value x.sourceCidrBlock)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceCidrBlock") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let protocol =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Protocol") in
      let sourcePortRange =
        (Option.map ~f:TrafficMirrorPortRangeRequest.of_xml)
          (Xml.child xml_arg0 "SourcePortRange") in
      let destinationPortRange =
        (Option.map ~f:TrafficMirrorPortRangeRequest.of_xml)
          (Xml.child xml_arg0 "DestinationPortRange") in
      let ruleAction =
        TrafficMirrorRuleAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleAction") in
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleNumber") in
      let trafficDirection =
        TrafficDirection.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficDirection") in
      let trafficMirrorFilterId =
        TrafficMirrorFilterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficMirrorFilterId") in
      make ?tagSpecifications ?clientToken ?dryRun ?description
        ~sourceCidrBlock ~destinationCidrBlock ?protocol ?sourcePortRange
        ?destinationPortRange ~ruleAction ~ruleNumber ~trafficDirection
        ~trafficMirrorFilterId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let sourceCidrBlock =
        field_map_exn json__ "SourceCidrBlock" String_.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      let protocol = field_map json__ "Protocol" Integer.of_json in
      let sourcePortRange =
        field_map json__ "SourcePortRange"
          TrafficMirrorPortRangeRequest.of_json in
      let destinationPortRange =
        field_map json__ "DestinationPortRange"
          TrafficMirrorPortRangeRequest.of_json in
      let ruleAction =
        field_map_exn json__ "RuleAction" TrafficMirrorRuleAction.of_json in
      let ruleNumber = field_map_exn json__ "RuleNumber" Integer.of_json in
      let trafficDirection =
        field_map_exn json__ "TrafficDirection" TrafficDirection.of_json in
      let trafficMirrorFilterId =
        field_map_exn json__ "TrafficMirrorFilterId"
          TrafficMirrorFilterId.of_json in
      make ?tagSpecifications ?clientToken ?dryRun ?description
        ~sourceCidrBlock ~destinationCidrBlock ?protocol ?sourcePortRange
        ?destinationPortRange ~ruleAction ~ruleNumber ~trafficDirection
        ~trafficMirrorFilterId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Traffic Mirror filter rule. A Traffic Mirror rule defines the Traffic Mirror source traffic to mirror. You need the Traffic Mirror filter ID when you create the rule."]
module CreateTrafficMirrorFilterResult =
  struct
    type nonrec t =
      {
      trafficMirrorFilter: TrafficMirrorFilter.t option
        [@ocaml.doc "Information about the Traffic Mirror filter."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?trafficMirrorFilter =
      fun ?clientToken -> fun () -> { trafficMirrorFilter; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trafficMirrorFilter",
           (Option.map x.trafficMirrorFilter ~f:TrafficMirrorFilter.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let trafficMirrorFilter =
        (Option.map ~f:TrafficMirrorFilter.of_xml)
          (Xml.child xml_arg0 "trafficMirrorFilter") in
      make ?clientToken ?trafficMirrorFilter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let trafficMirrorFilter =
        field_map json__ "TrafficMirrorFilter" TrafficMirrorFilter.of_json in
      make ?clientToken ?trafficMirrorFilter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Traffic Mirror filter. A Traffic Mirror filter is a set of rules that defines the traffic to mirror. By default, no traffic is mirrored. To mirror traffic, use CreateTrafficMirrorFilterRule to add Traffic Mirror rules to the filter. The rules you add define what traffic gets mirrored. You can also use ModifyTrafficMirrorFilterNetworkServices to mirror supported network services."]
module CreateTrafficMirrorFilterRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "The description of the Traffic Mirror filter."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to a Traffic Mirror filter."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    let make ?description =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ?clientToken ->
            fun () -> { description; tagSpecifications; dryRun; clientToken }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?clientToken ?dryRun ?tagSpecifications ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json__ "Description" String_.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Traffic Mirror filter. A Traffic Mirror filter is a set of rules that defines the traffic to mirror. By default, no traffic is mirrored. To mirror traffic, use CreateTrafficMirrorFilterRule to add Traffic Mirror rules to the filter. The rules you add define what traffic gets mirrored. You can also use ModifyTrafficMirrorFilterNetworkServices to mirror supported network services."]
module CreateTagsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      resources: ResourceIdList.t
        [@ocaml.doc
          "The IDs of the resources, separated by spaces. Constraints: Up to 1000 resource IDs. We recommend breaking up this request into smaller batches."];
      tags: TagList.t
        [@ocaml.doc
          "The tags. The value parameter is required, but if you don't want the tag to have a value, specify the parameter with no value, and we set the value to an empty string."]}
    let context_ = "CreateTagsRequest"
    let make ?dryRun =
      fun ~resources -> fun ~tags -> fun () -> { dryRun; resources; tags }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ResourceId", (Some (ResourceIdList.to_value x.resources)));
        ("Tag", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tag") in
      let resources =
        ResourceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~tags ~resources ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" TagList.of_json in
      let resources = field_map_exn json__ "Resources" ResourceIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~tags ~resources ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds or overwrites only the specified tags for the specified Amazon EC2 resource or resources. When you specify an existing tag key, the value is overwritten with the new value. Each resource can have a maximum of 50 tags. Each tag consists of a key and optional value. Tag keys must be unique per resource. For more information about tags, see Tag your Amazon EC2 resources in the Amazon Elastic Compute Cloud User Guide. For more information about creating IAM policies that control users' access to resources based on tags, see Supported resource-level permissions for Amazon EC2 API actions in the Amazon Elastic Compute Cloud User Guide."]
module CreateSubnetResult =
  struct
    type nonrec t =
      {
      subnet: Subnet.t option [@ocaml.doc "Information about the subnet."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnet = fun () -> { subnet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnet", (Option.map x.subnet ~f:Subnet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnet =
        (Option.map ~f:Subnet.of_xml) (Xml.child xml_arg0 "subnet") in
      make ?subnet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnet = field_map json__ "Subnet" Subnet.of_json in
      make ?subnet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a subnet in the specified VPC. For an IPv4 only subnet, specify an IPv4 CIDR block. If the VPC has an IPv6 CIDR block, you can create an IPv6 only subnet or a dual stack subnet instead. For an IPv6 only subnet, specify an IPv6 CIDR block. For a dual stack subnet, specify both an IPv4 CIDR block and an IPv6 CIDR block. A subnet CIDR block must not overlap the CIDR block of an existing subnet in the VPC. After you create a subnet, you can't change its CIDR block. The allowed size for an IPv4 subnet is between a /28 netmask (16 IP addresses) and a /16 netmask (65,536 IP addresses). Amazon Web Services reserves both the first four and the last IPv4 address in each subnet's CIDR block. They're not available for your use. If you've associated an IPv6 CIDR block with your VPC, you can associate an IPv6 CIDR block with a subnet when you create it. If you add more than one subnet to a VPC, they're set up in a star topology with a logical router in the middle. When you stop an instance in a subnet, it retains its private IPv4 address. It's therefore possible to have a subnet with no running instances (they're all stopped), but no remaining IP addresses available. For more information, see Subnets in the Amazon VPC User Guide."]
module CreateSubnetRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the subnet."];
      availabilityZone: String_.t option
        [@ocaml.doc
          "The Availability Zone or Local Zone for the subnet. Default: Amazon Web Services selects one for you. If you create more than one subnet in your VPC, we do not necessarily select a different zone for each subnet. To create a subnet in a Local Zone, set this value to the Local Zone ID, for example us-west-2-lax-1a. For information about the Regions that support Local Zones, see Available Local Zones. To create a subnet in an Outpost, set this value to the Availability Zone for the Outpost and specify the Outpost ARN."];
      availabilityZoneId: String_.t option
        [@ocaml.doc "The AZ ID or the Local Zone ID of the subnet."];
      cidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 network range for the subnet, in CIDR notation. For example, 10.0.0.0/24. We modify the specified CIDR block to its canonical form; for example, if you specify 100.68.0.18/18, we modify it to 100.68.0.0/18. This parameter is not supported for an IPv6 only subnet."];
      ipv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 network range for the subnet, in CIDR notation. This parameter is required for an IPv6 only subnet."];
      outpostArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Outpost. If you specify an Outpost ARN, you must also specify the Availability Zone of the Outpost subnet."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      ipv6Native: Boolean.t option
        [@ocaml.doc "Indicates whether to create an IPv6 only subnet."];
      ipv4IpamPoolId: IpamPoolId.t option
        [@ocaml.doc "An IPv4 IPAM pool ID for the subnet."];
      ipv4NetmaskLength: NetmaskLength.t option
        [@ocaml.doc "An IPv4 netmask length for the subnet."];
      ipv6IpamPoolId: IpamPoolId.t option
        [@ocaml.doc "An IPv6 IPAM pool ID for the subnet."];
      ipv6NetmaskLength: NetmaskLength.t option
        [@ocaml.doc "An IPv6 netmask length for the subnet."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateSubnetRequest"
    let make ?tagSpecifications =
      fun ?availabilityZone ->
        fun ?availabilityZoneId ->
          fun ?cidrBlock ->
            fun ?ipv6CidrBlock ->
              fun ?outpostArn ->
                fun ?ipv6Native ->
                  fun ?ipv4IpamPoolId ->
                    fun ?ipv4NetmaskLength ->
                      fun ?ipv6IpamPoolId ->
                        fun ?ipv6NetmaskLength ->
                          fun ?dryRun ->
                            fun ~vpcId ->
                              fun () ->
                                {
                                  tagSpecifications;
                                  availabilityZone;
                                  availabilityZoneId;
                                  cidrBlock;
                                  ipv6CidrBlock;
                                  outpostArn;
                                  ipv6Native;
                                  ipv4IpamPoolId;
                                  ipv4NetmaskLength;
                                  ipv6IpamPoolId;
                                  ipv6NetmaskLength;
                                  dryRun;
                                  vpcId
                                }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:String_.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:String_.to_value));
        ("CidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("Ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("Ipv6Native", (Option.map x.ipv6Native ~f:Boolean.to_value));
        ("Ipv4IpamPoolId",
          (Option.map x.ipv4IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv4NetmaskLength",
          (Option.map x.ipv4NetmaskLength ~f:NetmaskLength.to_value));
        ("Ipv6IpamPoolId",
          (Option.map x.ipv6IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv6NetmaskLength",
          (Option.map x.ipv6NetmaskLength ~f:NetmaskLength.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let ipv6NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv6NetmaskLength") in
      let ipv6IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv6IpamPoolId") in
      let ipv4NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv4NetmaskLength") in
      let ipv4IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv4IpamPoolId") in
      let ipv6Native =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Ipv6Native") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ipv6CidrBlock") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      let availabilityZoneId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?dryRun ?ipv6NetmaskLength ?ipv6IpamPoolId ?ipv4NetmaskLength
        ?ipv4IpamPoolId ?ipv6Native ~vpcId ?outpostArn ?ipv6CidrBlock
        ?cidrBlock ?availabilityZoneId ?availabilityZone ?tagSpecifications
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let ipv6NetmaskLength =
        field_map json__ "Ipv6NetmaskLength" NetmaskLength.of_json in
      let ipv6IpamPoolId =
        field_map json__ "Ipv6IpamPoolId" IpamPoolId.of_json in
      let ipv4NetmaskLength =
        field_map json__ "Ipv4NetmaskLength" NetmaskLength.of_json in
      let ipv4IpamPoolId =
        field_map json__ "Ipv4IpamPoolId" IpamPoolId.of_json in
      let ipv6Native = field_map json__ "Ipv6Native" Boolean.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let outpostArn = field_map json__ "OutpostArn" String_.of_json in
      let ipv6CidrBlock = field_map json__ "Ipv6CidrBlock" String_.of_json in
      let cidrBlock = field_map json__ "CidrBlock" String_.of_json in
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" String_.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ?dryRun ?ipv6NetmaskLength ?ipv6IpamPoolId ?ipv4NetmaskLength
        ?ipv4IpamPoolId ?ipv6Native ~vpcId ?outpostArn ?ipv6CidrBlock
        ?cidrBlock ?availabilityZoneId ?availabilityZone ?tagSpecifications
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a subnet in the specified VPC. For an IPv4 only subnet, specify an IPv4 CIDR block. If the VPC has an IPv6 CIDR block, you can create an IPv6 only subnet or a dual stack subnet instead. For an IPv6 only subnet, specify an IPv6 CIDR block. For a dual stack subnet, specify both an IPv4 CIDR block and an IPv6 CIDR block. A subnet CIDR block must not overlap the CIDR block of an existing subnet in the VPC. After you create a subnet, you can't change its CIDR block. The allowed size for an IPv4 subnet is between a /28 netmask (16 IP addresses) and a /16 netmask (65,536 IP addresses). Amazon Web Services reserves both the first four and the last IPv4 address in each subnet's CIDR block. They're not available for your use. If you've associated an IPv6 CIDR block with your VPC, you can associate an IPv6 CIDR block with a subnet when you create it. If you add more than one subnet to a VPC, they're set up in a star topology with a logical router in the middle. When you stop an instance in a subnet, it retains its private IPv4 address. It's therefore possible to have a subnet with no running instances (they're all stopped), but no remaining IP addresses available. For more information, see Subnets in the Amazon VPC User Guide."]
module CreateSubnetCidrReservationResult =
  struct
    type nonrec t =
      {
      subnetCidrReservation: SubnetCidrReservation.t option
        [@ocaml.doc "Information about the created subnet CIDR reservation."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnetCidrReservation = fun () -> { subnetCidrReservation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnetCidrReservation",
           (Option.map x.subnetCidrReservation
              ~f:SubnetCidrReservation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnetCidrReservation =
        (Option.map ~f:SubnetCidrReservation.of_xml)
          (Xml.child xml_arg0 "subnetCidrReservation") in
      make ?subnetCidrReservation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnetCidrReservation =
        field_map json__ "SubnetCidrReservation"
          SubnetCidrReservation.of_json in
      make ?subnetCidrReservation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a subnet CIDR reservation. For more information, see Subnet CIDR reservations in the Amazon VPC User Guide and Manage prefixes for your network interfaces in the Amazon EC2 User Guide."]
module CreateSubnetCidrReservationRequest =
  struct
    type nonrec t =
      {
      subnetId: SubnetId.t [@ocaml.doc "The ID of the subnet."];
      cidr: String_.t [@ocaml.doc "The IPv4 or IPV6 CIDR range to reserve."];
      reservationType: SubnetCidrReservationType.t
        [@ocaml.doc
          "The type of reservation. The reservation type determines how the reserved IP addresses are assigned to resources. prefix - Amazon Web Services assigns the reserved IP addresses to network interfaces. explicit - You assign the reserved IP addresses to network interfaces."];
      description: String_.t option
        [@ocaml.doc
          "The description to assign to the subnet CIDR reservation."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the subnet CIDR reservation."]}
    let context_ = "CreateSubnetCidrReservationRequest"
    let make ?description =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~subnetId ->
            fun ~cidr ->
              fun ~reservationType ->
                fun () ->
                  {
                    description;
                    dryRun;
                    tagSpecifications;
                    subnetId;
                    cidr;
                    reservationType
                  }
    let to_value x =
      structure_to_value
        [("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("Cidr", (Some (String_.to_value x.cidr)));
        ("ReservationType",
          (Some (SubnetCidrReservationType.to_value x.reservationType)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let reservationType =
        SubnetCidrReservationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservationType") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      make ?tagSpecifications ?dryRun ?description ~reservationType ~cidr
        ~subnetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let reservationType =
        field_map_exn json__ "ReservationType"
          SubnetCidrReservationType.of_json in
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      let subnetId = field_map_exn json__ "SubnetId" SubnetId.of_json in
      make ?tagSpecifications ?dryRun ?description ~reservationType ~cidr
        ~subnetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a subnet CIDR reservation. For more information, see Subnet CIDR reservations in the Amazon VPC User Guide and Manage prefixes for your network interfaces in the Amazon EC2 User Guide."]
module CreateStoreImageTaskResult =
  struct
    type nonrec t =
      {
      objectKey: String_.t option
        [@ocaml.doc "The name of the stored AMI object in the S3 bucket."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?objectKey = fun () -> { objectKey }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("objectKey", (Option.map x.objectKey ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectKey =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "objectKey") in
      make ?objectKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let objectKey = field_map json__ "ObjectKey" String_.of_json in
      make ?objectKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stores an AMI as a single object in an Amazon S3 bucket. To use this API, you must have the required permissions. For more information, see Permissions for storing and restoring AMIs using S3 in the Amazon EC2 User Guide. For more information, see Store and restore an AMI using S3 in the Amazon EC2 User Guide."]
module CreateStoreImageTaskRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t [@ocaml.doc "The ID of the AMI."];
      bucket: String_.t
        [@ocaml.doc
          "The name of the Amazon S3 bucket in which the AMI object will be stored. The bucket must be in the Region in which the request is being made. The AMI object appears in the bucket only after the upload task has completed."];
      s3ObjectTags: S3ObjectTagList.t option
        [@ocaml.doc
          "The tags to apply to the AMI object that will be stored in the Amazon S3 bucket."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateStoreImageTaskRequest"
    let make ?s3ObjectTags =
      fun ?dryRun ->
        fun ~imageId ->
          fun ~bucket -> fun () -> { s3ObjectTags; dryRun; imageId; bucket }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("Bucket", (Some (String_.to_value x.bucket)));
        ("S3ObjectTag",
          (Option.map x.s3ObjectTags ~f:S3ObjectTagList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let s3ObjectTags =
        (Option.map ~f:S3ObjectTagList.of_xml)
          (Xml.child xml_arg0 "S3ObjectTag") in
      let bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Bucket") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ?s3ObjectTags ~bucket ~imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let s3ObjectTags =
        field_map json__ "S3ObjectTags" S3ObjectTagList.of_json in
      let bucket = field_map_exn json__ "Bucket" String_.of_json in
      let imageId = field_map_exn json__ "ImageId" ImageId.of_json in
      make ?dryRun ?s3ObjectTags ~bucket ~imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stores an AMI as a single object in an Amazon S3 bucket. To use this API, you must have the required permissions. For more information, see Permissions for storing and restoring AMIs using S3 in the Amazon EC2 User Guide. For more information, see Store and restore an AMI using S3 in the Amazon EC2 User Guide."]
module CreateSpotDatafeedSubscriptionResult =
  struct
    type nonrec t =
      {
      spotDatafeedSubscription: SpotDatafeedSubscription.t option
        [@ocaml.doc "The Spot Instance data feed subscription."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?spotDatafeedSubscription =
      fun () -> { spotDatafeedSubscription }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotDatafeedSubscription",
           (Option.map x.spotDatafeedSubscription
              ~f:SpotDatafeedSubscription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotDatafeedSubscription =
        (Option.map ~f:SpotDatafeedSubscription.of_xml)
          (Xml.child xml_arg0 "spotDatafeedSubscription") in
      make ?spotDatafeedSubscription ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let spotDatafeedSubscription =
        field_map json__ "SpotDatafeedSubscription"
          SpotDatafeedSubscription.of_json in
      make ?spotDatafeedSubscription ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateSpotDatafeedSubscription."]
module CreateSpotDatafeedSubscriptionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      bucket: String_.t
        [@ocaml.doc
          "The name of the Amazon S3 bucket in which to store the Spot Instance data feed. For more information about bucket names, see Bucket naming rules in the Amazon S3 User Guide."];
      prefix: String_.t option
        [@ocaml.doc "The prefix for the data feed file names."]}
    let context_ = "CreateSpotDatafeedSubscriptionRequest"
    let make ?dryRun =
      fun ?prefix -> fun ~bucket -> fun () -> { dryRun; prefix; bucket }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("bucket", (Some (String_.to_value x.bucket)));
        ("prefix", (Option.map x.prefix ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "bucket") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?prefix ~bucket ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix = field_map json__ "Prefix" String_.of_json in
      let bucket = field_map_exn json__ "Bucket" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?prefix ~bucket ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for CreateSpotDatafeedSubscription."]
module CreateSnapshotsResult =
  struct
    type nonrec t =
      {
      snapshots: SnapshotSet.t option [@ocaml.doc "List of snapshots."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?snapshots = fun () -> { snapshots }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("snapshotSet", (Option.map x.snapshots ~f:SnapshotSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snapshots =
        (Option.map ~f:SnapshotSet.of_xml) (Xml.child xml_arg0 "snapshotSet") in
      make ?snapshots ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let snapshots = field_map json__ "Snapshots" SnapshotSet.of_json in
      make ?snapshots ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates crash-consistent snapshots of multiple EBS volumes attached to an Amazon EC2 instance. Volumes are chosen by specifying an instance. Each volume attached to the specified instance will produce one snapshot that is crash-consistent across the instance. You can include all of the volumes currently attached to the instance, or you can exclude the root volume or specific data (non-root) volumes from the multi-volume snapshot set. The location of the source instance determines where you can create the snapshots. If the source instance is in a Region, you must create the snapshots in the same Region as the instance. If the source instance is in a Local Zone, you can create the snapshots in the same Local Zone or in its parent Amazon Web Services Region. If the source instance is on an Outpost, you can create the snapshots on the same Outpost or in its parent Amazon Web Services Region."]
module CreateSnapshotsRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc
          "A description propagated to every snapshot specified by the instance."];
      instanceSpecification: InstanceSpecification.t
        [@ocaml.doc
          "The instance to specify which volumes should be included in the snapshots."];
      outpostArn: String_.t option
        [@ocaml.doc
          "Only supported for instances on Outposts. If the source instance is not on an Outpost, omit this parameter. To create the snapshots on the same Outpost as the source instance, specify the ARN of that Outpost. The snapshots must be created on the same Outpost as the instance. To create the snapshots in the parent Region of the Outpost, omit this parameter. For more information, see Create local snapshots from volumes on an Outpost in the Amazon EBS User Guide."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "Tags to apply to every snapshot specified by the instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      copyTagsFromSource: CopyTagsFromSource.t option
        [@ocaml.doc
          "Copies the tags from the specified volume to corresponding snapshot."];
      location: SnapshotLocationEnum.t option
        [@ocaml.doc
          "Only supported for instances in Local Zones. If the source instance is not in a Local Zone, omit this parameter. To create local snapshots in the same Local Zone as the source instance, specify local. To create regional snapshots in the parent Region of the Local Zone, specify regional or omit this parameter. Default value: regional"]}
    let context_ = "CreateSnapshotsRequest"
    let make ?description =
      fun ?outpostArn ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ?copyTagsFromSource ->
              fun ?location ->
                fun ~instanceSpecification ->
                  fun () ->
                    {
                      description;
                      outpostArn;
                      tagSpecifications;
                      dryRun;
                      copyTagsFromSource;
                      location;
                      instanceSpecification
                    }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("InstanceSpecification",
          (Some (InstanceSpecification.to_value x.instanceSpecification)));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CopyTagsFromSource",
          (Option.map x.copyTagsFromSource ~f:CopyTagsFromSource.to_value));
        ("Location",
          (Option.map x.location ~f:SnapshotLocationEnum.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:SnapshotLocationEnum.of_xml)
          (Xml.child xml_arg0 "Location") in
      let copyTagsFromSource =
        (Option.map ~f:CopyTagsFromSource.of_xml)
          (Xml.child xml_arg0 "CopyTagsFromSource") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let instanceSpecification =
        InstanceSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?location ?copyTagsFromSource ?dryRun ?tagSpecifications
        ?outpostArn ~instanceSpecification ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" SnapshotLocationEnum.of_json in
      let copyTagsFromSource =
        field_map json__ "CopyTagsFromSource" CopyTagsFromSource.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let outpostArn = field_map json__ "OutpostArn" String_.of_json in
      let instanceSpecification =
        field_map_exn json__ "InstanceSpecification"
          InstanceSpecification.of_json in
      let description = field_map json__ "Description" String_.of_json in
      make ?location ?copyTagsFromSource ?dryRun ?tagSpecifications
        ?outpostArn ~instanceSpecification ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates crash-consistent snapshots of multiple EBS volumes attached to an Amazon EC2 instance. Volumes are chosen by specifying an instance. Each volume attached to the specified instance will produce one snapshot that is crash-consistent across the instance. You can include all of the volumes currently attached to the instance, or you can exclude the root volume or specific data (non-root) volumes from the multi-volume snapshot set. The location of the source instance determines where you can create the snapshots. If the source instance is in a Region, you must create the snapshots in the same Region as the instance. If the source instance is in a Local Zone, you can create the snapshots in the same Local Zone or in its parent Amazon Web Services Region. If the source instance is on an Outpost, you can create the snapshots on the same Outpost or in its parent Amazon Web Services Region."]
module CreateSnapshotRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description for the snapshot."];
      outpostArn: String_.t option
        [@ocaml.doc
          "Only supported for volumes on Outposts. If the source volume is not on an Outpost, omit this parameter. To create the snapshot on the same Outpost as the source volume, specify the ARN of that Outpost. The snapshot must be created on the same Outpost as the volume. To create the snapshot in the parent Region of the Outpost, omit this parameter. For more information, see Create local snapshots from volumes on an Outpost in the Amazon EBS User Guide."];
      volumeId: VolumeId.t [@ocaml.doc "The ID of the Amazon EBS volume."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the snapshot during creation."];
      location: SnapshotLocationEnum.t option
        [@ocaml.doc
          "Only supported for volumes in Local Zones. If the source volume is not in a Local Zone, omit this parameter. To create a local snapshot in the same Local Zone as the source volume, specify local. To create a regional snapshot in the parent Region of the Local Zone, specify regional or omit this parameter. Default value: regional"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateSnapshotRequest"
    let make ?description =
      fun ?outpostArn ->
        fun ?tagSpecifications ->
          fun ?location ->
            fun ?dryRun ->
              fun ~volumeId ->
                fun () ->
                  {
                    description;
                    outpostArn;
                    tagSpecifications;
                    location;
                    dryRun;
                    volumeId
                  }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("Location",
          (Option.map x.location ~f:SnapshotLocationEnum.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let location =
        (Option.map ~f:SnapshotLocationEnum.of_xml)
          (Xml.child xml_arg0 "Location") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?dryRun ?location ?tagSpecifications ~volumeId ?outpostArn
        ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let location = field_map json__ "Location" SnapshotLocationEnum.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let volumeId = field_map_exn json__ "VolumeId" VolumeId.of_json in
      let outpostArn = field_map json__ "OutpostArn" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      make ?dryRun ?location ?tagSpecifications ~volumeId ?outpostArn
        ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a snapshot of an EBS volume and stores it in Amazon S3. You can use snapshots for backups, to make copies of EBS volumes, and to save data before shutting down an instance. The location of the source EBS volume determines where you can create the snapshot. If the source volume is in a Region, you must create the snapshot in the same Region as the volume. If the source volume is in a Local Zone, you can create the snapshot in the same Local Zone or in its parent Amazon Web Services Region. If the source volume is on an Outpost, you can create the snapshot on the same Outpost or in its parent Amazon Web Services Region. When a snapshot is created, any Amazon Web Services Marketplace product codes that are associated with the source volume are propagated to the snapshot. You can take a snapshot of an attached volume that is in use. However, snapshots only capture data that has been written to your Amazon EBS volume at the time the snapshot command is issued; this might exclude any data that has been cached by any applications or the operating system. If you can pause any file systems on the volume long enough to take a snapshot, your snapshot should be complete. However, if you cannot pause all file writes to the volume, you should unmount the volume from within the instance, issue the snapshot command, and then remount the volume to ensure a consistent and complete snapshot. You may remount and use your volume while the snapshot status is pending. When you create a snapshot for an EBS volume that serves as a root device, we recommend that you stop the instance before taking the snapshot. Snapshots that are taken from encrypted volumes are automatically encrypted. Volumes that are created from encrypted snapshots are also automatically encrypted. Your encrypted volumes and any associated snapshots always remain protected. For more information, see Amazon EBS encryption in the Amazon EBS User Guide."]
module CreateSecurityGroupResult =
  struct
    type nonrec t =
      {
      groupId: String_.t option [@ocaml.doc "The ID of the security group."];
      tags: TagList.t option
        [@ocaml.doc "The tags assigned to the security group."];
      securityGroupArn: String_.t option
        [@ocaml.doc "The security group ARN."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?groupId =
      fun ?tags ->
        fun ?securityGroupArn ->
          fun () -> { groupId; tags; securityGroupArn }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("groupId", (Option.map x.groupId ~f:String_.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value));
        ("securityGroupArn",
          (Option.map x.securityGroupArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "securityGroupArn") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let groupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupId") in
      make ?securityGroupArn ?tags ?groupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupArn =
        field_map json__ "SecurityGroupArn" String_.of_json in
      let tags = field_map json__ "Tags" TagList.of_json in
      let groupId = field_map json__ "GroupId" String_.of_json in
      make ?securityGroupArn ?tags ?groupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a security group. A security group acts as a virtual firewall for your instance to control inbound and outbound traffic. For more information, see Amazon EC2 security groups in the Amazon EC2 User Guide and Security groups for your VPC in the Amazon VPC User Guide. When you create a security group, you specify a friendly name of your choice. You can't have two security groups for the same VPC with the same name. You have a default security group for use in your VPC. If you don't specify a security group when you launch an instance, the instance is launched into the appropriate default security group. A default security group includes a default rule that grants instances unrestricted network access to each other. You can add or remove rules from your security groups using AuthorizeSecurityGroupIngress, AuthorizeSecurityGroupEgress, RevokeSecurityGroupIngress, and RevokeSecurityGroupEgress. For more information about VPC security group limits, see Amazon VPC Limits."]
module CreateSecurityGroupRequest =
  struct
    type nonrec t =
      {
      description: String_.t
        [@ocaml.doc
          "A description for the security group. Constraints: Up to 255 characters in length Valid characters: a-z, A-Z, 0-9, spaces, and ._-:/()#,\\@\\[\\]+=&;\\{\\}!$*"];
      groupName: String_.t
        [@ocaml.doc
          "The name of the security group. Names are case-insensitive and must be unique within the VPC. Constraints: Up to 255 characters in length. Can't start with sg-. Valid characters: a-z, A-Z, 0-9, spaces, and ._-:/()#,\\@\\[\\]+=&;\\{\\}!$*"];
      vpcId: VpcId.t option
        [@ocaml.doc "The ID of the VPC. Required for a nondefault VPC."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the security group."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateSecurityGroupRequest"
    let make ?vpcId =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~description ->
            fun ~groupName ->
              fun () ->
                { vpcId; tagSpecifications; dryRun; description; groupName }
    let to_value x =
      structure_to_value
        [("GroupDescription", (Some (String_.to_value x.description)));
        ("GroupName", (Some (String_.to_value x.groupName)));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let groupName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      let description =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupDescription") in
      make ?dryRun ?tagSpecifications ?vpcId ~groupName ~description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map json__ "VpcId" VpcId.of_json in
      let groupName = field_map_exn json__ "GroupName" String_.of_json in
      let description = field_map_exn json__ "Description" String_.of_json in
      make ?dryRun ?tagSpecifications ?vpcId ~groupName ~description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a security group. A security group acts as a virtual firewall for your instance to control inbound and outbound traffic. For more information, see Amazon EC2 security groups in the Amazon EC2 User Guide and Security groups for your VPC in the Amazon VPC User Guide. When you create a security group, you specify a friendly name of your choice. You can't have two security groups for the same VPC with the same name. You have a default security group for use in your VPC. If you don't specify a security group when you launch an instance, the instance is launched into the appropriate default security group. A default security group includes a default rule that grants instances unrestricted network access to each other. You can add or remove rules from your security groups using AuthorizeSecurityGroupIngress, AuthorizeSecurityGroupEgress, RevokeSecurityGroupIngress, and RevokeSecurityGroupEgress. For more information about VPC security group limits, see Amazon VPC Limits."]
module CreateSecondarySubnetResult =
  struct
    type nonrec t =
      {
      secondarySubnet: SecondarySubnet.t option
        [@ocaml.doc "Information about the secondary subnet."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondarySubnet =
      fun ?clientToken -> fun () -> { secondarySubnet; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondarySubnet",
           (Option.map x.secondarySubnet ~f:SecondarySubnet.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let secondarySubnet =
        (Option.map ~f:SecondarySubnet.of_xml)
          (Xml.child xml_arg0 "secondarySubnet") in
      make ?clientToken ?secondarySubnet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let secondarySubnet =
        field_map json__ "SecondarySubnet" SecondarySubnet.of_json in
      make ?clientToken ?secondarySubnet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a secondary subnet in a secondary network. A secondary subnet CIDR block must not overlap with the CIDR block of an existing secondary subnet in the secondary network. After you create a secondary subnet, you can't change its CIDR block. The allowed size for a secondary subnet CIDR block is between /28 netmask (16 IP addresses) and /12 netmask (1,048,576 IP addresses). Amazon reserves the first four IP addresses and the last IP address in each secondary subnet for internal use."]
module CreateSecondarySubnetRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "The Availability Zone for the secondary subnet. You cannot specify both AvailabilityZone and AvailabilityZoneId in the same request."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "The ID of the Availability Zone for the secondary subnet. This option is preferred over AvailabilityZone as it provides a consistent identifier across Amazon Web Services accounts. You cannot specify both AvailabilityZone and AvailabilityZoneId in the same request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipv4CidrBlock: String_.t
        [@ocaml.doc
          "The IPv4 CIDR block for the secondary subnet. The CIDR block size must be between /12 and /28."];
      secondaryNetworkId: SecondaryNetworkId.t
        [@ocaml.doc
          "The ID of the secondary network in which to create the secondary subnet."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the secondary subnet."]}
    let context_ = "CreateSecondarySubnetRequest"
    let make ?clientToken =
      fun ?availabilityZone ->
        fun ?availabilityZoneId ->
          fun ?dryRun ->
            fun ?tagSpecifications ->
              fun ~ipv4CidrBlock ->
                fun ~secondaryNetworkId ->
                  fun () ->
                    {
                      clientToken;
                      availabilityZone;
                      availabilityZoneId;
                      dryRun;
                      tagSpecifications;
                      ipv4CidrBlock;
                      secondaryNetworkId
                    }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Ipv4CidrBlock", (Some (String_.to_value x.ipv4CidrBlock)));
        ("SecondaryNetworkId",
          (Some (SecondaryNetworkId.to_value x.secondaryNetworkId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let secondaryNetworkId =
        SecondaryNetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecondaryNetworkId") in
      let ipv4CidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Ipv4CidrBlock") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~secondaryNetworkId ~ipv4CidrBlock ?dryRun
        ?availabilityZoneId ?availabilityZone ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let secondaryNetworkId =
        field_map_exn json__ "SecondaryNetworkId" SecondaryNetworkId.of_json in
      let ipv4CidrBlock =
        field_map_exn json__ "Ipv4CidrBlock" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?tagSpecifications ~secondaryNetworkId ~ipv4CidrBlock ?dryRun
        ?availabilityZoneId ?availabilityZone ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a secondary subnet in a secondary network. A secondary subnet CIDR block must not overlap with the CIDR block of an existing secondary subnet in the secondary network. After you create a secondary subnet, you can't change its CIDR block. The allowed size for a secondary subnet CIDR block is between /28 netmask (16 IP addresses) and /12 netmask (1,048,576 IP addresses). Amazon reserves the first four IP addresses and the last IP address in each secondary subnet for internal use."]
module CreateSecondaryNetworkResult =
  struct
    type nonrec t =
      {
      secondaryNetwork: SecondaryNetwork.t option
        [@ocaml.doc "Information about the secondary network."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?secondaryNetwork =
      fun ?clientToken -> fun () -> { secondaryNetwork; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("secondaryNetwork",
           (Option.map x.secondaryNetwork ~f:SecondaryNetwork.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let secondaryNetwork =
        (Option.map ~f:SecondaryNetwork.of_xml)
          (Xml.child xml_arg0 "secondaryNetwork") in
      make ?clientToken ?secondaryNetwork ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let secondaryNetwork =
        field_map json__ "SecondaryNetwork" SecondaryNetwork.of_json in
      make ?clientToken ?secondaryNetwork ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a secondary network. The allowed size for a secondary network CIDR block is between /28 netmask (16 IP addresses) and /12 netmask (1,048,576 IP addresses)."]
module CreateSecondaryNetworkRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipv4CidrBlock: String_.t
        [@ocaml.doc
          "The IPv4 CIDR block for the secondary network. The CIDR block size must be between /12 and /28."];
      networkType: SecondaryNetworkType.t
        [@ocaml.doc "The type of secondary network."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the secondary network."]}
    let context_ = "CreateSecondaryNetworkRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~ipv4CidrBlock ->
            fun ~networkType ->
              fun () ->
                {
                  clientToken;
                  dryRun;
                  tagSpecifications;
                  ipv4CidrBlock;
                  networkType
                }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Ipv4CidrBlock", (Some (String_.to_value x.ipv4CidrBlock)));
        ("NetworkType", (Some (SecondaryNetworkType.to_value x.networkType)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let networkType =
        SecondaryNetworkType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkType") in
      let ipv4CidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Ipv4CidrBlock") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~networkType ~ipv4CidrBlock ?dryRun
        ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let networkType =
        field_map_exn json__ "NetworkType" SecondaryNetworkType.of_json in
      let ipv4CidrBlock =
        field_map_exn json__ "Ipv4CidrBlock" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?tagSpecifications ~networkType ~ipv4CidrBlock ?dryRun
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a secondary network. The allowed size for a secondary network CIDR block is between /28 netmask (16 IP addresses) and /12 netmask (1,048,576 IP addresses)."]
module CreateRouteTableResult =
  struct
    type nonrec t =
      {
      routeTable: RouteTable.t option
        [@ocaml.doc "Information about the route table."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeTable =
      fun ?clientToken -> fun () -> { routeTable; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeTable", (Option.map x.routeTable ~f:RouteTable.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let routeTable =
        (Option.map ~f:RouteTable.of_xml) (Xml.child xml_arg0 "routeTable") in
      make ?clientToken ?routeTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let routeTable = field_map json__ "RouteTable" RouteTable.of_json in
      make ?clientToken ?routeTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a route table for the specified VPC. After you create a route table, you can add routes and associate the table with a subnet. For more information, see Route tables in the Amazon VPC User Guide."]
module CreateRouteTableRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the route table."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."]}
    let context_ = "CreateRouteTableRequest"
    let make ?tagSpecifications =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ~vpcId ->
            fun () -> { tagSpecifications; clientToken; dryRun; vpcId }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ~vpcId ?dryRun ?clientToken ?tagSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ~vpcId ?dryRun ?clientToken ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a route table for the specified VPC. After you create a route table, you can add routes and associate the table with a subnet. For more information, see Route tables in the Amazon VPC User Guide."]
module CreateRouteServerResult =
  struct
    type nonrec t =
      {
      routeServer: RouteServer.t option
        [@ocaml.doc "Information about the created route server."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServer = fun () -> { routeServer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServer", (Option.map x.routeServer ~f:RouteServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServer =
        (Option.map ~f:RouteServer.of_xml) (Xml.child xml_arg0 "routeServer") in
      make ?routeServer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServer = field_map json__ "RouteServer" RouteServer.of_json in
      make ?routeServer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new route server to manage dynamic routing in a VPC. Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability with third-party workloads. Route server supports the follow route table types: VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route server does not support route tables associated with virtual private gateways. To propagate routes into a transit gateway route table, use Transit Gateway Connect. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module CreateRouteServerRequest =
  struct
    type nonrec t =
      {
      amazonSideAsn: Long.t
        [@ocaml.doc
          "The private Autonomous System Number (ASN) for the Amazon side of the BGP session. Valid values are from 1 to 4294967295. We recommend using a private ASN in the 64512\226\128\14765534 (16-bit ASN) or 4200000000\226\128\1474294967294 (32-bit ASN) range."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure idempotency of the request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      persistRoutes: RouteServerPersistRoutesAction.t option
        [@ocaml.doc
          "Indicates whether routes should be persisted after all BGP sessions are terminated."];
      persistRoutesDuration: BoxedLong.t option
        [@ocaml.doc
          "The number of minutes a route server will wait after BGP is re-established to unpersist the routes in the FIB and RIB. Value must be in the range of 1-5. Required if PersistRoutes is enabled. If you set the duration to 1 minute, then when your network appliance re-establishes BGP with route server, it has 1 minute to relearn it's adjacent network and advertise those routes to route server before route server resumes normal functionality. In most cases, 1 minute is probably sufficient. If, however, you have concerns that your BGP network may not be capable of fully re-establishing and re-learning everything in 1 minute, you can increase the duration up to 5 minutes."];
      snsNotificationsEnabled: Boolean.t option
        [@ocaml.doc
          "Indicates whether SNS notifications should be enabled for route server events. Enabling SNS notifications persists BGP status changes to an SNS topic provisioned by Amazon Web Services."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the route server during creation."]}
    let context_ = "CreateRouteServerRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?persistRoutes ->
          fun ?persistRoutesDuration ->
            fun ?snsNotificationsEnabled ->
              fun ?tagSpecifications ->
                fun ~amazonSideAsn ->
                  fun () ->
                    {
                      clientToken;
                      dryRun;
                      persistRoutes;
                      persistRoutesDuration;
                      snsNotificationsEnabled;
                      tagSpecifications;
                      amazonSideAsn
                    }
    let to_value x =
      structure_to_value
        [("AmazonSideAsn", (Some (Long.to_value x.amazonSideAsn)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PersistRoutes",
          (Option.map x.persistRoutes
             ~f:RouteServerPersistRoutesAction.to_value));
        ("PersistRoutesDuration",
          (Option.map x.persistRoutesDuration ~f:BoxedLong.to_value));
        ("SnsNotificationsEnabled",
          (Option.map x.snsNotificationsEnabled ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let snsNotificationsEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SnsNotificationsEnabled") in
      let persistRoutesDuration =
        (Option.map ~f:BoxedLong.of_xml)
          (Xml.child xml_arg0 "PersistRoutesDuration") in
      let persistRoutes =
        (Option.map ~f:RouteServerPersistRoutesAction.of_xml)
          (Xml.child xml_arg0 "PersistRoutes") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let amazonSideAsn =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AmazonSideAsn") in
      make ?tagSpecifications ?snsNotificationsEnabled ?persistRoutesDuration
        ?persistRoutes ?dryRun ?clientToken ~amazonSideAsn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let snsNotificationsEnabled =
        field_map json__ "SnsNotificationsEnabled" Boolean.of_json in
      let persistRoutesDuration =
        field_map json__ "PersistRoutesDuration" BoxedLong.of_json in
      let persistRoutes =
        field_map json__ "PersistRoutes"
          RouteServerPersistRoutesAction.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let amazonSideAsn = field_map_exn json__ "AmazonSideAsn" Long.of_json in
      make ?tagSpecifications ?snsNotificationsEnabled ?persistRoutesDuration
        ?persistRoutes ?dryRun ?clientToken ~amazonSideAsn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new route server to manage dynamic routing in a VPC. Amazon VPC Route Server simplifies routing for traffic between workloads that are deployed within a VPC and its internet gateways. With this feature, VPC Route Server dynamically updates VPC and internet gateway route tables with your preferred IPv4 or IPv6 routes to achieve routing fault tolerance for those workloads. This enables you to automatically reroute traffic within a VPC, which increases the manageability of VPC routing and interoperability with third-party workloads. Route server supports the follow route table types: VPC route tables not associated with subnets Subnet route tables Internet gateway route tables Route server does not support route tables associated with virtual private gateways. To propagate routes into a transit gateway route table, use Transit Gateway Connect. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module CreateRouteServerPeerResult =
  struct
    type nonrec t =
      {
      routeServerPeer: RouteServerPeer.t option
        [@ocaml.doc "Information about the created route server peer."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerPeer = fun () -> { routeServerPeer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerPeer",
           (Option.map x.routeServerPeer ~f:RouteServerPeer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServerPeer =
        (Option.map ~f:RouteServerPeer.of_xml)
          (Xml.child xml_arg0 "routeServerPeer") in
      make ?routeServerPeer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServerPeer =
        field_map json__ "RouteServerPeer" RouteServerPeer.of_json in
      make ?routeServerPeer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new BGP peer for a specified route server endpoint. A route server peer is a session between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other network security function running on an EC2 instance). The device must meet these requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP sessions For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module CreateRouteServerPeerRequest =
  struct
    type nonrec t =
      {
      routeServerEndpointId: RouteServerEndpointId.t
        [@ocaml.doc
          "The ID of the route server endpoint for which to create a peer."];
      peerAddress: String_.t
        [@ocaml.doc "The IPv4 address of the peer device."];
      bgpOptions: RouteServerBgpOptionsRequest.t
        [@ocaml.doc
          "The BGP options for the peer, including ASN (Autonomous System Number) and BFD (Bidrectional Forwarding Detection) settings."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the route server peer during creation."]}
    let context_ = "CreateRouteServerPeerRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ~routeServerEndpointId ->
          fun ~peerAddress ->
            fun ~bgpOptions ->
              fun () ->
                {
                  dryRun;
                  tagSpecifications;
                  routeServerEndpointId;
                  peerAddress;
                  bgpOptions
                }
    let to_value x =
      structure_to_value
        [("RouteServerEndpointId",
           (Some (RouteServerEndpointId.to_value x.routeServerEndpointId)));
        ("PeerAddress", (Some (String_.to_value x.peerAddress)));
        ("BgpOptions",
          (Some (RouteServerBgpOptionsRequest.to_value x.bgpOptions)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let bgpOptions =
        RouteServerBgpOptionsRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BgpOptions") in
      let peerAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerAddress") in
      let routeServerEndpointId =
        RouteServerEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteServerEndpointId") in
      make ?tagSpecifications ?dryRun ~bgpOptions ~peerAddress
        ~routeServerEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let bgpOptions =
        field_map_exn json__ "BgpOptions"
          RouteServerBgpOptionsRequest.of_json in
      let peerAddress = field_map_exn json__ "PeerAddress" String_.of_json in
      let routeServerEndpointId =
        field_map_exn json__ "RouteServerEndpointId"
          RouteServerEndpointId.of_json in
      make ?tagSpecifications ?dryRun ~bgpOptions ~peerAddress
        ~routeServerEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new BGP peer for a specified route server endpoint. A route server peer is a session between a route server endpoint and the device deployed in Amazon Web Services (such as a firewall appliance or other network security function running on an EC2 instance). The device must meet these requirements: Have an elastic network interface in the VPC Support BGP (Border Gateway Protocol) Can initiate BGP sessions For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module CreateRouteServerEndpointResult =
  struct
    type nonrec t =
      {
      routeServerEndpoint: RouteServerEndpoint.t option
        [@ocaml.doc "Information about the created route server endpoint."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerEndpoint = fun () -> { routeServerEndpoint }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerEndpoint",
           (Option.map x.routeServerEndpoint ~f:RouteServerEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServerEndpoint =
        (Option.map ~f:RouteServerEndpoint.of_xml)
          (Xml.child xml_arg0 "routeServerEndpoint") in
      make ?routeServerEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServerEndpoint =
        field_map json__ "RouteServerEndpoint" RouteServerEndpoint.of_json in
      make ?routeServerEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new endpoint for a route server in a specified subnet. A route server endpoint is an Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol) connections between your route server and your BGP peers. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module CreateRouteServerEndpointRequest =
  struct
    type nonrec t =
      {
      routeServerId: RouteServerId.t
        [@ocaml.doc
          "The ID of the route server for which to create an endpoint."];
      subnetId: SubnetId.t
        [@ocaml.doc
          "The ID of the subnet in which to create the route server endpoint."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure idempotency of the request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the route server endpoint during creation."]}
    let context_ = "CreateRouteServerEndpointRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~routeServerId ->
            fun ~subnetId ->
              fun () ->
                {
                  clientToken;
                  dryRun;
                  tagSpecifications;
                  routeServerId;
                  subnetId
                }
    let to_value x =
      structure_to_value
        [("RouteServerId", (Some (RouteServerId.to_value x.routeServerId)));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let routeServerId =
        RouteServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteServerId") in
      make ?tagSpecifications ?dryRun ?clientToken ~subnetId ~routeServerId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let subnetId = field_map_exn json__ "SubnetId" SubnetId.of_json in
      let routeServerId =
        field_map_exn json__ "RouteServerId" RouteServerId.of_json in
      make ?tagSpecifications ?dryRun ?clientToken ~subnetId ~routeServerId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new endpoint for a route server in a specified subnet. A route server endpoint is an Amazon Web Services-managed component inside a subnet that facilitates BGP (Border Gateway Protocol) connections between your route server and your BGP peers. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module CreateRouteResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a route in a route table within a VPC. You must specify either a destination CIDR block or a prefix list ID. You must also specify exactly one of the resources from the parameter list. When determining how to route traffic, we use the route with the most specific match. For example, traffic is destined for the IPv4 address 192.0.2.3, and the route table includes the following two IPv4 routes: 192.0.2.0/24 (goes to some target A) 192.0.2.0/28 (goes to some target B) Both routes apply to the traffic destined for 192.0.2.3. However, the second route in the list covers a smaller number of IP addresses and is therefore more specific, so we use that route to determine where to target the traffic. For more information about route tables, see Route tables in the Amazon VPC User Guide."]
module CreateRouteRequest =
  struct
    type nonrec t =
      {
      destinationPrefixListId: PrefixListResourceId.t option
        [@ocaml.doc
          "The ID of a prefix list used for the destination match."];
      vpcEndpointId: VpcEndpointId.t option
        [@ocaml.doc
          "The ID of a VPC endpoint. Supported for Gateway Load Balancer endpoints only."];
      transitGatewayId: TransitGatewayId.t option
        [@ocaml.doc "The ID of a transit gateway."];
      localGatewayId: LocalGatewayId.t option
        [@ocaml.doc "The ID of the local gateway."];
      carrierGatewayId: CarrierGatewayId.t option
        [@ocaml.doc
          "The ID of the carrier gateway. You can only use this option when the VPC contains a subnet which is associated with a Wavelength Zone."];
      coreNetworkArn: CoreNetworkArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the core network."];
      odbNetworkArn: OdbNetworkArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the ODB network."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      routeTableId: RouteTableId.t
        [@ocaml.doc "The ID of the route table for the route."];
      destinationCidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 CIDR address block used for the destination match. Routing decisions are based on the most specific match. We modify the specified CIDR block to its canonical form; for example, if you specify 100.68.0.18/18, we modify it to 100.68.0.0/18."];
      gatewayId: RouteGatewayId.t option
        [@ocaml.doc
          "The ID of an internet gateway or virtual private gateway attached to your VPC."];
      destinationIpv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 CIDR block used for the destination match. Routing decisions are based on the most specific match."];
      egressOnlyInternetGatewayId: EgressOnlyInternetGatewayId.t option
        [@ocaml.doc
          "\\[IPv6 traffic only\\] The ID of an egress-only internet gateway."];
      instanceId: InstanceId.t option
        [@ocaml.doc
          "The ID of a NAT instance in your VPC. The operation fails if you specify an instance ID unless exactly one network interface is attached."];
      networkInterfaceId: NetworkInterfaceId.t option
        [@ocaml.doc "The ID of a network interface."];
      vpcPeeringConnectionId: VpcPeeringConnectionId.t option
        [@ocaml.doc "The ID of a VPC peering connection."];
      natGatewayId: NatGatewayId.t option
        [@ocaml.doc "\\[IPv4 traffic only\\] The ID of a NAT gateway."]}
    let context_ = "CreateRouteRequest"
    let make ?destinationPrefixListId =
      fun ?vpcEndpointId ->
        fun ?transitGatewayId ->
          fun ?localGatewayId ->
            fun ?carrierGatewayId ->
              fun ?coreNetworkArn ->
                fun ?odbNetworkArn ->
                  fun ?dryRun ->
                    fun ?destinationCidrBlock ->
                      fun ?gatewayId ->
                        fun ?destinationIpv6CidrBlock ->
                          fun ?egressOnlyInternetGatewayId ->
                            fun ?instanceId ->
                              fun ?networkInterfaceId ->
                                fun ?vpcPeeringConnectionId ->
                                  fun ?natGatewayId ->
                                    fun ~routeTableId ->
                                      fun () ->
                                        {
                                          destinationPrefixListId;
                                          vpcEndpointId;
                                          transitGatewayId;
                                          localGatewayId;
                                          carrierGatewayId;
                                          coreNetworkArn;
                                          odbNetworkArn;
                                          dryRun;
                                          destinationCidrBlock;
                                          gatewayId;
                                          destinationIpv6CidrBlock;
                                          egressOnlyInternetGatewayId;
                                          instanceId;
                                          networkInterfaceId;
                                          vpcPeeringConnectionId;
                                          natGatewayId;
                                          routeTableId
                                        }
    let to_value x =
      structure_to_value
        [("DestinationPrefixListId",
           (Option.map x.destinationPrefixListId
              ~f:PrefixListResourceId.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointId ~f:VpcEndpointId.to_value));
        ("TransitGatewayId",
          (Option.map x.transitGatewayId ~f:TransitGatewayId.to_value));
        ("LocalGatewayId",
          (Option.map x.localGatewayId ~f:LocalGatewayId.to_value));
        ("CarrierGatewayId",
          (Option.map x.carrierGatewayId ~f:CarrierGatewayId.to_value));
        ("CoreNetworkArn",
          (Option.map x.coreNetworkArn ~f:CoreNetworkArn.to_value));
        ("OdbNetworkArn",
          (Option.map x.odbNetworkArn ~f:OdbNetworkArn.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)));
        ("destinationCidrBlock",
          (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("gatewayId", (Option.map x.gatewayId ~f:RouteGatewayId.to_value));
        ("destinationIpv6CidrBlock",
          (Option.map x.destinationIpv6CidrBlock ~f:String_.to_value));
        ("egressOnlyInternetGatewayId",
          (Option.map x.egressOnlyInternetGatewayId
             ~f:EgressOnlyInternetGatewayId.to_value));
        ("instanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("vpcPeeringConnectionId",
          (Option.map x.vpcPeeringConnectionId
             ~f:VpcPeeringConnectionId.to_value));
        ("natGatewayId",
          (Option.map x.natGatewayId ~f:NatGatewayId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayId =
        (Option.map ~f:NatGatewayId.of_xml)
          (Xml.child xml_arg0 "natGatewayId") in
      let vpcPeeringConnectionId =
        (Option.map ~f:VpcPeeringConnectionId.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnectionId") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "instanceId") in
      let egressOnlyInternetGatewayId =
        (Option.map ~f:EgressOnlyInternetGatewayId.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGatewayId") in
      let destinationIpv6CidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationIpv6CidrBlock") in
      let gatewayId =
        (Option.map ~f:RouteGatewayId.of_xml)
          (Xml.child xml_arg0 "gatewayId") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationCidrBlock") in
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let odbNetworkArn =
        (Option.map ~f:OdbNetworkArn.of_xml)
          (Xml.child xml_arg0 "OdbNetworkArn") in
      let coreNetworkArn =
        (Option.map ~f:CoreNetworkArn.of_xml)
          (Xml.child xml_arg0 "CoreNetworkArn") in
      let carrierGatewayId =
        (Option.map ~f:CarrierGatewayId.of_xml)
          (Xml.child xml_arg0 "CarrierGatewayId") in
      let localGatewayId =
        (Option.map ~f:LocalGatewayId.of_xml)
          (Xml.child xml_arg0 "LocalGatewayId") in
      let transitGatewayId =
        (Option.map ~f:TransitGatewayId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayId") in
      let vpcEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      make ?natGatewayId ?vpcPeeringConnectionId ?networkInterfaceId
        ?instanceId ?egressOnlyInternetGatewayId ?destinationIpv6CidrBlock
        ?gatewayId ?destinationCidrBlock ~routeTableId ?dryRun ?odbNetworkArn
        ?coreNetworkArn ?carrierGatewayId ?localGatewayId ?transitGatewayId
        ?vpcEndpointId ?destinationPrefixListId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGatewayId = field_map json__ "NatGatewayId" NatGatewayId.of_json in
      let vpcPeeringConnectionId =
        field_map json__ "VpcPeeringConnectionId"
          VpcPeeringConnectionId.of_json in
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let egressOnlyInternetGatewayId =
        field_map json__ "EgressOnlyInternetGatewayId"
          EgressOnlyInternetGatewayId.of_json in
      let destinationIpv6CidrBlock =
        field_map json__ "DestinationIpv6CidrBlock" String_.of_json in
      let gatewayId = field_map json__ "GatewayId" RouteGatewayId.of_json in
      let destinationCidrBlock =
        field_map json__ "DestinationCidrBlock" String_.of_json in
      let routeTableId =
        field_map_exn json__ "RouteTableId" RouteTableId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let odbNetworkArn =
        field_map json__ "OdbNetworkArn" OdbNetworkArn.of_json in
      let coreNetworkArn =
        field_map json__ "CoreNetworkArn" CoreNetworkArn.of_json in
      let carrierGatewayId =
        field_map json__ "CarrierGatewayId" CarrierGatewayId.of_json in
      let localGatewayId =
        field_map json__ "LocalGatewayId" LocalGatewayId.of_json in
      let transitGatewayId =
        field_map json__ "TransitGatewayId" TransitGatewayId.of_json in
      let vpcEndpointId =
        field_map json__ "VpcEndpointId" VpcEndpointId.of_json in
      let destinationPrefixListId =
        field_map json__ "DestinationPrefixListId"
          PrefixListResourceId.of_json in
      make ?natGatewayId ?vpcPeeringConnectionId ?networkInterfaceId
        ?instanceId ?egressOnlyInternetGatewayId ?destinationIpv6CidrBlock
        ?gatewayId ?destinationCidrBlock ~routeTableId ?dryRun ?odbNetworkArn
        ?coreNetworkArn ?carrierGatewayId ?localGatewayId ?transitGatewayId
        ?vpcEndpointId ?destinationPrefixListId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a route in a route table within a VPC. You must specify either a destination CIDR block or a prefix list ID. You must also specify exactly one of the resources from the parameter list. When determining how to route traffic, we use the route with the most specific match. For example, traffic is destined for the IPv4 address 192.0.2.3, and the route table includes the following two IPv4 routes: 192.0.2.0/24 (goes to some target A) 192.0.2.0/28 (goes to some target B) Both routes apply to the traffic destined for 192.0.2.3. However, the second route in the list covers a smaller number of IP addresses and is therefore more specific, so we use that route to determine where to target the traffic. For more information about route tables, see Route tables in the Amazon VPC User Guide."]
module CreateRestoreImageTaskResult =
  struct
    type nonrec t = {
      imageId: String_.t option [@ocaml.doc "The AMI ID."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let imageId = field_map json__ "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a task that restores an AMI from an Amazon S3 object that was previously created by using CreateStoreImageTask. To use this API, you must have the required permissions. For more information, see Permissions for storing and restoring AMIs using S3 in the Amazon EC2 User Guide. For more information, see Store and restore an AMI using S3 in the Amazon EC2 User Guide."]
module CreateRestoreImageTaskRequest =
  struct
    type nonrec t =
      {
      bucket: String_.t
        [@ocaml.doc
          "The name of the Amazon S3 bucket that contains the stored AMI object."];
      objectKey: String_.t
        [@ocaml.doc "The name of the stored AMI object in the bucket."];
      name: ImageNameRequest.t option
        [@ocaml.doc
          "The name for the restored AMI. The name must be unique for AMIs in the Region for this account. If you do not provide a name, the new AMI gets the same name as the original AMI."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the AMI and snapshots on restoration. You can tag the AMI, the snapshots, or both. To tag the AMI, the value for ResourceType must be image. To tag the snapshots, the value for ResourceType must be snapshot. The same tag is applied to all of the snapshots that are created."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateRestoreImageTaskRequest"
    let make ?name =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~bucket ->
            fun ~objectKey ->
              fun () ->
                { name; tagSpecifications; dryRun; bucket; objectKey }
    let to_value x =
      structure_to_value
        [("Bucket", (Some (String_.to_value x.bucket)));
        ("ObjectKey", (Some (String_.to_value x.objectKey)));
        ("Name", (Option.map x.name ~f:ImageNameRequest.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let name =
        (Option.map ~f:ImageNameRequest.of_xml) (Xml.child xml_arg0 "Name") in
      let objectKey =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ObjectKey") in
      let bucket =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Bucket") in
      make ?dryRun ?tagSpecifications ?name ~objectKey ~bucket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let name = field_map json__ "Name" ImageNameRequest.of_json in
      let objectKey = field_map_exn json__ "ObjectKey" String_.of_json in
      let bucket = field_map_exn json__ "Bucket" String_.of_json in
      make ?dryRun ?tagSpecifications ?name ~objectKey ~bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a task that restores an AMI from an Amazon S3 object that was previously created by using CreateStoreImageTask. To use this API, you must have the required permissions. For more information, see Permissions for storing and restoring AMIs using S3 in the Amazon EC2 User Guide. For more information, see Store and restore an AMI using S3 in the Amazon EC2 User Guide."]
module CreateReservedInstancesListingResult =
  struct
    type nonrec t =
      {
      reservedInstancesListings: ReservedInstancesListingList.t option
        [@ocaml.doc
          "Information about the Standard Reserved Instance listing."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesListings =
      fun () -> { reservedInstancesListings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesListingsSet",
           (Option.map x.reservedInstancesListings
              ~f:ReservedInstancesListingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListings =
        (Option.map ~f:ReservedInstancesListingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingsSet") in
      make ?reservedInstancesListings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesListings =
        field_map json__ "ReservedInstancesListings"
          ReservedInstancesListingList.of_json in
      make ?reservedInstancesListings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateReservedInstancesListing."]
module CreateReservedInstancesListingRequest =
  struct
    type nonrec t =
      {
      reservedInstancesId: ReservationId.t
        [@ocaml.doc "The ID of the active Standard Reserved Instance."];
      instanceCount: Integer.t
        [@ocaml.doc
          "The number of instances that are a part of a Reserved Instance account to be listed in the Reserved Instance Marketplace. This number should be less than or equal to the instance count associated with the Reserved Instance ID specified in this call."];
      priceSchedules: PriceScheduleSpecificationList.t
        [@ocaml.doc
          "A list specifying the price of the Standard Reserved Instance for each month remaining in the Reserved Instance term."];
      clientToken: String_.t
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure idempotency of your listings. This helps avoid duplicate listings. For more information, see Ensuring Idempotency."]}
    let context_ = "CreateReservedInstancesListingRequest"
    let make ~reservedInstancesId =
      fun ~instanceCount ->
        fun ~priceSchedules ->
          fun ~clientToken ->
            fun () ->
              {
                reservedInstancesId;
                instanceCount;
                priceSchedules;
                clientToken
              }
    let to_value x =
      structure_to_value
        [("reservedInstancesId",
           (Some (ReservationId.to_value x.reservedInstancesId)));
        ("instanceCount", (Some (Integer.to_value x.instanceCount)));
        ("priceSchedules",
          (Some (PriceScheduleSpecificationList.to_value x.priceSchedules)));
        ("clientToken", (Some (String_.to_value x.clientToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientToken") in
      let priceSchedules =
        PriceScheduleSpecificationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "priceSchedules") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceCount") in
      let reservedInstancesId =
        ReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reservedInstancesId") in
      make ~clientToken ~priceSchedules ~instanceCount ~reservedInstancesId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map_exn json__ "ClientToken" String_.of_json in
      let priceSchedules =
        field_map_exn json__ "PriceSchedules"
          PriceScheduleSpecificationList.of_json in
      let instanceCount =
        field_map_exn json__ "InstanceCount" Integer.of_json in
      let reservedInstancesId =
        field_map_exn json__ "ReservedInstancesId" ReservationId.of_json in
      make ~clientToken ~priceSchedules ~instanceCount ~reservedInstancesId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for CreateReservedInstancesListing."]
module CreateReplaceRootVolumeTaskResult =
  struct
    type nonrec t =
      {
      replaceRootVolumeTask: ReplaceRootVolumeTask.t option
        [@ocaml.doc "Information about the root volume replacement task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?replaceRootVolumeTask = fun () -> { replaceRootVolumeTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("replaceRootVolumeTask",
           (Option.map x.replaceRootVolumeTask
              ~f:ReplaceRootVolumeTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let replaceRootVolumeTask =
        (Option.map ~f:ReplaceRootVolumeTask.of_xml)
          (Xml.child xml_arg0 "replaceRootVolumeTask") in
      make ?replaceRootVolumeTask ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let replaceRootVolumeTask =
        field_map json__ "ReplaceRootVolumeTask"
          ReplaceRootVolumeTask.of_json in
      make ?replaceRootVolumeTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces the EBS-backed root volume for a running instance with a new volume that is restored to the original root volume's launch state, that is restored to a specific snapshot taken from the original root volume, or that is restored from an AMI that has the same key characteristics as that of the instance. For more information, see Replace a root volume in the Amazon EC2 User Guide."]
module CreateReplaceRootVolumeTaskRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t
        [@ocaml.doc
          "The ID of the instance for which to replace the root volume."];
      snapshotId: SnapshotId.t option
        [@ocaml.doc
          "The ID of the snapshot from which to restore the replacement root volume. The specified snapshot must be a snapshot that you previously created from the original root volume. If you want to restore the replacement root volume to the initial launch state, or if you want to restore the replacement root volume from an AMI, omit this parameter."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure the idempotency of the request. If you do not specify a client token, a randomly generated token is used for the request to ensure idempotency. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the root volume replacement task."];
      imageId: ImageId.t option
        [@ocaml.doc
          "The ID of the AMI to use to restore the root volume. The specified AMI must have the same product code, billing information, architecture type, and virtualization type as that of the instance. If you want to restore the replacement volume from a specific snapshot, or if you want to restore it to its launch state, omit this parameter."];
      deleteReplacedRootVolume: Boolean.t option
        [@ocaml.doc
          "Indicates whether to automatically delete the original root volume after the root volume replacement task completes. To delete the original root volume, specify true. If you choose to keep the original root volume after the replacement task completes, you must manually delete it when you no longer need it."];
      volumeInitializationRate: Long.t option
        [@ocaml.doc
          "Specifies the Amazon EBS Provisioned Rate for Volume Initialization (volume initialization rate), in MiB/s, at which to download the snapshot blocks from Amazon S3 to the replacement root volume. This is also known as volume initialization. Specifying a volume initialization rate ensures that the volume is initialized at a predictable and consistent rate after creation. Omit this parameter if: You want to create the volume using fast snapshot restore. You must specify a snapshot that is enabled for fast snapshot restore. In this case, the volume is fully initialized at creation. If you specify a snapshot that is enabled for fast snapshot restore and a volume initialization rate, the volume will be initialized at the specified rate instead of fast snapshot restore. You want to create a volume that is initialized at the default rate. For more information, see Initialize Amazon EBS volumes in the Amazon EC2 User Guide. Valid range: 100 - 300 MiB/s"]}
    let context_ = "CreateReplaceRootVolumeTaskRequest"
    let make ?snapshotId =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ?tagSpecifications ->
            fun ?imageId ->
              fun ?deleteReplacedRootVolume ->
                fun ?volumeInitializationRate ->
                  fun ~instanceId ->
                    fun () ->
                      {
                        snapshotId;
                        clientToken;
                        dryRun;
                        tagSpecifications;
                        imageId;
                        deleteReplacedRootVolume;
                        volumeInitializationRate;
                        instanceId
                      }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("SnapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ImageId", (Option.map x.imageId ~f:ImageId.to_value));
        ("DeleteReplacedRootVolume",
          (Option.map x.deleteReplacedRootVolume ~f:Boolean.to_value));
        ("VolumeInitializationRate",
          (Option.map x.volumeInitializationRate ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeInitializationRate =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "VolumeInitializationRate") in
      let deleteReplacedRootVolume =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DeleteReplacedRootVolume") in
      let imageId =
        (Option.map ~f:ImageId.of_xml) (Xml.child xml_arg0 "ImageId") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "SnapshotId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?volumeInitializationRate ?deleteReplacedRootVolume ?imageId
        ?tagSpecifications ?dryRun ?clientToken ?snapshotId ~instanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let volumeInitializationRate =
        field_map json__ "VolumeInitializationRate" Long.of_json in
      let deleteReplacedRootVolume =
        field_map json__ "DeleteReplacedRootVolume" Boolean.of_json in
      let imageId = field_map json__ "ImageId" ImageId.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let snapshotId = field_map json__ "SnapshotId" SnapshotId.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      make ?volumeInitializationRate ?deleteReplacedRootVolume ?imageId
        ?tagSpecifications ?dryRun ?clientToken ?snapshotId ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Replaces the EBS-backed root volume for a running instance with a new volume that is restored to the original root volume's launch state, that is restored to a specific snapshot taken from the original root volume, or that is restored from an AMI that has the same key characteristics as that of the instance. For more information, see Replace a root volume in the Amazon EC2 User Guide."]
module CreatePublicIpv4PoolResult =
  struct
    type nonrec t =
      {
      poolId: Ipv4PoolEc2Id.t option
        [@ocaml.doc "The ID of the public IPv4 pool."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?poolId = fun () -> { poolId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("poolId", (Option.map x.poolId ~f:Ipv4PoolEc2Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let poolId =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml) (Xml.child xml_arg0 "poolId") in
      make ?poolId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let poolId = field_map json__ "PoolId" Ipv4PoolEc2Id.of_json in
      make ?poolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a public IPv4 address pool. A public IPv4 pool is an EC2 IP address pool required for the public IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses you bring to Amazon Web Services, however, use IPAM pools only. To monitor the status of pool creation, use DescribePublicIpv4Pools."]
module CreatePublicIpv4PoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."];
      networkBorderGroup: String_.t option
        [@ocaml.doc
          "The Availability Zone (AZ) or Local Zone (LZ) network border group that the resource that the IP address is assigned to is in. Defaults to an AZ network border group. For more information on available Local Zones, see Local Zone availability in the Amazon EC2 User Guide."]}
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ?networkBorderGroup ->
          fun () -> { dryRun; tagSpecifications; networkBorderGroup }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("NetworkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkBorderGroup") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?networkBorderGroup ?tagSpecifications ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkBorderGroup =
        field_map json__ "NetworkBorderGroup" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?networkBorderGroup ?tagSpecifications ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a public IPv4 address pool. A public IPv4 pool is an EC2 IP address pool required for the public IPv4 CIDRs that you own and bring to Amazon Web Services to manage with IPAM. IPv6 addresses you bring to Amazon Web Services, however, use IPAM pools only. To monitor the status of pool creation, use DescribePublicIpv4Pools."]
module CreatePlacementGroupResult =
  struct
    type nonrec t =
      {
      placementGroup: PlacementGroup.t option
        [@ocaml.doc "Information about the placement group."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?placementGroup = fun () -> { placementGroup }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("placementGroup",
           (Option.map x.placementGroup ~f:PlacementGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementGroup =
        (Option.map ~f:PlacementGroup.of_xml)
          (Xml.child xml_arg0 "placementGroup") in
      make ?placementGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let placementGroup =
        field_map json__ "PlacementGroup" PlacementGroup.of_json in
      make ?placementGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a placement group in which to launch instances. The strategy of the placement group determines how the instances are organized within the group. A cluster placement group is a logical grouping of instances within a single Availability Zone that benefit from low network latency, high network throughput. A spread placement group places instances on distinct hardware. A partition placement group places groups of instances in different partitions, where instances in one partition do not share the same hardware with instances in another partition. For more information, see Placement groups in the Amazon EC2 User Guide."]
module CreatePlacementGroupRequest =
  struct
    type nonrec t =
      {
      partitionCount: Integer.t option
        [@ocaml.doc
          "The number of partitions. Valid only when Strategy is set to partition."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the new placement group."];
      spreadLevel: SpreadLevel.t option
        [@ocaml.doc
          "Determines how placement groups spread instances. Host \226\128\147 You can use host only with Outpost placement groups. Rack \226\128\147 No usage restrictions."];
      linkedGroupId: PlacementGroupId.t option
        [@ocaml.doc "Reserved for future use."];
      operator: OperatorRequest.t option
        [@ocaml.doc "Reserved for internal use."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupName: String_.t option
        [@ocaml.doc
          "A name for the placement group. Must be unique within the scope of your account for the Region. Constraints: Up to 255 ASCII characters"];
      strategy: PlacementStrategy.t option
        [@ocaml.doc "The placement strategy."]}
    let make ?partitionCount =
      fun ?tagSpecifications ->
        fun ?spreadLevel ->
          fun ?linkedGroupId ->
            fun ?operator ->
              fun ?dryRun ->
                fun ?groupName ->
                  fun ?strategy ->
                    fun () ->
                      {
                        partitionCount;
                        tagSpecifications;
                        spreadLevel;
                        linkedGroupId;
                        operator;
                        dryRun;
                        groupName;
                        strategy
                      }
    let to_value x =
      structure_to_value
        [("PartitionCount",
           (Option.map x.partitionCount ~f:Integer.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SpreadLevel", (Option.map x.spreadLevel ~f:SpreadLevel.to_value));
        ("LinkedGroupId",
          (Option.map x.linkedGroupId ~f:PlacementGroupId.to_value));
        ("Operator", (Option.map x.operator ~f:OperatorRequest.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupName", (Option.map x.groupName ~f:String_.to_value));
        ("strategy", (Option.map x.strategy ~f:PlacementStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let strategy =
        (Option.map ~f:PlacementStrategy.of_xml)
          (Xml.child xml_arg0 "strategy") in
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let operator =
        (Option.map ~f:OperatorRequest.of_xml)
          (Xml.child xml_arg0 "Operator") in
      let linkedGroupId =
        (Option.map ~f:PlacementGroupId.of_xml)
          (Xml.child xml_arg0 "LinkedGroupId") in
      let spreadLevel =
        (Option.map ~f:SpreadLevel.of_xml) (Xml.child xml_arg0 "SpreadLevel") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let partitionCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PartitionCount") in
      make ?strategy ?groupName ?dryRun ?operator ?linkedGroupId ?spreadLevel
        ?tagSpecifications ?partitionCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let strategy = field_map json__ "Strategy" PlacementStrategy.of_json in
      let groupName = field_map json__ "GroupName" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let operator = field_map json__ "Operator" OperatorRequest.of_json in
      let linkedGroupId =
        field_map json__ "LinkedGroupId" PlacementGroupId.of_json in
      let spreadLevel = field_map json__ "SpreadLevel" SpreadLevel.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let partitionCount = field_map json__ "PartitionCount" Integer.of_json in
      make ?strategy ?groupName ?dryRun ?operator ?linkedGroupId ?spreadLevel
        ?tagSpecifications ?partitionCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a placement group in which to launch instances. The strategy of the placement group determines how the instances are organized within the group. A cluster placement group is a logical grouping of instances within a single Availability Zone that benefit from low network latency, high network throughput. A spread placement group places instances on distinct hardware. A partition placement group places groups of instances in different partitions, where instances in one partition do not share the same hardware with instances in another partition. For more information, see Placement groups in the Amazon EC2 User Guide."]
module CreateNetworkInterfaceResult =
  struct
    type nonrec t =
      {
      networkInterface: NetworkInterface.t option
        [@ocaml.doc "Information about the network interface."];
      clientToken: String_.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterface =
      fun ?clientToken -> fun () -> { networkInterface; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterface",
           (Option.map x.networkInterface ~f:NetworkInterface.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let networkInterface =
        (Option.map ~f:NetworkInterface.of_xml)
          (Xml.child xml_arg0 "networkInterface") in
      make ?clientToken ?networkInterface ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let networkInterface =
        field_map json__ "NetworkInterface" NetworkInterface.of_json in
      make ?clientToken ?networkInterface ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a network interface in the specified subnet. The number of IP addresses you can assign to a network interface varies by instance type. For more information about network interfaces, see Elastic network interfaces in the Amazon EC2 User Guide."]
module CreateNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      ipv4Prefixes: Ipv4PrefixList.t option
        [@ocaml.doc
          "The IPv4 prefixes assigned to the network interface. You can't specify IPv4 prefixes if you've specified one of the following: a count of IPv4 prefixes, specific private IPv4 addresses, or a count of private IPv4 addresses."];
      ipv4PrefixCount: Integer.t option
        [@ocaml.doc
          "The number of IPv4 prefixes that Amazon Web Services automatically assigns to the network interface. You can't specify a count of IPv4 prefixes if you've specified one of the following: specific IPv4 prefixes, specific private IPv4 addresses, or a count of private IPv4 addresses."];
      ipv6Prefixes: Ipv6PrefixList.t option
        [@ocaml.doc
          "The IPv6 prefixes assigned to the network interface. You can't specify IPv6 prefixes if you've specified one of the following: a count of IPv6 prefixes, specific IPv6 addresses, or a count of IPv6 addresses."];
      ipv6PrefixCount: Integer.t option
        [@ocaml.doc
          "The number of IPv6 prefixes that Amazon Web Services automatically assigns to the network interface. You can't specify a count of IPv6 prefixes if you've specified one of the following: specific IPv6 prefixes, specific IPv6 addresses, or a count of IPv6 addresses."];
      interfaceType: NetworkInterfaceCreationType.t option
        [@ocaml.doc
          "The type of network interface. The default is interface. If you specify efa-only, do not assign any IP addresses to the network interface. EFA-only network interfaces do not support IP addresses. The only supported values are interface, efa, efa-only, and trunk."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the new network interface."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      enablePrimaryIpv6: Boolean.t option
        [@ocaml.doc
          "If you\226\128\153re creating a network interface in a dual-stack or IPv6-only subnet, you have the option to assign a primary IPv6 IP address. A primary IPv6 address is an IPv6 GUA address associated with an ENI that you have enabled to use a primary IPv6 address. Use this option if the instance that this ENI will be attached to relies on its IPv6 address not changing. Amazon Web Services will automatically assign an IPv6 address associated with the ENI attached to your instance to be the primary IPv6 address. Once you enable an IPv6 GUA address to be a primary IPv6, you cannot disable it. When you enable an IPv6 GUA address to be a primary IPv6, the first IPv6 GUA will be made the primary IPv6 address until the instance is terminated or the network interface is detached. If you have multiple IPv6 addresses associated with an ENI attached to your instance and you enable a primary IPv6 address, the first IPv6 GUA address associated with the ENI becomes the primary IPv6 address."];
      connectionTrackingSpecification:
        ConnectionTrackingSpecificationRequest.t option
        [@ocaml.doc
          "A connection tracking specification for the network interface."];
      operator: OperatorRequest.t option
        [@ocaml.doc "Reserved for internal use."];
      subnetId: SubnetId.t
        [@ocaml.doc
          "The ID of the subnet to associate with the network interface."];
      description: String_.t option
        [@ocaml.doc "A description for the network interface."];
      privateIpAddress: String_.t option
        [@ocaml.doc
          "The primary private IPv4 address of the network interface. If you don't specify an IPv4 address, Amazon EC2 selects one for you from the subnet's IPv4 CIDR range. If you specify an IP address, you cannot indicate any IP addresses specified in privateIpAddresses as primary (only one IP address can be designated as primary)."];
      groups: SecurityGroupIdStringList.t option
        [@ocaml.doc "The IDs of the security groups."];
      privateIpAddresses: PrivateIpAddressSpecificationList.t option
        [@ocaml.doc
          "The private IPv4 addresses. You can't specify private IPv4 addresses if you've specified one of the following: a count of private IPv4 addresses, specific IPv4 prefixes, or a count of IPv4 prefixes."];
      secondaryPrivateIpAddressCount: Integer.t option
        [@ocaml.doc
          "The number of secondary private IPv4 addresses to assign to a network interface. When you specify a number of secondary IPv4 addresses, Amazon EC2 selects these IP addresses within the subnet's IPv4 CIDR range. You can't specify this option and specify more than one private IP address using privateIpAddresses. You can't specify a count of private IPv4 addresses if you've specified one of the following: specific private IPv4 addresses, specific IPv4 prefixes, or a count of IPv4 prefixes."];
      ipv6Addresses: InstanceIpv6AddressList.t option
        [@ocaml.doc
          "The IPv6 addresses from the IPv6 CIDR block range of your subnet. You can't specify IPv6 addresses using this parameter if you've specified one of the following: a count of IPv6 addresses, specific IPv6 prefixes, or a count of IPv6 prefixes."];
      ipv6AddressCount: Integer.t option
        [@ocaml.doc
          "The number of IPv6 addresses to assign to a network interface. Amazon EC2 automatically selects the IPv6 addresses from the subnet range. You can't specify a count of IPv6 addresses using this parameter if you've specified one of the following: specific IPv6 addresses, specific IPv6 prefixes, or a count of IPv6 prefixes. If your subnet has the AssignIpv6AddressOnCreation attribute set, you can override that setting by specifying 0 as the IPv6 address count."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateNetworkInterfaceRequest"
    let make ?ipv4Prefixes =
      fun ?ipv4PrefixCount ->
        fun ?ipv6Prefixes ->
          fun ?ipv6PrefixCount ->
            fun ?interfaceType ->
              fun ?tagSpecifications ->
                fun ?clientToken ->
                  fun ?enablePrimaryIpv6 ->
                    fun ?connectionTrackingSpecification ->
                      fun ?operator ->
                        fun ?description ->
                          fun ?privateIpAddress ->
                            fun ?groups ->
                              fun ?privateIpAddresses ->
                                fun ?secondaryPrivateIpAddressCount ->
                                  fun ?ipv6Addresses ->
                                    fun ?ipv6AddressCount ->
                                      fun ?dryRun ->
                                        fun ~subnetId ->
                                          fun () ->
                                            {
                                              ipv4Prefixes;
                                              ipv4PrefixCount;
                                              ipv6Prefixes;
                                              ipv6PrefixCount;
                                              interfaceType;
                                              tagSpecifications;
                                              clientToken;
                                              enablePrimaryIpv6;
                                              connectionTrackingSpecification;
                                              operator;
                                              description;
                                              privateIpAddress;
                                              groups;
                                              privateIpAddresses;
                                              secondaryPrivateIpAddressCount;
                                              ipv6Addresses;
                                              ipv6AddressCount;
                                              dryRun;
                                              subnetId
                                            }
    let to_value x =
      structure_to_value
        [("Ipv4Prefix",
           (Option.map x.ipv4Prefixes ~f:Ipv4PrefixList.to_value));
        ("Ipv4PrefixCount",
          (Option.map x.ipv4PrefixCount ~f:Integer.to_value));
        ("Ipv6Prefix",
          (Option.map x.ipv6Prefixes ~f:Ipv6PrefixList.to_value));
        ("Ipv6PrefixCount",
          (Option.map x.ipv6PrefixCount ~f:Integer.to_value));
        ("InterfaceType",
          (Option.map x.interfaceType
             ~f:NetworkInterfaceCreationType.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("EnablePrimaryIpv6",
          (Option.map x.enablePrimaryIpv6 ~f:Boolean.to_value));
        ("ConnectionTrackingSpecification",
          (Option.map x.connectionTrackingSpecification
             ~f:ConnectionTrackingSpecificationRequest.to_value));
        ("Operator", (Option.map x.operator ~f:OperatorRequest.to_value));
        ("subnetId", (Some (SubnetId.to_value x.subnetId)));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("privateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("SecurityGroupId",
          (Option.map x.groups ~f:SecurityGroupIdStringList.to_value));
        ("privateIpAddresses",
          (Option.map x.privateIpAddresses
             ~f:PrivateIpAddressSpecificationList.to_value));
        ("secondaryPrivateIpAddressCount",
          (Option.map x.secondaryPrivateIpAddressCount ~f:Integer.to_value));
        ("ipv6Addresses",
          (Option.map x.ipv6Addresses ~f:InstanceIpv6AddressList.to_value));
        ("ipv6AddressCount",
          (Option.map x.ipv6AddressCount ~f:Integer.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let ipv6AddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ipv6AddressCount") in
      let ipv6Addresses =
        (Option.map ~f:InstanceIpv6AddressList.of_xml)
          (Xml.child xml_arg0 "ipv6Addresses") in
      let secondaryPrivateIpAddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "secondaryPrivateIpAddressCount") in
      let privateIpAddresses =
        (Option.map ~f:PrivateIpAddressSpecificationList.of_xml)
          (Xml.child xml_arg0 "privateIpAddresses") in
      let groups =
        (Option.map ~f:SecurityGroupIdStringList.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subnetId") in
      let operator =
        (Option.map ~f:OperatorRequest.of_xml)
          (Xml.child xml_arg0 "Operator") in
      let connectionTrackingSpecification =
        (Option.map ~f:ConnectionTrackingSpecificationRequest.of_xml)
          (Xml.child xml_arg0 "ConnectionTrackingSpecification") in
      let enablePrimaryIpv6 =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnablePrimaryIpv6") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let interfaceType =
        (Option.map ~f:NetworkInterfaceCreationType.of_xml)
          (Xml.child xml_arg0 "InterfaceType") in
      let ipv6PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv6PrefixCount") in
      let ipv6Prefixes =
        (Option.map ~f:Ipv6PrefixList.of_xml)
          (Xml.child xml_arg0 "Ipv6Prefix") in
      let ipv4PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv4PrefixCount") in
      let ipv4Prefixes =
        (Option.map ~f:Ipv4PrefixList.of_xml)
          (Xml.child xml_arg0 "Ipv4Prefix") in
      make ?dryRun ?ipv6AddressCount ?ipv6Addresses
        ?secondaryPrivateIpAddressCount ?privateIpAddresses ?groups
        ?privateIpAddress ?description ~subnetId ?operator
        ?connectionTrackingSpecification ?enablePrimaryIpv6 ?clientToken
        ?tagSpecifications ?interfaceType ?ipv6PrefixCount ?ipv6Prefixes
        ?ipv4PrefixCount ?ipv4Prefixes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let ipv6AddressCount =
        field_map json__ "Ipv6AddressCount" Integer.of_json in
      let ipv6Addresses =
        field_map json__ "Ipv6Addresses" InstanceIpv6AddressList.of_json in
      let secondaryPrivateIpAddressCount =
        field_map json__ "SecondaryPrivateIpAddressCount" Integer.of_json in
      let privateIpAddresses =
        field_map json__ "PrivateIpAddresses"
          PrivateIpAddressSpecificationList.of_json in
      let groups =
        field_map json__ "Groups" SecurityGroupIdStringList.of_json in
      let privateIpAddress =
        field_map json__ "PrivateIpAddress" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let subnetId = field_map_exn json__ "SubnetId" SubnetId.of_json in
      let operator = field_map json__ "Operator" OperatorRequest.of_json in
      let connectionTrackingSpecification =
        field_map json__ "ConnectionTrackingSpecification"
          ConnectionTrackingSpecificationRequest.of_json in
      let enablePrimaryIpv6 =
        field_map json__ "EnablePrimaryIpv6" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let interfaceType =
        field_map json__ "InterfaceType" NetworkInterfaceCreationType.of_json in
      let ipv6PrefixCount =
        field_map json__ "Ipv6PrefixCount" Integer.of_json in
      let ipv6Prefixes =
        field_map json__ "Ipv6Prefixes" Ipv6PrefixList.of_json in
      let ipv4PrefixCount =
        field_map json__ "Ipv4PrefixCount" Integer.of_json in
      let ipv4Prefixes =
        field_map json__ "Ipv4Prefixes" Ipv4PrefixList.of_json in
      make ?dryRun ?ipv6AddressCount ?ipv6Addresses
        ?secondaryPrivateIpAddressCount ?privateIpAddresses ?groups
        ?privateIpAddress ?description ~subnetId ?operator
        ?connectionTrackingSpecification ?enablePrimaryIpv6 ?clientToken
        ?tagSpecifications ?interfaceType ?ipv6PrefixCount ?ipv6Prefixes
        ?ipv4PrefixCount ?ipv4Prefixes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a network interface in the specified subnet. The number of IP addresses you can assign to a network interface varies by instance type. For more information about network interfaces, see Elastic network interfaces in the Amazon EC2 User Guide."]
module CreateNetworkInterfacePermissionResult =
  struct
    type nonrec t =
      {
      interfacePermission: NetworkInterfacePermission.t option
        [@ocaml.doc
          "Information about the permission for the network interface."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?interfacePermission = fun () -> { interfacePermission }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("interfacePermission",
           (Option.map x.interfacePermission
              ~f:NetworkInterfacePermission.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let interfacePermission =
        (Option.map ~f:NetworkInterfacePermission.of_xml)
          (Xml.child xml_arg0 "interfacePermission") in
      make ?interfacePermission ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let interfacePermission =
        field_map json__ "InterfacePermission"
          NetworkInterfacePermission.of_json in
      make ?interfacePermission ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateNetworkInterfacePermission."]
module CreateNetworkInterfacePermissionRequest =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      awsAccountId: String_.t option
        [@ocaml.doc "The Amazon Web Services account ID."];
      awsService: String_.t option
        [@ocaml.doc
          "The Amazon Web Services service. Currently not supported."];
      permission: InterfacePermissionType.t
        [@ocaml.doc "The type of permission to grant."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateNetworkInterfacePermissionRequest"
    let make ?awsAccountId =
      fun ?awsService ->
        fun ?dryRun ->
          fun ~networkInterfaceId ->
            fun ~permission ->
              fun () ->
                {
                  awsAccountId;
                  awsService;
                  dryRun;
                  networkInterfaceId;
                  permission
                }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("AwsAccountId", (Option.map x.awsAccountId ~f:String_.to_value));
        ("AwsService", (Option.map x.awsService ~f:String_.to_value));
        ("Permission",
          (Some (InterfacePermissionType.to_value x.permission)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let permission =
        InterfacePermissionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Permission") in
      let awsService =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AwsService") in
      let awsAccountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AwsAccountId") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NetworkInterfaceId") in
      make ?dryRun ~permission ?awsService ?awsAccountId ~networkInterfaceId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let permission =
        field_map_exn json__ "Permission" InterfacePermissionType.of_json in
      let awsService = field_map json__ "AwsService" String_.of_json in
      let awsAccountId = field_map json__ "AwsAccountId" String_.of_json in
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?dryRun ~permission ?awsService ?awsAccountId ~networkInterfaceId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for CreateNetworkInterfacePermission."]
module CreateNetworkInsightsPathResult =
  struct
    type nonrec t =
      {
      networkInsightsPath: NetworkInsightsPath.t option
        [@ocaml.doc "Information about the path."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsPath = fun () -> { networkInsightsPath }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsPath",
           (Option.map x.networkInsightsPath ~f:NetworkInsightsPath.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsPath =
        (Option.map ~f:NetworkInsightsPath.of_xml)
          (Xml.child xml_arg0 "networkInsightsPath") in
      make ?networkInsightsPath ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsPath =
        field_map json__ "NetworkInsightsPath" NetworkInsightsPath.of_json in
      make ?networkInsightsPath ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a path to analyze for reachability. Reachability Analyzer enables you to analyze and debug network reachability between two resources in your virtual private cloud (VPC). For more information, see the Reachability Analyzer Guide."]
module CreateNetworkInsightsPathRequest =
  struct
    type nonrec t =
      {
      sourceIp: IpAddress.t option
        [@ocaml.doc "The IP address of the source."];
      destinationIp: IpAddress.t option
        [@ocaml.doc "The IP address of the destination."];
      source: NetworkInsightsResourceId.t
        [@ocaml.doc
          "The ID or ARN of the source. If the resource is in another account, you must specify an ARN."];
      destination: NetworkInsightsResourceId.t option
        [@ocaml.doc
          "The ID or ARN of the destination. If the resource is in another account, you must specify an ARN."];
      protocol: Protocol.t [@ocaml.doc "The protocol."];
      destinationPort: Port.t option [@ocaml.doc "The destination port."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to add to the path."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      filterAtSource: PathRequestFilter.t option
        [@ocaml.doc
          "Scopes the analysis to network paths that match specific filters at the source. If you specify this parameter, you can't specify the parameters for the source IP address or the destination port."];
      filterAtDestination: PathRequestFilter.t option
        [@ocaml.doc
          "Scopes the analysis to network paths that match specific filters at the destination. If you specify this parameter, you can't specify the parameter for the destination IP address."]}
    let context_ = "CreateNetworkInsightsPathRequest"
    let make ?sourceIp =
      fun ?destinationIp ->
        fun ?destination ->
          fun ?destinationPort ->
            fun ?tagSpecifications ->
              fun ?dryRun ->
                fun ?filterAtSource ->
                  fun ?filterAtDestination ->
                    fun ~source ->
                      fun ~protocol ->
                        fun ~clientToken ->
                          fun () ->
                            {
                              sourceIp;
                              destinationIp;
                              destination;
                              destinationPort;
                              tagSpecifications;
                              dryRun;
                              filterAtSource;
                              filterAtDestination;
                              source;
                              protocol;
                              clientToken
                            }
    let to_value x =
      structure_to_value
        [("SourceIp", (Option.map x.sourceIp ~f:IpAddress.to_value));
        ("DestinationIp", (Option.map x.destinationIp ~f:IpAddress.to_value));
        ("Source", (Some (NetworkInsightsResourceId.to_value x.source)));
        ("Destination",
          (Option.map x.destination ~f:NetworkInsightsResourceId.to_value));
        ("Protocol", (Some (Protocol.to_value x.protocol)));
        ("DestinationPort", (Option.map x.destinationPort ~f:Port.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Some (String_.to_value x.clientToken)));
        ("FilterAtSource",
          (Option.map x.filterAtSource ~f:PathRequestFilter.to_value));
        ("FilterAtDestination",
          (Option.map x.filterAtDestination ~f:PathRequestFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filterAtDestination =
        (Option.map ~f:PathRequestFilter.of_xml)
          (Xml.child xml_arg0 "FilterAtDestination") in
      let filterAtSource =
        (Option.map ~f:PathRequestFilter.of_xml)
          (Xml.child xml_arg0 "FilterAtSource") in
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let destinationPort =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "DestinationPort") in
      let protocol =
        Protocol.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Protocol") in
      let destination =
        (Option.map ~f:NetworkInsightsResourceId.of_xml)
          (Xml.child xml_arg0 "Destination") in
      let source =
        NetworkInsightsResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Source") in
      let destinationIp =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "DestinationIp") in
      let sourceIp =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "SourceIp") in
      make ?filterAtDestination ?filterAtSource ~clientToken ?dryRun
        ?tagSpecifications ?destinationPort ~protocol ?destination ~source
        ?destinationIp ?sourceIp ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filterAtDestination =
        field_map json__ "FilterAtDestination" PathRequestFilter.of_json in
      let filterAtSource =
        field_map json__ "FilterAtSource" PathRequestFilter.of_json in
      let clientToken = field_map_exn json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let destinationPort = field_map json__ "DestinationPort" Port.of_json in
      let protocol = field_map_exn json__ "Protocol" Protocol.of_json in
      let destination =
        field_map json__ "Destination" NetworkInsightsResourceId.of_json in
      let source =
        field_map_exn json__ "Source" NetworkInsightsResourceId.of_json in
      let destinationIp = field_map json__ "DestinationIp" IpAddress.of_json in
      let sourceIp = field_map json__ "SourceIp" IpAddress.of_json in
      make ?filterAtDestination ?filterAtSource ~clientToken ?dryRun
        ?tagSpecifications ?destinationPort ~protocol ?destination ~source
        ?destinationIp ?sourceIp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a path to analyze for reachability. Reachability Analyzer enables you to analyze and debug network reachability between two resources in your virtual private cloud (VPC). For more information, see the Reachability Analyzer Guide."]
module CreateNetworkInsightsAccessScopeResult =
  struct
    type nonrec t =
      {
      networkInsightsAccessScope: NetworkInsightsAccessScope.t option
        [@ocaml.doc "The Network Access Scope."];
      networkInsightsAccessScopeContent:
        NetworkInsightsAccessScopeContent.t option
        [@ocaml.doc "The Network Access Scope content."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInsightsAccessScope =
      fun ?networkInsightsAccessScopeContent ->
        fun () ->
          { networkInsightsAccessScope; networkInsightsAccessScopeContent }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInsightsAccessScope",
           (Option.map x.networkInsightsAccessScope
              ~f:NetworkInsightsAccessScope.to_value));
        ("networkInsightsAccessScopeContent",
          (Option.map x.networkInsightsAccessScopeContent
             ~f:NetworkInsightsAccessScopeContent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInsightsAccessScopeContent =
        (Option.map ~f:NetworkInsightsAccessScopeContent.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScopeContent") in
      let networkInsightsAccessScope =
        (Option.map ~f:NetworkInsightsAccessScope.of_xml)
          (Xml.child xml_arg0 "networkInsightsAccessScope") in
      make ?networkInsightsAccessScopeContent ?networkInsightsAccessScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInsightsAccessScopeContent =
        field_map json__ "NetworkInsightsAccessScopeContent"
          NetworkInsightsAccessScopeContent.of_json in
      let networkInsightsAccessScope =
        field_map json__ "NetworkInsightsAccessScope"
          NetworkInsightsAccessScope.of_json in
      make ?networkInsightsAccessScopeContent ?networkInsightsAccessScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Network Access Scope. Amazon Web Services Network Access Analyzer enables cloud networking and cloud operations teams to verify that their networks on Amazon Web Services conform to their network security and governance objectives. For more information, see the Amazon Web Services Network Access Analyzer Guide."]
module CreateNetworkInsightsAccessScopeRequest =
  struct
    type nonrec t =
      {
      matchPaths: AccessScopePathListRequest.t option
        [@ocaml.doc "The paths to match."];
      excludePaths: AccessScopePathListRequest.t option
        [@ocaml.doc "The paths to exclude."];
      clientToken: String_.t
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateNetworkInsightsAccessScopeRequest"
    let make ?matchPaths =
      fun ?excludePaths ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ~clientToken ->
              fun () ->
                {
                  matchPaths;
                  excludePaths;
                  tagSpecifications;
                  dryRun;
                  clientToken
                }
    let to_value x =
      structure_to_value
        [("MatchPath",
           (Option.map x.matchPaths ~f:AccessScopePathListRequest.to_value));
        ("ExcludePath",
          (Option.map x.excludePaths ~f:AccessScopePathListRequest.to_value));
        ("ClientToken", (Some (String_.to_value x.clientToken)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientToken") in
      let excludePaths =
        (Option.map ~f:AccessScopePathListRequest.of_xml)
          (Xml.child xml_arg0 "ExcludePath") in
      let matchPaths =
        (Option.map ~f:AccessScopePathListRequest.of_xml)
          (Xml.child xml_arg0 "MatchPath") in
      make ?dryRun ?tagSpecifications ~clientToken ?excludePaths ?matchPaths
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map_exn json__ "ClientToken" String_.of_json in
      let excludePaths =
        field_map json__ "ExcludePaths" AccessScopePathListRequest.of_json in
      let matchPaths =
        field_map json__ "MatchPaths" AccessScopePathListRequest.of_json in
      make ?dryRun ?tagSpecifications ~clientToken ?excludePaths ?matchPaths
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Network Access Scope. Amazon Web Services Network Access Analyzer enables cloud networking and cloud operations teams to verify that their networks on Amazon Web Services conform to their network security and governance objectives. For more information, see the Amazon Web Services Network Access Analyzer Guide."]
module CreateNetworkAclResult =
  struct
    type nonrec t =
      {
      networkAcl: NetworkAcl.t option
        [@ocaml.doc "Information about the network ACL."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkAcl =
      fun ?clientToken -> fun () -> { networkAcl; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkAcl", (Option.map x.networkAcl ~f:NetworkAcl.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let networkAcl =
        (Option.map ~f:NetworkAcl.of_xml) (Xml.child xml_arg0 "networkAcl") in
      make ?clientToken ?networkAcl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let networkAcl = field_map json__ "NetworkAcl" NetworkAcl.of_json in
      make ?clientToken ?networkAcl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a network ACL in a VPC. Network ACLs provide an optional layer of security (in addition to security groups) for the instances in your VPC. For more information, see Network ACLs in the Amazon VPC User Guide."]
module CreateNetworkAclRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the network ACL."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."]}
    let context_ = "CreateNetworkAclRequest"
    let make ?tagSpecifications =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ~vpcId ->
            fun () -> { tagSpecifications; clientToken; dryRun; vpcId }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ~vpcId ?dryRun ?clientToken ?tagSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ~vpcId ?dryRun ?clientToken ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a network ACL in a VPC. Network ACLs provide an optional layer of security (in addition to security groups) for the instances in your VPC. For more information, see Network ACLs in the Amazon VPC User Guide."]
module CreateNetworkAclEntryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkAclId: NetworkAclId.t [@ocaml.doc "The ID of the network ACL."];
      ruleNumber: Integer.t
        [@ocaml.doc
          "The rule number for the entry (for example, 100). ACL entries are processed in ascending order by rule number. Constraints: Positive integer from 1 to 32766. The range 32767 to 65535 is reserved for internal use."];
      protocol: String_.t
        [@ocaml.doc
          "The protocol number. A value of \"-1\" means all protocols. If you specify \"-1\" or a protocol number other than \"6\" (TCP), \"17\" (UDP), or \"1\" (ICMP), traffic on all ports is allowed, regardless of any ports or ICMP types or codes that you specify. If you specify protocol \"58\" (ICMPv6) and specify an IPv4 CIDR block, traffic for all ICMP types and codes allowed, regardless of any that you specify. If you specify protocol \"58\" (ICMPv6) and specify an IPv6 CIDR block, you must specify an ICMP type and code."];
      ruleAction: RuleAction.t
        [@ocaml.doc
          "Indicates whether to allow or deny the traffic that matches the rule."];
      egress: Boolean.t
        [@ocaml.doc
          "Indicates whether this is an egress rule (rule is applied to traffic leaving the subnet)."];
      cidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 network range to allow or deny, in CIDR notation (for example 172.16.0.0/24). We modify the specified CIDR block to its canonical form; for example, if you specify 100.68.0.18/18, we modify it to 100.68.0.0/18."];
      ipv6CidrBlock: String_.t option
        [@ocaml.doc
          "The IPv6 network range to allow or deny, in CIDR notation (for example 2001:db8:1234:1a00::/64)."];
      icmpTypeCode: IcmpTypeCode.t option
        [@ocaml.doc
          "ICMP protocol: The ICMP or ICMPv6 type and code. Required if specifying protocol 1 (ICMP) or protocol 58 (ICMPv6) with an IPv6 CIDR block."];
      portRange: PortRange.t option
        [@ocaml.doc
          "TCP or UDP protocols: The range of ports the rule applies to. Required if specifying protocol 6 (TCP) or 17 (UDP)."]}
    let context_ = "CreateNetworkAclEntryRequest"
    let make ?dryRun =
      fun ?cidrBlock ->
        fun ?ipv6CidrBlock ->
          fun ?icmpTypeCode ->
            fun ?portRange ->
              fun ~networkAclId ->
                fun ~ruleNumber ->
                  fun ~protocol ->
                    fun ~ruleAction ->
                      fun ~egress ->
                        fun () ->
                          {
                            dryRun;
                            cidrBlock;
                            ipv6CidrBlock;
                            icmpTypeCode;
                            portRange;
                            networkAclId;
                            ruleNumber;
                            protocol;
                            ruleAction;
                            egress
                          }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkAclId", (Some (NetworkAclId.to_value x.networkAclId)));
        ("ruleNumber", (Some (Integer.to_value x.ruleNumber)));
        ("protocol", (Some (String_.to_value x.protocol)));
        ("ruleAction", (Some (RuleAction.to_value x.ruleAction)));
        ("egress", (Some (Boolean.to_value x.egress)));
        ("cidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("Icmp", (Option.map x.icmpTypeCode ~f:IcmpTypeCode.to_value));
        ("portRange", (Option.map x.portRange ~f:PortRange.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portRange =
        (Option.map ~f:PortRange.of_xml) (Xml.child xml_arg0 "portRange") in
      let icmpTypeCode =
        (Option.map ~f:IcmpTypeCode.of_xml) (Xml.child xml_arg0 "Icmp") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipv6CidrBlock") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cidrBlock") in
      let egress =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "egress") in
      let ruleAction =
        RuleAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleAction") in
      let protocol =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "protocol") in
      let ruleNumber =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ruleNumber") in
      let networkAclId =
        NetworkAclId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkAclId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?portRange ?icmpTypeCode ?ipv6CidrBlock ?cidrBlock ~egress
        ~ruleAction ~protocol ~ruleNumber ~networkAclId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let portRange = field_map json__ "PortRange" PortRange.of_json in
      let icmpTypeCode = field_map json__ "IcmpTypeCode" IcmpTypeCode.of_json in
      let ipv6CidrBlock = field_map json__ "Ipv6CidrBlock" String_.of_json in
      let cidrBlock = field_map json__ "CidrBlock" String_.of_json in
      let egress = field_map_exn json__ "Egress" Boolean.of_json in
      let ruleAction = field_map_exn json__ "RuleAction" RuleAction.of_json in
      let protocol = field_map_exn json__ "Protocol" String_.of_json in
      let ruleNumber = field_map_exn json__ "RuleNumber" Integer.of_json in
      let networkAclId =
        field_map_exn json__ "NetworkAclId" NetworkAclId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?portRange ?icmpTypeCode ?ipv6CidrBlock ?cidrBlock ~egress
        ~ruleAction ~protocol ~ruleNumber ~networkAclId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an entry (a rule) in a network ACL with the specified rule number. Each network ACL has a set of numbered ingress rules and a separate set of numbered egress rules. When determining whether a packet should be allowed in or out of a subnet associated with the ACL, we process the entries in the ACL according to the rule numbers, in ascending order. Each network ACL has a set of ingress rules and a separate set of egress rules. We recommend that you leave room between the rule numbers (for example, 100, 110, 120, ...), and not number them one right after the other (for example, 101, 102, 103, ...). This makes it easier to add a rule between existing ones without having to renumber the rules. After you add an entry, you can't modify it; you must either replace it, or create an entry and delete the old one. For more information about network ACLs, see Network ACLs in the Amazon VPC User Guide."]
module CreateNatGatewayResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier to ensure the idempotency of the request. Only returned if a client token was provided in the request."];
      natGateway: NatGateway.t option
        [@ocaml.doc "Information about the NAT gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?natGateway -> fun () -> { clientToken; natGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("natGateway", (Option.map x.natGateway ~f:NatGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGateway =
        (Option.map ~f:NatGateway.of_xml) (Xml.child xml_arg0 "natGateway") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?natGateway ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGateway = field_map json__ "NatGateway" NatGateway.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?natGateway ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a NAT gateway in the specified subnet. This action creates a network interface in the specified subnet with a private IP address from the IP address range of the subnet. You can create either a public NAT gateway or a private NAT gateway. With a public NAT gateway, internet-bound traffic from a private subnet can be routed to the NAT gateway, so that instances in a private subnet can connect to the internet. With a private NAT gateway, private communication is routed across VPCs and on-premises networks through a transit gateway or virtual private gateway. Common use cases include running large workloads behind a small pool of allowlisted IPv4 addresses, preserving private IPv4 addresses, and communicating between overlapping networks. For more information, see NAT gateways in the Amazon VPC User Guide. When you create a public NAT gateway and assign it an EIP or secondary EIPs, the network border group of the EIPs must match the network border group of the Availability Zone (AZ) that the public NAT gateway is in. If it's not the same, the NAT gateway will fail to launch. You can see the network border group for the subnet's AZ by viewing the details of the subnet. Similarly, you can view the network border group of an EIP by viewing the details of the EIP address. For more information about network border groups and EIPs, see Allocate an Elastic IP address in the Amazon VPC User Guide."]
module CreateNatGatewayRequest =
  struct
    type nonrec t =
      {
      availabilityMode: AvailabilityMode.t option
        [@ocaml.doc
          "Specifies whether to create a zonal (single-AZ) or regional (multi-AZ) NAT gateway. Defaults to zonal. A zonal NAT gateway is a NAT Gateway that provides redundancy and scalability within a single availability zone. A regional NAT gateway is a single NAT Gateway that works across multiple availability zones (AZs) in your VPC, providing redundancy, scalability and availability across all the AZs in a Region. For more information, see Regional NAT gateways for automatic multi-AZ expansion in the Amazon VPC User Guide."];
      allocationId: AllocationId.t option
        [@ocaml.doc
          "\\[Public NAT gateways only\\] The allocation ID of an Elastic IP address to associate with the NAT gateway. You cannot specify an Elastic IP address with a private NAT gateway. If the Elastic IP address is associated with another resource, you must first disassociate it."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency. Constraint: Maximum 64 ASCII characters."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      subnetId: SubnetId.t option
        [@ocaml.doc
          "The ID of the subnet in which to create the NAT gateway."];
      vpcId: VpcId.t option
        [@ocaml.doc
          "The ID of the VPC where you want to create a regional NAT gateway."];
      availabilityZoneAddresses: AvailabilityZoneAddresses.t option
        [@ocaml.doc
          "For regional NAT gateways only: Specifies which Availability Zones you want the NAT gateway to support and the Elastic IP addresses (EIPs) to use in each AZ. The regional NAT gateway uses these EIPs to handle outbound NAT traffic from their respective AZs. If not specified, the NAT gateway will automatically expand to new AZs and associate EIPs upon detection of an elastic network interface. If you specify this parameter, auto-expansion is disabled and you must manually manage AZ coverage. A regional NAT gateway is a single NAT Gateway that works across multiple availability zones (AZs) in your VPC, providing redundancy, scalability and availability across all the AZs in a Region. For more information, see Regional NAT gateways for automatic multi-AZ expansion in the Amazon VPC User Guide."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the NAT gateway."];
      connectivityType: ConnectivityType.t option
        [@ocaml.doc
          "Indicates whether the NAT gateway supports public or private connectivity. The default is public connectivity."];
      privateIpAddress: String_.t option
        [@ocaml.doc
          "The private IPv4 address to assign to the NAT gateway. If you don't provide an address, a private IPv4 address will be automatically assigned."];
      secondaryAllocationIds: AllocationIdList.t option
        [@ocaml.doc
          "Secondary EIP allocation IDs. For more information, see Create a NAT gateway in the Amazon VPC User Guide."];
      secondaryPrivateIpAddresses: IpList.t option
        [@ocaml.doc
          "Secondary private IPv4 addresses. For more information about secondary addresses, see Create a NAT gateway in the Amazon VPC User Guide."];
      secondaryPrivateIpAddressCount: PrivateIpAddressCount.t option
        [@ocaml.doc
          "\\[Private NAT gateway only\\] The number of secondary private IPv4 addresses you want to assign to the NAT gateway. For more information about secondary addresses, see Create a NAT gateway in the Amazon VPC User Guide."]}
    let make ?availabilityMode =
      fun ?allocationId ->
        fun ?clientToken ->
          fun ?dryRun ->
            fun ?subnetId ->
              fun ?vpcId ->
                fun ?availabilityZoneAddresses ->
                  fun ?tagSpecifications ->
                    fun ?connectivityType ->
                      fun ?privateIpAddress ->
                        fun ?secondaryAllocationIds ->
                          fun ?secondaryPrivateIpAddresses ->
                            fun ?secondaryPrivateIpAddressCount ->
                              fun () ->
                                {
                                  availabilityMode;
                                  allocationId;
                                  clientToken;
                                  dryRun;
                                  subnetId;
                                  vpcId;
                                  availabilityZoneAddresses;
                                  tagSpecifications;
                                  connectivityType;
                                  privateIpAddress;
                                  secondaryAllocationIds;
                                  secondaryPrivateIpAddresses;
                                  secondaryPrivateIpAddressCount
                                }
    let to_value x =
      structure_to_value
        [("AvailabilityMode",
           (Option.map x.availabilityMode ~f:AvailabilityMode.to_value));
        ("AllocationId",
          (Option.map x.allocationId ~f:AllocationId.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SubnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("AvailabilityZoneAddress",
          (Option.map x.availabilityZoneAddresses
             ~f:AvailabilityZoneAddresses.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ConnectivityType",
          (Option.map x.connectivityType ~f:ConnectivityType.to_value));
        ("PrivateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("SecondaryAllocationId",
          (Option.map x.secondaryAllocationIds ~f:AllocationIdList.to_value));
        ("SecondaryPrivateIpAddress",
          (Option.map x.secondaryPrivateIpAddresses ~f:IpList.to_value));
        ("SecondaryPrivateIpAddressCount",
          (Option.map x.secondaryPrivateIpAddressCount
             ~f:PrivateIpAddressCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondaryPrivateIpAddressCount =
        (Option.map ~f:PrivateIpAddressCount.of_xml)
          (Xml.child xml_arg0 "SecondaryPrivateIpAddressCount") in
      let secondaryPrivateIpAddresses =
        (Option.map ~f:IpList.of_xml)
          (Xml.child xml_arg0 "SecondaryPrivateIpAddress") in
      let secondaryAllocationIds =
        (Option.map ~f:AllocationIdList.of_xml)
          (Xml.child xml_arg0 "SecondaryAllocationId") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "PrivateIpAddress") in
      let connectivityType =
        (Option.map ~f:ConnectivityType.of_xml)
          (Xml.child xml_arg0 "ConnectivityType") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let availabilityZoneAddresses =
        (Option.map ~f:AvailabilityZoneAddresses.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneAddress") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "SubnetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let allocationId =
        (Option.map ~f:AllocationId.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      let availabilityMode =
        (Option.map ~f:AvailabilityMode.of_xml)
          (Xml.child xml_arg0 "AvailabilityMode") in
      make ?secondaryPrivateIpAddressCount ?secondaryPrivateIpAddresses
        ?secondaryAllocationIds ?privateIpAddress ?connectivityType
        ?tagSpecifications ?availabilityZoneAddresses ?vpcId ?subnetId
        ?dryRun ?clientToken ?allocationId ?availabilityMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondaryPrivateIpAddressCount =
        field_map json__ "SecondaryPrivateIpAddressCount"
          PrivateIpAddressCount.of_json in
      let secondaryPrivateIpAddresses =
        field_map json__ "SecondaryPrivateIpAddresses" IpList.of_json in
      let secondaryAllocationIds =
        field_map json__ "SecondaryAllocationIds" AllocationIdList.of_json in
      let privateIpAddress =
        field_map json__ "PrivateIpAddress" String_.of_json in
      let connectivityType =
        field_map json__ "ConnectivityType" ConnectivityType.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let availabilityZoneAddresses =
        field_map json__ "AvailabilityZoneAddresses"
          AvailabilityZoneAddresses.of_json in
      let vpcId = field_map json__ "VpcId" VpcId.of_json in
      let subnetId = field_map json__ "SubnetId" SubnetId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let allocationId = field_map json__ "AllocationId" AllocationId.of_json in
      let availabilityMode =
        field_map json__ "AvailabilityMode" AvailabilityMode.of_json in
      make ?secondaryPrivateIpAddressCount ?secondaryPrivateIpAddresses
        ?secondaryAllocationIds ?privateIpAddress ?connectivityType
        ?tagSpecifications ?availabilityZoneAddresses ?vpcId ?subnetId
        ?dryRun ?clientToken ?allocationId ?availabilityMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a NAT gateway in the specified subnet. This action creates a network interface in the specified subnet with a private IP address from the IP address range of the subnet. You can create either a public NAT gateway or a private NAT gateway. With a public NAT gateway, internet-bound traffic from a private subnet can be routed to the NAT gateway, so that instances in a private subnet can connect to the internet. With a private NAT gateway, private communication is routed across VPCs and on-premises networks through a transit gateway or virtual private gateway. Common use cases include running large workloads behind a small pool of allowlisted IPv4 addresses, preserving private IPv4 addresses, and communicating between overlapping networks. For more information, see NAT gateways in the Amazon VPC User Guide. When you create a public NAT gateway and assign it an EIP or secondary EIPs, the network border group of the EIPs must match the network border group of the Availability Zone (AZ) that the public NAT gateway is in. If it's not the same, the NAT gateway will fail to launch. You can see the network border group for the subnet's AZ by viewing the details of the subnet. Similarly, you can view the network border group of an EIP by viewing the details of the EIP address. For more information about network border groups and EIPs, see Allocate an Elastic IP address in the Amazon VPC User Guide."]
module CreateManagedPrefixListResult =
  struct
    type nonrec t =
      {
      prefixList: ManagedPrefixList.t option
        [@ocaml.doc "Information about the prefix list."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?prefixList = fun () -> { prefixList }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("prefixList",
           (Option.map x.prefixList ~f:ManagedPrefixList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefixList =
        (Option.map ~f:ManagedPrefixList.of_xml)
          (Xml.child xml_arg0 "prefixList") in
      make ?prefixList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefixList =
        field_map json__ "PrefixList" ManagedPrefixList.of_json in
      make ?prefixList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a managed prefix list. You can specify entries for the prefix list. Each entry consists of a CIDR block and an optional description."]
module CreateManagedPrefixListRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      prefixListName: String_.t
        [@ocaml.doc
          "A name for the prefix list. Constraints: Up to 255 characters in length. The name cannot start with com.amazonaws."];
      entries: AddPrefixListEntries.t option
        [@ocaml.doc "One or more entries for the prefix list."];
      maxEntries: Integer.t
        [@ocaml.doc "The maximum number of entries for the prefix list."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the prefix list during creation."];
      addressFamily: String_.t
        [@ocaml.doc "The IP address type. Valid Values: IPv4 | IPv6"];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency. Constraints: Up to 255 UTF-8 characters in length."]}
    let context_ = "CreateManagedPrefixListRequest"
    let make ?dryRun =
      fun ?entries ->
        fun ?tagSpecifications ->
          fun ?clientToken ->
            fun ~prefixListName ->
              fun ~maxEntries ->
                fun ~addressFamily ->
                  fun () ->
                    {
                      dryRun;
                      entries;
                      tagSpecifications;
                      clientToken;
                      prefixListName;
                      maxEntries;
                      addressFamily
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PrefixListName", (Some (String_.to_value x.prefixListName)));
        ("Entry", (Option.map x.entries ~f:AddPrefixListEntries.to_value));
        ("MaxEntries", (Some (Integer.to_value x.maxEntries)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("AddressFamily", (Some (String_.to_value x.addressFamily)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let addressFamily =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AddressFamily") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let maxEntries =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxEntries") in
      let entries =
        (Option.map ~f:AddPrefixListEntries.of_xml)
          (Xml.child xml_arg0 "Entry") in
      let prefixListName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListName") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ~addressFamily ?tagSpecifications ~maxEntries
        ?entries ~prefixListName ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let addressFamily =
        field_map_exn json__ "AddressFamily" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let maxEntries = field_map_exn json__ "MaxEntries" Integer.of_json in
      let entries = field_map json__ "Entries" AddPrefixListEntries.of_json in
      let prefixListName =
        field_map_exn json__ "PrefixListName" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ~addressFamily ?tagSpecifications ~maxEntries
        ?entries ~prefixListName ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a managed prefix list. You can specify entries for the prefix list. Each entry consists of a CIDR block and an optional description."]
module CreateMacSystemIntegrityProtectionModificationTaskResult =
  struct
    type nonrec t =
      {
      macModificationTask: MacModificationTask.t option
        [@ocaml.doc "Information about the SIP modification task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?macModificationTask = fun () -> { macModificationTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("macModificationTask",
           (Option.map x.macModificationTask ~f:MacModificationTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let macModificationTask =
        (Option.map ~f:MacModificationTask.of_xml)
          (Xml.child xml_arg0 "macModificationTask") in
      make ?macModificationTask ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let macModificationTask =
        field_map json__ "MacModificationTask" MacModificationTask.of_json in
      make ?macModificationTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a System Integrity Protection (SIP) modification task to configure the SIP settings for an x86 Mac instance or Apple silicon Mac instance. For more information, see Configure SIP for Amazon EC2 instances in the Amazon EC2 User Guide. When you configure the SIP settings for your instance, you can either enable or disable all SIP settings, or you can specify a custom SIP configuration that selectively enables or disables specific SIP settings. If you implement a custom configuration, connect to the instance and verify the settings to ensure that your requirements are properly implemented and functioning as intended. SIP configurations might change with macOS updates. We recommend that you review custom SIP settings after any macOS version upgrade to ensure continued compatibility and proper functionality of your security configurations. To enable or disable all SIP settings, use the MacSystemIntegrityProtectionStatus parameter only. For example, to enable all SIP settings, specify the following: MacSystemIntegrityProtectionStatus=enabled To specify a custom configuration that selectively enables or disables specific SIP settings, use the MacSystemIntegrityProtectionStatus parameter to enable or disable all SIP settings, and then use the MacSystemIntegrityProtectionConfiguration parameter to specify exceptions. In this case, the exceptions you specify for MacSystemIntegrityProtectionConfiguration override the value you specify for MacSystemIntegrityProtectionStatus. For example, to enable all SIP settings, except NvramProtections, specify the following: MacSystemIntegrityProtectionStatus=enabled MacSystemIntegrityProtectionConfigurationRequest \"NvramProtections=disabled\""]
module CreateMacSystemIntegrityProtectionModificationTaskRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the Amazon EC2 Mac instance."];
      macCredentials: SensitiveMacCredentials.t option
        [@ocaml.doc
          "\\[Apple silicon Mac instances only\\] Specifies the following credentials: Internal disk administrative user Username - Only the default administrative user (aws-managed-user) is supported and it is used by default. You can't specify a different administrative user. Password - If you did not change the default password for aws-managed-user, specify the default password, which is blank. Otherwise, specify your password. Amazon EBS root volume administrative user Username - If you did not change the default administrative user, specify ec2-user. Otherwise, specify the username for your administrative user. Password - Specify the password for the administrative user. The credentials must be specified in the following JSON format: \\{ \"internalDiskPassword\":\"internal-disk-admin_password\", \"rootVolumeUsername\":\"root-volume-admin_username\", \"rootVolumepassword\":\"root-volume-admin_password\" \\}"];
      macSystemIntegrityProtectionConfiguration:
        MacSystemIntegrityProtectionConfigurationRequest.t option
        [@ocaml.doc
          "Specifies the overrides to selectively enable or disable individual SIP settings. The individual settings you specify here override the overall SIP status you specify for MacSystemIntegrityProtectionStatus."];
      macSystemIntegrityProtectionStatus:
        MacSystemIntegrityProtectionSettingStatus.t
        [@ocaml.doc
          "Specifies the overall SIP status for the instance. To enable all SIP settings, specify enabled. To disable all SIP settings, specify disabled."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "Specifies tags to apply to the SIP modification task."]}
    let context_ =
      "CreateMacSystemIntegrityProtectionModificationTaskRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?macCredentials ->
          fun ?macSystemIntegrityProtectionConfiguration ->
            fun ?tagSpecifications ->
              fun ~instanceId ->
                fun ~macSystemIntegrityProtectionStatus ->
                  fun () ->
                    {
                      clientToken;
                      dryRun;
                      macCredentials;
                      macSystemIntegrityProtectionConfiguration;
                      tagSpecifications;
                      instanceId;
                      macSystemIntegrityProtectionStatus
                    }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("MacCredentials",
          (Option.map x.macCredentials ~f:SensitiveMacCredentials.to_value));
        ("MacSystemIntegrityProtectionConfiguration",
          (Option.map x.macSystemIntegrityProtectionConfiguration
             ~f:MacSystemIntegrityProtectionConfigurationRequest.to_value));
        ("MacSystemIntegrityProtectionStatus",
          (Some
             (MacSystemIntegrityProtectionSettingStatus.to_value
                x.macSystemIntegrityProtectionStatus)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let macSystemIntegrityProtectionStatus =
        MacSystemIntegrityProtectionSettingStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "MacSystemIntegrityProtectionStatus") in
      let macSystemIntegrityProtectionConfiguration =
        (Option.map
           ~f:MacSystemIntegrityProtectionConfigurationRequest.of_xml)
          (Xml.child xml_arg0 "MacSystemIntegrityProtectionConfiguration") in
      let macCredentials =
        (Option.map ~f:SensitiveMacCredentials.of_xml)
          (Xml.child xml_arg0 "MacCredentials") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~macSystemIntegrityProtectionStatus
        ?macSystemIntegrityProtectionConfiguration ?macCredentials
        ~instanceId ?dryRun ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let macSystemIntegrityProtectionStatus =
        field_map_exn json__ "MacSystemIntegrityProtectionStatus"
          MacSystemIntegrityProtectionSettingStatus.of_json in
      let macSystemIntegrityProtectionConfiguration =
        field_map json__ "MacSystemIntegrityProtectionConfiguration"
          MacSystemIntegrityProtectionConfigurationRequest.of_json in
      let macCredentials =
        field_map json__ "MacCredentials" SensitiveMacCredentials.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?tagSpecifications ~macSystemIntegrityProtectionStatus
        ?macSystemIntegrityProtectionConfiguration ?macCredentials
        ~instanceId ?dryRun ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a System Integrity Protection (SIP) modification task to configure the SIP settings for an x86 Mac instance or Apple silicon Mac instance. For more information, see Configure SIP for Amazon EC2 instances in the Amazon EC2 User Guide. When you configure the SIP settings for your instance, you can either enable or disable all SIP settings, or you can specify a custom SIP configuration that selectively enables or disables specific SIP settings. If you implement a custom configuration, connect to the instance and verify the settings to ensure that your requirements are properly implemented and functioning as intended. SIP configurations might change with macOS updates. We recommend that you review custom SIP settings after any macOS version upgrade to ensure continued compatibility and proper functionality of your security configurations. To enable or disable all SIP settings, use the MacSystemIntegrityProtectionStatus parameter only. For example, to enable all SIP settings, specify the following: MacSystemIntegrityProtectionStatus=enabled To specify a custom configuration that selectively enables or disables specific SIP settings, use the MacSystemIntegrityProtectionStatus parameter to enable or disable all SIP settings, and then use the MacSystemIntegrityProtectionConfiguration parameter to specify exceptions. In this case, the exceptions you specify for MacSystemIntegrityProtectionConfiguration override the value you specify for MacSystemIntegrityProtectionStatus. For example, to enable all SIP settings, except NvramProtections, specify the following: MacSystemIntegrityProtectionStatus=enabled MacSystemIntegrityProtectionConfigurationRequest \"NvramProtections=disabled\""]
module CreateLocalGatewayVirtualInterfaceResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterface: LocalGatewayVirtualInterface.t option
        [@ocaml.doc "Information about the local gateway virtual interface."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterface =
      fun () -> { localGatewayVirtualInterface }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterface",
           (Option.map x.localGatewayVirtualInterface
              ~f:LocalGatewayVirtualInterface.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayVirtualInterface =
        (Option.map ~f:LocalGatewayVirtualInterface.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterface") in
      make ?localGatewayVirtualInterface ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayVirtualInterface =
        field_map json__ "LocalGatewayVirtualInterface"
          LocalGatewayVirtualInterface.of_json in
      make ?localGatewayVirtualInterface ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a virtual interface for a local gateway."]
module CreateLocalGatewayVirtualInterfaceRequest =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroupId:
        LocalGatewayVirtualInterfaceGroupId.t
        [@ocaml.doc "The ID of the local gateway virtual interface group."];
      outpostLagId: OutpostLagId.t
        [@ocaml.doc
          "References the Link Aggregation Group (LAG) that connects the Outpost to on-premises network devices."];
      vlan: Integer.t
        [@ocaml.doc
          "The virtual local area network (VLAN) used for the local gateway virtual interface."];
      localAddress: String_.t
        [@ocaml.doc
          "The IP address assigned to the local gateway virtual interface on the Outpost side. Only IPv4 is supported."];
      peerAddress: String_.t
        [@ocaml.doc
          "The peer IP address for the local gateway virtual interface. Only IPv4 is supported."];
      peerBgpAsn: Integer.t option
        [@ocaml.doc
          "The Autonomous System Number (ASN) of the Border Gateway Protocol (BGP) peer."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to a resource when the local gateway virtual interface is being created."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      peerBgpAsnExtended: Long.t option
        [@ocaml.doc
          "The extended 32-bit ASN of the BGP peer for use with larger ASN values."]}
    let context_ = "CreateLocalGatewayVirtualInterfaceRequest"
    let make ?peerBgpAsn =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ?peerBgpAsnExtended ->
            fun ~localGatewayVirtualInterfaceGroupId ->
              fun ~outpostLagId ->
                fun ~vlan ->
                  fun ~localAddress ->
                    fun ~peerAddress ->
                      fun () ->
                        {
                          peerBgpAsn;
                          tagSpecifications;
                          dryRun;
                          peerBgpAsnExtended;
                          localGatewayVirtualInterfaceGroupId;
                          outpostLagId;
                          vlan;
                          localAddress;
                          peerAddress
                        }
    let to_value x =
      structure_to_value
        [("LocalGatewayVirtualInterfaceGroupId",
           (Some
              (LocalGatewayVirtualInterfaceGroupId.to_value
                 x.localGatewayVirtualInterfaceGroupId)));
        ("OutpostLagId", (Some (OutpostLagId.to_value x.outpostLagId)));
        ("Vlan", (Some (Integer.to_value x.vlan)));
        ("LocalAddress", (Some (String_.to_value x.localAddress)));
        ("PeerAddress", (Some (String_.to_value x.peerAddress)));
        ("PeerBgpAsn", (Option.map x.peerBgpAsn ~f:Integer.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("PeerBgpAsnExtended",
          (Option.map x.peerBgpAsnExtended ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let peerBgpAsnExtended =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "PeerBgpAsnExtended") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let peerBgpAsn =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PeerBgpAsn") in
      let peerAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerAddress") in
      let localAddress =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LocalAddress") in
      let vlan =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Vlan") in
      let outpostLagId =
        OutpostLagId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OutpostLagId") in
      let localGatewayVirtualInterfaceGroupId =
        LocalGatewayVirtualInterfaceGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayVirtualInterfaceGroupId") in
      make ?peerBgpAsnExtended ?dryRun ?tagSpecifications ?peerBgpAsn
        ~peerAddress ~localAddress ~vlan ~outpostLagId
        ~localGatewayVirtualInterfaceGroupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let peerBgpAsnExtended =
        field_map json__ "PeerBgpAsnExtended" Long.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let peerBgpAsn = field_map json__ "PeerBgpAsn" Integer.of_json in
      let peerAddress = field_map_exn json__ "PeerAddress" String_.of_json in
      let localAddress = field_map_exn json__ "LocalAddress" String_.of_json in
      let vlan = field_map_exn json__ "Vlan" Integer.of_json in
      let outpostLagId =
        field_map_exn json__ "OutpostLagId" OutpostLagId.of_json in
      let localGatewayVirtualInterfaceGroupId =
        field_map_exn json__ "LocalGatewayVirtualInterfaceGroupId"
          LocalGatewayVirtualInterfaceGroupId.of_json in
      make ?peerBgpAsnExtended ?dryRun ?tagSpecifications ?peerBgpAsn
        ~peerAddress ~localAddress ~vlan ~outpostLagId
        ~localGatewayVirtualInterfaceGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a virtual interface for a local gateway."]
module CreateLocalGatewayVirtualInterfaceGroupResult =
  struct
    type nonrec t =
      {
      localGatewayVirtualInterfaceGroup:
        LocalGatewayVirtualInterfaceGroup.t option
        [@ocaml.doc
          "Information about the created local gateway virtual interface group."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayVirtualInterfaceGroup =
      fun () -> { localGatewayVirtualInterfaceGroup }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayVirtualInterfaceGroup",
           (Option.map x.localGatewayVirtualInterfaceGroup
              ~f:LocalGatewayVirtualInterfaceGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayVirtualInterfaceGroup =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroup.of_xml)
          (Xml.child xml_arg0 "localGatewayVirtualInterfaceGroup") in
      make ?localGatewayVirtualInterfaceGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayVirtualInterfaceGroup =
        field_map json__ "LocalGatewayVirtualInterfaceGroup"
          LocalGatewayVirtualInterfaceGroup.of_json in
      make ?localGatewayVirtualInterfaceGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a local gateway virtual interface group."]
module CreateLocalGatewayVirtualInterfaceGroupRequest =
  struct
    type nonrec t =
      {
      localGatewayId: LocalGatewayId.t
        [@ocaml.doc "The ID of the local gateway."];
      localBgpAsn: Integer.t option
        [@ocaml.doc
          "The Autonomous System Number(ASN) for the local Border Gateway Protocol (BGP)."];
      localBgpAsnExtended: Long.t option
        [@ocaml.doc
          "The extended 32-bit ASN for the local BGP configuration."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the local gateway virtual interface group when the resource is being created."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateLocalGatewayVirtualInterfaceGroupRequest"
    let make ?localBgpAsn =
      fun ?localBgpAsnExtended ->
        fun ?tagSpecifications ->
          fun ?dryRun ->
            fun ~localGatewayId ->
              fun () ->
                {
                  localBgpAsn;
                  localBgpAsnExtended;
                  tagSpecifications;
                  dryRun;
                  localGatewayId
                }
    let to_value x =
      structure_to_value
        [("LocalGatewayId",
           (Some (LocalGatewayId.to_value x.localGatewayId)));
        ("LocalBgpAsn", (Option.map x.localBgpAsn ~f:Integer.to_value));
        ("LocalBgpAsnExtended",
          (Option.map x.localBgpAsnExtended ~f:Long.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let localBgpAsnExtended =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LocalBgpAsnExtended") in
      let localBgpAsn =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "LocalBgpAsn") in
      let localGatewayId =
        LocalGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LocalGatewayId") in
      make ?dryRun ?tagSpecifications ?localBgpAsnExtended ?localBgpAsn
        ~localGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let localBgpAsnExtended =
        field_map json__ "LocalBgpAsnExtended" Long.of_json in
      let localBgpAsn = field_map json__ "LocalBgpAsn" Integer.of_json in
      let localGatewayId =
        field_map_exn json__ "LocalGatewayId" LocalGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?localBgpAsnExtended ?localBgpAsn
        ~localGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a local gateway virtual interface group."]
module CreateLocalGatewayRouteTableVpcAssociationResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVpcAssociation:
        LocalGatewayRouteTableVpcAssociation.t option
        [@ocaml.doc "Information about the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVpcAssociation =
      fun () -> { localGatewayRouteTableVpcAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVpcAssociation",
           (Option.map x.localGatewayRouteTableVpcAssociation
              ~f:LocalGatewayRouteTableVpcAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableVpcAssociation =
        (Option.map ~f:LocalGatewayRouteTableVpcAssociation.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTableVpcAssociation") in
      make ?localGatewayRouteTableVpcAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayRouteTableVpcAssociation =
        field_map json__ "LocalGatewayRouteTableVpcAssociation"
          LocalGatewayRouteTableVpcAssociation.of_json in
      make ?localGatewayRouteTableVpcAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified VPC with the specified local gateway route table."]
module CreateLocalGatewayRouteTableVpcAssociationRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to assign to the local gateway route table VPC association."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateLocalGatewayRouteTableVpcAssociationRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~localGatewayRouteTableId ->
          fun ~vpcId ->
            fun () ->
              { tagSpecifications; dryRun; localGatewayRouteTableId; vpcId }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Some
              (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      make ?dryRun ?tagSpecifications ~vpcId ~localGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let localGatewayRouteTableId =
        field_map_exn json__ "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      make ?dryRun ?tagSpecifications ~vpcId ~localGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified VPC with the specified local gateway route table."]
module CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTableVirtualInterfaceGroupAssociation:
        LocalGatewayRouteTableVirtualInterfaceGroupAssociation.t option
        [@ocaml.doc
          "Information about the local gateway route table virtual interface group association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTableVirtualInterfaceGroupAssociation =
      fun () -> { localGatewayRouteTableVirtualInterfaceGroupAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTableVirtualInterfaceGroupAssociation",
           (Option.map
              x.localGatewayRouteTableVirtualInterfaceGroupAssociation
              ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTableVirtualInterfaceGroupAssociation =
        (Option.map
           ~f:LocalGatewayRouteTableVirtualInterfaceGroupAssociation.of_xml)
          (Xml.child xml_arg0
             "localGatewayRouteTableVirtualInterfaceGroupAssociation") in
      make ?localGatewayRouteTableVirtualInterfaceGroupAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayRouteTableVirtualInterfaceGroupAssociation =
        field_map json__
          "LocalGatewayRouteTableVirtualInterfaceGroupAssociation"
          LocalGatewayRouteTableVirtualInterfaceGroupAssociation.of_json in
      make ?localGatewayRouteTableVirtualInterfaceGroupAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a local gateway route table virtual interface group association."]
module CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      localGatewayVirtualInterfaceGroupId:
        LocalGatewayVirtualInterfaceGroupId.t
        [@ocaml.doc
          "The ID of the local gateway route table virtual interface group association."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags assigned to the local gateway route table virtual interface group association."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ =
      "CreateLocalGatewayRouteTableVirtualInterfaceGroupAssociationRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~localGatewayRouteTableId ->
          fun ~localGatewayVirtualInterfaceGroupId ->
            fun () ->
              {
                tagSpecifications;
                dryRun;
                localGatewayRouteTableId;
                localGatewayVirtualInterfaceGroupId
              }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Some
              (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("LocalGatewayVirtualInterfaceGroupId",
          (Some
             (LocalGatewayVirtualInterfaceGroupId.to_value
                x.localGatewayVirtualInterfaceGroupId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let localGatewayVirtualInterfaceGroupId =
        LocalGatewayVirtualInterfaceGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayVirtualInterfaceGroupId") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      make ?dryRun ?tagSpecifications ~localGatewayVirtualInterfaceGroupId
        ~localGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let localGatewayVirtualInterfaceGroupId =
        field_map_exn json__ "LocalGatewayVirtualInterfaceGroupId"
          LocalGatewayVirtualInterfaceGroupId.of_json in
      let localGatewayRouteTableId =
        field_map_exn json__ "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      make ?dryRun ?tagSpecifications ~localGatewayVirtualInterfaceGroupId
        ~localGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a local gateway route table virtual interface group association."]
module CreateLocalGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      localGatewayRouteTable: LocalGatewayRouteTable.t option
        [@ocaml.doc "Information about the local gateway route table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?localGatewayRouteTable = fun () -> { localGatewayRouteTable }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("localGatewayRouteTable",
           (Option.map x.localGatewayRouteTable
              ~f:LocalGatewayRouteTable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let localGatewayRouteTable =
        (Option.map ~f:LocalGatewayRouteTable.of_xml)
          (Xml.child xml_arg0 "localGatewayRouteTable") in
      make ?localGatewayRouteTable ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let localGatewayRouteTable =
        field_map json__ "LocalGatewayRouteTable"
          LocalGatewayRouteTable.of_json in
      make ?localGatewayRouteTable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a local gateway route table."]
module CreateLocalGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      localGatewayId: LocalGatewayId.t
        [@ocaml.doc "The ID of the local gateway."];
      mode: LocalGatewayRouteTableMode.t option
        [@ocaml.doc "The mode of the local gateway route table."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags assigned to the local gateway route table."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateLocalGatewayRouteTableRequest"
    let make ?mode =
      fun ?tagSpecifications ->
        fun ?dryRun ->
          fun ~localGatewayId ->
            fun () -> { mode; tagSpecifications; dryRun; localGatewayId }
    let to_value x =
      structure_to_value
        [("LocalGatewayId",
           (Some (LocalGatewayId.to_value x.localGatewayId)));
        ("Mode", (Option.map x.mode ~f:LocalGatewayRouteTableMode.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let mode =
        (Option.map ~f:LocalGatewayRouteTableMode.of_xml)
          (Xml.child xml_arg0 "Mode") in
      let localGatewayId =
        LocalGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LocalGatewayId") in
      make ?dryRun ?tagSpecifications ?mode ~localGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let mode = field_map json__ "Mode" LocalGatewayRouteTableMode.of_json in
      let localGatewayId =
        field_map_exn json__ "LocalGatewayId" LocalGatewayId.of_json in
      make ?dryRun ?tagSpecifications ?mode ~localGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a local gateway route table."]
module CreateLocalGatewayRouteResult =
  struct
    type nonrec t =
      {
      route: LocalGatewayRoute.t option
        [@ocaml.doc "Information about the route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?route = fun () -> { route }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("route", (Option.map x.route ~f:LocalGatewayRoute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let route =
        (Option.map ~f:LocalGatewayRoute.of_xml) (Xml.child xml_arg0 "route") in
      make ?route ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let route = field_map json__ "Route" LocalGatewayRoute.of_json in
      make ?route ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a static route for the specified local gateway route table. You must specify one of the following targets: LocalGatewayVirtualInterfaceGroupId NetworkInterfaceId"]
module CreateLocalGatewayRouteRequest =
  struct
    type nonrec t =
      {
      destinationCidrBlock: String_.t option
        [@ocaml.doc
          "The CIDR range used for destination matches. Routing decisions are based on the most specific match."];
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      localGatewayVirtualInterfaceGroupId:
        LocalGatewayVirtualInterfaceGroupId.t option
        [@ocaml.doc "The ID of the virtual interface group."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceId: NetworkInterfaceId.t option
        [@ocaml.doc "The ID of the network interface."];
      destinationPrefixListId: PrefixListResourceId.t option
        [@ocaml.doc
          "The ID of the prefix list. Use a prefix list in place of DestinationCidrBlock. You cannot use DestinationPrefixListId and DestinationCidrBlock in the same request."]}
    let context_ = "CreateLocalGatewayRouteRequest"
    let make ?destinationCidrBlock =
      fun ?localGatewayVirtualInterfaceGroupId ->
        fun ?dryRun ->
          fun ?networkInterfaceId ->
            fun ?destinationPrefixListId ->
              fun ~localGatewayRouteTableId ->
                fun () ->
                  {
                    destinationCidrBlock;
                    localGatewayVirtualInterfaceGroupId;
                    dryRun;
                    networkInterfaceId;
                    destinationPrefixListId;
                    localGatewayRouteTableId
                  }
    let to_value x =
      structure_to_value
        [("DestinationCidrBlock",
           (Option.map x.destinationCidrBlock ~f:String_.to_value));
        ("LocalGatewayRouteTableId",
          (Some
             (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("LocalGatewayVirtualInterfaceGroupId",
          (Option.map x.localGatewayVirtualInterfaceGroupId
             ~f:LocalGatewayVirtualInterfaceGroupId.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("DestinationPrefixListId",
          (Option.map x.destinationPrefixListId
             ~f:PrefixListResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationPrefixListId =
        (Option.map ~f:PrefixListResourceId.of_xml)
          (Xml.child xml_arg0 "DestinationPrefixListId") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let localGatewayVirtualInterfaceGroupId =
        (Option.map ~f:LocalGatewayVirtualInterfaceGroupId.of_xml)
          (Xml.child xml_arg0 "LocalGatewayVirtualInterfaceGroupId") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      let destinationCidrBlock =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationCidrBlock") in
      make ?destinationPrefixListId ?networkInterfaceId ?dryRun
        ?localGatewayVirtualInterfaceGroupId ~localGatewayRouteTableId
        ?destinationCidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationPrefixListId =
        field_map json__ "DestinationPrefixListId"
          PrefixListResourceId.of_json in
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let localGatewayVirtualInterfaceGroupId =
        field_map json__ "LocalGatewayVirtualInterfaceGroupId"
          LocalGatewayVirtualInterfaceGroupId.of_json in
      let localGatewayRouteTableId =
        field_map_exn json__ "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      let destinationCidrBlock =
        field_map json__ "DestinationCidrBlock" String_.of_json in
      make ?destinationPrefixListId ?networkInterfaceId ?dryRun
        ?localGatewayVirtualInterfaceGroupId ~localGatewayRouteTableId
        ?destinationCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a static route for the specified local gateway route table. You must specify one of the following targets: LocalGatewayVirtualInterfaceGroupId NetworkInterfaceId"]
module CreateLaunchTemplateVersionResult =
  struct
    type nonrec t =
      {
      launchTemplateVersion: LaunchTemplateVersion.t option
        [@ocaml.doc "Information about the launch template version."];
      warning: ValidationWarning.t option
        [@ocaml.doc
          "If the new version of the launch template contains parameters or parameter combinations that are not valid, an error code and an error message are returned for each issue that's found."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplateVersion =
      fun ?warning -> fun () -> { launchTemplateVersion; warning }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplateVersion",
           (Option.map x.launchTemplateVersion
              ~f:LaunchTemplateVersion.to_value));
        ("warning", (Option.map x.warning ~f:ValidationWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let warning =
        (Option.map ~f:ValidationWarning.of_xml)
          (Xml.child xml_arg0 "warning") in
      let launchTemplateVersion =
        (Option.map ~f:LaunchTemplateVersion.of_xml)
          (Xml.child xml_arg0 "launchTemplateVersion") in
      make ?warning ?launchTemplateVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let warning = field_map json__ "Warning" ValidationWarning.of_json in
      let launchTemplateVersion =
        field_map json__ "LaunchTemplateVersion"
          LaunchTemplateVersion.of_json in
      make ?warning ?launchTemplateVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new version of a launch template. You must specify an existing launch template, either by name or ID. You can determine whether the new version inherits parameters from a source version, and add or overwrite parameters as needed. Launch template versions are numbered in the order in which they are created. You can't specify, change, or replace the numbering of launch template versions. Launch templates are immutable; after you create a launch template, you can't modify it. Instead, you can create a new version of the launch template that includes the changes that you require. For more information, see Modify a launch template (manage launch template versions) in the Amazon EC2 User Guide."]
module CreateLaunchTemplateVersionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure the idempotency of the request. If a client token isn't specified, a randomly generated token is used in the request to ensure idempotency. For more information, see Ensuring idempotency. Constraint: Maximum 128 ASCII characters."];
      launchTemplateId: LaunchTemplateId.t option
        [@ocaml.doc
          "The ID of the launch template. You must specify either the launch template ID or the launch template name, but not both."];
      launchTemplateName: LaunchTemplateName.t option
        [@ocaml.doc
          "The name of the launch template. You must specify either the launch template ID or the launch template name, but not both."];
      sourceVersion: String_.t option
        [@ocaml.doc
          "The version of the launch template on which to base the new version. Snapshots applied to the block device mapping are ignored when creating a new version unless they are explicitly included. If you specify this parameter, the new version inherits the launch parameters from the source version. If you specify additional launch parameters for the new version, they overwrite any corresponding launch parameters inherited from the source version. If you omit this parameter, the new version contains only the launch parameters that you specify for the new version."];
      versionDescription: VersionDescription.t option
        [@ocaml.doc "A description for the version of the launch template."];
      launchTemplateData: RequestLaunchTemplateData.t
        [@ocaml.doc "The information for the launch template."];
      resolveAlias: Boolean.t option
        [@ocaml.doc
          "If true, and if a Systems Manager parameter is specified for ImageId, the AMI ID is displayed in the response for imageID. For more information, see Use a Systems Manager parameter instead of an AMI ID in the Amazon EC2 User Guide. Default: false"]}
    let context_ = "CreateLaunchTemplateVersionRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?launchTemplateId ->
          fun ?launchTemplateName ->
            fun ?sourceVersion ->
              fun ?versionDescription ->
                fun ?resolveAlias ->
                  fun ~launchTemplateData ->
                    fun () ->
                      {
                        dryRun;
                        clientToken;
                        launchTemplateId;
                        launchTemplateName;
                        sourceVersion;
                        versionDescription;
                        resolveAlias;
                        launchTemplateData
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("LaunchTemplateId",
          (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("SourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:VersionDescription.to_value));
        ("LaunchTemplateData",
          (Some (RequestLaunchTemplateData.to_value x.launchTemplateData)));
        ("ResolveAlias", (Option.map x.resolveAlias ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resolveAlias =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ResolveAlias") in
      let launchTemplateData =
        RequestLaunchTemplateData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateData") in
      let versionDescription =
        (Option.map ~f:VersionDescription.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SourceVersion") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?resolveAlias ~launchTemplateData ?versionDescription
        ?sourceVersion ?launchTemplateName ?launchTemplateId ?clientToken
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resolveAlias = field_map json__ "ResolveAlias" Boolean.of_json in
      let launchTemplateData =
        field_map_exn json__ "LaunchTemplateData"
          RequestLaunchTemplateData.of_json in
      let versionDescription =
        field_map json__ "VersionDescription" VersionDescription.of_json in
      let sourceVersion = field_map json__ "SourceVersion" String_.of_json in
      let launchTemplateName =
        field_map json__ "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json__ "LaunchTemplateId" LaunchTemplateId.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?resolveAlias ~launchTemplateData ?versionDescription
        ?sourceVersion ?launchTemplateName ?launchTemplateId ?clientToken
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new version of a launch template. You must specify an existing launch template, either by name or ID. You can determine whether the new version inherits parameters from a source version, and add or overwrite parameters as needed. Launch template versions are numbered in the order in which they are created. You can't specify, change, or replace the numbering of launch template versions. Launch templates are immutable; after you create a launch template, you can't modify it. Instead, you can create a new version of the launch template that includes the changes that you require. For more information, see Modify a launch template (manage launch template versions) in the Amazon EC2 User Guide."]
module CreateLaunchTemplateResult =
  struct
    type nonrec t =
      {
      launchTemplate: LaunchTemplate.t option
        [@ocaml.doc "Information about the launch template."];
      warning: ValidationWarning.t option
        [@ocaml.doc
          "If the launch template contains parameters or parameter combinations that are not valid, an error code and an error message are returned for each issue that's found."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?launchTemplate =
      fun ?warning -> fun () -> { launchTemplate; warning }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("launchTemplate",
           (Option.map x.launchTemplate ~f:LaunchTemplate.to_value));
        ("warning", (Option.map x.warning ~f:ValidationWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let warning =
        (Option.map ~f:ValidationWarning.of_xml)
          (Xml.child xml_arg0 "warning") in
      let launchTemplate =
        (Option.map ~f:LaunchTemplate.of_xml)
          (Xml.child xml_arg0 "launchTemplate") in
      make ?warning ?launchTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let warning = field_map json__ "Warning" ValidationWarning.of_json in
      let launchTemplate =
        field_map json__ "LaunchTemplate" LaunchTemplate.of_json in
      make ?warning ?launchTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a launch template. A launch template contains the parameters to launch an instance. When you launch an instance using RunInstances, you can specify a launch template instead of providing the launch parameters in the request. For more information, see Store instance launch parameters in Amazon EC2 launch templates in the Amazon EC2 User Guide. To clone an existing launch template as the basis for a new launch template, use the Amazon EC2 console. The API, SDKs, and CLI do not support cloning a template. For more information, see Create a launch template from an existing launch template in the Amazon EC2 User Guide."]
module CreateLaunchTemplateRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure the idempotency of the request. If a client token isn't specified, a randomly generated token is used in the request to ensure idempotency. For more information, see Ensuring idempotency. Constraint: Maximum 128 ASCII characters."];
      launchTemplateName: LaunchTemplateName.t
        [@ocaml.doc "A name for the launch template."];
      versionDescription: VersionDescription.t option
        [@ocaml.doc
          "A description for the first version of the launch template."];
      launchTemplateData: RequestLaunchTemplateData.t
        [@ocaml.doc "The information for the launch template."];
      operator: OperatorRequest.t option
        [@ocaml.doc "Reserved for internal use."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the launch template on creation. To tag the launch template, the resource type must be launch-template. To specify the tags for the resources that are created when an instance is launched, you must use the TagSpecifications parameter in the launch template data structure."]}
    let context_ = "CreateLaunchTemplateRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?versionDescription ->
          fun ?operator ->
            fun ?tagSpecifications ->
              fun ~launchTemplateName ->
                fun ~launchTemplateData ->
                  fun () ->
                    {
                      dryRun;
                      clientToken;
                      versionDescription;
                      operator;
                      tagSpecifications;
                      launchTemplateName;
                      launchTemplateData
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("LaunchTemplateName",
          (Some (LaunchTemplateName.to_value x.launchTemplateName)));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:VersionDescription.to_value));
        ("LaunchTemplateData",
          (Some (RequestLaunchTemplateData.to_value x.launchTemplateData)));
        ("Operator", (Option.map x.operator ~f:OperatorRequest.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let operator =
        (Option.map ~f:OperatorRequest.of_xml)
          (Xml.child xml_arg0 "Operator") in
      let launchTemplateData =
        RequestLaunchTemplateData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateData") in
      let versionDescription =
        (Option.map ~f:VersionDescription.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let launchTemplateName =
        LaunchTemplateName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateName") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?operator ~launchTemplateData
        ?versionDescription ~launchTemplateName ?clientToken ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let operator = field_map json__ "Operator" OperatorRequest.of_json in
      let launchTemplateData =
        field_map_exn json__ "LaunchTemplateData"
          RequestLaunchTemplateData.of_json in
      let versionDescription =
        field_map json__ "VersionDescription" VersionDescription.of_json in
      let launchTemplateName =
        field_map_exn json__ "LaunchTemplateName" LaunchTemplateName.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ?operator ~launchTemplateData
        ?versionDescription ~launchTemplateName ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a launch template. A launch template contains the parameters to launch an instance. When you launch an instance using RunInstances, you can specify a launch template instead of providing the launch parameters in the request. For more information, see Store instance launch parameters in Amazon EC2 launch templates in the Amazon EC2 User Guide. To clone an existing launch template as the basis for a new launch template, use the Amazon EC2 console. The API, SDKs, and CLI do not support cloning a template. For more information, see Create a launch template from an existing launch template in the Amazon EC2 User Guide."]
module CreateKeyPairRequest =
  struct
    type nonrec t =
      {
      keyName: String_.t
        [@ocaml.doc
          "A unique name for the key pair. Constraints: Up to 255 ASCII characters"];
      keyType: KeyType.t option
        [@ocaml.doc
          "The type of key pair. Note that ED25519 keys are not supported for Windows instances. Default: rsa"];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the new key pair."];
      keyFormat: KeyFormat.t option
        [@ocaml.doc "The format of the key pair. Default: pem"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateKeyPairRequest"
    let make ?keyType =
      fun ?tagSpecifications ->
        fun ?keyFormat ->
          fun ?dryRun ->
            fun ~keyName ->
              fun () ->
                { keyType; tagSpecifications; keyFormat; dryRun; keyName }
    let to_value x =
      structure_to_value
        [("KeyName", (Some (String_.to_value x.keyName)));
        ("KeyType", (Option.map x.keyType ~f:KeyType.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("KeyFormat", (Option.map x.keyFormat ~f:KeyFormat.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let keyFormat =
        (Option.map ~f:KeyFormat.of_xml) (Xml.child xml_arg0 "KeyFormat") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let keyType =
        (Option.map ~f:KeyType.of_xml) (Xml.child xml_arg0 "KeyType") in
      let keyName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeyName") in
      make ?dryRun ?keyFormat ?tagSpecifications ?keyType ~keyName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let keyFormat = field_map json__ "KeyFormat" KeyFormat.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let keyType = field_map json__ "KeyType" KeyType.of_json in
      let keyName = field_map_exn json__ "KeyName" String_.of_json in
      make ?dryRun ?keyFormat ?tagSpecifications ?keyType ~keyName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an ED25519 or 2048-bit RSA key pair with the specified name and in the specified format. Amazon EC2 stores the public key and displays the private key for you to save to a file. The private key is returned as an unencrypted PEM encoded PKCS#1 private key or an unencrypted PPK formatted private key for use with PuTTY. If a key with the specified name already exists, Amazon EC2 returns an error. The key pair returned to you is available only in the Amazon Web Services Region in which you create it. If you prefer, you can create your own key pair using a third-party tool and upload it to any Region using ImportKeyPair. You can have up to 5,000 key pairs per Amazon Web Services Region. For more information, see Amazon EC2 key pairs in the Amazon EC2 User Guide."]
module CreateIpamScopeResult =
  struct
    type nonrec t =
      {
      ipamScope: IpamScope.t option
        [@ocaml.doc "Information about the created scope."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamScope = fun () -> { ipamScope }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamScope", (Option.map x.ipamScope ~f:IpamScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamScope =
        (Option.map ~f:IpamScope.of_xml) (Xml.child xml_arg0 "ipamScope") in
      make ?ipamScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamScope = field_map json__ "IpamScope" IpamScope.of_json in
      make ?ipamScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an IPAM scope. In IPAM, a scope is the highest-level container within IPAM. An IPAM contains two default scopes. Each scope represents the IP space for a single network. The private scope is intended for all private IP address space. The public scope is intended for all public IP address space. Scopes enable you to reuse IP addresses across multiple unconnected networks without causing IP address overlap or conflict. For more information, see Add a scope in the Amazon VPC IPAM User Guide."]
module CreateIpamScopeRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t
        [@ocaml.doc
          "The ID of the IPAM for which you're creating this scope."];
      description: String_.t option
        [@ocaml.doc "A description for the scope you're creating."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      externalAuthorityConfiguration: ExternalAuthorityConfiguration.t option
        [@ocaml.doc
          "The configuration that links an Amazon VPC IPAM scope to an external authority system. It specifies the type of external system and the external resource identifier that identifies your account or instance in that system. In IPAM, an external authority is a third-party IP address management system that provides CIDR blocks when you provision address space for top-level IPAM pools. This allows you to use your existing IP management system to control which address ranges are allocated to Amazon Web Services while using Amazon VPC IPAM to manage subnets within those ranges."]}
    let context_ = "CreateIpamScopeRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?tagSpecifications ->
          fun ?clientToken ->
            fun ?externalAuthorityConfiguration ->
              fun ~ipamId ->
                fun () ->
                  {
                    dryRun;
                    description;
                    tagSpecifications;
                    clientToken;
                    externalAuthorityConfiguration;
                    ipamId
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("ExternalAuthorityConfiguration",
          (Option.map x.externalAuthorityConfiguration
             ~f:ExternalAuthorityConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let externalAuthorityConfiguration =
        (Option.map ~f:ExternalAuthorityConfiguration.of_xml)
          (Xml.child xml_arg0 "ExternalAuthorityConfiguration") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?externalAuthorityConfiguration ?clientToken ?tagSpecifications
        ?description ~ipamId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let externalAuthorityConfiguration =
        field_map json__ "ExternalAuthorityConfiguration"
          ExternalAuthorityConfiguration.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?externalAuthorityConfiguration ?clientToken ?tagSpecifications
        ?description ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an IPAM scope. In IPAM, a scope is the highest-level container within IPAM. An IPAM contains two default scopes. Each scope represents the IP space for a single network. The private scope is intended for all private IP address space. The public scope is intended for all public IP address space. Scopes enable you to reuse IP addresses across multiple unconnected networks without causing IP address overlap or conflict. For more information, see Add a scope in the Amazon VPC IPAM User Guide."]
module CreateIpamResult =
  struct
    type nonrec t =
      {
      ipam: Ipam.t option [@ocaml.doc "Information about the IPAM created."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipam = fun () -> { ipam }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("ipam", (Option.map x.ipam ~f:Ipam.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipam = (Option.map ~f:Ipam.of_xml) (Xml.child xml_arg0 "ipam") in
      make ?ipam ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipam = field_map json__ "Ipam" Ipam.of_json in make ?ipam ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an IPAM. Amazon VPC IP Address Manager (IPAM) is a VPC feature that you can use to automate your IP address management workflows including assigning, tracking, troubleshooting, and auditing IP addresses across Amazon Web Services Regions and accounts throughout your Amazon Web Services Organization. For more information, see Create an IPAM in the Amazon VPC IPAM User Guide."]
module CreateIpamResourceDiscoveryResult =
  struct
    type nonrec t =
      {
      ipamResourceDiscovery: IpamResourceDiscovery.t option
        [@ocaml.doc "An IPAM resource discovery."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamResourceDiscovery = fun () -> { ipamResourceDiscovery }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamResourceDiscovery",
           (Option.map x.ipamResourceDiscovery
              ~f:IpamResourceDiscovery.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamResourceDiscovery =
        (Option.map ~f:IpamResourceDiscovery.of_xml)
          (Xml.child xml_arg0 "ipamResourceDiscovery") in
      make ?ipamResourceDiscovery ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamResourceDiscovery =
        field_map json__ "IpamResourceDiscovery"
          IpamResourceDiscovery.of_json in
      make ?ipamResourceDiscovery ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module CreateIpamResourceDiscoveryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      description: String_.t option
        [@ocaml.doc "A description for the IPAM resource discovery."];
      operatingRegions: AddIpamOperatingRegionSet.t option
        [@ocaml.doc
          "Operating Regions for the IPAM resource discovery. Operating Regions are Amazon Web Services Regions where the IPAM is allowed to manage IP address CIDRs. IPAM only discovers and monitors resources in the Amazon Web Services Regions you select as operating Regions."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "Tag specifications for the IPAM resource discovery."];
      clientToken: String_.t option
        [@ocaml.doc "A client token for the IPAM resource discovery."]}
    let make ?dryRun =
      fun ?description ->
        fun ?operatingRegions ->
          fun ?tagSpecifications ->
            fun ?clientToken ->
              fun () ->
                {
                  dryRun;
                  description;
                  operatingRegions;
                  tagSpecifications;
                  clientToken
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("OperatingRegion",
          (Option.map x.operatingRegions
             ~f:AddIpamOperatingRegionSet.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let operatingRegions =
        (Option.map ~f:AddIpamOperatingRegionSet.of_xml)
          (Xml.child xml_arg0 "OperatingRegion") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ?operatingRegions ?description
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let operatingRegions =
        field_map json__ "OperatingRegions" AddIpamOperatingRegionSet.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ?operatingRegions ?description
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM resource discovery. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module CreateIpamRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      description: String_.t option
        [@ocaml.doc "A description for the IPAM."];
      operatingRegions: AddIpamOperatingRegionSet.t option
        [@ocaml.doc
          "The operating Regions for the IPAM. Operating Regions are Amazon Web Services Regions where the IPAM is allowed to manage IP address CIDRs. IPAM only discovers and monitors resources in the Amazon Web Services Regions you select as operating Regions. For more information about operating Regions, see Create an IPAM in the Amazon VPC IPAM User Guide."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      tier: IpamTier.t option
        [@ocaml.doc
          "IPAM is offered in a Free Tier and an Advanced Tier. For more information about the features available in each tier and the costs associated with the tiers, see Amazon VPC pricing > IPAM tab."];
      enablePrivateGua: Boolean.t option
        [@ocaml.doc
          "Enable this option to use your own GUA ranges as private IPv6 addresses. This option is disabled by default."];
      meteredAccount: IpamMeteredAccount.t option
        [@ocaml.doc
          "A metered account is an Amazon Web Services account that is charged for active IP addresses managed in IPAM. For more information, see Enable cost distribution in the Amazon VPC IPAM User Guide. Possible values: ipam-owner (default): The Amazon Web Services account which owns the IPAM is charged for all active IP addresses managed in IPAM. resource-owner: The Amazon Web Services account that owns the IP address is charged for the active IP address."]}
    let make ?dryRun =
      fun ?description ->
        fun ?operatingRegions ->
          fun ?tagSpecifications ->
            fun ?clientToken ->
              fun ?tier ->
                fun ?enablePrivateGua ->
                  fun ?meteredAccount ->
                    fun () ->
                      {
                        dryRun;
                        description;
                        operatingRegions;
                        tagSpecifications;
                        clientToken;
                        tier;
                        enablePrivateGua;
                        meteredAccount
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("OperatingRegion",
          (Option.map x.operatingRegions
             ~f:AddIpamOperatingRegionSet.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Tier", (Option.map x.tier ~f:IpamTier.to_value));
        ("EnablePrivateGua",
          (Option.map x.enablePrivateGua ~f:Boolean.to_value));
        ("MeteredAccount",
          (Option.map x.meteredAccount ~f:IpamMeteredAccount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let meteredAccount =
        (Option.map ~f:IpamMeteredAccount.of_xml)
          (Xml.child xml_arg0 "MeteredAccount") in
      let enablePrivateGua =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnablePrivateGua") in
      let tier = (Option.map ~f:IpamTier.of_xml) (Xml.child xml_arg0 "Tier") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let operatingRegions =
        (Option.map ~f:AddIpamOperatingRegionSet.of_xml)
          (Xml.child xml_arg0 "OperatingRegion") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?meteredAccount ?enablePrivateGua ?tier ?clientToken
        ?tagSpecifications ?operatingRegions ?description ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let meteredAccount =
        field_map json__ "MeteredAccount" IpamMeteredAccount.of_json in
      let enablePrivateGua =
        field_map json__ "EnablePrivateGua" Boolean.of_json in
      let tier = field_map json__ "Tier" IpamTier.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let operatingRegions =
        field_map json__ "OperatingRegions" AddIpamOperatingRegionSet.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?meteredAccount ?enablePrivateGua ?tier ?clientToken
        ?tagSpecifications ?operatingRegions ?description ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an IPAM. Amazon VPC IP Address Manager (IPAM) is a VPC feature that you can use to automate your IP address management workflows including assigning, tracking, troubleshooting, and auditing IP addresses across Amazon Web Services Regions and accounts throughout your Amazon Web Services Organization. For more information, see Create an IPAM in the Amazon VPC IPAM User Guide."]
module CreateIpamPrefixListResolverTargetResult =
  struct
    type nonrec t =
      {
      ipamPrefixListResolverTarget: IpamPrefixListResolverTarget.t option
        [@ocaml.doc
          "Information about the IPAM prefix list resolver target that was created."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPrefixListResolverTarget =
      fun () -> { ipamPrefixListResolverTarget }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPrefixListResolverTarget",
           (Option.map x.ipamPrefixListResolverTarget
              ~f:IpamPrefixListResolverTarget.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolverTarget =
        (Option.map ~f:IpamPrefixListResolverTarget.of_xml)
          (Xml.child xml_arg0 "ipamPrefixListResolverTarget") in
      make ?ipamPrefixListResolverTarget ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolverTarget =
        field_map json__ "IpamPrefixListResolverTarget"
          IpamPrefixListResolverTarget.of_json in
      make ?ipamPrefixListResolverTarget ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM prefix list resolver target. An IPAM prefix list resolver target is an association between a specific customer-managed prefix list and an IPAM prefix list resolver. The target enables the resolver to synchronize CIDRs selected by its rules into the specified prefix list, which can then be referenced in Amazon Web Services resources. For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide."]
module CreateIpamPrefixListResolverTargetRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPrefixListResolverId: IpamPrefixListResolverId.t
        [@ocaml.doc
          "The ID of the IPAM prefix list resolver that will manage the synchronization of CIDRs to the target prefix list."];
      prefixListId: String_.t
        [@ocaml.doc
          "The ID of the managed prefix list that will be synchronized with CIDRs selected by the IPAM prefix list resolver. This prefix list becomes an IPAM managed prefix list. An IPAM-managed prefix list is a customer-managed prefix list that has been associated with an IPAM prefix list resolver target. When a prefix list becomes IPAM managed, its CIDRs are automatically synchronized based on the IPAM prefix list resolver's CIDR selection rules, and direct CIDR modifications are restricted."];
      prefixListRegion: String_.t
        [@ocaml.doc
          "The Amazon Web Services Region where the prefix list is located. This is required when referencing a prefix list in a different Region."];
      desiredVersion: BoxedLong.t option
        [@ocaml.doc
          "The specific version of the prefix list to target. If not specified, the resolver will target the latest version."];
      trackLatestVersion: Boolean.t
        [@ocaml.doc
          "Indicates whether the resolver target should automatically track the latest version of the prefix list. When enabled, the target will always synchronize with the most current version of the prefix list. Choose this for automatic updates when you want your prefix lists to stay current with infrastructure changes without manual intervention."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the IPAM prefix list resolver target during creation. Tags help you organize and manage your Amazon Web Services resources."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."]}
    let context_ = "CreateIpamPrefixListResolverTargetRequest"
    let make ?dryRun =
      fun ?desiredVersion ->
        fun ?tagSpecifications ->
          fun ?clientToken ->
            fun ~ipamPrefixListResolverId ->
              fun ~prefixListId ->
                fun ~prefixListRegion ->
                  fun ~trackLatestVersion ->
                    fun () ->
                      {
                        dryRun;
                        desiredVersion;
                        tagSpecifications;
                        clientToken;
                        ipamPrefixListResolverId;
                        prefixListId;
                        prefixListRegion;
                        trackLatestVersion
                      }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPrefixListResolverId",
          (Some
             (IpamPrefixListResolverId.to_value x.ipamPrefixListResolverId)));
        ("PrefixListId", (Some (String_.to_value x.prefixListId)));
        ("PrefixListRegion", (Some (String_.to_value x.prefixListRegion)));
        ("DesiredVersion",
          (Option.map x.desiredVersion ~f:BoxedLong.to_value));
        ("TrackLatestVersion",
          (Some (Boolean.to_value x.trackLatestVersion)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let trackLatestVersion =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrackLatestVersion") in
      let desiredVersion =
        (Option.map ~f:BoxedLong.of_xml)
          (Xml.child xml_arg0 "DesiredVersion") in
      let prefixListRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListRegion") in
      let prefixListId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrefixListId") in
      let ipamPrefixListResolverId =
        IpamPrefixListResolverId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "IpamPrefixListResolverId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ~trackLatestVersion
        ?desiredVersion ~prefixListRegion ~prefixListId
        ~ipamPrefixListResolverId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let trackLatestVersion =
        field_map_exn json__ "TrackLatestVersion" Boolean.of_json in
      let desiredVersion =
        field_map json__ "DesiredVersion" BoxedLong.of_json in
      let prefixListRegion =
        field_map_exn json__ "PrefixListRegion" String_.of_json in
      let prefixListId = field_map_exn json__ "PrefixListId" String_.of_json in
      let ipamPrefixListResolverId =
        field_map_exn json__ "IpamPrefixListResolverId"
          IpamPrefixListResolverId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ~trackLatestVersion
        ?desiredVersion ~prefixListRegion ~prefixListId
        ~ipamPrefixListResolverId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM prefix list resolver target. An IPAM prefix list resolver target is an association between a specific customer-managed prefix list and an IPAM prefix list resolver. The target enables the resolver to synchronize CIDRs selected by its rules into the specified prefix list, which can then be referenced in Amazon Web Services resources. For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide."]
module CreateIpamPrefixListResolverResult =
  struct
    type nonrec t =
      {
      ipamPrefixListResolver: IpamPrefixListResolver.t option
        [@ocaml.doc
          "Information about the IPAM prefix list resolver that was created."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPrefixListResolver = fun () -> { ipamPrefixListResolver }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPrefixListResolver",
           (Option.map x.ipamPrefixListResolver
              ~f:IpamPrefixListResolver.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPrefixListResolver =
        (Option.map ~f:IpamPrefixListResolver.of_xml)
          (Xml.child xml_arg0 "ipamPrefixListResolver") in
      make ?ipamPrefixListResolver ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPrefixListResolver =
        field_map json__ "IpamPrefixListResolver"
          IpamPrefixListResolver.of_json in
      make ?ipamPrefixListResolver ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM prefix list resolver. An IPAM prefix list resolver is a component that manages the synchronization between IPAM's CIDR selection rules and customer-managed prefix lists. It automates connectivity configurations by selecting CIDRs from IPAM's database based on your business logic and synchronizing them with prefix lists used in resources such as VPC route tables and security groups. For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide."]
module CreateIpamPrefixListResolverRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t
        [@ocaml.doc
          "The ID of the IPAM that will serve as the source of the IP address database for CIDR selection. The IPAM must be in the Advanced tier to use this feature."];
      description: String_.t option
        [@ocaml.doc
          "A description for the IPAM prefix list resolver to help you identify its purpose and configuration."];
      addressFamily: AddressFamily.t
        [@ocaml.doc
          "The address family for the IPAM prefix list resolver. Valid values are ipv4 and ipv6. You must create separate resolvers for IPv4 and IPv6 CIDRs as they cannot be mixed in the same resolver."];
      rules: IpamPrefixListResolverRuleRequestSet.t option
        [@ocaml.doc
          "The CIDR selection rules for the resolver. CIDR selection rules define the business logic for selecting CIDRs from IPAM. If a CIDR matches any of the rules, it will be included. If a rule has multiple conditions, the CIDR has to match every condition of that rule. You can create a prefix list resolver without any CIDR selection rules, but it will generate empty versions (containing no CIDRs) until you add rules."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the IPAM prefix list resolver during creation. Tags help you organize and manage your Amazon Web Services resources."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."]}
    let context_ = "CreateIpamPrefixListResolverRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?rules ->
          fun ?tagSpecifications ->
            fun ?clientToken ->
              fun ~ipamId ->
                fun ~addressFamily ->
                  fun () ->
                    {
                      dryRun;
                      description;
                      rules;
                      tagSpecifications;
                      clientToken;
                      ipamId;
                      addressFamily
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("AddressFamily", (Some (AddressFamily.to_value x.addressFamily)));
        ("Rule",
          (Option.map x.rules
             ~f:IpamPrefixListResolverRuleRequestSet.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let rules =
        (Option.map ~f:IpamPrefixListResolverRuleRequestSet.of_xml)
          (Xml.child xml_arg0 "Rule") in
      let addressFamily =
        AddressFamily.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AddressFamily") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ?rules ~addressFamily ?description
        ~ipamId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let rules =
        field_map json__ "Rules" IpamPrefixListResolverRuleRequestSet.of_json in
      let addressFamily =
        field_map_exn json__ "AddressFamily" AddressFamily.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ?rules ~addressFamily ?description
        ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM prefix list resolver. An IPAM prefix list resolver is a component that manages the synchronization between IPAM's CIDR selection rules and customer-managed prefix lists. It automates connectivity configurations by selecting CIDRs from IPAM's database based on your business logic and synchronizing them with prefix lists used in resources such as VPC route tables and security groups. For more information about IPAM prefix list resolver, see Automate prefix list updates with IPAM in the Amazon VPC IPAM User Guide."]
module CreateIpamPoolResult =
  struct
    type nonrec t =
      {
      ipamPool: IpamPool.t option
        [@ocaml.doc "Information about the IPAM pool created."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPool = fun () -> { ipamPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPool", (Option.map x.ipamPool ~f:IpamPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPool =
        (Option.map ~f:IpamPool.of_xml) (Xml.child xml_arg0 "ipamPool") in
      make ?ipamPool ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPool = field_map json__ "IpamPool" IpamPool.of_json in
      make ?ipamPool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an IP address pool for Amazon VPC IP Address Manager (IPAM). In IPAM, a pool is a collection of contiguous IP addresses CIDRs. Pools enable you to organize your IP addresses according to your routing and security needs. For example, if you have separate routing and security needs for development and production applications, you can create a pool for each. For more information, see Create a top-level pool in the Amazon VPC IPAM User Guide."]
module CreateIpamPoolRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamScopeId: IpamScopeId.t
        [@ocaml.doc
          "The ID of the scope in which you would like to create the IPAM pool."];
      locale: String_.t option
        [@ocaml.doc
          "The locale for the pool should be one of the following: An Amazon Web Services Region where you want this IPAM pool to be available for allocations. The network border group for an Amazon Web Services Local Zone where you want this IPAM pool to be available for allocations (supported Local Zones). This option is only available for IPAM IPv4 pools in the public scope. Possible values: Any Amazon Web Services Region or supported Amazon Web Services Local Zone. Default is none and means any locale."];
      sourceIpamPoolId: IpamPoolId.t option
        [@ocaml.doc
          "The ID of the source IPAM pool. Use this option to create a pool within an existing pool. Note that the CIDR you provision for the pool within the source pool must be available in the source pool's CIDR range."];
      description: String_.t option
        [@ocaml.doc "A description for the IPAM pool."];
      addressFamily: AddressFamily.t
        [@ocaml.doc
          "The IP protocol assigned to this IPAM pool. You must choose either IPv4 or IPv6 protocol for a pool."];
      autoImport: Boolean.t option
        [@ocaml.doc
          "If selected, IPAM will continuously look for resources within the CIDR range of this pool and automatically import them as allocations into your IPAM. The CIDRs that will be allocated for these resources must not already be allocated to other resources in order for the import to succeed. IPAM will import a CIDR regardless of its compliance with the pool's allocation rules, so a resource might be imported and subsequently marked as noncompliant. If IPAM discovers multiple CIDRs that overlap, IPAM will import the largest CIDR only. If IPAM discovers multiple CIDRs with matching CIDRs, IPAM will randomly import one of them only. A locale must be set on the pool for this feature to work."];
      publiclyAdvertisable: Boolean.t option
        [@ocaml.doc
          "Determines if the pool is publicly advertisable. The request can only contain PubliclyAdvertisable if AddressFamily is ipv6 and PublicIpSource is byoip."];
      allocationMinNetmaskLength: IpamNetmaskLength.t option
        [@ocaml.doc
          "The minimum netmask length required for CIDR allocations in this IPAM pool to be compliant. The minimum netmask length must be less than the maximum netmask length. Possible netmask lengths for IPv4 addresses are 0 - 32. Possible netmask lengths for IPv6 addresses are 0 - 128."];
      allocationMaxNetmaskLength: IpamNetmaskLength.t option
        [@ocaml.doc
          "The maximum netmask length possible for CIDR allocations in this IPAM pool to be compliant. The maximum netmask length must be greater than the minimum netmask length. Possible netmask lengths for IPv4 addresses are 0 - 32. Possible netmask lengths for IPv6 addresses are 0 - 128."];
      allocationDefaultNetmaskLength: IpamNetmaskLength.t option
        [@ocaml.doc
          "The default netmask length for allocations added to this pool. If, for example, the CIDR assigned to this pool is 10.0.0.0/8 and you enter 16 here, new allocations will default to 10.0.0.0/16."];
      allocationResourceTags: RequestIpamResourceTagList.t option
        [@ocaml.doc
          "Tags that are required for resources that use CIDRs from this IPAM pool. Resources that do not have these tags will not be allowed to allocate space from the pool. If the resources have their tags changed after they have allocated space or if the allocation tagging requirements are changed on the pool, the resource may be marked as noncompliant."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      awsService: IpamPoolAwsService.t option
        [@ocaml.doc
          "Limits which service in Amazon Web Services that the pool can be used in. \"ec2\", for example, allows users to use space for Elastic IP addresses and VPCs."];
      publicIpSource: IpamPoolPublicIpSource.t option
        [@ocaml.doc
          "The IP address source for pools in the public scope. Only used for provisioning IP address CIDRs to pools in the public scope. Default is byoip. For more information, see Create IPv6 pools in the Amazon VPC IPAM User Guide. By default, you can add only one Amazon-provided IPv6 CIDR block to a top-level IPv6 pool if PublicIpSource is amazon. For information on increasing the default limit, see Quotas for your IPAM in the Amazon VPC IPAM User Guide."];
      sourceResource: IpamPoolSourceResourceRequest.t option
        [@ocaml.doc
          "The resource used to provision CIDRs to a resource planning pool."]}
    let context_ = "CreateIpamPoolRequest"
    let make ?dryRun =
      fun ?locale ->
        fun ?sourceIpamPoolId ->
          fun ?description ->
            fun ?autoImport ->
              fun ?publiclyAdvertisable ->
                fun ?allocationMinNetmaskLength ->
                  fun ?allocationMaxNetmaskLength ->
                    fun ?allocationDefaultNetmaskLength ->
                      fun ?allocationResourceTags ->
                        fun ?tagSpecifications ->
                          fun ?clientToken ->
                            fun ?awsService ->
                              fun ?publicIpSource ->
                                fun ?sourceResource ->
                                  fun ~ipamScopeId ->
                                    fun ~addressFamily ->
                                      fun () ->
                                        {
                                          dryRun;
                                          locale;
                                          sourceIpamPoolId;
                                          description;
                                          autoImport;
                                          publiclyAdvertisable;
                                          allocationMinNetmaskLength;
                                          allocationMaxNetmaskLength;
                                          allocationDefaultNetmaskLength;
                                          allocationResourceTags;
                                          tagSpecifications;
                                          clientToken;
                                          awsService;
                                          publicIpSource;
                                          sourceResource;
                                          ipamScopeId;
                                          addressFamily
                                        }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamScopeId", (Some (IpamScopeId.to_value x.ipamScopeId)));
        ("Locale", (Option.map x.locale ~f:String_.to_value));
        ("SourceIpamPoolId",
          (Option.map x.sourceIpamPoolId ~f:IpamPoolId.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("AddressFamily", (Some (AddressFamily.to_value x.addressFamily)));
        ("AutoImport", (Option.map x.autoImport ~f:Boolean.to_value));
        ("PubliclyAdvertisable",
          (Option.map x.publiclyAdvertisable ~f:Boolean.to_value));
        ("AllocationMinNetmaskLength",
          (Option.map x.allocationMinNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationMaxNetmaskLength",
          (Option.map x.allocationMaxNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationDefaultNetmaskLength",
          (Option.map x.allocationDefaultNetmaskLength
             ~f:IpamNetmaskLength.to_value));
        ("AllocationResourceTag",
          (Option.map x.allocationResourceTags
             ~f:RequestIpamResourceTagList.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("AwsService",
          (Option.map x.awsService ~f:IpamPoolAwsService.to_value));
        ("PublicIpSource",
          (Option.map x.publicIpSource ~f:IpamPoolPublicIpSource.to_value));
        ("SourceResource",
          (Option.map x.sourceResource
             ~f:IpamPoolSourceResourceRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceResource =
        (Option.map ~f:IpamPoolSourceResourceRequest.of_xml)
          (Xml.child xml_arg0 "SourceResource") in
      let publicIpSource =
        (Option.map ~f:IpamPoolPublicIpSource.of_xml)
          (Xml.child xml_arg0 "PublicIpSource") in
      let awsService =
        (Option.map ~f:IpamPoolAwsService.of_xml)
          (Xml.child xml_arg0 "AwsService") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let allocationResourceTags =
        (Option.map ~f:RequestIpamResourceTagList.of_xml)
          (Xml.child xml_arg0 "AllocationResourceTag") in
      let allocationDefaultNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationDefaultNetmaskLength") in
      let allocationMaxNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationMaxNetmaskLength") in
      let allocationMinNetmaskLength =
        (Option.map ~f:IpamNetmaskLength.of_xml)
          (Xml.child xml_arg0 "AllocationMinNetmaskLength") in
      let publiclyAdvertisable =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PubliclyAdvertisable") in
      let autoImport =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoImport") in
      let addressFamily =
        AddressFamily.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AddressFamily") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceIpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "SourceIpamPoolId") in
      let locale =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Locale") in
      let ipamScopeId =
        IpamScopeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamScopeId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?sourceResource ?publicIpSource ?awsService ?clientToken
        ?tagSpecifications ?allocationResourceTags
        ?allocationDefaultNetmaskLength ?allocationMaxNetmaskLength
        ?allocationMinNetmaskLength ?publiclyAdvertisable ?autoImport
        ~addressFamily ?description ?sourceIpamPoolId ?locale ~ipamScopeId
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceResource =
        field_map json__ "SourceResource"
          IpamPoolSourceResourceRequest.of_json in
      let publicIpSource =
        field_map json__ "PublicIpSource" IpamPoolPublicIpSource.of_json in
      let awsService =
        field_map json__ "AwsService" IpamPoolAwsService.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let allocationResourceTags =
        field_map json__ "AllocationResourceTags"
          RequestIpamResourceTagList.of_json in
      let allocationDefaultNetmaskLength =
        field_map json__ "AllocationDefaultNetmaskLength"
          IpamNetmaskLength.of_json in
      let allocationMaxNetmaskLength =
        field_map json__ "AllocationMaxNetmaskLength"
          IpamNetmaskLength.of_json in
      let allocationMinNetmaskLength =
        field_map json__ "AllocationMinNetmaskLength"
          IpamNetmaskLength.of_json in
      let publiclyAdvertisable =
        field_map json__ "PubliclyAdvertisable" Boolean.of_json in
      let autoImport = field_map json__ "AutoImport" Boolean.of_json in
      let addressFamily =
        field_map_exn json__ "AddressFamily" AddressFamily.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let sourceIpamPoolId =
        field_map json__ "SourceIpamPoolId" IpamPoolId.of_json in
      let locale = field_map json__ "Locale" String_.of_json in
      let ipamScopeId =
        field_map_exn json__ "IpamScopeId" IpamScopeId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?sourceResource ?publicIpSource ?awsService ?clientToken
        ?tagSpecifications ?allocationResourceTags
        ?allocationDefaultNetmaskLength ?allocationMaxNetmaskLength
        ?allocationMinNetmaskLength ?publiclyAdvertisable ?autoImport
        ~addressFamily ?description ?sourceIpamPoolId ?locale ~ipamScopeId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an IP address pool for Amazon VPC IP Address Manager (IPAM). In IPAM, a pool is a collection of contiguous IP addresses CIDRs. Pools enable you to organize your IP addresses according to your routing and security needs. For example, if you have separate routing and security needs for development and production applications, you can create a pool for each. For more information, see Create a top-level pool in the Amazon VPC IPAM User Guide."]
module CreateIpamPolicyResult =
  struct
    type nonrec t =
      {
      ipamPolicy: IpamPolicy.t option
        [@ocaml.doc
          "Information about the created IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPolicy = fun () -> { ipamPolicy }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPolicy", (Option.map x.ipamPolicy ~f:IpamPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPolicy =
        (Option.map ~f:IpamPolicy.of_xml) (Xml.child xml_arg0 "ipamPolicy") in
      make ?ipamPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPolicy = field_map json__ "IpamPolicy" IpamPolicy.of_json in
      make ?ipamPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations. For more information, see Define public IPv4 allocation strategy with IPAM policies in the Amazon VPC IPAM User Guide."]
module CreateIpamPolicyRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the IPAM policy."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier to ensure the idempotency of the request."];
      ipamId: IpamId.t
        [@ocaml.doc
          "The ID of the IPAM for which you're creating the policy."]}
    let context_ = "CreateIpamPolicyRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ?clientToken ->
          fun ~ipamId ->
            fun () -> { dryRun; tagSpecifications; clientToken; ipamId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~ipamId ?clientToken ?tagSpecifications ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~ipamId ?clientToken ?tagSpecifications ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an IPAM policy. An IPAM policy is a set of rules that define how public IPv4 addresses from IPAM pools are allocated to Amazon Web Services resources. Each rule maps an Amazon Web Services service to IPAM pools that the service will use to get IP addresses. A single policy can have multiple rules and be applied to multiple Amazon Web Services Regions. If the IPAM pool run out of addresses then the services fallback to Amazon-provided IP addresses. A policy can be applied to an individual Amazon Web Services account or an entity within Amazon Web Services Organizations. For more information, see Define public IPv4 allocation strategy with IPAM policies in the Amazon VPC IPAM User Guide."]
module CreateIpamExternalResourceVerificationTokenResult =
  struct
    type nonrec t =
      {
      ipamExternalResourceVerificationToken:
        IpamExternalResourceVerificationToken.t option
        [@ocaml.doc "The verification token."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamExternalResourceVerificationToken =
      fun () -> { ipamExternalResourceVerificationToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamExternalResourceVerificationToken",
           (Option.map x.ipamExternalResourceVerificationToken
              ~f:IpamExternalResourceVerificationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamExternalResourceVerificationToken =
        (Option.map ~f:IpamExternalResourceVerificationToken.of_xml)
          (Xml.child xml_arg0 "ipamExternalResourceVerificationToken") in
      make ?ipamExternalResourceVerificationToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamExternalResourceVerificationToken =
        field_map json__ "IpamExternalResourceVerificationToken"
          IpamExternalResourceVerificationToken.of_json in
      make ?ipamExternalResourceVerificationToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a verification token. A verification token is an Amazon Web Services-generated random value that you can use to prove ownership of an external resource. For example, you can use a verification token to validate that you control a public IP address range when you bring an IP address range to Amazon Web Services (BYOIP)."]
module CreateIpamExternalResourceVerificationTokenRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t
        [@ocaml.doc "The ID of the IPAM that will create the token."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "Token tags."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."]}
    let context_ = "CreateIpamExternalResourceVerificationTokenRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ?clientToken ->
          fun ~ipamId ->
            fun () -> { dryRun; tagSpecifications; clientToken; ipamId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ~ipamId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ~ipamId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a verification token. A verification token is an Amazon Web Services-generated random value that you can use to prove ownership of an external resource. For example, you can use a verification token to validate that you control a public IP address range when you bring an IP address range to Amazon Web Services (BYOIP)."]
module CreateInterruptibleCapacityReservationAllocationResult =
  struct
    type nonrec t =
      {
      sourceCapacityReservationId: CapacityReservationId.t option
        [@ocaml.doc
          "The ID of the source Capacity Reservation from which the interruptible Capacity Reservation was created."];
      targetInstanceCount: Integer.t option
        [@ocaml.doc
          "The number of instances allocated to the interruptible reservation."];
      status: InterruptibleCapacityReservationAllocationStatus.t option
        [@ocaml.doc
          "The current status of the allocation request (creating, active, updating)."];
      interruptionType: InterruptionType.t option
        [@ocaml.doc
          "The type of interruption applied to the interruptible reservation."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?sourceCapacityReservationId =
      fun ?targetInstanceCount ->
        fun ?status ->
          fun ?interruptionType ->
            fun () ->
              {
                sourceCapacityReservationId;
                targetInstanceCount;
                status;
                interruptionType
              }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sourceCapacityReservationId",
           (Option.map x.sourceCapacityReservationId
              ~f:CapacityReservationId.to_value));
        ("targetInstanceCount",
          (Option.map x.targetInstanceCount ~f:Integer.to_value));
        ("status",
          (Option.map x.status
             ~f:InterruptibleCapacityReservationAllocationStatus.to_value));
        ("interruptionType",
          (Option.map x.interruptionType ~f:InterruptionType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let interruptionType =
        (Option.map ~f:InterruptionType.of_xml)
          (Xml.child xml_arg0 "interruptionType") in
      let status =
        (Option.map
           ~f:InterruptibleCapacityReservationAllocationStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let targetInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "targetInstanceCount") in
      let sourceCapacityReservationId =
        (Option.map ~f:CapacityReservationId.of_xml)
          (Xml.child xml_arg0 "sourceCapacityReservationId") in
      make ?interruptionType ?status ?targetInstanceCount
        ?sourceCapacityReservationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let interruptionType =
        field_map json__ "InterruptionType" InterruptionType.of_json in
      let status =
        field_map json__ "Status"
          InterruptibleCapacityReservationAllocationStatus.of_json in
      let targetInstanceCount =
        field_map json__ "TargetInstanceCount" Integer.of_json in
      let sourceCapacityReservationId =
        field_map json__ "SourceCapacityReservationId"
          CapacityReservationId.of_json in
      make ?interruptionType ?status ?targetInstanceCount
        ?sourceCapacityReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an interruptible Capacity Reservation by specifying the number of unused instances you want to allocate from your source reservation. This helps you make unused capacity available for other workloads within your account while maintaining control to reclaim it."]
module CreateInterruptibleCapacityReservationAllocationRequest =
  struct
    type nonrec t =
      {
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc
          "The ID of the source Capacity Reservation from which to create the interruptible Capacity Reservation. Your Capacity Reservation must be in active state with no end date set and have available capacity for allocation."];
      instanceCount: Integer.t
        [@ocaml.doc
          "The number of instances to allocate from your source reservation. You can only allocate available instances (also called unused capacity)."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the interruptible Capacity Reservation during creation."]}
    let context_ = "CreateInterruptibleCapacityReservationAllocationRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~capacityReservationId ->
            fun ~instanceCount ->
              fun () ->
                {
                  clientToken;
                  dryRun;
                  tagSpecifications;
                  capacityReservationId;
                  instanceCount
                }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      make ?tagSpecifications ?dryRun ?clientToken ~instanceCount
        ~capacityReservationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let instanceCount =
        field_map_exn json__ "InstanceCount" Integer.of_json in
      let capacityReservationId =
        field_map_exn json__ "CapacityReservationId"
          CapacityReservationId.of_json in
      make ?tagSpecifications ?dryRun ?clientToken ~instanceCount
        ~capacityReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an interruptible Capacity Reservation by specifying the number of unused instances you want to allocate from your source reservation. This helps you make unused capacity available for other workloads within your account while maintaining control to reclaim it."]
module CreateInternetGatewayResult =
  struct
    type nonrec t =
      {
      internetGateway: InternetGateway.t option
        [@ocaml.doc "Information about the internet gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?internetGateway = fun () -> { internetGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("internetGateway",
           (Option.map x.internetGateway ~f:InternetGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let internetGateway =
        (Option.map ~f:InternetGateway.of_xml)
          (Xml.child xml_arg0 "internetGateway") in
      make ?internetGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let internetGateway =
        field_map json__ "InternetGateway" InternetGateway.of_json in
      make ?internetGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an internet gateway for use with a VPC. After creating the internet gateway, you attach it to a VPC using AttachInternetGateway. For more information, see Internet gateways in the Amazon VPC User Guide."]
module CreateInternetGatewayRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the internet gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?tagSpecifications =
      fun ?dryRun -> fun () -> { tagSpecifications; dryRun }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?dryRun ?tagSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ?dryRun ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an internet gateway for use with a VPC. After creating the internet gateway, you attach it to a VPC using AttachInternetGateway. For more information, see Internet gateways in the Amazon VPC User Guide."]
module CreateInstanceExportTaskResult =
  struct
    type nonrec t =
      {
      exportTask: ExportTask.t option
        [@ocaml.doc "Information about the export instance task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exportTask = fun () -> { exportTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exportTask", (Option.map x.exportTask ~f:ExportTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTask =
        (Option.map ~f:ExportTask.of_xml) (Xml.child xml_arg0 "exportTask") in
      make ?exportTask ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exportTask = field_map json__ "ExportTask" ExportTask.of_json in
      make ?exportTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports a running or stopped instance to an Amazon S3 bucket. For information about the prerequisites for your Amazon S3 bucket, supported operating systems, image formats, and known limitations for the types of instances you can export, see Exporting an instance as a VM Using VM Import/Export in the VM Import/Export User Guide."]
module CreateInstanceExportTaskRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the export instance task during creation."];
      description: String_.t option
        [@ocaml.doc
          "A description for the conversion task or the resource being exported. The maximum length is 255 characters."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      targetEnvironment: ExportEnvironment.t
        [@ocaml.doc "The target virtualization environment."];
      exportToS3Task: ExportToS3TaskSpecification.t
        [@ocaml.doc "The format and location for an export instance task."]}
    let context_ = "CreateInstanceExportTaskRequest"
    let make ?tagSpecifications =
      fun ?description ->
        fun ~instanceId ->
          fun ~targetEnvironment ->
            fun ~exportToS3Task ->
              fun () ->
                {
                  tagSpecifications;
                  description;
                  instanceId;
                  targetEnvironment;
                  exportToS3Task
                }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("targetEnvironment",
          (Some (ExportEnvironment.to_value x.targetEnvironment)));
        ("exportToS3",
          (Some (ExportToS3TaskSpecification.to_value x.exportToS3Task)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportToS3Task =
        ExportToS3TaskSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "exportToS3") in
      let targetEnvironment =
        ExportEnvironment.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "targetEnvironment") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ~exportToS3Task ~targetEnvironment ~instanceId ?description
        ?tagSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exportToS3Task =
        field_map_exn json__ "ExportToS3Task"
          ExportToS3TaskSpecification.of_json in
      let targetEnvironment =
        field_map_exn json__ "TargetEnvironment" ExportEnvironment.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ~exportToS3Task ~targetEnvironment ~instanceId ?description
        ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports a running or stopped instance to an Amazon S3 bucket. For information about the prerequisites for your Amazon S3 bucket, supported operating systems, image formats, and known limitations for the types of instances you can export, see Exporting an instance as a VM Using VM Import/Export in the VM Import/Export User Guide."]
module CreateInstanceEventWindowResult =
  struct
    type nonrec t =
      {
      instanceEventWindow: InstanceEventWindow.t option
        [@ocaml.doc "Information about the event window."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindow = fun () -> { instanceEventWindow }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindow",
           (Option.map x.instanceEventWindow ~f:InstanceEventWindow.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindow =
        (Option.map ~f:InstanceEventWindow.of_xml)
          (Xml.child xml_arg0 "instanceEventWindow") in
      make ?instanceEventWindow ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceEventWindow =
        field_map json__ "InstanceEventWindow" InstanceEventWindow.of_json in
      make ?instanceEventWindow ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an event window in which scheduled events for the associated Amazon EC2 instances can run. You can define either a set of time ranges or a cron expression when creating the event window, but not both. All event window times are in UTC. You can create up to 200 event windows per Amazon Web Services Region. When you create the event window, targets (instance IDs, Dedicated Host IDs, or tags) are not yet associated with it. To ensure that the event window can be used, you must associate one or more targets with it by using the AssociateInstanceEventWindow API. Event windows are applicable only for scheduled events that stop, reboot, or terminate instances. Event windows are not applicable for: Expedited scheduled events and network maintenance events. Unscheduled maintenance such as AutoRecovery and unplanned reboots. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module CreateInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      name: String_.t option [@ocaml.doc "The name of the event window."];
      timeRanges: InstanceEventWindowTimeRangeRequestSet.t option
        [@ocaml.doc
          "The time range for the event window. If you specify a time range, you can't specify a cron expression."];
      cronExpression: InstanceEventWindowCronExpression.t option
        [@ocaml.doc
          "The cron expression for the event window, for example, * 0-4,20-23 * * 1,5. If you specify a cron expression, you can't specify a time range. Constraints: Only hour and day of the week values are supported. For day of the week values, you can specify either integers 0 through 6, or alternative single values SUN through SAT. The minute, month, and year must be specified by *. The hour value must be one or a multiple range, for example, 0-4 or 0-4,20-23. Each hour range must be >= 2 hours, for example, 0-2 or 20-23. The event window must be >= 4 hours. The combined total time ranges in the event window must be >= 4 hours. For more information about cron expressions, see cron on the Wikipedia website."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the event window."]}
    let make ?dryRun =
      fun ?name ->
        fun ?timeRanges ->
          fun ?cronExpression ->
            fun ?tagSpecifications ->
              fun () ->
                { dryRun; name; timeRanges; cronExpression; tagSpecifications
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("TimeRange",
          (Option.map x.timeRanges
             ~f:InstanceEventWindowTimeRangeRequestSet.to_value));
        ("CronExpression",
          (Option.map x.cronExpression
             ~f:InstanceEventWindowCronExpression.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let cronExpression =
        (Option.map ~f:InstanceEventWindowCronExpression.of_xml)
          (Xml.child xml_arg0 "CronExpression") in
      let timeRanges =
        (Option.map ~f:InstanceEventWindowTimeRangeRequestSet.of_xml)
          (Xml.child xml_arg0 "TimeRange") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?cronExpression ?timeRanges ?name ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let cronExpression =
        field_map json__ "CronExpression"
          InstanceEventWindowCronExpression.of_json in
      let timeRanges =
        field_map json__ "TimeRanges"
          InstanceEventWindowTimeRangeRequestSet.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ?cronExpression ?timeRanges ?name ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an event window in which scheduled events for the associated Amazon EC2 instances can run. You can define either a set of time ranges or a cron expression when creating the event window, but not both. All event window times are in UTC. You can create up to 200 event windows per Amazon Web Services Region. When you create the event window, targets (instance IDs, Dedicated Host IDs, or tags) are not yet associated with it. To ensure that the event window can be used, you must associate one or more targets with it by using the AssociateInstanceEventWindow API. Event windows are applicable only for scheduled events that stop, reboot, or terminate instances. Event windows are not applicable for: Expedited scheduled events and network maintenance events. Unscheduled maintenance such as AutoRecovery and unplanned reboots. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module CreateInstanceConnectEndpointResult =
  struct
    type nonrec t =
      {
      instanceConnectEndpoint: Ec2InstanceConnectEndpoint.t option
        [@ocaml.doc "Information about the EC2 Instance Connect Endpoint."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive idempotency token provided by the client in the the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceConnectEndpoint =
      fun ?clientToken -> fun () -> { instanceConnectEndpoint; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceConnectEndpoint",
           (Option.map x.instanceConnectEndpoint
              ~f:Ec2InstanceConnectEndpoint.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let instanceConnectEndpoint =
        (Option.map ~f:Ec2InstanceConnectEndpoint.of_xml)
          (Xml.child xml_arg0 "instanceConnectEndpoint") in
      make ?clientToken ?instanceConnectEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let instanceConnectEndpoint =
        field_map json__ "InstanceConnectEndpoint"
          Ec2InstanceConnectEndpoint.of_json in
      make ?clientToken ?instanceConnectEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an EC2 Instance Connect Endpoint. An EC2 Instance Connect Endpoint allows you to connect to an instance, without requiring the instance to have a public IPv4 or public IPv6 address. For more information, see Connect to your instances using EC2 Instance Connect Endpoint in the Amazon EC2 User Guide."]
module CreateInstanceConnectEndpointRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      subnetId: SubnetId.t
        [@ocaml.doc
          "The ID of the subnet in which to create the EC2 Instance Connect Endpoint."];
      securityGroupIds: SecurityGroupIdStringListRequest.t option
        [@ocaml.doc
          "One or more security groups to associate with the endpoint. If you don't specify a security group, the default security group for your VPC will be associated with the endpoint."];
      preserveClientIp: Boolean.t option
        [@ocaml.doc
          "Indicates whether the client IP address is preserved as the source. The following are the possible values. true - Use the client IP address as the source. false - Use the network interface IP address as the source. PreserveClientIp is only supported on IPv4 EC2 Instance Connect Endpoints. To use PreserveClientIp, the value for IpAddressType must be ipv4. Default: false"];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the EC2 Instance Connect Endpoint during creation."];
      ipAddressType: IpAddressType.t option
        [@ocaml.doc
          "The IP address type of the endpoint. If no value is specified, the default value is determined by the IP address type of the subnet: dualstack - If the subnet has both IPv4 and IPv6 CIDRs ipv4 - If the subnet has only IPv4 CIDRs ipv6 - If the subnet has only IPv6 CIDRs PreserveClientIp is only supported on IPv4 EC2 Instance Connect Endpoints. To use PreserveClientIp, the value for IpAddressType must be ipv4."]}
    let context_ = "CreateInstanceConnectEndpointRequest"
    let make ?dryRun =
      fun ?securityGroupIds ->
        fun ?preserveClientIp ->
          fun ?clientToken ->
            fun ?tagSpecifications ->
              fun ?ipAddressType ->
                fun ~subnetId ->
                  fun () ->
                    {
                      dryRun;
                      securityGroupIds;
                      preserveClientIp;
                      clientToken;
                      tagSpecifications;
                      ipAddressType;
                      subnetId
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:SecurityGroupIdStringListRequest.to_value));
        ("PreserveClientIp",
          (Option.map x.preserveClientIp ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("IpAddressType",
          (Option.map x.ipAddressType ~f:IpAddressType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipAddressType =
        (Option.map ~f:IpAddressType.of_xml)
          (Xml.child xml_arg0 "IpAddressType") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let preserveClientIp =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "PreserveClientIp") in
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIdStringListRequest.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?ipAddressType ?tagSpecifications ?clientToken ?preserveClientIp
        ?securityGroupIds ~subnetId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipAddressType =
        field_map json__ "IpAddressType" IpAddressType.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let preserveClientIp =
        field_map json__ "PreserveClientIp" Boolean.of_json in
      let securityGroupIds =
        field_map json__ "SecurityGroupIds"
          SecurityGroupIdStringListRequest.of_json in
      let subnetId = field_map_exn json__ "SubnetId" SubnetId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?ipAddressType ?tagSpecifications ?clientToken ?preserveClientIp
        ?securityGroupIds ~subnetId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an EC2 Instance Connect Endpoint. An EC2 Instance Connect Endpoint allows you to connect to an instance, without requiring the instance to have a public IPv4 or public IPv6 address. For more information, see Connect to your instances using EC2 Instance Connect Endpoint in the Amazon EC2 User Guide."]
module CreateImageUsageReportResult =
  struct
    type nonrec t =
      {
      reportId: ImageUsageReportId.t option
        [@ocaml.doc "The ID of the report."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reportId = fun () -> { reportId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reportId", (Option.map x.reportId ~f:ImageUsageReportId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportId =
        (Option.map ~f:ImageUsageReportId.of_xml)
          (Xml.child xml_arg0 "reportId") in
      make ?reportId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportId = field_map json__ "ReportId" ImageUsageReportId.of_json in
      make ?reportId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a report that shows how your image is used across other Amazon Web Services accounts. The report provides visibility into which accounts are using the specified image, and how many resources (EC2 instances or launch templates) are referencing it. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module CreateImageUsageReportRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t [@ocaml.doc "The ID of the image to report on."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      resourceTypes: ImageUsageResourceTypeRequestList.t
        [@ocaml.doc "The resource types to include in the report."];
      accountIds: ImageUsageReportUserIdStringList.t option
        [@ocaml.doc
          "The Amazon Web Services account IDs to include in the report. To include all accounts, omit this parameter."];
      clientToken: CreateImageUsageReportClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure idempotency of the request."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the report on creation. The ResourceType must be set to image-usage-report; any other value will cause the report creation to fail. To tag a report after it has been created, see CreateTags."]}
    let context_ = "CreateImageUsageReportRequest"
    let make ?dryRun =
      fun ?accountIds ->
        fun ?clientToken ->
          fun ?tagSpecifications ->
            fun ~imageId ->
              fun ~resourceTypes ->
                fun () ->
                  {
                    dryRun;
                    accountIds;
                    clientToken;
                    tagSpecifications;
                    imageId;
                    resourceTypes
                  }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ResourceType",
          (Some (ImageUsageResourceTypeRequestList.to_value x.resourceTypes)));
        ("AccountId",
          (Option.map x.accountIds
             ~f:ImageUsageReportUserIdStringList.to_value));
        ("ClientToken",
          (Option.map x.clientToken
             ~f:CreateImageUsageReportClientToken.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:CreateImageUsageReportClientToken.of_xml)
          (Xml.child xml_arg0 "ClientToken") in
      let accountIds =
        (Option.map ~f:ImageUsageReportUserIdStringList.of_xml)
          (Xml.child xml_arg0 "AccountId") in
      let resourceTypes =
        ImageUsageResourceTypeRequestList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?tagSpecifications ?clientToken ?accountIds ~resourceTypes ?dryRun
        ~imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken =
        field_map json__ "ClientToken"
          CreateImageUsageReportClientToken.of_json in
      let accountIds =
        field_map json__ "AccountIds"
          ImageUsageReportUserIdStringList.of_json in
      let resourceTypes =
        field_map_exn json__ "ResourceTypes"
          ImageUsageResourceTypeRequestList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let imageId = field_map_exn json__ "ImageId" ImageId.of_json in
      make ?tagSpecifications ?clientToken ?accountIds ~resourceTypes ?dryRun
        ~imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a report that shows how your image is used across other Amazon Web Services accounts. The report provides visibility into which accounts are using the specified image, and how many resources (EC2 instances or launch templates) are referencing it. For more information, see View your AMI usage in the Amazon EC2 User Guide."]
module CreateImageResult =
  struct
    type nonrec t =
      {
      imageId: String_.t option [@ocaml.doc "The ID of the new AMI."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let imageId = field_map json__ "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Amazon EBS-backed AMI from an Amazon EBS-backed instance that is either running or stopped. If you customized your instance with instance store volumes or Amazon EBS volumes in addition to the root device volume, the new AMI contains block device mapping information for those volumes. When you launch an instance from this new AMI, the instance automatically launches with those additional volumes. The location of the source instance determines where you can create the snapshots of the AMI: If the source instance is in a Region, you must create the snapshots in the same Region as the instance. If the source instance is in a Local Zone, you can create the snapshots in the same Local Zone or in its parent Region. For more information, see Create an Amazon EBS-backed AMI in the Amazon Elastic Compute Cloud User Guide."]
module CreateImageRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the AMI and snapshots on creation. You can tag the AMI, the snapshots, or both. To tag the AMI, the value for ResourceType must be image. To tag the snapshots that are created of the root volume and of other Amazon EBS volumes that are attached to the instance, the value for ResourceType must be snapshot. The same tag is applied to all of the snapshots that are created. If you specify other values for ResourceType, the request fails. To tag an AMI or snapshot after it has been created, see CreateTags."];
      snapshotLocation: SnapshotLocationEnum.t option
        [@ocaml.doc
          "Only supported for instances in Local Zones. If the source instance is not in a Local Zone, omit this parameter. The Amazon S3 location where the snapshots will be stored. To create local snapshots in the same Local Zone as the source instance, specify local. To create regional snapshots in the parent Region of the Local Zone, specify regional or omit this parameter. Default: regional"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      name: ImageNameRequest.t
        [@ocaml.doc
          "A name for the new image. Constraints: 3-128 alphanumeric characters, parentheses (()), square brackets (\\[\\]), spaces ( ), periods (.), slashes (/), dashes (-), single quotes ('), at-signs (\\@), or underscores(_)"];
      description: ImageDescriptionRequest.t option
        [@ocaml.doc "A description for the new image."];
      noReboot: Boolean.t option
        [@ocaml.doc
          "Indicates whether or not the instance should be automatically rebooted before creating the image. Specify one of the following values: true - The instance is not rebooted before creating the image. This creates crash-consistent snapshots that include only the data that has been written to the volumes at the time the snapshots are created. Buffered data and data in memory that has not yet been written to the volumes is not included in the snapshots. false - The instance is rebooted before creating the image. This ensures that all buffered data and data in memory is written to the volumes before the snapshots are created. Default: false"];
      blockDeviceMappings: BlockDeviceMappingRequestList.t option
        [@ocaml.doc
          "The block device mappings. When using the CreateImage action: You can't change the volume size using the VolumeSize parameter. If you want a different volume size, you must first change the volume size of the source instance. You can't modify the encryption status of existing volumes or snapshots. To create an AMI with volumes or snapshots that have a different encryption status (for example, where the source volume and snapshots are unencrypted, and you want to create an AMI with encrypted volumes or snapshots), copy the image instead. The only option that can be changed for existing mappings or snapshots is DeleteOnTermination."]}
    let context_ = "CreateImageRequest"
    let make ?tagSpecifications =
      fun ?snapshotLocation ->
        fun ?dryRun ->
          fun ?description ->
            fun ?noReboot ->
              fun ?blockDeviceMappings ->
                fun ~instanceId ->
                  fun ~name ->
                    fun () ->
                      {
                        tagSpecifications;
                        snapshotLocation;
                        dryRun;
                        description;
                        noReboot;
                        blockDeviceMappings;
                        instanceId;
                        name
                      }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SnapshotLocation",
          (Option.map x.snapshotLocation ~f:SnapshotLocationEnum.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("name", (Some (ImageNameRequest.to_value x.name)));
        ("description",
          (Option.map x.description ~f:ImageDescriptionRequest.to_value));
        ("noReboot", (Option.map x.noReboot ~f:Boolean.to_value));
        ("blockDeviceMapping",
          (Option.map x.blockDeviceMappings
             ~f:BlockDeviceMappingRequestList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let blockDeviceMappings =
        (Option.map ~f:BlockDeviceMappingRequestList.of_xml)
          (Xml.child xml_arg0 "blockDeviceMapping") in
      let noReboot =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "noReboot") in
      let description =
        (Option.map ~f:ImageDescriptionRequest.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        ImageNameRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let snapshotLocation =
        (Option.map ~f:SnapshotLocationEnum.of_xml)
          (Xml.child xml_arg0 "SnapshotLocation") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?blockDeviceMappings ?noReboot ?description ~name ~instanceId
        ?dryRun ?snapshotLocation ?tagSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let blockDeviceMappings =
        field_map json__ "BlockDeviceMappings"
          BlockDeviceMappingRequestList.of_json in
      let noReboot = field_map json__ "NoReboot" Boolean.of_json in
      let description =
        field_map json__ "Description" ImageDescriptionRequest.of_json in
      let name = field_map_exn json__ "Name" ImageNameRequest.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let snapshotLocation =
        field_map json__ "SnapshotLocation" SnapshotLocationEnum.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ?blockDeviceMappings ?noReboot ?description ~name ~instanceId
        ?dryRun ?snapshotLocation ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Amazon EBS-backed AMI from an Amazon EBS-backed instance that is either running or stopped. If you customized your instance with instance store volumes or Amazon EBS volumes in addition to the root device volume, the new AMI contains block device mapping information for those volumes. When you launch an instance from this new AMI, the instance automatically launches with those additional volumes. The location of the source instance determines where you can create the snapshots of the AMI: If the source instance is in a Region, you must create the snapshots in the same Region as the instance. If the source instance is in a Local Zone, you can create the snapshots in the same Local Zone or in its parent Region. For more information, see Create an Amazon EBS-backed AMI in the Amazon Elastic Compute Cloud User Guide."]
module CreateFpgaImageResult =
  struct
    type nonrec t =
      {
      fpgaImageId: String_.t option
        [@ocaml.doc "The FPGA image identifier (AFI ID)."];
      fpgaImageGlobalId: String_.t option
        [@ocaml.doc "The global FPGA image identifier (AGFI ID)."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageId =
      fun ?fpgaImageGlobalId -> fun () -> { fpgaImageId; fpgaImageGlobalId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageId", (Option.map x.fpgaImageId ~f:String_.to_value));
        ("fpgaImageGlobalId",
          (Option.map x.fpgaImageGlobalId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageGlobalId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "fpgaImageGlobalId") in
      let fpgaImageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fpgaImageId") in
      make ?fpgaImageGlobalId ?fpgaImageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fpgaImageGlobalId =
        field_map json__ "FpgaImageGlobalId" String_.of_json in
      let fpgaImageId = field_map json__ "FpgaImageId" String_.of_json in
      make ?fpgaImageGlobalId ?fpgaImageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Amazon FPGA Image (AFI) from the specified design checkpoint (DCP). The create operation is asynchronous. To verify that the AFI was successfully created and is ready for use, check the output logs. An AFI contains the FPGA bitstream that is ready to download to an FPGA. You can securely deploy an AFI on multiple FPGA-accelerated instances. For more information, see the Amazon Web Services FPGA Hardware Development Kit."]
module CreateFpgaImageRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      inputStorageLocation: StorageLocation.t
        [@ocaml.doc
          "The location of the encrypted design checkpoint in Amazon S3. The input must be a tarball."];
      logsStorageLocation: StorageLocation.t option
        [@ocaml.doc "The location in Amazon S3 for the output logs."];
      description: String_.t option [@ocaml.doc "A description for the AFI."];
      name: String_.t option [@ocaml.doc "A name for the AFI."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the FPGA image during creation."]}
    let context_ = "CreateFpgaImageRequest"
    let make ?dryRun =
      fun ?logsStorageLocation ->
        fun ?description ->
          fun ?name ->
            fun ?clientToken ->
              fun ?tagSpecifications ->
                fun ~inputStorageLocation ->
                  fun () ->
                    {
                      dryRun;
                      logsStorageLocation;
                      description;
                      name;
                      clientToken;
                      tagSpecifications;
                      inputStorageLocation
                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InputStorageLocation",
          (Some (StorageLocation.to_value x.inputStorageLocation)));
        ("LogsStorageLocation",
          (Option.map x.logsStorageLocation ~f:StorageLocation.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let logsStorageLocation =
        (Option.map ~f:StorageLocation.of_xml)
          (Xml.child xml_arg0 "LogsStorageLocation") in
      let inputStorageLocation =
        StorageLocation.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InputStorageLocation") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ?clientToken ?name ?description
        ?logsStorageLocation ~inputStorageLocation ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let logsStorageLocation =
        field_map json__ "LogsStorageLocation" StorageLocation.of_json in
      let inputStorageLocation =
        field_map_exn json__ "InputStorageLocation" StorageLocation.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ?clientToken ?name ?description
        ?logsStorageLocation ~inputStorageLocation ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Amazon FPGA Image (AFI) from the specified design checkpoint (DCP). The create operation is asynchronous. To verify that the AFI was successfully created and is ready for use, check the output logs. An AFI contains the FPGA bitstream that is ready to download to an FPGA. You can securely deploy an AFI on multiple FPGA-accelerated instances. For more information, see the Amazon Web Services FPGA Hardware Development Kit."]
module CreateFlowLogsResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      flowLogIds: ValueStringList.t option
        [@ocaml.doc "The IDs of the flow logs."];
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the flow logs that could not be created successfully."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?flowLogIds ->
        fun ?unsuccessful ->
          fun () -> { clientToken; flowLogIds; unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("flowLogIdSet",
          (Option.map x.flowLogIds ~f:ValueStringList.to_value));
        ("unsuccessful",
          (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      let flowLogIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "flowLogIdSet") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?unsuccessful ?flowLogIds ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessful =
        field_map json__ "Unsuccessful" UnsuccessfulItemSet.of_json in
      let flowLogIds = field_map json__ "FlowLogIds" ValueStringList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?unsuccessful ?flowLogIds ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates one or more flow logs to capture information about IP traffic for a specific network interface, subnet, or VPC. Flow log data for a monitored network interface is recorded as flow log records, which are log events consisting of fields that describe the traffic flow. For more information, see Flow log records in the Amazon VPC User Guide. When publishing to CloudWatch Logs, flow log records are published to a log group, and each network interface has a unique log stream in the log group. When publishing to Amazon S3, flow log records for all of the monitored network interfaces are published to a single log file object that is stored in the specified bucket. For more information, see VPC Flow Logs in the Amazon VPC User Guide."]
module CreateFlowLogsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."];
      deliverLogsPermissionArn: String_.t option
        [@ocaml.doc
          "The ARN of the IAM role that allows Amazon EC2 to publish flow logs to the log destination. This parameter is required if the destination type is cloud-watch-logs, or if the destination type is kinesis-data-firehose and the delivery stream and the resources to monitor are in different accounts."];
      deliverCrossAccountRole: String_.t option
        [@ocaml.doc
          "The ARN of the IAM role that allows Amazon EC2 to publish flow logs across accounts."];
      logGroupName: String_.t option
        [@ocaml.doc
          "The name of a new or existing CloudWatch Logs log group where Amazon EC2 publishes your flow logs. This parameter is valid only if the destination type is cloud-watch-logs."];
      resourceIds: FlowLogResourceIds.t
        [@ocaml.doc
          "The IDs of the resources to monitor. For example, if the resource type is VPC, specify the IDs of the VPCs. Constraints: Maximum of 25 for transit gateway resource types. Maximum of 1000 for the other resource types."];
      resourceType: FlowLogsResourceType.t
        [@ocaml.doc "The type of resource to monitor."];
      trafficType: TrafficType.t option
        [@ocaml.doc
          "The type of traffic to monitor (accepted traffic, rejected traffic, or all traffic). This parameter is not supported for transit gateway resource types. It is required for the other resource types."];
      logDestinationType: LogDestinationType.t option
        [@ocaml.doc
          "The type of destination for the flow log data. Default: cloud-watch-logs"];
      logDestination: String_.t option
        [@ocaml.doc
          "The destination for the flow log data. The meaning of this parameter depends on the destination type. If the destination type is cloud-watch-logs, specify the ARN of a CloudWatch Logs log group. For example: arn:aws:logs:region:account_id:log-group:my_group Alternatively, use the LogGroupName parameter. If the destination type is s3, specify the ARN of an S3 bucket. For example: arn:aws:s3:::my_bucket/my_subfolder/ The subfolder is optional. Note that you can't use AWSLogs as a subfolder name. If the destination type is kinesis-data-firehose, specify the ARN of a Kinesis Data Firehose delivery stream. For example: arn:aws:firehose:region:account_id:deliverystream:my_stream"];
      logFormat: String_.t option
        [@ocaml.doc
          "The fields to include in the flow log record. List the fields in the order in which they should appear. If you omit this parameter, the flow log is created using the default format. If you specify this parameter, you must include at least one field. For more information about the available fields, see Flow log records in the Amazon VPC User Guide or Transit Gateway Flow Log records in the Amazon Web Services Transit Gateway Guide. Specify the fields using the $\\{field-id\\} format, separated by spaces."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the flow logs."];
      maxAggregationInterval: Integer.t option
        [@ocaml.doc
          "The maximum interval of time during which a flow of packets is captured and aggregated into a flow log record. The possible values are 60 seconds (1 minute) or 600 seconds (10 minutes). This parameter must be 60 seconds for transit gateway resource types. When a network interface is attached to a Nitro-based instance, the aggregation interval is always 60 seconds or less, regardless of the value that you specify. Default: 600"];
      destinationOptions: DestinationOptionsRequest.t option
        [@ocaml.doc "The destination options."]}
    let context_ = "CreateFlowLogsRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?deliverLogsPermissionArn ->
          fun ?deliverCrossAccountRole ->
            fun ?logGroupName ->
              fun ?trafficType ->
                fun ?logDestinationType ->
                  fun ?logDestination ->
                    fun ?logFormat ->
                      fun ?tagSpecifications ->
                        fun ?maxAggregationInterval ->
                          fun ?destinationOptions ->
                            fun ~resourceIds ->
                              fun ~resourceType ->
                                fun () ->
                                  {
                                    dryRun;
                                    clientToken;
                                    deliverLogsPermissionArn;
                                    deliverCrossAccountRole;
                                    logGroupName;
                                    trafficType;
                                    logDestinationType;
                                    logDestination;
                                    logFormat;
                                    tagSpecifications;
                                    maxAggregationInterval;
                                    destinationOptions;
                                    resourceIds;
                                    resourceType
                                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DeliverLogsPermissionArn",
          (Option.map x.deliverLogsPermissionArn ~f:String_.to_value));
        ("DeliverCrossAccountRole",
          (Option.map x.deliverCrossAccountRole ~f:String_.to_value));
        ("LogGroupName", (Option.map x.logGroupName ~f:String_.to_value));
        ("ResourceId", (Some (FlowLogResourceIds.to_value x.resourceIds)));
        ("ResourceType",
          (Some (FlowLogsResourceType.to_value x.resourceType)));
        ("TrafficType", (Option.map x.trafficType ~f:TrafficType.to_value));
        ("LogDestinationType",
          (Option.map x.logDestinationType ~f:LogDestinationType.to_value));
        ("LogDestination", (Option.map x.logDestination ~f:String_.to_value));
        ("LogFormat", (Option.map x.logFormat ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("MaxAggregationInterval",
          (Option.map x.maxAggregationInterval ~f:Integer.to_value));
        ("DestinationOptions",
          (Option.map x.destinationOptions
             ~f:DestinationOptionsRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationOptions =
        (Option.map ~f:DestinationOptionsRequest.of_xml)
          (Xml.child xml_arg0 "DestinationOptions") in
      let maxAggregationInterval =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxAggregationInterval") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let logFormat =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogFormat") in
      let logDestination =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogDestination") in
      let logDestinationType =
        (Option.map ~f:LogDestinationType.of_xml)
          (Xml.child xml_arg0 "LogDestinationType") in
      let trafficType =
        (Option.map ~f:TrafficType.of_xml) (Xml.child xml_arg0 "TrafficType") in
      let resourceType =
        FlowLogsResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      let resourceIds =
        FlowLogResourceIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let logGroupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogGroupName") in
      let deliverCrossAccountRole =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DeliverCrossAccountRole") in
      let deliverLogsPermissionArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DeliverLogsPermissionArn") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?destinationOptions ?maxAggregationInterval ?tagSpecifications
        ?logFormat ?logDestination ?logDestinationType ?trafficType
        ~resourceType ~resourceIds ?logGroupName ?deliverCrossAccountRole
        ?deliverLogsPermissionArn ?clientToken ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationOptions =
        field_map json__ "DestinationOptions"
          DestinationOptionsRequest.of_json in
      let maxAggregationInterval =
        field_map json__ "MaxAggregationInterval" Integer.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let logFormat = field_map json__ "LogFormat" String_.of_json in
      let logDestination = field_map json__ "LogDestination" String_.of_json in
      let logDestinationType =
        field_map json__ "LogDestinationType" LogDestinationType.of_json in
      let trafficType = field_map json__ "TrafficType" TrafficType.of_json in
      let resourceType =
        field_map_exn json__ "ResourceType" FlowLogsResourceType.of_json in
      let resourceIds =
        field_map_exn json__ "ResourceIds" FlowLogResourceIds.of_json in
      let logGroupName = field_map json__ "LogGroupName" String_.of_json in
      let deliverCrossAccountRole =
        field_map json__ "DeliverCrossAccountRole" String_.of_json in
      let deliverLogsPermissionArn =
        field_map json__ "DeliverLogsPermissionArn" String_.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?destinationOptions ?maxAggregationInterval ?tagSpecifications
        ?logFormat ?logDestination ?logDestinationType ?trafficType
        ~resourceType ~resourceIds ?logGroupName ?deliverCrossAccountRole
        ?deliverLogsPermissionArn ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates one or more flow logs to capture information about IP traffic for a specific network interface, subnet, or VPC. Flow log data for a monitored network interface is recorded as flow log records, which are log events consisting of fields that describe the traffic flow. For more information, see Flow log records in the Amazon VPC User Guide. When publishing to CloudWatch Logs, flow log records are published to a log group, and each network interface has a unique log stream in the log group. When publishing to Amazon S3, flow log records for all of the monitored network interfaces are published to a single log file object that is stored in the specified bucket. For more information, see VPC Flow Logs in the Amazon VPC User Guide."]
module CreateFleetResult =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option [@ocaml.doc "The ID of the EC2 Fleet."];
      errors: CreateFleetErrorsSet.t option
        [@ocaml.doc
          "Information about the instances that could not be launched by the fleet. Supported only for fleets of type instant."];
      instances: CreateFleetInstancesSet.t option
        [@ocaml.doc
          "Information about the instances that were launched by the fleet. Supported only for fleets of type instant."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?errors ->
        fun ?instances -> fun () -> { fleetId; errors; instances }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("errorSet", (Option.map x.errors ~f:CreateFleetErrorsSet.to_value));
        ("fleetInstanceSet",
          (Option.map x.instances ~f:CreateFleetInstancesSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instances =
        (Option.map ~f:CreateFleetInstancesSet.of_xml)
          (Xml.child xml_arg0 "fleetInstanceSet") in
      let errors =
        (Option.map ~f:CreateFleetErrorsSet.of_xml)
          (Xml.child xml_arg0 "errorSet") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "fleetId") in
      make ?instances ?errors ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instances =
        field_map json__ "Instances" CreateFleetInstancesSet.of_json in
      let errors = field_map json__ "Errors" CreateFleetErrorsSet.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?instances ?errors ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an EC2 Fleet that contains the configuration information for On-Demand Instances and Spot Instances. Instances are launched immediately if there is available capacity. A single EC2 Fleet can include multiple launch specifications that vary by instance type, AMI, Availability Zone, or subnet. For more information, see EC2 Fleet in the Amazon EC2 User Guide."]
module CreateFleetRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you do not specify a client token, a randomly generated token is used for the request to ensure idempotency. For more information, see Ensuring idempotency."];
      spotOptions: SpotOptionsRequest.t option
        [@ocaml.doc
          "Describes the configuration of Spot Instances in an EC2 Fleet."];
      onDemandOptions: OnDemandOptionsRequest.t option
        [@ocaml.doc
          "Describes the configuration of On-Demand Instances in an EC2 Fleet."];
      reservedCapacityOptions: ReservedCapacityOptionsRequest.t option
        [@ocaml.doc
          "Defines EC2 Fleet preferences for utilizing reserved capacity when DefaultTargetCapacityType is set to reserved-capacity. Supported only for fleets of type instant."];
      excessCapacityTerminationPolicy:
        FleetExcessCapacityTerminationPolicy.t option
        [@ocaml.doc
          "Indicates whether running instances should be terminated if the total target capacity of the EC2 Fleet is decreased below the current size of the EC2 Fleet. Supported only for fleets of type maintain."];
      launchTemplateConfigs: FleetLaunchTemplateConfigListRequest.t
        [@ocaml.doc "The configuration for the EC2 Fleet."];
      targetCapacitySpecification: TargetCapacitySpecificationRequest.t
        [@ocaml.doc "The number of units to request."];
      terminateInstancesWithExpiration: Boolean.t option
        [@ocaml.doc
          "Indicates whether running instances should be terminated when the EC2 Fleet expires."];
      type_: FleetType.t option
        [@ocaml.doc
          "The fleet type. The default value is maintain. maintain - The EC2 Fleet places an asynchronous request for your desired capacity, and continues to maintain your desired Spot capacity by replenishing interrupted Spot Instances. request - The EC2 Fleet places an asynchronous one-time request for your desired capacity, but does submit Spot requests in alternative capacity pools if Spot capacity is unavailable, and does not maintain Spot capacity if Spot Instances are interrupted. instant - The EC2 Fleet places a synchronous one-time request for your desired capacity, and returns errors for any instances that could not be launched. For more information, see EC2 Fleet request types in the Amazon EC2 User Guide."];
      validFrom: DateTime.t option
        [@ocaml.doc
          "The start date and time of the request, in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ). The default is to start fulfilling the request immediately."];
      validUntil: DateTime.t option
        [@ocaml.doc
          "The end date and time of the request, in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ). At this point, no new EC2 Fleet requests are placed or able to fulfill the request. If no value is specified, the request remains until you cancel it."];
      replaceUnhealthyInstances: Boolean.t option
        [@ocaml.doc
          "Indicates whether EC2 Fleet should replace unhealthy Spot Instances. Supported only for fleets of type maintain. For more information, see EC2 Fleet health checks in the Amazon EC2 User Guide."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The key-value pair for tagging the EC2 Fleet request on creation. For more information, see Tag your resources. If the fleet type is instant, specify a resource type of fleet to tag the fleet or instance to tag the instances at launch. If the fleet type is maintain or request, specify a resource type of fleet to tag the fleet. You cannot specify a resource type of instance. To tag instances at launch, specify the tags in a launch template."];
      context: String_.t option [@ocaml.doc "Reserved."]}
    let context_ = "CreateFleetRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?spotOptions ->
          fun ?onDemandOptions ->
            fun ?reservedCapacityOptions ->
              fun ?excessCapacityTerminationPolicy ->
                fun ?terminateInstancesWithExpiration ->
                  fun ?type_ ->
                    fun ?validFrom ->
                      fun ?validUntil ->
                        fun ?replaceUnhealthyInstances ->
                          fun ?tagSpecifications ->
                            fun ?context ->
                              fun ~launchTemplateConfigs ->
                                fun ~targetCapacitySpecification ->
                                  fun () ->
                                    {
                                      dryRun;
                                      clientToken;
                                      spotOptions;
                                      onDemandOptions;
                                      reservedCapacityOptions;
                                      excessCapacityTerminationPolicy;
                                      terminateInstancesWithExpiration;
                                      type_;
                                      validFrom;
                                      validUntil;
                                      replaceUnhealthyInstances;
                                      tagSpecifications;
                                      context;
                                      launchTemplateConfigs;
                                      targetCapacitySpecification
                                    }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("SpotOptions",
          (Option.map x.spotOptions ~f:SpotOptionsRequest.to_value));
        ("OnDemandOptions",
          (Option.map x.onDemandOptions ~f:OnDemandOptionsRequest.to_value));
        ("ReservedCapacityOptions",
          (Option.map x.reservedCapacityOptions
             ~f:ReservedCapacityOptionsRequest.to_value));
        ("ExcessCapacityTerminationPolicy",
          (Option.map x.excessCapacityTerminationPolicy
             ~f:FleetExcessCapacityTerminationPolicy.to_value));
        ("LaunchTemplateConfigs",
          (Some
             (FleetLaunchTemplateConfigListRequest.to_value
                x.launchTemplateConfigs)));
        ("TargetCapacitySpecification",
          (Some
             (TargetCapacitySpecificationRequest.to_value
                x.targetCapacitySpecification)));
        ("TerminateInstancesWithExpiration",
          (Option.map x.terminateInstancesWithExpiration ~f:Boolean.to_value));
        ("Type", (Option.map x.type_ ~f:FleetType.to_value));
        ("ValidFrom", (Option.map x.validFrom ~f:DateTime.to_value));
        ("ValidUntil", (Option.map x.validUntil ~f:DateTime.to_value));
        ("ReplaceUnhealthyInstances",
          (Option.map x.replaceUnhealthyInstances ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let replaceUnhealthyInstances =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ReplaceUnhealthyInstances") in
      let validUntil =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "ValidUntil") in
      let validFrom =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "ValidFrom") in
      let type_ =
        (Option.map ~f:FleetType.of_xml) (Xml.child xml_arg0 "Type") in
      let terminateInstancesWithExpiration =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TerminateInstancesWithExpiration") in
      let targetCapacitySpecification =
        TargetCapacitySpecificationRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TargetCapacitySpecification") in
      let launchTemplateConfigs =
        FleetLaunchTemplateConfigListRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplateConfigs") in
      let excessCapacityTerminationPolicy =
        (Option.map ~f:FleetExcessCapacityTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "ExcessCapacityTerminationPolicy") in
      let reservedCapacityOptions =
        (Option.map ~f:ReservedCapacityOptionsRequest.of_xml)
          (Xml.child xml_arg0 "ReservedCapacityOptions") in
      let onDemandOptions =
        (Option.map ~f:OnDemandOptionsRequest.of_xml)
          (Xml.child xml_arg0 "OnDemandOptions") in
      let spotOptions =
        (Option.map ~f:SpotOptionsRequest.of_xml)
          (Xml.child xml_arg0 "SpotOptions") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?context ?tagSpecifications ?replaceUnhealthyInstances ?validUntil
        ?validFrom ?type_ ?terminateInstancesWithExpiration
        ~targetCapacitySpecification ~launchTemplateConfigs
        ?excessCapacityTerminationPolicy ?reservedCapacityOptions
        ?onDemandOptions ?spotOptions ?clientToken ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let context = field_map json__ "Context" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let replaceUnhealthyInstances =
        field_map json__ "ReplaceUnhealthyInstances" Boolean.of_json in
      let validUntil = field_map json__ "ValidUntil" DateTime.of_json in
      let validFrom = field_map json__ "ValidFrom" DateTime.of_json in
      let type_ = field_map json__ "Type" FleetType.of_json in
      let terminateInstancesWithExpiration =
        field_map json__ "TerminateInstancesWithExpiration" Boolean.of_json in
      let targetCapacitySpecification =
        field_map_exn json__ "TargetCapacitySpecification"
          TargetCapacitySpecificationRequest.of_json in
      let launchTemplateConfigs =
        field_map_exn json__ "LaunchTemplateConfigs"
          FleetLaunchTemplateConfigListRequest.of_json in
      let excessCapacityTerminationPolicy =
        field_map json__ "ExcessCapacityTerminationPolicy"
          FleetExcessCapacityTerminationPolicy.of_json in
      let reservedCapacityOptions =
        field_map json__ "ReservedCapacityOptions"
          ReservedCapacityOptionsRequest.of_json in
      let onDemandOptions =
        field_map json__ "OnDemandOptions" OnDemandOptionsRequest.of_json in
      let spotOptions =
        field_map json__ "SpotOptions" SpotOptionsRequest.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?context ?tagSpecifications ?replaceUnhealthyInstances ?validUntil
        ?validFrom ?type_ ?terminateInstancesWithExpiration
        ~targetCapacitySpecification ~launchTemplateConfigs
        ?excessCapacityTerminationPolicy ?reservedCapacityOptions
        ?onDemandOptions ?spotOptions ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an EC2 Fleet that contains the configuration information for On-Demand Instances and Spot Instances. Instances are launched immediately if there is available capacity. A single EC2 Fleet can include multiple launch specifications that vary by instance type, AMI, Availability Zone, or subnet. For more information, see EC2 Fleet in the Amazon EC2 User Guide."]
module CreateEgressOnlyInternetGatewayResult =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      egressOnlyInternetGateway: EgressOnlyInternetGateway.t option
        [@ocaml.doc "Information about the egress-only internet gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientToken =
      fun ?egressOnlyInternetGateway ->
        fun () -> { clientToken; egressOnlyInternetGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("egressOnlyInternetGateway",
          (Option.map x.egressOnlyInternetGateway
             ~f:EgressOnlyInternetGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let egressOnlyInternetGateway =
        (Option.map ~f:EgressOnlyInternetGateway.of_xml)
          (Xml.child xml_arg0 "egressOnlyInternetGateway") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?egressOnlyInternetGateway ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let egressOnlyInternetGateway =
        field_map json__ "EgressOnlyInternetGateway"
          EgressOnlyInternetGateway.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?egressOnlyInternetGateway ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "\\[IPv6 only\\] Creates an egress-only internet gateway for your VPC. An egress-only internet gateway is used to enable outbound communication over IPv6 from instances in your VPC to the internet, and prevents hosts outside of your VPC from initiating an IPv6 connection with your instance."]
module CreateEgressOnlyInternetGatewayRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcId: VpcId.t
        [@ocaml.doc
          "The ID of the VPC for which to create the egress-only internet gateway."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to assign to the egress-only internet gateway."]}
    let context_ = "CreateEgressOnlyInternetGatewayRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~vpcId ->
            fun () -> { clientToken; dryRun; tagSpecifications; vpcId }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~vpcId ?dryRun ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?tagSpecifications ~vpcId ?dryRun ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "\\[IPv6 only\\] Creates an egress-only internet gateway for your VPC. An egress-only internet gateway is used to enable outbound communication over IPv6 from instances in your VPC to the internet, and prevents hosts outside of your VPC from initiating an IPv6 connection with your instance."]
module CreateDhcpOptionsResult =
  struct
    type nonrec t =
      {
      dhcpOptions: DhcpOptions.t option [@ocaml.doc "A set of DHCP options."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?dhcpOptions = fun () -> { dhcpOptions }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("dhcpOptions", (Option.map x.dhcpOptions ~f:DhcpOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dhcpOptions =
        (Option.map ~f:DhcpOptions.of_xml) (Xml.child xml_arg0 "dhcpOptions") in
      make ?dhcpOptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dhcpOptions = field_map json__ "DhcpOptions" DhcpOptions.of_json in
      make ?dhcpOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a custom set of DHCP options. After you create a DHCP option set, you associate it with a VPC. After you associate a DHCP option set with a VPC, all existing and newly launched instances in the VPC use this set of DHCP options. The following are the individual DHCP options you can specify. For more information, see DHCP option sets in the Amazon VPC User Guide. domain-name - If you're using AmazonProvidedDNS in us-east-1, specify ec2.internal. If you're using AmazonProvidedDNS in any other Region, specify region.compute.internal. Otherwise, specify a custom domain name. This value is used to complete unqualified DNS hostnames. Some Linux operating systems accept multiple domain names separated by spaces. However, Windows and other Linux operating systems treat the value as a single domain, which results in unexpected behavior. If your DHCP option set is associated with a VPC that has instances running operating systems that treat the value as a single domain, specify only one domain name. domain-name-servers - The IP addresses of up to four DNS servers, or AmazonProvidedDNS. To specify multiple domain name servers in a single parameter, separate the IP addresses using commas. To have your instances receive custom DNS hostnames as specified in domain-name, you must specify a custom DNS server. ntp-servers - The IP addresses of up to eight Network Time Protocol (NTP) servers (four IPv4 addresses and four IPv6 addresses). netbios-name-servers - The IP addresses of up to four NetBIOS name servers. netbios-node-type - The NetBIOS node type (1, 2, 4, or 8). We recommend that you specify 2. Broadcast and multicast are not supported. For more information about NetBIOS node types, see RFC 2132. ipv6-address-preferred-lease-time - A value (in seconds, minutes, hours, or years) for how frequently a running instance with an IPv6 assigned to it goes through DHCPv6 lease renewal. Acceptable values are between 140 and 2147483647 seconds (approximately 68 years). If no value is entered, the default lease time is 140 seconds. If you use long-term addressing for EC2 instances, you can increase the lease time and avoid frequent lease renewal requests. Lease renewal typically occurs when half of the lease time has elapsed."]
module CreateDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpConfigurations: NewDhcpConfigurationList.t
        [@ocaml.doc "A DHCP configuration option."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the DHCP option."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateDhcpOptionsRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~dhcpConfigurations ->
          fun () -> { tagSpecifications; dryRun; dhcpConfigurations }
    let to_value x =
      structure_to_value
        [("dhcpConfiguration",
           (Some (NewDhcpConfigurationList.to_value x.dhcpConfigurations)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dhcpConfigurations =
        NewDhcpConfigurationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dhcpConfiguration") in
      make ?dryRun ?tagSpecifications ~dhcpConfigurations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dhcpConfigurations =
        field_map_exn json__ "DhcpConfigurations"
          NewDhcpConfigurationList.of_json in
      make ?dryRun ?tagSpecifications ~dhcpConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a custom set of DHCP options. After you create a DHCP option set, you associate it with a VPC. After you associate a DHCP option set with a VPC, all existing and newly launched instances in the VPC use this set of DHCP options. The following are the individual DHCP options you can specify. For more information, see DHCP option sets in the Amazon VPC User Guide. domain-name - If you're using AmazonProvidedDNS in us-east-1, specify ec2.internal. If you're using AmazonProvidedDNS in any other Region, specify region.compute.internal. Otherwise, specify a custom domain name. This value is used to complete unqualified DNS hostnames. Some Linux operating systems accept multiple domain names separated by spaces. However, Windows and other Linux operating systems treat the value as a single domain, which results in unexpected behavior. If your DHCP option set is associated with a VPC that has instances running operating systems that treat the value as a single domain, specify only one domain name. domain-name-servers - The IP addresses of up to four DNS servers, or AmazonProvidedDNS. To specify multiple domain name servers in a single parameter, separate the IP addresses using commas. To have your instances receive custom DNS hostnames as specified in domain-name, you must specify a custom DNS server. ntp-servers - The IP addresses of up to eight Network Time Protocol (NTP) servers (four IPv4 addresses and four IPv6 addresses). netbios-name-servers - The IP addresses of up to four NetBIOS name servers. netbios-node-type - The NetBIOS node type (1, 2, 4, or 8). We recommend that you specify 2. Broadcast and multicast are not supported. For more information about NetBIOS node types, see RFC 2132. ipv6-address-preferred-lease-time - A value (in seconds, minutes, hours, or years) for how frequently a running instance with an IPv6 assigned to it goes through DHCPv6 lease renewal. Acceptable values are between 140 and 2147483647 seconds (approximately 68 years). If no value is entered, the default lease time is 140 seconds. If you use long-term addressing for EC2 instances, you can increase the lease time and avoid frequent lease renewal requests. Lease renewal typically occurs when half of the lease time has elapsed."]
module CreateDelegateMacVolumeOwnershipTaskResult =
  struct
    type nonrec t =
      {
      macModificationTask: MacModificationTask.t option
        [@ocaml.doc
          "Information about the volume ownership delegation task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?macModificationTask = fun () -> { macModificationTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("macModificationTask",
           (Option.map x.macModificationTask ~f:MacModificationTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let macModificationTask =
        (Option.map ~f:MacModificationTask.of_xml)
          (Xml.child xml_arg0 "macModificationTask") in
      make ?macModificationTask ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let macModificationTask =
        field_map json__ "MacModificationTask" MacModificationTask.of_json in
      make ?macModificationTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delegates ownership of the Amazon EBS root volume for an Apple silicon Mac instance to an administrative user."]
module CreateDelegateMacVolumeOwnershipTaskRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the Amazon EC2 Mac instance."];
      macCredentials: SensitiveMacCredentials.t
        [@ocaml.doc
          "Specifies the following credentials: Internal disk administrative user Username - Only the default administrative user (aws-managed-user) is supported and it is used by default. You can't specify a different administrative user. Password - If you did not change the default password for aws-managed-user, specify the default password, which is blank. Otherwise, specify your password. Amazon EBS root volume administrative user Username - If you did not change the default administrative user, specify ec2-user. Otherwise, specify the username for your administrative user. Password - Specify the password for the administrative user. The credentials must be specified in the following JSON format: \\{ \"internalDiskPassword\":\"internal-disk-admin_password\", \"rootVolumeUsername\":\"root-volume-admin_username\", \"rootVolumepassword\":\"root-volume-admin_password\" \\}"];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to assign to the volume ownership delegation task."]}
    let context_ = "CreateDelegateMacVolumeOwnershipTaskRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ?tagSpecifications ->
          fun ~instanceId ->
            fun ~macCredentials ->
              fun () ->
                {
                  clientToken;
                  dryRun;
                  tagSpecifications;
                  instanceId;
                  macCredentials
                }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("MacCredentials",
          (Some (SensitiveMacCredentials.to_value x.macCredentials)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let macCredentials =
        SensitiveMacCredentials.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MacCredentials") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tagSpecifications ~macCredentials ~instanceId ?dryRun
        ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let macCredentials =
        field_map_exn json__ "MacCredentials" SensitiveMacCredentials.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?tagSpecifications ~macCredentials ~instanceId ?dryRun
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delegates ownership of the Amazon EBS root volume for an Apple silicon Mac instance to an administrative user."]
module CreateDefaultVpcResult =
  struct
    type nonrec t =
      {
      vpc: Vpc.t option [@ocaml.doc "Information about the VPC."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpc = fun () -> { vpc }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("vpc", (Option.map x.vpc ~f:Vpc.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpc = (Option.map ~f:Vpc.of_xml) (Xml.child xml_arg0 "vpc") in
      make ?vpc ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpc = field_map json__ "Vpc" Vpc.of_json in make ?vpc ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a default VPC with a size /16 IPv4 CIDR block and a default subnet in each Availability Zone. For more information about the components of a default VPC, see Default VPCs in the Amazon VPC User Guide. You cannot specify the components of the default VPC yourself. If you deleted your previous default VPC, you can create a default VPC. You cannot have more than one default VPC per Region."]
module CreateDefaultVpcRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?dryRun = fun () -> { dryRun }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a default VPC with a size /16 IPv4 CIDR block and a default subnet in each Availability Zone. For more information about the components of a default VPC, see Default VPCs in the Amazon VPC User Guide. You cannot specify the components of the default VPC yourself. If you deleted your previous default VPC, you can create a default VPC. You cannot have more than one default VPC per Region."]
module CreateDefaultSubnetResult =
  struct
    type nonrec t =
      {
      subnet: Subnet.t option [@ocaml.doc "Information about the subnet."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?subnet = fun () -> { subnet }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subnet", (Option.map x.subnet ~f:Subnet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnet =
        (Option.map ~f:Subnet.of_xml) (Xml.child xml_arg0 "subnet") in
      make ?subnet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnet = field_map json__ "Subnet" Subnet.of_json in
      make ?subnet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a default subnet with a size /20 IPv4 CIDR block in the specified Availability Zone in your default VPC. You can have only one default subnet per Availability Zone. For more information, see Create a default subnet in the Amazon VPC User Guide."]
module CreateDefaultSubnetRequest =
  struct
    type nonrec t =
      {
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "The Availability Zone in which to create the default subnet. Either AvailabilityZone or AvailabilityZoneId must be specified, but not both."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipv6Native: Boolean.t option
        [@ocaml.doc
          "Indicates whether to create an IPv6 only subnet. If you already have a default subnet for this Availability Zone, you must delete it before you can create an IPv6 only subnet."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "The ID of the Availability Zone. Either AvailabilityZone or AvailabilityZoneId must be specified, but not both."]}
    let make ?availabilityZone =
      fun ?dryRun ->
        fun ?ipv6Native ->
          fun ?availabilityZoneId ->
            fun () ->
              { availabilityZone; dryRun; ipv6Native; availabilityZoneId }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Ipv6Native", (Option.map x.ipv6Native ~f:Boolean.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let ipv6Native =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Ipv6Native") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?availabilityZoneId ?ipv6Native ?dryRun ?availabilityZone ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let ipv6Native = field_map json__ "Ipv6Native" Boolean.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      make ?availabilityZoneId ?ipv6Native ?dryRun ?availabilityZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a default subnet with a size /20 IPv4 CIDR block in the specified Availability Zone in your default VPC. You can have only one default subnet per Availability Zone. For more information, see Create a default subnet in the Amazon VPC User Guide."]
module CreateCustomerGatewayResult =
  struct
    type nonrec t =
      {
      customerGateway: CustomerGateway.t option
        [@ocaml.doc "Information about the customer gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?customerGateway = fun () -> { customerGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("customerGateway",
           (Option.map x.customerGateway ~f:CustomerGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customerGateway =
        (Option.map ~f:CustomerGateway.of_xml)
          (Xml.child xml_arg0 "customerGateway") in
      make ?customerGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let customerGateway =
        field_map json__ "CustomerGateway" CustomerGateway.of_json in
      make ?customerGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateCustomerGateway."]
module CreateCustomerGatewayRequest =
  struct
    type nonrec t =
      {
      bgpAsn: Integer.t option
        [@ocaml.doc
          "For customer gateway devices that support BGP, specify the device's ASN. You must specify either BgpAsn or BgpAsnExtended when creating the customer gateway. If the ASN is larger than 2,147,483,647, you must use BgpAsnExtended. Default: 65000 Valid values: 1 to 2,147,483,647"];
      publicIp: String_.t option
        [@ocaml.doc
          "This member has been deprecated. The Internet-routable IP address for the customer gateway's outside interface. The address must be static."];
      certificateArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for the customer gateway certificate."];
      type_: GatewayType.t
        [@ocaml.doc
          "The type of VPN connection that this customer gateway supports (ipsec.1)."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the customer gateway."];
      deviceName: String_.t option
        [@ocaml.doc
          "A name for the customer gateway device. Length Constraints: Up to 255 characters."];
      ipAddress: String_.t option
        [@ocaml.doc
          "The IP address for the customer gateway device's outside interface. The address must be static. If OutsideIpAddressType in your VPN connection options is set to PrivateIpv4, you can use an RFC6598 or RFC1918 private IPv4 address. If OutsideIpAddressType is set to Ipv6, you can use an IPv6 address."];
      bgpAsnExtended: Long.t option
        [@ocaml.doc
          "For customer gateway devices that support BGP, specify the device's ASN. You must specify either BgpAsn or BgpAsnExtended when creating the customer gateway. If the ASN is larger than 2,147,483,647, you must use BgpAsnExtended. Valid values: 2,147,483,648 to 4,294,967,295"];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateCustomerGatewayRequest"
    let make ?bgpAsn =
      fun ?publicIp ->
        fun ?certificateArn ->
          fun ?tagSpecifications ->
            fun ?deviceName ->
              fun ?ipAddress ->
                fun ?bgpAsnExtended ->
                  fun ?dryRun ->
                    fun ~type_ ->
                      fun () ->
                        {
                          bgpAsn;
                          publicIp;
                          certificateArn;
                          tagSpecifications;
                          deviceName;
                          ipAddress;
                          bgpAsnExtended;
                          dryRun;
                          type_
                        }
    let to_value x =
      structure_to_value
        [("BgpAsn", (Option.map x.bgpAsn ~f:Integer.to_value));
        ("PublicIp", (Option.map x.publicIp ~f:String_.to_value));
        ("CertificateArn", (Option.map x.certificateArn ~f:String_.to_value));
        ("Type", (Some (GatewayType.to_value x.type_)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DeviceName", (Option.map x.deviceName ~f:String_.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:String_.to_value));
        ("BgpAsnExtended", (Option.map x.bgpAsnExtended ~f:Long.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let bgpAsnExtended =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BgpAsnExtended") in
      let ipAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let deviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "DeviceName") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let type_ =
        GatewayType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let certificateArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CertificateArn") in
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PublicIp") in
      let bgpAsn =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "BgpAsn") in
      make ?dryRun ?bgpAsnExtended ?ipAddress ?deviceName ?tagSpecifications
        ~type_ ?certificateArn ?publicIp ?bgpAsn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let bgpAsnExtended = field_map json__ "BgpAsnExtended" Long.of_json in
      let ipAddress = field_map json__ "IpAddress" String_.of_json in
      let deviceName = field_map json__ "DeviceName" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let type_ = field_map_exn json__ "Type" GatewayType.of_json in
      let certificateArn = field_map json__ "CertificateArn" String_.of_json in
      let publicIp = field_map json__ "PublicIp" String_.of_json in
      let bgpAsn = field_map json__ "BgpAsn" Integer.of_json in
      make ?dryRun ?bgpAsnExtended ?ipAddress ?deviceName ?tagSpecifications
        ~type_ ?certificateArn ?publicIp ?bgpAsn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateCustomerGateway."]
module CreateCoipPoolResult =
  struct
    type nonrec t =
      {
      coipPool: CoipPool.t option
        [@ocaml.doc "Information about the CoIP address pool."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipPool = fun () -> { coipPool }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipPool", (Option.map x.coipPool ~f:CoipPool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coipPool =
        (Option.map ~f:CoipPool.of_xml) (Xml.child xml_arg0 "coipPool") in
      make ?coipPool ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let coipPool = field_map json__ "CoipPool" CoipPool.of_json in
      make ?coipPool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a pool of customer-owned IP (CoIP) addresses."]
module CreateCoipPoolRequest =
  struct
    type nonrec t =
      {
      localGatewayRouteTableId: LocalGatewayRoutetableId.t
        [@ocaml.doc "The ID of the local gateway route table."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the CoIP address pool."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateCoipPoolRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~localGatewayRouteTableId ->
          fun () -> { tagSpecifications; dryRun; localGatewayRouteTableId }
    let to_value x =
      structure_to_value
        [("LocalGatewayRouteTableId",
           (Some
              (LocalGatewayRoutetableId.to_value x.localGatewayRouteTableId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let localGatewayRouteTableId =
        LocalGatewayRoutetableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "LocalGatewayRouteTableId") in
      make ?dryRun ?tagSpecifications ~localGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let localGatewayRouteTableId =
        field_map_exn json__ "LocalGatewayRouteTableId"
          LocalGatewayRoutetableId.of_json in
      make ?dryRun ?tagSpecifications ~localGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a pool of customer-owned IP (CoIP) addresses."]
module CreateCoipCidrResult =
  struct
    type nonrec t =
      {
      coipCidr: CoipCidr.t option
        [@ocaml.doc
          "Information about a range of customer-owned IP addresses."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?coipCidr = fun () -> { coipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("coipCidr", (Option.map x.coipCidr ~f:CoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coipCidr =
        (Option.map ~f:CoipCidr.of_xml) (Xml.child xml_arg0 "coipCidr") in
      make ?coipCidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let coipCidr = field_map json__ "CoipCidr" CoipCidr.of_json in
      make ?coipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a range of customer-owned IP addresses."]
module CreateCoipCidrRequest =
  struct
    type nonrec t =
      {
      cidr: String_.t
        [@ocaml.doc "A customer-owned IP address range to create."];
      coipPoolId: Ipv4PoolCoipId.t [@ocaml.doc "The ID of the address pool."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateCoipCidrRequest"
    let make ?dryRun =
      fun ~cidr -> fun ~coipPoolId -> fun () -> { dryRun; cidr; coipPoolId }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("CoipPoolId", (Some (Ipv4PoolCoipId.to_value x.coipPoolId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let coipPoolId =
        Ipv4PoolCoipId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CoipPoolId") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?dryRun ~coipPoolId ~cidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let coipPoolId =
        field_map_exn json__ "CoipPoolId" Ipv4PoolCoipId.of_json in
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      make ?dryRun ~coipPoolId ~cidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a range of customer-owned IP addresses."]
module CreateClientVpnRouteResult =
  struct
    type nonrec t =
      {
      status: ClientVpnRouteStatus.t option
        [@ocaml.doc "The current state of the route."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ClientVpnRouteStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnRouteStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" ClientVpnRouteStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds a route to a network to a Client VPN endpoint. Each Client VPN endpoint has a route table that describes the available destination network routes. Each route in the route table specifies the path for tra\239\172\131c to speci\239\172\129c resources or networks."]
module CreateClientVpnRouteRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc
          "The ID of the Client VPN endpoint to which to add the route."];
      destinationCidrBlock: String_.t
        [@ocaml.doc
          "The IPv4 address range, in CIDR notation, of the route destination. For example: To add a route for Internet access, enter 0.0.0.0/0 To add a route for a peered VPC, enter the peered VPC's IPv4 CIDR range To add a route for an on-premises network, enter the Amazon Web Services Site-to-Site VPN connection's IPv4 CIDR range To add a route for the local network, enter the client CIDR range"];
      targetVpcSubnetId: SubnetId.t option
        [@ocaml.doc
          "The ID of the subnet through which you want to route traffic. The specified subnet must be an existing target network of the Client VPN endpoint. Alternatively, if you're adding a route for the local network, specify local. This parameter is required for VPC-based Client VPN endpoints. For Transit Gateway-based endpoints, this parameter is not required."];
      description: String_.t option
        [@ocaml.doc "A brief description of the route."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateClientVpnRouteRequest"
    let make ?targetVpcSubnetId =
      fun ?description ->
        fun ?clientToken ->
          fun ?dryRun ->
            fun ~clientVpnEndpointId ->
              fun ~destinationCidrBlock ->
                fun () ->
                  {
                    targetVpcSubnetId;
                    description;
                    clientToken;
                    dryRun;
                    clientVpnEndpointId;
                    destinationCidrBlock
                  }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("DestinationCidrBlock",
          (Some (String_.to_value x.destinationCidrBlock)));
        ("TargetVpcSubnetId",
          (Option.map x.targetVpcSubnetId ~f:SubnetId.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let targetVpcSubnetId =
        (Option.map ~f:SubnetId.of_xml)
          (Xml.child xml_arg0 "TargetVpcSubnetId") in
      let destinationCidrBlock =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationCidrBlock") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?clientToken ?description ?targetVpcSubnetId
        ~destinationCidrBlock ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let targetVpcSubnetId =
        field_map json__ "TargetVpcSubnetId" SubnetId.of_json in
      let destinationCidrBlock =
        field_map_exn json__ "DestinationCidrBlock" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ?clientToken ?description ?targetVpcSubnetId
        ~destinationCidrBlock ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds a route to a network to a Client VPN endpoint. Each Client VPN endpoint has a route table that describes the available destination network routes. Each route in the route table specifies the path for tra\239\172\131c to speci\239\172\129c resources or networks."]
module CreateClientVpnEndpointResult =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: String_.t option
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      status: ClientVpnEndpointStatus.t option
        [@ocaml.doc "The current state of the Client VPN endpoint."];
      dnsName: String_.t option
        [@ocaml.doc
          "The DNS name to be used by clients when establishing their VPN session."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clientVpnEndpointId =
      fun ?status ->
        fun ?dnsName -> fun () -> { clientVpnEndpointId; status; dnsName }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientVpnEndpointId",
           (Option.map x.clientVpnEndpointId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:ClientVpnEndpointStatus.to_value));
        ("dnsName", (Option.map x.dnsName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dnsName") in
      let status =
        (Option.map ~f:ClientVpnEndpointStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let clientVpnEndpointId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "clientVpnEndpointId") in
      make ?dnsName ?status ?clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dnsName = field_map json__ "DnsName" String_.of_json in
      let status = field_map json__ "Status" ClientVpnEndpointStatus.of_json in
      let clientVpnEndpointId =
        field_map json__ "ClientVpnEndpointId" String_.of_json in
      make ?dnsName ?status ?clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Client VPN endpoint. A Client VPN endpoint is the resource you create and configure to enable and manage client VPN sessions. It is the destination endpoint at which all client VPN sessions are terminated."]
module CreateClientVpnEndpointRequest =
  struct
    type nonrec t =
      {
      clientCidrBlock: String_.t option
        [@ocaml.doc
          "The IPv4 address range, in CIDR notation, from which to assign client IP addresses. The address range cannot overlap with the local CIDR of the VPC in which the associated subnet is located, or the routes that you add manually. The address range cannot be changed after the Client VPN endpoint has been created. Client CIDR range must have a size of at least /22 and must not be greater than /12."];
      serverCertificateArn: String_.t
        [@ocaml.doc
          "The ARN of the server certificate. For more information, see the Certificate Manager User Guide."];
      authenticationOptions: ClientVpnAuthenticationRequestList.t
        [@ocaml.doc
          "Information about the authentication method to be used to authenticate clients."];
      connectionLogOptions: ConnectionLogOptions.t
        [@ocaml.doc
          "Information about the client connection logging options. If you enable client connection logging, data about client connections is sent to a Cloudwatch Logs log stream. The following information is logged: Client connection requests Client connection results (successful and unsuccessful) Reasons for unsuccessful client connection requests Client connection termination time"];
      dnsServers: ValueStringList.t option
        [@ocaml.doc
          "Information about the DNS servers to be used for DNS resolution. A Client VPN endpoint can have up to two DNS servers. If no DNS server is specified, the DNS address configured on the device is used for the DNS server."];
      transportProtocol: TransportProtocol.t option
        [@ocaml.doc
          "The transport protocol to be used by the VPN session. Default value: udp"];
      vpnPort: Integer.t option
        [@ocaml.doc
          "The port number to assign to the Client VPN endpoint for TCP and UDP traffic. Valid Values: 443 | 1194 Default Value: 443"];
      description: String_.t option
        [@ocaml.doc "A brief description of the Client VPN endpoint."];
      splitTunnel: Boolean.t option
        [@ocaml.doc
          "Indicates whether split-tunnel is enabled on the Client VPN endpoint. By default, split-tunnel on a VPN endpoint is disabled. For information about split-tunnel VPN endpoints, see Split-tunnel Client VPN endpoint in the Client VPN Administrator Guide."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the Client VPN endpoint during creation."];
      securityGroupIds: ClientVpnSecurityGroupIdSet.t option
        [@ocaml.doc
          "The IDs of one or more security groups to apply to the target network. You must also specify the ID of the VPC that contains the security groups."];
      vpcId: VpcId.t option
        [@ocaml.doc
          "The ID of the VPC to associate with the Client VPN endpoint. If no security group IDs are specified in the request, the default security group for the VPC is applied."];
      selfServicePortal: SelfServicePortal.t option
        [@ocaml.doc
          "Specify whether to enable the self-service portal for the Client VPN endpoint. Default Value: enabled"];
      clientConnectOptions: ClientConnectOptions.t option
        [@ocaml.doc
          "The options for managing connection authorization for new client connections."];
      sessionTimeoutHours: Integer.t option
        [@ocaml.doc
          "The maximum VPN session duration time in hours. Valid values: 8 | 10 | 12 | 24 Default value: 24"];
      clientLoginBannerOptions: ClientLoginBannerOptions.t option
        [@ocaml.doc
          "Options for enabling a customizable text banner that will be displayed on Amazon Web Services provided clients when a VPN session is established."];
      clientRouteEnforcementOptions: ClientRouteEnforcementOptions.t option
        [@ocaml.doc
          "Client route enforcement is a feature of the Client VPN service that helps enforce administrator defined routes on devices connected through the VPN. T his feature helps improve your security posture by ensuring that network traffic originating from a connected client is not inadvertently sent outside the VPN tunnel. Client route enforcement works by monitoring the route table of a connected device for routing policy changes to the VPN connection. If the feature detects any VPN routing policy modifications, it will automatically force an update to the route table, reverting it back to the expected route configurations."];
      disconnectOnSessionTimeout: Boolean.t option
        [@ocaml.doc
          "Indicates whether the client VPN session is disconnected after the maximum timeout specified in SessionTimeoutHours is reached. If true, users are prompted to reconnect client VPN. If false, client VPN attempts to reconnect automatically. The default value is true."];
      endpointIpAddressType: EndpointIpAddressType.t option
        [@ocaml.doc
          "The IP address type for the Client VPN endpoint. Valid values are ipv4 (default) for IPv4 addressing only, ipv6 for IPv6 addressing only, or dual-stack for both IPv4 and IPv6 addressing. When set to dual-stack, clients can connect to the endpoint using either IPv4 or IPv6 addresses.."];
      trafficIpAddressType: TrafficIpAddressType.t option
        [@ocaml.doc
          "The IP address type for traffic within the Client VPN tunnel. Valid values are ipv4 (default) for IPv4 traffic only, ipv6 for IPv6 addressing only, or dual-stack for both IPv4 and IPv6 traffic. When set to dual-stack, clients can access both IPv4 and IPv6 resources through the VPN ."];
      transitGatewayConfiguration:
        TransitGatewayConfigurationInputStructure.t option
        [@ocaml.doc
          "The Transit Gateway configuration for the Client VPN endpoint. Use this parameter to associate the endpoint with a Transit Gateway instead of a VPC. You cannot specify both TransitGatewayConfiguration and VpcId/SecurityGroupIds."]}
    let context_ = "CreateClientVpnEndpointRequest"
    let make ?clientCidrBlock =
      fun ?dnsServers ->
        fun ?transportProtocol ->
          fun ?vpnPort ->
            fun ?description ->
              fun ?splitTunnel ->
                fun ?dryRun ->
                  fun ?clientToken ->
                    fun ?tagSpecifications ->
                      fun ?securityGroupIds ->
                        fun ?vpcId ->
                          fun ?selfServicePortal ->
                            fun ?clientConnectOptions ->
                              fun ?sessionTimeoutHours ->
                                fun ?clientLoginBannerOptions ->
                                  fun ?clientRouteEnforcementOptions ->
                                    fun ?disconnectOnSessionTimeout ->
                                      fun ?endpointIpAddressType ->
                                        fun ?trafficIpAddressType ->
                                          fun ?transitGatewayConfiguration ->
                                            fun ~serverCertificateArn ->
                                              fun ~authenticationOptions ->
                                                fun ~connectionLogOptions ->
                                                  fun () ->
                                                    {
                                                      clientCidrBlock;
                                                      dnsServers;
                                                      transportProtocol;
                                                      vpnPort;
                                                      description;
                                                      splitTunnel;
                                                      dryRun;
                                                      clientToken;
                                                      tagSpecifications;
                                                      securityGroupIds;
                                                      vpcId;
                                                      selfServicePortal;
                                                      clientConnectOptions;
                                                      sessionTimeoutHours;
                                                      clientLoginBannerOptions;
                                                      clientRouteEnforcementOptions;
                                                      disconnectOnSessionTimeout;
                                                      endpointIpAddressType;
                                                      trafficIpAddressType;
                                                      transitGatewayConfiguration;
                                                      serverCertificateArn;
                                                      authenticationOptions;
                                                      connectionLogOptions
                                                    }
    let to_value x =
      structure_to_value
        [("ClientCidrBlock",
           (Option.map x.clientCidrBlock ~f:String_.to_value));
        ("ServerCertificateArn",
          (Some (String_.to_value x.serverCertificateArn)));
        ("Authentication",
          (Some
             (ClientVpnAuthenticationRequestList.to_value
                x.authenticationOptions)));
        ("ConnectionLogOptions",
          (Some (ConnectionLogOptions.to_value x.connectionLogOptions)));
        ("DnsServers", (Option.map x.dnsServers ~f:ValueStringList.to_value));
        ("TransportProtocol",
          (Option.map x.transportProtocol ~f:TransportProtocol.to_value));
        ("VpnPort", (Option.map x.vpnPort ~f:Integer.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("SplitTunnel", (Option.map x.splitTunnel ~f:Boolean.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupIds
             ~f:ClientVpnSecurityGroupIdSet.to_value));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("SelfServicePortal",
          (Option.map x.selfServicePortal ~f:SelfServicePortal.to_value));
        ("ClientConnectOptions",
          (Option.map x.clientConnectOptions ~f:ClientConnectOptions.to_value));
        ("SessionTimeoutHours",
          (Option.map x.sessionTimeoutHours ~f:Integer.to_value));
        ("ClientLoginBannerOptions",
          (Option.map x.clientLoginBannerOptions
             ~f:ClientLoginBannerOptions.to_value));
        ("ClientRouteEnforcementOptions",
          (Option.map x.clientRouteEnforcementOptions
             ~f:ClientRouteEnforcementOptions.to_value));
        ("DisconnectOnSessionTimeout",
          (Option.map x.disconnectOnSessionTimeout ~f:Boolean.to_value));
        ("EndpointIpAddressType",
          (Option.map x.endpointIpAddressType
             ~f:EndpointIpAddressType.to_value));
        ("TrafficIpAddressType",
          (Option.map x.trafficIpAddressType ~f:TrafficIpAddressType.to_value));
        ("TransitGatewayConfiguration",
          (Option.map x.transitGatewayConfiguration
             ~f:TransitGatewayConfigurationInputStructure.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayConfiguration =
        (Option.map ~f:TransitGatewayConfigurationInputStructure.of_xml)
          (Xml.child xml_arg0 "TransitGatewayConfiguration") in
      let trafficIpAddressType =
        (Option.map ~f:TrafficIpAddressType.of_xml)
          (Xml.child xml_arg0 "TrafficIpAddressType") in
      let endpointIpAddressType =
        (Option.map ~f:EndpointIpAddressType.of_xml)
          (Xml.child xml_arg0 "EndpointIpAddressType") in
      let disconnectOnSessionTimeout =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DisconnectOnSessionTimeout") in
      let clientRouteEnforcementOptions =
        (Option.map ~f:ClientRouteEnforcementOptions.of_xml)
          (Xml.child xml_arg0 "ClientRouteEnforcementOptions") in
      let clientLoginBannerOptions =
        (Option.map ~f:ClientLoginBannerOptions.of_xml)
          (Xml.child xml_arg0 "ClientLoginBannerOptions") in
      let sessionTimeoutHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "SessionTimeoutHours") in
      let clientConnectOptions =
        (Option.map ~f:ClientConnectOptions.of_xml)
          (Xml.child xml_arg0 "ClientConnectOptions") in
      let selfServicePortal =
        (Option.map ~f:SelfServicePortal.of_xml)
          (Xml.child xml_arg0 "SelfServicePortal") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let securityGroupIds =
        (Option.map ~f:ClientVpnSecurityGroupIdSet.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let splitTunnel =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SplitTunnel") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let vpnPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "VpnPort") in
      let transportProtocol =
        (Option.map ~f:TransportProtocol.of_xml)
          (Xml.child xml_arg0 "TransportProtocol") in
      let dnsServers =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "DnsServers") in
      let connectionLogOptions =
        ConnectionLogOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectionLogOptions") in
      let authenticationOptions =
        ClientVpnAuthenticationRequestList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Authentication") in
      let serverCertificateArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerCertificateArn") in
      let clientCidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientCidrBlock") in
      make ?transitGatewayConfiguration ?trafficIpAddressType
        ?endpointIpAddressType ?disconnectOnSessionTimeout
        ?clientRouteEnforcementOptions ?clientLoginBannerOptions
        ?sessionTimeoutHours ?clientConnectOptions ?selfServicePortal ?vpcId
        ?securityGroupIds ?tagSpecifications ?clientToken ?dryRun
        ?splitTunnel ?description ?vpnPort ?transportProtocol ?dnsServers
        ~connectionLogOptions ~authenticationOptions ~serverCertificateArn
        ?clientCidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayConfiguration =
        field_map json__ "TransitGatewayConfiguration"
          TransitGatewayConfigurationInputStructure.of_json in
      let trafficIpAddressType =
        field_map json__ "TrafficIpAddressType" TrafficIpAddressType.of_json in
      let endpointIpAddressType =
        field_map json__ "EndpointIpAddressType"
          EndpointIpAddressType.of_json in
      let disconnectOnSessionTimeout =
        field_map json__ "DisconnectOnSessionTimeout" Boolean.of_json in
      let clientRouteEnforcementOptions =
        field_map json__ "ClientRouteEnforcementOptions"
          ClientRouteEnforcementOptions.of_json in
      let clientLoginBannerOptions =
        field_map json__ "ClientLoginBannerOptions"
          ClientLoginBannerOptions.of_json in
      let sessionTimeoutHours =
        field_map json__ "SessionTimeoutHours" Integer.of_json in
      let clientConnectOptions =
        field_map json__ "ClientConnectOptions" ClientConnectOptions.of_json in
      let selfServicePortal =
        field_map json__ "SelfServicePortal" SelfServicePortal.of_json in
      let vpcId = field_map json__ "VpcId" VpcId.of_json in
      let securityGroupIds =
        field_map json__ "SecurityGroupIds"
          ClientVpnSecurityGroupIdSet.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let splitTunnel = field_map json__ "SplitTunnel" Boolean.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let vpnPort = field_map json__ "VpnPort" Integer.of_json in
      let transportProtocol =
        field_map json__ "TransportProtocol" TransportProtocol.of_json in
      let dnsServers = field_map json__ "DnsServers" ValueStringList.of_json in
      let connectionLogOptions =
        field_map_exn json__ "ConnectionLogOptions"
          ConnectionLogOptions.of_json in
      let authenticationOptions =
        field_map_exn json__ "AuthenticationOptions"
          ClientVpnAuthenticationRequestList.of_json in
      let serverCertificateArn =
        field_map_exn json__ "ServerCertificateArn" String_.of_json in
      let clientCidrBlock =
        field_map json__ "ClientCidrBlock" String_.of_json in
      make ?transitGatewayConfiguration ?trafficIpAddressType
        ?endpointIpAddressType ?disconnectOnSessionTimeout
        ?clientRouteEnforcementOptions ?clientLoginBannerOptions
        ?sessionTimeoutHours ?clientConnectOptions ?selfServicePortal ?vpcId
        ?securityGroupIds ?tagSpecifications ?clientToken ?dryRun
        ?splitTunnel ?description ?vpnPort ?transportProtocol ?dnsServers
        ~connectionLogOptions ~authenticationOptions ~serverCertificateArn
        ?clientCidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Client VPN endpoint. A Client VPN endpoint is the resource you create and configure to enable and manage client VPN sessions. It is the destination endpoint at which all client VPN sessions are terminated."]
module CreateCarrierGatewayResult =
  struct
    type nonrec t =
      {
      carrierGateway: CarrierGateway.t option
        [@ocaml.doc "Information about the carrier gateway."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?carrierGateway = fun () -> { carrierGateway }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("carrierGateway",
           (Option.map x.carrierGateway ~f:CarrierGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let carrierGateway =
        (Option.map ~f:CarrierGateway.of_xml)
          (Xml.child xml_arg0 "carrierGateway") in
      make ?carrierGateway ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let carrierGateway =
        field_map json__ "CarrierGateway" CarrierGateway.of_json in
      make ?carrierGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a carrier gateway. For more information about carrier gateways, see Carrier gateways in the Amazon Web Services Wavelength Developer Guide."]
module CreateCarrierGatewayRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t
        [@ocaml.doc
          "The ID of the VPC to associate with the carrier gateway."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to associate with the carrier gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see How to ensure idempotency."]}
    let context_ = "CreateCarrierGatewayRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ?clientToken ->
          fun ~vpcId ->
            fun () -> { tagSpecifications; dryRun; clientToken; vpcId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?clientToken ?dryRun ?tagSpecifications ~vpcId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      make ?clientToken ?dryRun ?tagSpecifications ~vpcId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a carrier gateway. For more information about carrier gateways, see Carrier gateways in the Amazon Web Services Wavelength Developer Guide."]
module CreateCapacityReservationResult =
  struct
    type nonrec t =
      {
      capacityReservation: CapacityReservation.t option
        [@ocaml.doc "Information about the Capacity Reservation."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityReservation = fun () -> { capacityReservation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityReservation",
           (Option.map x.capacityReservation ~f:CapacityReservation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservation =
        (Option.map ~f:CapacityReservation.of_xml)
          (Xml.child xml_arg0 "capacityReservation") in
      make ?capacityReservation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityReservation =
        field_map json__ "CapacityReservation" CapacityReservation.of_json in
      make ?capacityReservation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Capacity Reservation with the specified attributes. Capacity Reservations enable you to reserve capacity for your Amazon EC2 instances in a specific Availability Zone for any duration. You can create a Capacity Reservation at any time, and you can choose when it starts. You can create a Capacity Reservation for immediate use or you can request a Capacity Reservation for a future date. For more information, see Reserve compute capacity with On-Demand Capacity Reservations in the Amazon EC2 User Guide. Your request to create a Capacity Reservation could fail if: Amazon EC2 does not have sufficient capacity. In this case, try again at a later time, try in a different Availability Zone, or request a smaller Capacity Reservation. If your workload is flexible across instance types and sizes, try with different instance attributes. The requested quantity exceeds your On-Demand Instance quota. In this case, increase your On-Demand Instance quota for the requested instance type and try again. For more information, see Amazon EC2 Service Quotas in the Amazon EC2 User Guide."]
module CreateCapacityReservationRequest =
  struct
    type nonrec t =
      {
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      instanceType: String_.t
        [@ocaml.doc
          "The instance type for which to reserve capacity. You can request future-dated Capacity Reservations for instance types in the C, M, R, I, T, and G instance families only. For more information, see Instance types in the Amazon EC2 User Guide."];
      instancePlatform: CapacityReservationInstancePlatform.t
        [@ocaml.doc
          "The type of operating system for which to reserve capacity."];
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "The Availability Zone in which to create the Capacity Reservation."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "The ID of the Availability Zone in which to create the Capacity Reservation."];
      tenancy: CapacityReservationTenancy.t option
        [@ocaml.doc
          "Indicates the tenancy of the Capacity Reservation. A Capacity Reservation can have one of the following tenancy settings: default - The Capacity Reservation is created on hardware that is shared with other Amazon Web Services accounts. dedicated - The Capacity Reservation is created on single-tenant hardware that is dedicated to a single Amazon Web Services account."];
      instanceCount: Integer.t
        [@ocaml.doc
          "The number of instances for which to reserve capacity. You can request future-dated Capacity Reservations for an instance count with a minimum of 32 vCPUs. For example, if you request a future-dated Capacity Reservation for m5.xlarge instances, you must request at least 8 instances (8 * m5.xlarge = 32 vCPUs). Valid range: 1 - 1000"];
      ebsOptimized: Boolean.t option
        [@ocaml.doc
          "Indicates whether the Capacity Reservation supports EBS-optimized instances. This optimization provides dedicated throughput to Amazon EBS and an optimized configuration stack to provide optimal I/O performance. This optimization isn't available with all instance types. Additional usage charges apply when using an EBS- optimized instance."];
      ephemeralStorage: Boolean.t option [@ocaml.doc "Deprecated."];
      endDate: DateTime.t option
        [@ocaml.doc
          "The date and time at which the Capacity Reservation expires. When a Capacity Reservation expires, the reserved capacity is released and you can no longer launch instances into it. The Capacity Reservation's state changes to expired when it reaches its end date and time. You must provide an EndDate value if EndDateType is limited. Omit EndDate if EndDateType is unlimited. If the EndDateType is limited, the Capacity Reservation is cancelled within an hour from the specified time. For example, if you specify 5/31/2019, 13:30:55, the Capacity Reservation is guaranteed to end between 13:30:55 and 14:30:55 on 5/31/2019. If you are requesting a future-dated Capacity Reservation, you can't specify an end date and time that is within the commitment duration."];
      endDateType: EndDateType.t option
        [@ocaml.doc
          "Indicates the way in which the Capacity Reservation ends. A Capacity Reservation can have one of the following end types: unlimited - The Capacity Reservation remains active until you explicitly cancel it. Do not provide an EndDate if the EndDateType is unlimited. limited - The Capacity Reservation expires automatically at a specified date and time. You must provide an EndDate value if the EndDateType value is limited."];
      instanceMatchCriteria: InstanceMatchCriteria.t option
        [@ocaml.doc
          "Indicates the type of instance launches that the Capacity Reservation accepts. The options include: open - The Capacity Reservation automatically matches all instances that have matching attributes (instance type, platform, and Availability Zone). Instances that have matching attributes run in the Capacity Reservation automatically without specifying any additional parameters. targeted - The Capacity Reservation only accepts instances that have matching attributes (instance type, platform, and Availability Zone), and explicitly target the Capacity Reservation. This ensures that only permitted instances can use the reserved capacity. If you are requesting a future-dated Capacity Reservation, you must specify targeted. Default: open"];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the Capacity Reservation during launch."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      outpostArn: OutpostArn.t option
        [@ocaml.doc
          "Not supported for future-dated Capacity Reservations. The Amazon Resource Name (ARN) of the Outpost on which to create the Capacity Reservation."];
      placementGroupArn: PlacementGroupArn.t option
        [@ocaml.doc
          "Not supported for future-dated Capacity Reservations. The Amazon Resource Name (ARN) of the cluster placement group in which to create the Capacity Reservation. For more information, see Capacity Reservations for cluster placement groups in the Amazon EC2 User Guide."];
      startDate: MillisecondDateTime.t option
        [@ocaml.doc
          "Required for future-dated Capacity Reservations only. To create a Capacity Reservation for immediate use, omit this parameter. The date and time at which the future-dated Capacity Reservation should become available for use, in the ISO8601 format in the UTC time zone (YYYY-MM-DDThh:mm:ss.sssZ). You can request a future-dated Capacity Reservation between 5 and 120 days in advance."];
      commitmentDuration: CapacityReservationCommitmentDuration.t option
        [@ocaml.doc
          "Required for future-dated Capacity Reservations only. To create a Capacity Reservation for immediate use, omit this parameter. Specify a commitment duration, in seconds, for the future-dated Capacity Reservation. The commitment duration is a minimum duration for which you commit to having the future-dated Capacity Reservation in the active state in your account after it has been delivered. For more information, see Commitment duration."];
      deliveryPreference: CapacityReservationDeliveryPreference.t option
        [@ocaml.doc
          "Required for future-dated Capacity Reservations only. To create a Capacity Reservation for immediate use, omit this parameter. Indicates that the requested capacity will be delivered in addition to any running instances or reserved capacity that you have in your account at the requested date and time. The only supported value is incremental."]}
    let context_ = "CreateCapacityReservationRequest"
    let make ?clientToken =
      fun ?availabilityZone ->
        fun ?availabilityZoneId ->
          fun ?tenancy ->
            fun ?ebsOptimized ->
              fun ?ephemeralStorage ->
                fun ?endDate ->
                  fun ?endDateType ->
                    fun ?instanceMatchCriteria ->
                      fun ?tagSpecifications ->
                        fun ?dryRun ->
                          fun ?outpostArn ->
                            fun ?placementGroupArn ->
                              fun ?startDate ->
                                fun ?commitmentDuration ->
                                  fun ?deliveryPreference ->
                                    fun ~instanceType ->
                                      fun ~instancePlatform ->
                                        fun ~instanceCount ->
                                          fun () ->
                                            {
                                              clientToken;
                                              availabilityZone;
                                              availabilityZoneId;
                                              tenancy;
                                              ebsOptimized;
                                              ephemeralStorage;
                                              endDate;
                                              endDateType;
                                              instanceMatchCriteria;
                                              tagSpecifications;
                                              dryRun;
                                              outpostArn;
                                              placementGroupArn;
                                              startDate;
                                              commitmentDuration;
                                              deliveryPreference;
                                              instanceType;
                                              instancePlatform;
                                              instanceCount
                                            }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("InstanceType", (Some (String_.to_value x.instanceType)));
        ("InstancePlatform",
          (Some
             (CapacityReservationInstancePlatform.to_value x.instancePlatform)));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value));
        ("Tenancy",
          (Option.map x.tenancy ~f:CapacityReservationTenancy.to_value));
        ("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("EbsOptimized", (Option.map x.ebsOptimized ~f:Boolean.to_value));
        ("EphemeralStorage",
          (Option.map x.ephemeralStorage ~f:Boolean.to_value));
        ("EndDate", (Option.map x.endDate ~f:DateTime.to_value));
        ("EndDateType", (Option.map x.endDateType ~f:EndDateType.to_value));
        ("InstanceMatchCriteria",
          (Option.map x.instanceMatchCriteria
             ~f:InstanceMatchCriteria.to_value));
        ("TagSpecifications",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:OutpostArn.to_value));
        ("PlacementGroupArn",
          (Option.map x.placementGroupArn ~f:PlacementGroupArn.to_value));
        ("StartDate",
          (Option.map x.startDate ~f:MillisecondDateTime.to_value));
        ("CommitmentDuration",
          (Option.map x.commitmentDuration
             ~f:CapacityReservationCommitmentDuration.to_value));
        ("DeliveryPreference",
          (Option.map x.deliveryPreference
             ~f:CapacityReservationDeliveryPreference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deliveryPreference =
        (Option.map ~f:CapacityReservationDeliveryPreference.of_xml)
          (Xml.child xml_arg0 "DeliveryPreference") in
      let commitmentDuration =
        (Option.map ~f:CapacityReservationCommitmentDuration.of_xml)
          (Xml.child xml_arg0 "CommitmentDuration") in
      let startDate =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "StartDate") in
      let placementGroupArn =
        (Option.map ~f:PlacementGroupArn.of_xml)
          (Xml.child xml_arg0 "PlacementGroupArn") in
      let outpostArn =
        (Option.map ~f:OutpostArn.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecifications") in
      let instanceMatchCriteria =
        (Option.map ~f:InstanceMatchCriteria.of_xml)
          (Xml.child xml_arg0 "InstanceMatchCriteria") in
      let endDateType =
        (Option.map ~f:EndDateType.of_xml) (Xml.child xml_arg0 "EndDateType") in
      let endDate =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "EndDate") in
      let ephemeralStorage =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EphemeralStorage") in
      let ebsOptimized =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "EbsOptimized") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      let tenancy =
        (Option.map ~f:CapacityReservationTenancy.of_xml)
          (Xml.child xml_arg0 "Tenancy") in
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let instancePlatform =
        CapacityReservationInstancePlatform.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstancePlatform") in
      let instanceType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?deliveryPreference ?commitmentDuration ?startDate
        ?placementGroupArn ?outpostArn ?dryRun ?tagSpecifications
        ?instanceMatchCriteria ?endDateType ?endDate ?ephemeralStorage
        ?ebsOptimized ~instanceCount ?tenancy ?availabilityZoneId
        ?availabilityZone ~instancePlatform ~instanceType ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deliveryPreference =
        field_map json__ "DeliveryPreference"
          CapacityReservationDeliveryPreference.of_json in
      let commitmentDuration =
        field_map json__ "CommitmentDuration"
          CapacityReservationCommitmentDuration.of_json in
      let startDate =
        field_map json__ "StartDate" MillisecondDateTime.of_json in
      let placementGroupArn =
        field_map json__ "PlacementGroupArn" PlacementGroupArn.of_json in
      let outpostArn = field_map json__ "OutpostArn" OutpostArn.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let instanceMatchCriteria =
        field_map json__ "InstanceMatchCriteria"
          InstanceMatchCriteria.of_json in
      let endDateType = field_map json__ "EndDateType" EndDateType.of_json in
      let endDate = field_map json__ "EndDate" DateTime.of_json in
      let ephemeralStorage =
        field_map json__ "EphemeralStorage" Boolean.of_json in
      let ebsOptimized = field_map json__ "EbsOptimized" Boolean.of_json in
      let instanceCount =
        field_map_exn json__ "InstanceCount" Integer.of_json in
      let tenancy =
        field_map json__ "Tenancy" CapacityReservationTenancy.of_json in
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      let instancePlatform =
        field_map_exn json__ "InstancePlatform"
          CapacityReservationInstancePlatform.of_json in
      let instanceType = field_map_exn json__ "InstanceType" String_.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      make ?deliveryPreference ?commitmentDuration ?startDate
        ?placementGroupArn ?outpostArn ?dryRun ?tagSpecifications
        ?instanceMatchCriteria ?endDateType ?endDate ?ephemeralStorage
        ?ebsOptimized ~instanceCount ?tenancy ?availabilityZoneId
        ?availabilityZone ~instancePlatform ~instanceType ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Capacity Reservation with the specified attributes. Capacity Reservations enable you to reserve capacity for your Amazon EC2 instances in a specific Availability Zone for any duration. You can create a Capacity Reservation at any time, and you can choose when it starts. You can create a Capacity Reservation for immediate use or you can request a Capacity Reservation for a future date. For more information, see Reserve compute capacity with On-Demand Capacity Reservations in the Amazon EC2 User Guide. Your request to create a Capacity Reservation could fail if: Amazon EC2 does not have sufficient capacity. In this case, try again at a later time, try in a different Availability Zone, or request a smaller Capacity Reservation. If your workload is flexible across instance types and sizes, try with different instance attributes. The requested quantity exceeds your On-Demand Instance quota. In this case, increase your On-Demand Instance quota for the requested instance type and try again. For more information, see Amazon EC2 Service Quotas in the Amazon EC2 User Guide."]
module CreateCapacityReservationFleetResult =
  struct
    type nonrec t =
      {
      capacityReservationFleetId: CapacityReservationFleetId.t option
        [@ocaml.doc "The ID of the Capacity Reservation Fleet."];
      state: CapacityReservationFleetState.t option
        [@ocaml.doc "The status of the Capacity Reservation Fleet."];
      totalTargetCapacity: Integer.t option
        [@ocaml.doc
          "The total number of capacity units for which the Capacity Reservation Fleet reserves capacity."];
      totalFulfilledCapacity: Double.t option
        [@ocaml.doc
          "The requested capacity units that have been successfully reserved."];
      instanceMatchCriteria: FleetInstanceMatchCriteria.t option
        [@ocaml.doc
          "The instance matching criteria for the Capacity Reservation Fleet."];
      allocationStrategy: String_.t option
        [@ocaml.doc
          "The allocation strategy used by the Capacity Reservation Fleet."];
      createTime: MillisecondDateTime.t option
        [@ocaml.doc
          "The date and time at which the Capacity Reservation Fleet was created."];
      endDate: MillisecondDateTime.t option
        [@ocaml.doc
          "The date and time at which the Capacity Reservation Fleet expires."];
      tenancy: FleetCapacityReservationTenancy.t option
        [@ocaml.doc "Indicates the tenancy of Capacity Reservation Fleet."];
      fleetCapacityReservations: FleetCapacityReservationSet.t option
        [@ocaml.doc
          "Information about the individual Capacity Reservations in the Capacity Reservation Fleet."];
      tags: TagList.t option
        [@ocaml.doc "The tags assigned to the Capacity Reservation Fleet."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityReservationFleetId =
      fun ?state ->
        fun ?totalTargetCapacity ->
          fun ?totalFulfilledCapacity ->
            fun ?instanceMatchCriteria ->
              fun ?allocationStrategy ->
                fun ?createTime ->
                  fun ?endDate ->
                    fun ?tenancy ->
                      fun ?fleetCapacityReservations ->
                        fun ?tags ->
                          fun () ->
                            {
                              capacityReservationFleetId;
                              state;
                              totalTargetCapacity;
                              totalFulfilledCapacity;
                              instanceMatchCriteria;
                              allocationStrategy;
                              createTime;
                              endDate;
                              tenancy;
                              fleetCapacityReservations;
                              tags
                            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityReservationFleetId",
           (Option.map x.capacityReservationFleetId
              ~f:CapacityReservationFleetId.to_value));
        ("state",
          (Option.map x.state ~f:CapacityReservationFleetState.to_value));
        ("totalTargetCapacity",
          (Option.map x.totalTargetCapacity ~f:Integer.to_value));
        ("totalFulfilledCapacity",
          (Option.map x.totalFulfilledCapacity ~f:Double.to_value));
        ("instanceMatchCriteria",
          (Option.map x.instanceMatchCriteria
             ~f:FleetInstanceMatchCriteria.to_value));
        ("allocationStrategy",
          (Option.map x.allocationStrategy ~f:String_.to_value));
        ("createTime",
          (Option.map x.createTime ~f:MillisecondDateTime.to_value));
        ("endDate", (Option.map x.endDate ~f:MillisecondDateTime.to_value));
        ("tenancy",
          (Option.map x.tenancy ~f:FleetCapacityReservationTenancy.to_value));
        ("fleetCapacityReservationSet",
          (Option.map x.fleetCapacityReservations
             ~f:FleetCapacityReservationSet.to_value));
        ("tagSet", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      let fleetCapacityReservations =
        (Option.map ~f:FleetCapacityReservationSet.of_xml)
          (Xml.child xml_arg0 "fleetCapacityReservationSet") in
      let tenancy =
        (Option.map ~f:FleetCapacityReservationTenancy.of_xml)
          (Xml.child xml_arg0 "tenancy") in
      let endDate =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "endDate") in
      let createTime =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "createTime") in
      let allocationStrategy =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "allocationStrategy") in
      let instanceMatchCriteria =
        (Option.map ~f:FleetInstanceMatchCriteria.of_xml)
          (Xml.child xml_arg0 "instanceMatchCriteria") in
      let totalFulfilledCapacity =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "totalFulfilledCapacity") in
      let totalTargetCapacity =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "totalTargetCapacity") in
      let state =
        (Option.map ~f:CapacityReservationFleetState.of_xml)
          (Xml.child xml_arg0 "state") in
      let capacityReservationFleetId =
        (Option.map ~f:CapacityReservationFleetId.of_xml)
          (Xml.child xml_arg0 "capacityReservationFleetId") in
      make ?tags ?fleetCapacityReservations ?tenancy ?endDate ?createTime
        ?allocationStrategy ?instanceMatchCriteria ?totalFulfilledCapacity
        ?totalTargetCapacity ?state ?capacityReservationFleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let fleetCapacityReservations =
        field_map json__ "FleetCapacityReservations"
          FleetCapacityReservationSet.of_json in
      let tenancy =
        field_map json__ "Tenancy" FleetCapacityReservationTenancy.of_json in
      let endDate = field_map json__ "EndDate" MillisecondDateTime.of_json in
      let createTime =
        field_map json__ "CreateTime" MillisecondDateTime.of_json in
      let allocationStrategy =
        field_map json__ "AllocationStrategy" String_.of_json in
      let instanceMatchCriteria =
        field_map json__ "InstanceMatchCriteria"
          FleetInstanceMatchCriteria.of_json in
      let totalFulfilledCapacity =
        field_map json__ "TotalFulfilledCapacity" Double.of_json in
      let totalTargetCapacity =
        field_map json__ "TotalTargetCapacity" Integer.of_json in
      let state =
        field_map json__ "State" CapacityReservationFleetState.of_json in
      let capacityReservationFleetId =
        field_map json__ "CapacityReservationFleetId"
          CapacityReservationFleetId.of_json in
      make ?tags ?fleetCapacityReservations ?tenancy ?endDate ?createTime
        ?allocationStrategy ?instanceMatchCriteria ?totalFulfilledCapacity
        ?totalTargetCapacity ?state ?capacityReservationFleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Capacity Reservation Fleet. For more information, see Create a Capacity Reservation Fleet in the Amazon EC2 User Guide."]
module CreateCapacityReservationFleetRequest =
  struct
    type nonrec t =
      {
      allocationStrategy: String_.t option
        [@ocaml.doc
          "The strategy used by the Capacity Reservation Fleet to determine which of the specified instance types to use. Currently, only the prioritized allocation strategy is supported. For more information, see Allocation strategy in the Amazon EC2 User Guide. Valid values: prioritized"];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      instanceTypeSpecifications: ReservationFleetInstanceSpecificationList.t
        [@ocaml.doc
          "Information about the instance types for which to reserve the capacity."];
      tenancy: FleetCapacityReservationTenancy.t option
        [@ocaml.doc
          "Indicates the tenancy of the Capacity Reservation Fleet. All Capacity Reservations in the Fleet inherit this tenancy. The Capacity Reservation Fleet can have one of the following tenancy settings: default - The Capacity Reservation Fleet is created on hardware that is shared with other Amazon Web Services accounts. dedicated - The Capacity Reservations are created on single-tenant hardware that is dedicated to a single Amazon Web Services account."];
      totalTargetCapacity: Integer.t
        [@ocaml.doc
          "The total number of capacity units to be reserved by the Capacity Reservation Fleet. This value, together with the instance type weights that you assign to each instance type used by the Fleet determine the number of instances for which the Fleet reserves capacity. Both values are based on units that make sense for your workload. For more information, see Total target capacity in the Amazon EC2 User Guide."];
      endDate: MillisecondDateTime.t option
        [@ocaml.doc
          "The date and time at which the Capacity Reservation Fleet expires. When the Capacity Reservation Fleet expires, its state changes to expired and all of the Capacity Reservations in the Fleet expire. The Capacity Reservation Fleet expires within an hour after the specified time. For example, if you specify 5/31/2019, 13:30:55, the Capacity Reservation Fleet is guaranteed to expire between 13:30:55 and 14:30:55 on 5/31/2019."];
      instanceMatchCriteria: FleetInstanceMatchCriteria.t option
        [@ocaml.doc
          "Indicates the type of instance launches that the Capacity Reservation Fleet accepts. All Capacity Reservations in the Fleet inherit this instance matching criteria. Currently, Capacity Reservation Fleets support open instance matching criteria only. This means that instances that have matching attributes (instance type, platform, and Availability Zone) run in the Capacity Reservations automatically. Instances do not need to explicitly target a Capacity Reservation Fleet to use its reserved capacity."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to assign to the Capacity Reservation Fleet. The tags are automatically assigned to the Capacity Reservations in the Fleet."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CreateCapacityReservationFleetRequest"
    let make ?allocationStrategy =
      fun ?clientToken ->
        fun ?tenancy ->
          fun ?endDate ->
            fun ?instanceMatchCriteria ->
              fun ?tagSpecifications ->
                fun ?dryRun ->
                  fun ~instanceTypeSpecifications ->
                    fun ~totalTargetCapacity ->
                      fun () ->
                        {
                          allocationStrategy;
                          clientToken;
                          tenancy;
                          endDate;
                          instanceMatchCriteria;
                          tagSpecifications;
                          dryRun;
                          instanceTypeSpecifications;
                          totalTargetCapacity
                        }
    let to_value x =
      structure_to_value
        [("AllocationStrategy",
           (Option.map x.allocationStrategy ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("InstanceTypeSpecification",
          (Some
             (ReservationFleetInstanceSpecificationList.to_value
                x.instanceTypeSpecifications)));
        ("Tenancy",
          (Option.map x.tenancy ~f:FleetCapacityReservationTenancy.to_value));
        ("TotalTargetCapacity",
          (Some (Integer.to_value x.totalTargetCapacity)));
        ("EndDate", (Option.map x.endDate ~f:MillisecondDateTime.to_value));
        ("InstanceMatchCriteria",
          (Option.map x.instanceMatchCriteria
             ~f:FleetInstanceMatchCriteria.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let instanceMatchCriteria =
        (Option.map ~f:FleetInstanceMatchCriteria.of_xml)
          (Xml.child xml_arg0 "InstanceMatchCriteria") in
      let endDate =
        (Option.map ~f:MillisecondDateTime.of_xml)
          (Xml.child xml_arg0 "EndDate") in
      let totalTargetCapacity =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TotalTargetCapacity") in
      let tenancy =
        (Option.map ~f:FleetCapacityReservationTenancy.of_xml)
          (Xml.child xml_arg0 "Tenancy") in
      let instanceTypeSpecifications =
        ReservationFleetInstanceSpecificationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "InstanceTypeSpecification") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let allocationStrategy =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "AllocationStrategy") in
      make ?dryRun ?tagSpecifications ?instanceMatchCriteria ?endDate
        ~totalTargetCapacity ?tenancy ~instanceTypeSpecifications
        ?clientToken ?allocationStrategy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let instanceMatchCriteria =
        field_map json__ "InstanceMatchCriteria"
          FleetInstanceMatchCriteria.of_json in
      let endDate = field_map json__ "EndDate" MillisecondDateTime.of_json in
      let totalTargetCapacity =
        field_map_exn json__ "TotalTargetCapacity" Integer.of_json in
      let tenancy =
        field_map json__ "Tenancy" FleetCapacityReservationTenancy.of_json in
      let instanceTypeSpecifications =
        field_map_exn json__ "InstanceTypeSpecifications"
          ReservationFleetInstanceSpecificationList.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let allocationStrategy =
        field_map json__ "AllocationStrategy" String_.of_json in
      make ?dryRun ?tagSpecifications ?instanceMatchCriteria ?endDate
        ~totalTargetCapacity ?tenancy ~instanceTypeSpecifications
        ?clientToken ?allocationStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Capacity Reservation Fleet. For more information, see Create a Capacity Reservation Fleet in the Amazon EC2 User Guide."]
module CreateCapacityReservationBySplittingResult =
  struct
    type nonrec t =
      {
      sourceCapacityReservation: CapacityReservation.t option
        [@ocaml.doc "Information about the source Capacity Reservation."];
      destinationCapacityReservation: CapacityReservation.t option
        [@ocaml.doc
          "Information about the destination Capacity Reservation."];
      instanceCount: Integer.t option
        [@ocaml.doc
          "The number of instances in the new Capacity Reservation. The number of instances in the source Capacity Reservation was reduced by this amount."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?sourceCapacityReservation =
      fun ?destinationCapacityReservation ->
        fun ?instanceCount ->
          fun () ->
            {
              sourceCapacityReservation;
              destinationCapacityReservation;
              instanceCount
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sourceCapacityReservation",
           (Option.map x.sourceCapacityReservation
              ~f:CapacityReservation.to_value));
        ("destinationCapacityReservation",
          (Option.map x.destinationCapacityReservation
             ~f:CapacityReservation.to_value));
        ("instanceCount", (Option.map x.instanceCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "instanceCount") in
      let destinationCapacityReservation =
        (Option.map ~f:CapacityReservation.of_xml)
          (Xml.child xml_arg0 "destinationCapacityReservation") in
      let sourceCapacityReservation =
        (Option.map ~f:CapacityReservation.of_xml)
          (Xml.child xml_arg0 "sourceCapacityReservation") in
      make ?instanceCount ?destinationCapacityReservation
        ?sourceCapacityReservation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceCount = field_map json__ "InstanceCount" Integer.of_json in
      let destinationCapacityReservation =
        field_map json__ "DestinationCapacityReservation"
          CapacityReservation.of_json in
      let sourceCapacityReservation =
        field_map json__ "SourceCapacityReservation"
          CapacityReservation.of_json in
      make ?instanceCount ?destinationCapacityReservation
        ?sourceCapacityReservation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a new Capacity Reservation by splitting the capacity of the source Capacity Reservation. The new Capacity Reservation will have the same attributes as the source Capacity Reservation except for tags. The source Capacity Reservation must be active and owned by your Amazon Web Services account."]
module CreateCapacityReservationBySplittingRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      sourceCapacityReservationId: CapacityReservationId.t
        [@ocaml.doc
          "The ID of the Capacity Reservation from which you want to split the capacity."];
      instanceCount: Integer.t
        [@ocaml.doc
          "The number of instances to split from the source Capacity Reservation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the new Capacity Reservation."]}
    let context_ = "CreateCapacityReservationBySplittingRequest"
    let make ?dryRun =
      fun ?clientToken ->
        fun ?tagSpecifications ->
          fun ~sourceCapacityReservationId ->
            fun ~instanceCount ->
              fun () ->
                {
                  dryRun;
                  clientToken;
                  tagSpecifications;
                  sourceCapacityReservationId;
                  instanceCount
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("SourceCapacityReservationId",
          (Some
             (CapacityReservationId.to_value x.sourceCapacityReservationId)));
        ("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      let sourceCapacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "SourceCapacityReservationId") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?tagSpecifications ~instanceCount ~sourceCapacityReservationId
        ?clientToken ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let instanceCount =
        field_map_exn json__ "InstanceCount" Integer.of_json in
      let sourceCapacityReservationId =
        field_map_exn json__ "SourceCapacityReservationId"
          CapacityReservationId.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?tagSpecifications ~instanceCount ~sourceCapacityReservationId
        ?clientToken ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a new Capacity Reservation by splitting the capacity of the source Capacity Reservation. The new Capacity Reservation will have the same attributes as the source Capacity Reservation except for tags. The source Capacity Reservation must be active and owned by your Amazon Web Services account."]
module CreateCapacityManagerDataExportResult =
  struct
    type nonrec t =
      {
      capacityManagerDataExportId: CapacityManagerDataExportId.t option
        [@ocaml.doc
          "The unique identifier for the created data export configuration. Use this ID to reference the export in other API calls."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?capacityManagerDataExportId =
      fun () -> { capacityManagerDataExportId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityManagerDataExportId",
           (Option.map x.capacityManagerDataExportId
              ~f:CapacityManagerDataExportId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityManagerDataExportId =
        (Option.map ~f:CapacityManagerDataExportId.of_xml)
          (Xml.child xml_arg0 "capacityManagerDataExportId") in
      make ?capacityManagerDataExportId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityManagerDataExportId =
        field_map json__ "CapacityManagerDataExportId"
          CapacityManagerDataExportId.of_json in
      make ?capacityManagerDataExportId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new data export configuration for EC2 Capacity Manager. This allows you to automatically export capacity usage data to an S3 bucket on a scheduled basis. The exported data includes metrics for On-Demand, Spot, and Capacity Reservations usage across your organization."]
module CreateCapacityManagerDataExportRequest =
  struct
    type nonrec t =
      {
      s3BucketName: String_.t
        [@ocaml.doc
          "The name of the S3 bucket where the capacity data export files will be delivered. The bucket must exist and you must have write permissions to it."];
      s3BucketPrefix: String_.t option
        [@ocaml.doc
          "The S3 key prefix for the exported data files. This allows you to organize exports in a specific folder structure within your bucket. If not specified, files are placed at the bucket root."];
      schedule: Schedule.t
        [@ocaml.doc "The frequency at which data exports are generated."];
      outputFormat: OutputFormat.t
        [@ocaml.doc
          "The file format for the exported data. Parquet format is recommended for large datasets and better compression."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the data export configuration. You can tag the export for organization and cost tracking purposes."]}
    let context_ = "CreateCapacityManagerDataExportRequest"
    let make ?s3BucketPrefix =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ?tagSpecifications ->
            fun ~s3BucketName ->
              fun ~schedule ->
                fun ~outputFormat ->
                  fun () ->
                    {
                      s3BucketPrefix;
                      clientToken;
                      dryRun;
                      tagSpecifications;
                      s3BucketName;
                      schedule;
                      outputFormat
                    }
    let to_value x =
      structure_to_value
        [("S3BucketName", (Some (String_.to_value x.s3BucketName)));
        ("S3BucketPrefix", (Option.map x.s3BucketPrefix ~f:String_.to_value));
        ("Schedule", (Some (Schedule.to_value x.schedule)));
        ("OutputFormat", (Some (OutputFormat.to_value x.outputFormat)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let outputFormat =
        OutputFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OutputFormat") in
      let schedule =
        Schedule.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Schedule") in
      let s3BucketPrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "S3BucketPrefix") in
      let s3BucketName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "S3BucketName") in
      make ?tagSpecifications ?dryRun ?clientToken ~outputFormat ~schedule
        ?s3BucketPrefix ~s3BucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let outputFormat =
        field_map_exn json__ "OutputFormat" OutputFormat.of_json in
      let schedule = field_map_exn json__ "Schedule" Schedule.of_json in
      let s3BucketPrefix = field_map json__ "S3BucketPrefix" String_.of_json in
      let s3BucketName = field_map_exn json__ "S3BucketName" String_.of_json in
      make ?tagSpecifications ?dryRun ?clientToken ~outputFormat ~schedule
        ?s3BucketPrefix ~s3BucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new data export configuration for EC2 Capacity Manager. This allows you to automatically export capacity usage data to an S3 bucket on a scheduled basis. The exported data includes metrics for On-Demand, Spot, and Capacity Reservations usage across your organization."]
module CopyVolumesResult =
  struct
    type nonrec t =
      {
      volumes: VolumeList.t option
        [@ocaml.doc "Information about the volume copy."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?volumes = fun () -> { volumes }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("volumeSet", (Option.map x.volumes ~f:VolumeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumes =
        (Option.map ~f:VolumeList.of_xml) (Xml.child xml_arg0 "volumeSet") in
      make ?volumes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let volumes = field_map json__ "Volumes" VolumeList.of_json in
      make ?volumes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a crash-consistent, point-in-time copy of an existing Amazon EBS volume within the same Availability Zone. The volume copy can be attached to an Amazon EC2 instance once it reaches the available state. For more information, see Copy an Amazon EBS volume."]
module CopyVolumesRequest =
  struct
    type nonrec t =
      {
      sourceVolumeId: VolumeId.t
        [@ocaml.doc "The ID of the source EBS volume to copy."];
      iops: Integer.t option
        [@ocaml.doc
          "The number of I/O operations per second (IOPS) to provision for the volume copy. Required for io1 and io2 volumes. Optional for gp3 volumes. Omit for all other volume types. Full provisioned IOPS performance can be achieved only once the volume copy is fully initialized. Valid ranges: gp3: 3,000 (default) - 80,000 IOPS io1: 100 - 64,000 IOPS io2: 100 - 256,000 IOPS Instances built on the Nitro System can support up to 256,000 IOPS. Other instances can support up to 32,000 IOPS."];
      size: Integer.t option
        [@ocaml.doc
          "The size of the volume copy, in GiBs. The size must be equal to or greater than the size of the source volume. If not specified, the size defaults to the size of the source volume. Maximum supported sizes: gp2: 16,384 GiB gp3: 65,536 GiB io1: 16,384 GiB io2: 65,536 GiB st1 and sc1: 16,384 GiB standard: 1024 GiB"];
      volumeType: VolumeType.t option
        [@ocaml.doc
          "The volume type for the volume copy. If not specified, the volume type defaults to gp2."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the volume copy during creation."];
      multiAttachEnabled: Boolean.t option
        [@ocaml.doc
          "Indicates whether to enable Amazon EBS Multi-Attach for the volume copy. If you enable Multi-Attach, you can attach the volume to up to 16 Nitro instances in the same Availability Zone simultaneously. Supported with io1 and io2 volumes only. For more information, see Amazon EBS Multi-Attach."];
      throughput: Integer.t option
        [@ocaml.doc
          "The throughput to provision for the volume copy, in MiB/s. Supported for gp3 volumes only. Omit for all other volume types. Full provisioned throughput performance can be achieved only once the volume copy is fully initialized. Valid Range: 125 - 2000 MiB/s"];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensure Idempotency."]}
    let context_ = "CopyVolumesRequest"
    let make ?iops =
      fun ?size ->
        fun ?volumeType ->
          fun ?dryRun ->
            fun ?tagSpecifications ->
              fun ?multiAttachEnabled ->
                fun ?throughput ->
                  fun ?clientToken ->
                    fun ~sourceVolumeId ->
                      fun () ->
                        {
                          iops;
                          size;
                          volumeType;
                          dryRun;
                          tagSpecifications;
                          multiAttachEnabled;
                          throughput;
                          clientToken;
                          sourceVolumeId
                        }
    let to_value x =
      structure_to_value
        [("SourceVolumeId", (Some (VolumeId.to_value x.sourceVolumeId)));
        ("Iops", (Option.map x.iops ~f:Integer.to_value));
        ("Size", (Option.map x.size ~f:Integer.to_value));
        ("VolumeType", (Option.map x.volumeType ~f:VolumeType.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("MultiAttachEnabled",
          (Option.map x.multiAttachEnabled ~f:Boolean.to_value));
        ("Throughput", (Option.map x.throughput ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let throughput =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Throughput") in
      let multiAttachEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "MultiAttachEnabled") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let volumeType =
        (Option.map ~f:VolumeType.of_xml) (Xml.child xml_arg0 "VolumeType") in
      let size = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Size") in
      let iops = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Iops") in
      let sourceVolumeId =
        VolumeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceVolumeId") in
      make ?clientToken ?throughput ?multiAttachEnabled ?tagSpecifications
        ?dryRun ?volumeType ?size ?iops ~sourceVolumeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let throughput = field_map json__ "Throughput" Integer.of_json in
      let multiAttachEnabled =
        field_map json__ "MultiAttachEnabled" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let volumeType = field_map json__ "VolumeType" VolumeType.of_json in
      let size = field_map json__ "Size" Integer.of_json in
      let iops = field_map json__ "Iops" Integer.of_json in
      let sourceVolumeId =
        field_map_exn json__ "SourceVolumeId" VolumeId.of_json in
      make ?clientToken ?throughput ?multiAttachEnabled ?tagSpecifications
        ?dryRun ?volumeType ?size ?iops ~sourceVolumeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a crash-consistent, point-in-time copy of an existing Amazon EBS volume within the same Availability Zone. The volume copy can be attached to an Amazon EC2 instance once it reaches the available state. For more information, see Copy an Amazon EBS volume."]
module CopySnapshotResult =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc "Any tags applied to the new snapshot."];
      snapshotId: String_.t option [@ocaml.doc "The ID of the new snapshot."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun ?snapshotId -> fun () -> { tags; snapshotId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tagSet", (Option.map x.tags ~f:TagList.to_value));
        ("snapshotId", (Option.map x.snapshotId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snapshotId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "snapshotId") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tagSet") in
      make ?snapshotId ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let snapshotId = field_map json__ "SnapshotId" String_.of_json in
      let tags = field_map json__ "Tags" TagList.of_json in
      make ?snapshotId ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an exact copy of an Amazon EBS snapshot. The location of the source snapshot determines whether you can copy it or not, and the allowed destinations for the snapshot copy. If the source snapshot is in a Region, you can copy it within that Region, to another Region, to an Outpost associated with that Region, or to a Local Zone in that Region. If the source snapshot is in a Local Zone, you can copy it within that Local Zone, to another Local Zone in the same zone group, or to the parent Region of the Local Zone. If the source snapshot is on an Outpost, you can't copy it. When copying snapshots to a Region, the encryption outcome for the snapshot copy depends on the Amazon EBS encryption by default setting for the destination Region, the encryption status of the source snapshot, and the encryption parameters you specify in the request. For more information, see Encryption and snapshot copying. Snapshots copied to an Outpost must be encrypted. Unencrypted snapshots are not supported on Outposts. For more information, Amazon EBS local snapshots on Outposts. Snapshots copies have an arbitrary source volume ID. Do not use this volume ID for any purpose. For more information, see Copy an Amazon EBS snapshot in the Amazon EBS User Guide."]
module CopySnapshotRequest =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc "A description for the EBS snapshot."];
      destinationOutpostArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Outpost to which to copy the snapshot. Only supported when copying a snapshot to an Outpost. For more information, see Copy snapshots from an Amazon Web Services Region to an Outpost in the Amazon EBS User Guide."];
      destinationRegion: String_.t option
        [@ocaml.doc
          "The destination Region to use in the PresignedUrl parameter of a snapshot copy operation. This parameter is only valid for specifying the destination Region in a PresignedUrl parameter, where it is required. The snapshot copy is sent to the regional endpoint that you sent the HTTP request to (for example, ec2.us-east-1.amazonaws.com). With the CLI, this is specified using the --region parameter or the default Region in your Amazon Web Services configuration file."];
      encrypted: Boolean.t option
        [@ocaml.doc
          "To encrypt a copy of an unencrypted snapshot if encryption by default is not enabled, enable encryption using this parameter. Otherwise, omit this parameter. Copies of encrypted snapshots are encrypted, even if you omit this parameter and encryption by default is not enabled. You cannot set this parameter to false. For more information, see Amazon EBS encryption in the Amazon EBS User Guide."];
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "The identifier of the KMS key to use for Amazon EBS encryption. If this parameter is not specified, your KMS key for Amazon EBS is used. If KmsKeyId is specified, the encrypted state must be true. You can specify the KMS key using any of the following: Key ID. For example, 1234abcd-12ab-34cd-56ef-1234567890ab. Key alias. For example, alias/ExampleAlias. Key ARN. For example, arn:aws:kms:us-east-1:012345678910:key/1234abcd-12ab-34cd-56ef-1234567890ab. Alias ARN. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. Amazon Web Services authenticates the KMS key asynchronously. Therefore, if you specify an ID, alias, or ARN that is not valid, the action can appear to complete, but eventually fails."];
      presignedUrl: CopySnapshotRequestPSU.t option
        [@ocaml.doc
          "When you copy an encrypted source snapshot using the Amazon EC2 Query API, you must supply a pre-signed URL. This parameter is optional for unencrypted snapshots. For more information, see Query requests. The PresignedUrl should use the snapshot source endpoint, the CopySnapshot action, and include the SourceRegion, SourceSnapshotId, and DestinationRegion parameters. The PresignedUrl must be signed using Amazon Web Services Signature Version 4. Because EBS snapshots are stored in Amazon S3, the signing algorithm for this parameter uses the same logic that is described in Authenticating Requests: Using Query Parameters (Amazon Web Services Signature Version 4) in the Amazon S3 API Reference. An invalid or improperly signed PresignedUrl will cause the copy operation to fail asynchronously, and the snapshot will move to an error state."];
      sourceRegion: String_.t
        [@ocaml.doc
          "The ID of the Region that contains the snapshot to be copied."];
      sourceSnapshotId: String_.t
        [@ocaml.doc "The ID of the EBS snapshot to copy."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to apply to the new snapshot."];
      completionDurationMinutes:
        SnapshotCompletionDurationMinutesRequest.t option
        [@ocaml.doc
          "Not supported when copying snapshots to or from Local Zones or Outposts. Specify a completion duration, in 15 minute increments, to initiate a time-based snapshot copy. Time-based snapshot copy operations complete within the specified duration. For more information, see Time-based copies. If you do not specify a value, the snapshot copy operation is completed on a best-effort basis."];
      destinationAvailabilityZone: String_.t option
        [@ocaml.doc
          "The Local Zone, for example, cn-north-1-pkx-1a to which to copy the snapshot. Only supported when copying a snapshot to a Local Zone."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CopySnapshotRequest"
    let make ?description =
      fun ?destinationOutpostArn ->
        fun ?destinationRegion ->
          fun ?encrypted ->
            fun ?kmsKeyId ->
              fun ?presignedUrl ->
                fun ?tagSpecifications ->
                  fun ?completionDurationMinutes ->
                    fun ?destinationAvailabilityZone ->
                      fun ?dryRun ->
                        fun ~sourceRegion ->
                          fun ~sourceSnapshotId ->
                            fun () ->
                              {
                                description;
                                destinationOutpostArn;
                                destinationRegion;
                                encrypted;
                                kmsKeyId;
                                presignedUrl;
                                tagSpecifications;
                                completionDurationMinutes;
                                destinationAvailabilityZone;
                                dryRun;
                                sourceRegion;
                                sourceSnapshotId
                              }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value));
        ("DestinationOutpostArn",
          (Option.map x.destinationOutpostArn ~f:String_.to_value));
        ("destinationRegion",
          (Option.map x.destinationRegion ~f:String_.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("kmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("presignedUrl",
          (Option.map x.presignedUrl ~f:CopySnapshotRequestPSU.to_value));
        ("SourceRegion", (Some (String_.to_value x.sourceRegion)));
        ("SourceSnapshotId", (Some (String_.to_value x.sourceSnapshotId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("CompletionDurationMinutes",
          (Option.map x.completionDurationMinutes
             ~f:SnapshotCompletionDurationMinutesRequest.to_value));
        ("DestinationAvailabilityZone",
          (Option.map x.destinationAvailabilityZone ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationAvailabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationAvailabilityZone") in
      let completionDurationMinutes =
        (Option.map ~f:SnapshotCompletionDurationMinutesRequest.of_xml)
          (Xml.child xml_arg0 "CompletionDurationMinutes") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let sourceSnapshotId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceSnapshotId") in
      let sourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceRegion") in
      let presignedUrl =
        (Option.map ~f:CopySnapshotRequestPSU.of_xml)
          (Xml.child xml_arg0 "presignedUrl") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let destinationRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationRegion") in
      let destinationOutpostArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationOutpostArn") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?dryRun ?destinationAvailabilityZone ?completionDurationMinutes
        ?tagSpecifications ~sourceSnapshotId ~sourceRegion ?presignedUrl
        ?kmsKeyId ?encrypted ?destinationRegion ?destinationOutpostArn
        ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let destinationAvailabilityZone =
        field_map json__ "DestinationAvailabilityZone" String_.of_json in
      let completionDurationMinutes =
        field_map json__ "CompletionDurationMinutes"
          SnapshotCompletionDurationMinutesRequest.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let sourceSnapshotId =
        field_map_exn json__ "SourceSnapshotId" String_.of_json in
      let sourceRegion = field_map_exn json__ "SourceRegion" String_.of_json in
      let presignedUrl =
        field_map json__ "PresignedUrl" CopySnapshotRequestPSU.of_json in
      let kmsKeyId = field_map json__ "KmsKeyId" KmsKeyId.of_json in
      let encrypted = field_map json__ "Encrypted" Boolean.of_json in
      let destinationRegion =
        field_map json__ "DestinationRegion" String_.of_json in
      let destinationOutpostArn =
        field_map json__ "DestinationOutpostArn" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      make ?dryRun ?destinationAvailabilityZone ?completionDurationMinutes
        ?tagSpecifications ~sourceSnapshotId ~sourceRegion ?presignedUrl
        ?kmsKeyId ?encrypted ?destinationRegion ?destinationOutpostArn
        ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an exact copy of an Amazon EBS snapshot. The location of the source snapshot determines whether you can copy it or not, and the allowed destinations for the snapshot copy. If the source snapshot is in a Region, you can copy it within that Region, to another Region, to an Outpost associated with that Region, or to a Local Zone in that Region. If the source snapshot is in a Local Zone, you can copy it within that Local Zone, to another Local Zone in the same zone group, or to the parent Region of the Local Zone. If the source snapshot is on an Outpost, you can't copy it. When copying snapshots to a Region, the encryption outcome for the snapshot copy depends on the Amazon EBS encryption by default setting for the destination Region, the encryption status of the source snapshot, and the encryption parameters you specify in the request. For more information, see Encryption and snapshot copying. Snapshots copied to an Outpost must be encrypted. Unencrypted snapshots are not supported on Outposts. For more information, Amazon EBS local snapshots on Outposts. Snapshots copies have an arbitrary source volume ID. Do not use this volume ID for any purpose. For more information, see Copy an Amazon EBS snapshot in the Amazon EBS User Guide."]
module CopyImageResult =
  struct
    type nonrec t =
      {
      imageId: String_.t option [@ocaml.doc "The ID of the new AMI."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?imageId = fun () -> { imageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("imageId", (Option.map x.imageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      make ?imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let imageId = field_map json__ "ImageId" String_.of_json in
      make ?imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CopyImage."]
module CopyImageRequest =
  struct
    type nonrec t =
      {
      clientToken: CopyImageClientToken.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier you provide to ensure idempotency of the request. For more information, see Ensuring idempotency in Amazon EC2 API requests in the Amazon EC2 API Reference."];
      description: ImageDescriptionRequest.t option
        [@ocaml.doc "A description for the new AMI."];
      encrypted: Boolean.t option
        [@ocaml.doc
          "Specifies whether to encrypt the snapshots of the copied image. You can encrypt a copy of an unencrypted snapshot, but you cannot create an unencrypted copy of an encrypted snapshot. The default KMS key for Amazon EBS is used unless you specify a non-default Key Management Service (KMS) KMS key using KmsKeyId. For more information, see Use encryption with EBS-backed AMIs in the Amazon EC2 User Guide."];
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "The identifier of the symmetric Key Management Service (KMS) KMS key to use when creating encrypted volumes. If this parameter is not specified, your Amazon Web Services managed KMS key for Amazon EBS is used. If you specify a KMS key, you must also set the encrypted state to true. You can specify a KMS key using any of the following: Key ID. For example, 1234abcd-12ab-34cd-56ef-1234567890ab. Key alias. For example, alias/ExampleAlias. Key ARN. For example, arn:aws:kms:us-east-1:012345678910:key/1234abcd-12ab-34cd-56ef-1234567890ab. Alias ARN. For example, arn:aws:kms:us-east-1:012345678910:alias/ExampleAlias. Amazon Web Services authenticates the KMS key asynchronously. Therefore, if you specify an identifier that is not valid, the action can appear to complete, but eventually fails. The specified KMS key must exist in the destination Region. Amazon EBS does not support asymmetric KMS keys."];
      name: ImageNameRequest.t [@ocaml.doc "The name of the new AMI."];
      sourceImageId: String_.t [@ocaml.doc "The ID of the AMI to copy."];
      sourceRegion: String_.t
        [@ocaml.doc "The name of the Region that contains the AMI to copy."];
      destinationOutpostArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Outpost for the new AMI. Only specify this parameter when copying an AMI from an Amazon Web Services Region to an Outpost. The AMI must be in the Region of the destination Outpost. You can't copy an AMI from an Outpost to a Region, from one Outpost to another, or within the same Outpost. For more information, see Copy AMIs from an Amazon Web Services Region to an Outpost in the Amazon EBS User Guide. Only one of DestinationAvailabilityZone, DestinationAvailabilityZoneId, or DestinationOutpostArn can be specified."];
      copyImageTags: Boolean.t option
        [@ocaml.doc
          "Specifies whether to copy your user-defined AMI tags to the new AMI. The following tags are not be copied: System tags (prefixed with aws:) For public and shared AMIs, user-defined tags that are attached by other Amazon Web Services accounts Default: Your user-defined AMI tags are not copied."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the new AMI and new snapshots. You can tag the AMI, the snapshots, or both. To tag the new AMI, the value for ResourceType must be image. To tag the new snapshots, the value for ResourceType must be snapshot. The same tag is applied to all the new snapshots. If you specify other values for ResourceType, the request fails. To tag an AMI or snapshot after it has been created, see CreateTags."];
      snapshotCopyCompletionDurationMinutes: Long.t option
        [@ocaml.doc
          "Specify a completion duration, in 15 minute increments, to initiate a time-based AMI copy. The specified completion duration applies to each of the snapshots associated with the AMI. Each snapshot associated with the AMI will be completed within the specified completion duration, with copy throughput automatically adjusted for each snapshot based on its size to meet the timing target. If you do not specify a value, the AMI copy operation is completed on a best-effort basis. This parameter is not supported when copying an AMI to or from a Local Zone, or to an Outpost. For more information, see Time-based copies for Amazon EBS snapshots and EBS-backed AMIs."];
      destinationAvailabilityZone: String_.t option
        [@ocaml.doc
          "The Local Zone for the new AMI (for example, cn-north-1-pkx-1a). Only one of DestinationAvailabilityZone, DestinationAvailabilityZoneId, or DestinationOutpostArn can be specified."];
      destinationAvailabilityZoneId: String_.t option
        [@ocaml.doc
          "The ID of the Local Zone for the new AMI (for example, cnn1-pkx1-az1). Only one of DestinationAvailabilityZone, DestinationAvailabilityZoneId, or DestinationOutpostArn can be specified."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CopyImageRequest"
    let make ?clientToken =
      fun ?description ->
        fun ?encrypted ->
          fun ?kmsKeyId ->
            fun ?destinationOutpostArn ->
              fun ?copyImageTags ->
                fun ?tagSpecifications ->
                  fun ?snapshotCopyCompletionDurationMinutes ->
                    fun ?destinationAvailabilityZone ->
                      fun ?destinationAvailabilityZoneId ->
                        fun ?dryRun ->
                          fun ~name ->
                            fun ~sourceImageId ->
                              fun ~sourceRegion ->
                                fun () ->
                                  {
                                    clientToken;
                                    description;
                                    encrypted;
                                    kmsKeyId;
                                    destinationOutpostArn;
                                    copyImageTags;
                                    tagSpecifications;
                                    snapshotCopyCompletionDurationMinutes;
                                    destinationAvailabilityZone;
                                    destinationAvailabilityZoneId;
                                    dryRun;
                                    name;
                                    sourceImageId;
                                    sourceRegion
                                  }
    let to_value x =
      structure_to_value
        [("ClientToken",
           (Option.map x.clientToken ~f:CopyImageClientToken.to_value));
        ("Description",
          (Option.map x.description ~f:ImageDescriptionRequest.to_value));
        ("encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("kmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("Name", (Some (ImageNameRequest.to_value x.name)));
        ("SourceImageId", (Some (String_.to_value x.sourceImageId)));
        ("SourceRegion", (Some (String_.to_value x.sourceRegion)));
        ("DestinationOutpostArn",
          (Option.map x.destinationOutpostArn ~f:String_.to_value));
        ("CopyImageTags", (Option.map x.copyImageTags ~f:Boolean.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("SnapshotCopyCompletionDurationMinutes",
          (Option.map x.snapshotCopyCompletionDurationMinutes
             ~f:Long.to_value));
        ("DestinationAvailabilityZone",
          (Option.map x.destinationAvailabilityZone ~f:String_.to_value));
        ("DestinationAvailabilityZoneId",
          (Option.map x.destinationAvailabilityZoneId ~f:String_.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let destinationAvailabilityZoneId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationAvailabilityZoneId") in
      let destinationAvailabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationAvailabilityZone") in
      let snapshotCopyCompletionDurationMinutes =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "SnapshotCopyCompletionDurationMinutes") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let copyImageTags =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "CopyImageTags") in
      let destinationOutpostArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationOutpostArn") in
      let sourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceRegion") in
      let sourceImageId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceImageId") in
      let name =
        ImageNameRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "encrypted") in
      let description =
        (Option.map ~f:ImageDescriptionRequest.of_xml)
          (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:CopyImageClientToken.of_xml)
          (Xml.child xml_arg0 "ClientToken") in
      make ?dryRun ?destinationAvailabilityZoneId
        ?destinationAvailabilityZone ?snapshotCopyCompletionDurationMinutes
        ?tagSpecifications ?copyImageTags ?destinationOutpostArn
        ~sourceRegion ~sourceImageId ~name ?kmsKeyId ?encrypted ?description
        ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let destinationAvailabilityZoneId =
        field_map json__ "DestinationAvailabilityZoneId" String_.of_json in
      let destinationAvailabilityZone =
        field_map json__ "DestinationAvailabilityZone" String_.of_json in
      let snapshotCopyCompletionDurationMinutes =
        field_map json__ "SnapshotCopyCompletionDurationMinutes" Long.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let copyImageTags = field_map json__ "CopyImageTags" Boolean.of_json in
      let destinationOutpostArn =
        field_map json__ "DestinationOutpostArn" String_.of_json in
      let sourceRegion = field_map_exn json__ "SourceRegion" String_.of_json in
      let sourceImageId =
        field_map_exn json__ "SourceImageId" String_.of_json in
      let name = field_map_exn json__ "Name" ImageNameRequest.of_json in
      let kmsKeyId = field_map json__ "KmsKeyId" KmsKeyId.of_json in
      let encrypted = field_map json__ "Encrypted" Boolean.of_json in
      let description =
        field_map json__ "Description" ImageDescriptionRequest.of_json in
      let clientToken =
        field_map json__ "ClientToken" CopyImageClientToken.of_json in
      make ?dryRun ?destinationAvailabilityZoneId
        ?destinationAvailabilityZone ?snapshotCopyCompletionDurationMinutes
        ?tagSpecifications ?copyImageTags ?destinationOutpostArn
        ~sourceRegion ~sourceImageId ~name ?kmsKeyId ?encrypted ?description
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CopyImage."]
module CopyFpgaImageResult =
  struct
    type nonrec t =
      {
      fpgaImageId: String_.t option [@ocaml.doc "The ID of the new AFI."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?fpgaImageId = fun () -> { fpgaImageId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fpgaImageId", (Option.map x.fpgaImageId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fpgaImageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fpgaImageId") in
      make ?fpgaImageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fpgaImageId = field_map json__ "FpgaImageId" String_.of_json in
      make ?fpgaImageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Copies the specified Amazon FPGA Image (AFI) to the current Region."]
module CopyFpgaImageRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      sourceFpgaImageId: String_.t [@ocaml.doc "The ID of the source AFI."];
      description: String_.t option
        [@ocaml.doc "The description for the new AFI."];
      name: String_.t option
        [@ocaml.doc
          "The name for the new AFI. The default is the name of the source AFI."];
      sourceRegion: String_.t
        [@ocaml.doc "The Region that contains the source AFI."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."]}
    let context_ = "CopyFpgaImageRequest"
    let make ?dryRun =
      fun ?description ->
        fun ?name ->
          fun ?clientToken ->
            fun ~sourceFpgaImageId ->
              fun ~sourceRegion ->
                fun () ->
                  {
                    dryRun;
                    description;
                    name;
                    clientToken;
                    sourceFpgaImageId;
                    sourceRegion
                  }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SourceFpgaImageId", (Some (String_.to_value x.sourceFpgaImageId)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("SourceRegion", (Some (String_.to_value x.sourceRegion)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let sourceRegion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceRegion") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let sourceFpgaImageId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceFpgaImageId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ~sourceRegion ?name ?description ~sourceFpgaImageId
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let sourceRegion = field_map_exn json__ "SourceRegion" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let sourceFpgaImageId =
        field_map_exn json__ "SourceFpgaImageId" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ~sourceRegion ?name ?description ~sourceFpgaImageId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Copies the specified Amazon FPGA Image (AFI) to the current Region."]
module ConfirmProductInstanceResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "The return value of the request. Returns true if the specified product code is owned by the requester and associated with the specified instance."];
      ownerId: String_.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the instance owner. This is only present if the product code is attached to the instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun ?ownerId -> fun () -> { return; ownerId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("ownerId", (Option.map x.ownerId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ownerId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ownerId") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?ownerId ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ownerId = field_map json__ "OwnerId" String_.of_json in
      let return = field_map json__ "Return" Boolean.of_json in
      make ?ownerId ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Determines whether a product code is associated with an instance. This action can only be used by the owner of the product code. It is useful when a product code owner must verify whether another user's instance is eligible for support."]
module ConfirmProductInstanceRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      productCode: String_.t
        [@ocaml.doc
          "The product code. This must be a product code that you own."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ConfirmProductInstanceRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~productCode -> fun () -> { dryRun; instanceId; productCode }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("ProductCode", (Some (String_.to_value x.productCode)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let productCode =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProductCode") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~productCode ~instanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let productCode = field_map_exn json__ "ProductCode" String_.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      make ?dryRun ~productCode ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Determines whether a product code is associated with an instance. This action can only be used by the owner of the product code. It is useful when a product code owner must verify whether another user's instance is eligible for support."]
module CancelSpotInstanceRequestsResult =
  struct
    type nonrec t =
      {
      cancelledSpotInstanceRequests:
        CancelledSpotInstanceRequestList.t option
        [@ocaml.doc "The Spot Instance requests."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?cancelledSpotInstanceRequests =
      fun () -> { cancelledSpotInstanceRequests }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("spotInstanceRequestSet",
           (Option.map x.cancelledSpotInstanceRequests
              ~f:CancelledSpotInstanceRequestList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cancelledSpotInstanceRequests =
        (Option.map ~f:CancelledSpotInstanceRequestList.of_xml)
          (Xml.child xml_arg0 "spotInstanceRequestSet") in
      make ?cancelledSpotInstanceRequests ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cancelledSpotInstanceRequests =
        field_map json__ "CancelledSpotInstanceRequests"
          CancelledSpotInstanceRequestList.of_json in
      make ?cancelledSpotInstanceRequests ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CancelSpotInstanceRequests."]
module CancelSpotInstanceRequestsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      spotInstanceRequestIds: SpotInstanceRequestIdList.t
        [@ocaml.doc "The IDs of the Spot Instance requests."]}
    let context_ = "CancelSpotInstanceRequestsRequest"
    let make ?dryRun =
      fun ~spotInstanceRequestIds ->
        fun () -> { dryRun; spotInstanceRequestIds }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("SpotInstanceRequestId",
          (Some (SpotInstanceRequestIdList.to_value x.spotInstanceRequestIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spotInstanceRequestIds =
        SpotInstanceRequestIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SpotInstanceRequestId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~spotInstanceRequestIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let spotInstanceRequestIds =
        field_map_exn json__ "SpotInstanceRequestIds"
          SpotInstanceRequestIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~spotInstanceRequestIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CancelSpotInstanceRequests."]
module CancelSpotFleetRequestsResponse =
  struct
    type nonrec t =
      {
      successfulFleetRequests: CancelSpotFleetRequestsSuccessSet.t option
        [@ocaml.doc
          "Information about the Spot Fleet requests that are successfully canceled."];
      unsuccessfulFleetRequests: CancelSpotFleetRequestsErrorSet.t option
        [@ocaml.doc
          "Information about the Spot Fleet requests that are not successfully canceled."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulFleetRequests =
      fun ?unsuccessfulFleetRequests ->
        fun () -> { successfulFleetRequests; unsuccessfulFleetRequests }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulFleetRequestSet",
           (Option.map x.successfulFleetRequests
              ~f:CancelSpotFleetRequestsSuccessSet.to_value));
        ("unsuccessfulFleetRequestSet",
          (Option.map x.unsuccessfulFleetRequests
             ~f:CancelSpotFleetRequestsErrorSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessfulFleetRequests =
        (Option.map ~f:CancelSpotFleetRequestsErrorSet.of_xml)
          (Xml.child xml_arg0 "unsuccessfulFleetRequestSet") in
      let successfulFleetRequests =
        (Option.map ~f:CancelSpotFleetRequestsSuccessSet.of_xml)
          (Xml.child xml_arg0 "successfulFleetRequestSet") in
      make ?unsuccessfulFleetRequests ?successfulFleetRequests ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessfulFleetRequests =
        field_map json__ "UnsuccessfulFleetRequests"
          CancelSpotFleetRequestsErrorSet.of_json in
      let successfulFleetRequests =
        field_map json__ "SuccessfulFleetRequests"
          CancelSpotFleetRequestsSuccessSet.of_json in
      make ?unsuccessfulFleetRequests ?successfulFleetRequests ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CancelSpotFleetRequests."]
module CancelSpotFleetRequestsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      spotFleetRequestIds: SpotFleetRequestIdList.t
        [@ocaml.doc
          "The IDs of the Spot Fleet requests. Constraint: You can specify up to 100 IDs in a single request."];
      terminateInstances: Boolean.t
        [@ocaml.doc
          "Indicates whether to terminate the associated instances when the Spot Fleet request is canceled. The default is to terminate the instances. To let the instances continue to run after the Spot Fleet request is canceled, specify no-terminate-instances."]}
    let context_ = "CancelSpotFleetRequestsRequest"
    let make ?dryRun =
      fun ~spotFleetRequestIds ->
        fun ~terminateInstances ->
          fun () -> { dryRun; spotFleetRequestIds; terminateInstances }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("spotFleetRequestId",
          (Some (SpotFleetRequestIdList.to_value x.spotFleetRequestIds)));
        ("terminateInstances",
          (Some (Boolean.to_value x.terminateInstances)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminateInstances =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "terminateInstances") in
      let spotFleetRequestIds =
        SpotFleetRequestIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "spotFleetRequestId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~terminateInstances ~spotFleetRequestIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let terminateInstances =
        field_map_exn json__ "TerminateInstances" Boolean.of_json in
      let spotFleetRequestIds =
        field_map_exn json__ "SpotFleetRequestIds"
          SpotFleetRequestIdList.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~terminateInstances ~spotFleetRequestIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CancelSpotFleetRequests."]
module CancelReservedInstancesListingResult =
  struct
    type nonrec t =
      {
      reservedInstancesListings: ReservedInstancesListingList.t option
        [@ocaml.doc "The Reserved Instance listing."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?reservedInstancesListings =
      fun () -> { reservedInstancesListings }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reservedInstancesListingsSet",
           (Option.map x.reservedInstancesListings
              ~f:ReservedInstancesListingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListings =
        (Option.map ~f:ReservedInstancesListingList.of_xml)
          (Xml.child xml_arg0 "reservedInstancesListingsSet") in
      make ?reservedInstancesListings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesListings =
        field_map json__ "ReservedInstancesListings"
          ReservedInstancesListingList.of_json in
      make ?reservedInstancesListings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CancelReservedInstancesListing."]
module CancelReservedInstancesListingRequest =
  struct
    type nonrec t =
      {
      reservedInstancesListingId: ReservedInstancesListingId.t
        [@ocaml.doc "The ID of the Reserved Instance listing."]}
    let context_ = "CancelReservedInstancesListingRequest"
    let make ~reservedInstancesListingId =
      fun () -> { reservedInstancesListingId }
    let to_value x =
      structure_to_value
        [("reservedInstancesListingId",
           (Some
              (ReservedInstancesListingId.to_value
                 x.reservedInstancesListingId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reservedInstancesListingId =
        ReservedInstancesListingId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "reservedInstancesListingId") in
      make ~reservedInstancesListingId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reservedInstancesListingId =
        field_map_exn json__ "ReservedInstancesListingId"
          ReservedInstancesListingId.of_json in
      make ~reservedInstancesListingId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for CancelReservedInstancesListing."]
module CancelImportTaskResult =
  struct
    type nonrec t =
      {
      importTaskId: String_.t option
        [@ocaml.doc "The ID of the task being canceled."];
      previousState: String_.t option
        [@ocaml.doc "The current state of the task being canceled."];
      state: String_.t option
        [@ocaml.doc "The current state of the task being canceled."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?importTaskId =
      fun ?previousState ->
        fun ?state -> fun () -> { importTaskId; previousState; state }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("importTaskId", (Option.map x.importTaskId ~f:String_.to_value));
        ("previousState", (Option.map x.previousState ~f:String_.to_value));
        ("state", (Option.map x.state ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "state") in
      let previousState =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "previousState") in
      let importTaskId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "importTaskId") in
      make ?state ?previousState ?importTaskId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let state = field_map json__ "State" String_.of_json in
      let previousState = field_map json__ "PreviousState" String_.of_json in
      let importTaskId = field_map json__ "ImportTaskId" String_.of_json in
      make ?state ?previousState ?importTaskId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels an in-process import virtual machine or import snapshot task."]
module CancelImportTaskRequest =
  struct
    type nonrec t =
      {
      cancelReason: String_.t option
        [@ocaml.doc "The reason for canceling the task."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      importTaskId: ImportTaskId.t option
        [@ocaml.doc
          "The ID of the import image or import snapshot task to be canceled."]}
    let make ?cancelReason =
      fun ?dryRun ->
        fun ?importTaskId -> fun () -> { cancelReason; dryRun; importTaskId }
    let to_value x =
      structure_to_value
        [("CancelReason", (Option.map x.cancelReason ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ImportTaskId",
          (Option.map x.importTaskId ~f:ImportTaskId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let importTaskId =
        (Option.map ~f:ImportTaskId.of_xml)
          (Xml.child xml_arg0 "ImportTaskId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let cancelReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CancelReason") in
      make ?importTaskId ?dryRun ?cancelReason ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let importTaskId = field_map json__ "ImportTaskId" ImportTaskId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let cancelReason = field_map json__ "CancelReason" String_.of_json in
      make ?importTaskId ?dryRun ?cancelReason ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels an in-process import virtual machine or import snapshot task."]
module CancelImageLaunchPermissionResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes your Amazon Web Services account from the launch permissions for the specified AMI. For more information, see Cancel having an AMI shared with your Amazon Web Services account in the Amazon EC2 User Guide."]
module CancelImageLaunchPermissionRequest =
  struct
    type nonrec t =
      {
      imageId: ImageId.t
        [@ocaml.doc
          "The ID of the AMI that was shared with your Amazon Web Services account."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CancelImageLaunchPermissionRequest"
    let make ?dryRun = fun ~imageId -> fun () -> { dryRun; imageId }
    let to_value x =
      structure_to_value
        [("ImageId", (Some (ImageId.to_value x.imageId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let imageId =
        ImageId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ImageId") in
      make ?dryRun ~imageId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let imageId = field_map_exn json__ "ImageId" ImageId.of_json in
      make ?dryRun ~imageId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes your Amazon Web Services account from the launch permissions for the specified AMI. For more information, see Cancel having an AMI shared with your Amazon Web Services account in the Amazon EC2 User Guide."]
module CancelExportTaskRequest =
  struct
    type nonrec t =
      {
      exportTaskId: ExportVmTaskId.t
        [@ocaml.doc
          "The ID of the export task. This is the ID returned by the CreateInstanceExportTask and ExportImage operations."]}
    let context_ = "CancelExportTaskRequest"
    let make ~exportTaskId = fun () -> { exportTaskId }
    let to_value x =
      structure_to_value
        [("exportTaskId", (Some (ExportVmTaskId.to_value x.exportTaskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportTaskId =
        ExportVmTaskId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "exportTaskId") in
      make ~exportTaskId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exportTaskId =
        field_map_exn json__ "ExportTaskId" ExportVmTaskId.of_json in
      make ~exportTaskId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels an active export task. The request removes all artifacts of the export, including any partially-created Amazon S3 objects. If the export task is complete or is in the process of transferring the final disk image, the command fails and returns an error."]
module CancelDeclarativePoliciesReportResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Is true if the request succeeds, and an error otherwise."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels the generation of an account status report. You can only cancel a report while it has the running status. Reports with other statuses (complete, cancelled, or error) can't be canceled. For more information, see Generating the account status report for declarative policies in the Amazon Web Services Organizations User Guide."]
module CancelDeclarativePoliciesReportRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      reportId: DeclarativePoliciesReportId.t
        [@ocaml.doc "The ID of the report."]}
    let context_ = "CancelDeclarativePoliciesReportRequest"
    let make ?dryRun = fun ~reportId -> fun () -> { dryRun; reportId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ReportId",
          (Some (DeclarativePoliciesReportId.to_value x.reportId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportId =
        DeclarativePoliciesReportId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~reportId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportId =
        field_map_exn json__ "ReportId" DeclarativePoliciesReportId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~reportId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels the generation of an account status report. You can only cancel a report while it has the running status. Reports with other statuses (complete, cancelled, or error) can't be canceled. For more information, see Generating the account status report for declarative policies in the Amazon Web Services Organizations User Guide."]
module CancelConversionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      conversionTaskId: ConversionTaskId.t
        [@ocaml.doc "The ID of the conversion task."];
      reasonMessage: String_.t option
        [@ocaml.doc "The reason for canceling the conversion task."]}
    let context_ = "CancelConversionRequest"
    let make ?dryRun =
      fun ?reasonMessage ->
        fun ~conversionTaskId ->
          fun () -> { dryRun; reasonMessage; conversionTaskId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("conversionTaskId",
          (Some (ConversionTaskId.to_value x.conversionTaskId)));
        ("reasonMessage", (Option.map x.reasonMessage ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reasonMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reasonMessage") in
      let conversionTaskId =
        ConversionTaskId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "conversionTaskId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ?reasonMessage ~conversionTaskId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reasonMessage = field_map json__ "ReasonMessage" String_.of_json in
      let conversionTaskId =
        field_map_exn json__ "ConversionTaskId" ConversionTaskId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?reasonMessage ~conversionTaskId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels an active conversion task. The task can be the import of an instance or volume. The action removes all artifacts of the conversion, including a partially uploaded volume or instance. If the conversion is complete or is in the process of transferring the final disk image, the command fails and returns an exception."]
module CancelCapacityReservationResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels the specified Capacity Reservation, releases the reserved capacity, and changes the Capacity Reservation's state to cancelled. You can cancel a Capacity Reservation that is in the following states: assessing active and there is no commitment duration or the commitment duration has elapsed. You can't cancel a future-dated Capacity Reservation during the commitment duration. You can't modify or cancel a Capacity Block. For more information, see Capacity Blocks for ML. If a future-dated Capacity Reservation enters the delayed state, the commitment duration is waived, and you can cancel it as soon as it enters the active state. Instances running in the reserved capacity continue running until you stop them. Stopped instances that target the Capacity Reservation can no longer launch. Modify these instances to either target a different Capacity Reservation, launch On-Demand Instance capacity, or run in any open Capacity Reservation that has matching attributes and sufficient capacity."]
module CancelCapacityReservationRequest =
  struct
    type nonrec t =
      {
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc "The ID of the Capacity Reservation to be cancelled."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CancelCapacityReservationRequest"
    let make ?dryRun =
      fun ~capacityReservationId ->
        fun () -> { dryRun; capacityReservationId }
    let to_value x =
      structure_to_value
        [("CapacityReservationId",
           (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      make ?dryRun ~capacityReservationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let capacityReservationId =
        field_map_exn json__ "CapacityReservationId"
          CapacityReservationId.of_json in
      make ?dryRun ~capacityReservationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels the specified Capacity Reservation, releases the reserved capacity, and changes the Capacity Reservation's state to cancelled. You can cancel a Capacity Reservation that is in the following states: assessing active and there is no commitment duration or the commitment duration has elapsed. You can't cancel a future-dated Capacity Reservation during the commitment duration. You can't modify or cancel a Capacity Block. For more information, see Capacity Blocks for ML. If a future-dated Capacity Reservation enters the delayed state, the commitment duration is waived, and you can cancel it as soon as it enters the active state. Instances running in the reserved capacity continue running until you stop them. Stopped instances that target the Capacity Reservation can no longer launch. Modify these instances to either target a different Capacity Reservation, launch On-Demand Instance capacity, or run in any open Capacity Reservation that has matching attributes and sufficient capacity."]
module CancelCapacityReservationFleetsResult =
  struct
    type nonrec t =
      {
      successfulFleetCancellations:
        CapacityReservationFleetCancellationStateSet.t option
        [@ocaml.doc
          "Information about the Capacity Reservation Fleets that were successfully cancelled."];
      failedFleetCancellations:
        FailedCapacityReservationFleetCancellationResultSet.t option
        [@ocaml.doc
          "Information about the Capacity Reservation Fleets that could not be cancelled."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?successfulFleetCancellations =
      fun ?failedFleetCancellations ->
        fun () -> { successfulFleetCancellations; failedFleetCancellations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulFleetCancellationSet",
           (Option.map x.successfulFleetCancellations
              ~f:CapacityReservationFleetCancellationStateSet.to_value));
        ("failedFleetCancellationSet",
          (Option.map x.failedFleetCancellations
             ~f:FailedCapacityReservationFleetCancellationResultSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedFleetCancellations =
        (Option.map
           ~f:FailedCapacityReservationFleetCancellationResultSet.of_xml)
          (Xml.child xml_arg0 "failedFleetCancellationSet") in
      let successfulFleetCancellations =
        (Option.map ~f:CapacityReservationFleetCancellationStateSet.of_xml)
          (Xml.child xml_arg0 "successfulFleetCancellationSet") in
      make ?failedFleetCancellations ?successfulFleetCancellations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failedFleetCancellations =
        field_map json__ "FailedFleetCancellations"
          FailedCapacityReservationFleetCancellationResultSet.of_json in
      let successfulFleetCancellations =
        field_map json__ "SuccessfulFleetCancellations"
          CapacityReservationFleetCancellationStateSet.of_json in
      make ?failedFleetCancellations ?successfulFleetCancellations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels one or more Capacity Reservation Fleets. When you cancel a Capacity Reservation Fleet, the following happens: The Capacity Reservation Fleet's status changes to cancelled. The individual Capacity Reservations in the Fleet are cancelled. Instances running in the Capacity Reservations at the time of cancelling the Fleet continue to run in shared capacity. The Fleet stops creating new Capacity Reservations."]
module CancelCapacityReservationFleetsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      capacityReservationFleetIds: CapacityReservationFleetIdSet.t
        [@ocaml.doc "The IDs of the Capacity Reservation Fleets to cancel."]}
    let context_ = "CancelCapacityReservationFleetsRequest"
    let make ?dryRun =
      fun ~capacityReservationFleetIds ->
        fun () -> { dryRun; capacityReservationFleetIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CapacityReservationFleetId",
          (Some
             (CapacityReservationFleetIdSet.to_value
                x.capacityReservationFleetIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationFleetIds =
        CapacityReservationFleetIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CapacityReservationFleetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~capacityReservationFleetIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityReservationFleetIds =
        field_map_exn json__ "CapacityReservationFleetIds"
          CapacityReservationFleetIdSet.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~capacityReservationFleetIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Cancels one or more Capacity Reservation Fleets. When you cancel a Capacity Reservation Fleet, the following happens: The Capacity Reservation Fleet's status changes to cancelled. The individual Capacity Reservations in the Fleet are cancelled. Instances running in the Capacity Reservations at the time of cancelling the Fleet continue to run in shared capacity. The Fleet stops creating new Capacity Reservations."]
module CancelBundleTaskResult =
  struct
    type nonrec t =
      {
      bundleTask: BundleTask.t option
        [@ocaml.doc "Information about the bundle task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?bundleTask = fun () -> { bundleTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleInstanceTask",
           (Option.map x.bundleTask ~f:BundleTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleTask =
        (Option.map ~f:BundleTask.of_xml)
          (Xml.child xml_arg0 "bundleInstanceTask") in
      make ?bundleTask ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bundleTask = field_map json__ "BundleTask" BundleTask.of_json in
      make ?bundleTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CancelBundleTask."]
module CancelBundleTaskRequest =
  struct
    type nonrec t =
      {
      bundleId: BundleId.t [@ocaml.doc "The ID of the bundle task."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "CancelBundleTaskRequest"
    let make ?dryRun = fun ~bundleId -> fun () -> { dryRun; bundleId }
    let to_value x =
      structure_to_value
        [("BundleId", (Some (BundleId.to_value x.bundleId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let bundleId =
        BundleId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "BundleId") in
      make ?dryRun ~bundleId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let bundleId = field_map_exn json__ "BundleId" BundleId.of_json in
      make ?dryRun ~bundleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CancelBundleTask."]
module BundleInstanceResult =
  struct
    type nonrec t =
      {
      bundleTask: BundleTask.t option
        [@ocaml.doc "Information about the bundle task."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?bundleTask = fun () -> { bundleTask }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleInstanceTask",
           (Option.map x.bundleTask ~f:BundleTask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleTask =
        (Option.map ~f:BundleTask.of_xml)
          (Xml.child xml_arg0 "bundleInstanceTask") in
      make ?bundleTask ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bundleTask = field_map json__ "BundleTask" BundleTask.of_json in
      make ?bundleTask ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of BundleInstance."]
module BundleInstanceRequest =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the instance to bundle. Default: None"];
      storage: Storage.t
        [@ocaml.doc
          "The bucket in which to store the AMI. You can specify a bucket that you already own or a new bucket that Amazon EC2 creates on your behalf. If you specify a bucket that belongs to someone else, Amazon EC2 returns an error."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "BundleInstanceRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~storage -> fun () -> { dryRun; instanceId; storage }
    let to_value x =
      structure_to_value
        [("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("Storage", (Some (Storage.to_value x.storage)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let storage =
        Storage.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Storage") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      make ?dryRun ~storage ~instanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let storage = field_map_exn json__ "Storage" Storage.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      make ?dryRun ~storage ~instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for BundleInstance."]
module AuthorizeSecurityGroupIngressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."];
      securityGroupRules: SecurityGroupRuleList.t option
        [@ocaml.doc
          "Information about the inbound (ingress) security group rules that were added."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?securityGroupRules -> fun () -> { return; securityGroupRules }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("securityGroupRuleSet",
          (Option.map x.securityGroupRules ~f:SecurityGroupRuleList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRules =
        (Option.map ~f:SecurityGroupRuleList.of_xml)
          (Xml.child xml_arg0 "securityGroupRuleSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?securityGroupRules ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupRules =
        field_map json__ "SecurityGroupRules" SecurityGroupRuleList.of_json in
      let return = field_map json__ "Return" Boolean.of_json in
      make ?securityGroupRules ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds the specified inbound (ingress) rules to a security group. An inbound rule permits instances to receive traffic from the specified IPv4 or IPv6 address range, the IP address ranges that are specified by a prefix list, or the instances that are associated with a destination security group. For more information, see Security group rules. You must specify exactly one of the following sources: an IPv4 or IPv6 address range, a prefix list, or a security group. You must specify a protocol for each rule (for example, TCP). If the protocol is TCP or UDP, you must also specify a port or port range. If the protocol is ICMP or ICMPv6, you must also specify the ICMP/ICMPv6 type and code. Rule changes are propagated to instances associated with the security group as quickly as possible. However, a small delay might occur. For examples of rules that you can add to security groups for specific access scenarios, see Security group rules for different use cases in the Amazon EC2 User Guide. For more information about security group quotas, see Amazon VPC quotas in the Amazon VPC User Guide."]
module AuthorizeSecurityGroupIngressRequest =
  struct
    type nonrec t =
      {
      cidrIp: String_.t option
        [@ocaml.doc
          "The IPv4 address range, in CIDR format. Amazon Web Services canonicalizes IPv4 and IPv6 CIDRs. For example, if you specify 100.68.0.18/18 for the CIDR block, Amazon Web Services canonicalizes the CIDR block to 100.68.0.0/18. Any subsequent DescribeSecurityGroups and DescribeSecurityGroupRules calls will return the canonicalized form of the CIDR block. Additionally, if you attempt to add another rule with the non-canonical form of the CIDR (such as 100.68.0.18/18) and there is already a rule for the canonicalized form of the CIDR block (such as 100.68.0.0/18), the API throws an duplicate rule error. To specify an IPv6 address range, use IP permissions instead. To specify multiple rules and descriptions for the rules, use IP permissions instead."];
      fromPort: Integer.t option
        [@ocaml.doc
          "If the protocol is TCP or UDP, this is the start of the port range. If the protocol is ICMP, this is the ICMP type or -1 (all ICMP types). To specify multiple rules and descriptions for the rules, use IP permissions instead."];
      groupId: SecurityGroupId.t option
        [@ocaml.doc "The ID of the security group."];
      groupName: SecurityGroupName.t option
        [@ocaml.doc
          "\\[Default VPC\\] The name of the security group. For security groups for a default VPC you can specify either the ID or the name of the security group. For security groups for a nondefault VPC, you must specify the ID of the security group."];
      ipPermissions: IpPermissionList.t option
        [@ocaml.doc "The permissions for the security group rules."];
      ipProtocol: String_.t option
        [@ocaml.doc
          "The IP protocol name (tcp, udp, icmp) or number (see Protocol Numbers). To specify all protocols, use -1. To specify icmpv6, use IP permissions instead. If you specify a protocol other than one of the supported values, traffic is allowed on all ports, regardless of any ports that you specify. To specify multiple rules and descriptions for the rules, use IP permissions instead."];
      sourceSecurityGroupName: String_.t option
        [@ocaml.doc
          "\\[Default VPC\\] The name of the source security group. The rule grants full ICMP, UDP, and TCP access. To create a rule with a specific protocol and port range, specify a set of IP permissions instead."];
      sourceSecurityGroupOwnerId: String_.t option
        [@ocaml.doc
          "The Amazon Web Services account ID for the source security group, if the source security group is in a different account. The rule grants full ICMP, UDP, and TCP access. To create a rule with a specific protocol and port range, use IP permissions instead."];
      toPort: Integer.t option
        [@ocaml.doc
          "If the protocol is TCP or UDP, this is the end of the port range. If the protocol is ICMP, this is the ICMP code or -1 (all ICMP codes). If the start port is -1 (all ICMP types), then the end port must be -1 (all ICMP codes). To specify multiple rules and descriptions for the rules, use IP permissions instead."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags applied to the security group rule."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?cidrIp =
      fun ?fromPort ->
        fun ?groupId ->
          fun ?groupName ->
            fun ?ipPermissions ->
              fun ?ipProtocol ->
                fun ?sourceSecurityGroupName ->
                  fun ?sourceSecurityGroupOwnerId ->
                    fun ?toPort ->
                      fun ?tagSpecifications ->
                        fun ?dryRun ->
                          fun () ->
                            {
                              cidrIp;
                              fromPort;
                              groupId;
                              groupName;
                              ipPermissions;
                              ipProtocol;
                              sourceSecurityGroupName;
                              sourceSecurityGroupOwnerId;
                              toPort;
                              tagSpecifications;
                              dryRun
                            }
    let to_value x =
      structure_to_value
        [("CidrIp", (Option.map x.cidrIp ~f:String_.to_value));
        ("FromPort", (Option.map x.fromPort ~f:Integer.to_value));
        ("GroupId", (Option.map x.groupId ~f:SecurityGroupId.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value));
        ("IpPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value));
        ("IpProtocol", (Option.map x.ipProtocol ~f:String_.to_value));
        ("SourceSecurityGroupName",
          (Option.map x.sourceSecurityGroupName ~f:String_.to_value));
        ("SourceSecurityGroupOwnerId",
          (Option.map x.sourceSecurityGroupOwnerId ~f:String_.to_value));
        ("ToPort", (Option.map x.toPort ~f:Integer.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let toPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "ToPort") in
      let sourceSecurityGroupOwnerId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroupOwnerId") in
      let sourceSecurityGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroupName") in
      let ipProtocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "IpProtocol") in
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "IpPermissions") in
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let groupId =
        (Option.map ~f:SecurityGroupId.of_xml) (Xml.child xml_arg0 "GroupId") in
      let fromPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "FromPort") in
      let cidrIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrIp") in
      make ?dryRun ?tagSpecifications ?toPort ?sourceSecurityGroupOwnerId
        ?sourceSecurityGroupName ?ipProtocol ?ipPermissions ?groupName
        ?groupId ?fromPort ?cidrIp ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let toPort = field_map json__ "ToPort" Integer.of_json in
      let sourceSecurityGroupOwnerId =
        field_map json__ "SourceSecurityGroupOwnerId" String_.of_json in
      let sourceSecurityGroupName =
        field_map json__ "SourceSecurityGroupName" String_.of_json in
      let ipProtocol = field_map json__ "IpProtocol" String_.of_json in
      let ipPermissions =
        field_map json__ "IpPermissions" IpPermissionList.of_json in
      let groupName = field_map json__ "GroupName" SecurityGroupName.of_json in
      let groupId = field_map json__ "GroupId" SecurityGroupId.of_json in
      let fromPort = field_map json__ "FromPort" Integer.of_json in
      let cidrIp = field_map json__ "CidrIp" String_.of_json in
      make ?dryRun ?tagSpecifications ?toPort ?sourceSecurityGroupOwnerId
        ?sourceSecurityGroupName ?ipProtocol ?ipPermissions ?groupName
        ?groupId ?fromPort ?cidrIp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds the specified inbound (ingress) rules to a security group. An inbound rule permits instances to receive traffic from the specified IPv4 or IPv6 address range, the IP address ranges that are specified by a prefix list, or the instances that are associated with a destination security group. For more information, see Security group rules. You must specify exactly one of the following sources: an IPv4 or IPv6 address range, a prefix list, or a security group. You must specify a protocol for each rule (for example, TCP). If the protocol is TCP or UDP, you must also specify a port or port range. If the protocol is ICMP or ICMPv6, you must also specify the ICMP/ICMPv6 type and code. Rule changes are propagated to instances associated with the security group as quickly as possible. However, a small delay might occur. For examples of rules that you can add to security groups for specific access scenarios, see Security group rules for different use cases in the Amazon EC2 User Guide. For more information about security group quotas, see Amazon VPC quotas in the Amazon VPC User Guide."]
module AuthorizeSecurityGroupEgressResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, returns an error."];
      securityGroupRules: SecurityGroupRuleList.t option
        [@ocaml.doc
          "Information about the outbound (egress) security group rules that were added."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return =
      fun ?securityGroupRules -> fun () -> { return; securityGroupRules }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value));
        ("securityGroupRuleSet",
          (Option.map x.securityGroupRules ~f:SecurityGroupRuleList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupRules =
        (Option.map ~f:SecurityGroupRuleList.of_xml)
          (Xml.child xml_arg0 "securityGroupRuleSet") in
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?securityGroupRules ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupRules =
        field_map json__ "SecurityGroupRules" SecurityGroupRuleList.of_json in
      let return = field_map json__ "Return" Boolean.of_json in
      make ?securityGroupRules ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds the specified outbound (egress) rules to a security group. An outbound rule permits instances to send traffic to the specified IPv4 or IPv6 address ranges, the IP address ranges specified by a prefix list, or the instances that are associated with a source security group. For more information, see Security group rules. You must specify exactly one of the following destinations: an IPv4 or IPv6 address range, a prefix list, or a security group. You must specify a protocol for each rule (for example, TCP). If the protocol is TCP or UDP, you must also specify a port or port range. If the protocol is ICMP or ICMPv6, you must also specify the ICMP type and code. Rule changes are propagated to instances associated with the security group as quickly as possible. However, a small delay might occur. For examples of rules that you can add to security groups for specific access scenarios, see Security group rules for different use cases in the Amazon EC2 User Guide. For information about security group quotas, see Amazon VPC quotas in the Amazon VPC User Guide."]
module AuthorizeSecurityGroupEgressRequest =
  struct
    type nonrec t =
      {
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags applied to the security group rule."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      groupId: SecurityGroupId.t [@ocaml.doc "The ID of the security group."];
      sourceSecurityGroupName: String_.t option
        [@ocaml.doc "Not supported. Use IP permissions instead."];
      sourceSecurityGroupOwnerId: String_.t option
        [@ocaml.doc "Not supported. Use IP permissions instead."];
      ipProtocol: String_.t option
        [@ocaml.doc "Not supported. Use IP permissions instead."];
      fromPort: Integer.t option
        [@ocaml.doc "Not supported. Use IP permissions instead."];
      toPort: Integer.t option
        [@ocaml.doc "Not supported. Use IP permissions instead."];
      cidrIp: String_.t option
        [@ocaml.doc "Not supported. Use IP permissions instead."];
      ipPermissions: IpPermissionList.t option
        [@ocaml.doc "The permissions for the security group rules."]}
    let context_ = "AuthorizeSecurityGroupEgressRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ?sourceSecurityGroupName ->
          fun ?sourceSecurityGroupOwnerId ->
            fun ?ipProtocol ->
              fun ?fromPort ->
                fun ?toPort ->
                  fun ?cidrIp ->
                    fun ?ipPermissions ->
                      fun ~groupId ->
                        fun () ->
                          {
                            tagSpecifications;
                            dryRun;
                            sourceSecurityGroupName;
                            sourceSecurityGroupOwnerId;
                            ipProtocol;
                            fromPort;
                            toPort;
                            cidrIp;
                            ipPermissions;
                            groupId
                          }
    let to_value x =
      structure_to_value
        [("TagSpecification",
           (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("groupId", (Some (SecurityGroupId.to_value x.groupId)));
        ("sourceSecurityGroupName",
          (Option.map x.sourceSecurityGroupName ~f:String_.to_value));
        ("sourceSecurityGroupOwnerId",
          (Option.map x.sourceSecurityGroupOwnerId ~f:String_.to_value));
        ("ipProtocol", (Option.map x.ipProtocol ~f:String_.to_value));
        ("fromPort", (Option.map x.fromPort ~f:Integer.to_value));
        ("toPort", (Option.map x.toPort ~f:Integer.to_value));
        ("cidrIp", (Option.map x.cidrIp ~f:String_.to_value));
        ("ipPermissions",
          (Option.map x.ipPermissions ~f:IpPermissionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipPermissions =
        (Option.map ~f:IpPermissionList.of_xml)
          (Xml.child xml_arg0 "ipPermissions") in
      let cidrIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cidrIp") in
      let toPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "toPort") in
      let fromPort =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "fromPort") in
      let ipProtocol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipProtocol") in
      let sourceSecurityGroupOwnerId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceSecurityGroupOwnerId") in
      let sourceSecurityGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceSecurityGroupName") in
      let groupId =
        SecurityGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      make ?ipPermissions ?cidrIp ?toPort ?fromPort ?ipProtocol
        ?sourceSecurityGroupOwnerId ?sourceSecurityGroupName ~groupId ?dryRun
        ?tagSpecifications ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipPermissions =
        field_map json__ "IpPermissions" IpPermissionList.of_json in
      let cidrIp = field_map json__ "CidrIp" String_.of_json in
      let toPort = field_map json__ "ToPort" Integer.of_json in
      let fromPort = field_map json__ "FromPort" Integer.of_json in
      let ipProtocol = field_map json__ "IpProtocol" String_.of_json in
      let sourceSecurityGroupOwnerId =
        field_map json__ "SourceSecurityGroupOwnerId" String_.of_json in
      let sourceSecurityGroupName =
        field_map json__ "SourceSecurityGroupName" String_.of_json in
      let groupId = field_map_exn json__ "GroupId" SecurityGroupId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      make ?ipPermissions ?cidrIp ?toPort ?fromPort ?ipProtocol
        ?sourceSecurityGroupOwnerId ?sourceSecurityGroupName ~groupId ?dryRun
        ?tagSpecifications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds the specified outbound (egress) rules to a security group. An outbound rule permits instances to send traffic to the specified IPv4 or IPv6 address ranges, the IP address ranges specified by a prefix list, or the instances that are associated with a source security group. For more information, see Security group rules. You must specify exactly one of the following destinations: an IPv4 or IPv6 address range, a prefix list, or a security group. You must specify a protocol for each rule (for example, TCP). If the protocol is TCP or UDP, you must also specify a port or port range. If the protocol is ICMP or ICMPv6, you must also specify the ICMP type and code. Rule changes are propagated to instances associated with the security group as quickly as possible. However, a small delay might occur. For examples of rules that you can add to security groups for specific access scenarios, see Security group rules for different use cases in the Amazon EC2 User Guide. For information about security group quotas, see Amazon VPC quotas in the Amazon VPC User Guide."]
module AuthorizeClientVpnIngressResult =
  struct
    type nonrec t =
      {
      status: ClientVpnAuthorizationRuleStatus.t option
        [@ocaml.doc "The current state of the authorization rule."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status",
           (Option.map x.status ~f:ClientVpnAuthorizationRuleStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ClientVpnAuthorizationRuleStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status =
        field_map json__ "Status" ClientVpnAuthorizationRuleStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds an ingress authorization rule to a Client VPN endpoint. Ingress authorization rules act as firewall rules that grant access to networks. You must configure ingress authorization rules to enable clients to access resources in Amazon Web Services or on-premises networks."]
module AuthorizeClientVpnIngressRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      targetNetworkCidr: String_.t
        [@ocaml.doc
          "The IPv4 address range, in CIDR notation, of the network for which access is being authorized."];
      accessGroupId: String_.t option
        [@ocaml.doc
          "The ID of the group to grant access to, for example, the Active Directory group or identity provider (IdP) group. Required if AuthorizeAllGroups is false or not specified."];
      authorizeAllGroups: Boolean.t option
        [@ocaml.doc
          "Indicates whether to grant access to all clients. Specify true to grant all clients who successfully establish a VPN connection access to the network. Must be set to true if AccessGroupId is not specified."];
      description: String_.t option
        [@ocaml.doc "A brief description of the authorization rule."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AuthorizeClientVpnIngressRequest"
    let make ?accessGroupId =
      fun ?authorizeAllGroups ->
        fun ?description ->
          fun ?clientToken ->
            fun ?dryRun ->
              fun ~clientVpnEndpointId ->
                fun ~targetNetworkCidr ->
                  fun () ->
                    {
                      accessGroupId;
                      authorizeAllGroups;
                      description;
                      clientToken;
                      dryRun;
                      clientVpnEndpointId;
                      targetNetworkCidr
                    }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("TargetNetworkCidr", (Some (String_.to_value x.targetNetworkCidr)));
        ("AccessGroupId", (Option.map x.accessGroupId ~f:String_.to_value));
        ("AuthorizeAllGroups",
          (Option.map x.authorizeAllGroups ~f:Boolean.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let authorizeAllGroups =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AuthorizeAllGroups") in
      let accessGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccessGroupId") in
      let targetNetworkCidr =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetNetworkCidr") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ?clientToken ?description ?authorizeAllGroups
        ?accessGroupId ~targetNetworkCidr ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let authorizeAllGroups =
        field_map json__ "AuthorizeAllGroups" Boolean.of_json in
      let accessGroupId = field_map json__ "AccessGroupId" String_.of_json in
      let targetNetworkCidr =
        field_map_exn json__ "TargetNetworkCidr" String_.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ?clientToken ?description ?authorizeAllGroups
        ?accessGroupId ~targetNetworkCidr ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds an ingress authorization rule to a Client VPN endpoint. Ingress authorization rules act as firewall rules that grant access to networks. You must configure ingress authorization rules to enable clients to access resources in Amazon Web Services or on-premises networks."]
module AttachVpnGatewayResult =
  struct
    type nonrec t =
      {
      vpcAttachment: VpcAttachment.t option
        [@ocaml.doc "Information about the attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcAttachment = fun () -> { vpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachment",
           (Option.map x.vpcAttachment ~f:VpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcAttachment =
        (Option.map ~f:VpcAttachment.of_xml)
          (Xml.child xml_arg0 "attachment") in
      make ?vpcAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcAttachment =
        field_map json__ "VpcAttachment" VpcAttachment.of_json in
      make ?vpcAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of AttachVpnGateway."]
module AttachVpnGatewayRequest =
  struct
    type nonrec t =
      {
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      vpnGatewayId: VpnGatewayId.t
        [@ocaml.doc "The ID of the virtual private gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AttachVpnGatewayRequest"
    let make ?dryRun =
      fun ~vpcId ->
        fun ~vpnGatewayId -> fun () -> { dryRun; vpcId; vpnGatewayId }
    let to_value x =
      structure_to_value
        [("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("VpnGatewayId", (Some (VpnGatewayId.to_value x.vpnGatewayId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpnGatewayId =
        VpnGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpnGatewayId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      make ?dryRun ~vpnGatewayId ~vpcId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpnGatewayId =
        field_map_exn json__ "VpnGatewayId" VpnGatewayId.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      make ?dryRun ~vpnGatewayId ~vpcId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for AttachVpnGateway."]
module AttachVolumeRequest =
  struct
    type nonrec t =
      {
      device: String_.t
        [@ocaml.doc "The device name (for example, /dev/sdh or xvdh)."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      volumeId: VolumeId.t
        [@ocaml.doc
          "The ID of the EBS volume. The volume and instance must be within the same Availability Zone."];
      ebsCardIndex: BoxedInteger.t option
        [@ocaml.doc
          "The index of the EBS card. Some instance types support multiple EBS cards. The default EBS card index is 0."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AttachVolumeRequest"
    let make ?ebsCardIndex =
      fun ?dryRun ->
        fun ~device ->
          fun ~instanceId ->
            fun ~volumeId ->
              fun () ->
                { ebsCardIndex; dryRun; device; instanceId; volumeId }
    let to_value x =
      structure_to_value
        [("Device", (Some (String_.to_value x.device)));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)));
        ("VolumeId", (Some (VolumeId.to_value x.volumeId)));
        ("EbsCardIndex",
          (Option.map x.ebsCardIndex ~f:BoxedInteger.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let ebsCardIndex =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "EbsCardIndex") in
      let volumeId =
        VolumeId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VolumeId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let device =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Device") in
      make ?dryRun ?ebsCardIndex ~volumeId ~instanceId ~device ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let ebsCardIndex = field_map json__ "EbsCardIndex" BoxedInteger.of_json in
      let volumeId = field_map_exn json__ "VolumeId" VolumeId.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let device = field_map_exn json__ "Device" String_.of_json in
      make ?dryRun ?ebsCardIndex ~volumeId ~instanceId ~device ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches an Amazon EBS volume to a running or stopped instance, and exposes it to the instance with the specified device name. The maximum number of Amazon EBS volumes that you can attach to an instance depends on the instance type. If you exceed the volume attachment limit for an instance type, the attachment request fails with the AttachmentLimitExceeded error. For more information, see Instance volume limits. After you attach an EBS volume, you must make it available for use. For more information, see Make an EBS volume available for use. If a volume has an Amazon Web Services Marketplace product code: The volume can be attached only to a stopped instance. Amazon Web Services Marketplace product codes are copied from the volume to the instance. You must be subscribed to the product. The instance type and operating system of the instance must support the product. For example, you can't detach a volume from a Windows instance and attach it to a Linux instance. For more information, see Attach an Amazon EBS volume to an instance in the Amazon EBS User Guide."]
module AttachVerifiedAccessTrustProviderResult =
  struct
    type nonrec t =
      {
      verifiedAccessTrustProvider: VerifiedAccessTrustProvider.t option
        [@ocaml.doc "Details about the Verified Access trust provider."];
      verifiedAccessInstance: VerifiedAccessInstance.t option
        [@ocaml.doc "Details about the Verified Access instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?verifiedAccessTrustProvider =
      fun ?verifiedAccessInstance ->
        fun () -> { verifiedAccessTrustProvider; verifiedAccessInstance }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("verifiedAccessTrustProvider",
           (Option.map x.verifiedAccessTrustProvider
              ~f:VerifiedAccessTrustProvider.to_value));
        ("verifiedAccessInstance",
          (Option.map x.verifiedAccessInstance
             ~f:VerifiedAccessInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verifiedAccessInstance =
        (Option.map ~f:VerifiedAccessInstance.of_xml)
          (Xml.child xml_arg0 "verifiedAccessInstance") in
      let verifiedAccessTrustProvider =
        (Option.map ~f:VerifiedAccessTrustProvider.of_xml)
          (Xml.child xml_arg0 "verifiedAccessTrustProvider") in
      make ?verifiedAccessInstance ?verifiedAccessTrustProvider ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verifiedAccessInstance =
        field_map json__ "VerifiedAccessInstance"
          VerifiedAccessInstance.of_json in
      let verifiedAccessTrustProvider =
        field_map json__ "VerifiedAccessTrustProvider"
          VerifiedAccessTrustProvider.of_json in
      make ?verifiedAccessInstance ?verifiedAccessTrustProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches the specified Amazon Web Services Verified Access trust provider to the specified Amazon Web Services Verified Access instance."]
module AttachVerifiedAccessTrustProviderRequest =
  struct
    type nonrec t =
      {
      verifiedAccessInstanceId: VerifiedAccessInstanceId.t
        [@ocaml.doc "The ID of the Verified Access instance."];
      verifiedAccessTrustProviderId: VerifiedAccessTrustProviderId.t
        [@ocaml.doc "The ID of the Verified Access trust provider."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure idempotency of your modification request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AttachVerifiedAccessTrustProviderRequest"
    let make ?clientToken =
      fun ?dryRun ->
        fun ~verifiedAccessInstanceId ->
          fun ~verifiedAccessTrustProviderId ->
            fun () ->
              {
                clientToken;
                dryRun;
                verifiedAccessInstanceId;
                verifiedAccessTrustProviderId
              }
    let to_value x =
      structure_to_value
        [("VerifiedAccessInstanceId",
           (Some
              (VerifiedAccessInstanceId.to_value x.verifiedAccessInstanceId)));
        ("VerifiedAccessTrustProviderId",
          (Some
             (VerifiedAccessTrustProviderId.to_value
                x.verifiedAccessTrustProviderId)));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let verifiedAccessTrustProviderId =
        VerifiedAccessTrustProviderId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VerifiedAccessTrustProviderId") in
      let verifiedAccessInstanceId =
        VerifiedAccessInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "VerifiedAccessInstanceId") in
      make ?dryRun ?clientToken ~verifiedAccessTrustProviderId
        ~verifiedAccessInstanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let verifiedAccessTrustProviderId =
        field_map_exn json__ "VerifiedAccessTrustProviderId"
          VerifiedAccessTrustProviderId.of_json in
      let verifiedAccessInstanceId =
        field_map_exn json__ "VerifiedAccessInstanceId"
          VerifiedAccessInstanceId.of_json in
      make ?dryRun ?clientToken ~verifiedAccessTrustProviderId
        ~verifiedAccessInstanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches the specified Amazon Web Services Verified Access trust provider to the specified Amazon Web Services Verified Access instance."]
module AttachNetworkInterfaceResult =
  struct
    type nonrec t =
      {
      attachmentId: String_.t option
        [@ocaml.doc "The ID of the network interface attachment."];
      networkCardIndex: Integer.t option
        [@ocaml.doc "The index of the network card."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?attachmentId =
      fun ?networkCardIndex -> fun () -> { attachmentId; networkCardIndex }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachmentId", (Option.map x.attachmentId ~f:String_.to_value));
        ("networkCardIndex",
          (Option.map x.networkCardIndex ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkCardIndex =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "networkCardIndex") in
      let attachmentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "attachmentId") in
      make ?networkCardIndex ?attachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkCardIndex =
        field_map json__ "NetworkCardIndex" Integer.of_json in
      let attachmentId = field_map json__ "AttachmentId" String_.of_json in
      make ?networkCardIndex ?attachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of AttachNetworkInterface."]
module AttachNetworkInterfaceRequest =
  struct
    type nonrec t =
      {
      networkCardIndex: Integer.t option
        [@ocaml.doc
          "The index of the network card. Some instance types support multiple network cards. The primary network interface must be assigned to network card index 0. The default is network card index 0."];
      enaSrdSpecification: EnaSrdSpecification.t option
        [@ocaml.doc
          "Configures ENA Express for the network interface that this action attaches to the instance."];
      enaQueueCount: Integer.t option
        [@ocaml.doc
          "The number of ENA queues to be created with the instance."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."];
      deviceIndex: Integer.t
        [@ocaml.doc
          "The index of the device for the network interface attachment."]}
    let context_ = "AttachNetworkInterfaceRequest"
    let make ?networkCardIndex =
      fun ?enaSrdSpecification ->
        fun ?enaQueueCount ->
          fun ?dryRun ->
            fun ~networkInterfaceId ->
              fun ~instanceId ->
                fun ~deviceIndex ->
                  fun () ->
                    {
                      networkCardIndex;
                      enaSrdSpecification;
                      enaQueueCount;
                      dryRun;
                      networkInterfaceId;
                      instanceId;
                      deviceIndex
                    }
    let to_value x =
      structure_to_value
        [("NetworkCardIndex",
           (Option.map x.networkCardIndex ~f:Integer.to_value));
        ("EnaSrdSpecification",
          (Option.map x.enaSrdSpecification ~f:EnaSrdSpecification.to_value));
        ("EnaQueueCount", (Option.map x.enaQueueCount ~f:Integer.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("deviceIndex", (Some (Integer.to_value x.deviceIndex)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceIndex =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "deviceIndex") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let enaQueueCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "EnaQueueCount") in
      let enaSrdSpecification =
        (Option.map ~f:EnaSrdSpecification.of_xml)
          (Xml.child xml_arg0 "EnaSrdSpecification") in
      let networkCardIndex =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NetworkCardIndex") in
      make ~deviceIndex ~instanceId ~networkInterfaceId ?dryRun
        ?enaQueueCount ?enaSrdSpecification ?networkCardIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deviceIndex = field_map_exn json__ "DeviceIndex" Integer.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let enaQueueCount = field_map json__ "EnaQueueCount" Integer.of_json in
      let enaSrdSpecification =
        field_map json__ "EnaSrdSpecification" EnaSrdSpecification.of_json in
      let networkCardIndex =
        field_map json__ "NetworkCardIndex" Integer.of_json in
      make ~deviceIndex ~instanceId ~networkInterfaceId ?dryRun
        ?enaQueueCount ?enaSrdSpecification ?networkCardIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for AttachNetworkInterface."]
module AttachInternetGatewayRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      internetGatewayId: InternetGatewayId.t
        [@ocaml.doc "The ID of the internet gateway."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."]}
    let context_ = "AttachInternetGatewayRequest"
    let make ?dryRun =
      fun ~internetGatewayId ->
        fun ~vpcId -> fun () -> { dryRun; internetGatewayId; vpcId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("internetGatewayId",
          (Some (InternetGatewayId.to_value x.internetGatewayId)));
        ("vpcId", (Some (VpcId.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let internetGatewayId =
        InternetGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "internetGatewayId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcId ~internetGatewayId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let internetGatewayId =
        field_map_exn json__ "InternetGatewayId" InternetGatewayId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~vpcId ~internetGatewayId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches an internet gateway or a virtual private gateway to a VPC, enabling connectivity between the internet and the VPC. For more information, see Internet gateways in the Amazon VPC User Guide."]
module AttachClassicLinkVpcResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is deprecated. Links an EC2-Classic instance to a ClassicLink-enabled VPC through one or more of the VPC security groups. You cannot link an EC2-Classic instance to more than one VPC at a time. You can only link an instance that's in the running state. An instance is automatically unlinked from a VPC when it's stopped - you can link it to the VPC again when you restart it. After you've linked an instance, you cannot change the VPC security groups that are associated with it. To change the security groups, you must first unlink the instance, and then link it again. Linking your instance to a VPC is sometimes referred to as attaching your instance."]
module AttachClassicLinkVpcRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceId: InstanceId.t
        [@ocaml.doc "The ID of the EC2-Classic instance."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the ClassicLink-enabled VPC."];
      groups: GroupIdStringList.t
        [@ocaml.doc
          "The IDs of the security groups. You cannot specify security groups from a different VPC."]}
    let context_ = "AttachClassicLinkVpcRequest"
    let make ?dryRun =
      fun ~instanceId ->
        fun ~vpcId ->
          fun ~groups -> fun () -> { dryRun; instanceId; vpcId; groups }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("instanceId", (Some (InstanceId.to_value x.instanceId)));
        ("vpcId", (Some (VpcId.to_value x.vpcId)));
        ("SecurityGroupId", (Some (GroupIdStringList.to_value x.groups)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groups =
        GroupIdStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~groups ~vpcId ~instanceId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groups = field_map_exn json__ "Groups" GroupIdStringList.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~groups ~vpcId ~instanceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is deprecated. Links an EC2-Classic instance to a ClassicLink-enabled VPC through one or more of the VPC security groups. You cannot link an EC2-Classic instance to more than one VPC at a time. You can only link an instance that's in the running state. An instance is automatically unlinked from a VPC when it's stopped - you can link it to the VPC again when you restart it. After you've linked an instance, you cannot change the VPC security groups that are associated with it. To change the security groups, you must first unlink the instance, and then link it again. Linking your instance to a VPC is sometimes referred to as attaching your instance."]
module AssociateVpcCidrBlockResult =
  struct
    type nonrec t =
      {
      ipv6CidrBlockAssociation: VpcIpv6CidrBlockAssociation.t option
        [@ocaml.doc "Information about the IPv6 CIDR block association."];
      cidrBlockAssociation: VpcCidrBlockAssociation.t option
        [@ocaml.doc "Information about the IPv4 CIDR block association."];
      vpcId: String_.t option [@ocaml.doc "The ID of the VPC."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrBlockAssociation =
      fun ?cidrBlockAssociation ->
        fun ?vpcId ->
          fun () -> { ipv6CidrBlockAssociation; cidrBlockAssociation; vpcId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrBlockAssociation",
           (Option.map x.ipv6CidrBlockAssociation
              ~f:VpcIpv6CidrBlockAssociation.to_value));
        ("cidrBlockAssociation",
          (Option.map x.cidrBlockAssociation
             ~f:VpcCidrBlockAssociation.to_value));
        ("vpcId", (Option.map x.vpcId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "vpcId") in
      let cidrBlockAssociation =
        (Option.map ~f:VpcCidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "cidrBlockAssociation") in
      let ipv6CidrBlockAssociation =
        (Option.map ~f:VpcIpv6CidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "ipv6CidrBlockAssociation") in
      make ?vpcId ?cidrBlockAssociation ?ipv6CidrBlockAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcId = field_map json__ "VpcId" String_.of_json in
      let cidrBlockAssociation =
        field_map json__ "CidrBlockAssociation"
          VpcCidrBlockAssociation.of_json in
      let ipv6CidrBlockAssociation =
        field_map json__ "Ipv6CidrBlockAssociation"
          VpcIpv6CidrBlockAssociation.of_json in
      make ?vpcId ?cidrBlockAssociation ?ipv6CidrBlockAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a CIDR block with your VPC. You can associate a secondary IPv4 CIDR block, an Amazon-provided IPv6 CIDR block, or an IPv6 CIDR block from an IPv6 address pool that you provisioned through bring your own IP addresses (BYOIP). You must specify one of the following in the request: an IPv4 CIDR block, an IPv6 pool, or an Amazon-provided IPv6 CIDR block. For more information about associating CIDR blocks with your VPC and applicable restrictions, see IP addressing for your VPCs and subnets in the Amazon VPC User Guide."]
module AssociateVpcCidrBlockRequest =
  struct
    type nonrec t =
      {
      cidrBlock: String_.t option
        [@ocaml.doc "An IPv4 CIDR block to associate with the VPC."];
      ipv6CidrBlockNetworkBorderGroup: String_.t option
        [@ocaml.doc
          "The name of the location from which we advertise the IPV6 CIDR block. Use this parameter to limit the CIDR block to this location. You must set AmazonProvidedIpv6CidrBlock to true to use this parameter. You can have one IPv6 CIDR block association per network border group."];
      ipv6Pool: Ipv6PoolEc2Id.t option
        [@ocaml.doc
          "The ID of an IPv6 address pool from which to allocate the IPv6 CIDR block."];
      ipv6CidrBlock: String_.t option
        [@ocaml.doc
          "An IPv6 CIDR block from the IPv6 address pool. You must also specify Ipv6Pool in the request. To let Amazon choose the IPv6 CIDR block for you, omit this parameter."];
      ipv4IpamPoolId: IpamPoolId.t option
        [@ocaml.doc
          "Associate a CIDR allocated from an IPv4 IPAM pool to a VPC. For more information about Amazon VPC IP Address Manager (IPAM), see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv4NetmaskLength: NetmaskLength.t option
        [@ocaml.doc
          "The netmask length of the IPv4 CIDR you would like to associate from an Amazon VPC IP Address Manager (IPAM) pool. For more information about IPAM, see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv6IpamPoolId: IpamPoolId.t option
        [@ocaml.doc
          "Associates a CIDR allocated from an IPv6 IPAM pool to a VPC. For more information about Amazon VPC IP Address Manager (IPAM), see What is IPAM? in the Amazon VPC IPAM User Guide."];
      ipv6NetmaskLength: NetmaskLength.t option
        [@ocaml.doc
          "The netmask length of the IPv6 CIDR you would like to associate from an Amazon VPC IP Address Manager (IPAM) pool. For more information about IPAM, see What is IPAM? in the Amazon VPC IPAM User Guide."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      amazonProvidedIpv6CidrBlock: Boolean.t option
        [@ocaml.doc
          "Requests an Amazon-provided IPv6 CIDR block with a /56 prefix length for the VPC. You cannot specify the range of IPv6 addresses or the size of the CIDR block."]}
    let context_ = "AssociateVpcCidrBlockRequest"
    let make ?cidrBlock =
      fun ?ipv6CidrBlockNetworkBorderGroup ->
        fun ?ipv6Pool ->
          fun ?ipv6CidrBlock ->
            fun ?ipv4IpamPoolId ->
              fun ?ipv4NetmaskLength ->
                fun ?ipv6IpamPoolId ->
                  fun ?ipv6NetmaskLength ->
                    fun ?amazonProvidedIpv6CidrBlock ->
                      fun ~vpcId ->
                        fun () ->
                          {
                            cidrBlock;
                            ipv6CidrBlockNetworkBorderGroup;
                            ipv6Pool;
                            ipv6CidrBlock;
                            ipv4IpamPoolId;
                            ipv4NetmaskLength;
                            ipv6IpamPoolId;
                            ipv6NetmaskLength;
                            amazonProvidedIpv6CidrBlock;
                            vpcId
                          }
    let to_value x =
      structure_to_value
        [("CidrBlock", (Option.map x.cidrBlock ~f:String_.to_value));
        ("Ipv6CidrBlockNetworkBorderGroup",
          (Option.map x.ipv6CidrBlockNetworkBorderGroup ~f:String_.to_value));
        ("Ipv6Pool", (Option.map x.ipv6Pool ~f:Ipv6PoolEc2Id.to_value));
        ("Ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value));
        ("Ipv4IpamPoolId",
          (Option.map x.ipv4IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv4NetmaskLength",
          (Option.map x.ipv4NetmaskLength ~f:NetmaskLength.to_value));
        ("Ipv6IpamPoolId",
          (Option.map x.ipv6IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv6NetmaskLength",
          (Option.map x.ipv6NetmaskLength ~f:NetmaskLength.to_value));
        ("vpcId", (Some (VpcId.to_value x.vpcId)));
        ("amazonProvidedIpv6CidrBlock",
          (Option.map x.amazonProvidedIpv6CidrBlock ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let amazonProvidedIpv6CidrBlock =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "amazonProvidedIpv6CidrBlock") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let ipv6NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv6NetmaskLength") in
      let ipv6IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv6IpamPoolId") in
      let ipv4NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv4NetmaskLength") in
      let ipv4IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv4IpamPoolId") in
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ipv6CidrBlock") in
      let ipv6Pool =
        (Option.map ~f:Ipv6PoolEc2Id.of_xml) (Xml.child xml_arg0 "Ipv6Pool") in
      let ipv6CidrBlockNetworkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "Ipv6CidrBlockNetworkBorderGroup") in
      let cidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      make ?amazonProvidedIpv6CidrBlock ~vpcId ?ipv6NetmaskLength
        ?ipv6IpamPoolId ?ipv4NetmaskLength ?ipv4IpamPoolId ?ipv6CidrBlock
        ?ipv6Pool ?ipv6CidrBlockNetworkBorderGroup ?cidrBlock ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let amazonProvidedIpv6CidrBlock =
        field_map json__ "AmazonProvidedIpv6CidrBlock" Boolean.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let ipv6NetmaskLength =
        field_map json__ "Ipv6NetmaskLength" NetmaskLength.of_json in
      let ipv6IpamPoolId =
        field_map json__ "Ipv6IpamPoolId" IpamPoolId.of_json in
      let ipv4NetmaskLength =
        field_map json__ "Ipv4NetmaskLength" NetmaskLength.of_json in
      let ipv4IpamPoolId =
        field_map json__ "Ipv4IpamPoolId" IpamPoolId.of_json in
      let ipv6CidrBlock = field_map json__ "Ipv6CidrBlock" String_.of_json in
      let ipv6Pool = field_map json__ "Ipv6Pool" Ipv6PoolEc2Id.of_json in
      let ipv6CidrBlockNetworkBorderGroup =
        field_map json__ "Ipv6CidrBlockNetworkBorderGroup" String_.of_json in
      let cidrBlock = field_map json__ "CidrBlock" String_.of_json in
      make ?amazonProvidedIpv6CidrBlock ~vpcId ?ipv6NetmaskLength
        ?ipv6IpamPoolId ?ipv4NetmaskLength ?ipv4IpamPoolId ?ipv6CidrBlock
        ?ipv6Pool ?ipv6CidrBlockNetworkBorderGroup ?cidrBlock ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a CIDR block with your VPC. You can associate a secondary IPv4 CIDR block, an Amazon-provided IPv6 CIDR block, or an IPv6 CIDR block from an IPv6 address pool that you provisioned through bring your own IP addresses (BYOIP). You must specify one of the following in the request: an IPv4 CIDR block, an IPv6 pool, or an Amazon-provided IPv6 CIDR block. For more information about associating CIDR blocks with your VPC and applicable restrictions, see IP addressing for your VPCs and subnets in the Amazon VPC User Guide."]
module AssociateTrunkInterfaceResult =
  struct
    type nonrec t =
      {
      interfaceAssociation: TrunkInterfaceAssociation.t option
        [@ocaml.doc
          "Information about the association between the trunk network interface and branch network interface."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?interfaceAssociation =
      fun ?clientToken -> fun () -> { interfaceAssociation; clientToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("interfaceAssociation",
           (Option.map x.interfaceAssociation
              ~f:TrunkInterfaceAssociation.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let interfaceAssociation =
        (Option.map ~f:TrunkInterfaceAssociation.of_xml)
          (Xml.child xml_arg0 "interfaceAssociation") in
      make ?clientToken ?interfaceAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let interfaceAssociation =
        field_map json__ "InterfaceAssociation"
          TrunkInterfaceAssociation.of_json in
      make ?clientToken ?interfaceAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a branch network interface with a trunk network interface. Before you create the association, use CreateNetworkInterface command and set the interface type to trunk. You must also create a network interface for each branch network interface that you want to associate with the trunk network interface."]
module AssociateTrunkInterfaceRequest =
  struct
    type nonrec t =
      {
      branchInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the branch network interface."];
      trunkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the trunk network interface."];
      vlanId: Integer.t option
        [@ocaml.doc "The ID of the VLAN. This applies to the VLAN protocol."];
      greKey: Integer.t option
        [@ocaml.doc "The application key. This applies to the GRE protocol."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateTrunkInterfaceRequest"
    let make ?vlanId =
      fun ?greKey ->
        fun ?clientToken ->
          fun ?dryRun ->
            fun ~branchInterfaceId ->
              fun ~trunkInterfaceId ->
                fun () ->
                  {
                    vlanId;
                    greKey;
                    clientToken;
                    dryRun;
                    branchInterfaceId;
                    trunkInterfaceId
                  }
    let to_value x =
      structure_to_value
        [("BranchInterfaceId",
           (Some (NetworkInterfaceId.to_value x.branchInterfaceId)));
        ("TrunkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.trunkInterfaceId)));
        ("VlanId", (Option.map x.vlanId ~f:Integer.to_value));
        ("GreKey", (Option.map x.greKey ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let greKey =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "GreKey") in
      let vlanId =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "VlanId") in
      let trunkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrunkInterfaceId") in
      let branchInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BranchInterfaceId") in
      make ?dryRun ?clientToken ?greKey ?vlanId ~trunkInterfaceId
        ~branchInterfaceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let greKey = field_map json__ "GreKey" Integer.of_json in
      let vlanId = field_map json__ "VlanId" Integer.of_json in
      let trunkInterfaceId =
        field_map_exn json__ "TrunkInterfaceId" NetworkInterfaceId.of_json in
      let branchInterfaceId =
        field_map_exn json__ "BranchInterfaceId" NetworkInterfaceId.of_json in
      make ?dryRun ?clientToken ?greKey ?vlanId ~trunkInterfaceId
        ~branchInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a branch network interface with a trunk network interface. Before you create the association, use CreateNetworkInterface command and set the interface type to trunk. You must also create a network interface for each branch network interface that you want to associate with the trunk network interface."]
module AssociateTransitGatewayRouteTableResult =
  struct
    type nonrec t =
      {
      association: TransitGatewayAssociation.t option
        [@ocaml.doc "The ID of the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?association = fun () -> { association }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("association",
           (Option.map x.association ~f:TransitGatewayAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let association =
        (Option.map ~f:TransitGatewayAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      make ?association ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let association =
        field_map json__ "Association" TransitGatewayAssociation.of_json in
      make ?association ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified attachment with the specified transit gateway route table. You can associate only one route table with an attachment."]
module AssociateTransitGatewayRouteTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayRouteTableId: TransitGatewayRouteTableId.t
        [@ocaml.doc "The ID of the transit gateway route table."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateTransitGatewayRouteTableRequest"
    let make ?dryRun =
      fun ~transitGatewayRouteTableId ->
        fun ~transitGatewayAttachmentId ->
          fun () ->
            { dryRun; transitGatewayRouteTableId; transitGatewayAttachmentId
            }
    let to_value x =
      structure_to_value
        [("TransitGatewayRouteTableId",
           (Some
              (TransitGatewayRouteTableId.to_value
                 x.transitGatewayRouteTableId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayRouteTableId =
        TransitGatewayRouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayRouteTableId") in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayRouteTableId =
        field_map_exn json__ "TransitGatewayRouteTableId"
          TransitGatewayRouteTableId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayRouteTableId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified attachment with the specified transit gateway route table. You can associate only one route table with an attachment."]
module AssociateTransitGatewayPolicyTableResult =
  struct
    type nonrec t =
      {
      association: TransitGatewayPolicyTableAssociation.t option
        [@ocaml.doc
          "Describes the association of a transit gateway and a transit gateway policy table."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?association = fun () -> { association }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("association",
           (Option.map x.association
              ~f:TransitGatewayPolicyTableAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let association =
        (Option.map ~f:TransitGatewayPolicyTableAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      make ?association ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let association =
        field_map json__ "Association"
          TransitGatewayPolicyTableAssociation.of_json in
      make ?association ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified transit gateway attachment with a transit gateway policy table."]
module AssociateTransitGatewayPolicyTableRequest =
  struct
    type nonrec t =
      {
      transitGatewayPolicyTableId: TransitGatewayPolicyTableId.t
        [@ocaml.doc
          "The ID of the transit gateway policy table to associate with the transit gateway attachment."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc
          "The ID of the transit gateway attachment to associate with the policy table."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateTransitGatewayPolicyTableRequest"
    let make ?dryRun =
      fun ~transitGatewayPolicyTableId ->
        fun ~transitGatewayAttachmentId ->
          fun () ->
            { dryRun; transitGatewayPolicyTableId; transitGatewayAttachmentId
            }
    let to_value x =
      structure_to_value
        [("TransitGatewayPolicyTableId",
           (Some
              (TransitGatewayPolicyTableId.to_value
                 x.transitGatewayPolicyTableId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayPolicyTableId =
        TransitGatewayPolicyTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayPolicyTableId") in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayPolicyTableId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayPolicyTableId =
        field_map_exn json__ "TransitGatewayPolicyTableId"
          TransitGatewayPolicyTableId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ~transitGatewayPolicyTableId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified transit gateway attachment with a transit gateway policy table."]
module AssociateTransitGatewayMulticastDomainResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayMulticastDomainAssociations.t option
        [@ocaml.doc
          "Information about the transit gateway multicast domain associations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations = fun () -> { associations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayMulticastDomainAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociations.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?associations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associations =
        field_map json__ "Associations"
          TransitGatewayMulticastDomainAssociations.of_json in
      make ?associations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified subnets and transit gateway attachments with the specified transit gateway multicast domain. The transit gateway attachment must be in the available state before you can add a resource. Use DescribeTransitGatewayAttachments to see the state of the attachment."]
module AssociateTransitGatewayMulticastDomainRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId: TransitGatewayMulticastDomainId.t
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc
          "The ID of the transit gateway attachment to associate with the transit gateway multicast domain."];
      subnetIds: TransitGatewaySubnetIdList.t
        [@ocaml.doc
          "The IDs of the subnets to associate with the transit gateway multicast domain."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateTransitGatewayMulticastDomainRequest"
    let make ?dryRun =
      fun ~transitGatewayMulticastDomainId ->
        fun ~transitGatewayAttachmentId ->
          fun ~subnetIds ->
            fun () ->
              {
                dryRun;
                transitGatewayMulticastDomainId;
                transitGatewayAttachmentId;
                subnetIds
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Some
              (TransitGatewayMulticastDomainId.to_value
                 x.transitGatewayMulticastDomainId)));
        ("TransitGatewayAttachmentId",
          (Some
             (TransitGatewayAttachmentId.to_value
                x.transitGatewayAttachmentId)));
        ("SubnetIds",
          (Some (TransitGatewaySubnetIdList.to_value x.subnetIds)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetIds =
        TransitGatewaySubnetIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubnetIds") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      let transitGatewayMulticastDomainId =
        TransitGatewayMulticastDomainId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayMulticastDomainId") in
      make ?dryRun ~subnetIds ~transitGatewayAttachmentId
        ~transitGatewayMulticastDomainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let subnetIds =
        field_map_exn json__ "SubnetIds" TransitGatewaySubnetIdList.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayMulticastDomainId =
        field_map_exn json__ "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ~subnetIds ~transitGatewayAttachmentId
        ~transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified subnets and transit gateway attachments with the specified transit gateway multicast domain. The transit gateway attachment must be in the available state before you can add a resource. Use DescribeTransitGatewayAttachments to see the state of the attachment."]
module AssociateSubnetCidrBlockResult =
  struct
    type nonrec t =
      {
      ipv6CidrBlockAssociation: SubnetIpv6CidrBlockAssociation.t option
        [@ocaml.doc "Information about the IPv6 association."];
      subnetId: String_.t option [@ocaml.doc "The ID of the subnet."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipv6CidrBlockAssociation =
      fun ?subnetId -> fun () -> { ipv6CidrBlockAssociation; subnetId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipv6CidrBlockAssociation",
           (Option.map x.ipv6CidrBlockAssociation
              ~f:SubnetIpv6CidrBlockAssociation.to_value));
        ("subnetId", (Option.map x.subnetId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnetId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "subnetId") in
      let ipv6CidrBlockAssociation =
        (Option.map ~f:SubnetIpv6CidrBlockAssociation.of_xml)
          (Xml.child xml_arg0 "ipv6CidrBlockAssociation") in
      make ?subnetId ?ipv6CidrBlockAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnetId = field_map json__ "SubnetId" String_.of_json in
      let ipv6CidrBlockAssociation =
        field_map json__ "Ipv6CidrBlockAssociation"
          SubnetIpv6CidrBlockAssociation.of_json in
      make ?subnetId ?ipv6CidrBlockAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a CIDR block with your subnet. You can only associate a single IPv6 CIDR block with your subnet."]
module AssociateSubnetCidrBlockRequest =
  struct
    type nonrec t =
      {
      ipv6IpamPoolId: IpamPoolId.t option
        [@ocaml.doc "An IPv6 IPAM pool ID."];
      ipv6NetmaskLength: NetmaskLength.t option
        [@ocaml.doc "An IPv6 netmask length."];
      subnetId: SubnetId.t [@ocaml.doc "The ID of your subnet."];
      ipv6CidrBlock: String_.t option
        [@ocaml.doc "The IPv6 CIDR block for your subnet."]}
    let context_ = "AssociateSubnetCidrBlockRequest"
    let make ?ipv6IpamPoolId =
      fun ?ipv6NetmaskLength ->
        fun ?ipv6CidrBlock ->
          fun ~subnetId ->
            fun () ->
              { ipv6IpamPoolId; ipv6NetmaskLength; ipv6CidrBlock; subnetId }
    let to_value x =
      structure_to_value
        [("Ipv6IpamPoolId",
           (Option.map x.ipv6IpamPoolId ~f:IpamPoolId.to_value));
        ("Ipv6NetmaskLength",
          (Option.map x.ipv6NetmaskLength ~f:NetmaskLength.to_value));
        ("subnetId", (Some (SubnetId.to_value x.subnetId)));
        ("ipv6CidrBlock", (Option.map x.ipv6CidrBlock ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv6CidrBlock =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipv6CidrBlock") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subnetId") in
      let ipv6NetmaskLength =
        (Option.map ~f:NetmaskLength.of_xml)
          (Xml.child xml_arg0 "Ipv6NetmaskLength") in
      let ipv6IpamPoolId =
        (Option.map ~f:IpamPoolId.of_xml)
          (Xml.child xml_arg0 "Ipv6IpamPoolId") in
      make ?ipv6CidrBlock ~subnetId ?ipv6NetmaskLength ?ipv6IpamPoolId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipv6CidrBlock = field_map json__ "Ipv6CidrBlock" String_.of_json in
      let subnetId = field_map_exn json__ "SubnetId" SubnetId.of_json in
      let ipv6NetmaskLength =
        field_map json__ "Ipv6NetmaskLength" NetmaskLength.of_json in
      let ipv6IpamPoolId =
        field_map json__ "Ipv6IpamPoolId" IpamPoolId.of_json in
      make ?ipv6CidrBlock ~subnetId ?ipv6NetmaskLength ?ipv6IpamPoolId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a CIDR block with your subnet. You can only associate a single IPv6 CIDR block with your subnet."]
module AssociateSecurityGroupVpcResult =
  struct
    type nonrec t =
      {
      state: SecurityGroupVpcAssociationState.t option
        [@ocaml.doc "The state of the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?state = fun () -> { state }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("state",
           (Option.map x.state ~f:SecurityGroupVpcAssociationState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        (Option.map ~f:SecurityGroupVpcAssociationState.of_xml)
          (Xml.child xml_arg0 "state") in
      make ?state ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let state =
        field_map json__ "State" SecurityGroupVpcAssociationState.of_json in
      make ?state ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a security group with another VPC in the same Region. This enables you to use the same security group with network interfaces and instances in the specified VPC. The VPC you want to associate the security group with must be in the same Region. You can associate the security group with another VPC if your account owns the VPC or if the VPC was shared with you. You must own the security group. You cannot use this feature with default security groups. You cannot use this feature with the default VPC."]
module AssociateSecurityGroupVpcRequest =
  struct
    type nonrec t =
      {
      groupId: SecurityGroupId.t [@ocaml.doc "A security group ID."];
      vpcId: VpcId.t [@ocaml.doc "A VPC ID."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateSecurityGroupVpcRequest"
    let make ?dryRun =
      fun ~groupId -> fun ~vpcId -> fun () -> { dryRun; groupId; vpcId }
    let to_value x =
      structure_to_value
        [("GroupId", (Some (SecurityGroupId.to_value x.groupId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let groupId =
        SecurityGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupId") in
      make ?dryRun ~vpcId ~groupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let groupId = field_map_exn json__ "GroupId" SecurityGroupId.of_json in
      make ?dryRun ~vpcId ~groupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a security group with another VPC in the same Region. This enables you to use the same security group with network interfaces and instances in the specified VPC. The VPC you want to associate the security group with must be in the same Region. You can associate the security group with another VPC if your account owns the VPC or if the VPC was shared with you. You must own the security group. You cannot use this feature with default security groups. You cannot use this feature with the default VPC."]
module AssociateRouteTableResult =
  struct
    type nonrec t =
      {
      associationId: String_.t option
        [@ocaml.doc
          "The route table association ID. This ID is required for disassociating the route table."];
      associationState: RouteTableAssociationState.t option
        [@ocaml.doc "The state of the association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId =
      fun ?associationState -> fun () -> { associationId; associationState }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value));
        ("associationState",
          (Option.map x.associationState
             ~f:RouteTableAssociationState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationState =
        (Option.map ~f:RouteTableAssociationState.of_xml)
          (Xml.child xml_arg0 "associationState") in
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?associationState ?associationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associationState =
        field_map json__ "AssociationState"
          RouteTableAssociationState.of_json in
      let associationId = field_map json__ "AssociationId" String_.of_json in
      make ?associationState ?associationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a subnet in your VPC or an internet gateway or virtual private gateway attached to your VPC with a route table in your VPC. This association causes traffic from the subnet or gateway to be routed according to the routes in the route table. The action returns an association ID, which you need in order to disassociate the route table later. A route table can be associated with multiple subnets. For more information, see Route tables in the Amazon VPC User Guide."]
module AssociateRouteTableRequest =
  struct
    type nonrec t =
      {
      gatewayId: RouteGatewayId.t option
        [@ocaml.doc
          "The ID of the internet gateway or virtual private gateway."];
      publicIpv4Pool: Ipv4PoolEc2Id.t option
        [@ocaml.doc
          "The ID of a public IPv4 pool. A public IPv4 pool is a pool of IPv4 addresses that you've brought to Amazon Web Services with BYOIP."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      subnetId: SubnetId.t option [@ocaml.doc "The ID of the subnet."];
      routeTableId: RouteTableId.t [@ocaml.doc "The ID of the route table."]}
    let context_ = "AssociateRouteTableRequest"
    let make ?gatewayId =
      fun ?publicIpv4Pool ->
        fun ?dryRun ->
          fun ?subnetId ->
            fun ~routeTableId ->
              fun () ->
                { gatewayId; publicIpv4Pool; dryRun; subnetId; routeTableId }
    let to_value x =
      structure_to_value
        [("GatewayId", (Option.map x.gatewayId ~f:RouteGatewayId.to_value));
        ("PublicIpv4Pool",
          (Option.map x.publicIpv4Pool ~f:Ipv4PoolEc2Id.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("subnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("routeTableId", (Some (RouteTableId.to_value x.routeTableId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeTableId =
        RouteTableId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "routeTableId") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "subnetId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let publicIpv4Pool =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml)
          (Xml.child xml_arg0 "PublicIpv4Pool") in
      let gatewayId =
        (Option.map ~f:RouteGatewayId.of_xml)
          (Xml.child xml_arg0 "GatewayId") in
      make ~routeTableId ?subnetId ?dryRun ?publicIpv4Pool ?gatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeTableId =
        field_map_exn json__ "RouteTableId" RouteTableId.of_json in
      let subnetId = field_map json__ "SubnetId" SubnetId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let publicIpv4Pool =
        field_map json__ "PublicIpv4Pool" Ipv4PoolEc2Id.of_json in
      let gatewayId = field_map json__ "GatewayId" RouteGatewayId.of_json in
      make ~routeTableId ?subnetId ?dryRun ?publicIpv4Pool ?gatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a subnet in your VPC or an internet gateway or virtual private gateway attached to your VPC with a route table in your VPC. This association causes traffic from the subnet or gateway to be routed according to the routes in the route table. The action returns an association ID, which you need in order to disassociate the route table later. A route table can be associated with multiple subnets. For more information, see Route tables in the Amazon VPC User Guide."]
module AssociateRouteServerResult =
  struct
    type nonrec t =
      {
      routeServerAssociation: RouteServerAssociation.t option
        [@ocaml.doc
          "Information about the association between the route server and the VPC."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?routeServerAssociation = fun () -> { routeServerAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("routeServerAssociation",
           (Option.map x.routeServerAssociation
              ~f:RouteServerAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routeServerAssociation =
        (Option.map ~f:RouteServerAssociation.of_xml)
          (Xml.child xml_arg0 "routeServerAssociation") in
      make ?routeServerAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routeServerAssociation =
        field_map json__ "RouteServerAssociation"
          RouteServerAssociation.of_json in
      make ?routeServerAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a route server with a VPC to enable dynamic route updates. A route server association is the connection established between a route server and a VPC. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module AssociateRouteServerRequest =
  struct
    type nonrec t =
      {
      routeServerId: RouteServerId.t
        [@ocaml.doc
          "The unique identifier for the route server to be associated."];
      vpcId: VpcId.t
        [@ocaml.doc "The ID of the VPC to associate with the route server."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateRouteServerRequest"
    let make ?dryRun =
      fun ~routeServerId ->
        fun ~vpcId -> fun () -> { dryRun; routeServerId; vpcId }
    let to_value x =
      structure_to_value
        [("RouteServerId", (Some (RouteServerId.to_value x.routeServerId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let routeServerId =
        RouteServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RouteServerId") in
      make ?dryRun ~vpcId ~routeServerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let routeServerId =
        field_map_exn json__ "RouteServerId" RouteServerId.of_json in
      make ?dryRun ~vpcId ~routeServerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a route server with a VPC to enable dynamic route updates. A route server association is the connection established between a route server and a VPC. For more information see Dynamic routing in your VPC with VPC Route Server in the Amazon VPC User Guide."]
module AssociateNatGatewayAddressResult =
  struct
    type nonrec t =
      {
      natGatewayId: NatGatewayId.t option
        [@ocaml.doc "The ID of the NAT gateway."];
      natGatewayAddresses: NatGatewayAddressList.t option
        [@ocaml.doc "The IP addresses."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?natGatewayId =
      fun ?natGatewayAddresses ->
        fun () -> { natGatewayId; natGatewayAddresses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("natGatewayId",
           (Option.map x.natGatewayId ~f:NatGatewayId.to_value));
        ("natGatewayAddressSet",
          (Option.map x.natGatewayAddresses ~f:NatGatewayAddressList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayAddresses =
        (Option.map ~f:NatGatewayAddressList.of_xml)
          (Xml.child xml_arg0 "natGatewayAddressSet") in
      let natGatewayId =
        (Option.map ~f:NatGatewayId.of_xml)
          (Xml.child xml_arg0 "natGatewayId") in
      make ?natGatewayAddresses ?natGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGatewayAddresses =
        field_map json__ "NatGatewayAddresses" NatGatewayAddressList.of_json in
      let natGatewayId = field_map json__ "NatGatewayId" NatGatewayId.of_json in
      make ?natGatewayAddresses ?natGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates Elastic IP addresses (EIPs) and private IPv4 addresses with a public NAT gateway. For more information, see Work with NAT gateways in the Amazon VPC User Guide. By default, you can associate up to 2 Elastic IP addresses per public NAT gateway. You can increase the limit by requesting a quota adjustment. For more information, see Elastic IP address quotas in the Amazon VPC User Guide. When you associate an EIP or secondary EIPs with a public NAT gateway, the network border group of the EIPs must match the network border group of the Availability Zone (AZ) that the public NAT gateway is in. If it's not the same, the EIP will fail to associate. You can see the network border group for the subnet's AZ by viewing the details of the subnet. Similarly, you can view the network border group of an EIP by viewing the details of the EIP address. For more information about network border groups and EIPs, see Allocate an Elastic IP address in the Amazon VPC User Guide."]
module AssociateNatGatewayAddressRequest =
  struct
    type nonrec t =
      {
      natGatewayId: NatGatewayId.t [@ocaml.doc "The ID of the NAT gateway."];
      allocationIds: AllocationIdList.t
        [@ocaml.doc
          "The allocation IDs of EIPs that you want to associate with your NAT gateway."];
      privateIpAddresses: IpList.t option
        [@ocaml.doc
          "The private IPv4 addresses that you want to assign to the NAT gateway."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "For regional NAT gateways only: The Availability Zone where you want to associate an Elastic IP address (EIP). The regional NAT gateway uses a separate EIP in each AZ to handle outbound NAT traffic from that AZ. A regional NAT gateway is a single NAT Gateway that works across multiple availability zones (AZs) in your VPC, providing redundancy, scalability and availability across all the AZs in a Region."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "For regional NAT gateways only: The ID of the Availability Zone where you want to associate an Elastic IP address (EIP). The regional NAT gateway uses a separate EIP in each AZ to handle outbound NAT traffic from that AZ. Use this instead of AvailabilityZone for consistent identification of AZs across Amazon Web Services Regions. A regional NAT gateway is a single NAT Gateway that works across multiple availability zones (AZs) in your VPC, providing redundancy, scalability and availability across all the AZs in a Region."]}
    let context_ = "AssociateNatGatewayAddressRequest"
    let make ?privateIpAddresses =
      fun ?dryRun ->
        fun ?availabilityZone ->
          fun ?availabilityZoneId ->
            fun ~natGatewayId ->
              fun ~allocationIds ->
                fun () ->
                  {
                    privateIpAddresses;
                    dryRun;
                    availabilityZone;
                    availabilityZoneId;
                    natGatewayId;
                    allocationIds
                  }
    let to_value x =
      structure_to_value
        [("NatGatewayId", (Some (NatGatewayId.to_value x.natGatewayId)));
        ("AllocationId", (Some (AllocationIdList.to_value x.allocationIds)));
        ("PrivateIpAddress",
          (Option.map x.privateIpAddresses ~f:IpList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let privateIpAddresses =
        (Option.map ~f:IpList.of_xml) (Xml.child xml_arg0 "PrivateIpAddress") in
      let allocationIds =
        AllocationIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AllocationId") in
      let natGatewayId =
        NatGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NatGatewayId") in
      make ?availabilityZoneId ?availabilityZone ?dryRun ?privateIpAddresses
        ~allocationIds ~natGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let privateIpAddresses =
        field_map json__ "PrivateIpAddresses" IpList.of_json in
      let allocationIds =
        field_map_exn json__ "AllocationIds" AllocationIdList.of_json in
      let natGatewayId =
        field_map_exn json__ "NatGatewayId" NatGatewayId.of_json in
      make ?availabilityZoneId ?availabilityZone ?dryRun ?privateIpAddresses
        ~allocationIds ~natGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates Elastic IP addresses (EIPs) and private IPv4 addresses with a public NAT gateway. For more information, see Work with NAT gateways in the Amazon VPC User Guide. By default, you can associate up to 2 Elastic IP addresses per public NAT gateway. You can increase the limit by requesting a quota adjustment. For more information, see Elastic IP address quotas in the Amazon VPC User Guide. When you associate an EIP or secondary EIPs with a public NAT gateway, the network border group of the EIPs must match the network border group of the Availability Zone (AZ) that the public NAT gateway is in. If it's not the same, the EIP will fail to associate. You can see the network border group for the subnet's AZ by viewing the details of the subnet. Similarly, you can view the network border group of an EIP by viewing the details of the EIP address. For more information about network border groups and EIPs, see Allocate an Elastic IP address in the Amazon VPC User Guide."]
module AssociateIpamResourceDiscoveryResult =
  struct
    type nonrec t =
      {
      ipamResourceDiscoveryAssociation:
        IpamResourceDiscoveryAssociation.t option
        [@ocaml.doc
          "A resource discovery association. An associated resource discovery is a resource discovery that has been associated with an IPAM."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamResourceDiscoveryAssociation =
      fun () -> { ipamResourceDiscoveryAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamResourceDiscoveryAssociation",
           (Option.map x.ipamResourceDiscoveryAssociation
              ~f:IpamResourceDiscoveryAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamResourceDiscoveryAssociation =
        (Option.map ~f:IpamResourceDiscoveryAssociation.of_xml)
          (Xml.child xml_arg0 "ipamResourceDiscoveryAssociation") in
      make ?ipamResourceDiscoveryAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamResourceDiscoveryAssociation =
        field_map json__ "IpamResourceDiscoveryAssociation"
          IpamResourceDiscoveryAssociation.of_json in
      make ?ipamResourceDiscoveryAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an IPAM resource discovery with an Amazon VPC IPAM. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module AssociateIpamResourceDiscoveryRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamId: IpamId.t [@ocaml.doc "An IPAM ID."];
      ipamResourceDiscoveryId: IpamResourceDiscoveryId.t
        [@ocaml.doc "A resource discovery ID."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "Tag specifications."];
      clientToken: String_.t option [@ocaml.doc "A client token."]}
    let context_ = "AssociateIpamResourceDiscoveryRequest"
    let make ?dryRun =
      fun ?tagSpecifications ->
        fun ?clientToken ->
          fun ~ipamId ->
            fun ~ipamResourceDiscoveryId ->
              fun () ->
                {
                  dryRun;
                  tagSpecifications;
                  clientToken;
                  ipamId;
                  ipamResourceDiscoveryId
                }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamId", (Some (IpamId.to_value x.ipamId)));
        ("IpamResourceDiscoveryId",
          (Some (IpamResourceDiscoveryId.to_value x.ipamResourceDiscoveryId)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let ipamResourceDiscoveryId =
        IpamResourceDiscoveryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamResourceDiscoveryId") in
      let ipamId =
        IpamId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpamId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?clientToken ?tagSpecifications ~ipamResourceDiscoveryId ~ipamId
        ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let ipamResourceDiscoveryId =
        field_map_exn json__ "IpamResourceDiscoveryId"
          IpamResourceDiscoveryId.of_json in
      let ipamId = field_map_exn json__ "IpamId" IpamId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?clientToken ?tagSpecifications ~ipamResourceDiscoveryId ~ipamId
        ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an IPAM resource discovery with an Amazon VPC IPAM. A resource discovery is an IPAM component that enables IPAM to manage and monitor resources that belong to the owning account."]
module AssociateIpamByoasnResult =
  struct
    type nonrec t =
      {
      asnAssociation: AsnAssociation.t option
        [@ocaml.doc "The ASN and BYOIP CIDR association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?asnAssociation = fun () -> { asnAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("asnAssociation",
           (Option.map x.asnAssociation ~f:AsnAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let asnAssociation =
        (Option.map ~f:AsnAssociation.of_xml)
          (Xml.child xml_arg0 "asnAssociation") in
      make ?asnAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let asnAssociation =
        field_map json__ "AsnAssociation" AsnAssociation.of_json in
      make ?asnAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates your Autonomous System Number (ASN) with a BYOIP CIDR that you own in the same Amazon Web Services Region. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide. After the association succeeds, the ASN is eligible for advertisement. You can view the association with DescribeByoipCidrs. You can advertise the CIDR with AdvertiseByoipCidr."]
module AssociateIpamByoasnRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      asn: String_.t [@ocaml.doc "A public 2-byte or 4-byte ASN."];
      cidr: String_.t
        [@ocaml.doc "The BYOIP CIDR you want to associate with an ASN."]}
    let context_ = "AssociateIpamByoasnRequest"
    let make ?dryRun =
      fun ~asn -> fun ~cidr -> fun () -> { dryRun; asn; cidr }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("Asn", (Some (String_.to_value x.asn)));
        ("Cidr", (Some (String_.to_value x.cidr)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      let asn =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Asn") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~cidr ~asn ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      let asn = field_map_exn json__ "Asn" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~cidr ~asn ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates your Autonomous System Number (ASN) with a BYOIP CIDR that you own in the same Amazon Web Services Region. For more information, see Tutorial: Bring your ASN to IPAM in the Amazon VPC IPAM guide. After the association succeeds, the ASN is eligible for advertisement. You can view the association with DescribeByoipCidrs. You can advertise the CIDR with AdvertiseByoipCidr."]
module AssociateInstanceEventWindowResult =
  struct
    type nonrec t =
      {
      instanceEventWindow: InstanceEventWindow.t option
        [@ocaml.doc "Information about the event window."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?instanceEventWindow = fun () -> { instanceEventWindow }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceEventWindow",
           (Option.map x.instanceEventWindow ~f:InstanceEventWindow.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceEventWindow =
        (Option.map ~f:InstanceEventWindow.of_xml)
          (Xml.child xml_arg0 "instanceEventWindow") in
      make ?instanceEventWindow ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceEventWindow =
        field_map json__ "InstanceEventWindow" InstanceEventWindow.of_json in
      make ?instanceEventWindow ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates one or more targets with an event window. Only one type of target (instance IDs, Dedicated Host IDs, or tags) can be specified with an event window. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module AssociateInstanceEventWindowRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      instanceEventWindowId: InstanceEventWindowId.t
        [@ocaml.doc "The ID of the event window."];
      associationTarget: InstanceEventWindowAssociationRequest.t
        [@ocaml.doc
          "One or more targets associated with the specified event window."]}
    let context_ = "AssociateInstanceEventWindowRequest"
    let make ?dryRun =
      fun ~instanceEventWindowId ->
        fun ~associationTarget ->
          fun () -> { dryRun; instanceEventWindowId; associationTarget }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("InstanceEventWindowId",
          (Some (InstanceEventWindowId.to_value x.instanceEventWindowId)));
        ("AssociationTarget",
          (Some
             (InstanceEventWindowAssociationRequest.to_value
                x.associationTarget)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationTarget =
        InstanceEventWindowAssociationRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AssociationTarget") in
      let instanceEventWindowId =
        InstanceEventWindowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceEventWindowId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~associationTarget ~instanceEventWindowId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associationTarget =
        field_map_exn json__ "AssociationTarget"
          InstanceEventWindowAssociationRequest.of_json in
      let instanceEventWindowId =
        field_map_exn json__ "InstanceEventWindowId"
          InstanceEventWindowId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~associationTarget ~instanceEventWindowId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates one or more targets with an event window. Only one type of target (instance IDs, Dedicated Host IDs, or tags) can be specified with an event window. For more information, see Define event windows for scheduled events in the Amazon EC2 User Guide."]
module AssociateIamInstanceProfileResult =
  struct
    type nonrec t =
      {
      iamInstanceProfileAssociation: IamInstanceProfileAssociation.t option
        [@ocaml.doc
          "Information about the IAM instance profile association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?iamInstanceProfileAssociation =
      fun () -> { iamInstanceProfileAssociation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("iamInstanceProfileAssociation",
           (Option.map x.iamInstanceProfileAssociation
              ~f:IamInstanceProfileAssociation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let iamInstanceProfileAssociation =
        (Option.map ~f:IamInstanceProfileAssociation.of_xml)
          (Xml.child xml_arg0 "iamInstanceProfileAssociation") in
      make ?iamInstanceProfileAssociation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let iamInstanceProfileAssociation =
        field_map json__ "IamInstanceProfileAssociation"
          IamInstanceProfileAssociation.of_json in
      make ?iamInstanceProfileAssociation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an IAM instance profile with a running or stopped instance. You cannot associate more than one IAM instance profile with an instance."]
module AssociateIamInstanceProfileRequest =
  struct
    type nonrec t =
      {
      iamInstanceProfile: IamInstanceProfileSpecification.t
        [@ocaml.doc "The IAM instance profile."];
      instanceId: InstanceId.t [@ocaml.doc "The ID of the instance."]}
    let context_ = "AssociateIamInstanceProfileRequest"
    let make ~iamInstanceProfile =
      fun ~instanceId -> fun () -> { iamInstanceProfile; instanceId }
    let to_value x =
      structure_to_value
        [("IamInstanceProfile",
           (Some
              (IamInstanceProfileSpecification.to_value x.iamInstanceProfile)));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let iamInstanceProfile =
        IamInstanceProfileSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamInstanceProfile") in
      make ~instanceId ~iamInstanceProfile ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let iamInstanceProfile =
        field_map_exn json__ "IamInstanceProfile"
          IamInstanceProfileSpecification.of_json in
      make ~instanceId ~iamInstanceProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an IAM instance profile with a running or stopped instance. You cannot associate more than one IAM instance profile with an instance."]
module AssociateEnclaveCertificateIamRoleResult =
  struct
    type nonrec t =
      {
      certificateS3BucketName: String_.t option
        [@ocaml.doc
          "The name of the Amazon S3 bucket to which the certificate was uploaded."];
      certificateS3ObjectKey: String_.t option
        [@ocaml.doc
          "The Amazon S3 object key where the certificate, certificate chain, and encrypted private key bundle are stored. The object key is formatted as follows: role_arn/certificate_arn."];
      encryptionKmsKeyId: String_.t option
        [@ocaml.doc
          "The ID of the KMS key used to encrypt the private key of the certificate."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?certificateS3BucketName =
      fun ?certificateS3ObjectKey ->
        fun ?encryptionKmsKeyId ->
          fun () ->
            {
              certificateS3BucketName;
              certificateS3ObjectKey;
              encryptionKmsKeyId
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("certificateS3BucketName",
           (Option.map x.certificateS3BucketName ~f:String_.to_value));
        ("certificateS3ObjectKey",
          (Option.map x.certificateS3ObjectKey ~f:String_.to_value));
        ("encryptionKmsKeyId",
          (Option.map x.encryptionKmsKeyId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKmsKeyId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "encryptionKmsKeyId") in
      let certificateS3ObjectKey =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateS3ObjectKey") in
      let certificateS3BucketName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateS3BucketName") in
      make ?encryptionKmsKeyId ?certificateS3ObjectKey
        ?certificateS3BucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionKmsKeyId =
        field_map json__ "EncryptionKmsKeyId" String_.of_json in
      let certificateS3ObjectKey =
        field_map json__ "CertificateS3ObjectKey" String_.of_json in
      let certificateS3BucketName =
        field_map json__ "CertificateS3BucketName" String_.of_json in
      make ?encryptionKmsKeyId ?certificateS3ObjectKey
        ?certificateS3BucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an Identity and Access Management (IAM) role with an Certificate Manager (ACM) certificate. This enables the certificate to be used by the ACM for Nitro Enclaves application inside an enclave. For more information, see Certificate Manager for Nitro Enclaves in the Amazon Web Services Nitro Enclaves User Guide. When the IAM role is associated with the ACM certificate, the certificate, certificate chain, and encrypted private key are placed in an Amazon S3 location that only the associated IAM role can access. The private key of the certificate is encrypted with an Amazon Web Services managed key that has an attached attestation-based key policy. To enable the IAM role to access the Amazon S3 object, you must grant it permission to call s3:GetObject on the Amazon S3 bucket returned by the command. To enable the IAM role to access the KMS key, you must grant it permission to call kms:Decrypt on the KMS key returned by the command. For more information, see Grant the role permission to access the certificate and encryption key in the Amazon Web Services Nitro Enclaves User Guide."]
module AssociateEnclaveCertificateIamRoleRequest =
  struct
    type nonrec t =
      {
      certificateArn: CertificateId.t
        [@ocaml.doc
          "The ARN of the ACM certificate with which to associate the IAM role."];
      roleArn: RoleId.t
        [@ocaml.doc
          "The ARN of the IAM role to associate with the ACM certificate. You can associate up to 16 IAM roles with an ACM certificate."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateEnclaveCertificateIamRoleRequest"
    let make ?dryRun =
      fun ~certificateArn ->
        fun ~roleArn -> fun () -> { dryRun; certificateArn; roleArn }
    let to_value x =
      structure_to_value
        [("CertificateArn", (Some (CertificateId.to_value x.certificateArn)));
        ("RoleArn", (Some (RoleId.to_value x.roleArn)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let roleArn =
        RoleId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let certificateArn =
        CertificateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateArn") in
      make ?dryRun ~roleArn ~certificateArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let roleArn = field_map_exn json__ "RoleArn" RoleId.of_json in
      let certificateArn =
        field_map_exn json__ "CertificateArn" CertificateId.of_json in
      make ?dryRun ~roleArn ~certificateArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an Identity and Access Management (IAM) role with an Certificate Manager (ACM) certificate. This enables the certificate to be used by the ACM for Nitro Enclaves application inside an enclave. For more information, see Certificate Manager for Nitro Enclaves in the Amazon Web Services Nitro Enclaves User Guide. When the IAM role is associated with the ACM certificate, the certificate, certificate chain, and encrypted private key are placed in an Amazon S3 location that only the associated IAM role can access. The private key of the certificate is encrypted with an Amazon Web Services managed key that has an attached attestation-based key policy. To enable the IAM role to access the Amazon S3 object, you must grant it permission to call s3:GetObject on the Amazon S3 bucket returned by the command. To enable the IAM role to access the KMS key, you must grant it permission to call kms:Decrypt on the KMS key returned by the command. For more information, see Grant the role permission to access the certificate and encryption key in the Amazon Web Services Nitro Enclaves User Guide."]
module AssociateDhcpOptionsRequest =
  struct
    type nonrec t =
      {
      dhcpOptionsId: DefaultingDhcpOptionsId.t
        [@ocaml.doc
          "The ID of the DHCP options set, or default to associate no DHCP options with the VPC."];
      vpcId: VpcId.t [@ocaml.doc "The ID of the VPC."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssociateDhcpOptionsRequest"
    let make ?dryRun =
      fun ~dhcpOptionsId ->
        fun ~vpcId -> fun () -> { dryRun; dhcpOptionsId; vpcId }
    let to_value x =
      structure_to_value
        [("DhcpOptionsId",
           (Some (DefaultingDhcpOptionsId.to_value x.dhcpOptionsId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let dhcpOptionsId =
        DefaultingDhcpOptionsId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DhcpOptionsId") in
      make ?dryRun ~vpcId ~dhcpOptionsId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let dhcpOptionsId =
        field_map_exn json__ "DhcpOptionsId" DefaultingDhcpOptionsId.of_json in
      make ?dryRun ~vpcId ~dhcpOptionsId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a set of DHCP options (that you've previously created) with the specified VPC, or associates no DHCP options with the VPC. After you associate the options with the VPC, any existing instances and all new instances that you launch in that VPC use the options. You don't need to restart or relaunch the instances. They automatically pick up the changes within a few hours, depending on how frequently the instance renews its DHCP lease. You can explicitly renew the lease using the operating system on the instance. For more information, see DHCP option sets in the Amazon VPC User Guide."]
module AssociateClientVpnTargetNetworkResult =
  struct
    type nonrec t =
      {
      associationId: String_.t option
        [@ocaml.doc "The unique ID of the target network association."];
      status: AssociationStatus.t option
        [@ocaml.doc "The current state of the target network association."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId =
      fun ?status -> fun () -> { associationId; status }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:AssociationStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AssociationStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?status ?associationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" AssociationStatus.of_json in
      let associationId = field_map json__ "AssociationId" String_.of_json in
      make ?status ?associationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a target network with a Client VPN endpoint. A target network is a subnet in a VPC. You can associate multiple subnets from the same VPC with a Client VPN endpoint. You can associate only one subnet in each Availability Zone. We recommend that you associate at least two subnets to provide Availability Zone redundancy. If you specified a VPC when you created the Client VPN endpoint or if you have previous subnet associations, the specified subnet must be in the same VPC. To specify a subnet that's in a different VPC, you must first modify the Client VPN endpoint (ModifyClientVpnEndpoint) and change the VPC that's associated with it."]
module AssociateClientVpnTargetNetworkRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      subnetId: SubnetId.t option
        [@ocaml.doc
          "The ID of the subnet to associate with the Client VPN endpoint. Required for VPC-based endpoints. For Transit Gateway-based endpoints, use AvailabilityZone or AvailabilityZoneId instead."];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "The Availability Zone name for the Transit Gateway association. Required if when associating an Availability Zone with a Client VPN endpoint that uses a Transit Gateway. You cannot specify both SubnetId and AvailabilityZone."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc
          "The Availability Zone ID for the Transit Gateway association. Required if when associating an Availability Zone with a Client VPN endpoint that uses a Transit Gateway. You cannot specify both AvailabilityZone and AvailabilityZoneId."]}
    let context_ = "AssociateClientVpnTargetNetworkRequest"
    let make ?subnetId =
      fun ?clientToken ->
        fun ?dryRun ->
          fun ?availabilityZone ->
            fun ?availabilityZoneId ->
              fun ~clientVpnEndpointId ->
                fun () ->
                  {
                    subnetId;
                    clientToken;
                    dryRun;
                    availabilityZone;
                    availabilityZoneId;
                    clientVpnEndpointId
                  }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("SubnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("AvailabilityZone",
          (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "SubnetId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?availabilityZoneId ?availabilityZone ?dryRun ?clientToken
        ?subnetId ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let subnetId = field_map json__ "SubnetId" SubnetId.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?availabilityZoneId ?availabilityZone ?dryRun ?clientToken
        ?subnetId ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a target network with a Client VPN endpoint. A target network is a subnet in a VPC. You can associate multiple subnets from the same VPC with a Client VPN endpoint. You can associate only one subnet in each Availability Zone. We recommend that you associate at least two subnets to provide Availability Zone redundancy. If you specified a VPC when you created the Client VPN endpoint or if you have previous subnet associations, the specified subnet must be in the same VPC. To specify a subnet that's in a different VPC, you must first modify the Client VPN endpoint (ModifyClientVpnEndpoint) and change the VPC that's associated with it."]
module AssociateCapacityReservationBillingOwnerResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initiates a request to assign billing of the unused capacity of a shared Capacity Reservation to a consumer account that is consolidated under the same Amazon Web Services organizations payer account. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations."]
module AssociateCapacityReservationBillingOwnerRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc "The ID of the Capacity Reservation."];
      unusedReservationBillingOwnerId: AccountID.t
        [@ocaml.doc
          "The ID of the consumer account to which to assign billing."]}
    let context_ = "AssociateCapacityReservationBillingOwnerRequest"
    let make ?dryRun =
      fun ~capacityReservationId ->
        fun ~unusedReservationBillingOwnerId ->
          fun () ->
            { dryRun; capacityReservationId; unusedReservationBillingOwnerId
            }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CapacityReservationId",
          (Some (CapacityReservationId.to_value x.capacityReservationId)));
        ("UnusedReservationBillingOwnerId",
          (Some (AccountID.to_value x.unusedReservationBillingOwnerId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unusedReservationBillingOwnerId =
        AccountID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "UnusedReservationBillingOwnerId") in
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~unusedReservationBillingOwnerId ~capacityReservationId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unusedReservationBillingOwnerId =
        field_map_exn json__ "UnusedReservationBillingOwnerId"
          AccountID.of_json in
      let capacityReservationId =
        field_map_exn json__ "CapacityReservationId"
          CapacityReservationId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~unusedReservationBillingOwnerId ~capacityReservationId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initiates a request to assign billing of the unused capacity of a shared Capacity Reservation to a consumer account that is consolidated under the same Amazon Web Services organizations payer account. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations."]
module AssociateAddressResult =
  struct
    type nonrec t =
      {
      associationId: String_.t option
        [@ocaml.doc
          "The ID that represents the association of the Elastic IP address with an instance."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associationId = fun () -> { associationId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associationId", (Option.map x.associationId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "associationId") in
      make ?associationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associationId = field_map json__ "AssociationId" String_.of_json in
      make ?associationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an Elastic IP address, or carrier IP address (for instances that are in subnets in Wavelength Zones) with an instance or a network interface. Before you can use an Elastic IP address, you must allocate it to your account. If the Elastic IP address is already associated with a different instance, it is disassociated from that instance and associated with the specified instance. If you associate an Elastic IP address with an instance that has an existing Elastic IP address, the existing address is disassociated from the instance, but remains allocated to your account. \\[Subnets in Wavelength Zones\\] You can associate an IP address from the telecommunication carrier to the instance or network interface. You cannot associate an Elastic IP address with an interface in a different network border group. This is an idempotent operation. If you perform the operation more than once, Amazon EC2 doesn't return an error, and you may be charged for each time the Elastic IP address is remapped to the same instance. For more information, see the Elastic IP Addresses section of Amazon EC2 Pricing."]
module AssociateAddressRequest =
  struct
    type nonrec t =
      {
      allocationId: AllocationId.t option
        [@ocaml.doc "The allocation ID. This is required."];
      instanceId: InstanceId.t option
        [@ocaml.doc
          "The ID of the instance. The instance must have exactly one attached network interface. You can specify either the instance ID or the network interface ID, but not both."];
      publicIp: EipAllocationPublicIp.t option [@ocaml.doc "Deprecated."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkInterfaceId: NetworkInterfaceId.t option
        [@ocaml.doc
          "The ID of the network interface. If the instance has more than one network interface, you must specify a network interface ID. You can specify either the instance ID or the network interface ID, but not both."];
      privateIpAddress: String_.t option
        [@ocaml.doc
          "The primary or secondary private IP address to associate with the Elastic IP address. If no private IP address is specified, the Elastic IP address is associated with the primary private IP address."];
      allowReassociation: Boolean.t option
        [@ocaml.doc
          "Reassociation is automatic, but you can specify false to ensure the operation fails if the Elastic IP address is already associated with another resource."]}
    let make ?allocationId =
      fun ?instanceId ->
        fun ?publicIp ->
          fun ?dryRun ->
            fun ?networkInterfaceId ->
              fun ?privateIpAddress ->
                fun ?allowReassociation ->
                  fun () ->
                    {
                      allocationId;
                      instanceId;
                      publicIp;
                      dryRun;
                      networkInterfaceId;
                      privateIpAddress;
                      allowReassociation
                    }
    let to_value x =
      structure_to_value
        [("AllocationId",
           (Option.map x.allocationId ~f:AllocationId.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("PublicIp",
          (Option.map x.publicIp ~f:EipAllocationPublicIp.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value));
        ("privateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("allowReassociation",
          (Option.map x.allowReassociation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allowReassociation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "allowReassociation") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let publicIp =
        (Option.map ~f:EipAllocationPublicIp.of_xml)
          (Xml.child xml_arg0 "PublicIp") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let allocationId =
        (Option.map ~f:AllocationId.of_xml)
          (Xml.child xml_arg0 "AllocationId") in
      make ?allowReassociation ?privateIpAddress ?networkInterfaceId ?dryRun
        ?publicIp ?instanceId ?allocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allowReassociation =
        field_map json__ "AllowReassociation" Boolean.of_json in
      let privateIpAddress =
        field_map json__ "PrivateIpAddress" String_.of_json in
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let publicIp =
        field_map json__ "PublicIp" EipAllocationPublicIp.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let allocationId = field_map json__ "AllocationId" AllocationId.of_json in
      make ?allowReassociation ?privateIpAddress ?networkInterfaceId ?dryRun
        ?publicIp ?instanceId ?allocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates an Elastic IP address, or carrier IP address (for instances that are in subnets in Wavelength Zones) with an instance or a network interface. Before you can use an Elastic IP address, you must allocate it to your account. If the Elastic IP address is already associated with a different instance, it is disassociated from that instance and associated with the specified instance. If you associate an Elastic IP address with an instance that has an existing Elastic IP address, the existing address is disassociated from the instance, but remains allocated to your account. \\[Subnets in Wavelength Zones\\] You can associate an IP address from the telecommunication carrier to the instance or network interface. You cannot associate an Elastic IP address with an interface in a different network border group. This is an idempotent operation. If you perform the operation more than once, Amazon EC2 doesn't return an error, and you may be charged for each time the Elastic IP address is remapped to the same instance. For more information, see the Elastic IP Addresses section of Amazon EC2 Pricing."]
module AssignPrivateNatGatewayAddressResult =
  struct
    type nonrec t =
      {
      natGatewayId: NatGatewayId.t option
        [@ocaml.doc "The ID of the NAT gateway."];
      natGatewayAddresses: NatGatewayAddressList.t option
        [@ocaml.doc "NAT gateway IP addresses."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?natGatewayId =
      fun ?natGatewayAddresses ->
        fun () -> { natGatewayId; natGatewayAddresses }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("natGatewayId",
           (Option.map x.natGatewayId ~f:NatGatewayId.to_value));
        ("natGatewayAddressSet",
          (Option.map x.natGatewayAddresses ~f:NatGatewayAddressList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGatewayAddresses =
        (Option.map ~f:NatGatewayAddressList.of_xml)
          (Xml.child xml_arg0 "natGatewayAddressSet") in
      let natGatewayId =
        (Option.map ~f:NatGatewayId.of_xml)
          (Xml.child xml_arg0 "natGatewayId") in
      make ?natGatewayAddresses ?natGatewayId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGatewayAddresses =
        field_map json__ "NatGatewayAddresses" NatGatewayAddressList.of_json in
      let natGatewayId = field_map json__ "NatGatewayId" NatGatewayId.of_json in
      make ?natGatewayAddresses ?natGatewayId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns private IPv4 addresses to a private NAT gateway. For more information, see Work with NAT gateways in the Amazon VPC User Guide."]
module AssignPrivateNatGatewayAddressRequest =
  struct
    type nonrec t =
      {
      natGatewayId: NatGatewayId.t [@ocaml.doc "The ID of the NAT gateway."];
      privateIpAddresses: IpList.t option
        [@ocaml.doc
          "The private IPv4 addresses you want to assign to the private NAT gateway."];
      privateIpAddressCount: PrivateIpAddressCount.t option
        [@ocaml.doc
          "The number of private IP addresses to assign to the NAT gateway. You can't specify this parameter when also specifying private IP addresses."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AssignPrivateNatGatewayAddressRequest"
    let make ?privateIpAddresses =
      fun ?privateIpAddressCount ->
        fun ?dryRun ->
          fun ~natGatewayId ->
            fun () ->
              {
                privateIpAddresses;
                privateIpAddressCount;
                dryRun;
                natGatewayId
              }
    let to_value x =
      structure_to_value
        [("NatGatewayId", (Some (NatGatewayId.to_value x.natGatewayId)));
        ("PrivateIpAddress",
          (Option.map x.privateIpAddresses ~f:IpList.to_value));
        ("PrivateIpAddressCount",
          (Option.map x.privateIpAddressCount
             ~f:PrivateIpAddressCount.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let privateIpAddressCount =
        (Option.map ~f:PrivateIpAddressCount.of_xml)
          (Xml.child xml_arg0 "PrivateIpAddressCount") in
      let privateIpAddresses =
        (Option.map ~f:IpList.of_xml) (Xml.child xml_arg0 "PrivateIpAddress") in
      let natGatewayId =
        NatGatewayId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NatGatewayId") in
      make ?dryRun ?privateIpAddressCount ?privateIpAddresses ~natGatewayId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let privateIpAddressCount =
        field_map json__ "PrivateIpAddressCount"
          PrivateIpAddressCount.of_json in
      let privateIpAddresses =
        field_map json__ "PrivateIpAddresses" IpList.of_json in
      let natGatewayId =
        field_map_exn json__ "NatGatewayId" NatGatewayId.of_json in
      make ?dryRun ?privateIpAddressCount ?privateIpAddresses ~natGatewayId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns private IPv4 addresses to a private NAT gateway. For more information, see Work with NAT gateways in the Amazon VPC User Guide."]
module AssignPrivateIpAddressesResult =
  struct
    type nonrec t =
      {
      networkInterfaceId: String_.t option
        [@ocaml.doc "The ID of the network interface."];
      assignedPrivateIpAddresses: AssignedPrivateIpAddressList.t option
        [@ocaml.doc
          "The private IP addresses assigned to the network interface."];
      assignedIpv4Prefixes: Ipv4PrefixesList.t option
        [@ocaml.doc
          "The IPv4 prefixes that are assigned to the network interface."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?networkInterfaceId =
      fun ?assignedPrivateIpAddresses ->
        fun ?assignedIpv4Prefixes ->
          fun () ->
            {
              networkInterfaceId;
              assignedPrivateIpAddresses;
              assignedIpv4Prefixes
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkInterfaceId",
           (Option.map x.networkInterfaceId ~f:String_.to_value));
        ("assignedPrivateIpAddressesSet",
          (Option.map x.assignedPrivateIpAddresses
             ~f:AssignedPrivateIpAddressList.to_value));
        ("assignedIpv4PrefixSet",
          (Option.map x.assignedIpv4Prefixes ~f:Ipv4PrefixesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assignedIpv4Prefixes =
        (Option.map ~f:Ipv4PrefixesList.of_xml)
          (Xml.child xml_arg0 "assignedIpv4PrefixSet") in
      let assignedPrivateIpAddresses =
        (Option.map ~f:AssignedPrivateIpAddressList.of_xml)
          (Xml.child xml_arg0 "assignedPrivateIpAddressesSet") in
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      make ?assignedIpv4Prefixes ?assignedPrivateIpAddresses
        ?networkInterfaceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let assignedIpv4Prefixes =
        field_map json__ "AssignedIpv4Prefixes" Ipv4PrefixesList.of_json in
      let assignedPrivateIpAddresses =
        field_map json__ "AssignedPrivateIpAddresses"
          AssignedPrivateIpAddressList.of_json in
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" String_.of_json in
      make ?assignedIpv4Prefixes ?assignedPrivateIpAddresses
        ?networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns the specified secondary private IP addresses to the specified network interface. You can specify specific secondary IP addresses, or you can specify the number of secondary IP addresses to be automatically assigned from the subnet's CIDR block range. The number of secondary IP addresses that you can assign to an instance varies by instance type. For more information about Elastic IP addresses, see Elastic IP Addresses in the Amazon EC2 User Guide. When you move a secondary private IP address to another network interface, any Elastic IP address that is associated with the IP address is also moved. Remapping an IP address is an asynchronous operation. When you move an IP address from one network interface to another, check network/interfaces/macs/mac/local-ipv4s in the instance metadata to confirm that the remapping is complete. You must specify either the IP addresses or the IP address count in the request. You can optionally use Prefix Delegation on the network interface. You must specify either the IPv4 Prefix Delegation prefixes, or the IPv4 Prefix Delegation count. For information, see Assigning prefixes to network interfaces in the Amazon EC2 User Guide."]
module AssignPrivateIpAddressesRequest =
  struct
    type nonrec t =
      {
      ipv4Prefixes: IpPrefixList.t option
        [@ocaml.doc
          "One or more IPv4 prefixes assigned to the network interface. You can't use this option if you use the Ipv4PrefixCount option."];
      ipv4PrefixCount: Integer.t option
        [@ocaml.doc
          "The number of IPv4 prefixes that Amazon Web Services automatically assigns to the network interface. You can't use this option if you use the Ipv4 Prefixes option."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      privateIpAddresses: PrivateIpAddressStringList.t option
        [@ocaml.doc
          "The IP addresses to be assigned as a secondary private IP address to the network interface. You can't specify this parameter when also specifying a number of secondary IP addresses. If you don't specify an IP address, Amazon EC2 automatically selects an IP address within the subnet range."];
      secondaryPrivateIpAddressCount: Integer.t option
        [@ocaml.doc
          "The number of secondary IP addresses to assign to the network interface. You can't specify this parameter when also specifying private IP addresses."];
      allowReassignment: Boolean.t option
        [@ocaml.doc
          "Indicates whether to allow an IP address that is already assigned to another network interface or instance to be reassigned to the specified network interface."]}
    let context_ = "AssignPrivateIpAddressesRequest"
    let make ?ipv4Prefixes =
      fun ?ipv4PrefixCount ->
        fun ?privateIpAddresses ->
          fun ?secondaryPrivateIpAddressCount ->
            fun ?allowReassignment ->
              fun ~networkInterfaceId ->
                fun () ->
                  {
                    ipv4Prefixes;
                    ipv4PrefixCount;
                    privateIpAddresses;
                    secondaryPrivateIpAddressCount;
                    allowReassignment;
                    networkInterfaceId
                  }
    let to_value x =
      structure_to_value
        [("Ipv4Prefix", (Option.map x.ipv4Prefixes ~f:IpPrefixList.to_value));
        ("Ipv4PrefixCount",
          (Option.map x.ipv4PrefixCount ~f:Integer.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("privateIpAddress",
          (Option.map x.privateIpAddresses
             ~f:PrivateIpAddressStringList.to_value));
        ("secondaryPrivateIpAddressCount",
          (Option.map x.secondaryPrivateIpAddressCount ~f:Integer.to_value));
        ("allowReassignment",
          (Option.map x.allowReassignment ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allowReassignment =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "allowReassignment") in
      let secondaryPrivateIpAddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "secondaryPrivateIpAddressCount") in
      let privateIpAddresses =
        (Option.map ~f:PrivateIpAddressStringList.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let ipv4PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv4PrefixCount") in
      let ipv4Prefixes =
        (Option.map ~f:IpPrefixList.of_xml) (Xml.child xml_arg0 "Ipv4Prefix") in
      make ?allowReassignment ?secondaryPrivateIpAddressCount
        ?privateIpAddresses ~networkInterfaceId ?ipv4PrefixCount
        ?ipv4Prefixes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allowReassignment =
        field_map json__ "AllowReassignment" Boolean.of_json in
      let secondaryPrivateIpAddressCount =
        field_map json__ "SecondaryPrivateIpAddressCount" Integer.of_json in
      let privateIpAddresses =
        field_map json__ "PrivateIpAddresses"
          PrivateIpAddressStringList.of_json in
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let ipv4PrefixCount =
        field_map json__ "Ipv4PrefixCount" Integer.of_json in
      let ipv4Prefixes = field_map json__ "Ipv4Prefixes" IpPrefixList.of_json in
      make ?allowReassignment ?secondaryPrivateIpAddressCount
        ?privateIpAddresses ~networkInterfaceId ?ipv4PrefixCount
        ?ipv4Prefixes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for AssignPrivateIpAddresses."]
module AssignIpv6AddressesResult =
  struct
    type nonrec t =
      {
      assignedIpv6Addresses: Ipv6AddressList.t option
        [@ocaml.doc
          "The new IPv6 addresses assigned to the network interface. Existing IPv6 addresses that were assigned to the network interface before the request are not included."];
      assignedIpv6Prefixes: IpPrefixList.t option
        [@ocaml.doc
          "The IPv6 prefixes that are assigned to the network interface."];
      networkInterfaceId: String_.t option
        [@ocaml.doc "The ID of the network interface."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?assignedIpv6Addresses =
      fun ?assignedIpv6Prefixes ->
        fun ?networkInterfaceId ->
          fun () ->
            { assignedIpv6Addresses; assignedIpv6Prefixes; networkInterfaceId
            }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assignedIpv6Addresses",
           (Option.map x.assignedIpv6Addresses ~f:Ipv6AddressList.to_value));
        ("assignedIpv6PrefixSet",
          (Option.map x.assignedIpv6Prefixes ~f:IpPrefixList.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let assignedIpv6Prefixes =
        (Option.map ~f:IpPrefixList.of_xml)
          (Xml.child xml_arg0 "assignedIpv6PrefixSet") in
      let assignedIpv6Addresses =
        (Option.map ~f:Ipv6AddressList.of_xml)
          (Xml.child xml_arg0 "assignedIpv6Addresses") in
      make ?networkInterfaceId ?assignedIpv6Prefixes ?assignedIpv6Addresses
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInterfaceId =
        field_map json__ "NetworkInterfaceId" String_.of_json in
      let assignedIpv6Prefixes =
        field_map json__ "AssignedIpv6Prefixes" IpPrefixList.of_json in
      let assignedIpv6Addresses =
        field_map json__ "AssignedIpv6Addresses" Ipv6AddressList.of_json in
      make ?networkInterfaceId ?assignedIpv6Prefixes ?assignedIpv6Addresses
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns the specified IPv6 addresses to the specified network interface. You can specify specific IPv6 addresses, or you can specify the number of IPv6 addresses to be automatically assigned from the subnet's IPv6 CIDR block range. You can assign as many IPv6 addresses to a network interface as you can assign private IPv4 addresses, and the limit varies by instance type. You must specify either the IPv6 addresses or the IPv6 address count in the request. You can optionally use Prefix Delegation on the network interface. You must specify either the IPV6 Prefix Delegation prefixes, or the IPv6 Prefix Delegation count. For information, see Assigning prefixes to network interfaces in the Amazon EC2 User Guide."]
module AssignIpv6AddressesRequest =
  struct
    type nonrec t =
      {
      ipv6PrefixCount: Integer.t option
        [@ocaml.doc
          "The number of IPv6 prefixes that Amazon Web Services automatically assigns to the network interface. You cannot use this option if you use the Ipv6Prefixes option."];
      ipv6Prefixes: IpPrefixList.t option
        [@ocaml.doc
          "One or more IPv6 prefixes assigned to the network interface. You can't use this option if you use the Ipv6PrefixCount option."];
      networkInterfaceId: NetworkInterfaceId.t
        [@ocaml.doc "The ID of the network interface."];
      ipv6Addresses: Ipv6AddressList.t option
        [@ocaml.doc
          "The IPv6 addresses to be assigned to the network interface. You can't use this option if you're specifying a number of IPv6 addresses."];
      ipv6AddressCount: Integer.t option
        [@ocaml.doc
          "The number of additional IPv6 addresses to assign to the network interface. The specified number of IPv6 addresses are assigned in addition to the existing IPv6 addresses that are already assigned to the network interface. Amazon EC2 automatically selects the IPv6 addresses from the subnet range. You can't use this option if specifying specific IPv6 addresses."]}
    let context_ = "AssignIpv6AddressesRequest"
    let make ?ipv6PrefixCount =
      fun ?ipv6Prefixes ->
        fun ?ipv6Addresses ->
          fun ?ipv6AddressCount ->
            fun ~networkInterfaceId ->
              fun () ->
                {
                  ipv6PrefixCount;
                  ipv6Prefixes;
                  ipv6Addresses;
                  ipv6AddressCount;
                  networkInterfaceId
                }
    let to_value x =
      structure_to_value
        [("Ipv6PrefixCount",
           (Option.map x.ipv6PrefixCount ~f:Integer.to_value));
        ("Ipv6Prefix", (Option.map x.ipv6Prefixes ~f:IpPrefixList.to_value));
        ("networkInterfaceId",
          (Some (NetworkInterfaceId.to_value x.networkInterfaceId)));
        ("ipv6Addresses",
          (Option.map x.ipv6Addresses ~f:Ipv6AddressList.to_value));
        ("ipv6AddressCount",
          (Option.map x.ipv6AddressCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv6AddressCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ipv6AddressCount") in
      let ipv6Addresses =
        (Option.map ~f:Ipv6AddressList.of_xml)
          (Xml.child xml_arg0 "ipv6Addresses") in
      let networkInterfaceId =
        NetworkInterfaceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkInterfaceId") in
      let ipv6Prefixes =
        (Option.map ~f:IpPrefixList.of_xml) (Xml.child xml_arg0 "Ipv6Prefix") in
      let ipv6PrefixCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Ipv6PrefixCount") in
      make ?ipv6AddressCount ?ipv6Addresses ~networkInterfaceId ?ipv6Prefixes
        ?ipv6PrefixCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipv6AddressCount =
        field_map json__ "Ipv6AddressCount" Integer.of_json in
      let ipv6Addresses =
        field_map json__ "Ipv6Addresses" Ipv6AddressList.of_json in
      let networkInterfaceId =
        field_map_exn json__ "NetworkInterfaceId" NetworkInterfaceId.of_json in
      let ipv6Prefixes = field_map json__ "Ipv6Prefixes" IpPrefixList.of_json in
      let ipv6PrefixCount =
        field_map json__ "Ipv6PrefixCount" Integer.of_json in
      make ?ipv6AddressCount ?ipv6Addresses ~networkInterfaceId ?ipv6Prefixes
        ?ipv6PrefixCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns the specified IPv6 addresses to the specified network interface. You can specify specific IPv6 addresses, or you can specify the number of IPv6 addresses to be automatically assigned from the subnet's IPv6 CIDR block range. You can assign as many IPv6 addresses to a network interface as you can assign private IPv4 addresses, and the limit varies by instance type. You must specify either the IPv6 addresses or the IPv6 address count in the request. You can optionally use Prefix Delegation on the network interface. You must specify either the IPV6 Prefix Delegation prefixes, or the IPv6 Prefix Delegation count. For information, see Assigning prefixes to network interfaces in the Amazon EC2 User Guide."]
module ApplySecurityGroupsToClientVpnTargetNetworkResult =
  struct
    type nonrec t =
      {
      securityGroupIds: ClientVpnSecurityGroupIdSet.t option
        [@ocaml.doc "The IDs of the applied security groups."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?securityGroupIds = fun () -> { securityGroupIds }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroupIds",
           (Option.map x.securityGroupIds
              ~f:ClientVpnSecurityGroupIdSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupIds =
        (Option.map ~f:ClientVpnSecurityGroupIdSet.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      make ?securityGroupIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupIds =
        field_map json__ "SecurityGroupIds"
          ClientVpnSecurityGroupIdSet.of_json in
      make ?securityGroupIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies a security group to the association between the target network and the Client VPN endpoint. This action replaces the existing security groups with the specified security groups."]
module ApplySecurityGroupsToClientVpnTargetNetworkRequest =
  struct
    type nonrec t =
      {
      clientVpnEndpointId: ClientVpnEndpointId.t
        [@ocaml.doc "The ID of the Client VPN endpoint."];
      vpcId: VpcId.t
        [@ocaml.doc
          "The ID of the VPC in which the associated target network is located."];
      securityGroupIds: ClientVpnSecurityGroupIdSet.t
        [@ocaml.doc
          "The IDs of the security groups to apply to the associated target network. Up to 5 security groups can be applied to an associated target network."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "ApplySecurityGroupsToClientVpnTargetNetworkRequest"
    let make ?dryRun =
      fun ~clientVpnEndpointId ->
        fun ~vpcId ->
          fun ~securityGroupIds ->
            fun () ->
              { dryRun; clientVpnEndpointId; vpcId; securityGroupIds }
    let to_value x =
      structure_to_value
        [("ClientVpnEndpointId",
           (Some (ClientVpnEndpointId.to_value x.clientVpnEndpointId)));
        ("VpcId", (Some (VpcId.to_value x.vpcId)));
        ("SecurityGroupId",
          (Some (ClientVpnSecurityGroupIdSet.to_value x.securityGroupIds)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let securityGroupIds =
        ClientVpnSecurityGroupIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupId") in
      let vpcId =
        VpcId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let clientVpnEndpointId =
        ClientVpnEndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVpnEndpointId") in
      make ?dryRun ~securityGroupIds ~vpcId ~clientVpnEndpointId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let securityGroupIds =
        field_map_exn json__ "SecurityGroupIds"
          ClientVpnSecurityGroupIdSet.of_json in
      let vpcId = field_map_exn json__ "VpcId" VpcId.of_json in
      let clientVpnEndpointId =
        field_map_exn json__ "ClientVpnEndpointId"
          ClientVpnEndpointId.of_json in
      make ?dryRun ~securityGroupIds ~vpcId ~clientVpnEndpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies a security group to the association between the target network and the Client VPN endpoint. This action replaces the existing security groups with the specified security groups."]
module AllocateIpamPoolCidrResult =
  struct
    type nonrec t =
      {
      ipamPoolAllocation: IpamPoolAllocation.t option
        [@ocaml.doc "Information about the allocation created."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ipamPoolAllocation = fun () -> { ipamPoolAllocation }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ipamPoolAllocation",
           (Option.map x.ipamPoolAllocation ~f:IpamPoolAllocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipamPoolAllocation =
        (Option.map ~f:IpamPoolAllocation.of_xml)
          (Xml.child xml_arg0 "ipamPoolAllocation") in
      make ?ipamPoolAllocation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ipamPoolAllocation =
        field_map json__ "IpamPoolAllocation" IpamPoolAllocation.of_json in
      make ?ipamPoolAllocation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allocate a CIDR from an IPAM pool. The Region you use should be the IPAM pool locale. The locale is the Amazon Web Services Region where this IPAM pool is available for allocations. In IPAM, an allocation is a CIDR assignment from an IPAM pool to another IPAM pool or to a resource. For more information, see Allocate CIDRs in the Amazon VPC IPAM User Guide. This action creates an allocation with strong consistency. The returned CIDR will not overlap with any other allocations from the same pool."]
module AllocateIpamPoolCidrRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "A check for whether you have the required permissions for the action without actually making the request and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      ipamPoolId: IpamPoolId.t
        [@ocaml.doc
          "The ID of the IPAM pool from which you would like to allocate a CIDR."];
      cidr: String_.t option
        [@ocaml.doc
          "The CIDR you would like to allocate from the IPAM pool. Note the following: If there is no DefaultNetmaskLength allocation rule set on the pool, you must specify either the NetmaskLength or the CIDR. If the DefaultNetmaskLength allocation rule is set on the pool, you can specify either the NetmaskLength or the CIDR and the DefaultNetmaskLength allocation rule will be ignored. Possible values: Any available IPv4 or IPv6 CIDR."];
      netmaskLength: Integer.t option
        [@ocaml.doc
          "The netmask length of the CIDR you would like to allocate from the IPAM pool. Note the following: If there is no DefaultNetmaskLength allocation rule set on the pool, you must specify either the NetmaskLength or the CIDR. If the DefaultNetmaskLength allocation rule is set on the pool, you can specify either the NetmaskLength or the CIDR and the DefaultNetmaskLength allocation rule will be ignored. Possible netmask lengths for IPv4 addresses are 0 - 32. Possible netmask lengths for IPv6 addresses are 0 - 128."];
      clientToken: String_.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring idempotency."];
      description: String_.t option
        [@ocaml.doc "A description for the allocation."];
      previewNextCidr: Boolean.t option
        [@ocaml.doc "A preview of the next available CIDR in a pool."];
      allowedCidrs: IpamPoolAllocationAllowedCidrs.t option
        [@ocaml.doc
          "Include a particular CIDR range that can be returned by the pool. Allowed CIDRs are only allowed if using netmask length for allocation."];
      disallowedCidrs: IpamPoolAllocationDisallowedCidrs.t option
        [@ocaml.doc
          "Exclude a particular CIDR range from being returned by the pool. Disallowed CIDRs are only allowed if using netmask length for allocation."]}
    let context_ = "AllocateIpamPoolCidrRequest"
    let make ?dryRun =
      fun ?cidr ->
        fun ?netmaskLength ->
          fun ?clientToken ->
            fun ?description ->
              fun ?previewNextCidr ->
                fun ?allowedCidrs ->
                  fun ?disallowedCidrs ->
                    fun ~ipamPoolId ->
                      fun () ->
                        {
                          dryRun;
                          cidr;
                          netmaskLength;
                          clientToken;
                          description;
                          previewNextCidr;
                          allowedCidrs;
                          disallowedCidrs;
                          ipamPoolId
                        }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("IpamPoolId", (Some (IpamPoolId.to_value x.ipamPoolId)));
        ("Cidr", (Option.map x.cidr ~f:String_.to_value));
        ("NetmaskLength", (Option.map x.netmaskLength ~f:Integer.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("PreviewNextCidr",
          (Option.map x.previewNextCidr ~f:Boolean.to_value));
        ("AllowedCidr",
          (Option.map x.allowedCidrs
             ~f:IpamPoolAllocationAllowedCidrs.to_value));
        ("DisallowedCidr",
          (Option.map x.disallowedCidrs
             ~f:IpamPoolAllocationDisallowedCidrs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let disallowedCidrs =
        (Option.map ~f:IpamPoolAllocationDisallowedCidrs.of_xml)
          (Xml.child xml_arg0 "DisallowedCidr") in
      let allowedCidrs =
        (Option.map ~f:IpamPoolAllocationAllowedCidrs.of_xml)
          (Xml.child xml_arg0 "AllowedCidr") in
      let previewNextCidr =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "PreviewNextCidr") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let netmaskLength =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "NetmaskLength") in
      let cidr = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Cidr") in
      let ipamPoolId =
        IpamPoolId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IpamPoolId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?disallowedCidrs ?allowedCidrs ?previewNextCidr ?description
        ?clientToken ?netmaskLength ?cidr ~ipamPoolId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let disallowedCidrs =
        field_map json__ "DisallowedCidrs"
          IpamPoolAllocationDisallowedCidrs.of_json in
      let allowedCidrs =
        field_map json__ "AllowedCidrs"
          IpamPoolAllocationAllowedCidrs.of_json in
      let previewNextCidr =
        field_map json__ "PreviewNextCidr" Boolean.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let netmaskLength = field_map json__ "NetmaskLength" Integer.of_json in
      let cidr = field_map json__ "Cidr" String_.of_json in
      let ipamPoolId = field_map_exn json__ "IpamPoolId" IpamPoolId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?disallowedCidrs ?allowedCidrs ?previewNextCidr ?description
        ?clientToken ?netmaskLength ?cidr ~ipamPoolId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allocate a CIDR from an IPAM pool. The Region you use should be the IPAM pool locale. The locale is the Amazon Web Services Region where this IPAM pool is available for allocations. In IPAM, an allocation is a CIDR assignment from an IPAM pool to another IPAM pool or to a resource. For more information, see Allocate CIDRs in the Amazon VPC IPAM User Guide. This action creates an allocation with strong consistency. The returned CIDR will not overlap with any other allocations from the same pool."]
module AllocateHostsResult =
  struct
    type nonrec t =
      {
      hostIds: ResponseHostIdList.t option
        [@ocaml.doc
          "The ID of the allocated Dedicated Host. This is used to launch an instance onto a specific host."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?hostIds = fun () -> { hostIds }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("hostIdSet", (Option.map x.hostIds ~f:ResponseHostIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostIds =
        (Option.map ~f:ResponseHostIdList.of_xml)
          (Xml.child xml_arg0 "hostIdSet") in
      make ?hostIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let hostIds = field_map json__ "HostIds" ResponseHostIdList.of_json in
      make ?hostIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of AllocateHosts."]
module AllocateHostsRequest =
  struct
    type nonrec t =
      {
      instanceFamily: String_.t option
        [@ocaml.doc
          "Specifies the instance family to be supported by the Dedicated Hosts. If you specify an instance family, the Dedicated Hosts support multiple instance types within that instance family. If you want the Dedicated Hosts to support a specific instance type only, omit this parameter and specify InstanceType instead. You cannot specify InstanceFamily and InstanceType in the same request."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "The tags to apply to the Dedicated Host during creation."];
      hostRecovery: HostRecovery.t option
        [@ocaml.doc
          "Indicates whether to enable or disable host recovery for the Dedicated Host. Host recovery is disabled by default. For more information, see Host recovery in the Amazon EC2 User Guide. Default: off"];
      outpostArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon Web Services Outpost on which to allocate the Dedicated Host. If you specify OutpostArn, you can optionally specify AssetIds. If you are allocating the Dedicated Host in a Region, omit this parameter."];
      hostMaintenance: HostMaintenance.t option
        [@ocaml.doc
          "Indicates whether to enable or disable host maintenance for the Dedicated Host. For more information, see Host maintenance in the Amazon EC2 User Guide."];
      assetIds: AssetIdList.t option
        [@ocaml.doc
          "The IDs of the Outpost hardware assets on which to allocate the Dedicated Hosts. Targeting specific hardware assets on an Outpost can help to minimize latency between your workloads. This parameter is supported only if you specify OutpostArn. If you are allocating the Dedicated Hosts in a Region, omit this parameter. If you specify this parameter, you can omit Quantity. In this case, Amazon EC2 allocates a Dedicated Host on each specified hardware asset. If you specify both AssetIds and Quantity, then the value for Quantity must be equal to the number of asset IDs specified."];
      availabilityZoneId: AvailabilityZoneId.t option
        [@ocaml.doc "The ID of the Availability Zone."];
      autoPlacement: AutoPlacement.t option
        [@ocaml.doc
          "Indicates whether the host accepts any untargeted instance launches that match its instance type configuration, or if it only accepts Host tenancy instance launches that specify its unique host ID. For more information, see Understanding auto-placement and affinity in the Amazon EC2 User Guide. Default: off"];
      clientToken: String_.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Ensuring Idempotency."];
      instanceType: String_.t option
        [@ocaml.doc
          "Specifies the instance type to be supported by the Dedicated Hosts. If you specify an instance type, the Dedicated Hosts support instances of the specified instance type only. If you want the Dedicated Hosts to support multiple instance types in a specific instance family, omit this parameter and specify InstanceFamily instead. You cannot specify InstanceType and InstanceFamily in the same request."];
      quantity: Integer.t option
        [@ocaml.doc
          "The number of Dedicated Hosts to allocate to your account with these parameters. If you are allocating the Dedicated Hosts on an Outpost, and you specify AssetIds, you can omit this parameter. In this case, Amazon EC2 allocates a Dedicated Host on each specified hardware asset. If you specify both AssetIds and Quantity, then the value that you specify for Quantity must be equal to the number of asset IDs specified."];
      availabilityZone: AvailabilityZoneName.t option
        [@ocaml.doc
          "The Availability Zone in which to allocate the Dedicated Host."]}
    let make ?instanceFamily =
      fun ?tagSpecifications ->
        fun ?hostRecovery ->
          fun ?outpostArn ->
            fun ?hostMaintenance ->
              fun ?assetIds ->
                fun ?availabilityZoneId ->
                  fun ?autoPlacement ->
                    fun ?clientToken ->
                      fun ?instanceType ->
                        fun ?quantity ->
                          fun ?availabilityZone ->
                            fun () ->
                              {
                                instanceFamily;
                                tagSpecifications;
                                hostRecovery;
                                outpostArn;
                                hostMaintenance;
                                assetIds;
                                availabilityZoneId;
                                autoPlacement;
                                clientToken;
                                instanceType;
                                quantity;
                                availabilityZone
                              }
    let to_value x =
      structure_to_value
        [("InstanceFamily",
           (Option.map x.instanceFamily ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("HostRecovery",
          (Option.map x.hostRecovery ~f:HostRecovery.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:String_.to_value));
        ("HostMaintenance",
          (Option.map x.hostMaintenance ~f:HostMaintenance.to_value));
        ("AssetId", (Option.map x.assetIds ~f:AssetIdList.to_value));
        ("AvailabilityZoneId",
          (Option.map x.availabilityZoneId ~f:AvailabilityZoneId.to_value));
        ("autoPlacement",
          (Option.map x.autoPlacement ~f:AutoPlacement.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("quantity", (Option.map x.quantity ~f:Integer.to_value));
        ("availabilityZone",
          (Option.map x.availabilityZone ~f:AvailabilityZoneName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZone =
        (Option.map ~f:AvailabilityZoneName.of_xml)
          (Xml.child xml_arg0 "availabilityZone") in
      let quantity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "quantity") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let autoPlacement =
        (Option.map ~f:AutoPlacement.of_xml)
          (Xml.child xml_arg0 "autoPlacement") in
      let availabilityZoneId =
        (Option.map ~f:AvailabilityZoneId.of_xml)
          (Xml.child xml_arg0 "AvailabilityZoneId") in
      let assetIds =
        (Option.map ~f:AssetIdList.of_xml) (Xml.child xml_arg0 "AssetId") in
      let hostMaintenance =
        (Option.map ~f:HostMaintenance.of_xml)
          (Xml.child xml_arg0 "HostMaintenance") in
      let outpostArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "OutpostArn") in
      let hostRecovery =
        (Option.map ~f:HostRecovery.of_xml)
          (Xml.child xml_arg0 "HostRecovery") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceFamily") in
      make ?availabilityZone ?quantity ?instanceType ?clientToken
        ?autoPlacement ?availabilityZoneId ?assetIds ?hostMaintenance
        ?outpostArn ?hostRecovery ?tagSpecifications ?instanceFamily ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZone =
        field_map json__ "AvailabilityZone" AvailabilityZoneName.of_json in
      let quantity = field_map json__ "Quantity" Integer.of_json in
      let instanceType = field_map json__ "InstanceType" String_.of_json in
      let clientToken = field_map json__ "ClientToken" String_.of_json in
      let autoPlacement =
        field_map json__ "AutoPlacement" AutoPlacement.of_json in
      let availabilityZoneId =
        field_map json__ "AvailabilityZoneId" AvailabilityZoneId.of_json in
      let assetIds = field_map json__ "AssetIds" AssetIdList.of_json in
      let hostMaintenance =
        field_map json__ "HostMaintenance" HostMaintenance.of_json in
      let outpostArn = field_map json__ "OutpostArn" String_.of_json in
      let hostRecovery = field_map json__ "HostRecovery" HostRecovery.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let instanceFamily = field_map json__ "InstanceFamily" String_.of_json in
      make ?availabilityZone ?quantity ?instanceType ?clientToken
        ?autoPlacement ?availabilityZoneId ?assetIds ?hostMaintenance
        ?outpostArn ?hostRecovery ?tagSpecifications ?instanceFamily ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allocates a Dedicated Host to your account. At a minimum, specify the supported instance type or instance family, the Availability Zone in which to allocate the host, and the number of hosts to allocate."]
module AllocateAddressResult =
  struct
    type nonrec t =
      {
      allocationId: String_.t option
        [@ocaml.doc
          "The ID that represents the allocation of the Elastic IP address."];
      publicIpv4Pool: String_.t option
        [@ocaml.doc "The ID of an address pool that you own."];
      networkBorderGroup: String_.t option
        [@ocaml.doc
          "The set of Availability Zones, Local Zones, or Wavelength Zones from which Amazon Web Services advertises IP addresses."];
      domain: DomainType.t option [@ocaml.doc "The network (vpc)."];
      customerOwnedIp: String_.t option
        [@ocaml.doc "The customer-owned IP address."];
      customerOwnedIpv4Pool: String_.t option
        [@ocaml.doc "The ID of the customer-owned address pool."];
      carrierIp: String_.t option
        [@ocaml.doc
          "The carrier IP address. Available only for network interfaces that reside in a subnet in a Wavelength Zone."];
      publicIp: String_.t option
        [@ocaml.doc
          "The Amazon-owned IP address. Not available when using an address pool that you own."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?allocationId =
      fun ?publicIpv4Pool ->
        fun ?networkBorderGroup ->
          fun ?domain ->
            fun ?customerOwnedIp ->
              fun ?customerOwnedIpv4Pool ->
                fun ?carrierIp ->
                  fun ?publicIp ->
                    fun () ->
                      {
                        allocationId;
                        publicIpv4Pool;
                        networkBorderGroup;
                        domain;
                        customerOwnedIp;
                        customerOwnedIpv4Pool;
                        carrierIp;
                        publicIp
                      }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("allocationId", (Option.map x.allocationId ~f:String_.to_value));
        ("publicIpv4Pool", (Option.map x.publicIpv4Pool ~f:String_.to_value));
        ("networkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value));
        ("domain", (Option.map x.domain ~f:DomainType.to_value));
        ("customerOwnedIp",
          (Option.map x.customerOwnedIp ~f:String_.to_value));
        ("customerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool ~f:String_.to_value));
        ("carrierIp", (Option.map x.carrierIp ~f:String_.to_value));
        ("publicIp", (Option.map x.publicIp ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let publicIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "publicIp") in
      let carrierIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "carrierIp") in
      let customerOwnedIpv4Pool =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "customerOwnedIpv4Pool") in
      let customerOwnedIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "customerOwnedIp") in
      let domain =
        (Option.map ~f:DomainType.of_xml) (Xml.child xml_arg0 "domain") in
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "networkBorderGroup") in
      let publicIpv4Pool =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "publicIpv4Pool") in
      let allocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "allocationId") in
      make ?publicIp ?carrierIp ?customerOwnedIpv4Pool ?customerOwnedIp
        ?domain ?networkBorderGroup ?publicIpv4Pool ?allocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let publicIp = field_map json__ "PublicIp" String_.of_json in
      let carrierIp = field_map json__ "CarrierIp" String_.of_json in
      let customerOwnedIpv4Pool =
        field_map json__ "CustomerOwnedIpv4Pool" String_.of_json in
      let customerOwnedIp =
        field_map json__ "CustomerOwnedIp" String_.of_json in
      let domain = field_map json__ "Domain" DomainType.of_json in
      let networkBorderGroup =
        field_map json__ "NetworkBorderGroup" String_.of_json in
      let publicIpv4Pool = field_map json__ "PublicIpv4Pool" String_.of_json in
      let allocationId = field_map json__ "AllocationId" String_.of_json in
      make ?publicIp ?carrierIp ?customerOwnedIpv4Pool ?customerOwnedIp
        ?domain ?networkBorderGroup ?publicIpv4Pool ?allocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allocates an Elastic IP address to your Amazon Web Services account. After you allocate the Elastic IP address you can associate it with an instance or network interface. After you release an Elastic IP address, it is released to the IP address pool and can be allocated to a different Amazon Web Services account. You can allocate an Elastic IP address from one of the following address pools: Amazon's pool of IPv4 addresses Public IPv4 address range that you own and bring to your Amazon Web Services account using Bring Your Own IP Addresses (BYOIP) An IPv4 IPAM pool with an Amazon-provided or BYOIP public IPv4 address range IPv4 addresses from your on-premises network made available for use with an Outpost using a customer-owned IP address pool (CoIP pool) For more information, see Elastic IP Addresses in the Amazon EC2 User Guide. If you release an Elastic IP address, you might be able to recover it. You cannot recover an Elastic IP address that you released after it is allocated to another Amazon Web Services account. To attempt to recover an Elastic IP address that you released, specify it in this operation. You can allocate a carrier IP address which is a public IP address from a telecommunication carrier, to a network interface which resides in a subnet in a Wavelength Zone (for example an EC2 instance)."]
module AllocateAddressRequest =
  struct
    type nonrec t =
      {
      domain: DomainType.t option [@ocaml.doc "The network (vpc)."];
      address: PublicIpAddress.t option
        [@ocaml.doc
          "The Elastic IP address to recover or an IPv4 address from an address pool."];
      publicIpv4Pool: Ipv4PoolEc2Id.t option
        [@ocaml.doc
          "The ID of an address pool that you own. Use this parameter to let Amazon EC2 select an address from the address pool. To specify a specific address from the address pool, use the Address parameter instead."];
      networkBorderGroup: String_.t option
        [@ocaml.doc
          "A unique set of Availability Zones, Local Zones, or Wavelength Zones from which Amazon Web Services advertises IP addresses. Use this parameter to limit the IP address to this location. IP addresses cannot move between network border groups."];
      customerOwnedIpv4Pool: String_.t option
        [@ocaml.doc
          "The ID of a customer-owned address pool. Use this parameter to let Amazon EC2 select an address from the address pool. Alternatively, specify a specific address from the address pool."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc "The tags to assign to the Elastic IP address."];
      ipamPoolId: IpamPoolId.t option
        [@ocaml.doc
          "The ID of an IPAM pool which has an Amazon-provided or BYOIP public IPv4 CIDR provisioned to it. For more information, see Allocate sequential Elastic IP addresses from an IPAM pool in the Amazon VPC IPAM User Guide."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?domain =
      fun ?address ->
        fun ?publicIpv4Pool ->
          fun ?networkBorderGroup ->
            fun ?customerOwnedIpv4Pool ->
              fun ?tagSpecifications ->
                fun ?ipamPoolId ->
                  fun ?dryRun ->
                    fun () ->
                      {
                        domain;
                        address;
                        publicIpv4Pool;
                        networkBorderGroup;
                        customerOwnedIpv4Pool;
                        tagSpecifications;
                        ipamPoolId;
                        dryRun
                      }
    let to_value x =
      structure_to_value
        [("Domain", (Option.map x.domain ~f:DomainType.to_value));
        ("Address", (Option.map x.address ~f:PublicIpAddress.to_value));
        ("PublicIpv4Pool",
          (Option.map x.publicIpv4Pool ~f:Ipv4PoolEc2Id.to_value));
        ("NetworkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value));
        ("CustomerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool ~f:String_.to_value));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("IpamPoolId", (Option.map x.ipamPoolId ~f:IpamPoolId.to_value));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let ipamPoolId =
        (Option.map ~f:IpamPoolId.of_xml) (Xml.child xml_arg0 "IpamPoolId") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let customerOwnedIpv4Pool =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CustomerOwnedIpv4Pool") in
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkBorderGroup") in
      let publicIpv4Pool =
        (Option.map ~f:Ipv4PoolEc2Id.of_xml)
          (Xml.child xml_arg0 "PublicIpv4Pool") in
      let address =
        (Option.map ~f:PublicIpAddress.of_xml) (Xml.child xml_arg0 "Address") in
      let domain =
        (Option.map ~f:DomainType.of_xml) (Xml.child xml_arg0 "Domain") in
      make ?dryRun ?ipamPoolId ?tagSpecifications ?customerOwnedIpv4Pool
        ?networkBorderGroup ?publicIpv4Pool ?address ?domain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let ipamPoolId = field_map json__ "IpamPoolId" IpamPoolId.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let customerOwnedIpv4Pool =
        field_map json__ "CustomerOwnedIpv4Pool" String_.of_json in
      let networkBorderGroup =
        field_map json__ "NetworkBorderGroup" String_.of_json in
      let publicIpv4Pool =
        field_map json__ "PublicIpv4Pool" Ipv4PoolEc2Id.of_json in
      let address = field_map json__ "Address" PublicIpAddress.of_json in
      let domain = field_map json__ "Domain" DomainType.of_json in
      make ?dryRun ?ipamPoolId ?tagSpecifications ?customerOwnedIpv4Pool
        ?networkBorderGroup ?publicIpv4Pool ?address ?domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allocates an Elastic IP address to your Amazon Web Services account. After you allocate the Elastic IP address you can associate it with an instance or network interface. After you release an Elastic IP address, it is released to the IP address pool and can be allocated to a different Amazon Web Services account. You can allocate an Elastic IP address from one of the following address pools: Amazon's pool of IPv4 addresses Public IPv4 address range that you own and bring to your Amazon Web Services account using Bring Your Own IP Addresses (BYOIP) An IPv4 IPAM pool with an Amazon-provided or BYOIP public IPv4 address range IPv4 addresses from your on-premises network made available for use with an Outpost using a customer-owned IP address pool (CoIP pool) For more information, see Elastic IP Addresses in the Amazon EC2 User Guide. If you release an Elastic IP address, you might be able to recover it. You cannot recover an Elastic IP address that you released after it is allocated to another Amazon Web Services account. To attempt to recover an Elastic IP address that you released, specify it in this operation. You can allocate a carrier IP address which is a public IP address from a telecommunication carrier, to a network interface which resides in a subnet in a Wavelength Zone (for example an EC2 instance)."]
module AdvertiseByoipCidrResult =
  struct
    type nonrec t =
      {
      byoipCidr: ByoipCidr.t option
        [@ocaml.doc "Information about the address range."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?byoipCidr = fun () -> { byoipCidr }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("byoipCidr", (Option.map x.byoipCidr ~f:ByoipCidr.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byoipCidr =
        (Option.map ~f:ByoipCidr.of_xml) (Xml.child xml_arg0 "byoipCidr") in
      make ?byoipCidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let byoipCidr = field_map json__ "ByoipCidr" ByoipCidr.of_json in
      make ?byoipCidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Advertises an IPv4 or IPv6 address range that is provisioned for use with your Amazon Web Services resources through bring your own IP addresses (BYOIP). You can perform this operation at most once every 10 seconds, even if you specify different address ranges each time. We recommend that you stop advertising the BYOIP CIDR from other locations when you advertise it from Amazon Web Services. To minimize down time, you can configure your Amazon Web Services resources to use an address from a BYOIP CIDR before it is advertised, and then simultaneously stop advertising it from the current location and start advertising it through Amazon Web Services. It can take a few minutes before traffic to the specified addresses starts routing to Amazon Web Services because of BGP propagation delays."]
module AdvertiseByoipCidrRequest =
  struct
    type nonrec t =
      {
      cidr: String_.t
        [@ocaml.doc
          "The address range, in CIDR notation. This must be the exact range that you provisioned. You can't advertise only a portion of the provisioned range."];
      asn: String_.t option
        [@ocaml.doc
          "The public 2-byte or 4-byte ASN that you want to advertise."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      networkBorderGroup: String_.t option
        [@ocaml.doc
          "If you have Local Zones enabled, you can choose a network border group for Local Zones when you provision and advertise a BYOIPv4 CIDR. Choose the network border group carefully as the EIP and the Amazon Web Services resource it is associated with must reside in the same network border group. You can provision BYOIP address ranges to and advertise them in the following Local Zone network border groups: us-east-1-dfw-2 us-west-2-lax-1 us-west-2-phx-2 You cannot provision or advertise BYOIPv6 address ranges in Local Zones at this time."]}
    let context_ = "AdvertiseByoipCidrRequest"
    let make ?asn =
      fun ?dryRun ->
        fun ?networkBorderGroup ->
          fun ~cidr -> fun () -> { asn; dryRun; networkBorderGroup; cidr }
    let to_value x =
      structure_to_value
        [("Cidr", (Some (String_.to_value x.cidr)));
        ("Asn", (Option.map x.asn ~f:String_.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("NetworkBorderGroup",
          (Option.map x.networkBorderGroup ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkBorderGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "NetworkBorderGroup") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let asn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Asn") in
      let cidr =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Cidr") in
      make ?networkBorderGroup ?dryRun ?asn ~cidr ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkBorderGroup =
        field_map json__ "NetworkBorderGroup" String_.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let asn = field_map json__ "Asn" String_.of_json in
      let cidr = field_map_exn json__ "Cidr" String_.of_json in
      make ?networkBorderGroup ?dryRun ?asn ~cidr ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Advertises an IPv4 or IPv6 address range that is provisioned for use with your Amazon Web Services resources through bring your own IP addresses (BYOIP). You can perform this operation at most once every 10 seconds, even if you specify different address ranges each time. We recommend that you stop advertising the BYOIP CIDR from other locations when you advertise it from Amazon Web Services. To minimize down time, you can configure your Amazon Web Services resources to use an address from a BYOIP CIDR before it is advertised, and then simultaneously stop advertising it from the current location and start advertising it through Amazon Web Services. It can take a few minutes before traffic to the specified addresses starts routing to Amazon Web Services because of BGP propagation delays."]
module AcceptVpcPeeringConnectionResult =
  struct
    type nonrec t =
      {
      vpcPeeringConnection: VpcPeeringConnection.t option
        [@ocaml.doc "Information about the VPC peering connection."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringConnection = fun () -> { vpcPeeringConnection }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpcPeeringConnection",
           (Option.map x.vpcPeeringConnection
              ~f:VpcPeeringConnection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnection =
        (Option.map ~f:VpcPeeringConnection.of_xml)
          (Xml.child xml_arg0 "vpcPeeringConnection") in
      make ?vpcPeeringConnection ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringConnection =
        field_map json__ "VpcPeeringConnection" VpcPeeringConnection.of_json in
      make ?vpcPeeringConnection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accept a VPC peering connection request. To accept a request, the VPC peering connection must be in the pending-acceptance state, and you must be the owner of the peer VPC. Use DescribeVpcPeeringConnections to view your outstanding VPC peering connection requests. For an inter-Region VPC peering connection request, you must accept the VPC peering connection in the Region of the accepter VPC."]
module AcceptVpcPeeringConnectionRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      vpcPeeringConnectionId: VpcPeeringConnectionIdWithResolver.t
        [@ocaml.doc
          "The ID of the VPC peering connection. You must specify this parameter in the request."]}
    let context_ = "AcceptVpcPeeringConnectionRequest"
    let make ?dryRun =
      fun ~vpcPeeringConnectionId ->
        fun () -> { dryRun; vpcPeeringConnectionId }
    let to_value x =
      structure_to_value
        [("dryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("vpcPeeringConnectionId",
          (Some
             (VpcPeeringConnectionIdWithResolver.to_value
                x.vpcPeeringConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        VpcPeeringConnectionIdWithResolver.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpcPeeringConnectionId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      make ~vpcPeeringConnectionId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringConnectionId =
        field_map_exn json__ "VpcPeeringConnectionId"
          VpcPeeringConnectionIdWithResolver.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~vpcPeeringConnectionId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accept a VPC peering connection request. To accept a request, the VPC peering connection must be in the pending-acceptance state, and you must be the owner of the peer VPC. Use DescribeVpcPeeringConnections to view your outstanding VPC peering connection requests. For an inter-Region VPC peering connection request, you must accept the VPC peering connection in the Region of the accepter VPC."]
module AcceptVpcEndpointConnectionsResult =
  struct
    type nonrec t =
      {
      unsuccessful: UnsuccessfulItemSet.t option
        [@ocaml.doc
          "Information about the interface endpoints that were not accepted, if applicable."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?unsuccessful = fun () -> { unsuccessful }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("unsuccessful",
           (Option.map x.unsuccessful ~f:UnsuccessfulItemSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unsuccessful =
        (Option.map ~f:UnsuccessfulItemSet.of_xml)
          (Xml.child xml_arg0 "unsuccessful") in
      make ?unsuccessful ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unsuccessful =
        field_map json__ "Unsuccessful" UnsuccessfulItemSet.of_json in
      make ?unsuccessful ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts connection requests to your VPC endpoint service."]
module AcceptVpcEndpointConnectionsRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      serviceId: VpcEndpointServiceId.t
        [@ocaml.doc "The ID of the VPC endpoint service."];
      vpcEndpointIds: VpcEndpointIdList.t
        [@ocaml.doc "The IDs of the interface VPC endpoints."]}
    let context_ = "AcceptVpcEndpointConnectionsRequest"
    let make ?dryRun =
      fun ~serviceId ->
        fun ~vpcEndpointIds ->
          fun () -> { dryRun; serviceId; vpcEndpointIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ServiceId", (Some (VpcEndpointServiceId.to_value x.serviceId)));
        ("VpcEndpointId",
          (Some (VpcEndpointIdList.to_value x.vpcEndpointIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcEndpointIds =
        VpcEndpointIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcEndpointId") in
      let serviceId =
        VpcEndpointServiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~vpcEndpointIds ~serviceId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcEndpointIds =
        field_map_exn json__ "VpcEndpointIds" VpcEndpointIdList.of_json in
      let serviceId =
        field_map_exn json__ "ServiceId" VpcEndpointServiceId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~vpcEndpointIds ~serviceId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts connection requests to your VPC endpoint service."]
module AcceptTransitGatewayVpcAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayVpcAttachment: TransitGatewayVpcAttachment.t option
        [@ocaml.doc "The VPC attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayVpcAttachment =
      fun () -> { transitGatewayVpcAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayVpcAttachment",
           (Option.map x.transitGatewayVpcAttachment
              ~f:TransitGatewayVpcAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayVpcAttachment =
        (Option.map ~f:TransitGatewayVpcAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayVpcAttachment") in
      make ?transitGatewayVpcAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayVpcAttachment =
        field_map json__ "TransitGatewayVpcAttachment"
          TransitGatewayVpcAttachment.of_json in
      make ?transitGatewayVpcAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a request to attach a VPC to a transit gateway. The VPC attachment must be in the pendingAcceptance state. Use DescribeTransitGatewayVpcAttachments to view your pending VPC attachment requests. Use RejectTransitGatewayVpcAttachment to reject a VPC attachment request."]
module AcceptTransitGatewayVpcAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AcceptTransitGatewayVpcAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a request to attach a VPC to a transit gateway. The VPC attachment must be in the pendingAcceptance state. Use DescribeTransitGatewayVpcAttachments to view your pending VPC attachment requests. Use RejectTransitGatewayVpcAttachment to reject a VPC attachment request."]
module AcceptTransitGatewayPeeringAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayPeeringAttachment:
        TransitGatewayPeeringAttachment.t option
        [@ocaml.doc "The transit gateway peering attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayPeeringAttachment =
      fun () -> { transitGatewayPeeringAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayPeeringAttachment",
           (Option.map x.transitGatewayPeeringAttachment
              ~f:TransitGatewayPeeringAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayPeeringAttachment =
        (Option.map ~f:TransitGatewayPeeringAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayPeeringAttachment") in
      make ?transitGatewayPeeringAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayPeeringAttachment =
        field_map json__ "TransitGatewayPeeringAttachment"
          TransitGatewayPeeringAttachment.of_json in
      make ?transitGatewayPeeringAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a transit gateway peering attachment request. The peering attachment must be in the pendingAcceptance state."]
module AcceptTransitGatewayPeeringAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the transit gateway attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AcceptTransitGatewayPeeringAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a transit gateway peering attachment request. The peering attachment must be in the pendingAcceptance state."]
module AcceptTransitGatewayMulticastDomainAssociationsResult =
  struct
    type nonrec t =
      {
      associations: TransitGatewayMulticastDomainAssociations.t option
        [@ocaml.doc "Information about the multicast domain associations."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?associations = fun () -> { associations }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associations",
           (Option.map x.associations
              ~f:TransitGatewayMulticastDomainAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associations =
        (Option.map ~f:TransitGatewayMulticastDomainAssociations.of_xml)
          (Xml.child xml_arg0 "associations") in
      make ?associations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associations =
        field_map json__ "Associations"
          TransitGatewayMulticastDomainAssociations.of_json in
      make ?associations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a request to associate subnets with a transit gateway multicast domain."]
module AcceptTransitGatewayMulticastDomainAssociationsRequest =
  struct
    type nonrec t =
      {
      transitGatewayMulticastDomainId:
        TransitGatewayMulticastDomainId.t option
        [@ocaml.doc "The ID of the transit gateway multicast domain."];
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t option
        [@ocaml.doc "The ID of the transit gateway attachment."];
      subnetIds: ValueStringList.t option
        [@ocaml.doc
          "The IDs of the subnets to associate with the transit gateway multicast domain."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let make ?transitGatewayMulticastDomainId =
      fun ?transitGatewayAttachmentId ->
        fun ?subnetIds ->
          fun ?dryRun ->
            fun () ->
              {
                transitGatewayMulticastDomainId;
                transitGatewayAttachmentId;
                subnetIds;
                dryRun
              }
    let to_value x =
      structure_to_value
        [("TransitGatewayMulticastDomainId",
           (Option.map x.transitGatewayMulticastDomainId
              ~f:TransitGatewayMulticastDomainId.to_value));
        ("TransitGatewayAttachmentId",
          (Option.map x.transitGatewayAttachmentId
             ~f:TransitGatewayAttachmentId.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:ValueStringList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let subnetIds =
        (Option.map ~f:ValueStringList.of_xml)
          (Xml.child xml_arg0 "SubnetIds") in
      let transitGatewayAttachmentId =
        (Option.map ~f:TransitGatewayAttachmentId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayAttachmentId") in
      let transitGatewayMulticastDomainId =
        (Option.map ~f:TransitGatewayMulticastDomainId.of_xml)
          (Xml.child xml_arg0 "TransitGatewayMulticastDomainId") in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let subnetIds = field_map json__ "SubnetIds" ValueStringList.of_json in
      let transitGatewayAttachmentId =
        field_map json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      let transitGatewayMulticastDomainId =
        field_map json__ "TransitGatewayMulticastDomainId"
          TransitGatewayMulticastDomainId.of_json in
      make ?dryRun ?subnetIds ?transitGatewayAttachmentId
        ?transitGatewayMulticastDomainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a request to associate subnets with a transit gateway multicast domain."]
module AcceptTransitGatewayClientVpnAttachmentResult =
  struct
    type nonrec t =
      {
      transitGatewayClientVpnAttachment:
        TransitGatewayClientVpnAttachment.t option
        [@ocaml.doc
          "Information about the Transit Gateway Client VPN attachment."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?transitGatewayClientVpnAttachment =
      fun () -> { transitGatewayClientVpnAttachment }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transitGatewayClientVpnAttachment",
           (Option.map x.transitGatewayClientVpnAttachment
              ~f:TransitGatewayClientVpnAttachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transitGatewayClientVpnAttachment =
        (Option.map ~f:TransitGatewayClientVpnAttachment.of_xml)
          (Xml.child xml_arg0 "transitGatewayClientVpnAttachment") in
      make ?transitGatewayClientVpnAttachment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transitGatewayClientVpnAttachment =
        field_map json__ "TransitGatewayClientVpnAttachment"
          TransitGatewayClientVpnAttachment.of_json in
      make ?transitGatewayClientVpnAttachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a Transit Gateway attachment request for a Client VPN endpoint. The Transit Gateway owner must accept the attachment request before the Client VPN endpoint can route traffic through the Transit Gateway."]
module AcceptTransitGatewayClientVpnAttachmentRequest =
  struct
    type nonrec t =
      {
      transitGatewayAttachmentId: TransitGatewayAttachmentId.t
        [@ocaml.doc "The ID of the Transit Gateway attachment."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AcceptTransitGatewayClientVpnAttachmentRequest"
    let make ?dryRun =
      fun ~transitGatewayAttachmentId ->
        fun () -> { dryRun; transitGatewayAttachmentId }
    let to_value x =
      structure_to_value
        [("TransitGatewayAttachmentId",
           (Some
              (TransitGatewayAttachmentId.to_value
                 x.transitGatewayAttachmentId)));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let transitGatewayAttachmentId =
        TransitGatewayAttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TransitGatewayAttachmentId") in
      make ?dryRun ~transitGatewayAttachmentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let transitGatewayAttachmentId =
        field_map_exn json__ "TransitGatewayAttachmentId"
          TransitGatewayAttachmentId.of_json in
      make ?dryRun ~transitGatewayAttachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a Transit Gateway attachment request for a Client VPN endpoint. The Transit Gateway owner must accept the attachment request before the Client VPN endpoint can route traffic through the Transit Gateway."]
module AcceptReservedInstancesExchangeQuoteResult =
  struct
    type nonrec t =
      {
      exchangeId: String_.t option
        [@ocaml.doc "The ID of the successful exchange."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?exchangeId = fun () -> { exchangeId }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("exchangeId", (Option.map x.exchangeId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exchangeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "exchangeId") in
      make ?exchangeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let exchangeId = field_map json__ "ExchangeId" String_.of_json in
      make ?exchangeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of the exchange and whether it was successful."]
module AcceptReservedInstancesExchangeQuoteRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      reservedInstanceIds: ReservedInstanceIdSet.t
        [@ocaml.doc
          "The IDs of the Convertible Reserved Instances to exchange for another Convertible Reserved Instance of the same or higher value."];
      targetConfigurations: TargetConfigurationRequestSet.t option
        [@ocaml.doc
          "The configuration of the target Convertible Reserved Instance to exchange for your current Convertible Reserved Instances."]}
    let context_ = "AcceptReservedInstancesExchangeQuoteRequest"
    let make ?dryRun =
      fun ?targetConfigurations ->
        fun ~reservedInstanceIds ->
          fun () -> { dryRun; targetConfigurations; reservedInstanceIds }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("ReservedInstanceId",
          (Some (ReservedInstanceIdSet.to_value x.reservedInstanceIds)));
        ("TargetConfiguration",
          (Option.map x.targetConfigurations
             ~f:TargetConfigurationRequestSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetConfigurations =
        (Option.map ~f:TargetConfigurationRequestSet.of_xml)
          (Xml.child xml_arg0 "TargetConfiguration") in
      let reservedInstanceIds =
        ReservedInstanceIdSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservedInstanceId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ?targetConfigurations ~reservedInstanceIds ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetConfigurations =
        field_map json__ "TargetConfigurations"
          TargetConfigurationRequestSet.of_json in
      let reservedInstanceIds =
        field_map_exn json__ "ReservedInstanceIds"
          ReservedInstanceIdSet.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ?targetConfigurations ~reservedInstanceIds ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for accepting the quote."]
module AcceptCapacityReservationBillingOwnershipResult =
  struct
    type nonrec t =
      {
      return: Boolean.t option
        [@ocaml.doc
          "Returns true if the request succeeds; otherwise, it returns an error."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?return = fun () -> { return }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("return", (Option.map x.return ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let return =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "return") in
      make ?return ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let return = field_map json__ "Return" Boolean.of_json in
      make ?return ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a request to assign billing of the available capacity of a shared Capacity Reservation to your account. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations."]
module AcceptCapacityReservationBillingOwnershipRequest =
  struct
    type nonrec t =
      {
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."];
      capacityReservationId: CapacityReservationId.t
        [@ocaml.doc
          "The ID of the Capacity Reservation for which to accept the request."]}
    let context_ = "AcceptCapacityReservationBillingOwnershipRequest"
    let make ?dryRun =
      fun ~capacityReservationId ->
        fun () -> { dryRun; capacityReservationId }
    let to_value x =
      structure_to_value
        [("DryRun", (Option.map x.dryRun ~f:Boolean.to_value));
        ("CapacityReservationId",
          (Some (CapacityReservationId.to_value x.capacityReservationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationId =
        CapacityReservationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CapacityReservationId") in
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      make ~capacityReservationId ?dryRun ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityReservationId =
        field_map_exn json__ "CapacityReservationId"
          CapacityReservationId.of_json in
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      make ~capacityReservationId ?dryRun ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts a request to assign billing of the available capacity of a shared Capacity Reservation to your account. For more information, see Billing assignment for shared Amazon EC2 Capacity Reservations."]
module AcceptAddressTransferResult =
  struct
    type nonrec t =
      {
      addressTransfer: AddressTransfer.t option
        [@ocaml.doc "An Elastic IP address transfer."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?addressTransfer = fun () -> { addressTransfer }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("addressTransfer",
           (Option.map x.addressTransfer ~f:AddressTransfer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let addressTransfer =
        (Option.map ~f:AddressTransfer.of_xml)
          (Xml.child xml_arg0 "addressTransfer") in
      make ?addressTransfer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let addressTransfer =
        field_map json__ "AddressTransfer" AddressTransfer.of_json in
      make ?addressTransfer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts an Elastic IP address transfer. For more information, see Accept a transferred Elastic IP address in the Amazon VPC User Guide."]
module AcceptAddressTransferRequest =
  struct
    type nonrec t =
      {
      address: String_.t
        [@ocaml.doc "The Elastic IP address you are accepting for transfer."];
      tagSpecifications: TagSpecificationList.t option
        [@ocaml.doc
          "tag:<key> - The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is DryRunOperation. Otherwise, it is UnauthorizedOperation."]}
    let context_ = "AcceptAddressTransferRequest"
    let make ?tagSpecifications =
      fun ?dryRun ->
        fun ~address -> fun () -> { tagSpecifications; dryRun; address }
    let to_value x =
      structure_to_value
        [("Address", (Some (String_.to_value x.address)));
        ("TagSpecification",
          (Option.map x.tagSpecifications ~f:TagSpecificationList.to_value));
        ("DryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "DryRun") in
      let tagSpecifications =
        (Option.map ~f:TagSpecificationList.of_xml)
          (Xml.child xml_arg0 "TagSpecification") in
      let address =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Address") in
      make ?dryRun ?tagSpecifications ~address ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "DryRun" Boolean.of_json in
      let tagSpecifications =
        field_map json__ "TagSpecifications" TagSpecificationList.of_json in
      let address = field_map_exn json__ "Address" String_.of_json in
      make ?dryRun ?tagSpecifications ~address ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts an Elastic IP address transfer. For more information, see Accept a transferred Elastic IP address in the Amazon VPC User Guide."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let assign_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String"
       and layerIds =
         flag "layer-ids" (required json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assign_instance
           (Values.AssignInstanceRequest.make ~instanceId
              ~layerIds:(Values.Strings.of_json layerIds) ()) None None])
let assign_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and volumeId = flag "volume-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assign_volume
           (Values.AssignVolumeRequest.make ?instanceId ~volumeId ()) None
           None])
let associate_elastic_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and elasticIp =
         flag "elastic-ip" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_elastic_ip
           (Values.AssociateElasticIpRequest.make ?instanceId ~elasticIp ())
           None None])
let attach_elastic_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticLoadBalancerName =
         flag "elastic-load-balancer-name" (required string)
           ~doc:"STRING String"
       and layerId = flag "layer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_elastic_load_balancer
           (Values.AttachElasticLoadBalancerRequest.make
              ~elasticLoadBalancerName ~layerId ()) None None])
let clone_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and region = flag "region" (optional string) ~doc:"STRING String"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON StackAttributes"
       and defaultInstanceProfileArn =
         flag "default-instance-profile-arn" (optional string)
           ~doc:"STRING String"
       and defaultOs =
         flag "default-os" (optional string) ~doc:"STRING String"
       and hostnameTheme =
         flag "hostname-theme" (optional string) ~doc:"STRING String"
       and defaultAvailabilityZone =
         flag "default-availability-zone" (optional string)
           ~doc:"STRING String"
       and defaultSubnetId =
         flag "default-subnet-id" (optional string) ~doc:"STRING String"
       and customJson =
         flag "custom-json" (optional string) ~doc:"STRING String"
       and configurationManager =
         flag "configuration-manager" (optional json_arg)
           ~doc:"JSON StackConfigurationManager"
       and chefConfiguration =
         flag "chef-configuration" (optional json_arg)
           ~doc:"JSON ChefConfiguration"
       and useCustomCookbooks =
         flag "use-custom-cookbooks" (optional bool) ~doc:"BOOL Boolean"
       and useOpsworksSecurityGroups =
         flag "use-opsworks-security-groups" (optional bool)
           ~doc:"BOOL Boolean"
       and customCookbooksSource =
         flag "custom-cookbooks-source" (optional json_arg)
           ~doc:"JSON Source"
       and defaultSshKeyName =
         flag "default-ssh-key-name" (optional string) ~doc:"STRING String"
       and clonePermissions =
         flag "clone-permissions" (optional bool) ~doc:"BOOL Boolean"
       and cloneAppIds =
         flag "clone-app-ids" (optional json_arg) ~doc:"JSON Strings"
       and defaultRootDeviceType =
         flag "default-root-device-type" (optional json_arg)
           ~doc:"JSON RootDeviceType"
       and agentVersion =
         flag "agent-version" (optional string) ~doc:"STRING String"
       and sourceStackId =
         flag "source-stack-id" (required string) ~doc:"STRING String"
       and serviceRoleArn =
         flag "service-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.clone_stack
           (Values.CloneStackRequest.make ?name ?region ?vpcId
              ?attributes:(Option.map ~f:Values.StackAttributes.of_json
                             attributes) ?defaultInstanceProfileArn
              ?defaultOs ?hostnameTheme ?defaultAvailabilityZone
              ?defaultSubnetId ?customJson
              ?configurationManager:(Option.map
                                       ~f:Values.StackConfigurationManager.of_json
                                       configurationManager)
              ?chefConfiguration:(Option.map
                                    ~f:Values.ChefConfiguration.of_json
                                    chefConfiguration) ?useCustomCookbooks
              ?useOpsworksSecurityGroups
              ?customCookbooksSource:(Option.map ~f:Values.Source.of_json
                                        customCookbooksSource)
              ?defaultSshKeyName ?clonePermissions
              ?cloneAppIds:(Option.map ~f:Values.Strings.of_json cloneAppIds)
              ?defaultRootDeviceType:(Option.map
                                        ~f:Values.RootDeviceType.of_json
                                        defaultRootDeviceType) ?agentVersion
              ~sourceStackId ~serviceRoleArn ())
           (Some Values.CloneStackResult.to_json)
           (Some Values.CloneStackResult.error_to_json)])
let create_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shortname =
         flag "shortname" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and dataSources =
         flag "data-sources" (optional json_arg) ~doc:"JSON DataSources"
       and appSource =
         flag "app-source" (optional json_arg) ~doc:"JSON Source"
       and domains = flag "domains" (optional json_arg) ~doc:"JSON Strings"
       and enableSsl = flag "enable-ssl" (optional bool) ~doc:"BOOL Boolean"
       and sslConfiguration =
         flag "ssl-configuration" (optional json_arg)
           ~doc:"JSON SslConfiguration"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON AppAttributes"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON AppType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app
           (Values.CreateAppRequest.make ?shortname ?description
              ?dataSources:(Option.map ~f:Values.DataSources.of_json
                              dataSources)
              ?appSource:(Option.map ~f:Values.Source.of_json appSource)
              ?domains:(Option.map ~f:Values.Strings.of_json domains)
              ?enableSsl
              ?sslConfiguration:(Option.map
                                   ~f:Values.SslConfiguration.of_json
                                   sslConfiguration)
              ?attributes:(Option.map ~f:Values.AppAttributes.of_json
                             attributes)
              ?environment:(Option.map ~f:Values.EnvironmentVariables.of_json
                              environment) ~stackId ~name
              ~type_:(Values.AppType.of_json type_) ())
           (Some Values.CreateAppResult.to_json)
           (Some Values.CreateAppResult.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING String"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON Strings"
       and layerIds =
         flag "layer-ids" (optional json_arg) ~doc:"JSON Strings"
       and comment = flag "comment" (optional string) ~doc:"STRING String"
       and customJson =
         flag "custom-json" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String"
       and command =
         flag "command" (required json_arg) ~doc:"JSON DeploymentCommand" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make ?appId
              ?instanceIds:(Option.map ~f:Values.Strings.of_json instanceIds)
              ?layerIds:(Option.map ~f:Values.Strings.of_json layerIds)
              ?comment ?customJson ~stackId
              ~command:(Values.DeploymentCommand.of_json command) ())
           (Some Values.CreateDeploymentResult.to_json)
           (Some Values.CreateDeploymentResult.error_to_json)])
let create_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingType =
         flag "auto-scaling-type" (optional json_arg)
           ~doc:"JSON AutoScalingType"
       and hostname = flag "hostname" (optional string) ~doc:"STRING String"
       and os = flag "os" (optional string) ~doc:"STRING String"
       and amiId = flag "ami-id" (optional string) ~doc:"STRING String"
       and sshKeyName =
         flag "ssh-key-name" (optional string) ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and virtualizationType =
         flag "virtualization-type" (optional string) ~doc:"STRING String"
       and subnetId = flag "subnet-id" (optional string) ~doc:"STRING String"
       and architecture =
         flag "architecture" (optional json_arg) ~doc:"JSON Architecture"
       and rootDeviceType =
         flag "root-device-type" (optional json_arg)
           ~doc:"JSON RootDeviceType"
       and blockDeviceMappings =
         flag "block-device-mappings" (optional json_arg)
           ~doc:"JSON BlockDeviceMappings"
       and installUpdatesOnBoot =
         flag "install-updates-on-boot" (optional bool) ~doc:"BOOL Boolean"
       and ebsOptimized =
         flag "ebs-optimized" (optional bool) ~doc:"BOOL Boolean"
       and agentVersion =
         flag "agent-version" (optional string) ~doc:"STRING String"
       and tenancy = flag "tenancy" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String"
       and layerIds =
         flag "layer-ids" (required json_arg) ~doc:"JSON Strings"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance
           (Values.CreateInstanceRequest.make
              ?autoScalingType:(Option.map ~f:Values.AutoScalingType.of_json
                                  autoScalingType) ?hostname ?os ?amiId
              ?sshKeyName ?availabilityZone ?virtualizationType ?subnetId
              ?architecture:(Option.map ~f:Values.Architecture.of_json
                               architecture)
              ?rootDeviceType:(Option.map ~f:Values.RootDeviceType.of_json
                                 rootDeviceType)
              ?blockDeviceMappings:(Option.map
                                      ~f:Values.BlockDeviceMappings.of_json
                                      blockDeviceMappings)
              ?installUpdatesOnBoot ?ebsOptimized ?agentVersion ?tenancy
              ~stackId ~layerIds:(Values.Strings.of_json layerIds)
              ~instanceType ()) (Some Values.CreateInstanceResult.to_json)
           (Some Values.CreateInstanceResult.error_to_json)])
let create_layer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON LayerAttributes"
       and cloudWatchLogsConfiguration =
         flag "cloud-watch-logs-configuration" (optional json_arg)
           ~doc:"JSON CloudWatchLogsConfiguration"
       and customInstanceProfileArn =
         flag "custom-instance-profile-arn" (optional string)
           ~doc:"STRING String"
       and customJson =
         flag "custom-json" (optional string) ~doc:"STRING String"
       and customSecurityGroupIds =
         flag "custom-security-group-ids" (optional json_arg)
           ~doc:"JSON Strings"
       and packages = flag "packages" (optional json_arg) ~doc:"JSON Strings"
       and volumeConfigurations =
         flag "volume-configurations" (optional json_arg)
           ~doc:"JSON VolumeConfigurations"
       and enableAutoHealing =
         flag "enable-auto-healing" (optional bool) ~doc:"BOOL Boolean"
       and autoAssignElasticIps =
         flag "auto-assign-elastic-ips" (optional bool) ~doc:"BOOL Boolean"
       and autoAssignPublicIps =
         flag "auto-assign-public-ips" (optional bool) ~doc:"BOOL Boolean"
       and customRecipes =
         flag "custom-recipes" (optional json_arg) ~doc:"JSON Recipes"
       and installUpdatesOnBoot =
         flag "install-updates-on-boot" (optional bool) ~doc:"BOOL Boolean"
       and useEbsOptimizedInstances =
         flag "use-ebs-optimized-instances" (optional bool)
           ~doc:"BOOL Boolean"
       and lifecycleEventConfiguration =
         flag "lifecycle-event-configuration" (optional json_arg)
           ~doc:"JSON LifecycleEventConfiguration"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON LayerType"
       and name = flag "name" (required string) ~doc:"STRING String"
       and shortname =
         flag "shortname" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_layer
           (Values.CreateLayerRequest.make
              ?attributes:(Option.map ~f:Values.LayerAttributes.of_json
                             attributes)
              ?cloudWatchLogsConfiguration:(Option.map
                                              ~f:Values.CloudWatchLogsConfiguration.of_json
                                              cloudWatchLogsConfiguration)
              ?customInstanceProfileArn ?customJson
              ?customSecurityGroupIds:(Option.map ~f:Values.Strings.of_json
                                         customSecurityGroupIds)
              ?packages:(Option.map ~f:Values.Strings.of_json packages)
              ?volumeConfigurations:(Option.map
                                       ~f:Values.VolumeConfigurations.of_json
                                       volumeConfigurations)
              ?enableAutoHealing ?autoAssignElasticIps ?autoAssignPublicIps
              ?customRecipes:(Option.map ~f:Values.Recipes.of_json
                                customRecipes) ?installUpdatesOnBoot
              ?useEbsOptimizedInstances
              ?lifecycleEventConfiguration:(Option.map
                                              ~f:Values.LifecycleEventConfiguration.of_json
                                              lifecycleEventConfiguration)
              ~stackId ~type_:(Values.LayerType.of_json type_) ~name
              ~shortname ()) (Some Values.CreateLayerResult.to_json)
           (Some Values.CreateLayerResult.error_to_json)])
let create_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON StackAttributes"
       and defaultOs =
         flag "default-os" (optional string) ~doc:"STRING String"
       and hostnameTheme =
         flag "hostname-theme" (optional string) ~doc:"STRING String"
       and defaultAvailabilityZone =
         flag "default-availability-zone" (optional string)
           ~doc:"STRING String"
       and defaultSubnetId =
         flag "default-subnet-id" (optional string) ~doc:"STRING String"
       and customJson =
         flag "custom-json" (optional string) ~doc:"STRING String"
       and configurationManager =
         flag "configuration-manager" (optional json_arg)
           ~doc:"JSON StackConfigurationManager"
       and chefConfiguration =
         flag "chef-configuration" (optional json_arg)
           ~doc:"JSON ChefConfiguration"
       and useCustomCookbooks =
         flag "use-custom-cookbooks" (optional bool) ~doc:"BOOL Boolean"
       and useOpsworksSecurityGroups =
         flag "use-opsworks-security-groups" (optional bool)
           ~doc:"BOOL Boolean"
       and customCookbooksSource =
         flag "custom-cookbooks-source" (optional json_arg)
           ~doc:"JSON Source"
       and defaultSshKeyName =
         flag "default-ssh-key-name" (optional string) ~doc:"STRING String"
       and defaultRootDeviceType =
         flag "default-root-device-type" (optional json_arg)
           ~doc:"JSON RootDeviceType"
       and agentVersion =
         flag "agent-version" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String"
       and region = flag "region" (required string) ~doc:"STRING String"
       and serviceRoleArn =
         flag "service-role-arn" (required string) ~doc:"STRING String"
       and defaultInstanceProfileArn =
         flag "default-instance-profile-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stack
           (Values.CreateStackRequest.make ?vpcId
              ?attributes:(Option.map ~f:Values.StackAttributes.of_json
                             attributes) ?defaultOs ?hostnameTheme
              ?defaultAvailabilityZone ?defaultSubnetId ?customJson
              ?configurationManager:(Option.map
                                       ~f:Values.StackConfigurationManager.of_json
                                       configurationManager)
              ?chefConfiguration:(Option.map
                                    ~f:Values.ChefConfiguration.of_json
                                    chefConfiguration) ?useCustomCookbooks
              ?useOpsworksSecurityGroups
              ?customCookbooksSource:(Option.map ~f:Values.Source.of_json
                                        customCookbooksSource)
              ?defaultSshKeyName
              ?defaultRootDeviceType:(Option.map
                                        ~f:Values.RootDeviceType.of_json
                                        defaultRootDeviceType) ?agentVersion
              ~name ~region ~serviceRoleArn ~defaultInstanceProfileArn ())
           (Some Values.CreateStackResult.to_json)
           (Some Values.CreateStackResult.error_to_json)])
let create_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sshUsername =
         flag "ssh-username" (optional string) ~doc:"STRING String"
       and sshPublicKey =
         flag "ssh-public-key" (optional string) ~doc:"STRING String"
       and allowSelfManagement =
         flag "allow-self-management" (optional bool) ~doc:"BOOL Boolean"
       and iamUserArn =
         flag "iam-user-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_profile
           (Values.CreateUserProfileRequest.make ?sshUsername ?sshPublicKey
              ?allowSelfManagement ~iamUserArn ())
           (Some Values.CreateUserProfileResult.to_json)
           (Some Values.CreateUserProfileResult.error_to_json)])
let delete_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app (Values.DeleteAppRequest.make ~appId ()) None None])
let delete_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteElasticIp =
         flag "delete-elastic-ip" (optional bool) ~doc:"BOOL Boolean"
       and deleteVolumes =
         flag "delete-volumes" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance
           (Values.DeleteInstanceRequest.make ?deleteElasticIp ?deleteVolumes
              ~instanceId ()) None None])
let delete_layer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerId = flag "layer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_layer (Values.DeleteLayerRequest.make ~layerId ()) None
           None])
let delete_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stack (Values.DeleteStackRequest.make ~stackId ()) None
           None])
let delete_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iamUserArn =
         flag "iam-user-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_profile
           (Values.DeleteUserProfileRequest.make ~iamUserArn ()) None None])
let deregister_ecs_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ecsClusterArn =
         flag "ecs-cluster-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_ecs_cluster
           (Values.DeregisterEcsClusterRequest.make ~ecsClusterArn ()) None
           None])
let deregister_elastic_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticIp =
         flag "elastic-ip" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_elastic_ip
           (Values.DeregisterElasticIpRequest.make ~elasticIp ()) None None])
let deregister_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_instance
           (Values.DeregisterInstanceRequest.make ~instanceId ()) None None])
let deregister_rds_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rdsDbInstanceArn =
         flag "rds-db-instance-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_rds_db_instance
           (Values.DeregisterRdsDbInstanceRequest.make ~rdsDbInstanceArn ())
           None None])
let deregister_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeId = flag "volume-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_volume
           (Values.DeregisterVolumeRequest.make ~volumeId ()) None None])
let describe_agent_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and configurationManager =
         flag "configuration-manager" (optional json_arg)
           ~doc:"JSON StackConfigurationManager" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_agent_versions
           (Values.DescribeAgentVersionsRequest.make ?stackId
              ?configurationManager:(Option.map
                                       ~f:Values.StackConfigurationManager.of_json
                                       configurationManager) ())
           (Some Values.DescribeAgentVersionsResult.to_json)
           (Some Values.DescribeAgentVersionsResult.error_to_json)])
let describe_apps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and appIds = flag "app-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_apps
           (Values.DescribeAppsRequest.make ?stackId
              ?appIds:(Option.map ~f:Values.Strings.of_json appIds) ())
           (Some Values.DescribeAppsResult.to_json)
           (Some Values.DescribeAppsResult.error_to_json)])
let describe_commands =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and commandIds =
         flag "command-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_commands
           (Values.DescribeCommandsRequest.make ?deploymentId ?instanceId
              ?commandIds:(Option.map ~f:Values.Strings.of_json commandIds)
              ()) (Some Values.DescribeCommandsResult.to_json)
           (Some Values.DescribeCommandsResult.error_to_json)])
let describe_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (optional string) ~doc:"STRING String"
       and deploymentIds =
         flag "deployment-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_deployments
           (Values.DescribeDeploymentsRequest.make ?stackId ?appId
              ?deploymentIds:(Option.map ~f:Values.Strings.of_json
                                deploymentIds) ())
           (Some Values.DescribeDeploymentsResult.to_json)
           (Some Values.DescribeDeploymentsResult.error_to_json)])
let describe_ecs_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ecsClusterArns =
         flag "ecs-cluster-arns" (optional json_arg) ~doc:"JSON Strings"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ecs_clusters
           (Values.DescribeEcsClustersRequest.make
              ?ecsClusterArns:(Option.map ~f:Values.Strings.of_json
                                 ecsClusterArns) ?stackId ?nextToken
              ?maxResults ()) (Some Values.DescribeEcsClustersResult.to_json)
           (Some Values.DescribeEcsClustersResult.error_to_json)])
let describe_elastic_ips =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and ips = flag "ips" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elastic_ips
           (Values.DescribeElasticIpsRequest.make ?instanceId ?stackId
              ?ips:(Option.map ~f:Values.Strings.of_json ips) ())
           (Some Values.DescribeElasticIpsResult.to_json)
           (Some Values.DescribeElasticIpsResult.error_to_json)])
let describe_elastic_load_balancers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and layerIds =
         flag "layer-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elastic_load_balancers
           (Values.DescribeElasticLoadBalancersRequest.make ?stackId
              ?layerIds:(Option.map ~f:Values.Strings.of_json layerIds) ())
           (Some Values.DescribeElasticLoadBalancersResult.to_json)
           (Some Values.DescribeElasticLoadBalancersResult.error_to_json)])
let describe_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and layerId = flag "layer-id" (optional string) ~doc:"STRING String"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instances
           (Values.DescribeInstancesRequest.make ?stackId ?layerId
              ?instanceIds:(Option.map ~f:Values.Strings.of_json instanceIds)
              ()) (Some Values.DescribeInstancesResult.to_json)
           (Some Values.DescribeInstancesResult.error_to_json)])
let describe_layers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and layerIds =
         flag "layer-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_layers
           (Values.DescribeLayersRequest.make ?stackId
              ?layerIds:(Option.map ~f:Values.Strings.of_json layerIds) ())
           (Some Values.DescribeLayersResult.to_json)
           (Some Values.DescribeLayersResult.error_to_json)])
let describe_load_based_auto_scaling =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerIds =
         flag "layer-ids" (required json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_load_based_auto_scaling
           (Values.DescribeLoadBasedAutoScalingRequest.make
              ~layerIds:(Values.Strings.of_json layerIds) ())
           (Some Values.DescribeLoadBasedAutoScalingResult.to_json)
           (Some Values.DescribeLoadBasedAutoScalingResult.error_to_json)])
let describe_my_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_my_user_profile (Fn.id ())
           (Some Values.DescribeMyUserProfileResult.to_json)
           (Some Values.DescribeMyUserProfileResult.error_to_json)])
let describe_operating_systems =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_operating_systems (Fn.id ())
           (Some Values.DescribeOperatingSystemsResponse.to_json)
           (Some Values.DescribeOperatingSystemsResponse.error_to_json)])
let describe_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iamUserArn =
         flag "iam-user-arn" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_permissions
           (Values.DescribePermissionsRequest.make ?iamUserArn ?stackId ())
           (Some Values.DescribePermissionsResult.to_json)
           (Some Values.DescribePermissionsResult.error_to_json)])
let describe_raid_arrays =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and raidArrayIds =
         flag "raid-array-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_raid_arrays
           (Values.DescribeRaidArraysRequest.make ?instanceId ?stackId
              ?raidArrayIds:(Option.map ~f:Values.Strings.of_json
                               raidArrayIds) ())
           (Some Values.DescribeRaidArraysResult.to_json)
           (Some Values.DescribeRaidArraysResult.error_to_json)])
let describe_rds_db_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rdsDbInstanceArns =
         flag "rds-db-instance-arns" (optional json_arg) ~doc:"JSON Strings"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rds_db_instances
           (Values.DescribeRdsDbInstancesRequest.make
              ?rdsDbInstanceArns:(Option.map ~f:Values.Strings.of_json
                                    rdsDbInstanceArns) ~stackId ())
           (Some Values.DescribeRdsDbInstancesResult.to_json)
           (Some Values.DescribeRdsDbInstancesResult.error_to_json)])
let describe_service_errors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and serviceErrorIds =
         flag "service-error-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service_errors
           (Values.DescribeServiceErrorsRequest.make ?stackId ?instanceId
              ?serviceErrorIds:(Option.map ~f:Values.Strings.of_json
                                  serviceErrorIds) ())
           (Some Values.DescribeServiceErrorsResult.to_json)
           (Some Values.DescribeServiceErrorsResult.error_to_json)])
let describe_stack_provisioning_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_provisioning_parameters
           (Values.DescribeStackProvisioningParametersRequest.make ~stackId
              ())
           (Some Values.DescribeStackProvisioningParametersResult.to_json)
           (Some
              Values.DescribeStackProvisioningParametersResult.error_to_json)])
let describe_stack_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_summary
           (Values.DescribeStackSummaryRequest.make ~stackId ())
           (Some Values.DescribeStackSummaryResult.to_json)
           (Some Values.DescribeStackSummaryResult.error_to_json)])
let describe_stacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackIds =
         flag "stack-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stacks
           (Values.DescribeStacksRequest.make
              ?stackIds:(Option.map ~f:Values.Strings.of_json stackIds) ())
           (Some Values.DescribeStacksResult.to_json)
           (Some Values.DescribeStacksResult.error_to_json)])
let describe_time_based_auto_scaling =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (required json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_time_based_auto_scaling
           (Values.DescribeTimeBasedAutoScalingRequest.make
              ~instanceIds:(Values.Strings.of_json instanceIds) ())
           (Some Values.DescribeTimeBasedAutoScalingResult.to_json)
           (Some Values.DescribeTimeBasedAutoScalingResult.error_to_json)])
let describe_user_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iamUserArns =
         flag "iam-user-arns" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_profiles
           (Values.DescribeUserProfilesRequest.make
              ?iamUserArns:(Option.map ~f:Values.Strings.of_json iamUserArns)
              ()) (Some Values.DescribeUserProfilesResult.to_json)
           (Some Values.DescribeUserProfilesResult.error_to_json)])
let describe_volumes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING String"
       and raidArrayId =
         flag "raid-array-id" (optional string) ~doc:"STRING String"
       and volumeIds =
         flag "volume-ids" (optional json_arg) ~doc:"JSON Strings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_volumes
           (Values.DescribeVolumesRequest.make ?instanceId ?stackId
              ?raidArrayId
              ?volumeIds:(Option.map ~f:Values.Strings.of_json volumeIds) ())
           (Some Values.DescribeVolumesResult.to_json)
           (Some Values.DescribeVolumesResult.error_to_json)])
let detach_elastic_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticLoadBalancerName =
         flag "elastic-load-balancer-name" (required string)
           ~doc:"STRING String"
       and layerId = flag "layer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_elastic_load_balancer
           (Values.DetachElasticLoadBalancerRequest.make
              ~elasticLoadBalancerName ~layerId ()) None None])
let disassociate_elastic_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticIp =
         flag "elastic-ip" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_elastic_ip
           (Values.DisassociateElasticIpRequest.make ~elasticIp ()) None None])
let get_hostname_suggestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerId = flag "layer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_hostname_suggestion
           (Values.GetHostnameSuggestionRequest.make ~layerId ())
           (Some Values.GetHostnameSuggestionResult.to_json)
           (Some Values.GetHostnameSuggestionResult.error_to_json)])
let grant_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and validForInMinutes =
         flag "valid-for-in-minutes" (optional int)
           ~doc:"INT ValidForInMinutes"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.grant_access
           (Values.GrantAccessRequest.make ?validForInMinutes ~instanceId ())
           (Some Values.GrantAccessResult.to_json)
           (Some Values.GrantAccessResult.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags
           (Values.ListTagsRequest.make ?maxResults ?nextToken ~resourceArn
              ()) (Some Values.ListTagsResult.to_json)
           (Some Values.ListTagsResult.error_to_json)])
let reboot_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_instance
           (Values.RebootInstanceRequest.make ~instanceId ()) None None])
let register_ecs_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ecsClusterArn =
         flag "ecs-cluster-arn" (required string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_ecs_cluster
           (Values.RegisterEcsClusterRequest.make ~ecsClusterArn ~stackId ())
           (Some Values.RegisterEcsClusterResult.to_json)
           (Some Values.RegisterEcsClusterResult.error_to_json)])
let register_elastic_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticIp =
         flag "elastic-ip" (required string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_elastic_ip
           (Values.RegisterElasticIpRequest.make ~elasticIp ~stackId ())
           (Some Values.RegisterElasticIpResult.to_json)
           (Some Values.RegisterElasticIpResult.error_to_json)])
let register_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostname = flag "hostname" (optional string) ~doc:"STRING String"
       and publicIp = flag "public-ip" (optional string) ~doc:"STRING String"
       and privateIp =
         flag "private-ip" (optional string) ~doc:"STRING String"
       and rsaPublicKey =
         flag "rsa-public-key" (optional string) ~doc:"STRING String"
       and rsaPublicKeyFingerprint =
         flag "rsa-public-key-fingerprint" (optional string)
           ~doc:"STRING String"
       and instanceIdentity =
         flag "instance-identity" (optional json_arg)
           ~doc:"JSON InstanceIdentity"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_instance
           (Values.RegisterInstanceRequest.make ?hostname ?publicIp
              ?privateIp ?rsaPublicKey ?rsaPublicKeyFingerprint
              ?instanceIdentity:(Option.map
                                   ~f:Values.InstanceIdentity.of_json
                                   instanceIdentity) ~stackId ())
           (Some Values.RegisterInstanceResult.to_json)
           (Some Values.RegisterInstanceResult.error_to_json)])
let register_rds_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String"
       and rdsDbInstanceArn =
         flag "rds-db-instance-arn" (required string) ~doc:"STRING String"
       and dbUser = flag "db-user" (required string) ~doc:"STRING String"
       and dbPassword =
         flag "db-password" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_rds_db_instance
           (Values.RegisterRdsDbInstanceRequest.make ~stackId
              ~rdsDbInstanceArn ~dbUser ~dbPassword ()) None None])
let register_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ec2VolumeId =
         flag "ec2-volume-id" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_volume
           (Values.RegisterVolumeRequest.make ?ec2VolumeId ~stackId ())
           (Some Values.RegisterVolumeResult.to_json)
           (Some Values.RegisterVolumeResult.error_to_json)])
let set_load_based_auto_scaling =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enable = flag "enable" (optional bool) ~doc:"BOOL Boolean"
       and upScaling =
         flag "up-scaling" (optional json_arg)
           ~doc:"JSON AutoScalingThresholds"
       and downScaling =
         flag "down-scaling" (optional json_arg)
           ~doc:"JSON AutoScalingThresholds"
       and layerId = flag "layer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_load_based_auto_scaling
           (Values.SetLoadBasedAutoScalingRequest.make ?enable
              ?upScaling:(Option.map ~f:Values.AutoScalingThresholds.of_json
                            upScaling)
              ?downScaling:(Option.map
                              ~f:Values.AutoScalingThresholds.of_json
                              downScaling) ~layerId ()) None None])
let set_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowSsh = flag "allow-ssh" (optional bool) ~doc:"BOOL Boolean"
       and allowSudo = flag "allow-sudo" (optional bool) ~doc:"BOOL Boolean"
       and level = flag "level" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String"
       and iamUserArn =
         flag "iam-user-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_permission
           (Values.SetPermissionRequest.make ?allowSsh ?allowSudo ?level
              ~stackId ~iamUserArn ()) None None])
let set_time_based_auto_scaling =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingSchedule =
         flag "auto-scaling-schedule" (optional json_arg)
           ~doc:"JSON WeeklyAutoScalingSchedule"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_time_based_auto_scaling
           (Values.SetTimeBasedAutoScalingRequest.make
              ?autoScalingSchedule:(Option.map
                                      ~f:Values.WeeklyAutoScalingSchedule.of_json
                                      autoScalingSchedule) ~instanceId ())
           None None])
let start_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_instance
           (Values.StartInstanceRequest.make ~instanceId ()) None None])
let start_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_stack (Values.StartStackRequest.make ~stackId ()) None
           None])
let stop_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_instance
           (Values.StopInstanceRequest.make ?force ~instanceId ()) None None])
let stop_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_stack (Values.StopStackRequest.make ~stackId ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ()) None None])
let unassign_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unassign_instance
           (Values.UnassignInstanceRequest.make ~instanceId ()) None None])
let unassign_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeId = flag "volume-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unassign_volume
           (Values.UnassignVolumeRequest.make ~volumeId ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and dataSources =
         flag "data-sources" (optional json_arg) ~doc:"JSON DataSources"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON AppType"
       and appSource =
         flag "app-source" (optional json_arg) ~doc:"JSON Source"
       and domains = flag "domains" (optional json_arg) ~doc:"JSON Strings"
       and enableSsl = flag "enable-ssl" (optional bool) ~doc:"BOOL Boolean"
       and sslConfiguration =
         flag "ssl-configuration" (optional json_arg)
           ~doc:"JSON SslConfiguration"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON AppAttributes"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and appId = flag "app-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app
           (Values.UpdateAppRequest.make ?name ?description
              ?dataSources:(Option.map ~f:Values.DataSources.of_json
                              dataSources)
              ?type_:(Option.map ~f:Values.AppType.of_json type_)
              ?appSource:(Option.map ~f:Values.Source.of_json appSource)
              ?domains:(Option.map ~f:Values.Strings.of_json domains)
              ?enableSsl
              ?sslConfiguration:(Option.map
                                   ~f:Values.SslConfiguration.of_json
                                   sslConfiguration)
              ?attributes:(Option.map ~f:Values.AppAttributes.of_json
                             attributes)
              ?environment:(Option.map ~f:Values.EnvironmentVariables.of_json
                              environment) ~appId ()) None None])
let update_elastic_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and elasticIp =
         flag "elastic-ip" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_elastic_ip
           (Values.UpdateElasticIpRequest.make ?name ~elasticIp ()) None None])
let update_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerIds =
         flag "layer-ids" (optional json_arg) ~doc:"JSON Strings"
       and instanceType =
         flag "instance-type" (optional string) ~doc:"STRING String"
       and autoScalingType =
         flag "auto-scaling-type" (optional json_arg)
           ~doc:"JSON AutoScalingType"
       and hostname = flag "hostname" (optional string) ~doc:"STRING String"
       and os = flag "os" (optional string) ~doc:"STRING String"
       and amiId = flag "ami-id" (optional string) ~doc:"STRING String"
       and sshKeyName =
         flag "ssh-key-name" (optional string) ~doc:"STRING String"
       and architecture =
         flag "architecture" (optional json_arg) ~doc:"JSON Architecture"
       and installUpdatesOnBoot =
         flag "install-updates-on-boot" (optional bool) ~doc:"BOOL Boolean"
       and ebsOptimized =
         flag "ebs-optimized" (optional bool) ~doc:"BOOL Boolean"
       and agentVersion =
         flag "agent-version" (optional string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance
           (Values.UpdateInstanceRequest.make
              ?layerIds:(Option.map ~f:Values.Strings.of_json layerIds)
              ?instanceType
              ?autoScalingType:(Option.map ~f:Values.AutoScalingType.of_json
                                  autoScalingType) ?hostname ?os ?amiId
              ?sshKeyName
              ?architecture:(Option.map ~f:Values.Architecture.of_json
                               architecture) ?installUpdatesOnBoot
              ?ebsOptimized ?agentVersion ~instanceId ()) None None])
let update_layer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and shortname =
         flag "shortname" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON LayerAttributes"
       and cloudWatchLogsConfiguration =
         flag "cloud-watch-logs-configuration" (optional json_arg)
           ~doc:"JSON CloudWatchLogsConfiguration"
       and customInstanceProfileArn =
         flag "custom-instance-profile-arn" (optional string)
           ~doc:"STRING String"
       and customJson =
         flag "custom-json" (optional string) ~doc:"STRING String"
       and customSecurityGroupIds =
         flag "custom-security-group-ids" (optional json_arg)
           ~doc:"JSON Strings"
       and packages = flag "packages" (optional json_arg) ~doc:"JSON Strings"
       and volumeConfigurations =
         flag "volume-configurations" (optional json_arg)
           ~doc:"JSON VolumeConfigurations"
       and enableAutoHealing =
         flag "enable-auto-healing" (optional bool) ~doc:"BOOL Boolean"
       and autoAssignElasticIps =
         flag "auto-assign-elastic-ips" (optional bool) ~doc:"BOOL Boolean"
       and autoAssignPublicIps =
         flag "auto-assign-public-ips" (optional bool) ~doc:"BOOL Boolean"
       and customRecipes =
         flag "custom-recipes" (optional json_arg) ~doc:"JSON Recipes"
       and installUpdatesOnBoot =
         flag "install-updates-on-boot" (optional bool) ~doc:"BOOL Boolean"
       and useEbsOptimizedInstances =
         flag "use-ebs-optimized-instances" (optional bool)
           ~doc:"BOOL Boolean"
       and lifecycleEventConfiguration =
         flag "lifecycle-event-configuration" (optional json_arg)
           ~doc:"JSON LifecycleEventConfiguration"
       and layerId = flag "layer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_layer
           (Values.UpdateLayerRequest.make ?name ?shortname
              ?attributes:(Option.map ~f:Values.LayerAttributes.of_json
                             attributes)
              ?cloudWatchLogsConfiguration:(Option.map
                                              ~f:Values.CloudWatchLogsConfiguration.of_json
                                              cloudWatchLogsConfiguration)
              ?customInstanceProfileArn ?customJson
              ?customSecurityGroupIds:(Option.map ~f:Values.Strings.of_json
                                         customSecurityGroupIds)
              ?packages:(Option.map ~f:Values.Strings.of_json packages)
              ?volumeConfigurations:(Option.map
                                       ~f:Values.VolumeConfigurations.of_json
                                       volumeConfigurations)
              ?enableAutoHealing ?autoAssignElasticIps ?autoAssignPublicIps
              ?customRecipes:(Option.map ~f:Values.Recipes.of_json
                                customRecipes) ?installUpdatesOnBoot
              ?useEbsOptimizedInstances
              ?lifecycleEventConfiguration:(Option.map
                                              ~f:Values.LifecycleEventConfiguration.of_json
                                              lifecycleEventConfiguration)
              ~layerId ()) None None])
let update_my_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sshPublicKey =
         flag "ssh-public-key" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_my_user_profile
           (Values.UpdateMyUserProfileRequest.make ?sshPublicKey ()) None
           None])
let update_rds_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dbUser = flag "db-user" (optional string) ~doc:"STRING String"
       and dbPassword =
         flag "db-password" (optional string) ~doc:"STRING String"
       and rdsDbInstanceArn =
         flag "rds-db-instance-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rds_db_instance
           (Values.UpdateRdsDbInstanceRequest.make ?dbUser ?dbPassword
              ~rdsDbInstanceArn ()) None None])
let update_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON StackAttributes"
       and serviceRoleArn =
         flag "service-role-arn" (optional string) ~doc:"STRING String"
       and defaultInstanceProfileArn =
         flag "default-instance-profile-arn" (optional string)
           ~doc:"STRING String"
       and defaultOs =
         flag "default-os" (optional string) ~doc:"STRING String"
       and hostnameTheme =
         flag "hostname-theme" (optional string) ~doc:"STRING String"
       and defaultAvailabilityZone =
         flag "default-availability-zone" (optional string)
           ~doc:"STRING String"
       and defaultSubnetId =
         flag "default-subnet-id" (optional string) ~doc:"STRING String"
       and customJson =
         flag "custom-json" (optional string) ~doc:"STRING String"
       and configurationManager =
         flag "configuration-manager" (optional json_arg)
           ~doc:"JSON StackConfigurationManager"
       and chefConfiguration =
         flag "chef-configuration" (optional json_arg)
           ~doc:"JSON ChefConfiguration"
       and useCustomCookbooks =
         flag "use-custom-cookbooks" (optional bool) ~doc:"BOOL Boolean"
       and customCookbooksSource =
         flag "custom-cookbooks-source" (optional json_arg)
           ~doc:"JSON Source"
       and defaultSshKeyName =
         flag "default-ssh-key-name" (optional string) ~doc:"STRING String"
       and defaultRootDeviceType =
         flag "default-root-device-type" (optional json_arg)
           ~doc:"JSON RootDeviceType"
       and useOpsworksSecurityGroups =
         flag "use-opsworks-security-groups" (optional bool)
           ~doc:"BOOL Boolean"
       and agentVersion =
         flag "agent-version" (optional string) ~doc:"STRING String"
       and stackId = flag "stack-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stack
           (Values.UpdateStackRequest.make ?name
              ?attributes:(Option.map ~f:Values.StackAttributes.of_json
                             attributes) ?serviceRoleArn
              ?defaultInstanceProfileArn ?defaultOs ?hostnameTheme
              ?defaultAvailabilityZone ?defaultSubnetId ?customJson
              ?configurationManager:(Option.map
                                       ~f:Values.StackConfigurationManager.of_json
                                       configurationManager)
              ?chefConfiguration:(Option.map
                                    ~f:Values.ChefConfiguration.of_json
                                    chefConfiguration) ?useCustomCookbooks
              ?customCookbooksSource:(Option.map ~f:Values.Source.of_json
                                        customCookbooksSource)
              ?defaultSshKeyName
              ?defaultRootDeviceType:(Option.map
                                        ~f:Values.RootDeviceType.of_json
                                        defaultRootDeviceType)
              ?useOpsworksSecurityGroups ?agentVersion ~stackId ()) None None])
let update_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sshUsername =
         flag "ssh-username" (optional string) ~doc:"STRING String"
       and sshPublicKey =
         flag "ssh-public-key" (optional string) ~doc:"STRING String"
       and allowSelfManagement =
         flag "allow-self-management" (optional bool) ~doc:"BOOL Boolean"
       and iamUserArn =
         flag "iam-user-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_profile
           (Values.UpdateUserProfileRequest.make ?sshUsername ?sshPublicKey
              ?allowSelfManagement ~iamUserArn ()) None None])
let update_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and mountPoint =
         flag "mount-point" (optional string) ~doc:"STRING String"
       and volumeId = flag "volume-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_volume
           (Values.UpdateVolumeRequest.make ?name ?mountPoint ~volumeId ())
           None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("assign-instance", assign_instance);
    ("assign-volume", assign_volume);
    ("associate-elastic-ip", associate_elastic_ip);
    ("attach-elastic-load-balancer", attach_elastic_load_balancer);
    ("clone-stack", clone_stack);
    ("create-app", create_app);
    ("create-deployment", create_deployment);
    ("create-instance", create_instance);
    ("create-layer", create_layer);
    ("create-stack", create_stack);
    ("create-user-profile", create_user_profile);
    ("delete-app", delete_app);
    ("delete-instance", delete_instance);
    ("delete-layer", delete_layer);
    ("delete-stack", delete_stack);
    ("delete-user-profile", delete_user_profile);
    ("deregister-ecs-cluster", deregister_ecs_cluster);
    ("deregister-elastic-ip", deregister_elastic_ip);
    ("deregister-instance", deregister_instance);
    ("deregister-rds-db-instance", deregister_rds_db_instance);
    ("deregister-volume", deregister_volume);
    ("describe-agent-versions", describe_agent_versions);
    ("describe-apps", describe_apps);
    ("describe-commands", describe_commands);
    ("describe-deployments", describe_deployments);
    ("describe-ecs-clusters", describe_ecs_clusters);
    ("describe-elastic-ips", describe_elastic_ips);
    ("describe-elastic-load-balancers", describe_elastic_load_balancers);
    ("describe-instances", describe_instances);
    ("describe-layers", describe_layers);
    ("describe-load-based-auto-scaling", describe_load_based_auto_scaling);
    ("describe-my-user-profile", describe_my_user_profile);
    ("describe-operating-systems", describe_operating_systems);
    ("describe-permissions", describe_permissions);
    ("describe-raid-arrays", describe_raid_arrays);
    ("describe-rds-db-instances", describe_rds_db_instances);
    ("describe-service-errors", describe_service_errors);
    ("describe-stack-provisioning-parameters",
      describe_stack_provisioning_parameters);
    ("describe-stack-summary", describe_stack_summary);
    ("describe-stacks", describe_stacks);
    ("describe-time-based-auto-scaling", describe_time_based_auto_scaling);
    ("describe-user-profiles", describe_user_profiles);
    ("describe-volumes", describe_volumes);
    ("detach-elastic-load-balancer", detach_elastic_load_balancer);
    ("disassociate-elastic-ip", disassociate_elastic_ip);
    ("get-hostname-suggestion", get_hostname_suggestion);
    ("grant-access", grant_access);
    ("list-tags", list_tags);
    ("reboot-instance", reboot_instance);
    ("register-ecs-cluster", register_ecs_cluster);
    ("register-elastic-ip", register_elastic_ip);
    ("register-instance", register_instance);
    ("register-rds-db-instance", register_rds_db_instance);
    ("register-volume", register_volume);
    ("set-load-based-auto-scaling", set_load_based_auto_scaling);
    ("set-permission", set_permission);
    ("set-time-based-auto-scaling", set_time_based_auto_scaling);
    ("start-instance", start_instance);
    ("start-stack", start_stack);
    ("stop-instance", stop_instance);
    ("stop-stack", stop_stack);
    ("tag-resource", tag_resource);
    ("unassign-instance", unassign_instance);
    ("unassign-volume", unassign_volume);
    ("untag-resource", untag_resource);
    ("update-app", update_app);
    ("update-elastic-ip", update_elastic_ip);
    ("update-instance", update_instance);
    ("update-layer", update_layer);
    ("update-my-user-profile", update_my_user_profile);
    ("update-rds-db-instance", update_rds_db_instance);
    ("update-stack", update_stack);
    ("update-user-profile", update_user_profile);
    ("update-volume", update_volume)]
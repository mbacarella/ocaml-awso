(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and username = flag "username" (required string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String"
       and identityProvider =
         flag "identity-provider" (required json_arg)
           ~doc:"JSON IdentityProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_user
           (Values.AssociateUserRequest.make ?domain
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~username
              ~instanceId
              ~identityProvider:(Values.IdentityProvider.of_json
                                   identityProvider) ())
           (Some Values.AssociateUserResponse.to_json)
           (Some Values.AssociateUserResponse.error_to_json)])
let create_license_server_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and identityProviderArn =
         flag "identity-provider-arn" (required string) ~doc:"STRING Arn"
       and licenseServerSettings =
         flag "license-server-settings" (required json_arg)
           ~doc:"JSON LicenseServerSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license_server_endpoint
           (Values.CreateLicenseServerEndpointRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~identityProviderArn
              ~licenseServerSettings:(Values.LicenseServerSettings.of_json
                                        licenseServerSettings) ())
           (Some Values.CreateLicenseServerEndpointResponse.to_json)
           (Some Values.CreateLicenseServerEndpointResponse.error_to_json)])
let delete_license_server_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseServerEndpointArn =
         flag "license-server-endpoint-arn" (required string)
           ~doc:"STRING Arn"
       and serverType =
         flag "server-type" (required json_arg) ~doc:"JSON ServerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_license_server_endpoint
           (Values.DeleteLicenseServerEndpointRequest.make
              ~licenseServerEndpointArn
              ~serverType:(Values.ServerType.of_json serverType) ())
           (Some Values.DeleteLicenseServerEndpointResponse.to_json)
           (Some Values.DeleteLicenseServerEndpointResponse.error_to_json)])
let deregister_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityProvider =
         flag "identity-provider" (optional json_arg)
           ~doc:"JSON IdentityProvider"
       and product = flag "product" (optional string) ~doc:"STRING String"
       and identityProviderArn =
         flag "identity-provider-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_identity_provider
           (Values.DeregisterIdentityProviderRequest.make
              ?identityProvider:(Option.map
                                   ~f:Values.IdentityProvider.of_json
                                   identityProvider) ?product
              ?identityProviderArn ())
           (Some Values.DeregisterIdentityProviderResponse.to_json)
           (Some Values.DeregisterIdentityProviderResponse.error_to_json)])
let disassociate_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username = flag "username" (optional string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING String"
       and identityProvider =
         flag "identity-provider" (optional json_arg)
           ~doc:"JSON IdentityProvider"
       and instanceUserArn =
         flag "instance-user-arn" (optional string) ~doc:"STRING Arn"
       and domain = flag "domain" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_user
           (Values.DisassociateUserRequest.make ?username ?instanceId
              ?identityProvider:(Option.map
                                   ~f:Values.IdentityProvider.of_json
                                   identityProvider) ?instanceUserArn ?domain
              ()) (Some Values.DisassociateUserResponse.to_json)
           (Some Values.DisassociateUserResponse.error_to_json)])
let list_identity_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_providers
           (Values.ListIdentityProvidersRequest.make ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ())
           (Some Values.ListIdentityProvidersResponse.to_json)
           (Some Values.ListIdentityProvidersResponse.error_to_json)])
let list_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instances
           (Values.ListInstancesRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.ListInstancesResponse.to_json)
           (Some Values.ListInstancesResponse.error_to_json)])
let list_license_server_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListLicenseServerEndpointsRequestMaxResultsInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_server_endpoints
           (Values.ListLicenseServerEndpointsRequest.make ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ())
           (Some Values.ListLicenseServerEndpointsResponse.to_json)
           (Some Values.ListLicenseServerEndpointsResponse.error_to_json)])
let list_product_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and product = flag "product" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and identityProvider =
         flag "identity-provider" (required json_arg)
           ~doc:"JSON IdentityProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_product_subscriptions
           (Values.ListProductSubscriptionsRequest.make ?product ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken
              ~identityProvider:(Values.IdentityProvider.of_json
                                   identityProvider) ())
           (Some Values.ListProductSubscriptionsResponse.to_json)
           (Some Values.ListProductSubscriptionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_user_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING String"
       and identityProvider =
         flag "identity-provider" (required json_arg)
           ~doc:"JSON IdentityProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_associations
           (Values.ListUserAssociationsRequest.make ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ~instanceId
              ~identityProvider:(Values.IdentityProvider.of_json
                                   identityProvider) ())
           (Some Values.ListUserAssociationsResponse.to_json)
           (Some Values.ListUserAssociationsResponse.error_to_json)])
let register_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON Settings"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and identityProvider =
         flag "identity-provider" (required json_arg)
           ~doc:"JSON IdentityProvider"
       and product = flag "product" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_identity_provider
           (Values.RegisterIdentityProviderRequest.make
              ?settings:(Option.map ~f:Values.Settings.of_json settings)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~identityProvider:(Values.IdentityProvider.of_json
                                   identityProvider) ~product ())
           (Some Values.RegisterIdentityProviderResponse.to_json)
           (Some Values.RegisterIdentityProviderResponse.error_to_json)])
let start_product_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and username = flag "username" (required string) ~doc:"STRING String"
       and identityProvider =
         flag "identity-provider" (required json_arg)
           ~doc:"JSON IdentityProvider"
       and product = flag "product" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_product_subscription
           (Values.StartProductSubscriptionRequest.make ?domain
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~username
              ~identityProvider:(Values.IdentityProvider.of_json
                                   identityProvider) ~product ())
           (Some Values.StartProductSubscriptionResponse.to_json)
           (Some Values.StartProductSubscriptionResponse.error_to_json)])
let stop_product_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username = flag "username" (optional string) ~doc:"STRING String"
       and identityProvider =
         flag "identity-provider" (optional json_arg)
           ~doc:"JSON IdentityProvider"
       and product = flag "product" (optional string) ~doc:"STRING String"
       and productUserArn =
         flag "product-user-arn" (optional string) ~doc:"STRING Arn"
       and domain = flag "domain" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_product_subscription
           (Values.StopProductSubscriptionRequest.make ?username
              ?identityProvider:(Option.map
                                   ~f:Values.IdentityProvider.of_json
                                   identityProvider) ?product ?productUserArn
              ?domain ())
           (Some Values.StopProductSubscriptionResponse.to_json)
           (Some Values.StopProductSubscriptionResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_identity_provider_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityProvider =
         flag "identity-provider" (optional json_arg)
           ~doc:"JSON IdentityProvider"
       and product = flag "product" (optional string) ~doc:"STRING String"
       and identityProviderArn =
         flag "identity-provider-arn" (optional string) ~doc:"STRING Arn"
       and updateSettings =
         flag "update-settings" (required json_arg)
           ~doc:"JSON UpdateSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_identity_provider_settings
           (Values.UpdateIdentityProviderSettingsRequest.make
              ?identityProvider:(Option.map
                                   ~f:Values.IdentityProvider.of_json
                                   identityProvider) ?product
              ?identityProviderArn
              ~updateSettings:(Values.UpdateSettings.of_json updateSettings)
              ())
           (Some Values.UpdateIdentityProviderSettingsResponse.to_json)
           (Some Values.UpdateIdentityProviderSettingsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-user", associate_user);
    ("create-license-server-endpoint", create_license_server_endpoint);
    ("delete-license-server-endpoint", delete_license_server_endpoint);
    ("deregister-identity-provider", deregister_identity_provider);
    ("disassociate-user", disassociate_user);
    ("list-identity-providers", list_identity_providers);
    ("list-instances", list_instances);
    ("list-license-server-endpoints", list_license_server_endpoints);
    ("list-product-subscriptions", list_product_subscriptions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-user-associations", list_user_associations);
    ("register-identity-provider", register_identity_provider);
    ("start-product-subscription", start_product_subscription);
    ("stop-product-subscription", stop_product_subscription);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-identity-provider-settings", update_identity_provider_settings)]
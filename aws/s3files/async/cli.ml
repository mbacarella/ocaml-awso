(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and posixUser =
         flag "posix-user" (optional json_arg) ~doc:"JSON PosixUser"
       and rootDirectory =
         flag "root-directory" (optional json_arg) ~doc:"JSON RootDirectory"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_point
           (Values.CreateAccessPointRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?posixUser:(Option.map ~f:Values.PosixUser.of_json posixUser)
              ?rootDirectory:(Option.map ~f:Values.RootDirectory.of_json
                                rootDirectory) ~fileSystemId ())
           (Some Values.CreateAccessPointResponse.to_json)
           (Some Values.CreateAccessPointResponse.error_to_json)])
let create_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and prefix =
         flag "prefix" (optional string)
           ~doc:"STRING CreateFileSystemRequestPrefixString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING CreationToken"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and acceptBucketWarning =
         flag "accept-bucket-warning" (optional bool) ~doc:"BOOL Boolean"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketArn"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_file_system
           (Values.CreateFileSystemRequest.make ?prefix ?clientToken
              ?kmsKeyId ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?acceptBucketWarning ~bucket ~roleArn ())
           (Some Values.CreateFileSystemResponse.to_json)
           (Some Values.CreateFileSystemResponse.error_to_json)])
let create_mount_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipv4Address =
         flag "ipv4-address" (optional string) ~doc:"STRING Ipv4Address"
       and ipv6Address =
         flag "ipv6-address" (optional string) ~doc:"STRING Ipv6Address"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON SecurityGroups"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_mount_target
           (Values.CreateMountTargetRequest.make ?ipv4Address ?ipv6Address
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType)
              ?securityGroups:(Option.map ~f:Values.SecurityGroups.of_json
                                 securityGroups) ~fileSystemId ~subnetId ())
           (Some Values.CreateMountTargetResponse.to_json)
           (Some Values.CreateMountTargetResponse.error_to_json)])
let delete_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessPointId =
         flag "access-point-id" (required string) ~doc:"STRING AccessPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point
           (Values.DeleteAccessPointRequest.make ~accessPointId ()) None None])
let delete_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_file_system
           (Values.DeleteFileSystemRequest.make ?forceDelete ~fileSystemId ())
           None None])
let delete_file_system_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_file_system_policy
           (Values.DeleteFileSystemPolicyRequest.make ~fileSystemId ()) None
           None])
let delete_mount_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mountTargetId =
         flag "mount-target-id" (required string) ~doc:"STRING MountTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mount_target
           (Values.DeleteMountTargetRequest.make ~mountTargetId ()) None None])
let get_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessPointId =
         flag "access-point-id" (required string) ~doc:"STRING AccessPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point
           (Values.GetAccessPointRequest.make ~accessPointId ())
           (Some Values.GetAccessPointResponse.to_json)
           (Some Values.GetAccessPointResponse.error_to_json)])
let get_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_file_system
           (Values.GetFileSystemRequest.make ~fileSystemId ())
           (Some Values.GetFileSystemResponse.to_json)
           (Some Values.GetFileSystemResponse.error_to_json)])
let get_file_system_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_file_system_policy
           (Values.GetFileSystemPolicyRequest.make ~fileSystemId ())
           (Some Values.GetFileSystemPolicyResponse.to_json)
           (Some Values.GetFileSystemPolicyResponse.error_to_json)])
let get_mount_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mountTargetId =
         flag "mount-target-id" (required string) ~doc:"STRING MountTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mount_target
           (Values.GetMountTargetRequest.make ~mountTargetId ())
           (Some Values.GetMountTargetResponse.to_json)
           (Some Values.GetMountTargetResponse.error_to_json)])
let get_synchronization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_synchronization_configuration
           (Values.GetSynchronizationConfigurationRequest.make ~fileSystemId
              ())
           (Some Values.GetSynchronizationConfigurationResponse.to_json)
           (Some Values.GetSynchronizationConfigurationResponse.error_to_json)])
let list_access_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAccessPointsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_points
           (Values.ListAccessPointsRequest.make ?maxResults ?nextToken
              ~fileSystemId ())
           (Some Values.ListAccessPointsResponse.to_json)
           (Some Values.ListAccessPointsResponse.error_to_json)])
let list_file_systems =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucket = flag "bucket" (optional string) ~doc:"STRING BucketArn"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListFileSystemsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_file_systems
           (Values.ListFileSystemsRequest.make ?bucket ?maxResults ?nextToken
              ()) (Some Values.ListFileSystemsResponse.to_json)
           (Some Values.ListFileSystemsResponse.error_to_json)])
let list_mount_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (optional string) ~doc:"STRING FileSystemId"
       and accessPointId =
         flag "access-point-id" (optional string) ~doc:"STRING AccessPointId"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMountTargetsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mount_targets
           (Values.ListMountTargetsRequest.make ?fileSystemId ?accessPointId
              ?maxResults ?nextToken ())
           (Some Values.ListMountTargetsResponse.to_json)
           (Some Values.ListMountTargetsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTagsForResourceRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?maxResults ?nextToken
              ~resourceId ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_file_system_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and policy = flag "policy" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_file_system_policy
           (Values.PutFileSystemPolicyRequest.make ~fileSystemId ~policy ())
           (Some Values.PutFileSystemPolicyResponse.to_json)
           (Some Values.PutFileSystemPolicyResponse.error_to_json)])
let put_synchronization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and latestVersionNumber =
         flag "latest-version-number" (optional int) ~doc:"INT Integer"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and importDataRules =
         flag "import-data-rules" (required json_arg)
           ~doc:"JSON PutSynchronizationConfigurationRequestImportDataRulesList"
       and expirationDataRules =
         flag "expiration-data-rules" (required json_arg)
           ~doc:"JSON PutSynchronizationConfigurationRequestExpirationDataRulesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_synchronization_configuration
           (Values.PutSynchronizationConfigurationRequest.make
              ?latestVersionNumber ~fileSystemId
              ~importDataRules:(Values.PutSynchronizationConfigurationRequestImportDataRulesList.of_json
                                  importDataRules)
              ~expirationDataRules:(Values.PutSynchronizationConfigurationRequestExpirationDataRulesList.of_json
                                      expirationDataRules) ())
           (Some Values.PutSynchronizationConfigurationResponse.to_json)
           (Some Values.PutSynchronizationConfigurationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceId
              ~tags:(Values.TagList.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceId
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_mount_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mountTargetId =
         flag "mount-target-id" (required string) ~doc:"STRING MountTargetId"
       and securityGroups =
         flag "security-groups" (required json_arg)
           ~doc:"JSON SecurityGroups" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_mount_target
           (Values.UpdateMountTargetRequest.make ~mountTargetId
              ~securityGroups:(Values.SecurityGroups.of_json securityGroups)
              ()) (Some Values.UpdateMountTargetResponse.to_json)
           (Some Values.UpdateMountTargetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-access-point", create_access_point);
    ("create-file-system", create_file_system);
    ("create-mount-target", create_mount_target);
    ("delete-access-point", delete_access_point);
    ("delete-file-system", delete_file_system);
    ("delete-file-system-policy", delete_file_system_policy);
    ("delete-mount-target", delete_mount_target);
    ("get-access-point", get_access_point);
    ("get-file-system", get_file_system);
    ("get-file-system-policy", get_file_system_policy);
    ("get-mount-target", get_mount_target);
    ("get-synchronization-configuration", get_synchronization_configuration);
    ("list-access-points", list_access_points);
    ("list-file-systems", list_file_systems);
    ("list-mount-targets", list_mount_targets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-file-system-policy", put_file_system_policy);
    ("put-synchronization-configuration", put_synchronization_configuration);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-mount-target", update_mount_target)]
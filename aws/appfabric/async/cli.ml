(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_user_access_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and taskIdList =
         flag "task-id-list" (required json_arg) ~doc:"JSON TaskIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_user_access_tasks
           (Values.BatchGetUserAccessTasksRequest.make ~appBundleIdentifier
              ~taskIdList:(Values.TaskIdList.of_json taskIdList) ())
           (Some Values.BatchGetUserAccessTasksResponse.to_json)
           (Some Values.BatchGetUserAccessTasksResponse.error_to_json)])
let connect_app_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authRequest =
         flag "auth-request" (optional json_arg) ~doc:"JSON AuthRequest"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and appAuthorizationIdentifier =
         flag "app-authorization-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.connect_app_authorization
           (Values.ConnectAppAuthorizationRequest.make
              ?authRequest:(Option.map ~f:Values.AuthRequest.of_json
                              authRequest) ~appBundleIdentifier
              ~appAuthorizationIdentifier ())
           (Some Values.ConnectAppAuthorizationResponse.to_json)
           (Some Values.ConnectAppAuthorizationResponse.error_to_json)])
let create_app_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UUID"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and app = flag "app" (required string) ~doc:"STRING String255"
       and credential =
         flag "credential" (required json_arg) ~doc:"JSON Credential"
       and tenant = flag "tenant" (required json_arg) ~doc:"JSON Tenant"
       and authType =
         flag "auth-type" (required json_arg) ~doc:"JSON AuthType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_authorization
           (Values.CreateAppAuthorizationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appBundleIdentifier ~app
              ~credential:(Values.Credential.of_json credential)
              ~tenant:(Values.Tenant.of_json tenant)
              ~authType:(Values.AuthType.of_json authType) ())
           (Some Values.CreateAppAuthorizationResponse.to_json)
           (Some Values.CreateAppAuthorizationResponse.error_to_json)])
let create_app_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UUID"
       and customerManagedKeyIdentifier =
         flag "customer-managed-key-identifier" (optional string)
           ~doc:"STRING Identifier"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_bundle
           (Values.CreateAppBundleRequest.make ?clientToken
              ?customerManagedKeyIdentifier
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ())
           (Some Values.CreateAppBundleResponse.to_json)
           (Some Values.CreateAppBundleResponse.error_to_json)])
let create_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UUID"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and app = flag "app" (required string) ~doc:"STRING String255"
       and tenantId =
         flag "tenant-id" (required string) ~doc:"STRING TenantIdentifier"
       and ingestionType =
         flag "ingestion-type" (required json_arg) ~doc:"JSON IngestionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ingestion
           (Values.CreateIngestionRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appBundleIdentifier ~app ~tenantId
              ~ingestionType:(Values.IngestionType.of_json ingestionType) ())
           (Some Values.CreateIngestionResponse.to_json)
           (Some Values.CreateIngestionResponse.error_to_json)])
let create_ingestion_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UUID"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier"
       and processingConfiguration =
         flag "processing-configuration" (required json_arg)
           ~doc:"JSON ProcessingConfiguration"
       and destinationConfiguration =
         flag "destination-configuration" (required json_arg)
           ~doc:"JSON DestinationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ingestion_destination
           (Values.CreateIngestionDestinationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appBundleIdentifier ~ingestionIdentifier
              ~processingConfiguration:(Values.ProcessingConfiguration.of_json
                                          processingConfiguration)
              ~destinationConfiguration:(Values.DestinationConfiguration.of_json
                                           destinationConfiguration) ())
           (Some Values.CreateIngestionDestinationResponse.to_json)
           (Some Values.CreateIngestionDestinationResponse.error_to_json)])
let delete_app_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and appAuthorizationIdentifier =
         flag "app-authorization-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_authorization
           (Values.DeleteAppAuthorizationRequest.make ~appBundleIdentifier
              ~appAuthorizationIdentifier ())
           (Some Values.DeleteAppAuthorizationResponse.to_json)
           (Some Values.DeleteAppAuthorizationResponse.error_to_json)])
let delete_app_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_bundle
           (Values.DeleteAppBundleRequest.make ~appBundleIdentifier ())
           (Some Values.DeleteAppBundleResponse.to_json)
           (Some Values.DeleteAppBundleResponse.error_to_json)])
let delete_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ingestion
           (Values.DeleteIngestionRequest.make ~appBundleIdentifier
              ~ingestionIdentifier ())
           (Some Values.DeleteIngestionResponse.to_json)
           (Some Values.DeleteIngestionResponse.error_to_json)])
let delete_ingestion_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionDestinationIdentifier =
         flag "ingestion-destination-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ingestion_destination
           (Values.DeleteIngestionDestinationRequest.make
              ~appBundleIdentifier ~ingestionIdentifier
              ~ingestionDestinationIdentifier ())
           (Some Values.DeleteIngestionDestinationResponse.to_json)
           (Some Values.DeleteIngestionDestinationResponse.error_to_json)])
let get_app_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and appAuthorizationIdentifier =
         flag "app-authorization-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_authorization
           (Values.GetAppAuthorizationRequest.make ~appBundleIdentifier
              ~appAuthorizationIdentifier ())
           (Some Values.GetAppAuthorizationResponse.to_json)
           (Some Values.GetAppAuthorizationResponse.error_to_json)])
let get_app_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_bundle
           (Values.GetAppBundleRequest.make ~appBundleIdentifier ())
           (Some Values.GetAppBundleResponse.to_json)
           (Some Values.GetAppBundleResponse.error_to_json)])
let get_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ingestion
           (Values.GetIngestionRequest.make ~appBundleIdentifier
              ~ingestionIdentifier ())
           (Some Values.GetIngestionResponse.to_json)
           (Some Values.GetIngestionResponse.error_to_json)])
let get_ingestion_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionDestinationIdentifier =
         flag "ingestion-destination-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ingestion_destination
           (Values.GetIngestionDestinationRequest.make ~appBundleIdentifier
              ~ingestionIdentifier ~ingestionDestinationIdentifier ())
           (Some Values.GetIngestionDestinationResponse.to_json)
           (Some Values.GetIngestionDestinationResponse.error_to_json)])
let list_app_authorizations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String2048"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_authorizations
           (Values.ListAppAuthorizationsRequest.make ?maxResults ?nextToken
              ~appBundleIdentifier ())
           (Some Values.ListAppAuthorizationsResponse.to_json)
           (Some Values.ListAppAuthorizationsResponse.error_to_json)])
let list_app_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String2048" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_bundles
           (Values.ListAppBundlesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAppBundlesResponse.to_json)
           (Some Values.ListAppBundlesResponse.error_to_json)])
let list_ingestion_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ingestion_destinations
           (Values.ListIngestionDestinationsRequest.make ?maxResults
              ?nextToken ~appBundleIdentifier ~ingestionIdentifier ())
           (Some Values.ListIngestionDestinationsResponse.to_json)
           (Some Values.ListIngestionDestinationsResponse.error_to_json)])
let list_ingestions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ingestions
           (Values.ListIngestionsRequest.make ?maxResults ?nextToken
              ~appBundleIdentifier ())
           (Some Values.ListIngestionsResponse.to_json)
           (Some Values.ListIngestionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_ingestion
           (Values.StartIngestionRequest.make ~ingestionIdentifier
              ~appBundleIdentifier ())
           (Some Values.StartIngestionResponse.to_json)
           (Some Values.StartIngestionResponse.error_to_json)])
let start_user_access_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and email = flag "email" (required string) ~doc:"STRING Email" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_user_access_tasks
           (Values.StartUserAccessTasksRequest.make ~appBundleIdentifier
              ~email ()) (Some Values.StartUserAccessTasksResponse.to_json)
           (Some Values.StartUserAccessTasksResponse.error_to_json)])
let stop_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_ingestion
           (Values.StopIngestionRequest.make ~ingestionIdentifier
              ~appBundleIdentifier ())
           (Some Values.StopIngestionResponse.to_json)
           (Some Values.StopIngestionResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_app_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and credential =
         flag "credential" (optional json_arg) ~doc:"JSON Credential"
       and tenant = flag "tenant" (optional json_arg) ~doc:"JSON Tenant"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and appAuthorizationIdentifier =
         flag "app-authorization-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app_authorization
           (Values.UpdateAppAuthorizationRequest.make
              ?credential:(Option.map ~f:Values.Credential.of_json credential)
              ?tenant:(Option.map ~f:Values.Tenant.of_json tenant)
              ~appBundleIdentifier ~appAuthorizationIdentifier ())
           (Some Values.UpdateAppAuthorizationResponse.to_json)
           (Some Values.UpdateAppAuthorizationResponse.error_to_json)])
let update_ingestion_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appBundleIdentifier =
         flag "app-bundle-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionIdentifier =
         flag "ingestion-identifier" (required string)
           ~doc:"STRING Identifier"
       and ingestionDestinationIdentifier =
         flag "ingestion-destination-identifier" (required string)
           ~doc:"STRING Identifier"
       and destinationConfiguration =
         flag "destination-configuration" (required json_arg)
           ~doc:"JSON DestinationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ingestion_destination
           (Values.UpdateIngestionDestinationRequest.make
              ~appBundleIdentifier ~ingestionIdentifier
              ~ingestionDestinationIdentifier
              ~destinationConfiguration:(Values.DestinationConfiguration.of_json
                                           destinationConfiguration) ())
           (Some Values.UpdateIngestionDestinationResponse.to_json)
           (Some Values.UpdateIngestionDestinationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-user-access-tasks", batch_get_user_access_tasks);
    ("connect-app-authorization", connect_app_authorization);
    ("create-app-authorization", create_app_authorization);
    ("create-app-bundle", create_app_bundle);
    ("create-ingestion", create_ingestion);
    ("create-ingestion-destination", create_ingestion_destination);
    ("delete-app-authorization", delete_app_authorization);
    ("delete-app-bundle", delete_app_bundle);
    ("delete-ingestion", delete_ingestion);
    ("delete-ingestion-destination", delete_ingestion_destination);
    ("get-app-authorization", get_app_authorization);
    ("get-app-bundle", get_app_bundle);
    ("get-ingestion", get_ingestion);
    ("get-ingestion-destination", get_ingestion_destination);
    ("list-app-authorizations", list_app_authorizations);
    ("list-app-bundles", list_app_bundles);
    ("list-ingestion-destinations", list_ingestion_destinations);
    ("list-ingestions", list_ingestions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-ingestion", start_ingestion);
    ("start-user-access-tasks", start_user_access_tasks);
    ("stop-ingestion", stop_ingestion);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-app-authorization", update_app_authorization);
    ("update-ingestion-destination", update_ingestion_destination)]
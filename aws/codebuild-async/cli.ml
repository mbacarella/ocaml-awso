(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_delete_builds =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids = flag "ids" (required json_arg) ~doc:"JSON BuildIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_builds
           (Values.BatchDeleteBuildsInput.make
              ~ids:(Values.BuildIds.of_json ids) ())
           (Some Values.BatchDeleteBuildsOutput.to_json)
           (Some Values.BatchDeleteBuildsOutput.error_to_json)])
let batch_get_build_batches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids = flag "ids" (required json_arg) ~doc:"JSON BuildBatchIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_build_batches
           (Values.BatchGetBuildBatchesInput.make
              ~ids:(Values.BuildBatchIds.of_json ids) ())
           (Some Values.BatchGetBuildBatchesOutput.to_json)
           (Some Values.BatchGetBuildBatchesOutput.error_to_json)])
let batch_get_builds =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids = flag "ids" (required json_arg) ~doc:"JSON BuildIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_builds
           (Values.BatchGetBuildsInput.make
              ~ids:(Values.BuildIds.of_json ids) ())
           (Some Values.BatchGetBuildsOutput.to_json)
           (Some Values.BatchGetBuildsOutput.error_to_json)])
let batch_get_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names = flag "names" (required json_arg) ~doc:"JSON ProjectNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_projects
           (Values.BatchGetProjectsInput.make
              ~names:(Values.ProjectNames.of_json names) ())
           (Some Values.BatchGetProjectsOutput.to_json)
           (Some Values.BatchGetProjectsOutput.error_to_json)])
let batch_get_report_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportGroupArns =
         flag "report-group-arns" (required json_arg)
           ~doc:"JSON ReportGroupArns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_report_groups
           (Values.BatchGetReportGroupsInput.make
              ~reportGroupArns:(Values.ReportGroupArns.of_json
                                  reportGroupArns) ())
           (Some Values.BatchGetReportGroupsOutput.to_json)
           (Some Values.BatchGetReportGroupsOutput.error_to_json)])
let batch_get_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportArns =
         flag "report-arns" (required json_arg) ~doc:"JSON ReportArns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_reports
           (Values.BatchGetReportsInput.make
              ~reportArns:(Values.ReportArns.of_json reportArns) ())
           (Some Values.BatchGetReportsOutput.to_json)
           (Some Values.BatchGetReportsOutput.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProjectDescription"
       and secondarySources =
         flag "secondary-sources" (optional json_arg)
           ~doc:"JSON ProjectSources"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and secondarySourceVersions =
         flag "secondary-source-versions" (optional json_arg)
           ~doc:"JSON ProjectSecondarySourceVersions"
       and secondaryArtifacts =
         flag "secondary-artifacts" (optional json_arg)
           ~doc:"JSON ProjectArtifactsList"
       and cache = flag "cache" (optional json_arg) ~doc:"JSON ProjectCache"
       and timeoutInMinutes =
         flag "timeout-in-minutes" (optional int) ~doc:"INT TimeOut"
       and queuedTimeoutInMinutes =
         flag "queued-timeout-in-minutes" (optional int) ~doc:"INT TimeOut"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING NonEmptyString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and badgeEnabled =
         flag "badge-enabled" (optional bool) ~doc:"BOOL WrapperBoolean"
       and logsConfig =
         flag "logs-config" (optional json_arg) ~doc:"JSON LogsConfig"
       and fileSystemLocations =
         flag "file-system-locations" (optional json_arg)
           ~doc:"JSON ProjectFileSystemLocations"
       and buildBatchConfig =
         flag "build-batch-config" (optional json_arg)
           ~doc:"JSON ProjectBuildBatchConfig"
       and concurrentBuildLimit =
         flag "concurrent-build-limit" (optional int) ~doc:"INT WrapperInt"
       and name = flag "name" (required string) ~doc:"STRING ProjectName"
       and source =
         flag "source" (required json_arg) ~doc:"JSON ProjectSource"
       and artifacts =
         flag "artifacts" (required json_arg) ~doc:"JSON ProjectArtifacts"
       and environment =
         flag "environment" (required json_arg)
           ~doc:"JSON ProjectEnvironment"
       and serviceRole =
         flag "service-role" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectInput.make ?description
              ?secondarySources:(Option.map ~f:Values.ProjectSources.of_json
                                   secondarySources) ?sourceVersion
              ?secondarySourceVersions:(Option.map
                                          ~f:Values.ProjectSecondarySourceVersions.of_json
                                          secondarySourceVersions)
              ?secondaryArtifacts:(Option.map
                                     ~f:Values.ProjectArtifactsList.of_json
                                     secondaryArtifacts)
              ?cache:(Option.map ~f:Values.ProjectCache.of_json cache)
              ?timeoutInMinutes ?queuedTimeoutInMinutes ?encryptionKey
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?badgeEnabled
              ?logsConfig:(Option.map ~f:Values.LogsConfig.of_json logsConfig)
              ?fileSystemLocations:(Option.map
                                      ~f:Values.ProjectFileSystemLocations.of_json
                                      fileSystemLocations)
              ?buildBatchConfig:(Option.map
                                   ~f:Values.ProjectBuildBatchConfig.of_json
                                   buildBatchConfig) ?concurrentBuildLimit
              ~name ~source:(Values.ProjectSource.of_json source)
              ~artifacts:(Values.ProjectArtifacts.of_json artifacts)
              ~environment:(Values.ProjectEnvironment.of_json environment)
              ~serviceRole ()) (Some Values.CreateProjectOutput.to_json)
           (Some Values.CreateProjectOutput.error_to_json)])
let create_report_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ReportGroupName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON ReportType"
       and exportConfig =
         flag "export-config" (required json_arg)
           ~doc:"JSON ReportExportConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_report_group
           (Values.CreateReportGroupInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~type_:(Values.ReportType.of_json type_)
              ~exportConfig:(Values.ReportExportConfig.of_json exportConfig)
              ()) (Some Values.CreateReportGroupOutput.to_json)
           (Some Values.CreateReportGroupOutput.error_to_json)])
let create_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and branchFilter =
         flag "branch-filter" (optional string) ~doc:"STRING String"
       and filterGroups =
         flag "filter-groups" (optional json_arg) ~doc:"JSON FilterGroups"
       and buildType =
         flag "build-type" (optional json_arg) ~doc:"JSON WebhookBuildType"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_webhook
           (Values.CreateWebhookInput.make ?branchFilter
              ?filterGroups:(Option.map ~f:Values.FilterGroups.of_json
                               filterGroups)
              ?buildType:(Option.map ~f:Values.WebhookBuildType.of_json
                            buildType) ~projectName ())
           (Some Values.CreateWebhookOutput.to_json)
           (Some Values.CreateWebhookOutput.error_to_json)])
let delete_build_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_build_batch (Values.DeleteBuildBatchInput.make ~id ())
           (Some Values.DeleteBuildBatchOutput.to_json)
           (Some Values.DeleteBuildBatchOutput.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project (Values.DeleteProjectInput.make ~name ())
           (Some Values.DeleteProjectOutput.to_json)
           (Some Values.DeleteProjectOutput.error_to_json)])
let delete_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_report (Values.DeleteReportInput.make ~arn ())
           (Some Values.DeleteReportOutput.to_json)
           (Some Values.DeleteReportOutput.error_to_json)])
let delete_report_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteReports =
         flag "delete-reports" (optional bool) ~doc:"BOOL Boolean"
       and arn = flag "arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_report_group
           (Values.DeleteReportGroupInput.make ?deleteReports ~arn ())
           (Some Values.DeleteReportGroupOutput.to_json)
           (Some Values.DeleteReportGroupOutput.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyInput.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyOutput.to_json)
           (Some Values.DeleteResourcePolicyOutput.error_to_json)])
let delete_source_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_source_credentials
           (Values.DeleteSourceCredentialsInput.make ~arn ())
           (Some Values.DeleteSourceCredentialsOutput.to_json)
           (Some Values.DeleteSourceCredentialsOutput.error_to_json)])
let delete_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_webhook (Values.DeleteWebhookInput.make ~projectName ())
           (Some Values.DeleteWebhookOutput.to_json)
           (Some Values.DeleteWebhookOutput.error_to_json)])
let describe_code_coverages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON ReportCodeCoverageSortByType"
       and minLineCoveragePercentage =
         flag "min-line-coverage-percentage" (optional float)
           ~doc:"FLOAT Percentage"
       and maxLineCoveragePercentage =
         flag "max-line-coverage-percentage" (optional float)
           ~doc:"FLOAT Percentage"
       and reportArn =
         flag "report-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_code_coverages
           (Values.DescribeCodeCoveragesInput.make ?nextToken ?maxResults
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder)
              ?sortBy:(Option.map
                         ~f:Values.ReportCodeCoverageSortByType.of_json
                         sortBy) ?minLineCoveragePercentage
              ?maxLineCoveragePercentage ~reportArn ())
           (Some Values.DescribeCodeCoveragesOutput.to_json)
           (Some Values.DescribeCodeCoveragesOutput.error_to_json)])
let describe_test_cases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON TestCaseFilter"
       and reportArn =
         flag "report-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_test_cases
           (Values.DescribeTestCasesInput.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.TestCaseFilter.of_json filter)
              ~reportArn ()) (Some Values.DescribeTestCasesOutput.to_json)
           (Some Values.DescribeTestCasesOutput.error_to_json)])
let get_report_group_trend =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and numOfReports =
         flag "num-of-reports" (optional int) ~doc:"INT PageSize"
       and reportGroupArn =
         flag "report-group-arn" (required string)
           ~doc:"STRING NonEmptyString"
       and trendField =
         flag "trend-field" (required json_arg)
           ~doc:"JSON ReportGroupTrendFieldType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_report_group_trend
           (Values.GetReportGroupTrendInput.make ?numOfReports
              ~reportGroupArn
              ~trendField:(Values.ReportGroupTrendFieldType.of_json
                             trendField) ())
           (Some Values.GetReportGroupTrendOutput.to_json)
           (Some Values.GetReportGroupTrendOutput.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyInput.make ~resourceArn ())
           (Some Values.GetResourcePolicyOutput.to_json)
           (Some Values.GetResourcePolicyOutput.error_to_json)])
let import_source_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username =
         flag "username" (optional string) ~doc:"STRING NonEmptyString"
       and shouldOverwrite =
         flag "should-overwrite" (optional bool) ~doc:"BOOL WrapperBoolean"
       and token =
         flag "token" (required string) ~doc:"STRING SensitiveNonEmptyString"
       and serverType =
         flag "server-type" (required json_arg) ~doc:"JSON ServerType"
       and authType =
         flag "auth-type" (required json_arg) ~doc:"JSON AuthType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_source_credentials
           (Values.ImportSourceCredentialsInput.make ?username
              ?shouldOverwrite ~token
              ~serverType:(Values.ServerType.of_json serverType)
              ~authType:(Values.AuthType.of_json authType) ())
           (Some Values.ImportSourceCredentialsOutput.to_json)
           (Some Values.ImportSourceCredentialsOutput.error_to_json)])
let invalidate_project_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invalidate_project_cache
           (Values.InvalidateProjectCacheInput.make ~projectName ())
           (Some Values.InvalidateProjectCacheOutput.to_json)
           (Some Values.InvalidateProjectCacheOutput.error_to_json)])
let list_build_batches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON BuildBatchFilter"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_build_batches
           (Values.ListBuildBatchesInput.make
              ?filter:(Option.map ~f:Values.BuildBatchFilter.of_json filter)
              ?maxResults
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?nextToken ())
           (Some Values.ListBuildBatchesOutput.to_json)
           (Some Values.ListBuildBatchesOutput.error_to_json)])
let list_build_batches_for_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (optional string) ~doc:"STRING NonEmptyString"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON BuildBatchFilter"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_build_batches_for_project
           (Values.ListBuildBatchesForProjectInput.make ?projectName
              ?filter:(Option.map ~f:Values.BuildBatchFilter.of_json filter)
              ?maxResults
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?nextToken ())
           (Some Values.ListBuildBatchesForProjectOutput.to_json)
           (Some Values.ListBuildBatchesForProjectOutput.error_to_json)])
let list_builds =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_builds
           (Values.ListBuildsInput.make
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?nextToken ())
           (Some Values.ListBuildsOutput.to_json)
           (Some Values.ListBuildsOutput.error_to_json)])
let list_builds_for_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_builds_for_project
           (Values.ListBuildsForProjectInput.make
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?nextToken ~projectName ())
           (Some Values.ListBuildsForProjectOutput.to_json)
           (Some Values.ListBuildsForProjectOutput.error_to_json)])
let list_curated_environment_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_curated_environment_images
           (Values.ListCuratedEnvironmentImagesInput.make ())
           (Some Values.ListCuratedEnvironmentImagesOutput.to_json)
           (Some Values.ListCuratedEnvironmentImagesOutput.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON ProjectSortByType"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsInput.make
              ?sortBy:(Option.map ~f:Values.ProjectSortByType.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?nextToken ())
           (Some Values.ListProjectsOutput.to_json)
           (Some Values.ListProjectsOutput.error_to_json)])
let list_report_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON ReportGroupSortByType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_report_groups
           (Values.ListReportGroupsInput.make
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder)
              ?sortBy:(Option.map ~f:Values.ReportGroupSortByType.of_json
                         sortBy) ?nextToken ?maxResults ())
           (Some Values.ListReportGroupsOutput.to_json)
           (Some Values.ListReportGroupsOutput.error_to_json)])
let list_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ReportFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reports
           (Values.ListReportsInput.make
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.ReportFilter.of_json filter) ())
           (Some Values.ListReportsOutput.to_json)
           (Some Values.ListReportsOutput.error_to_json)])
let list_reports_for_report_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ReportFilter"
       and reportGroupArn =
         flag "report-group-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reports_for_report_group
           (Values.ListReportsForReportGroupInput.make ?nextToken
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?maxResults
              ?filter:(Option.map ~f:Values.ReportFilter.of_json filter)
              ~reportGroupArn ())
           (Some Values.ListReportsForReportGroupOutput.to_json)
           (Some Values.ListReportsForReportGroupOutput.error_to_json)])
let list_shared_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON SharedResourceSortByType"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_shared_projects
           (Values.ListSharedProjectsInput.make
              ?sortBy:(Option.map ~f:Values.SharedResourceSortByType.of_json
                         sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder) ?maxResults ?nextToken ())
           (Some Values.ListSharedProjectsOutput.to_json)
           (Some Values.ListSharedProjectsOutput.error_to_json)])
let list_shared_report_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrderType"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON SharedResourceSortByType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_shared_report_groups
           (Values.ListSharedReportGroupsInput.make
              ?sortOrder:(Option.map ~f:Values.SortOrderType.of_json
                            sortOrder)
              ?sortBy:(Option.map ~f:Values.SharedResourceSortByType.of_json
                         sortBy) ?nextToken ?maxResults ())
           (Some Values.ListSharedReportGroupsOutput.to_json)
           (Some Values.ListSharedReportGroupsOutput.error_to_json)])
let list_source_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_source_credentials
           (Values.ListSourceCredentialsInput.make ())
           (Some Values.ListSourceCredentialsOutput.to_json)
           (Some Values.ListSourceCredentialsOutput.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy =
         flag "policy" (required string) ~doc:"STRING NonEmptyString"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyInput.make ~policy ~resourceArn ())
           (Some Values.PutResourcePolicyOutput.to_json)
           (Some Values.PutResourcePolicyOutput.error_to_json)])
let retry_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING NonEmptyString"
       and idempotencyToken =
         flag "idempotency-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retry_build
           (Values.RetryBuildInput.make ?id ?idempotencyToken ())
           (Some Values.RetryBuildOutput.to_json)
           (Some Values.RetryBuildOutput.error_to_json)])
let retry_build_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING NonEmptyString"
       and idempotencyToken =
         flag "idempotency-token" (optional string) ~doc:"STRING String"
       and retryType =
         flag "retry-type" (optional json_arg)
           ~doc:"JSON RetryBuildBatchType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retry_build_batch
           (Values.RetryBuildBatchInput.make ?id ?idempotencyToken
              ?retryType:(Option.map ~f:Values.RetryBuildBatchType.of_json
                            retryType) ())
           (Some Values.RetryBuildBatchOutput.to_json)
           (Some Values.RetryBuildBatchOutput.error_to_json)])
let start_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secondarySourcesOverride =
         flag "secondary-sources-override" (optional json_arg)
           ~doc:"JSON ProjectSources"
       and secondarySourcesVersionOverride =
         flag "secondary-sources-version-override" (optional json_arg)
           ~doc:"JSON ProjectSecondarySourceVersions"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and artifactsOverride =
         flag "artifacts-override" (optional json_arg)
           ~doc:"JSON ProjectArtifacts"
       and secondaryArtifactsOverride =
         flag "secondary-artifacts-override" (optional json_arg)
           ~doc:"JSON ProjectArtifactsList"
       and environmentVariablesOverride =
         flag "environment-variables-override" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and sourceTypeOverride =
         flag "source-type-override" (optional json_arg)
           ~doc:"JSON SourceType"
       and sourceLocationOverride =
         flag "source-location-override" (optional string)
           ~doc:"STRING String"
       and sourceAuthOverride =
         flag "source-auth-override" (optional json_arg)
           ~doc:"JSON SourceAuth"
       and gitCloneDepthOverride =
         flag "git-clone-depth-override" (optional int)
           ~doc:"INT GitCloneDepth"
       and gitSubmodulesConfigOverride =
         flag "git-submodules-config-override" (optional json_arg)
           ~doc:"JSON GitSubmodulesConfig"
       and buildspecOverride =
         flag "buildspec-override" (optional string) ~doc:"STRING String"
       and insecureSslOverride =
         flag "insecure-ssl-override" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and reportBuildStatusOverride =
         flag "report-build-status-override" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and buildStatusConfigOverride =
         flag "build-status-config-override" (optional json_arg)
           ~doc:"JSON BuildStatusConfig"
       and environmentTypeOverride =
         flag "environment-type-override" (optional json_arg)
           ~doc:"JSON EnvironmentType"
       and imageOverride =
         flag "image-override" (optional string) ~doc:"STRING NonEmptyString"
       and computeTypeOverride =
         flag "compute-type-override" (optional json_arg)
           ~doc:"JSON ComputeType"
       and certificateOverride =
         flag "certificate-override" (optional string) ~doc:"STRING String"
       and cacheOverride =
         flag "cache-override" (optional json_arg) ~doc:"JSON ProjectCache"
       and serviceRoleOverride =
         flag "service-role-override" (optional string)
           ~doc:"STRING NonEmptyString"
       and privilegedModeOverride =
         flag "privileged-mode-override" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and timeoutInMinutesOverride =
         flag "timeout-in-minutes-override" (optional int) ~doc:"INT TimeOut"
       and queuedTimeoutInMinutesOverride =
         flag "queued-timeout-in-minutes-override" (optional int)
           ~doc:"INT TimeOut"
       and encryptionKeyOverride =
         flag "encryption-key-override" (optional string)
           ~doc:"STRING NonEmptyString"
       and idempotencyToken =
         flag "idempotency-token" (optional string) ~doc:"STRING String"
       and logsConfigOverride =
         flag "logs-config-override" (optional json_arg)
           ~doc:"JSON LogsConfig"
       and registryCredentialOverride =
         flag "registry-credential-override" (optional json_arg)
           ~doc:"JSON RegistryCredential"
       and imagePullCredentialsTypeOverride =
         flag "image-pull-credentials-type-override" (optional json_arg)
           ~doc:"JSON ImagePullCredentialsType"
       and debugSessionEnabled =
         flag "debug-session-enabled" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_build
           (Values.StartBuildInput.make
              ?secondarySourcesOverride:(Option.map
                                           ~f:Values.ProjectSources.of_json
                                           secondarySourcesOverride)
              ?secondarySourcesVersionOverride:(Option.map
                                                  ~f:Values.ProjectSecondarySourceVersions.of_json
                                                  secondarySourcesVersionOverride)
              ?sourceVersion
              ?artifactsOverride:(Option.map
                                    ~f:Values.ProjectArtifacts.of_json
                                    artifactsOverride)
              ?secondaryArtifactsOverride:(Option.map
                                             ~f:Values.ProjectArtifactsList.of_json
                                             secondaryArtifactsOverride)
              ?environmentVariablesOverride:(Option.map
                                               ~f:Values.EnvironmentVariables.of_json
                                               environmentVariablesOverride)
              ?sourceTypeOverride:(Option.map ~f:Values.SourceType.of_json
                                     sourceTypeOverride)
              ?sourceLocationOverride
              ?sourceAuthOverride:(Option.map ~f:Values.SourceAuth.of_json
                                     sourceAuthOverride)
              ?gitCloneDepthOverride
              ?gitSubmodulesConfigOverride:(Option.map
                                              ~f:Values.GitSubmodulesConfig.of_json
                                              gitSubmodulesConfigOverride)
              ?buildspecOverride ?insecureSslOverride
              ?reportBuildStatusOverride
              ?buildStatusConfigOverride:(Option.map
                                            ~f:Values.BuildStatusConfig.of_json
                                            buildStatusConfigOverride)
              ?environmentTypeOverride:(Option.map
                                          ~f:Values.EnvironmentType.of_json
                                          environmentTypeOverride)
              ?imageOverride
              ?computeTypeOverride:(Option.map ~f:Values.ComputeType.of_json
                                      computeTypeOverride)
              ?certificateOverride
              ?cacheOverride:(Option.map ~f:Values.ProjectCache.of_json
                                cacheOverride) ?serviceRoleOverride
              ?privilegedModeOverride ?timeoutInMinutesOverride
              ?queuedTimeoutInMinutesOverride ?encryptionKeyOverride
              ?idempotencyToken
              ?logsConfigOverride:(Option.map ~f:Values.LogsConfig.of_json
                                     logsConfigOverride)
              ?registryCredentialOverride:(Option.map
                                             ~f:Values.RegistryCredential.of_json
                                             registryCredentialOverride)
              ?imagePullCredentialsTypeOverride:(Option.map
                                                   ~f:Values.ImagePullCredentialsType.of_json
                                                   imagePullCredentialsTypeOverride)
              ?debugSessionEnabled ~projectName ())
           (Some Values.StartBuildOutput.to_json)
           (Some Values.StartBuildOutput.error_to_json)])
let start_build_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secondarySourcesOverride =
         flag "secondary-sources-override" (optional json_arg)
           ~doc:"JSON ProjectSources"
       and secondarySourcesVersionOverride =
         flag "secondary-sources-version-override" (optional json_arg)
           ~doc:"JSON ProjectSecondarySourceVersions"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and artifactsOverride =
         flag "artifacts-override" (optional json_arg)
           ~doc:"JSON ProjectArtifacts"
       and secondaryArtifactsOverride =
         flag "secondary-artifacts-override" (optional json_arg)
           ~doc:"JSON ProjectArtifactsList"
       and environmentVariablesOverride =
         flag "environment-variables-override" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and sourceTypeOverride =
         flag "source-type-override" (optional json_arg)
           ~doc:"JSON SourceType"
       and sourceLocationOverride =
         flag "source-location-override" (optional string)
           ~doc:"STRING String"
       and sourceAuthOverride =
         flag "source-auth-override" (optional json_arg)
           ~doc:"JSON SourceAuth"
       and gitCloneDepthOverride =
         flag "git-clone-depth-override" (optional int)
           ~doc:"INT GitCloneDepth"
       and gitSubmodulesConfigOverride =
         flag "git-submodules-config-override" (optional json_arg)
           ~doc:"JSON GitSubmodulesConfig"
       and buildspecOverride =
         flag "buildspec-override" (optional string) ~doc:"STRING String"
       and insecureSslOverride =
         flag "insecure-ssl-override" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and reportBuildBatchStatusOverride =
         flag "report-build-batch-status-override" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and environmentTypeOverride =
         flag "environment-type-override" (optional json_arg)
           ~doc:"JSON EnvironmentType"
       and imageOverride =
         flag "image-override" (optional string) ~doc:"STRING NonEmptyString"
       and computeTypeOverride =
         flag "compute-type-override" (optional json_arg)
           ~doc:"JSON ComputeType"
       and certificateOverride =
         flag "certificate-override" (optional string) ~doc:"STRING String"
       and cacheOverride =
         flag "cache-override" (optional json_arg) ~doc:"JSON ProjectCache"
       and serviceRoleOverride =
         flag "service-role-override" (optional string)
           ~doc:"STRING NonEmptyString"
       and privilegedModeOverride =
         flag "privileged-mode-override" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and buildTimeoutInMinutesOverride =
         flag "build-timeout-in-minutes-override" (optional int)
           ~doc:"INT TimeOut"
       and queuedTimeoutInMinutesOverride =
         flag "queued-timeout-in-minutes-override" (optional int)
           ~doc:"INT TimeOut"
       and encryptionKeyOverride =
         flag "encryption-key-override" (optional string)
           ~doc:"STRING NonEmptyString"
       and idempotencyToken =
         flag "idempotency-token" (optional string) ~doc:"STRING String"
       and logsConfigOverride =
         flag "logs-config-override" (optional json_arg)
           ~doc:"JSON LogsConfig"
       and registryCredentialOverride =
         flag "registry-credential-override" (optional json_arg)
           ~doc:"JSON RegistryCredential"
       and imagePullCredentialsTypeOverride =
         flag "image-pull-credentials-type-override" (optional json_arg)
           ~doc:"JSON ImagePullCredentialsType"
       and buildBatchConfigOverride =
         flag "build-batch-config-override" (optional json_arg)
           ~doc:"JSON ProjectBuildBatchConfig"
       and debugSessionEnabled =
         flag "debug-session-enabled" (optional bool)
           ~doc:"BOOL WrapperBoolean"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_build_batch
           (Values.StartBuildBatchInput.make
              ?secondarySourcesOverride:(Option.map
                                           ~f:Values.ProjectSources.of_json
                                           secondarySourcesOverride)
              ?secondarySourcesVersionOverride:(Option.map
                                                  ~f:Values.ProjectSecondarySourceVersions.of_json
                                                  secondarySourcesVersionOverride)
              ?sourceVersion
              ?artifactsOverride:(Option.map
                                    ~f:Values.ProjectArtifacts.of_json
                                    artifactsOverride)
              ?secondaryArtifactsOverride:(Option.map
                                             ~f:Values.ProjectArtifactsList.of_json
                                             secondaryArtifactsOverride)
              ?environmentVariablesOverride:(Option.map
                                               ~f:Values.EnvironmentVariables.of_json
                                               environmentVariablesOverride)
              ?sourceTypeOverride:(Option.map ~f:Values.SourceType.of_json
                                     sourceTypeOverride)
              ?sourceLocationOverride
              ?sourceAuthOverride:(Option.map ~f:Values.SourceAuth.of_json
                                     sourceAuthOverride)
              ?gitCloneDepthOverride
              ?gitSubmodulesConfigOverride:(Option.map
                                              ~f:Values.GitSubmodulesConfig.of_json
                                              gitSubmodulesConfigOverride)
              ?buildspecOverride ?insecureSslOverride
              ?reportBuildBatchStatusOverride
              ?environmentTypeOverride:(Option.map
                                          ~f:Values.EnvironmentType.of_json
                                          environmentTypeOverride)
              ?imageOverride
              ?computeTypeOverride:(Option.map ~f:Values.ComputeType.of_json
                                      computeTypeOverride)
              ?certificateOverride
              ?cacheOverride:(Option.map ~f:Values.ProjectCache.of_json
                                cacheOverride) ?serviceRoleOverride
              ?privilegedModeOverride ?buildTimeoutInMinutesOverride
              ?queuedTimeoutInMinutesOverride ?encryptionKeyOverride
              ?idempotencyToken
              ?logsConfigOverride:(Option.map ~f:Values.LogsConfig.of_json
                                     logsConfigOverride)
              ?registryCredentialOverride:(Option.map
                                             ~f:Values.RegistryCredential.of_json
                                             registryCredentialOverride)
              ?imagePullCredentialsTypeOverride:(Option.map
                                                   ~f:Values.ImagePullCredentialsType.of_json
                                                   imagePullCredentialsTypeOverride)
              ?buildBatchConfigOverride:(Option.map
                                           ~f:Values.ProjectBuildBatchConfig.of_json
                                           buildBatchConfigOverride)
              ?debugSessionEnabled ~projectName ())
           (Some Values.StartBuildBatchOutput.to_json)
           (Some Values.StartBuildBatchOutput.error_to_json)])
let stop_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_build (Values.StopBuildInput.make ~id ())
           (Some Values.StopBuildOutput.to_json)
           (Some Values.StopBuildOutput.error_to_json)])
let stop_build_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_build_batch (Values.StopBuildBatchInput.make ~id ())
           (Some Values.StopBuildBatchOutput.to_json)
           (Some Values.StopBuildBatchOutput.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProjectDescription"
       and source =
         flag "source" (optional json_arg) ~doc:"JSON ProjectSource"
       and secondarySources =
         flag "secondary-sources" (optional json_arg)
           ~doc:"JSON ProjectSources"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and secondarySourceVersions =
         flag "secondary-source-versions" (optional json_arg)
           ~doc:"JSON ProjectSecondarySourceVersions"
       and artifacts =
         flag "artifacts" (optional json_arg) ~doc:"JSON ProjectArtifacts"
       and secondaryArtifacts =
         flag "secondary-artifacts" (optional json_arg)
           ~doc:"JSON ProjectArtifactsList"
       and cache = flag "cache" (optional json_arg) ~doc:"JSON ProjectCache"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON ProjectEnvironment"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING NonEmptyString"
       and timeoutInMinutes =
         flag "timeout-in-minutes" (optional int) ~doc:"INT TimeOut"
       and queuedTimeoutInMinutes =
         flag "queued-timeout-in-minutes" (optional int) ~doc:"INT TimeOut"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING NonEmptyString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and badgeEnabled =
         flag "badge-enabled" (optional bool) ~doc:"BOOL WrapperBoolean"
       and logsConfig =
         flag "logs-config" (optional json_arg) ~doc:"JSON LogsConfig"
       and fileSystemLocations =
         flag "file-system-locations" (optional json_arg)
           ~doc:"JSON ProjectFileSystemLocations"
       and buildBatchConfig =
         flag "build-batch-config" (optional json_arg)
           ~doc:"JSON ProjectBuildBatchConfig"
       and concurrentBuildLimit =
         flag "concurrent-build-limit" (optional int) ~doc:"INT WrapperInt"
       and name = flag "name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectInput.make ?description
              ?source:(Option.map ~f:Values.ProjectSource.of_json source)
              ?secondarySources:(Option.map ~f:Values.ProjectSources.of_json
                                   secondarySources) ?sourceVersion
              ?secondarySourceVersions:(Option.map
                                          ~f:Values.ProjectSecondarySourceVersions.of_json
                                          secondarySourceVersions)
              ?artifacts:(Option.map ~f:Values.ProjectArtifacts.of_json
                            artifacts)
              ?secondaryArtifacts:(Option.map
                                     ~f:Values.ProjectArtifactsList.of_json
                                     secondaryArtifacts)
              ?cache:(Option.map ~f:Values.ProjectCache.of_json cache)
              ?environment:(Option.map ~f:Values.ProjectEnvironment.of_json
                              environment) ?serviceRole ?timeoutInMinutes
              ?queuedTimeoutInMinutes ?encryptionKey
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?badgeEnabled
              ?logsConfig:(Option.map ~f:Values.LogsConfig.of_json logsConfig)
              ?fileSystemLocations:(Option.map
                                      ~f:Values.ProjectFileSystemLocations.of_json
                                      fileSystemLocations)
              ?buildBatchConfig:(Option.map
                                   ~f:Values.ProjectBuildBatchConfig.of_json
                                   buildBatchConfig) ?concurrentBuildLimit
              ~name ()) (Some Values.UpdateProjectOutput.to_json)
           (Some Values.UpdateProjectOutput.error_to_json)])
let update_project_visibility =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceAccessRole =
         flag "resource-access-role" (optional string)
           ~doc:"STRING NonEmptyString"
       and projectArn =
         flag "project-arn" (required string) ~doc:"STRING NonEmptyString"
       and projectVisibility =
         flag "project-visibility" (required json_arg)
           ~doc:"JSON ProjectVisibilityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project_visibility
           (Values.UpdateProjectVisibilityInput.make ?resourceAccessRole
              ~projectArn
              ~projectVisibility:(Values.ProjectVisibilityType.of_json
                                    projectVisibility) ())
           (Some Values.UpdateProjectVisibilityOutput.to_json)
           (Some Values.UpdateProjectVisibilityOutput.error_to_json)])
let update_report_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportConfig =
         flag "export-config" (optional json_arg)
           ~doc:"JSON ReportExportConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and arn = flag "arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_report_group
           (Values.UpdateReportGroupInput.make
              ?exportConfig:(Option.map ~f:Values.ReportExportConfig.of_json
                               exportConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~arn ())
           (Some Values.UpdateReportGroupOutput.to_json)
           (Some Values.UpdateReportGroupOutput.error_to_json)])
let update_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and branchFilter =
         flag "branch-filter" (optional string) ~doc:"STRING String"
       and rotateSecret =
         flag "rotate-secret" (optional bool) ~doc:"BOOL Boolean"
       and filterGroups =
         flag "filter-groups" (optional json_arg) ~doc:"JSON FilterGroups"
       and buildType =
         flag "build-type" (optional json_arg) ~doc:"JSON WebhookBuildType"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_webhook
           (Values.UpdateWebhookInput.make ?branchFilter ?rotateSecret
              ?filterGroups:(Option.map ~f:Values.FilterGroups.of_json
                               filterGroups)
              ?buildType:(Option.map ~f:Values.WebhookBuildType.of_json
                            buildType) ~projectName ())
           (Some Values.UpdateWebhookOutput.to_json)
           (Some Values.UpdateWebhookOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-delete-builds", batch_delete_builds);
    ("batch-get-build-batches", batch_get_build_batches);
    ("batch-get-builds", batch_get_builds);
    ("batch-get-projects", batch_get_projects);
    ("batch-get-report-groups", batch_get_report_groups);
    ("batch-get-reports", batch_get_reports);
    ("create-project", create_project);
    ("create-report-group", create_report_group);
    ("create-webhook", create_webhook);
    ("delete-build-batch", delete_build_batch);
    ("delete-project", delete_project);
    ("delete-report", delete_report);
    ("delete-report-group", delete_report_group);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-source-credentials", delete_source_credentials);
    ("delete-webhook", delete_webhook);
    ("describe-code-coverages", describe_code_coverages);
    ("describe-test-cases", describe_test_cases);
    ("get-report-group-trend", get_report_group_trend);
    ("get-resource-policy", get_resource_policy);
    ("import-source-credentials", import_source_credentials);
    ("invalidate-project-cache", invalidate_project_cache);
    ("list-build-batches", list_build_batches);
    ("list-build-batches-for-project", list_build_batches_for_project);
    ("list-builds", list_builds);
    ("list-builds-for-project", list_builds_for_project);
    ("list-curated-environment-images", list_curated_environment_images);
    ("list-projects", list_projects);
    ("list-report-groups", list_report_groups);
    ("list-reports", list_reports);
    ("list-reports-for-report-group", list_reports_for_report_group);
    ("list-shared-projects", list_shared_projects);
    ("list-shared-report-groups", list_shared_report_groups);
    ("list-source-credentials", list_source_credentials);
    ("put-resource-policy", put_resource_policy);
    ("retry-build", retry_build);
    ("retry-build-batch", retry_build_batch);
    ("start-build", start_build);
    ("start-build-batch", start_build_batch);
    ("stop-build", stop_build);
    ("stop-build-batch", stop_build_batch);
    ("update-project", update_project);
    ("update-project-visibility", update_project_visibility);
    ("update-report-group", update_report_group);
    ("update-webhook", update_webhook)]
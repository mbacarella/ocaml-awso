(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let abort_environment_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.abort_environment_update
           (Values.AbortEnvironmentUpdateMessage.make ?environmentId
              ?environmentName ()) None None])
let apply_environment_managed_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string) ~doc:"STRING String"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING String"
       and actionId = flag "action-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_environment_managed_action
           (Values.ApplyEnvironmentManagedActionRequest.make ?environmentName
              ?environmentId ~actionId ())
           (Some Values.ApplyEnvironmentManagedActionResult.to_json)
           (Some Values.ApplyEnvironmentManagedActionResult.error_to_json)])
let associate_environment_operations_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (required string)
           ~doc:"STRING EnvironmentName"
       and operationsRole =
         flag "operations-role" (required string)
           ~doc:"STRING OperationsRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_environment_operations_role
           (Values.AssociateEnvironmentOperationsRoleMessage.make
              ~environmentName ~operationsRole ()) None None])
let check_d_n_s_availability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cNAMEPrefix =
         flag "c-n-a-m-e-prefix" (required string)
           ~doc:"STRING DNSCnamePrefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_d_n_s_availability
           (Values.CheckDNSAvailabilityMessage.make ~cNAMEPrefix ())
           (Some Values.CheckDNSAvailabilityResultMessage.to_json)
           (Some Values.CheckDNSAvailabilityResultMessage.error_to_json)])
let compose_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (optional string)
           ~doc:"STRING ApplicationName"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and versionLabels =
         flag "version-labels" (optional json_arg) ~doc:"JSON VersionLabels" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.compose_environments
           (Values.ComposeEnvironmentsMessage.make ?applicationName
              ?groupName
              ?versionLabels:(Option.map ~f:Values.VersionLabels.of_json
                                versionLabels) ())
           (Some Values.EnvironmentDescriptionsMessage.to_json)
           (Some Values.EnvironmentDescriptionsMessage.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and resourceLifecycleConfig =
         flag "resource-lifecycle-config" (optional json_arg)
           ~doc:"JSON ApplicationResourceLifecycleConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationMessage.make ?description
              ?resourceLifecycleConfig:(Option.map
                                          ~f:Values.ApplicationResourceLifecycleConfig.of_json
                                          resourceLifecycleConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~applicationName
              ()) (Some Values.ApplicationDescriptionMessage.to_json)
           (Some Values.ApplicationDescriptionMessage.error_to_json)])
let create_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and sourceBuildInformation =
         flag "source-build-information" (optional json_arg)
           ~doc:"JSON SourceBuildInformation"
       and sourceBundle =
         flag "source-bundle" (optional json_arg) ~doc:"JSON S3Location"
       and buildConfiguration =
         flag "build-configuration" (optional json_arg)
           ~doc:"JSON BuildConfiguration"
       and autoCreateApplication =
         flag "auto-create-application" (optional bool)
           ~doc:"BOOL AutoCreateApplication"
       and process =
         flag "process" (optional bool)
           ~doc:"BOOL ApplicationVersionProccess"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and versionLabel =
         flag "version-label" (required string) ~doc:"STRING VersionLabel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application_version
           (Values.CreateApplicationVersionMessage.make ?description
              ?sourceBuildInformation:(Option.map
                                         ~f:Values.SourceBuildInformation.of_json
                                         sourceBuildInformation)
              ?sourceBundle:(Option.map ~f:Values.S3Location.of_json
                               sourceBundle)
              ?buildConfiguration:(Option.map
                                     ~f:Values.BuildConfiguration.of_json
                                     buildConfiguration)
              ?autoCreateApplication ?process
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~applicationName
              ~versionLabel ())
           (Some Values.ApplicationVersionDescriptionMessage.to_json)
           (Some Values.ApplicationVersionDescriptionMessage.error_to_json)])
let create_configuration_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionStackName =
         flag "solution-stack-name" (optional string)
           ~doc:"STRING SolutionStackName"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn"
       and sourceConfiguration =
         flag "source-configuration" (optional json_arg)
           ~doc:"JSON SourceConfiguration"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and optionSettings =
         flag "option-settings" (optional json_arg)
           ~doc:"JSON ConfigurationOptionSettingsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING ConfigurationTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_template
           (Values.CreateConfigurationTemplateMessage.make ?solutionStackName
              ?platformArn
              ?sourceConfiguration:(Option.map
                                      ~f:Values.SourceConfiguration.of_json
                                      sourceConfiguration) ?environmentId
              ?description
              ?optionSettings:(Option.map
                                 ~f:Values.ConfigurationOptionSettingsList.of_json
                                 optionSettings)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~applicationName
              ~templateName ())
           (Some Values.ConfigurationSettingsDescription.to_json)
           (Some Values.ConfigurationSettingsDescription.error_to_json)])
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and cNAMEPrefix =
         flag "c-n-a-m-e-prefix" (optional string)
           ~doc:"STRING DNSCnamePrefix"
       and tier = flag "tier" (optional json_arg) ~doc:"JSON EnvironmentTier"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and versionLabel =
         flag "version-label" (optional string) ~doc:"STRING VersionLabel"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ConfigurationTemplateName"
       and solutionStackName =
         flag "solution-stack-name" (optional string)
           ~doc:"STRING SolutionStackName"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn"
       and optionSettings =
         flag "option-settings" (optional json_arg)
           ~doc:"JSON ConfigurationOptionSettingsList"
       and optionsToRemove =
         flag "options-to-remove" (optional json_arg)
           ~doc:"JSON OptionsSpecifierList"
       and operationsRole =
         flag "operations-role" (optional string)
           ~doc:"STRING OperationsRole"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentMessage.make ?environmentName ?groupName
              ?description ?cNAMEPrefix
              ?tier:(Option.map ~f:Values.EnvironmentTier.of_json tier)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?versionLabel
              ?templateName ?solutionStackName ?platformArn
              ?optionSettings:(Option.map
                                 ~f:Values.ConfigurationOptionSettingsList.of_json
                                 optionSettings)
              ?optionsToRemove:(Option.map
                                  ~f:Values.OptionsSpecifierList.of_json
                                  optionsToRemove) ?operationsRole
              ~applicationName ())
           (Some Values.EnvironmentDescription.to_json)
           (Some Values.EnvironmentDescription.error_to_json)])
let create_platform_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and optionSettings =
         flag "option-settings" (optional json_arg)
           ~doc:"JSON ConfigurationOptionSettingsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and platformName =
         flag "platform-name" (required string) ~doc:"STRING PlatformName"
       and platformVersion =
         flag "platform-version" (required string)
           ~doc:"STRING PlatformVersion"
       and platformDefinitionBundle =
         flag "platform-definition-bundle" (required json_arg)
           ~doc:"JSON S3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_platform_version
           (Values.CreatePlatformVersionRequest.make ?environmentName
              ?optionSettings:(Option.map
                                 ~f:Values.ConfigurationOptionSettingsList.of_json
                                 optionSettings)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~platformName
              ~platformVersion
              ~platformDefinitionBundle:(Values.S3Location.of_json
                                           platformDefinitionBundle) ())
           (Some Values.CreatePlatformVersionResult.to_json)
           (Some Values.CreatePlatformVersionResult.error_to_json)])
let create_storage_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_storage_location (Fn.id ())
           (Some Values.CreateStorageLocationResultMessage.to_json)
           (Some Values.CreateStorageLocationResultMessage.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and terminateEnvByForce =
         flag "terminate-env-by-force" (optional bool)
           ~doc:"BOOL TerminateEnvForce"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationMessage.make ?terminateEnvByForce
              ~applicationName ()) None None])
let delete_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteSourceBundle =
         flag "delete-source-bundle" (optional bool)
           ~doc:"BOOL DeleteSourceBundle"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and versionLabel =
         flag "version-label" (required string) ~doc:"STRING VersionLabel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application_version
           (Values.DeleteApplicationVersionMessage.make ?deleteSourceBundle
              ~applicationName ~versionLabel ()) None None])
let delete_configuration_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING ConfigurationTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_template
           (Values.DeleteConfigurationTemplateMessage.make ~applicationName
              ~templateName ()) None None])
let delete_environment_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and environmentName =
         flag "environment-name" (required string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_configuration
           (Values.DeleteEnvironmentConfigurationMessage.make
              ~applicationName ~environmentName ()) None None])
let delete_platform_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_platform_version
           (Values.DeletePlatformVersionRequest.make ?platformArn ())
           (Some Values.DeletePlatformVersionResult.to_json)
           (Some Values.DeletePlatformVersionResult.error_to_json)])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes (Fn.id ())
           (Some Values.DescribeAccountAttributesResult.to_json)
           (Some Values.DescribeAccountAttributesResult.error_to_json)])
let describe_application_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (optional string)
           ~doc:"STRING ApplicationName"
       and versionLabels =
         flag "version-labels" (optional json_arg)
           ~doc:"JSON VersionLabelsList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_application_versions
           (Values.DescribeApplicationVersionsMessage.make ?applicationName
              ?versionLabels:(Option.map ~f:Values.VersionLabelsList.of_json
                                versionLabels) ?maxRecords ?nextToken ())
           (Some Values.ApplicationVersionDescriptionsMessage.to_json)
           (Some Values.ApplicationVersionDescriptionsMessage.error_to_json)])
let describe_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationNames =
         flag "application-names" (optional json_arg)
           ~doc:"JSON ApplicationNamesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_applications
           (Values.DescribeApplicationsMessage.make
              ?applicationNames:(Option.map
                                   ~f:Values.ApplicationNamesList.of_json
                                   applicationNames) ())
           (Some Values.ApplicationDescriptionsMessage.to_json)
           (Some Values.ApplicationDescriptionsMessage.error_to_json)])
let describe_configuration_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (optional string)
           ~doc:"STRING ApplicationName"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ConfigurationTemplateName"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and solutionStackName =
         flag "solution-stack-name" (optional string)
           ~doc:"STRING SolutionStackName"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn"
       and options =
         flag "options" (optional json_arg) ~doc:"JSON OptionsSpecifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration_options
           (Values.DescribeConfigurationOptionsMessage.make ?applicationName
              ?templateName ?environmentName ?solutionStackName ?platformArn
              ?options:(Option.map ~f:Values.OptionsSpecifierList.of_json
                          options) ())
           (Some Values.ConfigurationOptionsDescription.to_json)
           (Some Values.ConfigurationOptionsDescription.error_to_json)])
let describe_configuration_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ConfigurationTemplateName"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration_settings
           (Values.DescribeConfigurationSettingsMessage.make ?templateName
              ?environmentName ~applicationName ())
           (Some Values.ConfigurationSettingsDescriptions.to_json)
           (Some Values.ConfigurationSettingsDescriptions.error_to_json)])
let describe_environment_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON EnvironmentHealthAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environment_health
           (Values.DescribeEnvironmentHealthRequest.make ?environmentName
              ?environmentId
              ?attributeNames:(Option.map
                                 ~f:Values.EnvironmentHealthAttributes.of_json
                                 attributeNames) ())
           (Some Values.DescribeEnvironmentHealthResult.to_json)
           (Some Values.DescribeEnvironmentHealthResult.error_to_json)])
let describe_environment_managed_action_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT ManagedActionHistoryMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environment_managed_action_history
           (Values.DescribeEnvironmentManagedActionHistoryRequest.make
              ?environmentId ?environmentName ?nextToken ?maxItems ())
           (Some Values.DescribeEnvironmentManagedActionHistoryResult.to_json)
           (Some
              Values.DescribeEnvironmentManagedActionHistoryResult.error_to_json)])
let describe_environment_managed_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string) ~doc:"STRING String"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ActionStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environment_managed_actions
           (Values.DescribeEnvironmentManagedActionsRequest.make
              ?environmentName ?environmentId
              ?status:(Option.map ~f:Values.ActionStatus.of_json status) ())
           (Some Values.DescribeEnvironmentManagedActionsResult.to_json)
           (Some Values.DescribeEnvironmentManagedActionsResult.error_to_json)])
let describe_environment_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environment_resources
           (Values.DescribeEnvironmentResourcesMessage.make ?environmentId
              ?environmentName ())
           (Some Values.EnvironmentResourceDescriptionsMessage.to_json)
           (Some Values.EnvironmentResourceDescriptionsMessage.error_to_json)])
let describe_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (optional string)
           ~doc:"STRING ApplicationName"
       and versionLabel =
         flag "version-label" (optional string) ~doc:"STRING VersionLabel"
       and environmentIds =
         flag "environment-ids" (optional json_arg)
           ~doc:"JSON EnvironmentIdList"
       and environmentNames =
         flag "environment-names" (optional json_arg)
           ~doc:"JSON EnvironmentNamesList"
       and includeDeleted =
         flag "include-deleted" (optional bool) ~doc:"BOOL IncludeDeleted"
       and includedDeletedBackTo =
         flag "included-deleted-back-to" (optional json_arg)
           ~doc:"JSON IncludeDeletedBackTo"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environments
           (Values.DescribeEnvironmentsMessage.make ?applicationName
              ?versionLabel
              ?environmentIds:(Option.map ~f:Values.EnvironmentIdList.of_json
                                 environmentIds)
              ?environmentNames:(Option.map
                                   ~f:Values.EnvironmentNamesList.of_json
                                   environmentNames) ?includeDeleted
              ?includedDeletedBackTo:(Option.map
                                        ~f:Values.IncludeDeletedBackTo.of_json
                                        includedDeletedBackTo) ?maxRecords
              ?nextToken ())
           (Some Values.EnvironmentDescriptionsMessage.to_json)
           (Some Values.EnvironmentDescriptionsMessage.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (optional string)
           ~doc:"STRING ApplicationName"
       and versionLabel =
         flag "version-label" (optional string) ~doc:"STRING VersionLabel"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ConfigurationTemplateName"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING RequestId"
       and severity =
         flag "severity" (optional json_arg) ~doc:"JSON EventSeverity"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimeFilterStart"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimeFilterEnd"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsMessage.make ?applicationName ?versionLabel
              ?templateName ?environmentId ?environmentName ?platformArn
              ?requestId
              ?severity:(Option.map ~f:Values.EventSeverity.of_json severity)
              ?startTime:(Option.map ~f:Values.TimeFilterStart.of_json
                            startTime)
              ?endTime:(Option.map ~f:Values.TimeFilterEnd.of_json endTime)
              ?maxRecords ?nextToken ())
           (Some Values.EventDescriptionsMessage.to_json)
           (Some Values.EventDescriptionsMessage.error_to_json)])
let describe_instances_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON InstancesHealthAttributes"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instances_health
           (Values.DescribeInstancesHealthRequest.make ?environmentName
              ?environmentId
              ?attributeNames:(Option.map
                                 ~f:Values.InstancesHealthAttributes.of_json
                                 attributeNames) ?nextToken ())
           (Some Values.DescribeInstancesHealthResult.to_json)
           (Some Values.DescribeInstancesHealthResult.error_to_json)])
let describe_platform_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_platform_version
           (Values.DescribePlatformVersionRequest.make ?platformArn ())
           (Some Values.DescribePlatformVersionResult.to_json)
           (Some Values.DescribePlatformVersionResult.error_to_json)])
let disassociate_environment_operations_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (required string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_environment_operations_role
           (Values.DisassociateEnvironmentOperationsRoleMessage.make
              ~environmentName ()) None None])
let list_available_solution_stacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_available_solution_stacks (Fn.id ())
           (Some Values.ListAvailableSolutionStacksResultMessage.to_json)
           (Some
              Values.ListAvailableSolutionStacksResultMessage.error_to_json)])
let list_platform_branches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchFilters"
       and maxRecords =
         flag "max-records" (optional int)
           ~doc:"INT PlatformBranchMaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_platform_branches
           (Values.ListPlatformBranchesRequest.make
              ?filters:(Option.map ~f:Values.SearchFilters.of_json filters)
              ?maxRecords ?nextToken ())
           (Some Values.ListPlatformBranchesResult.to_json)
           (Some Values.ListPlatformBranchesResult.error_to_json)])
let list_platform_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON PlatformFilters"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT PlatformMaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_platform_versions
           (Values.ListPlatformVersionsRequest.make
              ?filters:(Option.map ~f:Values.PlatformFilters.of_json filters)
              ?maxRecords ?nextToken ())
           (Some Values.ListPlatformVersionsResult.to_json)
           (Some Values.ListPlatformVersionsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceMessage.make ~resourceArn ())
           (Some Values.ResourceTagsDescriptionMessage.to_json)
           (Some Values.ResourceTagsDescriptionMessage.error_to_json)])
let rebuild_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rebuild_environment
           (Values.RebuildEnvironmentMessage.make ?environmentId
              ?environmentName ()) None None])
let request_environment_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and infoType =
         flag "info-type" (required json_arg) ~doc:"JSON EnvironmentInfoType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.request_environment_info
           (Values.RequestEnvironmentInfoMessage.make ?environmentId
              ?environmentName
              ~infoType:(Values.EnvironmentInfoType.of_json infoType) ())
           None None])
let restart_app_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restart_app_server
           (Values.RestartAppServerMessage.make ?environmentId
              ?environmentName ()) None None])
let retrieve_environment_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and infoType =
         flag "info-type" (required json_arg) ~doc:"JSON EnvironmentInfoType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retrieve_environment_info
           (Values.RetrieveEnvironmentInfoMessage.make ?environmentId
              ?environmentName
              ~infoType:(Values.EnvironmentInfoType.of_json infoType) ())
           (Some Values.RetrieveEnvironmentInfoResultMessage.to_json)
           (Some Values.RetrieveEnvironmentInfoResultMessage.error_to_json)])
let swap_environment_c_n_a_m_es =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceEnvironmentId =
         flag "source-environment-id" (optional string)
           ~doc:"STRING EnvironmentId"
       and sourceEnvironmentName =
         flag "source-environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and destinationEnvironmentId =
         flag "destination-environment-id" (optional string)
           ~doc:"STRING EnvironmentId"
       and destinationEnvironmentName =
         flag "destination-environment-name" (optional string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.swap_environment_c_n_a_m_es
           (Values.SwapEnvironmentCNAMEsMessage.make ?sourceEnvironmentId
              ?sourceEnvironmentName ?destinationEnvironmentId
              ?destinationEnvironmentName ()) None None])
let terminate_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and terminateResources =
         flag "terminate-resources" (optional bool)
           ~doc:"BOOL TerminateEnvironmentResources"
       and forceTerminate =
         flag "force-terminate" (optional bool) ~doc:"BOOL ForceTerminate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_environment
           (Values.TerminateEnvironmentMessage.make ?environmentId
              ?environmentName ?terminateResources ?forceTerminate ())
           (Some Values.EnvironmentDescription.to_json)
           (Some Values.EnvironmentDescription.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationMessage.make ?description
              ~applicationName ())
           (Some Values.ApplicationDescriptionMessage.to_json)
           (Some Values.ApplicationDescriptionMessage.error_to_json)])
let update_application_resource_lifecycle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and resourceLifecycleConfig =
         flag "resource-lifecycle-config" (required json_arg)
           ~doc:"JSON ApplicationResourceLifecycleConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application_resource_lifecycle
           (Values.UpdateApplicationResourceLifecycleMessage.make
              ~applicationName
              ~resourceLifecycleConfig:(Values.ApplicationResourceLifecycleConfig.of_json
                                          resourceLifecycleConfig) ())
           (Some
              Values.ApplicationResourceLifecycleDescriptionMessage.to_json)
           (Some
              Values.ApplicationResourceLifecycleDescriptionMessage.error_to_json)])
let update_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and versionLabel =
         flag "version-label" (required string) ~doc:"STRING VersionLabel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application_version
           (Values.UpdateApplicationVersionMessage.make ?description
              ~applicationName ~versionLabel ())
           (Some Values.ApplicationVersionDescriptionMessage.to_json)
           (Some Values.ApplicationVersionDescriptionMessage.error_to_json)])
let update_configuration_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and optionSettings =
         flag "option-settings" (optional json_arg)
           ~doc:"JSON ConfigurationOptionSettingsList"
       and optionsToRemove =
         flag "options-to-remove" (optional json_arg)
           ~doc:"JSON OptionsSpecifierList"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING ConfigurationTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_template
           (Values.UpdateConfigurationTemplateMessage.make ?description
              ?optionSettings:(Option.map
                                 ~f:Values.ConfigurationOptionSettingsList.of_json
                                 optionSettings)
              ?optionsToRemove:(Option.map
                                  ~f:Values.OptionsSpecifierList.of_json
                                  optionsToRemove) ~applicationName
              ~templateName ())
           (Some Values.ConfigurationSettingsDescription.to_json)
           (Some Values.ConfigurationSettingsDescription.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationName =
         flag "application-name" (optional string)
           ~doc:"STRING ApplicationName"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tier = flag "tier" (optional json_arg) ~doc:"JSON EnvironmentTier"
       and versionLabel =
         flag "version-label" (optional string) ~doc:"STRING VersionLabel"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ConfigurationTemplateName"
       and solutionStackName =
         flag "solution-stack-name" (optional string)
           ~doc:"STRING SolutionStackName"
       and platformArn =
         flag "platform-arn" (optional string) ~doc:"STRING PlatformArn"
       and optionSettings =
         flag "option-settings" (optional json_arg)
           ~doc:"JSON ConfigurationOptionSettingsList"
       and optionsToRemove =
         flag "options-to-remove" (optional json_arg)
           ~doc:"JSON OptionsSpecifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentMessage.make ?applicationName
              ?environmentId ?environmentName ?groupName ?description
              ?tier:(Option.map ~f:Values.EnvironmentTier.of_json tier)
              ?versionLabel ?templateName ?solutionStackName ?platformArn
              ?optionSettings:(Option.map
                                 ~f:Values.ConfigurationOptionSettingsList.of_json
                                 optionSettings)
              ?optionsToRemove:(Option.map
                                  ~f:Values.OptionsSpecifierList.of_json
                                  optionsToRemove) ())
           (Some Values.EnvironmentDescription.to_json)
           (Some Values.EnvironmentDescription.error_to_json)])
let update_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagsToAdd =
         flag "tags-to-add" (optional json_arg) ~doc:"JSON TagList"
       and tagsToRemove =
         flag "tags-to-remove" (optional json_arg) ~doc:"JSON TagKeyList"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_tags_for_resource
           (Values.UpdateTagsForResourceMessage.make
              ?tagsToAdd:(Option.map ~f:Values.TagList.of_json tagsToAdd)
              ?tagsToRemove:(Option.map ~f:Values.TagKeyList.of_json
                               tagsToRemove) ~resourceArn ()) None None])
let validate_configuration_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ConfigurationTemplateName"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and applicationName =
         flag "application-name" (required string)
           ~doc:"STRING ApplicationName"
       and optionSettings =
         flag "option-settings" (required json_arg)
           ~doc:"JSON ConfigurationOptionSettingsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_configuration_settings
           (Values.ValidateConfigurationSettingsMessage.make ?templateName
              ?environmentName ~applicationName
              ~optionSettings:(Values.ConfigurationOptionSettingsList.of_json
                                 optionSettings) ())
           (Some Values.ConfigurationSettingsValidationMessages.to_json)
           (Some Values.ConfigurationSettingsValidationMessages.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("abort-environment-update", abort_environment_update);
    ("apply-environment-managed-action", apply_environment_managed_action);
    ("associate-environment-operations-role",
      associate_environment_operations_role);
    ("check-d-n-s-availability", check_d_n_s_availability);
    ("compose-environments", compose_environments);
    ("create-application", create_application);
    ("create-application-version", create_application_version);
    ("create-configuration-template", create_configuration_template);
    ("create-environment", create_environment);
    ("create-platform-version", create_platform_version);
    ("create-storage-location", create_storage_location);
    ("delete-application", delete_application);
    ("delete-application-version", delete_application_version);
    ("delete-configuration-template", delete_configuration_template);
    ("delete-environment-configuration", delete_environment_configuration);
    ("delete-platform-version", delete_platform_version);
    ("describe-account-attributes", describe_account_attributes);
    ("describe-application-versions", describe_application_versions);
    ("describe-applications", describe_applications);
    ("describe-configuration-options", describe_configuration_options);
    ("describe-configuration-settings", describe_configuration_settings);
    ("describe-environment-health", describe_environment_health);
    ("describe-environment-managed-action-history",
      describe_environment_managed_action_history);
    ("describe-environment-managed-actions",
      describe_environment_managed_actions);
    ("describe-environment-resources", describe_environment_resources);
    ("describe-environments", describe_environments);
    ("describe-events", describe_events);
    ("describe-instances-health", describe_instances_health);
    ("describe-platform-version", describe_platform_version);
    ("disassociate-environment-operations-role",
      disassociate_environment_operations_role);
    ("list-available-solution-stacks", list_available_solution_stacks);
    ("list-platform-branches", list_platform_branches);
    ("list-platform-versions", list_platform_versions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("rebuild-environment", rebuild_environment);
    ("request-environment-info", request_environment_info);
    ("restart-app-server", restart_app_server);
    ("retrieve-environment-info", retrieve_environment_info);
    ("swap-environment-c-n-a-m-es", swap_environment_c_n_a_m_es);
    ("terminate-environment", terminate_environment);
    ("update-application", update_application);
    ("update-application-resource-lifecycle",
      update_application_resource_lifecycle);
    ("update-application-version", update_application_version);
    ("update-configuration-template", update_configuration_template);
    ("update-environment", update_environment);
    ("update-tags-for-resource", update_tags_for_resource);
    ("validate-configuration-settings", validate_configuration_settings)]
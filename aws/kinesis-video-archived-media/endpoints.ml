(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetClip: (GetClipInput.t, GetClipOutput.t, GetClipOutput.error) t 
  | GetDASHStreamingSessionURL: (GetDASHStreamingSessionURLInput.t,
  GetDASHStreamingSessionURLOutput.t, GetDASHStreamingSessionURLOutput.error)
  t 
  | GetHLSStreamingSessionURL: (GetHLSStreamingSessionURLInput.t,
  GetHLSStreamingSessionURLOutput.t, GetHLSStreamingSessionURLOutput.error) t
  
  | GetMediaForFragmentList: (GetMediaForFragmentListInput.t,
  GetMediaForFragmentListOutput.t, GetMediaForFragmentListOutput.error) t 
  | ListFragments: (ListFragmentsInput.t, ListFragmentsOutput.t,
  ListFragmentsOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetClip -> `POST
  | GetDASHStreamingSessionURL -> `POST
  | GetHLSStreamingSessionURL -> `POST
  | GetMediaForFragmentList -> `POST
  | ListFragments -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetClip -> (Format.kasprintf Uri.of_string) "/getClip"
      | GetDASHStreamingSessionURL ->
          (Format.kasprintf Uri.of_string) "/getDASHStreamingSessionURL"
      | GetHLSStreamingSessionURL ->
          (Format.kasprintf Uri.of_string) "/getHLSStreamingSessionURL"
      | GetMediaForFragmentList ->
          (Format.kasprintf Uri.of_string) "/getMediaForFragmentList"
      | ListFragments -> (Format.kasprintf Uri.of_string) "/listFragments")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetClip ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetClipInput.streamName
                         ~f:(fun x -> ("StreamName", (StreamName.to_value x)));
                      Option.map req.GetClipInput.streamARN
                        ~f:(fun x -> ("StreamARN", (ResourceARN.to_value x)));
                      Some
                        ("ClipFragmentSelector",
                          (ClipFragmentSelector.to_value
                             req.GetClipInput.clipFragmentSelector))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetDASHStreamingSessionURL ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.GetDASHStreamingSessionURLInput.streamName
                         ~f:(fun x -> ("StreamName", (StreamName.to_value x)));
                      Option.map
                        req.GetDASHStreamingSessionURLInput.streamARN
                        ~f:(fun x -> ("StreamARN", (ResourceARN.to_value x)));
                      Option.map
                        req.GetDASHStreamingSessionURLInput.playbackMode
                        ~f:(fun x ->
                              ("PlaybackMode", (DASHPlaybackMode.to_value x)));
                      Option.map
                        req.GetDASHStreamingSessionURLInput.displayFragmentTimestamp
                        ~f:(fun x ->
                              ("DisplayFragmentTimestamp",
                                (DASHDisplayFragmentTimestamp.to_value x)));
                      Option.map
                        req.GetDASHStreamingSessionURLInput.displayFragmentNumber
                        ~f:(fun x ->
                              ("DisplayFragmentNumber",
                                (DASHDisplayFragmentNumber.to_value x)));
                      Option.map
                        req.GetDASHStreamingSessionURLInput.dASHFragmentSelector
                        ~f:(fun x ->
                              ("DASHFragmentSelector",
                                (DASHFragmentSelector.to_value x)));
                      Option.map req.GetDASHStreamingSessionURLInput.expires
                        ~f:(fun x -> ("Expires", (Expires.to_value x)));
                      Option.map
                        req.GetDASHStreamingSessionURLInput.maxManifestFragmentResults
                        ~f:(fun x ->
                              ("MaxManifestFragmentResults",
                                (DASHMaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetHLSStreamingSessionURL ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.GetHLSStreamingSessionURLInput.streamName
                         ~f:(fun x -> ("StreamName", (StreamName.to_value x)));
                      Option.map req.GetHLSStreamingSessionURLInput.streamARN
                        ~f:(fun x -> ("StreamARN", (ResourceARN.to_value x)));
                      Option.map
                        req.GetHLSStreamingSessionURLInput.playbackMode
                        ~f:(fun x ->
                              ("PlaybackMode", (HLSPlaybackMode.to_value x)));
                      Option.map
                        req.GetHLSStreamingSessionURLInput.hLSFragmentSelector
                        ~f:(fun x ->
                              ("HLSFragmentSelector",
                                (HLSFragmentSelector.to_value x)));
                      Option.map
                        req.GetHLSStreamingSessionURLInput.containerFormat
                        ~f:(fun x ->
                              ("ContainerFormat",
                                (ContainerFormat.to_value x)));
                      Option.map
                        req.GetHLSStreamingSessionURLInput.discontinuityMode
                        ~f:(fun x ->
                              ("DiscontinuityMode",
                                (HLSDiscontinuityMode.to_value x)));
                      Option.map
                        req.GetHLSStreamingSessionURLInput.displayFragmentTimestamp
                        ~f:(fun x ->
                              ("DisplayFragmentTimestamp",
                                (HLSDisplayFragmentTimestamp.to_value x)));
                      Option.map req.GetHLSStreamingSessionURLInput.expires
                        ~f:(fun x -> ("Expires", (Expires.to_value x)));
                      Option.map
                        req.GetHLSStreamingSessionURLInput.maxMediaPlaylistFragmentResults
                        ~f:(fun x ->
                              ("MaxMediaPlaylistFragmentResults",
                                (HLSMaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetMediaForFragmentList ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetMediaForFragmentListInput.streamName
                         ~f:(fun x -> ("StreamName", (StreamName.to_value x)));
                      Option.map req.GetMediaForFragmentListInput.streamARN
                        ~f:(fun x -> ("StreamARN", (ResourceARN.to_value x)));
                      Some
                        ("Fragments",
                          (FragmentNumberList.to_value
                             req.GetMediaForFragmentListInput.fragments))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFragments ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListFragmentsInput.streamName
                         ~f:(fun x -> ("StreamName", (StreamName.to_value x)));
                      Option.map req.ListFragmentsInput.streamARN
                        ~f:(fun x -> ("StreamARN", (ResourceARN.to_value x)));
                      Option.map req.ListFragmentsInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListFragmentsMaxResults.to_value x)));
                      Option.map req.ListFragmentsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListFragmentsInput.fragmentSelector
                        ~f:(fun x ->
                              ("FragmentSelector",
                                (FragmentSelector.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | GetClip ->
      if is_success
      then
        let body = Payload.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetClipOutput.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some GetClipOutput.error_of_json))
  | GetDASHStreamingSessionURL ->
      if is_success
      then
        Ok (GetDASHStreamingSessionURLOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetDASHStreamingSessionURLOutput.error_of_json))
  | GetHLSStreamingSessionURL ->
      if is_success
      then
        Ok (GetHLSStreamingSessionURLOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetHLSStreamingSessionURLOutput.error_of_json))
  | GetMediaForFragmentList ->
      if is_success
      then
        let body = Payload.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetMediaForFragmentListOutput.of_header_and_body (headers, body))
      else
        Error
          (parse_aws_error (Some GetMediaForFragmentListOutput.error_of_json))
  | ListFragments ->
      if is_success
      then Ok (ListFragmentsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListFragmentsOutput.error_of_json))
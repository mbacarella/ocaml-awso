(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AppName"
       and description =
         flag "description" (optional string) ~doc:"STRING AppDescription"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and serverGroups =
         flag "server-groups" (optional json_arg) ~doc:"JSON ServerGroups"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app
           (Values.CreateAppRequest.make ?name ?description ?roleName
              ?clientToken
              ?serverGroups:(Option.map ~f:Values.ServerGroups.of_json
                               serverGroups)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateAppResponse.to_json)
           (Some Values.CreateAppResponse.error_to_json)])
let create_replication_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and frequency = flag "frequency" (optional int) ~doc:"INT Frequency"
       and runOnce = flag "run-once" (optional bool) ~doc:"BOOL RunOnce"
       and licenseType =
         flag "license-type" (optional json_arg) ~doc:"JSON LicenseType"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and numberOfRecentAmisToKeep =
         flag "number-of-recent-amis-to-keep" (optional int)
           ~doc:"INT NumberOfRecentAmisToKeep"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Encrypted"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and seedReplicationTime =
         flag "seed-replication-time" (required json_arg)
           ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_job
           (Values.CreateReplicationJobRequest.make ?frequency ?runOnce
              ?licenseType:(Option.map ~f:Values.LicenseType.of_json
                              licenseType) ?roleName ?description
              ?numberOfRecentAmisToKeep ?encrypted ?kmsKeyId ~serverId
              ~seedReplicationTime:(Values.Timestamp.of_json
                                      seedReplicationTime) ())
           (Some Values.CreateReplicationJobResponse.to_json)
           (Some Values.CreateReplicationJobResponse.error_to_json)])
let delete_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId"
       and forceStopAppReplication =
         flag "force-stop-app-replication" (optional bool)
           ~doc:"BOOL ForceStopAppReplication"
       and forceTerminateApp =
         flag "force-terminate-app" (optional bool)
           ~doc:"BOOL ForceTerminateApp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app
           (Values.DeleteAppRequest.make ?appId ?forceStopAppReplication
              ?forceTerminateApp ()) (Some Values.DeleteAppResponse.to_json)
           (Some Values.DeleteAppResponse.error_to_json)])
let delete_app_launch_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_launch_configuration
           (Values.DeleteAppLaunchConfigurationRequest.make ?appId ())
           (Some Values.DeleteAppLaunchConfigurationResponse.to_json)
           (Some Values.DeleteAppLaunchConfigurationResponse.error_to_json)])
let delete_app_replication_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_replication_configuration
           (Values.DeleteAppReplicationConfigurationRequest.make ?appId ())
           (Some Values.DeleteAppReplicationConfigurationResponse.to_json)
           (Some
              Values.DeleteAppReplicationConfigurationResponse.error_to_json)])
let delete_app_validation_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId =
         flag "app-id" (required string) ~doc:"STRING AppIdWithValidation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_validation_configuration
           (Values.DeleteAppValidationConfigurationRequest.make ~appId ())
           (Some Values.DeleteAppValidationConfigurationResponse.to_json)
           (Some
              Values.DeleteAppValidationConfigurationResponse.error_to_json)])
let delete_replication_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationJobId =
         flag "replication-job-id" (required string)
           ~doc:"STRING ReplicationJobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_job
           (Values.DeleteReplicationJobRequest.make ~replicationJobId ())
           (Some Values.DeleteReplicationJobResponse.to_json)
           (Some Values.DeleteReplicationJobResponse.error_to_json)])
let delete_server_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_server_catalog
           (Values.DeleteServerCatalogRequest.make ())
           (Some Values.DeleteServerCatalogResponse.to_json)
           (Some Values.DeleteServerCatalogResponse.error_to_json)])
let disassociate_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_connector
           (Values.DisassociateConnectorRequest.make ~connectorId ())
           (Some Values.DisassociateConnectorResponse.to_json)
           (Some Values.DisassociateConnectorResponse.error_to_json)])
let generate_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId"
       and changesetFormat =
         flag "changeset-format" (optional json_arg) ~doc:"JSON OutputFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_change_set
           (Values.GenerateChangeSetRequest.make ?appId
              ?changesetFormat:(Option.map ~f:Values.OutputFormat.of_json
                                  changesetFormat) ())
           (Some Values.GenerateChangeSetResponse.to_json)
           (Some Values.GenerateChangeSetResponse.error_to_json)])
let generate_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId"
       and templateFormat =
         flag "template-format" (optional json_arg) ~doc:"JSON OutputFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_template
           (Values.GenerateTemplateRequest.make ?appId
              ?templateFormat:(Option.map ~f:Values.OutputFormat.of_json
                                 templateFormat) ())
           (Some Values.GenerateTemplateResponse.to_json)
           (Some Values.GenerateTemplateResponse.error_to_json)])
let get_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app (Values.GetAppRequest.make ?appId ())
           (Some Values.GetAppResponse.to_json)
           (Some Values.GetAppResponse.error_to_json)])
let get_app_launch_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_launch_configuration
           (Values.GetAppLaunchConfigurationRequest.make ?appId ())
           (Some Values.GetAppLaunchConfigurationResponse.to_json)
           (Some Values.GetAppLaunchConfigurationResponse.error_to_json)])
let get_app_replication_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_replication_configuration
           (Values.GetAppReplicationConfigurationRequest.make ?appId ())
           (Some Values.GetAppReplicationConfigurationResponse.to_json)
           (Some Values.GetAppReplicationConfigurationResponse.error_to_json)])
let get_app_validation_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId =
         flag "app-id" (required string) ~doc:"STRING AppIdWithValidation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_validation_configuration
           (Values.GetAppValidationConfigurationRequest.make ~appId ())
           (Some Values.GetAppValidationConfigurationResponse.to_json)
           (Some Values.GetAppValidationConfigurationResponse.error_to_json)])
let get_app_validation_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId =
         flag "app-id" (required string) ~doc:"STRING AppIdWithValidation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_validation_output
           (Values.GetAppValidationOutputRequest.make ~appId ())
           (Some Values.GetAppValidationOutputResponse.to_json)
           (Some Values.GetAppValidationOutputResponse.error_to_json)])
let get_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connectors
           (Values.GetConnectorsRequest.make ?nextToken ?maxResults ())
           (Some Values.GetConnectorsResponse.to_json)
           (Some Values.GetConnectorsResponse.error_to_json)])
let get_replication_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationJobId =
         flag "replication-job-id" (optional string)
           ~doc:"STRING ReplicationJobId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_replication_jobs
           (Values.GetReplicationJobsRequest.make ?replicationJobId
              ?nextToken ?maxResults ())
           (Some Values.GetReplicationJobsResponse.to_json)
           (Some Values.GetReplicationJobsResponse.error_to_json)])
let get_replication_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and replicationJobId =
         flag "replication-job-id" (required string)
           ~doc:"STRING ReplicationJobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_replication_runs
           (Values.GetReplicationRunsRequest.make ?nextToken ?maxResults
              ~replicationJobId ())
           (Some Values.GetReplicationRunsResponse.to_json)
           (Some Values.GetReplicationRunsResponse.error_to_json)])
let get_servers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and vmServerAddressList =
         flag "vm-server-address-list" (optional json_arg)
           ~doc:"JSON VmServerAddressList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_servers
           (Values.GetServersRequest.make ?nextToken ?maxResults
              ?vmServerAddressList:(Option.map
                                      ~f:Values.VmServerAddressList.of_json
                                      vmServerAddressList) ())
           (Some Values.GetServersResponse.to_json)
           (Some Values.GetServersResponse.error_to_json)])
let import_app_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_app_catalog
           (Values.ImportAppCatalogRequest.make ?roleName ())
           (Some Values.ImportAppCatalogResponse.to_json)
           (Some Values.ImportAppCatalogResponse.error_to_json)])
let import_server_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_server_catalog
           (Values.ImportServerCatalogRequest.make ())
           (Some Values.ImportServerCatalogResponse.to_json)
           (Some Values.ImportServerCatalogResponse.error_to_json)])
let launch_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.launch_app (Values.LaunchAppRequest.make ?appId ())
           (Some Values.LaunchAppResponse.to_json)
           (Some Values.LaunchAppResponse.error_to_json)])
let list_apps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appIds = flag "app-ids" (optional json_arg) ~doc:"JSON AppIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_apps
           (Values.ListAppsRequest.make
              ?appIds:(Option.map ~f:Values.AppIds.of_json appIds) ?nextToken
              ?maxResults ()) (Some Values.ListAppsResponse.to_json)
           (Some Values.ListAppsResponse.error_to_json)])
let notify_app_validation_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationContext =
         flag "notification-context" (optional json_arg)
           ~doc:"JSON NotificationContext"
       and appId =
         flag "app-id" (required string) ~doc:"STRING AppIdWithValidation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_app_validation_output
           (Values.NotifyAppValidationOutputRequest.make
              ?notificationContext:(Option.map
                                      ~f:Values.NotificationContext.of_json
                                      notificationContext) ~appId ())
           (Some Values.NotifyAppValidationOutputResponse.to_json)
           (Some Values.NotifyAppValidationOutputResponse.error_to_json)])
let put_app_launch_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and autoLaunch =
         flag "auto-launch" (optional bool) ~doc:"BOOL AutoLaunch"
       and serverGroupLaunchConfigurations =
         flag "server-group-launch-configurations" (optional json_arg)
           ~doc:"JSON ServerGroupLaunchConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_app_launch_configuration
           (Values.PutAppLaunchConfigurationRequest.make ?appId ?roleName
              ?autoLaunch
              ?serverGroupLaunchConfigurations:(Option.map
                                                  ~f:Values.ServerGroupLaunchConfigurations.of_json
                                                  serverGroupLaunchConfigurations)
              ()) (Some Values.PutAppLaunchConfigurationResponse.to_json)
           (Some Values.PutAppLaunchConfigurationResponse.error_to_json)])
let put_app_replication_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId"
       and serverGroupReplicationConfigurations =
         flag "server-group-replication-configurations" (optional json_arg)
           ~doc:"JSON ServerGroupReplicationConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_app_replication_configuration
           (Values.PutAppReplicationConfigurationRequest.make ?appId
              ?serverGroupReplicationConfigurations:(Option.map
                                                       ~f:Values.ServerGroupReplicationConfigurations.of_json
                                                       serverGroupReplicationConfigurations)
              ())
           (Some Values.PutAppReplicationConfigurationResponse.to_json)
           (Some Values.PutAppReplicationConfigurationResponse.error_to_json)])
let put_app_validation_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appValidationConfigurations =
         flag "app-validation-configurations" (optional json_arg)
           ~doc:"JSON AppValidationConfigurations"
       and serverGroupValidationConfigurations =
         flag "server-group-validation-configurations" (optional json_arg)
           ~doc:"JSON ServerGroupValidationConfigurations"
       and appId =
         flag "app-id" (required string) ~doc:"STRING AppIdWithValidation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_app_validation_configuration
           (Values.PutAppValidationConfigurationRequest.make
              ?appValidationConfigurations:(Option.map
                                              ~f:Values.AppValidationConfigurations.of_json
                                              appValidationConfigurations)
              ?serverGroupValidationConfigurations:(Option.map
                                                      ~f:Values.ServerGroupValidationConfigurations.of_json
                                                      serverGroupValidationConfigurations)
              ~appId ())
           (Some Values.PutAppValidationConfigurationResponse.to_json)
           (Some Values.PutAppValidationConfigurationResponse.error_to_json)])
let start_app_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_app_replication
           (Values.StartAppReplicationRequest.make ?appId ())
           (Some Values.StartAppReplicationResponse.to_json)
           (Some Values.StartAppReplicationResponse.error_to_json)])
let start_on_demand_app_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_on_demand_app_replication
           (Values.StartOnDemandAppReplicationRequest.make ?description
              ~appId ())
           (Some Values.StartOnDemandAppReplicationResponse.to_json)
           (Some Values.StartOnDemandAppReplicationResponse.error_to_json)])
let start_on_demand_replication_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and replicationJobId =
         flag "replication-job-id" (required string)
           ~doc:"STRING ReplicationJobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_on_demand_replication_run
           (Values.StartOnDemandReplicationRunRequest.make ?description
              ~replicationJobId ())
           (Some Values.StartOnDemandReplicationRunResponse.to_json)
           (Some Values.StartOnDemandReplicationRunResponse.error_to_json)])
let stop_app_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_app_replication
           (Values.StopAppReplicationRequest.make ?appId ())
           (Some Values.StopAppReplicationResponse.to_json)
           (Some Values.StopAppReplicationResponse.error_to_json)])
let terminate_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_app (Values.TerminateAppRequest.make ?appId ())
           (Some Values.TerminateAppResponse.to_json)
           (Some Values.TerminateAppResponse.error_to_json)])
let update_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING AppId"
       and name = flag "name" (optional string) ~doc:"STRING AppName"
       and description =
         flag "description" (optional string) ~doc:"STRING AppDescription"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and serverGroups =
         flag "server-groups" (optional json_arg) ~doc:"JSON ServerGroups"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app
           (Values.UpdateAppRequest.make ?appId ?name ?description ?roleName
              ?serverGroups:(Option.map ~f:Values.ServerGroups.of_json
                               serverGroups)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.UpdateAppResponse.to_json)
           (Some Values.UpdateAppResponse.error_to_json)])
let update_replication_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and frequency = flag "frequency" (optional int) ~doc:"INT Frequency"
       and nextReplicationRunStartTime =
         flag "next-replication-run-start-time" (optional json_arg)
           ~doc:"JSON Timestamp"
       and licenseType =
         flag "license-type" (optional json_arg) ~doc:"JSON LicenseType"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and numberOfRecentAmisToKeep =
         flag "number-of-recent-amis-to-keep" (optional int)
           ~doc:"INT NumberOfRecentAmisToKeep"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Encrypted"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and replicationJobId =
         flag "replication-job-id" (required string)
           ~doc:"STRING ReplicationJobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_replication_job
           (Values.UpdateReplicationJobRequest.make ?frequency
              ?nextReplicationRunStartTime:(Option.map
                                              ~f:Values.Timestamp.of_json
                                              nextReplicationRunStartTime)
              ?licenseType:(Option.map ~f:Values.LicenseType.of_json
                              licenseType) ?roleName ?description
              ?numberOfRecentAmisToKeep ?encrypted ?kmsKeyId
              ~replicationJobId ())
           (Some Values.UpdateReplicationJobResponse.to_json)
           (Some Values.UpdateReplicationJobResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-app", create_app);
    ("create-replication-job", create_replication_job);
    ("delete-app", delete_app);
    ("delete-app-launch-configuration", delete_app_launch_configuration);
    ("delete-app-replication-configuration",
      delete_app_replication_configuration);
    ("delete-app-validation-configuration",
      delete_app_validation_configuration);
    ("delete-replication-job", delete_replication_job);
    ("delete-server-catalog", delete_server_catalog);
    ("disassociate-connector", disassociate_connector);
    ("generate-change-set", generate_change_set);
    ("generate-template", generate_template);
    ("get-app", get_app);
    ("get-app-launch-configuration", get_app_launch_configuration);
    ("get-app-replication-configuration", get_app_replication_configuration);
    ("get-app-validation-configuration", get_app_validation_configuration);
    ("get-app-validation-output", get_app_validation_output);
    ("get-connectors", get_connectors);
    ("get-replication-jobs", get_replication_jobs);
    ("get-replication-runs", get_replication_runs);
    ("get-servers", get_servers);
    ("import-app-catalog", import_app_catalog);
    ("import-server-catalog", import_server_catalog);
    ("launch-app", launch_app);
    ("list-apps", list_apps);
    ("notify-app-validation-output", notify_app_validation_output);
    ("put-app-launch-configuration", put_app_launch_configuration);
    ("put-app-replication-configuration", put_app_replication_configuration);
    ("put-app-validation-configuration", put_app_validation_configuration);
    ("start-app-replication", start_app_replication);
    ("start-on-demand-app-replication", start_on_demand_app_replication);
    ("start-on-demand-replication-run", start_on_demand_replication_run);
    ("stop-app-replication", stop_app_replication);
    ("terminate-app", terminate_app);
    ("update-app", update_app);
    ("update-replication-job", update_replication_job)]
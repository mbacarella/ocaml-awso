(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let count_closed_workflow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTimeFilter =
         flag "start-time-filter" (optional json_arg)
           ~doc:"JSON ExecutionTimeFilter"
       and closeTimeFilter =
         flag "close-time-filter" (optional json_arg)
           ~doc:"JSON ExecutionTimeFilter"
       and executionFilter =
         flag "execution-filter" (optional json_arg)
           ~doc:"JSON WorkflowExecutionFilter"
       and typeFilter =
         flag "type-filter" (optional json_arg)
           ~doc:"JSON WorkflowTypeFilter"
       and tagFilter =
         flag "tag-filter" (optional json_arg) ~doc:"JSON TagFilter"
       and closeStatusFilter =
         flag "close-status-filter" (optional json_arg)
           ~doc:"JSON CloseStatusFilter"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.count_closed_workflow_executions
           (Values.CountClosedWorkflowExecutionsInput.make
              ?startTimeFilter:(Option.map
                                  ~f:Values.ExecutionTimeFilter.of_json
                                  startTimeFilter)
              ?closeTimeFilter:(Option.map
                                  ~f:Values.ExecutionTimeFilter.of_json
                                  closeTimeFilter)
              ?executionFilter:(Option.map
                                  ~f:Values.WorkflowExecutionFilter.of_json
                                  executionFilter)
              ?typeFilter:(Option.map ~f:Values.WorkflowTypeFilter.of_json
                             typeFilter)
              ?tagFilter:(Option.map ~f:Values.TagFilter.of_json tagFilter)
              ?closeStatusFilter:(Option.map
                                    ~f:Values.CloseStatusFilter.of_json
                                    closeStatusFilter) ~domain ())
           (Some Values.WorkflowExecutionCount.to_json)
           (Some Values.WorkflowExecutionCount.error_to_json)])
let count_open_workflow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeFilter =
         flag "type-filter" (optional json_arg)
           ~doc:"JSON WorkflowTypeFilter"
       and tagFilter =
         flag "tag-filter" (optional json_arg) ~doc:"JSON TagFilter"
       and executionFilter =
         flag "execution-filter" (optional json_arg)
           ~doc:"JSON WorkflowExecutionFilter"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and startTimeFilter =
         flag "start-time-filter" (required json_arg)
           ~doc:"JSON ExecutionTimeFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.count_open_workflow_executions
           (Values.CountOpenWorkflowExecutionsInput.make
              ?typeFilter:(Option.map ~f:Values.WorkflowTypeFilter.of_json
                             typeFilter)
              ?tagFilter:(Option.map ~f:Values.TagFilter.of_json tagFilter)
              ?executionFilter:(Option.map
                                  ~f:Values.WorkflowExecutionFilter.of_json
                                  executionFilter) ~domain
              ~startTimeFilter:(Values.ExecutionTimeFilter.of_json
                                  startTimeFilter) ())
           (Some Values.WorkflowExecutionCount.to_json)
           (Some Values.WorkflowExecutionCount.error_to_json)])
let count_pending_activity_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and taskList =
         flag "task-list" (required json_arg) ~doc:"JSON TaskList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.count_pending_activity_tasks
           (Values.CountPendingActivityTasksInput.make ~domain
              ~taskList:(Values.TaskList.of_json taskList) ())
           (Some Values.PendingTaskCount.to_json)
           (Some Values.PendingTaskCount.error_to_json)])
let count_pending_decision_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and taskList =
         flag "task-list" (required json_arg) ~doc:"JSON TaskList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.count_pending_decision_tasks
           (Values.CountPendingDecisionTasksInput.make ~domain
              ~taskList:(Values.TaskList.of_json taskList) ())
           (Some Values.PendingTaskCount.to_json)
           (Some Values.PendingTaskCount.error_to_json)])
let deprecate_activity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and activityType =
         flag "activity-type" (required json_arg) ~doc:"JSON ActivityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprecate_activity_type
           (Values.DeprecateActivityTypeInput.make ~domain
              ~activityType:(Values.ActivityType.of_json activityType) ())
           None None])
let deprecate_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprecate_domain (Values.DeprecateDomainInput.make ~name ())
           None None])
let deprecate_workflow_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowType =
         flag "workflow-type" (required json_arg) ~doc:"JSON WorkflowType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprecate_workflow_type
           (Values.DeprecateWorkflowTypeInput.make ~domain
              ~workflowType:(Values.WorkflowType.of_json workflowType) ())
           None None])
let describe_activity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and activityType =
         flag "activity-type" (required json_arg) ~doc:"JSON ActivityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_activity_type
           (Values.DescribeActivityTypeInput.make ~domain
              ~activityType:(Values.ActivityType.of_json activityType) ())
           (Some Values.ActivityTypeDetail.to_json)
           (Some Values.ActivityTypeDetail.error_to_json)])
let describe_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain (Values.DescribeDomainInput.make ~name ())
           (Some Values.DomainDetail.to_json)
           (Some Values.DomainDetail.error_to_json)])
let describe_workflow_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and execution =
         flag "execution" (required json_arg) ~doc:"JSON WorkflowExecution" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_workflow_execution
           (Values.DescribeWorkflowExecutionInput.make ~domain
              ~execution:(Values.WorkflowExecution.of_json execution) ())
           (Some Values.WorkflowExecutionDetail.to_json)
           (Some Values.WorkflowExecutionDetail.error_to_json)])
let describe_workflow_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowType =
         flag "workflow-type" (required json_arg) ~doc:"JSON WorkflowType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_workflow_type
           (Values.DescribeWorkflowTypeInput.make ~domain
              ~workflowType:(Values.WorkflowType.of_json workflowType) ())
           (Some Values.WorkflowTypeDetail.to_json)
           (Some Values.WorkflowTypeDetail.error_to_json)])
let get_workflow_execution_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and execution =
         flag "execution" (required json_arg) ~doc:"JSON WorkflowExecution" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_execution_history
           (Values.GetWorkflowExecutionHistoryInput.make ?nextPageToken
              ?maximumPageSize ?reverseOrder ~domain
              ~execution:(Values.WorkflowExecution.of_json execution) ())
           (Some Values.History.to_json) (Some Values.History.error_to_json)])
let list_activity_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and registrationStatus =
         flag "registration-status" (required json_arg)
           ~doc:"JSON RegistrationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_activity_types
           (Values.ListActivityTypesInput.make ?name ?nextPageToken
              ?maximumPageSize ?reverseOrder ~domain
              ~registrationStatus:(Values.RegistrationStatus.of_json
                                     registrationStatus) ())
           (Some Values.ActivityTypeInfos.to_json)
           (Some Values.ActivityTypeInfos.error_to_json)])
let list_closed_workflow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTimeFilter =
         flag "start-time-filter" (optional json_arg)
           ~doc:"JSON ExecutionTimeFilter"
       and closeTimeFilter =
         flag "close-time-filter" (optional json_arg)
           ~doc:"JSON ExecutionTimeFilter"
       and executionFilter =
         flag "execution-filter" (optional json_arg)
           ~doc:"JSON WorkflowExecutionFilter"
       and closeStatusFilter =
         flag "close-status-filter" (optional json_arg)
           ~doc:"JSON CloseStatusFilter"
       and typeFilter =
         flag "type-filter" (optional json_arg)
           ~doc:"JSON WorkflowTypeFilter"
       and tagFilter =
         flag "tag-filter" (optional json_arg) ~doc:"JSON TagFilter"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_closed_workflow_executions
           (Values.ListClosedWorkflowExecutionsInput.make
              ?startTimeFilter:(Option.map
                                  ~f:Values.ExecutionTimeFilter.of_json
                                  startTimeFilter)
              ?closeTimeFilter:(Option.map
                                  ~f:Values.ExecutionTimeFilter.of_json
                                  closeTimeFilter)
              ?executionFilter:(Option.map
                                  ~f:Values.WorkflowExecutionFilter.of_json
                                  executionFilter)
              ?closeStatusFilter:(Option.map
                                    ~f:Values.CloseStatusFilter.of_json
                                    closeStatusFilter)
              ?typeFilter:(Option.map ~f:Values.WorkflowTypeFilter.of_json
                             typeFilter)
              ?tagFilter:(Option.map ~f:Values.TagFilter.of_json tagFilter)
              ?nextPageToken ?maximumPageSize ?reverseOrder ~domain ())
           (Some Values.WorkflowExecutionInfos.to_json)
           (Some Values.WorkflowExecutionInfos.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and registrationStatus =
         flag "registration-status" (required json_arg)
           ~doc:"JSON RegistrationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsInput.make ?nextPageToken ?maximumPageSize
              ?reverseOrder
              ~registrationStatus:(Values.RegistrationStatus.of_json
                                     registrationStatus) ())
           (Some Values.DomainInfos.to_json)
           (Some Values.DomainInfos.error_to_json)])
let list_open_workflow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeFilter =
         flag "type-filter" (optional json_arg)
           ~doc:"JSON WorkflowTypeFilter"
       and tagFilter =
         flag "tag-filter" (optional json_arg) ~doc:"JSON TagFilter"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and executionFilter =
         flag "execution-filter" (optional json_arg)
           ~doc:"JSON WorkflowExecutionFilter"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and startTimeFilter =
         flag "start-time-filter" (required json_arg)
           ~doc:"JSON ExecutionTimeFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_open_workflow_executions
           (Values.ListOpenWorkflowExecutionsInput.make
              ?typeFilter:(Option.map ~f:Values.WorkflowTypeFilter.of_json
                             typeFilter)
              ?tagFilter:(Option.map ~f:Values.TagFilter.of_json tagFilter)
              ?nextPageToken ?maximumPageSize ?reverseOrder
              ?executionFilter:(Option.map
                                  ~f:Values.WorkflowExecutionFilter.of_json
                                  executionFilter) ~domain
              ~startTimeFilter:(Values.ExecutionTimeFilter.of_json
                                  startTimeFilter) ())
           (Some Values.WorkflowExecutionInfos.to_json)
           (Some Values.WorkflowExecutionInfos.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_workflow_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and registrationStatus =
         flag "registration-status" (required json_arg)
           ~doc:"JSON RegistrationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_types
           (Values.ListWorkflowTypesInput.make ?name ?nextPageToken
              ?maximumPageSize ?reverseOrder ~domain
              ~registrationStatus:(Values.RegistrationStatus.of_json
                                     registrationStatus) ())
           (Some Values.WorkflowTypeInfos.to_json)
           (Some Values.WorkflowTypeInfos.error_to_json)])
let poll_for_activity_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (optional string) ~doc:"STRING Identity"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and taskList =
         flag "task-list" (required json_arg) ~doc:"JSON TaskList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.poll_for_activity_task
           (Values.PollForActivityTaskInput.make ?identity ~domain
              ~taskList:(Values.TaskList.of_json taskList) ())
           (Some Values.ActivityTask.to_json)
           (Some Values.ActivityTask.error_to_json)])
let poll_for_decision_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identity =
         flag "identity" (optional string) ~doc:"STRING Identity"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING PageToken"
       and maximumPageSize =
         flag "maximum-page-size" (optional int) ~doc:"INT PageSize"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and taskList =
         flag "task-list" (required json_arg) ~doc:"JSON TaskList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.poll_for_decision_task
           (Values.PollForDecisionTaskInput.make ?identity ?nextPageToken
              ?maximumPageSize ?reverseOrder ~domain
              ~taskList:(Values.TaskList.of_json taskList) ())
           (Some Values.DecisionTask.to_json)
           (Some Values.DecisionTask.error_to_json)])
let record_activity_task_heartbeat =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and details =
         flag "details" (optional string) ~doc:"STRING LimitedData"
       and taskToken =
         flag "task-token" (required string) ~doc:"STRING TaskToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.record_activity_task_heartbeat
           (Values.RecordActivityTaskHeartbeatInput.make ?details ~taskToken
              ()) (Some Values.ActivityTaskStatus.to_json)
           (Some Values.ActivityTaskStatus.error_to_json)])
let register_activity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and defaultTaskStartToCloseTimeout =
         flag "default-task-start-to-close-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and defaultTaskHeartbeatTimeout =
         flag "default-task-heartbeat-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and defaultTaskList =
         flag "default-task-list" (optional json_arg) ~doc:"JSON TaskList"
       and defaultTaskPriority =
         flag "default-task-priority" (optional string)
           ~doc:"STRING TaskPriority"
       and defaultTaskScheduleToStartTimeout =
         flag "default-task-schedule-to-start-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and defaultTaskScheduleToCloseTimeout =
         flag "default-task-schedule-to-close-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and version = flag "version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_activity_type
           (Values.RegisterActivityTypeInput.make ?description
              ?defaultTaskStartToCloseTimeout ?defaultTaskHeartbeatTimeout
              ?defaultTaskList:(Option.map ~f:Values.TaskList.of_json
                                  defaultTaskList) ?defaultTaskPriority
              ?defaultTaskScheduleToStartTimeout
              ?defaultTaskScheduleToCloseTimeout ~domain ~name ~version ())
           None None])
let register_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and name = flag "name" (required string) ~doc:"STRING DomainName"
       and workflowExecutionRetentionPeriodInDays =
         flag "workflow-execution-retention-period-in-days" (required string)
           ~doc:"STRING DurationInDays" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_domain
           (Values.RegisterDomainInput.make ?description
              ?tags:(Option.map ~f:Values.ResourceTagList.of_json tags) ~name
              ~workflowExecutionRetentionPeriodInDays ()) None None])
let register_workflow_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and defaultTaskStartToCloseTimeout =
         flag "default-task-start-to-close-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and defaultExecutionStartToCloseTimeout =
         flag "default-execution-start-to-close-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and defaultTaskList =
         flag "default-task-list" (optional json_arg) ~doc:"JSON TaskList"
       and defaultTaskPriority =
         flag "default-task-priority" (optional string)
           ~doc:"STRING TaskPriority"
       and defaultChildPolicy =
         flag "default-child-policy" (optional json_arg)
           ~doc:"JSON ChildPolicy"
       and defaultLambdaRole =
         flag "default-lambda-role" (optional string) ~doc:"STRING Arn"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and version = flag "version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_workflow_type
           (Values.RegisterWorkflowTypeInput.make ?description
              ?defaultTaskStartToCloseTimeout
              ?defaultExecutionStartToCloseTimeout
              ?defaultTaskList:(Option.map ~f:Values.TaskList.of_json
                                  defaultTaskList) ?defaultTaskPriority
              ?defaultChildPolicy:(Option.map ~f:Values.ChildPolicy.of_json
                                     defaultChildPolicy) ?defaultLambdaRole
              ~domain ~name ~version ()) None None])
let request_cancel_workflow_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId =
         flag "run-id" (optional string) ~doc:"STRING WorkflowRunIdOptional"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.request_cancel_workflow_execution
           (Values.RequestCancelWorkflowExecutionInput.make ?runId ~domain
              ~workflowId ()) None None])
let respond_activity_task_canceled =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and details = flag "details" (optional string) ~doc:"STRING Data"
       and taskToken =
         flag "task-token" (required string) ~doc:"STRING TaskToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.respond_activity_task_canceled
           (Values.RespondActivityTaskCanceledInput.make ?details ~taskToken
              ()) None None])
let respond_activity_task_completed =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and result = flag "result" (optional string) ~doc:"STRING Data"
       and taskToken =
         flag "task-token" (required string) ~doc:"STRING TaskToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.respond_activity_task_completed
           (Values.RespondActivityTaskCompletedInput.make ?result ~taskToken
              ()) None None])
let respond_activity_task_failed =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason =
         flag "reason" (optional string) ~doc:"STRING FailureReason"
       and details = flag "details" (optional string) ~doc:"STRING Data"
       and taskToken =
         flag "task-token" (required string) ~doc:"STRING TaskToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.respond_activity_task_failed
           (Values.RespondActivityTaskFailedInput.make ?reason ?details
              ~taskToken ()) None None])
let respond_decision_task_completed =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and decisions =
         flag "decisions" (optional json_arg) ~doc:"JSON DecisionList"
       and executionContext =
         flag "execution-context" (optional string) ~doc:"STRING Data"
       and taskToken =
         flag "task-token" (required string) ~doc:"STRING TaskToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.respond_decision_task_completed
           (Values.RespondDecisionTaskCompletedInput.make
              ?decisions:(Option.map ~f:Values.DecisionList.of_json decisions)
              ?executionContext ~taskToken ()) None None])
let signal_workflow_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId =
         flag "run-id" (optional string) ~doc:"STRING WorkflowRunIdOptional"
       and input = flag "input" (optional string) ~doc:"STRING Data"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and signalName =
         flag "signal-name" (required string) ~doc:"STRING SignalName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.signal_workflow_execution
           (Values.SignalWorkflowExecutionInput.make ?runId ?input ~domain
              ~workflowId ~signalName ()) None None])
let start_workflow_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskList =
         flag "task-list" (optional json_arg) ~doc:"JSON TaskList"
       and taskPriority =
         flag "task-priority" (optional string) ~doc:"STRING TaskPriority"
       and input = flag "input" (optional string) ~doc:"STRING Data"
       and executionStartToCloseTimeout =
         flag "execution-start-to-close-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and taskStartToCloseTimeout =
         flag "task-start-to-close-timeout" (optional string)
           ~doc:"STRING DurationInSecondsOptional"
       and childPolicy =
         flag "child-policy" (optional json_arg) ~doc:"JSON ChildPolicy"
       and lambdaRole =
         flag "lambda-role" (optional string) ~doc:"STRING Arn"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and workflowType =
         flag "workflow-type" (required json_arg) ~doc:"JSON WorkflowType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_workflow_execution
           (Values.StartWorkflowExecutionInput.make
              ?taskList:(Option.map ~f:Values.TaskList.of_json taskList)
              ?taskPriority ?input ?executionStartToCloseTimeout
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ?taskStartToCloseTimeout
              ?childPolicy:(Option.map ~f:Values.ChildPolicy.of_json
                              childPolicy) ?lambdaRole ~domain ~workflowId
              ~workflowType:(Values.WorkflowType.of_json workflowType) ())
           (Some Values.Run.to_json) (Some Values.Run.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON ResourceTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.ResourceTagList.of_json tags) ()) None None])
let terminate_workflow_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId =
         flag "run-id" (optional string) ~doc:"STRING WorkflowRunIdOptional"
       and reason =
         flag "reason" (optional string) ~doc:"STRING TerminateReason"
       and details = flag "details" (optional string) ~doc:"STRING Data"
       and childPolicy =
         flag "child-policy" (optional json_arg) ~doc:"JSON ChildPolicy"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_workflow_execution
           (Values.TerminateWorkflowExecutionInput.make ?runId ?reason
              ?details
              ?childPolicy:(Option.map ~f:Values.ChildPolicy.of_json
                              childPolicy) ~domain ~workflowId ()) None None])
let undeprecate_activity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and activityType =
         flag "activity-type" (required json_arg) ~doc:"JSON ActivityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.undeprecate_activity_type
           (Values.UndeprecateActivityTypeInput.make ~domain
              ~activityType:(Values.ActivityType.of_json activityType) ())
           None None])
let undeprecate_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.undeprecate_domain
           (Values.UndeprecateDomainInput.make ~name ()) None None])
let undeprecate_workflow_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainName"
       and workflowType =
         flag "workflow-type" (required json_arg) ~doc:"JSON WorkflowType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.undeprecate_workflow_type
           (Values.UndeprecateWorkflowTypeInput.make ~domain
              ~workflowType:(Values.WorkflowType.of_json workflowType) ())
           None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON ResourceTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.ResourceTagKeyList.of_json tagKeys) ()) None
           None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("count-closed-workflow-executions", count_closed_workflow_executions);
    ("count-open-workflow-executions", count_open_workflow_executions);
    ("count-pending-activity-tasks", count_pending_activity_tasks);
    ("count-pending-decision-tasks", count_pending_decision_tasks);
    ("deprecate-activity-type", deprecate_activity_type);
    ("deprecate-domain", deprecate_domain);
    ("deprecate-workflow-type", deprecate_workflow_type);
    ("describe-activity-type", describe_activity_type);
    ("describe-domain", describe_domain);
    ("describe-workflow-execution", describe_workflow_execution);
    ("describe-workflow-type", describe_workflow_type);
    ("get-workflow-execution-history", get_workflow_execution_history);
    ("list-activity-types", list_activity_types);
    ("list-closed-workflow-executions", list_closed_workflow_executions);
    ("list-domains", list_domains);
    ("list-open-workflow-executions", list_open_workflow_executions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workflow-types", list_workflow_types);
    ("poll-for-activity-task", poll_for_activity_task);
    ("poll-for-decision-task", poll_for_decision_task);
    ("record-activity-task-heartbeat", record_activity_task_heartbeat);
    ("register-activity-type", register_activity_type);
    ("register-domain", register_domain);
    ("register-workflow-type", register_workflow_type);
    ("request-cancel-workflow-execution", request_cancel_workflow_execution);
    ("respond-activity-task-canceled", respond_activity_task_canceled);
    ("respond-activity-task-completed", respond_activity_task_completed);
    ("respond-activity-task-failed", respond_activity_task_failed);
    ("respond-decision-task-completed", respond_decision_task_completed);
    ("signal-workflow-execution", signal_workflow_execution);
    ("start-workflow-execution", start_workflow_execution);
    ("tag-resource", tag_resource);
    ("terminate-workflow-execution", terminate_workflow_execution);
    ("undeprecate-activity-type", undeprecate_activity_type);
    ("undeprecate-domain", undeprecate_domain);
    ("undeprecate-workflow-type", undeprecate_workflow_type);
    ("untag-resource", untag_resource)]
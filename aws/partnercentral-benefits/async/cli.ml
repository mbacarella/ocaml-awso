(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let amend_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING AmendBenefitApplicationInputClientTokenString"
       and revision = flag "revision" (required string) ~doc:"STRING String"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier"
       and amendmentReason =
         flag "amendment-reason" (required string)
           ~doc:"STRING AmendBenefitApplicationInputAmendmentReasonString"
       and amendments =
         flag "amendments" (required json_arg) ~doc:"JSON AmendmentList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.amend_benefit_application
           (Values.AmendBenefitApplicationInput.make ~catalog ~clientToken
              ~revision ~identifier ~amendmentReason
              ~amendments:(Values.AmendmentList.of_json amendments) ())
           (Some Values.AmendBenefitApplicationOutput.to_json)
           (Some Values.AmendBenefitApplicationOutput.error_to_json)])
let associate_benefit_application_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and benefitApplicationIdentifier =
         flag "benefit-application-identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AssociateBenefitApplicationResourceInputResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_benefit_application_resource
           (Values.AssociateBenefitApplicationResourceInput.make ~catalog
              ~benefitApplicationIdentifier ~resourceArn ())
           (Some Values.AssociateBenefitApplicationResourceOutput.to_json)
           (Some
              Values.AssociateBenefitApplicationResourceOutput.error_to_json)])
let cancel_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason =
         flag "reason" (optional string)
           ~doc:"STRING CancelBenefitApplicationInputReasonString"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING CancelBenefitApplicationInputClientTokenString"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_benefit_application
           (Values.CancelBenefitApplicationInput.make ?reason ~catalog
              ~clientToken ~identifier ())
           (Some Values.CancelBenefitApplicationOutput.to_json)
           (Some Values.CancelBenefitApplicationOutput.error_to_json)])
let create_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING BenefitApplicationName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BenefitApplicationDescription"
       and fulfillmentTypes =
         flag "fulfillment-types" (optional json_arg)
           ~doc:"JSON FulfillmentTypes"
       and benefitApplicationDetails =
         flag "benefit-application-details" (optional json_arg)
           ~doc:"JSON Document"
       and tags =
         flag "tags" (optional json_arg)
           ~doc:"JSON CreateBenefitApplicationInputTagsList"
       and associatedResources =
         flag "associated-resources" (optional json_arg)
           ~doc:"JSON CreateBenefitApplicationInputAssociatedResourcesList"
       and partnerContacts =
         flag "partner-contacts" (optional json_arg)
           ~doc:"JSON CreateBenefitApplicationInputPartnerContactsList"
       and fileDetails =
         flag "file-details" (optional json_arg)
           ~doc:"JSON CreateBenefitApplicationInputFileDetailsList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING CreateBenefitApplicationInputClientTokenString"
       and benefitIdentifier =
         flag "benefit-identifier" (required string)
           ~doc:"STRING CreateBenefitApplicationInputBenefitIdentifierString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_benefit_application
           (Values.CreateBenefitApplicationInput.make ?name ?description
              ?fulfillmentTypes:(Option.map
                                   ~f:Values.FulfillmentTypes.of_json
                                   fulfillmentTypes)
              ?benefitApplicationDetails:(Option.map
                                            ~f:Values.Document.of_json
                                            benefitApplicationDetails)
              ?tags:(Option.map
                       ~f:Values.CreateBenefitApplicationInputTagsList.of_json
                       tags)
              ?associatedResources:(Option.map
                                      ~f:Values.CreateBenefitApplicationInputAssociatedResourcesList.of_json
                                      associatedResources)
              ?partnerContacts:(Option.map
                                  ~f:Values.CreateBenefitApplicationInputPartnerContactsList.of_json
                                  partnerContacts)
              ?fileDetails:(Option.map
                              ~f:Values.CreateBenefitApplicationInputFileDetailsList.of_json
                              fileDetails) ~catalog ~clientToken
              ~benefitIdentifier ())
           (Some Values.CreateBenefitApplicationOutput.to_json)
           (Some Values.CreateBenefitApplicationOutput.error_to_json)])
let disassociate_benefit_application_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and benefitApplicationIdentifier =
         flag "benefit-application-identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING DisassociateBenefitApplicationResourceInputResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_benefit_application_resource
           (Values.DisassociateBenefitApplicationResourceInput.make ~catalog
              ~benefitApplicationIdentifier ~resourceArn ())
           (Some Values.DisassociateBenefitApplicationResourceOutput.to_json)
           (Some
              Values.DisassociateBenefitApplicationResourceOutput.error_to_json)])
let get_benefit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING GetBenefitInputIdentifierString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_benefit
           (Values.GetBenefitInput.make ~catalog ~identifier ())
           (Some Values.GetBenefitOutput.to_json)
           (Some Values.GetBenefitOutput.error_to_json)])
let get_benefit_allocation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitAllocationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_benefit_allocation
           (Values.GetBenefitAllocationInput.make ~catalog ~identifier ())
           (Some Values.GetBenefitAllocationOutput.to_json)
           (Some Values.GetBenefitAllocationOutput.error_to_json)])
let get_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_benefit_application
           (Values.GetBenefitApplicationInput.make ~catalog ~identifier ())
           (Some Values.GetBenefitApplicationOutput.to_json)
           (Some Values.GetBenefitApplicationOutput.error_to_json)])
let list_benefit_allocations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fulfillmentTypes =
         flag "fulfillment-types" (optional json_arg)
           ~doc:"JSON ListBenefitAllocationsInputFulfillmentTypesList"
       and benefitIdentifiers =
         flag "benefit-identifiers" (optional json_arg)
           ~doc:"JSON ListBenefitAllocationsInputBenefitIdentifiersList"
       and benefitApplicationIdentifiers =
         flag "benefit-application-identifiers" (optional json_arg)
           ~doc:"JSON ListBenefitAllocationsInputBenefitApplicationIdentifiersList"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON ListBenefitAllocationsInputStatusList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListBenefitAllocationsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListBenefitAllocationsInputNextTokenString"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_benefit_allocations
           (Values.ListBenefitAllocationsInput.make
              ?fulfillmentTypes:(Option.map
                                   ~f:Values.ListBenefitAllocationsInputFulfillmentTypesList.of_json
                                   fulfillmentTypes)
              ?benefitIdentifiers:(Option.map
                                     ~f:Values.ListBenefitAllocationsInputBenefitIdentifiersList.of_json
                                     benefitIdentifiers)
              ?benefitApplicationIdentifiers:(Option.map
                                                ~f:Values.ListBenefitAllocationsInputBenefitApplicationIdentifiersList.of_json
                                                benefitApplicationIdentifiers)
              ?status:(Option.map
                         ~f:Values.ListBenefitAllocationsInputStatusList.of_json
                         status) ?maxResults ?nextToken ~catalog ())
           (Some Values.ListBenefitAllocationsOutput.to_json)
           (Some Values.ListBenefitAllocationsOutput.error_to_json)])
let list_benefit_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and programs =
         flag "programs" (optional json_arg)
           ~doc:"JSON ListBenefitApplicationsInputProgramsList"
       and fulfillmentTypes =
         flag "fulfillment-types" (optional json_arg)
           ~doc:"JSON ListBenefitApplicationsInputFulfillmentTypesList"
       and benefitIdentifiers =
         flag "benefit-identifiers" (optional json_arg)
           ~doc:"JSON ListBenefitApplicationsInputBenefitIdentifiersList"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON ListBenefitApplicationsInputStatusList"
       and stages =
         flag "stages" (optional json_arg)
           ~doc:"JSON ListBenefitApplicationsInputStagesList"
       and associatedResources =
         flag "associated-resources" (optional json_arg)
           ~doc:"JSON AssociatedResources"
       and associatedResourceArns =
         flag "associated-resource-arns" (optional json_arg) ~doc:"JSON Arns"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListBenefitApplicationsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListBenefitApplicationsInputNextTokenString"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_benefit_applications
           (Values.ListBenefitApplicationsInput.make
              ?programs:(Option.map
                           ~f:Values.ListBenefitApplicationsInputProgramsList.of_json
                           programs)
              ?fulfillmentTypes:(Option.map
                                   ~f:Values.ListBenefitApplicationsInputFulfillmentTypesList.of_json
                                   fulfillmentTypes)
              ?benefitIdentifiers:(Option.map
                                     ~f:Values.ListBenefitApplicationsInputBenefitIdentifiersList.of_json
                                     benefitIdentifiers)
              ?status:(Option.map
                         ~f:Values.ListBenefitApplicationsInputStatusList.of_json
                         status)
              ?stages:(Option.map
                         ~f:Values.ListBenefitApplicationsInputStagesList.of_json
                         stages)
              ?associatedResources:(Option.map
                                      ~f:Values.AssociatedResources.of_json
                                      associatedResources)
              ?associatedResourceArns:(Option.map ~f:Values.Arns.of_json
                                         associatedResourceArns) ?maxResults
              ?nextToken ~catalog ())
           (Some Values.ListBenefitApplicationsOutput.to_json)
           (Some Values.ListBenefitApplicationsOutput.error_to_json)])
let list_benefits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and programs =
         flag "programs" (optional json_arg)
           ~doc:"JSON ListBenefitsInputProgramsList"
       and fulfillmentTypes =
         flag "fulfillment-types" (optional json_arg)
           ~doc:"JSON ListBenefitsInputFulfillmentTypesList"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON ListBenefitsInputStatusList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListBenefitsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListBenefitsInputNextTokenString"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_benefits
           (Values.ListBenefitsInput.make
              ?programs:(Option.map
                           ~f:Values.ListBenefitsInputProgramsList.of_json
                           programs)
              ?fulfillmentTypes:(Option.map
                                   ~f:Values.ListBenefitsInputFulfillmentTypesList.of_json
                                   fulfillmentTypes)
              ?status:(Option.map
                         ~f:Values.ListBenefitsInputStatusList.of_json status)
              ?maxResults ?nextToken ~catalog ())
           (Some Values.ListBenefitsOutput.to_json)
           (Some Values.ListBenefitsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let recall_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier"
       and reason = flag "reason" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.recall_benefit_application
           (Values.RecallBenefitApplicationInput.make ?clientToken ~catalog
              ~identifier ~reason ())
           (Some Values.RecallBenefitApplicationOutput.to_json)
           (Some Values.RecallBenefitApplicationOutput.error_to_json)])
let submit_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_benefit_application
           (Values.SubmitBenefitApplicationInput.make ~catalog ~identifier ())
           (Some Values.SubmitBenefitApplicationOutput.to_json)
           (Some Values.SubmitBenefitApplicationOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_benefit_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING BenefitApplicationName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BenefitApplicationDescription"
       and benefitApplicationDetails =
         flag "benefit-application-details" (optional json_arg)
           ~doc:"JSON Document"
       and partnerContacts =
         flag "partner-contacts" (optional json_arg)
           ~doc:"JSON UpdateBenefitApplicationInputPartnerContactsList"
       and fileDetails =
         flag "file-details" (optional json_arg)
           ~doc:"JSON UpdateBenefitApplicationInputFileDetailsList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogName"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING UpdateBenefitApplicationInputClientTokenString"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING BenefitApplicationIdentifier"
       and revision = flag "revision" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_benefit_application
           (Values.UpdateBenefitApplicationInput.make ?name ?description
              ?benefitApplicationDetails:(Option.map
                                            ~f:Values.Document.of_json
                                            benefitApplicationDetails)
              ?partnerContacts:(Option.map
                                  ~f:Values.UpdateBenefitApplicationInputPartnerContactsList.of_json
                                  partnerContacts)
              ?fileDetails:(Option.map
                              ~f:Values.UpdateBenefitApplicationInputFileDetailsList.of_json
                              fileDetails) ~catalog ~clientToken ~identifier
              ~revision ())
           (Some Values.UpdateBenefitApplicationOutput.to_json)
           (Some Values.UpdateBenefitApplicationOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("amend-benefit-application", amend_benefit_application);
    ("associate-benefit-application-resource",
      associate_benefit_application_resource);
    ("cancel-benefit-application", cancel_benefit_application);
    ("create-benefit-application", create_benefit_application);
    ("disassociate-benefit-application-resource",
      disassociate_benefit_application_resource);
    ("get-benefit", get_benefit);
    ("get-benefit-allocation", get_benefit_allocation);
    ("get-benefit-application", get_benefit_application);
    ("list-benefit-allocations", list_benefit_allocations);
    ("list-benefit-applications", list_benefit_applications);
    ("list-benefits", list_benefits);
    ("list-tags-for-resource", list_tags_for_resource);
    ("recall-benefit-application", recall_benefit_application);
    ("submit-benefit-application", submit_benefit_application);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-benefit-application", update_benefit_application)]
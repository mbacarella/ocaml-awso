(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_aws_account_with_partner_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sidewalk =
         flag "sidewalk" (required json_arg) ~doc:"JSON SidewalkAccountInfo" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_aws_account_with_partner_account
           (Values.AssociateAwsAccountWithPartnerAccountRequest.make
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sidewalk:(Values.SidewalkAccountInfo.of_json sidewalk) ())
           (Some Values.AssociateAwsAccountWithPartnerAccountResponse.to_json)
           (Some
              Values.AssociateAwsAccountWithPartnerAccountResponse.error_to_json)])
let associate_multicast_group_with_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId"
       and multicastGroupId =
         flag "multicast-group-id" (required string)
           ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_multicast_group_with_fuota_task
           (Values.AssociateMulticastGroupWithFuotaTaskRequest.make ~id
              ~multicastGroupId ())
           (Some Values.AssociateMulticastGroupWithFuotaTaskResponse.to_json)
           (Some
              Values.AssociateMulticastGroupWithFuotaTaskResponse.error_to_json)])
let associate_wireless_device_with_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId"
       and wirelessDeviceId =
         flag "wireless-device-id" (required string)
           ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_wireless_device_with_fuota_task
           (Values.AssociateWirelessDeviceWithFuotaTaskRequest.make ~id
              ~wirelessDeviceId ())
           (Some Values.AssociateWirelessDeviceWithFuotaTaskResponse.to_json)
           (Some
              Values.AssociateWirelessDeviceWithFuotaTaskResponse.error_to_json)])
let associate_wireless_device_with_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId"
       and wirelessDeviceId =
         flag "wireless-device-id" (required string)
           ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_wireless_device_with_multicast_group
           (Values.AssociateWirelessDeviceWithMulticastGroupRequest.make ~id
              ~wirelessDeviceId ())
           (Some
              Values.AssociateWirelessDeviceWithMulticastGroupResponse.to_json)
           (Some
              Values.AssociateWirelessDeviceWithMulticastGroupResponse.error_to_json)])
let associate_wireless_device_with_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId"
       and thingArn =
         flag "thing-arn" (required string) ~doc:"STRING ThingArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_wireless_device_with_thing
           (Values.AssociateWirelessDeviceWithThingRequest.make ~id ~thingArn
              ())
           (Some Values.AssociateWirelessDeviceWithThingResponse.to_json)
           (Some
              Values.AssociateWirelessDeviceWithThingResponse.error_to_json)])
let associate_wireless_gateway_with_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId"
       and iotCertificateId =
         flag "iot-certificate-id" (required string)
           ~doc:"STRING IotCertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_wireless_gateway_with_certificate
           (Values.AssociateWirelessGatewayWithCertificateRequest.make ~id
              ~iotCertificateId ())
           (Some
              Values.AssociateWirelessGatewayWithCertificateResponse.to_json)
           (Some
              Values.AssociateWirelessGatewayWithCertificateResponse.error_to_json)])
let associate_wireless_gateway_with_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId"
       and thingArn =
         flag "thing-arn" (required string) ~doc:"STRING ThingArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_wireless_gateway_with_thing
           (Values.AssociateWirelessGatewayWithThingRequest.make ~id
              ~thingArn ())
           (Some Values.AssociateWirelessGatewayWithThingResponse.to_json)
           (Some
              Values.AssociateWirelessGatewayWithThingResponse.error_to_json)])
let cancel_multicast_group_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_multicast_group_session
           (Values.CancelMulticastGroupSessionRequest.make ~id ())
           (Some Values.CancelMulticastGroupSessionResponse.to_json)
           (Some Values.CancelMulticastGroupSessionResponse.error_to_json)])
let create_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and name = flag "name" (required string) ~doc:"STRING DestinationName"
       and expressionType =
         flag "expression-type" (required json_arg)
           ~doc:"JSON ExpressionType"
       and expression =
         flag "expression" (required string) ~doc:"STRING Expression"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_destination
           (Values.CreateDestinationRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken ~name
              ~expressionType:(Values.ExpressionType.of_json expressionType)
              ~expression ~roleArn ())
           (Some Values.CreateDestinationResponse.to_json)
           (Some Values.CreateDestinationResponse.error_to_json)])
let create_device_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING DeviceProfileName"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg)
           ~doc:"JSON LoRaWANDeviceProfile"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and sidewalk =
         flag "sidewalk" (optional json_arg)
           ~doc:"JSON SidewalkCreateDeviceProfile" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_device_profile
           (Values.CreateDeviceProfileRequest.make ?name
              ?loRaWAN:(Option.map ~f:Values.LoRaWANDeviceProfile.of_json
                          loRaWAN)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken
              ?sidewalk:(Option.map
                           ~f:Values.SidewalkCreateDeviceProfile.of_json
                           sidewalk) ())
           (Some Values.CreateDeviceProfileResponse.to_json)
           (Some Values.CreateDeviceProfileResponse.error_to_json)])
let create_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING FuotaTaskName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg) ~doc:"JSON LoRaWANFuotaTask"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and redundancyPercent =
         flag "redundancy-percent" (optional int)
           ~doc:"INT RedundancyPercent"
       and fragmentSizeBytes =
         flag "fragment-size-bytes" (optional int)
           ~doc:"INT FragmentSizeBytes"
       and fragmentIntervalMS =
         flag "fragment-interval-m-s" (optional int)
           ~doc:"INT FragmentIntervalMS"
       and descriptor =
         flag "descriptor" (optional string) ~doc:"STRING FileDescriptor"
       and firmwareUpdateImage =
         flag "firmware-update-image" (required string)
           ~doc:"STRING FirmwareUpdateImage"
       and firmwareUpdateRole =
         flag "firmware-update-role" (required string)
           ~doc:"STRING FirmwareUpdateRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fuota_task
           (Values.CreateFuotaTaskRequest.make ?name ?description
              ?clientRequestToken
              ?loRaWAN:(Option.map ~f:Values.LoRaWANFuotaTask.of_json loRaWAN)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?redundancyPercent ?fragmentSizeBytes ?fragmentIntervalMS
              ?descriptor ~firmwareUpdateImage ~firmwareUpdateRole ())
           (Some Values.CreateFuotaTaskResponse.to_json)
           (Some Values.CreateFuotaTaskResponse.error_to_json)])
let create_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING MulticastGroupName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and loRaWAN =
         flag "lo-ra-w-a-n" (required json_arg) ~doc:"JSON LoRaWANMulticast" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_multicast_group
           (Values.CreateMulticastGroupRequest.make ?name ?description
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~loRaWAN:(Values.LoRaWANMulticast.of_json loRaWAN) ())
           (Some Values.CreateMulticastGroupResponse.to_json)
           (Some Values.CreateMulticastGroupResponse.error_to_json)])
let create_network_analyzer_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and traceContent =
         flag "trace-content" (optional json_arg) ~doc:"JSON TraceContent"
       and wirelessDevices =
         flag "wireless-devices" (optional json_arg)
           ~doc:"JSON WirelessDeviceList"
       and wirelessGateways =
         flag "wireless-gateways" (optional json_arg)
           ~doc:"JSON WirelessGatewayList"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and multicastGroups =
         flag "multicast-groups" (optional json_arg)
           ~doc:"JSON NetworkAnalyzerMulticastGroupList"
       and name =
         flag "name" (required string)
           ~doc:"STRING NetworkAnalyzerConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_analyzer_configuration
           (Values.CreateNetworkAnalyzerConfigurationRequest.make
              ?traceContent:(Option.map ~f:Values.TraceContent.of_json
                               traceContent)
              ?wirelessDevices:(Option.map
                                  ~f:Values.WirelessDeviceList.of_json
                                  wirelessDevices)
              ?wirelessGateways:(Option.map
                                   ~f:Values.WirelessGatewayList.of_json
                                   wirelessGateways) ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken
              ?multicastGroups:(Option.map
                                  ~f:Values.NetworkAnalyzerMulticastGroupList.of_json
                                  multicastGroups) ~name ())
           (Some Values.CreateNetworkAnalyzerConfigurationResponse.to_json)
           (Some
              Values.CreateNetworkAnalyzerConfigurationResponse.error_to_json)])
let create_service_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ServiceProfileName"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg)
           ~doc:"JSON LoRaWANServiceProfile"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_profile
           (Values.CreateServiceProfileRequest.make ?name
              ?loRaWAN:(Option.map ~f:Values.LoRaWANServiceProfile.of_json
                          loRaWAN)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken ())
           (Some Values.CreateServiceProfileResponse.to_json)
           (Some Values.CreateServiceProfileResponse.error_to_json)])
let create_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING WirelessDeviceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg) ~doc:"JSON LoRaWANDevice"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and positioning =
         flag "positioning" (optional json_arg)
           ~doc:"JSON PositioningConfigStatus"
       and sidewalk =
         flag "sidewalk" (optional json_arg)
           ~doc:"JSON SidewalkCreateWirelessDevice"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON WirelessDeviceType"
       and destinationName =
         flag "destination-name" (required string)
           ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_wireless_device
           (Values.CreateWirelessDeviceRequest.make ?name ?description
              ?clientRequestToken
              ?loRaWAN:(Option.map ~f:Values.LoRaWANDevice.of_json loRaWAN)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?positioning:(Option.map
                              ~f:Values.PositioningConfigStatus.of_json
                              positioning)
              ?sidewalk:(Option.map
                           ~f:Values.SidewalkCreateWirelessDevice.of_json
                           sidewalk)
              ~type_:(Values.WirelessDeviceType.of_json type_)
              ~destinationName ())
           (Some Values.CreateWirelessDeviceResponse.to_json)
           (Some Values.CreateWirelessDeviceResponse.error_to_json)])
let create_wireless_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING WirelessGatewayName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and loRaWAN =
         flag "lo-ra-w-a-n" (required json_arg) ~doc:"JSON LoRaWANGateway" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_wireless_gateway
           (Values.CreateWirelessGatewayRequest.make ?name ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken
              ~loRaWAN:(Values.LoRaWANGateway.of_json loRaWAN) ())
           (Some Values.CreateWirelessGatewayResponse.to_json)
           (Some Values.CreateWirelessGatewayResponse.error_to_json)])
let create_wireless_gateway_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId"
       and wirelessGatewayTaskDefinitionId =
         flag "wireless-gateway-task-definition-id" (required string)
           ~doc:"STRING WirelessGatewayTaskDefinitionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_wireless_gateway_task
           (Values.CreateWirelessGatewayTaskRequest.make ~id
              ~wirelessGatewayTaskDefinitionId ())
           (Some Values.CreateWirelessGatewayTaskResponse.to_json)
           (Some Values.CreateWirelessGatewayTaskResponse.error_to_json)])
let create_wireless_gateway_task_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING WirelessGatewayTaskName"
       and update =
         flag "update" (optional json_arg)
           ~doc:"JSON UpdateWirelessGatewayTaskCreate"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and autoCreateTasks =
         flag "auto-create-tasks" (required bool) ~doc:"BOOL AutoCreateTasks" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_wireless_gateway_task_definition
           (Values.CreateWirelessGatewayTaskDefinitionRequest.make ?name
              ?update:(Option.map
                         ~f:Values.UpdateWirelessGatewayTaskCreate.of_json
                         update) ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~autoCreateTasks ())
           (Some Values.CreateWirelessGatewayTaskDefinitionResponse.to_json)
           (Some
              Values.CreateWirelessGatewayTaskDefinitionResponse.error_to_json)])
let delete_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_destination
           (Values.DeleteDestinationRequest.make ~name ())
           (Some Values.DeleteDestinationResponse.to_json)
           (Some Values.DeleteDestinationResponse.error_to_json)])
let delete_device_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DeviceProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device_profile
           (Values.DeleteDeviceProfileRequest.make ~id ())
           (Some Values.DeleteDeviceProfileResponse.to_json)
           (Some Values.DeleteDeviceProfileResponse.error_to_json)])
let delete_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fuota_task (Values.DeleteFuotaTaskRequest.make ~id ())
           (Some Values.DeleteFuotaTaskResponse.to_json)
           (Some Values.DeleteFuotaTaskResponse.error_to_json)])
let delete_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_multicast_group
           (Values.DeleteMulticastGroupRequest.make ~id ())
           (Some Values.DeleteMulticastGroupResponse.to_json)
           (Some Values.DeleteMulticastGroupResponse.error_to_json)])
let delete_network_analyzer_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING NetworkAnalyzerConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_analyzer_configuration
           (Values.DeleteNetworkAnalyzerConfigurationRequest.make
              ~configurationName ())
           (Some Values.DeleteNetworkAnalyzerConfigurationResponse.to_json)
           (Some
              Values.DeleteNetworkAnalyzerConfigurationResponse.error_to_json)])
let delete_queued_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wirelessDeviceType =
         flag "wireless-device-type" (optional json_arg)
           ~doc:"JSON WirelessDeviceType"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queued_messages
           (Values.DeleteQueuedMessagesRequest.make
              ?wirelessDeviceType:(Option.map
                                     ~f:Values.WirelessDeviceType.of_json
                                     wirelessDeviceType) ~id ~messageId ())
           (Some Values.DeleteQueuedMessagesResponse.to_json)
           (Some Values.DeleteQueuedMessagesResponse.error_to_json)])
let delete_service_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ServiceProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_profile
           (Values.DeleteServiceProfileRequest.make ~id ())
           (Some Values.DeleteServiceProfileResponse.to_json)
           (Some Values.DeleteServiceProfileResponse.error_to_json)])
let delete_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_wireless_device
           (Values.DeleteWirelessDeviceRequest.make ~id ())
           (Some Values.DeleteWirelessDeviceResponse.to_json)
           (Some Values.DeleteWirelessDeviceResponse.error_to_json)])
let delete_wireless_device_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ImportTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_wireless_device_import_task
           (Values.DeleteWirelessDeviceImportTaskRequest.make ~id ())
           (Some Values.DeleteWirelessDeviceImportTaskResponse.to_json)
           (Some Values.DeleteWirelessDeviceImportTaskResponse.error_to_json)])
let delete_wireless_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_wireless_gateway
           (Values.DeleteWirelessGatewayRequest.make ~id ())
           (Some Values.DeleteWirelessGatewayResponse.to_json)
           (Some Values.DeleteWirelessGatewayResponse.error_to_json)])
let delete_wireless_gateway_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_wireless_gateway_task
           (Values.DeleteWirelessGatewayTaskRequest.make ~id ())
           (Some Values.DeleteWirelessGatewayTaskResponse.to_json)
           (Some Values.DeleteWirelessGatewayTaskResponse.error_to_json)])
let delete_wireless_gateway_task_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING WirelessGatewayTaskDefinitionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_wireless_gateway_task_definition
           (Values.DeleteWirelessGatewayTaskDefinitionRequest.make ~id ())
           (Some Values.DeleteWirelessGatewayTaskDefinitionResponse.to_json)
           (Some
              Values.DeleteWirelessGatewayTaskDefinitionResponse.error_to_json)])
let deregister_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wirelessDeviceType =
         flag "wireless-device-type" (optional json_arg)
           ~doc:"JSON WirelessDeviceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_wireless_device
           (Values.DeregisterWirelessDeviceRequest.make
              ?wirelessDeviceType:(Option.map
                                     ~f:Values.WirelessDeviceType.of_json
                                     wirelessDeviceType) ~identifier ())
           (Some Values.DeregisterWirelessDeviceResponse.to_json)
           (Some Values.DeregisterWirelessDeviceResponse.error_to_json)])
let disassociate_aws_account_from_partner_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and partnerAccountId =
         flag "partner-account-id" (required string)
           ~doc:"STRING PartnerAccountId"
       and partnerType =
         flag "partner-type" (required json_arg) ~doc:"JSON PartnerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_aws_account_from_partner_account
           (Values.DisassociateAwsAccountFromPartnerAccountRequest.make
              ~partnerAccountId
              ~partnerType:(Values.PartnerType.of_json partnerType) ())
           (Some
              Values.DisassociateAwsAccountFromPartnerAccountResponse.to_json)
           (Some
              Values.DisassociateAwsAccountFromPartnerAccountResponse.error_to_json)])
let disassociate_multicast_group_from_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId"
       and multicastGroupId =
         flag "multicast-group-id" (required string)
           ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_multicast_group_from_fuota_task
           (Values.DisassociateMulticastGroupFromFuotaTaskRequest.make ~id
              ~multicastGroupId ())
           (Some
              Values.DisassociateMulticastGroupFromFuotaTaskResponse.to_json)
           (Some
              Values.DisassociateMulticastGroupFromFuotaTaskResponse.error_to_json)])
let disassociate_wireless_device_from_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId"
       and wirelessDeviceId =
         flag "wireless-device-id" (required string)
           ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_wireless_device_from_fuota_task
           (Values.DisassociateWirelessDeviceFromFuotaTaskRequest.make ~id
              ~wirelessDeviceId ())
           (Some
              Values.DisassociateWirelessDeviceFromFuotaTaskResponse.to_json)
           (Some
              Values.DisassociateWirelessDeviceFromFuotaTaskResponse.error_to_json)])
let disassociate_wireless_device_from_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId"
       and wirelessDeviceId =
         flag "wireless-device-id" (required string)
           ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_wireless_device_from_multicast_group
           (Values.DisassociateWirelessDeviceFromMulticastGroupRequest.make
              ~id ~wirelessDeviceId ())
           (Some
              Values.DisassociateWirelessDeviceFromMulticastGroupResponse.to_json)
           (Some
              Values.DisassociateWirelessDeviceFromMulticastGroupResponse.error_to_json)])
let disassociate_wireless_device_from_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_wireless_device_from_thing
           (Values.DisassociateWirelessDeviceFromThingRequest.make ~id ())
           (Some Values.DisassociateWirelessDeviceFromThingResponse.to_json)
           (Some
              Values.DisassociateWirelessDeviceFromThingResponse.error_to_json)])
let disassociate_wireless_gateway_from_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_wireless_gateway_from_certificate
           (Values.DisassociateWirelessGatewayFromCertificateRequest.make ~id
              ())
           (Some
              Values.DisassociateWirelessGatewayFromCertificateResponse.to_json)
           (Some
              Values.DisassociateWirelessGatewayFromCertificateResponse.error_to_json)])
let disassociate_wireless_gateway_from_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_wireless_gateway_from_thing
           (Values.DisassociateWirelessGatewayFromThingRequest.make ~id ())
           (Some Values.DisassociateWirelessGatewayFromThingResponse.to_json)
           (Some
              Values.DisassociateWirelessGatewayFromThingResponse.error_to_json)])
let get_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_destination (Values.GetDestinationRequest.make ~name ())
           (Some Values.GetDestinationResponse.to_json)
           (Some Values.GetDestinationResponse.error_to_json)])
let get_device_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DeviceProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_profile (Values.GetDeviceProfileRequest.make ~id ())
           (Some Values.GetDeviceProfileResponse.to_json)
           (Some Values.GetDeviceProfileResponse.error_to_json)])
let get_event_configuration_by_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_configuration_by_resource_types
           (Values.GetEventConfigurationByResourceTypesRequest.make ())
           (Some Values.GetEventConfigurationByResourceTypesResponse.to_json)
           (Some
              Values.GetEventConfigurationByResourceTypesResponse.error_to_json)])
let get_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_fuota_task (Values.GetFuotaTaskRequest.make ~id ())
           (Some Values.GetFuotaTaskResponse.to_json)
           (Some Values.GetFuotaTaskResponse.error_to_json)])
let get_log_levels_by_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_log_levels_by_resource_types
           (Values.GetLogLevelsByResourceTypesRequest.make ())
           (Some Values.GetLogLevelsByResourceTypesResponse.to_json)
           (Some Values.GetLogLevelsByResourceTypesResponse.error_to_json)])
let get_metric_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_configuration
           (Values.GetMetricConfigurationRequest.make ())
           (Some Values.GetMetricConfigurationResponse.to_json)
           (Some Values.GetMetricConfigurationResponse.error_to_json)])
let get_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and summaryMetricQueries =
         flag "summary-metric-queries" (optional json_arg)
           ~doc:"JSON SummaryMetricQueries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metrics
           (Values.GetMetricsRequest.make
              ?summaryMetricQueries:(Option.map
                                       ~f:Values.SummaryMetricQueries.of_json
                                       summaryMetricQueries) ())
           (Some Values.GetMetricsResponse.to_json)
           (Some Values.GetMetricsResponse.error_to_json)])
let get_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_multicast_group
           (Values.GetMulticastGroupRequest.make ~id ())
           (Some Values.GetMulticastGroupResponse.to_json)
           (Some Values.GetMulticastGroupResponse.error_to_json)])
let get_multicast_group_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_multicast_group_session
           (Values.GetMulticastGroupSessionRequest.make ~id ())
           (Some Values.GetMulticastGroupSessionResponse.to_json)
           (Some Values.GetMulticastGroupSessionResponse.error_to_json)])
let get_network_analyzer_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING NetworkAnalyzerConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_analyzer_configuration
           (Values.GetNetworkAnalyzerConfigurationRequest.make
              ~configurationName ())
           (Some Values.GetNetworkAnalyzerConfigurationResponse.to_json)
           (Some Values.GetNetworkAnalyzerConfigurationResponse.error_to_json)])
let get_partner_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and partnerAccountId =
         flag "partner-account-id" (required string)
           ~doc:"STRING PartnerAccountId"
       and partnerType =
         flag "partner-type" (required json_arg) ~doc:"JSON PartnerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_partner_account
           (Values.GetPartnerAccountRequest.make ~partnerAccountId
              ~partnerType:(Values.PartnerType.of_json partnerType) ())
           (Some Values.GetPartnerAccountResponse.to_json)
           (Some Values.GetPartnerAccountResponse.error_to_json)])
let get_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING PositionResourceIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON PositionResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_position
           (Values.GetPositionRequest.make ~resourceIdentifier
              ~resourceType:(Values.PositionResourceType.of_json resourceType)
              ()) (Some Values.GetPositionResponse.to_json)
           (Some Values.GetPositionResponse.error_to_json)])
let get_position_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING PositionResourceIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON PositionResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_position_configuration
           (Values.GetPositionConfigurationRequest.make ~resourceIdentifier
              ~resourceType:(Values.PositionResourceType.of_json resourceType)
              ()) (Some Values.GetPositionConfigurationResponse.to_json)
           (Some Values.GetPositionConfigurationResponse.error_to_json)])
let get_position_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wiFiAccessPoints =
         flag "wi-fi-access-points" (optional json_arg)
           ~doc:"JSON WiFiAccessPoints"
       and cellTowers =
         flag "cell-towers" (optional json_arg) ~doc:"JSON CellTowers"
       and ip = flag "ip" (optional json_arg) ~doc:"JSON Ip"
       and gnss = flag "gnss" (optional json_arg) ~doc:"JSON Gnss"
       and timestamp =
         flag "timestamp" (optional json_arg) ~doc:"JSON CreationDate"
       and advancedConfiguration =
         flag "advanced-configuration" (optional json_arg)
           ~doc:"JSON AdvancedConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_position_estimate
           (Values.GetPositionEstimateRequest.make
              ?wiFiAccessPoints:(Option.map
                                   ~f:Values.WiFiAccessPoints.of_json
                                   wiFiAccessPoints)
              ?cellTowers:(Option.map ~f:Values.CellTowers.of_json cellTowers)
              ?ip:(Option.map ~f:Values.Ip.of_json ip)
              ?gnss:(Option.map ~f:Values.Gnss.of_json gnss)
              ?timestamp:(Option.map ~f:Values.CreationDate.of_json timestamp)
              ?advancedConfiguration:(Option.map
                                        ~f:Values.AdvancedConfiguration.of_json
                                        advancedConfiguration) ())
           (Some Values.GetPositionEstimateResponse.to_json)
           (Some Values.GetPositionEstimateResponse.error_to_json)])
let get_resource_event_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and partnerType =
         flag "partner-type" (optional json_arg)
           ~doc:"JSON EventNotificationPartnerType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and identifierType =
         flag "identifier-type" (required json_arg)
           ~doc:"JSON IdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_event_configuration
           (Values.GetResourceEventConfigurationRequest.make
              ?partnerType:(Option.map
                              ~f:Values.EventNotificationPartnerType.of_json
                              partnerType) ~identifier
              ~identifierType:(Values.IdentifierType.of_json identifierType)
              ()) (Some Values.GetResourceEventConfigurationResponse.to_json)
           (Some Values.GetResourceEventConfigurationResponse.error_to_json)])
let get_resource_log_level =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and resourceType =
         flag "resource-type" (required string) ~doc:"STRING ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_log_level
           (Values.GetResourceLogLevelRequest.make ~resourceIdentifier
              ~resourceType ())
           (Some Values.GetResourceLogLevelResponse.to_json)
           (Some Values.GetResourceLogLevelResponse.error_to_json)])
let get_resource_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING PositionResourceIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON PositionResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_position
           (Values.GetResourcePositionRequest.make ~resourceIdentifier
              ~resourceType:(Values.PositionResourceType.of_json resourceType)
              ()) (Some Values.GetResourcePositionResponse.to_json)
           (Some Values.GetResourcePositionResponse.error_to_json)])
let get_service_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceType =
         flag "service-type" (optional json_arg)
           ~doc:"JSON WirelessGatewayServiceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_endpoint
           (Values.GetServiceEndpointRequest.make
              ?serviceType:(Option.map
                              ~f:Values.WirelessGatewayServiceType.of_json
                              serviceType) ())
           (Some Values.GetServiceEndpointResponse.to_json)
           (Some Values.GetServiceEndpointResponse.error_to_json)])
let get_service_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ServiceProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_profile
           (Values.GetServiceProfileRequest.make ~id ())
           (Some Values.GetServiceProfileResponse.to_json)
           (Some Values.GetServiceProfileResponse.error_to_json)])
let get_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and identifierType =
         flag "identifier-type" (required json_arg)
           ~doc:"JSON WirelessDeviceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_device
           (Values.GetWirelessDeviceRequest.make ~identifier
              ~identifierType:(Values.WirelessDeviceIdType.of_json
                                 identifierType) ())
           (Some Values.GetWirelessDeviceResponse.to_json)
           (Some Values.GetWirelessDeviceResponse.error_to_json)])
let get_wireless_device_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ImportTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_device_import_task
           (Values.GetWirelessDeviceImportTaskRequest.make ~id ())
           (Some Values.GetWirelessDeviceImportTaskResponse.to_json)
           (Some Values.GetWirelessDeviceImportTaskResponse.error_to_json)])
let get_wireless_device_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wirelessDeviceId =
         flag "wireless-device-id" (required string)
           ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_device_statistics
           (Values.GetWirelessDeviceStatisticsRequest.make ~wirelessDeviceId
              ()) (Some Values.GetWirelessDeviceStatisticsResponse.to_json)
           (Some Values.GetWirelessDeviceStatisticsResponse.error_to_json)])
let get_wireless_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and identifierType =
         flag "identifier-type" (required json_arg)
           ~doc:"JSON WirelessGatewayIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_gateway
           (Values.GetWirelessGatewayRequest.make ~identifier
              ~identifierType:(Values.WirelessGatewayIdType.of_json
                                 identifierType) ())
           (Some Values.GetWirelessGatewayResponse.to_json)
           (Some Values.GetWirelessGatewayResponse.error_to_json)])
let get_wireless_gateway_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_gateway_certificate
           (Values.GetWirelessGatewayCertificateRequest.make ~id ())
           (Some Values.GetWirelessGatewayCertificateResponse.to_json)
           (Some Values.GetWirelessGatewayCertificateResponse.error_to_json)])
let get_wireless_gateway_firmware_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_gateway_firmware_information
           (Values.GetWirelessGatewayFirmwareInformationRequest.make ~id ())
           (Some Values.GetWirelessGatewayFirmwareInformationResponse.to_json)
           (Some
              Values.GetWirelessGatewayFirmwareInformationResponse.error_to_json)])
let get_wireless_gateway_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wirelessGatewayId =
         flag "wireless-gateway-id" (required string)
           ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_gateway_statistics
           (Values.GetWirelessGatewayStatisticsRequest.make
              ~wirelessGatewayId ())
           (Some Values.GetWirelessGatewayStatisticsResponse.to_json)
           (Some Values.GetWirelessGatewayStatisticsResponse.error_to_json)])
let get_wireless_gateway_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_gateway_task
           (Values.GetWirelessGatewayTaskRequest.make ~id ())
           (Some Values.GetWirelessGatewayTaskResponse.to_json)
           (Some Values.GetWirelessGatewayTaskResponse.error_to_json)])
let get_wireless_gateway_task_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING WirelessGatewayTaskDefinitionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_wireless_gateway_task_definition
           (Values.GetWirelessGatewayTaskDefinitionRequest.make ~id ())
           (Some Values.GetWirelessGatewayTaskDefinitionResponse.to_json)
           (Some
              Values.GetWirelessGatewayTaskDefinitionResponse.error_to_json)])
let list_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_destinations
           (Values.ListDestinationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDestinationsResponse.to_json)
           (Some Values.ListDestinationsResponse.error_to_json)])
let list_device_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and deviceProfileType =
         flag "device-profile-type" (optional json_arg)
           ~doc:"JSON DeviceProfileType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_profiles
           (Values.ListDeviceProfilesRequest.make ?nextToken ?maxResults
              ?deviceProfileType:(Option.map
                                    ~f:Values.DeviceProfileType.of_json
                                    deviceProfileType) ())
           (Some Values.ListDeviceProfilesResponse.to_json)
           (Some Values.ListDeviceProfilesResponse.error_to_json)])
let list_devices_for_wireless_device_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON OnboardStatus"
       and id = flag "id" (required string) ~doc:"STRING ImportTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices_for_wireless_device_import_task
           (Values.ListDevicesForWirelessDeviceImportTaskRequest.make
              ?maxResults ?nextToken
              ?status:(Option.map ~f:Values.OnboardStatus.of_json status) ~id
              ())
           (Some
              Values.ListDevicesForWirelessDeviceImportTaskResponse.to_json)
           (Some
              Values.ListDevicesForWirelessDeviceImportTaskResponse.error_to_json)])
let list_event_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON EventNotificationResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_configurations
           (Values.ListEventConfigurationsRequest.make ?maxResults ?nextToken
              ~resourceType:(Values.EventNotificationResourceType.of_json
                               resourceType) ())
           (Some Values.ListEventConfigurationsResponse.to_json)
           (Some Values.ListEventConfigurationsResponse.error_to_json)])
let list_fuota_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fuota_tasks
           (Values.ListFuotaTasksRequest.make ?nextToken ?maxResults ())
           (Some Values.ListFuotaTasksResponse.to_json)
           (Some Values.ListFuotaTasksResponse.error_to_json)])
let list_multicast_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multicast_groups
           (Values.ListMulticastGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListMulticastGroupsResponse.to_json)
           (Some Values.ListMulticastGroupsResponse.error_to_json)])
let list_multicast_groups_by_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multicast_groups_by_fuota_task
           (Values.ListMulticastGroupsByFuotaTaskRequest.make ?nextToken
              ?maxResults ~id ())
           (Some Values.ListMulticastGroupsByFuotaTaskResponse.to_json)
           (Some Values.ListMulticastGroupsByFuotaTaskResponse.error_to_json)])
let list_network_analyzer_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_network_analyzer_configurations
           (Values.ListNetworkAnalyzerConfigurationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListNetworkAnalyzerConfigurationsResponse.to_json)
           (Some
              Values.ListNetworkAnalyzerConfigurationsResponse.error_to_json)])
let list_partner_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_partner_accounts
           (Values.ListPartnerAccountsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListPartnerAccountsResponse.to_json)
           (Some Values.ListPartnerAccountsResponse.error_to_json)])
let list_position_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional json_arg)
           ~doc:"JSON PositionResourceType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_position_configurations
           (Values.ListPositionConfigurationsRequest.make
              ?resourceType:(Option.map
                               ~f:Values.PositionResourceType.of_json
                               resourceType) ?maxResults ?nextToken ())
           (Some Values.ListPositionConfigurationsResponse.to_json)
           (Some Values.ListPositionConfigurationsResponse.error_to_json)])
let list_queued_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and wirelessDeviceType =
         flag "wireless-device-type" (optional json_arg)
           ~doc:"JSON WirelessDeviceType"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queued_messages
           (Values.ListQueuedMessagesRequest.make ?nextToken ?maxResults
              ?wirelessDeviceType:(Option.map
                                     ~f:Values.WirelessDeviceType.of_json
                                     wirelessDeviceType) ~id ())
           (Some Values.ListQueuedMessagesResponse.to_json)
           (Some Values.ListQueuedMessagesResponse.error_to_json)])
let list_service_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_profiles
           (Values.ListServiceProfilesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListServiceProfilesResponse.to_json)
           (Some Values.ListServiceProfilesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_wireless_device_import_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_wireless_device_import_tasks
           (Values.ListWirelessDeviceImportTasksRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListWirelessDeviceImportTasksResponse.to_json)
           (Some Values.ListWirelessDeviceImportTasksResponse.error_to_json)])
let list_wireless_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and destinationName =
         flag "destination-name" (optional string)
           ~doc:"STRING DestinationName"
       and deviceProfileId =
         flag "device-profile-id" (optional string)
           ~doc:"STRING DeviceProfileId"
       and serviceProfileId =
         flag "service-profile-id" (optional string)
           ~doc:"STRING ServiceProfileId"
       and wirelessDeviceType =
         flag "wireless-device-type" (optional json_arg)
           ~doc:"JSON WirelessDeviceType"
       and fuotaTaskId =
         flag "fuota-task-id" (optional string) ~doc:"STRING FuotaTaskId"
       and multicastGroupId =
         flag "multicast-group-id" (optional string)
           ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_wireless_devices
           (Values.ListWirelessDevicesRequest.make ?maxResults ?nextToken
              ?destinationName ?deviceProfileId ?serviceProfileId
              ?wirelessDeviceType:(Option.map
                                     ~f:Values.WirelessDeviceType.of_json
                                     wirelessDeviceType) ?fuotaTaskId
              ?multicastGroupId ())
           (Some Values.ListWirelessDevicesResponse.to_json)
           (Some Values.ListWirelessDevicesResponse.error_to_json)])
let list_wireless_gateway_task_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and taskDefinitionType =
         flag "task-definition-type" (optional json_arg)
           ~doc:"JSON WirelessGatewayTaskDefinitionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_wireless_gateway_task_definitions
           (Values.ListWirelessGatewayTaskDefinitionsRequest.make ?maxResults
              ?nextToken
              ?taskDefinitionType:(Option.map
                                     ~f:Values.WirelessGatewayTaskDefinitionType.of_json
                                     taskDefinitionType) ())
           (Some Values.ListWirelessGatewayTaskDefinitionsResponse.to_json)
           (Some
              Values.ListWirelessGatewayTaskDefinitionsResponse.error_to_json)])
let list_wireless_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_wireless_gateways
           (Values.ListWirelessGatewaysRequest.make ?nextToken ?maxResults ())
           (Some Values.ListWirelessGatewaysResponse.to_json)
           (Some Values.ListWirelessGatewaysResponse.error_to_json)])
let put_position_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solvers =
         flag "solvers" (optional json_arg)
           ~doc:"JSON PositionSolverConfigurations"
       and destination =
         flag "destination" (optional string) ~doc:"STRING DestinationName"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING PositionResourceIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON PositionResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_position_configuration
           (Values.PutPositionConfigurationRequest.make
              ?solvers:(Option.map
                          ~f:Values.PositionSolverConfigurations.of_json
                          solvers) ?destination ~resourceIdentifier
              ~resourceType:(Values.PositionResourceType.of_json resourceType)
              ()) (Some Values.PutPositionConfigurationResponse.to_json)
           (Some Values.PutPositionConfigurationResponse.error_to_json)])
let put_resource_log_level =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and resourceType =
         flag "resource-type" (required string) ~doc:"STRING ResourceType"
       and logLevel =
         flag "log-level" (required json_arg) ~doc:"JSON LogLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_log_level
           (Values.PutResourceLogLevelRequest.make ~resourceIdentifier
              ~resourceType ~logLevel:(Values.LogLevel.of_json logLevel) ())
           (Some Values.PutResourceLogLevelResponse.to_json)
           (Some Values.PutResourceLogLevelResponse.error_to_json)])
let reset_all_resource_log_levels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_all_resource_log_levels
           (Values.ResetAllResourceLogLevelsRequest.make ())
           (Some Values.ResetAllResourceLogLevelsResponse.to_json)
           (Some Values.ResetAllResourceLogLevelsResponse.error_to_json)])
let reset_resource_log_level =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and resourceType =
         flag "resource-type" (required string) ~doc:"STRING ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_resource_log_level
           (Values.ResetResourceLogLevelRequest.make ~resourceIdentifier
              ~resourceType ())
           (Some Values.ResetResourceLogLevelResponse.to_json)
           (Some Values.ResetResourceLogLevelResponse.error_to_json)])
let send_data_to_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId"
       and payloadData =
         flag "payload-data" (required string) ~doc:"STRING PayloadData"
       and wirelessMetadata =
         flag "wireless-metadata" (required json_arg)
           ~doc:"JSON MulticastWirelessMetadata" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_data_to_multicast_group
           (Values.SendDataToMulticastGroupRequest.make ~id ~payloadData
              ~wirelessMetadata:(Values.MulticastWirelessMetadata.of_json
                                   wirelessMetadata) ())
           (Some Values.SendDataToMulticastGroupResponse.to_json)
           (Some Values.SendDataToMulticastGroupResponse.error_to_json)])
let send_data_to_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wirelessMetadata =
         flag "wireless-metadata" (optional json_arg)
           ~doc:"JSON WirelessMetadata"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId"
       and transmitMode =
         flag "transmit-mode" (required int) ~doc:"INT TransmitMode"
       and payloadData =
         flag "payload-data" (required string) ~doc:"STRING PayloadData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_data_to_wireless_device
           (Values.SendDataToWirelessDeviceRequest.make
              ?wirelessMetadata:(Option.map
                                   ~f:Values.WirelessMetadata.of_json
                                   wirelessMetadata) ~id ~transmitMode
              ~payloadData ())
           (Some Values.SendDataToWirelessDeviceResponse.to_json)
           (Some Values.SendDataToWirelessDeviceResponse.error_to_json)])
let start_bulk_associate_wireless_device_with_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryString =
         flag "query-string" (optional string) ~doc:"STRING QueryString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_bulk_associate_wireless_device_with_multicast_group
           (Values.StartBulkAssociateWirelessDeviceWithMulticastGroupRequest.make
              ?queryString ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~id ())
           (Some
              Values.StartBulkAssociateWirelessDeviceWithMulticastGroupResponse.to_json)
           (Some
              Values.StartBulkAssociateWirelessDeviceWithMulticastGroupResponse.error_to_json)])
let start_bulk_disassociate_wireless_device_from_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryString =
         flag "query-string" (optional string) ~doc:"STRING QueryString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_bulk_disassociate_wireless_device_from_multicast_group
           (Values.StartBulkDisassociateWirelessDeviceFromMulticastGroupRequest.make
              ?queryString ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~id ())
           (Some
              Values.StartBulkDisassociateWirelessDeviceFromMulticastGroupResponse.to_json)
           (Some
              Values.StartBulkDisassociateWirelessDeviceFromMulticastGroupResponse.error_to_json)])
let start_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg)
           ~doc:"JSON LoRaWANStartFuotaTask"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_fuota_task
           (Values.StartFuotaTaskRequest.make
              ?loRaWAN:(Option.map ~f:Values.LoRaWANStartFuotaTask.of_json
                          loRaWAN) ~id ())
           (Some Values.StartFuotaTaskResponse.to_json)
           (Some Values.StartFuotaTaskResponse.error_to_json)])
let start_multicast_group_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId"
       and loRaWAN =
         flag "lo-ra-w-a-n" (required json_arg)
           ~doc:"JSON LoRaWANMulticastSession" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_multicast_group_session
           (Values.StartMulticastGroupSessionRequest.make ~id
              ~loRaWAN:(Values.LoRaWANMulticastSession.of_json loRaWAN) ())
           (Some Values.StartMulticastGroupSessionResponse.to_json)
           (Some Values.StartMulticastGroupSessionResponse.error_to_json)])
let start_single_wireless_device_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and deviceName =
         flag "device-name" (optional string) ~doc:"STRING DeviceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and positioning =
         flag "positioning" (optional json_arg)
           ~doc:"JSON PositioningConfigStatus"
       and destinationName =
         flag "destination-name" (required string)
           ~doc:"STRING DestinationName"
       and sidewalk =
         flag "sidewalk" (required json_arg)
           ~doc:"JSON SidewalkSingleStartImportInfo" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_single_wireless_device_import_task
           (Values.StartSingleWirelessDeviceImportTaskRequest.make
              ?clientRequestToken ?deviceName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?positioning:(Option.map
                              ~f:Values.PositioningConfigStatus.of_json
                              positioning) ~destinationName
              ~sidewalk:(Values.SidewalkSingleStartImportInfo.of_json
                           sidewalk) ())
           (Some Values.StartSingleWirelessDeviceImportTaskResponse.to_json)
           (Some
              Values.StartSingleWirelessDeviceImportTaskResponse.error_to_json)])
let start_wireless_device_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and positioning =
         flag "positioning" (optional json_arg)
           ~doc:"JSON PositioningConfigStatus"
       and destinationName =
         flag "destination-name" (required string)
           ~doc:"STRING DestinationName"
       and sidewalk =
         flag "sidewalk" (required json_arg)
           ~doc:"JSON SidewalkStartImportInfo" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_wireless_device_import_task
           (Values.StartWirelessDeviceImportTaskRequest.make
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?positioning:(Option.map
                              ~f:Values.PositioningConfigStatus.of_json
                              positioning) ~destinationName
              ~sidewalk:(Values.SidewalkStartImportInfo.of_json sidewalk) ())
           (Some Values.StartWirelessDeviceImportTaskResponse.to_json)
           (Some Values.StartWirelessDeviceImportTaskResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let test_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_wireless_device
           (Values.TestWirelessDeviceRequest.make ~id ())
           (Some Values.TestWirelessDeviceResponse.to_json)
           (Some Values.TestWirelessDeviceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expressionType =
         flag "expression-type" (optional json_arg)
           ~doc:"JSON ExpressionType"
       and expression =
         flag "expression" (optional string) ~doc:"STRING Expression"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and name = flag "name" (required string) ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_destination
           (Values.UpdateDestinationRequest.make
              ?expressionType:(Option.map ~f:Values.ExpressionType.of_json
                                 expressionType) ?expression ?description
              ?roleArn ~name ())
           (Some Values.UpdateDestinationResponse.to_json)
           (Some Values.UpdateDestinationResponse.error_to_json)])
let update_event_configuration_by_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceRegistrationState =
         flag "device-registration-state" (optional json_arg)
           ~doc:"JSON DeviceRegistrationStateResourceTypeEventConfiguration"
       and proximity =
         flag "proximity" (optional json_arg)
           ~doc:"JSON ProximityResourceTypeEventConfiguration"
       and join =
         flag "join" (optional json_arg)
           ~doc:"JSON JoinResourceTypeEventConfiguration"
       and connectionStatus =
         flag "connection-status" (optional json_arg)
           ~doc:"JSON ConnectionStatusResourceTypeEventConfiguration"
       and messageDeliveryStatus =
         flag "message-delivery-status" (optional json_arg)
           ~doc:"JSON MessageDeliveryStatusResourceTypeEventConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_configuration_by_resource_types
           (Values.UpdateEventConfigurationByResourceTypesRequest.make
              ?deviceRegistrationState:(Option.map
                                          ~f:Values.DeviceRegistrationStateResourceTypeEventConfiguration.of_json
                                          deviceRegistrationState)
              ?proximity:(Option.map
                            ~f:Values.ProximityResourceTypeEventConfiguration.of_json
                            proximity)
              ?join:(Option.map
                       ~f:Values.JoinResourceTypeEventConfiguration.of_json
                       join)
              ?connectionStatus:(Option.map
                                   ~f:Values.ConnectionStatusResourceTypeEventConfiguration.of_json
                                   connectionStatus)
              ?messageDeliveryStatus:(Option.map
                                        ~f:Values.MessageDeliveryStatusResourceTypeEventConfiguration.of_json
                                        messageDeliveryStatus) ())
           (Some
              Values.UpdateEventConfigurationByResourceTypesResponse.to_json)
           (Some
              Values.UpdateEventConfigurationByResourceTypesResponse.error_to_json)])
let update_fuota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING FuotaTaskName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg) ~doc:"JSON LoRaWANFuotaTask"
       and firmwareUpdateImage =
         flag "firmware-update-image" (optional string)
           ~doc:"STRING FirmwareUpdateImage"
       and firmwareUpdateRole =
         flag "firmware-update-role" (optional string)
           ~doc:"STRING FirmwareUpdateRole"
       and redundancyPercent =
         flag "redundancy-percent" (optional int)
           ~doc:"INT RedundancyPercent"
       and fragmentSizeBytes =
         flag "fragment-size-bytes" (optional int)
           ~doc:"INT FragmentSizeBytes"
       and fragmentIntervalMS =
         flag "fragment-interval-m-s" (optional int)
           ~doc:"INT FragmentIntervalMS"
       and descriptor =
         flag "descriptor" (optional string) ~doc:"STRING FileDescriptor"
       and id = flag "id" (required string) ~doc:"STRING FuotaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fuota_task
           (Values.UpdateFuotaTaskRequest.make ?name ?description
              ?loRaWAN:(Option.map ~f:Values.LoRaWANFuotaTask.of_json loRaWAN)
              ?firmwareUpdateImage ?firmwareUpdateRole ?redundancyPercent
              ?fragmentSizeBytes ?fragmentIntervalMS ?descriptor ~id ())
           (Some Values.UpdateFuotaTaskResponse.to_json)
           (Some Values.UpdateFuotaTaskResponse.error_to_json)])
let update_log_levels_by_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultLogLevel =
         flag "default-log-level" (optional json_arg) ~doc:"JSON LogLevel"
       and fuotaTaskLogOptions =
         flag "fuota-task-log-options" (optional json_arg)
           ~doc:"JSON FuotaTaskLogOptionList"
       and wirelessDeviceLogOptions =
         flag "wireless-device-log-options" (optional json_arg)
           ~doc:"JSON WirelessDeviceLogOptionList"
       and wirelessGatewayLogOptions =
         flag "wireless-gateway-log-options" (optional json_arg)
           ~doc:"JSON WirelessGatewayLogOptionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_log_levels_by_resource_types
           (Values.UpdateLogLevelsByResourceTypesRequest.make
              ?defaultLogLevel:(Option.map ~f:Values.LogLevel.of_json
                                  defaultLogLevel)
              ?fuotaTaskLogOptions:(Option.map
                                      ~f:Values.FuotaTaskLogOptionList.of_json
                                      fuotaTaskLogOptions)
              ?wirelessDeviceLogOptions:(Option.map
                                           ~f:Values.WirelessDeviceLogOptionList.of_json
                                           wirelessDeviceLogOptions)
              ?wirelessGatewayLogOptions:(Option.map
                                            ~f:Values.WirelessGatewayLogOptionList.of_json
                                            wirelessGatewayLogOptions) ())
           (Some Values.UpdateLogLevelsByResourceTypesResponse.to_json)
           (Some Values.UpdateLogLevelsByResourceTypesResponse.error_to_json)])
let update_metric_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and summaryMetric =
         flag "summary-metric" (optional json_arg)
           ~doc:"JSON SummaryMetricConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_metric_configuration
           (Values.UpdateMetricConfigurationRequest.make
              ?summaryMetric:(Option.map
                                ~f:Values.SummaryMetricConfiguration.of_json
                                summaryMetric) ())
           (Some Values.UpdateMetricConfigurationResponse.to_json)
           (Some Values.UpdateMetricConfigurationResponse.error_to_json)])
let update_multicast_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING MulticastGroupName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg) ~doc:"JSON LoRaWANMulticast"
       and id = flag "id" (required string) ~doc:"STRING MulticastGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_multicast_group
           (Values.UpdateMulticastGroupRequest.make ?name ?description
              ?loRaWAN:(Option.map ~f:Values.LoRaWANMulticast.of_json loRaWAN)
              ~id ()) (Some Values.UpdateMulticastGroupResponse.to_json)
           (Some Values.UpdateMulticastGroupResponse.error_to_json)])
let update_network_analyzer_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and traceContent =
         flag "trace-content" (optional json_arg) ~doc:"JSON TraceContent"
       and wirelessDevicesToAdd =
         flag "wireless-devices-to-add" (optional json_arg)
           ~doc:"JSON WirelessDeviceList"
       and wirelessDevicesToRemove =
         flag "wireless-devices-to-remove" (optional json_arg)
           ~doc:"JSON WirelessDeviceList"
       and wirelessGatewaysToAdd =
         flag "wireless-gateways-to-add" (optional json_arg)
           ~doc:"JSON WirelessGatewayList"
       and wirelessGatewaysToRemove =
         flag "wireless-gateways-to-remove" (optional json_arg)
           ~doc:"JSON WirelessGatewayList"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and multicastGroupsToAdd =
         flag "multicast-groups-to-add" (optional json_arg)
           ~doc:"JSON NetworkAnalyzerMulticastGroupList"
       and multicastGroupsToRemove =
         flag "multicast-groups-to-remove" (optional json_arg)
           ~doc:"JSON NetworkAnalyzerMulticastGroupList"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING NetworkAnalyzerConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network_analyzer_configuration
           (Values.UpdateNetworkAnalyzerConfigurationRequest.make
              ?traceContent:(Option.map ~f:Values.TraceContent.of_json
                               traceContent)
              ?wirelessDevicesToAdd:(Option.map
                                       ~f:Values.WirelessDeviceList.of_json
                                       wirelessDevicesToAdd)
              ?wirelessDevicesToRemove:(Option.map
                                          ~f:Values.WirelessDeviceList.of_json
                                          wirelessDevicesToRemove)
              ?wirelessGatewaysToAdd:(Option.map
                                        ~f:Values.WirelessGatewayList.of_json
                                        wirelessGatewaysToAdd)
              ?wirelessGatewaysToRemove:(Option.map
                                           ~f:Values.WirelessGatewayList.of_json
                                           wirelessGatewaysToRemove)
              ?description
              ?multicastGroupsToAdd:(Option.map
                                       ~f:Values.NetworkAnalyzerMulticastGroupList.of_json
                                       multicastGroupsToAdd)
              ?multicastGroupsToRemove:(Option.map
                                          ~f:Values.NetworkAnalyzerMulticastGroupList.of_json
                                          multicastGroupsToRemove)
              ~configurationName ())
           (Some Values.UpdateNetworkAnalyzerConfigurationResponse.to_json)
           (Some
              Values.UpdateNetworkAnalyzerConfigurationResponse.error_to_json)])
let update_partner_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sidewalk =
         flag "sidewalk" (required json_arg)
           ~doc:"JSON SidewalkUpdateAccount"
       and partnerAccountId =
         flag "partner-account-id" (required string)
           ~doc:"STRING PartnerAccountId"
       and partnerType =
         flag "partner-type" (required json_arg) ~doc:"JSON PartnerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_partner_account
           (Values.UpdatePartnerAccountRequest.make
              ~sidewalk:(Values.SidewalkUpdateAccount.of_json sidewalk)
              ~partnerAccountId
              ~partnerType:(Values.PartnerType.of_json partnerType) ())
           (Some Values.UpdatePartnerAccountResponse.to_json)
           (Some Values.UpdatePartnerAccountResponse.error_to_json)])
let update_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING PositionResourceIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON PositionResourceType"
       and position =
         flag "position" (required json_arg) ~doc:"JSON PositionCoordinate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_position
           (Values.UpdatePositionRequest.make ~resourceIdentifier
              ~resourceType:(Values.PositionResourceType.of_json resourceType)
              ~position:(Values.PositionCoordinate.of_json position) ())
           (Some Values.UpdatePositionResponse.to_json)
           (Some Values.UpdatePositionResponse.error_to_json)])
let update_resource_event_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and partnerType =
         flag "partner-type" (optional json_arg)
           ~doc:"JSON EventNotificationPartnerType"
       and deviceRegistrationState =
         flag "device-registration-state" (optional json_arg)
           ~doc:"JSON DeviceRegistrationStateEventConfiguration"
       and proximity =
         flag "proximity" (optional json_arg)
           ~doc:"JSON ProximityEventConfiguration"
       and join =
         flag "join" (optional json_arg) ~doc:"JSON JoinEventConfiguration"
       and connectionStatus =
         flag "connection-status" (optional json_arg)
           ~doc:"JSON ConnectionStatusEventConfiguration"
       and messageDeliveryStatus =
         flag "message-delivery-status" (optional json_arg)
           ~doc:"JSON MessageDeliveryStatusEventConfiguration"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and identifierType =
         flag "identifier-type" (required json_arg)
           ~doc:"JSON IdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_event_configuration
           (Values.UpdateResourceEventConfigurationRequest.make
              ?partnerType:(Option.map
                              ~f:Values.EventNotificationPartnerType.of_json
                              partnerType)
              ?deviceRegistrationState:(Option.map
                                          ~f:Values.DeviceRegistrationStateEventConfiguration.of_json
                                          deviceRegistrationState)
              ?proximity:(Option.map
                            ~f:Values.ProximityEventConfiguration.of_json
                            proximity)
              ?join:(Option.map ~f:Values.JoinEventConfiguration.of_json join)
              ?connectionStatus:(Option.map
                                   ~f:Values.ConnectionStatusEventConfiguration.of_json
                                   connectionStatus)
              ?messageDeliveryStatus:(Option.map
                                        ~f:Values.MessageDeliveryStatusEventConfiguration.of_json
                                        messageDeliveryStatus) ~identifier
              ~identifierType:(Values.IdentifierType.of_json identifierType)
              ())
           (Some Values.UpdateResourceEventConfigurationResponse.to_json)
           (Some
              Values.UpdateResourceEventConfigurationResponse.error_to_json)])
let update_resource_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and geoJsonPayload =
         flag "geo-json-payload" (optional json_arg)
           ~doc:"JSON GeoJsonPayload"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING PositionResourceIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON PositionResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_position
           (Values.UpdateResourcePositionRequest.make
              ?geoJsonPayload:(Option.map ~f:Values.GeoJsonPayload.of_json
                                 geoJsonPayload) ~resourceIdentifier
              ~resourceType:(Values.PositionResourceType.of_json resourceType)
              ()) (Some Values.UpdateResourcePositionResponse.to_json)
           (Some Values.UpdateResourcePositionResponse.error_to_json)])
let update_wireless_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationName =
         flag "destination-name" (optional string)
           ~doc:"STRING DestinationName"
       and name =
         flag "name" (optional string) ~doc:"STRING WirelessDeviceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and loRaWAN =
         flag "lo-ra-w-a-n" (optional json_arg)
           ~doc:"JSON LoRaWANUpdateDevice"
       and positioning =
         flag "positioning" (optional json_arg)
           ~doc:"JSON PositioningConfigStatus"
       and sidewalk =
         flag "sidewalk" (optional json_arg)
           ~doc:"JSON SidewalkUpdateWirelessDevice"
       and id = flag "id" (required string) ~doc:"STRING WirelessDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_wireless_device
           (Values.UpdateWirelessDeviceRequest.make ?destinationName ?name
              ?description
              ?loRaWAN:(Option.map ~f:Values.LoRaWANUpdateDevice.of_json
                          loRaWAN)
              ?positioning:(Option.map
                              ~f:Values.PositioningConfigStatus.of_json
                              positioning)
              ?sidewalk:(Option.map
                           ~f:Values.SidewalkUpdateWirelessDevice.of_json
                           sidewalk) ~id ())
           (Some Values.UpdateWirelessDeviceResponse.to_json)
           (Some Values.UpdateWirelessDeviceResponse.error_to_json)])
let update_wireless_device_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ImportTaskId"
       and sidewalk =
         flag "sidewalk" (required json_arg)
           ~doc:"JSON SidewalkUpdateImportInfo" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_wireless_device_import_task
           (Values.UpdateWirelessDeviceImportTaskRequest.make ~id
              ~sidewalk:(Values.SidewalkUpdateImportInfo.of_json sidewalk) ())
           (Some Values.UpdateWirelessDeviceImportTaskResponse.to_json)
           (Some Values.UpdateWirelessDeviceImportTaskResponse.error_to_json)])
let update_wireless_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING WirelessGatewayName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and joinEuiFilters =
         flag "join-eui-filters" (optional json_arg)
           ~doc:"JSON JoinEuiFilters"
       and netIdFilters =
         flag "net-id-filters" (optional json_arg) ~doc:"JSON NetIdFilters"
       and maxEirp =
         flag "max-eirp" (optional float) ~doc:"FLOAT GatewayMaxEirp"
       and id = flag "id" (required string) ~doc:"STRING WirelessGatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_wireless_gateway
           (Values.UpdateWirelessGatewayRequest.make ?name ?description
              ?joinEuiFilters:(Option.map ~f:Values.JoinEuiFilters.of_json
                                 joinEuiFilters)
              ?netIdFilters:(Option.map ~f:Values.NetIdFilters.of_json
                               netIdFilters) ?maxEirp ~id ())
           (Some Values.UpdateWirelessGatewayResponse.to_json)
           (Some Values.UpdateWirelessGatewayResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-aws-account-with-partner-account",
       associate_aws_account_with_partner_account);
    ("associate-multicast-group-with-fuota-task",
      associate_multicast_group_with_fuota_task);
    ("associate-wireless-device-with-fuota-task",
      associate_wireless_device_with_fuota_task);
    ("associate-wireless-device-with-multicast-group",
      associate_wireless_device_with_multicast_group);
    ("associate-wireless-device-with-thing",
      associate_wireless_device_with_thing);
    ("associate-wireless-gateway-with-certificate",
      associate_wireless_gateway_with_certificate);
    ("associate-wireless-gateway-with-thing",
      associate_wireless_gateway_with_thing);
    ("cancel-multicast-group-session", cancel_multicast_group_session);
    ("create-destination", create_destination);
    ("create-device-profile", create_device_profile);
    ("create-fuota-task", create_fuota_task);
    ("create-multicast-group", create_multicast_group);
    ("create-network-analyzer-configuration",
      create_network_analyzer_configuration);
    ("create-service-profile", create_service_profile);
    ("create-wireless-device", create_wireless_device);
    ("create-wireless-gateway", create_wireless_gateway);
    ("create-wireless-gateway-task", create_wireless_gateway_task);
    ("create-wireless-gateway-task-definition",
      create_wireless_gateway_task_definition);
    ("delete-destination", delete_destination);
    ("delete-device-profile", delete_device_profile);
    ("delete-fuota-task", delete_fuota_task);
    ("delete-multicast-group", delete_multicast_group);
    ("delete-network-analyzer-configuration",
      delete_network_analyzer_configuration);
    ("delete-queued-messages", delete_queued_messages);
    ("delete-service-profile", delete_service_profile);
    ("delete-wireless-device", delete_wireless_device);
    ("delete-wireless-device-import-task",
      delete_wireless_device_import_task);
    ("delete-wireless-gateway", delete_wireless_gateway);
    ("delete-wireless-gateway-task", delete_wireless_gateway_task);
    ("delete-wireless-gateway-task-definition",
      delete_wireless_gateway_task_definition);
    ("deregister-wireless-device", deregister_wireless_device);
    ("disassociate-aws-account-from-partner-account",
      disassociate_aws_account_from_partner_account);
    ("disassociate-multicast-group-from-fuota-task",
      disassociate_multicast_group_from_fuota_task);
    ("disassociate-wireless-device-from-fuota-task",
      disassociate_wireless_device_from_fuota_task);
    ("disassociate-wireless-device-from-multicast-group",
      disassociate_wireless_device_from_multicast_group);
    ("disassociate-wireless-device-from-thing",
      disassociate_wireless_device_from_thing);
    ("disassociate-wireless-gateway-from-certificate",
      disassociate_wireless_gateway_from_certificate);
    ("disassociate-wireless-gateway-from-thing",
      disassociate_wireless_gateway_from_thing);
    ("get-destination", get_destination);
    ("get-device-profile", get_device_profile);
    ("get-event-configuration-by-resource-types",
      get_event_configuration_by_resource_types);
    ("get-fuota-task", get_fuota_task);
    ("get-log-levels-by-resource-types", get_log_levels_by_resource_types);
    ("get-metric-configuration", get_metric_configuration);
    ("get-metrics", get_metrics);
    ("get-multicast-group", get_multicast_group);
    ("get-multicast-group-session", get_multicast_group_session);
    ("get-network-analyzer-configuration",
      get_network_analyzer_configuration);
    ("get-partner-account", get_partner_account);
    ("get-position", get_position);
    ("get-position-configuration", get_position_configuration);
    ("get-position-estimate", get_position_estimate);
    ("get-resource-event-configuration", get_resource_event_configuration);
    ("get-resource-log-level", get_resource_log_level);
    ("get-resource-position", get_resource_position);
    ("get-service-endpoint", get_service_endpoint);
    ("get-service-profile", get_service_profile);
    ("get-wireless-device", get_wireless_device);
    ("get-wireless-device-import-task", get_wireless_device_import_task);
    ("get-wireless-device-statistics", get_wireless_device_statistics);
    ("get-wireless-gateway", get_wireless_gateway);
    ("get-wireless-gateway-certificate", get_wireless_gateway_certificate);
    ("get-wireless-gateway-firmware-information",
      get_wireless_gateway_firmware_information);
    ("get-wireless-gateway-statistics", get_wireless_gateway_statistics);
    ("get-wireless-gateway-task", get_wireless_gateway_task);
    ("get-wireless-gateway-task-definition",
      get_wireless_gateway_task_definition);
    ("list-destinations", list_destinations);
    ("list-device-profiles", list_device_profiles);
    ("list-devices-for-wireless-device-import-task",
      list_devices_for_wireless_device_import_task);
    ("list-event-configurations", list_event_configurations);
    ("list-fuota-tasks", list_fuota_tasks);
    ("list-multicast-groups", list_multicast_groups);
    ("list-multicast-groups-by-fuota-task",
      list_multicast_groups_by_fuota_task);
    ("list-network-analyzer-configurations",
      list_network_analyzer_configurations);
    ("list-partner-accounts", list_partner_accounts);
    ("list-position-configurations", list_position_configurations);
    ("list-queued-messages", list_queued_messages);
    ("list-service-profiles", list_service_profiles);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-wireless-device-import-tasks", list_wireless_device_import_tasks);
    ("list-wireless-devices", list_wireless_devices);
    ("list-wireless-gateway-task-definitions",
      list_wireless_gateway_task_definitions);
    ("list-wireless-gateways", list_wireless_gateways);
    ("put-position-configuration", put_position_configuration);
    ("put-resource-log-level", put_resource_log_level);
    ("reset-all-resource-log-levels", reset_all_resource_log_levels);
    ("reset-resource-log-level", reset_resource_log_level);
    ("send-data-to-multicast-group", send_data_to_multicast_group);
    ("send-data-to-wireless-device", send_data_to_wireless_device);
    ("start-bulk-associate-wireless-device-with-multicast-group",
      start_bulk_associate_wireless_device_with_multicast_group);
    ("start-bulk-disassociate-wireless-device-from-multicast-group",
      start_bulk_disassociate_wireless_device_from_multicast_group);
    ("start-fuota-task", start_fuota_task);
    ("start-multicast-group-session", start_multicast_group_session);
    ("start-single-wireless-device-import-task",
      start_single_wireless_device_import_task);
    ("start-wireless-device-import-task", start_wireless_device_import_task);
    ("tag-resource", tag_resource);
    ("test-wireless-device", test_wireless_device);
    ("untag-resource", untag_resource);
    ("update-destination", update_destination);
    ("update-event-configuration-by-resource-types",
      update_event_configuration_by_resource_types);
    ("update-fuota-task", update_fuota_task);
    ("update-log-levels-by-resource-types",
      update_log_levels_by_resource_types);
    ("update-metric-configuration", update_metric_configuration);
    ("update-multicast-group", update_multicast_group);
    ("update-network-analyzer-configuration",
      update_network_analyzer_configuration);
    ("update-partner-account", update_partner_account);
    ("update-position", update_position);
    ("update-resource-event-configuration",
      update_resource_event_configuration);
    ("update-resource-position", update_resource_position);
    ("update-wireless-device", update_wireless_device);
    ("update-wireless-device-import-task",
      update_wireless_device_import_task);
    ("update-wireless-gateway", update_wireless_gateway)]
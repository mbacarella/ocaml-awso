(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_sol_network_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsLcmOpOccId =
         flag "ns-lcm-op-occ-id" (required string) ~doc:"STRING NsLcmOpOccId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_sol_network_operation
           (Values.CancelSolNetworkOperationInput.make ~nsLcmOpOccId ()) None
           None])
let create_sol_function_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sol_function_package
           (Values.CreateSolFunctionPackageInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ())
           (Some Values.CreateSolFunctionPackageOutput.to_json)
           (Some Values.CreateSolFunctionPackageOutput.error_to_json)])
let create_sol_network_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsDescription =
         flag "ns-description" (optional string)
           ~doc:"STRING CreateSolNetworkInstanceInputNsDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and nsName =
         flag "ns-name" (required string)
           ~doc:"STRING CreateSolNetworkInstanceInputNsNameString"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sol_network_instance
           (Values.CreateSolNetworkInstanceInput.make ?nsDescription
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~nsName
              ~nsdInfoId ())
           (Some Values.CreateSolNetworkInstanceOutput.to_json)
           (Some Values.CreateSolNetworkInstanceOutput.error_to_json)])
let create_sol_network_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sol_network_package
           (Values.CreateSolNetworkPackageInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ())
           (Some Values.CreateSolNetworkPackageOutput.to_json)
           (Some Values.CreateSolNetworkPackageOutput.error_to_json)])
let delete_sol_function_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sol_function_package
           (Values.DeleteSolFunctionPackageInput.make ~vnfPkgId ()) None None])
let delete_sol_network_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsInstanceId =
         flag "ns-instance-id" (required string) ~doc:"STRING NsInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sol_network_instance
           (Values.DeleteSolNetworkInstanceInput.make ~nsInstanceId ()) None
           None])
let delete_sol_network_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sol_network_package
           (Values.DeleteSolNetworkPackageInput.make ~nsdInfoId ()) None None])
let get_sol_function_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vnfInstanceId =
         flag "vnf-instance-id" (required string) ~doc:"STRING VnfInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_function_instance
           (Values.GetSolFunctionInstanceInput.make ~vnfInstanceId ())
           (Some Values.GetSolFunctionInstanceOutput.to_json)
           (Some Values.GetSolFunctionInstanceOutput.error_to_json)])
let get_sol_function_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_function_package
           (Values.GetSolFunctionPackageInput.make ~vnfPkgId ())
           (Some Values.GetSolFunctionPackageOutput.to_json)
           (Some Values.GetSolFunctionPackageOutput.error_to_json)])
let get_sol_function_package_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accept =
         flag "accept" (required json_arg) ~doc:"JSON PackageContentType"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_function_package_content
           (Values.GetSolFunctionPackageContentInput.make
              ~accept:(Values.PackageContentType.of_json accept) ~vnfPkgId ())
           (Some Values.GetSolFunctionPackageContentOutput.to_json)
           (Some Values.GetSolFunctionPackageContentOutput.error_to_json)])
let get_sol_function_package_descriptor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accept =
         flag "accept" (required json_arg) ~doc:"JSON DescriptorContentType"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_function_package_descriptor
           (Values.GetSolFunctionPackageDescriptorInput.make
              ~accept:(Values.DescriptorContentType.of_json accept) ~vnfPkgId
              ()) (Some Values.GetSolFunctionPackageDescriptorOutput.to_json)
           (Some Values.GetSolFunctionPackageDescriptorOutput.error_to_json)])
let get_sol_network_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsInstanceId =
         flag "ns-instance-id" (required string) ~doc:"STRING NsInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_network_instance
           (Values.GetSolNetworkInstanceInput.make ~nsInstanceId ())
           (Some Values.GetSolNetworkInstanceOutput.to_json)
           (Some Values.GetSolNetworkInstanceOutput.error_to_json)])
let get_sol_network_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsLcmOpOccId =
         flag "ns-lcm-op-occ-id" (required string) ~doc:"STRING NsLcmOpOccId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_network_operation
           (Values.GetSolNetworkOperationInput.make ~nsLcmOpOccId ())
           (Some Values.GetSolNetworkOperationOutput.to_json)
           (Some Values.GetSolNetworkOperationOutput.error_to_json)])
let get_sol_network_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_network_package
           (Values.GetSolNetworkPackageInput.make ~nsdInfoId ())
           (Some Values.GetSolNetworkPackageOutput.to_json)
           (Some Values.GetSolNetworkPackageOutput.error_to_json)])
let get_sol_network_package_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accept =
         flag "accept" (required json_arg) ~doc:"JSON PackageContentType"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_network_package_content
           (Values.GetSolNetworkPackageContentInput.make
              ~accept:(Values.PackageContentType.of_json accept) ~nsdInfoId
              ()) (Some Values.GetSolNetworkPackageContentOutput.to_json)
           (Some Values.GetSolNetworkPackageContentOutput.error_to_json)])
let get_sol_network_package_descriptor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sol_network_package_descriptor
           (Values.GetSolNetworkPackageDescriptorInput.make ~nsdInfoId ())
           (Some Values.GetSolNetworkPackageDescriptorOutput.to_json)
           (Some Values.GetSolNetworkPackageDescriptorOutput.error_to_json)])
let instantiate_sol_network_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and additionalParamsForNs =
         flag "additional-params-for-ns" (optional json_arg)
           ~doc:"JSON Document"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and nsInstanceId =
         flag "ns-instance-id" (required string) ~doc:"STRING NsInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.instantiate_sol_network_instance
           (Values.InstantiateSolNetworkInstanceInput.make
              ?additionalParamsForNs:(Option.map ~f:Values.Document.of_json
                                        additionalParamsForNs) ?dryRun
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~nsInstanceId
              ()) (Some Values.InstantiateSolNetworkInstanceOutput.to_json)
           (Some Values.InstantiateSolNetworkInstanceOutput.error_to_json)])
let list_sol_function_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSolFunctionInstancesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sol_function_instances
           (Values.ListSolFunctionInstancesInput.make ?maxResults ?nextToken
              ()) (Some Values.ListSolFunctionInstancesOutput.to_json)
           (Some Values.ListSolFunctionInstancesOutput.error_to_json)])
let list_sol_function_packages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSolFunctionPackagesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sol_function_packages
           (Values.ListSolFunctionPackagesInput.make ?maxResults ?nextToken
              ()) (Some Values.ListSolFunctionPackagesOutput.to_json)
           (Some Values.ListSolFunctionPackagesOutput.error_to_json)])
let list_sol_network_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSolNetworkInstancesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sol_network_instances
           (Values.ListSolNetworkInstancesInput.make ?maxResults ?nextToken
              ()) (Some Values.ListSolNetworkInstancesOutput.to_json)
           (Some Values.ListSolNetworkInstancesOutput.error_to_json)])
let list_sol_network_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSolNetworkOperationsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and nsInstanceId =
         flag "ns-instance-id" (optional string) ~doc:"STRING NsInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sol_network_operations
           (Values.ListSolNetworkOperationsInput.make ?maxResults ?nextToken
              ?nsInstanceId ())
           (Some Values.ListSolNetworkOperationsOutput.to_json)
           (Some Values.ListSolNetworkOperationsOutput.error_to_json)])
let list_sol_network_packages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSolNetworkPackagesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sol_network_packages
           (Values.ListSolNetworkPackagesInput.make ?maxResults ?nextToken ())
           (Some Values.ListSolNetworkPackagesOutput.to_json)
           (Some Values.ListSolNetworkPackagesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TNBResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_sol_function_package_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional json_arg)
           ~doc:"JSON PackageContentType"
       and file = flag "file" (required json_arg) ~doc:"JSON SensitiveBlob"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_sol_function_package_content
           (Values.PutSolFunctionPackageContentInput.make
              ?contentType:(Option.map ~f:Values.PackageContentType.of_json
                              contentType)
              ~file:(Values.SensitiveBlob.of_json file) ~vnfPkgId ())
           (Some Values.PutSolFunctionPackageContentOutput.to_json)
           (Some Values.PutSolFunctionPackageContentOutput.error_to_json)])
let put_sol_network_package_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional json_arg)
           ~doc:"JSON PackageContentType"
       and file = flag "file" (required json_arg) ~doc:"JSON SensitiveBlob"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_sol_network_package_content
           (Values.PutSolNetworkPackageContentInput.make
              ?contentType:(Option.map ~f:Values.PackageContentType.of_json
                              contentType)
              ~file:(Values.SensitiveBlob.of_json file) ~nsdInfoId ())
           (Some Values.PutSolNetworkPackageContentOutput.to_json)
           (Some Values.PutSolNetworkPackageContentOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TNBResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let terminate_sol_network_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and nsInstanceId =
         flag "ns-instance-id" (required string) ~doc:"STRING NsInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_sol_network_instance
           (Values.TerminateSolNetworkInstanceInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~nsInstanceId
              ()) (Some Values.TerminateSolNetworkInstanceOutput.to_json)
           (Some Values.TerminateSolNetworkInstanceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TNBResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_sol_function_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationalState =
         flag "operational-state" (required json_arg)
           ~doc:"JSON OperationalState"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sol_function_package
           (Values.UpdateSolFunctionPackageInput.make
              ~operationalState:(Values.OperationalState.of_json
                                   operationalState) ~vnfPkgId ())
           (Some Values.UpdateSolFunctionPackageOutput.to_json)
           (Some Values.UpdateSolFunctionPackageOutput.error_to_json)])
let update_sol_network_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modifyVnfInfoData =
         flag "modify-vnf-info-data" (optional json_arg)
           ~doc:"JSON UpdateSolNetworkModify"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and updateNs =
         flag "update-ns" (optional json_arg)
           ~doc:"JSON UpdateSolNetworkServiceData"
       and nsInstanceId =
         flag "ns-instance-id" (required string) ~doc:"STRING NsInstanceId"
       and updateType =
         flag "update-type" (required json_arg)
           ~doc:"JSON UpdateSolNetworkType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sol_network_instance
           (Values.UpdateSolNetworkInstanceInput.make
              ?modifyVnfInfoData:(Option.map
                                    ~f:Values.UpdateSolNetworkModify.of_json
                                    modifyVnfInfoData)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?updateNs:(Option.map
                           ~f:Values.UpdateSolNetworkServiceData.of_json
                           updateNs) ~nsInstanceId
              ~updateType:(Values.UpdateSolNetworkType.of_json updateType) ())
           (Some Values.UpdateSolNetworkInstanceOutput.to_json)
           (Some Values.UpdateSolNetworkInstanceOutput.error_to_json)])
let update_sol_network_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId"
       and nsdOperationalState =
         flag "nsd-operational-state" (required json_arg)
           ~doc:"JSON NsdOperationalState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sol_network_package
           (Values.UpdateSolNetworkPackageInput.make ~nsdInfoId
              ~nsdOperationalState:(Values.NsdOperationalState.of_json
                                      nsdOperationalState) ())
           (Some Values.UpdateSolNetworkPackageOutput.to_json)
           (Some Values.UpdateSolNetworkPackageOutput.error_to_json)])
let validate_sol_function_package_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional json_arg)
           ~doc:"JSON PackageContentType"
       and file = flag "file" (required json_arg) ~doc:"JSON SensitiveBlob"
       and vnfPkgId =
         flag "vnf-pkg-id" (required string) ~doc:"STRING VnfPkgId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_sol_function_package_content
           (Values.ValidateSolFunctionPackageContentInput.make
              ?contentType:(Option.map ~f:Values.PackageContentType.of_json
                              contentType)
              ~file:(Values.SensitiveBlob.of_json file) ~vnfPkgId ())
           (Some Values.ValidateSolFunctionPackageContentOutput.to_json)
           (Some Values.ValidateSolFunctionPackageContentOutput.error_to_json)])
let validate_sol_network_package_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional json_arg)
           ~doc:"JSON PackageContentType"
       and file = flag "file" (required json_arg) ~doc:"JSON SensitiveBlob"
       and nsdInfoId =
         flag "nsd-info-id" (required string) ~doc:"STRING NsdInfoId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_sol_network_package_content
           (Values.ValidateSolNetworkPackageContentInput.make
              ?contentType:(Option.map ~f:Values.PackageContentType.of_json
                              contentType)
              ~file:(Values.SensitiveBlob.of_json file) ~nsdInfoId ())
           (Some Values.ValidateSolNetworkPackageContentOutput.to_json)
           (Some Values.ValidateSolNetworkPackageContentOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-sol-network-operation", cancel_sol_network_operation);
    ("create-sol-function-package", create_sol_function_package);
    ("create-sol-network-instance", create_sol_network_instance);
    ("create-sol-network-package", create_sol_network_package);
    ("delete-sol-function-package", delete_sol_function_package);
    ("delete-sol-network-instance", delete_sol_network_instance);
    ("delete-sol-network-package", delete_sol_network_package);
    ("get-sol-function-instance", get_sol_function_instance);
    ("get-sol-function-package", get_sol_function_package);
    ("get-sol-function-package-content", get_sol_function_package_content);
    ("get-sol-function-package-descriptor",
      get_sol_function_package_descriptor);
    ("get-sol-network-instance", get_sol_network_instance);
    ("get-sol-network-operation", get_sol_network_operation);
    ("get-sol-network-package", get_sol_network_package);
    ("get-sol-network-package-content", get_sol_network_package_content);
    ("get-sol-network-package-descriptor",
      get_sol_network_package_descriptor);
    ("instantiate-sol-network-instance", instantiate_sol_network_instance);
    ("list-sol-function-instances", list_sol_function_instances);
    ("list-sol-function-packages", list_sol_function_packages);
    ("list-sol-network-instances", list_sol_network_instances);
    ("list-sol-network-operations", list_sol_network_operations);
    ("list-sol-network-packages", list_sol_network_packages);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-sol-function-package-content", put_sol_function_package_content);
    ("put-sol-network-package-content", put_sol_network_package_content);
    ("tag-resource", tag_resource);
    ("terminate-sol-network-instance", terminate_sol_network_instance);
    ("untag-resource", untag_resource);
    ("update-sol-function-package", update_sol_function_package);
    ("update-sol-network-instance", update_sol_network_instance);
    ("update-sol-network-package", update_sol_network_package);
    ("validate-sol-function-package-content",
      validate_sol_function_package_content);
    ("validate-sol-network-package-content",
      validate_sol_network_package_content)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.codebuild
let apiVersion = "2016-10-06"
let endpointPrefix = "codebuild"
let serviceFullName = "AWS CodeBuild"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "CodeBuild_20161006"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ArtifactsType =
  struct
    type nonrec t =
      | CODEPIPELINE 
      | S3 
      | NO_ARTIFACTS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CODEPIPELINE -> "CODEPIPELINE"
      | S3 -> "S3"
      | NO_ARTIFACTS -> "NO_ARTIFACTS"
      | Non_static_id s -> s
    let of_string =
      function
      | "CODEPIPELINE" -> CODEPIPELINE
      | "S3" -> S3
      | "NO_ARTIFACTS" -> NO_ARTIFACTS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ArtifactsType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ArtifactsType" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module ResolvedArtifact =
  struct
    type nonrec t =
      {
      type_: ArtifactsType.t option
        [@ocaml.doc "Specifies the type of artifact."];
      location: String_.t option [@ocaml.doc "The location of the artifact."];
      identifier: String_.t option
        [@ocaml.doc "The identifier of the artifact."]}
    let make ?type_ =
      fun ?location ->
        fun ?identifier -> fun () -> { type_; location; identifier }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:ArtifactsType.to_value));
        ("location", (Option.map x.location ~f:String_.to_value));
        ("identifier", (Option.map x.identifier ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "identifier") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      let type_ =
        (Option.map ~f:ArtifactsType.of_xml) (Xml.child xml_arg0 "type") in
      make ?identifier ?location ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier = field_map json__ "identifier" String_.of_json in
      let location = field_map json__ "location" String_.of_json in
      let type_ = field_map json__ "type" ArtifactsType.of_json in
      make ?identifier ?location ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a resolved build artifact. A resolved artifact is an artifact that is built and deployed to the destination, such as Amazon S3."]
module ImageVersions =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ImageVersions" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module PhaseContext =
  struct
    type nonrec t =
      {
      statusCode: String_.t option
        [@ocaml.doc "The status code for the context of the build phase."];
      message: String_.t option
        [@ocaml.doc
          "An explanation of the build phase's context. This might include a command ID and an exit code."]}
    let make ?statusCode = fun ?message -> fun () -> { statusCode; message }
    let to_value x =
      structure_to_value
        [("statusCode", (Option.map x.statusCode ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let statusCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusCode") in
      make ?message ?statusCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let statusCode = field_map json__ "statusCode" String_.of_json in
      make ?message ?statusCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Additional information about a build phase that has an error. You can use this information for troubleshooting."]
module WebhookFilterType =
  struct
    type nonrec t =
      | EVENT 
      | BASE_REF 
      | HEAD_REF 
      | ACTOR_ACCOUNT_ID 
      | FILE_PATH 
      | COMMIT_MESSAGE 
      | WORKFLOW_NAME 
      | TAG_NAME 
      | RELEASE_NAME 
      | REPOSITORY_NAME 
      | ORGANIZATION_NAME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EVENT -> "EVENT"
      | BASE_REF -> "BASE_REF"
      | HEAD_REF -> "HEAD_REF"
      | ACTOR_ACCOUNT_ID -> "ACTOR_ACCOUNT_ID"
      | FILE_PATH -> "FILE_PATH"
      | COMMIT_MESSAGE -> "COMMIT_MESSAGE"
      | WORKFLOW_NAME -> "WORKFLOW_NAME"
      | TAG_NAME -> "TAG_NAME"
      | RELEASE_NAME -> "RELEASE_NAME"
      | REPOSITORY_NAME -> "REPOSITORY_NAME"
      | ORGANIZATION_NAME -> "ORGANIZATION_NAME"
      | Non_static_id s -> s
    let of_string =
      function
      | "EVENT" -> EVENT
      | "BASE_REF" -> BASE_REF
      | "HEAD_REF" -> HEAD_REF
      | "ACTOR_ACCOUNT_ID" -> ACTOR_ACCOUNT_ID
      | "FILE_PATH" -> FILE_PATH
      | "COMMIT_MESSAGE" -> COMMIT_MESSAGE
      | "WORKFLOW_NAME" -> WORKFLOW_NAME
      | "TAG_NAME" -> TAG_NAME
      | "RELEASE_NAME" -> RELEASE_NAME
      | "REPOSITORY_NAME" -> REPOSITORY_NAME
      | "ORGANIZATION_NAME" -> ORGANIZATION_NAME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration WebhookFilterType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WebhookFilterType" j)
    let to_json = simple_to_json to_value
  end
module WrapperBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ResolvedSecondaryArtifacts =
  struct
    type nonrec t = ResolvedArtifact.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ResolvedArtifact.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResolvedArtifact.of_xml)
    let of_json j =
      list_of_json ~kind:"ResolvedSecondaryArtifacts"
        ~of_json:ResolvedArtifact.of_json j
    let to_json v = composed_to_json to_value v
  end
module StatusType =
  struct
    type nonrec t =
      | SUCCEEDED 
      | FAILED 
      | FAULT 
      | TIMED_OUT 
      | IN_PROGRESS 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUCCEEDED -> "SUCCEEDED"
      | FAILED -> "FAILED"
      | FAULT -> "FAULT"
      | TIMED_OUT -> "TIMED_OUT"
      | IN_PROGRESS -> "IN_PROGRESS"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUCCEEDED" -> SUCCEEDED
      | "FAILED" -> FAILED
      | "FAULT" -> FAULT
      | "TIMED_OUT" -> TIMED_OUT
      | "IN_PROGRESS" -> IN_PROGRESS
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StatusType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StatusType" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module EnvironmentImage =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the Docker image."];
      description: String_.t option
        [@ocaml.doc "The description of the Docker image."];
      versions: ImageVersions.t option
        [@ocaml.doc "A list of environment image versions."]}
    let make ?name =
      fun ?description ->
        fun ?versions -> fun () -> { name; description; versions }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("versions", (Option.map x.versions ~f:ImageVersions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versions =
        (Option.map ~f:ImageVersions.of_xml) (Xml.child xml_arg0 "versions") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?versions ?description ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let versions = field_map json__ "versions" ImageVersions.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?versions ?description ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a Docker image that is managed by CodeBuild."]
module NonEmptyString =
  struct
    type nonrec t = string
    let context_ = "NonEmptyString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonEmptyString" j
    let to_json = simple_to_json to_value
  end
module EnvironmentVariableType =
  struct
    type nonrec t =
      | PLAINTEXT 
      | PARAMETER_STORE 
      | SECRETS_MANAGER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PLAINTEXT -> "PLAINTEXT"
      | PARAMETER_STORE -> "PARAMETER_STORE"
      | SECRETS_MANAGER -> "SECRETS_MANAGER"
      | Non_static_id s -> s
    let of_string =
      function
      | "PLAINTEXT" -> PLAINTEXT
      | "PARAMETER_STORE" -> PARAMETER_STORE
      | "SECRETS_MANAGER" -> SECRETS_MANAGER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EnvironmentVariableType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"EnvironmentVariableType" j)
    let to_json = simple_to_json to_value
  end
module SourceAuthType =
  struct
    type nonrec t =
      | OAUTH 
      | CODECONNECTIONS 
      | SECRETS_MANAGER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OAUTH -> "OAUTH"
      | CODECONNECTIONS -> "CODECONNECTIONS"
      | SECRETS_MANAGER -> "SECRETS_MANAGER"
      | Non_static_id s -> s
    let of_string =
      function
      | "OAUTH" -> OAUTH
      | "CODECONNECTIONS" -> CODECONNECTIONS
      | "SECRETS_MANAGER" -> SECRETS_MANAGER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SourceAuthType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SourceAuthType" j)
    let to_json = simple_to_json to_value
  end
module LogsConfigStatusType =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration LogsConfigStatusType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LogsConfigStatusType" j)
    let to_json = simple_to_json to_value
  end
module BucketOwnerAccess =
  struct
    type nonrec t =
      | NONE 
      | READ_ONLY 
      | FULL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | READ_ONLY -> "READ_ONLY"
      | FULL -> "FULL"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "READ_ONLY" -> READ_ONLY
      | "FULL" -> FULL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BucketOwnerAccess" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BucketOwnerAccess" j)
    let to_json = simple_to_json to_value
  end
module PhaseContexts =
  struct
    type nonrec t = PhaseContext.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PhaseContext.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PhaseContext.of_xml)
    let of_json j =
      list_of_json ~kind:"PhaseContexts" ~of_json:PhaseContext.of_json j
    let to_json v = composed_to_json to_value v
  end
module WrapperLong =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module WebhookFilter =
  struct
    type nonrec t =
      {
      type_: WebhookFilterType.t
        [@ocaml.doc
          "The type of webhook filter. There are 11 webhook filter types: EVENT, ACTOR_ACCOUNT_ID, HEAD_REF, BASE_REF, FILE_PATH, COMMIT_MESSAGE, TAG_NAME, RELEASE_NAME, REPOSITORY_NAME, ORGANIZATION_NAME, and WORKFLOW_NAME. EVENT A webhook event triggers a build when the provided pattern matches one of nine event types: PUSH, PULL_REQUEST_CREATED, PULL_REQUEST_UPDATED, PULL_REQUEST_CLOSED, PULL_REQUEST_REOPENED, PULL_REQUEST_MERGED, RELEASED, PRERELEASED, and WORKFLOW_JOB_QUEUED. The EVENT patterns are specified as a comma-separated string. For example, PUSH, PULL_REQUEST_CREATED, PULL_REQUEST_UPDATED filters all push, pull request created, and pull request updated events. Types PULL_REQUEST_REOPENED and WORKFLOW_JOB_QUEUED work with GitHub and GitHub Enterprise only. Types RELEASED and PRERELEASED work with GitHub only. ACTOR_ACCOUNT_ID A webhook event triggers a build when a GitHub, GitHub Enterprise, or Bitbucket account ID matches the regular expression pattern. HEAD_REF A webhook event triggers a build when the head reference matches the regular expression pattern. For example, refs/heads/branch-name and refs/tags/tag-name. Works with GitHub and GitHub Enterprise push, GitHub and GitHub Enterprise pull request, Bitbucket push, and Bitbucket pull request events. BASE_REF A webhook event triggers a build when the base reference matches the regular expression pattern. For example, refs/heads/branch-name. Works with pull request events only. FILE_PATH A webhook triggers a build when the path of a changed file matches the regular expression pattern. Works with push and pull request events only. COMMIT_MESSAGE A webhook triggers a build when the head commit message matches the regular expression pattern. Works with push and pull request events only. TAG_NAME A webhook triggers a build when the tag name of the release matches the regular expression pattern. Works with RELEASED and PRERELEASED events only. RELEASE_NAME A webhook triggers a build when the release name matches the regular expression pattern. Works with RELEASED and PRERELEASED events only. REPOSITORY_NAME A webhook triggers a build when the repository name matches the regular expression pattern. Works with GitHub global or organization webhooks only. ORGANIZATION_NAME A webhook triggers a build when the organization name matches the regular expression pattern. Works with GitHub global webhooks only. WORKFLOW_NAME A webhook triggers a build when the workflow name matches the regular expression pattern. Works with WORKFLOW_JOB_QUEUED events only. For CodeBuild-hosted Buildkite runner builds, WORKFLOW_NAME filters will filter by pipeline name."];
      pattern: String_.t
        [@ocaml.doc
          "For a WebHookFilter that uses EVENT type, a comma-separated string that specifies one or more events. For example, the webhook filter PUSH, PULL_REQUEST_CREATED, PULL_REQUEST_UPDATED allows all push, pull request created, and pull request updated events to trigger a build. For a WebHookFilter that uses any of the other filter types, a regular expression pattern. For example, a WebHookFilter that uses HEAD_REF for its type and the pattern ^refs/heads/ triggers a build when the head reference is a branch with a reference name refs/heads/branch-name."];
      excludeMatchedPattern: WrapperBoolean.t option
        [@ocaml.doc
          "Used to indicate that the pattern determines which webhook events do not trigger a build. If true, then a webhook event that does not match the pattern triggers a build. If false, then a webhook event that matches the pattern triggers a build."]}
    let context_ = "WebhookFilter"
    let make ?excludeMatchedPattern =
      fun ~type_ ->
        fun ~pattern -> fun () -> { excludeMatchedPattern; type_; pattern }
    let to_value x =
      structure_to_value
        [("type", (Some (WebhookFilterType.to_value x.type_)));
        ("pattern", (Some (String_.to_value x.pattern)));
        ("excludeMatchedPattern",
          (Option.map x.excludeMatchedPattern ~f:WrapperBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let excludeMatchedPattern =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "excludeMatchedPattern") in
      let pattern =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "pattern") in
      let type_ =
        WebhookFilterType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?excludeMatchedPattern ~pattern ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let excludeMatchedPattern =
        field_map json__ "excludeMatchedPattern" WrapperBoolean.of_json in
      let pattern = field_map_exn json__ "pattern" String_.of_json in
      let type_ = field_map_exn json__ "type" WebhookFilterType.of_json in
      make ?excludeMatchedPattern ~pattern ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A filter used to determine which webhooks trigger a build."]
module PullRequestBuildApproverRole =
  struct
    type nonrec t =
      | GITHUB_READ 
      | GITHUB_TRIAGE 
      | GITHUB_WRITE 
      | GITHUB_MAINTAIN 
      | GITHUB_ADMIN 
      | GITLAB_GUEST 
      | GITLAB_PLANNER 
      | GITLAB_REPORTER 
      | GITLAB_DEVELOPER 
      | GITLAB_MAINTAINER 
      | GITLAB_OWNER 
      | BITBUCKET_READ 
      | BITBUCKET_WRITE 
      | BITBUCKET_ADMIN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GITHUB_READ -> "GITHUB_READ"
      | GITHUB_TRIAGE -> "GITHUB_TRIAGE"
      | GITHUB_WRITE -> "GITHUB_WRITE"
      | GITHUB_MAINTAIN -> "GITHUB_MAINTAIN"
      | GITHUB_ADMIN -> "GITHUB_ADMIN"
      | GITLAB_GUEST -> "GITLAB_GUEST"
      | GITLAB_PLANNER -> "GITLAB_PLANNER"
      | GITLAB_REPORTER -> "GITLAB_REPORTER"
      | GITLAB_DEVELOPER -> "GITLAB_DEVELOPER"
      | GITLAB_MAINTAINER -> "GITLAB_MAINTAINER"
      | GITLAB_OWNER -> "GITLAB_OWNER"
      | BITBUCKET_READ -> "BITBUCKET_READ"
      | BITBUCKET_WRITE -> "BITBUCKET_WRITE"
      | BITBUCKET_ADMIN -> "BITBUCKET_ADMIN"
      | Non_static_id s -> s
    let of_string =
      function
      | "GITHUB_READ" -> GITHUB_READ
      | "GITHUB_TRIAGE" -> GITHUB_TRIAGE
      | "GITHUB_WRITE" -> GITHUB_WRITE
      | "GITHUB_MAINTAIN" -> GITHUB_MAINTAIN
      | "GITHUB_ADMIN" -> GITHUB_ADMIN
      | "GITLAB_GUEST" -> GITLAB_GUEST
      | "GITLAB_PLANNER" -> GITLAB_PLANNER
      | "GITLAB_REPORTER" -> GITLAB_REPORTER
      | "GITLAB_DEVELOPER" -> GITLAB_DEVELOPER
      | "GITLAB_MAINTAINER" -> GITLAB_MAINTAINER
      | "GITLAB_OWNER" -> GITLAB_OWNER
      | "BITBUCKET_READ" -> BITBUCKET_READ
      | "BITBUCKET_WRITE" -> BITBUCKET_WRITE
      | "BITBUCKET_ADMIN" -> BITBUCKET_ADMIN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PullRequestBuildApproverRole"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PullRequestBuildApproverRole" j)
    let to_json = simple_to_json to_value
  end
module FleetProxyRuleEffectType =
  struct
    type nonrec t =
      | ALLOW 
      | DENY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ALLOW -> "ALLOW" | DENY -> "DENY" | Non_static_id s -> s
    let of_string =
      function | "ALLOW" -> ALLOW | "DENY" -> DENY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FleetProxyRuleEffectType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FleetProxyRuleEffectType" j)
    let to_json = simple_to_json to_value
  end
module FleetProxyRuleEntities =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetProxyRuleEntities" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetProxyRuleType =
  struct
    type nonrec t =
      | DOMAIN 
      | IP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | DOMAIN -> "DOMAIN" | IP -> "IP" | Non_static_id s -> s
    let of_string =
      function | "DOMAIN" -> DOMAIN | "IP" -> IP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FleetProxyRuleType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetProxyRuleType" j)
    let to_json = simple_to_json to_value
  end
module FleetScalingMetricType =
  struct
    type nonrec t =
      | FLEET_UTILIZATION_RATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FLEET_UTILIZATION_RATE -> "FLEET_UTILIZATION_RATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "FLEET_UTILIZATION_RATE" -> FLEET_UTILIZATION_RATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FleetScalingMetricType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FleetScalingMetricType" j)
    let to_json = simple_to_json to_value
  end
module WrapperDouble =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module BuildSummary =
  struct
    type nonrec t =
      {
      arn: String_.t option [@ocaml.doc "The batch build ARN."];
      requestedOn: Timestamp.t option
        [@ocaml.doc
          "When the build was started, expressed in Unix time format."];
      buildStatus: StatusType.t option
        [@ocaml.doc
          "The status of the build group. FAILED The build group failed. FAULT The build group faulted. IN_PROGRESS The build group is still in progress. STOPPED The build group stopped. SUCCEEDED The build group succeeded. TIMED_OUT The build group timed out."];
      primaryArtifact: ResolvedArtifact.t option
        [@ocaml.doc
          "A ResolvedArtifact object that represents the primary build artifacts for the build group."];
      secondaryArtifacts: ResolvedSecondaryArtifacts.t option
        [@ocaml.doc
          "An array of ResolvedArtifact objects that represents the secondary build artifacts for the build group."]}
    let make ?arn =
      fun ?requestedOn ->
        fun ?buildStatus ->
          fun ?primaryArtifact ->
            fun ?secondaryArtifacts ->
              fun () ->
                {
                  arn;
                  requestedOn;
                  buildStatus;
                  primaryArtifact;
                  secondaryArtifacts
                }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:String_.to_value));
        ("requestedOn", (Option.map x.requestedOn ~f:Timestamp.to_value));
        ("buildStatus", (Option.map x.buildStatus ~f:StatusType.to_value));
        ("primaryArtifact",
          (Option.map x.primaryArtifact ~f:ResolvedArtifact.to_value));
        ("secondaryArtifacts",
          (Option.map x.secondaryArtifacts
             ~f:ResolvedSecondaryArtifacts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondaryArtifacts =
        (Option.map ~f:ResolvedSecondaryArtifacts.of_xml)
          (Xml.child xml_arg0 "secondaryArtifacts") in
      let primaryArtifact =
        (Option.map ~f:ResolvedArtifact.of_xml)
          (Xml.child xml_arg0 "primaryArtifact") in
      let buildStatus =
        (Option.map ~f:StatusType.of_xml) (Xml.child xml_arg0 "buildStatus") in
      let requestedOn =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "requestedOn") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      make ?secondaryArtifacts ?primaryArtifact ?buildStatus ?requestedOn
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secondaryArtifacts =
        field_map json__ "secondaryArtifacts"
          ResolvedSecondaryArtifacts.of_json in
      let primaryArtifact =
        field_map json__ "primaryArtifact" ResolvedArtifact.of_json in
      let buildStatus = field_map json__ "buildStatus" StatusType.of_json in
      let requestedOn = field_map json__ "requestedOn" Timestamp.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      make ?secondaryArtifacts ?primaryArtifact ?buildStatus ?requestedOn
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains summary information about a batch build group."]
module EnvironmentImages =
  struct
    type nonrec t = EnvironmentImage.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EnvironmentImage.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnvironmentImage.of_xml)
    let of_json j =
      list_of_json ~kind:"EnvironmentImages"
        ~of_json:EnvironmentImage.of_json j
    let to_json v = composed_to_json to_value v
  end
module LanguageType =
  struct
    type nonrec t =
      | JAVA 
      | PYTHON 
      | NODE_JS 
      | RUBY 
      | GOLANG 
      | DOCKER 
      | ANDROID 
      | DOTNET 
      | BASE 
      | PHP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | JAVA -> "JAVA"
      | PYTHON -> "PYTHON"
      | NODE_JS -> "NODE_JS"
      | RUBY -> "RUBY"
      | GOLANG -> "GOLANG"
      | DOCKER -> "DOCKER"
      | ANDROID -> "ANDROID"
      | DOTNET -> "DOTNET"
      | BASE -> "BASE"
      | PHP -> "PHP"
      | Non_static_id s -> s
    let of_string =
      function
      | "JAVA" -> JAVA
      | "PYTHON" -> PYTHON
      | "NODE_JS" -> NODE_JS
      | "RUBY" -> RUBY
      | "GOLANG" -> GOLANG
      | "DOCKER" -> DOCKER
      | "ANDROID" -> ANDROID
      | "DOTNET" -> DOTNET
      | "BASE" -> BASE
      | "PHP" -> PHP
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LanguageType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LanguageType" j)
    let to_json = simple_to_json to_value
  end
module MachineType =
  struct
    type nonrec t =
      | GENERAL 
      | NVME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | GENERAL -> "GENERAL" | NVME -> "NVME" | Non_static_id s -> s
    let of_string =
      function | "GENERAL" -> GENERAL | "NVME" -> NVME | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MachineType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MachineType" j)
    let to_json = simple_to_json to_value
  end
module ComputeType =
  struct
    type nonrec t =
      | BUILD_GENERAL1_SMALL 
      | BUILD_GENERAL1_MEDIUM 
      | BUILD_GENERAL1_LARGE 
      | BUILD_GENERAL1_XLARGE 
      | BUILD_GENERAL1_2XLARGE 
      | BUILD_LAMBDA_1GB 
      | BUILD_LAMBDA_2GB 
      | BUILD_LAMBDA_4GB 
      | BUILD_LAMBDA_8GB 
      | BUILD_LAMBDA_10GB 
      | ATTRIBUTE_BASED_COMPUTE 
      | CUSTOM_INSTANCE_TYPE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BUILD_GENERAL1_SMALL -> "BUILD_GENERAL1_SMALL"
      | BUILD_GENERAL1_MEDIUM -> "BUILD_GENERAL1_MEDIUM"
      | BUILD_GENERAL1_LARGE -> "BUILD_GENERAL1_LARGE"
      | BUILD_GENERAL1_XLARGE -> "BUILD_GENERAL1_XLARGE"
      | BUILD_GENERAL1_2XLARGE -> "BUILD_GENERAL1_2XLARGE"
      | BUILD_LAMBDA_1GB -> "BUILD_LAMBDA_1GB"
      | BUILD_LAMBDA_2GB -> "BUILD_LAMBDA_2GB"
      | BUILD_LAMBDA_4GB -> "BUILD_LAMBDA_4GB"
      | BUILD_LAMBDA_8GB -> "BUILD_LAMBDA_8GB"
      | BUILD_LAMBDA_10GB -> "BUILD_LAMBDA_10GB"
      | ATTRIBUTE_BASED_COMPUTE -> "ATTRIBUTE_BASED_COMPUTE"
      | CUSTOM_INSTANCE_TYPE -> "CUSTOM_INSTANCE_TYPE"
      | Non_static_id s -> s
    let of_string =
      function
      | "BUILD_GENERAL1_SMALL" -> BUILD_GENERAL1_SMALL
      | "BUILD_GENERAL1_MEDIUM" -> BUILD_GENERAL1_MEDIUM
      | "BUILD_GENERAL1_LARGE" -> BUILD_GENERAL1_LARGE
      | "BUILD_GENERAL1_XLARGE" -> BUILD_GENERAL1_XLARGE
      | "BUILD_GENERAL1_2XLARGE" -> BUILD_GENERAL1_2XLARGE
      | "BUILD_LAMBDA_1GB" -> BUILD_LAMBDA_1GB
      | "BUILD_LAMBDA_2GB" -> BUILD_LAMBDA_2GB
      | "BUILD_LAMBDA_4GB" -> BUILD_LAMBDA_4GB
      | "BUILD_LAMBDA_8GB" -> BUILD_LAMBDA_8GB
      | "BUILD_LAMBDA_10GB" -> BUILD_LAMBDA_10GB
      | "ATTRIBUTE_BASED_COMPUTE" -> ATTRIBUTE_BASED_COMPUTE
      | "CUSTOM_INSTANCE_TYPE" -> CUSTOM_INSTANCE_TYPE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ComputeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ComputeType" j)
    let to_json = simple_to_json to_value
  end
module DockerServerStatus =
  struct
    type nonrec t =
      {
      status: String_.t option
        [@ocaml.doc "The status of the docker server."];
      message: String_.t option
        [@ocaml.doc
          "A message associated with the status of a docker server."]}
    let make ?status = fun ?message -> fun () -> { status; message }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      make ?message ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let status = field_map json__ "status" String_.of_json in
      make ?message ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the status of the docker server."]
module SecurityGroupIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:5); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupIds" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module EnvironmentVariable =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t
        [@ocaml.doc "The name or key of the environment variable."];
      value: String_.t
        [@ocaml.doc
          "The value of the environment variable. We strongly discourage the use of PLAINTEXT environment variables to store sensitive values, especially Amazon Web Services secret key IDs. PLAINTEXT environment variables can be displayed in plain text using the CodeBuild console and the CLI. For sensitive values, we recommend you use an environment variable of type PARAMETER_STORE or SECRETS_MANAGER."];
      type_: EnvironmentVariableType.t option
        [@ocaml.doc
          "The type of environment variable. Valid values include: PARAMETER_STORE: An environment variable stored in Systems Manager Parameter Store. For environment variables of this type, specify the name of the parameter as the value of the EnvironmentVariable. The parameter value will be substituted for the name at runtime. You can also define Parameter Store environment variables in the buildspec. To learn how to do so, see env/parameter-store in the CodeBuild User Guide. PLAINTEXT: An environment variable in plain text format. This is the default value. SECRETS_MANAGER: An environment variable stored in Secrets Manager. For environment variables of this type, specify the name of the secret as the value of the EnvironmentVariable. The secret value will be substituted for the name at runtime. You can also define Secrets Manager environment variables in the buildspec. To learn how to do so, see env/secrets-manager in the CodeBuild User Guide."]}
    let context_ = "EnvironmentVariable"
    let make ?type_ =
      fun ~name -> fun ~value -> fun () -> { type_; name; value }
    let to_value x =
      structure_to_value
        [("name", (Some (NonEmptyString.to_value x.name)));
        ("value", (Some (String_.to_value x.value)));
        ("type", (Option.map x.type_ ~f:EnvironmentVariableType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:EnvironmentVariableType.of_xml)
          (Xml.child xml_arg0 "type") in
      let value =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let name =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?type_ ~value ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" EnvironmentVariableType.of_json in
      let value = field_map_exn json__ "value" String_.of_json in
      let name = field_map_exn json__ "name" NonEmptyString.of_json in
      make ?type_ ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an environment variable for a build project or a build."]
module CredentialProviderType =
  struct
    type nonrec t =
      | SECRETS_MANAGER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SECRETS_MANAGER -> "SECRETS_MANAGER" | Non_static_id s -> s
    let of_string =
      function | "SECRETS_MANAGER" -> SECRETS_MANAGER | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CredentialProviderType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"CredentialProviderType" j)
    let to_json = simple_to_json to_value
  end
module FileSystemType =
  struct
    type nonrec t =
      | EFS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | EFS -> "EFS" | Non_static_id s -> s
    let of_string = function | "EFS" -> EFS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FileSystemType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FileSystemType" j)
    let to_json = simple_to_json to_value
  end
module BuildStatusConfig =
  struct
    type nonrec t =
      {
      context: String_.t option
        [@ocaml.doc
          "Specifies the context of the build status CodeBuild sends to the source provider. The usage of this parameter depends on the source provider. Bitbucket This parameter is used for the name parameter in the Bitbucket commit status. For more information, see build in the Bitbucket API documentation. GitHub/GitHub Enterprise Server This parameter is used for the context parameter in the GitHub commit status. For more information, see Create a commit status in the GitHub developer guide."];
      targetUrl: String_.t option
        [@ocaml.doc
          "Specifies the target url of the build status CodeBuild sends to the source provider. The usage of this parameter depends on the source provider. Bitbucket This parameter is used for the url parameter in the Bitbucket commit status. For more information, see build in the Bitbucket API documentation. GitHub/GitHub Enterprise Server This parameter is used for the target_url parameter in the GitHub commit status. For more information, see Create a commit status in the GitHub developer guide."]}
    let make ?context = fun ?targetUrl -> fun () -> { context; targetUrl }
    let to_value x =
      structure_to_value
        [("context", (Option.map x.context ~f:String_.to_value));
        ("targetUrl", (Option.map x.targetUrl ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "targetUrl") in
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "context") in
      make ?targetUrl ?context ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetUrl = field_map json__ "targetUrl" String_.of_json in
      let context = field_map json__ "context" String_.of_json in
      make ?targetUrl ?context ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information that defines how the CodeBuild build project reports the build status to the source provider."]
module GitCloneDepth =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for GitCloneDepth" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GitSubmodulesConfig =
  struct
    type nonrec t =
      {
      fetchSubmodules: WrapperBoolean.t
        [@ocaml.doc
          "Set to true to fetch Git submodules for your CodeBuild build project."]}
    let context_ = "GitSubmodulesConfig"
    let make ~fetchSubmodules = fun () -> { fetchSubmodules }
    let to_value x =
      structure_to_value
        [("fetchSubmodules",
           (Some (WrapperBoolean.to_value x.fetchSubmodules)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fetchSubmodules =
        WrapperBoolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "fetchSubmodules") in
      make ~fetchSubmodules ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fetchSubmodules =
        field_map_exn json__ "fetchSubmodules" WrapperBoolean.of_json in
      make ~fetchSubmodules ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the Git submodules configuration for an CodeBuild build project."]
module SourceAuth =
  struct
    type nonrec t =
      {
      type_: SourceAuthType.t
        [@ocaml.doc
          "The authorization type to use. Valid options are OAUTH, CODECONNECTIONS, or SECRETS_MANAGER."];
      resource: String_.t option
        [@ocaml.doc
          "The resource value that applies to the specified authorization type."]}
    let context_ = "SourceAuth"
    let make ?resource = fun ~type_ -> fun () -> { resource; type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (SourceAuthType.to_value x.type_)));
        ("resource", (Option.map x.resource ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resource") in
      let type_ =
        SourceAuthType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?resource ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resource = field_map json__ "resource" String_.of_json in
      let type_ = field_map_exn json__ "type" SourceAuthType.of_json in
      make ?resource ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the authorization settings for CodeBuild to access the source code to be built."]
module SourceType =
  struct
    type nonrec t =
      | CODECOMMIT 
      | CODEPIPELINE 
      | GITHUB 
      | GITLAB 
      | GITLAB_SELF_MANAGED 
      | S3 
      | BITBUCKET 
      | GITHUB_ENTERPRISE 
      | NO_SOURCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CODECOMMIT -> "CODECOMMIT"
      | CODEPIPELINE -> "CODEPIPELINE"
      | GITHUB -> "GITHUB"
      | GITLAB -> "GITLAB"
      | GITLAB_SELF_MANAGED -> "GITLAB_SELF_MANAGED"
      | S3 -> "S3"
      | BITBUCKET -> "BITBUCKET"
      | GITHUB_ENTERPRISE -> "GITHUB_ENTERPRISE"
      | NO_SOURCE -> "NO_SOURCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CODECOMMIT" -> CODECOMMIT
      | "CODEPIPELINE" -> CODEPIPELINE
      | "GITHUB" -> GITHUB
      | "GITLAB" -> GITLAB
      | "GITLAB_SELF_MANAGED" -> GITLAB_SELF_MANAGED
      | "S3" -> S3
      | "BITBUCKET" -> BITBUCKET
      | "GITHUB_ENTERPRISE" -> GITHUB_ENTERPRISE
      | "NO_SOURCE" -> NO_SOURCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SourceType" j)
    let to_json = simple_to_json to_value
  end
module CloudWatchLogsConfig =
  struct
    type nonrec t =
      {
      status: LogsConfigStatusType.t
        [@ocaml.doc
          "The current status of the logs in CloudWatch Logs for a build project. Valid values are: ENABLED: CloudWatch Logs are enabled for this build project. DISABLED: CloudWatch Logs are not enabled for this build project."];
      groupName: String_.t option
        [@ocaml.doc
          "The group name of the logs in CloudWatch Logs. For more information, see Working with Log Groups and Log Streams."];
      streamName: String_.t option
        [@ocaml.doc
          "The prefix of the stream name of the CloudWatch Logs. For more information, see Working with Log Groups and Log Streams."]}
    let context_ = "CloudWatchLogsConfig"
    let make ?groupName =
      fun ?streamName ->
        fun ~status -> fun () -> { groupName; streamName; status }
    let to_value x =
      structure_to_value
        [("status", (Some (LogsConfigStatusType.to_value x.status)));
        ("groupName", (Option.map x.groupName ~f:String_.to_value));
        ("streamName", (Option.map x.streamName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "streamName") in
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupName") in
      let status =
        LogsConfigStatusType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      make ?streamName ?groupName ~status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamName = field_map json__ "streamName" String_.of_json in
      let groupName = field_map json__ "groupName" String_.of_json in
      let status = field_map_exn json__ "status" LogsConfigStatusType.of_json in
      make ?streamName ?groupName ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about CloudWatch Logs for a build project."]
module S3LogsConfig =
  struct
    type nonrec t =
      {
      status: LogsConfigStatusType.t
        [@ocaml.doc
          "The current status of the S3 build logs. Valid values are: ENABLED: S3 build logs are enabled for this build project. DISABLED: S3 build logs are not enabled for this build project."];
      location: String_.t option
        [@ocaml.doc
          "The ARN of an S3 bucket and the path prefix for S3 logs. If your Amazon S3 bucket name is my-bucket, and your path prefix is build-log, then acceptable formats are my-bucket/build-log or arn:aws:s3:::my-bucket/build-log."];
      encryptionDisabled: WrapperBoolean.t option
        [@ocaml.doc
          "Set to true if you do not want your S3 build log output encrypted. By default S3 build logs are encrypted."];
      bucketOwnerAccess: BucketOwnerAccess.t option }
    let context_ = "S3LogsConfig"
    let make ?location =
      fun ?encryptionDisabled ->
        fun ?bucketOwnerAccess ->
          fun ~status ->
            fun () ->
              { location; encryptionDisabled; bucketOwnerAccess; status }
    let to_value x =
      structure_to_value
        [("status", (Some (LogsConfigStatusType.to_value x.status)));
        ("location", (Option.map x.location ~f:String_.to_value));
        ("encryptionDisabled",
          (Option.map x.encryptionDisabled ~f:WrapperBoolean.to_value));
        ("bucketOwnerAccess",
          (Option.map x.bucketOwnerAccess ~f:BucketOwnerAccess.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucketOwnerAccess =
        (Option.map ~f:BucketOwnerAccess.of_xml)
          (Xml.child xml_arg0 "bucketOwnerAccess") in
      let encryptionDisabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "encryptionDisabled") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      let status =
        LogsConfigStatusType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      make ?bucketOwnerAccess ?encryptionDisabled ?location ~status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bucketOwnerAccess =
        field_map json__ "bucketOwnerAccess" BucketOwnerAccess.of_json in
      let encryptionDisabled =
        field_map json__ "encryptionDisabled" WrapperBoolean.of_json in
      let location = field_map json__ "location" String_.of_json in
      let status = field_map_exn json__ "status" LogsConfigStatusType.of_json in
      make ?bucketOwnerAccess ?encryptionDisabled ?location ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about S3 logs for a build project."]
module SandboxSessionPhase =
  struct
    type nonrec t =
      {
      phaseType: String_.t option
        [@ocaml.doc "The name of the sandbox phase."];
      phaseStatus: StatusType.t option
        [@ocaml.doc
          "The current status of the sandbox phase. Valid values include: FAILED The sandbox phase failed. FAULT The sandbox phase faulted. IN_PROGRESS The sandbox phase is still in progress. STOPPED The sandbox phase stopped. SUCCEEDED The sandbox phase succeeded. TIMED_OUT The sandbox phase timed out."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox phase started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox phase ended, expressed in Unix time format."];
      durationInSeconds: WrapperLong.t option
        [@ocaml.doc
          "How long, in seconds, between the starting and ending times of the sandbox's phase."];
      contexts: PhaseContexts.t option
        [@ocaml.doc "An array of PhaseContext objects."]}
    let make ?phaseType =
      fun ?phaseStatus ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?durationInSeconds ->
              fun ?contexts ->
                fun () ->
                  {
                    phaseType;
                    phaseStatus;
                    startTime;
                    endTime;
                    durationInSeconds;
                    contexts
                  }
    let to_value x =
      structure_to_value
        [("phaseType", (Option.map x.phaseType ~f:String_.to_value));
        ("phaseStatus", (Option.map x.phaseStatus ~f:StatusType.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("durationInSeconds",
          (Option.map x.durationInSeconds ~f:WrapperLong.to_value));
        ("contexts", (Option.map x.contexts ~f:PhaseContexts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contexts =
        (Option.map ~f:PhaseContexts.of_xml) (Xml.child xml_arg0 "contexts") in
      let durationInSeconds =
        (Option.map ~f:WrapperLong.of_xml)
          (Xml.child xml_arg0 "durationInSeconds") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let phaseStatus =
        (Option.map ~f:StatusType.of_xml) (Xml.child xml_arg0 "phaseStatus") in
      let phaseType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "phaseType") in
      make ?contexts ?durationInSeconds ?endTime ?startTime ?phaseStatus
        ?phaseType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contexts = field_map json__ "contexts" PhaseContexts.of_json in
      let durationInSeconds =
        field_map json__ "durationInSeconds" WrapperLong.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let phaseStatus = field_map json__ "phaseStatus" StatusType.of_json in
      let phaseType = field_map json__ "phaseType" String_.of_json in
      make ?contexts ?durationInSeconds ?endTime ?startTime ?phaseStatus
        ?phaseType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about the sandbox phase."]
module ReportPackagingType =
  struct
    type nonrec t =
      | ZIP 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ZIP -> "ZIP" | NONE -> "NONE" | Non_static_id s -> s
    let of_string =
      function | "ZIP" -> ZIP | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReportPackagingType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ReportPackagingType" j)
    let to_json = simple_to_json to_value
  end
module WrapperInt =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for WrapperInt" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module KeyInput =
  struct
    type nonrec t = string
    let context_ = "KeyInput"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:127) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=@+\\-]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KeyInput" j
    let to_json = simple_to_json to_value
  end
module ValueInput =
  struct
    type nonrec t = string
    let context_ = "ValueInput"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=@+\\-]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ValueInput" j
    let to_json = simple_to_json to_value
  end
module ArtifactNamespace =
  struct
    type nonrec t =
      | NONE 
      | BUILD_ID 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | BUILD_ID -> "BUILD_ID"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "BUILD_ID" -> BUILD_ID
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ArtifactNamespace" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ArtifactNamespace" j)
    let to_json = simple_to_json to_value
  end
module ArtifactPackaging =
  struct
    type nonrec t =
      | NONE 
      | ZIP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | NONE -> "NONE" | ZIP -> "ZIP" | Non_static_id s -> s
    let of_string =
      function | "NONE" -> NONE | "ZIP" -> ZIP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ArtifactPackaging" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ArtifactPackaging" j)
    let to_json = simple_to_json to_value
  end
module ComputeTypesAllowed =
  struct
    type nonrec t = NonEmptyString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"ComputeTypesAllowed"
        ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetsAllowed =
  struct
    type nonrec t = NonEmptyString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetsAllowed" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module CacheMode =
  struct
    type nonrec t =
      | LOCAL_DOCKER_LAYER_CACHE 
      | LOCAL_SOURCE_CACHE 
      | LOCAL_CUSTOM_CACHE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LOCAL_DOCKER_LAYER_CACHE -> "LOCAL_DOCKER_LAYER_CACHE"
      | LOCAL_SOURCE_CACHE -> "LOCAL_SOURCE_CACHE"
      | LOCAL_CUSTOM_CACHE -> "LOCAL_CUSTOM_CACHE"
      | Non_static_id s -> s
    let of_string =
      function
      | "LOCAL_DOCKER_LAYER_CACHE" -> LOCAL_DOCKER_LAYER_CACHE
      | "LOCAL_SOURCE_CACHE" -> LOCAL_SOURCE_CACHE
      | "LOCAL_CUSTOM_CACHE" -> LOCAL_CUSTOM_CACHE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CacheMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CacheMode" j)
    let to_json = simple_to_json to_value
  end
module FilterGroup =
  struct
    type nonrec t = WebhookFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:WebhookFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WebhookFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"FilterGroup" ~of_json:WebhookFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module PullRequestBuildApproverRoles =
  struct
    type nonrec t = PullRequestBuildApproverRole.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PullRequestBuildApproverRole.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PullRequestBuildApproverRole.of_xml)
    let of_json j =
      list_of_json ~kind:"PullRequestBuildApproverRoles"
        ~of_json:PullRequestBuildApproverRole.of_json j
    let to_json v = composed_to_json to_value v
  end
module PullRequestBuildCommentApproval =
  struct
    type nonrec t =
      | DISABLED 
      | ALL_PULL_REQUESTS 
      | FORK_PULL_REQUESTS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | ALL_PULL_REQUESTS -> "ALL_PULL_REQUESTS"
      | FORK_PULL_REQUESTS -> "FORK_PULL_REQUESTS"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "ALL_PULL_REQUESTS" -> ALL_PULL_REQUESTS
      | "FORK_PULL_REQUESTS" -> FORK_PULL_REQUESTS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PullRequestBuildCommentApproval"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PullRequestBuildCommentApproval" j)
    let to_json = simple_to_json to_value
  end
module WebhookScopeType =
  struct
    type nonrec t =
      | GITHUB_ORGANIZATION 
      | GITHUB_GLOBAL 
      | GITLAB_GROUP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GITHUB_ORGANIZATION -> "GITHUB_ORGANIZATION"
      | GITHUB_GLOBAL -> "GITHUB_GLOBAL"
      | GITLAB_GROUP -> "GITLAB_GROUP"
      | Non_static_id s -> s
    let of_string =
      function
      | "GITHUB_ORGANIZATION" -> GITHUB_ORGANIZATION
      | "GITHUB_GLOBAL" -> GITHUB_GLOBAL
      | "GITLAB_GROUP" -> GITLAB_GROUP
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration WebhookScopeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WebhookScopeType" j)
    let to_json = simple_to_json to_value
  end
module FleetProxyRule =
  struct
    type nonrec t =
      {
      type_: FleetProxyRuleType.t [@ocaml.doc "The type of proxy rule."];
      effect_: FleetProxyRuleEffectType.t
        [@ocaml.doc "The behavior of the proxy rule."];
      entities: FleetProxyRuleEntities.t
        [@ocaml.doc "The destination of the proxy rule."]}
    let context_ = "FleetProxyRule"
    let make ~type_ =
      fun ~effect_ -> fun ~entities -> fun () -> { type_; effect_; entities }
    let to_value x =
      structure_to_value
        [("type", (Some (FleetProxyRuleType.to_value x.type_)));
        ("effect", (Some (FleetProxyRuleEffectType.to_value x.effect_)));
        ("entities", (Some (FleetProxyRuleEntities.to_value x.entities)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let entities =
        FleetProxyRuleEntities.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "entities") in
      let effect_ =
        FleetProxyRuleEffectType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "effect") in
      let type_ =
        FleetProxyRuleType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ~entities ~effect_ ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let entities =
        field_map_exn json__ "entities" FleetProxyRuleEntities.of_json in
      let effect_ =
        field_map_exn json__ "effect" FleetProxyRuleEffectType.of_json in
      let type_ = field_map_exn json__ "type" FleetProxyRuleType.of_json in
      make ~entities ~effect_ ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the proxy rule for your reserved capacity instances."]
module TargetTrackingScalingConfiguration =
  struct
    type nonrec t =
      {
      metricType: FleetScalingMetricType.t option
        [@ocaml.doc "The metric type to determine auto-scaling."];
      targetValue: WrapperDouble.t option
        [@ocaml.doc "The value of metricType when to start scaling."]}
    let make ?metricType =
      fun ?targetValue -> fun () -> { metricType; targetValue }
    let to_value x =
      structure_to_value
        [("metricType",
           (Option.map x.metricType ~f:FleetScalingMetricType.to_value));
        ("targetValue", (Option.map x.targetValue ~f:WrapperDouble.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetValue =
        (Option.map ~f:WrapperDouble.of_xml)
          (Xml.child xml_arg0 "targetValue") in
      let metricType =
        (Option.map ~f:FleetScalingMetricType.of_xml)
          (Xml.child xml_arg0 "metricType") in
      make ?targetValue ?metricType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetValue = field_map json__ "targetValue" WrapperDouble.of_json in
      let metricType =
        field_map json__ "metricType" FleetScalingMetricType.of_json in
      make ?targetValue ?metricType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines when a new instance is auto-scaled into the compute fleet."]
module BuildPhaseType =
  struct
    type nonrec t =
      | SUBMITTED 
      | QUEUED 
      | PROVISIONING 
      | DOWNLOAD_SOURCE 
      | INSTALL 
      | PRE_BUILD 
      | BUILD 
      | POST_BUILD 
      | UPLOAD_ARTIFACTS 
      | FINALIZING 
      | COMPLETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUBMITTED -> "SUBMITTED"
      | QUEUED -> "QUEUED"
      | PROVISIONING -> "PROVISIONING"
      | DOWNLOAD_SOURCE -> "DOWNLOAD_SOURCE"
      | INSTALL -> "INSTALL"
      | PRE_BUILD -> "PRE_BUILD"
      | BUILD -> "BUILD"
      | POST_BUILD -> "POST_BUILD"
      | UPLOAD_ARTIFACTS -> "UPLOAD_ARTIFACTS"
      | FINALIZING -> "FINALIZING"
      | COMPLETED -> "COMPLETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUBMITTED" -> SUBMITTED
      | "QUEUED" -> QUEUED
      | "PROVISIONING" -> PROVISIONING
      | "DOWNLOAD_SOURCE" -> DOWNLOAD_SOURCE
      | "INSTALL" -> INSTALL
      | "PRE_BUILD" -> PRE_BUILD
      | "BUILD" -> BUILD
      | "POST_BUILD" -> POST_BUILD
      | "UPLOAD_ARTIFACTS" -> UPLOAD_ARTIFACTS
      | "FINALIZING" -> FINALIZING
      | "COMPLETED" -> COMPLETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BuildPhaseType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BuildPhaseType" j)
    let to_json = simple_to_json to_value
  end
module BuildBatchPhaseType =
  struct
    type nonrec t =
      | SUBMITTED 
      | DOWNLOAD_BATCHSPEC 
      | IN_PROGRESS 
      | COMBINE_ARTIFACTS 
      | SUCCEEDED 
      | FAILED 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUBMITTED -> "SUBMITTED"
      | DOWNLOAD_BATCHSPEC -> "DOWNLOAD_BATCHSPEC"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMBINE_ARTIFACTS -> "COMBINE_ARTIFACTS"
      | SUCCEEDED -> "SUCCEEDED"
      | FAILED -> "FAILED"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUBMITTED" -> SUBMITTED
      | "DOWNLOAD_BATCHSPEC" -> DOWNLOAD_BATCHSPEC
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMBINE_ARTIFACTS" -> COMBINE_ARTIFACTS
      | "SUCCEEDED" -> SUCCEEDED
      | "FAILED" -> FAILED
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BuildBatchPhaseType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BuildBatchPhaseType" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module BuildSummaries =
  struct
    type nonrec t = BuildSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildSummaries" ~of_json:BuildSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Identifiers =
  struct
    type nonrec t = NonEmptyString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"Identifiers" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module EnvironmentLanguage =
  struct
    type nonrec t =
      {
      language: LanguageType.t option
        [@ocaml.doc "The programming language for the Docker images."];
      images: EnvironmentImages.t option
        [@ocaml.doc
          "The list of Docker images that are related by the specified programming language."]}
    let make ?language = fun ?images -> fun () -> { language; images }
    let to_value x =
      structure_to_value
        [("language", (Option.map x.language ~f:LanguageType.to_value));
        ("images", (Option.map x.images ~f:EnvironmentImages.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let images =
        (Option.map ~f:EnvironmentImages.of_xml)
          (Xml.child xml_arg0 "images") in
      let language =
        (Option.map ~f:LanguageType.of_xml) (Xml.child xml_arg0 "language") in
      make ?images ?language ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let images = field_map json__ "images" EnvironmentImages.of_json in
      let language = field_map json__ "language" LanguageType.of_json in
      make ?images ?language ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of Docker images that are related by programming language and are managed by CodeBuild."]
module ComputeConfiguration =
  struct
    type nonrec t =
      {
      vCpu: WrapperLong.t option
        [@ocaml.doc
          "The number of vCPUs of the instance type included in your fleet."];
      memory: WrapperLong.t option
        [@ocaml.doc
          "The amount of memory of the instance type included in your fleet."];
      disk: WrapperLong.t option
        [@ocaml.doc
          "The amount of disk space of the instance type included in your fleet."];
      machineType: MachineType.t option
        [@ocaml.doc
          "The machine type of the instance type included in your fleet."];
      instanceType: NonEmptyString.t option
        [@ocaml.doc "The EC2 instance type to be launched in your fleet."]}
    let make ?vCpu =
      fun ?memory ->
        fun ?disk ->
          fun ?machineType ->
            fun ?instanceType ->
              fun () -> { vCpu; memory; disk; machineType; instanceType }
    let to_value x =
      structure_to_value
        [("vCpu", (Option.map x.vCpu ~f:WrapperLong.to_value));
        ("memory", (Option.map x.memory ~f:WrapperLong.to_value));
        ("disk", (Option.map x.disk ~f:WrapperLong.to_value));
        ("machineType", (Option.map x.machineType ~f:MachineType.to_value));
        ("instanceType",
          (Option.map x.instanceType ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceType =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "instanceType") in
      let machineType =
        (Option.map ~f:MachineType.of_xml) (Xml.child xml_arg0 "machineType") in
      let disk =
        (Option.map ~f:WrapperLong.of_xml) (Xml.child xml_arg0 "disk") in
      let memory =
        (Option.map ~f:WrapperLong.of_xml) (Xml.child xml_arg0 "memory") in
      let vCpu =
        (Option.map ~f:WrapperLong.of_xml) (Xml.child xml_arg0 "vCpu") in
      make ?instanceType ?machineType ?disk ?memory ?vCpu ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceType =
        field_map json__ "instanceType" NonEmptyString.of_json in
      let machineType = field_map json__ "machineType" MachineType.of_json in
      let disk = field_map json__ "disk" WrapperLong.of_json in
      let memory = field_map json__ "memory" WrapperLong.of_json in
      let vCpu = field_map json__ "vCpu" WrapperLong.of_json in
      make ?instanceType ?machineType ?disk ?memory ?vCpu ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains compute attributes. These attributes only need be specified when your project's or fleet's computeType is set to ATTRIBUTE_BASED_COMPUTE or CUSTOM_INSTANCE_TYPE."]
module DockerServer =
  struct
    type nonrec t =
      {
      computeType: ComputeType.t
        [@ocaml.doc
          "Information about the compute resources the docker server uses. Available values include: BUILD_GENERAL1_SMALL: Use up to 4 GiB memory and 2 vCPUs for your docker server. BUILD_GENERAL1_MEDIUM: Use up to 8 GiB memory and 4 vCPUs for your docker server. BUILD_GENERAL1_LARGE: Use up to 16 GiB memory and 8 vCPUs for your docker server. BUILD_GENERAL1_XLARGE: Use up to 64 GiB memory and 32 vCPUs for your docker server. BUILD_GENERAL1_2XLARGE: Use up to 128 GiB memory and 64 vCPUs for your docker server."];
      securityGroupIds: SecurityGroupIds.t option
        [@ocaml.doc
          "A list of one or more security groups IDs. Security groups configured for Docker servers should allow ingress network traffic from the VPC configured in the project. They should allow ingress on port 9876."];
      status: DockerServerStatus.t option
        [@ocaml.doc
          "A DockerServerStatus object to use for this docker server."]}
    let context_ = "DockerServer"
    let make ?securityGroupIds =
      fun ?status ->
        fun ~computeType ->
          fun () -> { securityGroupIds; status; computeType }
    let to_value x =
      structure_to_value
        [("computeType", (Some (ComputeType.to_value x.computeType)));
        ("securityGroupIds",
          (Option.map x.securityGroupIds ~f:SecurityGroupIds.to_value));
        ("status", (Option.map x.status ~f:DockerServerStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:DockerServerStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIds.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      let computeType =
        ComputeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "computeType") in
      make ?status ?securityGroupIds ~computeType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" DockerServerStatus.of_json in
      let securityGroupIds =
        field_map json__ "securityGroupIds" SecurityGroupIds.of_json in
      let computeType =
        field_map_exn json__ "computeType" ComputeType.of_json in
      make ?status ?securityGroupIds ~computeType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains docker server information."]
module EnvironmentType =
  struct
    type nonrec t =
      | WINDOWS_CONTAINER 
      | LINUX_CONTAINER 
      | LINUX_GPU_CONTAINER 
      | ARM_CONTAINER 
      | WINDOWS_SERVER_2019_CONTAINER 
      | WINDOWS_SERVER_2022_CONTAINER 
      | LINUX_LAMBDA_CONTAINER 
      | ARM_LAMBDA_CONTAINER 
      | LINUX_EC2 
      | ARM_EC2 
      | WINDOWS_EC2 
      | MAC_ARM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WINDOWS_CONTAINER -> "WINDOWS_CONTAINER"
      | LINUX_CONTAINER -> "LINUX_CONTAINER"
      | LINUX_GPU_CONTAINER -> "LINUX_GPU_CONTAINER"
      | ARM_CONTAINER -> "ARM_CONTAINER"
      | WINDOWS_SERVER_2019_CONTAINER -> "WINDOWS_SERVER_2019_CONTAINER"
      | WINDOWS_SERVER_2022_CONTAINER -> "WINDOWS_SERVER_2022_CONTAINER"
      | LINUX_LAMBDA_CONTAINER -> "LINUX_LAMBDA_CONTAINER"
      | ARM_LAMBDA_CONTAINER -> "ARM_LAMBDA_CONTAINER"
      | LINUX_EC2 -> "LINUX_EC2"
      | ARM_EC2 -> "ARM_EC2"
      | WINDOWS_EC2 -> "WINDOWS_EC2"
      | MAC_ARM -> "MAC_ARM"
      | Non_static_id s -> s
    let of_string =
      function
      | "WINDOWS_CONTAINER" -> WINDOWS_CONTAINER
      | "LINUX_CONTAINER" -> LINUX_CONTAINER
      | "LINUX_GPU_CONTAINER" -> LINUX_GPU_CONTAINER
      | "ARM_CONTAINER" -> ARM_CONTAINER
      | "WINDOWS_SERVER_2019_CONTAINER" -> WINDOWS_SERVER_2019_CONTAINER
      | "WINDOWS_SERVER_2022_CONTAINER" -> WINDOWS_SERVER_2022_CONTAINER
      | "LINUX_LAMBDA_CONTAINER" -> LINUX_LAMBDA_CONTAINER
      | "ARM_LAMBDA_CONTAINER" -> ARM_LAMBDA_CONTAINER
      | "LINUX_EC2" -> LINUX_EC2
      | "ARM_EC2" -> ARM_EC2
      | "WINDOWS_EC2" -> WINDOWS_EC2
      | "MAC_ARM" -> MAC_ARM
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EnvironmentType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EnvironmentType" j)
    let to_json = simple_to_json to_value
  end
module EnvironmentVariables =
  struct
    type nonrec t = EnvironmentVariable.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EnvironmentVariable.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnvironmentVariable.of_xml)
    let of_json j =
      list_of_json ~kind:"EnvironmentVariables"
        ~of_json:EnvironmentVariable.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImagePullCredentialsType =
  struct
    type nonrec t =
      | CODEBUILD 
      | SERVICE_ROLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CODEBUILD -> "CODEBUILD"
      | SERVICE_ROLE -> "SERVICE_ROLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CODEBUILD" -> CODEBUILD
      | "SERVICE_ROLE" -> SERVICE_ROLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ImagePullCredentialsType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ImagePullCredentialsType" j)
    let to_json = simple_to_json to_value
  end
module ProjectFleet =
  struct
    type nonrec t =
      {
      fleetArn: String_.t option
        [@ocaml.doc "Specifies the compute fleet ARN for the build project."]}
    let make ?fleetArn = fun () -> { fleetArn }
    let to_value x =
      structure_to_value
        [("fleetArn", (Option.map x.fleetArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fleetArn") in
      make ?fleetArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetArn = field_map json__ "fleetArn" String_.of_json in
      make ?fleetArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the compute fleet of the build project. For more information, see Working with reserved capacity in CodeBuild."]
module RegistryCredential =
  struct
    type nonrec t =
      {
      credential: NonEmptyString.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) or name of credentials created using Secrets Manager. The credential can use the name of the credentials only if they exist in your current Amazon Web Services Region."];
      credentialProvider: CredentialProviderType.t
        [@ocaml.doc
          "The service that created the credentials to access a private Docker registry. The valid value, SECRETS_MANAGER, is for Secrets Manager."]}
    let context_ = "RegistryCredential"
    let make ~credential =
      fun ~credentialProvider -> fun () -> { credential; credentialProvider }
    let to_value x =
      structure_to_value
        [("credential", (Some (NonEmptyString.to_value x.credential)));
        ("credentialProvider",
          (Some (CredentialProviderType.to_value x.credentialProvider)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let credentialProvider =
        CredentialProviderType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "credentialProvider") in
      let credential =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "credential") in
      make ~credentialProvider ~credential ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let credentialProvider =
        field_map_exn json__ "credentialProvider"
          CredentialProviderType.of_json in
      let credential =
        field_map_exn json__ "credential" NonEmptyString.of_json in
      make ~credentialProvider ~credential ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about credentials that provide access to a private Docker registry. When this is set: imagePullCredentialsType must be set to SERVICE_ROLE. images cannot be curated or an Amazon ECR image. For more information, see Private Registry with Secrets Manager Sample for CodeBuild."]
module ProjectFileSystemLocation =
  struct
    type nonrec t =
      {
      type_: FileSystemType.t option
        [@ocaml.doc
          "The type of the file system. The one supported type is EFS."];
      location: String_.t option
        [@ocaml.doc
          "A string that specifies the location of the file system created by Amazon EFS. Its format is efs-dns-name:/directory-path. You can find the DNS name of file system when you view it in the Amazon EFS console. The directory path is a path to a directory in the file system that CodeBuild mounts. For example, if the DNS name of a file system is fs-abcd1234.efs.us-west-2.amazonaws.com, and its mount directory is my-efs-mount-directory, then the location is fs-abcd1234.efs.us-west-2.amazonaws.com:/my-efs-mount-directory. The directory path in the format efs-dns-name:/directory-path is optional. If you do not specify a directory path, the location is only the DNS name and CodeBuild mounts the entire file system."];
      mountPoint: String_.t option
        [@ocaml.doc
          "The location in the container where you mount the file system."];
      identifier: String_.t option
        [@ocaml.doc
          "The name used to access a file system created by Amazon EFS. CodeBuild creates an environment variable by appending the identifier in all capital letters to CODEBUILD_. For example, if you specify my_efs for identifier, a new environment variable is create named CODEBUILD_MY_EFS. The identifier is used to mount your file system."];
      mountOptions: String_.t option
        [@ocaml.doc
          "The mount options for a file system created by Amazon EFS. The default mount options used by CodeBuild are nfsvers=4.1,rsize=1048576,wsize=1048576,hard,timeo=600,retrans=2. For more information, see Recommended NFS Mount Options."]}
    let make ?type_ =
      fun ?location ->
        fun ?mountPoint ->
          fun ?identifier ->
            fun ?mountOptions ->
              fun () ->
                { type_; location; mountPoint; identifier; mountOptions }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:FileSystemType.to_value));
        ("location", (Option.map x.location ~f:String_.to_value));
        ("mountPoint", (Option.map x.mountPoint ~f:String_.to_value));
        ("identifier", (Option.map x.identifier ~f:String_.to_value));
        ("mountOptions", (Option.map x.mountOptions ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mountOptions =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "mountOptions") in
      let identifier =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "identifier") in
      let mountPoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "mountPoint") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      let type_ =
        (Option.map ~f:FileSystemType.of_xml) (Xml.child xml_arg0 "type") in
      make ?mountOptions ?identifier ?mountPoint ?location ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mountOptions = field_map json__ "mountOptions" String_.of_json in
      let identifier = field_map json__ "identifier" String_.of_json in
      let mountPoint = field_map json__ "mountPoint" String_.of_json in
      let location = field_map json__ "location" String_.of_json in
      let type_ = field_map json__ "type" FileSystemType.of_json in
      make ?mountOptions ?identifier ?mountPoint ?location ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a file system created by Amazon Elastic File System (EFS). For more information, see What Is Amazon Elastic File System?"]
module ProjectSourceVersion =
  struct
    type nonrec t =
      {
      sourceIdentifier: String_.t
        [@ocaml.doc
          "An identifier for a source in the build project. The identifier can only contain alphanumeric characters and underscores, and must be less than 128 characters in length."];
      sourceVersion: String_.t
        [@ocaml.doc
          "The source version for the corresponding source identifier. If specified, must be one of: For CodeCommit: the commit ID, branch, or Git tag to use. For GitHub: the commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example, pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For GitLab: the commit ID, branch, or Git tag to use. For Bitbucket: the commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Amazon S3: the version ID of the object that represents the build input ZIP file to use. For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."]}
    let context_ = "ProjectSourceVersion"
    let make ~sourceIdentifier =
      fun ~sourceVersion -> fun () -> { sourceIdentifier; sourceVersion }
    let to_value x =
      structure_to_value
        [("sourceIdentifier", (Some (String_.to_value x.sourceIdentifier)));
        ("sourceVersion", (Some (String_.to_value x.sourceVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceVersion =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sourceVersion") in
      let sourceIdentifier =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sourceIdentifier") in
      make ~sourceVersion ~sourceIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceVersion =
        field_map_exn json__ "sourceVersion" String_.of_json in
      let sourceIdentifier =
        field_map_exn json__ "sourceIdentifier" String_.of_json in
      make ~sourceVersion ~sourceIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A source identifier and its corresponding version."]
module ProjectSource =
  struct
    type nonrec t =
      {
      type_: SourceType.t
        [@ocaml.doc
          "The type of repository that contains the source code to be built. Valid values include: BITBUCKET: The source code is in a Bitbucket repository. CODECOMMIT: The source code is in an CodeCommit repository. CODEPIPELINE: The source code settings are specified in the source action of a pipeline in CodePipeline. GITHUB: The source code is in a GitHub repository. GITHUB_ENTERPRISE: The source code is in a GitHub Enterprise Server repository. GITLAB: The source code is in a GitLab repository. GITLAB_SELF_MANAGED: The source code is in a self-managed GitLab repository. NO_SOURCE: The project does not have input source code. S3: The source code is in an Amazon S3 bucket."];
      location: String_.t option
        [@ocaml.doc
          "Information about the location of the source code to be built. Valid values include: For source code settings that are specified in the source action of a pipeline in CodePipeline, location should not be specified. If it is specified, CodePipeline ignores it. This is because CodePipeline uses the settings in a pipeline's source action instead of this value. For source code in an CodeCommit repository, the HTTPS clone URL to the repository that contains the source code and the buildspec file (for example, https://git-codecommit.<region-ID>.amazonaws.com/v1/repos/<repo-name>). For source code in an Amazon S3 input bucket, one of the following. The path to the ZIP file that contains the source code (for example, <bucket-name>/<path>/<object-name>.zip). The path to the folder that contains the source code (for example, <bucket-name>/<path-to-source-code>/<folder>/). For source code in a GitHub repository, the HTTPS clone URL to the repository that contains the source and the buildspec file. You must connect your Amazon Web Services account to your GitHub account. Use the CodeBuild console to start creating a build project. When you use the console to connect (or reconnect) with GitHub, on the GitHub Authorize application page, for Organization access, choose Request access next to each repository you want to allow CodeBuild to have access to, and then choose Authorize application. (After you have connected to your GitHub account, you do not need to finish creating the build project. You can leave the CodeBuild console.) To instruct CodeBuild to use this connection, in the source object, set the auth object's type value to OAUTH. For source code in an GitLab or self-managed GitLab repository, the HTTPS clone URL to the repository that contains the source and the buildspec file. You must connect your Amazon Web Services account to your GitLab account. Use the CodeBuild console to start creating a build project. When you use the console to connect (or reconnect) with GitLab, on the Connections Authorize application page, choose Authorize. Then on the CodeConnections Create GitLab connection page, choose Connect to GitLab. (After you have connected to your GitLab account, you do not need to finish creating the build project. You can leave the CodeBuild console.) To instruct CodeBuild to override the default connection and use this connection instead, set the auth object's type value to CODECONNECTIONS in the source object. For source code in a Bitbucket repository, the HTTPS clone URL to the repository that contains the source and the buildspec file. You must connect your Amazon Web Services account to your Bitbucket account. Use the CodeBuild console to start creating a build project. When you use the console to connect (or reconnect) with Bitbucket, on the Bitbucket Confirm access to your account page, choose Grant access. (After you have connected to your Bitbucket account, you do not need to finish creating the build project. You can leave the CodeBuild console.) To instruct CodeBuild to use this connection, in the source object, set the auth object's type value to OAUTH. If you specify CODEPIPELINE for the Type property, don't specify this property. For all of the other types, you must specify Location."];
      gitCloneDepth: GitCloneDepth.t option
        [@ocaml.doc
          "Information about the Git clone depth for the build project."];
      gitSubmodulesConfig: GitSubmodulesConfig.t option
        [@ocaml.doc
          "Information about the Git submodules configuration for the build project."];
      buildspec: String_.t option
        [@ocaml.doc
          "The buildspec file declaration to use for the builds in this build project. If this value is set, it can be either an inline buildspec definition, the path to an alternate buildspec file relative to the value of the built-in CODEBUILD_SRC_DIR environment variable, or the path to an S3 bucket. The bucket must be in the same Amazon Web Services Region as the build project. Specify the buildspec file using its ARN (for example, arn:aws:s3:::my-codebuild-sample2/buildspec.yml). If this value is not provided or is set to an empty string, the source code must contain a buildspec file in its root directory. For more information, see Buildspec File Name and Storage Location."];
      auth: SourceAuth.t option
        [@ocaml.doc
          "Information about the authorization settings for CodeBuild to access the source code to be built."];
      reportBuildStatus: WrapperBoolean.t option
        [@ocaml.doc
          "Set to true to report the status of a build's start and finish to your source provider. This option is valid only when your source provider is GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, GitLab, GitLab Self Managed, or Bitbucket. If this is set and you use a different source provider, an invalidInputException is thrown. To be able to report the build status to the source provider, the user associated with the source provider must have write access to the repo. If the user does not have write access, the build status cannot be updated. For more information, see Source provider access in the CodeBuild User Guide. The status of a build triggered by a webhook is always reported to your source provider. If your project's builds are triggered by a webhook, you must push a new commit to the repo for a change to this property to take effect."];
      buildStatusConfig: BuildStatusConfig.t option
        [@ocaml.doc
          "Contains information that defines how the build project reports the build status to the source provider. This option is only used when the source provider is GITHUB, GITHUB_ENTERPRISE, or BITBUCKET."];
      insecureSsl: WrapperBoolean.t option
        [@ocaml.doc
          "Enable this flag to ignore SSL warnings while connecting to the project source code."];
      sourceIdentifier: String_.t option
        [@ocaml.doc
          "An identifier for this project source. The identifier can only contain alphanumeric characters and underscores, and must be less than 128 characters in length."]}
    let context_ = "ProjectSource"
    let make ?location =
      fun ?gitCloneDepth ->
        fun ?gitSubmodulesConfig ->
          fun ?buildspec ->
            fun ?auth ->
              fun ?reportBuildStatus ->
                fun ?buildStatusConfig ->
                  fun ?insecureSsl ->
                    fun ?sourceIdentifier ->
                      fun ~type_ ->
                        fun () ->
                          {
                            location;
                            gitCloneDepth;
                            gitSubmodulesConfig;
                            buildspec;
                            auth;
                            reportBuildStatus;
                            buildStatusConfig;
                            insecureSsl;
                            sourceIdentifier;
                            type_
                          }
    let to_value x =
      structure_to_value
        [("type", (Some (SourceType.to_value x.type_)));
        ("location", (Option.map x.location ~f:String_.to_value));
        ("gitCloneDepth",
          (Option.map x.gitCloneDepth ~f:GitCloneDepth.to_value));
        ("gitSubmodulesConfig",
          (Option.map x.gitSubmodulesConfig ~f:GitSubmodulesConfig.to_value));
        ("buildspec", (Option.map x.buildspec ~f:String_.to_value));
        ("auth", (Option.map x.auth ~f:SourceAuth.to_value));
        ("reportBuildStatus",
          (Option.map x.reportBuildStatus ~f:WrapperBoolean.to_value));
        ("buildStatusConfig",
          (Option.map x.buildStatusConfig ~f:BuildStatusConfig.to_value));
        ("insecureSsl",
          (Option.map x.insecureSsl ~f:WrapperBoolean.to_value));
        ("sourceIdentifier",
          (Option.map x.sourceIdentifier ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceIdentifier =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceIdentifier") in
      let insecureSsl =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "insecureSsl") in
      let buildStatusConfig =
        (Option.map ~f:BuildStatusConfig.of_xml)
          (Xml.child xml_arg0 "buildStatusConfig") in
      let reportBuildStatus =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "reportBuildStatus") in
      let auth =
        (Option.map ~f:SourceAuth.of_xml) (Xml.child xml_arg0 "auth") in
      let buildspec =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "buildspec") in
      let gitSubmodulesConfig =
        (Option.map ~f:GitSubmodulesConfig.of_xml)
          (Xml.child xml_arg0 "gitSubmodulesConfig") in
      let gitCloneDepth =
        (Option.map ~f:GitCloneDepth.of_xml)
          (Xml.child xml_arg0 "gitCloneDepth") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      let type_ =
        SourceType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?sourceIdentifier ?insecureSsl ?buildStatusConfig
        ?reportBuildStatus ?auth ?buildspec ?gitSubmodulesConfig
        ?gitCloneDepth ?location ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceIdentifier =
        field_map json__ "sourceIdentifier" String_.of_json in
      let insecureSsl = field_map json__ "insecureSsl" WrapperBoolean.of_json in
      let buildStatusConfig =
        field_map json__ "buildStatusConfig" BuildStatusConfig.of_json in
      let reportBuildStatus =
        field_map json__ "reportBuildStatus" WrapperBoolean.of_json in
      let auth = field_map json__ "auth" SourceAuth.of_json in
      let buildspec = field_map json__ "buildspec" String_.of_json in
      let gitSubmodulesConfig =
        field_map json__ "gitSubmodulesConfig" GitSubmodulesConfig.of_json in
      let gitCloneDepth =
        field_map json__ "gitCloneDepth" GitCloneDepth.of_json in
      let location = field_map json__ "location" String_.of_json in
      let type_ = field_map_exn json__ "type" SourceType.of_json in
      make ?sourceIdentifier ?insecureSsl ?buildStatusConfig
        ?reportBuildStatus ?auth ?buildspec ?gitSubmodulesConfig
        ?gitCloneDepth ?location ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the build input source code for the build project."]
module LogsLocation =
  struct
    type nonrec t =
      {
      groupName: String_.t option
        [@ocaml.doc
          "The name of the CloudWatch Logs group for the build logs."];
      streamName: String_.t option
        [@ocaml.doc
          "The name of the CloudWatch Logs stream for the build logs."];
      deepLink: String_.t option
        [@ocaml.doc
          "The URL to an individual build log in CloudWatch Logs. The log stream is created during the PROVISIONING phase of a build and the deeplink will not be valid until it is created."];
      s3DeepLink: String_.t option
        [@ocaml.doc "The URL to a build log in an S3 bucket."];
      cloudWatchLogsArn: String_.t option
        [@ocaml.doc
          "The ARN of the CloudWatch Logs stream for a build execution. Its format is arn:$\\{Partition\\}:logs:$\\{Region\\}:$\\{Account\\}:log-group:$\\{LogGroupName\\}:log-stream:$\\{LogStreamName\\}. The CloudWatch Logs stream is created during the PROVISIONING phase of a build and the ARN will not be valid until it is created. For more information, see Resources Defined by CloudWatch Logs."];
      s3LogsArn: String_.t option
        [@ocaml.doc
          "The ARN of S3 logs for a build project. Its format is arn:$\\{Partition\\}:s3:::$\\{BucketName\\}/$\\{ObjectName\\}. For more information, see Resources Defined by Amazon S3."];
      cloudWatchLogs: CloudWatchLogsConfig.t option
        [@ocaml.doc "Information about CloudWatch Logs for a build project."];
      s3Logs: S3LogsConfig.t option
        [@ocaml.doc "Information about S3 logs for a build project."]}
    let make ?groupName =
      fun ?streamName ->
        fun ?deepLink ->
          fun ?s3DeepLink ->
            fun ?cloudWatchLogsArn ->
              fun ?s3LogsArn ->
                fun ?cloudWatchLogs ->
                  fun ?s3Logs ->
                    fun () ->
                      {
                        groupName;
                        streamName;
                        deepLink;
                        s3DeepLink;
                        cloudWatchLogsArn;
                        s3LogsArn;
                        cloudWatchLogs;
                        s3Logs
                      }
    let to_value x =
      structure_to_value
        [("groupName", (Option.map x.groupName ~f:String_.to_value));
        ("streamName", (Option.map x.streamName ~f:String_.to_value));
        ("deepLink", (Option.map x.deepLink ~f:String_.to_value));
        ("s3DeepLink", (Option.map x.s3DeepLink ~f:String_.to_value));
        ("cloudWatchLogsArn",
          (Option.map x.cloudWatchLogsArn ~f:String_.to_value));
        ("s3LogsArn", (Option.map x.s3LogsArn ~f:String_.to_value));
        ("cloudWatchLogs",
          (Option.map x.cloudWatchLogs ~f:CloudWatchLogsConfig.to_value));
        ("s3Logs", (Option.map x.s3Logs ~f:S3LogsConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Logs =
        (Option.map ~f:S3LogsConfig.of_xml) (Xml.child xml_arg0 "s3Logs") in
      let cloudWatchLogs =
        (Option.map ~f:CloudWatchLogsConfig.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogs") in
      let s3LogsArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "s3LogsArn") in
      let cloudWatchLogsArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogsArn") in
      let s3DeepLink =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "s3DeepLink") in
      let deepLink =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "deepLink") in
      let streamName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "streamName") in
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupName") in
      make ?s3Logs ?cloudWatchLogs ?s3LogsArn ?cloudWatchLogsArn ?s3DeepLink
        ?deepLink ?streamName ?groupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Logs = field_map json__ "s3Logs" S3LogsConfig.of_json in
      let cloudWatchLogs =
        field_map json__ "cloudWatchLogs" CloudWatchLogsConfig.of_json in
      let s3LogsArn = field_map json__ "s3LogsArn" String_.of_json in
      let cloudWatchLogsArn =
        field_map json__ "cloudWatchLogsArn" String_.of_json in
      let s3DeepLink = field_map json__ "s3DeepLink" String_.of_json in
      let deepLink = field_map json__ "deepLink" String_.of_json in
      let streamName = field_map json__ "streamName" String_.of_json in
      let groupName = field_map json__ "groupName" String_.of_json in
      make ?s3Logs ?cloudWatchLogs ?s3LogsArn ?cloudWatchLogsArn ?s3DeepLink
        ?deepLink ?streamName ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about build logs in CloudWatch Logs."]
module NetworkInterface =
  struct
    type nonrec t =
      {
      subnetId: NonEmptyString.t option [@ocaml.doc "The ID of the subnet."];
      networkInterfaceId: NonEmptyString.t option
        [@ocaml.doc "The ID of the network interface."]}
    let make ?subnetId =
      fun ?networkInterfaceId -> fun () -> { subnetId; networkInterfaceId }
    let to_value x =
      structure_to_value
        [("subnetId", (Option.map x.subnetId ~f:NonEmptyString.to_value));
        ("networkInterfaceId",
          (Option.map x.networkInterfaceId ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "networkInterfaceId") in
      let subnetId =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "subnetId") in
      make ?networkInterfaceId ?subnetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInterfaceId =
        field_map json__ "networkInterfaceId" NonEmptyString.of_json in
      let subnetId = field_map json__ "subnetId" NonEmptyString.of_json in
      make ?networkInterfaceId ?subnetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a network interface."]
module SandboxSessionPhases =
  struct
    type nonrec t = SandboxSessionPhase.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SandboxSessionPhase.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SandboxSessionPhase.of_xml)
    let of_json j =
      list_of_json ~kind:"SandboxSessionPhases"
        ~of_json:SandboxSessionPhase.of_json j
    let to_json v = composed_to_json to_value v
  end
module Subnets =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:16); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"Subnets" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module NonNegativeInt =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NonNegativeInt" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Percentage =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:100.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module ReportExportConfigType =
  struct
    type nonrec t =
      | S3 
      | NO_EXPORT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | S3 -> "S3" | NO_EXPORT -> "NO_EXPORT" | Non_static_id s -> s
    let of_string =
      function | "S3" -> S3 | "NO_EXPORT" -> NO_EXPORT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReportExportConfigType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ReportExportConfigType" j)
    let to_json = simple_to_json to_value
  end
module S3ReportExportConfig =
  struct
    type nonrec t =
      {
      bucket: NonEmptyString.t option
        [@ocaml.doc
          "The name of the S3 bucket where the raw data of a report are exported."];
      bucketOwner: String_.t option
        [@ocaml.doc
          "The Amazon Web Services account identifier of the owner of the Amazon S3 bucket. This allows report data to be exported to an Amazon S3 bucket that is owned by an account other than the account running the build."];
      path: String_.t option
        [@ocaml.doc "The path to the exported report's raw data results."];
      packaging: ReportPackagingType.t option
        [@ocaml.doc
          "The type of build output artifact to create. Valid values include: NONE: CodeBuild creates the raw data in the output bucket. This is the default if packaging is not specified. ZIP: CodeBuild creates a ZIP file with the raw data in the output bucket."];
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The encryption key for the report's encrypted raw data."];
      encryptionDisabled: WrapperBoolean.t option
        [@ocaml.doc
          "A boolean value that specifies if the results of a report are encrypted."]}
    let make ?bucket =
      fun ?bucketOwner ->
        fun ?path ->
          fun ?packaging ->
            fun ?encryptionKey ->
              fun ?encryptionDisabled ->
                fun () ->
                  {
                    bucket;
                    bucketOwner;
                    path;
                    packaging;
                    encryptionKey;
                    encryptionDisabled
                  }
    let to_value x =
      structure_to_value
        [("bucket", (Option.map x.bucket ~f:NonEmptyString.to_value));
        ("bucketOwner", (Option.map x.bucketOwner ~f:String_.to_value));
        ("path", (Option.map x.path ~f:String_.to_value));
        ("packaging",
          (Option.map x.packaging ~f:ReportPackagingType.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("encryptionDisabled",
          (Option.map x.encryptionDisabled ~f:WrapperBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionDisabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "encryptionDisabled") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let packaging =
        (Option.map ~f:ReportPackagingType.of_xml)
          (Xml.child xml_arg0 "packaging") in
      let path = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "path") in
      let bucketOwner =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "bucketOwner") in
      let bucket =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "bucket") in
      make ?encryptionDisabled ?encryptionKey ?packaging ?path ?bucketOwner
        ?bucket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionDisabled =
        field_map json__ "encryptionDisabled" WrapperBoolean.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let packaging =
        field_map json__ "packaging" ReportPackagingType.of_json in
      let path = field_map json__ "path" String_.of_json in
      let bucketOwner = field_map json__ "bucketOwner" String_.of_json in
      let bucket = field_map json__ "bucket" NonEmptyString.of_json in
      make ?encryptionDisabled ?encryptionKey ?packaging ?path ?bucketOwner
        ?bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the S3 bucket where the raw data of a report are exported."]
module ReportStatusCounts =
  struct
    type nonrec t = (String_.t * WrapperInt.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (WrapperInt.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (WrapperInt.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:WrapperInt.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tag =
  struct
    type nonrec t =
      {
      key: KeyInput.t option [@ocaml.doc "The tag's key."];
      value: ValueInput.t option [@ocaml.doc "The tag's value."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Option.map x.key ~f:KeyInput.to_value));
        ("value", (Option.map x.value ~f:ValueInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:ValueInput.of_xml) (Xml.child xml_arg0 "value") in
      let key = (Option.map ~f:KeyInput.of_xml) (Xml.child xml_arg0 "key") in
      make ?value ?key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" ValueInput.of_json in
      let key = field_map json__ "key" KeyInput.of_json in
      make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A tag, consisting of a key and a value. This tag is available for use by Amazon Web Services services that support tags in CodeBuild."]
module ProjectArtifacts =
  struct
    type nonrec t =
      {
      type_: ArtifactsType.t
        [@ocaml.doc
          "The type of build output artifact. Valid values include: CODEPIPELINE: The build project has build output generated through CodePipeline. The CODEPIPELINE type is not supported for secondaryArtifacts. NO_ARTIFACTS: The build project does not produce any build output. S3: The build project stores build output in Amazon S3."];
      location: String_.t option
        [@ocaml.doc
          "Information about the build output artifact location: If type is set to CODEPIPELINE, CodePipeline ignores this value if specified. This is because CodePipeline manages its build output locations instead of CodeBuild. If type is set to NO_ARTIFACTS, this value is ignored if specified, because no build output is produced. If type is set to S3, this is the name of the output bucket."];
      path: String_.t option
        [@ocaml.doc
          "Along with namespaceType and name, the pattern that CodeBuild uses to name and store the output artifact: If type is set to CODEPIPELINE, CodePipeline ignores this value if specified. This is because CodePipeline manages its build output names instead of CodeBuild. If type is set to NO_ARTIFACTS, this value is ignored if specified, because no build output is produced. If type is set to S3, this is the path to the output artifact. If path is not specified, path is not used. For example, if path is set to MyArtifacts, namespaceType is set to NONE, and name is set to MyArtifact.zip, the output artifact is stored in the output bucket at MyArtifacts/MyArtifact.zip."];
      namespaceType: ArtifactNamespace.t option
        [@ocaml.doc
          "Along with path and name, the pattern that CodeBuild uses to determine the name and location to store the output artifact: If type is set to CODEPIPELINE, CodePipeline ignores this value if specified. This is because CodePipeline manages its build output names instead of CodeBuild. If type is set to NO_ARTIFACTS, this value is ignored if specified, because no build output is produced. If type is set to S3, valid values include: BUILD_ID: Include the build ID in the location of the build output artifact. NONE: Do not include the build ID. This is the default if namespaceType is not specified. For example, if path is set to MyArtifacts, namespaceType is set to BUILD_ID, and name is set to MyArtifact.zip, the output artifact is stored in MyArtifacts/<build-ID>/MyArtifact.zip."];
      name: String_.t option
        [@ocaml.doc
          "Along with path and namespaceType, the pattern that CodeBuild uses to name and store the output artifact: If type is set to CODEPIPELINE, CodePipeline ignores this value if specified. This is because CodePipeline manages its build output names instead of CodeBuild. If type is set to NO_ARTIFACTS, this value is ignored if specified, because no build output is produced. If type is set to S3, this is the name of the output artifact object. If you set the name to be a forward slash (\"/\"), the artifact is stored in the root of the output bucket. For example: If path is set to MyArtifacts, namespaceType is set to BUILD_ID, and name is set to MyArtifact.zip, then the output artifact is stored in MyArtifacts/<build-ID>/MyArtifact.zip. If path is empty, namespaceType is set to NONE, and name is set to \"/\", the output artifact is stored in the root of the output bucket. If path is set to MyArtifacts, namespaceType is set to BUILD_ID, and name is set to \"/\", the output artifact is stored in MyArtifacts/<build-ID>."];
      packaging: ArtifactPackaging.t option
        [@ocaml.doc
          "The type of build output artifact to create: If type is set to CODEPIPELINE, CodePipeline ignores this value if specified. This is because CodePipeline manages its build output artifacts instead of CodeBuild. If type is set to NO_ARTIFACTS, this value is ignored if specified, because no build output is produced. If type is set to S3, valid values include: NONE: CodeBuild creates in the output bucket a folder that contains the build output. This is the default if packaging is not specified. ZIP: CodeBuild creates in the output bucket a ZIP file that contains the build output."];
      overrideArtifactName: WrapperBoolean.t option
        [@ocaml.doc
          "If this flag is set, a name specified in the buildspec file overrides the artifact name. The name specified in a buildspec file is calculated at build time and uses the Shell Command Language. For example, you can append a date and time to your artifact name so that it is always unique."];
      encryptionDisabled: WrapperBoolean.t option
        [@ocaml.doc
          "Set to true if you do not want your output artifacts encrypted. This option is valid only if your artifacts type is Amazon S3. If this is set with another artifacts type, an invalidInputException is thrown."];
      artifactIdentifier: String_.t option
        [@ocaml.doc "An identifier for this artifact definition."];
      bucketOwnerAccess: BucketOwnerAccess.t option }
    let context_ = "ProjectArtifacts"
    let make ?location =
      fun ?path ->
        fun ?namespaceType ->
          fun ?name ->
            fun ?packaging ->
              fun ?overrideArtifactName ->
                fun ?encryptionDisabled ->
                  fun ?artifactIdentifier ->
                    fun ?bucketOwnerAccess ->
                      fun ~type_ ->
                        fun () ->
                          {
                            location;
                            path;
                            namespaceType;
                            name;
                            packaging;
                            overrideArtifactName;
                            encryptionDisabled;
                            artifactIdentifier;
                            bucketOwnerAccess;
                            type_
                          }
    let to_value x =
      structure_to_value
        [("type", (Some (ArtifactsType.to_value x.type_)));
        ("location", (Option.map x.location ~f:String_.to_value));
        ("path", (Option.map x.path ~f:String_.to_value));
        ("namespaceType",
          (Option.map x.namespaceType ~f:ArtifactNamespace.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("packaging", (Option.map x.packaging ~f:ArtifactPackaging.to_value));
        ("overrideArtifactName",
          (Option.map x.overrideArtifactName ~f:WrapperBoolean.to_value));
        ("encryptionDisabled",
          (Option.map x.encryptionDisabled ~f:WrapperBoolean.to_value));
        ("artifactIdentifier",
          (Option.map x.artifactIdentifier ~f:String_.to_value));
        ("bucketOwnerAccess",
          (Option.map x.bucketOwnerAccess ~f:BucketOwnerAccess.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucketOwnerAccess =
        (Option.map ~f:BucketOwnerAccess.of_xml)
          (Xml.child xml_arg0 "bucketOwnerAccess") in
      let artifactIdentifier =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "artifactIdentifier") in
      let encryptionDisabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "encryptionDisabled") in
      let overrideArtifactName =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "overrideArtifactName") in
      let packaging =
        (Option.map ~f:ArtifactPackaging.of_xml)
          (Xml.child xml_arg0 "packaging") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let namespaceType =
        (Option.map ~f:ArtifactNamespace.of_xml)
          (Xml.child xml_arg0 "namespaceType") in
      let path = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "path") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      let type_ =
        ArtifactsType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?bucketOwnerAccess ?artifactIdentifier ?encryptionDisabled
        ?overrideArtifactName ?packaging ?name ?namespaceType ?path ?location
        ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bucketOwnerAccess =
        field_map json__ "bucketOwnerAccess" BucketOwnerAccess.of_json in
      let artifactIdentifier =
        field_map json__ "artifactIdentifier" String_.of_json in
      let encryptionDisabled =
        field_map json__ "encryptionDisabled" WrapperBoolean.of_json in
      let overrideArtifactName =
        field_map json__ "overrideArtifactName" WrapperBoolean.of_json in
      let packaging = field_map json__ "packaging" ArtifactPackaging.of_json in
      let name = field_map json__ "name" String_.of_json in
      let namespaceType =
        field_map json__ "namespaceType" ArtifactNamespace.of_json in
      let path = field_map json__ "path" String_.of_json in
      let location = field_map json__ "location" String_.of_json in
      let type_ = field_map_exn json__ "type" ArtifactsType.of_json in
      make ?bucketOwnerAccess ?artifactIdentifier ?encryptionDisabled
        ?overrideArtifactName ?packaging ?name ?namespaceType ?path ?location
        ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the build output artifacts for the build project."]
module BatchReportModeType =
  struct
    type nonrec t =
      | REPORT_INDIVIDUAL_BUILDS 
      | REPORT_AGGREGATED_BATCH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | REPORT_INDIVIDUAL_BUILDS -> "REPORT_INDIVIDUAL_BUILDS"
      | REPORT_AGGREGATED_BATCH -> "REPORT_AGGREGATED_BATCH"
      | Non_static_id s -> s
    let of_string =
      function
      | "REPORT_INDIVIDUAL_BUILDS" -> REPORT_INDIVIDUAL_BUILDS
      | "REPORT_AGGREGATED_BATCH" -> REPORT_AGGREGATED_BATCH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BatchReportModeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BatchReportModeType" j)
    let to_json = simple_to_json to_value
  end
module BatchRestrictions =
  struct
    type nonrec t =
      {
      maximumBuildsAllowed: WrapperInt.t option
        [@ocaml.doc "Specifies the maximum number of builds allowed."];
      computeTypesAllowed: ComputeTypesAllowed.t option
        [@ocaml.doc
          "An array of strings that specify the compute types that are allowed for the batch build. See Build environment compute types in the CodeBuild User Guide for these values."];
      fleetsAllowed: FleetsAllowed.t option
        [@ocaml.doc
          "An array of strings that specify the fleets that are allowed for the batch build. See Run builds on reserved capacity fleets in the CodeBuild User Guide for more information."]}
    let make ?maximumBuildsAllowed =
      fun ?computeTypesAllowed ->
        fun ?fleetsAllowed ->
          fun () ->
            { maximumBuildsAllowed; computeTypesAllowed; fleetsAllowed }
    let to_value x =
      structure_to_value
        [("maximumBuildsAllowed",
           (Option.map x.maximumBuildsAllowed ~f:WrapperInt.to_value));
        ("computeTypesAllowed",
          (Option.map x.computeTypesAllowed ~f:ComputeTypesAllowed.to_value));
        ("fleetsAllowed",
          (Option.map x.fleetsAllowed ~f:FleetsAllowed.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetsAllowed =
        (Option.map ~f:FleetsAllowed.of_xml)
          (Xml.child xml_arg0 "fleetsAllowed") in
      let computeTypesAllowed =
        (Option.map ~f:ComputeTypesAllowed.of_xml)
          (Xml.child xml_arg0 "computeTypesAllowed") in
      let maximumBuildsAllowed =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "maximumBuildsAllowed") in
      make ?fleetsAllowed ?computeTypesAllowed ?maximumBuildsAllowed ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetsAllowed =
        field_map json__ "fleetsAllowed" FleetsAllowed.of_json in
      let computeTypesAllowed =
        field_map json__ "computeTypesAllowed" ComputeTypesAllowed.of_json in
      let maximumBuildsAllowed =
        field_map json__ "maximumBuildsAllowed" WrapperInt.of_json in
      make ?fleetsAllowed ?computeTypesAllowed ?maximumBuildsAllowed ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies restrictions for the batch build."]
module CacheType =
  struct
    type nonrec t =
      | NO_CACHE 
      | S3 
      | LOCAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NO_CACHE -> "NO_CACHE"
      | S3 -> "S3"
      | LOCAL -> "LOCAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "NO_CACHE" -> NO_CACHE
      | "S3" -> S3
      | "LOCAL" -> LOCAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CacheType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CacheType" j)
    let to_json = simple_to_json to_value
  end
module ProjectCacheModes =
  struct
    type nonrec t = CacheMode.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CacheMode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CacheMode.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectCacheModes" ~of_json:CacheMode.of_json j
    let to_json v = composed_to_json to_value v
  end
module FilterGroups =
  struct
    type nonrec t = FilterGroup.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FilterGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FilterGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"FilterGroups" ~of_json:FilterGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module PullRequestBuildPolicy =
  struct
    type nonrec t =
      {
      requiresCommentApproval: PullRequestBuildCommentApproval.t
        [@ocaml.doc
          "Specifies when comment-based approval is required before triggering a build on pull requests. This setting determines whether builds run automatically or require explicit approval through comments. DISABLED: Builds trigger automatically without requiring comment approval ALL_PULL_REQUESTS: All pull requests require comment approval before builds execute (unless contributor is one of the approver roles) FORK_PULL_REQUESTS: Only pull requests from forked repositories require comment approval (unless contributor is one of the approver roles)"];
      approverRoles: PullRequestBuildApproverRoles.t option
        [@ocaml.doc
          "List of repository roles that have approval privileges for pull request builds when comment approval is required. Only users with these roles can provide valid comment approvals. If a pull request contributor is one of these roles, their pull request builds will trigger automatically. This field is only applicable when requiresCommentApproval is not DISABLED."]}
    let context_ = "PullRequestBuildPolicy"
    let make ?approverRoles =
      fun ~requiresCommentApproval ->
        fun () -> { approverRoles; requiresCommentApproval }
    let to_value x =
      structure_to_value
        [("requiresCommentApproval",
           (Some
              (PullRequestBuildCommentApproval.to_value
                 x.requiresCommentApproval)));
        ("approverRoles",
          (Option.map x.approverRoles
             ~f:PullRequestBuildApproverRoles.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let approverRoles =
        (Option.map ~f:PullRequestBuildApproverRoles.of_xml)
          (Xml.child xml_arg0 "approverRoles") in
      let requiresCommentApproval =
        PullRequestBuildCommentApproval.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requiresCommentApproval") in
      make ?approverRoles ~requiresCommentApproval ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let approverRoles =
        field_map json__ "approverRoles"
          PullRequestBuildApproverRoles.of_json in
      let requiresCommentApproval =
        field_map_exn json__ "requiresCommentApproval"
          PullRequestBuildCommentApproval.of_json in
      make ?approverRoles ~requiresCommentApproval ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A PullRequestBuildPolicy object that defines comment-based approval requirements for triggering builds on pull requests. This policy helps control when automated builds are executed based on contributor permissions and approval workflows."]
module ScopeConfiguration =
  struct
    type nonrec t =
      {
      name: String_.t
        [@ocaml.doc
          "The name of either the group, enterprise, or organization that will send webhook events to CodeBuild, depending on the type of webhook."];
      domain: String_.t option
        [@ocaml.doc
          "The domain of the GitHub Enterprise organization or the GitLab Self Managed group. Note that this parameter is only required if your project's source type is GITHUB_ENTERPRISE or GITLAB_SELF_MANAGED."];
      scope: WebhookScopeType.t
        [@ocaml.doc
          "The type of scope for a GitHub or GitLab webhook. The scope default is GITHUB_ORGANIZATION."]}
    let context_ = "ScopeConfiguration"
    let make ?domain =
      fun ~name -> fun ~scope -> fun () -> { domain; name; scope }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("domain", (Option.map x.domain ~f:String_.to_value));
        ("scope", (Some (WebhookScopeType.to_value x.scope)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scope =
        WebhookScopeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "scope") in
      let domain =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "domain") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~scope ?domain ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scope = field_map_exn json__ "scope" WebhookScopeType.of_json in
      let domain = field_map json__ "domain" String_.of_json in
      let name = field_map_exn json__ "name" String_.of_json in
      make ~scope ?domain ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration information about the scope for a webhook."]
module WebhookBuildType =
  struct
    type nonrec t =
      | BUILD 
      | BUILD_BATCH 
      | RUNNER_BUILDKITE_BUILD 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BUILD -> "BUILD"
      | BUILD_BATCH -> "BUILD_BATCH"
      | RUNNER_BUILDKITE_BUILD -> "RUNNER_BUILDKITE_BUILD"
      | Non_static_id s -> s
    let of_string =
      function
      | "BUILD" -> BUILD
      | "BUILD_BATCH" -> BUILD_BATCH
      | "RUNNER_BUILDKITE_BUILD" -> RUNNER_BUILDKITE_BUILD
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration WebhookBuildType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WebhookBuildType" j)
    let to_json = simple_to_json to_value
  end
module WebhookStatus =
  struct
    type nonrec t =
      | CREATING 
      | CREATE_FAILED 
      | ACTIVE 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | CREATE_FAILED -> "CREATE_FAILED"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "CREATE_FAILED" -> CREATE_FAILED
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration WebhookStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WebhookStatus" j)
    let to_json = simple_to_json to_value
  end
module FleetContextCode =
  struct
    type nonrec t =
      | CREATE_FAILED 
      | UPDATE_FAILED 
      | ACTION_REQUIRED 
      | PENDING_DELETION 
      | INSUFFICIENT_CAPACITY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATE_FAILED -> "CREATE_FAILED"
      | UPDATE_FAILED -> "UPDATE_FAILED"
      | ACTION_REQUIRED -> "ACTION_REQUIRED"
      | PENDING_DELETION -> "PENDING_DELETION"
      | INSUFFICIENT_CAPACITY -> "INSUFFICIENT_CAPACITY"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATE_FAILED" -> CREATE_FAILED
      | "UPDATE_FAILED" -> UPDATE_FAILED
      | "ACTION_REQUIRED" -> ACTION_REQUIRED
      | "PENDING_DELETION" -> PENDING_DELETION
      | "INSUFFICIENT_CAPACITY" -> INSUFFICIENT_CAPACITY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetContextCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetContextCode" j)
    let to_json = simple_to_json to_value
  end
module FleetStatusCode =
  struct
    type nonrec t =
      | CREATING 
      | UPDATING 
      | ROTATING 
      | PENDING_DELETION 
      | DELETING 
      | CREATE_FAILED 
      | UPDATE_ROLLBACK_FAILED 
      | ACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | UPDATING -> "UPDATING"
      | ROTATING -> "ROTATING"
      | PENDING_DELETION -> "PENDING_DELETION"
      | DELETING -> "DELETING"
      | CREATE_FAILED -> "CREATE_FAILED"
      | UPDATE_ROLLBACK_FAILED -> "UPDATE_ROLLBACK_FAILED"
      | ACTIVE -> "ACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "UPDATING" -> UPDATING
      | "ROTATING" -> ROTATING
      | "PENDING_DELETION" -> PENDING_DELETION
      | "DELETING" -> DELETING
      | "CREATE_FAILED" -> CREATE_FAILED
      | "UPDATE_ROLLBACK_FAILED" -> UPDATE_ROLLBACK_FAILED
      | "ACTIVE" -> ACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetStatusCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetStatusCode" j)
    let to_json = simple_to_json to_value
  end
module FleetProxyRuleBehavior =
  struct
    type nonrec t =
      | ALLOW_ALL 
      | DENY_ALL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALLOW_ALL -> "ALLOW_ALL"
      | DENY_ALL -> "DENY_ALL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALLOW_ALL" -> ALLOW_ALL
      | "DENY_ALL" -> DENY_ALL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FleetProxyRuleBehavior" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FleetProxyRuleBehavior" j)
    let to_json = simple_to_json to_value
  end
module FleetProxyRules =
  struct
    type nonrec t = FleetProxyRule.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetProxyRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetProxyRule.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetProxyRules" ~of_json:FleetProxyRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetCapacity =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for FleetCapacity" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module FleetScalingType =
  struct
    type nonrec t =
      | TARGET_TRACKING_SCALING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TARGET_TRACKING_SCALING -> "TARGET_TRACKING_SCALING"
      | Non_static_id s -> s
    let of_string =
      function
      | "TARGET_TRACKING_SCALING" -> TARGET_TRACKING_SCALING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetScalingType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetScalingType" j)
    let to_json = simple_to_json to_value
  end
module TargetTrackingScalingConfigurations =
  struct
    type nonrec t = TargetTrackingScalingConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TargetTrackingScalingConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:TargetTrackingScalingConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"TargetTrackingScalingConfigurations"
        ~of_json:TargetTrackingScalingConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildArtifacts =
  struct
    type nonrec t =
      {
      location: String_.t option
        [@ocaml.doc "Information about the location of the build artifacts."];
      sha256sum: String_.t option
        [@ocaml.doc
          "The SHA-256 hash of the build artifact. You can use this hash along with a checksum tool to confirm file integrity and authenticity. This value is available only if the build project's packaging value is set to ZIP."];
      md5sum: String_.t option
        [@ocaml.doc
          "The MD5 hash of the build artifact. You can use this hash along with a checksum tool to confirm file integrity and authenticity. This value is available only if the build project's packaging value is set to ZIP."];
      overrideArtifactName: WrapperBoolean.t option
        [@ocaml.doc
          "If this flag is set, a name specified in the buildspec file overrides the artifact name. The name specified in a buildspec file is calculated at build time and uses the Shell Command Language. For example, you can append a date and time to your artifact name so that it is always unique."];
      encryptionDisabled: WrapperBoolean.t option
        [@ocaml.doc
          "Information that tells you if encryption for build artifacts is disabled."];
      artifactIdentifier: String_.t option
        [@ocaml.doc "An identifier for this artifact definition."];
      bucketOwnerAccess: BucketOwnerAccess.t option }
    let make ?location =
      fun ?sha256sum ->
        fun ?md5sum ->
          fun ?overrideArtifactName ->
            fun ?encryptionDisabled ->
              fun ?artifactIdentifier ->
                fun ?bucketOwnerAccess ->
                  fun () ->
                    {
                      location;
                      sha256sum;
                      md5sum;
                      overrideArtifactName;
                      encryptionDisabled;
                      artifactIdentifier;
                      bucketOwnerAccess
                    }
    let to_value x =
      structure_to_value
        [("location", (Option.map x.location ~f:String_.to_value));
        ("sha256sum", (Option.map x.sha256sum ~f:String_.to_value));
        ("md5sum", (Option.map x.md5sum ~f:String_.to_value));
        ("overrideArtifactName",
          (Option.map x.overrideArtifactName ~f:WrapperBoolean.to_value));
        ("encryptionDisabled",
          (Option.map x.encryptionDisabled ~f:WrapperBoolean.to_value));
        ("artifactIdentifier",
          (Option.map x.artifactIdentifier ~f:String_.to_value));
        ("bucketOwnerAccess",
          (Option.map x.bucketOwnerAccess ~f:BucketOwnerAccess.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucketOwnerAccess =
        (Option.map ~f:BucketOwnerAccess.of_xml)
          (Xml.child xml_arg0 "bucketOwnerAccess") in
      let artifactIdentifier =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "artifactIdentifier") in
      let encryptionDisabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "encryptionDisabled") in
      let overrideArtifactName =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "overrideArtifactName") in
      let md5sum =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "md5sum") in
      let sha256sum =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sha256sum") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      make ?bucketOwnerAccess ?artifactIdentifier ?encryptionDisabled
        ?overrideArtifactName ?md5sum ?sha256sum ?location ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bucketOwnerAccess =
        field_map json__ "bucketOwnerAccess" BucketOwnerAccess.of_json in
      let artifactIdentifier =
        field_map json__ "artifactIdentifier" String_.of_json in
      let encryptionDisabled =
        field_map json__ "encryptionDisabled" WrapperBoolean.of_json in
      let overrideArtifactName =
        field_map json__ "overrideArtifactName" WrapperBoolean.of_json in
      let md5sum = field_map json__ "md5sum" String_.of_json in
      let sha256sum = field_map json__ "sha256sum" String_.of_json in
      let location = field_map json__ "location" String_.of_json in
      make ?bucketOwnerAccess ?artifactIdentifier ?encryptionDisabled
        ?overrideArtifactName ?md5sum ?sha256sum ?location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about build output artifacts."]
module BuildPhase =
  struct
    type nonrec t =
      {
      phaseType: BuildPhaseType.t option
        [@ocaml.doc
          "The name of the build phase. Valid values include: BUILD Core build activities typically occur in this build phase. COMPLETED The build has been completed. DOWNLOAD_SOURCE Source code is being downloaded in this build phase. FINALIZING The build process is completing in this build phase. INSTALL Installation activities typically occur in this build phase. POST_BUILD Post-build activities typically occur in this build phase. PRE_BUILD Pre-build activities typically occur in this build phase. PROVISIONING The build environment is being set up. QUEUED The build has been submitted and is queued behind other submitted builds. SUBMITTED The build has been submitted. UPLOAD_ARTIFACTS Build output artifacts are being uploaded to the output location."];
      phaseStatus: StatusType.t option
        [@ocaml.doc
          "The current status of the build phase. Valid values include: FAILED The build phase failed. FAULT The build phase faulted. IN_PROGRESS The build phase is still in progress. STOPPED The build phase stopped. SUCCEEDED The build phase succeeded. TIMED_OUT The build phase timed out."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the build phase started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the build phase ended, expressed in Unix time format."];
      durationInSeconds: WrapperLong.t option
        [@ocaml.doc
          "How long, in seconds, between the starting and ending times of the build's phase."];
      contexts: PhaseContexts.t option
        [@ocaml.doc
          "Additional information about a build phase, especially to help troubleshoot a failed build."]}
    let make ?phaseType =
      fun ?phaseStatus ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?durationInSeconds ->
              fun ?contexts ->
                fun () ->
                  {
                    phaseType;
                    phaseStatus;
                    startTime;
                    endTime;
                    durationInSeconds;
                    contexts
                  }
    let to_value x =
      structure_to_value
        [("phaseType", (Option.map x.phaseType ~f:BuildPhaseType.to_value));
        ("phaseStatus", (Option.map x.phaseStatus ~f:StatusType.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("durationInSeconds",
          (Option.map x.durationInSeconds ~f:WrapperLong.to_value));
        ("contexts", (Option.map x.contexts ~f:PhaseContexts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contexts =
        (Option.map ~f:PhaseContexts.of_xml) (Xml.child xml_arg0 "contexts") in
      let durationInSeconds =
        (Option.map ~f:WrapperLong.of_xml)
          (Xml.child xml_arg0 "durationInSeconds") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let phaseStatus =
        (Option.map ~f:StatusType.of_xml) (Xml.child xml_arg0 "phaseStatus") in
      let phaseType =
        (Option.map ~f:BuildPhaseType.of_xml)
          (Xml.child xml_arg0 "phaseType") in
      make ?contexts ?durationInSeconds ?endTime ?startTime ?phaseStatus
        ?phaseType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contexts = field_map json__ "contexts" PhaseContexts.of_json in
      let durationInSeconds =
        field_map json__ "durationInSeconds" WrapperLong.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let phaseStatus = field_map json__ "phaseStatus" StatusType.of_json in
      let phaseType = field_map json__ "phaseType" BuildPhaseType.of_json in
      make ?contexts ?durationInSeconds ?endTime ?startTime ?phaseStatus
        ?phaseType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a stage for a build."]
module ExportedEnvironmentVariable =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t option
        [@ocaml.doc "The name of the exported environment variable."];
      value: String_.t option
        [@ocaml.doc
          "The value assigned to the exported environment variable."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:NonEmptyString.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" String_.of_json in
      let name = field_map json__ "name" NonEmptyString.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an exported environment variable. Exported environment variables are used in conjunction with CodePipeline to export environment variables from the current build stage to subsequent stages in the pipeline. For more information, see Working with variables in the CodePipeline User Guide. During a build, the value of a variable is available starting with the install phase. It can be updated between the start of the install phase and the end of the post_build phase. After the post_build phase ends, the value of exported variables cannot change."]
module BuildBatchPhase =
  struct
    type nonrec t =
      {
      phaseType: BuildBatchPhaseType.t option
        [@ocaml.doc
          "The name of the batch build phase. Valid values include: COMBINE_ARTIFACTS Build output artifacts are being combined and uploaded to the output location. DOWNLOAD_BATCHSPEC The batch build specification is being downloaded. FAILED One or more of the builds failed. IN_PROGRESS The batch build is in progress. STOPPED The batch build was stopped. SUBMITTED The btach build has been submitted. SUCCEEDED The batch build succeeded."];
      phaseStatus: StatusType.t option
        [@ocaml.doc
          "The current status of the batch build phase. Valid values include: FAILED The build phase failed. FAULT The build phase faulted. IN_PROGRESS The build phase is still in progress. STOPPED The build phase stopped. SUCCEEDED The build phase succeeded. TIMED_OUT The build phase timed out."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the batch build phase started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the batch build phase ended, expressed in Unix time format."];
      durationInSeconds: WrapperLong.t option
        [@ocaml.doc
          "How long, in seconds, between the starting and ending times of the batch build's phase."];
      contexts: PhaseContexts.t option
        [@ocaml.doc
          "Additional information about the batch build phase. Especially to help troubleshoot a failed batch build."]}
    let make ?phaseType =
      fun ?phaseStatus ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?durationInSeconds ->
              fun ?contexts ->
                fun () ->
                  {
                    phaseType;
                    phaseStatus;
                    startTime;
                    endTime;
                    durationInSeconds;
                    contexts
                  }
    let to_value x =
      structure_to_value
        [("phaseType",
           (Option.map x.phaseType ~f:BuildBatchPhaseType.to_value));
        ("phaseStatus", (Option.map x.phaseStatus ~f:StatusType.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("durationInSeconds",
          (Option.map x.durationInSeconds ~f:WrapperLong.to_value));
        ("contexts", (Option.map x.contexts ~f:PhaseContexts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contexts =
        (Option.map ~f:PhaseContexts.of_xml) (Xml.child xml_arg0 "contexts") in
      let durationInSeconds =
        (Option.map ~f:WrapperLong.of_xml)
          (Xml.child xml_arg0 "durationInSeconds") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let phaseStatus =
        (Option.map ~f:StatusType.of_xml) (Xml.child xml_arg0 "phaseStatus") in
      let phaseType =
        (Option.map ~f:BuildBatchPhaseType.of_xml)
          (Xml.child xml_arg0 "phaseType") in
      make ?contexts ?durationInSeconds ?endTime ?startTime ?phaseStatus
        ?phaseType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contexts = field_map json__ "contexts" PhaseContexts.of_json in
      let durationInSeconds =
        field_map json__ "durationInSeconds" WrapperLong.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let phaseStatus = field_map json__ "phaseStatus" StatusType.of_json in
      let phaseType =
        field_map json__ "phaseType" BuildBatchPhaseType.of_json in
      make ?contexts ?durationInSeconds ?endTime ?startTime ?phaseStatus
        ?phaseType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about a stage for a batch build."]
module BuildGroup =
  struct
    type nonrec t =
      {
      identifier: String_.t option
        [@ocaml.doc "Contains the identifier of the build group."];
      dependsOn: Identifiers.t option
        [@ocaml.doc
          "An array of strings that contain the identifiers of the build groups that this build group depends on."];
      ignoreFailure: Boolean.t option
        [@ocaml.doc
          "Specifies if failures in this build group can be ignored."];
      currentBuildSummary: BuildSummary.t option
        [@ocaml.doc
          "A BuildSummary object that contains a summary of the current build group."];
      priorBuildSummaryList: BuildSummaries.t option
        [@ocaml.doc
          "An array of BuildSummary objects that contain summaries of previous build groups."]}
    let make ?identifier =
      fun ?dependsOn ->
        fun ?ignoreFailure ->
          fun ?currentBuildSummary ->
            fun ?priorBuildSummaryList ->
              fun () ->
                {
                  identifier;
                  dependsOn;
                  ignoreFailure;
                  currentBuildSummary;
                  priorBuildSummaryList
                }
    let to_value x =
      structure_to_value
        [("identifier", (Option.map x.identifier ~f:String_.to_value));
        ("dependsOn", (Option.map x.dependsOn ~f:Identifiers.to_value));
        ("ignoreFailure", (Option.map x.ignoreFailure ~f:Boolean.to_value));
        ("currentBuildSummary",
          (Option.map x.currentBuildSummary ~f:BuildSummary.to_value));
        ("priorBuildSummaryList",
          (Option.map x.priorBuildSummaryList ~f:BuildSummaries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let priorBuildSummaryList =
        (Option.map ~f:BuildSummaries.of_xml)
          (Xml.child xml_arg0 "priorBuildSummaryList") in
      let currentBuildSummary =
        (Option.map ~f:BuildSummary.of_xml)
          (Xml.child xml_arg0 "currentBuildSummary") in
      let ignoreFailure =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ignoreFailure") in
      let dependsOn =
        (Option.map ~f:Identifiers.of_xml) (Xml.child xml_arg0 "dependsOn") in
      let identifier =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "identifier") in
      make ?priorBuildSummaryList ?currentBuildSummary ?ignoreFailure
        ?dependsOn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let priorBuildSummaryList =
        field_map json__ "priorBuildSummaryList" BuildSummaries.of_json in
      let currentBuildSummary =
        field_map json__ "currentBuildSummary" BuildSummary.of_json in
      let ignoreFailure = field_map json__ "ignoreFailure" Boolean.of_json in
      let dependsOn = field_map json__ "dependsOn" Identifiers.of_json in
      let identifier = field_map json__ "identifier" String_.of_json in
      make ?priorBuildSummaryList ?currentBuildSummary ?ignoreFailure
        ?dependsOn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a batch build build group. Build groups are used to combine builds that can run in parallel, while still being able to set dependencies on other build groups."]
module AuthType =
  struct
    type nonrec t =
      | OAUTH 
      | BASIC_AUTH 
      | PERSONAL_ACCESS_TOKEN 
      | CODECONNECTIONS 
      | SECRETS_MANAGER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OAUTH -> "OAUTH"
      | BASIC_AUTH -> "BASIC_AUTH"
      | PERSONAL_ACCESS_TOKEN -> "PERSONAL_ACCESS_TOKEN"
      | CODECONNECTIONS -> "CODECONNECTIONS"
      | SECRETS_MANAGER -> "SECRETS_MANAGER"
      | Non_static_id s -> s
    let of_string =
      function
      | "OAUTH" -> OAUTH
      | "BASIC_AUTH" -> BASIC_AUTH
      | "PERSONAL_ACCESS_TOKEN" -> PERSONAL_ACCESS_TOKEN
      | "CODECONNECTIONS" -> CODECONNECTIONS
      | "SECRETS_MANAGER" -> SECRETS_MANAGER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AuthType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AuthType" j)
    let to_json = simple_to_json to_value
  end
module ServerType =
  struct
    type nonrec t =
      | GITHUB 
      | BITBUCKET 
      | GITHUB_ENTERPRISE 
      | GITLAB 
      | GITLAB_SELF_MANAGED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GITHUB -> "GITHUB"
      | BITBUCKET -> "BITBUCKET"
      | GITHUB_ENTERPRISE -> "GITHUB_ENTERPRISE"
      | GITLAB -> "GITLAB"
      | GITLAB_SELF_MANAGED -> "GITLAB_SELF_MANAGED"
      | Non_static_id s -> s
    let of_string =
      function
      | "GITHUB" -> GITHUB
      | "BITBUCKET" -> BITBUCKET
      | "GITHUB_ENTERPRISE" -> GITHUB_ENTERPRISE
      | "GITLAB" -> GITLAB
      | "GITLAB_SELF_MANAGED" -> GITLAB_SELF_MANAGED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ServerType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ServerType" j)
    let to_json = simple_to_json to_value
  end
module EnvironmentLanguages =
  struct
    type nonrec t = EnvironmentLanguage.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EnvironmentLanguage.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnvironmentLanguage.of_xml)
    let of_json j =
      list_of_json ~kind:"EnvironmentLanguages"
        ~of_json:EnvironmentLanguage.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlatformType =
  struct
    type nonrec t =
      | DEBIAN 
      | AMAZON_LINUX 
      | UBUNTU 
      | WINDOWS_SERVER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEBIAN -> "DEBIAN"
      | AMAZON_LINUX -> "AMAZON_LINUX"
      | UBUNTU -> "UBUNTU"
      | WINDOWS_SERVER -> "WINDOWS_SERVER"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEBIAN" -> DEBIAN
      | "AMAZON_LINUX" -> AMAZON_LINUX
      | "UBUNTU" -> UBUNTU
      | "WINDOWS_SERVER" -> WINDOWS_SERVER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PlatformType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PlatformType" j)
    let to_json = simple_to_json to_value
  end
module CommandType =
  struct
    type nonrec t =
      | SHELL 
      | Non_static_id of string 
    let make i = i
    let to_string = function | SHELL -> "SHELL" | Non_static_id s -> s
    let of_string = function | "SHELL" -> SHELL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CommandType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CommandType" j)
    let to_json = simple_to_json to_value
  end
module SensitiveNonEmptyString =
  struct
    type nonrec t = string
    let context_ = "SensitiveNonEmptyString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveNonEmptyString" j
    let to_json = simple_to_json to_value
  end
module LogsConfig =
  struct
    type nonrec t =
      {
      cloudWatchLogs: CloudWatchLogsConfig.t option
        [@ocaml.doc
          "Information about CloudWatch Logs for a build project. CloudWatch Logs are enabled by default."];
      s3Logs: S3LogsConfig.t option
        [@ocaml.doc
          "Information about logs built to an S3 bucket for a build project. S3 logs are not enabled by default."]}
    let make ?cloudWatchLogs =
      fun ?s3Logs -> fun () -> { cloudWatchLogs; s3Logs }
    let to_value x =
      structure_to_value
        [("cloudWatchLogs",
           (Option.map x.cloudWatchLogs ~f:CloudWatchLogsConfig.to_value));
        ("s3Logs", (Option.map x.s3Logs ~f:S3LogsConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Logs =
        (Option.map ~f:S3LogsConfig.of_xml) (Xml.child xml_arg0 "s3Logs") in
      let cloudWatchLogs =
        (Option.map ~f:CloudWatchLogsConfig.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogs") in
      make ?s3Logs ?cloudWatchLogs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Logs = field_map json__ "s3Logs" S3LogsConfig.of_json in
      let cloudWatchLogs =
        field_map json__ "cloudWatchLogs" CloudWatchLogsConfig.of_json in
      make ?s3Logs ?cloudWatchLogs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about logs for a build project. These can be logs in CloudWatch Logs, built in a specified S3 bucket, or both."]
module ProjectEnvironment =
  struct
    type nonrec t =
      {
      type_: EnvironmentType.t
        [@ocaml.doc
          "The type of build environment to use for related builds. If you're using compute fleets during project creation, type will be ignored. For more information, see Build environment compute types in the CodeBuild user guide."];
      image: NonEmptyString.t
        [@ocaml.doc
          "The image tag or image digest that identifies the Docker image to use for this build project. Use the following formats: For an image tag: <registry>/<repository>:<tag>. For example, in the Docker repository that CodeBuild uses to manage its Docker images, this would be aws/codebuild/standard:4.0. For an image digest: <registry>/<repository>\\@<digest>. For example, to specify an image with the digest \"sha256:cbbf2f9a99b47fc460d422812b6a5adff7dfee951d8fa2e4a98caa0382cfbdbf,\" use <registry>/<repository>\\@sha256:cbbf2f9a99b47fc460d422812b6a5adff7dfee951d8fa2e4a98caa0382cfbdbf. For more information, see Docker images provided by CodeBuild in the CodeBuild user guide."];
      computeType: ComputeType.t
        [@ocaml.doc
          "Information about the compute resources the build project uses. Available values include: ATTRIBUTE_BASED_COMPUTE: Specify the amount of vCPUs, memory, disk space, and the type of machine. If you use ATTRIBUTE_BASED_COMPUTE, you must define your attributes by using computeConfiguration. CodeBuild will select the cheapest instance that satisfies your specified attributes. For more information, see Reserved capacity environment types in the CodeBuild User Guide. BUILD_GENERAL1_SMALL: Use up to 4 GiB memory and 2 vCPUs for builds. BUILD_GENERAL1_MEDIUM: Use up to 8 GiB memory and 4 vCPUs for builds. BUILD_GENERAL1_LARGE: Use up to 16 GiB memory and 8 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_XLARGE: Use up to 72 GiB memory and 36 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_2XLARGE: Use up to 144 GiB memory, 72 vCPUs, and 824 GB of SSD storage for builds. This compute type supports Docker images up to 100 GB uncompressed. BUILD_LAMBDA_1GB: Use up to 1 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_2GB: Use up to 2 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_4GB: Use up to 4 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_8GB: Use up to 8 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_10GB: Use up to 10 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. If you use BUILD_GENERAL1_SMALL: For environment type LINUX_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 16 GiB memory, 4 vCPUs, and 1 NVIDIA A10G Tensor Core GPU for builds. For environment type ARM_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs on ARM-based processors for builds. If you use BUILD_GENERAL1_LARGE: For environment type LINUX_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 255 GiB memory, 32 vCPUs, and 4 NVIDIA Tesla V100 GPUs for builds. For environment type ARM_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs on ARM-based processors for builds. For more information, see On-demand environment types in the CodeBuild User Guide."];
      computeConfiguration: ComputeConfiguration.t option
        [@ocaml.doc
          "The compute configuration of the build project. This is only required if computeType is set to ATTRIBUTE_BASED_COMPUTE."];
      fleet: ProjectFleet.t option
        [@ocaml.doc "A ProjectFleet object to use for this build project."];
      environmentVariables: EnvironmentVariables.t option
        [@ocaml.doc
          "A set of environment variables to make available to builds for this build project."];
      privilegedMode: WrapperBoolean.t option
        [@ocaml.doc
          "Enables running the Docker daemon inside a Docker container. Set to true only if the build project is used to build Docker images. Otherwise, a build that attempts to interact with the Docker daemon fails. The default setting is false. You can initialize the Docker daemon during the install phase of your build by adding one of the following sets of commands to the install phase of your buildspec file: If the operating system's base image is Ubuntu Linux: - nohup /usr/local/bin/dockerd --host=unix:///var/run/docker.sock --host=tcp://0.0.0.0:2375 --storage-driver=overlay& - timeout 15 sh -c \"until docker info; do echo .; sleep 1; done\" If the operating system's base image is Alpine Linux and the previous command does not work, add the -t argument to timeout: - nohup /usr/local/bin/dockerd --host=unix:///var/run/docker.sock --host=tcp://0.0.0.0:2375 --storage-driver=overlay& - timeout -t 15 sh -c \"until docker info; do echo .; sleep 1; done\""];
      certificate: String_.t option
        [@ocaml.doc
          "The ARN of the Amazon S3 bucket, path prefix, and object key that contains the PEM-encoded certificate for the build project. For more information, see certificate in the CodeBuild User Guide."];
      registryCredential: RegistryCredential.t option
        [@ocaml.doc "The credentials for access to a private registry."];
      imagePullCredentialsType: ImagePullCredentialsType.t option
        [@ocaml.doc
          "The type of credentials CodeBuild uses to pull images in your build. There are two valid values: CODEBUILD specifies that CodeBuild uses its own credentials. This requires that you modify your ECR repository policy to trust CodeBuild service principal. SERVICE_ROLE specifies that CodeBuild uses your build project's service role. When you use a cross-account or private registry image, you must use SERVICE_ROLE credentials. When you use an CodeBuild curated image, you must use CODEBUILD credentials."];
      dockerServer: DockerServer.t option
        [@ocaml.doc "A DockerServer object to use for this build project."]}
    let context_ = "ProjectEnvironment"
    let make ?computeConfiguration =
      fun ?fleet ->
        fun ?environmentVariables ->
          fun ?privilegedMode ->
            fun ?certificate ->
              fun ?registryCredential ->
                fun ?imagePullCredentialsType ->
                  fun ?dockerServer ->
                    fun ~type_ ->
                      fun ~image ->
                        fun ~computeType ->
                          fun () ->
                            {
                              computeConfiguration;
                              fleet;
                              environmentVariables;
                              privilegedMode;
                              certificate;
                              registryCredential;
                              imagePullCredentialsType;
                              dockerServer;
                              type_;
                              image;
                              computeType
                            }
    let to_value x =
      structure_to_value
        [("type", (Some (EnvironmentType.to_value x.type_)));
        ("image", (Some (NonEmptyString.to_value x.image)));
        ("computeType", (Some (ComputeType.to_value x.computeType)));
        ("computeConfiguration",
          (Option.map x.computeConfiguration ~f:ComputeConfiguration.to_value));
        ("fleet", (Option.map x.fleet ~f:ProjectFleet.to_value));
        ("environmentVariables",
          (Option.map x.environmentVariables ~f:EnvironmentVariables.to_value));
        ("privilegedMode",
          (Option.map x.privilegedMode ~f:WrapperBoolean.to_value));
        ("certificate", (Option.map x.certificate ~f:String_.to_value));
        ("registryCredential",
          (Option.map x.registryCredential ~f:RegistryCredential.to_value));
        ("imagePullCredentialsType",
          (Option.map x.imagePullCredentialsType
             ~f:ImagePullCredentialsType.to_value));
        ("dockerServer",
          (Option.map x.dockerServer ~f:DockerServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dockerServer =
        (Option.map ~f:DockerServer.of_xml)
          (Xml.child xml_arg0 "dockerServer") in
      let imagePullCredentialsType =
        (Option.map ~f:ImagePullCredentialsType.of_xml)
          (Xml.child xml_arg0 "imagePullCredentialsType") in
      let registryCredential =
        (Option.map ~f:RegistryCredential.of_xml)
          (Xml.child xml_arg0 "registryCredential") in
      let certificate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "certificate") in
      let privilegedMode =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "privilegedMode") in
      let environmentVariables =
        (Option.map ~f:EnvironmentVariables.of_xml)
          (Xml.child xml_arg0 "environmentVariables") in
      let fleet =
        (Option.map ~f:ProjectFleet.of_xml) (Xml.child xml_arg0 "fleet") in
      let computeConfiguration =
        (Option.map ~f:ComputeConfiguration.of_xml)
          (Xml.child xml_arg0 "computeConfiguration") in
      let computeType =
        ComputeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "computeType") in
      let image =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "image") in
      let type_ =
        EnvironmentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?dockerServer ?imagePullCredentialsType ?registryCredential
        ?certificate ?privilegedMode ?environmentVariables ?fleet
        ?computeConfiguration ~computeType ~image ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dockerServer = field_map json__ "dockerServer" DockerServer.of_json in
      let imagePullCredentialsType =
        field_map json__ "imagePullCredentialsType"
          ImagePullCredentialsType.of_json in
      let registryCredential =
        field_map json__ "registryCredential" RegistryCredential.of_json in
      let certificate = field_map json__ "certificate" String_.of_json in
      let privilegedMode =
        field_map json__ "privilegedMode" WrapperBoolean.of_json in
      let environmentVariables =
        field_map json__ "environmentVariables" EnvironmentVariables.of_json in
      let fleet = field_map json__ "fleet" ProjectFleet.of_json in
      let computeConfiguration =
        field_map json__ "computeConfiguration" ComputeConfiguration.of_json in
      let computeType =
        field_map_exn json__ "computeType" ComputeType.of_json in
      let image = field_map_exn json__ "image" NonEmptyString.of_json in
      let type_ = field_map_exn json__ "type" EnvironmentType.of_json in
      make ?dockerServer ?imagePullCredentialsType ?registryCredential
        ?certificate ?privilegedMode ?environmentVariables ?fleet
        ?computeConfiguration ~computeType ~image ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the build environment of the build project."]
module ProjectFileSystemLocations =
  struct
    type nonrec t = ProjectFileSystemLocation.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ProjectFileSystemLocation.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProjectFileSystemLocation.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectFileSystemLocations"
        ~of_json:ProjectFileSystemLocation.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProjectSecondarySourceVersions =
  struct
    type nonrec t = ProjectSourceVersion.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:12) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ProjectSourceVersion.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProjectSourceVersion.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectSecondarySourceVersions"
        ~of_json:ProjectSourceVersion.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProjectSources =
  struct
    type nonrec t = ProjectSource.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:12) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ProjectSource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProjectSource.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectSources" ~of_json:ProjectSource.of_json j
    let to_json v = composed_to_json to_value v
  end
module SandboxSession =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc "The ID of the sandbox session."];
      status: String_.t option
        [@ocaml.doc "The status of the sandbox session."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox session started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox session ended, expressed in Unix time format."];
      currentPhase: String_.t option
        [@ocaml.doc "The current phase for the sandbox."];
      phases: SandboxSessionPhases.t option
        [@ocaml.doc "An array of SandboxSessionPhase objects."];
      resolvedSourceVersion: NonEmptyString.t option
        [@ocaml.doc
          "An identifier for the version of this sandbox's source code."];
      logs: LogsLocation.t option ;
      networkInterface: NetworkInterface.t option }
    let make ?id =
      fun ?status ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?currentPhase ->
              fun ?phases ->
                fun ?resolvedSourceVersion ->
                  fun ?logs ->
                    fun ?networkInterface ->
                      fun () ->
                        {
                          id;
                          status;
                          startTime;
                          endTime;
                          currentPhase;
                          phases;
                          resolvedSourceVersion;
                          logs;
                          networkInterface
                        }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("currentPhase", (Option.map x.currentPhase ~f:String_.to_value));
        ("phases", (Option.map x.phases ~f:SandboxSessionPhases.to_value));
        ("resolvedSourceVersion",
          (Option.map x.resolvedSourceVersion ~f:NonEmptyString.to_value));
        ("logs", (Option.map x.logs ~f:LogsLocation.to_value));
        ("networkInterface",
          (Option.map x.networkInterface ~f:NetworkInterface.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterface =
        (Option.map ~f:NetworkInterface.of_xml)
          (Xml.child xml_arg0 "networkInterface") in
      let logs =
        (Option.map ~f:LogsLocation.of_xml) (Xml.child xml_arg0 "logs") in
      let resolvedSourceVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "resolvedSourceVersion") in
      let phases =
        (Option.map ~f:SandboxSessionPhases.of_xml)
          (Xml.child xml_arg0 "phases") in
      let currentPhase =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currentPhase") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?networkInterface ?logs ?resolvedSourceVersion ?phases
        ?currentPhase ?endTime ?startTime ?status ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkInterface =
        field_map json__ "networkInterface" NetworkInterface.of_json in
      let logs = field_map json__ "logs" LogsLocation.of_json in
      let resolvedSourceVersion =
        field_map json__ "resolvedSourceVersion" NonEmptyString.of_json in
      let phases = field_map json__ "phases" SandboxSessionPhases.of_json in
      let currentPhase = field_map json__ "currentPhase" String_.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let status = field_map json__ "status" String_.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?networkInterface ?logs ?resolvedSourceVersion ?phases
        ?currentPhase ?endTime ?startTime ?status ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about the sandbox session."]
module VpcConfig =
  struct
    type nonrec t =
      {
      vpcId: NonEmptyString.t option [@ocaml.doc "The ID of the Amazon VPC."];
      subnets: Subnets.t option
        [@ocaml.doc "A list of one or more subnet IDs in your Amazon VPC."];
      securityGroupIds: SecurityGroupIds.t option
        [@ocaml.doc
          "A list of one or more security groups IDs in your Amazon VPC."]}
    let make ?vpcId =
      fun ?subnets ->
        fun ?securityGroupIds ->
          fun () -> { vpcId; subnets; securityGroupIds }
    let to_value x =
      structure_to_value
        [("vpcId", (Option.map x.vpcId ~f:NonEmptyString.to_value));
        ("subnets", (Option.map x.subnets ~f:Subnets.to_value));
        ("securityGroupIds",
          (Option.map x.securityGroupIds ~f:SecurityGroupIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIds.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      let subnets =
        (Option.map ~f:Subnets.of_xml) (Xml.child xml_arg0 "subnets") in
      let vpcId =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "vpcId") in
      make ?securityGroupIds ?subnets ?vpcId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupIds =
        field_map json__ "securityGroupIds" SecurityGroupIds.of_json in
      let subnets = field_map json__ "subnets" Subnets.of_json in
      let vpcId = field_map json__ "vpcId" NonEmptyString.of_json in
      make ?securityGroupIds ?subnets ?vpcId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the VPC configuration that CodeBuild accesses."]
module CodeCoverageReportSummary =
  struct
    type nonrec t =
      {
      lineCoveragePercentage: Percentage.t option
        [@ocaml.doc
          "The percentage of lines that are covered by your tests."];
      linesCovered: NonNegativeInt.t option
        [@ocaml.doc "The number of lines that are covered by your tests."];
      linesMissed: NonNegativeInt.t option
        [@ocaml.doc
          "The number of lines that are not covered by your tests."];
      branchCoveragePercentage: Percentage.t option
        [@ocaml.doc
          "The percentage of branches that are covered by your tests."];
      branchesCovered: NonNegativeInt.t option
        [@ocaml.doc
          "The number of conditional branches that are covered by your tests."];
      branchesMissed: NonNegativeInt.t option
        [@ocaml.doc
          "The number of conditional branches that are not covered by your tests."]}
    let make ?lineCoveragePercentage =
      fun ?linesCovered ->
        fun ?linesMissed ->
          fun ?branchCoveragePercentage ->
            fun ?branchesCovered ->
              fun ?branchesMissed ->
                fun () ->
                  {
                    lineCoveragePercentage;
                    linesCovered;
                    linesMissed;
                    branchCoveragePercentage;
                    branchesCovered;
                    branchesMissed
                  }
    let to_value x =
      structure_to_value
        [("lineCoveragePercentage",
           (Option.map x.lineCoveragePercentage ~f:Percentage.to_value));
        ("linesCovered",
          (Option.map x.linesCovered ~f:NonNegativeInt.to_value));
        ("linesMissed",
          (Option.map x.linesMissed ~f:NonNegativeInt.to_value));
        ("branchCoveragePercentage",
          (Option.map x.branchCoveragePercentage ~f:Percentage.to_value));
        ("branchesCovered",
          (Option.map x.branchesCovered ~f:NonNegativeInt.to_value));
        ("branchesMissed",
          (Option.map x.branchesMissed ~f:NonNegativeInt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let branchesMissed =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "branchesMissed") in
      let branchesCovered =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "branchesCovered") in
      let branchCoveragePercentage =
        (Option.map ~f:Percentage.of_xml)
          (Xml.child xml_arg0 "branchCoveragePercentage") in
      let linesMissed =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "linesMissed") in
      let linesCovered =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "linesCovered") in
      let lineCoveragePercentage =
        (Option.map ~f:Percentage.of_xml)
          (Xml.child xml_arg0 "lineCoveragePercentage") in
      make ?branchesMissed ?branchesCovered ?branchCoveragePercentage
        ?linesMissed ?linesCovered ?lineCoveragePercentage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let branchesMissed =
        field_map json__ "branchesMissed" NonNegativeInt.of_json in
      let branchesCovered =
        field_map json__ "branchesCovered" NonNegativeInt.of_json in
      let branchCoveragePercentage =
        field_map json__ "branchCoveragePercentage" Percentage.of_json in
      let linesMissed = field_map json__ "linesMissed" NonNegativeInt.of_json in
      let linesCovered =
        field_map json__ "linesCovered" NonNegativeInt.of_json in
      let lineCoveragePercentage =
        field_map json__ "lineCoveragePercentage" Percentage.of_json in
      make ?branchesMissed ?branchesCovered ?branchCoveragePercentage
        ?linesMissed ?linesCovered ?lineCoveragePercentage ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains a summary of a code coverage report. Line coverage measures how many statements your tests cover. A statement is a single instruction, not including comments, conditionals, etc. Branch coverage determines if your tests cover every possible branch of a control structure, such as an if or case statement."]
module ReportExportConfig =
  struct
    type nonrec t =
      {
      exportConfigType: ReportExportConfigType.t option
        [@ocaml.doc
          "The export configuration type. Valid values are: S3: The report results are exported to an S3 bucket. NO_EXPORT: The report results are not exported."];
      s3Destination: S3ReportExportConfig.t option
        [@ocaml.doc
          "A S3ReportExportConfig object that contains information about the S3 bucket where the run of a report is exported."]}
    let make ?exportConfigType =
      fun ?s3Destination -> fun () -> { exportConfigType; s3Destination }
    let to_value x =
      structure_to_value
        [("exportConfigType",
           (Option.map x.exportConfigType ~f:ReportExportConfigType.to_value));
        ("s3Destination",
          (Option.map x.s3Destination ~f:S3ReportExportConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Destination =
        (Option.map ~f:S3ReportExportConfig.of_xml)
          (Xml.child xml_arg0 "s3Destination") in
      let exportConfigType =
        (Option.map ~f:ReportExportConfigType.of_xml)
          (Xml.child xml_arg0 "exportConfigType") in
      make ?s3Destination ?exportConfigType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Destination =
        field_map json__ "s3Destination" S3ReportExportConfig.of_json in
      let exportConfigType =
        field_map json__ "exportConfigType" ReportExportConfigType.of_json in
      make ?s3Destination ?exportConfigType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the location where the run of a report is exported."]
module ReportStatusType =
  struct
    type nonrec t =
      | GENERATING 
      | SUCCEEDED 
      | FAILED 
      | INCOMPLETE 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GENERATING -> "GENERATING"
      | SUCCEEDED -> "SUCCEEDED"
      | FAILED -> "FAILED"
      | INCOMPLETE -> "INCOMPLETE"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "GENERATING" -> GENERATING
      | "SUCCEEDED" -> SUCCEEDED
      | "FAILED" -> FAILED
      | "INCOMPLETE" -> INCOMPLETE
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ReportStatusType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ReportStatusType" j)
    let to_json = simple_to_json to_value
  end
module ReportType =
  struct
    type nonrec t =
      | TEST 
      | CODE_COVERAGE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TEST -> "TEST"
      | CODE_COVERAGE -> "CODE_COVERAGE"
      | Non_static_id s -> s
    let of_string =
      function
      | "TEST" -> TEST
      | "CODE_COVERAGE" -> CODE_COVERAGE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ReportType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ReportType" j)
    let to_json = simple_to_json to_value
  end
module TestReportSummary =
  struct
    type nonrec t =
      {
      total: WrapperInt.t option
        [@ocaml.doc
          "The number of test cases in this TestReportSummary. The total includes truncated test cases."];
      statusCounts: ReportStatusCounts.t option
        [@ocaml.doc
          "A map that contains the number of each type of status returned by the test results in this TestReportSummary."];
      durationInNanoSeconds: WrapperLong.t option
        [@ocaml.doc
          "The number of nanoseconds it took to run all of the test cases in this report."]}
    let make ?total =
      fun ?statusCounts ->
        fun ?durationInNanoSeconds ->
          fun () -> { total; statusCounts; durationInNanoSeconds }
    let to_value x =
      structure_to_value
        [("total", (Option.map x.total ~f:WrapperInt.to_value));
        ("statusCounts",
          (Option.map x.statusCounts ~f:ReportStatusCounts.to_value));
        ("durationInNanoSeconds",
          (Option.map x.durationInNanoSeconds ~f:WrapperLong.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let durationInNanoSeconds =
        (Option.map ~f:WrapperLong.of_xml)
          (Xml.child xml_arg0 "durationInNanoSeconds") in
      let statusCounts =
        (Option.map ~f:ReportStatusCounts.of_xml)
          (Xml.child xml_arg0 "statusCounts") in
      let total =
        (Option.map ~f:WrapperInt.of_xml) (Xml.child xml_arg0 "total") in
      make ?durationInNanoSeconds ?statusCounts ?total ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let durationInNanoSeconds =
        field_map json__ "durationInNanoSeconds" WrapperLong.of_json in
      let statusCounts =
        field_map json__ "statusCounts" ReportStatusCounts.of_json in
      let total = field_map json__ "total" WrapperInt.of_json in
      make ?durationInNanoSeconds ?statusCounts ?total ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a test report."]
module ReportGroupName =
  struct
    type nonrec t = string
    let context_ = "ReportGroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:2));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReportGroupName" j
    let to_json = simple_to_json to_value
  end
module ReportGroupStatusType =
  struct
    type nonrec t =
      | ACTIVE 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReportGroupStatusType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ReportGroupStatusType" j)
    let to_json = simple_to_json to_value
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildTimeOut =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:2160) >>= (fun () -> check_int_min i ~min:5));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BuildTimeOut" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ProjectArtifactsList =
  struct
    type nonrec t = ProjectArtifacts.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:12) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ProjectArtifacts.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProjectArtifacts.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectArtifactsList"
        ~of_json:ProjectArtifacts.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProjectBadge =
  struct
    type nonrec t =
      {
      badgeEnabled: Boolean.t option
        [@ocaml.doc
          "Set this to true to generate a publicly accessible URL for your project's build badge."];
      badgeRequestUrl: String_.t option
        [@ocaml.doc
          "The publicly-accessible URL through which you can access the build badge for your project."]}
    let make ?badgeEnabled =
      fun ?badgeRequestUrl -> fun () -> { badgeEnabled; badgeRequestUrl }
    let to_value x =
      structure_to_value
        [("badgeEnabled", (Option.map x.badgeEnabled ~f:Boolean.to_value));
        ("badgeRequestUrl",
          (Option.map x.badgeRequestUrl ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let badgeRequestUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "badgeRequestUrl") in
      let badgeEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "badgeEnabled") in
      make ?badgeRequestUrl ?badgeEnabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let badgeRequestUrl =
        field_map json__ "badgeRequestUrl" String_.of_json in
      let badgeEnabled = field_map json__ "badgeEnabled" Boolean.of_json in
      make ?badgeRequestUrl ?badgeEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the build badge for the build project."]
module ProjectBuildBatchConfig =
  struct
    type nonrec t =
      {
      serviceRole: NonEmptyString.t option
        [@ocaml.doc
          "Specifies the service role ARN for the batch build project."];
      combineArtifacts: WrapperBoolean.t option
        [@ocaml.doc
          "Specifies if the build artifacts for the batch build should be combined into a single artifact location."];
      restrictions: BatchRestrictions.t option
        [@ocaml.doc
          "A BatchRestrictions object that specifies the restrictions for the batch build."];
      timeoutInMins: WrapperInt.t option
        [@ocaml.doc
          "Specifies the maximum amount of time, in minutes, that the batch build must be completed in."];
      batchReportMode: BatchReportModeType.t option
        [@ocaml.doc
          "Specifies how build status reports are sent to the source provider for the batch build. This property is only used when the source provider for your project is Bitbucket, GitHub, or GitHub Enterprise, and your project is configured to report build statuses to the source provider. REPORT_AGGREGATED_BATCH (Default) Aggregate all of the build statuses into a single status report. REPORT_INDIVIDUAL_BUILDS Send a separate status report for each individual build."]}
    let make ?serviceRole =
      fun ?combineArtifacts ->
        fun ?restrictions ->
          fun ?timeoutInMins ->
            fun ?batchReportMode ->
              fun () ->
                {
                  serviceRole;
                  combineArtifacts;
                  restrictions;
                  timeoutInMins;
                  batchReportMode
                }
    let to_value x =
      structure_to_value
        [("serviceRole",
           (Option.map x.serviceRole ~f:NonEmptyString.to_value));
        ("combineArtifacts",
          (Option.map x.combineArtifacts ~f:WrapperBoolean.to_value));
        ("restrictions",
          (Option.map x.restrictions ~f:BatchRestrictions.to_value));
        ("timeoutInMins",
          (Option.map x.timeoutInMins ~f:WrapperInt.to_value));
        ("batchReportMode",
          (Option.map x.batchReportMode ~f:BatchReportModeType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchReportMode =
        (Option.map ~f:BatchReportModeType.of_xml)
          (Xml.child xml_arg0 "batchReportMode") in
      let timeoutInMins =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "timeoutInMins") in
      let restrictions =
        (Option.map ~f:BatchRestrictions.of_xml)
          (Xml.child xml_arg0 "restrictions") in
      let combineArtifacts =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "combineArtifacts") in
      let serviceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRole") in
      make ?batchReportMode ?timeoutInMins ?restrictions ?combineArtifacts
        ?serviceRole ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let batchReportMode =
        field_map json__ "batchReportMode" BatchReportModeType.of_json in
      let timeoutInMins = field_map json__ "timeoutInMins" WrapperInt.of_json in
      let restrictions =
        field_map json__ "restrictions" BatchRestrictions.of_json in
      let combineArtifacts =
        field_map json__ "combineArtifacts" WrapperBoolean.of_json in
      let serviceRole = field_map json__ "serviceRole" NonEmptyString.of_json in
      make ?batchReportMode ?timeoutInMins ?restrictions ?combineArtifacts
        ?serviceRole ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration information about a batch build project."]
module ProjectCache =
  struct
    type nonrec t =
      {
      type_: CacheType.t
        [@ocaml.doc
          "The type of cache used by the build project. Valid values include: NO_CACHE: The build project does not use any cache. S3: The build project reads and writes from and to S3. LOCAL: The build project stores a cache locally on a build host that is only available to that build host."];
      location: String_.t option
        [@ocaml.doc
          "Information about the cache location: NO_CACHE or LOCAL: This value is ignored. S3: This is the S3 bucket name/prefix."];
      modes: ProjectCacheModes.t option
        [@ocaml.doc
          "An array of strings that specify the local cache modes. You can use one or more local cache modes at the same time. This is only used for LOCAL cache types. Possible values are: LOCAL_SOURCE_CACHE Caches Git metadata for primary and secondary sources. After the cache is created, subsequent builds pull only the change between commits. This mode is a good choice for projects with a clean working directory and a source that is a large Git repository. If you choose this option and your project does not use a Git repository (GitHub, GitHub Enterprise, or Bitbucket), the option is ignored. LOCAL_DOCKER_LAYER_CACHE Caches existing Docker layers. This mode is a good choice for projects that build or pull large Docker images. It can prevent the performance issues caused by pulling large Docker images down from the network. You can use a Docker layer cache in the Linux environment only. The privileged flag must be set so that your project has the required Docker permissions. You should consider the security implications before you use a Docker layer cache. LOCAL_CUSTOM_CACHE Caches directories you specify in the buildspec file. This mode is a good choice if your build scenario is not suited to one of the other three local cache modes. If you use a custom cache: Only directories can be specified for caching. You cannot specify individual files. Symlinks are used to reference cached directories. Cached directories are linked to your build before it downloads its project sources. Cached items are overridden if a source item has the same name. Directories are specified using cache paths in the buildspec file."];
      cacheNamespace: String_.t option
        [@ocaml.doc
          "Defines the scope of the cache. You can use this namespace to share a cache across multiple projects. For more information, see Cache sharing between projects in the CodeBuild User Guide."]}
    let context_ = "ProjectCache"
    let make ?location =
      fun ?modes ->
        fun ?cacheNamespace ->
          fun ~type_ -> fun () -> { location; modes; cacheNamespace; type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (CacheType.to_value x.type_)));
        ("location", (Option.map x.location ~f:String_.to_value));
        ("modes", (Option.map x.modes ~f:ProjectCacheModes.to_value));
        ("cacheNamespace", (Option.map x.cacheNamespace ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cacheNamespace =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cacheNamespace") in
      let modes =
        (Option.map ~f:ProjectCacheModes.of_xml) (Xml.child xml_arg0 "modes") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "location") in
      let type_ =
        CacheType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?cacheNamespace ?modes ?location ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cacheNamespace = field_map json__ "cacheNamespace" String_.of_json in
      let modes = field_map json__ "modes" ProjectCacheModes.of_json in
      let location = field_map json__ "location" String_.of_json in
      let type_ = field_map_exn json__ "type" CacheType.of_json in
      make ?cacheNamespace ?modes ?location ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the cache for the build project."]
module ProjectDescription =
  struct
    type nonrec t = string
    let context_ = "ProjectDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProjectDescription" j
    let to_json = simple_to_json to_value
  end
module ProjectName =
  struct
    type nonrec t = string
    let context_ = "ProjectName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:150) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[A-Za-z0-9][A-Za-z0-9\\-_]{1,149}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProjectName" j
    let to_json = simple_to_json to_value
  end
module ProjectVisibilityType =
  struct
    type nonrec t =
      | PUBLIC_READ 
      | PRIVATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PUBLIC_READ -> "PUBLIC_READ"
      | PRIVATE -> "PRIVATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "PUBLIC_READ" -> PUBLIC_READ
      | "PRIVATE" -> PRIVATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProjectVisibilityType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProjectVisibilityType" j)
    let to_json = simple_to_json to_value
  end
module TimeOut =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:480) >>= (fun () -> check_int_min i ~min:5));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for TimeOut" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Webhook =
  struct
    type nonrec t =
      {
      url: NonEmptyString.t option [@ocaml.doc "The URL to the webhook."];
      payloadUrl: NonEmptyString.t option
        [@ocaml.doc "The CodeBuild endpoint where webhook events are sent."];
      secret: NonEmptyString.t option
        [@ocaml.doc
          "The secret token of the associated repository. A Bitbucket webhook does not support secret."];
      branchFilter: String_.t option
        [@ocaml.doc
          "A regular expression used to determine which repository branches are built when a webhook is triggered. If the name of a branch matches the regular expression, then it is built. If branchFilter is empty, then all branches are built. It is recommended that you use filterGroups instead of branchFilter."];
      filterGroups: FilterGroups.t option
        [@ocaml.doc
          "An array of arrays of WebhookFilter objects used to determine which webhooks are triggered. At least one WebhookFilter in the array must specify EVENT as its type. For a build to be triggered, at least one filter group in the filterGroups array must pass. For a filter group to pass, each of its filters must pass."];
      buildType: WebhookBuildType.t option
        [@ocaml.doc
          "Specifies the type of build this webhook will trigger. RUNNER_BUILDKITE_BUILD is only available for NO_SOURCE source type projects configured for Buildkite runner builds. For more information about CodeBuild-hosted Buildkite runner builds, see Tutorial: Configure a CodeBuild-hosted Buildkite runner in the CodeBuild user guide."];
      manualCreation: WrapperBoolean.t option
        [@ocaml.doc
          "If manualCreation is true, CodeBuild doesn't create a webhook in GitHub and instead returns payloadUrl and secret values for the webhook. The payloadUrl and secret values in the output can be used to manually create a webhook within GitHub. manualCreation is only available for GitHub webhooks."];
      lastModifiedSecret: Timestamp.t option
        [@ocaml.doc
          "A timestamp that indicates the last time a repository's secret token was modified."];
      scopeConfiguration: ScopeConfiguration.t option
        [@ocaml.doc
          "The scope configuration for global or organization webhooks. Global or organization webhooks are only available for GitHub and Github Enterprise webhooks."];
      status: WebhookStatus.t option
        [@ocaml.doc
          "The status of the webhook. Valid values include: CREATING: The webhook is being created. CREATE_FAILED: The webhook has failed to create. ACTIVE: The webhook has succeeded and is active. DELETING: The webhook is being deleted."];
      statusMessage: String_.t option
        [@ocaml.doc "A message associated with the status of a webhook."];
      pullRequestBuildPolicy: PullRequestBuildPolicy.t option }
    let make ?url =
      fun ?payloadUrl ->
        fun ?secret ->
          fun ?branchFilter ->
            fun ?filterGroups ->
              fun ?buildType ->
                fun ?manualCreation ->
                  fun ?lastModifiedSecret ->
                    fun ?scopeConfiguration ->
                      fun ?status ->
                        fun ?statusMessage ->
                          fun ?pullRequestBuildPolicy ->
                            fun () ->
                              {
                                url;
                                payloadUrl;
                                secret;
                                branchFilter;
                                filterGroups;
                                buildType;
                                manualCreation;
                                lastModifiedSecret;
                                scopeConfiguration;
                                status;
                                statusMessage;
                                pullRequestBuildPolicy
                              }
    let to_value x =
      structure_to_value
        [("url", (Option.map x.url ~f:NonEmptyString.to_value));
        ("payloadUrl", (Option.map x.payloadUrl ~f:NonEmptyString.to_value));
        ("secret", (Option.map x.secret ~f:NonEmptyString.to_value));
        ("branchFilter", (Option.map x.branchFilter ~f:String_.to_value));
        ("filterGroups",
          (Option.map x.filterGroups ~f:FilterGroups.to_value));
        ("buildType", (Option.map x.buildType ~f:WebhookBuildType.to_value));
        ("manualCreation",
          (Option.map x.manualCreation ~f:WrapperBoolean.to_value));
        ("lastModifiedSecret",
          (Option.map x.lastModifiedSecret ~f:Timestamp.to_value));
        ("scopeConfiguration",
          (Option.map x.scopeConfiguration ~f:ScopeConfiguration.to_value));
        ("status", (Option.map x.status ~f:WebhookStatus.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("pullRequestBuildPolicy",
          (Option.map x.pullRequestBuildPolicy
             ~f:PullRequestBuildPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pullRequestBuildPolicy =
        (Option.map ~f:PullRequestBuildPolicy.of_xml)
          (Xml.child xml_arg0 "pullRequestBuildPolicy") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:WebhookStatus.of_xml) (Xml.child xml_arg0 "status") in
      let scopeConfiguration =
        (Option.map ~f:ScopeConfiguration.of_xml)
          (Xml.child xml_arg0 "scopeConfiguration") in
      let lastModifiedSecret =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedSecret") in
      let manualCreation =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "manualCreation") in
      let buildType =
        (Option.map ~f:WebhookBuildType.of_xml)
          (Xml.child xml_arg0 "buildType") in
      let filterGroups =
        (Option.map ~f:FilterGroups.of_xml)
          (Xml.child xml_arg0 "filterGroups") in
      let branchFilter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "branchFilter") in
      let secret =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "secret") in
      let payloadUrl =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "payloadUrl") in
      let url =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "url") in
      make ?pullRequestBuildPolicy ?statusMessage ?status ?scopeConfiguration
        ?lastModifiedSecret ?manualCreation ?buildType ?filterGroups
        ?branchFilter ?secret ?payloadUrl ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pullRequestBuildPolicy =
        field_map json__ "pullRequestBuildPolicy"
          PullRequestBuildPolicy.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status = field_map json__ "status" WebhookStatus.of_json in
      let scopeConfiguration =
        field_map json__ "scopeConfiguration" ScopeConfiguration.of_json in
      let lastModifiedSecret =
        field_map json__ "lastModifiedSecret" Timestamp.of_json in
      let manualCreation =
        field_map json__ "manualCreation" WrapperBoolean.of_json in
      let buildType = field_map json__ "buildType" WebhookBuildType.of_json in
      let filterGroups = field_map json__ "filterGroups" FilterGroups.of_json in
      let branchFilter = field_map json__ "branchFilter" String_.of_json in
      let secret = field_map json__ "secret" NonEmptyString.of_json in
      let payloadUrl = field_map json__ "payloadUrl" NonEmptyString.of_json in
      let url = field_map json__ "url" NonEmptyString.of_json in
      make ?pullRequestBuildPolicy ?statusMessage ?status ?scopeConfiguration
        ?lastModifiedSecret ?manualCreation ?buildType ?filterGroups
        ?branchFilter ?secret ?payloadUrl ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a webhook that connects repository events to a build project in CodeBuild."]
module FleetName =
  struct
    type nonrec t = string
    let context_ = "FleetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[A-Za-z0-9][A-Za-z0-9\\-_]{1,127}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FleetName" j
    let to_json = simple_to_json to_value
  end
module FleetOverflowBehavior =
  struct
    type nonrec t =
      | QUEUE 
      | ON_DEMAND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | QUEUE -> "QUEUE"
      | ON_DEMAND -> "ON_DEMAND"
      | Non_static_id s -> s
    let of_string =
      function
      | "QUEUE" -> QUEUE
      | "ON_DEMAND" -> ON_DEMAND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FleetOverflowBehavior" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FleetOverflowBehavior" j)
    let to_json = simple_to_json to_value
  end
module FleetStatus =
  struct
    type nonrec t =
      {
      statusCode: FleetStatusCode.t option
        [@ocaml.doc
          "The status code of the compute fleet. Valid values include: CREATING: The compute fleet is being created. UPDATING: The compute fleet is being updated. ROTATING: The compute fleet is being rotated. PENDING_DELETION: The compute fleet is pending deletion. DELETING: The compute fleet is being deleted. CREATE_FAILED: The compute fleet has failed to create. UPDATE_ROLLBACK_FAILED: The compute fleet has failed to update and could not rollback to previous state. ACTIVE: The compute fleet has succeeded and is active."];
      context: FleetContextCode.t option
        [@ocaml.doc
          "Additional information about a compute fleet. Valid values include: CREATE_FAILED: The compute fleet has failed to create. UPDATE_FAILED: The compute fleet has failed to update."];
      message: String_.t option
        [@ocaml.doc
          "A message associated with the status of a compute fleet."]}
    let make ?statusCode =
      fun ?context ->
        fun ?message -> fun () -> { statusCode; context; message }
    let to_value x =
      structure_to_value
        [("statusCode",
           (Option.map x.statusCode ~f:FleetStatusCode.to_value));
        ("context", (Option.map x.context ~f:FleetContextCode.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let context =
        (Option.map ~f:FleetContextCode.of_xml)
          (Xml.child xml_arg0 "context") in
      let statusCode =
        (Option.map ~f:FleetStatusCode.of_xml)
          (Xml.child xml_arg0 "statusCode") in
      make ?message ?context ?statusCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let context = field_map json__ "context" FleetContextCode.of_json in
      let statusCode = field_map json__ "statusCode" FleetStatusCode.of_json in
      make ?message ?context ?statusCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The status of the compute fleet."]
module ProxyConfiguration =
  struct
    type nonrec t =
      {
      defaultBehavior: FleetProxyRuleBehavior.t option
        [@ocaml.doc "The default behavior of outgoing traffic."];
      orderedProxyRules: FleetProxyRules.t option
        [@ocaml.doc
          "An array of FleetProxyRule objects that represent the specified destination domains or IPs to allow or deny network access control to."]}
    let make ?defaultBehavior =
      fun ?orderedProxyRules ->
        fun () -> { defaultBehavior; orderedProxyRules }
    let to_value x =
      structure_to_value
        [("defaultBehavior",
           (Option.map x.defaultBehavior ~f:FleetProxyRuleBehavior.to_value));
        ("orderedProxyRules",
          (Option.map x.orderedProxyRules ~f:FleetProxyRules.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let orderedProxyRules =
        (Option.map ~f:FleetProxyRules.of_xml)
          (Xml.child xml_arg0 "orderedProxyRules") in
      let defaultBehavior =
        (Option.map ~f:FleetProxyRuleBehavior.of_xml)
          (Xml.child xml_arg0 "defaultBehavior") in
      make ?orderedProxyRules ?defaultBehavior ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let orderedProxyRules =
        field_map json__ "orderedProxyRules" FleetProxyRules.of_json in
      let defaultBehavior =
        field_map json__ "defaultBehavior" FleetProxyRuleBehavior.of_json in
      make ?orderedProxyRules ?defaultBehavior ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the proxy configurations that apply network access control to your reserved capacity instances."]
module ScalingConfigurationOutput =
  struct
    type nonrec t =
      {
      scalingType: FleetScalingType.t option
        [@ocaml.doc "The scaling type for a compute fleet."];
      targetTrackingScalingConfigs:
        TargetTrackingScalingConfigurations.t option
        [@ocaml.doc "A list of TargetTrackingScalingConfiguration objects."];
      maxCapacity: FleetCapacity.t option
        [@ocaml.doc
          "The maximum number of instances in the \239\172\130eet when auto-scaling."];
      desiredCapacity: FleetCapacity.t option
        [@ocaml.doc
          "The desired number of instances in the \239\172\130eet when auto-scaling."]}
    let make ?scalingType =
      fun ?targetTrackingScalingConfigs ->
        fun ?maxCapacity ->
          fun ?desiredCapacity ->
            fun () ->
              {
                scalingType;
                targetTrackingScalingConfigs;
                maxCapacity;
                desiredCapacity
              }
    let to_value x =
      structure_to_value
        [("scalingType",
           (Option.map x.scalingType ~f:FleetScalingType.to_value));
        ("targetTrackingScalingConfigs",
          (Option.map x.targetTrackingScalingConfigs
             ~f:TargetTrackingScalingConfigurations.to_value));
        ("maxCapacity", (Option.map x.maxCapacity ~f:FleetCapacity.to_value));
        ("desiredCapacity",
          (Option.map x.desiredCapacity ~f:FleetCapacity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let desiredCapacity =
        (Option.map ~f:FleetCapacity.of_xml)
          (Xml.child xml_arg0 "desiredCapacity") in
      let maxCapacity =
        (Option.map ~f:FleetCapacity.of_xml)
          (Xml.child xml_arg0 "maxCapacity") in
      let targetTrackingScalingConfigs =
        (Option.map ~f:TargetTrackingScalingConfigurations.of_xml)
          (Xml.child xml_arg0 "targetTrackingScalingConfigs") in
      let scalingType =
        (Option.map ~f:FleetScalingType.of_xml)
          (Xml.child xml_arg0 "scalingType") in
      make ?desiredCapacity ?maxCapacity ?targetTrackingScalingConfigs
        ?scalingType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let desiredCapacity =
        field_map json__ "desiredCapacity" FleetCapacity.of_json in
      let maxCapacity = field_map json__ "maxCapacity" FleetCapacity.of_json in
      let targetTrackingScalingConfigs =
        field_map json__ "targetTrackingScalingConfigs"
          TargetTrackingScalingConfigurations.of_json in
      let scalingType =
        field_map json__ "scalingType" FleetScalingType.of_json in
      make ?desiredCapacity ?maxCapacity ?targetTrackingScalingConfigs
        ?scalingType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The scaling configuration output of a compute fleet."]
module AutoRetryConfig =
  struct
    type nonrec t =
      {
      autoRetryLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of additional automatic retries after a failed build. For example, if the auto-retry limit is set to 2, CodeBuild will call the RetryBuild API to automatically retry your build for up to 2 additional times."];
      autoRetryNumber: WrapperInt.t option
        [@ocaml.doc
          "The number of times that the build has been retried. The initial build will have an auto-retry number of 0."];
      nextAutoRetry: String_.t option
        [@ocaml.doc
          "The build ARN of the auto-retried build triggered by the current build. The next auto-retry will be null for builds that don't trigger an auto-retry."];
      previousAutoRetry: String_.t option
        [@ocaml.doc
          "The build ARN of the build that triggered the current auto-retry build. The previous auto-retry will be null for the initial build."]}
    let make ?autoRetryLimit =
      fun ?autoRetryNumber ->
        fun ?nextAutoRetry ->
          fun ?previousAutoRetry ->
            fun () ->
              {
                autoRetryLimit;
                autoRetryNumber;
                nextAutoRetry;
                previousAutoRetry
              }
    let to_value x =
      structure_to_value
        [("autoRetryLimit",
           (Option.map x.autoRetryLimit ~f:WrapperInt.to_value));
        ("autoRetryNumber",
          (Option.map x.autoRetryNumber ~f:WrapperInt.to_value));
        ("nextAutoRetry", (Option.map x.nextAutoRetry ~f:String_.to_value));
        ("previousAutoRetry",
          (Option.map x.previousAutoRetry ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let previousAutoRetry =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "previousAutoRetry") in
      let nextAutoRetry =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextAutoRetry") in
      let autoRetryNumber =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "autoRetryNumber") in
      let autoRetryLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "autoRetryLimit") in
      make ?previousAutoRetry ?nextAutoRetry ?autoRetryNumber ?autoRetryLimit
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let previousAutoRetry =
        field_map json__ "previousAutoRetry" String_.of_json in
      let nextAutoRetry = field_map json__ "nextAutoRetry" String_.of_json in
      let autoRetryNumber =
        field_map json__ "autoRetryNumber" WrapperInt.of_json in
      let autoRetryLimit =
        field_map json__ "autoRetryLimit" WrapperInt.of_json in
      make ?previousAutoRetry ?nextAutoRetry ?autoRetryNumber ?autoRetryLimit
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the auto-retry configuration for the build."]
module BuildArtifactsList =
  struct
    type nonrec t = BuildArtifacts.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:12) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildArtifacts.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildArtifacts.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildArtifactsList" ~of_json:BuildArtifacts.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module BuildPhases =
  struct
    type nonrec t = BuildPhase.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildPhase.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildPhase.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildPhases" ~of_json:BuildPhase.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildReportArns =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildReportArns" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module DebugSession =
  struct
    type nonrec t =
      {
      sessionEnabled: WrapperBoolean.t option
        [@ocaml.doc
          "Specifies if session debugging is enabled for this build."];
      sessionTarget: NonEmptyString.t option
        [@ocaml.doc
          "Contains the identifier of the Session Manager session used for the build. To work with the paused build, you open this session to examine, control, and resume the build."]}
    let make ?sessionEnabled =
      fun ?sessionTarget -> fun () -> { sessionEnabled; sessionTarget }
    let to_value x =
      structure_to_value
        [("sessionEnabled",
           (Option.map x.sessionEnabled ~f:WrapperBoolean.to_value));
        ("sessionTarget",
          (Option.map x.sessionTarget ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionTarget =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "sessionTarget") in
      let sessionEnabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "sessionEnabled") in
      make ?sessionTarget ?sessionEnabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionTarget =
        field_map json__ "sessionTarget" NonEmptyString.of_json in
      let sessionEnabled =
        field_map json__ "sessionEnabled" WrapperBoolean.of_json in
      make ?sessionTarget ?sessionEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the debug session for a build. For more information, see Viewing a running build in Session Manager."]
module ExportedEnvironmentVariables =
  struct
    type nonrec t = ExportedEnvironmentVariable.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExportedEnvironmentVariable.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExportedEnvironmentVariable.of_xml)
    let of_json j =
      list_of_json ~kind:"ExportedEnvironmentVariables"
        ~of_json:ExportedEnvironmentVariable.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildBatchPhases =
  struct
    type nonrec t = BuildBatchPhase.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildBatchPhase.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildBatchPhase.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildBatchPhases" ~of_json:BuildBatchPhase.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module BuildGroups =
  struct
    type nonrec t = BuildGroup.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildGroups" ~of_json:BuildGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module SourceCredentialsInfo =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the token."];
      serverType: ServerType.t option
        [@ocaml.doc
          "The type of source provider. The valid options are GITHUB, GITHUB_ENTERPRISE, GITLAB, GITLAB_SELF_MANAGED, or BITBUCKET."];
      authType: AuthType.t option
        [@ocaml.doc
          "The type of authentication used by the credentials. Valid options are OAUTH, BASIC_AUTH, PERSONAL_ACCESS_TOKEN, CODECONNECTIONS, or SECRETS_MANAGER."];
      resource: String_.t option
        [@ocaml.doc
          "The connection ARN if your authType is CODECONNECTIONS or SECRETS_MANAGER."]}
    let make ?arn =
      fun ?serverType ->
        fun ?authType ->
          fun ?resource -> fun () -> { arn; serverType; authType; resource }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("serverType", (Option.map x.serverType ~f:ServerType.to_value));
        ("authType", (Option.map x.authType ~f:AuthType.to_value));
        ("resource", (Option.map x.resource ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resource") in
      let authType =
        (Option.map ~f:AuthType.of_xml) (Xml.child xml_arg0 "authType") in
      let serverType =
        (Option.map ~f:ServerType.of_xml) (Xml.child xml_arg0 "serverType") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      make ?resource ?authType ?serverType ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resource = field_map json__ "resource" String_.of_json in
      let authType = field_map json__ "authType" AuthType.of_json in
      let serverType = field_map json__ "serverType" ServerType.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      make ?resource ?authType ?serverType ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the credentials for a GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket repository."]
module EnvironmentPlatform =
  struct
    type nonrec t =
      {
      platform: PlatformType.t option [@ocaml.doc "The platform's name."];
      languages: EnvironmentLanguages.t option
        [@ocaml.doc
          "The list of programming languages that are available for the specified platform."]}
    let make ?platform = fun ?languages -> fun () -> { platform; languages }
    let to_value x =
      structure_to_value
        [("platform", (Option.map x.platform ~f:PlatformType.to_value));
        ("languages",
          (Option.map x.languages ~f:EnvironmentLanguages.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let languages =
        (Option.map ~f:EnvironmentLanguages.of_xml)
          (Xml.child xml_arg0 "languages") in
      let platform =
        (Option.map ~f:PlatformType.of_xml) (Xml.child xml_arg0 "platform") in
      make ?languages ?platform ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let languages =
        field_map json__ "languages" EnvironmentLanguages.of_json in
      let platform = field_map json__ "platform" PlatformType.of_json in
      make ?languages ?platform ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of Docker images that are related by platform and are managed by CodeBuild."]
module CommandExecution =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc "The ID of the command execution."];
      sandboxId: NonEmptyString.t option [@ocaml.doc "A sandboxId."];
      submitTime: Timestamp.t option
        [@ocaml.doc
          "When the command execution process was initially submitted, expressed in Unix time format."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the command execution process started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the command execution process ended, expressed in Unix time format."];
      status: NonEmptyString.t option
        [@ocaml.doc "The status of the command execution."];
      command: SensitiveNonEmptyString.t option
        [@ocaml.doc "The command that needs to be executed."];
      type_: CommandType.t option [@ocaml.doc "The command type."];
      exitCode: NonEmptyString.t option
        [@ocaml.doc "The exit code to return upon completion."];
      standardOutputContent: SensitiveNonEmptyString.t option
        [@ocaml.doc "The text written by the command to stdout."];
      standardErrContent: SensitiveNonEmptyString.t option
        [@ocaml.doc "The text written by the command to stderr."];
      logs: LogsLocation.t option ;
      sandboxArn: NonEmptyString.t option [@ocaml.doc "A sandboxArn."]}
    let make ?id =
      fun ?sandboxId ->
        fun ?submitTime ->
          fun ?startTime ->
            fun ?endTime ->
              fun ?status ->
                fun ?command ->
                  fun ?type_ ->
                    fun ?exitCode ->
                      fun ?standardOutputContent ->
                        fun ?standardErrContent ->
                          fun ?logs ->
                            fun ?sandboxArn ->
                              fun () ->
                                {
                                  id;
                                  sandboxId;
                                  submitTime;
                                  startTime;
                                  endTime;
                                  status;
                                  command;
                                  type_;
                                  exitCode;
                                  standardOutputContent;
                                  standardErrContent;
                                  logs;
                                  sandboxArn
                                }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("sandboxId", (Option.map x.sandboxId ~f:NonEmptyString.to_value));
        ("submitTime", (Option.map x.submitTime ~f:Timestamp.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("status", (Option.map x.status ~f:NonEmptyString.to_value));
        ("command",
          (Option.map x.command ~f:SensitiveNonEmptyString.to_value));
        ("type", (Option.map x.type_ ~f:CommandType.to_value));
        ("exitCode", (Option.map x.exitCode ~f:NonEmptyString.to_value));
        ("standardOutputContent",
          (Option.map x.standardOutputContent
             ~f:SensitiveNonEmptyString.to_value));
        ("standardErrContent",
          (Option.map x.standardErrContent
             ~f:SensitiveNonEmptyString.to_value));
        ("logs", (Option.map x.logs ~f:LogsLocation.to_value));
        ("sandboxArn", (Option.map x.sandboxArn ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sandboxArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "sandboxArn") in
      let logs =
        (Option.map ~f:LogsLocation.of_xml) (Xml.child xml_arg0 "logs") in
      let standardErrContent =
        (Option.map ~f:SensitiveNonEmptyString.of_xml)
          (Xml.child xml_arg0 "standardErrContent") in
      let standardOutputContent =
        (Option.map ~f:SensitiveNonEmptyString.of_xml)
          (Xml.child xml_arg0 "standardOutputContent") in
      let exitCode =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "exitCode") in
      let type_ =
        (Option.map ~f:CommandType.of_xml) (Xml.child xml_arg0 "type") in
      let command =
        (Option.map ~f:SensitiveNonEmptyString.of_xml)
          (Xml.child xml_arg0 "command") in
      let status =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "status") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let submitTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "submitTime") in
      let sandboxId =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "sandboxId") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?sandboxArn ?logs ?standardErrContent ?standardOutputContent
        ?exitCode ?type_ ?command ?status ?endTime ?startTime ?submitTime
        ?sandboxId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sandboxArn = field_map json__ "sandboxArn" NonEmptyString.of_json in
      let logs = field_map json__ "logs" LogsLocation.of_json in
      let standardErrContent =
        field_map json__ "standardErrContent" SensitiveNonEmptyString.of_json in
      let standardOutputContent =
        field_map json__ "standardOutputContent"
          SensitiveNonEmptyString.of_json in
      let exitCode = field_map json__ "exitCode" NonEmptyString.of_json in
      let type_ = field_map json__ "type" CommandType.of_json in
      let command =
        field_map json__ "command" SensitiveNonEmptyString.of_json in
      let status = field_map json__ "status" NonEmptyString.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let submitTime = field_map json__ "submitTime" Timestamp.of_json in
      let sandboxId = field_map json__ "sandboxId" NonEmptyString.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?sandboxArn ?logs ?standardErrContent ?standardOutputContent
        ?exitCode ?type_ ?command ?status ?endTime ?startTime ?submitTime
        ?sandboxId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains command execution information."]
module ReportWithRawData =
  struct
    type nonrec t =
      {
      reportArn: NonEmptyString.t option
        [@ocaml.doc "The ARN of the report."];
      data: String_.t option
        [@ocaml.doc "The value of the requested data field from the report."]}
    let make ?reportArn = fun ?data -> fun () -> { reportArn; data }
    let to_value x =
      structure_to_value
        [("reportArn", (Option.map x.reportArn ~f:NonEmptyString.to_value));
        ("data", (Option.map x.data ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let data = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "data") in
      let reportArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "reportArn") in
      make ?data ?reportArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let data = field_map json__ "data" String_.of_json in
      let reportArn = field_map json__ "reportArn" NonEmptyString.of_json in
      make ?data ?reportArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the unmodified data for the report. For more information, see ."]
module TestCase =
  struct
    type nonrec t =
      {
      reportArn: NonEmptyString.t option
        [@ocaml.doc "The ARN of the report to which the test case belongs."];
      testRawDataPath: String_.t option
        [@ocaml.doc
          "The path to the raw data file that contains the test result."];
      prefix: String_.t option
        [@ocaml.doc
          "A string that is applied to a series of related test cases. CodeBuild generates the prefix. The prefix depends on the framework used to generate the tests."];
      name: String_.t option [@ocaml.doc "The name of the test case."];
      status: String_.t option
        [@ocaml.doc
          "The status returned by the test case after it was run. Valid statuses are SUCCEEDED, FAILED, ERROR, SKIPPED, and UNKNOWN."];
      durationInNanoSeconds: WrapperLong.t option
        [@ocaml.doc
          "The number of nanoseconds it took to run this test case."];
      message: String_.t option
        [@ocaml.doc
          "A message associated with a test case. For example, an error message or stack trace."];
      expired: Timestamp.t option
        [@ocaml.doc
          "The date and time a test case expires. A test case expires 30 days after it is created. An expired test case is not available to view in CodeBuild."];
      testSuiteName: String_.t option
        [@ocaml.doc
          "The name of the test suite that the test case is a part of."]}
    let make ?reportArn =
      fun ?testRawDataPath ->
        fun ?prefix ->
          fun ?name ->
            fun ?status ->
              fun ?durationInNanoSeconds ->
                fun ?message ->
                  fun ?expired ->
                    fun ?testSuiteName ->
                      fun () ->
                        {
                          reportArn;
                          testRawDataPath;
                          prefix;
                          name;
                          status;
                          durationInNanoSeconds;
                          message;
                          expired;
                          testSuiteName
                        }
    let to_value x =
      structure_to_value
        [("reportArn", (Option.map x.reportArn ~f:NonEmptyString.to_value));
        ("testRawDataPath",
          (Option.map x.testRawDataPath ~f:String_.to_value));
        ("prefix", (Option.map x.prefix ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("durationInNanoSeconds",
          (Option.map x.durationInNanoSeconds ~f:WrapperLong.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("expired", (Option.map x.expired ~f:Timestamp.to_value));
        ("testSuiteName", (Option.map x.testSuiteName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let testSuiteName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "testSuiteName") in
      let expired =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "expired") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let durationInNanoSeconds =
        (Option.map ~f:WrapperLong.of_xml)
          (Xml.child xml_arg0 "durationInNanoSeconds") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let prefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "prefix") in
      let testRawDataPath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "testRawDataPath") in
      let reportArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "reportArn") in
      make ?testSuiteName ?expired ?message ?durationInNanoSeconds ?status
        ?name ?prefix ?testRawDataPath ?reportArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let testSuiteName = field_map json__ "testSuiteName" String_.of_json in
      let expired = field_map json__ "expired" Timestamp.of_json in
      let message = field_map json__ "message" String_.of_json in
      let durationInNanoSeconds =
        field_map json__ "durationInNanoSeconds" WrapperLong.of_json in
      let status = field_map json__ "status" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let prefix = field_map json__ "prefix" String_.of_json in
      let testRawDataPath =
        field_map json__ "testRawDataPath" String_.of_json in
      let reportArn = field_map json__ "reportArn" NonEmptyString.of_json in
      make ?testSuiteName ?expired ?message ?durationInNanoSeconds ?status
        ?name ?prefix ?testRawDataPath ?reportArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a test case created using a framework such as NUnit or Cucumber. A test case might be a unit test or a configuration test."]
module CodeCoverage =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc "The identifier of the code coverage report."];
      reportARN: NonEmptyString.t option
        [@ocaml.doc "The ARN of the report."];
      filePath: NonEmptyString.t option
        [@ocaml.doc "The path of the test report file."];
      lineCoveragePercentage: Percentage.t option
        [@ocaml.doc
          "The percentage of lines that are covered by your tests."];
      linesCovered: NonNegativeInt.t option
        [@ocaml.doc "The number of lines that are covered by your tests."];
      linesMissed: NonNegativeInt.t option
        [@ocaml.doc
          "The number of lines that are not covered by your tests."];
      branchCoveragePercentage: Percentage.t option
        [@ocaml.doc
          "The percentage of branches that are covered by your tests."];
      branchesCovered: NonNegativeInt.t option
        [@ocaml.doc
          "The number of conditional branches that are covered by your tests."];
      branchesMissed: NonNegativeInt.t option
        [@ocaml.doc
          "The number of conditional branches that are not covered by your tests."];
      expired: Timestamp.t option
        [@ocaml.doc "The date and time that the tests were run."]}
    let make ?id =
      fun ?reportARN ->
        fun ?filePath ->
          fun ?lineCoveragePercentage ->
            fun ?linesCovered ->
              fun ?linesMissed ->
                fun ?branchCoveragePercentage ->
                  fun ?branchesCovered ->
                    fun ?branchesMissed ->
                      fun ?expired ->
                        fun () ->
                          {
                            id;
                            reportARN;
                            filePath;
                            lineCoveragePercentage;
                            linesCovered;
                            linesMissed;
                            branchCoveragePercentage;
                            branchesCovered;
                            branchesMissed;
                            expired
                          }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("reportARN", (Option.map x.reportARN ~f:NonEmptyString.to_value));
        ("filePath", (Option.map x.filePath ~f:NonEmptyString.to_value));
        ("lineCoveragePercentage",
          (Option.map x.lineCoveragePercentage ~f:Percentage.to_value));
        ("linesCovered",
          (Option.map x.linesCovered ~f:NonNegativeInt.to_value));
        ("linesMissed",
          (Option.map x.linesMissed ~f:NonNegativeInt.to_value));
        ("branchCoveragePercentage",
          (Option.map x.branchCoveragePercentage ~f:Percentage.to_value));
        ("branchesCovered",
          (Option.map x.branchesCovered ~f:NonNegativeInt.to_value));
        ("branchesMissed",
          (Option.map x.branchesMissed ~f:NonNegativeInt.to_value));
        ("expired", (Option.map x.expired ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expired =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "expired") in
      let branchesMissed =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "branchesMissed") in
      let branchesCovered =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "branchesCovered") in
      let branchCoveragePercentage =
        (Option.map ~f:Percentage.of_xml)
          (Xml.child xml_arg0 "branchCoveragePercentage") in
      let linesMissed =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "linesMissed") in
      let linesCovered =
        (Option.map ~f:NonNegativeInt.of_xml)
          (Xml.child xml_arg0 "linesCovered") in
      let lineCoveragePercentage =
        (Option.map ~f:Percentage.of_xml)
          (Xml.child xml_arg0 "lineCoveragePercentage") in
      let filePath =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "filePath") in
      let reportARN =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "reportARN") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?expired ?branchesMissed ?branchesCovered
        ?branchCoveragePercentage ?linesMissed ?linesCovered
        ?lineCoveragePercentage ?filePath ?reportARN ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expired = field_map json__ "expired" Timestamp.of_json in
      let branchesMissed =
        field_map json__ "branchesMissed" NonNegativeInt.of_json in
      let branchesCovered =
        field_map json__ "branchesCovered" NonNegativeInt.of_json in
      let branchCoveragePercentage =
        field_map json__ "branchCoveragePercentage" Percentage.of_json in
      let linesMissed = field_map json__ "linesMissed" NonNegativeInt.of_json in
      let linesCovered =
        field_map json__ "linesCovered" NonNegativeInt.of_json in
      let lineCoveragePercentage =
        field_map json__ "lineCoveragePercentage" Percentage.of_json in
      let filePath = field_map json__ "filePath" NonEmptyString.of_json in
      let reportARN = field_map json__ "reportARN" NonEmptyString.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?expired ?branchesMissed ?branchesCovered
        ?branchCoveragePercentage ?linesMissed ?linesCovered
        ?lineCoveragePercentage ?filePath ?reportARN ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains code coverage report information. Line coverage measures how many statements your tests cover. A statement is a single instruction, not including comments, conditionals, etc. Branch coverage determines if your tests cover every possible branch of a control structure, such as an if or case statement."]
module BuildNotDeleted =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc
          "The ID of the build that could not be successfully deleted."];
      statusCode: String_.t option
        [@ocaml.doc
          "Additional information about the build that could not be successfully deleted."]}
    let make ?id = fun ?statusCode -> fun () -> { id; statusCode }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("statusCode", (Option.map x.statusCode ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusCode") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?statusCode ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statusCode = field_map json__ "statusCode" String_.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?statusCode ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a build that could not be successfully deleted."]
module Sandbox =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option [@ocaml.doc "The ID of the sandbox."];
      arn: NonEmptyString.t option [@ocaml.doc "The ARN of the sandbox."];
      projectName: NonEmptyString.t option
        [@ocaml.doc "The CodeBuild project name."];
      requestTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox process was initially requested, expressed in Unix time format."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox process started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the sandbox process ended, expressed in Unix time format."];
      status: String_.t option [@ocaml.doc "The status of the sandbox."];
      source: ProjectSource.t option ;
      sourceVersion: NonEmptyString.t option
        [@ocaml.doc
          "Any version identifier for the version of the sandbox to be built."];
      secondarySources: ProjectSources.t option
        [@ocaml.doc "An array of ProjectSource objects."];
      secondarySourceVersions: ProjectSecondarySourceVersions.t option
        [@ocaml.doc "An array of ProjectSourceVersion objects."];
      environment: ProjectEnvironment.t option ;
      fileSystemLocations: ProjectFileSystemLocations.t option
        [@ocaml.doc
          "An array of ProjectFileSystemLocation objects for a CodeBuild build project. A ProjectFileSystemLocation object specifies the identifier, location, mountOptions, mountPoint, and type of a file system created using Amazon Elastic File System."];
      timeoutInMinutes: WrapperInt.t option
        [@ocaml.doc
          "How long, in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before timing out this sandbox if it does not get marked as completed."];
      queuedTimeoutInMinutes: WrapperInt.t option
        [@ocaml.doc
          "The number of minutes a sandbox is allowed to be queued before it times out."];
      vpcConfig: VpcConfig.t option ;
      logConfig: LogsConfig.t option ;
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) to be used for encrypting the sandbox output artifacts."];
      serviceRole: NonEmptyString.t option
        [@ocaml.doc "The name of a service role used for this sandbox."];
      currentSession: SandboxSession.t option
        [@ocaml.doc "The current session for the sandbox."]}
    let make ?id =
      fun ?arn ->
        fun ?projectName ->
          fun ?requestTime ->
            fun ?startTime ->
              fun ?endTime ->
                fun ?status ->
                  fun ?source ->
                    fun ?sourceVersion ->
                      fun ?secondarySources ->
                        fun ?secondarySourceVersions ->
                          fun ?environment ->
                            fun ?fileSystemLocations ->
                              fun ?timeoutInMinutes ->
                                fun ?queuedTimeoutInMinutes ->
                                  fun ?vpcConfig ->
                                    fun ?logConfig ->
                                      fun ?encryptionKey ->
                                        fun ?serviceRole ->
                                          fun ?currentSession ->
                                            fun () ->
                                              {
                                                id;
                                                arn;
                                                projectName;
                                                requestTime;
                                                startTime;
                                                endTime;
                                                status;
                                                source;
                                                sourceVersion;
                                                secondarySources;
                                                secondarySourceVersions;
                                                environment;
                                                fileSystemLocations;
                                                timeoutInMinutes;
                                                queuedTimeoutInMinutes;
                                                vpcConfig;
                                                logConfig;
                                                encryptionKey;
                                                serviceRole;
                                                currentSession
                                              }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("projectName",
          (Option.map x.projectName ~f:NonEmptyString.to_value));
        ("requestTime", (Option.map x.requestTime ~f:Timestamp.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("source", (Option.map x.source ~f:ProjectSource.to_value));
        ("sourceVersion",
          (Option.map x.sourceVersion ~f:NonEmptyString.to_value));
        ("secondarySources",
          (Option.map x.secondarySources ~f:ProjectSources.to_value));
        ("secondarySourceVersions",
          (Option.map x.secondarySourceVersions
             ~f:ProjectSecondarySourceVersions.to_value));
        ("environment",
          (Option.map x.environment ~f:ProjectEnvironment.to_value));
        ("fileSystemLocations",
          (Option.map x.fileSystemLocations
             ~f:ProjectFileSystemLocations.to_value));
        ("timeoutInMinutes",
          (Option.map x.timeoutInMinutes ~f:WrapperInt.to_value));
        ("queuedTimeoutInMinutes",
          (Option.map x.queuedTimeoutInMinutes ~f:WrapperInt.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("logConfig", (Option.map x.logConfig ~f:LogsConfig.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("serviceRole",
          (Option.map x.serviceRole ~f:NonEmptyString.to_value));
        ("currentSession",
          (Option.map x.currentSession ~f:SandboxSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let currentSession =
        (Option.map ~f:SandboxSession.of_xml)
          (Xml.child xml_arg0 "currentSession") in
      let serviceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRole") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let logConfig =
        (Option.map ~f:LogsConfig.of_xml) (Xml.child xml_arg0 "logConfig") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let queuedTimeoutInMinutes =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutes") in
      let timeoutInMinutes =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "timeoutInMinutes") in
      let fileSystemLocations =
        (Option.map ~f:ProjectFileSystemLocations.of_xml)
          (Xml.child xml_arg0 "fileSystemLocations") in
      let environment =
        (Option.map ~f:ProjectEnvironment.of_xml)
          (Xml.child xml_arg0 "environment") in
      let secondarySourceVersions =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourceVersions") in
      let secondarySources =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySources") in
      let sourceVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "sourceVersion") in
      let source =
        (Option.map ~f:ProjectSource.of_xml) (Xml.child xml_arg0 "source") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let requestTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "requestTime") in
      let projectName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "projectName") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?currentSession ?serviceRole ?encryptionKey ?logConfig ?vpcConfig
        ?queuedTimeoutInMinutes ?timeoutInMinutes ?fileSystemLocations
        ?environment ?secondarySourceVersions ?secondarySources
        ?sourceVersion ?source ?status ?endTime ?startTime ?requestTime
        ?projectName ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let currentSession =
        field_map json__ "currentSession" SandboxSession.of_json in
      let serviceRole = field_map json__ "serviceRole" NonEmptyString.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let logConfig = field_map json__ "logConfig" LogsConfig.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let queuedTimeoutInMinutes =
        field_map json__ "queuedTimeoutInMinutes" WrapperInt.of_json in
      let timeoutInMinutes =
        field_map json__ "timeoutInMinutes" WrapperInt.of_json in
      let fileSystemLocations =
        field_map json__ "fileSystemLocations"
          ProjectFileSystemLocations.of_json in
      let environment =
        field_map json__ "environment" ProjectEnvironment.of_json in
      let secondarySourceVersions =
        field_map json__ "secondarySourceVersions"
          ProjectSecondarySourceVersions.of_json in
      let secondarySources =
        field_map json__ "secondarySources" ProjectSources.of_json in
      let sourceVersion =
        field_map json__ "sourceVersion" NonEmptyString.of_json in
      let source = field_map json__ "source" ProjectSource.of_json in
      let status = field_map json__ "status" String_.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let requestTime = field_map json__ "requestTime" Timestamp.of_json in
      let projectName = field_map json__ "projectName" NonEmptyString.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?currentSession ?serviceRole ?encryptionKey ?logConfig ?vpcConfig
        ?queuedTimeoutInMinutes ?timeoutInMinutes ?fileSystemLocations
        ?environment ?secondarySourceVersions ?secondarySources
        ?sourceVersion ?source ?status ?endTime ?startTime ?requestTime
        ?projectName ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains sandbox information."]
module Report =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t option [@ocaml.doc "The ARN of the report run."];
      type_: ReportType.t option
        [@ocaml.doc
          "The type of the report that was run. CODE_COVERAGE A code coverage report. TEST A test report."];
      name: String_.t option
        [@ocaml.doc "The name of the report that was run."];
      reportGroupArn: NonEmptyString.t option
        [@ocaml.doc
          "The ARN of the report group associated with this report."];
      executionId: String_.t option
        [@ocaml.doc "The ARN of the build run that generated this report."];
      status: ReportStatusType.t option
        [@ocaml.doc "The status of this report."];
      created: Timestamp.t option
        [@ocaml.doc "The date and time this report run occurred."];
      expired: Timestamp.t option
        [@ocaml.doc
          "The date and time a report expires. A report expires 30 days after it is created. An expired report is not available to view in CodeBuild."];
      exportConfig: ReportExportConfig.t option
        [@ocaml.doc
          "Information about where the raw data used to generate this report was exported."];
      truncated: WrapperBoolean.t option
        [@ocaml.doc
          "A boolean that specifies if this report run is truncated. The list of test cases is truncated after the maximum number of test cases is reached."];
      testSummary: TestReportSummary.t option
        [@ocaml.doc
          "A TestReportSummary object that contains information about this test report."];
      codeCoverageSummary: CodeCoverageReportSummary.t option
        [@ocaml.doc
          "A CodeCoverageReportSummary object that contains a code coverage summary for this report."]}
    let make ?arn =
      fun ?type_ ->
        fun ?name ->
          fun ?reportGroupArn ->
            fun ?executionId ->
              fun ?status ->
                fun ?created ->
                  fun ?expired ->
                    fun ?exportConfig ->
                      fun ?truncated ->
                        fun ?testSummary ->
                          fun ?codeCoverageSummary ->
                            fun () ->
                              {
                                arn;
                                type_;
                                name;
                                reportGroupArn;
                                executionId;
                                status;
                                created;
                                expired;
                                exportConfig;
                                truncated;
                                testSummary;
                                codeCoverageSummary
                              }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("type", (Option.map x.type_ ~f:ReportType.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("reportGroupArn",
          (Option.map x.reportGroupArn ~f:NonEmptyString.to_value));
        ("executionId", (Option.map x.executionId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:ReportStatusType.to_value));
        ("created", (Option.map x.created ~f:Timestamp.to_value));
        ("expired", (Option.map x.expired ~f:Timestamp.to_value));
        ("exportConfig",
          (Option.map x.exportConfig ~f:ReportExportConfig.to_value));
        ("truncated", (Option.map x.truncated ~f:WrapperBoolean.to_value));
        ("testSummary",
          (Option.map x.testSummary ~f:TestReportSummary.to_value));
        ("codeCoverageSummary",
          (Option.map x.codeCoverageSummary
             ~f:CodeCoverageReportSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeCoverageSummary =
        (Option.map ~f:CodeCoverageReportSummary.of_xml)
          (Xml.child xml_arg0 "codeCoverageSummary") in
      let testSummary =
        (Option.map ~f:TestReportSummary.of_xml)
          (Xml.child xml_arg0 "testSummary") in
      let truncated =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "truncated") in
      let exportConfig =
        (Option.map ~f:ReportExportConfig.of_xml)
          (Xml.child xml_arg0 "exportConfig") in
      let expired =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "expired") in
      let created =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "created") in
      let status =
        (Option.map ~f:ReportStatusType.of_xml) (Xml.child xml_arg0 "status") in
      let executionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "executionId") in
      let reportGroupArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "reportGroupArn") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let type_ =
        (Option.map ~f:ReportType.of_xml) (Xml.child xml_arg0 "type") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      make ?codeCoverageSummary ?testSummary ?truncated ?exportConfig
        ?expired ?created ?status ?executionId ?reportGroupArn ?name ?type_
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeCoverageSummary =
        field_map json__ "codeCoverageSummary"
          CodeCoverageReportSummary.of_json in
      let testSummary =
        field_map json__ "testSummary" TestReportSummary.of_json in
      let truncated = field_map json__ "truncated" WrapperBoolean.of_json in
      let exportConfig =
        field_map json__ "exportConfig" ReportExportConfig.of_json in
      let expired = field_map json__ "expired" Timestamp.of_json in
      let created = field_map json__ "created" Timestamp.of_json in
      let status = field_map json__ "status" ReportStatusType.of_json in
      let executionId = field_map json__ "executionId" String_.of_json in
      let reportGroupArn =
        field_map json__ "reportGroupArn" NonEmptyString.of_json in
      let name = field_map json__ "name" String_.of_json in
      let type_ = field_map json__ "type" ReportType.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      make ?codeCoverageSummary ?testSummary ?truncated ?exportConfig
        ?expired ?created ?status ?executionId ?reportGroupArn ?name ?type_
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the results from running a series of test cases during the run of a build project. The test cases are specified in the buildspec for the build project using one or more paths to the test case files. You can specify any type of tests you want, such as unit tests, integration tests, and functional tests."]
module ReportGroup =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t option [@ocaml.doc "The ARN of the ReportGroup."];
      name: ReportGroupName.t option
        [@ocaml.doc "The name of the ReportGroup."];
      type_: ReportType.t option
        [@ocaml.doc
          "The type of the ReportGroup. This can be one of the following values: CODE_COVERAGE The report group contains code coverage reports. TEST The report group contains test reports."];
      exportConfig: ReportExportConfig.t option
        [@ocaml.doc
          "Information about the destination where the raw data of this ReportGroup is exported."];
      created: Timestamp.t option
        [@ocaml.doc "The date and time this ReportGroup was created."];
      lastModified: Timestamp.t option
        [@ocaml.doc "The date and time this ReportGroup was last modified."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this report group. These tags are available for use by Amazon Web Services services that support CodeBuild report group tags."];
      status: ReportGroupStatusType.t option
        [@ocaml.doc
          "The status of the report group. This property is read-only. This can be one of the following values: ACTIVE The report group is active. DELETING The report group is in the process of being deleted."]}
    let make ?arn =
      fun ?name ->
        fun ?type_ ->
          fun ?exportConfig ->
            fun ?created ->
              fun ?lastModified ->
                fun ?tags ->
                  fun ?status ->
                    fun () ->
                      {
                        arn;
                        name;
                        type_;
                        exportConfig;
                        created;
                        lastModified;
                        tags;
                        status
                      }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("name", (Option.map x.name ~f:ReportGroupName.to_value));
        ("type", (Option.map x.type_ ~f:ReportType.to_value));
        ("exportConfig",
          (Option.map x.exportConfig ~f:ReportExportConfig.to_value));
        ("created", (Option.map x.created ~f:Timestamp.to_value));
        ("lastModified", (Option.map x.lastModified ~f:Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("status", (Option.map x.status ~f:ReportGroupStatusType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ReportGroupStatusType.of_xml)
          (Xml.child xml_arg0 "status") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let lastModified =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastModified") in
      let created =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "created") in
      let exportConfig =
        (Option.map ~f:ReportExportConfig.of_xml)
          (Xml.child xml_arg0 "exportConfig") in
      let type_ =
        (Option.map ~f:ReportType.of_xml) (Xml.child xml_arg0 "type") in
      let name =
        (Option.map ~f:ReportGroupName.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      make ?status ?tags ?lastModified ?created ?exportConfig ?type_ ?name
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" ReportGroupStatusType.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let lastModified = field_map json__ "lastModified" Timestamp.of_json in
      let created = field_map json__ "created" Timestamp.of_json in
      let exportConfig =
        field_map json__ "exportConfig" ReportExportConfig.of_json in
      let type_ = field_map json__ "type" ReportType.of_json in
      let name = field_map json__ "name" ReportGroupName.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      make ?status ?tags ?lastModified ?created ?exportConfig ?type_ ?name
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A series of reports. Each report contains information about the results from running a series of test cases. You specify the test cases for a report group in the buildspec for a build project using one or more paths to the test case files."]
module Project =
  struct
    type nonrec t =
      {
      name: ProjectName.t option
        [@ocaml.doc "The name of the build project."];
      arn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the build project."];
      description: ProjectDescription.t option
        [@ocaml.doc
          "A description that makes the build project easy to identify."];
      source: ProjectSource.t option
        [@ocaml.doc
          "Information about the build input source code for this build project."];
      secondarySources: ProjectSources.t option
        [@ocaml.doc "An array of ProjectSource objects."];
      sourceVersion: String_.t option
        [@ocaml.doc
          "A version of the build input to be built for this project. If not specified, the latest version is used. If specified, it must be one of: For CodeCommit: the commit ID, branch, or Git tag to use. For GitHub: the commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For GitLab: the commit ID, branch, or Git tag to use. For Bitbucket: the commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Amazon S3: the version ID of the object that represents the build input ZIP file to use. If sourceVersion is specified at the build level, then that version takes precedence over this sourceVersion (at the project level). For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."];
      secondarySourceVersions: ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects. If secondarySourceVersions is specified at the build level, then they take over these secondarySourceVersions (at the project level)."];
      artifacts: ProjectArtifacts.t option
        [@ocaml.doc
          "Information about the build output artifacts for the build project."];
      secondaryArtifacts: ProjectArtifactsList.t option
        [@ocaml.doc "An array of ProjectArtifacts objects."];
      cache: ProjectCache.t option
        [@ocaml.doc "Information about the cache for the build project."];
      environment: ProjectEnvironment.t option
        [@ocaml.doc
          "Information about the build environment for this build project."];
      serviceRole: NonEmptyString.t option
        [@ocaml.doc
          "The ARN of the IAM role that enables CodeBuild to interact with dependent Amazon Web Services services on behalf of the Amazon Web Services account."];
      timeoutInMinutes: BuildTimeOut.t option
        [@ocaml.doc
          "How long, in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before timing out any related build that did not get marked as completed. The default is 60 minutes."];
      queuedTimeoutInMinutes: TimeOut.t option
        [@ocaml.doc
          "The number of minutes a build is allowed to be queued before it times out."];
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) to be used for encrypting the build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>). If you don't specify a value, CodeBuild uses the managed CMK for Amazon Simple Storage Service (Amazon S3)."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this build project. These tags are available for use by Amazon Web Services services that support CodeBuild build project tags."];
      created: Timestamp.t option
        [@ocaml.doc
          "When the build project was created, expressed in Unix time format."];
      lastModified: Timestamp.t option
        [@ocaml.doc
          "When the build project's settings were last modified, expressed in Unix time format."];
      webhook: Webhook.t option
        [@ocaml.doc
          "Information about a webhook that connects repository events to a build project in CodeBuild."];
      vpcConfig: VpcConfig.t option
        [@ocaml.doc
          "Information about the VPC configuration that CodeBuild accesses."];
      badge: ProjectBadge.t option
        [@ocaml.doc
          "Information about the build badge for the build project."];
      logsConfig: LogsConfig.t option
        [@ocaml.doc
          "Information about logs for the build project. A project can create logs in CloudWatch Logs, an S3 bucket, or both."];
      fileSystemLocations: ProjectFileSystemLocations.t option
        [@ocaml.doc
          "An array of ProjectFileSystemLocation objects for a CodeBuild build project. A ProjectFileSystemLocation object specifies the identifier, location, mountOptions, mountPoint, and type of a file system created using Amazon Elastic File System."];
      buildBatchConfig: ProjectBuildBatchConfig.t option
        [@ocaml.doc
          "A ProjectBuildBatchConfig object that defines the batch build options for the project."];
      concurrentBuildLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of concurrent builds that are allowed for this project. New builds are only started if the current number of builds is less than or equal to this limit. If the current build count meets this limit, new builds are throttled and are not run."];
      projectVisibility: ProjectVisibilityType.t option ;
      publicProjectAlias: NonEmptyString.t option
        [@ocaml.doc
          "Contains the project identifier used with the public build APIs."];
      resourceAccessRole: NonEmptyString.t option
        [@ocaml.doc
          "The ARN of the IAM role that enables CodeBuild to access the CloudWatch Logs and Amazon S3 artifacts for the project's builds."];
      autoRetryLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of additional automatic retries after a failed build. For example, if the auto-retry limit is set to 2, CodeBuild will call the RetryBuild API to automatically retry your build for up to 2 additional times."]}
    let make ?name =
      fun ?arn ->
        fun ?description ->
          fun ?source ->
            fun ?secondarySources ->
              fun ?sourceVersion ->
                fun ?secondarySourceVersions ->
                  fun ?artifacts ->
                    fun ?secondaryArtifacts ->
                      fun ?cache ->
                        fun ?environment ->
                          fun ?serviceRole ->
                            fun ?timeoutInMinutes ->
                              fun ?queuedTimeoutInMinutes ->
                                fun ?encryptionKey ->
                                  fun ?tags ->
                                    fun ?created ->
                                      fun ?lastModified ->
                                        fun ?webhook ->
                                          fun ?vpcConfig ->
                                            fun ?badge ->
                                              fun ?logsConfig ->
                                                fun ?fileSystemLocations ->
                                                  fun ?buildBatchConfig ->
                                                    fun ?concurrentBuildLimit
                                                      ->
                                                      fun ?projectVisibility
                                                        ->
                                                        fun
                                                          ?publicProjectAlias
                                                          ->
                                                          fun
                                                            ?resourceAccessRole
                                                            ->
                                                            fun
                                                              ?autoRetryLimit
                                                              ->
                                                              fun () ->
                                                                {
                                                                  name;
                                                                  arn;
                                                                  description;
                                                                  source;
                                                                  secondarySources;
                                                                  sourceVersion;
                                                                  secondarySourceVersions;
                                                                  artifacts;
                                                                  secondaryArtifacts;
                                                                  cache;
                                                                  environment;
                                                                  serviceRole;
                                                                  timeoutInMinutes;
                                                                  queuedTimeoutInMinutes;
                                                                  encryptionKey;
                                                                  tags;
                                                                  created;
                                                                  lastModified;
                                                                  webhook;
                                                                  vpcConfig;
                                                                  badge;
                                                                  logsConfig;
                                                                  fileSystemLocations;
                                                                  buildBatchConfig;
                                                                  concurrentBuildLimit;
                                                                  projectVisibility;
                                                                  publicProjectAlias;
                                                                  resourceAccessRole;
                                                                  autoRetryLimit
                                                                }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ProjectName.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("description",
          (Option.map x.description ~f:ProjectDescription.to_value));
        ("source", (Option.map x.source ~f:ProjectSource.to_value));
        ("secondarySources",
          (Option.map x.secondarySources ~f:ProjectSources.to_value));
        ("sourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("secondarySourceVersions",
          (Option.map x.secondarySourceVersions
             ~f:ProjectSecondarySourceVersions.to_value));
        ("artifacts", (Option.map x.artifacts ~f:ProjectArtifacts.to_value));
        ("secondaryArtifacts",
          (Option.map x.secondaryArtifacts ~f:ProjectArtifactsList.to_value));
        ("cache", (Option.map x.cache ~f:ProjectCache.to_value));
        ("environment",
          (Option.map x.environment ~f:ProjectEnvironment.to_value));
        ("serviceRole",
          (Option.map x.serviceRole ~f:NonEmptyString.to_value));
        ("timeoutInMinutes",
          (Option.map x.timeoutInMinutes ~f:BuildTimeOut.to_value));
        ("queuedTimeoutInMinutes",
          (Option.map x.queuedTimeoutInMinutes ~f:TimeOut.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("created", (Option.map x.created ~f:Timestamp.to_value));
        ("lastModified", (Option.map x.lastModified ~f:Timestamp.to_value));
        ("webhook", (Option.map x.webhook ~f:Webhook.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("badge", (Option.map x.badge ~f:ProjectBadge.to_value));
        ("logsConfig", (Option.map x.logsConfig ~f:LogsConfig.to_value));
        ("fileSystemLocations",
          (Option.map x.fileSystemLocations
             ~f:ProjectFileSystemLocations.to_value));
        ("buildBatchConfig",
          (Option.map x.buildBatchConfig ~f:ProjectBuildBatchConfig.to_value));
        ("concurrentBuildLimit",
          (Option.map x.concurrentBuildLimit ~f:WrapperInt.to_value));
        ("projectVisibility",
          (Option.map x.projectVisibility ~f:ProjectVisibilityType.to_value));
        ("publicProjectAlias",
          (Option.map x.publicProjectAlias ~f:NonEmptyString.to_value));
        ("resourceAccessRole",
          (Option.map x.resourceAccessRole ~f:NonEmptyString.to_value));
        ("autoRetryLimit",
          (Option.map x.autoRetryLimit ~f:WrapperInt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRetryLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "autoRetryLimit") in
      let resourceAccessRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "resourceAccessRole") in
      let publicProjectAlias =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "publicProjectAlias") in
      let projectVisibility =
        (Option.map ~f:ProjectVisibilityType.of_xml)
          (Xml.child xml_arg0 "projectVisibility") in
      let concurrentBuildLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "concurrentBuildLimit") in
      let buildBatchConfig =
        (Option.map ~f:ProjectBuildBatchConfig.of_xml)
          (Xml.child xml_arg0 "buildBatchConfig") in
      let fileSystemLocations =
        (Option.map ~f:ProjectFileSystemLocations.of_xml)
          (Xml.child xml_arg0 "fileSystemLocations") in
      let logsConfig =
        (Option.map ~f:LogsConfig.of_xml) (Xml.child xml_arg0 "logsConfig") in
      let badge =
        (Option.map ~f:ProjectBadge.of_xml) (Xml.child xml_arg0 "badge") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let webhook =
        (Option.map ~f:Webhook.of_xml) (Xml.child xml_arg0 "webhook") in
      let lastModified =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastModified") in
      let created =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "created") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let queuedTimeoutInMinutes =
        (Option.map ~f:TimeOut.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutes") in
      let timeoutInMinutes =
        (Option.map ~f:BuildTimeOut.of_xml)
          (Xml.child xml_arg0 "timeoutInMinutes") in
      let serviceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRole") in
      let environment =
        (Option.map ~f:ProjectEnvironment.of_xml)
          (Xml.child xml_arg0 "environment") in
      let cache =
        (Option.map ~f:ProjectCache.of_xml) (Xml.child xml_arg0 "cache") in
      let secondaryArtifacts =
        (Option.map ~f:ProjectArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifacts") in
      let artifacts =
        (Option.map ~f:ProjectArtifacts.of_xml)
          (Xml.child xml_arg0 "artifacts") in
      let secondarySourceVersions =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourceVersions") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceVersion") in
      let secondarySources =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySources") in
      let source =
        (Option.map ~f:ProjectSource.of_xml) (Xml.child xml_arg0 "source") in
      let description =
        (Option.map ~f:ProjectDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let name =
        (Option.map ~f:ProjectName.of_xml) (Xml.child xml_arg0 "name") in
      make ?autoRetryLimit ?resourceAccessRole ?publicProjectAlias
        ?projectVisibility ?concurrentBuildLimit ?buildBatchConfig
        ?fileSystemLocations ?logsConfig ?badge ?vpcConfig ?webhook
        ?lastModified ?created ?tags ?encryptionKey ?queuedTimeoutInMinutes
        ?timeoutInMinutes ?serviceRole ?environment ?cache
        ?secondaryArtifacts ?artifacts ?secondarySourceVersions
        ?sourceVersion ?secondarySources ?source ?description ?arn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let autoRetryLimit =
        field_map json__ "autoRetryLimit" WrapperInt.of_json in
      let resourceAccessRole =
        field_map json__ "resourceAccessRole" NonEmptyString.of_json in
      let publicProjectAlias =
        field_map json__ "publicProjectAlias" NonEmptyString.of_json in
      let projectVisibility =
        field_map json__ "projectVisibility" ProjectVisibilityType.of_json in
      let concurrentBuildLimit =
        field_map json__ "concurrentBuildLimit" WrapperInt.of_json in
      let buildBatchConfig =
        field_map json__ "buildBatchConfig" ProjectBuildBatchConfig.of_json in
      let fileSystemLocations =
        field_map json__ "fileSystemLocations"
          ProjectFileSystemLocations.of_json in
      let logsConfig = field_map json__ "logsConfig" LogsConfig.of_json in
      let badge = field_map json__ "badge" ProjectBadge.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let webhook = field_map json__ "webhook" Webhook.of_json in
      let lastModified = field_map json__ "lastModified" Timestamp.of_json in
      let created = field_map json__ "created" Timestamp.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let queuedTimeoutInMinutes =
        field_map json__ "queuedTimeoutInMinutes" TimeOut.of_json in
      let timeoutInMinutes =
        field_map json__ "timeoutInMinutes" BuildTimeOut.of_json in
      let serviceRole = field_map json__ "serviceRole" NonEmptyString.of_json in
      let environment =
        field_map json__ "environment" ProjectEnvironment.of_json in
      let cache = field_map json__ "cache" ProjectCache.of_json in
      let secondaryArtifacts =
        field_map json__ "secondaryArtifacts" ProjectArtifactsList.of_json in
      let artifacts = field_map json__ "artifacts" ProjectArtifacts.of_json in
      let secondarySourceVersions =
        field_map json__ "secondarySourceVersions"
          ProjectSecondarySourceVersions.of_json in
      let sourceVersion = field_map json__ "sourceVersion" String_.of_json in
      let secondarySources =
        field_map json__ "secondarySources" ProjectSources.of_json in
      let source = field_map json__ "source" ProjectSource.of_json in
      let description =
        field_map json__ "description" ProjectDescription.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let name = field_map json__ "name" ProjectName.of_json in
      make ?autoRetryLimit ?resourceAccessRole ?publicProjectAlias
        ?projectVisibility ?concurrentBuildLimit ?buildBatchConfig
        ?fileSystemLocations ?logsConfig ?badge ?vpcConfig ?webhook
        ?lastModified ?created ?tags ?encryptionKey ?queuedTimeoutInMinutes
        ?timeoutInMinutes ?serviceRole ?environment ?cache
        ?secondaryArtifacts ?artifacts ?secondarySourceVersions
        ?sourceVersion ?secondarySources ?source ?description ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a build project."]
module Fleet =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t option
        [@ocaml.doc "The ARN of the compute fleet."];
      name: FleetName.t option [@ocaml.doc "The name of the compute fleet."];
      id: NonEmptyString.t option [@ocaml.doc "The ID of the compute fleet."];
      created: Timestamp.t option
        [@ocaml.doc "The time at which the compute fleet was created."];
      lastModified: Timestamp.t option
        [@ocaml.doc "The time at which the compute fleet was last modified."];
      status: FleetStatus.t option
        [@ocaml.doc "The status of the compute fleet."];
      baseCapacity: FleetCapacity.t option
        [@ocaml.doc
          "The initial number of machines allocated to the compute \239\172\130eet, which de\239\172\129nes the number of builds that can run in parallel."];
      environmentType: EnvironmentType.t option
        [@ocaml.doc
          "The environment type of the compute fleet. The environment type ARM_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), Asia Pacific (Mumbai), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), EU (Frankfurt), and South America (S\195\163o Paulo). The environment type ARM_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_GPU_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), and Asia Pacific (Sydney). The environment type MAC_ARM is available for Medium fleets only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), Asia Pacific (Sydney), and EU (Frankfurt) The environment type MAC_ARM is available for Large fleets only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), and Asia Pacific (Sydney). The environment type WINDOWS_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type WINDOWS_SERVER_2019_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), Asia Pacific (Sydney), Asia Pacific (Tokyo), Asia Pacific (Mumbai) and EU (Ireland). The environment type WINDOWS_SERVER_2022_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Sydney), Asia Pacific (Singapore), Asia Pacific (Tokyo), South America (S\195\163o Paulo) and Asia Pacific (Mumbai). For more information, see Build environment compute types in the CodeBuild user guide."];
      computeType: ComputeType.t option
        [@ocaml.doc
          "Information about the compute resources the compute fleet uses. Available values include: ATTRIBUTE_BASED_COMPUTE: Specify the amount of vCPUs, memory, disk space, and the type of machine. If you use ATTRIBUTE_BASED_COMPUTE, you must define your attributes by using computeConfiguration. CodeBuild will select the cheapest instance that satisfies your specified attributes. For more information, see Reserved capacity environment types in the CodeBuild User Guide. CUSTOM_INSTANCE_TYPE: Specify the instance type for your compute fleet. For a list of supported instance types, see Supported instance families in the CodeBuild User Guide. BUILD_GENERAL1_SMALL: Use up to 4 GiB memory and 2 vCPUs for builds. BUILD_GENERAL1_MEDIUM: Use up to 8 GiB memory and 4 vCPUs for builds. BUILD_GENERAL1_LARGE: Use up to 16 GiB memory and 8 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_XLARGE: Use up to 72 GiB memory and 36 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_2XLARGE: Use up to 144 GiB memory, 72 vCPUs, and 824 GB of SSD storage for builds. This compute type supports Docker images up to 100 GB uncompressed. BUILD_LAMBDA_1GB: Use up to 1 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_2GB: Use up to 2 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_4GB: Use up to 4 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_8GB: Use up to 8 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_10GB: Use up to 10 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. If you use BUILD_GENERAL1_SMALL: For environment type LINUX_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 16 GiB memory, 4 vCPUs, and 1 NVIDIA A10G Tensor Core GPU for builds. For environment type ARM_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs on ARM-based processors for builds. If you use BUILD_GENERAL1_LARGE: For environment type LINUX_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 255 GiB memory, 32 vCPUs, and 4 NVIDIA Tesla V100 GPUs for builds. For environment type ARM_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs on ARM-based processors for builds. For more information, see On-demand environment types in the CodeBuild User Guide."];
      computeConfiguration: ComputeConfiguration.t option
        [@ocaml.doc
          "The compute configuration of the compute fleet. This is only required if computeType is set to ATTRIBUTE_BASED_COMPUTE or CUSTOM_INSTANCE_TYPE."];
      scalingConfiguration: ScalingConfigurationOutput.t option
        [@ocaml.doc "The scaling configuration of the compute fleet."];
      overflowBehavior: FleetOverflowBehavior.t option
        [@ocaml.doc
          "The compute fleet overflow behavior. For overflow behavior QUEUE, your overflow builds need to wait on the existing fleet instance to become available. For overflow behavior ON_DEMAND, your overflow builds run on CodeBuild on-demand. If you choose to set your overflow behavior to on-demand while creating a VPC-connected fleet, make sure that you add the required VPC permissions to your project service role. For more information, see Example policy statement to allow CodeBuild access to Amazon Web Services services required to create a VPC network interface."];
      vpcConfig: VpcConfig.t option ;
      proxyConfiguration: ProxyConfiguration.t option
        [@ocaml.doc "The proxy configuration of the compute fleet."];
      imageId: NonEmptyString.t option
        [@ocaml.doc "The Amazon Machine Image (AMI) of the compute fleet."];
      fleetServiceRole: NonEmptyString.t option
        [@ocaml.doc
          "The service role associated with the compute fleet. For more information, see Allow a user to add a permission policy for a fleet service role in the CodeBuild User Guide."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this compute fleet. These tags are available for use by Amazon Web Services services that support CodeBuild build project tags."]}
    let make ?arn =
      fun ?name ->
        fun ?id ->
          fun ?created ->
            fun ?lastModified ->
              fun ?status ->
                fun ?baseCapacity ->
                  fun ?environmentType ->
                    fun ?computeType ->
                      fun ?computeConfiguration ->
                        fun ?scalingConfiguration ->
                          fun ?overflowBehavior ->
                            fun ?vpcConfig ->
                              fun ?proxyConfiguration ->
                                fun ?imageId ->
                                  fun ?fleetServiceRole ->
                                    fun ?tags ->
                                      fun () ->
                                        {
                                          arn;
                                          name;
                                          id;
                                          created;
                                          lastModified;
                                          status;
                                          baseCapacity;
                                          environmentType;
                                          computeType;
                                          computeConfiguration;
                                          scalingConfiguration;
                                          overflowBehavior;
                                          vpcConfig;
                                          proxyConfiguration;
                                          imageId;
                                          fleetServiceRole;
                                          tags
                                        }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("name", (Option.map x.name ~f:FleetName.to_value));
        ("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("created", (Option.map x.created ~f:Timestamp.to_value));
        ("lastModified", (Option.map x.lastModified ~f:Timestamp.to_value));
        ("status", (Option.map x.status ~f:FleetStatus.to_value));
        ("baseCapacity",
          (Option.map x.baseCapacity ~f:FleetCapacity.to_value));
        ("environmentType",
          (Option.map x.environmentType ~f:EnvironmentType.to_value));
        ("computeType", (Option.map x.computeType ~f:ComputeType.to_value));
        ("computeConfiguration",
          (Option.map x.computeConfiguration ~f:ComputeConfiguration.to_value));
        ("scalingConfiguration",
          (Option.map x.scalingConfiguration
             ~f:ScalingConfigurationOutput.to_value));
        ("overflowBehavior",
          (Option.map x.overflowBehavior ~f:FleetOverflowBehavior.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("proxyConfiguration",
          (Option.map x.proxyConfiguration ~f:ProxyConfiguration.to_value));
        ("imageId", (Option.map x.imageId ~f:NonEmptyString.to_value));
        ("fleetServiceRole",
          (Option.map x.fleetServiceRole ~f:NonEmptyString.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let fleetServiceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "fleetServiceRole") in
      let imageId =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "imageId") in
      let proxyConfiguration =
        (Option.map ~f:ProxyConfiguration.of_xml)
          (Xml.child xml_arg0 "proxyConfiguration") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let overflowBehavior =
        (Option.map ~f:FleetOverflowBehavior.of_xml)
          (Xml.child xml_arg0 "overflowBehavior") in
      let scalingConfiguration =
        (Option.map ~f:ScalingConfigurationOutput.of_xml)
          (Xml.child xml_arg0 "scalingConfiguration") in
      let computeConfiguration =
        (Option.map ~f:ComputeConfiguration.of_xml)
          (Xml.child xml_arg0 "computeConfiguration") in
      let computeType =
        (Option.map ~f:ComputeType.of_xml) (Xml.child xml_arg0 "computeType") in
      let environmentType =
        (Option.map ~f:EnvironmentType.of_xml)
          (Xml.child xml_arg0 "environmentType") in
      let baseCapacity =
        (Option.map ~f:FleetCapacity.of_xml)
          (Xml.child xml_arg0 "baseCapacity") in
      let status =
        (Option.map ~f:FleetStatus.of_xml) (Xml.child xml_arg0 "status") in
      let lastModified =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastModified") in
      let created =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "created") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      let name = (Option.map ~f:FleetName.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?fleetServiceRole ?imageId ?proxyConfiguration ?vpcConfig
        ?overflowBehavior ?scalingConfiguration ?computeConfiguration
        ?computeType ?environmentType ?baseCapacity ?status ?lastModified
        ?created ?id ?name ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let fleetServiceRole =
        field_map json__ "fleetServiceRole" NonEmptyString.of_json in
      let imageId = field_map json__ "imageId" NonEmptyString.of_json in
      let proxyConfiguration =
        field_map json__ "proxyConfiguration" ProxyConfiguration.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let overflowBehavior =
        field_map json__ "overflowBehavior" FleetOverflowBehavior.of_json in
      let scalingConfiguration =
        field_map json__ "scalingConfiguration"
          ScalingConfigurationOutput.of_json in
      let computeConfiguration =
        field_map json__ "computeConfiguration" ComputeConfiguration.of_json in
      let computeType = field_map json__ "computeType" ComputeType.of_json in
      let environmentType =
        field_map json__ "environmentType" EnvironmentType.of_json in
      let baseCapacity =
        field_map json__ "baseCapacity" FleetCapacity.of_json in
      let status = field_map json__ "status" FleetStatus.of_json in
      let lastModified = field_map json__ "lastModified" Timestamp.of_json in
      let created = field_map json__ "created" Timestamp.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      let name = field_map json__ "name" FleetName.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      make ?tags ?fleetServiceRole ?imageId ?proxyConfiguration ?vpcConfig
        ?overflowBehavior ?scalingConfiguration ?computeConfiguration
        ?computeType ?environmentType ?baseCapacity ?status ?lastModified
        ?created ?id ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A set of dedicated instances for your build environment."]
module Build =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option [@ocaml.doc "The unique ID for the build."];
      arn: NonEmptyString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the build."];
      buildNumber: WrapperLong.t option
        [@ocaml.doc
          "The number of the build. For each project, the buildNumber of its first build is 1. The buildNumber of each subsequent build is incremented by 1. If a build is deleted, the buildNumber of other builds does not change."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "When the build process started, expressed in Unix time format."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "When the build process ended, expressed in Unix time format."];
      currentPhase: String_.t option [@ocaml.doc "The current build phase."];
      buildStatus: StatusType.t option
        [@ocaml.doc
          "The current status of the build. Valid values include: FAILED: The build failed. FAULT: The build faulted. IN_PROGRESS: The build is still in progress. STOPPED: The build stopped. SUCCEEDED: The build succeeded. TIMED_OUT: The build timed out."];
      sourceVersion: NonEmptyString.t option
        [@ocaml.doc
          "Any version identifier for the version of the source code to be built. If sourceVersion is specified at the project level, then this sourceVersion (at the build level) takes precedence. For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."];
      resolvedSourceVersion: NonEmptyString.t option
        [@ocaml.doc
          "An identifier for the version of this build's source code. For CodeCommit, GitHub, GitHub Enterprise, and BitBucket, the commit ID. For CodePipeline, the source revision provided by CodePipeline. For Amazon S3, this does not apply."];
      projectName: NonEmptyString.t option
        [@ocaml.doc "The name of the CodeBuild project."];
      phases: BuildPhases.t option
        [@ocaml.doc
          "Information about all previous build phases that are complete and information about any current build phase that is not yet complete."];
      source: ProjectSource.t option
        [@ocaml.doc "Information about the source code to be built."];
      secondarySources: ProjectSources.t option
        [@ocaml.doc "An array of ProjectSource objects."];
      secondarySourceVersions: ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects. Each ProjectSourceVersion must be one of: For CodeCommit: the commit ID, branch, or Git tag to use. For GitHub: the commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example, pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Bitbucket: the commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Amazon S3: the version ID of the object that represents the build input ZIP file to use."];
      artifacts: BuildArtifacts.t option
        [@ocaml.doc "Information about the output artifacts for the build."];
      secondaryArtifacts: BuildArtifactsList.t option
        [@ocaml.doc "An array of ProjectArtifacts objects."];
      cache: ProjectCache.t option
        [@ocaml.doc "Information about the cache for the build."];
      environment: ProjectEnvironment.t option
        [@ocaml.doc
          "Information about the build environment for this build."];
      serviceRole: NonEmptyString.t option
        [@ocaml.doc "The name of a service role used for this build."];
      logs: LogsLocation.t option
        [@ocaml.doc "Information about the build's logs in CloudWatch Logs."];
      timeoutInMinutes: WrapperInt.t option
        [@ocaml.doc
          "How long, in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before timing out this build if it does not get marked as completed."];
      queuedTimeoutInMinutes: WrapperInt.t option
        [@ocaml.doc
          "The number of minutes a build is allowed to be queued before it times out."];
      buildComplete: Boolean.t option
        [@ocaml.doc
          "Whether the build is complete. True if complete; otherwise, false."];
      initiator: String_.t option
        [@ocaml.doc
          "The entity that started the build. Valid values include: If CodePipeline started the build, the pipeline's name (for example, codepipeline/my-demo-pipeline). If a user started the build, the user's name (for example, MyUserName). If the Jenkins plugin for CodeBuild started the build, the string CodeBuild-Jenkins-Plugin."];
      vpcConfig: VpcConfig.t option
        [@ocaml.doc
          "If your CodeBuild project accesses resources in an Amazon VPC, you provide this parameter that identifies the VPC ID and the list of security group IDs and subnet IDs. The security groups and subnets must belong to the same VPC. You must provide at least one security group and one subnet ID."];
      networkInterface: NetworkInterface.t option
        [@ocaml.doc "Describes a network interface."];
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) to be used for encrypting the build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>)."];
      exportedEnvironmentVariables: ExportedEnvironmentVariables.t option
        [@ocaml.doc
          "A list of exported environment variables for this build. Exported environment variables are used in conjunction with CodePipeline to export environment variables from the current build stage to subsequent stages in the pipeline. For more information, see Working with variables in the CodePipeline User Guide."];
      reportArns: BuildReportArns.t option
        [@ocaml.doc
          "An array of the ARNs associated with this build's reports."];
      fileSystemLocations: ProjectFileSystemLocations.t option
        [@ocaml.doc
          "An array of ProjectFileSystemLocation objects for a CodeBuild build project. A ProjectFileSystemLocation object specifies the identifier, location, mountOptions, mountPoint, and type of a file system created using Amazon Elastic File System."];
      debugSession: DebugSession.t option
        [@ocaml.doc
          "Contains information about the debug session for this build."];
      buildBatchArn: String_.t option
        [@ocaml.doc
          "The ARN of the batch build that this build is a member of, if applicable."];
      autoRetryConfig: AutoRetryConfig.t option
        [@ocaml.doc
          "Information about the auto-retry configuration for the build."]}
    let make ?id =
      fun ?arn ->
        fun ?buildNumber ->
          fun ?startTime ->
            fun ?endTime ->
              fun ?currentPhase ->
                fun ?buildStatus ->
                  fun ?sourceVersion ->
                    fun ?resolvedSourceVersion ->
                      fun ?projectName ->
                        fun ?phases ->
                          fun ?source ->
                            fun ?secondarySources ->
                              fun ?secondarySourceVersions ->
                                fun ?artifacts ->
                                  fun ?secondaryArtifacts ->
                                    fun ?cache ->
                                      fun ?environment ->
                                        fun ?serviceRole ->
                                          fun ?logs ->
                                            fun ?timeoutInMinutes ->
                                              fun ?queuedTimeoutInMinutes ->
                                                fun ?buildComplete ->
                                                  fun ?initiator ->
                                                    fun ?vpcConfig ->
                                                      fun ?networkInterface
                                                        ->
                                                        fun ?encryptionKey ->
                                                          fun
                                                            ?exportedEnvironmentVariables
                                                            ->
                                                            fun ?reportArns
                                                              ->
                                                              fun
                                                                ?fileSystemLocations
                                                                ->
                                                                fun
                                                                  ?debugSession
                                                                  ->
                                                                  fun
                                                                    ?buildBatchArn
                                                                    ->
                                                                    fun
                                                                    ?autoRetryConfig
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    id;
                                                                    arn;
                                                                    buildNumber;
                                                                    startTime;
                                                                    endTime;
                                                                    currentPhase;
                                                                    buildStatus;
                                                                    sourceVersion;
                                                                    resolvedSourceVersion;
                                                                    projectName;
                                                                    phases;
                                                                    source;
                                                                    secondarySources;
                                                                    secondarySourceVersions;
                                                                    artifacts;
                                                                    secondaryArtifacts;
                                                                    cache;
                                                                    environment;
                                                                    serviceRole;
                                                                    logs;
                                                                    timeoutInMinutes;
                                                                    queuedTimeoutInMinutes;
                                                                    buildComplete;
                                                                    initiator;
                                                                    vpcConfig;
                                                                    networkInterface;
                                                                    encryptionKey;
                                                                    exportedEnvironmentVariables;
                                                                    reportArns;
                                                                    fileSystemLocations;
                                                                    debugSession;
                                                                    buildBatchArn;
                                                                    autoRetryConfig
                                                                    }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("buildNumber", (Option.map x.buildNumber ~f:WrapperLong.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("currentPhase", (Option.map x.currentPhase ~f:String_.to_value));
        ("buildStatus", (Option.map x.buildStatus ~f:StatusType.to_value));
        ("sourceVersion",
          (Option.map x.sourceVersion ~f:NonEmptyString.to_value));
        ("resolvedSourceVersion",
          (Option.map x.resolvedSourceVersion ~f:NonEmptyString.to_value));
        ("projectName",
          (Option.map x.projectName ~f:NonEmptyString.to_value));
        ("phases", (Option.map x.phases ~f:BuildPhases.to_value));
        ("source", (Option.map x.source ~f:ProjectSource.to_value));
        ("secondarySources",
          (Option.map x.secondarySources ~f:ProjectSources.to_value));
        ("secondarySourceVersions",
          (Option.map x.secondarySourceVersions
             ~f:ProjectSecondarySourceVersions.to_value));
        ("artifacts", (Option.map x.artifacts ~f:BuildArtifacts.to_value));
        ("secondaryArtifacts",
          (Option.map x.secondaryArtifacts ~f:BuildArtifactsList.to_value));
        ("cache", (Option.map x.cache ~f:ProjectCache.to_value));
        ("environment",
          (Option.map x.environment ~f:ProjectEnvironment.to_value));
        ("serviceRole",
          (Option.map x.serviceRole ~f:NonEmptyString.to_value));
        ("logs", (Option.map x.logs ~f:LogsLocation.to_value));
        ("timeoutInMinutes",
          (Option.map x.timeoutInMinutes ~f:WrapperInt.to_value));
        ("queuedTimeoutInMinutes",
          (Option.map x.queuedTimeoutInMinutes ~f:WrapperInt.to_value));
        ("buildComplete", (Option.map x.buildComplete ~f:Boolean.to_value));
        ("initiator", (Option.map x.initiator ~f:String_.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("networkInterface",
          (Option.map x.networkInterface ~f:NetworkInterface.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("exportedEnvironmentVariables",
          (Option.map x.exportedEnvironmentVariables
             ~f:ExportedEnvironmentVariables.to_value));
        ("reportArns", (Option.map x.reportArns ~f:BuildReportArns.to_value));
        ("fileSystemLocations",
          (Option.map x.fileSystemLocations
             ~f:ProjectFileSystemLocations.to_value));
        ("debugSession",
          (Option.map x.debugSession ~f:DebugSession.to_value));
        ("buildBatchArn", (Option.map x.buildBatchArn ~f:String_.to_value));
        ("autoRetryConfig",
          (Option.map x.autoRetryConfig ~f:AutoRetryConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRetryConfig =
        (Option.map ~f:AutoRetryConfig.of_xml)
          (Xml.child xml_arg0 "autoRetryConfig") in
      let buildBatchArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "buildBatchArn") in
      let debugSession =
        (Option.map ~f:DebugSession.of_xml)
          (Xml.child xml_arg0 "debugSession") in
      let fileSystemLocations =
        (Option.map ~f:ProjectFileSystemLocations.of_xml)
          (Xml.child xml_arg0 "fileSystemLocations") in
      let reportArns =
        (Option.map ~f:BuildReportArns.of_xml)
          (Xml.child xml_arg0 "reportArns") in
      let exportedEnvironmentVariables =
        (Option.map ~f:ExportedEnvironmentVariables.of_xml)
          (Xml.child xml_arg0 "exportedEnvironmentVariables") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let networkInterface =
        (Option.map ~f:NetworkInterface.of_xml)
          (Xml.child xml_arg0 "networkInterface") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let initiator =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "initiator") in
      let buildComplete =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "buildComplete") in
      let queuedTimeoutInMinutes =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutes") in
      let timeoutInMinutes =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "timeoutInMinutes") in
      let logs =
        (Option.map ~f:LogsLocation.of_xml) (Xml.child xml_arg0 "logs") in
      let serviceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRole") in
      let environment =
        (Option.map ~f:ProjectEnvironment.of_xml)
          (Xml.child xml_arg0 "environment") in
      let cache =
        (Option.map ~f:ProjectCache.of_xml) (Xml.child xml_arg0 "cache") in
      let secondaryArtifacts =
        (Option.map ~f:BuildArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifacts") in
      let artifacts =
        (Option.map ~f:BuildArtifacts.of_xml)
          (Xml.child xml_arg0 "artifacts") in
      let secondarySourceVersions =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourceVersions") in
      let secondarySources =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySources") in
      let source =
        (Option.map ~f:ProjectSource.of_xml) (Xml.child xml_arg0 "source") in
      let phases =
        (Option.map ~f:BuildPhases.of_xml) (Xml.child xml_arg0 "phases") in
      let projectName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "projectName") in
      let resolvedSourceVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "resolvedSourceVersion") in
      let sourceVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "sourceVersion") in
      let buildStatus =
        (Option.map ~f:StatusType.of_xml) (Xml.child xml_arg0 "buildStatus") in
      let currentPhase =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currentPhase") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let buildNumber =
        (Option.map ~f:WrapperLong.of_xml) (Xml.child xml_arg0 "buildNumber") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?autoRetryConfig ?buildBatchArn ?debugSession ?fileSystemLocations
        ?reportArns ?exportedEnvironmentVariables ?encryptionKey
        ?networkInterface ?vpcConfig ?initiator ?buildComplete
        ?queuedTimeoutInMinutes ?timeoutInMinutes ?logs ?serviceRole
        ?environment ?cache ?secondaryArtifacts ?artifacts
        ?secondarySourceVersions ?secondarySources ?source ?phases
        ?projectName ?resolvedSourceVersion ?sourceVersion ?buildStatus
        ?currentPhase ?endTime ?startTime ?buildNumber ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let autoRetryConfig =
        field_map json__ "autoRetryConfig" AutoRetryConfig.of_json in
      let buildBatchArn = field_map json__ "buildBatchArn" String_.of_json in
      let debugSession = field_map json__ "debugSession" DebugSession.of_json in
      let fileSystemLocations =
        field_map json__ "fileSystemLocations"
          ProjectFileSystemLocations.of_json in
      let reportArns = field_map json__ "reportArns" BuildReportArns.of_json in
      let exportedEnvironmentVariables =
        field_map json__ "exportedEnvironmentVariables"
          ExportedEnvironmentVariables.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let networkInterface =
        field_map json__ "networkInterface" NetworkInterface.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let initiator = field_map json__ "initiator" String_.of_json in
      let buildComplete = field_map json__ "buildComplete" Boolean.of_json in
      let queuedTimeoutInMinutes =
        field_map json__ "queuedTimeoutInMinutes" WrapperInt.of_json in
      let timeoutInMinutes =
        field_map json__ "timeoutInMinutes" WrapperInt.of_json in
      let logs = field_map json__ "logs" LogsLocation.of_json in
      let serviceRole = field_map json__ "serviceRole" NonEmptyString.of_json in
      let environment =
        field_map json__ "environment" ProjectEnvironment.of_json in
      let cache = field_map json__ "cache" ProjectCache.of_json in
      let secondaryArtifacts =
        field_map json__ "secondaryArtifacts" BuildArtifactsList.of_json in
      let artifacts = field_map json__ "artifacts" BuildArtifacts.of_json in
      let secondarySourceVersions =
        field_map json__ "secondarySourceVersions"
          ProjectSecondarySourceVersions.of_json in
      let secondarySources =
        field_map json__ "secondarySources" ProjectSources.of_json in
      let source = field_map json__ "source" ProjectSource.of_json in
      let phases = field_map json__ "phases" BuildPhases.of_json in
      let projectName = field_map json__ "projectName" NonEmptyString.of_json in
      let resolvedSourceVersion =
        field_map json__ "resolvedSourceVersion" NonEmptyString.of_json in
      let sourceVersion =
        field_map json__ "sourceVersion" NonEmptyString.of_json in
      let buildStatus = field_map json__ "buildStatus" StatusType.of_json in
      let currentPhase = field_map json__ "currentPhase" String_.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let buildNumber = field_map json__ "buildNumber" WrapperLong.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?autoRetryConfig ?buildBatchArn ?debugSession ?fileSystemLocations
        ?reportArns ?exportedEnvironmentVariables ?encryptionKey
        ?networkInterface ?vpcConfig ?initiator ?buildComplete
        ?queuedTimeoutInMinutes ?timeoutInMinutes ?logs ?serviceRole
        ?environment ?cache ?secondaryArtifacts ?artifacts
        ?secondarySourceVersions ?secondarySources ?source ?phases
        ?projectName ?resolvedSourceVersion ?sourceVersion ?buildStatus
        ?currentPhase ?endTime ?startTime ?buildNumber ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a build."]
module BuildBatch =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc "The identifier of the batch build."];
      arn: NonEmptyString.t option [@ocaml.doc "The ARN of the batch build."];
      startTime: Timestamp.t option
        [@ocaml.doc "The date and time that the batch build started."];
      endTime: Timestamp.t option
        [@ocaml.doc "The date and time that the batch build ended."];
      currentPhase: String_.t option
        [@ocaml.doc "The current phase of the batch build."];
      buildBatchStatus: StatusType.t option
        [@ocaml.doc "The status of the batch build."];
      sourceVersion: NonEmptyString.t option
        [@ocaml.doc
          "The identifier of the version of the source code to be built."];
      resolvedSourceVersion: NonEmptyString.t option
        [@ocaml.doc
          "The identifier of the resolved version of this batch build's source code. For CodeCommit, GitHub, GitHub Enterprise, and BitBucket, the commit ID. For CodePipeline, the source revision provided by CodePipeline. For Amazon S3, this does not apply."];
      projectName: NonEmptyString.t option
        [@ocaml.doc "The name of the batch build project."];
      phases: BuildBatchPhases.t option
        [@ocaml.doc
          "An array of BuildBatchPhase objects the specify the phases of the batch build."];
      source: ProjectSource.t option ;
      secondarySources: ProjectSources.t option
        [@ocaml.doc
          "An array of ProjectSource objects that define the sources for the batch build."];
      secondarySourceVersions: ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects. Each ProjectSourceVersion must be one of: For CodeCommit: the commit ID, branch, or Git tag to use. For GitHub: the commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example, pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Bitbucket: the commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Amazon S3: the version ID of the object that represents the build input ZIP file to use."];
      artifacts: BuildArtifacts.t option
        [@ocaml.doc
          "A BuildArtifacts object the defines the build artifacts for this batch build."];
      secondaryArtifacts: BuildArtifactsList.t option
        [@ocaml.doc
          "An array of BuildArtifacts objects the define the build artifacts for this batch build."];
      cache: ProjectCache.t option ;
      environment: ProjectEnvironment.t option ;
      serviceRole: NonEmptyString.t option
        [@ocaml.doc
          "The name of a service role used for builds in the batch."];
      logConfig: LogsConfig.t option ;
      buildTimeoutInMinutes: WrapperInt.t option
        [@ocaml.doc
          "Specifies the maximum amount of time, in minutes, that the build in a batch must be completed in."];
      queuedTimeoutInMinutes: WrapperInt.t option
        [@ocaml.doc
          "Specifies the amount of time, in minutes, that the batch build is allowed to be queued before it times out."];
      complete: Boolean.t option
        [@ocaml.doc "Indicates if the batch build is complete."];
      initiator: String_.t option
        [@ocaml.doc
          "The entity that started the batch build. Valid values include: If CodePipeline started the build, the pipeline's name (for example, codepipeline/my-demo-pipeline). If a user started the build, the user's name. If the Jenkins plugin for CodeBuild started the build, the string CodeBuild-Jenkins-Plugin."];
      vpcConfig: VpcConfig.t option ;
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) to be used for encrypting the batch build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>)."];
      buildBatchNumber: WrapperLong.t option
        [@ocaml.doc
          "The number of the batch build. For each project, the buildBatchNumber of its first batch build is 1. The buildBatchNumber of each subsequent batch build is incremented by 1. If a batch build is deleted, the buildBatchNumber of other batch builds does not change."];
      fileSystemLocations: ProjectFileSystemLocations.t option
        [@ocaml.doc
          "An array of ProjectFileSystemLocation objects for the batch build project. A ProjectFileSystemLocation object specifies the identifier, location, mountOptions, mountPoint, and type of a file system created using Amazon Elastic File System."];
      buildBatchConfig: ProjectBuildBatchConfig.t option ;
      buildGroups: BuildGroups.t option
        [@ocaml.doc
          "An array of BuildGroup objects that define the build groups for the batch build."];
      debugSessionEnabled: WrapperBoolean.t option
        [@ocaml.doc
          "Specifies if session debugging is enabled for this batch build. For more information, see Viewing a running build in Session Manager. Batch session debugging is not supported for matrix batch builds."];
      reportArns: BuildReportArns.t option
        [@ocaml.doc
          "An array that contains the ARNs of reports created by merging reports from builds associated with this batch build."]}
    let make ?id =
      fun ?arn ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?currentPhase ->
              fun ?buildBatchStatus ->
                fun ?sourceVersion ->
                  fun ?resolvedSourceVersion ->
                    fun ?projectName ->
                      fun ?phases ->
                        fun ?source ->
                          fun ?secondarySources ->
                            fun ?secondarySourceVersions ->
                              fun ?artifacts ->
                                fun ?secondaryArtifacts ->
                                  fun ?cache ->
                                    fun ?environment ->
                                      fun ?serviceRole ->
                                        fun ?logConfig ->
                                          fun ?buildTimeoutInMinutes ->
                                            fun ?queuedTimeoutInMinutes ->
                                              fun ?complete ->
                                                fun ?initiator ->
                                                  fun ?vpcConfig ->
                                                    fun ?encryptionKey ->
                                                      fun ?buildBatchNumber
                                                        ->
                                                        fun
                                                          ?fileSystemLocations
                                                          ->
                                                          fun
                                                            ?buildBatchConfig
                                                            ->
                                                            fun ?buildGroups
                                                              ->
                                                              fun
                                                                ?debugSessionEnabled
                                                                ->
                                                                fun
                                                                  ?reportArns
                                                                  ->
                                                                  fun () ->
                                                                    {
                                                                    id;
                                                                    arn;
                                                                    startTime;
                                                                    endTime;
                                                                    currentPhase;
                                                                    buildBatchStatus;
                                                                    sourceVersion;
                                                                    resolvedSourceVersion;
                                                                    projectName;
                                                                    phases;
                                                                    source;
                                                                    secondarySources;
                                                                    secondarySourceVersions;
                                                                    artifacts;
                                                                    secondaryArtifacts;
                                                                    cache;
                                                                    environment;
                                                                    serviceRole;
                                                                    logConfig;
                                                                    buildTimeoutInMinutes;
                                                                    queuedTimeoutInMinutes;
                                                                    complete;
                                                                    initiator;
                                                                    vpcConfig;
                                                                    encryptionKey;
                                                                    buildBatchNumber;
                                                                    fileSystemLocations;
                                                                    buildBatchConfig;
                                                                    buildGroups;
                                                                    debugSessionEnabled;
                                                                    reportArns
                                                                    }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("arn", (Option.map x.arn ~f:NonEmptyString.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("currentPhase", (Option.map x.currentPhase ~f:String_.to_value));
        ("buildBatchStatus",
          (Option.map x.buildBatchStatus ~f:StatusType.to_value));
        ("sourceVersion",
          (Option.map x.sourceVersion ~f:NonEmptyString.to_value));
        ("resolvedSourceVersion",
          (Option.map x.resolvedSourceVersion ~f:NonEmptyString.to_value));
        ("projectName",
          (Option.map x.projectName ~f:NonEmptyString.to_value));
        ("phases", (Option.map x.phases ~f:BuildBatchPhases.to_value));
        ("source", (Option.map x.source ~f:ProjectSource.to_value));
        ("secondarySources",
          (Option.map x.secondarySources ~f:ProjectSources.to_value));
        ("secondarySourceVersions",
          (Option.map x.secondarySourceVersions
             ~f:ProjectSecondarySourceVersions.to_value));
        ("artifacts", (Option.map x.artifacts ~f:BuildArtifacts.to_value));
        ("secondaryArtifacts",
          (Option.map x.secondaryArtifacts ~f:BuildArtifactsList.to_value));
        ("cache", (Option.map x.cache ~f:ProjectCache.to_value));
        ("environment",
          (Option.map x.environment ~f:ProjectEnvironment.to_value));
        ("serviceRole",
          (Option.map x.serviceRole ~f:NonEmptyString.to_value));
        ("logConfig", (Option.map x.logConfig ~f:LogsConfig.to_value));
        ("buildTimeoutInMinutes",
          (Option.map x.buildTimeoutInMinutes ~f:WrapperInt.to_value));
        ("queuedTimeoutInMinutes",
          (Option.map x.queuedTimeoutInMinutes ~f:WrapperInt.to_value));
        ("complete", (Option.map x.complete ~f:Boolean.to_value));
        ("initiator", (Option.map x.initiator ~f:String_.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("buildBatchNumber",
          (Option.map x.buildBatchNumber ~f:WrapperLong.to_value));
        ("fileSystemLocations",
          (Option.map x.fileSystemLocations
             ~f:ProjectFileSystemLocations.to_value));
        ("buildBatchConfig",
          (Option.map x.buildBatchConfig ~f:ProjectBuildBatchConfig.to_value));
        ("buildGroups", (Option.map x.buildGroups ~f:BuildGroups.to_value));
        ("debugSessionEnabled",
          (Option.map x.debugSessionEnabled ~f:WrapperBoolean.to_value));
        ("reportArns", (Option.map x.reportArns ~f:BuildReportArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportArns =
        (Option.map ~f:BuildReportArns.of_xml)
          (Xml.child xml_arg0 "reportArns") in
      let debugSessionEnabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "debugSessionEnabled") in
      let buildGroups =
        (Option.map ~f:BuildGroups.of_xml) (Xml.child xml_arg0 "buildGroups") in
      let buildBatchConfig =
        (Option.map ~f:ProjectBuildBatchConfig.of_xml)
          (Xml.child xml_arg0 "buildBatchConfig") in
      let fileSystemLocations =
        (Option.map ~f:ProjectFileSystemLocations.of_xml)
          (Xml.child xml_arg0 "fileSystemLocations") in
      let buildBatchNumber =
        (Option.map ~f:WrapperLong.of_xml)
          (Xml.child xml_arg0 "buildBatchNumber") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let initiator =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "initiator") in
      let complete =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "complete") in
      let queuedTimeoutInMinutes =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutes") in
      let buildTimeoutInMinutes =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "buildTimeoutInMinutes") in
      let logConfig =
        (Option.map ~f:LogsConfig.of_xml) (Xml.child xml_arg0 "logConfig") in
      let serviceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRole") in
      let environment =
        (Option.map ~f:ProjectEnvironment.of_xml)
          (Xml.child xml_arg0 "environment") in
      let cache =
        (Option.map ~f:ProjectCache.of_xml) (Xml.child xml_arg0 "cache") in
      let secondaryArtifacts =
        (Option.map ~f:BuildArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifacts") in
      let artifacts =
        (Option.map ~f:BuildArtifacts.of_xml)
          (Xml.child xml_arg0 "artifacts") in
      let secondarySourceVersions =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourceVersions") in
      let secondarySources =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySources") in
      let source =
        (Option.map ~f:ProjectSource.of_xml) (Xml.child xml_arg0 "source") in
      let phases =
        (Option.map ~f:BuildBatchPhases.of_xml) (Xml.child xml_arg0 "phases") in
      let projectName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "projectName") in
      let resolvedSourceVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "resolvedSourceVersion") in
      let sourceVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "sourceVersion") in
      let buildBatchStatus =
        (Option.map ~f:StatusType.of_xml)
          (Xml.child xml_arg0 "buildBatchStatus") in
      let currentPhase =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currentPhase") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?reportArns ?debugSessionEnabled ?buildGroups ?buildBatchConfig
        ?fileSystemLocations ?buildBatchNumber ?encryptionKey ?vpcConfig
        ?initiator ?complete ?queuedTimeoutInMinutes ?buildTimeoutInMinutes
        ?logConfig ?serviceRole ?environment ?cache ?secondaryArtifacts
        ?artifacts ?secondarySourceVersions ?secondarySources ?source ?phases
        ?projectName ?resolvedSourceVersion ?sourceVersion ?buildBatchStatus
        ?currentPhase ?endTime ?startTime ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportArns = field_map json__ "reportArns" BuildReportArns.of_json in
      let debugSessionEnabled =
        field_map json__ "debugSessionEnabled" WrapperBoolean.of_json in
      let buildGroups = field_map json__ "buildGroups" BuildGroups.of_json in
      let buildBatchConfig =
        field_map json__ "buildBatchConfig" ProjectBuildBatchConfig.of_json in
      let fileSystemLocations =
        field_map json__ "fileSystemLocations"
          ProjectFileSystemLocations.of_json in
      let buildBatchNumber =
        field_map json__ "buildBatchNumber" WrapperLong.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let initiator = field_map json__ "initiator" String_.of_json in
      let complete = field_map json__ "complete" Boolean.of_json in
      let queuedTimeoutInMinutes =
        field_map json__ "queuedTimeoutInMinutes" WrapperInt.of_json in
      let buildTimeoutInMinutes =
        field_map json__ "buildTimeoutInMinutes" WrapperInt.of_json in
      let logConfig = field_map json__ "logConfig" LogsConfig.of_json in
      let serviceRole = field_map json__ "serviceRole" NonEmptyString.of_json in
      let environment =
        field_map json__ "environment" ProjectEnvironment.of_json in
      let cache = field_map json__ "cache" ProjectCache.of_json in
      let secondaryArtifacts =
        field_map json__ "secondaryArtifacts" BuildArtifactsList.of_json in
      let artifacts = field_map json__ "artifacts" BuildArtifacts.of_json in
      let secondarySourceVersions =
        field_map json__ "secondarySourceVersions"
          ProjectSecondarySourceVersions.of_json in
      let secondarySources =
        field_map json__ "secondarySources" ProjectSources.of_json in
      let source = field_map json__ "source" ProjectSource.of_json in
      let phases = field_map json__ "phases" BuildBatchPhases.of_json in
      let projectName = field_map json__ "projectName" NonEmptyString.of_json in
      let resolvedSourceVersion =
        field_map json__ "resolvedSourceVersion" NonEmptyString.of_json in
      let sourceVersion =
        field_map json__ "sourceVersion" NonEmptyString.of_json in
      let buildBatchStatus =
        field_map json__ "buildBatchStatus" StatusType.of_json in
      let currentPhase = field_map json__ "currentPhase" String_.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let arn = field_map json__ "arn" NonEmptyString.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?reportArns ?debugSessionEnabled ?buildGroups ?buildBatchConfig
        ?fileSystemLocations ?buildBatchNumber ?encryptionKey ?vpcConfig
        ?initiator ?complete ?queuedTimeoutInMinutes ?buildTimeoutInMinutes
        ?logConfig ?serviceRole ?environment ?cache ?secondaryArtifacts
        ?artifacts ?secondarySourceVersions ?secondarySources ?source ?phases
        ?projectName ?resolvedSourceVersion ?sourceVersion ?buildBatchStatus
        ?currentPhase ?endTime ?startTime ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about a batch build."]
module InvalidInputException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input value that was provided is not valid."]
module OAuthProviderException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "There was a problem with the underlying OAuth provider."]
module ResourceNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified Amazon Web Services resource cannot be found."]
module AccountLimitExceededException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Web Services service limit was exceeded for the calling Amazon Web Services account."]
module ScalingConfigurationInput =
  struct
    type nonrec t =
      {
      scalingType: FleetScalingType.t option
        [@ocaml.doc "The scaling type for a compute fleet."];
      targetTrackingScalingConfigs:
        TargetTrackingScalingConfigurations.t option
        [@ocaml.doc "A list of TargetTrackingScalingConfiguration objects."];
      maxCapacity: FleetCapacity.t option
        [@ocaml.doc
          "The maximum number of instances in the \239\172\130eet when auto-scaling."]}
    let make ?scalingType =
      fun ?targetTrackingScalingConfigs ->
        fun ?maxCapacity ->
          fun () ->
            { scalingType; targetTrackingScalingConfigs; maxCapacity }
    let to_value x =
      structure_to_value
        [("scalingType",
           (Option.map x.scalingType ~f:FleetScalingType.to_value));
        ("targetTrackingScalingConfigs",
          (Option.map x.targetTrackingScalingConfigs
             ~f:TargetTrackingScalingConfigurations.to_value));
        ("maxCapacity", (Option.map x.maxCapacity ~f:FleetCapacity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxCapacity =
        (Option.map ~f:FleetCapacity.of_xml)
          (Xml.child xml_arg0 "maxCapacity") in
      let targetTrackingScalingConfigs =
        (Option.map ~f:TargetTrackingScalingConfigurations.of_xml)
          (Xml.child xml_arg0 "targetTrackingScalingConfigs") in
      let scalingType =
        (Option.map ~f:FleetScalingType.of_xml)
          (Xml.child xml_arg0 "scalingType") in
      make ?maxCapacity ?targetTrackingScalingConfigs ?scalingType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxCapacity = field_map json__ "maxCapacity" FleetCapacity.of_json in
      let targetTrackingScalingConfigs =
        field_map json__ "targetTrackingScalingConfigs"
          TargetTrackingScalingConfigurations.of_json in
      let scalingType =
        field_map json__ "scalingType" FleetScalingType.of_json in
      make ?maxCapacity ?targetTrackingScalingConfigs ?scalingType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The scaling configuration input of a compute fleet."]
module AccountSuspendedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The CodeBuild access has been suspended for the calling Amazon Web Services account."]
module SensitiveString =
  struct
    type nonrec t = string
    let context_ = "SensitiveString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveString" j
    let to_json = simple_to_json to_value
  end
module SSMSession =
  struct
    type nonrec t =
      {
      sessionId: String_.t option [@ocaml.doc "The ID of the session."];
      tokenValue: String_.t option
        [@ocaml.doc
          "An encrypted token value containing session and caller information."];
      streamUrl: String_.t option
        [@ocaml.doc
          "A URL back to SSM Agent on the managed node that the Session Manager client uses to send commands and receive output from the node."]}
    let make ?sessionId =
      fun ?tokenValue ->
        fun ?streamUrl -> fun () -> { sessionId; tokenValue; streamUrl }
    let to_value x =
      structure_to_value
        [("sessionId", (Option.map x.sessionId ~f:String_.to_value));
        ("tokenValue", (Option.map x.tokenValue ~f:String_.to_value));
        ("streamUrl", (Option.map x.streamUrl ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "streamUrl") in
      let tokenValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "tokenValue") in
      let sessionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionId") in
      make ?streamUrl ?tokenValue ?sessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamUrl = field_map json__ "streamUrl" String_.of_json in
      let tokenValue = field_map json__ "tokenValue" String_.of_json in
      let sessionId = field_map json__ "sessionId" String_.of_json in
      make ?streamUrl ?tokenValue ?sessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about the Session Manager session."]
module RetryBuildBatchType =
  struct
    type nonrec t =
      | RETRY_ALL_BUILDS 
      | RETRY_FAILED_BUILDS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RETRY_ALL_BUILDS -> "RETRY_ALL_BUILDS"
      | RETRY_FAILED_BUILDS -> "RETRY_FAILED_BUILDS"
      | Non_static_id s -> s
    let of_string =
      function
      | "RETRY_ALL_BUILDS" -> RETRY_ALL_BUILDS
      | "RETRY_FAILED_BUILDS" -> RETRY_FAILED_BUILDS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RetryBuildBatchType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RetryBuildBatchType" j)
    let to_json = simple_to_json to_value
  end
module SourceCredentialsInfos =
  struct
    type nonrec t = SourceCredentialsInfo.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SourceCredentialsInfo.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SourceCredentialsInfo.of_xml)
    let of_json j =
      list_of_json ~kind:"SourceCredentialsInfos"
        ~of_json:SourceCredentialsInfo.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportGroupArns =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportGroupArns" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module PageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for PageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SharedResourceSortByType =
  struct
    type nonrec t =
      | ARN 
      | MODIFIED_TIME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ARN -> "ARN"
      | MODIFIED_TIME -> "MODIFIED_TIME"
      | Non_static_id s -> s
    let of_string =
      function
      | "ARN" -> ARN
      | "MODIFIED_TIME" -> MODIFIED_TIME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SharedResourceSortByType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SharedResourceSortByType" j)
    let to_json = simple_to_json to_value
  end
module SortOrderType =
  struct
    type nonrec t =
      | ASCENDING 
      | DESCENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASCENDING -> "ASCENDING"
      | DESCENDING -> "DESCENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASCENDING" -> ASCENDING
      | "DESCENDING" -> DESCENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrderType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrderType" j)
    let to_json = simple_to_json to_value
  end
module ProjectArns =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectArns" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module SandboxIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"SandboxIds" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportArns =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportArns" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportFilter =
  struct
    type nonrec t =
      {
      status: ReportStatusType.t option
        [@ocaml.doc
          "The status used to filter reports. You can filter using one status only."]}
    let make ?status = fun () -> { status }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ReportStatusType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ReportStatusType.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" ReportStatusType.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A filter used to return reports with the status specified by the input status parameter."]
module ReportGroupSortByType =
  struct
    type nonrec t =
      | NAME 
      | CREATED_TIME 
      | LAST_MODIFIED_TIME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NAME -> "NAME"
      | CREATED_TIME -> "CREATED_TIME"
      | LAST_MODIFIED_TIME -> "LAST_MODIFIED_TIME"
      | Non_static_id s -> s
    let of_string =
      function
      | "NAME" -> NAME
      | "CREATED_TIME" -> CREATED_TIME
      | "LAST_MODIFIED_TIME" -> LAST_MODIFIED_TIME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReportGroupSortByType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ReportGroupSortByType" j)
    let to_json = simple_to_json to_value
  end
module ProjectNames =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectNames" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProjectSortByType =
  struct
    type nonrec t =
      | NAME 
      | CREATED_TIME 
      | LAST_MODIFIED_TIME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NAME -> "NAME"
      | CREATED_TIME -> "CREATED_TIME"
      | LAST_MODIFIED_TIME -> "LAST_MODIFIED_TIME"
      | Non_static_id s -> s
    let of_string =
      function
      | "NAME" -> NAME
      | "CREATED_TIME" -> CREATED_TIME
      | "LAST_MODIFIED_TIME" -> LAST_MODIFIED_TIME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProjectSortByType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProjectSortByType" j)
    let to_json = simple_to_json to_value
  end
module FleetArns =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetArns" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetSortByType =
  struct
    type nonrec t =
      | NAME 
      | CREATED_TIME 
      | LAST_MODIFIED_TIME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NAME -> "NAME"
      | CREATED_TIME -> "CREATED_TIME"
      | LAST_MODIFIED_TIME -> "LAST_MODIFIED_TIME"
      | Non_static_id s -> s
    let of_string =
      function
      | "NAME" -> NAME
      | "CREATED_TIME" -> CREATED_TIME
      | "LAST_MODIFIED_TIME" -> LAST_MODIFIED_TIME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetSortByType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetSortByType" j)
    let to_json = simple_to_json to_value
  end
module EnvironmentPlatforms =
  struct
    type nonrec t = EnvironmentPlatform.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EnvironmentPlatform.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnvironmentPlatform.of_xml)
    let of_json j =
      list_of_json ~kind:"EnvironmentPlatforms"
        ~of_json:EnvironmentPlatform.of_json j
    let to_json v = composed_to_json to_value v
  end
module CommandExecutions =
  struct
    type nonrec t = CommandExecution.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CommandExecution.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CommandExecution.of_xml)
    let of_json j =
      list_of_json ~kind:"CommandExecutions"
        ~of_json:CommandExecution.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildIds" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildBatchIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildBatchIds" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildBatchFilter =
  struct
    type nonrec t =
      {
      status: StatusType.t option
        [@ocaml.doc
          "The status of the batch builds to retrieve. Only batch builds that have this status will be retrieved."]}
    let make ?status = fun () -> { status }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:StatusType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:StatusType.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" StatusType.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies filters when retrieving batch builds."]
module ResourceAlreadyExistsException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified Amazon Web Services resource cannot be created, because an Amazon Web Services resource with the same settings already exists."]
module ReportGroupTrendRawDataList =
  struct
    type nonrec t = ReportWithRawData.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ReportWithRawData.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReportWithRawData.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportGroupTrendRawDataList"
        ~of_json:ReportWithRawData.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportGroupTrendStats =
  struct
    type nonrec t =
      {
      average: String_.t option
        [@ocaml.doc "Contains the average of all values analyzed."];
      max: String_.t option
        [@ocaml.doc "Contains the maximum value analyzed."];
      min: String_.t option
        [@ocaml.doc "Contains the minimum value analyzed."]}
    let make ?average =
      fun ?max -> fun ?min -> fun () -> { average; max; min }
    let to_value x =
      structure_to_value
        [("average", (Option.map x.average ~f:String_.to_value));
        ("max", (Option.map x.max ~f:String_.to_value));
        ("min", (Option.map x.min ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let min = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "min") in
      let max = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "max") in
      let average =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "average") in
      make ?min ?max ?average ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let min = field_map json__ "min" String_.of_json in
      let max = field_map json__ "max" String_.of_json in
      let average = field_map json__ "average" String_.of_json in
      make ?min ?max ?average ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains trend statistics for a set of reports. The actual values depend on the type of trend being collected. For more information, see ."]
module ReportGroupTrendFieldType =
  struct
    type nonrec t =
      | PASS_RATE 
      | DURATION 
      | TOTAL 
      | LINE_COVERAGE 
      | LINES_COVERED 
      | LINES_MISSED 
      | BRANCH_COVERAGE 
      | BRANCHES_COVERED 
      | BRANCHES_MISSED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PASS_RATE -> "PASS_RATE"
      | DURATION -> "DURATION"
      | TOTAL -> "TOTAL"
      | LINE_COVERAGE -> "LINE_COVERAGE"
      | LINES_COVERED -> "LINES_COVERED"
      | LINES_MISSED -> "LINES_MISSED"
      | BRANCH_COVERAGE -> "BRANCH_COVERAGE"
      | BRANCHES_COVERED -> "BRANCHES_COVERED"
      | BRANCHES_MISSED -> "BRANCHES_MISSED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PASS_RATE" -> PASS_RATE
      | "DURATION" -> DURATION
      | "TOTAL" -> TOTAL
      | "LINE_COVERAGE" -> LINE_COVERAGE
      | "LINES_COVERED" -> LINES_COVERED
      | "LINES_MISSED" -> LINES_MISSED
      | "BRANCH_COVERAGE" -> BRANCH_COVERAGE
      | "BRANCHES_COVERED" -> BRANCHES_COVERED
      | "BRANCHES_MISSED" -> BRANCHES_MISSED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReportGroupTrendFieldType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ReportGroupTrendFieldType" j)
    let to_json = simple_to_json to_value
  end
module TestCases =
  struct
    type nonrec t = TestCase.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TestCase.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TestCase.of_xml)
    let of_json j =
      list_of_json ~kind:"TestCases" ~of_json:TestCase.of_json j
    let to_json v = composed_to_json to_value v
  end
module TestCaseFilter =
  struct
    type nonrec t =
      {
      status: String_.t option
        [@ocaml.doc
          "The status used to filter test cases. A TestCaseFilter can have one status. Valid values are: SUCCEEDED FAILED ERROR SKIPPED UNKNOWN"];
      keyword: String_.t option
        [@ocaml.doc
          "A keyword that is used to filter on the name or the prefix of the test cases. Only test cases where the keyword is a substring of the name or the prefix will be returned."]}
    let make ?status = fun ?keyword -> fun () -> { status; keyword }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:String_.to_value));
        ("keyword", (Option.map x.keyword ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyword =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "keyword") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      make ?keyword ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let keyword = field_map json__ "keyword" String_.of_json in
      let status = field_map json__ "status" String_.of_json in
      make ?keyword ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A filter used to return specific types of test cases. In order to pass the filter, the report must meet all of the filter properties."]
module CodeCoverages =
  struct
    type nonrec t = CodeCoverage.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CodeCoverage.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CodeCoverage.of_xml)
    let of_json j =
      list_of_json ~kind:"CodeCoverages" ~of_json:CodeCoverage.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportCodeCoverageSortByType =
  struct
    type nonrec t =
      | LINE_COVERAGE_PERCENTAGE 
      | FILE_PATH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LINE_COVERAGE_PERCENTAGE -> "LINE_COVERAGE_PERCENTAGE"
      | FILE_PATH -> "FILE_PATH"
      | Non_static_id s -> s
    let of_string =
      function
      | "LINE_COVERAGE_PERCENTAGE" -> LINE_COVERAGE_PERCENTAGE
      | "FILE_PATH" -> FILE_PATH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReportCodeCoverageSortByType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ReportCodeCoverageSortByType" j)
    let to_json = simple_to_json to_value
  end
module BuildsNotDeleted =
  struct
    type nonrec t = BuildNotDeleted.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildNotDeleted.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildNotDeleted.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildsNotDeleted" ~of_json:BuildNotDeleted.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Sandboxes =
  struct
    type nonrec t = Sandbox.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Sandbox.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Sandbox.of_xml)
    let of_json j = list_of_json ~kind:"Sandboxes" ~of_json:Sandbox.of_json j
    let to_json v = composed_to_json to_value v
  end
module Reports =
  struct
    type nonrec t = Report.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Report.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Report.of_xml)
    let of_json j = list_of_json ~kind:"Reports" ~of_json:Report.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportGroups =
  struct
    type nonrec t = ReportGroup.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ReportGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReportGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportGroups" ~of_json:ReportGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module Projects =
  struct
    type nonrec t = Project.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Project.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Project.of_xml)
    let of_json j = list_of_json ~kind:"Projects" ~of_json:Project.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetNames =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetNames" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module Fleets =
  struct
    type nonrec t = Fleet.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Fleet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Fleet.of_xml)
    let of_json j = list_of_json ~kind:"Fleets" ~of_json:Fleet.of_json j
    let to_json v = composed_to_json to_value v
  end
module CommandExecutionIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"CommandExecutionIds"
        ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module Builds =
  struct
    type nonrec t = Build.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Build.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Build.of_xml)
    let of_json j = list_of_json ~kind:"Builds" ~of_json:Build.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildBatches =
  struct
    type nonrec t = BuildBatch.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BuildBatch.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BuildBatch.of_xml)
    let of_json j =
      list_of_json ~kind:"BuildBatches" ~of_json:BuildBatch.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateWebhookOutput =
  struct
    type nonrec t =
      {
      webhook: Webhook.t option
        [@ocaml.doc
          "Information about a repository's webhook that is associated with a project in CodeBuild."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `OAuthProviderException of OAuthProviderException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?webhook = fun () -> { webhook }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "OAuthProviderException" ->
          `OAuthProviderException (OAuthProviderException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "OAuthProviderException" ->
          `OAuthProviderException (OAuthProviderException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `OAuthProviderException e ->
          `Assoc
            [("error", (`String "OAuthProviderException"));
            ("details", (OAuthProviderException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("webhook", (Option.map x.webhook ~f:Webhook.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let webhook =
        (Option.map ~f:Webhook.of_xml) (Xml.child xml_arg0 "webhook") in
      make ?webhook ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let webhook = field_map json__ "webhook" Webhook.of_json in
      make ?webhook ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the webhook associated with an CodeBuild build project. If you use Bitbucket for your repository, rotateSecret is ignored."]
module UpdateWebhookInput =
  struct
    type nonrec t =
      {
      projectName: ProjectName.t
        [@ocaml.doc "The name of the CodeBuild project."];
      branchFilter: String_.t option
        [@ocaml.doc
          "A regular expression used to determine which repository branches are built when a webhook is triggered. If the name of a branch matches the regular expression, then it is built. If branchFilter is empty, then all branches are built. It is recommended that you use filterGroups instead of branchFilter."];
      rotateSecret: Boolean.t option
        [@ocaml.doc
          "A boolean value that specifies whether the associated GitHub repository's secret token should be updated. If you use Bitbucket for your repository, rotateSecret is ignored."];
      filterGroups: FilterGroups.t option
        [@ocaml.doc
          "An array of arrays of WebhookFilter objects used to determine if a webhook event can trigger a build. A filter group must contain at least one EVENT WebhookFilter."];
      buildType: WebhookBuildType.t option
        [@ocaml.doc
          "Specifies the type of build this webhook will trigger. RUNNER_BUILDKITE_BUILD is only available for NO_SOURCE source type projects configured for Buildkite runner builds. For more information about CodeBuild-hosted Buildkite runner builds, see Tutorial: Configure a CodeBuild-hosted Buildkite runner in the CodeBuild user guide."];
      pullRequestBuildPolicy: PullRequestBuildPolicy.t option
        [@ocaml.doc
          "A PullRequestBuildPolicy object that defines comment-based approval requirements for triggering builds on pull requests. This policy helps control when automated builds are executed based on contributor permissions and approval workflows."]}
    let context_ = "UpdateWebhookInput"
    let make ?branchFilter =
      fun ?rotateSecret ->
        fun ?filterGroups ->
          fun ?buildType ->
            fun ?pullRequestBuildPolicy ->
              fun ~projectName ->
                fun () ->
                  {
                    branchFilter;
                    rotateSecret;
                    filterGroups;
                    buildType;
                    pullRequestBuildPolicy;
                    projectName
                  }
    let to_value x =
      structure_to_value
        [("projectName", (Some (ProjectName.to_value x.projectName)));
        ("branchFilter", (Option.map x.branchFilter ~f:String_.to_value));
        ("rotateSecret", (Option.map x.rotateSecret ~f:Boolean.to_value));
        ("filterGroups",
          (Option.map x.filterGroups ~f:FilterGroups.to_value));
        ("buildType", (Option.map x.buildType ~f:WebhookBuildType.to_value));
        ("pullRequestBuildPolicy",
          (Option.map x.pullRequestBuildPolicy
             ~f:PullRequestBuildPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pullRequestBuildPolicy =
        (Option.map ~f:PullRequestBuildPolicy.of_xml)
          (Xml.child xml_arg0 "pullRequestBuildPolicy") in
      let buildType =
        (Option.map ~f:WebhookBuildType.of_xml)
          (Xml.child xml_arg0 "buildType") in
      let filterGroups =
        (Option.map ~f:FilterGroups.of_xml)
          (Xml.child xml_arg0 "filterGroups") in
      let rotateSecret =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "rotateSecret") in
      let branchFilter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "branchFilter") in
      let projectName =
        ProjectName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ?pullRequestBuildPolicy ?buildType ?filterGroups ?rotateSecret
        ?branchFilter ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pullRequestBuildPolicy =
        field_map json__ "pullRequestBuildPolicy"
          PullRequestBuildPolicy.of_json in
      let buildType = field_map json__ "buildType" WebhookBuildType.of_json in
      let filterGroups = field_map json__ "filterGroups" FilterGroups.of_json in
      let rotateSecret = field_map json__ "rotateSecret" Boolean.of_json in
      let branchFilter = field_map json__ "branchFilter" String_.of_json in
      let projectName =
        field_map_exn json__ "projectName" ProjectName.of_json in
      make ?pullRequestBuildPolicy ?buildType ?filterGroups ?rotateSecret
        ?branchFilter ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the webhook associated with an CodeBuild build project. If you use Bitbucket for your repository, rotateSecret is ignored."]
module UpdateReportGroupOutput =
  struct
    type nonrec t =
      {
      reportGroup: ReportGroup.t option
        [@ocaml.doc "Information about the updated report group."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportGroup = fun () -> { reportGroup }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reportGroup", (Option.map x.reportGroup ~f:ReportGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportGroup =
        (Option.map ~f:ReportGroup.of_xml) (Xml.child xml_arg0 "reportGroup") in
      make ?reportGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportGroup = field_map json__ "reportGroup" ReportGroup.of_json in
      make ?reportGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a report group."]
module UpdateReportGroupInput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t
        [@ocaml.doc "The ARN of the report group to update."];
      exportConfig: ReportExportConfig.t option
        [@ocaml.doc
          "Used to specify an updated export type. Valid values are: S3: The report results are exported to an S3 bucket. NO_EXPORT: The report results are not exported."];
      tags: TagList.t option
        [@ocaml.doc
          "An updated list of tag key and value pairs associated with this report group. These tags are available for use by Amazon Web Services services that support CodeBuild report group tags."]}
    let context_ = "UpdateReportGroupInput"
    let make ?exportConfig =
      fun ?tags -> fun ~arn -> fun () -> { exportConfig; tags; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (NonEmptyString.to_value x.arn)));
        ("exportConfig",
          (Option.map x.exportConfig ~f:ReportExportConfig.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let exportConfig =
        (Option.map ~f:ReportExportConfig.of_xml)
          (Xml.child xml_arg0 "exportConfig") in
      let arn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?tags ?exportConfig ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let exportConfig =
        field_map json__ "exportConfig" ReportExportConfig.of_json in
      let arn = field_map_exn json__ "arn" NonEmptyString.of_json in
      make ?tags ?exportConfig ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a report group."]
module UpdateProjectVisibilityOutput =
  struct
    type nonrec t =
      {
      projectArn: NonEmptyString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the build project."];
      publicProjectAlias: NonEmptyString.t option
        [@ocaml.doc
          "Contains the project identifier used with the public build APIs."];
      projectVisibility: ProjectVisibilityType.t option }
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?projectArn =
      fun ?publicProjectAlias ->
        fun ?projectVisibility ->
          fun () -> { projectArn; publicProjectAlias; projectVisibility }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("projectArn", (Option.map x.projectArn ~f:NonEmptyString.to_value));
        ("publicProjectAlias",
          (Option.map x.publicProjectAlias ~f:NonEmptyString.to_value));
        ("projectVisibility",
          (Option.map x.projectVisibility ~f:ProjectVisibilityType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectVisibility =
        (Option.map ~f:ProjectVisibilityType.of_xml)
          (Xml.child xml_arg0 "projectVisibility") in
      let publicProjectAlias =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "publicProjectAlias") in
      let projectArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "projectArn") in
      make ?projectVisibility ?publicProjectAlias ?projectArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let projectVisibility =
        field_map json__ "projectVisibility" ProjectVisibilityType.of_json in
      let publicProjectAlias =
        field_map json__ "publicProjectAlias" NonEmptyString.of_json in
      let projectArn = field_map json__ "projectArn" NonEmptyString.of_json in
      make ?projectVisibility ?publicProjectAlias ?projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the public visibility for a project. The project's build results, logs, and artifacts are available to the general public. For more information, see Public build projects in the CodeBuild User Guide. The following should be kept in mind when making your projects public: All of a project's build results, logs, and artifacts, including builds that were run when the project was private, are available to the general public. All build logs and artifacts are available to the public. Environment variables, source code, and other sensitive information may have been output to the build logs and artifacts. You must be careful about what information is output to the build logs. Some best practice are: Do not store sensitive values in environment variables. We recommend that you use an Amazon EC2 Systems Manager Parameter Store or Secrets Manager to store sensitive values. Follow Best practices for using webhooks in the CodeBuild User Guide to limit which entities can trigger a build, and do not store the buildspec in the project itself, to ensure that your webhooks are as secure as possible. A malicious user can use public builds to distribute malicious artifacts. We recommend that you review all pull requests to verify that the pull request is a legitimate change. We also recommend that you validate any artifacts with their checksums to make sure that the correct artifacts are being downloaded."]
module UpdateProjectVisibilityInput =
  struct
    type nonrec t =
      {
      projectArn: NonEmptyString.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the build project."];
      projectVisibility: ProjectVisibilityType.t ;
      resourceAccessRole: NonEmptyString.t option
        [@ocaml.doc
          "The ARN of the IAM role that enables CodeBuild to access the CloudWatch Logs and Amazon S3 artifacts for the project's builds."]}
    let context_ = "UpdateProjectVisibilityInput"
    let make ?resourceAccessRole =
      fun ~projectArn ->
        fun ~projectVisibility ->
          fun () -> { resourceAccessRole; projectArn; projectVisibility }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (NonEmptyString.to_value x.projectArn)));
        ("projectVisibility",
          (Some (ProjectVisibilityType.to_value x.projectVisibility)));
        ("resourceAccessRole",
          (Option.map x.resourceAccessRole ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceAccessRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "resourceAccessRole") in
      let projectVisibility =
        ProjectVisibilityType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectVisibility") in
      let projectArn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?resourceAccessRole ~projectVisibility ~projectArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceAccessRole =
        field_map json__ "resourceAccessRole" NonEmptyString.of_json in
      let projectVisibility =
        field_map_exn json__ "projectVisibility"
          ProjectVisibilityType.of_json in
      let projectArn =
        field_map_exn json__ "projectArn" NonEmptyString.of_json in
      make ?resourceAccessRole ~projectVisibility ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Changes the public visibility for a project. The project's build results, logs, and artifacts are available to the general public. For more information, see Public build projects in the CodeBuild User Guide. The following should be kept in mind when making your projects public: All of a project's build results, logs, and artifacts, including builds that were run when the project was private, are available to the general public. All build logs and artifacts are available to the public. Environment variables, source code, and other sensitive information may have been output to the build logs and artifacts. You must be careful about what information is output to the build logs. Some best practice are: Do not store sensitive values in environment variables. We recommend that you use an Amazon EC2 Systems Manager Parameter Store or Secrets Manager to store sensitive values. Follow Best practices for using webhooks in the CodeBuild User Guide to limit which entities can trigger a build, and do not store the buildspec in the project itself, to ensure that your webhooks are as secure as possible. A malicious user can use public builds to distribute malicious artifacts. We recommend that you review all pull requests to verify that the pull request is a legitimate change. We also recommend that you validate any artifacts with their checksums to make sure that the correct artifacts are being downloaded."]
module UpdateProjectOutput =
  struct
    type nonrec t =
      {
      project: Project.t option
        [@ocaml.doc "Information about the build project that was changed."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?project = fun () -> { project }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("project", (Option.map x.project ~f:Project.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let project =
        (Option.map ~f:Project.of_xml) (Xml.child xml_arg0 "project") in
      make ?project ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let project = field_map json__ "project" Project.of_json in
      make ?project ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Changes the settings of a build project."]
module UpdateProjectInput =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t
        [@ocaml.doc
          "The name of the build project. You cannot change a build project's name."];
      description: ProjectDescription.t option
        [@ocaml.doc "A new or replacement description of the build project."];
      source: ProjectSource.t option
        [@ocaml.doc
          "Information to be changed about the build input source code for the build project."];
      secondarySources: ProjectSources.t option
        [@ocaml.doc "An array of ProjectSource objects."];
      sourceVersion: String_.t option
        [@ocaml.doc
          "A version of the build input to be built for this project. If not specified, the latest version is used. If specified, it must be one of: For CodeCommit: the commit ID, branch, or Git tag to use. For GitHub: the commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For GitLab: the commit ID, branch, or Git tag to use. For Bitbucket: the commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Amazon S3: the version ID of the object that represents the build input ZIP file to use. If sourceVersion is specified at the build level, then that version takes precedence over this sourceVersion (at the project level). For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."];
      secondarySourceVersions: ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects. If secondarySourceVersions is specified at the build level, then they take over these secondarySourceVersions (at the project level)."];
      artifacts: ProjectArtifacts.t option
        [@ocaml.doc
          "Information to be changed about the build output artifacts for the build project."];
      secondaryArtifacts: ProjectArtifactsList.t option
        [@ocaml.doc "An array of ProjectArtifact objects."];
      cache: ProjectCache.t option
        [@ocaml.doc
          "Stores recently used information so that it can be quickly accessed at a later time."];
      environment: ProjectEnvironment.t option
        [@ocaml.doc
          "Information to be changed about the build environment for the build project."];
      serviceRole: NonEmptyString.t option
        [@ocaml.doc
          "The replacement ARN of the IAM role that enables CodeBuild to interact with dependent Amazon Web Services services on behalf of the Amazon Web Services account."];
      timeoutInMinutes: BuildTimeOut.t option
        [@ocaml.doc
          "The replacement value in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before timing out any related build that did not get marked as completed."];
      queuedTimeoutInMinutes: TimeOut.t option
        [@ocaml.doc
          "The number of minutes a build is allowed to be queued before it times out."];
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) to be used for encrypting the build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>)."];
      tags: TagList.t option
        [@ocaml.doc
          "An updated list of tag key and value pairs associated with this build project. These tags are available for use by Amazon Web Services services that support CodeBuild build project tags."];
      vpcConfig: VpcConfig.t option
        [@ocaml.doc
          "VpcConfig enables CodeBuild to access resources in an Amazon VPC."];
      badgeEnabled: WrapperBoolean.t option
        [@ocaml.doc
          "Set this to true to generate a publicly accessible URL for your project's build badge."];
      logsConfig: LogsConfig.t option
        [@ocaml.doc
          "Information about logs for the build project. A project can create logs in CloudWatch Logs, logs in an S3 bucket, or both."];
      fileSystemLocations: ProjectFileSystemLocations.t option
        [@ocaml.doc
          "An array of ProjectFileSystemLocation objects for a CodeBuild build project. A ProjectFileSystemLocation object specifies the identifier, location, mountOptions, mountPoint, and type of a file system created using Amazon Elastic File System."];
      buildBatchConfig: ProjectBuildBatchConfig.t option ;
      concurrentBuildLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of concurrent builds that are allowed for this project. New builds are only started if the current number of builds is less than or equal to this limit. If the current build count meets this limit, new builds are throttled and are not run. To remove this limit, set this value to -1."];
      autoRetryLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of additional automatic retries after a failed build. For example, if the auto-retry limit is set to 2, CodeBuild will call the RetryBuild API to automatically retry your build for up to 2 additional times."]}
    let context_ = "UpdateProjectInput"
    let make ?description =
      fun ?source ->
        fun ?secondarySources ->
          fun ?sourceVersion ->
            fun ?secondarySourceVersions ->
              fun ?artifacts ->
                fun ?secondaryArtifacts ->
                  fun ?cache ->
                    fun ?environment ->
                      fun ?serviceRole ->
                        fun ?timeoutInMinutes ->
                          fun ?queuedTimeoutInMinutes ->
                            fun ?encryptionKey ->
                              fun ?tags ->
                                fun ?vpcConfig ->
                                  fun ?badgeEnabled ->
                                    fun ?logsConfig ->
                                      fun ?fileSystemLocations ->
                                        fun ?buildBatchConfig ->
                                          fun ?concurrentBuildLimit ->
                                            fun ?autoRetryLimit ->
                                              fun ~name ->
                                                fun () ->
                                                  {
                                                    description;
                                                    source;
                                                    secondarySources;
                                                    sourceVersion;
                                                    secondarySourceVersions;
                                                    artifacts;
                                                    secondaryArtifacts;
                                                    cache;
                                                    environment;
                                                    serviceRole;
                                                    timeoutInMinutes;
                                                    queuedTimeoutInMinutes;
                                                    encryptionKey;
                                                    tags;
                                                    vpcConfig;
                                                    badgeEnabled;
                                                    logsConfig;
                                                    fileSystemLocations;
                                                    buildBatchConfig;
                                                    concurrentBuildLimit;
                                                    autoRetryLimit;
                                                    name
                                                  }
    let to_value x =
      structure_to_value
        [("name", (Some (NonEmptyString.to_value x.name)));
        ("description",
          (Option.map x.description ~f:ProjectDescription.to_value));
        ("source", (Option.map x.source ~f:ProjectSource.to_value));
        ("secondarySources",
          (Option.map x.secondarySources ~f:ProjectSources.to_value));
        ("sourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("secondarySourceVersions",
          (Option.map x.secondarySourceVersions
             ~f:ProjectSecondarySourceVersions.to_value));
        ("artifacts", (Option.map x.artifacts ~f:ProjectArtifacts.to_value));
        ("secondaryArtifacts",
          (Option.map x.secondaryArtifacts ~f:ProjectArtifactsList.to_value));
        ("cache", (Option.map x.cache ~f:ProjectCache.to_value));
        ("environment",
          (Option.map x.environment ~f:ProjectEnvironment.to_value));
        ("serviceRole",
          (Option.map x.serviceRole ~f:NonEmptyString.to_value));
        ("timeoutInMinutes",
          (Option.map x.timeoutInMinutes ~f:BuildTimeOut.to_value));
        ("queuedTimeoutInMinutes",
          (Option.map x.queuedTimeoutInMinutes ~f:TimeOut.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("badgeEnabled",
          (Option.map x.badgeEnabled ~f:WrapperBoolean.to_value));
        ("logsConfig", (Option.map x.logsConfig ~f:LogsConfig.to_value));
        ("fileSystemLocations",
          (Option.map x.fileSystemLocations
             ~f:ProjectFileSystemLocations.to_value));
        ("buildBatchConfig",
          (Option.map x.buildBatchConfig ~f:ProjectBuildBatchConfig.to_value));
        ("concurrentBuildLimit",
          (Option.map x.concurrentBuildLimit ~f:WrapperInt.to_value));
        ("autoRetryLimit",
          (Option.map x.autoRetryLimit ~f:WrapperInt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRetryLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "autoRetryLimit") in
      let concurrentBuildLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "concurrentBuildLimit") in
      let buildBatchConfig =
        (Option.map ~f:ProjectBuildBatchConfig.of_xml)
          (Xml.child xml_arg0 "buildBatchConfig") in
      let fileSystemLocations =
        (Option.map ~f:ProjectFileSystemLocations.of_xml)
          (Xml.child xml_arg0 "fileSystemLocations") in
      let logsConfig =
        (Option.map ~f:LogsConfig.of_xml) (Xml.child xml_arg0 "logsConfig") in
      let badgeEnabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "badgeEnabled") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let queuedTimeoutInMinutes =
        (Option.map ~f:TimeOut.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutes") in
      let timeoutInMinutes =
        (Option.map ~f:BuildTimeOut.of_xml)
          (Xml.child xml_arg0 "timeoutInMinutes") in
      let serviceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRole") in
      let environment =
        (Option.map ~f:ProjectEnvironment.of_xml)
          (Xml.child xml_arg0 "environment") in
      let cache =
        (Option.map ~f:ProjectCache.of_xml) (Xml.child xml_arg0 "cache") in
      let secondaryArtifacts =
        (Option.map ~f:ProjectArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifacts") in
      let artifacts =
        (Option.map ~f:ProjectArtifacts.of_xml)
          (Xml.child xml_arg0 "artifacts") in
      let secondarySourceVersions =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourceVersions") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceVersion") in
      let secondarySources =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySources") in
      let source =
        (Option.map ~f:ProjectSource.of_xml) (Xml.child xml_arg0 "source") in
      let description =
        (Option.map ~f:ProjectDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?autoRetryLimit ?concurrentBuildLimit ?buildBatchConfig
        ?fileSystemLocations ?logsConfig ?badgeEnabled ?vpcConfig ?tags
        ?encryptionKey ?queuedTimeoutInMinutes ?timeoutInMinutes ?serviceRole
        ?environment ?cache ?secondaryArtifacts ?artifacts
        ?secondarySourceVersions ?sourceVersion ?secondarySources ?source
        ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let autoRetryLimit =
        field_map json__ "autoRetryLimit" WrapperInt.of_json in
      let concurrentBuildLimit =
        field_map json__ "concurrentBuildLimit" WrapperInt.of_json in
      let buildBatchConfig =
        field_map json__ "buildBatchConfig" ProjectBuildBatchConfig.of_json in
      let fileSystemLocations =
        field_map json__ "fileSystemLocations"
          ProjectFileSystemLocations.of_json in
      let logsConfig = field_map json__ "logsConfig" LogsConfig.of_json in
      let badgeEnabled =
        field_map json__ "badgeEnabled" WrapperBoolean.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let queuedTimeoutInMinutes =
        field_map json__ "queuedTimeoutInMinutes" TimeOut.of_json in
      let timeoutInMinutes =
        field_map json__ "timeoutInMinutes" BuildTimeOut.of_json in
      let serviceRole = field_map json__ "serviceRole" NonEmptyString.of_json in
      let environment =
        field_map json__ "environment" ProjectEnvironment.of_json in
      let cache = field_map json__ "cache" ProjectCache.of_json in
      let secondaryArtifacts =
        field_map json__ "secondaryArtifacts" ProjectArtifactsList.of_json in
      let artifacts = field_map json__ "artifacts" ProjectArtifacts.of_json in
      let secondarySourceVersions =
        field_map json__ "secondarySourceVersions"
          ProjectSecondarySourceVersions.of_json in
      let sourceVersion = field_map json__ "sourceVersion" String_.of_json in
      let secondarySources =
        field_map json__ "secondarySources" ProjectSources.of_json in
      let source = field_map json__ "source" ProjectSource.of_json in
      let description =
        field_map json__ "description" ProjectDescription.of_json in
      let name = field_map_exn json__ "name" NonEmptyString.of_json in
      make ?autoRetryLimit ?concurrentBuildLimit ?buildBatchConfig
        ?fileSystemLocations ?logsConfig ?badgeEnabled ?vpcConfig ?tags
        ?encryptionKey ?queuedTimeoutInMinutes ?timeoutInMinutes ?serviceRole
        ?environment ?cache ?secondaryArtifacts ?artifacts
        ?secondarySourceVersions ?sourceVersion ?secondarySources ?source
        ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Changes the settings of a build project."]
module UpdateFleetOutput =
  struct
    type nonrec t = {
      fleet: Fleet.t option [@ocaml.doc "A Fleet object."]}
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleet = fun () -> { fleet }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("fleet", (Option.map x.fleet ~f:Fleet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleet = (Option.map ~f:Fleet.of_xml) (Xml.child xml_arg0 "fleet") in
      make ?fleet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleet = field_map json__ "fleet" Fleet.of_json in make ?fleet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a compute fleet."]
module UpdateFleetInput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t [@ocaml.doc "The ARN of the compute fleet."];
      baseCapacity: FleetCapacity.t option
        [@ocaml.doc
          "The initial number of machines allocated to the compute \239\172\130eet, which de\239\172\129nes the number of builds that can run in parallel."];
      environmentType: EnvironmentType.t option
        [@ocaml.doc
          "The environment type of the compute fleet. The environment type ARM_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), Asia Pacific (Mumbai), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), EU (Frankfurt), and South America (S\195\163o Paulo). The environment type ARM_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_GPU_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), and Asia Pacific (Sydney). The environment type MAC_ARM is available for Medium fleets only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), Asia Pacific (Sydney), and EU (Frankfurt) The environment type MAC_ARM is available for Large fleets only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), and Asia Pacific (Sydney). The environment type WINDOWS_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type WINDOWS_SERVER_2019_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), Asia Pacific (Sydney), Asia Pacific (Tokyo), Asia Pacific (Mumbai) and EU (Ireland). The environment type WINDOWS_SERVER_2022_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Sydney), Asia Pacific (Singapore), Asia Pacific (Tokyo), South America (S\195\163o Paulo) and Asia Pacific (Mumbai). For more information, see Build environment compute types in the CodeBuild user guide."];
      computeType: ComputeType.t option
        [@ocaml.doc
          "Information about the compute resources the compute fleet uses. Available values include: ATTRIBUTE_BASED_COMPUTE: Specify the amount of vCPUs, memory, disk space, and the type of machine. If you use ATTRIBUTE_BASED_COMPUTE, you must define your attributes by using computeConfiguration. CodeBuild will select the cheapest instance that satisfies your specified attributes. For more information, see Reserved capacity environment types in the CodeBuild User Guide. CUSTOM_INSTANCE_TYPE: Specify the instance type for your compute fleet. For a list of supported instance types, see Supported instance families in the CodeBuild User Guide. BUILD_GENERAL1_SMALL: Use up to 4 GiB memory and 2 vCPUs for builds. BUILD_GENERAL1_MEDIUM: Use up to 8 GiB memory and 4 vCPUs for builds. BUILD_GENERAL1_LARGE: Use up to 16 GiB memory and 8 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_XLARGE: Use up to 72 GiB memory and 36 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_2XLARGE: Use up to 144 GiB memory, 72 vCPUs, and 824 GB of SSD storage for builds. This compute type supports Docker images up to 100 GB uncompressed. BUILD_LAMBDA_1GB: Use up to 1 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_2GB: Use up to 2 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_4GB: Use up to 4 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_8GB: Use up to 8 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_10GB: Use up to 10 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. If you use BUILD_GENERAL1_SMALL: For environment type LINUX_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 16 GiB memory, 4 vCPUs, and 1 NVIDIA A10G Tensor Core GPU for builds. For environment type ARM_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs on ARM-based processors for builds. If you use BUILD_GENERAL1_LARGE: For environment type LINUX_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 255 GiB memory, 32 vCPUs, and 4 NVIDIA Tesla V100 GPUs for builds. For environment type ARM_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs on ARM-based processors for builds. For more information, see On-demand environment types in the CodeBuild User Guide."];
      computeConfiguration: ComputeConfiguration.t option
        [@ocaml.doc
          "The compute configuration of the compute fleet. This is only required if computeType is set to ATTRIBUTE_BASED_COMPUTE or CUSTOM_INSTANCE_TYPE."];
      scalingConfiguration: ScalingConfigurationInput.t option
        [@ocaml.doc "The scaling configuration of the compute fleet."];
      overflowBehavior: FleetOverflowBehavior.t option
        [@ocaml.doc
          "The compute fleet overflow behavior. For overflow behavior QUEUE, your overflow builds need to wait on the existing fleet instance to become available. For overflow behavior ON_DEMAND, your overflow builds run on CodeBuild on-demand. If you choose to set your overflow behavior to on-demand while creating a VPC-connected fleet, make sure that you add the required VPC permissions to your project service role. For more information, see Example policy statement to allow CodeBuild access to Amazon Web Services services required to create a VPC network interface."];
      vpcConfig: VpcConfig.t option ;
      proxyConfiguration: ProxyConfiguration.t option
        [@ocaml.doc "The proxy configuration of the compute fleet."];
      imageId: NonEmptyString.t option
        [@ocaml.doc "The Amazon Machine Image (AMI) of the compute fleet."];
      fleetServiceRole: NonEmptyString.t option
        [@ocaml.doc
          "The service role associated with the compute fleet. For more information, see Allow a user to add a permission policy for a fleet service role in the CodeBuild User Guide."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this compute fleet. These tags are available for use by Amazon Web Services services that support CodeBuild build project tags."]}
    let context_ = "UpdateFleetInput"
    let make ?baseCapacity =
      fun ?environmentType ->
        fun ?computeType ->
          fun ?computeConfiguration ->
            fun ?scalingConfiguration ->
              fun ?overflowBehavior ->
                fun ?vpcConfig ->
                  fun ?proxyConfiguration ->
                    fun ?imageId ->
                      fun ?fleetServiceRole ->
                        fun ?tags ->
                          fun ~arn ->
                            fun () ->
                              {
                                baseCapacity;
                                environmentType;
                                computeType;
                                computeConfiguration;
                                scalingConfiguration;
                                overflowBehavior;
                                vpcConfig;
                                proxyConfiguration;
                                imageId;
                                fleetServiceRole;
                                tags;
                                arn
                              }
    let to_value x =
      structure_to_value
        [("arn", (Some (NonEmptyString.to_value x.arn)));
        ("baseCapacity",
          (Option.map x.baseCapacity ~f:FleetCapacity.to_value));
        ("environmentType",
          (Option.map x.environmentType ~f:EnvironmentType.to_value));
        ("computeType", (Option.map x.computeType ~f:ComputeType.to_value));
        ("computeConfiguration",
          (Option.map x.computeConfiguration ~f:ComputeConfiguration.to_value));
        ("scalingConfiguration",
          (Option.map x.scalingConfiguration
             ~f:ScalingConfigurationInput.to_value));
        ("overflowBehavior",
          (Option.map x.overflowBehavior ~f:FleetOverflowBehavior.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("proxyConfiguration",
          (Option.map x.proxyConfiguration ~f:ProxyConfiguration.to_value));
        ("imageId", (Option.map x.imageId ~f:NonEmptyString.to_value));
        ("fleetServiceRole",
          (Option.map x.fleetServiceRole ~f:NonEmptyString.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let fleetServiceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "fleetServiceRole") in
      let imageId =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "imageId") in
      let proxyConfiguration =
        (Option.map ~f:ProxyConfiguration.of_xml)
          (Xml.child xml_arg0 "proxyConfiguration") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let overflowBehavior =
        (Option.map ~f:FleetOverflowBehavior.of_xml)
          (Xml.child xml_arg0 "overflowBehavior") in
      let scalingConfiguration =
        (Option.map ~f:ScalingConfigurationInput.of_xml)
          (Xml.child xml_arg0 "scalingConfiguration") in
      let computeConfiguration =
        (Option.map ~f:ComputeConfiguration.of_xml)
          (Xml.child xml_arg0 "computeConfiguration") in
      let computeType =
        (Option.map ~f:ComputeType.of_xml) (Xml.child xml_arg0 "computeType") in
      let environmentType =
        (Option.map ~f:EnvironmentType.of_xml)
          (Xml.child xml_arg0 "environmentType") in
      let baseCapacity =
        (Option.map ~f:FleetCapacity.of_xml)
          (Xml.child xml_arg0 "baseCapacity") in
      let arn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?tags ?fleetServiceRole ?imageId ?proxyConfiguration ?vpcConfig
        ?overflowBehavior ?scalingConfiguration ?computeConfiguration
        ?computeType ?environmentType ?baseCapacity ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let fleetServiceRole =
        field_map json__ "fleetServiceRole" NonEmptyString.of_json in
      let imageId = field_map json__ "imageId" NonEmptyString.of_json in
      let proxyConfiguration =
        field_map json__ "proxyConfiguration" ProxyConfiguration.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let overflowBehavior =
        field_map json__ "overflowBehavior" FleetOverflowBehavior.of_json in
      let scalingConfiguration =
        field_map json__ "scalingConfiguration"
          ScalingConfigurationInput.of_json in
      let computeConfiguration =
        field_map json__ "computeConfiguration" ComputeConfiguration.of_json in
      let computeType = field_map json__ "computeType" ComputeType.of_json in
      let environmentType =
        field_map json__ "environmentType" EnvironmentType.of_json in
      let baseCapacity =
        field_map json__ "baseCapacity" FleetCapacity.of_json in
      let arn = field_map_exn json__ "arn" NonEmptyString.of_json in
      make ?tags ?fleetServiceRole ?imageId ?proxyConfiguration ?vpcConfig
        ?overflowBehavior ?scalingConfiguration ?computeConfiguration
        ?computeType ?environmentType ?baseCapacity ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a compute fleet."]
module StopSandboxOutput =
  struct
    type nonrec t =
      {
      sandbox: Sandbox.t option
        [@ocaml.doc "Information about the requested sandbox."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sandbox = fun () -> { sandbox }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sandbox", (Option.map x.sandbox ~f:Sandbox.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sandbox =
        (Option.map ~f:Sandbox.of_xml) (Xml.child xml_arg0 "sandbox") in
      make ?sandbox ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sandbox = field_map json__ "sandbox" Sandbox.of_json in
      make ?sandbox ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stops a sandbox."]
module StopSandboxInput =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t
        [@ocaml.doc "Information about the requested sandbox ID."]}
    let context_ = "StopSandboxInput"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (NonEmptyString.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        NonEmptyString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" NonEmptyString.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stops a sandbox."]
module StopBuildOutput =
  struct
    type nonrec t =
      {
      build: Build.t option [@ocaml.doc "Information about the build."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?build = fun () -> { build }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("build", (Option.map x.build ~f:Build.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let build = (Option.map ~f:Build.of_xml) (Xml.child xml_arg0 "build") in
      make ?build ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let build = field_map json__ "build" Build.of_json in make ?build ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Attempts to stop running a build."]
module StopBuildInput =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t [@ocaml.doc "The ID of the build."]}
    let context_ = "StopBuildInput"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (NonEmptyString.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        NonEmptyString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" NonEmptyString.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Attempts to stop running a build."]
module StopBuildBatchOutput =
  struct
    type nonrec t = {
      buildBatch: BuildBatch.t option }
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?buildBatch = fun () -> { buildBatch }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("buildBatch", (Option.map x.buildBatch ~f:BuildBatch.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildBatch =
        (Option.map ~f:BuildBatch.of_xml) (Xml.child xml_arg0 "buildBatch") in
      make ?buildBatch ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildBatch = field_map json__ "buildBatch" BuildBatch.of_json in
      make ?buildBatch ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stops a running batch build."]
module StopBuildBatchInput =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t
        [@ocaml.doc "The identifier of the batch build to stop."]}
    let context_ = "StopBuildBatchInput"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (NonEmptyString.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        NonEmptyString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" NonEmptyString.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stops a running batch build."]
module StartSandboxOutput =
  struct
    type nonrec t =
      {
      sandbox: Sandbox.t option
        [@ocaml.doc "Information about the requested sandbox."]}
    type nonrec error =
      [ `AccountSuspendedException of AccountSuspendedException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sandbox = fun () -> { sandbox }
    let error_of_json name json =
      match name with
      | "AccountSuspendedException" ->
          `AccountSuspendedException (AccountSuspendedException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountSuspendedException" ->
          `AccountSuspendedException (AccountSuspendedException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountSuspendedException e ->
          `Assoc
            [("error", (`String "AccountSuspendedException"));
            ("details", (AccountSuspendedException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sandbox", (Option.map x.sandbox ~f:Sandbox.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sandbox =
        (Option.map ~f:Sandbox.of_xml) (Xml.child xml_arg0 "sandbox") in
      make ?sandbox ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sandbox = field_map json__ "sandbox" Sandbox.of_json in
      make ?sandbox ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a sandbox."]
module StartSandboxInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t option
        [@ocaml.doc "The CodeBuild project name."];
      idempotencyToken: SensitiveString.t option
        [@ocaml.doc "A unique client token."]}
    let make ?projectName =
      fun ?idempotencyToken -> fun () -> { projectName; idempotencyToken }
    let to_value x =
      structure_to_value
        [("projectName",
           (Option.map x.projectName ~f:NonEmptyString.to_value));
        ("idempotencyToken",
          (Option.map x.idempotencyToken ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "idempotencyToken") in
      let projectName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "projectName") in
      make ?idempotencyToken ?projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let idempotencyToken =
        field_map json__ "idempotencyToken" SensitiveString.of_json in
      let projectName = field_map json__ "projectName" NonEmptyString.of_json in
      make ?idempotencyToken ?projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a sandbox."]
module StartSandboxConnectionOutput =
  struct
    type nonrec t =
      {
      ssmSession: SSMSession.t option
        [@ocaml.doc "Information about the Session Manager session."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ssmSession = fun () -> { ssmSession }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ssmSession", (Option.map x.ssmSession ~f:SSMSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ssmSession =
        (Option.map ~f:SSMSession.of_xml) (Xml.child xml_arg0 "ssmSession") in
      make ?ssmSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ssmSession = field_map json__ "ssmSession" SSMSession.of_json in
      make ?ssmSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a sandbox connection."]
module StartSandboxConnectionInput =
  struct
    type nonrec t =
      {
      sandboxId: NonEmptyString.t [@ocaml.doc "A sandboxId or sandboxArn."]}
    let context_ = "StartSandboxConnectionInput"
    let make ~sandboxId = fun () -> { sandboxId }
    let to_value x =
      structure_to_value
        [("sandboxId", (Some (NonEmptyString.to_value x.sandboxId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sandboxId =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sandboxId") in
      make ~sandboxId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sandboxId = field_map_exn json__ "sandboxId" NonEmptyString.of_json in
      make ~sandboxId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a sandbox connection."]
module StartCommandExecutionOutput =
  struct
    type nonrec t =
      {
      commandExecution: CommandExecution.t option
        [@ocaml.doc "Information about the requested command executions."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?commandExecution = fun () -> { commandExecution }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("commandExecution",
           (Option.map x.commandExecution ~f:CommandExecution.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commandExecution =
        (Option.map ~f:CommandExecution.of_xml)
          (Xml.child xml_arg0 "commandExecution") in
      make ?commandExecution ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let commandExecution =
        field_map json__ "commandExecution" CommandExecution.of_json in
      make ?commandExecution ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a command execution."]
module StartCommandExecutionInput =
  struct
    type nonrec t =
      {
      sandboxId: NonEmptyString.t [@ocaml.doc "A sandboxId or sandboxArn."];
      command: SensitiveNonEmptyString.t
        [@ocaml.doc "The command that needs to be executed."];
      type_: CommandType.t option [@ocaml.doc "The command type."]}
    let context_ = "StartCommandExecutionInput"
    let make ?type_ =
      fun ~sandboxId ->
        fun ~command -> fun () -> { type_; sandboxId; command }
    let to_value x =
      structure_to_value
        [("sandboxId", (Some (NonEmptyString.to_value x.sandboxId)));
        ("command", (Some (SensitiveNonEmptyString.to_value x.command)));
        ("type", (Option.map x.type_ ~f:CommandType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:CommandType.of_xml) (Xml.child xml_arg0 "type") in
      let command =
        SensitiveNonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "command") in
      let sandboxId =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sandboxId") in
      make ?type_ ~command ~sandboxId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" CommandType.of_json in
      let command =
        field_map_exn json__ "command" SensitiveNonEmptyString.of_json in
      let sandboxId = field_map_exn json__ "sandboxId" NonEmptyString.of_json in
      make ?type_ ~command ~sandboxId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a command execution."]
module StartBuildOutput =
  struct
    type nonrec t =
      {
      build: Build.t option
        [@ocaml.doc "Information about the build to be run."]}
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?build = fun () -> { build }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("build", (Option.map x.build ~f:Build.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let build = (Option.map ~f:Build.of_xml) (Xml.child xml_arg0 "build") in
      make ?build ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let build = field_map json__ "build" Build.of_json in make ?build ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts running a build with the settings defined in the project. These setting include: how to run a build, where to get the source code, which build environment to use, which build commands to run, and where to store the build output. You can also start a build run by overriding some of the build settings in the project. The overrides only apply for that specific start build request. The settings in the project are unaltered."]
module StartBuildInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t
        [@ocaml.doc
          "The name of the CodeBuild build project to start running a build."];
      secondarySourcesOverride: ProjectSources.t option
        [@ocaml.doc "An array of ProjectSource objects."];
      secondarySourcesVersionOverride:
        ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects that specify one or more versions of the project's secondary sources to be used for this build only."];
      sourceVersion: String_.t option
        [@ocaml.doc
          "The version of the build input to be built, for this build only. If not specified, the latest version is used. If specified, the contents depends on the source provider: CodeCommit The commit ID, branch, or Git tag to use. GitHub The commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. GitLab The commit ID, branch, or Git tag to use. Bitbucket The commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. Amazon S3 The version ID of the object that represents the build input ZIP file to use. If sourceVersion is specified at the project level, then this sourceVersion (at the build level) takes precedence. For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."];
      artifactsOverride: ProjectArtifacts.t option
        [@ocaml.doc
          "Build output artifact settings that override, for this build only, the latest ones already defined in the build project."];
      secondaryArtifactsOverride: ProjectArtifactsList.t option
        [@ocaml.doc "An array of ProjectArtifacts objects."];
      environmentVariablesOverride: EnvironmentVariables.t option
        [@ocaml.doc
          "A set of environment variables that overrides, for this build only, the latest ones already defined in the build project."];
      sourceTypeOverride: SourceType.t option
        [@ocaml.doc
          "A source input type, for this build, that overrides the source input defined in the build project."];
      sourceLocationOverride: String_.t option
        [@ocaml.doc
          "A location that overrides, for this build, the source location for the one defined in the build project."];
      sourceAuthOverride: SourceAuth.t option
        [@ocaml.doc
          "An authorization type for this build that overrides the one defined in the build project. This override applies only if the build project's source is BitBucket, GitHub, GitLab, or GitLab Self Managed."];
      gitCloneDepthOverride: GitCloneDepth.t option
        [@ocaml.doc
          "The user-defined depth of history, with a minimum value of 0, that overrides, for this build only, any previous depth of history defined in the build project."];
      gitSubmodulesConfigOverride: GitSubmodulesConfig.t option
        [@ocaml.doc
          "Information about the Git submodules configuration for this build of an CodeBuild build project."];
      buildspecOverride: String_.t option
        [@ocaml.doc
          "A buildspec file declaration that overrides the latest one defined in the build project, for this build only. The buildspec defined on the project is not changed. If this value is set, it can be either an inline buildspec definition, the path to an alternate buildspec file relative to the value of the built-in CODEBUILD_SRC_DIR environment variable, or the path to an S3 bucket. The bucket must be in the same Amazon Web Services Region as the build project. Specify the buildspec file using its ARN (for example, arn:aws:s3:::my-codebuild-sample2/buildspec.yml). If this value is not provided or is set to an empty string, the source code must contain a buildspec file in its root directory. For more information, see Buildspec File Name and Storage Location. Since this property allows you to change the build commands that will run in the container, you should note that an IAM principal with the ability to call this API and set this parameter can override the default settings. Moreover, we encourage that you use a trustworthy buildspec location like a file in your source repository or a Amazon S3 bucket. Alternatively, you can restrict overrides to the buildspec by using a condition key: Prevent unauthorized modifications to project buildspec."];
      insecureSslOverride: WrapperBoolean.t option
        [@ocaml.doc
          "Enable this flag to override the insecure SSL setting that is specified in the build project. The insecure SSL setting determines whether to ignore SSL warnings while connecting to the project source code. This override applies only if the build's source is GitHub Enterprise."];
      reportBuildStatusOverride: WrapperBoolean.t option
        [@ocaml.doc
          "Set to true to report to your source provider the status of a build's start and completion. If you use this option with a source provider other than GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket, an invalidInputException is thrown. To be able to report the build status to the source provider, the user associated with the source provider must have write access to the repo. If the user does not have write access, the build status cannot be updated. For more information, see Source provider access in the CodeBuild User Guide. The status of a build triggered by a webhook is always reported to your source provider."];
      buildStatusConfigOverride: BuildStatusConfig.t option
        [@ocaml.doc
          "Contains information that defines how the build project reports the build status to the source provider. This option is only used when the source provider is GITHUB, GITHUB_ENTERPRISE, or BITBUCKET."];
      environmentTypeOverride: EnvironmentType.t option
        [@ocaml.doc
          "A container type for this build that overrides the one specified in the build project."];
      imageOverride: NonEmptyString.t option
        [@ocaml.doc
          "The name of an image for this build that overrides the one specified in the build project."];
      computeTypeOverride: ComputeType.t option
        [@ocaml.doc
          "The name of a compute type for this build that overrides the one specified in the build project."];
      certificateOverride: String_.t option
        [@ocaml.doc
          "The name of a certificate for this build that overrides the one specified in the build project."];
      cacheOverride: ProjectCache.t option
        [@ocaml.doc
          "A ProjectCache object specified for this build that overrides the one defined in the build project."];
      serviceRoleOverride: NonEmptyString.t option
        [@ocaml.doc
          "The name of a service role for this build that overrides the one specified in the build project."];
      privilegedModeOverride: WrapperBoolean.t option
        [@ocaml.doc
          "Enable this flag to override privileged mode in the build project."];
      timeoutInMinutesOverride: BuildTimeOut.t option
        [@ocaml.doc
          "The number of build timeout minutes, from 5 to 2160 (36 hours), that overrides, for this build only, the latest setting already defined in the build project."];
      queuedTimeoutInMinutesOverride: TimeOut.t option
        [@ocaml.doc
          "The number of minutes a build is allowed to be queued before it times out."];
      encryptionKeyOverride: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) that overrides the one specified in the build project. The CMK key encrypts the build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>)."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A unique, case sensitive identifier you provide to ensure the idempotency of the StartBuild request. The token is included in the StartBuild request and is valid for 5 minutes. If you repeat the StartBuild request with the same token, but change a parameter, CodeBuild returns a parameter mismatch error."];
      logsConfigOverride: LogsConfig.t option
        [@ocaml.doc
          "Log settings for this build that override the log settings defined in the build project."];
      registryCredentialOverride: RegistryCredential.t option
        [@ocaml.doc "The credentials for access to a private registry."];
      imagePullCredentialsTypeOverride: ImagePullCredentialsType.t option
        [@ocaml.doc
          "The type of credentials CodeBuild uses to pull images in your build. There are two valid values: CODEBUILD Specifies that CodeBuild uses its own credentials. This requires that you modify your ECR repository policy to trust CodeBuild's service principal. SERVICE_ROLE Specifies that CodeBuild uses your build project's service role. When using a cross-account or private registry image, you must use SERVICE_ROLE credentials. When using an CodeBuild curated image, you must use CODEBUILD credentials."];
      debugSessionEnabled: WrapperBoolean.t option
        [@ocaml.doc
          "Specifies if session debugging is enabled for this build. For more information, see Viewing a running build in Session Manager."];
      fleetOverride: ProjectFleet.t option
        [@ocaml.doc
          "A ProjectFleet object specified for this build that overrides the one defined in the build project."];
      autoRetryLimitOverride: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of additional automatic retries after a failed build. For example, if the auto-retry limit is set to 2, CodeBuild will call the RetryBuild API to automatically retry your build for up to 2 additional times."]}
    let context_ = "StartBuildInput"
    let make ?secondarySourcesOverride =
      fun ?secondarySourcesVersionOverride ->
        fun ?sourceVersion ->
          fun ?artifactsOverride ->
            fun ?secondaryArtifactsOverride ->
              fun ?environmentVariablesOverride ->
                fun ?sourceTypeOverride ->
                  fun ?sourceLocationOverride ->
                    fun ?sourceAuthOverride ->
                      fun ?gitCloneDepthOverride ->
                        fun ?gitSubmodulesConfigOverride ->
                          fun ?buildspecOverride ->
                            fun ?insecureSslOverride ->
                              fun ?reportBuildStatusOverride ->
                                fun ?buildStatusConfigOverride ->
                                  fun ?environmentTypeOverride ->
                                    fun ?imageOverride ->
                                      fun ?computeTypeOverride ->
                                        fun ?certificateOverride ->
                                          fun ?cacheOverride ->
                                            fun ?serviceRoleOverride ->
                                              fun ?privilegedModeOverride ->
                                                fun ?timeoutInMinutesOverride
                                                  ->
                                                  fun
                                                    ?queuedTimeoutInMinutesOverride
                                                    ->
                                                    fun
                                                      ?encryptionKeyOverride
                                                      ->
                                                      fun ?idempotencyToken
                                                        ->
                                                        fun
                                                          ?logsConfigOverride
                                                          ->
                                                          fun
                                                            ?registryCredentialOverride
                                                            ->
                                                            fun
                                                              ?imagePullCredentialsTypeOverride
                                                              ->
                                                              fun
                                                                ?debugSessionEnabled
                                                                ->
                                                                fun
                                                                  ?fleetOverride
                                                                  ->
                                                                  fun
                                                                    ?autoRetryLimitOverride
                                                                    ->
                                                                    fun
                                                                    ~projectName
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    secondarySourcesOverride;
                                                                    secondarySourcesVersionOverride;
                                                                    sourceVersion;
                                                                    artifactsOverride;
                                                                    secondaryArtifactsOverride;
                                                                    environmentVariablesOverride;
                                                                    sourceTypeOverride;
                                                                    sourceLocationOverride;
                                                                    sourceAuthOverride;
                                                                    gitCloneDepthOverride;
                                                                    gitSubmodulesConfigOverride;
                                                                    buildspecOverride;
                                                                    insecureSslOverride;
                                                                    reportBuildStatusOverride;
                                                                    buildStatusConfigOverride;
                                                                    environmentTypeOverride;
                                                                    imageOverride;
                                                                    computeTypeOverride;
                                                                    certificateOverride;
                                                                    cacheOverride;
                                                                    serviceRoleOverride;
                                                                    privilegedModeOverride;
                                                                    timeoutInMinutesOverride;
                                                                    queuedTimeoutInMinutesOverride;
                                                                    encryptionKeyOverride;
                                                                    idempotencyToken;
                                                                    logsConfigOverride;
                                                                    registryCredentialOverride;
                                                                    imagePullCredentialsTypeOverride;
                                                                    debugSessionEnabled;
                                                                    fleetOverride;
                                                                    autoRetryLimitOverride;
                                                                    projectName
                                                                    }
    let to_value x =
      structure_to_value
        [("projectName", (Some (NonEmptyString.to_value x.projectName)));
        ("secondarySourcesOverride",
          (Option.map x.secondarySourcesOverride ~f:ProjectSources.to_value));
        ("secondarySourcesVersionOverride",
          (Option.map x.secondarySourcesVersionOverride
             ~f:ProjectSecondarySourceVersions.to_value));
        ("sourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("artifactsOverride",
          (Option.map x.artifactsOverride ~f:ProjectArtifacts.to_value));
        ("secondaryArtifactsOverride",
          (Option.map x.secondaryArtifactsOverride
             ~f:ProjectArtifactsList.to_value));
        ("environmentVariablesOverride",
          (Option.map x.environmentVariablesOverride
             ~f:EnvironmentVariables.to_value));
        ("sourceTypeOverride",
          (Option.map x.sourceTypeOverride ~f:SourceType.to_value));
        ("sourceLocationOverride",
          (Option.map x.sourceLocationOverride ~f:String_.to_value));
        ("sourceAuthOverride",
          (Option.map x.sourceAuthOverride ~f:SourceAuth.to_value));
        ("gitCloneDepthOverride",
          (Option.map x.gitCloneDepthOverride ~f:GitCloneDepth.to_value));
        ("gitSubmodulesConfigOverride",
          (Option.map x.gitSubmodulesConfigOverride
             ~f:GitSubmodulesConfig.to_value));
        ("buildspecOverride",
          (Option.map x.buildspecOverride ~f:String_.to_value));
        ("insecureSslOverride",
          (Option.map x.insecureSslOverride ~f:WrapperBoolean.to_value));
        ("reportBuildStatusOverride",
          (Option.map x.reportBuildStatusOverride ~f:WrapperBoolean.to_value));
        ("buildStatusConfigOverride",
          (Option.map x.buildStatusConfigOverride
             ~f:BuildStatusConfig.to_value));
        ("environmentTypeOverride",
          (Option.map x.environmentTypeOverride ~f:EnvironmentType.to_value));
        ("imageOverride",
          (Option.map x.imageOverride ~f:NonEmptyString.to_value));
        ("computeTypeOverride",
          (Option.map x.computeTypeOverride ~f:ComputeType.to_value));
        ("certificateOverride",
          (Option.map x.certificateOverride ~f:String_.to_value));
        ("cacheOverride",
          (Option.map x.cacheOverride ~f:ProjectCache.to_value));
        ("serviceRoleOverride",
          (Option.map x.serviceRoleOverride ~f:NonEmptyString.to_value));
        ("privilegedModeOverride",
          (Option.map x.privilegedModeOverride ~f:WrapperBoolean.to_value));
        ("timeoutInMinutesOverride",
          (Option.map x.timeoutInMinutesOverride ~f:BuildTimeOut.to_value));
        ("queuedTimeoutInMinutesOverride",
          (Option.map x.queuedTimeoutInMinutesOverride ~f:TimeOut.to_value));
        ("encryptionKeyOverride",
          (Option.map x.encryptionKeyOverride ~f:NonEmptyString.to_value));
        ("idempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("logsConfigOverride",
          (Option.map x.logsConfigOverride ~f:LogsConfig.to_value));
        ("registryCredentialOverride",
          (Option.map x.registryCredentialOverride
             ~f:RegistryCredential.to_value));
        ("imagePullCredentialsTypeOverride",
          (Option.map x.imagePullCredentialsTypeOverride
             ~f:ImagePullCredentialsType.to_value));
        ("debugSessionEnabled",
          (Option.map x.debugSessionEnabled ~f:WrapperBoolean.to_value));
        ("fleetOverride",
          (Option.map x.fleetOverride ~f:ProjectFleet.to_value));
        ("autoRetryLimitOverride",
          (Option.map x.autoRetryLimitOverride ~f:WrapperInt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRetryLimitOverride =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "autoRetryLimitOverride") in
      let fleetOverride =
        (Option.map ~f:ProjectFleet.of_xml)
          (Xml.child xml_arg0 "fleetOverride") in
      let debugSessionEnabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "debugSessionEnabled") in
      let imagePullCredentialsTypeOverride =
        (Option.map ~f:ImagePullCredentialsType.of_xml)
          (Xml.child xml_arg0 "imagePullCredentialsTypeOverride") in
      let registryCredentialOverride =
        (Option.map ~f:RegistryCredential.of_xml)
          (Xml.child xml_arg0 "registryCredentialOverride") in
      let logsConfigOverride =
        (Option.map ~f:LogsConfig.of_xml)
          (Xml.child xml_arg0 "logsConfigOverride") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "idempotencyToken") in
      let encryptionKeyOverride =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyOverride") in
      let queuedTimeoutInMinutesOverride =
        (Option.map ~f:TimeOut.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutesOverride") in
      let timeoutInMinutesOverride =
        (Option.map ~f:BuildTimeOut.of_xml)
          (Xml.child xml_arg0 "timeoutInMinutesOverride") in
      let privilegedModeOverride =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "privilegedModeOverride") in
      let serviceRoleOverride =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRoleOverride") in
      let cacheOverride =
        (Option.map ~f:ProjectCache.of_xml)
          (Xml.child xml_arg0 "cacheOverride") in
      let certificateOverride =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateOverride") in
      let computeTypeOverride =
        (Option.map ~f:ComputeType.of_xml)
          (Xml.child xml_arg0 "computeTypeOverride") in
      let imageOverride =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "imageOverride") in
      let environmentTypeOverride =
        (Option.map ~f:EnvironmentType.of_xml)
          (Xml.child xml_arg0 "environmentTypeOverride") in
      let buildStatusConfigOverride =
        (Option.map ~f:BuildStatusConfig.of_xml)
          (Xml.child xml_arg0 "buildStatusConfigOverride") in
      let reportBuildStatusOverride =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "reportBuildStatusOverride") in
      let insecureSslOverride =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "insecureSslOverride") in
      let buildspecOverride =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "buildspecOverride") in
      let gitSubmodulesConfigOverride =
        (Option.map ~f:GitSubmodulesConfig.of_xml)
          (Xml.child xml_arg0 "gitSubmodulesConfigOverride") in
      let gitCloneDepthOverride =
        (Option.map ~f:GitCloneDepth.of_xml)
          (Xml.child xml_arg0 "gitCloneDepthOverride") in
      let sourceAuthOverride =
        (Option.map ~f:SourceAuth.of_xml)
          (Xml.child xml_arg0 "sourceAuthOverride") in
      let sourceLocationOverride =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceLocationOverride") in
      let sourceTypeOverride =
        (Option.map ~f:SourceType.of_xml)
          (Xml.child xml_arg0 "sourceTypeOverride") in
      let environmentVariablesOverride =
        (Option.map ~f:EnvironmentVariables.of_xml)
          (Xml.child xml_arg0 "environmentVariablesOverride") in
      let secondaryArtifactsOverride =
        (Option.map ~f:ProjectArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifactsOverride") in
      let artifactsOverride =
        (Option.map ~f:ProjectArtifacts.of_xml)
          (Xml.child xml_arg0 "artifactsOverride") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceVersion") in
      let secondarySourcesVersionOverride =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourcesVersionOverride") in
      let secondarySourcesOverride =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySourcesOverride") in
      let projectName =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ?autoRetryLimitOverride ?fleetOverride ?debugSessionEnabled
        ?imagePullCredentialsTypeOverride ?registryCredentialOverride
        ?logsConfigOverride ?idempotencyToken ?encryptionKeyOverride
        ?queuedTimeoutInMinutesOverride ?timeoutInMinutesOverride
        ?privilegedModeOverride ?serviceRoleOverride ?cacheOverride
        ?certificateOverride ?computeTypeOverride ?imageOverride
        ?environmentTypeOverride ?buildStatusConfigOverride
        ?reportBuildStatusOverride ?insecureSslOverride ?buildspecOverride
        ?gitSubmodulesConfigOverride ?gitCloneDepthOverride
        ?sourceAuthOverride ?sourceLocationOverride ?sourceTypeOverride
        ?environmentVariablesOverride ?secondaryArtifactsOverride
        ?artifactsOverride ?sourceVersion ?secondarySourcesVersionOverride
        ?secondarySourcesOverride ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let autoRetryLimitOverride =
        field_map json__ "autoRetryLimitOverride" WrapperInt.of_json in
      let fleetOverride =
        field_map json__ "fleetOverride" ProjectFleet.of_json in
      let debugSessionEnabled =
        field_map json__ "debugSessionEnabled" WrapperBoolean.of_json in
      let imagePullCredentialsTypeOverride =
        field_map json__ "imagePullCredentialsTypeOverride"
          ImagePullCredentialsType.of_json in
      let registryCredentialOverride =
        field_map json__ "registryCredentialOverride"
          RegistryCredential.of_json in
      let logsConfigOverride =
        field_map json__ "logsConfigOverride" LogsConfig.of_json in
      let idempotencyToken =
        field_map json__ "idempotencyToken" String_.of_json in
      let encryptionKeyOverride =
        field_map json__ "encryptionKeyOverride" NonEmptyString.of_json in
      let queuedTimeoutInMinutesOverride =
        field_map json__ "queuedTimeoutInMinutesOverride" TimeOut.of_json in
      let timeoutInMinutesOverride =
        field_map json__ "timeoutInMinutesOverride" BuildTimeOut.of_json in
      let privilegedModeOverride =
        field_map json__ "privilegedModeOverride" WrapperBoolean.of_json in
      let serviceRoleOverride =
        field_map json__ "serviceRoleOverride" NonEmptyString.of_json in
      let cacheOverride =
        field_map json__ "cacheOverride" ProjectCache.of_json in
      let certificateOverride =
        field_map json__ "certificateOverride" String_.of_json in
      let computeTypeOverride =
        field_map json__ "computeTypeOverride" ComputeType.of_json in
      let imageOverride =
        field_map json__ "imageOverride" NonEmptyString.of_json in
      let environmentTypeOverride =
        field_map json__ "environmentTypeOverride" EnvironmentType.of_json in
      let buildStatusConfigOverride =
        field_map json__ "buildStatusConfigOverride"
          BuildStatusConfig.of_json in
      let reportBuildStatusOverride =
        field_map json__ "reportBuildStatusOverride" WrapperBoolean.of_json in
      let insecureSslOverride =
        field_map json__ "insecureSslOverride" WrapperBoolean.of_json in
      let buildspecOverride =
        field_map json__ "buildspecOverride" String_.of_json in
      let gitSubmodulesConfigOverride =
        field_map json__ "gitSubmodulesConfigOverride"
          GitSubmodulesConfig.of_json in
      let gitCloneDepthOverride =
        field_map json__ "gitCloneDepthOverride" GitCloneDepth.of_json in
      let sourceAuthOverride =
        field_map json__ "sourceAuthOverride" SourceAuth.of_json in
      let sourceLocationOverride =
        field_map json__ "sourceLocationOverride" String_.of_json in
      let sourceTypeOverride =
        field_map json__ "sourceTypeOverride" SourceType.of_json in
      let environmentVariablesOverride =
        field_map json__ "environmentVariablesOverride"
          EnvironmentVariables.of_json in
      let secondaryArtifactsOverride =
        field_map json__ "secondaryArtifactsOverride"
          ProjectArtifactsList.of_json in
      let artifactsOverride =
        field_map json__ "artifactsOverride" ProjectArtifacts.of_json in
      let sourceVersion = field_map json__ "sourceVersion" String_.of_json in
      let secondarySourcesVersionOverride =
        field_map json__ "secondarySourcesVersionOverride"
          ProjectSecondarySourceVersions.of_json in
      let secondarySourcesOverride =
        field_map json__ "secondarySourcesOverride" ProjectSources.of_json in
      let projectName =
        field_map_exn json__ "projectName" NonEmptyString.of_json in
      make ?autoRetryLimitOverride ?fleetOverride ?debugSessionEnabled
        ?imagePullCredentialsTypeOverride ?registryCredentialOverride
        ?logsConfigOverride ?idempotencyToken ?encryptionKeyOverride
        ?queuedTimeoutInMinutesOverride ?timeoutInMinutesOverride
        ?privilegedModeOverride ?serviceRoleOverride ?cacheOverride
        ?certificateOverride ?computeTypeOverride ?imageOverride
        ?environmentTypeOverride ?buildStatusConfigOverride
        ?reportBuildStatusOverride ?insecureSslOverride ?buildspecOverride
        ?gitSubmodulesConfigOverride ?gitCloneDepthOverride
        ?sourceAuthOverride ?sourceLocationOverride ?sourceTypeOverride
        ?environmentVariablesOverride ?secondaryArtifactsOverride
        ?artifactsOverride ?sourceVersion ?secondarySourcesVersionOverride
        ?secondarySourcesOverride ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts running a build with the settings defined in the project. These setting include: how to run a build, where to get the source code, which build environment to use, which build commands to run, and where to store the build output. You can also start a build run by overriding some of the build settings in the project. The overrides only apply for that specific start build request. The settings in the project are unaltered."]
module StartBuildBatchOutput =
  struct
    type nonrec t =
      {
      buildBatch: BuildBatch.t option
        [@ocaml.doc
          "A BuildBatch object that contains information about the batch build."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?buildBatch = fun () -> { buildBatch }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("buildBatch", (Option.map x.buildBatch ~f:BuildBatch.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildBatch =
        (Option.map ~f:BuildBatch.of_xml) (Xml.child xml_arg0 "buildBatch") in
      make ?buildBatch ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildBatch = field_map json__ "buildBatch" BuildBatch.of_json in
      make ?buildBatch ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a batch build for a project."]
module StartBuildBatchInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t [@ocaml.doc "The name of the project."];
      secondarySourcesOverride: ProjectSources.t option
        [@ocaml.doc
          "An array of ProjectSource objects that override the secondary sources defined in the batch build project."];
      secondarySourcesVersionOverride:
        ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects that override the secondary source versions in the batch build project."];
      sourceVersion: String_.t option
        [@ocaml.doc
          "The version of the batch build input to be built, for this build only. If not specified, the latest version is used. If specified, the contents depends on the source provider: CodeCommit The commit ID, branch, or Git tag to use. GitHub The commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. Bitbucket The commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. Amazon S3 The version ID of the object that represents the build input ZIP file to use. If sourceVersion is specified at the project level, then this sourceVersion (at the build level) takes precedence. For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."];
      artifactsOverride: ProjectArtifacts.t option
        [@ocaml.doc
          "An array of ProjectArtifacts objects that contains information about the build output artifact overrides for the build project."];
      secondaryArtifactsOverride: ProjectArtifactsList.t option
        [@ocaml.doc
          "An array of ProjectArtifacts objects that override the secondary artifacts defined in the batch build project."];
      environmentVariablesOverride: EnvironmentVariables.t option
        [@ocaml.doc
          "An array of EnvironmentVariable objects that override, or add to, the environment variables defined in the batch build project."];
      sourceTypeOverride: SourceType.t option
        [@ocaml.doc
          "The source input type that overrides the source input defined in the batch build project."];
      sourceLocationOverride: String_.t option
        [@ocaml.doc
          "A location that overrides, for this batch build, the source location defined in the batch build project."];
      sourceAuthOverride: SourceAuth.t option
        [@ocaml.doc
          "A SourceAuth object that overrides the one defined in the batch build project. This override applies only if the build project's source is BitBucket or GitHub."];
      gitCloneDepthOverride: GitCloneDepth.t option
        [@ocaml.doc
          "The user-defined depth of history, with a minimum value of 0, that overrides, for this batch build only, any previous depth of history defined in the batch build project."];
      gitSubmodulesConfigOverride: GitSubmodulesConfig.t option
        [@ocaml.doc
          "A GitSubmodulesConfig object that overrides the Git submodules configuration for this batch build."];
      buildspecOverride: String_.t option
        [@ocaml.doc
          "A buildspec file declaration that overrides, for this build only, the latest one already defined in the build project. If this value is set, it can be either an inline buildspec definition, the path to an alternate buildspec file relative to the value of the built-in CODEBUILD_SRC_DIR environment variable, or the path to an S3 bucket. The bucket must be in the same Amazon Web Services Region as the build project. Specify the buildspec file using its ARN (for example, arn:aws:s3:::my-codebuild-sample2/buildspec.yml). If this value is not provided or is set to an empty string, the source code must contain a buildspec file in its root directory. For more information, see Buildspec File Name and Storage Location."];
      insecureSslOverride: WrapperBoolean.t option
        [@ocaml.doc
          "Enable this flag to override the insecure SSL setting that is specified in the batch build project. The insecure SSL setting determines whether to ignore SSL warnings while connecting to the project source code. This override applies only if the build's source is GitHub Enterprise."];
      reportBuildBatchStatusOverride: WrapperBoolean.t option
        [@ocaml.doc
          "Set to true to report to your source provider the status of a batch build's start and completion. If you use this option with a source provider other than GitHub, GitHub Enterprise, or Bitbucket, an invalidInputException is thrown. The status of a build triggered by a webhook is always reported to your source provider."];
      environmentTypeOverride: EnvironmentType.t option
        [@ocaml.doc
          "A container type for this batch build that overrides the one specified in the batch build project."];
      imageOverride: NonEmptyString.t option
        [@ocaml.doc
          "The name of an image for this batch build that overrides the one specified in the batch build project."];
      computeTypeOverride: ComputeType.t option
        [@ocaml.doc
          "The name of a compute type for this batch build that overrides the one specified in the batch build project."];
      certificateOverride: String_.t option
        [@ocaml.doc
          "The name of a certificate for this batch build that overrides the one specified in the batch build project."];
      cacheOverride: ProjectCache.t option
        [@ocaml.doc "A ProjectCache object that specifies cache overrides."];
      serviceRoleOverride: NonEmptyString.t option
        [@ocaml.doc
          "The name of a service role for this batch build that overrides the one specified in the batch build project."];
      privilegedModeOverride: WrapperBoolean.t option
        [@ocaml.doc
          "Enable this flag to override privileged mode in the batch build project."];
      buildTimeoutInMinutesOverride: BuildTimeOut.t option
        [@ocaml.doc
          "Overrides the build timeout specified in the batch build project."];
      queuedTimeoutInMinutesOverride: TimeOut.t option
        [@ocaml.doc
          "The number of minutes a batch build is allowed to be queued before it times out."];
      encryptionKeyOverride: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) that overrides the one specified in the batch build project. The CMK key encrypts the build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>)."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A unique, case sensitive identifier you provide to ensure the idempotency of the StartBuildBatch request. The token is included in the StartBuildBatch request and is valid for five minutes. If you repeat the StartBuildBatch request with the same token, but change a parameter, CodeBuild returns a parameter mismatch error."];
      logsConfigOverride: LogsConfig.t option
        [@ocaml.doc
          "A LogsConfig object that override the log settings defined in the batch build project."];
      registryCredentialOverride: RegistryCredential.t option
        [@ocaml.doc
          "A RegistryCredential object that overrides credentials for access to a private registry."];
      imagePullCredentialsTypeOverride: ImagePullCredentialsType.t option
        [@ocaml.doc
          "The type of credentials CodeBuild uses to pull images in your batch build. There are two valid values: CODEBUILD Specifies that CodeBuild uses its own credentials. This requires that you modify your ECR repository policy to trust CodeBuild's service principal. SERVICE_ROLE Specifies that CodeBuild uses your build project's service role. When using a cross-account or private registry image, you must use SERVICE_ROLE credentials. When using an CodeBuild curated image, you must use CODEBUILD credentials."];
      buildBatchConfigOverride: ProjectBuildBatchConfig.t option
        [@ocaml.doc
          "A BuildBatchConfigOverride object that contains batch build configuration overrides."];
      debugSessionEnabled: WrapperBoolean.t option
        [@ocaml.doc
          "Specifies if session debugging is enabled for this batch build. For more information, see Viewing a running build in Session Manager. Batch session debugging is not supported for matrix batch builds."]}
    let context_ = "StartBuildBatchInput"
    let make ?secondarySourcesOverride =
      fun ?secondarySourcesVersionOverride ->
        fun ?sourceVersion ->
          fun ?artifactsOverride ->
            fun ?secondaryArtifactsOverride ->
              fun ?environmentVariablesOverride ->
                fun ?sourceTypeOverride ->
                  fun ?sourceLocationOverride ->
                    fun ?sourceAuthOverride ->
                      fun ?gitCloneDepthOverride ->
                        fun ?gitSubmodulesConfigOverride ->
                          fun ?buildspecOverride ->
                            fun ?insecureSslOverride ->
                              fun ?reportBuildBatchStatusOverride ->
                                fun ?environmentTypeOverride ->
                                  fun ?imageOverride ->
                                    fun ?computeTypeOverride ->
                                      fun ?certificateOverride ->
                                        fun ?cacheOverride ->
                                          fun ?serviceRoleOverride ->
                                            fun ?privilegedModeOverride ->
                                              fun
                                                ?buildTimeoutInMinutesOverride
                                                ->
                                                fun
                                                  ?queuedTimeoutInMinutesOverride
                                                  ->
                                                  fun ?encryptionKeyOverride
                                                    ->
                                                    fun ?idempotencyToken ->
                                                      fun ?logsConfigOverride
                                                        ->
                                                        fun
                                                          ?registryCredentialOverride
                                                          ->
                                                          fun
                                                            ?imagePullCredentialsTypeOverride
                                                            ->
                                                            fun
                                                              ?buildBatchConfigOverride
                                                              ->
                                                              fun
                                                                ?debugSessionEnabled
                                                                ->
                                                                fun
                                                                  ~projectName
                                                                  ->
                                                                  fun () ->
                                                                    {
                                                                    secondarySourcesOverride;
                                                                    secondarySourcesVersionOverride;
                                                                    sourceVersion;
                                                                    artifactsOverride;
                                                                    secondaryArtifactsOverride;
                                                                    environmentVariablesOverride;
                                                                    sourceTypeOverride;
                                                                    sourceLocationOverride;
                                                                    sourceAuthOverride;
                                                                    gitCloneDepthOverride;
                                                                    gitSubmodulesConfigOverride;
                                                                    buildspecOverride;
                                                                    insecureSslOverride;
                                                                    reportBuildBatchStatusOverride;
                                                                    environmentTypeOverride;
                                                                    imageOverride;
                                                                    computeTypeOverride;
                                                                    certificateOverride;
                                                                    cacheOverride;
                                                                    serviceRoleOverride;
                                                                    privilegedModeOverride;
                                                                    buildTimeoutInMinutesOverride;
                                                                    queuedTimeoutInMinutesOverride;
                                                                    encryptionKeyOverride;
                                                                    idempotencyToken;
                                                                    logsConfigOverride;
                                                                    registryCredentialOverride;
                                                                    imagePullCredentialsTypeOverride;
                                                                    buildBatchConfigOverride;
                                                                    debugSessionEnabled;
                                                                    projectName
                                                                    }
    let to_value x =
      structure_to_value
        [("projectName", (Some (NonEmptyString.to_value x.projectName)));
        ("secondarySourcesOverride",
          (Option.map x.secondarySourcesOverride ~f:ProjectSources.to_value));
        ("secondarySourcesVersionOverride",
          (Option.map x.secondarySourcesVersionOverride
             ~f:ProjectSecondarySourceVersions.to_value));
        ("sourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("artifactsOverride",
          (Option.map x.artifactsOverride ~f:ProjectArtifacts.to_value));
        ("secondaryArtifactsOverride",
          (Option.map x.secondaryArtifactsOverride
             ~f:ProjectArtifactsList.to_value));
        ("environmentVariablesOverride",
          (Option.map x.environmentVariablesOverride
             ~f:EnvironmentVariables.to_value));
        ("sourceTypeOverride",
          (Option.map x.sourceTypeOverride ~f:SourceType.to_value));
        ("sourceLocationOverride",
          (Option.map x.sourceLocationOverride ~f:String_.to_value));
        ("sourceAuthOverride",
          (Option.map x.sourceAuthOverride ~f:SourceAuth.to_value));
        ("gitCloneDepthOverride",
          (Option.map x.gitCloneDepthOverride ~f:GitCloneDepth.to_value));
        ("gitSubmodulesConfigOverride",
          (Option.map x.gitSubmodulesConfigOverride
             ~f:GitSubmodulesConfig.to_value));
        ("buildspecOverride",
          (Option.map x.buildspecOverride ~f:String_.to_value));
        ("insecureSslOverride",
          (Option.map x.insecureSslOverride ~f:WrapperBoolean.to_value));
        ("reportBuildBatchStatusOverride",
          (Option.map x.reportBuildBatchStatusOverride
             ~f:WrapperBoolean.to_value));
        ("environmentTypeOverride",
          (Option.map x.environmentTypeOverride ~f:EnvironmentType.to_value));
        ("imageOverride",
          (Option.map x.imageOverride ~f:NonEmptyString.to_value));
        ("computeTypeOverride",
          (Option.map x.computeTypeOverride ~f:ComputeType.to_value));
        ("certificateOverride",
          (Option.map x.certificateOverride ~f:String_.to_value));
        ("cacheOverride",
          (Option.map x.cacheOverride ~f:ProjectCache.to_value));
        ("serviceRoleOverride",
          (Option.map x.serviceRoleOverride ~f:NonEmptyString.to_value));
        ("privilegedModeOverride",
          (Option.map x.privilegedModeOverride ~f:WrapperBoolean.to_value));
        ("buildTimeoutInMinutesOverride",
          (Option.map x.buildTimeoutInMinutesOverride
             ~f:BuildTimeOut.to_value));
        ("queuedTimeoutInMinutesOverride",
          (Option.map x.queuedTimeoutInMinutesOverride ~f:TimeOut.to_value));
        ("encryptionKeyOverride",
          (Option.map x.encryptionKeyOverride ~f:NonEmptyString.to_value));
        ("idempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("logsConfigOverride",
          (Option.map x.logsConfigOverride ~f:LogsConfig.to_value));
        ("registryCredentialOverride",
          (Option.map x.registryCredentialOverride
             ~f:RegistryCredential.to_value));
        ("imagePullCredentialsTypeOverride",
          (Option.map x.imagePullCredentialsTypeOverride
             ~f:ImagePullCredentialsType.to_value));
        ("buildBatchConfigOverride",
          (Option.map x.buildBatchConfigOverride
             ~f:ProjectBuildBatchConfig.to_value));
        ("debugSessionEnabled",
          (Option.map x.debugSessionEnabled ~f:WrapperBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let debugSessionEnabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "debugSessionEnabled") in
      let buildBatchConfigOverride =
        (Option.map ~f:ProjectBuildBatchConfig.of_xml)
          (Xml.child xml_arg0 "buildBatchConfigOverride") in
      let imagePullCredentialsTypeOverride =
        (Option.map ~f:ImagePullCredentialsType.of_xml)
          (Xml.child xml_arg0 "imagePullCredentialsTypeOverride") in
      let registryCredentialOverride =
        (Option.map ~f:RegistryCredential.of_xml)
          (Xml.child xml_arg0 "registryCredentialOverride") in
      let logsConfigOverride =
        (Option.map ~f:LogsConfig.of_xml)
          (Xml.child xml_arg0 "logsConfigOverride") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "idempotencyToken") in
      let encryptionKeyOverride =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyOverride") in
      let queuedTimeoutInMinutesOverride =
        (Option.map ~f:TimeOut.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutesOverride") in
      let buildTimeoutInMinutesOverride =
        (Option.map ~f:BuildTimeOut.of_xml)
          (Xml.child xml_arg0 "buildTimeoutInMinutesOverride") in
      let privilegedModeOverride =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "privilegedModeOverride") in
      let serviceRoleOverride =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "serviceRoleOverride") in
      let cacheOverride =
        (Option.map ~f:ProjectCache.of_xml)
          (Xml.child xml_arg0 "cacheOverride") in
      let certificateOverride =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "certificateOverride") in
      let computeTypeOverride =
        (Option.map ~f:ComputeType.of_xml)
          (Xml.child xml_arg0 "computeTypeOverride") in
      let imageOverride =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "imageOverride") in
      let environmentTypeOverride =
        (Option.map ~f:EnvironmentType.of_xml)
          (Xml.child xml_arg0 "environmentTypeOverride") in
      let reportBuildBatchStatusOverride =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "reportBuildBatchStatusOverride") in
      let insecureSslOverride =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "insecureSslOverride") in
      let buildspecOverride =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "buildspecOverride") in
      let gitSubmodulesConfigOverride =
        (Option.map ~f:GitSubmodulesConfig.of_xml)
          (Xml.child xml_arg0 "gitSubmodulesConfigOverride") in
      let gitCloneDepthOverride =
        (Option.map ~f:GitCloneDepth.of_xml)
          (Xml.child xml_arg0 "gitCloneDepthOverride") in
      let sourceAuthOverride =
        (Option.map ~f:SourceAuth.of_xml)
          (Xml.child xml_arg0 "sourceAuthOverride") in
      let sourceLocationOverride =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "sourceLocationOverride") in
      let sourceTypeOverride =
        (Option.map ~f:SourceType.of_xml)
          (Xml.child xml_arg0 "sourceTypeOverride") in
      let environmentVariablesOverride =
        (Option.map ~f:EnvironmentVariables.of_xml)
          (Xml.child xml_arg0 "environmentVariablesOverride") in
      let secondaryArtifactsOverride =
        (Option.map ~f:ProjectArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifactsOverride") in
      let artifactsOverride =
        (Option.map ~f:ProjectArtifacts.of_xml)
          (Xml.child xml_arg0 "artifactsOverride") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceVersion") in
      let secondarySourcesVersionOverride =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourcesVersionOverride") in
      let secondarySourcesOverride =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySourcesOverride") in
      let projectName =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ?debugSessionEnabled ?buildBatchConfigOverride
        ?imagePullCredentialsTypeOverride ?registryCredentialOverride
        ?logsConfigOverride ?idempotencyToken ?encryptionKeyOverride
        ?queuedTimeoutInMinutesOverride ?buildTimeoutInMinutesOverride
        ?privilegedModeOverride ?serviceRoleOverride ?cacheOverride
        ?certificateOverride ?computeTypeOverride ?imageOverride
        ?environmentTypeOverride ?reportBuildBatchStatusOverride
        ?insecureSslOverride ?buildspecOverride ?gitSubmodulesConfigOverride
        ?gitCloneDepthOverride ?sourceAuthOverride ?sourceLocationOverride
        ?sourceTypeOverride ?environmentVariablesOverride
        ?secondaryArtifactsOverride ?artifactsOverride ?sourceVersion
        ?secondarySourcesVersionOverride ?secondarySourcesOverride
        ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let debugSessionEnabled =
        field_map json__ "debugSessionEnabled" WrapperBoolean.of_json in
      let buildBatchConfigOverride =
        field_map json__ "buildBatchConfigOverride"
          ProjectBuildBatchConfig.of_json in
      let imagePullCredentialsTypeOverride =
        field_map json__ "imagePullCredentialsTypeOverride"
          ImagePullCredentialsType.of_json in
      let registryCredentialOverride =
        field_map json__ "registryCredentialOverride"
          RegistryCredential.of_json in
      let logsConfigOverride =
        field_map json__ "logsConfigOverride" LogsConfig.of_json in
      let idempotencyToken =
        field_map json__ "idempotencyToken" String_.of_json in
      let encryptionKeyOverride =
        field_map json__ "encryptionKeyOverride" NonEmptyString.of_json in
      let queuedTimeoutInMinutesOverride =
        field_map json__ "queuedTimeoutInMinutesOverride" TimeOut.of_json in
      let buildTimeoutInMinutesOverride =
        field_map json__ "buildTimeoutInMinutesOverride" BuildTimeOut.of_json in
      let privilegedModeOverride =
        field_map json__ "privilegedModeOverride" WrapperBoolean.of_json in
      let serviceRoleOverride =
        field_map json__ "serviceRoleOverride" NonEmptyString.of_json in
      let cacheOverride =
        field_map json__ "cacheOverride" ProjectCache.of_json in
      let certificateOverride =
        field_map json__ "certificateOverride" String_.of_json in
      let computeTypeOverride =
        field_map json__ "computeTypeOverride" ComputeType.of_json in
      let imageOverride =
        field_map json__ "imageOverride" NonEmptyString.of_json in
      let environmentTypeOverride =
        field_map json__ "environmentTypeOverride" EnvironmentType.of_json in
      let reportBuildBatchStatusOverride =
        field_map json__ "reportBuildBatchStatusOverride"
          WrapperBoolean.of_json in
      let insecureSslOverride =
        field_map json__ "insecureSslOverride" WrapperBoolean.of_json in
      let buildspecOverride =
        field_map json__ "buildspecOverride" String_.of_json in
      let gitSubmodulesConfigOverride =
        field_map json__ "gitSubmodulesConfigOverride"
          GitSubmodulesConfig.of_json in
      let gitCloneDepthOverride =
        field_map json__ "gitCloneDepthOverride" GitCloneDepth.of_json in
      let sourceAuthOverride =
        field_map json__ "sourceAuthOverride" SourceAuth.of_json in
      let sourceLocationOverride =
        field_map json__ "sourceLocationOverride" String_.of_json in
      let sourceTypeOverride =
        field_map json__ "sourceTypeOverride" SourceType.of_json in
      let environmentVariablesOverride =
        field_map json__ "environmentVariablesOverride"
          EnvironmentVariables.of_json in
      let secondaryArtifactsOverride =
        field_map json__ "secondaryArtifactsOverride"
          ProjectArtifactsList.of_json in
      let artifactsOverride =
        field_map json__ "artifactsOverride" ProjectArtifacts.of_json in
      let sourceVersion = field_map json__ "sourceVersion" String_.of_json in
      let secondarySourcesVersionOverride =
        field_map json__ "secondarySourcesVersionOverride"
          ProjectSecondarySourceVersions.of_json in
      let secondarySourcesOverride =
        field_map json__ "secondarySourcesOverride" ProjectSources.of_json in
      let projectName =
        field_map_exn json__ "projectName" NonEmptyString.of_json in
      make ?debugSessionEnabled ?buildBatchConfigOverride
        ?imagePullCredentialsTypeOverride ?registryCredentialOverride
        ?logsConfigOverride ?idempotencyToken ?encryptionKeyOverride
        ?queuedTimeoutInMinutesOverride ?buildTimeoutInMinutesOverride
        ?privilegedModeOverride ?serviceRoleOverride ?cacheOverride
        ?certificateOverride ?computeTypeOverride ?imageOverride
        ?environmentTypeOverride ?reportBuildBatchStatusOverride
        ?insecureSslOverride ?buildspecOverride ?gitSubmodulesConfigOverride
        ?gitCloneDepthOverride ?sourceAuthOverride ?sourceLocationOverride
        ?sourceTypeOverride ?environmentVariablesOverride
        ?secondaryArtifactsOverride ?artifactsOverride ?sourceVersion
        ?secondarySourcesVersionOverride ?secondarySourcesOverride
        ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Starts a batch build for a project."]
module RetryBuildOutput =
  struct
    type nonrec t = {
      build: Build.t option }
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?build = fun () -> { build }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("build", (Option.map x.build ~f:Build.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let build = (Option.map ~f:Build.of_xml) (Xml.child xml_arg0 "build") in
      make ?build ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let build = field_map json__ "build" Build.of_json in make ?build ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Restarts a build."]
module RetryBuildInput =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc "Specifies the identifier of the build to restart."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A unique, case sensitive identifier you provide to ensure the idempotency of the RetryBuild request. The token is included in the RetryBuild request and is valid for five minutes. If you repeat the RetryBuild request with the same token, but change a parameter, CodeBuild returns a parameter mismatch error."]}
    let make ?id =
      fun ?idempotencyToken -> fun () -> { id; idempotencyToken }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("idempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "idempotencyToken") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?idempotencyToken ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let idempotencyToken =
        field_map json__ "idempotencyToken" String_.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?idempotencyToken ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Restarts a build."]
module RetryBuildBatchOutput =
  struct
    type nonrec t = {
      buildBatch: BuildBatch.t option }
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?buildBatch = fun () -> { buildBatch }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("buildBatch", (Option.map x.buildBatch ~f:BuildBatch.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildBatch =
        (Option.map ~f:BuildBatch.of_xml) (Xml.child xml_arg0 "buildBatch") in
      make ?buildBatch ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildBatch = field_map json__ "buildBatch" BuildBatch.of_json in
      make ?buildBatch ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restarts a failed batch build. Only batch builds that have failed can be retried."]
module RetryBuildBatchInput =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t option
        [@ocaml.doc
          "Specifies the identifier of the batch build to restart."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A unique, case sensitive identifier you provide to ensure the idempotency of the RetryBuildBatch request. The token is included in the RetryBuildBatch request and is valid for five minutes. If you repeat the RetryBuildBatch request with the same token, but change a parameter, CodeBuild returns a parameter mismatch error."];
      retryType: RetryBuildBatchType.t option
        [@ocaml.doc "Specifies the type of retry to perform."]}
    let make ?id =
      fun ?idempotencyToken ->
        fun ?retryType -> fun () -> { id; idempotencyToken; retryType }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:NonEmptyString.to_value));
        ("idempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("retryType",
          (Option.map x.retryType ~f:RetryBuildBatchType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryType =
        (Option.map ~f:RetryBuildBatchType.of_xml)
          (Xml.child xml_arg0 "retryType") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "idempotencyToken") in
      let id =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "id") in
      make ?retryType ?idempotencyToken ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryType =
        field_map json__ "retryType" RetryBuildBatchType.of_json in
      let idempotencyToken =
        field_map json__ "idempotencyToken" String_.of_json in
      let id = field_map json__ "id" NonEmptyString.of_json in
      make ?retryType ?idempotencyToken ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restarts a failed batch build. Only batch builds that have failed can be retried."]
module PutResourcePolicyOutput =
  struct
    type nonrec t =
      {
      resourceArn: NonEmptyString.t option
        [@ocaml.doc
          "The ARN of the Project or ReportGroup resource that is associated with a resource policy."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resourceArn = fun () -> { resourceArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("resourceArn",
           (Option.map x.resourceArn ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "resourceArn") in
      make ?resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn = field_map json__ "resourceArn" NonEmptyString.of_json in
      make ?resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stores a resource policy for the ARN of a Project or ReportGroup object."]
module PutResourcePolicyInput =
  struct
    type nonrec t =
      {
      policy: NonEmptyString.t
        [@ocaml.doc
          "A JSON-formatted resource policy. For more information, see Sharing a Project and Sharing a Report Group in the CodeBuild User Guide."];
      resourceArn: NonEmptyString.t
        [@ocaml.doc
          "The ARN of the Project or ReportGroup resource you want to associate with a resource policy."]}
    let context_ = "PutResourcePolicyInput"
    let make ~policy = fun ~resourceArn -> fun () -> { policy; resourceArn }
    let to_value x =
      structure_to_value
        [("policy", (Some (NonEmptyString.to_value x.policy)));
        ("resourceArn", (Some (NonEmptyString.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      let policy =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "policy") in
      make ~resourceArn ~policy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" NonEmptyString.of_json in
      let policy = field_map_exn json__ "policy" NonEmptyString.of_json in
      make ~resourceArn ~policy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stores a resource policy for the ARN of a Project or ReportGroup object."]
module ListSourceCredentialsOutput =
  struct
    type nonrec t =
      {
      sourceCredentialsInfos: SourceCredentialsInfos.t option
        [@ocaml.doc
          "A list of SourceCredentialsInfo objects. Each SourceCredentialsInfo object includes the authentication type, token ARN, and type of source provider for one set of credentials."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sourceCredentialsInfos = fun () -> { sourceCredentialsInfos }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sourceCredentialsInfos",
           (Option.map x.sourceCredentialsInfos
              ~f:SourceCredentialsInfos.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceCredentialsInfos =
        (Option.map ~f:SourceCredentialsInfos.of_xml)
          (Xml.child xml_arg0 "sourceCredentialsInfos") in
      make ?sourceCredentialsInfos ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceCredentialsInfos =
        field_map json__ "sourceCredentialsInfos"
          SourceCredentialsInfos.of_json in
      make ?sourceCredentialsInfos ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of SourceCredentialsInfo objects."]
module ListSourceCredentialsInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of SourceCredentialsInfo objects."]
module ListSharedReportGroupsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      reportGroups: ReportGroupArns.t option
        [@ocaml.doc
          "The list of ARNs for the report groups shared with the current Amazon Web Services account or user."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?reportGroups -> fun () -> { nextToken; reportGroups }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reportGroups",
          (Option.map x.reportGroups ~f:ReportGroupArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportGroups =
        (Option.map ~f:ReportGroupArns.of_xml)
          (Xml.child xml_arg0 "reportGroups") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reportGroups ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportGroups =
        field_map json__ "reportGroups" ReportGroupArns.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?reportGroups ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of report groups that are shared with other Amazon Web Services accounts or users."]
module ListSharedReportGroupsInput =
  struct
    type nonrec t =
      {
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which to list shared report groups. Valid values include: ASCENDING: List in ascending order. DESCENDING: List in descending order."];
      sortBy: SharedResourceSortByType.t option
        [@ocaml.doc
          "The criterion to be used to list report groups shared with the current Amazon Web Services account or user. Valid values include: ARN: List based on the ARN. MODIFIED_TIME: List based on when information about the shared report group was last changed."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated shared report groups per response. Use nextToken to iterate pages in the list of returned ReportGroup objects. The default value is 100."]}
    let make ?sortOrder =
      fun ?sortBy ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () -> { sortOrder; sortBy; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("sortBy",
          (Option.map x.sortBy ~f:SharedResourceSortByType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortBy =
        (Option.map ~f:SharedResourceSortByType.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      make ?maxResults ?nextToken ?sortBy ?sortOrder ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortBy = field_map json__ "sortBy" SharedResourceSortByType.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      make ?maxResults ?nextToken ?sortBy ?sortOrder ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of report groups that are shared with other Amazon Web Services accounts or users."]
module ListSharedProjectsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      projects: ProjectArns.t option
        [@ocaml.doc
          "The list of ARNs for the build projects shared with the current Amazon Web Services account or user."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?projects -> fun () -> { nextToken; projects }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("projects", (Option.map x.projects ~f:ProjectArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projects =
        (Option.map ~f:ProjectArns.of_xml) (Xml.child xml_arg0 "projects") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?projects ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let projects = field_map json__ "projects" ProjectArns.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?projects ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of projects that are shared with other Amazon Web Services accounts or users."]
module ListSharedProjectsInput =
  struct
    type nonrec t =
      {
      sortBy: SharedResourceSortByType.t option
        [@ocaml.doc
          "The criterion to be used to list build projects shared with the current Amazon Web Services account or user. Valid values include: ARN: List based on the ARN. MODIFIED_TIME: List based on when information about the shared project was last changed."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which to list shared build projects. Valid values include: ASCENDING: List in ascending order. DESCENDING: List in descending order."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated shared build projects returned per response. Use nextToken to iterate pages in the list of returned Project objects. The default value is 100."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."]}
    let make ?sortBy =
      fun ?sortOrder ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { sortBy; sortOrder; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("sortBy",
           (Option.map x.sortBy ~f:SharedResourceSortByType.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let sortBy =
        (Option.map ~f:SharedResourceSortByType.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      make ?nextToken ?maxResults ?sortOrder ?sortBy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NonEmptyString.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let sortBy = field_map json__ "sortBy" SharedResourceSortByType.of_json in
      make ?nextToken ?maxResults ?sortOrder ?sortBy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of projects that are shared with other Amazon Web Services accounts or users."]
module ListSandboxesOutput =
  struct
    type nonrec t =
      {
      ids: SandboxIds.t option
        [@ocaml.doc "Information about the requested sandbox IDs."];
      nextToken: String_.t option
        [@ocaml.doc
          "Information about the next token to get paginated results."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ids = fun ?nextToken -> fun () -> { ids; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ids", (Option.map x.ids ~f:SandboxIds.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ids = (Option.map ~f:SandboxIds.of_xml) (Xml.child xml_arg0 "ids") in
      make ?nextToken ?ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let ids = field_map json__ "ids" SandboxIds.of_json in
      make ?nextToken ?ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets a list of sandboxes."]
module ListSandboxesInput =
  struct
    type nonrec t =
      {
      maxResults: PageSize.t option
        [@ocaml.doc "The maximum number of sandbox records to be retrieved."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which sandbox records should be retrieved."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next token, if any, to get paginated results. You will get this value from previous execution of list sandboxes."]}
    let make ?maxResults =
      fun ?sortOrder ->
        fun ?nextToken -> fun () -> { maxResults; sortOrder; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?sortOrder ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      make ?nextToken ?sortOrder ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets a list of sandboxes."]
module ListSandboxesForProjectOutput =
  struct
    type nonrec t =
      {
      ids: SandboxIds.t option
        [@ocaml.doc "Information about the requested sandbox IDs."];
      nextToken: String_.t option
        [@ocaml.doc
          "Information about the next token to get paginated results."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ids = fun ?nextToken -> fun () -> { ids; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ids", (Option.map x.ids ~f:SandboxIds.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ids = (Option.map ~f:SandboxIds.of_xml) (Xml.child xml_arg0 "ids") in
      make ?nextToken ?ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let ids = field_map json__ "ids" SandboxIds.of_json in
      make ?nextToken ?ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets a list of sandboxes for a given project."]
module ListSandboxesForProjectInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t
        [@ocaml.doc "The CodeBuild project name."];
      maxResults: PageSize.t option
        [@ocaml.doc "The maximum number of sandbox records to be retrieved."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which sandbox records should be retrieved."];
      nextToken: SensitiveString.t option
        [@ocaml.doc
          "The next token, if any, to get paginated results. You will get this value from previous execution of list sandboxes."]}
    let context_ = "ListSandboxesForProjectInput"
    let make ?maxResults =
      fun ?sortOrder ->
        fun ?nextToken ->
          fun ~projectName ->
            fun () -> { maxResults; sortOrder; nextToken; projectName }
    let to_value x =
      structure_to_value
        [("projectName", (Some (NonEmptyString.to_value x.projectName)));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let projectName =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ?nextToken ?sortOrder ?maxResults ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" SensitiveString.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let projectName =
        field_map_exn json__ "projectName" NonEmptyString.of_json in
      make ?nextToken ?sortOrder ?maxResults ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets a list of sandboxes for a given project."]
module ListReportsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      reports: ReportArns.t option
        [@ocaml.doc
          "The list of returned ARNs for the reports in the current Amazon Web Services account."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?reports -> fun () -> { nextToken; reports }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reports", (Option.map x.reports ~f:ReportArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reports =
        (Option.map ~f:ReportArns.of_xml) (Xml.child xml_arg0 "reports") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reports ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reports = field_map json__ "reports" ReportArns.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?reports ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of ARNs for the reports in the current Amazon Web Services account."]
module ListReportsInput =
  struct
    type nonrec t =
      {
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "Specifies the sort order for the list of returned reports. Valid values are: ASCENDING: return reports in chronological order based on their creation date. DESCENDING: return reports in the reverse chronological order based on their creation date."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated reports returned per response. Use nextToken to iterate pages in the list of returned Report objects. The default value is 100."];
      filter: ReportFilter.t option
        [@ocaml.doc
          "A ReportFilter object used to filter the returned reports."]}
    let make ?sortOrder =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?filter ->
            fun () -> { sortOrder; nextToken; maxResults; filter }
    let to_value x =
      structure_to_value
        [("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("filter", (Option.map x.filter ~f:ReportFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:ReportFilter.of_xml) (Xml.child xml_arg0 "filter") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      make ?filter ?maxResults ?nextToken ?sortOrder ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filter = field_map json__ "filter" ReportFilter.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      make ?filter ?maxResults ?nextToken ?sortOrder ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of ARNs for the reports in the current Amazon Web Services account."]
module ListReportsForReportGroupOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      reports: ReportArns.t option [@ocaml.doc "The list of report ARNs."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?reports -> fun () -> { nextToken; reports }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reports", (Option.map x.reports ~f:ReportArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reports =
        (Option.map ~f:ReportArns.of_xml) (Xml.child xml_arg0 "reports") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reports ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reports = field_map json__ "reports" ReportArns.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?reports ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of ARNs for the reports that belong to a ReportGroup."]
module ListReportsForReportGroupInput =
  struct
    type nonrec t =
      {
      reportGroupArn: String_.t
        [@ocaml.doc
          "The ARN of the report group for which you want to return report ARNs."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "Use to specify whether the results are returned in ascending or descending order."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated reports in this report group returned per response. Use nextToken to iterate pages in the list of returned Report objects. The default value is 100."];
      filter: ReportFilter.t option
        [@ocaml.doc
          "A ReportFilter object used to filter the returned reports."]}
    let context_ = "ListReportsForReportGroupInput"
    let make ?nextToken =
      fun ?sortOrder ->
        fun ?maxResults ->
          fun ?filter ->
            fun ~reportGroupArn ->
              fun () ->
                { nextToken; sortOrder; maxResults; filter; reportGroupArn }
    let to_value x =
      structure_to_value
        [("reportGroupArn", (Some (String_.to_value x.reportGroupArn)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("filter", (Option.map x.filter ~f:ReportFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:ReportFilter.of_xml) (Xml.child xml_arg0 "filter") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reportGroupArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportGroupArn") in
      make ?filter ?maxResults ?sortOrder ?nextToken ~reportGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filter = field_map json__ "filter" ReportFilter.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let reportGroupArn =
        field_map_exn json__ "reportGroupArn" String_.of_json in
      make ?filter ?maxResults ?sortOrder ?nextToken ~reportGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of ARNs for the reports that belong to a ReportGroup."]
module ListReportGroupsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      reportGroups: ReportGroupArns.t option
        [@ocaml.doc
          "The list of ARNs for the report groups in the current Amazon Web Services account."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?reportGroups -> fun () -> { nextToken; reportGroups }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("reportGroups",
          (Option.map x.reportGroups ~f:ReportGroupArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportGroups =
        (Option.map ~f:ReportGroupArns.of_xml)
          (Xml.child xml_arg0 "reportGroups") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?reportGroups ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportGroups =
        field_map json__ "reportGroups" ReportGroupArns.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?reportGroups ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list ARNs for the report groups in the current Amazon Web Services account."]
module ListReportGroupsInput =
  struct
    type nonrec t =
      {
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "Used to specify the order to sort the list of returned report groups. Valid values are ASCENDING and DESCENDING."];
      sortBy: ReportGroupSortByType.t option
        [@ocaml.doc
          "The criterion to be used to list build report groups. Valid values include: CREATED_TIME: List based on when each report group was created. LAST_MODIFIED_TIME: List based on when each report group was last changed. NAME: List based on each report group's name."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated report groups returned per response. Use nextToken to iterate pages in the list of returned ReportGroup objects. The default value is 100."]}
    let make ?sortOrder =
      fun ?sortBy ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () -> { sortOrder; sortBy; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("sortBy", (Option.map x.sortBy ~f:ReportGroupSortByType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortBy =
        (Option.map ~f:ReportGroupSortByType.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      make ?maxResults ?nextToken ?sortBy ?sortOrder ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortBy = field_map json__ "sortBy" ReportGroupSortByType.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      make ?maxResults ?nextToken ?sortBy ?sortOrder ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list ARNs for the report groups in the current Amazon Web Services account."]
module ListProjectsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call."];
      projects: ProjectNames.t option
        [@ocaml.doc
          "The list of build project names, with each build project name representing a single build project."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?projects -> fun () -> { nextToken; projects }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("projects", (Option.map x.projects ~f:ProjectNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projects =
        (Option.map ~f:ProjectNames.of_xml) (Xml.child xml_arg0 "projects") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?projects ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let projects = field_map json__ "projects" ProjectNames.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?projects ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of build project names, with each build project name representing a single build project."]
module ListProjectsInput =
  struct
    type nonrec t =
      {
      sortBy: ProjectSortByType.t option
        [@ocaml.doc
          "The criterion to be used to list build project names. Valid values include: CREATED_TIME: List based on when each build project was created. LAST_MODIFIED_TIME: List based on when information about each build project was last changed. NAME: List based on each build project's name. Use sortOrder to specify in what order to list the build project names based on the preceding criteria."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which to list build projects. Valid values include: ASCENDING: List in ascending order. DESCENDING: List in descending order. Use sortBy to specify the criterion to be used to list build project names."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."]}
    let make ?sortBy =
      fun ?sortOrder ->
        fun ?nextToken -> fun () -> { sortBy; sortOrder; nextToken }
    let to_value x =
      structure_to_value
        [("sortBy", (Option.map x.sortBy ~f:ProjectSortByType.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let sortBy =
        (Option.map ~f:ProjectSortByType.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      make ?nextToken ?sortOrder ?sortBy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NonEmptyString.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let sortBy = field_map json__ "sortBy" ProjectSortByType.of_json in
      make ?nextToken ?sortOrder ?sortBy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of build project names, with each build project name representing a single build project."]
module ListFleetsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call."];
      fleets: FleetArns.t option
        [@ocaml.doc "The list of compute fleet names."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?fleets -> fun () -> { nextToken; fleets }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("fleets", (Option.map x.fleets ~f:FleetArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleets =
        (Option.map ~f:FleetArns.of_xml) (Xml.child xml_arg0 "fleets") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?fleets ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleets = field_map json__ "fleets" FleetArns.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?fleets ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of compute fleet names with each compute fleet name representing a single compute fleet."]
module ListFleetsInput =
  struct
    type nonrec t =
      {
      nextToken: SensitiveString.t option
        [@ocaml.doc
          "During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated compute fleets returned per response. Use nextToken to iterate pages in the list of returned compute fleets."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which to list compute fleets. Valid values include: ASCENDING: List in ascending order. DESCENDING: List in descending order. Use sortBy to specify the criterion to be used to list compute fleet names."];
      sortBy: FleetSortByType.t option
        [@ocaml.doc
          "The criterion to be used to list compute fleet names. Valid values include: CREATED_TIME: List based on when each compute fleet was created. LAST_MODIFIED_TIME: List based on when information about each compute fleet was last changed. NAME: List based on each compute fleet's name. Use sortOrder to specify in what order to list the compute fleet names based on the preceding criteria."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortOrder ->
          fun ?sortBy ->
            fun () -> { nextToken; maxResults; sortOrder; sortBy }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:SensitiveString.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("sortBy", (Option.map x.sortBy ~f:FleetSortByType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortBy =
        (Option.map ~f:FleetSortByType.of_xml) (Xml.child xml_arg0 "sortBy") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?sortBy ?sortOrder ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortBy = field_map json__ "sortBy" FleetSortByType.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let nextToken = field_map json__ "nextToken" SensitiveString.of_json in
      make ?sortBy ?sortOrder ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of compute fleet names with each compute fleet name representing a single compute fleet."]
module ListCuratedEnvironmentImagesOutput =
  struct
    type nonrec t =
      {
      platforms: EnvironmentPlatforms.t option
        [@ocaml.doc
          "Information about supported platforms for Docker images that are managed by CodeBuild."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?platforms = fun () -> { platforms }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("platforms",
           (Option.map x.platforms ~f:EnvironmentPlatforms.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let platforms =
        (Option.map ~f:EnvironmentPlatforms.of_xml)
          (Xml.child xml_arg0 "platforms") in
      make ?platforms ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let platforms =
        field_map json__ "platforms" EnvironmentPlatforms.of_json in
      make ?platforms ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about Docker images that are managed by CodeBuild."]
module ListCuratedEnvironmentImagesInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about Docker images that are managed by CodeBuild."]
module ListCommandExecutionsForSandboxOutput =
  struct
    type nonrec t =
      {
      commandExecutions: CommandExecutions.t option
        [@ocaml.doc "Information about the requested command executions."];
      nextToken: String_.t option
        [@ocaml.doc
          "Information about the next token to get paginated results."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?commandExecutions =
      fun ?nextToken -> fun () -> { commandExecutions; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("commandExecutions",
           (Option.map x.commandExecutions ~f:CommandExecutions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let commandExecutions =
        (Option.map ~f:CommandExecutions.of_xml)
          (Xml.child xml_arg0 "commandExecutions") in
      make ?nextToken ?commandExecutions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let commandExecutions =
        field_map json__ "commandExecutions" CommandExecutions.of_json in
      make ?nextToken ?commandExecutions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets a list of command executions for a sandbox."]
module ListCommandExecutionsForSandboxInput =
  struct
    type nonrec t =
      {
      sandboxId: NonEmptyString.t [@ocaml.doc "A sandboxId or sandboxArn."];
      maxResults: PageSize.t option
        [@ocaml.doc "The maximum number of sandbox records to be retrieved."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order in which sandbox records should be retrieved."];
      nextToken: SensitiveString.t option
        [@ocaml.doc
          "The next token, if any, to get paginated results. You will get this value from previous execution of list sandboxes."]}
    let context_ = "ListCommandExecutionsForSandboxInput"
    let make ?maxResults =
      fun ?sortOrder ->
        fun ?nextToken ->
          fun ~sandboxId ->
            fun () -> { maxResults; sortOrder; nextToken; sandboxId }
    let to_value x =
      structure_to_value
        [("sandboxId", (Some (NonEmptyString.to_value x.sandboxId)));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let sandboxId =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sandboxId") in
      make ?nextToken ?sortOrder ?maxResults ~sandboxId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" SensitiveString.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let sandboxId = field_map_exn json__ "sandboxId" NonEmptyString.of_json in
      make ?nextToken ?sortOrder ?maxResults ~sandboxId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets a list of command executions for a sandbox."]
module ListBuildsOutput =
  struct
    type nonrec t =
      {
      ids: BuildIds.t option
        [@ocaml.doc
          "A list of build IDs, with each build ID representing a single build."];
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ids = fun ?nextToken -> fun () -> { ids; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ids", (Option.map x.ids ~f:BuildIds.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ids = (Option.map ~f:BuildIds.of_xml) (Xml.child xml_arg0 "ids") in
      make ?nextToken ?ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let ids = field_map json__ "ids" BuildIds.of_json in
      make ?nextToken ?ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of build IDs, with each build ID representing a single build."]
module ListBuildsInput =
  struct
    type nonrec t =
      {
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order to list build IDs. Valid values include: ASCENDING: List the build IDs in ascending order by build ID. DESCENDING: List the build IDs in descending order by build ID."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."]}
    let make ?sortOrder =
      fun ?nextToken -> fun () -> { sortOrder; nextToken }
    let to_value x =
      structure_to_value
        [("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      make ?nextToken ?sortOrder ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      make ?nextToken ?sortOrder ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of build IDs, with each build ID representing a single build."]
module ListBuildsForProjectOutput =
  struct
    type nonrec t =
      {
      ids: BuildIds.t option
        [@ocaml.doc
          "A list of build identifiers for the specified build project, with each build ID representing a single build."];
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ids = fun ?nextToken -> fun () -> { ids; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ids", (Option.map x.ids ~f:BuildIds.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ids = (Option.map ~f:BuildIds.of_xml) (Xml.child xml_arg0 "ids") in
      make ?nextToken ?ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let ids = field_map json__ "ids" BuildIds.of_json in
      make ?nextToken ?ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of build identifiers for the specified build project, with each build identifier representing a single build."]
module ListBuildsForProjectInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t
        [@ocaml.doc "The name of the CodeBuild project."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "The order to sort the results in. The results are sorted by build number, not the build identifier. If this is not specified, the results are sorted in descending order. Valid values include: ASCENDING: List the build identifiers in ascending order, by build number. DESCENDING: List the build identifiers in descending order, by build number. If the project has more than 100 builds, setting the sort order will result in an error."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, if there are more than 100 items in the list, only the first 100 items are returned, along with a unique string called a nextToken. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."]}
    let context_ = "ListBuildsForProjectInput"
    let make ?sortOrder =
      fun ?nextToken ->
        fun ~projectName -> fun () -> { sortOrder; nextToken; projectName }
    let to_value x =
      structure_to_value
        [("projectName", (Some (NonEmptyString.to_value x.projectName)));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let projectName =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ?nextToken ?sortOrder ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let projectName =
        field_map_exn json__ "projectName" NonEmptyString.of_json in
      make ?nextToken ?sortOrder ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of build identifiers for the specified build project, with each build identifier representing a single build."]
module ListBuildBatchesOutput =
  struct
    type nonrec t =
      {
      ids: BuildBatchIds.t option
        [@ocaml.doc
          "An array of strings that contains the batch build identifiers."];
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more items to return, this contains a token that is passed to a subsequent call to ListBuildBatches to retrieve the next set of items."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ids = fun ?nextToken -> fun () -> { ids; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ids", (Option.map x.ids ~f:BuildBatchIds.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ids =
        (Option.map ~f:BuildBatchIds.of_xml) (Xml.child xml_arg0 "ids") in
      make ?nextToken ?ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let ids = field_map json__ "ids" BuildBatchIds.of_json in
      make ?nextToken ?ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the identifiers of your build batches in the current region."]
module ListBuildBatchesInput =
  struct
    type nonrec t =
      {
      filter: BuildBatchFilter.t option
        [@ocaml.doc
          "A BuildBatchFilter object that specifies the filters for the search."];
      maxResults: PageSize.t option
        [@ocaml.doc "The maximum number of results to return."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "Specifies the sort order of the returned items. Valid values include: ASCENDING: List the batch build identifiers in ascending order by identifier. DESCENDING: List the batch build identifiers in descending order by identifier."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a previous call to ListBuildBatches. This specifies the next item to return. To return the beginning of the list, exclude this parameter."]}
    let make ?filter =
      fun ?maxResults ->
        fun ?sortOrder ->
          fun ?nextToken ->
            fun () -> { filter; maxResults; sortOrder; nextToken }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:BuildBatchFilter.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let filter =
        (Option.map ~f:BuildBatchFilter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?nextToken ?sortOrder ?maxResults ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let filter = field_map json__ "filter" BuildBatchFilter.of_json in
      make ?nextToken ?sortOrder ?maxResults ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the identifiers of your build batches in the current region."]
module ListBuildBatchesForProjectOutput =
  struct
    type nonrec t =
      {
      ids: BuildBatchIds.t option
        [@ocaml.doc
          "An array of strings that contains the batch build identifiers."];
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more items to return, this contains a token that is passed to a subsequent call to ListBuildBatchesForProject to retrieve the next set of items."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ids = fun ?nextToken -> fun () -> { ids; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ids", (Option.map x.ids ~f:BuildBatchIds.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let ids =
        (Option.map ~f:BuildBatchIds.of_xml) (Xml.child xml_arg0 "ids") in
      make ?nextToken ?ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let ids = field_map json__ "ids" BuildBatchIds.of_json in
      make ?nextToken ?ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the identifiers of the build batches for a specific project."]
module ListBuildBatchesForProjectInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t option
        [@ocaml.doc "The name of the project."];
      filter: BuildBatchFilter.t option
        [@ocaml.doc
          "A BuildBatchFilter object that specifies the filters for the search."];
      maxResults: PageSize.t option
        [@ocaml.doc "The maximum number of results to return."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "Specifies the sort order of the returned items. Valid values include: ASCENDING: List the batch build identifiers in ascending order by identifier. DESCENDING: List the batch build identifiers in descending order by identifier."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a previous call to ListBuildBatchesForProject. This specifies the next item to return. To return the beginning of the list, exclude this parameter."]}
    let make ?projectName =
      fun ?filter ->
        fun ?maxResults ->
          fun ?sortOrder ->
            fun ?nextToken ->
              fun () ->
                { projectName; filter; maxResults; sortOrder; nextToken }
    let to_value x =
      structure_to_value
        [("projectName",
           (Option.map x.projectName ~f:NonEmptyString.to_value));
        ("filter", (Option.map x.filter ~f:BuildBatchFilter.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let filter =
        (Option.map ~f:BuildBatchFilter.of_xml) (Xml.child xml_arg0 "filter") in
      let projectName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "projectName") in
      make ?nextToken ?sortOrder ?maxResults ?filter ?projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let filter = field_map json__ "filter" BuildBatchFilter.of_json in
      let projectName = field_map json__ "projectName" NonEmptyString.of_json in
      make ?nextToken ?sortOrder ?maxResults ?filter ?projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the identifiers of the build batches for a specific project."]
module InvalidateProjectCacheOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Resets the cache for a project."]
module InvalidateProjectCacheInput =
  struct
    type nonrec t =
      {
      projectName: NonEmptyString.t
        [@ocaml.doc
          "The name of the CodeBuild build project that the cache is reset for."]}
    let context_ = "InvalidateProjectCacheInput"
    let make ~projectName = fun () -> { projectName }
    let to_value x =
      structure_to_value
        [("projectName", (Some (NonEmptyString.to_value x.projectName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectName =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let projectName =
        field_map_exn json__ "projectName" NonEmptyString.of_json in
      make ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Resets the cache for a project."]
module ImportSourceCredentialsOutput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the token."]}
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun () -> { arn }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      make ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "arn" NonEmptyString.of_json in make ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the source repository credentials for an CodeBuild project that has its source code stored in a GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket repository."]
module ImportSourceCredentialsInput =
  struct
    type nonrec t =
      {
      username: NonEmptyString.t option
        [@ocaml.doc
          "The Bitbucket username when the authType is BASIC_AUTH. This parameter is not valid for other types of source providers or connections."];
      token: SensitiveNonEmptyString.t
        [@ocaml.doc
          "For GitHub or GitHub Enterprise, this is the personal access token. For Bitbucket, this is either the access token or the app password. For the authType CODECONNECTIONS, this is the connectionArn. For the authType SECRETS_MANAGER, this is the secretArn."];
      serverType: ServerType.t
        [@ocaml.doc "The source provider used for this project."];
      authType: AuthType.t
        [@ocaml.doc
          "The type of authentication used to connect to a GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket repository. An OAUTH connection is not supported by the API and must be created using the CodeBuild console."];
      shouldOverwrite: WrapperBoolean.t option
        [@ocaml.doc
          "Set to false to prevent overwriting the repository source credentials. Set to true to overwrite the repository source credentials. The default value is true."]}
    let context_ = "ImportSourceCredentialsInput"
    let make ?username =
      fun ?shouldOverwrite ->
        fun ~token ->
          fun ~serverType ->
            fun ~authType ->
              fun () ->
                { username; shouldOverwrite; token; serverType; authType }
    let to_value x =
      structure_to_value
        [("username", (Option.map x.username ~f:NonEmptyString.to_value));
        ("token", (Some (SensitiveNonEmptyString.to_value x.token)));
        ("serverType", (Some (ServerType.to_value x.serverType)));
        ("authType", (Some (AuthType.to_value x.authType)));
        ("shouldOverwrite",
          (Option.map x.shouldOverwrite ~f:WrapperBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shouldOverwrite =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "shouldOverwrite") in
      let authType =
        AuthType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "authType") in
      let serverType =
        ServerType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "serverType") in
      let token =
        SensitiveNonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "token") in
      let username =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "username") in
      make ?shouldOverwrite ~authType ~serverType ~token ?username ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let shouldOverwrite =
        field_map json__ "shouldOverwrite" WrapperBoolean.of_json in
      let authType = field_map_exn json__ "authType" AuthType.of_json in
      let serverType = field_map_exn json__ "serverType" ServerType.of_json in
      let token =
        field_map_exn json__ "token" SensitiveNonEmptyString.of_json in
      let username = field_map json__ "username" NonEmptyString.of_json in
      make ?shouldOverwrite ~authType ~serverType ~token ?username ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the source repository credentials for an CodeBuild project that has its source code stored in a GitHub, GitHub Enterprise, GitLab, GitLab Self Managed, or Bitbucket repository."]
module GetResourcePolicyOutput =
  struct
    type nonrec t =
      {
      policy: NonEmptyString.t option
        [@ocaml.doc
          "The resource policy for the resource identified by the input ARN parameter."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policy = fun () -> { policy }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("policy", (Option.map x.policy ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "policy") in
      make ?policy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policy = field_map json__ "policy" NonEmptyString.of_json in
      make ?policy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a resource policy that is identified by its resource ARN."]
module GetResourcePolicyInput =
  struct
    type nonrec t =
      {
      resourceArn: NonEmptyString.t
        [@ocaml.doc
          "The ARN of the resource that is associated with the resource policy."]}
    let context_ = "GetResourcePolicyInput"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (NonEmptyString.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" NonEmptyString.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a resource policy that is identified by its resource ARN."]
module GetReportGroupTrendOutput =
  struct
    type nonrec t =
      {
      stats: ReportGroupTrendStats.t option
        [@ocaml.doc "Contains the accumulated trend data."];
      rawData: ReportGroupTrendRawDataList.t option
        [@ocaml.doc "An array that contains the raw data for each report."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stats = fun ?rawData -> fun () -> { stats; rawData }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("stats", (Option.map x.stats ~f:ReportGroupTrendStats.to_value));
        ("rawData",
          (Option.map x.rawData ~f:ReportGroupTrendRawDataList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rawData =
        (Option.map ~f:ReportGroupTrendRawDataList.of_xml)
          (Xml.child xml_arg0 "rawData") in
      let stats =
        (Option.map ~f:ReportGroupTrendStats.of_xml)
          (Xml.child xml_arg0 "stats") in
      make ?rawData ?stats ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rawData =
        field_map json__ "rawData" ReportGroupTrendRawDataList.of_json in
      let stats = field_map json__ "stats" ReportGroupTrendStats.of_json in
      make ?rawData ?stats ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Analyzes and accumulates test report values for the specified test reports."]
module GetReportGroupTrendInput =
  struct
    type nonrec t =
      {
      reportGroupArn: NonEmptyString.t
        [@ocaml.doc
          "The ARN of the report group that contains the reports to analyze."];
      numOfReports: PageSize.t option
        [@ocaml.doc
          "The number of reports to analyze. This operation always retrieves the most recent reports. If this parameter is omitted, the most recent 100 reports are analyzed."];
      trendField: ReportGroupTrendFieldType.t
        [@ocaml.doc
          "The test report value to accumulate. This must be one of the following values: Test reports: DURATION Accumulate the test run times for the specified reports. PASS_RATE Accumulate the percentage of tests that passed for the specified test reports. TOTAL Accumulate the total number of tests for the specified test reports. Code coverage reports: BRANCH_COVERAGE Accumulate the branch coverage percentages for the specified test reports. BRANCHES_COVERED Accumulate the branches covered values for the specified test reports. BRANCHES_MISSED Accumulate the branches missed values for the specified test reports. LINE_COVERAGE Accumulate the line coverage percentages for the specified test reports. LINES_COVERED Accumulate the lines covered values for the specified test reports. LINES_MISSED Accumulate the lines not covered values for the specified test reports."]}
    let context_ = "GetReportGroupTrendInput"
    let make ?numOfReports =
      fun ~reportGroupArn ->
        fun ~trendField ->
          fun () -> { numOfReports; reportGroupArn; trendField }
    let to_value x =
      structure_to_value
        [("reportGroupArn",
           (Some (NonEmptyString.to_value x.reportGroupArn)));
        ("numOfReports", (Option.map x.numOfReports ~f:PageSize.to_value));
        ("trendField",
          (Some (ReportGroupTrendFieldType.to_value x.trendField)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trendField =
        ReportGroupTrendFieldType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "trendField") in
      let numOfReports =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "numOfReports") in
      let reportGroupArn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportGroupArn") in
      make ~trendField ?numOfReports ~reportGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trendField =
        field_map_exn json__ "trendField" ReportGroupTrendFieldType.of_json in
      let numOfReports = field_map json__ "numOfReports" PageSize.of_json in
      let reportGroupArn =
        field_map_exn json__ "reportGroupArn" NonEmptyString.of_json in
      make ~trendField ?numOfReports ~reportGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Analyzes and accumulates test report values for the specified test reports."]
module DescribeTestCasesOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      testCases: TestCases.t option
        [@ocaml.doc "The returned list of test cases."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?testCases -> fun () -> { nextToken; testCases }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("testCases", (Option.map x.testCases ~f:TestCases.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let testCases =
        (Option.map ~f:TestCases.of_xml) (Xml.child xml_arg0 "testCases") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?testCases ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let testCases = field_map json__ "testCases" TestCases.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?testCases ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of details about test cases for a report."]
module DescribeTestCasesInput =
  struct
    type nonrec t =
      {
      reportArn: String_.t
        [@ocaml.doc
          "The ARN of the report for which test cases are returned."];
      nextToken: String_.t option
        [@ocaml.doc
          "During a previous call, the maximum number of items that can be returned is the value specified in maxResults. If there more items in the list, then a unique string called a nextToken is returned. To get the next batch of items in the list, call this operation again, adding the next token to the call. To get all of the items in the list, keep calling this operation with each subsequent next token that is returned, until no more next tokens are returned."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of paginated test cases returned per response. Use nextToken to iterate pages in the list of returned TestCase objects. The default value is 100."];
      filter: TestCaseFilter.t option
        [@ocaml.doc
          "A TestCaseFilter object used to filter the returned reports."]}
    let context_ = "DescribeTestCasesInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?filter ->
          fun ~reportArn ->
            fun () -> { nextToken; maxResults; filter; reportArn }
    let to_value x =
      structure_to_value
        [("reportArn", (Some (String_.to_value x.reportArn)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("filter", (Option.map x.filter ~f:TestCaseFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:TestCaseFilter.of_xml) (Xml.child xml_arg0 "filter") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reportArn =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "reportArn") in
      make ?filter ?maxResults ?nextToken ~reportArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filter = field_map json__ "filter" TestCaseFilter.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let reportArn = field_map_exn json__ "reportArn" String_.of_json in
      make ?filter ?maxResults ?nextToken ~reportArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of details about test cases for a report."]
module DescribeCodeCoveragesOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "If there are more items to return, this contains a token that is passed to a subsequent call to DescribeCodeCoverages to retrieve the next set of items."];
      codeCoverages: CodeCoverages.t option
        [@ocaml.doc
          "An array of CodeCoverage objects that contain the results."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?codeCoverages -> fun () -> { nextToken; codeCoverages }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("codeCoverages",
          (Option.map x.codeCoverages ~f:CodeCoverages.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeCoverages =
        (Option.map ~f:CodeCoverages.of_xml)
          (Xml.child xml_arg0 "codeCoverages") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?codeCoverages ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeCoverages =
        field_map json__ "codeCoverages" CodeCoverages.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?codeCoverages ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves one or more code coverage reports."]
module DescribeCodeCoveragesInput =
  struct
    type nonrec t =
      {
      reportArn: NonEmptyString.t
        [@ocaml.doc
          "The ARN of the report for which test cases are returned."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a previous call to DescribeCodeCoverages. This specifies the next item to return. To return the beginning of the list, exclude this parameter."];
      maxResults: PageSize.t option
        [@ocaml.doc "The maximum number of results to return."];
      sortOrder: SortOrderType.t option
        [@ocaml.doc
          "Specifies if the results are sorted in ascending or descending order."];
      sortBy: ReportCodeCoverageSortByType.t option
        [@ocaml.doc
          "Specifies how the results are sorted. Possible values are: FILE_PATH The results are sorted by file path. LINE_COVERAGE_PERCENTAGE The results are sorted by the percentage of lines that are covered."];
      minLineCoveragePercentage: Percentage.t option
        [@ocaml.doc "The minimum line coverage percentage to report."];
      maxLineCoveragePercentage: Percentage.t option
        [@ocaml.doc "The maximum line coverage percentage to report."]}
    let context_ = "DescribeCodeCoveragesInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortOrder ->
          fun ?sortBy ->
            fun ?minLineCoveragePercentage ->
              fun ?maxLineCoveragePercentage ->
                fun ~reportArn ->
                  fun () ->
                    {
                      nextToken;
                      maxResults;
                      sortOrder;
                      sortBy;
                      minLineCoveragePercentage;
                      maxLineCoveragePercentage;
                      reportArn
                    }
    let to_value x =
      structure_to_value
        [("reportArn", (Some (NonEmptyString.to_value x.reportArn)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrderType.to_value));
        ("sortBy",
          (Option.map x.sortBy ~f:ReportCodeCoverageSortByType.to_value));
        ("minLineCoveragePercentage",
          (Option.map x.minLineCoveragePercentage ~f:Percentage.to_value));
        ("maxLineCoveragePercentage",
          (Option.map x.maxLineCoveragePercentage ~f:Percentage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxLineCoveragePercentage =
        (Option.map ~f:Percentage.of_xml)
          (Xml.child xml_arg0 "maxLineCoveragePercentage") in
      let minLineCoveragePercentage =
        (Option.map ~f:Percentage.of_xml)
          (Xml.child xml_arg0 "minLineCoveragePercentage") in
      let sortBy =
        (Option.map ~f:ReportCodeCoverageSortByType.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      let sortOrder =
        (Option.map ~f:SortOrderType.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let reportArn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportArn") in
      make ?maxLineCoveragePercentage ?minLineCoveragePercentage ?sortBy
        ?sortOrder ?maxResults ?nextToken ~reportArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxLineCoveragePercentage =
        field_map json__ "maxLineCoveragePercentage" Percentage.of_json in
      let minLineCoveragePercentage =
        field_map json__ "minLineCoveragePercentage" Percentage.of_json in
      let sortBy =
        field_map json__ "sortBy" ReportCodeCoverageSortByType.of_json in
      let sortOrder = field_map json__ "sortOrder" SortOrderType.of_json in
      let maxResults = field_map json__ "maxResults" PageSize.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let reportArn = field_map_exn json__ "reportArn" NonEmptyString.of_json in
      make ?maxLineCoveragePercentage ?minLineCoveragePercentage ?sortBy
        ?sortOrder ?maxResults ?nextToken ~reportArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves one or more code coverage reports."]
module DeleteWebhookOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `OAuthProviderException of OAuthProviderException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "OAuthProviderException" ->
          `OAuthProviderException (OAuthProviderException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "OAuthProviderException" ->
          `OAuthProviderException (OAuthProviderException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `OAuthProviderException e ->
          `Assoc
            [("error", (`String "OAuthProviderException"));
            ("details", (OAuthProviderException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For an existing CodeBuild build project that has its source code stored in a GitHub or Bitbucket repository, stops CodeBuild from rebuilding the source code every time a code change is pushed to the repository."]
module DeleteWebhookInput =
  struct
    type nonrec t =
      {
      projectName: ProjectName.t
        [@ocaml.doc "The name of the CodeBuild project."]}
    let context_ = "DeleteWebhookInput"
    let make ~projectName = fun () -> { projectName }
    let to_value x =
      structure_to_value
        [("projectName", (Some (ProjectName.to_value x.projectName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectName =
        ProjectName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let projectName =
        field_map_exn json__ "projectName" ProjectName.of_json in
      make ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For an existing CodeBuild build project that has its source code stored in a GitHub or Bitbucket repository, stops CodeBuild from rebuilding the source code every time a code change is pushed to the repository."]
module DeleteSourceCredentialsOutput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the token."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun () -> { arn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "arn") in
      make ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "arn" NonEmptyString.of_json in make ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a set of GitHub, GitHub Enterprise, or Bitbucket source credentials."]
module DeleteSourceCredentialsInput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the token."]}
    let context_ = "DeleteSourceCredentialsInput"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (NonEmptyString.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map_exn json__ "arn" NonEmptyString.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a set of GitHub, GitHub Enterprise, or Bitbucket source credentials."]
module DeleteResourcePolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a resource policy that is identified by its resource ARN."]
module DeleteResourcePolicyInput =
  struct
    type nonrec t =
      {
      resourceArn: NonEmptyString.t
        [@ocaml.doc
          "The ARN of the resource that is associated with the resource policy."]}
    let context_ = "DeleteResourcePolicyInput"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (NonEmptyString.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" NonEmptyString.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a resource policy that is identified by its resource ARN."]
module DeleteReportOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a report."]
module DeleteReportInput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t [@ocaml.doc "The ARN of the report to delete."]}
    let context_ = "DeleteReportInput"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (NonEmptyString.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map_exn json__ "arn" NonEmptyString.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a report."]
module DeleteReportGroupOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a report group. Before you delete a report group, you must delete its reports."]
module DeleteReportGroupInput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t
        [@ocaml.doc "The ARN of the report group to delete."];
      deleteReports: Boolean.t option
        [@ocaml.doc
          "If true, deletes any reports that belong to a report group before deleting the report group. If false, you must delete any reports in the report group. Use ListReportsForReportGroup to get the reports in a report group. Use DeleteReport to delete the reports. If you call DeleteReportGroup for a report group that contains one or more reports, an exception is thrown."]}
    let context_ = "DeleteReportGroupInput"
    let make ?deleteReports = fun ~arn -> fun () -> { deleteReports; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (NonEmptyString.to_value x.arn)));
        ("deleteReports", (Option.map x.deleteReports ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deleteReports =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "deleteReports") in
      let arn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?deleteReports ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deleteReports = field_map json__ "deleteReports" Boolean.of_json in
      let arn = field_map_exn json__ "arn" NonEmptyString.of_json in
      make ?deleteReports ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a report group. Before you delete a report group, you must delete its reports."]
module DeleteProjectOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a build project. When you delete a project, its builds are not deleted."]
module DeleteProjectInput =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t [@ocaml.doc "The name of the build project."]}
    let context_ = "DeleteProjectInput"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("name", (Some (NonEmptyString.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map_exn json__ "name" NonEmptyString.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a build project. When you delete a project, its builds are not deleted."]
module DeleteFleetOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a compute fleet. When you delete a compute fleet, its builds are not deleted."]
module DeleteFleetInput =
  struct
    type nonrec t =
      {
      arn: NonEmptyString.t [@ocaml.doc "The ARN of the compute fleet."]}
    let context_ = "DeleteFleetInput"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (NonEmptyString.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map_exn json__ "arn" NonEmptyString.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a compute fleet. When you delete a compute fleet, its builds are not deleted."]
module DeleteBuildBatchOutput =
  struct
    type nonrec t =
      {
      statusCode: String_.t option [@ocaml.doc "The status code."];
      buildsDeleted: BuildIds.t option
        [@ocaml.doc
          "An array of strings that contain the identifiers of the builds that were deleted."];
      buildsNotDeleted: BuildsNotDeleted.t option
        [@ocaml.doc
          "An array of BuildNotDeleted objects that specify the builds that could not be deleted."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?statusCode =
      fun ?buildsDeleted ->
        fun ?buildsNotDeleted ->
          fun () -> { statusCode; buildsDeleted; buildsNotDeleted }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("statusCode", (Option.map x.statusCode ~f:String_.to_value));
        ("buildsDeleted", (Option.map x.buildsDeleted ~f:BuildIds.to_value));
        ("buildsNotDeleted",
          (Option.map x.buildsNotDeleted ~f:BuildsNotDeleted.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildsNotDeleted =
        (Option.map ~f:BuildsNotDeleted.of_xml)
          (Xml.child xml_arg0 "buildsNotDeleted") in
      let buildsDeleted =
        (Option.map ~f:BuildIds.of_xml) (Xml.child xml_arg0 "buildsDeleted") in
      let statusCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusCode") in
      make ?buildsNotDeleted ?buildsDeleted ?statusCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildsNotDeleted =
        field_map json__ "buildsNotDeleted" BuildsNotDeleted.of_json in
      let buildsDeleted = field_map json__ "buildsDeleted" BuildIds.of_json in
      let statusCode = field_map json__ "statusCode" String_.of_json in
      make ?buildsNotDeleted ?buildsDeleted ?statusCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a batch build."]
module DeleteBuildBatchInput =
  struct
    type nonrec t =
      {
      id: NonEmptyString.t
        [@ocaml.doc "The identifier of the batch build to delete."]}
    let context_ = "DeleteBuildBatchInput"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (NonEmptyString.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        NonEmptyString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" NonEmptyString.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a batch build."]
module CreateWebhookOutput =
  struct
    type nonrec t =
      {
      webhook: Webhook.t option
        [@ocaml.doc
          "Information about a webhook that connects repository events to a build project in CodeBuild."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `OAuthProviderException of OAuthProviderException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?webhook = fun () -> { webhook }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "OAuthProviderException" ->
          `OAuthProviderException (OAuthProviderException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "OAuthProviderException" ->
          `OAuthProviderException (OAuthProviderException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `OAuthProviderException e ->
          `Assoc
            [("error", (`String "OAuthProviderException"));
            ("details", (OAuthProviderException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("webhook", (Option.map x.webhook ~f:Webhook.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let webhook =
        (Option.map ~f:Webhook.of_xml) (Xml.child xml_arg0 "webhook") in
      make ?webhook ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let webhook = field_map json__ "webhook" Webhook.of_json in
      make ?webhook ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For an existing CodeBuild build project that has its source code stored in a GitHub or Bitbucket repository, enables CodeBuild to start rebuilding the source code every time a code change is pushed to the repository. If you enable webhooks for an CodeBuild project, and the project is used as a build step in CodePipeline, then two identical builds are created for each commit. One build is triggered through webhooks, and one through CodePipeline. Because billing is on a per-build basis, you are billed for both builds. Therefore, if you are using CodePipeline, we recommend that you disable webhooks in CodeBuild. In the CodeBuild console, clear the Webhook box. For more information, see step 5 in Change a Build Project's Settings."]
module CreateWebhookInput =
  struct
    type nonrec t =
      {
      projectName: ProjectName.t
        [@ocaml.doc "The name of the CodeBuild project."];
      branchFilter: String_.t option
        [@ocaml.doc
          "A regular expression used to determine which repository branches are built when a webhook is triggered. If the name of a branch matches the regular expression, then it is built. If branchFilter is empty, then all branches are built. It is recommended that you use filterGroups instead of branchFilter."];
      filterGroups: FilterGroups.t option
        [@ocaml.doc
          "An array of arrays of WebhookFilter objects used to determine which webhooks are triggered. At least one WebhookFilter in the array must specify EVENT as its type. For a build to be triggered, at least one filter group in the filterGroups array must pass. For a filter group to pass, each of its filters must pass."];
      buildType: WebhookBuildType.t option
        [@ocaml.doc
          "Specifies the type of build this webhook will trigger. RUNNER_BUILDKITE_BUILD is only available for NO_SOURCE source type projects configured for Buildkite runner builds. For more information about CodeBuild-hosted Buildkite runner builds, see Tutorial: Configure a CodeBuild-hosted Buildkite runner in the CodeBuild user guide."];
      manualCreation: WrapperBoolean.t option
        [@ocaml.doc
          "If manualCreation is true, CodeBuild doesn't create a webhook in GitHub and instead returns payloadUrl and secret values for the webhook. The payloadUrl and secret values in the output can be used to manually create a webhook within GitHub. manualCreation is only available for GitHub webhooks."];
      scopeConfiguration: ScopeConfiguration.t option
        [@ocaml.doc
          "The scope configuration for global or organization webhooks. Global or organization webhooks are only available for GitHub and Github Enterprise webhooks."];
      pullRequestBuildPolicy: PullRequestBuildPolicy.t option
        [@ocaml.doc
          "A PullRequestBuildPolicy object that defines comment-based approval requirements for triggering builds on pull requests. This policy helps control when automated builds are executed based on contributor permissions and approval workflows."]}
    let context_ = "CreateWebhookInput"
    let make ?branchFilter =
      fun ?filterGroups ->
        fun ?buildType ->
          fun ?manualCreation ->
            fun ?scopeConfiguration ->
              fun ?pullRequestBuildPolicy ->
                fun ~projectName ->
                  fun () ->
                    {
                      branchFilter;
                      filterGroups;
                      buildType;
                      manualCreation;
                      scopeConfiguration;
                      pullRequestBuildPolicy;
                      projectName
                    }
    let to_value x =
      structure_to_value
        [("projectName", (Some (ProjectName.to_value x.projectName)));
        ("branchFilter", (Option.map x.branchFilter ~f:String_.to_value));
        ("filterGroups",
          (Option.map x.filterGroups ~f:FilterGroups.to_value));
        ("buildType", (Option.map x.buildType ~f:WebhookBuildType.to_value));
        ("manualCreation",
          (Option.map x.manualCreation ~f:WrapperBoolean.to_value));
        ("scopeConfiguration",
          (Option.map x.scopeConfiguration ~f:ScopeConfiguration.to_value));
        ("pullRequestBuildPolicy",
          (Option.map x.pullRequestBuildPolicy
             ~f:PullRequestBuildPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pullRequestBuildPolicy =
        (Option.map ~f:PullRequestBuildPolicy.of_xml)
          (Xml.child xml_arg0 "pullRequestBuildPolicy") in
      let scopeConfiguration =
        (Option.map ~f:ScopeConfiguration.of_xml)
          (Xml.child xml_arg0 "scopeConfiguration") in
      let manualCreation =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "manualCreation") in
      let buildType =
        (Option.map ~f:WebhookBuildType.of_xml)
          (Xml.child xml_arg0 "buildType") in
      let filterGroups =
        (Option.map ~f:FilterGroups.of_xml)
          (Xml.child xml_arg0 "filterGroups") in
      let branchFilter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "branchFilter") in
      let projectName =
        ProjectName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectName") in
      make ?pullRequestBuildPolicy ?scopeConfiguration ?manualCreation
        ?buildType ?filterGroups ?branchFilter ~projectName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pullRequestBuildPolicy =
        field_map json__ "pullRequestBuildPolicy"
          PullRequestBuildPolicy.of_json in
      let scopeConfiguration =
        field_map json__ "scopeConfiguration" ScopeConfiguration.of_json in
      let manualCreation =
        field_map json__ "manualCreation" WrapperBoolean.of_json in
      let buildType = field_map json__ "buildType" WebhookBuildType.of_json in
      let filterGroups = field_map json__ "filterGroups" FilterGroups.of_json in
      let branchFilter = field_map json__ "branchFilter" String_.of_json in
      let projectName =
        field_map_exn json__ "projectName" ProjectName.of_json in
      make ?pullRequestBuildPolicy ?scopeConfiguration ?manualCreation
        ?buildType ?filterGroups ?branchFilter ~projectName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For an existing CodeBuild build project that has its source code stored in a GitHub or Bitbucket repository, enables CodeBuild to start rebuilding the source code every time a code change is pushed to the repository. If you enable webhooks for an CodeBuild project, and the project is used as a build step in CodePipeline, then two identical builds are created for each commit. One build is triggered through webhooks, and one through CodePipeline. Because billing is on a per-build basis, you are billed for both builds. Therefore, if you are using CodePipeline, we recommend that you disable webhooks in CodeBuild. In the CodeBuild console, clear the Webhook box. For more information, see step 5 in Change a Build Project's Settings."]
module CreateReportGroupOutput =
  struct
    type nonrec t =
      {
      reportGroup: ReportGroup.t option
        [@ocaml.doc "Information about the report group that was created."]}
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportGroup = fun () -> { reportGroup }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reportGroup", (Option.map x.reportGroup ~f:ReportGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportGroup =
        (Option.map ~f:ReportGroup.of_xml) (Xml.child xml_arg0 "reportGroup") in
      make ?reportGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportGroup = field_map json__ "reportGroup" ReportGroup.of_json in
      make ?reportGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a report group. A report group contains a collection of reports."]
module CreateReportGroupInput =
  struct
    type nonrec t =
      {
      name: ReportGroupName.t [@ocaml.doc "The name of the report group."];
      type_: ReportType.t [@ocaml.doc "The type of report group."];
      exportConfig: ReportExportConfig.t
        [@ocaml.doc
          "A ReportExportConfig object that contains information about where the report group test results are exported."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this report group. These tags are available for use by Amazon Web Services services that support CodeBuild report group tags."]}
    let context_ = "CreateReportGroupInput"
    let make ?tags =
      fun ~name ->
        fun ~type_ ->
          fun ~exportConfig -> fun () -> { tags; name; type_; exportConfig }
    let to_value x =
      structure_to_value
        [("name", (Some (ReportGroupName.to_value x.name)));
        ("type", (Some (ReportType.to_value x.type_)));
        ("exportConfig", (Some (ReportExportConfig.to_value x.exportConfig)));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let exportConfig =
        ReportExportConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "exportConfig") in
      let type_ =
        ReportType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let name =
        ReportGroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~exportConfig ~type_ ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let exportConfig =
        field_map_exn json__ "exportConfig" ReportExportConfig.of_json in
      let type_ = field_map_exn json__ "type" ReportType.of_json in
      let name = field_map_exn json__ "name" ReportGroupName.of_json in
      make ?tags ~exportConfig ~type_ ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a report group. A report group contains a collection of reports."]
module CreateProjectOutput =
  struct
    type nonrec t =
      {
      project: Project.t option
        [@ocaml.doc "Information about the build project that was created."]}
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?project = fun () -> { project }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("project", (Option.map x.project ~f:Project.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let project =
        (Option.map ~f:Project.of_xml) (Xml.child xml_arg0 "project") in
      make ?project ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let project = field_map json__ "project" Project.of_json in
      make ?project ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a build project."]
module CreateProjectInput =
  struct
    type nonrec t =
      {
      name: ProjectName.t [@ocaml.doc "The name of the build project."];
      description: ProjectDescription.t option
        [@ocaml.doc
          "A description that makes the build project easy to identify."];
      source: ProjectSource.t
        [@ocaml.doc
          "Information about the build input source code for the build project."];
      secondarySources: ProjectSources.t option
        [@ocaml.doc "An array of ProjectSource objects."];
      sourceVersion: String_.t option
        [@ocaml.doc
          "A version of the build input to be built for this project. If not specified, the latest version is used. If specified, it must be one of: For CodeCommit: the commit ID, branch, or Git tag to use. For GitHub: the commit ID, pull request ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a pull request ID is specified, it must use the format pr/pull-request-ID (for example pr/25). If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For GitLab: the commit ID, branch, or Git tag to use. For Bitbucket: the commit ID, branch name, or tag name that corresponds to the version of the source code you want to build. If a branch name is specified, the branch's HEAD commit ID is used. If not specified, the default branch's HEAD commit ID is used. For Amazon S3: the version ID of the object that represents the build input ZIP file to use. If sourceVersion is specified at the build level, then that version takes precedence over this sourceVersion (at the project level). For more information, see Source Version Sample with CodeBuild in the CodeBuild User Guide."];
      secondarySourceVersions: ProjectSecondarySourceVersions.t option
        [@ocaml.doc
          "An array of ProjectSourceVersion objects. If secondarySourceVersions is specified at the build level, then they take precedence over these secondarySourceVersions (at the project level)."];
      artifacts: ProjectArtifacts.t
        [@ocaml.doc
          "Information about the build output artifacts for the build project."];
      secondaryArtifacts: ProjectArtifactsList.t option
        [@ocaml.doc "An array of ProjectArtifacts objects."];
      cache: ProjectCache.t option
        [@ocaml.doc
          "Stores recently used information so that it can be quickly accessed at a later time."];
      environment: ProjectEnvironment.t
        [@ocaml.doc
          "Information about the build environment for the build project."];
      serviceRole: NonEmptyString.t
        [@ocaml.doc
          "The ARN of the IAM role that enables CodeBuild to interact with dependent Amazon Web Services services on behalf of the Amazon Web Services account."];
      timeoutInMinutes: BuildTimeOut.t option
        [@ocaml.doc
          "How long, in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before it times out any build that has not been marked as completed. The default is 60 minutes."];
      queuedTimeoutInMinutes: TimeOut.t option
        [@ocaml.doc
          "The number of minutes a build is allowed to be queued before it times out."];
      encryptionKey: NonEmptyString.t option
        [@ocaml.doc
          "The Key Management Service customer master key (CMK) to be used for encrypting the build output artifacts. You can use a cross-account KMS key to encrypt the build output artifacts if your service role has permission to that key. You can specify either the Amazon Resource Name (ARN) of the CMK or, if available, the CMK's alias (using the format alias/<alias-name>)."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this build project. These tags are available for use by Amazon Web Services services that support CodeBuild build project tags."];
      vpcConfig: VpcConfig.t option
        [@ocaml.doc
          "VpcConfig enables CodeBuild to access resources in an Amazon VPC. If you're using compute fleets during project creation, do not provide vpcConfig."];
      badgeEnabled: WrapperBoolean.t option
        [@ocaml.doc
          "Set this to true to generate a publicly accessible URL for your project's build badge."];
      logsConfig: LogsConfig.t option
        [@ocaml.doc
          "Information about logs for the build project. These can be logs in CloudWatch Logs, logs uploaded to a specified S3 bucket, or both."];
      fileSystemLocations: ProjectFileSystemLocations.t option
        [@ocaml.doc
          "An array of ProjectFileSystemLocation objects for a CodeBuild build project. A ProjectFileSystemLocation object specifies the identifier, location, mountOptions, mountPoint, and type of a file system created using Amazon Elastic File System."];
      buildBatchConfig: ProjectBuildBatchConfig.t option
        [@ocaml.doc
          "A ProjectBuildBatchConfig object that defines the batch build options for the project."];
      concurrentBuildLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of concurrent builds that are allowed for this project. New builds are only started if the current number of builds is less than or equal to this limit. If the current build count meets this limit, new builds are throttled and are not run."];
      autoRetryLimit: WrapperInt.t option
        [@ocaml.doc
          "The maximum number of additional automatic retries after a failed build. For example, if the auto-retry limit is set to 2, CodeBuild will call the RetryBuild API to automatically retry your build for up to 2 additional times."]}
    let context_ = "CreateProjectInput"
    let make ?description =
      fun ?secondarySources ->
        fun ?sourceVersion ->
          fun ?secondarySourceVersions ->
            fun ?secondaryArtifacts ->
              fun ?cache ->
                fun ?timeoutInMinutes ->
                  fun ?queuedTimeoutInMinutes ->
                    fun ?encryptionKey ->
                      fun ?tags ->
                        fun ?vpcConfig ->
                          fun ?badgeEnabled ->
                            fun ?logsConfig ->
                              fun ?fileSystemLocations ->
                                fun ?buildBatchConfig ->
                                  fun ?concurrentBuildLimit ->
                                    fun ?autoRetryLimit ->
                                      fun ~name ->
                                        fun ~source ->
                                          fun ~artifacts ->
                                            fun ~environment ->
                                              fun ~serviceRole ->
                                                fun () ->
                                                  {
                                                    description;
                                                    secondarySources;
                                                    sourceVersion;
                                                    secondarySourceVersions;
                                                    secondaryArtifacts;
                                                    cache;
                                                    timeoutInMinutes;
                                                    queuedTimeoutInMinutes;
                                                    encryptionKey;
                                                    tags;
                                                    vpcConfig;
                                                    badgeEnabled;
                                                    logsConfig;
                                                    fileSystemLocations;
                                                    buildBatchConfig;
                                                    concurrentBuildLimit;
                                                    autoRetryLimit;
                                                    name;
                                                    source;
                                                    artifacts;
                                                    environment;
                                                    serviceRole
                                                  }
    let to_value x =
      structure_to_value
        [("name", (Some (ProjectName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:ProjectDescription.to_value));
        ("source", (Some (ProjectSource.to_value x.source)));
        ("secondarySources",
          (Option.map x.secondarySources ~f:ProjectSources.to_value));
        ("sourceVersion", (Option.map x.sourceVersion ~f:String_.to_value));
        ("secondarySourceVersions",
          (Option.map x.secondarySourceVersions
             ~f:ProjectSecondarySourceVersions.to_value));
        ("artifacts", (Some (ProjectArtifacts.to_value x.artifacts)));
        ("secondaryArtifacts",
          (Option.map x.secondaryArtifacts ~f:ProjectArtifactsList.to_value));
        ("cache", (Option.map x.cache ~f:ProjectCache.to_value));
        ("environment", (Some (ProjectEnvironment.to_value x.environment)));
        ("serviceRole", (Some (NonEmptyString.to_value x.serviceRole)));
        ("timeoutInMinutes",
          (Option.map x.timeoutInMinutes ~f:BuildTimeOut.to_value));
        ("queuedTimeoutInMinutes",
          (Option.map x.queuedTimeoutInMinutes ~f:TimeOut.to_value));
        ("encryptionKey",
          (Option.map x.encryptionKey ~f:NonEmptyString.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("badgeEnabled",
          (Option.map x.badgeEnabled ~f:WrapperBoolean.to_value));
        ("logsConfig", (Option.map x.logsConfig ~f:LogsConfig.to_value));
        ("fileSystemLocations",
          (Option.map x.fileSystemLocations
             ~f:ProjectFileSystemLocations.to_value));
        ("buildBatchConfig",
          (Option.map x.buildBatchConfig ~f:ProjectBuildBatchConfig.to_value));
        ("concurrentBuildLimit",
          (Option.map x.concurrentBuildLimit ~f:WrapperInt.to_value));
        ("autoRetryLimit",
          (Option.map x.autoRetryLimit ~f:WrapperInt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRetryLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "autoRetryLimit") in
      let concurrentBuildLimit =
        (Option.map ~f:WrapperInt.of_xml)
          (Xml.child xml_arg0 "concurrentBuildLimit") in
      let buildBatchConfig =
        (Option.map ~f:ProjectBuildBatchConfig.of_xml)
          (Xml.child xml_arg0 "buildBatchConfig") in
      let fileSystemLocations =
        (Option.map ~f:ProjectFileSystemLocations.of_xml)
          (Xml.child xml_arg0 "fileSystemLocations") in
      let logsConfig =
        (Option.map ~f:LogsConfig.of_xml) (Xml.child xml_arg0 "logsConfig") in
      let badgeEnabled =
        (Option.map ~f:WrapperBoolean.of_xml)
          (Xml.child xml_arg0 "badgeEnabled") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let encryptionKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "encryptionKey") in
      let queuedTimeoutInMinutes =
        (Option.map ~f:TimeOut.of_xml)
          (Xml.child xml_arg0 "queuedTimeoutInMinutes") in
      let timeoutInMinutes =
        (Option.map ~f:BuildTimeOut.of_xml)
          (Xml.child xml_arg0 "timeoutInMinutes") in
      let serviceRole =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "serviceRole") in
      let environment =
        ProjectEnvironment.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "environment") in
      let cache =
        (Option.map ~f:ProjectCache.of_xml) (Xml.child xml_arg0 "cache") in
      let secondaryArtifacts =
        (Option.map ~f:ProjectArtifactsList.of_xml)
          (Xml.child xml_arg0 "secondaryArtifacts") in
      let artifacts =
        ProjectArtifacts.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "artifacts") in
      let secondarySourceVersions =
        (Option.map ~f:ProjectSecondarySourceVersions.of_xml)
          (Xml.child xml_arg0 "secondarySourceVersions") in
      let sourceVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceVersion") in
      let secondarySources =
        (Option.map ~f:ProjectSources.of_xml)
          (Xml.child xml_arg0 "secondarySources") in
      let source =
        ProjectSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let description =
        (Option.map ~f:ProjectDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        ProjectName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?autoRetryLimit ?concurrentBuildLimit ?buildBatchConfig
        ?fileSystemLocations ?logsConfig ?badgeEnabled ?vpcConfig ?tags
        ?encryptionKey ?queuedTimeoutInMinutes ?timeoutInMinutes ~serviceRole
        ~environment ?cache ?secondaryArtifacts ~artifacts
        ?secondarySourceVersions ?sourceVersion ?secondarySources ~source
        ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let autoRetryLimit =
        field_map json__ "autoRetryLimit" WrapperInt.of_json in
      let concurrentBuildLimit =
        field_map json__ "concurrentBuildLimit" WrapperInt.of_json in
      let buildBatchConfig =
        field_map json__ "buildBatchConfig" ProjectBuildBatchConfig.of_json in
      let fileSystemLocations =
        field_map json__ "fileSystemLocations"
          ProjectFileSystemLocations.of_json in
      let logsConfig = field_map json__ "logsConfig" LogsConfig.of_json in
      let badgeEnabled =
        field_map json__ "badgeEnabled" WrapperBoolean.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let encryptionKey =
        field_map json__ "encryptionKey" NonEmptyString.of_json in
      let queuedTimeoutInMinutes =
        field_map json__ "queuedTimeoutInMinutes" TimeOut.of_json in
      let timeoutInMinutes =
        field_map json__ "timeoutInMinutes" BuildTimeOut.of_json in
      let serviceRole =
        field_map_exn json__ "serviceRole" NonEmptyString.of_json in
      let environment =
        field_map_exn json__ "environment" ProjectEnvironment.of_json in
      let cache = field_map json__ "cache" ProjectCache.of_json in
      let secondaryArtifacts =
        field_map json__ "secondaryArtifacts" ProjectArtifactsList.of_json in
      let artifacts =
        field_map_exn json__ "artifacts" ProjectArtifacts.of_json in
      let secondarySourceVersions =
        field_map json__ "secondarySourceVersions"
          ProjectSecondarySourceVersions.of_json in
      let sourceVersion = field_map json__ "sourceVersion" String_.of_json in
      let secondarySources =
        field_map json__ "secondarySources" ProjectSources.of_json in
      let source = field_map_exn json__ "source" ProjectSource.of_json in
      let description =
        field_map json__ "description" ProjectDescription.of_json in
      let name = field_map_exn json__ "name" ProjectName.of_json in
      make ?autoRetryLimit ?concurrentBuildLimit ?buildBatchConfig
        ?fileSystemLocations ?logsConfig ?badgeEnabled ?vpcConfig ?tags
        ?encryptionKey ?queuedTimeoutInMinutes ?timeoutInMinutes ~serviceRole
        ~environment ?cache ?secondaryArtifacts ~artifacts
        ?secondarySourceVersions ?sourceVersion ?secondarySources ~source
        ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a build project."]
module CreateFleetOutput =
  struct
    type nonrec t =
      {
      fleet: Fleet.t option
        [@ocaml.doc "Information about the compute fleet"]}
    type nonrec error =
      [ `AccountLimitExceededException of AccountLimitExceededException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleet = fun () -> { fleet }
    let error_of_json name json =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountLimitExceededException" ->
          `AccountLimitExceededException
            (AccountLimitExceededException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountLimitExceededException e ->
          `Assoc
            [("error", (`String "AccountLimitExceededException"));
            ("details", (AccountLimitExceededException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("fleet", (Option.map x.fleet ~f:Fleet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleet = (Option.map ~f:Fleet.of_xml) (Xml.child xml_arg0 "fleet") in
      make ?fleet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleet = field_map json__ "fleet" Fleet.of_json in make ?fleet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a compute fleet."]
module CreateFleetInput =
  struct
    type nonrec t =
      {
      name: FleetName.t [@ocaml.doc "The name of the compute fleet."];
      baseCapacity: FleetCapacity.t
        [@ocaml.doc
          "The initial number of machines allocated to the \239\172\130eet, which de\239\172\129nes the number of builds that can run in parallel."];
      environmentType: EnvironmentType.t
        [@ocaml.doc
          "The environment type of the compute fleet. The environment type ARM_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), Asia Pacific (Mumbai), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), EU (Frankfurt), and South America (S\195\163o Paulo). The environment type ARM_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type LINUX_GPU_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), and Asia Pacific (Sydney). The environment type MAC_ARM is available for Medium fleets only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), Asia Pacific (Sydney), and EU (Frankfurt) The environment type MAC_ARM is available for Large fleets only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), and Asia Pacific (Sydney). The environment type WINDOWS_EC2 is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Tokyo), Asia Pacific (Singapore), Asia Pacific (Sydney), South America (S\195\163o Paulo), and Asia Pacific (Mumbai). The environment type WINDOWS_SERVER_2019_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), Asia Pacific (Sydney), Asia Pacific (Tokyo), Asia Pacific (Mumbai) and EU (Ireland). The environment type WINDOWS_SERVER_2022_CONTAINER is available only in regions US East (N. Virginia), US East (Ohio), US West (Oregon), EU (Ireland), EU (Frankfurt), Asia Pacific (Sydney), Asia Pacific (Singapore), Asia Pacific (Tokyo), South America (S\195\163o Paulo) and Asia Pacific (Mumbai). For more information, see Build environment compute types in the CodeBuild user guide."];
      computeType: ComputeType.t
        [@ocaml.doc
          "Information about the compute resources the compute fleet uses. Available values include: ATTRIBUTE_BASED_COMPUTE: Specify the amount of vCPUs, memory, disk space, and the type of machine. If you use ATTRIBUTE_BASED_COMPUTE, you must define your attributes by using computeConfiguration. CodeBuild will select the cheapest instance that satisfies your specified attributes. For more information, see Reserved capacity environment types in the CodeBuild User Guide. CUSTOM_INSTANCE_TYPE: Specify the instance type for your compute fleet. For a list of supported instance types, see Supported instance families in the CodeBuild User Guide. BUILD_GENERAL1_SMALL: Use up to 4 GiB memory and 2 vCPUs for builds. BUILD_GENERAL1_MEDIUM: Use up to 8 GiB memory and 4 vCPUs for builds. BUILD_GENERAL1_LARGE: Use up to 16 GiB memory and 8 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_XLARGE: Use up to 72 GiB memory and 36 vCPUs for builds, depending on your environment type. BUILD_GENERAL1_2XLARGE: Use up to 144 GiB memory, 72 vCPUs, and 824 GB of SSD storage for builds. This compute type supports Docker images up to 100 GB uncompressed. BUILD_LAMBDA_1GB: Use up to 1 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_2GB: Use up to 2 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_4GB: Use up to 4 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_8GB: Use up to 8 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. BUILD_LAMBDA_10GB: Use up to 10 GiB memory for builds. Only available for environment type LINUX_LAMBDA_CONTAINER and ARM_LAMBDA_CONTAINER. If you use BUILD_GENERAL1_SMALL: For environment type LINUX_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 16 GiB memory, 4 vCPUs, and 1 NVIDIA A10G Tensor Core GPU for builds. For environment type ARM_CONTAINER, you can use up to 4 GiB memory and 2 vCPUs on ARM-based processors for builds. If you use BUILD_GENERAL1_LARGE: For environment type LINUX_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs for builds. For environment type LINUX_GPU_CONTAINER, you can use up to 255 GiB memory, 32 vCPUs, and 4 NVIDIA Tesla V100 GPUs for builds. For environment type ARM_CONTAINER, you can use up to 16 GiB memory and 8 vCPUs on ARM-based processors for builds. For more information, see On-demand environment types in the CodeBuild User Guide."];
      computeConfiguration: ComputeConfiguration.t option
        [@ocaml.doc
          "The compute configuration of the compute fleet. This is only required if computeType is set to ATTRIBUTE_BASED_COMPUTE or CUSTOM_INSTANCE_TYPE."];
      scalingConfiguration: ScalingConfigurationInput.t option
        [@ocaml.doc "The scaling configuration of the compute fleet."];
      overflowBehavior: FleetOverflowBehavior.t option
        [@ocaml.doc
          "The compute fleet overflow behavior. For overflow behavior QUEUE, your overflow builds need to wait on the existing fleet instance to become available. For overflow behavior ON_DEMAND, your overflow builds run on CodeBuild on-demand. If you choose to set your overflow behavior to on-demand while creating a VPC-connected fleet, make sure that you add the required VPC permissions to your project service role. For more information, see Example policy statement to allow CodeBuild access to Amazon Web Services services required to create a VPC network interface."];
      vpcConfig: VpcConfig.t option ;
      proxyConfiguration: ProxyConfiguration.t option
        [@ocaml.doc "The proxy configuration of the compute fleet."];
      imageId: NonEmptyString.t option
        [@ocaml.doc "The Amazon Machine Image (AMI) of the compute fleet."];
      fleetServiceRole: NonEmptyString.t option
        [@ocaml.doc
          "The service role associated with the compute fleet. For more information, see Allow a user to add a permission policy for a fleet service role in the CodeBuild User Guide."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with this compute fleet. These tags are available for use by Amazon Web Services services that support CodeBuild build project tags."]}
    let context_ = "CreateFleetInput"
    let make ?computeConfiguration =
      fun ?scalingConfiguration ->
        fun ?overflowBehavior ->
          fun ?vpcConfig ->
            fun ?proxyConfiguration ->
              fun ?imageId ->
                fun ?fleetServiceRole ->
                  fun ?tags ->
                    fun ~name ->
                      fun ~baseCapacity ->
                        fun ~environmentType ->
                          fun ~computeType ->
                            fun () ->
                              {
                                computeConfiguration;
                                scalingConfiguration;
                                overflowBehavior;
                                vpcConfig;
                                proxyConfiguration;
                                imageId;
                                fleetServiceRole;
                                tags;
                                name;
                                baseCapacity;
                                environmentType;
                                computeType
                              }
    let to_value x =
      structure_to_value
        [("name", (Some (FleetName.to_value x.name)));
        ("baseCapacity", (Some (FleetCapacity.to_value x.baseCapacity)));
        ("environmentType",
          (Some (EnvironmentType.to_value x.environmentType)));
        ("computeType", (Some (ComputeType.to_value x.computeType)));
        ("computeConfiguration",
          (Option.map x.computeConfiguration ~f:ComputeConfiguration.to_value));
        ("scalingConfiguration",
          (Option.map x.scalingConfiguration
             ~f:ScalingConfigurationInput.to_value));
        ("overflowBehavior",
          (Option.map x.overflowBehavior ~f:FleetOverflowBehavior.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:VpcConfig.to_value));
        ("proxyConfiguration",
          (Option.map x.proxyConfiguration ~f:ProxyConfiguration.to_value));
        ("imageId", (Option.map x.imageId ~f:NonEmptyString.to_value));
        ("fleetServiceRole",
          (Option.map x.fleetServiceRole ~f:NonEmptyString.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let fleetServiceRole =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "fleetServiceRole") in
      let imageId =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "imageId") in
      let proxyConfiguration =
        (Option.map ~f:ProxyConfiguration.of_xml)
          (Xml.child xml_arg0 "proxyConfiguration") in
      let vpcConfig =
        (Option.map ~f:VpcConfig.of_xml) (Xml.child xml_arg0 "vpcConfig") in
      let overflowBehavior =
        (Option.map ~f:FleetOverflowBehavior.of_xml)
          (Xml.child xml_arg0 "overflowBehavior") in
      let scalingConfiguration =
        (Option.map ~f:ScalingConfigurationInput.of_xml)
          (Xml.child xml_arg0 "scalingConfiguration") in
      let computeConfiguration =
        (Option.map ~f:ComputeConfiguration.of_xml)
          (Xml.child xml_arg0 "computeConfiguration") in
      let computeType =
        ComputeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "computeType") in
      let environmentType =
        EnvironmentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "environmentType") in
      let baseCapacity =
        FleetCapacity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "baseCapacity") in
      let name =
        FleetName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ?fleetServiceRole ?imageId ?proxyConfiguration ?vpcConfig
        ?overflowBehavior ?scalingConfiguration ?computeConfiguration
        ~computeType ~environmentType ~baseCapacity ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let fleetServiceRole =
        field_map json__ "fleetServiceRole" NonEmptyString.of_json in
      let imageId = field_map json__ "imageId" NonEmptyString.of_json in
      let proxyConfiguration =
        field_map json__ "proxyConfiguration" ProxyConfiguration.of_json in
      let vpcConfig = field_map json__ "vpcConfig" VpcConfig.of_json in
      let overflowBehavior =
        field_map json__ "overflowBehavior" FleetOverflowBehavior.of_json in
      let scalingConfiguration =
        field_map json__ "scalingConfiguration"
          ScalingConfigurationInput.of_json in
      let computeConfiguration =
        field_map json__ "computeConfiguration" ComputeConfiguration.of_json in
      let computeType =
        field_map_exn json__ "computeType" ComputeType.of_json in
      let environmentType =
        field_map_exn json__ "environmentType" EnvironmentType.of_json in
      let baseCapacity =
        field_map_exn json__ "baseCapacity" FleetCapacity.of_json in
      let name = field_map_exn json__ "name" FleetName.of_json in
      make ?tags ?fleetServiceRole ?imageId ?proxyConfiguration ?vpcConfig
        ?overflowBehavior ?scalingConfiguration ?computeConfiguration
        ~computeType ~environmentType ~baseCapacity ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a compute fleet."]
module BatchGetSandboxesOutput =
  struct
    type nonrec t =
      {
      sandboxes: Sandboxes.t option
        [@ocaml.doc "Information about the requested sandboxes."];
      sandboxesNotFound: SandboxIds.t option
        [@ocaml.doc
          "The IDs of sandboxes for which information could not be found."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sandboxes =
      fun ?sandboxesNotFound -> fun () -> { sandboxes; sandboxesNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sandboxes", (Option.map x.sandboxes ~f:Sandboxes.to_value));
        ("sandboxesNotFound",
          (Option.map x.sandboxesNotFound ~f:SandboxIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sandboxesNotFound =
        (Option.map ~f:SandboxIds.of_xml)
          (Xml.child xml_arg0 "sandboxesNotFound") in
      let sandboxes =
        (Option.map ~f:Sandboxes.of_xml) (Xml.child xml_arg0 "sandboxes") in
      make ?sandboxesNotFound ?sandboxes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sandboxesNotFound =
        field_map json__ "sandboxesNotFound" SandboxIds.of_json in
      let sandboxes = field_map json__ "sandboxes" Sandboxes.of_json in
      make ?sandboxesNotFound ?sandboxes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the sandbox status."]
module BatchGetSandboxesInput =
  struct
    type nonrec t =
      {
      ids: SandboxIds.t
        [@ocaml.doc "A comma separated list of sandboxIds or sandboxArns."]}
    let context_ = "BatchGetSandboxesInput"
    let make ~ids = fun () -> { ids }
    let to_value x =
      structure_to_value [("ids", (Some (SandboxIds.to_value x.ids)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ids =
        SandboxIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ids") in
      make ~ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ids = field_map_exn json__ "ids" SandboxIds.of_json in make ~ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the sandbox status."]
module BatchGetReportsOutput =
  struct
    type nonrec t =
      {
      reports: Reports.t option
        [@ocaml.doc
          "The array of Report objects returned by BatchGetReports."];
      reportsNotFound: ReportArns.t option
        [@ocaml.doc
          "An array of ARNs passed to BatchGetReportGroups that are not associated with a Report."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reports =
      fun ?reportsNotFound -> fun () -> { reports; reportsNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reports", (Option.map x.reports ~f:Reports.to_value));
        ("reportsNotFound",
          (Option.map x.reportsNotFound ~f:ReportArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportsNotFound =
        (Option.map ~f:ReportArns.of_xml)
          (Xml.child xml_arg0 "reportsNotFound") in
      let reports =
        (Option.map ~f:Reports.of_xml) (Xml.child xml_arg0 "reports") in
      make ?reportsNotFound ?reports ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportsNotFound =
        field_map json__ "reportsNotFound" ReportArns.of_json in
      let reports = field_map json__ "reports" Reports.of_json in
      make ?reportsNotFound ?reports ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns an array of reports."]
module BatchGetReportsInput =
  struct
    type nonrec t =
      {
      reportArns: ReportArns.t
        [@ocaml.doc
          "An array of ARNs that identify the Report objects to return."]}
    let context_ = "BatchGetReportsInput"
    let make ~reportArns = fun () -> { reportArns }
    let to_value x =
      structure_to_value
        [("reportArns", (Some (ReportArns.to_value x.reportArns)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportArns =
        ReportArns.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportArns") in
      make ~reportArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportArns = field_map_exn json__ "reportArns" ReportArns.of_json in
      make ~reportArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns an array of reports."]
module BatchGetReportGroupsOutput =
  struct
    type nonrec t =
      {
      reportGroups: ReportGroups.t option
        [@ocaml.doc
          "The array of report groups returned by BatchGetReportGroups."];
      reportGroupsNotFound: ReportGroupArns.t option
        [@ocaml.doc
          "An array of ARNs passed to BatchGetReportGroups that are not associated with a ReportGroup."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportGroups =
      fun ?reportGroupsNotFound ->
        fun () -> { reportGroups; reportGroupsNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("reportGroups",
           (Option.map x.reportGroups ~f:ReportGroups.to_value));
        ("reportGroupsNotFound",
          (Option.map x.reportGroupsNotFound ~f:ReportGroupArns.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportGroupsNotFound =
        (Option.map ~f:ReportGroupArns.of_xml)
          (Xml.child xml_arg0 "reportGroupsNotFound") in
      let reportGroups =
        (Option.map ~f:ReportGroups.of_xml)
          (Xml.child xml_arg0 "reportGroups") in
      make ?reportGroupsNotFound ?reportGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportGroupsNotFound =
        field_map json__ "reportGroupsNotFound" ReportGroupArns.of_json in
      let reportGroups = field_map json__ "reportGroups" ReportGroups.of_json in
      make ?reportGroupsNotFound ?reportGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns an array of report groups."]
module BatchGetReportGroupsInput =
  struct
    type nonrec t =
      {
      reportGroupArns: ReportGroupArns.t
        [@ocaml.doc
          "An array of report group ARNs that identify the report groups to return."]}
    let context_ = "BatchGetReportGroupsInput"
    let make ~reportGroupArns = fun () -> { reportGroupArns }
    let to_value x =
      structure_to_value
        [("reportGroupArns",
           (Some (ReportGroupArns.to_value x.reportGroupArns)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportGroupArns =
        ReportGroupArns.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportGroupArns") in
      make ~reportGroupArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportGroupArns =
        field_map_exn json__ "reportGroupArns" ReportGroupArns.of_json in
      make ~reportGroupArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns an array of report groups."]
module BatchGetProjectsOutput =
  struct
    type nonrec t =
      {
      projects: Projects.t option
        [@ocaml.doc "Information about the requested build projects."];
      projectsNotFound: ProjectNames.t option
        [@ocaml.doc
          "The names of build projects for which information could not be found."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?projects =
      fun ?projectsNotFound -> fun () -> { projects; projectsNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("projects", (Option.map x.projects ~f:Projects.to_value));
        ("projectsNotFound",
          (Option.map x.projectsNotFound ~f:ProjectNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectsNotFound =
        (Option.map ~f:ProjectNames.of_xml)
          (Xml.child xml_arg0 "projectsNotFound") in
      let projects =
        (Option.map ~f:Projects.of_xml) (Xml.child xml_arg0 "projects") in
      make ?projectsNotFound ?projects ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let projectsNotFound =
        field_map json__ "projectsNotFound" ProjectNames.of_json in
      let projects = field_map json__ "projects" Projects.of_json in
      make ?projectsNotFound ?projects ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about one or more build projects."]
module BatchGetProjectsInput =
  struct
    type nonrec t =
      {
      names: ProjectNames.t
        [@ocaml.doc
          "The names or ARNs of the build projects. To get information about a project shared with your Amazon Web Services account, its ARN must be specified. You cannot specify a shared project using its name."]}
    let context_ = "BatchGetProjectsInput"
    let make ~names = fun () -> { names }
    let to_value x =
      structure_to_value [("names", (Some (ProjectNames.to_value x.names)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let names =
        ProjectNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "names") in
      make ~names ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let names = field_map_exn json__ "names" ProjectNames.of_json in
      make ~names ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about one or more build projects."]
module BatchGetFleetsOutput =
  struct
    type nonrec t =
      {
      fleets: Fleets.t option
        [@ocaml.doc "Information about the requested compute fleets."];
      fleetsNotFound: FleetNames.t option
        [@ocaml.doc
          "The names of compute fleets for which information could not be found."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleets =
      fun ?fleetsNotFound -> fun () -> { fleets; fleetsNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("fleets", (Option.map x.fleets ~f:Fleets.to_value));
        ("fleetsNotFound",
          (Option.map x.fleetsNotFound ~f:FleetNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetsNotFound =
        (Option.map ~f:FleetNames.of_xml)
          (Xml.child xml_arg0 "fleetsNotFound") in
      let fleets =
        (Option.map ~f:Fleets.of_xml) (Xml.child xml_arg0 "fleets") in
      make ?fleetsNotFound ?fleets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetsNotFound =
        field_map json__ "fleetsNotFound" FleetNames.of_json in
      let fleets = field_map json__ "fleets" Fleets.of_json in
      make ?fleetsNotFound ?fleets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about one or more compute fleets."]
module BatchGetFleetsInput =
  struct
    type nonrec t =
      {
      names: FleetNames.t
        [@ocaml.doc "The names or ARNs of the compute fleets."]}
    let context_ = "BatchGetFleetsInput"
    let make ~names = fun () -> { names }
    let to_value x =
      structure_to_value [("names", (Some (FleetNames.to_value x.names)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let names =
        FleetNames.of_xml (Xml.child_exn ~context:context_ xml_arg0 "names") in
      make ~names ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let names = field_map_exn json__ "names" FleetNames.of_json in
      make ~names ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about one or more compute fleets."]
module BatchGetCommandExecutionsOutput =
  struct
    type nonrec t =
      {
      commandExecutions: CommandExecutions.t option
        [@ocaml.doc "Information about the requested command executions."];
      commandExecutionsNotFound: CommandExecutionIds.t option
        [@ocaml.doc
          "The IDs of command executions for which information could not be found."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?commandExecutions =
      fun ?commandExecutionsNotFound ->
        fun () -> { commandExecutions; commandExecutionsNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("commandExecutions",
           (Option.map x.commandExecutions ~f:CommandExecutions.to_value));
        ("commandExecutionsNotFound",
          (Option.map x.commandExecutionsNotFound
             ~f:CommandExecutionIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commandExecutionsNotFound =
        (Option.map ~f:CommandExecutionIds.of_xml)
          (Xml.child xml_arg0 "commandExecutionsNotFound") in
      let commandExecutions =
        (Option.map ~f:CommandExecutions.of_xml)
          (Xml.child xml_arg0 "commandExecutions") in
      make ?commandExecutionsNotFound ?commandExecutions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let commandExecutionsNotFound =
        field_map json__ "commandExecutionsNotFound"
          CommandExecutionIds.of_json in
      let commandExecutions =
        field_map json__ "commandExecutions" CommandExecutions.of_json in
      make ?commandExecutionsNotFound ?commandExecutions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the command executions."]
module BatchGetCommandExecutionsInput =
  struct
    type nonrec t =
      {
      sandboxId: NonEmptyString.t [@ocaml.doc "A sandboxId or sandboxArn."];
      commandExecutionIds: CommandExecutionIds.t
        [@ocaml.doc "A comma separated list of commandExecutionIds."]}
    let context_ = "BatchGetCommandExecutionsInput"
    let make ~sandboxId =
      fun ~commandExecutionIds ->
        fun () -> { sandboxId; commandExecutionIds }
    let to_value x =
      structure_to_value
        [("sandboxId", (Some (NonEmptyString.to_value x.sandboxId)));
        ("commandExecutionIds",
          (Some (CommandExecutionIds.to_value x.commandExecutionIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commandExecutionIds =
        CommandExecutionIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "commandExecutionIds") in
      let sandboxId =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sandboxId") in
      make ~commandExecutionIds ~sandboxId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let commandExecutionIds =
        field_map_exn json__ "commandExecutionIds"
          CommandExecutionIds.of_json in
      let sandboxId = field_map_exn json__ "sandboxId" NonEmptyString.of_json in
      make ~commandExecutionIds ~sandboxId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the command executions."]
module BatchGetBuildsOutput =
  struct
    type nonrec t =
      {
      builds: Builds.t option
        [@ocaml.doc "Information about the requested builds."];
      buildsNotFound: BuildIds.t option
        [@ocaml.doc
          "The IDs of builds for which information could not be found."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?builds =
      fun ?buildsNotFound -> fun () -> { builds; buildsNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("builds", (Option.map x.builds ~f:Builds.to_value));
        ("buildsNotFound",
          (Option.map x.buildsNotFound ~f:BuildIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildsNotFound =
        (Option.map ~f:BuildIds.of_xml) (Xml.child xml_arg0 "buildsNotFound") in
      let builds =
        (Option.map ~f:Builds.of_xml) (Xml.child xml_arg0 "builds") in
      make ?buildsNotFound ?builds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildsNotFound = field_map json__ "buildsNotFound" BuildIds.of_json in
      let builds = field_map json__ "builds" Builds.of_json in
      make ?buildsNotFound ?builds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about one or more builds."]
module BatchGetBuildsInput =
  struct
    type nonrec t = {
      ids: BuildIds.t [@ocaml.doc "The IDs of the builds."]}
    let context_ = "BatchGetBuildsInput"
    let make ~ids = fun () -> { ids }
    let to_value x =
      structure_to_value [("ids", (Some (BuildIds.to_value x.ids)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ids =
        BuildIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ids") in
      make ~ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ids = field_map_exn json__ "ids" BuildIds.of_json in make ~ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about one or more builds."]
module BatchGetBuildBatchesOutput =
  struct
    type nonrec t =
      {
      buildBatches: BuildBatches.t option
        [@ocaml.doc
          "An array of BuildBatch objects that represent the retrieved batch builds."];
      buildBatchesNotFound: BuildBatchIds.t option
        [@ocaml.doc
          "An array that contains the identifiers of any batch builds that are not found."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?buildBatches =
      fun ?buildBatchesNotFound ->
        fun () -> { buildBatches; buildBatchesNotFound }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("buildBatches",
           (Option.map x.buildBatches ~f:BuildBatches.to_value));
        ("buildBatchesNotFound",
          (Option.map x.buildBatchesNotFound ~f:BuildBatchIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildBatchesNotFound =
        (Option.map ~f:BuildBatchIds.of_xml)
          (Xml.child xml_arg0 "buildBatchesNotFound") in
      let buildBatches =
        (Option.map ~f:BuildBatches.of_xml)
          (Xml.child xml_arg0 "buildBatches") in
      make ?buildBatchesNotFound ?buildBatches ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildBatchesNotFound =
        field_map json__ "buildBatchesNotFound" BuildBatchIds.of_json in
      let buildBatches = field_map json__ "buildBatches" BuildBatches.of_json in
      make ?buildBatchesNotFound ?buildBatches ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about one or more batch builds."]
module BatchGetBuildBatchesInput =
  struct
    type nonrec t =
      {
      ids: BuildBatchIds.t
        [@ocaml.doc
          "An array that contains the batch build identifiers to retrieve."]}
    let context_ = "BatchGetBuildBatchesInput"
    let make ~ids = fun () -> { ids }
    let to_value x =
      structure_to_value [("ids", (Some (BuildBatchIds.to_value x.ids)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ids =
        BuildBatchIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ids") in
      make ~ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ids = field_map_exn json__ "ids" BuildBatchIds.of_json in
      make ~ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about one or more batch builds."]
module BatchDeleteBuildsOutput =
  struct
    type nonrec t =
      {
      buildsDeleted: BuildIds.t option
        [@ocaml.doc "The IDs of the builds that were successfully deleted."];
      buildsNotDeleted: BuildsNotDeleted.t option
        [@ocaml.doc
          "Information about any builds that could not be successfully deleted."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?buildsDeleted =
      fun ?buildsNotDeleted -> fun () -> { buildsDeleted; buildsNotDeleted }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("buildsDeleted", (Option.map x.buildsDeleted ~f:BuildIds.to_value));
        ("buildsNotDeleted",
          (Option.map x.buildsNotDeleted ~f:BuildsNotDeleted.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildsNotDeleted =
        (Option.map ~f:BuildsNotDeleted.of_xml)
          (Xml.child xml_arg0 "buildsNotDeleted") in
      let buildsDeleted =
        (Option.map ~f:BuildIds.of_xml) (Xml.child xml_arg0 "buildsDeleted") in
      make ?buildsNotDeleted ?buildsDeleted ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildsNotDeleted =
        field_map json__ "buildsNotDeleted" BuildsNotDeleted.of_json in
      let buildsDeleted = field_map json__ "buildsDeleted" BuildIds.of_json in
      make ?buildsNotDeleted ?buildsDeleted ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes one or more builds."]
module BatchDeleteBuildsInput =
  struct
    type nonrec t =
      {
      ids: BuildIds.t [@ocaml.doc "The IDs of the builds to delete."]}
    let context_ = "BatchDeleteBuildsInput"
    let make ~ids = fun () -> { ids }
    let to_value x =
      structure_to_value [("ids", (Some (BuildIds.to_value x.ids)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ids =
        BuildIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ids") in
      make ~ids ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ids = field_map_exn json__ "ids" BuildIds.of_json in make ~ids ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes one or more builds."]
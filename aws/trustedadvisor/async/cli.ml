(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_update_recommendation_resource_exclusion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommendationResourceExclusions =
         flag "recommendation-resource-exclusions" (required json_arg)
           ~doc:"JSON RecommendationResourceExclusionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_recommendation_resource_exclusion
           (Values.BatchUpdateRecommendationResourceExclusionRequest.make
              ~recommendationResourceExclusions:(Values.RecommendationResourceExclusionList.of_json
                                                   recommendationResourceExclusions)
              ())
           (Some
              Values.BatchUpdateRecommendationResourceExclusionResponse.to_json)
           (Some
              Values.BatchUpdateRecommendationResourceExclusionResponse.error_to_json)])
let get_organization_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationRecommendationIdentifier =
         flag "organization-recommendation-identifier" (required string)
           ~doc:"STRING OrganizationRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_organization_recommendation
           (Values.GetOrganizationRecommendationRequest.make
              ~organizationRecommendationIdentifier ())
           (Some Values.GetOrganizationRecommendationResponse.to_json)
           (Some Values.GetOrganizationRecommendationResponse.error_to_json)])
let get_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and language =
         flag "language" (optional json_arg)
           ~doc:"JSON RecommendationLanguage"
       and recommendationIdentifier =
         flag "recommendation-identifier" (required string)
           ~doc:"STRING AccountRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendation
           (Values.GetRecommendationRequest.make
              ?language:(Option.map ~f:Values.RecommendationLanguage.of_json
                           language) ~recommendationIdentifier ())
           (Some Values.GetRecommendationResponse.to_json)
           (Some Values.GetRecommendationResponse.error_to_json)])
let list_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListChecksRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListChecksRequestMaxResultsInteger"
       and pillar =
         flag "pillar" (optional json_arg) ~doc:"JSON RecommendationPillar"
       and awsService =
         flag "aws-service" (optional string)
           ~doc:"STRING RecommendationAwsService"
       and source =
         flag "source" (optional json_arg) ~doc:"JSON RecommendationSource"
       and language =
         flag "language" (optional json_arg)
           ~doc:"JSON RecommendationLanguage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_checks
           (Values.ListChecksRequest.make ?nextToken ?maxResults
              ?pillar:(Option.map ~f:Values.RecommendationPillar.of_json
                         pillar) ?awsService
              ?source:(Option.map ~f:Values.RecommendationSource.of_json
                         source)
              ?language:(Option.map ~f:Values.RecommendationLanguage.of_json
                           language) ())
           (Some Values.ListChecksResponse.to_json)
           (Some Values.ListChecksResponse.error_to_json)])
let list_organization_recommendation_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListOrganizationRecommendationAccountsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOrganizationRecommendationAccountsRequestMaxResultsInteger"
       and affectedAccountId =
         flag "affected-account-id" (optional string) ~doc:"STRING AccountId"
       and organizationRecommendationIdentifier =
         flag "organization-recommendation-identifier" (required string)
           ~doc:"STRING OrganizationRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_recommendation_accounts
           (Values.ListOrganizationRecommendationAccountsRequest.make
              ?nextToken ?maxResults ?affectedAccountId
              ~organizationRecommendationIdentifier ())
           (Some
              Values.ListOrganizationRecommendationAccountsResponse.to_json)
           (Some
              Values.ListOrganizationRecommendationAccountsResponse.error_to_json)])
let list_organization_recommendation_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListOrganizationRecommendationResourcesRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOrganizationRecommendationResourcesRequestMaxResultsInteger"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ResourceStatus"
       and exclusionStatus =
         flag "exclusion-status" (optional json_arg)
           ~doc:"JSON ExclusionStatus"
       and regionCode =
         flag "region-code" (optional string) ~doc:"STRING String"
       and affectedAccountId =
         flag "affected-account-id" (optional string) ~doc:"STRING AccountId"
       and organizationRecommendationIdentifier =
         flag "organization-recommendation-identifier" (required string)
           ~doc:"STRING OrganizationRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_recommendation_resources
           (Values.ListOrganizationRecommendationResourcesRequest.make
              ?nextToken ?maxResults
              ?status:(Option.map ~f:Values.ResourceStatus.of_json status)
              ?exclusionStatus:(Option.map ~f:Values.ExclusionStatus.of_json
                                  exclusionStatus) ?regionCode
              ?affectedAccountId ~organizationRecommendationIdentifier ())
           (Some
              Values.ListOrganizationRecommendationResourcesResponse.to_json)
           (Some
              Values.ListOrganizationRecommendationResourcesResponse.error_to_json)])
let list_organization_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListOrganizationRecommendationsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOrganizationRecommendationsRequestMaxResultsInteger"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON RecommendationType"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RecommendationStatus"
       and pillar =
         flag "pillar" (optional json_arg) ~doc:"JSON RecommendationPillar"
       and awsService =
         flag "aws-service" (optional string)
           ~doc:"STRING RecommendationAwsService"
       and source =
         flag "source" (optional json_arg) ~doc:"JSON RecommendationSource"
       and checkIdentifier =
         flag "check-identifier" (optional string)
           ~doc:"STRING CheckIdentifier"
       and afterLastUpdatedAt =
         flag "after-last-updated-at" (optional json_arg)
           ~doc:"JSON Timestamp"
       and beforeLastUpdatedAt =
         flag "before-last-updated-at" (optional json_arg)
           ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_recommendations
           (Values.ListOrganizationRecommendationsRequest.make ?nextToken
              ?maxResults
              ?type_:(Option.map ~f:Values.RecommendationType.of_json type_)
              ?status:(Option.map ~f:Values.RecommendationStatus.of_json
                         status)
              ?pillar:(Option.map ~f:Values.RecommendationPillar.of_json
                         pillar) ?awsService
              ?source:(Option.map ~f:Values.RecommendationSource.of_json
                         source) ?checkIdentifier
              ?afterLastUpdatedAt:(Option.map ~f:Values.Timestamp.of_json
                                     afterLastUpdatedAt)
              ?beforeLastUpdatedAt:(Option.map ~f:Values.Timestamp.of_json
                                      beforeLastUpdatedAt) ())
           (Some Values.ListOrganizationRecommendationsResponse.to_json)
           (Some Values.ListOrganizationRecommendationsResponse.error_to_json)])
let list_recommendation_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListRecommendationResourcesRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendationResourcesRequestMaxResultsInteger"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ResourceStatus"
       and exclusionStatus =
         flag "exclusion-status" (optional json_arg)
           ~doc:"JSON ExclusionStatus"
       and regionCode =
         flag "region-code" (optional string) ~doc:"STRING String"
       and language =
         flag "language" (optional json_arg)
           ~doc:"JSON RecommendationLanguage"
       and recommendationIdentifier =
         flag "recommendation-identifier" (required string)
           ~doc:"STRING AccountRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendation_resources
           (Values.ListRecommendationResourcesRequest.make ?nextToken
              ?maxResults
              ?status:(Option.map ~f:Values.ResourceStatus.of_json status)
              ?exclusionStatus:(Option.map ~f:Values.ExclusionStatus.of_json
                                  exclusionStatus) ?regionCode
              ?language:(Option.map ~f:Values.RecommendationLanguage.of_json
                           language) ~recommendationIdentifier ())
           (Some Values.ListRecommendationResourcesResponse.to_json)
           (Some Values.ListRecommendationResourcesResponse.error_to_json)])
let list_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListRecommendationsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendationsRequestMaxResultsInteger"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON RecommendationType"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RecommendationStatus"
       and pillar =
         flag "pillar" (optional json_arg) ~doc:"JSON RecommendationPillar"
       and awsService =
         flag "aws-service" (optional string)
           ~doc:"STRING RecommendationAwsService"
       and source =
         flag "source" (optional json_arg) ~doc:"JSON RecommendationSource"
       and checkIdentifier =
         flag "check-identifier" (optional string)
           ~doc:"STRING CheckIdentifier"
       and afterLastUpdatedAt =
         flag "after-last-updated-at" (optional json_arg)
           ~doc:"JSON Timestamp"
       and beforeLastUpdatedAt =
         flag "before-last-updated-at" (optional json_arg)
           ~doc:"JSON Timestamp"
       and language =
         flag "language" (optional json_arg)
           ~doc:"JSON RecommendationLanguage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendations
           (Values.ListRecommendationsRequest.make ?nextToken ?maxResults
              ?type_:(Option.map ~f:Values.RecommendationType.of_json type_)
              ?status:(Option.map ~f:Values.RecommendationStatus.of_json
                         status)
              ?pillar:(Option.map ~f:Values.RecommendationPillar.of_json
                         pillar) ?awsService
              ?source:(Option.map ~f:Values.RecommendationSource.of_json
                         source) ?checkIdentifier
              ?afterLastUpdatedAt:(Option.map ~f:Values.Timestamp.of_json
                                     afterLastUpdatedAt)
              ?beforeLastUpdatedAt:(Option.map ~f:Values.Timestamp.of_json
                                      beforeLastUpdatedAt)
              ?language:(Option.map ~f:Values.RecommendationLanguage.of_json
                           language) ())
           (Some Values.ListRecommendationsResponse.to_json)
           (Some Values.ListRecommendationsResponse.error_to_json)])
let update_organization_recommendation_lifecycle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateReason =
         flag "update-reason" (optional string)
           ~doc:"STRING RecommendationUpdateReason"
       and updateReasonCode =
         flag "update-reason-code" (optional json_arg)
           ~doc:"JSON UpdateRecommendationLifecycleStageReasonCode"
       and lifecycleStage =
         flag "lifecycle-stage" (required json_arg)
           ~doc:"JSON UpdateRecommendationLifecycleStage"
       and organizationRecommendationIdentifier =
         flag "organization-recommendation-identifier" (required string)
           ~doc:"STRING OrganizationRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_organization_recommendation_lifecycle
           (Values.UpdateOrganizationRecommendationLifecycleRequest.make
              ?updateReason
              ?updateReasonCode:(Option.map
                                   ~f:Values.UpdateRecommendationLifecycleStageReasonCode.of_json
                                   updateReasonCode)
              ~lifecycleStage:(Values.UpdateRecommendationLifecycleStage.of_json
                                 lifecycleStage)
              ~organizationRecommendationIdentifier ()) None None])
let update_recommendation_lifecycle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateReason =
         flag "update-reason" (optional string)
           ~doc:"STRING RecommendationUpdateReason"
       and updateReasonCode =
         flag "update-reason-code" (optional json_arg)
           ~doc:"JSON UpdateRecommendationLifecycleStageReasonCode"
       and lifecycleStage =
         flag "lifecycle-stage" (required json_arg)
           ~doc:"JSON UpdateRecommendationLifecycleStage"
       and recommendationIdentifier =
         flag "recommendation-identifier" (required string)
           ~doc:"STRING AccountRecommendationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_recommendation_lifecycle
           (Values.UpdateRecommendationLifecycleRequest.make ?updateReason
              ?updateReasonCode:(Option.map
                                   ~f:Values.UpdateRecommendationLifecycleStageReasonCode.of_json
                                   updateReasonCode)
              ~lifecycleStage:(Values.UpdateRecommendationLifecycleStage.of_json
                                 lifecycleStage) ~recommendationIdentifier ())
           None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-update-recommendation-resource-exclusion",
       batch_update_recommendation_resource_exclusion);
    ("get-organization-recommendation", get_organization_recommendation);
    ("get-recommendation", get_recommendation);
    ("list-checks", list_checks);
    ("list-organization-recommendation-accounts",
      list_organization_recommendation_accounts);
    ("list-organization-recommendation-resources",
      list_organization_recommendation_resources);
    ("list-organization-recommendations", list_organization_recommendations);
    ("list-recommendation-resources", list_recommendation_resources);
    ("list-recommendations", list_recommendations);
    ("update-organization-recommendation-lifecycle",
      update_organization_recommendation_lifecycle);
    ("update-recommendation-lifecycle", update_recommendation_lifecycle)]
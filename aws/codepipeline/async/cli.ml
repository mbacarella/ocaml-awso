(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let acknowledge_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and nonce = flag "nonce" (required string) ~doc:"STRING Nonce" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.acknowledge_job
           (Values.AcknowledgeJobInput.make ~jobId ~nonce ())
           (Some Values.AcknowledgeJobOutput.to_json)
           (Some Values.AcknowledgeJobOutput.error_to_json)])
let acknowledge_third_party_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING ThirdPartyJobId"
       and nonce = flag "nonce" (required string) ~doc:"STRING Nonce"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.acknowledge_third_party_job
           (Values.AcknowledgeThirdPartyJobInput.make ~jobId ~nonce
              ~clientToken ())
           (Some Values.AcknowledgeThirdPartyJobOutput.to_json)
           (Some Values.AcknowledgeThirdPartyJobOutput.error_to_json)])
let create_custom_action_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON ActionTypeSettings"
       and configurationProperties =
         flag "configuration-properties" (optional json_arg)
           ~doc:"JSON ActionConfigurationPropertyList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and category =
         flag "category" (required json_arg) ~doc:"JSON ActionCategory"
       and provider =
         flag "provider" (required string) ~doc:"STRING ActionProvider"
       and version = flag "version" (required string) ~doc:"STRING Version"
       and inputArtifactDetails =
         flag "input-artifact-details" (required json_arg)
           ~doc:"JSON ArtifactDetails"
       and outputArtifactDetails =
         flag "output-artifact-details" (required json_arg)
           ~doc:"JSON ArtifactDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_action_type
           (Values.CreateCustomActionTypeInput.make
              ?settings:(Option.map ~f:Values.ActionTypeSettings.of_json
                           settings)
              ?configurationProperties:(Option.map
                                          ~f:Values.ActionConfigurationPropertyList.of_json
                                          configurationProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~category:(Values.ActionCategory.of_json category) ~provider
              ~version
              ~inputArtifactDetails:(Values.ArtifactDetails.of_json
                                       inputArtifactDetails)
              ~outputArtifactDetails:(Values.ArtifactDetails.of_json
                                        outputArtifactDetails) ())
           (Some Values.CreateCustomActionTypeOutput.to_json)
           (Some Values.CreateCustomActionTypeOutput.error_to_json)])
let create_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and pipeline =
         flag "pipeline" (required json_arg) ~doc:"JSON PipelineDeclaration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipeline
           (Values.CreatePipelineInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~pipeline:(Values.PipelineDeclaration.of_json pipeline) ())
           (Some Values.CreatePipelineOutput.to_json)
           (Some Values.CreatePipelineOutput.error_to_json)])
let delete_custom_action_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (required json_arg) ~doc:"JSON ActionCategory"
       and provider =
         flag "provider" (required string) ~doc:"STRING ActionProvider"
       and version = flag "version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_action_type
           (Values.DeleteCustomActionTypeInput.make
              ~category:(Values.ActionCategory.of_json category) ~provider
              ~version ()) None None])
let delete_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipeline (Values.DeletePipelineInput.make ~name ()) None
           None])
let delete_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING WebhookName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_webhook (Values.DeleteWebhookInput.make ~name ())
           (Some Values.DeleteWebhookOutput.to_json)
           (Some Values.DeleteWebhookOutput.error_to_json)])
let deregister_webhook_with_third_party =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webhookName =
         flag "webhook-name" (optional string) ~doc:"STRING WebhookName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_webhook_with_third_party
           (Values.DeregisterWebhookWithThirdPartyInput.make ?webhookName ())
           (Some Values.DeregisterWebhookWithThirdPartyOutput.to_json)
           (Some Values.DeregisterWebhookWithThirdPartyOutput.error_to_json)])
let disable_stage_transition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName"
       and transitionType =
         flag "transition-type" (required json_arg)
           ~doc:"JSON StageTransitionType"
       and reason =
         flag "reason" (required string) ~doc:"STRING DisabledReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_stage_transition
           (Values.DisableStageTransitionInput.make ~pipelineName ~stageName
              ~transitionType:(Values.StageTransitionType.of_json
                                 transitionType) ~reason ()) None None])
let enable_stage_transition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName"
       and transitionType =
         flag "transition-type" (required json_arg)
           ~doc:"JSON StageTransitionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_stage_transition
           (Values.EnableStageTransitionInput.make ~pipelineName ~stageName
              ~transitionType:(Values.StageTransitionType.of_json
                                 transitionType) ()) None None])
let get_action_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (required json_arg) ~doc:"JSON ActionCategory"
       and owner =
         flag "owner" (required string) ~doc:"STRING ActionTypeOwner"
       and provider =
         flag "provider" (required string) ~doc:"STRING ActionProvider"
       and version = flag "version" (required string) ~doc:"STRING Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_action_type
           (Values.GetActionTypeInput.make
              ~category:(Values.ActionCategory.of_json category) ~owner
              ~provider ~version ())
           (Some Values.GetActionTypeOutput.to_json)
           (Some Values.GetActionTypeOutput.error_to_json)])
let get_job_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_details (Values.GetJobDetailsInput.make ~jobId ())
           (Some Values.GetJobDetailsOutput.to_json)
           (Some Values.GetJobDetailsOutput.error_to_json)])
let get_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and version = flag "version" (optional int) ~doc:"INT PipelineVersion"
       and name = flag "name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pipeline (Values.GetPipelineInput.make ?version ~name ())
           (Some Values.GetPipelineOutput.to_json)
           (Some Values.GetPipelineOutput.error_to_json)])
let get_pipeline_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and pipelineExecutionId =
         flag "pipeline-execution-id" (required string)
           ~doc:"STRING PipelineExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pipeline_execution
           (Values.GetPipelineExecutionInput.make ~pipelineName
              ~pipelineExecutionId ())
           (Some Values.GetPipelineExecutionOutput.to_json)
           (Some Values.GetPipelineExecutionOutput.error_to_json)])
let get_pipeline_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pipeline_state (Values.GetPipelineStateInput.make ~name ())
           (Some Values.GetPipelineStateOutput.to_json)
           (Some Values.GetPipelineStateOutput.error_to_json)])
let get_third_party_job_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING ThirdPartyJobId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_third_party_job_details
           (Values.GetThirdPartyJobDetailsInput.make ~jobId ~clientToken ())
           (Some Values.GetThirdPartyJobDetailsOutput.to_json)
           (Some Values.GetThirdPartyJobDetailsOutput.error_to_json)])
let list_action_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ActionExecutionFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_action_executions
           (Values.ListActionExecutionsInput.make
              ?filter:(Option.map ~f:Values.ActionExecutionFilter.of_json
                         filter) ?maxResults ?nextToken ~pipelineName ())
           (Some Values.ListActionExecutionsOutput.to_json)
           (Some Values.ListActionExecutionsOutput.error_to_json)])
let list_action_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionOwnerFilter =
         flag "action-owner-filter" (optional json_arg)
           ~doc:"JSON ActionOwner"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and regionFilter =
         flag "region-filter" (optional string) ~doc:"STRING AWSRegionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_action_types
           (Values.ListActionTypesInput.make
              ?actionOwnerFilter:(Option.map ~f:Values.ActionOwner.of_json
                                    actionOwnerFilter) ?nextToken
              ?regionFilter ()) (Some Values.ListActionTypesOutput.to_json)
           (Some Values.ListActionTypesOutput.error_to_json)])
let list_pipeline_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipeline_executions
           (Values.ListPipelineExecutionsInput.make ?maxResults ?nextToken
              ~pipelineName ())
           (Some Values.ListPipelineExecutionsOutput.to_json)
           (Some Values.ListPipelineExecutionsOutput.error_to_json)])
let list_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPipelines" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipelines
           (Values.ListPipelinesInput.make ?nextToken ?maxResults ())
           (Some Values.ListPipelinesOutput.to_json)
           (Some Values.ListPipelinesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?nextToken ?maxResults
              ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_webhooks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_webhooks
           (Values.ListWebhooksInput.make ?nextToken ?maxResults ())
           (Some Values.ListWebhooksOutput.to_json)
           (Some Values.ListWebhooksOutput.error_to_json)])
let poll_for_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxBatchSize =
         flag "max-batch-size" (optional int) ~doc:"INT MaxBatchSize"
       and queryParam =
         flag "query-param" (optional json_arg) ~doc:"JSON QueryParamMap"
       and actionTypeId =
         flag "action-type-id" (required json_arg) ~doc:"JSON ActionTypeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.poll_for_jobs
           (Values.PollForJobsInput.make ?maxBatchSize
              ?queryParam:(Option.map ~f:Values.QueryParamMap.of_json
                             queryParam)
              ~actionTypeId:(Values.ActionTypeId.of_json actionTypeId) ())
           (Some Values.PollForJobsOutput.to_json)
           (Some Values.PollForJobsOutput.error_to_json)])
let poll_for_third_party_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxBatchSize =
         flag "max-batch-size" (optional int) ~doc:"INT MaxBatchSize"
       and actionTypeId =
         flag "action-type-id" (required json_arg) ~doc:"JSON ActionTypeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.poll_for_third_party_jobs
           (Values.PollForThirdPartyJobsInput.make ?maxBatchSize
              ~actionTypeId:(Values.ActionTypeId.of_json actionTypeId) ())
           (Some Values.PollForThirdPartyJobsOutput.to_json)
           (Some Values.PollForThirdPartyJobsOutput.error_to_json)])
let put_action_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName"
       and actionName =
         flag "action-name" (required string) ~doc:"STRING ActionName"
       and actionRevision =
         flag "action-revision" (required json_arg)
           ~doc:"JSON ActionRevision" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_action_revision
           (Values.PutActionRevisionInput.make ~pipelineName ~stageName
              ~actionName
              ~actionRevision:(Values.ActionRevision.of_json actionRevision)
              ()) (Some Values.PutActionRevisionOutput.to_json)
           (Some Values.PutActionRevisionOutput.error_to_json)])
let put_approval_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName"
       and actionName =
         flag "action-name" (required string) ~doc:"STRING ActionName"
       and result =
         flag "result" (required json_arg) ~doc:"JSON ApprovalResult"
       and token = flag "token" (required string) ~doc:"STRING ApprovalToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_approval_result
           (Values.PutApprovalResultInput.make ~pipelineName ~stageName
              ~actionName ~result:(Values.ApprovalResult.of_json result)
              ~token ()) (Some Values.PutApprovalResultOutput.to_json)
           (Some Values.PutApprovalResultOutput.error_to_json)])
let put_job_failure_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and failureDetails =
         flag "failure-details" (required json_arg)
           ~doc:"JSON FailureDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_job_failure_result
           (Values.PutJobFailureResultInput.make ~jobId
              ~failureDetails:(Values.FailureDetails.of_json failureDetails)
              ()) None None])
let put_job_success_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and currentRevision =
         flag "current-revision" (optional json_arg)
           ~doc:"JSON CurrentRevision"
       and continuationToken =
         flag "continuation-token" (optional string)
           ~doc:"STRING ContinuationToken"
       and executionDetails =
         flag "execution-details" (optional json_arg)
           ~doc:"JSON ExecutionDetails"
       and outputVariables =
         flag "output-variables" (optional json_arg)
           ~doc:"JSON OutputVariablesMap"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_job_success_result
           (Values.PutJobSuccessResultInput.make
              ?currentRevision:(Option.map ~f:Values.CurrentRevision.of_json
                                  currentRevision) ?continuationToken
              ?executionDetails:(Option.map
                                   ~f:Values.ExecutionDetails.of_json
                                   executionDetails)
              ?outputVariables:(Option.map
                                  ~f:Values.OutputVariablesMap.of_json
                                  outputVariables) ~jobId ()) None None])
let put_third_party_job_failure_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING ThirdPartyJobId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and failureDetails =
         flag "failure-details" (required json_arg)
           ~doc:"JSON FailureDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_third_party_job_failure_result
           (Values.PutThirdPartyJobFailureResultInput.make ~jobId
              ~clientToken
              ~failureDetails:(Values.FailureDetails.of_json failureDetails)
              ()) None None])
let put_third_party_job_success_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and currentRevision =
         flag "current-revision" (optional json_arg)
           ~doc:"JSON CurrentRevision"
       and continuationToken =
         flag "continuation-token" (optional string)
           ~doc:"STRING ContinuationToken"
       and executionDetails =
         flag "execution-details" (optional json_arg)
           ~doc:"JSON ExecutionDetails"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING ThirdPartyJobId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_third_party_job_success_result
           (Values.PutThirdPartyJobSuccessResultInput.make
              ?currentRevision:(Option.map ~f:Values.CurrentRevision.of_json
                                  currentRevision) ?continuationToken
              ?executionDetails:(Option.map
                                   ~f:Values.ExecutionDetails.of_json
                                   executionDetails) ~jobId ~clientToken ())
           None None])
let put_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and webhook =
         flag "webhook" (required json_arg) ~doc:"JSON WebhookDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_webhook
           (Values.PutWebhookInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~webhook:(Values.WebhookDefinition.of_json webhook) ())
           (Some Values.PutWebhookOutput.to_json)
           (Some Values.PutWebhookOutput.error_to_json)])
let register_webhook_with_third_party =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webhookName =
         flag "webhook-name" (optional string) ~doc:"STRING WebhookName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_webhook_with_third_party
           (Values.RegisterWebhookWithThirdPartyInput.make ?webhookName ())
           (Some Values.RegisterWebhookWithThirdPartyOutput.to_json)
           (Some Values.RegisterWebhookWithThirdPartyOutput.error_to_json)])
let retry_stage_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName"
       and pipelineExecutionId =
         flag "pipeline-execution-id" (required string)
           ~doc:"STRING PipelineExecutionId"
       and retryMode =
         flag "retry-mode" (required json_arg) ~doc:"JSON StageRetryMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retry_stage_execution
           (Values.RetryStageExecutionInput.make ~pipelineName ~stageName
              ~pipelineExecutionId
              ~retryMode:(Values.StageRetryMode.of_json retryMode) ())
           (Some Values.RetryStageExecutionOutput.to_json)
           (Some Values.RetryStageExecutionOutput.error_to_json)])
let start_pipeline_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and name = flag "name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_pipeline_execution
           (Values.StartPipelineExecutionInput.make ?clientRequestToken ~name
              ()) (Some Values.StartPipelineExecutionOutput.to_json)
           (Some Values.StartPipelineExecutionOutput.error_to_json)])
let stop_pipeline_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and abandon = flag "abandon" (optional bool) ~doc:"BOOL Boolean"
       and reason =
         flag "reason" (optional string)
           ~doc:"STRING StopPipelineExecutionReason"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and pipelineExecutionId =
         flag "pipeline-execution-id" (required string)
           ~doc:"STRING PipelineExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_pipeline_execution
           (Values.StopPipelineExecutionInput.make ?abandon ?reason
              ~pipelineName ~pipelineExecutionId ())
           (Some Values.StopPipelineExecutionOutput.to_json)
           (Some Values.StopPipelineExecutionOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_action_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionType =
         flag "action-type" (required json_arg)
           ~doc:"JSON ActionTypeDeclaration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_action_type
           (Values.UpdateActionTypeInput.make
              ~actionType:(Values.ActionTypeDeclaration.of_json actionType)
              ()) None None])
let update_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipeline =
         flag "pipeline" (required json_arg) ~doc:"JSON PipelineDeclaration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipeline
           (Values.UpdatePipelineInput.make
              ~pipeline:(Values.PipelineDeclaration.of_json pipeline) ())
           (Some Values.UpdatePipelineOutput.to_json)
           (Some Values.UpdatePipelineOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("acknowledge-job", acknowledge_job);
    ("acknowledge-third-party-job", acknowledge_third_party_job);
    ("create-custom-action-type", create_custom_action_type);
    ("create-pipeline", create_pipeline);
    ("delete-custom-action-type", delete_custom_action_type);
    ("delete-pipeline", delete_pipeline);
    ("delete-webhook", delete_webhook);
    ("deregister-webhook-with-third-party",
      deregister_webhook_with_third_party);
    ("disable-stage-transition", disable_stage_transition);
    ("enable-stage-transition", enable_stage_transition);
    ("get-action-type", get_action_type);
    ("get-job-details", get_job_details);
    ("get-pipeline", get_pipeline);
    ("get-pipeline-execution", get_pipeline_execution);
    ("get-pipeline-state", get_pipeline_state);
    ("get-third-party-job-details", get_third_party_job_details);
    ("list-action-executions", list_action_executions);
    ("list-action-types", list_action_types);
    ("list-pipeline-executions", list_pipeline_executions);
    ("list-pipelines", list_pipelines);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-webhooks", list_webhooks);
    ("poll-for-jobs", poll_for_jobs);
    ("poll-for-third-party-jobs", poll_for_third_party_jobs);
    ("put-action-revision", put_action_revision);
    ("put-approval-result", put_approval_result);
    ("put-job-failure-result", put_job_failure_result);
    ("put-job-success-result", put_job_success_result);
    ("put-third-party-job-failure-result",
      put_third_party_job_failure_result);
    ("put-third-party-job-success-result",
      put_third_party_job_success_result);
    ("put-webhook", put_webhook);
    ("register-webhook-with-third-party", register_webhook_with_third_party);
    ("retry-stage-execution", retry_stage_execution);
    ("start-pipeline-execution", start_pipeline_execution);
    ("stop-pipeline-execution", stop_pipeline_execution);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-action-type", update_action_type);
    ("update-pipeline", update_pipeline)]
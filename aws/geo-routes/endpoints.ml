(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CalculateIsolines: (CalculateIsolinesRequest.t,
  CalculateIsolinesResponse.t, CalculateIsolinesResponse.error) t 
  | CalculateRouteMatrix: (CalculateRouteMatrixRequest.t,
  CalculateRouteMatrixResponse.t, CalculateRouteMatrixResponse.error) t 
  | CalculateRoutes: (CalculateRoutesRequest.t, CalculateRoutesResponse.t,
  CalculateRoutesResponse.error) t 
  | OptimizeWaypoints: (OptimizeWaypointsRequest.t,
  OptimizeWaypointsResponse.t, OptimizeWaypointsResponse.error) t 
  | SnapToRoads: (SnapToRoadsRequest.t, SnapToRoadsResponse.t,
  SnapToRoadsResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CalculateIsolines -> `POST
  | CalculateRouteMatrix -> `POST
  | CalculateRoutes -> `POST
  | OptimizeWaypoints -> `POST
  | SnapToRoads -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CalculateIsolines ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/isolines")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | CalculateRouteMatrix ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/route-matrix")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | CalculateRoutes ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/routes")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | OptimizeWaypoints ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/optimize-waypoints")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | SnapToRoads ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/snap-to-roads")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CalculateIsolines ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CalculateIsolinesRequest.allow
                         ~f:(fun x ->
                               ("Allow", (IsolineAllowOptions.to_value x)));
                      Option.map req.CalculateIsolinesRequest.arrivalTime
                        ~f:(fun x ->
                              ("ArrivalTime",
                                (TimestampWithTimezoneOffset.to_value x)));
                      Option.map req.CalculateIsolinesRequest.avoid
                        ~f:(fun x ->
                              ("Avoid", (IsolineAvoidanceOptions.to_value x)));
                      Option.map req.CalculateIsolinesRequest.departNow
                        ~f:(fun x ->
                              ("DepartNow", (SensitiveBoolean.to_value x)));
                      Option.map req.CalculateIsolinesRequest.departureTime
                        ~f:(fun x ->
                              ("DepartureTime",
                                (TimestampWithTimezoneOffset.to_value x)));
                      Option.map req.CalculateIsolinesRequest.destination
                        ~f:(fun x -> ("Destination", (Position.to_value x)));
                      Option.map
                        req.CalculateIsolinesRequest.destinationOptions
                        ~f:(fun x ->
                              ("DestinationOptions",
                                (IsolineDestinationOptions.to_value x)));
                      Option.map
                        req.CalculateIsolinesRequest.isolineGeometryFormat
                        ~f:(fun x ->
                              ("IsolineGeometryFormat",
                                (GeometryFormat.to_value x)));
                      Option.map
                        req.CalculateIsolinesRequest.isolineGranularity
                        ~f:(fun x ->
                              ("IsolineGranularity",
                                (IsolineGranularityOptions.to_value x)));
                      Option.map
                        req.CalculateIsolinesRequest.optimizeIsolineFor
                        ~f:(fun x ->
                              ("OptimizeIsolineFor",
                                (IsolineOptimizationObjective.to_value x)));
                      Option.map
                        req.CalculateIsolinesRequest.optimizeRoutingFor
                        ~f:(fun x ->
                              ("OptimizeRoutingFor",
                                (RoutingObjective.to_value x)));
                      Option.map req.CalculateIsolinesRequest.origin
                        ~f:(fun x -> ("Origin", (Position.to_value x)));
                      Option.map req.CalculateIsolinesRequest.originOptions
                        ~f:(fun x ->
                              ("OriginOptions",
                                (IsolineOriginOptions.to_value x)));
                      Some
                        ("Thresholds",
                          (IsolineThresholds.to_value
                             req.CalculateIsolinesRequest.thresholds));
                      Option.map req.CalculateIsolinesRequest.traffic
                        ~f:(fun x ->
                              ("Traffic", (IsolineTrafficOptions.to_value x)));
                      Option.map req.CalculateIsolinesRequest.travelMode
                        ~f:(fun x ->
                              ("TravelMode", (IsolineTravelMode.to_value x)));
                      Option.map
                        req.CalculateIsolinesRequest.travelModeOptions
                        ~f:(fun x ->
                              ("TravelModeOptions",
                                (IsolineTravelModeOptions.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CalculateRouteMatrix ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CalculateRouteMatrixRequest.allow
                         ~f:(fun x ->
                               ("Allow",
                                 (RouteMatrixAllowOptions.to_value x)));
                      Option.map req.CalculateRouteMatrixRequest.avoid
                        ~f:(fun x ->
                              ("Avoid",
                                (RouteMatrixAvoidanceOptions.to_value x)));
                      Option.map req.CalculateRouteMatrixRequest.departNow
                        ~f:(fun x ->
                              ("DepartNow", (SensitiveBoolean.to_value x)));
                      Option.map
                        req.CalculateRouteMatrixRequest.departureTime
                        ~f:(fun x ->
                              ("DepartureTime",
                                (TimestampWithTimezoneOffset.to_value x)));
                      Some
                        ("Destinations",
                          (CalculateRouteMatrixRequestDestinationsList.to_value
                             req.CalculateRouteMatrixRequest.destinations));
                      Option.map req.CalculateRouteMatrixRequest.exclude
                        ~f:(fun x ->
                              ("Exclude",
                                (RouteMatrixExclusionOptions.to_value x)));
                      Option.map
                        req.CalculateRouteMatrixRequest.optimizeRoutingFor
                        ~f:(fun x ->
                              ("OptimizeRoutingFor",
                                (RoutingObjective.to_value x)));
                      Some
                        ("Origins",
                          (CalculateRouteMatrixRequestOriginsList.to_value
                             req.CalculateRouteMatrixRequest.origins));
                      Option.map
                        req.CalculateRouteMatrixRequest.routingBoundary
                        ~f:(fun x ->
                              ("RoutingBoundary",
                                (RouteMatrixBoundary.to_value x)));
                      Option.map req.CalculateRouteMatrixRequest.traffic
                        ~f:(fun x ->
                              ("Traffic",
                                (RouteMatrixTrafficOptions.to_value x)));
                      Option.map req.CalculateRouteMatrixRequest.travelMode
                        ~f:(fun x ->
                              ("TravelMode",
                                (RouteMatrixTravelMode.to_value x)));
                      Option.map
                        req.CalculateRouteMatrixRequest.travelModeOptions
                        ~f:(fun x ->
                              ("TravelModeOptions",
                                (RouteMatrixTravelModeOptions.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CalculateRoutes ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CalculateRoutesRequest.allow
                         ~f:(fun x ->
                               ("Allow", (RouteAllowOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.arrivalTime
                        ~f:(fun x ->
                              ("ArrivalTime",
                                (TimestampWithTimezoneOffset.to_value x)));
                      Option.map req.CalculateRoutesRequest.avoid
                        ~f:(fun x ->
                              ("Avoid", (RouteAvoidanceOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.departNow
                        ~f:(fun x ->
                              ("DepartNow", (SensitiveBoolean.to_value x)));
                      Option.map req.CalculateRoutesRequest.departureTime
                        ~f:(fun x ->
                              ("DepartureTime",
                                (TimestampWithTimezoneOffset.to_value x)));
                      Some
                        ("Destination",
                          (Position.to_value
                             req.CalculateRoutesRequest.destination));
                      Option.map
                        req.CalculateRoutesRequest.destinationOptions
                        ~f:(fun x ->
                              ("DestinationOptions",
                                (RouteDestinationOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.driver
                        ~f:(fun x ->
                              ("Driver", (RouteDriverOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.exclude
                        ~f:(fun x ->
                              ("Exclude", (RouteExclusionOptions.to_value x)));
                      Option.map
                        req.CalculateRoutesRequest.instructionsMeasurementSystem
                        ~f:(fun x ->
                              ("InstructionsMeasurementSystem",
                                (MeasurementSystem.to_value x)));
                      Option.map req.CalculateRoutesRequest.languages
                        ~f:(fun x ->
                              ("Languages",
                                (CalculateRoutesRequestLanguagesList.to_value
                                   x)));
                      Option.map
                        req.CalculateRoutesRequest.legAdditionalFeatures
                        ~f:(fun x ->
                              ("LegAdditionalFeatures",
                                (RouteLegAdditionalFeatureList.to_value x)));
                      Option.map req.CalculateRoutesRequest.legGeometryFormat
                        ~f:(fun x ->
                              ("LegGeometryFormat",
                                (GeometryFormat.to_value x)));
                      Option.map req.CalculateRoutesRequest.maxAlternatives
                        ~f:(fun x ->
                              ("MaxAlternatives",
                                (CalculateRoutesRequestMaxAlternativesInteger.to_value
                                   x)));
                      Option.map
                        req.CalculateRoutesRequest.optimizeRoutingFor
                        ~f:(fun x ->
                              ("OptimizeRoutingFor",
                                (RoutingObjective.to_value x)));
                      Some
                        ("Origin",
                          (Position.to_value
                             req.CalculateRoutesRequest.origin));
                      Option.map req.CalculateRoutesRequest.originOptions
                        ~f:(fun x ->
                              ("OriginOptions",
                                (RouteOriginOptions.to_value x)));
                      Option.map
                        req.CalculateRoutesRequest.spanAdditionalFeatures
                        ~f:(fun x ->
                              ("SpanAdditionalFeatures",
                                (RouteSpanAdditionalFeatureList.to_value x)));
                      Option.map req.CalculateRoutesRequest.tolls
                        ~f:(fun x -> ("Tolls", (RouteTollOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.traffic
                        ~f:(fun x ->
                              ("Traffic", (RouteTrafficOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.travelMode
                        ~f:(fun x ->
                              ("TravelMode", (RouteTravelMode.to_value x)));
                      Option.map req.CalculateRoutesRequest.travelModeOptions
                        ~f:(fun x ->
                              ("TravelModeOptions",
                                (RouteTravelModeOptions.to_value x)));
                      Option.map req.CalculateRoutesRequest.travelStepType
                        ~f:(fun x ->
                              ("TravelStepType",
                                (RouteTravelStepType.to_value x)));
                      Option.map req.CalculateRoutesRequest.waypoints
                        ~f:(fun x ->
                              ("Waypoints", (RouteWaypointList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | OptimizeWaypoints ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.OptimizeWaypointsRequest.avoid
                         ~f:(fun x ->
                               ("Avoid",
                                 (WaypointOptimizationAvoidanceOptions.to_value
                                    x)));
                      Option.map req.OptimizeWaypointsRequest.clustering
                        ~f:(fun x ->
                              ("Clustering",
                                (WaypointOptimizationClusteringOptions.to_value
                                   x)));
                      Option.map req.OptimizeWaypointsRequest.departureTime
                        ~f:(fun x ->
                              ("DepartureTime",
                                (TimestampWithTimezoneOffset.to_value x)));
                      Option.map req.OptimizeWaypointsRequest.destination
                        ~f:(fun x -> ("Destination", (Position.to_value x)));
                      Option.map
                        req.OptimizeWaypointsRequest.destinationOptions
                        ~f:(fun x ->
                              ("DestinationOptions",
                                (WaypointOptimizationDestinationOptions.to_value
                                   x)));
                      Option.map req.OptimizeWaypointsRequest.driver
                        ~f:(fun x ->
                              ("Driver",
                                (WaypointOptimizationDriverOptions.to_value x)));
                      Option.map req.OptimizeWaypointsRequest.exclude
                        ~f:(fun x ->
                              ("Exclude",
                                (WaypointOptimizationExclusionOptions.to_value
                                   x)));
                      Option.map
                        req.OptimizeWaypointsRequest.optimizeSequencingFor
                        ~f:(fun x ->
                              ("OptimizeSequencingFor",
                                (WaypointOptimizationSequencingObjective.to_value
                                   x)));
                      Some
                        ("Origin",
                          (Position.to_value
                             req.OptimizeWaypointsRequest.origin));
                      Option.map req.OptimizeWaypointsRequest.originOptions
                        ~f:(fun x ->
                              ("OriginOptions",
                                (WaypointOptimizationOriginOptions.to_value x)));
                      Option.map req.OptimizeWaypointsRequest.traffic
                        ~f:(fun x ->
                              ("Traffic",
                                (WaypointOptimizationTrafficOptions.to_value
                                   x)));
                      Option.map req.OptimizeWaypointsRequest.travelMode
                        ~f:(fun x ->
                              ("TravelMode",
                                (WaypointOptimizationTravelMode.to_value x)));
                      Option.map
                        req.OptimizeWaypointsRequest.travelModeOptions
                        ~f:(fun x ->
                              ("TravelModeOptions",
                                (WaypointOptimizationTravelModeOptions.to_value
                                   x)));
                      Option.map req.OptimizeWaypointsRequest.waypoints
                        ~f:(fun x ->
                              ("Waypoints",
                                (WaypointOptimizationWaypointList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SnapToRoads ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.SnapToRoadsRequest.snappedGeometryFormat
                         ~f:(fun x ->
                               ("SnappedGeometryFormat",
                                 (GeometryFormat.to_value x)));
                      Option.map req.SnapToRoadsRequest.snapRadius
                        ~f:(fun x ->
                              ("SnapRadius",
                                (SnapToRoadsRequestSnapRadiusLong.to_value x)));
                      Some
                        ("TracePoints",
                          (SnapToRoadsRequestTracePointsList.to_value
                             req.SnapToRoadsRequest.tracePoints));
                      Option.map req.SnapToRoadsRequest.travelMode
                        ~f:(fun x ->
                              ("TravelMode", (RoadSnapTravelMode.to_value x)));
                      Option.map req.SnapToRoadsRequest.travelModeOptions
                        ~f:(fun x ->
                              ("TravelModeOptions",
                                (RoadSnapTravelModeOptions.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CalculateIsolines ->
      if is_success
      then Ok (CalculateIsolinesResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CalculateIsolinesResponse.error_of_json))
  | CalculateRouteMatrix ->
      if is_success
      then Ok (CalculateRouteMatrixResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CalculateRouteMatrixResponse.error_of_json))
  | CalculateRoutes ->
      if is_success
      then Ok (CalculateRoutesResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CalculateRoutesResponse.error_of_json))
  | OptimizeWaypoints ->
      if is_success
      then Ok (OptimizeWaypointsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some OptimizeWaypointsResponse.error_of_json))
  | SnapToRoads ->
      if is_success
      then Ok (SnapToRoadsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SnapToRoadsResponse.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | SendSSHPublicKey: (SendSSHPublicKeyRequest.t, SendSSHPublicKeyResponse.t,
  SendSSHPublicKeyResponse.error) t 
  | SendSerialConsoleSSHPublicKey: (SendSerialConsoleSSHPublicKeyRequest.t,
  SendSerialConsoleSSHPublicKeyResponse.t,
  SendSerialConsoleSSHPublicKeyResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | SendSSHPublicKey -> `POST
  | SendSerialConsoleSSHPublicKey -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | SendSSHPublicKey -> (Format.kasprintf Uri.of_string) "/"
      | SendSerialConsoleSSHPublicKey -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | SendSSHPublicKey ->
      let json = SendSSHPublicKeyRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "AWSEC2InstanceConnectService.SendSSHPublicKey")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | SendSerialConsoleSSHPublicKey ->
      let json = SendSerialConsoleSSHPublicKeyRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AWSEC2InstanceConnectService.SendSerialConsoleSSHPublicKey")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    let generic_error () = Error (`Transport err) in
    match err with
    | `Too_many_redirects -> generic_error ()
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = _ } ->
        (match (error_of_json, ((code >= 400) && (code <= 599))) with
         | (Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             (match json |> (Yojson.Safe.Util.member "__type") with
              | `String error_type ->
                  Error (`AWS (error_of_json error_type json))
              | `Null -> generic_error ()
              | _ ->
                  failwith
                    (sprintf "Error '__type' did not have string type: %s"
                       body))
         | (None, _) | (_, false) -> generic_error ()) in
  match endpoint with
  | SendSSHPublicKey ->
      (match resp with
       | Error err ->
           handle_error err (Some SendSSHPublicKeyResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (SendSSHPublicKeyResponse.of_json json))
  | SendSerialConsoleSSHPublicKey ->
      (match resp with
       | Error err ->
           handle_error err
             (Some SendSerialConsoleSSHPublicKeyResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (SendSerialConsoleSSHPublicKeyResponse.of_json json))
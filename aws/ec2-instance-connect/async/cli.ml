(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let send_s_s_h_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZone =
         flag "availability-zone" (optional string)
           ~doc:"STRING AvailabilityZone"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and instanceOSUser =
         flag "instance-o-s-user" (required string)
           ~doc:"STRING InstanceOSUser"
       and sSHPublicKey =
         flag "s-s-h-public-key" (required string) ~doc:"STRING SSHPublicKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_s_s_h_public_key
           (Values.SendSSHPublicKeyRequest.make ?availabilityZone ~instanceId
              ~instanceOSUser ~sSHPublicKey ())
           (Some Values.SendSSHPublicKeyResponse.to_json)
           (Some Values.SendSSHPublicKeyResponse.error_to_json)])
let send_serial_console_s_s_h_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serialPort =
         flag "serial-port" (optional int) ~doc:"INT SerialPort"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sSHPublicKey =
         flag "s-s-h-public-key" (required string) ~doc:"STRING SSHPublicKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_serial_console_s_s_h_public_key
           (Values.SendSerialConsoleSSHPublicKeyRequest.make ?serialPort
              ~instanceId ~sSHPublicKey ())
           (Some Values.SendSerialConsoleSSHPublicKeyResponse.to_json)
           (Some Values.SendSerialConsoleSSHPublicKeyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("send-s-s-h-public-key", send_s_s_h_public_key);
    ("send-serial-console-s-s-h-public-key",
      send_serial_console_s_s_h_public_key)]
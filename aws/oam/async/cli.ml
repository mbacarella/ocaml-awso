(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkConfiguration =
         flag "link-configuration" (optional json_arg)
           ~doc:"JSON LinkConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and labelTemplate =
         flag "label-template" (required string) ~doc:"STRING LabelTemplate"
       and resourceTypes =
         flag "resource-types" (required json_arg)
           ~doc:"JSON ResourceTypesInput"
       and sinkIdentifier =
         flag "sink-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_link
           (Values.CreateLinkInput.make
              ?linkConfiguration:(Option.map
                                    ~f:Values.LinkConfiguration.of_json
                                    linkConfiguration)
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags)
              ~labelTemplate
              ~resourceTypes:(Values.ResourceTypesInput.of_json resourceTypes)
              ~sinkIdentifier ()) (Some Values.CreateLinkOutput.to_json)
           (Some Values.CreateLinkOutput.error_to_json)])
let create_sink =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and name = flag "name" (required string) ~doc:"STRING SinkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sink
           (Values.CreateSinkInput.make
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags) ~name ())
           (Some Values.CreateSinkOutput.to_json)
           (Some Values.CreateSinkOutput.error_to_json)])
let delete_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_link (Values.DeleteLinkInput.make ~identifier ())
           (Some Values.DeleteLinkOutput.to_json)
           (Some Values.DeleteLinkOutput.error_to_json)])
let delete_sink =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sink (Values.DeleteSinkInput.make ~identifier ())
           (Some Values.DeleteSinkOutput.to_json)
           (Some Values.DeleteSinkOutput.error_to_json)])
let get_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeTags =
         flag "include-tags" (optional bool) ~doc:"BOOL IncludeTags"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_link (Values.GetLinkInput.make ?includeTags ~identifier ())
           (Some Values.GetLinkOutput.to_json)
           (Some Values.GetLinkOutput.error_to_json)])
let get_sink =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeTags =
         flag "include-tags" (optional bool) ~doc:"BOOL IncludeTags"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sink (Values.GetSinkInput.make ?includeTags ~identifier ())
           (Some Values.GetSinkOutput.to_json)
           (Some Values.GetSinkOutput.error_to_json)])
let get_sink_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sinkIdentifier =
         flag "sink-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sink_policy
           (Values.GetSinkPolicyInput.make ~sinkIdentifier ())
           (Some Values.GetSinkPolicyOutput.to_json)
           (Some Values.GetSinkPolicyOutput.error_to_json)])
let list_attached_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAttachedLinksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sinkIdentifier =
         flag "sink-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attached_links
           (Values.ListAttachedLinksInput.make ?maxResults ?nextToken
              ~sinkIdentifier ())
           (Some Values.ListAttachedLinksOutput.to_json)
           (Some Values.ListAttachedLinksOutput.error_to_json)])
let list_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLinksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_links
           (Values.ListLinksInput.make ?maxResults ?nextToken ())
           (Some Values.ListLinksOutput.to_json)
           (Some Values.ListLinksOutput.error_to_json)])
let list_sinks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListSinksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sinks
           (Values.ListSinksInput.make ?maxResults ?nextToken ())
           (Some Values.ListSinksOutput.to_json)
           (Some Values.ListSinksOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_sink_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy = flag "policy" (required string) ~doc:"STRING SinkPolicy"
       and sinkIdentifier =
         flag "sink-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_sink_policy
           (Values.PutSinkPolicyInput.make ~policy ~sinkIdentifier ())
           (Some Values.PutSinkPolicyOutput.to_json)
           (Some Values.PutSinkPolicyOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMapInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMapInput.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeTags =
         flag "include-tags" (optional bool) ~doc:"BOOL IncludeTags"
       and linkConfiguration =
         flag "link-configuration" (optional json_arg)
           ~doc:"JSON LinkConfiguration"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier"
       and resourceTypes =
         flag "resource-types" (required json_arg)
           ~doc:"JSON ResourceTypesInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_link
           (Values.UpdateLinkInput.make ?includeTags
              ?linkConfiguration:(Option.map
                                    ~f:Values.LinkConfiguration.of_json
                                    linkConfiguration) ~identifier
              ~resourceTypes:(Values.ResourceTypesInput.of_json resourceTypes)
              ()) (Some Values.UpdateLinkOutput.to_json)
           (Some Values.UpdateLinkOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-link", create_link);
    ("create-sink", create_sink);
    ("delete-link", delete_link);
    ("delete-sink", delete_sink);
    ("get-link", get_link);
    ("get-sink", get_sink);
    ("get-sink-policy", get_sink_policy);
    ("list-attached-links", list_attached_links);
    ("list-links", list_links);
    ("list-sinks", list_sinks);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-sink-policy", put_sink_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-link", update_link)]
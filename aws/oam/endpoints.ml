(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateLink: (CreateLinkInput.t, CreateLinkOutput.t,
  CreateLinkOutput.error) t 
  | CreateSink: (CreateSinkInput.t, CreateSinkOutput.t,
  CreateSinkOutput.error) t 
  | DeleteLink: (DeleteLinkInput.t, DeleteLinkOutput.t,
  DeleteLinkOutput.error) t 
  | DeleteSink: (DeleteSinkInput.t, DeleteSinkOutput.t,
  DeleteSinkOutput.error) t 
  | GetLink: (GetLinkInput.t, GetLinkOutput.t, GetLinkOutput.error) t 
  | GetSink: (GetSinkInput.t, GetSinkOutput.t, GetSinkOutput.error) t 
  | GetSinkPolicy: (GetSinkPolicyInput.t, GetSinkPolicyOutput.t,
  GetSinkPolicyOutput.error) t 
  | ListAttachedLinks: (ListAttachedLinksInput.t, ListAttachedLinksOutput.t,
  ListAttachedLinksOutput.error) t 
  | ListLinks: (ListLinksInput.t, ListLinksOutput.t, ListLinksOutput.error) t
  
  | ListSinks: (ListSinksInput.t, ListSinksOutput.t, ListSinksOutput.error) t
  
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | PutSinkPolicy: (PutSinkPolicyInput.t, PutSinkPolicyOutput.t,
  PutSinkPolicyOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateLink: (UpdateLinkInput.t, UpdateLinkOutput.t,
  UpdateLinkOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateLink -> `POST
  | CreateSink -> `POST
  | DeleteLink -> `POST
  | DeleteSink -> `POST
  | GetLink -> `POST
  | GetSink -> `POST
  | GetSinkPolicy -> `POST
  | ListAttachedLinks -> `POST
  | ListLinks -> `POST
  | ListSinks -> `POST
  | ListTagsForResource -> `GET
  | PutSinkPolicy -> `POST
  | TagResource -> `PUT
  | UntagResource -> `DELETE
  | UpdateLink -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateLink -> (Format.kasprintf Uri.of_string) "/CreateLink"
      | CreateSink -> (Format.kasprintf Uri.of_string) "/CreateSink"
      | DeleteLink -> (Format.kasprintf Uri.of_string) "/DeleteLink"
      | DeleteSink -> (Format.kasprintf Uri.of_string) "/DeleteSink"
      | GetLink -> (Format.kasprintf Uri.of_string) "/GetLink"
      | GetSink -> (Format.kasprintf Uri.of_string) "/GetSink"
      | GetSinkPolicy -> (Format.kasprintf Uri.of_string) "/GetSinkPolicy"
      | ListAttachedLinks ->
          (Format.kasprintf Uri.of_string) "/ListAttachedLinks"
      | ListLinks -> (Format.kasprintf Uri.of_string) "/ListLinks"
      | ListSinks -> (Format.kasprintf Uri.of_string) "/ListSinks"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.ListTagsForResourceInput.resourceArn)
      | PutSinkPolicy -> (Format.kasprintf Uri.of_string) "/PutSinkPolicy"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (Arn.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeys.to_header x.tagKeys))])
      | UpdateLink -> (Format.kasprintf Uri.of_string) "/UpdateLink")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateLink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("LabelTemplate",
                           (LabelTemplate.to_value
                              req.CreateLinkInput.labelTemplate));
                      Option.map req.CreateLinkInput.linkConfiguration
                        ~f:(fun x ->
                              ("LinkConfiguration",
                                (LinkConfiguration.to_value x)));
                      Some
                        ("ResourceTypes",
                          (ResourceTypesInput.to_value
                             req.CreateLinkInput.resourceTypes));
                      Some
                        ("SinkIdentifier",
                          (ResourceIdentifier.to_value
                             req.CreateLinkInput.sinkIdentifier));
                      Option.map req.CreateLinkInput.tags
                        ~f:(fun x -> ("Tags", (TagMapInput.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateSink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Name",
                           (SinkName.to_value req.CreateSinkInput.name));
                      Option.map req.CreateSinkInput.tags
                        ~f:(fun x -> ("Tags", (TagMapInput.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteLink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Identifier",
                           (ResourceIdentifier.to_value
                              req.DeleteLinkInput.identifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteSink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Identifier",
                           (ResourceIdentifier.to_value
                              req.DeleteSinkInput.identifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetLink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Identifier",
                           (ResourceIdentifier.to_value
                              req.GetLinkInput.identifier));
                      Option.map req.GetLinkInput.includeTags
                        ~f:(fun x ->
                              ("IncludeTags", (IncludeTags.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Identifier",
                           (ResourceIdentifier.to_value
                              req.GetSinkInput.identifier));
                      Option.map req.GetSinkInput.includeTags
                        ~f:(fun x ->
                              ("IncludeTags", (IncludeTags.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSinkPolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("SinkIdentifier",
                           (ResourceIdentifier.to_value
                              req.GetSinkPolicyInput.sinkIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListAttachedLinks ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListAttachedLinksInput.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListAttachedLinksMaxResults.to_value x)));
                      Option.map req.ListAttachedLinksInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Some
                        ("SinkIdentifier",
                          (ResourceIdentifier.to_value
                             req.ListAttachedLinksInput.sinkIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListLinks ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListLinksInput.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListLinksMaxResults.to_value x)));
                      Option.map req.ListLinksInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSinks ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListSinksInput.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListSinksMaxResults.to_value x)));
                      Option.map req.ListSinksInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutSinkPolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Policy",
                           (SinkPolicy.to_value req.PutSinkPolicyInput.policy));
                      Some
                        ("SinkIdentifier",
                          (ResourceIdentifier.to_value
                             req.PutSinkPolicyInput.sinkIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateLink ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Identifier",
                           (ResourceIdentifier.to_value
                              req.UpdateLinkInput.identifier));
                      Option.map req.UpdateLinkInput.includeTags
                        ~f:(fun x ->
                              ("IncludeTags", (IncludeTags.to_value x)));
                      Option.map req.UpdateLinkInput.linkConfiguration
                        ~f:(fun x ->
                              ("LinkConfiguration",
                                (LinkConfiguration.to_value x)));
                      Some
                        ("ResourceTypes",
                          (ResourceTypesInput.to_value
                             req.UpdateLinkInput.resourceTypes))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateLink ->
      if is_success
      then Ok (CreateLinkOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateLinkOutput.error_of_json))
  | CreateSink ->
      if is_success
      then Ok (CreateSinkOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateSinkOutput.error_of_json))
  | DeleteLink ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteLinkOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteLinkOutput.error_of_json))
  | DeleteSink ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteSinkOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteSinkOutput.error_of_json))
  | GetLink ->
      if is_success
      then Ok (GetLinkOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetLinkOutput.error_of_json))
  | GetSink ->
      if is_success
      then Ok (GetSinkOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetSinkOutput.error_of_json))
  | GetSinkPolicy ->
      if is_success
      then Ok (GetSinkPolicyOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetSinkPolicyOutput.error_of_json))
  | ListAttachedLinks ->
      if is_success
      then Ok (ListAttachedLinksOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListAttachedLinksOutput.error_of_json))
  | ListLinks ->
      if is_success
      then Ok (ListLinksOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListLinksOutput.error_of_json))
  | ListSinks ->
      if is_success
      then Ok (ListSinksOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListSinksOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | PutSinkPolicy ->
      if is_success
      then Ok (PutSinkPolicyOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some PutSinkPolicyOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateLink ->
      if is_success
      then Ok (UpdateLinkOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateLinkOutput.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_experiment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targets =
         flag "targets" (optional json_arg)
           ~doc:"JSON CreateExperimentTemplateTargetInputMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and logConfiguration =
         flag "log-configuration" (optional json_arg)
           ~doc:"JSON CreateExperimentTemplateLogConfigurationInput"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (required string)
           ~doc:"STRING ExperimentTemplateDescription"
       and stopConditions =
         flag "stop-conditions" (required json_arg)
           ~doc:"JSON CreateExperimentTemplateStopConditionInputList"
       and actions =
         flag "actions" (required json_arg)
           ~doc:"JSON CreateExperimentTemplateActionInputMap"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_experiment_template
           (Values.CreateExperimentTemplateRequest.make
              ?targets:(Option.map
                          ~f:Values.CreateExperimentTemplateTargetInputMap.of_json
                          targets)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?logConfiguration:(Option.map
                                   ~f:Values.CreateExperimentTemplateLogConfigurationInput.of_json
                                   logConfiguration) ~clientToken
              ~description
              ~stopConditions:(Values.CreateExperimentTemplateStopConditionInputList.of_json
                                 stopConditions)
              ~actions:(Values.CreateExperimentTemplateActionInputMap.of_json
                          actions) ~roleArn ())
           (Some Values.CreateExperimentTemplateResponse.to_json)
           (Some Values.CreateExperimentTemplateResponse.error_to_json)])
let delete_experiment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING ExperimentTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_experiment_template
           (Values.DeleteExperimentTemplateRequest.make ~id ())
           (Some Values.DeleteExperimentTemplateResponse.to_json)
           (Some Values.DeleteExperimentTemplateResponse.error_to_json)])
let get_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_action (Values.GetActionRequest.make ~id ())
           (Some Values.GetActionResponse.to_json)
           (Some Values.GetActionResponse.error_to_json)])
let get_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperimentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_experiment (Values.GetExperimentRequest.make ~id ())
           (Some Values.GetExperimentResponse.to_json)
           (Some Values.GetExperimentResponse.error_to_json)])
let get_experiment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING ExperimentTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_experiment_template
           (Values.GetExperimentTemplateRequest.make ~id ())
           (Some Values.GetExperimentTemplateResponse.to_json)
           (Some Values.GetExperimentTemplateResponse.error_to_json)])
let get_target_resource_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required string)
           ~doc:"STRING TargetResourceTypeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_target_resource_type
           (Values.GetTargetResourceTypeRequest.make ~resourceType ())
           (Some Values.GetTargetResourceTypeResponse.to_json)
           (Some Values.GetTargetResourceTypeResponse.error_to_json)])
let list_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListActionsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_actions
           (Values.ListActionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListActionsResponse.to_json)
           (Some Values.ListActionsResponse.error_to_json)])
let list_experiment_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListExperimentTemplatesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_experiment_templates
           (Values.ListExperimentTemplatesRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListExperimentTemplatesResponse.to_json)
           (Some Values.ListExperimentTemplatesResponse.error_to_json)])
let list_experiments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListExperimentsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_experiments
           (Values.ListExperimentsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListExperimentsResponse.to_json)
           (Some Values.ListExperimentsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_target_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTargetResourceTypesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_target_resource_types
           (Values.ListTargetResourceTypesRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListTargetResourceTypesResponse.to_json)
           (Some Values.ListTargetResourceTypesResponse.error_to_json)])
let start_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and experimentTemplateId =
         flag "experiment-template-id" (required string)
           ~doc:"STRING ExperimentTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_experiment
           (Values.StartExperimentRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~clientToken
              ~experimentTemplateId ())
           (Some Values.StartExperimentResponse.to_json)
           (Some Values.StartExperimentResponse.error_to_json)])
let stop_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperimentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_experiment (Values.StopExperimentRequest.make ~id ())
           (Some Values.StopExperimentResponse.to_json)
           (Some Values.StopExperimentResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ~resourceArn ()) (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_experiment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ExperimentTemplateDescription"
       and stopConditions =
         flag "stop-conditions" (optional json_arg)
           ~doc:"JSON UpdateExperimentTemplateStopConditionInputList"
       and targets =
         flag "targets" (optional json_arg)
           ~doc:"JSON UpdateExperimentTemplateTargetInputMap"
       and actions =
         flag "actions" (optional json_arg)
           ~doc:"JSON UpdateExperimentTemplateActionInputMap"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and logConfiguration =
         flag "log-configuration" (optional json_arg)
           ~doc:"JSON UpdateExperimentTemplateLogConfigurationInput"
       and id =
         flag "id" (required string) ~doc:"STRING ExperimentTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_experiment_template
           (Values.UpdateExperimentTemplateRequest.make ?description
              ?stopConditions:(Option.map
                                 ~f:Values.UpdateExperimentTemplateStopConditionInputList.of_json
                                 stopConditions)
              ?targets:(Option.map
                          ~f:Values.UpdateExperimentTemplateTargetInputMap.of_json
                          targets)
              ?actions:(Option.map
                          ~f:Values.UpdateExperimentTemplateActionInputMap.of_json
                          actions) ?roleArn
              ?logConfiguration:(Option.map
                                   ~f:Values.UpdateExperimentTemplateLogConfigurationInput.of_json
                                   logConfiguration) ~id ())
           (Some Values.UpdateExperimentTemplateResponse.to_json)
           (Some Values.UpdateExperimentTemplateResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-experiment-template", create_experiment_template);
    ("delete-experiment-template", delete_experiment_template);
    ("get-action", get_action);
    ("get-experiment", get_experiment);
    ("get-experiment-template", get_experiment_template);
    ("get-target-resource-type", get_target_resource_type);
    ("list-actions", list_actions);
    ("list-experiment-templates", list_experiment_templates);
    ("list-experiments", list_experiments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-target-resource-types", list_target_resource_types);
    ("start-experiment", start_experiment);
    ("stop-experiment", stop_experiment);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-experiment-template", update_experiment_template)]
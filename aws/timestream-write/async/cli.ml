(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING StringValue2048"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and databaseName =
         flag "database-name" (required string)
           ~doc:"STRING ResourceCreateAPIName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_database
           (Values.CreateDatabaseRequest.make ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~databaseName
              ()) (Some Values.CreateDatabaseResponse.to_json)
           (Some Values.CreateDatabaseResponse.error_to_json)])
let create_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionProperties =
         flag "retention-properties" (optional json_arg)
           ~doc:"JSON RetentionProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and magneticStoreWriteProperties =
         flag "magnetic-store-write-properties" (optional json_arg)
           ~doc:"JSON MagneticStoreWriteProperties"
       and databaseName =
         flag "database-name" (required string)
           ~doc:"STRING ResourceCreateAPIName"
       and tableName =
         flag "table-name" (required string)
           ~doc:"STRING ResourceCreateAPIName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table
           (Values.CreateTableRequest.make
              ?retentionProperties:(Option.map
                                      ~f:Values.RetentionProperties.of_json
                                      retentionProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?magneticStoreWriteProperties:(Option.map
                                               ~f:Values.MagneticStoreWriteProperties.of_json
                                               magneticStoreWriteProperties)
              ~databaseName ~tableName ())
           (Some Values.CreateTableResponse.to_json)
           (Some Values.CreateTableResponse.error_to_json)])
let delete_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_database
           (Values.DeleteDatabaseRequest.make ~databaseName ()) None None])
let delete_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table
           (Values.DeleteTableRequest.make ~databaseName ~tableName ()) None
           None])
let describe_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_database
           (Values.DescribeDatabaseRequest.make ~databaseName ())
           (Some Values.DescribeDatabaseResponse.to_json)
           (Some Values.DescribeDatabaseResponse.error_to_json)])
let describe_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoints (Values.DescribeEndpointsRequest.make ())
           (Some Values.DescribeEndpointsResponse.to_json)
           (Some Values.DescribeEndpointsResponse.error_to_json)])
let describe_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_table
           (Values.DescribeTableRequest.make ~databaseName ~tableName ())
           (Some Values.DescribeTableResponse.to_json)
           (Some Values.DescribeTableResponse.error_to_json)])
let list_databases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_databases
           (Values.ListDatabasesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatabasesResponse.to_json)
           (Some Values.ListDatabasesResponse.error_to_json)])
let list_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING ResourceName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tables
           (Values.ListTablesRequest.make ?databaseName ?nextToken
              ?maxResults ()) (Some Values.ListTablesResponse.to_json)
           (Some Values.ListTablesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName"
       and kmsKeyId =
         flag "kms-key-id" (required string) ~doc:"STRING StringValue2048" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_database
           (Values.UpdateDatabaseRequest.make ~databaseName ~kmsKeyId ())
           (Some Values.UpdateDatabaseResponse.to_json)
           (Some Values.UpdateDatabaseResponse.error_to_json)])
let update_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionProperties =
         flag "retention-properties" (optional json_arg)
           ~doc:"JSON RetentionProperties"
       and magneticStoreWriteProperties =
         flag "magnetic-store-write-properties" (optional json_arg)
           ~doc:"JSON MagneticStoreWriteProperties"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table
           (Values.UpdateTableRequest.make
              ?retentionProperties:(Option.map
                                      ~f:Values.RetentionProperties.of_json
                                      retentionProperties)
              ?magneticStoreWriteProperties:(Option.map
                                               ~f:Values.MagneticStoreWriteProperties.of_json
                                               magneticStoreWriteProperties)
              ~databaseName ~tableName ())
           (Some Values.UpdateTableResponse.to_json)
           (Some Values.UpdateTableResponse.error_to_json)])
let write_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commonAttributes =
         flag "common-attributes" (optional json_arg) ~doc:"JSON Record"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING ResourceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING ResourceName"
       and records = flag "records" (required json_arg) ~doc:"JSON Records" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.write_records
           (Values.WriteRecordsRequest.make
              ?commonAttributes:(Option.map ~f:Values.Record.of_json
                                   commonAttributes) ~databaseName ~tableName
              ~records:(Values.Records.of_json records) ())
           (Some Values.WriteRecordsResponse.to_json)
           (Some Values.WriteRecordsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-database", create_database);
    ("create-table", create_table);
    ("delete-database", delete_database);
    ("delete-table", delete_table);
    ("describe-database", describe_database);
    ("describe-endpoints", describe_endpoints);
    ("describe-table", describe_table);
    ("list-databases", list_databases);
    ("list-tables", list_tables);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-database", update_database);
    ("update-table", update_table);
    ("write-records", write_records)]
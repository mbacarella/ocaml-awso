(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and kMSKeyDetails =
         flag "k-m-s-key-details" (optional json_arg)
           ~doc:"JSON KMSKeyDetails"
       and repository =
         flag "repository" (required json_arg) ~doc:"JSON Repository" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_repository
           (Values.AssociateRepositoryRequest.make ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?kMSKeyDetails:(Option.map ~f:Values.KMSKeyDetails.of_json
                                kMSKeyDetails)
              ~repository:(Values.Repository.of_json repository) ())
           (Some Values.AssociateRepositoryResponse.to_json)
           (Some Values.AssociateRepositoryResponse.error_to_json)])
let create_code_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and name = flag "name" (required string) ~doc:"STRING CodeReviewName"
       and repositoryAssociationArn =
         flag "repository-association-arn" (required string)
           ~doc:"STRING AssociationArn"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON CodeReviewType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_code_review
           (Values.CreateCodeReviewRequest.make ?clientRequestToken ~name
              ~repositoryAssociationArn
              ~type_:(Values.CodeReviewType.of_json type_) ())
           (Some Values.CreateCodeReviewResponse.to_json)
           (Some Values.CreateCodeReviewResponse.error_to_json)])
let describe_code_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeReviewArn =
         flag "code-review-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_code_review
           (Values.DescribeCodeReviewRequest.make ~codeReviewArn ())
           (Some Values.DescribeCodeReviewResponse.to_json)
           (Some Values.DescribeCodeReviewResponse.error_to_json)])
let describe_recommendation_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and codeReviewArn =
         flag "code-review-arn" (required string) ~doc:"STRING Arn"
       and recommendationId =
         flag "recommendation-id" (required string)
           ~doc:"STRING RecommendationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_recommendation_feedback
           (Values.DescribeRecommendationFeedbackRequest.make ?userId
              ~codeReviewArn ~recommendationId ())
           (Some Values.DescribeRecommendationFeedbackResponse.to_json)
           (Some Values.DescribeRecommendationFeedbackResponse.error_to_json)])
let describe_repository_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationArn =
         flag "association-arn" (required string)
           ~doc:"STRING AssociationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_repository_association
           (Values.DescribeRepositoryAssociationRequest.make ~associationArn
              ()) (Some Values.DescribeRepositoryAssociationResponse.to_json)
           (Some Values.DescribeRepositoryAssociationResponse.error_to_json)])
let disassociate_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationArn =
         flag "association-arn" (required string)
           ~doc:"STRING AssociationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_repository
           (Values.DisassociateRepositoryRequest.make ~associationArn ())
           (Some Values.DisassociateRepositoryResponse.to_json)
           (Some Values.DisassociateRepositoryResponse.error_to_json)])
let list_code_reviews =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and providerTypes =
         flag "provider-types" (optional json_arg) ~doc:"JSON ProviderTypes"
       and states = flag "states" (optional json_arg) ~doc:"JSON JobStates"
       and repositoryNames =
         flag "repository-names" (optional json_arg)
           ~doc:"JSON RepositoryNames"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCodeReviewsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON Type" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_code_reviews
           (Values.ListCodeReviewsRequest.make
              ?providerTypes:(Option.map ~f:Values.ProviderTypes.of_json
                                providerTypes)
              ?states:(Option.map ~f:Values.JobStates.of_json states)
              ?repositoryNames:(Option.map ~f:Values.RepositoryNames.of_json
                                  repositoryNames) ?maxResults ?nextToken
              ~type_:(Values.Type.of_json type_) ())
           (Some Values.ListCodeReviewsResponse.to_json)
           (Some Values.ListCodeReviewsResponse.error_to_json)])
let list_recommendation_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and userIds = flag "user-ids" (optional json_arg) ~doc:"JSON UserIds"
       and recommendationIds =
         flag "recommendation-ids" (optional json_arg)
           ~doc:"JSON RecommendationIds"
       and codeReviewArn =
         flag "code-review-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendation_feedback
           (Values.ListRecommendationFeedbackRequest.make ?nextToken
              ?maxResults
              ?userIds:(Option.map ~f:Values.UserIds.of_json userIds)
              ?recommendationIds:(Option.map
                                    ~f:Values.RecommendationIds.of_json
                                    recommendationIds) ~codeReviewArn ())
           (Some Values.ListRecommendationFeedbackResponse.to_json)
           (Some Values.ListRecommendationFeedbackResponse.error_to_json)])
let list_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendationsMaxResults"
       and codeReviewArn =
         flag "code-review-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendations
           (Values.ListRecommendationsRequest.make ?nextToken ?maxResults
              ~codeReviewArn ())
           (Some Values.ListRecommendationsResponse.to_json)
           (Some Values.ListRecommendationsResponse.error_to_json)])
let list_repository_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and providerTypes =
         flag "provider-types" (optional json_arg) ~doc:"JSON ProviderTypes"
       and states =
         flag "states" (optional json_arg)
           ~doc:"JSON RepositoryAssociationStates"
       and names = flag "names" (optional json_arg) ~doc:"JSON Names"
       and owners = flag "owners" (optional json_arg) ~doc:"JSON Owners"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repository_associations
           (Values.ListRepositoryAssociationsRequest.make
              ?providerTypes:(Option.map ~f:Values.ProviderTypes.of_json
                                providerTypes)
              ?states:(Option.map
                         ~f:Values.RepositoryAssociationStates.of_json states)
              ?names:(Option.map ~f:Values.Names.of_json names)
              ?owners:(Option.map ~f:Values.Owners.of_json owners)
              ?maxResults ?nextToken ())
           (Some Values.ListRepositoryAssociationsResponse.to_json)
           (Some Values.ListRepositoryAssociationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AssociationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_recommendation_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeReviewArn =
         flag "code-review-arn" (required string) ~doc:"STRING Arn"
       and recommendationId =
         flag "recommendation-id" (required string)
           ~doc:"STRING RecommendationId"
       and reactions =
         flag "reactions" (required json_arg) ~doc:"JSON Reactions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_recommendation_feedback
           (Values.PutRecommendationFeedbackRequest.make ~codeReviewArn
              ~recommendationId
              ~reactions:(Values.Reactions.of_json reactions) ())
           (Some Values.PutRecommendationFeedbackResponse.to_json)
           (Some Values.PutRecommendationFeedbackResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AssociationArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AssociationArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-repository", associate_repository);
    ("create-code-review", create_code_review);
    ("describe-code-review", describe_code_review);
    ("describe-recommendation-feedback", describe_recommendation_feedback);
    ("describe-repository-association", describe_repository_association);
    ("disassociate-repository", disassociate_repository);
    ("list-code-reviews", list_code_reviews);
    ("list-recommendation-feedback", list_recommendation_feedback);
    ("list-recommendations", list_recommendations);
    ("list-repository-associations", list_repository_associations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-recommendation-feedback", put_recommendation_feedback);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
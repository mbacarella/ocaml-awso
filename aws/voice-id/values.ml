(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.voice_id
let apiVersion = "2021-09-27"
let endpointPrefix = "voiceid"
let serviceFullName = "Amazon Voice ID"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "VoiceID"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module FraudDetectionAction =
  struct
    type nonrec t =
      | IGNORE 
      | FAIL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | IGNORE -> "IGNORE" | FAIL -> "FAIL" | Non_static_id s -> s
    let of_string =
      function | "IGNORE" -> IGNORE | "FAIL" -> FAIL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FraudDetectionAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FraudDetectionAction" j)
    let to_json = simple_to_json to_value
  end
module Score =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Score" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module KmsKeyId =
  struct
    type nonrec t = string
    let context_ = "KmsKeyId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyId" j
    let to_json = simple_to_json to_value
  end
module GeneratedFraudsterId =
  struct
    type nonrec t = string
    let context_ = "GeneratedFraudsterId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:25) >>=
             (fun () ->
                (check_string_max i ~max:25) >>=
                  (fun () -> check_pattern i ~pattern:"^id#[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeneratedFraudsterId" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module EnrollmentJobFraudDetectionConfig =
  struct
    type nonrec t =
      {
      fraudDetectionAction: FraudDetectionAction.t option
        [@ocaml.doc
          "The action to take when the given speaker is flagged by the fraud detection system. The default value is FAIL, which fails the speaker enrollment. Changing this value to IGNORE results in the speaker being enrolled even if they are flagged by the fraud detection system."];
      riskThreshold: Score.t option
        [@ocaml.doc
          "Threshold value for determining whether the speaker is a high risk to be fraudulent. If the detected risk score calculated by Voice ID is greater than or equal to the threshold, the speaker is considered a fraudster."]}
    let make ?fraudDetectionAction =
      fun ?riskThreshold -> fun () -> { fraudDetectionAction; riskThreshold }
    let to_value x =
      structure_to_value
        [("FraudDetectionAction",
           (Option.map x.fraudDetectionAction
              ~f:FraudDetectionAction.to_value));
        ("RiskThreshold", (Option.map x.riskThreshold ~f:Score.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let riskThreshold =
        (Option.map ~f:Score.of_xml) (Xml.child xml_arg0 "RiskThreshold") in
      let fraudDetectionAction =
        (Option.map ~f:FraudDetectionAction.of_xml)
          (Xml.child xml_arg0 "FraudDetectionAction") in
      make ?riskThreshold ?fraudDetectionAction ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let riskThreshold = field_map json "RiskThreshold" Score.of_json in
      let fraudDetectionAction =
        field_map json "FraudDetectionAction" FraudDetectionAction.of_json in
      make ?riskThreshold ?fraudDetectionAction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration defining the action to take when a speaker is flagged by the fraud detection system during a batch speaker enrollment job, and the risk threshold to use for identification."]
module ExistingEnrollmentAction =
  struct
    type nonrec t =
      | SKIP 
      | OVERWRITE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SKIP -> "SKIP"
      | OVERWRITE -> "OVERWRITE"
      | Non_static_id s -> s
    let of_string =
      function
      | "SKIP" -> SKIP
      | "OVERWRITE" -> OVERWRITE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExistingEnrollmentAction" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ExistingEnrollmentAction" j)
    let to_json = simple_to_json to_value
  end
module S3Uri =
  struct
    type nonrec t = string
    let context_ = "S3Uri"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^s3://[a-z0-9][\\.\\-a-z0-9]{1,61}[a-z0-9](/.*)?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Uri" j
    let to_json = simple_to_json to_value
  end
module DuplicateRegistrationAction =
  struct
    type nonrec t =
      | SKIP 
      | REGISTER_AS_NEW 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SKIP -> "SKIP"
      | REGISTER_AS_NEW -> "REGISTER_AS_NEW"
      | Non_static_id s -> s
    let of_string =
      function
      | "SKIP" -> SKIP
      | "REGISTER_AS_NEW" -> REGISTER_AS_NEW
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DuplicateRegistrationAction"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DuplicateRegistrationAction" j)
    let to_json = simple_to_json to_value
  end
module CustomerSpeakerId =
  struct
    type nonrec t = string
    let context_ = "CustomerSpeakerId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9][a-zA-Z0-9_-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomerSpeakerId" j
    let to_json = simple_to_json to_value
  end
module DomainId =
  struct
    type nonrec t = string
    let context_ = "DomainId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:22) >>=
             (fun () ->
                (check_string_max i ~max:22) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DomainId" j
    let to_json = simple_to_json to_value
  end
module GeneratedSpeakerId =
  struct
    type nonrec t = string
    let context_ = "GeneratedSpeakerId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:25) >>=
             (fun () ->
                (check_string_max i ~max:25) >>=
                  (fun () -> check_pattern i ~pattern:"^id#[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeneratedSpeakerId" j
    let to_json = simple_to_json to_value
  end
module SpeakerStatus =
  struct
    type nonrec t =
      | ENROLLED 
      | EXPIRED 
      | OPTED_OUT 
      | PENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENROLLED -> "ENROLLED"
      | EXPIRED -> "EXPIRED"
      | OPTED_OUT -> "OPTED_OUT"
      | PENDING -> "PENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENROLLED" -> ENROLLED
      | "EXPIRED" -> EXPIRED
      | "OPTED_OUT" -> OPTED_OUT
      | "PENDING" -> PENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SpeakerStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SpeakerStatus" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module FailureDetails =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "A description of the error that caused the batch job failure."];
      statusCode: Integer.t option
        [@ocaml.doc
          "An HTTP status code representing the nature of the error."]}
    let make ?message = fun ?statusCode -> fun () -> { message; statusCode }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("StatusCode", (Option.map x.statusCode ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusCode =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "StatusCode") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?statusCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let statusCode = field_map json "StatusCode" Integer.of_json in
      let message = field_map json "Message" String_.of_json in
      make ?statusCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains error details for a failed batch job."]
module JobId =
  struct
    type nonrec t = string
    let context_ = "JobId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:22) >>=
             (fun () ->
                (check_string_max i ~max:22) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"JobId" j
    let to_json = simple_to_json to_value
  end
module JobName =
  struct
    type nonrec t = string
    let context_ = "JobName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9][a-zA-Z0-9_-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"JobName" j
    let to_json = simple_to_json to_value
  end
module JobProgress =
  struct
    type nonrec t =
      {
      percentComplete: Score.t option
        [@ocaml.doc
          "Shows the completed percentage of enrollment or registration requests listed in the input file."]}
    let make ?percentComplete = fun () -> { percentComplete }
    let to_value x =
      structure_to_value
        [("PercentComplete",
           (Option.map x.percentComplete ~f:Score.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let percentComplete =
        (Option.map ~f:Score.of_xml) (Xml.child xml_arg0 "PercentComplete") in
      make ?percentComplete ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let percentComplete = field_map json "PercentComplete" Score.of_json in
      make ?percentComplete ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Indicates the completion progress for a batch job."]
module SpeakerEnrollmentJobStatus =
  struct
    type nonrec t =
      | SUBMITTED 
      | IN_PROGRESS 
      | COMPLETED 
      | COMPLETED_WITH_ERRORS 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUBMITTED -> "SUBMITTED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETED -> "COMPLETED"
      | COMPLETED_WITH_ERRORS -> "COMPLETED_WITH_ERRORS"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUBMITTED" -> SUBMITTED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETED" -> COMPLETED
      | "COMPLETED_WITH_ERRORS" -> COMPLETED_WITH_ERRORS
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SpeakerEnrollmentJobStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SpeakerEnrollmentJobStatus" j)
    let to_json = simple_to_json to_value
  end
module FraudsterRegistrationJobStatus =
  struct
    type nonrec t =
      | SUBMITTED 
      | IN_PROGRESS 
      | COMPLETED 
      | COMPLETED_WITH_ERRORS 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUBMITTED -> "SUBMITTED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETED -> "COMPLETED"
      | COMPLETED_WITH_ERRORS -> "COMPLETED_WITH_ERRORS"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUBMITTED" -> SUBMITTED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETED" -> COMPLETED
      | "COMPLETED_WITH_ERRORS" -> COMPLETED_WITH_ERRORS
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FraudsterRegistrationJobStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FraudsterRegistrationJobStatus" j)
    let to_json = simple_to_json to_value
  end
module Arn =
  struct
    type nonrec t = string
    let context_ = "Arn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^arn:aws(-[^:]+)?:voiceid.+:[0-9]{12}:domain/[a-zA-Z0-9]{22}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Arn" j
    let to_json = simple_to_json to_value
  end
module Description =
  struct
    type nonrec t = string
    let context_ = "Description"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-%@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Description" j
    let to_json = simple_to_json to_value
  end
module DomainName =
  struct
    type nonrec t = string
    let context_ = "DomainName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9][a-zA-Z0-9_-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DomainName" j
    let to_json = simple_to_json to_value
  end
module DomainStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | PENDING 
      | SUSPENDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | PENDING -> "PENDING"
      | SUSPENDED -> "SUSPENDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "PENDING" -> PENDING
      | "SUSPENDED" -> SUSPENDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DomainStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DomainStatus" j)
    let to_json = simple_to_json to_value
  end
module ServerSideEncryptionConfiguration =
  struct
    type nonrec t =
      {
      kmsKeyId: KmsKeyId.t
        [@ocaml.doc
          "The identifier of the KMS Key you want Voice ID to use to encrypt your data."]}
    let context_ = "ServerSideEncryptionConfiguration"
    let make ~kmsKeyId = fun () -> { kmsKeyId }
    let to_value x =
      structure_to_value
        [("KmsKeyId", (Some (KmsKeyId.to_value x.kmsKeyId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyId =
        KmsKeyId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KmsKeyId") in
      make ~kmsKeyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let kmsKeyId = field_map_exn json "KmsKeyId" KmsKeyId.of_json in
      make ~kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration containing information about the customer-managed KMS Key used for encrypting customer data."]
module FraudDetectionReason =
  struct
    type nonrec t =
      | KNOWN_FRAUDSTER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | KNOWN_FRAUDSTER -> "KNOWN_FRAUDSTER" | Non_static_id s -> s
    let of_string =
      function | "KNOWN_FRAUDSTER" -> KNOWN_FRAUDSTER | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FraudDetectionReason" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FraudDetectionReason" j)
    let to_json = simple_to_json to_value
  end
module KnownFraudsterRisk =
  struct
    type nonrec t =
      {
      generatedFraudsterId: GeneratedFraudsterId.t option
        [@ocaml.doc
          "The identifier of the fraudster that is the closest match to the speaker. If there are no fraudsters registered in a given domain, or if there are no fraudsters with a non-zero RiskScore, this value is null."];
      riskScore: Score.t
        [@ocaml.doc
          "The score indicating the likelihood the speaker is a known fraudster."]}
    let context_ = "KnownFraudsterRisk"
    let make ?generatedFraudsterId =
      fun ~riskScore -> fun () -> { generatedFraudsterId; riskScore }
    let to_value x =
      structure_to_value
        [("GeneratedFraudsterId",
           (Option.map x.generatedFraudsterId
              ~f:GeneratedFraudsterId.to_value));
        ("RiskScore", (Some (Score.to_value x.riskScore)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let riskScore =
        Score.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RiskScore") in
      let generatedFraudsterId =
        (Option.map ~f:GeneratedFraudsterId.of_xml)
          (Xml.child xml_arg0 "GeneratedFraudsterId") in
      make ~riskScore ?generatedFraudsterId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let riskScore = field_map_exn json "RiskScore" Score.of_json in
      let generatedFraudsterId =
        field_map json "GeneratedFraudsterId" GeneratedFraudsterId.of_json in
      make ~riskScore ?generatedFraudsterId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details produced as a result of performing known fraudster risk analysis on a speaker."]
module ConflictType =
  struct
    type nonrec t =
      | ANOTHER_ACTIVE_STREAM 
      | DOMAIN_NOT_ACTIVE 
      | CANNOT_CHANGE_SPEAKER_AFTER_ENROLLMENT 
      | ENROLLMENT_ALREADY_EXISTS 
      | SPEAKER_NOT_SET 
      | SPEAKER_OPTED_OUT 
      | CONCURRENT_CHANGES 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ANOTHER_ACTIVE_STREAM -> "ANOTHER_ACTIVE_STREAM"
      | DOMAIN_NOT_ACTIVE -> "DOMAIN_NOT_ACTIVE"
      | CANNOT_CHANGE_SPEAKER_AFTER_ENROLLMENT ->
          "CANNOT_CHANGE_SPEAKER_AFTER_ENROLLMENT"
      | ENROLLMENT_ALREADY_EXISTS -> "ENROLLMENT_ALREADY_EXISTS"
      | SPEAKER_NOT_SET -> "SPEAKER_NOT_SET"
      | SPEAKER_OPTED_OUT -> "SPEAKER_OPTED_OUT"
      | CONCURRENT_CHANGES -> "CONCURRENT_CHANGES"
      | Non_static_id s -> s
    let of_string =
      function
      | "ANOTHER_ACTIVE_STREAM" -> ANOTHER_ACTIVE_STREAM
      | "DOMAIN_NOT_ACTIVE" -> DOMAIN_NOT_ACTIVE
      | "CANNOT_CHANGE_SPEAKER_AFTER_ENROLLMENT" ->
          CANNOT_CHANGE_SPEAKER_AFTER_ENROLLMENT
      | "ENROLLMENT_ALREADY_EXISTS" -> ENROLLMENT_ALREADY_EXISTS
      | "SPEAKER_NOT_SET" -> SPEAKER_NOT_SET
      | "SPEAKER_OPTED_OUT" -> SPEAKER_OPTED_OUT
      | "CONCURRENT_CHANGES" -> CONCURRENT_CHANGES
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConflictType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConflictType" j)
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t =
      | BATCH_JOB 
      | COMPLIANCE_CONSENT 
      | DOMAIN 
      | FRAUDSTER 
      | SESSION 
      | SPEAKER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BATCH_JOB -> "BATCH_JOB"
      | COMPLIANCE_CONSENT -> "COMPLIANCE_CONSENT"
      | DOMAIN -> "DOMAIN"
      | FRAUDSTER -> "FRAUDSTER"
      | SESSION -> "SESSION"
      | SPEAKER -> "SPEAKER"
      | Non_static_id s -> s
    let of_string =
      function
      | "BATCH_JOB" -> BATCH_JOB
      | "COMPLIANCE_CONSENT" -> COMPLIANCE_CONSENT
      | "DOMAIN" -> DOMAIN
      | "FRAUDSTER" -> FRAUDSTER
      | "SESSION" -> SESSION
      | "SPEAKER" -> SPEAKER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceType" j)
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t
        [@ocaml.doc
          "The first part of a key:value pair that forms a tag associated with a given resource. For example, in the tag \226\128\152Department\226\128\153:\226\128\153Sales\226\128\153, the key is 'Department'."];
      value: TagValue.t
        [@ocaml.doc
          "The second part of a key:value pair that forms a tag associated with a given resource. For example, in the tag \226\128\152Department\226\128\153:\226\128\153Sales\226\128\153, the value is 'Sales'."]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A tag that can be assigned to a Voice ID resource."]
module EnrollmentConfig =
  struct
    type nonrec t =
      {
      existingEnrollmentAction: ExistingEnrollmentAction.t option
        [@ocaml.doc
          "The action to take when the specified speaker is already enrolled in the specified domain. The default value is SKIP, which skips the enrollment for the existing speaker. Setting the value to OVERWRITE replaces the existing voice prints and enrollment audio stored for that speaker with new data generated from the latest audio."];
      fraudDetectionConfig: EnrollmentJobFraudDetectionConfig.t option
        [@ocaml.doc
          "The fraud detection configuration to use for the speaker enrollment job."]}
    let make ?existingEnrollmentAction =
      fun ?fraudDetectionConfig ->
        fun () -> { existingEnrollmentAction; fraudDetectionConfig }
    let to_value x =
      structure_to_value
        [("ExistingEnrollmentAction",
           (Option.map x.existingEnrollmentAction
              ~f:ExistingEnrollmentAction.to_value));
        ("FraudDetectionConfig",
          (Option.map x.fraudDetectionConfig
             ~f:EnrollmentJobFraudDetectionConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fraudDetectionConfig =
        (Option.map ~f:EnrollmentJobFraudDetectionConfig.of_xml)
          (Xml.child xml_arg0 "FraudDetectionConfig") in
      let existingEnrollmentAction =
        (Option.map ~f:ExistingEnrollmentAction.of_xml)
          (Xml.child xml_arg0 "ExistingEnrollmentAction") in
      make ?fraudDetectionConfig ?existingEnrollmentAction ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let fraudDetectionConfig =
        field_map json "FraudDetectionConfig"
          EnrollmentJobFraudDetectionConfig.of_json in
      let existingEnrollmentAction =
        field_map json "ExistingEnrollmentAction"
          ExistingEnrollmentAction.of_json in
      make ?fraudDetectionConfig ?existingEnrollmentAction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations defining enrollment behavior for the batch job."]
module IamRoleArn =
  struct
    type nonrec t = string
    let context_ = "IamRoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(-[^:]+)?:iam::[0-9]{12}:role/.+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IamRoleArn" j
    let to_json = simple_to_json to_value
  end
module InputDataConfig =
  struct
    type nonrec t =
      {
      s3Uri: S3Uri.t
        [@ocaml.doc
          "The S3 location for the input manifest file that contains the list of individual enrollment or registration job requests."]}
    let context_ = "InputDataConfig"
    let make ~s3Uri = fun () -> { s3Uri }
    let to_value x =
      structure_to_value [("S3Uri", (Some (S3Uri.to_value x.s3Uri)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Uri =
        S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "S3Uri") in
      make ~s3Uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3Uri = field_map_exn json "S3Uri" S3Uri.of_json in make ~s3Uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration containing input file information for a batch job."]
module OutputDataConfig =
  struct
    type nonrec t =
      {
      kmsKeyId: KmsKeyId.t option
        [@ocaml.doc
          "the identifier of the KMS key you want Voice ID to use to encrypt the output file of the fraudster registration job."];
      s3Uri: S3Uri.t
        [@ocaml.doc
          "The S3 path of the folder to which Voice ID writes the job output file, which has a *.out extension. For example, if the input file name is input-file.json and the output folder path is s3://output-bucket/output-folder, the full output file path is s3://output-bucket/output-folder/job-Id/input-file.json.out."]}
    let context_ = "OutputDataConfig"
    let make ?kmsKeyId = fun ~s3Uri -> fun () -> { kmsKeyId; s3Uri }
    let to_value x =
      structure_to_value
        [("KmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("S3Uri", (Some (S3Uri.to_value x.s3Uri)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Uri =
        S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "S3Uri") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "KmsKeyId") in
      make ~s3Uri ?kmsKeyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3Uri = field_map_exn json "S3Uri" S3Uri.of_json in
      let kmsKeyId = field_map json "KmsKeyId" KmsKeyId.of_json in
      make ~s3Uri ?kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration containing output file information for a batch job."]
module RegistrationConfig =
  struct
    type nonrec t =
      {
      duplicateRegistrationAction: DuplicateRegistrationAction.t option
        [@ocaml.doc
          "The action to take when a fraudster is identified as a duplicate. The default action is SKIP, which skips registering the duplicate fraudster. Setting the value to REGISTER_AS_NEW always registers a new fraudster into the specified domain."];
      fraudsterSimilarityThreshold: Score.t option
        [@ocaml.doc
          "The minimum similarity score between the new and old fraudsters in order to consider the new fraudster a duplicate."]}
    let make ?duplicateRegistrationAction =
      fun ?fraudsterSimilarityThreshold ->
        fun () ->
          { duplicateRegistrationAction; fraudsterSimilarityThreshold }
    let to_value x =
      structure_to_value
        [("DuplicateRegistrationAction",
           (Option.map x.duplicateRegistrationAction
              ~f:DuplicateRegistrationAction.to_value));
        ("FraudsterSimilarityThreshold",
          (Option.map x.fraudsterSimilarityThreshold ~f:Score.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fraudsterSimilarityThreshold =
        (Option.map ~f:Score.of_xml)
          (Xml.child xml_arg0 "FraudsterSimilarityThreshold") in
      let duplicateRegistrationAction =
        (Option.map ~f:DuplicateRegistrationAction.of_xml)
          (Xml.child xml_arg0 "DuplicateRegistrationAction") in
      make ?fraudsterSimilarityThreshold ?duplicateRegistrationAction ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let fraudsterSimilarityThreshold =
        field_map json "FraudsterSimilarityThreshold" Score.of_json in
      let duplicateRegistrationAction =
        field_map json "DuplicateRegistrationAction"
          DuplicateRegistrationAction.of_json in
      make ?fraudsterSimilarityThreshold ?duplicateRegistrationAction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration definining the action to take when a duplicate fraudster is detected, and the similarity threshold to use for detecting a duplicate fraudster during a batch fraudster registration job."]
module SpeakerSummary =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc "A timestamp showing the speaker's creation time."];
      customerSpeakerId: CustomerSpeakerId.t option
        [@ocaml.doc "The client-provided identifier for the speaker."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier of the domain that contains the speaker."];
      generatedSpeakerId: GeneratedSpeakerId.t option
        [@ocaml.doc "The service-generated identifier for the speaker."];
      status: SpeakerStatus.t option
        [@ocaml.doc "The current status of the speaker."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "A timestamp showing the speaker's last update."]}
    let make ?createdAt =
      fun ?customerSpeakerId ->
        fun ?domainId ->
          fun ?generatedSpeakerId ->
            fun ?status ->
              fun ?updatedAt ->
                fun () ->
                  {
                    createdAt;
                    customerSpeakerId;
                    domainId;
                    generatedSpeakerId;
                    status;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("CustomerSpeakerId",
          (Option.map x.customerSpeakerId ~f:CustomerSpeakerId.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("GeneratedSpeakerId",
          (Option.map x.generatedSpeakerId ~f:GeneratedSpeakerId.to_value));
        ("Status", (Option.map x.status ~f:SpeakerStatus.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let status =
        (Option.map ~f:SpeakerStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let generatedSpeakerId =
        (Option.map ~f:GeneratedSpeakerId.of_xml)
          (Xml.child xml_arg0 "GeneratedSpeakerId") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let customerSpeakerId =
        (Option.map ~f:CustomerSpeakerId.of_xml)
          (Xml.child xml_arg0 "CustomerSpeakerId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?updatedAt ?status ?generatedSpeakerId ?domainId
        ?customerSpeakerId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let status = field_map json "Status" SpeakerStatus.of_json in
      let generatedSpeakerId =
        field_map json "GeneratedSpeakerId" GeneratedSpeakerId.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let customerSpeakerId =
        field_map json "CustomerSpeakerId" CustomerSpeakerId.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?updatedAt ?status ?generatedSpeakerId ?domainId
        ?customerSpeakerId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains a summary of information about a speaker."]
module SpeakerEnrollmentJobSummary =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing the creation time of the speaker enrollment job."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier of the domain that contains the speaker enrollment job."];
      endedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing when the speaker enrollment job ended."];
      failureDetails: FailureDetails.t option
        [@ocaml.doc
          "Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed."];
      jobId: JobId.t option
        [@ocaml.doc
          "The service-generated identifier for the speaker enrollment job."];
      jobName: JobName.t option
        [@ocaml.doc
          "The client-provided name for the speaker enrollment job."];
      jobProgress: JobProgress.t option
        [@ocaml.doc
          "Provides details regarding job progress. This field shows the completed percentage of enrollment requests listed in the input file."];
      jobStatus: SpeakerEnrollmentJobStatus.t option
        [@ocaml.doc "The current status of the speaker enrollment job."]}
    let make ?createdAt =
      fun ?domainId ->
        fun ?endedAt ->
          fun ?failureDetails ->
            fun ?jobId ->
              fun ?jobName ->
                fun ?jobProgress ->
                  fun ?jobStatus ->
                    fun () ->
                      {
                        createdAt;
                        domainId;
                        endedAt;
                        failureDetails;
                        jobId;
                        jobName;
                        jobProgress;
                        jobStatus
                      }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("EndedAt", (Option.map x.endedAt ~f:Timestamp.to_value));
        ("FailureDetails",
          (Option.map x.failureDetails ~f:FailureDetails.to_value));
        ("JobId", (Option.map x.jobId ~f:JobId.to_value));
        ("JobName", (Option.map x.jobName ~f:JobName.to_value));
        ("JobProgress", (Option.map x.jobProgress ~f:JobProgress.to_value));
        ("JobStatus",
          (Option.map x.jobStatus ~f:SpeakerEnrollmentJobStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobStatus =
        (Option.map ~f:SpeakerEnrollmentJobStatus.of_xml)
          (Xml.child xml_arg0 "JobStatus") in
      let jobProgress =
        (Option.map ~f:JobProgress.of_xml) (Xml.child xml_arg0 "JobProgress") in
      let jobName =
        (Option.map ~f:JobName.of_xml) (Xml.child xml_arg0 "JobName") in
      let jobId = (Option.map ~f:JobId.of_xml) (Xml.child xml_arg0 "JobId") in
      let failureDetails =
        (Option.map ~f:FailureDetails.of_xml)
          (Xml.child xml_arg0 "FailureDetails") in
      let endedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndedAt") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?jobStatus ?jobProgress ?jobName ?jobId ?failureDetails ?endedAt
        ?domainId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let jobStatus =
        field_map json "JobStatus" SpeakerEnrollmentJobStatus.of_json in
      let jobProgress = field_map json "JobProgress" JobProgress.of_json in
      let jobName = field_map json "JobName" JobName.of_json in
      let jobId = field_map json "JobId" JobId.of_json in
      let failureDetails =
        field_map json "FailureDetails" FailureDetails.of_json in
      let endedAt = field_map json "EndedAt" Timestamp.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?jobStatus ?jobProgress ?jobName ?jobId ?failureDetails ?endedAt
        ?domainId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains a summary of information about a speaker enrollment job."]
module FraudsterRegistrationJobSummary =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing when the fraudster registration job is created."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier of the domain containing the fraudster registration job."];
      endedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing when the fraudster registration job ended."];
      failureDetails: FailureDetails.t option
        [@ocaml.doc
          "Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed."];
      jobId: JobId.t option
        [@ocaml.doc
          "The service-generated identifier for the fraudster registration job."];
      jobName: JobName.t option
        [@ocaml.doc
          "The client-provied name for the fraudster registration job."];
      jobProgress: JobProgress.t option
        [@ocaml.doc
          "Shows the completed percentage of registration requests listed in the input file."];
      jobStatus: FraudsterRegistrationJobStatus.t option
        [@ocaml.doc "The current status of the fraudster registration job."]}
    let make ?createdAt =
      fun ?domainId ->
        fun ?endedAt ->
          fun ?failureDetails ->
            fun ?jobId ->
              fun ?jobName ->
                fun ?jobProgress ->
                  fun ?jobStatus ->
                    fun () ->
                      {
                        createdAt;
                        domainId;
                        endedAt;
                        failureDetails;
                        jobId;
                        jobName;
                        jobProgress;
                        jobStatus
                      }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("EndedAt", (Option.map x.endedAt ~f:Timestamp.to_value));
        ("FailureDetails",
          (Option.map x.failureDetails ~f:FailureDetails.to_value));
        ("JobId", (Option.map x.jobId ~f:JobId.to_value));
        ("JobName", (Option.map x.jobName ~f:JobName.to_value));
        ("JobProgress", (Option.map x.jobProgress ~f:JobProgress.to_value));
        ("JobStatus",
          (Option.map x.jobStatus ~f:FraudsterRegistrationJobStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobStatus =
        (Option.map ~f:FraudsterRegistrationJobStatus.of_xml)
          (Xml.child xml_arg0 "JobStatus") in
      let jobProgress =
        (Option.map ~f:JobProgress.of_xml) (Xml.child xml_arg0 "JobProgress") in
      let jobName =
        (Option.map ~f:JobName.of_xml) (Xml.child xml_arg0 "JobName") in
      let jobId = (Option.map ~f:JobId.of_xml) (Xml.child xml_arg0 "JobId") in
      let failureDetails =
        (Option.map ~f:FailureDetails.of_xml)
          (Xml.child xml_arg0 "FailureDetails") in
      let endedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndedAt") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?jobStatus ?jobProgress ?jobName ?jobId ?failureDetails ?endedAt
        ?domainId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let jobStatus =
        field_map json "JobStatus" FraudsterRegistrationJobStatus.of_json in
      let jobProgress = field_map json "JobProgress" JobProgress.of_json in
      let jobName = field_map json "JobName" JobName.of_json in
      let jobId = field_map json "JobId" JobId.of_json in
      let failureDetails =
        field_map json "FailureDetails" FailureDetails.of_json in
      let endedAt = field_map json "EndedAt" Timestamp.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?jobStatus ?jobProgress ?jobName ?jobId ?failureDetails ?endedAt
        ?domainId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains a summary of information about a fraudster registration job."]
module DomainSummary =
  struct
    type nonrec t =
      {
      arn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the domain."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The timestamp showing when the domain is created."];
      description: Description.t option
        [@ocaml.doc "The client-provided description of the domain."];
      domainId: DomainId.t option
        [@ocaml.doc "The service-generated identifier for the domain."];
      domainStatus: DomainStatus.t option
        [@ocaml.doc "The current status of the domain."];
      name: DomainName.t option
        [@ocaml.doc "The client-provided name for the domain."];
      serverSideEncryptionConfiguration:
        ServerSideEncryptionConfiguration.t option
        [@ocaml.doc
          "The server-side encryption configuration containing the KMS Key Identifier you want Voice ID to use to encrypt your data.."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The timestamp showing the domain's last update."]}
    let make ?arn =
      fun ?createdAt ->
        fun ?description ->
          fun ?domainId ->
            fun ?domainStatus ->
              fun ?name ->
                fun ?serverSideEncryptionConfiguration ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        arn;
                        createdAt;
                        description;
                        domainId;
                        domainStatus;
                        name;
                        serverSideEncryptionConfiguration;
                        updatedAt
                      }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("DomainStatus",
          (Option.map x.domainStatus ~f:DomainStatus.to_value));
        ("Name", (Option.map x.name ~f:DomainName.to_value));
        ("ServerSideEncryptionConfiguration",
          (Option.map x.serverSideEncryptionConfiguration
             ~f:ServerSideEncryptionConfiguration.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let serverSideEncryptionConfiguration =
        (Option.map ~f:ServerSideEncryptionConfiguration.of_xml)
          (Xml.child xml_arg0 "ServerSideEncryptionConfiguration") in
      let name =
        (Option.map ~f:DomainName.of_xml) (Xml.child xml_arg0 "Name") in
      let domainStatus =
        (Option.map ~f:DomainStatus.of_xml)
          (Xml.child xml_arg0 "DomainStatus") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?serverSideEncryptionConfiguration ?name ?domainStatus
        ?domainId ?description ?createdAt ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let serverSideEncryptionConfiguration =
        field_map json "ServerSideEncryptionConfiguration"
          ServerSideEncryptionConfiguration.of_json in
      let name = field_map json "Name" DomainName.of_json in
      let domainStatus = field_map json "DomainStatus" DomainStatus.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let description = field_map json "Description" Description.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      make ?updatedAt ?serverSideEncryptionConfiguration ?name ?domainStatus
        ?domainId ?description ?createdAt ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains a summary of information about a domain."]
module AuthenticationConfiguration =
  struct
    type nonrec t =
      {
      acceptanceThreshold: Score.t
        [@ocaml.doc
          "The minimum threshold needed to successfully authenticate a speaker."]}
    let context_ = "AuthenticationConfiguration"
    let make ~acceptanceThreshold = fun () -> { acceptanceThreshold }
    let to_value x =
      structure_to_value
        [("AcceptanceThreshold",
           (Some (Score.to_value x.acceptanceThreshold)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptanceThreshold =
        Score.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AcceptanceThreshold") in
      make ~acceptanceThreshold ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let acceptanceThreshold =
        field_map_exn json "AcceptanceThreshold" Score.of_json in
      make ~acceptanceThreshold ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration used to authenticate a speaker during a session."]
module AuthenticationDecision =
  struct
    type nonrec t =
      | ACCEPT 
      | REJECT 
      | NOT_ENOUGH_SPEECH 
      | SPEAKER_NOT_ENROLLED 
      | SPEAKER_OPTED_OUT 
      | SPEAKER_ID_NOT_PROVIDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCEPT -> "ACCEPT"
      | REJECT -> "REJECT"
      | NOT_ENOUGH_SPEECH -> "NOT_ENOUGH_SPEECH"
      | SPEAKER_NOT_ENROLLED -> "SPEAKER_NOT_ENROLLED"
      | SPEAKER_OPTED_OUT -> "SPEAKER_OPTED_OUT"
      | SPEAKER_ID_NOT_PROVIDED -> "SPEAKER_ID_NOT_PROVIDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCEPT" -> ACCEPT
      | "REJECT" -> REJECT
      | "NOT_ENOUGH_SPEECH" -> NOT_ENOUGH_SPEECH
      | "SPEAKER_NOT_ENROLLED" -> SPEAKER_NOT_ENROLLED
      | "SPEAKER_OPTED_OUT" -> SPEAKER_OPTED_OUT
      | "SPEAKER_ID_NOT_PROVIDED" -> SPEAKER_ID_NOT_PROVIDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AuthenticationDecision" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AuthenticationDecision" j)
    let to_json = simple_to_json to_value
  end
module UniqueIdLarge =
  struct
    type nonrec t = string
    let context_ = "UniqueIdLarge"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:22) >>=
             (fun () ->
                (check_string_max i ~max:22) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UniqueIdLarge" j
    let to_json = simple_to_json to_value
  end
module FraudDetectionConfiguration =
  struct
    type nonrec t =
      {
      riskThreshold: Score.t
        [@ocaml.doc
          "Threshold value for determining whether the speaker is a fraudster. If the detected risk score calculated by Voice ID is higher than the threshold, the speaker is considered a fraudster."]}
    let context_ = "FraudDetectionConfiguration"
    let make ~riskThreshold = fun () -> { riskThreshold }
    let to_value x =
      structure_to_value
        [("RiskThreshold", (Some (Score.to_value x.riskThreshold)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let riskThreshold =
        Score.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RiskThreshold") in
      make ~riskThreshold ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let riskThreshold = field_map_exn json "RiskThreshold" Score.of_json in
      make ~riskThreshold ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration used for performing fraud detection over a speaker during a session."]
module FraudDetectionDecision =
  struct
    type nonrec t =
      | HIGH_RISK 
      | LOW_RISK 
      | NOT_ENOUGH_SPEECH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HIGH_RISK -> "HIGH_RISK"
      | LOW_RISK -> "LOW_RISK"
      | NOT_ENOUGH_SPEECH -> "NOT_ENOUGH_SPEECH"
      | Non_static_id s -> s
    let of_string =
      function
      | "HIGH_RISK" -> HIGH_RISK
      | "LOW_RISK" -> LOW_RISK
      | "NOT_ENOUGH_SPEECH" -> NOT_ENOUGH_SPEECH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FraudDetectionDecision" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FraudDetectionDecision" j)
    let to_json = simple_to_json to_value
  end
module FraudDetectionReasons =
  struct
    type nonrec t = FraudDetectionReason.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:FraudDetectionReason.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FraudDetectionReason.of_xml)
    let of_json j =
      list_of_json ~kind:"FraudDetectionReasons"
        ~of_json:FraudDetectionReason.of_json j
    let to_json v = composed_to_json to_value v
  end
module FraudRiskDetails =
  struct
    type nonrec t =
      {
      knownFraudsterRisk: KnownFraudsterRisk.t
        [@ocaml.doc
          "The details resulting from 'Known Fraudster Risk' analysis of the speaker."]}
    let context_ = "FraudRiskDetails"
    let make ~knownFraudsterRisk = fun () -> { knownFraudsterRisk }
    let to_value x =
      structure_to_value
        [("KnownFraudsterRisk",
           (Some (KnownFraudsterRisk.to_value x.knownFraudsterRisk)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let knownFraudsterRisk =
        KnownFraudsterRisk.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KnownFraudsterRisk") in
      make ~knownFraudsterRisk ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let knownFraudsterRisk =
        field_map_exn json "KnownFraudsterRisk" KnownFraudsterRisk.of_json in
      make ~knownFraudsterRisk ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details regarding various fraud risk analyses performed against the current session state and streamed audio of the speaker."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient permissions to perform this action. Check the error message and try again."]
module ConflictException =
  struct
    type nonrec t =
      {
      conflictType: ConflictType.t option
        [@ocaml.doc
          "The type of conflict which caused a ConflictException. Possible types and the corresponding error messages are as follows: DOMAIN_NOT_ACTIVE: The domain is not active. CANNOT_CHANGE_SPEAKER_AFTER_ENROLLMENT: You cannot change the speaker ID after an enrollment has been requested. ENROLLMENT_ALREADY_EXISTS: There is already an enrollment for this session. SPEAKER_NOT_SET: You must set the speaker ID before requesting an enrollment. SPEAKER_OPTED_OUT: You cannot request an enrollment for an opted out speaker. CONCURRENT_CHANGES: The request could not be processed as the resource was modified by another request during execution."];
      message: String_.t option }
    let make ?conflictType =
      fun ?message -> fun () -> { conflictType; message }
    let to_value x =
      structure_to_value
        [("ConflictType",
           (Option.map x.conflictType ~f:ConflictType.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let conflictType =
        (Option.map ~f:ConflictType.of_xml)
          (Xml.child xml_arg0 "ConflictType") in
      make ?message ?conflictType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let conflictType = field_map json "ConflictType" ConflictType.of_json in
      make ?message ?conflictType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request failed due to a conflict. Check the ConflictType and error message for more details."]
module Domain =
  struct
    type nonrec t =
      {
      arn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the domain."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The timestamp at which the domain is created."];
      description: Description.t option
        [@ocaml.doc "The client-provided description of the domain."];
      domainId: DomainId.t option
        [@ocaml.doc "The service-generated identifier for the domain."];
      domainStatus: DomainStatus.t option
        [@ocaml.doc "The current status of the domain."];
      name: DomainName.t option
        [@ocaml.doc "The client-provided name for the domain."];
      serverSideEncryptionConfiguration:
        ServerSideEncryptionConfiguration.t option
        [@ocaml.doc
          "The server-side encryption configuration containing the KMS Key Identifier you want Voice ID to use to encrypt your data."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The timestamp showing the domain's last update."]}
    let make ?arn =
      fun ?createdAt ->
        fun ?description ->
          fun ?domainId ->
            fun ?domainStatus ->
              fun ?name ->
                fun ?serverSideEncryptionConfiguration ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        arn;
                        createdAt;
                        description;
                        domainId;
                        domainStatus;
                        name;
                        serverSideEncryptionConfiguration;
                        updatedAt
                      }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("DomainStatus",
          (Option.map x.domainStatus ~f:DomainStatus.to_value));
        ("Name", (Option.map x.name ~f:DomainName.to_value));
        ("ServerSideEncryptionConfiguration",
          (Option.map x.serverSideEncryptionConfiguration
             ~f:ServerSideEncryptionConfiguration.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let serverSideEncryptionConfiguration =
        (Option.map ~f:ServerSideEncryptionConfiguration.of_xml)
          (Xml.child xml_arg0 "ServerSideEncryptionConfiguration") in
      let name =
        (Option.map ~f:DomainName.of_xml) (Xml.child xml_arg0 "Name") in
      let domainStatus =
        (Option.map ~f:DomainStatus.of_xml)
          (Xml.child xml_arg0 "DomainStatus") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?serverSideEncryptionConfiguration ?name ?domainStatus
        ?domainId ?description ?createdAt ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let serverSideEncryptionConfiguration =
        field_map json "ServerSideEncryptionConfiguration"
          ServerSideEncryptionConfiguration.of_json in
      let name = field_map json "Name" DomainName.of_json in
      let domainStatus = field_map json "DomainStatus" DomainStatus.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let description = field_map json "Description" Description.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      make ?updatedAt ?serverSideEncryptionConfiguration ?name ?domainStatus
        ?domainId ?description ?createdAt ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains all the information about a domain."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request failed due to an unknown error on the server side."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of resource which cannot not be found. Possible types are BATCH_JOB, COMPLIANCE_CONSENT, DOMAIN, FRAUDSTER, SESSION and SPEAKER."]}
    let make ?message =
      fun ?resourceType -> fun () -> { message; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let message = field_map json "Message" String_.of_json in
      make ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified resource cannot be found. Check the ResourceType and error message for more details."]
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was denied due to request throttling. Please slow down your request rate. Refer to Amazon Connect Voice ID Service API throttling quotas and try your request again."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request failed one or more validations; check the error message for more details."]
module AmazonResourceName =
  struct
    type nonrec t = string
    let context_ = "AmazonResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(-[^:]+)?:voiceid.+:[0-9]{12}:domain/[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AmazonResourceName" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request exceeded the service quota. Refer to Voice ID Service Quotas and try your request again."]
module SpeakerEnrollmentJob =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing the creation of the speaker enrollment job."];
      dataAccessRoleArn: IamRoleArn.t option
        [@ocaml.doc
          "The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the job output file."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier of the domain that contains the speaker enrollment job."];
      endedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing when the speaker enrollment job ended."];
      enrollmentConfig: EnrollmentConfig.t option
        [@ocaml.doc
          "The configuration that defines the action to take when the speaker is already enrolled in Voice ID, and the FraudDetectionConfig to use."];
      failureDetails: FailureDetails.t option
        [@ocaml.doc
          "Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed."];
      inputDataConfig: InputDataConfig.t option
        [@ocaml.doc
          "The input data config containing an S3 URI for the input manifest file that contains the list of speaker enrollment job requests."];
      jobId: JobId.t option
        [@ocaml.doc
          "The service-generated identifier for the speaker enrollment job."];
      jobName: JobName.t option
        [@ocaml.doc
          "The client-provided name for the speaker enrollment job."];
      jobProgress: JobProgress.t option
        [@ocaml.doc
          "Provides details on job progress. This field shows the completed percentage of registration requests listed in the input file."];
      jobStatus: SpeakerEnrollmentJobStatus.t option
        [@ocaml.doc "The current status of the speaker enrollment job."];
      outputDataConfig: OutputDataConfig.t option
        [@ocaml.doc
          "The output data config containing the S3 location where Voice ID writes the job output file; you must also include a KMS Key ID to encrypt the file."]}
    let make ?createdAt =
      fun ?dataAccessRoleArn ->
        fun ?domainId ->
          fun ?endedAt ->
            fun ?enrollmentConfig ->
              fun ?failureDetails ->
                fun ?inputDataConfig ->
                  fun ?jobId ->
                    fun ?jobName ->
                      fun ?jobProgress ->
                        fun ?jobStatus ->
                          fun ?outputDataConfig ->
                            fun () ->
                              {
                                createdAt;
                                dataAccessRoleArn;
                                domainId;
                                endedAt;
                                enrollmentConfig;
                                failureDetails;
                                inputDataConfig;
                                jobId;
                                jobName;
                                jobProgress;
                                jobStatus;
                                outputDataConfig
                              }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DataAccessRoleArn",
          (Option.map x.dataAccessRoleArn ~f:IamRoleArn.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("EndedAt", (Option.map x.endedAt ~f:Timestamp.to_value));
        ("EnrollmentConfig",
          (Option.map x.enrollmentConfig ~f:EnrollmentConfig.to_value));
        ("FailureDetails",
          (Option.map x.failureDetails ~f:FailureDetails.to_value));
        ("InputDataConfig",
          (Option.map x.inputDataConfig ~f:InputDataConfig.to_value));
        ("JobId", (Option.map x.jobId ~f:JobId.to_value));
        ("JobName", (Option.map x.jobName ~f:JobName.to_value));
        ("JobProgress", (Option.map x.jobProgress ~f:JobProgress.to_value));
        ("JobStatus",
          (Option.map x.jobStatus ~f:SpeakerEnrollmentJobStatus.to_value));
        ("OutputDataConfig",
          (Option.map x.outputDataConfig ~f:OutputDataConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputDataConfig =
        (Option.map ~f:OutputDataConfig.of_xml)
          (Xml.child xml_arg0 "OutputDataConfig") in
      let jobStatus =
        (Option.map ~f:SpeakerEnrollmentJobStatus.of_xml)
          (Xml.child xml_arg0 "JobStatus") in
      let jobProgress =
        (Option.map ~f:JobProgress.of_xml) (Xml.child xml_arg0 "JobProgress") in
      let jobName =
        (Option.map ~f:JobName.of_xml) (Xml.child xml_arg0 "JobName") in
      let jobId = (Option.map ~f:JobId.of_xml) (Xml.child xml_arg0 "JobId") in
      let inputDataConfig =
        (Option.map ~f:InputDataConfig.of_xml)
          (Xml.child xml_arg0 "InputDataConfig") in
      let failureDetails =
        (Option.map ~f:FailureDetails.of_xml)
          (Xml.child xml_arg0 "FailureDetails") in
      let enrollmentConfig =
        (Option.map ~f:EnrollmentConfig.of_xml)
          (Xml.child xml_arg0 "EnrollmentConfig") in
      let endedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndedAt") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let dataAccessRoleArn =
        (Option.map ~f:IamRoleArn.of_xml)
          (Xml.child xml_arg0 "DataAccessRoleArn") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?outputDataConfig ?jobStatus ?jobProgress ?jobName ?jobId
        ?inputDataConfig ?failureDetails ?enrollmentConfig ?endedAt ?domainId
        ?dataAccessRoleArn ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let outputDataConfig =
        field_map json "OutputDataConfig" OutputDataConfig.of_json in
      let jobStatus =
        field_map json "JobStatus" SpeakerEnrollmentJobStatus.of_json in
      let jobProgress = field_map json "JobProgress" JobProgress.of_json in
      let jobName = field_map json "JobName" JobName.of_json in
      let jobId = field_map json "JobId" JobId.of_json in
      let inputDataConfig =
        field_map json "InputDataConfig" InputDataConfig.of_json in
      let failureDetails =
        field_map json "FailureDetails" FailureDetails.of_json in
      let enrollmentConfig =
        field_map json "EnrollmentConfig" EnrollmentConfig.of_json in
      let endedAt = field_map json "EndedAt" Timestamp.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let dataAccessRoleArn =
        field_map json "DataAccessRoleArn" IamRoleArn.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?outputDataConfig ?jobStatus ?jobProgress ?jobName ?jobId
        ?inputDataConfig ?failureDetails ?enrollmentConfig ?endedAt ?domainId
        ?dataAccessRoleArn ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains all the information about a speaker enrollment job."]
module ClientTokenString =
  struct
    type nonrec t = string
    let context_ = "ClientTokenString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-_]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientTokenString" j
    let to_json = simple_to_json to_value
  end
module FraudsterRegistrationJob =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing the creation time of the fraudster registration job."];
      dataAccessRoleArn: IamRoleArn.t option
        [@ocaml.doc
          "The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the job output file."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier of the domain containing the fraudster registration job."];
      endedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp showing when the fraudster registration job ended."];
      failureDetails: FailureDetails.t option
        [@ocaml.doc
          "Contains details that are populated when an entire batch job fails. In cases of individual registration job failures, the batch job as a whole doesn't fail; it is completed with a JobStatus of COMPLETED_WITH_ERRORS. You can use the job output file to identify the individual registration requests that failed."];
      inputDataConfig: InputDataConfig.t option
        [@ocaml.doc
          "The input data config containing an S3 URI for the input manifest file that contains the list of fraudster registration job requests."];
      jobId: JobId.t option
        [@ocaml.doc
          "The service-generated identifier for the fraudster registration job."];
      jobName: JobName.t option
        [@ocaml.doc
          "The client-provied name for the fraudster registration job."];
      jobProgress: JobProgress.t option
        [@ocaml.doc
          "Shows the completed percentage of registration requests listed in the input file."];
      jobStatus: FraudsterRegistrationJobStatus.t option
        [@ocaml.doc "The current status of the fraudster registration job."];
      outputDataConfig: OutputDataConfig.t option
        [@ocaml.doc
          "The output data config containing the S3 location where you want Voice ID to write your job output file; you must also include a KMS Key ID in order to encrypt the file."];
      registrationConfig: RegistrationConfig.t option
        [@ocaml.doc
          "The registration config containing details such as the action to take when a duplicate fraudster is detected, and the similarity threshold to use for detecting a duplicate fraudster."]}
    let make ?createdAt =
      fun ?dataAccessRoleArn ->
        fun ?domainId ->
          fun ?endedAt ->
            fun ?failureDetails ->
              fun ?inputDataConfig ->
                fun ?jobId ->
                  fun ?jobName ->
                    fun ?jobProgress ->
                      fun ?jobStatus ->
                        fun ?outputDataConfig ->
                          fun ?registrationConfig ->
                            fun () ->
                              {
                                createdAt;
                                dataAccessRoleArn;
                                domainId;
                                endedAt;
                                failureDetails;
                                inputDataConfig;
                                jobId;
                                jobName;
                                jobProgress;
                                jobStatus;
                                outputDataConfig;
                                registrationConfig
                              }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DataAccessRoleArn",
          (Option.map x.dataAccessRoleArn ~f:IamRoleArn.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("EndedAt", (Option.map x.endedAt ~f:Timestamp.to_value));
        ("FailureDetails",
          (Option.map x.failureDetails ~f:FailureDetails.to_value));
        ("InputDataConfig",
          (Option.map x.inputDataConfig ~f:InputDataConfig.to_value));
        ("JobId", (Option.map x.jobId ~f:JobId.to_value));
        ("JobName", (Option.map x.jobName ~f:JobName.to_value));
        ("JobProgress", (Option.map x.jobProgress ~f:JobProgress.to_value));
        ("JobStatus",
          (Option.map x.jobStatus ~f:FraudsterRegistrationJobStatus.to_value));
        ("OutputDataConfig",
          (Option.map x.outputDataConfig ~f:OutputDataConfig.to_value));
        ("RegistrationConfig",
          (Option.map x.registrationConfig ~f:RegistrationConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let registrationConfig =
        (Option.map ~f:RegistrationConfig.of_xml)
          (Xml.child xml_arg0 "RegistrationConfig") in
      let outputDataConfig =
        (Option.map ~f:OutputDataConfig.of_xml)
          (Xml.child xml_arg0 "OutputDataConfig") in
      let jobStatus =
        (Option.map ~f:FraudsterRegistrationJobStatus.of_xml)
          (Xml.child xml_arg0 "JobStatus") in
      let jobProgress =
        (Option.map ~f:JobProgress.of_xml) (Xml.child xml_arg0 "JobProgress") in
      let jobName =
        (Option.map ~f:JobName.of_xml) (Xml.child xml_arg0 "JobName") in
      let jobId = (Option.map ~f:JobId.of_xml) (Xml.child xml_arg0 "JobId") in
      let inputDataConfig =
        (Option.map ~f:InputDataConfig.of_xml)
          (Xml.child xml_arg0 "InputDataConfig") in
      let failureDetails =
        (Option.map ~f:FailureDetails.of_xml)
          (Xml.child xml_arg0 "FailureDetails") in
      let endedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndedAt") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let dataAccessRoleArn =
        (Option.map ~f:IamRoleArn.of_xml)
          (Xml.child xml_arg0 "DataAccessRoleArn") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?registrationConfig ?outputDataConfig ?jobStatus ?jobProgress
        ?jobName ?jobId ?inputDataConfig ?failureDetails ?endedAt ?domainId
        ?dataAccessRoleArn ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let registrationConfig =
        field_map json "RegistrationConfig" RegistrationConfig.of_json in
      let outputDataConfig =
        field_map json "OutputDataConfig" OutputDataConfig.of_json in
      let jobStatus =
        field_map json "JobStatus" FraudsterRegistrationJobStatus.of_json in
      let jobProgress = field_map json "JobProgress" JobProgress.of_json in
      let jobName = field_map json "JobName" JobName.of_json in
      let jobId = field_map json "JobId" JobId.of_json in
      let inputDataConfig =
        field_map json "InputDataConfig" InputDataConfig.of_json in
      let failureDetails =
        field_map json "FailureDetails" FailureDetails.of_json in
      let endedAt = field_map json "EndedAt" Timestamp.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let dataAccessRoleArn =
        field_map json "DataAccessRoleArn" IamRoleArn.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?registrationConfig ?outputDataConfig ?jobStatus ?jobProgress
        ?jobName ?jobId ?inputDataConfig ?failureDetails ?endedAt ?domainId
        ?dataAccessRoleArn ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains all the information about a fraudster registration job."]
module Speaker =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc "A timestamp showing when the speaker is created."];
      customerSpeakerId: CustomerSpeakerId.t option
        [@ocaml.doc "The client-provided identifier for the speaker."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier of the domain that contains the speaker."];
      generatedSpeakerId: GeneratedSpeakerId.t option
        [@ocaml.doc "The service-generated identifier for the speaker."];
      status: SpeakerStatus.t option
        [@ocaml.doc "The current status of the speaker."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "A timestamp showing the speaker's last update."]}
    let make ?createdAt =
      fun ?customerSpeakerId ->
        fun ?domainId ->
          fun ?generatedSpeakerId ->
            fun ?status ->
              fun ?updatedAt ->
                fun () ->
                  {
                    createdAt;
                    customerSpeakerId;
                    domainId;
                    generatedSpeakerId;
                    status;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("CustomerSpeakerId",
          (Option.map x.customerSpeakerId ~f:CustomerSpeakerId.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("GeneratedSpeakerId",
          (Option.map x.generatedSpeakerId ~f:GeneratedSpeakerId.to_value));
        ("Status", (Option.map x.status ~f:SpeakerStatus.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let status =
        (Option.map ~f:SpeakerStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let generatedSpeakerId =
        (Option.map ~f:GeneratedSpeakerId.of_xml)
          (Xml.child xml_arg0 "GeneratedSpeakerId") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let customerSpeakerId =
        (Option.map ~f:CustomerSpeakerId.of_xml)
          (Xml.child xml_arg0 "CustomerSpeakerId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?updatedAt ?status ?generatedSpeakerId ?domainId
        ?customerSpeakerId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let status = field_map json "Status" SpeakerStatus.of_json in
      let generatedSpeakerId =
        field_map json "GeneratedSpeakerId" GeneratedSpeakerId.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let customerSpeakerId =
        field_map json "CustomerSpeakerId" CustomerSpeakerId.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?updatedAt ?status ?generatedSpeakerId ?domainId
        ?customerSpeakerId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains all the information about a speaker."]
module SpeakerId =
  struct
    type nonrec t = string
    let context_ = "SpeakerId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(id#[a-zA-Z0-9]{22}|[a-zA-Z0-9][a-zA-Z0-9_-]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SpeakerId" j
    let to_json = simple_to_json to_value
  end
module SpeakerSummaries =
  struct
    type nonrec t = SpeakerSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SpeakerSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SpeakerSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SpeakerSummaries" ~of_json:SpeakerSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResultsForList =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResultsForList" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () -> check_pattern i ~pattern:"^\\p{ASCII}{0,8192}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module SpeakerEnrollmentJobSummaries =
  struct
    type nonrec t = SpeakerEnrollmentJobSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SpeakerEnrollmentJobSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SpeakerEnrollmentJobSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SpeakerEnrollmentJobSummaries"
        ~of_json:SpeakerEnrollmentJobSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module FraudsterRegistrationJobSummaries =
  struct
    type nonrec t = FraudsterRegistrationJobSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FraudsterRegistrationJobSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FraudsterRegistrationJobSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"FraudsterRegistrationJobSummaries"
        ~of_json:FraudsterRegistrationJobSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module DomainSummaries =
  struct
    type nonrec t = DomainSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DomainSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DomainSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DomainSummaries" ~of_json:DomainSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResultsForListDomainFe =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResultsForListDomainFe"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AuthenticationResult =
  struct
    type nonrec t =
      {
      audioAggregationEndedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp indicating when audio aggregation ended for this authentication result."];
      audioAggregationStartedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp indicating when audio aggregation started for this authentication result."];
      authenticationResultId: UniqueIdLarge.t option
        [@ocaml.doc
          "The unique identifier for this authentication result. Because there can be multiple authentications for a given session, this field helps to identify if the returned result is from a previous streaming activity or a new result. Note that in absence of any new streaming activity, AcceptanceThreshold changes, or SpeakerId changes, Voice ID always returns cached Authentication Result for this API."];
      configuration: AuthenticationConfiguration.t option
        [@ocaml.doc
          "The AuthenticationConfiguration used to generate this authentication result."];
      customerSpeakerId: CustomerSpeakerId.t option
        [@ocaml.doc
          "The client-provided identifier for the speaker whose authentication result is produced. Only present if a SpeakerId is provided for the session."];
      decision: AuthenticationDecision.t option
        [@ocaml.doc
          "The authentication decision produced by Voice ID, processed against the current session state and streamed audio of the speaker."];
      generatedSpeakerId: GeneratedSpeakerId.t option
        [@ocaml.doc
          "The service-generated identifier for the speaker whose authentication result is produced."];
      score: Score.t option
        [@ocaml.doc
          "The authentication score for the speaker whose authentication result is produced. This value is only present if the authentication decision is either ACCEPT or REJECT."]}
    let make ?audioAggregationEndedAt =
      fun ?audioAggregationStartedAt ->
        fun ?authenticationResultId ->
          fun ?configuration ->
            fun ?customerSpeakerId ->
              fun ?decision ->
                fun ?generatedSpeakerId ->
                  fun ?score ->
                    fun () ->
                      {
                        audioAggregationEndedAt;
                        audioAggregationStartedAt;
                        authenticationResultId;
                        configuration;
                        customerSpeakerId;
                        decision;
                        generatedSpeakerId;
                        score
                      }
    let to_value x =
      structure_to_value
        [("AudioAggregationEndedAt",
           (Option.map x.audioAggregationEndedAt ~f:Timestamp.to_value));
        ("AudioAggregationStartedAt",
          (Option.map x.audioAggregationStartedAt ~f:Timestamp.to_value));
        ("AuthenticationResultId",
          (Option.map x.authenticationResultId ~f:UniqueIdLarge.to_value));
        ("Configuration",
          (Option.map x.configuration ~f:AuthenticationConfiguration.to_value));
        ("CustomerSpeakerId",
          (Option.map x.customerSpeakerId ~f:CustomerSpeakerId.to_value));
        ("Decision",
          (Option.map x.decision ~f:AuthenticationDecision.to_value));
        ("GeneratedSpeakerId",
          (Option.map x.generatedSpeakerId ~f:GeneratedSpeakerId.to_value));
        ("Score", (Option.map x.score ~f:Score.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let score = (Option.map ~f:Score.of_xml) (Xml.child xml_arg0 "Score") in
      let generatedSpeakerId =
        (Option.map ~f:GeneratedSpeakerId.of_xml)
          (Xml.child xml_arg0 "GeneratedSpeakerId") in
      let decision =
        (Option.map ~f:AuthenticationDecision.of_xml)
          (Xml.child xml_arg0 "Decision") in
      let customerSpeakerId =
        (Option.map ~f:CustomerSpeakerId.of_xml)
          (Xml.child xml_arg0 "CustomerSpeakerId") in
      let configuration =
        (Option.map ~f:AuthenticationConfiguration.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      let authenticationResultId =
        (Option.map ~f:UniqueIdLarge.of_xml)
          (Xml.child xml_arg0 "AuthenticationResultId") in
      let audioAggregationStartedAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "AudioAggregationStartedAt") in
      let audioAggregationEndedAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "AudioAggregationEndedAt") in
      make ?score ?generatedSpeakerId ?decision ?customerSpeakerId
        ?configuration ?authenticationResultId ?audioAggregationStartedAt
        ?audioAggregationEndedAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let score = field_map json "Score" Score.of_json in
      let generatedSpeakerId =
        field_map json "GeneratedSpeakerId" GeneratedSpeakerId.of_json in
      let decision = field_map json "Decision" AuthenticationDecision.of_json in
      let customerSpeakerId =
        field_map json "CustomerSpeakerId" CustomerSpeakerId.of_json in
      let configuration =
        field_map json "Configuration" AuthenticationConfiguration.of_json in
      let authenticationResultId =
        field_map json "AuthenticationResultId" UniqueIdLarge.of_json in
      let audioAggregationStartedAt =
        field_map json "AudioAggregationStartedAt" Timestamp.of_json in
      let audioAggregationEndedAt =
        field_map json "AudioAggregationEndedAt" Timestamp.of_json in
      make ?score ?generatedSpeakerId ?decision ?customerSpeakerId
        ?configuration ?authenticationResultId ?audioAggregationStartedAt
        ?audioAggregationEndedAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The authentication result produced by Voice ID, processed against the current session state and streamed audio of the speaker."]
module FraudDetectionResult =
  struct
    type nonrec t =
      {
      audioAggregationEndedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp indicating when audio aggregation ended for this fraud detection result."];
      audioAggregationStartedAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp indicating when audio aggregation started for this fraud detection result."];
      configuration: FraudDetectionConfiguration.t option
        [@ocaml.doc
          "The FraudDetectionConfiguration used to generate this fraud detection result."];
      decision: FraudDetectionDecision.t option
        [@ocaml.doc
          "The fraud detection decision produced by Voice ID, processed against the current session state and streamed audio of the speaker."];
      fraudDetectionResultId: UniqueIdLarge.t option
        [@ocaml.doc
          "The unique identifier for this fraud detection result. Given there can be multiple fraud detections for a given session, this field helps in identifying if the returned result is from previous streaming activity or a new result. Note that in the absence of any new streaming activity or risk threshold changes, Voice ID always returns cached Fraud Detection result for this API."];
      reasons: FraudDetectionReasons.t option
        [@ocaml.doc
          "The reason speaker was flagged by the fraud detection system. This is only be populated if fraud detection Decision is HIGH_RISK, and only has one possible value: KNOWN_FRAUDSTER."];
      riskDetails: FraudRiskDetails.t option
        [@ocaml.doc "Details about each risk analyzed for this speaker."]}
    let make ?audioAggregationEndedAt =
      fun ?audioAggregationStartedAt ->
        fun ?configuration ->
          fun ?decision ->
            fun ?fraudDetectionResultId ->
              fun ?reasons ->
                fun ?riskDetails ->
                  fun () ->
                    {
                      audioAggregationEndedAt;
                      audioAggregationStartedAt;
                      configuration;
                      decision;
                      fraudDetectionResultId;
                      reasons;
                      riskDetails
                    }
    let to_value x =
      structure_to_value
        [("AudioAggregationEndedAt",
           (Option.map x.audioAggregationEndedAt ~f:Timestamp.to_value));
        ("AudioAggregationStartedAt",
          (Option.map x.audioAggregationStartedAt ~f:Timestamp.to_value));
        ("Configuration",
          (Option.map x.configuration ~f:FraudDetectionConfiguration.to_value));
        ("Decision",
          (Option.map x.decision ~f:FraudDetectionDecision.to_value));
        ("FraudDetectionResultId",
          (Option.map x.fraudDetectionResultId ~f:UniqueIdLarge.to_value));
        ("Reasons", (Option.map x.reasons ~f:FraudDetectionReasons.to_value));
        ("RiskDetails",
          (Option.map x.riskDetails ~f:FraudRiskDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let riskDetails =
        (Option.map ~f:FraudRiskDetails.of_xml)
          (Xml.child xml_arg0 "RiskDetails") in
      let reasons =
        (Option.map ~f:FraudDetectionReasons.of_xml)
          (Xml.child xml_arg0 "Reasons") in
      let fraudDetectionResultId =
        (Option.map ~f:UniqueIdLarge.of_xml)
          (Xml.child xml_arg0 "FraudDetectionResultId") in
      let decision =
        (Option.map ~f:FraudDetectionDecision.of_xml)
          (Xml.child xml_arg0 "Decision") in
      let configuration =
        (Option.map ~f:FraudDetectionConfiguration.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      let audioAggregationStartedAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "AudioAggregationStartedAt") in
      let audioAggregationEndedAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "AudioAggregationEndedAt") in
      make ?riskDetails ?reasons ?fraudDetectionResultId ?decision
        ?configuration ?audioAggregationStartedAt ?audioAggregationEndedAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let riskDetails = field_map json "RiskDetails" FraudRiskDetails.of_json in
      let reasons = field_map json "Reasons" FraudDetectionReasons.of_json in
      let fraudDetectionResultId =
        field_map json "FraudDetectionResultId" UniqueIdLarge.of_json in
      let decision = field_map json "Decision" FraudDetectionDecision.of_json in
      let configuration =
        field_map json "Configuration" FraudDetectionConfiguration.of_json in
      let audioAggregationStartedAt =
        field_map json "AudioAggregationStartedAt" Timestamp.of_json in
      let audioAggregationEndedAt =
        field_map json "AudioAggregationEndedAt" Timestamp.of_json in
      make ?riskDetails ?reasons ?fraudDetectionResultId ?decision
        ?configuration ?audioAggregationStartedAt ?audioAggregationEndedAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The fraud detection result produced by Voice ID, processed against the current session state and streamed audio of the speaker."]
module SessionId =
  struct
    type nonrec t = string
    let context_ = "SessionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:25) >>=
             (fun () ->
                (check_string_max i ~max:25) >>=
                  (fun () -> check_pattern i ~pattern:"^id#[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionId" j
    let to_json = simple_to_json to_value
  end
module SessionName =
  struct
    type nonrec t = string
    let context_ = "SessionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9][a-zA-Z0-9_-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionName" j
    let to_json = simple_to_json to_value
  end
module StreamingStatus =
  struct
    type nonrec t =
      | PENDING_CONFIGURATION 
      | ONGOING 
      | ENDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING_CONFIGURATION -> "PENDING_CONFIGURATION"
      | ONGOING -> "ONGOING"
      | ENDED -> "ENDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING_CONFIGURATION" -> PENDING_CONFIGURATION
      | "ONGOING" -> ONGOING
      | "ENDED" -> ENDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamingStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamingStatus" j)
    let to_json = simple_to_json to_value
  end
module SessionNameOrId =
  struct
    type nonrec t = string
    let context_ = "SessionNameOrId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(id#[a-zA-Z0-9]{22}|[a-zA-Z0-9][a-zA-Z0-9_-]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionNameOrId" j
    let to_json = simple_to_json to_value
  end
module Fraudster =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc "The timestamp when Voice ID identified the fraudster."];
      domainId: DomainId.t option
        [@ocaml.doc
          "The identifier for the domain containing the fraudster."];
      generatedFraudsterId: GeneratedFraudsterId.t option
        [@ocaml.doc "The service-generated identifier for the fraudster."]}
    let make ?createdAt =
      fun ?domainId ->
        fun ?generatedFraudsterId ->
          fun () -> { createdAt; domainId; generatedFraudsterId }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("GeneratedFraudsterId",
          (Option.map x.generatedFraudsterId ~f:GeneratedFraudsterId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let generatedFraudsterId =
        (Option.map ~f:GeneratedFraudsterId.of_xml)
          (Xml.child xml_arg0 "GeneratedFraudsterId") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?generatedFraudsterId ?domainId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let generatedFraudsterId =
        field_map json "GeneratedFraudsterId" GeneratedFraudsterId.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?generatedFraudsterId ?domainId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains all the information about a fraudster."]
module FraudsterId =
  struct
    type nonrec t = string
    let context_ = "FraudsterId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:25) >>=
             (fun () ->
                (check_string_max i ~max:25) >>=
                  (fun () -> check_pattern i ~pattern:"^id#[a-zA-Z0-9]{22}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FraudsterId" j
    let to_json = simple_to_json to_value
  end
module UpdateDomainResponse =
  struct
    type nonrec t =
      {
      domain: Domain.t option [@ocaml.doc "Details about the updated domain"]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?domain = fun () -> { domain }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "Domain") in
      make ?domain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "Domain" Domain.of_json in make ?domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the specified domain. This API has clobber behavior, and clears and replaces all attributes. If an optional field, such as 'Description' is not provided, it is removed from the domain."]
module UpdateDomainRequest =
  struct
    type nonrec t =
      {
      description: Description.t option
        [@ocaml.doc "A brief description about this domain."];
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain to be updated."];
      name: DomainName.t [@ocaml.doc "The name of the domain."];
      serverSideEncryptionConfiguration: ServerSideEncryptionConfiguration.t
        [@ocaml.doc
          "The configuration, containing the KMS Key Identifier, to be used by Voice ID for the server-side encryption of your data. Note that all the existing data in the domain are still encrypted using the existing key, only the data added to domain after updating the key is encrypted using the new key."]}
    let context_ = "UpdateDomainRequest"
    let make ?description =
      fun ~domainId ->
        fun ~name ->
          fun ~serverSideEncryptionConfiguration ->
            fun () ->
              {
                description;
                domainId;
                name;
                serverSideEncryptionConfiguration
              }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:Description.to_value));
        ("DomainId", (Some (DomainId.to_value x.domainId)));
        ("Name", (Some (DomainName.to_value x.name)));
        ("ServerSideEncryptionConfiguration",
          (Some
             (ServerSideEncryptionConfiguration.to_value
                x.serverSideEncryptionConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverSideEncryptionConfiguration =
        ServerSideEncryptionConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ServerSideEncryptionConfiguration") in
      let name =
        DomainName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      make ~serverSideEncryptionConfiguration ~name ~domainId ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let serverSideEncryptionConfiguration =
        field_map_exn json "ServerSideEncryptionConfiguration"
          ServerSideEncryptionConfiguration.of_json in
      let name = field_map_exn json "Name" DomainName.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      let description = field_map json "Description" Description.of_json in
      make ~serverSideEncryptionConfiguration ~name ~domainId ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the specified domain. This API has clobber behavior, and clears and replaces all attributes. If an optional field, such as 'Description' is not provided, it is removed from the domain."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes specified tags from a specified Amazon Connect Voice ID resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Voice ID resource you want to remove tags from."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "The list of tag keys you want to remove from the specified resource."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (AmazonResourceName.to_value x.resourceArn)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json "ResourceArn" AmazonResourceName.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes specified tags from a specified Amazon Connect Voice ID resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tags an Amazon Connect Voice ID resource with the provided list of tags."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Voice ID resource you want to tag."];
      tags: TagList.t
        [@ocaml.doc "The list of tags to assign to the specified resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (AmazonResourceName.to_value x.resourceArn)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resourceArn =
        field_map_exn json "ResourceArn" AmazonResourceName.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tags an Amazon Connect Voice ID resource with the provided list of tags."]
module StartSpeakerEnrollmentJobResponse =
  struct
    type nonrec t =
      {
      job: SpeakerEnrollmentJob.t option
        [@ocaml.doc "Details about the started speaker enrollment job."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?job = fun () -> { job }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Job", (Option.map x.job ~f:SpeakerEnrollmentJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let job =
        (Option.map ~f:SpeakerEnrollmentJob.of_xml)
          (Xml.child xml_arg0 "Job") in
      make ?job ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let job = field_map json "Job" SpeakerEnrollmentJob.of_json in
      make ?job ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new batch speaker enrollment job using specified details."]
module StartSpeakerEnrollmentJobRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientTokenString.t option
        [@ocaml.doc
          "The idempotency token for starting a new speaker enrollment Job. If not provided, Amazon Web Services SDK populates this field."];
      dataAccessRoleArn: IamRoleArn.t
        [@ocaml.doc
          "The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the job output file. Refer to Batch enrollment using audio data from prior calls documentation for the permissions needed in this role."];
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain that contains the speaker enrollment job and in which the speakers are enrolled."];
      enrollmentConfig: EnrollmentConfig.t option
        [@ocaml.doc
          "The enrollment config that contains details such as the action to take when a speaker is already enrolled in the Voice ID system or when a speaker is identified as a fraudster."];
      inputDataConfig: InputDataConfig.t
        [@ocaml.doc
          "The input data config containing the S3 location for the input manifest file that contains the list of speaker enrollment requests."];
      jobName: JobName.t option
        [@ocaml.doc "A name for your speaker enrollment job."];
      outputDataConfig: OutputDataConfig.t
        [@ocaml.doc
          "The output data config containing the S3 location where Voice ID writes the job output file; you must also include a KMS Key ID to encrypt the file."]}
    let context_ = "StartSpeakerEnrollmentJobRequest"
    let make ?clientToken =
      fun ?enrollmentConfig ->
        fun ?jobName ->
          fun ~dataAccessRoleArn ->
            fun ~domainId ->
              fun ~inputDataConfig ->
                fun ~outputDataConfig ->
                  fun () ->
                    {
                      clientToken;
                      enrollmentConfig;
                      jobName;
                      dataAccessRoleArn;
                      domainId;
                      inputDataConfig;
                      outputDataConfig
                    }
    let to_value x =
      structure_to_value
        [("ClientToken",
           (Option.map x.clientToken ~f:ClientTokenString.to_value));
        ("DataAccessRoleArn",
          (Some (IamRoleArn.to_value x.dataAccessRoleArn)));
        ("DomainId", (Some (DomainId.to_value x.domainId)));
        ("EnrollmentConfig",
          (Option.map x.enrollmentConfig ~f:EnrollmentConfig.to_value));
        ("InputDataConfig",
          (Some (InputDataConfig.to_value x.inputDataConfig)));
        ("JobName", (Option.map x.jobName ~f:JobName.to_value));
        ("OutputDataConfig",
          (Some (OutputDataConfig.to_value x.outputDataConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputDataConfig =
        OutputDataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OutputDataConfig") in
      let jobName =
        (Option.map ~f:JobName.of_xml) (Xml.child xml_arg0 "JobName") in
      let inputDataConfig =
        InputDataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InputDataConfig") in
      let enrollmentConfig =
        (Option.map ~f:EnrollmentConfig.of_xml)
          (Xml.child xml_arg0 "EnrollmentConfig") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      let dataAccessRoleArn =
        IamRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DataAccessRoleArn") in
      let clientToken =
        (Option.map ~f:ClientTokenString.of_xml)
          (Xml.child xml_arg0 "ClientToken") in
      make ~outputDataConfig ?jobName ~inputDataConfig ?enrollmentConfig
        ~domainId ~dataAccessRoleArn ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let outputDataConfig =
        field_map_exn json "OutputDataConfig" OutputDataConfig.of_json in
      let jobName = field_map json "JobName" JobName.of_json in
      let inputDataConfig =
        field_map_exn json "InputDataConfig" InputDataConfig.of_json in
      let enrollmentConfig =
        field_map json "EnrollmentConfig" EnrollmentConfig.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      let dataAccessRoleArn =
        field_map_exn json "DataAccessRoleArn" IamRoleArn.of_json in
      let clientToken =
        field_map json "ClientToken" ClientTokenString.of_json in
      make ~outputDataConfig ?jobName ~inputDataConfig ?enrollmentConfig
        ~domainId ~dataAccessRoleArn ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new batch speaker enrollment job using specified details."]
module StartFraudsterRegistrationJobResponse =
  struct
    type nonrec t =
      {
      job: FraudsterRegistrationJob.t option
        [@ocaml.doc "Details about the started fraudster registration job."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?job = fun () -> { job }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Job", (Option.map x.job ~f:FraudsterRegistrationJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let job =
        (Option.map ~f:FraudsterRegistrationJob.of_xml)
          (Xml.child xml_arg0 "Job") in
      make ?job ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let job = field_map json "Job" FraudsterRegistrationJob.of_json in
      make ?job ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new batch fraudster registration job using provided details."]
module StartFraudsterRegistrationJobRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientTokenString.t option
        [@ocaml.doc
          "The idempotency token for starting a new fraudster registration job. If not provided, Amazon Web Services SDK populates this field."];
      dataAccessRoleArn: IamRoleArn.t
        [@ocaml.doc
          "The IAM role Amazon Resource Name (ARN) that grants Voice ID permissions to access customer's buckets to read the input manifest file and write the Job output file. Refer to the Create and edit a fraudster watchlist documentation for the permissions needed in this role."];
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain containing the fraudster registration job and in which the fraudsters are registered."];
      inputDataConfig: InputDataConfig.t
        [@ocaml.doc
          "The input data config containing an S3 URI for the input manifest file that contains the list of fraudster registration requests."];
      jobName: JobName.t option
        [@ocaml.doc "The name of the new fraudster registration job."];
      outputDataConfig: OutputDataConfig.t
        [@ocaml.doc
          "The output data config containing the S3 location where Voice ID writes the job output file; you must also include a KMS Key ID to encrypt the file."];
      registrationConfig: RegistrationConfig.t option
        [@ocaml.doc
          "The registration config containing details such as the action to take when a duplicate fraudster is detected, and the similarity threshold to use for detecting a duplicate fraudster."]}
    let context_ = "StartFraudsterRegistrationJobRequest"
    let make ?clientToken =
      fun ?jobName ->
        fun ?registrationConfig ->
          fun ~dataAccessRoleArn ->
            fun ~domainId ->
              fun ~inputDataConfig ->
                fun ~outputDataConfig ->
                  fun () ->
                    {
                      clientToken;
                      jobName;
                      registrationConfig;
                      dataAccessRoleArn;
                      domainId;
                      inputDataConfig;
                      outputDataConfig
                    }
    let to_value x =
      structure_to_value
        [("ClientToken",
           (Option.map x.clientToken ~f:ClientTokenString.to_value));
        ("DataAccessRoleArn",
          (Some (IamRoleArn.to_value x.dataAccessRoleArn)));
        ("DomainId", (Some (DomainId.to_value x.domainId)));
        ("InputDataConfig",
          (Some (InputDataConfig.to_value x.inputDataConfig)));
        ("JobName", (Option.map x.jobName ~f:JobName.to_value));
        ("OutputDataConfig",
          (Some (OutputDataConfig.to_value x.outputDataConfig)));
        ("RegistrationConfig",
          (Option.map x.registrationConfig ~f:RegistrationConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let registrationConfig =
        (Option.map ~f:RegistrationConfig.of_xml)
          (Xml.child xml_arg0 "RegistrationConfig") in
      let outputDataConfig =
        OutputDataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OutputDataConfig") in
      let jobName =
        (Option.map ~f:JobName.of_xml) (Xml.child xml_arg0 "JobName") in
      let inputDataConfig =
        InputDataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InputDataConfig") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      let dataAccessRoleArn =
        IamRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DataAccessRoleArn") in
      let clientToken =
        (Option.map ~f:ClientTokenString.of_xml)
          (Xml.child xml_arg0 "ClientToken") in
      make ?registrationConfig ~outputDataConfig ?jobName ~inputDataConfig
        ~domainId ~dataAccessRoleArn ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let registrationConfig =
        field_map json "RegistrationConfig" RegistrationConfig.of_json in
      let outputDataConfig =
        field_map_exn json "OutputDataConfig" OutputDataConfig.of_json in
      let jobName = field_map json "JobName" JobName.of_json in
      let inputDataConfig =
        field_map_exn json "InputDataConfig" InputDataConfig.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      let dataAccessRoleArn =
        field_map_exn json "DataAccessRoleArn" IamRoleArn.of_json in
      let clientToken =
        field_map json "ClientToken" ClientTokenString.of_json in
      make ?registrationConfig ~outputDataConfig ?jobName ~inputDataConfig
        ~domainId ~dataAccessRoleArn ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new batch fraudster registration job using provided details."]
module OptOutSpeakerResponse =
  struct
    type nonrec t =
      {
      speaker: Speaker.t option
        [@ocaml.doc "Details about the opted-out speaker."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?speaker = fun () -> { speaker }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Speaker", (Option.map x.speaker ~f:Speaker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let speaker =
        (Option.map ~f:Speaker.of_xml) (Xml.child xml_arg0 "Speaker") in
      make ?speaker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let speaker = field_map json "Speaker" Speaker.of_json in
      make ?speaker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Opts out a speaker from Voice ID system. A speaker can be opted out regardless of whether or not they already exist in the system. If they don't yet exist, a new speaker is created in an opted out state. If they already exist, their existing status is overridden and they are opted out. Enrollment and evaluation authentication requests are rejected for opted out speakers, and opted out speakers have no voice embeddings stored in the system."]
module OptOutSpeakerRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain containing the speaker."];
      speakerId: SpeakerId.t
        [@ocaml.doc "The identifier of the speaker you want opted-out."]}
    let context_ = "OptOutSpeakerRequest"
    let make ~domainId = fun ~speakerId -> fun () -> { domainId; speakerId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("SpeakerId", (Some (SpeakerId.to_value x.speakerId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let speakerId =
        SpeakerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SpeakerId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~speakerId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let speakerId = field_map_exn json "SpeakerId" SpeakerId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~speakerId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Opts out a speaker from Voice ID system. A speaker can be opted out regardless of whether or not they already exist in the system. If they don't yet exist, a new speaker is created in an opted out state. If they already exist, their existing status is overridden and they are opted out. Enrollment and evaluation authentication requests are rejected for opted out speakers, and opted out speakers have no voice embeddings stored in the system."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc
          "The list of tags associated with the specified resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all tags associated with a specified Voice ID resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Voice ID resource for which you want to list the tags."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (AmazonResourceName.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceArn =
        field_map_exn json "ResourceArn" AmazonResourceName.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all tags associated with a specified Voice ID resource."]
module ListSpeakersResponse =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."];
      speakerSummaries: SpeakerSummaries.t option
        [@ocaml.doc
          "A list containing details about each speaker in the Amazon Web Services account."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?speakerSummaries -> fun () -> { nextToken; speakerSummaries }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("SpeakerSummaries",
          (Option.map x.speakerSummaries ~f:SpeakerSummaries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let speakerSummaries =
        (Option.map ~f:SpeakerSummaries.of_xml)
          (Xml.child xml_arg0 "SpeakerSummaries") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?speakerSummaries ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let speakerSummaries =
        field_map json "SpeakerSummaries" SpeakerSummaries.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?speakerSummaries ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all speakers in a specified domain."]
module ListSpeakersRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t [@ocaml.doc "The identifier of the domain."];
      maxResults: MaxResultsForList.t option
        [@ocaml.doc
          "The maximum number of results that are returned per call. You can use NextToken to obtain further pages of results. The default is 100; the maximum allowed page size is also 100."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    let context_ = "ListSpeakersRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~domainId -> fun () -> { maxResults; nextToken; domainId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("MaxResults",
          (Option.map x.maxResults ~f:MaxResultsForList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResultsForList.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ?nextToken ?maxResults ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResultsForList.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ?nextToken ?maxResults ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all speakers in a specified domain."]
module ListSpeakerEnrollmentJobsResponse =
  struct
    type nonrec t =
      {
      jobSummaries: SpeakerEnrollmentJobSummaries.t option
        [@ocaml.doc
          "A list containing details about each specified speaker enrollment job."];
      nextToken: String_.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobSummaries =
      fun ?nextToken -> fun () -> { jobSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobSummaries",
           (Option.map x.jobSummaries
              ~f:SpeakerEnrollmentJobSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let jobSummaries =
        (Option.map ~f:SpeakerEnrollmentJobSummaries.of_xml)
          (Xml.child xml_arg0 "JobSummaries") in
      make ?nextToken ?jobSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let jobSummaries =
        field_map json "JobSummaries" SpeakerEnrollmentJobSummaries.of_json in
      make ?nextToken ?jobSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the speaker enrollment jobs in the domain with the specified JobStatus. If JobStatus is not provided, this lists all jobs with all possible speaker enrollment job statuses."]
module ListSpeakerEnrollmentJobsRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain containing the speaker enrollment jobs."];
      jobStatus: SpeakerEnrollmentJobStatus.t option
        [@ocaml.doc "Provides the status of your speaker enrollment Job."];
      maxResults: MaxResultsForList.t option
        [@ocaml.doc
          "The maximum number of results that are returned per call. You can use NextToken to obtain further pages of results. The default is 100; the maximum allowed page size is also 100."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    let context_ = "ListSpeakerEnrollmentJobsRequest"
    let make ?jobStatus =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~domainId ->
            fun () -> { jobStatus; maxResults; nextToken; domainId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("JobStatus",
          (Option.map x.jobStatus ~f:SpeakerEnrollmentJobStatus.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:MaxResultsForList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResultsForList.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let jobStatus =
        (Option.map ~f:SpeakerEnrollmentJobStatus.of_xml)
          (Xml.child xml_arg0 "JobStatus") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ?nextToken ?maxResults ?jobStatus ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResultsForList.of_json in
      let jobStatus =
        field_map json "JobStatus" SpeakerEnrollmentJobStatus.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ?nextToken ?maxResults ?jobStatus ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the speaker enrollment jobs in the domain with the specified JobStatus. If JobStatus is not provided, this lists all jobs with all possible speaker enrollment job statuses."]
module ListFraudsterRegistrationJobsResponse =
  struct
    type nonrec t =
      {
      jobSummaries: FraudsterRegistrationJobSummaries.t option
        [@ocaml.doc
          "A list containing details about each specified fraudster registration job."];
      nextToken: String_.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobSummaries =
      fun ?nextToken -> fun () -> { jobSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobSummaries",
           (Option.map x.jobSummaries
              ~f:FraudsterRegistrationJobSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let jobSummaries =
        (Option.map ~f:FraudsterRegistrationJobSummaries.of_xml)
          (Xml.child xml_arg0 "JobSummaries") in
      make ?nextToken ?jobSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let jobSummaries =
        field_map json "JobSummaries"
          FraudsterRegistrationJobSummaries.of_json in
      make ?nextToken ?jobSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the fraudster registration jobs in the domain with the given JobStatus. If JobStatus is not provided, this lists all fraudster registration jobs in the given domain."]
module ListFraudsterRegistrationJobsRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain containing the fraudster registration Jobs."];
      jobStatus: FraudsterRegistrationJobStatus.t option
        [@ocaml.doc
          "Provides the status of your fraudster registration job."];
      maxResults: MaxResultsForList.t option
        [@ocaml.doc
          "The maximum number of results that are returned per call. You can use NextToken to obtain further pages of results. The default is 100; the maximum allowed page size is also 100."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    let context_ = "ListFraudsterRegistrationJobsRequest"
    let make ?jobStatus =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~domainId ->
            fun () -> { jobStatus; maxResults; nextToken; domainId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("JobStatus",
          (Option.map x.jobStatus ~f:FraudsterRegistrationJobStatus.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:MaxResultsForList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResultsForList.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let jobStatus =
        (Option.map ~f:FraudsterRegistrationJobStatus.of_xml)
          (Xml.child xml_arg0 "JobStatus") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ?nextToken ?maxResults ?jobStatus ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResultsForList.of_json in
      let jobStatus =
        field_map json "JobStatus" FraudsterRegistrationJobStatus.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ?nextToken ?maxResults ?jobStatus ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the fraudster registration jobs in the domain with the given JobStatus. If JobStatus is not provided, this lists all fraudster registration jobs in the given domain."]
module ListDomainsResponse =
  struct
    type nonrec t =
      {
      domainSummaries: DomainSummaries.t option
        [@ocaml.doc
          "A list containing details about each domain in the Amazon Web Services account."];
      nextToken: String_.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?domainSummaries =
      fun ?nextToken -> fun () -> { domainSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DomainSummaries",
           (Option.map x.domainSummaries ~f:DomainSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let domainSummaries =
        (Option.map ~f:DomainSummaries.of_xml)
          (Xml.child xml_arg0 "DomainSummaries") in
      make ?nextToken ?domainSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let domainSummaries =
        field_map json "DomainSummaries" DomainSummaries.of_json in
      make ?nextToken ?domainSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the domains in the Amazon Web Services account."]
module ListDomainsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResultsForListDomainFe.t option
        [@ocaml.doc
          "The maximum number of results that are returned per call. You can use NextToken to obtain further pages of results. The default is 100; the maximum allowed page size is also 100."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is returned, there are more results available. The value of NextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults",
           (Option.map x.maxResults ~f:MaxResultsForListDomainFe.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResultsForListDomainFe.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults =
        field_map json "MaxResults" MaxResultsForListDomainFe.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the domains in the Amazon Web Services account."]
module EvaluateSessionResponse =
  struct
    type nonrec t =
      {
      authenticationResult: AuthenticationResult.t option
        [@ocaml.doc
          "Details resulting from the authentication process, such as authentication decision and authentication score."];
      domainId: DomainId.t option
        [@ocaml.doc "The identifier of the domain containing the session."];
      fraudDetectionResult: FraudDetectionResult.t option
        [@ocaml.doc
          "Details resulting from the fraud detection process, such as fraud detection decision and risk score."];
      sessionId: SessionId.t option
        [@ocaml.doc "The service-generated identifier of the session."];
      sessionName: SessionName.t option
        [@ocaml.doc "The client-provided name of the session."];
      streamingStatus: StreamingStatus.t option
        [@ocaml.doc
          "The current status of audio streaming for this session. This field is useful to infer next steps when the Authentication or Fraud Detection results are empty or the decision is NOT_ENOUGH_SPEECH. In this situation, if the StreamingStatus is ONGOING/PENDING_CONFIGURATION, it can mean that the client should call the API again later, once Voice ID has enough audio to produce a result. If the decision remains NOT_ENOUGH_SPEECH even after StreamingStatus is ENDED, it means that the previously streamed session did not have enough speech to perform evaluation, and a new streaming session is needed to try again."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?authenticationResult =
      fun ?domainId ->
        fun ?fraudDetectionResult ->
          fun ?sessionId ->
            fun ?sessionName ->
              fun ?streamingStatus ->
                fun () ->
                  {
                    authenticationResult;
                    domainId;
                    fraudDetectionResult;
                    sessionId;
                    sessionName;
                    streamingStatus
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AuthenticationResult",
           (Option.map x.authenticationResult
              ~f:AuthenticationResult.to_value));
        ("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("FraudDetectionResult",
          (Option.map x.fraudDetectionResult ~f:FraudDetectionResult.to_value));
        ("SessionId", (Option.map x.sessionId ~f:SessionId.to_value));
        ("SessionName", (Option.map x.sessionName ~f:SessionName.to_value));
        ("StreamingStatus",
          (Option.map x.streamingStatus ~f:StreamingStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamingStatus =
        (Option.map ~f:StreamingStatus.of_xml)
          (Xml.child xml_arg0 "StreamingStatus") in
      let sessionName =
        (Option.map ~f:SessionName.of_xml) (Xml.child xml_arg0 "SessionName") in
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "SessionId") in
      let fraudDetectionResult =
        (Option.map ~f:FraudDetectionResult.of_xml)
          (Xml.child xml_arg0 "FraudDetectionResult") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      let authenticationResult =
        (Option.map ~f:AuthenticationResult.of_xml)
          (Xml.child xml_arg0 "AuthenticationResult") in
      make ?streamingStatus ?sessionName ?sessionId ?fraudDetectionResult
        ?domainId ?authenticationResult ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamingStatus =
        field_map json "StreamingStatus" StreamingStatus.of_json in
      let sessionName = field_map json "SessionName" SessionName.of_json in
      let sessionId = field_map json "SessionId" SessionId.of_json in
      let fraudDetectionResult =
        field_map json "FraudDetectionResult" FraudDetectionResult.of_json in
      let domainId = field_map json "DomainId" DomainId.of_json in
      let authenticationResult =
        field_map json "AuthenticationResult" AuthenticationResult.of_json in
      make ?streamingStatus ?sessionName ?sessionId ?fraudDetectionResult
        ?domainId ?authenticationResult ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Evaluates a specified session based on audio data accumulated during a streaming Amazon Connect Voice ID call."]
module EvaluateSessionRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain where the session started."];
      sessionNameOrId: SessionNameOrId.t
        [@ocaml.doc
          "The session identifier, or name of the session, that you want to evaluate. In Voice ID integration, this is the Contact-Id."]}
    let context_ = "EvaluateSessionRequest"
    let make ~domainId =
      fun ~sessionNameOrId -> fun () -> { domainId; sessionNameOrId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("SessionNameOrId",
          (Some (SessionNameOrId.to_value x.sessionNameOrId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionNameOrId =
        SessionNameOrId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SessionNameOrId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~sessionNameOrId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let sessionNameOrId =
        field_map_exn json "SessionNameOrId" SessionNameOrId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~sessionNameOrId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Evaluates a specified session based on audio data accumulated during a streaming Amazon Connect Voice ID call."]
module DescribeSpeakerResponse =
  struct
    type nonrec t =
      {
      speaker: Speaker.t option
        [@ocaml.doc "Information about the specified speaker."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?speaker = fun () -> { speaker }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Speaker", (Option.map x.speaker ~f:Speaker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let speaker =
        (Option.map ~f:Speaker.of_xml) (Xml.child xml_arg0 "Speaker") in
      make ?speaker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let speaker = field_map json "Speaker" Speaker.of_json in
      make ?speaker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified speaker."]
module DescribeSpeakerRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain that contains the speaker."];
      speakerId: SpeakerId.t
        [@ocaml.doc "The identifier of the speaker you are describing."]}
    let context_ = "DescribeSpeakerRequest"
    let make ~domainId = fun ~speakerId -> fun () -> { domainId; speakerId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("SpeakerId", (Some (SpeakerId.to_value x.speakerId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let speakerId =
        SpeakerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SpeakerId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~speakerId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let speakerId = field_map_exn json "SpeakerId" SpeakerId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~speakerId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified speaker."]
module DescribeSpeakerEnrollmentJobResponse =
  struct
    type nonrec t =
      {
      job: SpeakerEnrollmentJob.t option
        [@ocaml.doc
          "Contains details about the specified speaker enrollment job."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?job = fun () -> { job }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Job", (Option.map x.job ~f:SpeakerEnrollmentJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let job =
        (Option.map ~f:SpeakerEnrollmentJob.of_xml)
          (Xml.child xml_arg0 "Job") in
      make ?job ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let job = field_map json "Job" SpeakerEnrollmentJob.of_json in
      make ?job ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified speaker enrollment job."]
module DescribeSpeakerEnrollmentJobRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier of the domain containing the speaker enrollment job."];
      jobId: JobId.t
        [@ocaml.doc
          "The identifier of the speaker enrollment job you are describing."]}
    let context_ = "DescribeSpeakerEnrollmentJobRequest"
    let make ~domainId = fun ~jobId -> fun () -> { domainId; jobId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("JobId", (Some (JobId.to_value x.jobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobId =
        JobId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "JobId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~jobId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let jobId = field_map_exn json "JobId" JobId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~jobId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified speaker enrollment job."]
module DescribeFraudsterResponse =
  struct
    type nonrec t =
      {
      fraudster: Fraudster.t option
        [@ocaml.doc "Information about the specified fraudster."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fraudster = fun () -> { fraudster }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Fraudster", (Option.map x.fraudster ~f:Fraudster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fraudster =
        (Option.map ~f:Fraudster.of_xml) (Xml.child xml_arg0 "Fraudster") in
      make ?fraudster ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let fraudster = field_map json "Fraudster" Fraudster.of_json in
      make ?fraudster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified fraudster."]
module DescribeFraudsterRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain containing the fraudster."];
      fraudsterId: FraudsterId.t
        [@ocaml.doc "The identifier of the fraudster you are describing."]}
    let context_ = "DescribeFraudsterRequest"
    let make ~domainId =
      fun ~fraudsterId -> fun () -> { domainId; fraudsterId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("FraudsterId", (Some (FraudsterId.to_value x.fraudsterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fraudsterId =
        FraudsterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FraudsterId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~fraudsterId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let fraudsterId = field_map_exn json "FraudsterId" FraudsterId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~fraudsterId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified fraudster."]
module DescribeFraudsterRegistrationJobResponse =
  struct
    type nonrec t =
      {
      job: FraudsterRegistrationJob.t option
        [@ocaml.doc
          "Contains details about the specified fraudster registration job."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?job = fun () -> { job }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Job", (Option.map x.job ~f:FraudsterRegistrationJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let job =
        (Option.map ~f:FraudsterRegistrationJob.of_xml)
          (Xml.child xml_arg0 "Job") in
      make ?job ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let job = field_map json "Job" FraudsterRegistrationJob.of_json in
      make ?job ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified fraudster registration job."]
module DescribeFraudsterRegistrationJobRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc
          "The identifier for the domain containing the fraudster registration job."];
      jobId: JobId.t
        [@ocaml.doc
          "The identifier for the fraudster registration job you are describing."]}
    let context_ = "DescribeFraudsterRegistrationJobRequest"
    let make ~domainId = fun ~jobId -> fun () -> { domainId; jobId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("JobId", (Some (JobId.to_value x.jobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobId =
        JobId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "JobId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~jobId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let jobId = field_map_exn json "JobId" JobId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~jobId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified fraudster registration job."]
module DescribeDomainResponse =
  struct
    type nonrec t =
      {
      domain: Domain.t option
        [@ocaml.doc "Information about the specified domain."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?domain = fun () -> { domain }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "Domain") in
      make ?domain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "Domain" Domain.of_json in make ?domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified domain."]
module DescribeDomainRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain you are describing."]}
    let context_ = "DescribeDomainRequest"
    let make ~domainId = fun () -> { domainId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified domain."]
module DeleteSpeakerRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain containing the speaker."];
      speakerId: SpeakerId.t
        [@ocaml.doc "The identifier of the speaker you want to delete."]}
    let context_ = "DeleteSpeakerRequest"
    let make ~domainId = fun ~speakerId -> fun () -> { domainId; speakerId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("SpeakerId", (Some (SpeakerId.to_value x.speakerId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let speakerId =
        SpeakerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SpeakerId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~speakerId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let speakerId = field_map_exn json "SpeakerId" SpeakerId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~speakerId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified speaker from the Amazon Connect Voice ID system."]
module DeleteFraudsterRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain containing the fraudster."];
      fraudsterId: FraudsterId.t
        [@ocaml.doc "The identifier of the fraudster you want to delete."]}
    let context_ = "DeleteFraudsterRequest"
    let make ~domainId =
      fun ~fraudsterId -> fun () -> { domainId; fraudsterId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)));
        ("FraudsterId", (Some (FraudsterId.to_value x.fraudsterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fraudsterId =
        FraudsterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FraudsterId") in
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~fraudsterId ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let fraudsterId = field_map_exn json "FraudsterId" FraudsterId.of_json in
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~fraudsterId ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified fraudster from the Amazon Connect Voice ID system."]
module DeleteDomainRequest =
  struct
    type nonrec t =
      {
      domainId: DomainId.t
        [@ocaml.doc "The identifier of the domain you want to delete."]}
    let context_ = "DeleteDomainRequest"
    let make ~domainId = fun () -> { domainId }
    let to_value x =
      structure_to_value
        [("DomainId", (Some (DomainId.to_value x.domainId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainId =
        DomainId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DomainId") in
      make ~domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domainId = field_map_exn json "DomainId" DomainId.of_json in
      make ~domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified domain from the Amazon Connect Voice ID system."]
module CreateDomainResponse =
  struct
    type nonrec t =
      {
      domain: Domain.t option
        [@ocaml.doc "Information about the newly created domain."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?domain = fun () -> { domain }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "Domain") in
      make ?domain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "Domain" Domain.of_json in make ?domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a domain that contains all Amazon Connect Voice ID data, such as speakers, fraudsters, customer audio, and voiceprints."]
module CreateDomainRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientTokenString.t option
        [@ocaml.doc
          "The idempotency token for creating a new domain. If not provided, Amazon Web Services SDK populates this field."];
      description: Description.t option
        [@ocaml.doc "A brief description of this domain."];
      name: DomainName.t [@ocaml.doc "The name of the domain."];
      serverSideEncryptionConfiguration: ServerSideEncryptionConfiguration.t
        [@ocaml.doc
          "The configuration, containing the KMS Key Identifier, to be used by Voice ID for the server-side encryption of your data. Refer to Amazon Connect VoiceID encryption at rest for more details on how the KMS Key is used."];
      tags: TagList.t option
        [@ocaml.doc "A list of tags you want added to the domain."]}
    let context_ = "CreateDomainRequest"
    let make ?clientToken =
      fun ?description ->
        fun ?tags ->
          fun ~name ->
            fun ~serverSideEncryptionConfiguration ->
              fun () ->
                {
                  clientToken;
                  description;
                  tags;
                  name;
                  serverSideEncryptionConfiguration
                }
    let to_value x =
      structure_to_value
        [("ClientToken",
           (Option.map x.clientToken ~f:ClientTokenString.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("Name", (Some (DomainName.to_value x.name)));
        ("ServerSideEncryptionConfiguration",
          (Some
             (ServerSideEncryptionConfiguration.to_value
                x.serverSideEncryptionConfiguration)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let serverSideEncryptionConfiguration =
        ServerSideEncryptionConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ServerSideEncryptionConfiguration") in
      let name =
        DomainName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:ClientTokenString.of_xml)
          (Xml.child xml_arg0 "ClientToken") in
      make ?tags ~serverSideEncryptionConfiguration ~name ?description
        ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let serverSideEncryptionConfiguration =
        field_map_exn json "ServerSideEncryptionConfiguration"
          ServerSideEncryptionConfiguration.of_json in
      let name = field_map_exn json "Name" DomainName.of_json in
      let description = field_map json "Description" Description.of_json in
      let clientToken =
        field_map json "ClientToken" ClientTokenString.of_json in
      make ?tags ~serverSideEncryptionConfiguration ~name ?description
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a domain that contains all Amazon Connect Voice ID data, such as speakers, fraudsters, customer audio, and voiceprints."]
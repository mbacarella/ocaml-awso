(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING DomainName"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (required json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~serverSideEncryptionConfiguration:(Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ()) (Some Values.CreateDomainResponse.to_json)
           (Some Values.CreateDomainResponse.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainId ())
           None None])
let delete_fraudster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fraudsterId =
         flag "fraudster-id" (required string) ~doc:"STRING FraudsterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fraudster
           (Values.DeleteFraudsterRequest.make ~domainId ~fraudsterId ())
           None None])
let delete_speaker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and speakerId =
         flag "speaker-id" (required string) ~doc:"STRING SpeakerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_speaker
           (Values.DeleteSpeakerRequest.make ~domainId ~speakerId ()) None
           None])
let describe_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain
           (Values.DescribeDomainRequest.make ~domainId ())
           (Some Values.DescribeDomainResponse.to_json)
           (Some Values.DescribeDomainResponse.error_to_json)])
let describe_fraudster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fraudsterId =
         flag "fraudster-id" (required string) ~doc:"STRING FraudsterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fraudster
           (Values.DescribeFraudsterRequest.make ~domainId ~fraudsterId ())
           (Some Values.DescribeFraudsterResponse.to_json)
           (Some Values.DescribeFraudsterResponse.error_to_json)])
let describe_fraudster_registration_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fraudster_registration_job
           (Values.DescribeFraudsterRegistrationJobRequest.make ~domainId
              ~jobId ())
           (Some Values.DescribeFraudsterRegistrationJobResponse.to_json)
           (Some
              Values.DescribeFraudsterRegistrationJobResponse.error_to_json)])
let describe_speaker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and speakerId =
         flag "speaker-id" (required string) ~doc:"STRING SpeakerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_speaker
           (Values.DescribeSpeakerRequest.make ~domainId ~speakerId ())
           (Some Values.DescribeSpeakerResponse.to_json)
           (Some Values.DescribeSpeakerResponse.error_to_json)])
let describe_speaker_enrollment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_speaker_enrollment_job
           (Values.DescribeSpeakerEnrollmentJobRequest.make ~domainId ~jobId
              ()) (Some Values.DescribeSpeakerEnrollmentJobResponse.to_json)
           (Some Values.DescribeSpeakerEnrollmentJobResponse.error_to_json)])
let evaluate_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and sessionNameOrId =
         flag "session-name-or-id" (required string)
           ~doc:"STRING SessionNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.evaluate_session
           (Values.EvaluateSessionRequest.make ~domainId ~sessionNameOrId ())
           (Some Values.EvaluateSessionResponse.to_json)
           (Some Values.EvaluateSessionResponse.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsForListDomainFe"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDomainsResponse.to_json)
           (Some Values.ListDomainsResponse.error_to_json)])
let list_fraudster_registration_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobStatus =
         flag "job-status" (optional json_arg)
           ~doc:"JSON FraudsterRegistrationJobStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsForList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fraudster_registration_jobs
           (Values.ListFraudsterRegistrationJobsRequest.make
              ?jobStatus:(Option.map
                            ~f:Values.FraudsterRegistrationJobStatus.of_json
                            jobStatus) ?maxResults ?nextToken ~domainId ())
           (Some Values.ListFraudsterRegistrationJobsResponse.to_json)
           (Some Values.ListFraudsterRegistrationJobsResponse.error_to_json)])
let list_speaker_enrollment_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobStatus =
         flag "job-status" (optional json_arg)
           ~doc:"JSON SpeakerEnrollmentJobStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsForList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_speaker_enrollment_jobs
           (Values.ListSpeakerEnrollmentJobsRequest.make
              ?jobStatus:(Option.map
                            ~f:Values.SpeakerEnrollmentJobStatus.of_json
                            jobStatus) ?maxResults ?nextToken ~domainId ())
           (Some Values.ListSpeakerEnrollmentJobsResponse.to_json)
           (Some Values.ListSpeakerEnrollmentJobsResponse.error_to_json)])
let list_speakers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsForList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_speakers
           (Values.ListSpeakersRequest.make ?maxResults ?nextToken ~domainId
              ()) (Some Values.ListSpeakersResponse.to_json)
           (Some Values.ListSpeakersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let opt_out_speaker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and speakerId =
         flag "speaker-id" (required string) ~doc:"STRING SpeakerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.opt_out_speaker
           (Values.OptOutSpeakerRequest.make ~domainId ~speakerId ())
           (Some Values.OptOutSpeakerResponse.to_json)
           (Some Values.OptOutSpeakerResponse.error_to_json)])
let start_fraudster_registration_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and registrationConfig =
         flag "registration-config" (optional json_arg)
           ~doc:"JSON RegistrationConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_fraudster_registration_job
           (Values.StartFraudsterRegistrationJobRequest.make ?clientToken
              ?jobName
              ?registrationConfig:(Option.map
                                     ~f:Values.RegistrationConfig.of_json
                                     registrationConfig) ~dataAccessRoleArn
              ~domainId
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.StartFraudsterRegistrationJobResponse.to_json)
           (Some Values.StartFraudsterRegistrationJobResponse.error_to_json)])
let start_speaker_enrollment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and enrollmentConfig =
         flag "enrollment-config" (optional json_arg)
           ~doc:"JSON EnrollmentConfig"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_speaker_enrollment_job
           (Values.StartSpeakerEnrollmentJobRequest.make ?clientToken
              ?enrollmentConfig:(Option.map
                                   ~f:Values.EnrollmentConfig.of_json
                                   enrollmentConfig) ?jobName
              ~dataAccessRoleArn ~domainId
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.StartSpeakerEnrollmentJobResponse.to_json)
           (Some Values.StartSpeakerEnrollmentJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING DomainName"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (required json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain
           (Values.UpdateDomainRequest.make ?description ~domainId ~name
              ~serverSideEncryptionConfiguration:(Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ()) (Some Values.UpdateDomainResponse.to_json)
           (Some Values.UpdateDomainResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-domain", create_domain);
    ("delete-domain", delete_domain);
    ("delete-fraudster", delete_fraudster);
    ("delete-speaker", delete_speaker);
    ("describe-domain", describe_domain);
    ("describe-fraudster", describe_fraudster);
    ("describe-fraudster-registration-job",
      describe_fraudster_registration_job);
    ("describe-speaker", describe_speaker);
    ("describe-speaker-enrollment-job", describe_speaker_enrollment_job);
    ("evaluate-session", evaluate_session);
    ("list-domains", list_domains);
    ("list-fraudster-registration-jobs", list_fraudster_registration_jobs);
    ("list-speaker-enrollment-jobs", list_speaker_enrollment_jobs);
    ("list-speakers", list_speakers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("opt-out-speaker", opt_out_speaker);
    ("start-fraudster-registration-job", start_fraudster_registration_job);
    ("start-speaker-enrollment-job", start_speaker_enrollment_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-domain", update_domain)]
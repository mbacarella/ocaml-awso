(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let complete_attachment_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentIds =
         flag "attachment-ids" (required json_arg)
           ~doc:"JSON AttachmentIdList"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING NonEmptyClientToken"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_attachment_upload
           (Values.CompleteAttachmentUploadRequest.make
              ~attachmentIds:(Values.AttachmentIdList.of_json attachmentIds)
              ~clientToken ~connectionToken ())
           (Some Values.CompleteAttachmentUploadResponse.to_json)
           (Some Values.CompleteAttachmentUploadResponse.error_to_json)])
let create_participant_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectParticipant =
         flag "connect-participant" (optional bool) ~doc:"BOOL Bool"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ConnectionTypeList"
       and participantToken =
         flag "participant-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_participant_connection
           (Values.CreateParticipantConnectionRequest.make
              ?connectParticipant
              ~type_:(Values.ConnectionTypeList.of_json type_)
              ~participantToken ())
           (Some Values.CreateParticipantConnectionResponse.to_json)
           (Some Values.CreateParticipantConnectionResponse.error_to_json)])
let disconnect_participant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disconnect_participant
           (Values.DisconnectParticipantRequest.make ?clientToken
              ~connectionToken ())
           (Some Values.DisconnectParticipantResponse.to_json)
           (Some Values.DisconnectParticipantResponse.error_to_json)])
let get_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING ArtifactId"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_attachment
           (Values.GetAttachmentRequest.make ~attachmentId ~connectionToken
              ()) (Some Values.GetAttachmentResponse.to_json)
           (Some Values.GetAttachmentResponse.error_to_json)])
let get_transcript =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactId =
         flag "contact-id" (optional string) ~doc:"STRING ContactId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and scanDirection =
         flag "scan-direction" (optional json_arg) ~doc:"JSON ScanDirection"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortKey"
       and startPosition =
         flag "start-position" (optional json_arg) ~doc:"JSON StartPosition"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transcript
           (Values.GetTranscriptRequest.make ?contactId ?maxResults
              ?nextToken
              ?scanDirection:(Option.map ~f:Values.ScanDirection.of_json
                                scanDirection)
              ?sortOrder:(Option.map ~f:Values.SortKey.of_json sortOrder)
              ?startPosition:(Option.map ~f:Values.StartPosition.of_json
                                startPosition) ~connectionToken ())
           (Some Values.GetTranscriptResponse.to_json)
           (Some Values.GetTranscriptResponse.error_to_json)])
let send_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and content =
         flag "content" (optional string) ~doc:"STRING ChatContent"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING ChatContentType"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_event
           (Values.SendEventRequest.make ?content ?clientToken ~contentType
              ~connectionToken ()) (Some Values.SendEventResponse.to_json)
           (Some Values.SendEventResponse.error_to_json)])
let send_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING ChatContentType"
       and content =
         flag "content" (required string) ~doc:"STRING ChatContent"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_message
           (Values.SendMessageRequest.make ?clientToken ~contentType ~content
              ~connectionToken ()) (Some Values.SendMessageResponse.to_json)
           (Some Values.SendMessageResponse.error_to_json)])
let start_attachment_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING ContentType"
       and attachmentSizeInBytes =
         flag "attachment-size-in-bytes" (required json_arg)
           ~doc:"JSON AttachmentSizeInBytes"
       and attachmentName =
         flag "attachment-name" (required string)
           ~doc:"STRING AttachmentName"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING NonEmptyClientToken"
       and connectionToken =
         flag "connection-token" (required string)
           ~doc:"STRING ParticipantToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_attachment_upload
           (Values.StartAttachmentUploadRequest.make ~contentType
              ~attachmentSizeInBytes:(Values.AttachmentSizeInBytes.of_json
                                        attachmentSizeInBytes)
              ~attachmentName ~clientToken ~connectionToken ())
           (Some Values.StartAttachmentUploadResponse.to_json)
           (Some Values.StartAttachmentUploadResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("complete-attachment-upload", complete_attachment_upload);
    ("create-participant-connection", create_participant_connection);
    ("disconnect-participant", disconnect_participant);
    ("get-attachment", get_attachment);
    ("get-transcript", get_transcript);
    ("send-event", send_event);
    ("send-message", send_message);
    ("start-attachment-upload", start_attachment_upload)]